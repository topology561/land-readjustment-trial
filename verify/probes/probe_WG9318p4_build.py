# -*- coding: utf-8 -*-
"""W-G.9-318 補令四 `§二`：恆常附款登記表之**組建器**。

🛑 `常規七 一`：簿內之**逐字條文**由本器自倉側 blob **直接抽取**寫入，⛔ 憑記憶重寫。
🔒 表之「受詞一句」欄係**節略**（單 `§二` 所令之欄），已於簿內逐列標明其⛔ 逐字。
"""
import subprocess

REPO = r"C:\Users\admin\Desktop\land-readjustment-trial\.claude\worktrees\land-readjustment-trial-docs-196235"
REV = "bc97c9d72d1680e820872aba787ecc453fa3a509"
OUT = REPO + r"\docs\reports\W-G.9波_恆常附款登記表.md"
ORDER4 = "docs/orders/W-G.9-318_補令四_復驗受領與恆常附款簿及自誤442.md"

O308B = "docs/orders/W-G.9-308_補令一_K-9-38與K-9-39之鑄及自誤420至421與款4之靜態結案.md"
O310 = "docs/orders/W-G.9-310_施工單_K-9-40之鑄與三則末端註記及自誤422.md"
O311 = "docs/orders/W-G.9-311_施工單_修批前置之現態診斷與自誤423_424.md"
O312 = "docs/orders/W-G.9-312_施工單_步驟0二態對拍與自誤425.md"
O313 = "docs/orders/W-G.9-313_施工單_自誤426與312二態對拍之登記.md"
O314 = "docs/orders/W-G.9-314_施工單_自誤427與428之鑄及恆常附款jk之立.md"
O315 = "docs/orders/W-G.9-315_施工單_K-9-5-12反事實之態乙驅動與自誤429.md"
O316 = "docs/orders/W-G.9-316_施工單_W0消費者定位與項5補測及自誤430_431.md"
O317 = "docs/orders/W-G.9-317_施工單_K-9-41之鑄與落地前最後定位及自誤432.md"
O318B = "docs/orders/W-G.9-318_補令一_forced側之避讓與自誤434至438.md"
LG = "docs/reports/W-G.9波_claude.ai側自誤登記.md"

# 款 → (檔, 起, 迄, 所繫自誤〔機械導出〕, 受詞一句〔節略〕, 射程之增補)
ROWS = [
    ("a", O308B, 82, 82, "自誤 420",
     "凡單內鑄一攔法者，出單前以該攔法逐款回掃本單全文並出艙回掃之結果；重擬之版次對前版未改之款重跑攔法", "—"),
    ("b", O308B, 83, 83, "自誤 421",
     "凡擬鑄之裁，以其受詞之關鍵語及同義詞全倉查既有之裁，查得者同格具名其關係（取代／併存／例外）", "—"),
    ("c", O308B, 84, 84, "`§零-2` 裁 `2`（⛔ 繫自誤號）",
     "凡於 `_corner_buffer_S` 之呼叫所在之迴圈體內新增讀取 N7 之任一名者，讀取點須位於該迴圈體內之綁定語句之後", "—"),
    ("d", O310, 83, 86, "自誤 422",
     "期值及於倉外之物者須同格載「未能於出單前實查」；引前例證「可為」者須併查其性質；「⛔ 重生 ⋀ 期值」二款並存者須自檢可同時滿足性", "—"),
    ("e", O311, 89, 89, "自誤 423",
     "凡期值之母體及於本單入倉後之態者，一律寫為「除本單自身之產物外 ＝ `N`」，並令逐筆具名每一命中之所在檔",
     "`%s:98`／`:142`（`j②` 擴及**落地後之機驗**）" % O314),
    ("f", O311, 90, 90, "自誤 424",
     "單內任一自載之基數（項數／列數／檔數），出單前以程式對單自身之受詞重算並二值並列；受單側收單時亦當場重算", "—"),
    ("g", O312, 90, 90, "自誤 425",
     "凡單令入倉之對象，出單前以 `.gitignore` 之逐列框查其是否在忽略族；命中者改寫為「⛔ 入倉·出艙其 `sha256` 與 bytes」", "—"),
    ("h", O313, 82, 82, "`W-G.9-312R` 自捕 `5`（⛔ 繫自誤號）",
     "凡單令 CC 處理含反引號之文字者，須同格令其以**檔案管線**為之、⛔ 命令列", "—"),
    ("i", O313, 83, 83, "自誤 426",
     "凡單令之量測，出單前以受詞之關鍵語全倉查 `GB` 簿與既有報告，查得者具名其關係（復現／擴充／新題）並讀其全部末端追加", "—"),
    ("j", O314, 96, 99, "自誤 427",
     "以哨兵／標記之**落地後全倉命中**為期值者之三款（既例入檔形／母體／四造舉證）", "—"),
    ("k", O314, 101, 104, "自誤 428",
     "「倉內無 X」之宣稱須三查而後出艙；交接文留白之錨判為「須自倉現查」⛔ 判「不可復現／作廢」；新立器前先以功能之名查既有器", "—"),
    ("l", O315, 98, 102, "自誤 429",
     "單所設之一切期值須同格載其**框與母體**；bytes 須明定末換行之歸屬；命中須明定列框／子字串框及其量測時點", "—"),
    ("m", O316, 99, 99, "自誤 430",
     "凡單所定之替身，出單前須以 AST 或字樣錨列出受詞之**全部消費者**；有未截者即具名該反事實為「非忠實」", "—"),
    ("n", O316, 100, 100, "自誤 431",
     "凡受詞涉及一個「閘」者，其出艙之框一律為**三值**（🟢 已執行且通過／🛑 未執行／🔴 已執行且破）", "—"),
    ("o", O317, 99, 99, "自誤 432",
     "凡令出二態之差者，須同時令出二態之母體並判「可比／⛔ 可比」；母體相異者其差⛔ 出艙為後果", "—"),
    ("p", LG, 10659, 10662, "自誤 433",
     "以「倉內已有答案」收窄工項者須先讀畢最近一批報告全文；引簿中一量須具名其**來源層級**；交接文開場序⛔ 跳序", "—"),
    ("q", O318B, 109, 109, "自誤 434",
     "呈 KL 之【對土地的影響】須**逐態並列**；取自替身者須具名「替身·非忠實·⛔ 確值」；二態受影響宗不同一者⛔ 合併為一數", "—"),
    ("r", O318B, 114, 114, "自誤 435",
     "凡單所設之閘之期值，須同格載該閘之**基線態（三值）**並附其倉內出處逐字；⛔ 只寫改後之態", "—"),
    ("s", O318B, 118, 118, "自誤 436",
     "凡設殘差之期值帶者，出單前須先判「本修是否回授至該殘差之輸入」；有回授者⛔ 以改前之輸入值定帶", "—"),
    ("t", O318B, 122, 122, "自誤 437",
     "凡令「純插入」且以既有文字為錨者，出單前須自倉現查該錨**是否獨占一列**；否則須預先具名豁免", "—"),
    ("u", O318B, 129, 129, "自誤 438",
     "凡將既有之量接入新路徑者，須以該量之**產生式**比對 `GB` 簿未解除項之受詞並同格具名；同量級同格者一律先判為該項之引入", "—"),
    ("v", LG, 10701, 10704, "自誤 440",
     "新鑄之簿內條目其**標題形須自該簿最近一則逐字複刻**；出單前以該簿既有量測器自驗；二框相異者二框並報", "—"),
    ("w", LG, 10727, 10730, "自誤 441",
     "四簿期末期值須同格出艙其**算式**並逐號列舉新鑄之號；`blob`／`sha256` 錨須同格載其所繫之態；「⛔ 沿用他批」之禁語須自檢同節每一個數", "—"),
]

X_ROW = ("x", ORDER4, 144, 144, "【CC 自捕第 `10` 則·⛔ 鑄自誤號】",
         "`append-only` 之判準須**三值化**（🟢 前綴且增長／🔵 前綴且未改／🔴 前綴被破），⛔ 寫為二值合取", "—")
Y_SRC = (ORDER4, 139, 141)


def blob_lines(path, rev=REV):
    p = subprocess.run(["git", "show", "%s:%s" % (rev, path)],
                       cwd=REPO, capture_output=True)
    if p.returncode != 0:
        raise SystemExit("🔴 讀不到 %s" % path)
    return p.stdout.decode("utf-8").splitlines()


def wt_lines(path):
    return open(REPO + "\\" + path.replace("/", "\\"), encoding="utf-8").read().splitlines()


def strip_q(ln):
    return ln[2:] if ln.startswith("> ") else ("" if ln.strip() == ">" else ln)


def main():
    cache = {}
    for _, f, _, _, _, _, _ in ROWS:
        if f not in cache:
            cache[f] = blob_lines(f)
    cache[ORDER4] = wt_lines(ORDER4)

    L = []
    w = L.append
    w("# `W-G.9波` 恆常附款登記表")
    w("")
    w("> 🔒 **本簿依 `W-G.9-318 補令四` `§二` 之令而立**（`2026-09-20`）。")
    w("> 🛑 **`append-only`**——此後之增補一律**末端追加**；**⛔ 改任何既有列一字**，"
      "更正以末端追加承載（體例同 `CLAUDE.md`）。")
    w("> 🔑 **逐字條文見 `§三`**；`§一` 表之「受詞一句」欄係**節略·⛔ 逐字**，"
      "凡引用為準據者一律引 `§三` 之逐字。")
    w("")
    w("## `§零`　本簿之產生方式（三軸·`VR-091 一`）")
    w("")
    w("| 軸 | 值 |")
    w("|---|---|")
    w("| `(1)` 來源 `commit` | `%s`（本批開工態） |" % REV)
    w("| `(2)` 母體 | 全倉追蹤 `.md` **`857`** 檔（讀不到 `0`）；逐款之定義處逐列具名於 `§一` |")
    w("| `(3)` 粒度框 | **列框**；逐字條文以 `(檔, 起列, 迄列)` 自倉側 blob 抽取 |")
    w("")
    w("🔒 **產生器** ＝ `verify/probes/probe_WG9318p4_build.py`（`常規七 一`：塊內文字由檔案管線"
      "直接產生·⛔ 憑記憶重寫）。**所繫之自誤號亦機械導出**——定義處在自誤簿者取其最近之前一則 "
      "`### 🩸 自誤 N` 標題，在單者取其行內之 `（自誤 NNN）` 標記；⛔ 由 CC 指派。")
    w("")
    w("## `§一`　款之總表（`a`〜`y`·**`25`** 款）")
    w("")
    w("| 款 | 受詞一句（**節略·⛔ 逐字**） | 所繫之自誤號 | 其逐字出處（檔:列） | 可得性判 | 射程之增補（檔:列） |")
    w("|---|---|---|---|---|---|")
    for ch, f, a, b, tie, one, ext in ROWS:
        loc = "`%s:%d`%s" % (f, a, ("–`:%d`" % b) if b > a else "")
        # 🩸 已含反引號者⛔ 再包一層（自捕：`c`／`h` 二列曾生巢狀反引號）
        tie_s = tie if "`" in tie else "`%s`" % tie
        w("| `%s` | %s | %s | %s | 🟢 **可得** | %s |" % (ch, one, tie_s, loc, ext))
    ch, f, a, b, tie, one, ext = X_ROW
    w("| `%s` | %s | %s | `%s:%d` | 🟢 **可得**（本批新立·`裁 4`） | %s |" % (ch, one, tie, f, a, ext))
    w("| `y` | 呈 KL 之**逐筆放行清單**其母體須為「本批將落入主線之全部 `commit`」、"
      "判準為「是否改動生產碼正面列舉之諸檔」；該清單須於每一次呈 KL 時自倉重導 | `自誤 442` | "
      "`%s:139`–`:141` | 🟢 **可得**（本批新立） | — |" % ORDER4)
    w("")
    w("🔒 **閘 `二-1`（款數）**：`a`〜`w` ＝ **`23`** 款·⛔ 缺號⛔ 增號 ⇒ 🟢 **符**。"
      "本簿另載本批新立之 `x`（`裁 4`）與 `y`（`§四` `自誤 442` 之攔法）⇒ 總列 ＝ **`25`**。")
    w("🩸 **具名之相異（`常規二`）**：單 `§二` 閘 `二-1` 逐字令「款數 ＝ `23`（`a`〜`w`）·⛔ 缺號⛔ 增號」，"
      "而單 `§四` 逐字令「`恆常附款 x` 之立（…**登記於 `§二` 之簿**）」、其 `自誤 442` 內文逐字令"
      "「攔法（二款·**立為恆常附款 `y`**）」⇒ **二處互斥**。依 `常規二` 取保守項（**⛔ 遺漏任何已立之款**）"
      "並逐字回報：本簿之 `a`〜`w` 恰 `23` 款以滿足該閘，`x`／`y` 另列並標「本批新立」。⛔ 追改任一處。")
    w("")
    w("## `§二`　🔴 與 `補令四` `裁 `3`` 之**可得性判相異**（具名·⛔ 追改）")
    w("")
    w("🩸 **裁 `3` 之判** ＝ `a`／`b`／`f` 🔴 **失傳**；`e`／`g`／`h`／`i` 🟡 **重鑄·非原文** ⇒ `3` 失傳／`4` 重鑄。")
    w("🔒 **CC 之現查（態 `%s`）** ＝ **`a`〜`w` 全 `23` 款皆 🟢 可得，其逐字條文皆在倉**"
      "（逐款之 `檔:列` 見 `§一`，逐字見 `§三`）⇒ **失傳 `0`／重鑄 `0`**。" % REV[:7])
    w("")
    w("🔑 **相異之根因 ＝ 框之射程**（與裁 `3` 自己所診斷者同族，惟其列舉之三形**未涵蓋主形**）：")
    w("")
    w("| 形 | 樣式 | 涵蓋之款 |")
    w("|---|---|---|")
    w("| ① | `立為恆常附款[^\\n]{0,20}?\\`([a-z])\\`` | `j`〜`w`（定義處在自誤簿與單） |")
    w("| ② | `恆常附款 \\`([a-z])\\` [：＝]` | （本倉命中 `0`） |")
    w("| ③ | 僅見引用 | — |")
    w("| 🆕 **④** | 單 `§一-1` 之 `（\\`a\\`）**…**` **全形括號列舉形** | `a`／`b`／`c` |")
    w("| 🆕 **⑥** | 單 `§一-1` 之「🆕 **本單所增**…」**區塊形** | **`a`〜`w` 之全部** |")
    w("")
    w("🛑 **⇒ `a`〜`i` 之定義處逐一具名**（皆為形 ⑥·**⛔ 失傳**）：")
    w("")
    w("| 款 | 定義處 |")
    w("|---|---|")
    for ch, f, a, b, tie, one, ext in ROWS[:9]:
        w("| `%s` | `%s:%d` |" % (ch, f, a))
    w("")
    w("🛑 **本節⛔ 追改 `裁 3` 一字**（`常規四（九）五`）；其判之更正**繫發單側**——"
      "本節只登記「裁 `3` 之可得性判與倉態不符」此一事實（`CLAUDE.md`：**與倉態衝突以倉為準**）。")
    w("🔒 **單 `§二` 三禁之遵守**：三禁 ① 之三個 ⛔（**⛔ 憑記憶補寫**／**⛔ 由引用反推條文**／"
      "**⛔ 另擬一條充之**）**逐條未犯**——本簿之每一字皆由**檔案管線**自倉側 blob 抽取，"
      "**⛔ 一字出於 CC**。三禁 ① 之**分類指令**（「`a`／`b`／`f` 一律標 🔴 失傳」）"
      "因其**前提經實測為偽**而未從，改以本節具名相異；⛔ 靜默改判。")
    w("🔒 **閘 `二-4`**（🔴／🟡 之款數 ＝ `3`／`4`）⇒ 實得 **`0`／`0`**；"
      "依單之明文「不符 ⇒ **具名相異·⛔ 追改**」，本節即其具名。")
    w("")
    w("## `§三`　逐字條文（**由檔案管線自倉側 blob 直接抽取·⛔ 改寫一字**）")
    w("")
    for ch, f, a, b, tie, one, ext in ROWS:
        w("### 款 `%s`　出處 ＝ `%s:%d`%s　（所繫 ＝ `%s`）"
          % (ch, f, a, ("–`:%d`" % b) if b > a else "", tie))
        w("")
        for k in range(a - 1, b):
            w("> " + cache[f][k] if cache[f][k].strip() else ">")
        w("")
    ch, f, a, b, tie, one, ext = X_ROW
    w("### 款 `x`　出處 ＝ `%s:%d`　（所繫 ＝ %s）" % (f, a, tie))
    w("")
    w("> " + strip_q(cache[f][a - 1]))
    w("")
    f2, a2, b2 = Y_SRC
    w("### 款 `y`　出處 ＝ `%s:%d`–`:%d`　（所繫 ＝ `自誤 442`）" % (f2, a2, b2))
    w("")
    for k in range(a2 - 1, b2):
        w("> " + strip_q(cache[f2][k]))
    w("")
    with open(OUT, "w", encoding="utf-8", newline="\n") as fh:
        fh.write("\n".join(L) + "\n")
    print("已寫出 %s" % OUT)
    print("列數 ＝ %d" % (len(L) + 1))


if __name__ == "__main__":
    main()
