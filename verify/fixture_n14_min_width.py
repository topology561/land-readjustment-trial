# -*- coding: utf-8 -*-
"""W-G.5 N-14／N-15／E 系列夾具 — **宗地最小寬度**（KL 裁 2026-07-26／E 系列定稿 2026-07-27）。

## N-14 定義（畸零地使用規則 §4③）
最小寬度＝**自 FRONTLINE 起算、深度至法定最小深度為止**之帶內，
**二側境界線間平行 FRONTLINE 之距離**之**最小值**。

## 🔒 可達宗地形狀集合（KL 裁·**封閉**·夾具母體以此為準·禁再自造形狀）
| # | 形狀 | 邊序 |
|---|---|---|
| ① | 街角地第 1 宗 | FRONT → **截角** → SIDE → BASELINE → ALLOC（五邊形·含截角） |
| ② | 中間宗地（第 2 宗起） | FRONT → ALLOC → BASELINE → ALLOC（**兩側 ALLOC 平行**） |
| ③ | 末端塊 | FRONT → **街廓界(R–R)** → BASELINE → ALLOC |

**本案實測角度**（KL 由 V6.dxf 坐實·寫入正典但**禁硬編為常數**）：
`∠(ALLOC, FRONT)` **84.706°～87.395°**（**非**手冊示意圖之 90°）；
`∠(SIDE, ALLOC)` **0.000°～4.322°**（側街線與宗地分配線**不平行**）。
⇒ 幾何推論：
· ② 兩側平行 ⇒ 平行 FRONT 之弦**恆等長** ⇒ ②之最小寬度 **≡ 該宗臨街寬度**。
· ①③ 非平行四邊形 ⇒ 寬度**隨深度變化**（4.322° 於 14m 深內差約 1.05m，
  足以翻越 3.5m 門檻）⇒ ①③ **必須**跑真掃描，**禁以臨街寬度代替**。

## 🔒 E-1′～E-10 不變量守衛（量測前先驗·一律 loud raise·**禁回任何數字**）
形狀集合封閉 ⇒ 下列依作業規範**不可能發生**，發生即上游有誤。
- **E-1′** `abs(min(t)) > 0.01`（**雙向**）⇒ 本宗未臨接正面路街線（⛔ 禁改為夾近端後續算）。
  容差 `0.01m` ＝ **法定粒度**（實施辦法 §3 長度 2dp·**KL 域裁·已鎖**），
  **非** KL 交辦文原字面之 `1e-6`（該值實測會誤咬 0m 25 筆／3.5m 23 筆正確宗地）。
  **雙向**係 claude.ai 判定之施工完整性要求（純技術·非 KL 裁示）。
- **E-2′** `max(t) < min_depth` ⇒ 分配線深度不合規（手冊 二(一)3）。
  ⛔ 舊之 `_t_hi = min(min_depth, max(t))` 夾制**已撤**——它會**靜默吸收不合規分配線**。
  （其「尖刺可越過本判準」之缺口由 **E-8a** 根除。）
- **E-3′** 帶內每斷點之弦須存在。
  ⛔ 舊之 `_ln.length` 總長語意**已撤**——多段加總會**製造一個不存在的寬度**。
  附註：「U 字形宗地」之讀法選項經 KL 裁定**作廢**（該形狀不可能存在）⇒ **禁實作讀法選項**。
- **E-4** `buffer(0)` 後非單一 `Polygon` ⇒ loud raise。
- **E-8a 凸性**：`convex_hull.area − area > 0.01㎡`（面積之法定粒度）⇒ loud raise。
  可達形狀集合①②③**全為凸多邊形**。一擊同滅尖刺／U 形／多段弦。
  🔒 **適用域＝原位次宗形**；E 世代之 `_fuse(cut, wedge)` 未必凸，**不得外推**。
- **E-8b 單點接觸**：斷點處弦長 ≤ **數值零 1e-9**（非法定門檻）⇒ loud raise。
  ⚠️ **不得改用 0.01m**——本案存在**真實 5.87mm 臨街邊**（R6 `628-53(1)`）。
- **E-9 量測起點 ＝ 前緣邊之上緣**（**邊基**·非頂點門檻基）。
  舊式 `max(0, min(t))` 取全宗最低頂點 ⇒ 前緣近平行時只切到一角、
  回 0.03m 級薄片（實測 R1 `628-35(2)` 回 0.0327、真臨街 18.1027）。
  無任何前緣**邊** ⇒ 單點接觸 ⇒ loud raise。
- **E-10 弦長解析求**（禁 `LineString ∩ Polygon`）。TWD97 量級（~3.1e5）下 GEOS
  少算至 **1.08e-2 m**（＞法定粒度）；僅平移原點**不夠**（仍差 1.6e-4）。
  改於 `(s,t)` 局部框以邊之線性內插求交點，弦 ＝ `max(s) − min(s)`。

## 期望值一律**手算**（`fixture-provenance`：禁由新碼現跑回填）

## 重跑
    python verify/fixture_n14_min_width.py        # rc=0 綠／rc=1 紅
"""
import math
import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
sys.path.insert(0, HERE)

from app_harvest import harvest                                     # noqa: E402

MD = 14.0          # 法定最小深度（住宅區×8m 路寬查表值·本夾具之量測帶上界）
DEEP = 40.0        # 合成宗地深度（> MD ⇒ 滿足 E-2′）


def _mid_parcel(front_w, alloc_deg, depth=DEEP, base_deg=0.0):
    """② 中間宗地：FRONT 在 y=0（x∈[0, front_w]），兩側 ALLOC **平行**（傾角 alloc_deg），
    BASELINE 與 FRONT 夾 base_deg。回頂點序（逆時針）。"""
    _sx = math.tan(math.radians(alloc_deg))          # 每單位深度之側向位移（兩側同 ⇒ 平行）
    _by = lambda x: depth + x * math.tan(math.radians(base_deg))   # noqa: E731
    _xl_top = 0.0 + _by(0.0) * _sx
    _xr_top = front_w + _by(front_w) * _sx
    return [(0.0, 0.0), (front_w, 0.0),
            (_xr_top, _by(front_w)), (_xl_top, _by(0.0)), (0.0, 0.0)]


def main():
    for _st in (sys.stdout, sys.stderr):
        try:
            _st.reconfigure(encoding="utf-8")
        except Exception:
            pass
    ns, _ = harvest()
    f = ns.get("parcel_min_width_n14")
    if not callable(f):
        print("🔴 harvest 未取得 parcel_min_width_n14")
        return 1
    out, ok = [], True

    def _chk(desc, geom, exp, md=MD, d=(1, 0), fp=(0, 0)):
        nonlocal ok
        got = f(geom, d, fp, md, _label="fx")
        hit = abs(got - exp) < 1e-6
        ok &= hit
        out.append(f"  {desc:<42} → {got:.6f}（手算 {exp}）　{'✅' if hit else '🔴'}")

    # ── ②中間宗地：三格皆須 ≡ 臨街寬度 S（證「兩側平行 ⇒ 弦恆等長」）──────────────
    S = 5.0
    _chk("① ②中間宗地·ALLOC ⊥ FRONT（傾 0°）", _mid_parcel(S, 0.0), S)
    _chk("② ②中間宗地·ALLOC 傾 5°（近本案實況）", _mid_parcel(S, 5.0), S)
    _chk("③ ②中間宗地·BASELINE 微斜 0.6°", _mid_parcel(S, 5.0, base_deg=0.6), S)

    # ── ①街角地：截角後 vs 截角前（**併存＝判別力反例**）───────────────────────
    #   手算：前寬 8、截角腿 4。
    #   截角前＝矩形 x∈[0,8] ⇒ 帶內弦恆 8.0。
    #   截角後＝切掉右下角之 (8,0)-(4,0)-(8,4) 三角形 ⇒
    #     w(t) = 8 − (4 − t)  於 t∈[0,4]（截角斜邊 x = 8−(4−t)）；t≥4 起恆 8
    #     ⇒ min 落 t=0：w(0) = 8 − 4 = **4.0**
    CHAM_AFTER = [(0.0, 0.0), (4.0, 0.0), (8.0, 4.0), (8.0, DEEP), (0.0, DEEP), (0.0, 0.0)]
    CHAM_BEFORE = [(0.0, 0.0), (8.0, 0.0), (8.0, DEEP), (0.0, DEEP), (0.0, 0.0)]
    _chk("④ ①街角地·**截角後**（前寬8·截角腿4）", CHAM_AFTER, 4.0)
    _chk("⑤ ①街角地·**截角前**（同上）", CHAM_BEFORE, 8.0)
    out.append("      ↑ ④⑤ 併存＝**判別力反例**：同一宗地截角前後差 2 倍"
               "⇒ E-7「街角宗須以截角前幾何量」非空話")

    # ── ⑥①街角地·SIDE 與 ALLOC 夾 4.322°（本案實測上界）───────────────────────
    #   手算：左界為 ALLOC 垂直線 x=0；右界為 SIDE，自 (6,0) 起向**內**傾 4.322°
    #     ⇒ 右界 x(t) = 6 − t·tan(4.322°)；w(t) = 6 − t·tan(4.322°) 遞減
    #     ⇒ min 落**帶之深端** t=MD=14：w = 6 − 14×tan(4.322°)
    _TAN = math.tan(math.radians(4.322))
    W6 = 6.0 - MD * _TAN
    SIDE_TILT = [(0.0, 0.0), (6.0, 0.0),
                 (6.0 - DEEP * _TAN, DEEP), (0.0, DEEP), (0.0, 0.0)]
    _chk(f"⑥ ①街角地·SIDE×ALLOC 4.322°（min 落深端）", SIDE_TILT, W6)
    out.append(f"      ↑ 手算 6 − 14×tan(4.322°) = {W6:.6f}"
               f"；臨街寬 6.0 ⇒ **差 {6.0 - W6:.4f}m·足以翻越 3.5m 門檻** ⇒ ①③ 必須真掃描")

    # ── ⑦③末端塊·街廓界(R–R) 斜率 ≠ ALLOC ────────────────────────────────────
    #   手算：左界 ALLOC 垂直 x=0；右界為街廓界，自 (5,0) 起**外**傾 3° ⇒ w 遞增
    #     ⇒ min 落 t=0 ＝ 臨街寬 **5.0**（外傾者最小值在近端·與⑥反向·證掃描非單點）
    _T3 = math.tan(math.radians(3.0))
    END_BLK = [(0.0, 0.0), (5.0, 0.0),
               (5.0 + DEEP * _T3, DEEP), (0.0, DEEP), (0.0, 0.0)]
    _chk("⑦ ③末端塊·街廓界外傾 3°（min 落近端）", END_BLK, 5.0)

    # ── ⑧ 不變量格：一律 loud raise ──────────────────────────────────────────
    #   🆕 **鑑別力補格**（claude.ai 2026-07-27）：原 ⑧a 用偏移 **2.0m**
    #      ⇒ 對「容差 1e-6 vs 0.01」**零鑑別力**（兩者皆 raise）。
    #      補 ⑧a′-1／⑧a′-2 **跨容差兩格** ＋ ⑧a″ **負向**（雙向守衛之唯一鑑別格）。
    def _shift_front(off):
        """把整宗沿 t 平移 `off`（前緣邊仍為單一水平邊）。"""
        return [(0.0, off), (5.0, off), (5.0, DEEP + off), (0.0, DEEP + off), (0.0, off)]

    _NOT_TOUCH = _shift_front(2.0)
    _TOO_SHALLOW = _mid_parcel(5.0, 0.0, depth=MD - 1.0)
    # ⑪ 菱形：前緣退化為**單點**（無前緣邊）⇒ E-9 之單點接觸 raise
    _RHOMBUS = [(0.0, 0.0), (3.0, 20.0), (0.0, 40.0), (-3.0, 20.0), (0.0, 0.0)]
    # ⑪′ 三角形：前緣邊正常，但**深端於 t=MD 收斂為一點** ⇒ E-8b 弦長 0 raise
    _APEX = [(0.0, 0.0), (5.0, 0.0), (2.5, MD), (0.0, 0.0)]
    # ⑫ 尖刺（非凸）：本體僅深 13 < MD，靠一根尖刺到 15 **越過 E-2′** ⇒ 必須由 E-8a 攔
    _SPIKE = [(0.0, 0.0), (5.0, 0.0), (5.0, 13.0), (2.6, 13.0),
              (2.55, 15.0), (2.45, 15.0), (2.4, 13.0), (0.0, 13.0), (0.0, 0.0)]
    # ⑭ 前緣**微凹口**：凹口面積 ½×0.2×0.02 = **0.002㎡ < E-8a 之 0.01㎡** ⇒ **越過凸性閘**；
    #   而凹口頂 t=0.02 > 容差 0.01 ⇒ 前緣邊被切成 **[0,2.4] 與 [2.6,5] 兩段**
    #   ⇒ 唯有 **E-9 連續性斷言**能咬（否則 `_t_lo` 取全體 max 會橫跨非臨街段）。
    _NOTCH = [(0.0, 0.0), (2.4, 0.0), (2.5, 0.02), (2.6, 0.0), (5.0, 0.0),
              (5.0, DEEP), (0.0, DEEP), (0.0, 0.0)]
    for desc, args in (
        ("⑧a E-1′ 未臨接（偏移 +2.0m·粗例）", (_NOT_TOUCH, (1, 0), (0, 0), MD)),
        ("⑧a′-2 E-1′ 偏移 +0.011（容差外·**1e-6 無鑑別**）",
         (_shift_front(0.011), (1, 0), (0, 0), MD)),
        ("⑧a″ E-1′ 偏移 **−0.011**（負向·單向守衛會放行）",
         (_shift_front(-0.011), (1, 0), (0, 0), MD)),
        ("⑧b E-2′ 深度 < 法定最小深", (_TOO_SHALLOW, (1, 0), (0, 0), MD)),
        ("⑧c E-4 座標<3", ([(0, 0), (1, 1)], (1, 0), (0, 0), MD)),
        ("⑧d min_depth≤0", (_mid_parcel(5.0, 0.0), (1, 0), (0, 0), 0.0)),
        ("⑧e d_hat 退化", (_mid_parcel(5.0, 0.0), (0, 0), (0, 0), MD)),
        ("⑪ E-9 前緣退化為單點（菱形）", (_RHOMBUS, (1, 0), (0, 0), MD)),
        ("⑪′ E-8b 深端收斂單點（弦長 0）", (_APEX, (1, 0), (0, 0), MD)),
        ("⑫ E-8a 尖刺非凸（**已越過 E-2′**）", (_SPIKE, (1, 0), (0, 0), MD)),
        ("⑭ E-9 前緣邊分裂兩段（**已越過 E-8a**）", (_NOTCH, (1, 0), (0, 0), MD)),
    ):
        try:
            f(*args, _label="fx")
            ok = False
            out.append(f"  {desc:<42} → 🔴 未 raise（上游錯誤被靜默吸收）")
        except RuntimeError:
            out.append(f"  {desc:<42} → ✅ loud raise")

    # ── 🆕 容差**內**側之對照格（證本閘不誤咬·⑧a′-2 之孿生）─────────────────────
    _chk("⑧a′-1 E-1′ 偏移 +0.009（容差內·須**不** raise）", _shift_front(0.009), 5.0)
    # ── 🆕 ⑩ 前緣邊被中間頂點切成**兩段共線**（舊碼此處 `line∩poly` 回 MultiLineString
    #      ⇒ 走退避法；E-10 解析式下**直接**得 5.0·退避法已整段廢除）───────────────
    _chk("⑩ 前緣兩段共線（舊碼之退避法路徑）",
         [(0.0, 0.0), (2.5, 0.0), (5.0, 0.0), (5.0, DEEP), (0.0, DEEP), (0.0, 0.0)], 5.0)
    # ── 🆕 ⑬ E-9 迴歸格：前緣邊微傾（兩端 t 差 5e-5·**本案實況之合成版**）──────────
    #      舊式 `_t_lo = max(0, min(t))` 於此回**薄片**；E-9 之邊基起點回**臨街寬**。
    _chk("⑬ E-9 前緣微傾 5e-5（舊式回薄片）",
         [(0.0, 0.0), (5.0, 5e-5), (5.0, DEEP), (0.0, DEEP), (0.0, 0.0)], 5.0)

    # ── 舊格保留（末端塊型梯形）：底5→頂1@t=20·md=14 ⇒ w(14)=5−4(14/20)=2.2 ──────
    _chk("⑨ ③末端塊型梯形 底5→頂1@t=20（舊格保留）",
         [(0, 0), (5, 0), (3, 20), (2, 20), (0, 0)], 2.2)
    out.append("      ↑ 舊「梯形超深 md=25 → 1.0」格**已改為 E-2′ raise**（見 ⑧b 同族）")

    # ══ 🆕 `baseline_pts` 分支（K-9-6-b 新帶底·K-6-A2 段五(c)-2）══════════════════
    #   ⚠️ 上方**每一格**皆走 `baseline_pts=None`（舊帶·附表 `MD`）⇒ 對新分支**零覆蓋**，
    #     而**切換後生產走的正是新分支**。本段補之。
    #   🔒 上方既有格之 pin 值**一律不動**——本段**只加不改**。
    #
    #   帶底之定式（正典 `grep -n "^#### K-9-6-b " docs/rulings/K-6_*.md`）：
    #     **後緣連續段之全部端點取 min**（⛔ 非「二個角點」·實測端點數分布 `[2, 3]`）。
    #
    #   ── 共用構形（**手算友善**）：左界 `x=0` 垂直；右界自 `(6,0)` 直連後緣右端 `(Rx, Ry)`
    #      ⇒ `x_r(t) = 6 − (6−Rx)·t/Ry` **遞減** ⇒ 弦 `w(t) = x_r(t)` 之 min 落**帶底**。
    #      取帶底 `t*` ⇒ `w = 6 − (6−Rx)·t*/Ry`。
    def _bl(px, py, deg):
        """BASELINE 之二端點（過 `(px,py)`、傾角 `deg`）——⛔ 不呼叫 app 之合成器，
        本夾具**自備獨立構造**（其正確性由下列手算值反過來把關）。"""
        _c = math.cos(math.radians(deg)); _s = math.sin(math.radians(deg))
        return [(px - 100.0 * _c, py - 100.0 * _s), (px + 100.0 * _c, py + 100.0 * _s)]

    def _chk_bl(desc, geom, exp, bl, md=MD):
        nonlocal ok
        got = f(geom, (1, 0), (0, 0), md, _label="fx", baseline_pts=bl)
        hit = abs(got - exp) < 1e-6
        ok &= hit
        out.append(f"  {desc:<42} → {got:.6f}（手算 {exp:.6f}）　{'✅' if hit else '🔴'}")

    _RY_A = 30.0 + 4.0 * math.tan(math.radians(-2.0))   # 後緣右端之 t（BASELINE 斜 −2°）
    #   ⑮ BASELINE 微斜 −2° ⇒ 後緣二端 t ＝ {30.0（左）, _RY_A（右·**較淺**）}
    #      ⇒ 帶底 ＝ `_RY_A`；`x_r(_RY_A)` 恰為右端頂點 `x = 4.0` ⇒ w ＝ **4.0**
    _chk_bl("⑮ 新帶·BASELINE 斜 −2°（帶底落**較淺之右端**）",
            [(0.0, 0.0), (6.0, 0.0), (4.0, _RY_A), (0.0, 30.0), (0.0, 0.0)],
            4.0, _bl(0.0, 30.0, -2.0))
    #   ⑯ BASELINE ∥ FRONT（`g = 0`）⇒ 後緣二端 t **相等**（皆 30）⇒ 帶底 30
    #      右界 `x_r(t) = 6 − 3t/30` ⇒ w(30) ＝ **3.0**
    _chk_bl("⑯ 新帶·BASELINE ∥ FRONT（二後緣 t 相等·g=0）",
            [(0.0, 0.0), (6.0, 0.0), (3.0, 30.0), (0.0, 30.0), (0.0, 0.0)],
            3.0, _bl(0.0, 30.0, 0.0))
    #   ⑰ 後緣端點 **3 個**（共線冗餘點·對應實測分布 `[2, 3]`）
    #      右界 `x_r(t) = 6 − 2t/30` ⇒ w(30) ＝ **4.0**
    _chk_bl("⑰ 新帶·後緣端點 3 個（共線冗餘·分布 [2,3]）",
            [(0.0, 0.0), (6.0, 0.0), (4.0, 30.0), (2.0, 30.0), (0.0, 30.0), (0.0, 0.0)],
            4.0, _bl(0.0, 30.0, 0.0))
    #   ⑱ **判別力對照**：同一宗地、同一 BASELINE，舊帶（`MD=14`）與新帶（抵 30）**不同值**
    #      舊：w(14) ＝ 6 − 3×14/30 ＝ **4.6**；新：w(30) ＝ **3.0**（＝⑯）
    _chk("⑱ 判別力對照·**舊帶**同一宗地（w(14)＝6−3×14/30）",
         [(0.0, 0.0), (6.0, 0.0), (3.0, 30.0), (0.0, 30.0), (0.0, 0.0)], 4.6)
    out.append("      ↑ ⑯⑱ 併存＝**新舊帶之判別力反例**：同宗地 3.0 vs 4.6"
               "⇒ `baseline_pts` 確實改變量測帶（非擺設）")

    # ── ⑲ 新分支之 loud raise 格 ────────────────────────────────────────────────
    _NO_REAR = [(0.0, 0.0), (5.0, 0.0), (5.0, 20.0), (0.0, 20.0), (0.0, 0.0)]
    for desc, geom, bl in (
        ("⑲a 後緣連續段缺件（宗地未達 BASELINE）", _NO_REAR, _bl(0.0, 30.0, 0.0)),
        ("⑲b baseline_pts 退化為點", _mid_parcel(5.0, 0.0), [(0.0, 30.0), (0.0, 30.0)]),
        ("⑲c baseline_pts 缺件（僅 1 點）", _mid_parcel(5.0, 0.0), [(0.0, 30.0)]),
    ):
        try:
            f(geom, (1, 0), (0, 0), MD, _label="fx", baseline_pts=bl)
            ok = False
            out.append(f"  {desc:<42} → 🔴 未 raise（新分支之缺件被靜默吸收）")
        except RuntimeError:
            out.append(f"  {desc:<42} → ✅ loud raise")
    out.append("      ↑ ⑲ 三格皆為**新分支**專屬（舊分支根本不看 BASELINE）")

    print("=" * 104)
    print("【N-14／E-5 夾具】宗地最小寬度·母體＝可達形狀集合（①街角／②中間／③末端）")
    print("=" * 104)
    for ln in out:
        print(ln)
    print("-" * 104)
    print(f"RESULT: {'PASS' if ok else 'FAIL'}")
    print("=" * 104)
    return 0 if ok else 1


if __name__ == "__main__":
    sys.exit(main())
