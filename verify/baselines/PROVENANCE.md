# baselines 出處（PROVENANCE）— v1

> KL 2026-07-05 裁示：把 baseline 權威性口述封成檔案；日後 baselines v2 同法記載。

## 產出資訊

- **產出者**：KL（Streamlit UI 實跑，非 harness 生成——與 driver 非同源，對拍有效）。
- **產出日期**：2026-07-04（10 檔 CSV 檔案時戳 16:14–16:16）。
- **當時 app.py commit**：`db14034`（W-D.1.3-d 清理＋固化波，2026-07-04 08:11 +0800；
  為產出時 HEAD——其後至 W-V 全程 additive-only，app.py 未再變動）。
- **輸入檔**：`data/V6.dxf`（已掃淨）＋ `data/地籍資料來源_匿名版.xlsx`
  （KL 以 `anonymize_cadastre.py` 產、`匿名化驗證報告.md` 佐證；21,476 群同構）。

## UI 參數清單（＝快照 `case_params_UC9898.json` 內容；KL lock 2026-07-04）

- 全區：分區＝住宅區、法定最小寬 3.5m／最小深 14m、重劃區總面積 34,950㎡、
  C%＝8.96、重劃總負擔率＝0.40（Tab7 未跑之 fallback、baseline 反解；真值待財務接線波）。
- 退縮雙情境：0m ／ 3.5m（`f3L_setback_default`）。
- R1、R4 正面路寬＝**12m 既成道路**（正街尺度 0；系統預設 8m 之覆寫）；
  其餘街廓正面 8m 新闢（尺度 2）；有 SIDE_LINE 側 8m 尺度 1。
- 各街廓分配深度／截角／街角最小面積雙情境值：見快照 `blocks.*`。
- 法定最小寬 3.5＝住宅區×(12m/8m) 查畸零地附表**恰同檔**——巧合非常數
  （doctrine v2；新案例須重查表）。

## 檔案清單（10 CSV）

| 檔 | 對拍者 |
|---|---|
| 退縮0m參數.csv／退縮3.5m參數.csv | 參數表（截角/街角最小面積） |
| W-D.1.3-a 診斷_退縮0m/3.5m.csv | 截角拓樸定錨診斷 |
| W-D.1.2 診斷_退縮0m/3.5m.csv | 逐候選三分項診斷 |
| 第 1 宗街角地指配結果_退縮0m/3.5m.csv | 指配表 |
| W-D.1.3-d 驗收_退縮3.5m.csv | 抵費地（0m 無檔＝應空，harness 驗證之） |
| V6dxf_各街廓鄰街地負擔.csv | 快照來源（harness 不對拍） |

## 保險（非必需）

KL 日後若順路開 app，抽對一次 R1左／R2左 指配數字即可外部錨定；不需專程重跑。
