# W-G.9-255　`c1` 停機之撤回 ＋ `K-9-29` 補款 `十`（跨區段合併之許可）＋ 自誤 `317`／`318` ＋ `c1`／`c2` 續辦

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。承 `W-G.9-254`（工項零＋一已推 `92e4244`；`c1` 停機 `70a676d`）。
> 🛑 **本單撤回 `c1` 之停機**——其三環中**環一、環二經發單側實測為偽**，環三係 KL 已答之題。
> 🔒 `-254` `c2`（廢步驟 0）與 `-253` 段乙（`-251`）**原令全效**，本單⛔ 動其一字。

---

## §零　開工閘

1. `git fetch`；錨 ＝ **`origin/wip/s1-endpart @ 70a676d`**。不符 ⇒ 停機。
2. 號占用（宣告框 `①`–`⑦`·三數分列 ＋ 雙屬列數）：發單側於 `70a676d` 實測 `W-G.9-255` ＝ `D1 0`／`D2 0`／`D3 0`／雙屬 `0`；對照甲 `-254` ＝ `D1 3`／`D2 6`／`D3 0`／檔框 `5`。CC 當場復現。
3. 次號當場復算：發單側實測 自誤 `MAX 316`／缺號 `[106]`／恆等式 `True` ⇒ 鑄 **`317`**／**`318`**。
4. `probe_order_preflight.py`：`P-3`／`P-5` 綠。
5. worktree 須先切 **wip 態**（`GB-152`）。

---

## §一　工項零：本單原封入倉 `docs/orders/`（`cp`·⛔ 重打）。`P-5` 對拍。

---

## §二　工項一：`c1` 停機三環之處置（**零生產碼**·記於報告·⛔ 鑄 CC 側號）

發單側於 `70a676d` 逐一復現，判如下。**CC 之停機行為本身⛔ 有過**——停機是正確的紀律；錯的是其中二環之**框**。

### 環一「逐筆重劃前地價於全鏈結構上不存在」— 🔴 **判：偽（框錯）**

🔑 **KL 逐字（`2026-09-08`）**：「目前程式把『同區段 ⇒ 同單價』寫死在資料結構裡。**這個沒有錯**，要修正的是在 UI 點選指定地價區段前，程式已將各重劃前同建築街廓同歸戶相鄰土地做合併了，導致點選時會以合併前地籍範圍及地號為該指定地價代碼，就不是各重劃前地號土地均有其重劃前地價了。」

⇒ **`K-9-29 一`「逐筆」之受詞 ＝ 每一原地號各自被指定一個區段碼**，⛔ 「每一原地號各有一個獨立價格欄」。
⇒ 逐筆價 ＝ `pre_price_by_zone[ zone_of[原地號] ]`，**現行結構即足**。環一所查之十二組同義詞（`原地號_單價` 等）**本就不該存在** ⇒ 其命中 `0` **⛔ 構成不可得之證**。
🔒 **本則之責⛔ 全歸 CC**——`-254` 單 `§四 c1` 由發單側書「`p` ＝ 重劃前地價（元/㎡）逐筆」而未定義「逐筆」之受詞，係**單之瑕**。見自誤 `317`。

### 環二「`wf_f0` 內⛔ 有任何地價資料」— 🔴 **判：偽（框錯·CC 側）**

**發單側實測（`blob@70a676d`／`verify/wf_f0.py` ＋ `verify/case_params_UC9898.json` 二檔／列框）**：
`wf_f0:199` 已逐字讀 `snap["財務接線_v3"]["原地號_區段"]`；**同一 dict 之鄰鍵** `"重劃前區段_面積單價"` ＝ `{"a": {"單價_元每m2": 43909.2308486259, …}, "b": {"單價_元每m2": 37322.846221332016, …}}`。
⇒ **價一直在手上**，取之⛔ 須擴充任何資料源。
🩸 **真因**：以**變數名** `pre_price_by_zone` 為框 grep，而快照側之鍵名為 `重劃前區段_面積單價`——**縮寫／異名致 regex 盲**（既登記之族）。⛔ 以「命中 `0`」證不可得，須改以**受詞可達性**為框。
🔒 **`_transform` 之簽章**：`_decide(ns, snap, …)` 已收 `snap`；`_transform(build_parcels, decisions)` 未收，其呼叫點 `:288` 在 `compute()` 內、**該作用域有 `snap`** ⇒ **一行傳參即足**，屬 `-254 §四 c1` 射程內（受詞未變、母體未變）。**發單側裁：在射程內，⛔ 另單。**

### 環三「`停機#2` 使 `a′≠a` 不可達」— 🟢 **判：成立**

其閘逐字括號「（A 折算係數≠1，須域裁 7-4 雙模式）」正指本題。**KL 已於 `2026-09-08` 裁畢** ⇒ 見 `§三`。

### 附帶：CC 所指「單載行號差 `2` 列」— 🔴 **判：偽（CC 之量測器紅）**

`-254 §四 c1` 載受詞 `verify/wf_f0.py:241`–`:246`。發單側實測（`blob@70a676d`）：`add = 0.0` ＠ `:241`／`add +=` ＠ `:243`–`:244`／`t["面積_m2"]` 寫入 ＠ `:246` ⇒ **單載逐位正確**。CC 所報之「`:239`–`:242`／`:244`」為誤。
🔒 **令 CC 於本批報告<u>具名撤回</u>該項**，⛔ 追改 `-254R` 既出一字（`常規四（九）五`）。

---

## §三　工項二：`K-9-29` 之**補款 `十`**（`docs/rulings/K-6_…md` **檔末**追加·⛔ 動 `K-9-29` 原節一字）

節題形取既有 `🔧 …之補款` 節為範本。🛑 **本節⛔ 鑄任何號**（其為 `K-9-29` 之補款）。

> ## 🔧 `K-9-29` 之補款 `十`：**同歸戶相鄰之跨地價區段合併<u>准予為之</u>，以 `a′` 折算**（KL 裁 `2026-09-08`·`W-G.9-255`·⛔ 上文原句一字不刪·純末端追加）
>
> 🩸 **案由**　`verify/wf_f0.py:272`–`:276` 之 `停機#2` 閘（`d["target"] and len(d["zones"]) != 1 ⇒ RuntimeError`），其逐字括號「（**A 折算係數≠1，須域裁 7-4 雙模式**）」即在候本裁。`K-9-29 三` 已令「一切合併恆走 `a′`」，惟**未明言跨區段合併是否<u>准予</u>** ⇒ 本款補之。
>
> > **KL 逐字（`2026-09-08`）**：「可以 ⇒ 拆掉 `停機#2`，以 `a′ = a × p(自身) / p(標的)` 折算後併入（會動配地面積）。」
>
> 🔒 **十（跨區段合併之許可）**
> 同街廓同歸戶相鄰之數宗，**⛔ 因分屬不同重劃前地價區段而不得合併**。`停機#2` 閘**廢止**。
> 合併一律以 **`a′ = a × p(自身) / p(標的)`** 折算後併入，`p` ＝ **該宗原地號所屬區段之重劃前地價（元/㎡）**（`K-9-29 一` 之「逐筆」＝ **每一原地號各自被指定一個區段碼**，⛔ 每一原地號各有獨立價格欄）。
> 🛑 **`p ≤ 0` ⇒ `raise`**；⛔ 靜默退 `1.0`、⛔ 靜默退 `a′ = a`。
> 🔒 **標的宗**沿用 `K-9-29 四`（`G` 最大；並列取暫編字典序最小）；**位次**沿用 `K-9-29 五`（投影序在前者·前遞）。
> 🔴 **本款有土地後果**（KL 逐字「會動配地面積」）⇒ 其碼側落地逐 commit 候 KL 放行。
> 🔒 **⛔ 及者**：⛔ 動 `停機#3`（被併宗 ∩ 街角 winners／forced）一字；⛔ 動 `K-9-27`／`K-9-28`。

---

## §四　工項三：自誤 `317`／`318`（**責 ＝ 發單側**·append-only·標題形取簿最末一則為範本）

**`317`　令 CC 取「逐筆地價」而未定義其受詞，致其以「逐筆價格欄」為框而停機（`受詞未定義` 族）**
受詞：`W-G.9-254 §四 c1` 逐字「`p` ＝ **重劃前地價（元/㎡）逐筆**（`K-9-29 一`）」。`K-9-29 一` 之「逐筆」實指**逐筆各自被指定區段碼**（KL `2026-09-08` 逐字），而單未書其受詞 ⇒ CC 讀為「逐筆價格欄」、窮查十二組同義詞得 `0`、據以停機。
加重：`K-9-29 一` 係發單側自書之條文形（KL 僅裁「對」）⇒ 其歧義由發單側所生；且發單側於 `-254` 呈 KL 時**亦以同一誤框**斷言「乙案：先做逐筆地價之資料層（UI 步驟 D 改逐筆）」，係**對 KL 出艙之誤導**（KL 逐字更正之）。
通則：凡於單中令 CC 取某量，其**粒度之受詞**須以「＝ 何物之何鍵」具名，⛔ 以形容詞（逐筆／逐宗／逐級）代之。

**`318`　以截圖為證斷言「倉內快照已與 KL 實際標註脫節」而未查倉（`proxy vs property` 族）**
受詞：發單側於 `2026-09-08` 對 KL 出艙「倉內快照已與你的實際標註脫節……該判斷現已失效，請不要再拿它當依據」，據 ＝ KL 所貼截圖之三區段（`a`／`e`／`b`·`70%`）。KL 逐字更正：「我上傳截圖只是本次為了向你說明示意給你看而已，**與快照無涉**。」
加重：發單側於同一回合已 `clone` 在手，`verify/case_params_UC9898.json` 一次 `cat-file` 即可證其未動；且「脫節」係**否定存在之斷言**，依既規須窮查給證，而發單側以**單一畫面**代之。
通則：凡斷言「倉內某物已失效／已脫節」，其證須來自**倉側 blob 對拍**，⛔ 以 UI 畫面、聊天引述或推斷代之；截圖僅得作**待驗之線索**。

---

## §五　工項四：🔴 **`c1` 復工**（生產碼·側分支 `verify/W-G.9-255-c1`·候 KL 放行）

受詞 ＝ `verify/wf_f0.py`（`:22` docstring／`:241`–`:246` `_transform`／`:272`–`:276` `停機#2`／`:288` 呼叫點）。

a. `_transform(build_parcels, decisions)` → `_transform(build_parcels, decisions, snap)`；`:288` 呼叫點傳 `c` 之 `snap`（**⛔ 新增任何資料源**）。
b. `p_of(原地號) = float(snap["財務接線_v3"]["重劃前區段_面積單價"][ snap["財務接線_v3"]["原地號_區段"][原地號] ]["單價_元每m2"])`。**`p ≤ 0` 或鍵缺 ⇒ `raise`**（⛔ `.get(..., 0)` 靜默）。
c. `add` 改：`Σ_m ( a_m × p_of(m) / p_of(target) )`，`a_m` 之取法**一字不改**（仍 `分攤登記面積_m2 + 面積_m2`）。
d. **廢 `停機#2`**（`:272`–`:276`）依 `K-9-29 十`；**`停機#3` 一字不動**。廢除處留逐字註：`# 🛑 經 K-9-29 十（KL 裁 2026-09-08）廢止；跨區段改以 a′ 折算。`
e. `:22` docstring 之「合併＝標的宗.面積_m2 += Σ(被併宗 a)」同批更正為 `a′` 式。
🔒 **⛔ 及者**：⛔ 改 `_decide` 之 `_key`（標的宗仍 `G` 最大／並列取字典序最小·`K-9-29 四`）；⛔ 改 `zones` 之蒐集（其仍須產出，供 `V-G′` 之分流）。

### `c1` 之預期與閘

🔑 **發單側於 `70a676d` 實測**：步驟 0 仍在之現態下，`F.0` 六組級0／0′ 之成員**跨區段組數 ＝ `0`**（快照 `a`／`b`·單價 `43909.23`／`37322.85`）⇒ `p(被併) = p(標的)` ⇒ `a′ = a`。

| 閘 | 判準 | 停機款 |
|---|---|---|
| `V-G` | `f0`／`f2`／`f3`／`f4` × 雙情境之 `G 值`／`合併決策` CSV **逐欄逐值 `Δ = 0`** | 🔴 |
| `V-G′` | 判別力三組（人造測資·⛔ 省）：① `p(被併) ≠ p(標的)` ⇒ `a′ ≠ a` 且該值**與手算相符**；② `p = 0` ⇒ `raise`；③ 舊 `停機#2` 之觸發條件（跨區段）⇒ **⛔ 再 raise、改走折算** | 🔴 |
| `V-A` | 十一檔中除 `wf_f0.py` 外逐位不變（`app.py` ＝ `4860f10828fb8ae9`） | 🔴 |

🛑 **`V-G` 非零 ⇒ 停機上呈**，⛔ 自行調和。
🛑 **`V-G′` 三組任一綠 ⇒ 器無鑑別力、⛔ 得出艙**（`-249R` `B-2` 之教訓：閘全綠而受詞從未被測到）。

---

## §六　工項五：`c2` 續辦（依 `-254 §四 c2` 原令·**⛔ 動一字**）

🔒 **序**：`c1` **先於** `c2`。理由：`c1` 於現態為零差（可證），`c2` 落地後合併群改變、**跨區段組可能出現**（屆時 `a′ ≠ a`）⇒ 先落 `c1` 可使 `c2` 之 `V-H` 差異純由「廢合併」所生，⛔ 與折算混源。
🆕 **`c2` 之 `V-H` 增一欄**：逐組具名其成員之區段集與 `a′`／`a` 之比，供辨「該差是廢合併所生抑或折算所生」。

---

## §七　工項六：`-253` 段乙（`-251` `c1`／`c2`）——依原令，**⛔ 受本單影響**。

---

## 【驗收】

| 閘 | 判準 |
|---|---|
| `V-B` | append-only：`numstat` deletions 全 `0`（`K-6`／自誤簿／`W-G.4`） |
| `V-C` | 自誤 `MAX 316→318`、缺號仍 `[106]`、恆等式 `True`；`K-9` 主序列 `MAX` **仍 `29`**（本單⛔ 鑄 `K-9` 號） |
| `V-L` | `K-9-29 十` 落於**檔末**（⛔ 節末）；`K-9-29` 原節 `blob` 之前綴逐位不變 |
| `V-M` | `-254R` 之「行號差 `2` 列」已於本批報告**具名撤回**，且 `-254R` 檔 blob **未動** |
| `V-G`／`V-G′`／`V-A` | 見 `§五` |
| `V-H`／`V-I`／`V-J` | 見 `-254 §四 c2` ＋ 本單 `§六` 增欄 |
| `V-K` | `-251` 段乙：KL 目檢 |

---

## §八　產出與放行

| 批 | 內容 | 放行 |
|---|---|---|
| 工項零〜四 | 入倉／環一〜三之處置／`K-9-29 十`／自誤 `317`　`318` | **零生產碼 ⇒ 直推主線** |
| `c1` | `wf_f0` `a′` ＋ 廢 `停機#2` | 側分支；`V-G`／`V-G′` 全綠 ⇒ 呈 KL 一句：「本批於本案逐值零差；跨區段之折算路徑已開且經三組對照證其有鑑別力」 |
| `c2` | 廢步驟 0 | 側分支；`V-H` 差異表 ＋ 一句實務語言 ⇒ 候放行 |
| `-251` 段乙 | 同 `-253 §六` | 側分支；`V-K` 後候放行 |

🛑 `c1`／`c2`／`-251` 三者**⛔ 混同 commit**；`c1` 未放行前 `c2` ⛔ 開工（`§六` 之序）。

---

## §九　待落地登記（本單登記·⛔ 辦）

| 項 | 依賴 |
|---|---|
| **UI 步驟 D 逐宗點選之驗證**（`c2` 後 `628(1)+` 應散回逐宗·KL 目檢畫面） | `c2` 併線後·**KL 目檢**（`V-K` 同批） |
| `-249` 段三觀測**重跑** | `c2` 併線後 |
| `a′` 模式二（`p_average`·跨街廓無同歸戶地） | 五級射程 |
| `K-9-29 六` 入池閘之五級層 | 五級 ⬜ |
| `GB-67`／telescoping | 未解·⛔ 阻塞本單（用 A／B 活體差） |

---

*受詞 ＝ CC。工項零〜四直推；`c1` → `c2` 依序，各候 KL 放行；`-251` 段乙並行。*

SELF_SHA256: 72e929ebe5c3ac2ba1c9f20a2dd1a7931df79530592528118e6a8f5b837f326f
