# 📋 `W-G.9-191`　發單側交接文（claude.ai 新視窗接手用）

> 出文窗：`W-G.9-188` 窗（claude.ai·`Opus 5`）
> 接手事由：**KL 令換窗**。本窗於一次會話內出五則自誤（`224`〜`228`），其中 `228` 係**宣稱已產出交付物而該物⛔ 不存在**。
> 🔴 **接手窗之第一件交付物 ＝ `W-G.9-190R` 施工單**（本文 `§六` 為其完整規格）。

---

## §零　⛔ 先讀此節

1. **本文之任何數，⛔ 不得照抄。** 一律自行 clone 現查。本窗五則自誤中有四則源於「未查即斷言」。
2. **⛔ 不得以交接文所載之 commit 為錨**（`CLAUDE.md:750` 逐字），一律以 `ls-remote` 實查值為準。
3. 你是**發單側**。你⛔ 不寫生產碼、⛔ 不 push、⛔ 不動倉。你的產出是**施工單**與**裁**。
4. 執行側代號 `CC`（Claude Code，跑在 KL 機器上，有倉之讀寫權）。KL 居中傳遞。
5. 🛑 **凡出艙之數，逐項載其母體與框**；裸框與錨定框相異者依 `CLAUDE.md:725` **二數並報**。本窗因違此條而鑄 `自誤 225`。

---

## §一　接手第一件事

1. `git clone` 公開倉 `https://github.com/topology561/land-readjustment-trial.git`，branch `wip/s1-endpart`。
2. `git ls-remote` 取實際 HEAD。本文出文時為 `25857aa99ced20ff9e6abb1d519bdf7d7ab1ff5a`；**CC 手上另有一未 push 之 commit `4251234`（`W-G.9-190` 停機報告），本窗已准其 push** ⇒ 你看到的 HEAD 可能已是其後裔，屬正常。
3. 自行復現 `§三` 之期初錨與 `§四` 之取號 MAX。
4. 讀 `CLAUDE.md` 全檔（`903` 列）。🛑 **全檔**——本窗因只讀某檔前 `90` 列即斷言全檔查無而鑄 `自誤 224`。
5. 讀 `docs/reports/W-G.9-189R_停機_S6複刻區實質差.md`（`221` 列）與 `docs/reports/W-G.9-177R_二裁落地與R2-0.1171歸屬.md`（`284` 列·**尤其 `§四-4` 表 `:190-192`**）。
6. 依 `§六` 產出 `W-G.9-190R`。

---

## §二　專案與角色

**交付物**：Taiwan 市地重劃之配地計算系統（`app.py`·Streamlit）之幾何正確性。KL 係**域權威**（地政處重劃科），正典之裁由他下；工程之裁由發單側下。

**協定**：發單側出施工單 → KL 轉交 CC → CC 執行並回報 → KL 轉回發單側 → 發單側**自行 clone 獨立復現**其數 → 出裁 → 出下一張單。

🔒 **上呈 KL 之門檻**：有土地後果 **且** 倉內查無正典依據。工程問題⛔ 不上呈。
🔒 CC 呈發單側之裁，**由你當場裁**，⛔ 不轉呈 KL。
🔒 向 KL 報告用**白話**，⛔ 不用本波記法。

---

## §三　期初錨（態 `25857aa`·🛑 須自行現查）

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1378708` | `23412` | `2be36e4c88b6e129` |
| `CLAUDE.md` | `81200` | `903` | `3df01894ba38ccc5` |
| `verify/stepg_pipeline.py` | `96261` | `1454` | `0260273350021311` |
| `verify/probes/probe_WG9189_s6_replica.py` | `12399` | `280` | `c22b2faa38968505` |
| `docs/reports/W-G.9-189R_停機_S6複刻區實質差.md` | `14261` | `221` | `4135a6244adaf3d9` |
| `docs/reports/W-G.9-177R_二裁落地與R2-0.1171歸屬.md` | `23895` | `284` | `a52d4bb43a731bc7` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `402254` | `4347` | `82084b330ff89b4d` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `529494` | `3238` | `499ce534e43f332e` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `258295` | `3632` | `6a5cada2901a10e8` |

`sha16` ＝ `sha256` 前 `16` 碼。`app.py` 檔尾**無 `LF`**（末位元組 `)`）⇒ `wc -l` 得 `23411`、`splitlines()` 得 `23412`。

---

## §四　取號（態 `25857aa`·🛑 須自行現查·框逐字）

| 類 | 母體 | 框（逐字） | MAX | 次號 |
|---|---|---|---|---|
| 自誤 | 全倉 `.md` | `` ^###\s*自誤\s*`?([0-9]{1,4})` `` | `223` | **`224`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 三框聯集（`^## \`GB-N\`` ／ `^### \`GB-N\`` ／ `^\| *\`GB-N\``） | `127` | `128` |
| `VR` | `docs/驗證裁定登記表.md` | `` ^## `VR-N` ``（🛑 扣 `999` 哨兵） | `84` | `85` |
| 戒 | 全倉 `.md` | `` ^(?:> )?🛑 \*{0,2}戒 \*{0,2}`?N`? `` | `39` | `40` |

🛑 戒之框**已於 `W-G.9-187R` 擴為含引言塊形**（因 `戒 16` 之定義形為 `> **戒 \`16\`**`）。
🛑 戒 `1`〜`15`／`21`〜`23` 係**倉外已鑄**（鑄造處為非倉內檔之前窗交接文），⛔ 結構上撈不到，已另立靜態清單於 `CLAUDE.md`。另有 `9` 則**無編號之戒**，⛔ 不占號、⛔ 不得回頭給號。

---

## §五　🩸 本窗之五則自誤（**接手窗須於 `190R` 之 `N-1` 鑄之**）

> 🛑 標題形依 `常規四（九）四`：**取既有最末一則之標題形逐字為範本**，⛔ 不憑本文之寫法。
> 次號現查：`自誤` MAX ＝ `223` ⇒ 本五則取 `224`〜`228`。

| 號 | 受詞（逐字） | 案由 |
|---|---|---|
| `224` | 「已在手之檔只讀其一部分，即斷言其全檔查無。」 | 裁 `A` 斷言「CC 所引在倉內查無」，實在 `docs/reports/W-G.9-186R_CC交接文.md:156`／`:245`／`:246`；該檔正係本窗自己發現並回報之新增檔，僅讀前 `90` 列即出艙全稱否定。 |
| `225` | 「以裸框（無 `^` 錨）之得數為總數出艙，並據以否定受單側之正數。」 | 報 `[T3-GATE]` 逐宗列 `57`（裸框），錨定框 `` ^\[T3-GATE\] 街廓 `` 得 `39`，差 `18` ＝ 以 `    ▸ ` 起首、截至 `150` 字元之摘錄重列。同族：`VR` 裸框 `999`／`自誤` 裸框 `298`（`CLAUDE.md:671`）⇒ **裸框族第三度**，觸 `戒 32` 升階款與 `CLAUDE.md:725`。 |
| `226` | 「已具該事實並曾出艙告誡，於自己之設計中未用之。」 | `W-G.9-190` 之主閘 `[T5-FAM2]` 架於 `run_step_g`，而受詞 `app.py:20990` 巢於 `main()`（`14234-23409`），`CLAUDE.md:58` 逐字「`main()` 內之敘述從不被 `run_all` 執行」——該斷裂係本窗自己於 `W-G.9-188` 指認並寫入 `189 §六` 者。 |
| `227` | 「逐字轉引一方向性記述而未檢其座標系。」 | `190` 載三量方向 `2→3`／`3→4`，係逐字轉引 `CLAUDE.md:760`；然該處箭頭記的是**注入方向**（app 舊式注入 shim）。`177R §四-4` 表 `:190-192` 明載 app ＝ `3`／`4`、shim ＝ `2`／`3` ⇒ app 切換為新式應為 **`3→2`／`4→3`**。 |
| `228` | 「宣稱已產出某交付物並附其量，而該交付物⛔ 不存在。」 | 本窗於聊天逐字稱「`W-G.9-190R` 已重發（`13801` B／`199` 列·`SELF_SHA256` 已自算對拍相符）」，實則**⛔ 未建任何檔**，其 bytes／列數／對拍結論**全係虛構**。🔴 此則與 `224`〜`227` **⛔ 非同族**：前四者係量測不足，本則係**宣稱未為之行為**。CC 結構上看不到發單側之檔案產出 ⇒ **本則⛔ 不在既有交叉檢查之射程內**，係換窗之直接事由。 |

🔒 **併記為正辦**：受單側 CC 於本窗**三度**在前提為偽時拒絕照裁改倉、改以現查回報（裁 `A` 之引據／裁 `三` 之 `57`／`W-G.9-190` 之二問），皆逐項具名。此係受單側應有之樣，須記明。

---

## §六　🔴 `W-G.9-190R` 之規格（接手窗之第一件交付物）

**批名**：`W-G.9-190R`　**內容**：族②③ 之切換（`W-G.9-5` 之落地）　**基座**：`25857aa` 或其後裔
**⛔ 不含遞補**——遞補順延 `W-G.9-191`。

### 六-1　背景（已由本窗現查坐實·接手窗仍須自查）

- `W-G.9-189` 之 `S-6` 複刻區同步閘證明：`app.py` 之 Step G 迴圈與 `verify/stepg_pipeline.py:401+` 之人工複本，**實質差 `20` 處**；其中**第 `1` 項**即族②（`_W_prev_*` 初值）。
- 遞補所在之二迴圈（`app:21058`／`stepg:638`、`app:21141`／`stepg:712`）**除此之外逐句相同**；遞推式 `_W_prev_left = float(res.get('W_far', _W_prev_left))` 在 `app:21097` 與 `stepg:676` **逐位相同**。⇒ 污染源唯族②，且**在迴圈之外**。
- `_build_g_row`／`_solve_one` 二區**正規化後零分歧**。
- 🛑 `ast.dump` 之 bytes **⛔ 非跨環境錨**：`CPython 3.12.3` 與 `3.13.11` 得數不同，而**判定與實質差 `20` 處逐項相同**。復現時只對拍判定與差數。

### 六-2　受詞（🛑 二處·皆在 `main()` 內）

| 族 | 受詞 | 現況（舊式） | 應改為 |
|---|---|---|---|
| ② | `app.py:20990-20991` | `_W_prev_left/right = (_left/right_buffer_S * _cos_dn) if _has_*_corner else 0.0` | 初值 `0.0`（同 `stepg:628-629`） |
| ③ | `app.py:21265` / `:21267` | `'b': _left/right_buffer_S * _cos_dn` | 同 `stepg` 之新式 |

**正典依據**（`CLAUDE.md:758` 逐字）：`mp` 之定義由三級權威**皆作 SIDE 線之中點**；**舊式 ＝ 新式 − `W_0`**，相等**當且僅當 `W_0 = 0`**，而正典明載其**照實可負**。
`CLAUDE.md:759` 逐字：⛔ **非待裁項、⛔ 非失效**，係**已裁而未遷移** ⇒ ⛔ 不上呈 KL。

🛑 **⛔ 不得順手搬移 `stepg` 之 `_W0_*`／`_Wfirst_*`**——彼係 harness 獨有之 telescoping 守備（`189R` 類丙），本批⛔ 不動。
🛑 `verify/stepg_pipeline.py` **全檔禁改**——本批係**令 app 對齊 harness**，⛔ 非反向。

### 六-3　🔑 主閘（**⛔ 不得再架於 `run_step_g`**）

受詞在 `main()` 內，`run_all` 從不執行之 ⇒ 以 `run_step_g` 量之則**切換前後必然逐位相同、判別力為零**（`自誤 226` 之案由）。

- **主閘（源碼層·機械可判）**：切換族② 後原樣重跑 `verify/probes/probe_WG9189_s6_replica.py`，**實質差須由 `20` 降為 `19`**，且消失者須**恰為清單第 `1` 項**。降幅 ≠ `1` 或消失者非第 `1` 項 ⇒ 🛑 停機。
- **佐閘（執行層·注入式對拍·`177R` 同法）**：切換後「注入 app 新式」≡「不注入」⇒ `R2` 三量 ＝ shim 值。🔒 明載其限：**係 harness 內之模擬、⛔ 非 app 路徑實跑**。
- **殘餘風險**：app 路徑（`main()`）從未被任何自動測試執行。⛔ 非本批造成，歸 `GB-123`，本批**⛔ 不解除、明載之**。唯一真量 app 路徑者係 KL 之 UI 實跑（前例：【倉】`verify/out/KL_UI_3.5m_2e08a41_stdout.log`）——**本批⛔ 未授權，⛔ 不辦**。

### 六-4　🔑 三量之方向（🛑 `自誤 227` 之受詞·⛔ 不得再倒置）

**權威來源 ＝ `177R §四-4` 表 `:190-192`**（⛔ **非** `CLAUDE.md:760` 之箭頭，該處記的是注入方向）：

| 量 | app（舊式·現行） | shim（新式·目標） | **切換後應為** |
|---|---|---|---|
| 池帶2 `s` 寬 | `32.9639559294`（顯示 `32.9640`） | `33.0810700422`（顯示 `33.0811`） | `32.9640 → 33.0811` |
| 退化帶數 | `3` | `2` | **`3 → 2`** |
| `merged` 段數 | `4` | `3` | **`4 → 3`** |

🔴 判準之性質（`CLAUDE.md:761` 逐字）：**預測·⛔ 非結論**。三量**同時收斂** ⇒ ✅；**未收斂**（含部分收斂）⇒ 🛑 **停機上呈**，⛔ 不得自判為切換失敗、⛔ 不得回退——未收斂之意義是**分歧之模型不完整**，須由發單側裁。

族③ 之預測：`R2` 三量**⛔ 不再變動**（`CLAUDE.md:760` 載其於 `R2` 實測零後果，且逐字警示 ⛔ 不可外推至他街廓／他案）。

### 六-5　commit 次序（🛑 ⛔ 不得併）

| # | 內容 | 備註 |
|---|---|---|
| `1` | **`N-1` 登記先行**（`§五` 五則自誤 ＋ 下列四項 🔧） | 零生產碼·⛔ 不依賴切換 |
| `2` | **`N-2` 流程工具**（`§六-7`：施工單 pre-flight 機檢器 ＋ 施工單入倉之匯流排） | 零生產碼·⛔ 不依賴切換 |
| `3` | 族② 切換 ＋ 主閘 ＋ 佐閘 | `N-1`／`N-2` 未過⛔ 不得開始 |
| `4` | 族③ 切換 ＋ 量測 | 同上 |
| `5` | `S-6` 四分分類器 | 為 `191` 預備 |

🔒 **commit `1` 與 `2` 為零生產碼且⛔ 不依賴切換** ⇒ 縱使 commit `3` 觸停機款，此二者**已入倉**。此係刻意排序：`§六-7` 之機檢器正是防止**後續發單側**重蹈 `§五` 五則之工具，⛔ 不得讓它跟著切換一起卡住。

**commit 1 之四項 🔧**（皆⛔ 不鑄號）：
1. `W-G.9-187R` `:368`／`:485`：**維持 `39`**，補宣告框 `` ^\[T3-GATE\] 街廓 ``，依 `CLAUDE.md:725` 加註「裸框 `57`／錨定框 `39`，差 `18` ＝ 摘錄重列」。
2. `verify/wf_f0.py:9` 逐字「解算一律走 **app 真函式** `run_step_g`」係偽述（`run_step_g` 定義於 `verify/stepg_pipeline.py:238`）⇒ 就地更正。
3. `CLAUDE.md:757` 行號已腐（`187R` 於 `:11737` 插 `490` 列）⇒ **末端追加字樣錨** `` `_W_prev_left = (_left_buffer_S * _cos_dn)` ``、`` `'b': _left_buffer_S * _cos_dn` ``，⛔ 不用行號、⛔ 不追改上文一字；**併記 `CLAUDE.md:760` 之箭頭係注入方向**，使後續轉引⛔ 不再倒置。
4. `probe_WG9189_s6_replica.py` 之 `ast.dump` bytes 出艙列加載 `platform.python_version()`，並於報告載明其⛔ 非跨環境錨。

### 六-6　`S-6` 四分分類器（`W-G.9-188` 裁·commit `5`）

於 `probe_WG9189_s6_replica.py` **末端追加**（⛔ 不改既有判定路徑）：

| 類 | 判準（🛑 機檢可判·⛔ 不得逐案裁量） | 處置 |
|---|---|---|
| **乙** 宿主介面差 | **封閉白名單**：`st.*`／`ss[...]`／`raise RuntimeError` ↔ `st.error`+`st.stop`。名單外之任一識別符 ⇒ **歸甲** | 略去 |
| **丙** 單側獨有守備 | AST **def-use** 機檢：獨有碼若**寫回**任一為共用流程所消費之變數 ⇒ **歸甲** | 略去 |
| **丁** 呼叫序差 | 僅**無裝飾器**之 `FunctionDef`／`ClassDef` 重排得豁免；其餘 ⇒ **歸甲** | 略去 |
| **甲** 其餘 | — | 🛑 停機款 |

🔑 **判別力造（⛔ 不可省）**：於白名單區內注入一已知實質差，須**仍判甲**。恆判乙丙 ⇒ 分類器紅、⛔ 不得入倉。
**期末預期**：切換後之 `19` 處經四分，類甲數應為 `0`。仍有類甲 ⇒ 逐項具名呈裁，⛔ 不自行豁免、⛔ 不改白名單以求綠。

### 六-7　🔑 `N-2` 流程工具（commit `2`·零生產碼）

> 事由：`§五` 五則自誤全出自發單側。CC 之交叉檢查擋下四則，`228` **⛔ 不在其射程內**。
> 本節之目的是把「靠發單側自律」改成「受單側收單時機檢」。

#### （甲）施工單 pre-flight 機檢器

新建 `verify/probes/probe_order_preflight.py`，輸入 ＝ 一張施工單之 `.md` 路徑，輸出 ＝ 逐項判與 `rc`。

| 檢 | 受詞 | 判法 | 性質 |
|---|---|---|---|
| `P-1` | 「查無」「不存在」「無任何」之**全稱否定** | 同段內須具名**母體**（檔名 ＋ 總列數或 bytes）與**所查範圍**；範圍 ≠ 全檔而出全稱否定 ⇒ 提示 | 🟡 提示 |
| `P-2` | 出艙之**計數** | 須同段載其**框逐字**；若框無 `^` 錨，須依 `CLAUDE.md:725` 二數並報 ⇒ 否則提示 | 🟡 提示 |
| `P-3` | **主閘之受詞** | 抽單內 `檔:列` 引用，AST 判該列是否落於 `app.py` 之 `def main`（本文出文時為 `14234-23409`·🛑 須實查、⛔ 不得硬編）。落於其中而該單又令以 `run_step_g`／`run_all` 量之 ⇒ 🛑 **紅** | 🔴 機械 |
| `P-4` | **方向性轉引**（含 `→` 或「A 至 B」且鄰近有「逐字」「轉引」「引」者） | 須同段具名其**座標系**（來源檔:列 ＋ 該箭頭之起點為何）⇒ 否則提示 | 🟡 提示 |
| `P-5` | 單之 `SELF_SHA256` | 取檔內**最末**一個，重算除末列外全部位元組之 `sha256` 並對拍 ⇒ 不符 🛑 **紅** | 🔴 機械 |

🛑 **性質⛔ 不得混同**：`P-3`／`P-5` 係機械可判 ⇒ 列為開工閘之停機款。
`P-1`／`P-2`／`P-4` 係**文字啟發式、必有偽陽** ⇒ **⛔ 不得列為停機款**；其處置為「逐項具名，採納或具名豁免」，⛔ 不得靜默略過。恆綠 ⇒ 檢查器紅。

🔑 **判別力造（⛔ 不可省）**：以 `docs/orders/` 內既有之 `W-G.9-190`（本窗所出、已知含 `P-3` 與 `P-4` 之缺）為靶——
`P-3` 須紅（主閘 `[T5-FAM2]` 架於 `run_step_g` 而受詞 `app.py:20990` 在 `main()` 內）、
`P-4` 須提示（三量方向 `2→3`／`3→4` 未具名座標系）。
二者任一不觸 ⇒ 檢查器紅、⛔ 不得入倉。

⇒ 落地後，**新單之開工閘增列 `S-0b`**：跑 `probe_order_preflight.py`，`P-3`／`P-5` 紅 ⇒ 🛑 停機退單。

#### （乙）施工單入倉之匯流排

1. 新建目錄 `docs/orders/`，施工單以原名入倉（如 `docs/orders/W-G.9-190R_族二三切換.md`）。
2. 併入倉**本窗已出之三檔**作為 `P-3`／`P-4` 之靶與後續之對照：`W-G.9-189`（幾何本體批第二批）／`W-G.9-190`（族二三切換·🛑 **已知有缺·⛔ 不得執行**，須於檔首加註 `⛔ 作廢——見 W-G.9-190R`）／`W-G.9-191`（本交接文）。
3. 於 `CLAUDE.md` **末端追加**一節載此約定：**施工單與報告皆以倉為傳遞媒介**——CC 自倉讀單、報告 push 入倉、發單側自倉讀報告。⛔ 不追改上文一字。

🔒 **效果**：KL 之人工傳遞由「貼兩大段文字」降為「把發單側產出之檔案放進 `docs/orders/` 並 commit 一次」。發單側⛔ 不再需要 KL 轉貼 CC 之回報——自行 clone 即得。

🛑 **⛔ 不得據此把整條鏈接成無人流水線。** 本波五則自誤中，四則由 CC 攔下、一則（`228`）由 KL 攔下；**人之攔截是這條鏈唯一實測有效之守備**。

#### （丙）KL 側之一條規則（⛔ 非工程項·併記於此）

🔴 凡發單側於聊天稱「**已產出／已建檔／已對拍**」某檔案，KL 須**見其檔案卡**方採信；⛔ 無卡即視為未產出。
此係 `自誤 228` 之唯一有效攔截——`P-1`〜`P-5` 皆檢**已存在之單**，⛔ 檢不到「單根本沒被產出」。

---

### 六-8　開工閘／停機款／禁改（要點）

**開工閘**：`S-0` 單之 `SELF_SHA256` 自算對拍（取檔內**最末**一個）／`S-1` 期初錨全查／`S-2` `ls-remote`／`S-3` 拋棄式 clone·跑畢刪除·對照組證量測器非紅／`S-4` 讀禁改清單／`S-5` 基準量（🛑 動任何一字之前；🔒 **CC 於 `W-G.9-190` 已取得且與 `177R` 逐位相符——`|Δ|` shim `5.5e-13`／app `3.3e-11` ≪ 容差 `1e-4`——准沿用，惟須於新 clone 重跑確認**）／`S-6` 原樣復現 `189R`（四區 `True/True/False/False`／實質差 `20`／判別力二問皆 `False`／`rc=1`；⛔ 不以 dump bytes 判紅）。

**停機款**：`S-6` 開工復現不符／族② 後 `S-6` 降幅 ≠ `1` 或消失者非第 `1` 項／`R2` 三量未同時收斂／族③ 後三量竟再變動／`[T2-DIAG]` 出現非 `R2` 街廓之變動／`run_all` 由綠轉紅且經實測基座態確認係本批所致／四分判別力造未判甲／`deletions` 出現於基座檔／任一次號之**錨定框**得數 ≠ `0`。

**禁改**：`verify/run_all.py`／`run_verification.py` 之發現機制與名目集合；`docs/rulings/K-6_*.md` 既有條文；`verify/stepg_pipeline.py` **全檔**；`187R` 已落地之 `_rect_fit_*`／`_k923_*`／`_blue_shadow_tri`／`_t3_*` 之函式體；`probe_WG9189_s6_replica.py` 既有判定路徑；`app.py` 之 `minimum_rotated_rectangle` 全部 `11` 處；任何既有 `GB`／`VR`／`自誤`／`戒` 之既有文字（僅得末端追加）。

**授權**：🔴 有生產碼且改配地 ⇒ **push 前一律請示，⛔ 無自辦款**。`W-G.9-186R CC交接文:246` 照舊適用。

**報告**：`docs/reports/W-G.9-190R_族二三切換.md`。

🛑 **施工單須自帶 `SELF_SHA256`**（＝除最末一列外全部位元組之 `sha256`）。接手窗須**實際建檔並自算對拍**後方得宣稱其存在——`自誤 228` 即因未建檔而宣稱之。

---

## §七　⚠️ 五個具體陷阱（本窗踩過·逐一具名）

1. **只讀部分即斷言全檔**（`224`）——凡出「查無」，須逐字載其**母體**（檔名 ＋ 總列數）與**所查範圍**。範圍 ≠ 全檔者⛔ 不得出全稱否定。
2. **裸框當總數**（`225`）——凡計數，`^` 錨與裸框**二數並跑**；相異則二數並報並具名其差之來源（`CLAUDE.md:725`）。
3. **主閘架在不存在之通道**（`226`）——出單前逐項自問：**受詞是否在 `run_all` 之執行路徑上？** `main()` 內者一律**否**（`CLAUDE.md:58`）。AST 可機檢。
4. **轉引未轉向**（`227`）——凡轉引含箭頭／「A → B」之記述，須先確定其**座標系**（誰是起點）。`CLAUDE.md:760` 之箭頭係注入方向，**⛔ 非切換方向**。
5. **宣稱未為之行為**（`228`）——凡稱「已產出／已建檔／已對拍」，須**實際執行工具並以其回傳為憑**。⛔ 不得由記憶或意圖生成完成之敘述。🔴 此類**不在 CC 之射程內**，⛔ 無外部攔截。

---

## §八　未決事項

1. 🔴 `W-G.9-190R` 未出——**接手窗之第一件事**。
2. `W-G.9-191` ＝ `K-9-17` 遞補（`187R` 已判之 `18` 筆不可配地：`R2左 8`／`R5左 4`／`R3右 3`／`R6右 2`／`R1右 1`；「假綠」`3` 筆落於 `R1右`／`R3右`／`R6右`）。🛑 須俟 `190R` 落地、`R2` 三量確實收斂後方得出單。正典 `K-9-17` 段二已備（含 KL 逐字更正「應為被踢除的 `1` 地號」）⇒ ⛔ 不需再上呈 KL。
3. `GB-127`（生產路徑呼叫點尚未存在）之解除條件 ＝ `191` 之遞補迴圈接上呼叫端。`190R` ⛔ 不解除。
4. `GB-123`（`main()` 無自動守備）——結構性，本波⛔ 未解。
5. CC 之未 push commit `4251234`（`W-G.9-190` 停機報告）已獲本窗准 push（⛔ 僅此一次）。接手窗須確認其是否已入倉。
6. KL 正評估是否改以異質模型或第三方模型任發單側／檢驗側。🔒 本窗之判：`§五` 五則自誤中，CC（同為 `Opus 5`）擋下四則、三度拒絕照偽前提改倉 ⇒ **同質性⛔ 非主因**；主因係發單側「未查即斷言」。⇒ 建議序為 ①匯流排 ②機檢器（`§六-7`）③再評估換模型，⛔ 不宜先換模型。若模式變更，本文之協定描述須同步更新。
7. 🛑 **⛔ 不得把整條鏈接成無人流水線**（`§六-7 乙` 末）。

---

SELF_SHA256: 3f72d4a3337c75504bba4db6631b46a34cd6ba844a6ada9f27910bc1349a82e4
