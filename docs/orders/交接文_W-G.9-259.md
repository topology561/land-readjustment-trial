# 交接文_W-G.9-259（發單側 · `claude.ai` 窗轉換）

> **受詞 ＝ 下一個 `claude.ai` 窗**，非 CC。
> CC 收此文之處置 ＝ **原封入倉 `docs/orders/`，⛔ 執行其所列之任何工項**。
> 入倉前依現行條款查號占用（**宣告框** `①`–`⑦`·三數分列 ＋ **雙屬列數** ＋ 檔框聯集式）。

---

## §一　態錨（`2026-09-08`·本窗最後一次 `fetch`）

```
origin/wip/s1-endpart = a5b4edd（主線·c1 尚未併）
origin/verify/W-G.9-255-c1 = 3242e96（c1·KL 已放行·候 CC 依 -258 併線）
追蹤 1700 檔 ／ .md 623 檔 ／ docs/ 601 檔 ／ left-right = 0 0 ／ 遠端 heads 16 支
```

**生產路徑十檔 ＋ 夾具**（sha256 前 16·本窗於 `a5b4edd` 實測）：

| 檔 | sha256 |
|---|---|
| `app.py` | `4860f10828fb8ae9` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` |
| `verify/run_verification.py` | `d0d531f7dc051b65` |
| `verify/stepg_pipeline.py` | `c8d46d2e5fb79dc5` |
| `verify/run_all.py` | `4ba89fef90979491` |
| `verify/wf_f0.py` | `6758ea766b001b95`（**併線後應為 `b0722355c1978635`**）|
| `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| `verify/fixture_wf_ns_wiring.py`（**⛔ 屬十檔**·另計）| `b17826a6f7b98bf5` |

🛑 「十檔」＝ 閘 `P` 五檔 ＋ `wf_f*` 五檔；**夾具⛔ 在內**（`自誤 307`）。

自誤簿（定義框 ∪ **範圍框展開**·母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔）：
相異 `312` ／ `MAX` `319` ／ `MIN` `7` ／ 缺號 `[106]` ／ 恆等式 `True`。
🔒 範圍框須展開（`8` 條攢批標題）——只用定義框會得 `[106, 170–182]`。
`GB MAX 153` ／ `K-9 MAX 29`。

號占用（`a5b4edd`）：`256` `D1 2/D2 3/D3 0`（已占）／`257` `1/1/0`（**已入倉·未辦**）／`258` `0/0/0`（**已出單·未貼**）／`259` `0/0/0`。

**⛔ 採信上表任一值**——接手窗須當場 `clone` 並以 `git cat-file blob` 復現。

---

## §二　接手窗必須先知道的五件事

**一　`claude.ai` 側可以直接讀倉，也可以<u>跑 harness</u>。**

```
git -c core.autocrlf=false -c core.quotepath=false clone https://github.com/topology561/land-readjustment-trial.git
git checkout wip/s1-endpart
pip install ezdxf shapely plotly streamlit pandas numpy openpyxl --break-system-packages
python3 verify/run_verification.py        # 約 3–5 分鐘·現態 RESULT: FAIL
```

🆕 **本窗新開之能力**：`run_verification.py` 可在 `claude.ai` 容器實跑，且可**在 scratchpad 複本上加診斷輸出**（⛔ 動倉內 `app.py`）。`GB-67` 之定位即由此得。**接手窗務必用它**——本窗最大的一則自誤（`319`）正是「立驗收而未跑該路徑一次」。
界限：只看得到已推送者；容器不留；**⛔ 推送**。

**二　`git ls-tree` 之 `core.quotepath` 會把 CJK 檔名跳脫。** 🔒 凡對倉內檔名計數，一律加 `-c core.quotepath=false`。

**三　⛔ 沿用任何前批之 `app.py`／`wf_f0.py` 行號**，一律當場對拍。

**四　🔴 本窗自誤集中於<u>兩個受詞</u>，接手窗須提防。**
本窗鑄 `314`–`320`（`7` 則）：

| 族 | 則 | 共相 |
|---|---|---|
| **宣告框／載框名 ≠ 框實作** | `314`／`320` | 單書 A 框，自己以 B 框產數並出艙 |
| **驗收前提未查** | `319` | 立「逐值對拍」而未跑該輸出之產生路徑一次 |
| 受詞未定義 | `317` | 令 CC 取某量而未具名其粒度受詞（「逐筆」）|
| 以 proxy 代 property | `318` | 以截圖斷言倉內快照脫節，未 `cat-file` |
| 單內部相斥 | `316` | 同段令「皆檔末追加」而其下逐款書「節末」 |

🔑 **攔截者：`314`／`316`／`320` 由 CC 攔下、`317` 由 KL 逐字更正、`318` 由 KL 更正、`319` 為發單側實跑自捕。**
⇒ **⛔ 靠 KL 或 CC 當偵測器。** 出艙前之自檢須含：①單所書之框與自己產數之框是否同一；②所立之驗收路徑是否**實際執行過一次**。

**五　CC 本波表現可靠。** `-249R` 六項 `BLOCKED`、`-255R` 三環自我推翻、`-256R` 之 `60` vs `114` 照實出艙不採信單之數——**三度攔下發單側之框錯**。其自報仍⛔ 採信，但其停機**每一次都成立或部分成立**。

---

## §三　本波所裁之域裁（`K-9-29` ＋ 補款十·全部已入典）

`K-9-29`（KL 裁 `2026-09-08`·`W-G.9-253`·九款）＋ **補款 `十`**（KL 裁 `2026-09-08`·`W-G.9-255`）：

| 款 | 要旨 |
|---|---|
| 一 | 重劃前地價**逐筆**評定。🔑 **「逐筆」＝ 每一原地號各自被指定一個區段碼**，⛔ 各有獨立價格欄。「同區段⇒同單價」**⛔ 錯**（KL 逐字）|
| 二 | **廢** `K-6` 步驟 0 前置合併；`K-9-21`／`K-9-24 一` 失所附麗；`K-9-22` **作廢·無須落地** |
| 三 | 一切合併恆走 `a′ = a × p(自身)/p(標的)`（`v3:432`–`435` **雙模式**逐字為準）；⛔ `Σa` |
| 四 | 標的宗 ＝ `G` 最大；並列取暫編字典序最小（沿用 `wf_f0` `_key`）|
| 五 | 位次 ＝ **投影序在前者**，`(a+Σa′)` 前遞至該位重算 `G`，⛔ 留空位 |
| 六 | 五級**入池閘**：個別 `G < MinA` **且**與同街廓同歸戶相鄰宗合併試算**亦** `< MinA` 方入池 ⇒ 「合併可達卻因入池離位」⛔ 發生 |
| 七 | 正式案 `RD`／公設上之土地各有逕割地號、各有地價；`628(1)`／`628(2)` 共價係測試簡化 |
| 八 | 本案土地後果：步驟 0 之 `9` 組／`13` 宗解併；`R4` 左街角 `226.01㎡` 由抵費地變回 `G009`（**承推定·須實算**）|
| 九 | 碼側落地有土地後果·逐 commit 放行·序在五級前 |
| **十** | **跨區段合併准予為之**；`停機#2` 廢止；`p ≤ 0 ⇒ raise` |

🔒 **授權之界（KL 裁 `2026-09-06`）**：純程式結構（射程、母體、粒度、**框**、工序、批之切分、量測器設計、seam 之介面形）**得由發單側自裁**，判準 ＝ **改之不動任何一宗地之配地結果**；改變準據本身者恆須 KL；界不明 ⇒ 回 KL。

🔒 **呈 KL 之形**：發單側先自倉逐位復驗，再以**一句實務語言**呈 KL；**KL 放行的是那句話、⛔ 那段程式碼**。KL 自陳不懂程式。

---

## §四　現況：四張單在飛

| 單 | `SELF_SHA256` | 狀態 | 下一步 |
|---|---|---|---|
| **`-256`** | `2582ca5a8e0d7589…` | ✅ **已辦畢**（`a5b4edd`）| —— |
| **`-257`** | `0394745232cb7e7c…` | **已入倉·未辦**（`GB-67` 定位波·純診斷）| 🔴 **本窗移交項·CC 下一輪主工作·結果呈接手窗** |
| **`-258`** | `8d5cc44db0bceb71…` | **已出單·未貼給 CC**（`c1` 放行併線 ＋ 自誤 `320`）| 接手窗須確認 KL 是否已貼；未貼則貼之 |
| **`-253` 段乙** | —— | `-251` `c1`／`c2`·純 UI·⛔ 受阻 | 側分支 `verify/W-G.9-251-c1` **仍不存在** ⇒ 未施工 |

🔒 **KL 已放行 `c1`**（`2026-09-08`·放行句逐字見 `-258 §零`）。`-258` 係執行該放行之單。

---

## §五　🔴 `GB-67` 定位：本窗已測之基態（接手窗承接 `-257` 之起點）

**發單側於 `8a52dbd` 實跑 `run_verification.py` ⇒ `RESULT: FAIL`、紅 `32` 項**，`W-F F.0`–`F.4` 全紅。根因：`StepG` 於街廓 `R2` 中止。

**scratchpad 診斷所得**（⛔ 入倉·接手窗須自行復現）：

```
[GB67-DIAG R2] 宗數=14 總疊=45.9766 上界=2.8906 深度=44.4700 配對數=3（/91）
  宗0 × 宗3  疊=26.1624  (i面積 175.946 / j面積 293.478)   佔 56.9%
  宗0 × 宗2  疊=19.8142  (i面積 175.946 / j面積  29.085)   佔 43.1%
  宗1 × 宗2  疊= 0.0000
```

`14` 宗全部 `valid=True`／`rings=0`／`type=Polygon`（⛔ 自交、⛔ 洞、⛔ MultiPolygon）。
`bounds` 之 `x0` 逐宗單調遞增（`310363.91 → 310408.31`），惟 **`y0` 於 `k=1` 突跳 `2651908.42`、`k=2` 回落 `2651883.02`**；`宗1` 面積僅 `3.808㎡`（碎片）。`宗0`（`175.946㎡`）之 `y` 上界 `2651915.17` **橫跨宗1／2/3 之全域**；`宗2` 有 `68.1%` 面積落在 `宗0` 內。

🔴 **發單側之推定（⛔ 已證·`-257` 令 CC 證之或推翻）**：
`GB-67` 登記表所載受詞「**不相鄰**之宗之間為何會重疊」**恐為誤框**——三宗 `bounds` 顯示實相鄰。真受詞應為「**`宗0` 之推進終點（切帶 `s` 上界）為何未在 `宗0`／`宗1` 界面停住**」。此框錯了八十餘張單（`GB-67` 自 `W-G.9-76` 即列波末佇列**第 0 項·真瓶頸**）。
🔴 **上界式亦證偽**：`(宗數−1)×0.005×深度` 之模型假設「每對疊一點點·遍佈全體」，實測 `91` 對僅 `3` 對非零、`2` 對吃掉全額 ⇒ 模型與病灶形狀不符，**調鬆也攔不到對的東西**。

🛑 **KL 已裁丙案（`2026-09-08`）**：「只做定位、不修」。`-257` `V-P6` 設有射程閘（報告內「應改／建議／修正為」命中須 ＝ `0`）。
🛑 **`-257` 之 `Q-6` 可能擴大 `GB-153` 射程**：現僅知 `R1` 綠、`R2` 紅，`R3`–`R6` **從未被跑到**。

---

## §六　待落地（**接手窗須自倉重生此表·⛔ 沿用**）

| 項 | 狀態 | 依賴 |
|---|---|---|
| `-258` `c1` 併線 | ⬜ | 單已出·KL 已放行·候貼 |
| `-257` `GB-67` 定位（`Q-1`〜`Q-7`）| ⬜ | **本窗移交項** |
| `GB-67` 之**修** | ⬜ | 候 `-257` 結果 ＋ KL 裁（甲開修／乙續押）|
| **`c2`**（廢步驟 0·`R4` `226.01㎡`）| ⬜ **⛔ 開工** | `V-H` 由 `GB-153` 判不可得；得俟 `GB-67` 可修則回復 |
| `-249` 段三觀測（`V-S` 表）| ⬜ | 同上；且須俟 `c2` 併線後**重跑**（`K-9-29 九`）|
| `-253` 段乙（`-251` UI）| ⬜ | ⛔ 受阻·可並行·`V-K` 候 KL 目檢 |
| UI 步驟 D 逐宗點選之 KL 目檢 | ⬜ | `c2` 併線後（`628(1)+` 應散回逐宗）|
| `K-9-29 六` 入池閘之**五級層** | ⬜ | 五級（`r1`–`r8`）未起 |
| `a′` 模式二（`p_average`）| ⬜ | 五級射程 |
| telescoping（`R2 left ΣRw 100.02 ≠ 44.41`）| ⬜ | `-257 Q-7` 判其與 `②-宗` 是否同源 |
| 五級實作／區外道路／通用化波 | ⬜ | 見 `W-G.4` 依賴序 `3′ → 3″ → 4` |

---

## §七　未答之題（⛔ 待 KL）

**無。** 本窗所提之題（跨區段合併之許可、`V-G` 改用代數證明、`GB-67` 排程甲乙丙、`c1` 放行）KL 已全數答畢。

接手窗第一次需要 KL 之時點，預期為：
- **`-257` 定位完成後**：一句話「`R2` 那 `46㎡` 的成因是 `X`，修它要動 `Y`」⇒ KL 裁**開修 `GB-67` 與否**。
  🛑 **該裁會動配地幾何** ⇒ 依窗切換規則，⛔ 在飽和窗做。
- `-251` 段乙併線前之 `V-K` 目檢。
- `c2` 若因 `GB-67` 可修而解封：`R4` `226.01㎡` 之差異表。

---

## §八　接手窗之建議首四步

1. **clone 並復現 `§一` 全部態錨**（⛔ 採信本文任一值·`core.quotepath=false`·檔框聯集式·列框依 `⑦` 亦聯集）。
2. **裝 harness 依賴並實跑一次 `run_verification.py`**，記其 `RESULT` 與紅項數——這是本波最貴一課（`自誤 319`）。
3. **查 `-257`／`-258` 之執行狀態**（`docs/reports/` 有無其報告；主線是否已含 `wf_f0 = b0722355c1978635`）。
4. **⛔ 逕自決定 `GB-67` 開修** —— KL 已裁丙案（只定位）；改甲案須 KL 另裁，且應在新窗為之。

---

*本文受詞 ＝ 下一個 `claude.ai` 窗。CC 之處置 ＝ 原封入倉，⛔ 執行。*

SELF_SHA256: f5dc385fe1ba20412b7ef6579ee580a60495b594aade180a6d2a0006ef144d7a
