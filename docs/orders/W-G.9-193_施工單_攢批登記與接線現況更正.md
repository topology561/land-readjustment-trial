# 🔧 `W-G.9-193` 施工單：**攢批登記（自誤 `241`〜`243`）＋ 接線現況之更正**

> **性質**：🟢 **零生產碼**（⛔ 不動 `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 一字）
> ⇒ 依 `CLAUDE.md` `🔧 常規一 之補款 一`（KL `2026-08-31` 逐字「零生產碼 commit 逕行 push，生產碼 commit 才請示」）**逕行 `push`、⛔ 不必請示 KL**。
> **出單窗**：claude.ai（接 `W-G.9-193` 發單側交接文·第二版）
> **基座**：`wip/s1-endpart` ＝ `ab5983fb7c0800e9245347620eb06b831126115e`（🛑 開工時須自行 `git ls-remote` 實查；不符即停機）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　受詞與落地點（🛑 逐一具名·⛔ 本單不出艙任何裸計數）

**落地點（逐一具名·⛔ 無其他）**

| # | 檔 | 動作 |
|---|---|---|
| `L-1` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **末端追加**三則（`自誤 241`／`242`／`243`） |
| `L-2` | `CLAUDE.md` | **末端追加**一節（更正四） |

**同批入倉但⛔ 非落地點**

| # | 檔 | 動作 |
|---|---|---|
| `A-1` | `docs/orders/W-G.9-193_施工單_攢批登記與接線現況更正.md` | 本單以原名入倉（`🔧 施工單與報告之傳遞媒介 ＝ 倉` 一） |
| `A-2` | `docs/reports/W-G.9-193R_攢批登記與接線現況更正_執行報告.md` | 執行報告 |

🛑 **`L-1`／`L-2` 二檔屬 `常規一 ②` 補款③ `(2)` 之正典檔集** ⇒ 其 `deletions` **恆須 ＝ `0`**（純末端追加）。

---

## §零-b　開工閘

| 閘 | 指令 | 判準 |
|---|---|---|
| `S-0a` | `git ls-remote https://github.com/topology561/land-readjustment-trial.git refs/heads/wip/s1-endpart` | 逐字 ＝ `ab5983fb7c0800e9245347620eb06b831126115e`；**不符即停機上呈**（`常規九 三`） |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-193_施工單_攢批登記與接線現況更正.md` | `P-3`／`P-5` 須**不紅**；`P-1`／`P-2`／`P-4`／`P-6` 之提示**逐項具名，採納或具名豁免**，⛔ 不得靜默略過（`🔧 二` 逐字）。`rc≠0` 時須先依 `常規五` 判其係量測器紅抑或受詞紅 |
| `S-0c` | 見 `§五` 之 `S-6`（本單 `SELF_SHA256` 之自驗·逐字指令置於該節） | 二值逐位相同 |

🛑 **`S-0c` 之逐字指令⛔ 不置於本表**（表格格內無法安全承載引號與 heredoc）——**一律取 `§五 S-6` 之 code block 逕貼。**

🔒 **`S-0b` 之已知提示**（發單側已實跑·**母體 ＝ 本單單檔**·**所查範圍 ＝ `verify/probes/probe_order_preflight.py` 之 `P-1`〜`P-6` 六檢**·態 `ab5983f`·`rc = 0`）
> **唯一之 `P-1` 提示**（🛑 以**字樣錨**指之·⛔ 不用行號·`行號衛生` 第 `4` 款）：其命中列之逐字起首 ＝ `` 🔒 **一般式**　凡轉寫他批之**倉態陳述** ``，位於 `自誤 242` payload 內。🔑 **唯一性之如實揭露**：該字樣於本單共 `2` 命中——**其一為本註記之引述、其二為 payload 內之本體**，`P-1` 所指者為**後者**；判別力對照：寬字樣 `一般式` 於本單 ＝ `5` 命中 ⇒ 該檢⛔ 非恆為 `1`，其所含「不存在」「未實作」四字為**被引述之詞例**（該式所規範者正是此類陳述之轉寫），⛔ **非本單自身之全稱否定主張**。
> ⇒ 🔒 **發單側予以「具名豁免」**，理由如上。CC **仍須於報告內逐字複述本項並標「具名豁免（承發單側）」**，⛔ 不得靜默略過。
> 🛑 CC 之實跑若出現**上列以外**之任何提示或紅項 ⇒ **逐項具名回報**；紅項 ⇒ **停機上呈發單側**。

---

## §一　`N-1`　鑄 `自誤 241`

🔒 **取號現查（發單側·態 `ab5983f`·⛔ CC 須自行復現並二數並報）**

- **母體** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**（`🔧 自誤 取號之母體與零對照之二補款 一`）
- **框（逐字）** ＝ `` ^#+ 自誤 `?([0-9]{1,4})`? ``（`W-G.9-184` 所裁之寬形）
- **扣除集** ＝ 七則人造值（本框皆不命中）⇒ **未扣除與已扣除二數相同**
- **意指占用之現查**（母體 ＝ **全倉 `.md`**·錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·**二形並取**）：
  `自誤 241` 與 `` 自誤 `241` `` **二形皆 `0` 命中**；
  🔑 **對照組**（證量測器非紅）：`自誤 239` 二形分別為 `1`／`7` 命中 ⇒ **⛔ 非恆零**。

🛑 **鑄號前之範本**（`常規四（九）四`）：既有最末一則之標題形逐字 ＝
`` ### 自誤 `240`（發單側·出單窗）：**引用一則<u>已作廢</u>之條款去否定一則<u>現行</u>條款，並據以向 KL 出艙錯誤結論** ``

### `N-1` payload（🛑 **逐字追加於 `L-1` 檔尾**·⛔ 不改既有任何一字）

```
### 自誤 `241`（發單側·`W-G.9-192-PRE-c` 出單窗）：**單內之受詞檔集合以計數表述，而該計數與同單所令之落地點數不符**

**案由**　`docs/orders/W-G.9-192-PRE-c_KL裁K6B提前.md` 之 `§零` 將受詞檔表述為「`docs/rulings/**`／`CLAUDE.md`／**二**登記表」（＝ `4`）、`S-3` 稱「**四**受詞檔」；而同單之 `§一`／`§二`／`§三-1`／`§四-1`／`§五-1` **逐節可數為 `5` 個落地點**（`K-6` ＋ `CLAUDE.md` ＋ `VR`／`GB`／自誤三登記表）。
🔑 **攔截者 ＝ CC**（依 `常規二` 取保守項**全驗五檔**並逐字回報該互斥）——**其處置正確**。
🩸 **同族第四度**：本則與 `自誤 235`（取號之框非正典所裁且未宣告母體）／`自誤 238`（同一受詞出艙三數，各出自不同之框）／`自誤 239`（為某號類選用與正典現查相異之母體且未具名）**同族**——皆為「**出艙之數與其框／母體不同源**」。
🔴 **其中三則在同一張單內**（`W-G.9-192-PRE-c`）⇒ `CLAUDE.md`「立戒者在下一批最容易違反自己的戒」之**更強形**：**立戒者在同一批就會違反自己的戒。**
🔒 **一般式**　凡於單內以**計數**表述受詞檔集合者，須**同格逐一具名該集合之成員**，且**⛔ 不得**在同單他節另出一個未經同一次列舉所得之計數。⇒ **受詞檔一律以「具名表」出艙，計數（若出）須由該表逐列可數。**
🔒 **⛔ 不追改 `W-G.9-192-PRE-c` 一字**（`常規四（九）五`）——該單已入倉，其互斥留在原地作為引述。

```

---

## §二　`N-2`　鑄 `自誤 242`

🔒 **取號現查**：`自誤 242` 與 `` 自誤 `242` `` **二形皆 `0` 命中**（母體 ＝ 全倉 `.md`·錨定框·二形並取）；對照組同 `§一`。

🔑 **本則之受詞係<u>發單側自查所得</u>**（接手窗自倉現查·態 `ab5983f`），**⛔ 未出艙至 KL 前即攔下**。

### `N-2` payload（🛑 **逐字追加於 `L-1` 檔尾**·緊接 `N-1` 之後）

```
### 自誤 `242`（發單側·`W-G.9-192-PRE-c` 出單窗）：**把一則<u>已於五日前被證偽</u>之倉態陳述，原樣轉寫入 `CLAUDE.md` 與交接文**

**案由**　`CLAUDE.md` 之「🔧 待落地清單之更正三」（KL 裁 `2026-09-01`·`W-G.9-192-PRE-c`）表 `序 1` 之依賴欄逐字含「**`K-6 §一` 幾何連通判定已落地但⛔ 零接線（`k6_merge_groups`／`k6_shares_segment`）**」；`W-G.9-193` 發單側交接文 `§三-4` 表末列亦逐字載「**已落地但⛔ 零接線——段三須接上**」。
🔴 **該陳述於 `2026-09-01` 已為偽。**

🔒 **其為真之時點與被證偽之時點（皆現查可復現）**
① **立於 `f4c5385`（`W-G.9-131`·丙案）**——該批 commit message 逐字載「🔒 零接線之證＝二函式呼叫端命中 `0` 附判別力對照」⇒ **當時為真**。
② **被 `a2c4b2b`（`W-G.9-136`·`2026-08-26`·步驟 0 落地）證偽**——該批 commit message **自身**逐字載「`S-2` 逐街廓呼叫倉內既有 `k6_merge_groups`（⛔ 未另寫連通判定）」⇒ **接線之事實早在倉內、且係同一發單側所令**。
③ ⇒ **`2026-09-01` 之更正三與交接文，距其被證偽已 `5` 日**，且**⛔ 未重跑呼叫端現查**即原樣轉寫。

🔒 **現查（態 `ab5983f`·字樣錨·`grep -Fn`·⛔ 不以行號為活指標）**
| 受詞（逐字·`grep -F` 固定字串） | 檔 | 命中 |
|---|---|---|
| `for _g in k6_merge_groups(_pk, own_map, min_len):` | `app.py` | `1` |
| `temp_parcels, _diag_k6 = k6_step0_merge(` | `app.py` | `1` |
| `if not k6_step0_enabled():` | `app.py` | `1` |
| `temp_parcels = _annotate_temp_parcel_cut_type(temp_parcels)` | `app.py` | `1` |
| `temp_parcels = ns["_annotate_temp_parcel_cut_type"](temp_parcels)` | `verify/selection_pipeline.py` | `1` |

🔑 **判別力自檢（證上表之框⛔ 非恆為 `1`）**：寬字樣 `k6_merge_groups(` @ `app.py` ＝ `11`；`_annotate_temp_parcel_cut_type` @ `app.py` ＝ `4`。
🔒 **可達性鏈（逐段字樣錨）**：`k6_merge_groups` ← `k6_step0_merge` ← `_annotate_temp_parcel_cut_type` ← **app 路徑**（`app.py` 之 `temp_parcels = _annotate_temp_parcel_cut_type(temp_parcels)`）**及** harness 路徑（`verify/selection_pipeline.py` 之同名 `ns[...]` 呼叫）⇒ **二路徑皆執行**。
🔒 **閘之狀態**：`k6_step0_enabled()` 之預設為 `on`（環境變數 `WV_K6_STEP0` 未設時）⇒ **非旁路**。

🩸 **⛔ 非文字瑕疵——其有<u>施工後果</u>**：若 `K-6-B` 段三之施工單以「零接線」為前提出單，受單方之保守處置係**另建**一條連通判定；而倉內既有之 `k6_merge_groups` **本身⛔ 不濾街廓**（其僅依 `gid` 建圖），⇒ 段三所需者**⛔ 非新函式，而係換一組跨街廓之輸入 ＋ 上鎖濾網**。**以偽前提出單 ⇒ 受單方重複造輪，且二條連通判定必然分岔。**

🔒 **精確之陳述（取代上開偽陳述·⛔ 不追改原句）**
> `k6_merge_groups`／`k6_shares_segment` **已接線且二路徑皆執行**，惟其**唯一呼叫點在 `k6_step0_merge` 內、且輸入係<u>逐街廓</u>建構**（`for _lbl in sorted(_idx_by_blk)` 之內層）。⇒ **未接線者⛔ 非該二函式，而係「段三之跨街廓輸入」與「段一之上鎖濾網」**——後二者於 `app.py`／`verify/stepg_pipeline.py` 之正面列舉現查（`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` 六者）**逐檔皆 `0`**（對照組 `步驟 0` @ `app.py` ＝ `9` ⇒ 量測器非紅）。

🔒 **一般式**　凡轉寫他批之**倉態陳述**（「已落地」「零接線」「不存在」「未實作」等）者，須**於轉寫當次重跑其現查**並同格報框與命中數；⛔ **不得**以「該陳述已在 `CLAUDE.md`／正典內」為由免除。**倉態陳述之保鮮期 ＝ 下一次該受詞被改**（`行號衛生` 第 `1` 款之射程，自本則起**及於倉態陳述本身，⛔ 不限行號**）。
🩸 **併記**：本則與 `自誤 240`（引用一則已作廢之條款去否定一則現行條款）**同族**——皆為「**取一個曾經為真、今已不真之句子，當作現行依據**」。**⛔ 不同者**：`240` 之失效係「條款被明文作廢」，本則之失效係「**倉態被自己的下一批改掉**」，後者**無任何作廢公告可查** ⇒ **唯一守備 ＝ 當次重跑現查**。

```

---

## §三　`N-3`　鑄 `自誤 243`

🔒 **取號現查**：`自誤 243` 與 `` 自誤 `243` `` **二形皆 `0` 命中**；對照組同 `§一`。

### `N-3` payload（🛑 **逐字追加於 `L-1` 檔尾**·緊接 `N-2` 之後）

```
### 自誤 `243`（發單側·`W-G.9-193` 交接文出文窗）：**以「街角」為量詞出艙一個實為「街廓」之計數**

**案由**　`W-G.9-193` 發單側交接文 `§三-5` 末逐字載「🔒 **其餘五街角之 winner 皆於段一即定案**」。
🔴 **「五」係<u>街廓</u>之數，⛔ 非<u>街角</u>之數。**

🔒 **現查（態 `ab5983f`·母體 ＝ `verify/out/KL_UI_3.5m_7a6b7f3_第1宗街角地指配結果.csv` 單檔·逐列具名）**
| 街廓 | 左 | 右 |
|---|---|---|
| `R4` | winner | winner |
| `R6` | 無此側 | winner |
| `R5` | winner | 無此側 |
| `R2` | **⚠️ 強制抵費地** | 無此側 |
| `R3` | 無此側 | winner |
| `R1` | winner | winner |

⇒ **有街角之側共 `8`**（`R4` 二、`R1` 二、`R6`／`R5`／`R2`／`R3` 各一），其中 `R2 左` ＝ 強制抵費地 ⇒ **段一即定案者 ＝ `7` 個街角，分布於 `5` 個街廓**。
🔒 **⇒ 正確之陳述 ＝「其餘 `7` 街角（分布於 `5` 街廓）之 winner 皆於段一即定案」。**
🔒 **判別力自檢（證上表非杜撰）**：同目錄另二代 CSV（`…_2e08a41_…`／`…_a3c97aa_…`）之 `R2` 列與 `7a6b7f3` 代**逐格相同**（`R2`／`V13`／`🤖 自動 PK`／`14`／`308.17`／`無此側`／`0`／`⚠️ 強制抵費地`／`—`／`—`／`無此側`／`—`）⇒ 該格⛔ 非單代偶然。

🩸 **同族**：本則與 `自誤 236`（以同名不同量之集合成員資格論證另一集合之性質）**同族**——「街角」與「街廓」在本倉為**兩個不同基數之集合**（`8` vs `6`），而交接文以其一之量詞承載其二之數。
🔒 **⛔ 無土地後果**：該句之用途為「段三對其餘街角結構上不生效」之範圍陳述，`5` 或 `7` **不改變該陳述之真值**（段三僅處理「待處理街角」，而待處理者恰 `R2 左` 一個）。🛑 惟其**⛔ 不因無後果而免登**——量詞錯配之族已四度（`235`／`238`／`239`／`241`）。
🔒 **一般式**　凡出艙「其餘 N 個 X」之陳述者，須**同格具名該 `N` 個 X 之逐一列舉**，且列舉所用之母體須與 `X` 之量詞同基數。⛔ 不得以「街廓表」之列數充當「街角」之計數。

```

---

## §四　`N-4`　`CLAUDE.md` 末端追加（更正四）

🛑 **⛔ 不追改「🔧 待落地清單之更正三」一字**（append-only·`wg942_append_audit` 嚴格前綴）。

### `N-4` payload（🛑 **逐字追加於 `CLAUDE.md` 檔尾**）

```

---

## 🔧 待落地清單之更正四：**`k6_merge_groups`／`k6_shares_segment` 之「零接線」已失準**（`W-G.9-193`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為**加註**（`戒 36`：非鑄號者⛔ 不得採該類號之定義列式形）。

🩸 **被更正之對象**：本檔「🔧 待落地清單之更正三」表 `序 1` 依賴欄之一段，其逐字為
「`K-6 §一` 幾何連通判定已落地但**⛔ 零接線**（`k6_merge_groups`／`k6_shares_segment`）」。
🔒 **⛔ 不追改該段一字**（`行號衛生` 第 `5` 款：只能止血、不能回溯）。

🔴 **該陳述立於 `f4c5385`（`W-G.9-131`）時為真，已由 `a2c4b2b`（`W-G.9-136`·`2026-08-26` 步驟 0 落地）證偽**——`a2c4b2b` 之 commit message 自身逐字載「`S-2` 逐街廓呼叫倉內既有 `k6_merge_groups`（⛔ 未另寫連通判定）」。**更正三寫於 `2026-09-01`，距其被證偽已 `5` 日。** 登記 ＝ `自誤 242`。

🔒 **現行事實（字樣錨·⛔ 不用行號·`行號衛生` 第 `4` 款）**

| 字樣錨（`grep -F` 固定字串·⛔ 非 regex） | 檔 |
|---|---|
| `for _g in k6_merge_groups(_pk, own_map, min_len):` | `app.py` |
| `temp_parcels, _diag_k6 = k6_step0_merge(` | `app.py` |
| `if not k6_step0_enabled():` | `app.py` |
| `temp_parcels = _annotate_temp_parcel_cut_type(temp_parcels)` | `app.py` |
| `temp_parcels = ns["_annotate_temp_parcel_cut_type"](temp_parcels)` | `verify/selection_pipeline.py` |

🔑 **唯一性之判別力自檢**（`行號衛生` 第 `4` 款所令）：上表五錨於其檔各命中 `1`；寬字樣 `k6_merge_groups(` @ `app.py` ＝ `11`、`_annotate_temp_parcel_cut_type` @ `app.py` ＝ `4` ⇒ **該檢⛔ 非恆為 `1`**。

🔒 **⇒ 二函式已接線，且 app 路徑與 harness 路徑皆執行**（`k6_step0_enabled()` 預設 `on`）。

🛑 **`K-6-B` 段三之真正受詞（⛔ 非「新建連通判定」）**
1. `k6_merge_groups` **本身⛔ 不濾街廓**（其僅依 `gid` 建圖·`K-6 §一` 逐字「連通路徑得經 RD 片或其他公設片」由「⛔ 不濾使用分區」自動滿足）；**⇒ 跨街廓能力已在函式內。**
2. **缺者係其<u>輸入</u>與<u>濾網</u>**：現行唯一呼叫點在 `k6_step0_merge` 之**逐街廓**內層 ⇒ 段三須改以**跨街廓母體**餵入，並套 **段一之「上鎖」濾網**（未上鎖之他街廓片／RD 片／公設片）。
3. **正面列舉現查（態 `ab5983f`·二檔）**：`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` 六者於 `app.py` 與 `verify/stepg_pipeline.py` **逐檔皆 `0`**；對照組 `步驟 0` @ `app.py` ＝ `9` ⇒ **量測器非紅**。

🔒 **本節⛔ 不變更更正三之優先序**（`K-6-B` 段一〜段四仍為 `序 1`），亦⛔ 不變更其「幾何影響批」之性質。

```

---

## §五　收工閘（🛑 三欄交叉核對：`rc`／逐字結論列／`stderr` 位元組數）

### `S-1`　`L-1`／`L-2` 之 `deletions` 恆 ＝ `0`

```
git diff --numstat ab5983fb7c0800e9245347620eb06b831126115e -- "docs/reports/W-G.9波_claude.ai側自誤登記.md" CLAUDE.md
```
🛑 **判準**：二列之**刪除欄逐列為 `0`**（`常規一 ②` 補款③ `(3)` 之驗法·⛔ 不得用 `--cached`）。

### `S-2`　嚴格前綴（機械證既有檔未被改寫）

```
PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py
```
🛑 依 `常規五`：`rc≠0` 時**先判量測器紅抑或受詞紅**（讀 `stderr` traceback 之尾），⛔ 不得逕報「稽核失敗」。

### `S-3`　`常規四（九）二 ①`　**形之自證**

以自誤之正典定義列式掃 **payload 文字**，須命中 `241`／`242`／`243`：

```
PYTHONIOENCODING=utf-8 python - <<'PY'
import re
pat = r'^#+ 自誤 `?([0-9]{1,4})`?'
src = open('docs/reports/W-G.9波_claude.ai側自誤登記.md', encoding='utf-8').read()
hit = sorted({int(m) for m in re.findall(pat, src, flags=re.M)})
need = [241, 242, 243]
print('MAX =', max(hit))
print('新號皆命中 =', all(n in hit for n in need), [n for n in need if n in hit])
print('缺號集含新號 =', [n for n in need if n not in hit])
PY
```
🛑 **判準**：`新號皆命中 = True` ∧ `缺號集含新號 = []`。

### `S-4`　`常規四（九）二 ②`　**MAX 之推進**（🛑 入倉後重掃）

同 `S-3` 之腳本，於 **commit 之後**重跑：`MAX` 須**由 `240` 轉為 `243`**。
🛑 **⛔ 不得以「本地已改」代替入倉後之重掃**（`常規一` 補款③ 一般式：比較端須為具名 `commit` 之 blob）。

### `S-5`　零生產碼之逐 `commit` 出艙（🛑 含空輸出亦須貼）

```
git diff --name-only ab5983fb7c0800e9245347620eb06b831126115e..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```
🛑 **判準**：命中 `0` 行 ⇒ **零生產碼 commit ⇒ 逕行 `push`**。命中 `≥ 1` 行 ⇒ **停機上呈**（本單⛔ 不授權任何生產碼變更）。

### `S-6`　本單 `SELF_SHA256` 之自驗（逐字指令）

```
PYTHONIOENCODING=utf-8 python - <<'PY'
import hashlib
p = 'docs/orders/W-G.9-193_施工單_攢批登記與接線現況更正.md'
b = open(p, 'rb').read()
i = b.rfind(b'SELF_SHA256:')
print('declared  =', b[i:].split(b'\n')[0].decode().split(':', 1)[1].strip())
print('recomputed=', hashlib.sha256(b[:i]).hexdigest())
PY
```
🛑 **判準**：二值逐位相同。**不符即停機上呈**，⛔ 不得逕行入倉。

---

## §六　`push` 之處置

🔒 **本批四項齊備**（`常規一`）：① 零生產碼變更（`S-5` 證）② 純附加·`deletions ＝ 0`（`S-1`／`S-2` 證·補款② 明文「既有檔之純附加亦滿足 ②」）③ ⛔ 本批不動 `app.py` ⇒ 其期末 blob 恆 ＝ `da805fc276f0f2b7dbe7ec4738929c51973b12e0`（**絕對值出艙**·須實查）④ 開工／收工閘全綠。
⇒ 🟢 **逕行 `push`、⛔ 不必請示 KL。**

🛑 **例外**：`S-5` 命中 `≥ 1` 行、或任一閘不綠 ⇒ **停機上呈發單側**（⛔ 非 KL）。

---

## §七　執行報告（`A-2`）須含

1. `S-0a`〜`S-6` 逐閘之**三欄**：`rc` ／ **逐字結論列** ／ `stderr` 位元組數。
2. `S-0b` 之 `P-1`／`P-2`／`P-4`／`P-6` 提示**逐項具名**，並標「採納」或「具名豁免（附理由）」。⛔ 不得靜默略過。
3. **取號現查之二數並報**（未扣除／已扣除）＋ 所扣之值逐字（`🔧 常規四（八） 之補款` 受單方之義務）。
4. **意指占用之現查**：`241`／`242`／`243` 各以**二形並取**報命中數 ＋ **對照組**之非零命中數。
5. `git log -1 --format="%h %s"` 之**原樣輸出** ＋ 倉內報告路徑（`完工聊天回報`）。
6. 🔑 **凡發現本單自相矛盾者**：依 `常規二`，**取保守項續辦 ＋ 逐字回報該互斥**，⛔ 不上呈 KL。

---

## §八　⛔ 本單**不**授權之事

- ⛔ 不動 `app.py`／`verify/**` 任何一字（含註解）。
- ⛔ 不辦 `K-6-B` 任何段（段一〜段四）。
- ⛔ 不辦 `K-9-23`／`K-9-16`／`K-9-17`／`K-9-12`。
- ⛔ 不重產任何 baseline、⛔ 不重記期望 `FAIL` 名單。
- ⛔ 不追改「更正三」「更正二」「原清單」任何一字。
- ⛔ 不鑄 `GB`／`VR`／`戒`／`常規` 任何號。

🔒 **只有真觸及域邊界才停機上呈、純技術自行做完。**

SELF_SHA256: aa47627c206a6ee13646e0d60c8eb78752dec92e624040ef9b622544f80a4f28
