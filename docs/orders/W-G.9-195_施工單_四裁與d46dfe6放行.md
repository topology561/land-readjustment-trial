# 🔧 `W-G.9-195` 施工單：**發單側四裁 ＋ 自誤 `245` ＋ `GB-111` 加註**，並**放行 `d46dfe6`**

> **性質**：`commit 1` ＝ 🟢 **零生產碼**（逕行 `push`）；`commit 2` ＝ **推送 CC 已備妥之 `d46dfe6`**（🛑 **⛔ 不得重做、⛔ 不得改一字**）。
> **出單窗**：claude.ai（接 `W-G.9-194R`）
> **基座**：`wip/s1-endpart` ＝ `39f8fec`（🛑 開工時自行 `git ls-remote` 實查全 `40` 碼；不符即停機）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **前置之併記（發單側自倉復驗·⛔ 非採信自報·態 `39f8fec`）**：`f44e000`／`39f8fec` 二 commit 皆在 `origin`；`HEAD:app.py` blob ＝ `da805fc276f0f2b7dbe7ec4738929c51973b12e0`（＝基座值）⇒ **`d46dfe6` 確未 push**；生產碼判法（`daa618d..HEAD`）命中 `0` 行；`自誤 244`／`GB-134` 之 payload **逐字落入其登記表**（發單側自單內 fence 抽出後逐位比對 ＝ `True`）；二登記表刪除欄逐列 `0`。**CC 之回報全數坐實。**

---

## §零　落地點

| # | 檔／物 | 動作 | commit |
|---|---|---|---|
| `L-1` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 末端追加 `自誤 245` | `1` |
| `L-2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 末端追加 **`GB-111` 之加註**（🛑 **⛔ 鑄任何號**） | `1` |
| `L-3` | `CLAUDE.md` | 末端追加**發單側四裁** | `1` |
| `L-4` | **`d46dfe6`**（CC 已備妥之 `app.py` commit） | **`push`**（⛔ 不重做、⛔ 不 amend、⛔ 不改訊息） | `2` |

| `A-1` | `docs/orders/W-G.9-195_施工單_四裁與d46dfe6放行.md` | 本單以原名入倉（併入 `commit 1`） |
| `A-2` | `docs/reports/W-G.9-195R_四裁與d46dfe6放行_執行報告.md` | 執行報告（🛑 置於 `commit 2` **之後**·承 `W-G.9-194R` 之偏離處置） |

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | `git ls-remote … refs/heads/wip/s1-endpart` | 逐字 ＝ `39f8fec` 之全 `40` 碼（🛑 自查·⛔ 不以本單所載之短碼為錨·`常規九 三`） |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-195_施工單_四裁與d46dfe6放行.md` | `P-3`／`P-5` 須不紅；🟡 逐項具名（見 `§零-c`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗 | `declared` ＝ `recomputed` |
| **`S-0d`** | 🔴 **`d46dfe6` 之同一性**：`git cat-file -t d46dfe6` 存在 ∧ `git rev-parse d46dfe6^` ＝ `f44e000` ∧ `git diff --name-only f44e000..d46dfe6` **逐字僅 `app.py` 一列** ∧ `git rev-parse d46dfe6:app.py` **⛔ ≠ `da805fc276f0f2b7dbe7ec4738929c51973b12e0`** | 四項齊備；任一不符 ⇒ **停機上呈發單側**，⛔ 不得自行重建 |

🔒 **`S-0d` 之立法理由**：本單所放行者係**那一個已被 `W-G.9-194R` 全閘量過之物件**，⛔ 非「一個內容相同的新 commit」。⇒ 其同一性須機械證，⛔ 不得以「我重做一個一樣的」代之。

---

## §零-c　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6` 六檢·🔴 機械停機款 `0` 項）

🛑 承 `GB-134`：`P-2` 對本倉主流語序結構上盲 ⇒ 本單一切計數**皆於同段自載其框係 `列` 抑或 `次`**，⛔ 不以 `P-2` 未提示為據。
| 檢 | 觸發點（字樣錨） | 發單側之處置 |
|---|---|---|
| `P-1` | `` 🔒 **本批新增之 `128` 列已逐格歸因 `` | ✅ **已採納**——同段已補具名母體（不截斷違規全集之差集）與所查範圍（該四閘） |
| `P-4` | `` **處置**：`S-0d` 四項齊備 `` | 🔒 **具名豁免**：該 `⇒` 係**條件之推論連詞**，⛔ 非方向性量之轉引；本單全篇之「改前 → 改後」方向已於 `GB-111` 加註內逐字宣告 |
| `P-6` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§五` 表頭） | 🔒 **具名豁免**：`§五` 為閘之**索引表**，每一閘之實測出處逐列具名於同表「受詞」欄；⛔ 無任何停機款之數僅見於表頭 |

🛑 CC 實跑若出現**上表以外**之 🟡，逐項具名回報；🔴 ⇒ 停機上呈。

---

## §一　`N-1`　鑄 `自誤 245`（`commit 1`）

🔒 **取號現查（發單側·態 `39f8fec`）**
- 母體 ＝ 自誤登記表**單檔**；框 ＝ `` ^#+ 自誤 `?([0-9]{1,4})`? ``；**MAX ＝ `244`**（未扣除／已扣除二數相同）⇒ 次號 **`245`**
- **意指占用**（全倉 `.md`·錨定框·二形並取·框 ＝ **`列`**）：`自誤 245` ＝ `0`／`` 自誤 `245` `` ＝ **`1`**
  🔒 **該 `1` 命中之逐筆歸類 ＝ 「零對照」** ⇒ 依 `CLAUDE.md` `🔧 二 一`（`W-G.9-192-PRE-b`）**⛔ 不構成意指占用、⛔ 不順延**：
  出處 ＝ `docs/reports/W-G.9-194R_GB133解除與攢批登記_執行報告.md`，其逐字 ＝ `` **判別力對照**（未鑄之 自誤 `245`／`GB-135`）皆 `False` ``
  🔑 **對照組**：`自誤 244` ＝ `10`／`4` ⇒ 量測器非紅
- **範本（`常規四（九）四`）**：`` ### 自誤 `244`（發單側·`W-G.9-193` 出單窗）：**出艙一個「命中數」而⛔ 未宣告其框係<u>列數</u>抑或<u>次數</u>** ``

### `N-1` payload（🛑 逐字追加於 `L-1` 檔尾）

```
### 自誤 `245`（發單側·`W-G.9-194` 出單窗）：**寫下一條倉內<u>已登記為結構上不可滿足</u>之驗收判準，使該閘恆真**

**案由**　`docs/orders/W-G.9-194_施工單_GB133解除與攢批登記.md` 之 `§四-2（二）` 第 `5` 款，其**情形**欄逐字 ＝ 「`S-9` 出現類 III」，**理由**欄逐字 ＝ 「既有紅之組成已變 ⇒ ⛔ 非『與凍存名單逐項相同』」。
🔴 **二欄互斥**：「出現類 III」係**絕對量**，「組成已變」係**相對於基座之差量**。本倉之基座**即有**類 III ⇒ 依情形欄之字面，該款**結構上不可能不觸發** ⇒ **該閘恆真、判別力為零**。
🔴 **且該不可滿足性倉內<u>早有登記</u>**：`GB-111` 逐字載凍存期望 `FAIL` 名單「**⛔ 無結構化之原因欄** ⇒ **結構上無法滿足 `CLAUDE.md:76-78`（KL 裁 `2026-08-11`）之『名稱＋原因一併比對』**」，其**失效條件**繫於**波末重烤**（時點已由 KL 裁「俟二閘落地後」）。⇒ **發單側寫該款時⛔ 未查 `GB-111`，亦⛔ 未查 `CLAUDE.md` 之分支狀態宣告。**
🩸 **⛔ 非新戒——係違反一則<u>倉內既有</u>之無編號之戒**：`docs/reports/W-G.9波_claude.ai側自誤登記.md:3702` 逐字「🛑 凡於單中寫下**驗收判準**者，須先現查 `CLAUDE.md` 之**分支狀態宣告**（准紅碼／全綠／其他），並於單內標『✅ 判準已對分支狀態現查·態 ＝ `<commit>`』；⛔ 不得預設『綠』為常態。」⇒ **本則係該戒立後之首宗違反**（該戒自身⛔ 不占號·`檔:列` 錨定）。
🔑 **攔截者 ＝ CC**——其依單之保守項**停機備妥不推**，**同時**逐字回報「第 `5` 款所載之理由『既有紅之組成已變』**為偽**」並附二態對拍（對帳段 `117` **列**二態逐位相同·判別力對照 `False`）。**其處置正確**：⛔ 未為使其轉綠而動任何濾式／容差／斷言，亦⛔ 未逕自認定該款無效而放行。
🔒 **⛔ 無土地後果**：`D-3` 實測全 `12` 側之 `winner`／`qualified`／`note`／`group_size` 逐格不變。本則之後果係**一次不必要之停機**（成本落在受單方）。
🩸 **同族**：與 `自誤 240`（引已作廢之條款）／`242`（轉寫已被證偽之倉態陳述）**同族**——皆為「**倉內查得到，而發單側未查**」。🔴 **⛔ 不同者**：`240`／`242` 之受詞係**他人所立之句**，本則之受詞係**發單側自己正在寫的驗收判準**——即戒之**射程最短**、最應現查之處。
🔒 **一般式**　凡於單中寫下停機款者，其**情形欄**與**理由欄**須為**同一述詞**；二者若一為絕對量、一為差量，須擇一並刪其他。⇒ 出單前之自檢問句 ＝ **「把基座原封不動地餵進這一款，它會不會觸發？」——會，即為恆真閘，⛔ 不得列為停機款。**

```

---

## §二　`N-2`　`GB-111` 之末端加註（`commit 1`）

🛑 **本節⛔ 鑄任何號**——其為**加註**（`戒 36`：非鑄號者⛔ 不得採該類號之定義列式形，本倉體例 ＝ `🔬`／`🔧` 起首）。
🛑 **⛔ 不追改 `GB-111` 本文一字**（append-only）。

### `N-2` payload（🛑 逐字追加於 `L-2` 檔尾）

```

### 🔧 `GB-111` 之加註（`W-G.9-195`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為加註（`戒 36`）。

🩸 **`W-G.9-194R` 之實測，為 `GB-111` 添一則<u>更強</u>之佐證**：本批（`GB-133` 解除）使四閘之違規總計由 `72`→`96`／`72`→`96`／`64`→`104`／`64`→`104`（**方向 ＝ 改前 → 改後**），新增 `24`／`24`／`40`／`40`、**消失 `0`**；而**對帳器 `verify/wv_reconcile.py` 之對帳段 `117` <u>列</u>二態逐位相同**（判別力對照 `False`）。

🔴 **⇒ 一次<u>真實且已具名</u>之行為變更，於對帳器上完全不可見。**
🔒 **成因⛔ 非對帳器有瑕疵，而係 `GB-111` 本文所載之同一根因**：凍存名單**⛔ 無結構化之原因欄** ⇒ 「原因」之比較端不含**違規列數** ⇒ 同一名目下違規列由 `72` 變 `96`，對帳仍為綠。**此即 `CLAUDE.md:76-78`（`W-G.9-6` 實證「同一名目下換一個全新的破損，對帳仍為綠」）之同形第二處。**

🔒 **本批新增之 `128` 列已逐格歸因（⛔ 非未明之紅）**——**母體 ＝ `probe_WG9168_viol_full` 所取之<u>不截斷</u>違規全集（二態各一份）之差集**（其比較式係 `import run_verification.diff_rows`·⛔ 非自寫）；**所查範圍 ＝ 該四閘**：逐格全部為 `baseline='—' got=<數值>`，且**僅出現於落選（未達標）候選之四指數欄**（`0m` `6` 列 × `4` 欄 ＝ `24`；`3.5m` `10` 列 × `4` 欄 ＝ `40`；二退縮各二閘 ⇒ 合 `128`）。**⛔ 無任何 `winner`／達標候選列之欄值改變。**
🔒 ⇒ **係 `GB-133` 登記本文所<u>預告</u>之行為變更**（「該移動會改變既有出艙 ⇒ 屬行為變更，須以判別力閘看守」），**⛔ 非新破損**。

🛑 **`GB-111` 之失效條件⛔ 不因本節而變**——仍為 ① 波末重烤後重產該名單 **且** ② 新名單逐列含名稱＋原因二欄；**重產時點仍繫於 KL 已裁之「俟二閘落地後」**，本節⛔ 不變更之。
🔒 **本節之作用**：使**重產後之新名單**負一項明文義務——**其「原因」欄須能承載「違規列數」**，否則本節所載之盲區於重產後**原樣留存**。

```

---

## §三　`N-3`　發單側之四裁（`commit 1`·追加於 `CLAUDE.md` 檔尾）

🔒 **依據**：`常規二`（施工單內部條款互斥者，由**發單側當場裁**並具名為發單側自誤；⛔ 不上呈 KL）＋ `常規三`（無土地後果之事項，發單側核可；⛔ 不上呈 KL）。

### `N-3` payload（🛑 逐字追加於 `L-3` 檔尾）

```

---

## 🔧 `W-G.9-194` 之四裁（發單側·`W-G.9-195`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為裁之落地（`戒 36`）。

### 🔧 裁 一　`W-G.9-194 §四-2（二）` 第 `5` 款之**情形欄無效**，operative 述詞 ＝ **理由欄**

🔒 **依據三項（皆現查可復現）**
1. **本檔之分支狀態宣告**（`准紅碼`）逐字：「期間之驗收改為**與凍存之期望 FAIL 名單逐項相同**，⛔ **非「全綠」**」——而該款情形欄之字面**要求類 III ＝ `0`**，即要求全綠 ⇒ **與上位權威牴觸**。
2. **`GB-111`** 逐字登記該名單「**結構上無法滿足**『名稱＋原因一併比對』」，其失效條件繫於**波末重烤** ⇒ 該款於重烤前**結構上不可能通過**。
3. **`常規二`**：情形欄（絕對量）與理由欄（差量）**互斥** ⇒ 由發單側當場裁。

🔒 **裁**：**該款之 operative 述詞 ＝ 「類 III 之組成<u>相對基座</u>有變」**；情形欄之「出現類 III」**⛔ 不生效**。
🔒 **適用於 `W-G.9-194`**：CC 實測二態（基座 `app.py` blob `da805fc2…` vs 本批態）之**對帳段 `117` 列逐位相同**、相異列**無**、判別力對照 `False` ⇒ 類 III `13` 項係**基座既有** ⇒ 🔒 **第 `5` 款<u>未觸發</u>**。
🔒 ⇒ `§四-2（二）` 五款**全數未觸發** ⇒ **KL 之預先核可（`2026-09-01` 逐字「準改動推上去」）生效** ⇒ `d46dfe6` **准 `push`**。
🛑 **本裁⛔ 不放寬 `§四-2（二）` 之其餘四款一字**，亦⛔ 不使「發單側事後改判準」成為常態——其可行**專因**該款自身互斥且牴觸上位權威；**⛔ 不得類推至任何非互斥之停機款**。登記 ＝ `自誤 245`。

### 🔧 裁 二　本批之 `128` 列違規增量 ＝ **行為變更**，⛔ **非土地後果**

🔒 `§四-2（二）` 第 `2` 款之**受詞逐字** ＝ 「任一街角之 `winner` 或其 `priority_index` 有變」；CC 實測全 `12` 側之 `winner`／`qualified`／`note`／`group_size` **逐格不變**、`7` 個有得標街角之 `winner` 與其 `priority_index` **逐格不變** ⇒ **未觸發**。
🔒 增量逐格為**落選候選之診斷欄** `—` → 數值（方向 ＝ 改前 → 改後），係 `GB-133` 本文所**預告**者 ⇒ **⛔ 非新破損**。
🛑 **⛔ 不變更 baseline 重烤之時點**（KL 已裁「俟二閘落地後」·`GB-111`）——本裁只**登記**其迫切性，加註見 `GB-111` 末端。

### 🔧 裁 三　`# 🆕 V13 修正 #3 + #7：**第二關**優先權指數…` 一段之措辭

🔒 **`常規三`（無土地後果）** ⇒ 發單側**核可其修正**：該段現位於早退之前，「第二關」之名對落選候選略不精確，宜改述。
🔴 **惟 CC 建議之「列次一<u>零生產碼</u> commit」<u>為誤</u>**——`常規一` 補款之**生產碼判法逐字以<u>檔名</u>為準**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`），**⛔ 不分碼與註解**；且 `docs/reports/W-G.9-183R…:19` 逐字「凡須修改 `app.py` … **一字（含註解）**」。⇒ **動該註解一字即係生產碼 commit。**
🔒 ⇒ **併入次一動 `app.py` 之批**（`K-6-B` 段一），⛔ 不另開 commit、⛔ 不於本批辦。**CC 於 `W-G.9-194` 內⛔ 未動該段（逾核可射程）之判斷正確。**

### 🔧 裁 四　`W-G.9-194R` 之 commit 順序偏離 ＝ **處置正確**

🔒 單 `§零` 令 `A-2` 為 `commit 3`（置於 `app.py` commit 之後）；CC 依 `push` 判單位 ＝ **ref 推進區間** 判定「置後之報告將**結構上不可 push**、發單側無從自倉讀取」，遂**報告先行入倉、`app.py` commit 置其後備妥**，並依 `常規二` **取保守項 ＋ 逐字回報 ＋ ⛔ 未改單一字**。
🔒 **裁：其處置正確**，本裁採為明文——🛑 **凡單所令之 commit 順序，會使「回報所繫之報告」落在<u>不可 push 之區間</u>者，受單方應<u>先推報告</u>並逐字回報該偏離。** 出單側之對應義務：**排序時須先問「這張報告推得出去嗎？」**

```

---

## §四　`N-4`　放行 `d46dfe6`（`commit 2`）

🛑 **⛔ 不得重做、⛔ 不得 `amend`、⛔ 不得改其訊息一字。** 其為 `W-G.9-194R` 全閘所量之**同一物件**（`S-0d` 機械證之）。

**處置**：`S-0d` 四項齊備 ∧ `commit 1` 已 push ⇒ **逕行 `push d46dfe6`**。
🔒 **授權之依據鏈（逐字）**：KL `2026-09-01`「準改動推上去」→ 射程 ＝ `W-G.9-194 §四-2（二）` 五款 → 五款全數未觸發（第 `5` 款依**裁 一**）⇒ 核可生效。
🛑 **⛔ 不得**因本單而推送任何**其他**未經 `W-G.9-194` 量過之 `app.py` 變更。

---

## §五　收工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `T-1` | `git diff --numstat 39f8fec -- <L-1> <L-2> <L-3>` | 三列**刪除欄逐列 `0`** |
| `T-2` | `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py` | 依 `常規五` 先判量測器紅抑或受詞紅 |
| `T-3` | **形之自證**：自誤寬形掃 `L-1` | `245` 命中；🔑 判別力對照（未鑄之 `246`）須 `False` |
| `T-4` | **MAX 之推進**（🛑 入倉後重掃） | 自誤 `244` → `245`，且 `245` ⛔ 不在缺號集 |
| `T-5` | `L-2`／`L-3` 之加註**⛔ 未鑄號** | `GB` MAX 仍 `134`（⛔ 未動）；`常規`／`戒`／`VR` 之 MAX 皆⛔ 未動——**四類逐類報 MAX 之改前／改後二值** |
| `T-6` | 逐 `commit` 之生產碼判法輸出（**含空輸出**） | `commit 1` 命中 `0` 行；`commit 2` 命中 `1` 行（`app.py`·**係本單所放行者**） |
| `T-7` | 🔴 **期末 blob 對拍** | `git rev-parse origin/wip/s1-endpart:app.py` ＝ `d46dfe6:app.py`（逐位·**絕對值出艙**） |
| `T-8` | `git rev-list --left-right --count origin/wip/s1-endpart...HEAD` | ＝ `0 0` |

🛑 **`T-5` 之立法理由**：本單有**二處加註**（`L-2`／`L-3`）採 `🔧` 起首之非鑄號形；`GB-132` 類 ② 之受詞即「payload 內之真定義列式使 MAX 提前推進」⇒ 須機械證本批**未污染任何號類之 MAX**。

---

## §六　執行報告（`A-2`·🛑 置於 `commit 2` 之後）須含

1. `S-0a`〜`S-0d`、`T-1`〜`T-8` 逐閘之**三欄**：`rc` ／ **逐字結論列** ／ `stderr` 位元組數。
2. `S-0d` 四項**逐項出艙其實查值**（含 `d46dfe6^`、`d46dfe6:app.py` 之全 `40` 碼）。
3. 🛑 **凡出艙任何計數，同段宣告其框係 `列` 抑或 `次`**（`自誤 244` 之一般式·⛔ 不倚賴 `P-2`·`GB-134`）。
4. 取號之**二數並報** ＋ **意指占用之二形並取** ＋ **零對照之逐筆歸類** ＋ 對照組之非零命中。
5. `T-5` 之四類 MAX 改前／改後二值。
6. 🛑 **harness／KL UI 二通道⛔ 不得混列**——本批之 `push` **⛔ 不產生任何新的土地後果證據**；`第1宗街角地指配結果.csv` 之倉內三代**皆為改前態**，**土地後果待 KL 實跑**（`GB-123`）。
7. `git log -1 --format="%h %s"` 之**原樣輸出** ＋ 倉內報告路徑。
8. 依 `常規二`：發現本單自相矛盾者，**取保守項續辦 ＋ 逐字回報**，⛔ 不上呈 KL。

---

## §七　⛔ 本單**不**授權之事

- ⛔ 不改 `d46dfe6` 之內容或訊息一字。
- ⛔ 不動 `app.py` 於 `d46dfe6` 以外之任何一字（含 `裁 三` 所核可之註解——其列**次一批**）。
- ⛔ 不動 `verify/**` 任何一字。
- ⛔ 不辦 `K-6-B` 段一「上鎖」／段二／段三／段四。
- ⛔ 不重產任何 baseline、⛔ 不重記期望 `FAIL` 名單（`GB-111` 之失效條件⛔ 未成就）。
- ⛔ 不追改 `GB-111`／`GB-133`／`W-G.9-194` 任何既有一字。

🔒 **只有真觸及域邊界才停機上呈、純技術自行做完。**

SELF_SHA256: b849bb768c83ba45b65c95a5500363f3223affebbf4b178d53f412818347098a
