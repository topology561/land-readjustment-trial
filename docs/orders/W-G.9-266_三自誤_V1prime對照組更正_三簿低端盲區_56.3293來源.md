# W-G.9-266　三自誤之鑄 ＋ `V-1′` 對照組更正 ＋ 三簿低端盲區之裁 ＋ `56.3293` 之來源追溯（**中級批**）

> 🛑 **零生產碼**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 一字不動·含註解）。
> 🛑 **零土地後果**：⛔ 動任何一宗地之 `G`／面積／街角歸屬／抵費地／配地幾何。
> 🛑 **⛔ 含修法主張**；**⛔ 決定 `GB-67`／`telescoping` 是否開修**；**⛔ 開工 `c2`**。
> 🔒 本批之級 ＝ **中級**（純登記 ＋ 純診斷）⇒ `run_verification.py` **全批只跑一次**，落檔全批共用。

---

## §零　工項零：二檔原封入倉

**a**　本單 ⇒ `docs/orders/W-G.9-266_三自誤_V1prime對照組更正_三簿低端盲區_56.3293來源.md`。
**b**　**審查窗之 `-266` 報告與草稿全文**原封入倉 ⇒ `docs/orders/W-G.9-266_附件甲_審查窗報告與草稿.md`，⛔ 改一字。
　　🔒 其地位 ＝ 本單之**受詞**；與本單相異處，**本單勝**。

出艙：來源／落檔／入倉 blob 之 `sha256` 三者逐位相符，並載 **blob 位元組數**／`LF`／`CR`，**明載其為 blob 層**。

---

## §一　開工閘（中級·逐條當場復現·任一不符 ⇒ 停機上呈）

| # | 閘 | 應得 |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` ＝ 本地 `HEAD` | 逐位相同 |
| `2` | `app.py`／`verify/stepg_pipeline.py` blob `sha256[:16]` 當場重算並出艙 | 異於開工值 ⇒ 停 |
| `3` | 四簿當場復算（框逐字 ＋ 母體 ＋ 三軸） | 見 `3-a` |
| `4` | 號占用：`W-G.9-266` 六數分列皆 `0`；對照甲 `-265` `≥ 1`；對照乙人造號 `= 0` | 見 `4-a` |
| `5` | 本單原封入倉，入倉 blob `sha256` ＝ 落檔前 `sha256` | 不符 ⇒ 停 |
| `6` | `verify/probes/probe_order_preflight.py <本單>` 之 `P-3`／`P-5` | 紅 ⇒ 停；惟見 `6-a` |
| `7` | `run_verification.py` **全批一次**：`rc`／`stderr` 位元組／`RESULT` 逐字／紅項數 ＋ **計數式** ＋ 第二態同式結果 | 見 `7-a` |

**`3-a`**（發單側於態 `5ff9f409a53f739ada077305adf793e2d8452df6` 實測·**⛔ 採信，須當場復算**）：

| 簿 | 相異 | `MAX` | `MIN` | 缺號〔`[MIN..MAX]`〕|
|---|---|---|---|---|
| 自誤 | `320` | `327` | `7` | `[106]` |
| `GB`（`GB-116` 三框聯集）| `153` | `155` | `1` | `[12, 87]` |
| `VR`（扣哨兵 `999`）| `79` | `94` | `14` | `[73, 75]` |
| `K-9` | `28` | `29` | `2` | `[]` |

🛑 缺號集取 `[MIN..MAX]`（`VR-092`）——**惟見 §四 之裁：該慣例於低端有盲區，本批加註而⛔ 廢**。
🛑 **⛔ 以恆等式 `True` 為框正確之證據**（`自誤 325`）。

**`4-a`**（同態實測·母體 ＝ 全 `docs/` 之倉內清單·讀不到者逐項具名其副檔名）：
`W-G.9-266` ＝ `0/0/0/0/0/0`（鬆框亦 `0/0`）；`W-G.9-265` ＝ `D1 3`／`D2 10`／`D3 6`／雙屬 `0`／列框 `16`／檔框 `5`。
🩸 **自指款**：若本單（或附件甲）已入倉致 `266 ≠ 0`，**⛔ 改號**；改判「占用者是否全為本單自身之產物」，是則過閘。

**`6-a`**（承 `-265 §一 6-a`）：`P-5` 不符時先**多框並列復算**。**所查之框集（母體·逐字·⛔ 少於此）**：①`SELF_SHA256` 列以前之全部位元組（＝ `P-5` 之框）／②同上但⛔ 含末一個 `\n`／③自檔首至該列前一**非空**列末（去尾隨空白列）／④全檔（含 `SELF_SHA256` 列）／⑤以 `CRLF` 正規化後之①／⑥以 `CRLF` 正規化後之②。任一框命中單載值 ⇒ 判**框之別**，記入自解清單續辦，⛔ 動 `P-5` 一字、⛔ 改本單一字。**該六框皆不命中 ⇒ 傳輸損毀，停機上呈**（須出艙六框之逐框實算值）。

**`7-a`**：發單側先前三態實測皆 `rc=1`／`stderr 0 B`／`RESULT: FAIL`／紅項 `24`，名目集合雙向差集皆空。
🛑 **計數式須逐字出艙**——`^🔴 FAIL` 於本 log 得 `0`（該類列有縮排）係**框之別、⛔ 態之別**；⛔ 以 log 列號為錨（其係 clone 路徑長度之函數）。

---

## §二　工項一：鑄 `自誤 328`／`329`／`330`（責 ＝ 發單側·純登記·檔末追加）

落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md`。標題形取既有最末一則為範本。⛔ 動既有一字。

> ### 🩸 `自誤 328`　**以未經量測之斷言充作閘之陰性對照基礎**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-266`·`2026-09-10`。
>
> **受詞**：`W-G.9-264 §三 (c)` 與 `W-G.9-265 §九 (c)` 逐字令「陰性 ＝ 同二態之 `3.5m·R2·right` 七宗（**該側二態逐位相同**）」。括號內之斷言**發單側從未量測**。
> 實測（`023765c^` ＝ `70ce7d46b27a1c9f5fd057f697ba3b7f382af7ad` → `e9d1b9c3e6e46580da28392e1ed7495d5b34638f`）：**`R2·right` 七宗之 `cut_coords` 逐位相同 ＝ `0/7`**，而其 `ΔG` 最大 `0.0000`。
> ⇒ 該側係「**`G` 於 `2dp` 不變而幾何全變**」之組，**⛔ 未動之組**；以之為陰性，`(1b)`／`(3)` 結構上必然雙紅。
>
> 🔴 **誤之所在**：閘之陰性對照係**判別力之全部依據**；其前提為未量之斷言 ⇒ 該閘之「失能」判定**⛔ 可信**，而 `-265R` 據以將 `(1b)`／`(3)` 記為「於 `1e-6` 下⛔ 區辨」——**係對照組之誤所生之假象**。
> 🔒 **CC 之處置正確**：其照實測、照實紅、**⛔ 自行放寬判準**，並具名底噪後上呈（`-265 §十一 8` 逐字）。
> 🔒 **通則（自本則起）**：凡新立之閘，其陰性對照之受詞須為**已證未動**者——須出艙該受詞二態之**逐位比對結果**（`cut_coords` 或等價之全精度幾何），**⛔ 以「應該沒動」之判斷充之**。

> ### 🩸 `自誤 329`　**指定對照組時未驗其判定集非空，致空集得以「通過」之姿混入**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-266`·`2026-09-10`。**由審查窗於出單前捕獲**（記功）。
>
> **受詞**：發單側於 `-266` 開場文令「真陰性 ＝ `3.5m·R1`（`left 1` 宗 ＋ `right 4` 宗）」。實測 `3.5m·R1·left` **僅 `1` 宗** `628-36(1)`，且其 `驗_宗序 ＝ 街角第1宗`（＝ 鏈頭）⇒ **非鏈頭 `0` 宗** ⇒ `V-1′ (3)` 回傳 `None`、三款一列未印。
> 若判別力寫作 `(陽性.n5 > 0) and (陰性.n5 == 0)`，該組將以**判定集為空**之姿「通過」。
>
> 🔴 **誤之所在**：對照組係以**容器**（街廓·側）指定，而未驗該容器內**判定集**是否非空。空集之「無反例」與真陰性之「無反例」在輸出上不可區分。
> 🔒 **同族**：`自誤 328`（對照組之前提未驗）。二者共根 ＝ **指定對照組而未驗其性質**。
> 🔒 **通則（自本則起）**：凡指定對照組，須同時出艙其**判定集之基數**；基數 `= 0` ⇒ **loud 拒測**，⛔ 計為通過、⛔ 靜默略。

> ### 🩸 `自誤 330`　**裁一族之結構缺陷時只掃一簿，未掃同族之其餘各簿**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-266`·`2026-09-10`。**由審查窗捕獲**（記功）。
>
> **受詞**：`W-G.9-265 §三 三-3` 裁定 `自誤 1`–`6` 係「已鑄而定義框結構上撈不到」，另立靜態清單；同單 `三-1` 裁缺號集取 `[MIN..MAX]`（`VR-092`）、`三-4` 裁 `GB` 取三框聯集**扣哨兵 `997`**（`VR-093`）。發單側**未對 `GB`／`VR`／`K-9` 三簿施同一檢查**。實測（態 `5ff9f40`）：
> - **`K-9-1` 定義框命中 `0`**，其全檔僅 `2` 列且**皆表列式**；`K-9` 之 `MIN ＝ 2` ⇒ `[MIN..MAX]` 得 `[]`，`[1..MAX]` 才得 `[1]`。⇒ **`VR-092` 之慣例於低端恰好掩蓋之**，而 `K-9-1` 係已裁之則（「街角第 1 宗**不豁免**街廓最小建築面積」·`CLAUDE.md` 列其為 🔴 **有土地後果**·⬜ 未實作）。
> - **`GB` 之「扣哨兵 `997`」為空操作**：`997` **不在**三框聯集內；而 `VR` 之 `999` **在**其定義框內 ⇒ 二簿不對稱，而 `VR-093` 以同一句話涵蓋之。
>
> 🔴 **誤之所在**：`自誤 1`–`6` 之缺陷為**結構性**（表列式 vs 定義框），其成立與否只繫於「該簿是否有表列式之則」，**⛔ 繫於簿別** ⇒ 一經發現即應**同批掃全部四簿**。
> 🔒 **同族**：家法既有之「同族缺陷須一批掃，⛔ 一次打一個」。
> 🔒 **通則（自本則起）**：凡裁一**結構性**缺陷，須於同批出艙**同族全部母體之逐簿檢查表**（含判為「無此缺陷」者及其機械證據），⛔ 只掃觸發該裁之那一簿。

**(b) 驗收閘**：三則標題須被定義框命中；判別力對照 ＝ 未鑄之 `331` 須命中 `0`。落地後自誤 相異 ＝ `323`／`MAX 330`／`MIN 7`／缺號 `[106]`。
**(c) 否證對照**：以已廢止之寬形重算，其相異須**小於**正字框。

---

## §三　工項二：`-265R` 之就地加註（**乙**·⛔ 改 `-265R` 原文一字）

落點 ＝ `docs/reports/W-G.9-265R_自解款與五裁與覆蓋帳.md` **檔末追加**。⛔ 鑄號。

**受詞**：`-265R §九` 逐字載「本器所量之 `s` 位置有一 `~4e-4 m` 之底噪（來源 ＝ `G` 於 `tol` 下之非確定性·`GB-101`）」。

**加註須含四項（逐項附機械證據）**：
1. **同主機同碼同輸入為確定性**：生產碼十一檔逐檔相同之 `e9d1b9c` 與 `2d1eaf4`，二情境落檔 `sha256` **逐位相同**（`98aebf88260047b5`／`437a07a475149f19`）。
2. **判別力（決定性）**：同探針同二態，`3.5m·R1·right` 三宗之 `δ` 皆 `+0.000000000`、極差 `0.000000000`；`3.5m·R2·right` 為 `3.89e-4`。⇒ **該現象挑街廓** ⇒ 噪音假說被否證。
3. **`GB-101` 之界（照實·⛔ 主張其為誤）**：`GB-101` 逐字之機制為 `solve_G_binary` 二分法「**停點取決於迭代軌跡（初始區間／`S_max_limit`）**」，其坐實證據係 **KL 之 `g_rows` JSON 對 CC 實測**＝**跨環境**；量級為 `S` 差 `0.01`／面積差 `0.04 ㎡`，較 `~4e-4 m` 大約 **`25` 倍**。⇒ 本項所測之同主機確定性與 `GB-101` **⛔ 相斥**；`GB-101` 於跨環境**仍成立**。
4. **正確之對應條目 ＝ `GB-102`**（「法定記載值（長度記至二位小數）落於四捨五入之刀鋒 ⇒ **亞毫米級之幾何差即翻動該值**」）——`R2·right` 之 `G` 於 `2dp` 不變而 `cut_coords` 逐位全異，正是該形。

🛑 **本加註⛔ 更動 `GB-101`／`GB-102` 之狀態欄、⛔ 判其解除、⛔ 改 `-265R` 任一字。**

---

## §四　工項三：`V-1′` 之三組對照重跑 ＋ `VR-094` 末端追加（**丙**）

**(a) 受詞**（**逐字·⛔ 以容器名代之**）：

| 角色 | 受詞逐字 | 判定集基數（非鏈頭宗數）|
|---|---|---|
| 陽性 | `3.5m`·`R2`·`left` | `6` |
| **真陰性** | **`3.5m`·`R1`·`right`** | **`3`** |
| 第三類 | `3.5m`·`R2`·`right` | `6` |

🛑 **陰性⛔ 寫作「`3.5m·R1`」**——`3.5m·R1·left` 僅 `1` 宗且即鏈頭 ⇒ 非鏈頭 `0` 宗、判定集為空（`自誤 329`）。
🛑 **每一組須同時出艙其判定集基數**；基數 `= 0` ⇒ **loud 拒測**，⛔ 計為通過。

**(b) 驗收閘**：`V-1′` 五款**判準一字不動**（`1e-6` ⛔ 放寬）。三組並列出艙 `(1a)`／`(1b)`／`(2)`／`(3)`／`(4)`／`(5)` 逐款。
**(c) 判別力**：陽性 `(5)` 須 `> 0` 宗且 `(1b)`／`(3)` 紅；**真陰性 `(5)` 須 `= 0` 宗且 `(1b)`／`(3)` 綠**；第三類須 `(5) = 0` 而 `(1b)`／`(3)` 紅。三者若同色 ⇒ **閘失能，停機**。
**(d)** 發單側已復現：`R1·right` 二態 `cut_coords` 逐位相同 `4/4`、`ΔG 0.0000`；`R1·left` `1/1`、`R2·right` `0/7`、`R2·left` `0/7`（`ΔG` 最大 `4.7700`）。
**(e)** `cut_coords`／`G`／`驗_宗序` ＝ **harness 落檔**；`δ`／`s` 區間 ＝ **探針全精度**（⛔ 以 `*.csv` 之 `2dp` 欄評估）。
**(f)** `s` 軸 ＝ **生產解析器之 `FRONT_LINE`**（⛔ 質心導出——`-265R` 已證其致逐宗一律偏 `+3.39 m`）。

**落地**：`docs/驗證裁定登記表.md` **檔末追加**（⛔ 改 `VR-094` 原文一字·⛔ 鑄新號），內容 ＝ 三組對照之逐字界定 ＋ 本次三組實測 ＋ `自誤 328`／`329` 之通則。
🔒 **`(1)` 之二讀法並列**（`-265R` 自解清單第 `5` 項）：`(1a)` 落檔 `宗地寬度` 欄／`(1b)` 全精度 `s` 區間長度。⛔ 靜默擇一。
🔒 **界之照實**：真陰性之判定集基數 `= 3`，須於出艙處具名該 `n`。

---

## §五　工項四：三簿低端盲區之裁（**純登記·攢批**）

🔒 **授權之界**：下列各裁皆屬框／母體／粒度，**不動任何一宗地之配地結果** ⇒ 發單側自裁。

### 四-1　`K-9` 靜態清單（`K-9-1`）

**裁**：`K-9-1` 視為**已鑄而定義框結構上撈不到**，⛔ 視為缺號、⛔ 得重用。落 `CLAUDE.md` **檔末追加**（⛔ 鑄號），比照 `自誤 1`–`6` 之靜態清單體例。
**由**：其全檔 `2` 列皆表列式，內容為完整之已裁則（「街角第 1 宗**不豁免** §零-2 街廓最小建築面積」），且 `CLAUDE.md` 列其為 🔴 **有土地後果**·⬜ 未實作。
⚠️ **落 `K-6` 者 `GB-132` 風險結構性較高**（清單敘述句被定義列式命中）⇒ **⛔ 落 `K-6`**。
**驗收**：落地後 `K-9` 之相異須**與立前相同**（`28`）；判別力 ＝ 未鑄之 `K-9-30` 命中 `0`。

### 四-2　`VR-092` 之就地加註（低端盲區）

**裁**：`[MIN..MAX]` 慣例**維持**（⛔ 廢·五態外部錨仍逐位相符），惟**加註其界**：該慣例對「`MIN` 以下之已鑄而框外之則」結構上不可見。
**落地**：`docs/驗證裁定登記表.md` 檔末追加（⛔ 改 `VR-092` 原文一字·⛔ 鑄新號）。須逐簿載其低端狀態：自誤 `MIN 7`（`1`–`6` 已另立清單）／`K-9` `MIN 2`（`K-9-1` 本批另立清單）／`GB` `MIN 1`（無低端盲區）／`VR` `MIN 14`（**須查 `1`–`13` 是否同族·見驗收**）。
**驗收**：須出艙 `VR-1`–`VR-13` 之逐號檢查——其為「未鑄」抑或「已鑄而框外」，各附機械證據（全檔命中數 ＋ 命中列之形）。⛔ 以「MIN 即起點」了事。

### 四-3　`VR-093` 之就地加註（扣哨兵為空操作）

**裁**：`GB` 之三框聯集**維持**，惟**加註**：其「扣哨兵 `997`」係**空操作**（`997` 不在聯集內·裸 `GB-997` 全檔 `8` 列皆散文），而 `VR` 之 `999` **在**其定義框內 ⇒ **二簿不對稱**。該扣除**⛔ 證明任何事**，保留僅為體例一致。
**落地**：同 四-2 之落點（⛔ 改 `VR-093` 原文一字·⛔ 鑄新號）。

### 四-4　`U-1`／`U-4`／`U-5`／`U-7` 之裁

| 題 | 裁 |
|---|---|
| `U-1` **意指宣告是否構成占用** | **⛔ 構成占用**。占用 ＝ 該號**已依其簿之宣告框落於該簿**；`附件甲` 係已入倉之**草稿**，非登記簿。`GB-156` 仍可用。🛑 惟號占用查**須出艙該命中並逐筆歸類**，⛔ 靜默略 |
| `U-4` **第三類之命名** | **⛔ 造專名**。一律以其判準描述指稱：「`G` 於 `2dp` 不變而 `cut_coords` 逐位相異之組」 |
| `U-5` **`(1b)`／`(3)` 判準是否定讞** | **判準無誤·`1e-6` 定讞·⛔ 放寬**。惟出艙處**須載真陰性之判定集基數 `n = 3`** 為其界 |
| `U-7` **更正體例** | **末端追加**（既例 `24` 處）。⛔ 原地編修 |

---

## §六　工項五：`56.3293` 之來源追溯（**純診斷**·⛔ 改任何值）

**受詞**：`CLAUDE.md` 二處（內容錨 `grep -n "56.3293" CLAUDE.md`）將「`②-宗` 破量 ＝ `R2 45.9766`／`R5 56.3293`」整組標於**退縮 `0`** 之情境；`VR-041 四` 為其倉內錨；`verify/probes/probe_WG991_yishim.py` 將 `NEG = {"R2": 45.9766, "R5": 56.3293}` **寫死為判別力負對照**。

**發單側之實測（⛔ 採信·須獨立復現）**：`0m` 於三態皆止於 `R2`，可達街廓 ＝ `{R1, R2}`，**從未到達 `R5`**；`3.5m` 之 `R5` 中止閘實測為 **`56.8186`**（`-197`／`-202`／`-204` 另記 `56.818x`）。

**CC 須答（三選一·各附機械證據）**：
- **①** `56.3293` 出自某一**具名之歷史態**（出艙其 `commit`／情境／落檔），該組之情境標籤為**誤標**；
- **②** `56.3293` 於任一可達態皆產不出 ⇒ 其為**孤值**；
- **③** 尚不能判，具名**還差哪一項證據**。

🛑 **⛔ 改 `CLAUDE.md` 之狀態欄、⛔ 改 `VR-041` 一字、⛔ 改任何探針之寫死值。** 本工項只**登記**追溯結果（末端追加）。
🔑 **併須答**：若判為 ① 或 ②，則 `probe_WG991_yishim.py` 之負對照是否**結構上不可命中** ⇒ 該探針之判別力是否為假（`自誤 324` 族）。**只判、⛔ 修。**

---

## §七　工項六：典側不同步之登記（**純登記**·⛔ 更動狀態欄）

**受詞**：`GB` 簿**已載**「`3.5m` 之 `telescoping` 已不破（`-261` 後）」與可達序 `R1→R2→R4→R6→R5`；而 `CLAUDE.md` 序 `12`／序 `14` **未同步**。
**活體實測（須復現）**：態A（`023765c^`）`telescoping` 命中 `1`（`Δ=55.606`）；態B（`e9d1b9c`）／態C（`2d1eaf4`）命中 `0`，中止改為 `②-宗@R5 56.8186`。`HEAD` 與態B 生產碼**十一檔逐檔相同** ⇒ 可外推至現態。
**落地**：`CLAUDE.md` **檔末追加**一則「典側不同步之登記」，⛔ 鑄號。
🛑 **只登記不同步之事實** ——⛔ 更動序 `12`／序 `14` 之任一字、⛔ 判 `telescoping` 已解除、⛔ 移除任何 ⬜。

---

## §八　工項七：`d-1`／`d-3` 之補齊（`d-2` 本批⛔ 辦·具名其由）

🔒 **批之切分（發單側自裁）**：`d-3` 之受詞**已在既有落檔內**（`驗_A幾何` 三欄）⇒ 最廉；`d-1` 有**三處碼面明文斷言**可充自我驗證閘且**自帶必紅組**（街角第 1 宗須 ∥SIDELINE）⇒ 可辦。`d-2` 之受詞（`K-9-10`／`有效垂線`）於二生產檔命中皆 `0`，**須新造一閘** ⇒ **本批⛔ 辦**，順延並具名其阻斷點。

- **`d-3`　`K-9-12` 之充分側**：`G ≥ 49.00 ㎡` 之 `35` 宗能否**實際容納**不旋轉之 `3.50 × 14.00` 矩形。
  🛑 `W × D` 須自生產函式 `get_min_lot_size(category, front_road_width_m)` 取，**⛔ 硬編 `3.5`／`14`**；`category` 與 `front_road_width_m` 須逐街廓自 CAD／`_build_cb_cad` 實測，⛔ 一律套 `10.0`。
  **否證對照**：必紅 ＝ 人造 `3.0 × 13.0` 之四至；必綠 ＝ 人造 `10 × 20` 之四至。
- **`d-1`　`K-9-9 一`**：各宗遠側界是否 ∥ALLOCLINE。**自我驗證閘** ＝ 該三處碼面明文斷言（須逐字出艙其內容錨）；**必紅組** ＝ 街角第 1 宗（其遠側界須 ∥SIDELINE 而非 ∥ALLOCLINE）。閘不過 ⇒ 該項⛔ 得下結論，具名為不可得 ＋ 其阻斷點。

🔒 **併記（照實）**：三者於現可達之 `55` 宗上**⛔ 待 `GB-67`**；且**解了 `GB-67` 亦⛔ 使三者自動可得** ⇒ 可脫鉤先辦。

---

## §九　驗收（**性質閘**·⛔ 計數式）

| 閘 | 受詞 |
|---|---|
| `V-a` | §一 七閘逐條復現並出艙；本批之級自載為**中級**，長跑全批只跑一次 |
| `V-b` | 工項一：三自誤被定義框命中、`331` 命中 `0`；落地後 相異 `323`／`MAX 330`／缺號 `[106]` |
| `V-c` | 工項二：四項加註齊備；`GB-101`／`GB-102` 狀態欄**未動一字**（`git diff` 逐列出艙）|
| `V-d` | 工項三：三組對照並列出艙，**各載其判定集基數**；判別力 ＝ 陽性紅／真陰性綠／第三類紅，三者⛔ 同色 |
| `V-e` | 工項四：`K-9` 相異落地前後皆 `28`；`VR-1`–`VR-13` 逐號檢查出艙；`VR-092`／`VR-093` 原文未動一字 |
| `V-f` | 工項五：三選一之判 ＋ 機械證據；`CLAUDE.md` 狀態欄與 `VR-041` 與探針寫死值**皆未動** |
| `V-g` | 工項六：序 `12`／序 `14` 原文**未動一字**；加註只述事實 |
| `V-h` | 工項七：`d-3`／`d-1` 各出艙其自我驗證閘與二對照；`d-2` 之順延**具名其阻斷點**，⛔ 靜默略 |
| `V-i` | **零生產碼**：四檔於 `git diff --name-only <基座>..<本 commit>` 命中 `0` 行（**逐 commit** 出艙，含空輸出）|
| `V-j` | 既有檔 `deletions ＝ 0`（`git diff --numstat <基座> -- <檔>`·⛔ `--cached`）|
| `V-k` | `run_verification.py` 前後 `rc`／`stderr`／`RESULT`／紅項**名目集合**不變（⛔ 只比計數）|
| `V-l` | 「**本批之自解清單**」存在（空亦須明寫「空」），每項五款俱全 |
| `V-m` | 🆕 凡出艙一對照組，**同格載其判定集基數**；基數 `0` 者須為 loud 拒測，⛔ 計為通過 |

---

## §十　停機款

1. 任一對照組之判定集基數 `= 0` 而被計為通過 ⇒ 停機（`自誤 329`）。
2. 工項三之三組同色 ⇒ **閘失能**，停機，⛔ 放寬 `1e-6`。
3. 工項七之自我驗證閘不過 ⇒ 該項⛔ 得下結論，停機具名。
4. 任一工項須改生產碼四檔**一字（含註解）** ⇒ **停機上呈**（級之升不降）。
5. 本批若量得**新的**覆蓋帳違反組（`13` 組以外或值改變）⇒ 停機上呈，⛔ 逕改任何閘、⛔ 調任何上界。
6. 疑義落入自解款**黑名單**任一項 ⇒ **恆停機**。
7. 🔒 **CC 停機並回報，恆為正確程序。⛔ 自行放寬判準而呈上。**

---

## §十一　出艙形

報告 ⇒ `docs/reports/W-G.9-266R_三自誤與對照組更正與低端盲區.md`，須含：

a. 逐工具跑之**三欄**：`rc` ／ 逐字結論列 ／ `stderr` 位元組數。
b. 凡出艙一計數，同時出艙其**計數式**與**第二態之同式結果**。
c. 凡引用一數，標其**來源層**：倉 blob／工作副本／app／harness 落檔／harness log。
d. 凡出艙「復現」二字，載**框之來源**（正典逐字／自擬）；自擬之框⛔ 以待驗值為停止條件。
e. 凡「不可得」者，具名其**阻斷點**，⛔ 以「未查」了事。
f. **「本批之自解清單」**——空亦須明寫「空」。
g. 🆕 凡出艙一對照組，**同格載其判定集基數**。
h. **⛔ 含任何生產碼 diff、⛔ 含任何修法主張。**

🔒 **行號衛生**：⛔ 以行號（含 log 列號）為錨，一律用 hunk 範圍或內容錨。

---

*本單受詞 ＝ CC。零生產碼 ⇒ 得逕推。*
SELF_SHA256（受詞 ＝ 本列以前之全部位元組·含各列換行·框 ＝ verify/probes/probe_order_preflight.py 之 P-5）: fa0bbb7a6a2743528a4275e87d4dd2c6ea131b5d2e9b8297468bd6f6b8f1b444
