# 🔧 `W-G.9-203` 施工單：**`K-9-25` 入倉（地籍縫隙裁·`R4` 歸側定案）＋ 鑄 `GB-138`／`自誤 254`／`VR-087`**

> **性質**：🟢 **全單零生產碼**（⛔ 不動 `app.py` 一字含註解·⛔ 不動任何**既有** `verify/**.py` 一字）⇒ **CC 可逕行 `push`**。
> **出單窗**：claude.ai（接 `W-G.9-201R`）
> **基座**：`wip/s1-endpart` 之現時 `HEAD`（判準見 `S-0a`·🔒 綁**性質**·⛔ 綁 commit 身分·承 `自誤 246`／`252`）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **事由**　`W-G.9-201` 之實算得「`R4` 之 `M` 歸右、左街角強制抵費地」。發單側依 `-201 §三` 作**獨立幾何重建**（⛔ 讀該探針之 log），復現其**方向**，並查出勝負之**唯一來源**係地籍界線與街廓邊界間一條 **`7.9615 ㎡`、⛔ 有地號**之縫隙。該事實上呈 KL，KL 於 `2026-09-02` 裁「**可**」。本單將該裁逐字入倉並清其連帶，**⛔ 動任何生產碼**；碼面落地由重發之 `W-G.9-198` 辦。

---

## §零　落地點（單一 commit）

| # | 檔／物 | 動作 |
|---|---|---|
| `L-1` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | **檔尾追加** `### 🔒 K-9-25　` 全節（`§二-1`·逐字） |
| `L-2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | **檔尾追加** 新鑄 **`GB-138`**（`§二-2`） |
| `L-3` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **檔尾追加** 新鑄 **`自誤 254`**（`§二-3`·逐字） |
| `L-4` | `docs/驗證裁定登記表.md` | **檔尾追加** 新鑄 **`VR-087`**（`§二-4`） |
| `L-5` | `CLAUDE.md` | **檔尾追加** 裁摘要 ＋ ⬜ 待落地警語之更新（`§二-5`） |
| `A-1` | `docs/orders/W-G.9-203_施工單_K-9-25地籍縫隙裁入倉與三號鑄造.md` | 本單以**原名**入倉 |
| `A-2` | `docs/reports/W-G.9-203R_K-9-25入倉_執行報告.md` | 執行報告（**同一 commit**） |

🛑 **`L-1`〜`L-5` 皆為<u>末端追加</u>**——⛔ 不得就地改寫任何既有文字一字；`deletions` 須逐檔為 `0`。
🛑 **⛔ 得動** `app.py` 之 `k6_step0_block_locked`／`k6_merge_groups`／`k6_shares_segment` 一字。
🛑 **⛔ 得改** `verify/probes/probe_WG9201_side_assign.py` 一字（本單⛔ 觸該探針）。

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之**性質**（⛔ 綁 commit 身分） | ① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` 逐列**⛔ 含** `app.py`、**⛔ 含**任何**既有** `verify/**.py`（新增之 `verify/probes/probe_WG9201_side_assign.py` **⛔ 在此限**·其係 `-201` 之新增唯讀探針）。任一不符 ⇒ 停機上呈。🛑 `ls-remote` 全 `40` 碼須記錄回報，但**⛔ 作判準** |
| `S-0b` | `K-9-24` 已在倉 | `grep -c '^### 🔒 K-9-24　' docs/rulings/K-6_…md` ＝ `1`；🔒 **判別力對照**：同框對 `K-9-26` 須 ＝ `0`（⛔ 用 `K-9-25`——本單即鑄之） |
| `S-0c` | 本單 `SELF_SHA256` 自驗（受詞 ＝ 自檔首至該列前一個 `\n` 含之） | `declared` ＝ `recomputed`；不符 ⇒ 停機上呈 KL（表示傳遞受損） |
| `S-0d` | `probe_order_preflight.py` 對本單單檔 | `P-3`／`P-5` 須不紅；🟡 逐項具名（`§五`） |
| `S-0e` | 🔴 **量測與動作之分離**（`CLAUDE.md` 常規補款 `一`） | `§三` 各驗收閘之量測與 `commit`／`push` **⛔ 得置於同一不可中斷之命令塊**；須貼出二者之呼叫界線 |

🛑 **`S-0a` ②之豁免係本單新增**——`-201` 已合法新增一支唯讀探針，若沿用舊式「`verify/**.py` 逐列不得出現」之判準，本閘**會在合法基座上變紅**（承 `自誤 252` 之一般式：閘須能承受合法之零生產碼插入）。

---

## §一　取號現查（發單側·態 `ad2b5f977befbf2cc8f16d651d007b664acb3ca5`）

**母體**　`git -c core.quotepath=false ls-tree -r --name-only HEAD` ⇒ 追蹤檔 **`1569`【檔】**／`.md` **`502`【檔】**。
🛑 ⛔ 得用 `grep -r … .` 取母體（`自誤 250`）。

| 類 | 母體 | 框（逐字） | MAX | 次號 |
|---|---|---|---|---|
| `K-9` | `docs/rulings/K-6_…md` 單檔·扣 `K-9-99` | `` ^### 🔒 K-9-([0-9]{1,3})　 `` | `24` | **`25`** |
| `GB` | `docs/reports/W-G.4_…md` 單檔·扣 `GB-997` | `` ^#{2,4} `?GB-([0-9]{1,4})`? `` | `137` | **`138`** |
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔 | `` ^#+ 自誤 `?([0-9]{1,4})`? ``（`CLAUDE.md` 之**採用寬形**） | `253` | **`254`** |
| `VR` | `docs/驗證裁定登記表.md` 單檔·扣 `VR-999`／`VR-751` | `` ^#+ `?VR-([0-9]{1,4})`? `` | `86` | **`87`** |

🛑 **⛔ 出艙 `GB` 之「相異／缺號集」**——其框未釘（即本單 `GB-138` 之受詞）。**MAX 在所有標題錨定框下恆 ＝ `137`**（發單側實測四框：`^#{2,4}` 單檔 `137`／`^### \`GB-N\`` 單檔 `137`／`^#{2,4}` 全倉 `137`／`^\| \`GB-N\`` 單檔 `135`）⇒ **次號 `138` ⛔ 受該缺陷影響**。

### 🔒 意指占用之逐筆歸類（依 **裁 `H`**·🛑 ⛔ 以計數為判準）

母體 ＝ 全倉 `.md` `502`【檔】·錨定框。

| 號 | 命中 | 逐筆歸類 |
|---|---|---|
| `K-9-25` | `7` | `W-G.9-200:222`（`V-5` 判準列）／`W-G.9-200R:158`／`:173`／`:266`／`:278`／`W-G.9-201R:20`／`:245` ⇒ **逐筆皆判別力對照**（`CLAUDE.md` 之「判別力對照／零對照⛔ 構成意指占用」款）|
| `GB-138` | `8` | `W-G.9-200:66`／`:124`／`:222`／`W-G.4_…:3509`／`W-G.9-200R:76`／`:159`／`:266`／`:279` ⇒ **逐筆皆對照組宣告／判別力對照** |
| `自誤 254` | `3` | `W-G.9-200:70`／`W-G.9-200R:92`／`:281` ⇒ **逐筆皆零對照** |
| `VR-087` | 🛑 CC 現查並逐筆歸類 | — |

🔑 **判別力自證**：同一組命令對 `K-9-26`／`GB-139` 須各得 **`0`**【列】 ⇒ 量測器⛔ 恆非零。

---

## §二　payload（逐字·⛔ 改一字）

### §二-1　`L-1`　`K-9-25` 入倉

````text

### 🔒 K-9-25　地籍界線與街廓邊界間之無地號縫隙**⛔ 作密合處理**（KL 裁 `2026-09-02`·canonical·逐字）

🔑 **受詞行號索引**（承 `GB-137` 之施行）：本節**⛔ 取代**任何既有條文，係**新增**之量測前提。
關聯條文：`K-6:63-69`（段一之 `G` 與指數試算）／`K-8 §五`（街角規定範圍與面積）。

**KL 逐字（`2026-09-02`）**

> 「可以由地籍圖與街廓邊界之間那條沒有地號的縫來決定嗎：**可**」

**一（規則）**　重劃前地籍界線與街廓邊界之間若存在**無地號之縫隙**，於街角地評選之各項量測
（臨截角長、臨側街長、真交集面積、`G`）中，**⛔ 作任何密合、補齊、吸收或歸併之前處理**；
一律**逕依現況地籍幾何**量測。該縫隙**⛔ 是土地**、**⛔ 參與評選**、**⛔ 分配**。

**二（後果之接受）**　由此產生之街角地歸屬變動，**KL 接受**（體例同 `K-8 §五-1`）。
🛑 ⇒ 縱令左右兩側之差**全部來自該縫隙之寬度差**，該差**仍為有效之判準**。

**三（本案之定案）**　`R4` 街廓：`628(1)` 與 `628-1(1)` 同歸戶 `G009`、同街廓、界線相連
⇒ 依 `K-9-24 一` 合併為 `628(1)+`；依 `K-9-24 二`：

| 側 | 臨截角（發單側重建·頂點級） | 截角全長 | 項一比 |
|---|---|---|---|
| 左 | `4.9042` m | `4.999998` m | `0.98084` |
| 右 | **`4.9145` m** | `4.999993` m | **`0.98290`** |

⇒ **右 > 左** ⇒ `628(1)+` **歸右**（`K-9-24 二·2`）。
⇒ 左街角依 `K-9-24 二·5` 成為**強制抵費地**，範圍嚴格等於左街角規定範圍。

**四（合併⛔ 影響項一之實證）**　`628` 臨**右**截角 ＝ `0.0000`、`628-1` 臨**左**截角 ＝ `0.0000`
（頂點級·⛔ 容差）⇒ 合併後兩側之臨截角**逐位不變** ⇒ 合併前之項一比較**得以沿用**。
🔒 合併後該片占街廓 `3199.9046 / 3207.8665` ＝ **`99.752%`** ⇒ 項二／項三兩側皆趨近 `1`
⇒ **項一為唯一鑑別項**（🛑 其成因係**合併後之飽和**，⛔ 是合併前二者恰好相同）。

**五（縫隙之具名）**　`R4` 內存在一無地號面：面積 **`7.9615 ㎡`**、最長邊 **`91.638453` m**
（＝ `R4` 正街邊全長）、平均寬 **`0.0869` m**；其臨左截角 **`0.0958` m**、臨右截角 **`0.0855` m`**。
🔑 **`0.0958 − 0.0855 = 0.0103` ＝ 左右臨截角之差** ⇒ **勝負之唯一來源**。

**六（泛化·⛔ 綁本案）**　本則⛔ 綁 `R4`、⛔ 綁 `628` 系、⛔ 綁 `SB` 值。
凡地籍與街廓邊界未密合之案，**一律照本則辦**。

````

### §二-2　`L-2`　鑄 `GB-138`

````text

### `GB-138` 🆕　`GB` 類**⛔ 有經正典釘定之定義列式**，且既有先例框對帶 emoji 之標題形**靜默漏抓**

| 欄 | 內容 |
|---|---|
| **現象** | `CLAUDE.md` `常規四（八）一` 逐字所舉之定義列式**僅三類**：`` ^## `VR-NNN` ``／`` ^### 自誤 `NNN` ``／`^### 🔒? K-9-NN`——**`GB` ⛔ 在其列**。倉內唯一之 `GB` 框先例係 `docs/reports/W-G.9-157R_…md:62` 之「**三框聯集**」，**⛔ 曾入 `CLAUDE.md`**。⇒ `GB` 之「相異／缺號集」之任何斷言**皆繫於出艙者當下所選之框**。 |
| **實證（框差之幅度）** | 同一態下四框實測：`^#{2,4}` 單檔 ＝ 相異 `51`；`` ^### `GB-N` `` 單檔 ＝ `48`；`^#{2,4}` 全倉 `.md` ＝ `51`；`` ^\| `GB-N` `` 單檔 ＝ `4`。另 `W-G.9-202 §二` 載 `73`、CC `W-G.9-200R` 載 `134`——**六個值、⛔ 一致**。 |
| **既有先例框之缺陷** | `W-G.9-157R §三` 已實測坐實：三框皆要求 `#` 之後**緊接**反引號，而 `### 🟡 \`GB-110\` 🆕　…` 之形（`#` 與反引號間置 emoji）**三框皆不可見** ⇒ 該號**⛔ 入相異集**、下一批取號將**再度提出同一號**。 |
| **形（⛔ 綁本案）** | **一個編號類別，其定義列式未經正典釘定 ⇒ 該類之「相異／缺號集」不可斷言，且其鑄造端無自證。** 與 `VR-999` 之裸框得 `999`（`自誤 220`）、自誤裸框得 `298`（`常規四（八）` 案由）、`戒 40` 被次號之書寫污染（`W-G.9-190R:122`）**同族**。 |
| **必觸條件** | 凡出艙 `GB` 之相異數或缺號集者 ⇒ 【必觸】。⛔ 綁 `GB-137`、⛔ 綁本案。 |
| **處置** | 🔒 **⛔ 本則只登記·⛔ 零生產碼·⛔ 追改任何既有 `GB` 標題形一字。** **現況處置** ＝ ① 自本則起，凡出艙 `GB` 之**相異／缺號集**者，須同格載明所用之框（`戒 29`：一張表只准一個框）；② `MAX` **⛔ 受影響**（四框皆得 `137`），取號照常；③ 釘框之正典化另案辦——須比照 `W-G.9-184` 之體例，先實測各形之相異／MAX／差集，**證明所採之形涵蓋既有全部鑄號**（`甲∖寬 ＝ 0`）後方得施行。 |
| **字樣錨** | `grep -n "常規四（八）" CLAUDE.md`；`grep -n "三框聯集" docs/reports/W-G.9-157R_R3右遞補宗更正與GB109解除.md`；`grep -n "^## 🔧 自誤之" CLAUDE.md` |
| **土地後果** | ⛔ **無**——純取號可發現性缺陷。🛑 惟其**已實際致誤**：`W-G.9-202 §二` 之 `73` 即無框之數（見 `自誤 254`）。 |

````

### §二-3　`L-3`　鑄 `自誤 254`

````text

### 自誤 `254`　**否定存在之斷言，其窮舉檢索之射程僅及一檔**

**案由**　發單側於 `2026-09-02` 對 KL 逐字出艙：

> 「`CLAUDE.md:672`（`常規四（八）一`）逐字只舉了 `VR`／`自誤`／`K-9` 三類之定義列式，**`GB` 從來沒有被釘死過**。」

**實況**　該斷言之後半為**過廣**。發單側於出艙前**僅 grep 過 `CLAUDE.md` 單檔**。
事後以全倉 `.md`（`502`【檔】）多組同義詞窮舉，`docs/reports/W-G.9-157R_…md:62` 逐字載：

> 「單 `§零` 以**三框聯集**為 `GB` 之定義列式（並據以判 MAX ＝ `109`）」

⇒ `GB` **有**先例框，只是**⛔ 曾入 `CLAUDE.md`**、且該三框已被同報告 `§三` 實測證明對帶 emoji
之標題形靜默漏抓。**正確之斷言**應為：「`GB` 有先例框（`W-G.9-157`），惟⛔ 入正典、且已知有漏抓缺陷」。

**危害**　① 使 CC 之 `134`（依先例框所得）被發單側描述為「也得不到、⛔ 有依據」，實則其**有先例依據**；
② 若據該過廣斷言逕行釘框，將**⛔ 檢視先例框之差集**而重蹈 `W-G.9-157R §三` 已具名之漏抓。

**攔截者**　**發單側自查**（於起草 `GB-138` payload 時，依 `自誤 248` 之一般式擴大射程而自捕）。

**一般式（🔒 新窗須內化）**
> **凡出艙「⛔ 存在」「從未」「只有」之斷言，其窮舉檢索之母體須為<u>全倉</u>（`git ls-tree` 取得），
> ⛔ 得僅及於「最可能在的那一檔」**；並須同格載明**所用之同義詞組**與**母體檔數**。

🩸 **族**：與 `自誤 240`（引已作廢之 `K-4-3` 否認救援機制存在）、`自誤 248`（射程未含生產碼註解）
同族 ＝ **「否定存在之斷言，射程不足」**，本則為**第三度**。

````

### §二-4　`L-4`　鑄 `VR-087`

````text

## `VR-087`　發單側之獨立幾何重建（`R4` 歸側）——**方向坐實·絕對值有殘差**

**受詞**　`W-G.9-201` 探針所出之「`R4` 之 `M` 歸右」。

**方法（獨立性之聲明）**　發單側⛔ 讀 `verify/out/probe_WG9201_side_assign_*.log` 之任何數；
自 `data/V6_1.dxf`（`K-9-20` 之權威版本）以 `ezdxf` 讀 `CADASTRAL_BOUND` 與 `BLOCK`，
`shapely.ops.polygonize` 重建面，**頂點級**量測（⛔ buffer 容差）。

**結果**

| 受詞 | 生產／`-201` | 發單側重建 | 差 | 判 |
|---|---|---|---|---|
| 左·臨截角 | `4.900` | `4.9042` | `0.0042` m | 🔶 殘差 |
| 右·臨截角 | `4.911` | `4.9145` | `0.0035` m | 🔶 殘差 |
| **右 − 左** | `0.011` | **`0.0103`** | `0.0007` m | ✅ **同號同量級** |
| `628`／`628-1` 共用界線 | `47.057897` m | `47.051783` m | `0.0061` m | 🔶 殘差 |
| **歸側之結論** | 右 | **右** | — | ✅ **坐實** |

**穩健性（⛔ 以性質判斷代量測·承 `自誤 41`）**　貼線容差掃描：

| 容差 | 左 | 右 | 差 |
|---|---|---|---|
| `0.001` | `4.9055` | `4.9159` | `0.0104` |
| `0.010` | `4.9177` | `4.9286` | `0.0109` |
| `0.050` | `4.9718` | `4.9849` | `0.0131` |

⇒ **絕對值隨容差飄 `~0.07` m（＝ 決勝差之 `6` 倍），惟差值恆為正且量級穩定**
⇒ 結論**對容差不敏感**；翻轉需 `≥ 0.010` m 之反向偏差，而實測殘差 `≤ 0.0061` m。

🔶 **未閉合**　三筆殘差（`0.0035`〜`0.0061` m）同號同量級，疑似同一成因
（發單側 polygonize `CADASTRAL_BOUND` vs 生產取 `地籍資料來源_匿名版.xlsx`）；**⛔ 已查明**。
🛑 **⛔ 足以翻轉結論**（需 `≥ 0.010` m），但**⛔ 得以「無關緊要」帶過** ⇒ 列為附帶量測。

🔒 **發單側⛔ 重建者（照實回報·⛔ 頂替）**：門檻 `226.01`／`225.42`、真 `G` `1521.05`／`457.73`、
真交集。其需重建 `K-9-5-13` 裁定四／五之新構造，本窗未辦。`226.01` 之旁證 ＝ `docs/驗證裁定登記表.md:792`
（探針與 `run_all` 三格逐位相符）。

````

### §二-5　`L-5`　`CLAUDE.md` 檔尾追加

````text

## 🔧 `K-9-25` 之摘要與待落地警語之更新（`W-G.9-203`·⛔ 上文一字不刪·純末端追加）

🔒 **KL 裁 `2026-09-02`（`K-9-25`）**：地籍界線與街廓邊界間之**無地號縫隙⛔ 作密合處理**，
一律逕依現況地籍幾何量測；由此產生之街角地歸屬變動 **KL 接受**。⛔ 綁本案。

🔒 **`R4` 歸側已定案**：`628(1)+` **歸右**；左街角依 `K-9-24 二·5` 為**強制抵費地**。
🛑 ⚠️ **此係<u>正典層</u>之定案，⛔ 是碼面之現況**——現行 `app.py` **⛔ 實作** `K-9-24`／`K-9-25`
（`k6_step0_block_locked` 之保守替代仍在），其落地由**重發之 `W-G.9-198`** 辦。

⬜ **待落地清單之更新**（⛔ 得誤讀為已落地）：
- ⬜ 步驟 0 回歸無條件 ＋ `k6_step0_block_locked` 死碼化（`K-9-24 一`）
- ⬜ 歸側四分支（`K-9-24 二`）
- ⬜ 段一之上鎖（現行碼**⛔ 存在**：`上鎖`／`未上鎖`／`預掃`／`集中規則` 於 `app.py` 與
  `verify/stepg_pipeline.py` **逐檔皆 `0` 列**；對照組 `合併群` @ `app.py` ＝ `7` 列、
  `步驟 0` ＝ `9` 列 ⇒ 量測器非紅）
- ⬜ 街角指數**名次**之產出與出艙（`GB-133` 剩餘肢）＋ 段二全域排序
- ⬜ `K-6-A2` 寬深雙檢
- ⬜ `K-9-23` 藍影與二閘（結構下游於 `K-6-B`）

🔒 **`K-9-24 §四` 之上鎖受詞推論 ＝ KL 已於 `2026-09-02` 表態「可」** ⇒ 段一「上鎖」之實際受詞
＝ **段三之合併群操作**（`K-6 §二 段三 1`「取該合併群中**未上鎖**之其他片」）；⛔ 作用於步驟 0。

🔒 **`GB` 之取號**（承 `GB-138`）：`MAX` ⛔ 受框差影響（四框皆 `137`）；🛑 **⛔ 得出艙 `GB` 之
「相異／缺號集」** 除非同格載明所用之框。

````

---

## §三　驗收（🛑 量測與 `commit`／`push` **⛔ 得同塊**）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `V-1` | **零生產碼** | 變更檔清單對 `app.py` 與**既有** `verify/**.py` 之命中 ＝ `0`；🔒 **判別力對照**：同一命令對 `.md` 之命中須 ＝ `5`；🔒 **零對照**：對 `*.py` 須 ＝ `0`。🛑 取清單須加 `-c core.quotepath=false`（CJK 檔名之八進位跳脫陷阱·`-200` 已具名） |
| `V-2` | **append-only** | `git diff --numstat` 對 `L-1`〜`L-5` 五檔之 `deletions` **逐檔 ＝ `0`**；並以**位元組級嚴格前綴**自證（落地後之 blob 之前 `N` 位元組 ＝ 落地前之 blob 全文） |
| `V-3` | payload 逐字落地 | `§二-1`〜`§二-5` 五個 fence 之首列於各該目標檔**各命中 `1`** |
| `V-4` | 取號 | 落地後：`K-9` MAX ＝ **`25`**／`GB` MAX ＝ **`138`**／自誤 MAX ＝ **`254`**／`VR` MAX ＝ **`87`**。🛑 `戒` MAX **仍 ＝ `39`**（本單⛔ 鑄戒） |
| `V-5` | **判別力對照（裁 `H` 之形）** | 取 `K-9-26`／`GB-139`／`自誤 255` 之錨定框全倉 `.md` 命中，**逐筆歸類**須逐筆為零對照／判別力對照（**含本單與本報告自身之宣告列**）。🛑 **命中數⛔ 判準**。🔴 **報告寫成後須以同器再測一次**（`自誤 251`／`裁 H` 之形：宣告列自身即命中源） |
| `V-6` | `K-9-25` 之倉內錨 | 落地**前** `` grep -c '^### 🔒 K-9-25　' `` ＝ `0`；落地**後** ＝ `1`。🔒 對照組：`^### 🔒 K-9-24　` 落地前後皆 ＝ `1` ⇒ 量測器⛔ 恆零亦⛔ 恆非零 |
| `V-7` | 行尾 | 五目標檔之 blob 側 `CR` ＝ `0`；追加一律用 `LF`；各檔落地前檔尾須為 `\n`（不足者**先補一個 `\n` 再追加**，並於報告具名） |
| `V-8` | `S-0e` 之自證 | 貼出量測塊與動作塊之呼叫界線 |

---

## §四　回報（`A-2` 須逐項載明）

1. `ls-remote` 之全 `40` 碼（開工時／`push` 後各一次）＋ `left-right` 計數。
2. `S-0a`〜`S-0e` 五閘之實測值（含 `S-0a` ② 之豁免是否被觸及）。
3. `§一` 取號之**獨立復現**——CC ⛔ 採信本單所載之四個 MAX，須自行以本單所載之框重量並回報；**不符即停機上呈**。
4. `V-1`〜`V-8` 之逐項實測值（🛑 每個數旁附**產生它的命令逐字**·`自誤 249`）。
5. 五檔各自之：bytes／`\n`【次】／`CR`【次】／`sha256`（全檔）／落地前後之嚴格前綴自證。
6. 🔴 **`V-5` 之逐筆歸類須於報告寫成後重測**，並具名其自身之宣告列。

---

## §五　pre-flight 之發單側實跑（`S-0d` 之材料）

🔒 **發單側實跑逐字**（自簽後·`PYTHONIOENCODING=utf-8 python3 verify/probes/probe_order_preflight.py <本單>`）：

| 款 | 實測 |
|---|---|
| 🔴 機械·停機款（`P-3`／`P-5`） | **`0`** 項 |
| 🟡 提示（`P-1`／`P-2`／`P-4`／`P-6`） | **`0`** 項 |
| ℹ️ 記錄 | **`2`** 項（`P-5` 自簽相符 ＋ `P-3` 之 `def main` ＝ `14242`-`23460`·與 `S-0a` 之基座性質一致） |

🛑 **`🟡 ＝ 0` ⛔ 得逕採為「本器非恆綠」之證**——`W-G.9-202 §十` 載本器於 `-197` ＝ `5`／`-198` 改版 ＝ `6`／`-199` ＝ `0`／`-200` ＝ `2`／`-201` ＝ `1`【項】⇒ **本器⛔ 恆綠亦⛔ 恆紅**，此為其判別力之**既有**自證，本單沿用而**⛔ 另造**。
🛑 CC 實跑若出現**表外**之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔒 **發單側自留之三項判斷（⛔ 本器所報·純供 CC 參考）**：① `§二-1` 之 KL 逐字係單句是非答之**全文**，⛔ 節略；② `§二-4` 已逐項具名未重建之受詞與旁證出處；③ `§一` ⛔ 出艙 `GB` 相異，係本單 `GB-138` 之受詞。

SELF_SHA256: 46aa90944d5c092a64ccf0246070908d2f8c016d7f24dae1c5ae2bbf3db3b218
