# 🧾 `W-G.9-204` 發單側交接文（claude.ai 窗 → 新窗）

> **出文窗**：claude.ai（本窗接 `W-G.9-202` 交接文而起，經 `W-G.9-203` 一批落地，並承 KL `2026-09-02` 之二裁）
> **交接時態**：`wip/s1-endpart` ＝ **`829bdc98132792807067cb7e201fc615827ed72b`**
> **換窗理由**：下一批（`W-G.9-198` 重發本）係**含生產碼**且**動土地面積**之批（`226.01 ㎡` 強制抵費地將首次出現於分配圖）。依 KL 所定「幾何影響批須開新窗」「已飽和之驗證窗⛔ 宜作影響分配面積之決定」。

🛑 **本文之數⛔ 得照抄。新窗接手第一件事就是把 `§二` 逐項自倉重量一次。**
🔒 承 `自誤 254` 之一般式：**凡「⛔ 存在」「從未」「只有」之斷言，其窮舉檢索之母體須為<u>全倉</u>（`git ls-tree` 取得），⛔ 得僅及於「最可能在的那一檔」**。

---

## §零　三方分工與工作流（⛔ 得偏離）

**KL**：域裁（土地法規之唯一權威）、UI 執行、一切土地決定之最終裁決者。
**CC**（Claude Code）：唯一實作者。**只有 KL 把任務貼進 Claude Code 才會動**，⛔ 自行啟動。
**claude.ai（你）**：獨立驗證（對已推送之 commit 作 grep ＋ CAD 幾何重建）、起草施工單、裁決非域問題。

**工作流（⛔ 得跳步）**
```
KL 交辦 CC → CC 執行並 push → KL 把 CC 輸出貼給 claude.ai
→ claude.ai 自倉獨立復驗（⛔ 採信自報）→ claude.ai 寫可直接貼給 CC 之施工單 → KL 貼給 CC
```
🛑 **claude.ai ⛔ 在 CC 推送之前預作工程。**

**每張施工單必含之通則逐字**：
> 只有真觸及域邊界才停機上呈、純技術自行做完。

**push 之權限**
- 🟢 **零生產碼**（⛔ 動 `app.py` 一字含註解、⛔ 動任何**既有** `verify/**.py` 一字）⇒ CC 逕推。
- 🔒 **新增** `verify/probes/*.py` 唯讀探針 **屬零生產碼**（最新先例 ＝ `probe_WG9201_side_assign.py` 入 `ad2b5f9`）。
- 🔴 **含生產碼** ⇒ commit **備妥後停機**，由發單側請示 KL 放行方得 push。
  🛑 **下一批（`-198` 重發）即屬此類**——⛔ 得讓 CC 逕推。

---

## §一　接手第一件事（⛔ 得省）

1. `git ls-remote https://github.com/topology561/land-readjustment-trial.git refs/heads/wip/s1-endpart` — **自取全 `40` 碼**，與 `§二` 對拍。不符即知本文已過時。
2. `git -c core.autocrlf=false -c core.quotepath=false clone --branch wip/s1-endpart …`
   🛑 **`core.quotepath=false` ⛔ 得省**——git 預設把 CJK 路徑加引號，`ls-tree`／`diff --name-only` 之輸出會使 `.endswith(".md")` 全數落空（已有先例）。
3. **逐項重量 `§二` 之期末錨。**
4. 通讀：`CLAUDE.md` **全文**（`1284` 列·末端三節係 `-203` 新增）／`docs/rulings/K-6_…md`（`3738` 列·**特別是檔尾 `K-9-24`／`K-9-25` 二節**）／`docs/reports/W-G.9-203R`。
5. 讀 `§四` 之二裁與 `§五` 之 `-198` 射程。

---

## §二　期末錨（發單側自倉現查·態 `829bdc9`·🛑 新窗須逐項重量）

**母體之取法**　🛑 一律 `git -c core.quotepath=false ls-tree -r --name-only HEAD`，⛔ 得用 `grep -r … .`（該形恆含未入倉之草稿檔·`自誤 250`）。

| 受詞 | 現查值 | 產生它的命令 |
|---|---|---|
| 追蹤檔／`.md` | `1571`【檔】／`504`【檔】 | `ls-tree -r --name-only HEAD \| wc -l` ／ `\| grep -c '\.md$'` |
| `app.py` | `1383993` B ／ `\n` `23462`【次】 ／ `CR` `0` | `git cat-file -s HEAD:app.py` 等 |
| `app.py` blob | **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** | `git rev-parse HEAD:app.py` |
| `def main`（AST） | `14242`-`23460` | `ast.parse(app.py)` |
| `_pk_one_side_v12`（AST） | `12966`-`13059`（**`94` 列**·非空 `87`） | 同上 |
| `docs/rulings/K-6_…md` | `269196` B ／ `3738` 列 | `git cat-file -s` ／ `tr -cd '\n' \| wc -c` |
| `CLAUDE.md` | `114162` B ／ `1284` 列 | 同上 |
| 自誤（**寬形**·`CLAUDE.md:783` 所採） | 相異 `219`／**MAX `254`** ⇒ 次號 `255` | 框 `` ^#+ 自誤 `?([0-9]{1,4})`? ``·單檔 |
| `GB`（扣 `GB-997`） | **MAX `138`** ⇒ 次號 `139` | 框 `` ^#{2,4} `?GB-([0-9]{1,4})`? ``·單檔 |
| `VR`（扣 `999`／`751`） | 相異 `72`／**MAX `87`** ⇒ 次號 **`088`**（🛑 三位補零形） | 框 `` ^#+ `?VR-([0-9]{1,4})`? ``·單檔 |
| `K-9`（`K-6` 單檔·扣 `K-9-99`） | `{14…25}`·相異 `12`／**MAX `25`** ⇒ 次號 `26` | 框 `` ^### 🔒 K-9-([0-9]{1,3})　 `` |
| `戒`（**正典定義列式**·`CLAUDE.md:824`） | 相異 `22`／**MAX `39`** ⇒ 次號 `40` | 框 `` ^(?:🛑 \|> )?\*{0,2}戒 \*{0,2}`?([0-9]{1,3})`? ``·**全倉 `.md`** |

🛑 **`GB` ⛔ 得出艙「相異／缺號集」**——見 `GB-138`：`GB` 之定義列式**⛔ 經正典釘定**，六個框得六個值（`4`／`48`／`51`／`51`／`73`／`134`）。**MAX 在所有標題錨定框下恆 ＝ `138`** ⇒ 取號⛔ 受影響。

🔒 **哨兵（扣除集·⛔ 得計入）**：`GB-997`（`S-4` 人造哨兵）；`K-9-99`（陽性對照·出處 `docs/reports/W-G.9-89b_CC交接文.md:72`）；`VR-999`／`VR-751`。

🔒 **⛔ 生產碼未動**：`app.py` blob 自 `7fab020` 起**逐次未變**，含本窗四 commit（`fb494f5`／`8020d3d`／`ad2b5f9`／`829bdc9`）。

---

## §三　本窗已辦（皆已入 `origin`）

| commit | 內容 |
|---|---|
| `fb494f5` | `W-G.9-200`：`K-9-24` 入倉·取代 `K-9-21` 與舊特例 ＋ 鑄 `GB-137` ＋ `GB-135` 解除 |
| `8020d3d` | `W-G.9-200` 增量（單於施工期間被就地改版）：鑄 `自誤 253` ＋ 交接文 `196`／`202` 入倉 |
| `ad2b5f9` | `W-G.9-201`：歸側實算探針 `probe_WG9201_side_assign.py` ＋ 實測 log |
| `829bdc9` | `W-G.9-203`：`K-9-25` 入倉 ＋ 鑄 `GB-138`／`自誤 254`／`VR-087` |

🛑 **`W-G.9-202` ＝ 前窗之交接文**（已入倉）；**`W-G.9-198` ⛔ 曾入倉**（見 `§五`）。

---

## §四　🔴 KL `2026-09-02` 之二裁（**本交接文最重要之一節**·皆已入倉）

### 一、`K-9-24 §四` 之上鎖受詞推論 ⇒ KL 答「**可**」

**內容**：步驟 0（同街廓 ∧ 同歸戶 ∧ 相鄰 ⇒ 視為同一筆）既回歸**無條件**且完成於段一之前，段一之「**上鎖**」（`K-6:66-67`）⛔ 有可作用於步驟 0 之對象 ⇒ 其**實際受詞 ＝ 段三之合併群操作**（`K-6 §二 段三 1`「取該合併群中**未上鎖**之其他片」）。

🔒 **一句話**：**「上鎖」管的是跨街廓的併，⛔ 管同街廓同地主的併。**
🔒 已入 `CLAUDE.md` 末端（`W-G.9-203 L-5`）。
⚠️ **對 `-198` 之影響**：實作段一上鎖時，**⛔ 得讓它凍住步驟 0 之同街廓合併**。若採相反讀法，同一地主在已定案街角旁之兩塊相鄰地將併不起來，**分配結果會不同**。

### 二、`K-9-25` 地籍縫隙裁 ⇒ KL 答「**可**」

**KL 逐字**：
> 「可以由地籍圖與街廓邊界之間那條沒有地號的縫來決定嗎：**可**」

**條文（`K-6` 檔尾·`### 🔒 K-9-25　`）**：重劃前地籍界線與街廓邊界之間若存在**無地號之縫隙**，於街角地評選之各項量測（臨截角長、臨側街長、真交集面積、`G`）中**⛔ 作任何密合、補齊、吸收或歸併之前處理**，一律逕依現況地籍幾何量測；該縫隙**⛔ 是土地、⛔ 參與評選、⛔ 分配**。由此產生之街角地歸屬變動 **KL 接受**（體例同 `K-8 §五-1`）。⛔ 綁本案。

### 三、`R4` 歸側之定案（**本波第一筆動分配面積者**）

`628(1)` 與 `628-1(1)` 同歸戶 `G009`、同街廓、界線相連 ⇒ 依 `K-9-24 一` 合併為 `628(1)+`。

| 側 | 臨截角 | 截角全長 | 項一比 |
|---|---|---|---|
| 左 | `4.9042` m | `4.999998` m | `0.98084` |
| 右 | **`4.9145` m** | `4.999993` m | **`0.98290`** |

⇒ **歸右**（`K-9-24 二·2`）；**左街角依 `K-9-24 二·5` 為強制抵費地，範圍嚴格等於左街角規定範圍（`226.01 ㎡`）**。

🔑 **勝負之唯一來源已查明**：`R4` 內有一**無地號**之面，面積 `7.9615 ㎡`、最長邊 `91.638453` m（＝ 正街邊全長）、平均寬 `0.0869` m；其臨左截角 `0.0958` m、臨右截角 `0.0855` m ⇒ `0.0958 − 0.0855 = 0.0103` **＝ 左右臨截角之差**。

🔒 **二項旁證（`-198` 會用到）**
1. `628` 臨**右**截角 ＝ `0.0000`、`628-1` 臨**左**截角 ＝ `0.0000`（頂點級·⛔ 容差）⇒ **合併⛔ 影響項一**，合併前之項一比較得以沿用。
2. 合併後占街廓 `3199.9046 / 3207.8665` ＝ `99.752%` ⇒ 項二／項三兩側皆趨近 `1` ⇒ **項一為唯一鑑別項**（成因 ＝ **合併後之飽和**，⛔ 是合併前二者恰好相同——`W-G.9-202 §四-五` 之措辭在這一點上結論對而理由錯）。

🔶 **未閉合殘差（`VR-087`）**：發單側重建 vs 生產之三筆差——左臨截角 `0.0042` m／右臨截角 `0.0035` m／共用界線 `0.0061` m，同號同量級，疑同一成因（重建 polygonize `CADASTRAL_BOUND` vs 生產取 `地籍資料來源_匿名版.xlsx`），**⛔ 已查明**。翻轉結論需 `≥ 0.010` m ⇒ ⛔ 影響定案，但**⛔ 得以「無關緊要」帶過**。

---

## §五　待辦與依賴序（🛑 ⛔ 得跳）

### 🔴 下一批 ＝ `W-G.9-198` 重發本（**含生產碼·動土地面積**）

```
W-G.9-198 重發（🔴 生產碼）
   └→ commit 備妥後停機 → 發單側請示 KL 放行 → 方得 push
```

**射程四項（序⛔ 得亂）**
1. 🔴 **步驟 0 回歸無條件** ＋ `k6_step0_block_locked` 之死碼化（`K-9-24 一`）。
2. 🔴 **歸側四分支**（`K-9-24 二`）＋ `K-9-25` 之量測前提。
3. ⬜ **`K-6-B` 段一之上鎖**——**現行碼⛔ 存在**：正面列舉（母體 ＝ `app.py` 與 `verify/stepg_pipeline.py` **二檔**·框 ＝ 列）`上鎖`／`未上鎖`／`預掃`／`集中規則`／`合併群跨街廓`／`K-6-B` **逐檔皆 `0` 列**；🔒 **對照組**：`合併群` @ `app.py` ＝ `7` 列、`步驟 0` @ `app.py` ＝ `9` 列、`k6_step0` ＝ `8` 列 ⇒ 量測器非紅。
   🛑 實作時⛔ 得讓上鎖作用於步驟 0（`§四-一`）。
4. ⬜ **街角指數名次之產出與出艙**（`GB-133` 剩餘肢）＋ **段二全域排序**（鍵序 ① 名次 → ② 街角試算 `G` → ③ 指數數值 → ④ 暫編地號字典序最小）。
   🛑 `GB-133` 載明：指數之計算須**移至 `qualified` 判斷之前**，且**該移動會改變既有出艙**（`未達標` 列之四欄由 `—` 變數值）⇒ 屬**行為變更**，須以判別力閘看守。

### 🔴 `W-G.9-198` 原檔**⛔ 在倉**

**檔名逐字**：`docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md`
**現況**：全倉 `.md` 對 `W-G.9-198` 命中 `36` 列，其中含 `docs/orders/` 路徑者 `3` 列——`W-G.9-197R:106`／`W-G.9-199R_CC交接文:75`／`W-G.9-199R_…執行報告:117`。**其中二列自陳其為「未追蹤」「工作樹內」** ⇒ ⛔ 是 `自誤 253` 之重演（該三列⛔ 宣稱其在倉）。

🛑 **新窗之處置**：`W-G.9-202 §五` 所稱「舊 `-198` 之可沿用部分（`§二` `R1-1`〜`R1-9`／`§三` `R2-1`〜`R2-6`／`D1-*`／`D2-*`）」**你在倉裡讀不到**。⇒ **開工第一件事就是向 KL 索取該原檔**（在 KL 本機工作樹），或明告 KL「將自零重寫，⛔ 沿用」。⛔ 得憑 `-202` 之轉述當作已讀。
🔴 **⛔ 得沿用**其 `§零` 之「⛔ 不得動保守替代」與舊版 `S-0a`——二者之前提皆已被 `K-9-24` 消滅。

### 攢批中（⛔ 為其單獨開批·`KL 2026-08-24` 工作法）
1. 🔶 **CC 側之不精確（`W-G.9-203R`）**：報告逐字「`Δ` 恰 ＝ payload·⛔ 補任何位元組」。發單側逐檔實測：**`Δ` ＝ payload <u>去尾一個 `\n`</u>，五檔皆然**（`2642/2641`、`2548/2547`、`1813/1812`、`2084/2083`、`1774/1773` B）。「⛔ **補**」為真（係**減**）；「恰 ＝」為**偽**。內容逐字相符、五檔仍以 `\n` 收尾 ⇒ **⛔ 實質危害**。擬立之形：**宣告「`Δ` 恰 ＝ payload」者，須同格報二端之位元組數。**
2. `GB` 定義列式之正典化（`GB-138` 處置③）——須比照 `W-G.9-184` 之體例：先實測各形之相異／MAX／差集，**證明所採之形涵蓋既有全部鑄號**（`甲∖寬 ＝ 0`）後方得施行；🛑 且須檢視 `W-G.9-157R §三` 已具名之「`#` 與反引號間置 emoji ⇒ 三框皆不可見」之漏抓。

### 其餘在案（⛔ 本波射程）
`GB-133` 剩餘肢／`GB-136`（`_ni` 區間夾制）／`GB-67`（`②-宗 圍堵閘破[R5]`·成因未定）／`GB-104`〜`GB-106`／`GB-111`／`GB-123`／`VR-086`／`K-9-23` 藍影與二閘（結構下游於 `K-6-B`）／`K-6-A2` 寬深雙檢／九份 baseline 重產／波末重烤／泛化波。

### 🔒 既有終局（⛔ 本波所致·⛔ 得誤認為新紅）
KL `7fab020` UI 實跑**未跑完**，止於 `k94_assert_baseline_touch`：`【未配到屁股線】1 宗——R5 之 628-53(2)（差 38.803m）`。
`②-宗 圍堵閘破[R5]`：`宗-宗重疊 = 56.8187 > 上界 1.5999`；倉內既有實測 `verify/out/probe_WG9187_baseline_76378d09.log:14` ＝ `56.8186`；⚠️ 二值差 `0.0001` **⛔ 未查**，繫 `GB-67`。
`R3` `Phase 8 抵費地碎片合併產生 MultiPolygon（2 片）⇒ 取最大片、小片幾何丟失而面積留帳`。

---

## §六　🩸 本窗之自誤（`254`·一則）

| 號 | 一句話 | 攔截者 | 一般式（**新窗須內化**） |
|---|---|---|---|
| **`254`** | 對 KL 出艙「`GB` **從未**被釘死過」，而出艙前**僅 grep 過 `CLAUDE.md` 單檔**；全倉窮舉後 `docs/reports/W-G.9-157R_…md:62` 逐字載有 `GB` 之「三框聯集」先例框 | **發單側自查**（起草 `GB-138` payload 時依 `自誤 248` 之一般式擴大射程而自捕） | **凡出艙「⛔ 存在」「從未」「只有」之斷言，其窮舉檢索之母體須為<u>全倉</u>（`git ls-tree` 取得），⛔ 得僅及於「最可能在的那一檔」；並須同格載明所用之同義詞組與母體檔數** |

🩸 **族**：與 `自誤 240`（引已作廢之 `K-4-3` 否認救援機制存在）、`自誤 248`（射程未含生產碼註解）同族 ＝ **「否定存在之斷言，射程不足」**，本則為**第三度**。

🔒 **一項未鑄但須記**：本窗曾對 KL 出艙「`202` 交接文⛔ 在倉、`自誤 253` 未鑄」——該斷言於量測當下（`ls-remote` ＝ `fb494f5`）**為真**，`8020d3d` 一落地即消滅。**⛔ 鑄號**（數無誤），惟其形為：**出艙之數未標其「會被下一個合法 push 推翻」之時態**。⇒ 戒：**凡出艙倉態之數，須同格載明其 `ls-remote` 之全 `40` 碼與量測時刻。**

---

## §七　給新窗之具體戒律（承 `W-G.9-202 §七`·本窗新增第 `9`〜`10` 條）

1. 🔴 **每一個出艙之數，旁邊都要放得出產生它的那條命令**，且命令的受詞要與標籤字面相同（`自誤 249`）。
2. 🔴 **量倉一律 `git -c core.quotepath=false ls-tree -r --name-only HEAD`**（`自誤 250` ＋ CJK 引號陷阱）。
3. 🔴 **判別力對照依裁 `H`**：報命中數 ＋ **逐筆具名歸類**，⛔ 寫「命中 ＝ `0`」。
4. 🔴 **開工閘綁性質**（`app.py` blob ＋ 變更檔清單），⛔ 綁 commit 身分；且須能承受**合法之零生產碼插入**（`自誤 252`）。
   🔒 **`-203` 之實例**：`S-0a` ② 若沿用「`verify/**.py` 逐列不得出現」，會被 `-201` 合法新增之唯讀探針弄紅 ⇒ 該單已預先寫入豁免款，CC 實跑確實觸及。**`-198` 之 `S-0a` 須同樣預留。**
5. 🔴 **⛔ 採信 CC 自報。** CC 本波品質很高（三次擋下發單側之錯），但那正是為什麼你**必須**自倉復驗。本窗即以逐檔位元組比對抓出 `§五` 攢批第 `1` 項。
6. 🔴 **同時交付多張單時**，後單的閘要能承受前單以外的合法插入。
7. 🔒 **CC 呈裁時，先驗其受詞是否正確**。
8. 🔒 **對 KL 說話一律用土地話**（街廓／抵費地／街角／面積／地主／位次），⛔ 用程式詞（`閘⑤`／`k*`／`ΣRw`／`s_min` 等）。格式：**【現況】【要改成】【對土地的影響（帶數字）】【要你判斷（單一是非）】**，並標明「**真需 KL 域專業判斷**」抑或「**只是知會、點頭即可**」。
   🔒 **本窗之成功先例**：把「`0.0009` 之總分差」翻譯成「**街廓裡一條寬 `8.7` 公分、沒有地號的縫，左端比右端粗 `1` 公分**」，KL 當場即裁。
9. 🆕 🔴 **獨立幾何重建⛔ 得讀受驗探針之 log**。本窗之 `VR-087` 係自 `data/V6_1.dxf` 以 `ezdxf` ＋ `shapely.ops.polygonize` 重建，**頂點級量測**（⛔ buffer 容差），並附**容差掃描**證明結論對容差不敏感。🛑 **⛔ 得以性質判斷代量測**（`自誤 41`）。
10. 🆕 🔴 **⛔ 得因「與推定相符」而升格為裁**。`-201` 之實算與 `-202 §四-五` 之推定逐項相符，但相符本身⛔ 是證據——本窗仍作了完整之獨立重建，且查出其**理由與推定所載者不同**。

---

## §八　上呈 KL 之門檻

**門檻** ＝ **有土地後果 ∧ 倉內查無正典依據**。二者缺一 ⇒ 發單側當場裁，⛔ 上呈。
**問 KL 之前**須窮舉檢索**三處**：`docs/rulings/`、各登記表、**`app.py`／`verify/**.py` 之註解**（`自誤 248`），且母體須為**全倉**（`自誤 254`）；多組同義詞；並於出艙時報命中數與其母體。

🔒 **目前掛在 KL 身上者 ＝ ⛔ 有**（`§四` 二裁皆已答、皆已入倉）。
🔴 **新窗第一個須上呈者**，預期為 `-198` 之**放行**（含生產碼 ⇒ commit 備妥後停機請示）。

---

## §九　落地稽核（KL 之常設指令·⛔ 得省）

每次對話收束前，發單側須自問並向 KL 明說：
> **「本輪有沒有已驗證／已裁定、但尚未派工落地的結論？」**

有 ⇒ 必須開落地單。

🔒 **本窗交接時之答**：**正典層⛔ 有**——`K-9-24`／`K-9-25` 二裁已全數入倉，`GB-138`／`自誤 254`／`VR-087` 已鑄。
🔴 **碼面層有，且是整波最大的一筆**：`K-9-24`／`K-9-25` **`app.py` 尚未實作一字**（`k6_step0_block_locked` 之保守替代仍在），落地全繫於 **`-198` 重發本**。
🛑 `CLAUDE.md` 末端現載**六項 ⬜**（步驟 0 死碼化／歸側四分支／段一上鎖／街角名次＋段二排序／`K-6-A2` 寬深雙檢／`K-9-23` 藍影二閘），**⛔ 得誤讀為已落地**。

---

## §十　工具與環境

- 倉：`topology561/land-readjustment-trial`·分支 `wip/s1-endpart`·clone 須 `-c core.autocrlf=false -c core.quotepath=false`
- CAD 權威版本：`data/V6_1.dxf`（`K-9-20`；`V6.dxf` 已廢）
- 正典：`docs/rulings/K-6_街角地分配程序與可分配判準.md`（含檔尾 `K-9-*` 系列）／`CLAUDE.md`／`AGENTS.md`
- 登記表：`docs/reports/W-G.4_泛用阻塞項登記表.md`（`GB`）／`docs/驗證裁定登記表.md`（`VR`）／`docs/reports/W-G.9波_claude.ai側自誤登記.md`（自誤）
- 出單前必跑：`PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py <單>`；🟡 須**逐項以字樣錨具名**於單內（⛔ 用行號）。🔒 **判別力之既有自證**（⛔ 另造）：`-197` ＝ `5`／`-198` 改版 ＝ `6`／`-199` ＝ `0`／`-200` ＝ `2`／`-201` ＝ `1`／**`-203` ＝ `0`**【項】⇒ 本器⛔ 恆綠亦⛔ 恆紅。
- 自簽：每張單末端 `SELF_SHA256: <64 碼>`，受詞 ＝ 自檔首至該列前一個 `\n` 含之。`-203` 之值 ＝ `46aa90944d5c092a64ccf0246070908d2f8c016d7f24dae1c5ae2bbf3db3b218`（倉內自驗相符 ⇒ 係**放檔**、⛔ 重打）。
- KL UI 證據：`verify/out/KL_UI_3.5m_<commit 短碼>_…`（🛑 短碼須為 **commit**·⛔ 用 `app.py` 之 blob 短碼）
- 環境：KL 為 Windows；`stdout.log` 係純 CRLF，入倉須走 `autocrlf=false` 保全行尾
- 幾何驗證：Shapely ＋ ezdxf；UI 為 Streamlit；headless 為 `verify/stepg_pipeline.py`／`verify/run_all.py`
SELF_SHA256: 3ecd03ecfcec0fdca6ed0dad0202a103a7eb9d009a91e4064061f958ab1e4329
