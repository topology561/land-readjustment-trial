# 🧾 `W-G.9-192-PRE-c`　KL 裁：`K-6-B` 段一〜段四提前 ＋ `VR-086`／`GB-133`／自誤 `240`（⛔ 零生產碼）

> **發單側**：claude.ai·`Opus 5`
> **基座**：`wip/s1-endpart` ＝ `fc92965213491f6a387fb92e5542e19363471087`（`ls-remote` 現查）
> **性質**：**⛔ 零生產碼** ⇒ 依 `CLAUDE.md`「`常規一` 之補款 🔧 一」逐 commit 逕行 `push`、⛔ 不必請示。
> **來由**：KL 於 `2026-09-01` 之域裁——**街角第 1 宗（第 0 宗）與末端塊須先定案，`K-6-B` 段一〜段四須提前**。發單側自倉坐實其結構強制性，並自查一則自誤。

🔒 **只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　開工閘

```
git rev-parse HEAD                # 期望 fc92965213491f6a387fb92e5542e19363471087
git rev-parse HEAD:app.py         # 期望 da805fc276f0f2b7dbe7ec4738929c51973b12e0
git config --get core.autocrlf    # 🟡 只須出艙、⛔ 非判準
python verify/probes/probe_order_preflight.py docs/orders/W-G.9-192-PRE-c_KL裁K6B提前.md
```
🛑 二 blob 錨任一不符 ⇒ 停機。⛔ 不得以工作區 `wc -c` 判之。
🔒 **`add` 之分流（承 `W-G.9-192-PRE-b` 裁 `D`）**：本批受詞檔（`docs/rulings/**`／`CLAUDE.md`／二登記表）於工作樹皆為 CRLF ⇒ **一律走常規 `add`**，⛔ 不得用 `-c core.autocrlf=false`。⛔ 全程禁 `git add -A`／`.`。

🛑 **本批⛔ 改 `docs/rulings/**`**——此係**例外授權**：`docs/rulings/` 平時在禁改清單內，本批**僅得末端追加**（`deletions` 須為 `0`），⛔ 不得改既有一字。授權來源 ＝ KL `2026-09-01` 之域裁本身。

---

## §一　`N-1`　KL 裁入正典（末端追加至 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 檔尾）

### 一-0　發單側之現查（🔒 本裁之結構依據·⛔ 非發單側之意見）

🔒 **正典既有之逐字**（`docs/rulings/K-6_…`）：
- `:2115`「沿 FRONTLINE 之**推進方向** ＝ 自該側之起點（**街角端**）往街廓中間」
- `§二 段四`「winner（**或強制抵費地**）佔住街角位置；跨占左街角者靠左、跨占右街角者靠右……**池 ＝ 扣除所有已定位宗後之中段**」
- `CLAUDE.md:335`「推進方向一律由 `FRONT_LINE` 定；**兩端往中間排、調配池居中**」

🔒 **`K-9-23` 對 winner 之依賴（`app.py:11941-11942` 引正典 `K-9-23 三-一` 逐字）**：
> 街角地（**第 0 宗**）**依 winner 之 `G`** 定其**遠側境界線**（∥SIDELINE）；該線交 FRONTLINE 於 `P1`、交 BASELINE 於 `B1`……藍影 ＝ 該帶落在街角地外之那一半。

⇒ 🔴 **藍影之構造以 winner 之 `G` 為輸入** ⇒ 若 `K-6-B` 段三之遞補救援改變了 winner（或使原無 winner 之側產生 winner），**藍影、二閘、遞補之受詞集全部改變** ⇒ **先跑 `K-9-23` 者，其所算之閘係建立在一組將被段三推翻之 winner 上。**

🔴 **現行 `待落地清單` 之依賴標註方向倒置**：`CLAUDE.md` 之更正二清單 `序 5`（`K-6-B` 段一〜段四）依賴欄逐字為「**後於 `1`**」（`1` ＝ `K-9-23` 系列）——**與上開結構相反**。

### 一-1　📦 逐字追加至 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 檔尾

```
---

## 🔒 `K-6-B` 段一〜段四之**優先序**（KL 裁 `2026-09-01`·`W-G.9-192-PRE-c`·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為**裁之落地**。🔒 **⛔ 不改 `§二` 段一〜段四本文一字。**

### 一　KL 之裁（逐字）

> **所有配地的順序都是從街廓最左右側開始往街廓內分配，所以一定要先把街角第 1 宗（或稱第 0 宗，以及末端塊）給確定後才能逐宗辦理後續的配地，所以一定要提前辦理。**

### 二　其射程

🔒 **`K-6-B` 段一〜段四（含步驟 0）之落地，優先於 `K-9-23`／`K-9-16`／`K-9-17`／`K-9-12`。**
⇒ 原「待落地清單」之 `序 5` 依賴欄「後於 `1`」**自本節起作廢**（⛔ 不追改該列一字，見 `CLAUDE.md` 之更正三）。

### 三　結構依據（⛔ 非優先序偏好·三項皆現查可復現）

**(1)　推進方向自街角端起**——本檔 `:2115` 逐字「沿 FRONTLINE 之推進方向 ＝ 自該側之起點（**街角端**）往街廓中間」；`CLAUDE.md:335`「兩端往中間排、調配池居中」。⇒ **第 0 宗未定，其後每一宗之起算點皆未定。**

**(2)　段四之位次重排以第 0 宗為錨**——本檔 `§二 段四` 逐字「winner（或強制抵費地）佔住街角位置……池 ＝ 扣除所有已定位宗後之中段」。⇒ **第 0 宗之身分（winner 或強制抵費地）決定其餘全部宗之落位。**

**(3)　`K-9-23` 之藍影以 winner 之 `G` 為輸入**——本檔 `K-9-23 三-一` 逐字「街角地（第 0 宗）**依 winner 之 `G`** 定其遠側境界線」。⇒ **winner 改變 ⇒ 藍影改變 ⇒ 二閘與遞補之受詞集全部改變。**

⇒ 🔒 **三者合之：`K-9-23` 系列係<u>下游</u>，`K-6-B` 段一〜段四係<u>上游</u>。** 先辦下游者，其結果必被上游推翻。

### 四　`末端塊` 之併記

🔒 KL 之裁併及**末端塊**。本檔既有之處置**⛔ 不變**（`:1024-1026` 逐字：範圍定義 `R_end = (p1 之外未臨正街土地) ∪ (p1→pz 帶)`、勝者規則、`strip_area(S) = G − wedge_area` 常數項 **一律不動**·KL `2026-08-07` 明示「末端塊仍沿用之前裁示的作法」）。
🔒 **現查**：末端塊之處置**已在生產碼內**（`grep -c "末端塊" app.py` ＝ `8`／`grep -c "R_end" app.py` ＝ `12`；`verify/stepg_pipeline.py` 命中 `0`）⇒ **⛔ 非待落地項**。
🛑 惟 `K-9-23` **⛔ 不涵蓋末端塊**（本檔 `K-9-23 三` 之漏承加註·`W-G.9-150′`／`-154`）⇒ 該漏承**⛔ 不因本裁而解除**。
```

---

## §二　`N-2`　`CLAUDE.md` 待落地清單之更正三（末端追加）

🛑 **⛔ 不改既有清單一字**（append-only·留作沿革）。

### 📦 逐字追加至 `CLAUDE.md` 檔尾

```
---

## 🔧 待落地清單之更正三：**`K-6-B` 段一〜段四提前**（KL 裁 `2026-09-01`·`W-G.9-192-PRE-c`·⛔ 上文原句一字不刪）

🩸 **被更正之對象**：「🔧 待落地清單之更正二（`W-G.9-144`）」表之 `序 5` 一列，其依賴欄逐字為「**後於 `1`**」。🔒 **⛔ 不追改該列一字。**

🔴 **該標註方向倒置**——`K-9-23`（`序 1`）之藍影構造**以 winner 之 `G` 為輸入**（正典 `K-9-23 三-一`·`app.py:11941-11942` 引其逐字），而 winner 由 `K-6-B` 段一〜段三決定 ⇒ **`序 1` 係 `序 5` 之下游。**

🔒 **KL 裁（`2026-09-01` 逐字）**：
> 所有配地的順序都是從街廓最左右側開始往街廓內分配，所以一定要先把街角第 1 宗（或稱第 0 宗，以及末端塊）給確定後才能逐宗辦理後續的配地，所以一定要提前辦理。

🔒 **更正後之清單（以本節為準·⛔ 上方原清單不刪）**：

| 序 | 項 | 狀態 | 依賴 |
|---|---|---|---|
| **`1`** | 🆕 **`K-6-B` 段一〜段四**（段一 預掃／段二 全域排序／段三 逐一嘗試（含 RD 片整筆併入·重算 `G`）／段四 重排位次）（**幾何影響批**） | ⬜ **未開工** | 步驟 0 已落地（`a2c4b2b`）；`K-6 §一` 幾何連通判定已落地但**⛔ 零接線**（`k6_merge_groups`／`k6_shares_segment`）；🛑 **另見 `GB-133`（段二之排序鍵現行碼不可得）** |
| **`2`** | `K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17` 重排／遞補 ＋ `K-9-12` 矩形容納（**幾何影響批**） | ⬜ 未開工 | 🔴 **後於 `1`**（藍影以 winner 之 `G` 為輸入）；前置 ＝ `GB-104`〜`GB-106` 之處置 |
| `3` | `K-6-A2` 剩餘（`K-9-1`／`K-9-3`／`K-9-5`／`K-9-6` 宗地層 `min_depth`／`K-9-8` 之 `GB-18`／`K-9-5-2` ①⑤） | ⬜ 未開工 | — |
| `4` | `K-9-22` 落地（原地號保留 ＋ 區段回退） | ⬜ 未開工 | — |
| `5` | `K-6 §四` 合併述詞置換 → 波末批 → 泛化波 | ⬜ 未開工 | 末 |
| （併行） | 九份 baseline 重產（**須 KL 決時點**·建議俟 `1`＋`2` 落地後） | ⬜ | ⛔ 不入主鏈 |

🛑 **原清單之 `序 1`／`序 2`（`K-9-23`＋`K-9-12` 同批）之「同批」關係⛔ 不變**——其仍須同批（否則假綠不消），只是整組**後移於 `K-6-B` 之後**。
🔒 **末端塊⛔ 非待落地項**（已在生產碼內·`grep -c "末端塊" app.py` ＝ `8`）；其處置依 KL `2026-08-07` 明示「仍沿用之前裁示的作法」，本裁⛔ 不變更之。
```

---

## §三　`N-3`　鑄 `VR-086`

### 三-0　取號現查

🔒 **框** `` ^## `VR-([0-9]{1,4})` ``·**母體 ＝ `docs/驗證裁定登記表.md` 單檔**。
未扣除 相異 `71`／MAX `999`；**已扣除 相異 `70`／MAX `85`／缺號 `15`**（所扣 ＝ `VR-999`）⇒ **次號 `86`**。
🛑 **意指占用**（錨定框 `(?<![0-9\-])VR-086(?![0-9])`·母體 全倉）＝ **`7` 筆**，**全部係零對照**（`S-5` 判別力對照 ＋ 其案由之轉引）⇒ 依 `CLAUDE.md`「🔧 二　零對照之出艙⛔ 不構成意指占用」（`W-G.9-192-PRE-b` 落地）**⛔ 不構成占用** ⇒ 可取。對照組 `VR-085` ＝ `33` ⇒ 量測器非紅。
🔒 **標題形範本 ＝ `## \`VR-084\`　…（claude.ai 裁 \`日期\`·\`單號\`）`**（全形空格 `　` 分隔）。

### 三-1　📦 逐字追加至 `docs/驗證裁定登記表.md` 檔尾

```
## `VR-086`　現行碼實作了 `K-6 §二` **段三-6 之<u>結果</u>而無段三-1〜5 之<u>過程</u>**：段一全滅即直接回傳強制抵費地，段二／段三結構上不存在（claude.ai 裁 `2026-09-01`·`W-G.9-192-PRE-c`）

**一**　**受詞**　正典 `K-6 §二` 定四段程序：段一（街廓內預掃）→ 段二（全域排序）→ 段三（逐一嘗試·取合併群中未上鎖之他街廓片／RD 片／公設片併入·**RD 片整筆併入**·重算 `G`）→ 段四（重排位次）；**段三-6 逐字「某街角全部候選失敗 ⇒ 強制留設抵費地」**。

**二**　**現查（發單側自倉·態 `fc92965`）**　`app.py` 之 `if not qualified:` 分支逐字：

> `return {'winner': None, 'qualified': [], 'eliminated': eliminated, 'note': '🚫 無人達 G 值門檻 → 該端保留為抵費地', 'group_size': len(group)}`

⇒ **段一全滅即直接回傳抵費地**，段二／段三**從未執行**。
🔒 **字樣現查**（母體 ＝ `app.py`／`verify/stepg_pipeline.py` 二檔·**正面列舉**）：`預掃` `0`／`0`；`上鎖` `0`／`0`；`未上鎖` `0`／`0`；`集中規則` `0`／`0`；`合併群跨街廓` `0`／`0`；`K-6-B` `0`／`0`。**對照組** `步驟 0` @ `app.py` ＝ `9` ⇒ **量測器非紅**。
🔒 **正典自載其未實作**（`K-6:331` 逐字）：「`K-6 §二` 段三之『合併群跨街廓集中』（同歸戶、得經 RD 或公設連通）**尚未實作（`K-6-B`）**。」

**三**　🔴 **土地後果（實測·⛔ 非推論）**　`SB = 3.5m`／態 `7a6b7f3` 之 KL UI 實跑：`R2` 左之四候選真 `G` 皆低於門檻 `308.17` ㎡（`628-42(1)` `4.28`／`628-41(1)` `175.95`／`628-40(1)+` `286.65`／`628-27(1)` `26.34`）⇒ 現行碼判 **`⚠️ 強制抵費地`**（`verify/out/KL_UI_3.5m_7a6b7f3_第1宗街角地指配結果.csv` 之 `R2` 列逐字 ＝ `R2,V13,🤖 自動 PK,14,308.17,無此側,0,⚠️ 強制抵費地,—,—,無此側,—`）。
⇒ **該側成為強制抵費地，係<u>救援從未被嘗試</u>之結果，⛔ 非段三-1〜5 試過而失敗之結果。** 二者於出艙上**同形**（`GB-91`／`GB-115`／`GB-130` 族之又一例：**輸出不自證**）。

**四**　🛑 **射程限**
① 本則**⛔ 未斷言**段三跑完後 `R2` 必得 winner——`628-41` 之地籍面實測 `561.95` ㎡（DXF·與登記面積 `561` 相符）而落於 `R2` 街廓者僅 `319.23` ㎡；若其廓外片依段三-2 併回，**粗估**真 `G` 落於門檻 `308.17` ㎡ 附近（線性外推）。🔴 **`G` ⛔ 非面積之線性函數**（負擔率隨寬度、側街負擔、街角指數而變）⇒ **該外推⛔ 不足以判定會過或不會過**，須實跑。
② 本則**⛔ 未量**其餘五街角——其 winner 皆於段一即定案，段三對其**結構上不生效**（段三僅處理「待處理街角」）⇒ **預期不動**，惟⛔ 未實測。
③ 本則之受詞係**碼面之結構**，⛔ 非某一情境之數；`SB = 0.0m` 情境⛔ 未量。
```

---

## §四　`N-4`　鑄 `GB-133`

### 四-0　取號現查

🔒 **三框聯集·母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔**：未扣除／已扣除皆 相異 `130`／MAX `132`／缺號 `[12, 87]` ⇒ **次號 `133`**。
🛑 **意指占用**（`(?<![0-9\-])GB-133(?![0-9])`·全倉）＝ **`5` 筆**，**全部係零對照** ⇒ 依上開新款⛔ 不構成占用。對照組 `GB-132` ＝ `42` ⇒ 量測器非紅。
🔒 **標題形範本 ＝ `GB-132` 逐字。**

### 四-1　📦 逐字追加至 `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔尾

```
### `GB-133` 🆕

**編號現查**（`W-G.9-192-PRE-c`·態 ＝ `fc92965`·三框聯集·母體 ＝ 本檔單檔）：未扣除／已扣除皆 相異 `130`／MAX `132`／缺 `[12, 87]` ⇒ 本則取次一號 ＝ **`133`**。
🛑 **錨定框** `` (?<![0-9\-])GB-133(?![0-9]) `` 全倉命中 `5`，**全部係零對照**（依 `CLAUDE.md`「🔧 二」⛔ 不構成占用）；對照組 `GB-132` ＝ `42` ⇒ 量測器非紅。🔒 標題形同 `GB-132`。

| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |
|---|---|---|---|---|---|---|
| **`GB-133`** 🆕 | 🔴 **`K-6 §二` 段二之第一排序鍵「街角指數<u>名次</u>」，於現行碼<u>恰在需要它的情況下</u>結構上取不到。**正典段一逐字：「全部未達 ⇒ 該街角**待處理**，**記下各候選之街角指數與名次**」；段二鍵序 ①＝街角指數名次。而現行碼之 `if not qualified: return`（`app.py`·字樣錨 `grep -n "無人達 G 值門檻" app.py`）**在指數計算之前**，指數之計算式為 `for cand in qualified:`（`grep -n "第二關優先權指數採用三項法定加權" app.py`）⇒ **段一全滅時，`qualified` 為空 ⇒ 迴圈零次 ⇒ 無任何候選被算過指數**。實測佐證：`verify/out/KL_UI_3.5m_7a6b7f3_W-D.1.2診斷_逐候選評點明細.csv` 之 `未達標` 各列，其「正街角分／側街分／跨占分／總分」四欄**逐列為 `—`**。🔑 **本則之形**：一個排序鍵，在**唯一需要它的分支**上必為空——與 `GB-130`（空母體與全過同形）同族，皆屬**輸出不自證**。 | **凡實作 `K-6-B` 段二者 ⇒ 【必觸】。**⛔ 不綁 `R2`、⛔ 不綁本案、⛔ 不綁 `SB` 值——任何案件只要某街角段一全滅，段二即無鍵可排。 | 🔒 **⛔ 本則只登記·⛔ 零生產碼。** **現況處置** ＝ 具名並登記，列為 `K-6-B` 段二落地之**隱性前置**（原「待落地清單」⛔ 未列）。🛑 落地時須將指數之計算**移至 `qualified` 判斷之前**、或對 `eliminated` 亦算指數；🔴 **該移動會改變既有出艙**（`未達標` 列之四欄由 `—` 變為數值）⇒ 屬**行為變更**，須以判別力閘看守，⛔ 不得逕改。 | 字樣錨：`grep -n "無人達 G 值門檻" app.py`／`grep -n "第二關優先權指數採用三項法定加權" app.py`／`grep -n "for cand in qualified" app.py`；正典 `docs/rulings/K-6_街角地分配程序與可分配判準.md` `§二 段一`／`段二`；實測 `verify/out/KL_UI_3.5m_7a6b7f3_W-D.1.2診斷_逐候選評點明細.csv` | ✅ **有**——`R2` 左四候選皆 `未達標`，其指數四欄實測逐列為 `—`（真資料·KL UI 實跑·`SB=3.5m`·態 `7a6b7f3`）。⚠️ `SB = 0.0m` 情境⛔ 未量。 | ⛔ **不需**——正典段一已明文「記下各候選之街角指數與名次」⇒ 應算而未算，屬**實作缺口**、⛔ 非域裁題。 |

🔒 **失效條件（逐字）** ＝ 「段一全滅之分支上，各候選之街角指數與名次**皆已算出並出艙**，且該出艙可供段二之鍵序 ① 直接消費」。
```

---

## §五　`N-5`　鑄自誤 `240`

### 五-0　取號現查

🔒 **框 ＝ 正典寬形** `` ^#+ 自誤 `?([0-9]{1,4})`? ``·**母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔**（`CLAUDE.md`「🔧 一」）：未扣除／已扣除皆 相異 `204`／MAX `239`／缺 `35` ⇒ **次號 `240`**。
🛑 **意指占用**（錨定框 ＋ 二形並取·全倉）：`自誤 240` **二形皆 `0`**；對照組 `` 自誤 `239` `` ＝ `4` ⇒ 量測器非紅。
🔒 **標題形範本 ＝ `### 自誤 \`239\`（發單側·出單窗）：**…**`**

### 五-1　📦 逐字追加至 `docs/reports/W-G.9波_claude.ai側自誤登記.md` 檔尾

```
### 自誤 `240`（發單側·出單窗）：**引用一則<u>已作廢</u>之條款去否定一則<u>現行</u>條款，並據以向 KL 出艙錯誤結論**

**案由**　KL 於 `2026-09-01` 問：街角第 1 宗決定階段，若街廓內真 `G` 皆未達，是否應將同歸戶連通之廓外土地（含 RD 片）併入後重算真 `G`，以盡量避免強制抵費地。
🔴 發單側答「**不會改變街角資格**」，所引之依據為正典 `K-6:109` 逐字「跨街廓同歸戶『小往較大集中』一律歸七級調配（`F.0`／`F.2`）；**街角不再有專屬救援機制**」。
🔴 **該引用為誤**——`:109` 所指之「專屬救援機制」係 **`K-4-3` 整套街角救援**，而同檔 `§四` 逐字載其「**作廢**」（「`K-4-3` 整套街角救援作廢（`M-5` ①②③、來源母體 `(a)(b)(c)`、`A4 registry` 硬閘），碼與裁定一併封存」）。⇒ **發單側拿一條已作廢之條款，去否定一條現行條款。**
🔒 **KL 所述之機制<u>逐字在正典</u>**（`K-6 §二` 段一〜段三·⛔ 非新裁）：段一「全部未達 ⇒ 該街角**待處理**」→ 段二 全域排序 → 段三「取該合併群中**未上鎖之其他片（他街廓片、RD 片、其他公設片）**……**RD 片整筆併入**……**重算 `G`**」→ 段三-6「某街角**全部候選失敗** ⇒ 強制留設抵費地」。**強制抵費地係最後一步，⛔ 非第一步。**
🩸 **二層錯，且第二層更深**：
① **層一**：未辨「同名不同物」——`:109` 之「救援」與 `§二 段三` 之「救援」是**兩個機制**，一廢一存。
② **層二**：發單側**先答了，才去查**。同一輪內已跑過 `docs/rulings/` 之檢索，卻**以單一命中即下結論**，未讀該命中之上下文（`§四` 之作廢列與 `:109` 相距 `3` 列）。⇒ **`常規四（八）三`「所用之定義列式須逐字列出」之精神於裁定引用層之對應物 ＝「所引之條款須先確認其未作廢」，本倉⛔ 尚無明文。**
🔴 **土地後果**：該錯誤結論若被採信，`R2` 左之 `308.17` ㎡ 將**永久停留於強制抵費地**，而正典本要求先試段三之救援。**⇒ 本則係本波唯一一則<u>有土地後果</u>之發單側自誤。**
🔑 **攔截者 ＝ KL**（逐字：「不對，我指的不是……」）——**⛔ 非任何機檢**。`P-1`〜`P-6` 檢文字形式、`S-*` 檢倉態，**皆檢不到「引錯條款」**。
🔒 **一般式**　凡引正典條款以**否定**某主張者，須同格報三事：① 所引條款之**逐字**；② 其**作廢狀態之現查**（同檔 `§四`／「作廢與置換」節之命中）；③ **反向檢索**——以該主張之受詞（而非以自己的結論）為框，另跑一次檢索並報命中數。⛔ 不得以單一正向命中即下否定結論。
🩸 **併記**：本則之受詞與 `自誤 236`（以同名不同量之集合成員資格論證另一集合之性質）**同族**——皆為「**兩個同名之物，取甲之性質論乙**」。**本窗第二度。**
```

---

## §六　收工閘

| # | 閘 | 判準 |
|---|---|---|
| `S-1` | 零生產碼（逐 commit） | 判法輸出每一 commit 皆 `0` 行。🛑 空輸出亦須逐 commit 出艙 |
| `S-2` | `app.py` blob 不變 | `HEAD:app.py` ＝ `da805fc276f0f2b7dbe7ec4738929c51973b12e0` |
| `S-3` | **純附加（🛑 含 `docs/rulings/`）** | `git diff --numstat fc92965..HEAD` 刪除欄逐列 `0`；並以 `git cat-file blob fc92965:<檔>` 對 `HEAD:<檔>` 證**嚴格前綴**（⛔ 不得僅跑 `wg942_append_audit`）；四受詞檔 blob `CR` 皆須 ＝ `0` |
| `S-4` | 形之自證 | 各類定義列式掃 payload，須命中 `VR-086`／`GB-133`／`自誤 240` |
| `S-5` | MAX 之推進（新母體） | `VR` 單檔 `85`→`86`／`GB` 單檔 `132`→`133`／自誤 單檔 `239`→`240`；新號⛔ 不在缺號集。🛑 未扣除／已扣除二數並報 |
| `S-6` | **零對照款之首次適用自證** | 出艙 `VR-086`／`GB-133` 之意指占用筆數與**逐筆歸類**（須全為零對照）；並具名所依之款（`CLAUDE.md`「🔧 二」）。🛑 此係該款落地後之**首次實際適用**，須逐筆列出、⛔ 不得只報「不構成占用」 |
| `S-7` | `GB` 缺號集不變 | 已扣除缺號集仍 `[12, 87]` |
| `S-8` | **正典之逐字抽出** | `N-1`／`N-2` 之 payload 須自本單 fence **逐字抽出**（`常規七`），⛔ 不得手抄；覆核以 `diff` 為之，⛔ 不得僅比三數 |
| `S-9` | `常規五` | 含 emoji 之工具先設 `PYTHONIOENCODING=utf-8`；`rc≠0` 須先判量測器紅抑或受詞紅 |

🛑 **停機款**：`§零` 二 blob 錨任一不符／`S-1` 任一 commit 命中 `≥1` 行／`S-3` 任一檔非嚴格前綴或 `deletions > 0`／`S-4` 或 `S-5` 不過。

---

## §七　禁改清單

- `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`
- `.claude/skills/**`
- **任何既有檔之既有文字**——本批**全部為末端追加**
- 🛑 **`docs/rulings/` 本批僅得末端追加**（例外授權·見 `§零`）；⛔ 不得改 `§二` 段一〜段四本文、⛔ 不得改 `:109`、`:1024-1026` 一字
- ⛔ 不追改任何既有已鑄之號之標題形，⛔ 不追改任何既有已出艙之數

**`常規二`**：本單條款互斥時，取保守項續辦 ＋ 逐字回報，⛔ 不上呈 KL。

---

## §八　本單⛔ 未辦（具名）

| 項 | 狀態 |
|---|---|
| `K-6-B` 段一〜段四**本體** | ⬜ **未起**——幾何影響批·須另開新視窗·**每張單須請示 KL 方得 push** |
| `GB-133` 之處置（指數移至 `qualified` 前） | ⬜ 屬 `K-6-B` 段二落地批 |
| `GB-132` 類 ①（敘述句污染 `戒` 之定義列式） | 🔴 ⛔ 未解除 |
| `GB-123`／`127`／`111`／`97`／`129`／`130`／`131` | 本批⛔ 未動 |
| `628-41` 併回廓外片後之真 `G` | ⬜ **未量**——建議列為 `K-6-B` 批之觀測項（`[T3-GATE]` 已為觀測模式·多印一數⛔ 不動配地） |

---

SELF_SHA256: e8e0e886e472cc1e2fc306f2bfa1c8b5228a21843a64dcb6ef510e2d30dacbdb
