# W-G.9-264　`V-1′` 之立（🔴 擬形已否證）＋ 角退判準登記 ＋ `GB-155` ＋ `自誤 325` ＋ `-263R` 二加註

> **零生產碼批**（`app.py`／`verify/` 生產路徑一字不動）⇒ 依 `常規一` 補款 `🔧 一`，CC 得逕推主線。
> **⛔ 含任何修法主張。** 凡「應改為…／建議…／修正為…」之句 ⇒ 逾射程，刪之。
> **⛔ 逕自決定 `GB-67` 開修**；`.svg` 一項**已結案**，⛔ 再提。
> 🩸 **本單係 `W-G.9-263`（V-1′ 版）之改號重發**——`263` 已由另一份同號單占用（入倉與落簿批·`2bc0429`），本單**⛔ 與其同號**。原 `263`（V-1′ 版）**未曾出艙**，⛔ 視為作廢單。

---

## §零　開工閘（**逐條當場復現·任一不符 ⇒ 停機上呈**）

| 閘 | 應得 |
|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `2d1eaf47c326fd0209270c7aa6210d62e6870305` |
| `2` 受詞 | `app.py` ＝ `1e15d45ae84b02ca`／`verify/stepg_pipeline.py` ＝ `cb0bbaaa0f1eb8bc`（sha256 前 `16`·`-263` 二 commit 未動十檔任一位）|
| `3` 號占用 | 見下表·三數分列 |
| `4` 次號 | 三簿前值 ＝ **自誤 `324`／`GB 154`／`K-9 29`** ⇒ 本批鑄 **`自誤 325`** ＋ **`GB-155`**；`K-9`⛔ 鑄 |
| `5` `S-0b` | `probe_order_preflight.py` `rc=0`、`P-5` 與 `§末` `SELF_SHA256` 相符 |

**閘 `3` 之應得**（發單側於態 `2d1eaf4` 實測·母體 ＝ 全 `docs/` **`615`** 檔·讀不到 `19` ＝ `jpg 9` ＋ `png 10`）：

| 號 | `D1` | `D2` | `D3` | 雙屬 | 列框 | 檔框 | 角色 |
|---|---|---|---|---|---|---|---|
| `W-G.9-264` | `0` | `0` | `0` | `0` | `0` | `0` | 受詢（須 `= 0`）|
| `W-G.9-263` | `2` | `2` | `1` | `0` | `3` | `2` | 對照組甲（須 `≥ 1`）|
| 一人造號（字樣⛔ 入倉·`GB-147` 代稱）| `0` | `0` | `0` | `0` | `0` | `0` | 對照組乙（須 `= 0`）|

🩸 **自指款**：若本單已入倉致 `264 ≠ 0`，**⛔ 改號**；改判「占用者是否全為本單自身之產物」，是則過閘。
🛑 **⛔ 採信上表任一值。** `列框 ＝ |D2 ∪ D3|`；`檔框 ＝ |D1 ∪ D2之檔 ∪ D3之檔|`。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-264_V1prime之立_角退判準登記_GB155_自誤325.md`。
出艙：來源／落檔／入倉 blob 之 `sha256` 三者逐位相符，並載 **blob 位元組數**／`LF`／`CR`。
🔑 **層之具名（`§六 c` 之硬性款）**：凡出艙「位元組數」，須明載其為 **blob 層**抑或**工作副本層**。

---

## §二　工項一：鑄 `自誤 325`（純登記·責 ＝ 發單側）

落地點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（⛔ 節末）。
標題形取既有最末一則（`### 🩸 \`自誤 324\`　**…**`）為範本。⛔ 動既有任一字。

> ### 🩸 `自誤 325`　**以待驗之目標值調校量測框，再將其結果出艙為「獨立復現」**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-264`·`2026-09-09`。
>
> **受詞**：發單側承接 `交接文-262` 時，該文 `§一` 明令「⛔ 採信本表任一值——接手窗須當場 `clone` 並以 `git cat-file blob` 復現」，其所載自誤簿冊為「相異 `314`／`MAX 321`／`MIN 7`／缺號 `[106]`」。
> 發單側對 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **⛔ 先取正典所裁之框**，而以標題樣式自擬計數框，**連續四版**得相異 `152`／`179`／`309`／`314`，**於第四版命中該文所載之 `314`／`[106]` 時停止調校**，並出艙「簿冊我自建計數式重數…與 `§一` 逐項相符」「`§一` 無一項不符」。
>
> 🔴 **誤之所在**：該文之值係**待驗之受詞**，⛔ 擬合之目標。以目標值決定停止點，所得者為**一致**，⛔ 為**獨立復現**。
>
> 🔒 **恆等式⛔ 為擋板（機械證據）**：四版之**每一版**其「相異 ＋ 缺號 ＝ `MAX − MIN + 1`」皆為 `True`。CC 於 `-263` 同批亦以未展開範圍之框得相異 `305`／缺號 `10`，其恆等式亦 `True`（`315 ＝ 305 + 10`）。⇒ **恆等式對相斥之二框皆自洽，結構上⛔ 具判別力**（`常規八` 同族）。
>
> 🔒 **對照（⛔ 同誤者）**：同一輪之**宣告框**係先讀 `CLAUDE.md` 之 `①`–`⑦` 逐字、後施測，其結果⛔ 經目標調校 ⇒ 該項為真獨立復現。**二者之別在「框之來源」，⛔ 在「數之對錯」。**
>
> 🔒 **通則（自本則起）**：凡出艙「復現」二字，須同時載**框之來源**——正典逐字／自擬。自擬之框，**⛔ 以待驗值為停止條件**；其停止條件須為**框自身之性質**（如：能被正典所裁之取號器命中、對未鑄號之對照得 `0`）。

---

## §三　工項二：`V-1′` 之立 —— 🔴 **擬形已被否證，須改綁**

### a　否證量測（發單側已實跑·CC 須獨立復現，⛔ 採信下表）

**二態**：改前 ＝ `023765c^` ＝ `70ce7d46b27a1c9f5fd057f697ba3b7f382af7ad`（`app.py` ＝ `4860f10828fb8ae9`）；改後 ＝ `e9d1b9c3e6e46580da28392e1ed7495d5b34638f`（`app.py` ＝ `1e15d45ae84b02ca`）。
**受詞** ＝ `3.5m`·`R2 left` 之**非鏈頭六宗**（鏈頭 ＝ `628-42(1)`）。
**來源層** ＝ **harness 落檔** `verify/out/got_G值_退縮3.5m_partial.csv`（⛔ 倉內值、⛔ CC 自報）。

| 量 | 非鏈頭六宗之實測 |
|---|---|
| `ΔS` | `0.0000000000`（十位皆零·六宗全數）|
| `Δ宗地寬度` | `0.0000000000`（十位皆零·六宗全數）|
| `cut_coords` 逐位相同 | **`0 / 6`** |
| 質心位移 | `0.1067` – `0.1120 m`（六宗全數非零）|
| `ΔG` | `628-40(1)+` **`+0.19㎡`**／`628-41(1)` **`+0.06㎡`**／餘四宗 `< 0.005㎡` |

🔴 **判**：`交接文-262 §四` 所擬之 `V-1′`（「該側除鏈頭外 `S` 與宗地寬度逐宗不變 ＋ `ΣG ＋ 池 ＝ 街廓`」），其二合取項於本資料上**皆成立** ⇒ **閘為綠**；而同一資料上**二宗地之面積確實變動**。⇒ 擬形所綁者為代理量 ⇒ **`自誤 324` 之同形**。

🔑 **成因（純機制·⛔ 修法主張）**：非鏈頭整排沿推進向平移 `≈0.11m`，各宗之寬度配額與鏈序皆未變；`G` 之變動係「同寬之帶被移至街廓兩側界非平行處重切」所生，**⛔ 重新分配**。故 `ΔG ≠ 0` 本身⛔ 得為紅之充分條件。

🔒 **守恆之獨立復現**（`T2-DIAG`·harness log 層）：`R2` 池帶 `1471.2568 → 1466.2584`（`Δ = −4.9984`）、街角強制抵費地 `308.1700` 不變、`Σ 殘差 +0.0000`。
🩸 **併記**：`交接文-262 §五` 所載 `ΔΣG ＝ +5.02` 係**二位小數落檔欄之逐宗加總**，⛔ 真值；真值 ＝ `−Δ池 ＝ +4.9984`。其與 `−5.00` 之 `0.02` 差係捨入，**⛔ 守恆缺口**。

### b　`V-1′` 之立（CC 施工）

落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**（⛔ 鑄 `GB` 號——其為閘之定義，非阻塞項）。若家法要求閘定義另有落點，CC **停機上呈**其依據，⛔ 自裁。

> **`V-1′`　射程宣告為「僅動鏈頭」之改動，其「無重新分配」之驗**
>
> 對該側**非鏈頭**各宗須同時成立：
> **(1)** 宗地寬度（`s` 區間長度）逐宗不變；
> **(2)** 鏈序（宗之排列）不變；
> **(3)** `s` 區間下界之位移 `δ` **逐宗為同一常數**；
> **(4)** `ΣG ＋ Σ池 ＝ 街廓面積`，覆蓋殘差 `＝ 0`；
> **(5)** 🔑 **出艙款**：凡任一非鏈頭宗之 `ΔG ≠ 0`，須**逐宗出艙其 `ΔG` 與 `δ`**，⛔ 因 `(1)`–`(4)` 全綠而略之。
>
> 🛑 **精度款**：`(3)` **⛔ 以 `verify/out/*.csv` 之欄評估**——該檔 `累積S` 為二位小數，二已捨入值相減可差 `±0.01`。發單側實測 `Δ累積S` 得 `{0.10, 0.11, 0.12}` 三值，**與「`δ` 恆為 `0.11`」相容而⛔ 可判**。`(3)` 須由探針讀全精度值。
>
> 🔒 **判別力自檢**（立閘同批須實跑）：**陽性對照** ＝ `-261` 之二態——`(1)(2)(4)` 須綠、`(5)` 須出艙 `+0.19`／`+0.06` 二宗。**陰性對照** ＝ 同二態之 `3.5m`·`R2 right` 七宗（該側二態逐位相同）⇒ `(5)` 須出艙 `0` 宗。二者皆 `0` 或皆非零 ⇒ **閘失能，停機**。

🛑 **`V-1` 之「未達」定讞⛔ 回改**。本節只立後繼閘。

---

## §四　工項三：鑄 `GB-155`（`_b_L0` 退化分支殘值·**純診斷**·⛔ 修）

落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**。⛔ 動既有任一字。

> ## 🆕 `GB-155`　`_b_L0` 於退化分支未重算、殘留前一街廓之值
>
> **受詞**（CC 於 `-261` 施工中捕獲·尚未登記）：`3.5m`·`R4 left` 之 `_b_L0` 與 `R2` **逐位相同**（`6.98048044737404`）而其 `buf` 相異。
> **射程**：⬜ **未定** —— 本單令定之。
> 🛑 **⛔ 含修法主張。**

**CC 須答（機械證據·逐字器 ＋ 逐字輸出）**：

- **`Q-a`　復現**　於態 `2d1eaf4` 獨立復現該逐位相同與 `buf` 相異。⛔ 採信 `-261R` 之自報。
- **`Q-b`　射程**　`_b_L0` 之殘值是否及於 `R1`／`R5`／`R6`（`0m`／`3.5m` 各自）？逐街廓逐側出艙 `_b_L0` 與 `buf`。`R3` 二情境皆未跑到 ⇒ 具名其為**阻斷**，⛔ 以「未查」了事。
- **`Q-c`　土地後果之有無**　`R4 left` 之 `226.01㎡` 強制抵費地（`c2` 之受詞）之值是否**依賴**該 `_b_L0`？
  🔑 只答「依賴／不依賴」＋ 機械證據，**⛔ 答「差多少」、⛔ 動任何數**。
  答「依賴」⇒ **停機上呈**，由發單側具差異表呈 KL。
- **`Q-d`　觸發條件**　具名該退化分支之進入條件（`app.py` 逐字 ＋ **hunk 範圍或內容錨**·⛔ 行號）。

---

## §五　工項四：**角退判準**之登記（`GB-67`／`GB-154` 之補款·append-only·**純診斷**·⛔ 修）

### a　發單側之實測（CC 須獨立復現，⛔ 採信）

態 `e9d1b9c`（其落檔與 `2d1eaf4` 逐位相同：`0m` `98aebf88260047b5`／`3.5m` `437a07a475149f19`）·**軸自由之面積交集法**（⛔ 需方向假設）。
**器**：讀落檔 `cut_coords` → `shapely.Polygon`（`is_valid` 否則 `buffer(0)`）→ 同街廓同側**兩兩** `intersection().area`，`> 1e-6` 計入。

**定義　角退**　該側存在 `驗_宗序 ＝ 街角第1宗` 之宗，且其 `W` **大於**該側 `第2宗` 之 `W`（`ΔW ＝ W(第2宗) − W(街角第1宗) < 0`）。

| 情境 | 街廓 | 側 | 街角第1宗 | `ΔW` | 兩兩重疊合計 `㎡` |
|---|---|---|---|---|---|
| `0m` | `R1` | left | `628-37(1)` | `+18.03` | `0.0000` |
| `0m` | `R1` | right | `628(5)` | `+2.59` | `0.0000` |
| `3.5m` | `R1` | right | `628(5)` | `+2.59` | `0.0000` |
| `3.5m` | `R6` | right | `628-18(1)` | `+0.00` | `0.0000` |
| **`0m`** | **`R2`** | **left** | **`628-41(1)`** | **`−0.95`** | **`45.9766`** |
| **`3.5m`** | **`R5`** | **left** | **`628-45(1)+`** | **`−1.29`** | **`56.8186`** |
| 無街角第1宗之七組 | — | — | — | — | `0.0000`（全數）|

🔒 **閘值逐位命中**：`45.9766` ＝ `StepG0m` 中止閘所載；`56.8186` ＝ `StepG3.5m` 中止閘所載；差 `0.0000`。
🔒 **疊之施加者**：二例皆為**街角第1宗單宗**（`0m R2` 疊 `628-27(1) 19.8142` ＋ `628-40(1)+ 26.1624`；`3.5m R5` 疊 `628-18(2) 56.8186`）。
🔒 **顯示 `W` 倒退之那一宗本身⛔ 被疊到**（`628-42(1)`／`628-53(2)` 皆 `0`）。
🔒 **器能歸零**：`13` 組中 `11` 組得 `0`；有街角第1宗之六組中四組得 `0` ⇒ **⛔ 恆真**。

### b　CC 須答

- **`Q-e`　獨立復現**上表全 `13` 組（含零者），以證「僅 `2` 組非零」非取樣所致。
- **`Q-f`　三選一之判**（具名證據）：① 角退 ⇔ 重疊 `> 0` 於現可達母體成立；② 為偽，具名反例；③ 尚不能判，具名**還差哪一項證據**。
- **`Q-g`　射程之界**　`R3` 於二情境**皆未跑到**（`0m` 止於 `R2`；`3.5m` 處理序 `R1 R2 R4 R6 R5`，止於 `R5`）。具名 `R3` 是否含街角第1宗、其 `ΔW` 是否可得；不可得則具名阻斷點。
- **`Q-h`　與 `GB-154` 之關係**　`GB-154` 逐字載「🔒 **與 `GB-67` ⛔ 同源之機械證據**：見 `-257R §五` 二項」。而 `-257R §五` 之證據一為「**`3.5m` 有 `telescoping` 破而 `②-宗` 未破**」。
  🔴 **該證據之受詞已由 `-261` 移除**（`3.5m` telescoping `Δ=55.606` 已修 ⇒ 管線達 `R5` ⇒ `②-宗` 於該處破 `56.8186`）。
  CC 須判 `GB-154` 之「⛔ 同源」結論，其**所餘證據**是否仍足；三選一（仍足／不足／不能判 ＋ 還差哪一項）。

### c　落地形

以 **`GB-67` 之補款** ＋ **`GB-154` 之補款**二節，`docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**。
🛑 ⛔ 動 `GB-67`／`GB-154` 原文一字；⛔ 鑄新號；⛔ 改二者之現況欄；⛔ 含修法主張；⛔ 調整任何上界。

---

## §六　工項五：`W-G.9-263R` 之**二則就地加註**（⛔ 上文原句一字不刪）

落點 ＝ `docs/reports/W-G.9-263R_交接文入倉與自誤322至324.md` **檔末追加**。⛔ 鑄號（其為加註）。

**加註一　位元組數之層未具名**
發單側於態 `2d1eaf4` 以 `git cat-file blob` 實測 `docs/reports/W-G.9波_claude.ai側自誤登記.md`：

| 態 | blob 位元組 | `LF` | `CR` | `blob + LF` | `-263R` 所載 |
|---|---|---|---|---|---|
| `e9d1b9c` | `653571` | `7687` | `0` | `661258` | `661258` |
| `2bc0429` | `658030` | `7731` | `0` | `665761` | `665761` |

⇒ `-263R` 之該二數 ＝ **`blob + LF`**，即**工作副本以 CRLF 呈現之長度**，⛔ blob 大小；而同一報告之工項零段又明載「blob size ＝ 位元組數（無 CRLF 正規化）」。**同一報告內「B」指二層。**
🔒 **⛔ 追改 `-263R` 原文任一字**（`常規四（九）五`）；本加註只具名其層。
🔒 **⛔ 影響任何實質結論**：`+44` 列、`sha256`、嚴格前綴 `True` 皆經發單側獨立復現。

**加註二　`run_verification.py` 之 log 列號係 clone 路徑之函數，⛔ 得為錨**
三態實測（生產碼十檔 blob 逐位相同、二情境落檔 `sha256` 逐位相同、紅數皆 `24`）：

| 跑 | clone 路徑 | `RESULT` 之列號 |
|---|---|---|
| 發單側 甲 | `/home/claude/land-readjustment-trial` | `310` |
| 發單側 乙 | `/home/claude/lr3` | `311` |
| CC | 其本機路徑 | `312` |

**機制**（發單側以 `diff` 定位）：例外回溯（traceback）中之源碼列，其換行位置隨**檔案絕對路徑之長度**而異；短路徑之跑多出一列 `tep_g_impl(`（`_run_step_g_impl(` 之續行）。
⇒ **凡以 log 列號為驗收判準者，其所綁為操作者之目錄配置**（代理量），⛔ 為程式之性質 ——`自誤 324` 之同族。
🔒 `-263R` 之 `:312` ⛔ 判為誤；本加註只具名該數**⛔ 得作錨**。

---

## §七　工項六：唯讀探針入倉

`verify/probes/probe_corner_retro_overlap.py`（**新檔·唯讀**·⛔ import 生產路徑之寫入面）。

- 讀 `verify/out/got_G值_退縮{0m,3.5m}_partial.csv`，輸出 `§五 a` 之 `13` 列表。
- **⛔ 覆寫任何 tracked 檔**。發單側實測：實跑 `run_verification.py` 後 `git status --porcelain` ＝ `0` 項（落檔為 untracked）；CC 須復現此 `0`。
- 探針**自帶陰性對照**：對一必不存在之街廓名（`GB-147` 代稱）須輸出 `0` 列。

---

## §八　驗收（**性質閘**·⛔ 計數式驗收）

| 閘 | 判準 | 否證對照 |
|---|---|---|
| `V-a` | `§零` 閘 `3` 三列由 CC 自測復現，對照甲 `≥ 1`、對照乙 `= 0` | 對照乙非零 ⇒ 框錯 |
| `V-b` | `自誤 325` 之標題可被該檔現行定義框命中 | 一未鑄之號（`326`）須命中 `0` |
| `V-c` | `§三 a` 五量由 CC 二態獨立復現；`Δ宗地寬度`／`ΔS` 十位皆零且 `cut_coords` 逐位相同數 ＝ `0/6` | 得 `6/6` ⇒ 器讀錯態 |
| `V-d` | `V-1′` 之 `(5)` 於 `-261` 二態出艙 `2` 宗、於 `R2 right` 出艙 `0` 宗 | 二者皆 `0` 或皆非零 ⇒ 閘失能 |
| `V-e` | `§五 a` 之 `13` 組全數復現，二非零組與中止閘逐位相同（差 `< 1e-6`）| `13` 組全非零 ⇒ 器恆紅 |
| `V-f` | `§六 加註二`：CC 自二**不同長度**之 clone 路徑各跑一次，出艙二列號 ＋ 二落檔 `sha256`；列號須相異而 `sha256` 須逐位相同 | 二列號相同 ⇒ 未變路徑長度，重設 |
| `V-g` | 本批 `app.py` 與 `verify/` **生產路徑** `deletions ＋ insertions ＝ 0`（新探針⛔ 屬生產路徑，另列）| 非零 ⇒ 逾射程，停機 |
| `V-h` | 三簿末值 ＝ 自誤 `325`／`GB 155`／`K-9 29`；恆等式 `True`；缺號集 `[106]` 不變 | 缺號集變 ⇒ 落地錯位 |

🛑 **`V-g` 未達 ⇒ ⛔ 推，停機上呈。**

---

## §九　停機款

1. **`Q-c` 答「依賴」** ⇒ 停機上呈（動 `226.01㎡` ⇒ 須 KL）。
2. **`V-1′` 之落點於家法上不明** ⇒ 停機上呈其依據，⛔ 自裁。
3. **任一答須改動 `GB-67`／`GB-154`／`-263R` 原文** ⇒ 停機上呈，⛔ 逕改。
4. **任一工項須動生產碼** ⇒ 停機上呈，⛔ 逕改。
5. 🔒 **CC 停機並回報，恆為正確程序。⛔ 自行放寬判準而呈上。**

---

## §十　出艙形

報告 `docs/reports/W-G.9-264R_V1prime與角退登記.md`，須含：

a. 每一工具跑之**三欄**：`rc` ／ 逐字結論列 ／ `stderr` 位元組數。
b. 凡出艙一個計數，須**同時**出艙其**計數式**與**第二態之同式結果**。
c. 🔑 凡引用一個數，須標其**來源層**：**倉 blob**（`git cat-file`）／**工作副本**／**app**／**harness 落檔**／**harness log**。位元組數尤須具名。
d. 凡出艙「復現」二字，須載**框之來源**（正典逐字／自擬），且自擬之框⛔ 以待驗值為停止條件（`自誤 325` 之通則）。
e. 凡「不可得」者，具名其**阻斷點**，⛔ 以「未查」了事。
f. **⛔ 含任何 `app.py` diff、⛔ 含任何修法主張。**

🔒 **行號衛生**：本單⛔ 載倉內行號為錨；CC 亦⛔ 以行號（含 **log 列號**）為錨，一律用 **hunk 範圍**或**內容錨**。

---

*本單受詞 ＝ CC。零生產碼 ⇒ 得逕推。*

SELF_SHA256（＝ 本行以上全文之 sha256sum 值）: 0429b3a1546f043f45b94ac5996d0e3c586c9909e391c36a95f3f3e2a368ef52
