# 🔧 `W-G.9-198` 施工單：**`K-6-B` 段一「上鎖」＋ 街角指數<u>名次</u>之產出與出艙 ＋ 段二全域排序**

> **性質**：🔴 **幾何影響批·含生產碼**（`app.py`）。⇒ **二 commit 皆須<u>備妥後停機、請示 KL 方得 `push`</u>**（`常規一` 補款 `🔧 一`·`W-G.9-196 §零 6`）。
> **出單窗**：claude.ai（接 `W-G.9-197`）
> **基座**：`wip/s1-endpart` 之現時 `HEAD`（🛑 開工時自行 `git ls-remote` 實查全 `40` 碼**並記錄**；🔴 **該碼⛔ 為判準**——判準見 `S-0a`，綁**性質**⛔ 綁 commit 身分）
>
> 🔴 **本單為改版（`W-G.9-199` 同批發出）**：舊版之 `S-0a` 繫於「與 `W-G.9-197R` 所報之 commit 同一物件」，係 `自誤 246` 一般式所禁之形，且已因 `W-G.9-199` 之合法零生產碼插入而**實際失效** ⇒ 已登記為 `自誤 252`。舊版⛔ 未入倉 ⇒ ⛔ 無 append-only 之拘束。🛑 **CC 須以本檔<u>覆蓋</u>工作樹內同名舊檔**（以 `SELF_SHA256` 確認係新版）。

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　本單之寫法（🔒 承 `W-G.9-196 §七-9`）

🛑 **本單只寫<u>結果態</u>，⛔ 不寫<u>操作步驟</u>。** 凡 `§二`／`§三` 所列者皆係「落地後系統須呈現之性質」；**其達成手段由 CC 自定**，只要結果態逐項成立即為正辦。
🔴 CC 若發現某一結果態與另一結果態**互斥**、或與正典逐字牴觸 ⇒ **停機上呈發單側**（⛔ 不轉呈 KL·`常規二`／`常規三`），⛔ 不得自行取捨。

🛑 **⛔ 不得動之受詞（逐項具名）**
- `k6_step0_block_locked` 之**保守替代**（判準「兩側皆有 SIDE_LINE」）——**⛔ 不改一字**。其正典解係待 KL 域裁之循環依賴，已由 `GB-135` 登記。
- `k6_merge_groups`／`k6_shares_segment` 之**函式本體**——⛔ 不改一字（`W-G.9-196` 已證其非「零接線」）。
- `_pk_one_side_v12` 內既有之**三 `score_*` 算式、權重、`round` 位數、`qualified.sort` 之二鍵**——⛔ 不改一字。
- `k94_assert_baseline_touch` 及其訊息——⛔ 不改一字。
- `_ni` 之持久化還原路徑——⛔ 不改一字（已由 `GB-136` 登記·另案）。

🔒 **本單之一切「判別力」閘均依<u>裁 `H`</u>（`W-G.9-199`）之形**：判準綁**性質**（如「`指數名次` 欄⛔ 非全同值」「擾動該值 ⇒ 段二之序必變」），⛔ 綁計數代理。凡本單出現之計數皆為**待歸類之材料**、⛔ 非 operative 判準。

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之**性質**（🔒 承 `自誤 246`／`252` 之一般式·⛔ 綁 commit 身分） | 🔒 **立法理由 ＝ 「生產碼自 `7fab020` 起未動」**⇒ 判準二項：① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` **逐列⛔ 含** `app.py`、⛔ 不含任何 `verify/**.py`。二者任一不符 ⇒ 停機上呈。🛑 `ls-remote` 之全 `40` 碼**須記錄並回報**，但⛔ **作為判準**——倉內合法地多出任意數量之**零生產碼** commit 皆⛔ 得使本閘紅 |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md` | `P-3`／`P-5` 須不紅；🟡 逐項具名（見 `§零-c`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗 | `declared` ＝ `recomputed` |
| `S-0d` | 🔴 **改前基線之凍存** | 開工前先跑一次現行 harness 通道，將四閘違規全集（不截斷）與 `第 1 宗街角地` 相關出艙**存為改前態**，供 `§二-2`／`§三-2` 之判別力比對。⛔ 不得於改後才回頭重建 |

## §零-c　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6` 六檢·🔴 機械停機款 `0` 項）

| 檢 | 觸發點（字樣錨·`grep -F` 固定字串·⛔ 不用行號） | 處置 |
|---|---|---|
| `P-1`／`P-4` | `` \| # \| 落地後須成立之性質 \| ``（`§二-1`／`§三-1` 表頭·**二檢各命中**） | 🔒 **具名豁免**：係結果態之索引表頭；各列之母體、框與方向逐列自載於該列本文 |
| `P-1`／`P-4` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§零-b`／`§二-2`／`§三-2` 表頭·**二檢各命中**） | 🔒 **具名豁免**：係閘之索引表，各閘之母體／所查範圍／方向逐列具名於同表「受詞」與「判準」欄 |
| `P-1` | `` ⛔ 不辦段三（逐一嘗試·集中規則·跨街廓合併） `` | ✅ **已採納**——該否定之母體 ＝ **本單**（`§六` 逐字標題 ＝「本單不辦」），⛔ 非對倉之全稱否定；阻塞依據 `VR-086`／`GB-104`〜`GB-106` 已逐項具名 |
| `P-4` | `` - **段一**（`§二`·標題列 `` | 🔒 **具名豁免**：係正典標題列之引用（`→` 未出現於量之轉引）；所引逐字須由 CC 自倉重抽（`§一` 已明令） |

🔒 **發單側已實跑（改版後之本檔）**：🔴 機械停機款 **`0`【項】**；🟡 **`6`【項】**，逐項即上表（`P-1` `3`【項】／`P-4` `3`【項】；其中二個表頭係二檢各命中一次）；`P-5` `SELF_SHA256` ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。
🔑 **判別力自證**：同器對 `W-G.9-197` 得 `5`【項】、對 `W-G.9-199` 得 `0`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。

---

## §一　正典受詞（🛑 CC 開工時須自倉重抽·⛔ 不以本單所引為準）

`docs/rulings/K-6_街角地分配程序與可分配判準.md`：

- **段一**（`§二`·標題列 `### 段一：預掃（街廓內先評一輪，完全不看外部）`）——其上鎖逐字：
  「有任一筆 `G ≥ 街角規定面積` ⇒ **該街角定案**（達標者中比街角指數取 winner）；且該街廓內**所有**跨占該街角規定範圍之土地（含落選者）**一律上鎖**——不得被任何合併群抽走，亦不得作為合併標的（**既不出也不進**）。」
  「全部未達 ⇒ 該街角**待處理**，記下各候選之街角指數與名次。」
- **段二**（標題列 `### 段二：全域排序（僅排待處理街角之候選）`）——鍵序逐字：
  「① 街角指數**名次** → ② 街角試算 **G** → ③ 街角指數**數值** → ④ 暫編地號**字典序最小**（最終回退）。」

🔒 **現況正面列舉（發單側·態 `7fab020`·母體 ＝ `app.py` 與 `verify/stepg_pipeline.py` <u>二檔</u>·框 ＝ **列**）**：`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` 六者**逐檔皆 `0` 列**；對照組 `步驟 0` @ `app.py` ＝ `9` 列 ⇒ 量測器非紅。

---

## §二　`commit 1`　**名次之產出與出艙 ＋ 段二全域排序 ＋ 措辭更正**（🔴 預期：**⛔ 無土地後果**）

### §二-1　結果態（逐項·⛔ 不寫手段）

| # | 落地後須成立之性質 |
|---|---|
| `R1-1` | **每一側之每一候選**（含 `qualified` 與 `eliminated`、含**段一全滅**之早退分支）皆帶一個**街角指數名次**。其值 ＝ 該側 `group` 全體依 `priority_index` **降冪**之名次，`1` 起算、**連續**（`1,2,…,n`·⛔ 不跳號）。 |
| `R1-2` | `R1-1` 之**平手決之**⛔ 不另立第二套：其平手鍵須與現行 `qualified.sort` **同源**（`priority_index` 取 `4dp` 判平手 → `_pre_position_rank` 升冪）。🛑 ⛔ 不得引入任何新的比較量。 |
| `R1-3` | 出艙「第 1 宗街角地逐候選評點明細」**增一欄**，欄名逐字 ＝ **`指數名次`**，位置 ＝ `總分` 之**後**、`原位次(投影序)` 之**前**。**該欄逐列皆有整數值**（⛔ 無 `—`、⛔ 無空白）。 |
| `R1-4` | 既有 `23` 欄之**欄名、順序、值**逐格⛔ 不變（新欄係**插入**，⛔ 非取代）。🛑 ⚠️ **例外之預告**：若某欄因 `R1-3` 而由「含 `—`」變為「全數值」，其零之**字面**可能由 `'0.0'` 變為 `'0'`（型別轉換之最小表示法·成因已於 `W-G.9-197` 閉合）⇒ **該類差須逐格具名並證其數值相等**，⛔ 不得計入 `D1-a` 之土地後果。 |
| `R1-5` | 🔴 **`GB-133` 之失效條件成就**：段一全滅之側，其各候選之**指數與名次皆已算出並出艙**，且該出艙**可供段二鍵序 ① 直接消費**（＝ 段二之實作**確實讀該欄／同一資料源**，⛔ 不自行重算）。 |
| `R1-6` | **段二全域排序之出艙存在**：一份**僅含待處理街角**（＝ 該側無任一候選達標）之候選清單，依鍵序 ① 名次 → ② 街角試算 `G` → ③ 指數數值 → ④ 暫編地號字典序最小 排定，並出艙為可檢視之表（欄須含：街廓、端、暫編地號、名次、街角試算 `G`、指數數值、最終序位）。 |
| `R1-7` | 🛑 **段二之出艙係<u>唯讀</u>**——⛔ 不得被任何既有分配邏輯消費、⛔ 不得改變任何既有分支之走向。（段三尚未落地，其消費端不存在。） |
| `R1-8` | **措辭更正**：`app.py` 內 `` # 🆕 V13 修正 #3 + #7：**第二關**優先權指數採用三項法定加權 `` 一段（現位於早退**之前**），其「第二關」之表述改為對**落選候選亦精確**者。🛑 **本項係生產碼**（改 `app.py` 一字含註解即是·`W-G.9-195 裁 E`）⇒ **併入本 commit**、⛔ 不另開 commit。 |
| `R1-9` | 🔒 **側無關·案無關**：`R1-1`〜`R1-8` 之實作⛔ 不得出現任何街廓名（`R1`〜`R6` 等）、側標（`左`／`右`／`p1`／`p2`）之硬編分支，⛔ 不得出現任何本案特有常數（`308.17`／`3.5` 等）。 |

### §二-2　`commit 1` 之驗收（🔴 判別力須具名·⛔ 不得只報綠）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `D1-a` | 🔒 **土地欄零變** | 以 `S-0d` 所凍存之改前態為基準，「第 1 宗街角地逐候選評點明細」之**土地欄**（`真交集`／`整筆幾何`／`範圍面積`／`G估`／`真G`／`門檻`／`達標`／`原位次(投影序)`／**`選中`**）**逐格有變 `0` 格**。有一格即 ⇒ **停機上呈發單側**（⛔ 不得自行歸因） |
| `D1-b` | 四指數欄零變 | 四指數欄**數值真變 `0` 格**；字面差須逐格具名並報其數值是否相等（承 `R1-4` 之例外預告） |
| `D1-c` | 🔴 **新欄之判別力** | `指數名次` 欄**⛔ 非全同值**（若全為 `1` 或全為同一數 ⇒ 判別力零 ⇒ 紅）；且其與 `總分` 之降冪序**逐側一致**（機械驗·⛔ 不目視） |
| `D1-d` | 🔴 **`R1-5` 之機械證** | 段二排序之實作，其鍵序 ① **確實取自 `指數名次` 之同一資料源**——以「把該值人為擾動 ⇒ 段二輸出之序**必變**」證之。**不變 ⇒ 未接線 ⇒ 紅** |
| `D1-e` | `R1-7` 之機械證 | 段二出艙**存在**且**⛔ 無任何呼叫端消費**（正面列舉其符號於 `app.py`／`verify/**.py` 之出現處，逐處具名其係「產出」抑或「消費」） |
| `D1-f` | 四閘違規全集 | 以 `probe_WG9168_viol_full` 取**不截斷**全集，二態差集**逐格歸因**；🛑 **凡出現於 `winner`／達標候選列者 ⇒ 停機上呈** |
| `D1-g` | `--selftest` | 既有全部自檢仍過；⛔ 不得刪改任一既有自檢 |
| `D1-h` | 🔴 **既有終局⛔ 未移位** | harness 之終止點與 `W-G.9-197R` 所載者**逐字相同**（`k94_assert_baseline_touch`／`R5` `628-53(2)`／差 `38.803m`）。**若終止點移動 ⇒ 停機上呈**（⛔ 不得視為改善） |

🛑 **⛔ 不得以「對帳器綠」宣稱無行為變更**（`GB-111` 加註：`128` 列真實變更於對帳器上完全不可見）。
🛑 **⛔ 不得以 harness 之綠宣稱土地後果**（`GB-123`）——`commit 1` 之土地後果**須待 KL UI 實跑**方得確認；CC 之回報須逐字寫明「土地後果⛔ 未確認·待 KL UI」。

### §二-3　`commit 1` 之 `push`

🔴 **備妥後停機**，向發單側逐字回報 `D1-a`〜`D1-h`、`numstat`、`app.py` 新 blob 與 `def main`／`_pk_one_side_v12` 之**新 AST 區間**（⛔ 不得沿用舊值）。**⛔ 不得自行 `push`**——由發單側請示 KL 後放行。

---

## §三　`commit 2`　**段一「上鎖」**（🔴 **可能有土地後果**）

### §三-1　結果態（逐項）

| # | 落地後須成立之性質 |
|---|---|
| `R2-1` | **上鎖之判準**：某街角於段一**定案**（＝ 該街角之候選中存在任一筆 `G ≥ 該街角規定面積`）⇒ 該**街廓內所有跨占該街角規定範圍之土地**（🛑 **含落選者**）取得「上鎖」狀態。 |
| `R2-2` | **上鎖之效力（逐字·二向皆須）**：上鎖之土地 ⛔ **不得被任何合併群抽走**（不出）∧ ⛔ **不得作為合併標的**（不進）。 |
| `R2-3` | **生效時點 ＝ 段一**（正典 `K-6` 令段一「只取…**步驟 0 後**…之土地」）。🛑 **⛔ 不得**改動 `k6_step0_block_locked` 之保守替代一字——步驟 0 之時點另有 `K-9-21` 之保守替代，其正典解係待 KL 域裁之循環依賴（`GB-135`）。 |
| `R2-4` | 上鎖狀態**可出艙**：新增一份（或於既有診斷增欄）可檢視之清單，逐筆載：街廓、暫編地號、所跨占之街角、上鎖與否、上鎖之緣由（哪一個街角定案）。 |
| `R2-5` | 🔒 **側無關·案無關**：⛔ 無街廓名、側標、本案常數之硬編。判準綁**性質**（`G ≥ 規定面積`、`跨占規定範圍`、`同街廓`），⛔ 不綁塊名。 |
| `R2-6` | 🔴 **與 `K-9-21` 之關係須具名**：CC 須逐字回報「`R2-1` 之上鎖集合」與「`k6_step0_block_locked` 所擋之群」二者之**包含關係**（於本案實測）。🛑 若二者**互斥或衝突** ⇒ **停機上呈發單側**。 |

### §三-2　`commit 2` 之驗收

| 閘 | 受詞 | 判準 |
|---|---|---|
| `D2-a` | 🔴 **土地後果之逐格歸因** | 以 `commit 1` 落地後之態為基準，「第 1 宗街角地逐候選評點明細」之**土地欄**逐格比對。**凡有變之格，逐格具名其緣由**（哪一筆被上鎖、原本會與誰合併）。🛑 **⛔ 不得只報「有 N 格變」**——須逐格 |
| `D2-b` | 上鎖集合非空且非全集 | 上鎖集合之基數 **⛔ 為 `0`**（⇒ 未咬到）**亦⛔ 為全部候選**（⇒ 判別力零）。二者任一 ⇒ 紅 |
| `D2-c` | 🔴 **反靜默退路** | 上鎖判定⛔ 不得有任何 `else` 靜默預設（承 `GB-106` 之教訓）；凡取不到值 ⇒ **停機報錯**，⛔ 不得視為「未上鎖」 |
| `D2-d` | `R2-6` 之實測 | 二集合之包含關係逐字回報；本案預期 `k6_step0_block_locked` 只擋 `R4` 之群（該實測逐字見其註解），CC 須報**實際值**·⛔ 不得沿用該註解之數 |
| `D2-e` | 四閘違規全集 | 同 `D1-f`；差集逐格歸因 |
| `D2-f` | `--selftest` | 既有全部自檢仍過 |
| `D2-g` | 🔴 **既有終局⛔ 未移位** | 同 `D1-h`。🛑 **`②-宗 圍堵閘破[R5]` 之破量亦須併報**（改前 → 改後）：其既有實測 ＝ `56.8186`（`verify/out/probe_WG9187_baseline_76378d09.log:14`·上界 `1.5999`·宗數 `8`·深度 `45.71`），KL `7fab020` 實跑得 `56.8187`。**若本批使該值改變 ⇒ 逐格歸因，⛔ 不得靜默** |

🛑 **本案之預期受詞**（供 CC 對照·⛔ 非判準·⛔ 不得寫入碼）：八街角中僅 **`R2 左`** 為「待處理街角」（四候選真 `G` 皆低於門檻 `308.17` ㎡·最接近者 `628-40(1)+` 差 `−21.52`）；其餘 **`7` 街角**（分布於 **`5` 街廓**）皆於段一定案 ⇒ 其跨占者依 `R2-1` 皆應上鎖。
🔒 **上開已由 KL `7fab020` 實跑逐列坐實**：UI 逐字「已採用步驟 L 之街角地 PK 結果（覆寫 **7** 筆暫編地號之街角標記）」／「**1** 個街廓有強制抵費地街角：`R2`（左側）」；`T2-DIAG` 之 `R2` 池帶面積首片 ＝ `308.1700` ㎡（＝ 該街角規定面積）。

### §三-3　`commit 2` 之 `push`

🔴 **備妥後停機**，逐字回報 `D2-a`〜`D2-g`。**⛔ 不得自行 `push`**——由發單側請示 KL 後放行。
🔴 **土地後果之最終確認繫於 KL UI 實跑**（`GB-123` ⛔ 未解除）：CC 之回報須逐字寫明「土地後果⛔ 未確認·待 KL UI 實跑」。

---

## §四　附帶量測（🛑 ⛔ 不阻塞本批之驗收·須逐項回報）

| # | 受詞 | 要回報者 |
|---|---|---|
| `M-1` | `②-宗 圍堵閘破[R5]` 之破量：倉內既有 `56.8186` vs KL `7fab020` 實跑 `56.8187`（差 `0.0001`） | 二值是否同一母體（宗數 `8`／深度 `45.71`／上界 `1.5999`）；差是否僅為 4dp 顯示末位。⛔ 不修、⛔ 不鑄號，繫 `GB-67` |
| `M-2` | `R3` 之 `Phase 8 抵費地碎片合併產生 MultiPolygon（2 片）⇒ 取最大片、小片幾何丟失而面積留帳` | 該警示於本批前後是否逐字不變；⛔ 不修（§N5 重審標的） |

---

## §五　落地點與報告

| # | 檔／物 | commit |
|---|---|---|
| `L-1` | `app.py`（`R1-1`〜`R1-9`） | `1` |
| `L-2` | `app.py`（`R2-1`〜`R2-6`） | `2` |
| `A-1` | `docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md` | 🔒 **置於 `commit 1` <u>之前</u>之一個獨立零生產碼 commit**（承 `W-G.9-195 裁 F`：出單側須先問「這張報告推得出去嗎」——本單之生產碼 commit 須待 KL 放行，故單與報告⛔ 不得綁在其後） |
| `A-2` | `docs/reports/W-G.9-198R_…_執行報告.md` | 🔒 同上·**零生產碼 commit**，CC 可逕行 `push`；其內容 ＝ `commit 1`／`commit 2` **備妥但未推**之逐項量測 |

🔒 ⇒ **三 commit**：`A`（零生產碼·單＋報告·逕推）→ `1`（app.py·備妥待放行）→ `2`（app.py·備妥待放行）。
🛑 `commit 1`／`2` 之 SHA 一經備妥即**⛔ 不得重做、⛔ 不得 amend**；放行時以**同一物件**之判準（全 `40` 碼 ∧ `tree` ∧ `numstat` ∧ `app.py` blob）核驗（承 `自誤 246` 之一般式）。

---

## §六　⛔ 本單不辦

⛔ 不辦段三（逐一嘗試·集中規則·跨街廓合併）——其消費端不存在，且 `VR-086`／`GB-104`〜`GB-106` 未解除。
⛔ 不辦 `K-9-23` 二閘（結構上下游·`K-6-B` 優先序 KL 裁 `2026-09-01`）。
⛔ 不辦 `GB-111` 凍存名單重產（時點繫 KL 已裁「俟二閘落地後」）。
⛔ 不辦 `k6_step0_block_locked` 之正典化（待 KL 域裁·`GB-135`）。
⛔ 不辦 `GB-136`（`_ni` 之區間夾制）。
⛔ 不辦 `GB-67`（`②-宗` 破量之成因）／`R5` 之屁股線終局／`R3` 之 MultiPolygon。
⛔ 不辦九份 baseline 重產。
⛔ 不辦 `GB-132` 類 ①。

SELF_SHA256: f16bdb9e5c52b4040fd7ebb73dcb9776c9c19b8f98908f94be8caea1e88695ec
