# `W-G.9-290`　施工單（**現查·零生產碼**）：winner 側之第 1 宗如何取得其**近側界** ／ 二態之路徑分歧點

> **態**｜開工 ＝ `39ab6e3252a735940918f2a1c284f2dac34e67c6`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-290_施工單_winner側第1宗之近側界與路徑分歧點.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（純現查與量測·**零生產碼**·⛔ 改碼一字·⛔ 替身·⛔ 反事實）
> 🛑 **本批⛔ 鑄任何號**（`自誤`／`GB`／`VR`／`K-9`／坑 皆⛔）；`ALPHA_LABELS` **⛔ 動一字**；**⛔ 開任何分支**。
> 🔒 **KL 之放行仍在**（`W-G.9-288` 已登記）——惟其所准之改**尚⛔ 可施**（`W-G.9-289` 硬停機款觸發）⇒ **本批⛔ 動用之**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·**同格載產生指令**·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `39ab6e3252a735940918f2a1c284f2dac34e67c6` 或其後裔（**全 `40` 碼**·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`）；判別力[必為祖先] ＝ 本批基座之任一祖先（`rc = 0`）。🛑 `rc` 之取用須**緊接該指令**，⛔ 經 `\| head` 等管線（`W-G.9-289` 自捕三） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `SELF_SHA256` 逐位相符；判別力二造須**先自證 bytes 相異** |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**） | [必真] 含 `W-G.9-290` ＝ `True`／[必偽] 含 `W-G.9-289` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`383`**／**`393`**｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`** |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec `verify/*.py` 多咬之子層檔數 **須 `> 0`** |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**、payload 之落地形機驗）**逐項適用·⛔ 省**。
🆕 **三則新附款**（`W-G.9-289` 自捕所立·**皆朝放寬失準** ⇒ ⛔ 省）：
`(a)` 凡以**函式名**為字樣錨者，一律**二形並取**：`名(` ⋀ `ns["名"](`（⛔ 只取其一）；
`(b)` 凡蒐集**模組頂層全域**者，框 ＝ `ast` 之 **`t.body` 頂層敘述**，**⛔ `ast.walk`**（其會把巢狀函式之區域名算成全域）；
`(c)` 凡取 `rc` 者，**緊接該指令**取之，⛔ 經管線（`\| head`／`\| tail` 之 `rc` 恆 `0`）。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-289` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `5c03acb12c23d851…`）／
生產碼 `34` 檔於 `d01749c9..39ab6e32` 異動 **`0`**（**硬不變量成立**）／自誤 `383`／`393`（＋一）·其餘三簿未動／
分支 `verify/W-G.9-289-gb170` 之 `ls-remote` 命中 **`0`**（**⛔ 開**）／`_build_corner_range_v3` 之外層唯一 `return` ＝ `return rng`（發單側自倉覆核）
⇒ **✅ 全批核可**。
🔑 **硬停機款之觸發：處置完全正確**——其為**單自身所預置**之路徑，⛔ 受單側之自裁；`§四`／`§五` **⛔ 辦**、分支**⛔ 開**、
**KL 之放行未被動用**。收工閘 `6'` 因**受詞缺漏**而 **loud 拒測**（⛔ 以「未觸」充綠·⛔ 以「無從回測」充「回測不過」），處置亦正確。
🩸 三則自捕**皆朝放寬之方向失準**（皆會把「停機」誤讀為「可續」），皆於出艙前攔下 ⇒ 已立為 `§零` 之三則新附款。

---

## `§一`　裁

| # | 事 | **裁** |
|---|---|---|
| `1` | `S1_par` 之既有來源於**母體 ＝ 生產碼 `34` 檔**（正面列舉·`W-G.9-289` 之現查·四個 `forced` 消費端逐處具名）內**不存在** | **受領**。⇒ `W-G.9-289 §四-1` 所設之「最小實作之形」**⛔ 成立**，**撤回**。🔴 **⛔ 鑄自誤**——該單 `§三` 即為此而設之現查，其硬停機款**如期攔下**，⛔ 為未驗證之斷言 |
| `2` | 由 `corner_range_polys` 之多邊形反推 `S1_par` | **維持⛔**（「另算第二份」）。**且本批已不取該路**——見 `§三-1` 之由 |
| `3` | 單載分級「重」而實為零生產碼 | **照實受領**·⛔ 鑄號。分級係**預期**之標示，其與實況之落差於停機時屬必然 |
| `4` | 下一步之形 | **⛔ 逕擬第二實作形**。改辦 `§三` 之**現查**：winner 側之第 1 宗**已然**取得一條 ∥SIDELINE 之近側界（`R2/left@3.5m` 之藍影閘 `2` 次·`ok=True` ⇒ 該機制**存在且運作**）⇒ 先查**它怎麼取**，再談 forced 側能否走同一路 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-290_施工單_winner側第1宗之近側界與路徑分歧點.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-289` 之單須 `≥ 1`／造乙 ＝ 執行期組出之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（框 ＝ `tr -cd '\r' \| wc -c`·⛔ `od`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢·現查**）：winner 側之第 1 宗如何取得其**近側界**

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

`W-G.9-287` 已量得：`R2/left@3.5m` 之藍影閘（`K-9-23` 閘一）求值 **`2` 次**且 `ok=True`
⇒ 該側**確有**一個「近側界 ∥SIDELINE ⋀ 遠側界 ∥ALLOCLINE」之宗，且其不交叉之檢**已在跑**。
⇒ **該機制存在且運作**。本項查其**怎麼取那條近側界**——若 forced 側能走同一路，則所需之改在**路由**，⛔ 在幾何。
🛑 **⛔ 由此推定任何實作形**；本項**出艙即止**。

### 三-2　逐款出艙（🛑 錨取字樣·**二形並取**·**先驗唯一性**·無 `^` 錨 ⇒ **二數並報**）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | `is_second_after_corner` 之**賦值鏈逐字**（自其定義處至其消費處·逐處具名 `檔:函式`） | 母體 ＝ `app.py` ＋ `verify/` 全部 `.py`（**含子層**·正面列舉檔數） |
| `2` | 於 `R2/left@3.5m`，該為真之宗（**第 1 宗**）之**近側界**係取自何物 ⇒ **逐字具名其賦值鏈**與**該物之產生者** | 🛑 出艙其**方向單位向量**與**二端點**；併出艙其與 `corner_range_polys[(blk,side)]` 之**遠側境界線**之逐位對拍（`\|cos\|`／垂距／端點差） |
| `3` | 同格之 `cum_S`／`W_0` 之**當場值**與其**產生式逐字**；其起算**是否亦走 s-帶** | ⛔ 轉引前批之數 |
| `4` | 🔑 **二態之路徑分歧點**：forced 側（`R4/left@0m`）與 winner 側（`R2/left@3.5m`）自**同一入口**起，其**第一個取值相異之分支**——逐字出艙該 `if`／其條件之**各分項真值**（逐格逐分項·⛔ 只報合取） | 入口由 CC 自碼面現查並**逐字具名**；⛔ 採本單之任何預設 |
| `5` | 街角地本身（**第 0 宗**）之**遠側界**於二態之產生者：winner 態（由 WINNER 之 `G` 決定）與 forced 態（`_corner_buffer_S` 之帶）⇒ **二者之產生式逐字並列** | 🛑 **⛔ 判二者應否同一**；出艙即止 |
| `6` | `corner_range_polys` 之**鋪設處**（`ctx` 之 `:413`／`:429` 之當場現查·⛔ 採本單之行號）與其**消費處全部** | 逐處具名 `檔:函式`；併出艙其於 forced 側**是否被消費** |
| `7` | 判別力二造 | [必命中] `is_second_after_corner` 為真之格數 `≥ 1`（若 `0` ⇒ **loud**·⛔ 以空集充綠）／[必為零] **執行期組出**之人造名於同框須 `0`（**先驗現母體命中為 `0`**）。任一不成立 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判 forced 側應否走同一路、⛔ 擬任何實作形、⛔ 改碼一字、⛔ 開分支、
⛔ 鑄任何號、⛔ 解除或收窄 `GB-170`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§四`　工項二：一則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號·⛔ 改任何原節一字）

> **`GB-170` 之修之第一次嘗試：硬停機款觸發之登記（`W-G.9-289`·⛔ 解除、⛔ 收窄）**
> 所擬之最小實作形 ＝「`forced` 消費端之 `cum_S` 改取 `S1_par`（自**既有**來源取回）」。
> **現查坐實其不可施**：`_build_corner_range_v3`（AST 區間 `12676`–`12899`·當場重得）之**外層唯一** `return`
> ＝ `return rng`（`Name` 節點·shapely 多邊形）；`S1_perp`／`S1_par`／`_sin_t`／`_Tp` **皆為區域值**
> ⇒ 四個 `forced` 消費端（`app.py` 之 `main`·`verify/stepg_pipeline.py` 之 `_run_step_g_impl`·
> `verify/wf_f1.py` 之 `compute`·`verify/wf_f4.py` 之 `_reshape_block`）**皆判 `⛔ 可達`**。
> 併記：`\|S1_par · sinθ − S1_perp\|` 於三 `forced` 格皆 **`0.0`**（當場重得）⇒ 該恆等**成立**，
> 不可施者係**取得該值之途徑**、⛔ 該恆等。
> 🛑 **生產碼一字未動**（三區間 `diff` 皆 `0` 行）；**KL 之放行未被動用**；分支 `verify/W-G.9-289-gb170` **⛔ 開**。
> 🛑 **`GB-170` 之失效條件三款⛔ 有一款成就。**

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**、簡繁機檢之 `rc`；
🛑 **本 payload ⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留**（**⛔ 剝層**），並出艙其落地形之機驗。

---

## `§五`　收工閘

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob） | `0`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob |
| `4` | 四簿期末 | 自誤 `383`／`393`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` ⇒ **四簿逐位未動**（坐實⛔ 鑄號） |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` `git diff --numstat` 輸出**空** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add`；⛔ 以「意思有到」充命中 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修** |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-289-gb170` 命中 **`0`**（⛔ 開）；`p3a` 命中 `1`（判別力[必存在]） |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§六`　🛑 **⛔ 為之事**

⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 擬任何第二實作形；
⛔ 由多邊形反推 `S1_par`；⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／任何函式簽章；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、⛔ 併線 `p3a`、**⛔ 引任何自 baseline 取得之數**；
⛔ 鑄任何號；⛔ 解除或收窄任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 改藍影一字、⛔ 判該側是否應有第 2 宗、⛔ 判 forced 側應否走 winner 側之路；
⛔ 追改任何已入倉文件一字；⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§七`　報告之格

`§零` 開工閘 → `§一`〜`§二` 逐工項（逐款以落檔**全量**出艙·⛔ `tail -N`）→ 判別力二造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`is_second_after_corner`／`corner_range_polys`／`_k923_gate1`／`_build_corner_range_v3`／`_corner_buffer_S`／
`cum_S`／`W_0`／`S1_par`／`GB-170`／`ast`／`numstat`／`ALPHA_LABELS`／`baselines`／`sha256`／`ls-tree`／`merge-base`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: 3f8ea148da416230c10d12f2d8e75639e38ca7deaa4baa4041230ecb8e2e1f44
