# W-G.9-260　交接文 `-259` 入倉 ＋ `自誤 321`（前窗「紅 `32`」量測誤）＋ `GB-153` 歸因更正（相異 `8` → `0`）＋ `GB-154` 鑄（`telescoping` 獨立）＋ `telescoping` 定位（**純診斷·⛔ 修**）

> 🛑 **本單全批零生產碼** ⇒ 依現行條款 CC **得逕推**，⛔ 候 KL 逐 commit 放行。
> 🛑 **射程**：`工項四` **只定位、⛔ 修**（同 `-257` 之丙案射程·KL 裁 `2026-09-08`）。
> 🔒 **接手窗已換**（`claude.ai` 側）。`-257R`／`-258R` 之結果本窗已獨立復驗，見 `§三`。

---

## §零　開工閘（**逐條當場復現·任一不符 ⇒ 停機上呈**）

| 閘 | 應得 | 器 |
|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ **`1531916`** | `git rev-parse origin/wip/s1-endpart` |
| `2` 受詞未動 | `app.py` ＝ `4860f10828fb8ae9`／`verify/stepg_pipeline.py` ＝ `c8d46d2e5fb79dc5`／`verify/wf_f0.py` ＝ `b0722355c1978635`（sha256 前 `16`）| `git cat-file blob HEAD:<f> \| sha256sum` |
| `3` 號占用 | `W-G.9-260` 之**宣告框** ＝ `D1 0`／`D2 0`／`D3 0`（母體全 `docs/`）| 見 `§六 V-N` |
| `4` 次號 | `自誤 MAX` ＝ **`320`**／`GB MAX` ＝ **`153`**／`K-9 MAX` ＝ **`29`** ⇒ 本批鑄 `自誤 321`／`GB-154`，**⛔ 鑄其他號** | 見 `§六 V-N` |
| `5` `S-0b` | `probe_order_preflight.py` `rc=0`、`P-5` 與 `§末` 之 `SELF_SHA256` 相符 | 現行條款 |

🩸 **錨漂移之處置**：若 `1` 不符而 `2` 三檔逐位相符 ⇒ **受詞未變**，續辦並於報告 `§零` 具名漂移之 commit；若 `2` 任一不符 ⇒ **停機上呈**。

---

## §一　工項零：**二檔原封入倉**（`docs/orders/`）

a. **本單**（`W-G.9-260`）。
b. **交接文 `W-G.9-259`**（`SELF_SHA256: f5dc385fe1ba20412b7ef6579ee580a60495b594aade180a6d2a0006ef144d7a`）
　⇒ 🛑 **原封入倉，`⛔` 執行其所列之任何工項**。其受詞係 `claude.ai` 窗，非 CC。
　🔒 KL 已裁本項併入本單（`2026-09-08`）。

🔒 二檔皆**新建**，⛔ 動任何既有檔一字。

---

## §二　工項一：`自誤 321` 落簿（**檔末追加**·`docs/reports/W-G.9波_claude.ai側自誤登記.md`）

> ### 🩸 `自誤 321`　**前窗發單側之 harness 紅項數量測誤：載 `32`、真值 `24`**
>
> **受詞**：`W-G.9-256` 案由逐字所載——「發單側於 `2026-09-08` 實跑 `verify/run_verification.py`＠`8a52dbd`（安裝六依賴後）得 `RESULT: FAIL`、紅 `32` 項」；`GB-153` 案由（`W-G.4:5125`）與交接文 `-259 §五` 沿用之。
>
> **真值**：接手窗於 `2026-09-08` 以**同一入口、同一 commit、同一依賴集**實跑，得 **紅 `24`**。
> 復現三態：`8a52dbd`／`a5b4edd`／`1531916` **皆 `24`、紅項名稱逐名全等**（差集雙向皆空）。
> 🔒 **`8a52dbd → a5b4edd` 為零生產碼**（純文件五檔·`git diff --name-only` 具名）⇒ 二態行為**必**同 ⇒ 「`8a52dbd` `32` 而其後 `24`」**內部相斥**。
> 🔒 **⛔ 計數框之異**：`24` FAIL 列／`29` 個 `🔴`／`27` 含 `FAIL` 之列——**無一得 `32`**。
>
> **共相**：**⛔ 為己之實測立復現式**。該數係單一次跑之目視計數，未以機械式（`grep -c`）出艙、未於第二態復現。
> 🔑 **與 `自誤 319` 同族之反面**：`319` 係「立驗收而未跑該路徑一次」；本則係「**跑了一次而未使其可復現**」。⇒ **跑過** ⛔ 蘊含 **數對**。
>
> 🔒 **通則（本則所立）**：凡出艙一個**計數**，須同時出艙其**計數式**（可貼即跑之一行）與**第二態之同式結果**；⛔ 以目視計數出艙。
>
> 🛑 **⛔ 追改 `W-G.9-256`／`GB-153` 原節任一字**（`常規四（九）五`）——其更正之唯一載體為 `工項二`。

---

## §三　工項二：`GB-153` 之**歸因更正**（`W-G.4` **檔末追加**·⛔ 動原列一字·⛔ 鑄號）

> ## 🔧 `GB-153` 歸因之更正：`②-宗` 停用之相異由「上界 `8`」更正為 **`0`**（`W-G.9-260`）
>
> 🛑 **本節⛔ 鑄任何號**（`戒 36`）。🔒 **⛔ 動 `GB-153`／`GB-67` 原節任一字**。
>
> ### 一　`W-G.9-257R` 所載「相異 `8`（上界）」**不成立**
>
> `-257R §九` 之「停用 `②-宗` 後紅 `24`（案由所載 `32`）⇒ 相異 `8`·上界」係
> **CC 之閘關實測（容器甲）減前窗之閘開實測（容器乙）**——二數分屬**二容器**。
> 🔴 **⇒ 該相減係 `proxy` 代 `property`**；且其減數 `32` 已由 `自誤 321` 證為誤值。
> 🔒 **⛔ 歸咎 CC**——CC 已逐字自限為「上界·⛔ 逐項歸因」，且⛔ 能知他窗之數為誤。
>
> ### 二　**同一容器**之閘開／閘關對照（接手窗實測·`2026-09-08`）
>
> 於 scratchpad 複本（⛔ 本倉）將 `app.py:9523` `②-宗 圍堵閘` 之 `raise` 降為 `print`（受詞命中恰 `1`·`py_compile rc=0`），與未改態同容器對跑：
>
> | 態 | 紅項數 | 紅項名稱差集 |
> |---|---|---|
> | 閘**開**（`1531916` 原態）| **`24`** | —— |
> | 閘**關** | **`24`** | **雙向皆空** |
>
> ⇒ 🔒 **相異 ＝ `0`（恆等·⛔ 上界）**。
>
> 🛑 **界（⛔ 逾）**：本測證「**停用該閘之停機⛔ 動任何紅項**」，
> **⛔ 證「修好 `R2` 那 `45.9766㎡` 之幾何⛔ 動土地」**——後者必動（該面積係 `R2` 內被重複計之真實面積）。**二者⛔ 混。**
>
> ### 三　`R2` 四次呼叫之**情境全部具名**（`-257R §四` 之阻斷項**已解**·⛔ 需另一次注入）
>
> | 呼叫 | 節（`rv.log` 節題界定）| `biz_x` | 破? |
> |---|---|---|---|
> | `#0` | `Step G headless（0m…）` | `45.9766` | 🔴 |
> | `#1` | `Step G headless（3.5m…）` | **`0.0000`** | 🟢 |
> | `#2` | `W-D.3 碎片` | `45.9766` | 🔴 |
> | `#3` | `W-D.4 四梯` | `45.9766` | 🔴 |
>
> ⇒ `-257R §八`「尚差三項證據」**去其一**（那一次 `0.0000` ＝ **`3.5m`**）。
> 🔑 **通則**：其阻斷點係「**注入未記標籤**」，惟**未改之日誌以節題界定即足** ⇒ 凡宣告一量不可得前，須先查**既有輸出之結構**是否已足。
>
> ### 四　`GB-153` 射程**維持原載**；歸因確定為
>
> `GB-67`（`②-宗`）與 `telescoping` **二病**，且：
> **`0m` 情境二者皆必要**（`②-宗` 先擋）／**`3.5m` 情境僅 `telescoping` 在擋**（`②-宗` 該次 `0.0000`）
> ⇒ 🔴 **二者皆必要、皆非充分**；**只解 `GB-67` ⛔ 能使 `R3`–`R6` 可達**。
>
> ### 五　工序之更正（發單側自裁·判準 ＝ ⛔ 動任何一宗地之配地結果·KL 已知會 `2026-09-08`）
>
> `telescoping` 自 `W-G.9-241R` 起恆以 `GB-67／telescoping` 之附屬形出現、**⛔ 自己之號**。
> 依 `一`–`四`，改列**獨立第 0 項**，與 `GB-67` **並列**（見 `工項三`）。

---

## §四　工項三：鑄 `GB-154`（`telescoping` 結構閘之獨立登記·`W-G.4` **檔末追加**）

> ## 🆕 `GB-154`　`結構閘 telescoping` 破（`R2 left`·**二情境二機制**·`GB-67` 之外的獨立阻斷點）
>
> 🔒 **與 `GB-67` ⛔ 同源之機械證據**：見 `-257R §五` 二項 ＋ 本單 `工項二 二`（閘關 `Δ=0`）。
>
> **受詞**（接手窗於 `1531916` 閘關態實測·`verify/stepg_pipeline.py:1436`–`:1444`）：
>
> | 情境 | `ΣRw_實跑` | `R(W_final)−R(W₀)` | `Δ` | 逐界面 |
> |---|---|---|---|---|
> | `0m` | `106.16` | `R(26.03)−R(0.00)＝100.00` | `6.160` | 首界面 `628-41(1)`→`628-42(1)` `Δ_self=-11.3570`·🔄方向改變 |
> | `3.5m` | `100.02` | `R(34.39)−R(6.98)＝44.41` | `55.606` | **全 `6` 界面 `Δ_self=+0.0000`·⛔ 方向改變** |
>
> 🔴 **`3.5m` 之詭**：**鏈逐界面全未斷**（`_bad_a` 空·未觸 `:1431` 之鏈斷 raise），**總和仍差 `55.606`**。
>
> 🛑 **本節⛔ 含任何修法主張**；成因待 `工項四`。

---

## §五　工項四：`telescoping` 定位（**純診斷·⛔ 修**·同 `-257` 之丙案射程）

### a　發單側之推定（**⛔ 已證**·令 CC 證之或推翻）

🔒 **算術閉合**（接手窗以 `app.py:5728 rw_from_width` 之表逐位重算）：

| 量 | 算得 | 日誌實載 | 殘差 |
|---|---|---|---|
| `R(34.39) − R(6.98)` | `44.4140` | `44.41` | — ⇒ **我之 `R` 與生產同源** |
| `R(34.39) − R(0)` | `100.0000` | `100.02`（`ΣRw_實跑`）| **`0.020`** |
| `R(6.98)` | `55.5860` | `55.606`（`Δ`）| **`0.020`** |

🔴 **推定**：`3.5m` 之 `ΣRw` **自 `W=0` 起累**，而閘期望**自 `W₀=6.98` 起累**
⇒ 那 `55.6` 之差**即 `R(W₀)` 本身**；**同一 `0.020` 殘差同時出現於二處**，疑為逐宗 `Rw(%)` 之 2dp 捨入累積。
🔒 **`0m` ⛔ 適用本推定**（其 `W₀=0.00` ⇒ 本推定預測 `Δ=R(0)=0`，實得 `6.160`）⇒ **`0m` 係逾 `100%` 之溢計、另一機制**。

### b　令 CC 答之四題（**注入落拋棄式 clone·⛔ 本倉**·`-257` 之法沿用）

- **`Q-1`（決定性單值）**：`3.5m`·`R2 left` 之**鏈首宗**（`628-42(1)`）之 `Rw(%)` 實值為何？
  🔑 **判**：若 ≈ `0`（＝`R(6.98)−R(6.98)`）⇒ 推定**偽**；若 ≈ `55.59`（＝`R(6.98)−R(0)`）⇒ 推定**真**，且全部差額集中於**該一宗**。
- **`Q-2`**：`3.5m`·`R2 left` **逐宗**出艙 `暫編地號`／`W_near`／`W_far`／`W_rw_start`／`Rw(%)`／`F(m)`／`推進側別`／`配地階段`，並列出 `Σ`。
  🔒 **母體須二式並列**：`_rw_real_wd2` 之母體（`rows` 濾 `推進側別` ∧ `F>0`）與 `_chain` 之母體（`{side}_results` join `暫編地號`），**逐宗對照並明載其差集**——`:1256` docstring 宣稱二者對齊，**須實測其是否果然對齊**。
- **`Q-3`**：`0m`·`R2 left` 同上逐宗表；具名**哪一宗（或哪幾宗）之 `Rw(%)` 使 `Σ` 逾 `100`**，及其 `Δ_self=-11.3570` 之界面與之是否同一宗。
- **`Q-4`**：`W₀`（`_adv_final['W0_left']`）之**產生路徑**：`3.5m` 得 `6.98`、`0m` 得 `0.00` 之成因（`forced` 角落之有無？）——**⛔ 主張其應為何**，只述其**現由何式得**。

🛑 **凡某題不可得** ⇒ **具名阻斷點**（`-257R` 之形：受詞 ＋ 該值僅存於何處 ＋ 何以本次注入取不到），**⛔「未查」「不明」之含混**。

---

## §六　【驗收】（**發單側已實跑之路徑始得立閘**·`自誤 319` 之教訓）

| 閘 | 判準 | 發單側是否已實跑該路徑 |
|---|---|---|
| `V-A` | 本倉 `app.py`／`verify/stepg_pipeline.py`／`verify/wf_f0.py` 倉側 blob **前後逐位不變**（`§零 2` 三值）；`git status --porcelain` ＝ `0` 列 | ✅ |
| `V-B` | `run_verification.py` 於本倉現態實跑 ⇒ `RESULT: FAIL`、**紅 `24`**；出艙其**計數式**（`grep -c '🔴 FAIL'`）| ✅ 四跑（`8a52dbd`／`a5b4edd`／`1531916`／閘關）皆 `24` |
| `V-C` | 三登記表之 `deletions` ＝ `0`（`git show --numstat`）| ✅ |
| `V-D` | 落地後 `自誤 MAX` `320→321`／`GB MAX` `153→154`／`K-9 MAX` `29`（不變）；`自誤` 缺號仍 `[106]`、恆等式 `True`。🔒 框 ＝ **定義框 ∪ 範圍框展開**（`8` 條攢批標題），母體 ＝ 登記表**單檔** | ✅ 已實作並實跑（現態得 相異 `313`／`MAX 320`／`MIN 7`／缺號 `[106]`／恆等式 `True`）|
| `V-N` | 號占用出艙依**宣告框 `①`–`⑦`**：`D1` 檔數／`D2` 列數／`D3` 列數**三數分列** ＋ **雙屬列數** ＋ `列框 ＝ \|D2∪D3\|` ＋ `檔框 ＝ \|D1∪D2檔∪D3檔\|`。母體 ＝ 全 `docs/`，**並載讀不到之檔數及其副檔名分布** | ✅ 已實作並實跑（現態 `604` 檔·讀不到 `19` 全為 `jpg`／`png`）|
| `V-P` | `工項四` 四題各有**答**或**具名之阻斷點**；`Q-2`／`Q-3` 之表為**逐宗完整**、⛔ 取樣 | 🟡 **⛔ 已跑**——故本閘⛔ 立「必可得」，只立「答或具名阻斷點」|
| `V-S` | **射程閘**：`工項三`／`工項四` 之報告內，祈使性之「改」字系／「議」字系／「正為」字系命中 ＝ `0` | ✅ 同 `-257` `V-P6` |

🔒 **`V-S` 之自檢須以代稱行之**——受檢字樣**⛔ 逐字寫入受檢報告**，含自檢句自身（`-257R` 所立之通則·本單沿用）。其逐字即本節 `V-S` 欄。

🩸 **發單側出艙前自檢二款（`自誤 314`／`320`／`319` 之族）**：
① 本單所**書**之框（`§六 V-D`／`V-N`）與發單側**自己產數**之框**同一** ⇒ ✅ 二器皆當場實作、逐款對照 `CLAUDE.md:1358`–`1360`／`:2161`–`:2181`／`:2246`–`:2251`／`:2277`–`:2290`／`:2368`；
② 本單所立之驗收路徑**是否實際執行過一次** ⇒ `V-A`／`V-B`／`V-C`／`V-D`／`V-N` **皆已跑**；`V-P` **未跑**，故其判準已降為「答或具名阻斷點」，⛔ 立「必可得」。

---

## §七　出艙

- 報告：`docs/reports/W-G.9-260R_telescoping定位.md`
- commit message 須載：單號 `-260`、**零生產碼**、及 `工項四` 之 `Q-1` 判（推定真／偽）。
- 🛑 **全批零生產碼** ⇒ CC 逕推，⛔ 候 KL。
- 🛑 **`Q-1` 之判無論真偽，⛔ 於本批動任何碼** ——修否係 KL 之裁，且須於新窗為之（其動配地幾何）。

SELF_SHA256: a0ca842495407870b61867b15080f4c81f14a4dcdeed9740c178c38b5e10d3c4
