# 🧾 `W-G.9-206` 施工單　側分支復驗機制 ＋ `K-9-26` ＋ `VR-089` ＋ 攢批七則

> **性質**：🟢 **零生產碼**（⛔ 動 `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 任一字）⇒ **CC 逕推，⛔ 請示 KL**。
> 🛑 **例外之澄清**：`§一` 之側分支推送係**既有物件之 ref 推送**，⛔ 產生任何新的生產碼變更，且**⛔ 動主分支一個 commit** ⇒ 仍屬零生產碼批。
> **開工態**：`wip/s1-endpart` ＝ `549bfe8958cac2d1db8f9f0cedef2b29c8738dc6`
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔒 **本單承 KL `2026-09-02` 之一裁一點頭**：① 段二鍵序 ②③ 之方向 ＝ **面積大的、街角指數大的排前面**（🔴 域裁）；② 生產碼 commit 改推**側分支**供發單側復驗，主分支原地不動（🔵 工作法·點頭）。

---

## §零　開工閘

| 閘 | 判準 |
|---|---|
| `S-0a` | `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**。🔒 若 `HEAD` 已前進而此 blob 仍相同 ⇒ 視為合法零生產碼插入，閘綠，僅須載實際 `HEAD` 全 `40` 碼與量測時刻 |
| `S-0b` | 本地存在 commit **`48bad3c2e480349944bee5bf29100a261dd5e98b`**，其 `tree` ＝ `a18cbb2d6d08191b6f0a01083e88afb4ff7bde68`、父 ＝ `549bfe8`。不符 ⇒ **停機上呈**，⛔ 重做 |
| `S-0c` | 本單 `SELF_SHA256` 自驗相符 |
| `S-0d` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py <本單>`；🟡 逐項以**字樣錨**具名（⛔ 用行號），逐筆歸類為「零參照」／「判別力參照」（裁 `H`） |

🔒 **行尾（`GB-139`·⛔ 省）**：`clone` 用 `-c core.autocrlf=false -c core.quotepath=false`；**`add` 既有檔一律走常規 `add`**（⛔ 以 `autocrlf=false` 覆寫）；**閘之受詞一律取倉側 `git cat-file blob`**。

### §零-e　`S-0d` 之已知 🟡（🔒 發單側已對**本檔**實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6`）

🔒 **實跑結果**：🔴 機械停機款 **`0`【項】**；🟡 **`3`【項】**；`P-5` 自簽 ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。

| 檢 | 觸發之字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-1` | `` - `S-0b` 之 `48bad3c` 不存在或 `tree` 不符 `` | **判別力參照 ⇒ 具名豁免**：母體 ＝ **CC 本機之 object store**，所查範圍 ＝ 單一物件 `48bad3c2e480349944bee5bf29100a261dd5e98b`，命令 ＝ `git cat-file -t`／`git rev-parse <sha>^{tree}`；⛔ 對倉之全稱否定 |
| `P-4` | `` 🛑 戒 `40`（承 `戒 39`·`常規五`） `` | **零參照 ⇒ 具名豁免**：`Δ`／`payload` 係**位元組數之二端**，⛔ 方向量；其座標系即位元組（`B`），已於同節 ① 逐檔並報二數 |
| `P-4` | `` **一（🔵 KL 點頭 `2026-09-02`）　生產碼 commit 之側分支復驗** `` | **零參照 ⇒ 具名豁免**：`→` 係**流程之時序箭號**（commit → 推 → 復驗 → 放行），⛔ 幾何方向；⛔ 涉座標系 |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造·取既有實跑）**：同器對 `-197` ＝ `5`／`-199` ＝ `0`／`-200` ＝ `2`／`-201` ＝ `1`／`-203` ＝ `0`／`-205` ＝ `1`／`-198R` ＝ `5`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。

---

## §一　🔴 工項 `1`：**先辦** —— 推側分支 `verify/W-G.9-198R-c1`

🛑 **本工項必須在 `§二`〜`§七` 之任何 commit 入倉<u>之前</u>完成。** 理由：`48bad3c` 之父為 `549bfe8`；本批一旦於主分支落地新 commit，該物件即⛔ 能快轉，必須改走 `§八` 之 rebase 款。

1. `git push origin 48bad3c2e480349944bee5bf29100a261dd5e98b:refs/heads/verify/W-G.9-198R-c1`
2. 🛑 **⛔ 動 `wip/s1-endpart` 一個 commit。** 推後立即 `git ls-remote origin refs/heads/wip/s1-endpart` 復驗其仍 ＝ `549bfe8`，逐字回報。
3. 回報側分支之 `ls-remote` 全 `40` 碼，須 ＝ `48bad3c2e480349944bee5bf29100a261dd5e98b`。

🔒 **此後之放行流程（本單所立·承 KL 點頭）**
```
CC 備妥生產碼 commit（⛔ 推主分支）
  → 推至側分支 verify/<單號>-c<n>
  → 發單側自側分支逐位元組復驗
  → 發單側請示 KL → KL 放行
  → CC 將主分支快轉（或依 §八 rebase 後重推側分支、發單側重驗）至該物件
```

---

## §二　工項 `2`：鑄 `K-9-26`（追加於 `docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔尾**）

🔒 取號現查（發單側·態 `549bfe8`）：框 `` ^### 🔒 K-9-([0-9]{1,3})　 ``、母體 ＝ `K-6` 單檔、扣哨兵 `K-9-99` ⇒ `{14…25}`·相異 `12`／MAX `25` ⇒ **次號 `26`**。

**payload（逐字追加）**

```markdown
### 🔒 K-9-26　**段二全域排序之鍵 ②③ 為<u>降冪</u>**（KL 裁 `2026-09-02`·`W-G.9-206`）

🔑 **受詞**：`K-6 §二 段二`（標題列 `### 段二：全域排序（僅排待處理街角之候選）`）之鍵序逐字
「① 街角指數**名次** → ② 街角試算 **G** → ③ 街角指數**數值** → ④ 暫編地號**字典序最小**（最終回退）」。
🔴 **既有缺口**：該逐字**只給鍵之順序，⛔ 給 ②③ 之方向**（僅 ④ 明載「最小」）。
發單側已窮舉檢索（母體 ＝ 全倉 `.md` ＋ `.py`·框 ＝ 列·取「段二／全域排序」與「`G`／指數」共現之列）
得四十餘處，**無一處載 ②③ 之方向** ⇒ 倉內查無正典依據 ∧ 有土地後果 ⇒ 上呈 KL。

**KL 逐字（`2026-09-02`）**

> 「面積大的、街角指數大的 排前面」

**一（規則）**　段二全域排序之四鍵，其方向逐鍵定為：
| 鍵 | 受詞 | 方向 |
|---|---|---|
| ① | 街角指數**名次** | **升冪**（名次定義所內生：`1` 為最前） |
| ② | 街角試算 **`G`** | 🔴 **降冪**（面積大的排前面） |
| ③ | 街角指數**數值** | 🔴 **降冪**（指數大的排前面） |
| ④ | 暫編地號 | **升冪**（字典序最小·正典逐字） |

**二（與既有規則之同向性）**　②③ 之降冪與下列既有規則同向，⛔ 另立新的比較觀點：
- `Phase 8 防護一`「大塊合法街角地⛔ 得被微小畸零地淘汰」（②）；
- 街角地 winner 取街角指數之**最大**者（`K-9-24 二·2`「歸指數數值**較大**之一側」）（③）。

**三（土地後果之時態）**　本裁作成之時，段三尚未落地（`VR-086`）⇒ 段二之序**⛔ 有消費端、⛔ 有土地後果**。
🛑 段三一落地，本序即決定**哪一筆候選先去湊足街角地之最小規定面積**，先湊足者取得該街角
⇒ **方向反轉會使同名次之街角改由較小之候選先取** ⇒ 本裁自段三落地起即有土地後果。

**四（泛化·⛔ 綁本案）**　本則⛔ 綁 `R2`、⛔ 綁任何街廓名或側標、⛔ 綁 `SB` 值。
```

---

## §三　工項 `3`：鑄 `VR-089`（追加於 `docs/驗證裁定登記表.md` **檔尾**）

🔒 取號現查：框 `` ^#+ `?VR-([0-9]{1,4})`? ``、母體 ＝ 該單檔、扣 `VR-999`／`VR-751` ⇒ 相異 `73`／MAX `88` ⇒ **次號 `089`**。

**payload（逐字追加）**

```markdown
## `VR-089`　`W-G.9-198R` `commit 1` 之二項發單側裁（claude.ai 裁 `2026-09-02`·`W-G.9-206`）

**肢一　`S-0a②` ＝ 綠：閘之立法理由勝其字面。**
`W-G.9-198R §一 S-0a②` 令「`git diff --name-only 7fab020..HEAD` 逐列⛔ 含任何 `verify/**.py`」，
而 `verify/probes/probe_WG9201_side_assign.py`（`-201` 所新增之唯讀探針）落於該區間 ⇒ **字面為紅**。
🔒 **裁**：同閘之立法理由逐字為「倉內合法多出任意數量之**零生產碼** commit 皆⛔ 得使本閘紅」，
而生產碼之機械界定（`CLAUDE.md 🔧 二`·出處 `docs/reports/W-G.9-183R…:19`）**逐字僅四檔**
（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`），
**⛔ 含 `verify/probes/**`** ⇒ 該探針係合法零生產碼插入 ⇒ **`S-0a②` 判綠**。
🛑 **一般式**：凡閘之字面與其同格所載之立法理由互斥者，**以立法理由為準**，並於同批鑄自誤更正該閘之字面。
（本則之字面缺陷已鑄 `自誤 259`。）

**肢二　`W-G.9-198R` `commit 1` 之射程 ＝ 准，含三檔。**
單之落地點 `L-1` 只名 `app.py`，而 `commit 1` 另動二檔 ⇒ **逐檔准駁如下**：
1. `verify/selection_pipeline.py`（`+11/-0`）⇒ **准**。`R1-3` 所令之新欄須出艙於「第 1 宗街角地逐候選評點明細」，
   而該表由此檔產生；且此檔**⛔ 在四檔生產碼集內**。⇒ 係 `R1-3` 之**必要條件**。
2. `verify/run_verification.py`（`+3/-0`·`_DECLARED_EXTRAS` 具名申報 `指數名次`）⇒ **准**。
   係 `U-K2` 守衛之**明文處置**，且倉內有 `真G(㎡)`（`2026-07-27`）之同理由先例；
   另一選項「補入 baseline」＝ 重烤，係 `W-G.9-198R §八` 明令⛔ 辦。
🔴 **准之附條件（⛔ 省）**：該申報使 `U-K2` 對欄名 `指數名次` **永久失明**。
⇒ **九份 baseline 重產時，`_DECLARED_EXTRAS` 內之 `指數名次` 須一併移除**；本條已入 `CLAUDE.md` 末端。
（射程列舉不完整之缺陷已鑄 `自誤 260`。）
```

---

## §四　工項 `4`：鑄 `自誤 256`〜`260` ＋ `戒 40`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

🔒 取號現查：自誤 框 `` ^#+ 自誤 `?([0-9]{1,4})`? ``、母體 ＝ 該單檔 ⇒ 相異 `220`／MAX `255` ⇒ **次號 `256`**。
🔒 戒 框 `` ^(?:🛑 |> )?\*{0,2}戒 ?\*{0,2}`?([0-9]{1,3})`? ``、母體 ＝ **全倉 `.md`** ⇒ 相異 `22`／MAX `39` ⇒ **次號 `40`**。
🔒 **本檔即 `戒` 之現行唯一鑄號地**（全倉現查：`戒` 之定義列式於 `docs/orders/W-G.9-190R…md`／`W-G.9-191…md` 各命中 `1`，其值皆 `1` ＝ 引用而非鑄號；MAX `39` 之筆 `22` 則全在本檔）。

**payload（逐字追加）**

```markdown
## 自誤 `256`　對環境下全稱命令，而⛔ 先量測該環境

`W-G.9-205 §零-3` 逐字令「全程 `git -c core.autocrlf=false -c core.quotepath=false`」。
發單側出單前**⛔ 量測過 KL 本機之行尾狀態**。CC 以 `hash-object` 唯讀實測後證其與 append-only **互斥**：
追加三檔之工作區為 `w/mixed`，走 `false` 會把 `4258`／`4528`／`1041` 個 `CR` 寫進 blob、全檔偽異動。
🔒 **事後實查（KL 提供）**：`core.autocrlf` ＝ `true`，設於 **system 層**（`C:/Program Files/Git/etc/gitconfig`）；
`git ls-files --eol` 得 `CLAUDE.md` ＝ `i/lf w/mixed`、`docs/驗證裁定登記表.md` ＝ `i/lf w/mixed`、
新入倉之 `docs/orders/W-G.9-204_發單側交接文.md` ＝ `i/lf w/lf`（從未經 checkout ⇒ 保持原貌）。
**一般式**：**凡對執行環境下「全程／一律／全部」之命令，須先以唯讀量測取得該環境之現況**；
⛔ 得以「倉內是純 `LF`」推論「工作樹是純 `LF`」——二者隔著一層 checkout 轉換。
**攔截者** ＝ CC（唯讀實測後依 `常規二` 取保守項並逐字回報·**正辦**）。**土地後果** ＝ 無。
已鑄 `GB-139` 立案。

## 自誤 `257`　同單二條款之**關係未具名**，互斥由受單方當場裁

`W-G.9-205 §零-2` 令變更檔清單「恰 `6` 檔·⛔ 得多」，`§六` 又令執行報告「**同批**入倉」。
二者⛔ 並存於同一 commit，而單**⛔ 定義「同批」與「同一 commit」之關係**。
CC 讀「同批 ≠ 同一 commit」（並舉 `-200`／`-203` 需同一 commit 時皆逐字寫後者為證）而拆為同批二 commit ⇒ **正辦**。
**一般式**：**凡單內同時出現「檔數／commit 數之上限」與「另一物須同批入倉」者，須於同格逐字定義二者之關係**；
未定義者，受單方之保守裁**即為正解**，發單側⛔ 得事後主張其原意。
**攔截者** ＝ CC。**土地後果** ＝ 無。

## 自誤 `258`　閘之字樣錨與**同單 payload 之落地字樣**不符；且閘⛔ 自扣其自身之增量

`W-G.9-205 S-7` 令以字樣錨 `W-G.9-205 更正註` 查 `CLAUDE.md`，而同單 `§四` payload 之落地字樣為
`` `W-G.9-205` 更正註 ``（帶反引號）⇒ 該錨實得 `0` ⇒ **量測器紅、⛔ 受詞紅**。
同閘又令「`四框皆` 須 `1`」，而同單 payload **自身即引用該句** ⇒ 實得 `2`。
🩸 **二層皆為發單側之疏**：① 錨未取自同單 payload 之落地字樣；② 閘未自扣其自身之增量。
**族**：② 係 `VR-083`（量測器自扣）之**累犯**。
**一般式**：**凡閘之受詞為「本批將寫入之字樣」者，其錨須逐字複製自同單之 payload**（⛔ 另行手寫）；
**且該閘之期望值須先減去本批 payload 自身之貢獻**。
**攔截者** ＝ CC（依 `常規五` 先自證量測器、再以二獨立法證受詞綠 ⇒ **正辦**）。**土地後果** ＝ 無。

## 自誤 `259`　🩸 **同窗內讀過之戒律仍重犯**：`S-0a②` 未預留唯讀探針之豁免

`W-G.9-204 §七-4` 逐字載：「`-203` 之 `S-0a` ② 若沿用『`verify/**.py` 逐列不得出現』，會被 `-201` 合法新增之
唯讀探針弄紅 ⇒ 該單已預先寫入豁免款……**`-198` 之 `S-0a` 須同樣預留**。」
🔴 該交接文係本窗**開窗第一件事所通讀者**；發單側起草 `W-G.9-198R` 時仍原封沿用舊本之 `S-0a②`
（「逐列⛔ 含任何 `verify/**.py`」）而**⛔ 補豁免** ⇒ CC 開工即撞紅。
**一般式**：**凡交接文載有「下一批之某閘須預留某款」者，該款須於起草時逐條轉為<u>檢查項</u>並於出單前逐項打勾**；
⛔ 得以「我讀過那份交接文」代替「我逐項核對過那份交接文之待辦款」。**讀過與照辦是兩件事。**
**攔截者** ＝ CC。**土地後果** ＝ 無（該閘之 operative 判準見 `VR-089` 肢一）。

## 自誤 `260`　落地點之列舉不完整，射程由受單方當場擴張

`W-G.9-198R §七` 之 `L-1`〜`L-3` 只名 `app.py`，而 `commit 1` 之達成**必然**須動
`verify/selection_pipeline.py`（新欄之出艙產生檔）與 `verify/run_verification.py`（`U-K2` 守衛之申報檔）。
🔒 發單側出單前**⛔ 追過「第 1 宗街角地逐候選評點明細」之產生鏈**，遂只名 `app.py`。
**一般式**：**凡結果態要求「某出艙須增／改欄」者，落地點之列舉須涵蓋<u>該出艙之產生鏈全程</u>**——
自計算處、經序列化處、至比對／守衛處；⛔ 得只名計算處。
**族** ＝ 與 `自誤 257` 同族（**射程／關係之列舉不完整，逼受單方當場裁**），本則為該族之**第二度**。
**攔截者** ＝ CC。**土地後果** ＝ 無（准駁見 `VR-089` 肢二）。

🛑 戒 `40`（承 `戒 39`·`常規五`）：**凡出艙「`Δ` 恰 ＝ payload」「逐位元組相同」「一字不差」之等同斷言，須於同格並報<u>二端</u>之位元組數。**
  ① 案由 ＝ `W-G.9-203R` 逐字「`Δ` 恰 ＝ payload·⛔ 補任何位元組」；發單側逐檔實測得
     **`Δ` ＝ payload <u>去尾一個 `\n`</u>**，五檔皆然（`2642/2641`、`2548/2547`、`1813/1812`、`2084/2083`、`1774/1773` B）。
     「⛔ **補**」為真（係**減**）；「**恰 ＝**」為偽。內容逐字相符、五檔仍以 `\n` 收尾 ⇒ **⛔ 實質危害**，故⛔ 鑄自誤而立戒。
  ② 適用於**發單側與受單方雙方**；⛔ 得以「內容相同」代「位元組數相同」。
  ③ 🔒 **首次適用之先例** ＝ `W-G.9-205R`（三 payload 之 `Δ` 與 payload 二端位元組數同格並報·`1060→Δ1062`／`1407→Δ1409`／`784→Δ785`）。
```

---

## §五　工項 `5`：鑄 `GB-139`（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔尾**）

🔒 取號現查：框 `` ^#{2,4} `?GB-([0-9]{1,4})`? ``、母體 ＝ 該單檔、扣哨兵 `GB-997` ⇒ 相異 `52`／MAX `138` ⇒ **次號 `139`**。
🛑 依 `GB-138`：`GB` 之定義列式**⛔ 經正典釘定** ⇒ 上開之數**須同格載其框**，且⛔ 得出艙「缺號集」。

**payload（逐字追加）**

```markdown
### `GB-139` 🆕　KL 本機 `core.autocrlf=true`（system 層）⇒ 工作樹行尾 ≠ 倉內行尾

**實測（`2026-09-02`·KL 本機 `C:\Users\admin\Desktop\land-readjustment-trial`）**
- `git config --show-origin --get core.autocrlf` ⇒ `file:C:/Program Files/Git/etc/gitconfig  true`（**system 層·非 KL 所設**）
- `git ls-files --eol` ⇒ `CLAUDE.md` `i/lf w/mixed`／`docs/驗證裁定登記表.md` `i/lf w/mixed`／
  `docs/orders/W-G.9-204_發單側交接文.md` `i/lf w/lf`（從未經 checkout）

**肢一（施工單之措辭）**　任何令 `-c core.autocrlf=false` 之 `add`，對**既有檔**會把工作樹之 `CR` 寫進 blob
⇒ 全檔偽異動、`deletions` 爆表、破 append-only。
⇒ 🔴 **施工單⛔ 得對既有檔下 `autocrlf` 之覆寫，一律走常規 `add`。**

**肢二（閘之受詞）**　任何以「工作樹位元組 ≡ blob」為判準之閘，對**既有檔恆偽紅**。
⇒ 🔴 **閘之受詞一律取 `git cat-file blob`（倉側），⛔ 取工作樹側。**
🔒 `W-G.9-205` 之 `S-1` 恰因其三受詞皆為 `w/lf` 之新入倉檔而未踩到；**下一張單若對既有檔下同閘即偽紅**。

**例外（⛔ 受本項影響）**　`clone` 時之 `-c core.autocrlf=false -c core.quotepath=false` **仍為必要**
——驗證側新 clone 須取得逐位元組原貌。

**處置之時點**　🔒 **KL 裁 `2026-09-02`：⛔ 於本波變更該設定**（現行 `true` 已使倉內 blob 恆為純 `LF`，
且⛔ 宜在動分配面積之批前改環境）；**留至波末與重烤同批處理**。
⇒ 本項於波末前**⛔ 解除**，其功用 ＝ 拘束施工單之措辭與閘之受詞。
```

---

## §六　工項 `6`：`CLAUDE.md` **末端追加**一節（🛑 append-only·⛔ 就地改任何既有一字）

```markdown

### 🔒 `W-G.9-206` 補款　側分支復驗 ／ 段二方向已裁 ／ `_DECLARED_EXTRAS` 之移除時點

**一（🔵 KL 點頭 `2026-09-02`）　生產碼 commit 之側分支復驗**
KL 逐字：「CC 把生產碼 commit 推到一個側分支（例如 `verify/W-G.9-198R-c1`），主分支 `wip/s1-endpart` 原地不動：OK」
⇒ 自本款起，生產碼 commit 之流程為：
```
備妥（⛔ 推主分支）→ 推至側分支 `verify/<單號>-c<n>` → 發單側自側分支逐位元組復驗
→ 發單側請示 KL → KL 放行 → CC 將主分支推進至該物件
```
🛑 **「⛔ 推」之保護一字不減**：側分支⛔ 是主線，未放行前主分支⛔ 前進一個 commit。
🛑 主分支若於其間前進，該物件即⛔ 能快轉 ⇒ 須依 `W-G.9-206 §八` rebase 後**重推側分支並重驗**。

**二（🔴 KL 域裁 `2026-09-02`）　段二鍵序 ②③ ＝ 降冪**
已入正典 `K-9-26`（`docs/rulings/K-6_…md` 檔尾）。`CLAUDE.md` 前文各處所載之段二鍵序
（逐字「① 名次 → ② 街角試算 `G` → ③ 指數數值 → ④ 暫編地號字典序最小」）**⛔ 曾載方向**；
自本款起，其方向逐鍵為 **① 升冪／② 降冪／③ 降冪／④ 升冪**。

**三　`_DECLARED_EXTRAS` 之 `指數名次` 須於 baseline 重產時移除**
`W-G.9-198R commit 1` 於 `verify/run_verification.py` 之 `_DECLARED_EXTRAS` 具名申報欄 `指數名次`
（`U-K2` 守衛之明文處置·准駁見 `VR-089` 肢二）。該申報使該守衛對此欄名**永久失明**。
🛑 **九份 baseline 重產（波末）時，須將 `指數名次` 自 `_DECLARED_EXTRAS` 移除**，
使該欄回歸守衛之射程；⛔ 得任其留存。
```

---

## §七　工項 `7`：`W-G.9-198R` **補款一**（🛑 ⛔ 就地改 `-198R` 一字·append-only）

🔒 `-198R` 已入倉（`dd39f34`）⇒ **⛔ 得就地修改**。本補款以**獨立節**置於本單，並由 `§六` 之 `CLAUDE.md` 追加指引之。

| # | 補款內容 |
|---|---|
| `補一-1` | `-198R §一 S-0a②` 之字面**經 `VR-089` 肢一更正**：其判準應讀為「`git diff --name-only 7fab020..HEAD` 逐列⛔ 含 **`CLAUDE.md 🔧 二` 所列之四檔**」；`verify/probes/**` 之新增**⛔ 使本閘紅**。 |
| `補一-2` | `-198R §七` 之落地點 `L-1`〜`L-3` **經 `VR-089` 肢二擴張**為：`app.py` ＋ `verify/selection_pipeline.py` ＋ `verify/run_verification.py`（逐檔之准駁理由見該裁）。 |
| `補一-3` | 🔴 **`commit 2` 之新增結果態 `R2-9`**：`k6b_stage2_global_order` 之 docstring 內，現載為「CC 之技術判讀」之 ②③ 方向，須改述為**引 `K-9-26`**（KL 裁 `2026-09-02`）。🛑 **`commit 1` ⛔ 得因此重做或 amend**——該 docstring 之措辭更正併入 `commit 2`（其本即動 `app.py`）。 |
| `補一-4` | `-198R §五`（`commit 3`）之 `D3-*` 不變；`§八`「⛔ 本單不辦」不變。 |

---

## §八　🔴 `48bad3c` 之 rebase 款（**本批落地後必然觸發**）

本批於主分支落地後，`48bad3c`（父 ＝ `549bfe8`）即⛔ 能快轉。⇒ 放行時之程序改為：

1. CC 將 `48bad3c` **rebase** 至當時之主分支頭，得 `48bad3c′`。
2. 🔴 **rebase 之等值判準（三項全須成立·⛔ 得省）**
   - `48bad3c′` 之 `diff --name-only`（對其父）**恰三檔** ＝ `app.py`／`verify/selection_pipeline.py`／`verify/run_verification.py`；
   - 三檔之 `numstat` 逐檔 ＝ `+110/-1`／`+11/-0`／`+3/-0`；
   - 三檔之新 blob 逐檔 ＝ `6f0317e47ef92c7e06d6d904ecf2d129656bd331`／`10f90c50…`／`d07955c5…`（🛑 CC 須自報**全 `40` 碼**·⛔ 用短碼）。
   任一不符 ⇒ **停機上呈**，⛔ 自行歸因。
3. **強制更新側分支** `verify/W-G.9-198R-c1` 至 `48bad3c′`，回報其全 `40` 碼。
4. 發單側**重新復驗**後方請示 KL 放行。
🛑 **本批之 `§一` 仍須先推 `48bad3c` 原物件**——供發單側取得 rebase 前之原貌，作為 rebase 等值之對照端。

---

## §九　驗收閘

| 閘 | 判準 | 命令 |
|---|---|---|
| `S-1` | 側分支 `verify/W-G.9-198R-c1` ＝ `48bad3c2e480349944bee5bf29100a261dd5e98b`；且**推側分支之後、本批 commit 之前**，`wip/s1-endpart` 仍 ＝ `549bfe8` | `git ls-remote origin` 二 ref 並報 |
| `S-2` | 追加五檔之 **append-only 位元組級自證**：逐檔 `git cat-file blob <本批>:<檔>` 之**前 N 位元組 ≡ `549bfe8` 之同檔全文**（`N` ＝ 基座位元組數），逐檔報 `N`／新長／`Δ` | `python` 前綴比對 |
| `S-3` | 🆕 **`戒 40` 之適用**：五檔之 `Δ` 與其 payload **二端位元組數同格並報**；⛔ 得寫「`Δ` 恰 ＝ payload」 | 同上 |
| `S-4` | 追加五檔之 staged blob `CR` 皆 `0`（`GB-139` 肢一之保守項生效） | `git cat-file blob` |
| `S-5` | 追蹤檔 `1577` → **`1579`**；`.md` `510` → **`512`**（＝ 新增本單 ＋ 執行報告） | `ls-tree -r --name-only HEAD` |
| `S-6` | `K-9`（`K-6` 單檔·扣 `99`）：相異 `12`→**`13`**、MAX `25`→**`26`** | 框 `` ^### 🔒 K-9-([0-9]{1,3})　 `` |
| `S-7` | `VR`（扣 `999`／`751`）：相異 `73`→**`74`**、MAX `88`→**`89`** | 框 `` ^#+ `?VR-N`? `` |
| `S-8` | 自誤：相異 `220`→**`225`**、MAX `255`→**`260`**；戒（**全倉 `.md`**）：相異 `22`→**`23`**、MAX `39`→**`40`** | 二框各報 |
| `S-9` | `GB`（扣 `997`·框 `` ^#{2,4} `?GB-N`? ``）：相異 `52`→**`53`**、MAX `138`→**`139`** | 同左 |
| `S-10` | 🔴 **生產碼四檔⛔ 動**：`app.py` blob 仍 ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`；`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 三檔之 blob 與 `549bfe8` 逐檔相同 | `git rev-parse HEAD:<檔>` 四次 |
| `S-11` | 🆕 **判別力自證（`戒 40` 之器）**：對本批之五 payload，**至少一檔之 `Δ` ≠ payload 位元組數**（若五檔全等，須逐檔證其確為原封追加而非巧合），逐檔具名其差之成因（fence／前後空列） | 同 `S-3` |
| `S-12` | `probe_order_preflight.py` 對本單：🔴 `0`；🟡 逐項以字樣錨具名並依裁 `H` 歸類 | 同左 |

🛑 **`S-2` 之受詞一律取倉側 blob**（`GB-139` 肢二）——⛔ 以工作樹位元組為受詞。

---

## §十　報告要求（`docs/reports/W-G.9-206R_…md`·**同批、⛔ 同一 commit**）

🔒 **本單已依 `自誤 257` 之一般式逐字定義二者關係：報告與登記 payload <u>同批入倉、置於不同 commit</u>；**
且因本批⛔ 含生產碼，二 commit **皆可逕推**，⛔ 有 `W-G.9-205 §二-b` 之互斥。

1. 實際 `HEAD` 全 `40` 碼（**push 後之 `git ls-remote`**）＋ 量測時刻；側分支之全 `40` 碼。
2. `S-1`〜`S-12` 逐閘：判準、實測值、產生它的命令逐字。
3. `§四` 之取號現查：自誤／戒二框之**自行復算**（⛔ 照抄本單）。
4. 🔴 **`戒 40` 之適用**：五 payload 之 `Δ` 與 payload 二端位元組數同格並報。

---

## §十一　停機條件

- `S-0b` 之 `48bad3c` 不存在或 `tree` 不符 ⇒ **停機上呈**，⛔ 重做該 commit。
- `§一-2` 復驗發現主分支已被本批以外之物推進 ⇒ **停機上呈**。
- 任一取號之現查與本單所載不符 ⇒ **停機上呈**（⛔ 自行改號）。
- 其餘閘紅 ⇒ 依通則：**純技術自行做完**，於報告載明。

✅ 全綠 ⇒ **逕行 push**（零生產碼），⛔ 請示 KL。
🛑 **`W-G.9-198R` 之 `commit 2`／`3` 仍⛔ 開工**——候 `commit 1` 經側分支復驗並由 KL 放行。
SELF_SHA256: 65976cdf78dc6c0be350251c8a33cba87a0a9b9aadf668c79870fd47d1ae1705
