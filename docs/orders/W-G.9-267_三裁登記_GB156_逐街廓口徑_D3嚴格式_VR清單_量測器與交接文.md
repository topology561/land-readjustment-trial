# W-G.9-267　三裁之登記（`GB-67` 開·`GB-101` 可·甲）＋ `d-1` 盲區 `GB-156` ＋ 逐街廓口徑之裁 ＋ `D3` 嚴格式 ＋ `VR` 清單授權 ＋ 發單側量測器與交接文入倉（**中級批**）

> 🛑 **零生產碼**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 一字不動·含註解）。
> 🛑 **零土地後果**：⛔ 動任何一宗地之 `G`／面積／街角歸屬／抵費地／配地幾何。
> 🛑 **`GB-67` 之修 ⛔ 於本批開工**——本批只**登記** KL 之「開」，其落地 ＝ `W-G.9-268`（新窗·側分支·逐 commit 放行）。
> 🔒 本批之級 ＝ **中級**（純登記 ＋ 純診斷）⇒ `run_verification.py` **全批只跑一次**，落檔全批共用。

---

## §零　工項零：四檔原封入倉（審查窗辦·辦畢即推即停）

| # | 檔 | 落點 |
|---|---|---|
| **a** | 本單 | `docs/orders/W-G.9-267_三裁登記_GB156_逐街廓口徑_D3嚴格式_VR清單_量測器與交接文.md` |
| **b** | 審查窗之 `-267` 報告與草稿全文 | `docs/orders/W-G.9-267_附件甲_審查窗報告與草稿.md`（⛔ 改一字）|
| **c** | 發單側之量測器 | `verify/probes/probe_WG9267_issuer_measurers.py`（**唯讀探針**·自誤 `331` 之落地）|
| **d** | 交接文 | `docs/orders/交接文_W-G.9-267.md`（受詞 ＝ 下一個 `claude.ai` 窗·CC ⛔ 執行其所列任何工項）|

出艙：四檔之來源／落檔／入倉 blob `sha256` 三者逐位相符，並載 **blob 位元組數**／`LF`／`CR`（明載 blob 層）。
🔒 附件甲與本單相異處，**本單勝**。
🔒 **c** 之地位：**自擬框·⛔ 正典**·唯讀·供歸因用；⛔ 以其輸出為任何閘之通過條件。入倉後 CC 須實跑 `registry` 與 `occupancy 265 267` 二模式，其輸出須與附件甲 `§1` 甲部／`§2`／`§丁` 之數逐位相符（此即歸因之活體實例）。

---

## §一　開工閘（中級·逐條當場復現·任一不符 ⇒ 停機上呈）

| # | 閘 | 應得 |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` ＝ 本地 `HEAD` | 逐位相同 |
| `2` | 生產碼 **34 檔**（正面列舉 ＝ `app.py` ＋ `verify/` 頂層 `*.py`·見 §五 `U-4` 之裁）blob 期初出艙；`app.py` ＝ `1e15d45ae84b02ca` | 異於開工值 ⇒ 停 |
| `3` | 四簿當場復算·**母體逐簿為單檔**（⛔ 全 `docs/`）·框逐字 ＋ 三軸 ＋ `VR-092` 加註所令之低端狀態 | 見 `3-a` |
| `4` | 號占用六數分列（**`D3` 用嚴格式**·見 §五 `U-1` 之裁）；受詢 `W-G.9-267` 全 `0`；對照甲 `-266` `≥ 1`；對照乙人造號 `= 0` | 見 `4-a` |
| `5` | 本單原封入倉，入倉 blob `sha256` ＝ 落檔前 | 不符 ⇒ 停 |
| `6` | `probe_order_preflight.py <本單>`：`P-3`／`P-5` 機械款；`P-1`／`P-2`／`P-4`／`P-6` 逐項採納或具名豁免 | 紅 ⇒ 停；惟見 `6-a` |
| `7` | `run_verification.py` 全批一次：`rc`／`stderr` B／`RESULT` 逐字／紅綠項數 ＋ **計數式逐字**（自源碼 `print` 字面讀取）＋ 第二態 | 見 `7-a` |

**`3-a`**（發單側於態 `3b44a34d308ff046c0099dc098cd56bb5bd0a89d`·**單檔母體**·⛔ 採信）：自誤 `323/330/7/[106]`／`GB 153/155/1/[12,87]`／`VR 79/94/14/[73,75]`／`K-9 28/29/2/[]`。
**`4-a`**（嚴格式·全 `docs/` `624` 檔·讀不到 `19`）：`-267` ＝ `0/0/0/0/0/0`；`-266` ＝ `D1 3／D2 7／D3 5／雙屬 0／列框 12／檔框 5`。
🩸 **自指款**：本單／附件甲／量測器／交接文入倉後致 `267 ≠ 0` ⇒ ⛔ 改號，改判「占用者是否全為本單自身之產物」。
**`6-a`**：`P-5` 不符時先六框並列復算（①`SELF_SHA256` 列以前全部位元組／②去末一 `\n`／③去尾隨空白列／④全檔／⑤⑥ 為①②之 `CRLF` 正規化）；任一命中 ⇒ 框之別·記入自解清單續辦；六框皆不命中 ⇒ 傳輸損毀·停機。
**`7-a`**：三態實測 `rc=1`／`stderr 0 B`／`RESULT: FAIL`／`24` 紅／`10` 綠／`34` 名目；計數式 `^  🔴 FAIL  (.+)$`／`^  ✅ PASS  (.+)$`（附件甲 `§4` 逐字·自源碼字面讀取）。⛔ 以 log 列號為錨。

---

## §二　工項一：KL 三裁之登記（**純登記**·⛔ 開工）

🔒 **KL 裁 `2026-09-10`（逐字）**：「`GB-67` 開」「`GB-101` 可」「甲」。

### 一-1　`GB-67`：丙案（只定位）→ **甲案（開修）**

落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末追加 `GB-67` 補款（⛔ 動原文與現況欄一字·⛔ 鑄號）。須載：
- KL 裁「開」·`2026-09-10`。
- **方法⛔ 新裁**：`K-9-23` 二閘由觀測轉主動 ＋ `K-9-17` 遞補迴圈 ＋ 第 1 宗境界線不動（皆既有裁定·逐字引其內容錨）。
- **落地之形（發單側裁）**：`W-G.9-268`·**新 `claude.ai` 窗 ＋ 新 CC 窗**·側分支 `verify/W-G.9-268-c<n>`·**逐 commit KL 放行**·併線前呈差異表（現值 `2` 宗／`4.7950㎡`）·驗於**六街廓**（`R3` 依 §五 `U-2` 之裁取得）。
- **下游解扣**：`W-G.4:4723` 之「五級⛔ 俟 `GB-67`」自 `-268` 併線起解除（⛔ 於本批解除）。
- 🛑 本批 **⛔ 動 `app.py` 一字、⛔ 開側分支、⛔ 跑任何修法探針**。

### 一-2　`GB-101`：`±0.05 ㎡` **可接受**

落點 ＝ 同檔檔末追加 `GB-101` 補款（⛔ 動原文一字）。須載：
- KL 裁「可」·`2026-09-10`：法定分配面積之跨環境迭代停點差（至 `0.04 ㎡`）**可接受**。
- **後果**：凡以 `G`／幾何面積作**跨環境**對拍者，容差**固定** `±0.05 ㎡`（`GB-101` 原文已載之式）；**⛔ 改 `solve_G_binary` 之起算區間、⛔ 改任何碼**。
- `GB-101` 原文之「需 KL 域裁？」欄所指之題**已裁**；⛔ 追改該欄字面，以本補款為準。
- 🔒 **同主機同碼同輸入為確定性**（`-266R` 加註已證·落檔 `sha256` 逐位相同）⇒ 同主機對拍容差仍 `1e-6`（⛔ 因本裁放寬）。

### 一-3　路徑「甲」＋ baseline 重產時點（發單側建議之登記·⛔ KL 裁）

落點 ＝ `CLAUDE.md` 檔末追加（⛔ 鑄號）。須載：KL 採甲（`-267` 收尾·`-268` 幾何批）；九份 baseline 重產 ＝ **發單側建議俟 `-268` 併線且六街廓全跑通後一次重產**，其時點**仍候 KL 決**（`CLAUDE.md` 既有款）。

---

## §三　工項二：`d-1` 閘之結構盲區 ⇒ 鑄 **`GB-156`**（甲之落地·純診斷）

**受詞**：`d-1`（`K-9-9 一` ∥ALLOCLINE 之驗）於 **`SIDE ∥ ALLOC`** 之街廓側，「∥ALLOC」與「∥SIDE」為**同一述詞** ⇒ 鑑別力為零。全 `8` 個街廓側中恰 `3.5m·R6·right` 一組（`|∠SIDE−∠ALLOC| = 2.7e-06°`·`cross 4.742e-08`）。
**判（附件甲 §甲）**：`628-18(1)` ＝ **① 幾何巧合·⛔ 缺陷·⛔ 土地後果**——決定性對照 `R1·right`（`0.6908°`）之遠側界**取 SIDE** ⇒ `K-9-5-4 ②` 換軸在生效。
**落點**：`GB` 簿檔末，鑄 `GB-156`（次號**當場現查**·⛔ 憑本單）。內容 ＝ 盲區之定義 ＋ 全 `8` 側之 `|∠SIDE−∠ALLOC|` 表（`自誤 330` 之全母體）＋ `d-1` 探針須逐側出艙 `SIDE∥ALLOC` 判、該類標「**不可判**」、⛔ 併入違反計數。
**否證對照**（同格載判定集基數）：必紅 ＝ `R6/right` 須輸出「不可判」（`n=1`）；必綠 ＝ `R1/right` 須輸出「可判·取 SIDE」（`n=1`）。
**併記**：角退表之 `ΔW = +0.00` 係 `W(m)` 欄 `2dp` 捨入之讀數（真值僅知 `|ΔW| < 0.05`），**⛔ 獨立佐證**——本單發單側前次「二個獨立量測同落臨界值」之措辭**撤回**（附件甲已具名）。
🛑 ⛔ 修配地幾何、⛔ 動 `app.py`。

---

## §四　工項三：逐街廓獨立口徑之登記（乙之落地·純診斷·**⛔ 建為常設**）

**受詞**：口徑甲（全區連跑·`run_verification`）vs 口徑乙（逐街廓獨立呼叫 `run_step_g`），`SB = 3.5m`，五街廓 `47` 宗 `329` 格。
**判（附件甲 §乙）**：靜態（`8` 候選皆 write-only 累加器）＋ 跨行程經驗（`329` 格相異 `0`·四個非首街廓）⇒ **① 無狀態相依**。
**落點**：`docs/reports/` 出執行報告 ＋ `docs/驗證裁定登記表.md` 檔末追加（⛔ 鑄號·其為 §五 `U-2` 之裁之機械依據）。
**否證對照**：必紅 ＝ 任一宗 `G` 人造 `+1` 須偵得（`n=1`）；必綠 ＝ CSV 自比（`n=47`）。
**射程界（照實）**：`R3` 之 `11` 宗（含 `628-30(3)+` 街角地·`G 627.57`）**⛔ 有第二來源** ⇒ 其值標「**單源·歸納支持**」。
🛑 停機款：任一非首街廓相異格 `≥ 1` ⇒ 停機上呈（蘊含跨街廓相依·可能有土地後果）。

---

## §五　工項四：五則未決之裁（發單側·皆零土地後果）

| # | 題 | **裁** | 落點 |
|---|---|---|---|
| `U-1` | `D3`「列首即該號」是否含前導 markdown 標記 | **嚴格式**（列首**即**該號·⛔ 前導 `` ` ``／`*`／`>`／空白）。由：寬式下每張單之 `S-1` 表使其自占其號 ⇒「出艙未占用」之行為自身使其已占用 ⇒ 閘**恆假**（`自誤 251` 同形·`GB-132` 同族）；依補款 `②` 引用⛔ 構成占用。**⛔ 追改 `-265` 單載之 `6/16`**，只於本裁具名其為寬式 | `CLAUDE.md` 檔末·宣告框補款（⛔ 鑄號）|
| `U-2` | `W-G.9-265 §三 三-5` 之由是否仍成立 | **由不成立、裁改**：逐街廓獨立呼叫為**合法之診斷取值途徑**（⛔ 生產途徑·⛔ 常設）；`R3` 之射程得以此取得，其值恆標「單源·歸納支持」；`三-5` 之「⛔ 另闢臨時途徑」款**⛔ 廢字面**，以本裁為後法 | `docs/驗證裁定登記表.md` 檔末·鑄 **`VR-095`** |
| `U-3` | `VR-1`–`VR-13` 靜態清單 | **授權立**。落 `CLAUDE.md` 檔末（⛔ 落 `驗證裁定登記表`——`GB-132` 結構風險）；節題依附件甲 `§丙` 已驗之候選（六框全 `0`）；內容列全表列式；立後 `VR` 相異／`MAX`／缺號集須**與立前逐位相同**（`79`／`94`／`[73,75]`）| `CLAUDE.md` 檔末（⛔ 鑄號）|
| `U-4` | 「十一檔」之成員 | **② 改稱「生產碼 34 檔（正面列舉 ＝ `app.py` ＋ `verify/` 頂層 `*.py`）」**；「十一檔」一語**⛔ 再用**；⛔ 追改既有文本 | `CLAUDE.md` 檔末（⛔ 鑄號）|
| `U-5` | `CLAUDE.md` 序 `12`／序 `14` 與活體不同步 | **① 末端追加加註**（`-266R` 已登記事實·本批只加註「`3.5m` telescoping 命中 `0`·中止改 `②-宗@R5`」）；⛔ 動序 `12`／`14` 一字、⛔ 判解除 | `CLAUDE.md` 檔末（⛔ 鑄號）|

🔒 **鑄號之授權**：本批鑄 `GB-156`、`VR-095`、`自誤 331`／`332`；⛔ 逾此。

---

## §六　工項五：鑄 `自誤 331`／`332`（責 ＝ 發單側·純登記）

落點 ＝ 自誤簿檔末。標題形取既有最末一則為範本。

> ### 🩸 `自誤 331`　**自擬量測器之逐字⛔ 在倉，致受單方於相異時結構上不可歸因**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-267`·`2026-09-10`。**由 `-266R` 具名、`-267` 審查窗擬受詞**（記功）。
> **受詞**：發單側於 `-263`〜`-266` 各輪出艙之三簿計數、宣告框六數、覆蓋帳，皆由其對話內之自擬器產生，**器之可跑逐字從未入倉**。活體實例：`-265` 單載 `D3 6`／列框 `16` 與 `-266R` 實測 `5`／`15` 相異，CC 只能記「成因⛔ 可得（發單側量測器⛔ 在倉）」；發單側一道指令即定其為前導反引號之框差——**該歸因能力⛔ 在受單方手上**。
> 🔒 **通則（自本則起）**：凡出艙自擬器所產生之數，須同格出艙該器之**可跑逐字**或其入倉路徑；⛔ 附者，該數⛔ 得為任何閘之通過條件，受單方⛔ 負歸因義務。**落地**：`verify/probes/probe_WG9267_issuer_measurers.py`（本單工項零 `c`）。

> ### 🩸 `自誤 332`　**三軸 `(2)` 母體誤標：四簿之數係單檔母體之值，卻宣告為全 `docs/`**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-267`·`2026-09-10`。**由 `-267` 審查窗捕獲**（記功）。
> **受詞**：`-267` 開場文 `§二` 逐字「母體 全 `docs/` `624` 檔·讀不到 `19`」下列四簿之相異／`MAX`／`MIN`／缺號，實測**逐簿皆等於單檔母體之值**（自誤 `323`／`GB 153`／`VR 79`／`K-9 28`），與全 `docs/` 母體之值四簿皆異（`330`／`154`／`80`／`29`）。檔數與讀不到數本身正確、值本身合正典（單檔母體係正典所令）⇒ **數對、母體標錯**。
> 🔒 **同族**：`自誤 238`（同一受詞出艙之數各出自不同之框而未具名）。
> 🔒 **通則**：三軸 `(2)` 所載之母體須為**產生該數之母體**，⛔ 為該輪其他量測之母體。

---

## §七　工項六：`d-2` 阻斷可解之登記（己·純登記·⛔ 辦本體）

**判（附件甲 §己）**：`-266R` 之阻斷點「`K-9-10`／`有效垂線` 命中 `0`」係**域語言字樣層之偽陰性**；程式語言之同義符號俱在（`_n14_band_hi` `9`／`t_hi` `24`／`_t_hi` `18`／`_rear` `4`）。碼面現況 ＝ 舊式 `t_hi = min(後緣連續段角點之 t)`；`K-9-10` **⛔ 實作**。
**落點**：`GB` 簿檔末·`GB-155` 之後追加「`d-2` 阻斷可解」之登記（⛔ 鑄號·⛔ 動 `GB-155` 一字）。須載完整同義詞集（域組 ＋ 程式組）與所需三閘（附件甲 §己 逐字）。
🛑 **⛔ 辦 `d-2` 本體、⛔ 實作 `K-9-10`**（後者變更帶底定式·有土地後果·屬幾何批之後）。

---

## §八　驗收（性質閘·⛔ 計數式）

| 閘 | 受詞 |
|---|---|
| `V-a` | §一 七閘逐條復現；級自載中級；長跑全批一次 |
| `V-b` | 工項零 `c`：量測器實跑 `registry`／`occupancy 265 267`，輸出與附件甲 §1 甲部／§2／§丁 逐位相符；`git status --porcelain` `= 0` |
| `V-c` | 工項一：三則補款齊備；`GB-67`／`GB-101` 原文與現況欄**未動一字**（`git diff` 逐列）；`app.py` 未動 |
| `V-d` | 工項二：`GB-156` 被三框聯集命中、`157` 命中 `0`；八側表整表出艙；二對照如預期（各載 `n`）|
| `V-e` | 工項三：`329` 格逐格出艙、相異 `0`；二對照如預期；`R3` 之值標「單源」 |
| `V-f` | 工項四：`VR-095` 被定義框命中、`096` 命中 `0`；`VR` 清單立後 `79/94/[73,75]` 不變；四框並掃 `CLAUDE.md` 立前後差集 `∅`；序 `12`／`14` 原文未動 |
| `V-g` | 工項五：`331`／`332` 被定義框命中、`333` 命中 `0`；落地後自誤 `325/332/[106]` |
| `V-h` | 工項六：同義詞集二組齊備；`GB-155` 原文未動；`K-9-10` 仍命中 `0`（已證未動）|
| `V-i` | **零生產碼**：`34` 檔 blob 期初＝期末；四檔 `git diff --name-only` 逐 commit 命中 `0`（含空輸出）|
| `V-j` | 既有檔 `deletions = 0`（`--numstat`·⛔ `--cached`）|
| `V-k` | `run_verification` 前後 `rc`／`stderr`／`RESULT`／`24` 紅 `10` 綠名目集合雙向差集 `∅` |
| `V-l` | 自解清單存在（空亦明寫），每項五款；每一對照組同格載判定集基數 |

---

## §九　停機款

1. 任一對照組判定集基數 `= 0` 而計為通過 ⇒ 停機（`自誤 329`）。
2. 工項三任一非首街廓相異格 `≥ 1` ⇒ 停機上呈。
3. 任一工項須改生產碼一字（含註解）⇒ 停機上呈（級之升不降）。
4. 本批量得新的覆蓋帳違反組或既有值改變 ⇒ 停機上呈。
5. 疑義落入自解款黑名單 ⇒ 恆停機。
6. **`GB-67` 之任何修法動作** ⇒ 停機（本批只登記「開」）。
7. 🔒 **CC 停機並回報，恆為正確程序。⛔ 自行放寬判準而呈上。**

---

## §十　出艙形

報告 ⇒ `docs/reports/W-G.9-267R_三裁登記與GB156與逐街廓口徑.md`。
a. 逐工具跑三欄（`rc`／逐字結論列／`stderr` B）。 b. 每一計數附計數式 ＋ 第二態。 c. 每一數標來源層。 d. 每一「復現」載框之來源。 e. 每一「不可得」具名阻斷點。 f. 自解清單。 g. 每一對照組同格載 `n`。 h. ⛔ 生產碼 diff、⛔ 修法主張。
🔒 行號衛生：⛔ 行號（含 log 列號）為錨。

---

*本單受詞 ＝ CC。零生產碼 ⇒ 得逕推。`-268` ＝ 幾何影響批·新窗·側分支·逐 commit KL 放行。*
SELF_SHA256（受詞 ＝ 本列以前之全部位元組·含各列換行·框 ＝ verify/probes/probe_order_preflight.py 之 P-5）: e6fecb2d0ef6494ad01d7705681e4d671949a865bf7f4f193a2beab07c7e1f6f
