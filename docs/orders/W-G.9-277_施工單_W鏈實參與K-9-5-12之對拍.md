# W-G.9-277　施工單（**零生產碼**：`W_prev = 0.1775` 之受詞與 `K-9-5-12` 之對拍／二解算器之不一致／`自誤 380` 之鑄）

> **受詞** ＝ 現施工窗。｜**主線** ＝ `52cf3a39d2b9b543b71cada1452ca3c1dc3a5075` **或其後裔**（🛑 `ls-remote` 實查）。
> **凍存** `p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
> **此單此前是否已交付** ＝ **否**。｜**分級** ＝ **中**（含雙態驅動·**零生產碼**）。
> 🔒 **⛔ 動生產碼 `34` 檔一字。**｜🔴 **推送一律 `git push origin HEAD:refs/heads/wip/s1-endpart`**，push 後 `ls-remote` 實查方得稱已推。

---

## §零　開工閘（**⛔ 略**）

| # | 受詞 | 期值 |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `52cf3a3…` 或其後裔 |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須 **非祖先** |
| `4` | **`SELF_SHA256`**（口徑 ＝ `P-5` 內建） | 見本檔**末列** |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 紅 ⇒ 停機款；其餘逐項具名 |
| `6` | 四簿 ＋ 坑 | 自誤 `369`／`379`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `81`／`81` |
| `7` | `verify/baselines` | `298` 檔·`a898f4e1…`（**口徑 ＝ `core.quotePath` 預設**·`自誤 379`） |

---

## §零′　`W-G.9-276` 全批核可 ＋ 二項發單側之誤

**全批核可**（發單側自倉自跑·rev `52cf3a3`）：三 commit 鏈相符；`fe1d865..HEAD` 對生產碼母體 `numstat` ＝ **空**；`app.py` blob 期初＝期末；`p3a` 非祖先；四簿 **自誤 相異 `369`／MAX `379`·恰 `+1`**·三簿⛔ 動；坑 `81`／`81`。三則入倉前自捕與三項自解，**逐項核可**。

🩸 **你之否證成立**。發單側自證：`verify/app_harvest.py` 逐字 `exec(code, ns)` ⇒ **`ns` 即 `__globals__`** ⇒ **鑄 `自誤 380`**（`§一`）。
🩸 **另一項亦為我之誤**：我令「二對照皆 `>0`」為出艙條件，而其一 `k6_step0_merge` 於**態乙結構上必不被呼叫**（step0 關閉）⇒ 該對照於態乙**⛔ 具必命中性** ⇒ **`自誤 345` 之再現（第二例）**，以**末端追加**登記（`§二`·**⛔ 鑄新號**）。

---

## §一　🩸 `自誤 380` 之鑄（**責 ＝ 發單側**·逐字轉錄·**⛔ 改寫**）

> **形**：**以一個未經自倉查證之<u>機制假說</u>否定一個實測數，並將該假說寫入開場文而使其取得常規之地位。**
>
> **本案**：`W-G.9-275 §二`、開場文 `§四` 坑 `12`、開場文 `§六` 三處皆載「間諜掛於 `ns[...]` 而受測路徑以**模組全域名解析** ⇒ 其呼叫**構造上看不見**」。**實測否證**：`verify/app_harvest.py` 逐字 `exec(code, ns)` ⇒ **`ns` 即真函式之 `__globals__`**；二法之標的機驗為**同一 dict**（`8`／`8` 次 drive 皆 `True`），十二列計數**逐格相同**；且 `k6_step0_merge`（頂層 `def`·正是模組全域名解析）**確被法甲攔下**（計數 `1`）。
>
> 🔴 **本則之處置正確而<u>由</u>錯**：「該 `0` ⛔ 採信」之判成立——其**真正**之射程限制係「`app.py` 之五個 `k956` 呼叫點 AST 實查**全落於 `main()` 內**，而 `CLAUDE.md §7` 逐字載 harvest **從不呼叫 `main()`**」。🛑 **⛔ 以結論之正確掩其由之誤。**
>
> **通則**
> > **凡以「器之構造」為由否定一個實測數，該構造之主張<u>本身</u>須先自倉逐字查證（其綁定處、其命名空間之來源）；⛔ 以「聽起來對」之機制假說代之。** 承本波之戒：**自洽而直覺上有說服力之論證最危險**——其恰好壓制了查證之衝動。
>
> 🔒 **連帶處置**：開場文 `§四` 坑 `12` 之**由**作廢（其攔法「先以必命中之對照證器」**仍存**，且本批正因該攔法而查出真由）；**⛔ 追改已入倉之任何文件**——以本節為準。

**節題之形須與既有九則同構**。**取號現查**依三形聯集框；對照組甲 `自誤 379`／甲2 `378`（皆須 `>0`）／乙 人造哨兵須 `0`。

## §二　`自誤 345` 之再現（**第二例**·末端追加·**⛔ 鑄新號**）

於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末**（⛔ 節末）純末端追加一節，題逐字：

```
## 🔧 `自誤 345` 之再現第二例：對照組之必命中性**隨受測態而變**，而以單一對照套用於二態（`W-G.9-277` §二·⛔ 上文一字不刪·純末端追加）
```

內載：🛑 本節**⛔ 鑄任何號**；🔒 ⛔ 改 `自誤 345` 原節一字。
**本例**：`W-G.9-276 §三-1` 之準據令「二對照皆 `>0` 方得出艙」，而對照 `k6_step0_merge` 於**態乙**（`WV_K6_STEP0=off`）**結構上必不被呼叫** ⇒ 態乙之四格**恆判「部分」**，`k956` 之計數於態乙**永無出艙之日**。**責 ＝ 發單側**（CC 依令照辦並具名「⛔ 可得」，正確）。
**通則之補**：**對照組之「必命中性」須<u>逐態</u>成立**；受測物有二態以上者，**每一態各需其自身之必命中對照**；⛔ 以單一對照套用於全部態。

---

## §三　工項（**零生產碼**）

| 序 | 事 |
|---|---|
| `0` | 本單原封入倉（`§二-B` 搬移法·搬移前「所指之檔數 ＝ `1`」自證） |
| `1` | **`W` 鏈之逐宗實參 ＋ `W_prev = 0.1775` 之受詞**（`三-1`·**本批主檢**） |
| `2` | **二解算器之不一致：本案之 `理論@k*` 出自何者**（`三-2`） |
| `3` | **`_宗地寬度` 之產生式 ＋ 「虛胖陷阱」之逐字對拍**（`三-3`） |
| `4` | `自誤 380`（`§一`）＋ `345` 再現第二例之末端追加（`§二`） |
| `5` | 收工閘（`§四`）⇒ **停** |

### 三-1　`W` 鏈之逐宗實參 ＋ `W_prev` 之受詞（**主檢**）

**已知（發單側自倉查得·逐字·⛔ 你再查）**：`K-9-5-6` 之 `W_0` 款原載「`W_0` ＝ MP→corner（照實可負·⛔ 禁 clamp）；**forced 時為其 buffer 起算點**」，其**前半已由 `K-9-5-12`（KL 裁 `2026-08-12`）撤回** ⇒ **「首宗起點一律為 `0`（即自 MP 起量）」**；其**後半（forced 時為 buffer 起算點）⛔ 一併撤，列為<u>未決</u>**。
**已知**：`0m` 態乙 `R4` 之 `_fo_left = False`／`_fo_right = False`（`W-G.9-276` 落檔逐字）⇒ **該側非 forced**。

**器** ＝ `verify/probes/probe_WG9277_wchain.py`（以前批 `probe_WG9276_k956_count.py` 之 drive 為底本·⛔ 另寫第二份判準）。🛑 坑 `bb` 之攔法適用（框內省 `depth + 1` ＋ 自我驗證閘）。

**辦法**：**包裹 `rw_increment`**，錄其每次呼叫之**實參 `(W_prev, W_cur)`、回傳、及其呼叫端之街廓／側別／宗**（框內省）。逐情境（`0m`／`3.5m`）× 逐態（甲／乙）出艙：

1. **`R4 right` 之該側宗序列**（暫編地號·投影序）與其**逐宗 `W_i`／`Rw_i`**（未捨入）；
2. `W_prev = 0.1775004071` 係**首宗之 `W_0`** 抑或**前一宗之 `W_i`**——以序位具名，**⛔ 推定**；
3. 若其為 `W_0`：與 `K-9-5-12` 之逐字「**首宗起點一律為 `0`**」**同格並列**，並同格具名該側之 `_fo_right`（已知 `False`）；
4. `0.1775004071` 之**產生式逐字**（其被賦值之運算式·⛔ 行號為錨）；
5. 全六街廓 × 二側 × 二情境 × 二態之 `W_0` 值表（**期望多數為 `0`**·⛔ 預寫）。

**判別力（逐態各備·`§二` 之通則）**：**必命中**對照 ＝ `rw_increment` 本身——其於**每一態**皆須 `>0`（`理論@k*` 與實跑二側皆用之）；**必為零**對照 ＝ 一人造實參（執行期組出·字面⛔ 出艙）須⛔ 出現於所錄集合。某態之必命中對照得 `0` ⇒ **該態之計數⛔ 出艙為事實**，逐態具名。

🛑 **⛔ 判實跑側違反 `K-9-5-12`、⛔ 判孰為正確之 `W_0`、⛔ 提修法主張**——**出艙即止**。

### 三-2　二解算器之不一致：本案之 `理論@k*` 出自何者

**已知（`K-9-5-6` 之 `D-2b-5` 具名更正·逐字）**：
> **偏離者僅為幾何解算器**：`def iterate_G_S`（代數）之路徑**一直合規**（`rw_increment(W_prev, W_cur)`）；`def solve_G_binary`（幾何）之路徑偏離（`rw_increment(_W_near, W)`）⇒ **同一檔內兩個解算器互不一致**，而**無任何檢測捕捉之**。

**須出艙**：
1. 二解算器之**現行**呼叫式逐字（`iterate_G_S`／`solve_G_binary` 內之 `rw_increment(...)` 實參名）——**⛔ 採上開更正文之轉述，一律自碼面現查**；
2. 本案（`0m` 態乙 `R4 right`）之 **`理論@k*` 係經哪一解算器產生**（以 `三-1` 之框內省呼叫端定之）；
3. 該結構閘之**新增檢測**逐字（`逐界面 Δ_i == 0`·`D-2b-4 §四(b)`）於**界面 `0`** 時之行為——其是否**退化為空檢**（本案出艙「（無界面）」）。

🛑 **⛔ 判二解算器孰誤、⛔ 判該檢測是否退化為缺陷**——出艙即止。

### 三-3　`_宗地寬度` 之產生式 ＋ 「虛胖陷阱」之逐字對拍

**已知（`K-9-5-6` 逐字）**：`W_i` 之量測**仍為 intrinsic 直量**（MP → 本宗**遠側**分配界線之垂距）——**⛔ 非以寬度累加堆出**（「虛胖陷阱」`app.py`）；**累積的是 `Rw`、不是 `W`**。

**須出艙**：`_宗地寬度` 之**產生式逐字**與其**受詞**（其係何二物之距）；「虛胖陷阱」該處註解之**全段逐字**；並將 `理論@k*` 所餵之 `_宗地寬度` 與上開逐字**同格並列**。
🛑 **⛔ 判其是否落入虛胖陷阱、⛔ 提修法主張**——出艙即止。

---

## §四　收工閘（**commit 前·與被守護之動作分開呼叫**）

1. `numstat` 刪除欄逐檔具名。
2. 生產碼判法四檔命中 `0` 行 ⇒ 零生產碼 ⇒ 逕推（受詞見文首推送款）。
3. `.md`／`.py` 之 `CR` ＝ `0`；前綴檢之判別力二造。
4. **四簿期末**——**期值**：自誤 相異 `370`／MAX **`380`**；`GB`／`VR`／`K-9` **⛔ 動**。🔴 期值⛔ 為現值。
5. **坑之定義處清單自證**（報告入倉**後**量）——**期值 `81`／`81`**（本批⛔ 鑄坑）。
6. **自限復驗**：`app.py` blob 期初＝期末／`baselines` `298`·`a898f4e1…`（**口徑 ＝ `quotePath` 預設**）／`p3a` 未併線／`WV_BAKE` 未設／`K-9-29` 任何子項 ⛔ 落地／`GB-167`·`GB-168` ⛔ 修。
7. push 後 `ls-remote` 實查其值。

## §五　鑄號之授權（**⛔ 逾此**）

**自誤 `380`** ——**僅此一號**。🛑 `GB`／`VR`／`K-9`／**坑** 本批 **⛔ 鑄**（`§二` 為末端追加·`戒 36`）。坑若自捕須鑄，先出艙**形／徵候／攔法**候准；准則標籤 ＝ **`bd`** ＋ 同批擴 `ALPHA_LABELS`。

## §六　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字；⛔ 改五個 `*_EXPECT` 任一字；⛔ 修 `GB-167`／`GB-168`。
2. ⛔ 落地 `K-9-29` 任何子項；⛔ 落地 `GB-164`／`165`／`166`。
3. `GB-162` 三禁令維持；⛔ 引任何自 baseline 取得之數。
4. **⛔ 判孰為正確之 `W_0`／`W`、⛔ 判二解算器孰誤、⛔ 判實跑側或理論側違反任何裁決**——**四者皆出艙即止**。
5. **⛔ 提「結構閘應放寬／應停用」之任何主張**。
6. **⛔ 呈 KL 任何事**；⛔ 併線 `p3a`、⛔ 刪任何 ref。
7. **⛔ 於任何入倉文件宣稱一件尚未發生之事**——及於 commit 訊息與「已推送」之陳述。
8. **⛔ 追改已入倉之任何文件**（含開場文 `§四` 坑 `12` 之由·`§一` 已載其處置）。

**辦畢 ⇒ 出報告 `docs/reports/W-G.9-277R_…md` ⇒ 停**（⛔ 自續下一批）。

*本單受詞 ＝ CC 施工窗。發單側 ＝ claude.ai。⛔ 呈 KL。*
SELF_SHA256: 49e3f93e205f37bd1a3fef5a6445bd7c221ef5b72b2cb17805c93d28b19319a5
