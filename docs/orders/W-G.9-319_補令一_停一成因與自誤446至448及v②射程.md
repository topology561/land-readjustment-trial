# `W-G.9-319` 補令一（**輕·唯讀·⛔ 零生產碼**）：`停一` 成因之坐實與 `[必相符]` 造之撤回重設／`自誤 446`〜`448` 之鑄／`恆常附款 v②` 射程之增補／階段 `b` 之三造重設計

> **單** ＝ `docs/orders/W-G.9-319_補令一_停一成因與自誤446至448及v②射程.md`（工項零 ＝ 本補令原封入倉）。
> 🔒 **本補令於衝突點取代 `W-G.9-319` 原單**；二者**皆留倉不改一字**（`常規四（九）五`）。
> 🛑 ⛔ 動生產碼一字、⛔ 寫 `verify/baselines`、⛔ 設 `WV_BAKE`、⛔ 鑄 `GB`／`VR`／`K-9` 號、⛔ 解除或收窄 `GB-168`／`169`／`170`／`171`。
> 🛑 ⛔ 以命令列處理含反引號之文字（`恆常附款 h`）——一律 `git commit -F` 檔案管線。

---

## `§零-1`　發單側之裁（**五裁**）

**裁 `1`　`停一` 之判維持·CC 之處置受領為正·⛔ 追改**
`§二-1` `[必相符]` 造不成立為**事實**，CC 依單明文停機、⛔ 追改、⛔ 改取法——**處置正確**。惟其**成因⛔ 在受單側，亦⛔ 在碼面**，而在**發單側之期值繫他態**（`自誤 447`）。

**裁 `2`　`[必相符]` 造之撤回與重設**
原造（`GB` 簿 `:7359`–`:7360` 之 `14.01`／`14.20` ⇒ `87.835000`／`88.500000`）**全部撤回**——其三軸之來源 `f46ce92`（`2026-09-13`）係開工態之**祖先**，且 `f46ce92..c286a79` **恰含 `W-G.9-318` 之六筆生產碼**（`K-9-41 ①` 之落地）⇒ **跨批·⛔ 可比**（`恆常附款 w②`）。
**重設之造**（繫開工態·同簿之**末端追加**節 `:8254` 逐字）：
> `🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.72 ≠ 實跑(階段1)=88.40（Δ=0.676 >0.1）`

🔒 **CC 之實得對此造之判（發單側當場覆核）**：`R(13.98)` ＝ `87.724000` ⇒ `2dp` **`87.72`** ⇒ 與 `理論@k*` **逐位相符** 🟢 ⇒ **其器非紅、其量為正**。
🩸 **併記（只登記·⛔ 判其成因·⛔ 據以改任何判）**：`R(14.17)` ＝ `88.395000` ⇒ `2dp` **`88.39`**，而閘之 `實跑(階段1)` ＝ **`88.40`** ⇒ **⛔ 逐位相符**，具名其差 `0.01`（`2dp` 顯示層）。其可能之由（**⛔ 判**）＝ 實跑側所餵之 `W` 為**未捨入**而本器之 `W(m)` 欄為 `2dp`——即 `GB` 簿 `W₀ 二消費者之粒度不對稱` 一節所登記之同族現象。

**裁 `3`　階段 `b` 之**重設計**（⛔ 依原單之二造逕辦）
原設計（以**未捨入** `W` 代 `widths`）於本批坐實**同時更動二事**：
`(一)` **軸**（`_宗地寬度` 含一 `|cos|` 投影／`W` 不含）；`(二)` **粒度**（`_宗地寬度` 之未捨入**結構上不存在**·其賦值處即 `round(_pw, 2)`·`app.py:22036`／`verify/stepg_pipeline.py:737`；而 `W` 為未捨入）。
⇒ 其產物**⛔ 可歸因** ⇒ **重設為三造**：

| 造 | `widths` 所餵 | 分離之因 |
|---|---|---|
| `α` | `_宗地寬度`（現況·`2dp`） | 基座 |
| `β` | `round(W, 2)` | **只換軸**·粒度同 `α` |
| `γ` | `W`（未捨入） | 換軸 ＋ 換粒度 |

🛑 **三造之執行⛔ 於本補令辦**——其受詞於此釘死，另單（`W-G.9-320`）辦，且須於**新窗**擬（本波之窗已近收口）。

**裁 `4`　`P-5` ⛔ 擴框·⛔ 動一字——改單之形**
`P-5` 之解析逐字 ＝ `ls[i].decode(...).split(":")[-1].strip().strip("`")`；原單採 `` `SELF_SHA256` ＝ `<hash>` `` 之形，其**無半形冒號** ⇒ 整列入 `split` ⇒ **結構上必不符**。
⇒ 🔒 **發單側此後一律採 P-5 既認之形**：`SELF_SHA256:` ＋ 半形空格 ＋ **裸 hash**（⛔ 反引號·⛔ 全形等號），且置於**檔末最末一列**。
🔒 **其據** ＝ `恆常附款 v②` 逐字「失配即改 payload、**⛔ 改器**」；本補令將其射程增補（`§三`）。
🔒 CC 本批依**自解款白名單 `2`** 機械確證傳輸保真未破 ⇒ **處置正確·受領**。

**裁 `5`　`W-G.9-319` `§零-3` 項 `3` 與 `§八` 款 `w` 之相異·⛔ 追改**
原單錨表項 `3` 之判別力值載 `356`，實測 `357`（差 ＝ `c286a79` 所加之 `verify/probes/probe_WG9318p6_ffverify.py`·發單側當場重得）；原單 `§八` 款 `w` 判 🟢「`§零-3` 項 `1`〜`8` 皆繫 `c286a79` 當場重取」與結語「`12`／`12` 格皆量得」**對該格為偽陳述**。
🛑 **⛔ 追改原單一字**（`常規四（九）五`）——本補令即其具名；其登記 ＝ `自誤 446`。

---

## `§一`　工項零：本補令原封入倉

`docs/orders/`·`sha256` 與 bytes 同格出艙·⛔ 改一字。

## `§二`　工項一：`自誤 446`／`447`／`448` 之鑄（自誤簿**檔末嚴格末端追加**）

🔒 **標題形自 `自誤 445` 之節題列逐字複刻**（`恆常附款 v①`·範本 ＝ 自誤簿 `:10760`）。
🔒 **出單前已以二框自驗三則 payload**（`恆常附款 v②`）：自擬框 `RE_ZW_DEF` 與正典框（`VR-091 四`）**三則皆命中** ⇒ ⛔ 落入缺號集。

### `§二-1`　`自誤 446`（逐字·⛔ 改一字）

> ### 🩸 `自誤 446`　**態錨表之判別力格繫他態而表宣稱繫開工態，且同單之逐款回掃對此作成偽陳述——重踩同單所鑄之 `自誤 445`**
>
> **形**：`W-G.9-319` 原單 `§零-3` 項 `3` 之判別力[必不命中]值載 **`356`**，而該表卷首逐字宣稱「下表每一格**皆為當場量得**」且項 `1` 載開工態 ＝ `c286a79…`。實測：`356` 係 **`be54799`** 之值，`c286a79` 之值為 **`357`**（差 ＝ `verify/probes/probe_WG9318p6_ffverify.py`·即 `c286a79` 所加者）。
> **加重情節（其一）**：同單 `§八` 逐款回掃之款 `w` 判 🟢「`§零-3` 項 `1`〜`8` 皆繫 `c286a79` 當場重取」、其結語逐字「🟢 `12`／`12` 格皆量得·⛔ 一格為推論」——**二處皆為偽陳述**，且該回掃正是 `恆常附款 ab①` 之施行。
> **加重情節（其二）**：本則之形**即同單所鑄之 `自誤 445` 之受詞**（錨表之格繫他態／推論與量得同格）⇒ **同一單內鑄款而即刻重踩**，`445` 之攔法於該單生效 **`0`** 次。
> **根因**：`恆常附款 w②` 之施行止於「**錨之值**逐格重取」之**自述**，而⛔ 以**器**逐格重跑；`§八` 之回掃亦係**自述**而⛔ 機械。⇒ 二層皆為**人工宣稱**，無一道機械閘。
> **後果之框**：⛔ 偽數入倉（CC 於 `§二-4` 照實出艙並具名相異·⛔ 追改）；其害止於**一格判別力值之偽**（該格⛔ operative——`357` 與 `356` 皆 `> 0`，其判別力之結論⛔ 變）。其攔點 ＝ **受單側之態錨復現**（`11` 項中 `10` 項 🟢／`1` 項相異），**該工項生效一次**。
> **攔法（`恆常附款 w②` 射程之再增補）**：凡錨表之任一格，**須由一支器對開工態逐格重跑並落檔**，其落檔之 `commit` 須與表所載之開工態**同一** ⇒ ⛔ 以自述充「皆當場量得」；`§八` 型之逐款回掃，凡其判之受詞為「某表皆量得」者，**須引該落檔之路徑**，⛔ 逕判 🟢。

### `§二-2`　`自誤 447`（逐字·⛔ 改一字）

> ### 🩸 `自誤 447`　**`[必相符]` 造之期值取自倉內一個前於本波生產碼落地之態，而同一簿之末端追加已載改後之值且發單側於同窗已逐字讀過 ⇒ 停機款誤觸、階段 `b` ⛔ 辦**
>
> **形**：`W-G.9-319` `§二-1` 之 `[必相符]` 造，令 `0m` 態乙 `R4 right` 之 `628-1(1)` 須與 `GB` 簿 `:7359`–`:7360`（`宗地寬度(m)` `14.01` ⇒ `R` `87.835000`；`W(m)` `14.20` ⇒ `R` `88.500000`）逐位相符。該節之三軸來源 ＝ `blob@f46ce92`（`2026-09-13`），而開工態 ＝ `c286a79`，`f46ce92..c286a79` **恰含 `W-G.9-318` 之六筆生產碼**（`K-9-41 ①` 之落地）⇒ 該期值**繫前波之態**。
> **加重情節**：**同一簿之末端追加節 `:8254`** 已逐字載改後之值（`理論@k*=87.72` ／ `實跑(階段1)=88.40`），而發單側**於同一窗已逐字讀畢該節**（交接文 `§七-5` 序 `5` 之辦理）。⇒ 其非「未讀」，而係**已讀而仍取舊值** —— 較 `自誤 438`（只讀節題未讀受詞段）**更進一層**。
> **後果之框**：🔴 **有工項後果**——`停一` 成立 ⇒ **階段 `b`（替身）⛔ 辦**，本單為域裁所備之土地後果**未能取得**，域裁因而延後一輪。其攔點 ＝ **CC 依單明文停機上呈**（⛔ 追改、⛔ 改取法），**該停機款生效一次**；若 CC 曾「順手把期值改成實得」，則該偽合格將直達域裁。
> **根因**：`恆常附款 w②`（跨批沿用之錨一律先判⛔ 可比、須以本批開工態重取）之射程及於「**錨**」，而未及於「**閘之判別力造之期值**」——二者同為「單所設之數」，惟前者有明文、後者無。
> **攔法（`恆常附款 w②` 射程之再增補·與 `自誤 446` 同節承載）**：`w②` 之「跨批沿用者一律先判⛔ 可比」**明文擴及單內一切閘之期值與判別力二造之期值**；凡其值取自倉內既有文件者，**須同格載該文件該節之三軸來源 `commit`**，並與**開工態**作 `git merge-base --is-ancestor` 之判；為祖先且區間含生產碼者，**一律先判⛔ 可比**，須改引**同簿之最近一則末端追加**或以開工態現查重取。

### `§二-3`　`自誤 448`（逐字·⛔ 改一字·**發單側自捕**）

> ### 🩸 `自誤 448`　**斷言二個相異源為同一量（`_宗地寬度` 式中之 `|cos|` ＝ `GB-171` 之軸差餘弦）而未機械驗證，並據以作成一張單之裁與整個域裁之框**
>
> **形**：發單側於接交接文十三之窗，`§七-2` 逐字出艙「**殘餘之 `Δ` 與 `GB-171` 之軸，其受詞為同一個 `|cos|`**——`_宗地寬度` 式中之 `|cos|` 即該 `2.7137°` 之餘弦」，並據以作成 `W-G.9-319` `§零-2` 裁 `1`（「候選甲與候選乙收斂為同一域裁」）。**該等同⛔ 經任何機械驗證**。
> **反證之量（本批實得·發單側當場算）**：`0m` 態乙 `R4 right` 之二寬度 ＝ `13.98`（`_宗地寬度`）／`14.17`（`W`），差 **`0.19` m**（落地前為 `14.01`／`14.20`·差亦 `0.19`）；而該角所能解釋之量 ＝ `14.17 × (1 − 0.9988785831)` ＝ **`0.0159` m** ⇒ **⛔ 同量級**（相差逾一個數量級）。
> 🛑 **⛔ 反向斷言「該角與本差無關」**——`_宗地寬度` ＝ `round(S × |cos|, 2)` 之 `S` 是否即 `W`，**本批⛔ 量**；本則只坐實**「同一量」之斷言未成立為已證**，其真正之分解**候 `W-G.9-320` 之三造**。
> **根因**：`恆常附款 aa②`（二框以上之並取須先具名其相異維度）之**逆形**——本則係**二量之合併**而未先具名其可分離之維度；`u④`（先判須同格載可反證之檢：產生點／量測方向／口徑是否同一）之受詞為「先判」，未及於「**逕判為同一**」。族 ＝ `441`／`443`／`444`／`446`（**未經本批驗證之數／框／歸因／等同**），本波第 **`6`** 度。
> **後果之框**：⛔ 偽數入倉（本窗該訊⛔ 有 `commit`；`W-G.9-319` `§零-2` 裁 `1` 已入倉，**⛔ 追改**，其更正即本則）。其害為**域裁之框被預先收窄為一問**，若未攔，呈 KL 之呈文將以「同一條幾何」為前提，而該前提未經證立。其攔點 ＝ **發單側自攔**（覆核 CC 之階段 `a` 實得時當場算得）。
> **攔法（二款·併入 `恆常附款 u`·⛔ 新立款）**：
> `u⑥` 凡出艙「二量之相異源為**同一**」之斷言者，須同格出艙**一個數值檢**——以該共同源所能解釋之量與實得之差作**同量級判**；⛔ 以「角相同」「量級相仿」之語充之。
> `u⑦` 凡據一個未經 `u⑥` 之斷言收窄工項之框、或據以作成一張單之裁者，該裁**自始標【未證】**；其解除須待 `u⑥` 之數出艙。

## `§三`　工項二：`恆常附款` 射程增補之登記（恆常附款簿**末端追加節**·⛔ 追改 `§一` 表一字）

節題形：`## 🔧 恆常附款 w② 與 v② 與 u 之射程增補（W-G.9-319 補令一·⛔ 上文一字不刪·純末端追加）`

| 款 | 增補之逐字 | 所繫 |
|---|---|---|
| `w②` | ① 錨表之任一格須由**一支器對開工態逐格重跑並落檔**，其落檔 `commit` 與表所載開工態須同一；⛔ 以自述充「皆當場量得」。② 「跨批沿用者一律先判⛔ 可比」**明文擴及單內一切閘之期值與判別力二造之期值**；其值取自倉內既有文件者須同格載該節之**三軸來源 `commit`**，並與開工態作 `is-ancestor` 之判。 | `自誤 446`／`447` |
| `v②` | 「失配即改 payload、⛔ 改器」之射程**明文擴及單之任何書寫形與任何既有量測器（含 `probe_order_preflight.py` 之 `P-5`）之框相衝之情形**——一律**改單之形**，⛔ 擴器之框。 | `裁 4` |
| `u⑥`／`u⑦` | 見 `§二-3` 逐字。 | `自誤 448` |

🔒 **款數之算式**（`恆常附款 f`·⛔ 直接寫一個數）：期初 **`28`** 款 ＋ 本批新立 **`0`** ⇒ 期末 **`28`**；本批**修訂 `3` 款**（`w`／`v`／`u`）。

## `§四`　工項三：`W-G.9-319R` 之末端追加（⛔ 改上文一字）

於 `docs/reports/W-G.9-319R_…_執行報告.md` 檔末追加節 `## 🔧 停一 成因之坐實與 [必相符] 造之撤回重設（W-G.9-319 補令一·⛔ 上文一字不刪·純末端追加）`，承載 `§零-1` 裁 `1`〜`5` 之逐字，並附：
① 重設之造與 CC 實得之逐位對拍（`87.724000` ⇒ `2dp` `87.72` 🟢）；
② `88.395000` ⇒ `2dp` `88.39` vs 閘載 `88.40` 之**具名相異**（⛔ 判其成因）；
③ 階段 `b` 三造（`α`／`β`／`γ`）之受詞釘死，其執行**另單·新窗**。

---

## `§五`　收工閘（輕·報告入倉後量·⛔ 沿用他批之判準字串與錨值）

| 閘 | 期值（**繫開工態 `51a641d334847d285f323257909748acdf7052e1`**·逐格當場量得） |
|---|---|
| `1` | 逐 `commit` `numstat` 刪除欄全 **`0`** |
| `2` | 生產碼 `34` 檔之相異 **`0`**；判別力[必非零] ＝ 對 `40375702378af4cefd2d11515fdf3f98c0791580` 之 `app.py` 須相異 |
| `3` | 本批 `.md` 之 `CR` ＝ **`0`** |
| `4` | 四簿·**二框並報**：自擬框 自誤 ＝ `431 + 3` ＝ **`434`**／`MAX` **`448`**（缺號集⛔ 變）；正典框 ＝ `435 + 3` ＝ **`438`**／`MAX` **`448`**；`GB` `169`／`171`（⛔ 變）｜`VR` `80`／`95`（⛔ 變）｜`K-9` `40`／`41`（⛔ 變） |
| `5` | `append-only` 三值化：自誤簿 🟢／恆常附款簿 🟢／`W-G.9-319R` 🟢／`GB` 簿 🔵（本批⛔ 涉·blob 須仍 `64e91c72779fe84e36a6b124711cdce7c6eba893`）／`K-6` 典 🔵 |
| `6` | 自限：新增檔僅 `docs/orders/W-G.9-319_補令一_*.md`；`git check-ignore -v` 之輸出（**含空輸出**）逐檔出艙 |
| `7` | 母體三數（期初 ＝ 追蹤 **`2566`**／`.md` **`873`**／`docs/` **`844`**）期初期末並列·自指增長逐項具名 |
| `8` | `ls-remote` 全 `40` 碼；遠端 **`25`**／`verify/*` **`20`**；主線與 `preserve` 之值並列（期初 `preserve` ＝ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`·**已落後主線**） |

🛑 凡閘三值出艙（`恆常附款 n①`）並同格載基線態（`r`）；不符即**具名相異·⛔ 追改**。
🛑 全批零生產碼 ⇒ 依 `常規一` 補款 `🔧 一` **逕推主線·⛔ 請示**；逐 `commit` 須出艙生產碼判法之輸出（含空輸出）。

## `§六`　`恆常附款 a` ＋ `ab①` 之本補令自身施行（**逐款 `28`**）

🔒 本補令所**修訂**之款 ＝ `w②`／`v②`／`u`（`u⑥`／`u⑦`）；依 `ab①` 逐條施行於本補令自身：

| 款 | 施行 | 判 |
|---|---|---|
| `w②`（增補後）① | 本補令之錨表（`§五` 閘之期值）是否由器對開工態逐格重跑？ | 🟢 `§五` 每一數皆於 `51a641d` 當場量得（`431`／`435`／`2566`／`873`／`844`／`25`／`20`／`c286a79`）；**並令 CC 收單時以器重跑並二值並列** |
| `w②`（增補後）② | 本補令一切閘之期值，其取自倉內文件者是否載三軸來源 `commit` 並作 `is-ancestor` 判？ | 🟢 `§零-1` 裁 `2` 逐字載 `f46ce92` 之 `is-ancestor` 判（`rc 0`·區間含六筆生產碼）；重設之造改引**同簿最近一則末端追加** `:8254` |
| `v②`（增補後） | 本補令之書寫形是否已改為 `P-5` 既認者？ | 🟢 檔末採 `SELF_SHA256:` ＋ 半形空格 ＋ 裸 hash（⛔ 反引號·⛔ 全形等號） |
| `u⑥` | 本補令有無「二量之相異源為同一」之斷言？ | 🟢 **有一處**（`§零-1` 裁 `2` 併記之 `0.01` 差與粒度不對稱之關聯）⇒ 已標**⛔ 判其成因**、⛔ 斷言同一 |
| `u⑦` | 本補令有無據未證之等同作成之裁？ | 🟢 `§零-1` 裁 `3` 之三造設計**即為解除 `448` 而設**，其自身⛔ 預設任何等同 |
| 其餘 `23` 款 | 依 `W-G.9-319` `§八` 之逐款表，其判**⛔ 變**（本補令與原單同為輕級唯讀零生產碼，`c`／`d`／`j`／`s`／`t`／`u`／`y` 仍⛔ 生·惟 `u` 因本批修訂而轉為 🟢） | 🟢 |

🔒 **基數**（`恆常附款 f`）：受掃 **`28`** 款 ＝ 🟢 **`22`** ＋ ⚪ **`6`**（`c d j s t y`·`u` 已轉 🟢）；`22 + 6 = 28` 🟢 **符**。CC 收單時須當場重算此三數並二值並列。

## `§七`　上呈（意思決定·⛔ CC 自裁）

1. 檔名之核可（`常規三`）。
2. 🛑 **階段 `b` 三造⛔ 於本補令辦**——其單（`W-G.9-320`）由發單側於**新窗**擬。
3. 🛑 本補令⛔ 及於：四項 `GB` 之解除或收窄、`GB-170` 取錯線之修、`GB-138` 處置 ③、`K-9-41 ②`〜`⑥`／`K-9-30`〜`K-9-37` 之落地、泛用化波。

SELF_SHA256: 274796741bb39160d81531c2ceda3cccc228ac211b82260b825b6f283a9147b9
