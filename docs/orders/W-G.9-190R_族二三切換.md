# 🔧 `W-G.9-190R`　施工單：族②③ 之切換（`W-G.9-5` 之落地）

> **出單窗**：claude.ai·`Opus 5`（`W-G.9-191` 交接文之接手窗）　**出單日**：`2026-08-31`
> **基座**：`25857aa99ced20ff9e6abb1d519bdf7d7ab1ff5a`（`wip/s1-endpart`）**或其後裔**
> **廢止**：`W-G.9-190`（⛔ 不得執行·已知含 `P-3`／`P-4` 之缺，另含 `六-3` 之偽停機款）
> **⛔ 不含遞補**——遞補順延 `W-G.9-192`（🛑 交接文 `§八-2` 稱 `191`，該號已被交接文自身佔用 ⇒ 順延一號）

---

## §零　⛔ 先讀此節

1. **本單之任何數，⛔ 不得照抄。** 一律自行現查。本單所載之數皆由發單側於**拋棄式 clone** 上實跑取得，逐項具名其母體與框；CC 仍須自行復現。
2. 🔒 **只有真觸及域邊界才停機上呈、純技術自行做完。** 工程問題（命名、接縫、實作細節、測試寫法）⛔ 不上呈、⛔ 不回問發單側。
3. 🛑 **凡出艙之數，逐項載其母體與框（逐字）**；裸框與錨定框相異者依 `CLAUDE.md:725` 二數並報。
4. 🛑 **同一字樣於不同母體之命中數可以不同**——本單已實測一例：`app.py` 原始碼含**字面括號**、而 `probe_WG9189_s6_replica.py` 之清單經 `ast.unparse` **正規化去括號** ⇒ 二母體之框⛔ 不通用（見 `§六-3` 之框表）。
5. 🛑 本單之停機款集中於 `§十一`，⛔ 不散落他節；他節出現之數若未列於 `§十一`，一律為**記錄**、⛔ 非判準。

---

## §一　開工閘（`S-0`〜`S-6`·🛑 全綠方得動任何一字）

| 閘 | 受詞 | 判法 |
|---|---|---|
| `S-0` | 本單之 `SELF_SHA256` | 取檔內**最末**一個，重算「除最末一列外全部位元組」之 `sha256` 並對拍。🔒 口徑已由發單側於 `W-G.9-191` 交接文上**反解坐實**：受詞 ＝ **自檔首至 `SELF_SHA256:` 該列之前一個 `\n`（含該 `\n`）為止**之全部位元組 |
| `S-1` | 期初錨 | `§三` 十檔逐項現查（bytes／列／`sha16`） |
| `S-2` | 遠端態 | `git ls-remote` 取實際 `HEAD`；⛔ 不以本單所載之 commit 為錨（`CLAUDE.md:750`） |
| `S-3` | 環境 | 拋棄式 clone·跑畢刪除；`core.autocrlf=false`；跑前後 `git status --porcelain` 出艙 |
| `S-4` | 禁改清單 | 讀 `§十一-3` 全文 |
| `S-5` | 基準量 | 🔒 CC 於 `W-G.9-190` 已取得且與 `177R` 逐位相符（`\|Δ\|` shim `5.5e-13`／app `3.3e-11` ≪ 容差 `1e-4`）⇒ **准沿用**，惟須於新 clone **重跑確認** |
| `S-6` | 原樣復現 `189R` | 見下 `一-1` |

### 一-1　`S-6` 之基座量（🔒 發單側已於 `Python 3.12.3` 實跑復現·CC 須自行重跑）

執行（🛑 **⛔ 不得覆寫已追蹤之 log**——該探針 `:169` 逐字設有拒絕覆寫；合法出口 ＝ 環境變數 `WV_OUT_NAME`）：

```
WV_OUT_NAME="_scratch_s6_base.log" python3 verify/probes/probe_WG9189_s6_replica.py
```

期望（逐項）：

| 受詞 | 期望值 |
|---|---|
| `_build_g_row` 正規化後全等 | `True`（dump `4569` B vs `4569` B） |
| `_solve_one` 正規化後全等 | `True`（dump `1344` B vs `1344` B） |
| `_advance_block_with_split` 正規化後全等 | `False` ⇒ 本區新增：實質差 `3` |
| 逐街廓迴圈 正規化後全等 | `False` ⇒ 本區新增：實質差 `17` |
| `st.*` 略去 | `0` 處 |
| 實質差總處數 | `20` 處 |
| 判別力對照 | 源碼相同 ＝ `False`／AST 仍判全等 ＝ `False`（二問皆須 `False`） |
| `rc` | `1` |

🛑 `ast.dump` 之 **bytes ⛔ 非跨環境錨**（`CPython 3.12.3` 與 `3.13.11` 得數不同，而判定與實質差 `20` 處逐項相同）⇒ **只對拍判定與差數，⛔ 不以 dump bytes 判紅**。
🛑 跑畢**刪除** `_scratch_s6_base.log`，並以 `git status --porcelain` 空輸出證工作樹未被污染（發單側已實行）。

---

## §二　授權款

### 二-1　🔴 **KL 新增款**（`2026-08-31` 逐字·本款為新增，⛔ 非改寫）

> **零生產碼 commit 逕行 push，生產碼 commit 才請示。**

### 二-2　「生產碼」之機械界定（發單側之**工程裁**·⛔ 非域裁·⛔ 不上呈 KL）

🔒 **生產碼檔集**（承倉內既有之四檔清單·⛔ 非本單新創）：

```
app.py
verify/stepg_pipeline.py
verify/run_all.py
verify/run_verification.py
```

出處逐字（`sed -n '19p' docs/reports/W-G.9-183R_VR取號污染處置與式ii擴大驗證.md`）：
「凡須修改 `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 一字（含註解）方能取得之量 ⇒ **停機上呈**。」

**判法（🛑 逐 `commit`·⛔ 非逐批）**：

```
git diff --name-only <前一已 push 之 HEAD>..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

- 命中 `0` 行 ⇒ **零生產碼 commit** ⇒ **逕行 `push`**、⛔ 不必請示。
- 命中 `≥ 1` 行 ⇒ **生產碼 commit** ⇒ **`push` 前請示**。

🛑 該指令之輸出（含空輸出）須**逐 commit 出艙**，⛔ 不得以「我判斷是零生產碼」代之。

### 二-3　與 `常規一` 之關係（🛑 ⛔ 不得誤讀）

`常規一` 逐字（`sed -n '501,502p' CLAUDE.md`）：
> 凡四項齊備者，CC **逕行 `push`**、⛔ 不必請示：① 零生產碼變更 ② 純新增檔（⛔ 未改既有檔既有文字）③ `app.py` 期末 blob 逐位＝期望值（**絕對值出艙**）④ 開工／收工閘全綠。
> 任一項不齊 ⇒ 須請示。

🔴 **`二-1` 於零生產碼側優於 `常規一`**：零生產碼 commit ⛔ 不再受 ②③④ 之拘束——縱使含 `deletions > 0` 之**就地更正**（本批 commit `1` 之 🔧 第 `2` 項即屬之）、縱使閘未全綠，仍**逕行 push**。

🔒 **本款放寬的是「誰批准 `push`」，⛔ 不是「什麼可以改」**——`§十一-3` 之禁改清單⛔ 不因本款而鬆動一字。凡屬禁改清單者，**無論零生產碼與否，一律不得改**。

🔒 **生產碼側⛔ 不放寬**，承 `sed -n '247p' docs/reports/W-G.9-186R_CC交接文.md` 逐字：「🛑 **生產碼批⛔ 不得逕行** ⇒ **幾何本體批第一張單起須請示**。」

⚠️ **本款與 `CLAUDE.md:501-502` 之關係須入倉**：commit `1` 併於 `CLAUDE.md` **末端追加**一則「常規一之補款（`W-G.9-190R §二`）」，逐字載 `二-1`〜`二-3`。🛑 ⛔ **不得追改 `:501-502` 上文一字**（append-only）。

### 二-4　本批各 commit 之歸類（🛑 **預判**·CC 須自行實跑 `二-2` 之指令確認，⛔ 不照抄本表）

| commit | 預判 | 依據 |
|---|---|---|
| `1`　`N-1` 登記 | **零生產碼** ⇒ 逕行 push | 受詞在 `docs/`／`verify/wf_f0.py`／`verify/probes/`——⛔ 皆不在四檔內 |
| `2`　`N-2` 流程工具 | **零生產碼** ⇒ 逕行 push | 純新增 `verify/probes/`／`docs/orders/` |
| `3`　族② 切換 | **生產碼** ⇒ 請示 | 改 `app.py` |
| `4`　族③ 切換 | **生產碼** ⇒ 請示 | 改 `app.py` |
| `5`　`S-6` 四分分類器 | **零生產碼** ⇒ 逕行 push | 末端追加於 `probe_WG9189_s6_replica.py`——⛔ 不在四檔內 |

### 二-5　CC 手上之未 push commit `4251234`（`W-G.9-190` 停機報告）

`ls-remote` 實查：發單側於 `2026-08-31` 取得 `HEAD = 25857aa99ced20ff9e6abb1d519bdf7d7ab1ff5a` ⇒ **該 commit 尚未入倉**。
依 `二-1`，其為零生產碼 ⇒ **逕行 push**、⛔ 不必再請示。

---

## §三　期初錨（態 `25857aa`·🔒 發單側十檔全數獨立復現·CC 仍須自查）

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1378708` | `23412` | `2be36e4c88b6e129` |
| `CLAUDE.md` | `81200` | `903` | `3df01894ba38ccc5` |
| `verify/stepg_pipeline.py` | `96261` | `1454` | `0260273350021311` |
| `verify/probes/probe_WG9189_s6_replica.py` | `12399` | `280` | `c22b2faa38968505` |
| `docs/reports/W-G.9-189R_停機_S6複刻區實質差.md` | `14261` | `221` | `4135a6244adaf3d9` |
| `docs/reports/W-G.9-177R_二裁落地與R2-0.1171歸屬.md` | `23895` | `284` | `a52d4bb43a731bc7` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `402254` | `4347` | `82084b330ff89b4d` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `529494` | `3238` | `499ce534e43f332e` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `258295` | `3632` | `6a5cada2901a10e8` |

`sha16` ＝ `sha256` 前 `16` 碼。`app.py` 檔尾**無 `LF`**（末位元組 `)`）⇒ `wc -l` 得 `23411`、`splitlines()` 得 `23412`。

---

## §四　取號（態 `25857aa`·🔒 發單側已復現·框逐字·CC 仍須自查）

🛑 **出艙式依 `GB-125`**：一律出 `相異`／`MAX`／`缺號數` **三數本身**，⛔ 不得以「`MAX − 缺號數 == 相異`」之算術自檢為憑（該式**構造恆真**、判別力為零）。

| 類 | 母體 | 框（逐字） | 相異 | MAX | 缺號數 | 次號 |
|---|---|---|---|---|---|---|
| 自誤 | 全倉 `.md` | `` ^###\s*自誤\s*`?([0-9]{1,4})` `` | `41` | `223` | `182` | **`224`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `` ^(?:## \|### \|\| *)`GB-([0-9]{1,4})` `` | `44` | `127` | `83` | `128` |
| `VR` | `docs/驗證裁定登記表.md` | `` ^## `VR-([0-9]{1,4})` ``（🛑 扣 `999` 哨兵） | `69` | `84` | `15` | `85` |
| 戒 | 全倉 `.md` | `` ^(?:> )?🛑 \*{0,2}戒 \*{0,2}`?([0-9]{1,4})`? `` | `20` | `39` | `19` | **`40`** |

🛑 **母體之取得法（`GB-88` 族·發單側本回合親踩）**：`git ls-files` **預設 `core.quotepath=true`**，含 CJK 之檔名會被轉義為 `"docs/reports/W-G.9\346\263\242_…"` ⇒ 以其輸出直接 `open()` 者**全部開不到、靜默得 `0`**。🔴 發單側首跑即因此對「自誤」與「戒」二類得 `0`，若當場出艙即為 `自誤 224`／`225` 之同形第三度。
⇒ **一律用 `git ls-files -z` 並以 `\0` 切分**；且須併報「母體中開不到之檔數」，其須為 `0`。

🛑 戒之框**已於 `W-G.9-187R` 擴為含引言塊形**（因 `戒 16` 之定義形為 `> **戒 \`16\`**`）。
🛑 戒 `1`〜`15`／`21`〜`23` 係**倉外已鑄**，⛔ 結構上撈不到，靜態清單在 `CLAUDE.md`；另有 `9` 則**無編號之戒**，⛔ 不占號、⛔ 不得回頭給號。

---

## §五　commit 次序（🛑 ⛔ 不得併）

| # | 內容 | push 授權（依 `§二`） | 前置 |
|---|---|---|---|
| `1` | **`N-1` 登記先行**（`§八`：自誤 `224`〜`229` ＋ 四項 🔧 ＋ `CLAUDE.md` 之常規一補款） | 零生產碼 ⇒ **逕行** | — |
| `2` | **`N-2` 流程工具**（`§九`：pre-flight 機檢器 ＋ 施工單入倉之匯流排） | 零生產碼 ⇒ **逕行** | — |
| `3` | **族② 切換 ＋ 主閘 `G-1`〜`G-3`** | 生產碼 ⇒ **請示** | `1`／`2` 已 push |
| `4` | **族③ 切換 ＋ 主閘 `G-4`〜`G-5` ＋ 佐閘** | 生產碼 ⇒ **請示** | `3` 已過 |
| `5` | **`S-6` 四分分類器**（`§十`） | 零生產碼 ⇒ **逕行** | `4` 已過 |

🔒 **commit `1` 與 `2` ⛔ 不依賴切換** ⇒ 縱使 commit `3` 觸停機款，此二者**已入倉**。此係刻意排序：`§九` 之機檢器正是防止**後續發單側**重蹈 `§八` 六則之工具，⛔ 不得讓它跟著切換一起卡住。

---

## §六　受詞與主閘

### 六-1　背景（🔒 發單側已現查坐實·CC 仍須自查）

- `S-6` 證：`app.py` 之 Step G 迴圈與 `verify/stepg_pipeline.py:401+` 之人工複本，**實質差 `20` 處**；其中**第 `1` 項**即族②。
- 遞補所在之二迴圈（`app:21058`／`stepg:638`、`app:21141`／`stepg:712`）除此之外逐句相同；遞推式 `_W_prev_left = float(res.get('W_far', _W_prev_left))` 在 `app:21097` 與 `stepg:676` 逐位相同 ⇒ 污染源唯族②，且**在迴圈之外**。
- `_build_g_row`／`_solve_one` 二區**正規化後零分歧**。
- 🔒 **`CLAUDE.md:757` 之行號已腐**：其載族② 在 `:20500-20501`、族③ 在 `:20775/20777`，實為 `:20990-20991`／`:21265/:21267`（`187R` 於 `:11737` 插 `490` 列，`20500+490 = 20990`、`20775+490 = 21265` ⇒ **位移量恰為 `490`，已對上**）⇒ **一律用字樣錨、⛔ 不用行號**。

### 六-2　受詞（🛑 二處·**皆在 `main()` 內**）

🔒 `app.py` 之 `def main` 區間 ＝ **`14234`-`23409`**（發單側以 AST 現查·CC 須自查、⛔ 不得硬編）。

| 族 | 受詞（字樣錨·`grep -F`） | 現況（舊式·逐字） | 應改為 |
|---|---|---|---|
| ② | `_W_prev_left = (_left_buffer_S * _cos_dn)`<br>`_W_prev_right = (_right_buffer_S * _cos_dn)` | `_W_prev_left = (_left_buffer_S * _cos_dn) if _has_left_corner else 0.0`<br>`_W_prev_right = (_right_buffer_S * _cos_dn) if _has_right_corner else 0.0` | `_W_prev_left = 0.0`<br>`_W_prev_right = 0.0`（同 `stepg:628-629` 逐字） |
| ③ | `'b': _left_buffer_S * _cos_dn`<br>`'b': _right_buffer_S * _cos_dn` | 同左 | 同 `stepg:837-838`／`:841-844` 之新式（見 `六-5`） |

**正典依據**（`sed -n '758p' CLAUDE.md` 逐字）：`mp` 之定義由三級權威**皆作 SIDE 線之中點**；**舊式 ＝ 新式 − `W_0`**，相等**當且僅當 `W_0 = 0`**，而正典明載其**照實可負**。
（`sed -n '759p' CLAUDE.md` 逐字）：⛔ **非待裁項、⛔ 非失效**，係**已裁而未遷移** ⇒ ⛔ **不上呈 KL**。

🛑 **⛔ 不得順手搬移 `stepg` 之 `_W0_*`／`_Wfirst_*`**——彼係 harness 獨有之 telescoping 守備（`189R` 類丙），本批⛔ 不動。
🛑 `verify/stepg_pipeline.py` **全檔禁改**——本批係**令 app 對齊 harness**，⛔ 非反向。

### 六-3　🔑 族② 之主閘（🔴 **本節廢止交接文 `W-G.9-191 §六-3` 之原款**）

🩸 **原款為偽（已由發單側實測推翻）**。交接文逐字令：「切換族② 後原樣重跑 `probe_WG9189_s6_replica.py`，**實質差須由 `20` 降為 `19`**，且消失者須**恰為清單第 `1` 項**。降幅 ≠ `1` 或消失者非第 `1` 項 ⇒ 🛑 停機。」

🔴 **實測（發單側·拋棄式複本·只套族② 一項變更·`Python 3.12.3`）**：實質差 **`20` → `21`**（⛔ 非 `19`）。
逐區：`_advance_block_with_split` 由 `3` → **`4`**；逐街廓迴圈 `17` → `17`。
**成因（⛔ 非新增分歧·係差異對齊之重分段）**：基座之清單第 `1` 項係一個 `replace` 塊（app 側 `3` 句／stg 側 `8` 句）；族② 之二列與 stg 對應二列相同之後，該塊裂為
`1. insert`（stg 之 `_W0_left`／`_W0_right`／`_W0_left_set` …）＋ `2. delete`（app 之 `try:`）＋ `3. replace`
⇒ **項數 `+1`**。
⇒ 🛑 **依原款施工者，正確之實作必然停機。** 本單廢止之，並鑄 `自誤 229`（`§八`）。

**主閘（修正版·三項合取·皆機械可判）**：

- **`G-1`（源碼層·⛔ 不經 log 通道 ⇒ 無截斷風險）**
  以 AST 取 `app.py` 與 `verify/stepg_pipeline.py` 中 `_W_prev_left`／`_W_prev_right` 之**首次賦值節點**，`ast.dump` 逐位相同。⛔ 不符 ⇒ 🛑 **停機**。
- **`G-2`（判別力造·⛔ 不可省）**
  於**拋棄式複本**上還原舊式後重跑 `G-1`，須判**不同**。恆判相同 ⇒ 閘紅、⛔ 不得入倉。複本跑畢刪除並以 `git status --porcelain` 空輸出證之。
- **`G-3`（`S-6` 之受詞層·🛑 框須逐字宣告母體）**
  重跑 `probe_WG9189_s6_replica.py`（`WV_OUT_NAME` 另命名），清單中之框命中數須如下表。

**框表（🛑 二母體之框⛔ 不通用）**

| 母體 | 框（`grep -F` 固定字串） | 基座 | 族②後 |
|---|---|---|---|
| `app.py` 原始碼 | `_W_prev_left = (_left_buffer_S * _cos_dn)` | `1` | `0` |
| `app.py` 原始碼 | `_W_prev_right = (_right_buffer_S * _cos_dn)` | `1` | `0` |
| `S-6` log（`ast.unparse` **正規化去括號**） | `_W_prev_left = _left_buffer_S * _cos_dn` | `1` | `0` |
| `S-6` log（同上） | `_W_prev_right = _right_buffer_S * _cos_dn` | `1` | `0` |

🛑 **`G-3` 之限（`GB-115` 族·發單側現查）**：該探針每項每側**只印前 `3` 句**（`sed -n '219p;223p' verify/probes/probe_WG9189_s6_replica.py` 逐字 `av[:3]`／`sv[:3]`），每句截至 `130`／`150` 字元；基座之 log 有 **`8` 個截斷標記、隱藏 `25` 句**，族②後為 **`8` 個、`23` 句**。
⇒ **字樣計數只在該字樣落於每項前 `3` 句時可信**。族② 恰落第 `1` 項第 `1` 句（發單側於**基座與族②後二態**皆已現查）⇒ 本框可用。
🛑 **⛔ 不得把同法套用於族③**——見 `六-5`。

**出艙記錄（🛑 ⛔ **非**停機款）**：`S-6` 之實質差**總處數**。發單側實測 基座 `20` → 族②後 `21`。
🛑 該數⛔ 不得作為任何判準：差異對齊之重分段使其**⛔ 非單調**，且其變動方向與「分歧變多」**同形而異義**。

### 六-4　🔑 三量之方向（佐閘·🛑 `自誤 227` 之受詞·⛔ 不得再倒置）

**權威來源 ＝ `sed -n '188,194p' docs/reports/W-G.9-177R_二裁落地與R2-0.1171歸屬.md`**
（⛔ **非** `CLAUDE.md:760` 之箭頭——發單側已逐字覆核，該處記的是 **shim 之注入方向**，⛔ 非 app 之切換方向）。

該表逐字（發單側以 `sed` 抽出）：欄序為「shim 原 ｜ **注入族② 後** ｜ app（【倉】`[T2-DIAG]`）」。

| 量 | app（舊式·現行） | shim（新式·目標） | **切換後應為** |
|---|---|---|---|
| 池帶2 `s` 寬 | `32.9639559294`（顯示 `32.9640`） | `33.0810700422`（顯示 `33.0811`） | **`32.9640 → 33.0811`** |
| 退化帶數 | `3` | `2` | **`3 → 2`** |
| `merged` 段數 | `4` | `3` | **`4 → 3`** |

🔴 **判準之性質**（`sed -n '761p' CLAUDE.md` 逐字）：**預測·⛔ 非結論**。
三量**同時收斂** ⇒ ✅；**未收斂**（含部分收斂）⇒ 🛑 **停機上呈**，⛔ 不得自判為切換失敗、⛔ 不得回退——未收斂之意義是**分歧之模型不完整**，須由發單側裁。

🔒 **佐閘之限（明載·⛔ 不得省）**：本量係 **harness 內之注入式模擬**、⛔ **非 app 路徑實跑**。
**殘餘風險**：app 路徑（`main()`）從未被任何自動測試執行（`sed -n '58p' CLAUDE.md` 逐字「**`main()` 內之敘述從不被 `run_all` 執行**」）⇒ 歸 `GB-123`，本批**⛔ 不解除、明載之**。唯一真量 app 路徑者係 KL 之 UI 實跑（前例：【倉】`verify/out/KL_UI_3.5m_2e08a41_stdout.log`）——**本批⛔ 未授權、⛔ 不辦**。

### 六-5　族③ 之受詞與主閘

**stepg 之新式逐字**（`sed -n '837,838p;841,844p' verify/stepg_pipeline.py`）：

```
_b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block) if _fo_left else 0.0
_b_R0 = _mp_base_W0(_end_pt_o, _right_buffer_S, _dhr_o, _side_mid_right, allocation_dir_block) if _fo_right else 0.0
```
其 `'b'` 槽取 `_b_L0`／`_b_R0`。

🔒 **`_mp_base_W0` 之身分（發單側現查·使本單自足）**：`verify/stepg_pipeline.py:812` 之區域閉包，其註解逐字自陳「改為**純委派**至 app 之單一產生者 `k956_W_from_mp`」。
`app.py` 全檔 `_mp_base_W0` 命中 `2` 處（`:9116`／`:9123`）——**二者皆在註解／docstring 內、⛔ 非可呼叫之物**。
⇒ app 側之切換須經 **`k956_W_from_mp`**（`grep -n "def k956_W_from_mp" app.py`），⛔ **不得於 `app.py` 另寫第二份 `W` 定義**（`GB-48` 族·`app.py:9111-9112` 逐字明令）。
🔒 現查 app 側所需之名皆存在：`_fo_left` 命中 `3`、`_side_mid_left` 命中 `6`。

**主閘**：

- **`G-4`（源碼層·🛑 直接對 `app.py` 檔案 `grep -F`·⛔ 不經 `S-6` 之 log 通道）**

| 框（`grep -F`） | 基座（發單側實測） | 族③後 |
|---|---|---|
| `'b': _left_buffer_S * _cos_dn` | `1` | `0` |
| `'b': _right_buffer_S * _cos_dn` | `1` | `0` |

  且新式之錨字樣（`k956_W_from_mp` 於 `main()` 內之呼叫）命中 `≥ 1`。
- **`G-5`（判別力造·⛔ 不可省）**：於複本上還原舊式後重跑 `G-4`，二框須各得 `1`。恆得 `0` ⇒ 閘紅。

🛑 **⛔ 不得以 `S-6` 之清單為族③ 之閘**——發單側已現查：族③ 之二字樣於**基座與族②後之 log 中命中皆為 `0`**，而其於 `app.py` 原始碼中命中為 `1`／`1` ⇒ 🔴 **該 `0` 係 `GB-115` 之顯示截斷所致，⛔ 非「受詞不存在」**。以其立閘者將**恆綠**。

**族③ 之預測**：`R2` 三量**⛔ 不再變動**（`CLAUDE.md:760` 載其於 `R2` 實測零後果，且逐字警示 ⛔ 不可外推至他街廓／他案）。

---

## §七　`[T2-DIAG]` 之射程限（🛑 出艙時須併記）

🔒 `GB-118` 逐字之實測：`run_all` 之主幹於 `②-宗 圍堵閘破[R2]` 中止 ⇒ `T2-DIAG` **僅有 `R1`／`R2`**，`R4`／`R5`／`R6` **一筆皆無**。
⇒ 凡本批出艙「`[T2-DIAG]` 未見他街廓變動」者，須**同格**併記「本趟之 `T2-DIAG` 只涵蓋 `R1`／`R2`」。⛔ 不得寫成「他街廓未變動」。

---

## §八　commit `1` 之內容（`N-1` 登記·零生產碼）

### 八-1　自誤 `224`〜`229`（🛑 **六則**·⛔ 非五則）

🛑 **標題形依 `常規四（九）四`：取既有最末一則（`自誤 223`）之標題形逐字為範本**，⛔ 不憑本單之寫法。次號現查 ＝ `224`。

| 號 | 受詞（逐字） | 案由 |
|---|---|---|
| `224` | 「已在手之檔只讀其一部分，即斷言其全檔查無。」 | 裁 `A` 斷言「CC 所引在倉內查無」，實在 `docs/reports/W-G.9-186R_CC交接文.md:156`／`:245`／`:246`；該檔正係前窗自己發現並回報之新增檔，僅讀前 `90` 列即出艙全稱否定。 |
| `225` | 「以裸框（無 `^` 錨）之得數為總數出艙，並據以否定受單側之正數。」 | 報 `[T3-GATE]` 逐宗列 `57`（裸框），錨定框 `` ^\[T3-GATE\] 街廓 `` 得 `39`，差 `18` ＝ 以 `    ▸ ` 起首、截至 `150` 字元之摘錄重列。同族：`VR` 裸框 `999`／`自誤` 裸框 `298`（`CLAUDE.md:671`）⇒ **裸框族第三度**，觸 `戒 32` 升階款與 `CLAUDE.md:725`。 |
| `226` | 「已具該事實並曾出艙告誡，於自己之設計中未用之。」 | `W-G.9-190` 之主閘 `[T5-FAM2]` 架於 `run_step_g`，而受詞 `app.py:20990` 巢於 `main()`（`14234-23409`），`CLAUDE.md:58` 逐字「`main()` 內之敘述從不被 `run_all` 執行」——該斷裂係前窗自己於 `W-G.9-188` 指認並寫入 `189 §六` 者。 |
| `227` | 「逐字轉引一方向性記述而未檢其座標系。」 | `190` 載三量方向 `2→3`／`3→4`，係逐字轉引 `CLAUDE.md:760`；然該處箭頭記的是**注入方向**（app 舊式注入 shim）。`177R §四-4` 表 `:190-192` 明載 app ＝ `3`／`4`、shim ＝ `2`／`3` ⇒ app 切換為新式應為 **`3→2`／`4→3`**。 |
| `228` | 「宣稱已產出某交付物並附其量，而該交付物⛔ 不存在。」 | 前窗於聊天逐字稱「`W-G.9-190R` 已重發（`13801` B／`199` 列·`SELF_SHA256` 已自算對拍相符）」，實則**⛔ 未建任何檔**，其 bytes／列數／對拍結論**全係虛構**。🔴 此則與 `224`〜`227` **⛔ 非同族**：前四者係量測不足，本則係**宣稱未為之行為**。CC 結構上看不到發單側之檔案產出 ⇒ **⛔ 不在既有交叉檢查之射程內**，係換窗之直接事由。 |
| `229` 🆕 | 「把一個**未經實測之預測**寫成**機械停機款**，且該預測經實測為偽。」 | `W-G.9-191` 交接文 `§六-3` 令「族② 後 `S-6` 實質差須由 `20` 降為 `19`，降幅 ≠ `1` ⇒ 停機」。接手窗於拋棄式複本實測得 **`21`**（成因＝差異對齊之重分段·⛔ 非新增分歧）⇒ **正確之實作必然停機**。🔴 與 `227` **同族而更深**：`227` 係轉引未轉向，本則係**自造之判準未經實跑即定為停機款**；且交接文於 `§六-4` 已知標明三量係「預測·⛔ 非結論」，卻於 `§六-3` 對另一數**未作同樣分層** ⇒ **同一份文件內之模態強度不一致**。 |

🔒 **併記為正辦**：受單側 CC 於前窗**三度**在前提為偽時拒絕照裁改倉、改以現查回報（裁 `A` 之引據／裁 `三` 之 `57`／`W-G.9-190` 之二問），皆逐項具名。此係受單側應有之樣，須記明。

### 八-2　四項 🔧（皆⛔ 不鑄號）

1. `docs/reports/W-G.9-187R_幾何本體批第一批.md` 之 `:368`／`:485`：**維持 `39`**，補宣告框 `` ^\[T3-GATE\] 街廓 ``，依 `CLAUDE.md:725` 加註「裸框 `57`／錨定框 `39`，差 `18` ＝ 摘錄重列」。（發單側已逐字覆核該二列：`:368` ＝ `| \`6\` | \`[T3-GATE]\` 逐街廓逐宗一列 | ✅ \`39\` 列（\`probe_WG9187_t3gate.log\`）·四態出艙碼 |`；`:485` ＝ `| \`verify/out/probe_WG9187_t3gate.log\` | 新檔 | — | \`[T3-GATE]\` \`39\` 列 |`）
2. `verify/wf_f0.py:9` 逐字「解算一律走 app 真函式 run_step_g（F.0 僅為 Step G 前之 parcels 資料變換）。」係**偽述** ⇒ 就地更正。
   🔒 **證**（發單側現查·母體 ＝ `git ls-files -z` 之全部追蹤檔）：`^def run_step_g` 全倉命中 **`1`** 處 ＝ `verify/stepg_pipeline.py:238` ⇒ 其⛔ 非 `app` 之函式。
   🛑 本項含 `deletions > 0`；依 `§二-3`，仍屬零生產碼 ⇒ **逕行 push**。
3. `CLAUDE.md:757` 行號已腐 ⇒ **末端追加字樣錨** `` `_W_prev_left = (_left_buffer_S * _cos_dn)` ``、`` `'b': _left_buffer_S * _cos_dn` ``，⛔ 不用行號、⛔ 不追改上文一字；**併記 `CLAUDE.md:760` 之箭頭係注入方向**，使後續轉引⛔ 不再倒置。
4. `probe_WG9189_s6_replica.py` 之 `ast.dump` bytes 出艙列加載 `platform.python_version()`，並於報告載明其⛔ 非跨環境錨。🛑 本項屬**末端追加**、⛔ 不改既有判定路徑。

### 八-3　`CLAUDE.md` 之常規一補款

依 `§二-3` 末段，於 `CLAUDE.md` **末端追加**，逐字載 `§二-1`〜`§二-3`。⛔ 不追改 `:501-502` 上文一字。

---

## §九　commit `2` 之內容（`N-2` 流程工具·零生產碼）

> 事由：`§八` 六則自誤全出自發單側。CC 之交叉檢查擋下四則，`228` **⛔ 不在其射程內**，`229` 由接手窗自查而得。
> 本節之目的是把「靠發單側自律」改成「受單側收單時機檢」。

### （甲）施工單 pre-flight 機檢器

新建 `verify/probes/probe_order_preflight.py`，輸入 ＝ 一張施工單之 `.md` 路徑，輸出 ＝ 逐項判與 `rc`。

| 檢 | 受詞 | 判法 | 性質 |
|---|---|---|---|
| `P-1` | 「查無」「不存在」「無任何」之**全稱否定** | 同段內須具名**母體**（檔名 ＋ 總列數或 bytes）與**所查範圍**；範圍 ≠ 全檔而出全稱否定 ⇒ 提示 | 🟡 提示 |
| `P-2` | 出艙之**計數** | 須同段載其**框逐字**；若框無 `^` 錨，須依 `CLAUDE.md:725` 二數並報 ⇒ 否則提示 | 🟡 提示 |
| `P-3` | **主閘之受詞** | 抽單內 `檔:列` 引用，AST 判該列是否落於 `app.py` 之 `def main`（🛑 須實查、⛔ 不得硬編）。落於其中而該單又令以 `run_step_g`／`run_all` 量之 ⇒ 🛑 **紅** | 🔴 機械 |
| `P-4` | **方向性轉引**（含 `→` 或「A 至 B」且鄰近有「逐字」「轉引」「引」者） | 須同段具名其**座標系**（來源檔:列 ＋ 該箭頭之起點為何）⇒ 否則提示 | 🟡 提示 |
| `P-5` | 單之 `SELF_SHA256` | 取檔內**最末**一個，依 `§一 S-0` 之口徑重算並對拍 ⇒ 不符 🛑 **紅** | 🔴 機械 |
| `P-6` 🆕 | **停機款所繫之數**是否經實測 | 凡一段文字**同時**含停機字樣（`停機`／`🛑`）與一個**期望數值之變化**（`由 N 降為 M`／`N → M`／`須為 N`），該段須同格具名其**實測出處**（log 檔名或「本單發單側實測」＋ 環境）⇒ 否則 🟡 提示 | 🟡 提示 |

🛑 **性質⛔ 不得混同**：`P-3`／`P-5` 係機械可判 ⇒ 列為開工閘之停機款。
`P-1`／`P-2`／`P-4`／`P-6` 係**文字啟發式、必有偽陽** ⇒ **⛔ 不得列為停機款**；其處置為「逐項具名，採納或具名豁免」，⛔ 不得靜默略過。恆綠 ⇒ 檢查器紅。

🔑 **判別力造（⛔ 不可省）**：以 `docs/orders/` 內既有之 `W-G.9-190`（前窗所出、已知含 `P-3` 與 `P-4` 之缺）為靶——
`P-3` 須紅（主閘 `[T5-FAM2]` 架於 `run_step_g` 而受詞 `app.py:20990` 在 `main()` 內）、
`P-4` 須提示（三量方向 `2→3`／`3→4` 未具名座標系）。
另以 **`W-G.9-191` 交接文之 `§六-3`** 為 `P-6` 之靶：其「實質差須由 `20` 降為 `19`」**未具名實測出處** ⇒ `P-6` 須提示。
三者任一不觸 ⇒ 檢查器紅、⛔ 不得入倉。

⇒ 落地後，**新單之開工閘增列 `S-0b`**：跑 `probe_order_preflight.py`，`P-3`／`P-5` 紅 ⇒ 🛑 停機退單。

### （乙）施工單入倉之匯流排

1. 新建目錄 `docs/orders/`，施工單以原名入倉（本單 ＝ `docs/orders/W-G.9-190R_族二三切換.md`）。
2. 併入倉**前窗已出之三檔**作為 `P-3`／`P-4`／`P-6` 之靶與後續之對照：`W-G.9-189`／`W-G.9-190`（🛑 **已知有缺·⛔ 不得執行**，須於檔首加註 `⛔ 作廢——見 W-G.9-190R`）／`W-G.9-191`（交接文）。
3. 於 `CLAUDE.md` **末端追加**一節載此約定：**施工單與報告皆以倉為傳遞媒介**——CC 自倉讀單、報告 push 入倉、發單側自倉讀報告。⛔ 不追改上文一字。

🔒 **效果**：KL 之人工傳遞由「貼兩大段文字」降為「把發單側產出之檔案放進 `docs/orders/` 並 commit 一次」。發單側⛔ 不再需要 KL 轉貼 CC 之回報——自行 clone 即得。

🛑 **⛔ 不得據此把整條鏈接成無人流水線。** 本波六則自誤中，四則由 CC 攔下、一則（`228`）由 KL 攔下、一則（`229`）由接手窗自查攔下；**人之攔截是這條鏈唯一實測有效之守備**。

### （丙）KL 側之一條規則（⛔ 非工程項·併記於此）

🔴 凡發單側於聊天稱「**已產出／已建檔／已對拍**」某檔案，KL 須**見其檔案卡**方採信；⛔ 無卡即視為未產出。
此係 `自誤 228` 之唯一有效攔截——`P-1`〜`P-6` 皆檢**已存在之單**，⛔ 檢不到「單根本沒被產出」。

---

## §十　commit `5`：`S-6` 四分分類器（`W-G.9-188` 裁）

於 `probe_WG9189_s6_replica.py` **末端追加**（⛔ 不改既有判定路徑）：

| 類 | 判準（🛑 機檢可判·⛔ 不得逐案裁量） | 處置 |
|---|---|---|
| **乙** 宿主介面差 | **封閉白名單**：`st.*`／`ss[...]`／`raise RuntimeError` ↔ `st.error`+`st.stop`。名單外之任一識別符 ⇒ **歸甲** | 略去 |
| **丙** 單側獨有守備 | AST **def-use** 機檢：獨有碼若**寫回**任一為共用流程所消費之變數 ⇒ **歸甲** | 略去 |
| **丁** 呼叫序差 | 僅**無裝飾器**之 `FunctionDef`／`ClassDef` 重排得豁免；其餘 ⇒ **歸甲** | 略去 |
| **甲** 其餘 | — | 🛑 停機款 |

🔑 **判別力造（⛔ 不可省）**：於白名單區內注入一已知實質差，須**仍判甲**。恆判乙丙 ⇒ 分類器紅、⛔ 不得入倉。

**期末預期（🛑 預測·⛔ 非停機款）**：切換後之清單經四分，類甲數應為 `0`。
🛑 ⛔ **不得以「處數應為 N」立閘**——`六-3` 已實測該數受差異對齊重分段影響而⛔ 非單調（`自誤 229` 之案由）。
仍有類甲 ⇒ **逐項具名呈裁**，⛔ 不自行豁免、⛔ 不改白名單以求綠。
🛑 `stg` 之 `_W0_*`／`_Wfirst_*` 於族② 切換後將成為一個**獨立之 `insert` 項** ⇒ 其應歸 **類丙**（harness 獨有之 telescoping 守備·`189R` 已如此判）；若機檢判其為甲，**逐項具名呈裁**、⛔ 不得逕改分類器。

---

## §十一　停機款／禁改／報告

### 十一-1　停機款（🛑 逐項·⛔ 本節以外之數皆為記錄）

1. `S-6` **開工復現不符**（四區判定／實質差 `20`／判別力二問皆 `False`／`rc=1`；⛔ 不以 dump bytes 判紅）。
2. 族② 後 **`G-1` 不符**（二賦值節點 `ast.dump` 不逐位相同）。
3. **`G-2` 恆判相同**（判別力造未過）。
4. 族② 後 **`G-3` 之四框未全數由 `1` 降為 `0`**。
5. 族③ 後 **`G-4` 二框未由 `1` 降為 `0`**，或 **`G-5` 恆得 `0`**。
6. `R2` 三量**未同時收斂**（含部分收斂）。
7. 族③ 後三量**竟再變動**。
8. `[T2-DIAG]` 出現**非 `R2` 街廓**之變動（🛑 併記 `§七` 之射程限）。
9. `run_all` **由綠轉紅**且經實測基座態確認係本批所致。
10. 四分分類器**判別力造未判甲**。
11. **`deletions` 出現於基座檔**（＝ `§十一-3` 禁改清單所列之檔）。
12. 任一次號之**錨定框**得數 ≠ `0`。

🛑 **⛔ 不再以「`S-6` 實質差總處數」為停機款**（原交接文之款已由 `六-3` 廢止）。

### 十一-2　🛑 停機時之處置

停機報告**逕行 push**（依 `§二-1`：停機報告係零生產碼）。報告須逐字引錄**所觸之停機款原文**（⛔ 非摘要、⛔ 非改寫），並標其來源管線——`GB-121` 之施行式。

### 十一-3　禁改（⛔ 無論零生產碼與否，一律不得改）

- `verify/run_all.py`／`run_verification.py` 之**發現機制與名目集合**
- `docs/rulings/K-6_*.md` **既有條文**
- `verify/stepg_pipeline.py` **全檔**
- `187R` 已落地之 `_rect_fit_*`／`_k923_*`／`_blue_shadow_tri`／`_t3_*` 之**函式體**
- `probe_WG9189_s6_replica.py` **既有判定路徑**（末端追加除外）
- `app.py` 之 `minimum_rotated_rectangle` **全部 `11` 處**（🔒 發單側現查 ＝ `11`）
- 任何既有 `GB`／`VR`／`自誤`／`戒` 之**既有文字**（僅得末端追加）

### 十一-4　報告

`docs/reports/W-G.9-190R_族二三切換.md`。
🔒 收工時須併報：`§二-2` 之判法指令**逐 commit 之輸出**、`§一 S-3` 之 `git status --porcelain` 二態、`§六-3` 之框表四數（自測值）、`§六-5` 之框表二數（自測值）。

---

## §十二　⚠️ 六個具體陷阱（前窗與本窗踩過·逐一具名）

1. **只讀部分即斷言全檔**（`224`）——凡出「查無」，須逐字載其**母體**（檔名 ＋ 總列數）與**所查範圍**。
2. **裸框當總數**（`225`）——凡計數，`^` 錨與裸框**二數並跑**；相異則二數並報並具名其差之來源。
3. **主閘架在不存在之通道**（`226`）——出單前逐項自問：**受詞是否在 `run_all` 之執行路徑上？** `main()` 內者一律**否**。
4. **轉引未轉向**（`227`）——凡轉引含箭頭之記述，須先確定其**座標系**。`CLAUDE.md:760` 之箭頭係注入方向，**⛔ 非切換方向**。
5. **宣稱未為之行為**（`228`）——凡稱「已產出／已建檔／已對拍」，須**實際執行工具並以其回傳為憑**。🔴 此類**不在 CC 之射程內**。
6. **把預測寫成停機款**（`229` 🆕）——凡一個數要當停機款，**先在拋棄式複本上把它跑出來**。跑不出來者，只能寫成「預測·未收斂則停機上呈由發單側裁」，⛔ 不得寫成「≠ N 即停機」。

---

SELF_SHA256: 28fad809f2063941205ec01dc67274ef2552cd5e2ecefaa4521e5573e3b1dcd8
