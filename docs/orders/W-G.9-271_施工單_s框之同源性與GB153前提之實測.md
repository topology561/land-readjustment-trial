# W-G.9-271　施工單（**零生產碼·純量測**：`GB-153` 前提之實測／`s` 框之同源性／`R6` 起點端／步驟 D 分母／`自誤 375` 之鑄）

> **受詞** ＝ `W-G.9-271` 之現施工窗。
> **主線** ＝ `c168e993e73cb6775ebf72b46e4337ca812967ca` **或其後裔**（🛑 一律 `git ls-remote` 實查·本地追蹤 ref ⛔ 為證）。
> **凍存** `verify/W-G.9-269-p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
> **此單此前是否已交付** ＝ **否**（首次交付）。
> **分級** ＝ **中**（含一次實跑·**零生產碼**）。
> 🔒 **本批零生產碼** ⇒ ⛔ 動生產碼 `34` 檔（`app.py` ＋ `verify/*.py` **單層** `33`）一字。

---

## §零　開工閘（**⛔ 略**·全綠方得開工；任一項紅 ⇒ 停機回報）

| # | 受詞 | 期值 |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `c168e99…` 或其後裔 |
| `2` | `git rev-parse HEAD:app.py` | **`4379108a4856714078c410ff4e6291ecfdf6d2c1`**（⛔ 相符 ⇒ 停機） |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須 **非祖先** |
| `4` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py <本單之 .md 路徑>` | `P-3`／`P-5` 紅 ⇒ **停機款**；`P-1`／`P-2`／`P-4`／`P-6` **逐項具名採納或具名豁免**（⛔ 靜默略過·恆綠即器紅） |
| `5` | **環境自證**：逐一印 `ezdxf`／`shapely`／`streamlit`／`pandas`／`numpy`／`pyproj` 之**在否與版本** | ⛔ 以 `try/except` 靜默吞之；缺者逐一具名 |

🛑 `git` pathspec 一律以 `/` 組（⛔ `os.path.join` 於 Windows 產生 `\`）；`subprocess` 一律 `encoding="utf-8", errors="replace"`（⛔ `text=True` 之 `cp950`）。
🛑 ⛔ `python3`（本機 `rc=49`）⇒ 用 `python`；**⛔ heredoc**（硬閘攔）⇒ 以 `Write`／`Edit` 落腳本。
🛑 **⛔ 以 `tail -N` 取長跑之結果**（坑 `av`）——落全量檔，再以**逐字錨** `grep`。

---

## §零′　發單側之復驗與核可（承前窗·**已辦畢**）

補令七全批**主檢全綠**（發單側自倉自跑·rev `c168e99`）：四 commit 鏈相符；`app.py` blob 期初＝期末 `4379108a…`；`5a14dc2..c168e99` 對生產碼母體之 `numstat` ＝ **空**（判別力二造：必真 `dc373d0→17fd981` ＝ `26／42`；必偽 自比 ＝ 空）；`baselines` `298`／`a898f4e1…`；四簿⛔ 動；坑 `78`／`78`（器非紅）；`Σa` 落檔之自我驗證閘二情境皆「不符之項 ＝ `0`」。
**你之第七、八、九個自捕皆為真**；第九則（同名而異物之跨層比對）**核可**，且其為「先對齊框／受詞再判」之典例。

---

## §一　🩸 `自誤 375` 之鑄（**責 ＝ 發單側**·逐字轉錄·**⛔ 改寫**）

> **形**：**以「一片落在二已配之宗之間」為「中間穿插」之判準，而未先驗該片與各宗之區間<u>是否分離</u>——與宗<u>重疊</u>之片遂被誤計為缺口。**
>
> **本案**：發單側據 `verify/out/WG9270R4_p6_interleave.log` 呈 KL 一表，稱六街廓中有五街廓之配餘地「被已分配的土地隔成兩塊」並逐格列出被隔開之宗。其後重算：`12` 格中之池片，與配地宗**重疊**者 `8` 片（其中 `6` 片重疊長 ＝ 本片 `s` 長之 **`100%`**）；真正**分離**之片，每街廓恰一處中間片，其餘皆在兩端。⇒ 原呈報之表**作廢**，其域裁題**撤回**。
>
> **通則**
> > **凡以區間之相對位置為判準者，須先逐片出艙其與受比母體之<u>重疊長</u>（＝ `0` 方為分離），⛔ 以「夾擠」代「分離」。** ⇒ 承 `自誤 353`：判準之受詞須與其所欲證之性質（此處 ＝「街廓中段存在**未配**之地」）**逐字對位**，⛔ 以相關述詞代 operative 述詞。

🔒 **鑄之授權** ＝ `§四`。**節題之形須與既有四則同構**（`### 🩸 \`自誤 375\`　**…**`）——⛔ 自創形，否則四簿之框不命中 ＝ 靜默漏。
🔒 **取號現查**（**二形並取**·母體 ＝ HEAD 之追蹤 `.md` 全檔·⛔ 預寫命中數）：
出艙 `自誤 375` 鑄前之命中（檔／列），並**逐筆歸類**其是否為「本單自身之授權條款」（`裁 H`）；
**對照組**：甲 `自誤 374`（已鑄·須 `>0`）／甲2 `自誤 373`（已鑄·須 `>0`）／乙 一人造哨兵（**執行期組出**·字面⛔ 出艙·`GB-147`）須 `0`／`0` ⇒ **器非紅**。

---

## §二　工項（**零生產碼**）

> **序**：工項 `1` **背景啟動**後，同時辦 `2`〜`6`；`7` 為收工閘。

| 序 | 事 |
|---|---|
| `0` | **本單原封入倉** `docs/orders/`（依 `§二-B` 搬移法：**python 內以完整檔名逐字指定**，搬移**前**出艙「所指之檔數 ＝ `1`」之自證·⛔ 為 `1` 即 **loud 停**；⛔ 全 ASCII glob、⛔ 差集法）。往返逐位保存之二條款照辦 |
| `1` | **`GB-153` 前提之實測**（`二-1`） |
| `2` | **`s` 框之同源性**（`二-2`） |
| `3` | **`R6` 起點端 `85.71 ㎡` 之成因**（`二-3`） |
| `4` | **步驟 D 分母之二態量**（`二-4`） |
| `5` | **`wf_f0` `a′` 已落地之登記**（`二-5`） |
| `6` | **`自誤 375` 之鑄**（`§一`） |
| `7` | **收工閘**（`§三`）⇒ **停** |

### 二-1　`GB-153` 前提之實測（**出艙即止**）

**背景**：`GB-153` 逐字之前提 ＝ 「`StepG` 於 `R2` 中止 ⇒ ① `F.0`–`F.4` 六族 CSV 全不可得；② `c2` 之 `V-H`（`R4` 左街角之 A／B 差異表）亦不可得——`R4` 在 `R2` 之後」。病一（`W-G.9-269` `c1`／`c2`／`c3`）已併線（發單側實查：`dc373d0`、`17fd981` 皆為 `c168e99` 之祖先）⇒ **該前提是否仍成立，須一次實跑坐實**。

**辦法**：`verify/run_verification.py` 實跑一次，落**全量**檔 `verify/out/WG9271_fullrun.log`。出艙：

1. `RESULT` 與**紅項數**；
2. **逐街廓 `R1`→`R6` 之到達與否**（以該檔內之逐街廓出艙列為據·**同格具名其逐字錨**·⛔ 行號）；
3. `F.0`–`F.4` 六族 CSV 之**可得性**：逐檔具名其**完整路徑**與**列數**；並與 `GB-153` 案由所載之 `got_G值_退縮0m_partial.csv`／`got_G值_退縮3.5m_partial.csv`（`21`／`24` 列）**照實並列**；
4. `R4` 左街角之量是否可得（`c2` 之 `V-H` 之前提）。

**判別力**：本項係存在性量測 ⇒ 對照 ＝ 一**必命中**之既有落檔（`verify/out/` 內任一已知檔）之列數須 `>0`；對照全零 ⇒ **先判量測器紅**。
🛑 **遇求值失敗之態**：照實具名其 rev／失敗之**逐字**／可歸屬之既有號；略過之；**⛔ 視為紅、⛔ 視為綠、⛔ 與「判定為偽」共用出艙碼**——其出艙碼須為**第三種**。
🛑 **⛔ 判 `GB-153` 已失效／應解除／應收窄**——**出艙即止**，其裁由發單側為之。
🛑 `GB-162` 三禁令維持：⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數**。

### 二-2　`s` 框之同源性（**本批之主檢**）

**器** ＝ 新寫 `verify/probes/probe_WG9271_s_frame.py`，**以 `verify/probes/probe_WG9270R4_p6_interleave.py` 之 `drive_spy` 為底本**（⛔ 另寫第二份判準、⛔ 器內重導切線座標）。

逐情境（`0m`／`3.5m`）× 逐街廓，出艙**三組對拍**：

| 組 | 池側 | 宗側 |
|---|---|---|
| **甲** | 間諜所錄之**回傳** pieces | **同一次呼叫所錄之 `biz_polys`** |
| **乙** | 同上 | `g_rows` 之 `cut_coords`（＝ `p6` 之取法） |
| **丙** | — | **同宗之二形**（`biz_polys` vs `cut_coords`）逐宗對拍其 `s` 區間之差 |

**準據（發單側定·⛔ CC 自訂·`自解款` 黑名單）**：

- 重疊長 ＝ `max(0, min(b, y) − max(a, x))`；容差**取 `app.py` 之 `_S_EPS`**（自 ns 取值·**⛔ 器內另定常數**）。
- **組甲之逐片重疊長須全為 `0`**（＝ 池片確為 `biz_polys` 之 `s` 區間聯集之補區間·`_pool_strips_for_block` 步驟 `4` 逐字）。
- 組甲⛔ 全為 `0` ⇒ **逐片具名**其重疊長與重疊之宗，**⛔ 判其孰誤**。
- 併出艙：間諜所錄之**呼叫次數**（逐街廓·逐情境）與每次之 `_label`；一街廓一情境被呼叫 `>1` 次者，**逐次分列**其 `corner_pt`／`d_hat`／`allocation_dir` 與回傳片數。

**判別力二造**：甲[必重疊] 取任一宗之區間與**其自身**對拍 ⇒ 重疊長 ＝ 其長（`>0`）；乙[必不重疊] 一人造區間（**執行期組出**·字面⛔ 出艙）置於 `s` 域之外 ⇒ `0`。⇒ **二造異色方為器非紅**。

🛑 **⛔ 判 `p6` 之結論作廢、⛔ 判「交錯」之有無**——**出艙即止**。

### 二-3　`R6` 起點端 `85.71 ㎡` 之成因（**出艙即止**）

逐情境（`0m`／`3.5m`）出艙 `R6` 之：

1. `s` 域 ＝ `ns["_strip_s_range"](block_poly, d_hat, corner_pt, allocation_dir)`（**取間諜所錄之同一次實參**）之 `(s_min, s_max)`；
2. 鏈頭第一宗（`s` 起最小之配地宗）之**暫編地號**與其 `s` 起；
3. `2` − `1` 之差（**未捨入值**·落檔 2dp 僅為顯示）；
4. 該片（`R6-池#2`）之 `s` 區間與面積。

🔒 **照實並列**已登記之錨移二值 **`+3.39 m`**（`0m·R2·left`）／**`+3.32 m`**（`3.5m·R5·left`）——🛑 **⛔ 判其為同一量**（先對齊母體／受詞再判）。
**判別力二造**（施於量測式本身）：甲[必非零] `s_max − s_min` ⇒ `>0`；乙[必為零] 同一 `block_poly` 自比之 `s_min` 差 ⇒ `0`。
🛑 **⛔ 判其為缺陷、⛔ 提任何處置主張**——候發單側與 KL。

### 二-4　步驟 D 分母之二態量（**⛔ 動任何碼**）

**受詞之逐字錨**（`app.py`·⛔ 行號）：

```
all_original_parcels = sorted({tp['原地號'] for tp in temp_parcels
```

**共用性之逐字根據**（`app.py` 註解）：「本函式係 app `main()` 與 harness `verify/selection_pipeline.build_build_parcels` **共用之最後一道 app 側轉換**」。

**辦法**：以 harness 之 `build_build_parcels` 取 `temp_parcels`，二態各量 `{tp['原地號'] for tp if not _is_ghost_sliver}` 之**大小**與**成員**：

| 態 | 環境 |
|---|---|
| 甲 | `WV_K6_STEP0` **未設**（＝預設 `on`·**現行生產態**） |
| 乙 | `WV_K6_STEP0=off` |

出艙：二態之大小、**差集之逐筆原地號**、`snap["財務接線_v3"]["原地號_區段"]` 之筆數（第三方對照），以及步驟 0 之 `_diag` 四欄 `parcels_in`／`parcels_absorbed`／`parcels_out`／`groups_merged`。
**準據**：二態之大小須**相異**；⛔ 相異 ⇒ **loud 停**（`rc ≠ 0`），⛔ 靜默續。
🛑 本項**僅以環境變數驅動既有出口**——**⛔ 落地 `K-9-29` 任何子項、⛔ 動 `k6_step0_enabled` 之預設一字**。

### 二-5　`wf_f0` `a′` 已落地之登記（**末端追加·⛔ 鑄號**）

於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（**⛔ 節末**）純末端追加一節，題逐字：

```
## 🔧 「已裁未實作之逐款對位表」之更正：`K-9-29` 子項 `2`（`wf_f0` `a′`）**已落地**（`W-G.9-271` 工項五·⛔ 上文一字不刪·純末端追加）
```

其內須載：

- 🛑 **本節⛔ 鑄任何號**（`戒 36`）；🔒 **⛔ 改上文任一字**。
- **受詞之逐字錨**（⛔ 行號）＝ `## 🔧 **已裁未實作之逐款對位表**（`W-G.9-265` 工項七·⛔ 上文一字不刪·純末端追加）`
- **證**（**CC 當場復現·逐字**）：`verify/wf_f0.py` 之 `add += a_m * (p_of(m) / p_t)`（含 `p ≤ 0 ⇒ raise` 與「先除後乘·括號不得省」之硬性款）；`停機#2` 全檔之命中數與其所在（**先實測後填**）。
- **判別力**：寬字樣之命中數同格並報，證該檢⛔ 恆為 `1`。
- 🛑 **⛔ 及於子項 `1`**（`temp_parcels, _diag_k6 = k6_step0_merge(` 命中 `1`·**仍在**）、**⛔ 及於子項 `3`／`4`**。

🛑 **⛔ 在片段中預寫命中數**——一律**先實測後填**。

---

## §三　收工閘（**commit 前·與被守護之動作分開呼叫**）

1. `numstat` **刪除欄**逐檔具名；非零者逐檔具名其由。
2. **生產碼判法四檔**命中 `0` 行 ⇒ **零生產碼 commit** ⇒ 逕行 push（`常規一` 補款 `🔧 一`）。
3. 新增／異動之 `.md`／`.py` 之 `CR` ＝ `0`；**前綴檢之判別力二造**（必真 `True`／必偽 `False`）。
4. **四簿期末**（`verify/probes/probe_WG9267_issuer_measurers.py registry`·逐簿具名其框）——**期值**：自誤 MAX **`375`**（相異 `365`）；`GB` `164`／`166`、`VR` `80`／`95`、`K-9` `28`／`29` **⛔ 動**。🔴 **期值⛔ 為現值**：實測須同格並報，相異即照實。
5. **坑之定義處清單自證**（`通則 I`：**報告入倉後**量·rev 同格具名）。
6. **自限復驗**：`app.py` blob 期初＝期末 `4379108a…`／`verify/baselines` `298`·`a898f4e1…`／`p3a` 未併線／`WV_BAKE` 未設／`K-9-29` 任何子項 ⛔ 落地。

---

## §四　鑄號之授權（**⛔ 逾此**）

**自誤 `375`** ——僅此一號（`§一`）。🛑 `GB`／`VR`／`K-9` **本批⛔ 鑄**。
🛑 **坑：本批⛔ 預先授權**——若自捕須鑄，**先出艙其形／徵候／攔法候發單側准**，⛔ 自行鑄。准鑄時標籤 ＝ **`ba`**，且須**同批擴 `verify/probes/probe_WG9269_pit_index.py` 之 `ALPHA_LABELS` 至 `ba`**（現行硬編止於 `az` ⇒ 不擴即**靜默漏**）。

---

## §五　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字；**⛔ 改五個 `*_EXPECT` 任一字**。
2. ⛔ 落地 `K-9-29` 之任何子項；⛔ 落地 `GB-164`／`GB-166`。
3. `GB-162` 三禁令維持；**⛔ 引任何自 baseline 取得之數**。
4. **⛔ 判 `GB-153` 之存廢、⛔ 判 `p6` 之結論作廢、⛔ 判 `R6` 之片為缺陷、⛔ 判 `p6` 二數孰誤**——四者皆**出艙即止**，裁由發單側。
5. **⛔ 呈 KL 任何事**。
6. ⛔ 併線 `p3a`、⛔ 刪任何 ref；側支待併期間主線⛔ 前進。
7. **⛔ 於任何入倉文件宣稱一件尚未發生之事**（放行／核可／結案／鑄號）——**及於 commit 訊息**。
8. ⛔ 令自己以 commit 空間二分跨越任何「洞」。

**辦畢 ⇒ 出報告 `docs/reports/W-G.9-271R_…md` ⇒ 停**（⛔ 自續下一批）。
