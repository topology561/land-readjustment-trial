# `W-G.9-318`　**補令二**：換窗前之保全／歸因之覆核（`GB-171`）／`自誤 439`〜`440`／CC 側交接

> **態**｜遠端 `wip/s1-endpart` ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（發單側自倉 `ls-remote` 實查·**未動**）。
> **分級** ＝ **中**（零生產碼·⛔ 改生產碼一字·⛔ 驅動全量）。
> 🛑 本補令於相衝之點取代 `W-G.9-318` 與**補令一**；三者皆留倉、⛔ 改前二者一字。
> 🔴 **最優先款**：`§一` 之保全（KL 已裁走**乙**）**未 `push` 成功前，⛔ 換窗、⛔ 結束本窗、⛔ 清工作樹、⛔ 任何 `git reset`／`rebase`／`checkout -f`／`clean`**。
> 🔑 **本補令貼<u>現窗</u>（持有 `17` 個本地 `commit` 之窗）**；新窗所取者為 `§四` 所產之交接文，⛔ 本補令。
> 🔑 **序**：**① `§一` 保全** ⇒ ② `§二` 現查三事 ⇒ ③ `§三` 登記 ⇒ ④ `§四` 交接文 ⇒ ⑤ `§五` 收工閘。

---

## `§零`　受領・裁

### `§零-1`　受領（**⛔ 鑄號**）

1. **`W-G.9-318R2` 受領**：`c7′`（`c13` `8e056b4`）六閘全綠；態甲 `got_*.csv` **`18` 檔 `sha256` 逐檔相同**、名目序列逐位相同、對帳類 III `0` ⇒ **forced 側逐位不變**之證成立；態乙受詞維持（`R4` 右 `628-1(1)`·`ΔRw +2.99`／`ΔG −0.90`·與 `c7`、替身 `α` 三者逐值相符）。
2. **CC 自捕 `3` 則全數受領**。其中 **`③` 首版誤取街廓軸實參致器紅** 一則，係 CC **先判量測器紅、⛔ 判受詞紅**，且據以查出真差因——**本批最有價值之一動**。
3. **CC ⛔ 自行改寫歷史**（未 rebase）⇒ **判斷正確**。
4. **CC ⛔ 改器以遷就 payload**（`probe_WG9267_issuer_measurers.py` 未動一字，二框並報）⇒ **判斷正確**。
5. **CC ⛔ 代擬放行格**（`序 11`）⇒ 維持正確。

### `§零-2`　裁（**`5` 項**）

**裁 `1`　🔴 換窗前之保全（最優先）**
發單側自倉實查：遠端分支共 **`24`** 支（`verify/*` `20`／`wip/*` `2`／`main` `1`／`claude/*` `1` ＝ `claude/wg9-95z-v3-resume-hgpzfs`）——**⛔ 有 `claude/landing-w0-adjustment-d81995`**。
⇒ **`17` 個 `commit` 僅存於 CC 本地工作樹**；換窗若取新環境，**其全數失之**（含 `c13` 之六閘實測、`§三` 對帳之五量、二份執行報告）。
⇒ **保全先於一切**。🔑 **KL 已裁（`2026-09-20`·逐字）：「問1：乙」** ⇒ **行乙**：先推保全分支，**⛔ 觸主線**；主線之 fast-forward 候新窗辦。
🛑 **保全 `push` 未成功前：⛔ 換窗、⛔ 結束本窗。**

**裁 `2`　`push` 粒度 ＝ <u>甲</u>（受領 `c7` 為被取代之中間態·歷史留痕）**
其由三：① 與「作廢單⛔ 刪·補令與原單並存」之體例同構；② `W-G.9-318R` 已**逐列載其雜湊**，rebase 將使倉內自相矛盾，其代價高於歷史不整；③ 改寫歷史係為掩一項**發單側之誤**而行之不可逆動作——⛔ 為此而行。
🔒 **附一款**：須於 `W-G.9-318R2` 之末端追加一節，逐字具名「`c7`（`b1141a3`）為**被 `c13` 完全取代之中間態**·⛔ 為現行碼面·凡 `checkout` 該 `commit` 者將取得一含土地後果退步之樹」，以免後窗誤讀。

**裁 `3`　🔑 歸因之覆核——`自誤 438` 之<u>形</u>維持，<u>歸因段</u>更正**
CC `§三` 之新查**受領**：① 與 ③ 係**同一點**（`corner_pt + buf·d̂`），唯一差源為**量測軸**——① 取街廓軸 `allocation_dir_block`，②③ 取街角第 `1` 宗之換軸 `allocation_dir`（`K-9-5-4 ②`），`|cos|` ＝ `0.9988785831`；而 `GB-170` 之受詞為**線之別**（`|cos|` ＝ `0.0473`）⇒ **⛔ 同一相異源**。
🔒 **其算術自洽**（發單側覆核）：軸差約 `2.72°`，而 `buf` 線之垂距沿深度自 `4.4804` 變至 `730.3629`（`GB-170` 原節併記）⇒ 槓桿臂大，`2.72°` 之旋轉足致 `0.77 m` 之差（`4.4804447558 − 3.7076106246 = 0.7728341312`）。
⇒ **`自誤 438` 之形（令傳一個有缺之值·有土地後果·未攔之由為未讀 `GB-170` 受詞段）維持**；其**歸因段**（「態甲變動 ＝ `GB-170` 之引入」）**更正為**：態甲變動之**近因 ＝ 量測軸之別**；`GB-170` 之線之別係**同一值上之第二項缺陷**，於 `∩FRONT` 處二線共點故未顯，於他深度必顯。
⇒ 🔒 **`§二` 之避讓（forced ⇒ `None`）<u>不變</u>**，且其立據由一項增為**二項獨立缺陷**。
⇒ 鑄 **`自誤 439`**（歸因之誤）＋ **`GB-171`**（軸之別·新項）。
🔒 **恆常附款 `u③` 之判**：該款（「同量級同格 ⇒ 一律先判為該項之引入·舉證責任在主張其非之一方」）**如設計運作**——CC 未舉證故保留發單側之判，本補令之覆核即其舉證。**該款維持**，僅增一補款（見 `§三`）。

**裁 `4`　四簿之二框 ⇒ **正典框（`VR-091 四`）為準**；鑄 `自誤 440`**
`自誤 434`–`437` 之標題形落入器之自擬框之缺號集、而正典框命中全五號 ⇒ 二框相異之由在**發單側之 payload 形**（`438` 二框皆命中·`434`–`437` 否）⇒ **⛔ 器之框缺陷**。
🔒 **⛔ 改器**（`probe_WG9267_issuer_measurers.py` 一字不動）；🔒 **⛔ 重貼 `434`–`437`**（重貼將使正典框之相異數虛增）；🔒 四簿之期末**一律二框並報**，並同格具名其相異之由。

**裁 `5`　`§二` 三事**（卡死任務／未 `commit` 之二檔／分支名）——其現查**先於**交接文之撰寫。

**裁 `6`　🔑 KL 已放行本波之受詞（`2026-09-20`·逐字：「問2：放行」）**
其受詞 ＝ 補令一 `附錄甲′`【要你判斷】：**只把「街角第一塊不是強制抵費地」那一側之側街負擔起算點校回側面道路邊線（`0` 公尺）；「第一塊是強制抵費地」那一側維持不動。**
⇒ **`GB-168` 解除條件 `(2)`（修入生產碼 ＋ KL 放行）就此成就**；`(1)`（成因具名）亦已成就；`(3)`（判別力二造）與 `(4)`（態乙六街廓全可得）**仍未成就** ⇒ 🔴 **`GB-168` ⛔ 解除**。
🛑 **本放行⛔ 等同五筆之逐筆放行**——主線之 fast-forward 須另經 KL 對 `c3` `7f43924`／`c4` `7c346b1`／`c5` `7c07a36`／`c6` `6cc51e6`／`c13` `8e056b4` 五個雜湊逐筆確認，且其辦理之窗 ＝ **新窗**（本窗⛔ 觸主線）。

---

## `§一`　工項一：保全（**最優先**·🛑 KL 覆前⛔ 換窗）

### `§一-1`　即辦（**⛔ 待 KL·⛔ 有風險·純讀**）

1. `git status --porcelain=v1 -uall` 全文出艙；`git stash list`；`git log --oneline --all --graph -25`。
2. **本地 `17` 個 `commit` 之全 `40` 碼雜湊逐列出艙**（`git log --format='%H %s' origin/wip/s1-endpart..HEAD`），與 `W-G.9-318R`／`R2` 所載逐列對拍。
3. `git bundle create /tmp/WG9318_all.bundle --all` ＋ 出艙其 `sha256` 與 bytes ⇒ **離線保全**（⛔ 入倉·⛔ 依賴其為唯一保全）。
4. 出艙 `git branch --show-current`、`git remote -v`、`git config --get remote.origin.url`。

### `§一-2`　候 KL（`附錄甲`）

### `§一-2`　保全之行（**KL 已裁 ＝ 乙**·⛔ 改法·⛔ 自選）

`git push origin HEAD:refs/heads/wip/W-G.9-318-preserve`　⇒ **全 `17` 筆**。

| 款 | 規 |
|---|---|
| 分支名 | **`wip/W-G.9-318-preserve`**（逐字·⛔ 另擬·⛔ 落入 `verify/*` 命名空間） |
| 主線 | 🛑 **⛔ 觸 `wip/s1-endpart` 一字**；`push` 後 `ls-remote` 須仍 ＝ `40375702378af4cefd2d11515fdf3f98c0791580` |
| 力度 | ⛔ `--force`／`--force-with-lease`（本分支為新建·必為快轉） |
| 性質之具名 | 🔴 該支**內含尚未經 KL 逐筆放行之生產碼** ⇒ 須於 `§四` 交接文與 `§五` 收工閘**逐處具名**「`wip/W-G.9-318-preserve` **⛔ 為現行碼面**·其存在僅為換窗之保全·⛔ 據以認定任何 `commit` 已獲放行」 |
| 出艙 | `push` 後 `git ls-remote --heads origin` 全列；遠端分支總數 `24` → **`25`**（`→` ＝ 期初→期末之**時序方向**·⛔ 幾何方向·⛔ 涉座標系）；`refs/heads/verify/*` 仍 ＝ **`20`** |
🔒 若行**乙**：`refs/heads/verify/*` 仍須 ＝ `20`；遠端分支總數 `24` → `25`（本分支在 `wip/*` 下），此一增須於收工閘具名。
🔒 **上開二數之實測出處**（發單側自倉·態 `4037570`·`core.quotePath` 預設）：`git ls-remote --heads origin | wc -l` ⇒ **`24`**；其分類 `git ls-remote --heads origin | sed 's#.*refs/heads/##'` ⇒ `verify/*` **`20`**／`wip/*` **`2`**（`wip/s1-endpart`·`wip/W-G.4-S0b-S0c`）／`main` **`1`**／`claude/*` **`1`**（`claude/wg9-95z-v3-resume-hgpzfs`）。🛑 **CC 須自跑·⛔ 採信本表**；不符即停機上呈。

---

## `§二`　工項二：現查三事（唯讀）

| # | 受詞 | 出艙 |
|---|---|---|
| `1` | 背景任務「`Patch exit-gate script for 閘4 dual frame`」已逾 `5h57m` 而同面板 `Finished` ＝ `59` | 其**最末一次輸出之時間戳**；其子程序之 `ps` 逐列；判其係卡死抑或仍在動。**若卡死** ⇒ 殺之，並**必查殘留鎖**（`.git/index.lock`／`.git/*.lock`／`.git/worktrees/*/index.lock`）逐檔具名。🛑 該任務之產物已由 `c17`（`d9e06c0`）取代，殺之⛔ 損已辦之事 |
| `2` | 畫面所示 `Edited 2 files`：`probe_WG9318p_bl0_recon.py`（`+57 −9`）／`W-G.9-318R2…執行報告.md`（`+234 −0`） | 其**是否已入 `commit`**。🔴 **若報告之 `+234` 列未入 `commit`，則「收工閘＝報告入倉後量」之前提⛔ 成立** ⇒ 須補 `commit` 後**重跑收工閘**（⛔ 沿用舊值） |
| `3` | 分支 `claude/landing-w0-adjustment-d81995`（`+811,275 −1,710`）；原單 `§八-13` ⛔ 開新分支 | ① `git log --oneline origin/wip/s1-endpart..HEAD`；② `git diff --numstat origin/wip/s1-endpart...HEAD \| sort -k1 -nr \| head -20`；③ `+811,275` 之組成**逐目錄具名**（期值：絕大多數落於 `verify/out/`）。🔒 發單側已查：該分支**⛔ 在遠端**（遠端 `24` 支·`claude/*` 僅 `claude/wg9-95z-v3-resume-hgpzfs`） ⇒ 其為**本地工作分支**，`§八-13` 之禁（其受詞為 `refs/heads/verify/*` 之增生）**⛔ 及之**——照實具名，⛔ 鑄自誤 |

---

## `§三`　工項三：登記（**零生產碼**·append-only·⛔ 改上文一字）

### `§三-1`　`GB-171`（`GB` 簿檔末嚴格末端追加）

> ### `GB-171` 🆕　**選槽側之 forced 起算點與實跑側之鏈頭 `W`，其<u>量測軸</u>⛔ 為同一軸**
>
> **受詞**：`_b_L0`／`_b_R0`（`_mp_base_W0(…, allocation_dir_block)`）取**街廓軸** `allocation_dir_block`；而實跑側鏈頭之 `_W_near`（`solve_G_binary`）取**街角第 `1` 宗之換軸** `allocation_dir`（`app.py:13989`–`:13990`：`if is_corner: allocation_dir = _first_corner_alloc_dir(side_mid)`·`K-9-5-4 ②`）。二軸 `|cos|` ＝ **`0.9988785831`**。
> **實測**（`W-G.9-318R2 §三`·落檔）：**同一點** `corner_pt + buf·d̂` 之 `W`——`0m` 甲 `R4 left`：街廓軸 `3.7076106246`／換軸 `4.4804447558`（差 `0.7728341312`）；`3.5m`：`7.0230161898`／`7.7850157181`（差 `0.7619995283`）。其負擔率之別 ＝ `−4.8792884257`／`−4.1147974529` 百分點。
> **與 `GB-170` 之分野**：`GB-170` 之受詞為**線之別**（`buf` 線向 `allocation_dir` vs 乙式遠側境界線向 SIDELINE·`|cos|` ＝ `0.0473`）；本項之受詞為**軸之別**（量測方向）。**二者為同一值上之二項獨立缺陷**；於 `∩FRONT` 處二線共點，故 `GB-170` 於該點不顯而本項顯。
> **失效條件**：`(1)` 二軸孰為正典經 KL 裁；`(2)` 統一後之行為差逐宗量得並具名；`(3)` 其餘行為逐位不變之證。🛑 **⛔ 解除、⛔ 收窄、⛔ 提修法主張**——三者皆未成就。
> **併記**：本項於 `W-G.9-318 補令一` 之避讓（forced ⇒ `None`）後**⛔ 觸及實跑側**；其仍在**選槽側之理論 `ΣRw`** 內生效，故 `理論＝實跑` 閘之殘差**可能含本項之貢獻**——⛔ 判、候量。

🔒 `GB-132` 之避讓：本節除節題外，凡以 `#` 起首者皆⛔ 含 `GB-N` 形；立後 `GB` 之相異 `168`→**`169`**、`MAX` `170`→**`171`**、缺號集 `[12, 87]` 不變。

### `§三-2`　`自誤 439`／`440`（自誤簿檔末·🛑 **標題形逐字複刻既例**）

🛑 **標題形之正典**（自 `自誤 432` 之既例**逐字複刻**·⛔ 另擬）：`### 🩸 \`自誤 N\`　**<形之一句>**`，其後空一列，次列為 `**根因**：…`，再次為 `**攔法（<n> 款·立為恆常附款 \`x\`）**：`。

> ### 🩸 `自誤 439`　**以「同量級・同格」逕判一項新測之變動為某未解除 `GB` 項之引入，而未先查被比較之二量其「點」是否同一、「軸」是否同一**
>
> **根因**：`恆常附款 u③`（同量級同格 ⇒ 先判為該項之引入·舉證責任在主張其非者）之**先判**本為保守設計，然發單側於下先判時**未同時載其可反證之檢**（點之同一性／軸之同一性），致該先判在受單側看來近乎定論。其與 `自誤 429`／`431`／`432`／`434` 同族——**款之射程不足**：`u③` 給了先判之規則，未給**先判之可反證框**。
> **後果之框**：⛔ 偽數入倉；`補令一 §三` 之對帳工項即為其攔點，**該工項生效一次**。歸因之誤⛔ 改變避讓之結論（其立據反由一項增為二項）。
> **攔法（二款·立為恆常附款 `u④`／`u⑤`·⛔ 廢 `u①`〜`u③`）**：
> `u④` 凡依 `u③` 下先判者，須於同格載**可反證之檢**（至少：被比較之二量其**產生點**是否同一、其**量測方向／軸**是否同一、其**口徑**是否同一），並具名「本判係先判·舉證責任在主張其非者·下列三檢任一不過即推翻」；
> `u⑤` 先判經覆核推翻者，**原自誤之形⛔ 撤**（其形為「令傳一有缺之值」，⛔ 為「該缺屬某項」），僅其**歸因段**更正，並另鑄一號記歸因之誤——⛔ 以「結論仍對」為由略之。

> ### 🩸 `自誤 440`　**新鑄之自誤條目其標題形⛔ 依既例，致既有之四簿量測器之自擬框失配，四號落入缺號集**
>
> **根因**：`補令一 §四` 之四則（`434`–`437`）由發單側另擬標題形（`### \`自誤 N\`` 後換列始述其形），而既例為 `### 🩸 \`自誤 N\`　**<形>**` 同列並置；`438` 因循既例故二框皆命中。發單側**出單前⛔ 以量測器自驗其 payload**——此與 `自誤 429 l③`（期值須載其框與量測時點）同族，惟受詞為**payload 自身之可量測性**。
> **後果之框**：⛔ 偽數入倉（落檔與報告皆二框並報）；四簿之相異／`MAX` 於二框下不相等。⛔ 改器遷就、⛔ 重貼四則（重貼將虛增正典框之相異數）。
> **攔法（三款·立為恆常附款 `v`）**：
> ① 凡新鑄之簿內條目，其**標題形須自該簿之最近一則逐字複刻**（`sed -n` 取其節題列為範本），⛔ 另擬；
> ② 出單前須以該簿之**既有量測器**對 payload 自驗一次，出艙其命中；失配即改 payload、**⛔ 改器**；
> ③ 二框相異而已入倉者，其後一律**二框並報**並同格具名相異之由，⛔ 擇一出艙。

### `§三-3`　`W-G.9-318R2` 之末端追加（裁 `2` 附款）
逐字具名 `c7`（`b1141a3`）為被 `c13`（`8e056b4`）完全取代之中間態、⛔ 為現行碼面、`checkout` 該 `commit` 將取得一含土地後果退步之樹。

### `§三-4`　`自誤 438` 之歸因更正節（**末端追加**·⛔ 改 `438` 原條目一字）
節題：`## 🔧 自誤 438 之歸因段更正（W-G.9-318 補令二 裁 3·原條目⛔ 改一字·純末端追加）`
內容 ＝ 裁 `3` 之逐字（形維持／近因改判為軸之別／`GB-170` 為同一值上之第二項缺陷／避讓不變且立據增為二項）。

---

## `§四`　工項四：CC 側交接文（`docs/reports/` 之 `W-G.9-318_CC側換手_一.md`）

🛑 **`§一` 保全辦畢後方得撰**。須載（⛔ 缺一）：
1. **保全之實得**：所行為甲抑乙、遠端之實態（`ls-remote` 全 `40` 碼）、`17` 個 `commit` 之逐列雜湊與其對應工項。
2. **態錨**：主線、生產碼 `34` 檔、四簿（**二框並報**）、`baselines`、`refs/heads/verify/*` `20`、遠端分支總數、三簿／典之 blob 與 bytes、母體四數。
3. **本波之受詞現況**：`K-9-41 ①` 前段已落地（待放行）／後段⛔ 動（`GB-169 §三` 已合裁）；`GB-168`／`169`／`170`／`171` 皆🔴 未解除，逐項載其失效條件之成就狀態。
4. **KL 已覆者（逐字）**：保全法 ＝ 「問1：乙」；本波受詞 ＝ 「問2：放行」（＝ 補令一 `附錄甲′`【要你判斷】）。
   **待 KL 者（新窗承辦）**：五筆雜湊之**逐筆放行**（`c3` `7f43924`／`c4` `7c346b1`／`c5` `7c07a36`／`c6` `6cc51e6`／`c13` `8e056b4`）⇒ 主線 fast-forward。
5. **新窗之開場序列**：① 重跑態錨全部 ② 驗 `17` 筆是否在遠端（⛔ 假定）③ 讀 `W-G.9-318`／補令一／補令二 三件與二份執行報告 ④ 讀 `GB-168`〜`GB-171` **自節題至檔末** ⑤ 讀 `自誤 433`–`440` 之攔法逐字 ⑥ 方得動工。
6. **恆常附款現況表**：`a`〜`v` 逐款一列（號·受詞一句·所出之自誤號）。
7. **本窗之自捕與教訓**：CC `8` 則（`318` 五則 ＋ 補令一 三則）逐則一列。

---

## `§五`　收工閘（**報告入倉後量**·⛔ 沿用他批之判準字串）

| # | 閘 | 判準 |
|---|---|---|
| `1` | 逐 `commit` `numstat` | 刪除欄全 `0`（本補令零生產碼） |
| `2` | 生產碼 `34` 檔 | 相異 `0`；`app.py` ＝ `4379108a…`（**相對本補令之開工態**） |
| `3` | 本批異動 `.md` 之 `CR` | 全 `0`；🩸 `numstat` 之 CJK 路徑須**先解八進位跳脫與引號**再判；**受檢集 ＝ `0` ⇒ 器紅**（⛔ 以空集充綠） |
| `4` | 四簿期末 | **二框並報**：正典框（`VR-091 四`）自誤 `433`／`440`·`GB` `169`／`171`；器之自擬框照實出艙並具名其相異之由 |
| `5` | append-only | 自誤簿 ＋ `GB` 簿前綴逐位相符 |
| `6` | 自限 | `baselines` `a898f4e1…`·`298` 列；`refs/heads/verify/*` ＝ **`20`**；遠端分支總數 **`25`**（`24` ＋ `wip/W-G.9-318-preserve`·須具名其性質）；`wip/s1-endpart` 仍 ＝ `4037570…` |
| `7` | 母體 | `docs/`／`verify/out/`／全倉追蹤 三數（框與時點同格具名·`→` ＝ 期初→期末之**時序方向**·⛔ 幾何方向·⛔ 涉座標系） |
| `8` | `ls-remote` | 全 `40` 碼；並具名 `17` 筆是否已在遠端 |

---

## `§六`　⛔ 為之事

1. ⛔ 在 `§一` 保全辦畢前換窗、結束本窗、清工作樹、`reset`／`rebase`／`checkout -f`／`clean`。
2. ⛔ 改保全之法或分支名（KL 已裁 ＝ 乙·`wip/W-G.9-318-preserve`）；⛔ 觸 `wip/s1-endpart` 一字；⛔ 於本窗 fast-forward 主線。
3. ⛔ 改寫歷史以去除 `c7`（裁 `2`）。
4. ⛔ 改 `probe_WG9267_issuer_measurers.py` 一字以遷就 payload；⛔ 重貼 `自誤 434`–`437`。
5. ⛔ 改生產碼一字；⛔ 驅動全量。
6. ⛔ 解除或收窄 `GB-168`／`169`／`170`／`171`。
7. ⛔ 代擬呈 KL 之放行格（`序 11`）。
8. ⛔ 以「量測器綠」代「受詞綠」；⛔ 以空集充綠；⛔ 擇一框出艙。

---

## 附錄`甲`　KL 裁示之落款（**逐字**·`2026-09-20`）

> **問1：乙**
> **問2：放行**

**其義（發單側之解讀·CC ⛔ 自解）**：
· **問 1** ＝ 保全走乙——先推保全分支 `wip/W-G.9-318-preserve`，⛔ 觸主線；主線之 fast-forward 候新窗。
· **問 2** ＝ 放行補令一 `附錄甲′` 之受詞（只改非強制抵費地之側·強制抵費地之側不動）。其土地後果已由 CC 實測坐實：

| 狀態 | 受影響之宗 | 負擔率 | 分配到的土地 | 其餘地主 |
|---|---|---|---|---|
| 現行預設 | **無**（態甲 `got_*.csv` `18` 檔逐檔 `sha256` 相同） | — | — | **全部分毫未變** |
| 廢前置合併後 | `R4` 街廓**右**側 `628-1(1)`（一宗） | ＋`2.99` 個百分點 | **減少 `0.90` 平方公尺** | 其餘 `28`／`31` 宗分毫未變 |

減少之 `0.90` ㎡ 進入調配池（街廓面積 `3207.87` 未變·強制抵費地三列合計變動 `0.0000`）。

🛑 **⛔ 逾此二句之解讀**：問 2 之「放行」係對**受詞**之放行，**⛔ 五筆雜湊之逐筆放行**（裁 `6`）。

---

SELF_SHA256: 682da2c4947b1bafef916856b79d6561dcc71f596e72493a433d049c1b4c9c21
