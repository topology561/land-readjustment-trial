# `W-G.9-307`　施工單（**中**）：`自誤 416`・坑 `bp` 之鑄 ／ `GB-170` 末端追加（**KL 放行射程之釐清**）／ **七處之在域現查** ／ **改前之釘紅與逐宗基線**

> **態**｜開工 ＝ `da9ca62eb2d007d13c08eaa9a066031228932e8a` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·坑 `bh`）。
> **單** ＝ `docs/orders/W-G.9-307_施工單_自誤416坑bp與七處在域現查及改前釘紅.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **中**（登記 ＋ 靜態現查 ＋ **`run_verification` 全量一次** ＋ 既有量測器 `probe_WG9287_landeffect.py` 一次·**零生產碼**·**⛔ 開分支**·**⛔ 修批**）。
> 🛑 **本批鑄號 ＝ `自誤 416` ＋ 坑 `bp`**（**二號**·逐字由本單供）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔴 **KL 之放行於本批<u>仍不動用</u>**（本批為修批之**前置量測**）；⛔ 改生產碼 `34` 檔一字。
> 🔑 **序**：**① `§二` 工項零**（單獨 `commit`·**單獨 `push`**）⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一** ⇒ **④ `§四` 工項二** ⇒ **⑤ `§五` 工項三**（靜態）⇒ **⑥ `§六` 工項四**（🛑 **款 `1` 綠後方得啟動長跑**）。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-0`　受詞與三方

本單之受詞 ＝ CC 施工窗（辦 `W-G.9-306` 之同一窗或其新窗皆可；**狀態一律自倉重建**：`CLAUDE.md` ／ `docs/reports/W-G.9-306R_自誤415與可達性二根集重測_執行報告.md` 全文 ／ 本單）。
三方之職：**KL** ＝ 域裁與 UI 操作者（⛔ 程式員）；**CC** ＝ 唯一實作者（KL 貼單方啟動·⛔ 自走·⛔ 自續下一批）；**`claude.ai`** ＝ 出單與獨立復驗（⛔ 動倉）。
🛑 **泛用化（絕對）**：⛔ 硬寫街廓名、側標籤、個案常數；本批之量測器亦同（街廓與側一律自執行期或自 AST 取得）。

### `§零-1`　`W-G.9-306` 全批之核可（發單側自倉獨立復驗·發單側沙盒 `Python 3.12.3`）

- `5ddf50c4…`..`da9ca62e…` ＝ **`8`** 個 `commit`；`git diff --numstat 5ddf50c4 HEAD` 之刪除欄合計 **`0`**（`6` 檔）。
- 生產碼 `34` 檔 `git diff --name-only f24e1d7a HEAD -- <34 檔>` ＝ **`0`** 行；`app.py` ＝ `4379108a…`。
- `W-G.9-306` 單 blob ＝ `31040` B·`P-5` ✅（受詞 `30962` B·`8ba7fe23…`）。
- 二簿之追加皆逐位 ＝ `old + b'\n' + <界內去首尾空列之 payload> + b'\n'`（以 `5ddf50c4`／`2a503e9` 為 `old`·以 `W-G.9-306` 單 blob 之界為 payload）；哨兵入簿 `0`。
- `registry` ⇒ 自誤 `405`／`415`·`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `29`／`30`；坑 **`93`／`93`**·末 `bo`。
- `verify/probes/probe_WG9306R_reach.py` 內容 `sha256` ＝ `0829b3f1a199db167c606d6131787c077444d12c1676172b3b949bdf3ec1ebb2`（`37364` B·`CR` `0`）；`verify/out/WG9306R_reach.log` ＝ `176220` B·`sha256` 首 `16` 碼 `07bcf65dcec68473`·`CR` `0`。
- **量測器之碼逐行復讀**：規則判定序 ＝ `R0`〜`R7` 依序；`R4` 以 `type(o)` 檢；`R7` 以 `gc.get_referents`；二根集各自 `walk_roots`、登記分列。落檔之 `21` 列判定表與報告 `§五-4` **逐格相符**。
- **`§八`／`§九` 事故之傷害面（發單側獨立查）**：被執行之二 `.md`（態 `bbe5c3a`）內，反引號所夾而以指令名起首之片段 ＝ 單 `21`／報告 `33`；其中可改倉者**僅** `git add`（**無路徑**·不暫存任何物）；**無** `push`／`commit`／`reset`／`checkout`／`rm`／`update-ref`。
⇒ **✅ 全批核可**。

### `§零-2`　裁（十項）

| # | 事 | **裁** |
|---|---|---|
| `1` | 不透明型別（每根集 `2`） | `builtins.NotImplementedType`／`builtins.ellipsis` 係**無參照之不可變單例** ⇒ **結構上不持候選值** ⇒ **⛔ 阻停九** |
| `2` | `R2`（模組／類別／函式⛔ 穿越）是否影響停九（體）之判 | **⛔ 影響**。由：① 同一進入內之同鍵產出 ＝ `0`（`21` 次逐次）⇒ 可漏者僅「跨進入之持久儲存」；② 母體 ＝ 生產碼 `34` 檔（正面列舉）：`lru_cache` `0`／`@cache` 形 `0`／`functools` 之 `import` `0`／行首 `global` `2`（皆 `verify/run_verification.py`·名 `_N19P_DEPTH_CACHE`／`_GXXX_RE`）；③ 該向之流徑 ＝ `app.py` `_first_corner_alloc_dir` 之回傳 ⇒ `_solve_G_one` 之 `_r['_alloc_dir_used']` ⇒ 呼叫端之 `res`／`_near_dir_*`（`_alloc_dir_used` 於 `app.py` `8` 列·`verify/stepg_pipeline.py` `6` 列·皆此鏈）。🔒 殘餘之邊角（同具 `__dict__` 與 C 層參照之物件於 `R6` 即止）**照實具名**、無流徑可達 |
| `3` | 路丙之落點 | ✅ **停九（體）`21/21` 成就之判受領** ⇒ **落點 ①／②／③／④′ 於「程式只改一處」之讀法下全數閉合** |
| `4` | 🔑 **KL 之放行射程之釐清**（逐字·`2026-09-16`） | 發單側呈 KL 之【要你判斷】欄逐字 ＝「您 9/15 之「放行」，是否涵蓋「同一條界線在軟體中 7 處一併同樣修改」？（是／否）」；**KL 逐字所答 ＝「是」**。🔒 **射程（由發單側釘死·⛔ CC 擴之）**：`(b)` 之「只改該處」＝ **只改該一條界線之取法**，及於 `_corner_buffer_S` 之呼叫端**全 `7` 處**（`W-G.9-300R` 之 AST 二形並取）之**同一改法**；`(a)`、`(b)` 之三段辦法、`(c)`（入主線須第二次放行·修批落凍存分支 `verify/W-G.9-299-gb170`）、`(d)`（⛔ 及 `GB-167`／`168`／`169`、`K-9-29`、藍影）**一字不變**；呈文【要改成】所載「畫面分配之 `2` 處，須請您在畫面上實跑一次核對」**併入 `(b)`** |
| `5` | 🔑 **次一受詞之切分** | **本批 ＝ 登記 ＋ 七處之在域現查（靜態）＋ 三段辦法 ① 之釘紅 ＋ 改前之逐宗基線**（皆零生產碼）；**修批 ＝ `W-G.9-308`**——其**逐處之改法由發單側依本批實測釘死**（⛔ CC 自擬）。由：`verify/wf_f1.py`／`verify/wf_f4.py` 之呼叫端所在函式內，`side_mid` 與「街角第 `1` 宗」之述詞**⛔ 見其名**（發單側預查·⛔ 充結論），其取式須經現查方可釘 |
| `6` | `W-G.9-306` 閘表之「十三項」與資料列 `14` 之互斥 | 🔴 **責在發單側** ⇒ 鑄 **`自誤 416`**（`§三`）；受單側取保守項（`14` 項全跑、逐字回報）**處置正確** |
| `7` | `W-G.9-306R` 之自捕三則 | 自捕 `1`（閘 `6'` 捕獲器留末 `12` 列致結果滅失）＝ 恆常附款「⛔ `\| tail -N`」之重踩 ⇒ **⛔ 鑄**；自捕 `2`（報告之臨時解析漏一列·經器自印值當場攔下）⇒ **⛔ 鑄**；自捕 `3` ＋ `§九` 之更正 ⇒ 鑄 **坑 `bp`**（`§三`） |
| `8` | `P-1`／`P-4` 之具名豁免（`W-G.9-306R` `§一-1`） | ✅ **受領**；`P-1` 之「mention 而非 use」之偽陽形照實具名**正確** |
| `9` | 發單側本窗之自捕（照實併記·**⛔ 鑄號**） | 擬本批前以**文字層** `_corner_buffer_S\(` 檢呼叫端，初得 `4` 處；與 `W-G.9-300R` 之 AST 二形並取 `7` 處對照後攔下（漏 `ns["…"](` 形）——恆常附款「函式名二形並取」之重踩 |
| `10` | `GB-170` 之現態 | 🛑 失效條件 `(2)`／`(3)` **仍未成就** ⇒ **⛔ 解除、⛔ 收窄、⛔ 提修法主張**；`K-9-30` ⬜ **未實作** |

---

## `§一`　開工閘（**十四項**·**每列同格載其產生指令**·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；🔑 期 `3.13.11` 起首（倉內既載）——不符 ⇒ **loud 具名、停** |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `da9ca62eb2d007d13c08eaa9a066031228932e8a` **或其後裔**（全 `40` 碼·後裔以 `merge-base --is-ancestor` 證） |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` 緊接指令·先 `git cat-file -t` 證其存在） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符（🔒 **整全性之判準即此**·`自誤 412`）；`P-3` 不紅；🟡 **逐項具名處置**（先查該器之觸發式定位其真受詞·`P-4` 之豁免三類同 `W-G.9-305 §一-1` 逐字；`P-2` 若觸於 `§零-2` 之表，其各數之母體與產生指令**同格已具名**（裁 `2` 之「生產碼 `34` 檔（正面列舉）」）——**須先查該器之觸發式**再具名處置） |
| `4'` | 受詞自證（坑 `bf`·**以器所印之檔名列**） | [必真] 含 `W-G.9-307` ＝ `True`／[必偽] 含 `W-G.9-306` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 自誤 **`405`**／**`415`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `29`／`30`（缺 `[]`） |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py <rev 釘死>`（**全量落檔**·`open(p,'wb')`） | **`93`／`93`**·末 **`bo`** ⇒ 下一 **`bp`**；器自印判別力二造綠 |
| `7` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**） | **`34`** 檔；判別力[必不命中] 子層 `*.py` **須 `> 0`**（⛔ 定值·前批 `324`／發單側於 `da9ca62e` 得 `325`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 `20` 支；`verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`** |
| `10` | **本波十一單之在倉性**（三態分列·`rc` 緊接指令） | `git ls-tree -r --name-only -z HEAD docs/orders/` ⋂ 檔名前綴 `W-G.9-303_`／`-304_`／`-305_`／`-306_`／`-307_`（**取 bytes**·坑 `bk`）⇒ `7`／`1`／`1`／`1`／`1` ⇒ **`11`** **皆態1**；判別力[必為零] ＝ **執行期組出**之人造路徑 `rc ≠ 0`（字面⛔ 出艙） |
| `11` | **物證落檔**（🔒 **框 ＝ blob `sha1`**·先 `cat-file -e`·三態分列·取 bytes） | 集合甲 ＝ `docs/reports/W-G.9-303_交接註_舊窗收工二.md` 節 `10` 表內之相異 `.log` 名（以程式自 blob 抽取·⛔ 轉錄）⇒ `13`；集合乙 ＝ `git ls-tree -r --name-only -z HEAD verify/out/` ⋂ 名前綴 `WG9303R6_`／`WG9304R_`／`WG9305`／`WG9306R_` ⋂ `*.log` ⇒ **`9`**；聯集 **`22`** ⇒ **皆態1·逐位未動**；🆕 另列**對照物** `verify/out/WG9287R_landeffect.log`（`§六` 款 `1` 之期值源）⇒ 態1·期初 blob `sha1` 存錄 |


### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **恆常附款逐項適用·⛔ 省**（坑 `bh`／`bi`／`bj`／`bk`／`bl`／`bc`／`bd`／`be`／`bf`／`bg`／`g`／`3`、
`GB-158` **三形分列**（🔒 自誤之標題形為 **C 形** `` `自誤 N` ``·⛔ B 形）、簡繁誤字機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、payload 之落地形機驗**繫於性質**、
函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、**逐閘出艙執行次數**、**等待綁行程結束**、
**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N`**、**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、
**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**、`f_locals` 鍵須**自證存在**並**增第二鍵**、
`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退** ⇒ 先 `cat-file -e`·**三態分列**、
**簿之數一律取器之輸出**、**母體之基數須綁外部錨**、**含 CJK 路徑之 `git` 輸出取 bytes**、
**摘要值一律逐字具名其產生指令**、**母體一律以產生指令界定**、
**⛔ `git ls-files --with-tree=<rev>`**（坑 `bl`）、**`grep` 含 alternation 者逐支分列**·**字面之框自 blob 逐字取**（`自誤 409`）、
**落檔一律 `open(p,'wb')`**·**⛔ shell 重導向 Python `stdout`**（`W-G.9-303R6` 自捕 `5`）、
**凡長跑在跑，任何修復動作須先確認無在途之量測**、
🆕 **框之語法形須窮舉**·**母體以頂層名界定者須同格出艙其非頂層之基數**（`自誤 411`）、
🆕 **機械順序**：⛔ 預書 之款一律「佔位符 → 跑 → 填」·**逐款回掃**同文件之警告（坑 `bm`）、
自捕為零者須併具「**⛔ 讀為『本批無誤』**」）。
**`W-G.9-305` 所增之恆常附款**：**「出現」一律以「每一節點之每一欄位值逐字相等」構造上界定**（`自誤 413`）、
**時序閘須先具名其比較之執行脈絡**·全程序號⛔ 作判準（`自誤 414`）、
**以 `stdout` 落檔之器一律 `reconfigure(encoding="utf-8", newline="\n")`**（坑 `bn`）、
**欄之標籤逐字須與其運算式逐項對照後方得出艙**（坑 `bo`）。
**`W-G.9-306` 所增之恆常附款**：**走訪規則之容器一律依協定界定**·以 `type(o)` 檢屬性（⛔ 以實例屬性）、
**每一根須於長跑前以同一直譯器之真實同型物自證可命中**·[必命中] 造⛔ 僅以合成 `dict`、
**凡規則依直譯器型別語義者，開工閘出艙 `sys.version`**（`自誤 415`）、
**「payload」＝ 界內之列去其首尾之空列**（`§零-1`）。
🆕 **本單所增之恆常附款**：**凡碼含反引號者一律落檔後以路徑呼叫**·⛔ 內嵌於 shell 命令列·確須生反引號者以 `chr(96)` 執行期組出（坑 `bp`）、
**`stderr` 見「以文件名為前綴之 `line N: … command not found`」者一律當作該文件已被執行**·逐項實測傷害面（坑 `bp`）、
**清除副作用後須間隔再掃一次**·二次皆空方得稱已復原·**⛔ 以任務之停止回覆充行程已死**（坑 `bp`）、
**文件內一切「N 項／N 造／N 款」之計數標籤，出艙前以程式自其資料列計數對照並出艙對照結果**（`自誤 416`）。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**）

落點 ＝ `docs/orders/W-G.9-307_施工單_自誤416坑bp與七處在域現查及改前釘紅.md`；**⛔ 改一字**。
🔒 **整全性之判準 ＝ `SELF_SHA256`（`P-5`）**——**⛔ 另設任何帶外之釘死**；其判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須**出艙**。
**入倉後之出艙**：**blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源**（🔒 **二框並報**）；`CR` ＝ `0`（**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；
`deletions` ＝ **`0`**；檔數 ＝ `1` 之自證（造甲[必≥1] ＝ `docs/orders/W-G.9-306_施工單_自誤415與可達性二根集之重測.md`／造乙[必為0] ＝ **執行期組出**之人造名）。
🛑 **落地後即 `push`**（生產碼判法 `0` 行 ⇒ 逕行 `push`），**其後方跑 `§一`**。

---

## `§三`　工項一：🔑 **`自誤 416` ＋ 坑 `bp` 之鑄 ＋ `ALPHA_LABELS` 之擴**（主線·**三段分列 `commit`**）

🔑 **明文授權一項手段**：**擴 `ALPHA_LABELS` 至 `bp`**——射程 ＝ **僅** `verify/probes/probe_WG9269_pit_index.py` 內
**新增一列** `ALPHA_LABELS += ["bp"]`（形同其既有之 `+= ["bm"]` 列·尾註同體例·尾註載 `W-G.9-307` §三），**⛔ 改該檔任何既有列一字**、**⛔ 動其任何框或判準**。
🔑 界：自誤 ＝ `<!-- ZW416-BEGIN -->`／`<!-- ZW416-END -->`；坑 ＝ `<!-- PITBP-BEGIN -->`／`<!-- PITBP-END -->`。
落點皆 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（**自誤先、坑後·二段分列 `commit`**；`ALPHA_LABELS` 之擴為第三 `commit`）。
🛑 唯一性之框 ＝ **整列逐位相等**（`^…$`）·母體 ＝ 本單單檔·各哨兵須命中 **`1`**；併報**子字串框**之數（**二數並報**）。
🛑 payload **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**；**「payload」＝ 界內之列去其首尾之空列**；**落檔 `open(p,'wb')`**；追加之式 ＝ `old + b'\n' + payload + b'\n'`。
🔒 **坑之定義處<u>多於一處</u>之預先受領**（本單內含其定義標題）——**本單⛔ 予其定義處之期值**（`自誤 403` 攔法 ②）；CC 以器之定義框出艙其清單並照實具名，**⛔ 判紅、⛔ 逕修**。

<!-- ZW416-BEGIN -->

### 🩸 `自誤 416`　**施工單以「N 項」標其閘表，增列一閘後未回改該標籤——標籤與其受詞不符之形由發單側於施工單自身之計數重踩**

**形**：`W-G.9-306` 之 `§一` 標題與 `§八` 報告之格皆載「開工閘（十三項）」；而 `§一` 表之資料列 `#` 欄標籤集（逐字·保序）＝
`0`／`1`／`2`／`3`／`4`／`4'`／`5`／`6`／`6'`／`7`／`8`／`9`／`10`／`11` ⇒ **`14`**。
該單依 `自誤 415` 攔法 ③ **新增閘 `0`**（`sys.version`），**未回改**二處之「十三項」。

**徵候**：`W-G.9-306R` `§六-4` 之標籤對照捕獲（`常規二`·施工單內部互斥）；受單側取保守項（`14` 項全跑、逐字回報）。

**根因**：
① 發單側出單前自檢之第五款（欄之標籤與運算式逐項對照）其受詞只及**報告之欄**，⛔ 及**施工單自身之標題計數**；
② 增列之編修與其計數之標籤分處二節，**無機械繫結**。

**族** ＝ 坑 `bo`（欄之標籤與其實際量測之受詞不符）之**發單側重踩**（第二度·首度係 `W-G.9-305` 出單前自捕之 `probe_WG9305_issuer_static.py` 節 B）。

**攔法（二款）**：
① 施工單內一切「N 項／N 造／N 款／N 處」之計數標籤，出單前以**程式自其表之資料列計數**對照，並於出單時併記對照結果；
② 增列或刪列之同一編修，須**同時回掃同文件內之全部計數標籤**。

<!-- ZW416-END -->

<!-- PITBP-BEGIN -->

### 🕳 坑 `bp`　**命令列之字串含反引號，經 shell 之命令替換而把文件當作腳本執行**

**形**：以 `bash` 之**雙引號**包 `python -c "…"`，其內作 markdown 標記之反引號**仍被 `bash` 行命令替換**；
反引號所夾之片段若為一文件之路徑，`bash` 即**逐列執行該文件**——文件內之 `>` 成重導向、行首之指令名真被呼叫。

**徵候（實測·`W-G.9-306R` `§八`／`§九`）**：
① `stderr` 出現「**以文件名為前綴**之 `line N: … command not found`」；
② 工作區生成以文件內文為名之 `0` B 雜散檔（全程至少 `7` 個相異名）；
③ 該命令逾時後被 harness **移入背景而持續執行**：刪除之雜散檔於 `15`〜`25` 秒內復現；任務之停止回覆成功後，以 `ps -ef` 實查仍見其行程。

**攔法（四款）**：
① 凡碼含反引號者，一律**落檔後以路徑呼叫**，⛔ 內嵌於 shell 命令列；確須於命令列生反引號者，以 `chr(96)` 於執行期組出。
② 凡 `stderr` 見徵候 ①，一律當作「該文件已被執行」，**逐項實測傷害面**（`HEAD`／`reflog`／生產碼 blob／物證落檔／`refs/heads/verify/*`／工作區）。
③ 清除副作用後須**間隔一段時間再掃一次**，二次皆空方得稱已復原。
④ **⛔ 以任務之停止回覆充行程已死**——一律以 `ps` 實查其行程樹。

<!-- PITBP-END -->

**落地後之機驗**：以**器本身**量 ⇒ 自誤 相異 **`406`**／MAX **`416`**；`416` 命中 **`1`**（🔒 **C 形**）；
坑 **`94`／`94`**·末 **`bp`**（器須以擴後之 `ALPHA_LABELS` 跑·**`rev` 釘死**·全量落檔）；`GB`／`VR`／`K-9` **一字未動**；
哨兵 `ZW416`／`PITBP` 入簿命中 **`0`**；**三框交叉誤中**（自誤框·坑框·`K-9` 框）分列並報；append-only 二造 ＋ `wg942_append_audit.py` `rc = 0`；
`probe_WG9269_pit_index.py` 之 `numstat` ＝ **`+1`／`-0`**。

---

## `§四`　工項二：`GB-170` 之末端追加（主線·落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**·⛔ 上文一字不刪·🛑 **`§三` 入倉後方得辦**）

🔑 本 payload **⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**；界 ＝ `<!-- GB307-BEGIN -->`／`<!-- GB307-END -->`（**去首尾空列**）。
🔒 **`GB-132` 之避讓**：凡**以 `#` 起首**之列，其列內**⛔ 含任何 `GB-` 之號**。

<!-- GB307-BEGIN -->

---

## 🔧 路丙落點第四度現查之**判定** ＋ KL 之放行射程之**釐清**（`W-G.9-306R`／`-307`·⛔ 解除、⛔ 收窄·純末端追加）

🛑 **本節⛔ 鑄任何號**（`戒 36`）；⛔ 改任何原節一字、**⛔ 解除、⛔ 收窄任何阻塞項**。

> **`W-G.9-306R` 之實測**（`run_verification` 全量一次·強制呼叫 `21` 次·`_run_step_g_impl` 進入 `15` 次·走訪規則依協定重立·根型別自證 `6` 造全綠）：
> 二根集之截斷與走訪例外皆 `0`（**不可判款不成就**）；**(體)** 與 **(端)** 之同命中皆 `0`（**停九（體）／停九（端）皆 `21/21` 成就**）；
> (端) 之他命中 `160`〜`1860`（`15` 次）證其走訪確觸及候選值；不透明型別每根集 `2`（`builtins.NotImplementedType`／`builtins.ellipsis`）。
> **發單側之裁（`W-G.9-307 §零-2`）**：① 二不透明型別係無參照之不可變單例 ⇒ ⛔ 阻停九；② 走訪規則不穿越模組／類別／函式之邊界**⛔ 影響判**
> （同一進入內之同鍵產出 `0`；生產碼 `34` 檔內 `lru_cache`／`@cache` 形／`functools` 之 `import` 皆 `0`、行首 `global` 僅 `verify/run_verification.py` `2` 處且其名無關）；
> ③ ⇒ **路丙於「程式只改一處」之讀法下，落點 ①〜④′ 全數閉合**。
> **KL 之放行射程之釐清（逐字·`2026-09-16`）**：發單側呈 KL 之【要你判斷】欄逐字 ＝
> 「您 9/15 之「放行」，是否涵蓋「同一條界線在軟體中 7 處一併同樣修改」？（是／否）」；**KL 逐字所答 ＝「是」**。
> 🔒 **射程（由發單側釘死·⛔ CC 擴之）**：`(b)` 之「只改該處」＝ **只改該一條界線之取法**，及於 `_corner_buffer_S` 之呼叫端**全 `7` 處**之**同一改法**；
> `(a)`、`(b)` 之三段辦法、`(c)`（入主線須第二次放行·修批落凍存分支）、`(d)` 之排除**一字不變**；
> 呈文【要改成】所載「畫面分配之 `2` 處，須請您在畫面上實跑一次核對」**併入 `(b)`**。
> 🛑 **KL 之放行迄未動用於任何生產碼**；`verify/W-G.9-299-gb170` **⛔ 開**；`K-9-30` ⬜ **未實作**；
> **失效條件 `(2)`／`(3)` 仍未成就**——⛔ 解除、⛔ 收窄、⛔ 提修法主張。

<!-- GB307-END -->

**落地後之反向機驗**（**三形分列並報**·框**自器碼當場取**）：聯集 MAX **須仍 `170`**、相異 **須仍 `168`**、缺號 **須仍 `[12, 87]`**；
判別力[必命中] ＝ `GB-170` 之既有定義列於三形至少一形須 `True`／[必為零] ＝ **執行期組出**之人造號於三形皆須 `False`。
併驗 `GB-132` 避讓：本 payload 內 `^#` 起首且含 `GB-` 者 ＝ **`0`**（判別力[必非零] ＝ 同一偵測器掃 `GB` 簿全檔）。

---

## `§五`　工項三：**七處之在域現查**（主線·**零生產碼**·**靜態**·⛔ 跑 harness）

🔑 **明文授權一項手段**：**新增量測器** `verify/probes/probe_WG9307R_sites.py` ＋ 其落檔 `verify/out/WG9307R_sites.log`（**AST 為框**·讀 blob 取 bytes·**`rev` 釘死**）。
🛑 **本工項⛔ 判改法、⛔ 判可施與否、⛔ 擬任何 diff**——其結果供發單側釘 `W-G.9-308` 之逐處改法。

### 五-1　款 `1`　呼叫端集 `S`（**母體 ＝ 生產碼 `34` 檔**·外部錨 ＝ `§一` 閘 `7`）

🔒 **框（語法形窮舉·構造上界定）**：`ast.Call` 之 `func` 為下列之一者——
① `ast.Name` 且 `id` ＝ `_corner_buffer_S`；② `ast.Subscript` 且其 `slice` 為 `ast.Constant` 字串 ＝ `_corner_buffer_S`；③ `ast.Attribute` 且 `attr` ＝ `_corner_buffer_S`；
④ `ast.Name` 且其 `id` 於同一模組或同一函式內由 `Assign` 綁定、其 RHS 為 ①／②／③ 之 `func` 形（**別名一層**·鏈更深者 loud 具名）。
🔒 **殘餘之全量**：34 檔內凡含字樣 `_corner_buffer_S` 之節點（`Name`／`Constant`／`Attribute`／`arg`／`FunctionDef`·**每一節點之每一欄位值**）而**⛔ 屬上列 `func`** 者，逐一出艙其節點型別與列（⛔ 靜默略過）。
**期**：`S` 之基數 ＝ **`7`**；並與 `W-G.9-300R` 之 `S1`〜`S7` 逐一對照（檔·所在函式之 qualname·`side` 實參）——**對照表全量出艙**；行號**⛔ 作錨**（僅出艙）。

### 五-2　款 `2`　逐處之在域事實（**全量 `7` 列**·每處一節）

| 欄 | 受詞（**以性質界定**） | 出艙 |
|---|---|---|
| `(a)` | 所在函式與外圍守衛 | qualname（含巢狀）與其 `def` 列；外圍 `If`／`IfExp`／`Try` 之 `test` 逐字（由內而外至函式體首層） |
| `(b)` | 實參 | `allocation_dir` 位（位置 `3` 或 kw）、`side`、`_label` 之原始碼逐字（`ast.get_source_segment`） |
| `(c)` | **街廓鍵** | 該處之守衛取強制旗標所用之鍵名逐字（如 `….get(<鍵名>)`），及該名於同一函式內之賦值來源逐字 |
| `(d1)` | 🔑 **側界線之 `mid` 之在域來源** ＝「生產碼對同一街廓同一側之街角第 `1` 宗，傳入 `_first_corner_alloc_dir` 之實參」之來源 | 同一函式（含其巢狀函式）內，凡以 kw `side_mid` 傳入 `_solve_G_one`、或以 kw `_side_mid` 傳入其**薄殼**（🔒 薄殼 ＝ 同檔內以形參原樣轉傳 `side_mid=` 至 `_solve_G_one` 之函式·**以 AST 證其轉傳**）之實參逐字；逆溯其名之**每一**綁定（`Assign`／`AnnAssign`／`AugAssign`／`NamedExpr`／`for` 目標／`with` 目標／形參）之 RHS 逐字，至**以字面鍵取自登記表之式**為止（鍵鏈逐字） |
| `(d2)` | 綁定與強制呼叫之**先後** | 以**同一作用域之語句路徑**判（**⛔ 以行號判**）：「前」／「後」；並逐名出艙二者之間**重綁**該鏈上任一名之語句（無 ⇒ 空集·loud 具名其為空） |
| `(d3)` | `(d1)` 為空之處 | 該函式內可達**生產碼 `_first_corner_alloc_dir` 所讀之同一登記表**之名，並以**靜態賦值鏈**逐列具名其為同一物（例之形：`verify/run_verification.py` 內對 `f3_cad_side_lines_by_side` 之賦值與其 RHS 之來源·**當場自 blob 取·⛔ 採本單之例**）；仍為空 ⇒ **loud 具名「⛔ 在域」** |
| `(e1)` | 🔑 **`K-9-30` 之述詞之在域來源** ＝「生產碼決定該側之某宗以 `is_corner` 真傳入 `_solve_G_one`（因而其 `allocation_dir` 由 `_first_corner_alloc_dir` 取）之式」 | 同一函式內傳入 `_solve_G_one` 或其薄殼之 `is_corner` 位實參逐字；逆溯其鏈上**每一**綁定之 RHS 逐字，至**取自登記表或參數表之欄位**為止（字面鍵名逐字） |
| `(e2)` | 該鏈上每一名於強制呼叫之時**是否已綁定** | 逐名「已綁定／未綁定」（語句路徑判）；🔑 **凡其值依強制呼叫之後始決定之量者**（例之形：分側之切點、推進迴圈內之旗標）**loud 具名其名與其決定之語句** |
| `(e3)` | 該函式內無 `_solve_G_one` 之呼叫之處 | 出艙該函式內讀取 `(e1)` 所得**同一欄位名**之處；無 ⇒ **loud 具名「⛔ 在域」** |
| `(f)` | 該處是否由 `run_verification` 驅動 | **⛔ 重量**；逕引 `W-G.9-300R` 之 `F`（逐處具名其引文所在之列） |

### 五-3　款 `3`　判別力造

| 造 | 受詞 | 期 |
|---|---|---|
| [必為 `7`] | 外部錨 ＝ `W-G.9-300R` 之「AST 之處」 | `S` 之基數 ＝ `7` |
| [必命中甲] | `verify/stepg_pipeline.py` 之左支（`side` 實參 ＝ `'left'`）之 `(d1)` | 得名 `_side_mid_left`，其鍵鏈含字面 `f3_cad_side_lines_by_side` |
| [必命中乙] | 同上之 `(d2)` | ＝「**後**」（發單側自倉讀得：強制呼叫之語句先於該名之綁定語句） |
| [必為零] | 以**執行期組出**之人造函式名替 `_corner_buffer_S` 重跑款 `1` 之框 | `S` 之基數 ＝ `0` |

🔒 **[必命中甲]／[必命中乙] 係發單側之預查**——任一不中 ⇒ **判器紅、停、上呈**（⛔ 以之改寫預查、⛔ 續工項四）。

### 五-4　停機款

> **停十一**：款 `1` 之 `S` 之基數 ≠ `7`，或與 `W-G.9-300R` 之 `S1`〜`S7` 不能逐一對照 ⇒ **停、上呈**（⛔ 續工項四）。
> 🔒 款 `2` 之任一「⛔ 在域」**⛔ 停**——其為量測之結果，**逐處照實具名**，裁屬發單側。

---

## `§六`　工項四：**改前之釘紅與逐宗基線**（主線·**零生產碼**·🛑 **款 `1` 綠後方得啟動長跑**）

🔑 **明文授權三項手段**（射程逐字·⛔ 逾之）：
① **執行既有量測器** `verify/probes/probe_WG9287_landeffect.py` **一次**（**⛔ 動該器一字**·以其既有呼叫法逐字·**等待綁行程結束**·全量落檔 `verify/out/WG9307R_landeffect_pre.log`）；
② **新增量測器** `verify/probes/probe_WG9307R_prepin.py`（**得以 `probe_WG9306R_reach.py` 之包裝骨架為底本另存新檔**·**⛔ 動該既有探針一字**），驅動 **`run_verification` 全量一次**（**⛔ 改生產碼一字**·**其執行期間⛔ 動受測物**）；
③ **於行程內包裝**（⛔ 動任何檔·**冪等**）：`ns` 之三鍵 `_corner_buffer_S`／`_solve_G_one`／`_first_corner_alloc_dir`，及模組 `verify/stepg_pipeline.py` 之屬性 `_run_step_g_impl`（**僅供計其進入序號**）。**各觸發數須出艙且 `≥ 1`**。
落檔 ＝ `verify/out/WG9307R_prepin.log` ＋ `verify/out/WG9307R_lots_pre.jsonl`（**皆 `open(p,'wb')`**·`CR` `0`）。

### 六-1　款 `1`　🔑 **釘紅**（KL 射程 `(b)` 三段辦法之 ①）

| 項 | 內容 |
|---|---|
| 受詞 | `probe_WG9287_landeffect.py` 所出艙之**逐格對稱差（㎡）**與**格集** |
| 期 | 與倉內 `verify/out/WG9287R_landeffect.log` 之同格之值**字串逐字相同**、格集相同（🔒 **二檔皆以程式抽取**·⛔ 轉錄） |
| 發單側之參考（⛔ 充期值） | `GB-170` 定義節所載：`R4/left@0.0m` `13.055884224441684`／`R2/left@3.5m` `38.06818855324997`／`R4/left@3.5m` `13.05518976183967` |
| 附 | 該器之自我驗證閘三重須綠、`rc` 出艙 |

> **停十二**：任一格不逐字相同、或格集相異、或該器之自我驗證閘不綠 ⇒ **停、⛔ 啟動長跑、上呈**（現態已漂移·⛔ 自判其因）。

### 六-2　款 `2`　🔑 **逐宗基線**（`W-G.9-308` 改後對拍之**改前**側·**物證落檔**）

| 項 | 內容 |
|---|---|
| 受詞 | `run_verification` 二情境之**最終分配結果之全部宗**（含抵費地之片）。CC **逐字具名**其所取之物（模組·函式·名），並證其為 harness 據以對拍 baseline 之**同一物**（`is` 或同名傳遞鏈逐字） |
| 每宗一筆（`jsonl`·`utf-8`·`LF`） | 情境標籤／街廓／側（原字）／宗序（原欄名逐字）／地號·暫編地號（原欄名逐字）／**面積之未捨入實算值 `float.hex`**（其欄名或來源式逐字）／表列面積（原字串）／**幾何之 WKB `sha256`**（取既有幾何物件之 `wkb`·**⛔ 重建幾何**） |
| 自證 ① | 每情境之筆數 ＝ 一**獨立於本傾印**之外部錨（CC 具名其產生指令逐字）——相等 |
| 自證 ② | 判別力[必相異]：二情境之紀錄集相異 |
| 自證 ③ | 判別力[必為零]：一**執行期組出**之人造地號於紀錄集命中 `0` |
| 自證 ④ | 缺幾何或缺未捨入面積者**逐筆 loud 具名**（⛔ 靜默略過·⛔ 補算） |
| 出艙 | 逐情境筆數·檔 bytes·`sha256`·`CR` |

🔒 **強制格集之執行期產出**：`{(情境, 街廓, 側)}` ＝ 本次實際發生強制呼叫者（街廓取自工項三 `(c)` 之鍵名之值）——與款 `1` 之格集**對照出艙**（⛔ 預設其相等）。

### 六-3　款 `3`　**實走之強制呼叫處之在域對拍**（**逐次·全量**）

每一次強制呼叫（期 `21` 次·⛔ 抽樣），以 `sys._getframe(1).f_locals`（**鍵須自證存在並增第二鍵**）**唯讀**取值：

| 欄 | 內容 |
|---|---|
| `(甲)` | 強制呼叫時，以工項三 `(d1)` 所得之**鍵鏈逐字**自登記表讀該 (街廓,側) 之 `mid`（`float.hex` 逐分量）；其後**同一進入序號內**同 (街廓,側) 之**首次** `_first_corner_alloc_dir` 實參（同記法）——**逐分量位元相等否** |
| `(乙)` | 同上二時點之**登記表物件**：`is` 同一否（器持其參照·**⛔ 以 `id` 為錨**）＋ 該 (街廓,側) 項之內容 `sha256`（鍵排序之 JSON·浮點以 `float.hex`） |
| `(丙)` | 同一進入序號內，同 (街廓,側) 之 `_solve_G_one` 是否出現 `is_corner` 真之呼叫（`True`／`False`） |
| `(丁)` | 工項三 `(e1)` 鏈上**於呼叫時已綁定**之名之值（`repr`·截至 `200` 字·截斷者標記）；**未綁定者記「未綁定」**（⛔ 補算） |

🔒 (街廓,側) 於其後呼叫之取法 ＝ **承用 `probe_WG9306R_reach.py` 之 `find_blk`／側之取法逐字**（⛔ 另寫第二份）。
🔒 **四禁仍在**（`W-G.9-301 §六-3` 逐字）：⛔ 新增形參、⛔ 改簽章、⛔ 新增 `import` 於生產碼、⛔ 自寫第二套幾何——本器**只讀既有之值**、**⛔ 呼叫 `_first_corner_alloc_dir` 或任何生產函式以求反事實之值**。

### 六-4　器紅與出艙即止

> **器紅**（停、上呈）：三包裝或進入計數之任一觸發數 ＝ `0`；款 `2` 自證 ①〜③ 任一不過；款 `3` 之列數 ≠ 強制呼叫總數。
> 🔒 款 `3` 之 `(甲)` 不相等、`(乙)` 非同一、`(丙)` 為 `False` 者——**⛔ 停**，逐次照實具名，裁屬發單側。
> 🛑 **⛔ 判改法、⛔ 判可施與否、⛔ 擬任何 diff、⛔ 開分支、⛔ 動用 KL 之放行。**

---

## `§七`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **落檔 `open(p,'wb')`**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | 本批異動之 `.md`／`.py` 之 `CR`（倉內 blob·取 bytes） | **`0`**；判別力[必非零] ＝ `data/V6.dxf` |
| `3'` | 本批新增之 `.log`／`.jsonl` 之 `CR` | **`0`**（坑 `bn`） |
| `4` | 四簿期末（**器之輸出**） | 自誤 相異 **`406`**／MAX **`416`**；`K-9` `29`／`30`·`GB` `168`／`170`·`VR` `80`／`95` **一字未動** |
| `5` | 坑之期末（**器之輸出**·`rev` 釘死·全量落檔） | **`94`／`94`**·末 **`bp`**；母體 `.md` 檔數之增量 ＝ `git diff --name-only --diff-filter=A` 之 `.md` 數（坑 `bl`／`bo`） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 **機械順序**：佔位符 ⇒ 跑 ⇒ 填；受詞係 `index` ⇒ 復跑前先 `git add <路徑>` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`app.py` ＝ `4379108a…`；`baselines` `298`·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-30` ⬜ **未實作**；物證落檔 `22` 檔 ＋ 對照物 `WG9287R_landeffect.log` **逐位未動**；`probe_WG9306R_reach.py`／`probe_WG9287_landeffect.py` blob 期初＝期末 |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 `20` 支；`gb170` 命中 **`0`**；`p3a` 命中 **`1`** |
| `6''` | 本波十一單之在倉性（`§一` 閘 `10` 之產生指令） | **`11`** **皆態1** |
| `6'''` | **逐款回掃之出艙**（坑 `bm`） | 本報告內全部「某框／某法有 X 缺陷」之警告，逐項及其對後續各款之回掃結果 |
| `6''''` | **標籤對照之出艙**（坑 `bo`·🆕 `自誤 416`） | 本報告全部含範圍詞、指代詞或**計數標籤**之欄，逐欄具名其指涉之集合與基數（**以程式自資料列計數**） |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§八`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**（射程逐字見 `§三`／`§五`／`§六` 首段·⛔ 逾之）：`ALPHA_LABELS` 新增一列；新增 `probe_WG9307R_sites.py`／`probe_WG9307R_prepin.py` 及其落檔；執行 `probe_WG9287_landeffect.py` 一次；`run_verification` 全量**一次**；行程內包裝 `§六` 所列之四物。
其餘：⛔ 改生產碼 `34` 檔**一字**；⛔ 開任何分支（含 `verify/W-G.9-299-gb170`）；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續 `W-G.9-308`；
⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**；⛔ `WV_BAKE`；**⛔ 引任何自 baseline 取得之數**；
⛔ 改任何函式簽章、⛔ 新增任何形參、⛔ 新增任何 `import` 於生產碼、⛔ 改任何 `*_EXPECT` 一字；
⛔ 自寫第二套幾何或第二份 `W`／`side_mid`／側界線／街廓識別之定義（`GB-48` 族）；⛔ 為求反事實之值而呼叫任何生產函式；
⛔ 動任何既有探針一字（含 `probe_WG9306R_reach.py`·`probe_WG9287_landeffect.py`·`probe_WG9269_pit_index.py` 之既有列）；
⛔ 鑄 `GB`／`VR`／`K-9` 之任何號（**`自誤 416`／坑 `bp` 為本批唯二之鑄**）；⛔ 解除或收窄任何 `GB`；⛔ 落地任何 `K-9` 子項；
⛔ 追改任何已入倉文件一字；⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文；
⛔ 判改法、⛔ 判可施與否、⛔ 擬任何 diff 或 fallback 之實作形、⛔ 判孰是孰誤、⛔ 提修法主張；
⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
**⛔ 估算或出艙本窗 context 之百分比**（`自誤 410`）——惟遇**可觀測之硬限事件**一律 loud 照實具名、停、上呈；
⛔ 以雙引號內嵌含反引號之碼於 shell 命令列（坑 `bp`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述）。

---

## `§九`　報告之格

`§二` 工項零 ⇒ `§一` 開工閘（**十四項**）⇒ `§三` 工項一（`自誤 416`·坑 `bp`·`ALPHA_LABELS`·三框交叉誤中分列）⇒ `§四` 工項二（`GB-170`·反向機驗）⇒
`§五` 工項三（款 `1` 之框與殘餘全量 ⇒ `S1`〜`S7` 對照表 ⇒ 款 `2` 逐處一節 `(a)`〜`(f)` ⇒ 判別力造）⇒
`§六` 工項四（款 `1` 釘紅 ⇒ 款 `2` 逐宗基線之自證 ⇒ 強制格集對照 ⇒ 款 `3` 逐次表**全量**）⇒
收工閘（入倉後末端追加·閘 `7` **出艙於回報·⛔ 入倉**）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**，且該指令須**逐字原樣執行過**；**母體一律以產生指令界定**；**「出現」以構造上之定義界定**；**計數標籤以程式自資料列計數**。
🔒 判定組為空一律 **loud 拒測**並**照實具名**；自捕為零者須併具「**⛔ 讀為『本批無誤』**」；凡自捕於出艙前攔下、照實併記（**⛔ 鑄號**·鑄號候發單側）。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`acceptance_audit`／`deletions`／`cat-file`／`merge-base`／
`append_audit`／`ZW416`／`PITBP`／`GB307-BEGIN`／`ALPHA_LABELS`／`ls-tree`／`get_source_segment`／`f3_cad_side_lines_by_side`／`_side_mid_left`／
`probe_WG9307R_sites.py`／`probe_WG9307R_prepin.py`／`probe_WG9287_landeffect.py`／`WG9287R_landeffect.log`／`WG9307R_lots_pre.jsonl`／
`_corner_buffer_S`／`_solve_G_one`／`_first_corner_alloc_dir`／`_run_step_g_impl`／`is_corner`／`find_blk`／`f_locals`／`float.hex`／`wkb`／
`run_verification`／`baselines`／`numstat`／`sha256`／`WV_BAKE`／`W-G.9-299-gb170`／`p3a`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: eddd84f7db52caced32103bce1bd9a3fe707eea65dbf2ada00a4dfe6ad955acb
