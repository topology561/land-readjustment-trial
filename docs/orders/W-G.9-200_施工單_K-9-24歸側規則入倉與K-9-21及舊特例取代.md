# 🔧 `W-G.9-200` 施工單：**`K-9-24` 入倉（步驟 0 回歸無條件 ＋ 歸側四分支 ＋ `p1` 平手）·取代 `K-9-21` 與舊特例 ＋ 鑄 `GB-137` ＋ `GB-135` 解除**

> **性質**：🟢 **全單零生產碼**（⛔ 不動 `app.py` 一字·⛔ 不動任何 `verify/**.py` 一字）⇒ **CC 可逕行 `push`**。
> **出單窗**：claude.ai（接 `W-G.9-199R`）
> **基座**：`wip/s1-endpart` 之現時 `HEAD`（判準見 `S-0a`·🔒 綁**性質**·⛔ 綁 commit 身分·承 `自誤 246`／`252`）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **事由**：KL 於 `2026-09-01` 就「步驟 0 之循環依賴」作成新裁，**取代** `K-9-21`。本單僅將該裁逐字入倉並清理其連帶，**⛔ 動任何生產碼**。落地由重發之 `W-G.9-198` 辦。

---

## §零　落地點（單一 commit）

| # | 檔／物 | 動作 |
|---|---|---|
| `L-1` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | **檔尾追加** `### 🔒 K-9-24　…` 全節（`§二-1`·逐字） |
| `L-2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | **檔尾追加** 新鑄 **`GB-137`**（`§二-2`） |
| `L-3` | 同上檔 | **檔尾追加** **`GB-135` 之解除**（`§二-3`）🛑 ⛔ 鑄號·⛔ 不改 `GB-135` 原節一字 |
| `L-4` | `CLAUDE.md` | **檔尾追加** 裁摘要 ＋ ⬜ 待落地警語之更新（`§二-4`） |
| `A-1` | `docs/orders/W-G.9-200_施工單_K-9-24歸側規則入倉與K-9-21及舊特例取代.md` | 本單以原名入倉 |
| `A-2` | `docs/reports/W-G.9-200R_K-9-24入倉_執行報告.md` | 執行報告（**同一 commit**） |

🛑 **`L-1`〜`L-4` 皆為<u>末端追加</u>**——⛔ 不得就地改寫任何既有文字一字；`deletions` 須逐檔為 `0`。
🛑 **⛔ 得動** `app.py` 之 `k6_step0_block_locked`／`k6_merge_groups`／`k6_shares_segment` 一字（其死碼化由重發之 `-198` 辦）。

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之**性質**（⛔ 綁 commit 身分） | ① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` **逐列⛔ 含** `app.py`、⛔ 不含任何 `verify/**.py`。任一不符 ⇒ 停機上呈。🛑 `ls-remote` 之全 `40` 碼須記錄並回報，但⛔ **作為判準** |
| `S-0b` | `probe_order_preflight.py` 對本單單檔 | `P-3`／`P-5` 須不紅；🟡 逐項具名（`§五`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗（受詞 ＝ 自檔首至該列前一個 `\n` 含之） | `declared` ＝ `recomputed` |
| `S-0d` | 🔴 **量測與動作之分離**（`CLAUDE.md` 常規補款 `一`） | `§四` 各驗收閘之量測與 `commit`／`push` **⛔ 得置於同一不可中斷之命令塊**；須貼出二者之呼叫界線 |

---

## §一　取號現查（發單側·態 `6c2b5f5`）

**母體**　`git ls-tree -r --name-only HEAD` ⇒ 追蹤檔 **`1561`【檔】**／`.md` **`496`【檔】**。

**`K-9` 系列**　母體 ＝ `docs/rulings/K-6_…md` 單檔·框 `` ^### 🔒 K-9-([0-9]{1,3})　 ``（框 ＝ **列**）⇒ 現存最大 ＝ **`23`** ⇒ 次號 **`24`**。
🛑 **扣除集 ＝ `K-9-99`**（**陽性對照哨兵**·定義出處 `docs/reports/W-G.9-89b_CC交接文.md:72` 逐字「刻意造不存在之錨·須 `0`」）。
**意指占用**（全倉 `.md` `496`【檔】·錨定框 `(?<![0-9\-])K-9-24(?![0-9])`）＝ **`0`【列】**；對照組 `K-9-23` 非零（CC 實測並報）⇒ 量測器非紅。

**`GB`**　母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔·扣除集 ＝ `GB-997`（`S-4` 人造哨兵）⇒ 相異 `73`／MAX **`136`** ⇒ 次號 **`137`**。

🔒 **`GB-137` 之意指占用 ＝ `19`／`19`【列】——依<u>裁 `H`</u>逐筆歸類**（🛑 ⛔ 以計數為判準）：

| 出處（逐筆） | 歸類 |
|---|---|
| `CLAUDE.md:1213` | 裁 `H` 之事由敘述 ⇒ **判別力對照** |
| `docs/orders/W-G.9-197_…:269` | `V-6` 宣告列本身 ⇒ **判別力對照** |
| `docs/orders/W-G.9-199_…:26`／`:77`／`:110`／`:111`／`:173`／`:216` | 皆為 `V-6` 恆假之案由、`自誤 249` 之反證、裁 `H` 條文、`V-5` 之新形 ⇒ **判別力對照／零對照** |
| `docs/reports/W-G.9-197R_…:141`／`:142`／`:145`／`:149` | `V-6` 之實測與扣除後受詞 ⇒ **零對照** |
| `docs/reports/W-G.9-199R_…:81`／`:98`／`:161`／`:163` | `V-5` 之裁 `H` 形實測與逐筆歸類 ⇒ **零對照** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md:4802`／`:4831`／`:4832` | `自誤 249`／`251` 之案由 ⇒ **判別力對照** |

⇒ 🔒 **`19` 筆逐筆皆為判別力對照／零對照**，依 `CLAUDE.md`「🔧 二」**⛔ 構成意指占用** ⇒ **`GB-137` 可取·⛔ 順延**。
🔑 **對照組**：`GB-138` ＝ `0`／`0`【列】；`GB-134` ＝ 非零（CC 實測並報二數）⇒ 量測器⛔ 恆零亦⛔ 恆非零。

**單號**　`W-G.9-200` 全倉 `.md` ＝ `0`【列】（對照組 `W-G.9-199` 非零 ⇒ 量測器非紅）。
🛑 **本單⛔ 鑄任何自誤號、⛔ 鑄任何 `VR` 號。**

---

## §二　payload（🛑 逐字追加·⛔ 不改一字）

### §二-1　`L-1`　`K-6` 檔尾追加 `K-9-24` 全節

```
### 🔒 K-9-24　**步驟 0 回歸「無條件」；同時跨占左右兩街角者依「歸側四分支」定其側**（KL 裁 `2026-09-01`·`W-G.9-200`）

> **KL 逐字**：「兩側都有街角的街廓還是依步驟 0，將同歸戶相鄰土地合併，分配時往街角地評比指數較大者開始為街角第 1 宗土地……這 `628(1)+` 跨占左側街角規定範圍的街角指數 > 跨占右側街角指數，則 `628(1)+` 就為左側街角地第 1 宗土地，此時 `628-1(1)` 因被併成 `628(1)+`，所以就不能再視為有跨占到右側街角地最小規定範圍了，此時右側街角地就照街角地評選機制，扣除 `628-1(1)` 外的其他跨占土地有無 winner，若無其他 winner，右側街角地當然為強制抵費地」
> **KL 補裁（同日·三問）**：① `K-9-21` 記為取代；② 舊特例採**乙·一律取代**；③ 歸側後採**甲·仍須比 winner**；④ 平手**沿用 `p1` 側**。

🔒 **一（步驟 0 回歸無條件·`K-9-21` 取代）**
`K-6 §二 步驟 0`（`K-6:57-60`）之「同街廓 ∧ 同歸戶 ∧ 相鄰 ⇒ 視為同一筆」**回復其逐字之「前置·無條件」**，⛔ 無任何例外。
🛑 **`K-9-21`（KL 裁 `2026-08-24`·`W-G.9-134`）自本裁起<u>經取代而不再適用</u>**（⛔ 上文原節一字未刪·`K-6:3134` 起）。其所立之例外（「已各自定案之左右街角第 1 宗⛔ 不合併」）**⛔ 適用**。
🔑 **取代之理由（KL 裁之效果）**：`K-9-21` 之判準三項 `(a)(b)(c)` 皆係**段一之產物**，而步驟 0 在段一之前 ⇒ **循環依賴**（求不到值）。本裁使步驟 0 只依賴**重劃前地籍事實**（同街廓／同歸戶／相鄰），⛔ 依賴任何評選結果 ⇒ **資訊單向流動、⛔ 有回路**。

🔒 **二（歸側四分支·⛔ 綁塊名·⛔ 綁側標）**
設 `M` 為一宗（**⛔ 論其係步驟 0 合併所生，抑或地籍上本來就是一片**）同時跨占**同一街廓左右兩街角**之規定範圍者：
1. **分別**以左、右街角**第 1 宗位置**試算 `M` 之 `G` 與街角指數，並使 `M` **在各該側照常參與該側之評選**（達標 ∧ 於達標者中比街角指數取 winner）。
2. **兩側皆拿得下** ⇒ 歸**街角指數數值較大**之一側；**兩側指數並列 ⇒ 取前緣線 `p1` 側**（決定性·**禁隨機**）。
3. **僅一側拿得下** ⇒ **歸該側**。🛑 **⛔ 論該側之指數數值是否較小。**
4. **兩側皆拿不下** ⇒ 兩側皆不歸；`M` **辦原位次**。
5. **一經歸定，另一側<u>視為未被 `M` 跨占</u>**；該側扣除 `M` 後照常評選其餘跨占者；**若無 winner（含候選集合為空）⇒ 強制留設抵費地，範圍嚴格等於該街角規定範圍**。

🛑 **「拿得下」之逐字定義** ＝ `G ≥ 該側街角規定面積`（達標）**∧** 於該側**全部達標候選**中之街角指數為最大（＝ 該側 winner）。⛔ 得以「第 1 名」單獨代之——第 1 名而未達標者**⛔ 拿得下**。
🔒 **`M` 之名稱與位次**：依 `K-6:57-60` 逐字，`{投影序在前者之暫編地號}+`、佔**單一原位次**、面積相加、幾何取聯集；原地號之保留依 `K-9-22`（⛔ 變更一字）。

🔒 **三（舊特例之取代·KL 採乙）**
`K-6 §二`「**特例：一片同時跨占同街廓左右兩街角**」（`K-6:97-103`）之「以垂直於前緣線之分割線…**真切為兩片**」及其四款分支，**自本裁起經取代而不再適用**（⛔ 上文原句一字未刪）。⇒ **⛔ 得再切開**；一律改依**二**之四分支。
🔑 **保留者**：該特例之**平手法**（「兩側指數並列 ⇒ 取**前緣線 `p1` 側**，決定性，禁隨機」）**逐字保留**，即**二·2** 之末句。
🔑 **取代之理由**：切開會使已各自算定之左右街角地面積被打散重來（本案 `R4` 之 `1521.08`／`458.48` 將變為約略相等），與 `K-6 §五` 逐字「**已定案者永不重開**」相牴觸；`K-9-21` 當初即為防此而設，本裁改以「不切、歸側」達成同一保護。

🔒 **四（與段一「上鎖」之關係·⛔ 新增規範·僅為受詞之澄清）**
`K-6:66-67` 之上鎖（「不得被任何合併群抽走，亦不得作為合併標的」）**逐字⛔ 變更**。惟因步驟 0 已回歸無條件且完成於段一之前，**上鎖對步驟 0 已⛔ 有可作用之對象**；其實際受詞 ＝ **段三之合併群操作**（`K-6 §二 段三 1`「取該合併群中**未上鎖**之其他片」）。
⚠️ **本項係自正典逐字所導之推論**，⛔ 非新裁；若 KL 認為不然，逕予駁回即可。

🔒 **五（本案之實例·⛔ 非本裁之一部·僅供溯源）**
`R4` 之 `628(1)`（左·真 `G` `1521.05`·門檻 `226.01`）與 `628-1(1)`（右·真 `G` `457.73`·門檻 `225.42`），同歸戶 `G009`、同街廓、共用界線 `47.057897 m` ⇒ 依**一**照常合併為 `628(1)+`。
⚠️ 合併後之左右指數**須實算**（`W-G.9-201`）。以分別計算之現行值為參考：左總分 `0.9916`／右總分 `0.9925`（差 `0.0009`，唯一差源為正街角分 `0.392` vs `0.3929`）⇒ **可能歸右**，左街角則因⛔ 有其他跨占者而依**二·5** 成為強制抵費地 `226.01 ㎡`。🛑 **此為推定·⛔ 得作為判準或寫入碼。**

⛔ **本裁不辦**：⛔ 不變更 `K-6 §一` 合併群述詞一字；⛔ 不變更 `K-9-22`（表示層）一字；⛔ 不變更段一上鎖、段二鍵序、段三、段四之任何一字；⛔ 不授權任何重烤或重產基準；⛔ 不變更 `K-9-18`（該側有 `SIDELINE` ⇒ 該側有街角地）。
```

### §二-2　`L-2`　新鑄 `GB-137`

```
### `GB-137` 🆕

**編號現查**（`W-G.9-200`·發單側·態 `6c2b5f5`·母體 ＝ 本檔單檔·扣除集 ＝ `GB-997`）：相異 `73`／MAX `136` ⇒ 本則取次一號 ＝ **`137`**。
🛑 **依<u>裁 `H`</u>**：`GB-137` 之意指占用（母體 ＝ `git ls-tree -r HEAD` 之 `.md` `496`【檔】·錨定框·二形並取）＝ `19`／`19`【列】，**逐筆歸類皆為判別力對照／零對照**（逐筆出處見 `W-G.9-200` `§一`）⇒ 依「🔧 二」**⛔ 構成占用** ⇒ **可取·⛔ 順延**。對照組 `GB-138` ＝ `0`／`0` ⇒ 量測器⛔ 恆非零。

| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |
|---|---|---|---|---|---|---|
| **`GB-137`** 🆕 | 🔴 **正典本文之條文被<u>檔尾新裁</u>取代，而<u>原條文所在處⛔ 有任何指標</u>** ⇒ 逐字讀正典本文者將讀到一條**已失效之規則**，且⛔ 無從自該處得知其已失效。**具名實例（本批新增二處）**：① `K-6:57-60` 步驟 0 之「前置·**無條件**」曾被 `K-6:3134` 之 `K-9-21` 加上例外，原處⛔ 無指標；② `K-6:97-103` 之「同時跨占左右兩街角 ⇒ **真切為兩片**」特例，自 `K-9-24` 起⛔ 適用，原處⛔ 無指標。**成因 ＝ append-only（⛔ 得就地改寫）與「本文之可讀性」二者之結構性張力。**🔑 **本則之形**：**一條規則之現行效力，繫於讀者是否恰好讀完全檔並自行拼合。**⛔ 綁 `K-6`、⛔ 綁本案。 | **凡以檔尾追加之新裁取代／限縮正典本文既有條文者 ⇒ 【必觸】。** | 🔒 **⛔ 本則只登記·⛔ 零生產碼·⛔ 不就地改寫任何正典本文一字。****現況處置** ＝ 令爾後每一則**取代型**之裁，其節內須設一「**受詞行號索引**」段，逐筆列出所取代之正典本文位置（如 `K-6:57-60`／`K-6:97-103`），使**反向查找**（由本文行號 → 現行效力）成為機械可為。`K-9-24` `§三` 已照此寫。 | 字樣錨：`grep -n "^### 🔒 K-9-24" docs/rulings/K-6_…md`；`grep -n "^### 特例：一片同時跨占同街廓左右兩街角" docs/rulings/K-6_…md` | ✅ **有**——`K-9-21` 自 `2026-08-24` 至 `2026-09-01` 期間，`K-6:57-60` 之「無條件」逐字未變而實際已有例外；`W-G.9-196` 交接窗即據該本文逐字作出過推論 | ❌ **不需**——純可發現性缺陷·⛔ 無土地後果 |

🔒 **失效條件（逐字）** ＝ 「倉內存在一份**機械產生**之對照表，其自 `docs/rulings/**` 之每一條被取代／限縮之本文位置，指向其現行有效之裁；且該表之產生可由單一命令復現」。

🩸 **同族**　`GB-135`（待裁事項只存在於生產碼註解）之姊妹——二者同為**可發現性**缺陷，惟本則之藏身處係**正典本文與檔尾之距離**。
```

### §二-3　`L-3`　`GB-135` 之解除

```
## 🔒 `GB-135` **解除**（`W-G.9-200`·⛔ 上文原節一字未刪·⛔ 鑄號）

🔒 **失效條件逐字（引 `GB-135` 本節）** ＝ 「該註解所載之待裁事項，已於 `docs/rulings/` 或某一登記表內具名登記，且其失效條件逐字可查」。

**現查**：該註解（`app.py` 之 `k6_step0_block_locked`）所載之待裁事項 ＝ 「`K-9-21` 循環依賴之解」。
🔒 **KL 已於 `2026-09-01` 作成該裁**，並以 `K-9-24`（`docs/rulings/K-6_街角地分配程序與可分配判準.md` 檔尾·`### 🔒 K-9-24　`）**逐字入倉**。
⇒ 🔴 **失效條件成就<u>且不止於成就</u>**——該待裁事項⛔ 僅「已登記」，而係**已裁決且循環依賴本身已消滅**（步驟 0 回歸無條件 ⇒ ⛔ 依賴任何評選結果）。

🛑 ⇒ **`GB-135` 解除。**
🩸 **其一般式⛔ 隨之消滅**：`自誤 248` 之一般式（「凡出艙『落地須如何如何』之要求者，窮舉檢索之射程須含 `app.py`／`verify/**.py` 之註解」）**仍為有效常規**，⛔ 受本解除影響。

⬜ **連帶之待落地（⛔ 本單辦·由重發之 `W-G.9-198` 辦）**：`app.py` 之 `k6_step0_block_locked` 及其呼叫點，自 `K-9-24` 起**成為死碼**（步驟 0 已無條件）⇒ 須隨 `K-9-24` 之落地一併處置。🛑 **本單⛔ 動其一字。**
```

### §二-4　`L-4`　`CLAUDE.md` 檔尾追加

```
## 🔒 `W-G.9-200`：`K-9-24` 入倉（KL 裁 `2026-09-01`）

### 🔴 **三項取代**（逐字見 `docs/rulings/K-6_…md` 檔尾 `### 🔒 K-9-24　`）

| 受詞（正典本文位置） | 原效力 | 現行效力 |
|---|---|---|
| `K-6:57-60` 步驟 0 | 「前置·無條件」，惟自 `K-9-21` 起有例外 | 🔒 **回歸「前置·無條件」**·⛔ 無例外 |
| `K-6:3134` `K-9-21` | 已各自定案之左右街角第 1 宗⛔ 不合併 | 🛑 **經取代·⛔ 適用** |
| `K-6:97-103` 特例 | 一片同時跨占左右兩街角 ⇒ **真切為兩片** | 🛑 **經取代·⛔ 得再切開**；改依 `K-9-24` **二**之四分支。**平手法（`p1` 側·禁隨機）逐字保留** |

### 🔒 **歸側四分支**（`K-9-24` **二**·⛔ 綁塊名側標）
`M` 同時跨占同街廓左右兩街角規定範圍者：**兩側皆拿得下** ⇒ 歸指數大者（並列取 `p1` 側）／**僅一側拿得下** ⇒ 歸該側（⛔ 論指數大小）／**兩側皆拿不下** ⇒ 辦原位次。一經歸定，另一側**視為未被 `M` 跨占**；該側扣除 `M` 後無 winner（含候選為空）⇒ **強制抵費地·範圍嚴格等於街角規定範圍**。
🛑 「拿得下」＝ **達標 ∧ 為該側 winner**；⛔ 得以「第 1 名」代之。

### 🔒 **循環依賴已消滅**
步驟 0 現只依賴重劃前地籍事實（同街廓／同歸戶／相鄰）⇒ **資訊單向、⛔ 有回路**。`GB-135` 已解除。

### ⬜ **待落地（序·⛔ 得跳）**
1. 🔴 **`K-9-24` 之落地**——步驟 0 去除保守替代（`k6_step0_block_locked` 死碼化）＋ 歸側四分支。**幾何影響·有土地後果。**
2. ⬜ `K-6-B` **段一之上鎖**（現行碼⛔ 存在；其受詞已澄清為**段三**之合併群操作）。
3. ⬜ **街角指數名次之產出與出艙**（`GB-133` 剩餘肢）＋ **段二全域排序**。
🛑 **`W-G.9-198` 之舊版已因本裁而前提消滅**（其 `§零` 明令「⛔ 不得動 `k6_step0_block_locked` 之保守替代一字」）⇒ **⛔ 得依舊版開工**，須重發。
```

---

## §三　驗收（🛑 判別力對照依<u>裁 `H`</u>之形）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `V-1` | 零生產碼 | `git diff --name-only <基座>..HEAD` **逐列⛔ 含** `app.py`、⛔ 不含任何 `verify/**.py`；且 `HEAD:app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` |
| `V-2` | append-only | `git diff --numstat <基座>..HEAD` **逐檔 `deletions` ＝ `0`** |
| `V-3` | 🔑 **payload 逐位落地** | 自本單 `§二-1`〜`§二-4` **四**個 fence 抽出之文字，與其落入之檔尾對應區段**逐位元組相同**（各報 `sha256`） |
| `V-4` | 取號 | 落地後 `K-6` 之 `` ^### 🔒 K-9-([0-9]{1,3})　 `` 最大（扣哨兵 `K-9-99`）＝ **`24`**；`GB`（扣哨兵）MAX ＝ **`137`**；🛑 自誤 MAX **仍 ＝ `252`**、`VR` MAX **仍 ＝ `86`**（本單⛔ 鑄二者） |
| `V-5` | 🔒 **判別力對照（裁 `H` 之形）** | 取 `K-9-25` 與 `GB-138` 之錨定框全倉 `.md` 命中，**逐筆歸類**須逐筆皆為零對照／判別力對照（含本單自身之宣告列）。🛑 **命中數⛔ 判準**；🔴 命中非 `0` 而未逐筆具名歸類 ⇒ 停機。對照組 `K-9-23`／`GB-134` 皆非零 ⇒ 量測器⛔ 恆零 |
| `V-6` | 🔴 **取代之可追溯性自證**（`GB-137` 之受詞） | 落地後須能以**單一命令**自 `K-9-24` 節內取出其所取代之本文位置清單（`K-6:57-60`／`K-6:3134`／`K-6:97-103`），且該三處**原文逐字仍在**（append-only 自證） |
| `V-7` | `S-0d` 之自證 | 貼出 `§三` 各閘之量測與 `commit`／`push` **分開呼叫**之界線 |

🛑 **⛔ 得以「計數相符」為單獨之驗收判準**——`V-3` 之逐位比對為 `L-1`〜`L-4` 之唯一 operative 判準。

---

## §四　commit 與 `push`

- **單一 commit**。訊息建議：`W-G.9-200：K-9-24 入倉（步驟 0 回歸無條件＋歸側四分支）·取代 K-9-21 與舊特例 ＋ 鑄 GB-137 ＋ GB-135 解除 ⛔ 零生產碼`
- 🟢 零生產碼 ⇒ CC 逕行 `push`。
- 🛑 **執行報告 `A-2` 併入同一 commit**；🛑 惟其量測須與 `push` **分開呼叫**（`S-0d`）。

---

## §五　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6`）

| 檢 | 觸發點（字樣錨·`grep -F` 固定字串·⛔ 不用行號） | 處置 |
|---|---|---|
| `P-1` | `` 🔒 **一（步驟 0 回歸無條件·`K-9-21` 取代）** `` | ✅ **已採納**——該段之全稱否定（「⛔ 無任何例外」）其母體已於同段逐字具名 ＝ **`K-6 §二 步驟 0`（`K-6:57-60`）之合併規則**，⛔ 非對倉之全稱否定；所取代之受詞位置（`K-6:3134`）亦同段具名 |
| `P-4` | `` \| # \| 阻塞項 \| 觸發條件（換案） \| 現況處置 \| 倉內出處 \| 真資料覆蓋 \| 需 KL 域裁？ \| `` | 🔒 **具名豁免**：係 `GB` 登記表之既有制式表頭（⛔ 本單所創），其下單列之各欄自載母體與方向 |

🔒 **發單側已實跑**（`probe_order_preflight.py` 對本單單檔）：🔴 機械停機款 **`0`【項】**；🟡 **`2`【項】**，逐項即上表（`P-1` `1`／`P-4` `1`）；`P-5` `SELF_SHA256` ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。
🔑 **判別力自證**：同器對 `W-G.9-197` 得 `5`【項】、`W-G.9-198`（改版）得 `6`【項】、`W-G.9-199` 得 `0`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。
🛑 CC 實跑若出現**表外**之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
SELF_SHA256: 78d42fbd567a5576f8ea58a365952ed82bacc8ef2c732dacb9182a95fe6c631b
