# 🔧 施工單 `W-G.9-189`　幾何本體批 **第二批**（`K-9-17` 遞補）

> 發單側：`claude.ai`（`W-G.9-188` 窗）　受單側：CC
> 前批：`W-G.9-187R`（已驗收·`761a4fa`）　基座：`761a4fa`
> 🛑 **本批會改變配地結果** ⇒ `X-T`（落地前後逐位相同）於本批 **⛔ 不適用**，改立守恆式為主閘。

---

## §零　開工閘（🛑 動任何一字之前逐項辦畢；任一不過 ⇒ 停機上呈）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，自算對拍。不符 ⇒ 停機。

**`S-1`（期初錨·全部現查·⛔ 不得照抄本表）**

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1378708` | `23412` | `2be36e4c88b6e129` |
| `CLAUDE.md` | `81200` | `903` | `3df01894ba38ccc5` |
| `verify/stepg_pipeline.py` | `96261` | `1454` | `0260273350021311` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `402254` | `4347` | `82084b330ff89b4d` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `529494` | `3238` | `499ce534e43f332e` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `258295` | `3632` | `6a5cada2901a10e8` |
| `docs/design/W-G.9-185_幾何本體批施工規格書.md` | `42908` | `574` | `dfd46da5825c645e` |
| `docs/reports/W-G.9-187R_幾何本體批第一批.md` | `44424` | `610` | `f8937e374e79d6db` |

`sha16` ＝ `sha256` 之前 `16` 碼。`app.py` 檔尾**無 `LF`**（末位元組 ＝ `)`）⇒ `wc -l` 得 `23411`，非 `23412`。

**`S-2`（倉態）**　`git ls-remote origin refs/heads/wip/s1-endpart`：
`= 761a4fa` 或其後裔 ⇒ 正常開工；為 `761a4fa` 之**祖先** ⇒ 🛑 停機。

**`S-3`（拋棄式 clone）**　新 clone、跑畢整份刪除，並於回報中以**對照組**確認刪除之量測器非紅。

**`S-4`（禁改清單）**　讀 `§五`。

**`S-5`（基準量·🛑 動任何一字之前）**　取本批之守恆基準：逐街廓 `run_step_g` 之
`[T2-DIAG]` 全文 ＋ **街廓面積守恆式各項之期初值**，落檔
`verify/out/probe_WG9189_baseline_761a4fa.log`（🔒 檔名須載**真實基座**）。

**🔑 `S-6`（複刻區同步閘·**本批新增**·🛑 停機款）**

倉內三處逐字自陳迴圈層係**人工複本**：
`verify/stepg_pipeline.py:35`（「約 `14650-15450` 之**逐行複刻**——該段波動**必同波更新本檔**」）／
`:345`（`_build_g_row` / `_solve_one` 之逐行複刻）／`:401`（逐街廓迴圈之逐行複刻）。
發單側已現查：`verify/` 內**⛔ 無任何比對此二區之自動閘**（`W-G.9-188` 窗·態 `761a4fa`）。

⇒ 動任何一字之前：

1. 逐段對拍 `app.py` Step G 迴圈區與 `stepg_pipeline.py:401` 起之複刻區，**出艙現存分歧清單**（逐項具名：僅縮排／僅註解／僅 `ns` 取用形 ⇒ 記為**形式差**；控制流、判斷式、賦值、呼叫序之任一差異 ⇒ 記為**實質差**）。
2. 同法對拍 `:345` 起之 `_build_g_row` / `_solve_one` 二份。
3. 🛑 **若出現任一「實質差」⇒ 停機上呈，⛔ 不得先寫遞補。** 理由：遞補整個落在此區，在已歪之基準上疊加即是假綠。
4. 僅有形式差 ⇒ 逐項具名後正常開工，並將該清單納入 `§四` 收工閘之期末重驗。

🔒 **出艙之數一律宣告框**：本批任何計數，須逐字載其**母體**與**框**；凡裸框與錨定框得數相異者，依 `CLAUDE.md:725` **二數並報**並具名其差之來源。⛔ 不得只報其一。

---

## §一　任務

### `N-1`　`K-9-17` 遞補之落地（生產碼）

**受詞（逐字·`docs/rulings/K-6_...md` `K-9-17` 段二·KL 裁 `2026-08-22`）**：

> 此時若 1 地號（第 1 宗土地，假設街角第為第 0 宗）若無法內接矩形成立而併入合併/調配機制，那麼該空位就由 3 遞補（空位位置係由第 0 宗遠側境界線開始依據遞補宗的 G 再決定第 1 宗遠側境界線，並注意宗與宗之間境界線不要交叉），而 1 地號空出之面積當然依據街廓面積守恆式……

🩸 併記：原文作「2 地號空出之面積」，KL 同日逐字更正「**是筆誤，應為被踢除的 1 地號。**」⇒ 以更正後為準。

**實作要件**：

1. 觸發源 ＝ `W-G.9-187R` 已落地之閘一／閘二（`_k923_gate1` / `_k923_gate2`）。**⛔ 不得另寫一套判準**。
2. 被踢除宗 ⇒ 退出該側序列，其**面積**依街廓面積守恆式回流；⛔ 不得就地縮小、⛔ 不得留空位。
3. 遞補宗之遠側境界線 ＝ 自**前一宗（第 `0` 宗或前一已定宗）之遠側境界線**起、依**遞補宗自身之 `G`** 決定。⛔ 非沿用被踢除宗之 `G`。
4. 🛑 **境界線⛔ 不得交叉**——落地後須逐街廓逐側機檢，非斷言。
5. 位次（`rank`）之重排須與 `K-9-17` 段一之街角重排機制一致；⛔ 不得自創序。

### `N-2`　生產路徑呼叫點（解除 `GB-127`）

`W-G.9-187R` 所落之 `_rect_fits_free_pose` ／藍影骨架／閘一二，於生產路徑**尚無呼叫端**（`GB-127`）。本批之遞補迴圈即其消費端 ⇒ 接上呼叫端後，**現查 `GB-127` 之解除條件是否成立**，成立則於 `W-G.4` 就地標註解除（⛔ 不刪原文、⛔ 不鑄新號）。

### `N-3`　守恆式之驗收（**本批主閘·取代 `X-T`**）

逐街廓出艙 `[T4-CONS]`（**逐街廓一列**·框 ＝ `` ^\[T4-CONS\] ``）：

```
[T4-CONS] 街廓 <label>｜期初 Σ配地面積 <a>｜期末 Σ配地面積 <b>｜被踢除 Σ <c>｜
          池增量 <d>｜Σ差 <b+c-a>｜池差 <d-c>｜判 <✅/🛑>
```

**判準**：`|(b + c) − a| ≤ 5e-4 ㎡` **且** `|d − c| ≤ 5e-4 ㎡`。任一街廓不成立 ⇒ 🛑 停機。

🛑 **判別力**（⛔ 不得省）：另跑一造，人為令某宗之 `G` 偏移 `+1.0`，須使該街廓之守恆式**轉紅**。恆綠 ⇒ 量測器紅、⛔ 非通過。

### `N-4`　隨單搭載（純登記·⛔ 零生產碼）

1. 🔒 **鑄 `自誤 224`**（發單側·次號現查：全倉 `.md` 之 `^###\s*自誤\s*\`?([0-9]{1,4})\`` MAX ＝ `223`）
   受詞逐字：**「已在手之檔只讀其一部分，即斷言其全檔查無。」**
   案由：`W-G.9-188` 窗於裁 `A` 斷言「CC 所引在倉內查無」，實則該引據在
   `docs/reports/W-G.9-186R_CC交接文.md:156`／`:245`／`:246`；而該檔正是發單側自己於同窗發現並回報之新增檔，僅讀前 `90` 列即出艙全稱否定。形甲第二度。
2. 🔒 **鑄 `自誤 225`**（發單側·承上取次號）
   受詞逐字：**「以裸框（無 `^` 錨）之得數為總數出艙，並據以否定受單側之正數。」**
   案由：裁 `三` 報 `[T3-GATE]` 逐宗列 `57`，實為裸框所致；錨定框 `` ^\[T3-GATE\] 街廓 `` 得 `39`，差 `18` ＝ log `§三` 末以 `    ▸ ` 起首、截至 `150` 字元之**摘錄重列**。同族：`VR` 裸框得 `999`／`自誤` 裸框得 `298`（`CLAUDE.md:671`）⇒ **裸框族第三度**，觸 `戒 32` 升階款、`CLAUDE.md:725` 二數並報款。
   🔒 併記：`57` 之分解「`39` 過 ＋ `18` 不過」**⛔ 不成立**；`39` 之綜合判分佈為**不配地 `18`／配地 `13`／不適用 `8`**。
   🔒 併記：受單側**二度**於前提為偽時拒絕照裁改倉、改以現查回報 ⇒ 記明為正辦。
   ⚠️ 二則之標題形，依 `常規四（九）四` **取既有最末一則之標題形逐字為範本**，⛔ 不得憑本單之寫法。
3. 🔧 `W-G.9-187R` 報告 `:368`／`:485`：**維持 `39`**，補宣告框 `` ^\[T3-GATE\] 街廓 ``，並依 `CLAUDE.md:725` 加註「裸框 `57`／錨定框 `39`，差 `18` ＝ 摘錄重列」。⛔ 不鑄號。
4. 🔧 `verify/wf_f0.py:9` 逐字「解算一律走 **app 真函式** `run_step_g`」係偽述——`run_step_g` 定義於 `verify/stepg_pipeline.py:238`。就地更正為「走 harness 之 `run_step_g`（其葉子函式 `_solve_G_one`／`_pool_strips_for_block` 經 `ns` 取 app 單一真相源）」。⛔ 不鑄號。

---

## §二　授權與 `push`

🔴 本批**有生產碼**且**改變配地結果** ⇒ `常規一` 及 `187 §二` 之替代款**皆⛔ 不適用**。

⇒ **`push` 前一律請示**，並於請示時同時提交：`§四` 全項、`[T4-CONS]` 全文、`S-6` 之分歧清單、`numstat` 逐 commit 逐檔。

🔒 `W-G.9-186R CC交接文:246` 之「生產碼批⛔ 不得逕行 ⇒ 須請示」於本批**照舊適用**，本單⛔ 不排除之。

---

## §三　停機款（任一觸發 ⇒ 🛑 停機上呈，⛔ 不先辦後呈）

- **`X-1`**　`S-6` 出現**實質差**。
- **`X-2`**　`[T4-CONS]` 任一街廓不成立，或其判別力造**未轉紅**。
- **`X-3`**　落地後出現**境界線交叉**（`K-9-17` 段二逐字禁止）。
- **`X-4`**　`run_all` 之任一 fixture 由綠轉紅，且經實測基座態後確認**係本批所致**。
- **`X-5`**　`app.py` 之改動落在 `§五` 禁改清單之任一處。
- **`X-6`**　任一取號之次號，其**錨定框**得數 ≠ `0`（⛔ 不得以裸框判之）。
- **`X-7`**　`K-9-17` 逐字重抽與 `§一 N-1` 所引不符。
- **`X-8`**　`deletions` 出現於**基座檔**（`app.py`／`CLAUDE.md`／`stepg_pipeline.py`／各登記表）而非同批新增之檔。

---

## §五　禁改清單（⛔ 一字不改）

1. `verify/run_all.py`、`verify/run_verification.py` 之**發現機制與名目集合**。
2. `docs/rulings/K-6_...md` 之任何既有條文（含 `K-9-17` 全文）。
3. `W-G.9-187R` 已落地之 `_rect_fit_*`／`_k923_*`／`_blue_shadow_tri`／`_t3_*` **之函式體**（⛔ 得新增呼叫端，⛔ 不得改其內部）。
4. `app.py` 之 `minimum_rotated_rectangle` **全部 `11` 處**。
5. 任何既有 `GB`／`VR`／`自誤`／`戒` 之**既有文字**（僅得末端追加）。
6. 各 `verify/baselines/` 之既有基準檔。

---

## §四　收工閘（全項齊備方得請示）

| # | 項 |
|---|---|
| `1` | `S-1`〜`S-6` 全過，`S-6` 分歧清單已出艙且期末重驗仍僅形式差 |
| `2` | `[T4-CONS]` 逐街廓 ✅，且判別力造**已轉紅** |
| `3` | 境界線交叉機檢 ✅（逐街廓逐側·非斷言） |
| `4` | `run_all` 全綠，或紅項經實測基座態證非本批所致 |
| `5` | `GB-127` 解除條件之現查已辦（成立則就地標註） |
| `6` | `N-4` 四項全辦；`自誤 224`／`225` 之標題形取自既有最末一則 |
| `7` | 取號現查：四類之**錨定框** MAX 與次號命中 `0`，並逐類載其框逐字 |
| `8` | `numstat` 逐 commit 逐檔已備；基座檔 `deletions = 0` |
| `9` | 倉內報告已撰：`docs/reports/W-G.9-189R_幾何本體批第二批.md` |

---

## §六　背景（⛔ 非指令·供判斷）

`W-G.9-187R` 已驗收：`[T3-GATE]` 逐宗列 `39`（錨定框），其中閘二不過 `18` 筆
（`R2左 8`／`R5左 4`／`R3右 3`／`R6右 2`／`R1右 1`），`閘一 True ∧ 閘二 False` 之
「假綠」`3` 筆，逐一落於 `R1右`／`R3右`／`R6右`——與 `W-G.9-185` 規格書之預言逐格相符。
本批即處理該 `18` 筆之後果。

葉子層（`_solve_G_one`／`_pool_strips_for_block`）係 `app.py` 之單一真相源
（`stepg_pipeline.py:327`／`:956` 逐字），**⛔ 非本批之風險**；風險全在迴圈層之人工複本，故立 `S-6`。

---

## §七　回報

`push` 後（經請示核可）單獨跑 `git log -1 --format="%h %s"`、原樣貼其輸出 ＋ 倉內報告路徑。
凡呈發單側之裁，列於報告 `§九`。凡數，逐項載其母體與框。

---

SELF_SHA256: 5df961612b26c0403a8190f9ebc90a9f1cd34aade2b533b2ef9a0dbabe9f4527
