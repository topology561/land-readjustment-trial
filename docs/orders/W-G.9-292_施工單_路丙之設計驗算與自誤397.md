# `W-G.9-292`　施工單（**設計驗算·零生產碼**）：路甲／路乙之否決 ／ **路丙**之設計驗算 ／ `自誤 397`

> **態**｜開工 ＝ `f7331bc43ba468d2d11173822397ceb60397eaa0`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-292_施工單_路丙之設計驗算與自誤397.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（設計驗算 ＋ 登記·**零生產碼**·⛔ 改碼一字·⛔ 開分支·⛔ 驅動生產路徑之替身）
> 🛑 **本批鑄號 ＝ `自誤 397` 一號**（逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔒 **KL 之放行仍在·仍⛔ 動用**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·**同格載產生指令**·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `f7331bc43ba468d2d11173822397ceb60397eaa0` 或其後裔（**全 `40` 碼**） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·🛑 `rc` **緊接該指令**取之） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>`（🛑 **`--selftest` 之 `argv` 位置須先自證其被讀入**·`W-G.9-291` 自捕一） | `SELF_SHA256` 逐位相符；判別力二造須**先自證 bytes 相異** |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**） | [必真] 含 `W-G.9-292` ＝ `True`／[必偽] 含 `W-G.9-291` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0`（**且須出艙其判別力靶之逐項結果**·⛔ 以 `rc = 0` 充其被讀入） |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`386`**／**`396`**｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`** |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec 多咬之子層檔數 **須 `> 0`** |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**、payload 之落地形機驗、函式名**二形並取**、頂層全域取 `t.body`、`rc` 緊接指令、
向量夾角**須釘槽並以實邊機驗**）**逐項適用·⛔ 省**。
🆕 **三則新附款**（`W-G.9-291` 自捕所立·⛔ 省）：
`(a)` **單字元／極短之名**⛔ 以子字串比對，一律以 **AST 節點**或詞界框取之；
`(b)` 「其後之用法」之框須**自賦值處之後**起算，⛔ 含其前之列；
`(c)` **型別歸類一律出艙其實測之節點型別**，⛔ 以概稱代之。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-291` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `0c052790bca7b076…`）／
生產碼 `34` 檔於 `7574727b..f7331bc4` 異動 **`0`**／自誤 `386`／`396`（**恰 `+3`**）·`GB`／`VR`／`K-9` 未動
⇒ **✅ 全批核可**。四則自捕皆於出艙前攔下，處置正確——尤以 `--selftest` 之 `argv` 位置被**靜默忽略**：
其為「跑了但沒被讀入而 `rc` 仍 `0`」之形，已立為 `§零` 閘 `5` 之附款。

---

## `§一`　裁

| # | 事 | **裁** |
|---|---|---|
| `1` | **路甲**（擴 `_build_corner_range_v3` 之回傳） | 🔴 **否決**。其接觸面 ＝ **`23` 處**呼叫端（`Assign(Name)` `14`／`Assign(Subscript)` `1`／`Return` `8`），須同批改。KL 放行之射程 `(b)` 逐字為「**只改該處**」⇒ `23` 處**遠逾**其射程；且⛔ 得以本批之技術理由擴 KL 之放行 |
| `2` | **路乙**（自 `corner_range_polys` 反求遠側境界線） | 🔴 **否決**。`4` 個 `forced` 消費端中 **`3` 個** `(iii) ⛔ 可達`；且反求須 `6` 項運算，其 `②` 須**另行重算線向**、`⑤` 即 **`S1_par` 之重算** ⇒ 正落「**另算第二份**」之禁 |
| `3` | 三處單／倉行號相異 | **照實受領·⛔ 追改單一字**。`stepg:657` ⇒ 實 `:655`；`app.py:13990 if is_corner:` ⇒ 實 `:13989`（`:13990` 係其被守護之賦值式）；追加段 `38` 列 vs `+37` ＝ **二框之別**（已二數並報·⛔ 鑄號） |
| `4` | 🔴 **發單側自認**：`W-G.9-291 §四(1)` 之**入倉 payload** 內以**行號**為錨，且其值與倉**相異一列** | 責 ＝ 發單側 ⇒ 鑄 **`自誤 397`**（`§四`）。🛑 **⛔ 追改該已入倉之 payload**——以本批之末端追加更正 |
| `5` | 下一步 | **⛔ 逕擬實作**。改辦 **路丙之設計驗算**（`§三`）——其形 ＝ **一個實參之改**，其接觸面**可能為 `1` 處**；惟**是否滿足準據 `B` 尚未算過** ⇒ 先算，⛔ 先改 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-292_施工單_路丙之設計驗算與自誤397.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-291` 之單須 `≥ 1`／造乙 ＝ 執行期組出之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（框 ＝ `tr -cd '\r' \| wc -c`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：**路丙**之設計驗算

### 三-1　路丙之形（**發單側明定·⛔ CC 擴之**）

**路丙** ＝ 於 `forced` 消費端，`_corner_buffer_S` 之 **`allocation_dir` 實參**改取
**`_first_corner_alloc_dir(side_mid)`**（＝ 街角第 1 宗所用者·`rot90_cw(SIDE)`），
而**⛔ 改** `_corner_buffer_S` 一字、**⛔ 改**其簽章、**⛔ 改** `_build_corner_range_v3` 一字。

**其由（發單側具名·⛔ CC 推定）**：`W-G.9-290` 已坐實該宗之 `_alloc_dir_used` 即
`_first_corner_alloc_dir(side_mid)`，其切邊 ∥SIDELINE（閘 `(iv)` `8`／`8`·`\|cos\|` ＝ `1.0`）。
⇒ 若帶之切向與規定範圍之遠側界**同向**，且其面積仍解至 `range_area`，**二者可能逐位同一**。
🛑 **「可能」二字係發單側之假說，⛔ 結論**——本項即為驗之。

### 三-2　**設計驗算**之界定（🛑 **⛔ 充實測**）

「設計驗算」＝ **於探針內**以**純幾何**複算一個**假設之輸入**所生之結果，**⛔ 改生產碼一字**、
**⛔ 驅動生產路徑之替身**、**⛔ 入倉為生產態之數**。其出艙一律冠 **`【設計驗算·⛔ 實測】`**。
🔒 本項**唯一**獲此授權；本波其餘一切「⛔ 反事實」之禁**不變**。

### 三-3　逐款出艙

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | `_first_corner_alloc_dir` 之**定義層級**（模組頂層？巢狀於何函式？）與其**於四個 `forced` 消費端之可達性**（三態·同 `W-G.9-289 §三` 之框） | 框 ＝ `ast` 之 **`t.body` 頂層**（⛔ `ast.walk`）；逐處具名 `檔:函式` |
| `2` | `_corner_buffer_S` 之簽章逐字，及其 `allocation_dir` 實參於**每一** `forced` 消費端之**當場值**（`repr` 全位） | ⛔ 推定；併出艙該處 `side_mid` 之可達性 |
| `3` | **【設計驗算】**：以 `_first_corner_alloc_dir(side_mid)` 為切向，解至**同一** `range_area`，所得之帶多邊形 | 🛑 複算須用**碼面同一之 `_block_strip`／`_strip_s_range`／bisect 容差**（逐字具名其來源），⛔ 自寫第二套幾何 |
| `4` | 🔑 該帶與 `corner_range_polys[(blk,side)]` 之 **對稱差面積**（及 `甲∖乙`／`乙∖甲` **分列**） | 期 **`0`**（或具名之幾何容差內）；🛑 **⛔ 為達期值而調任何容差** |
| `5` | 該帶之**實切邊**與規定範圍**遠側境界線**之 `\|cos\|`（**釘槽**·以二者之**實邊**取之） | 期 `≥ 1 − 1e-6`；併出艙其與 `1` 之差 |
| `6` | 該帶所對應之 `cum_S`（即解得之 `buf′`）與現行 `buf` 之**逐位並列**；及由 `buf′` 導出之 `W_0′` 與 `S1_perp` 之差 | 期：`\|W_0′ − S1_perp\| ≤ 1e-9`；三 `forced` 格逐格 |
| `7` | 判別力三造 | [必命中] `forced` 格數 `≥ 1`／[必相異] **現行** `buf` 所生之帶與規定範圍之對稱差須 `> 0`（＝ `W-G.9-287` 之 `13.06`／`38.07`／`13.06` 之**當場重得**·⛔ 轉引）⇒ 證款 `4` **非恆零**／[必為零] 規定範圍與其自身之對稱差須 `0`（**先自證可滿足**）。任一不成立 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判路丙成立與否、⛔ 改碼一字、⛔ 開分支、⛔ 動用 KL 之放行、
⛔ 鑄 `GB`、⛔ 解除或收窄 `GB-170`、⛔ 提修法主張、⛔ 呈 KL。
🛑 **若款 `4` 之對稱差 `> 0`** ⇒ **照實出艙並停**，**⛔ 自擬路丁**。

---

## `§四`　工項二：`自誤 397` ＋ 一則末端追加

### 四-1　`自誤 397`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 397`**：**於入倉之 payload 內以<u>行號</u>為錨**，而倉內之真值與之相異。
> `W-G.9-291 §四(1)` 之 payload 逐字載「`verify/stepg_pipeline.py:657` `if _fo_left:` 與
> `app.py:13990` `if is_corner:`」，當場重量得 **`:655`** 與 **`:13989`**（`:13990` 係其**被守護之賦值式**）。
> **徵候**：單於**其他各節**皆明令「錨取字樣·**⛔ 行號為錨**」，而**payload 內自破其例**；
> 且該 payload **已入倉**（⛔ 追改）⇒ 錨腐之數取得常規地位。
> **族** ＝ 錨腐（開場文 `§三-11` 之戒）。
> **攔法**：**payload 內一律以字樣為錨**；若必須載行號，同格載「**⛔ 採此號·須當場現查**」，
> 並令受單側**重量並二數並報**。

### 四-2　一則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號·⛔ 改任何原節一字）

> **`GB-170` 之修：路甲／路乙之否決 ＋ 行號之更正（`W-G.9-292`·⛔ 解除、⛔ 收窄）**
> **路甲**（擴 `_build_corner_range_v3` 之回傳）**否決**：其接觸面 ＝ **`23` 處**呼叫端
> （`Assign(Name)` `14`／`Assign(Subscript)` `1`／`Return` `8`／解包 `0`·`W-G.9-291` 實測），
> 遠逾 KL 放行射程 `(b)` 之「**只改該處**」。
> **路乙**（自 `corner_range_polys` 反求遠側境界線）**否決**：`4` 個 `forced` 消費端中 **`3` 個**
> `⛔ 可達`；反求須 `6` 項運算，其中 `②` 須另行重算線向、`⑤` 即 `S1_par` 之重算 ⇒ 落「另算第二份」之禁。
> **行號之更正**（⛔ 追改 `W-G.9-291` 之 payload 一字）：該 payload 所載之 `stepg:657` 實為 **`:655`**、
> `app.py:13990 if is_corner:` 實為 **`:13989`**；二者皆**⛔ 得為錨**（`自誤 397`）——錨一律取字樣。
> 🛑 **失效條件三款仍⛔ 有一款成就。**

🔒 二落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**、簡繁機檢之 `rc`；
🛑 二 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留**（⛔ 剝層）。

---

## `§五`　收工閘

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob） | `0`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob |
| `4` | 四簿期末 | 自誤 相異 **`387`**／MAX **`397`**（＋一號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` `git diff --numstat` 輸出**空** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修** |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-289-gb170` 命中 **`0`**（⛔ 開）；`p3a` 命中 `1`（判別力[必存在]） |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§六`　🛑 **⛔ 為之事**

⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 擬路丁或任何第四形；
⛔ 驅動生產路徑之替身（`§三-2` 之設計驗算**⛔ 為替身**·其出艙須冠標記）；
⛔ 為達任一期值而調任何容差；⛔ 自寫第二套幾何；
⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／任何函式簽章；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、⛔ 併線 `p3a`、**⛔ 引任何自 baseline 取得之數**；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 解除或收窄任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改任何已入倉文件一字（含 `W-G.9-291` 之 payload）；⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§七`　報告之格

`§零` 開工閘 → `§一`〜`§二` 逐工項（逐格表以落檔**全量**出艙·⛔ `tail -N`）→ 判別力三造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**；向量夾角**釘槽**並以實邊機驗。
🔒 **設計驗算之出艙一律冠 `【設計驗算·⛔ 實測】`**，⛔ 與實測之數同表混列。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`GB-170`／`_first_corner_alloc_dir`／`_corner_buffer_S`／`_build_corner_range_v3`／`_block_strip`／
`corner_range_polys`／`range_area`／`cum_S`／`S1_perp`／`rot90`／`ALPHA_LABELS`／`baselines`／`numstat`／
`sha256`／`ls-tree`／`merge-base`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: 77e3447b6f15c921942968802a89b0546e3a5ff5f9150f9184242aefe9c27d80
