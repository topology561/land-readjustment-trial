# W-G.9-269　補令十六（KL 之 `c3` 放行之登記 ／ 🟢 **併線令** ／ `c4` 之續辦）

> **受詞** ＝ `W-G.9-269` 之施工窗。
> **主線** ＝ `65e418ffb3f1d05bc8c3ce58e0d1f5808267e1d9`（`ls-remote` 實查·**未前進**）｜**側支** `verify/W-G.9-269-c3` ＝ `17fd9818f769c771f6faf7a630ba61c7e52e8066`｜**凍存** `05a11bd`（未動）。
> 🔒 `W-G.9-269` 施工單、補令一〜十五、本補令**並存**；相斥處以**時序最後者**為準。

---

## §一　KL 之裁（`2026-09-12`·逐字登記）

> **「放行 `c3`」**

### 一-1　所裁之受詞（發單側自倉、自跑復驗者）

`c3`（`contract` 段）＝ 移除旗標 `WG9269_K917_BACKFILL`／`K917_BACKFILL_ENV`／`k917_backfill_enabled()` 及其**全部呼叫點之條件分歧** ⇒ **`K-9-17` 遞補迴圈無條件執行**；並更新受其影響之註解。
生產碼異動 ＝ `app.py` `+26/−42` ⋀ `verify/stepg_pipeline.py` `+16/−18`（生產碼 `34` 檔中相異恰此二檔）。

| 閘 | 發單側實測 |
|---|---|
| `Q-1` 行為 | 發單側**自跑二態**（父 `65e418f`·`app.py` `54bf0987…` ／ `c3` `17fd981`·`app.py` `4379108a…`）⇒ **`20` 檔逐位相同**（`18` 個 `got_*` ＋ `stdout` ＋ `stderr`）·`stderr` 二端皆 `0` B·**未動用自解款** |
| `Q-1` 判別力 | 同一 `cmp` 法對他期落檔比 ⇒ **相異** ⇒ ⛔ 恆綠 |
| `Q-2` 旗標 | AST 去 docstring 命中 **`0`**（父態對照 **`9`**） |
| `Q-3` 探針 | 三支皆 `rc=4` **loud 拒測**、⛔ 印任何判定 |
| `verify/baselines` | 二端樹皆 `241b2a2f98881148…` ⇒ `GB-162` 守住 |

🔒 **`c3` 刪 `60` 列（含縮排調整）而輸出逐位不變** ⇒ **零土地後果**。

### 一-2　本裁之射程（發單側所界·⛔ 擴張）

| 及於 | ⛔ 及於 |
|---|---|
| `c3` 之併線（`§二`） | **`c4`**（零生產碼·依 `常規一 🔧 一` 得逕推·⛔ 另呈 KL）|
| 補令十五 `§五` 序 `1` 之零生產碼物件隨側支併入 | **`GB-159` 之解除**——須俟 `c4` 三條件逐條結清後**呈發單側 → 呈 KL**，⛔ CC 自裁、⛔ 發單側自裁 |
| | **`GB-160`／`GB-162`／`GB-163`** 之解除 |

🛑 **失效條款**：併線之當下，`Q-1`（二態逐位相同）須仍成立，且生產碼 `34` 檔相異須仍恰 `app.py` ⋀ `verify/stepg_pipeline.py` 二檔；相異 ⇒ **停機上呈**，**KL 之放行⛔ 及於該情形**。

---

## §二　🟢 併線令

> **CC 得將主分支 `wip/s1-endpart` <u>快轉</u>推進至 `c3` 側支之 tip。⛔ 再問 KL。**

🛑 **次序**：補令十五 `§五` 序 `1`（工項零 ＋ 三立準入交接註）**先落 `c3` 側支**，其後方併線——⛔ 先推主線（其將破快轉）。
🛑 推進前須以 `git ls-remote` 實查主線仍為 `65e418f…`；若已前進 ⇒ **rebase 後重推側支並由發單側重驗**，⛔ 逕併；**rebase 須為該批推向任何 ref 之最後一個動作**。
🔒 併線完成後，主線已無待併之側支（`p3a` 係凍存·⛔ 併線）⇒「主線⛔ 前進」自併線完成起**解除**。
🔒 **併線完成之 commit 須載於 `c4` 報告**。

🔒 **`c3` 併線 ＝ `W-G.9-269` 之<u>最後一個生產碼工項</u>**；其後僅餘 `c4`（零生產碼）。

---

## §三　`c4` 之續辦（受詞悉依補令十五 `§五`）

`(a)` **差異表定稿**：三表 ＋ 「既有裁定」欄（補令十 `§三-3`）＋ `ΣG_被剔` ⋀ `Δ池（實測）` **分欄並列**（補令十一 `§二-1`）＋ 規格 §7 與當次實跑**二組數並列並具名其時點**（`§二-2`）；域語言·⛔ 程式術語／變數名／行號／旗標名。
`(b)` **`GB-160` 閘 `γ` 重量**：退化之宗移出配地母體後，重量 `3.5m·R5·left` ⋀ `R6·right`；**仍不過 ⇒ 成因⛔ 在退化幾何，須另診並具名**。
`(c)` **`GB-159` 三條件之逐條結清判**：
　`(1)` 病一落地併線 —— **錨 ＝ `dc373d0`**（`c2` 之併線·補令十五 `§一` 已定）；
　`(2)` 二宗依 `K-9-11 三` 處置完畢（不配地 ＋ 合併／調配 ＋ 入調配池 ＋ ⛔ 超配）；
　`(3)` 二宗⛔ 再以近乎零之配地出現。
　**三條齊備方得判其解除**；未齊備者**逐條具名其缺**。
🛑 **`GB-159` 之解除⛔ CC 自裁**——備齊後**呈發單側**，由發單側呈 KL。

🛑 **`c4` 之禁**：⛔ 引任何自 baseline 取得之數（`GB-162`）；⛔ 引 `P-3` 為「每一留存宗皆臨二線」之證（`GB-163`）；⛔ 出艙三戶之最終補償數（`GB-162` 生效期間）。

---

## §四　鑄號之授權

🔒 **本補令⛔ 授任何鑄號。**
🛑 意指占用查**二形並取**；四簿之數須具名其框（**標題形 ∪ 表列形**）。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §五　續辦

| 序 | 事 | 落點 |
|---|---|---|
| `1` | 補令十五 `§五` 序 `1`（工項零 ＋ 三立準）＋ 本補令原封入倉 | **`c3` 側支** |
| `2` | **`§二` 之快轉併線**（`ls-remote` 實查 ＋ 失效條款當下復算） | 主線 |
| `3` | **`c4`**（`§三` 之 `(a)`〜`(c)`·零生產碼·得逕推） | 主線 |
| `4` | `W-G.9-269` **結案報告**（全批之條件逐條 ＋ 結案 commit ＋ 四簿 ＋ 未結之阻塞項清單） | 主線 |
| `5` | 收工交接（交接註 ＋ 坑之定義處清單自證 ＋ 起手清單） | — |

🔒 **未結之阻塞項（`c4` 報告須逐則列其現況與解除條件）**：`GB-159`（俟三條件）／`GB-160`（俟閘 `γ` 重量）／`GB-162`（俟 `1137` 格之歸因）／`GB-163`（俟 `P-3` 判定集補全）。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
