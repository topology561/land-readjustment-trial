# W-G.9-268　`GB-67` 開修：`K-9-23` 二閘由觀測轉主動 ＋ `K-9-17` 遞補迴圈 ＋ 第 `1` 宗境界線不動（**重級批**·🔴 **有土地後果**）

> **期初態** ＝ `e57d2ef86894472761e1403974ff1f2ce1e6a2df`（`origin/wip/s1-endpart`·須 `ls-remote` 實查）
> **審定** ＝ 發單側（`claude.ai`）·`2026-09-10`。本單係 `W-G.9-268 附件甲` 之審定版，**⛔ 逐字沿用草稿**——三處已改，見 `§十`。
> **受詞** ＝ `GB-67`。KL `2026-09-10` 逐字裁「`GB-67` **開**」⇒ 丙案（只定位）轉甲案（開修）。
> 🛑 **方法⛔ 新裁**：三支皆既有裁定（`K-9-23` 二閘轉主動／`K-9-17` 遞補迴圈／第 `1` 宗境界線不動）。凡本單未列之方法一律 **停機上呈**。

---

## §零　級與受詞之界

**級 ＝ 重級**（動生產碼、動配地幾何、有土地後果）⇒ 開工閘完整儀式，一款不減。
🛑 **級之升不降**：中途若須動及本單未列之受詞 ⇒ **停機上呈**，⛔ 就地升級續辦。

**生產碼受詞（二檔·🔴 審定所改·⛔ 只 `app.py`）**

| 檔 | 站 | 字樣錨（`grep -F`·⛔ 行號） |
|---|---|---|
| `app.py` | 站 `1／4`（左鏈）·站 `2／4`（右鏈） | `res['_lg_cols'] = _lot_gate(` |
| `verify/stepg_pipeline.py` | 站 `3／4`（左鏈）·站 `4／4`（右鏈） | `res['_lg_cols'] = ns['_lot_gate'](` |

🔒 **二檔並列之由（發單側實測·⛔ 得省略其一）**：併線前必答一（覆蓋帳歸零）所讀之
`verify/out/got_G值_退縮*_partial.csv` 由 **`verify/run_verification.py:727`** 自 **stepg 管線**寫出。
⇒ **只改 `app.py` 則覆蓋帳結構上不可能歸零，驗收條件不可達。**
🔒 碼內自載之佐證（逐字）：`# 🆕 \`W-G.9-246′\` 工項二 **站 1／4（app 左鏈）**` ／ `**站 3／4（harness 左鏈）**`。

**⛔ 動之檔**：`verify/run_all.py`／`verify/run_verification.py` 一字不動。其餘 `32` 檔生產碼一字不動。

🛑 **⛔ 併入本批**（各為其後之獨立批）：`K-9-12` 矩形容納轉主動／五級／`K-9-10`／`K-9-1`／`c2`（他義）／`n_hat_far` harness 側補齊／`GB-105` 母體同步之獨立批／`GB-106`。

---

## §一　開工閘（逐條當場復現·任一不符 ⇒ 🛑 停機上呈·⛔ 續辦）

| # | 閘 | 判準 |
|---|---|---|
| `S-0` | 本單原封入倉 `docs/orders/` | 入倉 blob `sha256` ＝ 落檔前來源（`追加四` 款一）|
| `S-0b` | `probe_order_preflight.py <本單>` | `P-3`／`P-5` 停機款；`P-1/2/4/6` 逐項具名採納或豁免 |
| `S-1` | 號占用（宣告框六數分列 ＋ 二對照） | `W-G.9-268` 嚴格式與寬式**六數全 `0`**；對照甲（已知占用之號）須 `≥1`；對照乙（執行期組出之人造號·⛔ 入倉）須 `0`。🩸 取母體須用 `git ls-files -z`／`ls-tree -z`（`quotePath` CJK 陷阱·`附件甲 §5 紅②`）|
| `S-2` | `left-right` ＝ `0　0`；`ls-remote` 與期初態逐位相符 | ⛔ 採信本地 ref |
| `S-3` | 生產碼 `34` 檔 blob 期初 | 與 `附件甲 §3` 逐檔相符；`app.py` ＝ `c5ec503c740f59e084cedd83531b9d029fe8ddc0` |
| `S-4` | `run_verification` 期初實跑 | `RESULT: FAIL`／`stderr` `0` B／紅 `24` 綠 `10`。🩸 抽名目 ⛔ 用 `grep -oP`，用 `python re.match`；框自生產碼取（`run_verification.py:1497`）|
| `S-5` | 🔴 **`run_all` 期初對帳態**（本批新增） | 跑一次 `run_all`，記錄 `wv_reconcile` 之類 I／II／III 三數與名目差集之**期初值**。🛑 **⛔ 以「與凍存相同」為通過條件**——期初即不相同（凍存 `22`／現況 `24`／`B∖A = 3`／`A∖B = 1`，發單側已獨立復現）。本閘只**取值**，不判過 |
| `S-6` | 覆蓋帳期初 | `13` 組·違反 `2`（`0m·R2·left = 45.9766`／`3.5m·R5·left = 56.8186`）·三造否證對照如期（相疊 `1.0`／相離 `0.0`／注入 `+1㎡` 轉紅）|
| `S-7` | 硬閘活體生效 | `--selftest` **⋀ 活體攔截實測**（⛔ 只 `test -f`），須出艙命中偏移並自證其讀到真命令 |
| `S-8` | 🔴 **四站現查**（本批新增） | 二檔之 `_lg_cols` 寫入點合計須 **`4`**（`app.py` `2` ＋ `stepg` `2`）；讀出點合計 `4`。**判別力**：`驗_rejected` 於全 `34` 檔生產碼命中須為 **`1`**（僅其定義處）⇒ 觀測模式成立；若 `> 1` ⇒ 已有消費點 ⇒ **停機上呈** |

🔒 **`S-5` 之立法理由**：`§二` 之修法必然改變覆蓋帳與部分名目。期初對帳態未知 ⇒ 修後之任何紅**無從歸因於本批抑或既存**。

---

## §二　工項（逐 `commit`·每 `commit` 一件事·皆可獨立回退）

### `c0`（零生產碼·得逕推）　受詞母體之對位出艙

🔴 **本批之首要出艙項（審定所加）**。以唯讀探針出艙下表，**⛔ 動一字生產碼**：

| 出艙項 | 形 |
|---|---|
| `A` 母體 | `K-9-9 二`（地界線不交叉）之違反宗集 ＝ `GB-67` 之「`2` 宗／`4.7950 ㎡`」逐宗**暫編地號**與其 `G` |
| `B` 母體 | 覆蓋帳 `13` 組中違反組之**逐重疊對**及其面積 |
| `A ∩ B`／`A ∖ B`／`B ∖ A` | 逐宗具名·各載判定集基數 |

🩸 **發單側之實測（供對拍·⛔ 採信·須自行重得）**：`B` 之重疊對為
`0m R2/left`：`628-41(1)`×`628-27(1)` ＝ `19.8142`、`628-41(1)`×`628-40(1)+` ＝ `26.1624`；
`3.5m R5/left`：`628-45(1)+`×`628-18(2)` ＝ `56.8186`。
而 `A` 所指之 `628-42(1)`／`628-53(2)` 於落檔皆為 `驗_宗序 = 第2宗`，**不在任一重疊對內**。

🛑 **⇒ `A` 與 `B` 係二母體，⛔ 預設其相同。** 草稿所稱「此二宗正是閘一轉主動所應剔除者」**未經量測**，本單**⛔ 採為前提**。
🛑 若 `A ∩ B = ∅` ⇒ **停機上呈**（其意謂閘一轉主動未必使覆蓋帳歸零，方法與受詞須重新對位）。
🔒 **⛔ 鑄任何號**；若查得結構性缺陷 ⇒ 停機上呈，由發單側裁其登記形。

### `c1`（🔴 生產碼·須 KL 放行）　遞補迴圈骨架 ＋ 旗標（預設 `off`）⇒ 行為逐位不變

於**四站**（`§零` 之表）之後新增 `_lot_gate` 消費點與遞補迴圈，置於具名旗標之後（預設 `off`）。
**⛔ 動**：`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2` 一字。
🔒 **`expand`（並存）段**——⛔ 刪任何既有碼路徑。

**驗收（性質）**：旗標 `off` 下全部輸出與父 `commit` **逐位相同**（`stdout` `diff` 空 ∧ 四 CSV 逐位同）。
**判別力**：同一 `commit` 內以旗標 `on` 跑一次，輸出**須相異**（證非死碼）。

### `c2`（🔴 生產碼·幾何變動·**有土地後果**·須 KL 單獨放行）　旗標 `on`

- 依 `K-9-17` 五之**逐字**（「遞補宗的G值當然要重算……後面的宗也會整體往前分配，所以也要重算G」）：
  停於首個 `驗_rejected == False` 之候選；其後各宗**整體往前**並**重算 `G`**。
- 遞補取 **街角重排後**序列之下一位。🛑 **⛔ 依 `K-9-9 四` 字面之「重劃前投影順序」**——`K-9-17`（`2026-08-22`）晚於 `K-9-9`（`2026-08-16`），以 `K-9-17` 為準。照 `K-9-9 四` 字面實作即錯，且其錯**⛔ 會被任何既有閘擋下**。
- 剔除宗之處置（**已裁·⛔ 上呈 KL**·`K-9-11 三`）：**不配地** ＋ 走合併／調配機制 ＋ 騰出之地入**調配池** ＋ **⛔ 超配**。
- 候選耗盡 ⇒ **loud raise**（`K-9-9 六` 逐字：「必然產生配餘地……⛔ 不會發生『塞不下』」）。🛑 **⛔ 靜默兜底、⛔ 回 `None`**。
- **`GB-105` 之處置（審定：採乙）**：⛔ 另開批；於本 `commit` 內**逐呼叫點出艙 `_projection_order` 之暫編地號集合並斷言逐位相同**（`app.py` `3` ＋ stepg 以 `ns["_projection_order"](` `1` ＝ 合計 `4`）。🛑 `GB-105` 逐字：「⛔ 不得以『閘沒紅』作為母體同步之證據」。
- **首宗境界線不動**：係構造保證（第 `0` 宗遠側界之唯一決定量為 winner 之 `G`，而 winner 由街角 PK 決定、⛔ 受遞補影響）⇒ ⛔ 須另設閘；**惟仍須補一道 loud 斷言**（`GB-104` 逐字：「⛔ 不得以『上游擋住了』為由不設」）。

**出艙**：`§三` 性質閘 ＋ **差異表**（`§四`）。

### `c3`（🔴 生產碼·須 KL 放行）　`_block_strip` 之 loud 斷言

內容 ＝「兩線於街廓深度內相交 ⇒ **loud raise**」（⛔ 回楔形三角）。
🔒 **平行門檻⛔ 另立**：沿用 `verify/stepg_pipeline.py` 之 `_PAR_TOL = 1e-6`（`K-6:1010`·其原受詞逐字即「該側 `SIDE` 與 `ALLOC`」）。
**驗收**：`c2` 之後該斷言⛔ 觸發；**判別力**：以人造收斂雙線餵入 ⇒ **須觸發**。

### `c4`（🔴 生產碼·須 KL 放行）　`contract` 段

移除旗標 ＋ 更新三處宣告：錨 `A`（四站之「⛔ 依其 verdict 寫任何 `if`」）／錨 `B`（`🛑 **觀測模式**：本函式**只回傳判定**`）／錨 `C`（`其呼叫端亦僅為診斷`）。
🛑 **⛔ 動錨 `D`**（`_t3_gate_emit` 之「只印」——診斷輸出應保留）。
**驗收**：與 `c3` 輸出**逐位相同**。

### `c5`（零生產碼·得逕推）　差異表產出 ＋ 執行報告入倉

---

## §三　驗收（**性質閘**·⛔ 計數式·⛔ 以列號或筆數為證據）

| # | **所保證之性質** | 判定集基數 | 兩造對照（自證⛔ 恆綠） |
|---|---|---|---|
| `P-0` | **`A`／`B` 二母體之對位已出艙且非空** | `A` 之宗數／`B` 之重疊對數 | `A ∩ B = ∅` ⇒ 停機（⛔ 判為通過）|
| `P-1` | **同一 `(情境,街廓,側)` 內任二宗之分配範圍<u>交集面積為零</u>** | `13` 組 ＋ `R3` 之組數；逐組宗對數 | 相疊矩形須紅／相離須綠／注入 `+1㎡` 於原綠之組須轉紅 |
| `P-2` | **每一非鏈頭宗之 `G` <u>嚴格大於</u>其鏈之藍影面積** | 逐鏈非鏈頭宗數 | 已知須紅之靶 ＋ 已知須綠之靶，各具名 |
| `P-3` | **每一宗<u>同時臨</u> FRONTLINE 與 BASELINE，臨接長 `> 0`** | 全部落位宗數 | 修前須紅之宗具名；修後若不在母體，須具名其已被剔除 |
| `P-4` | **`ΣG ＋ Σ池 ＝ 街廓面積`，守恆殘差為零** | 可達街廓數 | 人造扣 `1㎡` ⇒ 須紅 |
| `P-5` | **街角地遠側境界線<u>改前改後逐位相同</u>** | 有 winner 之街角側數 | 人造移動該線 `1e-3` ⇒ 須紅 |
| `P-6` | **街角地<u>歸屬</u>改前改後不變** | 全 `12` 側 | 🛑 `R6/right` 標「**不可判**」（`GB-156`·`SIDE ∥ ALLOC` ⇒ `d-1` 鑑別力為零）——**⛔ 併入違反計數**（既⛔ 計為違反、亦⛔ 計為未違反）|
| `P-7` | **`V-1′` 五款**（`VR-094` ＋ 其末端追加） | 陽性 `3.5m·R2·left` `n=6`／真陰性 `3.5m·R1·**right**` `n=3`／第三類 `3.5m·R2·right` `n=6` | 三者**同色 ⇒ 閘失能**（須 `False`）；🛑 `1e-6` **⛔ 放寬** |

🛑 **`P-1` ⛔ 寫作「違反組數 ＝ `0`」**——該形為計數代理。operative 形為「**逐組逐對之交集面積皆為零**」，每組須同格出艙 `n`，**`n = 0` ⇒ loud 拒測**。
🛑 **`P-7` 之陰性⛔ 寫作 `3.5m·R1`**——`R1·left` 非鏈頭 `0` 宗 ⇒ 判定集為空 ⇒ 會以「空集通過」假綠（`自誤 329`）。
🛑 **精度款**：`V-1′ (3)` ⛔ 以 `verify/out/*.csv` 之 `2dp` 欄評估，須讀全精度；`(1)` 二讀法（落檔 `宗地寬度(m)` 欄／全精度 `s` 區間長）**並列出艙·⛔ 靜默擇一**。

---

## §四　差異表之形（**域語言**·⛔ 程式術語／變數名／行號）

**表一（逐宗）**《街角地配地調整前後對照表（`退縮 <X> 公尺`·`<街廓>` 街廓 `<左／右>` 側）》
欄：配地次序（序數）｜重劃前地號／土地所有權人｜調整前・調整後分配面積（㎡·2dp）｜**面積增減**（㎡·2dp·帶號）｜調整前・調整後臨路寬度（m·2dp）｜**寬度增減**（m·2dp·帶號）｜位置變動（m·2dp·沿正面路之起點位移）｜**處置**（`維持原配地`／`改列調配池`／`遞補進位`／`位次順移`）｜是否街角地／備註

**表二（逐街廓·守恆）**：街廓｜分配地面積合計（前・後・增減·帶號）｜調配池面積（前・後・增減·帶號）｜街廓總面積｜**守恆殘差（應為 `0`）**

**表三（彙總·首頁）**：改列調配池之宗數・面積合計｜遞補進位宗數｜面積增減宗數・`ΣΔ`・`Σ|Δ|`（**帶號與絕對值並列**）｜**街角地歸屬有變動之街廓（預期 ＝ 無）**｜所有權人受影響戶數

🔒 **基準之界（審定所改）**：`GB-67` 之「`2` 宗／`4.7950 ㎡`」係 **`A` 母體**（`K-9-9 二` 不交叉之違反）之現值；**⛔ 逕作為覆蓋帳歸零之基準**（`B` 母體）。二者之對位由 `c0` 出艙。
🛑 **若差異表出現街角地歸屬變動 ⇒ 停機上呈**（`P-6`）。
🔒 **⛔ 承諾列數**；量級估計（發單側認可）：直接受詞 `10⁰`、同鏈下游 `10¹`、上界 `10²`。

---

## §五　側分支與逐 `commit` KL 放行

```
備妥（⛔ 推主分支）→ 推至側分支 verify/W-G.9-268-c<n>
→ 發單側自側分支逐位元組復驗 → 發單側請示 KL → KL 放行
→ CC 將主分支 wip/s1-endpart 推進至該物件
```
🛑 主分支**原地不動**；未放行前⛔ 前進一個 `commit`。主分支若於其間前進 ⇒ rebase 後**重推側分支並重驗**。
🔒 **零生產碼 `commit`**（`c0`／`c5`）依 `常規一 🔧 一` **逕行 `push`**；其判法逐 `commit` 出艙（**含空輸出**）：
```
git diff --name-only <前一已push之HEAD>..<本commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

---

## §六　併線前必答

1. **覆蓋帳** `13` 組 ＋ `R3` 組**全部歸零**（`R3` 依 `VR-095` 逐街廓獨立呼叫取值，**恆標「單源·歸納支持」**）。
2. **`V-1′`** 五款 ＋ 三組對照（各載 `n`·`1e-6` ⛔ 放寬）。
3. **`run_all` 名目差集** —— 比較端 ＝ **`S-5` 所記之期初態**（⛔ 凍存名單之舊態）；逐項歸類 I／II／III；**類 II ⛔ 自動視為同**。
4. **差異表**呈 KL。
5. **`c0` 之 `A`／`B` 對位表**併呈——須說明覆蓋帳歸零與 `4.7950 ㎡` 之關係究竟為何。

---

## §七　鑄號之授權

🔒 **本批得鑄者 ＝ `自誤 333` 一號；⛔ 逾此。**

`自誤 333`（責 ＝ **發單側**·純登記）：**指定搜索母體時以檔名字樣為框，致正本落於框外，並據該殘缺母體出艙「復現不出」之否定判**。
形：發單側查凍存名單時取框 `檔名含 凍存｜runall｜reconcile`，命中二檔（皆為凍存副本），遂判 `09474ae0…` 復現不出；實則正本 `verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt` 不含該三字樣而落於框外，其檔內第 `49` 列逐字自載該 hash，且依檔頭自載式復算**逐位相符**。
同族：`自誤 330`（裁一族結構缺陷時只掃一簿）／`自誤 328`（以未經量測之斷言充作否定判之基礎）。
通則：**凡出艙「查無」「復現不出」之否定判，其母體之取法須先自證涵蓋受詞之正本**——以檔名字樣取框者，須同時出艙**內容框**之結果作為交叉。

🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §八　停機款

- 開工閘任一不符。
- `c0` 之 `A ∩ B = ∅`。
- 差異表出現街角地歸屬變動。
- 須動及 `§零` 未列之受詞或未列之方法。
- `S-8` 之 `驗_rejected` 命中 `> 1`。
- 單與正典相斥 ⇒ 依 `常規二` 取**保守項**續辦並**逐字上呈**，⛔ 自行放寬。

---

## §九　出艙形

`docs/reports/W-G.9-268R_….md`：開工閘八閘逐條／`c0` 對位表／逐 `commit` 之生產碼判（含空輸出）／`§三` 七閘逐閘（各載 `n` 與兩造對照）／差異表／併線前必答五項／🩸 **本批之自捕**（照實·⛔ 隱去·各載 形／徵候／攔法）／自限復驗（`porcelain`／`left-right`／四簿 MAX 未動）。

---

## §十　審定所改（**⛔ 逐字沿用草稿**·三處）

| # | 草稿 | 審定 | 由（發單側實測） |
|---|---|---|---|
| `1` | 真改點 `2` 處（app 左／右鏈）；`U-3` 以 app 路徑為母體 | **四站·二檔**（含 `verify/stepg_pipeline.py`） | `_lg_cols` 寫入點 app `2` ＋ stepg `2`；`got_G值_*_partial.csv` 由 `run_verification.py:727` 自 stepg 寫出 ⇒ 只改 app 則覆蓋帳結構上不可能歸零，驗收不可達 |
| `2` | 「`628-42(1)`／`628-53(2)` 正是閘一轉主動所應剔除者 ⇒ 受詞與方法直接對位」 | **降為待證**·立 `c0` 與 `P-0` | 實測重疊對為 `628-41(1)`×`628-27(1)`／`628-41(1)`×`628-40(1)+`／`628-45(1)+`×`628-18(2)`，**⛔ 含該二宗** |
| `3` | `U-5`：`GB-104` 混列二口徑，擬末端追加具名 | **撤除** | `CLAUDE.md`（`-266` 工項五末端追加）逐字已裁：`45.9766` 與 `56.3293` 併列**可行**（同批同口徑同情境）；口徑混同者係 `56.3293` vs `56.8186`。`GB-104` 所併者為前者 ⇒ ⛔ 瑕 |

**其餘採納**：`U-1` ⇒ `S-5`；`U-2` ⇒ 採乙（`c2` 內斷言·⛔ 另開批）；`U-4` ⇒ 採甲（只具名·⛔ 立條文·列入 `-269` 候辦）。
**草稿之 `S-0`〜`S-7`／`P-1`〜`P-7`／`§四` 差異表欄位／`c1`〜`c5` 之切分**均予維持。

---

## §十一　工項零（**審查窗辦**·辦畢即推即停·零生產碼 ⇒ 得逕推）

原封入倉四檔，**⛔ 動一字**，**⛔ 執行 `§二` 任何工項**：
1. 本單 ⇒ `docs/orders/W-G.9-268_施工單_GB67開修_K923二閘轉主動_K917遞補迴圈.md`
2. 附件甲（審查窗報告與草稿）⇒ `docs/orders/W-G.9-268_附件甲_審查窗報告與草稿.md`
3. 本窗所跑之唯讀探針（若有）⇒ `verify/probes/`
4. 出艙 `git status --porcelain`（須 `0`）與四簿 MAX（須未動）

辦畢即推即停。`§二` 之工項由**新 CC 施工窗**辦。

---

*本單受詞 ＝ CC。`c0`／`c5` 零生產碼得逕推；`c1`〜`c4` 逐 `commit` 須 KL 放行。純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
