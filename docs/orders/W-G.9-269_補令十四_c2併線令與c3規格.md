# W-G.9-269　補令十四（🟢 **`c2` 之併線令** ／ `c3` 之規格）

> **受詞** ＝ `W-G.9-269` 之 `c2` 專窗。
> **主線** ＝ `eef4a8c9a92e80867c6617a0be4c4268d6ac7a0b`（**未前進**）｜**側支** `verify/W-G.9-269-c2` ＝ `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d`｜**凍存** `05a11bd`（未動）。皆須 `git ls-remote` 實查。
> 🔒 `W-G.9-269` 施工單、補令一〜十三、本補令**並存**；相斥處以**時序最後者**為準。

---

## §一　序 `3`（發單側重驗）＝ **全項通過**

| 款 | 發單側自倉實測 |
|---|---|
| `(a)` 去-docstring AST 對拍 | 基準（`5f517da`·**KL 所放行**）與側支 tip 之 dump **逐位相同**（發單側之框：`ast.dump(annotate_fields=True, include_attributes=False)`·長 `3090630`）。**判別力**：以**父態**為對照 ⇒ **相異** ⇒ 本檢⛔ 恆綠 |
| `(b)` `numstat` | `app.py` **`1／1`**，逐字恰該一句 docstring（新形於**任何時點皆為真**）|
| `(c)` 生產碼 `34` 檔 | 相異**恰 `app.py` 一檔** |
| `verify/baselines` 樹 | 二端皆 `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9` ⇒ `GB-162` 三禁令守住 |
| 四簿 | 自誤 `363`／缺號 `[106, 355, 356, 357]` **未變**；`GB` `162 → 163`（**經授權**）；`VR` `95`／`K-9` `29` 未動 |
| 結構 | 主線 `eef4a8c` **未前進**；快轉成立（＋`6` commit） |

🔒 **⇒ 補令十三 `§一-2` 之失效條款<u>未觸發</u>**：`app.py` 之可執行敘述與 KL 所放行之 `5f517da` **逐位相同**。
🩸 **框之別（⛔ 判孰誤）**：CC 報 dump 長 `2893922`、發單側 `3090630` ⇒ `ast.dump` 選項之別；依補令二 `§三`「**先對齊母體再判孰誤**」，各報其框即可。

🔒 **CC 之二項處置核可**：
`(a)` 照實自陳「已經 KL 放行」一句係本窗所寫、**由發單側指出而⛔ 自捕**，並將 `§二-3` 立準入交接註；
`(b)` **拒絕自行併線**（`三方分工鐵律`：CC ⛔ 得兼為發單側）——正確。

---

## §二　🟢 `c2` 之併線令

> **CC 得將主分支 `wip/s1-endpart` <u>快轉</u>推進至 `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d`。⛔ 再問 KL。**

🛑 推進前須以 `git ls-remote` 實查主線仍為 `eef4a8c…` ⋀ 側支 tip 仍為 `dc373d0…`；任一相異 ⇒ **停機上呈**，⛔ 逕併。
🛑 **失效條款**：併線之當下，`app.py` 之**可執行敘述**須仍與 `5f517da` 逐位相同（去-docstring AST 對拍·附判別力對照）；相異 ⇒ 停機上呈，**KL 之放行⛔ 及於該情形**。
🔒 併線完成後，主線已無待併之側支（`p3a` 係凍存·⛔ 併線）⇒「主線⛔ 前進」自併線完成起**解除**，至下一側支建立為止。
🔒 **併線完成之 commit 須載於 `c4` 報告**，並作為 `GB-159` 三條件之條件 `(1)`（病一落地併線）之**結清錨**。

---

## §三　`c3` 之規格（`contract` 段·🔴 生產碼·**須 KL 放行**）

### 三-1　受詞

**移除旗標**：`K917_BACKFILL_ENV`（`app.py:9279`）／`k917_backfill_enabled()`（`:9296`）／其**全部呼叫點之條件分歧** ⇒ 遞補迴圈**無條件執行**；並更新受其影響之註解（含 `c2` 所立之 docstring）。
🛑 **⛔ 動**：`_oblique_s_max`／`_wg9268p_anchor_advance`／`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate`／`_t3_gate_emit` 之**內部**一字。
🛑 **⛔ 改遞補之語意**——`c3` 為 `contract`，**⛔ 為第二次語意變更**。

### 三-2　🔴 **前置**（須**先於** `c3`·零生產碼·得逕推）

**三支探針讀該旗標**（發單側自倉實查）：`verify/probes/probe_WG9269_c2_blk.py`（`1`）／`probe_WG9269_c2_diff.py`（`2`）／`probe_WG9269_c2_pool.py`（`1`）。
🔴 `c3` 移除旗標後，該三支以 `environ[FLAG]="0"` 取 `off` 態者將**取得 `on` 態** ⇒ 二態逐位相同 ⇒ **靜默之假綠**（`W-G.9-268′` 補令十一 `§二` 之同族·**第二次**）。

**處置**：三支**各**增一道**選擇器活體檢**，置於其全部判定**之前**——
> 本探針所構造之二態，須於一個**具名之可觀測量**上確有相異；**二態於該量上逐位相同 ⇒ 🛑 loud 拒測（`rc ≠ 0`）**，並逐字具名二種可能成因：`(a)` 受測之變更為 no-op；`(b)` **態之選擇機制已失效（如旗標已被 `c3` 移除）**。**⛔ 判綠、⛔ 靜默續跑。**

🛑 **須於旗標<u>尚在</u>時先跑一輪，證各支之活體檢於旗標仍活時通過**（⛔ 無從分辨「檢不過」係因旗標已移除抑或因檢本身有瑕）。
🔒 **得沿用** `W-G.9-268′` 之 `verify/probes/wg9268p_selector_liveness.py`（其可觀測量取「**分支之被觀測**」·⛔ 需任何幾何為真）；沿用者須出艙其適用性之證，⛔ 逕用。

### 三-3　驗收（性質·三款 ＋ 判別力）

| # | 所保證之性質 | 判別力（自證⛔ 恆綠） |
|---|---|---|
| `Q-1` | **`c3` 之輸出與其父 `commit`（`c2` 態）逐位相同** —— 全部 `got_*` ∧ `stdout` ∧ `stderr` | 與**父態之父**（`c2` 之前）相比須**相異** |
| `Q-2` | **旗標字樣（`WG9269_K917_BACKFILL` ⋀ `K917_BACKFILL_ENV`）於全 `34` 檔生產碼命中 `0`**，且遞補迴圈**無條件執行** | 父態對照須 `> 0`；🛑 **須以 AST 去 docstring 判**（⛔ 以字樣命中代之·坑 `q`） |
| `Q-3` | **`§三-2` 三支之活體檢逐支 loud 拒測**——⛔ 任一支以「二態逐位相同」判綠 | 逐支出艙其判；**任一支靜默判綠 ⇒ 🛑 停機上呈** |

🔒 **`Q-1` 之自解款**：若 `stdout` 僅相異於 traceback 框行號且其位移與該檔增減列數**閉式相等**，得依 `W-G.9-268′` 補令四 `§一-2` 自解並具名。
🛑 **停機款**：`Q-1` 不成立（且非上開閉式可自解者）／`Q-2` 命中 `≠ 0`／`Q-3` 任一支靜默判綠／須動 `§三-1` 之 ⛔ 動符號內部／`§三-2` 之前置未先辦。

### 三-4　落點與放行

側支 `verify/W-G.9-269-c3`；⛔ 推主線；側支待併期間主線⛔ 前進（零生產碼之 commit 亦落側支）；**rebase 須為該批推向任何 ref 之最後一個動作**。
備妥後由**發單側逐位元組復驗** → **呈 KL** → 放行 → 快轉併線。
🔒 **`c3` 行為逐位不變 ⇒ 零土地後果**；惟其動生產碼，仍須 KL 放行（`c1` 之前例）。

---

## §四　鑄號之授權

🔒 **本補令⛔ 授任何鑄號。**
🛑 意指占用查**二形並取**；四簿之數須具名其框（**標題形 ∪ 表列形**）。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §五　續辦

| 序 | 事 | 落點 |
|---|---|---|
| `1` | **`§二` 之快轉併線**（`ls-remote` 實查 ＋ 失效條款當下復算） | 主線 |
| `2` | 工項零（本補令原封入倉） | 主線（併線後·已無待併側支） |
| `3` | `§三-2` 之前置（三支活體檢 ＋ 旗標尚在時之一輪自證） | 主線（零生產碼·得逕推） |
| `4` | `c3` 備妥 ＋ `Q-1`／`Q-2`／`Q-3` 出艙 | **新側支 `verify/W-G.9-269-c3`** |
| `5` | 發單側復驗 → **呈 KL** → 放行 → 快轉併線 | — |
| `6` | `c4`（零生產碼：差異表定稿 ＋ 執行報告 ＋ 補令三 `§五` 後置二項：`GB-160` 閘 `γ` 重量 ⋀ `GB-159` 三條件之逐條結清判） | — |

🔒 **`GB-162` 生效**：⛔ `WV_BAKE=BASELINES`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；⛔ 引任何自 baseline 取得之數。
🔒 **`GB-163` 生效**：⛔ 引 `P-3` 為「每一留存宗皆臨二線」之證。
🔒 **`GB-159` 仍有效**：受影響之宗之配地圖與負擔表**⛔ 對外**；其解除俟 `c4` 之三條件逐條結清後**呈發單側 → 呈 KL**，⛔ CC 自裁。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
