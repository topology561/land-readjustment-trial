# W-G.9-269　補令一（`p1`／`p2` 之核可 ／ 🔴 **`p1` 受詞之誤指與重定** ／ `p3` 之二裁 ／ `自誤 353`）

> **受詞** ＝ `W-G.9-269` 之施工窗。**主線期初態** ＝ `20a4030728a7999eaf2c0189717baa3c4ed0d8ce`（須 **`git ls-remote`** 實查·⛔ 採信本地追蹤 ref）。
> 🔒 `W-G.9-269` 施工單與本補令**並存**；相斥處以**時序最後者**為準。

---

## §一　`p1`／`p2` 之核可

### 一-1　`p2`（`GB-161` 全族）**核可**

母體實測 `worktrees 14` ⋀ `os.chdir( 12` ⇒ 聯集 **`23`**（單載 `19`）；依單之明文「以實測為準」——**核可**，並核可其逐項歸因（單之框較 `GB-161` 正典框為寬）。
**真硬編他樹者恰 `4` 支**，餘為 `__file__` 相對或 `GB-35` 排除邏輯 ⇒ **免修·具名** —— **核可**（單 `§二 p2` 已令「免修者須具名其由，⛔ 靜默略過」，CC 照辦）。
🔑 **修後三支 `wg9268_*` 由「靜默讀他樹而全綠」轉為「loud 失敗並具名其所缺之檔」——此即該修之正確徵候**，核可。
🔒 **`wg953-handoff-ready-d1e3c0` 已不存在 ⇒ 【22】現行 loud 失敗**：核可其照實出艙；該支之效力判須載明「其既往落檔產於一棵**已消失**之樹 ⇒ ⛔ 復現、全部無效」。
🩸 **CRLF 自捕（成因未明）——核可其處置**：`numstat` 閘攔下、改以**二進位模式**重做、隔離複現不出即**照實報為成因未明**而⛔ 編造機制。**⛔ 以「已修好」掩其成因未明**；入交接註。

### 一-2　`p1` 之**量測**核可，其**結論**見 `§二`

🔒 **`A`（`K-9-9 二` 不交叉之違反宗集）於新錨態為<u>空集</u>——核可。**
🔒 **CC 之三項處置立為常例**：
`(a)` 探針自身之陰性對照失敗時，**拒絕出艙 `A = 0 宗`**，先分辨**量測器紅**抑或**受詞紅**；
`(b)` 以 `run_verification`（**獨立器**）交叉確認（`結構不變量永久閘` 二情境皆 `PASS`、`圍堵閘` 命中 `0`、六街廓全達）；
`(c)` 母體由 `59→46` 時**⛔ 逕讀為「違反消失」**，而先排除「宗根本未被量」。
🔒 **`A` 空集之意義（發單側所定）**：**病二（錨點更正）已將 `K-9-9 二` 之不交叉違反一併修除**——此為病二之**附帶成果**，應以**末端追加**登記於 `GB-67`（⛔ 追改其原文）。

---

## §二　🔴 `p1` 受詞之**誤指**與**重定**

### 二-1　所診（發單側自倉重跑·⛔ 採信報告）

發單側於主線 `20a4030` 自跑 `run_verification`，逐宗取 `驗_總判`／`驗_rejected`／三閘之分判：

> **二情境皆：`保留 25` ／ `剔除 21`**（配地宗 `46`）

| 拒因 | 所屬裁定 | 在 `W-G.9-269` 射程內？ |
|---|---|---|
| `A = 不合格`（矩形容納） | **`K-9-12`** | 🛑 **否**——歷次補令逐字「`K-9-12` 矩形容納轉主動 ⛔ 併入」 |
| `B = 不合格`（藍影） | **`K-9-23` 閘一** | ✅ **是**——即病一 |
| `C`（面積 `v3 ⑨⑩`） | — | 全區 `C = 不適用` |

**`B = 不合格` 者，全區恰二宗**：

| 宗地 | 歸戶 | 情境 | `G(㎡)` | 宗地寬度(m) | 三閘 |
|---|---|---|---|---|---|
| `628-42(1)` | `G011` | `0m` | `0.23` | `0.18` | `A` 不合格／**`B` 不合格**／`C` 不適用 |
| `628-53(2)` | `G025` | `0m`／`3.5m` | `0.00` | `0.02` | `A` 不合格／**`B` 不合格**／`C` 不適用 |

另 `B = 合格` 三宗（`628-34(3)`／`628-53(1)`／`628-42(2)`）；餘 `16` 宗 `B = 不適用`。
（`628-42(1)` 於 `3.5m` 為 `B = 不適用`·`G = 4.77`／寬 `0.11`。）

### 二-2　重定（**取代單 `§二 p1` 之受詞**）

> **`W-G.9-269`（病一）之受詞 ＝ `K-9-23` <u>閘一（藍影·`_k923_gate1`）</u>判為<u>不合格</u>之宗集**，
> **⛔ `K-9-9 二`（不交叉）之違反宗集，亦⛔ `_lot_gate` 之合取（`驗_rejected`）。**

🔒 **由**：病一之方法為「`K-9-23` **閘一**由觀測轉主動」；其所消費者即閘一之判。`驗_rejected ＝ any(A, B, C)` 含 `K-9-12`（閘二·**射程外**），以其為受詞將使本批逾射程 `19` 宗。
🛑 **⇒ `A` 之空集⛔ 得讀為「病一受詞為空」**——二者係不同述詞。
🔒 **現態之受詞（發單側實測·對拍用·⛔ 採信·須自行重得）** ＝ `628-42(1)`（`0m`）⋀ `628-53(2)`（`0m`／`3.5m`）——**恰為 `GB-159` 之二宗**。

### 二-3　`自誤 353`（責 ＝ **發單側**）

**形**：**以受詞之<u>相關述詞</u>代其<u>operative 述詞</u>，致其空集被誤讀為「本批無事可辦」。**
`W-G.9-269 §二 p1` 令重量之 `A` ＝ `K-9-9 二` 不交叉之違反宗集；其出處為 `GB-67` 之成因鏈（該鏈將「破量」與「不交叉違反」繫為同一幾何事件）。然**病一之機制所消費者為 `K-9-23` 閘一之判**，⛔ 不交叉。⇒ 錨點更正修除不交叉違反後，`A` 成空集，而閘一之不合格宗**仍為二宗**。
🔒 **與 `344` 之別**：`344` 係判別力校準組與本批受詞之**敏感度**錯配；本則係**受詞本身**之述詞錯指。
**通則**
> **凡令重量一「母體」，須自<u>本批之方法所實際消費之判</u>回推其述詞，⛔ 自其成因鏈或其相關述詞取之。立單時須逐項自問：「本批之碼將讀哪一個欄／哪一道閘之判？」——該欄即受詞。**

### 二-4　併記（**射程外·⛔ 本批辦·須具名於報告**）

🔴 現態下 `A = 不合格`（`K-9-12` 矩形容納）者 **`19` 宗**（`21` 減去 `B` 亦不合格之 `2`）。**若他日 `K-9-12` 亦轉主動，其土地後果遠大於本批。**
🛑 **本批⛔ 觸及**；列為**獨立批**之候辦，其受詞、母體與土地後果由發單側另單定之。⛔ 於本批之報告作任何修法主張。

---

## §三　`p3` 之停機：**二裁**

### 三-1　裁一：**重烤係 CC 可代**（舊註已被取代）

`docs/reports/W-G.9-122_CC交接文.md §七` 載「九份 baseline 之重產 ＝ **KL 於 UI 之動作**」。
🔒 **該註已被取代**：倉內確有 `verify/run_verification.py:417-420` 之 `WV_BAKE=BASELINES`（原地覆寫）機制，且該註所載之阻礙（圖資之取捨）已由 `K-9-20` 結清 ⇒ **CC 得代行**。
🛑 **⛔ 追改該交接文一字**；本裁以本補令承載，並須於 `p3` 之報告具名其取代關係。
🔒 **「九份」之清單 ＝ `docs/reports/W-G.9-122_CC交接文.md:87-90`**（CC 機械抽出·核可）。🩸 **CC 之唯一性 assert 命中二列（`87`／`146`）而當場攔下——核可並立為例**：錨之唯一性須自證，命中 `>1` 即須逐處讀過再窄化框，⛔ 取首筆。

### 三-2　裁二：🔴 **`p3` 移出前置階**（發單側之排程錯）

`CLAUDE.md:1314` 逐字令重烤時須自 `_DECLARED_EXTRAS` 移除 `指數名次`；而 `_DECLARED_EXTRAS` **只存在於 `verify/run_verification.py`**——**四個生產碼檔之一**。
🔴 **⇒ `p3` 結構上⛔ 於零生產碼階完成**。發單側將一個**必須動生產碼**之工項置於零生產碼批，係排程之誤。
🔒 **⇒ `p3` 自前置階移出**，改列**本體階**之獨立工項（生產碼·側支·**須 KL 逐 `commit` 放行**）。
🛑 **本批⛔ 辦 `p3` 之任何一款**，含「先跑一次重烤看看」——其會**原地覆寫** `verify/baselines/**`。
🔒 **前置階自此僅 `p1`（重定後）＋ `p2`** 二項。

---

## §四　三項併呈之處置

| # | CC 所報 | 裁 |
|---|---|---|
| `1` | 本單於期初態⛔ 在倉，而開場文令「自倉讀本單」 | **發單側之誤**：單係經 KL 於對話側交付後方入倉 ⇒ 開場文之「正本全在倉內」對**本單自身**不成立。爾後開場文須改為「**本單由 KL 交付；其餘正本自倉讀**」。⛔ 鑄號（其為措辭之誤，未致任何判之誤） |
| `2` | 單所載期初態已過期；主 checkout 之本地 `wip` 落後 `origin` `90` commit | **核可其處置**（依單以 `ls-remote` 實查為準）。🛑 **本地落後 `90` commit** 須入交接註為一坑：**凡於主 checkout 動作前，須先 `git ls-remote` ⋀ `left-right` 實查，⛔ 假定本地為最新**（坑 `23` 之同族） |
| `3` | `p2` 母體、`p3` 九份、`S-5` 之 `GB` 框，三者單所載之數與實測相異 | `p2`／`p3` 依單之明文「以實測為準」——**核可**。🔴 **`S-5` 之 `GB` 框係發單側之瑕**：單載 `159`／`161`／`[12, 87]` 而未具名其框；其正解為**標題形 ∪ 表列形**之聯集（CC 實測所得，與單相符）。⇒ **爾後凡出艙四簿之數，須同格具名其框**（與雜湊二式並報同理）。⛔ 鑄號（值正確、框未具名，併入 `自誤 350` 之通則射程） |

---

## §五　鑄號之授權

🔒 **本補令得鑄者 ＝ `自誤 353` 一號；⛔ 逾此。**
（次號當場現查：期初自誤 相異 `345`／MAX `352`／缺號 `[106]`；`GB` `159`／`161`／`[12, 87]`（框 ＝ 標題形 ∪ 表列形）。）
🛑 意指占用查一律**二形並取**；哨兵執行期自倉掃出、字面⛔ 出艙。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §六　續辦（前置階之收尾）

| 序 | 事 | 落點 |
|---|---|---|
| `1` | `自誤 353` 之鑄 | 主線（零生產碼·得逕推） |
| `2` | **`p1′`**：依 `§二-2` 之重定受詞重量——逐宗出艙 `K-9-23` 閘一（`_k923_gate1`）之判，二情境分列；載每宗之歸戶／`G`／宗地寬度／`驗_宗序`／三閘分判；母體基數同格出艙。**判別力**：須同時出艙 `B = 合格` 與 `B = 不適用` 之宗數，三類和須等於配地宗數（⛔ 只出不合格者） | 主線（零生產碼·得逕推） |
| `3` | `§二-4` 之併記（`K-9-12` 之 `19` 宗·**⛔ 修法主張**）＋ `§一-2` 之 `GB-67` 末端追加（病二已修除不交叉違反） | 主線 |
| `4` | 前置階報告補全 ＋ 坑入交接註（CRLF 成因未明／本地落後 `90` commit／錨唯一性 assert） ⇒ **停**，候發單側復驗與本體階之單 | — |

🔒 **本體階（另單）之工項（發單側預告·⛔ 本批辦）**：`p3`（`_DECLARED_EXTRAS` 之改 ＋ 九份重烤·生產碼·須 KL 放行）／病一之 `c1`〜（`K-9-23` 閘一轉主動 ＋ `K-9-17` 遞補迴圈 ＋ 第 `1` 宗境界線不動）／其後 `GB-160` 閘 `γ` 重量 ＋ `GB-159` 三條件結清。
🔒 **`GB-159` 仍有效**：二宗近乎零之配地，其配地圖與負擔表**⛔ 對外**。
🔒 **舊錨態之數（`p3` 完成前）**：凡自 baseline 取得之數皆帶舊錨，引之須同格標「**舊錨態·待重烤**」。

本補令由施工窗於續辦前**原封入倉**（`docs/orders/W-G.9-269_補令一_p1受詞之重定與p3之二裁.md`），⛔ 動一字。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
