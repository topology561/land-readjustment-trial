# W-G.9-246′　逐宗驗證站 **`_lot_gate`** 續辦（觀測模式·縮小版）

> 承 `W-G.9-246`（工項零已入倉·`34230bb`）與 `-246R` 停機上呈之四裁（發單側 `2026-09-07`）。
> 🔴 **生產碼**：`app.py` ＋ `verify/stepg_pipeline.py` 二檔；**行為零變更**（只判不動）。
> 🔒 **本單依 `自誤 305` 之通則：只載<u>意圖、不變量、驗收</u>；施工形由 CC 定**，逐字形之偏離⛔ 需上呈，惟須具名。
> 側分支 `verify/W-G.9-246-c1`（自 `61e503b` 起）候發單側復驗、KL 放行。
> 🛑 ⛔ 依 verdict 動任何控制流；⛔ 動 `_solve_G_one`／`_corner_first_lot_G`／`_run_step_g_impl` 之既有敘述；⛔ 實作 `r2`／五級；⛔ 動 `verify/` 其他檔。

---

## §零　開工閘

**`S-0`**／**`S-0b`** 依現行款。**`S-1`**：本號已占（工項零），⛔ 重查；只查對照乙 `W-G.9-248` 須 `0/0/0`。
**`S-3`（`61e503b`）**：`app.py 445585e238c26154`／`stepg_pipeline.py 508708b9c792f6a8`／`run_verification.py d0d531f7dc051b65`／`fixture_wf_ns_wiring.py b17826a6f7b98bf5`；餘七檔同前。**本批只得動前二者**；`run_verification.py`、夾具與餘七檔前後逐位不變。

---

## §一　意圖

把 `配地計算總規格_v3.md` ⑧–⑪（更正節為準）之逐宗判定，做成**一處**可被 app 與 harness 共用的判定式，掛在每宗解定案之後、鏈推進之前，**只把判定結果寫進新欄**。目的 ＝ 在接閘前，讓 KL 與 harness 都看得到「若接閘，哪些宗、因哪一項、差多少」。

---

## §二　不變量（缺一即停機）

| # | 不變量 |
|---|---|
| `I-1` | **判定式只寫一處**（`app.py` module 級純函式 `_lot_gate`，⛔ 讀 `st`、⛔ 副作用）；harness 經 `ns["_lot_gate"]` 取用 ⇒ **`_WF_NS_NAMES` 須補列 `"_lot_gate"`**（`fixture_wf_ns_wiring.py` 綠為證）。 |
| `I-2` | 四項判定各為**四態**（`合格`／`不合格`／`不適用`／`不可判`），沿用既有四態基建（`_T3_NA`／`_t3_fmt_gate`）。`A` 幾何 ＝ `K-9-12` 矩形；`B` 藍影 ＝ `K-9-23` 閘一（**僅有 sideline 之鏈之第 2 宗**，KL 語序·碼側 index `1`）；`C` 面積 ＝ ⑨ 之有效值式，街角第 1 宗走 ⑩ 之 `max`；總判 `rejected ＝ 任一不合格`。 |
| `I-3` | 街角第 1 宗：`A`／`B` ⇒ `不適用`。末端塊之鏈：`B` ⇒ `不適用`。`e ＝ 0` ⇒ `C` `不適用`。所需輸入取不到 ⇒ `不可判` 並具名缺者；**⛔ 當合格、⛔ 當不合格**。 |
| `I-4` | 掛載點 ＝ **每鏈之解定案後、鏈推進前**（`_W_prev_left/right ＝ res['W_far']` 之前；harness 右鏈有二次 solve ⇒ 掛於定案之 `res`）。app 左右鏈 ＋ harness 左右鏈，**四站**。 |
| `I-5` | 四站只做二事：呼叫、寫欄（`驗_` 前綴）。**⛔ 依 verdict 寫任何 `if`**——⛔ `continue`、⛔ 改 `_W_prev_*`、⛔ 改界面單線鏈（`D-2b-23【甲】`）。 |
| `I-6` | 既有欄一字不改；`驗_*` 欄之量須含各項之實測值與閾值（`A`：`W×D`；`B`：`G`／`area(藍影)`／`Δ`；`C`：`G`／`e`／街角範圍面積）。 |
| `I-7` | harness 之 `B` 輸入自 `ctx['corner_range_polys'][(blk, side)]`（`-247` 已鋪）；`C` 輸入自 `eff_min_build_by_blk`（`-247` 已鋪·主跑二情境為 `{}`）。app 側 `C` 自 `st.session_state[K91_SS_MBA_EFFECTIVE]`。 |

---

## 【驗收】

| 證 | 內容 | 判 |
|---|---|---|
| **`V-0`** | `run_all` 全名目 `rc` 前後逐項相同（既有 `24` 紅逐項具名） | 判翻轉即紅 |
| **`V-A`（🔴 本波首次為真）** | 二情境 `got_G值_退縮{tag}_partial.csv`（`-247` 工項一·落檔於 `run_verification.py:727` `except` 內 ⇒ raise 時執行）**去除 `驗_*` 欄後逐位 diff 空**；`驗_*` 欄**非全空**；`R1` 全部宗皆有 verdict | 缺一即紅 |
| **`V-S` 觀測統計（次一單之地基）** | 每情境 × 每街廓 × 每鏈：`A`／`B`／`C` 四態計數；**不合格宗逐宗具名**（暫編地號、鏈、項、量、與閾值之差）；`不可判` 逐宗具名缺者。🔒 主跑 `eff ＝ {}` ⇒ `C` 恆 `不適用`——**具名**，⛔ 讀為全過 | 缺即紅 |
| **`V-S′` `C` 之端到端（拋棄式）** | 以 `run_step_g(…, eff_min_build_by_blk={'R1': X})` 擇 `X` 使 `R1` **至少一宗** `C` `不合格`、至少一宗 `合格`，二者於 partial 之 `驗_*` 欄可見 | 缺一即紅 |
| **`V-D` 純函式對拍** | `_lot_gate` 合成輸入：四項各四態至少一例；邊界 `G ＝ area(藍影)` 須 `不合格`、`G ＝ e` 須 `合格`、街角第 1 宗之 `max` 二臂各一例 | 缺一即紅 |
| **`V-B`／`V-C`** | 二檔 diff 逐行人閱；十檔 ＝ **變 `2`／不變 `8`**，夾具另計 **不變** | — |
| **`V-K` KL 目檢（app 側·非自動）** | KL 於 app 實跑 UC9898、開 Step G 表、讀 `驗_*` 欄，回報 (a) 欄在且非空 (b) 實務上看來不對之 verdict 逐宗指名。此為 `B`／`C` 於 app 側活著之**唯一**證據 | 由發單側轉呈 |
| 判別力自證 | `V-A` 對照：改一既有欄值須紅；`V-S′` 對照：`X ＝ 0` 須全 `不適用` | — |

🔒 凡出數載三軸；宣告框三數分列。

---

## §三　上呈（候 KL·⛔ CC 自裁）

本批綠後，發單側以 `V-S`／`V-S′`／`V-K` 呈 KL：**接閘之射程與影響面**。KL 據以裁接閘單（`-248`：依 verdict 剔宗、整包回滾、左右雙鏈）。不合格宗數異常（`0` 或近全數）⇒ 先疑判定式。

SELF_SHA256: 33f9ad776cbf738d2ec71b3676caa67154a236a19d189eadbbe8de56887bdfc4
