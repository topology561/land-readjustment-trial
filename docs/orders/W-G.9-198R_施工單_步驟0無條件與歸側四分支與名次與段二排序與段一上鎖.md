# 🔧 `W-G.9-198R` 施工單：**步驟 0 回歸無條件 ＋ 歸側四分支 ＋ 街角指數<u>名次</u>與段二全域排序 ＋ 段一上鎖**

> **性質**：🔴 **幾何影響批·含生產碼**（`app.py`）。⇒ **`commit 1`／`2`／`3` 皆須<u>備妥後停機、請示 KL 方得 `push`</u>**。
> **出單窗**：claude.ai（接 `W-G.9-204` 交接文；經 `W-G.9-205` 一批落地）
> **基座**：`wip/s1-endpart`（發單側現查 ＝ `6a3f5feb81823f8ff84be7d269fa78056d3e3556`·`2026-09-02`）
> 🛑 **該碼⛔ 為判準**——判準見 `S-0a`，綁**性質**、⛔ 綁 commit 身分（`自誤 246`／`252`）。

🔒 **本單係 `W-G.9-198` 之<u>重發本</u>。** 舊本已依 `VR-088` 入倉為**存查**（`docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md`·自簽 `f16bdb9e…`）。
🛑 **舊本⛔ 得作為施工依據。** 其下列三處已被 `K-9-24` 消滅，本重發本**整段作廢重寫**：舊 `R2-3`（「⛔ 得動 `k6_step0_block_locked` 保守替代一字」）／舊 `R2-6`／舊 `D2-d`（皆繫已被取代之 `K-9-21`）。
🔒 **舊本可沿用者**：`§二` 之 `R1-1`〜`R1-9` 與 `D1-a`〜`D1-h`（本單 `§三` 逐字承接，僅編號與少數條文調整），及 `§四` 附帶量測。

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　本單之寫法

🛑 **本單只寫<u>結果態</u>，⛔ 寫<u>操作步驟</u>。** 凡 `§三`〜`§五` 所列 `R*` 皆係「落地後系統須呈現之性質」；**其達成手段由 CC 自定**，只要結果態逐項成立即為正辦。
🔴 CC 若發現某一結果態與另一結果態**互斥**、或與正典逐字牴觸 ⇒ **停機上呈發單側**（⛔ 轉呈 KL·`常規二`／`常規三`），⛔ 得自行取捨。

🔒 **一切「判別力」閘均依裁 `H`**：判準綁**性質**，⛔ 綁計數代理。本單出現之計數皆為**待歸類之材料**、⛔ 非 operative 判準。

### §零-1　🆕 `GB-139`：行尾之處置（本單新增·🛑 ⛔ 得省）

發單側於 `W-G.9-205` 後實查 KL 本機：`core.autocrlf` ＝ **`true`**，設於 **system 層**（`C:/Program Files/Git/etc/gitconfig`）；既有檔 `git ls-files --eol` 得 **`i/lf` ／ `w/mixed`**。⇒ 三款：

1. 🔒 **`clone` 時仍須 `-c core.autocrlf=false -c core.quotepath=false`**（驗證側取原貌之必要條件·⛔ 受本項影響）。
2. 🔴 **`add` 既有檔一律走常規 `add`**，⛔ 以 `-c core.autocrlf=false` 覆寫——否則工作樹之 `CR` 會寫進 blob、全檔偽異動、破 append-only。
3. 🔴 **凡閘之受詞，一律取<u>倉側</u> `git cat-file blob`**，⛔ 取工作樹側位元組——對既有檔，「工作樹 ≡ blob」之判準**恆偽紅**。

### §零-2　🛑 ⛔ 得動之受詞（逐項具名）

- `k6_merge_groups`／`k6_shares_segment` 之**函式本體**——⛔ 改一字。
- `_pk_one_side_v12` 內既有之**三 `score_*` 算式、權重、`round` 位數、`qualified.sort` 之二鍵**——⛔ 改一字。
- `k94_assert_baseline_touch` 及其訊息——⛔ 改一字。
- `_ni` 之持久化還原路徑——⛔ 改一字（`GB-136` 另案）。
- `K-6 §一` 合併群述詞／`K-9-22`（表示層）／段二鍵序／段三／段四——⛔ 改一字（`K-9-24` 末段逐字所令）。

🔑 **⛔ 在此列**：`k6_step0_block_locked` ——舊本令「⛔ 改一字」，**本重發本反令其死碼化**（見 `R2-1`）。

---

## §一　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之**性質**（⛔ 綁 commit 身分） | ① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` **逐列⛔ 含** `app.py`、⛔ 含任何 `verify/**.py`。二者任一不符 ⇒ 停機上呈。🛑 `ls-remote` 全 `40` 碼須**記錄並回報**，但⛔ 作判準——倉內合法多出任意數量之**零生產碼** commit 皆⛔ 得使本閘紅 |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-198R_施工單_步驟0無條件與歸側四分支與名次與段二排序與段一上鎖.md` | 🔴 逐項以**字樣錨**具名（⛔ 用行號）；每一 🟡 須逐筆歸類為「零參照」或「判別力參照」（裁 `H`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗 | `declared` ＝ `recomputed`（受詞 ＝ 自檔首至該列前一個 `\n` 含之） |
| `S-0d` | 🔴 **改前基線之凍存** | 開工前先跑一次現行 harness 通道，將**四閘違規全集（不截斷）**與「第 1 宗街角地逐候選評點明細」之全部出艙**存為改前態**（檔名帶基座 commit 短碼）。⛔ 得於改後才回頭重建 |
| `S-0e` | 🆕 **`K-9-25` 之現況查核** | 正面列舉：`app.py` 與 `verify/stepg_pipeline.py` 二檔中，街角評選量測路徑（臨截角長／臨側街長／真交集面積／`G`）上**是否存在**任何 `buffer`／`snap`／`simplify`／`make_valid` 之**密合性**前處理。逐處具名其符號與所在函式；**若一處也無 ⇒ `K-9-25` 於碼面⛔ 須改一字**，`R2-4` 降為「⛔ 得新增」之禁令 |

### §一-c　`S-0b` 之已知 🟡（🔒 發單側已對**本檔**實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6` 六檢）

🔒 **發單側實跑結果**：🔴 機械停機款 **`0`【項】**；🟡 **`5`【項】**，逐項即下表；`P-5` 自簽 ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。

| 檢 | 觸發之字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-1`／`P-4` | `` \| # \| 落地後須成立之性質 \| `` | **判別力參照 ⇒ 具名豁免**：係結果態之**索引表頭**；各列之母體、框與方向逐列自載於該列本文（如 `R2-1` 之受詞 ＝ `k6_step0_block_locked`、`R3-3` 之受詞 ＝ 步驟 0 之呼叫鏈） |
| `P-1` | `` \| 閘 \| 受詞 \| 判準 \| `` | **判別力參照 ⇒ 具名豁免**：係閘之索引表頭，各閘之母體／所查範圍／方向逐列具名於同表「受詞」與「判準」二欄 |
| `P-4` | `` - **`K-9-24`**（標題列 `` | **零參照 ⇒ 具名豁免**：係**正典標題列之引用**，其「一／二／三／四」係節內序號、⛔ 方向量；所引逐字須由 CC 自倉重抽（`§二` 已明令） |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造·取既有實跑）**：同器對 `W-G.9-197` 得 `5`【項】／`-198` 改版得 `6`【項】／`-199` 得 `0`【項】／`-200` 得 `2`【項】／`-201` 得 `1`【項】／`-203` 得 `0`【項】／`-205` 得 `1`【項】 ⇒ 本器⛔ 恆綠亦⛔ 恆紅。

---

## §二　正典受詞（🛑 CC 開工時須自倉重抽·⛔ 以本單所引為準）

`docs/rulings/K-6_街角地分配程序與可分配判準.md`：

- **`K-9-24`**（標題列 `### 🔒 K-9-24　`）——**一**（步驟 0 回歸無條件·`K-9-21` 取代）／**二**（歸側四分支五款）／**三**（舊特例取代·平手法保留）／**四**（上鎖之受詞 ＝ 段三）。
- **`K-9-25`**（標題列 `### 🔒 K-9-25　`）——無地號縫隙⛔ 作密合處理。
- **段一**（標題列 `### 段一：預掃（街廓內先評一輪，完全不看外部）`）之上鎖逐字（`K-6:66-67`）。
- **段二**（標題列 `### 段二：全域排序（僅排待處理街角之候選）`）之鍵序逐字：① 名次 → ② 街角試算 `G` → ③ 指數數值 → ④ 暫編地號字典序最小。
- **步驟 0**（`K-6:57-60`）之命名／位次／面積／幾何逐字。

🔒 **現況正面列舉（發單側自倉現查·態 `6a3f5fe`·母體 ＝ `app.py` 與 `verify/stepg_pipeline.py` <u>二檔</u>·框 ＝ 列）**

| 字樣 | `app.py` | `stepg_pipeline.py` |
|---|---|---|
| `上鎖`／`未上鎖`／`預掃`／`集中規則`／`K-6-B`／`歸側`／`K-9-24`／`K-9-25` | 逐項 **`0`** | 逐項 **`0`** |
| `合併群`（對照組） | `7` | `0` |
| `縫隙`（對照組） | `3` | `0` |
| `k6_step0_block_locked` | **`3`**（`def` `1` ＋ 註解 `1` ＋ 呼叫 `1`） | `0` |

⇒ 量測器**非恆紅**（對照組有值）亦**非恆綠**。🛑 CC 須自行復算一次，⛔ 照抄本表。

---

## §三　`commit 1`　**名次之產出與出艙 ＋ 段二全域排序 ＋ 措辭更正**（🔴 預期：**⛔ 有土地後果**）

### §三-1　結果態

| # | 落地後須成立之性質 |
|---|---|
| `R1-1` | **每一側之每一候選**（含 `qualified` 與 `eliminated`、含**段一全滅**之早退分支）皆帶一個**街角指數名次**。其值 ＝ 該側 `group` 全體依 `priority_index` **降冪**之名次，`1` 起算、**連續**（`1,2,…,n`·⛔ 跳號）。 |
| `R1-2` | `R1-1` 之**平手決之**⛔ 另立第二套：其平手鍵須與現行 `qualified.sort` **同源**（`priority_index` 取 `4dp` 判平手 → `_pre_position_rank` 升冪）。🛑 ⛔ 引入任何新的比較量。 |
| `R1-3` | 出艙「第 1 宗街角地逐候選評點明細」**增一欄**，欄名逐字 ＝ **`指數名次`**，位置 ＝ `總分` 之**後**、`原位次(投影序)` 之**前**。**該欄逐列皆有整數值**（⛔ 有 `—`、⛔ 有空白）。 |
| `R1-4` | 既有 `23` 欄之**欄名、順序、值**逐格⛔ 變（新欄係**插入**、⛔ 取代）。⚠️ **例外之預告**：若某欄因 `R1-3` 而由「含 `—`」變為「全數值」，其零之**字面**可能由 `'0.0'` 變為 `'0'`（型別轉換之最小表示法·成因已於 `W-G.9-197` 閉合）⇒ **該類差須逐格具名並證其數值相等**，⛔ 得計入 `D1-a` 之土地後果。 |
| `R1-5` | 🔴 **`GB-133` 之失效條件成就**：段一全滅之側，其各候選之**指數與名次皆已算出並出艙**，且該出艙**可供段二鍵序 ① 直接消費**（＝ 段二之實作**確實讀該欄／同一資料源**，⛔ 自行重算）。 |
| `R1-6` | **段二全域排序之出艙存在**：一份**僅含待處理街角**（＝ 該側無任一候選達標）之候選清單，依鍵序 ① 名次 → ② 街角試算 `G` → ③ 指數數值 → ④ 暫編地號字典序最小 排定，出艙為可檢視之表（欄須含：街廓、端、暫編地號、名次、街角試算 `G`、指數數值、最終序位）。 |
| `R1-7` | 🛑 **段二之出艙係<u>唯讀</u>**——⛔ 得被任何既有分配邏輯消費、⛔ 得改變任何既有分支之走向。（段三尚未落地，其消費端不存在。） |
| `R1-8` | **措辭更正**：`app.py` 內 `` # 🆕 V13 修正 #3 + #7：第二關優先權指數採用三項法定加權 `` 一段（現位於早退**之前**），其「第二關」之表述改為對**落選候選亦精確**者。🛑 **本項係生產碼**（改 `app.py` 一字含註解即是·`W-G.9-195 裁 E`）⇒ **併入本 commit**、⛔ 另開 commit。 |
| `R1-9` | 🔒 **側無關·案無關**：`R1-1`〜`R1-8` 之實作⛔ 出現任何街廓名（`R1`〜`R6` 等）、側標（`左`／`右`／`p1`／`p2`）之硬編分支，⛔ 出現任何本案特有常數（`308.17`／`226.01`／`3.5` 等）。 |

### §三-2　`commit 1` 之驗收

| 閘 | 受詞 | 判準 |
|---|---|---|
| `D1-a` | 🔒 **土地欄零變** | 以 `S-0d` 之改前態為基準，「第 1 宗街角地逐候選評點明細」之**土地欄**（`真交集`／`整筆幾何`／`範圍面積`／`G估`／`真G`／`門檻`／`達標`／`原位次(投影序)`／**`選中`**）**逐格有變 `0` 格**。有一格即 ⇒ **停機上呈發單側**（⛔ 自行歸因） |
| `D1-b` | 四指數欄零變 | 四指數欄**數值真變 `0` 格**；字面差須逐格具名並報其數值是否相等（承 `R1-4`） |
| `D1-c` | 🔴 **新欄之判別力** | `指數名次` 欄**⛔ 全同值**（若全為 `1` 或全為同一數 ⇒ 判別力零 ⇒ 紅）；且其與 `總分` 之降冪序**逐側一致**（機械驗·⛔ 目視） |
| `D1-d` | 🔴 **`R1-5` 之機械證** | 段二排序之鍵序 ① **確實取自 `指數名次` 之同一資料源**——以「人為擾動該值 ⇒ 段二輸出之序**必變**」證之。**不變 ⇒ 未接線 ⇒ 紅** |
| `D1-e` | `R1-7` 之機械證 | 段二出艙**存在**且**⛔ 有任何呼叫端消費**（正面列舉其符號於 `app.py`／`verify/**.py` 之出現處，逐處具名其係「產出」抑或「消費」） |
| `D1-f` | 四閘違規全集 | 以 `probe_WG9168_viol_full` 取**不截斷**全集，二態差集**逐格歸因**；🛑 凡出現於 `winner`／達標候選列者 ⇒ 停機上呈 |
| `D1-g` | `--selftest` | 既有全部自檢仍過；⛔ 刪改任一既有自檢 |
| `D1-h` | 🔴 **既有終局⛔ 移位** | harness 之終止點與 `W-G.9-197R` 所載者**逐字相同**（`k94_assert_baseline_touch`／`R5` `628-53(2)`／差 `38.803m`）。**若終止點移動 ⇒ 停機上呈**（⛔ 視為改善） |

🛑 ⛔ 得以「對帳器綠」宣稱無行為變更（`GB-111`：`128` 列真實變更於對帳器上完全不可見）。
🛑 ⛔ 得以 harness 之綠宣稱土地後果（`GB-123`）——回報須逐字寫「**土地後果⛔ 確認·待 KL UI 實跑**」。

### §三-3　`push`

🔴 **備妥後停機**，逐字回報 `D1-a`〜`D1-h`、`numstat`、`app.py` 新 blob、`def main`／`_pk_one_side_v12` 之**新 AST 區間**（⛔ 沿用舊值）。**⛔ 自行 `push`。**

---

## §四　`commit 2`　**步驟 0 回歸無條件 ＋ 歸側四分支 ＋ `K-9-25` 量測前提**（🔴 **確有土地後果**）

🛑 **`R2-1` 與 `R2-2` 係<u>連鎖</u>，⛔ 得拆入不同 commit。** 理由：解除步驟 0 之封鎖後，本案 `R4` 之 `628(1)`／`628-1(1)` 即合併為一片同時跨占左右兩街角者；若此時歸側規則尚未存在，該片之歸屬**無所依循** ⇒ 會產生一個正典未定義之中間態。⇒ **二者同 commit。**

### §四-1　結果態

| # | 落地後須成立之性質 |
|---|---|
| `R2-1` | 🔴 **步驟 0 回歸無條件**：`k6_step0_block_locked` 之保守替代**⛔ 再擋任何街廓**。其實作須**死碼化而非刪除**——函式本體與其逐字說明**保留**，於其首加註「**經 `K-9-24 一` 取代·⛔ 再被呼叫**」，並使 `k6_step0_merge` 之呼叫點**⛔ 再據其結果 `continue`**。🛑 `_diag["blocks_locked_k921"]` 之鍵**⛔ 刪**（供對照）；其落地後之值須為**空**。 |
| `R2-2` | 🔴 **歸側四分支**（`K-9-24 二`·五款逐項）：設 `M` 為同時跨占同一街廓左右兩街角規定範圍者（⛔ 論其係步驟 0 所生抑或地籍本來一片）——① 分別以左右街角**第 1 宗位置**試算 `M` 之 `G` 與街角指數，並使 `M` 在各該側**照常參與評選**；② **兩側皆拿得下** ⇒ 歸**指數數值較大**之側，**並列 ⇒ 取前緣線 `p1` 側**（決定性·**禁隨機**）；③ **僅一側拿得下** ⇒ **歸該側**（⛔ 論其指數是否較小）；④ **兩側皆拿不下** ⇒ 兩側皆不歸、`M` 辦原位次；⑤ **一經歸定，另一側視為未被 `M` 跨占**，該側扣除 `M` 後照常評選其餘跨占者，**若無 winner（含候選集合為空）⇒ 強制留設抵費地，範圍嚴格等於該街角規定範圍**。 |
| `R2-3` | 🛑 **「拿得下」之逐字定義** ＝ `G ≥ 該側街角規定面積`（達標）**∧** 於該側**全部達標候選**中之街角指數為最大（＝ 該側 winner）。⛔ 得以「第 1 名」單獨代之——**第 1 名而未達標者⛔ 拿得下**。 |
| `R2-4` | 🔴 **`K-9-25` 之量測前提**：街角評選之各項量測（臨截角長／臨側街長／真交集面積／`G`）⛔ 得對「地籍界線與街廓邊界間之無地號縫隙」作任何**密合、補齊、吸收或歸併**之前處理，一律逕依現況地籍幾何。依 `S-0e` 之查核：若現況本無此類前處理 ⇒ 本項落為**禁令**（⛔ 新增），碼面⛔ 須改；若有 ⇒ **須於街角量測路徑上移除或旁路**，並逐處具名。 |
| `R2-5` | **舊特例之取代**：`K-6 §二`「一片同時跨占同街廓左右兩街角」之「**真切為兩片**」及其四款分支⛔ 得再被實作或呼叫。🛑 若現行碼中**根本不存在**該切分之實作 ⇒ 正面列舉其不存在（母體 ＝ 二檔·框 ＝ 列），本項即以「⛔ 得新增」之形成立。 |
| `R2-6` | **歸側之出艙**：新增（或於既有診斷增欄）一份可檢視之清單，逐筆載：街廓、跨占之左右街角、`M` 之暫編地號、**左側**（`G`／門檻／達標／指數／是否 winner）、**右側**（同）、**歸側結果**、**歸側之依據款次**（②／③／④）、**另一側之後果**（有 winner ／ 強制抵費地及其面積）。 |
| `R2-7` | 🔒 **側無關·案無關**：⛔ 出現街廓名、側標、本案常數（`226.01`／`225.42`／`308.17`／`1521.05`／`457.73`／`0.0103`／`7.9615` 等）之硬編。判準綁**性質**。🛑 **`p1` 側之平手退路**係正典所令之**幾何性質**（前緣線之 `p1` 端），⛔ 得以「左」或「右」之字面實作。 |
| `R2-8` | 🛑 **⛔ 辦者**：⛔ 因本批而改動 `K-9-22`（原地號保留之表示層）一字；⛔ 因本批而重烤或重產任何基準。 |

### §四-2　`commit 2` 之驗收

| 閘 | 受詞 | 判準 |
|---|---|---|
| `D2-a` | 🔴 **土地後果之逐格歸因** | 以 `commit 1` 落地後之態為基準，「第 1 宗街角地逐候選評點明細」之**土地欄**逐格比對。**凡有變之格，逐格具名其緣由**（哪兩筆被合併、歸了哪一側、另一側成為什麼）。🛑 **⛔ 得只報「有 N 格變」** |
| `D2-b` | 🔴 **步驟 0 之解除確有咬到** | `_diag["blocks_locked_k921"]` 落地後為**空**；且 `_diag` 之合併計數較改前**增加**，增加之每一筆逐筆具名（街廓／歸戶／二暫編地號／共用界線長）。**若合併筆數零增 ⇒ 未咬到 ⇒ 紅** |
| `D2-c` | 🔴 **歸側之判別力** | 歸側出艙中，**至少一筆**之左右指數**不相等**（⇒ 走款②）；且該筆之歸側結果與「指數較大之側」**一致**。🛑 若全案無任一筆跨占雙街角 ⇒ **紅**（判別力零·代表未咬到） |
| `D2-d` | 🔴 **另一側之後果具名** | 凡依款⑤ 成為強制抵費地者，其面積須**嚴格等於該街角規定範圍**——逐筆報「規定範圍面積」與「實際留設面積」二數，**二數相等**方為過（⛔ 以「約略」帶過） |
| `D2-e` | 🆕 **`R2-3` 之反例證** | 正面列舉：是否存在「該側第 1 名但未達標」之候選。若存在，須證其**⛔ 被判為拿得下**（其側之歸屬⛔ 因它而定）。若不存在，據實報「本案無此反例」並說明該分支之守衛仍在碼中 |
| `D2-f` | 反靜默退路 | 歸側判定⛔ 得有任何 `else` 靜默預設（承 `GB-106`）；凡取不到值 ⇒ **停機報錯**，⛔ 視為「不跨占」或「歸某側」 |
| `D2-g` | 四閘違規全集 | 同 `D1-f`；差集逐格歸因 |
| `D2-h` | `--selftest` | 既有全部自檢仍過 |
| `D2-i` | 🔴 **既有終局⛔ 移位** | 同 `D1-h`。🛑 **`②-宗 圍堵閘破[R5]` 之破量亦須併報**（改前 → 改後）：既有實測 ＝ `56.8186`（`verify/out/probe_WG9187_baseline_76378d09.log:14`·上界 `1.5999`·宗數 `8`·深度 `45.71`）；KL `7fab020` 實跑得 `56.8187`。**若本批使該值改變 ⇒ 逐格歸因，⛔ 靜默** |

🛑 **本案之預期受詞**（供 CC 對照·🔴 **⛔ 判準**·🔴 **⛔ 寫入碼**）
`R4`：`628(1)`（左·真 `G` `1521.05`·門檻 `226.01`）與 `628-1(1)`（右·真 `G` `457.73`·門檻 `225.42`）同歸戶 `G009`、共用界線 `47.057897 m` ⇒ 合併為 `628(1)+`；依 `K-9-25 三` 之發單側重建，右臨截角 `4.9145` > 左 `4.9042` ⇒ **歸右**，左街角依款⑤ 成為**強制抵費地 `226.01 ㎡`**。
🛑 **CC 須報實算值，⛔ 沿用上開重建值**；若 CC 之實算與上開**方向相反**（歸左）⇒ **停機上呈發單側**，⛔ 自行歸因。
🔶 已知殘差（`VR-087`）：發單側重建 vs 生產之三筆差 ＝ 左臨截角 `0.0042` m／右 `0.0035` m／共用界線 `0.0061` m，同號同量級，成因⛔ 查明；**翻轉結論需 `≥ 0.010` m**。

### §四-3　`push`

🔴 **備妥後停機**，逐字回報 `D2-a`〜`D2-i`。**⛔ 自行 `push`。**
🔴 **土地後果之最終確認繫於 KL UI 實跑**（`GB-123` ⛔ 解除）：回報須逐字寫「土地後果⛔ 確認·待 KL UI 實跑」。

---

## §五　`commit 3`　**段一「上鎖」**（🔒 預期：**⛔ 有土地後果**·唯讀出艙）

🔑 **本 commit 之性質已因 `K-9-24 四` 改變。** 上鎖之受詞 ＝ **段三之合併群操作**（`K-6 §二 段三 1`「取該合併群中**未上鎖**之其他片」），而**段三尚未落地**（`VR-086`：現行碼只實作了段三-6 之結果而無段三-1〜5 之過程）⇒ **上鎖於本批⛔ 有消費端** ⇒ 落地後應為**純唯讀出艙**、土地後果為零。
🛑 **若 CC 落地後發現土地欄有變 ⇒ 停機上呈**——那代表上鎖被接到了它不該作用的地方。

### §五-1　結果態

| # | 落地後須成立之性質 |
|---|---|
| `R3-1` | **上鎖之判準**：某街角於段一**定案**（＝ 該街角之候選中存在任一筆 `G ≥ 該街角規定面積`）⇒ 該**街廓內所有跨占該街角規定範圍之土地**（🛑 **含落選者**）取得「上鎖」狀態。 |
| `R3-2` | **上鎖之效力（逐字·二向皆須）**：上鎖之土地 ⛔ **得被任何合併群抽走**（不出）∧ ⛔ **得作為合併標的**（不進）。 |
| `R3-3` | 🔴 **上鎖之受詞 ＝ 段三之合併群操作**（`K-9-24 四`）。🛑 **上鎖⛔ 得作用於步驟 0**——步驟 0 已回歸無條件且完成於段一之前。若實作使上鎖凍住步驟 0 之同街廓同歸戶合併 ⇒ **分配結果會不同 ⇒ 紅**。 |
| `R3-4` | 上鎖狀態**可出艙**：逐筆載：街廓、暫編地號、所跨占之街角、上鎖與否、上鎖之緣由（哪一個街角定案）。 |
| `R3-5` | 🛑 **本 commit 之上鎖係<u>唯讀</u>**：⛔ 得被任何既有分配邏輯消費、⛔ 得改變任何既有分支之走向（段三之消費端不存在）。 |
| `R3-6` | 🔒 **側無關·案無關**：⛔ 有街廓名、側標、本案常數之硬編。判準綁**性質**（`G ≥ 規定面積`／`跨占規定範圍`／`同街廓`），⛔ 綁塊名。 |

### §五-2　`commit 3` 之驗收

| 閘 | 受詞 | 判準 |
|---|---|---|
| `D3-a` | 🔴 **土地欄零變** | 以 `commit 2` 落地後之態為基準，土地欄**逐格有變 `0` 格**。**有一格即 ⇒ 停機上呈**（承 `R3-5`：唯讀） |
| `D3-b` | 上鎖集合非空且非全集 | 其基數 **⛔ 為 `0`**（⇒ 未咬到）**亦⛔ 為全部候選**（⇒ 判別力零）。二者任一 ⇒ 紅 |
| `D3-c` | 🔴 **`R3-3` 之機械證** | 正面列舉：上鎖之值於 `app.py`／`verify/**.py` 之全部出現處，逐處具名其係「產出」抑或「消費」；**⛔ 得有任一消費處位於步驟 0 之路徑上**（`k6_step0_merge` 及其被呼叫鏈） |
| `D3-d` | 反靜默退路 | 上鎖判定⛔ 得有任何 `else` 靜默預設（`GB-106`）；取不到值 ⇒ **停機報錯**，⛔ 視為「未上鎖」 |
| `D3-e` | 四閘違規全集／`--selftest`／既有終局 | 同 `D2-g`／`D2-h`／`D2-i` |

### §五-3　`push`

🔴 **備妥後停機**，逐字回報 `D3-a`〜`D3-e`。**⛔ 自行 `push`。**

---

## §六　附帶量測（🛑 ⛔ 阻塞本批之驗收·須逐項回報）

| # | 受詞 | 要回報者 |
|---|---|---|
| `M-1` | `②-宗 圍堵閘破[R5]` 之破量：倉內既有 `56.8186` vs KL `7fab020` 實跑 `56.8187`（差 `0.0001`） | 二值是否同一母體（宗數 `8`／深度 `45.71`／上界 `1.5999`）；差是否僅為 `4dp` 顯示末位。⛔ 修、⛔ 鑄號，繫 `GB-67` |
| `M-2` | `R3` 之 `Phase 8 抵費地碎片合併產生 MultiPolygon（2 片）⇒ 取最大片、小片幾何丟失而面積留帳` | 該警示於本批前後是否逐字不變；⛔ 修 |
| `M-3` | 🆕 `R4` 內之無地號面 | 生產路徑上該面之面積與其臨左／臨右截角長之**實算值**，與 `K-9-25 五` 之發單側重建值（`7.9615 ㎡`／`0.0958`／`0.0855` m）並列。⛔ 修、⛔ 鑄號，繫 `VR-087` |

---

## §七　落地點與報告

| # | 檔／物 | commit |
|---|---|---|
| `A-1` | `docs/orders/W-G.9-198R_施工單_步驟0無條件與歸側四分支與名次與段二排序與段一上鎖.md` | 🔒 **置於 `commit 1` <u>之前</u>之一個獨立零生產碼 commit**（承 `W-G.9-195 裁 F`） |
| `A-2` | `docs/reports/W-G.9-198R_…_執行報告.md` | 🔒 **零生產碼 commit**，CC 可逕推；內容 ＝ 三生產碼 commit **備妥但未推**之逐項量測 |
| `L-1` | `app.py`（`R1-1`〜`R1-9`） | `1`（備妥待放行） |
| `L-2` | `app.py`（`R2-1`〜`R2-8`） | `2`（備妥待放行） |
| `L-3` | `app.py`（`R3-1`〜`R3-6`） | `3`（備妥待放行） |

🔒 **`A-1`／`A-2` 與 `L-*` ⛔ 得綁在同一 commit**（承 `W-G.9-205` 之 `自誤 257`：本單已明定二者關係——**同批、不同 commit**）。
🛑 `commit 1`／`2`／`3` 之 SHA 一經備妥即**⛔ 得重做、⛔ 得 amend`**；放行時以**同一物件**之判準（全 `40` 碼 ∧ `tree` ∧ `numstat` ∧ `app.py` blob）核驗（`自誤 246`）。
🔒 **放行之粒度**：發單側將**逐 commit** 請示 KL，⛔ 一次放行三個。`commit 1` 放行後方進 `commit 2`。

---

## §八　⛔ 本單不辦

⛔ 辦段三（逐一嘗試·集中規則·跨街廓合併）——`VR-086`／`GB-104`〜`GB-106` 未解除。
⛔ 辦 `K-9-23` 藍影與二閘（結構下游於 `K-6-B`·KL 裁 `2026-09-01`）。
⛔ 辦 `K-6-A2` 寬深雙檢。
⛔ 辦 `GB-111` 凍存名單重產（繫 KL 已裁「俟二閘落地後」）。
⛔ 辦 `GB-136`（`_ni` 之區間夾制）／`GB-67`（`②-宗` 破量之成因）／`R5` 之屁股線終局／`R3` 之 MultiPolygon。
⛔ 辦九份 baseline 重產／波末重烤／泛化波。
⛔ 辦 `core.autocrlf` 之變更（KL 已裁：留至波末與重烤同批）。

---

## §九　攢批中（🛑 本單⛔ 辦·僅為交接之連續性而記）

`自誤 256`（對環境下全稱命令而未先量測環境之行尾）／`自誤 257`（同單二條款之關係未具名）／`自誤 258`（閘之字樣錨與同單 payload 落地字樣不符；閘未自扣其自身增量·`VR-083` 累犯）／`GB-139`（KL 本機 `autocrlf=true` 之二肢）／`W-G.9-203R` 之「`Δ` 恰 ＝ payload」不精確／`GB` 定義列式之正典化（`GB-138` 處置③）。
🔒 **次號現為**：自誤 `256`／`GB 139`／`VR 089`／戒 `40`／`K-9 26`。
SELF_SHA256: b291070e2bf1ee3aaf2c20200f385f64aef5c62e99016e8550548ba81d3dab54
