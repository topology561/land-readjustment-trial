# W-G.9-253　`K-9-29` 入典（逐筆地價·廢前置合併·合併恆走 `a′`·入池閘）＋ `-249` 改單 ＋ 宣告框補款 `⑦` ＋ 自誤二則 ＋ `-251` 段乙放行

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。
> 🛑 **三段**：段甲（工項零〜三·**零生產碼**·直推）／段乙（`-251` 段乙 `c1`／`c2`·**純 UI**·側分支）／段丙（`-249` plan v2·**觀測模式**·側分支）。
> 🔒 **本單整體⛔ 改變任何一宗地之配地結果**——`K-9-29` 之碼側落地（廢步驟 0）**有土地後果**，⛔ 在本單射程，另單辦（見 `§八`）。
> 🔒 KL 已裁四題（`2026-09-08`）：條文形「對」／標的宗「沿用 `wf_f0` G 最大者」／位次「前遞至投影序在前者」／`R4` 土地後果「確認無誤」。

---

## §零　開工閘

1. `git fetch`；本單以 **`origin/wip/s1-endpart @ bf5980a`** 為錨。錨不符 ⇒ 停機回報，⛔ 逕辦。
2. **號占用**（宣告框·`①`–`⑥` 逐字·三數分列·母體全 `docs/`）：發單側於 `bf5980a` 實測 `W-G.9-253` ＝ `D1 0`／`D2 0`／`D3 0`。**CC 當場復現**；非 `0/0/0` ⇒ 停機。
3. **次號當場復算**（⛔ 採信本單所載）：自誤 `MAX`（定義框＋範圍框·單檔）、`K-9` 主序列 `MAX`。發單側實測：自誤 `MAX 313` ⇒ 本單鑄 **`314`／`315`**；`K-9 MAX 28` ⇒ 本單鑄 **`K-9-29`**。不符 ⇒ 依常規以實測為準並於報告具名。
4. `verify/probes/probe_order_preflight.py` 對本單：`P-3`／`P-5` 須綠。

---

## §一　工項零：入倉二檔（原封·`cp`·⛔ 重打）

a. 本單 → `docs/orders/`（檔名即本檔名）。`P-5` `SELF_SHA256` 對拍。
b. **`交接文_W-G.9-252.md`**（發單側交接文·受詞為下一 `claude.ai` 窗）→ `docs/orders/交接文_W-G.9-252.md`。**僅於 KL 一併貼給你時辦**；其自載 `SELF_SHA256: 1783ad68…`，檔 `sha256[:16] = bcaf32a74b88f89b`。⛔ 執行其所列任何工項。KL 未貼 ⇒ 本款跳過並於報告具名。

---

## §二　工項一：`K-9-29` 入典（`docs/rulings/K-6_…md` **末端追加**·⛔ 上文一字不刪）

節標題形取既有 `K-9-28` 為範本。條文**逐字如下**（`>` 引文為 KL 原話·⛔ 改寫；其餘為發單側條文形·KL 裁「對」）：

> ### 🔒 K-9-29　**重劃前地價逐筆評定；廢前置合併；一切合併恆走 `a′`；五級之入池閘**（KL 裁 `2026-09-08`·`W-G.9-253`）
>
> > **KL 逐字**：「重劃前地價其實是不管是屬哪個歸戶地主的，就是以重劃前地號土地均會有一個重劃前地價，而決定了該筆地號土地重劃前地價後，才可以依據a'公式辦理合併分配或調配至另一筆a」／「還是回歸同一建築街廓內同歸戶相鄰土地也先不要辦理合併，就照同街廓內原位次排序的機制」／「街角地評定方式併入相鄰的RD或公設地的土地面積也是依據a'方式併入」／標的宗：「沿用 `wf_f0`『G 最大者』」。
>
> 🔒 **一（逐筆地價）** 重劃前地價係**逐筆重劃前地號**評定，⛔ 依歸戶、⛔ 依區段預設同價。最極端 `N` 筆 `N` 價。UI 步驟 D **逐筆點選**。
>
> 🔒 **二（廢前置合併）** `K-6 §二 步驟 0`（`K-6:57-60`「同街廓 ∧ 同歸戶 ∧ 相鄰 ⇒ 前置無條件合併」）**廢止**。`K-9-24 一`、`K-9-21` 隨之失所附麗而不再適用。`K-9-22`（表示層·⬜ 未落地）**作廢·無須落地**——無前置合併即無「原地號保留／區段回退」之題。`K-6 §二` 各段凡「步驟 0 後」之語，其受詞改為**未合併之逐筆暫編地號**。（⛔ 上開各節原句一字不刪。）
>
> 🔒 **三（合併恆走 `a′`）** 任何併入——同街廓同歸戶相鄰、跨街廓調配、街角地併入 `RD`／公設片——一律以 **`G(a_標的 + Σa′)`** 重算，`a′` 依 `配地計算總規格_v3.md:433` `a′ = a × p(原位置) / p(標的)`；**⛔ `Σa`**。
>
> 🔒 **四（標的宗）** 同街廓同歸戶相鄰數筆之合併：**標的宗 ＝ 個別原位次 `G` 最大者；並列取暫編地號字典序最小**（`wf_f0` 既有法·沿用）；其餘各折 `a′` 併入。
>
> 🔒 **五（位次·前遞）** 合併後之單元佔**投影序在前者**之位（`K-6:59` **保留**），以 `(a_標的 + Σa′)` 於該位**重算 `G`**（即算即驗）。來源宗離序後其後各宗自然前遞，**⛔ 留空位**。位次規則與 `a′` 方向規則**各自獨立**。⚠️ 本款係發單側依 KL 問句之傾向所答、KL 以「出單」默認；KL 認不然即駁回。
>
> 🔒 **六（五級之入池閘）** 一宗進入五級調配池，須**同時**：(a) 個別原位次 `G < MinA`；且 (b) 與同街廓同歸戶相鄰之宗依 **四／五** 合併試算**亦** `G < MinA`。⇒ 各自達標者辦原位次；一達一未達者於閘內併入（不進池）；各自未達而合併可達者於閘內合併留置（不進池）；合併仍未達者**全部**入池。🔑 閘在池之前，故「合併可達卻因入池而離位」**⛔ 發生**。
>
> 🔒 **七（通用化提醒）** 正式案 `RD`／公設上之重劃前土地各有逕割地號、各有地價；本測試案 `628(1)`／`628(2)` 等暫編共用一價係測試簡化，⛔ 寫入通則。
>
> 🔒 **八（本案土地後果·⛔ 非本裁之一部·KL 已確認）** 步驟 0 之 `9` 組／`13` 宗解併。`R4`：`628(1)+` 依 `K-9-24 五` 推定「歸右、左街角強制抵費地 `226.01 ㎡`」；解併後 `628(1)`（左·G `1521.05`）與 `628-1(1)`（右·G `457.73`）各自達標各佔一角 ⇒ **左街角不再強制留設，`226.01 ㎡` 留在 `G009`**。⚠️ 承 `K-9-24 五` 推定數·須實算。`wf_f0` 六組本案同區段 ⇒ `a′ = a` ⇒ 數字不變。
>
> 🔒 **九（落地序）** 本裁碼側落地（廢 `app.py:4292` 之 `k6_step0_merge` 呼叫、`wf_f0` `Σa → a + a′`、步驟 D 逐筆）**有土地後果·另單·逐 commit 放行**；於 `W-G.4` 依賴序插入 `4 五級` 之**前**。`-249`（段三觀測）之算式先依**三**改單，其觀測母體於步驟 0 廢除後**須重跑**。

**同批更正標記**（皆末端追加·⛔ 動原句）：
- `K-6:57-60` 步驟 0 節末追加一列：`🔧 更正標記（W-G.9-253）：本節經 K-9-29 二 廢止。`
- `K-9-21`／`K-9-24 一`／`K-9-22` 各節末同形追加。
- `docs/reports/W-G.4_泛用阻塞項登記表.md` 依賴序：於 `4 五級` 前插入 `3′ K-9-29 碼側落地（廢步驟 0·a′·入池閘）`（末端追加更正段·⛔ 改原表）。
- `docs/配地計算總規格_v3.md` 末端：`🔧 更正標記（W-G.9-253）：v3:433 a′ 式經 K-9-29 三 升為一切合併之唯一式；級0/0′ 之 Σa（v3:359 所指 wf_f0）改 a + a′。`

---

## §三　工項二：宣告框補款 `⑦`（`CLAUDE.md` **末端追加**）

🩸 案由（CC 於 `-249R` 段甲自捕·發單側於 `bf5980a` 復現並擴大）：`⑥` 之「`列框 ＝ D2 ＋ D3`」與 `①` 之聯集語意相斥；凡 `段甲_工項零與工項四.md:1`（`# \`W-G.9-NNNR\`　段甲：工項零（**本單**原封入倉）…`）同時符 `D2` 與 `D3` ⇒ **每份段甲報告必雙計**。實測 `-248`／`-249`／`-251` 各雙計 `1` 列。

> **⑦　列框之聯集**　`列框 ＝ |D2 之列 ∪ D3 之列|`；`D2`／`D3` 仍**分列**出艙（供漏框偵察），並明載雙屬列數。`檔框` 一字不動。
> 🔒 **⛔ 追改任何既出之取號結論**：聯集與和之別永不改變「零 vs 非零」，占用判準不受影響。
> 🔒 `①`–`⑥` 一字未動；`⑥` 之「`列框 ＝ D2 ＋ D3`」自本款起**讀為**上式。

---

## §四　工項三：自誤二則（**責 ＝ 發單側**·號 CC 復算·標題形取簿最末一則為範本·append-only）

**`314`　宣告框之列框私自實作為聯集而出艙時載 `⑥` 之名（`293`／`295`／`313` 同族·第四度）**
受詞：發單側 `2026-09-08` 呈 KL 之號占用表，`-251` 載 `D2 11／D3 1／列框 12`；依 `⑥` 逐字應 `D3 2／列框 13`（量測器以 `elif` 排除雙屬列）。
加重：交接文 `§二 四` 已明示本族為本窗高發區；且發單側就是 `⑦` 之立法者，卻先於立法而私改實作。
通則：量測器之實作**須與所載框之定義列式逐字同構**；框有瑕 ⇒ 先立款、再改器，⛔ 顛倒。

**`315`　重問 KL 已答之題（`290` 同族·第二度）**
受詞：發單側 `2026-09-08` 呈 KL「`-249` 與 `-251` 先辦哪一張」，而 KL 已於 `2026-09-07` 末句令「讀 `-249` 開工」，CC 並已將解讀入倉於 `docs/reports/W-G.9-251R_段乙停機_工項一_意圖與I-2相斥.md` 檔尾。
加重：發單側同一回合已讀 `-249R` 停機報告，卻未讀 `-251R` 停機報告之檔尾；交接文 `§六 3` 之「先問 KL」係封文時態，發單側未察其已被倉內新態取代。
通則：交接文所載「待問 KL」之題，開窗後須先於倉內查其是否已答，⛔ 逕問。

---

## §五　工項四：`-249` 段乙 **plan v2**（觀測模式不變·`winner` 與強制抵費地一格不動）

CC `-249R` 停機報告 `§四` 六題之裁：

| 題 | 裁 | 依據 |
|---|---|---|
| `§四 1` `A_ratio` 跨區段 | **已由 `K-9-29 三` 答**：段三-3 重算 ＝ `G(a_標的 + Σa′)`，`a′` 逐片依 `v3:433` 以**各片自身**之 `p(原位置)` 折算；⛔ 單一 `A` 乘混源 `Σa`。`pre_p ≤ 0` ⇒ **`raise`**（⛔ 靜默退 `1.0`） | KL 裁 `2026-09-08` |
| `§四 2` 重劃前地籍之受詞 | 既存行為·非本批新引入；`K-9-29 二` 廢步驟 0 後入參自然為逐筆地籍 ⇒ **⛔ 另裁**。plan v2 明載「入參於步驟 0 廢除前係合併後 `temp_parcels`，觀測結果屆時須重跑」 | `K-9-29 九` |
| `§四 3` solver 靜默退代數解 | **准**：觀測表加 `solver` 欄；`_corner_first_lot_G` **新增**回傳 label（⛔ 改既有回傳值之語意·⛔ 改 `G` 值）。判準：改前後 `G` 逐位同 | 發單側自裁（射程·不動配地） |
| `§四 4` `V-0`／`V-1`／`V-2` | **准 CC `B-5` ①②③**：①改受詞為「`app.py` 改動⛔ 影響 harness 路徑」並**具名為圍欄非證明**；②真受詞由 **KL localhost 手跑**取得（量測通道：`verify/out/KL_UI_3.5m_<sha>_*.csv`）；③段三 trial 抽為**可離線呼叫之純函式**，以離線測資覆蓋合併群建構／門檻取值／重算 `G` | 發單側自裁（量測器設計） |
| `§四 5` `WARNING-D` 名實 | **准**：欄名改「`門檻(min_area_to_apply)`」、取值**沿用 `_thr_of`**；⛔ 另寫取值；`_rngarea_of` 另欄具名 | 發單側自裁 |
| `§四 6` `cand_by_pid` | **准**：母體 ＝ `_k6b_side_results` 之原始 `qualified`／`eliminated` dict；⛔ `_corner_cand_diag`（2dp）| 發單側自裁（母體） |

`B-1`〜`B-6` 依 CC `§三` 之修正形辦（`B-6` 之插入行號 `:13674` 准；`-251` 行號錨位移量須同批補登）。
🔒 **段三 `2` 第一分支之 `RD` 片整筆併入**：亦走 `a′`（`K-9-29 三`）；`RD` 片之 `p(原位置)` 取其**所屬原地號**之區段（本案·`K-9-29 七`）。
🔒 plan v2 完成即送 reviewer（`CLAUDE.md` 既規），reviewer 綠後方得動生產碼；側分支 `verify/W-G.9-249-c1`。
🔒 凡引 `main()` 內行號（`:20638`／`:20714`／`:20887` 等），量測通道 ＝ **KL localhost 手跑 ＋ `§四 4 ③` 之離線純函式 harness**（`P-3` 具名）。

---

## §六　工項五：`-251` 段乙放行

依 `6ce7017` 所載改令：`c1` ＝ 工項二＋三（路名管理／`RD` 下拉）、`c2` ＝ 工項一（丁案）。**⛔ 待 `-249`**（其停機中）；側分支 `verify/W-G.9-251-c1`。純 UI·零土地後果；併線前 `V-K` 由 KL 目檢。

---

## 【驗收】

| 閘 | 判準 | 通道 |
|---|---|---|
| `V-A` 段甲零生產碼 | 十檔＋夾具 `sha256[:16]` 與 `bf5980a` 逐位同（`app.py` `4860f10828fb8ae9`） | `git cat-file blob` |
| `V-B` append-only | `K-6`／`CLAUDE.md`／自誤簿／`W-G.4`／`v3.md` 之 `numstat` deletions 皆 `0` | `git diff --numstat` |
| `V-C` 取號 | 自誤 `MAX` 前 `313` 後 `315`、缺號仍 `[106]`、恆等式 `True`；`K-9 MAX` 前 `28` 後 `29` | 定義框＋範圍框；`^### 🔒 K-9-(\d+)` |
| `V-D` `⑦` 兩造 | 對 `-248`／`-249`／`-251` 各得雙屬 `1`、聯集列 ＝ `D2＋D3−1`；對 `-253` 三數 `0` | 器另附於報告 |
| `V-E` `-249` plan v2 | 六裁逐項有落點；`a′` 式逐字 `v3:433`；`pre_p ≤ 0` 有 `raise` 之測資且**紅**；`_corner_first_lot_G` 改前後 `G` 逐位同 | 離線 harness |
| `V-F` `-251` 段乙 | 依 `-251` 單 `【驗收】`＋改令 | KL 目檢 |

---

## §七　產出與放行

| 段 | 內容 | 推送 |
|---|---|---|
| 甲 | 工項零〜三（入倉／`K-9-29`／`⑦`／`314` `315`）＋報告 `W-G.9-253R_段甲.md` | **零生產碼 ⇒ 直推主線** |
| 乙 | `-251` `c1`／`c2` | 側分支；`V-F` 後候 KL 一句放行 |
| 丙 | `-249` plan v2 → reviewer → 觀測碼 → `V-S` 表 | 側分支；觀測表出 ⇒ 呈 KL「是否接閘、`R2` 左由誰拿到」 |

乙／丙**互不相待**；同一 session 內⛔ 混同 commit。

---

## §八　待落地登記（本單登記·⛔ 辦）

| 項 | 依賴 |
|---|---|
| `K-9-29` 碼側落地（廢 `k6_step0_merge` 呼叫·`wf_f0` `a+a′`·步驟 D 逐筆·入池閘） | **有土地後果**（`R4` `226.01 ㎡`）·另單·逐 commit 放行·序在五級前 |
| `-249` 觀測於步驟 0 廢除後重跑 | 上項 |
| `K-9-22` 作廢（`CLAUDE.md` ⬜ 表更正標記） | 本單段甲可一併加標記·⛔ 改原列 |
| `GB-143` heredoc 硬閘活體未生效（CC 第 `9` 度） | CC 側·session 開啟方式·本單⛔ 及 |

---

*受詞 ＝ CC。段甲直推；乙、丙側分支候放行。*

SELF_SHA256: 149b573ef6038cf8c5103795875e4a5250d9d1a1761af604e13e658d0e741ac1
