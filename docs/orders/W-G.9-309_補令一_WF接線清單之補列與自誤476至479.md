# `W-G.9-309` 補令一：停機上呈之裁 ＋ `_WF_NS_NAMES` 之補列（二名·凍存分支）＋ `自誤 476`〜`479`

> **發單** ＝ 發單側窗二十一·`2026-09-22`。**受單** ＝ CC 窗 B。**級** ＝ **重**（生產碼 `app.py` `1` 檔·`3` 列·落凍存分支）。
> **基態** ＝ 主線 `wip/s1-endpart` ＝ `7eddd915c501983a3952a1d670176f423cc60749`（`c1`）；凍存分支 `verify/W-G.9-299-gb170` ＝ `86a1680d5858adcf542425e2708f7f2b0185083c`（`c2`）。發單側已以拋棄式 clone 復驗：主線本單檔 `sha256` ＝ `ac4488dd…`、主線 `app.py` blob ＝ `8466f749…`（⛔ 變）、二樹之差 ＝ 附件 `B` 逐位（`sha256` `5356801b…`）。
> 🔑 **本補令於衝突處優於 `W-G.9-309`**；二者皆原封入倉、⛔ 改原單一字。
> 🛑 **KL 對本補令之放行須由 KL 於交付時一併告知 CC**；CC 將其逐字錄於報告。未得 ⇒ 停機款 `1`。

---

## `§零`　效力與取號

- **被本補令取代者**：原單工項五-`1` 之抽取器呼叫（改以本補令為其輸入·見工項丁）、原單附件 `C-1`（作廢·由本補令附件 `C-1′` 代之）、原單 `§四` 之閘 `1`／`2` 之參數與 `§四-1`／`§四-2` 之期值（見本補令 `§三`）。原單附件 `C-2`／`C-3`／`C-4` 之內容**⛔ 變**，本補令逐位重載之（`sha256` 同）。
- **取號**（`恆常附款 e③`·態 ＝ 主線 `7eddd91`）：`自誤 476`〜`479`——`git grep -nE '自誤 47[6-9]'` ＝ `0`；判別力［必命中］＝ 同式之 `自誤 468` ⇒ 命中 ⇒ **取**。受單側當場重算（`恆常附款 f`）。
- **本補令檔** ＝ `docs/orders/W-G.9-309_補令一_WF接線清單之補列與自誤476至479.md`（`常規三`：沿 `W-G.9-318_補令一_…` 之形）。

---

## `§一`　對停機上呈之裁（發單側裁·逐項）

| # | CC 之候裁 | 裁 |
|---|---|---|
| `①` | `_WF_NS_NAMES` 補列 | **取**：補 `_corner_band_geom`、`_first_corner_alloc_dir` 二名（附件 `D`）；落凍存分支，為**新一筆** `commit`（`c2b`）；**⛔ `amend`、⛔ `force-push`**。`_wg9268p_anchor_advance`（`GB-167`）**⛔ 補**——其修屬 KL 放行射程 `(d)` 之排除項 |
| `②` | 補列後是否重跑 | **重跑三者**，皆為閘（見工項乙）：閘器十閘、`fixture_wf_ns_wiring.py`、`run_verification`（基座／受驗二值並列） |
| `③` | 工項四順延 | **順延至 `c2b` 之後**。🔴 **更正**：「工項四結構上不可行」⛔ 成立——`_wf_ns()` 之呼叫點於 `app.py` 僅 `:14635` 一處（`_build_wf_ctx`·「執行七級調配」路徑），一般之分配畫面⛔ 經之；該路徑於基座已因 `GB-167` 必崩（夾具實拋 `KeyError @ stepg_pipeline.py:388`），與本批無關。KL 核對時**勿按「執行七級調配」** |
| `④` | `C-1`〜`C-4` 是否修訂 | `C-1` 作廢、以 `C-1′` 代（增補令一之放行與 `GB-167` 之註）；`C-2`／`C-3`／`C-4` ⛔ 變；增 `C-5`（`自誤 476`〜`479`） |
| `⑤` | `data/sv_tmp00.dxf` 之複製 | **⛔ 複製**：`git grep -n sv_tmp00 -- app.py verify` ＝ `0`（生產碼與驗證碼皆⛔ 讀之）；判別力［必命中］＝ 同式之 `V6_1.dxf` 須命中 |
| `⑥` | 閘器增列一閘 | **取其旨、⛔ 改閘器**：以既有之 `verify/fixture_wf_ns_wiring.py` 為本補令之閘（工項乙-`4`）；攔法入 `自誤 476` |

**CC 之自解四項**：① `X-0` 以檔案管線抽出——**正確**，原單與 `常規七` 之相斥為發單側之誤（`自誤 477`）；② 拋棄式 worktree 置 `C:\Users\admin\Desktop\wg309b`——**核可**（`常規三`）；③ 路徑參數以反斜線形傳入——**正確**，器之平台脆弱為發單側之誤（`自誤 479`），本批⛔ 改器；④ 工項三 `5`／`6` 之改序——**正確**（`自誤 478`）。

---

## `§二`　停機款（`恆常附款 n`：三值）

| # | 停機款 |
|---|---|
| `1` | KL 對本補令之放行未經 KL 告知 |
| `2` | 本補令含 `CR`；抽取器（原單附件 `X-0`）對本補令之任一附件 `sha256` 不符，或 `rc ≠ 0` |
| `3` | 凍存分支之工作區於 `c2b` 之前有 `app.py`／`verify/stepg_pipeline.py` 以外之追蹤檔變動未還原；或還原後其 blob ≠ `c2` 之 blob（以 `git hash-object` 對 `git rev-parse HEAD:<檔>`·**⛔ 以 `git status` 判**） |
| `4` | `git apply` 附件 `D` 失敗；或其後 `app.py` blob ≠ `b4288438b6b029f4799a79fffacde1e7722c369f`、`verify/stepg_pipeline.py` blob ≠ `e4bd912b4f9e8cfe7cc49aef66f41ed566020937` |
| `5` | `fixture_wf_ns_wiring.py` 靜態層之漏列於受驗樹 ≠ 基座樹（期二者皆 ＝ `['_wg9268p_anchor_advance']`） |
| `6` | 閘器 `rc ≠ 0`，或土地影響與原單附件 `B-1` 相異 |
| `7` | `run_verification`：受驗樹之 `FAIL` 名目集 ≠ 基座樹，或任一 `FAIL` 之**原因**（名稱 ＋ 原因一併比對）≠ 基座樹 |
| `8` | 收工閘（`§三`）任一非 🟢 |
| `9` | 主線生產碼四檔之任何變動；任何併入主線之動作；`c2` 之改寫 |

---

## `§三`　工項

**依賴序**：工項乙（`c2b`·凍存分支）→ 工項丙（原工項四）→ 工項丁（原工項五·以本補令為輸入）＋ 工項戊（原工項六 ＋ 本補令入倉）＝ `c3`（主線）→ 收工閘。

### 工項乙　`c2b`（凍存分支·🔴 生產碼）

1. 於主 checkout（現在凍存分支）列出追蹤檔之變動；三靜態器所寫之已追蹤 log `3` 檔以 `git checkout -- <檔>` 還原，並逐檔以 `git hash-object <檔>` 對 `git rev-parse HEAD:<檔>` 核之（停機款 `3`·`自誤 472` 之族：⛔ 以依 stat 之判為據）。
2. 以原單附件 `X-0`（倉外·`sha256` 已核）對本補令跑 `python <倉外>/extract_wg9309.py <本補令之路徑> . <倉外暫存目錄>`（無 `--append`）⇒ 期：附件 `6`（倉外 `1`＝`D`·末端追加 `5`）、`rc 0`（停機款 `2`）。
3. `git apply --check` ⇒ `git apply <倉外暫存目錄>/W-G.9-309_補令一_D.diff`；核二 blob（停機款 `4`）；`git diff --stat` ＝ `1 file changed, 3 insertions(+)`。
4. `python verify/fixture_wf_ns_wiring.py` 於本樹與基座 worktree（`C:\Users\admin\Desktop\wg309b`）各跑一次；二者之「引擎消費但 `_WF_NS_NAMES` 漏列」列逐字相同、皆 ＝ `['_wg9268p_anchor_advance']`（二者之 `rc` 皆 ＝ `1`·既有紅·`GB-167`）（停機款 `5`·發單側沙盒實跑相同）。
5. 閘器：`python verify/probes/probe_WG9309_gates.py <基座之反斜線路徑> . <倉外>\out_gates_c2b` ⇒ 期 `rc 0`·十閘 🟢·土地影響與附件 `B-1` 逐列相同（停機款 `6`·發單側沙盒：附件 `D` 施作後之四格逐宗列與 `c2` 逐位相同）。
6. `run_verification`：本樹與基座各跑一次（⛔ 併跑·前景或以完成通知為準）；`FAIL` 名目集與逐項原因二值並列（停機款 `7`）。🔒 期值之來源 ＝ CC 於原單工項三-`5` 之實測（基座 `FAIL` `40`；`G.1` 之原因 ＝ `['_wg9268p_anchor_advance']`）；**發單側未能於出單前實查**（`恆常附款 d`）。`PASS` 側之差（原單所觀測之 `v3 A 逐宗全查` `87 → 88` 列）逐項出艙、⛔ 為停機款。
7. 以逐檔 `git add app.py` 為 `c2b`（訊息須含 `W-G.9-309 補令一`、`🔴 生產碼`、`凍存分支`）；二者之追蹤檔變動 ＝ `app.py` `1` 檔；停機款 `1`〜`7` 全 🟢 後 `push` 凍存分支（⛔ `force`）。

### 工項丙　畫面核對之備（原單工項四·⛔ 零 commit）

依原單工項四辦之，並：路徑取 `C:\Users\admin\Desktop\lrt-309`（原單所擬）；核該處 `app.py` blob ＝ `b4288438b6b029f4799a79fffacde1e7722c369f`；`data/sv_tmp00.dxf` **⛔ 複製**（`§一 ⑤`）。KL 核對事項加一句：**「請勿按『執行七級調配』（其於正式版本即會停止，係另一已登記之問題，與本次修改無關）。」**

### 工項丁　登記（原單工項五·以本補令為輸入·純末端追加）

1. 切回主線（主 checkout）；核 `HEAD` ＝ `7eddd91`、追蹤檔變動 `0`。
2. `python <倉外>/extract_wg9309.py <本補令之路徑> . <倉外暫存目錄> --append` ⇒ 五則末端追加依序皆「已附（嚴格前綴 ✅）」、`rc 0`：`C-1′`（`GB` 簿）→ `C-2`（自誤簿·`469`〜`475`）→ `C-3`（`K-6` 典）→ `C-4`（`CLAUDE.md`）→ `C-5`（自誤簿·`476`〜`479`）。🔒 **⛔ 以原單為輸入跑 `--append`**（原單之 `C-1` 已作廢）。
3. 原單工項五-`2`／`3` 照辦（`wg942_append_audit.py` 三值·防重附）。

### 工項戊　入倉（原單工項六 ＋ 本補令·`c3`）

原單工項六照辦，另：本補令原封入倉（路徑見 `§零`）；報告 `docs/reports/W-G.9-309R_執行報告.md` 增列本補令之逐款三值、**KL 對本補令之放行逐字**、工項乙之全部出艙、`c2b` 之 `commit` 與 `push`。`verify/out/` 之新檔仍為原單所列 `2` 支（`WG9309_gates.md` 取工項乙-`5` 之出艙）。`c3` 逕 `push` 主線（`常規一`）。

### 收工閘（取代原單 `§四` 之閘 `1`／`2` 與 `§四-1`／`§四-2`）

| # | 閘 | 期值 |
|---|---|---|
| `1` | `python verify/probes/probe_WG9270_closegate.py 479 468 .` | `rc 0` |
| `2` | `python verify/probes/probe_WG9321_issuer_anchor.py . <倉外落檔> 479 468` | `rc 0` |
| `3` | 主線 `app.py` blob | `8466f7493082bf84aa4f02d6c1c6e55ff746fc4a`（**⛔ 變**） |
| `4` | 凍存分支 | 遠端 ＝ `c2b`；`app.py` ＝ `b4288438…`、`verify/stepg_pipeline.py` ＝ `e4bd912b…`；與主線 `c1` 之分歧 ＝ `c2`／`c2b` 二筆 |

- **四簿之算式**（`w①`）：自誤（正典框）相異 ＝ `458 ＋ 11`（`469`〜`479`）＝ `469`；`MAX` `479`；缺號 `[106, 355, 356, 357]`；自擬框 `454 ＋ 11 ＝ 465`。`GB` `169 ＋ 2 ＝ 171`／`MAX` `173`／`[12, 87]`。`VR`、`K-9`、恆常附款⛔ 變。
- **母體之算式**（`e`／`e②`）：除本批之產物外 ＝ `2620`／`.md` `901`／`docs/` `861`／`verify/out/` `1024`／子層 `*.py` `375`／refs `25`。本批之產物 ＝ 原單所列 `14` 支 ＋ 本補令 `1` 支 ⇒ 全倉 `2635`／`.md` `906`／`docs/` `864`／`verify/out/` `1026`／子層 `*.py` `385`／refs `26`。
- 🔒 發單側已於沙盒乾跑（倉外 worktree 自 `7eddd91`·`commit` ⛔ `push`）：五則末端追加皆嚴格前綴 ✅；收工閘 `479 468` 與態錨器 `479 468` 之結果見附錄 `P′`。

---

## 附錄 `P′`　發單側出單前之自驗（沙盒·`2026-09-22`）

🔒 本附錄之數皆發單側沙盒實跑（Linux·基座 `2f46579`·凍存分支 `86a1680`）；受單側之平台所得依各款之判準處置（`自誤 479` 之呼叫形規定適用）。

- **復驗**：拋棄式 clone 得主線 `7eddd91`、凍存分支 `86a1680`；本單檔 `sha256` ＝ `ac4488dd…`；主線 `app.py` ＝ `8466f749…`；二樹之差 ＝ 附件 `B` 逐位；`c1` `11` 檔·刪除 `0`。
- **夾具**：`fixture_wf_ns_wiring.py` 靜態層之漏列——基座 `['_wg9268p_anchor_advance']`、`c2` `['_corner_band_geom', '_first_corner_alloc_dir', '_wg9268p_anchor_advance']`、附件 `D` 施作後 `['_wg9268p_anchor_advance']`（三者 `rc` 皆 `1`·動態層於三者皆實拋 `KeyError @ stepg_pipeline.py:388`）⇒ 與 CC 之實測相符。
- **附件 `D`**：施作於 `c2` 之樹 ⇒ `app.py` blob ＝ `b4288438…`、`1 file changed, 3 insertions(+)`；四格之量測器輸出與 `c2` 之樹**逐位相同**。
- **乾跑**（自 `7eddd91`）：抽取器 `rc 0`（附件 `6`）；五則末端追加皆嚴格前綴 ✅（`945020 → 950471`／`989176 → 996655`／`370539 → 371944`／`238340 → 239352`／`996655 → 1001228` B）；收工閘 `479 468` ⇒ `rc 0`；態錨器 `479 468` ⇒ `rc 0`：自誤 `469`／`479`／`[106,355,356,357]`（自擬 `465`）、`GB` `171`／`173`／`[12,87]`、`VR` `80`／`95`、`K-9` `42`／`43`；全倉 `2635`／`.md` `906`／`docs/` `864`／`verify/out/` `1026`／子層 `*.py` `385`；主線 `app.py` ＝ `8466f749…`。
- **pre-flight**（出單版）：`rc 0`；🟡 `4` 項——`[P-4]`（本附錄「復驗」列·所述為 `commit` 與檔之對照）⇒ 豁免；`[P-1]`（`自誤 479` 之「形」，所述為實測之二造，⛔ 全稱否定）⇒ 豁免；`[P-4]`（工項乙-`1`「切回／還原」係 `git` 操作）⇒ 豁免；`[P-4]`（`自誤 476` 之「形」所引碼名）⇒ 豁免；`[P-5]` 相符。

---

## 附件（以原單附件 `X-0` 之抽取器讀之·⛔ 改標題列一字）

| 附件 | 方式 | 路徑 | bytes | sha256 |
|---|---|---|---|---|
| `D` | 倉外 | `W-G.9-309_補令一_D.diff` | `1012` | `aa4a49384e3d8c13…` |
| `C-1′` | 末端追加 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `5451` | `4558e3985521c70f…` |
| `C-2` | 末端追加 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `7479` | `406ec0010088569e…` |
| `C-3` | 末端追加 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `1405` | `d12b394a6e6b6c15…` |
| `C-4` | 末端追加 | `CLAUDE.md` | `1012` | `16b95b767c03e2e5…` |
| `C-5` | 末端追加 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `4573` | `89270d94ef1a09cd…` |

#### 附件 D　倉外　`W-G.9-309_補令一_D.diff`　sha256 `aa4a49384e3d8c13fd4d05ea4e562ef687d340261e03eb725fef10dfb050f6e7`

````diff
diff --git a/app.py b/app.py
index 5ceddfc..b428843 100644
--- a/app.py
+++ b/app.py
@@ -14517,6 +14517,9 @@ _WF_NS_NAMES = [
     # §3 街角 forced band：幾何 bisect（band≡range）·side 參數化單一真相源
     #   （app／stepg／wf_f1／wf_f4 四處共用·plan v3 §3·補丁九·#25/#20）；`_strip_s_range` 為其 s 域基元
     "_strip_s_range", "_corner_buffer_S",
+    # 🆕 `W-G.9-309` 補令一：強制帶幾何之單一真相源與強制側之遠側線向——`verify/stepg_pipeline.py`
+    #   經 `ns['_corner_band_geom']`／`ns['_first_corner_alloc_dir']` 消費（強制側有值時）⇒ 漏列即 app 路徑 KeyError。
+    "_corner_band_geom", "_first_corner_alloc_dir",
     # §N3-0 帳對幾何閘（兩級化·補丁三 §二）：閘寬單一真相源（stepg／run_verification／wf_f4 共用）
     "_acct_geom_tol_per_lot", "_acct_geom_tol_block",
     # 🆕 W-8 補漏（reviewer 活抓·**既有缺陷非本波引入**）：引擎實耗但清單漏列者。
````

#### 附件 C-1′　末端追加　`docs/reports/W-G.4_泛用阻塞項登記表.md`　sha256 `4558e3985521c70f4e26e2ecc5f4710dc6440433f1d14552a4d95381559f9a7e`

````md

## 🔧 `GB-170` 之修批（`W-G.9-309`·含補令一）／KL 第二次放行之登記／`GB-171` 修屬之更正／`GB-172`・`GB-173` 之立（`W-G.9-309`·⛔ 上文一字不刪·純末端追加）

🔒 **態** ＝ `2f46579c6c9c91d9f1b88431a7e6ae9d998ce311`（本批開工態）。

**(1) KL 之放行（逐字·`2026-09-22`·⛔ 增刪一字、⛔ 擴其射程）**
> 發單側（窗二十一）呈 KL 之【要你判斷】欄為單一是非：「是否放行：將上述兩項修改寫入測試版本並逐宗核對，仍不併入正式版本（併入時另行請您放行）？（是／否）」。所稱「兩項」＝ 同呈文【要改成】所載：① 強制抵費地實際分出來的土地，形狀一併照 9/15 之界線切出；② 依 9/17 裁二，緊鄰強制抵費地的那一宗受藍影檢查，不合格者不配地、面積入調配池，由下一宗遞補到緊鄰位置並同樣受檢。
> **KL 逐字所答 ＝「是，放行，出 `W-G.9-309`（重量批）」。**
> 🩸 該呈文之用語有誤（`自誤 475`）：稱強制抵費地之「上緣」者，其所指為**遠側界線**（與側街相對之一側）；稱「沿分配方向」者，其所指為**與宗地分配線平行**。本放行之受詞以該呈文所附之平面圖與【對土地的影響】之逐宗數為準，二者皆⛔ 繫於該二詞。

**(1-b) 補令一之放行**：`W-G.9-309` 工項三-`5` 之觀測揭附件 `B` 漏補 `app.py` 之接線清單 `_WF_NS_NAMES` 二名（`_corner_band_geom`／`_first_corner_alloc_dir`·`自誤 476`）；其補列（`W-G.9-309` 補令一附件 `D`·無土地後果）經 KL 放行，逐字載於 `docs/reports/W-G.9-309R_執行報告.md`。

🛑 **射程（由發單側釘死·⛔ CC 擴之）**：
`(a)` 及於 `W-G.9-309` 附件 `B`（`app.py`／`verify/stepg_pipeline.py` 二檔之修·含本項之 `(i)(ii)(iii)` 及 `K-9-31 ④`／`K-9-32 ②④` 之受檢宗序）與補令一附件 `D`（`_WF_NS_NAMES` 補列二名）寫入凍存分支 `verify/W-G.9-299-gb170` 並 `push`；
`(b)` 及於該分支之逐宗核對（`verify/probes/probe_WG9309_gates.py` 十閘）及畫面核對之準備；
`(c)` 🔴 **⛔ 及於入主線**——主線之入倉須**另一次**放行（呈文逐字「仍不併入正式版本（併入時另行請您放行）」）；
`(d)` ⛔ 及於 `GB-167`（`_WF_NS_NAMES` 之既有漏列 `_wg9268p_anchor_advance`·「執行七級調配」之畫面路徑於基座已因之必崩）／`168`／`169`／`171` 之任何修、`K-9-29` 之任何子項、`K-9-33 ⑤` 所增二條件（內接矩形、臨正街寬 `≥` 畸零地寬）之接線、`GB-172`／`GB-173` 之修。

**(2) 本項失效條件之現況**：`(1)` ⇒ 凍存分支上成就；`(2)` ⇒ 凍存分支上成就（逐宗之差見 `docs/reports/W-G.9-309R_執行報告.md`）；`(3)` ⇒ 主線未入 ⇒ **未成就**。🛑 **本項⛔ 解除、⛔ 收窄。**

**(3) `GB-171` 之修屬（更正上文 `GB-171` 末節「修法之施作與行為差之量測屬 `W-G.9-309`」之讀法）**：依發單側交接文十九 `§三（二）`（`2026-09-22`·發單側裁），選槽側 `W_1` 之軸與選槽估算之寬度（`GB-168` 之殘差）**⛔ 併入** `W-G.9-309`，另單辦之、帶數請放行；其正典依據 ＝ `K-9-43`。`W-G.9-309` 修後選槽側之強制起算點隨 `buf` 變（`3.5m`·`R2`：`6.9805 → 5.2888`·選槽結果⛔ 變），其量測軸仍為街廓軸 ⇒ 與 `K-9-43` 不合之處**屬該另單**。

---

### `GB-172` 🆕　**有側街之端，左側強制帶之下界夾於 `0`：側街外傾（`s_min < 0`）時強制帶 ≠ 規定範圍**

**受詞**：`app.py` `_corner_buffer_S` 之 `_lo = max(s_min, 0.0)` 及 `_corner_band_geom` 之左側下界（`W-G.9-309` 所立）；`p1` 楔形歸 `N0-20`（`docs/specs/W-G.4_S1_plan.md:71`／`:84`）。
**實測**（`verify/probes/wg9309/synth_attr.py`·態 ＝ `W-G.9-309` 之凍存分支·合成街廓正面 `60 m`·深 `30 m` × 側街傾角 `{−6°, −2.7°, 2.7°, 6°}` × 宗地分配線傾角 `{0°, 3°, −4°}` × 遠側線向之正負號·左右各 `24` 例）：右側 `24`／`24` 成立（對稱差 `≤ 1.2e-11` ㎡）；左側 `12` 成立／`12` ⛔ 成立，⛔ 成立者恰為 `s_min < 0` 之 `12` 例（對稱差 `20.49`〜`157.53` ㎡）。本案四格⛔ 觸發（`W-G.9-309` 閘一之最大對稱差 `0.0046` ㎡）。
**失效條件**：`(1)` 左側 `s_min < 0` 時楔形之處置經 KL 裁或依 `N0-20` 落地；`(2)` 合成例左側 `24`／`24` 成立；`(3)` 本案逐位不變之證。🛑 **⛔ 解除、⛔ 收窄、⛔ 提修法主張**——三者皆未成就。

### `GB-173` 🆕　**`verify/wf_f1.py`／`verify/wf_f4.py` 之強制帶與池片仍為單線（側界線登記表⛔ 在域）**

**受詞**（態 `2f46579`·字樣 `_corner_buffer_S"](`／`_pool_strips_for_block"](` 現查）：`verify/wf_f1.py` 強制帶 `1` 處（`:216`）與池片 `1` 處（`:388`）；`verify/wf_f4.py` 強制帶 `2` 處（`:1351`／`:1362`）與池片 `1` 處（`:956`）。
**由**：側界線登記表於該二路徑⛔ 在域（`W-G.9-308R §五-3`）⇒ 無從求遠側線向；`F.1`〜`F.4` 於 `run_all` 全跳過 ⇒ 結構上不可驗。`W-G.9-309` 所增之參數皆為選用、預設 `None` ⇒ 該二路徑之行為逐位不變。
**失效條件**：`(1)` 側界線登記表接入該二路徑；`(2)` 同改並經閘驗；`(3)` 其餘逐位不變之證。🛑 **⛔ 解除、⛔ 收窄。**
````

#### 附件 C-2　末端追加　`docs/reports/W-G.9波_claude.ai側自誤登記.md`　sha256 `406ec0010088569ec2a7670856e395f592e46dbf99bebcdcf6ce5f7bca552f7b`

````md

---

### 🩸 `自誤 469`　**判原型「界線已對」而未查其後首宗之有效性：`628-42(1)` 已成三角形、`628-41(1)` 已侵入規定範圍 `50.85` ㎡，皆在原型之落檔內可見**

**形**：發單側（窗十九）於交接文十九 `§四 甲-1` 以「強制帶 vs 規定範圍之對稱差 `≤ 0.005`」判原型 `P1` 之界線已對，並將 `P1` 下業主宗之變動標為「⛔ 期值」；而 `P1` 之落檔內 `R2` 左（`3.5m`）`628-42(1)` 已為頂點數 `3` 之三角形（`2.5466` ㎡），`628-41(1)` 與規定範圍重疊 `50.8488` ㎡（窗二十以 `ovl2.py`／`r2_geom.py` 量得；窗二十一復現·`verify/probes/wg9309/`）。
**後果之界**：未出單、未入倉；窗二十以原型 `P2` 觸發既存之 `②-池` 閘而揭。🟢
**根因**：閘綁中間量（界線）而未綁土地性質（物化形與全部鄰宗不重疊）——交接文十九 `§五-1` 自身所載教訓之同族；「⛔ 期值」之標記使該批⛔ 被檢。
**後果之框**：🟡 原型判讀之誤·零入倉。攔點 ＝ **發單側（窗二十）＋ 既存之硬停閘**。
**攔法**：既有 `恆常附款 r`；`W-G.9-309` 之閘二（規定範圍 ∩ 業主宗）即其施行。⛔ 新立款。

---

### 🩸 `自誤 470`　**一句之內二數分屬二量：`−1.51`（幾何面積）與 `−1.28`（`G`）並列為同一宗之變動**

**形**：交接文十九 `§四 甲-2`「`R4` `628(1)+` `−1.51`／`−1.28` ㎡」——`0m` 之 `−1.51` 係幾何面積（`2017.54 → 2016.03`），`3.5m` 之 `−1.28` 係 `G`（`2023.33 → 2022.05`）；同口徑 ＝ `G` `−1.50`／`−1.28`、幾何面積 `−1.51`／`−1.30`（窗二十更正；窗二十一以 `drv.py` 復現）。
**後果之界**：未入倉、未呈 KL。🟢
**根因**：`恆常附款 l`（期值同格載其框）之再犯——未載量名。
**後果之框**：🟡 交接文之數失真。攔點 ＝ **發單側（窗二十）**。
**攔法**：既有 `恆常附款 l`。⛔ 新立款。

---

### 🩸 `自誤 471`　**取號現查之命中數未載母體：「內文側 `45` 檔」僅於 `*.md` 成立，全倉為 `51`**

**形**：交接文十九 `§一` 項 `10`「`W-G.9-309` 保留（內文側 `45` 檔引）」；`git grep -l 'W-G\.9-309' -- '*.md'` ＝ `45`，全倉 ＝ `51`（另 `verify/out/` `5` 檔、`verify/probes/` `1` 檔）。
**後果之界**：未據之取捨。🟢
**根因**：`恆常附款 l`／`P-2`（計數須同段載其框）之再犯。
**後果之框**：🟡。攔點 ＝ **發單側（窗二十）**。
**攔法**：既有 `恆常附款 l`。⛔ 新立款。

---

### 🩸 `自誤 472`　**病灶已具名而未就其全部消費者查其判法：主 checkout 之更新令三度以依 stat 之 git 路徑為據**

**形**：`W-G.9-325` 交接註 `5`-`1` 已具名主 checkout 之 `app.py` 差純為行尾、index 所記 size 過期；發單側（窗十九）其後所出之主 checkout 更新令，前令一-`3`（`git diff`）、補令一（`restore`·`rc 0` 而零效果）、補令二（`merge --ff-only` 被拒）三度仍以依 stat 之路徑為判；至補令三 `read-tree HEAD` ＋ `update-index --refresh` 後內容框閘始全綠（`read-cache.c` `ie_modified` 之 size 捷徑讀法經該閘證實）。
**後果之界**：主 checkout 未受損（備份夾 `C:\Users\admin\Desktop\lrt_premerge_backup_20260921`·`135` 檔）；三令之誤皆為內容框後驗所擋。🟢
**根因**：病灶具名後，未就其**全部消費者**逐一查其判法。
**後果之框**：🟡 三令之無效操作·零資料損失。攔點 ＝ **閘（內容框後驗）與受單側**。
**攔法**：既有 `恆常附款 m`（全部消費者之列出）之類推施行。⛔ 新立款。

---

### 🩸 `自誤 473`　**以過時之治理文件狀態斷言碼面：稱 `K-9-17` 遞補迴圈「部分落地·觀測模式」「未開工」，而 `W-G.9-269 c3`（`17fd981`·`2026-09-12`）已令其無條件執行**

**形**：交接文二十 `§三-2` 判 `2` 引 `CLAUDE.md:1457`「部分落地·觀測模式」及 `:590`／`:1099`「未開工」，並稱 `_lot_gate` 為觀測模式；而態 `2f46579` 之 `app.py` `k917_should_drop`／`k917_note_drop`（`:9286`／`:9316`）已消費 `驗_B藍影`，不合格者不配地、由鏈之下一宗遞補（`app.py` 與 `verify/stepg_pipeline.py` 左右鏈·四處）；`CLAUDE.md` 全檔 `W-G.9-269` 之命中 ＝ `0`。
**後果之界**：交接文二十據之擬原型 `P3` 之射程為「藍影之執行與遞補」（重量）；窗二十一於開場序 `4` 讀碼而更正——`P3` 實為強制側受檢宗序一處之改（每鏈一判別式）。未出單、未入倉。🟢
**根因**：`恆常附款 p`（以倉內已有答案收窄或定射程前須讀畢）之同族——以治理文件代碼面；「已落地而治理文件未更新」與「已裁而未落地」同為誤診之源。
**後果之框**：🟡 射程之高估·零入倉。攔點 ＝ **發單側（窗二十一·開場序 `4`）**。
**攔法**：既有 `恆常附款 p`；`CLAUDE.md` 進度表之更正（`W-G.9-309` 工項五-`4`）。⛔ 新立款。

---

### 🩸 `自誤 474`　**所引行號未標其所繫之樹：`app.py:12487`／`:22234`／`:22373`、`verify/stepg_pipeline.py:846`／`:967` 係原型 `P2` 樹之行號**

**形**：交接文二十 `§三-2` 判 `2` 所引；基座（`2f46579`）之對應行號為 `app.py:12453`／`:22200`／`:22339`、`verify/stepg_pipeline.py:839`／`:960`（窗二十一實查）。
**後果之界**：未入倉；窗二十一以字樣重錨。🟢
**根因**：`恆常附款 w`（`blob`／`sha256` 錨須同格載其所繫之態）之類推——行號亦為繫態之錨。
**後果之框**：🟡。攔點 ＝ **發單側（窗二十一）**。
**攔法**：既有 `恆常附款 w`。⛔ 新立款。

---

### 🩸 `自誤 475`　**呈 KL 之文用語有誤：稱強制抵費地之「上緣」而實指遠側界線；稱其「沿分配方向」而實指與宗地分配線平行（`q②` 再犯）**

**形**：發單側（窗二十一）於 `2026-09-22` 呈 KL 之放行請求，【現況】作「其上緣沿分配方向斜出」、【要改成】作「即上緣與側街平行」，平面圖標註同。所指之線係強制抵費地與其後一宗之交界（**遠側界線**·平面圖上與側街相對之一側）；現況該線**與宗地分配線平行**，而依 `K-9-43`（五）（`docs/rulings/K-6_街角地分配程序與可分配判準.md` 檔末），「分配方向」＝ 垂直於宗地分配線之方向 ⇒「沿分配方向」與所指相反。
**後果之界**：KL 已答「是，放行」；該答之受詞以平面圖（線形依實際座標繪製）與【對土地的影響】之逐宗數為準，二者皆⛔ 繫於該二詞 ⇒ 放行之受詞⛔ 變；已於 `W-G.9-309` 之 `GB` 簿登記與交付時一併告知 KL。🟡
**根因**：`恆常附款 q②`（程式之方向名須先以地政用語定義）之再犯；以方位字（上／下）指稱界線，而平面圖之擺置係發單側所定、⛔ 為地籍之定義。
**後果之框**：🟡 呈文用語之誤·放行受詞未變。攔點 ＝ **發單側（窗二十一·擬 `W-G.9-309` 時自捕）**。
**攔法**：既有 `恆常附款 q②`；另：呈 KL 之文稱界線者，一律以「近側界線／遠側界線／臨正面道路／臨側街／臨後側」稱之，⛔ 以圖之方位字稱之（併入 `q②` 之施行·⛔ 新立款）。
````

#### 附件 C-3　末端追加　`docs/rulings/K-6_街角地分配程序與可分配判準.md`　sha256 `d12b394a6e6b6c159e2b048dfdae7ec1936f1f29577d525cd9b4fd59b771fb20`

````md

### 🔧 `K-9-31 ④`／`K-9-32 ②③④` 之落地狀態（`W-G.9-309`·⛔ 上文一字不刪·純末端追加）

🔒 **態** ＝ 凍存分支 `verify/W-G.9-299-gb170`（主線 `wip/s1-endpart` **⛔ 入**·KL `2026-09-22` 放行之射程 `(c)`）。
① `K-9-32 ①②`（強制抵費地之遠側地界線與側街平行；其後 `1` 宗之裡側界線 ＝ 該線）及強制抵費地之物化形 ≡ 規定範圍 ⇒ **凍存分支上落地**（`GB-170` 之 `(i)(ii)(iii)`）。
② `K-9-31 ④`／`K-9-32 ④`（藍影受檢宗 ＝ 緊鄰強制抵費地之宗·含遞補上來者）⇒ **凍存分支上落地**：強制側之受檢宗 ＝ 推進鏈之碼側 index `0`（強制抵費地⛔ 在推進鏈內）；鏈首為街角第 `1` 宗者⛔ 受檢（`_lot_gate` 之既有射程·`K-9-32 ④` 所載之形）。
③ `K-9-32 ③` 之遞補 ＝ 既有 `W-G.9-269 c3` 之 `k917_should_drop`／`k917_note_drop`（主線已落地·無條件執行）；其受詞僅 `K-9-23` 閘一（藍影）——`K-9-33 ⑤` 所增之內接矩形與「臨正街寬 `≥` 畸零地寬」**⬜ 未接線**。
④ 本案之逐宗結果（`R2` 左·退縮 `3.5 m`·態甲乙同）：`628-42(1)` 不配地；`628-41(1)` 遞補至緊鄰位置、藍影合格（`176.60` ㎡）——見 `docs/reports/W-G.9-309R_執行報告.md`。
🛑 **主線之落地狀態 ＝ ⬜ 未實作**（入主線須 KL 另一次放行）。
````

#### 附件 C-4　末端追加　`CLAUDE.md`　sha256 `16b95b767c03e2e53be06f713b2e321bc0fad6d70895f6c3e96040dbfd18a4a0`

````md

## 🔧 進度表之現況更正：`K-9-17` 遞補迴圈已落地（`W-G.9-309`·⛔ 上文一字不刪·純末端追加）

🩸 **被更正之對象**：`:590`（⬜ 未開工）、`:1099`（⬜ 未開工）、`:1457`（🔶 部分落地·「`K-9-17` 遞補迴圈……本批⛔ 不辦」）。
🔒 **現況**（態 `2f46579`·碼面實查）：`W-G.9-269 c3`（`17fd981`·`2026-09-12`）已移除旗標 `WG9269_K917_BACKFILL` ⇒ `K-9-23` 閘一（藍影）之判由 `k917_should_drop` 消費，不合格者不配地、由鏈之下一宗遞補（`app.py` 左右鏈與 `verify/stepg_pipeline.py` 左右鏈·四處）；**閘二**（`K-9-12` 矩形容納）⬜ 仍未接線；`_lot_gate` 本身仍無副作用（只回傳判定）。
🔒 **`W-G.9-309`**：`GB-170` 之修與強制側之藍影受檢宗序（`K-9-31 ④`／`K-9-32 ②④`）⇒ 凍存分支 `verify/W-G.9-299-gb170` 落地；主線 ⬜。
🛑 本節⛔ 改上文一字（append-only）；上文三列之字⛔ 改，以本節為準讀之。
````

#### 附件 C-5　末端追加　`docs/reports/W-G.9波_claude.ai側自誤登記.md`　sha256 `89270d94ef1a09cd0c9e1e881547d6c229790542375c419947fad47425de7f4a`

````md

---

### 🩸 `自誤 476`　**引擎新增經 `ns` 取用之名而未補入 app 之接線清單 `_WF_NS_NAMES`：`_corner_band_geom`／`_first_corner_alloc_dir`**

**形**：原型 `P1`（窗十九）於 `verify/stepg_pipeline.py` 增 `ns['_first_corner_alloc_dir']`、原型 `P2`（窗二十）增 `ns['_corner_band_geom']`，二者於強制側有值時執行；`W-G.9-309` 附件 `B` 對 `_WF_NS_NAMES` 之命中 ＝ `0`。CC 於 `W-G.9-309` 工項三-`5` 之觀測（`run_verification` `G.1`）捕得：基座之漏列 `['_wg9268p_anchor_advance']`（`GB-167`）→ 凍存分支 `['_corner_band_geom', '_first_corner_alloc_dir', '_wg9268p_anchor_advance']`；`verify/fixture_wf_ns_wiring.py` 靜態層同（發單側自倉復現）。
**後果之界**：凍存分支 `c2`（`86a1680`）已 `push`；主線未受。「執行七級調配」之畫面路徑（`_build_wf_ctx` → `_wf_ns()` → `run_step_g`）於基座已因 `GB-167` 於 `stepg_pipeline.py:388` 必崩，故本漏列現⛔ 另生可觀之崩潰，惟 `GB-167` 修後即於強制側有值之街廓現形。補令一補列。🟡
**根因**：`恆常附款 m`（受詞之全部消費者）施行時只列呼叫點，未列「經 `ns` 取用之名」之登記處；閘器十閘皆走 harness，結構上量不到 app 之 `ns` 清單；既有守護夾具 `fixture_wf_ns_wiring.py` 未列為本單之閘（`run_verification` 僅列為觀測）。
**後果之框**：🔴 生產碼之接線缺陷入凍存分支·土地後果 `0`。攔點 ＝ **受單側**（觀測項之二值並列與類 III 之判）。
**攔法**：既有 `恆常附款 m` 之施行擴及：凡增減引擎經 `ns` 取用之名者，單之閘須含 `verify/fixture_wf_ns_wiring.py` 靜態層之基座／受驗二值並列（併入 `m` 之施行·⛔ 新立款）。

---

### 🩸 `自誤 477`　**單令以檔案工具「逐字寫出」附件 `X-0`，與 `常規七 一`（逐字交付塊須由檔案管線直接產生、⛔ 憑記憶或理解重寫）相斥**

**形**：`W-G.9-309` 工項一-`1` 逐字「以 CC 之檔案工具，將附件 `X-0` 之內容逐字寫為倉外之 `extract_wg9309.py`」；CC 依 `常規二` 逐字回報並取保守項（以檔案管線抽出、`sha256` 自證）。
**後果之界**：CC 取保守項，附件之保真未受影響。🟢
**根因**：出單前未以 `CLAUDE.md` 之諸常規回掃本單之交付形；只回掃 `恆常附款`。
**後果之框**：🟡 單內條款與常規之互斥。攔點 ＝ **受單側**（`常規二`）。
**攔法**：既有 `常規七`；出單前之回掃母體擴及 `CLAUDE.md` 常規一〜七（併入 `恆常附款 a` 之施行·⛔ 新立款）。

---

### 🩸 `自誤 478`　**單序令會改寫已追蹤落檔之器（三靜態器·`run_verification`）先於凍存分支之 `commit`，且未於 `e②` 之產物列舉中具名其所寫之檔**

**形**：`W-G.9-309` 工項三-`4`（靜態器）與 -`5`（觀測）皆序於 -`6`（`commit`）之前；三靜態器改寫已追蹤 log `3` 檔。CC 以逐檔 `git add` 及「先 `commit` 後觀測」之自解避免其混入 `c2`（`94`／`23` 逐字相符）。
**後果之界**：`c2` 未受污染。🟢
**根因**：`恆常附款 e②`（受單側為執行本單所必生之物須列舉）之再犯。
**後果之框**：🟡 單序之缺陷。攔點 ＝ **受單側**（自解）。
**攔法**：既有 `恆常附款 e②`。⛔ 新立款。

---

### 🩸 `自誤 479`　**閘器與量測器以 `os.path.join(REPO, "app.py")` 組路徑：於 Windows 與 `app_harvest.APP_PY` 之分隔符相異 ⇒ `harvest` 快取鍵分裂，第二個 `_FakeStreamlit` 覆寫 `sys.modules`**

**形**：CC 於 `W-G.9-309` 工項二實測——以正斜線傳路徑 ⇒ 驅動 `rc 1`（生產碼 `K-9-5-4②` 之 loud raise：側界表查無）；以反斜線傳 ⇒ `rc 0`；反事實（令 `sys.modules['streamlit']` 復為驅動器所持之實例）即成功。發單側之自驗僅於 Linux 沙盒（分隔符同一 ⇒ 快取命中）。
**後果之界**：工項二首跑 `rc 10`（期 `7`）；CC 診斷後以反斜線形重跑得期值；⛔ 改器一字。🟢
**根因**：`恆常附款 d`（期值及於倉外之物須載「未能於出單前實查」）之類推——受單側之平台即倉外之物；器未正規化路徑。
**後果之框**：🟡 器之平台脆弱。攔點 ＝ **受單側**。
**攔法**：既有 `恆常附款 d`；器之呼叫形規定為「路徑參數以 `os.path.abspath` 所得之形傳入」；下批修器時於器內正規化（⛔ 本批改器）。⛔ 新立款。
````

SELF_SHA256: a5fe089fb648da87cd4b27cb6dea925d34f7a3d633b5dee364490f30891f55fa
