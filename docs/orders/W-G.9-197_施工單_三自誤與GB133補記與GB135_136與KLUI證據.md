# 🔧 `W-G.9-197` 施工單：**三自誤（`246`／`247`／`248`）＋ `GB-133` 補記 ＋ 鑄 `GB-135`／`GB-136` ＋ KL UI 實跑證據二檔入倉**

> **性質**：🟢 **全單零生產碼**（⛔ 不動 `app.py` 一字·⛔ 不動任何 `verify/**.py` 一字）⇒ **CC 可逕行 `push`**（`常規一` 補款 `🔧 一`）。
> **出單窗**：claude.ai（接 `W-G.9-196` 交接文·本窗為接手窗）
> **基座**：`wip/s1-endpart` ＝ `7fab020`（🛑 開工時自行 `git ls-remote` 實查全 `40` 碼；不符即停機上呈）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **前置之併記（發單側自倉復驗·⛔ 非採信自報·態 `7fab020`）**：母體 `git ls-files -z` ＝ `1554` **檔**·開不到 **`0`** 檔·全倉 `.md` ＝ `491` **檔**；`app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`／`1383993` B／`\n` `23462` **次**／`CR` `0` **次**；`docs/rulings/K-6_…` ＝ `3663` **列**；`CLAUDE.md` ＝ `1178` **列**。
🔒 **KL 工作副本之同一性已由 KL 逐字回報坐實**：`git pull` ⇒ `Already up to date.`；`git rev-parse HEAD:app.py` ⇒ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`（＝ 基座之 `app.py` blob）⇒ **KL 實跑之態 ＝ `7fab020`**。

---

## §零　落地點（單一 commit）

| # | 檔／物 | 動作 |
|---|---|---|
| `L-1` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 末端追加 **`自誤 246`**（`§二-1`·逐字） |
| `L-2` | 同上檔 | 末端追加 **`自誤 247`**（`§二-2`·逐字） |
| `L-3` | 同上檔 | 末端追加 **`自誤 248`**（`§二-3`·逐字） |
| `L-4` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 末端追加 **`GB-133` 之補記**（`§二-4`）🛑 ⛔ 鑄號·⛔ 不改 `GB-133` 原節一字 |
| `L-5` | 同上檔 | 末端追加 **新鑄 `GB-135`**（`§二-5`） |
| `L-6` | 同上檔 | 末端追加 **新鑄 `GB-136`**（`§二-6`） |
| `L-7` | `verify/out/KL_UI_3.5m_7fab020_W-D.1.2診斷_逐候選評點明細.csv` | **KL 放檔**（`§三`）·CC 逕行 `git add` |
| `L-8` | `verify/out/KL_UI_3.5m_7fab020_stdout.log` | **KL 放檔**（`§三`）·CC 逕行 `git add` |
| `L-9` | `CLAUDE.md` | 末端追加 **裁 `G`** ＋ 待落地警語 ＋ 土地後果實測結論（`§二-7`） |
| `A-1` | `docs/orders/W-G.9-197_施工單_三自誤與GB133補記與GB135_136與KLUI證據.md` | 本單以原名入倉 |
| `A-2` | `docs/reports/W-G.9-197R_三自誤與GB133補記與GB135_136_執行報告.md` | 執行報告（同 commit） |

🛑 **`L-1`〜`L-6`／`L-9` 皆為<u>末端追加</u>（append-only）**——⛔ 不得就地改寫任何既有文字一字；`deletions` 須逐檔為 `0`。

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | `git ls-remote … refs/heads/wip/s1-endpart` | 逐字 ＝ `7fab020` 之全 `40` 碼（🛑 自查·⛔ 不以本單所載短碼為錨·`常規九 三`）。不符 ⇒ 停機上呈 |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-197_施工單_三自誤與GB133補記與GB135_136與KLUI證據.md` | `P-3`／`P-5` 須不紅；🟡 逐項具名（發單側預告之集合見 `§零-c`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗（受詞 ＝ 自檔首至該列前一個 `\n` 含之） | `declared` ＝ `recomputed` |
| `S-0d` | 🔴 **`L-7`／`L-8` 二檔是否已在工作樹** | `verify/out/` 內查無 `§三` 所命之**二**檔名者 ⇒ **停機上呈 KL**（⛔ 不得自行生成、⛔ 不得以倉內既有三代檔頂替、⛔ 不得改名） |
| `S-0e` | 🔴 **零生產碼自證** | `git diff --name-only <基座>..HEAD` 之結果**逐列⛔ 不含** `app.py`、⛔ 不含任何 `verify/**.py`。有一列即停機上呈 |

🛑 承 `GB-134`：`P-2` 對本倉主流語序**結構上盲**（可見 `71` **次**／盲 `442` **次**）⇒ 本單一切計數**皆於同段自載其母體與框**（框 ＝ `列`／`次`／`格`／`檔`），⛔ 不以 `P-2` 未提示為據。

## §零-c　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6` 六檢·🔴 機械停機款 `0` 項）

| 檢 | 觸發點（字樣錨·`grep -F` 固定字串·⛔ 不用行號） | 處置 |
|---|---|---|
| `P-1` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§零-b` 表頭） | 🔒 **具名豁免**：係閘之索引表，各閘之母體／所查範圍逐列具名於同表「受詞」欄 |
| `P-1` | `` \| 合取項 \| 現查 \| 判 \| ``（`§二-4` 表頭） | 🔒 **具名豁免**：同表「現查」欄逐列自載母體（`app.py` 單檔）與框（列） |
| `P-1` | `` 土地欄（`真交集`／`整筆幾何` ``（`§二-7` 之實測段） | ✅ **已採納**——同段已具名母體（`20` 列 × `9` 欄 ＝ `180` 格）與框（格） |
| `P-4` | `` \| 欄群（母體逐字） \| 逐格比對（框 ＝ **格**） \| `` | 🔒 **具名豁免**：表頭無方向性量；其下二列各自宣告方向（`—` → 數值／基準 → 新） |
| `P-4` | `` 🔒 **成因已查明·⛔ 非行為變更** `` | 🔒 **具名豁免**：該 `→` 係**字面值之轉寫**（`'0.0'` → `'0'`），⛔ 非方向性量之轉引；方向（基準 → 新）已於同句逐字宣告 |

🔒 **發單側已實跑**：🔴 機械停機款 **`0` 項**；🟡 **`5` 項**，逐項即上表（`P-1` `3` 項／`P-4` `2` 項）；`P-5` `SELF_SHA256` ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。

🛑 CC 實跑若出現**上表以外**之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。

---

## §一　取號現查（發單側·態 `7fab020`）

**自誤**　母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**；框 ＝ `` ^#+ 自誤 `?([0-9]{1,4})`? ``（框 ＝ **列**）⇒ 相異 `210`／MAX `245`／缺號 `35` **個** ⇒ 次三號 ＝ **`246`／`247`／`248`**。
**意指占用**（母體 ＝ 全倉 `.md` `491` **檔**·錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·二形並取·框 ＝ **列**）：`246` ＝ `0`／`0`；`247` ＝ `0`／`0`；`248` ＝ `0`／`0`。🔑 **對照組** `自誤 245` ＝ `6`／`9` **列** ⇒ 量測器非紅。

**`GB`**　母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔；🛑 **扣除集 ＝ `GB-997`**（`S-4` 人造哨兵·`常規四（八）` 補款·定義列 `docs/reports/W-G.9-177R_…:93`）⇒ 相異 `132`／MAX `134`／缺 `[12, 87]` ⇒ 次二號 ＝ **`135`／`136`**。
**意指占用**（全倉 `.md`·錨定框·二形並取·框 ＝ **列**）：`GB-135` ＝ `0`／`0`；`GB-136` ＝ `0`／`0`。🔑 **對照組** `GB-134` ＝ `18`／CC 實測並報二數 ⇒ 量測器非紅。

**單號**　`W-G.9-197` 全倉 `.md` ＝ `0` **列**（對照組 `W-G.9-195` ＝ `11` **列** ⇒ 量測器非紅）。

🩸 **發單側自陳（未出艙即攔下·⛔ 不鑄自誤）**：本窗初次以裸框（未扣哨兵）量 `GB` 得 MAX `997`，係**本窗自身之框失當**，已於出艙前更正。

**範本（`常規四（九）四`）** ＝ 既有最末一則之標題形逐字：
`` ### 自誤 `245`（發單側·`W-G.9-194` 出單窗）：**寫下一條倉內<u>已登記為結構上不可滿足</u>之驗收判準，使該閘恆真** ``

---

## §二　payload（🛑 逐字追加·⛔ 不改一字）

### §二-1　`L-1`　`自誤 246`

```
### 自誤 `246`（發單側·`W-G.9-195` 出單窗）：**單內之機械閘假定了一條 commit 父子關係，而該假定與<u>同一張單自身所核可之 commit 順序</u>相牴觸**

**案由（二肢·同根）**　`docs/orders/W-G.9-195_施工單_四裁與d46dfe6放行.md` `§零-b` 之 `S-0d` 令驗 `git rev-parse d46dfe6^` ＝ `f44e000`；而**同單 `§三 裁 四`** 正是核可「報告先行入倉、`app.py` commit 置其後」之序 ⇒ 實際父為 `39f8fec`。

- **肢 ①（未將兩項已知事實相互對照）**：發單側下筆時**手上已有** `W-G.9-194R` 之逐字「已 push：`f44e000`／`39f8fec`（報告）；`d46dfe6` 備妥未推」⇒ **父為 `39f8fec` 之事實在下筆時即可得**。
- **肢 ②（該序結構上不可能同時成就）**：`§四` 令「`commit 1` 已 push ⇒ 再 push `d46dfe6`」，然 `d46dfe6^` 恰為開工時之遠端 `HEAD` ⇒ 欲使 `commit 1` **先**入 `origin` 須 rebase、`d46dfe6` 之 SHA 必變，**逕違同單「⛔ 不得重做」**。

🔑 **攔截者 ＝ CC**（二肢皆是）。其處置正確：肢 ① 以**保全立法理由**之錨（同一物件：全 `40` 碼／`tree`／`numstat`／blob）復驗，⛔ 未自行重建；肢 ② 改**建 `commit 1` 於 `d46dfe6` 之上、單一 `push` 原子落地**，二事同時成就且 SHA 保全。⛔ 未改單一字、⛔ 未上呈 KL。

🔒 **⛔ 無土地後果。**

🩸 **同族**　與 `自誤 241` 同族（**單內二節互斥**），且係 `241` 之一般式「立戒者在同一批就會違反自己的戒」**第三度**——🔴 本則之特殊處在於：所違反者正是<u>同一張單裡自己剛立的那一條裁</u>（`裁 四`），二者相距不足一檔。

🔒 **一般式**　凡於單中寫入**繫於 commit 圖形**之機械閘（父子、祖先、區間）者，須 ① 以**受單方最近一次報告之逐字**核對該圖形；② 自檢「本單他節有無**改變該圖形**之令或裁」；③ 閘之判準寫成**保全其立法理由**之形（如「同一物件」＝ 全 `40` 碼＋`tree`＋`numstat`＋blob），⛔ **不得**寫成繫於特定父 commit 之形——後者隨任何一次合法之序調整即失效。
```

### §二-2　`L-2`　`自誤 247`

```
### 自誤 `247`（發單側·`W-G.9-196` 出文窗）：**出艙一個計數，其值與同段所宣告之母體不同源——母體宣告為「該函式」，而該函式在任一常規框下皆⛔ 非該值**

**案由**　`docs/orders/W-G.9-196_發單側交接文.md` `§三-1` 逐字 ＝ 「該函式 `96` **列**」，用以標示 `_pk_one_side_v12` 多重集差之母體。
🔴 **現查（接手窗·態 `7fab020`·亦同 `39f8fec`／`d46dfe6`）**：該函式之 AST 區間 ＝ `12966`-`13059` ⇒ **`94` 列**；其中非空 **`87` 列**。`96` **僅**在框 ＝ 「`def` 起始列至<u>下一個 `def` 前一列</u>」（`12966`-`13061`·含二尾空列）時成立——**該框之母體⛔ 非「該函式」，而係「該函式加其後之空白間隔」**。

🔒 **實質結論⛔ 不受影響**——接手窗以 `94` 列母體重跑多重集差，**逐字得同一結果**：僅在改前 ＝ `    for cand in qualified:`（`1`）／僅在改後 ＝ `    for cand in group:`（`1`）；`git diff --numstat 39f8fec d46dfe6` ＝ `6`／`6`／`app.py`。

🔒 **⛔ 無土地後果。**

🩸 **同族**　與 `自誤 241`／`243`／`244` 同族（**出艙之數與其框／母體不同源**），併 `235`／`238`／`239` ⇒ **族內累計第六度**。
🔴 **本則之特殊處**：`244` 已令「凡出計數須宣告其框係 `列` 抑或 `次`」，本則**確已宣告框為「列」** ⇒ **`244` 之戒⛔ 不足**：宣告「框」而不同時宣告「**母體**」者，仍可出艙一個不可復現之數。

🔒 **一般式（`244` 之補強）**　凡出艙任何計數者，須於同段**同時**宣告 ① **母體**（可機械復現之集合逐字，如「AST `def` 區間」「`git ls-files -z`」「本檔單檔」）與 ② **框**（`列`／`次`／`格`／`檔`）；二者缺一，該數即⛔ 不可復現。**自檢法 ＝ 把該段交給一個只讀該段的人，他能否用同一口徑重跑出同一個數。**
```

### §二-3　`L-3`　`自誤 248`

```
### 自誤 `248`（發單側·`W-G.9-197` 出單窗）：**出艙一項「落地須同時滿足二個生效時點」之要求，而該二時點之一，倉內<u>生產碼註解</u>早已載明其係「循環依賴·待 KL 裁」並已裝有保守替代**

**案由**　接手窗於交接復驗之出艙中逐字寫下：「**下一批之上鎖實作須同時滿足二個生效時點（步驟 0 與段一），⛔ 不得只做段一，否則 `K-9-21` 之落地與段一之上鎖將出現二套判準**」。
🔴 **該要求⛔ 不可成就，且倉內已載其不可成就之理由**——`app.py` 之 `k6_step0_block_locked` 註解逐字：「🔒 **K-9-21 之保守替代**（⛔ 非正典本文·**待 KL 裁循環依賴之解**）…(a)(b)(c) **三者皆係段一街角 PK 之產物**…而正典 K-6 §二 令段一「只取…**步驟 0 後**…之土地」⇒ **循環依賴** ⇒ 於步驟 0 之時點，該三欄尚未存在、**求不到值**。」

🔴 **⇒ 三重失準**：① 步驟 0 之時點**已落地**（保守替代·超集判準「兩側皆有 SIDE_LINE」），⛔ 非待辦；② 其**正典形**結構上不可於步驟 0 求值，故「須同時滿足」係一項**不可成就之要求**；③ 「二套判準」之顧慮**倉內早已具名並已作保守處置**（超集 ⇒ ⛔ 不可能漏擋、至多多擋），⛔ 非新發現。

🔒 **正確之表述**：`段一之上鎖`**可獨立落地**（段一在步驟 0 之後，(a)(b)(c) 三欄皆已存在）；`步驟 0` 之時點維持**保守替代⛔ 不動**；**循環依賴之正典解係一項<u>尚未登記</u>之待 KL 域裁事項**（本批同時鑄 `GB-135` 記之）。

**攔截者 ＝ 本窗自查**（於起草下一批施工單、正面列舉 `k6_*` 現況時）。🔒 **⛔ 無土地後果**（該語⛔ 未進入任何施工單之令）。

🩸 **同族**　與 `自誤 240`／`242`／`245` 同族（**倉內查得到，而發單側未查**）。
🔴 **本則之特殊處**：所未查者⛔ 非 `docs/rulings/`，而係**生產碼之註解**——`W-G.9-196 §八` 之上呈門檻只令「問 KL 前須先窮舉檢索 `docs/rulings/`」，**射程⛔ 不含碼註解** ⇒ 該門檻本身有漏。

🔒 **一般式**　凡出艙任何「落地須如何如何」之**要求**者，須先對該要求之**每一個受詞**（此處 ＝ 「步驟 0 之上鎖」）於**三處**窮舉檢索：`docs/rulings/`、各**登記表**、以及 **`app.py`／`verify/**.py` 之註解**；並於同段報命中數與其母體。⛔ **不得**只查前二處。
```

### §二-4　`L-4`　`GB-133` 補記（🛑 追加於 `GB` 登記表**檔尾**·⛔ 鑄號·⛔ 不動 `GB-133` 原節一字）

```
## 🔒 `GB-133` 之補記：**部分成就·⛔ 未解除**（`W-G.9-197` 接手窗自倉現查·態 `7fab020`）

🛑 **本節⛔ 鑄任何號**——其係既有 `GB-133` 之狀態補記。

🔒 **失效條件逐字（引 `GB-133` 本節）** ＝ 「段一全滅之分支上，各候選之街角指數**與名次**皆已算出並出艙，且該出艙可供段二之鍵序 ① 直接消費」。

**逐合取項現查**（母體 ＝ `app.py` 單檔·態 `7fab020`·框 ＝ **列**）

| 合取項 | 現查 | 判 |
|---|---|---|
| 街角指數已算出 | `for cand in group:` @ `app.py:13020`（AST `def _pk_one_side_v12` ＝ `12966`-`13059`），位於早退 `if not qualified: return` @ `:13043` **之前** | ✅ 成就 |
| 街角指數已出艙 | 四欄 `正街角分(0.4)`／`側街分(0.2)`／`跨占分(0.4)`／`總分` @ `app.py:19917`-`19924`；真資料實測坐實（見下節） | ✅ 成就 |
| **名次**已算出並出艙 | 🔴 窮舉檢索：`名次` `0` 列／`排名` `0` 列／`priority_rank` `0` 列／`順位` `4` 列（**逐筆為「第 1 調配順位」＝ 畸零地旗標·異義**）。出艙之 `23` 欄**無名次欄**；`_pre_position_rank` 係**投影序**（＝ 鍵序 **②** 之源·⛔ 非鍵序 ①）。早退分支所 `return` 之 `eliminated` **未經任何排序** | ❌ **未成就** |
| 可供段二鍵序 ① 直接消費 | 鍵序 ① 逐字 ＝ 街角指數**名次**（正典 `K-6:73-74`）；名次不存在 | ❌ **未成就** |

🔴 **⇒ `GB-133` 現況 ＝ 部分成就·⛔ 未解除。** 「指數結構上取不到」之肢已由 `d46dfe6` 消滅；「名次」之肢⛔ 未動。
🛑 ⇒ **⛔ 不得於本登記表記其「已解除／結案」**。`docs/orders/W-G.9-196_發單側交接文.md` `§三-1`「`GB-133` 已解除」與 `§三-3`「段二之落地不再受阻」二語**⛔ 不成立**，應讀為「段二鍵序 ① 之**上游素材**（指數）已備妥，**鍵本身（名次）仍待產出**」。
🔒 **剩餘肢⛔ 非新阻塞項**（⛔ 不鑄新號），係同一失效條件之未竟部分。
🔒 **⛔ 無土地後果**（名次僅供段二排序，段二尚未落地）⇒ 依上呈門檻，⛔ 不上呈 KL。

---

## 🔒 `GB-123` 之通道首次走通：**`GB-133` 之土地後果 ＝ ⛔ 無**（KL UI app 路徑實跑真資料·`SB = 3.5m`·態 `7fab020`）

🔒 **母體與比較端逐字**：基準 ＝ `verify/out/KL_UI_3.5m_7a6b7f3_W-D.1.2診斷_逐候選評點明細.csv`（改前態）；新 ＝ `verify/out/KL_UI_3.5m_7fab020_W-D.1.2診斷_逐候選評點明細.csv`（改後態）。二檔皆 **`20` 資料列 × `23` 欄**；鍵 ＝ (`街廓`,`端`,`候選地號`)，**鍵集合逐筆相同**（僅基準有 `0` 筆／僅新有 `0` 筆）。

| 欄群（母體逐字） | 逐格比對（框 ＝ **格**） |
|---|---|
| 🔒 **土地欄** ＝ `真交集(㎡)`／`整筆幾何(㎡)`／`範圍面積(㎡)`／`G估(㎡)`／`真G(㎡)`／`門檻(㎡)`／`達標`／`原位次(投影序)`／**`選中`**（`20` 列 × `9` 欄 ＝ `180` 格） | 🔒 **土地欄逐格有變 `0` 格** |
| 四指數欄 ＝ `正街角分`／`側街分`／`跨占分`／`總分`（`20` 列 × `4` 欄 ＝ `80` 格） | `—` → 數值 **`48` 格**（＝ `12` 未達標列 × `4` 欄）；純字面（數值相等）**`4` 格**；**數值真變 `0` 格** |

🔴 **⇒ `GB-133` 之落地（`d46dfe6`）於 app 路徑真資料上：`達標`／`選中`／`真G`／`門檻`／`原位次` 逐格未變 ⇒ ⛔ 無土地後果。** 此係 `W-G.9-195 裁 D` 之**真資料坐實**，⛔ 非以 harness 之綠代之（`GB-123` 之禁）。

🔒 **成因已查明·⛔ 非行為變更**（純字面 `4` 格 ＝ `正街角分(0.4)` 之 `R6/右/628-18(1)`、`R1/左/628-36(1)`、`R1/左/628-35(2)`；`側街分(0.2)` 之 `R6/右/628-18(1)`；逐格 ＝ 基準 `'0.0'` → 新 `'0'`，**數值相等**）：
🔑 **對照組取自<u>基準檔自身</u>**（母體 ＝ 該檔 `20` 列·同一序列化器·同一次匯出）：`臨截角` 欄**不含 `—`** ⇒ 其零之字面 ＝ `'0'`；`正街角分(0.4)` 欄**含 `—`** ⇒ 其零之字面 ＝ `'0.0'`。⇒ **同一檔內、同一寫出路徑，「含 `—` 之欄」與「全數值之欄」對零之字面本即不同。**
🔒 ⇒ **成因 ＝ 該欄之型別由「字串（因含 `—`）」轉為「浮點（`—` 已消失）」，序列化之最小表示法使 `0.0` 印為 `0`。** 係 `GB-133` 落地（未達標列之四欄由 `—` 變為數值）之**必然副產物**，⛔ 非數值變化、⛔ 非行為變更、⛔ 非匯出工具所致（KL 逐字回報：**直接下載、未經 Excel 開啟**）。
🛑 ⇒ **`W-G.9-195 裁 D` 之逐字「⛔ 無任何 `winner`／達標候選列之欄值改變」須加註其框**：以**數值**為框成立；以**字面**為框則有 `4` 格之差，而該差已逐格歸因如上。**本節⛔ 不撤銷該裁，僅補其框並閉合其成因。**

🔒 **本節⛔ 變更 `GB-111`／`GB-123` 之失效條件一字。** `GB-123` 之解除須另案裁。
```

### §二-5　`L-5`　新鑄 `GB-135`

```
### `GB-135` 🆕

**編號現查**（`W-G.9-197`·發單側·態 `7fab020`·母體 ＝ 本檔單檔·**扣除集 ＝ `GB-997`**（`S-4` 人造哨兵））：相異 `132`／MAX `134`／缺 `[12, 87]` ⇒ 本則取次一號 ＝ **`135`**。
🛑 **錨定框** `` (?<![0-9\-])GB-135(?![0-9]) `` 全倉 `.md`（`491` 檔）二形並取皆 `0` **列** ⇒ ⛔ 無占用；對照組 `GB-134` ＝ `18` **列** ⇒ 量測器非紅。

| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |
|---|---|---|---|---|---|---|
| **`GB-135`** 🆕 | 🔴 **一項<u>待 KL 域裁</u>之事項，其唯一存在處係生產碼之註解，⛔ 不在 `docs/rulings/`、⛔ 不在任何登記表、⛔ 不在待落地清單。**具名實例：`app.py` 之 `k6_step0_block_locked` 註解逐字載「**待 KL 裁循環依賴之解**」——`K-9-21` 之正典判準 (a)(b)(c) 皆係**段一街角 PK 之產物**，而正典 `K-6 §二` 令段一「只取…**步驟 0 後**…之土地」⇒ **循環依賴** ⇒ 步驟 0 之時點求不到值；現行碼裝**保守替代**（判準 ＝ 「兩側皆有 SIDE_LINE」·所擋恆為正典判準所擋者之**超集**）。**現查**：`循環依賴` 於 `app.py` ＝ `2` **列**、於全倉 `docs/**.md` ＝ **`0` 列**（唯一之 `1` 列出處係 `docs/W-D.1.3_…plan.md` 之 `D-1` `w_i↔k`，**異義**）。🔑 **本則之形**：**待裁事項之可發現性繫於「有人恰好讀到那段碼」**——換案、換窗、換人即遺失。 | **凡以生產碼註解承載「待 KL 裁」「待補」「保守替代」「暫行」者 ⇒ 【必觸】。**⛔ 不綁 `K-9-21`、⛔ 不綁本案。 | 🔒 **⛔ 本則只登記·⛔ 零生產碼·⛔ 不改該註解一字。****現況處置** ＝ 具名並登記，並令爾後凡出艙「某事項是否待辦／已落地」之斷言者，其窮舉檢索之射程須含 **`app.py`／`verify/**.py` 之註解**（承 `自誤 248` 之一般式）。🛑 `K-9-21` 循環依賴之**正典解**另案上呈 KL，**⛔ 不在本則之射程**。 | 字樣錨：`grep -n "待 KL 裁循環依賴之解" app.py`／`grep -n "K-9-21 之保守替代" app.py`；正典 `docs/rulings/K-6_街角地分配程序與可分配判準.md` `### 🔒 K-9-21　` 節 | ✅ **有**——保守替代之超集判準於本案實測「與 winner 讀法、跨占讀法**同解**（皆只擋 `R4` 之群）」（該實測逐字見同註解）；`R4` 二筆之達標於 `KL_UI_3.5m_7fab020_…csv` 逐列坐實（`628(1)` 真 G `1521.05` ≥ 門檻 `226.01`；`628-1(1)` 真 G `457.73` ≥ 門檻 `225.42`） | 🔴 **本則本身⛔ 不需**（純可發現性缺陷）；**其所指之循環依賴正典解⛔ 需**，另案上呈 |

🔒 **失效條件（逐字）** ＝ 「該註解所載之待裁事項，已於 `docs/rulings/` 或某一登記表內具名登記，且其失效條件逐字可查」。
```

### §二-6　`L-6`　新鑄 `GB-136`

```
### `GB-136` 🆕

**編號現查**（`W-G.9-197`·發單側·態 `7fab020`·母體 ＝ 本檔單檔·扣除集 ＝ `GB-997`）：本則取 `GB-135` 之次一號 ＝ **`136`**。
🛑 **錨定框** `` (?<![0-9\-])GB-136(?![0-9]) `` 全倉 `.md` 二形並取皆 `0` **列**；對照組 `GB-134` ＝ `18` **列** ⇒ 量測器非紅。

| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |
|---|---|---|---|---|---|---|
| **`GB-136`** 🆕 | 🔴 **數值輸入之持久化包裝，於還原時⛔ 不驗其是否仍落在該欄位之 `min_value`／`max_value` 區間 ⇒ 使用者切頁後回來即整頁崩潰。**具名：`app.py` 之 `_ni`（`grep -n "def _ni" app.py`）逐字 ＝ `if key and f"_sv_{key}" in st.session_state: kwargs['value'] = st.session_state[f"_sv_{key}"]`——**⛔ 無任何 clamp、⛔ 無任何區間檢**。實跑逐字：`StreamlitValueBelowMinError: The 'value' 0.0 is less than the 'min_value' 50.0`，發生點 `app.py` 之 `_ni("價格日期物價指數", min_value=50.0, max_value=200.0, …)`。🔑 **本則之形**：**一個為了「切 Tab 保值」而加的包裝，把「未初始化之 0.0」寫回 session 後，於下一次 render 反過來違反該欄位自己宣告之合法域** ⇒ **UI 全頁 traceback、⛔ 非可恢復之警示**。 | **凡任一 `number_input` 具 `min_value`（或 `max_value`）且經 `_ni` 包裝者 ⇒ 【必觸】**，只需其 `_sv_` 快照曾落在區間外。⛔ 不綁「物價指數」、⛔ 不綁本案、⛔ 不綁 `SB` 值。 | 🔒 **⛔ 本則只登記·⛔ 零生產碼。****現況處置** ＝ 具名並登記。🛑 落地時之**結果態**（⛔ 非操作步驟）＝「還原之值若落在宣告區間外，須被夾回區間內（或視為無快照而採預設），且**⛔ 不得靜默**——須具名記錄該次夾制」。⛔ **不得**以移除持久化為解（會回退「切 Tab 掉值」之既有功能）。 | 字樣錨：`grep -n "def _ni" app.py`／`grep -n "_sv_" app.py`；實跑逐字：`verify/out/KL_UI_3.5m_7fab020_stdout.log` | ✅ **有**——🔴 **KL 之 UI 實跑<u>四代皆現</u>**（母體 ＝ `verify/out/` 之 `KL_UI_3.5m_*_stdout.log`·框 ＝ **檔**）：`2e08a41`／`a3c97aa`／`7a6b7f3` 三檔各 `1` **列**，加本批 `7fab020` ⇒ **`4` 檔 `4` 列**。⇒ **既有、可重現、⛔ 非本波所致。** | ❌ **不需**——純 UI 狀態管理缺陷、⛔ 無土地後果、⛔ 非域裁 |

🔒 **失效條件（逐字）** ＝ 「`_ni` 之還原路徑對 `min_value`／`max_value` 具名夾制或棄用快照，且 `verify/out/` 內新一代 `KL_UI_*_stdout.log` 之 `StreamlitValueBelowMinError` 命中 ＝ `0` 列」。

🩸 **本則之發現方式即 `GB-135` 之同族第二例**：其唯一紀錄係**三份未經分類之 stdout log**，`_sv_` 於全倉 `docs/**.md` ＝ **`0` 列** ⇒ 三代實跑皆載此崩潰而**⛔ 從未登記**。
```

### §二-7　`L-9`　`CLAUDE.md` 檔尾追加

```
## 🔒 `W-G.9-197` 接手窗：裁 `G`、待落地警語、土地後果實測（`2026-09-01`）

### 裁 `G`：**`GB-133` ⛔ 未解除，係「部分成就」**

🔒 `GB-133` 之失效條件為**三合取**（指數已算 ∧ **名次**已算並出艙 ∧ 可供段二鍵序 ① 直接消費）。`d46dfe6` 只成就第一項與指數之出艙；**名次於 `app.py` 全檔⛔ 不存在**（`名次`／`排名`／`priority_rank` 皆 `0` **列**·母體 ＝ 該檔單檔；`順位` `4` **列** 逐筆為「第 1 調配順位」＝ 畸零地旗標·異義）。
🔴 ⇒ `W-G.9-196` 交接文 `§三-1`／`§三-3` 之「已解除」「不再受阻」**⛔ 不成立**；`GB` 登記表**⛔ 不得記其解除**。🔒 ⛔ 無土地後果 ⇒ ⛔ 不上呈 KL。

### ⬜ **待落地之警語（承 `更正三`·序⛔ 不變）**

🛑 `K-6-B` **段一之「上鎖」** 於現行碼**⛔ 不存在**——正面列舉現查（態 `7fab020`·母體 ＝ `app.py` 與 `verify/stepg_pipeline.py` **二檔**·框 ＝ **列**）：`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` 六者**逐檔皆 `0` 列**；對照組 `步驟 0` @ `app.py` ＝ `9` 列 ⇒ 量測器非紅。
🛑 `K-6-B` **段二之全域排序**⛔ 不存在；其鍵序 ① 所需之**名次**亦⛔ 不存在（見裁 `G`）。
🔒 **`步驟 0` 之上鎖保護已落地，惟係<u>保守替代</u>**（`k6_step0_block_locked`·判準「兩側皆有 SIDE_LINE」·所擋為正典判準所擋者之**超集**）；其**正典解繫於一項待 KL 域裁之循環依賴**，已鑄 `GB-135` 記之。
🔴 ⇒ **`段一之上鎖`可獨立落地**（段一在步驟 0 之後，正典判準三欄皆已存在）；**⛔ 不得**於本波內改動 `k6_step0_block_locked` 之保守替代（未經 KL 裁）。

### 🔒 `GB-133` 之土地後果 ＝ ⛔ 無（KL UI app 路徑實跑·`SB = 3.5m`·態 `7fab020`·真資料）

土地欄（`真交集`／`整筆幾何`／`範圍面積`／`G估`／`真G`／`門檻`／`達標`／`原位次`／`選中`·`20` 列 × `9` 欄 ＝ `180` **格**）**逐格有變 `0` 格**；四指數欄（`80` 格）`—`→數值 `48` **格**、純字面 `4` **格**、數值真變 `0` **格**。
🔒 純字面 `4` 格之**成因已閉合**：同一基準檔內，`臨截角`（不含 `—`）之零字面 ＝ `'0'`，`正街角分`（含 `—`）之零字面 ＝ `'0.0'` ⇒ 成因 ＝ 欄型別由字串轉浮點（`—` 消失所致），⛔ 非數值變化。
⇒ `裁 D` 獲真資料坐實；其「無任何達標候選列之欄值改變」一語**須以<u>數值</u>為框**。

### 🔒 本次實跑之既有終局（⛔ 非本波所致·⛔ 不列為新紅）

app 路徑**仍未跑完**，終止於 `k94_assert_baseline_touch`：`【未配到屁股線】1 宗——R5 之 628-53(2)（差 38.803m）`。
🔒 `②-宗 圍堵閘破[R5]`（`宗-宗重疊 = 56.8187 > 上界 1.5999`·`(宗數8−1)×0.005×深度45.71`）於 app 路徑係 `st.warning`（`GB-54`）⇒ ⛔ 不中止；其**同一母體之既有實測**見 `verify/out/probe_WG9187_baseline_76378d09.log:14`（`56.8186`·上界 `1.5999`·宗數 `8`·深度 `45.71`）⇒ **既有現象、繫 `GB-67`（成因未定）**。⚠️ 二值差 `0.0001`（顯示 4dp 之末位）**⛔ 未查**，列為下一批之附帶量測。
```

---

## §三　`L-7`／`L-8`　KL UI 實跑證據二檔之入倉

🔴 **KL 放檔**（`常規`：施工單以倉為媒介，KL 只需放檔）。置於 `C:\Users\admin\Desktop\land-readjustment-trial\verify\out\`，**檔名逐字**（🛑 ⛔ 不得含空白、⛔ 不得含全形冒號、🛑 短碼須為 **commit** 之前 `7` 碼·⛔ **不得**用 `app.py` 之 **blob** 短碼）：

```
KL_UI_3.5m_7fab020_W-D.1.2診斷_逐候選評點明細.csv
KL_UI_3.5m_7fab020_stdout.log
```

🔒 **命名之立法理由**：倉內既有三代逐字 ＝ `KL_UI_3.5m_2e08a41_…`／`KL_UI_3.5m_a3c97aa_…`／`KL_UI_3.5m_7a6b7f3_…`，其短碼**皆為 commit**。`b9a50a2` 係 `app.py` 之 **blob**（`git cat-file -t b9a50a2` ⇒ `blob`），以之命名將使讀者之 `git show <短碼>` 取不到態 ⇒ **假錨**。

🔒 **CC 入倉時須逐字回報**（各數皆自載母體與框）：二檔各之 bytes／`\n` **次**／`CR` **次**／`sha256`；CSV 另報資料**列**數與**欄**數；並報 `git diff --numstat` 二檔之 `deletions` 皆 ＝ `0`。

---

## §四　驗收（🛑 判準⛔ 繫於任何 commit 圖形·承 `自誤 246` 之一般式）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `V-1` | 零生產碼 | `git diff --name-only <基座>..HEAD` **逐列⛔ 不含** `app.py`、⛔ 不含任何 `verify/**.py` |
| `V-2` | append-only | `git diff --numstat <基座>..HEAD` **逐檔 `deletions` ＝ `0`** |
| `V-3` | 🔑 **payload 逐位落地** | 自本單 `§二-1`〜`§二-7` 七個 fence 抽出之文字，與其落入之檔尾對應區段**逐位元組相同**（各報 `sha256`） |
| `V-4` | 取號未被搶佔 | 落地後自誤登記表之框 MAX ＝ **`248`**；`GB` 登記表（扣哨兵後）MAX ＝ **`136`** |
| `V-5` | `L-7`／`L-8` 在倉 | `git ls-files -z` 之集合含該**二**檔路徑；其 `sha256` 與 `§三` 回報值相同 |
| `V-6` | 🔴 **判別力對照** | 未鑄之 `自誤 249` 與 `GB-137` 之錨定框全倉 `.md` 命中**皆 `0` 列** ⇒ `False`。若非 `False` ⇒ 量測器紅、停機 |
| `V-7` | 🔴 **`GB-136` 之真資料覆蓋自證** | `grep -c -F "StreamlitValueBelowMinError" verify/out/KL_UI_3.5m_*_stdout.log` 之**檔數** ＝ `4`、**列數**逐檔 ＝ `1`（含本批新入之 `7fab020`）。⛔ 不符 ⇒ 停機上呈 |

🛑 **⛔ 不得以「計數相符」為單獨之驗收判準**——`V-3` 之**逐位比對**係 `L-1`〜`L-6`／`L-9` 之唯一 operative 判準。

---

## §五　commit 與 `push`

- **單一 commit**。訊息建議：`W-G.9-197：自誤 246/247/248 ＋ GB-133 補記 ＋ 鑄 GB-135/136 ＋ KL UI 實跑證據二檔入倉 ⛔ 零生產碼`
- 🟢 **零生產碼 ⇒ CC 逕行 `push`**，⛔ 不需請示 KL。
- 執行報告 `A-2` **併入同一 commit**（🔒 承 `W-G.9-195 裁 F`：單一 commit ⇒ 結構上無「報告推不出去」之風險）。

🔒 **CC 回報須含**：`S-0a`〜`S-0e` 五閘逐項實測值；`§零-c` 六項 🟡 之實跑是否「恰為本單所預告之集合」；`V-1`〜`V-7` 七閘逐項；`§三` 之各項量。

SELF_SHA256: 7633c384551bd4be056b8142f679604dae0d80bfe2d619033cf4aa5012d1379a
