# `W-G.9-303`　補令三：期值取代之**窮舉** ／ `自誤 408` ＋ 坑 `bl` 之鑄 ／ **`§三`／`§四` 之起手授權**

> **受詞** ＝ **新**施工窗（`W-G.9-303` `§三`／`§四` **起手前之最後一則**）。
> **態**｜開工 ＝ `fb7f415881f57af930234c2f8c1af8280f1fcee1` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·坑 `bh`）。
> **凍存** `p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（**⛔ 併線·⛔ 刪除**）。
> **此單此前是否已交付** ＝ **否**。｜**分級** ＝ **輕**（鑄二號 ＋ 期值之取代·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**）。
> 🛑 **本批鑄號 ＝ `自誤 408` ＋ 坑 `bl`**（**二號**·逐字由本單供·**`###` 形**）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔑 **`ALPHA_LABELS` 之擴至 `bl` 於本批<u>明文授權</u>**（射程逐字見 `§四`·⛔ 逾之）。
> 🔴 **KL 之放行於本批<u>仍不動用</u>**；⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；**⛔ 辦修批**。
> 🔑 **本批辦畢 ⇒ 出報告 ⇒ 停；其<u>次一批</u>即 `W-G.9-303` 之 `§三`／`§四`**（本補令 `§零-3` 明文授權其起手）。

---

## `§零`　三事（**發單側·⛔ CC 自裁**）

### `§零-1`　🔴 責 ＝ 發單側：期值之取代**未窮舉其載體**

`W-G.9-303 補令二` `§零` 款 `3` 只點名 `W-G.9-303` `§零` 閘 `6` 之期值，而同一數於該單內另有**二處**載體。
受單側於**起手前**現查逐字回報，**處置完全正確**。發單側自倉獨立復現（產生指令 ＝
`git show HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md | grep -n '396\|406'`）⇒ 命中 **`3`** 列。
⇒ 鑄 `自誤 408`（`§三`）。

### `§零-2`　🔑 期值之取代（**逐處具名其受詞**·⛔ 一律改、⛔ 一律不改）

🛑 **CC 須先以下列<u>產生指令</u>復現其載體，並與本表逐處對拍**；不符即**停、照實具名、⛔ 續**：

- `git show HEAD:<303 單> | grep -n '396\|406'` ⇒ 期 **`3`** 列
- `git show HEAD:<303 單> | grep -n '89／89'` ⇒ 期 **`2`** 列
- `git show HEAD:<303 單> | grep -n 'bk'` ⇒ **其數由指令得·本補令⛔ 予期值**（`自誤 406` 攔法 ②·含恆常附款之坑列舉）

| 處（**字樣錨**·⛔ 行號為錨） | 受詞 | **處置** |
|---|---|---|
| `§零` 閘 `6` ——`` `python verify/probes/probe_WG9267_issuer_measurers.py registry` `` 之列 | **期值** | 🔴 **改**：自誤 相異 **`398`**／MAX **`408`**（`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **不動**） |
| `§零` 閘 `6'` ——`` `python verify/probes/probe_WG9269_pit_index.py` `` 之列 | **期值** | 🔴 **改**：坑 **`90`／`90`**·末標籤 **`bl`** ⇒ 下一 **`bm`** |
| `§六` 收工閘 `4` ——`` 四簿期末（**器之輸出**） `` 之列 | **期值** | 🔴 **改**：自誤 相異 **`398`**／MAX **`408`**；其「🔑 **零鑄號**」之判**⛔ 改**——其受詞係**該批**之鑄號數（仍為 `0`） |
| `§六` 收工閘 `5` ——`` 坑之期末（**器之輸出**） `` 之列 | **期值** | 🔴 **改**：坑 **`90`／`90`**·末標籤 **`bl`** ⇒ 下一 **`bm`**；其「⛔ 鑄坑」之判**⛔ 改** |
| `§零′` ——`` 四簿 自誤 相異 **`396`**／MAX **`406`**（`395 + 1`） `` 之列 | 🔑 **前批（`W-G.9-302`）之實測·⛔ 期值** | 🟢 **⛔ 改** |
| `§零` 閘 `1` 之 `4bc9c77347416ff474f778d24dcc7dc23132f9a0` **或其後裔** | 期值 | 🟢 **⛔ 改**——「或其後裔」已涵蓋 `fb7f415…` 及本批之 `commit`（🛑 CC 須以 `git merge-base --is-ancestor 4bc9c773… HEAD` 出艙其 `rc = 0`） |
| 恆常附款之坑列舉（`坑 bh／bi／bj／bk／…`） | **附款之列舉**·⛔ 期值 | 🟢 **⛔ 改**；🔒 **惟坑 `bl` 之攔法自本批起適用**（逐字見 `§四` payload） |

🛑 **`W-G.9-303` 施工單本文⛔ 改一字**——上開取代**僅存於本補令**；`補令二` `§零` 款 `3` 於**閘 `6`** 之取代由本表**再取代**。
🛑 **三單並存**（`W-G.9-303` 施工單／補令二／補令三）·**⛔ 追改任何一字**；相斥之點以**本補令**為準。

### `§零-3`　🔑 `W-G.9-303` `§三`／`§四` 之**起手授權**

`W-G.9-303` 之 `§二`（工項零）**已由 `補令二` `§二` 代行**（`commit` `bb742f8d13f90269ecd805ad9eb79ce4c35b6767`·
發單側自倉復驗：倉內 blob 內容 `sha256` ＝ `460779b22a491bc8132f424525ebab1df308eca1944e5a81483391244d568142`
＝ 發單側所交付之檔**逐位相同**·blob `sha1` ＝ `8c85fc1ccdf781ce08c10ba397038bfe97c50238`·**二框並報**）
⇒ 🛑 **⛔ 重辦**，惟須於該批報告之對應節**具名該 `commit`（全 `40` 碼）為代行**。
⇒ **本補令辦畢後，`W-G.9-303` 之 `§三`／`§四` 即得起手**（**另批**·⛔ 與本批同批）。
🛑 其 `§七` 之「報告首行具名本窗 context 百分比·`≥ 70%` 具名**建議換窗**」**仍在**——🔑 **且其判之時點提前**：
**起手<u>前</u>即須具名**；若起手前已 `≥ 70%`，**⛔ 起手**，逕出收工批並**建議換窗**（`W-G.9-303 補令一` 之形）。

---

## `§一`　開工閘（**⛔ 略**·**⛔ 驅動任何長跑**·每列同格載其產生指令·`自誤 392`）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `fb7f415881f57af930234c2f8c1af8280f1fcee1` **或其後裔**（全 `40` 碼） |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**·`自誤 405`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**） |
| `4` | `python verify/probes/probe_order_preflight.py <本補令>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 **逐項具名處置**（豁免三類見 `§一-1`）·⛔ 靜默略過 |
| `4'` | 受詞自證（坑 `bf`） | [必真] 器所印之檔名含 `補令三` ＝ `True`／[必偽] 含 `補令二` ＝ `False` |
| `5` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 自誤 **`397`**／**`407`**｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6` | `python verify/probes/probe_WG9269_pit_index.py` | **`89`／`89`**·末標籤 **`bk`** ⇒ 下一 **`bl`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；判別力[必不命中] 子層 **須 `> 0`**（⛔ 定值）；🔑 **本數即本批一切母體之外部錨**（坑 `bj`） |
| `8` | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`（**三態分列**·`rc` **緊接指令**） | **態1**（`rc = 0`）；判別力[必為零] ＝ 同指令對一**執行期組出**之人造路徑須 `rc ≠ 0`（字面⛔ 出艙） |
| `9` | 🔑 `§零-2` 之**載體復現**（其三條產生指令） | `396\|406` ⇒ **`3`** 列；`89／89` ⇒ **`2`** 列；`bk` ⇒ **其數由指令得**（⛔ 對拍任何期值·照實出艙） |

### `§一-1`　`P-4`／`P-1` 之逐項處置（**⛔ 靜默略過**）

🟡 之受詞若屬**三類**⇒ **受具名豁免**：**(甲)** `→` 係**賦值鏈／消費鏈／查表鏈／流程節序**之串接；
**(乙)** 方向性之詞係**序之詞而非幾何**（`前綴`／`後裔`／`首`／`末`／`前`／`後`／`首則`／`次則`／`首行`）；
**(丙)** `⇒` 係**推論之連詞**，⛔ 空間之指向。
🛑 三類皆⛔ 幾何向量之方向性轉引 ⇒ **無座標系可具名**。**凡不在此三類內者，⛔ 受本豁免，須停、照實具名、⛔ 續**。

🛑 **恆常附款逐項適用·⛔ 省**（坑 `bh`／`bi`／`bj`／`bk`／`bc`／`bd`／`be`／`bf`／`bg`／`g`／`3`、`GB-158` **三形分列**、
簡繁誤字機檢、`rev` 釘死、**先跑再寫**、[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、
payload 之落地形機驗**繫於性質**、`rc` **緊接指令**、**等待綁行程結束**、**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N`**、
**讀 blob 取 bytes**、**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**、
`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退** ⇒ 先 `cat-file -e`·**三態分列**、
**簿之數一律取器之輸出**、**母體之基數須綁外部錨**、**含 CJK 路徑之 `git` 輸出取 bytes**、
**摘要值一律逐字具名其產生指令**、**母體一律以產生指令界定·⛔ 取單內之列舉**、
🆕 **⛔ `git ls-files --with-tree=<rev>` 取某 `rev` 之樹**（坑 `bl`·逐字見 `§四`）、
自捕為零者須併具「**⛔ 讀為『本批無誤』**」）。

---

## `§二`　工項零：**本補令原封入倉**（主線）

落點 ＝ `docs/orders/W-G.9-303_補令三_期值取代之窮舉與起手授權.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`·[必真] 含 `補令三`／[必偽] 含 `補令二`）；檔數 ＝ `1` 之自證（造甲[必≥1] ＝ `docs/orders/W-G.9-303_補令二_新窗首則與施工單之入倉.md`／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；**blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源**（🔒 **二框並報**）；`deletions` ＝ `0`。

---

## `§三`　工項一：`自誤 408` 之鑄（主線·落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

🔑 界 ＝ `<!-- ZW408-BEGIN -->` 與 `<!-- ZW408-END -->` 之間（**不含哨兵列**·哨兵⛔ 入簿）·**⛔ 行號為錨**。
🛑 唯一性之框 ＝ **整列<u>逐位相等</u>**（`^…$`）·母體 ＝ 本補令單檔·各須命中 **`1`**；併報**子字串框**之數（⇒ 必得 `2`·**二數並報**）。payload **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**。

<!-- ZW408-BEGIN -->

### 🩸 `自誤 408`　**取代一款之期值，而未窮舉該數於同一單內之<u>全部載體</u>**

**形**：`W-G.9-303 補令二` `§零` 款 `3` 逐字取代「`W-G.9-303` `§零` 閘 `6` 之期值『自誤 `396`／`406`』」，改為 `397`／`407`。
惟同一數於該單內另有**二處**載體：`§六` 收工閘 `4`（**期值**·須同改）與 `§零′` 之核可敘述（**前批之實測**·⛔ 改）。
補令二**只點名其一**。

**徵候**：受單側於**起手前**現查逐字回報——「其 `開工閘 6` 與 `收工閘 4` expect 自誤 `396`／`406`」。
發單側自倉獨立復現（產生指令 ＝ `git show HEAD:<該單> | grep -n '396\|406'`）⇒ 命中 **`3`** 列。

**根因**：取代之款以**單內之位置**（「`§零` 閘 `6`」）界定其受詞，而非以**產生指令**界定
（「該單內凡以該數為**期值**者」）⇒ 同數之其餘載體**靜默留舊**。
🔑 其根與 `自誤 406` **同**——`406` 窄化**停機款之母體**，本則窄化**取代之母體**。

🛑 **併須具名其反面（⛔ 讀為「凡命中該數者一律改」）**：`§零′` 之 `396`／`406` 係**前批（`W-G.9-302`）之實測**、
**⛔ 本批之期值** ⇒ **⛔ 改**。⇒ 本則之攔法係「**逐處具名其受詞後分別處置**」，**⛔ 一律改、⛔ 一律不改**。

**族** ＝ `自誤 406`（母體預先窄化）之同根；併涉 `自誤 400`（一詞承載二受詞而未釘口徑）
——同一數字於單內**兼為**「期值」與「前批實測」。

**攔法（三款）**：
① 凡以補令取代前單之任一**數**者，須以**產生指令**界定其母體（`git show <rev>:<單> | grep -n <該數>` 之**全量列舉**），
並**逐處具名其受詞**（期值／事實敘述／附款之列舉／其他）；**⛔ 以單內之位置（節號）界定**。
② 逐處之處置須**分別出艙**（改／⛔ 改）並各具其由；**⛔ 一律改、⛔ 一律不改**。
③ 取代之後，須令受單側於**起手時**以同一產生指令**復現**其載體數，並與本補令之逐處處置**對拍**；不符即停、照實具名、⛔ 續。

<!-- ZW408-END -->

**落地後之機驗（🔑 繫於性質·`自誤 402` 攔法）**：以**器本身**（`registry`）量落地後之簿 ⇒
自誤 相異 ＝ **`398`**（`397 + 1`）／MAX ＝ **`408`**；`408` 命中 **`1`**（⛔ `> 1`）；
判別力[必命中] ＝ `399` 須 `True`／[必為零] ＝ 一**執行期組出**之人造號（字面⛔ 出艙）須 `False`；
哨兵 `ZW408` 入簿命中 ＝ **`0`**；append-only 二造 ＋ `new.endswith(payload + b'\n')` ＋
`python verify/tools/wg942_append_audit.py`（先設 `PYTHONIOENCODING=utf-8`）`rc = 0`。
🛑 任一不成立 ⇒ **判器紅、停、上呈**。

---

## `§四`　工項二：坑 `bl` 之鑄 ＋ `ALPHA_LABELS` 之擴（主線·**與 `§三` 分列 `commit`**）

🔑 **本工項<u>明文授權</u>一項手段**（射程逐字·⛔ 逾之）：**擴 `ALPHA_LABELS` 至 `bl`**——射程 ＝ **僅**
`verify/probes/probe_WG9269_pit_index.py` 內**新增一列** `ALPHA_LABELS += ["bl"]`（形同其既有之 `+= ["bj", "bk"]` 列），
**⛔ 改該檔任何既有列一字**、**⛔ 動該檔之任何框或判準**。🔒 該檔屬 `verify/` **子層**、**⛔ 在生產碼 `34` 檔母體內**。

🔑 界 ＝ `<!-- PITBL-BEGIN -->` 與 `<!-- PITBL-END -->` 之間（**不含哨兵列**·哨兵⛔ 入簿）；
落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**。唯一性之框同 `§三`（**整列逐位相等**·二數並報）。
🔒 **定義處<u>多於一處</u>之預先受領**——與坑 `bh`／`bj` 同形、已於 `W-G.9-285 §五-2` 受領。
🛑 **本補令⛔ 予其期值**（`自誤 403` 攔法 ②·`自誤 406` 之併記：`W-G.9-301` 預告 `bj` ＝ `2` 而實測 `4`）；
CC 須以**器之定義框**出艙其清單（逐處之 `檔` ＋ **字樣錨**）並**照實具名其數**，**⛔ 判紅、⛔ 逕修該框**。
🔒 **發單側之併記（⛔ 充期值·CC 須自行重得）**：本補令單檔內，以坑之定義框（`^#{1,6}…坑…`）自量得 **`3`** 列
（H1 ／ `§四` 節題 ／ payload 之標題）——⇒ 連同簿之落地，其總數**由器得**。

<!-- PITBL-BEGIN -->

### 🕳 坑 `bl`　**`git ls-files --with-tree=<rev>` 之語義係 `index ∪ tree`，⛔ 該 `rev` 之樹**

**形**：以 `git ls-files --with-tree=<rev>` 取「某 `rev` 之樹」作為**期初**之母體，而其實際語義為
**index 與該樹之<u>聯集</u>**。

**徵候（`W-G.9-303R` 補令二·自捕 `5`）**：`.md` 之**期初**被本批**已 `add`** 之新檔污染成 `810`（＝ 期末），
其增量遂為 **`0`** ⇒ 收工閘之**自洽檢**（「母體檔數之增量 ＝ 本批新增之 `.md` 檔數」）**以通過之姿**
對「新增了什麼」**完全失明**。🔑 **假綠**——綠之來源係**二端同被污染**，⛔ 增量實為零。

**根因**：`--with-tree` 之設計目的係「令尚未 `commit` 之 index 與某樹**併觀**」，其為**聯集**而非**取代**。

**攔法（三款）**：
① 凡欲取**某 `rev` 之樹**者，一律用 `git ls-tree -r <rev>`（或其他**不含 index** 之指令），**⛔ `--with-tree`**；
② 凡**期初／期末**對拍之二端，其**至少一端**須綁**外部錨**（坑 `bj`）——本案攔下它的正是**倉內既載之 `807`**、**⛔ 自洽檢**；
③ 凡自洽檢之增量得 **`0`** 而本批**確有新增**者，一律 **loud** 並查其二端之框**是否同源**；⛔ 以「增量為零」充「無新增」。

<!-- PITBL-END -->

### 四-1　落地後之機驗（🔑 **繫於性質**）

| 款 | 受詞 | 判準 |
|---|---|---|
| `1` | 坑之鑄號框之**當場現查**（自 `verify/probes/probe_WG9269_pit_index.py` 取其定義框之**逐字**·⛔ 轉引本補令） | 出艙其 pattern 逐字（**若為多形則分列**） |
| `2` | 以**器本身**量落地後之態 | **`90`／`90`**（`89 + 1`）·末標籤 **`bl`** ⇒ 下一 **`bm`**；`bl` 之**可解析** ＝ `True` |
| `3` | `ALPHA_LABELS` 之擴 | 成員數 **`+1`**、尾元 ＝ `bl`（🛑 **先實測後填**）；該檔之 `git diff --numstat` ＝ **`1` 增／`0` 刪**；🔒 框字串之處數**須現查**（其若以 `ALPHA_LABELS[-1]` 之 f-string 生成則自動齊一·須出艙實測而⛔ 假定） |
| `4` | 定義處清單 | 逐處出艙 `檔` ＋ 字樣錨；**其數由器得**·照實具名·**⛔ 判紅、⛔ 逕修** |
| `5` | 哨兵⛔ 入簿 | 落地後簿內 `PITBL` 之命中 ＝ **`0`** |
| `6` | 判別力二造 | 造甲[必≥1] 坑 `n` 之定義處 `≥ 1`／造乙[必為0] **執行期組出**之人造坑 ＝ `0`／`0` |

---

## `§五`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄（`deletions`）**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·含 CJK 路徑者取 bytes·坑 `bk`） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末（**器之輸出**） | 自誤 相異 **`398`**／MAX **`408`**（＋**一**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末（**器之輸出**） | **`90`／`90`**·末標籤 **`bl`** ⇒ 下一 **`bm`**；🔑 **母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 檔數**——🛑 其**期初**須以 `git ls-tree -r <期初 rev> -- '*.md'` 取（**⛔ `git ls-files --with-tree`**·坑 `bl`），且**至少一端綁外部錨**（坑 `bj`） |
| `5'` | `wg9223_acceptance_audit.py <本補令> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 本批確有異動之檔）；`app.py` ＝ `4379108a…`（**框 ＝ blob `sha1`**）；`baselines` `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；`K-9-5-*`／`K-9-12`／`K-9-29` 任何子項⛔ 落地；**六物證落檔**之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（[必為零]·**⛔ 開**）；判別力[必存在] `p3a` 命中 **`1`** |
| `6''` | **新窗次則之可取得性**（`自誤 407` 攔法 ③） | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` ⇒ **態1**；判別力[必為零] ＝ 一人造路徑須 `rc ≠ 0` |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§六`　🛑 **⛔ 為之事**

1. **⛔ 辦 `W-G.9-303` 之 `§三`／`§四`**（其起手為**次一批**）；**⛔ 驅動 `run_verification` 或任何長跑**；**⛔ 新增任何探針**。
2. ⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 辦修批。
3. ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；**⛔ 引任何自 baseline 取得之數**。
4. **⛔ 改 `W-G.9-303` 施工單一字**（其期值之取代**僅存於本補令 `§零-2`**）；⛔ 修補、⛔ 重排、⛔ 補其缺漏；⛔ 重辦其 `§二` 工項零。
5. ⛔ 鑄 `GB`／`VR`／`K-9` 之任何號（**`自誤 408` ＋ 坑 `bl` 為本批唯一之鑄**）；⛔ 追加任何 `GB` 之末端登記。
6. ⛔ 動 `probe_WG9269_pit_index.py` 之**任何既有列**（僅准新增一列）；⛔ 動任何**其他既有**探針一字。
7. ⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項。
8. **⛔ 追改 `W-G.9-303` 補令一／補令二／交接註／`W-G.9-303R` 或任何已入倉文件一字**。
9. ⛔ 判 `GB-170` 可否解除、⛔ 判路丙之落點可施與否、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 擬任何改法或 diff。
10. **⛔ 呈 KL 任何事。**
11. **⛔ 於任何入倉文件宣稱一件尚未發生之事**——及於 `commit` 訊息與「已推送」之陳述。

---

## `§七`　報告之格

`§一` 開工閘（**每列同格載其產生指令**·閘 `9` **載體之復現**）→ `§二` 工項零 → `§三` 工項一 → `§四` 工項二 →
收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→ `⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；**摘要值一律逐字具名其產生指令**；框無 `^` 錨者**二數並報**；**母體之基數綁外部錨**。
🔒 凡自捕一律於出艙前攔下、照實併記（**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **報告之末須併具：本窗 context 之百分比（照實）＋ 其是否 `≥ 70%` 之判**（`§零-3` 之起手前判）。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。⛔ 自續 `W-G.9-303` 之 `§三`／`§四`。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`acceptance_audit`／`deletions`／`cat-file`／`merge-base`／
`append_audit`／`ZW408`／`PITBL`／`ALPHA_LABELS`／`ls-tree`／`with-tree`／`baselines`／`numstat`／
`sha256`／`WV_BAKE`／`W-G.9-299-gb170`／`p3a`。

【單止】本補令受詞 ＝ CC **新**施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: 0a48c670e138e20b2bf5ab7d68153bd98a488ed5898c7336905d4060d27ab2aa
