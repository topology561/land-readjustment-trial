# W-G.9-268′　補令二（`c0-3` 停機之處置：受詞精確化 ／ shim 停用 ／ 新靶重得 ／ `c1` 之依據改繫）

> **受詞** ＝ `W-G.9-268′` 之施工窗。**期初態** ＝ `4ff8b35523ddd43448330f63e264ca565d95bf50`（須 `ls-remote` 實查）。
> 🔒 本補令**⛔ 改**原單之方法、`§二` 工項切分（`c1`〜`c4`）、`§三` 十閘、`§六` 併線前必答一字。
> 🔒 所改者限於：`§零-2`（驗收靶）／`§四` 之量級估計／`§八` 停機款第 `3` 款／`§七` 鑄號授權；並增 `c0-5` 一項。
> 🔒 原單、補令一、本補令**並存**；相斥處以**時序最後者**為準。
> 🔒 `-268pR` 之 `c0-1`／`c0-2`／`c0-4`、`§一` 九閘、`§三` 補令一三事、`§四` 自解 `1`〜`3`、`§五` 自捕——發單側**逐項核可**，⛔ 重辦。

---

## §一　題 `A` 之裁：**受詞之精確化**（⛔ 新裁·目標未變）

**採納 `-268pR c0-1`（丙）之實測語意為現態之正字**（逐字）：
> 宗 `j+1` 之起算點 ＝ **前一宗（`j`）之起算點 ＋ 前一宗（`j`）之標稱 `S`（`S_raw`）**
> （＝ **前一宗之標稱終點**），而**⛔ 前一宗多邊形沿 `d̂` 之幾何極值**。

**⇒ 本批受詞之精確形（取代原單 `零-1`）**
> **起算垂線之錨，由「前一宗之<u>標稱終點</u>（起算點 ＋ `S_raw`）」改為「前一宗之<u>幾何終點</u>（其遠側界離開街廓之點 ＝ `S_req` ＝ 藍影外邊界）」。**

🔒 **此⛔ 為新裁**：**目標端未動一字**（`S_req` ＝ 幾何終點，由 `W-G.9-84` 量得、`K-9-14` 所裁、`K-9-15` 三明令**維持**）。所改者僅為對**現態**之記述——現態係事實，非裁定。
🔒 **受詞之否定面仍成立**：現行錨確**⛔ 取在幾何終點** ⇒ `GB-67` 成因鏈之方向不變。

### 一-1　`GB-67` 加註之不精確：以**末端追加**登記（⛔ 追改原文一字）

`GB-67` 之 `W-G.9-87` 加註逐字載「**現行實作將錨取在第 0 宗之<u>起點</u>（`0.2163` ≈ 其 `s` 下界 `0.2677`）**」。
🔴 **`0.2163` 之倉內受詞為「CC 所算之『`K-9-9 二` 下限』」**（`GB-67` 同表逐字、`docs/驗證裁定登記表.md` 同載），**⛔ 錨之 `s` 位置**。⇒ 該加註將**約束之下限值**與**錨之位置**併為一事。

施工窗於 `c0-5` 同一 `commit` 內，於 `GB-67` **檔末**追加一則更正註，載：
`(i)` 原述之逐字與其落點；`(ii)` `0.2163` 於倉內之受詞（附其出處逐字）；`(iii)` `c0-1`（丙）所實測之現態語意（附字樣錨與其唯一性自檢）；`(iv)` 更正之時態錨（commit）。
🛑 **⛔ 追改 `GB-67` 原列與其歷次加註一字**（`常規四（九）五`）。

---

## §二　題 `B` 之裁：`probe_WG985_grouphead.py` `A-4` shim **停用**

🔒 **停用之範圍（逐項）**：
1. ⛔ 作為 `c1` 之**設計依據**（其原錨模型與生產碼之錨為**不同之量**）。
2. ⛔ 作為本批之**驗收靶**。
3. ⛔ 引其任何數（`5／5`／`3／5`／`3／3`）為任何結論之基礎。
4. 🟢 **得** import 其**模組級常數**與 `S_req` 之**定式**——惟凡 import 者，皆須經 `c0-5` 之自我驗證閘證其與現態相符方得使用；未過閘者⛔ 用。
5. 🛑 **⛔ 改該檔一字**（其為既有落檔之來源，改之將破 `67cd1a4` 落檔之可復現性）。

🔒 **鑄 `GB-157`**（授權見 `§六`）。其受詞逐字為：
> **既有探針之內部模型與生產碼之對應量為不同之量，其自我驗證閘於現態不過；凡引該探針所出之數者，須先於現態重得。**
> 具體：`probe_WG985_grouphead.py` 之原錨下限取 `w40.eval_lot(...)` 之 `st[0]`（前一宗遠側界之 **FRONTLINE 截距**），生產碼之錨為 `corner_pt + cum_S·d̂`（起算點 ＋ **標稱 `S`**）。於 `67cd1a4` 二者效果相合（`2 ＝ 2`／`2 ＝ 2`），於 `bf1d92f` 已不合（`R2左j0`：現況 `2` 對／原錨重建 `1` 對）。
> 🔒 **現況 `2` 對之獨立佐證**：`probe_WG9265_coverage_account.py`（**不同口徑·不同來源**）於 `0m·R2·left` 亦得恰 `2` 對（`19.8142`／`26.1624`）⇒ 不符者係**重建**，⛔ 現況之量測。
> 🩸 **併記（器之判準之界）**：該器之判式為 `n_new == 0 and n_old > 0`，**⛔ 檢 `n_old == n_now`** ⇒ 其於 `R2左j0` 仍印 `✅`。**器之綠⛔ 蘊含自我驗證閘綠。**
> 🔒 **全倉波及面**：凡引 `W-G.9-85` `A-4` 之數者，逐處具名並標「**已停用·須於現態重得**」。其掃描母體 ＝ 全 `docs/` ＋ `verify/`，掃描結果逐處出艙（**含命中 `0` 之情形**）。

---

## §三　題 `C` 之裁：既有預測與量級估計**一併作廢**，新靶由 `c0-5` 重得

**作廢（逐項具名）**
1. 原單 `§零-2` 之「🔒 既有之可證偽預測」整段——**⛔ 為本批之驗收靶**。
2. 原單 `§四` 之「`0m·R2` 之錨位移約 `7.2` 公尺（`0.2163` → `S_req 7.4487`）」——其減數 `0.2163` 之受詞為「`K-9-9 二` 下限」而非錨之位置（`§一-1`）⇒ **該量級⛔ 有據**。
3. 🔒 **⛔ 作廢者（照實併記）**：`S_req` 本身**未動**——`0m R2 j=0` 之 `S_req ＝ 7.4487` 於 `67cd1a4` 與 `bf1d92f` 二落檔**逐位相同**；`宗0` 之 `s` 域 `[0.2677, 7.4487]` 亦同。作廢者係**減數**與**由其導出之差**，⛔ 被減數。

**新靶之來源** ＝ `c0-5`（`§四`）。在 `c0-5` 出艙之前，`c1` **⛔ 開工**。

---

## §四　新增工項 `c0-5`（**零生產碼·得逕推**·置於 `c0` 之後、`c1` 之前）

**器** ＝ 新建 `verify/probes/probe_WG9268p_anchor.py`（唯讀·⛔ 動生產碼一字）。

### 四-1　出艙（逐 `(情境, 街廓, 側, j)` 一列·母體基數同格出艙）

| 欄 | 內容 |
|---|---|
| `s_錨_現行` | 現行錨之 `s` ＝ 前一宗起算點 ＋ 其標稱 `S`（`S_raw`）|
| `s_錨_幾何` | 幾何終點之 `s` ＝ 前一宗遠側界**離開街廓**之點（＝ `S_req`）|
| `δ` | `s_錨_幾何 − s_錨_現行`（**帶號**）|
| `現況重疊` | 該組之重疊**對數** ⋀ **逐對面積** |
| `新錨後重疊` | 以 `s_錨_幾何` 重建後之重疊**對數** ⋀ **逐對面積** |

### 四-2　🛑 自我驗證閘（**硬閘·不過即 loud 拒測並停機**·⛔ 據以下任何結論）

> **以<u>現行錨</u>重建之重疊，其<u>對數</u>與<u>逐對面積</u>須與 `S-6` 覆蓋帳之實測逐位相符。**
> 已知之靶：`0m·R2·left` ＝ **`2` 對**（`19.8142`／`26.1624`）；`3.5m·R5·left` ＝ **`1` 對**（`56.8186`）；其餘可達組 ＝ **`0` 對**。
> 🛑 此即 `probe_WG985_grouphead.py` 所不過之閘（`n_old == n_now`）——**新器之存在理由即在過此閘**。

### 四-3　兩造對照（自證⛔ 恆綠亦⛔ 恆紅）

| 造 | 構造 | 須得 |
|---|---|---|
| 陽性 | 人造將錨往**回**移 `Δ > 0` | 重疊**增** |
| 陰性 | 錨移至 `s_錨_幾何` | 重疊**減**（靶：全數為 `0`）|
| 判別力 | 二造同值 ⇒ 器無鑑別力 ⇒ 🛑 停機 |

### 四-4　`c0-5` 之出艙即為**新靶**

> **新靶** ＝ 「以幾何終點為錨時，**全部可達組之重疊對數 ⋀ 逐對面積皆為 `0`**」。

🛑 **若 `c0-5` 顯示新錨後仍有任一組之重疊 `> 0`** ⇒ **停機上呈**（其意謂錨⛔ 為破量之唯一成因，方法須重新對位）。
🔒 **`δ` 之逐格值即原單 `§四` 差異表量級之來源**——`c0-5` 出艙後，量級始得填。

### 四-5　`c0-5` 併辦

`§一-1` 之 `GB-67` 末端追加更正註（同一 `commit`）；`§二` 之全倉波及面掃描（逐處具名·含命中 `0`）。

---

## §五　題 `D` 之裁 ＋ `§八` 停機款第 `3` 款之更正

**題 `D`（`probe_order_preflight.py` 之 `P-5` 受詞解析）**：核可 `-268pR §四` 自解 `2` 之處置。純程序、零土地後果。施工窗沿用，⛔ 再自解。

**原單 `§八` 第 `3` 款（逐字保留於單內·⛔ 追改）**
> 「`c0-3` 之 shim 二態同值，或原錨態⛔ 復現 `45.9766`／`56.3293`」

🔴 **其瑕**：後肢之受詞（**面積值**）繫於該器結構上**⛔ 出艙**之量（其只出**對數**）⇒ 該肢無從驗。

**更正後之停機款（取代第 `3` 款）**
> **`3′`　`c0-5` 之自我驗證閘（`§四-2`）不過；或其兩造對照同值（`§四-3`）；或新錨後仍有任一組之重疊 `> 0`（`§四-4`）。**

---

## §六　鑄號授權之增列

🔒 **既授權（維持）**：`自誤 334`／`335`（原單 `§七`）／`自誤 336`／`337`（補令一 `§四`）。
🔒 **本補令增列**：**`GB-157`**（`§二`）／**`自誤 338`**／**`自誤 339`**。**合計七號；⛔ 逾此。**
（次號當場現查。`GB` 之「相異／缺號集」依 `W-G.9-205` 更正註⛔ 出艙，只對拍 MAX。）

**`自誤 338`**（責 ＝ **發單側**）　**立停機款時，其受詞繫於該器結構上⛔ 出艙之量。**
形：原單 `§八` 第 `3` 款之後肢令「原錨態⛔ 復現 `45.9766`／`56.3293`」，而 `c0-3` 所指定之器（`probe_WG985_grouphead.py`）之出艙**只有對數、⛔ 面積** ⇒ 該肢**結構上不可判**；施工窗須自行構造「肢二′（對數）」方能作成判斷。
通則：**立停機款（或任何閘）之前，須先查其所繫之器之<u>出艙欄位集</u>；受詞須落於該集之內。受詞不在集內者，其判為「無從驗」，⛔ 與「驗不過」共用出艙碼。**

**`自誤 339`**（責 ＝ **發單側**·🔴 **已出艙予 KL**）　**引一數為土地量級而未查該數之受詞。**
形：以「`0m·R2` 錨位移約 `7.2` 公尺（`0.2163` → `7.4487`）」出艙予 KL，其減數 `0.2163` 於倉內之受詞係「**CC 所算之『`K-9-9 二` 下限』**」（≈ `宗0` 之 `s` **下**界 `0.2677`），**⛔ 錨之 `s` 位置**；`GB-67` 加註將二者併為一事，發單側逕引之而未回查該值之受詞。
🔒 **與 `335` 之別**：`335` 係**未讀成因鏈**（漏讀一節）；本則係**讀了、但未查其中一個數之受詞**（讀了而未究）。
🔒 **後果**：該量級已出艙予 KL 且已由發單側自行撤回（見 `§三` 之作廢二）。
通則：**凡出艙一數為土地量級，其<u>每一個組成項</u>（被減數、減數、比之分母）皆須各自查其倉內受詞並具名；⛔ 因其同列於一表而推定同受詞。**

🛑 逾此七號者 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §七　`c1` 之依據改繫（取代原單 `c1` 之「語意逐字沿用 `probe_WG985_grouphead.py` `A-4` 之聲明」一句）

`c1` 之設計依據 ＝ **`c0-1`（丙）之實測語意** ＋ **`c0-5` 之 `s_錨_幾何` 定式與其自我驗證閘**。
🔒 **保留原單 `c1` 之其餘全部逐字**：只改錨點與沿 `d̂` 平移一條界線之位置；**⛔ 改面積目標**、**⛔ 解 `S`**（⛔ 呼叫 `solve_G_binary`）；`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 一字不動；`expand` 並存段、旗標預設 `off`、`off` 態逐位不變、`on` 態須相異。
🔒 **二檔須同改**（`c0-2` 已坐實錨之二列於二檔逐字相同）。

---

## §八　續辦

`c0-5` → （出艙·得逕推）→ `c1`〜`c3`（逐 `commit` 須 KL 放行）→ `c4`。
本補令由施工窗於續辦前**原封入倉**（`docs/orders/W-G.9-268p_補令二_受詞精確化與新靶重得.md`），⛔ 動一字。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
