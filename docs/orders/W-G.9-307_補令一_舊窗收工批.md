# `W-G.9-307`　補令一（**舊窗收工批**：態之判 ／ 交接註 ／ 收工 ⇒ **停 ⇒ 本窗封存**）

> **受詞** ＝ 現施工窗（**辦畢即封存·⛔ 再用**）。
> **主線** ＝ `e2d26b1ac6c15391d6dc4001811184754d6f6d52` **或其後裔**（🛑 `ls-remote` 實查·**全 `40` 碼**）。
> **分級** ＝ **輕**（交接註一檔·**⛔ 長跑**·**零生產碼**·**⛔ 開分支**·**⛔ 鑄任何號**）。
> 🔴 **換窗之由**：KL 告知本施工窗 context 已達 **`75%`**。次批 `W-G.9-308` 含 `run_verification` 全量一次、二探針與大量逐宗落檔，其後之 `W-G.9-309` **直接影響配地面積幾何** ⇒ **⛔ 於本窗起辦**（前例同形：`W-G.9-299` 補令一·`79%`；`W-G.9-280` 補令一·`82%`）。
> 🛑 **`W-G.9-308` 之任何版本，本窗⛔ 辦**；其現行版由發單側另於新窗重擬後交付。
> 🛑 **辦畢 ⇒ 停 ⇒ 本窗封存。**

---

## `§零`　開工閘（**⛔ 略**·**⛔ 驅動任何長跑**·每列同格載其產生指令）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `e2d26b1ac6c15391d6dc4001811184754d6f6d52` **或其後裔**（全 `40` 碼） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` 緊接指令·先 `git cat-file -t` 證其存在） |
| `4` | `python verify/probes/probe_order_preflight.py <本補令>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 項逐項具名處置·⛔ 靜默略過 |
| `4'` | 受詞自證（以器所印之檔名列） | [必真] 含 `補令一` ＝ `True`／[必偽] 含 `W-G.9-308` ＝ `False` |
| `5` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `406`／`416`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `29`／`30` |
| `6` | `git status --porcelain` ／ `git rev-list --left-right --count origin/wip/s1-endpart...HEAD` | 空 ／ `0 0` |

---

## `§一`　態之判（🛑 **自倉現查·⛔ 憑記憶**）

框 ＝ `git log --format='%H %s' e2d26b1ac6c15391d6dc4001811184754d6f6d52..HEAD`，逐列出艙全 `40` 碼與題；另 `git ls-tree -r --name-only -z HEAD docs/orders/` ⋂ 檔名前綴 `W-G.9-308`（**取 bytes**·逐名出艙）。

| 態 | 現況 | **本補令所令** |
|---|---|---|
| **甲** | 上框皆空 | 逕辦 `§二` |
| **乙** | 上框任一非空 | **⛔ 續辦任何 `W-G.9-308` 之工項**；其 `commit` 照實留倉、於交接註**逐項具名**（含已辦之工項與未辦之工項）；辦 `§二` |

🔴 **任一態下**，若 `docs/orders/` 內見檔名含 `K-9-30依據之更正` 之 `W-G.9-308`（**發單側已作廢之版**）⇒ **loud 具名、⛔ 動之、於交接註首列載明**。

---

## `§二`　交接註（**新檔入倉** ＝ `docs/reports/W-G.9-307_交接註_舊窗收工.md`·單獨 `commit`·`open(p,'wb')`·`CR` `0`）

**每列同格載其產生指令**·**⛔ 轉引本補令之數**·逐項**自跑當場重得**：

| # | 受詞 |
|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart`（**全 `40` 碼**·`push` 前之值） |
| `2` | `git rev-parse HEAD:app.py` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` ⇒ `rc` |
| `4` | `probe_WG9267_issuer_measurers.py registry` ⇒ 四簿（**含缺號**） |
| `5` | 生產碼母體 ＝ `34` 檔（**正面列舉**）＋ 子層 `*.py` 檔數（須 `> 0`·⛔ 定值） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum` ＋ 檔數 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` ⇒ 該族之數·`W-G.9-299-gb170` 命中·`p3a` 命中 |
| `8` | `§一` 之態與其框之全量輸出 |
| `9` | **`W-G.9-307` 之全部 `commit`**（`git log --format='%H %s' da9ca62eb2d007d13c08eaa9a066031228932e8a..HEAD`·全 `40` 碼·題逐字） |
| `10` | **本窗之環境事實**（逐項具名其出處·⛔ 判）：`sys.version`；`git config core.autocrlf` 之值；子程序 `stdout` 之換行轉譯（`W-G.9-307R §六` 款 `1` 之 `CR` 正規化）；`ast` 之 `col_offset` 為位元組偏移；`id(·)` 快取之失效（`W-G.9-307R` 自捕） |
| `11` | **本窗工作區內未入倉之物**：`git status --porcelain --ignored` 之全量（含 `verify/out/got_*`），逐項具名其生成之批；**⛔ 刪、⛔ 入倉** |
| `12` | **本窗於倉外所為之寫入**（如受單側自身之記憶檔）：逐項具名其路徑與要旨（**照實**·無則書「無」） |
| `13` | **本窗自捕之全部**（`W-G.9-307R` 所載 `4` 則 ＋ 其後所增·逐則：形／徵候／根因／攔法·⛔ 略） |

🛑 交接註**⛔ 載任何判**；交接註內之數**⛔ 冠「已交付」「已核可」之詞**。
🛑 交接註內**凡以 `#` 起首之列⛔ 含「坑」字**（坑索引器之 `D-H` 形·避免誤計為坑之定義處）。

---

## `§三`　收工閘（**交接註入倉後量**·嚴格末端追加於報告回文·⛔ 追改交接註）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <本批各 commit>` 逐檔具名 | 刪除欄**全 `0`**；檔數 ＝ `1`（交接註） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 本批區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | 交接註之 `CR`（倉內 blob·取 bytes） | `0`；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末（器之輸出） | 與 `§零` 閘 `5` **逐字相同** |
| `5` | `python verify/probes/probe_WG9269_pit_index.py <rev 釘死>`（全量落檔） | **`94`／`94`**·末 **`bp`**；母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 數 |
| `6` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 `20` 支；`gb170` 命中 **`0`**；`p3a` 命中 **`1`** |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§四`　🛑 **⛔ 為之事**

1. **⛔ 辦 `W-G.9-308` 之任何版本、任何工項**；**⛔ 驅動任何長跑**（`run_verification`、任何管線驅動）。**本窗已近飽和。**
2. ⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支（含 `verify/W-G.9-299-gb170`）；⛔ 動用 KL 之放行。
3. ⛔ 鑄任何號（自誤／`GB`／`VR`／`K-9`／坑）；`ALPHA_LABELS` ⛔ 動一字。
4. ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或任一物證落檔；⛔ `WV_BAKE`。
5. ⛔ 追改任何已入倉文件一字。
6. ⛔ 判任何域事、⛔ 提修法主張、⛔ 呈 KL。
7. ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述）。

---

## `§五`　報告之格

`§零` 開工閘 ⇒ `§一` 態之判（含框之全量輸出）⇒ `§二` 交接註之落點與逐位（blob `sha1`／內容 `sha256`／`CR`）⇒ `§三` 收工閘 ⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`merge-base`／`numstat`／`deletions`／`sha256`／`ls-tree`／`porcelain`／`--ignored`／`col_offset`／`autocrlf`／`WV_BAKE`／`p3a`。

【單止】本補令受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**
**辦畢 ⇒ 停 ⇒ 本窗封存。**

SELF_SHA256: be4fc16456b42a2d7239f0c022a9c1ec64f715834fc511592099f59d547f075d
