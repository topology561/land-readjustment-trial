# 交接文_W-G.9-262（發單側 · `claude.ai` 窗轉換）

> **受詞 ＝ 下一個 `claude.ai` 窗**，非 CC。
> CC 收此文之處置 ＝ **原封入倉 `docs/orders/`，⛔ 執行其所列之任何工項**。

---

## §一　態錨（`2026-09-09`）

```
origin/wip/s1-endpart = e9d1b9c3e6e46580da28392e1ed7495d5b34638f（-261 已快轉併線）
追蹤 1711 檔 ／ .md 634 ／ docs/ 612 ／ 遠端 heads 17
```

**🔴 二受詞之錨已於本波變更**（`W-G.9-261` 生產碼 `023765c`）：

| 檔 | sha256 前 16 | 備註 |
|---|---|---|
| `app.py` | `1e15d45ae84b02ca` | **舊值 `4860f10828fb8ae9` 已失效** |
| `verify/stepg_pipeline.py` | `cb0bbaaa0f1eb8bc` | **舊值 `c8d46d2e5fb79dc5` 已失效** |
| `verify/wf_f0.py` | `b0722355c1978635` | 不變 |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | 不變 |
| `verify/run_verification.py` | `d0d531f7dc051b65` | 不變 |
| `verify/run_all.py` | `4ba89fef90979491` | 不變 |
| `verify/wf_f1..f4.py` | `30e19048dfd781bc`／`f974724d7e694ad7`／`226c7c5fa00464f3`／`b36aeca6c4f6b364` | 不變 |
| `verify/fixture_wf_ns_wiring.py`（⛔ 屬十檔） | `b17826a6f7b98bf5` | 不變 |

簿冊：自誤 相異 `314`／`MAX 321`／`MIN 7`／缺號 `[106]`／恆等式 `True`（框 ＝ 定義框 ∪ 範圍框展開·`8` 條攢批標題·母體 ＝ 登記表單檔）。
`GB MAX 154` ／ `K-9 MAX 29` ／ `CLAUDE.md` 之 `⬜` ＝ `69`。
號占用（宣告框 `①`–`⑦`·母體全 `docs/` `612` 檔·讀不到 `19` ＝ `jpg 9`＋`png 10`）：`261` `D1 2/D2 2/D3 0`（已辦畢）／**`262` `0/0/0`**（留予本文）／`263` `0/0/0`。

**⛔ 採信上表任一值——接手窗須當場 `clone` 並以 `git cat-file blob` 復現。**

---

## §二　接手窗必須先知道的六件事

**一　`claude.ai` 側可直接讀倉，亦可實跑 harness。**
```
git -c core.autocrlf=false -c core.quotepath=false clone https://github.com/topology561/land-readjustment-trial.git
cd land-readjustment-trial && git checkout wip/s1-endpart
pip install ezdxf shapely plotly streamlit pandas numpy openpyxl --break-system-packages
python3 verify/run_verification.py          # 約 4 分鐘·現態 RESULT: FAIL·紅 24
```
落檔在 `verify/out/got_G值_退縮{0m,3.5m}_partial.csv`——**逐宗 `Rw`／`G`／`宗地寬度`／`驗_總判` 皆在其中**，多數問題⛔ 需注入即可判。本波三題（`Q-1`／`Q-3`／碎片成因）皆由該 CSV 就地閉合。

**二　🔴「紅 `32`」是前前窗之誤值，真值 `24`。**（`自誤 321`·已入簿）
本窗於 `8a52dbd`／`a5b4edd`／`1531916`／閘關／`023765c` **五態實跑皆得 `24`**。
⇒ **凡出艙一個計數，須同時出艙其計數式與第二態之同式結果。** `grep -c "^🔴 FAIL"` 得 `0`（該類列有縮排）——**框之別、⛔ 態之別**。

**三　⛔ 沿用任何前批之行號。** 本窗即因此誤判過禁區（以固定行號比對，未計 `stepg` 插入 `6` 列之位移）。一律以 **hunk 範圍**或**內容錨**判。

**四　🔴 本窗發單側之三則自誤<u>尚未鑄號</u>**（`322`／`323`／`324`·純登記·宜攢批）：
| 擬號 | 受詞 |
|---|---|
| `322` | 在窮盡倉內搜索前，即向 KL 宣告「這一句才需要你裁」——該題 KL 已於 `2026-08-12` 裁畢（`K-9-5-12（四）`）。**再問已答之題**係登記在案之錯誤型 |
| `323` | `A-3` 之基準值出艙時未套 `F>0` 濾（＝閘之母體定義），致「`R2 right` 鏈頭為否」實為「**無鏈頭**」 |
| `324` | `V-1` 綁在**代理量**（其餘各宗之值恆等）而非其所欲保證之**性質**（無重新分配）⇒ 閘因發單側之框錯而未達 |

**五　CC 本波表現可靠，且三度／二度器紅皆<u>入倉前自捕</u>。**
`-260`：`quotepath`×CJK×Windows `basename` 致檔名比對靜默歸零／clone 之 CRLF 使注入錨命中 `0`／`V-S` 首跑實得 `6` 列而其已書「命中 ＝ `0`」。
`-261`：巢狀函式內 `eval` 讀不到外層 locals（探針全 `None`）／`_b_L0` 退化分支殘值。
**`V-1` 未達時 CC ⛔ 自行放寬判準而呈上**——程序正確。其自報仍⛔ 採信，但其停機每次皆成立。

**六　⛔ 靠 KL 或 CC 當偵測器。** 出艙前自檢須含：①單所書之框與自己產數之框是否同一；②所立之驗收路徑是否**實際執行過一次**；③所立之閘是否綁在**性質**而非代理量。

---

## §三　本波之裁與其落地

**KL 裁 `2026-09-09`（逐字）**：「所以第2宗起點那個錯，要修正」「而碎片產生的問題已釐清」。
**KL 之複述（逐字·域語言）**：「第2宗的臨側街負擔Rw＝RW(8)−RW(3.5)……而至於街角第1宗強制抵費地的Rw，免計，由該區調配池吸收(調配池減少)」。

**所依之既有裁**（⛔ 新裁）：
- `K-9-5-12（四）` KL `2026-08-12`：無 WINNER 而街角為強制抵費地時，**第 2 宗**之起算為該抵費地之**遠側境界線**；抵費地本身不計 `G`、不負擔。
- `K-9-5-12（五）1`：首宗起點一律為 `0`。
- `K-9-5-13` 裁定二 KL `2026-08-12`：起算一律用真正的地界，面積換算出來的替身線在側街負擔上不再有角色。🔒 **已定案**。

**已落地（`023765c`·KL 已放行·已併線）**：`_rw_start = _W_near if (is_corner or is_chain_head) else W_prev`（`app.py` 唯一決定點 ＋ 四處薄殼 ＋ 六處呼叫端）。`is_chain_head` 預設 `False` ⇒ 未傳之三處逐位不變。

🔒 **授權之界（KL 裁 `2026-09-06`）**：純程式結構（射程、母體、粒度、框、工序、批之切分、量測器設計、seam 之介面形）得由發單側自裁，判準 ＝ **改之不動任何一宗地之配地結果**；改變準據本身者恆須 KL。
🔒 **呈 KL 之形**：一句實務語言；**KL 放行的是那句話、⛔ 那段程式碼**。KL 自陳不懂程式。

---

## §四　現況：**無單在飛**

| 單 | 狀態 |
|---|---|
| `-260`（含補令一）| ✅ 辦畢 |
| `-261` | ✅ 辦畢·KL 已放行·已快轉併線 |
| `.svg`（`R2左側街角第1宗_推進超跑_幾何示意.svg`）| ✅ **結案**——KL 裁「無須入倉，我會手動刪除檔案」（`2026-09-09`）。**⛔ 於後續輪次再提** |
| `V-1` | ✅ 判「未達」**定讞·⛔ 回改**。替代閘 `V-1′` **待立**（形：該側除鏈頭外 `S` 與宗地寬度逐宗不變 ＋ `ΣG ＋ 池 ＝ 街廓`）|

**CC 停機中，候發單側。**

---

## §五　`R2` 之結案與新射程

### 已結案

| 題 | 判 |
|---|---|
| `3.5m` telescoping `Δ=55.606` | **＝ `R(W₀=6.98)`**。鏈頭 `628-42(1)` 自 `W=0` 起累而非自抵費地遠側界。**已修** |
| `0m` telescoping `Δ=6.160` | ＝ `628-41(1)(W=4.06)→628-42(1)(W=3.11)` 之 **`W` 倒退**被 `rw_increment` 夾為 `0`、該段重複收費。`R(4.06)−R(3.11)=6.157`。**未修** |
| `②-宗` 停用之相異 | **`0`（恆等·⛔ 上界）**——同容器閘開／閘關皆紅 `24`、名稱差集雙向皆空 |
| `_rw_real_wd2` vs `_chain` 二母體 | **全等**（`A∖B ＝ ∅`）。🔒 `B∖A ＝ ∅` 係構造必然、**無證據力** |
| `628-42(1)` 碎片之成因 | **其重劃前 `a ＝ 8.34㎡` 本身即小**，⛔ `GB-67` 之產物。`GB-67` 那 `45.98㎡` 係 `628-41(1)` 疊到 `628-27(1)`（`19.81`）與 `628-40(1)+`（`26.16`）——**`628-42(1)` 一點未被疊到** |
| `W₀ ＝ 6.98` 之身分 | **確為強制抵費地之遠側境界線**（`_b_L0 ＝ 6.980480447374`·`T2-DIAG` 池帶 `0.2677+8.4283=8.6960=buf` 佐證），⛔ 巧合等值 |

### `-261` 之土地後果（發單側於 `023765c` 獨立實跑復現）

`3.5m`·`R2 left`：`628-42(1)` `Rw` `55.59% → 0.58%`／`G` `0 → 4.77㎡`／宗地寬度 `0 → 0.11m`（**仍判剔除**·`4.77 ≪ 3.5×14=49`）。
同側其餘六宗**整排內移 `0.11m`**，面積微調（最大 `+0.19㎡`）——**⛔「完全不動」**。
`R2` 中央調配池 `1471.26 → 1466.26㎡`（`−5.00`）；街角強制抵費地 `308.17㎡` 不變；`ΔΣG ＝ +5.02`／`ΔΣ池 ＝ −5.00`；守恆成立。
🔑 **此係正確之<u>中間態</u>**：`628-42(1)` 仍列移出／第 1 調配順位，五級調配落地後將整宗移出 `R2`，那六宗會再挪回。

### 🆕 新射程（**本波首次可達·⛔ 迴歸**）

`3.5m` 落檔 `24 → 47` 列，中止街廓 `R2 → R5`，跑到之街廓 ＝ `R1 R2 R4 R5 R6`。
- **`②-宗 圍堵閘破[R5]：56.8186`** ← `GB-67` 之既有現象，於新街廓現形
- `0m` 仍止於 `R2`（`②-宗 45.9766`·落檔 `21` 列·改前改後逐位相同）
- `run_all` 紅 `24 → 24`、名稱差集雙向皆空 ⇒ 🔴 **名目層對本改動全盲**（`v3·StepG3.5m` 名目不變而其中止閘已由 `telescoping@R2` 換為 `②-宗@R5`）——`W-G.9-7` 所立「**同名目換新破損**」之活體實例。**接手窗⛔ 以名目層之恆定推斷無事**

### 🔴 CC 於 `-261` 捕獲之**未登記**缺陷

> `3.5m`·`R4 left` 之 `_b_L0` 與 `R2` **逐位相同**（`6.98048044737404`）而其 `buf` 相異
> ⇒ **`_b_L0` 於退化分支未重算、殘留前一街廓之值。**

**尚未鑄 `GB` 號、尚未定射程。** 接手窗宜列為早期工項——`R4` 左角正是 `226.01㎡` 抵費地之所在（`c2` 之受詞）。

---

## §六　待落地（**接手窗須自倉重生此表·⛔ 沿用**）

| 項 | 狀態 | 依賴 |
|---|---|---|
| `自誤 322`／`323`／`324` 鑄號落簿 | ⬜ | 純登記·宜與他項攢批 |
| `V-1′` 之立 | ⬜ | 下一單 |
| `_b_L0` 退化分支殘值 | ⬜ | **未鑄號**·須先定射程 |
| `②-宗@R5`（`56.8186`）| ⬜ | `GB-67` 同族 |
| `GB-67` 之**修**（`0m`·`R2` `45.9766`）| ⬜ | KL 前裁丙案（只定位）。**改甲案須 KL 另裁**，且動配地幾何⇒宜新窗 |
| `0m` telescoping（`W` 倒退 `6.157`）| ⬜ | 推定與 `GB-67` **共根**（皆源於 `628-41(1)` 排第一）·⛔ 已證 |
| `c2`（廢步驟 0·`R4` `226.01㎡`）| ⬜ ⛔ 開工 | 候 `GB-67` 可修 |
| `-249` 段三觀測（`V-S` 表）| ⬜ | 須俟 `c2` 併線後重跑（`K-9-29 九`）|
| `-253` 段乙（`-251` UI）| ⬜ | 側分支 `verify/W-G.9-251-c1` **仍不存在** ⇒ 未施工·`V-K` 候 KL 目檢 |
| `K-9-29 六` 入池閘之五級層／`a′` 模式二 | ⬜ | 五級未起 |
| `K-6-B` 四階段街角分配／`K-6-A2` 寬深雙檢 | ⬜ | **裁定已入倉·生產碼未變·勿誤以為已生效** |
| 五級實作（七級調配）| ⬜ | 🔒 KL 已裁「五級實作**得以替代證據放行**，⛔ 俟 `GB-67`／telescoping 解除」（`W-G.4:4723`）⇒ **⛔ 被此二病扣住** |
| 區外道路／**通用化波** | ⬜ | `W-G.4` 依賴序 `3′ → 3″ → 4` |

---

## §七　未答之題（⛔ 待 KL）

**無。** 本波所提之題（第 2 宗起點是否要修、碎片成因、`.svg` 之處置、`023765c` 之放行）KL 已全數答畢。

接手窗第一次需要 KL 之預期時點：
- **`GB-67` 是否由丙案（只定位）改甲案（開修）** ——⚠️ 該裁動配地幾何 ⇒ ⛔ 在飽和窗做。
- `_b_L0` 殘值若證實動 `R4` 之 `226.01㎡` ⇒ 差異表呈 KL。
- `-251` 段乙併線前之 `V-K` 目檢。

---

## §八　接手窗之建議首四步

1. **clone 並復現 `§一` 全部態錨**（⛔ 採信本文任一值·`core.quotepath=false`）。**特別注意二受詞之錨已變。**
2. **實跑一次 `run_verification.py`**，記 `RESULT`／紅項數**與其計數式**，並讀 `verify/out/*.csv` 之逐宗欄。
3. **⛔ 以 `run_all` 名目層之恆定推斷無事**——`§五` 已證其對本類改動全盲。
4. **⛔ 逕自決定 `GB-67` 開修**；`.svg` 一項**已結案**，⛔ 再提。

---

*本文受詞 ＝ 下一個 `claude.ai` 窗。CC 之處置 ＝ 原封入倉，⛔ 執行。*

SELF_SHA256: d9a832565952b99b67570be3bd6d0167c8094c92808804f4d4cd3722a1f102ea
