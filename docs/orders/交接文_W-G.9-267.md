# 交接文_W-G.9-267（發單側 · `claude.ai` 窗轉換）

> **受詞 ＝ 下一個 `claude.ai` 窗**，非 CC。CC 之處置 ＝ 原封入倉 `docs/orders/`，⛔ 執行其所列任何工項。
> 🔒 本文**只指路、⛔ 轉述數字**。凡本文提及之數，一律以倉內檔為準；本文與倉相異，**倉勝**。

## §一　態錨（`2026-09-10`）

出單時態 ＝ `3b44a34d308ff046c0099dc098cd56bb5bd0a89d`（`W-G.9-266` 推後）。接手時 `-267` 應已推畢（`工項零` 四檔 ＋ 七工項），接手窗第一件事 ＝ **clone、實跑 `verify/probes/probe_WG9267_issuer_measurers.py registry` 與 `occupancy 267 268`、復驗 `-267R`**。
⛔ 採信本文或 `-267R` 任一值。三簿之正典框：`VR-091 補款二 ①`／`VR-092`（含就地加註）／`VR-093`（含就地加註）；`D3` 用嚴格式（`-267 §五 U-1`）。

## §二　接手窗必須先知道的七件事

1. **工作流（`2026-09-09` 起·KL 定）**：CC **審查窗**擬草稿 → KL 貼給 `claude.ai` → `claude.ai` 審定出單 → **同一審查窗只辦 `工項零`**（原封入倉·推·停）→ **施工窗**辦其餘工項 → KL 貼 `-NR` 給 `claude.ai` 復驗。審查窗與施工窗**分立**；新單之審查窗須為**新 session**（⛔ 查自己的產物）。
2. **自解款 ＋ 開工閘三級**已入 `CLAUDE.md`（作業常規之追加五·`-265`）。純登記批 ＝ 輕級（⛔ 跑 harness）；純診斷 ＝ 中級（全批跑一次）；動生產碼／幾何 ＝ 重級。
3. **KL 今日三裁（逐字）**：「`GB-67` 開」「`GB-101` 可」「甲」。其登記 ＝ `-267 §二`。`GB-67` 之落地 ＝ **`W-G.9-268`**：新 `claude.ai` 窗（即接手窗）＋ 新 CC 窗、側分支、逐 commit KL 放行、併線前呈差異表。**方法⛔ 新裁**（`K-9-23` 二閘轉主動 ＋ `K-9-17` 遞補 ＋ 第 1 宗境界線不動）。
4. **`R3` 可達**：逐街廓獨立呼叫判為合法之**診斷**取值途徑（`-267 §五 U-2`·`VR-095`），其值標「單源」。幾何批得以六街廓驗。
5. **發單側本窗之自誤 `325`〜`332`**（八則）皆同一族：**指定框／對照組／母體時未驗其性質、器不在倉**。接手窗凡出艙一數，須：框之來源具名、對照組載判定集基數且陰性須已證未動、器之逐字入倉。
6. **配地程序全貌**（KL `2026-09-10` 索閱·發單側已交付）：準據層幾乎全裁完，碼面停在第四段（不交叉 ＝ `GB-67`）；段三（`RD` 片併入＋重算 `G`＋遞補）／藍影閘／矩形容納／五級為**同一條鏈**，皆「裁了·骨架在·觀測模式」。倉內正本 ＝ `CLAUDE.md`「待落地清單之更正五」表（三態）＋ `K-6 §四` 落地表。
7. **候 KL 者只餘**：九份 baseline 重產之時點（發單側建議俟 `-268` 併線後）。**⛔ 再問已裁之題**（`GB-67` 方法、`GB-101`、`K-9-9`〜`K-9-29` 皆已裁）。

## §三　`-268` 幾何影響批之界（發單側所定·接手窗得改·改須具名）

- 受詞 ＝ `GB-67`：`K-9-23` 二閘由觀測轉主動 ＋ `K-9-17` 遞補迴圈；**第 1 宗境界線不動**。
- 開工閘 ＝ **重級**（完整儀式·二態對拍·`run_all` 名目差集·逐 commit 放行）。
- 差異表之形 ＝ 逐宗 `ΔG`／`Δ宗地寬度`／`Δ位置`，以域語言呈 KL（面積／街角／抵費地／地主），⛔ 程式術語。
- 驗於六街廓：五街廓走口徑甲，`R3` 走口徑乙（標單源）。
- 併線前之必答：覆蓋帳 `13`＋`R3` 組全部歸零否；`V-1′`（`VR-094`＋`VR-095` 之三組對照）；`run_all` 名目差集。
- 🛑 `K-9-12` 矩形容納轉主動、五級、`K-9-10`、`K-9-1`、`c2` **⛔ 併入 `-268`**——各為其後之獨立批。

## §四　接手窗之首四步

1. clone，實跑 `probe_WG9267_issuer_measurers.py` 三模式，與 `-267R` 對拍（此即歸因之活體）。
2. 復驗 `-267R`：`V-a`〜`V-l` 逐閘自倉復現，⛔ 採信自報。
3. 開 **`-268` 審查窗**（新 CC session），開場文形同 `-267`（受詞換為 `GB-67` 幾何批·級改重級·加「側分支、⛔ 推主線、逐 commit 呈 KL」）。
4. ⛔ 逕出 `-268` 單——須經審查窗草稿。

## §五　未答之題（⛔ 待 KL）

**無。**

*本文受詞 ＝ 下一個 `claude.ai` 窗。CC 之處置 ＝ 原封入倉，⛔ 執行。*
