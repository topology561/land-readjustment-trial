# `W-G.9-341`　重量單：`GB-182` 之修（幾何餘與池帶以邊相接者併入該池帶·一個界面一條線）＋ `GB-182` 之進度 ＋ `GB-186`／`GB-187`／`GB-188` 之立 ＋ `自誤 518`／`520`／`521` ＋ 量測器二支入倉

> **發單** ＝ 發單側窗三十六·`2026-09-25`。**受單** ＝ CC 新窗（CC 於 KL 之主 checkout 施工）。
> **級** ＝ **重**（生產碼 `1` 檔：`app.py`·僅函式 `_pool_strips_for_block`·⛔ 在 `def main()` 內）。**開工態** ＝ `09170db85f12af623c2b8832ac253794f25c6d9e`（主線 `wip/s1-endpart`）。
> **KL 放行** ＝ 見 `§二`（射程：生產碼 `commit` 推至**側支** `verify/W-G.9-341-gb182`；**入主線另候 KL 放行**）。
> 🔑 本單之一切呼叫形，其直譯器名一律寫 `python`（`自誤 466`）。塊 `P1`〜`P3` 一律以**檔案管線**、依**圍欄之逐列索引**機械抽取；`commit` 訊息一律 `git commit -F`（`恆常附款 h`）。含 emoji 之器先設 `PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1`。Windows 下 `<repo>` 一律以反斜線傳入（`W-G.9-333R` ⑦-1）。
> 🔑 **四來源檔**（KL 置於**主 checkout 之根**·檔名逐字）：`W-G.9-341_重量單.md`（本單）／`W-G.9-341_c1.diff`（檔 `F1`）／`probe_WG9341_poolpieces.py`（檔 `F2`）／`probe_WG9341_synth.py`（檔 `F3`）。CC 一律**二進位複製**、⛔ 讀入改寫。
> 🔑 **`run_all` 一律於倉外之拋棄式 worktree 跑**（`git worktree add --detach <倉外目錄> <commit>`；跑畢 `git worktree remove --force <倉外目錄>`）：`run_all` 改寫已追蹤之 `verify/out/probe_ruling_*.log` 並新生 `verify/out/E系列實測快照_退縮*.csv`（`W-G.9-333R` ⑦-5；發單側窗三十六實跑復見：改寫 `3` 檔、新生 `2` 檔）⇒ ⛔ 於施工樹跑之。
> 🛑 **本單⛔ 及於**：`K-9-29` 任一子項之碼側落地；段三之落地；入池閘；`GB-186`／`GB-187`／`GB-188` 之任何修；`GB-178`／`GB-182` 之解除；`R6` `85.71 ㎡` 之歸因；`verify/stepg_pipeline.py`、`verify/wf_f1.py`、`verify/wf_f4.py` 之任何修改；`probe_WG9338_gapcheck.py` 之修改；主線之生產碼。
> 🔴 **本單⛔ 令 CC 作任何「孰為正典」「應改為」之判**——出艙即止。

---

## `§零`　開場・取號・停機款・pre-flight・容量停

### `§零-0`　受單側之開場（**先於工項零**·⛔ 跳序）

1. 自報 context 餘量；預估不足以辦畢工項零〜五者，先辦至工項二止（見 `§零-4`）。
2. `git fetch origin`；`git rev-parse HEAD` ＝ `git rev-parse origin/wip/s1-endpart` ＝ 開工態；`git status --porcelain` 之追蹤檔變動 ＝ `0`（主 checkout 之根之四來源檔為未追蹤檔·⛔ 計）。
3. `python verify/probes/probe_WG9321_issuer_anchor.py <repo 絕對路徑> <輸出檔之絕對路徑（檔·非目錄）> 517 516` ⇒ `rc 0`，逐格對 `§一`。

### `§零-1`　取號現查（`e`／`e②`／`e③`／`z`／`aa`）

母體 ＝ 開工態全倉追蹤 **`2672`** 檔；內文側 ＝ 其中 `*.md`＋`*.py` **`1362`** 檔。框式（可原樣執行·逐列 `re.search`）：單號 ＝ ``W-G\.9-N(?![0-9])``；自誤號 ＝ ``自誤[\s　`]*`?N(?![0-9])``；裁號 ＝ ``K-9-N(?![0-9-])``；`GB` 號 ＝ ``GB-N(?![0-9])``；檔名側 ＝ 同式施於路徑字串（`core.quotepath=false`）。四欄 ＝ 檔名側／檔框／列框／子字串框（子字串框 ＝ 逐列 `re.findall` 之和）。發單側窗三十六以此框於開工態實跑得下表。

| 受詞 | 檔名側 | 檔框 | 列框 | 子字串框 | `e③` 之三分 | 判 |
|---|---|---|---|---|---|---|
| `W-G.9-341`（本單之號） | `0` | `0` | `0` | `0` | — | 🟢 可取 |
| `GB-186`、`GB-187`、`GB-188`（逐號） | `0` | `0` | `0` | `0` | — | 🟢 可取 |
| `自誤 518`、`520`、`521`（逐號） | `0` | `0` | `0` | `0` | — | 🟢 可取 |
| ⛔ 取 `自誤 519` | `0` | `1` | `1` | `1` | `(c)`：`docs/orders/W-G.9-338_重量單.md:35`（引為對照乙） | 🔴 內文非零 ⇒ 缺號 |
| ⛔ 取 `GB-183` | `0` | `1` | `1` | `1` | `(c)`：`docs/orders/W-G.9-338_重量單.md:35`（引為對照乙） | 🔴 內文非零 ⇒ 缺號 |
| ⛔ 取 `GB-184` | `0` | `2` | `2` | `2` | `(c)`：`docs/orders/W-G.9-332_輕量單.md:28`、`docs/reports/W-G.9-332R_CC交接文.md:88`（皆引為對照乙） | 🔴 內文非零 ⇒ 缺號 |
| ⛔ 取 `GB-185` | `0` | `1` | `1` | `1` | `(c)`：`docs/orders/W-G.9-333_重量單.md:34`（引為對照乙） | 🔴 內文非零 ⇒ 缺號 |
| 對照甲［必命中·同形族］`W-G.9-340` | `2` | `6` | `26` | `27` | — | 🟢 框非恆空 |
| 對照乙［必為零］`W-G.9-397`／`自誤 597`／`GB-197`／`K-9-57` | `0` | `0` | `0` | `0` | 定義域：路徑以 `docs/orders/W-G.9-N` 起首之號（`re.match`）＝ `130` 個·`MAX` `340`·⛔ 含 `397` | 🟢 ⛔ 占用 |

🔒 本單入倉後，上表各受詞之命中當含本單自身（白名單 `1`·⛔ 撞號）。本單⛔ 鑄裁號。

### `§零-2`　停機款（三值·`n`）

| # | 款 | 初態 |
|---|---|---|
| `1` | 開工時主線 ≠ `09170db`，或追蹤檔有變動（接續時依 `§零-4`） | 🛑 未執行 |
| `2` | 任一塊 `P1`〜`P3` 之 bytes 或 `sha256` 與 `§五-1` 不符 | 🛑 未執行 |
| `3` | 檔 `F1`〜`F3` 之 bytes 或 `sha256` 與 `§五-1` 不符；或 `F2`／`F3` 入倉後 `git cat-file blob` 與來源不逐位相同 | 🛑 未執行 |
| `4` | `git apply --check W-G.9-341_c1.diff` 失敗；或施後 `git diff --numstat` ≠ `54 0 app.py`；或 `app.py` blob ≠ 工項四閘 `2` | 🛑 未執行 |
| `5` | 工項三之任一期（必紅／比較基）不符 | 🛑 未執行 |
| `6` | 工項四之任一閘不符 | 🛑 未執行 |
| `7` | 生產碼 `commit` 推至**主線**，或推至側支而其父 ≠ 工項二之 `commit` | 🛑 未執行 |
| `8` | 本批任一新檔為 `git check-ignore` 所命中 | 🛑 未執行 |
| `9` | `§四` 收工閘任一 ≠ 期 | 🛑 未執行 |
| `10` | CC 作任何「孰為正典」「應改為」之判，或改 `F1`〜`F3`、塊、命令一字以求其過 | 🛑 未執行 |

### `§零-3`　pre-flight 機檢之逐項處置（`W-G.9-190R §九（甲）`·⛔ 靜默略過）

| 項 | 性質 | 實得 | 處置 |
|---|---|---|---|
| `P-3`／`P-5` | 機械可判·停機款 | 見 `§五-1` 項 `8` | 🟢 通過 |
| `P-1`／`P-2`／`P-4`／`P-6` | 文字啟發式·⛔ 停機款 | 見 `§五-1` 項 `8` | 逐項具名 |

受單側收單時須自倉重跑 `python verify/probes/probe_order_preflight.py docs/orders/W-G.9-341_重量單.md` 並逐項再處置；`rc ≠ 0` ⇒ **停機回報**。

### `§零-4`　容量停與接續（逐工項具名其已落之證·`自誤 512` 之攔）

1. **觸發**：KL 告 CC「完成本項後停止」；或 CC 自判上下文將盡。
2. **處置**：完成當前工項（⛔ 留半套）⇒ 零生產碼之 `commit` 逕推主線；生產碼之 `commit` 依工項四推側支 ⇒ 於對話回報「已完成至工項 `k`」及逐 `commit` 之 hash。⛔ 寫報告檔。
3. **接續之證**（新 CC 窗先讀 `CLAUDE.md` 與本單）：

| 工項 | 已落之證 |
|---|---|
| 零 | 主線有訊息「`W-G.9-341` 工項零」之 `commit`，且 `docs/orders/W-G.9-341_重量單.md` 之 blob 與來源逐位相同 |
| 一 | 主線有「`W-G.9-341` 工項一」之 `commit`，且三受檔之末端逐位等於塊 `P1`〜`P3` |
| 二 | 主線有「`W-G.9-341` 工項二」之 `commit`，且二器之 blob 與 `F2`／`F3` 逐位相同 |
| 三 | ⛔ 產生 `commit`；其出艙存倉外 ⇒ **接續時一律重做**（⛔ 沿用前窗之出艙） |
| 四 | 側支 `verify/W-G.9-341-gb182` 存在、其端之訊息為「`W-G.9-341` 工項四」、其父 ＝ 工項二之 `commit` |
| 五 | 側支之端之訊息為「`W-G.9-341` 工項五」 |

4. 🛑 ⛔ 重辦已落之工項（工項三除外）；有疑 ⇒ 停機回報。停機款 `1` 於接續時讀作「主線 ＝ `09170db` 後恰接本單工項零〜`k` 之 `commit`」。

---

## `§一`　態錨（錨於 `09170db`·發單側窗三十六以 `probe_WG9321_issuer_anchor.py … 517 516` 自倉實跑·`rc 0`）

| # | 項 | 值 |
|---|---|---|
| `1` | 主線／側支／凍存 | 主線 `09170db85f12af623c2b8832ac253794f25c6d9e`（`W-G.9-340` 工項三）；側支 `verify/W-G.9-338-origin` ＝ `2ec1eb2bf43616ce662d95dc633b4c71f366eb15`·`verify/W-G.9-333-gb168` ＝ `be5108d0067e63e23a397fdcbe43358800381470`；凍存分支 `verify/W-G.9-299-gb170` ＝ `488c4858da63ebe370ed950788947aa742eb44ad`·為主線之祖 |
| `2` | 生產碼 | `app.py` blob ＝ `0269a8c18d0b3aa18ca8a881d232623b4644b444`；`verify/stepg_pipeline.py` blob ＝ `eb557b4bb3caeaf7ef2459dadc7919f3334a432e`；生產碼 `34` 檔 ＝ `app.py` ＋ `verify/` 頂層 `*.py` `33` |
| `3` | `baselines` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列（`git ls-tree -r <態> -- verify/baselines`·`core.quotepath=true`） |
| `4` | refs | 遠端 heads **`28`**（`verify/` `23`·`wip/` `3`·`claude/` `1`·`main` `1`） |
| `5` | 母體（`core.quotepath=false`） | 全倉 **`2672`**｜`.md` **`922`**｜`docs/` **`881`**｜`verify/out/` **`1029`**｜`verify/` 頂層 `*.py` `33`｜子層 `*.py` **`402`** |
| `6` | 四簿（收工閘 `517 516` 之出艙） | 自誤 `503`／`MAX 517`／缺 `[106,355,356,357,489,492,499,511]`｜`GB` `178`／`182`／缺 `[12,87,179,181]`｜`VR` `80`／`95`／缺 `[73,75]`｜`K-9` `45`／`48`／缺 `[44,47]` |
| `7` | 受追加之檔 | `GB` 簿 `982094` B｜自誤簿 `1034102` B｜`CLAUDE.md` `247861` B（皆以換行結尾、CR `0`） |
| `8` | `probe_WG9330_wfns_ast.py` | `rc 0`·成員 `40`／`40`·引擎經 `ns` 取用 `39` |
| `9` | `probe_WG9340_main_synth.py` | `rc 0` |

---

## `§二`　KL 之語（逐字）與射程

> 依擬調整的順序出單

> 是

（KL·發單側窗三十六。前者所答者係同窗之提議「先辦 `GB-182`（街角宗旁碎片併入中間抵費地）……之後再辦 `K-9-29 二`」；後者所答者係同窗之放行請示，其【要你判斷】逐字：「同意把「街角宗旁碎片併入中間抵費地」的修正推上側支（主線暫不動，待側支驗畢再請您放行併入）嗎？（是／否）」。同請示之【現況】載：街角第 1 宗之後緊接中間抵費地時，交界現有二線（街角宗側沿側街方向、抵費地側沿宗地分配線方向）夾出細長碎片、另成一塊抵費地，其外緣另帶零寬折返線（最長約 `36 m`〔實測 `42.42 m`·`自誤 521`〕）；【要改成】載：交界只留一條線（沿用街角宗之界線），碎片併入相鄰之中間抵費地、零寬折返線一併清除；只以一點相接或不相接之碎片、及強制抵費地之形維持原樣；依據為 `8/9`「一個交界只有一條線」及 `9/23`「配餘地僅可能在街廓左右側及中間位置」二裁示。【對土地的影響】逐態並列：甲 `0 m` `R4` `26.09` 併入（`1049.67` → `1075.75`·片數 `10` → `9`）；甲 `3.5 m` `R1` `5.38`、`R4` `26.08` 併入（`1169.71` → `1175.09`、`933.73` → `959.82`·`12` → `10`）；乙 `0 m` `R4` `26.09`、`27.44` 併入（`1175.55` → `1229.08`·`10` → `8`）；乙 `3.5 m` `R1` `5.38`、`R4` `26.09`、`27.44` 併入（`1169.71` → `1175.09`、`1175.55` → `1229.08`·`14` → `11`）；地主宗四情境 `43`／`44`／`55`／`57` 宗之面積與形狀逐宗不變；抵費地合計面積不變（清除折返線之差 `≤ 0.0001 ㎡`〔實測 `0.00011 ㎡`·`自誤 521`〕）；並具名更正甲 `0 m` 之漏列、列明 `R1` `0.005 ㎡` 細縫與 `R6` `85.71 ㎡` 不在範圍。四格之改前改後平面對照圖四張已隨請示附呈。）

🔒 **逐筆放行清單**（`恆常附款 y`·擬本單時自倉重導）：本批將落入主線之 `commit` ＝ 工項零〜二之三筆（皆零生產碼·依 `常規一` 補款 `🔧 一` 逕推）；生產碼之 `commit` ＝ 工項四之一筆（`app.py`），**只落側支**；工項五之報告亦落側支。判準 ＝ 是否改動生產碼 `34` 檔之任一。
🛑 **射程**：`(a)` 工項四之生產碼 `commit` 推至**側支** `verify/W-G.9-341-gb182`（新建·父 ＝ 工項二之 `commit`）；`(b)` 工項五之報告同落該側支；`(c)` **主線之快轉（至側支之端）⛔ 在本放行內**——待發單側自倉復驗後，另呈 KL 放行。
🔒 **修之依據**（`恆常附款 b②` 之查·開工態全倉）：`K-9-5-4 ②` 釋示 `1`「一條線、一個方向」（`docs/rulings/K-6_街角地分配程序與可分配判準.md:717`–`:718`）與釋示 `2`「一個界面只能有一條線」（同檔 `:724`–`:728`）——KL `2026-08-09`·**其受詞為街角第 1、2 宗之界**；本修引其理於街角宗與中間配餘地之界 ⇒ 屬延伸，KL 於本請示知情而放行；`K-9-48` 其四之 `2`（同檔 `:4759`–`:4761`·KL `2026-09-23`：配餘地僅可能在街廓左右側及中間位置）；KL `2026-09-12` 之應然「街廓最多只有左右兩側街角地及中間集中配餘地」（`docs/orders/W-G.9-270_補令三_p6交錯檢與二數之更正.md:25`）。另查得 `K-9-5-7`（抵費地係配餘）——與本修併存（配餘之總量不變）；`N0-19`（池片身分鍵 ＝ `s` 區間）——⛔ 引為本修之據（`W-G.9-270 補令三 §二`：該裁⛔ 越其落點適用）。

---

## `§三`　工項（依序）

### 工項零　本單原封入倉（主線·零生產碼）

`docs/orders/W-G.9-341_重量單.md`（**新檔**）。自主 checkout 之根二進位複製；`SELF_SHA256` 依 `P-5` 原口徑自驗。`commit` 訊息逐字 `W-G.9-341 工項零：本單原封入倉 ⛔ 零生產碼` ⇒ `push origin wip/s1-endpart`。

### 工項一　三塊之末端追加（主線·零生產碼·一 `commit`）

| 塊 | 受檔（**檔末追加**） | 標題形之所本（`v`） |
|---|---|---|
| `P1` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `W-G.9-338` 之 `GB-180`／`GB-182` 之立節（`### \`GB-N\` 🆕` 之形）；同批 `GB-178` 之進度節（`### \`GB-N\` 之進度（⛔ 解除）` 之形） |
| `P2` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `自誤 517` 之題形（`### 🩸 \`自誤 N\`　**…**`） |
| `P3` | `CLAUDE.md` | `W-G.9-340` 之待落地更新節 |

各塊依圍欄之逐列索引抽出（圍欄內全文·末附換行）、對拍 `§五-1` 之 bytes／`sha256`（停機款 `2`）後，以二進位附於受檔之末。三受檔之刪除欄逐檔 `0`。`commit` 訊息逐字 `W-G.9-341 工項一：三塊之末端追加（GB-182 之進度 ＋ GB-186／GB-187／GB-188 之立／自誤 518・520・521／待落地清單之更新）⛔ 零生產碼` ⇒ `push origin wip/s1-endpart`。

**塊 `P1`**（`GB-182` 之進度 ＋ `GB-186`／`GB-187`／`GB-188` 之立）：

````markdown

---

## 🔧 `GB-182` 之進度（修入側支）＋ `GB-186`／`GB-187`／`GB-188` 之立（`W-G.9-341`·⛔ 上文一字不刪·純末端追加）

🔒 **態** ＝ `09170db85f12af623c2b8832ac253794f25c6d9e`（本批開工態）。**取號**（`W-G.9-341 §零-1`）：`GB-186`、`GB-187`、`GB-188` 於開工態全倉 `0` 命中；`GB-183`（`docs/orders/W-G.9-338_重量單.md:35`）、`GB-184`（`docs/orders/W-G.9-332_輕量單.md:28`、`docs/reports/W-G.9-332R_CC交接文.md:88`）、`GB-185`（`docs/orders/W-G.9-333_重量單.md:34`）皆為前單對照乙之引、內文非零 ⇒ ⛔ 取，自此為缺號。

### `GB-182` 之進度（⛔ 解除）

失效條件三項（本簿 `GB-182` 立項節逐字）之現況：
`(1)` **成就**——成因經量測具名（態 `09170db`·`verify/probes/wg9309/chk_wg9309.py` 四格之出艙逐邊量方位，對 CAD 之 SIDELINE／ALLOC 方位）：各片之二長邊，一邊與該端街角第 `1` 宗共界、其方位 ＝ 該端側街線（SIDELINE）之方位；另一邊與中間配餘地共界、其方位 ＝ 宗地分配線（ALLOC）之方位；二線交於街廓後側、向正面臨路線張開。例：`R4` 甲 `3.5 m` 之片 ＝ `48.741°`（`R4` 左 SIDELINE `48.741°`）／`46.027°`（`R4` ALLOC `46.027°`）·夾角 `2.714°`·長 `33.25 m`·正面寬 `1.58 m`；`R1` 甲 `3.5 m` 之片 ＝ `47.492°`（`R1` 左 SIDELINE）／`48.05°`（`R1` ALLOC）；`R4` 乙 `0 m` 右之片 ＝ `43.159°`（`R4` 右 SIDELINE）。產生點 ＝ `app.py` `_pool_strips_for_block` 步驟 `5b`（`D-2b-24`·幾何餘另成一片）。正典之繫：`K-9-5-4 ②` 釋示 `1`「一條線、一個方向」、釋示 `2`「一個界面只能有一條線」（`docs/rulings/K-6_街角地分配程序與可分配判準.md:715`–`:728`·KL `2026-08-09`·其受詞為街角第 `1`、`2` 宗之界）；`K-9-48` 其四之 `2`（同檔 `:4759`–`:4761`·KL `2026-09-23`）。
🔴 **立項節「實測」之漏列**：甲 `0 m` `R4` 左 `26.09 ㎡` 同屬本項之受詞，立項節未列——`verify/probes/probe_WG9338_gapcheck.py` 之「旁片」以二邊界之**精確交集長 `> 0.05 m`** 判共界，該片與 `628(1)+` 之精確交集長 ＝ `0.0`（`1e-4` 緩衝下共界 `33.25 m`·浮點級之界差）⇒ 漏判（`自誤 520`）。四格全數（態 `09170db`·器 `verify/probes/probe_WG9341_poolpieces.py` 之「改前之相接對」）：甲 `0 m` `R4` `26.0861`｜甲 `3.5 m` `R1` `5.3835`、`R4` `26.0846`｜乙 `0 m` `R4` `27.4425`、`26.0938`｜乙 `3.5 m` `R1` `5.3835`、`R4` `27.4425`、`26.0938`（㎡·共 `8` 片·皆於 `1e-4` 緩衝下與該端街角第 `1` 宗共界）。
🔒 自本批起，本項之據改為 `verify/probes/probe_WG9341_poolpieces.py`（相接之判 ＝ 小片之頂點以 `2e-4` 吸附至大片後，二者之聯集為單一 Polygon）；`probe_WG9338_gapcheck.py` 之「旁片」列⛔ 作本項之據（其主受詞 `GB-178` 之夾片不受影響·⛔ 修該器）。
`(2)` **側支態成就**（`W-G.9-341` 工項四）：修 ＝ 步驟 `5b` 之幾何餘，與步驟 `5` 之某一池帶以邊相接者併入該池帶（多者取共界最長者；只點接或不接者、及只與強制帶相接者，沿舊法另成一片）；併入者刪零寬之折返頂點。KL 放行（側支）＝ `W-G.9-341 §二`；入主線另候 KL 放行 ⇒ 主線未成就。
`(3)` **側支態成就**：四格之上開 `8` 片全數併入（改後相接之抵費地對 ＝ `0`·四格）；地主宗逐宗逐欄不變（`43`／`44`／`55`／`57` 宗）；抵費地之面積和之差 `≤ 0.00011 ㎡`。
🛑 **⛔ 解除**（`(2)` 於主線未成就）。
🔒 **射程之界**：⛔ 及 `R6` `85.71 ㎡`（街廓端之楔形·`GB-178` 失效條件 `(3)` 之另項）；⛔ 及 `GB-186`。`GB-178` 失效條件 `(3)` 所列屬本項之片（`R1` `5.38`、`R4` `26.08`／`26.09`／`27.44`·本簿 `GB-178` 之進度（二））於側支態併入；`GB-178` 之進度⛔ 本批登記（待入主線）。

### `GB-186` 🆕　**`R1` 左端街角宗之近側界旁另有寬約 `1 mm` 之細縫，自成一片抵費地（`0.0046 ㎡`）**

**受詞**：`R1` 之抵費地列中面積 `0.0046 ㎡` 之片（四格皆有）。
**實測**（態 `09170db`·甲 `3.5 m`·`chk_wg9309.py` 之出艙）：沿 `628-36(1)` 之近側界（與截角邊相鄰之界·長 `29.58 m`·方位 `47.492°` ＝ `R1` 左 SIDELINE 之方位）；最小外接矩形之短邊 `0.001 m`；s ∈ `[−0.3248, −0.0358]`（`_pool_strips_for_block` 之 `[配餘]` 診斷列）。與 `628-36(1)` 之精確邊界交集為一點（`1e-4` 緩衝下共界 `29.58 m`）；改前與 `5.38 ㎡` 之片僅點接、與中間配餘地相距 `17.54 m`；改後（側支）與併後之中間配餘地相距 `17.52 m` ⇒ ⛔ 以邊與任何池帶相接。
**成因**：【未證】——發單側之推測（街廓外框之側邊與 SIDELINE 之繪製差約 `1 mm`）；⛔ 據以修。
**與既有登記之界**：`GB-182`（與池帶以邊相接者）——異受詞；`W-G.9-341` 之修⛔ 及之。
**失效條件**：`(1)` 成因經量測具名；`(2)` 屬缺陷者修入生產碼 ＋ KL 放行；`(3)` 四格之該片消失，或具名其為合法之分片。

### `GB-187` 🆕　**畫面：街角地選定之結果被清除後逕行 G 值計算，程式以技術語中斷、⛔ 指示操作者先重跑街角地選定**

**受詞**：`app.py` `def main()` 內 G 值計算之右鏈首宗斷言（字樣錨「非街角第 1 宗」·開工態 `app.py:22449`）於 `st.session_state['f3_corner_winners']` 缺該街廓時之觸發。清除點之一 ＝ `_f3L_invalidate_g_cache`（`app.py:20309`·退縮值／分配深度之 `on_change`·清 `f3_corner_winners`／`f3L_corner_winners` 等鍵）。
**實測**：KL 本機日誌 `2026-09-24 23:15:25`，訊息逐字「🔴 街廓 R1 右鏈：起點取 FRONT p2（有側街·非強制），而首宗 628(5) 非街角第 1 宗（其近側界 ∥ 分配線 ⇒ p2 外側之楔形無人承受）·停」（主 checkout 之態 ＝ CC `W-G.9-340` 工項四之出艙 `09170db`·發單側⛔ 能實查；其時之操作序⛔ 能自日誌判定）。發單側窗三十六以引擎路徑重現（態 `09170db`·甲 `3.5 m`）：`run_step_g` 以 `run_corner_pk` 之結果驅動 ⇒ 無中斷；以 `wins`／`forced` 皆 `{}` 驅動 ⇒ 同文中斷。`chk_wg9309.py` 四格（以 `run_corner_pk` 之結果驅動）皆 `err None`。
**性質**：安全檢查如設計觸發（舊碼於同態不做街角處理、照常配地而⛔ 示警）；所缺者 ＝ 地政用語之操作指示。
**失效條件**：`(1)` 畫面於街角地選定之結果缺漏時，以地政用語提示先重跑街角地選定（修入生產碼 ＋ KL 放行·附畫面路徑之合成案〔`自誤 517`〕）；`(2)` 上開重現之二造（缺／齊）於改後分別得「提示」與「無中斷」。
🔒 **排程**：與 Streamlit 升級（`st.components.v1.html` ⇒ `st.iframe`；`use_container_width` ⇒ `width`）及 `GB-188` 同批（皆畫面程式）。

### `GB-188` 🆕　**畫面：營造工程物價指數之 API 資料含值為 `0` 之月份，帶入後觸發欄位下限（`50`）之錯誤**

**受詞**：`app.py` `def main()` 內「從 API 帶入最新指數」鈕（取 `cci['data'][-1]`）與月份選單（取 `_cci['by_roc'].get(sel_ci)`）之寫入 `st.session_state['ci']`；`fetch_construction_price_index`（`app.py:26`）只略過不可轉為數之值（`float(val_list[0])` 之例外）、⛔ 濾非正值。
**實測**：KL 本機日誌 `2026-09-24 23:12:33`，`StreamlitValueBelowMinError: The value 0.0 is less than the min_value 50.0.`（traceback 指 `curr_idx = _ni("價格日期物價指數", …, key="ci")`）。`app.py` 全檔以字樣 `'ci'`、`"ci"`、`_sv_ci` 查 ⇒ 命中 `:15420`、`:15421`、`:15447`（讀）、`:15449`、`:15450`、`:15455`（欄位本身）；寫入者恰 `:15420`–`:15421`（API 帶入鈕）與 `:15449`–`:15450`（月份選單）二處（另 `_ni`〔`app.py:110`〕回存其自身之回傳值），值皆取自 API 資料 ⇒ 資料中有月份之值為 `0`（研判為尚未公布之月份·【未證】）。
**與配地之關係**：無（營造單價之物價調整）。
**失效條件**：`(1)` 非正值之月份⛔ 帶入並提示（修入生產碼 ＋ KL 放行）；`(2)` 以合成之 API 回應（末月之值為 `0`）驗之。
🔒 **排程**：同 `GB-187`。
````

**塊 `P2`**（`自誤 518`／`520`／`521`）：

````markdown

---

**取號**（`W-G.9-341 §零-1`）：本批取 `518`、`520`、`521`（`3` 號）；`519` 為 `docs/orders/W-G.9-338_重量單.md:35` 對照乙之引、內文非零 ⇒ ⛔ 取，自此為缺號。三者皆發單側窗三十六所自捕。

### 🩸 `自誤 518`　**發單側窗三十六向 KL 述「物價指數欄位錯誤」之成因為「介面還原舊值時帶入 0」，未讀寫入點即斷**

**形**：窗三十六答 KL 之首輪逐字「這是介面還原舊值時帶入 0，低於欄位下限 50 所致」；其時僅讀日誌之 traceback 與 `_ni` 之影子鍵機制，⛔ 讀 `app.py` 之寫入點。其後自倉實查：報錯者為 `ci`（價格日期物價指數），其寫入者恰 API 帶入鈕與月份選單二處，值取自主計總處資料（`GB-188`）。
**後果之界**：更正已於同窗之放行請示中向 KL 出艙；與配地無關；零入倉之誤。**根因**：以日誌所呈之機制推成因，未先讀其產生式。**後果之框**：🟢 零後果。攔點 ＝ 發單側窗三十六（擬放行請示時自查）。
**攔法**：既有（`恆常附款 m③`／`m④`：以碼面之產生式逐字定之）。⛔ 新立款。

---

### 🩸 `自誤 520`　**`GB-182` 立項節之「實測」漏列甲 `0 m` `R4` `26.09 ㎡`；發單側窗三十六首輪之四片表承之**

**形**：`GB-182` 立項節（`W-G.9-338` 塊 `P1`）之實測以 `verify/probes/probe_WG9338_gapcheck.py` 之「旁片」列為據，其共界之判為二邊界之**精確交集長 `> 0.05 m`**；甲 `0 m` `R4` 之片與 `628(1)+` 之精確交集長 ＝ `0.0`（浮點級之界差·`1e-4` 緩衝下共界 `33.25 m`）⇒ 漏列；`docs/reports/W-G.9-338R_執行報告.md` 閘 `7` 遂載甲 `0.0` 無旁片。窗三十六首輪答 KL 之四片表（甲 `3.5 m` 二片、乙 `0 m` 二片）承該列舉。
**後果之界**：窗三十六擬放行請示時，以改前改後之全片對拍發現，請示中已具名更正並列入【對土地的影響】之表；本批之修涵蓋之。零入倉之誤（生產碼未變）。**根因**：器之判繫於代理量（精確交集長）而非性質（以邊相接）；引簿中一量未具名其來源層級（器之判準）。**後果之框**：🟢 零後果。攔點 ＝ 發單側窗三十六。
**攔法**：既有（閘須綁性質；`恆常附款 p` 之來源層級）。⛔ 新立款；本項之據改為 `verify/probes/probe_WG9341_poolpieces.py`（`GB` 簿 `GB-182` 之進度節）。

---

### 🩸 `自誤 521`　**發單側窗三十六之放行請示，【現況】與【對土地的影響】各有一數未對實測：折返線「最長約 `36 m`」、面積差「`≤ 0.0001 ㎡`」**

**形**：請示逐字「零寬度的折返線，沿正面臨路線或宗地界線延伸，最長約 36 m」與「清除折返線的差額 ≤ 0.0001 ㎡」。實測（態 `09170db`·四格之 `8` 片·`verify/probes/probe_WG9341_poolpieces.py` 之判準）：折返之尖所夾之短邊最長 ＝ `42.42 m`（乙 `0`／`3.5 m` `R4` `26.0938 ㎡` 之片·沿正面臨路線）；抵費地面積和之差最大 ＝ `0.00011 ㎡`（乙 `0`／`3.5 m`）。二數皆取自甲 `3.5 m` 單格之量而外推於四格。
**後果之界**：二數皆⛔ 涉地主宗與抵費地之配置；KL 之放行所據之性質（地主宗逐宗不變、碎片併入、折返線清除）不變；更正於交付本單時向 KL 出艙。零入倉之誤。**根因**：單格之量未經四格重導即寫入呈 KL 之文。**後果之框**：🟢 零後果。攔點 ＝ 發單側窗三十六（擬本單工項四之期時）。
**攔法**：既有（`恆常附款 q`：呈 KL 之【對土地的影響】須逐態並列；`q②`：量級須引倉內同受詞之實測）。⛔ 新立款。
````

**塊 `P3`**（待落地清單之更新）：

````markdown

---

## 🔧 待落地清單之更新：`GB-182` 之修入側支；`GB-186`／`GB-187`／`GB-188` 之補登；依賴序之改（`W-G.9-341`·⛔ 上文一字不刪·純末端追加）

🔒 **態** ＝ `09170db85f12af623c2b8832ac253794f25c6d9e`（本批開工態）。狀態用三態（✅ 已落地／🔶 部分落地／⬜ 未落地）。

| 序 | 裁／項 | 要旨 | 態 | 出處 |
|---|---|---|---|---|
| `1` | `GB-182`（`K-9-5-4 ②` 釋示 `1`／`2` 之理·`K-9-48` 其四之 `2`） | 幾何餘與池帶以邊相接者併入該池帶（一個界面一條線）；併入者刪零寬折返頂點 | 🔶（側支 `verify/W-G.9-341-gb182`·入主線另候 KL 放行） | `GB` 簿 `GB-182` 之進度（`W-G.9-341`） |
| `2` | `GB-186` | `R1` 左端街角宗近側界旁之 `1 mm` 細縫自成一片（`0.0046 ㎡`） | ⬜ | `GB` 簿 `GB-186` |
| `3` | `GB-187` | 街角地選定之結果被清除後之 G 值計算，改以地政用語提示 | ⬜ | `GB` 簿 `GB-187` |
| `4` | `GB-188` | 物價指數 API 之值為 `0` 之月份⛔ 帶入 | ⬜ | `GB` 簿 `GB-188` |

🔒 **依賴序**（改自 `W-G.9-340` 之更新·發單側擬·⛔ 域裁·KL 允「依擬調整的順序出單」）：`GB-182`（本單·側支）→ 入主線 → `K-9-29 二` 碼側落地（廢前置合併）→ 段三落地（`K-6 §二 段三` ＋ `K-9-48`；「不影響原位次」之檢核器原型 ＝ `verify/probes/probe_WG9338_noaffect.py`）→ `K-9-29 六` 入池閘（具名受測案例：乙態 `0 m` `R5` 左 `628-45(1)`、`628-30(1)` 之剔除·器 `verify/probes/probe_WG9338_owner_adj.py`）→ 五級。
🔒 **改序之由**：`K-9-29 二` 不消除 `GB-182` 之片（乙態亦有 `26.09`／`27.44 ㎡`）；`GB-182` 之修⛔ 動地主宗、不涉域裁，先辦者其後各單之四格對照不再含此類碎片。
🔒 **畫面批**（與上開依賴序併行·皆畫面程式·須附畫面路徑之合成案〔`自誤 517`〕）：`GB-187` ＋ `GB-188` ＋ Streamlit 升級（`st.components.v1.html` ⇒ `st.iframe`；`use_container_width` ⇒ `width`）。
🔒 **本機介面**：主線之生產碼⛔ 變（本單之生產碼只推側支）。
````

### 工項二　量測器二支入倉（主線·零生產碼·一 `commit`·`恆常附款 k`）

| 檔 | 來源（主 checkout 之根） | 落點 | 其用 |
|---|---|---|---|
| `F2` | `probe_WG9341_poolpieces.py` | `verify/probes/probe_WG9341_poolpieces.py` | 四格改前改後之抵費地片對拍：地主宗逐宗逐欄不變、面積和守恆、相接之抵費地對歸 `0`、受併之片無零寬折返頂點、無相接對之街廓逐列不變（`GB-182` 之器） |
| `F3` | `probe_WG9341_synth.py` | `verify/probes/probe_WG9341_synth.py` | 合成幾何之閉式期值（甲併入／乙夾於二宗⛔ 併／丙只接強制帶⛔ 併／丁無幾何餘／戊正面零寬縫之折返）；受詞 ＝ `ns["_pool_strips_for_block"]` |

二進位複製、對拍 bytes 與 `sha256`（停機款 `3`）；內容⛔ 改。`commit` 訊息逐字 `W-G.9-341 工項二：量測器二支入倉 ⛔ 零生產碼` ⇒ `push origin wip/s1-endpart`。

### 工項三　改前釘紅與比較基（⛔ 零 `commit`·於工項二之 `commit` 上量·出艙存倉外）

環境：`PYTHONIOENCODING=utf-8`、`PYTHONUTF8=1`。四格 ＝ `甲`／`乙` × `0.0`／`3.5`（`chk_wg9309.py` 之態引數 ＝ `甲|乙`）。發單側窗三十六以開工態之拋棄式 clone 實跑得下列各期（工項二之 `commit` 之生產碼 ＝ 開工態）。

1. **逐宗基線**：`python verify/probes/wg9309/chk_wg9309.py <repo> <甲|乙> <0.0|3.5> <倉外>/before_<態>_<退縮>.json` 四格。期（列數·`err`）：甲 `0.0` ＝ `53`·`None`｜甲 `3.5` ＝ `56`·`None`｜乙 `0.0` ＝ `65`·`None`｜乙 `3.5` ＝ `71`·`None`。
2. **必紅（`F2` 之判別力）**：`python verify/probes/probe_WG9341_poolpieces.py <before 之格> <before 之同格>`（同一檔充二參）四格。期：`rc` 甲 `0.0` **`1`**｜甲 `3.5` **`2`**｜乙 `0.0` **`2`**｜乙 `3.5` **`3`**（皆來自 ③「改後之相接對」＝ 改前之相接對；①②④⑤ 皆 🟢）。改前之相接對逐對（街廓·大片（㎡）＋ 小片（㎡））：

| 格 | 相接對 |
|---|---|
| 甲 `0.0` | `R4` `R4-抵費地-1`（`1049.6677`）＋ `R4-抵費地-3`（`26.0861`） |
| 甲 `3.5` | `R1` `R1-抵費地-1`（`1169.7086`）＋ `R1-抵費地-2`（`5.3835`）；`R4` `R4-抵費地-1`（`933.7309`）＋ `R4-抵費地-3`（`26.0846`） |
| 乙 `0.0` | `R4` `R4-抵費地-1`（`1175.5464`）＋ `R4-抵費地-2`（`27.4425`）；同 ＋ `R4-抵費地-3`（`26.0938`） |
| 乙 `3.5` | `R1` `R1-抵費地-1`（`1169.7086`）＋ `R1-抵費地-2`（`5.3835`）；`R4` `R4-抵費地-1`（`1175.5464`）＋ `R4-抵費地-2`（`27.4425`）；同 ＋ `R4-抵費地-3`（`26.0938`） |

3. **必紅（`F3` 之判別力）**：`python verify/probes/probe_WG9341_synth.py <repo 絕對路徑>` ⇒ **`rc 5`**（🔴 恰 `甲1`、`甲2`、`戊1`、`戊2`、`戊3`；`甲3`、`乙1`、`丙1`、`丙2`、`丁1` 皆 ✅）。
4. **比較基（`GB-178` 之器）**：`python verify/probes/probe_WG9338_gapcheck.py <倉外>/before_<態>_<退縮>.json` 四格。期：`rc` 甲 `0.0` `0`｜甲 `3.5` `0`｜乙 `0.0` `0`｜乙 `3.5` `5`（無受詞）；「旁片（另列）」列數 `0`／`2`／`2`／`3`。
5. **harness 改前**：於倉外之拋棄式 worktree（`git worktree add --detach <倉外> <工項二之 commit>`）跑 `python verify/run_all.py`（甲態·旗標未設）一次，出艙存倉外；記其名目之 `✅ PASS`／`🔴 FAIL` 逐項清單與各項「本閘違規總計」（工項四閘 `7` 之比較基）。發單側預跑（開工態之拋棄式 clone·`555` 秒）：`rc 1`·名目 **`64`** 項（`✅ PASS` `25`／`🔴 FAIL` `39`）；下列五項之違規總計：`v3·G值0m` `239`／`v3·G值3.5m` `228`／`v3·滑池槽3.5m` `81`／`W-D.3 碎片幾何/三分類（v3）` `84`／`W-D.3 碎片逐邊CAD（v3）` `50`。

### 工項四　生產碼（側支 `verify/W-G.9-341-gb182`·🔴 生產碼·一 `commit`）

1. 自工項二之 `commit` 建側支；`git apply --check W-G.9-341_c1.diff` ⇒ 施之（停機款 `4`）。
2. **改之內容**（`F1` 之全文即規格·⛔ 另寫）：`app.py` 之 `_pool_strips_for_block`（`def main()` 之外）三處——
   - 步驟 `5` 之後記 `_n_strip`（步驟 `5` 所切之池帶數；強制帶與幾何餘不在其內）；
   - 步驟 `5b` 之前立內部函式 `_despike_g182`（刪零寬折返頂點：與前一頂點相距 `≤ _S_EPS`、或原路折返且距直線 `≤ _S_EPS` 者）；
   - 步驟 `5b` 之每一幾何餘：先刪折返、以 `_S_EPS` 吸附至 `pieces[:_n_strip]` 之各池帶，聯集為單一 Polygon 者取共界最長之一併入（併後再刪折返、`kept_iv` 取二區間之聯集、印 `🟢 [配餘·併]` 列）；皆⛔ 相接者沿舊法另成一片。
   - ⛔ 動：`verify/stepg_pipeline.py`（經 `ns` 取用同一函式·⛔ 另算）；`verify/wf_f1.py`／`verify/wf_f4.py`；步驟 `6` 之自檢四閘；`_S_EPS` 之值。⛔ 新訂常數。
3. **閘**（逐閘三值出艙·發單側窗三十六以 `F1` 施後之拋棄式樹實跑得各期）：

| 閘 | 受詞 | 期 |
|---|---|---|
| `1` | `git diff --numstat`（對工項二之 `commit`） | `54 0 app.py`；其餘 `0` 檔 |
| `2` | blob | `app.py` ＝ `9ac432188037395e39e18eae4ec9b6fbd97c5a15`；`verify/stepg_pipeline.py` ＝ `eb557b4bb3caeaf7ef2459dadc7919f3334a432e`（⛔ 變） |
| `3` | `chk_wg9309.py` 四格（改後·`after_<態>_<退縮>.json`） | 列數 甲 `52`／`54`、乙 `63`／`68`；**四格 `err` 皆 `None`** |
| `4` | `python verify/probes/probe_WG9341_poolpieces.py <before> <after>` 四格 | 四格皆 **`rc 0`**；見 `§三-甲` |
| `5` | `python verify/probes/probe_WG9341_synth.py <repo 絕對路徑>`（側支之樹） | **`rc 0`**（十項皆 ✅；工項三 `3` 為 `rc 5`） |
| `6` | `probe_WG9338_gapcheck.py` 四格（改後） | `rc` 同工項三 `4`（`0`／`0`／`0`／`5`）；「旁片（另列）」列數 `0`／`0`／`0`／`0`；「夾片」列 `0` |
| `7` | 於倉外之拋棄式 worktree（側支之端）跑 `python verify/run_all.py`（甲態）一次 | 名目 `64` 項與工項三 `5` 逐項對應、**狀態逐項相同**（`✅ PASS` `25`／`🔴 FAIL` `39`）；各項「本閘違規總計」相異者恰下列五項：`v3·G值0m` `239` → `238`｜`v3·G值3.5m` `228` → `226`｜`v3·滑池槽3.5m` `81` → `80`｜`W-D.3 碎片幾何/三分類（v3）` `84` → `79`｜`W-D.3 碎片逐邊CAD（v3）` `50` → `47`；`rc 1`（發單側預跑·`F1` 施後之樹·`542` 秒） |
| `8` | `python verify/probes/probe_WG9330_wfns_ast.py <repo 絕對路徑>` | **`rc 0`**·成員 `40`／`40`·經 `ns` 取用之相異名 `39`（同 `§一` 項 `8`） |
| `9` | `python verify/probes/probe_WG9340_main_synth.py <repo 絕對路徑>` | **`rc 0`**（畫面路徑之合成案·本修⛔ 在 `def main()` 內） |

4. `commit`（訊息逐字 `W-G.9-341 工項四：幾何餘與池帶以邊相接者併入該池帶（GB-182）🔴 生產碼`）⇒ `push origin verify/W-G.9-341-gb182`（**⛔ 主線**·停機款 `7`）。
5. 對話回報：`commit` hash；閘 `1`〜`9` 三值；`F2` 四格之出艙全文（併入報告）。

#### `§三-甲`　閘 `4` 之期（`F2` 之逐項·改前 → 改後）

| 格 | 地主宗 | 抵費地列 | ② 面積和之差（㎡） | 改前之相接對 | 受併之片（改後·㎡·折返頂點） | ③ 改後相接對 | ⑤ 無相接對之街廓相異 | 另列（`GB-186`） |
|---|---|---|---|---|---|---|---|---|
| 甲 `0.0` | `43`／`43`·相異 `0` | `10` → `9` | `+0.000000` | `1` | `R4-抵費地-1` `1075.7538`·`0` | `0` | `0`（`R1`／`R2`／`R3`／`R5`／`R6`） | `R1-抵費地-2` `0.0046` |
| 甲 `3.5` | `44`／`44`·相異 `0` | `12` → `10` | `-0.000000` | `2` | `R1-抵費地-1` `1175.0921`·`0`；`R4-抵費地-1` `959.8155`·`0` | `0` | `0`（`R2`／`R3`／`R5`／`R6`） | `R1-抵費地-2` `0.0046` |
| 乙 `0.0` | `55`／`55`·相異 `0` | `10` → `8` | `-0.000110` | `2` | `R4-抵費地` `1229.0827`·`0` | `0` | `0`（`R1`／`R2`／`R3`／`R5`／`R6`） | `R1-抵費地-2` `0.0046` |
| 乙 `3.5` | `57`／`57`·相異 `0` | `14` → `11` | `-0.000110` | `3` | `R1-抵費地-1` `1175.0921`·`0`；`R4-抵費地` `1229.0827`·`0` | `0` | `0`（`R2`／`R3`／`R5`／`R6`） | `R1-抵費地-2` `0.0046` |

🔒 改後之抵費地片名因片數減少而重編（面積遞減序·`_pool_strips_for_block` 之既有慣例）：`R4` 乙態由三片成一片 ⇒ 名為 `R4-抵費地`（無序號）。地主宗之 `暫編地號` 與 `cut_coords` 逐宗不變（① 之受詞）。

### 工項五　執行報告入倉（側支·新檔 `docs/reports/W-G.9-341R_執行報告.md`）

須載：① 逐 `commit` 之 hash 與工項之對應（主線三筆、側支二筆）；② 停機款 `1`〜`10` 之三值；③ 塊 `P1`〜`P3` 與檔 `F1`〜`F3` 之實得；④ 工項三／四之全部出艙（`F2` 四格之出艙全文附於報告末）；⑤ `§五-1` 基數之重算；⑥ `GB-182` 之失效條件之現況（本批只量·⛔ 登記解除）；⑦ CC 之自捕；⑧ 下一 CC 窗須知。收工閘之實測值出艙於對話（⛔ 寫入本檔·自指）。`commit` 訊息逐字 `W-G.9-341 工項五：執行報告入倉（側支）⛔ 零生產碼` ⇒ `push origin verify/W-G.9-341-gb182`。

---

## `§四`　收工閘（於側支之端量·基線態 ＝ `§一`）

🔒 **`n③` 之施行**：發單側已於開工態之拋棄式 clone 模擬工項零〜二、四（本單 ＋ 三塊末端追加 ＋ 二器 ＋ `F1`）並實跑閘 `8`／`9`／`10`／`12`／`13` ⇒ 皆 `rc 0`（**必過之實例**）；於未追加之態（開工態）跑閘 `8`（`521 517`）⇒ **`rc 7`**（**必破之實例**）。

| 閘 | 受詞 | 期 |
|---|---|---|
| `1` | 本批五筆 `commit` 之刪除欄 | 逐檔 **`0`** |
| `2` | 生產碼 `34` 檔對開工態之相異 | 側支之端恰 `1` 檔（`app.py`·blob 見工項四閘 `2`）；主線之 `app.py` blob ⛔ 變（`0269a8c1…`） |
| `3` | 本批文字檔（本單／二器／三受檔／報告）之 `CR` | 合計 **`0`** |
| `4` | 四簿期末 | 見 `§四-1` |
| `5` | `append-only`（三值·`x`） | `GB` 簿／自誤簿／`CLAUDE.md` 🟢 純末端追加；`K-6` 典／`VR` 簿／恆常附款簿 🔵 ⛔ 動 |
| `6` | 自限 | `baselines` ⛔ 變；遠端 heads **`29`**（＝ `28` ＋ 側支 `1`）；凍存分支與側支 `verify/W-G.9-338-origin`／`verify/W-G.9-333-gb168` 之 tip ⛔ 變 |
| `7` | 母體（側支之端·`core.quotepath=false`） | 全倉 **`2676`**（＝ `2672` ＋ 本單 ＋ 器 `2` ＋ 報告 ＋ `N_cc`）｜`.md` **`924`**｜`docs/` **`883`**｜`verify/out/` **`1029`**｜`verify/` 頂層 `*.py` `33`｜子層 `*.py` **`404`**；`N_cc` 逐筆具名（`e②`·器之落檔與 `run_all` 之副作用檔一律置倉外） |
| `8` | `python verify/probes/probe_WG9270_closegate.py 521 517 .` | **`rc 0`** |
| `9` | `python verify/probes/probe_WG9321_issuer_anchor.py <repo 絕對路徑> <輸出檔之絕對路徑> 521 517` | **`rc 0`** |
| `10` | `probe_WG9330_wfns_ast.py` | 同工項四閘 `8` |
| `11` | 檔 `F2`／`F3` 之入倉 blob | `git cat-file blob HEAD:<落點> \| sha256sum` 逐檔 ＝ `§五-1` |
| `12` | `probe_WG9341_synth.py` | 同工項四閘 `5` |
| `13` | `probe_WG9340_main_synth.py` | 同工項四閘 `9` |

### `§四-1`　四簿期末之算式（`w①`·收工閘 `521 517` 之出艙）

| 簿 | 算式 | 期末 |
|---|---|---|
| 自誤 | `503` ＋ `3`（`518`、`520`、`521`） | **`506`**／`MAX` **`521`**／缺 **`[106,355,356,357,489,492,499,511,519]`** |
| `GB` | `178` ＋ `3`（`186`、`187`、`188`） | **`181`**／`MAX` **`188`**／缺 **`[12,87,179,181,183,184,185]`** |
| `K-9` | ⛔ 鑄 | **`45`**／`MAX` **`48`**／缺 **`[44,47]`** |
| `VR` | ⛔ 鑄 | **`80`**／`95`／缺 **`[73,75]`** |

三受追加之檔期末 bytes（算式）：`GB` 簿 `982094 + 8411`｜自誤簿 `1034102 + 3753`｜`CLAUDE.md` `247861 + 2154`。

---

## `§五`　驗收與上呈

### `§五-1`　受單側收單時須當場重算之基數（`f`）

| # | 基數 | 單載 |
|---|---|---|
| `1` | 工項數 | **`6`**（零〜五） |
| `2` | `§六` 款數 | **`28`** |
| `3` | 所鑄之號 | 裁 **`0`**／`GB` **`3`**（`186`、`187`、`188`）／自誤 **`3`**（`518`、`520`、`521`）；解除 **`0`** |
| `4` | 停機款數 | **`10`**（另 `§零-4` 容量停） |
| `5` | 塊 `P1`〜`P3`（圍欄內·末附換行） | `P1` `:132`–`:172`·**`8411`** B·`83bff841c567948a…`／`P2` `:178`–`:203`·**`3753`** B·`e107821418d18a14…`／`P3` `:209`–`:226`·**`2154`** B·`b7acbd7829fb79cf…` |
| `6` | 檔 `F1`〜`F3` | `F1` **`5672`** B·`163789beee4fd7a0874935ae229117e13343611a5e08e91a6e3455d7fcc67bea`／`F2` **`6255`** B·`6a0a0e1affb98428026452b77d133777201ba8943f62b0f940d424261158d3d9`／`F3` **`5393`** B·`97cd5b7c080d7b84ff1f46f5c804677e247e05d8e5aa832040d82d4117346cca` |
| `7` | 逐筆放行清單 | 主線 **`3`** 筆／動生產碼 **`0`** 筆；側支 **`2`** 筆／動生產碼 **`1`** 筆（工項四） |
| `8` | pre-flight（發單側出單前實跑） | 紅 **`0`** 項（`P-5` 於自簽後相符）；🟡 提示 **`0`** 項（初稿有 `P-4` 一項：工項四閘表同段含箭號字元與「引擎」一詞 ⇒ 依 `v②` 改單之形、⛔ 改器）；記錄 `P-3`：開工態 `app.py`（blob `0269a8c1…`）之 `def main` 區間 ＝ `15127`-`24794`；`F1` 施後（blob `9ac43218…`）＝ `15181`-`24848`，`_pool_strips_for_block` ＝ `9551`-`9820`（改前 `9551`-`9766`）⇒ 本修⛔ 在 `def main` 內 |

🔒 抽取一律依**圍欄之逐列索引**；bytes 不符 ⇒ 停機款 `2`。其餘不符者以列舉為準並具名·⛔ 追改本單一字。

### `§五-2`　上呈

1. 任一停機款成就 ⇒ **停機上呈**·⛔ 自改。
2. 工項零〜二 ⇒ 逕行 `push` 主線；工項四、五 ⇒ 只推側支；**主線之快轉候發單側復驗與 KL 放行**。
3. 收工後，主 checkout 之根之四來源檔由 KL 自行移除（CC ⛔ 刪之）。

---

## `§六`　恆常附款逐款回掃（`28` 款）

| 款 | 對本單之施行 | 判 |
|---|---|---|
| `a` | 本單⛔ 新訂款（`自誤 518`／`520`／`521` 之攔法皆「既有·⛔ 新立款」） | ⚪ 不生 |
| `b`／`b②` | 本單⛔ 鑄裁；放行請示之受詞（碎片併入中間抵費地）於請示前以關鍵語與否定形查既有之裁（`docs/rulings`／`CLAUDE.md`／`.claude`）：「一條線」「一個界面」「配餘地」「零星」「另成一片」「自成一片」「獨立成片」「分片」「不併入」「併入抵費地」「碎片」——查得 `K-9-5-4 ②` 釋示 `1`／`2`、`K-9-48` 其四之 `2`、KL `2026-09-12` 之應然（皆為本修之據·已裁·引而延伸）；`K-9-5-7`（併存）；`docs/W-D.4_域裁鎖定.md:44`–`:65` 之機制 `(B)`「碎片遞補形狀調整」已由 KL 裁定 `N-6`（`2026-07-26`）廢止 ⇒ 本修⛔ 觸及；`K-6:3099`（地主宗之碎塊）與 `K-6:4773`（跨街廓建地不併入）——異受詞 | 🟢 施行 |
| `c` | 本單⛔ 動 `_corner_buffer_S` 之迴圈 | ⚪ 不生 |
| `d` | `GB-187`／`GB-188` 之實測取自 KL 本機日誌（倉外之物）——同格載「發單側⛔ 能實查」；`GB-188` 之 API 資料之值為 `0` 載【未證】 | 🟢 施行 |
| `e`／`e②`／`e③` | `§零-1` 三分逐筆具名；`§四` 閘 `7` 之本單自身之產物含 CC 之報告，另計 `N_cc`；`run_all` 之副作用檔令置倉外 | 🟢 施行 |
| `f` | `§五-1` 令受單側重算其基數；本單自載之基數出單前以程式重算 | 🟢 施行 |
| `g` | 本批新檔四（本單／二器／報告）以 `git check-ignore -v` 於開工態實查 ⇒ `rc 1`（四檔皆⛔ 命中） | 🟢 施行 |
| `h` | 塊以檔案管線、依圍欄之逐列索引抽取；本單與 `F1`〜`F3` 以二進位複製；`commit` 訊息 `-F` | 🟢 施行 |
| `i`／`i②`／`i③` | `GB-182` 已讀其立項節全文與其後二處提及（`GB` 簿 `GB-178` 之進度與進度（二））；反向受詞：併入使中間配餘地增大、⛔ 減——`K-9-48` 其三之「配餘地仍容內接矩形」之檢核只可能由不容轉為容；`GB-83`（量測格之注入值吸附·已結案）——異受詞；本單⛔ 立新禁令 | 🟢 施行 |
| `j` | ⛔ 以哨兵之落地後全倉命中為期值 | ⚪ 不生 |
| `k`／`k②`／`k④` | 新立器前以功能之名查既有器（`verify/` 內 `*.py` `435` 檔）：「折返」「spike」「毛刺」「despike」`0` 檔；「相接」`6` 檔（`probe_WG9270R3_p5_pool_connectivity.py` 量中央池之連通性·其受詞為連通分量、⛔ 為片對之邊接 ⇒ 新題）；「共界」「旁片」`1` 檔（`probe_WG9338_gapcheck.py`·其判準為精確交集長 ⇒ 即 `自誤 520` 之漏判之源 ⇒ `F2` 改以吸附後聯集判之）；「吸附」`1` 檔（`probe_WG962_gate_power.py`·閘之判別力·異題） | 🟢 施行 |
| `l`／`l⑥` | `§零-1` 之框式可原樣執行；已發單號之定義域具名；各閘之框與母體同格 | 🟢 施行 |
| `m`／`m③` | `F2` 之吸附容差 `2e-4` 取自 `app.py` 之產生式逐字 `_S_EPS = 2.0e-4`（開工態 `app.py:9139`）；`F3` 自 `ns` 直讀 `_S_EPS`；`F2` 之面積和容差 `0.01` 取自同函式之 ①' 覆蓋閘 | 🟢 施行 |
| `n`／`n②`／`n③`／`n④` | 停機款三值；`F2` 以改前充二參為必破（`rc 1`／`2`／`2`／`3`）、`F3` 於開工態為必破（`rc 5`）；發單側另以停用 `_despike_g182` 之突變樹實跑 `F3` ⇒ `rc 1`（🔴 恰 `戊3`）證折返之刪有其判別力；二器遇空輸入 ⇒ `rc 90`；`§四` 載必過／必破二實例；一切呼叫形寫 `python` | 🟢 施行 |
| `o` | 改前改後二態之地主宗集合同（`43`／`44`／`55`／`57`）⇒ 可比；`run_all` 二態之名目逐項對應 | 🟢 施行 |
| `p` | 本單係發單側窗三十六開場序畢、`W-G.9-340` 自倉復驗全符（含其報告全文）後所擬 | 🟢 施行 |
| `q`／`q②` | 放行請示之【對土地的影響】逐態並列四格；其二數未經四格重導 ⇒ `自誤 521` 具名並於 `§二` 併記實測 | 🟢 施行 |
| `r` | `§四` 各閘之基線態即 `§一`；工項四各閘之基線即工項三 | 🟢 施行 |
| `s` | `F2` ② 之容差 `0.01` 為既有閘之容差、⛔ 以改前之殘差定之；本修回授至面積和者僅刪折返之差（實測 `≤ 0.00011 ㎡`） | 🟢 施行 |
| `t` | ⛔ 令以既有文字為錨之純插入（三塊皆檔末追加；`F1` 以 `git apply` 施之） | ⚪ 不生 |
| `u` | 既有之量 `_S_EPS`（T1 退化界）接入新路徑（吸附容差與折返之判）：以其產生式之名與值（`_S_EPS`／`2e-4`／`2.0e-4`／`2×1e-4`）查 `GB` 簿 ⇒ `_S_EPS` `0` 列、`2e-4` `1` 列（`GB` 簿 `:7936`·係 `5.087498459772632e-4` 之子字串命中·異受詞之逐位差）⇒ 無同量級同格之未解除項 | 🟢 施行 |
| `v` | 三塊之標題形自各該檔最近同類一則逐字複刻；`§四-1` 以收工閘之框出艙 | 🟢 施行 |
| `w` | `§四-1` 為算式並逐號列舉；`§一` 逐格繫開工態、由器落檔 | 🟢 施行 |
| `x` | `§四` 閘 `5` 為三值 | 🟢 施行 |
| `y` | 逐筆放行清單於擬本單時自倉重導（`§二`·`§五-1` 項 `7`） | 🟢 施行 |
| `z` | 對照乙之定義域已先列舉（`§零-1`） | 🟢 施行 |
| `aa` | 對照甲 ＝ `W-G.9-340`（同形族） | 🟢 施行 |
| `ab` | 本單⛔ 新訂款 | ⚪ 不生 |

**算式**：施行 ＝ **`23`**（`b d e f g h i k l m n o p q r s u v w x y z aa`）／不生 ＝ **`5`**（`a c j t ab`）；`23 + 5 = 28`·重疊 `[]`。

---

SELF_SHA256: b03514221b3513cbca1a82f26fd767d51c1dacc8b6994f53e541e5accff6221c
