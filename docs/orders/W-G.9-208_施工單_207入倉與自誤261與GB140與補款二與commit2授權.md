# 🧾 `W-G.9-208` 施工單　`-207` 入倉 ＋ `自誤 261`／`GB-140` ＋ **`-198R` 補款二** ＋ `commit 2` 開工授權

> **基座**：`wip/s1-endpart` ＝ **`b772f8aa09820f01312081588fd8c8d718c8ac86`**（`W-G.9-198R commit 1` 已落地·純快轉·⛔ merge commit）
> **性質**：**二段**。`§一` 🟢 零生產碼（**CC 逕推**）；`§三` 🔴 含生產碼且**確有土地後果**（**⛔ 推主線**·走側分支）。
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔴 **本單之首要事項 ＝ 錨已換代。** `app.py` 之 blob 自 `7fab020` 起首度改變：
`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` → **`6f0317e47ef92c7e06d6d904ecf2d129656bd331`**。
🛑 **`-198R §一 S-0a` 之二款皆已過時**，其更正見 `§二` 補款二；⛔ 得沿用舊值開工。

---

## §零　開工閘（🔒 錨皆為發單側自倉現查·態 `b772f8a`·CC 須逐項重量）

| 閘 | 判準 |
|---|---|
| `S-0a` | 生產碼四檔 ＋ 出艙產生檔之 blob 逐檔：`app.py` ＝ `6f0317e47ef92c7e06d6d904ecf2d129656bd331`／`verify/stepg_pipeline.py` ＝ `fef5865a73033274697790e0e307aedbf6122422`／`verify/run_all.py` ＝ `e5cf9a224517533b183b34e4fd0f5fbe35ee406b`／`verify/run_verification.py` ＝ `d07955c58e67ec06260971a74b5aabe5e846f106`／`verify/selection_pipeline.py` ＝ `10f90c509ae13c77c83e3e5c76fe3c09ca675b14` |
| `S-0b` | 母體：追蹤檔 **`1579`**【檔】／`.md` **`512`**【檔】（`git -c core.quotepath=false ls-tree -r --name-only HEAD`） |
| `S-0c` | `app.py`：`1391897` B ／ `LF` `23571` ／ `CR` `0`；AST ＝ `k6_step0_block_locked` `11567`-`11583`／`k6_step0_merge` `11586`-`11743`／`_pk_one_side_v12` `12966`-`13076`／`k6b_stage2_global_order` `13079`-`13143`／`main` `14326`-`23569` |
| `S-0d` | 取號現查：自誤 相異 `225`／MAX `260` ⇒ 次 `261`；`GB`（框 `` ^#{2,4} `?GB-N`? ``·扣 `997`）相異 `53`／MAX `139` ⇒ 次 `140`；`VR` `74`／`89`；戒 `23`／`40`；`K-9` `13`／`26` |
| `S-0e` | 本單 `SELF_SHA256` 自驗相符；`probe_order_preflight.py` 之 🟡 逐項以**字樣錨**具名並依裁 `H` 歸類 |

🔒 **行尾（`GB-139`）**：`clone` 用 `-c core.autocrlf=false -c core.quotepath=false`；`add` 既有檔一律常規 `add`；**閘之受詞一律取倉側 `git cat-file blob`**。

🔒 **`S-0e` 之已知 🟡（發單側已對本檔實跑·母體 ＝ 本單單檔·範圍 ＝ `P-1`〜`P-6`）**：🔴 `0`【項】／🟡 **`1`**【項】。

| 檢 | 字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-4` | `` 1. `commit 2` 備妥後**⛔ 推 `wip/s1-endpart`**。 `` | **零參照 ⇒ 具名豁免**：`推` 係 **git ref 之推送**，⛔ 幾何方向量、⛔ 涉座標系；其受詞（分支名）已於同列逐字具名 |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造）**：同器對 `-197` ＝ `5`／`-199` ＝ `0`／`-203` ＝ `0`／`-205` ＝ `1`／`-206` ＝ `3`／`-207` ＝ `0`／`-198R` ＝ `5`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。
🔒 **`P-3` 已隨錨換代**：本器對本單實得 `def main` ＝ `14326`-`23569`（＝ `S-0c` 之新值·⛔ 舊值 `14242`-`23460`）⇒ 器已咬到新態。

---

## §一　🟢 工項 `1`：零生產碼批（**CC 逕推**）

### 一-1　二單入倉（原封逐字·⛔ 重打、⛔ 改行尾、⛔ 補尾端換行）

```
docs/orders/W-G.9-207_施工單_commit1放行第一段_rebase與側分支重推.md   （自簽 26dfe7ad…）
docs/orders/W-G.9-208_施工單_207入倉與自誤261與GB140與補款二與commit2授權.md   （本單）
```
🛑 入倉前後各取 `bytes/LF/CR/sha256` 四值，**逐檔相同**方為過；`-207` 之自簽須**自算**相符（⛔ 照抄）。

### 一-2　鑄 `自誤 261`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

```markdown
## 自誤 `261`　令受單方推送某物件，而⛔ 載該物件與其本機分支之關係

`W-G.9-206 §一` 令 CC 將 `48bad3c` 推至側分支 `verify/W-G.9-198R-c1`，並令「⛔ 動主分支一個 commit」。
🩸 發單側**⛔ 設想「CC 之本機 `wip/s1-endpart` 當時正停在該物件上」**——而事實如此。
⇒ 若逕於其上落地 `§二`〜`§七` 之登記批，推之即**連帶推出未放行之 `commit 1`**，
與 `-198R §三-3`／`-206 §一-2`／`-206 §九 S-1` **三者皆互斥**。
🔒 **CC 之處置（正辦）**：建**本地 ref** ＋ 推**遠端側分支**二重保全 → 確認已追蹤異動為空
→ `reset --hard 549bfe8` → 逐項復驗（`HEAD`／`HEAD:app.py`／`left-right`／`cat-file -t`）。
**`commit 1` 之 SHA／`tree`／三檔 blob 皆未變** ⇒ `-206 §七 補一-3`「⛔ 得重做或 amend」未被違反。
**一般式**：**凡令受單方「推送某物件」者，須於同格載明該物件與其<u>本機 `HEAD`／分支</u>之關係，
並明訂本機分支之處置**（保留／退回何處／以何保全）；⛔ 得只寫「推某某 SHA」而任由本機狀態自理。
**族** ＝ 與 `自誤 257`／`260` 同族（**單內關係／射程之列舉不完整，逼受單方當場裁**），本則為該族之**第三度**。
**攔截者** ＝ CC。**土地後果** ＝ 無。
```

### 一-3　鑄 `GB-140`（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔尾**）

```markdown
### `GB-140` 🆕　同一量（`priority_index`）於段一與段二採**相異精度**（`4dp` vs `6dp`）

**實測（態 `b772f8a`·母體 ＝ `app.py` 單檔·框 ＝ 列）**
- 段一：名次之排序鍵與 `qualified.sort` 之平手鍵**同源**，皆取 `round(priority_index, 4)`
  （`-198R R1-2` 所令·二處同源已由 `W-G.9-207` 復驗）。
- 段二：鍵③ 之出艙值為 `round(priority_index, 6)`，且排序**直接以該出艙值**為鍵
  （`k6b_stage2_global_order` 之 `_rows.sort` 逐字 `-r['指數數值']`）。

**性質**　若二候選之 `priority_index` 於 `4dp` 相同而於 `6dp` 相異：
段一判其**平手**（落至 `_pre_position_rank`），段二鍵③ 則判其**有高下** ⇒ **同一量、二處異解**。

**現況之土地後果 ＝ 無**　段三尚未落地（`VR-086`），段二之序⛔ 有消費端（`-198R D1-e` 已機械證）。
🔴 **失效條件**：**段三落地前須收斂**——擇一：(a) 段二鍵③ 改用與段一同源之 `4dp`；
(b) 段一平手鍵改用 `6dp`；(c) 出艙值與排序鍵**分離**（出艙 `6dp`、排序用全精度）。
🛑 三者之土地後果相異 ⇒ 屬**須先量測後裁**之項，⛔ 得逕改。

**⛔ 綁本案**　⛔ 綁 `R2`、⛔ 綁街廓名、⛔ 綁 `SB` 值：任何案件只要有二候選之指數於 `4dp` 相同即必觸。
**現況處置** ＝ 只登記、⛔ 零生產碼；列為 `K-6-B` **段三**落地之前置。
```

### 一-4　`§一` 之驗收

| 閘 | 判準 |
|---|---|
| `A-1` | 二新檔之四值**入倉前 ≡ 入倉後**；`-207` 自簽自算相符（宣告值與重算值**並列**） |
| `A-2` | 二追加檔之 **append-only 位元組級自證**：前 `N` 位元組 ≡ 基座同檔全文；🆕 **依 `戒 40`，`Δ` 與 payload 二端位元組數同格並報** |
| `A-3` | 追蹤檔 `1579` → **`1582`**；`.md` `512` → **`515`**（＝ `-207`／`-208`／本批執行報告） |
| `A-4` | 自誤 相異 `225`→**`226`**、MAX `260`→**`261`**；`GB` 相異 `53`→**`54`**、MAX `139`→**`140`** |
| `A-5` | 🔴 **生產碼四檔 ＋ `selection_pipeline.py` 五檔 blob ⛔ 動**（逐檔與 `S-0a` 相同） |

✅ `§一` 全綠 ⇒ **逕推**（零生產碼）。🛑 `§一` 之 commit **⛔ 得與 `§三` 之 commit 合併**。

---

## §二　🔴 `W-G.9-198R` **補款二**（承 `-206 §七` 補款一·⛔ 就地改 `-198R` 一字）

| # | 補款內容 |
|---|---|
| `補二-1` | 🔴 **`-198R §一 S-0a①` 之錨換代**：`app.py` blob 之判準值由 `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` 改為 **`6f0317e47ef92c7e06d6d904ecf2d129656bd331`**。 |
| `補二-2` | 🔴 **`-198R §一 S-0a②` 之區間換代**：其比較基座由 `7fab020` 改為 **`b772f8aa09820f01312081588fd8c8d718c8ac86`**；判準（承 `VR-089` 肢一）＝ `git diff --name-only b772f8aa..HEAD` 逐列**⛔ 含 `CLAUDE.md 🔧 二` 所列之四檔**；`verify/probes/**` 之新增⛔ 使本閘紅。 |
| `補二-3` | 🔴 **`-198R §一 S-0d`（改前基線之凍存）須<u>重做</u>**：其受詞改為**態 `b772f8a`**（＝ `commit 1` 落地後），產物之檔名須帶 `b772f8a` 短碼。🛑 **⛔ 沿用 `6a3f5fe` 之凍存**——`D2-a` 逐字令「以 `commit 1` 落地後之態為基準」。 |
| `補二-4` | **`commit 2` 之落地點預先擴張**（承 `VR-089` 肢二與 `自誤 260` 之一般式）：准動 `app.py` ＋ `verify/selection_pipeline.py` ＋ `verify/run_verification.py`——`R2-6` 之歸側出艙須與 app 同構於 `selection_pipeline.py`；若 `U-K2` 守衛因新欄再度觸發，其**明文處置**（`_DECLARED_EXTRAS` 具名申報）同准，惟**須逐欄具名於報告**，並適用 `-206 §六 三`（波末重產時一併移除）。🛑 動**四檔生產碼集以外**之其他檔 ⇒ **停機上呈**。 |
| `補二-5` | 🔴 **`R2-9`（新增結果態·承 `-206 §七 補一-3`）**：`k6b_stage2_global_order` docstring 內現載為「CC 之技術判讀」「須於段三落地前經發單側確認」之 ②③ 方向段落，改述為**引正典 `K-9-26`**（KL 裁 `2026-09-02`）。🛑 ⛔ 為此重做或 `amend` `commit 1`。 |
| `補二-6` | 🆕 **`R2-10`**：`commit 2` **⛔ 得順手收斂 `GB-140`**（`4dp`／`6dp`）——該項須先量測後裁，屬另案。docstring 若提及精度，只得**陳述現況並引 `GB-140`**，⛔ 更動任何 `round` 之位數。 |

---

## §三　🔴 `commit 2` 之開工授權（**確有土地後果**）

🔒 **結果態與驗收⛔ 重寫**——逐字沿用 `W-G.9-198R §四`（`R2-1`〜`R2-8`／`D2-a`〜`D2-i`），連同本單 `§二` 之 `補二-1`〜`補二-6`。CC 須自倉重抽 `-198R`（`docs/orders/W-G.9-198R_…md`·自簽 `b291070e…`）與正典 `K-9-24`／`K-9-25`／`K-9-26`，⛔ 以本單所引為準。

🛑 **重申三項最易失守者**
1. `R2-1` 與 `R2-2` **⛔ 得拆入不同 commit**（解除封鎖而歸側規則未存在 ⇒ 正典未定義之中間態）。
2. `R2-3`「拿得下」＝ **達標 ∧ 該側全部達標候選中指數最大**；**第 1 名而未達標者⛔ 拿得下**。
3. `R2-7` `p1` 側之平手退路係**幾何性質**（前緣線之 `p1` 端），⛔ 得以「左」「右」之字面實作。

🔴 **本案之預期受詞**（⛔ 判準·⛔ 寫入碼）：`R4` 之 `628(1)`＋`628-1(1)` 合併為 `628(1)+`，右臨截角 `4.9145` > 左 `4.9042` ⇒ **歸右**；左街角依 `K-9-24 二·5` 成為**強制抵費地 `226.01 ㎡`**。
🛑 **CC 須報實算值**；若實算方向**相反（歸左）** ⇒ **停機上呈發單側**，⛔ 自行歸因（`VR-087` 之殘差 `≤ 0.0061` m，翻轉需 `≥ 0.010` m）。

### §三-1　commit 之處置（走側分支·⛔ 推主線）

1. `commit 2` 備妥後**⛔ 推 `wip/s1-endpart`**。
2. 推至側分支 **`verify/W-G.9-198R-c2`**；回報其全 `40` 碼，並復驗主線**⛔ 前進**。
3. 🔴 **停機**，逐字回報 `D2-a`〜`D2-i` ＋ `numstat` ＋ 三檔新 blob 全 `40` 碼 ＋ **新 AST 區間**（⛔ 沿用 `S-0c` 之值）。
4. 發單側自側分支逐位元組復驗 → 請示 KL → 放行後依 `-206 §八` 之等值款推進主線。
🛑 `commit 3`（段一上鎖）**⛔ 開工**，候 `commit 2` 放行。

### §三-2　報告

`docs/reports/W-G.9-208R_…md`：`§一` 之 `A-1`〜`A-5`（可與 `§一` 同批、不同 commit·逕推）；`commit 2` 之 `D2-*` 另俟其備妥後補記或另出報告，由 CC 自定，惟**⛔ 得與 `commit 2` 綁在同一 commit**，亦**⛔ 得置於其後**（承 `-198R §八-零-b` 之結構性理由：置後即結構上不可推）。

---

## §四　停機條件

- `S-0a` 五檔任一 blob 與本單所載不符 ⇒ **停機上呈**（⛔ 自行更新錨）。
- `補二-3` 之改前基線若沿用舊態 ⇒ 判紅，重做。
- `D2-c` 歸側出艙中**無任一筆左右指數相異** ⇒ 判別力零 ⇒ **停機上呈**。
- 實算歸側方向與 `§三` 之預期相反 ⇒ **停機上呈**。
- `§三` 之任一結果態與另一結果態互斥、或與正典逐字牴觸 ⇒ **停機上呈發單側**（⛔ 轉呈 KL）。

---

## §五　攢批與次號

**攢批（本單⛔ 辦）**：`GB` 定義列式之正典化（`GB-138` 處置③·須比照 `W-G.9-184` 體例先證所採之形涵蓋既有全部鑄號）。
🔒 **次號（本批落地後）**：自誤 `262`／`GB 141`／`VR 090`／戒 `41`／`K-9 27`。
🔒 **側分支現況**：`verify/W-G.9-198R-c1` ＝ `b772f8aa…`（⛔ 刪·史料）；本機另有 `-c1-preRebase` ＝ `48bad3c…`。
SELF_SHA256: 9dbe1749e2dd678b077c665eecd1e7a20efc1a41fbfc7636909253a9e0dcdbdc
