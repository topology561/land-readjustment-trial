# `W-G.9-316`　施工單（**中**）：`W₀` 之消費者全定位 ／ 項 `5` 之補測 ／ `自誤 430`·`431` ／ `GB` 簿之登記

> **態**｜開工 ＝ `1b2c400f98bc5053a71a2b746ec0651b9368cc04` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-316_施工單_W0消費者定位與項5補測及自誤430_431.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **中**（**零生產碼**·**⛔ `run_verification`**·**⛔ 改生產碼 `34` 檔一字**·得**改一支探針之一行**（`§四` 逐字）·得新增 `verify/out/` 之本批落檔·⛔ 開分支·⛔ 修批）。
> 🛑 **本批鑄號 ＝ `自誤 430`、`自誤 431`**（**二號**·逐字由本單供·**同一 `commit`**）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`§六` 之 `GB` 簿追加係**末端追加**（`戒 36`）**⛔ 鑄號**、**⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`**。
> 🔴 **修批之號仍為 `W-G.9-309`**（於 `1b2c400` 態宣告框六數皆 `0`·保留）；**本單⛔ 占其號**、⛔ 辦修批。
> 🔑 **序**：**① `§二` 工項零** ⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一**（唯讀定位·單獨 `commit`）⇒ **④ `§四` 工項二**（一行修 ＋ 二重跑·單獨 `commit`）⇒ **⑤ `§五` 工項三**（二自誤·單獨 `commit`）⇒ **⑥ `§六` 工項四**（`GB` 簿·單獨 `commit`）⇒ **⑦ `§七` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-315R` 之復驗（發單側自倉獨立·態 `1b2c400`·`core.quotePath` 預設·框同格）

| # | 項 | 發單側實得（產生方式） | 判 |
|---|---|---|---|
| `1` | 主線 | `1b2c400f98bc5053a71a2b746ec0651b9368cc04`；`74ccd47..HEAD` ＝ `6` 個 `commit`；受改 `10` 檔·**刪除欄全 `0`**（框 ＝ `git diff --numstat` 之刪除欄） | ✅ |
| `2` | 生產碼 `34` 檔 | 逐檔 blob 相異 ＝ `0`；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；判別力[必不命中] `verify/` 子層 `*.py` ＝ `331`（`> 0`） | ✅ |
| `3` | 底本探針 | `probe_WG9278_w0zero.py` ＝ `85acdbe6863cc078cb569c82d6e7b3a11dbcdc3a`·`26016` B **一字未動** | ✅ |
| `4` | 自捕 `2` 之撤回 | `be8fe57f` **⛔ 在遠端史**（`git cat-file -t` ⇒ `Not a valid object name`）；新器 blob 於工項一（`3915251`）與 `HEAD` **逐位相同**（`cc34dce612314e8589fd8df2c6a610363084748b`）⇒ 倉態無殘留 | ✅ |
| `5` | 自誤簿 | 前綴 `910123` B 逐位相符；期末 `912558` B（Δ `2435`）；`CR` `0`；blob `aa75f14a1f8ef3b0ba2f94e1655c38e7a250906c` | ✅ |
| `6` | 四簿 | 自誤 `419`／`429`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40` | ✅ |
| `7` | 自限 | `GB` 簿 `96755bc35b666d92b2cdc6a78c2658902a94f507` 未動；`baselines` `a898f4e1…`·`298` 列；`refs/heads/verify/*` `20` 支；**`verify/out/` 既有 `913` 檔 blob 相異 `0`**、新增 `6`（`WG9315_*`） | ✅ |
| `8` | 報告 | `53947` B·`CR` `0`（嵌入落檔之行尾正規化，其處置正確） | ✅ |
| `9` | 母體 | 全 `docs/` `820`→`822`；`verify/out/` 追蹤 `913`→`919`；全倉追蹤 `2423`→`2432` | ✅ |

### `§零-2`　裁（**`6` 項**）

1. **`W-G.9-315R` 之受領**：工項零、開工閘 `14`／`14`、工項一（器之新增·判別力三造）、工項二（`α`／`β` 之項 `1`〜`4`）、工項三（`自誤 429`）、收工閘八閘 ⇒ **全部受領**。

2. **自捕 `2` 則 ⇒ 受領·⛔ 鑄號**。其二（一度 `commit` 已入倉之器、旋即 `git reset --hard` 撤回而**⛔ `push`**）之判**正確**：器已於工項一入倉，改之即觸 `§七`「⛔ 追改任何已入倉文件一字」⋀ 收工閘 `1`。其一（項 `5` 之 `TypeError` 而**⛔ 重跑**）亦合單。

3. 🔴 **項 `1` 之「破否」欄須更正為三值（發單側自倉碼面實查·⛔ CC 之誤）**：
   `verify/stepg_pipeline.py` 之二閘**次序**經實查為 —— `telescoping`（`:1443`〜`:1477`·含 `Δ_i` 閘 `:1461`／端點差閘 `:1471`）**先於** `理論＝實跑`（`:1553` 起）。
   ⇒ **替身 `α` 下管線已於 `telescoping` 中止**（`R4 right`·`Δ=3.137`），其後之 `理論＝實跑` 閘 **🛑 未執行**；報告所載之「🟢 未破」係器**離線重建**之值（`Δ=0.676`），⛔ 該閘之實際判。
   🔒 「**未執行**」與「**未破**」之別係 `GB-168` 一次精化 `§三` 所嚴分者（`0m`／`3.5m` 態甲之 `R4` 即以「`_N=1` ⇒ 該閘結構上不執行」立論）⇒ ⛔ 混同。
   🛑 **責在單側**（受詞之框為二值而正典為三值）⇒ 鑄 `自誤 431`；⛔ 追改 CC 之報告。

4. 🔴 **替身 `α`／`β` 皆非 `K-9-5-12` 之忠實反事實（發單側之責 ⇒ 鑄 `自誤 430`）**：
   碼面逐字（`verify/stepg_pipeline.py:1470`）`_tele_exp = _rw_from_width(_sd_Wf) - _rw_from_width(_sd_W0)`，其 `_sd_W0` ＝ `_adv_final['W0_left'／'W0_right']`（`:1448`–`:1449`）⇒ **`W₀` 另有一個消費者**。單 `§零-2` 裁 `5` 所定之替身只截 `rw_increment`（分子 `ΣRw_實跑` 由 `85.41` 變 `88.40`），而期望式仍用 `R(W₀=0.18)` ⇒ **人為地打破了 telescoping**。
   ⇒ 二替身之「仍中止」**⛔ 讀為「`K-9-5-12` 之落地無效」**；本批之假說（`W-G.9-315` `§零-2` 裁 `4`）**⛔ 獲證亦⛔ 獲否**。
   🔑 併記碼面自身之不一致：`:1445` 註解逐字「**W₀＝forced 角落鎖定之 buffer 起算點（無 forced → 0）**」，而 `W-G.9-278R` 實測本案 `R4 right` 之 `_fo_right = False`（**非 forced**）卻得 `W₀ ≈ 0.18`。🛑 **⛔ 判其為缺陷**——照實具名。

5. **本批⛔ 施任何新替身**：忠實替身（`γ`）之設計繫於 `W₀` 之**全部消費者**與其**產生鏈**之逐字，而該定位尚未在手。⇒ 本批只辦 **`§三` 之唯讀定位**；`γ` 之設計與驅動**另單**。🛑 **CC ⛔ 自行設計或施加 `γ`**。

6. **依賴序之更新（登記·⛔ 據以自行落地）**：⓪ 之受詞已由「`GB-168` 之修」細分為 **⓪-a**（`W₀` 之歸零 ＝ `K-9-5-12` 之落地·`GB-169` 之射程）與 **⓪-b**（理論側之**絕對值制** 對 實跑側之**差分制**·`GB-168` 之本體）。🛑 **二者皆為生產碼批·須 KL 放行·⛔ 於本批擬**；`⓪-b` 之受詞疑為**域題**（重劃負擔率之計算基礎），其呈 KL 之題俟 `γ` 之實測補齊土地欄後一次呈上。

### `§零-3`　本單取號之宣告框（母體 ＝ 全 `docs/` 追蹤檔 **`822`** 檔（讀不到 `19`）·態 `1b2c400`·錨定框 `(?<![0-9\-])<號>(?![0-9])`·框 ＝ `D1` 檔框 ＋ 列框 ＋ 檔框·時點 ＝ 工項零**之前**）

| 號 | `D1` 檔 | 列框 | 檔框 | 角色 |
|---|---|---|---|---|
| **`W-G.9-316`** | **`0`** | **`0`** | **`0`** | 受詢 ⇒ **未占用·可用** |
| `W-G.9-315` | `2` | `20` | `3` | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0`（依補款 `⑨` 他指排除後） | `0` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱） | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組），⛔ 採信本表。

### `§零-4`　既往同題之清單與其結論（`自誤 426` 攔法 `③`）

| 受詞 | 既有登記 | 與本批之關係 |
|---|---|---|
| `Δ=2.425` 之值空間定位 | `GB-168` 原節 ＋ 一次／二次精化 ＋ 現態復現 | **⛔ 重測**·本批僅引述 |
| `W₀ → 0` 之反事實 | `W-G.9-278R`（停機）／`W-G.9-279R`（受詞改繫·`GB-169` 收窄）／`W-G.9-315R`（態乙·二替身） | **擴充**：本批只定位 `W₀` 之消費者，⛔ 再測反事實 |
| `_rw_start` 決定式 | `GB-169` 受詞欄逐字（`W-G.9-277` 實測） | **⛔ 重測**·本批以其為錨 |
| 二解算器之實參 | `GB-168` 二次精化（`solve_G_binary :10244` ＝ `_rw_start, W`／`iterate_G_S` ＝ `W_prev, W_cur`） | **⛔ 重測**·本批以其為錨 |
| `.gitignore`（恆常附款 `g`） | `:30` `verify/out/got_*`／`:31` `verify/out/W-D.4_*` | 本批落檔名 `WG9316_*` ⛔ 落入該二族 |

---

## `§一`　開工閘（**`15` 項**·資料列之數見本節末之機驗·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準**（框同格） |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首 |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `1b2c400f98bc5053a71a2b746ec0651b9368cc04` 或其後裔（`merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` 逐位相符；🔴 ＝ `0`；🟡 **逐項具名處置**（⛔ 靜默略過·⛔ 列為停機款） |
| `3'` | 受詞自證（以器所印之檔名列·框 ＝ 子字串框） | [必真] 含 `W-G.9-316` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `419`／`429`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40` |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py` | `34` 檔；判別力[必不命中] ＝ `verify/` 子層 `*.py` 須 `> 0`（發單側於 `1b2c400` 得 `331`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**·⛔ 設 `false`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`gb170` ＝ `0`；`p3a` ＝ `1` |
| `8` | 受改檔之期初（框 ＝ blob `sha1` ＋ bytes） | 自誤簿 ＝ `aa75f14a1f8ef3b0ba2f94e1655c38e7a250906c`·**`912558` B**；`GB` 簿 ＝ `96755bc35b666d92b2cdc6a78c2658902a94f507`·**`924914` B**；本批之器 `verify/probes/probe_WG9315_k95_12_stateB.py` ＝ `cc34dce612314e8589fd8df2c6a610363084748b`·**`30151` B**·`CR` `0` |
| `9` | 底本探針之不動（🛑 **本批⛔ 改其一字**） | `verify/probes/probe_WG9278_w0zero.py` ＝ `85acdbe6863cc078cb569c82d6e7b3a11dbcdc3a`·`26016` B |
| `10` | 本批新物之未占（**跑於 `§二` 之後**·框 ＝ 子字串框·母體 ＝ `git ls-tree -r --name-only -z HEAD`） | 檔名含 `W-G.9-316` 者 ＝ `1`；哨兵 `ZW430`／`ZW431`／`GB316N` 之全倉命中 ＝ **除本單自身之產物外 `0`**（逐筆具名所在檔）。發單側於 `1b2c400` 得三哨兵全倉 `0` 檔 |
| `11` | 宣告框（`§零-3` 四列·**CC 自跑**·含二對照組） | 受詢之命中**除本單自身之產物外 ＝ `0`**；對照甲 `D1` `≥ 1`；對照乙 ＝ `0` |
| `12` | `verify/out/` 之期初（框 ＝ 追蹤檔數 ＋ `git status --porcelain -z`） | 倉內追蹤 **`919`** 檔；本批**得新增**其下之落檔，⛔ 改既有 `919` 檔任一位元組 |
| `13` | `.gitignore` 之 `:30`／`:31` 逐字 | `verify/out/got_*`／`verify/out/W-D.4_*`；本批落檔名 `WG9316_*` ⛔ 落入該二族（恆常附款 `g`） |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `|` 起首且**⛔** 為分隔列（`|---`）者，扣表頭 `1`），與本單自載之 `15` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以列舉為準、逐項皆跑並具名。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308`、`W-G.9-308_補令一`（`a`／`b`／`c`）、`W-G.9-310`（`d-1`〜`d-3`）、`W-G.9-311`（`e`／`f`）、`W-G.9-312`（`g`）、`W-G.9-313`（`h`／`i`）、`W-G.9-314`（`j`／`k`）、`W-G.9-315`（`l`）之恆常附款全數逐項適用**（逐字見該八單·⛔ 省）。
🆕 **本單所增**：
（`m`·`自誤 430` 之攔法）**凡單所定之替身，出單前須先以 AST 或字樣錨列出該受詞之<u>全部消費者</u>**，並於單內同格具名「本替身所截者」與「本替身**未**截者」二欄；未截者存在時，該反事實一律具名為「**非忠實**」，其結果⛔ 讀為所擬之裁之成敗。
（`n`·`自誤 431` 之攔法）**凡受詞涉及一個「閘」者，其出艙之框一律為三值**——🟢 **已執行且通過**／🛑 **未執行**／🔴 **已執行且破**；⛔ 用「破否」等二值框。凡以器**離線重建**之值出艙者，須同格具名「該值⛔ 為該閘之實際判」。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-316_施工單_W0消費者定位與項5補測及自誤430_431.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名含 `W-G.9-316` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。🛑 **落地後即 `push`**，其後方跑 `§一`。

---

## `§三`　工項一：**`W₀` 之消費者全定位**（主線·單獨 `commit`·🛑 **全唯讀·⛔ 驅動·⛔ 修任何檔一字·⛔ 施任何替身**）

**器** ＝ 新探針 `verify/probes/probe_WG9316_w0_consumers.py`（唯讀·AST ＋ 字樣錨·⛔ 匯入即驅動）。
**落檔** ＝ `verify/out/WG9316_w0_consumers.log`。
🛑 **⛔ 判孰為正確、⛔ 提修法主張、⛔ 擬任何 diff、⛔ 稱任何處為「缺陷」**——**照實具名**。

**出艙之受詞（`a`〜`f`·⛔ 省·每項同格載其框、母體與產生指令）**：

| # | 受詞 | 框／母體 |
|---|---|---|
| `a` | `_tele_exp` 之賦值式逐字與其所在（現查·⛔ 採本單所載之行號為錨） | 列框·母體 ＝ `verify/stepg_pipeline.py` 單檔 |
| `b` | `_sd_W0` 之**產生鏈**：自 `_tele_exp` 之實參上溯至 `W0_left`／`W0_right` 之**賦值節點**，逐環出艙其逐字與包層（AST `Assign`·⛔ 文字層） | AST·母體 ＝ `verify/stepg_pipeline.py` ＋ `app.py` |
| `c` | `rw_increment` 之**全部呼叫節點**及其實參（`ast.unparse`），並具名其包層函式 | AST·母體 ＝ `app.py` ＋ `verify/` 之 `*.py` **`364`** 檔 |
| `d` | `_rw_start` 之決定式逐字 ＋ `W_cur`（`W`）之**累積起點**：`W_cur = W_prev + S` 之 `W_prev` 執行緒其**初值**之賦值節點逐字與包層 | AST ＋ 列框·母體 同 `c` |
| `e` | `rw_from_width` 之**全部呼叫節點**（供後批判斷「歸零」之射程），逐節點具名其包層與實參 | AST·母體 同 `c` |
| `f` | `_fo_left`／`_fo_right`（`forced` 旗標）之賦值節點逐字、其包層、及其與 `b` 之 `W₀` 賦值節點**是否同框**（可見性） | AST·母體 同 `c` |

🔒 **判別力二造**：`(甲·必有命中)` `rw_increment` 之呼叫節點數須 `> 0`（`GB-168` 二次精化載其 `solve_G_binary` `1` ／ `iterate_G_S` `2`·本批須自行重得並與之對拍）；`(乙·必為零)` 一人造之函式名（**執行期組出**·字面⛔ 出艙·坑 `bd`）之呼叫節點數 ＝ `0`。**二造同色 ⇒ 器紅 ⇒ ⛔ 出艙任何定位之數。**

🛑 **⛔ 對 `(b)`／`(d)` 之上溯以推定代之**——上溯不可達者一律照實載為「**⛔ 可達·其止於 <逐字>**」，⛔ 以「應為」「推測」填之。

---

## `§四`　工項二：**一行排序鍵之修 ＋ 項 `5` 之補測**（主線·單獨 `commit`）

### `§四-1`　修（🛑 **逐字·⛔ 逾此一行·⛔ 動任何判準或值**）

受詞 ＝ `verify/probes/probe_WG9315_k95_12_stateB.py` 之**項 `5`** 段內，形如 `ks = sorted(set(fo) & set(fn))` 之**該一列**（現查·其於 `1b2c400` 之列框命中須 ＝ **`1`**；不為 `1` ⇒ **停、上呈**）。
**改為**：於該 `sorted(...)` 補入排序鍵 `key=lambda k: (str(k[0]), str(k[1]), str(k[2]))`。
🛑 **⛔ 改該檔其他任何一字**；🛑 其 `diff` 須**逐字出艙**（增 `1` 刪 `1`）。
🔒 **本修係收工閘 `1`「刪除欄全 `0`」之<u>唯一例外</u>**（見 `§七` 閘 `1`）；⛔ 據此擴及他檔。

### `§四-2`　驅動（🛑 **`α` 一次 ⋀ `β` 一次·⛔ 逾二·⛔ 新增任何替身**）

母體 ＝ **態乙**（`WV_K6_STEP0=off`）；落檔 ＝ `verify/out/WG9316_alpha.log`／`verify/out/WG9316_beta.log`（⛔ 改 `WG9315_*` 任一位元組）。
🛑 **判別力三造須重新成立**（`W-G.9-315` `§三` 逐字沿用）；三造未全成立 ⇒ **⛔ 出艙任何數**、停、上呈。

**出艙之項**：
1. **項 `5`**（`ΔΣG`／`Σ|ΔG|`／`ΔΣ抵費地面積`·帶號合計與絕對值合計並列）——本批之主受詞。
2. **項 `1` 之三值框重出**（恆常附款 `n`）：`telescoping`（`Δ_i` 閘／端點差閘）與 `理論＝實跑` **二閘分列**，每閘每情境每替身載 🟢 已執行且通過／🛑 **未執行**／🔴 已執行且破 之一，並同格載其**判據**（`err` 之首行逐字·或「管線已於前一閘中止」）。
3. **`W₀`／`W_final`／`forced` 三實值**（`R4 right`·態乙·基線與二替身各一）：`_adv_final['W0_right']`／`['Wf_right']`（**未捨入** ＋ `2dp`）與 `_fo_right` 之布林值。
🛑 **項 `2`〜`4` ⛔ 重出**（`W-G.9-315R` 已登記·`自誤 426` 攔法）；惟若其值與 `W-G.9-315R` **相異**，須 loud 具名該相異並停、上呈。

---

## `§五`　工項三：**`自誤 430` ＋ `自誤 431` 之鑄**（主線·**單一 `commit`**·嚴格末端追加）

落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（⛔ 改上文一字）。
🔑 界：`<!-- ZW430-BEGIN -->`／`<!-- ZW430-END -->`、`<!-- ZW431-BEGIN -->`／`<!-- ZW431-END -->`（**定界以整列相等為之**）；**payload ＝ 二界列之間**，**界列⛔ 隨 payload 入簿**（恆常附款 `j③`）。順序 ＝ `430` 在前、`431` 在後。
🛑 凡含反引號之文字一律**以檔案管線**為之（恆常附款 `h`）。

<!-- ZW430-BEGIN -->

### 🩸 `自誤 430`　**單所定之替身，其射程未涵蓋受詞之全部消費者——只截 `rw_increment`，未及於結構閘 `telescoping` 之期望式所消費之同一 `W₀`，致該反事實非忠實，其「仍中止」之結果⛔ 可讀為所擬之裁之成敗**

**形**：`W-G.9-315` 單 `§零-2` 裁 `5` 逐字定二替身，皆以「凡 `rw_increment` 之呼叫端經框內省判定為鏈頭者，以 `rw_increment(0.0, …)` 代之」為其全部射程。而碼面逐字（`verify/stepg_pipeline.py`）`_tele_exp = _rw_from_width(_sd_Wf) - _rw_from_width(_sd_W0)`，其 `_sd_W0` 取自 `_adv_final` 之 `W0_left`／`W0_right` ⇒ **同一個 `W₀` 另有一個消費者**，而替身⛔ 及之。

**徵候**：`W-G.9-315R` 實測——替身 `α` 使該側之 `ΣRw_實跑` 由 `85.41` 變 `88.40`（分子受替身影響），而期望式仍用 `R(W₀=0.18)`（分母未受影響）⇒ `telescoping` 由**通過**轉為**破**（`Δ=3.137`），管線改於該閘中止。替身 `β` 同理而更早中止（`R1 right`·`Δ=2.180`·到達街廓僅 `['R1']`·配地宗數 `5`／`5`）。

**根因**：替身之設計以「**受詞之名**」（`rw_increment` 之實參）為射程，而非以「**受詞之值之全部消費者**」為射程。單出前未以 AST 或字樣錨列出 `W₀` 之消費者清單——該清單之第二筆即在同一檔、同一函式、相距三十列之內。

**後果**：`W-G.9-315` 之二驅動**⛔ 為 `K-9-5-12` 之忠實反事實**；其「二替身皆仍中止」**⛔ 得讀為「該裁之落地無效」**，該批之假說（單 `§零-2` 裁 `4`）因而**⛔ 獲證亦⛔ 獲否**。耗一施工批之二次驅動。未動土地、未致錯鑄、未致錯裁。該批仍取得三項有效之量：基線逐位復現 `GB-168`；二替身之值必不同（裁 `5` 之預告坐實）；`W₀` 之第二消費者因而**被發現**。

**併記（⛔ 判其為缺陷·照實具名）**：`verify/stepg_pipeline.py` 之 `telescoping` 段註解逐字載「**W₀＝forced 角落鎖定之 buffer 起算點（無 forced → 0）**」，而 `W-G.9-278R` 實測本案 `R4 right` 之 `_fo_right` ＝ `False`（**非 forced**）而其 `W₀ ≈ 0.18`。

**攔法（三款·立為恆常附款 `m`）**：
① 凡單所定之替身，**出單前須先以 AST 或字樣錨列出該受詞之全部消費者**；
② 單內須同格具名「本替身**所截**者」與「本替身**未截**者」二欄；
③ 未截者存在時，該反事實一律具名為「**非忠實**」，其結果⛔ 讀為所擬之裁之成敗。

<!-- ZW430-END -->

<!-- ZW431-BEGIN -->

### 🩸 `自誤 431`　**單令出艙之「閘」之框為二值（破否），而正典之框為三值（已執行且通過／未執行／已執行且破），致器之離線重建值與閘之實際判被並列為同一欄**

**形**：`W-G.9-315` 單 `§四` 項 `1` 逐字令出「`R4` `right` 之 `理論@k*`／`實跑(階段1)`／`Δ` ＋ 結構閘**破否**」——「破否」係**二值框**。而 `GB-168` 一次精化 `§三` 之既有表逐字用**三值**（🟢 已執行且通過／🛑 未執行／🔴 已執行且破／⛔ 可得），且該節之全部立論即建立於「態甲之 `R4` 之 `_N=1` ⇒ 該閘**結構上不執行**」與「態乙之 `R4` 之 `_N=2` ⇒ 該閘**首次執行即破**」之別。

**徵候**：`W-G.9-315R` 項 `1` 之表載替身 `α` 之 `Δ` ＝ `0.6760000000` 而「破否」＝ 🟢 **未破**——二者在容差 `0.1` 下表面互斥。發單側自倉碼面實查二閘之**次序**：`telescoping` 在 `verify/stepg_pipeline.py` 之 `:1443`〜`:1477`，`理論＝實跑` 在 `:1553` 起 ⇒ **前者先行**；`α` 下管線已於 `telescoping` 中止 ⇒ `理論＝實跑` 閘 **🛑 未執行**，其 `Δ=0.676` 係器以 `import theo/real` **離線重建**之值，⛔ 該閘之實際判。二者並列於同一「破否」欄 ⇒ 表面之互斥由框而生。

**根因**：單之受詞框較正典**少一值**，CC 只能就其所令回答（其填「未破」係二值框下之唯一可填者）。⛔ 責 CC。

**後果**：一處讀法之歧義，已於發單側之復驗更正；未入倉錯值（報告所載之 `Δ` 與訊息逐字皆真）、未動土地、未致錯鑄。

**攔法（二款·立為恆常附款 `n`）**：
① 凡受詞涉及一個**閘**者，其出艙之框一律為**三值**——🟢 已執行且通過／🛑 未執行／🔴 已執行且破；⛔ 用「破否」等二值框；同一街廓有多閘者，**逐閘分列**並載其次序；
② 凡以器**離線重建**之值出艙者，須同格具名「**該值⛔ 為該閘之實際判**」，並載該閘於該次驅動中之三值判與其判據（`err` 之首行逐字·或「管線已於前一閘中止」）。

<!-- ZW431-END -->

**落地後之機驗（恆常附款 `j`／`l`）**：
1. `probe_WG9267_issuer_measurers.py registry` ⇒ 自誤 相異 **`421`**／MAX **`431`**（缺號 `[106, 355, 356, 357]` 未變）；`GB`／`VR`／`K-9` **一字未動**。
2. **append-only**：期末 bytes 之前綴 ＝ `§一` 閘 `8` 之自誤簿期初 bytes（`912558` B）逐位相符。
3. **payload 逐位**：自本單 blob 以整列相等定界抽出二 payload，與簿內落地段逐位相符；**期末 bytes ＝ `912558` ＋ 二 payload bytes ＋ 分隔 bytes**，三者分列（框：payload **⛔ 含**其末換行·分隔 `NL` 獨立計·恆常附款 `l②`）。
4. 🔒 哨兵 `ZW430`／`ZW431` 於自誤簿之期值 ＝ **`0`**（界列⛔ 入簿）——**如期·⛔ 讀為器紅**；須另以 `C` 形（`` `自誤 430` ``／`` `自誤 431` `` 各 `1`）＋ 一必為零造自證器非紅。全倉命中之期值 ＝ **除本單自身之產物外 `0`**（框 ＝ 子字串框·時點 ＝ 本工項之 `commit`）。

---

## `§六`　工項四：**`GB` 簿之末端登記**（主線·單獨 `commit`·**末端追加**·⛔ 鑄號）

🔑 界：`<!-- GB316N-BEGIN -->`／`<!-- GB316N-END -->`（**定界以整列相等為之**·payload ＝ 二界列之間·界列⛔ 入簿）。
落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字·**⛔ 改 `GB-168`／`GB-169`／`GB-170` 之原節、收窄與各次精化任一字**）。
🛑 **本節⛔ 鑄任何號、⛔ 解除、⛔ 收窄任何 `GB`、⛔ 提修法主張**。

<!-- GB316N-BEGIN -->

## 🔧 `W₀` 之**第二消費者** ＋ `α`／`β` 之非忠實 ＋ 態乙之強制抵費地集合（`W-G.9-315` 二替身·`W-G.9-316` 工項四·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為末端追加（`戒 36`）；**⛔ 解除、⛔ 收窄 `GB-168`／`GB-169`／`GB-170`**；⛔ 改其原節、收窄與各次精化任一字。
🩸 **本節之由**：`W-G.9-315` 所令之二替身其射程未涵蓋 `W₀` 之全部消費者——該疏失已鑄為 `自誤 430`（`W-G.9-316` 工項三）。本節只登記**其量與其碼面之逐字**，⛔ 據以改任何判。

### 一　`W₀` 之二消費者（碼面逐字·⛔ 判）

| # | 消費者 | 逐字 | `W-G.9-315` 之替身是否截之 |
|---|---|---|---|
| `1` | `rw_increment` 之鏈頭實參 | `solve_G_binary` 之呼叫實參 ＝ `_rw_start, W`（`GB-168` 二次精化）；`_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)`（`GB-169` 受詞欄） | **是** |
| `2` | 結構閘 `telescoping` 之期望式 | `_tele_exp = _rw_from_width(_sd_Wf) - _rw_from_width(_sd_W0)`；`_sd_W0` ＝ `_adv_final['W0_left'／'W0_right']` | **否** |

🔒 **碼面自身之註解逐字**（同段）：「**W₀＝forced 角落鎖定之 buffer 起算點（無 forced → 0）**」；而 `W-G.9-278R` 實測本案 `R4 right` 之 `_fo_right` ＝ `False`（非 forced），其 `W₀` 於 `W-G.9-315` 之落檔印為 `0.18`（`2dp`）。🛑 **⛔ 判其為缺陷、⛔ 提修法**——照實具名。

### 二　二替身之實測（態乙·`W-G.9-315` 落檔·框 ＝ 各落檔之列框）

| 量 | 基線（替身關） | 替身 `α` | 替身 `β` |
|---|---|---|---|
| `R4 right` `理論@k*`（重建） | `87.8350000000` | `87.7240000000` | 【⛔ 可得】 |
| `R4 right` `實跑(階段1)`（重建） | `85.41` | `88.40` | `0.0`（未到達） |
| `telescoping` 閘 | 🟢 已執行且通過 | 🔴 **已執行且破**（`R4 right`·`Δ=3.137`） | 🔴 **已執行且破**（**`R1 right`**·`Δ=2.180`） |
| `理論＝實跑` 閘 | 🔴 已執行且破（`Δ=2.425`） | 🛑 **未執行** | 🛑 **未執行** |
| 到達之街廓 | `['R1','R2','R4']` | `['R1','R2','R4']` | **`['R1']`** |
| 配地宗數 `0m`／`3.5m` | `20`／`22`（＝ `GB-168` 原節逐位） | `20`／`22` | **`5`／`5`** |

🔒 **二閘之次序**（碼面實查）：`telescoping` 段先於 `理論＝實跑` 段 ⇒ 前者破則後者**未執行**。🛑 「**未執行**」⛔ 讀為「未破」（`GB-168` 一次精化 `§三` 之既有分法）。
🔒 ⇒ **`GB-168` 現態 ＝ 🔴 未解除**，其四項解除條件**無一於該批成就**；且因替身非忠實，**`K-9-5-12` 之落地與 `GB-168` 之關係於該批⛔ 獲證亦⛔ 獲否**。

### 三　態乙之強制抵費地集合（射程外之新量·⛔ 在既有登記內）

`3.5m`·態乙·基線之 `抵費地面積＝range(㎡)`（源 ＝ `run_corner_pk` 之第三回傳 `_off`）＝ **三格**：`R5` 左 `299.73`／`R2` 左 `308.17`／`R3` 右 `307.79`。
其中 `R2` 左 `308.17` 與 `GB-170` 倉載**逐位相符**；`GB-170` 另載之 `0m` `R4` 左 `116.08` 與 `3.5m` `R4` 左 `226.01` 於態乙為【**⛔ 可得**】——其由 ＝ **態乙之強制抵費地集合本即與態甲不同**，且態乙之管線未達 `R4` 之該端。
🛑 **⛔ 讀為「`GB-170` 之三格相異」或「該項可解除或收窄」**——此僅為態乙之集合實測。

<!-- GB316N-END -->

**落地後之機驗**：`GB` 框之計數 ⇒ `GB` 相異 **`168`**／MAX **`170`**／缺號 `[12, 87]` **一字未動**（本節⛔ 鑄號）；自誤 `421`／`431`；append-only：期末 bytes 之前綴 ＝ `§一` 閘 `8` 之 `GB` 簿期初 bytes（`924914` B）；`CR` ＝ `0`；哨兵 `GB316N` 於 `GB` 簿之期值 ＝ **`0`**（界列⛔ 入簿·如期·⛔ 讀為器紅），其全倉命中 ＝ **除本單自身之產物外 `0`**。

---

## `§七`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**，🔑 **唯一例外 ＝ `verify/probes/probe_WG9315_k95_12_stateB.py`**（`§四-1` 之一行修·刪 `1` 增 `1`·其 `diff` 逐字出艙）；同格載受檢檔數 `> 0` |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動之 `.md` 之 `CR` | ＝ `0`；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末 | 自誤 `421`／`431`；`GB` `168`／`170`·`K-9` `39`／`40`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（自誤簿 ＋ `GB` 簿） | 各自之期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes（`912558`／`924914`） |
| `6` | 自限復驗 | `verify/baselines` `298` 列·`a898f4e1…` 未動；`data/` 全樹未動；`CLAUDE.md`／`K-6` blob 未動；**底本探針 `85acdbe6…` 一字未動**；**`verify/out/` 既有 `919` 檔逐位未動**（本批新落檔另列）；`refs/heads/verify/*` `20` 支·`gb170` `0`·`p3a` `rc` `1`；`WV_BAKE` ＝ `None`；驅動**外**之 `WV_K6_STEP0` ＝ `None` |
| `7` | 標籤對照 | 本報告全部計數標籤逐欄具名其**框**、集合與基數（**當場以程式自資料列計數**）；凡命中之數並載其**時點**；凡閘之判一律**三值框**（恆常附款 `n`） |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`〜`6` 任一不如期 ⇒ **停、上呈**（照實全量出艙·⛔ 判改法）。

---

## `§八`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；新增 `probe_WG9316_w0_consumers.py`（**唯讀·⛔ 驅動**）；`probe_WG9315_k95_12_stateB.py` 之**一行**排序鍵修；以 `α`／`β` 各驅動一次並出艙 `§四-2` 之三項；`自誤 430`／`431` 之鑄；`GB` 簿之末端登記節。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ 改底本探針 `probe_WG9278_w0zero.py` 一字；⛔ 改 `probe_WG9315_k95_12_stateB.py` 之 `§四-1` 一行以外之任何一字；⛔ 新增或修改其他任何探針；⛔ `run_verification`；⛔ 改 `verify/out/` 既有 `919` 檔任一位元組；
🛑 ⛔ **設計或施加替身 `γ`**（或任何新替身）；⛔ 改 `α`／`β` 之定義一字；⛔ 逾二次驅動；
🛑 ⛔ **落地**、⛔ 稱本批為「落地」、⛔ 判 `K-9-5-12` 應否落地、⛔ 判 `K-9-5-12（五）3.` 之「未決」後半款；⛔ 模擬**情形乙**；
⛔ 判孰為正確之 `W`、⛔ 判理論側與實跑側何者為是、⛔ 判 `W₀` 之二消費者何者應改、⛔ 稱任何處為「缺陷」、⛔ 提修法主張、⛔ 擬任何 diff、⛔ 判結構閘應否放寬或停用；
⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`；⛔ 鑄 `自誤 430`／`431` 以外之任何號；
⛔ 開分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字（`§四-1` 所授之一行除外）；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——遇可觀測之硬限事件一律 loud 照實具名、停、上呈；
🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**（恆常附款 `h`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§九`　報告之格

`docs/reports/W-G.9-316R_W0消費者定位與項5補測及自誤430_431_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `3` 之 🟡 逐項具名處置、閘 `11` 之自跑與二對照）⇒ `§三` 工項一（`a`〜`f` ＋ 判別力二造）⇒ `§四` 工項二（一行修之 `diff` 逐字 ＋ 三項）⇒ `§五` 工項三（四款機驗）⇒ `§六` 工項四（機驗·含 `GB` 框未動之證）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其**框**、母體與產生指令；凡命中之數並載其**時點**；凡閘之判一律**三值框**；**計數標籤一律當場以程式自資料列計數**。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框與 `§零-3` 同源；四簿框於 `1b2c400` 實跑 ＝ `419`／`429`｜`168`／`170`｜`80`／`95`｜`39`／`40`。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`registry`／`自誤 430`／`自誤 431`／`_tele_exp`／`rw_increment`／`_rw_start`／`未執行`／`ΔΣG`／`WV_K6_STEP0`／`W-G.9-309`／`1b2c400`／`data/V6.dxf`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: e1c0785b27c80ace18a1f74fdd48a881eff81f79ec0f4e23c2ebf15c7a4d9a86
