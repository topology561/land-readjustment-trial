# W-G.9-268′　補令十六（KL 之 `c3` 放行之登記 ／ 併線授權 ／ `c4` 差異表之必載）

> **受詞** ＝ `W-G.9-268′` 之施工窗。
> **主線期初態** ＝ `7eaa485248239863ac2ac6d992e7b00e8d6b30f9`｜**側支** `verify/W-G.9-268p-c3` ＝ `9f8d980cdd0f1e167dbb6db8afc19cf1cb7565b8`（皆須 **`git ls-remote`** 實查·⛔ 採信本地追蹤 ref）。
> 🔒 原單、補令一〜六、補令八〜十五、本補令**並存**；相斥處以**時序最後者**為準。
> 🔒 本補令**⛔ 改**補令十五一字——其 `§四-3` 之末次 rebase、`§六` 之序 `1`〜`5` 悉依原文。

---

## §一　KL 之裁（`2026-09-11`·逐字登記）

> **「同意 `c3` 併線」**

**受詞**：補令十五 `§二` 所呈之 `c3`——`app.py` `+4/-5`（移除 `import os as _os_a` ＋ `if _os_a.environ.get('WG9268P_ANCHOR_GEOM', '1') != '1': return cum_S` 三列可執行碼 ＋ 二列 docstring），`bytes 1458081`／blob `sha1 1d7d7c7324719f13…`／file `sha256 b74fd07c3c86a914…`。

### 一-1　本裁之射程（發單側所界·⛔ 擴張）

| 及於 | ⛔ 及於 |
|---|---|
| `c3` 之併線（含補令十五 `§四-3` 末次 rebase 後之同一 `app.py` 內容） | **`c4`**——其為獨立工項，惟零生產碼，依 `常規一 🔧 一` 得逕推，⛔ 另呈 KL |
| 補令十五 `§六` 序 `1`／`2` 之零生產碼物件隨側支併入 | **`GB-159` 之解除**——中間態之配地圖與負擔表仍**⛔ 對外**，俟 `W-G.9-269` 三條件齊備方解 |
| | **`W-G.9-269`**（病一）之任何工項 |

🛑 **失效條款（重申補令十五 `§六`）**：序 `3`（發單側重驗）若 `app.py` 之 `bytes`／blob `sha1`／file `sha256` **任一相異** ⇒ **停機上呈**，**本裁⛔ 及於該情形**。KL 所裁者為**該內容**之併線，⛔ 為「側支之任何後續物件」。

---

## §二　併線之授權

> **序 `3` 發單側重驗通過後，CC 得將主分支 `wip/s1-endpart` <u>快轉</u>推進至側支 tip。⛔ 再問 KL。**

🛑 推進前須以 `git ls-remote` 實查主線仍為 `7eaa485…`（或補令十五 `§六` 序 `1`／`2` 所落之側支物件未改變主線）；若主線已前進 ⇒ **依補令十五 `§四-2`**：該前進即為違例，須具名其成因並重 rebase 重驗，⛔ 逕併。
🔒 **併線完成之時態**須載於 `c4` 之報告，並作為 `GB-159` 現行有效期間之起點之後續錨。

---

## §三　`c4` 之差異表：**必載三項**（⛔ 省）

`c4` 為零生產碼，惟其產出係**唯一呈 KL 之土地文件**，故其形須明定：

1. **`表三` 之 `ΣΔ` 與值域，其 `min`／`max` 須<u>各自具名其所屬之宗與歸戶</u>**（`自誤 343` 之通則）。逐字已定之值：
   `0m` 值域 **`−3.5900`〜`+3.5100 ㎡`**（`min ＝ 628-42(1)`／`G011`；`max ＝ 628-39(1)`）；
   `3.5m` 值域 **`−0.9900`〜`+3.6000 ㎡`**（`min ＝ 628-53(2)`／`G025`；`max ＝ 628-7(2)`）。
   🛑 **⛔ 以區間掩去負值之個體**——負值之個體即受損之地主。
2. **二宗近乎零之配地須逐宗單列**（`GB-159` 之受詞）：`628-42(1)`（`G011`）`3.82 → 0.23 ㎡`·寬 `0.76 → 0.18 m`；`628-53(2)`（`G025`）`0.99 → 0.00 ㎡`·寬 `0.39 → 0.02 m`；並載其 `畸零地旗標` 改前改後**皆**為 `⚠️移出/第1調配順位`，及 **`GB-159` 之「⛔ 對外」與其解除條件**。
3. **`表二` 守恆**：逐街廓 `ΣG`／`Σ池`／街廓面積／殘差，`Σ池` 之取數端須具名為**獨立量測之池**（`推進側別 == '抵費地'` 之 `幾何面積(㎡)`），**⛔ `街廓面積 − ΣG`**（該式使殘差恆真·`自誤 245` 族）。

🔒 **域語言**：表內⛔ 出現程式術語、變數名、行號、旗標名。
🔒 `GB-67` 之末端追加（`c4` 之另一受詞）**⛔ 追改其原文與歷次加註一字**。

---

## §四　鑄號之授權

🔒 **本補令⛔ 授任何鑄號**（`自誤 350`／`351` 之授權見補令十五 `§五`，仍有效）。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §五　續辦

悉依**補令十五 `§六`** 之序 `1`〜`5`。本補令與補令十五**同批入倉**（`docs/orders/W-G.9-268p_補令十六_KL之c3放行與c4差異表.md`·**落側支**），⛔ 動一字。

🔒 **`W-G.9-269` 之前置（累計·⛔ 本批辦）**：`(i)` `A` 母體須以新錨態重量·⛔ 沿用 `2` 宗／`4.7950 ㎡`；`(ii)` `GB-160` 閘 `γ` 重量；`(iii)` `GB-161` 全族（聯集 `19`）之修與效力判；`(iv)` `GB-159` 三條件之逐條結清。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
