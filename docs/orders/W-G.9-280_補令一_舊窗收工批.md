# W-G.9-280　補令一（**舊窗收工批**：`自誤 382` ／ 坑 `bf`·`bg` ／ 交接註 ／ 收工 ⇒ **停 ⇒ 本窗封存**）

> **受詞** ＝ 現施工窗（**辦畢即封存·⛔ 再用**）。
> **主線** ＝ `00db72322ff400c7fe00ab6542cb7ff7ecde057d` **或其後裔**（🛑 `ls-remote` 實查）。
> **凍存** `p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
> **此單此前是否已交付** ＝ **否**。｜**分級** ＝ **輕**（純登記 ＋ 交接註·**⛔ 長跑**·**零生產碼**）。
> 🔴 **換窗之由**：本窗 context 已達 `82%`。下一批之受詞（`W_0 = 0.1775` 之產生鏈、`K-9-5-12` 之碼側落地）**直接影響配地面積幾何** ⇒ **⛔ 於飽和窗內為之**。

---

## §零　開工閘（**⛔ 略**·**⛔ 驅動任何長跑**）

| # | 受詞 | 期值 |
|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `00db723…` 或其後裔 |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須 **非祖先** |
| `4` | **`SELF_SHA256`**（`P-5` 內建口徑） | 見本檔**末列**；🛑 **閘器須印出其所量之檔名並與本單號對拍**（坑 `bf`） |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 紅 ⇒ 停機款；其餘逐項具名 |
| `6` | 四簿 ＋ 坑 | 自誤 `371`／`381`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 **`83`／`83`** |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（坑 `be`） |

---

## §一　發單側之復驗與核可（`W-G.9-279`）

**機械閘全批核可**（發單側自倉自跑·rev `00db723`）：三 commit 鏈相符；`2bf3458..HEAD` 對**正面列舉之 `34` 檔** `numstat` ＝ **空**；blob 期初＝期末；`p3a` 非祖先；自誤 相異 `371`／MAX `381`（**恰 `+1`**）·三簿⛔ 動；坑 `83`／`83`。

**已為【倉】者**（核可）：
1. **主檢母體非空、二造異色·器非紅**：全母體 `48` 列·有值 `22`；甲[必為零] `W_0 ≤ 0` 之 `16` 格 `R(W_0)` **全 `== 0`**；乙[必非零] `W_0 > 0` 之 `6` 格 `R(W_0)` **全 `> 0`**。**`R(W_0) > 0` ＝ `6` 格**（情形甲 `4`／情形乙 `2`）。
2. 🟢 **情形乙與 `K-9-5-12（四）` 之對拍 ＝ <u>逐位相符</u>**（`Δ = +0.000e+00`·二可量格；第三格 `3.5m R2 left`【⛔ 可得】），判別力甲[必不符]（近側 `corner_pt`）⇒ ⛔ 相符 ✅。
3. **態乙 `R4 right`**（二情境皆然）＝ 情形甲 ∧ `_fo_right = False` ∧ `is_chain_head = True` ∧ **`R(W_0) = 3.0885070835`** ⇒ 目前**唯一**落在 `K-9-5-12（五）1.` 射程內之疑似點。
   🔑 **發單側併記**：該值**逐位等於** `GB-168` 之 `Δ` 分解中之 `+3.088507`（`W_prev` 之扣除項）⇒ 二登記項之數值鏈於此**閉合**。

**三則自捕全部核可**，其 ① 與 ② **准鑄為坑**（`§三`）；③（收工閘之框以字樣 `shim`／`替身` 掃得倉內既有 ⇒ 改以「`34` 檔 blob 期初＝期末」為決定性受詞）**⛔ 鑄坑**——其形已在 `自誤 381` 之通則射程內（受詞須取性質、⛔ 取字樣），於交接註具名即足。

## §二　🩸 `自誤 382` 之鑄（**責 ＝ 發單側**·逐字轉錄·**⛔ 改寫**）

> **形**：**以一個未具名其母體之計數，於次一單中充作<u>另一母體</u>之計數。**
>
> **本案**：`W-G.9-278R` 出艙「`W_0 > 0` 者 `3` 格恰全落情形乙」——其母體係該批之**受限子集**。發單側於 `W-G.9-279 §一`／`§二-4` 之**事實欄**逐字轉為「`W_0 > 0` 者 `3` 格恰全落情形乙」「生產態 ⇒ **僅**情形乙之 `3` 格」，充作**全母體**之計數。
> **實測否證**（`W-G.9-279`）：全母體 `48` 列·有值 `22`；**`R(W_0) > 0` ＝ `6` 格**（情形甲 **`4`**／情形乙 `2`）；**態甲 ∩ 情形甲 `11` 格中 `R(W_0) > 0` 者 `1`**（`0m R2 left`·`3.3e-9`）⇒ 「行為上等同自 MP 起量」成立於 **`10`／`11`·⛔ 全部**；單所指之第三格 `3.5m R2 left` 實為 **【⛔ 可得】**。另：`0m` 態乙之 `_off` ＝ **`0` 列**（⛔ 任何強制抵費地），`3.5m` 態乙 `3` 列（含 `R3 right`／`R5 left`·**態甲所無**）。
>
> **通則**
> > **凡轉引前一批之計數，須連同其<u>母體之定義</u>逐字轉引，並於新母體下<u>重量</u>；⛔ 以「同一受詞」充「同一母體」。** 承出艙三要件（框／時態錨／角色）：**母體係「框」之一部，⛔ 可省。**
> 🔒 **連帶處置**：`W-G.9-279 §一`／`§二-4` 之事實欄**⛔ 追改**（已入倉）；以本節為準。CC 之「照實併記·⛔ 判孰誤·⛔ 改單一字」**正確**。

**節題之形須與既有十一則同構**。**取號現查**依三形聯集框；對照組甲 `自誤 381`／甲2 `380`（皆須 `>0`）／乙 **人造哨兵（執行期組出·字面⛔ 出艙**·坑 `bd`**）** 須 `0`。

## §三　🆕 坑 `bf`／`bg` 之准鑄（**二坑**）

| 標籤 | 形／徵候／攔法 |
|---|---|
| `bf` | **形** ＝ **量測器讀到<u>他批</u>之受詞，而其輸出一切正常**（字串替換未生效 ⇒ 器仍指前一單之檔）。**徵候** ＝ `SELF_SHA256` 相符、preflight `rc = 0`、各欄皆綠——**徵候只在標題之號**。**攔法** ＝ 閘器一律**印出其所量之檔名**並與本批之單號**對拍**，附 **[必為偽] 對照**（一他批之號須判不符）；⛔ 以「各欄皆綠」充「受詞正確」 |
| `bg` | **形** ＝ **以框內省 climb 外層取內部幾何之參數，而該參數於呼叫時點<u>尚未賦值</u>或<u>已被替換</u>**。**徵候** ＝ 還原自我驗證閘 `0`／`22`；**若無該閘，其所印之 `W(遠側)` ＝ `73.8689182960` 是一張漂亮而錯的表**。二根因：`_side_mid_left` 於 `_corner_buffer_S` 呼叫時**尚未賦值**（取到**前一街廓**之值）；`app.py` 逐字「**街角第 1 宗之 `allocation_dir` 已換為 `_first_corner_alloc_dir(side_mid)`**」。**攔法** ＝ `(1)` 一律取**受測函式之參數本身**，⛔ climb 外層；`(2)` 凡探針還原內部幾何，**必設還原自我驗證閘**（還原值與碼面值逐位對拍·**分母須為全母體**） |

🛑 `ALPHA_LABELS` 現止於 `be` ⇒ **須同批擴至 `bg`**（`+2`）＋ **三處**框字串齊一。判別力：擴後尾元須為 `bg` 且成員數 `+2`（**先實測後填**）。

---

## §四　工項（**零生產碼·⛔ 驅動任何長跑**）

| 序 | 事 |
|---|---|
| `0` | 本單原封入倉（`§二-B` 搬移法·搬移前「所指之檔數 ＝ `1`」自證·**併受詞自證**·坑 `bf`） |
| `1` | 鑄 **`自誤 382`**（`§二`）＋ **坑 `bf`·`bg`**（`§三`）＋ `ALPHA_LABELS`→`bg` ＋ 三處框字串齊一 |
| `2` | **交接註**（`§五`）⇒ 入報告 `W-G.9-280R` |
| `3` | **收工閘**（`§六`）⇒ **停 ⇒ 本窗封存** |

---

## §五　交接註之定式（入報告·七項）

1. **結案 commit** 與本批逐 commit 之 sha；
2. **四簿期末**（逐簿具名其框）＋ **坑之期末數**；**次號**（⛔ 預寫·實測後填）；
3. **自限復驗**：`app.py` blob 期初＝期末／`baselines` `298`·`a898f4e1…`（**口徑 ＝ `core.quotePath` 預設**·`自誤 379`）／`p3a` 未併線／`WV_BAKE` 未設／`K-9-29` 任何子項 ⛔ 落地／`GB-167`·`168`·`169` ⛔ 修；
4. **本波新立之通則逐字**：`(a)` 受詞須取**性質**、⛔ 取代理量或字樣，且須先查下游有無**使不同輸入等價之分支**（飽和／clamp／`<=0` 早退／`round`）（`自誤 381`）；`(b)` 轉引前批之計數須連同**母體定義**並於新母體**重量**（`自誤 382`）；`(c)` 對照組之必命中性須**逐態**成立（`345` 再現二）；`(d)` ⛔ 以「未執行」充「已通過」（`378`）；`(e)` 以「器之構造」否定實測數者，該構造之主張**本身**須先自倉查證（`380`）；`(f)` git 輸出為框者須具名 `core.quotePath`（`379`）；`(g)` 結構化物件之基數取**語法真值**（`377`）；
5. 🩸 **本會期自捕之全表**（形／徵候／攔法·含 `bb`／`bd`／`be`／`bf`／`bg` 五坑之由來，及「收工閘之框以字樣掃得倉內既有」一則**⛔ 鑄坑者**）；
6. **起手清單**（取**本批收工後**之倉態·供新窗用）；
7. 🔴 **未結項之逐條**，須逐字載明：
   - **`GB-168`**：`Δ = 2.425` ＝ `−0.661141`（理論側餵 `round(S×|cos|,2)`）＋ `+3.088507`（`W_prev` 之扣除）；實拋者係**第二閘**；`Δ_i` 閘於界面 `0` 時判定組為空；`solve_G_binary` 現行實參 `(_rw_start, W)` 而 `_rw_start` 於鏈頭**即 `_W_near`** ⇒ **改名⛔ 等同改行為**。**⛔ 解除、⛔ 收窄**；
   - **`GB-169`**（受詞已收窄）：射程 ＝ `R(W_0) > 0` 之側；**情形乙二可量格與 `（四）` 逐位相符**；**唯一疑似點 ＝ 態乙 `R4 right`**（`R(W_0) = 3.0885070835`·情形甲·`_fo_right = False`·`is_chain_head = True`）；`（五）3.` 之「未決」後半款**待 KL**；
   - **`GB-167`** ⬜ 未修（`_WF_NS_NAMES`（AST 真值 `35`）⛔ 含 `_wg9268p_anchor_advance` ⇒ app 路徑必 `KeyError`·**生產碼·須 KL 放行**）；
   - `GB-153`／`159`／`160`／`162`（**三禁令**）／`164`／`165`／`166`／`GB-7`；`ok_t`／`ok_f` **⛔ 改一字**；
   - **`K-9-29`**：子項 `2` 已落地；子項 `1` ⬜ 未落地且**⛔ 為旗標翻轉**（`GB-168`＋`GB-169` 為其先決）；子項 `3` 隨子項 `1` 消解；子項 `4` 屬五級實作；
   - **情形甲 ∧ `R(W_0) > 0` 之 `4` 格中，尚未逐格具名者**——新窗須先補其具名；
   - `p5`／`p6` 之事實、`K-9-12` **待 KL**。

## §六　收工閘（**commit 前·與被守護之動作分開呼叫**）

1. `numstat` 刪除欄逐檔具名（`ALPHA_LABELS` 之就地更正為唯一非零者·⛔ 前綴式）。
2. **生產碼判法四檔**命中 `0` 行（母體**正面列舉 `34` 檔**·坑 `be`）⇒ 零生產碼 ⇒ 逕推（`git push origin HEAD:refs/heads/wip/s1-endpart`）。
3. `.md`／`.py` 之 `CR` ＝ `0`；前綴檢之判別力二造。
4. **四簿期末**——**期值**：自誤 相異 **`372`**／MAX **`382`**；`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`（**三簿⛔ 動**）。🔴 期值⛔ 為現值。
5. **坑之定義處清單自證**（`通則 I`·報告入倉**後**量）——**期值 `85`／`85`**。
6. **自限復驗**：`34` 檔 blob **期初＝期末**（決定性受詞·⛔ 以字樣掃）／`baselines` `298`·`a898f4e1…`（口徑 ＝ `quotePath` 預設）／`p3a` 未併線／`WV_BAKE` 未設。
7. push 後 `ls-remote` 實查其值方得稱已推。

## §七　鑄號之授權（**⛔ 逾此**）

**`自誤 382`**／**坑 `bf`**／**坑 `bg`** ——三號，**⛔ 逾**。🛑 `GB`／`VR`／`K-9` 本批 **⛔ 鑄**。

## §八　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字；⛔ 改五個 `*_EXPECT` 任一字；⛔ 修 `GB-167`／`168`／`169`。
2. **⛔ 驅動任何長跑**（含 `run_verification`、任何雙態或替身驅動）——本窗已飽和。
3. ⛔ 落地 `K-9-5-12`／`K-9-29` 任何子項；`GB-162` 三禁令維持；⛔ 引任何自 baseline 取得之數。
4. **⛔ 判態乙 `R4 right` 為違反、⛔ 判 `（五）3.` 之未決後半款、⛔ 提修法主張**。
5. **⛔ 追改 `W-G.9-279` 或任何已入倉之文件一字**。
6. **⛔ 呈 KL 任何事**；⛔ 併線 `p3a`、⛔ 刪任何 ref。
7. **⛔ 於任何入倉文件宣稱一件尚未發生之事**——及於 commit 訊息與「已推送」之陳述。

**辦畢 ⇒ 停 ⇒ 本窗封存。** 新施工窗之首則為另行交付之**開場文 `W-G.9-281`**。

*本單受詞 ＝ CC 施工窗。發單側 ＝ claude.ai。⛔ 呈 KL。*
SELF_SHA256: b7b0d6c49648ae3871f087cf5e218040386748716b4307241ef57484ff960a6e
