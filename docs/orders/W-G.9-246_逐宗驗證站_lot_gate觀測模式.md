# W-G.9-246　逐宗驗證站 **`_lot_gate`**（單一真相源）＋ 四站掛載——🛑 **觀測模式**（判而⛔ 動控制流）

> 🔴 **生產碼**：`app.py` ＋ `verify/stepg_pipeline.py` **二檔**（鏡像·`#20` 同改），**行為零變更**。
> 工項零（入倉）得逕推；工項一–三 **一 commit → 側分支 `verify/W-G.9-246-c1`**，候發單側復驗、KL 放行。
> 🛑 本批**只判不動**：verdict 只寫入新欄，**⛔ 依 verdict 剔宗、⛔ 改鏈推進、⛔ 改任何既有欄一字**。
> 接閘（依 verdict 剔宗＋整包回滾）＝ **次一單**，須先看本批之觀測統計。
> 🛑 ⛔ 動 `verify/` 其他檔、⛔ 實作 `r2`／五級、⛔ 動 `_solve_G_one`／`_corner_first_lot_G` 一字。

---

## §零　開工閘（缺一即停）

**`S-0`**　最末 `SELF_SHA256` 依既有口徑重算對拍。

**`S-1`（宣告框·三數分列·補款 `⑤⑥`）**　發單側於 `blob@a9e84c237ddb20f5a8781950aff77084cb2cfa19`／全 `docs/` **569** 檔（`19` 二進位）：

| 號 | `D1` | `D2` | `D3` | 宣告框 | 鬆框 | 角色 |
|---|---|---|---|---|---|---|
| **`W-G.9-246`** | 0 | 0 | 0 | **0／0** | 1／1 | ✅ 未占用 |
| `W-G.9-245` | 1 | 1 | 0 | 1／1 | 4／5 | 對照甲 ✅ |
| `W-G.9-247` | 0 | 0 | 0 | 0／0 | 0／0 | 對照乙 ✅ |

**`S-3`（生產路徑錨）**　十檔同 `a9e84c2`（`app.py ad7a91dba08119f5`／`stepg_pipeline.py 0260273350021311`／餘同前）。
**本批只得動 `app.py`、`verify/stepg_pipeline.py` 二檔**；餘八檔前後逐位不變。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-246_逐宗驗證站_lot_gate觀測模式.md`。

---

## §二　工項一：`_lot_gate`——**判定式之單一真相源**（`app.py`·module 級純函式）

### `a`　地位

同 `_solve_G_one` 之先例（`P-0b`·裁定 M·`Q-M4`）：**判定式只寫一處**，harness 經 `ns["_lot_gate"]` 取用。
⛔ 讀 `st`、⛔ 改入參、⛔ 有副作用。典據 ＝ `配地計算總規格_v3.md` 之 ⑧–⑪（更正節為準）。

### `b`　四項判定（各為**四態**：`合格`／`不合格`／`不適用`／`不可判`）

| 項 | 判 | 適用 | 沿用之既有函式 |
|---|---|---|---|
| **A 幾何** | 宗地範圍可完整容納 `W × D` 矩形（`K-9-12`·可旋轉平移） | 非街角第 1 宗之全部宗 | `K-9-12` 現行判（`app.py:11755` 一帶）／`_k923_gate2(:12203)` |
| **B 藍影** | `G(第2宗) > area(藍影)` **嚴格大於**（`K-9-23` 閘一） | **僅有 sideline 之鏈之第 2 宗**（KL 語序；碼側 index `1`） | `_k923_gate1(:12174)` ＋ 藍影構造（`:12057` 一帶） |
| **C 面積** | 有效值 `e > 0` ⇒ `G ≥ e`；街角第 1 宗 ⇒ `G ≥ max(街角規定範圍面積, e)`（⑩） | `e > 0` 之宗；`e = 0` ⇒ `不適用` | `k91_effective_min_build_area`／街角規定範圍（`_build_corner_range_v3`） |
| **總判** | `rejected ＝ 任一項為「不合格」` | — | — |

🔒 **街角第 1 宗**：A ⇒ `不適用`（`K-9-12-e`）、B ⇒ `不適用`；只走 C 之 `max` 式。
🔒 **末端塊之鏈**：B ⇒ `不適用`（無 sideline·`K-9-23 三` 加註）。
🔒 **`不可判`**：所需輸入於掛載點**取不到**者——記其缺者之名，**⛔ 當合格、⛔ 當不合格、⛔ 停機**（觀測）。
🔒 **宗序對照**：KL 語「第 1／第 2 宗」＝ 碼側 `K-9-23` index `0／1`；函式簽章用 KL 語序命名。

### `c`　簽章（形由 CC 定·下列為**必有**之入出參）

入：`res`（`_solve_G_one` 之回傳·含地籍範圍與 `G`）、`tp`、街廓 ctx（`blk_poly`／`front`／`baseline`／`category`／`front_road_width`／街角地多邊形與側別）、`eff_min_build_by_blk`（**harness 經 ctx 傳入·預設 `{}` ⇒ 全 `不適用`**）、`chain_side`、`is_corner_first`、`is_second_after_corner`。
出：`dict`——四項各載 `verdict` ＋ **量**（A：`W×D`；B：`G`／`area(藍影)`／`Δ`；C：`G`／`e`／街角範圍面積）＋ `rejected` ＋ `unknown_inputs`。

---

## §三　工項二：四站掛載（**鏡像·`#20` 同改**·⛔ 改既有欄）

| 站 | 檔:列（`a9e84c2`） | 掛載點 |
|---|---|---|
| app 左鏈 | `app.py:21694` 之**前** | `res` 定案後、`_W_prev_left = res['W_far']` **之前** |
| app 右鏈 | `app.py:21798` 之前 | 同上（右） |
| harness 左鏈 | `stepg_pipeline.py:676` 之前 | 同上 |
| harness 右鏈 | `stepg_pipeline.py:767` 之前 | 同上（🔒 右鏈有二次 solve `:737`／`:751`·掛於**定案之 `res`** 後，⛔ 各 solve 後各掛） |

🔒 四站**只做二事**：`v ＝ _lot_gate(...)`；把 `v` 之各欄以 **`驗_` 前綴**寫入該宗之 row（`g_rows`／`conv_rows`）。
🛑 **⛔ 依 `v` 寫任何 `if`**——⛔ `continue`、⛔ 跳過推進、⛔ 改 `_W_prev_*`／界面單線鏈（`D-2b-23【甲】`）。
🔒 harness ctx 加 `eff_min_build_by_blk`（預設 `{}`）；app 側自 `st.session_state[K91_SS_MBA_EFFECTIVE]` 取。

---

## §四　工項三：驗收（本批之要義 ＝ **第一次可鑑別之 `V-A`**）

**`V-A` 迴歸（🔴 本批為真·非構造性）**　拋棄式 clone 改前／改後各實跑 UC9898 二情境至自然中止；
Step G 之 `g_rows`／`conv_rows` **去除 `驗_*` 新欄後逐位 diff 空**。⛔ 主 checkout。
🔒 併證 `V-A` 之判別力：改後之 `驗_*` 欄**非全空**（否則量測器未接）。

**`V-S` 觀測統計（🔴 次一單之地基·⛔ 略）**　每情境 × 每街廓 × 每鏈：A／B／C 各四態之計數；
**不合格宗逐宗具名**（暫編地號、鏈、項、量、與閾值之差）；`不可判` 逐宗具名其 `unknown_inputs`。
🔒 harness 因 `eff ＝ {}` ⇒ C 恆 `不適用`——**須具名**，⛔ 讀為「面積判全過」。

**`V-B`** 二檔 diff 逐行人閱。**`V-C`** 十檔雜湊：僅 `app.py`／`stepg_pipeline.py` 變。
**`V-D`** `_lot_gate` 之純函式對拍（合成輸入·四項各造 `合格`／`不合格`／`不適用`／`不可判` 至少一例；含邊界 `G ＝ area(藍影)` 須 `不合格`、`G ＝ e` 須 `合格`）。

---

## §五　上呈（候 KL·⛔ CC 自裁）

本批綠後，發單側以 `V-S` 之統計呈 KL：**若接閘，UC9898 二情境各有幾宗、哪幾宗、因哪一項會被剔**。
KL 據以裁次一單（接閘＋整包回滾）之出單。若不合格宗數異常（`0` 或近全數）⇒ 先疑判定式、⛔ 逕出接閘單。

---

## §六　產出

工項零逕推。工項一–三一 commit → `verify/W-G.9-246-c1`（`wip/s1-endpart` ⛔ 動）→ 發單側復驗 → KL 放行 → 依 `-244` 之 `-c2` 同一性閘或快轉併線。
凡出數載三軸；宣告框三數分列。

SELF_SHA256: a3087deadad44031d669866c858704c13ac4a51a2cae6261dce409e1e860a42f
