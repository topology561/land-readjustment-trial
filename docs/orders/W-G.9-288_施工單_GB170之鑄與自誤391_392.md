# `W-G.9-288`　施工單（**輕批·登記**）：鑄 `GB-170` ／ `自誤 391`／`392` ／ 三則末端追加

> **態**｜開工 ＝ `005fc6a080c2f70dc9df9efef6d64f3e8c34a460`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-288_施工單_GB170之鑄與自誤391_392.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **輕**（純登記·**零生產碼**·⛔ 量測新物·⛔ 替身·⛔ 反事實）
> 🛑 **本批鑄號 ＝ `GB-170` ＋ `自誤 391`／`392`**（逐字全由本單供）；`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🛑 **本批與 KL 之放行並行**，其結果**⛔ 影響**任何待 KL 之項。**辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值／判準**（🆕 **同格載產生該框之指令逐字**·由 ＝ `自誤 392`） |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `005fc6a080c2f70dc9df9efef6d64f3e8c34a460` 或其後裔（**全 `40` 碼**·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`）；判別力[必為祖先] ＝ 本批基座之任一祖先 `commit`（`rc = 0`） |
| `4` | `SELF_SHA256`（`P-5` 口徑·器 ＝ `python verify/probes/probe_order_preflight.py <本單>`） | 見本檔末列；判別力二造須**先自證 bytes 相異**再比對（`W-G.9-287` 自捕） |
| `4'` | 受詞自證（坑 `bf`） | 受詞 ＝ **所量之檔名**（⛔ 本文正文）：[必真] 含 `W-G.9-288` ＝ `True`／[必偽] 含 `W-G.9-287` ＝ `False` |
| `5` | `python verify/probes/probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | 四簿：`python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`380`**／**`390`**｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | 坑：`python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`** |
| `7` | 生產碼母體：`git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec `verify/*.py` 多咬之子層檔數 **須 `> 0`**（判準式·⛔ 定值） |
| `8` | `verify/baselines`：檔數 `git ls-files verify/baselines \| wc -l`；框 `sha256` ＝ **`git ls-tree -r HEAD verify/baselines \| sha256sum`**（🛑 `core.quotePath` **預設**·⛔ `ls-files`） | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（併報 `-c core.quotePath=false` ＝ `7f2a5fba0467…`） |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**）**逐項適用·⛔ 省**。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-287` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `130ed4610b94272d…`）／
生產碼 `34` 檔於 `52c9d97b..005fc6a0` 異動 **`0`**／自誤 `380`／`390`（恰 `+1`）·其餘三簿未動／`p3a` **非祖先**
⇒ **✅ 全批核可**。
🔑 **主檢之受領**：三格之對稱差 ＝ `13.055884224441684`／`38.06818855324997`／`13.05518976183967` ㎡；
負擔率差 ＝ `4.955250038496402`／`9.019091904808164`／`4.265999999999998` 個百分點；
`forced` 帶**已物化**（`R4-抵費地-2` `116.08` 逐位等於 `range_area`）；藍影之差異**僅落於** `is_second_after_corner`
（自我驗證閘 `22`／`22` 預測 ＝ 實際）⇒ **器非紅**。
🩸 六則自捕皆於出艙前攔下，處置正確——尤以閘 `4` 之造乙「與 `P-5` payload 逐位相同 ⇒ 恆同」：
**先自證二口徑 bytes 相異**之處置，正是 `W-G.9-286` 所立附款之正確施行。

---

## `§一`　裁：`W-G.9-287R` 所呈之二事

| # | 事 | **裁** |
|---|---|---|
| `1` | 「第 2 宗」之**二讀法**相異 | **二讀法皆保留·⛔ 擇一廢另一**。單內之本意為 **(a) 位次讀法**；惟 **(b) 碼面 `驗_宗序` 讀法**所揭者（`R4/left` 二格**碼面不認為有第 2 宗**）**本身即發現**，已入 `§三-1` 之 `GB-170` 受詞。🔴 責 ＝ 發單側（單內未釘該詞之口徑）⇒ 鑄 **`自誤 391`** |
| `2` | 閘 `8` 之**框未於單內具名** | **受領其建議**。🔴 責 ＝ 發單側 ⇒ 鑄 **`自誤 392`**；本單 `§零` 起手表**每一列已同格載其產生指令逐字** |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-288_施工單_GB170之鑄與自誤391_392.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-287` 之單須 `≥ 1`／造乙 ＝ **執行期組出**之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（框 ＝ `tr -cd '\r' \| wc -c`·**⛔ `od -c \| grep -c`**·坑 ＝ `W-G.9-287` 自捕三；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一：鑄 `GB-170`（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md`·**新節·置於檔末**·⛔ 改任何原節一字）

### 三-1　逐字（**由本單供·⛔ 改寫·⛔ 增刪一字**）

> ### `GB-170` 🆕　**強制抵費地之裡側界線（第 2 宗之起算）⛔ 取自乙式之遠側境界線**
>
> **受詞**：街角 `forced`（無 WINNER·強制抵費地）之側，第 2 宗之起算線係取自
> `_corner_buffer_S` 之 `buf`（**以 bisect 解至「真實池帶面積 ＝ `range_area`」**），
> 其線向 ＝ **`allocation_dir`**；而 `_build_corner_range_v3` 依乙式（`K-9-5-14` KL 裁 `2026-08-17`／
> `K-9-5-15` KL 裁 `2026-08-18`·canonical·逐字）所建之**遠側境界線**，其線向 ＝ **SIDELINE 向**。
> ⇒ **二線⛔ 為同一條**（`|cos|` ＝ `0.04734528804692001`／`0.07536112595772865`／`0.04734528804692001`）。
>
> **實測（`W-G.9-287`·落檔 `verify/out/WG9287R_landeffect.log`【倉】）**
>
> | 格 | `range_area` (㎡) | 對稱差 (㎡) | `R(W_0) − R(S1_perp)`（百分點） |
> |---|---|---|---|
> | `R4/left@0.0m` | `116.08` | `13.055884224441684` | `4.955250038496402` |
> | `R2/left@3.5m` | `308.17` | `38.06818855324997` | `9.019091904808164`（`@∩FRONT` 口徑） |
> | `R4/left@3.5m` | `226.01` | `13.05518976183967` | `4.265999999999998` |
>
> 併記：`buf` 線相對 SIDELINE 之垂距**沿深度變動**（`R4/left@0.0m`：`@∩FRONT` `4.480444756023346`／
> `@∩BASELINE` `730.3628739265594`）；`forced` 帶**已物化**（`R4-抵費地-2` 之幾何面積 `116.08`
> **逐位等於** `range_area`）⇒ **⛔ 為「缺一塊圖形」，係該片之位置與形狀**。
> 另記（**⛔ 本項之受詞**）：`R4/left` 二情境之 `_lot_gate` **各僅 `1` 筆**且其 `驗_宗序` ＝ `街角第1宗`
> ⇒ `is_second_after_corner` 恆 `False` ⇒ 藍影（`K-9-23` 閘一）於該二格**求值 `0` 次**；其應否求值**未判**。
>
> **失效條件**：`(1)` 該起算線改取乙式之遠側境界線且**經 KL 放行**；`(2)` 改後之行為差**逐宗量得並具名**；
> `(3)` 其餘行為**逐位不變**之證。🛑 **⛔ 解除、⛔ 收窄、⛔ 提修法主張**——三者皆**未成就**。

### 三-2　鑄號之現查（**入倉前·`GB-158` 之體例**）

`GB-170` 之占用須以 **`B`⋀`C`⋀`D` 三形分列並取**（判由**非空形**承擔；二形皆 `0` 須 **loud 具名**而⛔ 逕判紅）；
對照組甲 ＝ `GB-169`（**已鑄**·須 `> 0`）／對照組乙 ＝ **執行期組出**之人造號（**先驗現母體命中為 `0`**）⇒ 須 `0`。
🛑 **本節含 `GB-` 之號** ⇒ `GB-132` 之自指增長：**⛔ 追改既出之數**；避讓宣告之字面須**逐字可機驗**並附實測。

---

## `§四`　工項二：`自誤 391`／`392`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 391`**：**以一個在本案有二個可機械施行之讀法之詞為受詞，而單內⛔ 釘其口徑**。
> 具體形 ＝ `W-G.9-287 §三 款 7` 令出艙「該側**第 2 宗**之暫編地號、其 `G`」，而「第 2 宗」有
> **(a) 位次讀法**（強制抵費地為第 1 宗 ⇒ 第 2 宗 ＝ `g_rows` 首列）與 **(b) 碼面 `驗_宗序` 讀法**
> （`is_second_after_corner == True`）二解，**三格中有二格其結論相異**（`R4/left` 二格於 (b) 下**無此宗**）。
> **徵候**：受單側須並列二讀法並上呈，而單本可一句話釘死。
> **族** ＝ 受詞界定（`345`／`376`／`378`／`381`／`382`／`384` 之同根）。
> **攔法**：凡單內出現**土地側之序數詞**（第 N 宗／首宗／末宗／鏈頭）者，一律於同格**釘其口徑**
> （位次？碼面欄？何欄？），並令受單側**併報另一讀法之數**；⛔ 令受單側擇一。

> **`自誤 392`**：**起手表只載框之<u>輸出值</u>，而⛔ 同格載<u>產生該框之指令</u>**。
> 具體形 ＝ `W-G.9-287 §零` 閘 `8` 只載 `verify/baselines` 之框 `sha256` 值，未載其產生指令
> ⇒ 受單側首次取框用 `git ls-files`，二值皆不符，須**自倉回查** `docs/orders/W-G.9-276_…md:43`
> 方得真框 `git ls-tree -r HEAD verify/baselines`。
> **徵候**：閘印「不符」而受詞其實無誤——**⛔ 受詞紅、係取框紅**（`自誤 379` 之同族·其形為「框未具名口徑」，
> 本形為「框未具名**指令**」）。
> **攔法**：起手表**每一列**同格載其**產生該值之指令逐字**（含 `-c core.quotePath=…` 等一切選項）；
> ⛔ 令受單側自擬取框之法。

🔒 二落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**（造甲 `True`／造乙 `old+b'X'` ⇒ `False`）、簡繁機檢之 `rc`。

---

## `§五`　工項三：**四**則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號·⛔ 改任何原節一字）

> `(1)` **`GB-169` 與 `GB-170` 之分野（⛔ 合併、⛔ 互相解除）**：`GB-169` 之受詞 ＝ **情形甲**（非 forced）之側，
> 其成因 ＝ 右鏈鏈頭錨 `end_pt` ⛔ 落於該側 `SIDELINE` 上；`GB-170` 之受詞 ＝ **forced** 之側，
> 其成因 ＝ 起算線取自面積反解之 `buf` 帶。二者**成因不同、射程不交**。

> `(2)` **「第 2 宗」二讀法之並列登記（⛔ 擇一）**：見 `W-G.9-287R §二-6` 之表。
> 於 `R4/left` 二情境，碼面 `_lot_gate` **各僅 `1` 筆**且其 `驗_宗序` ＝ `街角第1宗`。
> 🛑 該側**是否應有第 2 宗**——**未判**，⛔ 於本批作成任何判斷。

> `(3)` **`GB-79` 之「先證紅後修」體例之援用登記**：`GB-170` 之處置依同法——
> ① 先釘紅（`W-G.9-287` 之數）；② 改碼**只改該一處取線**並證其餘**逐位不變**；③ 改後**逐宗量差**。
> 🛑 **②③ 須 KL 放行後始得為之**；本批⛔ 為之。

> `(4)` 🔑 **KL 之放行之登記（逐字·`2026-09-15`·⛔ 增刪一字、⛔ 擴其射程）**
> 發單側於 `2026-09-15` 呈 KL 之文（`呈KL_抵費地裡側界線取錯線之實測與放行請求`·**⛔ 入倉**）
> 之【要您放行】欄為**單一是非**：「是否放行修改：把強制抵費地的裡側界線（＝第 2 宗的起算線），
> 由現在『面積反解出來的那條』改為『規定範圍的遠側境界線』（SIDELINE 沿法向平移·退縮＋畸零地寬）？」
> **KL 逐字所答 ＝ 「放行」。**
>
> 🛑 **射程（由發單側釘死·⛔ CC 擴之）**：
> `(a)` 及於**該一處取線之修改**與其量測；
> `(b)` 及於該呈文所載之三段辦法（① 先釘紅 ② 只改該處並證其餘**逐位不變** ③ 改後**逐宗量面積差**）；
> `(c)` 🔴 **⛔ 及於入倉主線**——該呈文逐字載「帶著數字**逐 `commit` 再請您放行入倉**」
> ⇒ **主線之入倉須<u>第二次</u>放行**；在其取得前，修批一律落於**凍存分支**、**⛔ 併線**；
> `(d)` ⛔ 及於 `GB-167`／`168`／`169` 之任何修、⛔ 及於 `K-9-29` 之任何子項、⛔ 及於藍影之任何改。
> 🛑 **本批⛔ 據此放行動任何生產碼一字**——修批另單。

🔒 各落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之二造、簡繁機檢之 `rc`。

---

## `§六`　收工閘

| # | 受詞（**同格載指令**） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0` 行；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·`git cat-file blob \| tr -cd '\r' \| wc -c`） | `0`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob |
| `4` | 四簿期末（同閘 `6` 之指令） | 自誤 相異 **`382`**／MAX **`392`**（＝ 期初 ＋ **二**號）；`GB` 相異 **`168`**／MAX **`170`**（＋ 一號·🛑 `GB-132` 之自指增長⛔ 追改既出之數）；`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末（同閘 `6'` 之指令） | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` `git diff --numstat` 輸出**空** |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add`；🛑 **⛔ 以「意思有到」充命中**——逐 token **書出字面**並具名實測 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169` **⛔ 修** |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§七`　🛑 **⛔ 為之事**

⛔ 動生產碼 `34` 檔一字；⛔ 動 `ALPHA_LABELS` 一字；⛔ 改任何器之判準／框／輸出格式；
⛔ 修 `GB-79`／`167`／`168`／`169`；⛔ 解除、收窄或結清任何 `GB`（含**本批新鑄之 `GB-170`**）；
⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；⛔ 改起算線一字；⛔ 改 `ok_t`／`ok_f` 一字；
`GB-162` **三禁令維持**；**⛔ 引任何自 baseline 取得之數**；⛔ 量測任何新物、⛔ 施替身、⛔ 反事實、⛔ 長跑（除閘 `6'` 與收工閘 `5`）；
⛔ 鑄 `VR`／`K-9`／坑之任何號；⛔ 追改任何已入倉文件一字；
⛔ 判該側是否應有第 2 宗、⛔ 判藍影為缺陷、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘 → `§一`〜`§三` 逐工項（**全量出艙**·⛔ `tail -N`）→ 判別力二造之判 → 一句話推導 →
收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框與母體**與**產生指令**；框無 `^` 錨者**二數並報**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`GB-170`／`GB-169`／`GB-158`／`GB-132`／`GB-79`／`K-9-5-14`／`K-9-5-15`／`_corner_buffer_S`／
`_build_corner_range_v3`／`allocation_dir`／`range_area`／`S1_perp`／`is_second_after_corner`／
`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`merge-base`／`ls-tree`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: ae6ae344033e396c05c06442a8a725f6a265292efd5404f4d2c535fb82c64e85
