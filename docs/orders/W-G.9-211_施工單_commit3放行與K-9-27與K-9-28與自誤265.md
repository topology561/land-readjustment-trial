# 🧾 `W-G.9-211` 施工單　`commit 3` 之放行 ＋ 鑄 `K-9-27`／`K-9-28` ＋ `自誤 265`

> **基座**：`wip/s1-endpart` ＝ **`3d5b8e0eb9c7fbb21400e1a45244daee39685dd1`**
> **側分支**：`verify/W-G.9-198R-c3` ＝ `cead6449854ba43a950d63ee2f16041c022e505c`（父 ＝ `7e29eab3…` ⇒ ⛔ 能快轉·須 rebase）
> **性質**：**三段**。`§一` 🔴 含生產碼（rebase ＋ 側分支重推 ＋ **停機**）；`§二` 主線推進（**觸發權在發單側**）；`§三` 🟢 零生產碼登記批（**CC 逕推**·須於 `§二` 完成後辦）。
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔒 **KL 已放行（`2026-09-02`·逐字）**

> 「是 放行 `commit 3` 進主線」

🛑 **放行之受詞 ＝ 發單側已自側分支逐位元組復驗之<u>內容</u>**（二檔／`numstat` 二值／二 blob），⛔ 是某一個 SHA。

---

## §零　開工閘

| 閘 | 判準 |
|---|---|
| `S-0a` | `ls-remote` `wip/s1-endpart` ＝ `3d5b8e0eb9c7fbb21400e1a45244daee39685dd1`；五檔 blob ＝ `app.py` `144349c834c2289434e1b0edbcfc8d0a27d88435`／`verify/selection_pipeline.py` `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c`／`verify/run_verification.py` `d07955c58e67ec06260971a74b5aabe5e846f106`／`verify/stepg_pipeline.py` `fef5865a73033274697790e0e307aedbf6122422`／`verify/run_all.py` `e5cf9a224517533b183b34e4fd0f5fbe35ee406b` |
| `S-0b` | `ls-remote` `verify/W-G.9-198R-c3` ＝ `cead6449854ba43a950d63ee2f16041c022e505c` |
| `S-0c` | 母體 追蹤檔 **`1588`**／`.md` **`521`**；`K-6` `271342` B／`3768` 列；取號 `K-9` 相異 `13`／MAX `26` ⇒ 次 `27`；自誤 `229`／`264` ⇒ 次 `265`；`GB` `55`／`141`（🛑 **本批⛔ 鑄 `GB`**）；`VR` `75`／`90` |
| `S-0d` | 本單 `SELF_SHA256` 自驗相符；`probe_order_preflight.py` 之 🟡 逐項以**字樣錨**具名並依裁 `H` 歸類 |

🔒 **`S-0d` 之已知 🟡（發單側已對本檔實跑·母體 ＝ 本單單檔·範圍 ＝ `P-1`〜`P-6`）**：🔴 `0`【項】／🟡 **`2`**【項】。

| 檢 | 字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-4` | `` | 檢 | 字樣錨 `` | **判別力參照 ⇒ 具名豁免**：係**本豁免表自身之表頭**（已知之自我遞迴形·⛔ 再迭代） |
| `P-4` | `` | 閘 | 判準 | ``（`§三-5` 之表頭） | **判別力參照 ⇒ 具名豁免**：驗收表之索引表頭，其 `→` 係取號前後態之轉引，各閘之母體／框逐列具名於同表 |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造）**：同器對 `-203` ＝ `0`／`-205` ＝ `1`／`-206` ＝ `3`／`-207` ＝ `0`／`-208` ＝ `1`／`-209` ＝ `1`／`-210` ＝ `4`／`-198R` ＝ `5`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。

🔒 **行尾（`GB-139`）**：`clone` 用 `-c core.autocrlf=false -c core.quotepath=false`；`add` 既有檔一律常規 `add`；閘之受詞一律取**倉側** `git cat-file blob`。
🔒 **本機分支之處置（`自誤 261`）**：開工前報 `git rev-parse HEAD` 與 `git symbolic-ref -q HEAD`；若本機 `wip/s1-endpart` 停在非 `3d5b8e0` 之處，先二重保全再退回，逐項復驗後方進 `§一`。

---

## §一　🔴 rebase ＋ 側分支重推 ＋ **停機**

1. 將 `cead6449854ba43a950d63ee2f16041c022e505c` **rebase 至當時之主線頭**，得 `cead6449′`。🛑 ⛔ 動 payload 一字、⛔ `amend` 訊息、⛔ 併入任何新變更。
2. 🔴 **等值判準（四項全須成立·任一不符 ⇒ 停機上呈）**

| # | 判準 |
|---|---|
| `E-1` | `cead6449′` 之父 ＝ `S-0a` 所實查之主線頭全 `40` 碼 |
| `E-2` | `git show --name-only --format=` **恰二檔** ＝ `app.py`／`verify/selection_pipeline.py`（🛑 ⛔ 含 `verify/run_verification.py`） |
| `E-3` | `numstat` ＝ `app.py 127/0`／`verify/selection_pipeline.py 11/0`（🔒 **純加性·刪除逐檔 `0`**） |
| `E-4` | 二 blob 逐檔與 `cead6449` 相同（**全 `40` 碼**）：`app.py` ＝ `a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190`／`verify/selection_pipeline.py` ＝ `167a7beb8e9f7176ea70a53e6ad3bcb37c130b38` |

3. 🔴 **判別力自證——依 `VR-090` 肢一之機械測試選出見證受詞**（🛑 本單⛔ 以檔名建議·承 `自誤 263`）：
   取 `S ＝ {f : git diff --name-only 7e29eab3 <新基座 ＝ S-0a 實查值> 命中 f} ∖ {f : git show --name-only cead6449 命中 f}`。
   **`S` 為空 ⇒ 本自證⛔ 適用，須改採他法並具名**；`S` 非空 ⇒ 任取其一，報其於 `cead6449` 與 `cead6449′` 之 blob，**二者應相異**且後者恰 ＝ 主線頭之同檔值。併報 commit 訊息之**二端位元組數**（`戒 40`·⛔ 只寫「相同」）。
4. `git push --force-with-lease=refs/heads/verify/W-G.9-198R-c3:cead6449854ba43a950d63ee2f16041c022e505c origin cead6449′:refs/heads/verify/W-G.9-198R-c3`
5. 復驗主線**⛔ 前進**（期望值取 `S-0a` **實查**之值·⛔ 寫死常數）。
6. 🔴 **停機。⛔ 執行 `§二`／`§三`。**

---

## §二　主線推進（🛑 ⛔ 得逕行）

須待發單側逐字回覆「**`cead6449′` 等值已驗·准推主線**」後方得執行：

```
git push origin cead6449′:refs/heads/wip/s1-endpart
```

推後回報：① `ls-remote` 全 `40` 碼 ＋ 量測時刻；② `git rev-parse <新主線頭>:app.py` ＝ `a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190`；③ `left-right` ＝ `0 0`；④ 三側分支 `c1`／`c2`／`c3` **保留⛔ 刪**。

🔒 **此推之意義**：`W-G.9-198R` 之 `commit 1`／`2`／`3` **全部落地**，該重發本結案。土地後果 ＝ **零**（`D3-a`：四 CSV 逐位相同、`34` 閘 `diff` `0` 列、`run_all` stdout 全文 `diff` `0` 列）。

---

## §三　🟢 零生產碼登記批（**CC 逕推**·🛑 須於 `§二` 完成後辦）

🛑 四 payload 皆為 append-only 之**檔尾追加**，⛔ 就地改任何既有一字。
🛑 **節框**：`§三-1`〜`§三-4` 之 payload 首列本身即 `###`／`##` 標題 ⇒ 抽取須以 `^### 三-\d` 為節框。

### 三-1　鑄 `K-9-27`（追加於 `docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔尾**）

```markdown
### 🔒 K-9-27　**歸側之四階階梯**（KL 裁 `2026-09-02`·`W-G.9-211`）　🛑 **取代 `K-9-24 二·2`**

🔴 **本則取代 `K-9-24 二·2`**（原逐字：「兩側皆拿得下 ⇒ 歸**指數數值**較大之側，**並列**取前緣線 `p1` 側」）。
KL 逐字答「**是取代**」（`2026-09-02`）。
🔒 **惟 `p1` 側之退路<u>並未作廢</u>，而係<u>降為第 ④ 階</u>**（KL `2026-09-02`：「第 ④ 階用乙（`p1` 側）：確定」）。
（`K-9-24 二` 之款 ①③④⑤ **不受影響，逐字續行**；本則只換款②。）

**一（KL 之逐字·<u>初裁</u>）**
> 「兩側街廓名次較前面的那一側，若 2 側名次都一樣，就比指數數值（比到小數第 4 位），
> 若指數又相同，往合併前土地面積較大的那一側，若合併前土面積一樣大，就往暫編地號由小到大」

**二（KL 之逐字·<u>修正</u>·同日）**　KL 覆核時指出「`M` 就是一塊土地」⇒ 第 ③④ 階⛔ 回頭拆合併前地塊。
> 「選 乙-1」（＝ 第 ③ 階改為 `M` 與該側街角規定範圍之**真交集面積**）
> 「第 ④ 階用乙（`p1` 側）：確定」

🔑 **修正之理由（發單側·KL 已閱）**：`M` 係一塊地，其**暫編地號左右同一** ⇒ 原第 ④ 階恆平手；
而「合併前面積」須把 `M` 拆回原始地塊再按側加總，會生出「某側之地塊集為空」之未定態。
改採真交集面積後，`M` 於左右**各有一個恆存之值**，二病俱消。

**三（🔴 現行階梯·operative）**　`M` 兩側皆拿得下時，其歸側依下列四階，**前階分出勝負即止**：
| 階 | 判準 | 方向 |
|---|---|---|
| ① | 該側之**街角指數名次** | **較小者**（＝較前）勝 |
| ② | 名次相同 ⇒ **街角指數數值** | 比較前四捨五入至**小數第 4 位**，**較大者**勝 |
| ③ | 指數（`4dp`）相同 ⇒ `M` 與該側街角規定範圍之**真交集面積**（現行出艙欄 `真交集(㎡)`） | **較大者**勝 |
| ④ | 真交集面積相同 ⇒ **取前緣線 `p1` 側** | 決定性·**禁隨機**·保證必分勝負 |

🛑 第 ③ 階之受詞係**既有量**（`真交集(㎡)`），⛔ 新增任何計算；其比較精度⛔ 經正典釘定 ⇒ 繫 `GB-140`。

**四（名次之受詞）**　🔴 「名次」＝ **街角指數名次**（`W-G.9-198R R1-1` 所產出者），**⛔ 原位次（投影序）**。
KL 逐字：「是指 指數名次，名次低的（或小的，如第 1 名），代表街角指數高」。
🛑 **入正典之警語（發單側·KL 已閱）**：「名次小 ＝ 指數高」**於同一側內恆真，跨側比較時⛔ 成立**
——弱側之第 1 名，其指數可能低於強側之第 3 名。本則第 ① 階即係擇「在自己那一側贏得較徹底」
勝於「絕對評分較高」。⛔ 得將二者視為等價。

**五（⛔ 綁本案）**　⛔ 綁街廓名、⛔ 綁「左」「右」之字面（以幾何端 `p1_end`／`p2_end` 識別）、⛔ 綁本案常數。
🔒 **第 ④ 階之 `p1` 係<u>幾何性質</u>**（前緣線之 `p1` 端），⛔ 得以「左」或「右」之字面實作。
```

### 三-2　鑄 `K-9-28`（追加於同檔**檔尾**·置於 `三-1` 之後）

```markdown
### 🔒 K-9-28　**同街廓有二筆以上 `M` 時之處理次序**（KL 裁 `2026-09-02`·`W-G.9-211`）

🔑 **前提之澄清（KL `2026-09-02`）**：二筆 `M` **⛔ 會彼此合併**。步驟 0 之合併條件為
**同街廓 ∧ 同歸戶 ∧ 相鄰**（三者並存·`app.py` 逐字）⇒ 二筆 `M` 於**不同歸戶**、或**同歸戶而不相鄰**時
各自獨立 ⇒ 「先辦誰」之問題成立。（`GB-141` 之受詞。）

🔑 **「先辦」之逐字定義（發單側·KL 已閱）**　＝ 對該 `M` 執行完整之**歸側三步**並定案，然後才輪到下一筆：
1. 判其在左右各是否**拿得下**（＝ 達標 ∧ 為該側指數最大者·`W-G.9-198R R2-3`）；
2. 依 `K-9-24 二` 之款②〜④（款② 改依 `K-9-27` 之四階階梯）決定其歸側；
3. 🔴 **一經歸定，另一側即「視為未被 `M` 跨占」**（`K-9-24 二·5`）——將其自該側候選集**移除**並
   **重新評選一次**；若重評後無 winner（含候選集為空）⇒ 該街角**強制留設抵費地**。
⇒ **次序之所以有意義，全在第 3 步**：先辦者會自另一側之名單消失，後辦者所面對之名單因而不同。

**一（KL 之逐字）**
> 「1. 先比這兩塊土地是否有跨占街角序位是第 1 名的，有序位第 1 名者即先辦理那一筆土地，
> 若兩 2 筆土地分別占左右街廓各為第 1 名時，就比誰面積大先辦誰，若面積一樣大，
> 就比左右側街廓指數相加最大者先辦。
> 2. 若 2 塊土地於左右側街角均無序位第 1 時，就比左右側序位數相加最小者，
> 若 2 塊左右側序位相加數均相同，則比面積誰大，若面積一樣大就比左右側街廓指數相加最大者先辦。」

**二（分群）**　設 `D` ＝ 該街廓內全部 `M`。以「是否於左右任一側之**指數名次**為第 `1`」分二群：
**A 群**（有第 1 名者）**恆先於** B 群（皆無第 1 名者）。

**三（A 群內之次序）**
| 階 | 判準 |
|---|---|
| ①' | **兩側皆為第 1 名**者，先於**僅一側為第 1 名**者（發單側補充·KL 已點頭） |
| ② | **面積**較大者先 |
| ③ | 面積相同 ⇒ **左右側街角指數之和**較大者先 |
| ④ | 仍同 ⇒ **暫編地號**較小者先（發單側補充·KL 已點頭·保證必分勝負） |

**四（B 群內之次序）**
| 階 | 判準 |
|---|---|
| ① | **左右側指數名次之和**較小者先 |
| ② | 和相同 ⇒ **面積**較大者先 |
| ③ | 面積相同 ⇒ **左右側街角指數之和**較大者先 |
| ④ | 仍同 ⇒ **暫編地號**較小者先（發單側補充·KL 已點頭） |

🛑 **「面積」之受詞**（三·② 與 四·②）：係**該筆 `M` 之整筆面積**（`M` 為一塊地·⛔ 拆回合併前）。
承 `K-9-27 二` 之同一理由——KL `2026-09-02`：「`M` 就是一塊土地」。

**五（🔴 凍結款·KL 裁）**　KL 逐字：
> 「排序鍵一律取『這個街廓開辦前的初始狀態』，一經排定就不隨處理過程重算。」

⇒ 本則所用之**一切排序鍵**（名次、指數、面積、地號）**皆取該街廓開辦前之初始態**，
**一經排定即⛔ 因任何 `M` 之歸定而重算**。
🔑 **立法理由**：名次係**相對量**——某 `M` 一經歸定，另一側即扣除之並重新評選，
其餘候選之名次會前移；若排序鍵隨之重算，規則將自我循環（`GB-141` 之成因）。

**六（🔑 順序無關性之<u>推定</u>·⛔ 已證）**　發單側推得：本則之次序**恰保證「會讓位者必先辦」**，
故順序相依⛔ 會真的發生。推理三步：
1. **一側只有一個 winner** ⇒ 一街廓內**至多一塊 `M` 能「兩側皆拿得下」**（記為 `M₁`），
   亦只有它會離開某一側、把位置空出來。
2. 需靠讓位方能遞補者（記為 `M₂`），須在該側**達標而輸給 `M₁`** ⇒ 其指數低於 `M₁`
   ⇒ 其在該側之**名次必排在 `M₁` 之後**。
3. `M₁` 為兩側之 winner ⇒ 兩側指數皆高於 `M₂` ⇒ **兩側名次皆在 `M₂` 之前**。
⇒ 若有第 1 名者必為 `M₁`（進 A 群先辦）；若皆無第 1 名，`M₁` 之名次和亦較小（B 群第 ① 階先辦）。
🛑 **此為<u>推理</u>而非量測，⛔ 得據以解除 `GB-141`。** 須以窮舉探針機械驗證後方得解除（見 `GB-141` 更新註）。

**七（⛔ 綁本案）**　⛔ 綁街廓名、側標之字面、本案常數。本案 `R4` 之 `|D| = 1`、其餘五街廓 `|D| = 0`
⇒ **本則於本案⛔ 被觸發**，其落地係為泛化。
```

### 三-3　`GB-140`／`GB-141` 之更新註（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔尾**）

```markdown
### `GB-140`／`GB-141` 更新註（`W-G.9-211`·`2026-09-02`）　正典已補，**碼面未依**

🛑 **本註⛔ 鑄新號**——標題所含之 `140`／`141` 係既有者，`GB` 之相異數與 MAX **⛔ 因本註改變**。

**`GB-140` 處三**（歸側之並列判定用 `4dp`）⇒ **KL 已裁**：`K-9-27 三·②` 逐字
「比較前四捨五入至**小數第 4 位**，較大者勝」⇒ **現行碼之 `4dp` 與正典一致**。
🛑 惟 `K-9-27` 以**名次**為第 ① 階、指數降為第 ② 階，並新增第 ③ 階（`真交集(㎡)`）與第 ④ 階（`p1` 側），
而**現行碼僅比指數**（`app.py` 逐字 `_to = _P1 if round(_idx_of(...), 4) >= round(_idx_of(...), 4) else _P2`）
⇒ **碼面尚未依 `K-9-27`** ⇒ 處三**⛔ 解除**，其失效條件改為「**碼面實作 `K-9-27` 之四階階梯**」。
🆕 **新增之未定**：`K-9-27 三·③`（真交集面積）之**比較精度⛔ 經正典釘定** ⇒ 併入 `GB-140` 之射程，
與處一（段一 `4dp`）／處二（段二 `6dp`）同批上呈 KL。
**處一／處二不受本裁影響**，處二仍繫段三。

**`GB-141`**（`|D| ≥ 2` 之次序相依）⇒ **KL 已裁**：`K-9-28` 定分群與四階，並於第 **五** 項立**凍結款**
（排序鍵取街廓開辦前初始態·⛔ 隨處理重算）⇒ 循環之成因已由正典消滅。
🔑 **且發單側推得順序無關性**（`K-9-28 六`：`M₁` 恆先辦）——🛑 **惟此係<u>推理</u>、⛔ 量測**
⇒ **⛔ 得據以解除本項**（本項目之規矩：屬性判斷須附可證偽之量測）。
🔴 **失效條件改為二者並存**：
1. 碼面實作 `K-9-28`（含凍結款）；
2. **窮舉探針**機械驗證順序無關性——對合成候選組態（各側 `2`〜`4` 個候選 × 達標與否 × 指數高低之全排列）
   跑遍所有處理順序，證明**結果與順序無關**。探針綠方得解除；若得反例，即係 `K-9-28 六` 之推理有誤，
   該反例直接指出所缺之款。

🔒 二項與碼面重寫同批，列為**泛化波**之前置；本批⛔ 動生產碼一字。
```

### 三-4　鑄 `自誤 265`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

```markdown
## 自誤 `265`　閘之**母體**與其加數之**受詞不同源**（`自誤 263`／`264` 之第三面）

`W-G.9-210 T-6` 令：母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**，
判準 ＝ 「總命中 ＝ 落地前之數 ＋ **本批 payload 內之數**」，並附發單側實測 `2 ＋ 2 ＝ 4`。
🔴 而該批之二 payload **分落** `W-G.9波_claude.ai側自誤登記.md` 與 `docs/驗證裁定登記表.md`，
**無一落於 `T-6` 所指之母體** ⇒ 該母體之命中數本批**恆不變**（實測 blob `2d69e092…` 前後全同）
⇒ 「＋ payload 內之數」一項之受詞**⛔ 在母體內** ⇒ 該式恆偽。CC 窮舉四讀法，**無一得 `4`**。

🩸 **本則之教訓在於「修一個代理換上另一個代理」**。同一個閘連錯三次：
| 度 | 錯法 | 鑄號 |
|---|---|---|
| 一 | 期望寫死常數，未扣 payload 自身之貢獻 | `自誤 264`（承 `258` 之累犯） |
| 二 | 改為「＋ payload 內之數」，惟**數錯次數**（`1` vs 實 `2`）——出單前發單側自捕 | ⛔ 出艙·⛔ 鑄號 |
| 三 | 次數修對了，**母體錯了**——payload 根本不落入該檔 | **本則** |

**一般式**
> **凡閘之判準形如「總命中 ＝ 落地前 ＋ 本批 payload 內」者，須先機械確認<u>三者同源</u>**：
> 母體之檔 ＝ payload 之落地檔 ＝ 落地前計數之檔。**三者有一不同 ⇒ 該式⛔ 適用。**
> 🛑 且此確認須以 `git` 之**檔路徑**為之，⛔ 以「感覺是同一個登記系統」為之。

**族** ＝ 「將驗證判準綁在代理上，而非綁在它所要保證的性質上」（`自誤 240`／`248`／`263`／`264`）之**第四度**。
**攔截者** ＝ CC（窮舉四讀法後逐一依立法理由判綠 ⇒ **正辦**）。**土地後果** ＝ 無。
```

### 三-5　`§三` 之驗收

| 閘 | 判準 |
|---|---|
| `T-1` | 三追加檔（`K-6`／`GB` 登記表／自誤登記）之 append-only 位元組級自證（受詞取**倉側** blob）；依 `戒 40`，`Δ` 與 payload **二端**位元組數同格並報 |
| `T-2` | `K-9`（`K-6` 單檔·扣 `99`）：相異 `13`→**`15`**、MAX `26`→**`28`**（新增恰 `{27, 28}`） |
| `T-3` | 🔴 `GB` **⛔ 得變**：相異仍 **`55`**、MAX 仍 **`141`**（`三-3` 之更新註標題含既有之 `140`／`141`）。若相異變 `56` ⇒ **判紅**（代表更正註被誤鑄為新號） |
| `T-4` | 自誤：相異 `229`→**`230`**、MAX `264`→**`265`** |
| `T-5` | 追蹤檔 `1588`→**`1590`**；`.md` `521`→**`523`**（＝ 本單 ＋ 執行報告） |
| `T-6` | 🔴 生產碼四檔 ＋ `selection_pipeline.py` 五檔 blob **⛔ 動**（逐檔 ＝ `§二` 推畢後之主線：`app.py` 應為 `a6a6ffaf…`、`selection_pipeline.py` 應為 `167a7beb…`） |
| `T-7` | 🔴 **判別力自證（依 `VR-090` 肢二之形 ＋ `自誤 265` 之同源款）**：字樣錨 `` **取代 `K-9-24 二·2`** ``，母體 ＝ **`docs/rulings/K-6_…md` 單檔**。🛑 **CC 須先機械確認三者同源**（母體之檔 ＝ 本批 `K-9-27` payload 之落地檔 ＝ 落地前計數之檔）；確認後，期望 ＝ 落地前之數 ＋ payload 內之數，且**⛔ 得為 `0`**。🛑 **二加數皆由 CC 自行復算**，本單⛔ 給常數 |

✅ 全綠 ⇒ **逕推**。🛑 `§三` 之 commit ⛔ 得與 `§一` 合併。

---

## §四　其後與停機條件

1. 🔒 **`W-G.9-198R` 於 `§二` 推畢後結案**（三 commit 全落地）。
2. 🔴 **下一波之工作內容（本單⛔ 辦）**
   - **碼面重寫歸側**依 `K-9-27` 四階階梯（🔴 有土地後果·走側分支 ＋ KL 放行）；
   - **碼面實作 `K-9-28`** 含凍結款；
   - **窮舉探針**驗證順序無關性（`GB-141` 之解除條件二）；
   - `GB-140` 三處之精度**同批上呈 KL**（處一 `4dp`／處二 `6dp`／新增之真交集面積精度）。
3. 🛑 **`K-9-27`／`K-9-28` 落地為正典後，現行碼即與正典不一致**（現行只比指數、無次序規則、無凍結款）⇒ 該不一致**已由 `三-3` 之更新註具名**，⛔ 得視為新缺陷、亦⛔ 得由 CC 逕行修正。
4. **停機條件**：`E-1`〜`E-4` 任一不符／`§一-3` 之 `S` 為空而未改採他法／`S-0a` 五檔任一 blob 已變／`§一-5` 發現主線已前進／`T-3` 之 `GB` 相異數變動 ⇒ 皆**停機上呈**。
5. 🔒 **次號（本批落地後）**：自誤 `266`／`GB 142`／`VR 091`／戒 `41`／`K-9 29`。
6. 🔒 **攢批**：`GB` 定義列式之正典化（`GB-138` 處置③）。
SELF_SHA256: b94dd58bd58af2ef862fd033de8cfe9165eb444984aa2f55c0b379be3d914fe7
