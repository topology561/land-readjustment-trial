# `W-G.9-284`　施工單：`（五）3.` 後半款之**重判** ／ 「真正的地界」是否為可取之物 ／ `自誤 387` ＋ 二坑

> **態**｜開工 ＝ `05f220cce97bcaa86387e47fde62b2d945605575`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·⛔ 採本單之數）
> **單** ＝ `docs/orders/W-G.9-284_施工單_五之三後半款之重判與真正地界之可取性.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（出處判定 ＋ 逐字量測 ＋ 登記·**零生產碼**·**⛔ 替身**·**⛔ 反事實**）
> 🛑 **本批鑄號 ＝ `自誤 387` ＋ 坑 `bh`／`bi`**（逐字全由本單供）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🛑 **坑之鑄 ⇒ `ALPHA_LABELS` <u>須同批擴</u>**（否則器**結構上看不見新坑而輸出仍全綠** ⇒ 靜默漏）。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批·⛔ 呈 KL）。

---

## `§零`　開工閘（九項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `05f220cce97bcaa86387e47fde62b2d945605575` 或其後裔（🛑 **比對一律取<u>全 `40` 碼</u>·⛔ 短前綴**·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | 須**非**祖先；判別力[必為祖先] ＝ 本批基座之任一祖先 `commit` |
| `4` | `SELF_SHA256`（`P-5` 口徑） | 見本檔末列·**由發單側供值**；判別力二造須皆相異 |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-284` ＝ `True`；[必偽] 含 `W-G.9-283` ＝ `False` |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | 四簿 ＋ 坑 | 自誤 **`376`**／**`386`**｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 **`85`／`85`**·下一標籤 `bh` |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] 子層檔數 **須 `> 0`**（**判準式·⛔ 定值**·其值逐批漂移·`自誤 386`） |
| `8` | `verify/baselines` | `298` 檔·框 `sha256` `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**） |

🛑 **本批之三項恆常附款**（前批自捕之攔法·⛔ 省）：
`(a)` 一切批次替換，於報「期／實」**之外**須再驗**替換後之組成**，並出艙**舊字樣之殘留數**（含**短前綴**·坑 `bh`）；
`(b)` 一切字樣錨須先驗其**唯一性**（`uniq()`），非唯一即 **loud 拒測**·⛔ 逕取首命中（坑 `bi`）；
`(c)` ⛔ 以 `python -c "…"` 承載含**反引號**之文字——一律 `Write` 落腳本（坑 `3`）。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-283` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `5571c9c822068518…`）／
生產碼 `34` 檔於 `3fb2f250..05f220cc` 異動 **`0`**／自誤 `376`／`386`（**恰 `+1`**）·`GB`／`VR`／`K-9` 未動／`p3a` **非祖先**
⇒ **✅ 全批核可**。
🔑 **主檢之決定性事實經發單側自倉逐位覆核**：`K-6` 受詞行之行首 `od -c` ＝ `空 空 >` ⇒ 依規則第 `1` 款 `is_quote` ＝ `False`。**核可其判**。
🩸 四則自捕**皆於出艙前攔下**，處置正確——尤以第 `4` 則（字樣錨非唯一致二造同判）：若未攔下，**出處判定本身即為偽**。

---

## `§一`　裁（**發單側·逐條**）：`W-G.9-283R §十二` 所呈六項

| # | 所呈 | **裁** |
|---|---|---|
| `1` | `§三` 之判之效力 | **受領**：該**等同**（「該點即『面積換算出來的替身線』」）＝ **助手側補充**。⇒ 🛑 **⛔ 據以鑄 `GB`、⛔ 據以判碼面違反、⛔ 據以更正 KL**。🔴 **連帶**：`W-G.9-282 §一-4` 我所裁之「`（五）3.` 後半款**已了**」**係架在該助手側補充上** ⇒ **該裁撤回，改列<u>待重判</u>**（`§三`）；責 ＝ **發單側** ⇒ 鑄 `自誤 387` |
| `2` | `失效加註` 三處判**不一致** | **不一致本身即受詞之事實**（三處之**行首縮排**與**引言**不同）⇒ **⛔ 以其一概括三處**。本批逐處出艙其行首 `repr`／引言／判（`§三-2`） |
| `3` | `§四-2` 之射程差 | **受領**：前批（`W-G.9-282R §四 款 5`）之否定性宣稱**受詞過寬**，以**末端追加收窄**（`§五-3`）·**⛔ 追改 `282R`／`283R` 一字**·**⛔ 鑄號**（其係本設計之附款所預期並攔下者） |
| `4` | 規格之法定產物是否已實作 | 🛑 **⛔ 判**——該二列之受詞**可能係精度分類而非產物要求**。改為 `§四` 之逐字出艙 ＋ **正典之查**；在其出艙前 **⛔ 鑄 `GB-170`**（否則即以「規格有載」充「正典要求」·`自誤 387` 之同形） |
| `5` | 本批自捕四則之鑄號 | 自捕 `1`＋`2` **同族** ⇒ **鑄坑 `bh`**（批次替換之覆蓋不全）；自捕 `4` ⇒ **鑄坑 `bi`**（字樣錨非唯一而逕取首命中）；自捕 `3` ＝ **坑 `3` 重踩** ⇒ **⛔ 鑄**、末端追加登記即足 |
| `6` | `GB-158` 是否升格為體例 | **受領其為體例**（取號現查一律 `B`⋀`C`⋀`D` 三形**分列並取**，判由**非空形**承擔；二形皆 `0` 須 **loud 具名**而⛔ 逕判紅），以**末端追加**登記。🛑 **⛔ 解除 `GB-158`、⛔ 改其原節一字、⛔ 改 `CLAUDE.md` 一字**（`CLAUDE.md` 之修屬另一射程·須另單） |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-284_施工單_五之三後半款之重判與真正地界之可取性.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-283` 之單須 `1`／造乙 ＝ **執行期組出**之人造名須 `0`·🛑 且須**先驗其於現母體之命中為 `0`**）；
`CR` ＝ `0`（併報 `LF` 與 bytes；判別力 ＝ 一真 CRLF 人造檔須 `> 0`）；blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：`K-9-5-12（五）3.` 後半款之**重判**

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

`W-G.9-283` 已判該**等同**為助手側補充。⇒ 「`forced` 時之起算不再取其 buffer 起算點」此一**效力**，
其是否成立，**⛔ 得再倚該加註**。惟 KL 裁定二之逐字（**已判 KL 逐字**）載有一**原則**
（「起算一律用真正的地界」）；該原則是否**逐字**及於本款，係另一事。
🛑 **本項僅出艙三者之逐字與其出處判，⛔ 由 CC 作「是否已了」之判。**

### 三-2　逐款出艙（🛑 **出艙即止**·器 ＝ `verify/tools/wg915_ruling_provenance.py` 之**同一機械規則**·**`import`** 其符號·⛔ 另寫第二份判準·⛔ 改該器一字）

| 款 | 受詞 | 框／錨（字樣·**⛔ 行號為錨**·🛑 先驗**唯一性**·坑 `bi`） |
|---|---|---|
| `1` | `K-9-5-13（三）新裁之效力` 第 `2` 項之**行逐字**、其**行首 `repr`**、所屬區塊（若有）、**引言逐字**、**判** | 錨 ＝ `^#### （三）` 之節內·母體 ＝ `K-6` 單檔（併報 B 數／列數） |
| `2` | `K-9-5-12（七）` **指路加註**之同上五欄 | 錨 ＝ `grep -n "指路加註" <K-6>`（無 `^` 錨 ⇒ 二數並報） |
| `3` | `失效加註` **三處**（`[252]`／`[276]`／`[910]` 之當場現查·⛔ 採本單之號）之同上五欄，**逐處並列** | 同上；🛑 **併出艙其判不一致之<u>成因</u>**：逐處具名其**行首縮排位元組**與**引言**之異同 |
| `4` | KL 裁定二**逐字**（已判 KL 逐字）之**射程**：其字面是否含「`forced`」「buffer」「起算點」等字樣 | 逐字樣**逐項出艙其命中**（`0` 亦出艙）·🛑 **⛔ 由此判其射程及於何款**——出艙即止 |
| `5` | 判別力二造 | [必為 KL 逐字] ＝ `K-9-5-13（二）` 裁定二行／[必為助手側] ＝ `K-9-5-6` 之「首宗起點」行 ⇒ 二者經**同一器**須得**相異**之判；同判 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判該款是否已了、⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 鑄 `GB`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§四`　工項二：「真正的地界」於街角 `forced` 側是否為**可取之物**

| 款 | 受詞 | 框／母體 |
|---|---|---|
| `1` | `docs/specs/W-G.4_規格_v3.md` 之「法定成果／地籍界線／內部幾何構造參數」**整表 ＋ 其下全部 bullet** 之**逐字** | 母體 ＝ 該檔單檔（併報 B 數／列數）·框 ＝ 表之起迄列（**逐列出艙·⛔ 摘**） |
| `2` | 上開之**受詞為何**：其係**精度分類**抑或**產物要求** —— 🛑 **⛔ 判**；僅出艙該表之**表頭逐字**（`類`／`例`／`精度`）與該 bullet 之**判準逐字**（「它出不出報表欄」） | 同上 |
| `3` | **正典之查**：碼面是否被任何正典**要求**產出街角 `forced` 抵費地之**地籍界線（座標）** | 母體 ＝ `docs/specs/` 全部 `.md`（**正面列舉檔數**）＋ `docs/rulings/` 全部 `.md`（**正面列舉檔數**）＋ `CLAUDE.md` 之**待落地正本表**；字樣**逐項具名**並**逐項出艙其命中**（`0` 亦出艙）；逐處併出艙其**出處判**（同 `§三-2` 之器） |
| `4` | 前批 `§四-2` 之宣稱之**收窄後受詞**之重量 | 受詞 ＝ 「**街角 `forced` 之 `buf` 帶**⛔ 具名多邊形物件」；母體 ＝ `verify/` 全部 `.py`（**含子層**·正面列舉檔數）＋ `docs/specs/` 全部 `.md`；**正控[必命中]** ＝ `wf_f4.py` 之 `fb_polys`（末端抵費地）須 `≥ 1` ⇒ 器非紅；[必為零] ＝ 執行期組出之哨兵（**先驗現母體命中為 `0`**） |

🛑 **出艙即止**：⛔ 判規格是否構成要求、⛔ 判碼面是否違反、⛔ 鑄 `GB-170`、⛔ 呈 KL。

---

## `§五`　工項三：鑄號與登記（**逐字由本單供·⛔ 改寫**）

### 五-1　`自誤 387`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 387`**：**以「倉內有記載」充「KL 已裁」**——`W-G.9-282 §一-4` 據倉內三處記載，裁
> 「`K-9-5-12（五）3.` 之未決後半款**已了**」，其中 `K-9-5-6` 之**失效加註**經 `W-G.9-283` 之機械規則
> 判為**助手側補充**（受詞行以**二空白**起首 ⇒ `is_quote` ＝ `False`）⇒ 該裁**架在助手側補充上**。
> **徵候**：該裁並已據以入倉一則末端追加（「`GB-169` 解除條件 `(4)` **已成就**」）⇒ 誤已取得常規地位。
> **族** ＝ `K-9-5-12（六）` 之形（一句助手側補充被當成 KL 裁示）；亦屬受詞界定之根（`345`／`376`／`378`／`381`／`382`）。
> **攔法**：凡引倉內之「已裁／已失效／已成就」，一律**先過出處判定**（引文行 ＝ `^>` 之純語法規則）；
> **⛔ 以「倉內有載」充「KL 已裁」**；**⛔ 以「在 `>` 內」充「KL 逐字」**。

### 五-2　二坑之鑄（🛑 **`ALPHA_LABELS` 須同批擴**·落點 ＝ 本批報告之坑表 ＋ `verify/probes/probe_WG9269_pit_index.py`）

> **坑 `bh`　批次替換之覆蓋不全**：以批次替換更新單／腳本之態錨時——`(1)` 替換**順序相依**，
> 後一替換把**前一替換剛插入之字樣**一併改掉（致對照組失效）；`(2)` `40` 碼 `commit` 之替換
> **未覆蓋判準式內之短前綴**（如 `startswith("8d1e50e8")`）⇒ 判準式仍比對**舊態** ⇒ **量測器紅而印 `❌`**。
> **徵候**：二值**逐位相同**而閘印 `❌`；或[必為 `1`]之對照組得 `0`。
> **攔法**：逐替換報「期／實」**之外**，再驗**替換後之組成**，並出艙**舊字樣之殘留數（含短前綴）**須為 `0`。

> **坑 `bi`　字樣錨非唯一而逕取首命中**：以字樣為錨取行時，該字樣於母體內命中 `> 1`
> （如縮排之**引述**與**正本**各一），而程式逕取 `hs[0]` ⇒ **取到錯的一行**，其下游之判**全數為偽**。
> **徵候**：二造之判**相同**（本應相異）；或受詞行之內容與所期之節不符。
> **攔法**：一切字樣錨**先驗唯一性**（`uniq()`），非唯一即 **loud 拒測**並出艙其全部命中列；⛔ 逕取首命中。

🛑 `ALPHA_LABELS` 之擴：於其末端 `"bf", "bg"` 之後續列 `"bh", "bi"`（註 ＝ `W-G.9-284 §五-2 所准之二坑`）；
**⛔ 動其餘一字**；期末坑索引器須得 **`87`／`87`**·下一標籤 **`bj`**。
🔒 該檔位於 `verify/probes/`（**子層**）⇒ **⛔ 生產碼 `34` 檔之一**——須於報告逐字具名此事並以 `34` 檔之期初＝期末證之。

### 五-3　三則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號·⛔ 改任何原節一字）

> `(1)` **`GB-169` 解除條件 `(4)` 之倉態<u>更正</u>（`W-G.9-284`·⛔ 追改前一則末端追加一字）**：
> 前一則末端追加載該條件「**已成就**」，其所倚之 `K-9-5-6` **失效加註**經 `W-G.9-283` 判為**助手側補充**
> ⇒ **該載述撤回**，條件 `(4)` 改列 **待重判**（見 `W-G.9-284 §三`）。🛑 `GB-169` **仍⛔ 解除、⛔ 收窄**。

> `(2)` **`W-G.9-282R §四 款 5` 之否定性宣稱之<u>收窄</u>（⛔ 追改原文一字）**：
> 原載「碼面⛔ 產生抵費地之實際配地多邊形」，其母體為**三檔**。`W-G.9-283` 以 **`368`** 檔之母體查得
> `verify/wf_f4.py` 之 `fb_polys`（**末端**抵費地）⇒ 原宣稱**受詞過寬**，收窄為：
> 「**街角 `forced` 之 `buf` 帶**⛔ 具名多邊形物件」。🛑 ⛔ 判其效力。

> `(3)` **`GB-158` 案型第三度再現之體例化（⛔ 解除 `GB-158`·⛔ 改其原節一字）**：
> 取號現查一律 `B`⋀`C`⋀`D` **三形分列並取**，判由**非空形**承擔；二形皆 `0` 須 **loud 具名**而⛔ 逕判紅。
> 併記 **坑 `3` 之重踩**（`python -c` 內之反引號遭 bash 命令替換 ⇒ 修補文字**靜默缺字**）⇒ **⛔ 鑄新號**；
> 攔法 ＝ 一律 `Write` 落腳本承載含反引號之文字。

🔒 各落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**（造甲 `True`／造乙 `old+b'X'` ⇒ `False`）。

---

## `§六`　收工閘

| # | 受詞 | 期／判準 |
|---|---|---|
| `1` | `numstat` 刪除欄逐檔具名 | **全 `0`**（`ALPHA_LABELS` 之擴係**續列**·⛔ 就地更正既有列） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0`（🛑 含 `probe_WG9269_pit_index.py` 之本批異動——須逐字證其**非** `34` 檔之一）；對照組[必非零] `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 ＝ 一已知含 `CR` 之倉內檔須 `> 0` |
| `4` | 四簿期末 | 自誤 相異 **`377`**／MAX **`387`**（＝ 期初 ＋ 一號·**⛔ 其他增減**）；`GB`／`VR`／`K-9` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；🛑 判別力[必命中] ＝ 新二坑 `bh`／`bi` 各於器之輸出**現身**（若器仍輸出 `85`／`85` 而全綠 ⇒ **靜默漏** ⇒ 判器紅、停） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；併報判定組之個數 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必非零] `≥ 1`）；`baselines` `298` 檔·`a898f4e1…`；`p3a` **非祖先**；`WV_BAKE` **未設**；`K-9-29` 任何子項⛔ 落地；`GB-167`／`168`／`169` **⛔ 修** |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit`（**全 `40` 碼比對**·⛔ 短前綴·⛔ 預寫） |

---

## `§七`　🛑 **⛔ 為之事**（逐項於報告自檢）

⛔ 動生產碼 `34` 檔一字；⛔ 改 `wg915_ruling_provenance.py` 一字；⛔ 另寫第二份出處判準；
⛔ 修 `GB-167`／`168`／`169`；⛔ 解除、收窄或結清 `GB-158`／`GB-168`／`GB-169`；
⛔ 落地 `K-9-5-12`／`K-9-5-13`／`K-9-29` 之任何子項；⛔ 改 `ok_t`／`ok_f` 一字；`GB-162` **三禁令維持**；
**⛔ 引任何自 baseline 取得之數**；⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑；
⛔ 鑄 `GB`／`VR`／`K-9` 之任何號；⛔ 動 `ALPHA_LABELS` 除本單所令之**續列二字**外之一字；
⛔ 追改任何已入倉文件一字（含 `W-G.9-282R`／`283R` 與既有之末端追加）；
⛔ 判「替身線」之所指、⛔ 判該款是否已了、⛔ 判規格是否構成要求、⛔ 判碼面是否違反任何裁、⛔ 提修法主張；
⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘 → `§一`〜`§三` 逐工項（逐款以落檔**全量**出艙·⛔ 以 `tail -N` 取結果）→ 判別力二造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框與母體**；框無 `^` 錨者**二數並報**；以 `git` 輸出為框者**具名 `core.quotePath`** 或取 `-z`。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`provenance`／
`is_quote`／`失效加註`／`指路加註`／`K-9-5-13`／`K-9-5-12`／`GB-169`／`GB-158`／`ALPHA_LABELS`／
`fb_polys`／`wf_f4`／`_corner_buffer_S`／`range_area`／`baselines`／`numstat`／`deletions`／`sha256`／
`merge-base`／`uniq`。

【單止】本單受詞 ＝ 下一批之 CC 窗。⛔ 呈 KL。

SELF_SHA256: b3f0ac653ef2d999e718fb20c49ceae0cc9794d402d0683dccb25e3545e9e956
