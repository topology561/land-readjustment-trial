# W-G.9-269　補令三（**本體階**之施工令：`p3` 重烤 ＋ 病一之落地）（**重級批**·🔴 **有土地後果**）

> **主線期初態** ＝ `0e835f6aeb86fa83b4642cf3821592b352ea0a3f` 之後裔（前置階結案 ＋ 補令二之收尾）·須 **`git ls-remote`** 實查。
> 🔒 `W-G.9-269` 施工單、補令一、補令二、本補令**並存**；相斥處以**時序最後者**為準。
> 🛑 **方法⛔ 新裁**：病一之三支方法皆既有裁定（`K-9-23` 閘一轉主動／`K-9-17` 遞補迴圈／第 `1` 宗境界線不動）。

---

## §零　受詞與界

### 零-1　病一之受詞（前置階 `p1′` 所定·⛔ 擴張）

> **`K-9-23` 閘一（藍影·`_k923_gate1`）判為<u>不合格</u>之宗**——其判落於落檔之 `驗_B藍影` 欄。

| 情境 | 受詞之宗 | 歸戶 | `G(㎡)` | 宗地寬度(m) |
|---|---|---|---|---|
| `0m` | `628-42(1)` | `G011` | `0.23` | `0.18` |
| `0m` | `628-53(2)` | `G025` | `0.00` | `0.02` |
| `3.5m` | `628-53(2)` | `G025` | `0.00` | `0.02` |

🔒 **開工時須自行重得**（⛔ 採信本表）；若與上表相異，**照實出艙並停機上呈**——受詞之變動⛔ CC 可裁。

### 零-2　🛑 ⛔ 併入本批

`K-9-12` 矩形容納轉主動（其 `A = 不合格` 者 **`0m` `19` 宗／`3.5m` `20` 宗**·土地後果遠大於本批·**另立獨立批**）／五級／`K-9-10`／`K-9-1`。
🛑 **⛔ 於本批之任何報告作 `K-9-12` 之修法主張。**

### 零-3　生產碼受詞（**逐檔逐站具名**）

| 檔 | 站 | 字樣錨 |
|---|---|---|
| `app.py` | 站 `1／4`（左鏈）·站 `2／4`（右鏈） | `res['_lg_cols'] = _lot_gate(` |
| `verify/stepg_pipeline.py` | 站 `3／4`（左鏈）·站 `4／4`（右鏈） | `res['_lg_cols'] = ns['_lot_gate'](` |
| `verify/run_verification.py` | `_DECLARED_EXTRAS`（`:390`）之 `指數名次` 二則（`:394`／`:399`） | **僅 `p3a`**·⛔ 逾此動該檔一字 |

🔒 **觀測模式之前提（發單側實測·須自行重得）**：`驗_rejected` 於全 `34` 檔生產碼命中 **`1`**（`app.py` 其定義處）⇒ 判定**寫入而從未被讀取**。**若 `> 1` ⇒ 已有消費點 ⇒ 停機上呈。**
**⛔ 動**：`_oblique_s_max`／`_wg9268p_anchor_advance`／`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 之**內部**一字（本批只改**其呼叫端**之消費）。

---

## §一　開工閘（逐條當場復現·任一不符 ⇒ 🛑 停機上呈）

| # | 閘 | 判準 |
|---|---|---|
| `S-1` | 號占用 | 受詢 ＝ `W-G.9-269`（**本單自身之號**）⇒ 必非零；**逐筆歸類**，任一筆為第三方 ⇒ 停機。二形並取（`GB-158`），哨兵執行期自倉掃出 |
| `S-2` | `ls-remote` 實查 ＋ `left-right` | ⛔ 採信本地 ref |
| `S-3` | 生產碼 `34` 檔 blob **當場現查並出艙全表** | ⛔ 以任何既有表為比較端（`自誤 334`）|
| `S-4` | `run_verification` 期初實跑 | 記 `RESULT`／`stderr` 位元組／**紅綠名目集合**（⛔ 只記數）。前置階所得 ＝ `24 PASS／40 FAIL／64 名目`·`RESULT: FAIL`（准紅碼常態）|
| `S-5` | `run_all` 期初對帳態 | 於**拋棄式 clone**內跑（其改寫已追蹤 log）。記類 I／II／III 與名目差集之**期初值**；**⛔ 以「與凍存相同」為通過條件**，只取值 |
| `S-6` | 四簿取號量（**框 ＝ 標題形 ∪ 表列形**·須具名） | 期初：自誤 MAX `354` 之後裔；`GB` `161`／`VR` `95`／`K-9` `29` |
| `S-7` | 硬閘活體生效 | `--selftest` **⋀** 活體攔截實測 |
| `S-8` | 探針定位普查 | `grep "worktrees\|REPO *="` 全掃並出艙（`GB-161`）——**須先於任何實跑** |
| `S-9` | 🔴 **觀測模式現查** | `驗_rejected` 全 `34` 檔命中須 ＝ `1`；`_lg_cols` 寫入點須 ＝ `4` 站（`app 2` ＋ `stepg 2`），讀出點 `4`；**判別力**：`_lg_cols` 之寬字樣命中須 `> 4` ⇒ 證該檢非恆為 `4` |
| `S-10` | 覆蓋帳期初（病二之成果須維持） | 可達之全部組逐組逐對交集面積為零（記憶體側器·⛔ `_partial`）；**本閘為 `c2` 之不變量基準** |

---

## §二　工項（逐 `commit`·每 `commit` 一件事·皆可獨立回退）

### `p3a`（🔴 **生產碼**·須 KL 放行）　`_DECLARED_EXTRAS` 之更正

自 `verify/run_verification.py:390` 之 `_DECLARED_EXTRAS` **移除 `指數名次` 二則**（`:394`／`:399`），逐字依 `CLAUDE.md:1314` 之明令。
🛑 **⛔ 動該檔其餘一字**；⛔ 動其他 `33` 檔。
**驗收**：`(a)` `指數名次` 於該檔命中 `0`（父態對照 `2` ⇒ 非恆為 `0`）；`(b)` `run_verification` 之輸出與父態之差異**逐項歸因**——凡新紅須歸因於「該欄自此被比對」，⛔ 其他；歸因為其他者 ⇒ **停機上呈**。

### `p3b`（零生產碼·得逕推）　九份 baseline 之重烤

**「九份」之清單（`docs/reports/W-G.9-122_CC交接文.md:87-90` 逐字·開工時須自行重得）**
`退縮0m參數.csv`／`退縮3.5m參數.csv`／`第 1 宗街角地指配結果_退縮0m.csv`／`第 1 宗街角地指配結果_退縮3.5m.csv`／`W-D.1.2 診斷_退縮0m.csv`／`W-D.1.2 診斷_退縮3.5m.csv`／`v3/W-D.1.2 診斷_退縮0m.csv`／`v3/W-D.1.2 診斷_退縮3.5m.csv`／`W-D.1.3-d 驗收_退縮3.5m.csv`

**機制** ＝ `WV_BAKE=BASELINES`（`verify/run_verification.py:417-420`·原地覆寫）。
🔴 **必答一（射程）**：`WV_BAKE` 所覆寫者為 **`run_verification` 所比對之<u>全部</u> baseline**，⛔ 僅上開九份。⇒ 須先出艙**其實際覆寫集**之逐檔清單與基數，並將其分為「**九份**」與「**其餘**」二類。
🔴 **必答二（`GB-159` 之判據所繫）**：`verify/baselines/**/W-D.4_四梯分級清單_退縮{0m,3.5m}.csv` **⛔ 在九份之列**，而 `G011`／`G025` 二歸戶之補償數出自該檔。⇒ 須現查該檔**是否在 `WV_BAKE` 之覆寫集內**：
　`(甲)` 在 ⇒ 出艙該二歸戶之列**重烤前後並列**（`增配a′(㎡)`／`差額地價 §52-1`／`放棄改領 §53-2`／`補償_本文式 §53-1`／`補償_但書式`）；
　`(乙)` 不在 ⇒ **停機上呈**——`GB-159` 之解除判據無從更新，其處置⛔ CC 可裁。

**出艙**：逐檔之改前／改後 **blob `sha1` ⋀ file `sha256`**；**逐檔差異之歸因**——每一處歸因於「錨點更正」／「`p3a` 之欄被比對」／「其他」；**歸因為「其他」者逐項具名並停機上呈**。
🛑 重烤後跑一次 `run_all`，與 `S-5` 之期初態比較並**逐項歸類三類**；**真迴歸（期初綠 → 期末紅）非空 ⇒ 停機上呈**。

### `c1`（🔴 生產碼·須 KL 放行）　遞補迴圈骨架 ＋ 旗標（預設 `off`）⇒ **行為逐位不變**

於 `§零-3` 之**四站**加入閘一之消費點與 `K-9-17` 遞補迴圈，置於具名旗標之後（**預設 `off`**）。
🔒 **`expand`（並存）段**——⛔ 刪任何既有碼路徑。
**驗收**：旗標 `off` 下全部輸出與父 `commit` **逐位相同**（全部 `got_*` ∧ `stdout` ∧ `stderr`）；**若 `stdout` 僅相異於 traceback 框行號且其位移與該檔增減列數閉式相等**，得依 `W-G.9-268′` 補令四 `§一-2` 自解並具名。
**判別力**：同一 `commit` 內以旗標 `on` 跑一次，輸出**須相異**（證非死碼）。

### `c2`（🔴 生產碼·幾何變動·**有土地後果**·須 KL **單獨**放行）　旗標 `on`

- **剔除之判** ＝ `_k923_gate1`（閘一）之 `不合格`，**⛔ `驗_rejected` 之合取**（其含 `K-9-12`·射程外）。
- **剔除宗之處置**（`K-9-11 三`·**已裁·⛔ 上呈 KL**）：**不配地** ＋ 走合併／調配機制 ＋ 騰出之地**入調配池** ＋ **⛔ 超配**。
- **遞補**（`K-9-17` 二·四·五）：空位由**街角重排<u>後</u>序列之下一位**遞補。
  🛑 **⛔ 依 `K-9-9 四` 字面之「重劃前投影順序」**——`K-9-17`（`2026-08-22`）晚於 `K-9-9`（`2026-08-16`），以 `K-9-17` 為準。**照 `K-9-9 四` 字面實作即錯，且其錯⛔ 會被任何既有閘擋下。**
  🛑 **遞補宗及其後各宗之 `G` 全部重算**（`K-9-17` 五逐字：「後面的宗也會整體往前分配，所以也要重算 G」）。
- **候選耗盡 ⇒ loud raise**（`K-9-9 六` 逐字：「必然產生配餘地……⛔ 不會發生『塞不下』」）。🛑 **⛔ 靜默兜底、⛔ 回 `None`**。
- **第 `1` 宗境界線不動**：係**構造保證**（第 `0` 宗遠側界之唯一決定量為 winner 之 `G`，而 winner 由街角 PK 決定、⛔ 受遞補影響）⇒ ⛔ 須另設閘；**惟仍須補一道 loud 斷言**（`GB-104` 逐字：「⛔ 不得以『上游擋住了』為由不設」）。

### `c3`（🔴 生產碼·須 KL 放行）　`contract` 段
移除旗標；更新受其影響之註解（含 `_lot_gate`／四站之「觀測模式」宣告）。
🛑 **⛔ 動** `_t3_gate_emit` 之診斷輸出一字。
**驗收**：與 `c2` 態之輸出**逐位相同**；旗標字樣於全 `34` 檔命中 `0`（父態對照須 `> 0`）；**七支以上讀該旗標之探針須逐支 loud 拒測、⛔ 靜默判綠**（承 `W-G.9-268′` 補令十一 `§二`）。

### `c4`（零生產碼·得逕推）　差異表 ＋ 執行報告 ＋ 登記
差異表之形見 `§四`；併辦 `GB-159` 三條件之逐條結清判與 `GB-160` 閘 `γ` 之重量（`§五`）。

---

## §三　驗收（**性質閘**·⛔ 計數式·⛔ 以列號或筆數為證據）

| # | 所保證之性質 | 判別力（自證⛔ 恆綠） |
|---|---|---|
| `P-0` | **受詞於開工時自行重得，與 `§零-1` 逐宗相符** | 相異 ⇒ 停機上呈 |
| `P-1` | **覆蓋帳維持歸零**（病二之成果⛔ 被本批破壞） | 與 `S-10` 逐組逐對對拍；人造 `+1㎡` 注入須轉紅 |
| `P-2` | **被剔除之宗⛔ 出現於配地母體**，且其面積**全數入調配池** | 剔除前後之 `ΣG ＋ Σ池 ＝ 街廓面積` 逐街廓守恆（`Σ池` **獨立量測**·⛔ 倒算）|
| `P-3` | **每一留存宗同時臨 FRONTLINE 與 BASELINE，臨接長 `> 0`** | 已知須紅之宗具名 |
| `P-4` | **第 `0` 宗（街角地）之 `G`、分配範圍、遠側境界線、`cut_coords` 全多邊形逐位不變** | 人造移動該線 `1e-3` ⇒ 須紅 |
| `P-5` | **街角地<u>歸屬</u>不變** | 🛑 `R6/right` 標「**不可判**」（`GB-156`）·**⛔ 併入違反計數** |
| `P-6` | **鏈序（`驗_宗序`）之變動<u>恰可由遞補解釋</u>** | 逐側出艙改前／改後之序列；**⛔ 只報「有變動」** |
| `P-7` | **`ΣΔG` 與 `Σ\|ΔG\|` 逐情境並列**，其 `min`／`max` **各自具名其宗與歸戶** | `自誤 343`／`354` 之通則 |
| `P-8` | **可達性只增不減** | 任一街廓由可達轉不可達 ⇒ 停機 |
| `P-9` | **名目之增減已逐項歸類三類**（本批所修轉綠／新可達所新曝／**真迴歸**） | 🛑 紅之總數增減本身⛔ 為判準；類 `(iii)` 非空 ⇒ 停機 |

🛑 **`P-2` ⛔ 寫作「剔除宗數 ＝ N」**——operative 形為「**逐街廓之守恆殘差在具名容差內** ⋀ **剔除宗之地號⛔ 出現於配地列**」。

---

## §四　差異表（**域語言**·⛔ 程式術語／變數名／行號／旗標名）

**表一（逐宗）**：配地次序｜重劃前地號／土地所有權人｜調整前・後分配面積（㎡·2dp）｜**面積增減**（帶號）｜調整前・後臨路寬度｜**寬度增減**（帶號）｜位置變動（m）｜**處置**（`維持原配地`／`改列調配池`／`遞補進位`／`位次順移`）｜是否街角地／備註
**表二（逐街廓·守恆）**：分配地合計・調配池・街廓面積・**殘差**（`Σ池` 須獨立量測·⛔ 倒算）
**表三（彙總）**：改列調配池之宗數・面積合計・**逐戶具名**｜遞補進位宗數｜面積增減 `ΣΔ`・`Σ|Δ|`（帶號與絕對值並列·`min`／`max` 各具名其宗與歸戶）｜**街角地面積或歸屬有變動之街廓（預期 ＝ 無）**｜所有權人受影響戶數

🛑 **必載**：`G011`／`G025` 二戶之**最終處置**（其地入調配池後之合併／調配／現金補償），並載 `p3b` 重烤後之補償數；**⛔ 引任何未入倉或舊錨態之數**。
🛑 **⛔ 以區間掩去負值之個體**——負值之個體即受損之地主。

---

## §五　後置（`c4` 併辦）

1. **`GB-160` 閘 `γ` 重量**：退化之宗移出配地母體後，重量 `3.5m·R5·left` 與 `3.5m·R6·right` 之閘 `γ`；**仍不過 ⇒ 成因⛔ 在退化幾何，須另診並具名**。
2. **`GB-159` 三條件之逐條結清判**：`(1)` 病一落地併線；`(2)` 二宗依 `K-9-11 三` 處置完畢；`(3)` 二宗⛔ 再以近乎零之配地出現。**三條齊備方得判其解除**；未齊備者逐條具名其缺。
🛑 **`GB-159` 之解除⛔ CC 自裁**——備齊後**呈發單側**，由發單側呈 KL。

---

## §六　停機款

開工閘任一不符（含 `S-9` 之 `驗_rejected > 1`）／`P-0` 受詞相異／`p3b` 必答二判為 `(乙)`／任一逐檔差異歸因為「其他」／`P-9` 真迴歸非空／`P-4`／`P-5` 轉紅／候選耗盡而未 loud raise／須動 `§零-3` 未列之檔或 ⛔ 動之符號內部／單與正典相斥（依 `常規二` 取**保守項**續辦並逐字上呈）。

---

## §七　側支與放行

```
備妥（⛔ 推主線）→ 推側支 verify/W-G.9-269-<工項>
→ 發單側逐位元組復驗 → 呈 KL → KL 放行 → 快轉併線
```
🛑 **側支待併期間主線⛔ 前進**——零生產碼之 commit 亦落側支（`W-G.9-268′` 補令十五 `§四-2`）；**rebase 須為該批推向任何 ref 之最後一個動作**。
🔒 `p3b`／`c4` 零生產碼，惟其於側支待併期間仍落側支。

---

## §八　鑄號之授權

🔒 **本階⛔ 預授任何號。** 自誤之鑄逐則上呈由發單側授權；`GB`／`VR`／`K-9` 一律**停機上呈**。
🛑 意指占用查**二形並取**；哨兵執行期自倉掃出、字面⛔ 出艙；四簿之數須具名其框（**標題形 ∪ 表列形**）。

---

## §九　出艙形

`docs/reports/W-G.9-269R_本體階.md`：開工閘十閘逐條／`p3a`／`p3b`（覆寫集清單 ＋ 必答二 ＋ 逐檔二式雜湊 ＋ 歸因 ＋ `run_all` 三類）／逐 `commit` 之生產碼判（含空輸出）／`§三` 十閘（各載判定集基數與兩造對照）／`§四` 差異表／`§五` 後置二項／🩸 **本批之自捕**（照實·⛔ 隱去）／自限復驗。

---

## §十　續辦

| 序 | 事 |
|---|---|
| `1` | 工項零（本補令原封入倉）＋ 開工閘 `S-1`〜`S-10` |
| `2` | `p3a`（側支·呈 KL） |
| `3` | `p3b`（側支·必答二判為 `(乙)` ⇒ 停機） |
| `4` | `c1`（側支·呈 KL） |
| `5` | `c2`（側支·**單獨**呈 KL·附差異表） |
| `6` | `c3`（側支·呈 KL） |
| `7` | `c4`（零生產碼）⇒ 全批結案 |

🔒 **`GB-159` 於 `§五` 判其解除前，二宗之配地圖與負擔表⛔ 對外。**
🔒 **`p3b` 完成前**，凡自 baseline 取得之數皆帶舊錨，引之須同格標「**舊錨態·待重烤**」。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
