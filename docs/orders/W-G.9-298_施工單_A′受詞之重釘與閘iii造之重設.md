# `W-G.9-298`　施工單（**現查·量測·登記·零生產碼**）：`A′` 受詞之**重釘** ／ `W-G.9-295` 閘 `(iii)` 造之**重設** ／ `自誤 400`・`401` 之**修訂重發**

> **態**｜開工 ＝ `2df61769b467fceeb68b297d74d87c49138426a0`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·⛔ 短前綴）
> **單** ＝ `docs/orders/W-G.9-298_施工單_A′受詞之重釘與閘iii造之重設.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（現查 ＋ 量測 ＋ 登記·**零生產碼**·⛔ 改碼一字·⛔ 開分支）
> 🛑 **本批鑄號 ＝ `自誤 400`／`401`**（逐字由本單供·修訂後之形）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔑 **本批<u>明文准</u>二項手段**（`⛔ 在 §六 之射程內`·`自誤 384` 攔法之施行）：① 包裹／`sys.settrace` 式之執行期量測；② **設計驗算**（`§三` 款 `4` 所需·其射程具名於該款）。
> 🔒 **KL 之放行仍在·仍⛔ 動用**；生產碼 `34` 檔自 `W-G.9-281` 起一字未改。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　開工閘（九項·**每列同格載其產生指令**·`自誤 392`·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `2df61769b467fceeb68b297d74d87c49138426a0` 或其後裔（**全 `40` 碼**·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**取之） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 項**逐項具名處置或具名豁免**·⛔ 靜默略過 |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ 器**所印之檔名列**） | [必真] 含 `W-G.9-298` ＝ `True`／[必偽] 含 `W-G.9-297` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0` ＋ 四靶逐項出艙 |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`389`**／**`399`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py`（**正面列舉**·⛔ 以 pathspec `verify/*.py` 界定·坑 `be`） | **`34`** 檔（`33 + 1`）；判別力[必不命中] 子層 `^verify/.+/.+\.py$` **須 `> 0`**（其值**逐批漂移**·⛔ 定值·`自誤 386`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **未設 ⇒ 預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |

🔒 **`P-4` 三項之具名豁免（發單側已辦·⛔ CC 重判·⛔ 靜默略過）**：本單經發單側實跑 `probe_order_preflight.py`
得 🔴 **`0`** 項、🟡 **`3`** 項（皆 `P-4`·落於 `§一` 之裁表、`§四` 之 `自誤 400` payload、`§五` 之登記 payload）。
三者之 `→` 皆為**賦值鏈／消費鏈之串接**（`(甲)` → `_rw_start` → `Rw` → `G_target` 等），**⛔ 幾何向量之方向性轉引**
⇒ **無座標系可具名**，**具名豁免**。🛑 惟凡本單出艙**幾何向量**之夾角者（`§三` 款 `5`），一律**釘其槽**（線向／法向）
並以**實邊**機驗——該禁**⛔ 在本豁免之射程內**。

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之三形分列體例、簡繁誤字機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**（⛔ `all()` 空集恆真）、payload 之**落地形機驗**、
函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、向量夾角**釘槽**並以實邊機驗、單字元名取 AST、
型別歸類出艙**實測節點型別**、**逐閘出艙執行次數**、**等待綁行程結束**、**`id(·)` ⛔ 作錨**、
**⛔ `\| tail -N` 取長跑輸出**、**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、**帶號與絕對值並列**、
**物證落檔保全⛔ 覆寫**、`f_locals` 變數名為鍵者須**自證其存在**並**增第二鍵**）**逐項適用·⛔ 省**。
🆕 **一則新附款**（`W-G.9-297` 自捕四所立·⛔ 省）：`git rev-parse <rev>:<路徑>` **未加 `--`** 時，路徑不存在則
**回退為解析該 `rev` 本身並以 `rc = 0` 返回** ⇒ 凡取 blob 前，一律**先以 `git cat-file -e <rev>:<path>` 驗存在**；
存在與否**三態分列**（`True/True` ⇒ 比 blob／`False/True` ⇒ **本批新入倉·受詞⛔ 適用**／`*/False` ⇒ 疑遭刪除）。

---

## `§零′`　前批（`W-G.9-297`）之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-297` 四 `commit`（`3b88a855`／`c2a088ee`／`e479423b`／`2df61769`·**全 `40` 碼見 `§六`**）經發單側自倉獨立復驗：

| 受詞（指令） | 實測 | 判 |
|---|---|---|
| 工項零之單 blob 內容 `sha256` | `77cc9a2d7e98ccb2daac6cdd311c3ac4e75c10b73bf4f3ee68aae55dcf8766d3`（＝ `297R §一` 所載·**逐位相同**） | ✅ |
| 生產碼判法（母體**正面列舉 `34` 檔**·框 ＝ `git diff --name-only 454d5e3d..2df61769` ⋂ 該母體） | **`0`** 行（本批異動 `5` 檔）；對照組[必非零] `17fd981^..17fd981` ＝ **`2`** | ✅ |
| 逐 `commit` 之 `git show --numstat` 刪除欄 | **全 `0`** | ✅ |
| 四簿（`probe_WG9267_issuer_measurers.py registry`） | 自誤 `389`／`399`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` ⇒ **逐位未動**（坐實停機後⛔ 鑄號） | ✅ |
| 三物證落檔之 blob（**先 `cat-file -e` 驗存在**） | `1a35dc168655…`／`f93eb2fcb7e6…`／`16742f3805e0…`（＝ `297R §五` 閘 `6` 所載） | ✅ |

⇒ **✅ 全批核可**。🩸 四則自捕皆於出艙前攔下、處置正確；**第四則**（`rev-parse` 之回退）已立為 `§零` 新附款。
🔒 **停機之處置正確**：`§三-2` 款 `7` [必相異] 不成立 ⇒ 判器紅、停、上呈，**⛔ 續辦工項二**——四簿逐位未動係**停機之應然**，⛔ 判為缺失。

---

## `§一`　裁與鑄號之逐字（**發單側·⛔ CC 自裁**）

| # | 事 | **裁** |
|---|---|---|
| `1` | `自誤 400` 之 payload 原載徵候：「二量所描繪之缺陷**量級相差逾一個數量級**（`(甲)` 距 `S1_perp` `≈0.785`／`(乙)` `≈0.012`）」 | 🔴 **該徵候撤回**：其二端係**跨格**之比對（`(甲)` 取自 `R4/left`、`(乙)` 取自 `R2/left`），**⛔ 同一格之二量**（`W-G.9-297` 二-7（一）實測）。⇒ **payload 修訂後重發**（逐字見 `§四`）：**保留**其核心形（一詞承載二量而單⛔ 釘口徑），**刪除**跨格之量級敘述，**改載** `W-G.9-297` 之逐格實測 |
| `2` | `自誤 401` 之 payload 所載「不可施係**結構性**」 | **成立·並補其分項成因**：不可施之分項成因 ＝ **`_N > 1` 為偽**（三格之 `_degenerate_order` 皆 `False` ⇒ **⛔ 歸因於 `_degenerate_order`**·`W-G.9-297` 二-4 實測）。⇒ payload **補此一句**後重發 |
| `3` | 🔑 `(甲)`／`(乙)` 於唯一二者皆可得之格（`3.5 R2/left`）**逐位相同**（`6.98048044737404`），其**機制未驗** | 🛑 **裁：⛔ 驗其機制**（本波⛔ 再開此工）。**理由（發單側具名）**：`W-G.9-297` 二-5 已機械指名二鏈**互不相交**——`(甲)` → `_rw_start` → `Rw` → `G_target`（**負擔之鏈**）；`(乙)` → `_b_L0`／`_b_R0` → `_select_pool_slot` 之 `'b'` 槽（**選槽理論**）。二量於一格巧合而其下游**不相交** ⇒ **無土地後果**，驗之係無受詞之工。🛑 **惟⛔ 據此判二者同物**——`W-G.9-297` 二-6 之 [必相異] 造**本身不成立**（其判定組僅 `1`／`3` 格），⛔ 以其紅充「二者同物」之證 |
| `4` | 🔑 `A′` 之受詞（`W-G.9-297 §一` 款 `4` 所留之「**待重釘**」） | **裁：釘為 `W-G.9-295` 之 `W_0′`**——即以路丙之 `buf′` 代入 `ns["k956_W_from_mp"]` 所得之值；**⛔ 為 `(甲)`、⛔ 為 `(乙)` 之生產錄**。**理由（三者逐一具名）**：`(甲)` `_W_near_out` 之受詞係**本宗近側界線**而⛔ 帶之界線，其 `R((甲)) − R(S1_perp)` 三格 ＝ `+4.958007524346591`／`-0.11126144996796938`／`+4.2390848776873895`（`W-G.9-297` 二-5 實測），與 `A′` 所欲界之**捨入級偏移**⛔ 相稱；`(乙)` 於三格中**二格結構上不可得**（裁 `2`）；⇒ 三者中唯一**於三格皆可得且受詞相稱**者為該重建值。🛑 **⛔ 因此判 `A′` 已通過**——其可信與否繫於裁 `5` |
| `5` | 🔑 `W-G.9-295` 自我驗證閘 `(iii)` 之造：「`W_0′` 須與 `drive()` 期間之**生產錄**逐位相同」，實測三格中**二格 loud 不可驗** | 🔴 **裁：該造撤回並重設**。**其誤之形（責 ＝ 發單側）**：該造之 [必命中性] 於三格中**二格結構上不成立**——`_mp_base_W0` 之**定義與呼叫皆在** `not _degenerate_order and _N > 1` 之分支內，而該二格 `_N` ＝ `1`（`W-G.9-297` 二-4 實測）⇒ 生產期**從不**產生可供對拍之錄。**族** ＝ `自誤 398`（未自機制導出之準據）＋ 六問第 `4` 問（**必命中性於每一格**）之漏。🛑 **⛔ 鑄新號**——其形已在 `自誤 398`／`401` 之射程內。**重設之造**（受詞見 `§三` 款 `1`〜`3`）：對拍之受詞由「其輸出**曾於生產期出現**」改為「器所調用之 `k956_W_from_mp` **與 `ns` 之項係同一物件**」＋「於**可得之格**（`3.5 R2/left`）逐位相同」——此造於三格皆可成立 |
| `6` | 路丙之**受領** | 🛑 **⛔ 於本批判**——其前提為裁 `5` 之重設造須**先成立**（`§三`）。受領之判**候發單側於本批報告後為之**；⛔ 由 CC 判、⛔ 由 CC 擬實作形 |
| `7` | `GB-170` 之現態 | 🛑 **失效條件三款⛔ 有一款成就**·**⛔ 解除、⛔ 收窄、⛔ 提修法主張**。本批之登記見 `§五`（工項三） |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-298_施工單_A′受詞之重釘與閘iii造之重設.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`）＝ 器所印之檔名須含 `W-G.9-298`（[必真]）且⛔ 含 `W-G.9-297`（[必偽]）；
所指之檔數 ＝ `1` 之自證（框 ＝ 磁碟 `os.path.isfile`·母體 ＝ 工作區；造甲[必≥1] ＝ `W-G.9-297` 之單／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（框 ＝ `bytes.count(b'\r')`·**讀 blob 取 bytes**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf`）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同；`numstat` 之 `deletions` 欄 ＝ `0`。

---

## `§三`　工項一（**主檢**）：閘 `(iii)` 重設造之機驗 ＋ `A′` 之當場重得

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

`W-G.9-295` 據以判 `A′`／`B′` 之 `W_0′`，其**可信性**原繫於閘 `(iii)`（與生產錄對拍），而該閘於三格中**二格 loud 不可驗**。
`W-G.9-297` 二-4 已指名其不可驗之**分項成因**（`_N > 1` 為偽）⇒ 該不可驗係**結構性**、⛔ 量測之缺失。
⇒ 本項**不再求**一個結構上不可得之對拍，改驗**可於三格皆成立**之二事：① 器所用之 `k956_W_from_mp` **即生產之同一物件**；
② 於**唯一可得之格**其輸出與生產錄**逐位相同**。二者成立 ⇒ 重建式之可信性有**機制上之依據**（同一函式、同一參數形）。
🛑 **⛔ 因此判路丙成立**（裁 `6`）；本項只出艙。

### 三-2　逐款出艙（**同一次執行**·`repr` 全位·三 `forced` 格逐格·⛔ 轉引前批之數）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **同一性**：器所調用之 `k956_W_from_mp` 與 `ns["k956_W_from_mp"]` 之 **`is` 比對**；併出艙 `ns` 之**來源**（`verify/app_harvest.py` 之 `harvest()` 內新建之純 `dict`·**⛔ 任何模組之 `__globals__`**·`W-G.9-293` 實測·`自誤 380` 之舊假說已否證·**⛔ 再引**） | 🛑 **`id(·)` ⛔ 作錨**（恆常附款）⇒ 以 `is` 出艙；判別力[必為偽] ＝ 一個**執行期組出**之同式包裝物（字面⛔ 出艙）其 `is` 須 `False`；[必為真] ＝ `ns` 之項與其自身 |
| `2` | **守衛之各分項於三格之當場值**：`_degenerate_order`／`_N`／`not _degenerate_order`／`_N > 1`／**合取**（⛔ 只報合取·⛔ 轉引 `W-G.9-297`） | `f_locals` 鍵一律**先自證其存在**（`in f_locals`）並**增第二鍵**（實參之逐位相等）**二鍵並報**；不一致即 **loud** |
| `3` | **可得之格之逐位對拍**：`3.5 R2/left` 之**生產錄**（母體 ＝ `drive()` 期間之**前綴** `d["K956"][:n_k956_0]`·🛑 **⛔ 含器自身之輸出**）與器以**現行 `buf`** 重建之值之差 | 同格出艙「生產錄 `N` 筆／器自身已追加 `M` 筆」**二數並報**；另二格 **loud 具名「結構上不可得」**並載其分項成因（款 `2`）·⛔ 判為 `0`、⛔ 判為「回測不過」 |
| `4` | 🔑 **`A′`／`B′` 之當場重得**（**⛔ 轉引 `W-G.9-295`**）：三格之 `buf′`／`W_0′`／`S1_perp`／跨距（`L_su`／`L_edge` **二式並報·取較大者**）／`A′` 界／`A′` 實測／對稱差（`B′` 實測）／`B′` 界 | 🔑 **本款<u>明文授權設計驗算</u>**（`§六` 之禁**⛔ 及於本款**·`自誤 384` 攔法）：**射程 ＝ 於 `forced` 消費端以 `_first_corner_alloc_dir(side_mid)` 取代當場之 `allocation_dir` 複算路丙之帶**，**⛔ 逾此一處**。出艙一律冠 `【設計驗算·⛔ 非實測】`、**⛔ 與實測之數同表混列**。一切原語**委派 `ns`**（`_block_strip`／`_strip_s_range`／`_corner_buffer_S`／`k956_W_from_mp`）·**⛔ 自寫第二套幾何**；帶之構造復用 `probe_WG9292_pathC.band_poly`（**⛔ 動該檔一字**）。🛑 **界值逐格由 `§一` 裁 `4` 所釘之算式當場導出·⛔ 調界、⛔ 事後放寬** |
| `5` | **切向之釘槽機驗**（`自誤 394`）：`rot90(_alloc_dir_used)` 與該宗 `cut_coords` **實邊**之 `\|cos\|`；`su` 與遠側境界線**實邊**之 `\|cos\|` | **⛔ 報「向量之夾角」而不釘其槽**（線向／法向）；以**實邊**機驗·三格逐格 |
| `6` | **判別力四造** | [必為真] 款 `1` 之 `ns` 項與其自身之 `is` ＝ `True`／[必為偽] 款 `1` 之人造包裝物 `is` ＝ `False`／[必非零] 現行 `buf` 之對稱差 `> 0`（三格）／[必為零] 範圍多邊形與其自身之對稱差 ＝ `0`（**先自證可滿足**·判定組為空一律 **loud 拒測**）。🛑 **任一不成立 ⇒ 判器紅、停、上呈**；⛔ 以「未執行」充「通過」 |

🛑 **出艙即止**：⛔ 判路丙成立與否、⛔ 判受領、⛔ 擬任何實作形、⛔ 改碼一字、⛔ 開分支、⛔ 動用 KL 之放行、
⛔ 解除或收窄 `GB-170`、⛔ 提修法主張、⛔ 呈 KL、⛔ 判前批呈 KL 之數孰是。

---

## `§四`　工項二：`自誤 400`／`401` 之**修訂重發**（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 400`（修訂重發·原 payload 之徵候款已由 `W-G.9-297` 實測推翻·責 ＝ 發單側）**：**以一個詞承載二個相異之量，而單未釘其口徑**。
> 「`W_0`」於本波至少指涉：`(甲)` `app.py` 之 `_W_near_out`——其註逐字「**本宗<u>近側</u>界線之 KL W**
> （mp→近側·供 stepg telescoping 閘 `W₀` 用）」；`(乙)` `verify/stepg_pipeline.py` 之 `_mp_base_W0`
> （其註逐字「自此**純委派**至 `k956_W_from_mp`」）所得之 `_b_L0`／`_b_R0`。
> **徵候（`W-G.9-297` 二-2／二-7 實測·逐格）**：`(甲)` 於三 `forced` 格 ＝ `4.4804447557822895`（`0.0 R4/left`）／
> `6.98048044737404`（`3.5 R2/left`）／`7.785015718090257`（`3.5 R4/left`）；`(乙)` **僅於 `3.5 R2/left` 可得**且
> 與 `(甲)` **逐位相同**（`6.98048044737404`）。`W-G.9-296 §三-1` 以 `(甲)` 之二值為「生產態之 `W_0`」而與
> `(乙)` 之唯一生產值判「⛔ 相符」——**其二端係<u>跨格</u>之比對**，⛔ 同一格之二個相異量。
> 🔴 **原 payload 所載之「二量所描繪之缺陷量級相差逾一個數量級」係該跨格比對之產物，於本則<u>撤回</u>。**
> **後果之指名**：`W-G.9-287` 據以呈 KL 之「負擔率差」係取 `(甲)`；其三格實測（帶號）＝ `+4.958007524346591`／
> `-0.11126144996796938`／`+4.2390848776873895`（`W-G.9-297` 二-5）⇒ 前呈之中間一列 `9.02` 係
> **`@∩FRONT` 之代理量**、**⛔ `(甲)` 之值**（發單側已向 KL 更正）。
> **族** ＝ 受詞界定（`自誤 391`「序數詞二讀法」之孿生）。
> **攔法**：凡單內出現一個**在碼面有二處以上同名或近名之量**者，一律於同格**釘其口徑**（檔／函式／變數名**逐字**）
> 並令受單側**併報另一讀法之值**；**⛔ 令受單側擇一**。🆕 **併立**：凡以「相符／不符」判二值者，
> 須於同格**具名其二端之格**（街廓／側／情境），⛔ 以跨格之二值作相符判。

> **`自誤 401`（修訂重發·補其分項成因）**：**以一個未查證之<u>可得性</u>為前提，斷言某對拍「於三格皆可施」**。
> `W-G.9-296 §三-1` 逐字：「此一對拍**不倚賴包裹之錄**，故於三格**皆可施**」；實測僅 **`1`／`3`** 格可施。
> **徵候**：該斷言之理由（「不倚賴包裹之錄」）**只排除了一個**不可得之來源，而未窮舉其餘。
> 🆕 **分項成因之指名（`W-G.9-297` 二-4 實測·逐格）**：`_mp_base_W0` 之定義與呼叫**皆在**
> `not _degenerate_order and _N > 1` 之分支內；三格之 `_degenerate_order` **皆 `False`**，而 `_N` ＝
> `1`（`0.0 R4`）／`14`（`3.5 R2`）／`1`（`3.5 R4`）⇒ **不可得之分項成因 ＝ `_N > 1` 為偽**，
> **⛔ 歸因於 `_degenerate_order`**。
> 🆕 **第二後果之指名**：同一未查證之可得性，亦使 `W-G.9-295` 自我驗證閘 `(iii)` 之造
> （「`W_0′` 須與生產錄逐位相同」）於三格中**二格結構上不可成立** ⇒ 該閘之判別力於該二格為零。
> 該造已由發單側於 `W-G.9-298 §一` 裁 `5` **撤回並重設**；**⛔ 鑄新號**（其形在本則射程內）。
> **族** ＝ 受詞母體未窮舉（`自誤 388`／`399` 族）。
> **攔法**：凡斷言一項量測「於某母體**皆**可施」者，須先**逐格現查其可得性之全部前提**（守衛條件之**各分項**），
> 並於單內**具名其不可施之格**；⛔ 以「已排除其一」充「皆可施」。

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**（`new.startswith(old)` [必真]／`old.startswith(new)` [必偽]）、
`new.endswith(payload + b'\n')`、**字樣錨之唯一性**（`uniq`·坑 `bi`·列框·母體 ＝ 該檔單檔·判別力 ＝ 寬字樣須 `> 1`·對照組[必為零] ＝ 執行期組出之人造錨）、簡繁機檢之 `rc`、`append-only` 機檢（`wg942_append_audit.py`·先設 `PYTHONIOENCODING=utf-8`）。
🛑 本 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留 `> `**（⛔ 剝層），並出艙其落地形之機驗
（payload 內 `^###` 起首列 ＝ `0`·對照組[必非零] ＝ 本單全檔 `^##` 起首列 須 `> 0`·**二數並報**）。
🛑 payload 之產生一律**由檔案管線直接產生**（`常規七 一`）·**⛔ 憑記憶重寫**。

---

## `§五`　工項三：`GB-170` 之現態登記（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**·嚴格 append-only·⛔ 上文一字不刪）

> **`GB-170` 之修：`A′` 受詞之重釘 ＋ 閘 `(iii)` 造之重設（`W-G.9-296`／`-297`／`-298`·⛔ 解除、⛔ 收窄）**
> **`W-G.9-296`**：`W_0` 之重建式與生產值之對拍——其所斷「於三格皆可施」為誤（見 `自誤 401`）。
> **`W-G.9-297`（停機上呈）**：`W_0` 之口徑釘死 ＝ `(甲)` `app.py` `_W_near_out`（→ `_rw_start` → `Rw` → `G_target`·**負擔之鏈**）／
> `(乙)` `verify/stepg_pipeline.py` `_mp_base_W0`（純委派 `k956_W_from_mp`）→ `_b_L0`／`_b_R0` → `_select_pool_slot` 之 `'b'` 槽（**選槽理論**）；
> **二鏈互不相交**；三格鏈頭之 `_rw_start` 皆 ＝ `(甲)` ⇒ **負擔計算實取 `(甲)`**。
> 唯一二者皆可得之格（`3.5 R2/left`）**逐位相同**（`6.98048044737404`）⇒ 款 `7` [必相異] 不成立 ⇒ **停、上呈**。
> `(乙)` 於另二格不可得之**分項成因 ＝ `_N > 1` 為偽**（`_degenerate_order` 三格皆 `False`）。
> 🔑 **藍影（`K-9-23` 閘一）⛔ 被架空**：其第一實參逐字取自 `corner_range_polys[_side]`（**範圍多邊形**）；
> `R4/left` 二格求值 `0` 次，成因 ＝ 該側 `_lot_gate` **僅 `1` 筆**且為街角第 `1` 宗 ⇒ `is_second_after_corner` 恆 `False`。
> **`W-G.9-298`（發單側之裁）**：`A′` 之受詞**釘為** `W-G.9-295` 之 `W_0′`（以 `buf′` 代入 `ns["k956_W_from_mp"]`）——
> **⛔ `(甲)`**（其受詞係本宗近側界線，`R((甲)) − R(S1_perp)` ＝ `+4.958007524346591`／`-0.11126144996796938`／
> `+4.2390848776873895`，與捨入級偏移⛔ 相稱）、**⛔ `(乙)`**（二格結構上不可得）。
> `W-G.9-295` 閘 `(iii)` 之造**撤回並重設**為「同一性 ＋ 可得之格逐位相同」。
> 🛑 **`GB-170` 之失效條件三款仍⛔ 有一款成就；生產碼一字未動；KL 之放行未被動用；路丙之受領⛔ 判。**

🛑 **本節⛔ 鑄任何號**——其為末端追加（`戒 36`）；⛔ 改任何原節一字、**⛔ 解除、⛔ 收窄任何 `GB`**。
🔒 出艙項同 `§四`（期初／期末 bytes、`+N／−0`、二造前綴檢、末端逐位、字樣錨唯一性、`append-only` 機檢、落地形機驗）。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·框 ＝ `git diff --name-only -z` ＋ `core.quotePath=false`） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末 | 自誤 相異 **`391`**／MAX **`401`**（＋**二**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**（⛔ 鑄坑）；`ALPHA_LABELS` 之 `git diff --numstat` 輸出**空**；母體檔數之增量須 ＝ 本批新增之 `.md` 檔數（**自洽檢**） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 一本批確有異動之檔）；`app.py` ＝ `4379108a…`；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**三物證落檔**（`WG9295R_joint_v1_selfpolluted.log`／`WG9296R_w0rec_v1_blknone.log`／`WG9297R_w0caliber_v1_blkabsent.log`）之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-289-gb170` 命中 **`0`**（⛔ 開）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 `1` |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉**（其 `sha` 於自身落地前不存在·結構上遞迴） |

---

## `§七`　🛑 **⛔ 為之事**

🔑 **二項手段<u>已明文授權</u>、⛔ 在本節之射程內**：① `§三` 之包裹／`sys.settrace` 式執行期量測；
② `§三` 款 `4` 之**設計驗算**（射程 ＝ **該一處實參之取代**·逐字見該款·⛔ 逾之）。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 擬任何實作形；
⛔ 調任何界、⛔ 事後放寬任何門檻；⛔ 自寫第二套幾何或第二份 `W` 之定義（`GB-48` 族）；
⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp`／`_mp_base_W0`／任何函式簽章；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 動 `probe_WG9292_pathC.py` 一字；
⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**、⛔ `WV_BAKE`、⛔ 併線 `p3a`、⛔ 刪 `p3a`、
**⛔ 引任何自 baseline 取得之數**；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 解除或收窄任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改任何已入倉文件一字；
⛔ 判路丙成立與否、⛔ 判受領、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制（`§一` 裁 `3`）；
⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘（**每列同格載其產生指令**）→ `§零′` 之⛔ 重辦 → 逐工項（逐格表以落檔**全量**出艙·⛔ `\| tail -N`）→
判別力四造之判 → 一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→
`⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**；**帶號與絕對值並列**。
🔒 **逐閘出艙執行次數**；🔒 同一性一律 `is` 比對（**`id(·)` ⛔ 作錨**）；🔒 判定組為空一律 **loud 拒測**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**；物證落檔**保全⛔ 覆寫**。
🔒 設計驗算之數一律冠 `【設計驗算·⛔ 非實測】`、**⛔ 與實測之數同表混列**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`cat-file`／`k956_W_from_mp`／`_mp_base_W0`／`_degenerate_order`／`_first_corner_alloc_dir`／`_corner_buffer_S`／
`S1_perp`／`rw_from_width`／`band_poly`／`GB-170`／`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／
`ls-tree`／`merge-base`／`append_audit`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: 33b8f7cc43190e26d919bfddac70d6d0cfd6ba90fec1a86dc8e553ef8e444d3b
