# 一　機械驗證（⛔ 採信開場文任一值·全部當場復算）

**三軸**：`(1)` `5ff9f409a53f739ada077305adf793e2d8452df6`（`ls-remote` 實查同值 ⇒ 已 push）／`(2)`(3) 逐節自載。
**開窗四件事**：`left-right` ＝ `0 0` ✅／**硬閘活體實測已生效**（送一含 `<<` 之命令，`wg9237_heredoc_guard` 當場攔下）／`-260R` 已讀（其態錨 `cd3e809` 與十檔 sha 均已過期）／主 checkout `docs/orders/` 對倉**倉內查無者 `0` 檔**。

## 三簿 ＋ K-9（列框·框逐字自 `VR-092`／`VR-093`／`-265R` 閘3）

| 簿 | 相異 | MAX | MIN | 缺號 `[MIN..MAX]` |
|---|---|---|---|---|
| 自誤 | **320** | **327** | **7** | **[106]** |
| GB | **153** | **155** | 1 | **[12, 87]** |
| VR | **79** | **94** | 14 | [73, 75] |
| K-9 | 28 | **29** | 2 | [] |

⇒ **開場文四數逐位相符**。計數式：自誤 ＝ 定義框 `325` 列 ∪ 範圍框 `8` 列（展開 `46` 號）；GB ＝ 式表 `131`/式A `3`/式B `69`(66 相異)；VR ＝ `80` 列扣 `999`。

### 🔴 本窗新捕之框缺陷二則
- **`框-1`：GB 之「扣哨兵 997」是空操作** — `997` 根本不在三框聯集內（`False`），`GB-997` 裸框 8 列全是散文。而 VR 的 `999` **確在**定義框內 ⇒ **二者不對稱**。該扣除現在證明不了任何事。
- **`框-2`：K-9 定義框對 `K-9-1` 結構上撈不到** — 命中 `0`（對照組 `K-9-2`=26／`K-9-29`=13／`K-9-888`=0 ⇒ 器非紅）。而 `K-9-1` 是**已裁之則**（`K-6` 內 2 列**皆表列式**；`CLAUDE.md` 列其為 🔴 **有土地後果**）。**與 `自誤 1`–`6` 同族，而 K-9 類尚無靜態清單**。🛑 且 `[MIN..MAX]` 恰好掩蓋之（MIN=2 ⇒ 缺號 `[]`；`[1..MAX]` 才得 `[1]`）⇒ **`VR-092` 於低端有盲區**。

## 十一檔 blob ／ 號占用 ／ run_verification

- **十一檔**：`app.py` sha256[:16] ＝ `1e15d45ae84b02ca`（git-blob `c5ec503c740f59e0`）；`stepg_pipeline` ＝ `cb0bbaaa0f1eb8bc`。⚠️ `-260R §一` 之 `4860f108…`／`c8d46d2e…` **已過期**。
- **號占用（宣告框六數分列）**：`W-G.9-266` ＝ `0/0/0/0/0/0`，鬆框亦 `0/0` ⇒ **未占用**；對照甲 `-265`(3/10/6/0/16/5)、`-264`(3/6/1/0/7/5)、對照乙人造號全 `0` ✅。🔑 `-264` 六數與 **`-265R` 閘4 所載逐位相符** ⇒ 我的框實作有外部錨（⛔ `N/N` 自證）。
- **run_verification（三態各實跑一次）**：`rc=1`／`stderr=0 B`／`RESULT: FAIL`／**紅項 24**（相異 24），三態名目集合**雙向差集全空**。
- 🩸 **本節量測器紅一次·自捕**：首版框 `^\s*(?:❌|FAIL\b)` 得 **0 項**而 `RESULT: FAIL` ⇒ 自相矛盾 ⇒ 判量測器紅、⛔ 出艙；改框為 `^\s*🔴 FAIL\s+(.+)$` 後得 24。

---

# 二　甲乙丙之獨立復現

## 【甲】CONFIRMED — 逐格相符

| 街廓·側 | 宗數 | cc 相同 | ΔG 最大 |
|---|---|---|---|
| R1·left | 1 | **1** | 0.0000 |
| R1·right | 4 | **4** | 0.0000 |
| R2·left | 7 | **0** | **+4.7700** |
| R2·right | 7 | **0** | **0.0000** |

`R2·left` 之 ΔG≠0 逐宗：`628-42(1)` +4.7700（**鏈頭**）／`628-41(1)` +0.0600／`628-40(1)+` +0.1900 ⇒ 後二宗與單 §九(c) 外部錨**逐位相符**。
🔑 **加測**：`R1·抵費地` 3 宗亦全同 ⇒ **R1 全 8 列**四欄逐格相同；`R2·抵費地` 2 宗中 1 宗已變。

## 【乙】CONFIRMED — 三證

1. **確定性**：生產碼十一檔逐檔相同之 `e9d1b9c` vs `2d1eaf4`，落檔 sha256 **逐位相同**（`98aebf88260047b5`／`437a07a475149f19`，與開場文相符）。
2. 🔑 **判別力（決定性·本窗新增）**：同探針同二態，`R1·right` 三宗 δ 皆 **`+0.000000000`**、極差 **`0.000000000`**；`R2·right` 為 `3.89e-4`。**噪音⛔ 挑街廓** ⇒ 噪音假說被否證。
3. **量級（本窗新增）**：`GB-101` 登記量級為 `S` 差 `0.01`、面積差 `0.04 ㎡`，較 `~4e-4 m` **大約 25 倍**；其機制逐字為「停點取決於**迭代軌跡（初始區間／S_max_limit）**」＝ 輸入相異、⛔ 隨機。

⚠️ **界（照實）**：證一所證為**同主機同碼同輸入**之確定性；`GB-101` 原始證據係**跨主機** ⇒ 二者⛔ 相斥，`GB-101` 於跨環境仍成立。

## 【丙】CONFIRMED — ⛔ 放寬 1e-6，換對照組即恢復判別力

先**逐位復現** `-265R` 原式（`(1b)` 區間、`(3)` 極差 `0.005683`／`0.000389`、`(5)` 2 宗／0 宗、三閘 24/20/28 全中）。再以複製至暫存之探針（**只換 main()**，判準一字未動）跑三組：

| 受詞 | 角色 | (1b) | (3) | (5) | 非鏈頭 |
|---|---|---|---|---|---|
| R2·left | 陽性 | 🔴 False | 🔴 False (0.005683348) | **2 宗** | 6 |
| **R1·right** | **真陰性** | ✅ **True** | ✅ **True (0.000000000)** | **0 宗** | **3** |
| R2·right | 第三類 | 🔴 False | 🔴 False (0.000388562) | 0 宗 | 6 |
| R1·left | ⚠️ 空判定組 | True | **None** | 0 宗 | **0** |

⇒ `(5)`／`(3)`／`(1b)` **三款皆恢復判別力**。`-265R` 之「該二款於 1e-6 下⛔ 區辨」係**對照組之誤所生之假象**。

### 🔴 本窗新捕：「`3.5m·R1`」之表述會引入**空判定組**
`R1·left` 僅 1 宗（`628-36(1)` ＝ 鏈頭）⇒ 非鏈頭 **0 宗**，`(3)` 回傳 **`None`**，三款一列未印。若判別力寫作 `(pos.n5>0) and (neg.n5==0)`，它會以**判定組為空**之姿「通過」——即「`序 11` 之一般化」款二所令須 **loud 拒測**者。
⇒ 🛑 **陰性須逐字寫作 `3.5m·R1·right`（3 個非鏈頭宗），⛔ 寫作「3.5m·R1（5 宗）」**。

---

# 三　草稿要點（全文見附檔）

**級 ＝ 中級**（純登記 ＋ 純診斷·⛔ 動生產碼四檔）。七道開工閘（含長跑全批只跑一次）。

**取**：①`自誤 328` 之鑄（甲）②`-265R §九` 就地加註（乙·⛔ 改原文一字）③`V-1′` 三組對照重跑 ＋ `VR-094` 末端追加（丙）④`d-1`/`d-2`/`d-3` 補齊 ⑤`K-9` 靜態清單 ⑥`CLAUDE.md` 序12/序14 同步更正。
**棄**（具名之由）：`GB-67` 之修／`telescoping` 之修（**候 KL·開場文明令本窗⛔ 決定**）；`c2`（⛔ 開工之明令）；`K-9-29` 碼側（有土地後果·另單）；五級／通用化波（動生產碼）；`_b_L0`（已為 `GB-155`·定性「純診斷⛔ 修」·`-265 附件甲`已判棄）；`R3` 射程（`-265 三-5` 已裁）。

**`d-1/d-2/d-3` 之判**（七組偵察 ＋ 七組對抗複驗）：三者**皆⛔ 為結構性不可得**，阻斷點互不相同。`d-3` 受詞**已在既有落檔內**（`驗_A幾何` 三欄）；`d-1` 有**三處碼面明文斷言**可充自我驗證閘且**自帶必紅組**（街角第1宗須 ∥SIDELINE）；`d-2` 最硬（`K-9-10`／`有效垂線` 於二生產檔命中皆 0），須新造一閘。
🔑 **三者於現可達之 55 宗上⛔ 待 `GB-67`；解了 `GB-67` 亦⛔ 使三者自動可得** ⇒ 可脫鉤先辦。

**🔴 典側不同步（本窗實測 ＋ 倉內已登記）**：`GB` 簿**已載**「`3.5m` 之 `telescoping` **已不破**（`-261` 後 Δ=0.034≤0.1）」與可達序 `R1→R2→R4→R6→R5`；而 `CLAUDE.md` 序12／序14 **未同步**。活體實測：態A `telescoping` 命中 **1**（Δ=55.606）／態B、C **0**，中止改為 `②-宗@R5 56.8186`；HEAD 與態B 生產碼**十一檔逐檔相同** ⇒ 可外推。🛑 草稿**只登記不同步之事實**，⛔ 更動狀態欄、⛔ 判其解除。

---

# 四　二清單

**【未決清單】7 項**（各含現況／≥2 讀法／各讀法後果／CC 傾向＋機械證據）：`U-1` GB-156 之意指占用（附件甲之擬鑄宣告，而 `-265` 實未鑄 — 傾向「⛔ 構成占用」惟**⛔ 由 CC 自裁**）／`U-2` GB 扣哨兵 997 為空操作（傾向加註）／`U-3` K-9 靜態清單落點（傾向 `CLAUDE.md`；落 `K-6` 者 `GB-132` 風險結構性較高）／`U-4` 第三類之命名（傾向不命名；⛔ CC 造詞）／`U-5` (1b)(3) 判準是否定讞（傾向「判準無誤」惟須載 n=3 之界）／`U-6` `56.3293` 之情境標籤（`0m` 三態皆止於 R2、⛔ 到 R5 ⇒ 該值⛔ 可由現態 `0m` 原態取得；**繫 GB-67 ⇒ 本窗⛔ 判**）／`U-7` 更正體例（傾向末端追加·既例 24 處）。

**【須 KL 域裁清單】＝ 空。** 四類候選項逐項出艙【已裁檢索】三軸後**皆為【查有】** ⇒ ⛔ 得上呈：`GB-67`／`telescoping` 已在 KL 待裁佇列（`-260R §六` 逐字）；`c2` 已明令⛔ 開工；`U-1`〜`U-7` 皆⛔ 有土地後果 ⇒ 依 `常規三` 歸發單側核可。
