# `W-G.9-344` 補令二（**重**·承原單與補令一）：量測器 `F3` 之修正／`k*` 經驗錨翻轉之歸因與 `V-12` 之收窄／`F.3` 零遺漏項之處置／`y_dump_diff.py` 之具名豁免／reviewer 二款之受領

> **單** ＝ `docs/orders/W-G.9-344_補令二_F3修正與k星錨歸因.md`（工項零″ ＝ 本補令原封入倉）。
> **發單** ＝ 發單側窗四十·`2026-09-26`。**受單** ＝ CC（續 `W-G.9-344`·同窗或新窗皆可）。
> **級** ＝ **重**（承原單·生產碼之射程⛔ 擴）。
> **開工態** ＝ 側支 `verify/W-G.9-343-k929b` ＝ `e6c8c5cc12b1154e5dae3ed7885827fcf46f7574`（工項零′）；主線 `wip/s1-endpart` ＝ `6090a7f8b7e37452fa118aec1ec446ecc5e9c6bc`（⛔ 動）。CC 本機之工項三 `commit` `3a4fa34`（父 `e6c8c5c`·⛔ 推）。
> 🔒 **與前二單之關係**：本補令於衝突點取代原單與補令一；三者皆留倉不改一字（`常規四（九）五`）。未經本補令明改之款，一律照舊。
> 🔑 **二來源檔**（KL 置於 CC 讀取前二單來源檔之同一處·檔名逐字；`F3′` **覆蓋**該處之舊 `F3`）：`W-G.9-344_補令二_F3修正與k星錨歸因.md`（本補令）／`probe_WG9344p1_pooltemp.py`（檔 `F3′`）。CC 一律二進位複製、⛔ 讀入改寫。
> 🛑 ⛔ 以命令列傳遞含反引號之文字（`恆常附款 h`）；`commit` 訊息一律 `git commit -F`。

---

## `§零`　CC 之停機與其處置之受領

1. 工項零′（`e6c8c5c`·父 `bd70bd9`）——發單側窗四十自倉復驗：二檔 blob 與補令一 `§六` 逐位相同 ⇒ **受領**。
2. 工項四之停機款 `10`、`12` 成就而停、工項三之 `commit` 未推——**處置正確·受領**。
3. CC 所報 `temp` 之重號（`126` 片／相異 `124`；`_GHOST_(G1)`、`_GHOST_(RD3)` 各二片·皆零面積）——發單側於 `bd70bd9` 以同一 `_boot` 復現，退縮 `3.5`／`0` 皆同 ⇒ **坐實**。

---

## `§一`　裁一：`F3` 之片數判法係**發單側之誤**；以 `F3′` 代之

🩸 **缺陷**：`F3` 之 `judge` 以 `set(all_ids) − set(marks)` 算「排除後之期」，而以串列比實得 ⇒ `temp` 含重號時**必紅**（`V-15`「期 `114`」、`V-16`「期 `124`」皆此）。其自檢 `9/9` 無一造含重號，未以真資料跑過其判法（`常規八`：`N/N` 之自證只證一致）。

🔒 **修正（`F3′`）**：排除後之 temp 改為**逐元素依序比對、保留重號**（期 ＝ 回傳之 temp 中未帶標記之元素，依序），並驗其元素與之為同一物件；帶標記之暫編地號若重複 ⇒ 判不符。自檢增為 `13` 造（含「重號而無標記·全數保留」必綠、「重號而排除時漏其一」必紅；前者於 `F3` 原版實跑 `rc 1`，坐實原缺陷）。`EXPECT_35_ON` 與 `rc` 之義**一字未改**。

🔒 **`F3′` 於真資料上之判別力**（發單側窗四十·生產碼 ＝ `bd70bd9` ＝ `e6c8c5c`；段三入口以「依補令一裁三之規格之替身」代之，⛔ 入倉）：
- 替身合規：`3.5 on` ⇒ `rc 0`（排除後 `116` ＝ `126` − 帶標記 `10`）；`0.0 on`、`3.5 off` ⇒ 皆 `rc 0`（`126` 片·回傳原物件）——與 CC 實得之片數逐位相同；
- 替身漏標 `628-45(4)`：`3.5 on` ⇒ **`rc 1`**（必紅成立）。

---

## `§二`　裁二：`k* 六塊經驗錨3.5m` 之翻轉 ＝ **全數歸因於 KL 放行之改後配地**

🔒 **歸因之實測**（發單側窗四十·態 `bd70bd9`·以檔 `F2` 之 `legacy` 與 `oracle` 分別經 `run_step_g` 取 `pool_diag` 之 `k*`；`oracle` ⛔ 呼叫段三碼）

| 態 | 退縮 `3.5 m` 之 `k*` | 退縮 `0 m` 之 `k*` |
|---|---|---|
| `legacy`（開工態） | `R1 1`／`R2 8`／`R3 7`／`R4 1`／`R5 7`／`R6 6` ＝ `K_STAR_EXPECT["3.5m"]` | `R1 2`／`R2 8`／`R3 7`／`R4 1`／`R5 7`／`R6 6` ＝ `K_STAR_EXPECT["0m"]` |
| `oracle`（KL 放行之改後配地） | `R1 1`／**`R2 7`**／`R3 7`／`R4 1`／**`R5 4`**／`R6 6` ＝ **CC 實得逐塊相同** | 同 `legacy` |

`oracle` 之 `run_step_g` 無中止（`err` ＝ `None`）⇒ 其結構閘（含 `J(k*) ≥ J(naive)`）未觸。

🔒 **裁**：該翻轉係 `R2`／`R5` 之配地依 KL 所放行者改變之**必然後果**，**受領**。`K_STAR_EXPECT` 在 `verify/run_verification.py`，原單 `§零` ⛔ 改任何錨 ⇒ **本單⛔ 重錨**；其重錨（`3.5m`：`R2 8→7`、`R5 7→4`·依上表歸因）併入「入主線後須重量之錨」。

🔒 **`V-12` 之收窄**（取代原單 `V-12` 之「✅→🔴 ＝ `0`」）：✅→🔴 之名目集 ＝ **恰** `{k* 六塊經驗錨3.5m …}` 一項，且其違規列之實得（以值比·鍵序可異）＝ `{R1: 1, R2: 7, R3: 7, R4: 1, R5: 4, R6: 6}`。其餘任何 ✅→🔴，或該項之實得異於此 ⇒ 停機款 `10`。

🩸 **併令**：CC 所報「另 `18` 項名目或違規數有變、狀態未變」——`runall_cmp.txt` 全文照原單工項五 ④ 入報告；發單側於復驗時逐項讀之（本補令⛔ 判）。

---

## `§三`　裁三：`F.3 零遺漏{tag}` 項（reviewer 第 `3` 點）

🔒 **現查**（發單側窗四十·態 `bd70bd9`）：該項在 `verify/run_verification.py`，其式 ＝ `n_pub ＝ 59 ∧ n_in ＝ 35 ∧ n_74 ＝ 24`，`n_pub` ＝ `wf_f3` 自 ctx 之 `"temp"` 所取之公設母體（`街廓分類` ∈ {道路, 鄰里公園} ∧ 非 ghost）。退縮 `3.5` 之公設母體 ＝ `59` 筆，其中屬補令一 `EXPECT_35_ON` 者 `6` 筆：`628-41(2)`（`RD4`）、`628-41(3)`（`RD1`）、`628-45(3)`（`G1`）、`628-45(4)`（`RD4`）、`628-45(5)`（`RD2`）、`628-30(4)`（`RD2`）。

🔒 **裁**
1. 補令一裁三**照舊**：交予 F.3／F.4 者為排除後之 temp。
2. 段三之後，退縮 `3.5` 之 `n_pub` 將為 `53`（＝ `59` − `6`），`0 m` 仍 `59`。開工態之 `run_all` 中 F.3 以「F.2 未成功，F.3 跳過（存在性守衛）」而未執行 ⇒ 該項現態不產生 ⇒ 本單之 `V-12` 不受其影響。
3. 本單⛔ 改該項之錨或式。其**重錨**（「`59` ＝ 併入 ＋ 轉 7-4 ＋ 段三併出之公設片」·上列 `6` 筆為其歸因）併入「入主線後須重量之錨」，並以 F.3 可執行時為其觸發。
4. `F.3 母數錨 公設地 59 筆／28 群`（`wd4` 之原始 temp）照舊——CC 所報已合。

---

## `§四`　裁四：`verify/tools/y_dump_diff.py`（reviewer 第 `4` 點）＝ **本單具名豁免**

🔒 **現查**：其用途 ＝ 對拍 **KL 本機畫面之 dump** 與 harness 之 `run_corner_pk` 接 `run_step_g` 所得（其檔首說明逐字）；倉內之 `.py` 除其自身外⛔ 呼叫之（態 `e6c8c5c`·母體 ＝ 追蹤之 `*.py`·框 ＝ 字樣 `y_dump_diff`·檔框 ＝ `1`〔僅其自身〕；對照組 字樣 `run_corner_pk` 之檔框 ＝ `196`；`verify/run_all.py` 之字樣 `y_dump` ＝ `0`）⇒ ⛔ 在 `run_all` 之內。
🔒 **裁**：畫面路徑於段三次單方接入 ⇒ 若本單單改其 harness 側，將與畫面側之 dump 生出**假差異** ⇒ **本單⛔ 動**；其改接併入段三畫面路徑之次單（與畫面同批使二側同構）。CC 於報告 `§三-4` 表列之為「豁免·理由 ＝ 本裁」。

---

## `§五`　裁五：reviewer 第 `1`、`2` 點——**受領，照 CC 所擬修正**

1. 後處理 (b) 二半片皆 `N ＝ 0` 時「未處置」列之 `受併宗` 重複傳入而拋 `TypeError` ⇒ 修正使其照補令一裁二出 `🔴`「未處置」列續辦。
2. `B` 為空、某受併宗之街廓 ∉ `B`、(c) 之 `|B| ＝ 0`（除以 `0`）⇒ 一律停機款 `9`（loud·⛔ 靜默）。
🔒 二者於本案之路徑⛔ 觸（`V-5`／`V-7` 不受影響）；其修正之 diff 與 reviewer 之覆審出艙併入報告。

---

## `§六`　工項之增改

**工項零″**（**新**·先於工項三之重建）：本補令原封入倉 `docs/orders/W-G.9-344_補令二_F3修正與k星錨歸因.md`（新檔）；`F3′` 二進位覆蓋 `verify/probes/probe_WG9344p1_pooltemp.py`；入倉 blob 須與 `§八` 之值逐位相同（停機款 `2` 之延伸）。一 `commit`，訊息逐字 `W-G.9-344 工項零″：補令二原封入倉 ＋ 量測器 F3 之修正版（F3′）入倉（側支）⛔ 零生產碼` ⇒ `push origin HEAD:verify/W-G.9-343-k929b`。

**工項二**：
- 步驟 `1`〜`8` 之出艙（於 `e6c8c5c` 所得）**得沿用**，其條件 ＝ `git diff --name-only e6c8c5c <工項零″ 之 commit>` 之輸出**恰為**本補令與 `F3′` 二路徑（出艙該輸出）；不合即於工項零″ 之 `commit` 重辦。
- 步驟 `9` 改期：`F3′ selftest` ⇒ `rc 0`（**`13/13`**）。步驟 `10` 於工項零″ 之 `commit` 重跑：`F3′ run <repo> 3.5 on` ⇒ **`rc 3`**。

**工項三**：以 `3a4fa34` 為本（得 `cherry-pick`），加本補令裁五之修正，重建為**一** `commit`，其父 ＝ **工項零″ 之 `commit`**（取代補令一所定之父）；`3a4fa34` ⛔ 推。訊息照原單工項三逐字。

**工項四**：原單 `V-1`〜`V-13` ＋ 補令一 `V-14`〜`V-16`，以 `F3′` 跑；`V-11` 之比較基 ＝ **工項零″ 之 `commit`**；`V-12` 依本補令 `§二` 之收窄；`V-14` 之期 ＝ `13/13`。
🩸 `V-12` 若於收窄後仍有他項 ✅→🔴：先於工項零″ 之 `commit` 重跑改前之 `run_all`、再比，仍有 ⇒ 停機款 `10`。

**工項五**：報告於前二單所令者外，加載：⑮ 本補令 `§二` 之 `#27` 實得與其比對；⑯ 裁五之修正 diff 與 reviewer 覆審；⑰ 工項二沿用之條件之出艙（或重辦之出艙）。

---

## `§七`　發單側自誤之具名（續補令一 `§七`·號於攢批登記時現查取號·本補令⛔ 鑄）

4. **`F3` 之片數判法以集合算期、以串列比實**，其自檢無重號之造，出單前未以真資料跑其判法 ⇒ 真資料必紅之假紅（`常規八` 之形）。由 CC 停機擋下。
5. **原單 `V-12` 之「✅→🔴 ＝ `0`」於本案結構上不可滿足**：`K_STAR_EXPECT` 係開工態之經驗值，而 KL 放行之改後配地必改 `R2`／`R5` 之切點；出單時未以 `oracle` 態量 `run_all` 之錨項。由 CC 停機擋下。
6. **補令一裁三未查 `wf_f3` 之 `n_pub` 與其排除之受詞同源**（同一 ctx 鍵兼餵調配與零遺漏之母數）。由 CC 之 reviewer 捕得。
7. **原單 `§三-4` 之呼叫端清單漏 `verify/tools/y_dump_diff.py`**。由 CC 之 reviewer 捕得。

---

## `§八`　受單側收單時須當場重算之基數

| # | 受詞 | 值 |
|---|---|---|
| `1` | 本補令 | 見本補令末列之 `SELF_SHA256`（`P-5` 原口徑） |
| `2` | 檔 `F3′`（`probe_WG9344p1_pooltemp.py`） | `8877` B·`sha256` `1c47e17a773a5836835856e14fb26cb2475300c6b8275fb75dd1e60a3ac82fc1`·CR `0` |
SELF_SHA256: d78df961d10eb4dc3b73897d9ed010504431df70a2aa48acd5b39bdb78f279c6
