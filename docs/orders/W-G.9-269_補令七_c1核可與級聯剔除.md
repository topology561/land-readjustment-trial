# W-G.9-269　補令七（序 `1`／`2` 之核可 ／ `355`–`357` 之內容補鑄 ／ `P-1` 基準器之取入 ／ 🔴 **級聯剔除**）

> **受詞** ＝ `W-G.9-269` 本體階之施工窗。
> **主線** ＝ `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2`｜**側支** `verify/W-G.9-269-c1` ＝ `5bd0868aeddb2d36f01a6cfba3937f999fe754dc`（`c1` 生產碼 commit ＝ `8f3dccc`）｜**凍存側支** `verify/W-G.9-269-p3a` ＝ `05a11bd`（皆須 `git ls-remote` 實查）。
> 🔒 `W-G.9-269` 施工單、補令一〜六、本補令**並存**；相斥處以**時序最後者**為準。

---

## §一　核可

### 一-1　序 `1`（主線）

`GB-162` 鑄畢（MAX `161→162`·缺號 `[12, 87]` 未變）；`自誤 358` 鑄畢（MAX `354→358`·缺號 `[106] → [106, 355, 356, 357]` **已明文登記**）；主線生產碼相異 `0／34`。
🔒 **簿態衝突之處置——核可**：採**乙（取 `358`）**，其依據為**既有二規之合取**（「編號衝突以倉內為準」＋「缺號永不重用」），⛔ 新訂準據；甲案（取 `355`）將與側支已鑄者撞號，**嚴格劣**。`GB-132` 避讓已當場復算。

### 一-2　序 `2`（`c1` 備妥）——**發單側自倉復驗·全項通過**

`app.py` `+94/-0`／`verify/stepg_pipeline.py` `+21/-0` ⇒ **純 `expand`·`deletions 0`**；`c1` 側支之生產碼相異恰此二檔。
**驗收（旗標 `off`）**：`stdout` `168296` B（父＝子·`34c942c3…`）／`stderr` `0` B／**`got_*` `18/18` 逐位相同**，且**⛔ 出現 traceback 行號位移** ⇒ **⛔ 須援用任何自解款** ✅
**判別力（旗標 `on`）**：`stdout` `168296 → 161084` B／`got_*` 相異 `12/18` ⇒ **非死碼** ✅
🔒 **三跑同一 worktree 依序執行、切態後逐跑前驗 `app.py` 之 `sha256`——核可**（⛔ 二 clone ⇒ ⛔ 路徑長度混淆）。

### 一-3　`c1` 實作之三處 **核可並立為例**

1. **受詞逐字取 `驗_B藍影`，docstring 明文 ⛔ 取 `驗_rejected`** 並載其由（「以之為受詞將使本批逾射程 `19` 宗」）⇒ 發單側補令一 `§二-2` 之裁**原樣落入碼面**。
2. **四態⛔ 併三**：只有逐字 `不合格` 剔除；`合格`／`不適用`／**`無從判定`** 一律留，並於碼面載「**「無從判定」⛔ 與「判定為偽」共用出艙碼**」⇒ **本案之核心紀律自報告落入生產碼**。
3. **街角第 `1` 宗⛔ 得被剔除 ⇒ `raise`**，並具名其為 `GB-104` 所令之 **loud 斷言**、**⛔ 為其判準**（構造保證仍為其真正之保證）。

### 一-4　CC 之克制 **核可**

`_wg9268p_anchor_advance` 於 `_WF_NS_NAMES` 之**既有漏列刻意未動**——修之將令 `G.1` 由紅轉綠，**破壞本 `commit` 之逐位驗收**。CC 具名該事而⛔ 順手修之。
> **立準**：**凡於一「行為須逐位不變」之 `commit` 內發現<u>與本受詞無涉</u>之既有瑕，一律<u>具名而不修</u>**——修之即破壞該 `commit` 之驗收，且使「本次改動之效」與「順手之效」不可分。其修另立工項。

---

## §二　🔴 `355`–`357` 之內容補鑄（`359`–`361`）

### 二-1　所診

`自誤 355`（機制之存在≠適用）／`356`（二態器之態已消滅）／`357`（跨器逐位對拍未證序列化約定）**僅鑄於凍存側支 `05a11bd`**，該側支依補令六 `§二-2` **⛔ 併線·⛔ 刪除** ⇒ **其內容永不入主線**。
🔒 三者之**通則**皆為留予後窗者，**⛔ 得只存於一條不會併線之分支**。

### 二-2　處置（**補鑄·⛔ 追改缺號登記**）

於**主線**（落點見 `§五`）補鑄 **`自誤 359`／`360`／`361`**，逐則：
1. **內容逐字轉錄**自凍存側支之 `355`／`356`／`357`（⛔ 改寫·⛔ 摘要）；
2. 標題或首列**逐字載其對應**：「本則之內容原鑄為 `自誤 355`（於側支 `05a11bd`·依 `W-G.9-269` 補令六 `§二-2` ⛔ 併線）⇒ 其內容以本號承載；`355` 於主線**維持缺號**。」三則各自對應 `355`／`356`／`357`。
3. 🛑 **⛔ 追改**主線既有之 `355`–`357` 缺號登記一字——其為真（該三號於主線確為缺）。
4. 🛑 **⛔ cherry-pick 凍存側支之任何 commit**——該側支係 `GB-162` 之證據，其物件須保持原狀。

---

## §三　`P-1` 基準器之取入

**採 CC 之 `(甲)`**：`verify/probes/probe_WG9269_coverage_single.py` 係**純新增檔·零生產碼**，且為補令四 `§三-2` 所令之 `P-1` 唯一基準器 ⇒ **取入**。
**辦法**：`git checkout <凍存側支之物件> -- <該路徑>` 後逐檔具名 commit；⛔ cherry-pick 該側支之 commit（理由同 `§二-2` 款 `4`）。
🛑 **須出艙**：取入之檔與凍存側支之同名檔 **blob `sha1` ⋀ file `sha256` 逐位相同**；並於本批報告具名其**來源之側支與 commit**。
🛑 取入後須**於主線態重跑一次**，其人造三造須全成立、其 `34` 組·逐對總數須為 `0`（＝ 補令四 `§三-2` 之父態基準）。

---

## §四　🔴 級聯剔除：`c2` 之土地後果**大於**受詞表

### 四-1　所診（CC 所量·發單側自倉復算相符）

旗標 `on` 之終態：`0m` 配地宗 `46 → 43`（**消失 `3` 宗**）／`3.5m` `46 → 45`（消失 `1` 宗）；二情境之終態**皆⛔ 再有閘一不合格者** ⇒ **迴圈收斂**；**新增之宗 ＝ 無**。

| 宗地 | `a 面積(㎡)` | 原分配 `G(㎡)` | 宗地寬度(m) | 閘一（`off` 態） | 何以消失 |
|---|---|---|---|---|---|
| `628-42(1)` | `8.34` | `0.23` | `0.18` | **不合格** | 直接剔除 |
| `628-53(2)` | `2.78` | `0.00` | `0.02` | **不合格** | 直接剔除 |
| **`628-27(1)`** | **`51.32`** | **`29.26`** | `0.65` | **不適用** | 🔴 **遞補後幾何前移方轉為不合格 ⇒ <u>級聯</u>剔除** |

🔒 **此係 `K-9-17` 五（「後面的宗也會整體往前分配，所以也要重算 `G`」）照裁定運作之必然**，⛔ 新生之缺陷。
🔴 **惟其量級**：`628-27(1)` 之原分配較前二宗**大二個數量級** ⇒ **`c2` 之土地後果大於補令三 `§零-1` 之受詞表**（`0m` 實為 `3` 宗）。

### 四-2　處置（`c2`／`c4` 之必載）

1. **差異表 `表一` 須逐宗載明該 `3`（`0m`）／`1`（`3.5m`）宗**，並於「處置」欄區分 **`閘一直接剔除`** 與 **`級聯剔除`** 二類；**⛔ 併為一類**。
2. **`628-27(1)` 之歸戶須具名**（發單側查其歸戶對映於 `harvest()` 時為空 ⇒ 須於**管線跑畢之態**取之）。
3. **`表三` 須載**：改列調配池之宗數・面積合計・**逐戶具名**；其中**級聯剔除者單獨小計**。
4. 🛑 **`c2` 呈 KL 時，本節之三宗須逐宗列出**——**⛔ 以「受詞二宗」呈之**。

---

## §五　`c1` 之併線：**呈 KL**

🔒 **發單側已於 `5bd0868` 逐位元組復驗，全項通過**（`§一-2`）。
🛑 **`c1` 之併線俟 KL 放行。**
🔒 **落點之序（保全快轉）**：`§二` 之補鑄、`§三` 之取入、及本補令之工項零，**一律落 `c1` 側支**（主線待併期間⛔ 前進·補令十五 `§四-2`）；三者皆**零生產碼**、⛔ 影響 `c1` 之逐位驗收。
🛑 其後發單側**重驗**（主檢 ＝ `app.py` ⋀ `stepg_pipeline.py` 二檔之 blob 仍為 `8f3dccc` 之內容 ⋀ 生產碼 `34` 檔中恰此二檔相異），通過即快轉併線，**⛔ 再問 KL**。

---

## §六　鑄號之授權

🔒 **本補令得鑄者 ＝ `自誤 359`／`360`／`361` 三號；⛔ 逾此。**
（次號當場現查·框 ＝ **標題形 ∪ 表列形**；期初主線 自誤 MAX `358`·缺號 `[106, 355, 356, 357]`。三則須各自逐字載其對應之凍存原號。）
🛑 意指占用查**二形並取**；哨兵執行期自倉掃出、字面⛔ 出艙。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §七　續辦

| 序 | 事 | 落點 |
|---|---|---|
| `1` | 工項零 ＋ `§二` 補鑄 `359`–`361` ＋ `§三` 取入 `P-1` 基準器並於主線態重跑一次 ＋ `§一-4` 立準入交接註 | **`c1` 側支**（⛔ 主線） |
| `2` | 發單側重驗 → **KL 放行** → 快轉併線 | — |
| `3` | `c2`（🔴 旗標 `on`·**有土地後果**·側支·**單獨呈 KL** ＋ **`§四-2` 之差異表**） | 新側支 |
| `4` | `c3`（contract）→ `c4`（差異表 ＋ 報告 ＋ `§五` 後置二項） | — |

🛑 序 `2` 之前：⛔ 推主線、⛔ 翻旗標之預設。
🔒 **`GB-162` 生效**：⛔ 執行 `WV_BAKE=BASELINES`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數**——差異表之數一律自**當次實跑**取得。
🔒 **`GB-159` 仍有效**：二宗（今為三宗）之配地圖與負擔表⛔ 對外。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
