# W-G.9-241　排序準據 seam 之**偵察** ＋ 交接文 `§七` 該列之更正 ＋ 自誤一則

> **零生產碼**（偵察報告 ＋ `docs/` 之 `.md` 追加）⇒ **CC 得逕行 push**。
> ⛔ 動 `app.py`／`verify/**.py` 任何一字。**本單⛔ 抽 seam**——抽 seam 為 `W-G.9-242`，
> 其為生產碼、須 KL 逐 commit 放行。動之即停機上呈。

---

## §零　開工閘（缺一即停）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，依既有口徑（自檔首至
`SELF_SHA256:` 該列之前一個 `\n`（含該 `\n`）為止之全部位元組）重算對拍。不符 ⇒ 停機。

**`S-1`（號占用·宣告框）**　依 `CLAUDE.md` 之宣告框 ①–④ 查 `W-G.9-241`。

發單側於 `2026-09-05` 當場實測（**三軸**：來源 `blob@09d1deda3b41e64272a9b0ca24df3d53d116a100`
／母體 **全 `docs/` 556 檔**（其中 `19` 檔為 `.png`／`.jpg` 二進位讀不到，檔名皆⛔ 含單號）
／粒度框 **檔框 ＋ 列框**）：

| 號 | 宣告框 檔／列 | 鬆框 檔／列 | 角色 |
|---|---|---|---|
| **`W-G.9-241`** | **0**／**0** | **0**／**0** | 受詢 ⇒ ✅ 未占用 |
| `W-G.9-240` | 4／7 | 4／20 | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-242` | 0／0 | 0／0 | 對照組 乙（次號·須 `0/0`）✅ |

CC 須**自行復算**上表三列，⛔ 採信本單任一值。任一列不符 ⇒ 停機上呈。

**`S-2`（append-only）**　本單受詞之既有二檔皆為**純末端追加**，⛔ 改其上文一字。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-241_排序準據seam偵察與交接文更正.md`。⛔ 改一字。

---

## §二　工項一：**seam 偵察**（本單要害·零生產碼·⛔ 動碼）

### `a`　已知之錨（發單側於 `09d1ded` 實測·CC 須逐一復現）

| 落點 | 逐字 | 性質 |
|---|---|---|
| `verify/wf_f2.py:99` | `"""§2 相鄰：共享 BLOCK 邊(<0.5m) 或 隔單一 RD 相望。回傳 set of frozenset({a,b})。"""` | **相鄰之定義**（幾何） |
| `verify/wf_f2.py:161`–`165` | `lvl = "級1相鄰"` ／ `"級2鄰近"` ／ `"級3非鄰近"` | **級之指派**（字面） |
| `verify/wf_f4.py:487` | `LV = {0: "級1相鄰", 1: "級2鄰近", 2: "級3非鄰近"}` | **級之字面表** |
| `verify/wf_f4.py:798` | `"級別": LV[_level(gid, blk)]` | **級之消費點** |

計數（發單側實測·`grep -c`）：

```
「級1」   wf_f0 = 1   wf_f2 = 3   wf_f4 = 13
「相鄰」  wf_f0 = 1   wf_f2 = 4   wf_f4 = 4
「鄰近」  wf_f0 = 0   wf_f2 = 3   wf_f4 = 2
sorted(   wf_f0 = 11  wf_f2 = 6   wf_f4 = 39
```

### `b`　CC 須產出之偵察表（**逐點·⛔ 抽樣**）

母體 ＝ `verify/wf_f0.py` ∪ `verify/wf_f2.py` ∪ `verify/wf_f4.py` ∪ `app.py`。
對**每一個** `sorted(` ／ `.sort(` ／ 級字面之出現點，逐列判其角色，出表：

| 欄 | 內容 |
|---|---|
| 檔:列 | `git cat-file blob` 之列號 |
| 逐字 | 該列原文（截 120 字元內） |
| 角色 | `A` 選塊準據（**seam 之受詞**）／`B` 輸出排版（報表列序）／`C` 內部決定性（tie-break、去重、穩定序）／`D` 其他 |
| 判之依據 | 逐字理由；**⛔「看起來像」** |

🔒 **判 `A` 者須另載**：其**輸入**（何者被排）、**鍵**（依何排）、**消費端**（誰讀其結果）。
🔒 **⛔ 以「數量太多」為由抽樣**——若逐點不可行，**停機上呈並報其數**，⛔ 自行縮母體。

### `c`　🔴 **判別力之前置量測**（本工項之要害·⛔ 略）

`verify/wf_f2.py:20` 逐字載：

> UC9898 七轉出**全級1（相鄰）**；級2/3 不現。

⇒ **本案之級2／級3 路徑從未被執行。**

**CC 須實測並回報**：
1. 現行 UC9898 之各級實際命中筆數（`級1` ／ `級2` ／ `級3`），**分退縮 `0` 與 `3.5` 二情境**；
   **⛔ 引用上開 docstring 之數**——其為前批之記載，須當場復現。
2. 若級2／級3 之命中確為 `0`：**逐字載明**「本案對『級序之更換』**無鑑別力**」。

🔒 **此測之用途**：`W-G.9-242` 抽 seam 後，若以 UC9898 之輸出「逐值不變」為驗收，
在級2／級3 從未執行之下，**該綠無鑑別力**（同 `GB-51` 所引之失敗考古：退化案例無鑑別力）。
⇒ `-242` 之驗收須改以**構造 ＋ 注入對照**（注入一個蓄意相異之準據，證輸出**確實改變**），
而其可行性取決於本測之結果。

### `d`　產出

`docs/reports/W-G.9-241R_排序準據seam偵察_執行報告.md`。
⛔ 於本單提出修法建議——**偵察歸偵察**；修法由發單側依本報告作成 `-242`。

---

## §三　工項二：交接文 `§七` 該列之**更正**（發單側之誤·⛔ 就地改）

### `a`　被更正之對象

`docs/orders/交接文_W-G.9-240.md` `§七` 待落地表之列，逐字：

> `| GB-67／telescoping 結構閘 | ⬜ | 二者致 R4 逐值迴歸不可得 |`

字樣錨：`grep -c "二者致" docs/orders/交接文_W-G.9-240.md`（CC 須報其數）。

### `b`　更正之逐字（**追加於該檔末端**·⛔ 改上文一字）

```markdown
---

## 🔧 `§七` 之更正：`GB-67`／telescoping 一列（`W-G.9-241`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄號**。

🩸 **案由**　`§七` 該列之「二者致 `R4` 逐值迴歸不可得」係**發單側照抄 `W-G.9-238` 交接文
`§五`、⛔ 自倉復現**。經查 `CLAUDE.md` 序 `12` 之依賴鏈，**三處與正典不符**：

🔒 **一　射程寫窄**
> 環 `2` 逐字：其後果 ＝ `run_step_g` 於 **`R2`** 終止 ⇒ **`R4`／`R5`／`R6` 一筆不產生**。
⇒ 受詞為**三個街廓**，⛔ 僅 `R4`。

🔒 **二　二事被併為一事**
> 序 `12` 之解除條件 ＝ **環 `1`（telescoping）轉綠**；
> 「其與 `GB-67`（環 `4`）**係二事**——`W-G.9-172R:59` 逐字載二中止點『🔴 **相異**』」。
⇒ ⛔ 以「此二閘」稱之；二者分列、各有其解除條件。
   退縮 `3.5` 之中止點 ＝ telescoping 結構閘（`verify/stepg_pipeline.py:1261`）；
   退縮 `0` 之中止點 ＝ `②-宗 圍堵閘` ＝ `GB-67`（破量 `R2` `45.9766 ㎡`／`R5` `56.3293 ㎡`）。

🔒 **三　性質誤植（最要害）**
> 環 `3` 逐字：該閘係 **harness 專屬**（`app.py` 命中 `0`）⇒ **⛔ 影響 app 之配地**。
⇒ 其為**驗收能力缺口**，**⛔ 產品缺陷**。
   ⛔ 以「把驗收缺口帶進土地後果批」推論其會使配地算錯。

🔒 **更正後之該列應讀為**
> `GB-67`（退縮 `0`·`②-宗 圍堵閘`）與 telescoping 結構閘（退縮 `3.5`·harness 專屬）
> **係二事**；後者致 harness 於 `R2` 終止 ⇒ **`R4`／`R5`／`R6` 逐值迴歸不可得**，
> 現行迴歸皆為**替代證據**（同一鏈之行為逐位相同·含同點 `raise`）。
> **二者皆⛔ 影響 app 之配地。**
```

### `c`　驗收

`deletions ＝ 0`；並報字樣錨之前後二數。

---

## §四　工項三：自誤一則（號須 CC 當場復算）

🔒 **末號之獨立復算**（框 ＝ `VR-091` 補款二 ① 之正字定義框 `A` ＋ 範圍框；
母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔；粒度框 列框）。
發單側於 `09d1ded` 實測 ⇒ 相異 `284`／`MAX` `291`／缺號 `[106]` ⇒ **`N` ＝ `292`**。
**CC 須自行復算；不符即停機上呈。**

### `自誤 N`｜**沿用交接文之未證斷言，且該斷言成為下游論證之前提**

🩸 **案由**　發單側作成 `交接文_W-G.9-240 §七` 時，將 `W-G.9-238` 交接文 `§五` 之
「二者致 `R4` 逐值迴歸不可得」**照抄入表**，⛔ 自倉復現。CC 其後以該句為據，
向 KL 提出「先解此二閘、否則五級落地將無法對 `R4` 逐值驗收」之排序主張。
發單側查證後，該句**三處與正典不符**（見 §三 `b`）。

🔒 **受詞** ＝ **交接文之繼受斷言**。
🔒 **族** ＝ `W-G.9-238` 交接文 `§六` 之「憑設想給錨」（**第 `8` 次**）。
🔒 **加重情狀**：`-238` 交接文 `§六` 紀律 `1` 逐字明令
「⛔ 沿用本交接文、⛔ 沿用記憶」，而發單側**在自己作成的交接文裡違反它**，
並使該誤傳至 CC、再傳至 KL 之決策桌面。
🔒 **未致實害之原因**：KL 將該選單轉回發單側複核 ⇒ 於決策前攔下；
**⛔ 讀為「機制有效」**——攔下者係 KL 之轉呈，⛔ 發單側之自檢。

🔒 **通則（自本則起）**
> **交接文之「待落地表」每一列，凡含斷言者，作成當下即須自倉復現並於同列載其錨**
> （檔:列 或 `grep -c` 之數）。**⛔ 以表頭之「接手窗須自倉重生此表」概括免除**——
> 該句之受詞為**接手窗**，⛔ 免除**作成者**當下之查證義務。

**責** ＝ **發單側**。

---

## §五　產出與 push

- **零生產碼** ⇒ CC **得逕行 push**，⛔ 待授權。
- 報告須逐項載：`S-1` 三列之自行復算值、工項一 `b` 之全表與 `c` 之二情境命中數、
  工項二之 `deletions` 與字樣錨前後二數、`N` 之自行復算值。
- 🔒 凡出艙任何命中數，須同格載三軸（來源 commit ／ 母體 ／ 粒度框·`VR-091`）。

SELF_SHA256: 7fb2848757369ca6652dbe5992e519cf4d6c37f22bac7f7e01e20d076ed71b10
