> 🛑 **本文⛔ 為補令**——其為 `c4` 窗之開工令（KL 對話側交付）。本窗之工項零之正本 ＝ `docs/orders/W-G.9-269_補令十七_c3併線核可與c4續辦.md`（`file sha256 ee8b3a66…`）。更名之由見 `W-G.9-269` 補令十八 `§一`。

【W-G.9-269 · `c4` 專窗 · 受詞 ＝ CC · 零生產碼 · 本批最後一項】

倉：topology561/land-readjustment-trial。二 ref 皆以 `git ls-remote` 實查
（⛔ 本地追蹤 ref；fetch refspec 未帶 `+` 會使其半靜默停在舊值；本地 checkout 亦曾落後 origin）：
  wip/s1-endpart        ＝ `c3` 併線後之 tip（主線）
  verify/W-G.9-269-p3a  ＝ 05a11bd…（🛑 凍存·⛔ 併線·⛔ 刪除·`GB-162` 之證據）

🔒 **本窗之工項零 ＝ 補令十七（由 KL 於對話側交付）；其餘正本全在倉內**，
   `c4` 之完整規格 ＝ 補令十六 `§三`（`(a)`〜`(c)`）＋ `§五` 序 `3`〜`5`，自倉逐字讀。
  docs/reports/W-G.9-269R_c2.md（`c2`／`c3` 之三表與 `P-0`〜`P-9`·**差異表之來源**）
  docs/reports/W-G.9-269R_本體階_新窗開工與c2前置.md（**末端交接註 ＋ 坑之定義處清單**）
  verify/probes/probe_WG9269_pit_index.py（索引器·先跑一次取現態之索引）

🛑 **開工閘之前**：跑索引器取全倉之坑索引，就其所列**逐坑覆述徵候與攔法**；
   查不到定義者⛔ 猜、⛔ 略過 ⇒ 停機上呈。

🔒 **硬準則**（違者停機）
  〔遠端〕`ls-remote` 實查；動主 checkout 前另驗 `left-right`；
    **態宣告二值並報**（`rev-parse HEAD:<檔>` ⋀ `hash-object <檔>`·**以後者為實**）。
  〔器〕跑任何倉內探針前先 `grep "worktrees\|REPO *="`；於生產呼叫點之外取生產函式之值，
    **輸入須以間諜法取自生產**；跨器逐位對拍須先證序列化約定；
    以「某字樣命中 0」為閘者須以 **AST 去 docstring** 判；
    以表頭欄名定位欄索引者須先證「表頭欄數 ＝ 資料列格數」；
    **⛔ 以 `environ.pop(FLAG)` 取某一具名之態**（所取實為預設）。
  〔對照〕**凡否定判（查無／命中 0／無定義）須備「必命中之已知成員」對照並出艙其結果**；
    **對照組全零 ⇒ 先判量測器紅**；二造同色 ⇒ 閘失能。
  〔數〕逐情境分列或具名其情境；相減之二數須同情境；冠以「全掃／母體 ＝ …」者其列須為母體全體；
    二方之數相異 ⇒ **先對齊母體再判孰誤**；值域之 min／max 須各自具名其宗與歸戶；
    凡引一數其來源須為**已入倉**之物；**未量⛔ 印 0**（印「⛔ 可得」）。
  〔紀律〕「未量」⛔ 與「量得為零」共用出艙碼；長跑之「無輸出」⛔ 讀為「無差異」，
    須先證該跑確已執行；雜湊具名其式；四簿之數須具名其框（**標題形 ∪ 表列形**）；
    意指占用查**二形並取**；**⛔ 於生產碼或任何入倉文件宣稱一件尚未發生之事**。

🛑 **現行阻塞項（`c4` 報告須逐則列其現況與解除條件）**
  `GB-159`：受影響之宗之配地圖與負擔表 **⛔ 對外**；其解除 ＝ 本窗 `§三-(c)` 之三條件結清判，
            **⛔ CC 自裁**——備齊後**呈發單側**，由發單側呈 KL。
  `GB-160`：`V-1′` 器於 `3.5m·R5·left`／`R6·right` 之閘 γ 不過 ⇒ 本窗 `§三-(b)` 重量；
            **仍不過 ⇒ 成因⛔ 在退化幾何，須另診並具名**。
  `GB-162`：⛔ `WV_BAKE=BASELINES`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；
            **⛔ 引任何自 baseline 取得之數**——表內之數一律自**當次實跑**取得；
            **⛔ 出艙三戶之最終補償數**。
  `GB-163`：**⛔ 引 `P-3` 為「每一留存宗皆臨二線」之證**（其判定集僅 `6/56`／`5/59`）。

🔴 **差異表之硬款（`c4` 之交付物·域語言·⛔ 程式術語／變數名／行號／旗標名）**
  ‧ 三表 ＋ **「既有裁定」欄**逐宗標明（`G025`／`G030` ＝ 已由 KL 裁定 (D) 判不配地（梯3）；
    **`G011` ＝ ⛔ 既有裁定·本批為新**）。
  ‧ **`ΣG_被剔` ⋀ `Δ池（實測）` 分欄並列**並具名其**非同一量**
    （`0m`：`ΣG_被剔 29.49` vs `Δ池 +45.03`）。
  ‧ 規格 §7（`v3` 錨態：`1.84`／`55.64`／Σ`57.48`）與**當次實跑**（`0.26`／`55.80`／Σ`56.06`）
    **二組並列並具名其時點**，⛔ 只呈其一。
  ‧ 值域之 min／max **各自具名其宗與歸戶**；**⛔ 以區間掩去負值之個體**。
  ‧ `GB-159` 之「⛔ 對外」與其三解除條件須載於表首。

工項依補令十六 `§五` 序 `3`〜`5`：`c4` → `W-G.9-269` **結案報告** → 收工交接。
零生產碼 ⇒ 得逕推主線。⛔ 動生產碼 `34` 檔一字。

純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。
