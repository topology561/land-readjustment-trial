# `W-G.9-283`　施工單：「替身線」等同之**出處判定** ／ `forced` band 之界係**定義**抑或**近似** ／ `GB-169` 之成因指名

> **態**｜開工 ＝ `3fb2f250a7a665f46ca87cd6e96f10e377efc13e`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·⛔ 採本單之數）
> **單** ＝ `docs/orders/W-G.9-283_施工單_替身線等同之出處判定與forced帶之界.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（出處判定 ＋ 逐字量測 ＋ 登記·**零生產碼**·**⛔ 替身**·**⛔ 反事實**）
> 🛑 **本批鑄號 ＝ `自誤 386` 一號**（逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批·⛔ 呈 KL）。

---

## `§零`　開工閘（九項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `3fb2f250a7a665f46ca87cd6e96f10e377efc13e` 或其後裔（⛔ 以本地追蹤 ref 為證） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | 須**非**祖先；判別力[必為祖先] ＝ 本批基座之任一祖先 `commit` |
| `4` | `SELF_SHA256`（`P-5` 口徑） | 見本檔末列·**由發單側供值**；判別力二造（全檔同口徑／受詞多 `1` byte）須皆相異 |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-283` ＝ `True`；[必偽] 含 `W-G.9-282` ＝ `False` |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0`（⛔ 以「提示 `0`」逕充綠） |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | 四簿 ＋ 坑 | 自誤 **`375`**／**`385`**｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 **`85`／`85`**·下一標籤 `bh` |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec `verify/*.py` 多咬之子層檔數 **須 `> 0`**（🆕 **判準式·⛔ 定值**；發單側於 `3fb2f250` 實跑得 **`299`**·**其值逐批漂移·⛔ 據以判異常**·由 ＝ `自誤 386`） |
| `8` | `verify/baselines` | `298` 檔·框 `sha256` `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**；併報 `=false` ＝ `7f2a5fba0467…`） |

🛑 **本批一切以 `git` 輸出文字為框者，一律同格具名 `core.quotePath`**，或改取 `-z` 之原始位元組（坑 `bc`·`自誤 379`）。
🛑 **一切[必為零]之哨兵一律<u>執行期組出</u>、字面⛔ 出艙，且須<u>先驗其於現母體之命中為 `0`</u>**（坑 `bd`）。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-282` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `302b281bcf2f1901…`）／
生產碼 `34` 檔於 `8d1e50e8..3fb2f250` 異動 **`0`**／自誤 相異 `375`·MAX `385`（**恰 `+3`**）·`GB`／`VR`／`K-9` **逐位未動**／
`ALPHA_LABELS` 一字未動／`p3a` **非祖先** ⇒ **✅ 全批核可**。
🩸 CC 之二則自捕（取號現查之器紅：坑 `bc` ＋ 坑 `bd` 併發／落檔 `CRLF`）**皆於出艙前攔下**，處置正確 ⇒ 核可。

---

## `§一`　裁（**發單側·逐條**）：`W-G.9-282R §十二` 所呈五項

| # | 所呈 | **裁** |
|---|---|---|
| `1` | 「應分列」之**法律效果** | **分列已成立為機械事實**（`A1` 二格／`B1` 二格），予以**登記**（`§五-1`）。🛑 **⛔ 據以收窄 `GB-169` 之射程**——其射程仍為 `R(W_0) > 0` 之側，**四格全數在內**。其**法律效果屬 KL**，惟**尚⛔ 呈**：須先完成 `§三` 之出處判定（見該節之由） |
| `2` | `§四` 款 `5` 之**替代對拍** | **受領**（`cum_S ↔ buf` 之逐位對拍）。**附款**：其前提係一**否定性存在宣稱**，依 `W-G.9-57 §C` 之款，母體須逐一具名所查之側 ⇒ 本批**補足其射程**（`§四-2`），⛔ 以前批之三檔為足 |
| `3` | 本批自捕是否須**鑄新坑** | **⛔ 鑄**。二者皆**重踩**：`core.quotePath` 之形在坑 `bc` 與 `自誤 379` 射程內；哨兵字面入倉之形在坑 `bd` 射程內 ⇒ 以**末端追加**登記其重踩即足（`§五-3`）·**⛔ 動 `ALPHA_LABELS` 一字** |
| `4` | 閘 `7` 期值 `298` vs 實測 `299` | **照實受領·⛔ 頂替·判別力不受影響**（判準為「須 `> 0`」）。🔴 **責 ＝ 發單側** ⇒ 鑄 **`自誤 386`**（`§五-2`）；本單 `§零-7` 已改書為**判準式**並具名其實跑之 `commit` |
| `5` | `_corner_buffer_S` 呼叫次數少於乙格數 | **照實受領**。其由 ＝ `run_step_g` 於 `R4 right` **提早中止**（＝ `GB-168` 之已登記現象）⇒ **⛔ 鑄新號**；報告須與 `GB-168` **互引**，⛔ 另立解釋 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-283_施工單_替身線等同之出處判定與forced帶之界.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-282` 之單須 `1`／造乙 ＝ 執行期組出之人造名須 `0`）；
`CR` ＝ `0`（併報 `LF` 與 bytes；判別力 ＝ 一真 CRLF 人造檔須 `> 0`）；blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：「該點即『面積換算出來的替身線』」之**出處判定**

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

`W-G.9-282R` 已坐實 `forced` 側鏈頭之 `left_cum_S` 與 `_corner_buffer_S` 之回傳 `buf` **逐位相同**，
且 `buf` 之目標量為**面積**（`range_area`）。**若**倉內「該 buffer 起算點**即**『面積換算出來的替身線』」
此一**等同**係 KL 之語，則碼面與 `K-9-5-13` 裁定二相斥；**若**其係**助手側補充**，則該相斥**尚未成立**。
🔴 **此一分辨即 `K-9-5-12（六）` 通篇之教訓**（一句助手側補充被當成 KL 裁示用了十幾輪）
⇒ **在其判定之前，⛔ 鑄 `GB`、⛔ 呈 KL、⛔ 判碼面違反**。

### 三-2　準據（**發單側明定·⛔ CC 自訂**）

採 `W-G.9-15` 甲組之**同一**機械規則（器 ＝ `verify/tools/wg915_ruling_provenance.py`——
其框**逐字出艙**·**⛔ 另寫第二份判準**；若該器之受詞與本項不合，**loud 明載其不合之處**、⛔ 逕自改寫）：
引文行 ＝ `^>`；引文區塊 ＝ **極大連續段**；引言 ＝ 區塊起點**往上最近之非空白非引文行**；
判三類 ＝ **KL 逐字**／**助手側補充**／**無法判定**。

### 三-3　逐款出艙（🛑 **出艙即止**）

| 款 | 受詞 | 框／錨（字樣·**⛔ 行號為錨**） |
|---|---|---|
| `1` | 該**括號子句**所在之**行逐字**、其所屬引文區塊之**起迄**、該區塊之**引言逐字** | 錨 ＝ `grep -n "失效加註" <K-6>`（無 `^` 錨 ⇒ **二數並報**）·母體 ＝ `K-6` 單檔（併報 B 數／列數） |
| `2` | 該行與其**前一行**（載 KL 裁定二之「」段）之**分界**：二行逐字並列 | 併出艙該括號子句**是否落於任一「」引號對之內**（框 ＝ 該列內 `「[^」]*」` 之比對·**逐列**） |
| `3` | 該括號子句之**引入 `commit`** 與其日期 | `git log -S` 之**字面**（⛔ regex）；併出艙 `K-9-5-13` 節之引入 `commit` 與日期，及二者之 `merge-base --is-ancestor`（含**反向同式**之判別力自檢） |
| `4` | 器之判（三類之一） | 逐字出艙器之輸出列；**⛔ 以「在 `>` 內」充「KL 逐字」**——須依 `三-2` 之**引言**判之 |
| `5` | 判別力二造 | [必為 KL 逐字] ＝ `K-9-5-13（二）所答` 之裁定二該行／[必為助手側] ＝ `K-9-5-12（六）` 已判之「首宗起點」行 ⇒ 二者經**同一器**須得**相異**之判；若同判 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判該等同成立與否、⛔ 判碼面是否違反 `K-9-5-13`／`K-9-5-12（四）`、⛔ 鑄 `GB`、
⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§四`　工項二：`forced` band 之界係**定義**抑或**近似** ＋ 否定性宣稱之**射程補足**

### 四-1　`buf` 所定之界之性質（**逐字·⛔ 判**）

| 款 | 受詞 | 框／錨 |
|---|---|---|
| `1` | `_corner_buffer_S` 之「**帶之構造**」段與「bisect 解 `buf`」段**逐字**（🛑 前批已出艙者**本批重量**·同格具名框與母體） | 字樣錨 `grep -n "_corner_buffer_S" app.py`（無 `^` 錨 ⇒ 二數並報） |
| `2` | **「真實池帶面積」之算式**：bisect 之目標函式**逐字**——其面積係由 `_block_strip`／`_strip_s_range` 之**實際多邊形**算得，抑或由任何**矩形／平均深度**之近似算得 | 母體 ＝ `_corner_buffer_S` 之**函式體**（AST 區間·⛔ 文字層估界）；[必命中] `_block_strip(` 於該函式體內須 `≥ 1`（若 `0` ⇒ **loud 明載**）／[必為零] 執行期組出之人造字樣 |
| `3` | `range_area` 之**來源**：其產生式逐字 ＋ 所依之裁（字樣錨） | 母體 ＝ 呼叫端（`app.py` ＋ `verify/stepg_pipeline.py`·逐檔具名） |
| `4` | 該帶之**上界**是否即 `buf`（`s ∈ [max(s_min,0), buf]`）與消費端 `left_cum_S = buf` 之**逐位對映** | 前批已得二格之逐位相同 ⇒ 本批**重量**並併報其框 |

🛑 **出艙即止**：⛔ 判該界為「真正的地界」抑或「替身線」——該判繫於 `§三` 之出處判定。

### 四-2　否定性存在宣稱之**射程補足**（`W-G.9-282R §四 款 5`）

前批以**三檔**為母體斷「碼面⛔ 產生抵費地之實際配地多邊形」。本批**補足其射程**：
母體 ＝ `verify/` 之**全部** `.py`（**含子層**·正面列舉其檔數·⛔ 以 pathspec glob 界定·坑 `be`）
＋ `docs/specs/` 之**全部** `.md`（正面列舉其檔數）。
所查字樣**逐項具名並逐項出艙其命中**（檔數／列數二數並報）；🛑 **⛔ 只書「查無」**——
須逐一列出**所查之側**（`W-G.9-57 §C` 之款）。若於 `docs/specs/` 查得其**規格**而碼面未實作 ⇒ **loud 具名**，⛔ 判其效力。

---

## `§五`　工項三：登記（**逐字由本單供·⛔ 改寫·⛔ 鑄 `GB`／`VR`／`K-9`／坑**）

### 五-1　`GB-169` 之**成因指名**（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**）

> **`GB-169` 之成因指名（`W-G.9-282` 實測·⛔ 解除、⛔ 收窄、⛔ 鑄號）**
> 其射程內之 `R(W_0) > 0` 四格，依 `W-G.9-282 §三` 之判準落於**二支**：
> **`A1`（`0m` 態甲／態乙 `R2/left` 二格）**：錨 ＝ `corner_pt`，且**逐位等於**該側 `SIDE_LINE` 之 `p1`
> ⇒ 其 `W_0`（`1.9195844913610927e-10`）之非零係**浮點殘差**。
> **`B1`（`0m`／`3.5m` 態乙 `R4/right` 二格）**：錨 ＝ `end_pt`（＝ `corner_pt ＋ actual_max_proj·d̂`·
> `actual_max_proj` 源 ＝ `_oblique_s_max`），**⛔ 逐位等於** `p1`；其 `â·(錨 − p1)` ＝ `0.17750040690957658`，
> 與碼面 `W_0`（`0.17750040709932335`）同量級 ⇒ 其 `W_0` 係**真實幾何垂距**、**⛔ 浮點殘差**。
> ⇒ **成因 ＝ 右鏈之鏈頭錨取自沿 `d̂` 推進所得之 `end_pt`，而該點⛔ 落於該側 `SIDE_LINE` 上。**
> 🛑 **本節⛔ 解除、⛔ 收窄 `GB-169`**（射程仍為 `R(W_0) > 0` 之側·**四格全數在內**）；
> ⛔ 判任何側為違反、⛔ 提修法主張。

### 五-2　`自誤 386`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 386`**：**以一個隨倉態漂移之量書為開工閘之「期值」**——`W-G.9-282 §零-7` 之判別力[必不命中]
> 載「發單側實跑得 `298`」，而該數**隨每批新增之 `verify/` 子層檔而增**（受單側於 `8d1e50e8` 實測 `299`·
> 差 `1` ＝ 前批所新增之探針）。
> **徵候**：受單側須自行判「差 `1` 是否為異常」，而該款之真正判準本即「須 `> 0`」。
> **族** ＝ `自誤 382`（轉引計數須帶母體）之**時間軸變體**：母體隨時點漂移而期值書為定值。
> **攔法**：凡期值係隨倉態漂移者，一律書為**判準**（如「須 `> 0`」）＋**具名其實跑之 `commit`**；⛔ 書為定值。

### 五-3　二則**重踩**之登記（同 `§五-1` 之檔末追加節內·續列·**⛔ 鑄坑**）

> **坑 `bc`／`bd` 之重踩（`W-G.9-282`·⛔ 鑄新號）**：取號現查之器於首版同時踩中二坑——
> `(1)` `git ls-tree` 未釘 `core.quotePath` ⇒ 含 CJK 之路徑被引號跳脫 ⇒ `.md` 母體僅 `32` 檔而
> **自誤登記表自身被排除** ⇒ 對照組甲（**已鑄**之號·須 `> 0`）三形皆得 `0`；
> `(2)` 所取之人造哨兵**其字面已入倉** ⇒ 反得 `1`／`1`。
> **攔法（已施）**：改取 `-z` 之原始位元組；哨兵一律**執行期組出**並**先驗其於現母體之命中為 `0`**。

🔒 各落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**（造甲 `True`／造乙 `old+b'X'` ⇒ `False`）。

---

## `§六`　收工閘（**量測與其所守護之動作分開呼叫**）

| # | 受詞 | 期／判準 |
|---|---|---|
| `1` | `numstat` 刪除欄逐檔具名 | **全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0`；對照組[必非零] ＝ 一含 `app.py` 之歷史區間須 `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 ＝ 一已知含 `CR` 之倉內檔須 `> 0` |
| `4` | 四簿期末 | 自誤 相異 **`376`**／MAX **`386`**（＝ 期初 `375`／`385` ＋ 本批一號·**⛔ 其他增減**）；`GB`／`VR`／`K-9` **一字未動** |
| `5` | 坑之期末 | **`85`／`85`**·下一標籤 `bh`；`ALPHA_LABELS` **一字未動** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；併報判定組之個數（判定組為空 ⇒ 該器恆綠 ⇒ 須具名回報） |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必非零] `≥ 1`）；`baselines` `298` 檔·`a898f4e1…`；`p3a` **非祖先**；`WV_BAKE` **未設**；`K-9-29` 任何子項⛔ 落地；`GB-167`／`168`／`169` **⛔ 修** |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit`（⛔ 預寫） |

---

## `§七`　🛑 **⛔ 為之事**（逐項於報告自檢）

⛔ 動生產碼 `34` 檔一字；⛔ 改任何 `*_EXPECT`；⛔ 修 `GB-167`／`168`／`169`；⛔ 解除、收窄或結清 `GB-168`／`GB-169`；
⛔ 落地 `K-9-5-12`／`K-9-5-13`／`K-9-29` 之任何子項；⛔ 改 `ok_t`／`ok_f` 一字；`GB-162` **三禁令維持**；
**⛔ 引任何自 baseline 取得之數**；⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號、⛔ 動 `ALPHA_LABELS` 一字；⛔ 追改任何已入倉文件一字；
⛔ 判「替身線」之所指、⛔ 判碼面是否違反任何裁、⛔ 判任何側為違反、⛔ 判孰誤、⛔ 提修法主張；
⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘 → `§一`〜`§三` 逐工項（逐款以落檔**全量**出艙·⛔ 以 `tail -N` 取結果）→ 判別力二造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框與母體**；框無 `^` 錨者**二數並報**；凡以 `git` 輸出為框者**具名 `core.quotePath`** 或取 `-z`。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`provenance`／
`失效加註`／`K-9-5-13`／`GB-169`／`GB-168`／`range_area`／`_corner_buffer_S`／`_block_strip`／
`_oblique_s_max`／`end_pt`／`corner_pt`／`left_cum_S`／`ALPHA_LABELS`／`baselines`／`numstat`／
`deletions`／`sha256`／`merge-base`／`log -S`。

【單止】本單受詞 ＝ 下一批之 CC 窗。⛔ 呈 KL。

SELF_SHA256: 42a4074dbb4f45f2cfcd3c4e52f1d1e752d1b2fdbcfad07b8325b8b2d399a03f
