# W-G.9-270　補令四（`p7` 三項之裁 ／ `TIER_EXPECT` 之歸屬 ／ 三提案之核可）

> **受詞** ＝ `W-G.9-270` 之現施工窗。**主線** ＝ `3d3aa80d66ac1b8a5022ca24c199cd8028e39a86` 或其後裔（須 `git ls-remote` 實查）｜**凍存** `verify/W-G.9-269-p3a` ＝ `05a11bd`（未動）。
> 🔒 **本批仍為零生產碼**——`§三`／`§四` 之工項為**量測與歸因**，⛔ 動 `34` 檔一字。
> 🔒 本補令與補令一並存；相斥處以**時序最後者**為準。

---

## §零　復驗之核可（發單側自倉自跑·⛔ 轉引 CC 報告）

補令一全批之**主檢全綠**，核可：條款一／二／三辦畢（倉內 blob 內容 `sha256` ＝ `af2ba742d2f93a77576a25b5c4e7728851c13e0271007517cc8a895ec5eb1d4f` ＝ 工作區值·`CR ＝ 0`）；生產碼 `34` 檔期初＝期末；`verify/baselines` `298` 檔聚合 `a898f4e1…` 期初＝期末；四簿期末逐值相符；坑索引 `70`／`70`·器非紅。

🔒 **自此以倉內 blob 之全長 `sha256` 為補令一之唯一指涉錨**，前此八處之 `16` 碼截斷形⛔ 再用於新出艙。

⚠️ **一項須對齊框（⛔ 判孰誤）**：`R4` 前綴之占用，發單側之框（`git ls-files` @ `HEAD`）得 **`8`** 件（`3` 探針 ＋ `5` log），你報 `10` 件。**凡出艙件數須同格具名其框**（追蹤／工作區）。二框下結論同 ⇒ 不影響 `§二-1` 之核可。

---

## §一　三提案之核可

### 一-1　產物命名取 `R5` ⇒ **准**

`R4` 前綴確已被前窗占用（二框下皆然）；取甲（報告名與落檔前綴**同一符號**）合 `常規三`。代價（報告序跳 `R4`）已具名，**⛔ 再議**。爾後本波之落檔前綴一律 `WG9270R5_`。

### 一-2　候選新坑三則 ⇒ **准鑄**（標籤 `as`／`at`／`au`）

三則皆已以**對照組**坐實（真 CRLF 檔之二造），非推斷 ⇒ 合鑄坑之要件。逐則須載 **形／徵候／攔法**，落於交接註之坑表（`D-T` 形）：

| 標籤 | 形（受詞須為**量測器**·⛔ 為某次之結果） |
|---|---|
| `as` | `cat -A \| grep '\^M'` 於 **UTF-8 CJK 文本**恆有**偽陽**：位元組 `0x8D` 之轉義形為 `M-^M`，其尾二字元即 `^M` ⇒ 命中數 ≈ 含該位元組之列數，與 CR 無關 |
| `at` | `grep -c $'\r'` 對**真 CRLF 檔**得 `0`（**偽陰**）⇒ ⛔ 得用於 CR 之量測 |
| `au` | Python `io.open(p, 'w')` 於 Windows 預設 `newline=None` ⇒ `\n` **靜默**寫成 `\r\n`；寫回既有 LF 檔即全檔換行改寫，而**無任何出艙** |

🔒 **攔法欄一律為**：以**位元組層**直讀（`od -An -tx1` 之 `0d` 計數 ⋀ Python `rb.count(b'\r')`）二法並取，且**須對真 CRLF 造甲得非零**。

### 一-3　自解 `3`（索引器硬編母體之擴充）⇒ **准**，惟**附一項更正令**

擴充本身合 `append-only`——`ALPHA_LABELS` 僅**新增** `al`〜`ar`，⛔ 動既有任一項（發單側逐字驗 `e2d4df9` 之 diff）。
🔴 **惟其出艙之框逐字未同步**：`probe_WG9269_pit_index.py:278`／`:280` 仍印「字母系 `a`〜`z` ＋ `aa`〜`ak`」，而實作已達 **`ar`**。⇒ 迄今每一次坑索引之出艙，**其所具名之框皆與其實作之框相異**（違 `§七-2`：框須逐字為真）。

**令**：於本批擴充 `ALPHA_LABELS` 至 `au` 時，**同一次**改正該二處字串為其實際射程，並於報告中載「此前之出艙框字串陳舊」之照實一句。⛔ 追改既有報告。

---

## §二　`p7` 三項之裁

### 二-1　「是否另鑄」⇒ **⛔ 鑄**

`Δ_depth +0.035`（`R4` 深度 `33.10 → 33.11`）之**成因未歸因**。鑄一個受詞未定之號，即重蹈 `GB-162`（「成因未歸因」而先鑄）之覆轍。
⇒ **改令 `p8`**（見 `§四-1`）。歸因辦畢後，發單側再定其是否鑄、受詞為何。

### 二-2　`GB-7` 之前提已不成立 ⇒ **末端追加·⛔ 追改·⛔ 解除**

你之判為真且已以二形之差 `−0.005` 坐實。**令**：於 `GB-7` 之登記**末端追加**一則，載：`(a)` 其「本案零後果」之前提（「min 落 `R4` 且該塊兩形同值」）**已被真資料走過**、現不成立；`(b)` 本批之實測值與**其角色**；`(c)` 追加之時態錨。
🛑 **⛔ 改 `GB-7` 之受詞一字、⛔ 解除、⛔ 重鑄**——其受詞（`round` 位置）本身仍正確，變的是其**適用之事實**。

### 二-3　`MinA_區` 四值之擇一 ⇒ **暫⛔ 呈 KL**（發單側之裁·附由）

發單側自倉查得三事，使此題**現階段⛔ 具呈 KL 之要件**：

1. **`114.07` 係已知之陳舊斷言錨**，⛔ 未決之候選——`verify/wd4_tier_list.py:101-108` 逐字：「`MINA_QU_EXPECT` 之全等斷言**現為紅**，係**已歸因、待波末重烤消化之登記紅**」。
2. **`115.85` 係已登記之遷移目標**——`verify/fixture_block_depth_n19p.py:317` 逐字載遷移項為「`wd4_tier_list.MINA_QU_EXPECT` `114.07→115.85` ＋ `HALF_EXPECT` `57.04→57.93`」。
3. **現態下四值之擇一⛔ 改本案任一戶之梯次**（你之實測：⛔ 任一戶之 `2ΣG_戶` 落於 `[114.07, 115.88]`·**限本案**·⛔ 外推）。

⇒ 真正未決者**僅一句**：遷移之目標應為 `115.85`（已登記）抑或 `115.885`（正典形@活體）——而其**取決於 `§四-1` 之歸因**。歸因未成即呈 KL，係**半成品上呈**。
🔒 **`§四-1` 辦畢後，發單側一次呈 KL**；在其裁下之前，⛔ 改 `MINA_QU_EXPECT`／`HALF_EXPECT` 一字，⛔ 以四值中任一為唯一真值作推論。

---

## §三　`TIER_EXPECT` 之歸屬 ⇒ **未判**（⛔ 判為「既有紅」·⛔ 判為「新破損」）

你之照實併記（`W-G.4_泛用阻塞項登記表`）核可。**歸屬之裁如下**：

- **⛔ 得判為「既有紅／待重烤」**——現有之登記（`wd4_tier_list.py:101-108`、`run_verification.py:892`、`fixture_block_depth_n19p.py:317`）其受詞**逐字僅及於 `MINA_QU_EXPECT`／`HALF_EXPECT`**，⛔ 及於 `TIER_EXPECT`。以「同一函式內另有已歸因之紅」推得「本紅亦已歸因」＝ `自誤 353`（以相關述詞代 operative 述詞）。
- **⛔ 得判為「新破損」**——其起點未量。
- ⇒ **歸屬 ＝ 未判**，以 `§四-2` 之量測定之。

🔑 **方向之併記（發單側之碼面實測·供你設計量測，⛔ 為結論）**：梯次之判定式為
`公設軌 → "—"`／`all_qual → "1"`／`any(sumG >= mina[l]) → "0"`／`2*sumG >= mina_qu → "2"`／`else → "3"`（`verify/wd4_tier_list.py:268-279`）。
⇒ `0`／`1` 之界由 **`all_qual`（逐宗達標）** 所定，**⛔ 由 `mina_qu`**。而深度增（`mina[l]` 變大）應使 `all_qual` 變**難** ⇒ 梯 `1` 應**減**；實測卻是梯 `1` **增 `2`**（`7 → 9`）。**⇒ 其方向與深度鏈之變相反** ⇒ ⛔ 得逕歸因於深度。`:13-14` 另載「梯次判定吃 `ΣG_戶`（**價格相依**）」——此為另一候選源。**二者皆須以量測分辨，⛔ 擇一書之。**

---

## §四　新工項（**零生產碼**·量測與歸因）

### 四-1　`p8`：`R4` 深度 `33.10 → 33.11` 之歸因

1. **縮小候選集**（⛔ 全序列實跑）：以 `git log -S` ／ `git log -L` 於深度鏈之來源（含 `app.py` 之 `'D_avg': round(D_avg, 2)` 一線）取候選 commit 集，並**具名其框**（所掃之路徑與 rev 範圍）。
2. **於候選上實跑**取 `D_avg@R4` 與 `mina_qu`，二分定位其**首次**為 `33.11` 之 commit。
3. 🔒 **判別力對照（必辦）**：於 `K-8 段三 commit A`（`32.59 → 33.10` 之變）上實跑須得 **`33.10`**；⛔ 得則**判量測器紅**並停機，⛔ 續二分。
4. **出艙**：該 commit 之 `sha`／其訊息逐字／其所改之受詞／`D_avg@R4` 之前後二值（**未捨入**）／`mina_qu` 之前後二值。
5. **⛔ 為之事**：⛔ 主張其為缺陷、⛔ 提任何處置、⛔ 改任何常數。

### 四-2　`p9`：`TIER_EXPECT` 之翻紅點

1. 受詞 ＝ `compute()` 之 `d["tiers"]`（二情境並取）。同 `四-1` 之法二分。
2. 🔒 **判別力對照（必辦·此對照即本題之答之半）**：於 `TIER_EXPECT` **最後一次更新之 commit**（其註解載 `2026-07-14`·須 `git log -S` 現查其 sha）上實跑 `d["tiers"]`：
   - **得 `{"—":8,"0":13,"1":7,"2":3,"3":2}`** ⇒ 該錨曾為真 ⇒ 其後有一次**漂移**，二分定位之。
   - **⛔ 得** ⇒ 該錨**自登記之日即為紅**（＝ 登記時未實測）⇒ 歸屬另判，**停機回報**、⛔ 續二分。
3. **併判同源否**：`四-1` 所得之 commit 與本項所得之 commit **是否同一**；同源與否須以 sha 逐位對，⛔ 以「時間相近」推之。
4. **⛔ 為之事**：⛔ 改 `TIER_EXPECT` 一字、⛔ 判其為缺陷、⛔ 提遷移主張。

### 四-3　🔴 `p10`：**登記紅之遮蔽**（發單側之新診·須先查其是否已有既有號）

**所診**：`verify/wd4_tier_list.py:449` 逐字 `allok = allok and ok_m and ok_f and ok_t and ok_p and ok_m2`，而 `ok_m`（`MINA_QU_EXPECT`）係**已登記之恆紅** ⇒ `allok` **恆 `False`**、`RESULT: W-D.4 FAIL` **恆印** ⇒ **同一聚合內另四錨之任一翻紅，其出艙與現況<u>逐字相同</u>** ⇒ **⛔ 可觀測**。本批之 `ok_t` 翻紅即其第一個受害者：其係由 `p4` 之附帶實跑撞見，**⛔ 由該閘報出**。

1. **先查既有號**：全倉查是否已有同受詞之 `GB`（「已歸因之恆紅遮蔽同一聚合內之新紅」）。**須備必命中之對照組**並出艙其結果；對照組全零 ⇒ 先判量測器紅。
2. **查無 ⇒ 授權鑄 `GB-166`**（受詞見下）；**查有 ⇒ 🛑 停機回報，⛔ 鑄、⛔ 併入既有號**。
3. **`GB-166` 之受詞（逐字·若鑄）**
   > 一個**已歸因、待消化之登記紅**若與其他錨共用單一聚合出艙碼（`allok`／`RESULT`），則該聚合對其餘各錨**永久失明**——其餘任一錨翻紅時，出艙逐字不變。⇒ 凡有登記紅之聚合，須**逐錨分列其出艙**，⛔ 以單一聚合碼代之。
   > **解除條件**：`(1)` 逐錨分列之出艙入生產碼並經 KL 放行；`(2)` 判別力二造（人造一錨翻紅須逐錨可見 ⋀ 全綠須全綠）；`(3)` 現態五錨之逐錨態出艙。
   > 🛑 **⛔ 以「消掉登記紅」為解除之法**——消之即動 `MINA_QU_EXPECT`，該遷移須經 KL。
4. 🛑 **本批⛔ 落地**（其須動 `verify/wd4_tier_list.py` ＝ 生產碼）⇒ **他批 ＋ KL 放行**。

### 四-4　但書式與規格之不同步 ⇒ **以 KL 二裁為準·末端追加**

你所報之不同步為真，惟**⛔ 為缺陷**：KL `2026-09-12` 裁二（補令一 `§一-2`）逐字為「但書式欄**⛔ 刪⛔ 停算**，惟**⛔ 為出艙之補償額**」⇒ **實算而不出艙即為現行之正解**；陳舊者係 `docs/W-D.4_合併分配規格.md:78` 之「僅標式不算值」。
**令**：於該規格檔**末端追加**一則更正註（⛔ 追改 `:78` 原文），載其已為 KL 二裁所取代、並具名補令一 `§一-2` 與本補令。
🔒 **`自誤 368` 之本文⛔ 追改**——其形（未查宣告射程）不因此變；若須註其所引之線索有陳舊處，一律**末端追加**。

---

## §五　鑄號之授權（**⛔ 逾此**）

| 號 | 條件 |
|---|---|
| 坑 `as`／`at`／`au` | 無條件（`§一-2`） |
| **`GB-166`** | **條件式**——僅於 `§四-3` 款 `1` 查無既有同受詞號時；查有即⛔ 鑄並停機 |

🛑 其餘一切號（`自誤`／`VR`／`K-9`）**本批⛔ 鑄**。若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。
🛑 取號一律**二形並取·定義列式求 MAX**（`常規四（八）`）；哨兵字面⛔ 出艙（`GB-147`）。

---

## §六　續辦

| 序 | 事 |
|---|---|
| `1` | 工項零（本補令原封入倉）＋ 鑄坑 `as`／`at`／`au` ＋ `ALPHA_LABELS` 擴充至 `au` **並同時改正其框字串**（`§一-3`） |
| `2` | `§二-2`（`GB-7` 末端追加）＋ `§四-4`（規格末端追加） |
| `3` | **`p8`**（`§四-1`）：深度 `33.10 → 33.11` 之歸因 |
| `4` | **`p9`**（`§四-2`）：`TIER_EXPECT` 之翻紅點 ＋ 同源判 |
| `5` | **`p10`**（`§四-3`）：既有號之查 ⇒ 鑄 `GB-166` 或停機 |
| `6` | 報告（`W-G.9-270R5` 系·含 `p8`／`p9`／`p10` 之結果與四簿期末）⇒ **停**，候發單側 |

---

## §七　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字——含 `MINA_QU_EXPECT`／`HALF_EXPECT`／`TIER_EXPECT`／`wd4_tier_list.py` 之任一行。
2. ⛔ 落地 `GB-164`（不可判帶）、⛔ 落地 `GB-166`。
3. `GB-162` **三禁令維持**：⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數**。
4. ⛔ 判 `p6` 之交錯為缺陷、⛔ 作任何處置主張（其呈 KL 之材料由發單側另備）。
5. ⛔ 呈 KL 任何事——本批全在射程內。
6. ⛔ 以「現態無一戶落帶」為由弱化 `GB-164`（補令一 `§三-2` 明令）。

*純技術項一律自行辦到推完；`p8`／`p9` 之判別力對照若紅，即停機回報，⛔ 續二分。*
