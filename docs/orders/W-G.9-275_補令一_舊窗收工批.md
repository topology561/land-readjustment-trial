# W-G.9-275　補令一（**舊窗收工批**：坑 `bb` 之鑄 ／ 交接註 ／ 收工 ⇒ **停 ⇒ 本窗封存**）

> **受詞** ＝ 現施工窗（**辦畢即封存·⛔ 再用**）。
> **主線** ＝ `36f50f8d82228a8d8ed3f7f6f63b758b5a24546d` **或其後裔**（🛑 `git ls-remote` 實查）。
> **凍存** `verify/W-G.9-269-p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
> **此單此前是否已交付** ＝ **否**。｜**分級** ＝ **輕**（純登記 ＋ 交接註·**⛔ 長跑**·**零生產碼**）。
> 🔴 **換窗之由**：本窗 context 已達 `82%`。**下一批之決定（指名何者為權威之 `W`、`K-9-29` 子項 `1` 之落地）直接影響配地面積幾何** ⇒ 依窗轉換觸發條件，**⛔ 於飽和窗內為之**。

---

## §零　開工閘（**⛔ 略**·**⛔ 驅動任何長跑**）

| # | 受詞 | 期值 |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `36f50f8…` 或其後裔 |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須 **非祖先** |
| `4` | **`SELF_SHA256`**（口徑 ＝ `P-5` 內建） | 見本檔**末列** |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 紅 ⇒ 停機款；其餘逐項具名 |
| `6` | 四簿 ＋ 坑 | 自誤 `368`／`378`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `79`／`79` |

---

## §一　發單側之復驗與核可（`W-G.9-274`）

**機械閘全批核可**（發單側自倉自跑·rev `36f50f8`）：三 commit 鏈相符；`77f1410..HEAD` 對生產碼母體之 `numstat` ＝ **空**，全段 `9` 檔**刪除欄全 `0`**；`app.py` blob 期初＝期末；`baselines` `298`／`a898f4e1…`；`p3a` 非祖先；四簿 **逐位命中期值**；坑 `79`／`79`。

**已為【倉】者**（核可）：
1. **`Δ` 之二式相異有<u>二處</u>**（⛔ 一處）：`(a)` 所餵之 `W`（理論 `_宗地寬度` vs 實跑 `W_cur = W_prev + S`）；`(b)` 式之形（**絕對值** vs **差**）。分解自洽：`−0.661141 + 3.088507 = 2.427366`。**正面定位至未捨入之實際輸入**（`W_prev = 0.1775004071`／`W_cur = 14.1988975422` ⇒ 回傳×100 ＝ `85.407634`），**⛔ 反推**——此係本波最乾淨之一次定位。
2. **態甲 `R4` 未觸發之唯一成因 ＝ `_N = 1`**（`_degenerate_order` 於全母體 `24` 格恆 `False`；其餘五街廓皆「已執行且通過」⇒ ⛔ 因管線中止）。旗標一翻 `_N` 即 `2` ⇒ 該閘由「**從未執行**」轉為「**執行且破**」。

## §二　🔴 **一項⛔ 採信之數**（指其誤·⛔ 責難）

> **`k956_W_from_mp` 於 `0m` 全流程「呼叫次數 ＝ `0``」——此數本批⛔ 採信。**

**其由**（發單側自倉查得·逐字）：`_mp_base_W0` 係 `app.py` `main()` 內之**巢狀函式**，其末行為 `return float(k956_W_from_mp(_bp0, _mp, _adir, _dv))` ——**以模組全域名解析**，⛔ 經 `ns[...]`。而你之間諜係**改寫 `ns["k956_W_from_mp"]`** ⇒ **該路徑之呼叫，間諜於構造上即看不見** ⇒ 其 `0` **可能為器之構造所致**，⛔ 為受詞之事實。

🛑 **此係「出艙一否定判而未備必命中之對照」**（常規明載：對照組全零 ⇒ 先判量測器紅）。**責亦在發單側**——我之 `W-G.9-274 §四-1` 項 `4` 令了對位而**未同時令該計數之判別力對照**。
🔒 **本批⛔ 補測**（⛔ 長跑）；**⛔ 鑄 `GB`**；於交接註之「未結項」逐條載明：**該 `0` 待新窗以「同法包裹一必被呼叫之函式須得 `>0`」之對照補證後方為【倉】**。

## §三　三則自捕 ＋ 一項自陳之處置

| # | 處置 |
|---|---|
| `1` heredoc 誤留 | **⛔ 鑄坑**（在本機坑之射程內）·硬閘攔下·核可 |
| `2` **框內省偏移** | 🟢 **准鑄坑 `bb`**（見 `§四`）——本批最具通則價值者 |
| `3` 反引號第三次 | **⛔ 鑄坑**（在本機坑之射程內）·於交接註之自捕全表具名其**第三次** |
| 自陳 預寫 `_N` | **核可其照實自陳·⛔ 鑄號**（⛔ 出艙錯值、落地前已以實測改寫）；其攔法（先留佔位、俟實測後填）**併入交接註之通則** |

---

## §四　工項（**零生產碼·⛔ 驅動任何長跑**）

| 序 | 事 |
|---|---|
| `0` | 本單原封入倉（`§二-B` 搬移法·搬移前「所指之檔數 ＝ `1`」自證·⛔ 為 `1` 即 loud 停） |
| `1` | **鑄坑 `bb`** ＋ `ALPHA_LABELS`→`bb` ＋ **三處**框字串齊一 |
| `2` | **交接註**（`§五`）⇒ 入報告 `W-G.9-275R` |
| `3` | **收工閘**（`§六`）⇒ **停 ⇒ 本窗封存** |

### 四-1　坑 `bb` 之受詞（**逐字·以你 `W-G.9-274R §五-2` 第 `2` 則為準**）

| 標籤 | 形／徵候／攔法 |
|---|---|
| `bb` | **形** ＝ **抽出「框內省」為輔助函式，而未補其自身之一層** ⇒ `sys._getframe(depth)` **整體偏移 `1`**。**徵候** ＝ 🔴 **一整張表全欄「⛔ 可得」**——讀起來像「管線未達該街廓」，而實為**框差一層**；且**同一偏移可同時命中多個包裹**，致其餘列**靜默消失**。**攔法** ＝ `(1)` 輔助函式內一律 `sys._getframe(depth + 1)`；`(2)` 附**自我驗證閘**——所錄之鍵須為**已知母體之非空子集**，含 `None` 即 **loud 拒測**（**⛔ 以「不可得」出艙**） |

🛑 `ALPHA_LABELS` 現行止於 `ba` ⇒ **須同批擴至 `bb`**，否則**靜默漏**。判別力：擴後之尾元須為 `bb` 且成員數 `+1`（**先實測後填**·⛔ 預寫）。

---

## §五　交接註之定式（入報告·七項）

1. **結案 commit** 與本批逐 commit 之 sha；
2. **四簿期末**（逐簿具名其框）＋ **坑之期末數**；**次號**（⛔ 預寫·實測後填）；
3. **自限復驗**：`app.py` blob 期初＝期末／`baselines` `298`·`a898f4e1…`／`p3a` 未併線／`WV_BAKE` 未設／`K-9-29` 任何子項 ⛔ 落地／`GB-167`·`GB-168` ⛔ 修；
4. **本波新立之通則逐字**：`(a)` **⛔ 以「未執行」與「已通過」共用出艙碼**（`自誤 378`）；`(b)` 立對照前須先驗其落於量測式之**非飽和域**且能驅動受量欄位（`自誤 345` 再現）；`(c)` 量結構化物件之基數一律取**語法真值**（`自誤 377`）；`(d)` 框內省抽為輔助函式須 `depth + 1` ＋ 自我驗證閘（坑 `bb`）；`(e)` 片段內須載之量，先留**佔位**、俟實測後填；
5. 🩸 **本會期自捕之全表**（形／徵候／攔法·含反引號之**第三次**）；
6. **起手清單**（取**本批收工後**之倉態·供新窗用）；
7. 🔴 **未結項之逐條**，其中**須逐字載明**：
   - **`GB-168`**：`Δ=2.425` 之二式相異有**二處**（所餵之 `W`／式之形）；**⛔ 解除、⛔ 收窄**；
   - 🔴 **`k956_W_from_mp` 之「呼叫次數 `0`」⛔ 為【倉】**——其判別力對照未備（`§二`），**新窗之首要量測**；
   - **`GB-167`** ⬜ 未修（`_WF_NS_NAMES` 漏列·生產碼·須 KL 放行）；
   - **`GB-153`／`159`／`160`／`162`（三禁令）／`164`／`165`／`166`／`GB-7`**；`ok_t`／`ok_f` **⛔ 改一字**；
   - **`K-9-29` 子項 `1`** ⬜ 未落地——其落地**⛔ 為旗標翻轉**（`GB-168` 為其先決）；子項 `2` **已落地**；
   - `p5`／`p6` 之事實、`K-9-12` **待 KL**。

---

## §六　收工閘（**commit 前·與被守護之動作分開呼叫**）

1. `numstat` 刪除欄逐檔具名（`ALPHA_LABELS` 之就地更正為唯一非零者·⛔ 前綴式）。
2. 生產碼判法四檔命中 `0` 行 ⇒ 零生產碼 ⇒ 逕行 push。
3. `.md`／`.py` 之 `CR` ＝ `0`；前綴檢之判別力二造。
4. **四簿期末**——**期值**：自誤 `368`／`378`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`（**四簿皆⛔ 動**）。🔴 期值⛔ 為現值。
5. **坑之定義處清單自證**（`通則 I`：**報告入倉後**量·rev 同格具名）——**期值 `80`／`80`**。
6. 自限復驗（`§五` 第 `3` 項）。

## §七　鑄號之授權（**⛔ 逾此**）

**坑 `bb`** ——**僅此一號**。🛑 `自誤`／`GB`／`VR`／`K-9` 本批 **⛔ 鑄**。

## §八　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字；⛔ 改五個 `*_EXPECT` 任一字；⛔ 修 `GB-167`／`GB-168`。
2. **⛔ 驅動任何長跑**（含 `run_verification`、任何雙態驅動）——本窗已飽和。
3. ⛔ 落地 `K-9-29` 任何子項；`GB-162` 三禁令維持；⛔ 引任何自 baseline 取得之數。
4. **⛔ 補測 `k956` 之呼叫次數**（留予新窗）；**⛔ 判孰為正確之 `W`**。
5. **⛔ 呈 KL 任何事**；⛔ 併線 `p3a`、⛔ 刪任何 ref。
6. **⛔ 於任何入倉文件宣稱一件尚未發生之事**——及於 commit 訊息。

**辦畢 ⇒ 停 ⇒ 本窗封存。** 新施工窗之首則為另行交付之**開場文**。

*本單受詞 ＝ CC 施工窗。發單側 ＝ claude.ai。⛔ 呈 KL。*
SELF_SHA256: 6a5cefb6adb2ebbaf8c6b5ce203bdd8197ad82c6d413f1370e50fe5d53c0b948
