# 🧾 `W-G.9-210` 施工單　`自誤 263`／`264` ＋ `VR-090` ＋ **補款三** ＋ `commit 3` 開工授權

> **基座**：`wip/s1-endpart` ＝ **`6c8d8ea45e053047605b7a9aaec99a48421be22a`**（`W-G.9-198R commit 2` 已落地·純快轉）
> **性質**：**二段**。`§一` 🟢 零生產碼（**CC 逕推**）；`§三` 🔴 含生產碼（`commit 3`·**⛔ 推主線**·走側分支 `verify/W-G.9-198R-c3`）。
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔴 **錨已第二度換代**：`app.py` blob ＝ `6f0317e4…` → **`144349c834c2289434e1b0edbcfc8d0a27d88435`**。

---

## §零　開工閘（🔒 錨皆為發單側自倉現查·態 `6c8d8ea`·CC 須逐項重量）

| 閘 | 判準 |
|---|---|
| `S-0a` | 五檔 blob：`app.py` ＝ `144349c834c2289434e1b0edbcfc8d0a27d88435`／`verify/selection_pipeline.py` ＝ `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c`／`verify/run_verification.py` ＝ `d07955c58e67ec06260971a74b5aabe5e846f106`／`verify/stepg_pipeline.py` ＝ `fef5865a73033274697790e0e307aedbf6122422`／`verify/run_all.py` ＝ `e5cf9a224517533b183b34e4fd0f5fbe35ee406b` |
| `S-0b` | 母體 追蹤檔 **`1585`**／`.md` **`518`**；`app.py` `1406177` B／`LF` `23779`／`CR` `0` |
| `S-0c` | AST：`k6_step0_block_locked` `11567`-`11589`／`k6_step0_merge` `11592`-`11752`／`select_corner_lots_both_sides_v12` `12603`-`12972`／`_pk_one_side_v12` `12975`-`13085`／`k6b_stage2_global_order` `13088`-`13156`／`k6b_resolve_dual_crossing_sides` `13159`-`…`／`main` `14505`-`23777` |
| `S-0d` | 取號：自誤 相異 `227`／MAX `262` ⇒ 次 `263`；`GB` `55`／`141`；`VR` `74`／`89` ⇒ 次 `090`；戒 `23`／`40`；`K-9` `13`／`26` |
| `S-0e` | 本單 `SELF_SHA256` 自驗相符；`probe_order_preflight.py` 之 🟡 逐項以**字樣錨**具名並依裁 `H` 歸類 |

🔒 **行尾（`GB-139`）**：`clone` 用 `-c core.autocrlf=false -c core.quotepath=false`；`add` 既有檔一律常規 `add`；閘之受詞一律取**倉側** `git cat-file blob`。
🔒 **`S-0e` 之已知 🟡（發單側已對本檔實跑·母體 ＝ 本單單檔·範圍 ＝ `P-1`〜`P-6`）**：🔴 `0`【項】／🟡 **`4`**【項】。

| 檢 | 字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-1` | `` **族** ＝ 「**將驗證判準綁在代理上，而非綁在它所要保證的性質上**」 `` | **零參照 ⇒ 具名豁免**：係**自誤之族別歸類**，⛔ 量測斷言；其所引之 `自誤 240`／`248` 已具名 |
| `P-4` | `` | 閘 | 判準 | ``／`` | # | 補款內容 | `` | **判別力參照 ⇒ 具名豁免**：二者皆索引表頭，各列之母體／框／方向逐列具名於同表 |
| `P-4` | `` 1. 備妥後**⛔ 推 `wip/s1-endpart`**；推至側分支 `` | **零參照 ⇒ 具名豁免**：`推` 係 git ref 之推送，⛔ 幾何方向量、⛔ 涉座標系 |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造）**：同器對 `-203` ＝ `0`／`-205` ＝ `1`／`-206` ＝ `3`／`-207` ＝ `0`／`-208` ＝ `1`／`-209` ＝ `1`／`-198R` ＝ `5`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。

🔒 **本機分支之處置（`自誤 261`）**：開工前報 `git rev-parse HEAD` 與 `git symbolic-ref -q HEAD`；若本機 `wip/s1-endpart` 停在非 `6c8d8ea` 之處，先二重保全再退回，逐項復驗後方進 `§一`。

---

## §一　🟢 零生產碼批（**CC 逕推**）

🛑 三 payload 皆為 append-only 之**檔尾追加**，⛔ 就地改任何既有一字。
🛑 **節框之提醒**（承 `-208`／`-209` 之實測）：本單 `§一-1`〜`§一-3` 之 payload 首列**本身即 `##` 標題** ⇒ 抽取須以 `^### 一-\d` 為節框，⛔ 用裸 `^## `／`^### `。

### 一-1　鑄 `自誤 263`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

```markdown
## 自誤 `263`　判別力自證之**見證檔綁「檔名」而非綁「性質」**

`W-G.9-209 §一-3` 令：「取**未參與本 commit** 之一檔——**建議** `docs/reports/W-G.4_泛用阻塞項登記表.md`
——報其於 `0d88e1a5` 與 `0d88e1a5′` 之 blob，**二者應相異**。」
🔴 該建議檔於 `0d88e1a5`／`0d88e1a5′`／主線頭**三態 blob 全同**（`c09d01168a93393164dc5e1436923920c8beac12`）
⇒ 其對本次 rebase 之**判別力恰為 `0`**，且此係**構造性**而非偶然：
二基座之差（`c746d2dd`→`be1b326`）**恰 `1` 檔** ＝ `docs/reports/W-G.9-208R_commit2_…md`，該建議檔不在其中。

🩸 **缺陷之形**：單所寫之判準（「未參與本 commit」）**⛔ 蘊含**它要保證的性質（「能證基座已換」）。
能證基座已換者，須是**於二基座之間確有差異**之檔；「未參與本 commit」只排除了一半。
**正確之一般式**
> **判別力自證之見證受詞，須取「⛔ 參與本 commit」∧「<u>參與基座之前進</u>」之交集**
> ——即 `git diff --name-only <舊基座> <新基座>` 命中它，而 `git show --name-only <本 commit>` ⛔ 命中它。
> 🛑 **⛔ 得以檔名指定之；須以上開二條件之<u>機械測試</u>選出之。**

**族** ＝ 「**將驗證判準綁在代理上，而非綁在它所要保證的性質上**」——與 `自誤 240`／`248` 同族。
**攔截者** ＝ CC（依 `常規五` 先證器紅、再改取合判準之見證檔〔`W-G.9-208R` 報告：舊態不存在、新態 ＝ `526247aad009ed423c51daf842e09497bdbe3027` 且恰 ＝ 主線頭值〕、另補三獨立法 ⇒ **正辦**）。
**土地後果** ＝ 無。

## 自誤 `264`　🩸 **`自誤 258` 之累犯**：閘⛔ 自扣其自身 payload 之貢獻

`W-G.9-209 T-6` 逐字令：「`GB-140` 前文之逐字『**現況之土地後果 ＝ 無**』於落地後之 `HEAD` **仍命中 `1`**」。
🔴 落地後實得 **`2`** ——因同批之 `GB-140` 更正註 payload **自身即引用該句**（其逐字：
「`GB-140` 前文逐字載「**現況之土地後果 ＝ 無**」——該句**未標時態**……」）。

🩸 **本則之嚴重性在於它是重犯**：`自誤 258` 之一般式逐字為
「**該閘之期望值須先減去本批 payload 自身之貢獻**」，鑄於 `W-G.9-206`；
而 `W-G.9-209` 係其後**第三張**施工單，`T-6` 即再度違反之。
⇒ 鑄戒⛔ 等於內化；**戒須於起草每一個「數某字樣」之閘時被主動援引**，⛔ 待事後回想。

**閘之正確形式（本則之正解）**
> 凡閘之受詞為「某既有字樣於落地後之命中數」者，其期望值須寫為
> **「⛔ 得為 `0`（證係 append 而非就地改）；總命中 ＝ 落地前之數 ＋ 本批 payload 內之數」**，
> ⛔ 寫死一個常數。

**攔截者** ＝ CC（依 `VR-089` 肢一之一般式「閘之字面與其立法理由互斥者以立法理由為準」判綠 ⇒ **正辦**）。
**土地後果** ＝ 無。
```

### 一-2　鑄 `VR-090`（追加於 `docs/驗證裁定登記表.md` **檔尾**）

```markdown
## `VR-090`　`W-G.9-209` 二閘之更正（claude.ai 裁 `2026-09-02`·`W-G.9-210`）

**肢一　判別力自證之見證受詞 ＝ 機械測試選出，⛔ 檔名指定。**
自本裁起，凡施工單令「判別力自證」者，其見證受詞之**選法**須逐字寫為：
> 取 `S ＝ {f : git diff --name-only <舊基座> <新基座> 命中 f} ∖ {f : git show --name-only <本 commit> 命中 f}`；
> **`S` 為空 ⇒ 該自證⛔ 適用，須改採他法並具名**；`S` 非空 ⇒ 任取其一為見證受詞。
🛑 施工單**⛔ 得以檔名「建議」之**——建議即誘導，`W-G.9-209` 已實證其誘導出判別力為 `0` 之選擇。
（缺陷已鑄 `自誤 263`。）

**肢二　`W-G.9-209 T-6` ＝ 綠（立法理由勝其字面）。**
該閘字面令「仍命中 `1`」，實得 `2`。其立法理由逐字為「**證係 append 而非就地改**」
——受詞為 `2`（＝ 落地前 `1` ＋ 本批 payload 內 `1`）**恰恰滿足**該理由（若係就地改則落地前之 `1` 會消失）。
⇒ 依 `VR-089` 肢一之一般式，**以立法理由為準 ⇒ 判綠**。
自本裁起，此類閘之期望值一律寫為「**⛔ 得為 `0`；總命中 ＝ 落地前 ＋ payload 內**」，⛔ 寫死常數。
（缺陷已鑄 `自誤 264`·係 `自誤 258` 之累犯。）
```

### 一-3　`§一` 之驗收

| 閘 | 判準 |
|---|---|
| `T-1` | 二追加檔之 append-only 位元組級自證（受詞取**倉側** blob）；依 `戒 40`，`Δ` 與 payload **二端**位元組數同格並報 |
| `T-2` | 自誤 相異 `227`→**`229`**、MAX `262`→**`264`**（🛑 本批鑄**二**則）；`VR` 相異 `74`→**`75`**、MAX `89`→**`90`** |
| `T-3` | `GB` **⛔ 得變**：相異仍 `55`、MAX 仍 `141`（本批⛔ 鑄 `GB`） |
| `T-4` | 追蹤檔 `1585`→**`1587`**；`.md` `518`→**`520`**（＝ 本單 ＋ 執行報告） |
| `T-5` | 🔴 生產碼四檔 ＋ `selection_pipeline.py` 五檔 blob **⛔ 動**（逐檔 ＝ `S-0a`） |
| `T-6` | 🔴 **依 `VR-090` 肢二之新形（本閘即該形之首次適用）**：字樣錨 `` **現況之土地後果 ＝ 無** ``（母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔）於落地後 **⛔ 得為 `0`**（為 `0` ⇒ 係就地改 ⇒ 紅），且**總命中 ＝ 落地前之數 ＋ 本批 payload 內之數**。🛑 **二加數皆由 CC <u>自行復算</u>**（前者 `git cat-file blob <基座>:<檔>`；後者數 `一-1` fence 內之出現次數），⛔ 照抄。🔒 發單側實測供**事後對照**（⛔ 作判準）＝ `2` ＋ `2` ＝ `4`——🩸 起草時初稿誤寫「payload 內 `1` 處 ⇒ 期望 `3`」，實測 `自誤 264` 之 payload 內該字樣出現**二**次（`T-6` 之引文 ＋ 更正註之引文），出單前自捕更正 |

✅ 全綠 ⇒ **逕推**。🛑 `§一` 之 commit ⛔ 得與 `§三` 合併。

---

## §二　🔴 `W-G.9-198R` **補款三**（承 `-206` 補款一／`-208` 補款二·⛔ 就地改 `-198R` 一字）

| # | 補款內容 |
|---|---|
| `補三-1` | 🔴 **`-198R §一 S-0a①` 之錨第二度換代**：`app.py` blob 判準值 → **`144349c834c2289434e1b0edbcfc8d0a27d88435`**。 |
| `補三-2` | 🔴 **`S-0a②` 之區間換代**：基座 → **`a1107a17825c37430f69349afaceef4d09c6f5f8`**（`commit 2` 之物件）；判準 ＝ `git diff --name-only a1107a17..HEAD` 逐列**⛔ 含 `CLAUDE.md 🔧 二` 所列之四檔**。 |
| `補三-3` | 🔴 **`S-0d` 改前基線<u>第三度</u>重做**：受詞改為**態 `6c8d8ea`**（＝ `commit 2` 落地後），檔名帶 `6c8d8ea` 短碼。🛑 ⛔ 沿用 `6a3f5fe`／`b772f8a` 之凍存——`D3-a` 逐字令「以 `commit 2` 落地後之態為基準」。 |
| `補三-4` | **`commit 3` 之落地點**：准 `app.py`；若 `R3-4` 之上鎖出艙須與 app 同構，准 `verify/selection_pipeline.py`；若 `U-K2` 因新欄觸發，其明文處置（`_DECLARED_EXTRAS` 具名申報）同准，**須逐欄具名**並適用 `-206 §六 三`（波末重產時一併移除）。🛑 動此三檔以外者 ⇒ **停機上呈**。 |
| `補三-5` | 🆕 **`R3-7`**：`commit 3` **⛔ 得順手收斂 `GB-140`／`GB-141`**（二者須先量測後裁·須上呈 KL），亦⛔ 更動 `k6b_resolve_dual_crossing_sides` 內任何 `round` 之位數、⛔ 更動 `_dual` 之次序。 |
| `補三-6` | 🆕 **`R3-8`**：`commit 3` ⛔ 得改動 `抵費地0m(應空)` 之凍存期望（依 `-209 §四-2`：登記為已知紅、繫 `GB-111`）。 |

---

## §三　🔴 `commit 3`（段一上鎖）之開工授權

🔒 **結果態與驗收⛔ 重寫**——逐字沿用 `W-G.9-198R §五`（`R3-1`〜`R3-6`／`D3-a`〜`D3-e`），連同 `§二` 之 `補三-1`〜`補三-6`。CC 須自倉重抽 `-198R`（自簽 `b291070e…`）與正典 `K-9-24 四`／`K-6 §二 段一`。

🔑 **本 commit 之性質預期 ＝ 唯讀·零土地後果。** 上鎖之受詞係**段三之合併群操作**（`K-9-24 四`），而段三尚未落地（`VR-086`）⇒ ⛔ 有消費端。
🔴 **`D3-a` 若見土地欄有一格變 ⇒ 停機上呈**——那代表上鎖被接到了它不該作用的地方（最可能之誤接 ＝ 凍住步驟 0 之同街廓同歸戶合併，`R3-3` 明令⛔ 得如此）。
🛑 **`D3-b`**：上鎖集合之基數 ⛔ 為 `0`（未咬到）亦⛔ 為全部候選（判別力零）。
🛑 **`D3-c`**：上鎖之值於 `app.py`／`verify/**.py` 之全部出現處須逐處具名其係「產出」抑「消費」；**⛔ 得有任一消費處位於 `k6_step0_merge` 及其被呼叫鏈上**。

### §三-1　commit 之處置

1. 備妥後**⛔ 推 `wip/s1-endpart`**；推至側分支 **`verify/W-G.9-198R-c3`**，回報其全 `40` 碼並復驗主線⛔ 前進（🛑 期望值取**當時實查**之主線頭，⛔ 寫死常數——承 `-208` 之量測器紅）。
2. 🔴 **停機**，逐字回報 `D3-a`〜`D3-e` ＋ `numstat` ＋ 新 blob 全 `40` 碼 ＋ **新 AST 區間**（⛔ 沿用 `S-0c`）。
3. 🔒 **判別力自證之見證受詞依 `VR-090` 肢一之機械測試選出**，⛔ 由本單以檔名建議。
4. 發單側自側分支逐位元組復驗 → 請示 KL → 放行後依等值四判準推進主線。

### §三-2　報告

`docs/reports/W-G.9-210R_…md`：`§一` 之 `T-1`〜`T-6` 可與 `§一` 同批、不同 commit、逕推；`commit 3` 之 `D3-*` 由 CC 自定置放，惟**⛔ 得與 `commit 3` 綁同一 commit**、亦**⛔ 得置於其後**。

---

## §四　停機條件與其後

1. `S-0a` 五檔任一 blob 不符 ⇒ **停機上呈**（⛔ 自行更新錨）。
2. `補三-3` 之改前基線若沿用舊態 ⇒ 判紅，重做。
3. `D3-a` 土地欄有變 ／ `D3-b` 基數為 `0` 或全集 ／ `D3-c` 發現步驟 0 路徑上有消費處 ⇒ 皆**停機上呈**。
4. 🔒 **`commit 3` 放行後之下一件** ＝ **`GB-140` 處三 ＋ `GB-141` 同批上呈 KL**（泛化波前置）。本單⛔ 辦。
5. 🔒 **次號（本批落地後）**：自誤 `265`／`GB 142`／`VR 091`／戒 `41`／`K-9 27`。
6. 🔒 **攢批**：`GB` 定義列式之正典化（`GB-138` 處置③）。
SELF_SHA256: c5103a2ff77492f7b9b4d60fe0ad4a981c79a5227721bd66ecf282496014f272
