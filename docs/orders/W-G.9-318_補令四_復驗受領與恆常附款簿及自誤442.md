# `W-G.9-318`　**補令四**：發單側復驗之受領／恆常附款簿之立／待放行清單之更正（`自誤 442`）／恆常附款 `x`

> **態**｜**發單側自倉獨立實查**（⛔ 採信 CC 之報）：
> · 主線 `wip/s1-endpart` ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（**一字未動**）
> · 遠端分支 **`25`** 支；`refs/heads/verify/*` ＝ **`20`**
> · `wip/W-G.9-318-preserve` ＝ **`bc97c9d72d1680e820872aba787ecc453fa3a509`**
> · `4037570..preserve` ＝ **`26`** 筆；`git merge-base --is-ancestor 4037570 bc97c9d` ⇒ `rc` ＝ `0`（**主線之快轉為 fast-forward**）
> **分級** ＝ **中**（零生產碼·⛔ 改生產碼一字·⛔ 驅動全量）。
> 🔑 **貼<u>新窗</u>**，與 `docs/reports/W-G.9-318_CC側換手_一.md` 併呈。
> 🛑 本補令於相衝之點取代原單與補令一〜三；五者皆留倉、⛔ 改前四者一字。
> 🔑 **新窗之唯一入口 ＝ `wip/W-G.9-318-preserve` @ `bc97c9d72d1680e820872aba787ecc453fa3a509`**——⛔ 假定新環境已有本波之任何產物。

---

## `§零`　受領・裁

### `§零-1`　受領（**⛔ 鑄號**）

1. **`W-G.9-318R4` 與保全之成就**：發單側逐項復驗，`§一` 出艙五項**全數相符**（見卷首態）。
2. **`push` 係重試後獲核可、⛔ 繞過分類器、⛔ 改命令／分支名／力度、⛔ 另闢傳輸途徑** ⇒ **判斷完全正確**。
3. **CC 自捕第 `10` 則受領**，且其**通則**價值高於該次之攔：「`append-only` 判準若寫成『前綴 ⋀ 嚴格增長』，會把『未改』與『前綴被破』混成同一色」。⇒ 立為恆常附款 `x`（裁 `4`）。
4. **CC ⛔ 憑記憶補寫恆常附款 `a`〜`i`，逐款標【⛔ 可得】** ⇒ **判斷完全正確**；其【⛔ 可得】之**框**經發單側以他框重掃後部分收窄（裁 `3`），⛔ 為 CC 之誤。

### `§零-2`　裁（**`6` 項**）

**裁 `1`　🟢 生產碼之技術復驗<u>通過</u>（發單側自倉·⛔ 採信 CC）**

| 受詞 | 發單側實得（產生方式逐字） | 判 |
|---|---|---|
| 生產碼相異 | `4037570` vs `preserve`·正面列舉 `34` 檔（`app.py` ＋ `verify/` 頂層 `*.py` `33`）⇒ 相異 **`2`**（`app.py`／`verify/stepg_pipeline.py`）·新增 `0` | ✅ |
| `c3` 純位移 | `git diff -U0 7f43924^ 7f43924` ⇒ 刪 **`36`**／增 **`36`**；**去首導空白後二序列逐列相同**（`True`）；縮排差之相異值集 ＝ **`{4}`**（單一值·全列一致） | ✅ **逐列坐實** |
| `c4` 純位移 | 同法 ⇒ 刪 **`30`**／增 **`30`**；序列相同 `True`；縮排差集 ＝ **`{4}`** | ✅ |
| `c3`／`c4` 之 AST | 全檔 AST 節點多重集：僅在改前／改後者，**各只有巢狀容器節點**（`Module`→`FunctionDef main`→`If`→…→`For`·`app.py` `15`／`stepg` `4`，恰為其巢深），**⛔ 任何葉節點相異** ⇒ 位移之形 | ✅ |
| 上移到位 | `app.py`：`_b_L0` 賦值 `:22443` **早於** `_advance_block_with_split` 之首次呼叫 `:22450`（三呼叫 `22450`／`22453`／`22476`） | ✅ |
| `c5` | `solve_G_binary` 具 `w0_start` 參數（`:10093`·預設 `None`）；覆寫段 `:10214`–`:10221` 含**二道 loud raise**（不可解析／非有限值） | ✅ |
| `c6` | `_solve_G_one` 具 `w0_start`（`:13985`）·薄殼直通 `:14029`；`iterate_G_S` 之 loud 閘 `:14036`；`_corner_first_lot_G`（`:14054`）**⛔ 傳** | ✅ |
| `c13`（`c7′`） | `_w0_start` 關鍵字實參 **`6`** 處（二檔各 `3`）：`app.py:22193`／`22308`／`22325`·`stepg:823`／`925`／`941`；其 `test` 逐處 ＝ `0.0 if not _fo_left else None`（**各 `1`**）／`0.0 if not _fo_right else None`（**各 `2`**）⇒ **左右⛔ 交叉** | ✅ |
| `_b_*0` ⛔ 及實跑側 | `_b_L0`／`_b_R0` 之 `Load` 節點 **二檔各 `2`**（`app.py:22461`／`22463`·`stepg:1058`／`1060`），**⛔ 出現於任何 `_solve_one` 呼叫端** | ✅ |
| 四簿 @ preserve（本表之 `→` 一律為「期初→期末」或「甲→乙」之**時序／版本方向**·⛔ 幾何方向·⛔ 涉座標系） | 自擬框：自誤 **`427`**／**`441`**（缺號 `[106, 355, 356, 357, 434, 435, 436, 437]`）｜`GB` **`169`**／**`171`**（缺號 `[12, 87]`）｜`VR` `80`／`95`｜`K-9` `40`／`41`。🔒 **二框之差恰 ＝ `4`**（`434`–`437`）⇒ 正典框 `427 + 4` ＝ **`431`**，與 CC 之報及補令三更正後期值**逐位相符** | ✅ |
| `append-only` | `4037570` → `preserve`：自誤簿嚴格前綴 `True`·`+15211` B（`919874`→`935085`）；`GB` 簿 `True`·`+15766` B（`928354`→`944120`）；`K-6` 典 `True`·**`+0`** B | ✅ |

⇒ 🟢 **五筆（正：<u>六筆</u>·見裁 `2`）生產碼 `commit` 之技術面，發單側復驗通過**；其土地後果已由 KL `2026-09-20`「問2：放行」核可。**餘者為逐筆放行之程序**。

**裁 `2`　🔴 待放行清單之漏——動生產碼者為 <u>`6`</u> 筆，⛔ `5` 筆 ⇒ 鑄 `自誤 442`**
發單側自倉逐筆實查（框 ＝ `git show --numstat --format= <h> -- app.py verify/stepg_pipeline.py`·母體 ＝ `4037570..preserve` 之 `26` 筆）：

| # | 雜湊 | 改生產碼 | 性質 |
|---|---|---|---|
| `1` | `7f43924`（`c3`） | `1` 檔 | `app.py` 上移 |
| `2` | `7c346b1`（`c4`） | `1` 檔 | `stepg` 上移 |
| `3` | `7c07a36`（`c5`） | `1` 檔 | `w0_start` 之增（預設 `None`） |
| `4` | `6cc51e6`（`c6`） | `2` 檔 | 薄殼直通 |
| `5` | **`b1141a3`（`c7`）** | **`2` 檔** | 🔴 **原清單漏列**——被 `c13` 完全取代之中間態 |
| `6` | `8e056b4`（`c13`） | `2` 檔 | `c7′` forced 側之避讓 |

🔒 **淨效已驗**：`4037570` → `preserve` 之淨樹，六呼叫端逐處 ＝ `0.0 if not _fo_* else None` ⇒ **`c7` 之效於淨樹中已全數為 `c13` 所取代**。
🛑 **仍須逐筆列呈**：依補令二裁 `2`（歷史留痕·⛔ rebase），`c7` 將隨快轉進入主線歷史 ⇒ **KL 所放行者實為 `6` 筆**，其中 `b1141a3` 係**中間態**。⛔ 以「反正被取代了」略之。
**責在發單側**：清單係依「何者為現行」而列，⛔ 依「何者動生產碼且將落入主線」而列；發單側於補令二裁 `6`、補令三附錄`甲`**二度沿用而未自倉重導** ⇒ `自誤 442`（與 `自誤 441` 同族：**沿用既有之數／表而未對本批自算**）。

**裁 `3`　恆常附款之可得性——CC 之【⛔ 可得】部分收窄（⛔ CC 之誤）**
發單側以**他框**重掃（正規式 `立為恆常附款[^\n]{0,20}?\`([a-z])\`` ⋁ `恆常附款 \`([a-z])\` [：＝]`·母體 ＝ `preserve` 之追蹤 `.md` **`857`** 檔）：

| 款 | 定義處 | 引用 | 判 |
|---|---|---|---|
| `a` | `0` | `1`（且該 `1` 即 CC 自身之報告） | 🔴 **⛔ 可得·失傳** |
| `b` | `0` | `2` | 🔴 **⛔ 可得·失傳** |
| `c` | **`1`** | `0` | 🟢 可得——`W-G.9-308_補令一…:38` 逐字「⇒ 以工項四之探針對**生產碼 `34` 檔之全部呼叫**全證之，並立為恆常附款（`§一-1` 款 `c`）」⇒ **其條文 ＝ 該單 `§一-1` 之款 `c` 列** |
| `d` | **`1`** | `0` | 🟢 可得——`W-G.9-310…:43` 逐字「`自誤 422` 之攔法立為恆常附款（`§一-1` 款 `d`）」 |
| `e` | `0` | `8` | 🟡 **可重建**——`W-G.9-314…:136` 逐字：「`恆常附款 e`（`自誤 423` 之攔法：期值之母體含本單自身之產物 ⇒ 寫為「除本單自身之產物外 ＝ `N`」）」；其射程經 `自誤 427` 擴及「落地後之機驗」（同檔 `:98`／`:142`） |
| `f` | `0` | `0` | 🔴 **⛔ 可得**（且⛔ 引用 ⇒ 可能自始未立） |
| `g` | `0` | `5` | 🟡 **可重建**——`.gitignore` 之現查：本批落檔之名⛔ 落入忽略族（`:30` `verify/out/got_*`／`:31` `verify/out/W-D.4_*`）（`W-G.9-315…:70`／`:91`·`W-G.9-316…:69`／`:91`） |
| `h` | `0` | `7` | 🟡 **可重建**——「凡含反引號之文字一律**以檔案管線**為之·⛔ 以 shell 命令列傳遞」（`W-G.9-314…:122`／`:207`·`W-G.9-315…:165`／`:224`） |
| `i` | `0` | `2` | 🟡 **可重建**——出單／開工前須現查 `GB` 簿之阻塞項，具名本批受詞是否涉任一未解除項（`W-G.9-314…:66`） |
| `j`〜`w` | 各 `2`（`u` ＝ `4`） | — | 🟢 可得 |

⇒ **根因**：恆常附款**無單一正典簿**，其文散落三形——①「立為恆常附款 `x`」＋ 同段條文；②「立為恆常附款（`§一-1` 款 `x`）」指向該單開工閘之列；③ 僅見引用。CC 之搜框只及形 ①，故將 ② 誤判為不可得。
⇒ **辦法**：`§二` 立**恆常附款簿**；`a`／`b`／`f` 標【**失傳**】，🛑 **⛔ 憑記憶補寫、⛔ 由引用反推其條文**；`e`／`g`／`h`／`i` 以**倉內逐字**重建並標【**重鑄·非原文·其出處逐檔逐列具名**】。

**裁 `4`　CC 自捕第 `10` 則之通則 ⇒ 立為恆常附款 `x`**
`append-only` 之判準⛔ 寫為二值之合取（`前綴 ⋀ 嚴格增長`）——須三值化：**🟢 前綴且增長／🔵 前綴且未改（本批⛔ 涉該檔）／🔴 前綴被破**。二值式把 🔵 誤染為 🔴。

**裁 `5`　二檔名之核可**：`docs/reports/W-G.9-318R4_補令三_保全push之授權與四簿期值更正及自誤441_執行報告.md` 與 `docs/reports/W-G.9-318_CC側換手_一.md` ⇒ **二者皆核可**，⛔ 改名。

**裁 `6`　`preserve` 末值之併入**：`bc97c9d72d1680e820872aba787ecc453fa3a509` 已載於本補令卷首，並為新窗開工閘之錨。

---

## `§一`　新窗開工閘（**`10` 項**·逐閘出艙·🛑 任一 🔴 ⇒ 停機）

| # | 閘 | 判準 |
|---|---|---|
| `1` | 環境 | `sys.version`／`git --version`／`ezdxf`·`shapely` 版本逐字 |
| `2` | **取回本波** | `git fetch origin refs/heads/wip/W-G.9-318-preserve` ⇒ `FETCH_HEAD` ＝ **`bc97c9d72d1680e820872aba787ecc453fa3a509`**（全 `40` 碼）。🛑 **⛔ 假定新環境已有**；不符 ⇒ 停機 |
| `3` | 主線 | `git ls-remote origin refs/heads/wip/s1-endpart` ⇒ **`4037570…`**（**未動**）；`git merge-base --is-ancestor 4037570 bc97c9d` ⇒ `rc` `0` |
| `4` | 分支 | 遠端總數 **`25`**；`refs/heads/verify/*` **`20`** |
| `5` | preflight | `python verify/probes/probe_order_preflight.py <本補令>` ⇒ 🔴 `0`；🟡 逐項具名處置 |
| `6` | `core` 設定 | `core.autocrlf` ＝ `false`；`core.quotepath` **未設** |
| `7` | 生產碼 | `34` 檔（正面列舉·⛔ 空 `pathspec`）；**blob 錨繫於 `bc97c9d`**（🛑 ⛔ 沿用任何前批之 blob·恆常附款 `w②`） |
| `8` | 四簿 @ `bc97c9d` | **二框並報**：自擬框 自誤 `427`／`441`（缺 `[106,355,356,357,434,435,436,437]`）｜正典框 自誤 **`431`**／`441`｜`GB` `169`／`171`｜`VR` `80`／`95`｜`K-9` `40`／`41` |
| `9` | `baselines` | `git ls-tree -r HEAD verify/baselines \| sha256sum` ＝ `a898f4e1…`·`298` 列（🩸 `quotePath` 須預設） |
| `10` | 取號現查 `自誤 442` ＋ `恆常附款 x` | **除本補令自身之產物外 ＝ `0`**（恆常附款 `e`·`j②`）；對照甲 `自誤 441` `> 0`；對照乙 人造哨兵（執行期組出·字面⛔ 出艙）＝ `0` |

---

## `§二`　工項一：恆常附款簿之立（**零生產碼**·新檔）

**檔**：`docs/reports/W-G.9波_恆常附款登記表.md`（**append-only**·此後之增補一律末端追加）。

**每款一列**，欄位（**⛔ 缺一**）：`款` ｜ `受詞一句` ｜ `所繫之自誤號` ｜ `其逐字出處（檔:列）` ｜ `可得性判`（🟢 可得／🟡 重鑄·非原文／🔴 失傳）｜ `射程之增補（檔:列）`。

🛑 **三禁**：
① `a`／`b`／`f` 一律標 🔴 **失傳**——**⛔ 憑記憶補寫、⛔ 由引用反推條文、⛔ 另擬一條充之**；其「受詞一句」欄一律填【**⛔ 可得**】。
② `e`／`g`／`h`／`i` 標 🟡 **重鑄**，其條文**逐字取自倉內之引用段**（裁 `3` 表所列之檔:列），並於同列具名「**重鑄·非原文**」。
③ `c`／`d` 之條文 ＝ 其所指之單 `§一-1` 之款列，**逐字轉錄**並具名其檔:列。

🔒 **閘**：
`二-1` 款數 ＝ **`23`**（`a`〜`w`）·⛔ 缺號⛔ 增號；
`二-2` 逐款之「出處」欄皆可由 `git show <檔>:<列>` 復現（抽驗 `5` 款·逐字相符）；
`二-3` 判別力[必不命中]：一人造款名（執行期組出·字面⛔ 出艙）於本簿之命中 ＝ `0`；
`二-4` 🔴／🟡 之款數 ＝ **`3`**／**`4`**（不符 ⇒ 具名相異·⛔ 追改）。

---

## `§三`　工項二：交接文之補正（末端追加·⛔ 改上文一字）

`W-G.9-318_CC側換手_一.md` 之第 `6` 項（恆常附款現況表）⇒ 改為**指向 `§二` 之簿**，並末端追加一節載：
· 裁 `2` 之更正清單（**`6` 筆**生產碼 `commit`·含 `b1141a3` 之中間態性質）；
· 裁 `3` 之可得性判（`3` 失傳／`4` 重鑄）；
· 恆常附款 `x` 之立。

---

## `§四`　工項三：登記（**零生產碼**·自誤簿檔末·🛑 標題形**自最近一則逐字複刻**·恆常附款 `v①`）

> ### 🩸 `自誤 442`　**待放行清單依「何者為現行碼面」而列，⛔ 依「何者動生產碼且將隨快轉落入主線」而列，致漏列一筆被取代之中間態生產碼 `commit`**
>
> **根因**：`c7`（`b1141a3`）於補令一被 `c13` 完全取代，發單側遂以「現行」為鍵列清單；然補令二裁 `2` 已裁定**歷史留痕·⛔ rebase**，故 `c7` 必隨快轉進入主線 ⇒ **「被取代」⛔ 等於「不落地」**。發單側於補令二裁 `6`、補令三附錄`甲` **二度沿用該五筆清單而未自倉重導**（框應為 `git show --numstat --format= <h> -- <生產碼 34 檔>`·母體 ＝ `4037570..preserve`）。與 `自誤 441` 同族：**沿用既有之數／表而未對本批自算**。
> **後果之框**：⛔ 偽數入倉；其害為**呈 KL 之放行受詞不完整**——KL 以為放行 `5` 筆，實為 `6` 筆。⛔ 影響淨樹（`c7` 之效已全數為 `c13` 取代·發單側逐處實查六呼叫端皆 ＝ `0.0 if not _fo_* else None`），故⛔ 土地後果；惟**受詞之完整性本身即放行制之前提**。
> **攔法（二款·立為恆常附款 `y`）**：
> ① 凡呈 KL 之**逐筆放行清單**，其母體須為「本批將落入主線之全部 `commit`」，判準為「**是否改動生產碼正面列舉之諸檔**」，⛔ 以「是否為現行碼面」為鍵；被取代之中間態**須列入並標其性質**；
> ② 該清單須於**每一次**呈 KL 時自倉重導一次（框與母體同格出艙），⛔ 沿用前一訊之清單。

**恆常附款 `x` 之立**（裁 `4`·登記於 `§二` 之簿，其「所繫之自誤號」欄填【CC 自捕第 `10` 則·⛔ 鑄自誤號】）：
> **`x`**：`append-only` 之判準⛔ 寫為二值合取（`前綴 ⋀ 嚴格增長`），須**三值化**——🟢 前綴且增長／🔵 前綴且未改（本批⛔ 涉該檔）／🔴 前綴被破。凡出艙 `append-only` 之判者，須同格具名該檔本批**是否受改**及其期初期末 blob。

---

## `§五`　收工閘（輕·報告入倉後量·⛔ 沿用他批之判準字串與錨值）

`1` 逐 `commit` `numstat`（刪除欄全 `0`·本補令零生產碼）｜`2` 生產碼 `34` 檔相對**本批開工態 `bc97c9d`** 之相異 ＝ `0`｜`3` 本批 `.md` 之 `CR` 全 `0`（🩸 CJK 路徑先解八進位跳脫；受檢集 `0` ⇒ 器紅）｜`4` 四簿**二框並報**（正典框 自誤 **`432`**／**`442`**·`GB` `169`／`171`）｜`5` `append-only`（🛑 **三值化**·恆常附款 `x`；`K-6` 典本批⛔ 受改 ⇒ 期值 🔵）｜`6` 自限（`baselines` `a898f4e1…`·`298` 列；`verify/*` `20`；遠端總數 `25`；主線 `4037570…`）｜`7` 母體三數｜`8` `ls-remote` 全 `40` 碼 ＋ 本批之筆數。

---

## `§六`　⛔ 為之事

1. ⛔ 改生產碼一字；⛔ 驅動全量；⛔ 開新分支。
2. ⛔ 觸 `wip/s1-endpart`；⛔ 自 `preserve` 支 fast-forward 主線（候 KL 逐筆放行）。
3. ⛔ 憑記憶補寫或由引用反推 `a`／`b`／`f` 之條文；⛔ 另擬一條充之。
4. ⛔ 以二值合取出艙 `append-only` 之判。
5. ⛔ 解除或收窄 `GB-168`／`169`／`170`／`171`。
6. ⛔ 沿用前批之 blob 錨、四簿期值或放行清單（恆常附款 `w`／`y`）。
7. ⛔ 代擬呈 KL 之放行格（`序 11`）。
8. ⛔ 改二檔名（裁 `5` 已核可）；⛔ 改前四件單一字。

---

## 附錄`甲`　呈 KL：逐筆放行（**清單已更正為 `6` 筆**）

**【現況】** 技術面我已自倉逐項復驗完畢，**全部相符**——主線一字未動、上移二筆確為純位移（去首導空白後逐列相同、縮排差一律 `4`）、六個呼叫端左右未交叉、`_b_*0` 確實不再進入實跑側、四簿與 `append-only` 皆如期。土地後果您已於 `2026-09-20` 核可。

**【一項更正】** 我先前呈給您的待放行清單是 **`5` 筆**，自倉重導後應為 **`6` 筆**——漏了 `b1141a3`。它是補令一之前那個版本（把起算點也傳給強制抵費地那一側的舊作法），後來被 `8e056b4` 完全取代。因為您已裁定「歷史留痕、不改寫歷史」，它會隨著快轉一起進入主線歷史，所以必須列給您看，不能因為「反正被取代了」就略過。

🔒 **淨結果不受影響**：我實查過最終樹，六個呼叫端全部是修正後的寫法，舊作法的效力在最終樹中已全數消失。

| # | 雜湊 | 內容 | 性質 |
|---|---|---|---|
| `1` | `7f43924` | `app.py` 區塊上移（純位移） | 現行 |
| `2` | `7c346b1` | `stepg` 同構上移（純位移） | 現行 |
| `3` | `7c07a36` | 新增 `w0_start`（不傳則行為完全不變） | 現行 |
| `4` | `6cc51e6` | 薄殼直通 | 現行 |
| `5` | **`b1141a3`** | 舊作法（兩側都傳） | 🔴 **中間態·已被 `8e056b4` 完全取代** |
| `6` | `8e056b4` | 只改非強制抵費地那一側（您核可之作法） | 現行 |

**【要你判斷】** **是否逐筆放行上列 `6` 筆，並將主線 `wip/s1-endpart` 自 `4037570…` 快轉至 `bc97c9d…`？**（是／否）

🛑 快轉會一併帶入本波全部 `26` 筆（含二十筆零生產碼之單、報告與登記）。主線現仍 ＝ `40375702378af4cefd2d11515fdf3f98c0791580`。

---

SELF_SHA256: 850016aede1f32a169801652d28d39f4452c30c50154318a16e3ba44e751ed64
