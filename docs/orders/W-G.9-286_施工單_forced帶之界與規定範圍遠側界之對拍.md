# `W-G.9-286`　施工單（**主檢**）：`forced` 帶之界 與 **規定範圍之遠側境界線** 是否為**同一條線** ／ `自誤 389`

> **態**｜開工 ＝ `d1d969e647beae01d475cf82b2d8f901e81d2783`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-286_施工單_forced帶之界與規定範圍遠側界之對拍.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（幾何量測 ＋ 登記·**零生產碼**·⛔ 替身·⛔ 反事實）
> 🛑 **本批鑄號 ＝ `自誤 389` 一號**（逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值／判準** |
|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `d1d969e647beae01d475cf82b2d8f901e81d2783` 或其後裔（**全 `40` 碼**·⛔ 短前綴·坑 `bh`） |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | 須**非**祖先；判別力[必為祖先] ＝ 本批基座之任一祖先 `commit` |
| `4` | `SELF_SHA256`（`P-5` 口徑） | 見本檔末列·**由發單側供值**；判別力二造須皆相異 |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-286` ＝ `True`；[必偽] 含 `W-G.9-285` ＝ `False` |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | 四簿 ＋ 坑 | 自誤 **`378`**／**`388`**｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 **`87`／`87`**·下一標籤 **`bj`** |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] 子層檔數 **須 `> 0`**（判準式·⛔ 定值·`自誤 386`） |
| `8` | `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**） |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g` 與 `GB-158` 之體例、簡繁誤字機檢）**逐項適用·⛔ 省**。
🆕 **二則新附款**（`W-G.9-285` 自捕所立·⛔ 省）：
`(a)` **二次對拍之 `rev` 一律釘死**——⛔ 用 `HEAD`（其於批內會被推進）；
`(b)` 凡書「復跑之結果」**一律先跑再寫**；凡書**避讓宣告**，其字面須**逐字可機驗**並附其實測。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-285` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `d8fd92d098b9c7ec…`）／
生產碼 `34` 檔於 `19b0b70a..d1d969e6` 異動 **`0`**／自誤 `378`／`388`（恰 `+1`）·其餘三簿未動／
`ALPHA_LABELS` **一字未動**（異動僅該二描述字串·`f"…{ALPHA_LABELS[-1]}"`）／`p3a` **非祖先**
⇒ **✅ 全批核可**。
🔑 **主檢之受領**：`^### 🔒 K-9-5-` 全系列 **`15`** 則；`K-9-5-14`（`2026-08-17`）／`K-9-5-15`（`2026-08-18`）
皆晚於 `K-9-5-13` 且皆載 `canonical`／`逐字` ⇒ **`自誤 388` 之由坐實**。
🩸 六則自捕皆於出艙前攔下，處置正確——**閘 `5'` 之 `rc=1` 尤其**：以「行文寫了『唯一』二字」充「該 token 命中」係
`⛔ 以意思有到充命中`，其**⛔ 事後改器或改單**之處置正確。

---

## `§一`　裁 ＋ **發單側之自認三**

| # | 事 | **裁** |
|---|---|---|
| `1` | `GB-79` 之**結案**登記於 `:1601`（`W-G.9-67`·乙式落地批） | **受領·並據以自認**（下列 `3`）。🛑 **⛔ 由此判 `forced` 帶是否亦已落地**——二者係**不同之物**，其關係即本批主檢 |
| `2` | 前版 `W-G.9-285` 從未送達 ⇒ 「取代前版」之自載與事實不符 | **照實受領**：該句係發單側於出單時之真實意圖（前版確曾產出並交付 KL），受單側**⛔ 臆造作廢本**之處置**正確**；⛔ 鑄號 |
| `3` | 🔴 **發單側自認三**：以 `K-9-5-14-a` 之**節內落地狀態加註**（書於 `W-G.9-56`）充**現時**之落地狀態，而⛔ 現查其後之批次是否已使之落地 | 責 ＝ 發單側 ⇒ **鑄 `自誤 389`**（`§四`）。🔴 **該誤已對 KL 陳述**（逐字＝「倉裡連『程式沒做』都已經登記好了」）⇒ **更正已於本批送出**·⛔ 悄悄改口 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-286_施工單_forced帶之界與規定範圍遠側界之對拍.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-285` 之單須 `1`／造乙 ＝ **執行期組出**之人造名·**先驗現母體命中為 `0`** ⇒ 須 `0`）；
`CR` ＝ `0`（併報 `LF` 與 bytes；判別力 ＝ 一真 CRLF 人造檔須 `> 0`）；blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：二條線之**逐位對拍**

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

乙式（`K-9-5-14`／`-15`）**已落地於 `_build_corner_range_v3`**（`GB-79` 結案·碼面 `S1_perp = max(_seg_P0Ps · sinθ, T)`·
平移後側界**仍平行於 SIDELINE**）。惟第 `2` 宗之**起算**並非直接取該遠側境界線，而係取
`_corner_buffer_S` 之 `buf`——其係以 **bisect 解至「真實池帶面積 ＝ `range_area`」**。
⇒ **二者是否為同一條線，尚未量過**。本項即量之。🛑 **⛔ 判其是否違反任何裁。**

### 三-2　射程與通道（🛑 **⛔ 逾**）

通道 ＝ **harness 路徑**（`verify/stepg_pipeline.py` 之 `run_step_g`）；受詞格 ＝ **`forced` 側之鏈頭**
（前批得 `0m` 態甲 `R4/left`／`3.5m` 態甲 `R4/left`／`3.5m` 態甲 `R2/left`——🛑 **本批於現母體重量並出艙其全部成員與其數**·⛔ 轉引）。
**⛔ 改碼一字、⛔ 施替身、⛔ 反事實**；二情境之驅動一律用既有旗標。

### 三-3　逐款出艙（`repr` 全位·**⛔ 捨入**·座標系 ＝ 平面直角座標）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | `_build_corner_range_v3` 於該側之產出：**遠側境界線之二端點**（與 FRONTLINE／BASELINE 之交點）、其**方向單位向量**、`S1_perp`、`S1_par`、`_seg_P0Ps`、`T`、`setback`、`min_width`、`sinθ`、`eps` | 取**該函式之回傳／其內之區域值**·⛔ 另算第二份 |
| `2` | `_corner_buffer_S` 之**實參逐項**：`block_poly` 之識別、`d_hat`、`front_p1`、**`allocation_dir` 之實際來源**（`allocation_dir_block`？`_first_corner_alloc_dir`？其他——**逐字具名其賦值鏈**）、`range_area`、`side`、`tol` | 🛑 **⛔ 預設任一來源**；⛔ climb 外層之同名物 |
| `3` | `range_area` 之**產生式逐字**：其是否 ＝ 款 `1` 所建範圍之**面積**（字樣錨 `grep -n "_build_corner_range_v3" app.py`·無 `^` 錨 ⇒ **二數並報**·先驗錨之唯一性·坑 `bi`） | 併出艙該面積之**當場值**與款 `1` 範圍多邊形之**實算面積**之逐位差 |
| `4` | `buf` 所定之線：以**同一** `_block_strip`／`_strip_s_range` 之切法還原其**二端點**與**方向單位向量** | ⛔ 自寫第二套切法；若該線於碼面⛔ 為具名物，即以其切法**還原**並逐字具名其還原式 |
| `5` | **二線之對拍** | `(a)` 方向之 `\|cos\|`（若同線則期 ＝ `1`·出艙其與 `1` 之差）；`(b)` 二線之**垂距**；`(c)` 端點之逐位差；`(d)` `buf` 線相對 SIDELINE 之**垂距** vs `S1_perp` 之**逐位差** |
| `6` | `W_0`（碼面·前批得 `4.4804447557822895`／`7.785015718090257`）與 `S1_perp` 之**逐位對拍** | 🛑 **當場重得 `W_0`**·⛔ 轉引前批之數（前批該二數之**實測**出處 ＝ `verify/out/WG9281R_w0chain.log`【倉】） |
| `7` | **藍影（`K-9-23` 閘一·`_k923_gate1`）於 `forced` 側之態**：`(a)` 該側是否被求值（`驗_B藍影` 欄之當場值·逐格）；`(b)` 其所據之「街角地遠側界」係**取自何物**——逐字具名其賦值鏈（是否即款 `1` 之遠側境界線？抑或款 `4` 之 `buf` 線？）；`(c)` 其 `Δs`／藍影面積之當場值 | 錨 ＝ `grep -n "_k923_gate1" app.py`（無 `^` 錨 ⇒ **二數並報**·先驗唯一性·坑 `bi`）；🛑 **⛔ 判藍影是否被架空**——出艙即止 |
| `8` | 判別力三造 | [必命中] `forced` 側之鏈頭格數 `≥ 1`（若 `0` ⇒ **loud**·⛔ 以空集充綠）／[必相異] 以 SIDELINE **自身**（平移量 `0`）之同式須與款 `5(b)` 得相異值／[必為零] **執行期組出**之人造向量與自身之 `\|cos\|−1` 須為 `0`；任一不成立 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判二線是否應為同一條、⛔ 判碼面是否違反 `K-9-5-12（四）`／`K-9-5-14`／`-15`、
⛔ 鑄 `GB`、⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL、⛔ 動任何生產碼。

---

## `§四`　工項二：`自誤 389`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 389`**：**以節內之「落地狀態」加註充現時之落地狀態，而⛔ 現查其後之批次是否已使之落地**。
> 具體形 ＝ 發單側引 `K-9-5-14-a`（書於 `W-G.9-56`）之逐字「**已裁定、⛔ 生產碼未實作**」，
> 據以向 KL 陳述「程式沒做、前置修正登記為 `GB-79`、實作屬 `K-6-A2`」；
> 而 `GB-79` **早已於 `W-G.9-67`（乙式落地批）結案**（`S1_perp = max(_seg_P0Ps · sinθ, T)`·
> 平移量改為**垂距**·構造自檢改比垂距且**兩種距離並檢**），碼面亦逐字自載「**乙式落地**」。
> **徵候**：該節之加註**自身未過期宣告**，而其真值隨後續批次而變；引用者若只讀該節即取得**過期之現況**。
> **族** ＝ `自誤 388`（受詞母體／時點未窮查）之**時間軸孿生**。
> **攔法**：凡引任何「落地狀態／未實作／待修」之記載，**須併查其所繫之 `GB` 號之現態**
> （`^### \`GB-N\` ` 之節 ⋀ 全檔之 `結案`／`解除` 字樣），並**以最晚之批次為準**；
> **⛔ 以節內加註充現況**；**⛔ 以「當時未實作」充「現在未實作」**。

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**（造甲 `True`／造乙 `old+b'X'` ⇒ `False`）、簡繁機檢之 `rc`。

---

## `§五`　收工閘

| # | 受詞 | 期／判準 |
|---|---|---|
| `1` | `numstat` 刪除欄**逐檔具名** | **全 `0`**（本批⛔ 就地更正任何既有檔） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0`；對照組[必非零] `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 ＝ 一已知含 `CR` 之倉內檔須 `> 0` |
| `4` | 四簿期末 | 自誤 相異 **`379`**／MAX **`389`**（＝ 期初 ＋ 一號·**⛔ 其他增減**）；`GB`／`VR`／`K-9` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` **一字未動** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 **其受詞係 `index`（`git cat-file blob :<path>`）** ⇒ 復跑前須先 `git add`，否則所報係**比較端未更新**之偽紅 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必非零] `≥ 1`）；`baselines` `298` 檔·`a898f4e1…`；`p3a` **非祖先**；`WV_BAKE` **未設**；`K-9-29` 任何子項⛔ 落地；`GB-167`／`168`／`169` **⛔ 修** |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§六`　🛑 **⛔ 為之事**

⛔ 動生產碼 `34` 檔一字；⛔ 動 `ALPHA_LABELS` 一字；⛔ 改坑索引器或任何器之判準／框／輸出格式；
⛔ 修 `GB-79`／`GB-167`／`168`／`169`；⛔ 解除、收窄或結清任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 改 `ok_t`／`ok_f` 一字；`GB-162` **三禁令維持**；**⛔ 引任何自 baseline 取得之數**；
⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑；⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 追改任何已入倉文件一字；
⛔ 判二線是否應為同一條、⛔ 判碼面是否違反任何裁、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§七`　報告之格

`§零` 開工閘 → `§一`〜`§二` 逐工項（逐格表以落檔**全量**出艙·⛔ `tail -N`）→ 判別力三造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框與母體**；框無 `^` 錨者**二數並報**；以 `git` 輸出為框者具名 `core.quotePath` 或取 `-z`。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／
`_build_corner_range_v3`／`_corner_buffer_S`／`_block_strip`／`range_area`／`S1_perp`／`S1_par`／
`_seg_P0Ps`／`sinθ`／`buf`／`W_0`／`allocation_dir`／`_k923_gate1`／`K-9-23`／`K-9-16`／
`ALPHA_LABELS`／`baselines`／`numstat`／
`deletions`／`sha256`／`merge-base`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: abc6f5c66ee136d258eed53f118b38b05b094a8ffafb1bd6c09330e16f5f9a31
