# `W-G.9-317`　施工單（**中**）：`K-9-41` 之鑄（KL 二裁）／落地前之最後定位（唯讀）／`自誤 432`

> **態**｜開工 ＝ `a4e4cf52417a9a1e295768afdb751debda1b76e8` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-317_施工單_K-9-41之鑄與落地前最後定位及自誤432.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **中**（**零生產碼**·**⛔ `run_verification`**·**⛔ 改生產碼 `34` 檔一字**·得新增**一支唯讀探針**·**⛔ 驅動管線**·⛔ 開分支·⛔ 修批）。
> 🛑 **本批鑄號 ＝ `K-9-41`（一則·入 `K-6` 典）＋ `自誤 432`（一號）**；`GB`／`VR`／坑 **⛔ 鑄**；**⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`**。
> 🔴 **本批⛔ 落地 `K-9-41`**——其係 KL 裁之**入典**，碼側落地為**下一張生產碼單**（須 KL 逐 `commit` 放行）。
> 🔴 **修批之號仍為 `W-G.9-309`**；本單⛔ 占其號、⛔ 辦修批。
> 🔑 **序**：**① `§二` 工項零** ⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一**（`K-9-41` 入典·單獨 `commit`）⇒ **④ `§四` 工項二**（唯讀定位·單獨 `commit`）⇒ **⑤ `§五` 工項三**（`自誤 432`·單獨 `commit`）⇒ **⑥ `§六` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-316R` 之復驗（發單側自倉獨立·態 `a4e4cf5`·`core.quotePath` 預設·框同格）

| # | 項 | 實得（產生方式） | 判 |
|---|---|---|---|
| `1` | 主線 | `a4e4cf52417a9a1e295768afdb751debda1b76e8`；`git log --oneline 1b2c400..HEAD \| wc -l` ⇒ **`7`**；受改 `15` 檔 | ✅ |
| `2` | 刪除欄 | 全 `0`，**唯一非零 ＝ `verify/probes/probe_WG9315_k95_12_stateB.py` 之 `1`**（＝ `W-G.9-316` `§四-1` 所授之唯一例外） | ✅ |
| `3` | 一行修 | `diff` ＝ 增 `1` 刪 `1`；補入之鍵**逐字 ＝ 單所令**（`key=lambda k: (str(k[0]), str(k[1]), str(k[2]))`）；該檔他處⛔ 動 | ✅ |
| `4` | 生產碼／底本 | 生產碼 `34` 檔相異 `0`；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；底本探針 `85acdbe6…` 一字未動 | ✅ |
| `5` | 二簿 append-only | 自誤簿 `912558`→**`917316`**（Δ `4758`·blob `2fd1c50892cd676addeecc5a44272904dd2d1d94`）；`GB` 簿 `924914`→**`928354`**（Δ `3440`·blob `800404b1a586cf2e56f272c1b6147a518d457e97`）；前綴皆逐位相符；`CR` 皆 `0` | ✅ |
| `6` | 四簿 | 自誤 `421`／`431`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40`（缺 `[]`） | ✅ |
| `7` | 自限 | `baselines` `a898f4e1…`·`298` 列；`refs/heads/verify/*` `20` 支；**`verify/out/` 既有 `919` 檔 blob 相異 `0`**、新增 `8`；`.err` 之 `22` 列經查全為他探針之 `SyntaxWarning`、與本批無涉 | ✅ |
| `8` | 母體 | 全 `docs/` `822`→**`824`**；`verify/out/` `919`→**`927`**；全倉追蹤 `2432`→**`2443`** | ✅ |
| `9` | 自捕 `1` 則 | `if True` 硬編之判別力 ⇒ 改真二造 ⇒ **受領·⛔ 鑄號** | ✅ |

### `§零-2`　裁（**`6` 項**）

1. **`W-G.9-316R` 之受領**：五工項、開工閘 `15`／`15`、收工閘八閘 ⇒ **全部受領**。

2. 🔴 **發單側之責 ⇒ 鑄 `自誤 432`**：`§四-2` 項 `1` 只令出二態之差而**未令出母體之可比性判**，致 `β` 之 `ΔΣ抵費地面積`（`−3062.83`／`−3002.11`·共有之宗 `5`·關 `22`／開 `5`·只在一側者 `17`）在報告中與 `α` 之可比值並列。**⛔ 偽數入倉**（落檔與報告皆已載其母體），惟缺一道判。逐字見 `§五`。

3. 🔑 **KL 之二裁已受領，鑄為 `K-9-41`（逐字見 `§三`）**。其對本波之三項直接效力（**登記·⛔ 據以自行落地**）：
   `(a)` **`W₀` 之二分已由 KL 明文**：非強制抵費地為首宗之側 ⇒ 起算點 `0`；強制抵費地為首宗之側 ⇒ 起算點 ＝ **該強制抵費地之 `W`**（其遠側境界線自 `SIDELINE` 中點之垂距）。
   `(b)` **累積差額制（乙案）維持** ⇒ `K-9-5-6` 之鏈定式與 `telescoping` 之期望式形制**⛔ 變**，`GB-168` 之 `⓪-b` 爭點**就此結清**（⛔ 據此解除 `GB-168`——其解除條件另有四項）。
   `(c)` **KL 逐字「強制抵費地無須計算 `G` 值」** ⇒ 其為既有行為抑或待落地，**本批須唯讀查證**（`§四` 受詞 `e`）。

4. 🔑 **落地之受詞已收斂至<u>一處</u>（發單側自倉碼面實查·⛔ CC 重推）**：
   `app.py:10208` `_W_near = float(np.dot(_bp_w - _mp_w, _ahat))`；其下 `_W_near_out = _W_near` 並逐字註記「**常數（不隨 `S_guess`）·供 telescoping 閘 `W₀`**」。
   ⇒ **`W-G.9-316` 所定位之二消費者（`rw_increment` 之鏈頭實參／`telescoping` 之期望式）<u>同源於 `_W_near`</u>** ⇒ **一處之修可同時校正二者**——此正是替身 `α`／`β` 之所以非忠實（`自誤 430`）。
   🔒 並列既例：`app.py:22425`–`:22431` 之**選槽側**早已落地同一二分（逐字註記「`K-9-5-12（五）-1`「首宗起點一律為 `0`」…⇒ 非 forced 之選槽起算點校回 `0`…⛔ forced 側維持原式」），其式為 `_b_L0 / _b_R0 = _mp_base_W0(…) if _fo_left / _fo_right else 0.0`；**實跑側（`:10243`）尚未有該分支**，且 `:10238` 之碼註已逐字載「⇒ **⛔ 需 `if forced` 之分支**」。

5. 🛑 **本批⛔ 動生產碼一字·其由（⛔ CC 自解）**：落地之形已近，然三事未定 —— `(i)` `_solve_G_one`（`app.py:13967`·薄殼直通·關鍵字參數）之**呼叫端**是否可見 `_fo_*` 或 `_b_L0`／`_b_R0`；`(ii)` 本案 `R4 right` 於**態乙**之 `_fo_right` 究為 `True` 抑 `False`（`W-G.9-278R` 之 `False` 係**態甲**之量·⛔ 直接轉用）；`(iii)` 強制抵費地是否已⛔ 計 `G`。三者皆唯讀可得 ⇒ **本批取之，下一張即為生產碼單**（須 KL 逐 `commit` 放行）。
   🛑 **若 `(ii)` 得 `_fo_right = True`**，則 `W₀ ≈ 0.18` 於該側**可能本即合於 `K-9-41 ①`** ⇒ 受詞須重定；⛔ 以「反正要改成 0」逕行。

6. **依賴序之更新**：`⓪-b`（分攤制）**已由 KL 裁結**（維持累積差額）；`⓪-a`（起算點）**已由 KL 放行修改程式**，其單待本批之定位。其後仍為 ① 廢前置合併之落地 ⇒ ② `F.0` 六鍵無決策之成因 ⇒ ③ `GB-170` 取錯線之修 ⇒ ④ `K-9-30`〜`K-9-41` 之落地。

### `§零-3`　本單取號之宣告框（母體 ＝ 全 `docs/` 追蹤檔 **`824`** 檔（讀不到 `19`）·態 `a4e4cf5`·錨定框 `(?<![0-9\-])<號>(?![0-9])`·框 ＝ `D1` 檔框 ＋ 列框 ＋ 檔框·時點 ＝ 工項零**之前**）

| 號 | `D1` 檔 | 列框 | 檔框 | 角色 |
|---|---|---|---|---|
| **`W-G.9-317`** | **`0`** | **`0`** | **`0`** | 受詢 ⇒ **未占用·可用** |
| `W-G.9-316` | `2` | `22` | `3` | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0`（依補款 `⑨` 他指排除後） | `0` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱） | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組），⛔ 採信本表。**另須查 `K-9-41` 之未占**：四簿之 `K-9` MAX ＝ `40`、缺號 ＝ `[]` ⇒ `41` 未占；不如期 ⇒ 停、上呈。

### `§零-4`　既往同題之清單與其結論（`自誤 426` 攔法 `③`）

| 受詞 | 既有登記 | 與本批之關係 |
|---|---|---|
| `W₀` 之消費者定位 | `W-G.9-316R` `§三`（`a`〜`f`） | **⛔ 重測**·本批只補 `§零-2` 裁 `5` 之三未定事 |
| `Δ=2.425` 之值空間 | `GB-168` 原節 ＋ 一次／二次精化 ＋ 二次末端登記 | **⛔ 重測**·本批⛔ 涉 |
| 首宗起點一律為 `0` | `K-9-5-12（五）-1`（KL 裁 `2026-08-12`）；選槽側已落地（`app.py:22425`–`:22431`·施工單 `W-G.9-27`） | `K-9-41` 係其**補充與確認**（forced 側之起算點明文化）·⛔ 推翻 |
| 累積差額制 | `K-9-5-6` 鏈定式（KL 裁 `2026-08-08`） | `K-9-41 ⑤` **維持**·⛔ 變 |
| `.gitignore`（附款 `g`） | `:30` `verify/out/got_*`／`:31` `verify/out/W-D.4_*` | 本批落檔名 `WG9317_*` ⛔ 落入該二族 |

---

## `§一`　開工閘（**`15` 項**·資料列之數見本節末之機驗·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準**（框同格） |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首 |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `a4e4cf52417a9a1e295768afdb751debda1b76e8` 或其後裔（`merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` 逐位相符；🔴 ＝ `0`；🟡 **逐項具名處置**（⛔ 靜默略過·⛔ 列為停機款） |
| `3'` | 受詞自證（以器所印之檔名列·框 ＝ 子字串框） | [必真] 含 `W-G.9-317` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `421`／`431`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜**`K-9` `39`／`40`（缺 `[]`）⇒ `41` 未占** |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py` | `34` 檔；判別力[必不命中] ＝ `verify/` 子層 `*.py` 須 `> 0`（發單側於 `a4e4cf5` 得 `332`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**·⛔ 設 `false`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`gb170` ＝ `0`；`p3a` ＝ `1` |
| `8` | 受改檔之期初（框 ＝ blob `sha1` ＋ bytes） | 自誤簿 ＝ `2fd1c50892cd676addeecc5a44272904dd2d1d94`·**`917316`** B；`K-6` 典（`docs/rulings/K-6_街角地分配程序與可分配判準.md`）＝ `234f8f9d7f416b8887bc1e562ba21574d8a4103d`·**`357541`** B·`CR` `0`·`4474` 列 |
| `9` | 不受改之錨（🛑 **本批⛔ 改其一字**） | `GB` 簿 ＝ `800404b1a586cf2e56f272c1b6147a518d457e97`；底本探針 `probe_WG9278_w0zero.py` ＝ `85acdbe6863cc078cb569c82d6e7b3a11dbcdc3a`；`probe_WG9315_k95_12_stateB.py` 與 `probe_WG9316_w0_consumers.py` 皆⛔ 動 |
| `10` | 本批新物之未占（**跑於 `§二` 之後**·框 ＝ 子字串框·母體 ＝ `git ls-tree -r --name-only -z HEAD`） | 檔名含 `W-G.9-317` 者 ＝ `1`；哨兵 `ZW432`／`KJ41N` 之全倉命中 ＝ **除本單自身之產物外 `0`**（逐筆具名所在檔）。發單側於 `a4e4cf5` 得二哨兵全倉 `0` 檔 |
| `11` | 宣告框（`§零-3` 四列·**CC 自跑**·含二對照組） | 受詢之命中**除本單自身之產物外 ＝ `0`**；對照甲 `D1` `≥ 1`；對照乙 ＝ `0` |
| `12` | `verify/out/` 之期初（框 ＝ 追蹤檔數 ＋ `git status --porcelain -z`） | 倉內追蹤 **`927`** 檔；本批**得新增**其下之落檔，⛔ 改既有 `927` 檔任一位元組 |
| `13` | `.gitignore` 之 `:30`／`:31` 逐字 | `verify/out/got_*`／`verify/out/W-D.4_*`；本批落檔名 `WG9317_*` ⛔ 落入該二族（附款 `g`） |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `|` 起首且**⛔** 為分隔列（`|---`）者，扣表頭 `1`），與本單自載之 `15` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以列舉為準、逐項皆跑並具名。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308` ＋ 補令一（`a`／`b`／`c`）·`310`（`d-1`〜`d-3`）·`311`（`e`／`f`）·`312`（`g`）·`313`（`h`／`i`）·`314`（`j`／`k`）·`315`（`l`）·`316`（`m`／`n`）之恆常附款全數逐項適用**（逐字見該八單·⛔ 省）。
🆕 **本單所增**（`o`·`自誤 432` 之攔法）：**凡令出二態之差者，須同時令出二態之母體（基數與只在一側者之數），並令其判「可比／⛔ 可比」；母體相異者一律先判⛔ 可比，其差⛔ 出艙為後果，僅得出艙為「母體相異之附帶量」。**

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-317_施工單_K-9-41之鑄與落地前最後定位及自誤432.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名含 `W-G.9-317` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。🛑 **落地後即 `push`**，其後方跑 `§一`。

---

## `§三`　工項一：**`K-9-41` 之鑄**（主線·單獨 `commit`·嚴格末端追加）

落點 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字）。
🔑 界：`<!-- KJ41N-BEGIN -->`／`<!-- KJ41N-END -->`（**定界以整列相等為之**·payload ＝ 二界列之間·**界列⛔ 隨 payload 入典**）。
🛑 凡含反引號之文字一律**以檔案管線**為之（附款 `h`）。

<!-- KJ41N-BEGIN -->

### 🔒 `K-9-41`　**側面道路負擔之起算點二分 ＋ `W` 之定義 ＋ 強制抵費地⛔ 計 `G` ＋ 累積差額制之維持**（KL 裁 `2026-09-19`·canonical·**逐字**）

**KL 原文（逐字·⛔ 增刪一字）**：

> 1. 題1：首宗之寬度一律自側面道路邊線（0 公尺）起算，並據以修改程式，是。但是若街角第1宗土地為強制抵費地時，假設強制抵費地W=7m，後一宗原位次分配後的土地W=12m（這裡的Ｗ是指從SIDELINE中點算到該宗土地的遠側境界線，所以後1宗W=12m是指含強制抵費地的7m），強制抵費地無須計算G值，所以該街廓該側的臨側街負擔=RW(12)-RW(7)
> 2. 題2：維持「累積差額」（乙案）分攤

**款（發單側之整理·⛔ 充 KL 原文）**：

① **起算點之二分**：街角第 `1` 宗**非**強制抵費地者 ⇒ 該側之起算點 ＝ **`0`**（側面道路邊線）；街角第 `1` 宗**為**強制抵費地者 ⇒ 該側之起算點 ＝ **該強制抵費地之 `W`**（`KL` 例：`7` 公尺）。
② **`W` 之定義**：自 `SIDELINE` **中點**量至**該宗土地之遠側境界線**之距離；⇒ 其**含**該宗之前所有宗（包括作為首宗之強制抵費地）之寬度（`KL` 例：後 `1` 宗之 `W = 12` 公尺，其中含強制抵費地之 `7` 公尺）。
③ **強制抵費地⛔ 計 `G` 值**。
④ **該街廓該側之臨側街負擔 ＝ `Rw(後宗之 W) − Rw(強制抵費地之 W)`**（`KL` 例：`Rw(12) − Rw(7)`）。
⑤ **累積差額制（乙案）維持**：同一街角側連續數宗，逐宗之負擔率 ＝ 累積至本宗之 `Rw` 減累積至前一宗之 `Rw`；全側合計 ＝ `Rw(最末宗之 W) − Rw(起算點之 W)`。⛔ 改採「逐宗各按自身寬度查表」（甲案）。
⑥ **正典依據**：`Rw` 之查表與 `W` 之定義源自**市地重劃實施辦法第二十九條附件二**（`W` ＝ 分配土地寬度；`Rw` ＝ 街角地側面道路負擔百分率·`W` ＝ `1`〜`18` 公尺對 `17.4`〜`100` ％）。附件二僅就「街角**第一筆**」明定（`F` ＝ 街角第一筆土地面臨側面道路之長度），連續數宗之分攤方式即本裁 `⑤` 所定。

**與既有裁之關係**：
· 承 `K-9-5-12（五）-1`「首宗起點一律為 `0`」，並**補充**其 forced 側之起算點（本裁 `①` 後段）——⛔ 推翻之。
· `K-9-5-6` 之鏈定式（KL 裁 `2026-08-08`：「第 2 宗之 `G` 值所用之 `Rw` ＝ `Rw_2 − Rw_1`……依此一路到最後一宗」）**⛔ 變**，本裁 `⑤` 為其確認。
· 選槽側之碼面二分（`app.py` 之 `_b_L0 / _b_R0 = _mp_base_W0(…) if _fo_left / _fo_right else 0.0`·施工單 `W-G.9-27` 落地）即本裁 `①` 之形；**實跑側尚未有該分支**。

**落地狀態**：⬜ **未實作**（實跑側之起算點分支；`③` 之現況待查）。其落地須**另單**並經 **KL 逐 `commit` 放行**。

<!-- KJ41N-END -->

**落地後之機驗**：`K-9` 框 ⇒ 相異 **`40`**／MAX **`41`**／缺號 **`[]`**；append-only 前綴 ＝ `§一` 閘 `8` 之 `K-6` 期初 bytes（`357541` B）；`CR` ＝ `0`；哨兵 `KJ41N` 於 `K-6` 之期值 ＝ **`0`**（界列⛔ 入典·如期·⛔ 讀為器紅），另以 `C` 形（`` `K-9-41` `` 於 `K-6` ＝ `1`）＋ 一必為零造自證器非紅。

---

## `§四`　工項二：**落地前之最後定位**（主線·單獨 `commit`·🛑 **全唯讀·⛔ 驅動管線·⛔ 修任何檔一字·⛔ 施任何替身**）

**器** ＝ 新探針 `verify/probes/probe_WG9317_w0_landing_scope.py`（唯讀·AST ＋ 字樣錨·⛔ 匯入即驅動）。落檔 ＝ `verify/out/WG9317_landing_scope.log`。
🛑 **⛔ 判孰為正確、⛔ 提修法主張、⛔ 擬任何 diff、⛔ 稱任何處為「缺陷」**——照實具名。

| # | 受詞 | 框／母體 |
|---|---|---|
| `a` | `_solve_G_one`（`app.py:13967` 附近·以 AST 現查其真實 `lineno`）之**完整簽章**（全部關鍵字參數與其預設值·`ast.unparse`） | AST·母體 ＝ `app.py` |
| `b` | `_solve_G_one` 之**全部呼叫節點**：所在檔、`lineno`、**包層函式名**、實參之 `ast.unparse` | AST·母體 ＝ `app.py` ＋ `verify/` 之 `*.py`（本批母體基數須**當場實得並出艙**·⛔ 採單所載之數） |
| `c` | 對 `b` 之每一呼叫節點，其**包層函式之區域名空間**是否含 `_fo_left`／`_fo_right`／`_b_L0`／`_b_R0`／`_mp_base_W0`／`_left_buffer_S`／`_right_buffer_S`（逐名逐節點之布林表·AST `Name`／`arg` 之出現） | AST·母體 同 `b` |
| `d` | `_mp_base_W0` 之**全部定義節點**（`app.py` ＋ `verify/stepg_pipeline.py`）之完整函式體 `ast.unparse`，二者是否**逐字相同** | AST |
| `e` | **「強制抵費地⛔ 計 `G`」之碼面現況**：以字樣錨（`forced`／`抵費地`／`skip`／`continue`／`G` 之組合）＋ AST，列出凡在 `forced` 條件下**跳過或歸零** `G` 之節點，逐筆出艙其逐字與包層；**無命中者照實載為 `0` 並具名其查法與母體** | AST ＋ 列框 |
| `f` | `_W_near`／`_W_near_out` 之**全部出現節點**（賦值與使用），逐筆具名包層與 `ast.unparse` | AST·母體 同 `b` |
| `g` | `f3L_forced_offset`（`left_forced_offset`／`right_forced_offset`）之**產生處**：其賦值節點逐字與包層；並自**倉內既有之落檔**（`verify/out/` 現有檔·⛔ 新驅動）擷取本案 `R4` **右**側於**態乙**之 `right_forced_offset` 與 `W0_right`／`Wf_right` 之值；**取不到者照實載為【⛔ 可得·其止於 <逐字>】**，⛔ 以推定填之 | 列框·母體 ＝ `verify/out/` 之 `927` 檔（逐檔具名所查者） |

🔒 **判別力二造**：`(甲·必有命中)` `_solve_G_one` 之呼叫節點數須 `> 0`；`(乙·必為零)` 一人造之函式名（**執行期組出**·字面⛔ 出艙·坑 `bd`）之呼叫節點數 ＝ `0`。**二造同色 ⇒ 器紅 ⇒ ⛔ 出艙任何定位之數。**
🩸 **自指污染之攔**（`GB-132` 族·`W-G.9-316` 之既例）：本器自身之原始碼含上列名之字面 ⇒ **母體須逐檔具名**，並同格載「已扣除本器自身者」與「未扣除者」二數。

---

## `§五`　工項三：**`自誤 432` 之鑄**（主線·單獨 `commit`·嚴格末端追加）

落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（⛔ 改上文一字）。
🔑 界：`<!-- ZW432-BEGIN -->`／`<!-- ZW432-END -->`（**定界以整列相等為之**·payload ＝ 二界列之間·界列⛔ 入簿）。

<!-- ZW432-BEGIN -->

### 🩸 `自誤 432`　**單令出二態之差而未令出母體之可比性判，致一個母體相異之差值（`ΔΣ抵費地面積`）與可比之差值並列於同一表**

**形**：`W-G.9-315` `§四` 項 `5` 與 `W-G.9-316` `§四-2` 項 `1` 皆令出「`ΔΣG`／`Σ|ΔG|`／`ΔΣ抵費地面積`（替身開／關之差·關 ＝ 同次驅動內之未替身基線）」，**只令出差，未令出二態之母體是否可比之判**。

**徵候**：`W-G.9-316R` 實得——替身 `α`：共有之宗 `22`（關 `22`／開 `22`·只在一側者 `0`）⇒ **可比**，其 `ΔΣ抵費地面積` ＝ `+0.9017197385`，與 `ΔΣG` ＝ `−0.9000000000` 守恆（差 `0.0017` 為捨入）。替身 `β`：共有之宗 **`5`**（關 `22`／開 `5`·**只在一側者 `17`**）⇒ **⛔ 可比**，其 `ΔΣ抵費地面積` ＝ `−3062.8253365401`／`−3002.1098366347` 係「開側止於 `R1`、其餘街廓之池未生成」之產物，**⛔ 土地後果**。二者於報告同表並列而無可比性之標記。

**根因**：單之受詞只及於「**差**」，未及於「**差之母體**」；恆常附款 `l①`（期值須同格載框與母體）之射程及於**數之框**，而未及於**二態之母體是否同一**。此為「單之受詞框不足」之**第四度**——`自誤 429`（期值未載框）／`431`（閘之值域為二值而正典三值）／`430`（替身射程未涵蓋受詞之全部消費者）／本則（母體可比性之判未令出）。

**後果**：⛔ 偽數入倉——落檔與報告皆已逐格載其母體（`關 22／開 5·只在一側者 17`），CC 之報告⛔ 誤。惟缺一道判，日後讀者可能將 `−3062.83` 讀為土地後果。未動土地、未致錯鑄、未致錯裁。

**併記（⛔ 鑄號·照實）**：發單側於同一回合之覆核訊中，將 `1b2c400..a4e4cf5` 之 `commit` 數寫為 `6`，實為 **`7`**（`a54eeb7`／`fc6f788`／`9f103e1`／`004ed69`／`6698c2f`／`0fdb258`／`a4e4cf5`）；已於次訊照實更正。該數係**逐項列舉可自證**者，其誤係未以程式對自身所列之集合計數 ⇒ 同 `自誤 424` 之族。

**攔法（三款·立為恆常附款 `o`）**：
① 凡令出**二態之差**者，須同時令出**二態之母體**（各自之基數、共有者之基數、只在一側者之基數）；
② 並令其判「**可比／⛔ 可比**」；**母體相異者一律先判⛔ 可比**；
③ ⛔ 可比之差**⛔ 出艙為後果**，僅得出艙為「母體相異之附帶量」，且須於同格具名其相異之由。

<!-- ZW432-END -->

**落地後之機驗**：`registry` ⇒ 自誤 相異 **`422`**／MAX **`432`**（缺號 `[106, 355, 356, 357]` 未變）；`GB`／`VR` 未動；`K-9` ＝ `40`／`41`（本批工項一之結果）；append-only 前綴 ＝ `§一` 閘 `8` 之自誤簿期初 bytes（`917316` B）；payload 逐位；bytes 三者分列（payload **⛔ 含**末換行·分隔 `NL` 獨立計·附款 `l②`）；哨兵 `ZW432` 於簿 ＝ `0`（如期·⛔ 讀為器紅），全倉命中 ＝ 除本單自身之產物外 `0`（框 ＝ 子字串框·時點 ＝ 本工項之 `commit`）。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**（本批**⛔ 例外**）；同格載受檢檔數 `> 0` |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動之 `.md` 之 `CR` | ＝ `0`；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末 | 自誤 `422`／`432`；**`K-9` `40`／`41`（缺 `[]`）**；`GB` `168`／`170`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（自誤簿 ＋ `K-6` 典） | 各自之期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes（`917316`／`357541`） |
| `6` | 自限復驗 | `verify/baselines` `298` 列·`a898f4e1…` 未動；`data/` 全樹未動；`CLAUDE.md` 未動；**`GB` 簿 `800404b1…` 一字未動**；**三既有探針（`9278`／`9315`／`9316`）blob 未動**；**`verify/out/` 既有 `927` 檔逐位未動**（本批新落檔另列）；`refs/heads/verify/*` `20` 支·`gb170` `0`·`p3a` `rc` `1`；`WV_BAKE` ＝ `None`；`WV_K6_STEP0` ＝ `None` |
| `7` | 標籤對照 | 本報告全部計數標籤逐欄具名其**框**、集合與基數（**當場以程式自資料列計數**）；凡命中之數並載其**時點**；凡閘之判一律**三值框**（附款 `n`）；凡二態之差一律並載母體與可比性判（附款 `o`） |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`〜`6` 任一不如期 ⇒ **停、上呈**（照實全量出艙·⛔ 判改法）。

---

## `§七`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；`K-9-41` 之入典（`K-6` 檔末追加）；新增 `probe_WG9317_w0_landing_scope.py`（**唯讀·⛔ 驅動**）；`自誤 432` 之鑄。
其餘：⛔ 改生產碼 `34` 檔**一字**（🛑 **本批之受詞雖已收斂至 `app.py:10208`／`:10243`，仍⛔ 動其一字**——其落地為**下一張單**，且須 KL 逐 `commit` 放行）；
⛔ 改任何既有探針一字（`9278`／`9315`／`9316`）；⛔ 新增第二支器；⛔ `run_verification`；⛔ 驅動管線（含 `run_step_g`／`run_corner_pk`／`solve_G_binary` 之任何執行）；⛔ 施加任何替身；⛔ 改 `verify/out/` 既有 `927` 檔任一位元組；
⛔ 擬任何 diff、⛔ 提修法主張、⛔ 判孰為正確、⛔ 稱任何處為「缺陷」；⛔ 判 `_fo_right` 之應然值；
⛔ 動 `GB` 簿一字；⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`；⛔ 鑄 `K-9-41`／`自誤 432` 以外之任何號；⛔ 改 `K-6` 典上文一字；⛔ 增刪 KL 原文一字；
⛔ 開分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——遇可觀測之硬限事件一律 loud 照實具名、停、上呈；
🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**（附款 `h`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`docs/reports/W-G.9-317R_K-9-41之鑄與落地前最後定位及自誤432_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `3` 之 🟡 逐項具名處置、閘 `11` 之自跑與二對照）⇒ `§三` 工項一（機驗·含 `K-9` 框之證）⇒ `§四` 工項二（`a`〜`g` ＋ 判別力二造 ＋ 自指污染之二數）⇒ `§五` 工項三（機驗）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其**框**、母體與產生指令；凡命中之數並載其**時點**；**計數標籤一律當場以程式自資料列計數**。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框與 `§零-3` 同源；四簿框於 `a4e4cf5` 實跑 ＝ `421`／`431`｜`168`／`170`｜`80`／`95`｜`39`／`40`。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`registry`／`K-9-41`／`自誤 432`／`_solve_G_one`／`_mp_base_W0`／`_W_near`／`right_forced_offset`／`⛔ 可比`／`W-G.9-309`／`a4e4cf5`／`data/V6.dxf`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: f12a368749f870ab4dbc2625d2e0dd970c1f14d848c766b64ecddc4d8c904020
