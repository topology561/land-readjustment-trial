# `W-G.9-313`　施工單（**輕**）：`自誤 426` 之鑄 ／ `W-G.9-312` 二態對拍結果之登記（`GB-168` 互引·**⛔ 解除·⛔ 收窄**）

> **態**｜開工 ＝ `cb661f7bd9e35eebe43fa20e945ea3780c30e514` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-313_施工單_自誤426與312二態對拍之登記.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **輕**（**純登記**·**零生產碼**·**⛔ `run_verification`**·**⛔ 跑任何探針以外之物**·**⛔ 開分支**·**⛔ 修批**·**⛔ 觸及 `verify/out/` 落檔**）。
> 🛑 **本批鑄號 ＝ `自誤 426`**（**一號**·逐字由本單供）；`GB` 簿之追加係**末端追加**（`戒 36`）**⛔ 鑄號**、**⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`**；`VR`／`K-9`／坑 **⛔ 鑄**。
> 🔴 **修批之號仍為 `W-G.9-309`**（於 `cb661f7` 態宣告框六數皆 `0`·保留）；**本單⛔ 占其號**、⛔ 辦修批、⛔ 自續 `W-G.9-309`。
> 🔑 **序**：**① `§二` 工項零**（單獨 `commit`·單獨 `push`）⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一** ⇒ **④ `§四` 工項二** ⇒ **⑤ `§五` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-312R` 之復驗（發單側自倉獨立·沙盒 `Python 3.12.3`·態 `cb661f7`·`core.quotePath` 預設）

| # | 項 | 發單側實得（產生方式） | 判 |
|---|---|---|---|
| `1` | 主線 | `cb661f7bd9e35eebe43fa20e945ea3780c30e514`；`git rev-list --count 16244f1..HEAD` ＝ `6`；刪除欄非 `0` 者 ＝ `0`；受改檔 ＝ 單／自誤簿／報告 ＋ `verify/out/` 十二落檔 | ✅ |
| `2` | 工項零之逐位 | `git show HEAD:<單>` 之 bytes ＝ 發單側交付之源（`25834` B） | ✅ |
| `3` | 工項一之逐位 | 自單 blob 以整列相等定界抽出 `ZW425` `2201` B；**期末 bytes ＝ 期初 bytes ＋ payload bytes**（`898339`→`900540` B） | ✅ |
| `4` | 四簿 | 自誤 `415`／`425`（缺 `[106, 355, 356, 357]`）；`K-9` `39`／`40`；`GB` `168`／`170`；`VR` `80`／`95` | ✅ |
| `5` | 生產碼 `34` 檔 | `16244f1` 對 `HEAD` 逐檔 blob 相異 ＝ `0`；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `6` | 自限 | `verify/baselines`、`data/` 全樹、`CLAUDE.md`、`K-6` blob 皆未動；`refs/heads/verify/*` ＝ `20` 支；`verify/out/` `901`→`913` | ✅ |
| `7` | 二態落檔 | `WG9312_runall_on.log` `161089` B·`1029` 列·`CR` `1029`；`WG9312_runall_off.log` `36032` B·`326` 列；`landeffect` 二態**逐位相同**（`86404` B·同 `sha256`）且與 `WG9311_landeffect.log` **逐位相同** | ✅ |
| `8` | 二態之名目 | 自二落檔逐列復算（框 ＝ 子字串框 `✅ PASS`／`🔴 FAIL`）⇒ 甲 `24` ＋ `40` ＝ `64`；乙 `12` ＋ `22` ＝ `34`；乙∖甲 `9` 名目、甲∖乙 `39` 名目；二態判相異者 `1`（`抵費地0m(應空)`：甲 🔴 ⇒ 乙 ✅） | ✅ |
| `9` | 乙態之破 | 乙態落檔載 `🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）`，`0m`／`3.5m` 二情境皆破（該字樣命中 `5`）；其後 `stepg_ctx[0m] 缺（trunk A 未成功？）` ⇒ `F.0` 以下全未執行；乙態 `GSA` 命中 `0` | ✅ |
| `10` | 通道之證 | `k6_step0_merge` 之呼叫在 `app.py` 之 `_annotate_temp_parcel_cut_type` 內；該函式之呼叫點 ＝ `app.py:18342` 與 **`verify/selection_pipeline.py:253`** ⇒ 二態對拍作用於 harness 通道（母體 ＝ `verify/` 之 `.py` `363` 檔·框 ＝ 子字串框） | ✅ |
| `11` | 事故 | CC 之 fork bomb（後於收工閘與 `push`）⇒ **倉態無損**：無多餘 `commit`、`refs/heads/verify/*` `20` 支未變、追蹤檔異動 `0`、生產碼與 `baselines` 逐位未動 | ✅ |

### `§零-2`　裁（**`5` 項**）

1. **`W-G.9-312R` 之受領**：工項零、開工閘 `15`／`15`、工項一（`自誤 425`）、工項二（`E-1`〜`E-8`）、工項三、收工閘 ⇒ **全部受領**。**CC 之五自捕**（量測器恆綠／`E-8` 之「無從判定」誤記為「出現決策列」／二態 `got_*` 之分離／閘 `7` 標籤之時點／`push` 後之 fork bomb）⇒ **受領·⛔ 鑄號**；其止息、清理與倉態復驗之處置**正確**。

2. **`E-8` 之判**：乙態之 `GSA` 六鍵 ＝ **無從判定**（⛔ 讀為「否」）；`F.1`〜`F.4` 二態皆未執行。**本假說⛔ 於本批獲證亦⛔ 獲否**。

3. 🔴 **發單側之責 ⇒ 鑄 `自誤 426`**：`W-G.9-312` 之乙態現象（`WV_K6_STEP0=off` 下 `run_step_g` 於 `R4 right` 之結構閘破）**早經 `GB-168` 登記**，且 `W-G.9-273`／`274`／`277`／`278` 已就其作四次值空間定位與二次受詞精化；發單側出單前未以該受詞全倉查 `GB` 簿即令重測。逐字見 `§三`。

4. **`W-G.9-312` 之殘值⛔ 為零**（照實併記）：其新得而**⛔ 在 `GB-168` 既有登記內**者為三——(`a`) 甲態（現態·`cb661f7`）之 `F.0` `GSA` 六鍵**逐鍵無決策列**；(`b`) `抵費地0m(應空)` 之判於二態**翻轉**（甲 🔴／乙 ✅）；(`c`) `probe_WG9287_landeffect.py` 之輸出於二態**逐位相同** ⇒ `GB-170` 三格之值**不受步驟 0 影響**。此三項以工項二登記於 `GB` 簿末端，**⛔ 解除、⛔ 收窄任何 `GB`**。

5. **依賴序之更新（登記·⛔ 據以自行落地）**：`GB-168` 之解除條件四項合取（成因具名 ＋ 修入生產碼並經 **KL 放行** ＋ 判別力二造 ＋ 態乙六街廓全可得）**係 `K-9-29` 子項 `1`（廢前置合併）碼側落地之前置**，而後者又係 `GB-170` 修批期值之前置。⇒ 序 ＝ **`GB-168` 之修（生產碼·須 KL 放行·另單）⇒ 廢前置合併之落地 ⇒ `F.0` `GSA` 之成因 ⇒ `GB-170` 取錯線之修（`W-G.9-309`）⇒ `K-9-30`〜`K-9-40` 之落地**。🛑 **本單⛔ 辦其任一**。

### `§零-3`　本單取號之宣告框（補款 `①`〜`⑨`·**六數分列**·母體 ＝ 全 `docs/`·態 `cb661f7`）

**三軸**：`(1)` 來源 ＝ `blob@cb661f7`／`(2)` 母體 ＝ 全 `docs/` **`816`** 檔（讀不到 `19`）／`(3)` 粒度框 ＝ 宣告框（`D3` 取補款 `⑧` 之嚴格式 `^` 起首·`D2` 取補款 `⑨` 之他指排除·三形皆帶數字邊界 `(?!\d)`）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 依 `⑨` 排除之 `D2` 列 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-313`** | **`0`** | **`0`** | **`0`** | `0` | **`0`** | **`0`** | `0` | 受詢 ⇒ **未占用·可用** |
| `W-G.9-312` | `2` | `2` | `6` | `0` | `8` | `2` | `0` | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組），⛔ 採信本表。

---

## `§一`　開工閘（**`13` 項**·資料列之數見本節末之機驗·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首 |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `cb661f7bd9e35eebe43fa20e945ea3780c30e514` 或其後裔（`merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` 逐位相符；🔴 ＝ `0`；🟡 逐項具名處置 |
| `3'` | 受詞自證（以器所印之檔名列） | [必真] 含 `W-G.9-313` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `415`／`425`（缺 `[106, 355, 356, 357]`）｜`K-9` `39`／`40`｜`GB` `168`／`170`｜`VR` `80`／`95` |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py` | `34` 檔；判別力[必不命中] ＝ `verify/` 子層 `*.py` 須 `> 0`（發單側於 `cb661f7` 得 `330`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`gb170` ＝ `0`；`p3a` ＝ `1` |
| `8` | 二受改檔之期初（`git cat-file -e` 後 `git rev-parse HEAD:<路徑>`·取 bytes） | 自誤簿 `b0b4aa2ecc1edae2788d94262f1837092c6d84ae`·`900540` B／`GB` 簿（`docs/reports/W-G.4_泛用阻塞項登記表.md`）`9dc9e780511e4f0df2c53d7622fa141e6732e7a5`·`921652` B；期初 bytes 存錄於記憶體 |
| `9` | 本批新物之未占（**跑於 `§二` 之後**） | 檔名含 `W-G.9-313` 者 ＝ `1`；哨兵 `ZW426`／`GB168N` 之全倉子字串框命中 ＝ **除本單自身之產物外 `0`**（`自誤 423` 攔法·逐筆具名所在檔）。發單側於 `cb661f7` 得二哨兵全倉 `0` 檔 |
| `10` | 宣告框（`§零-3` 六數·**CC 自跑**·含二對照組） | 受詢之命中**除本單自身之產物外 ＝ `0`**；對照甲 `D1` `≥ 1`；對照乙 ＝ `0` |
| `11` | `verify/out/` 之不動（本批⛔ 觸及） | 倉內 `913` 檔；`git status --porcelain -z -- verify/out/` 之輸出照實出艙 |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `\| ` 起首之列，扣表頭 `1`），與本單自載之 `13` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以列舉為準、逐項皆跑並具名。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308`、`W-G.9-308_補令一`（`a`／`b`／`c`）、`W-G.9-310`（`d-1`〜`d-3`）、`W-G.9-311`（`e`／`f`）、`W-G.9-312`（`g`）之恆常附款全數逐項適用**（逐字見該五單·⛔ 省）。
🆕 **本單所增**：
（`h`·反引號之攔法）**凡單令 CC 處理含反引號之文字者，須同格令其以檔案管線為之、⛔ 命令列**；【驗收】固定項**⛔ 列入須回顯之反引號字樣**（`W-G.9-312R` 自捕 `5`：命令列之反引號遭 shell 命令替換，致入倉之 `.md` 被當 bash 腳本遞迴執行）。
（`i`·`自誤 426` 之攔法）**凡單令之量測，出單前以其受詞之關鍵語全倉查 `GB` 簿與既有報告**，查得者於單內同格具名其與本批之關係（**復現／擴充／新題**），並讀該阻塞項之**全部末端追加至檔末**。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-313_施工單_自誤426與312二態對拍之登記.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名含 `W-G.9-313` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。🛑 **落地後即 `push`**，其後方跑 `§一`。

---

## `§三`　工項一：**`自誤 426` 之鑄**（主線·單獨 `commit`）

🔑 界：`<!-- ZW426-BEGIN -->`／`<!-- ZW426-END -->`（**定界以整列相等為之**）。
落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字）。

<!-- ZW426-BEGIN -->

### 🩸 `自誤 426`　**出二態對拍批前未以其受詞（`WV_K6_STEP0=off`／`R4 right`／結構閘）全倉查 `GB` 簿，致重測 `GB-168` 已登記且已四度定位之現象**

**形**：`W-G.9-312` 單 `§零-2` 裁 `3` 以「假說」之名令跑乙態（`WV_K6_STEP0=off`），其 `E-1`〜`E-5` 之受詞即「該態下 `F.0` 之判與 `F.1`〜`F.4` 是否執行」；而 `docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-168` 節題逐字已載「**`WV_K6_STEP0=off`（＝ `K-9-29 二` 所令之態）下 `run_step_g` 於 `R4 right` 之結構閘「理論＝實跑」破 ⇒ 態乙之 `R3`／`R5`／`R6` ⛔ 可得**」，其受詞欄並逐字載本批乙態所復現之同一 `RuntimeError`（`理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）`），且明載「🔴 **其直接後果：`K-9-29` 子項 `1` 之碼側落地⛔ 為單純之旗標翻轉**——旗標一翻，管線即於 `R4 right` 中止」。

**徵候**：新窗於擬下一批（`W-G.9-313`）時，為查既有量測器而全倉搜 `理論@k*`，命中 `verify/probes/probe_WG9273_delta_locate.py`（其 docstring 逐字載「端點 B（破）＝ 態乙（`WV_K6_STEP0=off`）之 `R4 right`」）⇒ 循其單號查得 `W-G.9-273`／`274`／`277`／`278` 四批之既有工作：`Δ=2.425` 已收斂至「理論側與實跑側所餵之 `W` 相異」（`GB-168` 一次精化），並已定位至「拋出者係第二閘（`verify/stepg_pipeline.py:1471`）」與「`理論@k*` 餵 `14.01`（＝ `round(S × |cos|, 2)`）／實跑用 `W 14.1988975422`、`W_prev 0.1775004071`」（二次精化）。

**根因**：假說之受詞（步驟 0 之開關 × `F.0` 之驅動）與既有阻塞項之受詞（同一開關 × `run_step_g` 之中止）**重疊而未被辨識**——新窗於前一批（`W-G.9-311`）只以 `F.0`／`七級調配`／`錨檢破` 為鍵搜正典，**未以「開關之名」與「街廓·側」為鍵搜 `GB` 簿**；且 `恆常附款 b`（擬鑄之裁須全倉查既有之裁）之受詞僅及於「**裁**」，**未及於「量測」**。

**後果**：耗一施工批（二次 `run_all`）以復現既登記之現象；未動土地、未致錯裁、未致錯鑄。該批仍取得三項既有登記所無之量（甲態 `F.0` `GSA` 六鍵逐鍵無決策列／`抵費地0m(應空)` 之二態翻轉／`landeffect` 二態逐位相同），已另行登記於 `GB` 簿末端（`W-G.9-313` 工項二）。

**攔法（三款）**：
① 凡單令之**量測**，出單前以其受詞之關鍵語（開關名、街廓·側、閘名、錯誤訊息之特徵字）全倉查 `GB` 簿與既有報告，查得者於單內同格具名其與本批之關係（**復現／擴充／新題**）；
② 凡假說之受詞涉及既有阻塞項者，須讀該項**自其節題至檔末之全部末端追加**（精化、解除條件、其後各批之引用），⛔ 只讀其節題；
③ 診斷批之單須設「**既往同題之清單與其結論**」一節；該節為空者，須同格載其查法、母體與命中數。

<!-- ZW426-END -->

**落地後之機驗**：`probe_WG9267_issuer_measurers.py registry` ⇒ 自誤 相異 **`416`**／MAX **`426`**（缺號 `[106, 355, 356, 357]`）；`GB`／`VR`／`K-9` 一字未動；append-only：期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes；`CR` ＝ `0`。

---

## `§四`　工項二：**`W-G.9-312` 二態對拍結果之登記**（主線·單獨 `commit`·**末端追加**·⛔ 鑄號）

🔑 界：`<!-- GB168N-BEGIN -->`／`<!-- GB168N-END -->`（**定界以整列相等為之**）。
落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字·**⛔ 改 `GB-168` 原節與其二次精化任一字**）。
🛑 **本節⛔ 鑄任何號、⛔ 解除、⛔ 收窄任何 `GB`、⛔ 提修法主張**。

<!-- GB168N-BEGIN -->

## 🔧 `GB-168` 之現態復現 ＋ 其**射程外**之三項新量（`W-G.9-312` 二態對拍·`W-G.9-313` 工項二·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為末端追加（`戒 36`）；**⛔ 解除、⛔ 收窄 `GB-168`**（其解除條件四項合取**一字不動**）；⛔ 改 `GB-168` 原節、一次精化、二次精化任一字。
🩸 **本節之由**：`W-G.9-312`（發單側令之二態對拍）於出單前未查本簿，致重測 `GB-168` 已登記之現象——該疏失已鑄為 `自誤 426`（同批工項一）。本節只登記**其量**，⛔ 據以改任何判。

### 一　`GB-168` 之現態復現（態 `cb661f7`·`run_all` 二態各一次）

| 受詞 | 甲態（`WV_K6_STEP0` 未設） | 乙態（`=off`） |
|---|---|---|
| 落檔 | `verify/out/WG9312_runall_on.log`（`161089` B·`1029` 列） | `verify/out/WG9312_runall_off.log`（`36032` B·`326` 列） |
| 名目（框 ＝ 子字串框 `✅ PASS`／`🔴 FAIL`） | `24` ＋ `40` ＝ **`64`** | `12` ＋ `22` ＝ **`34`** |
| 結構閘「理論＝實跑」破（字樣命中） | **`0`** | **`5`**（`0m`／`3.5m` 二情境·`R4 right`） |
| 其逐字 | —— | `理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）` ⇒ **與 `GB-168` 受詞欄逐字相符** |
| `F.0` | `GSA` 錨檢破（見下 二） | `stepg_ctx[0m] 缺（trunk A 未成功？）`·`GSA` 命中 `0` |
| `F.1`〜`F.4` | 全跳過 | 全跳過 |

🔒 ⇒ **`GB-168` 現態 ＝ 🔴 未解除**，其四項解除條件**無一於本批成就**。

### 二　三項**射程外**之新量（⛔ 在 `GB-168` 既有登記內）

`(a)` **甲態之 `F.0` `GSA` 六鍵逐鍵無決策列**：`【未被評估】錨鍵 ['G006', 'G007', 'G009', 'G010', 'G014', 'G017']`，其決策現況逐鍵皆為 `[該情境無任何決策列]`（來源：`verify/out/WG9312_runall_on.log`·母體 ＝ 該落檔 `1029` 列·框 ＝ 其列框）。🛑 **其成因⛔ 於本批判**——乙態因 `GB-168` 而於 `F.0` 前中止 ⇒ 二態對拍**無從判定**該六鍵之去向（`W-G.9-312R` `E-8`）。
`(b)` **`抵費地0m(應空)` 之判於二態翻轉**：甲 **🔴 FAIL** ⇒ 乙 **✅ PASS**（二態落檔之同名名目·框 ＝ 名目框）。🛑 **只登記其翻轉之事實**，⛔ 判其何者為正、⛔ 據以主張任何改法。
`(c)` **`probe_WG9287_landeffect.py` 之輸出於二態逐位相同**：`verify/out/WG9312_landeffect_on.log` ＝ `WG9312_landeffect_off.log` ＝ `WG9311_landeffect.log`（皆 `86404` B·同一 `sha256`）⇒ **`GB-170` 三格之 `range_area`（`116.08`／`308.17`／`226.01`）與三對稱差（`13.055884224441684`／`38.06818855324997`／`13.05518976183967`）於二態不變**。🛑 **⛔ 讀為「`GB-170` 之受詞不受步驟 0 影響」之一般命題**——其僅為**該器之通道**於二態同值之實測。

### 三　通道之證（供後批引用·⛔ 結論）

`k6_step0_merge` 之呼叫點在 `app.py` 之 `_annotate_temp_parcel_cut_type` 內；該函式之呼叫點**唯二** ＝ `app.py:18342` 與 **`verify/selection_pipeline.py:253`**（母體 ＝ HEAD 之 `app.py` 與 `verify/` 之 `.py` `363` 檔·框 ＝ 子字串框）⇒ **二態對拍確作用於 harness 通道**，⛔ 空操作。

<!-- GB168N-END -->

**落地後之機驗**：`GB` 框之計數（器之輸出）⇒ `GB` 相異 **`168`**／MAX **`170`**／缺號 `[12, 87]` **一字未動**（本節⛔ 鑄號）；自誤 `416`／`426`；append-only：期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes；`CR` ＝ `0`；哨兵 `GB168N` 入檔後之全倉命中 ＝ 本單 ＋ `GB` 簿（逐筆具名）。

---

## `§五`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**；同格載受檢檔數 `> 0` |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動檔之 `CR`（倉內 blob·取 bytes） | ＝ `0`（本批只動 `.md`·⛔ 觸及 `verify/out/`）；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末（器之輸出） | 自誤 相異 **`416`**／MAX **`426`**；`GB` `168`／`170`·`K-9` `39`／`40`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（二受改檔） | 期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes（逐檔） |
| `6` | 自限復驗 | `verify/baselines` `298` 列·`a898f4e1…` 未動；`data/` 全樹未動；`CLAUDE.md`／`K-6`／`W-G.4` 域裁檔 blob 未動；**`verify/out/` `913` 檔逐位未動**；`refs/heads/verify/*` `20` 支·`gb170` `0`·`p3a` `rc` `1`；`WV_BAKE` ＝ `None`；`WV_K6_STEP0` ＝ `None` |
| `7` | 標籤對照（`自誤 416`） | 本報告全部計數標籤逐欄具名其集合與基數（**當場以程式自資料列計數**·⛔ 轉引單之自載數） |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`〜`6` 任一不如期 ⇒ **停、上呈**（照實全量出艙·⛔ 判改法）。

---

## `§六`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；`自誤 426` 之鑄；`GB` 簿末端之登記節（工項二）。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ `run_verification`；⛔ 跑 `probe_order_preflight.py`／`probe_WG9267_issuer_measurers.py` 以外之任何器；⛔ 改或新增任何探針一字；⛔ 觸及 `verify/out/` 任一落檔；⛔ 設定任何環境變數；
⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`；⛔ 判 `Δ=2.425` 之改法、⛔ 提修法主張、⛔ 擬任何 diff；⛔ 判「步驟 0 應否落地」；⛔ 落地任何 `K-9` 子項；⛔ 鑄 `自誤 426` 以外之任何號；
⛔ 開分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——遇可觀測之硬限事件一律 loud 照實具名、停、上呈；
🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**——凡含反引號之文字一律**以檔案管線**為之（`§一-1` 款 `h`·`W-G.9-312R` 自捕 `5`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§七`　報告之格

`docs/reports/W-G.9-313R_自誤426與312二態對拍之登記_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `10` 之自跑與二對照）⇒ `§三` 工項一（機驗）⇒ `§四` 工項二（機驗·含 `GB` 框未動之證）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其框、母體與產生指令；**計數標籤一律當場以程式自資料列計數**。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框六數與 `§零-3` 同源；自誤框於暫存副本模擬工項一落地 ⇒ `416`／`426`；`GB` 框於暫存副本模擬工項二落地 ⇒ `168`／`170` 未動。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`自誤 426`／`GB-168`／`GB-170`／`K-9-29`／`R4 right`／`WV_K6_STEP0`／`抵費地0m`／`landeffect`／`selection_pipeline`／`W-G.9-309`／`cb661f7`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: db189e4f1a66c0d84e197f1fdb73de3f9101b57a7a8f7ff13e21ad329bbe6152
