# W-G.9-240　五級條文入典 ＋ `GB-143` 補款 `③` 更正 ＋ 事後協調合併登記 ＋ 自誤二則

> **零生產碼**（`docs/` 之 `.md` append-only ＋ 新檔）⇒ **CC 得逕行 push**。
> ⛔ 動 `app.py`／`verify/**.py` 任何一字。動之即停機上呈。

---

## §零　開工閘（缺一即停）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，依既有口徑（自檔首至 `SELF_SHA256:`
該列之前一個 `\n`（含該 `\n`）為止之全部位元組）重算對拍。不符 ⇒ 停機。

**`S-1`（號占用·宣告框）**　依 `CLAUDE.md` 之「號占用閘之框改宣告框」①–④ 查 `W-G.9-240`。

發單側已於 `2026-09-05` 當場實測（**三軸**：來源 `blob@8c698bd525df4617120b0a0c697e56b7fca8d9df`
／母體 **全 `docs/` 553 檔**（其中 `19` 檔為 `.png`／`.jpg` 二進位而讀不到，且其檔名皆⛔ 含單號）
／粒度框 **檔框 ＋ 列框**）：

| 號 | 宣告框 檔／列 | 鬆框 檔／列 | 角色 |
|---|---|---|---|
| **`W-G.9-240`** | **0**／**0** | **0**／**0** | 受詢 ⇒ ✅ 未占用 |
| `W-G.9-237` | 6／9 | 7／43 | 對照組 甲（須 `≥ 1`）✅ |
| 🔴 `W-G.9-239` | **1**／**1** | 1／2 | **已由 CC 之 `docs/reports/W-G.9-239_CC交接文.md` 占用** |

🩸 **發單側之自陳**：本單原擬取號 `239`。發單側於前一次量測（`blob@a3e141c`）得 `239` 為 `0/0`，
**出單前重新 `git fetch` 並於新態 `8c698bd` 復算**，方發現 `239` 已被占用。
⇒ **`自誤 288` 所立之戒（⛔ 沿用前批之次號結論、須當場查）於本次施行即生效並攔下一次誤用。**

`S-1` 之驗收：CC 須**自行復算**上表三列，⛔ 採信本單任一值。任一列不符 ⇒ 停機上呈。

**`S-2`（append-only）**　本單受詞之既有三檔皆為**純末端追加**，⛔ 改其上文一字。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-240_五級條文入典與GB143補款更正.md`。⛔ 改一字。

---

## §二　工項一：**五級條文入典**（本單要害·有土地後果）

### `a`　受詞與體例

受詞 ＝ `docs/配地計算總規格_v3.md`，**純末端追加**一節，體例依 **`K-9-15` 先例**
（`docs/rulings/K-6_街角地分配程序與可分配判準.md:2808`）＝ **廢止舊款 ＋ 新款**，
**⛔ 就地改 `§7-1`／`§7-4` 本文一字**；並依本檔 `:648` 之 `§7-4 更正標記`同式，
以**字樣錨**（⛔ 行號錨）指認被廢止之款。

### `b`　入典之逐字（CC 原文追加·⛔ 改寫）

```markdown
## 🔧 §7-1 級1/2/3 與 §7-4 之**區位順序**改採「五級（八鍵詞典序）」（KL 裁 2026-09-05·canonical）

🛑 **本節⛔ 鑄任何號**。**⛔ 改 §7-1／§7-4 本文一字**——其本文之對應款依下 ① 廢止。

### ① 被廢止之款（字樣錨·⛔ 行號錨）

> **一**　§7-1 之「級 1 相鄰街廓同歸戶合併／級 2 鄰近街廓同歸戶合併／級 3 非鄰近街廓同歸戶合併」
> 三款**之「以距離定先後」之部分**廢止；其「同歸戶合併」之實質**不廢止**。
> 字樣錨：`grep -c "級 1 | 相鄰街廓同歸戶合併" docs/配地計算總規格_v3.md`
>
> **二**　§7-4 之「3 級順序：相鄰 → 鄰近 → 非鄰近（自暫定錨點起算距離）」一款，
> **就其 R（可建築）軌**廢止；**`RD`／`PF` 軌⛔ 廢止、維持距離優先**。
> 字樣錨：`grep -c "3 級順序：相鄰 → 鄰近 → 非鄰近" docs/配地計算總規格_v3.md`
>
> 🔒 **§7-4「排序：重劃前總地價高者先調配」一款⛔ 受本節影響**——其已由
> `W-G.9-232` 之更正標記記為「已由裁定K 取代為純距離優先」，本節⛔ 動之。
> 🔒 **§7-5 第2梯（增配）⛔ 受本節影響**——見下 ⑤。

### ② 新款：五級之定義與射程

**五級** ＝ 市地重劃實施辦法 §31 第二款後段（手冊編碼「**丙**」·小地主調整分配）
之**區位判準**，經 KL 裁定**推廣**為 §7-1 級1/2/3 與 §7-4（R 軌）之選塊準據。

🔒 **射程**（逐字·⛔ 擴張）
- **適用**：§7-1 級 1／2／3；§7-4 之 **R 軌**。
- **⛔ 適用**：§7-1 級 0／級 0'（同街廓·無選塊問題）；§7-4 之 `RD`／`PF` 軌；§7-5。

### ③ 八鍵詞典序（升序取名單）

對每一候選街廓 `T`（相對於待調配地所在之原街廓 `S`）：

```
key(T) = ( r1, r2, r3, r4, r5, r6, r7, r8 )

r1  使用分區    同分區 0 ／ 否 1
r2  最小建築面積 往小：級距 0,1,2…（次一級先）
                往大：排於全部往小者之後，再按級距
                🔴 第二趟排除往大者
r3  正面道路    相同 0 ／ 否 1     識別符 ＝ 幾何推導 ∪ 區外道路清單
r4  正面路寬    相同 0 ／ 否 1
r5  路寬級距    往小之級距
r6  深度        較淺者按 |Δ深度| 升序在前；較深者按 |Δ深度| 升序在後
                容差 ＝ 具名常數 DEPTH_TIE_TOL_M（⛔ 字面 0.5）
r7  距離        暫定錨點 → T（承裁定K 之既有距離準據）
r8  街廓 label  字典序（終局 tie-break·決定性）
```

🔒 **「次一級」＝ 往小**（最小建築面積往小；正面路寬由大往小）。
🔒 **`r2` 之「往大者」**：第一趟允許、第二趟排除；排法採**丙**
（往小者按級距排完，再往大者按級距排）。
🔒 **`r2` 走到底**：所有比 `S` 小的級都要試完。
🔒 **`r1` 同分區有多個時**：沿用 `r2`–`r6` 在其間分先後。
🔒 **`r3`–`r5` 為<u>排名條件</u>、⛔ 淘汰條件**——容量不足 ⇒ 往名單下一名試
（同 `v3:429` 既有之往下遞補）。
🔒 **`r3` 之識別符**：幾何推導**有值時使用者⛔ 覆寫**；區外道路清單**只填推導之空缺**。

### ④ 兩趟

- **第一趟**：沿名單找**同歸戶**合併——以 `a ＋ a'` 後算 `G`（**⛔ `g ＋ g'`**）。
- **第二趟**：第一趟全無 ⇒ 沿**同一名單**單獨調配（歸 §7-4）。

### ⑤ §7-5 第2梯（增配）之明文保留（KL 裁 2026-09-05）

🔒 **§7-5 第2梯⛔ 改採五級**，維持現行：
- **資格** ＝ §31-1-2 比較級：`深度淺於原街廓 ∪ 後價低於原街廓`（公設軌無原街廓 → 全塊·手冊(三)）
- **選塊** ＝ 最佳化 `min Σ(增配面積 × 目標後價)`
- **tie** ＝ Σ增配面積 → Σ質心起迄距 → 暫編字典序
- **配額** ＝ `max(G(a'), MinA)`；**增配** ＝ `max(0, MinA − G(a'))`；禁超額

🔒 **KL 之逐字理由**（`2026-09-05`）：「重劃後盡量能讓地主拿得到土地」
——最佳化之目標式即「合格街廓中總差額地價最少」，與該精神同向。

### ⑥ 法源與其界

🔒 **法定判準二軸**（辦法 §31 二後段逐字）：`深度較淺`（→ `r6`）、`重劃後地價較低`。
🔒 **`r1` 使用分區與 `r3` 正面道路<u>法源無明文</u>**——係 KL 於法定判準之上增設之優先序。
   ⛔ 稱其為法定要求；凡出艙須標其來源為 KL 裁定。
🔒 **`r4`／`r5` 與「重劃後地價較低」之關係**：路寬**非**後地價之同義；
   逐街廓後地價之真值存於 `post_price_by_block`（`grep -n "post_price_by_block = " app.py` ⇒ 1）。
   **本款⛔ 令 `r4`／`r5` 代理後地價**——二者並存，後地價之直讀屬 §7-5 資格式之受詞。

### ⑦ 「最小分配面積」與「最小建築面積」禁互代（重申）

🔒 二者**同名不同量**（`CLAUDE.md` 已正字）：
- **最小分配面積** ＝ 該街廓深度 × 該街廓 `min_width`（畸零地規則查表）——已實作
  （`grep -n "_min_alloc_area_by_blk\[_lbl\] = " app.py`；引擎側 `verify/wf_f0.py` 之 `_mina_by_block`）
- **最小建築面積** ＝ 都市計畫書所定（`K-9-1`）——**尚未實作**（見 §五 依賴序）
⇒ **`r2` 之受詞為<u>最小建築面積</u>**，⛔ 以最小分配面積代之。
```

### `c`　驗收

1. 追加後 `docs/配地計算總規格_v3.md` 之 `deletions ＝ 0`（`git diff --numstat`）。
2. 上開三個字樣錨各自 `grep -c` ⇒ 逐一報其命中數（⛔ 只報「有」）。
3. `grep -c "post_price_by_block = " app.py` ⇒ 報其數；≠ `1` 即停機上呈
   （發單側於 `8c698bd` 實測為 `1`·`app.py:19635`）。

---

## §三　工項二：`GB-143` 補款 `③` 之更正（**已被活體實測證偽之條文**）

### `a`　被更正之對象（逐字·CC 須當場自倉復現）

`docs/reports/W-G.4_泛用阻塞項登記表.md` 內，`GB-143` 補款之四條件表，其 `③` 現載：

> `stderr` 逐字理由（含命中樣式之**名**與**偏移**）＋ `stdout` 之 `permissionDecision = "deny"` JSON ＋ `exit 2`

字樣錨：`grep -c 'permissionDecision' docs/reports/W-G.4_泛用阻塞項登記表.md`
（發單側於 `8c698bd` 實測 ⇒ CC 須自行復算並報其數）。

### `b`　更正之逐字（**純末端追加**·⛔ 改上文一字）

```markdown
## 🔧 `GB-143` 補款之 `③` 之**機制更正**（`W-G.9-240`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為補款之更正。

🩸 **案由**　`W-G.9-237` 所落之 `③` 載「二法並用」（`stdout` deny JSON ＋ `exit 2`）。
`W-G.9-238` 之**活體攔截實測**證偽之：**`stdout` 之 deny JSON 覆蓋 `exit 2` 而其驗證未過
⇒ 回落放行**（隔離實驗：同一事件直餵 guard 得 `rc 2` ＋ 正確 deny JSON ⇒ guard 側正確；
與 CJK 案「僅 `stderr` ＋ `exit 2`」之唯一差即該 `stdout` JSON）。

🔒 **`③` 之正字（自本節起）**
> **攔截須 loud，且⛔ 二法並用**：一律**只走 `stderr` 逐字理由 ＋ `exit 2`**。
> **⛔ 於 `stdout` 輸出任何 `hookSpecificOutput`／`permissionDecision` JSON**——其會覆蓋
> `exit 2` 而回落放行，使閘**看似存在而實不生效**。

🔒 **`GB-143` 之定性更新**（`W-G.9-238R` 附 `1` 所請·本節成就之）
> 自 `W-G.9-238` 起，「硬閘」二字**有對應機制**——`PreToolUse` hook 之活體攔截已成立
> （含引入符之命令確被攔且 loud）。⛔ 再稱其為「已知無閘之禁令」。
> 🔒 **計數器維持 `3`**（⛔ 追改既出之數）。

🔒 **通則（自本節起）**
> `--selftest` 與契約測皆綠**⛔ 蘊含閘已生效**——`W-G.9-238` 之三缺陷中，
> 缺陷 `2`（`cp950` 誤攔 CJK）與缺陷 `3`（`stdout` 覆蓋）**唯有活體實測捕得**。
> ⇒ 凡宣稱某閘「已接線」者，須附**活體實測**之逐字，⛔ 以自檢綠代之。
```

### `c`　驗收

`deletions ＝ 0`；且追加後 `grep -c 'permissionDecision'` 之數**須恰增 1**
（新節內僅該一處提及）——CC 須報前後二數。

---

## §四　工項三：**事後協調合併須重算**之登記（KL 裁 2026-09-05·新項）

### `a`　案由與現況（CC 須當場復現二數）

- `docs/W-D.4_域裁鎖定.md` 已載：「先協調與他地主合併為共有；**協調不成（試分配預設不成）**
  → 增配」⇒ **初步成果⛔ 需協調輸入**，現行行為即 KL 所要者。
  字樣錨：`grep -c "協調不成（試分配預設不成）" docs/W-D.4_域裁鎖定.md`
- **「公告期間」於全倉命中 `0` 檔**（發單側於 `8c698bd` 實測·母體 ＝ 全倉追蹤檔）
  ⇒ 事後協調之機制**尚無任何登記**。CC 須自行復算此數；≠ `0` 即停機上呈。

### `b`　登記之逐字（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` 末端）

```markdown
## 🔒 待落地項登記：**分配成果公告後之「協調合併」須回頭重算**（`W-G.9-240`·KL 裁 2026-09-05）

🛑 **本節⛔ 鑄 `GB` 號**——其為**待落地項**、⛔ 阻塞項（現行行為無誤，係功能缺口）。

🔒 **KL 逐字要旨**
> 初步的調配後土地分配成果，先不用管誰跟誰協調；這是之後分配成果公告期間，
> 由使用者知道哪個土地所有權人要併進去哪位土地所有權人。
> **事後指定「A 併入 B」之後，需要回頭重算 `G` 與配地位置。**

🔒 **性質判定**：**引擎項·有土地後果**（⛔ 報表項）。
其連鎖：合併後 `a` 變 → `a'` 換算之目標街廓可能變 → `run_step_g` 須重解 →
`G` 變 → 配地位置與調配池帳連動 → 五級之選塊路徑須重走。

🔒 **依賴序**：**須在五級實作之後**——重跑會走到五級之選塊路徑；
於五級落地前實作重算，其重算結果即為舊準據之結果。

---

### 🔒 重算之**射程**：受影響街廓相依（affected-block dependency）（KL 裁 2026-09-05·canonical）

🛑 **⛔ 每接受一筆合併即自第一個街廓全區重跑一次**（KL 逐字排除）。

🔒 **KL 逐字要旨**
> `B`、`C` 是「全區層級」；`G` 是「地主／宗地層級」；實際配置是「街廓層級」。
> 先固定全區 `B`、`C`，只有分配位置發生變動的 `G` 重算，
> 再從「**最早受到影響的街廓／位次**」向後重新配置。

#### 層一　全區層：**恆不變**（⛔「通常不變」·係硬不變量）

| 量 | 判 | 構造理由（發單側於 `8c698bd` 自倉復現） |
|---|---|---|
| `B`（一般負擔係數） | **不變** | `calc_B_value(general_burden_area, price_before, price_after, total_area, preexisting_public_area)`——**五個入參皆全區聚合量、無一逐宗配置量**（`grep -n "def calc_B_value" app.py`） |
| `C`（費用負擔係數） | **不變** | `calc_C_value(engineering_cost, redev_cost, loan_interest, price_after, total_area, public_burden_total)`——同上（`grep -n "def calc_C_value" app.py`） |
| `MinA_區`（`mina_qu`） | **不變** | ＝ `min(mina.values())`，`mina[blk]` ＝ 街廓深度 × `min_width`，二者皆與配置無關 |

🔒 **⇒ 機檢（⛔ 人工判斷）**：重算前後 `B`、`C`、`mina_qu` **逐位相同**；任一變動 ⇒ **停機上呈**
（其變動即證射程判定或輸入有誤）。

#### 層二　街廓層：受影響集 ＝ 種子之**傳遞閉包**

```
種子  S0 = { A 之原街廓 , B 之目標街廓 }
傳遞  若 X ∈ S 且 X 之調配池量改變，
      則凡「曾自 X 之池取得調配」或「曾調配入 X」之街廓 Y ∈ S
收斂  迭代至不動點
```

🔒 **傳遞邊之資料源**：**調配帳**（⛔ 幾何鄰接·⛔ 距離）——池之相依由**帳上實際發生之調配**決定。
🔒 **構造依據**：「**抵費地係配餘**——抵費地為該街廓原位次配地完成後**剩下的**街廓空間」
（`grep -c "抵費地係配餘" app.py`）⇒ 街廓內任一變動必改該街廓之池量 ⇒ 池之下游必受影響。

#### 層三　宗地層：自**最早受影響位次** `p*` 向後

於每一受影響街廓內：

```
p* = min{ 投影序(宗) | 該宗之 (a ／ 位次 ／ 街角身分 ／ 臨路條件) 任一改變 }
位次 <  p*  ⇒ ⛔ 重算（其 G 由構造不變）
位次 ≥  p*  ⇒ 重算 G 與幾何配置
```

🔒 **「位次」之單一真相源** ＝ `_projection_order`（宗地沿 `FRONT_LINE` 之投影順序·`W-D.2 §2`；
`grep -c "_projection_order" app.py` ⇒ 發單側於 `8c698bd` 實測 **11**）。
🔒 **`p*` 之後必變之機制** ＝ **累積 `S`**：`G` 式之 `S` 係沿分配序累積
（`grep -c "_alloc_cum_S" app.py` ⇒ 發單側實測 **5**）⇒ 變動點之後各宗之 `S` 必位移 ⇒ `G` 必變。
⇒ 🔒 **KL 所稱「同街廓其他地主 G 原則不一定」，於本碼庫可<u>機械判定</u>**：
以 `p*` 為界，**⛔ 逐案人工判斷**。

🔒 **街角之例外已內含於 `p*` 之定義**——若合併使某宗之街角身分或臨路條件改變
（`Rw`／`F` 之受詞變動），該宗即落入 `p*` 之取值集，`p*` 自動下修。

#### 驗收（三閘·皆機檢）

| 閘 | 判準 |
|---|---|
| `G-1` | `B`／`C`／`mina_qu` 重算前後逐位相同 |
| `G-2` | 未重算之宗（位次 `< p*`）其 `累積S(m)` 與 `G` 逐位不變——**若變即射程判定錯** |
| `G-3` | 每一受影響街廓滿足既有守恆式 `ΣG + 調配池 = 街廓 DXF 面積`（正典 `K-6`·`grep -c` ⇒ **1**） |

#### 🔒 `§7-5` 第2梯之窮舉母體（KL 裁 2026-09-05·**已逐字閱後點頭確認**）

> 若重算使某歸戶落入 `§7-5` 第2梯（增配），該梯之最佳化係**全域窮舉**。
> 依本裁「⛔ 全區重跑」之精神：
> **窮舉母體 ＝ 受影響集之池**；
> **已公告而未受影響者之配置<u>鎖定為約束</u>**（⛔ 進入窮舉、⛔ 被翻動）。

🔒 **稱謂義務（`VR-091` 族·⛔ 隱去）**：本款之**條文文字係發單側所擬**，
KL 於 `2026-09-05` 逐字閱後**點頭確認** ⇒ **自本批起為 KL 裁**，非發單側之推導。
⛔ 於下游文件轉述為「claude.ai 之建議」。

🔒 **機檢**：窮舉之候選集基數 `≤` 受影響集之塊數；且**鎖定宗**於重算前後
其 `所屬街廓`／`累積S(m)`／`G` 三欄逐位不變（違之即證約束未生效 ⇒ 停機）。
```

---

## §五　工項四：自誤二則（**分鑄**·號須 CC 當場復算）

🔒 **末號之獨立復算**（框 ＝ `VR-091` 補款二 ① 之正字定義框 `A` ＋ 範圍框；
母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔；粒度框 列框）。
發單側於 `8c698bd` 實測 ⇒ 相異 `282`／`MAX` `289`／缺號 `[106]`
⇒ **`N1` ＝ `290`、`N2` ＝ `291`**。**CC 須自行復算；不符即停機上呈。**

### `自誤 N1`｜**問了倉內已有答案之題**（發單側·域問之必要性未先窮查）

🩸 **案由**　發單側於本窗出艙一題予 KL：「五級之『深度較淺』與『重劃後地價較低』
二軸之連接詞採『或』、『且』抑或詞典序？」
KL 答「a（或）」。**而該讀法早已是碼側現行行為**：
`verify/wf_f4.py` 之 E2 資格式逐字 `深度淺於原街廓 ∪ 後價低於原街廓`。
發單側出題前**未以「比較級」「增配」「§31-1-2」等同義詞查倉**。

🔒 **受詞** ＝ **問題本身之必要性**（⛔ 問題之內容）。
🔒 **與 `自誤 290 之前`同族者**：交接文 `§六` 所列「憑設想給錨」族——
本則之錨 ＝「此題未有答案」，該斷言未經實測。
🔒 **後果**：耗 KL 一次域裁往返；**土地後果 ＝ 零**（其答與現行碼一致 ⇒ 零變更）。
🔒 **通則（自本則起）**
> 凡擬向 KL 出艙之域問，出艙前須以**至少三組同義詞**查 `verify/**.py` ∪ 全 `docs/`，
> 並於出艙時**同格載明所查之同義詞集與命中數**；⛔ 只查 `docs/` 而不查碼側。

**責** ＝ **發單側**。

### `自誤 N2`｜**誤述二碼區之關係**——稱不同階段之二機制為「同一條法規的兩種設計」

🩸 **案由**　發單側於本窗出艙一對照表，將「交接文 `§四` 之八鍵詞典序」與
「引擎現行之 `min Σ(增配面積×目標後價)` 最佳化」並列為**同一條法規之二設計**，
並據以請 KL 二擇一。**該關係為偽**：

| 機制 | 落點 | 受詞 |
|---|---|---|
| 八鍵詞典序（擬） | `§7-1` 級1/2/3、`§7-4` | 尚能按自身 `G(a')` 配地者之**選塊** |
| 最佳化（現行） | `§7-5` 第2梯 | `ΣG ≥ ½ 區標準`而 E1 無塊可配者之**增配** |

⇒ 二者**階段不同、受詞不同、⛔ 互斥**，可各行其是（KL 最終即如此裁）。

🔒 **受詞** ＝ **既有碼區之性質**（交接文 `§六` 第 `6` 次之同受詞·**再犯**）。
🔒 **發現方式**：**KL 之反問**（「這是針對已經調配後仍未達可分配的土地嗎？」）
——⛔ 發單側自捕。🔴 **此為加重情狀**：`§六` 已逐字警告該族，而本窗仍以之誤導 KL 一次選擇。
🔒 **通則（自本則起）**
> 凡出艙「二機制之對照表」，須於同表載明**各自之規格節次**（如 `v3:§7-4` / `v3:§7-5`）；
> ⛔ 以「同一條法規」為欄名而不指其節次。

**責** ＝ **發單側**。

---

## §六　🔴 五級**實作**之依賴序（本單⛔ 實作·僅登記）

發單側於 `8c698bd` 逐鍵實測其資料源：

| 鍵 | 資料源 | 狀態 | 錨 |
|---|---|---|---|
| `r1` 使用分區 | 街廓分類（15 項使用分區·UI 可改） | ✅ 有 | `grep -n "15 項使用分區" app.py` |
| `r2` 最小建築面積 | `K-9-1` 之 UI 欄 | 🔴 **未落地** | `app.py` 內「最小建築面積」命中 **1**，且該列為**註解**（`K-9-1` 說明）；`min_build_area` 僅見於 `docs/specs/W-G.5_裁定N_乙案_plan_v1／v2.md` |
| `r3` 正面道路識別符 | 幾何推導 ∪ **區外道路清單** | 🔴 **未落地** | `app.py` 內「區外道路」命中 **0** |
| `r4` 正面路寬 | `front_width` 逐街廓（UI·`step=0.5`·預設 `8.0`） | ✅ 有 | `grep -n "front_width" app.py` ⇒ **5** |
| `r5` 路寬級距 | `get_min_lot_size(category, front_road_width_m)` | ✅ 有 | `grep -n "def get_min_lot_size" app.py` |
| `r6` 深度 | `_depth_use_by_blk`／UI 覆寫 `f3L_depth_ov_{label}` | ✅ 有 | `grep -n "_depth_use_by_blk\[_lbl\] = " app.py` |
| `r7` 距離 | 質心起迄法 | ✅ 有 | `verify/wf_f4.py` 之「距離＝KL 質心起迄法」 |
| `r8` label | — | ✅ | — |
| **可替換點** | 排序準據之 seam | 🔴 **無** | 相鄰／鄰近／非鄰近內嵌於 `wf_f4`（`級1相鄰`／`級2鄰近`／`級3非鄰近` 之 `LV` 表） |

🔒 **⇒ 依賴序（發單側之建議·候 KL 排序）**
```
1. K-9-1 最小建築面積 UI 欄落地        （r2 之前提·有土地後果）
2. 區外道路清單 ＋ 正面道路識別符暴露   （r3 之前提·有土地後果）
3. 排序準據之可替換點（seam）          （純技術·無土地後果）
4. 五級實作                            （r1–r8 齊備後）
5. 事後協調合併重算                    （須在 4 之後）
```

⛔ **CC 於本單實作上列任一項。**本節僅為登記與上呈。

---

## §七　產出與 push

- **零生產碼** ⇒ CC **得逕行 push**，⛔ 待授權。
- 報告 ＝ `docs/reports/W-G.9-240R_五級條文入典與GB143補款更正_執行報告.md`，
  須逐項載 `deletions`、字樣錨命中數（前後二數）、`S-1` 三列之自行復算值、
  `N1`／`N2` 之自行復算值。
- 🔒 凡出艙任何命中數，須同格載三軸（來源 commit ／ 母體 ／ 粒度框·`VR-091`）。

SELF_SHA256: a51a7b66c6335ba8e2b4db7bf953546ae183b1fd70b0dd2b40c1dde10e882559
