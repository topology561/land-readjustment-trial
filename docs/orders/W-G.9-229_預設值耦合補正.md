W-G.9-229 :19720 之預設值耦合補正（生產碼·單一 commit·須 KL 放行後方得進主線）

【工項零：本單原封入倉（優先於一切工項）】
 原封存入 docs/orders/W-G.9-229_預設值耦合補正.md，⛔ 改一字、⛔ 改行尾。
 驗：入倉 blob 內容 sha256 ＝ 落檔前來源 sha256。不過即停。

【性質】含生產碼。CC 得完成實作與自驗並回報，
 ⛔ 未經 KL 逐字放行即推入主線；⛔ 與任何零生產碼批混入同一 commit。
【承接】W-G.9-228 全數受理。發單側已自倉獨立復現：
 _wf_tag_of 定義處唯一、呼叫處唯一且在獨立按鈕內（非 Step G 主路徑）。
 GB-150 待 KL 覆「七級調配是否每案須能跑」，本單⛔ 觸。

【⛔ 絕對禁項】
 ⛔ 改任何計算邏輯、⛔ 改任何數值行為（本批係純重構）。
 ⛔ 動 _wf_tag_of 及其呼叫點（GB-150 之射程，待 KL）。
 ⛔ 動 :22395／:22884 之 .get 預設（其在別段·見工項一 c）。
 ⛔ 硬寫街廓名、側別標籤、地號。
 ⛔ 採用本單所載之任何檔列為錨——全數當場復現，不符即停。
 ⛔ heredoc（GB-143 補款二）；⛔ 工作樹位元組作錨（GB-139）。

【前置閘】
 G1 態錨：HEAD／left-right／生產碼五檔逐檔內容 sha256（改前值）。
 G2 依 W-G.9-228 所立之**標記法新框**重跑，須相符；不符即停。
 G3 當場出艙顯示端常數與輸入欄字面之現態檔列與逐字，
  並證二者現值相同（不同即停——那是既有實害，須先上呈）。

【工項一：耦合補正（最小形）】
 a. 使輸入欄之預設值改讀顯示端所定之具名常數，⛔ 保留其字面。
  範圍限於該二處；⛔ 順手擴及他處。
 b. 驗證 Python 作用域確可及（顯示端在 with st.expander 之外、輸入欄在其內；
  with 非函式作用域）——以實測坐實，⛔ 以推理充證。
 c. 🔴 :22395／:22884 之 .get 預設**本批⛔ 動**：
  其位於不同區段、不同函式脈絡，且與顯示端無同一渲染輪次之耦合。
  出艙其現況與「何以⛔ 於本批併辦」之理由，列入通用化波
  （集中為單一具名常數之全面重構）。
 d. 出艙補正後之 3.5 字面清單（載三軸與粒度框），與補正前對照，
  證取值用之字面減少恰 1 且無他處變動。

【工項二：自驗（⛔ 以自報充驗收）】
 a. 三情境實測：首次渲染（key 未存在）／輸入 0／輸入其他值。
  每情境出艙：畫面顯示之值與來源、輸入欄之初值、下游逐入口實收之值。
  三者須一致；任一不一致即停。
 b. 🔴 分歧偵測之實測（本項要害）：
  暫時將顯示端常數改為一異於 3.5 之值（如 9.9），實測輸入欄之初值**隨之改變**
  ⇒ 證二者確已綁定，⛔ 只證「現值相同」（現值相同在補正前即成立，無判別力）。
  實測後還原，並驗還原後之 blob 與補正後逐位相同。
 c. 迴歸：出艙改動前後之行為對照。
  🔴 界限之載明：R4 逐值迴歸不可得（兩情境各有未決閘——0 m 之 GB-67、
   3.5 m 之 telescoping；見 W-G.9-228 工項五之二鏈）。
  替代證據 ＝ 行為逐位相同（含同點 raise），須逐字載明其為替代證據，
  ⛔ 讀為「已證 R4 逐值不變」。
 d. 出艙改動之全部檔列（逐檔逐行 numstat），deletions 逐行具名理由。

【驗收】
 全以 `git cat-file blob` 復驗；推後回報下列各項，缺一即為驗收不過：
 ・工項零之入倉 `sha256` 逐位相同
 ・`G1`／`G2`／`G3` 逐閘實測
 ・工項一 `a`–`d`（含 `b` 之作用域實測、`d` 之前後對照）
 ・工項二 `a` 三情境表、`b` 之分歧偵測實測與還原後 blob 逐位相同、`c` 之界限載明、`d` 之 numstat
 ・改前後生產碼五檔逐檔 `sha256`
 ・`left-right`
 ・`deletions`（逐行具名理由）
 ・改動摘要（何處、幾行、為何）

【回報後之處置】
 完成【驗收】所列後停，候 KL 逐字放行方得推入主線。⛔ 於放行前推入主線。
【停與升】G2 不符、G3 二者現值不同、工項一 b 作用域不可及、
 工項二 b 分歧偵測不成立、或觸真域邊界時停；
 純技術項自主 plan→reviewer→implement→push（push 須待放行）。
