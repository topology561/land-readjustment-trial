> ⛔ **作廢——見 `W-G.9-190R`**（`W-G.9-190R §零` 逐字：「**廢止**：`W-G.9-190`（⛔ 不得執行·已知含 `P-3`／`P-4` 之缺，另含 `六-3` 之偽停機款）」）
> 🛑 **本檔保留之目的 ＝ 作為 `verify/probes/probe_order_preflight.py --selftest` 之靶**（`P-3` 須紅／`P-4` 須提示）；⛔ **不得執行其任何指示**。
> ⚠️ **本加註使本檔之 `SELF_SHA256` 自此⛔ 不再相符**——係**刻意**（`W-G.9-190R §九（乙）2.` 所令），⛔ 非竄改；其**反成 `P-5` 之活體正對照**（證 `P-5` ⛔ 非恆綠）。

# 🔧 施工單 `W-G.9-190`　族②③ 之切換（`W-G.9-5` 之落地）

> 發單側：`claude.ai`（`W-G.9-188` 窗）　受單側：CC
> 前批：`W-G.9-189R`（🛑 停機·已驗收·`25857aa`）　基座：`25857aa`
> 🛑 **本批會改變配地結果** ⇒ `X-T` **⛔ 不適用**。
> 🔴 **⛔ 不含遞補**——遞補順延 `W-G.9-191`。本批一字不得寫遞補。

---

## §零　開工閘（🛑 動任何一字之前逐項辦畢；任一不過 ⇒ 停機上呈）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，自算對拍。不符 ⇒ 停機。

**`S-1`（期初錨·全部現查·⛔ 不得照抄本表）**

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1378708` | `23412` | `2be36e4c88b6e129` |
| `CLAUDE.md` | `81200` | `903` | `3df01894ba38ccc5` |
| `verify/stepg_pipeline.py` | `96261` | `1454` | `0260273350021311` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `402254` | `4347` | `82084b330ff89b4d` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `529494` | `3238` | `499ce534e43f332e` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `258295` | `3632` | `6a5cada2901a10e8` |
| `docs/reports/W-G.9-189R_停機_S6複刻區實質差.md` | `14261` | `221` | `4135a6244adaf3d9` |
| `verify/probes/probe_WG9189_s6_replica.py` | `12399` | `280` | `c22b2faa38968505` |

`sha16` ＝ `sha256` 前 `16` 碼。`app.py` 檔尾**無 `LF`**（末位元組 `)`）⇒ `wc -l` 得 `23411`。

**`S-2`（倉態）**　`git ls-remote origin refs/heads/wip/s1-endpart`：
`= 25857aa` 或其後裔 ⇒ 正常開工；為其**祖先** ⇒ 🛑 停機。

**`S-3`（拋棄式 clone）**　跑畢整份刪除，回報中以對照組確認量測器非紅。

**`S-4`（禁改清單）**　讀 `§五`。

**`S-5`（基準量·🛑 動任何一字之前）**　落檔 `verify/out/probe_WG9190_baseline_25857aa.log`（🔒 檔名載**真實基座**），內含：

1. 逐街廓 `run_step_g` 之 `[T2-DIAG]` 全文；
2. 🔑 **`R2` 之三量期初值**（族② 之預測受詞·【倉】`W-G.9-177R §四-4`）：**池帶2 寬**、**退化帶數**、**`merged` 段數**——各載 app 側與 shim 側二值；
3. `[T3-GATE]` 逐宗列（框 `` ^\[T3-GATE\] 街廓 ``·期初應得 `39`）。

**`S-6`（複刻區同步閘·**本批用二分版·作為量測器**）**

🛑 本批 `S-6` **⛔ 不改四分**——四分之建置係 `N-3`，須在切換**之後**。開工時以現行 `verify/probes/probe_WG9189_s6_replica.py` 原樣跑一次，須逐項復現 `W-G.9-189R`：四區判 `True`／`True`／`False`／`False`、**實質差 `20` 處**、判別力造二問皆 `False`、`rc=1`。不復現 ⇒ 🛑 停機。

🔒 **`ast.dump` 之 bytes ⛔ 非跨環境錨**。發單側於 `CPython 3.12.3` 復現時得
`4569`／`1344`／`24415`／`21163`／`76871`／`97486`，而倉內 log 載
`4353`／`1293`／`23821`／`20641`／`74569`／`94370`——**判定（全等 `True`/`False`）與實質差 `20` 處逐項相同，僅 dump bytes 相異**，係直譯器版本差。⇒ 復現時**只對拍判定與差數**，⛔ 不以 dump bytes 判紅；並見 `N-4 5.`。

---

## §一　任務（🔒 **三個 commit·⛔ 不得併為一個**——併之則歸因不能）

### `N-1`（commit 1）　族② 之切換

**受詞**：`app.py:20990-20991`

```python
_W_prev_left  = (_left_buffer_S  * _cos_dn) if _has_left_corner  else 0.0
_W_prev_right = (_right_buffer_S * _cos_dn) if _has_right_corner else 0.0
```

改為與 `verify/stepg_pipeline.py:628-629` 一致之新式（初值 `0.0`）。

**正典依據**（`CLAUDE.md:758` 逐字轉引）：`mp` 之定義由三級權威**皆作 SIDE 線之中點**；**舊式 ＝ 新式 − `W_0`**，相等**當且僅當 `W_0 = 0`**，而正典明載其**照實可負**。`CLAUDE.md:759` 逐字：⛔ **非待裁項、⛔ 非失效**，係**已裁而未遷移** ⇒ ⛔ 不上呈 KL。

🛑 **⛔ 不得順手搬移 `stegp` 之 `_W0_*`／`_Wfirst_*`**——彼係 harness 獨有之 telescoping 守備（`W-G.9-189R` 類丙），本批**⛔ 不動**。

**主閘 `[T5-FAM2]`**（逐街廓一列·框 `` ^\[T5-FAM2\] ``）：切換前後之 `R2` 三量並列。

🔴 **判準之性質**（`CLAUDE.md:761` 逐字：**預測·⛔ 非結論**）：
- 三量**同時收斂**至 shim 值（池帶2 寬 `32.9640 → 33.0811`、退化帶數 `2 → 3`、`merged` 段數 `3 → 4`）⇒ ✅ 預測坐實。
- **⛔ 未收斂**（含只收斂其中一或二量）⇒ 🛑 **停機上呈**，⛔ 不得自行判為「切換失敗」或回退——未收斂之意義是**分歧之模型不完整**，須由發單側裁。

🔑 **附閘（機械·⛔ 不可省）**：切換後原樣重跑 `probe_WG9189_s6_replica.py`，**實質差須由 `20` 降為 `19`**，且消失者須恰為清單**第 `1` 項**（`_W_prev_*` 初值）。降幅 ≠ 1 或消失者非第 `1` 項 ⇒ 🛑 停機。

### `N-2`（commit 2）　族③ 之切換

**受詞**：`app.py:21265` 與 `:21267` 之 `'b': _left_buffer_S * _cos_dn` ／ `'b': _right_buffer_S * _cos_dn`（`_select_pool_slot` 之 `'b'`），改為與 `stepg` 一致之新式。

🛑 `N-1` **未全項通過前⛔ 不得開始 `N-2`**。

【倉】載族③ 於 `R2` 實測**零後果**（`CLAUDE.md:760`）⇒ 本批之預測為「`R2` 三量**⛔ 不再變動**」。若竟變動 ⇒ 🛑 停機上呈（同 `N-1` 之性質，係模型不完整、⛔ 非失敗）。

出艙 `[T5-FAM3]`（同 `[T5-FAM2]` 之欄位）。

### `N-3`（commit 3）　`S-6` 四分分類器（零生產碼·`W-G.9-188` 裁 一）

於 `probe_WG9189_s6_replica.py` **末端追加**（⛔ 不改既有判定路徑）四分分類，規則**機檢可判、⛔ 不得逐案裁量**：

| 類 | 判準 | 處置 |
|---|---|---|
| **乙** 宿主介面差 | **封閉白名單**：`st.*`／`ss[...]`／`raise RuntimeError` ↔ `st.error`+`st.stop`。白名單外之任一識別符 ⇒ **歸甲** | 略去 |
| **丙** 單側獨有守備 | AST **def-use** 機檢：獨有碼若**寫回**任一為共用流程所消費之變數 ⇒ **歸甲** | 略去 |
| **丁** 呼叫序差 | 僅**無裝飾器**之 `FunctionDef`／`ClassDef` 之重排得豁免；其餘 ⇒ **歸甲** | 略去 |
| **甲** 其餘 | — | 🛑 **停機款** |

🔑 **四分自身之判別力造**（⛔ 不可省）：於白名單區內注入一已知實質差（如將某 `st.error(...)` 之引數改為一真實運算），須**仍判甲**。恆判乙丙 ⇒ 分類器紅、⛔ 不得入倉。

**期末預期**：`N-1`／`N-2` 落地後之 `19` 處，經四分後之**類甲數**應為 `0`（族② 已切換、餘者皆乙丙丁）。⛔ 若仍有類甲 ⇒ 逐項具名呈裁，⛔ 不自行豁免、⛔ 不改白名單以求綠。

### `N-4`（隨單搭載·純登記·⛔ 零生產碼）

1. 🔒 **鑄 `自誤 224`**（次號現查：全倉 `.md`·框 `` ^###\s*自誤\s*\`?([0-9]{1,4})\` ``·MAX ＝ `223`）
   受詞逐字：**「已在手之檔只讀其一部分，即斷言其全檔查無。」**
   案由：`W-G.9-188` 窗裁 `A` 斷言「CC 所引在倉內查無」，實則在
   `docs/reports/W-G.9-186R_CC交接文.md:156`／`:245`／`:246`；該檔正係發單側自己於同窗發現並回報之新增檔，僅讀前 `90` 列即出艙全稱否定。
2. 🔒 **鑄 `自誤 225`**（承上取次號）
   受詞逐字：**「以裸框（無 `^` 錨）之得數為總數出艙，並據以否定受單側之正數。」**
   案由：裁 `三` 報 `[T3-GATE]` 逐宗列 `57`（裸框），錨定框得 `39`，差 `18` ＝ 以 `    ▸ ` 起首、截至 `150` 字元之摘錄重列。同族：`VR` 裸框 `999`／`自誤` 裸框 `298`（`CLAUDE.md:671`）⇒ **裸框族第三度**，觸 `戒 32` 升階款及 `CLAUDE.md:725`。
   🔒 併記：`57 = 39 過 + 18 不過` 之分解 **⛔ 不成立**；`39` 之分佈為不配地 `18`／配地 `13`／不適用 `8`。
   🔒 併記：受單側**二度**於前提為偽時拒絕照裁改倉、改以現查回報 ⇒ 記明為正辦。
   ⚠️ 二則之標題形依 `常規四（九）四` **取既有最末一則之標題形逐字為範本**，⛔ 不憑本單之寫法。
3. 🔧 `W-G.9-187R` `:368`／`:485`：**維持 `39`**，補宣告框 `` ^\[T3-GATE\] 街廓 ``，並依 `CLAUDE.md:725` 加註「裸框 `57`／錨定框 `39`，差 `18` ＝ 摘錄重列」。⛔ 不鑄號。
4. 🔧 `verify/wf_f0.py:9` 逐字「解算一律走 **app 真函式** `run_step_g`」係偽述（`run_step_g` 定義於 `verify/stepg_pipeline.py:238`）⇒ 就地更正。⛔ 不鑄號。
5. 🔧 `CLAUDE.md:757` 之行號已腐（`+490`）⇒ **末端追加字樣錨**
   `` `_W_prev_left = (_left_buffer_S * _cos_dn)` ``、`` `'b': _left_buffer_S * _cos_dn` ``，
   ⛔ 不用行號、⛔ 不追改上文一字。併記本批已將二者切換。
6. 🔧 `probe_WG9189_s6_replica.py` 之 `ast.dump` bytes 須**標框直譯器版本**（出艙列加載 `platform.python_version()`），並於報告載明其**⛔ 非跨環境錨**。⛔ 不鑄號。

---

## §二　授權與 `push`

🔴 本批**有生產碼**且**改變配地結果** ⇒ **`push` 前一律請示**，⛔ 無自辦款。
`W-G.9-186R CC交接文:246` 照舊適用。請示時併提：`§四` 全項、`[T5-FAM2]`／`[T5-FAM3]` 全文、`S-6` 前後之實質差數、`numstat` 逐 commit 逐檔。

---

## §三　停機款（任一觸發 ⇒ 🛑 停機上呈，⛔ 不先辦後呈）

- **`X-1`**　`S-6` 開工復現不符（四區判／`20` 處／判別力造／`rc=1` 任一不符）。
- **`X-2`**　`N-1` 後 `R2` 三量**未同時收斂**（含部分收斂）。
- **`X-3`**　`N-1` 後 `S-6` 實質差降幅 ≠ `1`，或消失者非清單第 `1` 項。
- **`X-4`**　`N-2` 後 `R2` 三量竟再變動。
- **`X-5`**　`[T2-DIAG]` 出現**非** `R2` 街廓之變動（族②③ 之受詞僅及 `_W_prev` 初值與池槽 `'b'`；他街廓變動 ⇒ 射程外）。
- **`X-6`**　`run_all` 任一 fixture 由綠轉紅且經實測基座態確認係本批所致。
- **`X-7`**　四分分類器之判別力造**未判甲**。
- **`X-8`**　`deletions` 出現於基座檔（`app.py`／`CLAUDE.md`／`stepg_pipeline.py`／各登記表）。
- **`X-9`**　任一取號之次號，其**錨定框**得數 ≠ `0`（⛔ 不得以裸框判之）。

---

## §四　收工閘

| # | 項 |
|---|---|
| `1` | `S-0`〜`S-6` 全過，`S-6` 開工復現逐項相符（dump bytes 除外） |
| `2` | `[T5-FAM2]`：`R2` 三量同時收斂至 shim 值 |
| `3` | `N-1` 後 `S-6` 實質差 `20 → 19`，消失者恰為第 `1` 項 |
| `4` | `[T5-FAM3]`：`R2` 三量⛔ 未再變動 |
| `5` | `[T3-GATE]` 期末仍為 `39` 列（框 `` ^\[T3-GATE\] 街廓 ``）；若不為 `39` ⇒ 逐宗具名其變動並呈裁（⛔ 非自動紅——族②③ 會改幾何、本即可能改判） |
| `6` | 四分分類器已入倉、判別力造判甲、`19` 處之類甲數已出艙 |
| `7` | `N-4` 六項全辦；`自誤 224`／`225` 之標題形取自既有最末一則 |
| `8` | `run_all` 全綠，或紅項經實測基座態證非本批所致 |
| `9` | 取號現查：四類之**錨定框** MAX 與次號命中 `0`，逐類載其框逐字 |
| `10` | 三 commit 分立，`numstat` 逐 commit 逐檔已備，基座檔 `deletions = 0` |
| `11` | 倉內報告：`docs/reports/W-G.9-190R_族二三切換.md` |

---

## §五　禁改清單（⛔ 一字不改）

1. `verify/run_all.py`、`verify/run_verification.py` 之發現機制與名目集合。
2. `docs/rulings/K-6_...md` 之任何既有條文。
3. `verify/stepg_pipeline.py` **全檔**——本批係**令 app 對齊 harness**，⛔ 非反向。
4. `W-G.9-187R` 已落地之 `_rect_fit_*`／`_k923_*`／`_blue_shadow_tri`／`_t3_*` 之函式體。
5. `probe_WG9189_s6_replica.py` 之**既有判定路徑**（僅得末端追加四分與版本標框）。
6. `app.py` 之 `minimum_rotated_rectangle` 全部 `11` 處。
7. 任何既有 `GB`／`VR`／`自誤`／`戒` 之既有文字（僅得末端追加）。

---

## §六　背景（⛔ 非指令）

`W-G.9-189` 之 `S-6` 證明：迴圈層之複本除**一個起始值**外逐句同步——遞補所在之二迴圈，其遞推式 `_W_prev_left = float(res.get('W_far', _W_prev_left))` 在 `app:21097` 與 `stepg:676` **逐位相同**（發單側已現查）。污染源唯族②，且**在迴圈之外**。本批即拔除該源，使 `191` 之遞補得以在同一基準上施工並驗收。

`W-G.9-187R` 已判 `18` 筆不可配地（`R2左 8`／`R5左 4`／`R3右 3`／`R6右 2`／`R1右 1`），
「假綠」`3` 筆落於 `R1右`／`R3右`／`R6右`——`191` 之遞補即處理該 `18` 筆。

---

## §七　回報

經請示核可後 `push`，單獨跑 `git log -1 --format="%h %s"` 原樣貼其輸出 ＋ 倉內報告路徑。
凡呈發單側之裁列於報告 `§九`。**凡數，逐項載其母體與框**；裸框與錨定框相異者依 `CLAUDE.md:725` 二數並報。

---

SELF_SHA256: a5db7634127f960bd072bc1ba2a6dfb22ede5ae9470109c67b2ddb5e24f2cfa4
