# `W-G.9-330`　輕量單：`K-9-45` 之載入（KL `2026-09-22` 之裁及其補充）＋ `GB-170` 之解除 ＋ `GB-174` 受詞之改定 ＋ `GB-175`〜`177` ＋ `自誤 485`〜`488` ＋ 發單側量測器四支與 KL 參考簡報入倉

> **級** ＝ **輕**（三簿／典之末端追加 ＋ 五新檔 ＋ 一二進位檔·**零生產碼**·⛔ 跑 harness·⛔ 寫 `verify/baselines`）。
> **開工態** ＝ `77e3ede4b351027513e6f8ac2587ba002b120369`（主線 `wip/s1-endpart`）。**受詞** ＝ CC 施工窗（續 `W-G.9-328`）。
> 🔑 本單之一切呼叫形，其直譯器名一律寫 `python`（`自誤 466`）。
> 🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**（`恆常附款 h`）——全部 payload 一律以**檔案管線**、依**圍欄之逐列索引**機械抽取；`commit` 訊息一律 `git commit -F`。
> 🛑 **本單⛔ 及於**：任何生產碼；`GB-167`／`168`／`169`／`171`／`172`／`173`／`174`／`175`／`176`／`177` 之任何修；`K-9-45` 之實作；態錨器之修（`自誤 487` 僅登記）。
> 🔴 **本單⛔ 令 CC 作任何「孰為正典」「應改為」之判**——出艙即止。

---

## `§零`　取號・停機款・pre-flight

### `§零-1`　取號現查（`e`／`e②`／`e③`／`z`／`aa`）

母體 ＝ 開工態全倉追蹤 **`2641`** 檔；內文側 ＝ 其中 `*.md`＋`*.py` **`1332`** 檔。框式（**可原樣執行**·`l⑥`·逐列 `re.search`）：單號 ＝ ``W-G\.9-N(?![0-9])``；自誤號 ＝ ``自誤[\s　`]*`?N(?![0-9])``；裁號 ＝ ``K-9-N(?![0-9-])``；`GB` 號 ＝ ``GB-N(?![0-9])``；檔名側 ＝ 同式施於路徑字串（`core.quotepath=false`）。

| 受詞 | 檔名側 | 檔框 | 列框 | 子字串框 | `e③` 之三分 | 判 |
|---|---|---|---|---|---|---|
| `W-G.9-330`（本單之號） | `0` | `0` | `0` | `0` | — | 🟢 **可取** |
| `K-9-45` | `0` | `0` | `0` | `0` | — | 🟢 **可取** |
| `自誤 485`／`486`／`487`／`488` | `0` | `0` | `0` | `0` | — | 🟢 **可取** |
| `GB-175`／`176`／`177` | `0` | `0` | `0` | `0` | — | 🟢 **可取** |
| ⛔ 取 `W-G.9-329` | `0` | `2` | `2` | `2` | `(c)`：`docs/orders/W-G.9-325_輕量單.md:25`、`docs/reports/W-G.9-325R_CC交接文.md:58`（皆引為對照乙） | 🔴 內文非零 |
| ⛔ 取 `K-9-44` | `0` | `2` | `3` | `3` | `(c)`：`docs/orders/W-G.9-325_輕量單.md:25`、`docs/reports/W-G.9-325R_CC交接文.md:58`／`:136`（皆引為對照乙） | 🔴 內文非零 ⇒ 自此為缺號 |
| 對照甲［必命中·同形族］`W-G.9-328` | `3` | `4` | `20` | `24` | — | 🟢 框非恆空 |
| 對照乙［必為零］`W-G.9-334`／`自誤 492`／`GB-181`／`K-9-47` | `0` | `0` | `0` | `0` | 定義域：路徑以 `docs/orders/W-G.9-N` 起首之號（`re.match`）＝ `124` 個·`MAX` `328`·⛔ 含 `334` | 🟢 ⛔ 占用 |

### `§零-2`　停機款（三值·`n`）

| # | 款 | 初態 |
|---|---|---|
| `1` | 改動任何生產碼一字（`verify/` 頂層 `*.py` `33` 支 ＋ `app.py`） | 🛑 未執行 |
| `2` | 任一 payload（塊 `P1`〜`P7`）之 bytes 或 `sha256` 與 `§五-1` 所載不符 | 🛑 未執行 |
| `3` | 工項五之 PDF：bytes ≠ `3464240` 或 `sha256` ≠ `3c9bd4b02b3628e30defbe0ceb8c16ee93e1443a2172da9416033902431216b9`；或入倉後 worktree 與 blob 不逐位相同 | 🛑 未執行 |
| `4` | 本單於主 checkout 與 worktree 之二份⛔ 逐位相同 | 🛑 未執行 |
| `5` | 開工時主線 ≠ `77e3ede`（已前進 ⇒ 停機回報，⛔ 逕行） | 🛑 未執行 |
| `6` | 本批任一新檔為 `git check-ignore` 所命中 | 🛑 未執行 |
| `7` | `§四` 收工閘任一 ≠ 期 | 🛑 未執行 |
| `8` | CC 作任何「孰為正典」「應改為」之判 | 🛑 未執行 |

### `§零-3`　pre-flight 機檢之逐項處置（`W-G.9-190R §九（甲）`·⛔ 靜默略過）

| 項 | 性質 | 實得 | 處置 |
|---|---|---|---|
| `P-3`／`P-5` | **機械可判·停機款** | 紅 **`0`** 項；`P-5` ✅ 相符（受詞 **`54766`** B） | 🟢 通過 |
| `P-1`／`P-2`／`P-4`／`P-6` | 文字啟發式·⛔ 停機款 | **`0`** 項 | ⚪ 不生 |

受單側收單時須自倉重跑 `python verify/probes/probe_order_preflight.py docs/orders/W-G.9-330_輕量單.md` 並逐項再處置；`rc ≠ 0` ⇒ **停機回報**。

---

## `§一`　態錨（逐字嵌入·受單側須逐格自倉重跑·`w②①`）

呼叫形（`w②③`·⛔ 省引數）：`python verify/probes/probe_WG9321_issuer_anchor.py <repo 絕對路徑> <out 絕對路徑> 484 479`；🩸 項 `5` 之值隨 `core.quotePath` 而變（`自誤 487`）⇒ **本表項 `5` 以 `core.quotepath=true` 量**（器所列印之 `core.quotePath` 須同格並報）。

| # | 項 | 值（錨於 `77e3ede`·發單側自倉實跑） |
|---|---|---|
| `1` | 主線 | `77e3ede4b351027513e6f8ac2587ba002b120369`（`W-G.9-328` `d4`）；凍存分支 `verify/W-G.9-299-gb170` ＝ `488c4858da63ebe370ed950788947aa742eb44ad` |
| `2` | 保全分支 | `origin/wip/W-G.9-318-preserve` ＝ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`·為主線之祖 |
| `3` | 生產碼 | **`34`**（頂層 `33` ＋ `app.py`）；`app.py` blob ＝ `b4288438b6b029f4799a79fffacde1e7722c369f` |
| `3′` | 子層 `*.py` | **`385`**（`verify/*.py` 全 `418` − 頂層 `33`） |
| `4` | 自擬框 | 自誤 **`470`**／**`484`**｜`GB` `172`／`174`｜`VR` `80`／`95`｜`K-9` `42`／`43` |
| `4′` | 正典框 | 自誤 **`474`**／**`484`**（缺 `[106,355,356,357]`）｜`GB` `172`／`174`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `42`／`43`（缺 `[]`） |
| `5` | `baselines` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列（`core.quotepath=true`；`false` 時 ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`） |
| `6` | refs | 遠端 **`26`**（`verify/*` `21`·`wip/*` `3`·`claude/*` `1`·`main` `1`） |
| `7` | 五簿／典 | 自誤簿 `d0212ef9f6316a8d7880272d0a378c8fb081fa82`·`1006559` B·`11213` 列｜恆常附款簿 `c790fbc16c06efd3ac88195850cfcbef47c58dc4`·`59311` B·`554` 列｜`GB` 簿 `cb91d2ed9e5d7be18b6bc98c4f85b72a5426e667`·`958188` B·`8472` 列｜`K-6` 典 `8937375840601f90c8d3ad57fdbb5fdc2cb9da07`·`371944` B·`4587` 列｜`VR` 簿 `7cff9a3790641ee5f97c367a960aeec5efd3f1cd`·`394392` B·`5124` 列 |
| `8` | 母體 | 全倉 **`2641`**｜`.md` **`909`**｜`docs/` **`867`**｜`verify/out/` **`1029`** |
| `9` | 恆常附款 | 相異款號 **`28`**·表列式款列命中 **`37`** |

---

## `§二`　KL 之語（逐字）與射程

> 是，依你擬的程序辦理。

（KL `2026-09-22`·答發單側窗二十五之擬議「一、登記單（輕量）……二、調配階段規格……第 2 步屬動分配幾何之決策，依常規宜換窗後辦理」；同訊息之其餘逐字見塊 `P1`。）
🛑 **射程**：本單零生產碼 ⇒ ⛔ 需放行；依 `常規一` 補款 `🔧 一` 由 CC 逕行 `push`。

---

## `§三`　工項（依序·各一 `commit`）

### 工項零　本單原封入倉

`docs/orders/W-G.9-330_輕量單.md`（**新檔**）。KL 置本單於主 checkout；CC 二進位複製入 worktree 並逐位對拍（停機款 `4`）；`SELF_SHA256` 依 `P-5` 原口徑自驗。開工前先行停機款 `5` 之實查。

### 工項一　`K-9-45` 之載入（`docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔末追加**）

標題形自該典最近一則（`K-9-43`）逐字複刻（`v`）。**塊 `P1`（逐字·⛔ 改一字）**：

```markdown

---

### 🔒 `K-9-45`　**不能於原街廓分配之土地：連同該歸戶位於公設地、道路上及其他建築街廓內不得分配之土地，以 `G(a1＋a2′＋a3′＋…)` 合併計算、可拆分、試配至各建築街廓之調配池；非位於該街廓之重劃前面積先折算 `a′` 再計 `G`**（KL 裁 `2026-09-22`·canonical·**逐字**）

**編號之由**（`§零-1`）：`K-9-44` 已為 `W-G.9-325` 等檔引作對照乙（內文非零）⇒ 取 **`K-9-45`**；`K-9-44` 自此為缺號、⛔ 鑄。

**KL 原文（逐字·`2026-09-22`·其一·答發單側窗二十四所呈 `GB-174` 之一問·⛔ 增刪一字）**：

> 1. 回答這問題前請先參考我上傳的別縣市政府舉辦的重劃土地分配作業說明會簡報.pdf檔。請參考"02土地分配規定-現金補償"(p.20)、"土地調整分配原則"、"申請合併及調整分配"(我們目前先針對未辦理重劃評估試分配作業，後續各地主得申請合併分配時，就要參考此部分流程作法)。
> 2. 所以你的問題應該是要說，這些在原街廓內的土地若不能餘元街廓分配時，就要將他的重劃前原宗之切片面積a1，連同該歸戶其他位於公設地上、道路上、其他建築街廓內不得分配的重劃前面積a2、a3...，以G(a1+a2+a3...)可拆分，試分配至各建築街廓內調配池，若剩下的G或一開始合併後的G就若有辦法達到最淺街廓或其他較低地價街廓得以分配面積*1/2（達到1/2就停，記住該位置的G及重劃後地價），若地主不願意繳納差額領地，就依據該分配街廓位置的應分配面積*該街廓的重劃後地價，計算地價補償費。
> 3. "最淺街廓"或"其他較低地價街廓"得以分配面積*1/2（達到1/2就停)，這達1/2就停的機制，可能不適最佳解，原則要盡量讓地主都能配地，就是要讓調配池利用最佳化，當然剩餘的調配池土地要符合畸零地寬度才可以建築使用。

**KL 所附**：新北市政府地政局「新、泰塭仔圳市地重劃區（第一區）土地分配原則說明會」簡報（民國 `112` 年 `11` 月·`60` 頁）；入倉於 `docs/參考文件/1塭仔圳_第一區__土地原則說明會簡報.pdf`（`W-G.9-330` 工項五）。所引頁：p.`20`（現金補償二框）、第 `03` 章（p.`21`〜`30`·土地調整分配原則）、第 `04` 章（p.`31`〜`47`·申請合併及調整分配）。

**【讀法】二處（KL 已確認·逐字段保留原字）**：第 `2` 點「若不能**餘元**街廓分配時」讀作「**於原**街廓」；第 `3` 點「可能**不適**最佳解」讀作「**不是**最佳解」。
> KL 之確認（逐字·`2026-09-22`·窗二十五首回合）：
> 第 2 點「若不能餘元街廓分配時」我讀作「於原街廓」，第 3 點「可能不適最佳解」讀作「不是最佳解」，是否無誤？
> 以上無誤

**KL 原文（逐字·`2026-09-22`·其二·補充·⛔ 增刪一字）**：

> 是，依你擬的程序辦理。
> 但要注意一點，當所有的「連同該歸戶其他位於公設地上、道路上……以 G(a1+a2+a3...)」，詳細點應該是要以G(a1+a2'+a3'+...)，a1重劃前面積就是在原同一街廓(但無法分配)，而a2',a3',... 以 ' 的形式代表是從別街廓調配進來的，若非位於原街廓的重劃前面積，就要計算a'後再機算G值

🔒 **【讀法】一處（⛔ 寫回逐字段·待 KL 點頭）**：其二末句「再**機算** G 值」讀作「再**計算** G 值」。

**發單側之整理（⛔ 充 KL 原文）**：

（一）**受詞**：同一歸戶之土地，凡於原街廓不能分配者（含依 `K-9-11 三` 不配地之切片，及原位置應分配面積未達者），**連同**該歸戶位於公設地上、道路上、其他建築街廓內不得分配之土地，合併計算其應分配面積，**可拆分**，試配至各建築街廓之調配池。
（二）**合併之式**：`G(a1＋a2′＋a3′＋…)`——`a1` ＝ 位於該街廓之重劃前面積，⛔ 折算；`a2′`、`a3′`… ＝ 非位於該街廓之重劃前面積，**先折算 `a′`**（`docs/配地計算總規格_v3.md` §7-4「a' 折算雙模式」）**再與 `a1` 合計，後算 `G`**；⛔ 以各筆之 `G` 相加（`G(Σa) ≠ ΣG(a)`）。
（三）**½ 之判與二出口**：合併後之 `G`，或拆配後剩餘之 `G`，達 KL 所稱「最淺街廓」或「其他較低地價街廓」之得分配面積 × ½（二語之界見下「與既有正典之關係」）⇒ 記該位置之 `G` 及其重劃後地價；地主繳差額 ⇒ 增配領地；地主不願繳差額 ⇒ **補償 ＝ 該分配街廓位置之應分配面積 × 該街廓之重劃後地價**（簡報 p.`20` 下框「重劃後面積 × 分配位置重劃後地價」；辦法 §53-2）。未達 ½ ⇒ 現金補償 ＝ 重劃前面積 × 原位置重劃後地價（簡報 p.`20` 上框；KL `2026-09-12` 裁二）。
（四）**最佳化之原則**：「達 ½ 就停」之機制不是最佳解；原則為**盡量讓地主都能配地**、調配池之利用最佳化；剩餘之調配池須合畸零地寬度方可建築（≡ `K-9-40 ⑨`）。
（五）**射程**：本程式目前為「未辦理重劃評估試分配」；簡報第 `04` 章（地主申請合併、五順位、志願序）屬日後地主申請階段，**列參考、⛔ 實作**。

**蘊含（⛔ 新裁·發單側據正典導出·KL 另有意者須另裁）**：`′` 之有無，以**試配之目標街廓**為準——目標街廓 ＝ `a1` 所在之街廓時，`a1` ⛔ 折算；目標為他街廓時，`a1` 亦屬「從別街廓調配進來」者，同須折算。其據 ＝ KL 其二之定義（「以 ' 的形式代表是從別街廓調配進來的」）＋ v3 §7-4 之 `a′` 二式，其分母恆為**目標街廓**之地價（模式一 ＝ 目標街廓該歸戶地之地價；模式二 ＝ 目標街廓之幾何面積權重平均地價）。
🔒 **本案之量級**：`628-27`／`628-42`／`628-53`／`628-24` 皆屬重劃前 `b` 區段（`rv.load_snapshot()["財務接線_v3"]["原地號_區段"]`）；目標街廓全屬 `b` 區段者折算比恆為 `1`；目標街廓含 `a` 區段地者（本案 `R1`）折算比 ≠ `1`。
🔒 **例**（`628-27`·`0 m`）：`628-27(1)`（`R2`·`51.32` ㎡·不配地）與 `628-27(2)`（`R3`·`46.68` ㎡·未達）各於其街廓不能分配 ⇒ 試配於 `R2` 時 `a1` ＝ `51.32`、`a2′` ＝ `46.68` 之折算值；試配於 `R3` 時反之；試配於他街廓時二者皆折算。

**發單側呈 KL 之【通知】之登記（`2026-09-22`·窗二十五·⛔ 充 KL 裁）**：`628-53` 之地主兼有建築街廓內切片二筆（`628-53(1)` `0.44` ㎡·未達；`628-53(2)` `2.78` ㎡·不配地）與公設地、道路上三筆（`628-24(1)` `621.00`／`628-53(3)` `300.44`／`628-53(4)` `229.34`·合 `1,150.78` ㎡）。W-F 之作法（`docs/W-F_細部規格.md` 開放題裁定 `2.`「梯3 釋池於 F.0」·KL `2026-07-10` 採建議）：建築街廓部分先領現金，公設部分另於 7-4 配地（凍結基準 `verify/baselines/wf/f4/F.4_總決算_退縮0m.csv` 之 `G025` 列逐字「F.0梯3釋池補償(§53兩式已列清單)；F.3轉7-4；E1 7-4 配地 R2 G=163.04；E1 7-4 配地 R3 G=511.33」）。依本裁其一第 `2` 點 ⇒ 五筆合 `1,154.00` ㎡ 一併計算、試配；該戶之現金補償（現行清單 `30,193` 元／依 `2026-09-12` 裁二補計 `2.78` ㎡後 `231,559` 元）⇒ `0` 元，`3.22` ㎡ 併入其配地。該通知載「擬依後裁辦理；KL 若認為此類地主之建築街廓部分仍應先領現金，請示知」；**KL 其二之首句 ＝「是，依你擬的程序辦理。」，未示異議**。

**與既有正典之關係**（`恆常附款 b`·全倉以「合併／加總／`G(a+a'`／a′／調配池／½／放棄分配／分配位置」及同義語查得）：
- `K-9-11 三`（不配地 ⇒ 走合併／調配機制；騰出之地入調配池；⛔ 超配）——**併存**；本裁定其合併之受詞與式。
- KL `2026-09-12` 裁一（`G(a+a'+…)`·`docs/W-D.4_合併分配規格.md` 逐字）／裁二（現金補償一律本文式）——**併存**；本裁其二定 `a1` 與 `a′` 之分。
- `docs/配地計算總規格_v3.md` §7-4（前置「將同一歸戶的 R + RD + PF 全部加總」、a' 折算雙模式、最佳化三條件交集之「可拆部分面積調配」）與 §7-5（≥½ 增配／§53-2 放棄改領；<½ 現金補償）——**併存**。
- `W-G.9-240` 之五級（八鍵詞典序·選塊）與其 `⑤`（§7-5 第 2 梯之選塊目標式「合格街廓中總差額地價最少」·KL `2026-09-05` 逐字理由「重劃後盡量能讓地主拿得到土地」）——**併存**；本裁其一第 `3` 點與之同向，⛔ 構成目標式之變更。
- `K-9-40`（末端塊與調配池之進入順序三層、`⑥` 拆分後之剩餘面積、`⑨` 剩餘調配池之臨正街寬度 ≥ 畸零地寬）——**併存**。
- W-F F.4 之停機解除 Q1（KL `2026-07-12`·§31-1-2 讀為**比較級**：深度淺於原街廓 ∪ 後價低於原街廓）——本裁其一第 `2` 點之「最淺街廓」字面為最高級；**⛔ 據以變更 Q1**；KL 如意在改回最高級，須另裁。
- v3 §7-5 之 ½ 基準（區標準 ＝ `MIN_i(分配深度_i × min_width_i)`）——與本裁「最淺街廓」之得分配面積於本案同值（`R4`·`114.07` ㎡）；二者於他案得異，屆時須另裁（泛用之界）。
- 裁定 L（`docs/specs/W-G.4_KL域裁L_達半佇列_算術不可行剔除.md`）——**併存**。
- `docs/W-F_細部規格.md` 開放題裁定 `2.`（梯3 釋池於 F.0·KL `2026-07-10`）——就「於原街廓不能分配之土地須先合併計算 `G(a1＋a2′＋…)` 方判 ½」之部分，**由本裁取代**（上開通知·KL 未示異議）；其釋出（現金補償）僅於合併後仍未達 ½ 時行之。
- `GB-174`——本裁即其失效條件 `(2)` 之答（見 `GB` 簿 `W-G.9-330` 節）。

**落地狀態**：⬜ **未實作**。調配階段於現態不運轉（畫面路徑 `GB-167`；harness `W-F F.0` 之 GSA 錨檢破致 F.1〜F.4 跳過 `GB-153`；引擎凍結於本案 `GB-5`）⇒ 本裁與 `K-9-11 三`、`K-9-40`、五級同為「已裁未實作」，其落地另以調配階段之規格單辦理。
```

### 工項二　`GB` 簿之 `🔧` 節與 `GB-175`〜`177`（`docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**）

新號之標題形自 `GB-174` 逐字複刻（`v`）。**塊 `P2`**：

```markdown

## 🔧 `GB-170` 之解除 ／ `GB-174` 受詞之改定與失效條件 `(2)` 之答 ／ `GB-175`〜`GB-177` 之立（`W-G.9-330`·⛔ 上文一字不刪·純末端追加）

🔒 **態** ＝ `77e3ede4b351027513e6f8ac2587ba002b120369`（本批開工態）。**鑄之授權** ＝ `docs/orders/W-G.9-330_輕量單.md` 工項二。

**(1) `GB-170` 之解除**：其失效條件三款（本簿 `GB-170` 條逐字·去粗體：`(1)` 該起算線改取乙式之遠側境界線且經 KL 放行；`(2)` 改後之行為差逐宗量得並具名；`(3)` 其餘行為逐位不變之證）於主線 `77e3ede` **皆成就**——
`(1)` KL 第三次放行（`2026-09-22`·本簿 `W-G.9-328` 節逐字）且其受詞已以合併 `commit` `9842df0` 落入主線；`(2)`／`(3)` ＝ `W-G.9-309` 之閘器於合併樹重跑 `RC=0`·十閘 🟢、除首二列 ≡ `verify/out/WG9309_gates.md`（`W-G.9-328` 工項二）。
**收單復驗**（發單側窗二十四·逐款自倉重跑）：合併樹對第一親之差恰 `app.py`＋`verify/stepg_pipeline.py`（`numstat` `73/16`・`24/7` ≡ `7eddd91..488c485`）；四 blob `b4288438…`／`e4bd912b…`／`23a2e919…`／`df366b58…`（`77e3ede` ＝ `9842df0` ＝ `488c485`）；閘器二跑同；收工閘 `484 479` ⇒ `rc 0`。窗二十五於 `77e3ede` 重得收工閘與態錨器 `rc 0`、`app.py` blob `b4288438b6b029f4799a79fffacde1e7722c369f`。
⇒ **`GB-170` 解除**（發單側登記·本簿 `W-G.9-328` 節「本項之解除候發單側收單復驗後登記」）。

**(2) `GB-174` 受詞之改定**：原受詞「二處之歸戶母體⛔ 含不配地之宗」**改定為**——不配地之切片，連同同一歸戶於原街廓不能分配之其他土地、公設地與道路上之土地，依 `K-9-11 三` 與 `K-9-45` 進入合併／調配路徑，以 `G(a1＋a2′＋…)` 計（先合 `a`、後算 `G`）；法定「歸戶負擔計算表」與試分配清單之原有面積，以及未達 ½ 者之現金補償（KL `2026-09-12` 裁二），仍屬本項之一部。
**失效條件 `(2)`「梯 `2` 之併入值經 KL 裁」⇒ 成就**：`K-9-45`（KL `2026-09-22`）——併入者為**重劃前面積 `a`**（非位於目標街廓者折算 `a′`），合併後重算 `G`；原登記之三候選值（`2.55`／`4.77`／`0.23` ㎡）皆為 `G`，**⛔ 為併入之量**。`(1)`／`(3)` ⇒ **未成就**。🛑 **⛔ 解除、⛔ 收窄。**
**二態實測之登記**（發單側量測器 `verify/probes/probe_gb174_twostate.py`·主線 `77e3ede`·窗二十四實跑、窗二十五二跑出艙逐位元同·態甲 ≠ 態乙）：態甲（現行）之數逐元 ≡ 本項原登記；態乙（清單層以剔除時之 `G` 加回）之**現金補償部分**：`G025` `30,193 → 231,559`（二退縮）、`G030` `0 m` `3,381,204 → 6,902,853`（≡ `3.5 m`）。🔒 態乙之 `G` 取法已為 `K-9-45` 所否（其以剔除時之 `G` 相加）；又依 `K-9-45` 之通知，`G025` 之建築街廓部分併入其配地而⛔ 領現金。
**交接文二十四 `§四` 之既登者**（`恆常附款 b`）：`TIER_EXPECT` 與實跑梯次之差（`{"0":13,"1":7}` 對 `{"0":11,"1":9}`）⇒ 本簿已登（字樣錨 `grep -n "TIER_EXPECT" docs/reports/W-G.4_泛用阻塞項登記表.md`·歸屬「步驟 0·待 `K-9-29` 碼側落地後重量」）⇒ ⛔ 另鑄；法定歸戶負擔計算表之缺 ⇒ 本項原登記已載。

---

### `GB-175` 🆕　**同一宗跨分配線、一側未達時之「較大側」：規格文字為面積，碼取應分配面積 `G`；本案二宗二判相反**

**受詞**（態 `77e3ede`·字樣現查）：`verify/wd4_tier_list.py` 步 `1.5`（`big = max(lots, key=lambda r: float(r.get("G(㎡)", 0) or 0))`）。
**正典之文字**：`docs/W-D.4_合併分配規格.md` 步 `1.5` 逐字「未達側標 `跨線併大側` 並指向面積較大側之暫編」；`docs/配地計算總規格_v3.md` §7-8 之 `3.` 逐字「各側應分配面積對原街廓標準個別判，未達側併入」⏎「面積較大之一側」（原文跨列）；KL 參考簡報 p.`15` 之表：「一側未達最小分配面積」⇒「向面積較大之一側合併分配」。三者於「較大側」皆稱「面積」，⛔ 載其為重劃前面積抑應分配面積（v3 同款於「個別判」則明載「應分配面積」）。
**實測**（態 `77e3ede`·`0 m`）：`628-27`：`G` ＝ `R3` 側 `26.54` 對 `R2` 側 `20.52`（取 `R3`）／`a` ＝ `R3` 側 `46.68` 對 `R2` 側 `51.32`（取 `R2`）；`628-53`：`G` ＝ `R6` 側 `0.26` 對 `R5` 側 `0.00`（取 `R6`）／`a` ＝ `0.44` 對 `2.78`（取 `R5`）⇒ **二宗之二判皆相反**。另：本案三筆不配地（`628-42(1)`／`628-27(1)`／`628-53(2)`）皆為同一宗跨分配線之一半；剔除後步 `1.5` 之母體亦⛔ 含之（同 `GB-174` 族）。
**與 `K-9-45` 之關係**：`K-9-45` 定「合併以 `G(a1＋a2′＋…)`、試配之目標街廓定 `′` 之有無」；步 `1.5` 之「較大側」即定其**首選目標街廓** ⇒ 二者之判準須一致。
**失效條件**：`(1)` 「較大側」之判準經 KL 裁或正典逐字定之；`(2)` 碼與之一致；`(3)` 本案二宗之判改前改後並列。🛑 **⛔ 解除、⛔ 收窄。** KL 之一問候調配階段之規格單附圖呈之。

### `GB-176` 🆕　**試分配清單之「放棄改領」取原位置之街廓，而正典取分配（目標）位置；且以 `ΣG(a)` 計**

**受詞**（態 `77e3ede`）：`verify/wd4_tier_list.py:300` `_pos = max(lots, key=lambda r: float(r.get("G(㎡)", 0) or 0))["所屬街廓"]`、`:301` `_waive = round(sumG * post_price[_pos])`；同列之增配目標 ＝ `:295` `_tgt = min(mina, key=mina.get)`。
**正典**：KL 參考簡報 p.`20` 下框「重劃後面積 × 分配位置重劃後地價」；辦法 §53-2；`docs/配地計算總規格_v3.md` §7-5 出口b「應分配權利面積(G)×」⏎「重劃後分配位置評定重劃後地價」（原文跨列）；`docs/W-D.4_合併分配規格.md` 步 `2` 表「放棄補償≈ΣG_戶×分配位置後地價」；`K-9-45` 其一第 `2` 點「該分配街廓位置的應分配面積*該街廓的重劃後地價」。
**實測**（態 `77e3ede`·二退縮同）：`G011` §53-2 ＝ `6,516,134` 元 ＝ `89.96 × 72,433.68`（`R3`·`628-42(2)` 所在）；同列增配目標 ＝ `R4`（`76,245.98`）⇒ 以目標位置計為 `89.96 × 76,245.98 ≈ 6,859,088` 元（僅示取位之差；`G` 之口徑依 `K-9-45` 另計）。
**對照**：`verify/wf_f4.py` 之 E2 出口列（`"放棄改領(元)§53-2": _money(cm["G"] * post_price[b])`·`b` ＝ 目標塊、`cm["G"]` ＝ `G(a′)`）與正典一致——惟調配階段現態不運轉（`GB-153`／`GB-167`）。
**失效條件**：`(1)` 清單層之取位 ＝ 分配（目標）位置；`(2)` 其面積 ＝ `K-9-45` 之 `G`；`(3)` 本案梯 `2` 各戶改前改後並列。🛑 **⛔ 解除、⛔ 收窄。**

### `GB-177` 🆕　**剔除紀錄 `K917_DROPPED` 於一次 `run_step_g` 內每一不配地宗記二次**

**受詞**（態 `77e3ede`）：`app.py` 之 `K917_DROPPED`（模組層 `dict`·跨情境累加）與 `k917_note_drop`；呼叫點 `verify/stepg_pipeline.py` 左、右二側各一。
**實測**（`verify/probes/probe_gb174_twostate.py` 之 `dup` 欄·每次 `run_step_g` 前後取差·二跑同）：`[[0.0,6,3],[3.5,4,2],[0.0,6,3],[3.5,4,2]]`（`[退縮, 記數, 以 (街廓, 側, 暫編地號) 去重後]`）；重記者逐欄相同（器內斷言）。
**現況**：該紀錄**只記不判**、現僅供報表；⇒ 凡彙總之者須去重並斷言重記者逐欄相同，否則不配地之宗數與面積倍計。重記之因（多階段）**未歸因**。
**失效條件**：`(1)` 一宗一記，或消費端一律去重並斷言；`(2)` 重記之因具名。🛑 **⛔ 解除、⛔ 收窄。**
```

### 工項三　`自誤 485`〜`488` 之鑄（`docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**）

標題形自 `自誤 484` 逐字複刻（`v`）。**塊 `P3`**：

```markdown

---

### 🩸 `自誤 485`　**窗二十四呈 KL 之 `GB-174` 一問：呈前未讀下游調配引擎之規格，以清單層之「原位置試配 `G` 直加」為建議並呈是非，方向與 KL 所裁相反**

**形**：呈問前未讀 `docs/W-F_細部規格.md` F.2〜F.4，亦未查 `K-9-11 三`「走合併／調配機制」之落地狀態；即以清單層「不配地宗之原位置試配 `G` 併入」為建議，逐字「不配地的那一半，是否以其在原位置試配時算得之應分配面積，併入該地主之合併面積，據以判斷增配或現金補償及其數額？（是／否）」。窗二十四首回合已自列 KL `2026-09-12` 裁一逐字 `G(a+a'+…)` 與「`G(Σa) ≠ ΣG(a)`」為須對齊之處，次回合未對齊即略去。
**後果之界**：呈文之選項殘缺、建議方向與 KL 所裁相反（KL 裁 ＝ 先合併重劃前面積 `a`、後算 `G`·`K-9-45`）；KL 未答是否，另示正確之問法與作法。未出單、碼面後果 `0`。
**根因**：只讀報表層（`W-D.4`）即擬問；首回合自列之「須對齊處」於次回合⛔ 逐一對齊、亦⛔ 明示擱置。
**後果之框**：🟡 耗 KL 一次域裁往返；土地後果 `0`。攔點 ＝ **KL**。
**攔法**：既有 `恆常附款 b`（全倉查既有之裁）／`i`（讀全文）。⛔ 新立款；教訓入交接文：呈 KL 前須讀完受影響機制之下游引擎規格；首回合自列之須對齊處，次回合須逐一對齊或明示擱置。

---

### 🩸 `自誤 486`　**窗二十五稱「W-F 仍於主線生效」，所據僅為呼叫鏈之存在，未實跑；並據以將三項歸為「已實作」**

**形**：窗二十五首回合對 KL 逐字「W-F 仍於主線生效：畫面「執行七級調配」經 `_build_wf_ctx` 呼叫 `wf_f4.compute`」，並於對照表將交接文二十四 `§三` 新 `2`〜`4` 列為「W-F 已實作」。
**後果之界**：同窗次回合實測（態 `77e3ede`）：畫面路徑之 `_wf_ns()` 必崩——`_WF_NS_NAMES`（AST 取值）`37` 名，引擎六檔經 `ns[...]` 取用之名 `37`，唯缺 `_wg9268p_anchor_advance`（`GB-167` 仍成立；器 ＝ `verify/probes/probe_WG9330_wfns_ast.py`）；`run_verification.py` 實跑結果 `FAIL`（子字串框 `FAIL` `43` 列／`PASS` `24` 列 ≡ `W-G.9-311R` 所錄），`W-F F.0` 因 GSA 錨檢破、F.1〜F.4 連鎖跳過（`GB-153`）；引擎凍結於本案（`GB-5`）。⇒ 調配階段現態**不運轉**。首回合之文標【未完序·待覆】；次回合自捕並向 KL 更正。未出單。
**根因**：以代理量（呼叫鏈存在）代性質（實際運轉）；`GB-153`／`GB-167` 皆已登而未查。
**後果之框**：🟢 零入單之誤；KL 已見更正。攔點 ＝ **發單側**（窗二十五次回合）。
**攔法**：既有 `恆常附款 i`；專案常規「閘須繫於其所保之性質、⛔ 代理量」。⛔ 新立款。

---

### 🩸 `自誤 487`　**態錨器項 `5`（`baselines`）之 `sha256` 取 `git ls-tree` 之文字輸出，隨 `core.quotePath` 而變；而交接文二十四 `§七-4` 令比對前設 `core.quotepath=false`**

**形**：`verify/probes/probe_WG9321_issuer_anchor.py` 項 `5` ＝ `sha256(git ls-tree -r HEAD verify/baselines 之 stdout)`；中文路徑於 `core.quotePath=true` 為跳脫形、`false` 為原字 ⇒ 同一樹二值。
**後果之界**：窗二十五實測（態 `77e3ede`）：`false` ⇒ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`；`true` ⇒ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（≡ `W-G.9-328` `§一` 項 `5`）；基座 `2f46579` 於 `true` 亦 `a898f4e1…` ⇒ 樹未變、差僅在表示。依交接文之教示重跑者，項 `5` 必與單載之錨不符。當場歸因，未誤判。
**根因**：錨取文字輸出而非位元組不變量；器雖列印 `core.quotePath`，未繫之於項 `5`。
**後果之框**：🟢 零後果。攔點 ＝ **發單側**（窗二十五開場序）。
**攔法**：候修（⛔ 本單）：項 `5` 以 `git -c core.quotepath=true ls-tree` 或 `-z` 固定其表示。⛔ 新立款。

---

### 🩸 `自誤 488`　**交接文二十四 `§三` 之初讀三處：「W-F 僅標旗」未讀碼；最佳化目標之查只及一檔；入倉落點為 `.gitignore` 所忽略**

**形**：（一）`§三` 表新 `3` 逐字「W-F F.4 僅「標旗」⇒ **疑與 KL 之裁不一**」——實則 `verify/wf_f4.py` 之 E2 出口列已算 §53-2 之額 ＝ `G(a′)` × 目標塊後價（`"放棄改領(元)§53-2": _money(cm["G"] * post_price[b])`），與正典一致。（二）新 `4` 以 `docs/W-F_F4_上呈_2026-07-11.md` 一檔 `0` 命中列「是否經 KL 裁·待查」——全 `docs/` 以同義詞查得 `docs/orders/W-G.9-240_五級條文入典與GB143補款更正.md` 之 `⑤` 已載 KL `2026-09-05` 之裁。（三）KL 簡報之入倉落點擬 `docs/references/`——`.gitignore:15` `references/` 忽略之。
**後果之界**：三者皆未入單；窗二十五對齊時自捕（落點改 `docs/參考文件/`·`W-G.9-330` 工項五）。
**根因**：以規格文字推碼之行為而未讀碼；否定性之查母體僅一檔；落點候定而未施 `恆常附款 g`。
**後果之框**：🟢 零後果。攔點 ＝ **發單側**（窗二十五）。
**攔法**：既有 `恆常附款 b`／`g`／`i`。⛔ 新立款。
```

### 工項四　發單側量測器四支入倉（`verify/probes/`·**新檔** `4`·`恆常附款 k`）

| 塊 | 落點 | 其用 |
|---|---|---|
| `P4` | `verify/probes/probe_gb174_twostate.py` | `GB-174` 二態對拍（窗二十四·`GB-177` 之 `dup` 亦出於此） |
| `P5` | `verify/probes/probe_gb174_geom.py` | `GB-174` 呈文附圖之幾何取出 |
| `P6` | `verify/probes/probe_gb174_mksvg.py`（窗二十四原名 `mk_svg.py`·內容⛔ 改） | 呈文附圖之繪製（其出艙即 `W-G.9-328` 入倉之附圖） |
| `P7` | `verify/probes/probe_WG9330_wfns_ast.py` | `GB-167` 之現態（`自誤 486` 所引） |

呼叫形：`P4`／`P5` ＝ `python <器> <repo 絕對> <出艙 json 絕對>`；`P6` ＝ `python <器> <P5 之出艙> <svg 出艙>`；`P7` ＝ `python <器> <repo 絕對>`（`rc 3` ＝ 有缺·即 `GB-167` 仍成立）。🛑 本單⛔ 令 CC 執行四器（`P7` 除外·見 `§四` 閘 `10`）。

**塊 `P4`**：

```python
# -*- coding: utf-8 -*-
r"""發單側量測器（窗二十四·唯讀·⛔ 零生產碼）：GB-174 之二態對拍。

用法：python probe_gb174_twostate.py <repo 絕對路徑> <出艙 json 絕對路徑>
態甲（現行）：`verify.wd4_tier_list.compute(fixture=False)` 原樣。
態乙（擬）  ：同上，惟 `run_step_g` 之回傳 `g_rows` 追加「本情境 K917_DROPPED 所記之不配地宗」，
             其 `G(㎡)` ＝ 剔除時所記之 G（原位置試配之值），`a 面積(㎡)` ＝ 該宗之 a，
             `畸零地旗標` ＝「不配地」（⛔ 使其被判為逐宗達標）。
🔒 `K917_DROPPED` 為 app 模組層之 dict、跨情境累加 ⇒ 本器於每次 `run_step_g` 前後取差、⛔ 清之以外改動。
🔒 金額與梯次一律由 `compute` 本身算（⛔ 本器另寫一份公式）。
"""
import copy, json, os, sys
REPO, OUT = sys.argv[1], sys.argv[2]
os.chdir(REPO)
sys.path.insert(0, os.path.join(REPO, "verify"))
sys.stdout.reconfigure(encoding="utf-8")
import wd4_tier_list as wd4  # noqa: E402

_orig = wd4.run_step_g
CAP = {"mode": None, "calls": []}


def _a_of(tp):
    # 🔒 逐字複刻 `verify/stepg_pipeline.py` 之 `_build_g_row` 取 a 之式（⛔ 另立口徑）
    if not tp:
        return None, None
    if '分攤登記面積_m2' in tp:
        return "分攤+面積", round(float(tp.get('分攤登記面積_m2', 0) or 0)
                                 + float(tp.get('面積_m2', 0) or 0), 2)
    if '面積_m2' in tp:
        return "面積_m2", round(float(tp.get('面積_m2', 0) or 0), 2)
    return None, None


def wrapped(ns, fake_st, cb, cad, snapshot, param_rows, build_parcels, winners_state,
            forced_map, setback, *a, **kw):
    before = {k: len(v) for k, v in ns["K917_DROPPED"].items()}
    sg = _orig(ns, fake_st, cb, cad, snapshot, param_rows, build_parcels, winners_state,
               forced_map, setback, *a, **kw)
    new = []
    for k, v in ns["K917_DROPPED"].items():
        for rec in v[before.get(k, 0):]:
            new.append({"街廓": k[0], "側": k[1], **rec})
    # 🩸 實測：同一宗於一次 `run_step_g` 內被記 `2` 次（多階段）⇒ 以暫編地號去重，並斷言重複者逐欄相同
    _uniq = {}
    for d in new:
        k = (d["街廓"], d["側"], d["暫編地號"])
        if k in _uniq and _uniq[k] != d:
            raise RuntimeError("🔴 同宗二記而內容相異：%r / %r" % (_uniq[k], d))
        _uniq[k] = d
    CAP.setdefault("dup", []).append((setback, len(new), len(_uniq)))
    new = list(_uniq.values())
    by_pid = {tp.get("暫編地號"): tp for tp in build_parcels}
    rows = sg["g_rows"]
    tmpl_keys = sorted(rows[0].keys()) if rows else []
    info = []
    for d in new:
        tp = by_pid.get(d["暫編地號"], {})
        ak, av = _a_of(tp)
        info.append({**d, "原地號": tp.get("原地號"), "a_key": ak, "a": av,
                     "tp_keys": sorted(tp.keys())})
    CAP["calls"].append({"mode": CAP["mode"], "setback": setback, "dropped": info,
                         "g_row_keys": tmpl_keys, "n_g_rows": len(rows)})
    if CAP["mode"] == "乙":
        sg = dict(sg)
        aug = list(rows)
        for d in info:
            if d["a"] is None or d["原地號"] is None:
                raise RuntimeError("🔴 不配地宗之 a／原地號 缺：%r" % (d,))
            aug.append({"暫編地號": d["暫編地號"], "原地號": d["原地號"], "所屬街廓": d["街廓"],
                        "推進側別": d["側"], "G(㎡)": d["G(㎡)"], "a 面積(㎡)": d["a"],
                        "畸零地旗標": "不配地"})
        sg["g_rows"] = aug
    return sg


wd4.run_step_g = wrapped
res = {}
for mode in ("甲", "乙"):
    CAP["mode"] = mode
    out = wd4.compute(fixture=False)
    res[mode] = {tag: out[tag]["groups"] for tag in out}
    json.dump({"calls": CAP["calls"], "dup": CAP.get("dup"), "groups": res}, open(OUT, "w", encoding="utf-8"),
              ensure_ascii=False, indent=1, default=str)
    print("mode", mode, "done", flush=True)
json.dump({"calls": CAP["calls"], "dup": CAP.get("dup"), "groups": res}, open(OUT, "w", encoding="utf-8"),
          ensure_ascii=False, indent=1, default=str)
print("done")
```

**塊 `P5`**：

```python
# -*- coding: utf-8 -*-
"""發單側（窗二十四·唯讀·⛔ 零生產碼）：GB-174 附圖之幾何取出。
用法：python probe_gb174_geom.py <repo 絕對> <出艙 json 絕對>"""
import json, os, sys
REPO, OUT = sys.argv[1], sys.argv[2]
os.chdir(REPO); sys.path.insert(0, os.path.join(REPO, "verify"))
from app_harvest import harvest
import run_verification as rv
from selection_pipeline import build_build_parcels
snapshot = rv.load_snapshot()
ns, fake_st = harvest()
cb_by, cad = rv.build_pipeline(ns, fake_st, snapshot)
v6 = open(rv.V6DXF, "rb").read()
temp, build, _ = build_build_parcels(ns, fake_st, v6, list(cb_by.values()), snapshot)
blk = {}
for k, b in cb_by.items():
    geo = {kk: vv for kk, vv in b.items() if isinstance(vv, (list, tuple)) and vv and isinstance(vv[0], (list, tuple))}
    blk[k] = {"keys": sorted(b.keys()), "label": b.get("label", k), "category": b.get("category"), "geo": geo}
tgt = [tp for tp in temp if str(tp.get("原地號")) in ("628-42", "628-27", "628-53")]
json.dump({"blocks": blk, "parcels": [{k: tp.get(k) for k in ("暫編地號", "原地號", "所屬街廓", "街廓分類", "polygon_coords", "面積_m2", "分攤登記面積_m2", "幾何面積_m2", "登記面積_m2")} for tp in tgt],
           "n_temp": len(temp), "n_build": len(build)}, open(OUT, "w", encoding="utf-8"), ensure_ascii=False, default=str)
print("ok", len(tgt))
```

**塊 `P6`**：

```python
# -*- coding: utf-8 -*-
"""發單側（窗二十四）：GB-174 呈文附圖（原宗切片之平面圖）。輸入 = probe_gb174_geom.py 之出艙。"""
import json, sys
from shapely.geometry import Polygon, box
d = json.load(open(sys.argv[1], encoding="utf-8"))
OUT = sys.argv[2]
P = {p["暫編地號"]: Polygon(p["polygon_coords"]) for p in d["parcels"]}
B = {k: Polygon(v["geo"]["vertices"]) for k, v in d["blocks"].items()}
RED, BLUE, GRAY = "#c62828", "#1565c0", "#9e9e9e"
DROP = {"628-42(1)": "8.34", "628-27(1)": "51.32", "628-53(2)": "2.78"}
KEEP = {"628-42(2)": "153.46", "628-27(2)": "46.68", "628-53(1)": "0.44"}
NUM = {"628-42(1)": ("1", (0, 0)), "628-42(2)": ("2", (0, 0)), "628-27(1)": ("3", (0, 0)), "628-27(2)": ("4", (0, 0)),
       "628-53(2)": ("5", (0, 0)), "628-53(1)": ("6", (14, 12))}
NOTE = {"628-27(1)": "不配地（僅 0 m；3.5 m 照常分配）", "628-42(1)": "不配地（0 m、3.5 m 皆然）",
        "628-53(2)": "不配地（0 m、3.5 m 皆然）"}
BLK = {p["暫編地號"]: p["所屬街廓"] for p in d["parcels"]}
W = 760
panels = [
    ("甲　原地號 628-42、628-27（街廓 R2 與 R3 相接處）", box(310380, 2651857, 310426, 2651899), 10),
    ("乙　原地號 628-53（街廓 R5 與 R6 相接處）", box(310459.5, 2651814.5, 310470.5, 2651823.0), 2),
]
out = []
Y0 = 70
PW, PH = 340, 330
def path(poly, tf):
    if poly.is_empty:
        return ""
    geoms = [poly] if poly.geom_type == "Polygon" else [g for g in getattr(poly, "geoms", []) if g.geom_type == "Polygon"]
    s = ""
    for g in geoms:
        pts = [tf(x, y) for x, y in g.exterior.coords]
        s += "M" + " L".join("%.1f,%.1f" % p for p in pts) + " Z "
    return s
for i, (title, win, bar) in enumerate(panels):
    LBL = []
    ox = 20 + i * (PW + 40)
    minx, miny, maxx, maxy = win.bounds
    sc = min(PW / (maxx - minx), PH / (maxy - miny))
    tf = lambda x, y, ox=ox, minx=minx, maxy=maxy, sc=sc: (ox + (x - minx) * sc, Y0 + 30 + (maxy - y) * sc)
    out.append('<text x="%d" y="%d" font-size="13" font-weight="bold">%s</text>' % (ox, Y0 + 18, title))
    out.append('<rect x="%d" y="%d" width="%.1f" height="%.1f" fill="none" stroke="#bbb"/>' % (ox, Y0 + 30, (maxx - minx) * sc, (maxy - miny) * sc))
    for k, g in B.items():
        c = g.intersection(win)
        if c.is_empty:
            continue
        cat = d["blocks"][k]["category"] or ""
        isb = (cat == "住宅區")
        fill = "#f5f5f5" if isb else ("#e9f1e3" if "公園" in cat else "#e4e4e4")
        out.append('<path d="%s" fill="%s" stroke="#555" stroke-width="1.2"/>' % (path(c, tf), fill))
        LBL.append((k, cat, c, isb))
    # 分配線（相鄰二住宅街廓之共同邊）
    for a_, b_ in (("R2", "R3"), ("R5", "R6")):
        sh = B[a_].boundary.intersection(B[b_].boundary).intersection(win)
        if not sh.is_empty and sh.length > 0:
            for ln in (sh.geoms if hasattr(sh, "geoms") else [sh]):
                if ln.geom_type != "LineString":
                    continue
                pts = [tf(x, y) for x, y in ln.coords]
                out.append('<polyline points="%s" fill="none" stroke="#222" stroke-width="2.4" stroke-dasharray="7,4"/>' % " ".join("%.1f,%.1f" % q for q in pts))
    for pid, g in P.items():
        c = g.intersection(win)
        if c.is_empty or (pid not in DROP and pid not in KEEP):
            continue
        col = RED if pid in DROP else BLUE
        out.append('<path d="%s" fill="%s" fill-opacity="0.55" stroke="%s" stroke-width="1.5"/>' % (path(c, tf), col, col))
    # 街廓名（畫在切片之後，避免被蓋）
    for k, cat, c, isb in LBL:
        lp = c.representative_point(); x, y = tf(lp.x, lp.y)
        name = k if isb else "%s（%s）" % (k, cat)
        out.append('<text x="%.1f" y="%.1f" font-size="12" fill="#666" text-anchor="middle">%s</text>' % (x, y, name))
    # 編號圈 ＋ 圖下清單
    ly = Y0 + 30 + (maxy - miny) * sc + 52
    for pid in NUM:
        g = P[pid]
        if g.intersection(win).is_empty:
            continue
        n, off = NUM[pid]
        col = RED if pid in DROP else BLUE
        cx, cy = tf(g.centroid.x, g.centroid.y); cx += off[0]; cy += off[1]
        out.append('<circle cx="%.1f" cy="%.1f" r="9" fill="#fff" stroke="%s" stroke-width="1.6"/>' % (cx, cy, col))
        out.append('<text x="%.1f" y="%.1f" font-size="11" font-weight="bold" fill="%s" text-anchor="middle">%s</text>' % (cx, cy + 4, col, n))
        txt = "%s　%s　街廓 %s　原有 %s ㎡　%s" % (n, pid, BLK[pid], DROP.get(pid) or KEEP.get(pid), NOTE.get(pid, "不配地" if pid in DROP else "照常分配"))
        out.append('<text x="%d" y="%.1f" font-size="11.5" fill="%s" font-weight="bold">%s</text>' % (ox, ly, col, txt))
        ly += 18
    # 比例尺
    bx, by = ox + 10, Y0 + 30 + (maxy - miny) * sc + 22
    out.append('<line x1="%.1f" y1="%.1f" x2="%.1f" y2="%.1f" stroke="#000" stroke-width="2"/>' % (bx, by, bx + bar * sc, by))
    out.append('<text x="%.1f" y="%.1f" font-size="11">%d 公尺</text>' % (bx + bar * sc + 6, by + 4, bar))
H = Y0 + 30 + PH + 175
head = ['<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 %d %d" width="%d" height="%d" font-family="Noto Sans CJK TC, Microsoft JhengHei, sans-serif">' % (W, H, W, H),
        '<rect width="100%" height="100%" fill="#fff"/>',
        '<text x="20" y="28" font-size="16" font-weight="bold">附圖　三筆不配地之地，皆為同一筆原地號被新街廓界線切開之一半</text>',
        '<text x="20" y="50" font-size="12" fill="#444">重劃前原宗之切片；灰線＝重劃後街廓界線，黑虛線＝兩住宅街廓背對背之分配線。紅＝因位置太窄而不配地；藍＝同一地主照常分配。</text>']
foot = ['<text x="20" y="%d" font-size="11.5" fill="#444">原有面積：程式計算所用之值（628-42(2) 取畫面輸出「歸戶負擔計算表」之 153.46 ㎡）。兩圖比例尺不同。</text>' % (H - 40),
        '<text x="20" y="%d" font-size="11.5" fill="#444">628-42、628-53 另有落入道路、公園之部分（屬共同負擔），與本問無關，未著色。街廓 R5 與 R6 僅於一點相接。</text>' % (H - 20),
        '</svg>']
open(OUT, "w", encoding="utf-8", newline="\n").write("\n".join(head + out + foot) + "\n")
print("ok")
```

**塊 `P7`**：

```python
# -*- coding: utf-8 -*-
"""發單側（窗二十五·唯讀·⛔ 零生產碼）：`GB-167` 之現態——`_WF_NS_NAMES`（AST 取值）對引擎經 `ns[...]` 取用之名。

用法：python probe_WG9330_wfns_ast.py <repo 絕對路徑>
🔒 成員數一律以 AST `literal_eval` 取值（`GB-167` 登記之攔法·⛔ 文字層括號配對）。
🔒 取用框 ＝ 引擎六檔之 `ns["名"]`／`ns['名']`（regex）；判別力：`_pool_strips_for_block` 須命中（必非零）。
rc：0 ＝ 無缺；3 ＝ 有缺（逐名列出）；6 ＝ 器紅（取不到 `_WF_NS_NAMES` 或判別力不成立）。
"""
import ast, os, re, sys
REPO = sys.argv[1]
ENGINE = ["verify/stepg_pipeline.py", "verify/wf_f0.py", "verify/wf_f1.py",
          "verify/wf_f2.py", "verify/wf_f3.py", "verify/wf_f4.py"]
src = open(os.path.join(REPO, "app.py"), encoding="utf-8").read()
names = None
for n in ast.walk(ast.parse(src)):
    if isinstance(n, ast.Assign) and any(isinstance(t, ast.Name) and t.id == "_WF_NS_NAMES" for t in n.targets):
        if names is not None:
            print("🔴 器紅：`_WF_NS_NAMES` 賦值不只一處"); sys.exit(6)
        names = ast.literal_eval(n.value)
if not names:
    print("🔴 器紅：取不到 `_WF_NS_NAMES`"); sys.exit(6)
pat = re.compile(r'ns\[\s*["\']([A-Za-z_0-9]+)["\']\s*\]')
used = {}
for f in ENGINE:
    for m in pat.finditer(open(os.path.join(REPO, f), encoding="utf-8").read()):
        used.setdefault(m.group(1), set()).add(f)
if "_pool_strips_for_block" not in used:
    print("🔴 器紅：判別力不成立（`_pool_strips_for_block` 未命中）"); sys.exit(6)
missing = sorted(k for k in used if k not in names)
print("_WF_NS_NAMES 成員數（AST）＝", len(names), "／相異 ＝", len(set(names)))
print("引擎經 ns 取用之相異名 ＝", len(used))
for k in missing:
    print("缺：", k, "←", sorted(used[k]))
sys.exit(3 if missing else 0)
```

### 工項五　KL 參考簡報入倉（`docs/參考文件/1塭仔圳_第一區__土地原則說明會簡報.pdf`·**新檔**·二進位）

KL 將其原檔（新北市政府地政局·民國 `112` 年 `11` 月·`60` 頁）置於主 checkout 之根；CC 二進位複製至上開落點並對拍 bytes ＝ **`3464240`**、`sha256` ＝ **`3c9bd4b02b3628e30defbe0ceb8c16ee93e1443a2172da9416033902431216b9`**（停機款 `3`）；入倉後以 `git cat-file -p HEAD:<路徑> | sha256sum` 驗 blob 與 worktree 逐位相同。
🔒 **落點之由**：交接文二十四擬 `docs/references/`，為 `.gitignore:15` `references/` 所忽略（`自誤 488`）⇒ 改 `docs/參考文件/`（`git check-ignore` 於開工態⛔ 命中·停機款 `6`）。
🔒 claude.ai 專案知識庫所存之同名檔係平台轉存之包（`60` 頁圖檔＋文字·`8532133` B·`sha256` `7a65ecaa570b323754b71cacc2b774e521e5896f590512e2b4b89d2ae92a4542`），**⛔ 為原檔**，⛔ 以之代。

### 工項六　執行報告入倉（`docs/reports/W-G.9-330R_執行報告.md`·**新檔**）＋ 收工閘

報告須載：各 `commit` 之 hash；停機款 `1`〜`8` 之三值；塊 `P1`〜`P7` 之 bytes／`sha256`；PDF 之 bytes／`sha256`／blob 對拍；`§四` 各閘之實得。收工閘於報告入倉後量、其實測值出艙於回報（⛔ 寫入報告·自指）。

---

## `§四`　收工閘（報告入倉後於主線量·基線態 ＝ `§一`）

🔒 **`n③` 之施行**：發單側已於拋棄式分支模擬工項零〜六（PDF 與報告以佔位檔代）並實跑本節各閘——下表之期即其實得（**必過之實例**）；於**未追加之態**（開工態）跑閘 `8` ⇒ **`rc 7`**（**必破之實例**·新號 `488` 未命中、`MAX` 仍 `484`）。

| 閘 | 受詞 | 期 |
|---|---|---|
| `1` | 本批逐 `commit` 之刪除欄 | 逐檔 **`0`**（無豁免） |
| `2` | 生產碼 `34` 檔對開工態之相異 | **`0`**；`app.py` blob ＝ `b4288438b6b029f4799a79fffacde1e7722c369f` |
| `3` | 本批文字檔之 `CR` | 受檢 **`9`** 檔（本單／`K-6` 典／`GB` 簿／自誤簿／報告／四器）合計 **`0`** |
| `4` | 四簿期末（二框並報） | 見 `§四-1` |
| `5` | `append-only` 三值 | 自誤簿 🟢／`GB` 簿 🟢／`K-6` 典 🟢／恆常附款簿 🔵／`VR` 簿 🔵／`CLAUDE.md` 🔵 |
| `6` | 自限 | `baselines` 與遠端 `26` **⛔ 變**；凍存分支 tip ⛔ 變；保全分支仍為主線之祖 |
| `7` | 母體 | 全倉 **`2648`**（＋ 本單／報告／PDF／四器 ＝ `7` ＋ `N_cc`）｜`.md` **`911`**｜`docs/` **`870`**｜`verify/out/` **`1029`**｜頂層 `*.py` `33`｜子層 `*.py` **`389`**；`N_cc` 逐筆具名（`e②`） |
| `8` | `python verify/probes/probe_WG9270_closegate.py 488 484 .` | **`rc 0`** |
| `9` | `python verify/probes/probe_WG9321_issuer_anchor.py <repo 絕對路徑> <out 絕對路徑> 488 484` | **`rc 0`** |
| `10` | `python verify/probes/probe_WG9330_wfns_ast.py <repo 絕對路徑>` | **`rc 3`**·缺恰 `_wg9268p_anchor_advance`（`GB-167` 仍成立之證·本單⛔ 修） |

### `§四-1`　四簿期末之算式（`w①`）

| 框 | 簿 | 算式 | 期末 |
|---|---|---|---|
| 正典框 | 自誤 | `474` ＋ `4`（`485`／`486`／`487`／`488`） | **`478`**／`MAX` **`488`**／缺 **`[106,355,356,357]`** |
| 自擬框 | 自誤 | `470` ＋ `4` | **`474`**／`MAX` **`488`** |
| 正典框 | `GB` | `172` ＋ `3`（`175`／`176`／`177`） | **`175`**／`MAX` **`177`**／缺 **`[12,87]`** |
| 正典框 | `K-9` | `42` ＋ `1`（`45`） | **`43`**／`MAX` **`45`**／缺 **`[44]`** |
| 標題錨定框 | `VR` | ⛔ 鑄 | **`80`**／`95`·⛔ 變 |
| — | 恆常附款 | `28` ＋ 新立 `0` | **`28`** 款·⛔ 修訂 |

三簿／典期末 bytes（**算式**）：`K-6` 典 `371944 + 10329`｜`GB` 簿 `958188 + 7782`｜自誤簿 `1006559 + 5358`｜恆常附款簿 `59311 + 0`｜`VR` 簿 `394392 + 0`。

---

## `§五`　驗收與上呈

### `§五-1`　受單側收單時須當場重算之基數（`f`）

| # | 基數 | 單載 |
|---|---|---|
| `1` | 工項數 | **`7`**（零〜六） |
| `2` | `§六` 款數 | **`28`**（施行 `19`／不生 `9`） |
| `3` | 所鑄之號 | 裁 **`1`**（`K-9-45`）／`GB` **`3`**（`175`〜`177`）／自誤 **`4`**（`485`〜`488`）；解除 **`1`**（`GB-170`） |
| `4` | 停機款數 | **`8`** |
| `5` | 塊 `P1`〜`P7`（圍欄內·末附換行） | `P1` `:93`–`:146`·**`10329`** B·`0f64d866dac0a49c…`／`P2` `:154`–`:192`·**`7782`** B·`6588648555740c8c…`／`P3` `:200`–`:239`·**`5358`** B·`67a1f9f253ecf590…`／`P4` `:256`–`:343`·**`4237`** B·`b9fda2dca9790adc…`／`P5` `:349`–`:370`·**`1386`** B·`d2cd8fc2570c4d18…`／`P6` `:376`–`:473`·**`6282`** B·`5e5ef91ba7322180…`／`P7` `:479`–`:512`·**`1908`** B·`422d239a243c1d82…` |

🔒 抽取一律依**圍欄之逐列索引**（`常規七 一`）；bytes 不符 ⇒ 停機款 `2`。其餘不符者以列舉為準並具名·⛔ 追改本單一字。

### `§五-2`　上呈

1. 任一停機款成就 ⇒ **停機上呈**·⛔ 自改。
2. 零生產碼 ⇒ 依 `常規一` 補款 `🔧 一` 由 CC **逕行 `push`**；⛔ 及於繞過 harness 權限分類器。

---

## `§六`　恆常附款逐款回掃（`28` 款）

| 款 | 對本單之施行 | 判 |
|---|---|---|
| `a` | 本單⛔ 新訂款 | ⚪ 不生 |
| `b` | 塊 `P1` 擬鑄 `K-9-45`：已以「合併／加總／`G(a+a'`／a′／調配池／½／放棄分配／分配位置」及同義語全倉查既有之裁，其關係（併存／取代）具名於塊 `P1` 之末段；`GB-175`〜`177` 鑄前已查其⛔ 既登（`TIER_EXPECT` 之既登者⛔ 另鑄·見塊 `P2`） | 🟢 施行 |
| `c` | 本單⛔ 動 `_corner_buffer_S` 之迴圈 | ⚪ 不生 |
| `d` | 工項五之 PDF 係倉外之物 ⇒ 其 bytes／`sha256` 為窗二十四所量、**未能於出單前重量**；不符者停機款 `3` | 🟢 施行 |
| `e`／`e②`／`e③` | `§零-1` 三分逐筆具名；`§四` 閘 `7` 含 `N_cc` | 🟢 施行 |
| `f` | `§五-1` 令受單側重算其基數 | 🟢 施行 |
| `g` | 本批新檔（`docs/orders/`／`docs/reports/`／`docs/參考文件/`／`verify/probes/` 四器）已以 `git check-ignore` 實查⛔ 命中；`docs/references/` 之命中已具名（`自誤 488`） | 🟢 施行 |
| `h` | 全部 payload 以檔案管線、依圍欄之逐列索引抽取 | 🟢 施行 |
| `i` | 工項二之受詞已讀 `GB-170`／`GB-174` 全文（含其後之 `🔧` 節）；`自誤 486`／`488` 即 `i` 之再犯之登記 | 🟢 施行 |
| `j` | ⛔ 以哨兵之落地後全倉命中為期值 | ⚪ 不生 |
| `k` | 本單所引之發單側量測（二態、`dup`、`_WF_NS_NAMES`）之器皆於工項四入倉 | 🟢 施行 |
| `l`／`l⑥` | `§零-1` 之框式可原樣執行；已發單號之定義域具名 | 🟢 施行 |
| `m` | 本單⛔ 定替身 | ⚪ 不生 |
| `n`／`n②`／`n③`／`n④` | 停機款三值；`P7` 於取不到或判別力不成立時 `rc 6`（`n②`）；`§四` 載必過／必破二實例（`n③`）；一切呼叫形寫 `python`（`n④`） | 🟢 施行 |
| `o` | `§四` 之期係發單側於同一開工態之拋棄式分支實得；PDF 與報告以佔位檔代（其母體之計數同） | 🟢 施行 |
| `p` | 本單係受領 `W-G.9-328` 全批（窗二十四收單復驗·窗二十五重得態錨）後所擬 | 🟢 施行 |
| `q`／`q②` | 塊 `P1` 所登之【通知】已逐態列其面積與金額（`3.22`／`1,150.78`／`1,154.00` ㎡；`30,193`／`231,559`／`0` 元），用語為地政用語 | 🟢 施行 |
| `r` | `§四` 各閘之基線態即 `§一` | 🟢 施行 |
| `s` | ⛔ 設殘差之期值帶 | ⚪ 不生 |
| `t` | 本單⛔ 有替換（純末端追加與新檔） | ⚪ 不生 |
| `u` | 本單⛔ 將既有之量接入新路徑 | ⚪ 不生 |
| `v` | 工項一／二／三之標題形自該典／簿最近一則逐字複刻；`§四-1` 二框並報 | 🟢 施行 |
| `w` | `§四-1` 為算式並逐號列舉；`§一` 逐格繫 `77e3ede`；呼叫形含引數 | 🟢 施行 |
| `x` | `§四` 閘 `5` 為三值 | 🟢 施行 |
| `y` | 零生產碼 ⇒ 依 `常規一` 補款 `🔧 一` 逕行 `push` | ⚪ 不生 |
| `z` | 對照乙之定義域已先列舉（`§零-1`） | 🟢 施行 |
| `aa` | 對照甲 ＝ `W-G.9-328`（同形族） | 🟢 施行 |
| `ab` | 本單⛔ 新訂款 | ⚪ 不生 |

**算式**：施行 ＝ **`19`**（`b d e f g h i k l n o p q r v w x z aa`）／不生 ＝ **`9`**（`a c j m s t u y ab`）；`19 + 9 = 28`·重疊 `[]`。

---

SELF_SHA256: 83281b6998a2364d583e6acc5aee56bfdcf3cb31330270775b9615471a2c621d
