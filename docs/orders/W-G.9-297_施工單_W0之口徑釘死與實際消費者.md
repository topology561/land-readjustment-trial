# `W-G.9-297`　施工單（**現查·零生產碼**）：`W_0` 之**口徑釘死** ／ 負擔計算之**實際消費者** ／ `自誤 400`・`401`

> **態**｜開工 ＝ `454d5e3dadb860308dfd5e425e645486c15dc76f`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-297_施工單_W0之口徑釘死與實際消費者.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（現查 ＋ 量測 ＋ 登記·**零生產碼**·⛔ 改碼一字·⛔ 開分支）
> 🛑 **本批鑄號 ＝ `自誤 400`／`401`**（逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔑 **本批<u>明文准</u>包裹／`settrace` 式之執行期量測**（`⛔ 在 §六 之射程內`）；**⛔ 設計驗算**（本批不需）。
> 🔒 **KL 之放行仍在·仍⛔ 動用**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·**同格載產生指令**·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `454d5e3dadb860308dfd5e425e645486c15dc76f` 或其後裔（**全 `40` 碼**） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**取之） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `SELF_SHA256` 逐位相符；二造須**先自證 bytes 相異** |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**） | [必真] 含 `W-G.9-297` ＝ `True`／[必偽] 含 `W-G.9-296` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `P-3`／`P-5` 不紅；`rc = 0` ＋ 四靶逐項出艙 |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`389`**／**`399`**｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`**（等待**綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] 子層檔數 **須 `> 0`**（其值**逐批漂移**·⛔ 定值） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**（⛔ `all()` 空集恆真）、payload 之落地形機驗、
函式名**二形並取**、頂層全域取 `t.body`、`rc` 緊接指令、向量夾角**釘槽**、單字元名取 AST、
**逐閘出艙執行次數**、**等待綁行程結束**、**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N` 取長跑輸出**、
**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**）
**逐項適用·⛔ 省**。
🆕 **一則新附款**（`W-G.9-296` 自捕一所立·⛔ 省）：凡以 `f_locals` 之**變數名**為鍵作逐格比對者，
須**先自證該名於該 frame 存在**（`in f_locals`），並**增第二鍵**（如實參之逐位相等）**二鍵並報**；
不一致即 **loud**——⛔ 令「比對失配」被記成「未呼叫」。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-296` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `8ad7708e1689281b…`）／
生產碼 `34` 檔於 `f45f946b..454d5e3d` 異動 **`0`**／**四簿逐位未動**（坐實⛔ 鑄號）
⇒ **✅ 全批核可**。
🩸 三則自捕皆於出艙前攔下，處置正確——**第一則尤其重大**：`f_locals['blk']` 恆 `None`（真名 ＝ `blk_label`）
致「比對失配」被記成「生產路徑⛔ 呼叫」，而**同一輸出之表頭明載通道② 錄到 `1` 次** ⇒ 二數互斥即其證；
其**保全 v1 落檔為物證**之處置合體例，已立為 `§零` 新附款。

---

## `§一`　裁

| # | 事 | **裁** |
|---|---|---|
| `1` | 可施之唯一格（`R2/left@3.5`）款 `3` 逐位差 ＝ `0.0`（ULP `0`·實參三者亦逐位相同·二通道交叉對拍 `0.0`） | **受領**：重建式**於該格**可信。🛑 ⛔ 充「路丙成立」、⛔ 充「重建式於另二格可信」 |
| `2` | 單 `§三-1` 稱該對拍「**於三格皆可施**」，實測僅 **`1`／`3`** 格 | 🔴 責 ＝ **發單側**：以一個**未查證之可得性**為前提斷「皆可施」。其不可施係**結構性**（`_mp_base_W0` 之定義與呼叫皆在 `not _degenerate_order and _N > 1` 之分支內）⇒ 鑄 **`自誤 401`** |
| `3` | 🔴 單 `§三-1` 所載之「生產態 `W_0`」二值（`4.4804447557822895`／`7.785015718090257`）與本批唯一之生產值（`6.98048044737404`）**⛔ 相符** | 🔴 責 ＝ **發單側**：**`W_0` 一詞於本波至少指涉二個相異之量**——`(甲)` `app.py` 之 `_W_near_out`（**本宗<u>近側</u>界線之 KL W**·供 telescoping 閘）；`(乙)` `verify/stepg_pipeline.py` 之 `_mp_base_W0`（純委派 `k956_W_from_mp`）所得之 `_b_L0`／`_b_R0`。單未釘其口徑 ⇒ 鑄 **`自誤 400`** |
| `4` | `A′` 之受詞 | 🛑 **待重釘**——其應繫於 `(甲)` 抑或 `(乙)`，**由發單側於本批實測後裁**；**本批⛔ 判**、⛔ 據舊 `A′` 之綠充任何結論 |
| `5` | 倉內另載「`3.5m R4 left` 之 `_b_L0` ＝ `6.98048044737404`」（`交接文_W-G.9-262`）與本批「該格呼叫 `0` 次」 | **並列出艙·⛔ 判孰是**（`§三` 款 `5`） |
| `6` | 驗收閘 `rc = 1` ⇒ 當場量其受詞後補入 | **處置正確**（⛔ 以「未觸」充綠）·⛔ 鑄號 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-297_施工單_W0之口徑釘死與實際消費者.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-296` 之單須 `≥ 1`／造乙 ＝ 執行期組出之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（框 ＝ `tr -cd '\r' \| wc -c`·**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：`W_0` 之口徑釘死與**實際消費者**之指名

### 三-1　本項之由（**發單側具名·⛔ CC 推定**）

本波以「`W_0`」一詞承載**至少二個相異之量**；`W-G.9-287` 據以呈 KL 之「起算點之差 `0.78 m`」
與「負擔率差 `4.96`／`9.02`／`4.27` 個百分點」係取 **`(甲)`** 之值（`4.4804447557822895` 等），
而 `(乙)` 之值（同格 `3.7076106246376597`）與 `S1_perp`（`3.6955`）之差僅 `≈0.012`。
⇒ **二量所描繪之缺陷量級相差逾一個數量級。** 🛑 **⛔ 由 CC 判孰是**；本項只釘其口徑並指名**誰真的進入負擔計算**。

### 三-2　逐款出艙（**同一次執行**·`repr` 全位·三 `forced` 格逐格）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **`(甲)`** `_W_near_out` 之**產生式逐字**（含其 docstring／註之逐字）與其於三格之**當場值** | 錨取字樣·**二形並取**·先驗唯一性 |
| `2` | **`(乙)`** `_mp_base_W0`／`k956_W_from_mp` 之**產生式逐字**與其於三格之**當場值**（含 `_b_L0`／`_b_R0` 之實參逐項） | 同上；不可得者 **loud 具名**·⛔ 判為 `0` |
| `3` | 🔑 **負擔計算之實際消費者**：`Rw`（`Rw_i = R(W_i) − R(W_{i−1})`）與 `G` 之計算，其 `W` **實際取自何者** ⇒ **逐字具名其賦值鏈**（自 `R(·)` 之實參上溯至 `(甲)` 或 `(乙)` 或第三者） | 🛑 **⛔ 推定**；逐處具名 `檔:函式`；若二者**皆**有消費處，**逐處分列**並出艙其**各自之受詞** |
| `4` | `_mp_base_W0` 之**守衛**（`not _degenerate_order and _N > 1`）於三格之**各分項真值**（`_degenerate_order`／`_N` 之當場值） | ⛔ 只報合取之結果 |
| `5` | 倉內「`3.5m R4 left` 之 `_b_L0` ＝ `6.98048044737404`」之**現查**：其出處逐字、其批次、其與本批實測之**並列** | 母體 ＝ 追蹤之 `docs/**` ＋ `verify/**`（**正面列舉檔數**）·檔／列**二數並報**；🛑 **⛔ 判孰是** |
| `6` | 三格之 `R((甲))`／`R((乙))`／`R(S1_perp)` 之**當場值**與**二組差**（`R((甲)) − R(S1_perp)`／`R((乙)) − R(S1_perp)`） | 一律以 `ns["rw_from_width"]` 取值·⛔ 器內另算；**帶號與絕對值並列** |
| `7` | 判別力三造 | [必相異] `(甲)` 與 `(乙)` 於至少一格須**相異**（⇒ 證二者⛔ 同物）／[必非零] 款 `3` 之消費處數 `≥ 1`（若 `0` ⇒ **loud 拒測**·⛔ 靜默綠）／[必為零] 同一量自比之差 ＝ `0`（**先自證可滿足**）。任一不成立 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判 `A′` 應繫於何者、⛔ 判路丙成立與否、⛔ 判前批呈 KL 之數孰是、
⛔ 擬實作、⛔ 改碼一字、⛔ 開分支、⛔ 動用 KL 之放行、⛔ 解除或收窄 `GB-170`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§四`　工項二：`自誤 400`／`401`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 400`**：**以一個詞承載二個相異之量，而單未釘其口徑**。
> 「`W_0`」於本波至少指涉：`(甲)` `app.py` 之 `_W_near_out`——逐字「**本宗<u>近側</u>界線之 KL W**
> （mp→近側·供 stepg telescoping 閘 `W₀` 用·**座標系 ＝ 平面直角座標之垂距**）」；`(乙)` `verify/stepg_pipeline.py` 之 `_mp_base_W0`
> （逐字「自此**純委派**至 `k956_W_from_mp`」）所得之 `_b_L0`／`_b_R0`。
> **徵候**：`W-G.9-296 §三-1` 以 `(甲)` 之二值（`4.4804447557822895`／`7.785015718090257`）
> 為「生產態之 `W_0`」，而實測所得之唯一生產值為 `(乙)` 之 `6.98048044737404` ⇒ **⛔ 相符**；
> 🔴 二量所描繪之缺陷**量級相差逾一個數量級**（`(甲)` 距 `S1_perp` `≈0.785`／`(乙)` `≈0.012`）
> ⇒ 據 `(甲)` 呈 KL 之「起算點之差」與「負擔率差」**其受詞待重釘**。
> **族** ＝ 受詞界定（`自誤 391`「序數詞二讀法」之孿生）。
> **攔法**：凡單內出現一個**在碼面有二處以上同名或近名之量**者，一律於同格**釘其口徑**
> （檔／函式／變數名**逐字**），並令受單側**併報另一讀法之值**；**⛔ 令受單側擇一**。

> **`自誤 401`**：**以一個未查證之<u>可得性</u>為前提，斷言某對拍「於三格皆可施」**。
> `W-G.9-296 §三-1` 逐字：「此一對拍**不倚賴包裹之錄**，故於三格**皆可施**」；
> 實測僅 **`1`／`3`** 格可施，且其不可施係**結構性**——`_mp_base_W0` 之定義與呼叫**皆在**
> `not _degenerate_order and _N > 1` 之分支內。
> **徵候**：該斷言之理由（「不倚賴包裹之錄」）**只排除了一個**不可得之來源，而未窮舉其餘。
> **族** ＝ 受詞母體未窮舉（`自誤 388`／`399` 族）。
> **攔法**：凡斷言一項量測「於某母體**皆**可施」者，須先**逐格現查其可得性之全部前提**
> （守衛條件之各分項），並於單內**具名其不可施之格**；⛔ 以「已排除其一」充「皆可施」。

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**、簡繁機檢之 `rc`；
🛑 本 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留**（⛔ 剝層），並出艙其落地形之機驗
（`^###` 起首列 ＝ `0`·對照組 `^##` 須 `> 0`）。

---

## `§五`　收工閘

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**） | `0`；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末 | 自誤 相異 **`391`**／MAX **`401`**（＋**二**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` `git diff --numstat` 輸出**空** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 一本批確有異動之檔）；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**二物證落檔**（`WG9295R_joint_v1_selfpolluted.log`／`WG9296R_w0rec_v1_blknone.log`）之 blob **逐位未動** |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-289-gb170` 命中 **`0`**（⛔ 開）；判別力[必存在] `p3a` 命中 `1` |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§六`　🛑 **⛔ 為之事**

🔑 **本單 `§三` 之包裹／`settrace` 式執行期量測<u>已明文授權</u>，⛔ 在本節之射程內**。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 擬任何實作形；⛔ 設計驗算；
⛔ 調任何界、⛔ 事後放寬任何門檻；⛔ 自寫第二套幾何或第二份 `W` 之定義（`GB-48` 族）；
⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp`／任何函式簽章；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**、⛔ `WV_BAKE`、⛔ 併線 `p3a`、
**⛔ 引任何自 baseline 取得之數**；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 解除或收窄任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改任何已入倉文件一字；⛔ 判 `A′` 之受詞、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§七`　報告之格

`§零` 開工閘 → `§一`〜`§二` 逐工項（逐格表以落檔**全量**出艙·⛔ `tail -N`）→ 判別力三造之判 →
一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit`。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**；**帶號與絕對值並列**。
🔒 **逐閘出艙執行次數**；🔒 同一性 `is` 比對；🔒 判定組為空一律 **loud 拒測**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**；物證落檔**保全⛔ 覆寫**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`_W_near_out`／`_mp_base_W0`／`k956_W_from_mp`／`_b_L0`／`rw_from_width`／`S1_perp`／`GB-170`／
`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`ls-tree`／`merge-base`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: 038756861de46ce66b372e2744314fce70256a16eaa784f4e8a1dc695b9c709e
