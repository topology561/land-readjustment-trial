# `W-G.9-314`　施工單（**輕**）：`自誤 427`／`自誤 428` 之鑄 ＋ 恆常附款 `j`／`k` 之立

> **態**｜開工 ＝ `7016c9909f45597a45e5022acbfdcc7d9f2341cf` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-314_施工單_自誤427與428之鑄及恆常附款jk之立.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **輕**（**純登記**·**零生產碼**·**⛔ `run_verification`**·**⛔ 跑本單所列以外之任何器**·**⛔ 新增或修改任何探針一字**·**⛔ 開分支**·**⛔ 修批**·**⛔ 觸及 `verify/out/` 落檔**）。
> 🛑 **本批鑄號 ＝ `自誤 427`、`自誤 428`**（**二號**·逐字由本單供·**同一 `commit`**）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；**⛔ 解除或收窄任何 `GB`**。
> 🔴 **修批之號仍為 `W-G.9-309`**（於 `7016c99` 態宣告框六數皆 `0`·保留）；**本單⛔ 占其號**、⛔ 辦修批、⛔ 自續 `W-G.9-309`。
> 🔑 **序**：**① `§二` 工項零**（單獨 `commit`·單獨 `push`）⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一**（二則之鑄·單一 `commit`）⇒ **④ `§四` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-313R` 之復驗（發單側自倉獨立·拋棄式 `clone`·`git -c core.autocrlf=false`·沙盒 `Python 3.12.3`·態 `7016c99`·`core.quotePath` 預設）

| # | 項 | 發單側實得（產生方式） | 判 |
|---|---|---|---|
| `1` | 主線 | `7016c9909f45597a45e5022acbfdcc7d9f2341cf`；`cb661f7..7016c99` ＝ `5` 個 `commit`（`e8e49f6`／`d82a642`／`3cd7025`／`161e273`／`7016c99`）；`git diff --numstat cb661f7 7016c99` ＝ `4` 檔·**刪除欄全 `0`**·全在 `docs/` | ✅ |
| `2` | 生產碼 `34` 檔 | `verify/` 頂層 `*.py` `33` ＋ `app.py`；`cb661f7`→`7016c99` 逐檔 blob 相異 ＝ **`0`**；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；判別力[必不命中] `verify/` 子層 `*.py` ＝ `330`（`> 0`） | ✅ |
| `3` | 工項零之逐位 | 倉內單 ＝ `23734` B·`CR` `0`·自載 `SELF_SHA256` `db189e4f1a66c0d84e197f1fdb73de3f9101b57a7a8f7ff13e21ad329bbe6152` ⇒ 與交接文所載逐位相符 | ✅ |
| `4` | 工項一之 append-only | 自誤簿前 `900540` B 逐位 ＝ `cb661f7`；Δ ＝ **`3035`** B（＝ CC 所報 payload bytes）；追加 `16` 列；blob `b0b4aa2ecc1edae2788d94262f1837092c6d84ae`→`cc1941244918b6cd9136ce89788afab4064c0ec9`·`903575` B | ✅ |
| `5` | 工項二之 append-only | `GB` 簿前 `921652` B 逐位 ＝ `cb661f7`；Δ ＝ `3262` B；追加 `29` 列；blob `9dc9e780511e4f0df2c53d7622fa141e6732e7a5`→`96755bc35b666d92b2cdc6a78c2658902a94f507`·`924914` B。🔒 **`GB-168`／`169`／`170` 原節與二次精化一字未動——由前綴逐位相符（`921652` B）直接涵蓋**；追加節之標題僅引既有號 `GB-168`、⛔ 新號 | ✅ |
| `6` | 四簿（器 ＝ `probe_WG9267_issuer_measurers.py registry`） | 自誤 **`416`**／**`426`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40`（缺 `[]`）⇒ 自誤 `+1`（如期）、餘三簿未動 | ✅ |
| `7` | 自限 | `git ls-tree -r HEAD verify/baselines \| sha256sum` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列（`core.quotePath` **預設**）；`refs/heads/verify/*` ＝ `20` 支·`gb170` `0`·`p3a` `1`；**`verify/out/` 受本批之改 ＝ `0` 列·`913` 檔** | ✅ |
| `8` | 二態落檔之不動 | `WG9312_runall_on.log` `161089` B·`1029` 列；`WG9312_runall_off.log` `36032` B·`326` 列；`WG9312_landeffect_on.log` ＝ `_off.log` ＝ `WG9311_landeffect.log`（皆 `86404` B·`692` 列·同一 `sha256`） | ✅ |
| `9` | 母體 | 全 `docs/` 追蹤檔 `816`→**`818`**（讀不到 `19`）；`verify/out/` `913`；全倉追蹤 `2419`→**`2421`** ⇒ 皆 ＝ 舊值 ＋ 本批之二新檔 | ✅ |
| `10` | 坑（態錨 `4` 之重取） | `probe_WG9269_pit_index.py` 於 `7016c99` 實跑：數字系 `1`〜`26` ＋ 字母系 `a`〜`bp` ＝ **`94`**·末 `bp`·**逐標籤皆 ✅ 可解析**；判別力二造成立（造甲 坑 `n` 定義處 `6` ≥ `1`／造乙 人造之坑 定義 `0` 引用 `0`）⇒ 器非紅；母體 ＝ HEAD 之追蹤 `.md` `840` 檔 | ✅ |
| `11` | 號之保留（態錨 `9`） | `W-G.9-309` 之錨定框於全 `docs/` 得 `59` 列·`15` 檔·`D1` ＝ `0`，逐列實查**全屬他指**（「修批之號仍為」「⛔ 自續或占用」「落地候」等）⇒ 依補款 `⑨` 他指排除後**宣告框六數皆 `0`·保留** | ✅ |
| `12` | 哨兵之既例（`自誤 427` 之外部錨） | `ZW423`／`ZW424`／`ZW425`／`ZW426` 於自誤簿命中皆 **`0`**；`<!--` 於自誤簿與 `GB` 簿皆 **`0`**；`GB168N` 全倉命中 ＝ 單 `6` ＋ 報告 `9`、**`GB` 簿 `0`**（母體 ＝ HEAD 全追蹤檔·框 ＝ 子字串框） | ✅ |

### `§零-2`　裁（**`6` 項**）

1. **`W-G.9-313R` 之受領**：工項零、開工閘 `13`／`13`（含閘 `10` 之自跑與二對照組）、工項一（`自誤 426`）、工項二（`GB` 簿末端登記）、收工閘八閘 ⇒ **全部受領**。上列 `§零-1` 十二項係發單側自倉獨立復驗之實得，**⛔ 採 CC 自陳而受領**。

2. **CC 之自捕 `1` 則 ⇒ 受領·⛔ 鑄號**：查「哨兵是否入簿」時對照組取 `B` 形而倉內標題為 `C` 形，致對照組得 `0`；其依 `常規五`「對照組得 `0` 一律先判量測器紅」改以檔案管線重驗（`C` 形三造 `1`／`1`／`25`）、確認器非紅、並具「**⛔ 讀為『本批無誤』**」——**處置全合規**。

3. 🔴 **發單側之責 ⇒ 鑄 `自誤 427`**：`W-G.9-313` 單 `§四` 之界定義（payload ＝ 二界列之間 ⇒ 界列⛔ 隨 payload 入檔）與同節末之落地後機驗期值（「哨兵 `GB168N` 入檔後之全倉命中 ＝ 本單 ＋ `GB` 簿」）**結構上互斥**；且該期值之設未查同族哨兵之既例（`§零-1` 項 `12`）。⇒ **CC 依 `常規二` 取保守項（保留既有抽取法·⛔ 注入界列）之判正確**；⛔ 追改 CC 之處置、⛔ 為配合機驗而注入界列。逐字見 `§三-1`。

4. 🔴 **發單側之責 ⇒ 鑄 `自誤 428`**：新窗於復驗態錨 `3` 時以**檔名關鍵語**為唯一查法，出艙「倉內亦無 registry 普查之 probe」「態錨 `3` ⋯⋯作廢」之否定存在宣稱；實則 `W-G.9-313` 單 `§一` 閘 `4` 逐字即載該器（`probe_WG9267_issuer_measurers.py` 之 **`registry` 子命令**），實跑後態錨 `3` **全數復原**（`§零-1` 項 `6`）。⇒ **「態錨 `3` 須新立計數式並入倉」之候辦工項<u>於本單撤回</u>**（若照辦將於倉內產生第二個同功之器）。逐字見 `§三-2`。

5. **態錨之現況（登記·⛔ 據以自行落地）**：交接文十一 `§一` 之十項態錨，於 `7016c99` **全數復原或如期變動**——項 `3`（四簿）、項 `4`（坑 `94`／`94`·末 `bp`）、項 `9`（`W-G.9-309` 保留）三者經本窗補測後**⛔ 作廢**；項 `1`／`7`／`8` 依本批之 `+5` `commit` 與 `+2` 檔如期更新。**次交接文之 `§一` 表，「產生方式」欄須逐項載其指令或器名**（項 `3`／`8` 曾留白，係 `自誤 428` 之近因之一）。

6. **依賴序⛔ 變**（`W-G.9-313` `§零-2` 裁 `5` 逐字續用）：**⓪ `GB-168` 之修（生產碼·須 KL 放行·另單）⇒ ① 廢前置合併之落地（`K-9-29` 子項 `1`）⇒ ② `F.0` `GSA` 六鍵無決策之成因 ⇒ ③ `GB-170` 取錯線之修（`W-G.9-309`）⇒ ④ `K-9-30`〜`K-9-40` 之落地**。🛑 **本單⛔ 辦其任一**。

### `§零-3`　本單取號之宣告框（補款 `①`〜`⑨`·**六數分列**·母體 ＝ 全 `docs/` 追蹤檔 **`818`** 檔（讀不到 `19`）·態 `7016c99`）

**三軸**：`(1)` 來源 ＝ `blob@7016c99`／`(2)` 母體 ＝ 上列／`(3)` 粒度框 ＝ 宣告框（`D1` 檔框 ＋ `D2`∪`D3` 列框·錨定框 `(?<![0-9\-])<號>(?![0-9])`·`D3` 取補款 `⑧` 之嚴格式 `^` 起首·`D2` 取補款 `⑨` 之他指排除）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 依 `⑨` 排除之 `D2` 列 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-314`** | **`0`** | **`0`** | **`0`** | `0` | **`0`** | **`0`** | `0` | 受詢 ⇒ **未占用·可用** |
| `W-G.9-313` | `2` | —— | —— | —— | `27` | `4` | —— | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0` | `0` | `0` | `0` | `0` | `59` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組），⛔ 採信本表。

### `§零-4`　既往同題之清單與其結論（`自誤 426` 攔法 `③`）

| 受詞 | 查法（母體·框） | 命中 | 結論 |
|---|---|---|---|
| 「界列」（`自誤 427` 之受詞） | `GB` 簿 ＋ 自誤簿全文·子字串框 | **`0`**／**`0`** | **新題**·⛔ 既有登記 |
| 「哨兵」 | 同上 | `51`／`47` | 皆為**哨兵之用法**，⛔ 及於「界列是否隨 payload 入檔」之題 |
| 「否定存在」（`自誤 428` 之受詞） | 自誤簿全文·子字串框 | **`5`** | **既有族**：`自誤 254`「**否定存在之斷言，其窮舉檢索之射程僅及一檔**」（該則自載已為第三度）⇒ `自誤 428` 係**又一度**，其新形 ＝ 射程及於**檔名**而未及於**器之子命令與既有單之閘表** |
| `GB` 簿之阻塞項（恆常附款 `i`） | `GB` 簿全文 | 本批之受詞（二則自誤之鑄）**非量測** ⇒ ⛔ 相關阻塞項 | 本批⛔ 涉任何 `GB` |
| `.gitignore`（恆常附款 `g`） | `.gitignore` `31` 列 | 及於 `docs/` 或 `*.md` 者 ＝ **`0`** | 本批三入倉對象（單／自誤簿／報告）**皆非忽略族** |

---

## `§一`　開工閘（**`13` 項**·資料列之數見本節末之機驗·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首 |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `7016c9909f45597a45e5022acbfdcc7d9f2341cf` 或其後裔（`merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` 逐位相符；🔴 ＝ `0`；🟡 逐項具名處置 |
| `3'` | 受詞自證（以器所印之檔名列） | [必真] 含 `W-G.9-314` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `416`／`426`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40`（缺 `[]`） |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py` | `34` 檔；判別力[必不命中] ＝ `verify/` 子層 `*.py` 須 `> 0`（發單側於 `7016c99` 得 `330`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**·⛔ 設 `false`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`gb170` ＝ `0`；`p3a` ＝ `1` |
| `8` | 受改檔之期初（`git cat-file -e` 後 `git rev-parse HEAD:<路徑>`·取 bytes） | 自誤簿（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）＝ `cc1941244918b6cd9136ce89788afab4064c0ec9`·**`903575` B**；期初 bytes 存錄於記憶體。🔒 **本批只動此一簿**——`GB` 簿⛔ 受改 |
| `9` | 本批新物之未占（**跑於 `§二` 之後**） | 檔名含 `W-G.9-314` 者 ＝ `1`；哨兵 `ZW427`／`ZW428` 之全倉子字串框命中 ＝ **除本單自身之產物外 `0`**（`自誤 423` 攔法·逐筆具名所在檔）。發單側於 `7016c99` 得二哨兵全倉 `0` 檔 |
| `10` | 宣告框（`§零-3` 六數·**CC 自跑**·含二對照組） | 受詢之命中**除本單自身之產物外 ＝ `0`**；對照甲 `D1` `≥ 1`；對照乙 ＝ `0` |
| `11` | `verify/out/` 之不動（本批⛔ 觸及） | 倉內 `913` 檔；`git status --porcelain -z -- verify/out/` 之輸出照實出艙 |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `\| ` 起首之列，扣表頭 `1`），與本單自載之 `13` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以列舉為準、逐項皆跑並具名。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308`、`W-G.9-308_補令一`（`a`／`b`／`c`）、`W-G.9-310`（`d-1`〜`d-3`）、`W-G.9-311`（`e`／`f`）、`W-G.9-312`（`g`）、`W-G.9-313`（`h`／`i`）之恆常附款全數逐項適用**（逐字見該六單·⛔ 省）。
🆕 **本單所增**：

（`j`·`自誤 427` 之攔法）
① 凡以**哨兵／標記之落地後全倉命中**為期值者，出單前須 `(a)` 查倉內同族哨兵之既例入檔形（**界列是否隨 payload 入受改檔**）、`(b)` 以既例為期值之母體、`(c)` 逐句自檢該期值與同單之界定義是否相容；
② **`恆常附款 e` 之射程擴及「落地後之機驗」**——凡期值之母體含本單自身之產物者，一律寫為「**除本單自身之產物外 ＝ `N`**」，⛔ 只施於開工閘；
③ **payload 已入受改檔之舉證**，以「append-only 前綴逐位 ＋ payload 逐位 ＋ 期末 bytes ＝ 期初 bytes ＋ payload bytes ＋ `sha256`」四造承擔，**⛔ 以界列之命中承擔**。

（`k`·`自誤 428` 之攔法）
① 凡出艙「**倉內無 X**」之宣稱，母體須含 `(a)` `verify/probes/` 全檔之 **docstring 與 `argv` 子命令**、`(b)` 最近 `5` 張單之 `§一` 閘表所引之器、`(c)` 檔名關鍵語——**三者皆查而後乃得出艙**，並同格載其查法與各自之命中數；
② **交接文之錨若「產生方式」欄留白**，其判為「**產生方式未載·須自倉現查**」，**⛔ 判為「不可復現」或「作廢」**——由「文件未載」推「倉內不存在」係無效推論；
③ 凡擬**新立一器**者，出單前須以其**功能之名**（而非檔名）查既有器；查得同功者 ⇒ **⛔ 新立**。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-314_施工單_自誤427與428之鑄及恆常附款jk之立.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名含 `W-G.9-314` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。🛑 **落地後即 `push`**，其後方跑 `§一`。

---

## `§三`　工項一：**`自誤 427` ＋ `自誤 428` 之鑄**（主線·**單一 `commit`**·嚴格末端追加）

落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字）。
🔑 界：`<!-- ZW427-BEGIN -->`／`<!-- ZW427-END -->`、`<!-- ZW428-BEGIN -->`／`<!-- ZW428-END -->`（**定界以整列相等為之**）；**payload ＝ 二界列之間**，**界列⛔ 隨 payload 入簿**（既例見 `§零-1` 項 `12`）。
🛑 **順序 ＝ `427` 在前、`428` 在後**；二 payload 間之空列照本單之形。
🛑 **凡含反引號之文字一律以檔案管線為之**（恆常附款 `h`）。

### `§三-1`

<!-- ZW427-BEGIN -->

### 🩸 `自誤 427`　**單所設之「落地後機驗」期值（哨兵入受改檔）與同單之界定義（payload ＝ 二界列之間）結構上互斥，且該期值之設未查同族哨兵之既例**

**形**：`W-G.9-313` 單 `§四` 首段逐字定界為「`<!-- GB168N-BEGIN -->`／`<!-- GB168N-END -->`（**定界以整列相等為之**）」⇒ payload ＝ 二界列之**間**，界列本身⛔ 隨 payload 入受改檔；而同節末之「落地後之機驗」逐字載「哨兵 `GB168N` 入檔後之全倉命中 ＝ 本單 ＋ `GB` 簿（逐筆具名）」。**二句同在一節**：照前者辦，後者必不可滿足——哨兵之字面只存在於界列，界列既不入簿，`GB` 簿之命中恆為 `0`。

**徵候**：CC 於工項二落地後量之，`GB` 簿得 `0`；依 `常規五`「對照組得 `0` 一律先判量測器紅」改以檔案管線重驗（其自捕之 `B` 形／`C` 形之別另計），`C` 形三造得 `1`／`1`／`25` ⇒ **器非紅**；乃依 `常規二` 取保守項（保留既有抽取法·⛔ 為配合機驗而注入界列），並照實具名為單側之相異，候發單側處置。**CC 之處置全數正確。**

**既例（發單側於 `7016c99` 自倉實查·母體 ＝ HEAD 之該二簿·框 ＝ 子字串框）**：`ZW423`／`ZW424`／`ZW425`／`ZW426` 於自誤簿命中**皆 `0`**；`<!--` 於自誤簿與 `GB` 簿**皆 `0`**；`GB168N` 之全倉命中 ＝ 單 `6` ＋ 報告 `9`、`GB` 簿 `0`（母體 ＝ HEAD 全追蹤檔）。⇒ **「界列⛔ 入簿」係本倉之既有體例，四度一致**，發單側出單前未查之。

**根因**：① 期值之設未以既例坐實，而以「哨兵應可見於受改檔」之**自洽直覺**代之（`ways-of-working` 所載「自洽而直覺上有說服力之論證最危險」之形）；② `恆常附款 e`（`自誤 423` 之攔法：期值之母體含本單自身之產物 ⇒ 寫為「除本單自身之產物外 ＝ `N`」）**之射程僅及於開工閘之未占查**——該款於同單 `§一` 閘 `9` 確已生效，**卻未及於「落地後之機驗」**；③ `考古 81` 之自問（「這份單裡有沒有兩句話，照 A 辦會違反 B？」）於 `§四` 未施行。

**後果**：未動土地、未致錯裁、未致錯鑄、倉內無須更正之物。代價 ＝ 一處單側期值結構上不可滿足，致 CC 須以三造重驗量測器、改以四造承擔 payload 之舉證並上呈。

**攔法（三款·立為恆常附款 `j`）**：
① 凡以哨兵／標記之**落地後全倉命中**為期值者，出單前須 `(a)` 查倉內同族哨兵之既例入檔形（界列是否隨 payload 入受改檔）、`(b)` 以既例為期值之母體、`(c)` 逐句自檢該期值與同單之界定義是否相容；
② **`恆常附款 e` 之射程擴及「落地後之機驗」**：凡期值之母體含本單自身之產物者，一律寫為「除本單自身之產物外 ＝ `N`」，⛔ 只施於開工閘；
③ **payload 已入受改檔之舉證**，以「append-only 前綴逐位 ＋ payload 逐位 ＋ 期末 bytes ＝ 期初 bytes ＋ payload bytes ＋ `sha256`」四造承擔，**⛔ 以界列之命中承擔**。

<!-- ZW427-END -->

### `§三-2`

<!-- ZW428-BEGIN -->

### 🩸 `自誤 428`　**出艙「倉內無 registry 普查之器」之否定存在宣稱，而其查法僅及於檔名之四關鍵語，未及於器之子命令與既有單之閘表；並據以誤判態錨 `3` 為「不可復現·作廢」**

**形**：新窗（`claude.ai`）於 `7016c99` 復驗交接文十一 `§一` 態錨 `3`（四簿之相異／MAX／缺號）時，先以自擬之標題框三度重建皆不還原交接文所載之 `415`／`425`，繼以 `ls verify/probes/` 加四關鍵語（`regist`／`census`／`簿`／`pit\|index`）為查法，得二命中皆非所求，遂出艙三句：「**倉內亦無 registry 普查之 probe**」「**態錨 `3` ⋯⋯作廢，⛔ 復驗**」「**須以新立且逐字入倉之計數式重取**」，並據以擬「新立計數式並入倉」之候辦工項。

**徵候**：其後為擬本批而讀 `W-G.9-313` 單全文，`§一` 閘 `4` 逐字即載其器與**子命令**——`python verify/probes/probe_WG9267_issuer_measurers.py registry`。實跑於 `7016c99` 得：自誤 相異 `416`／MAX `426`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `39`／`40`（缺 `[]`）⇒ **態錨 `3` 全數復原**，且自誤簿之 `+1` 與本批如期相符。**該三句之第一句為偽，第二、三句係其推論。**

**族**：`自誤 254`「**否定存在之斷言，其窮舉檢索之射程僅及一檔**」（該則自載其時已為第三度）之**又一度**。**本則之新形**：射程及於**檔名**而未及於**器之子命令、器之 docstring、既有單之閘表**——即「查了，但查的是錯的母體」，非「未查」。

**根因**：① 母體之誤——應以「**既有單所引之器**」為第一母體（`W-G.9-310`〜`W-G.9-313` 四單之 `§一` 閘表逐字載之），而以檔名關鍵語掃為末位；② 器之**子命令**⛔ 見於檔名，而檔名關鍵語之查法對子命令**結構上無判別力**；③ 交接文十一 `§一` 之欄位名為「舊窗實得（**產生方式**）」，而項 `3`／`8` 只載結果、未載指令——該留白為真，但**由「文件未載」推「倉內不存在」係無效推論**；④ 該偽宣稱若照辦，將於倉內產生**第二個同功之器**，違「⛔ 新增或修改任何探針一字」與框之單一性。

**後果**：出艙一則為偽之否定存在宣稱（於對話側·⛔ 入倉）；一則據其推出之候辦工項（已於 `W-G.9-314` `§零-2` 裁 `4` **撤回**）。未動土地、未入倉、未致錯鑄、未致錯裁。

**攔法（三款·立為恆常附款 `k`）**：
① 凡出艙「**倉內無 X**」之宣稱，母體須含 `(a)` `verify/probes/` 全檔之 **docstring 與 `argv` 子命令**、`(b)` 最近 `5` 張單之 `§一` 閘表所引之器、`(c)` 檔名關鍵語——**三者皆查而後乃得出艙**，並同格載其查法與各自之命中數；
② **交接文之錨若「產生方式」欄留白**，其判為「產生方式未載·**須自倉現查**」，**⛔ 判為「不可復現」或「作廢」**；
③ 凡擬**新立一器**者，出單前須以其**功能之名**（而非檔名）查既有器；查得同功者 ⇒ **⛔ 新立**。
④ **次交接文之 `§一` 表，「產生方式」欄須逐項載其指令或器名**；留白者於交接文內即具名為「待現查」。

<!-- ZW428-END -->

### `§三-3`　落地後之機驗（**依恆常附款 `j`**）

1. `python verify/probes/probe_WG9267_issuer_measurers.py registry` ⇒ 自誤 相異 **`418`**／MAX **`428`**（缺號 `[106, 355, 356, 357]` **未變**）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `39`／`40` **一字未動**。
2. **append-only**：期末 bytes 之**前綴** ＝ `§一` 閘 `8` 之期初 bytes（`903575` B）逐位相符。
3. **payload 逐位**：自本單 blob 以整列相等定界抽出 `ZW427`／`ZW428` 二 payload，與簿內落地之對應段逐位相符；**期末 bytes ＝ 期初 bytes ＋ 二 payload bytes（＋界外空列之 bytes·同格具名）**。
4. `CR` ＝ `0`；`sha256` 出艙。
5. 🔒 **哨兵 `ZW427`／`ZW428` 於自誤簿之期值 ＝ `0`**（界列⛔ 入簿·既例見 `§零-1` 項 `12`）——**得 `0` 係如期，⛔ 讀為量測器紅**；其全倉命中之期值 ＝ **除本單自身之產物（單／本報告）外 `0`**，逐筆具名所在檔。

---

## `§四`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**；同格載受檢檔數 `> 0` |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動檔之 `CR`（倉內 blob·取 bytes） | ＝ `0`（本批只動 `.md`·⛔ 觸及 `verify/out/`）；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末（器之輸出） | 自誤 相異 **`418`**／MAX **`428`**；`GB` `168`／`170`·`K-9` `39`／`40`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（受改檔 ＝ 自誤簿一檔） | 期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes（`903575` B） |
| `6` | 自限復驗 | `verify/baselines` `298` 列·`a898f4e1…` 未動；`data/` 全樹未動；`CLAUDE.md`／`K-6`／**`GB` 簿**（`docs/reports/W-G.4_泛用阻塞項登記表.md`·blob `96755bc3…`）blob 未動；**`verify/out/` `913` 檔逐位未動**；`refs/heads/verify/*` `20` 支·`gb170` `0`·`p3a` `rc` `1`；`WV_BAKE` ＝ `None`；`WV_K6_STEP0` ＝ `None` |
| `7` | 標籤對照 | 本報告全部計數標籤逐欄具名其集合與基數（**當場以程式自資料列計數**·⛔ 轉引單之自載數） |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`〜`6` 任一不如期 ⇒ **停、上呈**（照實全量出艙·⛔ 判改法）。

---

## `§五`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；`自誤 427`／`自誤 428` 之鑄（同一 `commit`）。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ `run_verification`；⛔ 跑 `probe_order_preflight.py`／`probe_WG9267_issuer_measurers.py` 以外之任何器；⛔ 新增、修改或執行任何探針一字；⛔ 觸及 `verify/out/` 任一落檔；⛔ 設定任何環境變數；
⛔ 動 `GB` 簿一字；⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`；⛔ 判 `Δ=2.425` 之改法、⛔ 提修法主張、⛔ 擬任何 diff；⛔ 判「步驟 0 應否落地」；⛔ 落地任何 `K-9` 子項；⛔ 鑄 `自誤 427`／`自誤 428` 以外之任何號；
⛔ 追改 `W-G.9-313` 之單、報告或其落地之二簿一字；⛔ 為配合任何機驗而注入界列或改既有抽取法；
⛔ 開分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——遇可觀測之硬限事件一律 loud 照實具名、停、上呈；
🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**——凡含反引號之文字一律**以檔案管線**為之（恆常附款 `h`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§六`　報告之格

`docs/reports/W-G.9-314R_自誤427與428之鑄及恆常附款jk之立_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `10` 之自跑與二對照）⇒ `§三` 工項一（二則之鑄·含 `§三-3` 五款機驗）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其框、母體與產生指令；**計數標籤一律當場以程式自資料列計數**。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框六數與 `§零-3` 同源；四簿框於 `7016c99` 實跑 ＝ `416`／`426`｜`168`／`170`｜`80`／`95`｜`39`／`40`。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`registry`／`自誤 427`／`自誤 428`／`append-only`／`payload`／`W-G.9-309`／`W-G.9-313`／`7016c99`／`data/V6.dxf`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: a48de733aa512967af9ad5e93574e60ede93c965b7fc04dbff122823359ac99a
