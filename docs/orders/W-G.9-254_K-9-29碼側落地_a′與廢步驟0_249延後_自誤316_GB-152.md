# W-G.9-254　`K-9-29` 碼側落地（`a′` ＋ 廢步驟 0）＋ 段丙延後之裁 ＋ 自誤 `316` ＋ `GB-152`

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。承 `W-G.9-253`（段甲已畢·`4e6f00f`·發單側已獨立復現全綠）。
> 🛑 **本單有土地後果**（`R4` 左街角 `226.01 ㎡`）⇒ **逐 commit 候 KL 放行·⛔ 逕推主線**。
> 🔒 **`-253` 段乙（`-251` `c1`／`c2`）⛔ 受本單影響**，可並行；**`-253` 段丙（`-249` plan v2）本單令其<u>延後</u>**（見 `§三`）。

---

## §零　開工閘

1. `git fetch`；錨 ＝ **`origin/wip/s1-endpart @ 4e6f00f`**。不符 ⇒ 停機。
2. 號占用（宣告框·`①`–`⑦`·三數分列 ＋ **雙屬列數**）：發單側於 `4e6f00f` 實測 `W-G.9-254` ＝ `D1 0`／`D2 0`／`D3 0`／雙屬 `0`。CC 當場復現；非全 `0` ⇒ 停機。
3. 次號當場復算（⛔ 採信本單）：發單側實測 自誤 `MAX 315` ⇒ 鑄 **`316`**；`GB` 主序列 `MAX 151` ⇒ 鑄 **`GB-152`**。
4. `probe_order_preflight.py` 對本單：`P-3`／`P-5` 綠。
5. 🔒 **開窗前置**（`GB-152` 之標的）：worktree 須先切至 **wip 態**，`PreToolUse` heredoc 硬閘方生效。⛔ 於 `main` 態施工。

---

## §一　工項零：本單原封入倉 `docs/orders/`（`cp`·⛔ 重打）。`P-5` 對拍。

---

## §二　工項一：自誤 `316` ＋ `GB-152`（**零生產碼**·append-only）

**`自誤 316`　單內同節二款相斥：`§二` 令「皆末端追加」而其下逐款令「節末追加」（`296`／`297`／`303`／`311`／`-251` 工項編號 同族·第五度）**
🔒 **責 ＝ 發單側。**
受詞：`W-G.9-253` `§二`「**同批更正標記**（皆末端追加·⛔ 動原句）」，其下四款卻逐字令「`K-6:57-60` 步驟 0 **節末**追加一列」「`K-9-21`／`K-9-24 一`／`K-9-22` 各**節末**同形追加」。節末係**檔中間**插入 ⇒ 破 `wg942_append_audit` 之嚴格前綴、並使其後全體行號位移。
加重：發單側於**同一段之首句**已自書「皆末端追加」，四款即其正下方；且交接文 `§二 四` 已明列本族為本窗高發區。
攔截者：**CC 於施工前自捕並依 `常規二` 取保守項（檔末追加）**——非機檢。
通則：凡令「追加」，須於**同一款**明載其為「**檔末**」抑或「節末」；若同段已宣示檔末，其下各款⛔ 得另書節末。二形相斥時一律取**檔末**。
🔒 **CC 之處置（檔末立節·字樣錨逐則具名四受詞·⛔ 動原節一字）經追認為<u>正解</u>**，⛔ 追改既出（`常規四（九）五`）。

**`GB-152`　`GB-143` 硬閘之生效係**條件式**——繫於 worktree 是否在 wip 態**
🩸 案由：`-249R`／`-253R` 二度開窗，CC 活體實測報「硬閘⛔ 生效」，其後於 `-253R` 段甲**被同一硬閘實際攔下且攔對**。真因 ＝ 開窗時 worktree 停在 `main` 態、閘之二檔不存在；`git switch --detach` 至 wip 後二檔隨工作區出現、閘即生效。
🔒 **⇒ 先前「未生效」之出艙<u>當時為真</u>**（`GB-143` 補款之「未被攔 ≠ 未違」仍全效）；本則**⛔ 追改**其任一字，亦**⛔ 計入 `GB-143` 之發生次數計數器**。
🔒 **處置**：session 開啟程序須明載「**先切 wip 態、再驗閘**」；於 `main` 態所得之「閘未生效」判**⛔ 具證據力**。
🛑 **本則⛔ 解除 `GB-143`**——規則（⛔ heredoc）不因 backstop 生效而放寬。

---

## §三　工項二：**`-253` 段丙（`-249`）延後之裁**（發單側自裁·工序·⛔ 動配地結果）

🩸 **案由**　`K-9-29 九` 已載「`-249` 之觀測母體於步驟 0 廢除後**須重跑**」。發單側現查坐實其**必然**：本案步驟 0 於 `R2` 實際併出 `628-40(1)+`（`G010`·`628-40(1)`＋`628-43(1)`·`Σa 513.93`），而 `-249` 之受詞正是 **`R2` 左街角**。⇒ 於現母體所產之 `V-S` 觀測表，其候選集與位次**必與落地後相異**，KL 據以裁「接閘／`R2` 左由誰拿到」將據**已被正典廢止之母體**——即 `-249R` 自陳之「把綠當過」同形。

> **裁**　`-253` 段丙（`-249` plan v2 及其觀測）**移至本單 `§四` 之後**。plan v2 得先撰、先送 reviewer（零生產碼），**⛔ 產出 `V-S` 表、⛔ 呈 KL**，俟 `§四` `c2` 經 KL 放行並併線後，以**落地後母體**重跑方得出艙。

`W-G.4` 依賴序（**檔末**追加更正段·⛔ 改原表）：`3′ K-9-29 碼側落地` → `3″ -249 段三觀測（重跑）` → `4 五級`。

---

## §四　工項三：🔴 **`K-9-29` 碼側落地**（生產碼·側分支 `verify/W-G.9-254-c1`／`-c2`·**逐 commit 候 KL**）

🔒 **切為二 commit，因其土地後果之量截然不同。⛔ 混同。**

### `c1`　`wf_f0` 級0／0′ 之 `Σa` → `a + Σa′`（`K-9-29 三`）

受詞：`verify/wf_f0.py:241`–`:246`（`add` 之累加 ＋ `t["面積_m2"]` 之寫入）；docstring `:22` 逐字「合併＝標的宗.面積_m2 += Σ(被併宗 a)」同批更正。
改為：`a′ = a × p(被併宗原位置) / p(標的宗)`，`p` ＝ **重劃前地價（元/㎡）逐筆**（`K-9-29 一`）。
🛑 **`p` 之取值須依 `docs/配地計算總規格_v3.md:432`–`:435` 之<u>雙模式逐字</u>**（CC 於 `-253R` 已具名此差·**正確**）：本工項恆為模式一（同街廓同歸戶 ⇒ 標的宗即該歸戶自身之地）；模式二（`p_average`）⛔ 在本工項射程，**具名登記為五級射程**、⛔ 默漏。
🛑 **`p ≤ 0` ⇒ `raise`**（⛔ 靜默退 `1.0`·⛔ 靜默退 `a′ = a`）。
🔒 **標的宗**：沿用既有 `_key`（`G` 最大；並列取暫編字典序最小）——**KL `2026-09-08` 明裁沿用**，⛔ 改為面積較大者。

🔑 **本 commit 於 UC9898 之預期 ＝ <u>逐值零差</u>**：本案六組級0／0′ 之成員經發單側實測**皆同歸戶同區段** ⇒ `p(被併) = p(標的)` ⇒ `a′ = a` ⇒ `Σa′ = Σa`。
⇒ **`V-G` 為零差閘**：`f0`／`f2`／`f3`／`f4` 全部 `G 值` 與 `合併決策` CSV 逐欄逐值 **`Δ = 0`**（雙情境）。**非零即紅、停機上呈**。
🛑 **判別力對照組（⛔ 省）**：以人造測資餵一組 `p(被併) ≠ p(標的)` 者，該閘須**紅**；及一組 `p = 0` 者，須 `raise`。二者皆綠 ⇒ 器無鑑別力、⛔ 得出艙。

### `c2`　廢步驟 0 前置合併（`K-9-29 二`）

受詞：`app.py:11633` `k6_step0_enabled()` 之**預設值**，`"on"` → `"off"`。
🔒 **落地形取旗標翻轉，⛔ 刪碼**——理由：① 合本倉「死碼化而非刪除」既例（`k6_step0_block_locked:11640`）；② **旗標自身即對照組**，`K6_STEP0_ENV=on` 可逐位復現落地前行為 ⇒ 迴歸以**同一 blob 之 A／B 兩活體 run 相減**取得，**⛔ 依賴凍存基準** ⇒ **`GB-67`／telescoping ⛔ 阻塞本閘**（其阻塞者係與凍存基準對拍，非兩活體 run 之差；本款須於報告具名此界）。
同批：`k6_step0_enabled` 之 docstring 加註「經 `K-9-29 二` 廢止；預設 `off`；`on` 僅供對照，⛔ 為生產態」。`k6_step0_merge` 本體**一字不刪**。
🔒 **⛔ 及者**：⛔ 動 `K-9-27`／`K-9-28`（歸側階梯與 `M` 次序）一字；⛔ 動段一上鎖、段二鍵序；⛔ 重產或重烤任何 baseline（`GB-16`）。

🔴 **本 commit 有土地後果** ⇒ 出艙須含 **`V-H` 差異表**：A（`on`）／B（`off`）逐宗逐欄之差，含至少：`暫編地號`／`所屬街廓`／`a`／`G`／`街角地`／`第1筆街角`／強制抵費地格。
**預期（`K-9-29 八`·KL 已確認）**：步驟 0 之 `9` 組／`13` 宗解併；`R4` 左街角由**強制抵費地 `226.01 ㎡`** 變為 `628(1)` 配地。⚠️ 該值承 `K-9-24 五` 之**推定** ⇒ **以 `V-H` 實測為準**，實測≠推定即**具名回報、⛔ 逕自調和**。

---

## §五　工項四：`-253` 段乙（`-251` `c1`／`c2`）

依 `-253 §六` 原令辦，**⛔ 受本單影響**。純 UI·零土地後果；側分支 `verify/W-G.9-251-c1`；併線前 `V-K` 由 KL 目檢。

---

## 【驗收】

| 閘 | 判準 | 停機款 |
|---|---|---|
| `V-A` | 工項一之批：十一檔 `sha256[:16]` 與 `4e6f00f` 逐位同 | 🔴 |
| `V-B` | append-only：`numstat` deletions 全 `0`（自誤簿／`W-G.4`／`CLAUDE.md`） | 🔴 |
| `V-C` | 取號：自誤 `MAX 315→316`、缺號仍 `[106]`、恆等式 `True`；`GB MAX 151→152` | 🔴 |
| `V-G` | `c1` 逐值零差（`f0`/`f2`/`f3`/`f4` × 雙情境·`Δ = 0`） ＋ **二判別力對照組如期紅／`raise`** | 🔴 |
| `V-H` | `c2` A／B 差異表產出且逐欄具名；`R4` 左街角格之變化實測值出艙 | 🔴 |
| `V-I` | `c2` 之 `K6_STEP0_ENV=on` 逐位復現落地前行為（＝ `4e6f00f` 之 `off` 前態） | 🔴 |
| `V-J` | ⛔ 動 `K-9-27`／`K-9-28`／段一上鎖／段二鍵序；⛔ 重烤 baseline | 🔴 |
| `V-K` | `-251` 段乙：KL 目檢 | 人工 |

🛑 **`V-G`／`V-H` 之量測通道須具名**。凡受詞落於 `app.py` 之 `main()` 內者，通道 ＝ **KL localhost 手跑**（既例 `verify/out/KL_UI_3.5m_<sha>_*.csv`）或 `run_all` 可達之 harness 路徑；⛔ 以 `run_all` 宣稱涵蓋 `main()`（`CLAUDE.md:58`）。

---

## §六　產出與放行

| 批 | 內容 | 放行 |
|---|---|---|
| 工項零＋一 | 入倉／`316`／`GB-152`／`§三` 之裁與 `W-G.4` 更正段 | **零生產碼 ⇒ 直推主線** |
| `c1` | `wf_f0` `a′` | 側分支；`V-G` 全綠 ⇒ 呈 KL **一句**：「本批於本案逐值零差，係為他案可攜」⇒ 候放行 |
| `c2` | 廢步驟 0 | 側分支；`V-H`／`V-I` 出艙 ⇒ 呈 KL **差異表 ＋ 一句實務語言**（`R4` 左街角之實測歸屬）⇒ 候放行 |
| `-251` 段乙 | 同 `-253 §六` | 側分支；`V-K` 後候放行 |

🔒 **呈 KL 之形**（`-252` 交接文 `§七` 所立）：CC 先自倉逐位復驗，再以**一句實務語言**呈 KL；**KL 放行的是那句話、⛔ 那段程式碼**。
🛑 `c1` 與 `c2` **⛔ 同一 commit**；`c2` 未經 KL 放行前，**⛔ 併入主線**。

---

## §七　待落地登記（本單登記·⛔ 辦）

| 項 | 依賴 |
|---|---|
| `-249` 段三觀測**重跑**（`§三` 之裁） | `c2` 併線後 |
| `a′` 模式二（`p_average`·跨街廓無同歸戶地） | 五級射程 |
| `K-9-29 六` 入池閘之**五級層**實作（`c1` 只落 `F.0` 層） | 五級（`r1`–`r8`）⬜ |
| `K-9-22` 作廢之 `CLAUDE.md` ⬜ 表更正標記 | 本單工項一可一併加·⛔ 改原列 |
| `GB-67`／telescoping | 未解·**⛔ 阻塞本單**（本單用 A／B 活體差） |
| 通用化波 | 未起 |

---

*受詞 ＝ CC。工項零／一直推；`c1`／`c2`／`-251` 段乙各走側分支、逐一候 KL 放行。*

SELF_SHA256: 56a0437348b9b3e5245207383fdbcdcedb4884fb91e5fa4c198e5268be63434a
