# `W-G.9-291`　施工單（**登記 ＋ 接觸面現查·零生產碼**）：`自誤 394`／`395`／`396` ／ 既出數之**語意更正** ／ 二候選路之**接觸面**

> **態**｜開工 ＝ `7574727bb649aad329c26218937a79436480cae4`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-291_施工單_自誤394_396與二候選路之接觸面.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（登記 ＋ 純現查·**零生產碼**·⛔ 改碼一字·⛔ 開分支·⛔ 替身·⛔ 反事實）
> 🛑 **本批鑄號 ＝ `自誤 394`／`395`／`396`**（逐字全由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔒 **KL 之放行仍在·仍⛔ 動用**（其所准之改尚無可施之路）。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·**同格載產生指令**·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `7574727bb649aad329c26218937a79436480cae4` 或其後裔（**全 `40` 碼**） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·🛑 `rc` **緊接該指令**取之·⛔ 經管線）；判別力[必為祖先] ＝ 本批基座之任一祖先 |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `SELF_SHA256` 逐位相符；判別力二造須**先自證 bytes 相異** |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**） | [必真] 含 `W-G.9-291` ＝ `True`／[必偽] 含 `W-G.9-290` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`383`**／**`393`**｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`** |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec 多咬之子層檔數 **須 `> 0`** |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**、payload 之落地形機驗、**函式名二形並取**、**頂層全域取 `t.body`**、**`rc` 緊接指令**）
**逐項適用·⛔ 省**。
🆕 **一則新附款**（`W-G.9-290` 自捕三所立·⛔ 省）：凡出艙**向量之夾角**者，同格**釘其槽**（**線向**抑或**法向**），
並以**該物之實邊**機驗（`\|cos\| ≥ 1 − 1e-6`）；⛔ 逕以槽向量充線向。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-290` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `0d88b18a47e56984…`）／
生產碼 `34` 檔於 `39ab6e32..7574727b` 異動 **`0`**／**四簿逐位未動**（坐實⛔ 鑄號）／
分支 `verify/W-G.9-289-gb170` 於**母體 ＝ `git ls-remote origin 'refs/heads/verify/*'` 之全部命中列**內命中 **`0`**（⛔ 開·對照 `p3a` 命中 `1`）／閘 `(iv)` **`8`／`8`**（`\|cos\|` ＝ `1.0`·以 `cut_coords` 之實邊驗）
⇒ **✅ 全批核可**。
🩸 三則自捕**皆朝「機制不存在」之方向失準**，皆於出艙前攔下；**三者之責皆在發單側或及於發單側之既出數** ⇒ `§三`／`§四`。

---

## `§一`　裁

| # | 事 | **裁** |
|---|---|---|
| `1` | 主答（winner 側之近側界**由鏈交遞**取得·真 winner 格 `\|cos\| − 1` ＝ `2.22e-16`） | **受領·出艙即止** |
| `2` | 🔴 `forced` 與 `is_corner` **⛔ 一一對應**；`R4/left` 之 `is_corner` ＝ `True` ⇒ **其遠側界已 ∥SIDELINE** | **受領** ⇒ 🔑 **`GB-170` 之受詞收斂為「<u>起算點</u>」**（`cum_S` ＝ `buf`），**⛔ 遠側界之方向**。🛑 **⛔ 據此解除或收窄 `GB-170`**——其失效條件三款仍⛔ 有一款成就 |
| `3` | 🔴 既出數之**語意**（`\|cos\|` ＝ `0.0473`） | **受領其更正**：真值 ＝ `0.9988785830613192`（夾角 `≈ 2.71°`）。**結論方向不變**（`≠ 1`）。🛑 **⛔ 追改 `286R`／`287R` 一字**——以**末端追加**登記（`§四`）。責 ＝ **發單側**（單未釘槽）⇒ 鑄 `自誤 394` |
| `4` | 單 `§三-1` 稱 `R2/left@3.5m` 為「winner 側」，實測 `forced` ＝ `True` | 責 ＝ **發單側**（以**下游徵候**推定**態**）⇒ 鑄 `自誤 395`。受單側**併量真 winner 格**、二者並報、⛔ 改單 之處置 **✅ 正確** |
| `5` | 🔴 **發單側自認（本批新增）**：於回文書「已交付」而**呈送未為** | 責 ＝ 發單側 ⇒ 鑄 `自誤 396` |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-291_施工單_自誤394_396與二候選路之接觸面.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-290` 之單須 `≥ 1`／造乙 ＝ 執行期組出之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（框 ＝ `tr -cd '\r' \| wc -c`·⛔ `od`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一：三則 `自誤` 之鑄（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 394`**：**令出艙「向量之夾角」而⛔ 釘其<u>槽</u>**。`allocation_dir` 槽之向量，其**線向 ＝ `rot90(·)`**
> （碼面逐字：`_block_strip` 之切割帶兩側方向 `n_hat = rot90(allocation_dir)`）。
> `W-G.9-286 §三-3 款 5` 令「方向之 `\|cos\|`（遠側界向 vs `buf` 線向）」而**未載何者為線向、何者為法向**
> ⇒ 所出艙之 `0.0473`／`0.0754`／`0.0473` 係**二個不同槽**之向量之內積。
> **徵候**：該數讀來似「近乎垂直」；以多邊形**實邊**機驗，二線之真 `\|cos\|` ＝ `0.9988785830613192`（夾角 `≈ 2.71°`）。
> 🔒 **其結論方向偶然仍成立**（`≠ 1` ⇒ 二線⛔ 為同一條），**而語意全錯**——`自誤 380` 之族
> （一個讀來合理之數取得常規地位），且**該誤已呈 KL**（逐字見 `§四(2)`）。
> **族** ＝ 受詞界定（`自誤 381` 之代理量族）。
> **攔法**：凡令出艙向量之夾角者，同格**釘其槽**（線向／法向），並令以**該物之實邊**機驗（`\|cos\| ≥ 1 − 1e-6`）。

> **`自誤 395`**：**以一個<u>下游徵候</u>推定一格之「態」，而未現查該態之旗標本身**。
> `W-G.9-290 §三-1` 以「`R2/left@3.5m` 之藍影閘求值 `2` 次且 `ok=True`」推定該格為 **winner 側**，
> 當場重量得其 `forced` ＝ **`True`**（即 `forced` 側）。
> **徵候**：該推定**看似必然**（藍影只對第 2 宗求值），實則 `forced` 與 `is_corner` **⛔ 一一對應**。
> **族** ＝ `自誤 381`（以代理量充性質）。
> **攔法**：凡指名一格之**態**，一律現查**該態之旗標本身**（`forced`／`_fo_*`）；⛔ 以其下游徵候推定。

> **`自誤 396`**：**於回文書「已交付」，而交付之末一步（<u>呈送</u>）未為** ⇒ 該句於書時為**偽**。
> **徵候**：檔確已產出且 `SELF_SHA256` 確已核 ⇒ 前二件為真，**唯第三件未為**而通篇讀來如常；
> 係受詞方（KL）詢問方顯。
> **族** ＝ 「**先跑再寫**」（`W-G.9-285`／`-286` 所立）之**發單側版**。
> **攔法**：交付之三件——① 檔已產出 ② `SELF_SHA256` 已核 ③ **已呈送**——**逐項完成後**始得書「已交付」。

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**、簡繁機檢之 `rc`；
🛑 本 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留**（⛔ 剝層），並出艙其落地形之機驗。

---

## `§四`　工項二：二則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號·⛔ 改任何原節一字）

> `(1)` **`GB-170` 受詞之收斂（`W-G.9-290` 實測·⛔ 解除、⛔ 收窄）**
> `forced` 與 `is_corner` **⛔ 一一對應**：`R4/left`（`forced` ＝ `True`）之唯一之宗 `628(1)+` 其
> `is_first_corner_marker`／`is_corner` **皆 `True`** ⇒ 其 `allocation_dir` 已由 `app.py:13990`
> 換為 `_first_corner_alloc_dir(side_mid)`（＝ `rot90_cw(SIDE)`）⇒ **其遠側界已 ∥SIDELINE**
> （`rot90(_alloc_dir_used)` 與該宗 `cut_coords` 之實邊 `\|cos\|` ＝ `1.0`·閘 `(iv)` `8`／`8`）。
> ⇒ **`GB-170` 之受詞⛔ 在遠側界之方向，而在<u>起算點</u>**：`cum_S` ＝ `buf`（`4.49975224003872`／
> `7.8185635189090075`·面積反解）而⛔ 為乙式之量。**二態之取值相異之分支 ＝ 二處**：
> `verify/stepg_pipeline.py:657` `if _fo_left:` 與 `app.py:13990` `if is_corner:`（當場現查·⛔ 採此行號）。
> 🛑 **失效條件三款仍⛔ 有一款成就。**

> `(2)` **既出數之語意更正（`W-G.9-286`／`-287`·⛔ 追改其一字）**
> 該二報所出艙之「二線之 `\|cos\|`」＝ `0.0473`／`0.0754`／`0.0473`，係 **`su · allocation_dir`**
> ——**二個不同槽**之向量之內積（`allocation_dir` 槽之**線向** ＝ `rot90(·)`）。
> 以【倉】`verify/out/WG9287R_landeffect.log` 之多邊形**實邊**機驗：`su` 確為遠側境界線之線向
> （與實邊 `\|cos\|` ＝ `1.0`）、`rot90(allocation_dir)` 確為 `buf` 帶之實切邊方向（`\|cos\|` ＝ `1.0`）
> ⇒ **二線之真 `\|cos\|` ＝ `0.9988785830613192`**（夾角 `≈ 2.71°`）。
> 🔒 **結論方向不變**（`≠ 1` ⇒ 二線⛔ 為同一條）；受影響者為**該數之語意**
> （`0.0473` 讀來似「近乎垂直」）。🛑 **⛔ 判孰誤、⛔ 追改 `286R`／`287R`**；其更正以本節為準。
> 🔴 **該誤已呈 KL**（發單側逐字：「二者夾角之餘弦 `0.047`〜`0.075`（≈ 近乎垂直）」
> 及由之導出之「愈往街廓深處愈大／`@BASELINE` `730 m`」）⇒ **發單側已於本批向 KL 更正**；
> `自誤 394` 載其形。

🔒 二落點皆須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之二造、簡繁機檢之 `rc`；
🛑 本二 payload **⛔ 含 `###` 標題列** ⇒ **原樣保留**（⛔ 剝層）。

---

## `§五`　工項三：二候選路之**接觸面現查**（🛑 **⛔ 擬實作、⛔ 判孰優、⛔ 改碼一字**）

受詞 ＝ 使 `forced` 側之 `cum_S` 得取乙式之量，其**二候選路**各自**須動之處**（＝ 接觸面）。

| 款 | 候選路 | 須出艙（逐處具名 `檔:函式`·**⛔ 擬其碼**） |
|---|---|---|
| `1` | **路甲：擴 `_build_corner_range_v3` 之回傳** | `(a)` 其**全部呼叫端**（母體 ＝ 生產碼 `34` 檔 ＋ `verify/` 子層·正面列舉檔數·**二形並取**·**先驗唯一性**）；`(b)` 逐處之**回傳消費式逐字**（是否逕用 `rng`、是否解包）；`(c)` 若改為回傳 tuple／dict，**須同批改之呼叫端數** |
| `2` | **路乙：自 `corner_range_polys[(blk,side)]` 取其遠側境界線** | `(a)` 該物於**每一** `forced` 消費端之可達性（三態·同 `W-G.9-289 §三` 之框）；`(b)` 其**現有消費端全部**（前批得「唯一者為 `_lot_gate` 之藍影閘·觀測模式」⇒ **本批重量並出艙其框與母體**）；`(c)` 自該多邊形取其遠側境界線**所需之運算**逐項具名（🛑 **⛔ 判其是否構成「另算第二份」**——該判屬發單側） |
| `3` | **二路共同** | `verify/stepg_pipeline.py:657` 之 `if _fo_left:` 分支（當場現查其行·⛔ 採此號）其 `cum_S` 之**二支賦值式逐字**；及 `app.py` 同形之處（若有·逐處具名·**⛔ 本批改**） |
| `4` | 判別力二造 | [必命中] `_build_corner_range_v3` 之呼叫端數 `≥ 1`／[必為零] **執行期組出**之人造名（**先驗現母體命中為 `0`**）須 `0`。任一不成立 ⇒ **判器紅、停、上呈** |

🛑 **出艙即止**：⛔ 判孰優、⛔ 擬任何實作形、⛔ 改碼一字、⛔ 開分支、⛔ 動用 KL 之放行、
⛔ 鑄任何 `GB`、⛔ 解除或收窄 `GB-170`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§六`　收工閘

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob） | `0`；判別力[必非零] ＝ 一已知含 `CR` 之倉內 blob |
| `4` | 四簿期末 | 自誤 相異 **`386`**／MAX **`396`**（＝ 期初 ＋ **三**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` `git diff --numstat` 輸出**空** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add`；⛔ 以「意思有到」充命中 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修** |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-289-gb170` 命中 **`0`**（⛔ 開）；`p3a` 命中 `1`（判別力[必存在]） |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§七`　🛑 **⛔ 為之事**

⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 擬任何實作形、⛔ 判二路孰優；
⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／任何函式簽章；⛔ 動 `ALPHA_LABELS` 一字；
⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、⛔ 併線 `p3a`、**⛔ 引任何自 baseline 取得之數**；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 解除或收窄任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改 `286R`／`287R` 或任何已入倉文件一字；⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘 → `§一`〜`§三` 逐工項（**全量出艙**·⛔ `tail -N`）→ 判別力二造之判 → 一句話推導 →
收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**；凡向量之夾角，同格**釘其槽**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`GB-170`／`_build_corner_range_v3`／`_corner_buffer_S`／`corner_range_polys`／`_first_corner_alloc_dir`／
`is_corner`／`cum_S`／`rot90`／`cut_coords`／`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`ls-tree`／`merge-base`。

【單止】本單受詞 ＝ 新 CC 窗。⛔ 呈 KL。

SELF_SHA256: f7171131659d11cc35d58f91246d457061b7460333135e156adecc84cbbc5c25
