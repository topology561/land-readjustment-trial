# W-G.9-243　排序準據 **seam 抽出**（射程乙·四站）＋ 級之影響面清單 ＋ 具名 `key` 解析表 ＋ 自誤二則

> 🛑 **二段式·commit ⛔ 合併**
> **段甲（工項零–四）＝ 零生產碼** ⇒ 一 commit·CC **得逕行 push**。
> **段乙（工項五 ＝ seam）＝ 生產碼** ⇒ **獨立 commit**，**⛔ 逕行 push**；
> CC 須先出該 commit 之**完整 diff** 上呈，俟 **KL 逐字放行**後方得 push。
> 段甲若未全綠，**⛔ 進段乙**。

---

## §零　開工閘（缺一即停）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，依既有口徑（自檔首至
`SELF_SHA256:` 該列之前一個 `\n`（含該 `\n`）為止之全部位元組）重算對拍。不符 ⇒ 停機。

**`S-1`（號占用·宣告框·🔴 <u>三數分列</u>形）**　依 `CLAUDE.md` 宣告框 ①–⑤ 查 `W-G.9-243`。

🛑 **本次起改採三數分列**（承 `自誤 295` 之通則·見 §五）：
**`D1` 係檔框之款、⛔ 產生任何列命中**；**`列框 ＝ D2 ＋ D3`**。

發單側於 `2026-09-06` 當場實測（**三軸**：來源 `blob@135316e19402e2cbc72333e7862912d80cb98af8`
／母體 **全 `docs/` 560 檔**（其中 `19` 檔為 `.png`／`.jpg` 二進位讀不到）
／粒度框 **檔框 ＋ 列框**）：

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 宣告框 檔／列 | 鬆框 檔／列 | 角色 |
|---|---|---|---|---|---|---|
| **`W-G.9-243`** | **0** | **0** | **0** | **0**／**0** | 2／2 | 受詢 ⇒ ✅ **未占用** |
| `W-G.9-242` | 2 | 5 | 3 | 3／8 | 6／27 | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-244` | 0 | 0 | 0 | 0／0 | 0／0 | 對照組 乙（次號·須 `0/0`）✅ |

🔒 `D3` 之計數須依**補款 `⑤`**（「本單」款僅於該號 ＝ 該檔檔名之號時成立）。
CC 須**自行復算**上表六欄，⛔ 採信本單任一值。任一欄不符 ⇒ 停機上呈。
🔒 `243` 之鬆框 `2／2` 係 `-242` 單／報之**前瞻委派**，依 `②`／`⑤` ⛔ 構成占用。

**`S-2`（append-only）**　段甲受詞之既有各檔皆為**純末端追加**，⛔ 改其上文一字。

**`S-3`（生產路徑之錨·段乙前置）**　`verify/wf_f*.py` 五檔之內容 `sha256` 前 16 位，
發單側於 `135316e` 實測：

```
wf_f0.py 6758ea766b001b95   wf_f1.py 30e19048dfd781bc   wf_f2.py f974724d7e694ad7
wf_f3.py 226c7c5fa00464f3   wf_f4.py c40295c3f8cf8686
```

CC 須自行復算。**段乙<u>只得</u>動 `wf_f4.py` 一檔**；其餘四檔 ＋ 閘 `P` 五檔於前後皆須**逐位不變**。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-243_排序準據seam抽出與級之影響面清單.md`。⛔ 改一字。

---

## §二　工項一：宣告框出艙之**三數分列**補款（發單側之裁·`自誤 295` 之通則落地）

### `a`　受詞與體例

受詞 ＝ **`CLAUDE.md`**，**純末端追加**一節。⛔ 回改 `入倉前之號占用閘`／`號占用閘之母體`／
`號占用閘之框改宣告框`／`宣告框 D3 自稱形之邊界` 四節任一字。
字樣錨 ＝ `號占用閘之框`（CC 須報改前數）；判別力自檢 ＝ 寬字樣 `宣告框` 同格併報且須 `>` 前者。

### `b`　追加之逐字（⛔ 改一字）

````markdown
## 🔧 宣告框**出艙之形**：`D1`／`D2`／`D3` **三數分列**（`W-G.9-243` 工項一·發單側之裁 `2026-09-06`·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為補款之補款（`戒 36`）。⛔ 改 `①`–`⑤` 任一字。

🩸 **案由**　`W-G.9-242` 單之 `S-1` 表載對照組甲 `W-G.9-241` 為宣告框 `3／10`，
實測 `3／4`。真因 ＝ **量測器把 `D1`（檔名之款）誤寫成同時產生列命中**
⇒ 凡檔名含該號之檔，其每一列提及該號者皆被計入列框（該案為 `9` 列）。
🔒 **⛔ 讀為抄錄之誤**：同表 `W-G.9-242` 一列所載 `1／1` ＝ **正確之宣告框值**
（其鬆框為 `2／8`）⇒ 錯誤具**選擇性**，⛔ 抄錄所能致。
🔒 **`自誤 293` 之通則（須載框名並引正典出處）當時已遵行**——載了框名、引了 `①`–`④`，
數仍錯 ⇒ **`載框名 ≠ 框實作正確`**，該通則不足。

🔒 **補款（自本節起為正字規則）**

> **⑥　宣告框出艙之形：三數分列**
> 凡出艙宣告框之數，須**同格分列**三數：**`D1` 檔數**／**`D2` 列數**／**`D3` 列數**，
> 並明載 **`列框 ＝ D2 ＋ D3`**。
> 🔒 **`D1` 係<u>檔框</u>之款，⛔ 產生任何列命中**；**`檔框 ＝ |D1 ∪ D2 之檔 ∪ D3 之檔|`**。
> 🔒 **輔檢**：`宣告框列 ≤ 鬆框列` 恆成立；🛑 **該輔檢⛔ 足以攔下 `-242` 之瑕**
> （該案為 `10 ≤ 10` 恰好成立）⇒ **主檢須為三數分列**，⛔ 以輔檢代之。
> 🔒 **⛔ 追改任何既出之取號結論**（`常規四（九）五`）——本款只約束**今後**。
> 🔒 **`W-G.9-242` 單 `S-1` 表之對照組甲一列，其正確值為 `D1 2`／`D2 4`／`D3 0` ⇒ 檔 `3`／列 `4`**
> （`-242R §八 1` 已具名；本款只更正該值之**歸因**，⛔ 追改該報告一字）。
````

### `c`　驗收

`deletions ＝ 0`；報字樣錨二形二數。

---

## §三　工項二：**級之影響面清單**（承 `-242R §八 4`·發單側之裁：設框、⛔ 併入 AST 框）

### `a`　案由與界

`verify/wf_f4.py:798` `"級別": LV[_level(gid, blk)]` 係 `_level` 之**輸出欄消費點**，
既⛔ 在列框、亦⛔ 在 AST 框——**因其⛔ 為排序準據**，二框本就不取。

🔒 **裁**：另設「**級之落點框**」，**⛔ 併入 AST 排序準據框**。
受詞不同：AST 框問「**誰在排序**」，本框問「**級被生產／寫到哪**」。
併框會污染 `A`／`B`／`C`／`D` 角色判之母體；不設則抽 seam 時看不見影響面。

### `b`　框與母體

母體 ＝ `app.py` ∪ `verify/*.py`（**全部**·⛔ 限四檔）。
框 ＝ **列框**（量小·⛔ AST），字樣三組：`_level`／`LV`／`級別`；
併掃級字面 `級1相鄰`／`級2鄰近`／`級3非鄰近` 作**對照組**。

### `c`　產出

逐點表，每列載 `檔:列`／逐字原文／角色（**生產者** ＝ 算出級者／**消費者** ＝ 讀級者／
**字面** ＝ 註解·docstring·字串常量）。
🔒 併報一數：**`app.py` 之級字面命中**（發單側於 `135316e` 實測 ＝ **`0`**，CC 須復算）
——其意義為：級之機制**全在 `verify/` 側**，而 `app.py:23844` `import wf_f4 as _wf4`
⇒ **`wf_f4.py` 在 app 之生產路徑上**，⛔ 僅為 harness 之複刻。CC 須復現此二錨。

---

## §四　工項三：**具名 `key` 之解析表**（承 `-242R §八 5`·發單側之裁：⛔ 擴 AST）

🔒 **裁**：**⛔ 擴及「具名 `key` 函式之定義展開」**——跨模組、閉包、部分套用係無底洞，
且 `-242R` 已證人判可行（`367` 站全判、自捕漏判 `1` 站）。改以**有界之解析表**補之。

**工法**　沿用 `-242R` 之 AST 站點集（`367` 站），篩出 `key=` 之實參為 `ast.Name` 者，
逐點另列一表：`站點 檔:列`／`key` 實參名／**其定義之 `檔:列` 與原文全文**。
定義**取不到**（外部匯入、動態指派）者，須逐點具名並標 `⛔ 可解析`，⛔ 略去。

**量之預期**：`-242R §八 5` 已具名 `verify/wf_f0.py:218`／`:222` 之 `key=_key`（定義在 `:209`–`:210`）。
🛑 **⛔ 以「預期只有二站」為由略過全掃**——該數係發單側之引述，CC 須自行掃出全集。

---

## §五　工項四：自誤**二則**（**分鑄**·號須 CC 當場復算）

🔒 **末號之獨立復算**（框 ＝ `VR-091` 補款二 ① 之正字定義框 `A` **＋ 範圍框**·範圍框須**展開**；
母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔；粒度框 列框）。
發單側於 `135316e` 實測 ⇒ 相異 `287`／`MAX` `294`／`MIN` `7`／缺號 `[106]`／恆等式 `True`
⇒ **`N` ＝ `295`**、**`N+1` ＝ `296`**。**CC 須自行復算；不符即停機上呈。**

### `自誤 N`｜**宣告框之量測器把 `D1`（檔名之款）併入列計**

🩸 **案由**　見工項一 `b` 之案由（逐字同）。發單側於 `W-G.9-242` 單 `S-1` 表出艙
`W-G.9-241` ＝ 宣告框 `3／10`，實測 `3／4`。

🔒 **受詞** ＝ **框之實作**（⛔ 框之名、⛔ 所引之正典）。
🔒 **族** ＝ 宣告框（**連續第三次**：`自誤 293` → 本則）。
🔒 **加重情狀**：`自誤 293` 之通則（載框名並引正典出處）**已遵行而仍錯**
⇒ 該通則之受詞停在「有無引用」，⛔ 及於「實作是否合於所引」。
🔒 **⛔ 讀為 CC 之更正即已了結**——CC 之更正**值**正確而**歸因**（「疑係抄錄」）為偽；
發單側已當場復現瑕器，三列輸出（`3／10`・`1／1`・`0／0`）與單所載逐字相同。

🔒 **通則（自本則起）** ＝ `CLAUDE.md` 補款 `⑥`（三數分列）。

**責** ＝ **發單側**。

### `自誤 N+1`｜**單內收工閘之受詞與同單工項相斥**

🩸 **案由**　`W-G.9-242` 單 `§五` 收工閘 `③` 令「併報 `GB` 簿本批**未動**」，
而同單 `§四` 明令**追加**於該簿（五級依賴序之登記）⇒ **二者相斥**、`③` 恆不可能綠。
CC 依其**可成立之受詞**改報「`GB` 之**號**零污染」並具名更正 ⇒ 處置正確。

🔒 **受詞** ＝ **單之內部一致性**（⛔ 收工閘之內容）。
🔒 **族** ＝ 條文與工項之對拍未行（`-238` 交接文 `§六` 之外·**新形**）。
🔒 **加重情狀**：該閘之措辞係自 `-241` 單**照抄**——`-241` 未動 `GB` 簿故成立，
`-242` 動了而措辞未隨之調整 ⇒ **沿用前單樣板而未對拍本單工項**。

🔒 **通則（自本則起）**
> **凡自前單沿用之閘、驗收款或樣板措辞，出艙前須逐款對拍<u>本單之工項清單</u>；**
> **凡受詞為「某簿／某檔本批未動」之款，若本單有工項追加於該簿／該檔 ⇒ 該款須改寫**
> **（改以「**號**之零污染」「**deletions ＝ 0**」等本批確可成立之受詞），⛔ 原樣照抄。**

**責** ＝ **發單側**。

### 鑄號收工閘（`常規四（九）二`）

① `MAX` 自 `294` → `296`；② 缺號恆 `[106]`；③ **號**之零污染（`GB` 簿本批未動·本單無 `GB` 追加 ⇒ 本款受詞成立）。

---

## §六　工項五：**seam 抽出**（🔴 生產碼·段乙·獨立 commit·⛔ 逕行 push）

### `a`　射程（**乙**·KL 裁 `2026-09-06`·四站·**全在 `verify/wf_f4.py`**·🔒 逐字與行號係發單側於 `135316e` 自倉對拍）

| 站 | 現行逐字 | 準據之語意 |
|---|---|---|
| `:433` | `anchor = max(lots, key=lambda x: (x["a"], x["pid"]))` | 歸戶**錨點宗**之選定；`:484` `_level` 讀其 `["blk"]` ⇒ **級與全部距離之起算點** |
| `:488`–`:489` | `border = {gid: sorted(mina, key=lambda b: (_level(gid, b), dists[(gid, b)], b))` ／ `                  for gid in ginfo}` | **區位順序**（級 → 距離 → label）＝ 五級所要替換之**正主** |
| `:662`–`:663` | `act = [g for g in sorted(alloc - spset,` ／ `                                     key=lambda g: (min(dists[(g, b)] for b in mina), g))` 🔒 **鍵在續行 `:663`** | 全域佇列序（純距離優先·裁定 K） |
| `:689` | `gs = sorted(requests[blk], key=lambda g: (dists[(g, blk)], g))` | 同塊內請求者序（距離優先·裁定 K） |

🛑 **⛔ 及於**：`-242R` `A` 桶其餘 `15` 站（標的宗選定、`§7-5` 第2梯、F.2／E0 目標塊宗選定、
分攤剔除 `:703`）——受詞與五級不同，本單**僅登記為 seam 之未來擴展面**、⛔ 動。

### `b`　seam 之形（發單側之裁·⛔ CC 自創形）

1. 每站抽為一**具名 provider**，置於 **`verify/wf_f4.py` 同檔**（**⛔ 新增檔**、⛔ 動其他檔）。
2. 形：`def _key_<name>(...) -> tuple:`，**回傳與原 lambda 逐位相同之 tuple**。
   閉包變數（`dists`／`_level`／`ginfo`／`mina`）**以參數傳入**，⛔ 靠全域或自由變數。
3. 呼叫端改為 `key=lambda x: _key_<name>(x, …)`（或直接 `key=_key_<name>` 若無需捕獲）。
4. 置一 registry 作**單一掛點**：
   `_SORT_KEYS = {"anchor": _key_anchor, "border": _key_border, "queue": _key_queue, "share": _key_share}`
5. 🛑 **⛔ 於本批新增任何替代準據**——registry 每鍵**只有一個實作**；
   **⛔ 開關、⛔ 參數化準據、⛔ 條件分支、⛔ 預留 `if` 位**。

### `c`　⛔ 動者（逐項·任一被動即停機）

- `_level` 之內容（`:483`–`:486`）、`LV` 表（`:487`）、`dists`／`med`／`mina`／`adj_pub` 之算法：**一字不動**。
- `verify/wf_f0.py`／`wf_f1.py`／`wf_f2.py`／`wf_f3.py`、`app.py`、閘 `P` 五檔：**一字不動**。
- **⛔ 執行 `verify/run_all.py`／`run_verification.py`**（`R2` 中止 ⇒ 其無判別力，且會改寫已追蹤 log）。

### `d`　驗收（🔴 **逐值迴歸不可得**·⛔ 以其不可得為由放寬）

🛑 二情境皆於街廓 `R2` 中止（`-241R §三 c`）⇒ `F.2`／`F.4` **一筆未產** ⇒
**⛔ 以 `got_*.csv` 逐值不變為驗收**。改以下列**三重替代證據**，**缺一即不得放行**：

**`V-1` 靜態等價之逐行人閱**
報告須**逐站併列**「改前 lambda 全文／改後 provider 全文」，並逐字指出二者之差
**僅在名字與參數傳遞**。凡出現任何算式、比較子、tie 次序之改動 ⇒ **停機上呈**。

**`V-2` 單元級逐位對拍**（CC 自寫·**拋棄式**·⛔ 入倉·⛔ 動 `verify/`）
於**同一 process** 內保留原 lambda 之副本，斷言
`_key_<name>(x, …) == old_lambda(x)` **逐位相同**（含 tuple 之型別與元素順序）。
輸入之取得：優先取倉內既有資料；因 `R2` 中止而取不到真實輸入者，**得以構造之合成輸入**行之，
惟報告須逐字載明其為**合成**、及其**覆蓋之型別與邊界**——
**須含 tie 之情形**（`:433` 之 `a` 同值、`:488` 之同級同距、`:662`／`:689` 之同距）。
報告載：對拍筆數、不符筆數（須 `0`）、合成輸入之逐項具名。

**`V-3` 檔級雜湊**
`verify/wf_f*.py` 五檔 ＋ 閘 `P` 五檔之 `sha256` **前後各報一次**；
**只有 `wf_f4.py` 得變**（改前 `c40295c3f8cf8686`），其餘九檔**逐位不變**。

### `e`　放行（🛑 ⛔ 繞道）

段乙之 commit **⛔ 逕行 push**。CC 須先出**該 commit 之完整 diff**（`git diff --stat` ＋ 全文）
連同 `V-1`／`V-2`／`V-3` 上呈，**俟 KL 逐字放行**後方得 push。
🛑 **⛔ 以「段甲已綠」「diff 很小」「純重構」為由自行 push。**

### `f`　待落地登記（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` 末端·⛔ 改一字）

````markdown
## 🔒 待落地項登記：**閘 `P` ⛔ 涵蓋生產路徑上之 `verify/wf_f*.py`**（`W-G.9-243`·發單側於 `135316e` 實測）

🛑 **本節⛔ 鑄 `GB` 號**——其為**量測面之缺口**、⛔ 阻塞項（現行行為無誤）。

🩸 `app.py:23844` `import wf_f4 as _wf4`；`app.py:14095` 逐字載
「app 路徑（『執行七級調配』→ `_build_wf_ctx` → `wf_f4.compute`）」
⇒ **`verify/wf_f0`–`wf_f4` 在 app 之生產路徑上**，⛔ 僅為 harness 之複刻。
而閘 `P` 之五檔 ＝ `app.py`／`selection_pipeline.py`／`run_verification.py`／
`stepg_pipeline.py`／`run_all.py` ⇒ **`wf_f*.py` 五檔無雜湊閘**。

🔒 **⛔ 於本單擴充閘 `P` 之定義**（其為常規之修，另議）；
本單以工項五 `V-3` 之**逐批併報**代之，並登記此缺口候裁。
````

---

## §七　產出與 push

**段甲**（工項零–四）＝ 零生產碼 ⇒ 一 commit·CC **得逕行 push**。
報告須逐項載：`S-1` **六欄**之自行復算值、`S-3` 五檔雜湊、工項一之 `deletions` 與字樣錨二形二數、
工項二之逐點表與二錨（`app.py` 級字面 `0`／`import wf_f4`）、工項三之全表與 `⛔ 可解析`之逐點具名、
`N`／`N+1` 之自行復算值與鑄號收工閘三項。

**段乙**（工項五）＝ 生產碼 ⇒ **獨立 commit·⛔ push**，依 `§六 e` 上呈候放行。

🔒 凡出艙任何命中數，須同格載**三軸**（來源 commit ／ 母體 ／ 粒度框·`VR-091`）。
🔒 宣告框之數一律依補款 `⑥` **三數分列**。

SELF_SHA256: 99272dfd5ab110b70cfd7ef6342a40b60aab831e3a89a135668701b879d7060b
