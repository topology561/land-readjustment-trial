# 交接文_W-G.9-252（發單側 · `claude.ai` 窗轉換）

> **受詞 ＝ 下一個 `claude.ai` 窗**，非 CC。
> CC 收此文之處置 ＝ **原封入倉 `docs/orders/`，⛔ 執行其所列之任何工項**。
> 入倉前依現行條款查號占用（**宣告框**·三數分列 ＋ 檔框聯集式）。

---

## §一　態錨（`2026-09-07`·本窗最後一次 `fetch`）

```
origin/wip/s1-endpart = 5d054f2fcd4b3ab97f9828dc8a9c36fa53afe65a
追蹤 1685 檔 ／ .md 608 檔 ／ docs/ 586 檔（含 19 二進位）／ left-right = 0 0
遠端 heads 15 支（側分支 11 支·皆保留未刪）
```

**生產路徑十檔 ＋ 夾具**（內容 sha256 前 16·本窗於 `5d054f2` 實測）：

| 檔 | sha256 |
|---|---|
| `app.py` | `4860f10828fb8ae9` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` |
| `verify/run_verification.py` | `d0d531f7dc051b65` |
| `verify/stepg_pipeline.py` | `c8d46d2e5fb79dc5` |
| `verify/run_all.py` | `4ba89fef90979491` |
| `verify/wf_f0.py` | `6758ea766b001b95` |
| `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| `verify/fixture_wf_ns_wiring.py`（**⛔ 屬十檔**·另計） | `b17826a6f7b98bf5` |

🛑 **「十檔」之定義 ＝ 閘 `P` 五檔 ＋ `wf_f*` 五檔**（`W-G.9-245_CC交接文.md:19`）。
夾具**⛔ 在內**——本窗曾誤將其計入而得「四變六不變」（`自誤 307`）。

自誤簿（**定義框 ∪ 範圍框展開**·母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔）：
相異 `305` ／ `MAX` `312` ／ `MIN` `7` ／ 缺號 `[106]` ／ 恆等式 `True`。
🔒 **範圍框須展開**（`## 自誤 NNN〜MMM` 之攢批標題）——只用定義框會得 `[106, 170–182]`。

號占用（**宣告框**·`檔框 ＝ |D1 ∪ D2之檔 ∪ D3之檔|`）：
`249` `0/0`（**已出單·未入倉**）／`250` `0/0`（接閘單·**預告保留**）／`251` `5/9`（**已占**）／`252` `0/0`／`253` `0/0`。

**⛔ 採信上表任一值**——接手窗須當場 `clone` 並以 `git cat-file blob` 復現。

---

## §二　接手窗必須先知道的四件事

**一　`claude.ai` 側可以直接讀倉，也可以跑幾何。**

```
git -c core.autocrlf=false clone https://github.com/topology561/land-readjustment-trial.git
git checkout wip/s1-endpart
```

界限：只看得到已推送者；CC 之工作樹看不到；容器不留，每窗須重 clone；**⛔ 推送**。

**二　`git ls-tree` 之 `core.quotepath` 會把 CJK 檔名跳脫成 `\NNN` 形。**
🔒 **凡對倉內檔名之任何計數，一律加 `-c core.quotepath=false`。**

**三　出單前必須重新 `fetch`，且<u>行號一律當場對拍</u>。**
🩸 本窗於 `-249` 出艙前自捕：`k6b_stage2_global_order`／`k6b_stage1_locks` 之行號沿用前批舊態
（`:13322`／`:13559`），真解 `:13355`／`:13592`——`-248`／`-251` 動過 `app.py` 致位移。
**⛔ 沿用任何前批之 `app.py` 行號。**

**四　🔴 本窗之自誤率高，且集中於<u>三個受詞</u>。**
本窗鑄 `295`–`313`（`19` 則），族群分佈：

| 族 | 則 | 共相 |
|---|---|---|
| **宣告框** | `295`／`307`／`313` | 載框名 ≠ 框實作正確；檔框逕取 `D1` |
| **單內部相斥** | `296`／`297`／`303`／`311`／（`-251` 工項編號·未鑄） | 同節二款彼此、或條文與工項相斥 |
| **驗收前提／涵蓋面未查** | `300`／`301`／`306`／`310`／`312` | 以「某輸出可得」立驗收而未查其路徑；`V-B`／`V-K` 未涵蓋單自身之意圖 |

🔑 **攔截者幾乎全非機檢**：`290`–`294` 由 KL 反問／`295`–`298` 由 CC 獨立復算／`310`／`312` 由 **KL 目檢 UI**。
接手窗須把「先窮查再開口」提到出艙前，⛔ 靠 KL 或 CC 當偵測器。

---

## §三　KL 所裁之域裁（本窗新增·全部已入典或待入典）

| 裁 | 內容 | 落點 |
|---|---|---|
| `K-9-1` ⑧–⑪ | 最小分配面積之語意與新式／四裁（粒度丙·級距甲·`0`＝無規定·不合格入五級名單）／街角 `max` 式／即算即驗 | `docs/配地計算總規格_v3.md` 末端 ＋ 更正標記 |
| 藍影閘一之受詞 | **物理第 2 宗（碼側第 1 宗）自身之 `G`**·`K-6:3261` 為上位錨、`v3:893` 已標更正 | 同上（`v3:936` 更正標記） |
| 五級依賴序 ＋ 替代證據放行 | `1 K-9-1 → 2 區外道路 → 3 seam(已成) → 4 五級 → 5 GB-67/telescoping → 6 事後合併重算` | `W-G.4` 登記表 |
| **段三 `4` 之 winner（裁乙）** | 依段二之序**逐一嘗試·先達標者即 winner**，⛔ 再比指數 | `-249` 單 `§二 c`（**未入倉**） |
| `RD` 之自動偵測（`2026-09-07`） | `FRONTLINE` 中點 → 各 `RD` `CENTERLINE` 垂距 `min`；皆 `> 8.0 m` ⇒ 留空；**只在無值時填入** | `-251` 單 `§四 b` |
| `K-9-1` 面板位置（裁丁） | 移出 `⚙️ 街角地參數` expander、與其**並列**於步驟 L | `-251` 改令（**未入倉**） |
| `R4` 型撤回 | KL 逐字「現行程式處理是對的，那是我誤解」 | —— |

🔒 **授權之界（KL 裁 `2026-09-06`）**：純程式結構（射程、母體、粒度、框、工序、批之切分、量測器設計、seam 之介面形）
**得由發單側自裁**，判準 ＝ **改之不動任何一宗地之配地結果**；凡改變準據本身者恆須 KL；**界不明 ⇒ 回 KL**。

---

## §四　現況：三張單在飛

| 單 | 狀態 | 下一步 |
|---|---|---|
| **`W-G.9-251`** | 段甲已推（`d8b80e9`）；段乙**工項一停機**（意圖與 `I-2` 相斥）；**改令已出**（丁案·⛔ 入倉） | CC 依改令辦：`c1` ＝ 工項二＋三（路名管理／`RD` 下拉）·`c2` ＝ 工項一（丁案）；同推 `verify/W-G.9-251-c1` |
| **`W-G.9-249`** | **已出單·未貼給 CC·未入倉**（`SELF_SHA256: 53654fb5c370d5c7…`） | `K-6` 段三落地·第一分支·**觀測模式**。序上⛔ 與 `-251` 衝突（`-251` 純 UI） |
| **`W-G.9-250`** | 預告保留 ＝ **接閘單**（依 `_lot_gate` 之 verdict 剔宗） | 須先看 `-246′` 之 `V-S` 統計 |

🛑 **`-249` 與 `-250` 皆有土地後果**，須逐 commit 放行；`-251` 純 UI、零土地後果。

### `-249` 之要旨（接手窗須讀單全文·此僅索引）

`VR-086` 逐字：「現行碼實作**段三-6 之結果而無段三-1〜5 之過程**」。
⇒ `R2` 左之 `308.17㎡` 變抵費地**不是判準判它該變**，是中間五款一步沒跑就掉到最後一款。
本批**只補過程、先量**（觀測模式·`winner` 與強制抵費地一格不動），產出「**若接閘，`R2` 左由誰拿到**」之表，
KL 看過再裁接閘。射程**只做段三 `2` 第一分支**（第二分支需切割 `RD` 多邊形·`R2` 用不到·登記待落地）。

---

## §五　待落地（**接手窗須自倉重生此表·⛔ 沿用本表**）

| 項 | 狀態 | 依賴 |
|---|---|---|
| `-251` 段乙（丁案 ＋ 路名管理 ＋ `RD` 下拉） | ⬜ | 改令已出·候 CC |
| `-249` `K-6` 段三（觀測） | ⬜ | 單已出·候貼 |
| `-250` 接閘（`_lot_gate` 剔宗 ＋ 整包回滾） | ⬜ | 須先看 `-246′` 之 `V-S` |
| 段三接閘（依段三結果定案 ＋ 段四重排） | ⬜ | 須在 `-249` 之後 |
| 段三 `2` 第二分支（`RD` 切半各歸各側） | ⬜ | `-249` 工項三之登記 |
| 區外道路清單（`r3` 之另一半） | ⬜ | `-251` 之路名管理已含其資料層 |
| **五級實作**（`r1`–`r8`） | ⬜ | 須 `K-9-1` ＋ `r3` 齊備 |
| `GB-67`／telescoping | ⬜ | 二者致 `R4`／`R5`／`R6` 逐值迴歸不可得 |
| 閘 `P` ⛔ 涵蓋 `wf_f*` | ⬜ | `W-G.4` 已登記 |
| `app.py` `main()` 路徑無迴歸 harness | ⬜ | 同上 |
| `wg942_append_audit` 之 `TARGETS` 納入 `v3.md` | ⬜ | 發單側已裁納入·未辦 |
| `GB-143` heredoc（CC 側·計數器 `8`） | ⬜ | session 開啟方式 |
| 通用化波 | ⬜ | 未起 |

---

## §六　接手窗之建議首三步

1. **clone 並復現 `§一` 全部態錨**（⛔ 採信本文任一值·記得 `core.quotepath=false`·檔框用聯集式）。
2. **查 `-251`／`-249` 之執行狀態**（`docs/orders/` 有無其檔、`docs/reports/` 有無其報告、側分支 `verify/W-G.9-251-c1` 是否存在）。
3. **⛔ 逕自決定 `-249` 與 `-251` 之先後** —— 二者不衝突，但 KL 之注意力有限；先問 KL 要先辦哪一張。

---

## §七　未答之題（⛔ 待 KL）

**無。** 本窗所提之題（`K-9-1` 四裁、藍影受詞、依賴序、seam 射程、`RD` 偵測三題、段三 winner）KL 已全數答畢。

接手窗第一次需要 KL 之時點，預期為：
- `-251` 段乙復驗後之放行（純 UI·一句實務語言即可）
- `-249` 之 `V-S` 觀測表出來後：**是否接閘、`R2` 左由誰拿到**
- `V-K` 目檢之回報（`-251` 併線後）

🔒 **呈 KL 放行之形**（本窗所立·KL 自陳不懂程式）：發單側先自倉逐位復驗，
再以**一句實務語言**呈 KL（如「本批⛔ 改變任何一宗地之配地結果」），**KL 放行的是那句話、⛔ 那段程式碼**。

---

*本文受詞 ＝ 下一個 `claude.ai` 窗。CC 之處置 ＝ 原封入倉，⛔ 執行。*

SELF_SHA256: 1783ad6842e4be922c5a013f9e9c5ae94e69097c443e0c579f88e61514852811
