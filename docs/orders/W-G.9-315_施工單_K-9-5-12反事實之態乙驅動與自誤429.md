# `W-G.9-315`　施工單（**中**）：`K-9-5-12` 反事實之**態乙**驅動（二替身·⛔ 落地） ＋ `自誤 429` 之鑄

> **態**｜開工 ＝ `74ccd477fa3329f82573bda9b390a263155b12dc` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-315_施工單_K-9-5-12反事實之態乙驅動與自誤429.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **中**（**零生產碼**·**⛔ `run_verification`**·**⛔ 改生產碼 `34` 檔一字**·**得新增一支探針**·**得觸及 `verify/out/` 之本器落檔**·⛔ 開分支·⛔ 修批）。
> 🛑 **本批鑄號 ＝ `自誤 429`**（**一號**·逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；**⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`**。
> 🔴 **修批之號仍為 `W-G.9-309`**（於 `74ccd47` 態宣告框六數皆 `0`·保留）；**本單⛔ 占其號**、⛔ 辦修批。
> 🔑 **序**：**① `§二` 工項零** ⇒ **② `§一` 開工閘** ⇒ **③ `§三` 工項一**（器之新增·單獨 `commit`）⇒ **④ `§四` 工項二**（二替身之驅動與出艙·單獨 `commit`）⇒ **⑤ `§五` 工項三**（`自誤 429`）⇒ **⑥ `§六` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-314R` 之復驗（發單側自倉獨立·態 `74ccd47`·`core.quotePath` 預設）

| # | 項 | 發單側實得（產生方式·框同格） | 判 |
|---|---|---|---|
| `1` | 主線 | `74ccd477fa3329f82573bda9b390a263155b12dc`；`7016c99..HEAD` ＝ `4` 個 `commit`；受改 `3` 檔·**刪除欄全 `0`**（框 ＝ `git diff --numstat` 之刪除欄）·全在 `docs/` | ✅ |
| `2` | 生產碼 `34` 檔 | 逐檔 blob 相異 ＝ `0`；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；判別力[必不命中] `verify/` 子層 `*.py` ＝ `330`（`> 0`） | ✅ |
| `3` | 工項零之逐位 | 倉內單 `28206` B·`CR` `0`·`sha256` `82d7ccfe65ddbe1a…` ＝ 發單側交付之源**逐位相符** | ✅ |
| `4` | 工項一 | 自誤簿前 `903575` B 前綴逐位相符；期末 `910123` B；追加段 ＝ `payload427`（`3163` B·**⛔ 含其末換行**）＋ `NL` ＋ `payload428`（`3383` B·同框）＋ `NL` ＝ `6548` B；blob `cc194124…`→`1b821133…` | ✅ |
| `5` | 四簿 | 自誤 `418`／`428`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `39`／`40`（器 ＝ `probe_WG9267_issuer_measurers.py registry`） | ✅ |
| `6` | 自限 | `GB` 簿 blob `96755bc3…`／`CLAUDE.md`／`K-6`（`234f8f9d…`）未動；`baselines` `a898f4e1…`·`298` 列；`refs/heads/verify/*` `20` 支；`verify/out/` 受改 `0` 列·`913` 檔 | ✅ |
| `7` | 母體 | 全 `docs/` `818`→`820`；全倉追蹤 `2421`→`2423` ＝ 舊值 ＋ 單 ＋ 報告 | ✅ |

### `§零-2`　裁（**`5` 項**）

1. **`W-G.9-314R` 之受領**：工項零、開工閘 `13`／`13`、工項一（`自誤 427` ＋ `自誤 428`）、`§三-3` 五款機驗、收工閘八閘 ⇒ **全部受領**。閘 `3` 🟡 二項之**具名豁免**成立。自捕 `0` 則併具「⛔ 讀為本批無誤」之節制 ⇒ 受領。

2. 🔴 **發單側之責 ⇒ 鑄 `自誤 429`**：`W-G.9-314` `§三-3` 款 `3`／`5` 之期值只載數而**未同格載其框**（bytes 之末換行歸屬／命中之列框或子字串框及其時點），致 CC 與發單側就同一受詞各得一組真值而須對帳。**CC 之二報皆正確**、⛔ 追改。逐字見 `§五`。

3. 🔑 **`Δ=2.425` 之成因已於倉內完成值空間定位**（發單側讀 `GB-168` 原節 ＋ 一次精化 ＋ 二次精化 ＋ 現態復現之全部末端追加後之**引述**·⛔ 新量）：
   `(a)` 拋出者係**第二閘**（`verify/stepg_pipeline.py:1471`·判定組 ＝ 兩端點差）；第一閘（逐界面 `Δ_i`·`:1461`–`:1462`）因**界面數 `0`** 而不成立。
   `(b)` **理論側**餵 `_宗地寬度` ＝ `round(S × |cos(d̂, n_alloc)|, 2)` ＝ **`14.01`** ⇒ `R(14.01)` ＝ **`87.835`**。
   `(c)` **實跑側**用 `rw_increment(W_prev, W_cur)`，實參 `W_prev` ＝ **`0.1775004071`**／`W_cur` ＝ **`14.1988975422`** ⇒ `R(W_cur) − R(W_prev)` ＝ `88.496141 − 3.088507` ＝ **`85.407634`**。
   `(d)` 其**二項貢獻**（`GB-168` 一次精化逐字）：所餵之 `W` 之別 **`−0.661141`**／`W_prev` 之扣除 **`+3.088507`**；合 `2.427366`。
   `(e)` 碼面逐字 `W_cur = W_prev + S` ⇒ **`W_cur − W_prev` ＝ `S` ＝ `14.0213971351`**。

4. 🔺 **本批之受詞（發單側之假說·🛑 ⛔ 據以落地、⛔ 據以判孰為正確）**：
   `K-9-5-12`（KL 裁 `2026-08-12`：**首宗起點一律為 `0`**）若落地，則鏈頭之 `W_prev` ＝ `0` **且** `W_cur` ＝ `0 + S` ＝ `S`；其時實跑側 ＝ `R(S) − R(0)` ＝ `R(14.0214)`，與理論側 `R(14.01)` **同屬絕對值制** ⇒ `Δ` 之量級由 `2.425` 降至 `S` 與其投影捨入之差（`14.0214` 對 `14.01`）所生者。
   🛑 **此係推論、⛔ 結論**——依 `ways-of-working`「自洽而直覺上有說服力之論證最危險」，其**必須實測**。本批即為該實測。

5. 🛑 **二替身之別係本批之要害**（⛔ 混同·⛔ CC 自訂）：
   · **替身 `α`** ＝ `W-G.9-278` `§四-1` 之**逐字**：鏈頭之呼叫以 `rw_increment(0.0, W_cur)` 代之（**`W_cur` ⛔ 改**）。
   · **替身 `β`** ＝ `K-9-5-12` 之**忠實**反事實：鏈頭之呼叫以 `rw_increment(0.0, W_cur − W_prev)` 代之（即 `W_cur' = S`·承裁 `3(e)` 之碼面逐字）。
   ⇒ 二者於 `R4 right` 之實跑側**必不同值**（`α` ⇒ `R(14.1989)`；`β` ⇒ `R(14.0214)`），**⛔ 以其一代其二**。
   🔒 **`W-G.9-278` 之替身僅施於 `α`**，且其驅動母體為**態甲 ∩ 情形甲**（其 `W_prev` 皆 `≤ 0` ⇒ 結構上為空操作）而**停機上呈**（`W-G.9-278R` `§二`）；`W-G.9-279` 將受詞改繫於 `R(W_0)` 之性質量測並**收窄 `GB-169`**，其 `§二-7` 所列之方向 `(a)`（**改以態乙為驅動母體**）**迄 `W-G.9-314` 未取**。⇒ 依恆常附款 `i`，本批與既有登記之關係 ＝ **擴充**（⛔ 復現、⛔ 新題）。

### `§零-3`　本單取號之宣告框（母體 ＝ 全 `docs/` 追蹤檔 **`820`** 檔（讀不到 `19`）·態 `74ccd47`·錨定框 `(?<![0-9\-])<號>(?![0-9])`·框 ＝ `D1` 檔框 ＋ `D2`∪`D3` 列框·時點 ＝ 工項零**之前**）

| 號 | `D1` 檔 | 列框 | 檔框 | 角色 |
|---|---|---|---|---|
| **`W-G.9-315`** | **`0`** | **`0`** | **`0`** | 受詢 ⇒ **未占用·可用** |
| `W-G.9-314` | `2` | `20` | `3` | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0`（依補款 `⑨` 他指排除後） | `0` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱） | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組），⛔ 採信本表。

### `§零-4`　既往同題之清單與其結論（`自誤 426` 攔法 `③`）

| 受詞 | 既有登記 | 其結論 | 與本批之關係 |
|---|---|---|---|
| `Δ=2.425` 之值空間定位 | `GB-168` 原節 ＋ 一次精化（`W-G.9-274`）＋ 二次精化（`W-G.9-278`） | 已收斂至二項貢獻並具名拋出者 | **⛔ 重測**·本批僅**引述** |
| `W_0 → 0` 之反事實（態甲 ∩ 情形甲） | `W-G.9-278R` `§二`·替身 `α` | **停機上呈**（造甲母體結構上為空）；所需之裁候發單側 | 本批以裁 `5` 重立判別力並改驅動母體 ⇒ **擴充** |
| `R(W_0)` 之性質 ／ 情形乙對拍 | `W-G.9-279R` `§一`／`§二`；`GB-169` 之收窄 | 受詞已改繫、`GB-169` 已收窄 | **⛔ 重測**·本批⛔ 涉 |
| 二態之配地宗集合 | `GB-168` 原節：`0m` 甲 `43`／乙 `20`；`3.5m` 甲 `45`／乙 `22` | 已登記 | 本批以其為**外部錨對拍**·⛔ 重測甲態 |
| `GB-170` 三格 | `116.08`／`308.17`／`226.01`（`W-G.9-312`／`313` 已登記其二態不變） | 已登記 | 本批以其為**外部錨對拍** |
| `.gitignore`（恆常附款 `g`） | `:30` `verify/out/got_*`／`:31` `verify/out/W-D.4_*` | 本批落檔**⛔ 取 `got_` 或 `W-D.4_` 起首之名** | 見 `§四` |

---

## `§一`　開工閘（**`14` 項**·資料列之數見本節末之機驗·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準**（框同格） |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首 |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `74ccd477fa3329f82573bda9b390a263155b12dc` 或其後裔（`merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` 逐位相符；🔴 ＝ `0`；🟡 **逐項具名處置**（⛔ 靜默略過·⛔ 列為停機款） |
| `3'` | 受詞自證（以器所印之檔名列·框 ＝ 子字串框） | [必真] 含 `W-G.9-315` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `418`／`428`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `39`／`40` |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py` | `34` 檔；判別力[必不命中] ＝ `verify/` 子層 `*.py` 須 `> 0`（發單側於 `74ccd47` 得 `330`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**·⛔ 設 `false`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`gb170` ＝ `0`；`p3a` ＝ `1` |
| `8` | 受改檔之期初（框 ＝ blob `sha1` ＋ bytes） | 自誤簿 ＝ `1b821133c949f01602cc9a0ea76470436f5b7e1e`·**`910123` B**；底本探針 `verify/probes/probe_WG9278_w0zero.py` ＝ `85acdbe6863cc078cb569c82d6e7b3a11dbcdc3a`·`26016` B·`CR` `0`（🛑 **本批⛔ 改其一字**） |
| `9` | 本批新物之未占（**跑於 `§二` 之後**·框 ＝ 子字串框·母體 ＝ `git ls-tree -r --name-only -z HEAD`） | 檔名含 `W-G.9-315` 者 ＝ `1`；哨兵 `ZW429` 之全倉命中 ＝ **除本單自身之產物外 `0`**（逐筆具名所在檔）。發單側於 `74ccd47` 得該哨兵全倉 `0` 檔 |
| `10` | 宣告框（`§零-3` 四列·**CC 自跑**·含二對照組） | 受詢之命中**除本單自身之產物外 ＝ `0`**；對照甲 `D1` `≥ 1`；對照乙 ＝ `0` |
| `11` | `verify/out/` 之期初（框 ＝ 檔數 ＋ `git status --porcelain -z`） | 倉內 `913` 檔；本批**得新增**其下之落檔，⛔ 改既有 `913` 檔任一位元組 |
| `12` | `python -c` 讀 `.gitignore` 之 `:30`／`:31` 逐字 | `verify/out/got_*`／`verify/out/W-D.4_*`；本批落檔之名⛔ 落入該二族（恆常附款 `g`） |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `|` 起首且**⛔** 為分隔列（`|---`）者，扣表頭 `1`），與本單自載之 `14` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以列舉為準、逐項皆跑並具名。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308`、`W-G.9-308_補令一`（`a`／`b`／`c`）、`W-G.9-310`（`d-1`〜`d-3`）、`W-G.9-311`（`e`／`f`）、`W-G.9-312`（`g`）、`W-G.9-313`（`h`／`i`）、`W-G.9-314`（`j`／`k`）之恆常附款全數逐項適用**（逐字見該七單·⛔ 省）。
🆕 **本單所增**（`l`·`自誤 429` 之攔法）：
① **單所設之一切期值須同格載其框與母體**——⛔ 只載數；
② 凡以 **bytes** 為期值者，須明定**末換行之歸屬**（payload 含／不含）；
③ 凡以**命中**為期值者，須明定**列框或子字串框**，並載其量測之**時點**（`commit`）；
④ **既例之框須取「界列之整列相等框」，⛔ 子字串框**——由：`自誤 427` 之 payload 逐字引界列之字面，致自誤簿之 `<!--` 子字串框已由 `0` 變 `3`（列框 `2`）；日後再以「`<!--` ＝ `0`」為既例之對照組者必得非零而誤判器紅。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-315_施工單_K-9-5-12反事實之態乙驅動與自誤429.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名含 `W-G.9-315` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。🛑 **落地後即 `push`**，其後方跑 `§一`。

---

## `§三`　工項一：**器之新增**（主線·單獨 `commit`·⛔ 驅動）

落點 ＝ `verify/probes/probe_WG9315_k95_12_stateB.py`（**新檔**·⛔ 改底本一字）。
🔒 **底本** ＝ `verify/probes/probe_WG9278_w0zero.py`（其框內省、鏈頭判準、自我驗證閘、坑 `bb`／`bd` 之攔法**逐字沿用**·⛔ 另寫第二份判準）。

🛑 **替身之定義（發單側定·⛔ CC 自訂·逐字）**——二替身**分別**施行、⛔ 併施；每次驅動前後須還原並出艙還原之證：

> **替身 `α`**：凡 `rw_increment` 之呼叫端經**框內省**判定為鏈頭（判準一律取碼面之同一運算式 `is_corner or is_chain_head`·自呼叫端之區域名取值）者，以 **`rw_increment(0.0, W_cur)`** 代之（**`W_cur` ⛔ 改**）。
> **替身 `β`**：同一判定下，以 **`rw_increment(0.0, W_cur - W_prev)`** 代之。
> 🛑 **⛔ 寫入倉之生產碼**；⛔ 於框內省**不可判**之處施加（一律跳過並 loud 計數·`W-G.9-278R` `§二-3` 之既例）。

🛑 **判別力三造（發單側定·⛔ CC 自訂·三造之值皆自 `GB-168` 一次／二次精化逐字取·⛔ 人造之數）**：

> **造甲[必有差·純函式層·⛔ 驅動管線]**：直接呼叫生產碼之 `rw_increment`，取三值——
> `(1) rw_increment(0.1775004071, 14.1988975422)`／`(2) rw_increment(0.0, 14.1988975422)`／`(3) rw_increment(0.0, 14.0213971351)`
> ⇒ 三者須**兩兩相異**（`|差| > TOL0`）。期（`×100`·`6dp`·⛔ 據以判對錯）：`(1)` ≈ `85.407634`；`(2)` ≈ `88.496141`。`(3)` **⛔ 預寫·當場實得**。
> **造乙[必無差]**：`W_prev ≤ 0` 之鏈頭 ⇒ 替身 `α` 之 `|ΔRw| ≤ TOL0`（由：`rw_from_width` 於 `W ≤ 0` 逐字 `return 0.0`·`W-G.9-278R` `§二-5` 已載）。其格數須 `> 0`，逐格具名。
> **造丙[替身確已生效·反空操作]**：替身之「**咬到**」計數須 `> 0`，並逐段（`run_corner_pk`／`run_step_g`）與逐格具名；「不可判」之計數一律 loud 出艙。
> 🛑 **造甲 🔴 有差 ⋀ 造乙 🟢 無差 ⋀ 造丙 `> 0`** ⇒ 器非紅；**任二造同色 ⇒ 器紅 `rc = 5` ⇒ ⛔ 出艙任何反事實之數**。
> 🛑 **⛔ 以「反正都是 `0`」頂替**（`W-G.9-278R` `§二-6` 逐字）。

🔒 `TOL0 = 1e-6`（具名常數·⛔ 字面）。本工項**⛔ 驅動**，只入倉。

---

## `§四`　工項二：**二替身之驅動與出艙**（主線·單獨 `commit`）

驅動母體 ＝ **態乙**（`WV_K6_STEP0=off`·＝ `K-9-29 二` 所令之態）；情境 ＝ `0m` ⋀ `3.5m`（其二情境由器一次驅動內含·如 `W-G.9-312` 之落檔）。
落檔 ＝ `verify/out/WG9315_alpha.log`／`verify/out/WG9315_beta.log`（🛑 **名⛔ 落入 `.gitignore` 之 `got_*`／`W-D.4_*` 二族**·閘 `12`）。
🛑 **驅動次數 ＝ 二**（`α` 一次·`β` 一次）；⛔ 逾此；⛔ 跑 `verify/run_verification.py`。

**逐替身出艙之項（`1`〜`5`·⛔ 省·每項同格載其框、母體與產生指令）**：

| # | 受詞 | 框 |
|---|---|---|
| `1` | `R4` `right` 之 `理論@k*`／`實跑(階段1)`／`Δ`（**未捨入** ＋ `2dp`）與該結構閘**破否**（破則其訊息逐字） | 值框·母體 ＝ 該側之定案宗 |
| `2` | 六街廓 × 二側之**到達與判**（🟢 已執行且通過／🛑 未執行／🔴 已執行且破／⛔ 可得），二情境各一表 | 格框·母體 ＝ `24` 格 |
| `3` | **配地宗集合之宗數**（`0m`／`3.5m`），並與 `GB-168` 原節所載之甲 `43`／乙 `20`、甲 `45`／乙 `22` **逐位對拍**（⛔ 重測甲態） | 宗框·母體 ＝ 該情境之配地表 |
| `4` | **逐街廓之 `抵費地面積＝range(㎡)`**，並與 `116.08`／`308.17`／`226.01` **逐位對拍** | 值框·源 ＝ `run_corner_pk` 之第三回傳 `_off` |
| `5` | `ΔΣG`／`ΔΣ抵費地面積`（替身開／關之差·關 ＝ 同次驅動內之未替身基線） | 值框·母體 ＝ 全案 |

🛑 **`α` 與 `β` 之表須分列並置**，同格載其差；**⛔ 以其一代其二**、⛔ 合併。
🛑 器紅（判別力三造未全成立）⇒ **⛔ 出艙項 `1`〜`5` 任一數**，停、上呈。

---

## `§五`　工項三：**`自誤 429` 之鑄**（主線·單獨 `commit`·嚴格末端追加）

落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（⛔ 改上文一字）。
🔑 界：`<!-- ZW429-BEGIN -->`／`<!-- ZW429-END -->`（**定界以整列相等為之**）；**payload ＝ 二界列之間**，**界列⛔ 隨 payload 入簿**（恆常附款 `j③`）。
🛑 凡含反引號之文字一律**以檔案管線**為之（恆常附款 `h`）。

<!-- ZW429-BEGIN -->

### 🩸 `自誤 429`　**單所設之期值只載數而未同格載其框——bytes 未明定末換行之歸屬、命中未明定列框／子字串框及其量測時點，致同一受詞產生二組互不相等而皆為真之數**

**形**：`W-G.9-315` 之前批 `W-G.9-314` 之 `§三-3` 款 `3` 逐字作「期末 bytes ＝ 期初 bytes ＋ 二 payload bytes（＋界外空列之 bytes·同格具名）」、款 `5` 逐字作「全倉命中之期值 ＝ 除本單自身之產物（單／本報告）外 `0`，逐筆具名所在檔」——**二者皆只載數與判準，而⛔ 載其框**。

**徵候**：CC 報 payload ＝ `3164` ＋ `3384` B·界外空列 `0`；發單側復驗得 `3163` ＋ `3383` B·分隔 `NL` `2` ⇒ **總量皆 `6548`、連接式逐位同一**，差在「payload 是否含其末換行」之框。CC 報哨兵全倉命中 ＝ 單 `6`／報告 `5`（**列框**·`git grep -c`·量於報告入倉之 `commit`）；發單側得單 `7`／報告 `8`（**子字串框**·量於收工閘追加後之 `commit`）⇒ **框與時點皆異**；以列框於報告入倉時點復現，得 `5` ⇒ **二組皆真**。

**根因**：既規「凡出艙之數，同格載其框、母體與產生指令」逐字載於單之 `§六`（**報告之格**），**其受詞僅及於 CC 之報告，未及於單自身所設之期值**——射程之漏，與 `自誤 427` 之「`恆常附款 e` 之射程僅及開工閘」同族（**款之射程未涵蓋單自身**）。

**後果**：未動土地、未致錯鑄、未致錯裁；判準（總量逐位相符／本單自身之產物外 ＝ `0`）**皆不因框之別而改變** ⇒ 代價僅一輪對帳。CC 之二報**皆正確**、⛔ 追改。

**併記一項副作用（⛔ 鑄號·供後批）**：`自誤 427` 之 payload 逐字引了界列之字面 ⇒ **自誤簿之 `<!--` 子字串框已由 `0` 變為 `3`（列框 `2`）**；日後再以「`<!--` 於自誤簿 ＝ `0`」為既例之對照組者**必得非零**，而其非「器紅」。

**攔法（四款·立為恆常附款 `l`）**：
① **單所設之一切期值須同格載其框與母體**——⛔ 只載數；
② 凡以 **bytes** 為期值者，須明定**末換行之歸屬**（payload 含／不含）；
③ 凡以**命中**為期值者，須明定**列框或子字串框**，並載其量測之**時點**（`commit`）；
④ **既例之框須取「界列之整列相等框」，⛔ 子字串框**。

<!-- ZW429-END -->

**落地後之機驗（依恆常附款 `j`／`l`）**：
1. `probe_WG9267_issuer_measurers.py registry` ⇒ 自誤 相異 **`419`**／MAX **`429`**（缺號 `[106, 355, 356, 357]` 未變）；`GB`／`VR`／`K-9` **一字未動**。
2. **append-only**：期末 bytes 之**前綴** ＝ `§一` 閘 `8` 之期初 bytes（`910123` B）逐位相符。
3. **payload 逐位**：自本單 blob 以整列相等定界抽出 `ZW429`，與簿內落地段逐位相符；**期末 bytes ＝ `910123` ＋ payload bytes ＋ 分隔 bytes**，三者分列（框：payload **⛔ 含**其末換行·分隔 `NL` 獨立計）。
4. 🔒 哨兵 `ZW429` 於自誤簿之期值 ＝ **`0`**（界列⛔ 入簿）——**如期·⛔ 讀為器紅**；須另以 `C` 形（`` `自誤 429` `` ＝ `1`）＋ 一必為零造（**執行期組出**·字面⛔ 出艙）自證器非紅。全倉命中之期值 ＝ **除本單自身之產物外 `0`**（框 ＝ 子字串框·時點 ＝ 本工項之 `commit`）。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**；同格載受檢檔數 `> 0` |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動之 `.md` 之 `CR` | ＝ `0`；判別力[必非零] ＝ `data/V6.dxf` |
| `4` | 四簿期末 | 自誤 `419`／`429`；`GB` `168`／`170`·`K-9` `39`／`40`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（自誤簿） | 期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes（`910123` B） |
| `6` | 自限復驗 | `verify/baselines` `298` 列·`a898f4e1…` 未動；`data/` 全樹未動；`CLAUDE.md`／`K-6`／**`GB` 簿**（`96755bc3…`）blob 未動；**`verify/out/` 之既有 `913` 檔逐位未動**（本批之新落檔另列）；底本探針 `probe_WG9278_w0zero.py`（`85acdbe6…`）**一字未動**；`refs/heads/verify/*` `20` 支·`gb170` `0`·`p3a` `rc` `1`；`WV_BAKE` ＝ `None`；驅動**外**之 `WV_K6_STEP0` ＝ `None` |
| `7` | 標籤對照 | 本報告全部計數標籤逐欄具名其**框**、集合與基數（**當場以程式自資料列計數**·⛔ 轉引單之自載數）；凡命中之數並載其**時點** |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`〜`6` 任一不如期 ⇒ **停、上呈**（照實全量出艙·⛔ 判改法）。

---

## `§七`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；新增 `probe_WG9315_k95_12_stateB.py` 一支；以二替身各驅動一次並出艙 `§四` 之項 `1`〜`5`；`自誤 429` 之鑄。
其餘：⛔ 改生產碼 `34` 檔一字；⛔ 改底本探針 `probe_WG9278_w0zero.py` 一字；⛔ 新增或修改其他任何探針；⛔ `run_verification`；⛔ 改 `verify/out/` 既有 `913` 檔任一位元組；
🛑 ⛔ **落地**、⛔ 稱本批為「落地」、⛔ 判 `K-9-5-12` 應否落地、⛔ 判 `K-9-5-12（五）3.` 之「未決」後半款；⛔ 模擬**情形乙**（街角為強制抵費地之側·`W-G.9-278` `§七-5` 逐字沿用）；
⛔ 判孰為正確之 `W`、⛔ 判理論側與實跑側何者為是、⛔ 提修法主張、⛔ 擬任何 diff、⛔ 判結構閘應否放寬或停用（該閘係**承重閘**·其動須 KL 放行）；
⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170`；⛔ 鑄 `自誤 429` 以外之任何號；⛔ 動 `GB` 簿一字；
⛔ 開分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——遇可觀測之硬限事件一律 loud 照實具名、停、上呈；
🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**（恆常附款 `h`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`docs/reports/W-G.9-315R_K-9-5-12反事實之態乙驅動與自誤429_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `3` 之 🟡 逐項具名處置、閘 `10` 之自跑與二對照）⇒ `§三` 工項一（器之入倉 ＋ 判別力三造）⇒ `§四` 工項二（`α`／`β` 分列並置之五項）⇒ `§五` 工項三（四款機驗）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其**框**、母體與產生指令；凡命中之數並載其**時點**；**計數標籤一律當場以程式自資料列計數**。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框與 `§零-3` 同源；四簿框於 `74ccd47` 實跑 ＝ `418`／`428`｜`168`／`170`｜`80`／`95`｜`39`／`40`。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`registry`／`自誤 429`／`替身 α`／`替身 β`／`TOL0`／`WV_K6_STEP0`／`R4 right`／`理論@k*`／`抵費地面積＝range(㎡)`／`W-G.9-309`／`74ccd47`／`data/V6.dxf`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: 058b8442dd86ce708f490edecc743686cd09fb4eec5f8f7abad05f151a00175f
