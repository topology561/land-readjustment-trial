# `W-G.9-301`　施工單（**中**）：路丙**實作形之重釘** ／ `自誤 404`・`405` ＋ 坑 `bj`・`bk` 之鑄 ／ **自導可行性之現查**

> **態**｜開工 ＝ `e5c9924694a57ce3831873393119cb9104c8c1a2` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·⛔ 短前綴）
> **單** ＝ `docs/orders/W-G.9-301_施工單_實作形之重釘與自導可行性之現查.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（登記 ＋ 現查·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**·**⛔ 修批**）
> 🛑 **本批鑄號 ＝ `自誤 404`／`405` ＋ 坑 `bj`／`bk`**（**四號**·逐字由本單供·**`###` 形**）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔑 **`ALPHA_LABELS` 之擴至 `bk` 於本批<u>明文授權</u>**（射程逐字見 `§四`·⛔ 逾之）——`W-G.9-299`／`-300` 之「⛔ 動 `ALPHA_LABELS` 一字」於此點**撤回**。
> 🔴 **KL 之放行於本批<u>仍不動用</u>**；⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支。
> 🛑 **⛔ 辦修批**——其形已由本批之裁**重釘**，其可行性候本批工項四之現查；修批候 **`W-G.9-302`**。**此⛔ 跳序**：「受領 → 修批」之序未變，變者為**修批之受詞**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　開工閘（十一項·**每列同格載其產生指令**·`自誤 392`·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `e5c9924694a57ce3831873393119cb9104c8c1a2` **或其後裔**（全 `40` 碼·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**·`自誤 405`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 項**逐項具名處置**（本單之預告見 `§零-1`）——🔒 **判定組為空時一律照實具名為「判定組為空」**·⛔ 以「全數受豁免」出艙（`W-G.9-300R §零-1` 之正例） |
| `4'` | 受詞自證（坑 `bf`） | [必真] 器所印之檔名含 `W-G.9-301` ＝ `True`／[必偽] 含 `W-G.9-300` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`；preflight 四靶逐項成立 |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**·⛔ 自擬單框） | 自誤 **`393`**／**`403`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `28`／`29`（缺 `[]`） |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·末標籤 `bi` ⇒ 下一 **`bj`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；判別力[必不命中] 子層 **須 `> 0`**（**逐批漂移**·⛔ 定值·`自誤 386`）；🆕 **本數即本批一切母體之<u>外部錨</u>**（坑 `bj`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 `20` 個；`verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`** |
| `10` | **四物證落檔**（🔒 **框 ＝ blob `sha1`**·`git rev-parse <rev>:<path>`·**先 `git cat-file -e` 驗存在**·三態分列） | `WG9295R_joint_v1_selfpolluted.log` `1a35dc168655…`／`WG9296R_w0rec_v1_blknone.log` `f93eb2fcb7e6…`／`WG9297R_w0caliber_v1_blkabsent.log` `16742f3805e0…`／`WG9298R_gate3_v1_firstcall.log` `5cef4c8dfe7a…` ⇒ **逐位未動**；🆕 併驗本批新增之二 v1 落檔（`WG9300R_static_v1_popcollapse.log`／`WG9300R_dynamic_v1_wrongframe.log`）**存在且逐位未動** |

### `§零-1`　`P-4` 之逐項處置（**發單側已實跑·⛔ 靜默略過**）

本單經發單側實跑 `probe_order_preflight.py`：🔴 **`0`** 項。🟡 之受詞若屬下列**二類**，⇒ **受具名豁免**：
**(甲)** `→` 係**賦值鏈／消費鏈／流程節序之串接**（如 `§九` 之「開工閘 → 逐工項 → 收工閘」、`(甲)` → `_rw_start` → `Rw` → `G_target`）；
**(乙)** 方向性之詞係**序之詞而非幾何**（`前綴`／`後裔`／`首`／`末`／`前`／`後`·如 `§零` `態` 列之「或其後裔·⛔ 短前綴」）。
🛑 二類皆**⛔ 幾何向量之方向性轉引** ⇒ **無座標系可具名**。
🛑 **凡實跑所得之 🟡 項不在此二類內者，⛔ 受本豁免，須停、照實具名、⛔ 續**。
🛑 本批凡出艙**幾何向量**之夾角者，一律**釘其槽**（線向／法向）並以**實邊**機驗——該禁⛔ 在本豁免射程內。

🛑 **恆常附款逐項適用·⛔ 省**（坑 `bh`／`bi`／`bc`／`bd`／`be`／`bf`／`bg`／`g`／`3`、`GB-158` **三形分列**、簡繁誤字機檢、
`rev` 釘死、**先跑再寫**、[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、payload 之落地形機驗**繫於性質**、
函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、向量夾角**釘槽**並以實邊機驗、**逐閘出艙執行次數**、
**等待綁行程結束**、**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N`**、**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、
**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**、`f_locals` 鍵須**自證存在**並**增第二鍵**、
`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退** ⇒ 先 `cat-file -e`·**三態分列**、**本批方鑄之號⛔ 作其自身之寬字樣對照**、
**簿之數一律取器之輸出·自擬單框只作判別力對照**）。
🆕 **二則新附款**（本批所鑄之二坑·⛔ 省）：
① **母體之基數須綁<u>外部錨</u>**（＝ 本批他處獨立取得之同一量·如 `§零` 閘 `7` 之 `34`），並列為判別力造之一；`N/N` 之自證**只證一致、⛔ 證正確**（坑 `bj`）。
② 凡以 `text=True` 取**含 CJK 路徑**之 `git` 輸出者，其 `stderr` 於 `cp950` 主控台**可靜默丟失** ⇒ 凡**判繫於輸出字串**者一律**改取 bytes**（坑 `bk`）。

---

## `§零′`　前批（`W-G.9-300`）之核可（**發單側已辦·⛔ CC 重辦**）

三 `commit`（`5b39171c`／`86109af6`／`e5c99246`）經發單側自倉獨立復驗：工項零之單 **blob 內容 `sha256`** ＝
`7547a131b9d7e812a98ba9df114cb1766556901d866c244553be473409dff016`（**＝ 發單側所交付之檔逐位相同**·blob `sha1` ＝ `e78cb6d8705c369a…`·**二框並報**）；
生產碼判法 **`0`** 行（異動 `8` 檔·對照組[必非零] ＝ `2`）；逐 `commit` 刪除欄**全 `0`**；四簿**零鑄號**；
`app.py` `4379108a…`／`baselines` `298`·`a898f4e1…`／`p3a` `rc = 1`／`299-gb170` 命中 `0` ⇒ **✅ 全批核可**。

🔑 **本批之現查品質，發單側逐項具名其功**：① **二形並取⛔ 是形式要求**——單取文字層將**整組漏掉** `wf_f1`／`wf_f4` 之三處，而該三處**全為 `forced` 路徑**；
② `F` 之判**以乙形承擔**並 loud 具名甲乙二形**⛔ 同一述詞**（「可否載入」vs「本案是否走到」）；
③ `S∖F` 逐處之**分項成因**區辨「外圍未執行」與「外圍執行而分支為偽」（`S4` vs `S5`／`S6`／`S7`）；
④ `side_mid` 之靜態／動態**二量測相異**而**未取其一略其一**，並坐實其成因（同一 `for` 內、賦值在呼叫之後 ⇒ `f_locals` 之 `True` 綁**前一街廓**之值）。
🩸 **六則自捕皆於出艙前攔下、⛔ 頂替**；其第 `1` 則（母體自 `34` 靜默塌為 `1` 而原四造全綠）與第 `6` 則（`cp950` 解碼）**准鑄為坑**（`§四`）；
第 `4` 則（雜湊框）之**責在發單側** ⇒ 鑄 `自誤 405`；第 `2`（聚合鍵 climb 外層·坑 `bg` 射程內）／第 `3`（heredoc 硬閘之活體實證）／第 `5`（`%` 之 tuple）**⛔ 鑄號**。

---

## `§一`　裁（**發單側·⛔ CC 自裁**）

| # | 事 | **裁** |
|---|---|---|
| `1` | `停二` 之判 | ✅ **成就·CC 之處置完全正確**。`S∖F` 之 `6` 處**全數**為 `forced` 路徑 ⇒ 「改 `F` 即足」不成立；停、⛔ 辦工項二、⛔ 動用放行、⛔ 開分支——**逐項如令** |
| `2` | 🔴 **路丙之<u>實作形</u>** | **撤回並重釘**：受詞由「`_corner_buffer_S` 之 `allocation_dir` **實參**（於呼叫端）」改為「**`_corner_buffer_S` 之<u>函式體</u>內自導**」。⇒ `W-G.9-299 §六`／`W-G.9-300 §四`／`§五` 之明文禁令「⛔ 改 `_corner_buffer_S` 之函式體一字」**於此一點撤回**（其餘四函式之禁**不動**）。🛑 **路丙之<u>取線之設計</u>⛔ 變**（`W-G.9-299 §一` 裁 `3` 之受領**仍在**）——變者僅其**落點** |
| `3` | 裁 `2` 之由（**二項獨立·皆自本批實測導出**） | **(一) 射程**：`W-G.9-300R` 款 `4′` 實測——呼叫端 `7` 處而**全 `7` 處皆在 `forced` 守衛內**（判別力[必為否] 成立·非 `forced` 之呼叫端 ＝ **`0`／`7`**）⇒ 改**函式體**之射程**恰等於** `forced` 路徑、**⛔ 及於任何非 `forced` 之呼叫**；且其為**一處**（定義處 `1`·`app.py`）⇒ **較改 `7` 處更合** KL 射程 `(b)` 之「只改該處」。**(二) 正確性**：`side_mid` 於 `S3` 之**在域性不足**——在域之名（`_side_mid_left`／`_side_mid_right`）於呼叫當下綁**前一街廓**之值（賦值在呼叫之後·同一逐街廓 `for` 內·坑 `bg` 之形）⇒ 呼叫端之改**必取靜默錯值**；而函式體內之 `block_poly`／`side` **依構造即本街廓** ⇒ 該錯值之通道**自始不存在** |
| `4` | 🛑 裁 `2` 之**可行性** | **⛔ 逕改**——須先現查：`_corner_buffer_S` 之**現有形參**（`block_poly, d_hat, front_p1, allocation_dir, range_area, side, tol, _label`）是否足以導出「街角第 `1` 宗之臨街向」（＝ `_first_corner_alloc_dir(side_mid)` 之等價）。**此即本單工項四**；不足 ⇒ **停機上呈**（**⛔ 新增形參、⛔ 改簽章**之禁**不動**） |
| `5` | `停三` 之潛在成就（`wf_f4.py` 之頂層 `*_EXPECT` ＝ `2`） | **結構上不再有受詞**——裁 `2` 後受改之集 ＝ `{app.py 之 _corner_buffer_S 函式體}`，而 `app.py` 之頂層 `*_EXPECT` ＝ **`0`**（`W-G.9-300R` 三-4 實測）。🛑 **惟其形⛔ 撤**：列為 `W-G.9-302` 之收工閘固定項（**受改之集之各檔其頂層 `*_EXPECT` 須 ＝ `0`**）。🔒 併記：`wf_f4`／`wf_f1` 雖⛔ 受改，其**行為**仍隨函式體之改而變 ⇒ `W-G.9-302` 須**照實出艙**其 `*_EXPECT` 之成敗、**⛔ 調之** |
| `6` | `side_mid` 在域性不足之處置 | **⛔ 於呼叫端補救**（⛔ 移賦值列、⛔ 新增參數、⛔ 改簽章）——裁 `2` 已使該通道不必存在。🛑 **惟該在域性不足係一<u>既存</u>之隱患**（任何後續於該呼叫列前取 `_side_mid_*` 者皆中之）⇒ **登記於 `GB-170` 之末端追加**（`§五`）、**⛔ 鑄新 `GB`**、**⛔ 逕修** |
| `7` | 本批之鑄號 | `自誤 404`（未先現查接觸面即釘死改之位置·**責 ＝ 發單側**）／`自誤 405`（摘要值未具名其雜湊框·**責 ＝ 發單側**）／坑 `bj`（母體靜默塌陷而判別力造全盲）／坑 `bk`（`cp950` 之 `stderr` 靜默丟失）——**四號·⛔ 逾** |

---

## `§二`　工項零：**本單原封入倉**（主線）

落點 ＝ `docs/orders/W-G.9-301_施工單_實作形之重釘與自導可行性之現查.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`·[必真] 含 `W-G.9-301`／[必偽] 含 `W-G.9-300`）；檔數 ＝ `1` 之自證（造甲[必≥1] ＝ `W-G.9-300` 之單／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；**blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源**（🔒 **二框並報**·`自誤 405`）；`deletions` ＝ `0`。

---

## `§三`　工項一：`自誤 404`／`405` 之鑄（主線·落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

🔑 payload 之界 ＝ 二哨兵之間（**不含哨兵列**）·**⛔ 行號為錨**：起 ＝ `<!-- ZW404-BEGIN -->`；迄 ＝ `<!-- ZW404-END -->`。
🛑 唯一性之框 ＝ **整列<u>逐位相等</u>**（`^…$`）·母體 ＝ 本單單檔·各須命中 **`1`**；併報**子字串框**之數（本單行文另引該二字串各一次 ⇒ 子字串框必得 `2`·**二數並報**）。哨兵列**⛔ 入簿**；payload **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**。

<!-- ZW404-BEGIN -->

### 🩸 `自誤 404`　**未先現查改之<u>接觸面與守衛分布</u>，即於單內釘死「改之位置」並禁另一位置**

**形**：`W-G.9-299 §六` 與 `W-G.9-300 §四` 皆將路丙之受詞釘為「`_corner_buffer_S` 之 `allocation_dir` **實參**（於呼叫端）」，
並於二單之 `⛔ 為之事` 明令「**⛔ 改 `_corner_buffer_S` … 之函式體一字**」。二者皆**先於**接觸面之現查。

**徵候（`W-G.9-300R` 實測）**：`_corner_buffer_S` 之呼叫端 ＝ **`7`** 處（AST 二形並取；單取文字層只見 `5` 列而**整組漏掉**
`wf_f1`／`wf_f4` 之三處），其中生產期實走者 ＝ **`1`** 處（`stepg_pipeline._run_step_g_impl` 之左支·`21` 次），
而 `S∖F` 之 **`6`** 處**全數**經判為 `forced` 路徑（判別力[必為否]：`def _corner_buffer_S` 之外圍 `If`／`IfExp` ＝ `0` ⇒ 判否 ✅）
⇒ 單所設之 `停二` **成就**、修批⛔ 辦。

**根因**：「改之位置」之裁**本身**以接觸面之現查為前提。單先釘了一個位置（實參）、又禁了另一個位置（函式體），
而該禁之代價——`7` 處之逐一改、`side_mid` 之在域性不足、`wf_f4.py` 之判準載體風險——**皆在釘死之後方由現查揭出**。

**反面之實測（同批·同器）**：**非 `forced` 之呼叫端 ＝ `0`／`7`** ⇒ 改**函式體**之射程**恰等於** `forced` 路徑、
**⛔ 及於任何非 `forced` 之呼叫**，且其為**一處**（定義處 `1`·`app.py`）⇒ **較改 `7` 處更合**「只改該處」。
⇒ 單所禁者，實為**較合射程之落點**。

**族** ＝ `自誤 401`（未查證**可得性**即斷言「皆可施」）之孿生——`401` 未查可得性，本則未查**接觸面**。

**攔法**：凡單內欲**釘死一項改之位置**（或**禁某一位置**）者，須先以**獨立之現查工項**取得三事：
① 該符號之**全部定義處與呼叫處**（**AST·二形並取**·母體正面列舉·⛔ 文字層單取）；
② 逐處之**外圍守衛**（`If`／`IfExp` 之 `test` **逐字**·附[必為否]之判別力造）；
③ 逐處之**執行期是否走到**及其**分項成因**。
三者未齊，**⛔ 於單內釘死位置、⛔ 於 `⛔ 為之事` 禁某一位置**——改以「**位置候現查後由發單側裁**」出單。

### 🩸 `自誤 405`　**出艙一個摘要值，而未於同格具名其<u>雜湊框</u>**

**形**：`W-G.9-300 §零` 閘 `10` 載四物證落檔之十二碼（`1a35dc168655…`／`f93eb2fcb7e6…`／`16742f3805e0…`／`5cef4c8dfe7a…`）
而**未具名其雜湊函數**。本波**同時通行二框**——blob `sha1`（`git rev-parse <rev>:<path>`）與**內容 `sha256`**
（`git show <rev>:<path> | sha256sum`），且工項零之體例恆**二者並報** ⇒ 「摘要值」一詞於本波**必然歧義**。

**徵候（`W-G.9-300R` 閘 `10`）**：受單側首算以內容 `sha256` 為框 ⇒ 四者**同時**不符（`956bf39f…`／`000ae17f…`／`46b4e9e9…`／`11fb216c…`）。
🔑 **「全數不符」而非「部分不符」，即<u>框之誤</u>之徵候**——受單側據此查框而⛔ 逕判受詞紅，**處置正確**。

**根因**：「blob」一詞在本波同時指 blob 之 `sha1` **物件名**與 blob 之**內容摘要** ⇒ 一詞承載二框。

**族** ＝ `自誤 400`（一詞承載二量而未釘口徑）。

**攔法**：① 凡單或報告載**任何摘要值**，同格須**逐字具名其產生指令**（`git rev-parse <rev>:<path>` ／
`git show <rev>:<path> | sha256sum`），**⛔ 以「blob」「摘要」「校驗碼」等詞充框**；
② 凡受單側遇**全數不符**者，**⛔ 逕判受詞紅**，須**先查框**再判（`W-G.9-300R` 閘 `10` 之處置為正例）；
③ 二框之值凡並存者**一律並列出艙**、**⛔ 只報其一**。

<!-- ZW404-END -->

### 三-1　落地後之機驗（🔑 **繫於性質**·`自誤 402` 攔法）

| 款 | 受詞 | 判準 |
|---|---|---|
| `1` | 鑄號框之**當場現查**：自 `verify/probes/probe_WG9267_issuer_measurers.py` 取 `RE_ZW_DEF` 之**逐字**（⛔ 轉引本單） | 出艙其 pattern 逐字 |
| `2` | 以**器本身**（`registry`）量落地後之簿 | 自誤 相異 ＝ **`395`**（`393 + 2`）／MAX ＝ **`405`**；`400`〜`405` 各命中 **`1`**（⛔ `> 1`） |
| `3` | 判別力二造 | [必命中] ＝ `399`（**本批鑄成前即存在**）須 `True`／[必為零] ＝ 一**執行期組出**之人造號（字面⛔ 出艙）須 `False` |
| `4` | 哨兵⛔ 入簿 | 落地後簿內 `ZW404` 之命中 ＝ **`0`**（列框·母體 ＝ 該簿單檔） |
| `5` | append-only | 期初／期末 bytes·`+N／−0`·嚴格前綴檢**二造**·`new.endswith(payload + b'\n')`·`wg942_append_audit.py`（先設 `PYTHONIOENCODING=utf-8`）`rc = 0` |

🛑 **款 `2` 任一不成立 ⇒ 判器紅、停、上呈。**

---

## `§四`　工項二：坑 `bj`／`bk` 之鑄 ＋ `ALPHA_LABELS` 之擴（主線）

🔑 **本工項<u>明文授權</u>一項手段**（`自誤 384` 攔法·射程逐字·⛔ 逾之）：**擴 `ALPHA_LABELS` 至 `bk`**——
射程 ＝ **僅** `verify/probes/probe_WG9269_pit_index.py` 內**新增一列** `ALPHA_LABELS += ["bj", "bk"]`（形同其既有之 `+= ["bh", "bi"]` 列），
**⛔ 改該檔任何既有列一字**、**⛔ 動該檔之任何框或判準**。🔒 該檔屬 `verify/` **子層**、**⛔ 在生產碼 `34` 檔母體內**。

🔑 坑 payload 之界 ＝ `<!-- PIT-BEGIN -->` 與 `<!-- PIT-END -->` 之間（**不含哨兵列**）；落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（**與 `§三` 之 payload 分列 `commit`**）。唯一性之框同 `§三`（**整列逐位相等**·二數並報）。

🔒 **定義處為 `2` 之預先受領（⛔ 判為缺失·⛔ 逕修該框·⛔ 鑄號）**：本單於 payload 內**逐字給出**二坑之標題列，
而本單自身入倉 ⇒ 二坑之**定義處各為 `2`**（本單 ＋ 簿）。其與坑 `bh` **同形**、已於 `W-G.9-285 §五-2` 受領。
🛑 CC 須**出艙其定義處清單**（逐坑逐處之 `檔` ＋ **字樣錨**）並照實具名為 `2`，**⛔ 判為紅**、**⛔ 逕修**。

<!-- PIT-BEGIN -->

### 🕳 坑 `bj`　**量測器之母體<u>靜默塌陷</u>，而其判別力造全盲**

**形**：新器之母體由 `34` 檔靜默塌為 `1` 檔，而該器之判別力**四造全綠**。

**徵候（`W-G.9-300R §六` 自捕 `1`）**：器之輸出載「基數 ＝ `1`（`1 + 0`）」，而同一批之開工閘 `7` **獨立**得 `34` ⇒ **二數互斥**。

**根因**：器居 `verify/probes/`，其 `REPO` 取**二層** `dirname` ⇒ 解為 `…/verify`；`git ls-tree HEAD -- verify/` 於該 cwd
解為 `verify/verify/` ⇒ **靜默得空**。`app.py` 因 `HEAD:app.py` 與 cwd 無關而**倖存** ⇒ `[必非零] |S| ≥ 1` 與
自反之 `[必命中]` **二造照樣全綠**——**原四造對母體塌陷全盲**。

**攔法**：① **母體之基數須綁<u>外部錨</u>**——取本批他處**獨立**得之同一量（如開工閘之 `34`）列為**判別力造之一**（`[必為 N]`）；
② 🔒 **`N/N` 之自證只證<u>一致</u>、⛔ 證正確**——凡「受詞數 ＝ 母體數」型之自證，其分母若與分子同源，該造**恆真**；
③ 凡器之母體由路徑推導者，須同格出艙其**推導後之絕對路徑**與**該路徑下之檔數**，⛔ 只報檔數。

### 🕳 坑 `bk`　**以 `text=True` 取含 CJK 路徑之 `git` 輸出，其 `stderr` 於 `cp950` 主控台<u>靜默丟失</u>**

**形**：量測腳本擲 `UnicodeDecodeError: 'cp950' codec can't decode byte 0xe9`（於 `Thread-N (_readerthread)`），
**而其後各格照常印出綠** ⇒ 例外落在**子行程之讀取執行緒**、⛔ 主流程 ⇒ ⛔ 中斷、亦⛔ 使 `rc` 非零。

**徵候（`W-G.9-300R §九-2` 自捕 `6`）**：判別力[必相異]之造對一**含 CJK 之路徑**跑 `git cat-file -e <rev>:<path>`
（其**預期即失敗**）；`git` 之 `fatal:` 訊息含該路徑之 **UTF-8 bytes**，而 `subprocess(text=True)` 以 `cp950` 解 `stderr`
⇒ 擲例外**並丟棄該串流**。

**根因**：`text=True` 之解碼框係**主控台之 locale**（`cp950`），⛔ `git` 之輸出編碼（UTF-8）。

**攔法**：凡以 `text=True` 取**含 CJK 路徑**之 `git` 輸出者——① 若判**只繫於 `returncode`**，須**以 bytes 重取驗其未污染**
（二框之 `rc` 逐位相同）並照實併記；② 若判**繫於輸出字串**，一律**改取 bytes**（`subprocess(..., text=False)` 後自行以
UTF-8 解）；③ **⛔ 以「其後各格照常綠」充「未受污染」**——綠之來源可能只是該串流已被丟棄（`GB-88` 族·`常規五`）。

<!-- PIT-END -->

### 四-1　落地後之機驗（🔑 **繫於性質**）

| 款 | 受詞 | 判準 |
|---|---|---|
| `1` | 坑之鑄號框之**當場現查**：自 `verify/probes/probe_WG9269_pit_index.py` 取其定義框之**逐字**（⛔ 轉引本單） | 出艙其 pattern 逐字（**三形若有則三形分列**） |
| `2` | 以**器本身**（`probe_WG9269_pit_index.py`）量落地後之態 | **`89`／`89`**（`87 + 2`）·末標籤 **`bk`** ⇒ 下一 **`bl`**；`bj`／`bk` 之**可解析** ＝ `True` |
| `3` | `ALPHA_LABELS` 之擴 | 成員數 **`+2`**、尾元 ＝ `bk`（🛑 **先實測後填**·⛔ 憑臆測）；該檔之 `git diff --numstat` ＝ **`1` 增／`0` 刪**；🔒 **框字串之處數須現查**（其若以 `ALPHA_LABELS[-1]` 之 f-string 生成則**自動齊一**·須出艙其實測而⛔ 假定） |
| `4` | 定義處清單 | `bj`／`bk` **各 `2` 處**（本單 ＋ 簿·逐處出艙 `檔` ＋ 字樣錨）⇒ 照實具名·**⛔ 判紅** |
| `5` | 哨兵⛔ 入簿 | 落地後簿內 `PIT-BEGIN`／`PIT-END` 之命中 ＝ **`0`** |
| `6` | 判別力二造 | 造甲[必≥1] 坑 `n` 之定義處 ＝ `6 ≥ 1`／造乙[必為0] **執行期組出**之人造坑 ＝ `0`／`0` |

---

## `§五`　工項三：`GB-170` 之末端追加（主線·落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**·⛔ 上文一字不刪）

🔑 本 payload **⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**（與 `§三`／`§四` 之形**刻意相異**·`自誤 402` 攔法 ④）。
界 ＝ `<!-- GB301-BEGIN -->` 與 `<!-- GB301-END -->` 之間（**不含哨兵列**）。
🔒 **`GB-132` 之避讓**：本 payload 凡**以 `#` 起首**之列，其列內**⛔ 含任何 `GB-` 之號**。

<!-- GB301-BEGIN -->

---

## 🔧 路丙之實作形之**重釘** ＋ `side_mid` 在域性不足之登記（`W-G.9-300`／`-301`·⛔ 解除、⛔ 收窄·純末端追加）

🛑 **本節⛔ 鑄任何號**（`戒 36`）；⛔ 改任何原節一字、**⛔ 解除、⛔ 收窄任何阻塞項**。

> **接觸面之實測（`W-G.9-300R`·**母體 ＝ 生產碼 `34` 檔·正面列舉**·框 ＝ AST `Call` 之二形並取·
> 🔒 **框⛔ 有 `^` 錨 ⇒ 二數並報**：AST 之處 ＝ **`7`**／文字層列框 ＝ **`5`**）**：
> `_corner_buffer_S` 之呼叫端 ＝ **`7`** 處／**`4`** 檔（
> 文字層只得 `5` 列 ⇒ **整組漏掉** `wf_f1`／`wf_f4` 之三處）。生產期實走者 ＝ **`1`** 處
> （`stepg_pipeline._run_step_g_impl` 之左支·`21` 次·`side` ＝ `['left']`·`_label` ＝ `['R2','R4']`）。
> **`S∖F` 之 `6` 處<u>全數</u>經判為 `forced` 路徑**（外圍 `test` 逐字 ＝ `_fo_left`／`_fo_right`／
> `fo.get('left_forced_offset')`／`fo.get('right_forced_offset')`；判別力[必為否] ＝ 定義列之外圍 `If`／`IfExp` ＝ `0` ⇒ 判否）
> ⇒ **「改 `F` 即足」不成立**、逾 KL 射程 `(b)` 之「只改該處」⇒ **停二成就 ⇒ 修批⛔ 辦**。
> **`app.py` 之二處（`S1`／`S2`）係 UI 路徑**——落於 `def main` 內，`harvest` 保留 `main` 而**從不呼叫**
> ⇒ `run_verification` **結構上⛔ 驅動之** ⇒ 其改**於該批結構上不可驗**。
> 🔴 **`side_mid` 之在域性不足（既存隱患·本節之登記受詞）**：於 `S3` 之呼叫當下，`_side_mid_left`／`_side_mid_right`
> **在域**（`f_locals` 判 `True`），惟其賦值**在該呼叫列之後**且與之**同處一個逐街廓 `for` 迴圈內**
> ⇒ **首次**迭代該二名未綁；**其後**之迭代，該二名綁的是**前一街廓**之值。
> ⇒ 🛑 **`f_locals` 之 `True` ⛔ 等同「可取」**——於該處取之即**靜默取得他街廓之側界中點**（⛔ 型別可擋）。
> 本案 `_label` 分布 ＝ `['R2','R4']` 而 `R2` 之前尚有 `R1` 迭代 ⇒ **其可發生已坐實**。
> 🛑 **⛔ 逕修**（⛔ 移賦值列、⛔ 新增參數、⛔ 改簽章）——本節僅登記。
> **發單側之裁（`W-G.9-301 §一` 裁 `2`〜`4`）**：路丙之**取線之設計⛔ 變**（其受領仍在）；其**落點**由「呼叫端之實參」
> **重釘**為「`_corner_buffer_S` 之**函式體內自導**」。二由：`(一)` 非 `forced` 之呼叫端 ＝ **`0`／`7`**
> ⇒ 改函式體之射程**恰等於** `forced` 路徑且為**一處**，較改 `7` 處**更合**「只改該處」；
> `(二)` 函式體內之 `block_poly`／`side` **依構造即本街廓** ⇒ 上開靜默錯值之通道**自始不存在**。
> 🛑 **其可行性候 `W-G.9-301` 工項四之現查**（現有形參是否足以導出街角第 `1` 宗之臨街向）；不足 ⇒ **停機上呈**。
> 🛑 **KL 之放行仍未動用**；`verify/W-G.9-299-gb170` **⛔ 開**；生產碼 `34` 檔**一字未改**；
> **失效條件 `(2)`／`(3)` 仍未成就**——⛔ 解除、⛔ 收窄、⛔ 提修法主張。

<!-- GB301-END -->

### 五-1　落地後之機驗（**反向**·`自誤 402` 攔法 ④）

出艙項同 `§三` 三-1 款 `4`／`5`（哨兵⛔ 入簿·append-only 二造），**另加**：
🔑 **反向機驗**：以**三形聯集**（`F1`／`F2`／`F3`·**自器碼當場取**·**三形分列並報**）對落地後之簿重算 ⇒
MAX **須仍 `170`**、相異 **須仍 `168`**、缺號 **須仍 `[12, 87]`**（⛔ 鑄號之**性質**·**⛔ 以「payload 內無 `###`」或「未被框命中」充證**）；
判別力[必命中] ＝ `GB-170` 之既有定義列於三形至少一形須 `True`；[必為零] ＝ **執行期組出**之人造號於三形皆須 `False`；
二形以上皆 `0` 須 **loud 具名**（`GB-158`）。
🔒 併驗 `GB-132` 之避讓：本 payload 內 `^#` 起首之列，其列內含 `GB-` 者 ＝ **`0`**。

---

## `§六`　工項四：**自導可行性之現查**（主線·**零生產碼**·🛑 **含停機款**）

### 六-1　本項之由（**發單側具名·⛔ CC 推定**）

裁 `2` 已將落點重釘為函式體，惟「函式體內**能否**導出街角第 `1` 宗之臨街向」**未經量測**。
若逕出修批單而其不可導，即為 `自誤 404` 之**重踩**。⇒ 本項**只現查、⛔ 改一字**；修批候 `W-G.9-302`。

### 六-2　逐款出艙（**AST 為主**·⛔ 憑閱讀·⛔ 轉引 `W-G.9-300R` 之數）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **二函式之簽章逐字**：`_corner_buffer_S` 與 `_first_corner_alloc_dir` 之**定義處**（母體 ＝ 生產碼 `34` 檔·**正面列舉**·框 ＝ AST `FunctionDef`／`AsyncFunctionDef`·**二形並取**）＋ 其**形參名序**（含 `kwonlyargs`／預設）＋ 體之列數 | **檔／處二數並報**；🆕 **母體基數綁外部錨**（＝ `§零` 閘 `7` 之 `34`·坑 `bj`） |
| `2` | **二函式體內之<u>自由名</u>**（框 ＝ `Name.Load` 扣形參、扣體內 `Store`、扣 `builtins`）逐名出艙 | 併出艙**各自由名於 `ns` 之可得性**（`ns` 之鍵框·⛔ 模組 `__globals__`·`W-G.9-298R` 已證 `ns` 係純 `dict`） |
| `3` | 🔑 **所需輸入集 `R`**：`_first_corner_alloc_dir` 求值所**實需**之輸入（其形參 ＋ 其體所消費之自由名），逐名出艙 | ⛔ 判「可導」——**只出艙逐名之來源**（形參／`ns`／二者皆非） |
| `4` | 🔑 **`side_mid` 之等價量於 `_corner_buffer_S` 內之可得性**：以其**現有形參**（`block_poly`／`side`／`d_hat`／`front_p1` 等）為起點，現查其體內**已調用**之原語（`_block_strip`／`_strip_s_range`／`_band_area`）之**簽章與回傳結構**，並出艙「本街廓該側之**側界線**及其**中點**」是否在其中 | 🛑 **⛔ 自寫第二套幾何**（`GB-48` 族）——只現查**既有原語**之可得性；凡須新原語者一律具名為「**不可自現有形參導出**」 |
| `5` | **非 `forced` 呼叫端之復驗**（**本批獨立重得**·⛔ 轉引 `W-G.9-300R`） | 逐處出艙其外圍 `If`／`IfExp` 之 `test` 逐字 ⇒ 非 `forced` 者須 **`0`／`7`**；判別力[必為否] ＝ `def _corner_buffer_S` 之定義列（其外圍 ＝ `0` ⇒ 判否） |
| `6` | **判別力四造** | [必非零] 定義處 `≥ 1`／[必為 `34`] **母體基數之外部錨**（坑 `bj`）／[必為零] 一**執行期組出**之人造函式名之定義處 ＝ `0`（字面⛔ 出艙）／[必真] `_first_corner_alloc_dir` 在 `ns` 之鍵 ＝ `True`（`W-G.9-298R` 已證 `ns` 之來源） |

### 六-3　🛑 **停機款**（成就 ⇒ **判器紅、停、上呈**·⛔ 擬任何改法）

> **停四**：款 `3` 之 `R` 中**有任一名**既⛔ 在 `_corner_buffer_S` 之現有形參內、亦⛔ 可自其現有形參經**既有原語**導出
> （款 `4`），⇒ 函式體內自導**不可施**於現有簽章下 ⇒ **停、上呈**。
> 🛑 **⛔ 新增形參、⛔ 改簽章、⛔ 新增 `import`、⛔ 自寫第二套幾何**——此四禁於停機後**仍在**，⛔ 以「為使其可施」為由逾之。

🛑 **本工項⛔ 改生產碼一字、⛔ 開分支、⛔ 動用 KL 之放行、⛔ 驅動 `run_verification`、⛔ 擬任何改法或 diff。**

---

## `§七`　🛑 **⛔ 為之事**

🔑 **一項手段<u>已明文授權</u>**（射程逐字見 `§四` 首段·⛔ 逾之）：擴 `ALPHA_LABELS` 至 `bk`（**僅新增一列**）。
其餘：
⛔ 改生產碼 `34` 檔**一字**（含 `app.py` 之 `_corner_buffer_S` 函式體——其改候 `W-G.9-302`）；
⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 驅動 `run_verification` 或任何長跑；⛔ 辦修批；
⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；**⛔ 引任何自 baseline 取得之數**；
⛔ 改任何函式簽章、⛔ 新增任何形參、⛔ 新增任何 `import` 於生產碼、⛔ 改任何 `*_EXPECT` 一字；
⛔ 自寫第二套幾何或第二份 `W`／`side_mid` 之定義（`GB-48` 族）；
⛔ 動 `probe_WG9269_pit_index.py` 之**任何既有列**（僅准新增一列）；⛔ 動 `probe_WG9292_pathC.py`／`probe_WG9295_joint.py`／`probe_WG9298_gate3.py`／`probe_WG9300_contact_surface.py`／任何**其他既有**探針一字；
⛔ 鑄 `GB`／`VR`／`K-9` 之任何號（**自誤 `404`／`405` ＋ 坑 `bj`／`bk` 為本批唯一之鑄**）；
⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 逕修 `side_mid` 之在域性（⛔ 移賦值列）；
⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改 `W-G.9-299`／`-300`／補令一／交接註／`W-G.9-300R` 或任何已入倉文件一字；
⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文；
⛔ 判 `GB-170` 可否解除、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 擬任何改法或 diff；
⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制；
⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述）。

---

## `§八`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1`；🔒 `probe_WG9269_pit_index.py` 屬**子層**、⛔ 在母體內（併出艙其 `numstat` ＝ `1`／`0`） |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·🆕 **含 CJK 路徑者一律取 bytes**·坑 `bk`） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末（**器之輸出**） | 自誤 相異 **`395`**／MAX **`405`**（＋**二**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末（**器之輸出**） | **`89`／`89`**（＋**二**）·末標籤 **`bk`** ⇒ 下一 **`bl`**；`ALPHA_LABELS` 成員數 `+2`·尾元 `bk`；母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 檔數（**自洽檢**） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 本批確有異動之檔）；`app.py` ＝ `4379108a…`（**框 ＝ blob `sha1`**）；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**六物證落檔**之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列·**框逐字具名**·`自誤 405`） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**·[必為零]）；判別力[必存在] `p3a` 命中 **`1`** |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§九`　報告之格

`§零` 開工閘（**每列同格載其產生指令**）→ `§零′` 之⛔ 重辦 → 逐工項（逐格表以落檔**全量**出艙·⛔ `\| tail -N`）→
`§六` 停機款之判 → 各判別力造之判 → 一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→
`⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；**凡摘要值一律逐字具名其產生指令**（`自誤 405`）；框無 `^` 錨者**二數並報**；**帶號與絕對值並列**。
🔒 **母體之基數一律綁外部錨**（坑 `bj`）；🔒 含 CJK 路徑之 `git` 輸出一律**取 bytes**（坑 `bk`）。
🔒 **逐閘出艙執行次數**；🔒 同一性一律 `is` 比對（**`id(·)` ⛔ 作錨**）；🔒 判定組為空一律 **loud 拒測**並**照實具名**。
🔒 凡自捕一律於出艙前攔下、照實併記（**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）；物證落檔**保全⛔ 覆寫**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`deletions`／`cat-file`／
`merge-base`／`RE_ZW_DEF`／`append_audit`／`ZW404`／`PIT-BEGIN`／`GB301-BEGIN`／`ALPHA_LABELS`／
`_corner_buffer_S`／`_first_corner_alloc_dir`／`side_mid`／`block_poly`／`_block_strip`／`_strip_s_range`／
`GB-132`／`GB-170`／`baselines`／`numstat`／`sha256`／`ls-tree`／`WV_BAKE`。

【單止】本單受詞 ＝ CC 窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: f4d55500c07127889d62575469ababc63954c218ae291b5148a417970521aae5
