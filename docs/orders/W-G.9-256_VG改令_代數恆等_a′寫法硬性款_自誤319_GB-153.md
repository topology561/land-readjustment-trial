# W-G.9-256　`V-G` 之改令（逐值迴歸 → 代數恆等）＋ `a′` 寫法之硬性款 ＋ 自誤 `319` ＋ `GB-153`

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。承 `W-G.9-255`（工項零〜三已推 `8a52dbd`；`c1` 碼面本地 `dacd354`·**未推**）。
> 🛑 **本單改 `-255 §五` 之【驗收】`V-G` 一款**，其餘（`§五 a`–`e` 之碼面射程、`V-G′`、`V-A`）**一字不動**。
> 🔒 **KL 裁（`2026-09-08`）**：「**甲：改用代數證明**」——即接受「代數恆等 ＋ 人造測資鑑別力」代替「逐值迴歸」作為 `c1` 之放行證據。

---

## §零　開工閘

1. `git fetch`；錨 ＝ **`origin/wip/s1-endpart @ 8a52dbd`**。不符 ⇒ 停機。
2. 號占用（`①`–`⑦`·三數 ＋ 雙屬）：發單側於 `8a52dbd` 實測 `W-G.9-256` ＝ `0`／`0`／`0`／雙屬 `0`；對照甲 `-255` ＝ `D1 2`／`D2 5`／`D3 0`／檔框 `4`。CC 當場復現。
3. 次號當場復算：發單側實測 自誤 `MAX 318`、缺號 `[106]`、恆等式 `True` ⇒ 鑄 **`319`**；`GB` 主序列 `MAX 152` ⇒ 鑄 **`GB-153`**。
4. `probe_order_preflight.py`：`P-3`／`P-5` 綠。
5. worktree 須先切 **wip 態**（`GB-152`）。

---

## §一　工項零：本單原封入倉 `docs/orders/`。`P-5` 對拍。

---

## §二　工項一：自誤 `319` ＋ `GB-153`（**零生產碼**·append-only·標題形取簿最末一則為範本）

**`319`　立驗收於未查之輸出路徑：`V-G`／`V-H` 所需之 `F.0`–`F.4` 表在 `GB-67` 下結構上產不出（`300`／`301`／`306`／`310`／`312` 同族·第六度）**
🔒 **責 ＝ 發單側。**
受詞：
① `W-G.9-254 §四 c1` 立 `V-G` ＝「`f0`／`f2`／`f3`／`f4` × 雙情境逐欄逐值 `Δ = 0`」；`W-G.9-255 §五` 沿用。
② `W-G.9-254 §四 c2` 逐字斷言「**`GB-67`／telescoping ⛔ 阻塞本閘**（其阻塞者係與凍存基準對拍，非兩活體 run 之差）」。
**發單側於 `2026-09-08` 實跑 `verify/run_verification.py`＠`8a52dbd`（安裝 `ezdxf`／`shapely`／`plotly`／`streamlit`／`pandas`／`openpyxl` 後）得 `RESULT: FAIL`、紅 `32` 項，其中 `W-F F.0`／`F.1`／`F.2`／`F.3`／`F.4` 全紅。**
根因：`StepG` 於街廓 `R2` 中止——`②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 2.8906`（`GB-67`）＋ `telescoping 破：R2 left ΣRw_實跑=100.02 ≠ 44.41`。`R1` 可算，`R2` 起全無 ⇒ 逐值表（含 A／B 兩活體 run）**皆產不出**。②之斷言為偽：**A／B 相減同樣須管線先產出表。**
加重：
- 交接文 `§五` 已逐字載「`GB-67`／telescoping ⬜｜**二者致 `R4`／`R5`／`R6` 逐值迴歸不可得**」，發單側於本窗開窗時讀過該行，**卻未在立驗收時接上**。
- 發單側於 `-255 §五` 同一頁面令 CC「`V-G′` 三組任一綠 ⇒ 器無鑑別力、⛔ 得出艙」，而**自身所立之 `V-G` 連能否執行都未查**。
- 攔截者 ＝ **發單側自捕（實跑 harness）**，非 CC、非 KL——惟係在 CC 已被令去跑該閘之後。
通則：凡以「某輸出之逐值對拍」立驗收，**須先實際執行該輸出之產生路徑一次**並記其 `RESULT`；⛔ 以「該檔在倉」「該 harness 存在」推定其可得。既知之阻塞項（`GB`／⬜）須於立驗收時**逐一比對其射程**，⛔ 僅在 `§待落地` 登記了事。

**`GB-153`　`GB-67`／telescoping 使 `F.0`–`F.4` 全世代之逐值迴歸不可得（`c1`／`c2`／`-249` 皆受其限）**
🩸 案由：見 `自誤 319` 之實跑證據。
🔒 **射程具名**：`StepG` 於 `R2` 中止 ⇒ ①`F.0`–`F.4` 六族 CSV 全不可得；②`c2` 之 `V-H`（`R4` 左街角 `226.01㎡` 之 A／B 差異表）**亦不可得**——`R4` 在 `R2` 之後；③`-249` 段三觀測之 `V-S` 表同受此限。
🔒 **可得者具名（⛔ 一併否定）**：`StepG` 中止前之部分落檔 `got_G值_退縮{0m,3.5m}_partial.csv`（`21`／`24` 列）；`corner_range_areas` 全街廓（含 `R4/left = 226.006436`、`R2/left = 308.1654`）；`R1` 之完整診斷。
🔒 **處置**：本則⛔ 阻塞 `c1`（其放行改依 `§三` 之代數證明）；**`c2` 之放行證據形須另議**，本單⛔ 決之，登記於 `§六`。
🛑 **本則⛔ 解除 `GB-67`**，亦⛔ 為其診斷單。

---

## §三　工項二：`-255 §五`【驗收】之改令（`V-G` 一款）

🔒 **`V-G` 原款作廢**（逐值迴歸不可得·`GB-153`）。**代之以下三款**；`V-G′`（判別力三組 ＋ 反靶）與 `V-A` **一字不動、全效**。

### `V-G1`　代數恆等（同區段 ⇒ `a′` **逐位**還原 `a`）

**命題**：`zone(m) == zone(target)` ⇒ `a′_m == a_m`（**bitwise**，⛔ 容差）。
**母體**：`verify/baselines/wf/f0/F.0_G值_退縮{0m,3.5m}.csv` 之 `a 面積(㎡)` 全欄 × `snap["財務接線_v3"]["重劃前區段_面積單價"]` 之全部區段鍵。
**判準**：反例數 **＝ `0`**。
🔑 **發單側於 `8a52dbd` 已先證**（⛔ 令 CC 證未經證之命題）：母體 `57` 宗 × `2` 區段 ＝ **`114` 組，括號式反例 `0`**。CC 須以**自撰之器**獨立復現，⛔ 採信此數。
🛑 **反靶（⛔ 省）**：同一器對**左結合式** `(a*p)/p` 須得**反例 `9`**（發單側實測；例：`947.97/b` `Δ=-1.1368683772161603e-13`、`245.0/a` `Δ=-2.842170943040401e-14`）。反例為 `0` ⇒ **器無鑑別力、⛔ 得出艙**。

### `V-G2`　🔴 **`a′` 之寫法硬性款**

`a′` **必須**寫作 `a * (p_自身 / p_標的)`（**先除後乘·括號不得省**）。
**⛔ 寫作** `(a * p_自身) / p_標的`、`a * p_自身 / p_標的`（左結合同形）、或任何先乘後除之形。
🩸 理由：二者於同區段時**不等價**——後者因 `a*p` 之中間捨入，於本案母體有 `9` 個反例（最大 `1.14e-13`），將使 `V-G1` 假紅，並使日後之零差迴歸永久漂移。
**判準**：`dacd354` 之碼面須實查為括號式；非者 **改之**，並於報告具名改前改後之逐字。

### `V-G3`　射程未逾（沿用 `-255` 之自檢·具名復現）

`停機#2` 命中 `0`／`停機#3` `1`／`zones` 蒐集 `1`／`_key` `1`／`py_compile` rc `0`；十一檔中除 `verify/wf_f0.py` 外逐位不變（`app.py` ＝ `4860f10828fb8ae9`）。

🛑 **`V-G1`–`V-G3` ＋ `V-G′` 四者全綠**方得推側分支 `verify/W-G.9-255-c1` 並呈 KL。
🛑 **本組閘所證者<u>僅</u>「同區段時不動」**；「跨區段時算得對」僅由 `V-G′` 之人造測資支撐 ⇒ **出艙時須逐字具名此界**，⛔ 以「`c1` 已驗證」概括。

---

## §四　工項三：`c1` 出艙形

呈 KL 之句（**一句實務語言**·`-252` 交接文 `§七` 之形）：

> 「本批於本案**逐位零差**（同區段之合併恆還原原面積，`114` 組窮舉無反例，並經左結合式反靶證器有鑑別力）；跨區段之折算路徑已開，由三組人造測資證其算得對——**該路徑在本案尚未被真實資料走到**。」

🛑 ⛔ 向 KL 稱「已通過逐值迴歸」。

---

## §五　工項四：`-253` 段乙（`-251` `c1`／`c2`）——依原令，**⛔ 受本單影響**，可並行。

---

## 【驗收】

| 閘 | 判準 |
|---|---|
| `V-B` | append-only：`numstat` deletions 全 `0`（自誤簿／`W-G.4`） |
| `V-C` | 自誤 `MAX 318→319`、缺號仍 `[106]`、恆等式 `True`；`GB MAX 152→153`；`K-9 MAX` 仍 `29` |
| `V-N` | `-254`／`-255` 二單之 blob **逐位未動**（本單係改令，⛔ 追改既出） |
| `V-G1`／`V-G2`／`V-G3`／`V-G′`／`V-A` | 見 `§三`、`-255 §五` |

---

## §六　產出與放行

| 批 | 內容 | 放行 |
|---|---|---|
| 工項零〜二 | 入倉／`319`／`GB-153`／改令入報告 | **零生產碼 ⇒ 直推主線** |
| `c1` | `dacd354` ＋ `V-G2` 之寫法修正（如需） | 側分支；四閘全綠 ⇒ 呈 KL `§四` 之句 ⇒ 候放行 |
| `-251` 段乙 | 同 `-253 §六` | 側分支；`V-K` 後候放行 |

🛑 **`c2` 本單⛔ 開工**——其 `V-H` 已由 `GB-153` 判為不可得，放行證據形**須發單側另議並候 KL**。CC ⛔ 逕自續辦 `-254 §四 c2`。

---

## §七　待落地登記（本單登記·⛔ 辦）

| 項 | 狀態 |
|---|---|
| **`c2` 之放行證據形**（`V-H` 不可得下如何證 `R4` `226.01㎡`） | 🔴 **發單側須另議·候 KL** |
| `GB-67` 之診斷（宗-宗重疊 `45.9766`·成因未知） | ⬜ 未起·阻塞 `F.0`–`F.4` 全世代 |
| telescoping（`R2 left ΣRw 100.02 ≠ 44.41`） | ⬜ 同上 |
| `-249` 段三觀測（`V-S` 表）之可得性 | 🔴 受 `GB-153` 限·須重估 |
| UI 步驟 D 逐宗點選之 KL 目檢 | `c2` 併線後 |
| `a′` 模式二（`p_average`） | 五級射程 |

---

*受詞 ＝ CC。工項零〜二直推；`c1` 四閘全綠後候 KL 放行；`c2` ⛔ 開工。*

SELF_SHA256: 2582ca5a8e0d7589a0052856cb6ce14f6de893e540b8e12bfc4f033dcc046115
