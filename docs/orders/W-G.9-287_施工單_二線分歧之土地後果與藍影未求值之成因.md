# `W-G.9-287`　施工單（**主檢**）：二線分歧之**土地後果**（對稱差 ＋ 負擔率）／藍影未求值之成因 ／ `自誤 390`

> **態**｜開工 ＝ `52c9d97b3cada856833d0672b8dd363b1b9a19d1`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）
> **單** ＝ `docs/orders/W-G.9-287_施工單_二線分歧之土地後果與藍影未求值之成因.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（幾何量測 ＋ 純函式求值 ＋ 登記·**零生產碼**·⛔ 替身·⛔ 反事實）
> 🛑 **本批鑄號 ＝ `自誤 390` 一號**（逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。

---

## `§零`　開工閘（九項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值／判準** |
|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `52c9d97b3cada856833d0672b8dd363b1b9a19d1` 或其後裔（**全 `40` 碼**·坑 `bh`） |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | 須**非**祖先；判別力[必為祖先] ＝ 本批基座之任一祖先 `commit` |
| `4` | `SELF_SHA256`（`P-5` 口徑） | 見本檔末列·**由發單側供值**；判別力二造須皆相異 |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-287` ＝ `True`；[必偽] 含 `W-G.9-286` ＝ `False` |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | 四簿 ＋ 坑 | 自誤 **`379`**／**`389`**｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 **`87`／`87`**·下一標籤 **`bj`** |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] 子層檔數 **須 `> 0`**（判準式·⛔ 定值·`自誤 386`） |
| `8` | `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**） |

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之體例、簡繁機檢、`rev` 釘死、**先跑再寫**——
後者之受詞**及於任何尚未量得之數**）**逐項適用·⛔ 省**。
🆕 **一則新附款**：凡[必為零]之造，**須先自證其可滿足**（⛔ 立一個在 IEEE754 下結構上不可滿足之造·`W-G.9-286` 自捕）。

---

## `§零′`　前批之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-286` 三 `commit` 經發單側自倉獨立復驗：工項零之單**逐位相同**（`sha256` `e2ddf004f0c37f3b…`）／
生產碼 `34` 檔於 `d1d969e6..52c9d97b` 異動 **`0`**／自誤 `379`／`389`（恰 `+1`）·其餘三簿未動／`p3a` **非祖先**
⇒ **✅ 全批核可**。
🔑 **主檢之受領**：二線**⛔ 平行**（`|cos|` ＝ `0.0473`／`0.0754`／`0.0473`）；`W_0` 逐位走 `buf` 線（差 `≈2e-10`）；
藍影之第一實參取自 `corner_range_polys[_side]`。**自我驗證閘三格殘差皆 `0.0`** ⇒ 器非紅。
🩸 四則自捕皆於出艙前攔下，處置正確——尤以「單之[必為零]造在 IEEE754 下**結構上不可滿足**」：其**先判器紅、
依 `常規二` 取保守項、原值照實併呈而⛔ 改單**之處置**完全正確**；該款之缺失**責在發單側**，已立為 `§零` 之新附款。

---

## `§一`　裁

| # | 事 | **裁** |
|---|---|---|
| `1` | 二線是否為同一條 | **⛔ 是同一條**——方向不同（`\|cos\|` `≈0.05`／`0.08`）、距離亦不同。**此為機械事實，本批⛔ 再議** |
| `2` | 起算所走者 | **`buf` 線**（`W_0` 與其垂距逐位相符至 `≈2e-10`）——⛔ 規定範圍之遠側境界線 |
| `3` | 藍影是否被架空 | 🔴 **發單側前所呈之推測「藍影可能被架空」<u>經實測否證其半</u>**：其第一實參取自**範圍多邊形**、⛔ `buf` 線 ⇒ **構造上⛔ 被架空**。惟 `R4/left` 二情境該閘 **`0` 次** ⇒ **成因未查**（`§四`），⛔ 判 |
| `4` | 🔴 **發單側自認四** | 上開推測**曾以「後果二（更嚴重）」呈 KL**（雖標為「推測」）⇒ 已改變域權威之圖像。責 ＝ 發單側 ⇒ **鑄 `自誤 390`**（`§五`） |
| `5` | `GB-170` 之鑄 | **⛔ 鑄**——待本批之**土地後果**量得。⛔ 以二線分歧之幾何事實逕鑄 |

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-287_施工單_二線分歧之土地後果與藍影未求值之成因.md`；**⛔ 改一字**。
出艙：所指之檔數 ＝ `1` 之自證（造甲 ＝ `W-G.9-286` 之單須 `1`／造乙 ＝ **執行期組出**之人造名·**先驗現母體命中為 `0`**）；
`CR` ＝ `0`（併報 `LF` 與 bytes；判別力 ＝ 一真 CRLF 人造檔須 `> 0`）；blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同。

---

## `§三`　工項一（**主檢**）：二線分歧之**土地後果**（🛑 **⛔ 反事實·⛔ 替身**）

### 三-1　射程（**發單側明定**）

通道 ＝ harness（態甲·`SB ∈ {0, 3.5}`）；受詞格 ＝ `forced` 側之鏈頭（前批當場得 `3` 格·🛑 **本批重量並出艙其數**）。
🛑 **⛔ 改起算後重跑分配**（該為反事實）——本項只量**二個既有多邊形**與**一個純函式**之值。

### 三-2　逐款出艙（`repr` 全位·⛔ 捨入·座標系 ＝ 平面直角座標）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **多邊形甲** ＝ `corner_range_polys[_side]`（規定範圍）之**頂點逐點**與**面積** | 取碼面之同一物·⛔ 另建第二份 |
| `2` | **多邊形乙** ＝ `buf` 帶（`s ∈ [max(s_min,0), buf]`）之**頂點逐點**與**面積** | 以**碼面同一切法**（`_block_strip`／`_strip_s_range`）還原·逐字具名其還原式 |
| `3` | 二者之**面積差** | 期：`\|A甲 − A乙\| ≤ tol`（`tol` 之來源逐字出艙）——若差逾 `tol` ⇒ **loud**，⛔ 逕續 |
| `4` | 🔑 **對稱差之面積**（`(甲∖乙) ∪ (乙∖甲)`）及其**二半各自之面積** | ＝「**位置錯開了多少地**」。所用之幾何庫與其版本逐字出艙；🛑 二半須**分列**（⛔ 只報總和） |
| `5` | 對稱差之**最大偏離距離**（甲之邊界點至乙之邊界之最大垂距，反向亦取） | 二數並報（前批同族之**實測**出處 ＝ `verify/out/WG9286R_twolines.log`【倉】） |
| `6` | **負擔率**（純函式求值·⛔ 重跑分配）：`R(buf 線垂距)` 與 `R(S1_perp)` 逐格並列，及其**差** | 一律以 `ns["rw_from_width"]` 取值·⛔ 器內另算；🛑 二輸入之**來源**逐格具名 |
| `7` | 該側之**抵費地面積**（`range_area`）與該側**第 2 宗之暫編地號**、其 `G` | 供土地語言之陳述用 |
| `8` | 判別力三造 | [必命中] `forced` 側鏈頭格數 `≥ 1`（`0` ⇒ loud·⛔ 以空集充綠）／[必非零] 對稱差之面積須 `> 0`（若 `= 0` ⇒ 與前批之「⛔ 平行」相斥 ⇒ **判器紅、停**）／[必為零] **以多邊形甲與其自身**之對稱差須為 `0`（🛑 該造須先自證**可滿足**） |

🛑 **出艙即止**：⛔ 判碼面是否違反任何裁、⛔ 鑄 `GB`、⛔ 提修法主張、⛔ 呈 KL、⛔ 動任何生產碼。

---

## `§四`　工項二：藍影於 `R4/left` **求值 `0` 次**之成因（🛑 **出艙即止**）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | `_k923_gate1` 之**呼叫端**逐處（錨 ＝ 字樣·**先驗唯一性**·坑 `bi`·無 `^` 錨 ⇒ **二數並報**） | 逐處出艙其**守衛條件之逐字** |
| `2` | 該守衛條件於 `R4/left@0m`／`R4/left@3.5m`／`R2/left@3.5m` 三格之**各分項真值** | 逐分項具名·⛔ 只報合取之結果 |
| `3` | `R2/left@3.5m` 之 `2` 次求值與 `R4/left` 之 `0` 次，其**差異落在哪一個分項** | 逐格並列·⛔ 推定 |
| `4` | 判別力二造 | [必非零] `R2/left@3.5m` 之求值次數須 `> 0`（器有效）／[必為零] **執行期組出**之人造格須 `0` |

🛑 **⛔ 判藍影是否應於該二格求值、⛔ 判其為缺陷。**

---

## `§五`　工項三：`自誤 390`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

> **`自誤 390`**：**以未經量測之機制推測呈 KL**——縱令逐字標明「推測、非結論」，其仍已**改變域權威之圖像**。
> 具體形 ＝ 發單側於呈 KL 之文中列「**後果二（更嚴重）**：兩條界線若都平行 ALLOCLINE 就永遠不會交叉
> ⇒ **藍影在該側等於被架空**」；經 `W-G.9-286` 實測，藍影之第一實參逐字取自 `corner_range_polys[_side]`
> （**範圍多邊形**·⛔ `buf` 線）⇒ **該推測之半數經否證**。
> **徵候**：推測與已量得之事實**並列於同一份呈文**，域權威⛔ 有辨別二者之義務。
> **族** ＝ `自誤 380`（以未經查證之機制假說取得常規地位）。
> **攔法**：**呈 KL 之文一律只載已量得之事實**；推測留在發單側之工作面，**⛔ 出現於呈 KL 之文，
> 縱標明為推測亦然**；凡欲以推測請 KL 預作準備者，改為**先量後呈**。

🔒 落點須出艙：期初／期末 bytes、`+N／−0`、嚴格前綴檢之**二造**、簡繁機檢之 `rc`。

---

## `§六`　收工閘

| # | 受詞 | 期／判準 |
|---|---|---|
| `1` | `numstat` 刪除欄**逐檔具名** | **全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0`；對照組[必非零] `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 ＝ 一已知含 `CR` 之倉內檔須 `> 0` |
| `4` | 四簿期末 | 自誤 相異 **`380`**／MAX **`390`**（恰 `+1`）；`GB`／`VR`／`K-9` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；`ALPHA_LABELS` **一字未動** |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 其受詞係 `index` ⇒ 復跑前先 `git add`；🛑 **⛔ 以「意思有到」充命中**（本波已二度）——逐 token **書出其字面**並具名實測 |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**；`baselines` `298` 檔·`a898f4e1…`；`p3a` **非祖先**；`WV_BAKE` **未設**；`K-9-29` 任何子項⛔ 落地；`GB-167`／`168`／`169`／`79` **⛔ 修** |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit`（**全 `40` 碼**·⛔ 預寫） |

---

## `§七`　🛑 **⛔ 為之事**

⛔ 動生產碼 `34` 檔一字；⛔ 動 `ALPHA_LABELS` 一字；⛔ 改任何器之判準／框／輸出格式；
⛔ 修 `GB-79`／`GB-167`／`168`／`169`；⛔ 解除、收窄或結清任何 `GB`；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 改 `ok_t`／`ok_f` 一字；`GB-162` **三禁令維持**；**⛔ 引任何自 baseline 取得之數**；
⛔ **改起算後重跑分配**（反事實）、⛔ 施替身、⛔ 未經本單具名之長跑；⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；
⛔ 追改任何已入倉文件一字；⛔ 判碼面是否違反任何裁、⛔ 判藍影為缺陷、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL；
⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`§零` 開工閘 → `§一`〜`§三` 逐工項（逐格表以落檔**全量**出艙·⛔ `tail -N`）→ 判別力三造之判 →
一句話推導 → 收工閘（**入倉後末端追加**）→ `⛔ 為之事`之逐項自檢 → 逐 `commit` 與 `ls-remote` 實查。
🔒 凡出艙之數，同格載其**框與母體**；框無 `^` 錨者**二數並報**；以 `git` 輸出為框者具名 `core.quotePath` 或取 `-z`。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`corner_range_polys`／`_block_strip`／`_strip_s_range`／`buf`／`S1_perp`／`range_area`／`rw_from_width`／
`_k923_gate1`／`K-9-23`／`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`merge-base`。

【單止】本單受詞 ＝ 新 CC 窗。⛔ 呈 KL。

SELF_SHA256: d187962348fc9041ab8e6ee9bc3ec24d07c97b9b940d4864310b0cf1161e1734
