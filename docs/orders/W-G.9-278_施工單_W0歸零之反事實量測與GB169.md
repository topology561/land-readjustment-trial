# W-G.9-278　施工單（**零生產碼**：`W_0 → 0` 之反事實量測／`GB-169` 之鑄／`GB-168` 受詞二次精化／坑 `bd`）

> **受詞** ＝ 現施工窗。｜**主線** ＝ `03762e8b37fbc0145768494a93f90188a27ce25f` **或其後裔**（🛑 `ls-remote` 實查）。
> **凍存** `p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
> **此單此前是否已交付** ＝ **否**。｜**分級** ＝ **重**（反事實驅動四次·**零生產碼**）。
> 🔒 **⛔ 動生產碼 `34` 檔一字。**｜🔴 推送一律 `git push origin HEAD:refs/heads/wip/s1-endpart`，push 後 `ls-remote` 實查方得稱已推。

---

## §零　開工閘（**⛔ 略**）

| # | 受詞 | 期值 |
|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `03762e8…` 或其後裔 |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須 **非祖先** |
| `4` | **`SELF_SHA256`**（`P-5` 內建口徑） | 見本檔**末列** |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 紅 ⇒ 停機款；其餘逐項具名 |
| `6` | 四簿 ＋ 坑 | 自誤 `370`／`380`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `81`／`81` |
| `7` | `baselines` | `298` 檔·`a898f4e1…`（口徑 ＝ `core.quotePath` **預設**） |

---

## §零′　`W-G.9-277` 全批核可

**核可**（發單側自倉自跑·rev `03762e8`）：三 commit 鏈相符；`52cf3a3..HEAD` 對生產碼母體 `numstat` ＝ **空**；blob 期初＝期末；`p3a` 非祖先；自誤 相異 `370`／MAX `380`（**恰 `+1`**）·三簿⛔ 動；坑 `81`／`81`。
**三則入倉前自捕逐項核可**——其 ① 零判門檻 `1e-12` 過嚴致偽「單一類·無鑑別力」，尤佳：**門檻本身即框**，你把它具名為常數並改三分類，正是 `§二` 通則之正用。
🔴 **新捕之坑：准鑄，惟其標籤⛔ 為 `bz`** ——現行 `ALPHA_LABELS` 尾元 ＝ `bc` ⇒ **下一標籤 ＝ `bd`**（見 `§三`）。

---

## §一　🆕 `GB-169` 之鑄（**⛔ 修·純登記**）

> **受詞**：**`K-9-5-12`（KL 裁 `2026-08-12`）之「首宗起點一律為 `0`」，碼面<u>未落地</u>。**
>
> **正典逐字**（`docs/rulings/K-6_街角地分配程序與可分配判準.md` 之 `### 🔒 K-9-5-12 首宗起點一律為 0：撤回「\`W_0\` ＝ MP→corner」（KL 裁 2026-08-12）`）：
> `（二）所答　✅ KL 逐字` ＝ 「**是**」；`（五）效力 1.` ＝ 「**首宗起點一律為 `0`**（即自 **MP** 起量）」；`（五）效力 3.` ＝ 「該行**後半**（`forced` 時為其 buffer 起算點）**⛔ 未撤，列為未決**」。
>
> **碼面現態**（`W-G.9-277` 實測·逐字）：決定式 `_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)`；`_W_near = float(np.dot(_bp_w - _mp_w, _ahat))` ＝ **`(baseline_pt − side_mid)·â`** ＝ 即**已被撤回之「MP→corner」**。
> **全母體 `24` 格之鏈頭 `W_0`**：嚴格 `== 0` ＝ **`0` 格**／`|W_0| ≤ 1e-6` ＝ `10` 格／`> 1e-6` ＝ **`14` 格**。
> **本案**（`0m` 態乙 `R4 right`）：`W_0 = 0.1775004071`，該側 `_fo_right = False`（**非 forced** ⇒ ⛔ 落於「未決」之後半款）。
>
> **其與 `GB-168` 之關係**：`Δ=2.425` 之二項貢獻中，`W_prev` 之扣除 `+3.088507` 即本項；另一項（所餵之 `W` 之別 `−0.661141`）係 `理論@k*` 側，屬 `GB-168`。
> **解除條件**：`(1)` `W_0 → 0` 之**土地後果**經量測具名（本單工項一）；`(2)` 其修入生產碼 ＋ **KL 放行**；`(3)` 判別力二造；`(4)` `（五）3.` 之「未決」後半款經 KL 裁定。
> 🛑 **本批⛔ 修、⛔ 提修法主張、⛔ 判「未決」之後半款。**

CC 須**當場復現**上開三項現查（正典節題之逐字命中／決定式之逐字命中／`24` 格三分類之數），⛔ 預寫。

## §二　`GB-168` 受詞之**二次精化**（末端追加·**⛔ 改原節、⛔ 鑄號**）

於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（⛔ 節末）純末端追加一節，題逐字：

```
## 🔧 `GB-168` 受詞之二次精化：拋出者係**第二閘**；二解算器之現行實參；`_宗地寬度` ＝ `round(S × |cos|, 2)`（`W-G.9-278` §二·⛔ 上文一字不刪·純末端追加）
```

內載（**逐字·CC 當場復現**）：
1. **`Δ_i` 之檢於界面 `0` 時判定組為空 ⇒ 其 `raise` 不執行**；`R4 right` 定案宗數 `1` ⇒ 界面 `0`；**實際拋出者係其後之第二閘**——其逐字與其判定式；
2. **二解算器之現行實參（AST·⛔ 文字層）**：`solve_G_binary` ＝ `(_rw_start, W)`／`iterate_G_S` ＝ `(W_prev, W_cur)`；**全母體 `iterate_G_S` 之 `rw_increment` 計數 ＝ `0`** ⇒ 本案出自 `solve_G_binary`。🔑 **併記**：`D-2b-5` 所載之「`solve_G_binary` 偏離（`rw_increment(_W_near, W)`）」係**改前之態**；現行改為 `(_rw_start, W)`，而 `_rw_start` 於 `is_corner or is_chain_head` 時**即 `_W_near`** ⇒ **改名⛔ 等同改行為**，🛑 ⛔ 判其是否仍屬偏離；
3. **`_宗地寬度 = round(S × |cos(d̂, n_alloc)|, 2)`** ＝ **`S` 之投影**（⛔ 「何二物之距」·且 **2dp 捨入**）；`理論@k*` 餵 `14.01`、實跑用 `14.1989`／`0.1775`。
🛑 本節⛔ 鑄任何號；⛔ 改 `GB-168` 原節一字；**⛔ 解除、⛔ 收窄**。

## §三　🆕 坑 `bd` 之准鑄（**標籤已更正·⛔ `bz`**）

| 標籤 | 形／徵候／攔法 |
|---|---|
| `bd` | **形** ＝ **「必為零」之對照，其<u>字面</u>隨報告入倉而成為母體之一員** ⇒ 下一批之同一檢即命中該字面，**該對照遂失其「必為零」之性質**（哨兵自我污染）。**徵候** ＝ 前批得 `0`／`0`、本批同一檢得 `1` 檔／`1` 列，而受詞**一字未變**。**攔法** ＝ `(1)` 必為零之對照一律**執行期組出**且**字面⛔ 出艙**；`(2)` 凡沿用前批之對照，須先量其於**現母體**之命中；`(3)` `CLAUDE.md` 之「零對照之出艙⛔ 構成意指占用」一款只解**取號側**，**量測器側不在其射程**——此點於本坑之攔法內逐字載明 |

🛑 `ALPHA_LABELS` 現止於 `bc` ⇒ **須同批擴至 `bd`** ＋ **三處**框字串齊一。判別力：擴後尾元須為 `bd` 且成員數 `+1`（**先實測後填**）。

---

## §四　工項（**零生產碼**）

| 序 | 事 |
|---|---|
| `0` | 本單原封入倉（`§二-B` 搬移法·搬移前「所指之檔數 ＝ `1`」自證） |
| `1` | **`W_0 → 0` 之反事實量測**（`四-1`·**本批主檢**） |
| `2` | `GB-169` 之鑄（`§一`） |
| `3` | `GB-168` 受詞二次精化之末端追加（`§二`） |
| `4` | 坑 `bd` 之鑄 ＋ `ALPHA_LABELS`→`bd`（`§三`） |
| `5` | **「虛胖陷阱」錨腐之登記**（`四-2`·末端追加·⛔ 鑄號·⛔ 修） |
| `6` | 收工閘（`§五`）⇒ **停** |

### 四-1　`W_0 → 0` 之反事實量測（**主檢**·🛑 **⛔ 落地**）

**器** ＝ `verify/probes/probe_WG9278_w0zero.py`（以 `probe_WG9277_wchain.py` 之 drive 為底本·⛔ 另寫第二份判準）。🛑 坑 `bb`（框內省 `depth + 1` ＋ 自我驗證閘）與坑 `bd`（哨兵執行期組出）之攔法適用。

**替身之定義（發單側定·⛔ CC 自訂）**
- 包裹 `ns["rw_increment"]`；凡其呼叫端經**框內省**判定為**鏈頭**者，以 `rw_increment(0.0, W_cur)` 代之。
- 「鏈頭」之判準**一律取碼面之同一運算式** `is_corner or is_chain_head`（自呼叫端之區域名取值），**⛔ 器內另寫第二份判準**。
- 🛑 **⛔ 寫入倉之生產碼**；替身僅於本器之驅動中生效。

**情形之分（⛔ 混同）**
- **情形甲** ＝ 該側之街角**非**強制抵費地 ⇒ **可模擬**。
- **情形乙** ＝ 該側之街角**為強制抵費地** ⇒ 依 `K-9-5-12（四）` 其起算為「**該抵費地之遠側境界線**」，**⛔ 為 `0`** ⇒ 🛑 **⛔ 模擬**，出艙**第三種碼**。
- 情形之判別以 `verify/out/got_抵費地_退縮*.csv` 之 `強制抵費地` 欄為據，**同格具名該欄之逐字與其列**。

**自證閘（先決·⛔ 略）**
> 替身**關閉**時之輸出，須與 `W-G.9-277` 之落檔**逐位相同**（具名所比對之欄）。**⛔ 相同 ⇒ loud 拒測**（`rc = 5`），**⛔ 出艙任何反事實之數**。

**驅動**：**態甲**（生產態·`WV_K6_STEP0` 未設）× 二情境（`0m`／`3.5m`）× 替身（關／開）＝ **四次**。

**須出艙**
1. 逐側之情形甲／乙判別表（六街廓 × 二側 × 二情境）；
2. **逐宗** `ΔRw`／`ΔG`／`Δ幾何面積`（**未捨入**）；
3. 逐街廓 **`Δ抵費地` 之逐片面積**；
4. **守恆式**（`Σ配地幾何 ＋ Σ抵費地` vs `街廓面積`）於替身關／開**各自**成立與否（逐街廓）；
5. 六街廓之**到達與否**（反事實可能觸發別閘 ⇒ **第三種碼**·⛔ 視為紅或綠）；
6. `Σ` 層：全案 `ΔΣG`／`ΔΣ抵費地面積`。

**判別力二造**：甲[必有差] ＝ 任一 `W_0 > 1e-6` 之側（已知 `14` 格）⇒ 其 `Rw` 須變；乙[必無差] ＝ `|W_0| ≤ 1e-6` 之側（已知 `10` 格）⇒ 其 `|ΔRw| ≤ 1e-6`。**二造同色 ⇒ 器紅 `rc = 5`。**

🛑 **⛔ 判其為正確或錯誤、⛔ 提落地與否之主張、⛔ 稱其為「落地」**——**出艙即止**。材料交發單側轉土地語言。

### 四-2　「虛胖陷阱」錨腐之登記（末端追加·**⛔ 鑄號·⛔ 修**）

於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**純末端追加一節，題逐字：

```
## 🔧 錨腐之登記：`K-9-5-6`／`app.py` 所引之「虛胖陷阱 `:9344`」已腐，且 `app.py` 內⛔ 有其定義段（`W-G.9-278` §四-2·⛔ 上文一字不刪·純末端追加）
```

內載：`app.py` 全檔 `虛胖陷阱` 之命中（**`2` 列·皆為<u>引用</u>·⛔ 定義段**·先實測後填）；`app.py:10222` 與 `docs/rulings/K-6…md:890` 二處之錨 `` `:9344` `` 之**現行內容逐字**（實為 `_corner_buffer_S` 方位契約段）。
🔒 **`K-9-5-6` 之逐字規範本身<u>不因錨腐而失效</u>**——其「`W_i` 仍為 intrinsic 直量（MP → 本宗**遠側**分配界線之垂距）……**累積的是 `Rw`、不是 `W`**」係正文，⛔ 繫於該錨。
🛑 **⛔ 修二處之錨、⛔ 補寫定義段、⛔ 鑄號。**

---

## §五　收工閘（**commit 前·與被守護之動作分開呼叫**）

1. `numstat` 刪除欄逐檔具名（`ALPHA_LABELS` 之就地更正為唯一非零者）。
2. 生產碼判法四檔命中 `0` 行 ⇒ 零生產碼 ⇒ 逕推。
3. `.md`／`.py` 之 `CR` ＝ `0`；前綴檢之判別力二造。
4. **四簿期末**——**期值**：`GB` 相異 `167`／MAX **`169`**；自誤 `370`／`380`、`VR`／`K-9` **⛔ 動**。🔴 期值⛔ 為現值。
5. **坑之定義處清單自證**（報告入倉**後**量）——**期值 `82`／`82`**。
6. **自限復驗**：`app.py` blob 期初＝期末／`baselines` `298`·`a898f4e1…`（口徑 ＝ `quotePath` 預設）／`p3a` 未併線／`WV_BAKE` 未設／`K-9-29` 任何子項 ⛔ 落地／`GB-167`·`168`·`169` ⛔ 修／**替身⛔ 入生產碼**。
7. push 後 `ls-remote` 實查其值。

## §六　鑄號之授權（**⛔ 逾此**）

**`GB-169`**（`§一`）／**坑 `bd`**（`§三`）——各一號。🛑 `自誤`／`VR`／`K-9` 本批 **⛔ 鑄**；`§二`／`§四-2` 皆為末端追加（`戒 36`）。

## §七　🛑 本批⛔ 為之事

1. ⛔ 動生產碼 `34` 檔一字；⛔ 改五個 `*_EXPECT` 任一字；⛔ 修 `GB-167`／`168`／`169`。
2. **⛔ 落地 `K-9-5-12`**——本批之替身係**量測用之反事實**，⛔ 稱其為落地、⛔ 寫入倉之生產碼。
3. ⛔ 落地 `K-9-29` 任何子項；⛔ 落地 `GB-164`／`165`／`166`。
4. `GB-162` 三禁令維持；⛔ 引任何自 baseline 取得之數。
5. **⛔ 模擬情形乙**（街角為強制抵費地之側）；**⛔ 判 `K-9-5-12（五）3.` 之「未決」後半款**。
6. **⛔ 判二解算器孰誤、⛔ 判 `_宗地寬度` 是否落入虛胖陷阱、⛔ 提「結構閘應放寬」之主張**。
7. **⛔ 呈 KL 任何事**；⛔ 併線 `p3a`、⛔ 刪任何 ref。
8. **⛔ 於任何入倉文件宣稱一件尚未發生之事**——及於 commit 訊息與「已推送」之陳述。

**辦畢 ⇒ 出報告 `docs/reports/W-G.9-278R_…md` ⇒ 停**（⛔ 自續下一批）。

*本單受詞 ＝ CC 施工窗。發單側 ＝ claude.ai。⛔ 呈 KL。*
SELF_SHA256: b2f6d789f3fea8e5fd8ada32c5bba5e28afdc32cd96f366025553d398e8fdf93
