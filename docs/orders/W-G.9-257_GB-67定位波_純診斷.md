# W-G.9-257　`GB-67` 定位波（丙案·**純診斷**·⛔ 修·零生產碼·零土地後果）

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。承 `W-G.9-255`（`8a52dbd`）。
> 🛑 **KL 裁（`2026-09-08`）＝ 丙案**：「只做 `GB-67` 的**定位**，不修」。
> 🔒 **本單⛔ 動任何生產碼一字**（`app.py` blob 前後須 ＝ `4860f10828fb8ae9`）。全部器落 **scratchpad**（`GB-147`·⛔ 入倉）。
> 🔒 與 `W-G.9-256`（`c1` 之 `V-G` 改令）**互不相待**，得並行；與 `-253` 段乙（`-251`）亦然。

---

## §零　開工閘

1. `git fetch`；錨 ＝ **`origin/wip/s1-endpart @ 8a52dbd`**。不符 ⇒ 停機。
2. 號占用（`①`–`⑦`）：發單側於 `8a52dbd` 實測 `W-G.9-257` ＝ `0`／`0`／`0`／雙屬 `0`。CC 復現。
3. 次號當場復算。**本單⛔ 鑄自誤、⛔ 鑄 `GB`、⛔ 鑄 `K-9`**（`-256` 已鑄 `319`／`GB-153`；若 `-256` 尚未辦，本單仍⛔ 鑄）。
4. `probe_order_preflight.py`：`P-3`／`P-5` 綠。
5. worktree 先切 **wip 態**（`GB-152`）；環境須裝 `ezdxf`／`shapely`／`plotly`／`streamlit`／`pandas`／`openpyxl` 方能跑 harness。

---

## §一　工項零：本單原封入倉 `docs/orders/`。`P-5` 對拍。

---

## §二　發單側已測之基態（**CC 須獨立復現·⛔ 採信下列任一數**）

發單側於 `8a52dbd` 實跑 `verify/run_verification.py`（`RESULT: FAIL`／紅 `32`），並於 **scratchpad 複本**（⛔ 倉）對 `app.py:9523` 之 `②-宗 圍堵閘` 加診斷輸出後重跑，得：

**（甲）配對矩陣**——街廓 `R2`、宗數 `14`（配對 `91`）、總疊 `45.9766`、上界 `2.8906`、深度 `44.4700`：

| 配對 | 疊(㎡) | i 面積 | j 面積 | 佔總疊 |
|---|---|---|---|---|
| `宗0` × `宗3` | `26.1624` | `175.946` | `293.478` | `56.9%` |
| `宗0` × `宗2` | `19.8142` | `175.946` | `29.085` | `43.1%` |
| `宗1` × `宗2` | `0.0000` | `3.808` | `29.085` | `0%` |

⇒ **非零配對僅 `3`／`91`；`2` 對吃掉全額。** `宗2` 之 `68.1%` 面積落在 `宗0` 內。

**（乙）幾何清單**（全 `14` 宗 `valid=True`、`rings=0`、`type=Polygon` ⇒ ⛔ 自交、⛔ 洞、⛔ MultiPolygon）：

```
k= 0 area= 175.946 bounds=(310363.91,2651881.96,310397.51,2651915.17)
k= 1 area=   3.808 bounds=(310364.26,2651908.42,310371.69,2651915.73)
k= 2 area=  29.085 bounds=(310364.77,2651883.02,310396.18,2651916.21)
k= 3 area= 293.478 bounds=(310365.21,2651883.50,310400.56,2651921.02)
k= 4 area=  86.653 bounds=(310369.62,2651888.37,310401.85,2651922.45)
k= 5 area= 229.722 ... k=13 area= 437.015 bounds=(310408.31,2651930.98,310445.47,2651970.51)
```

⇒ `x0` 逐宗單調遞增（`310363.91 → 310408.31`），惟 **`y0` 於 `k=1` 突跳至 `2651908.42`、`k=2` 回落 `2651883.02`**；`宗1` 面積僅 `3.808㎡`（碎片）。`宗0` 之 `y` 上界 `2651915.17` **橫跨 `宗1`／`宗2`／`宗3` 之全域**。

**（丙）發單側之推定（⛔ 已證·CC 須證之或推翻）**：
`GB-67` 登記表所載受詞「**不相鄰**之宗之間為何會重疊」**恐為誤框**——`宗0`／`宗2`／`宗3` 之 `bounds` 顯示三者實**相鄰或近乎共域**；真受詞應為「**`宗0` 之推進終點（切帶 `s` 上界）為何未在 `宗0`／`宗1` 界面停住**」。`宗1` 之 `3.808㎡` 碎片與序列 `y0` 不連續為最可疑之觸發點。

---

## §三　工項一：定位（**scratchpad 器**·⛔ 入倉·⛔ 動 `app.py`）

🛑 **本工項只回答「是什麼」，⛔ 回答「怎麼修」。** 凡出現「應改為…」「建議…」之句 ⇒ 逾射程，刪之。

**Q-1　配對矩陣之獨立復現**　CC 自撰器復現（甲），並補報全 `91` 對之完整清單（含零者），以證「僅 `3` 對非零」非取樣所致。⛔ 採信發單側之三對。

**Q-2　`宗0`〜`宗3` 之身分**　具名四宗之 `暫編地號`／`原地號`／`歸戶`／`分配序位（投影序）`／其 `s` 區間 `(s_min, s_max)`。🔑 **`kept_iv` 只落了 `k=0` 一筆**（發單側實測），須查其成因——是 `kept_iv` 僅存池帶（非宗）抑或另有斷路。

**Q-3　推進終點之受詞**　定位 `宗0` 之 `s` 上界由何式算出（`app.py` 具名行號 ＋ 逐字），及該式所依賴之輸入（`W`／`Rw`／`F`／深度／退縮）於 `宗0` 之實值。與 `宗1` 之 `s` 下界比對，具名二者之差。

**Q-4　碎片之角色**　`宗1`（`3.808㎡`）之來源：係 CAD 原有之小宗、抑或切帶所生之殘片？若為後者，具名其產生處。並測**排除 `宗1` 後**（scratchpad 假想）總疊是否收斂——⛔ 以此作為修法主張，僅作**成因隔離之證據**。

**Q-5　上界式之適配性**　發單側判「`(宗數−1)×0.005×深度` 之模型（每對疊一點點·遍佈全體）與實測形狀（單宗跨多宗·集中於 `2` 對）不符」。CC 以（甲）之完整矩陣證之或推翻之。**⛔ 主張調整該上界。**

**Q-6　射程**　`R1` 綠、`R2` 紅。查 `R3`〜`R6` 是否亦破——**`StepG` 於 `R2` 中止 ⇒ 須以 scratchpad 之「記錄不 raise」形跑完全街廓**方得知。此問直接決定 `GB-153` 之射程是否須擴。

**Q-7　telescoping**　同批具名 `R2 left ΣRw_實跑=100.02 ≠ R(W_final=34.39)−R(W₀=6.98)=44.41（Δ=55.606）` 之逐界面明細，並判其與 `②-宗` 破**是否同源**（同為 `宗0` 之推進所致，抑或二病）。

---

## §四　工項二：出艙形

報告 `docs/reports/W-G.9-257R_GB-67定位.md`，須含：

a. `Q-1`〜`Q-7` 逐項之**機械證據**（器之逐字 ＋ 輸出逐字）。凡「不可得」者，具名其**阻斷點**，⛔ 以「未查」了事。
b. **成因之判**：三選一並具名證據——① 發單側推定（`宗0` 推進終點）成立；② 推定為偽，真成因為 `X`；③ 尚不能判，具名**還差哪一項證據**。
c. `GB-67` 登記表之**受詞更正**（若 `Q-2`／`Q-3` 證「不相鄰」為誤框）：以 **`GB-67` 之補款**形、`W-G.4` **檔末**追加（⛔ 節末·`自誤 316` 之教訓），⛔ 動原列一字，⛔ 鑄新號。
d. `GB-153` 射程之更正（依 `Q-6`）：同 c 之形。
e. **⛔ 含任何修法主張、⛔ 含任何 `app.py` diff。**

🔒 **scratchpad 之器**得於報告內**逐字引用**（供他窗復現），但⛔ 入倉為檔（`GB-147`）。

---

## 【驗收】

| 閘 | 判準 | 停機款 |
|---|---|---|
| `V-P1` | `app.py` blob 前後 ＝ `4860f10828fb8ae9`；十一檔全部逐位不變 | 🔴 |
| `V-P2` | `git status` 之 scratchpad 檔數 ＝ `0`（未追蹤者不得入 commit） | 🔴 |
| `V-P3` | `Q-1` 之 `91` 對完整清單出艙，且非零者之和 ＝ `45.9766`（逐位） | 🔴 |
| `V-P4` | `Q-2` 四宗之身分具名（暫編／原地號／歸戶／序位／`s` 區間），或具名阻斷點 | 🔴 |
| `V-P5` | `Q-6` 之 `R3`〜`R6` 破否已測（非「因中止而未知」） | 🔴 |
| `V-P6` | 報告內「應改／建議／修正為」之命中 ＝ `0`（射程閘） | 🔴 |
| `V-B` | append-only：`W-G.4` deletions ＝ `0` | 🔴 |
| `V-C` | 自誤 `MAX`／`GB MAX`／`K-9 MAX` **前後不變**（本單⛔ 鑄號） | 🔴 |

---

## §五　產出與放行

**全批零生產碼、零土地後果 ⇒ 直推主線，⛔ 候 KL 逐 commit。**
出艙後呈 KL **一句實務語言**：「`R2` 那 `46㎡` 的重疊，成因是 `X`；修它要動的是 `Y`，估計影響 `Z` 個街廓」——KL 據該句裁下一波（甲：開修／乙：續押）。
🛑 ⛔ 於本單內開修，⛔ 向 KL 索取修法授權。

---

## §六　待落地登記（本單登記·⛔ 辦）

| 項 | 狀態 |
|---|---|
| `GB-67` 之**修**（成因定位後） | 🔴 候本單結果 ＋ KL 裁 |
| `c2` 之放行證據形（`V-H` 不可得） | 🔴 發單側另議·**得俟本單**——若 `GB-67` 可修，`V-H` 即回復可得 |
| `-249` 段三觀測之可得性 | 同上 |
| 五級逐值迴歸 | 同上（KL 已裁「得以替代證據放行」·`W-G.4:4723`） |
| `-251` 段乙／`-256` `c1` | ⛔ 受本單影響·並行 |

---

*受詞 ＝ CC。純診斷、直推主線；⛔ 修、⛔ 動生產碼。*

SELF_SHA256: 0394745232cb7e7c35a4037b396179951e7ff7aef7389e873dda771aa6874496
