# W-G.9-242　宣告框 `D3` 補款 ＋ 排序準據母體改 **AST** ＋ 五級依賴序入典 ＋ 自誤二則

> **零生產碼**（`CLAUDE.md` ／ `docs/` 之 `.md` 追加 ＋ 一份報告）⇒ **CC 得逕行 push**。
> ⛔ 動 `app.py`／`verify/**.py` 任何一字，**含新增檔**。
> 🛑 **本單⛔ 抽 seam**——`-241` 曾預告抽 seam 為本號；因 KL 於 `2026-09-06` 裁定
> 排序準據母體改 **AST 為主**（`丙` 案），完備清單之重生成為抽 seam 之**前提**，
> 故抽 seam **順延為次一單**，其為生產碼、須 KL 逐 commit 放行。動之即停機上呈。

---

## §零　開工閘（缺一即停）

**`S-0`（單之完整性）**　取本檔**最末**一個 `SELF_SHA256`，依既有口徑（自檔首至
`SELF_SHA256:` 該列之前一個 `\n`（含該 `\n`）為止之全部位元組）重算對拍。不符 ⇒ 停機。

**`S-1`（號占用·宣告框）**　依 `CLAUDE.md` 之宣告框 ①–④ 查 `W-G.9-242`。

發單側於 `2026-09-06` 當場實測（**三軸**：來源 `blob@cb4b82f27f33e1182daaf413a1417035f4e333e6`
／母體 **全 `docs/` 558 檔**（其中 `19` 檔為 `.png`／`.jpg` 二進位讀不到，檔名皆⛔ 含單號）
／粒度框 **檔框 ＋ 列框**）：

| 號 | 宣告框 檔／列 | 鬆框 檔／列 | 角色 |
|---|---|---|---|
| **`W-G.9-242`** | **1**／**1** | **2**／**8** | 受詢 ⇒ 🔴 **咬中**（見下 `S-1′`）|
| `W-G.9-241` | 3／10 | 3／10 | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-243` | 0／0 | 0／0 | 對照組 乙（次號·須 `0/0`）✅ |

CC 須**自行復算**上表三列，⛔ 採信本單任一值。

**`S-1′`（本單之特則·⛔ 推廣至他單）**
`W-G.9-242` 之宣告框唯一落點須**逐筆比對**（考古 `82`：⛔ 憑命中數）：

```
docs/orders/W-G.9-241_排序準據seam偵察與交接文更正.md:4
> ⛔ 動 `app.py`／`verify/**.py` 任何一字。**本單⛔ 抽 seam**——抽 seam 為 `W-G.9-242`，
```

其形 ＝ **前瞻委派**（本單**不**做，`242` 才做），屬宣告框 `②` 所列之「引用」而應排除，
卻因同列共現「本單」二字被 `D3` 咬中 ⇒ **`②` 與 `D3` 同咬**（交接文 `§七` 該列所指之互斥，
`W-G.9-235R2` 之後**第二實例**）。

🔒 **KL 裁（`2026-09-06`）：判 `W-G.9-242` <u>未占用</u>，續用本號；同批補 `D3` 邊界款。**
⛔ CC 或發單側自裁——「已占即停並回報·⛔ 自行改號」之報已行、裁已下。

🛑 **停機條件**：宣告框之命中若**非恰為上列該一列**（多一列、少一列、或落點相異）⇒ **停機上呈**。

**`S-2`（append-only）**　本單受詞之既有二檔皆為**純末端追加**，⛔ 改其上文一字。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-242_宣告框D3補款與排序準據母體改AST與依賴序入典.md`。⛔ 改一字。

---

## §二　工項一：宣告框 `D3` 之**邊界補款**（KL 裁 `2026-09-06`）

### `a`　受詞與體例

受詞 ＝ **`CLAUDE.md`**，**純末端追加**一節，體例依「**號占用閘之框改宣告框**」節先例。

🔒 **⛔ 回改「`入倉前之號占用閘`」既有三款一字**，亦⛔ 回改「`號占用閘之母體`」、
「`號占用閘之框改宣告框`」二節任一字。
🔒 **受詞之字樣錨**（`行號衛生` 第 `4` 款）＝ `號占用閘之框` ——CC 須報其**改前**命中數。
**判別力自檢**：寬字樣 `宣告框` 於同一母體之命中數須**同格併報**，且須 `>` 前者。

### `b`　追加之逐字（⛔ 改一字·⛔ 自行潤飾）

````markdown
## 🔧 宣告框 `D3` **自稱形之邊界**（`W-G.9-242` 工項一·KL 裁 `2026-09-06`·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為補款之補款之補款（`戒 36`）。

🩸 **案由**　`D3` 之立法本意在抓「**本單 ＝ `W-G.9-NNN`**」之**自宣告**，
其實作卻僅為「同列共現」⇒ 對**否定式**（`本單⛔ …`）與**委派式**（`… 為 W-G.9-NNN`）
一律**偽陽**，而該二形正是 `②` 所列之「引用」⇒ **`②` 與 `D3` 同咬**。
🔒 **當場復現之二實例**：`W-G.9-235R2` 之表列；
`W-G.9-242`（唯一落點 ＝ `docs/orders/W-G.9-241_…:4`「**本單⛔ 抽 seam**——抽 seam 為 `W-G.9-242`」）。
🔒 **其為可自我複製之缺陷**：單文只要寫一句委派式，即毀掉被委派之號。

🔒 **補款（自本節起為正字規則）**

> **⑤　`D3` 自稱形之邊界**
> `D3` 之「**同列含「本單」二字 ＋ 該號**」款，**僅於該號 ＝ <u>該檔檔名所載之單號</u>時成立**。
> 🔒 **理由**：「本單」之受詞**恆為該檔自身**；同列出現之號若與檔名之號相異，
> 必屬**他指**（引用／委派／前瞻）⇒ 依 `②` **⛔ 構成占用**。
> 🔒 `D3` 之**另一款**（**列首即該號**）**⛔ 受本款影響**，亦⛔ 受本款放寬。
> 🔒 `D1`／`D2`／`②`／`③`／`④` **一字未動**。
> 🔒 **⛔ 追改任何既出之取號結論**（`常規四（九）五`）——本款只約束**今後**。

🔒 **首次施行**（`W-G.9-242` 之 `S-1′`）：`W-G.9-242` 於**改前**框 ＝ 宣告框 `1`／`1`，
唯一落點為上開委派式 ⇒ 依本款（及 `②`）**未占用**。
**此判係 KL 於 `2026-09-06` 之裁**，⛔ CC 或發單側自裁。

🛑 **本款⛔ 及於「作廢單」之未決項**——「是否於宣告框增設『作廢單⛔ 計入』一款」
仍為既存之意思決定（見「號占用閘之框改宣告框」節末），**本節⛔ 就其作成條文**。
````

### `c`　驗收

`deletions ＝ 0`；並報字樣錨之**改前／改後**二數（二形同格併載）。

---

## §三　工項二：排序準據母體改 **AST 為主 ＋ 列框為對照組**（KL 裁 `2026-09-06`·`丙` 案）

### `a`　案由（`-241R §三 d` 已具名·CC 須復現，⛔ 採信本單）

`-241` 所定之母體（`sorted(` ∪ `.sort(` ∪ 級字面·**列框**）經 `-241R` 當場機檢，漏抓三類：

| 洞 | 量 | 要害 |
|---|---|---|
| `max/min(…, key=)` 未入框 | 真漏 **2** 列 | `verify/wf_f4.py:433`（歸戶錨點宗之選定 ⇒ **全部距離準據之起算點**）／`:703`（同級比例分攤之剔除準據）|
| 鍵在**續行** | **3** 處 | `:377/:379`、`:662/:663`、`:1346/:1347` |
| 單自身錨表落在自身母體外 | **2**／`4` | `verify/wf_f2.py:99`／`verify/wf_f4.py:798` |

### `b`　主框：**AST**（⛔ 列框）

母體 ＝ `verify/wf_f0.py` ∪ `verify/wf_f2.py` ∪ `verify/wf_f4.py` ∪ `app.py`。

以 `ast.parse` ＋ `ast.walk` 取下列節點，逐點記 `lineno`–`end_lineno`（**故續行天然涵蓋**）：

1. `Call` 且 `func` 為 `Name(sorted)`／`Name(min)`／`Name(max)`；
2. `Call` 且 `func` 為 `Attribute(attr='sort')`（即 `xs.sort(...)`）；
3. `Call` 且 `func` 為 `Attribute(value=Name('heapq'))` 之 `nsmallest`／`nlargest`，
   或 `Name(nsmallest)`／`Name(nlargest)`；
4. 上列各節點之 `keywords` 中 `arg == 'key'` 者，其**實參之完整原文**（`ast.get_source_segment`）。

🔒 **出艙之三集（同格併載，⛔ 只報其一）**
`AST ∖ 列框`（新捕）／`列框 ∖ AST`（**若非空即紅**，須逐列具名並判其成因）／`AST ∩ 列框`。

🛑 **必須命中之五點**（`-241R` 已具名·任一未捕 ⇒ **停機上呈**）：
`wf_f4.py:433`、`:703`、以及 `:377/:379`、`:662/:663`、`:1346/:1347` 三處續行站點之**鍵所在列**。

### `c`　對照組：列框（沿用 `-241` 之框·⛔ 改其定義）

沿用 `sorted(` ∪ `.sort(` ∪ 級字面。**其地位 ＝ 對照組**，⛔ 判準。
🔒 **判別力自證**（承 `-241R §三 c` 之形）：對照組之數須**非零且非全等於 AST 之數**，
否則⛔ 證得量測器非恆綠。

### `d`　`app.py` 之界（`-241R §七 3` 已標旗）

`app.py` 之 `key=` 命中 **`216`** 列而 `key=lambda` 僅 **`48`** 列——差額多為 **Streamlit widget 參數**。
🛑 **⛔ 以 `216` 為「排序準據數」出艙**。CC 須報 AST 下 `app.py` 之命中數，
並與 `216`／`48` **三數同格對照**，逐字載明 AST 為何能排除 widget 參數（＝ 只取上列四類節點）。

### `e`　掃描器之處置（🔴 界限）

🛑 掃描器係**拋棄式**：得於工作副本內建立、跑完即棄，**⛔ 入倉**、
**⛔ 置於 `verify/` 或 `app.py` 側**（會動閘 `P` 之母體目錄）。
**⛔ 執行 `verify/run_all.py`／`run_verification.py`**——本單⛔ 需其輸出。
入倉者**只有其輸出表**（`docs/reports/` 之報告）。

### `f`　角色判（⛔ 得省）

AST 只抓**靜態**表達；`key=f`（`f` 另處定義）、動態組成之比較子仍須人判。
故逐點之 `A`／`B`／`C`／`D` 四角色判**沿用 `-241R` 之口徑、逐點為之**，⛔ 抽樣、⛔「看起來像」。
🔒 完備性檢查須 **GREEN**（各樣式逐列全數列出）方得出艙。

### `g`　驗收

報告須載：AST 命中總數、三集之數與逐列具名（`AST ∖ 列框` 全表）、五點必中之逐點確認、
`app.py` 三數同格對照、角色分布、完備性檢查之綠。
🔒 凡出艙任何命中數，須同格載**三軸**（來源 commit ／ 母體 ／ 粒度框·`VR-091`）。

---

## §四　工項三：五級實作**依賴序**之入典（KL 裁 `2026-09-06`）

### `a`　案由

`-240 §六` 所列之依賴序係**發單側之建議·候 KL 排序**；KL 已於 `2026-09-06` 裁定。
併裁一項**新款**：**五級實作得以替代證據放行**。

### `b`　登記之逐字（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` 末端·⛔ 改一字）

````markdown
## 🔒 待落地項登記：**五級實作之依賴序**（`W-G.9-242`·KL 裁 `2026-09-06`）

🛑 **本節⛔ 鑄 `GB` 號**——其為**排序之裁**、⛔ 阻塞項。

🔒 **KL 所裁之序**

```
1  K-9-1 最小建築面積 UI 欄            （r2 之前提·有土地後果）
2  區外道路清單 ＋ 正面道路識別符暴露   （r3 之前提·有土地後果）
3  排序準據 seam 抽出                  （純技術·無土地後果·得與 1／2 並行）
4  五級實作                            （r1–r8 齊備後）
5  GB-67 ／ telescoping                （驗收能力·得與 1–3 並行·得整批後推）
6  事後協調合併重算                    （須在 4 之後）
```

🔒 **`1`／`2` 何以在前（技術事實·⛔ 意思決定）**
`key(T) = (r1…r8)` 為**詞典序**；`r2`（最小建築面積）與 `r3`（正面道路）係第 `2`／`3` 鍵
⇒ **支配 `r4`–`r8`**。缺其一而實作五級，凡該鍵可分之案，其序必與所裁之序相異
⇒ **⛔ 是「暫時不夠準」，是「實作了另一條規則」**，且**有土地後果**。
錨：`app.py`「最小建築面積」命中 `1`（且該列為註解）／「區外道路」命中 `0`。

🔒 **`5` 何以得後推（KL 裁·本節之新款）**
`GB-67`（退縮 `0`·`②-宗 圍堵閘`）與 telescoping 結構閘（退縮 `3.5`）經 `W-G.9-241R` 實跑坐實：
二者**係二事**、中止點相異、且**皆為 harness 專屬**（`app.py` 命中 `0`）⇒ **⛔ 影響 app 之配地**。
其所阻塞者為 `R4`／`R5`／`R6` **逐值迴歸之驗收能力**，⛔ 配地之正確性。

> **🔒 KL 裁：五級實作<u>得以替代證據放行</u>，⛔ 俟 `GB-67`／telescoping 解除。**

🛑 **該裁之代價（逐字載明·⛔ 稀釋）**
五級實作之驗收**⛔ 得用 `R4`／`R5`／`R6` 逐值迴歸**——二情境皆於街廓 `R2` 中止
⇒ `F.0`〜`F.4` 全數跳過、`verify/out/got_F.2_*`／`got_F.4_*` 一檔未生成。
其驗收只得用 **`R2` 以前之替代證據 ＋ 單元級證據**。
🛑 **⛔ 讀為「級之命中 ＝ `0`」**——`-241R §三 c` 逐字：現態下級之命中筆數係**不可測**，
「量不到」與「量到而為零」**⛔ 同義**。
🛑 **⛔ 以本裁為由放寬 `5` 之任何解除條件**；二閘各有其解除條件，須分別出單。
````

### `c`　驗收

`deletions ＝ 0`；並報字樣錨之改前／改後二數。⛔ 於本單實作上列任一項。

---

## §五　工項四：自誤**二則**（**分鑄**·號須 CC 當場復算）

🔒 **末號之獨立復算**（框 ＝ `VR-091` 補款二 ① 之正字定義框 `A` **＋ 範圍框**；
母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔；粒度框 列框）。
🔒 **範圍框須<u>展開</u>後併入相異集**——`## 自誤 NNN〜MMM` 之攢批標題形，
⛔ 只計其二端。
發單側於 `cb4b82f` 實測 ⇒ 相異 `285`／`MAX` `292`／`MIN` `7`／缺號 `[106]`
（恆等式 `MAX − MIN + 1 − 缺號數 == 相異` ⇒ `True`）⇒ **`N` ＝ `293`**、**`N+1` ＝ `294`**。
**CC 須自行復算；不符即停機上呈。**

### `自誤 N`｜**以鬆框之數出艙為號占用之判準**

🩸 **案由**　發單側（`W-G.9-242` 出單窗·入場報告）以**裸列框**算 `W-G.9-242` 之占用，
得「列框 `8` ⇒ 已占」，並以「`242` 已被自指占用」為題呈 KL 裁示。
而 `CLAUDE.md`「號占用閘之框改宣告框」款 `④` 逐字：
**「判準取宣告框，鬆框僅作<u>漏框偵察</u>（＝ 宣告框漏抓時之上界指標），⛔ 為判準」**。
實測：宣告框 `1`／`1`（唯一落點 ＝ 委派式），鬆框 `2`／`8`。

🔒 **受詞** ＝ **判準之框**（⛔ 該框所得之數）。
🔒 **族** ＝ `W-G.9-238` 交接文 `§六` 之「憑設想給錨」（**第 `9` 次**）。
🔒 **加重情狀**：該款之全文即在 `CLAUDE.md`，且該窗當時**已 clone 全倉、具直接讀取權**，
並在同一回合內讀過 `docs/` 多檔——**追溯止於一層**（`自誤 194`／`292` 同形）。
🔒 **未致實害之原因**：KL 未逕裁，先命詳說 ⇒ 於裁示前攔下；
**⛔ 讀為「機制有效」**——攔下者係 KL 之追問，⛔ 發單側之自檢。

🔒 **通則（自本則起）**
> **凡出艙任何「占用／未占用」之判，須於同格載<u>所用之框名</u>並引其正典出處（`檔:列`）。**
> **⛔ 以自撰之框行之**；框有二以上者依 `④` **二框並列、各載框**，並逐字指明其一為判準。

**責** ＝ **發單側**。

### `自誤 N+1`｜**斷言正典「不存在」而未窮查**

🩸 **案由**　同窗於同一回合斷言：「正字框 `A` 之定義⛔ 在文內、亦⛔ 在 `docs/` 可檢」。
**偽**——其定義在 `docs/驗證裁定登記表.md:4615`（`VR-091` 補款二之補充），
且依該定義（定義框 ∪ **範圍框展開**）當場逐位復現：
相異 `285`／`MIN` `7`／`MAX` `292`／缺號 `[106]`／恆等式 `True`，
與 `交接文_W-G.9-240` 所載之 `282`／`289` 差 `3`（＝ `290`／`291`／`292` 三則）⇒ **完全相符**。

🔒 **受詞** ＝ **正典之存否**（⛔ 其內容）。
🔒 **族** ＝ `W-G.9-238` 交接文 `§六` **紀律 `8`** 之違反
（「凡擬出艙之問，出艙前須以**至少三組同義詞**查……並載明所查之同義詞集與命中數」）。
🔒 **加重情狀**：該窗只查 `正字框` **一形**，且其管線帶 `定義|＝|=` 之**過濾式**
——**過濾式本身把命中打掉**（未過濾之命中含 `docs/驗證裁定登記表.md:4634` 之三數表）。
⇒ 其所報之「查無」實為「**過濾掉了**」。

🔒 **通則（自本則起）**
> **凡出艙「`X` ⛔ 存在／⛔ 可檢」之<u>否定斷言</u>，須同格載所查之字樣集（`≥ 3` 形）與各自命中數；**
> **⛔ 以帶過濾之管線之空結果為據**——過濾式須另列，並同格併報其**未過濾**之命中數。

**責** ＝ **發單側**。

### 鑄號收工閘（`常規四（九）二`·三項全綠方得收工）

① `MAX` 自 `292` → `294`；② 缺號恆 `[106]`；③ 盲區（`> MAX` 之落號）`= 0`；
併報 `GB` 簿本批**未動**（零污染）。

---

## §六　產出與 push

- **零生產碼** ⇒ CC **得逕行 push**，⛔ 待授權。
- **閘 `P` 五檔**（`app.py`／`verify/selection_pipeline.py`／`run_verification.py`／
  `stepg_pipeline.py`／`run_all.py`）之內容 `sha256` 須**逐位不變**，前後各報一次。
  發單側於 `cb4b82f` 實測前 16 位：`e3e464ea2493d046`／`6c6783420dbc635c`／
  `e48f24c84079f365`／`0260273350021311`／`4ba89fef90979491`。CC 須自行復算。
- 報告須逐項載：`S-1` 三列 ＋ `S-1′` 之逐筆落點、工項一之 `deletions` 與字樣錨二形二數、
  工項二 `§三 g` 之全部、工項三之 `deletions` 與字樣錨、`N`／`N+1` 之自行復算值與收工閘三綠。
- 🔒 凡出艙任何命中數，須同格載三軸（來源 commit ／ 母體 ／ 粒度框·`VR-091`）。

SELF_SHA256: b1181a93b258a7261bc25ca944233a85aaae354781da0e444e9716b8eb78e88e
