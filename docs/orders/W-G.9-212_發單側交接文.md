# 🤝 `W-G.9-212` 發單側交接文（`claude.ai` 側·`2026-09-03`）

> **給下一個 `claude.ai` 窗。** 讀完本文之**第一件事**是 `§二` 之逐項重量，⛔ 採信本文任何數字。
> **態**：`wip/s1-endpart` ＝ **`259f9f0d9b8322cde59d315572f2ac8fba256eae`**（發單側自 `ls-remote` 實查·`2026-09-03`）

---

## §一　開窗後之首要動作（🛑 逐條打勾，⛔ 以「我讀過本文」代之）

🩸 **本款係 `自誤 259` 之直接產物**：前窗開窗第一件事即通讀 `W-G.9-204`，其中白紙黑字寫著「`-198` 之 `S-0a` 須同樣預留豁免」，出單時仍原封沿用舊本而漏補，CC 一開工即撞紅。**讀過與照辦是兩件事。**

1. `git -c core.autocrlf=false -c core.quotepath=false clone --branch wip/s1-endpart …`（🛑 `clone` 用 `false` 係必要；`add` 既有檔則**一律常規 `add`**——`GB-139`）。
2. 逐項重量 `§二` 之期末錨。**任一不符 ⇒ 停下來查，⛔ 開工。**
3. 自倉重抽 `K-9-24`／`K-9-25`／`K-9-26`／`K-9-27`／`K-9-28` 之逐字（`docs/rulings/K-6_…md`），⛔ 以本文所述為準。
4. 🔴 **出單前之檢查項（逐條打勾）**
   - [ ] `S-0a①` 之 `app.py` blob 錨 ＝ **`a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190`**（⛔ 沿用 `b9a50a2e…`／`6f0317e4…`／`144349c8…` 任一舊值）
   - [ ] `S-0a②` 之比較基座 ＝ **`6e677223e4ab2823f1a2d276bc5635472b4d51a7`**；判準 ＝ 逐列⛔ 含 `CLAUDE.md 🔧 二` 之**四檔**（`app.py`／`stepg_pipeline.py`／`run_all.py`／`run_verification.py`）；**`verify/probes/**` 之新增⛔ 使閘紅**（`VR-089` 肢一）
   - [ ] 凡「數某字樣」之閘 ⇒ **三者同源**已機械確認（母體檔 ＝ payload 落地檔 ＝ 落地前計數檔·`自誤 265`），且期望**⛔ 寫死常數**（`VR-090` 肢二）
   - [ ] 凡判別力自證 ⇒ 見證受詞由**機械測試**選出，**⛔ 以檔名建議**（`VR-090` 肢一·`自誤 263`）
   - [ ] 凡「⛔ 有 X」之現況斷言 ⇒ **標時態**，且已自問「同單所授權之變更會否**新增**一個 X 之受詞」（`自誤 262`）
   - [ ] 凡令受單方推送某物件 ⇒ 已載明該物件與其**本機分支**之關係與處置（`自誤 261`）
   - [ ] 落地點之列舉已涵蓋**出艙產生鏈全程**（計算處 → 序列化處 → 比對／守衛處·`自誤 260`）
   - [ ] 單內二條款之**關係**已具名（如「檔數上限」vs「報告同批」·`自誤 257`）
   - [ ] 對執行環境下全稱命令前，已先**唯讀量測**該環境（`自誤 256`）

---

## §二　期末錨（🛑 全部須逐項重量·⛔ 採信）

| 受詞 | 值 |
|---|---|
| `wip/s1-endpart` | `259f9f0d9b8322cde59d315572f2ac8fba256eae` |
| 追蹤檔／`.md` | `1590`／`523` |
| `app.py` | blob `a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190` |
| `verify/selection_pipeline.py` | `167a7beb8e9f7176ea70a53e6ad3bcb37c130b38` |
| `verify/run_verification.py` | `d07955c58e67ec06260971a74b5aabe5e846f106` |
| `verify/stepg_pipeline.py` | `fef5865a73033274697790e0e307aedbf6122422` |
| `verify/run_all.py` | `e5cf9a224517533b183b34e4fd0f5fbe35ee406b` |
| `K-6_…md` | `279126` B |
| `K-9`（`K-6` 單檔·框 `` ^### 🔒 K-9-([0-9]{1,3})　 ``·扣 `99`） | 相異 `15`／MAX `28` ⇒ 次 **`29`** |
| `GB`（`W-G.4_泛用阻塞項登記表.md` 單檔·框 `` ^#{2,4} `?GB-N`? ``·扣 `997`） | 相異 `55`／MAX `141` ⇒ 次 **`142`**　🛑 `GB-138`：定義列式⛔ 經正典釘定 ⇒ 此二數**須同格載其框**，⛔ 出艙「缺號集」 |
| 自誤（`W-G.9波_claude.ai側自誤登記.md` 單檔） | 相異 `230`／MAX `265` ⇒ 次 **`266`** |
| `VR`（`docs/驗證裁定登記表.md` 單檔·扣 `999`／`751`） | 相異 `75`／MAX `90` ⇒ 次 **`091`** |
| 戒（**全倉 `.md`**·鑄號地 ＝ 自誤登記檔） | 相異 `23`／MAX `40` ⇒ 次 **`41`** |
| 側分支（**保留⛔ 刪·史料**） | `verify/W-G.9-198R-c1` ＝ `b772f8aa…`／`-c2` ＝ `a1107a17…`／`-c3` ＝ `6e677223…` |

---

## §三　本波已完成者：`W-G.9-198R` **結案**

三 commit 皆已入主線（逐一 `merge-base --is-ancestor` ＝ 含）：

| commit | 物件 | 內容 | 土地後果 |
|---|---|---|---|
| `1` | `b772f8aa…` | 街角指數**名次**之產出與出艙 ＋ 段二全域排序（唯讀）＋ `R1-8` 措辭更正 | **零**（土地欄 `0` 格變） |
| `2` | `a1107a17…` | 🔴 步驟 0 回歸無條件 ＋ 歸側四分支 ＋ `K-9-25` 量測前提 | 🔴 **有** |
| `3` | `6e677223…` | 段一「上鎖」（唯讀） | **零**（四 CSV 逐位相同·`34` 閘 `diff` `0` 列·`run_all` stdout 全文 `diff` `0` 列） |

🔴 **`commit 2` 之土地後果（本波唯一改變分配結果者）**
`R4` 之 `628(1)`（左·真 `G` `1521.05`）與 `628-1(1)`（右·真 `G` `457.73`）同歸戶 `G009`、共用界線 `47.057897` m
⇒ 合併為 `628(1)+`；右臨截角指數 `0.992472` > 左 `0.991558` ⇒ **歸右街角**；
左街角依 `K-9-24 二·5` 成為**強制抵費地**：退縮 `3.5` m ＝ **`226.01 ㎡`**／退縮 `0` m ＝ **`116.08 ㎡`**（二者皆嚴格等於該街角規定範圍）。
其餘 `5` 街廓逐列不變；四閘違規 `96`／`104` → `92`／`100`。
🛑 **土地後果⛔ 經 KL UI 實跑確認**（`GB-123` 未解除·harness 通道走不到 app 路徑之終局）。

---

## §四　本波所鎖之裁定（皆已入倉·⛔ 得憑印象引用，須自倉重抽逐字）

| 號 | 一句話 |
|---|---|
| `K-9-26` | 段二全域排序之鍵 ②③ 為**降冪**（① 名次升冪／② 街角試算 `G` 降冪／③ 指數降冪／④ 地號升冪）·KL 逐字「面積大的、街角指數大的 排前面」 |
| 🔴 `K-9-27` | **歸側之四階階梯·取代 `K-9-24 二·2`**：① 指數名次小者 → ② 指數（`4dp`）大者 → ③ `M` 與該側街角規定範圍之**真交集面積**大者 → ④ **取 `p1` 側**。🛑 `p1` 側⛔ 作廢，係**降為第 ④ 階** |
| 🔴 `K-9-28` | 同街廓 `|D| ≥ 2` 時之**處理次序**（A 群／B 群分群 ＋ 四階）＋ 🔴 **凍結款**（排序鍵取街廓開辦前初始態·⛔ 隨處理重算）＋ 第六項之**順序無關性推定**（🛑 係推理⛔ 量測） |
| `VR-088` | 舊 `W-G.9-198` 入倉係**存查**、⛔ 得作施工依據 |
| `VR-089` | 肢一：閘之字面與其立法理由互斥者，**以立法理由為準**；肢二：`commit 1` 之射程准三檔 |
| `VR-090` | 肢一：判別力自證之見證受詞須**機械測試**選出、⛔ 檔名建議；肢二：「數字樣」之閘期望⛔ 寫死常數 |
| `戒 40` | 凡「`Δ` 恰 ＝ payload」「逐位元組相同」之等同斷言，須同格並報**二端**之位元組數 |
| `GB-139` | KL 本機 system 層 `core.autocrlf=true`（`i/lf` `w/mixed`）⇒ 二肢（`add` 一律常規／閘之受詞取倉側 blob）·**波末方改設定** |
| `GB-140` | `priority_index` 之精度**三處相異**（段一 `4dp` 有後果／段二 `6dp` 無後果／🔴 歸側並列 `4dp` **有後果**）＋ 🆕 `K-9-27` 第 ③ 階真交集面積之精度亦未釘定 |
| `GB-141` | `|D| ≥ 2` 之次序相依——正典已由 `K-9-28` 補，**碼面未依**；解除須**二者並存**（碼面實作 ＋ **窮舉探針**機械驗證） |

🔑 **`K-9-27` 之警語（KL 已閱·入正典）**：「名次小 ＝ 指數高」**於同一側內恆真、跨側⛔ 成立**。KL 選名次為第 ① 階，即係擇「在自己那側贏得較徹底」勝於「絕對評分較高」。⛔ 得視二者等價。

---

## §五　🔴 下一波之工作內容（尚未開工·**幾何影響波·須另開新窗**）

🛑 **`K-9-27`／`K-9-28` 已入正典，現行碼即與正典不一致**（現行只比指數、無次序規則、無凍結款）。該不一致**已由 `W-G.9-211 三-3` 之更新註具名** ⇒ ⛔ 得視為新缺陷、⛔ 得由 CC 逕行修正——它**就是**下一波的工作。

| # | 工項 | 性質 | 依賴 |
|---|---|---|---|
| `1` | 碼面重寫歸側為 `K-9-27` **四階階梯**（現行 `app.py` 逐字 `_to = _P1 if round(_idx_of(_cm[_P1]), 4) >= round(_idx_of(_cm[_P2]), 4) else _P2`） | 🔴 **有土地後果**·走側分支 ＋ KL 放行 | — |
| `2` | 碼面實作 `K-9-28`（分群四階 ＋ **凍結款**） | 🔴 有土地後果 | 與 `1` 宜同批 |
| `3` | **窮舉探針**驗證順序無關性：合成候選組態（各側 `2`〜`4` 候選 × 達標與否 × 指數高低全排列）× 全部處理順序 ⇒ 證結果與順序無關 | 🟢 零生產碼（`verify/probes/**`） | 後於 `2` |
| `4` | 🔴 **`GB-140` 之三處精度同批上呈 KL**（段一 `4dp`／段二 `6dp`／歸側 `4dp`／＋真交集面積之精度）——三種收法之土地後果相異，**須先量測後裁** | 🔴 **須 KL 域裁** | 宜先於 `1` |
| `5` | `GB-141` 之解除（須 `2` ∧ `3` 皆綠） | 🟢 登記 | 後於 `3` |

🔒 **建議次序**：`4`（先問 KL，免得 `1` 寫完又要改）→ `1`＋`2` 同批 → `3` → `5`。

---

## §六　工作法（本波所立·下一波沿用）

1. 🔒 **側分支復驗機制**（KL 點頭 `2026-09-02`·已入 `CLAUDE.md`）
   ```
   CC 備妥生產碼 commit（⛔ 推主線）→ 推至側分支 verify/<單號>-c<n>
   → 發單側自側分支逐位元組復驗 → 發單側請示 KL → KL 放行
   → 主線若已前進 ⇒ rebase → 四項等值判準 → 強制重推側分支 → 發單側重驗 → 推主線
   ```
   **等值四判準**：父 ＝ 現主線頭 ∧ 變更檔清單相同 ∧ `numstat` 逐檔相同 ∧ **各檔 blob 逐檔相同（全 `40` 碼）**；
   併加**方向相反**之見證（未參與本 commit ∧ 參與基座前進之檔，其 blob 須**相異**）。
2. **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 是某一個 SHA。**
3. **KL 之逐字須原封抄入單與正典**，⛔ 轉述。「continue」「好」之類⛔ 得記作放行。
4. **上呈 KL 之格式**：【現況】【要改成】【對土地的影響（面積／街角／抵費地·帶數字）】【要你判斷（單一問題）】，並標「🔴 真需域裁」或「🔵 知會即可」。**⛔ 用程式術語**。
5. **上呈前必先窮舉檢索**（母體 ＝ 全倉 `.md`·同義詞展開），並於單中載明命中數——本波二題皆得「四十餘處無一載答案」方上呈。

---

## §七　攢批（⛔ 為其單獨開批·俟下一張零生產碼單一併掃）

- `GB` 定義列式之**正典化**（`GB-138` 處置③·須比照 `W-G.9-184` 體例，先證所採之框涵蓋既有全部鑄號）。
- 🩸 **CC 側之一則（本波 CC 自捕自報·正辦）**：`W-G.9-211R` 撰報時「`K-6` 基座 blob」初稿誤寫佔位符 `d02…`（憑印象·未量測），提交前自查更正為 `eb3b6ec5557de5573e6223cd34d07c6b443d6c9d`。形同 `自誤 240` 族，**發單側宜於下一批具名登記其一般式**（凡報告中之 blob／SHA，須逐值出自 `git` 之實測輸出，⛔ 得先寫佔位符再回填）。

---

## §八　`CLAUDE.md` 之 ⬜ 項（🛑 每輪結束前須主動向 KL 報告·`CLAUDE.md:571` 所令）

全檔 `⬜` 命中 **`37`** 處。主鏈六項（`:590`-`:596`）：

| # | 項 | 狀態 |
|---|---|---|
| `1` | `K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17` 重排／遞補（**幾何影響批·須另開新窗**） | ⬜ 未開工·前置 ＝ `GB-104`〜`GB-106` |
| `2` | `K-9-12` 矩形容納（＝閘二） | ⬜·與 `1` **同批**（否則假綠不消） |
| `3` | `K-6-A2` 剩餘（`K-9-1`／`3`／`5`／`6` 宗地層 `min_depth`／`K-9-8` 之 `GB-18`／`K-9-5-2` ①⑤） | ⬜ |
| `4` | `K-9-22` 落地（原地號保留 ＋ 區段回退） | ⬜ |
| `5` | `K-6-B` 段一〜段四 | 🔶 **本波已推進**：步驟 0 ✅（`a2c4b2b` ＋ 本波之無條件化）／段一上鎖 ✅（`commit 3`·唯讀）／段二排序 ✅（`commit 1`·唯讀）／**段三、段四仍 ⬜**（`VR-086`） |
| `6` | `K-6 §四` 合併述詞置換 → 波末批 → 泛化波 | ⬜ 末 |
| 併行 | 九份 baseline 重產（**須 KL 決時點**）／`W-G.9-130` 偵察報告入倉 | ⬜·⛔ 入主鏈 |

🛑 **`:549` 之逐字提醒**：進度表之 `K-6-A2ﾠ⬜ 未開工` 一列**已不再為真**（⛔ 刪·留作沿革）。
🛑 **波末批**：換快照 ＋ 重烤 `v3`／`wf`／`E` 系列 baselines ＋ 改遷移項 ＋ **`core.autocrlf` 之設定更正**（`GB-139`）＋ **`_DECLARED_EXTRAS` 內 `指數名次` 之移除**（`VR-089` 肢二之附條件）。

---

## §九　目前掛在 KL 身上者

**⛔ 有。** 本波三 commit 皆已放行落地，二域裁（`K-9-27`／`K-9-28`）皆已答並入正典。
🔴 **下一個要上呈 KL 者 ＝ `§五` 之工項 `4`（`GB-140` 三處精度）**——三種收法之土地後果相異，宜在動碼之前先問。
SELF_SHA256: 6c64f91458b47cbf6377797bc7c59dedc4d76bb6a3324f0dc264c8ab53e9027c
