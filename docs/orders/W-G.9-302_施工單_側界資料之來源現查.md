# `W-G.9-302`　施工單（**中**）：`自誤 406` 之鑄 ／ **側界資料之來源現查**（母體以產生指令界定·含二停機款）

> **態**｜開工 ＝ `eaeb4224b4a6ddbc2a97ab77bd7913a27016630a` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·⛔ 短前綴）
> **單** ＝ `docs/orders/W-G.9-302_施工單_側界資料之來源現查.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（登記 ＋ 現查·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**·**⛔ 修批**）
> 🛑 **本批鑄號 ＝ `自誤 406`**（**一號**·逐字由本單供·**`###` 形**）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔴 **KL 之放行於本批<u>仍不動用</u>**；⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；**⛔ 辦修批**。
> 🔑 **`W-G.9-301` `§六-3` 之 `停四` <u>於其所載之母體下成立</u>，而該母體係發單側預先窄化之產物**（`§一` 裁 `2`）
> ⇒ 本批以**產生指令**重界其母體並重查；**⛔ 以本批之重查為「`停四` 之推翻」**——`停四` 之判於其母體下**仍成立**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　開工閘（十一項·**每列同格載其產生指令**·`自誤 392`·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `eaeb4224b4a6ddbc2a97ab77bd7913a27016630a` **或其後裔**（全 `40` 碼·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**·`自誤 405`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 **逐項具名處置**（本單之預告見 `§零-1`）；判定組為空時**照實具名為「判定組為空」** |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-302` ＝ `True`／[必偽] 含 `W-G.9-301` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`；preflight 四靶逐項成立 |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 自誤 **`395`**／**`405`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `28`／`29`（缺 `[]`） |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`89`／`89`**·末標籤 **`bk`** ⇒ 下一 **`bl`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；判別力[必不命中] 子層 **須 `> 0`**（**逐批漂移**·⛔ 定值）；🔑 **本數即本批一切母體之外部錨**（坑 `bj`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`** |
| `10` | **六物證落檔**（🔒 **框 ＝ blob `sha1`**·`git rev-parse <rev>:<path>`·**先 `cat-file -e` 驗存在**·三態分列·🆕 **含 CJK 路徑者取 bytes**·坑 `bk`） | `WG9295R_joint_v1_selfpolluted.log`／`WG9296R_w0rec_v1_blknone.log`／`WG9297R_w0caliber_v1_blkabsent.log`／`WG9298R_gate3_v1_firstcall.log`／`WG9300R_static_v1_popcollapse.log`／`WG9300R_dynamic_v1_wrongframe.log` ⇒ **逐位未動** |

### `§零-1`　`P-4` 之逐項處置（**發單側已實跑·⛔ 靜默略過**）

本單經發單側實跑 `probe_order_preflight.py`：🔴 **`0`** 項。🟡 之受詞若屬**三類**⇒ **受具名豁免**：
**(甲)** `→` 係**賦值鏈／消費鏈／查表鏈／流程節序**之串接（如 `_st_fc.session_state.get(…)` → `['left'|'right']` → `['p1'|'p2'|'mid']`、`§七` 之「開工閘 → 逐工項 → 收工閘」）；
**(乙)** 方向性之詞係**序之詞而非幾何**（`前綴`／`後裔`／`首`／`末`／`前`／`後`）；
**(丙)** `⇒` 係**推論之連詞**（前提 `⇒` 判），⛔ 空間之指向。
🛑 三類皆**⛔ 幾何向量之方向性轉引** ⇒ **無座標系可具名**。
🛑 **凡實跑所得之 🟡 項不在此三類內者，⛔ 受本豁免，須停、照實具名、⛔ 續**。
🛑 本批凡出艙**幾何向量**之夾角者，一律**釘其槽**（線向／法向）並以**實邊**機驗——該禁⛔ 在本豁免射程內。

🛑 **恆常附款逐項適用·⛔ 省**（坑 `bh`／`bi`／`bj`／`bk`／`bc`／`bd`／`be`／`bf`／`bg`／`g`／`3`、`GB-158` **三形分列**、
簡繁誤字機檢、`rev` 釘死、**先跑再寫**、[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、
payload 之落地形機驗**繫於性質**、函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、
**逐閘出艙執行次數**、**等待綁行程結束**、**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N`**、**讀 blob 取 bytes**、
**量測器之母體⛔ 含其自身輸出**、**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**、
`f_locals` 鍵須**自證存在**並**增第二鍵**、`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退** ⇒ 先 `cat-file -e`·**三態分列**、
**本批方鑄之號⛔ 作其自身之寬字樣對照**、**簿之數一律取器之輸出**、**母體之基數須綁外部錨**、
**含 CJK 路徑之 `git` 輸出取 bytes**、**摘要值一律逐字具名其產生指令**）。
🆕 **一則新附款**（本批所鑄·⛔ 省）：**凡母體之界定一律取<u>產生指令</u>，⛔ 取單內之列舉**；
凡單內以括號給出一組名者，其為**窮舉**抑或**例示**須同格具名（`自誤 406`）。

---

## `§零′`　前批（`W-G.9-301`）之核可（**發單側已辦·⛔ CC 重辦**）

六 `commit`（`d65b2511`／`bbf2ba5e`／`8e3b0d32`／`6a3c87c3`／`fdd3b69c`／`eaeb4224`）經發單側自倉獨立復驗：

| 受詞（指令） | 實測 | 判 |
|---|---|---|
| 工項零之單（**二框並報**·`自誤 405`） | blob 內容 `sha256` ＝ `7d0ff35a163ae9f75d8dcf5b71c5e44c4ad31a37764a3a3c2c43dae81bbda935`（**＝ 發單側所交付之檔逐位相同**）·blob `sha1` ＝ `b27a2cc05161f3d9…` | ✅ |
| 生產碼判法（母體**正面列舉 `34` 檔**·`eaeb4224` 對 `e5c99246`） | **`0`** 行（異動 `8` 檔）；對照組[必非零] ＝ **`2`** | ✅ |
| 逐 `commit` 刪除欄 | **全 `0`** | ✅ |
| `ALPHA_LABELS` 之擴（**射程 ＝ 僅新增一列**） | `git diff` ＝ **`+ALPHA_LABELS += ["bj", "bk"]`** 一列·`1` 增／`0` 刪·**⛔ 動任何既有列** | ✅ |
| 四簿（器之輸出） | 自誤 相異 **`395`**／MAX **`405`**（`393 + 2`）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** | ✅ |
| 自限 | `app.py` `4379108a…`／`baselines` `298`／`p3a` `rc = 1`／`299-gb170` 命中 `0` | ✅ |

⇒ **✅ 全批核可。** 🔑 **本批之現查品質，發單側逐項具名其功**：
① 款 `4` 於同一節**主動併查**受詞函式自身之取值路徑（`_st_fc.session_state.get('f3_cad_side_lines_by_side')` → `['left'|'right']` → `['p1'|'p2'|'mid']`），**該一併查即本批得以重開之全部依據**；
② 款 `4′` 自受詞函式之 **docstring 逐字**取得「⛔ 靠字串標籤」之禁，並以**實參逐字**坐實 `7` 處中 `3` 處之 `_label` 為複合字串；
③ 坑 `bj` 定義處實測 `4`（單載 `2`）**照實具名、⛔ 頂替、⛔ 逕修**——**處置完全正確**，其責在發單側（`§一` 裁 `3`）；
④ 自捕**零則**之出艙附「**⛔ 讀為『本批無誤』**」之具名，係 `[必為零]` 之正確體例。

---

## `§一`　裁（**發單側·⛔ CC 自裁**）

| # | 事 | **裁** |
|---|---|---|
| `1` | `停四` 之判 | ✅ **於其所載之母體下成立·CC 之處置完全正確**。二合取項皆經實測、判別力四造全成立、四禁於停機後仍在、⛔ 擬任何改法——**逐項如令** |
| `2` | 🔴 `停四` 之**母體** | **責 ＝ 發單側·鑄 `自誤 406`**。`W-G.9-301 §六-2` 款 `4` 將 `side_mid` 之可導路徑限於「其體內**已調用**之原語（`_block_strip`／`_strip_s_range`／`_band_area`）」——該括號內三名係**發單側之例示**（取自其自身一次自由名掃描），而單**未具名其為例示抑或窮舉** ⇒ 受單側依文義以之為母體。🔑 而 `W-G.9-301R` 五-4 於同節**已具名**受詞函式自身之取值路徑係 `_st_fc.session_state`，**⛔ 任何幾何原語** ⇒ **該母體恰不含目標函式自己所走之路** |
| `3` | 坑 `bj` 定義處實測 `4`（單載 `2`） | **責 ＝ 發單側**——其係**未實跑定義框**（`D-H`／`D-T` 之聯集）而出艙之數 ⇒ **`自誤 403` 攔法 ② 之重踩**。🛑 **⛔ 鑄新號**（其形在 `403` 射程內）·**⛔ 逕修該框**·⛔ 判受單側紅——已於 `自誤 406` payload 內**併記** |
| `4` | 🔑 **本批之受詞** | **⛔ 判「函式體內自導可施」**——本批只以**產生指令**重界母體並現查**側界資料之來源與其鍵結構**。🛑 **⛔ 以本批之重查為「`停四` 之推翻」**：`停四` 於其母體下**仍成立**且**留倉不改**；本批所重者係**母體**，⛔ 其判 |
| `5` | 🛑 **本批之二停機款** | `停五`（**鍵結構**）／`停六`（**可達性**）——逐字見 `§四-3`。任一成就 ⇒ 停、上呈。🔒 **二者皆繫於<u>性質</u>**（跨街廓錯值之可能／符號之可達），⛔ 繫於代理量 |
| `6` | **路丙之地位** | **受領仍在**（`W-G.9-299 §一` 裁 `3`）；其**落點**已二度受阻（呼叫端／函式體＋幾何原語）。🔒 **發單側之預告（⛔ 現在之裁）**：若本批之第三路（`session_state` 之側界表）亦受阻，**路丙之落點即無現存候選** ⇒ 屆時發單側將重評**路丙本身**是否須退回 KL 重裁取線。🛑 **本批⛔ 據此作成任何裁、⛔ 呈 KL** |
| `7` | `GB-170` | **⛔ 解除、⛔ 收窄、⛔ 提修法主張**；失效條件 `(2)`／`(3)` 仍未成就。本批之登記見 `§三-2` |

---

## `§二`　工項零：**本單原封入倉**（主線）

落點 ＝ `docs/orders/W-G.9-302_施工單_側界資料之來源現查.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`·[必真] 含 `W-G.9-302`／[必偽] 含 `W-G.9-301`）；檔數 ＝ `1` 之自證（造甲[必≥1] ＝ `W-G.9-301` 之單／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；**blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源**（🔒 **二框並報**）；`deletions` ＝ `0`。

---

## `§三`　工項一：`自誤 406` 之鑄 ＋ `GB-170` 之末端追加（主線·**二 payload 分列 `commit`**）

### 三-1　`自誤 406`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

🔑 界 ＝ `<!-- ZW406-BEGIN -->` 與 `<!-- ZW406-END -->` 之間（**不含哨兵列**·哨兵⛔ 入簿）·**⛔ 行號為錨**。
🛑 唯一性之框 ＝ **整列<u>逐位相等</u>**（`^…$`）·母體 ＝ 本單單檔·各須命中 **`1`**；併報**子字串框**之數（本單行文另引該二字串各一次 ⇒ 必得 `2`·**二數並報**）。payload **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**。

<!-- ZW406-BEGIN -->

### 🩸 `自誤 406`　**停機款之受詞母體由發單側<u>預先窄化</u>，而該窄化未經現查**（`自誤 404` 之重踩·同根第三度）

**形**：`W-G.9-301 §六-2` 款 `4` 逐字將 `side_mid` 之可導路徑限於「以其**現有形參**為起點，現查其體內**已調用**之原語
（`_block_strip`／`_strip_s_range`／`_band_area`）」，而 `§六-3` 之 `停四` 合取項 `(ii)` 即以此為母體
（「亦⛔ 可自其現有形參經**既有原語**導出（款 `4`）」）。該括號內之三名係**發單側之例示**
（取自其自身一次自由名掃描），而單**未具名其為例示抑或窮舉** ⇒ 受單側依單之文義以之為**母體**——**其讀法正確**。

**徵候（`W-G.9-301R` 五-4·同一節·同一器）**：受詞函式自身之取值路徑已被具名為
`_st_fc.session_state.get` → `'f3_cad_side_lines_by_side'` → `['left'|'right']` → `['p1'|'p2'|'mid']`，
**⛔ 任何幾何原語**。⇒ `停四` 所判「不可導」之母體，**恰不含目標函式自己所走之那條路**。

**補測（發單側·同批·⛔ 充結論·其可施與否方為受詞）**：字樣 `f3_cad_side_lines_by_side` 於生產碼 `34` 檔母體內
命中 **`19`** 處／**`6`** 檔（`app.py` `13`／`verify/run_verification.py` `2`／`verify/stepg_pipeline.py` `1`／
`verify/wg_g1_smoke.py` `1`／`verify/wg_g2_smoke.py` `1`／`verify/wg_g3.py` `1`）；`_st_fc` 命中 **`2`** 處／**`1`** 檔。

**根因**：停機款之 `(ii)` 係一**全稱否定**（「亦⛔ 可自現有形參導出」），而其母體**由單內之列舉界定**、
⛔ 由**產生指令**界定 ⇒ 該否定之射程被單之作者**靜默決定**，受單側無從察其窄。

**族** ＝ `自誤 404`（未先現查接觸面即釘死改之位置）之**重踩**（第二度）＋ `自誤 388`（母體未窮舉）。
🔒 同一根之**第三度**：`404` 釘死**位置**、本則窄化**母體**——二者皆係「發單側以未經量測之界劃限縮受單側之受詞」。

**併記（⛔ 鑄新號·其形在 `自誤 403` 射程內）**：同單 `§四` 逐字預告二坑之定義處「**各為 `2`**」，
係發單側**未實跑**其定義框（`D-H`／`D-T` 之聯集）而出艙之數；`W-G.9-301R` 三-2 實測 `bj` ＝ **`4`**／`bk` ＝ **`1`**。
⇒ `自誤 403` 攔法 ②（**⛔ 出艙任何未對該物實際執行之機檢數**）之**重踩**。受單側依單明令照實具名、⛔ 判紅、⛔ 逕修——**處置正確**。

**攔法（三款）**：
① 凡單內以括號給出一組名（「（`A`／`B`／`C`）」）者，須同格具名其為**窮舉**抑或**例示**，並給出其**產生指令**；**⛔ 以例示充母體**。
② 🔑 **停機款之受詞母體一律以<u>產生指令</u>界定**（如「`ns` 之全部鍵·其基數由 `<指令>` 得」），**⛔ 以單內之列舉界定**；
凡停機款含**全稱否定**者，其母體之**基數**須與判別力造**同格出艙**。
③ 凡欲判「`X` **不可導**」者，須**先現查<u>既有生產碼中 `X` 實際如何被取得</u>**（該路徑必存在，否則 `X` 不存在於生產態），
並以該路徑為母體之**必含項**；⛔ 以「某一類手段不可導」充「不可導」。

<!-- ZW406-END -->

**落地後之機驗（繫於性質）**：以**器本身**（`registry`）量 ⇒ 自誤 相異 ＝ **`396`**（`395 + 1`）／MAX ＝ **`406`**；`406` 命中 **`1`**（⛔ `> 1`）；
判別力[必命中] ＝ `399` 須 `True`／[必為零] ＝ **執行期組出**之人造號須 `False`；哨兵 `ZW406` 入簿命中 ＝ **`0`**；
append-only 二造 ＋ `wg942_append_audit.py`（先設 `PYTHONIOENCODING=utf-8`）`rc = 0`。🛑 任一不成立 ⇒ 判器紅、停、上呈。

### 三-2　`GB-170` 之末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**·⛔ 上文一字不刪）

🔑 本 payload **⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**（`自誤 402` 攔法 ④）。
界 ＝ `<!-- GB302-BEGIN -->` 與 `<!-- GB302-END -->` 之間。🔒 **`GB-132` 之避讓**：凡**以 `#` 起首**之列，其列內**⛔ 含任何 `GB-` 之號**。

<!-- GB302-BEGIN -->

---

## 🔧 路丙落點之**二度受阻** ＋ 側界資料來源之待查（`W-G.9-301`／`-302`·⛔ 解除、⛔ 收窄·純末端追加）

🛑 **本節⛔ 鑄任何號**（`戒 36`）；⛔ 改任何原節一字、**⛔ 解除、⛔ 收窄任何阻塞項**。

> **`W-G.9-301R` 之實測（`停四` 成就）**：`_corner_buffer_S` 之簽章逐字 ＝
> `def _corner_buffer_S(block_poly, d_hat, front_p1, allocation_dir, range_area, side, tol=0.01, _label='')`（體 `104` 列·定義處 `1`／`1`）；
> `_first_corner_alloc_dir` 之簽章 ＝ `def _first_corner_alloc_dir(side_mid)`（體 `36` 列·定義處 `1`／`1`）。
> 所需輸入集 `R` ＝ `['side_mid', 'np']`；🔴 **`side_mid` 既⛔ 在八個現有形參內、亦⛔ 在 `ns` 之鍵框內**（`ns` 鍵基數 ＝ `286`）。
> 其體內已調用而屬 `ns` 者 ＝ `_strip_s_range`／`_block_strip`，二者之回傳結構 ＝ `(min(ts), max(ts))`／`None`
> 與 `(None, 0.0)`／`(cut, area)` ⇒ **側界資料⛔ 在其回傳結構中**。⇒ `停四` 之二合取項皆成立。
> **第二項獨立之由**：`_first_corner_alloc_dir` 之 docstring **逐字**令「以 `side_mid` 幾何反查 SIDE_LINE——⛔ 不靠任何字串標籤」，
> 其案由逐字：「`_corner_first_lot_G` 之 `_label` 實為複合字串 `'R4·628(1)·左'`、**非街廓標籤**；以其查表必 `None` ⇒ 靜默退回」；
> 而實參實測——`7` 處中 **`3`** 處之 `_label` 為複合字串（`f'{lbl}·F.1'`／`f'{blk}·E3'`·`wf_f1`／`wf_f4`）。
> **非 `forced` 之呼叫端 ＝ `0`／`7`**（本批**獨立重得**·判別力[必為否] ＝ 定義列之外圍 `If`／`IfExp` ＝ `0` ⇒ 判否）。
> 🔴 **`停四` 之母體係發單側預先窄化之產物**（`自誤 406`）——受詞函式自身之取值路徑
> `_st_fc.session_state.get('f3_cad_side_lines_by_side')` → `['left'|'right']` → `['p1'|'p2'|'mid']` **不在該母體內**。
> ⇒ `W-G.9-302` 以**產生指令**重界母體並現查**側界資料之來源與其鍵結構**；🛑 `停四` 之判於其母體下**仍成立**且**留倉不改**。
> **路丙之落點已二度受阻**（① 呼叫端·`停二`／② 函式體＋幾何原語·`停四`）；其**取線之設計⛔ 變**（受領仍在）。
> 🛑 **KL 之放行仍未動用**；`verify/W-G.9-299-gb170` **⛔ 開**；生產碼 `34` 檔**一字未改**；
> **失效條件 `(2)`／`(3)` 仍未成就**——⛔ 解除、⛔ 收窄、⛔ 提修法主張。

<!-- GB302-END -->

**落地後之反向機驗**（`自誤 402` 攔法 ④·**三形分列並報**·`GB-158`·框**自器碼當場取**）：
聯集 MAX **須仍 `170`**、相異 **須仍 `168`**、缺號 **須仍 `[12, 87]`**（⛔ 鑄號之**性質**·**⛔ 以「未被框命中」充證**）；
判別力[必命中] ＝ `GB-170` 之既有定義列於三形至少一形須 `True`／[必為零] ＝ **執行期組出**之人造號於三形皆須 `False`；
二形以上皆 `0` 須 **loud 具名**。併驗 `GB-132` 避讓：本 payload 內 `^#` 起首且含 `GB-` 者 ＝ **`0`**。

---

## `§四`　工項二：**側界資料之來源現查**（主線·**零生產碼**·🛑 **含二停機款**）

### 四-1　本項之由（**發單側具名·⛔ CC 推定**）

`停四` 已證「經**幾何原語**不可導」。本項**⛔ 重問該事**，只問一個**新**受詞：
**生產態中 `side_mid`（側界中點）究竟自何處取得、其表以何為鍵、於 `_corner_buffer_S` 之體內是否可達且可正確取本街廓者。**
🛑 **⛔ 判「可施」或「不可施」**——只出艙。⛔ 擬任何改法或 diff。

### 四-2　逐款出艙（**母體一律以<u>產生指令</u>界定**·`自誤 406` 攔法 ②）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | 🔑 **側界符號之全量現查**：母體 ＝ **`ns` 之全部鍵**（＝ `app.py` 之 `t.body` 頂層名·**基數由指令得**·`W-G.9-301R` 實測 `286`·🛑 **本批自行重得**）。框 ＝ 各鍵若為 `FunctionDef`，其**體內**是否含字樣 `f3_cad_side_lines_by_side`／`side_line`／`SIDE_LINE`／`side_mid`（**四字樣分列並報**·⛔ 合併） | **全量列舉**其命中之鍵名（⛔ 只報基數）；🆕 **母體基數綁外部錨**（坑 `bj`）；判別力[必為零] ＝ 一**執行期組出**之人造字樣於全母體命中 `0`（字面⛔ 出艙） |
| `2` | 🔑 **`f3_cad_side_lines_by_side` 之全部<u>寫入處</u>與<u>讀取處</u>**：母體 ＝ 生產碼 **`34`** 檔（**正面列舉**）；框 ＝ **AST**（`Subscript` 之 `slice` 為該常數者、`Assign` 之目標含之者、`.get(<該常數>)` 之 `Call`·**三形分列並報**）＋ **文字層列框**以資對照 | **二數並報**·相異即 **loud 具名其成因**；逐處出艙 `檔` ＋ **所在函式名** ＋ **字樣錨**（⛔ 行號為錨）＋ 其**寫入／讀取**之別。🔒 發單側之**補測（⛔ 充母體·⛔ 充結論）**：文字層命中 `19` 處／`6` 檔 |
| `3` | 🔑 **其表之<u>鍵結構</u>逐層**：自寫入處之 AST 逐層取其鍵（**字串常數逐字**）——出艙其**層數**與**各層之鍵集**；🛑 **明確回答一問**：其鍵**是否含街廓層**（`blk_label` 或等價） | ⛔ 以「讀來像是」判——一律取 **AST 之 `Subscript`／`Assign` 鏈逐字**；凡鍵為**變數**而非常數者，出艙該變數名並**溯其賦值**（同函式內·`t.body` 或函式體之 `Store`） |
| `4` | 🔑 **寫入之<u>時機</u>**：各寫入處是否落於**逐街廓之 `for` 迴圈內**（框 ＝ AST 外圍 `For`／`While` 之 `target` 與 `iter` 逐字）；若在迴圈內 ⇒ 其**每次迭代是否覆寫**（同鍵重賦） | 🔑 **此款即 `停五` 之受詞**；⛔ 以「未在迴圈內」充「全案唯一」——須出艙其外圍結構逐字 |
| `5` | 🔑 **`_st_fc` 之可達性**：`_st_fc` 是否在 `ns` 之鍵框內；其於 `_corner_buffer_S` 之體內**是否可解**（框 ＝ `app.py` 頂層名之集·⛔ 模組 `__globals__`·`W-G.9-298R` 已證 `ns` 係純 `dict`）；併出艙 `_st_fc` 之**賦值逐字**（其為何物） | 🔑 **此款即 `停六` 之受詞**；三態分列（**在 `ns`**／**⛔ 在 `ns` 而可自他途**／**⛔ 可達**）；⛔ 以「`app.py` 內有此名」充「`_corner_buffer_S` 內可解」 |
| `6` | **`_first_corner_alloc_dir` 之接觸面**（供後續之裁·本批**只量**）：其**定義處**與**全部呼叫端**（AST·**二形並取**·母體 ＝ `34` 檔正面列舉）＋ 逐處之**外圍守衛** `test` 逐字 | **檔／處二數並報**；判別力[必為否] ＝ 其定義列之外圍 `If`／`IfExp` ＝ `0` ⇒ 判否 |
| `7` | **判別力四造** | [必非零] 款 `2` 之處數 `≥ 1`／[必為 `34`] 母體基數之外部錨（坑 `bj`）／[必為零] **執行期組出**之人造字樣於款 `1` 之框下命中 `0`／[必真] `_first_corner_alloc_dir` 在 `ns` 之鍵 ＝ `True` |

### 四-3　🛑 **二停機款**（任一成就 ⇒ **判器紅、停、上呈**·⛔ 擬任何改法）

> **停五（鍵結構·繫於性質）**：款 `3` 之鍵結構**⛔ 含街廓層**，**且**款 `4` 實測其寫入**不隨街廓更新**
> （即多街廓共用一份而未逐街廓覆寫）⇒ 於 `_corner_buffer_S` 內以 `side` 單鍵取之，**於多街廓下必取他街廓之側界**
> ——與 `W-G.9-300R` 所坐實之 `side_mid` **在域性不足同形**（靜默錯值·⛔ 型別可擋）⇒ **停、上呈**。
> 🔒 **二合取項須分列出艙**；任一為偽則本款**未成就**，仍須**照實併記**其另一項之值。
>
> **停六（可達性·繫於性質）**：款 `5` 判 `_st_fc` 於 `_corner_buffer_S` 之體內**⛔ 可達** ⇒ 該路徑**不可施** ⇒ **停、上呈**。

🛑 **四禁於停機後仍在**（`W-G.9-301 §六-3` 逐字·⛔ 以「為使其可施」為由逾之）：
**⛔ 新增形參、⛔ 改簽章、⛔ 新增 `import`、⛔ 自寫第二套幾何**。
🛑 **本工項⛔ 改生產碼一字、⛔ 開分支、⛔ 動用 KL 之放行、⛔ 驅動 `run_verification`、⛔ 擬任何改法或 diff。**

---

## `§五`　🛑 **⛔ 為之事**

⛔ 改生產碼 `34` 檔**一字**；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 驅動 `run_verification` 或任何長跑；⛔ 辦修批；
⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；**⛔ 引任何自 baseline 取得之數**；
⛔ 改任何函式簽章、⛔ 新增任何形參、⛔ 新增任何 `import` 於生產碼、⛔ 改任何 `*_EXPECT` 一字；
⛔ 自寫第二套幾何或第二份 `W`／`side_mid`／側界線之定義（`GB-48` 族）；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 動 `probe_WG9269_pit_index.py`／`probe_WG9292_pathC.py`／`probe_WG9295_joint.py`／`probe_WG9298_gate3.py`／`probe_WG9300_contact_surface.py`／`probe_WG9301_selfderive.py`／任何**既有**探針一字（准於 `verify/probes/` **新增**量測器）；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號（**`自誤 406` 為本批唯一之鑄**）；
⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 逕修 `side_mid` 之在域性（⛔ 移賦值列）；⛔ 逕修坑之定義框；
⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改 `W-G.9-299`／`-300`／`-301`／補令一／交接註／`W-G.9-300R`／`W-G.9-301R` 或任何已入倉文件一字；
⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文；
⛔ 判 `GB-170` 可否解除、⛔ 判路丙之落點可施與否、⛔ 判 `停四` 之判為誤（其於其母體下**仍成立**）、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張；
⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制；
⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述）。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·含 CJK 路徑者取 bytes·坑 `bk`） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末（**器之輸出**） | 自誤 相異 **`396`**／MAX **`406`**（＋**一**號）；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末（**器之輸出**） | **`89`／`89`**·末標籤 **`bk`** ⇒ 下一 **`bl`**（⛔ 鑄坑）；`ALPHA_LABELS` 之 `git diff --numstat` 輸出**空**；母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 檔數（**自洽檢**） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 本批確有異動之檔）；`app.py` ＝ `4379108a…`（**框 ＝ blob `sha1`**）；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**六物證落檔**之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列·**框逐字具名**） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（[必為零]）；判別力[必存在] `p3a` 命中 **`1`** |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§七`　報告之格

`§零` 開工閘（**每列同格載其產生指令**）→ `§零′` 之⛔ 重辦 → 逐工項（逐格表以落檔**全量**出艙·⛔ `\| tail -N`）→
`§四-3` 二停機款之逐款判（**合取項分列**）→ 各判別力造之判 → 一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→
`⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；**母體一律以產生指令界定**（`自誤 406`）；**摘要值一律逐字具名其產生指令**（`自誤 405`）；
框無 `^` 錨者**二數並報**；**帶號與絕對值並列**；**母體之基數綁外部錨**（坑 `bj`）。
🔒 判定組為空一律 **loud 拒測**並**照實具名**；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔒 凡自捕一律於出艙前攔下、照實併記（**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）；物證落檔**保全⛔ 覆寫**。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`deletions`／`cat-file`／
`merge-base`／`RE_ZW_DEF`／`append_audit`／`ZW406`／`GB302-BEGIN`／`ALPHA_LABELS`／
`f3_cad_side_lines_by_side`／`_st_fc`／`side_mid`／`_corner_buffer_S`／`_first_corner_alloc_dir`／
`blk_label`／`GB-132`／`GB-170`／`baselines`／`numstat`／`sha256`／`ls-tree`／`WV_BAKE`。

【單止】本單受詞 ＝ CC 窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: 754974cddc608cdf48d37982ad7be2aea38e9c3d3381cd9939bfab369d10b03d
