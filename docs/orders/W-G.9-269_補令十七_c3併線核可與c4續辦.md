# W-G.9-269　補令十七（`c3` 併線之核可 ／ 坑 `ad`〜`ak` 之核可 ／ `自捕 17` 之立準 ／ `c4` 續辦）

> **受詞** ＝ `W-G.9-269` 之 **`c4` 專窗**。**主線** ＝ `8f4281c8933a18c33ae40c0b4b975304ed3c6402`（須 `git ls-remote` 實查）｜**凍存** `verify/W-G.9-269-p3a` ＝ `05a11bd`（未動）。
> 🔒 `W-G.9-269` 施工單、補令一〜十六、本補令**並存**；相斥處以**時序最後者**為準。
> 🔒 **本補令為 `c4` 窗之工項零**；`c4` 之**完整規格** ＝ **補令十六 `§三`（`(a)`〜`(c)`）＋ `§五` 序 `3`〜`5`**，自倉逐字讀。

---

## §一　`c3` 併線之核可（發單側自倉復驗）

| 項 | 實測 |
|---|---|
| `c3` 生產碼 commit `17fd981` | **為主線祖先**（**原物件**·⛔ rebase 重造）✅ |
| `app.py` 於主線 blob | `4379108a48567140…` ＝ `17fd981` 之值·**逐位相同** ✅ |
| `ce06622` 之後主線生產碼 | 相異 **`0／34`** ✅（其後 `deletions 4` 落於**探針**，⛔ 生產碼）|
| `verify/baselines` 樹 | `241b2a2f98881148…`·自本波基座**逐批未動** ✅（`GB-162` 守住）|
| 四簿（框 ＝ 標題形 ∪ 表列形） | 自誤 `353`／`363`／`[106,355,356,357]`｜`GB` `161`／`163`／`[12,87]`｜`VR` `80`／`95`｜`K-9` `28`／`29` —— **期初＝期末** ✅ |

🔒 **⇒ `W-G.9-269` 之生產碼工項<u>全畢</u>**（`c1`／`c2`／`c3` 三筆皆經 KL 逐筆放行、皆以原物件併入）。餘 `c4`（零生產碼）與結案報告。

### 一-1　`自捕 17` 之核可 ＋ 發單側之框自陳

🔒 **CC 之自我更正核可**：其 `Q-1` 母體 `22`（`20` 個 `got_*`）含 `got_G值_退縮{0m,3.5m}_partial.csv` 二陳舊檔（`mtime 09-10 18:50`·早於三跑二日）⇒ 虛增；扣除後 `20`（`18` 個 `got_*`），**與發單側之框逐數相符**。
🔒 **發單側之框自陳（供對齊·⛔ 判孰誤）**：發單側三跑**跑前皆 `rm -f verify/out/got_*`**，其落檔清單逐檔具名，**⛔ 含任何 `_partial`** ⇒ `18` 成立。
🔑 **CC 之觀察尤須立為準**：該二陳舊檔**因未被觸及而三態逐位相同**——係「**保證同分之填充**」、⛔ 帶資訊；**且若其碰巧相異，反將產出一次<u>假紅</u>**。

**立準（承 `自捕 17`·CC 自立·發單側核可）**
> **凡以「目錄內全部 `X*`」充某次執行之產物者，須先證<u>每一檔確為該次所寫</u>——`mtime` 晚於起跑，或**跑前清空該目錄**；⛔ 以「它在那個目錄裡」代之。**
> **併記：陳舊檔入母體者，其「相同」⛔ 帶資訊，其「相異」則為<u>假紅</u>——二向皆污染。**

### 一-2　同段自捕之核可

索引器擴至 `ak` 後**結語仍宣告舊範圍 `aa`〜`ac`**（`坑 25` 之族·**結語虛報覆蓋**）⇒ CC 同段更正三處並**重跑**，使入倉之落檔與其自身宣告一致——**核可**。

---

## §二　坑 `ad`〜`ak` 之核可（`常規三`）

🔒 **CC 依 `常規三` 提案坑標籤並具名其由、⛔ 自定、候發單側核可——處置正確。**
🟢 **核可**其八則（`ad` `environ.pop` 取態即取預設／`ae` 沿用他批活體檢須先證其選擇器／`af` 計數 `0` 之二讀法 ⇒ 二款並取／`ag` 批次變換器之寫檔置於全部變換之後／`ah` 註解錨多站相同 ⇒ 改 `expect=N` 遞減／`ai` 形近 CJK 致 `.get` 靜默 `None`／`aj` 「目錄內全部 `X*`」⛔ 等同該次執行之產物／`ak` 其餘自捕八則）。
🔒 **自證核可**：`63／63` 皆可解析、`0` 不可解析；二造對照 造甲 坑 `n` ＝ `5`（`≥1`）⋀ 造乙 人造哨兵 `0`／`0` ⇒ **器非紅**。
🛑 **⛔ 追改**已入倉之坑定義一字；爾後新坑續 `al` 起，**每次收工須附本波所立之坑之定義處清單並以二造自證**（補令九 `§二-4`）。

---

## §三　`c4` 之續辦（規格見補令十六 `§三`·此處⛔ 重述）

🛑 **`c4` 零生產碼·得逕推主線**；⛔ 動生產碼 `34` 檔一字。
🛑 **四阻塞項之現況與解除條件須逐則載於 `c4` 報告**：`GB-159`（俟三條件）／`GB-160`（俟閘 `γ` 重量·仍不過⇒ 成因⛔ 在退化幾何·須另診具名）／`GB-162`（俟 `1137` 格之歸因）／`GB-163`（俟 `P-3` 判定集補全）。
🛑 **`GB-159` 之解除⛔ CC 自裁、⛔ 發單側自裁**——三條件備齊後**呈發單側**，由發單側**呈 KL**。
🔒 **`GB-159` 條件 `(1)` 之結清錨 ＝ `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d`**（`c2` 之併線）。

---

## §四　鑄號之授權

🔒 **本補令⛔ 授任何鑄號。** 若 `c4` 查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。
🛑 意指占用查**二形並取**；四簿之數須具名其框（**標題形 ∪ 表列形**）。

---

## §五　續辦

| 序 | 事 | 落點 |
|---|---|---|
| `1` | 工項零（本補令原封入倉） | 主線（零生產碼·得逕推） |
| `2` | **`c4`**（補令十六 `§三` 之 `(a)`〜`(c)`） | 主線 |
| `3` | `W-G.9-269` **結案報告**（全批條件逐條 ＋ 結案 commit ＋ 四簿 ＋ 未結阻塞項四則之現況與解除條件） | 主線 |
| `4` | 收工交接（交接註 ＋ 坑之定義處清單自證 ＋ 起手清單） | — |

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
