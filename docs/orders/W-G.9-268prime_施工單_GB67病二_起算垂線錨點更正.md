# W-G.9-268′　`GB-67` 病二：**起算垂線錨點更正**（前一宗之起點 → **終點** ＝ 藍影外邊界 ＝ `S_req`）（**重級批**·🔴 **有土地後果**）

> **期初態** ＝ `95882de570fe840cf5ae8069ef6237eb5e83991c`（`origin/wip/s1-endpart`·須 `ls-remote` 實查）
> **本單係 `W-G.9-268` 之改寫版**。`W-G.9-268` 已於 `c0` 依其 `§八` 停機上呈（`A ∩ B = ∅`），**該判成立**，其單與附件甲與 `-268R` **原文一字不刪**，本單為其唯一之改寫載體。
> **KL 之裁**：「`GB-67` **開**」（`2026-09-10`）＋「**是**」（`2026-09-10`·同意先修病二、病一延至 `W-G.9-269`）。
> 🛑 **方法⛔ 新裁**。本批之方法係 `GB-67` **自身成因鏈**所載之既有裁定（見 `§一`）。

---

## §零　受詞、由來與界

### 零-1　受詞（逐字·⛔ 自擬）

**起算垂線之錨，由「前一宗之<u>起點</u>」改為「前一宗之<u>終點</u>」（＝藍影外邊界 ＝ `S_req`）。**

### 零-2　既有裁定之逐字錨（`grep -F` 固定字串·⛔ 行號·⛔ 憑本單轉述）

| # | 內容錨 | 載何 |
|---|---|---|
| `1` | `` 🔒 **⇒ `②-宗` 破量之近因已坐實：起算垂線之錨取在前一宗之<u>起點</u>而非<u>終點</u>。** `` | `docs/reports/W-G.4_泛用阻塞項登記表.md`（`GB-67` 之 `W-G.9-85` 加註 一） |
| `2` | `` 將起算垂線之錨由「前一宗之**起點**」改為「前一宗之**終點**」（＝藍影外邊界 ＝ `S_req`） `` | 同上 |
| `3` | `` 🔴 **現行實作將錨取在第 0 宗之<u>起點</u>** `` | 同檔（`GB-67` 之 `W-G.9-87` 加註 二·成因鏈） |
| `4` | `` - **A-4** **錨點更正**：起算垂線之錨由「前一宗之**起點**」改為「前一宗之**終點**」＋ 弦區間閉式重跑。 `` | `verify/probes/probe_WG985_grouphead.py` docstring |
| `5` | `` 🔒 **⇒ 其剩餘作用 ＝ 僅保證「地界線不交叉」** `` 及 `` `K-9-14` 之裁**維持** `` | `GB-67` 之 `K-9-15` 加註 |

🔒 **既有之可證偽預測（`W-G.9-85` 一·逐字·本批之驗收靶）**
> 弦區間閉式重跑（**新錨**）⇒ **重疊 ＝ 0·5／5**；**負對照（原錨）**⇒ 重疊 > 0 者 **3／5**；
> 🔒 有效母體 **3／3**（`R2左j0`／`R3右j7`／`R5左j0`），另二格現況本即 `0` ⇒ 判別力 `0`。

🔒 **與 `-268` `c0` 之實測對位（發單側已獨立復現）**：現行之三個重疊對，其二端**恆為 第0宗 × 第2／第3宗**（`0m R2左`：`628-41(1)`×`628-27(1)` `19.8142`、`628-41(1)`×`628-40(1)+` `26.1624`；`3.5m R5左`：`628-45(1)+`×`628-18(2)` `56.8186`）——**組首即 `W-G.9-85` 所指之 `j0`**。

### 零-3　生產碼受詞（二檔並列·⛔ 只一檔）

`app.py` ＋ `verify/stepg_pipeline.py`。
🔒 **由**：配地鏈於二檔各有一份（碼內自載「站 `1／4`（app 左鏈）」／「站 `3／4`（harness 左鏈）」），而覆蓋帳所讀之 `verify/out/got_G值_退縮*_partial.csv` 由 `verify/run_verification.py` 自 **stepg 管線**寫出。⇒ 只改 `app.py` 則覆蓋帳結構上不可能歸零。
🩸 **⛔ 預設二檔同源**：`c0` 須現查該錨在二檔是否為同一段碼（發單側已測得二檔於其鄰近段**確有分歧**：`stepg` 另有 `_W0_left`／`_Wfirst_left` 之取值，`app.py` 無）。

**⛔ 動之檔**：`verify/run_all.py`／`verify/run_verification.py` 一字不動；其餘 `32` 檔生產碼一字不動。

### 零-4　🛑 ⛔ 併入本批（各為其後之獨立批）

`K-9-23` 閘一轉主動 ＋ `K-9-17` 遞補迴圈（＝ **病一** ⇒ `W-G.9-269`）／`K-9-12` 矩形容納轉主動／五級／`K-9-10`／`K-9-1`／`GB-105` 母體同步之獨立批／`GB-106`／`n_hat_far` harness 側補齊。

---

## §一　開工閘（逐條當場復現·任一不符 ⇒ 🛑 停機上呈·⛔ 續辦）

| # | 閘 | 判準 |
|---|---|---|
| `S-0` | 本單原封入倉 `docs/orders/` | 入倉 blob `sha256` ＝ 落檔前來源 |
| `S-0b` | `probe_order_preflight.py <本單>` | `P-3`／`P-5` 停機款；其餘逐項具名採納或豁免 |
| `S-1` | 號占用（宣告框六數分列 ＋ 二對照） | 受詢 ＝ **`W-G.9-268′`**（須六數全 `0`）。🛑 **同批須併出艙 `W-G.9-268` 之占用態**（發單側實測：嚴格式 `D1 3／D2 6／D3 4／列框 10／檔框 3`）以證二號**可辨**；若二者於所用之框下不可辨 ⇒ **停機上呈**。對照乙（執行期組出之人造號·⛔ 入倉）須 `0`。母體取用須 `-z`（`quotePath` CJK 陷阱） |
| `S-2` | `left-right` ＝ `0　0`；`ls-remote` 與期初態逐位相符 | ⛔ 採信本地 ref |
| `S-3` | 🔴 **生產碼 `34` 檔 blob 期初** | **當場自倉逐檔現查並出艙全表**。🛑 **⛔ 以 `W-G.9-268 附件甲` 之表為比較端**——該表已查實有一筆抄錄誤（`fixture_yi_construction` 載 `69f4a18e…`，實為 `69f4a18a…`）。本閘之比較端 ＝ **`git rev-parse HEAD:<檔>` 之當場輸出**，本表即期末對拍之錨 |
| `S-4` | `run_verification` 期初實跑 | 記其 `RESULT`／`stderr` 位元組／紅綠**名目集合**（⛔ 只記數）。發單側於 `e57d2ef` 實測 ＝ `FAIL`／`0` B／紅 `24` 綠 `10`；生產碼未動 ⇒ 期初應同，**不同即停機上呈** |
| `S-5` | `run_all` 期初對帳態 | 於**拋棄式工作樹**內跑（`run_all` 改寫已追蹤 log）。記 `wv_reconcile` 之類 I／II／III 三數與名目差集之**期初值**。🛑 **⛔ 以「與凍存相同」為通過條件**（期初即 `對帳 FAIL`）——本閘只**取值** |
| `S-6` | 覆蓋帳期初 | 出艙**逐組逐對**（⛔ 只出組數）：期初 `13` 組·違反 `2`（`0m·R2·left = 45.9766`／`3.5m·R5·left = 56.8186`）·三造否證對照如期（相疊 `1.0`／相離 `0.0`／注入 `+1㎡` 於原綠之組須轉紅）|
| `S-7` | 硬閘活體生效 | `--selftest` **⋀** 活體攔截實測，須出艙命中偏移並自證其讀到真命令 |
| `S-8` | 🔴 **harness 可達性期初** | 出艙二 CSV 之 bytes／資料列／逐街廓宗數／可達街廓序。發單側於 `e57d2ef` 實測：`0m` `15979` B／`21` 列／`R1:7 R2:14`；`3.5m` `33670` B／`47` 列／`R1:8 R2:16 R4:5 R6:10 R5:8`；`R3` 二情境皆不可達。**此為 `§三 P-8` 之比較端** |

---

## §二　工項（逐 `commit`·每 `commit` 一件事·皆可獨立回退）

### `c0`（零生產碼·得逕推）　錨之**定位**（純診斷）

🛑 **⛔ 動生產碼一字。⛔ 憑本單或任何報告指認碼位；一律機械現查。**

**c0-1　錨之生產碼落點**
以機械方法定位「前一宗之錨」在生產碼中之**真實計算點**，逐點出艙其字樣錨（⛔ 行號）、所在檔、所在函式、其值之來源欄。
🔒 **已知之線索（⛔ 結論·須自證）**：`_W_prev_*` 之 thread（`app.py` 之 `_W_prev_left = float(res.get('W_far', _W_prev_left))`／`stepg` 之同名式）與 `W_far`／`W_near`／`W` 三欄。
🛑 **若定位所得之碼位其語意⛔ 為「錨取在前一宗之起點」**（即與 `零-2` 錨 `3` 之逐字不符）⇒ **停機上呈**，⛔ 自行改寫受詞。

**c0-2　二檔同源否**
出艙 `app.py` 與 `verify/stepg_pipeline.py` 於該錨之鄰近段之**逐行對照**，判其為同源複製或已分歧，並具名分歧之逐項。

**c0-3　既有 shim 之期初復現**
實跑 `verify/probes/probe_WG985_grouphead.py`（其 `A-4` 即本批之受詞·唯讀·⛔ 改一字），出艙其**新錨／原錨二態之重疊**，與 `零-2` 之既有預測（新錨 `0`·5／5；原錨 > 0 者 `3／5`；有效母體 `3／3`）逐位對拍。
🛑 **若二態同值** ⇒ 判別力 `0` ⇒ **停機上呈**（該 shim 已不能表達本批之受詞，須另定器）。
🛑 **若原錨態之重疊⛔ 復現 `45.9766`／`56.3293`** ⇒ 停機上呈。

**c0-4　受詞之界**
出艙本批**⛔ 觸及**者之具名清單：`第 0 宗（街角地）之 G 與其分配範圍`／`街角評比之 winner`／`藍影面積`／`K-9-23` 二閘之判定式／`K-9-17` 遞補。此四者於本批**逐位不變**，係 `§三 P-5`／`P-6` 之受詞。

### `c1`（🔴 生產碼·須 KL 放行）　錨點更正 ＋ 旗標（預設 `off`）⇒ 行為逐位不變

於 `c0-1` 所定位之**全部**碼位（二檔）加入新錨，置於具名旗標之後（預設 `off`）。
🔒 **語意逐字沿用 `probe_WG985_grouphead.py` `A-4` 之聲明**：本更正**只改「起算垂線之錨點」（前一宗之起點 → 終點）與沿 `d̂` 平移一條界線之位置**；**⛔ 改面積目標**、**⛔ 解 `S`**（⛔ 呼叫 `solve_G_binary`）。
**⛔ 動**：`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 一字。
🔒 **`expand`（並存）段**——⛔ 刪任何既有碼路徑。

**驗收（性質）**：旗標 `off` 下全部輸出與父 `commit` **逐位相同**（`stdout` `diff` 空 ∧ 四 CSV 逐位同 ∧ `S-8` 之可達性表逐位同）。
**判別力**：同一 `commit` 內以旗標 `on` 跑一次，輸出**須相異**（證非死碼）。

### `c2`（🔴 生產碼·幾何變動·**有土地後果**·須 KL 單獨放行）　旗標 `on`

出艙 `§三` 全部性質閘 ＋ `§四` 差異表。
🛑 **本 `commit` 預期使管線通過原中止點**（`0m·R2`／`3.5m·R5`）⇒ 後續世代將**首次可達**，並因此**新曝既有之紅**。其歸類見 `§三 P-9`。

### `c3`（🔴 生產碼·須 KL 放行）　`contract` 段

移除旗標；更新受該錨之註解（含 `零-2` 錨 `3` 所述之「現行實作」一語，改為現況）。
🛑 **⛔ 追改 `GB-67` 原文與其歷次加註一字**（`常規四（九）五`）——現況之更正一律以**末端追加**承載，且落 `c4`。

### `c4`（零生產碼·得逕推）　差異表 ＋ 執行報告 ＋ `GB-67` 之末端追加

---

## §三　驗收（**性質閘**·⛔ 計數式·⛔ 以列號或筆數為證據）

| # | **所保證之性質** | 判定集基數 | 兩造對照（自證⛔ 恆綠） |
|---|---|---|---|
| `P-0` | **錨之碼位已定位且其語意與 `零-1` 逐字相符** | `c0-1` 所得碼位數（二檔分列） | 語意不符 ⇒ 停機（⛔ 判為通過） |
| `P-1` | **同一 `(情境,街廓,側)` 內任二宗之分配範圍<u>交集面積為零</u>** | 🔴 **本批後可達之全部組**（期初 `13`·期末預期 **> `13`**·母體基數須同格出艙）＋ `R3` 組 | 相疊矩形須紅／相離須綠／注入 `+1㎡` 於原綠之組須轉紅。🛑 **⛔ 寫作「違反組數 ＝ 0」**——operative 形為「逐組逐對之交集面積皆為零」；**每組 `n = 0` ⇒ loud 拒測** |
| `P-2` | **`②-宗` 圍堵閘於全部可達組皆⛔ 觸發** | 可達組數 | 以原錨態為負對照，須於 `0m·R2`／`3.5m·R5` 觸發 |
| `P-3` | **每一宗<u>同時臨</u> FRONTLINE 與 BASELINE，臨接長 `> 0`** | 全部落位宗數 | 修前已知須紅之宗具名 |
| `P-4` | **`ΣG ＋ Σ池 ＝ 街廓面積`，守恆殘差為零** | 可達街廓數 | 人造扣 `1㎡` ⇒ 須紅 |
| `P-5` | 🔴 **第 `0` 宗（街角地）之 `G`、分配範圍面積、遠側境界線<u>改前改後逐位相同</u>** | 有 winner 之街角側數 | 人造移動該線 `1e-3` ⇒ 須紅。🛒 **本閘係本批之核心不變量**——錨移動的是**其後各宗之起點**，⛔ 街角地本身 |
| `P-6` | **街角地<u>歸屬</u>改前改後不變** | 全 `12` 側 | 🛑 `R6/right` 標「**不可判**」（`GB-156`·`SIDE ∥ ALLOC` ⇒ `d-1` 鑑別力為零）——**⛔ 併入違反計數** |
| `P-7` | **`V-1′` 五款**（`VR-094` ＋ 其末端追加） | 陽性 `3.5m·R2·left` `n=6`／真陰性 `3.5m·R1·**right**` `n=3`／第三類 `3.5m·R2·right` `n=6` | 三者**同色 ⇒ 閘失能**（須 `False`）；🛑 `1e-6` **⛔ 放寬**。🛑 陰性⛔ 寫作 `3.5m·R1`（`R1·left` 非鏈頭 `0` 宗 ⇒ 判定集為空 ⇒ 假綠·`自誤 329`） |
| `P-8` | **harness 可達性<u>只增不減</u>** | 二情境 | 與 `S-8` 之期初表逐格對拍；任一街廓由可達轉不可達 ⇒ 🛑 停機 |
| `P-9` | 🔴 **名目之增減已<u>逐項歸類</u>** | 期末名目集基數 | 三類：`(i)` 本批所修者轉綠／`(ii)` **新可達之世代所新曝**之紅（⛔ 本批所致之迴歸）／`(iii)` **真迴歸**（原可達且原綠而今紅）。🛑 **紅之總數增減本身⛔ 為判準**；`(iii)` 非空 ⇒ 停機上呈 |

🛑 **精度款**：`V-1′ (3)` ⛔ 以 `verify/out/*.csv` 之 `2dp` 欄評估，須讀全精度；`(1)` 二讀法（落檔 `宗地寬度(m)` 欄／全精度 `s` 區間長）**並列出艙·⛔ 靜默擇一**。

---

## §四　差異表之形（**域語言**·⛔ 程式術語／變數名／行號）

**表一（逐宗）**《街角地後續配地位置更正前後對照表（`退縮 <X> 公尺`·`<街廓>` 街廓 `<左／右>` 側）》
欄：配地次序（序數）｜重劃前地號／土地所有權人｜調整前・調整後分配面積（㎡·2dp）｜**面積增減**（㎡·2dp·帶號）｜調整前・調整後臨路寬度（m·2dp）｜**寬度增減**（m·2dp·帶號）｜**位置變動**（m·2dp·沿正面路之起點位移）｜處置（`位置外移`／`維持原位`／`新入可分配`）｜是否街角地／備註

**表二（逐街廓·守恆）**：街廓｜分配地面積合計（前・後・增減·帶號）｜調配池面積（前・後・增減·帶號）｜街廓總面積｜**守恆殘差（應為 `0`）**

**表三（彙總·首頁）**：**街角地面積有變動之街廓（預期 ＝ 無·`P-5`）**｜街角地歸屬有變動之街廓（預期 ＝ 無·`P-6`）｜位置外移之宗數・最大外移量（m）・中位外移量｜面積增減宗數・`ΣΔ`・`Σ|Δ|`（**帶號與絕對值並列**）｜所有權人受影響戶數｜**因本批而首次可分配之街廓**

🔒 **量級（發單側之估計·⛔ 承諾值）**：`0m·R2` 之錨位移約 `7.2` 公尺（`0.2163` → `S_req 7.4487`·`W-G.9-84` 所載）；受影響宗數之量級 `10¹`，因新可達街廓而擴及者上界 `10²`。
🛑 **若差異表出現街角地面積或歸屬變動 ⇒ 停機上呈。**

---

## §五　側分支與逐 `commit` KL 放行

```
備妥（⛔ 推主分支）→ 推至側分支 verify/W-G.9-268p-c<n>
→ 發單側自側分支逐位元組復驗 → 發單側請示 KL → KL 放行
→ CC 將主分支 wip/s1-endpart 推進至該物件
```
🛑 主分支**原地不動**；未放行前⛔ 前進一個 `commit`。主分支若於其間前進 ⇒ rebase 後**重推側分支並重驗**。
🔒 **零生產碼 `commit`**（`c0`／`c4`）依 `常規一 🔧 一` **逕行 `push`**；其判法逐 `commit` 出艙（**含空輸出**）：
```
git diff --name-only <前一已push之HEAD>..<本commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```
🩸 **`push` 復驗之對照組**：其受詞須逐字寫為「**為 `origin/wip/s1-endpart` 之祖先**」，⛔ 寫為「在 `origin`」。`6cb77b7`（＝ `origin/main` 之 tip）**在 `origin`** 而**非該分支之祖先**——以「在 origin」為述詞時該對照為假。（`-267R 自捕 6` 同族·已具名二次。）

---

## §六　併線前必答

1. **覆蓋帳**：本批後**可達之全部組**逐組逐對交集面積為零（母體基數同格出艙·期初 `13` ⇒ 期末預期 > `13`）＋ `R3` 組（依 `VR-095` 逐街廓獨立呼叫取值，**恆標「單源·歸納支持」**）。
2. **`V-1′`** 五款 ＋ 三組對照（各載 `n`·`1e-6` ⛔ 放寬）。
3. **`run_all` 名目差集**——比較端 ＝ `S-5` 所記之期初態（⛔ 凍存名單之舊態）；逐項歸類 I／II／III **⋀** `P-9` 之三類；**類 II ⛔ 自動視為同**。
4. **差異表**呈 KL。
5. **`P-5` 之逐街角側出艙**——街角地之 `G`、範圍面積、遠側境界線改前改後逐位對照。

---

## §七　鑄號之授權

🔒 **本批得鑄者 ＝ `自誤 334`、`自誤 335` 二號；⛔ 逾此。**（次號當場現查：期初四簿 ＝ 自誤 `325`／`332`／缺 `[106]`、`GB` `154`／`156`、`VR` `80`／`95`、`K-9` `28`／`29`。）

**`自誤 334`**（責 ＝ **發單側**）　**以未經自行復驗之他方報告表格充作開工閘之通過條件**。
形：審定 `W-G.9-268` 時，將附件甲之 `33` 檔生產碼短 `hash` 表逕採為 `S-3` 之比較端，而當時僅復驗 `app.py` 一筆與檔數 `34`。事後補驗查得一筆抄錄誤（`fixture_yi_construction`：表載 `69f4a18e…`，實為 `69f4a18a…`；`size` `11562` 相同 ⇒ 僅單字元異）。
通則：**凡將他方所出之表列為閘之比較端，該表須先逐列自行復驗；未復驗者一律改為「當場現查並出艙」，⛔ 引表。**

**`自誤 335`**（責 ＝ **發單側**·🔴 **本則使一個重級批空轉至 `c0`**）　**取一受詞之方法時只讀其最新補款所列，未回溯同一簿內該受詞自身之成因鏈**。
形：出 `W-G.9-268` 時取 `GB-67` 於 `W-G.9-267` 補款所列之三方法（`K-9-23` 二閘轉主動／`K-9-17` 遞補／第 `1` 宗境界線不動），未讀同檔上游之 `W-G.9-85`／`-87` 加註——該二節逐字載明破量之近因為**錨點**，且已量得新錨下重疊 `0`（5／5）。致本單將「覆蓋帳歸零」寫入併線前必答，而該條件在所列之三方法下**結構上不可達**。
🔒 **與 `自誤 330` 之別**：`330` 係「裁一族結構缺陷時只掃一簿」；本則係「**同一簿、同一受詞、只讀其末段補款而未讀其成因鏈**」。
通則：**凡以某阻塞項為受詞出單，其方法之取用須同時出艙該項之<u>成因鏈</u>與<u>歷次加註</u>之逐節清單，並具名所取與所棄；⛔ 只引其最新補款。**

🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §八　停機款

- 開工閘任一不符（含 `S-1` 之二號不可辨、`S-4` 之期初態與 `e57d2ef` 相異）。
- `c0-1` 所定位之碼位語意與 `零-1` 不符。
- `c0-3` 之 shim 二態同值，或原錨態⛔ 復現 `45.9766`／`56.3293`。
- `P-1` 於 `c2` 後仍有任一組之任一對交集面積 `> 0`（⇒ 尚有第三病，須重新對位）。
- `P-5` 或 `P-6` 轉紅（街角地本身或其歸屬被動到）。
- `P-8` 任一街廓由可達轉不可達。
- `P-9` 之類 `(iii)`（真迴歸）非空。
- 須動及 `§零-3` 未列之檔或 `§零-4` 所排除之受詞。
- 單與正典相斥 ⇒ 依 `常規二` 取**保守項**續辦並**逐字上呈**，⛔ 自行放寬。

---

## §九　出艙形

`docs/reports/W-G.9-268pR_錨點更正.md`：開工閘八閘逐條（各載其母體與粒度框）／`c0` 四項／逐 `commit` 之生產碼判（含空輸出）／`§三` 十閘逐閘（各載 `n` 與兩造對照）／差異表／併線前必答五項／🩸 **本批之自捕**（照實·⛔ 隱去·各載 形／徵候／攔法）／自限復驗（`porcelain`／`left-right`／四簿 MAX 未動）。

🛒 **本報告⛔ 載**：任何關於**病一**（`K-9-23` 閘一轉主動／`K-9-17` 遞補）之修法主張——其為 `W-G.9-269`。

---

## §十　工項零（**新 CC 審查窗辦**·辦畢即推即停·零生產碼 ⇒ 得逕推）

原封入倉，**⛔ 動一字**，**⛔ 執行 `§二` 任何工項**：
1. 本單 ⇒ `docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md`
2. 出艙 `git status --porcelain`（須 `0`）與四簿 MAX（須未動）與 `W-G.9-268′` 之號占用六數

辦畢即推即停。`§二` 之工項由**新 CC 施工窗**辦。

---

## §十一　`W-G.9-269` 之預告（⛔ 本批辦）

**受詞 ＝ 病一**：`K-9-23` 閘一由觀測轉主動 ＋ `K-9-17` 遞補迴圈 ＋ 第 `1` 宗境界線不動；土地現值 ＝ `2` 宗／`4.7950 ㎡`（`628-42(1)` `3.8082`／`628-53(2)` `0.9867`）。
**前置**：本批併線；並俟管線可達更多街廓後**重量 `A` 母體**——`A` 之現值係於原錨態下量得，錨更正後其違反宗集**可能改變**，⛔ 沿用。
🔒 `W-G.9-269` 之號現查為**六數全 `0`**（發單側於 `95882de` 實測），⛔ 憑此免除其開工閘之現查。

---

*本單受詞 ＝ CC。`c0`／`c4` 零生產碼得逕推；`c1`〜`c3` 逐 `commit` 須 KL 放行。純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
