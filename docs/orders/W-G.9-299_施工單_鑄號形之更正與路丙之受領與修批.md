# `W-G.9-299`　施工單（**鑄號形之更正 ／ 路丙之受領 ／ 接觸面之現查 ／ 修批（凍存分支）**）

> **態**｜開工 ＝ `9660b53cdc61690840029c52ec7126476d205d1a`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·⛔ 短前綴）
> **單** ＝ `docs/orders/W-G.9-299_施工單_鑄號形之更正與路丙之受領與修批.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **重**（登記 ＋ 現查 ＋ **生產碼修批**·修批**一律落凍存分支**·**⛔ 併線**·**⛔ push 生產碼至主線**）
> 🛑 **本批鑄號 ＝ `自誤 400`／`401`／`402`**（逐字由本單供·**`###` 形**）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔑 **KL 之放行（`2026-09-15`·逐字「放行」）於本批<u>首次動用</u>**，其射程 `(a)(b)` 逐字見 `§一` 裁 `4`；
> 🔴 **`(c)` ⛔ 及於入倉主線** ⇒ 修批**一律落凍存分支 `verify/W-G.9-299-gb170`**、**⛔ 併線**、**⛔ 第二次放行前⛔ 推主線**。
> 🛑 **工項四受工項三之停機款拘束**（`§五` 款 `4`）；停機則工項一／二／三之成果**已在主線**、⛔ 回退。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　開工閘（十項·**每列同格載其產生指令**·`自誤 392`·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `9660b53cdc61690840029c52ec7126476d205d1a` 或其後裔（**全 `40` 碼**·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**取之） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 項**逐項具名處置或具名豁免**·⛔ 靜默略過（本單之預告見 `§零-1`） |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ 器**所印之檔名列**） | [必真] 含 `W-G.9-299` ＝ `True`／[必偽] 含 `W-G.9-298` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0` ＋ 四靶逐項成立 |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0` |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`389`**／**`399`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·下一標籤 **`bj`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；判別力[必不命中] 子層 `^verify/.+/.+\.py$` **須 `> 0`**（**逐批漂移**·⛔ 定值·`自誤 386`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **未設 ⇒ 預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（本批方開）；`verify/W-G.9-289-gb170` 命中 **`0`**；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`** |

### `§零-1`　`P-4` 之逐項處置（**發單側已實跑·⛔ 靜默略過**）

本單經發單側實跑 `probe_order_preflight.py`：🔴 **`0`** 項。🟡 項之列號與其節見報告，其 `→` 若皆係
**賦值鏈／消費鏈之串接**（`(甲)` → `_rw_start` → `Rw` → `G_target` 等）而⛔ 幾何向量之方向性轉引 ⇒ **受具名豁免**；
🛑 **凡實跑所得之 🟡 項不在上開性質內者，⛔ 受本豁免，須停、照實具名、⛔ 續**。
🔑 **一項<u>逐項具名之處置</u>（⛔ 豁免·發單側已釘其槽）**：`§三` 之 `自誤 400` payload 內所引之 `mp→近側`
係**幾何方向**、⛔ 賦值鏈 ⇒ ⛔ 受上開豁免。**其槽由發單側於此釘死**（`W-G.9-297` 二-2 實測·⛔ CC 重判）：
該量 ＝ `_W_near = float(np.dot(_bp_w - _mp_w, _ahat))`，**起點 ＝ `_mp_w`**／**終點 ＝ 本宗近側界線上之 `_bp_w`**／
**投影槽 ＝ `_ahat` 之<u>線向</u>**（⛔ 法向）；其值為**帶號純量**（`(甲)` 於三格 ＝ `4.4804447557822895`／
`6.98048044737404`／`7.785015718090257`）。🛑 該 payload 係 `app.py` 註之**逐字引**，**⛔ 改其一字**——
槽之具名落於本節，⛔ 落於 payload。
🛑 本批凡出艙**幾何向量**之夾角者（`§六` 款 `3`），一律**釘其槽**（線向／法向）並以**實邊**機驗——該禁⛔ 在本豁免射程內。

🛑 **恆常附款**（坑 `bh`／`bi`／`3`／`bc`／`bd`／`g`、`GB-158` 之三形分列體例、簡繁誤字機檢、`rev` 釘死、**先跑再寫**、
[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、payload 之**落地形機驗**（🆕 **繫於性質**·見 `§三`）、
函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、向量夾角**釘槽**並以實邊機驗、單字元名取 AST、
型別歸類出艙**實測節點型別**、**逐閘出艙執行次數**、**等待綁行程結束**、**`id(·)` ⛔ 作錨**、
**⛔ `\| tail -N` 取長跑輸出**、**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、**帶號與絕對值並列**、
**物證落檔保全⛔ 覆寫**、`f_locals` 變數名為鍵者須**自證其存在**並**增第二鍵**、
`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退**⇒ 先 `git cat-file -e` 驗存在·**三態分列**）**逐項適用·⛔ 省**。
🆕 **一則新附款**（`W-G.9-298` 自捕二所立·⛔ 省）：**本批方鑄之號⛔ 得作其自身之「寬字樣」判別力對照**
——其命中恆為 `1`、⛔ 比錨寬 ⇒ 判別力為零（`自誤 385` 族）。寬字樣須取**該號鑄成前即已存在**之字樣。

---

## `§零′`　前批（`W-G.9-298`）之核可（**發單側已辦·⛔ CC 重辦**）

`W-G.9-298` 四 `commit`（`fa079072`／`05fe5c52`／`de843d34`／`9660b53c`）經發單側自倉獨立復驗：

| 受詞（指令） | 實測 | 判 |
|---|---|---|
| 閘 `7`（`git ls-remote origin refs/heads/wip/s1-endpart`） | `9660b53cdc61690840029c52ec7126476d205d1a`（全 `40` 碼·＝ 回報值） | ✅ |
| 工項零之單 blob 內容 `sha256` | `6ecaa3d1d46e3c136a16a016ea9093e35e7a650c558c56a7cb2451678620f83e` ＝ **發單側所交付之檔逐位相同** | ✅ |
| 生產碼判法（母體**正面列舉 `34` 檔**·框 ＝ `git diff --name-only 2df61769..9660b53c` ⋂ 該母體） | **`0`** 行（本批異動 `7` 檔）；對照組[必非零] `17fd981^..17fd981` ＝ **`2`** | ✅ |
| 逐 `commit` 之 `git show --numstat` 刪除欄 | **全 `0`** | ✅ |
| 四簿 | 自誤 `389`／`399`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | ✅（見裁 `1`） |

🩸 **二則自捕皆於出艙前攔下、處置正確**：① `settrace` 取「第一個 `call` 事件」而攔到 `numpy` ⇒ 同一性**偽紅**；
其**徵候之辨識（偽紅與四造全綠並存）**尤為得當，⛔ 頂替、保全 v1 落檔為物證。② 寬字樣取本批方鑄之號 ⇒ ⛔ 判別力；
改取 `自誤 `（含尾空格）＝ `898` 列。**②之形已立為 `§零` 新附款。**
🔑 **單內互斥之逐字回報 ＋ 依 `常規二` 取保守項續辦 ＝ 處置完全正確**，⛔ 判為缺失；其責**全在發單側**（裁 `1`）。

⇒ **✅ 全批核可。**

---

## `§一`　裁與鑄號之逐字（**發單側·⛔ CC 自裁**）

| # | 事 | **裁** |
|---|---|---|
| `1` | 🔴 `W-G.9-298 §四` 之 payload **未能鑄號**（自誤簿期末仍 `389`／`399`） | 🔴 **責 ＝ 發單側·鑄 `自誤 402`**。**發單側自倉之獨立現查**（⛔ 轉引回報）：鑄號框 `RE_ZW_DEF` 逐字 ＝ `^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：]`（自 `probe_WG9267_issuer_measurers.py` 當場取）；落地後該框對簿命中 **`376`** 筆·MAX **`399`**·`400`／`401` 皆 `False`·[必命中] `399` ＝ `True`；簿內既有鑄號形 ＝ `### 🩸 \`自誤 399\`　**…**`。⇒ **互斥坐實**，其形見 `§三` 之 `自誤 402` payload |
| `2` | `自誤 400`／`401` 之 payload **內容**（`W-G.9-298 §四` 逐字） | **內容受領·⛔ 改一字**；**僅改其首列之形**為 `### 🩸 \`自誤 N\`　**…**`（⛔ 剝層之爭已無射程——本批 payload 自始即 `###` 形、⛔ 以 `> ` 承載） |
| `3` | 🔑 **路丙之受領**（`W-G.9-298R` 之全量結果） | ✅ **受領路丙為 `GB-170` 之修之<u>實作形</u>**。**依據（逐項具名）**：① 裁 `5` 之重設造二項皆成立（同一性 `3`／`3`·呼叫鏈委派至 `co_filename` ＝ `'<app.py:harvest>'` 之 `k956_W_from_mp`；唯一可得之格逐位相同·最小差 `0.0`）；② `A′`／`B′` 於三格皆未逾其**自機制導出**之界（`B′` ＝ `2dp` 捨入之半個 ulp `0.005 ㎡`；`A′` ＝ `0.005 ÷ 跨距`）；③ 切向**釘槽**二槽三格 `\|cos\| − 1` 皆 `≤ 2.22e-16`（以**實邊**機驗）。🛑 **受領之射程**：`(甲)` 及於**取線之設計**（哪一條線）；`(乙)` **⛔ 及於**其施行之可行性（接觸面·見裁 `5`）；`(丙)` **⛔ 解除或收窄 `GB-170`**——其失效條件 `(2)`（改後行為差逐宗量得並具名）與 `(3)`（其餘逐位不變之證）**均未成就**，本批工項四即為其量測 |
| `4` | 🔑 **KL 放行之動用**（`2026-09-15`·逐字「放行」） | **本批首次動用**，**射程逐字**（`W-G.4` 登記表 `(4)`·⛔ 擴）：`(a)` 及於**該一處取線之改與其量測**；`(b)` 及於三段辦法（① 先釘紅 ② **只改該處**並證其餘**逐位不變** ③ 改後**逐宗量面積差**）；`(c)` 🔴 **⛔ 及於入倉主線**——須**第二次放行** ⇒ 修批落**凍存分支**、**⛔ 併線**；`(d)` ⛔ 及於 `GB-167`／`168`／`169`、`K-9-29`、藍影。🛑 **⛔ 於本批呈 KL**（材料未備——逐宗面積差為第二次放行之受詞，本批方量） |
| `5` | 🔑 **`(b)`「只改該處」之可施性** | 🛑 **⛔ 由發單側逕斷·須現查**。**發單側之預查（⛔ 充結論·CC 須自行重得）**：`_corner_buffer_S` 之**呼叫端**於生產碼 `34` 檔母體內為**複數**（`app.py` 之 `main` 內 `2` 處·`verify/stepg_pipeline.py` `2` 處·`verify/wf_f1.py` `1` 處·`verify/wf_f4.py` `2` 處·字樣錨 `_corner_buffer_S(` 之列框）；而 `W-G.9-298R` 實測之**執行次數**為 `1`（`SB 0.0`）／`2`（`SB 3.5`）**且段內同數** ⇒ 生產期實走者僅**其一族**。⇒ **工項三**現查其全量並判「改之接觸面是否 ⊆ forced 之路徑」；**逾之即停機上呈**（`§五` 款 `4`·路甲之否決即同一形） |
| `6` | `GB-170` 之現態 | 🛑 **失效條件三款仍⛔ 有一款成就**·**⛔ 解除、⛔ 收窄、⛔ 提修法主張**；本批之登記見 `§四` |
| `7` | `W-G.9-295` 所載之**未解餘量**（`R4/left@0.0` 之 `6.157e-6 ㎡`·比值 `0.9879` vs 另二格 `1.000002`／`1.000001`） | **照實帶入本批·⛔ 判其成因**。🔒 併記其量級之機械事實：`6.157e-6 ㎡` **小於** `2dp` 捨入之半個 ulp（`0.005 ㎡`）**三個數量級** ⇒ ⛔ 能改變任一以 `2dp` 表之分配面積。🛑 **⛔ 以此充「其成因已閉合」**；工項四之**逐宗面積差**為其實測之場 |

---

## `§二`　工項零：**本單原封入倉**（主線）

落點 ＝ `docs/orders/W-G.9-299_施工單_鑄號形之更正與路丙之受領與修批.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`）＝ 器所印之檔名須含 `W-G.9-299`（[必真]）且⛔ 含 `W-G.9-298`（[必偽]）；
所指之檔數 ＝ `1` 之自證（框 ＝ 磁碟 `os.path.isfile`·母體 ＝ 工作區；造甲[必≥1] ＝ `W-G.9-298` 之單／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（框 ＝ `bytes.count(b'\r')`·**讀 blob 取 bytes**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf`）；
blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**三方逐位相同；`numstat` 之 `deletions` 欄 ＝ `0`。

---

## `§三`　工項一：`自誤 400`／`401`／`402` 之鑄（主線·落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

🔑 **payload 之界 ＝ 二哨兵之間（不含哨兵列本身）**·**⛔ 行號為錨**（`自誤 397`）：
起 ＝ `<!-- ZW-PAYLOAD-BEGIN W-G.9-299 -->`；迄 ＝ `<!-- ZW-PAYLOAD-END W-G.9-299 -->`。
🛑 二哨兵須先出艙其**唯一性**——🔑 **框 ＝ 整列<u>逐位相等</u>**（`^…$`·⛔ 子字串含有），母體 ＝ 本單單檔·列框·
各須命中 **`1`**；**⛔ 以子字串框量之**——本單之行文另引該二字串各一次，子字串框下必得 `2`（**此即其判別力**：
子字串框之命中 `2` ⋀ 整列框之命中 `1` **二數並報**·相異即證該框非恆同）；
🛑 **哨兵列本身⛔ 入簿**；payload **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**（`常規七 一`：由檔案管線直接產生·⛔ 憑記憶重寫）。

<!-- ZW-PAYLOAD-BEGIN W-G.9-299 -->

### 🩸 `自誤 400`　**以一個詞承載二個相異之量，而單未釘其口徑**（修訂重發·`W-G.9-299`）

「`W_0`」於本波至少指涉：`(甲)` `app.py` 之 `_W_near_out`——其註逐字「**本宗<u>近側</u>界線之 KL W**
（mp→近側·供 stepg telescoping 閘 `W₀` 用）」；`(乙)` `verify/stepg_pipeline.py` 之 `_mp_base_W0`
（其註逐字「自此**純委派**至 `k956_W_from_mp`」）所得之 `_b_L0`／`_b_R0`。

**徵候（`W-G.9-297` 二-2／二-7 實測·逐格）**：`(甲)` 於三 `forced` 格 ＝ `4.4804447557822895`（`0.0 R4/left`）／
`6.98048044737404`（`3.5 R2/left`）／`7.785015718090257`（`3.5 R4/left`）；`(乙)` **僅於 `3.5 R2/left` 可得**且與
`(甲)` **逐位相同**（`6.98048044737404`）。`W-G.9-296 §三-1` 以 `(甲)` 之二值為「生產態之 `W_0`」而與 `(乙)` 之
唯一生產值判「⛔ 相符」——**其二端係<u>跨格</u>之比對**，⛔ 同一格之二個相異量。

🔴 **原 payload（`W-G.9-297`／`-298` 所載）之「二量所描繪之缺陷量級相差逾一個數量級」係該跨格比對之產物，於本則<u>撤回</u>。**

**後果之指名**：`W-G.9-287` 據以呈 KL 之「負擔率差」係取 `(甲)`；其三格實測（帶號）＝ `+4.958007524346591`／
`-0.11126144996796938`／`+4.2390848776873895`（`W-G.9-297` 二-5）⇒ 前呈之中間一列 `9.02` 係 **`@∩FRONT` 之
代理量**、**⛔ `(甲)` 之值**（發單側已向 KL 更正）。

**族** ＝ 受詞界定（`自誤 391`「序數詞二讀法」之孿生）。

**攔法**：凡單內出現一個**在碼面有二處以上同名或近名之量**者，一律於同格**釘其口徑**（檔／函式／變數名**逐字**）
並令受單側**併報另一讀法之值**；**⛔ 令受單側擇一**。🆕 **併立**：凡以「相符／不符」判二值者，須於同格
**具名其二端之格**（街廓／側／情境），**⛔ 以跨格之二值作相符判**。

### 🩸 `自誤 401`　**以一個未查證之<u>可得性</u>為前提，斷言某對拍「於三格皆可施」**（修訂重發·`W-G.9-299`）

`W-G.9-296 §三-1` 逐字：「此一對拍**不倚賴包裹之錄**，故於三格**皆可施**」；實測僅 **`1`／`3`** 格可施。

**徵候**：該斷言之理由（「不倚賴包裹之錄」）**只排除了一個**不可得之來源，而未窮舉其餘。

**分項成因之指名（`W-G.9-297` 二-4／`W-G.9-298` 款 `2` 二度實測·逐格）**：`_mp_base_W0` 之定義與呼叫**皆在**
`not _degenerate_order and _N > 1` 之分支內；三格之 `_degenerate_order` **皆 `False`**，而 `_N` ＝ `1`（`0.0 R4`）／
`14`（`3.5 R2`）／`1`（`3.5 R4`）⇒ **不可得之分項成因 ＝ `_N > 1` 為偽**，**⛔ 歸因於 `_degenerate_order`**。

**第二後果之指名**：同一未查證之可得性，亦使 `W-G.9-295` 自我驗證閘 `(iii)` 之造（「`W_0′` 須與生產錄逐位相同」）
於三格中**二格結構上不可成立** ⇒ 該閘之判別力於該二格為零。該造已由發單側於 `W-G.9-298 §一` 裁 `5`
**撤回並重設**為「同一性 ＋ 可得之格逐位相同」，並於 `W-G.9-298R` 實測**二項皆成立**。

**族** ＝ 受詞母體未窮舉（`自誤 388`／`399` 族）。

**攔法**：凡斷言一項量測「於某母體**皆**可施」者，須先**逐格現查其可得性之全部前提**（守衛條件之**各分項**），
並於單內**具名其不可施之格**；⛔ 以「已排除其一」充「皆可施」。

### 🩸 `自誤 402`　**以一個與其所欲保證之性質<u>反向</u>繫結之機驗，充該性質之證**（`自誤 393` 之重踩·第二度）

**形**：`W-G.9-297`／`-298` 之 `§四` 逐字令「本 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留 `> `**
（⛔ 剝層）」，並以「payload 內 `^###` 起首列 ＝ `0`（對照組 `^##` 須 `> 0`）」為其**落地形之機驗**。

**徵候**：該機驗之**綠**（`^###` 起首 ＝ `0`）恰為鑄號框**必不命中**之**充分條件** ⇒ **閘綠 ⟺ 號必未被鑄**。
二者並存於同一單，而單之收工閘 `4` 又期「自誤 MAX 推進至 `401`」⇒ **單內互斥**。

**根因**：`自誤 393` 之教訓被**反向援用**。`393` 之受詞係「以 `>` 引用裝置承載之號**實未被鑄**（靜默）」——
其攔法應為「令 payload **以鑄號框可命中之形落地**」；而本單援之為「**⛔ 剝層**」之理由，恰得其反。

**實測（`W-G.9-298R`·發單側自倉獨立重得）**：鑄號框 `RE_ZW_DEF` 逐字（自 `verify/probes/probe_WG9267_issuer_measurers.py`
當場取）＝ `^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：]`；簿內既有鑄號形 ＝ `### 🩸 \`自誤 399\`　**…**`；
`W-G.9-298` 之 payload `30` 列中 `^#` 起首 ＝ **`0`**（**剝一層後仍無 `#`**）；落地後該框對簿命中 **`376`** 筆·
MAX 仍 **`399`**·`400`／`401` 皆 **`False`**·判別力[必命中] `399` ＝ **`True`**。

**族** ＝ `自誤 393` 之**重踩**（第二度）＋ 閘繫於**代理量**而非性質（`自誤 398`／`395` 族·六問第 `1` 問）。

**攔法**：凡 payload 之目的係**鑄一個號**者——
① 其落地形之機驗一律**繫於性質**：落地**後**以**該簿之鑄號框逐字**（自其量測器**當場現查**·⛔ 轉引）對簿逐列
match，該號須**命中**且該簿之 MAX 須**推進**；
② **⛔ 以 payload 之字面特徵**（有無 `###`／有無 `> `／有無 `^##` 對照組）**充證**；
③ 單內須**逐字給出 payload 之首列形**（如 `### 🩸 \`自誤 N\`　**…**`），並令受單側於出單當下即機驗該形**可被該框命中**；
④ 凡 payload **⛔ 鑄號**者（如 `GB` 登記表之末端追加），須**反向機驗**該簿之 MAX **不變**——⛔ 令「不鑄」亦成靜默。

<!-- ZW-PAYLOAD-END W-G.9-299 -->

### 三-1　落地後之機驗（🔑 **繫於性質·⛔ 繫於 payload 之字面**）

| 款 | 受詞 | 判準 |
|---|---|---|
| `1` | 鑄號框之**當場現查**：自 `verify/probes/probe_WG9267_issuer_measurers.py` 取 `RE_ZW_DEF` 之**逐字**（AST 或字樣錨·**⛔ 轉引本單**） | 出艙其 pattern 逐字 |
| `2` | 以該框對**落地後**之簿逐列 match | `400`／`401`／`402` **皆命中**（`3`／`3`）；MAX ＝ **`402`**；相異 ＝ **`392`**（`389 + 3`） |
| `3` | 判別力二造 | [必命中] ＝ `399`（**本批鑄成前即存在**·`§零` 新附款）須 `True`／[必為零] ＝ 一**執行期組出**之人造號（字面⛔ 出艙）須 `False` |
| `4` | `probe_WG9267_issuer_measurers.py registry` 之期末 | 自誤 相異 **`392`**／MAX **`402`**；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 哨兵列**⛔ 入簿** | 落地後簿內 `ZW-PAYLOAD` 之命中 ＝ **`0`**（列框·母體 ＝ 該簿單檔） |
| `6` | append-only | 期初／期末 bytes·`+N／−0`·嚴格前綴檢**二造**（`new.startswith(old)` [必真]／`old.startswith(new)` [必偽]）·`new.endswith(payload + b'\n')`·`python verify/tools/wg942_append_audit.py`（先設 `PYTHONIOENCODING=utf-8`）`rc = 0` |

🛑 **款 `2` 任一不成立 ⇒ 判器紅、停、上呈**（⛔ 續辦工項二以下）。

---

## `§四`　工項二：`GB-170` 之末端追加（主線·落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**·⛔ 上文一字不刪）

🔑 **本 payload ⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**（與 `§三` 之形**刻意相異**·其由見 `自誤 402` 攔法 ④）。
界 ＝ 二 `GB-PAYLOAD` 哨兵之間（**不含哨兵列**·哨兵⛔ 入簿）；其唯一性之框同 `§三`（**整列逐位相等**·二數並報）。

🔒 **GB 鑄號框之當場現查（⛔ 憑臆測·`自誤 402` 攔法 ①）**：`GB` 之框係**三形之聯集**（`GB-158` 體例）——
`RE_GB_F1`／`RE_GB_F2`／`RE_GB_F3`（自 `verify/probes/probe_WG9267_issuer_measurers.py` 當場取其逐字）。
🛑 **本 payload 之節標題 `## 🔧 \`GB-170\` 之修…` <u>確會</u>被 `F1`／`F3` 命中**——惟其所加之號 ＝ `170`
**已在聯集內** ⇒ 對聯集為**空操作**，MAX 與相異**皆不動**。🔑 **⛔ 以「未被框命中」充「⛔ 鑄號」之證**；
其證係**聯集之 MAX 與相異不變**（見 `四-1`）。

<!-- GB-PAYLOAD-BEGIN W-G.9-299 -->

---

## 🔧 `GB-170` 之修：路丙之**受領** ＋ 接觸面之現查 ＋ 修批（`W-G.9-299`·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為末端追加（`戒 36`）；⛔ 改任何原節一字、**⛔ 解除、⛔ 收窄任何 `GB`**。

> **`GB-170` 之修：路丙之受領（`W-G.9-298`／`-299`·⛔ 解除、⛔ 收窄）**
> **`W-G.9-298` 之實測（`rc = 0`·四造全成立）**：① 同一性 **`3`／`3`**——呼叫鏈 `_w0 → … → _spy_k → k956_W_from_mp`，
> 其 `co_filename` ＝ `'<app.py:harvest>'`；`ns` 之來源機驗 `type(ns)` ＝ `dict`／`ns is globals()` ＝ `False`／
> `sys.modules` 中 `__dict__ is ns` 之模組數 ＝ **`0`** ⇒ **⛔ 任何模組之 `__globals__`**。
> ② 唯一可得之格（`3.5 R2/left`）逐位相同（最小差 `0.0`·生產錄 `1`／器自身 `1`）；另二格 **loud 具名「結構上不可得」**，
> 其分項成因 ＝ `_N = 1`。③ `A′` 實測 `1.5293140231076308e-05`／`1.0248624639075388e-04`／`1.0714144877077558e-04`，
> 界 `1.5030237064795143e-04`／`1.1126495375137079e-04`／`1.4893142796154046e-04`；`B′` 實測 `5.02592537319901e-04`／
> `4.600271176178517e-03`／`3.564108301354407e-03`，界 `0.005` ⇒ **三格皆未逾**。④ 切向**釘槽**二槽三格
> `|cos| − 1` 皆 `≤ 2.22e-16`（以**實邊**機驗）。
> **發單側之裁（`W-G.9-299 §一` 裁 `3`）**：✅ **受領路丙為 `GB-170` 之修之<u>實作形</u>**；其射程 ＝ **取線之設計**，
> **⛔ 及於**施行之可行性（接觸面），**⛔ 解除或收窄 `GB-170`**。
> **未解之餘量（照實·⛔ 判其成因）**：`R4/left@0.0` 之「款 `1` 差 − 對稱差」＝ `6.157e-6 ㎡`（比值 `0.987897178336173`；
> 另二格 `1.000002`／`1.000001`）——其量**小於 `2dp` 捨入之半個 ulp（`0.005 ㎡`）三個數量級**，⛔ 能改變任一以
> `2dp` 表之分配面積；🛑 **⛔ 以此充「其成因已閉合」**。
> **KL 之放行於 `W-G.9-299` 首次動用**（射程 `(a)(b)`）；🔴 **`(c)` ⛔ 及於入倉主線** ⇒ 修批落**凍存分支**
> `verify/W-G.9-299-gb170`、**⛔ 併線**、**⛔ 第二次放行前⛔ 推主線**。
> 🛑 **`GB-170` 之失效條件三款仍⛔ 有一款成就**——`(1)` KL 放行**已得**；`(2)` 改後行為差逐宗量得並具名、
> `(3)` 其餘逐位不變之證——**二者候 `W-G.9-299` 工項四**。

<!-- GB-PAYLOAD-END W-G.9-299 -->

### 四-1　落地後之機驗（**反向**·`自誤 402` 攔法 ④）

出艙項同 `§三` 三-1 款 `5`／`6`（哨兵⛔ 入簿·append-only 二造），**另加**：
🔑 **反向機驗（繫於性質）**：以**三形聯集**對落地後之簿重算 ⇒ MAX **須仍 `170`**、相異 **須仍 `168`**、
缺號 **須仍 `[12, 87]`**（⛔ 鑄號之**性質**·**⛔ 以「payload 內無 `###`」或「未被框命中」充證**）；
判別力[必命中] ＝ `GB-170` 之既有定義列於三形之至少一形須 `True`；[必為零] ＝ 一**執行期組出**之人造 `GB` 號
（字面⛔ 出艙）於三形皆須 `False`。🛑 **三形須分列並取**·判由**非空形**承擔·二形以上皆 `0` 須 **loud 具名**（`GB-158`）。

---

## `§五`　工項三：**路丙之接觸面之現查**（主線·零生產碼·🛑 **含停機款**）

### 五-1　逐款出艙

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | `_corner_buffer_S` 之**全部呼叫端**：母體 ＝ 生產碼 **`34`** 檔（**正面列舉**）；框 ＝ **AST** 之 `Call` 節點（其 `func` 為 `Name('_corner_buffer_S')` **或** `Subscript(Name('ns'), Constant('_corner_buffer_S'))`·**二形並取**）；逐處出艙 `檔:函式:`**字樣錨**（⛔ 行號為錨）與其 `allocation_dir` 位之**實參逐字**（`ast.unparse`） | **檔／處二數並報**；併報**文字層**（`_corner_buffer_S(` 之列框）之數以資對照·**二數相異即 loud 具名** |
| `2` | 上開各處之**執行期是否走到**：以包裹 `ns["_corner_buffer_S"]` ＋ **呼叫鏈之 `co_name` 序**（⛔ `id(·)`）逐次出艙其**呼叫端之函式名**；二態（`SB` `0.0`／`3.5`）分列 | **逐閘出艙執行次數**（總數／段內數）；⛔ 以「未執行」充「非接觸面」——未執行者一律 **loud 具名為「本案未走·射程未定」** |
| `3` | **`_first_corner_alloc_dir` 於各呼叫端之可達性**（三態：模組全域直呼／`ns` 注入／**⛔ 可達**）與 **`side_mid` 於各處之在域性**（`f_locals`／`t.body` 頂層·**鍵先自證存在**） | `自誤 399` 族：注入通道須**窮舉**（模組 `__globals__`／`ns`／參數／閉包）；⛔ 以「其一可達」充「皆可達」 |
| `4` | 🛑 **停機款**：設 `S` ＝ 款 `1` 之全部呼叫端、`F` ＝ 款 `2` 實測**生產期走到**之呼叫端。判「**改 `F` 即足**」是否成立——即：`F` 之外之 `S∖F` 諸處，其**是否為 `forced` 之路徑**須逐處出艙其判與**其判之產生指令** | **`\|F\| = 0` ⇒ loud 拒測**（判定組為空）；**`S∖F` 中有任一處經判為 `forced` 之路徑而未在 `F` 內 ⇒ 判器紅、停、上呈**（⛔ 續辦工項四）；**⛔ 由 CC 判「改幾處算只改該處」**——CC 只出艙 `S`／`F`／逐處之判，**孰為 KL 射程 `(b)` 之「該處」由發單側裁** |
| `5` | **判別力三造** | [必非零] `\|S\|` ≥ `1`／[必相異] 文字層之數與 AST 之數須**各自出艙**（⛔ 只報其一）／[必為零] 一**執行期組出**之人造函式名（字面⛔ 出艙）於款 `1` 之框下命中 ＝ `0` |

🛑 **本工項⛔ 改碼一字、⛔ 開分支**。

---

## `§六`　工項四：**修批**（🔴 **凍存分支** `verify/W-G.9-299-gb170`·**⛔ 併線**·**⛔ push 生產碼至主線**）

🔑 **本工項<u>明文授權</u>三項受 `§七` 拘束之手段**（`自誤 384` 攔法·射程逐字具名·⛔ 逾之）：
① **改生產碼**——射程 ＝ **僅款 `1` 所列之處**，且**僅該一位實參**（`_corner_buffer_S` 之 `allocation_dir` 位）；
② **開分支**——射程 ＝ **僅** `verify/W-G.9-299-gb170` 一支，自本批主線末一 `commit` 起；
③ **執行 harness**——射程 ＝ 改前／改後各一次全量，供款 `3`／`4` 之對拍。
🔴 **⛔ 在授權內**：⛔ 併線、⛔ push 生產碼至 `wip/s1-endpart`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、
⛔ 改任何**函式簽章**、⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp` **之函式體一字**。

🛑 **工項四受 `§五` 款 `4` 之停機款拘束**——該款紅則**⛔ 辦本工項**，停、上呈。

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **改之逐字**：於 `§五` 款 `4` 所定之 `F`（**且經發單側裁為射程 `(b)` 之「該處」者**——本批之**預裁**：`F` ⋂「`run_step_g` 之 forced 路徑」）逐處，將 `_corner_buffer_S` 之 `allocation_dir` 實參改取 `_first_corner_alloc_dir(side_mid)` | 出艙**改前／改後之逐字 diff**（`git diff -U3`·**全量**）與 `numstat`；🛑 **改動列數須 ＝ 處數**（一處一列）·逾之即 **loud 具名**；🛑 `side_mid` 之取式須**自該處在域之名**取，**⛔ 新增參數、⛔ 改簽章**——若在域性不足 ⇒ **停、上呈**（⛔ 自行擴改） |
| `2` | **其餘逐位不變之證**（KL 射程 `(b)` ②） | 改前／改後各跑一次 `python verify/run_verification.py`（**等待綁行程結束**·⛔ `\| tail -N`·落檔全量），對拍**非 `forced` 之全部格**之輸出 ⇒ 期**逐位相同**；判別力[必相異] ＝ 三 `forced` 格之輸出須**確有相異**（若三格亦逐位相同 ⇒ **改未生效**·判器紅、停、上呈） |
| `3` | 🔑 **逐宗量分配面積差**（KL 射程 `(b)` ③·**第二次放行之受詞**） | **逐宗**出艙：地號／街廓／側／宗序／改前面積／改後面積／**差（帶號與絕對值並列）**；🛑 **⛔ 只報總計**；🛑 **⛔ 以 `2dp` 之相同充「無差」**——併報**未捨入之實算面積**之差。母體 ＝ 全部宗（**正面列舉宗數**·⛔ 抽樣） |
| `4` | **`A′`／`B′` 之<u>實測</u>**（⛔ 設計驗算·本工項之數係**改後之生產態**） | 三 `forced` 格逐格；與 `W-G.9-298R` 之**設計驗算值**並列而**⛔ 同表混列**（分表·各冠其標記）；🔑 **⛔ 因二者相符即判 `GB-170` 可解除** |
| `5` | **藍影（`K-9-23` 閘一）之求值次數**改前／改後 | 逐格出艙；🛑 **⛔ 判其應否求值**（`W-G.4` 登記表逐字「其應否求值**未判**」）·只出艙 |
| `6` | **分支之落地** | `git push origin verify/W-G.9-299-gb170`；出艙 `git ls-remote origin 'refs/heads/verify/*'` ⇒ 該支命中 **`1`**；🔴 `git merge-base --is-ancestor <該支> origin/wip/s1-endpart` ⇒ 須 `rc = 1`（**⛔ 併線**·`rc` 緊接指令）；主線 `git rev-parse origin/wip/s1-endpart:app.py` 須仍 ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1` |

🛑 **出艙即止**：⛔ 判 `GB-170` 可否解除、⛔ 提修法主張、⛔ 呈 KL、⛔ 併線、⛔ 擬第二次放行之呈文。

---

## `§七`　🛑 **⛔ 為之事**

🔑 **三項手段<u>已明文授權</u>、⛔ 在本節之射程內**（其射程逐字見 `§六` 首段·⛔ 逾之）：
① 改生產碼（**僅** `_corner_buffer_S` 之 `allocation_dir` 位·**僅**經判之處）；② 開 `verify/W-G.9-299-gb170` 一支；③ 跑 harness 二次。
其餘：
⛔ **併線**（含 `p3a`／本批之新支）；⛔ push 任何生產碼異動至 `wip/s1-endpart`；⛔ 第二次放行前⛔ 推主線之生產碼；
⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**、⛔ `WV_BAKE`、**⛔ 引任何自 baseline 取得之數**；
⛔ 改任何函式簽章；⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp`／`_mp_base_W0` 之**函式體**一字；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 動 `probe_WG9292_pathC.py`／`probe_WG9295_joint.py`／`probe_WG9298_gate3.py` 一字；
⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號（**自誤 `400`／`401`／`402` 為本批唯一之鑄**）；
⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改任何已入倉文件一字；⛔ 刪任何 ref；
⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文；
⛔ 判 `GB-170` 可否解除、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張；
⛔ 判「改幾處算只改該處」（`§五` 款 `4`·候發單側）；
⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制（`W-G.9-298 §一` 裁 `3`）；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名（**主線與分支分列**） | 刪除欄**全 `0`**（🔑 **工項四之 `commit` 除外**——其係生產碼之修改·其 `deletions` **須 ＝ 改動處數**·⛔ 期 `0`·**loud 具名其值**） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔**（**主線**之逐區間） | 主線逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1`；🔑 **分支**之區間須 **`≥ 1` 行**（[必相異]·證改確已落地） |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末 | 自誤 相異 **`392`**／MAX **`402`**；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**（⛔ 鑄坑）；`ALPHA_LABELS` 之 `git diff --numstat` 輸出**空**；母體檔數之增量須 ＝ 本批新增之 `.md` 檔數（**自洽檢**） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | **主線** `34` 檔 blob 期初＝期末**相異 `0`**·`app.py` ＝ `4379108a…`；`baselines` `298` 檔·`a898f4e1…`（**二態皆未變**）；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**五物證落檔**（`WG9295R_joint_v1_selfpolluted.log`／`WG9296R_w0rec_v1_blknone.log`／`WG9297R_w0caliber_v1_blkabsent.log`／`WG9298R_gate3_v1_firstcall.log`＋本批新增者）之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | **二態分列·⛔ 以其一充判**：工項四已辦 ⇒ `verify/W-G.9-299-gb170` 命中 **`1`** 且 `git merge-base --is-ancestor` 對主線 `rc = 1`；工項四因停機未辦 ⇒ 命中 **`0`**。`verify/W-G.9-289-gb170` 恆 **`0`**；判別力[必存在] `p3a` 命中 `1` |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉**（其 `sha` 於自身落地前不存在·結構上遞迴） |

---

## `§九`　報告之格

`§零` 開工閘（**每列同格載其產生指令**）→ `§零′` 之⛔ 重辦 → 逐工項（逐格表以落檔**全量**出艙·⛔ `\| tail -N`）→
各工項之判別力造之判 → 一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→
`⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**·**主線與分支分列**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；框無 `^` 錨者**二數並報**；**帶號與絕對值並列**。
🔒 **逐閘出艙執行次數**；🔒 同一性一律 `is` 比對（**`id(·)` ⛔ 作錨**）；🔒 判定組為空一律 **loud 拒測**。
🔒 凡自捕一律於出艙前攔下、照實併記，**⛔ 頂替**；物證落檔**保全⛔ 覆寫**。
🔒 **改後之數（工項四）與設計驗算之數（`W-G.9-298R`）⛔ 同表混列**——分表、各冠其標記。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`cat-file`／`merge-base`／`RE_ZW_DEF`／`append_audit`／`ZW-PAYLOAD`／`GB-PAYLOAD`／
`_corner_buffer_S`／`_first_corner_alloc_dir`／`allocation_dir`／`side_mid`／`run_verification`／
`S1_perp`／`GB-170`／`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`ls-tree`／`WV_BAKE`。

【單止】本單受詞 ＝ CC 窗。⛔ 呈 KL。

SELF_SHA256: 5d40545c53508e86798b7dfe7a2007d6d02654f46adf2ea09c62ac4482b400ac
