# `W-G.9-312`　施工單（**中**）：步驟 0 之**二態對拍**（`run_all` 二次·`on`／`off`） ／ `自誤 425` 之鑄

> **態**｜開工 ＝ `16244f13ca269ace1ce3245736c536908bb97986` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**）。
> **單** ＝ `docs/orders/W-G.9-312_施工單_步驟0二態對拍與自誤425.md`（工項零 ＝ 本單原封入倉）。
> **分級** ＝ **中**（**純登記 ＋ 純診斷**·**零生產碼**·`run_verification` **跑二次（二態各一）**·**⛔ 開分支**·**⛔ 修批**·**⛔ 新增或修改任何探針**·**⛔ 判改法**）。
> 🛑 **本批鑄號 ＝ `自誤 425`**（**一號**·逐字由本單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**。
> 🔴 **修批之號仍為 `W-G.9-309`**（其宣告框於 `16244f1` 態實測六數皆 `0`·保留）；**本單⛔ 占其號**、⛔ 辦修批、⛔ 自續 `W-G.9-309`。
> 🔑 **本批之目的**（KL `2026-09-18` 允行）＝ **測一假說**：`K-9-29 二` 已廢止而碼側仍行之**步驟 0 前置合併**，是否即 `F.0` 之 `GSA` 六鍵「該情境無任何決策列」之成因（母體 ＝ `WG9311_runall.log` `1029` 列·框 ＝ 其 `【未被評估】` 列）。**測法 ＝ 環境變數之二態對拍**（`app.py` 既有之出口·**⛔ 改生產碼一字**）。
> 🛑 **本批⛔ 判「步驟 0 應否落地」、⛔ 提修法主張、⛔ 擬 diff**——**只取二態之值並對拍**。
> 🔑 **序**：**① `§二` 工項零**（單獨 `commit`·單獨 `push`）⇒ **② `§一` 開工閘**（末二項 ＝ 甲態、乙態各跑一次）⇒ **③ `§三` 工項一** ⇒ **④ `§四` 工項二**（唯讀對拍）⇒ **⑤ `§五` 工項三**（落檔入倉）⇒ **⑥ `§六` 收工閘**。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　發單側之核可・裁（**⛔ CC 自裁·⛔ CC 重辦**）

### `§零-1`　`W-G.9-311R` 之復驗（發單側自倉獨立·沙盒 `Python 3.12.3`·態 `16244f1`·`core.quotePath` 預設）

| # | 項 | 發單側實得（產生方式） | 判 |
|---|---|---|---|
| `1` | 主線 | `16244f13ca269ace1ce3245736c536908bb97986`；`git rev-list --count d4c7807..HEAD` ＝ `6`；刪除欄非 `0` 者 ＝ `0` | ✅ |
| `2` | 工項零之逐位 | `git show HEAD:<單>` 之 bytes ＝ 發單側交付之源（`27765` B） | ✅ |
| `3` | 工項一之逐位 | 自單 blob 以整列相等定界抽出 `ZW423` `2472` B／`ZW424` `1951` B；**期末 bytes ＝ 期初 bytes ＋ 二 payload bytes**（`893916`→`898339` B·前綴相符） | ✅ |
| `4` | 四簿 | 自誤 `414`／`424`（缺 `[106, 355, 356, 357]`）；`K-9` `39`／`40`（缺 `[]`）；`GB` `168`／`170`；`VR` `80`／`95` | ✅ |
| `5` | 生產碼 `34` 檔 | `d4c7807` 對 `HEAD` 逐檔 blob 相異 ＝ `0`；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `6` | 自限 | `verify/baselines`、`data/` 全樹、`CLAUDE.md`、`K-6`、`W-G.4` 域裁檔 blob 皆未動；`refs/heads/verify/*` ＝ `20` 支；`verify/out/` `895`→`901`（新增 ＝ 本批六落檔·消失 `0`） | ✅ |
| `7` | 落檔之 `CR` | `WG9311_runall.log` `161114` B·`CR` `1029`；`WG9311_landeffect.log` `86404` B·`CR` `692`；`.rc` ＝ `1`／`0` ⇒ **與 `W-G.9-311` `§零-2` 裁 `4` 之預告相符**（照實原封入倉·⛔ 重生） | ✅ |
| `8` | `run_all` 之名目 | 自 `WG9311_runall.log` 逐列復算（框 ＝ 子字串框 `✅ PASS`／`🔴 FAIL`·母體 ＝ 該落檔 `1029` 列）⇒ `24` ＋ `40` ＝ **`64`** | ✅ |
| `9` | `F.0` 之破 | 該落檔載 `🔴 GSA 錨檢破（0m）`；`【未被評估】錨鍵 ['G006', 'G007', 'G009', 'G010', 'G014', 'G017']`；六鍵之決策現況**逐鍵皆為** `[該情境無任何決策列]`；`F.1`／`F.2`／`F.3`／`F.4` 之「跳過」訊息各 `3` 列 | ✅ |
| `10` | `GB-170` 三格 | `WG9311_landeffect.log` 內 `116.08`／`308.17`／`226.01` 與對稱差 `13.055884224441684`／`38.06818855324997`／`13.05518976183967` 逐值命中 ⇒ 與 `GB-170` 節所載**相異 `0`** | ✅ |
| `11` | `.gitignore` | 其第 `30` 列逐字 ＝ `verify/out/got_*`（其上一列 ＝ `# ══ harness 重生成物（b6_report.md 除外，追蹤中） ══`）⇒ **`got_*` 族係倉之既有忽略政策** | ✅ |

### `§零-2`　裁（**`6` 項**）

1. **`W-G.9-311R` 之受領**：工項零、開工閘 `14`／`14`（資料列之當場重算 ＝ `14` ＝ 單自載）、工項一（`自誤 423`／`424`）、工項二（`D-1`〜`D-7`）、工項三、收工閘 `1`〜`8` ⇒ **全部受領**。**CC 之三自捕**（量測器標籤與受詞不符／`PASS` 之實際形為 `✅ PASS` 致「全紅」假象／收工閘判準沿用他批）⇒ **受領·⛔ 鑄號**；其攔法（判準⛔ 得沿用他批，須逐條自本單條文重新錄入）**正確**。

2. **`§五` 入倉對象之偏離：受領 CC 之處置**（`got_*` 族逐檔命中 `.gitignore:30` ⇒ ⛔ 入倉、逐檔具名）。**責在發單側** ⇒ 本單工項一鑄 `自誤 425`，逐字見 `§三`。

3. **本批之假說與其測法**（⛔ 讀為結論）：`F.0` 之 `GSA` 六鍵於 `W-G.9-311` 之跑中**逐鍵皆無決策列**，而 `run_all` 自身之提示逐字載成因候選「被併宗遭上游消費移出 → 標的宗回復單獨 → 改判『全達標·無須併』或該塊宗數 `< 2`」（其箭頭係該提示之原文符號·⛔ 方向性轉引；來源：`verify/out/WG9311_runall.log` 之 `【未被評估】` 列）；同批 `D-7` 實測碼側仍行步驟 0（`K-9-29 二` 已廢止之款）。⇒ **假說**：步驟 0 之前置合併先行消費了 `F.0` 級 `0`／`0'` 之合併對象。**測法** ＝ 以 `app.py` 既有之出口 `WV_K6_STEP0` 作二態對拍。🛑 **本裁⛔ 令 CC 判其真偽以外之事**；二態之值出艙即止。

4. **二態之定義（逐字·⛔ 增減）**：
   - **甲態（現態）** ＝ 執行環境內 `WV_K6_STEP0` **未設定**（⇒ `k6_step0_enabled()` 取預設 `"on"`）；
   - **乙態（`K-9-29 二` 所令之態）** ＝ 該變數設為 `off`，**僅於該次跑之子行程有效**（⛔ 寫入任何 `.env`／設定檔／shell profile／`os.environ` 之持久變更）。
   🔒 二態皆須**當場出艙該變數之實際值**（跑前、跑後各一次·以程式讀 `os.environ.get("WV_K6_STEP0")`），⛔ 以「已設／未設」之語充之。

5. **`run_all` 之紅⛔ 為本批之停機款**（承 `W-G.9-311` `§零-2` 裁 `5`）：二態皆**⛔ 期其全綠**；紅列之名目與其 traceback **全量出艙**即為交付物。**⛔ 因紅而重跑、⛔ 改任何閘、⛔ 改任何 baseline、⛔ `WV_BAKE`**。

6. **入倉對象之限定（`自誤 425` 攔法 ①）**：本批**得入倉者僅** `verify/out/WG9312_*`（`runall_on`／`runall_off`／`landeffect_on`／`landeffect_off` 之 `.log`／`.rc`／`.err`）。`verify/out/got_*` 與 `verify/out/W-D.4_*` 族**命中 `.gitignore` 之忽略政策 ⇒ ⛔ 入倉**，改以**逐檔 `sha256` ＋ bytes 出艙於報告**。🛑 入倉前逐檔以 `git check-ignore -v <路徑>` 查驗並出艙其輸出（命中者⛔ `git add`；`rc` 為 `1` 者即未被忽略）。

### `§零-3`　本單取號之宣告框（補款 `①`〜`⑨`·**六數分列**·母體 ＝ 全 `docs/`·態 `16244f1`）

**三軸**：`(1)` 來源 ＝ `blob@16244f1`／`(2)` 母體 ＝ 全 `docs/` **`814`** 檔（讀不到 `19`）／`(3)` 粒度框 ＝ 宣告框（`D1` 檔框 ＋ `D2`∪`D3` 列框·三形皆帶數字邊界 `(?!\d)`·`D3` 取補款 `⑧` 之嚴格式 `^` 起首·`D2` 取補款 `⑨` 之他指排除）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 依 `⑨` 排除之 `D2` 列 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-312`** | **`0`** | **`0`** | **`0`** | `0` | **`0`** | **`0`** | `0` | 受詢 ⇒ **未占用·可用** |
| `W-G.9-311` | `2` | `2` | `8` | `0` | `10` | `2` | `0` | 對照組 甲（須 `≥ 1`）✅ |
| `W-G.9-309` | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 併記：修批之保留號·**本單⛔ 占之** |
| 一人造號（其字樣**⛔ 入倉**·`GB-147`·以代稱行之） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 對照組 乙（須 ＝ `0`）✅ |

🛑 **CC 須自行重跑本表**（含二對照組；對照組乙之號形由 CC 自造·**其字樣⛔ 入倉**），⛔ 採信本表。

---

## `§一`　開工閘（**`15` 項**·資料列之數見本節末之機驗·**每列同格載其產生指令**·🛑 **跑於 `§二` 之後**）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | 出艙逐字；期 `3.13.11` 起首——不符 ⇒ **loud 具名、停** |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `16244f13ca269ace1ce3245736c536908bb97986` 或其後裔（後裔以 `merge-base --is-ancestor` 證·`rc` 緊接指令） |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；🔴 ＝ `0`；🟡 **逐項具名處置**（發單側出單前實跑之數見 `§八`） |
| `3'` | 受詞自證（以器所印之檔名列） | [必真] 含 `W-G.9-312` ＝ `True`／[必偽] 含 `W-G.9-309` ＝ `False` |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `414`／`424`（缺 `[106, 355, 356, 357]`）｜`K-9` `39`／`40`（缺 `[]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`） |
| `5` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py`（正面列舉） | `34` 檔；判別力[必不命中] ＝ `verify/` 子層之 `*.py` 須 `> 0`（⛔ 定值·發單側於 `16244f1` 得 `330`） |
| `6` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（🛑 `core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列 |
| `7` | `git ls-remote origin 'refs/heads/verify/*'` | `20` 列；`verify/W-G.9-299-gb170` ＝ `0`；`verify/W-G.9-269-p3a` ＝ `1` |
| `8` | 自誤簿之期初（`git cat-file -e` 後 `git rev-parse HEAD:<路徑>`·取 bytes） | `d0a59f6e8e24ed9b9fc4c7106e5534d78cda007f`·`898339` B（態 `16244f1`）；期初 bytes 存錄於記憶體 |
| `9` | 本批新物之未占（`git ls-tree -r --name-only -z HEAD`·取 bytes·**跑於 `§二` 之後**） | 檔名含 `W-G.9-312` 者 ＝ `1`；哨兵 `ZW425` 之全倉子字串框命中 ＝ **除本單自身之產物外 `0`**（`自誤 423` 攔法·逐筆具名每一命中之所在檔；有任一落於本單以外 ⇒ **停、上呈**）。發單側於 `16244f1` 得該哨兵全倉 `0` 檔 |
| `10` | 宣告框（`§零-3` 之六數·**CC 自跑**·含二對照組·`D3` 取嚴格式 `^` 起首） | `W-G.9-312` 之命中**除本單自身之產物外 ＝ `0`**；對照組甲 `W-G.9-311` 之 `D1` `≥ 1`；對照組乙 ＝ `0`——對照組任一不如期 ⇒ **器紅、停** |
| `11` | **落檔之前態快照**（`verify/out/` 全目錄 ＋ 工作區·逐檔 `sha256` ＋ bytes·落於 scratchpad·**⛔ 入倉**） | 倉內 `901` 檔；工作區另有未入倉者一律具名。🔑 本快照係閘 `12`／`13` 判「本次新產／重寫」之唯一依據 |
| `12` | 🔑 **甲態**：`WV_K6_STEP0` **未設定**之驗證（跑前印其值）⇒ `python verify/run_verification.py` 一次 ⇒ stdout 全量落於 `verify/out/WG9312_runall_on.log`、`rc` 落於 `.rc`（`open(p,'wb')`）⇒ 跑後再印其值 | 跑前、跑後皆 ＝ `None`（未設定）；**⛔ 期其全綠**；`run_all` 之名目數、`FAIL` 全量清單、任何 traceback 全文皆出艙。🔒 **與 `W-G.9-311` 之對照**：名目 `64`（`24` ＋ `40`）——**相異者照實具名、⛔ 重跑**（跑逾 `30` 分鐘未竟 ⇒ loud 具名、停、上呈） |
| `13` | 🔑 **乙態**：`WV_K6_STEP0=off` **僅於該次子行程**（例：`env WV_K6_STEP0=off python verify/run_verification.py`）⇒ stdout 全量落於 `verify/out/WG9312_runall_off.log`、`rc` 落於 `.rc`；跑後印 `os.environ.get("WV_K6_STEP0")` | 跑中之子行程 ＝ `off`；**跑後之父行程 ＝ `None`**（⛔ 污染甲態）；**⛔ 期其全綠**；出艙同閘 `12`。🛑 **⛔ 以本閘之結果回頭重跑甲態** |

🔒 **本表之資料列**：CC 須**當場以程式對本單自身計數**（框 ＝ 本表中以 `\| ` 起首之列，扣表頭 `1`），並與本單自載之 `15` **二值並列**出艙（`自誤 424` 攔法）；不符 ⇒ 以**列舉**為準、逐項皆跑，照實具名、續辦。

### `§一-1`　恆常附款（**逐項適用·⛔ 省**）

🛑 **`W-G.9-308` `§一-1`、`W-G.9-308_補令一` `§一-1`（款 `a`／`b`／`c`）、`W-G.9-310` `§一-1`（款 `d-1`〜`d-3`）、`W-G.9-311` `§一-1`（款 `e`／`f`）之恆常附款全數逐項適用**（逐字見該四單·⛔ 省）。
🆕 **本單所增（`g`·`自誤 425` 之攔法）**：**凡單令入倉之對象，出單前以 `.gitignore` 之逐列框（及 `CLAUDE.md` 之族定義）查其是否在忽略族**；命中者一律改寫為「**⛔ 入倉·出艙其 `sha256` 與 bytes**」，⛔ 令受單側入倉其所不能入倉之物。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 **先於開工閘**·單獨 `commit`·單獨 `push`）

落點 ＝ `docs/orders/W-G.9-312_施工單_步驟0二態對拍與自誤425.md`；**⛔ 改一字**。
🛑 **入倉前閘**：`git ls-tree -r --name-only -z HEAD docs/orders/` 之名（取 bytes）含 `W-G.9-312` 者須 ＝ `0`；不如期 ⇒ **停、⛔ 辦本單、上呈**。
🔒 整全性之判準 ＝ `SELF_SHA256`（`P-5`）；判別力二造（必綠 ＝ 原檔／必紅 ＝ 改一位元組之副本·**該副本⛔ 入倉**）須出艙。
入倉後出艙：blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源；`CR` ＝ `0`；`deletions` ＝ `0`。
🛑 **落地後即 `push`**，**其後方跑 `§一`**。

---

## `§三`　工項一：**`自誤 425` 之鑄**（主線·單獨 `commit`）

🔑 界：`<!-- ZW425-BEGIN -->`／`<!-- ZW425-END -->`。
落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（**嚴格末端追加**·⛔ 改上文一字）。
🔒 追加之式：讀期初 bytes ⇒ 於其末尾接 payload 之 bytes（含末尾之換行）⇒ `open(p,'wb')` 寫回 ⇒ 期末 bytes 之前綴須 ＝ 期初 bytes。
🩸 **定界須以<u>整列相等</u>為之**（`W-G.9-310R` `自捕 1`）。

<!-- ZW425-BEGIN -->

### 🩸 `自誤 425`　**單令入倉之對象（`verify/out/got_*`）係倉之既有忽略族，發單側指定前未查 `.gitignore` 與其族定義**

**形**：`W-G.9-311` 單 `§五` 令「受詞 ＝ 本批新產或重寫之落檔……`§一` 閘 `12` 所重寫之 `got_*` 檔（以閘 `11` 快照判定·逐檔具名其前後 `sha256`）」，並令「未產生者 ⇒ loud 具名、**⛔ 重生**」；而 `.gitignore` 第 `30` 列逐字為 `verify/out/got_*`，其上一列逐字為 `# ══ harness 重生成物（b6_report.md 除外，追蹤中） ══` ⇒ **該族係倉之既有忽略政策**，`git add` 之於該族**構造上不成立**（除非 `-f`，而單未授權亦不應授權）。

**徵候**：CC 於 `W-G.9-311R` 之工項三逐字回報——`run_all` 本次**重寫 `0` 檔、新產 `18` 檔**，`18` 檔逐檔命中 `.gitignore:30` ⇒ 取保守項 **⛔ 入倉**、逐檔具名於 `D-2`。發單側於新窗自倉復查 `.gitignore` 逐列，得同值。

**根因**：指定**入倉對象**前未查**倉之政策檔**（`.gitignore` 與 `CLAUDE.md` 之族定義）——`自誤 418`／`422` 攔法（單所指名物件之**性質**須自 blob 實查）之射程只及於「受詞之內容」，**未及「受詞可否入倉」此一政策性質** ⇒ 同根之第三形。併：單同節以「**重寫**」為入倉之判準，而該族於乾淨工作區**必為新產**（被忽略者⛔ 在倉）⇒ 判準與受詞之性質亦不相容。

**後果**：未動土地、未致錯裁、未致誤入倉。`18` 檔留於工作區、其 `sha256` 與 bytes 由 CC 逐檔出艙於報告。

**攔法（三款）**：
① 凡單令入倉之對象，出單前以 `.gitignore` 之逐列框（及 `CLAUDE.md` 之族定義）查其是否在忽略族；命中者改寫為「**⛔ 入倉·出艙其 `sha256` 與 bytes**」，並令受單側入倉前以 `git check-ignore -v` 逐檔查驗；
② 凡以「**重寫／新產**」為入倉判準者，須**同時預設二情形之處置**，⛔ 只寫其一；
③ 凡單指定倉內路徑為受詞者，併查該路徑之**追蹤狀態**（追蹤／忽略／未追蹤），⛔ 以「其目錄在倉」推定其檔可入倉。

<!-- ZW425-END -->

**落地後之機驗**：`probe_WG9267_issuer_measurers.py registry` ⇒ 自誤 相異 **`415`**／MAX **`425`**（缺號 `[106, 355, 356, 357]`）；`GB`／`VR`／`K-9` 一字未動；append-only：期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes；本批新增之 `.md` `CR` ＝ `0`。

---

## `§四`　工項二：**二態對拍**（🛑 **唯讀**·全部取自 `§一` 閘 `12`／`13` 之落檔·**⛔ 三跑**·**⛔ 改任何檔**）

🔑 **凡出艙之項，一律「甲態值／乙態值／差」三欄並列**；⛔ 只出一態。母體 ＝ 二態落檔之全部資料列（各欄同格載其列數與框）。凡遇可疑，照實出艙 ＋ 具名，**⛔ 判改法**。

| 項 | 受詞 | 出艙之形 |
|---|---|---|
| `E-1` | **`F.0` 之判**：二態落檔內名目含 `W-F F.0` 之列及其判；`GSA 錨檢破` 之有無與其全文 | 三欄並列 ＋ 逐字 |
| `E-2` | **`GSA` 六鍵之決策現況**：`['G006','G007','G009','G010','G014','G017']` 逐鍵於二態之「決策列」有無與其值（甲態之既知值 ＝ 逐鍵 `[該情境無任何決策列]`·母體 ＝ 二態落檔各自之全部資料列·甲態母體 ＝ 該態落檔之全部資料列 `1029` 列） | 六鍵 × 二態 之表；差欄具名 |
| `E-3` | **`F.1`〜`F.4` 是否執行**：二態落檔內「跳過」訊息之命中數；`got_F.0_*`〜`got_F.4_*` 逐檔於二態跑後之存否與 `sha256`（對閘 `11` 快照） | 逐級三欄並列（**⛔ 入倉該族·`§零-2` 裁 `6`**） |
| `E-4` | **七級調配是否被驅動**：`F.2`／`F.3`／`F.4` 之落檔資料列數；`F.3` 之轉 `7-4` 群數；`F.4` 之 ½ 輪與補償群；未產出者照實載「未產出」 | 三欄並列 ＋ 各欄之框 |
| `E-5` | **`run_all` 之總況**：二態之 `rc`、名目數、`PASS`／`FAIL` 數（框 ＝ 子字串框 `✅ PASS`／`🔴 FAIL`·⛔ 他形）、**`FAIL` 名目之差集**（甲∖乙、乙∖甲·逐列逐字） | 二數並報 ＋ 二差集全量 |
| `E-6` | **`GB-170` 三格之二態值**：甲態 ＝ `python verify/probes/probe_WG9287_landeffect.py` 一次（落 `WG9312_landeffect_on.log`）；乙態 ＝ 同指令加 `WV_K6_STEP0=off`（落 `WG9312_landeffect_off.log`）。取 `R4/left@0.0m`／`R2/left@3.5m`／`R4/left@3.5m` 之 `range_area` 與對稱差 | 六值 × 二態 之表；與倉載（`116.08`／`308.17`／`226.01`；`13.055884224441684`／`38.06818855324997`／`13.05518976183967`）三欄並列。器紅（`rc` ＝ `5`）⇒ 照實出艙、⛔ 改器 |
| `E-7` | **二態之環境證明**：四次跑（`run_all` ×2、探針 ×2）各自之跑前／跑後 `os.environ.get("WV_K6_STEP0")` | 八值逐字；父行程之末值須 ＝ `None` |
| `E-8` | **假說之判（⛔ 逾此）**：僅陳述「乙態之 `GSA` 六鍵**是否**出現決策列」與「`F.1`〜`F.4` **是否**執行」二事實 | 二句事實 ＋ 其出處列號。🛑 **⛔ 由此推任何改法、⛔ 判步驟 0 應否落地、⛔ 論土地後果** |

🛑 **落檔之 `CR` ⛔ 逕期 `0`**（承 `W-G.9-311` `§零-2` 裁 `4`·`自誤 422` 攔法）：照實出艙其數與成因，原封入倉、⛔ 重生。

---

## `§五`　工項三：**本批落檔之入倉**（單獨 `commit`）

**得入倉者僅**：`verify/out/WG9312_runall_on.log`／`.rc`／`.err`、`verify/out/WG9312_runall_off.log`／`.rc`／`.err`、`verify/out/WG9312_landeffect_on.log`／`.rc`／`.err`、`verify/out/WG9312_landeffect_off.log`／`.rc`／`.err`（存在者·**逐檔 `git add <路徑>`**·**⛔ `git add -A verify/out/`**）。
🛑 **入倉前逐檔 `git check-ignore -v <路徑>` 並出艙其輸出**；命中忽略族者 ⇒ **⛔ `git add`**、⛔ `-f`、改以 `sha256` ＋ bytes 出艙（`自誤 425` 攔法 ①）。
🛑 **`verify/out/got_*` 與 `verify/out/W-D.4_*` 族⛔ 入倉**——逐檔出艙 `sha256` ＋ bytes 於報告 `E-3`。
逐檔出艙 bytes／`sha256`／`CR` 數。未產生者 ⇒ loud 具名、**⛔ 重生**。⛔ 入倉：閘 `11` 之快照檔、任何暫存副本。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **判準逐條自本單條文重新錄入·⛔ 沿用他批**·`W-G.9-311R` `自捕 6`）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`**；同格載受檢檔數 `> 0`（防空集假綠） |
| `2` | 生產碼 `34` 檔（**正面列舉**·⛔ 空 `pathspec`） | blob 期初 ＝ 期末，相異 ＝ `0`；`app.py` ＝ `4379108a…`；同格載母體基數 ＝ `34` |
| `3` | 本批異動檔之 `CR`（倉內 blob·取 bytes） | `.md` ＝ `0`；`verify/out/` 之落檔 **⛔ 逕期 `0`**——照實出艙其數與成因 |
| `4` | 四簿期末（器之輸出） | 自誤 相異 **`415`**／MAX **`425`**（缺 `[106, 355, 356, 357]`）；`K-9` `39`／`40`·`GB` `168`／`170`·`VR` `80`／`95` **一字未動** |
| `5` | append-only（自誤簿） | 期末 bytes 之前綴 ＝ `§一` 閘 `8` 之期初 bytes |
| `6` | 自限復驗（**本批之真受詞**） | `verify/baselines` `298` 列·`a898f4e1…` **未動**；`data/` 全樹未動；`CLAUDE.md`／`K-6`／`W-G.4` 域裁檔 blob 未動；`refs/heads/verify/*` `20` 支·`gb170` ＝ `0`·`p3a` `is-ancestor` `rc` ＝ `1`；`WV_BAKE` ＝ `None`；**`WV_K6_STEP0` ＝ `None`**（父行程末態）。🔒 `verify/out/` **得增**（`§五` 正令其入倉）——逐檔具名新增者 |
| `7` | 標籤對照（`自誤 416`） | 本報告全部計數標籤，逐欄具名其指涉之集合與基數（**當場以程式自資料列計數**·⛔ 轉引單之自載數） |
| `8` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

🛑 **停機款**：閘 `1`／`2`／`5`／`6` 任一不如期 ⇒ **停、上呈**。閘 `3`／`4` 與期相異 ⇒ 照實具名、續辦。`run_all` 之紅⛔ 為本節之停機款。

---

## `§七`　🛑 **⛔ 為之事**

🔑 **本批已明文授權者**：本單原封入倉；`自誤 425` 之鑄；`run_verification.py` **跑二次**（甲態一次、乙態一次）；`probe_WG9287_landeffect.py` **跑二次**（同上）；**僅於該次子行程**設 `WV_K6_STEP0=off`；`§五` 所列之落檔入倉。
其餘：⛔ 改生產碼 `34` 檔**一字**；⛔ 改或新增任何探針**一字**；⛔ 第三次執行 `run_verification`；⛔ 持久設定任何環境變數、⛔ 寫入任何設定檔；⛔ 設定 `WV_BAKE`；⛔ 覆寫 `verify/baselines` 任一檔；⛔ 改任何 baseline 或期值以求綠；
⛔ `git add -f` 任何檔；⛔ 入倉 `got_*`／`W-D.4_*` 族；
⛔ 判「步驟 0 應否落地」、⛔ 判 `GB-170` 可否解除或收窄、⛔ 提任何修法主張、⛔ 擬任何 diff；⛔ 落地任何 `K-9` 子項；⛔ 鑄 `自誤 425` 以外之任何號；
⛔ 開任何分支；⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續或占用 `W-G.9-309`；
⛔ 追改任何已入倉文件一字；⛔ 呈 KL；⛔ 於器紅或停機款成就時自擴規則、自調框或重跑；
⛔ 估算或出艙本窗 context 之百分比——惟遇可觀測之硬限事件一律 loud 照實具名、停、上呈；⛔ 以 shell 命令列傳遞含反引號之文字；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格

`docs/reports/W-G.9-312R_步驟0二態對拍與自誤425_執行報告.md`：
`§二` 工項零 ⇒ `§一` 開工閘（逐閘出艙·含閘 `10` 之自跑與二對照、閘 `11` 快照之基數、閘 `12`／`13` 之全量與環境值）⇒ `§三` 工項一（機驗）⇒ `§四` 工項二（`E-1`〜`E-8` 逐項·**三欄並列**）⇒ `§五` 工項三（含 `check-ignore` 之逐檔輸出）⇒ 收工閘（入倉後末端追加·閘 `8` 出艙於回報）⇒ `⛔ 為之事` 之逐項自檢 ⇒ 逐 `commit`（全 `40` 碼）。
🔒 凡出艙之數，同格載其框、母體與產生指令；**計數標籤一律當場以程式自資料列計數**，⛔ 轉引單之自載數。
🔒 自捕照實併記（⛔ 鑄號）；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔑 **發單側出單前之實跑**：`probe_order_preflight.py <本單>` ⇒ 🔴 ＝ `0`（`P-5` 相符）；宣告框六數之量測式與 `§零-3` 同源；自誤框於暫存副本模擬工項一落地 ⇒ `415`／`425`。

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`sys.version`／`ls-remote`／`SELF_SHA256`／`preflight`／`deletions`／`merge-base`／`ZW425`／`自誤 425`／`WV_K6_STEP0`／`check-ignore`／`WG9312_runall_on.log`／`WG9312_runall_off.log`／`WG9312_landeffect_on.log`／`WG9312_landeffect_off.log`／`GSA`／`G006`／`G017`／`F.0`／`F.4`／`range_area`／`got_*`／`.gitignore`／`GB-170`／`W-G.9-309`／`16244f1`。

【單止】本單受詞 ＝ CC 施工窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: 517cac49b0e31ae3b77696e2521b2493db1c07f8e25cbd5a01eee8deaf3a6cb9
