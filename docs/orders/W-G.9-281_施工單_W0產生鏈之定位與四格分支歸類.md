# `W-G.9-281`　施工單：態乙 `R4 right` 之 `W_0` **產生鏈**定位 ／ 情形甲 ∧ `R(W_0) > 0` 四格之**分支**歸類

> **態**｜開工 ＝ `cb4fac535fe27d0c529149735625ac670d9f2a2c`（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·⛔ 採本單之數）
> **單** ＝ `docs/orders/W-G.9-281_施工單_W0產生鏈之定位與四格分支歸類.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（純量測·**零生產碼**·**⛔ 施任何替身**·**⛔ 反事實**）
> 🛑 **本批⛔ 鑄任何號**——`自誤`／`GB`／`VR`／`K-9`／坑 **皆⛔**（由 ＝ `§一`）。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批·⛔ 呈 KL）。

---

## `§零`　開工閘（八項·任一不符 ⇒ **停、照實具名、⛔ 續**）

| # | 受詞 | **期值**（發單側自倉實查·`cb4fac53…`） |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `cb4fac535fe27d0c529149735625ac670d9f2a2c` 或其後裔（🛑 ⛔ 以本地追蹤 ref 為證） |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（⛔ 併線·⛔ 刪除） |
| `4` | `SELF_SHA256`（`P-5` 口徑 ＝ 檔首至末列 `SELF_SHA256:` 前一 `\n` 含之全部位元組） | 見本檔末列·**由發單側供值**·⛔ CC 自擬 |
| `4'` | **閘器之受詞自證**（坑 `bf` 之攔法） | 印出所量之檔名；[必真] 含 `W-G.9-281` ＝ `True`；[必偽] 含 `W-G.9-280` ＝ `False` |
| `5` | `python verify/probes/probe_order_preflight.py <本單>` | `P-3`／`P-5` **不紅**；🟡 提示逐項處置（見 `零-1`·⛔ 靜默略過） |
| `6` | 四簿（器 ＝ `probe_WG9267_issuer_measurers.py registry`） | 自誤 `372`／`382`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29` |
| `6'` | 坑（器 ＝ `probe_WG9269_pit_index.py`） | **`85`／`85`**·下一標籤 `bh`（🛑 本批⛔ 鑄坑 ⇒ `ALPHA_LABELS` **⛔ 動一字**） |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（`app.py` ＋ `verify/` **頂層** `*.py` `33`）；判別力[必不命中] ＝ pathspec `verify/*.py` 所多咬之**子層**檔須 `> 0`（發單側實跑得 `298`·坑 `be`） |
| `8` | `verify/baselines` | `298` 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（🛑 **口徑 ＝ `core.quotePath` 預設**；同格併報 `quotePath=false` ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`·`自誤 379`） |

🔒 **閘 `4` 之判別力**：對**全檔**施同口徑 ⇒ 須相異；受詞多 `1` byte ⇒ 須相異 ⇒ 證其非恆綠。
🔒 **閘 `6`／`6'` 係本單所明令之閘** ⇒ 得驅動該二器；**⛔ `run_verification`**、⛔ 任何未經本單具名之長跑。

### 零-1　🟡 preflight 提示之處置（**⛔ 靜默略過**·逐項具名採納或豁免）

發單側已先跑本器於本單（框 ＝ 該器之 `P-1`／`P-2`／`P-4`／`P-6` 四款·母體 ＝ 本單單檔）：
🟡 提示 ＝ **`0`** 項；`P-3` 為 ℹ️ 記錄（`def main` 之 AST 區間·本單⛔ 引任何 `app.py` 之行號）。

🛑 **「提示 `0`」⛔ 得充「本器於本單非恆綠」之證**（⛔ 以「未觸」充「已通過」）⇒ CC 須**併跑**
`python verify/probes/probe_order_preflight.py --selftest` 並出艙其 `rc`（發單側實跑 ＝ `0`·三靶皆觸·反靶不觸）；
若其 `rc ≠ 0` ⇒ **判檢查器紅、停機上呈**，⛔ 以本單之「提示 `0`」逕充綠。

🔒 **併存之附款（不因提示 `0` 而免）**：凡本單所令之計數，其框若無 `^` 錨，一律依 `CLAUDE.md` 之
**二數並報**款出艙（**命中檔數**／**命中列數**二數並列），⛔ 只報其一。

---

## `§零′`　前批復驗與核可（**發單側已辦·⛔ CC 重辦**）

1. **`W-G.9-280` 補令一**：逐位元組復驗畢 ⇒ **✅ 核可**（封存於 `cb4fac5…`）。
2. **本窗開場之 `§二` 態錨重跑**：發單側於自倉**獨立重跑**十一受詞，與 CC 之回報**逐位相符** ⇒ **✅ 核可**。
3. 🩸 **`GB` 框之自捕：核可，並補其三分報**（框 ＝ 器 `probe_WG9267_issuer_measurers.py` 內逐字之
   `RE_GB_F1`（`^#{2,4} ` 之標題嚴框）／`RE_GB_F2`（`^\| *\*\*GB-N\*\*` 之表列框）／`RE_GB_F3`（`^#{1,6} ` 之標題寬框）；
   母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**·列框）。
   發單側實跑（【倉】`cb4fac53…`）：**`F1` 相異 `92`／`F2` 相異 `131`／`F3` 相異 `94`；三框聯集 `167`·`F2` 獨有 `73`；MAX 三框皆 `169`；缺號 `[12, 87]`**。
   ⇒ 🔒 **`167` 須三框之聯集**；單一標題框（無論嚴寬）**皆得不到 `167`**。**MAX ⇒ 取號⛔ 受影響。**
   🔴 **併記（⛔ 判孰誤·⛔ 鑄號·⛔ 追改任何已入倉文件一字）**：`docs/reports/W-G.9-280R_補令一_舊窗收工批.md` 之
   交接註項 `2` 將 `GB` 之框載為**單一**「標題錨定框」而繫以 `167`——該載述與本節之實跑**相異**。
   其歸屬（發單側自誤／`VR`／`GB`）**留待下一批依實測歸之**，本批**⛔ 鑄號、⛔ 逕改、⛔ 判**。

---

## `§一`　鑄號：**本批⛔ 鑄任何號**

| 簿 | 本批 |
|---|---|
| `自誤`／`GB`／`VR`／`K-9`／坑 | **⛔ 鑄**（次號 `383`／`170`／`96`／`30`／`bh` **一律留空**） |

**由**（發單側具名·⛔ CC 推定）：本批為**純量測**，其所可能坐實之二事（`§零′-3` 之框差、`§三-3` 之二鏈產生式之別）
**其歸屬須以本批之實測為據**；於實測**之前**鑄號，即 `自誤 380` 之形（以未經查證之機制假說取得常規地位）。
⇒ 一律**照實出艙**，歸屬與鑄號由**下一批**為之。
🔒 **本批⛔ 鑄坑之另一效力**：`ALPHA_LABELS` **⛔ 動一字** ⇒ ⛔ 生「器結構上看不見新坑而輸出仍全綠」之靜默漏。

---

## `§二`　工項零：**本單原封入倉**

落點 ＝ `docs/orders/W-G.9-281_施工單_W0產生鏈之定位與四格分支歸類.md`；**⛔ 改一字**（含行尾）。
須出艙：`(1)` **完整檔名逐字指定**下「所指之檔數 ＝ `1`」之自證（判別力：造甲 ＝ `W-G.9-280` 之單須 `1`／造乙 ＝ **執行期組出**之人造名須 `0`·字面⛔ 出艙）；
`(2)` `CR` ＝ `0`（併報 `LF` 數與 bytes；判別力 ＝ 一真 CRLF 人造檔須得 `CR > 0`）；
`(3)` blob `sha1` ＋ **blob 內容 `sha256` ＝ 磁碟 ＝ 源**之三方逐位相同。

---

## `§三`　工項一（**主檢**）：`W_0` 之**加性分解**與**產生鏈**之具名

### 三-1　射程（🛑 **⛔ 逾**）

- **通道 ＝ harness 路徑**（`verify/stepg_pipeline.py` 之 `run_step_g`）。🛑 **⛔ 由本項推論 `app.py` 生產路徑之任何事**
  （其由 ＝ 交接所載「`k956_W_from_mp` 之呼叫點全落 `main()` 內、harvest 從不呼叫 `main()`」；本批**⛔ 擴射程至 app 路徑**）。
- **二情境 × 二態**之驅動一律用**既有 harness 之旗標**（`WV_K6_STEP0`）；**⛔ 施任何替身、⛔ 改碼一字、⛔ 做反事實**。
- 🛑 **⛔ 判任何側是否違反 `K-9-5-12`、⛔ 判碼註為誤、⛔ 提修法主張、⛔ 解除或收窄 `GB-168`／`GB-169`。**

### 三-2　分解式（**準據由發單側明定·⛔ CC 自訂**）

母體 ＝ `6` 街廓 × `2` 側 × `2` 情境 × `2` 態 ＝ **`48`** 列之**鏈頭格**；其**有值**者（該側有鏈頭之記錄）
前批得 `22` 格——🛑 **本批須於現母體重量並出艙其數**（⛔ 轉引·通則「轉引計數須帶母體」）。

對每一有值格，一律取 `solve_G_binary` 之**參數本身**（`baseline_pt`／`side_mid`／`allocation_dir`／`d_hat` 之**實參**），
**⛔ climb 外層**（坑 `bg` 之攔法），依碼面之定義式重建並三分：

```
W_0 ≡ (baseline_pt − side_mid)·â              ← 碼面 `_W_near`（â ＝ 依 d̂·â ≥ 0 定號之 _n_alloc）
T_corner ≡ (corner_pt  − side_mid)·â          ← 項甲：角點殘差項
T_adv    ≡ (baseline_pt − corner_pt )·â       ← 項乙：沿推進向之位移項
恆等檢    ： W_0 − (T_corner + T_adv)          ← 須出艙其 repr 全位（⛔ 捨入後出艙）
```

出艙欄（逐格·**未捨入**·`repr` 全位）：`corner_pt`／`side_mid`／`d_hat`／`allocation_dir` 實參／`â`／`d̂·â`／
`baseline_pt`／`W_0`／`T_corner`／`T_adv`／恆等檢之差／`is_corner`／`is_chain_head`／該側之 `_fo`／`R(W_0)`。
🔒 `R(·)` 一律以 `ns["rw_from_width"]` 取值·**⛔ 器內另算**。

### 三-3　`baseline_pt` 之產生式：**逐呼叫端各自取回**（🛑 **⛔ 以一式概括**）

母體 ＝ `verify/stepg_pipeline.py` **單檔**內**一切**對 `_solve_one` 之呼叫端；框 ＝ 列框
`^\s*baseline_pt = `（有 `^` 錨）；出艙**命中檔數**／**命中列數**二數並報。
逐處出艙：`(a)` 其 `baseline_pt` 之**賦值式逐字**；`(b)` 該處所餵之 `_is_chain_head` 實參之**式逐字**；
`(c)` 該式中**每一符號**於本格之當場值（`repr` 全位）。

🔴 **⛔ 以「`corner_pt` ＋ `cum_S`」為既定式**——該括號係**發單側交接文**之措辭，**⛔ 本單之準據**。
若二呼叫端之賦值式**逐字相異**，一律**各自**出艙其全式與全符號值（含任何非 `corner_pt`／`cum_S` 之項），
**⛔ 以其一概括另一、⛔ 以「兩者實質相同」代替逐字**。
🛑 **本項為 `§六` 問二（受測物之產生分支窮舉）之直接落地**；若因採一式而於某一鏈**結構上取不到**其主項，
即再現 `自誤 376`／`381` 之根。

### 三-4　`R4 right` 二格之定位（`0m`／`3.5m` 態乙各一格）

逐格出艙（**⛔ 以「應為」代之**）：
1. `is_corner` 之實參真值 ⇒ 是否觸「街角第 1 宗換軸」之分支（其決定式之錨 ＝ `grep -n "_first_corner_alloc_dir" app.py`·框 ＝ 列框·二數並報）。
2. `is_chain_head` 之實參真值；`_fo_right` 之真值。
3. 所用 `allocation_dir` 之**實際來源**：`allocation_dir_block` 抑或 `_first_corner_alloc_dir(side_mid)` 之回傳
   ——**二者之值皆出艙**並標明**所採者**（座標系 ＝ 平面直角座標之單位向量·⛔ 時序）。
4. `T_corner`／`T_adv` 二項之值，及 `T_adv` 之式中各因子（含其來源之符號名）之當場值。
5. `W_0 = 0.1775004071` 與 `R(W_0) = 3.0885070835` 之**當場重得**（⛔ 轉引前批之數）。
6. 該格之 `W_0` 係落於 `K-9-5-12（四）`、`（五）1.`、抑或**二者皆不及**——🛑 **僅出艙其三項前提之真值**
   （是否 forced／是否街角勝者／是否鏈頭），**⛔ 由 CC 作歸屬之判**。

### 三-5　碼註**二支**之窮舉檢（`§六` 問二·**出艙即止**）

碼面 `K-9-5-6` 決定點段之註（錨 ＝ `grep -n "K-9-5-6 W 鏈之唯一決定點" app.py`·框 ＝ 列框·二數並報）載**二支**：
「鏈頭為街角勝者 ⇒ 其 `W_near ＝ 0`」與「鏈頭為無 WINNER 之第 2 宗 ⇒ 其 `W_near ＝ 抵費地遠側界`」。

| 造 | 受詞（機械） | 出艙 |
|---|---|---|
| `A` | 有值格中 `is_corner` 為真者，其 `W_0` **嚴格 `== 0`** 之格數／`!= 0` 之格數 | 二數並報 ＋ 逐格具名 |
| `B` | `is_chain_head` 真 ∧ `is_corner` 偽 ∧ 該側 `_fo` 偽 之格數 | 若 `> 0` ⇒ 逐格具名（＝ 碼註二支**所未涵**之第三支之候選） |

🛑 **出艙即止**：⛔ 判碼註為誤、⛔ 判其「窮舉不成立」、⛔ 提修法主張——**該判⛔ 在本批之授權內**。

### 三-6　判別力**三造**（🛑 **逐態各自成立**·通則「對照必命中性逐態成立」）

| 造 | 受詞 | 期 | 🛑 附款 |
|---|---|---|---|
| 甲[必逐位相符] | 還原值與碼面 `W_near` 之逐格對拍 | 全有值格**逐位相符** | **併報該閘之<u>執行次數</u>**（⛔ 以「未執行」充「已通過」）；分母須為**全**有值格 |
| 乙[必相異] | 以 `corner_pt` 取代 `baseline_pt` 之同式，施於 `R4 right` 二格 | 須得**相異**值 | 若相同 ⇒ `T_adv ≡ 0` ⇒ **判器紅、停、上呈**，⛔ 續推論 |
| 丙[必為零] | `T_adv` 於「`baseline_pt` 與 `corner_pt` 逐位相同」之格 | 須 `== 0` | **態甲／態乙各自成立**；某態之該子母體為空 ⇒ **loud 明載「該態無此格」**，⛔ 以空集充綠 |

🛑 三造**任一不成立** ⇒ 判量測器紅、**停機上呈**，⛔ 以其餘欄之綠充「器非紅」。

---

## `§四`　工項二：情形甲 ∧ `R(W_0) > 0` 之 `4` 格——**分支**歸類（🛑 **⛔ 以門檻切**）

受詞 ＝ 前批所具名之 `4` 格（`0m` 態甲 `R2 left`／`0m` 態乙 `R2 left`／`0m` 態乙 `R4 right`／`3.5m` 態乙 `R4 right`）
——🛑 **須於本批當場重得其成員與其數**（⛔ 轉引；其母體 ＝ `§三-2` 之同一有值格母體）。

**準據（發單側明定·⛔ CC 自訂·⛔ 以 `W_0` 或 `R(W_0)` 之量值門檻切——門檻本身即框）**：
以**產生分支**歸類，三支互斥且窮盡（零判一律用**具名常數** `TOL0`·⛔ 裸 `1e-12`·其值與判法須同格出艙）：

| 支 | 定義 |
|---|---|
| `P` | `T_adv` 判為零 ∧ `T_corner` 判為非零 |
| `Q` | `T_adv` 判為非零 ∧ `T_corner` 判為零 |
| `R` | 二項**皆**判為非零 |

逐格出艙 `(T_corner, T_adv, 支)`。**歸類之效力（發單側明定）** ＝ **分屬不同支者即分列**；
⇒ 若該 `4` 格落於**二支以上**，「應分列」即為**機械事實**、⛔ 由 CC 另作判斷。
🛑 若有格**不落於**上開三支（如二項皆判為零而 `W_0` 判為非零）⇒ **loud 停機上呈**，**⛔ 自行擴充分支定義**。

---

## `§五`　工項三：`K-9-5-12（五）3.` 之**倉內現態**（**逐字出艙·⛔ 判其效力**）

🔴 **本項之由（發單側具名·⛔ CC 推定）**：發單側交接文載該「未決後半款」為**待 KL**；
倉內是否另有記載，**由本項逐字出艙定之**。凡交接文與倉相斥，**以倉為準**。

母體 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` **單檔** ＋ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**；框 ＝ **列框**（有 `^` 錨者具名之，無者二數並報）。
錨一律取**字樣**·**⛔ 行號**（錨腐之戒）：

1. `grep -n "^### 🔒 K-9-5-12 " <K-6>` ⇒ 出艙該節 `（五）效力` 之 `1.`／`2.`／`3.` **三項逐字**，
   **併出艙其後之任何加註區塊逐字**（若有；框 ＝ 該節起至下一 `^### ` 前一列）。
2. `grep -n "^### 🔒 K-9-5-13 " <K-6>` ⇒ 出艙其**是否存在**（命中檔數／命中列數二數並報）；若存在，出艙其
   `（二）所答` 之**逐字**與 `（三）新裁之效力` 之**逐條逐字**。
3. `grep -n "失效加註" <K-6>`／`grep -n "指路加註" <K-6>` ⇒ 二框各自之命中檔數／命中列數二數並報；
   逐處出艙其**所指之節**與**所載之效力逐字**。
4. `grep -n "解除條件" <GB 登記表>` 之 `GB-169` 該節 ⇒ 出艙其**四款逐字**（尤其繫於「`（五）3.` 之未決後半款」者）。

🛑 **出艙即止**：⛔ 判該款是否已了、⛔ 解除或收窄 `GB-169`、⛔ 改 `GB-169` 原節一字、
⛔ 追改任何已入倉文件一字、⛔ 呈 KL、⛔ 提修法主張。**歸屬與後續由發單側於復驗後定之。**

---

## `§六`　收工閘（**量測與其所守護之動作分開呼叫**）

| # | 受詞 | 期 |
|---|---|---|
| `1` | `numstat` **刪除欄逐檔具名** | 本批⛔ 就地更正任何既有檔 ⇒ 刪除欄**全 `0`**（新建檔與 append-only 之追加皆 `+N／−0`） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間命中 `0` 行；**對照組**＝ 一含 `app.py` 之歷史區間須 `≥ 1` ⇒ 器非紅 |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 ＝ 一已知含 `CR` 之檔須得 `CR > 0` |
| `4` | 四簿期末 | **四簿相異與 MAX 皆⛔ 動**（自誤 `372`／`382`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`）⇒ 坐實 `§一` 之⛔ 鑄號 |
| `5` | 坑之期末 | **`85`／`85`**·下一標籤 `bh`；`ALPHA_LABELS` **一字未動** |
| `6` | 自限復驗 | **`34` 檔 blob 期初＝期末·相異 `0`**（判別力[必非零] ＝ 一含 `app.py` 之歷史區間須 `1`）；`baselines` `298` 檔·框 `sha256` `a898f4e1…`（口徑 ＝ `core.quotePath` 預設）；`p3a` **非祖先**；`WV_BAKE` **未設**；`K-9-29` 任何子項**⛔ 落地**；`GB-167`／`168`／`169` **⛔ 修** |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit`（⛔ 預寫·⛔ 以本地 ref 為證） |

---

## `§七`　🛑 **⛔ 為之事**（逐項於報告自檢）

⛔ 動生產碼 `34` 檔一字（含註解）；⛔ 改任何 `*_EXPECT`；⛔ 修 `GB-167`／`168`／`169`；
⛔ 解除、收窄或結清 `GB-168`／`GB-169`；⛔ 落地 `K-9-5-12`／`K-9-29` 之任何子項；⛔ 改 `ok_t`／`ok_f` 一字；
`GB-162` **三禁令維持**（⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`）；**⛔ 引任何自 baseline 取得之數**；
⛔ 施任何替身、⛔ 做任何反事實、⛔ 驅動任何未經本單具名之長跑（含 `run_verification`）；
⛔ 鑄任何號、⛔ 動 `ALPHA_LABELS` 一字；⛔ 追改任何已入倉文件一字；
⛔ 判任何側是否違反 `K-9-5-12`、⛔ 判碼註為誤、⛔ 判 `（五）3.` 之效力、⛔ 判孰誤、⛔ 提修法主張；
⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

---

## `§八`　報告之格（`docs/reports/W-G.9-281R_…md`）

`§零` 開工閘實測 → `§一`〜`§三` 逐工項（**逐格表以落檔全量出艙·⛔ 以 `tail -N` 取結果**）→
`§四` 判別力三造之判 → `§五` 一句話推導（驗收固定項）→ `§六` 收工閘之實測（**入倉後末端追加**）→
`§七` `⛔ 為之事`之逐項自檢 → `§八` 逐 `commit` 與 `push` 之 `ls-remote` 實查。

🔒 **凡出艙之數，同格載其框與母體**；凡框無 `^` 錨者，**二數並報**（命中檔數／命中列數）。
🔒 **凡本批之任何自捕（含量測器紅），一律於出艙前攔下並照實併記**（形／徵候／攔法），**⛔ 頂替、⛔ 鑄號**。

*本單受詞 ＝ 下一批之 CC 窗。⛔ 呈 KL。*

SELF_SHA256: b9ec1b8c887a54e170c95dc734d5b6cae9d34a874db83545bfba1f60e352d050
