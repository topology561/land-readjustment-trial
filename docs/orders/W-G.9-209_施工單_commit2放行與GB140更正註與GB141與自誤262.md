# 🧾 `W-G.9-209` 施工單　`commit 2` 之放行 ＋ `GB-140` 更正註 ＋ `GB-141` ＋ `自誤 262`

> **基座**：`wip/s1-endpart` ＝ **`be1b326a9526c704a37a495474b04aa0694cb1e0`**
> **側分支**：`verify/W-G.9-198R-c2` ＝ `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b`（父 ＝ `c746d2dd…` ⇒ **⛔ 能快轉**·須 rebase）
> **性質**：**三段**。`§一` 🔴 含生產碼（rebase ＋ 側分支重推 ＋ **停機**）；`§二` 主線推進（**觸發權在發單側**）；`§三` 🟢 零生產碼登記批（**CC 逕推**·須於 `§二` 完成後辦）。
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔒 **KL 已放行（`2026-09-02`·逐字）**

> 「是 放行 `commit 2` 進主線」

🛑 **放行之受詞 ＝ 發單側已自側分支逐位元組復驗之<u>內容</u>**（二檔／`numstat` 二值／二 blob），**⛔ 是某一個 SHA**。

---

## §零　開工閘

| 閘 | 判準 |
|---|---|
| `S-0a` | `ls-remote` `wip/s1-endpart` ＝ `be1b326a9526c704a37a495474b04aa0694cb1e0`；其五檔 blob ＝ `app.py` `6f0317e47ef92c7e06d6d904ecf2d129656bd331`／`verify/selection_pipeline.py` `10f90c509ae13c77c83e3e5c76fe3c09ca675b14`／`verify/run_verification.py` `d07955c58e67ec06260971a74b5aabe5e846f106`／`verify/stepg_pipeline.py` `fef5865a73033274697790e0e307aedbf6122422`／`verify/run_all.py` `e5cf9a224517533b183b34e4fd0f5fbe35ee406b` |
| `S-0b` | `ls-remote` `verify/W-G.9-198R-c2` ＝ `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b` |
| `S-0c` | 母體 追蹤檔 **`1583`**／`.md` **`516`**；取號 `GB` 相異 `54`／MAX `140`、自誤 `226`／`261`、`VR` `74`／`89` |
| `S-0d` | 本單 `SELF_SHA256` 自驗相符；`probe_order_preflight.py` 之 🟡 逐項以**字樣錨**具名並依裁 `H` 歸類 |

🔒 **行尾（`GB-139`）**：`clone` 用 `-c core.autocrlf=false -c core.quotepath=false`；`add` 既有檔一律常規 `add`；閘之受詞一律取**倉側** `git cat-file blob`。

🔒 **`S-0d` 之已知 🟡（發單側已對本檔實跑·母體 ＝ 本單單檔·範圍 ＝ `P-1`〜`P-6`）**：🔴 `0`【項】／🟡 **`1`**【項】。

| 檢 | 字樣錨（`grep -F` 固定字串·⛔ 用行號） | 歸類（裁 `H`）與處置 |
|---|---|---|
| `P-4` | `` | 閘 | 判準 | ``（`§三-4` 之表頭） | **判別力參照 ⇒ 具名豁免**：係驗收表之索引表頭，各閘之母體／框／方向逐列具名於同表「判準」欄 |

🛑 上表以外之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
🔑 **判別力自證（⛔ 另造）**：同器對 `-203` ＝ `0`／`-205` ＝ `1`／`-206` ＝ `3`／`-207` ＝ `0`／`-208` ＝ `1`／`-198R` ＝ `5`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。
🔒 **本機分支之處置（`自誤 261` 之一般式·⛔ 省）**：開工前先報 `git rev-parse HEAD` 與 `git symbolic-ref -q HEAD`；若本機 `wip/s1-endpart` 正停在 `0d88e1a5` 上，須先以**本地 ref ＋ 遠端側分支**二重保全，再退回 `be1b326`，逐項復驗後方進 `§一`。

---

## §一　🔴 rebase ＋ 側分支重推 ＋ **停機**

1. 將 `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b` **rebase 至當時之主線頭**，得 `0d88e1a5′`。
   🛑 **⛔ 動 payload 一字**——⛔ `amend` 訊息、⛔ 併入任何新變更、⛔ 順手收斂 `GB-140`／`GB-141`（二者係 `§三` 之登記項，**⛔ 生產碼**）。
2. 🔴 **等值判準（四項全須成立·任一不符 ⇒ 停機上呈，⛔ 自行歸因）**

| # | 判準 |
|---|---|
| `E-1` | `0d88e1a5′` 之父 ＝ `S-0a` 所實查之主線頭全 `40` 碼 |
| `E-2` | `git show --name-only --format=` **恰二檔** ＝ `app.py`／`verify/selection_pipeline.py`（🛑 **⛔ 含 `verify/run_verification.py`**——`commit 2` 未觸該檔） |
| `E-3` | `numstat` ＝ `app.py 219/11`／`verify/selection_pipeline.py 9/0` |
| `E-4` | 二檔之 blob 逐檔與 `0d88e1a5` 相同（**全 `40` 碼**·⛔ 短碼）：`app.py` ＝ `144349c834c2289434e1b0edbcfc8d0a27d88435`／`verify/selection_pipeline.py` ＝ `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c` |

3. 🔒 **判別力自證（⛔ 省·與 `E-4` 方向相反）**：取**未參與本 commit** 之一檔——建議 `docs/reports/W-G.4_泛用阻塞項登記表.md`——報其於 `0d88e1a5` 與 `0d88e1a5′` 之 blob，**二者應相異**，且後者**恰 ＝ 主線頭之同檔 blob**。若相同 ⇒ rebase 未生效 ⇒ **停機**。
   併報 commit 訊息之**位元組數與逐位相同與否**。
4. `git push --force-with-lease=refs/heads/verify/W-G.9-198R-c2:0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b origin 0d88e1a5′:refs/heads/verify/W-G.9-198R-c2`
5. 復驗主線**⛔ 前進**（獨立量測·期望值取 `S-0a` 所實查之值，⛔ 寫死舊常數——承 `W-G.9-208` 之量測器紅）。
6. 🔴 **停機。⛔ 執行 `§二`／`§三`。** 回報 `E-1`〜`E-4` ＋ 判別力自證 ＋ 二 ref 之 `ls-remote`。

---

## §二　主線推進（🛑 **⛔ 得逕行**）

須待發單側逐字回覆「**`0d88e1a5′` 等值已驗·准推主線**」後方得執行：

```
git push origin 0d88e1a5′:refs/heads/wip/s1-endpart
```

推後回報：① `ls-remote` 之 `wip/s1-endpart` 全 `40` 碼 ＋ 量測時刻；② `git rev-parse <新主線頭>:app.py` ＝ `144349c834c2289434e1b0edbcfc8d0a27d88435`；③ `left-right` ＝ `0 0`；④ 側分支 `verify/W-G.9-198R-c2` **保留⛔ 刪**。

🔴 **此推之土地意義**：本波第一次**分配結果**改變——`R4` 之 `628(1)`＋`628-1(1)` 合併歸右街角，左街角成為**強制抵費地**（退縮 `3.5` m ＝ `226.01` ㎡／退縮 `0` m ＝ `116.08` ㎡）。

---

## §三　🟢 零生產碼登記批（**CC 逕推**·🛑 須於 `§二` 完成後辦）

🛑 **三項 payload 皆為 append-only 之檔尾追加**，⛔ 就地改任何既有一字。
🔒 **取號之預期**：`GB` 相異 `54`→**`55`**、MAX `140`→**`141`**（🛑 `GB-140` 之更正註**⛔ 使二數改變**——其標題含既有之 `140`）；自誤 相異 `226`→**`227`**、MAX `261`→**`262`**。

### 三-1　`GB-140` 更正註（追加於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔尾**）

```markdown
### `GB-140` 更正註（`W-G.9-209`·`2026-09-02`）　第三處已現，且**已有土地後果**

`GB-140` 前文逐字載「**現況之土地後果 ＝ 無**」——該句**未標時態**，於態 `b772f8a` 為真，
而 `W-G.9-198R commit 2` 落地後**即為偽**。更正如下（原句依 append-only ⛔ 就地改·已鑄 `自誤 262`）：

**第三處（新增·`K-9-24 二·2` 之並列判定）**
`app.py` 之歸側四分支逐字 `_to = _P1 if round(_idx_of(_cm[_P1]), 4) >= round(_idx_of(_cm[_P2]), 4) else _P2`
⇒ **以 `4dp` 判「並列」**。而正典 `K-9-24 二·2` 逐字為「歸指數數值**較大**之側，**並列**取前緣線 `p1` 側」。
🔴 **若二側之指數差落在小數第 5 位**，正典判「較大」（歸該側），而本實作判「並列」（歸 `p1` 側）
⇒ **街角歸屬因一個 `round` 而異** ⇒ **有土地後果**。

**本案未觸**　`R4` 之左右指數 ＝ `0.991558` vs `0.992472`，差在小數第 3 位 ⇒ 任何 `≥3dp` 之捨入皆歸右。
🛑 **⛔ 得以「本案未觸」推論「他案未觸」**——泛化母體為任意街廓幾何。

**三處之匯總（態 ＝ `commit 2` 落地後）**
| 處 | 位置 | 精度 | 土地後果 |
|---|---|---|---|
| 一 | 段一：名次鍵 ∧ `qualified.sort` 平手鍵（同源） | `4dp` | 有（現行分配即依之） |
| 二 | 段二：鍵③ 出艙值即排序鍵 | `6dp` | 無（段三未落地·⛔ 有消費端） |
| 三 | 歸側四分支之並列判定 | `4dp` | 🔴 **有**（決定街角歸哪一側） |

**收斂之時點更正**　原載「段三落地前須收斂」⇒ **改為：處三須於<u>泛化波</u>前收斂，處二仍繫段三**。
🛑 三種收法（統一 `4dp`／統一 `6dp`／出艙與排序鍵分離用全精度）之土地後果相異
⇒ 仍屬**須先量測後裁**之項，⛔ 得逕改；**須上呈 KL**。
```

### 三-2　鑄 `GB-141`（追加於同檔**檔尾**·置於 `三-1` 之後）

```markdown
### `GB-141` 🆕　同街廓有二筆以上跨占雙街角者，歸側之處理**順序相依**

**實測（態 ＝ `commit 2`·母體 ＝ `app.py` 單檔·框 ＝ 列）**
歸側四分支逐字 `_dual = sorted(set(_by[_P1]) & set(_by[_P2]))`，其後 `for _m in _dual:` 逐筆處理；
而每筆之款⑤「另一側扣除 `M` 後重新評選」所吃之候選集 ＝ `_grp(adjusted[_other])`
——**即前一筆處理後之結果**。

**性質**　`_dual` 之次序係暫編地號字典序（**決定性·⛔ 隨機**），惟
🔴 **當 `|_dual| ≥ 2` 時，處理次序會影響各筆之「另一側是否仍有 winner」** ⇒ 影響是否成立強制抵費地
⇒ **有土地後果**。正典 `K-9-24 二` **⛔ 載** `|_dual| ≥ 2` 之處理次序。

**本案未觸**　`R4` 之 `_dual` 恰 `1` 筆（`628(1)+`）；全案其餘 `5` 街廓之 `_dual` 皆為空。
🛑 **⛔ 得以「本案 `|_dual| = 1`」推論本項不存在**——泛化母體為任意街廓幾何與任意地籍。

**失效條件**　正典補充 `|_dual| ≥ 2` 之次序規則（**須 KL 域裁**），且實作依之。
**現況處置** ＝ 只登記、⛔ 零生產碼；列為**泛化波**之前置，與 `GB-140` 處三同批上呈 KL。
**⛔ 綁本案**　⛔ 綁 `R4`、⛔ 綁街廓名、⛔ 綁 `SB` 值。
```

### 三-3　鑄 `自誤 262`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

```markdown
## 自誤 `262`　對「土地後果 ＝ 無」之斷言**未標時態**，而同單所授權之 commit 即使其成偽

`W-G.9-208 §一-3` 所鑄之 `GB-140`，其量測段已標時態（「實測（態 `b772f8a`…）」），
惟結論段逐字「**現況之土地後果 ＝ 無**」**⛔ 標時態**。
而**同一張單之 `§三` 所授權之 `commit 2`**，其歸側四分支引入了第三處 `round(..., 4)`
——該處**決定街角歸哪一側** ⇒ 「土地後果 ＝ 無」於 `commit 2` 落地之刻即為偽。
🩸 **發單側於起草時已知 `§三` 將動歸側之比較邏輯**（`補二-6` 即為此而立「⛔ 得順手收斂 `GB-140`」），
**卻未預見該 commit 會<u>新增</u>一處同族之精度選擇**——只防了「改既有的」，未防「長出新的」。

**一般式**
> **凡出艙「⛔ 有 X」「X ＝ 無」之現況斷言，須同格標時態（態之 commit 全 `40` 碼）**；
> **且若同單另有授權之變更，須逐項自問「該變更會否<u>新增</u>一個本斷言之受詞」**——
> ⛔ 得只檢查「該變更是否修改既有受詞」。**禁止既有之改動 ≠ 禁止新受詞之產生。**

**族** ＝ 與 `自誤 254`（出艙倉態之數未標時態）同族之**第二度**；其新肢 ＝ **受詞集之增長未被納入射程**。
**攔截者** ＝ 發單側（`commit 2` 之側分支逐位復驗時自捕·CC 未報）。
**土地後果** ＝ 無（本案 `R4` 之指數差在小數第 3 位·任何 `≥3dp` 之捨入同解）。
```

### 三-4　`§三` 之驗收

| 閘 | 判準 |
|---|---|
| `T-1` | 三追加之 **append-only 位元組級自證**（受詞取**倉側** blob）：前 `N` B ≡ 基座同檔全文；🆕 依 `戒 40`，`Δ` 與 payload **二端**位元組數同格並報 |
| `T-2` | 🔴 **`GB` 之取號自證**：相異 `54`→**`55`**、MAX `140`→**`141`**。🛑 `三-1` 之更正註標題含 `GB-140` ⇒ **⛔ 使二數再增**；若實得相異 `56` ⇒ 判紅（代表更正註被誤鑄為新號） |
| `T-3` | 自誤 相異 `226`→**`227`**、MAX `261`→**`262`** |
| `T-4` | 🔴 **生產碼四檔 ＋ `selection_pipeline.py` 五檔 blob ⛔ 動**（逐檔與 `§二` 推畢後之主線相同——🛑 `app.py` 應為 `144349c8…`、`selection_pipeline.py` 應為 `f878276b…`） |
| `T-5` | 追蹤檔 `1583`→**`1585`**；`.md` `516`→**`518`**（＝ 本單 ＋ 執行報告） |
| `T-6` | 🔴 **判別力自證**：`GB-140` 前文之逐字「**現況之土地後果 ＝ 無**」於落地後之 `HEAD` **仍命中 `1`**（⛔ 得消失——證係 append 而非就地改）；且新增之字樣錨 `` `GB-140` 更正註（`W-G.9-209` `` 命中 `1` ⇒ **二者並存** |

✅ 全綠 ⇒ **逕推**（零生產碼）。

---

## §四　其後與停機條件

1. 🔒 **`commit 3`（段一上鎖）之開工條件**：`§二`／`§三` 皆完成後方得開工，依 `-198R §五`（`R3-1`〜`R3-6`／`D3-a`〜`D3-e`），並適用 `-208 §二` 補款二之錨換代（`app.py` 判準值改為 `144349c8…`、`S-0a②` 之基座改為 `§二` 推畢後之主線頭）。走側分支 `verify/W-G.9-198R-c3`、備妥後停機。
   🛑 **`D3-a` 之基準 ＝ `commit 2` 落地後之態**；改前基線須**第三度重做**，檔名帶該態之短碼。
2. 🔴 **`抵費地0m(應空)` 之凍存期望 ⇒ 判：本批⛔ 改。** 該閘由 ✅ 翻 🔴 係 `K-9-24 二·5` 所令之正確結果；凍存名單之重產繫 `GB-111`（KL 已裁「俟二閘落地後」），且 `-198R §八` 明令本批⛔ 辦 ⇒ **登記為已知紅、繫 `GB-111`**，⛔ 上呈 KL、⛔ 逕改。
3. **停機條件**：`E-1`〜`E-4` 任一不符／判別力自證顯示 rebase 未生效／`S-0a` 五檔任一 blob 已變／`§一-5` 發現主線已前進 ⇒ 皆**停機上呈**。
4. 🔒 **次號（本批落地後）**：自誤 `263`／`GB 142`／`VR 090`／戒 `41`／`K-9 27`。
5. 🔒 **攢批**：`GB` 定義列式之正典化（`GB-138` 處置③）；`GB-140` 處三與 `GB-141` **須同批上呈 KL**（泛化波前置·本單⛔ 辦）。
SELF_SHA256: 1fea1d8b69f8f6ec598f50efc0f9e3fdb6b84e599aa9c96fe8cd015627ad8eaa
