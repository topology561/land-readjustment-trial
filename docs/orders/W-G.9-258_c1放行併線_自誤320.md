# W-G.9-258　`c1` 之 KL 放行與併線 ＋ 自誤 `320`

> **受詞 ＝ CC。** 發單側 `claude.ai`·`2026-09-08`。承 `W-G.9-256`（主線 `a5b4edd`）／`c1` 側分支 `origin/verify/W-G.9-255-c1` ＝ `3242e96`。
> 🔒 **KL 放行（`2026-09-08`）**：「**`c1` 放行**」——KL 所放行者係下列**一句實務語言**，⛔ 該段程式碼（`-252` 交接文 `§七` 之形）：
>
> > 「`c1` 這一批，在本案不動任何一宗地的面積。同一區段內的合併，折算係數恰為 `1`，面積逐位還原、一個位元都不差（窮舉全部宗地 × 全部區段，反例 `0`；並用錯誤寫法當反靶，證這個檢查真的抓得到東西）。跨區段的折算路徑已經打開、由三組人造測資證明算得對——**但那條路在本案的真實資料裡還沒被走到過**。」
>
> 🛑 **`c2` 依 `-256 §六` 仍⛔ 開工**；`-257`（`GB-67` 定位波）**移交下一 `claude.ai` 窗**，本單⛔ 及。

---

## §零　開工閘

1. `git fetch`；主線錨 ＝ **`origin/wip/s1-endpart @ a5b4edd`**；側分支錨 ＝ **`origin/verify/W-G.9-255-c1 @ 3242e96`**。任一不符 ⇒ 停機。
2. 號占用（`①`–`⑦`）：發單側於 `a5b4edd` 實測 `W-G.9-258` ＝ `0`／`0`／`0`／雙屬 `0`；對照甲 `-256` ＝ `D1 2`／`D2 3`／`D3 0`／檔框 `3`。CC 復現。
3. 次號當場復算：發單側實測 自誤 `MAX 319`、缺號 `[106]`、恆等式 `True` ⇒ 鑄 **`320`**。`GB MAX 153`／`K-9 MAX 29` ⇒ **本單⛔ 鑄 `GB`、⛔ 鑄 `K-9`**。
4. `probe_order_preflight.py`：`P-3`／`P-5` 綠。
5. worktree 先切 **wip 態**（`GB-152`）。

---

## §一　工項零：本單原封入倉 `docs/orders/`。`P-5` 對拍。

---

## §二　工項一：自誤 `320`（**責 ＝ 發單側**·append-only·標題形取簿最末一則為範本）

**`320`　`V-G1` 之母體：單載「雙情境」而發單側自證只讀單一情境，出艙之 `114` 組為誤（`293`／`295`／`313`／`314` 同族·第五度）**
受詞：`W-G.9-256 §三 V-G1` 逐字載「**母體**：`verify/baselines/wf/f0/F.0_G值_退縮{0m,3.5m}.csv` 之 `a 面積(㎡)` **全欄**」，並於同款出艙「發單側已先證：母體 `57` 宗 × `2` 區段 ＝ **`114` 組**」。
發單側於 `a5b4edd` 覆核：`F.0_G值_退縮0m.csv` ＝ `54` 列、`退縮3.5m.csv` ＝ `57` 列 ⇒ **`57` 係只讀 `3.5m` 單檔之列數**，與單所載之雙情境母體不符。
加重：
- 發單側於同一款明令 CC「⛔ 採信此數」，卻仍將一個**與自書框不符**之數置於「已先證」之位；若 CC 採信，該款即成假綠。
- 本族（載框名 ≠ 框實作）於本窗已為第五度（`313` 檔框逕取 `D1`／`314` 列框私改聯集），且交接文 `§二 四` 已明列為高發區。
- **攔截者 ＝ CC**——其照單取雙情境得 `60`、**照實出艙相異且明書「未採信單之數，此差待發單側覆核」**。此為本波第二次由 CC 攔下發單側之框錯（前次為 `-249R` 六項 `BLOCKED`）。
通則：凡於單中並載「母體之定義」與「發單側已先證之數」，**二者須由同一次執行產出**；⛔ 以另一次（縮減）之執行結果充當該母體之證。
🔒 **⛔ 追改 `-256` 一字**（`常規四（九）五`）；`V-G1` 之判**不因母體口徑而動**——發單側以 CC 之 `60×2 = 120` 組復算，得**括號式反例 `0`／左結合反靶反例 `9`**，與單載逐字相符。

🩸 **併記（⛔ 鑄新號·CC 須辦）**：CC 之 `60` 與發單側覆核所得之「雙情境聯集**相異**值 `47`」亦不同 ⇒ **去重口徑未具名**。令 CC 於本批報告**具名其 `60` 之口徑**（是否去重／是否含 `a = 0.0` 之宗／是否跨情境合併）。⛔ 因此重跑 `V-G1`——三種口徑之反例數皆為 `0`／`9`。

---

## §三　工項二：🔴 **`c1` 併線**（KL 已放行）

a. 將 `origin/verify/W-G.9-255-c1` ＝ `3242e96` 併入 `wip/s1-endpart`。**併法**：`merge --no-ff` 或 `cherry-pick`，**擇一並於報告具名**；⛔ `rebase` 主線既有 commit。
b. **側分支保留⛔ 刪**（既例：遠端 `verify/*` 十一支皆未刪）。
c. commit message 須載：`SELF_SHA256` 之單號 `-258`、KL 放行日 `2026-09-08`、及 `§零` 所引之放行句首句。

**併線後之期望態**（發單側於 `3242e96` 實測·CC 須復現）：

| 檔 | 併線前（`a5b4edd`） | 併線後（期望） |
|---|---|---|
| `verify/wf_f0.py` | `6758ea766b001b95` | **`b0722355c1978635`** |
| `app.py` | `4860f10828fb8ae9` | **`4860f10828fb8ae9`**（不變） |
| 其餘九檔＋夾具 | 見 `§五` | **全部不變** |

---

## §四　工項三：併線後之復閘（**⛔ 只跑一次**·四閘全在併線後之主線態）

| 閘 | 判準 | 停機款 |
|---|---|---|
| `V-G1` | 括號式反例 `0`；左結合反靶反例 `9`（含 `947.97/b` `Δ=-1.1368683772161603e-13`、`245.0/a` `Δ=-2.842170943040401e-14`）。**口徑須與工項一之具名一致** | 🔴 |
| `V-G2` | 併線後之 `wf_f0.py` 中 `a_m * (p_of(m) / p_t)` 命中 `1`；左結合形（`a_m * p_of(m) / p_t`／`(a_m * p_of(m)) / p_t`）命中 `0` | 🔴 |
| `V-G3` | `停機#2` 命中 `0`／`停機#3` `1`／`zones` 蒐集 `1`／`_key` `1`／`py_compile` rc `0` | 🔴 |
| `V-G′` | 三組判別力 ＋ 反靶（於併線後之主線態重跑·⛔ 引用側分支之舊結果） | 🔴 |
| `V-A′` | 十一檔中**僅** `verify/wf_f0.py` 相異；`app.py` ＝ `4860f10828fb8ae9` | 🔴 |
| `V-B` | append-only：自誤簿 deletions ＝ `0` | 🔴 |
| `V-C` | 自誤 `MAX 319→320`、缺號仍 `[106]`、恆等式 `True`；`GB MAX` 仍 `153`、`K-9 MAX` 仍 `29` | 🔴 |

🛑 **任一紅 ⇒ 停機上呈，⛔ 回滾主線**（併線已由 KL 放行；紅表示併線動作本身有瑕，須具名而非撤銷）。

---

## §五　產出與放行

| 批 | 內容 | 放行 |
|---|---|---|
| 工項零〜一 | 入倉 ＋ 自誤 `320` | 零生產碼 ⇒ 可與工項二同批推 |
| 工項二〜三 | `c1` 併線 ＋ 復閘 ＋ 報告 `W-G.9-258R.md` | **KL 已放行 ⇒ 直推主線**，⛔ 再候 |

---

## §六　本單⛔ 及者（明列·防射程蔓延）

- **`c2`**（廢步驟 0）：`-256 §六` 明令⛔ 開工，其 `V-H` 由 `GB-153` 判為不可得。**本單⛔ 解除該令。**
- **`-257`**（`GB-67` 定位波）：已出單、`SELF_SHA256: 0394745232cb7e7c…`、已入倉 `docs/orders/`；**移交下一 `claude.ai` 窗承接**。CC 得依 `-257` 原單續辦，其結果**呈新窗**，⛔ 呈本窗。
- **`-253` 段乙**（`-251` `c1`／`c2`·純 UI）：原令全效、可並行、`V-K` 候 KL 目檢。
- ⛔ 動 `K-9-27`／`K-9-28`／段一上鎖／段二鍵序；⛔ 重烤任何 baseline（`GB-16`）。

---

*受詞 ＝ CC。KL 已放行 ⇒ 併線後直推主線。`-257` 之結果呈新窗。*

SELF_SHA256: 8d5cc44db0bceb715db92edcb5dc51ef6ca2a0748f94e5b0a79f4ec8d86dd12a
