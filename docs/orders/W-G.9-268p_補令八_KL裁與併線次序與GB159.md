# W-G.9-268′　補令八（KL 之裁之登記 ／ 併線次序之更正 ／ `GB-159` 中間態之界）

> **受詞** ＝ `W-G.9-268′` 之施工窗。
> **主線期初態** ＝ `683b7fa49e4e20f53593c363b7d7050c91218d20`｜**側分支** `verify/W-G.9-268p-c2` ＝ `2a1da8dc0680248ec0b4806ce168b93ff54df350`（皆須 `ls-remote` 實查）。
> 🔒 本補令**⛔ 改**原單之受詞、方法、`§二` 工項切分、`§三` 十閘一字。所改者限於 `§五`／`§六`／`§七` 之**次序**與鑄號授權。
> 🔒 原單、補令一〜七、本補令**並存**；相斥處以**時序最後者**為準。

---

## §一　KL 之裁（`2026-09-10`·逐字登記）

> **「先併線、暫時容許配地表上出現 `0.23` 與 `0.00`」**

**受詞**：補令七 `§二` 所呈之二宗——
`628-42(1)`（歸戶 `G011`·`0m·R2·left` 第2宗）：`G` `3.82 → 0.23 ㎡`、宗地寬度 `0.76 → 0.18 m`；
`628-53(2)`（歸戶 `G025`·`3.5m·R5·left` 第2宗）：`G` `0.99 → 0.00 ㎡`、宗地寬度 `0.39 → 0.02 m`。
二宗之重劃前面積（`8.34`／`2.78 ㎡`）**未變**；二宗於改前改後**皆**帶 `畸零地旗標 ＝ ⚠️移出/第1調配順位`。

🔒 **本裁之射程（發單側所界·⛔ 擴張）**
- **及於**：`c2` 之併線；中間態之配地表得存在該二筆近乎零之配地。
- **⛔ 及於**：該二宗之**最終處置**（其仍依 `K-9-11 三` 之既有裁定辦理）；亦⛔ 及於任何**對外**使用（見 `§三`）。
- **⛔ 及於**：`c3`／`c4`（其各為獨立 `commit`，仍須逐 `commit` 呈 KL 放行）。

---

## §二　🔴 併線次序之更正（`V-1′` 須先於側支答畢）

**所診**：原單 `§六` 併線前必答五項，現況為 **`1`／`3`／`4`／`5` 已答、`2`（`V-1′`）未答**。
`V-1′` 之不可量係補令七 `§四` 所診之自毀式第二例，其新器**已授權但尚未建**。
🛑 **在 `V-1′` 答畢前併線 ＝ 跳過一道發單側自立之閘**，⛔ 為。KL 之放行係就**土地後果**而發，⛔ 免除驗證閘。

**更正後之次序（取代補令七 `§七`）**

| 序 | 事 | 落點 | 推 |
|---|---|---|---|
| `1` | `§四` 之 `V-1′` 記憶體側新器建置 ＋ 其自我驗證閘 ＋ `c2` 態逐款出艙（含三組**實測 `n`**） | **側分支 `verify/W-G.9-268p-c2` 之上**（⛔ 主線） | 推側支 |
| `2` | 發單側**逐位元組復驗**該新 `commit`（生產碼須仍為 `c1376af` 之內容·相異 `0`） | — | — |
| `3` | **快轉併線**：主線推進至側支之 tip | 主線 | 推 |
| `4` | `自誤 343`／`GB-158`／`GB-159` 之鑄 ＋ `GB-158` 波及面掃描 ＋ `CLAUDE.md` 末端追加 | 主線（零生產碼·得逕推） | 推 |
| `5` | `c3`（`contract` 段）→ `c4` | 各為獨立側支·**逐 `commit` 呈 KL** | — |

🔒 **快轉之保全**：序 `1` 之 `commit` 落於**側支**，主線於序 `3` 前**原地不動** ⇒ 快轉仍成立（發單側實測：主線為側支之祖先 ✅）。**⛔ 先推主線再回頭 rebase 側支**。
🛑 **序 `1` 若 `V-1′` 任一款轉紅、或三組同色、或任一組 `n = 0`** ⇒ **停機上呈**，⛔ 併線；KL 之放行於該情形下**不生效**（其所裁者為土地後果，⛔ 為驗證結果）。
🔒 **序 `3` ⛔ 須 KL 再放行**：序 `1` 為零生產碼，生產碼內容仍為 KL 所核准之 `c1376af`，逐位未變（序 `2` 須機械自證此點）。

---

## §三　`GB-159` 之鑄（中間態之界）

**受詞**：`W-G.9-268′` 併線後至 `W-G.9-269`（病一）落地前之**中間態**，其配地表含二宗近乎零之配地。

**內容（逐字）**
> `628-42(1)`（`G011`）`G ＝ 0.23 ㎡`、宗地寬度 `0.18 m`；`628-53(2)`（`G025`）`G ＝ 0.00 ㎡`、宗地寬度 `0.02 m`。
> 二者於改前即帶 `畸零地旗標 ＝ ⚠️移出/第1調配順位`（發單側實測·改前改後逐字相同）⇒ **警示訊號已在落檔內，⛔ 另設**。
>
> 🛑 **中間態之配地圖與負擔表 ⛔ 對外**——⛔ 交付地主、⛔ 公告、⛔ 作為任何對外文件之底稿、⛔ 作為任何對外數字之引據。
> 🛑 **⛔ 以「旗標已在」為由略去本項之登記**——旗標所載者為該宗之屬性，本項所載者為**整份中間態配地表之使用界限**，二者受詞不同。
>
> **解除條件（三者皆備）**
> `(1)` `W-G.9-269`（病一：`K-9-23` 閘一轉主動 ＋ `K-9-17` 遞補迴圈）落地併線；
> `(2)` 該二宗依 `K-9-11 三` 之既有裁定處置完畢——**不配地 ＋ 走合併／調配機制 ＋ 騰出之地入調配池 ＋ ⛔ 超配**；
> `(3)` 二宗於配地表**不再以近乎零之配地出現**（其或不在配地母體、或其 `G` 達法定最小建築基地面積）。
>
> 🔒 **`W-G.9-269` 之前置（重申補令三 `§十一`）**：`A` 母體（`K-9-9 二` 不交叉之違反·現值 `2` 宗／`4.7950 ㎡`）係於**原錨態**下量得，錨更正後其違反宗集**可能改變** ⇒ **⛔ 沿用，須重量**。
> 🔒 **KL 之裁之出處**：`2026-09-10`·逐字見 `§一`。

---

## §四　鑄號之授權

🔒 **既授權（維持）**：`自誤 343`、`GB-158`（補令七 `§六`）。
🔒 **本補令增列**：**`GB-159`**（`§三`）。**合計三號；⛔ 逾此。**
🛑 鑄號之意指占用查須依補令七 `§五` **二形並取**（B 形 ⋀ C 形），並同格出艙對照組於各形之命中；任一形使對照組全 `0` ⇒ 該形⛔ 單獨採信。
🛑 若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號。

---

## §五　續辦

依 `§二` 之序 `1`〜`5`。序 `1` 辦畢即停，候發單側復驗。

🛑 **在序 `3` 之前**：⛔ 推主線、⛔ 動 `app.py` 一字、⛔ 辦 `c3`／`c4`。
🛑 **側分支保持於 `2a1da8d` 之後裔**；若主線因任何緣故前進，併線前須 rebase 並由發單側**重驗**。

本補令由施工窗於續辦前**原封入倉**（`docs/orders/W-G.9-268p_補令八_KL裁與併線次序與GB159.md`），⛔ 動一字。

*純技術項一律自行辦到推完，只在真正觸及土地法規之域界時才停機上呈。*
