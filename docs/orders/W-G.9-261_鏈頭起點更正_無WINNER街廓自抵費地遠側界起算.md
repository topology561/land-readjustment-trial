# W-G.9-261　**鏈頭起點更正**：無 WINNER 之街廓，`Rw` 鏈頭之起算改為抵費地遠側界（`K-9-5-12（四）`·KL 裁 `2026-08-12`·KL 開修 `2026-09-09`）

> 🔴 **本單含生產碼**，且**動配地面積** ⇒ 依 `K-9-29 九`：
> **⛔ 逕推主線。側分支 `verify/W-G.9-261-c1` 出艙後停機候 KL 逐 commit 放行。**
> 🛑 本單**只改鏈頭起點一件事**。⛔ 順手動 `GB-67`、⛔ 動 `telescoping` 閘本身、⛔ 動五級。

---

## §零　所依之裁（**⛔ 新裁·全部既有**）

**`K-9-5-12（四）`　併同之既有裁示　KL `2026-08-12`**（逐字·`docs/rulings/K-6_…md:1980`–`:1981`）
> 無 WINNER 而街角為**強制抵費地**時，**第 2 宗**之起算為**該抵費地之遠側境界線**；
> **抵費地本身不計 `G`、不負擔**。

**`K-9-5-12（五）1`**：首宗起點一律為 `0`（即自 `MP` 起量）。
🔒 二者⛔ 互斥：有 WINNER ⇒ 第 1 宗＝街角宗，自 `0` 起算；無 WINNER ⇒ 抵費地為第 1 宗（不入鏈），第 2 宗自抵費地遠側界起算。

**KL 開修之裁（`2026-09-09`·逐字）**：「所以第2宗起點那個錯，要修正」。
**KL 就本則之複述（`2026-09-09`·逐字）**：「第2宗的臨側街負擔Rw＝RW(8)−RW(3.5)……而至於街角第1宗強制抵費地的Rw，免計，由該區調配池吸收(調配池減少)」。

---

## §一　開工閘（**逐條當場復現·任一不符 ⇒ 停機上呈**）

| 閘 | 應得 | 
|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `eedfbc6` |
| `2` 受詞 | `app.py` ＝ `4860f10828fb8ae9`／`verify/stepg_pipeline.py` ＝ `c8d46d2e5fb79dc5`（sha256 前 `16`）|
| `3` 號占用 | `W-G.9-261` 之宣告框 `D1 0`／`D2 0`／`D3 0`（母體全 `docs/`·載讀不到之檔數與副檔名分布）|
| `4` 次號 | 本批**⛔ 鑄任何號**（`自誤`／`GB`／`K-9` 三簿 `MAX` 前後不變）|
| `5` `S-0b` | `probe_order_preflight.py` `rc=0`、`P-5` 與 `§末` `SELF_SHA256` 相符 |

🩸 **`3` 之自指款**：若本單已入倉致 `≠0`，**⛔ 改號**；改判「占用者是否全為本單自身之產物」，是則過閘（`-260` 補令一所立之同形第四例）。

---

## §二　工項一：**現況之確認**（改碼**前**·純量測·⛔ 動碼）

🛑 **本工項係全單之地基。任一項不成立 ⇒ 停機上呈，⛔ 進工項二。**

- **`A-1`**　`3.5m`·`R2` 左側確為**無 WINNER**（無私有街角勝者）且**強制抵費地充當第 1 宗**。出艙其判定之來源欄位／函式，⛔ 引前批轉述。
- **`A-2`**　🔴 **決定性**：`W₀_left ＝ 6.98` 是否**確為該強制抵費地之遠側境界線**之 `W`，抑或僅係 `628-42(1)` 自身 `W_near` 之巧合等值？
  出艙該抵費地之近側界 `W`／遠側界 `W`／其面積，與 `6.98` 逐位對照。
  🛑 **若二者⛔ 相等 ⇒ 停機上呈**——全單之改法立於「鏈頭 `W_near` ＝ 抵費地遠側界」，該前提偽則改法偽。
- **`A-3`**　列出**全部**（兩情境 × 全街廓 × 左右側）之**鏈頭**及其 `暫編地號`／`第1筆街角`／`W_near`／`W_far`／`Rw(%)`，並標明**哪些鏈頭之 `第1筆街角` 為否**。
  🔒 發單側於 `eedfbc6` 之閘關落檔實測所得（**⛔ 採信·須自行復現**）：`R1` 四條鏈頭皆「是」；`R2 right` 兩情境皆「否」且 `W ＝ 0.0`／`Rw ＝ 0.0`；`3.5m R2 left` 為「否」。
- **`A-4`**　`R2 right` 兩情境之鏈頭 `W_near` 實值——**若非 `0`，則本改動之射程不止一宗**，須於報告具名並停機候裁。

---

## §三　工項二：改碼（**性質判準·⛔ 指定實作形**）

**唯一應成立之性質**：

> **`Rw` 鏈之鏈頭，其起算點 ＝ 該鏈頭自身之 `W_near`；⛔ `0`。**
> 非鏈頭者維持「前一宗之 `W_far`」，⛔ 動。

🔑 **何以此式同時滿足二裁**：鏈頭為街角勝者時其 `W_near ＝ 0`（`K-9-5-12（五）1` 自動成立）；鏈頭為無 WINNER 之第 2 宗時其 `W_near ＝ 抵費地遠側界`（`K-9-5-12（四）` 成立）。**⛔ 需 `if forced` 之分支**。

🛑 **界（⛔ 逾）**：
- ⛔ 將 `_rw_start` 一律改為 `W_near`——**非鏈頭**之 `W_near` 與前一宗 `W_far` 在「🔄方向改變」之界面**不相等**（實測：`R1 left` `628-37(1)`→`628-35(2)` `W_far=6.46`／`W_near=6.62`），一律改將動 `R1`。**只動鏈頭。**
- ⛔ 動 `telescoping` 閘（`stepg_pipeline.py:1410`–`:1444`）一字。該閘之期望式**本即正確**，本單係讓鏈回到它。
- ⛔ 動 `②-宗` 閘、⛔ 動 `_select_pool_slot` 之 `_b_L0`／`_b_R0`（`:1012`–`:1016` 之 forced 分支仍卡 `W-G.9-19 D3`，**非本單射程**）。
- 🩸 `app.py` 與 `verify/stepg_pipeline.py` 若有**同源二份**，須二處同改（`K-9-5-6` 之「碼面唯一決定點」自述⛔ 逕信，當場查其是否果然唯一）。

---

## §四　工項三：土地後果之量測（**逐宗·⛔ 取樣**）

出艙**改前 / 改後**對照表，母體 ＝ 兩情境全部落檔宗：

| 欄 | 要求 |
|---|---|
| `Rw(%)` | 逐宗改前／改後／差 |
| `負擔比率`／`G(㎡)`／`宗地寬度(m)`／`幾何面積(㎡)` | 同上 |
| `ΣRw` 逐街廓逐側 | 同上 |
| **調配池**（`R2`）| 改前／改後／差。🔒 KL 之裁：抵費地那段免計⇒**池減少**。**須實測其確為減少**，⛔ 以「應該如此」出艙 |
| `ΣG ＋ 池 ＝ 街廓` | 改前／改後**皆須成立**（守恆恆等式）|

🔒 **發單側之預測（⛔ 已證·CC 證之或推翻）**：
`3.5m`·`R2 left`·`628-42(1)`：`Rw` `55.59 → 0`；`ΣRw` `100.02 → 44.41`（±`0.02` 捨入）；`G` `0.0 → 正值`；`R2` 池**減少**。**其餘一宗不動。**

---

## §五　【驗收】

| 閘 | 判準 |
|---|---|
| `V-1` **射程** | 兩情境**共同落檔宗**中，除 `3.5m·R2 left·628-42(1)` 外，`Rw(%)`／`G(㎡)`／`宗地寬度(m)` **逐位不變**。🔒 比對須以（情境, 街廓, 暫編地號）為鍵、⛔ 以列序 |
| `V-2` **閘解** | `3.5m`·`R2 left` 之 `結構閘 telescoping` **不再破**（其 `ΣRw` ＝ `R(W_final)−R(W₀)`，容差依現行 `0.1`）|
| `V-3` **`0m` 不動** | `0m` 之中止點仍為 `②-宗 圍堵閘`＠`R2`、`biz_x ＝ 45.9766`（**本單⛔ 修 `GB-67`**）|
| `V-4` **守恆** | `ΣG ＋ 池 ＝ 街廓` 於改後每一街廓成立 |
| `V-5` **紅項** | 出艙改前／改後之紅項數**與其計數式**。🛑 **⛔ 立「紅項須減少」為閘**——`3.5m` 解閘後將首次跑進 `R3`–`R6`，**新紅項係新射程之發現、⛔ 迴歸**。逐項具名增減即可 |
| `V-6` **哨兵** | `R1` 四條鏈頭之 `Rw` 逐位不變（`0m` `52.64`／`55.8`；`3.5m` `98.75`／`55.8`——**⛔ 採信此四值·當場自 `eedfbc6` 復現為基準**）|

🩸 **發單側出艙前自檢**：本單所立之 `V-1`／`V-3`／`V-6` 之基準值，發單側**已於 `eedfbc6` 之閘關落檔實跑取得**；`V-2`／`V-4` **未跑**（其需改後之碼），故其判準已寫為性質式、⛔ 立具體數。

---

## §六　出艙

- 側分支 **`verify/W-G.9-261-c1`**；報告 `docs/reports/W-G.9-261R_鏈頭起點更正.md`。
- 🛑 **推側分支後停機**，以**一句實務語言**呈 KL 候放行，⛔ 呈程式碼。
  該句之形：「`R2` 在退縮 `3.5` 公尺下，`628-42(1)` 的側街負擔從 `X%` 變成 `Y%`，它的分配面積從 `P㎡` 變成 `Q㎡`，`R2` 的調配池從 `M㎡` 變成 `N㎡`，其餘各宗不動。」
- ⛔ 併主線、⛔ 動 `CLAUDE.md` 之 ⬜ 項，直至 KL 放行。

SELF_SHA256: b26932fc6848574fb341fb1c0eba78ad3e256b93f9dc34e36fe005b0499e2f1f
