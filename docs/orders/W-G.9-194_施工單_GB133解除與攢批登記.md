# 🔧 `W-G.9-194` 施工單：**`K-6-B` 段二排序鍵之前置 —— `GB-133` 解除**＋攢批登記

> **性質**：🔴 **含生產碼**（`app.py`）。依 `CLAUDE.md` `🔧 常規一 之補款 一`（KL `2026-08-31` 逐字「零生產碼 commit 逕行 push，生產碼 commit 才請示」）**與** `🔧 三` 逐字「🛑 生產碼批⛔ 不得逕行 ⇒ 幾何本體批第一張單起須請示」，`commit 2` 本應請示。
> 🔒 **KL 已於出單後<u>預先核可</u>（`2026-09-01` 逐字：「準改動推上去」）** ⇒ **`commit 2` ⛔ 不必再請示，逕行 `push`**——🛑 **惟其射程以 `§四-2` 為限。**
> **出單窗**：claude.ai（接 `W-G.9-193R`）
> **基座**：`wip/s1-endpart` ＝ `daa618d0952d5d4c7e4d7e273edc5bcc32c6d1fe`（🛑 開工時自行 `git ls-remote` 實查；不符即停機）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　受詞與落地點（🛑 逐一具名·⛔ 本單不出艙任何裸計數）

| # | 檔 | 動作 | commit |
|---|---|---|---|
| `L-1` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 末端追加 `自誤 244` | `1`（零生產碼） |
| `L-2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 末端追加 `GB-134` | `1`（零生產碼） |
| `L-3` | `app.py` | **`GB-133` 之解除**（`§三`） | `2`（**生產碼·須請示**） |

| # | 檔 | 動作 |
|---|---|---|
| `A-1` | `docs/orders/W-G.9-194_施工單_GB133解除與攢批登記.md` | 本單以原名入倉 |
| `A-2` | `docs/reports/W-G.9-194R_GB133解除與攢批登記_執行報告.md` | 執行報告 |

🛑 **`L-1`／`L-2` 屬 `常規一 ②` 補款③ `(2)` 之正典檔集** ⇒ `deletions` 恆須 ＝ `0`。
🛑 **`L-3` 係搬移** ⇒ **`deletions > 0` 係預期**，⛔ 不觸 `常規一 ②`（本 commit 本即須請示）。

---

## §零-b　開工閘

| 閘 | 指令 | 判準 |
|---|---|---|
| `S-0a` | `git ls-remote https://github.com/topology561/land-readjustment-trial.git refs/heads/wip/s1-endpart` | 逐字 ＝ `daa618d0952d5d4c7e4d7e273edc5bcc32c6d1fe`；不符即停機（`常規九 三`） |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-194_施工單_GB133解除與攢批登記.md` | `P-3`／`P-5` 須**不紅**；🟡 提示**逐項具名，採納或具名豁免** |
| `S-0c` | 見 `§五 S-7`（`SELF_SHA256` 自驗） | 二值逐位相同 |

🔒 **`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6` 六檢·🔴 機械停機款 `0` 項）**

| 檢 | 觸發點（字樣錨） | 發單側之處置 |
|---|---|---|
| `P-1` | `` 🩸 **案由（活體）** ``（`§二`） | ✅ **已採納**——同段已補具名母體與所查範圍 |
| `P-2` | `` 🔑 **判別力造（四造** ``（`§二`） | 🔒 **具名豁免**：該段之數係**被引述之樣本逐字**（造①「命中 `5` 處」），⛔ 非本單之出艙計數；其真正之出艙計數（`71`／`442`）已於次段同表載其框逐字並標明係「**次**」框 |
| `P-4` | `` 1. 分類迴圈 ``／`` - ⛔ 不改 `priority_index` 之算式 ``／`` \| 閘 \| 受詞 \| 判準 \| ``（`§三`／`§五`） | 🔒 **具名豁免**：三處之「→」皆為**碼面控制流**與**禁改清單**之列舉，⛔ 不承載任何方向性量；`§三-2` 之座標系宣告已涵蓋全 `§三` |
| `P-4`／`P-6` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§五` 表頭） | 🔒 **具名豁免**：`§五` 為閘之**索引表**，其每一閘之實測出處逐列具名於 `§三-4`／`§三-5`／本表之「受詞」欄；⛔ 無任何停機款之數僅見於該表頭 |

| `P-2` | **本豁免表自身**（`` \| 檢 \| 觸發點（字樣錨） \| 發單側之處置 \| ``） | 🔒 **具名豁免**：本表**⛔ 不出艙任何新計數**，其所引之數（`5`／`71`／`442`／`0`）皆為**他節之數之引述**，各該節已同格載其框；⇒ `CLAUDE.md:725` 之二數並報義務**在其原節履行**，⛔ 非在本表。🩸 **併記**：本列係 `GB-134` 之**活體佐證**——一個以段落為單位之啟發式，會把「**引述他處之數**」與「**出艙新數**」判為同物，此即其⛔ 不得列為停機款之由 |

🛑 CC 之實跑若出現**上表以外**之任何 🟡 或任何 🔴 ⇒ **逐項具名回報**；🔴 ⇒ **停機上呈發單側**。

🛑 **`P-2` 之提示⛔ 不可倚賴**（見 `§二` 之 `GB-134`：其觸發式對本倉主流語序**結構上盲**）⇒ 本單之一切計數，**一律於同段自載其框並二數並報**，⛔ 不以 `P-2` 未提示為據。

---

## §一　`N-1`　鑄 `自誤 244`（`commit 1`）

🔒 **取號現查（發單側·態 `daa618d`·⛔ CC 須自行復現並二數並報）**
- **母體** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**；**框（逐字）** ＝ `` ^#+ 自誤 `?([0-9]{1,4})`? ``
- **MAX ＝ `243`**（未扣除／已扣除二數相同·所扣 ＝ 七則人造值·本框皆不命中）⇒ **次號 `244`**
- **意指占用**（母體 ＝ 全倉 `.md`·錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·二形並取）：`自誤 244` 與 `` 自誤 `244` `` **二形皆 `0`**；🔑 **對照組** `自誤 243` ＝ `3`／`3` ⇒ 量測器非紅
- **範本（`常規四（九）四`）**：既有最末一則之標題形逐字 ＝ `` ### 自誤 `243`（發單側·`W-G.9-193` 交接文出文窗）：**以「街角」為量詞出艙一個實為「街廓」之計數** ``

### `N-1` payload（🛑 逐字追加於 `L-1` 檔尾）

```
### 自誤 `244`（發單側·`W-G.9-193` 出單窗）：**出艙一個「命中數」而⛔ 未宣告其框係<u>列數</u>抑或<u>次數</u>**

**案由**　`docs/orders/W-G.9-193_施工單_攢批登記與接線現況更正.md` 之 `§零-b` 逐字載「判別力對照：寬字樣 `一般式` 於本單 ＝ `5` 命中 ⇒ 該檢⛔ 非恆為 `1`」。
🔴 **該數⛔ 未宣告其框**——`一般式` 於本單為 **`5` 列／`6` 次**（其中一列含二次：該註記之引述起首 ＋ 該句自身之「寬字樣 `一般式`」）。單載之 `5` 係**列數**框，而同格⛔ 未依 `CLAUDE.md:725`（`常規四（七）` 補款一 `三`）二數並報。
🔑 **攔截者 ＝ CC**（`W-G.9-193R` 逐字：「`一般式` 於本單命中 `6`（單載 `5`）。逐列具名以定其真偽」）——**其處置正確**：逐列具名、釐清係列數框、判為 `⛔ 非互斥`、⛔ 未追改單一字、續辦。
🔒 **⛔ 無土地後果**：該數之用途為「證判別力檢⛔ 非恆為 `1`」，`5` 與 `6` **皆 `> 1`** ⇒ **該結論之真值不受影響**。
🩸 **同族第五度**：本則與 `自誤 235`（框非正典所裁且未宣告母體）／`238`（同一受詞出艙三數，各出自不同之框）／`239`（母體與正典現查相異且未具名）／`241`（受詞檔計數與落地點數不符）／`243`（以「街角」之量詞承載「街廓」之數）**同族**——皆為「**出艙之數與其框／母體不同源**」。
🔴 **且係第二度發生於<u>該族之立法單自身</u>**：`W-G.9-193` 之 `§一`〜`§三` 正在鑄 `241`／`243` 二則同族自誤，而其 `§零-b` 同時犯之。⇒ `自誤 241` 所立之「立戒者在同一批就會違反自己的戒」**於下一批即再度坐實**。
🔒 **一般式**　凡出艙任何「命中數」者，須同格宣告其框係 **`列`（`grep -c`／`-n`）** 抑或 **`次`（`grep -o | wc -l`）**；二者相異時**二數並報**（`CLAUDE.md:725`）。⛔ **不得**以「量詞可由上下文推知」免除；⛔ 亦不得倚賴 `P-2` 之機檢代之（其對本倉主流語序結構上盲·`GB-134`）。

```

---

## §二　`N-2`　鑄 `GB-134`（`commit 1`）

🔒 **取號現查**：母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**；框 ＝ 三形聯集 `` ^## `GB-N` ``／`` ^### `GB-N` ``／`` ^\| *\*{0,2}`?GB-N`?\*{0,2} ``。**MAX ＝ `133`**（未扣除／已扣除二數相同·所扣 ＝ `{997, 883, 746, 357}`·本框皆不命中）⇒ **次號 `134`**。
**意指占用**（全倉 `.md`·錨定框）：`GB-134` ＝ `0`；🔑 **對照組** `GB-133` ＝ `27` ⇒ 量測器非紅。
**範本**：既有最末一則之標題形逐字 ＝ `` ### `GB-133` 🆕 ``

### `N-2` payload（🛑 逐字追加於 `L-2` 檔尾）

```
### `GB-134` 🆕

**受詞**　`verify/probes/probe_order_preflight.py` 之 `P-2` 檢（「出艙之**計數**須同段載其**框逐字**；框無 `^` 錨者須依 `CLAUDE.md:725` 二數並報」）**對本倉之主流語序結構上盲**。

🔒 **成因（碼面·字樣錨 `grep -F` 固定字串·⛔ 不用行號）**：其觸發式逐字 ＝
`(命中|得數|得|計|共)\s*`?\d+`?\s*(處|列|次|筆|個|檔)`
——其要求**量詞（`處`／`列`／`次`／`筆`／`個`／`檔`）緊接於數字<u>之後</u>**。⇒ 語序 **`「N 命中」`**（數字在前、`命中` 在後、無後綴量詞）**結構上不可能命中**。

🔑 **判別力造（四造·證本判斷⛔ 非「檢查器恆綠」之泛泛之詞）**
| 造 | 樣本（逐字） | 應然 | 實測 |
|---|---|---|---|
| ① 已知應觸發 | `寬字樣 `一般式` 命中 `5` 處。` | 觸發 | **觸發** |
| ② 已知應觸發 | `共 `5` 列。` | 觸發 | **觸發** |
| ③ 🔴 盲語序 | `寬字樣 `一般式` 於本單 ＝ `5` 命中。` | 應觸發 | **⛔ 不觸發** |
| ④ 🔴 盲語序 | `` `步驟 0` @ `app.py` ＝ `9` 命中 `` | 應觸發 | **⛔ 不觸發** |
⇒ ①② 觸發 ⇒ **⛔ 非量測器全紅**；③④ 不觸發 ⇒ **選擇性盲**。

🔴 **盲區之量（母體 ＝ 全倉 `.md`·二框逐字如下·二數並報之「次」框）**
| 語序 | 框（逐字） | 全倉 `.md` **次數** |
|---|---|---|
| `P-2` **可見** | `(命中\|得數\|共)\s*`?\d+`?\s*(處\|列\|次\|筆\|個\|檔)` | `71` |
| `P-2` **盲** | `` `?\d+`?\s*命中 `` | **`442`** |
⇒ **本倉實際使用之語序，逾八成落在該檢之盲區。**

🩸 **案由（活體）**：`W-G.9-193` 之 `§零-b` 以盲語序出艙一個未宣告框之計數（`自誤 244`）。**母體 ＝ `docs/orders/W-G.9-193_施工單_攢批登記與接線現況更正.md` 單檔**；**所查範圍 ＝ `probe_order_preflight.py` 之 `P-1`〜`P-6` 六檢之實跑輸出**（`S-0b` 逐字：🟡 提示 `1` **項**、且該項為 `P-1`）⇒ **`P-2` 於該次實跑之提示數 ＝ `0`**。該錯由 **CC 人工逐列具名**攔下，**⛔ 非機檢**。

🛑 **⛔ 非「修好即可」——本項之性質**
1. `P-2` 屬 `P-1`／`P-2`／`P-4`／`P-6` 之**文字啟發式**族，`W-G.9-190R §九（甲）` 逐字定其「**必有偽陽**⇒ ⛔ 不得列為停機款」。⇒ 擴其觸發式**必增偽陽**，而偽陽之處置成本落在受單方。
2. ⇒ **本項⛔ 不逕行擴框**；其解除條件為：**擴框後之 `--selftest` 須同時證 ① 新增之二造（③④）轉為觸發 ② 既有四造（靶①靶②靶③反靶）逐項不變**。二者任一不成立 ⇒ ⛔ 不得入倉。
3. 🔒 **在解除之前，其代償 ＝ 出單側之明文義務**（`自誤 244` 之一般式）：凡出艙計數，**同段自載框係 `列` 抑或 `次`**。⛔ 不得以「`P-2` 未提示」為據。

🔒 **一般式**　凡以**機檢**代替人自律者，須先量該機檢之**盲區大小**（以其欲防之受詞於倉內之真實分布為母體），⛔ 不得僅證其「對已知靶為紅」。**對靶為紅 ⇒ 只證⛔ 非恆綠；⛔ 不證其覆蓋。**

```

---

## §三　`N-3`　`GB-133` 之解除（`commit 2`·🔴 **生產碼·須請示 KL 方得 push**）

### 三-1　受詞（現況·態 `daa618d`·字樣錨·發單側已自倉復現）

`app.py` 之 `_pk_one_side_v12`（字樣錨 `` def _pk_one_side_v12(group: list, g_values_map: dict, ``·`grep -Fc` ＝ `1` **列**）內，其序為：

1. 分類迴圈 `for cand in group:` ⇒ 逐候選判 `cand_G < min_area_to_apply`，分入 `eliminated` 或 `qualified`
2. **早退**：字樣錨 `` if not qualified: ``（其 `return` 之 `note` 逐字含 `🚫 無人達 G 值門檻 → 該端保留為抵費地`）
3. 指數迴圈 `for cand in qualified:` ⇒ 算 `_score_corner_cut`／`_score_side`／`_score_overlap`／`priority_index`
4. `qualified.sort(...)` ⇒ `return {'winner': qualified[0], …}`

🔴 **⇒ 段一全滅之街角，其候選之指數迴圈跑<u>零次</u>**，而正典 `K-6 §二 段二` 之**第一排序鍵**逐字為「街角指數**名次**」——**恰在唯一需要它的分支上取不到。**

🔒 **實測佐證（母體 ＝ `verify/out/KL_UI_3.5m_7a6b7f3_第1宗街角地指配結果.csv` 單檔·KL UI 實跑）**：`R2` 列之「【左】達資格候選」＝ `0`、「【左】第1宗指配」＝ `⚠️ 強制抵費地`、「【左】優先權指數」＝ `—`。

### 三-2　🔒 **本批之解**（⛔ 最小·⛔ 不改公式一字）

> 🛑 **座標系宣告（`P-4`·`自誤 227` 之教訓）：以下一切箭頭與「由…改為…」，其方向皆 ＝ 「<u>改前</u> → <u>改後</u>」，⛔ 非注入實驗方向、⛔ 非 shim→app 方向。**
>
> **把指數迴圈之走訪對象由 `qualified` 改為 `group`，並將整段移至「早退」之前。**
> ⇒ 迴圈**本體逐字不動**（三 `score_*` 之算式、`round` 位數、四欄之寫入名皆⛔ 不變），**只改二事**：① `for cand in qualified:` → `for cand in group:` ② 該段之**位置**移至字樣錨 `` if not qualified: `` **之前**。

### 三-3　🔑 **土地不變之<u>構造證明</u>**（⛔ 非「實測沒變」）

| # | 命題 | 依據（碼面·字樣錨） |
|---|---|---|
| `1` | `qualified` 之每一元素**即** `group` 之元素本身（**同一物件**·⛔ 非副本） | 分類迴圈為 `for cand in group:`，其分支末逐字 `qualified.append(cand)` ⇒ 附加者即迴圈變數本身 |
| `2` | ⇒ 改走訪 `group` 後，`qualified` 之每一元素所得之四欄值**與改前逐位相同** | 算式、輸入欄、`round` 位數皆⛔ 未改；輸入四欄（`_corner_cut_len`／`_corner_cut_den`／`_side_line_len`／`_side_line_den`）與 `_corner_intersection_area`／`_corner_range_area` **係在 `_pk_one_side_v12` <u>被呼叫之前</u>、於候選分組時逐候選寫入**（字樣錨 `` cand_p1['_corner_cut_len'] = round(_corner_cut_len_p1.get(id(cand), 0.0), 3) ``／`` cand_p2['_corner_cut_len'] = round(_corner_cut_len_p2.get(id(cand), 0.0), 3) ``）⇒ **⛔ 不因其後落入 `qualified` 抑或 `eliminated` 而異** |
| `3` | ⇒ `qualified.sort(...)` 之鍵**逐格不變** ⇒ `winner = qualified[0]` **不變** | 排序鍵逐字 ＝ `-round(float(c.get('priority_index', 0)), 4)` 與 `float(c.get('_pre_position_rank', float('inf')))`，二者皆取自 `qualified` |
| `4` | `eliminated` **⛔ 從不參與** winner 之決定 | 該函式**唯一**之非早退 `return` 逐字 ＝ `return {'winner': qualified[0], 'qualified': qualified,` ⇒ `winner` 只讀 `qualified` |
| `5` | ⇒ **配地結果不變係構造保證**，實測僅為**佐證** | `1`〜`4` 合之 |

🛑 **`2` 之前提須由 CC <u>現查坐實</u>，⛔ 不得採信本單**：跑正面列舉之 `grep -Fn`，證上開二字樣錨各於 `app.py` 得 `1` **列**（🔑 判別力對照：寬字樣 `_corner_cut_len` 於 `app.py` 之**列**數須 `> 1`，證該檢⛔ 非恆為 `1`）。**若現查與本單不符 ⇒ 停機上呈發單側**，⛔ 不得自行調整解法。

### 三-4　🔴 **判別力閘（必須轉·否則 patch 未咬到）**

🛑 承 `CLAUDE.md`「修完之後那個閘要再看一次，而且要看它的**數字**」逐字：**⛔ 計數為 `0` 而結論為「無變化」者，一律先當作 patch 沒生效。**

| 閘 | 受詞 | 應然 |
|---|---|---|
| `D-1` | 段一全滅之街角，其候選之 `priority_index` 欄 | **由「無值」轉為數值** |
| `D-2` | 同上候選之 `priority_index` **⛔ 不得全為 `0.0`** | 🔴 全為 `0.0` ⇒ **視為 `三-3` 之 `2` 破** ⇒ **停機上呈**（其表示輸入四欄實際未被寫入，本單之構造證明失效） |
| `D-3` | 段一有得標之街角，其 winner 與其 `priority_index` | **逐格不變** |

🔒 **`D-2` 之立法理由**：`priority_index` 之各項皆以 `.get(..., 0)` 讀入 ⇒ **輸入缺欄不會拋錯、只會靜默得 `0.0`**。⇒ 若不設本閘，一個「看起來已解除、實則第一排序鍵恆為 `0`」之結果會逕行出艙——即 `CLAUDE.md` 所載「靜默退路**不會讓程式失敗**，它只讓程式算**另一件事**」。

### 三-5　🛑 **量測通道與其<u>非等價性</u>**（⛔ 不得混列）

| 通道 | 可證 | ⛔ 不可證 |
|---|---|---|
| **harness**（`verify/selection_pipeline.py` 之 `` v12 = ns["select_corner_lots_both_sides_v12"] ``） | **碼面行為**（`D-1`〜`D-3` 之逐格對拍） | 🔴 **土地後果** |
| **KL UI 實跑** | **土地後果**（`第1宗街角地指配結果.csv` 之逐格） | — |

🔴 **二者⛔ 非等價**（`GB-123`：`main()` 無自動守備·唯一守備 ＝ KL 之 UI 實跑）。
⇒ **報告須分列二通道之數，⛔ 不得以 harness 之綠宣稱土地後果**。本單**⛔ 不要求** CC 取得 KL UI 之數——其為 KL 側之非同步動作，列為**本單之後續觀測項**。

### 三-6　🔒 對帳與既有紅

依 `CLAUDE.md` 之「准紅碼」條款：驗收**⛔ 非「全綠」**，而係「**與凍存之期望 `FAIL` 名單<u>名稱＋原因</u>逐項相同**」（`W-G.9-7` 升級式·名單 ＝ `verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt`·對帳器 ＝ `verify/wv_reconcile.py`）。
🛑 **類 II（僅 traceback 行號差）須逐項具名確認**，⛔ 不得自動視為同。

### 三-7　⛔ 本節**不**授權

- ⛔ 不改 `priority_index` 之算式、權重（`0.4`／`0.2`／`0.4`）、`round` 位數、四欄之欄名。
- ⛔ 不動 `qualified.sort` 之鍵序一字。
- ⛔ 不改早退分支之 `note` 逐字（`🚫 無人達 G 值門檻 → 該端保留為抵費地`）——**本批⛔ 不解除強制抵費地**（其解除須待段三落地）。
- ⛔ 不辦段一「上鎖」、段二全域排序、段三逐一嘗試、段四重排位次。
- ⛔ 不動 `verify/**` 任何一字（含 `stepg_pipeline.py`）。
- ⛔ 不重產任何 baseline、⛔ 不重記期望 `FAIL` 名單。

---

## §四　🛑 `commit` 之切分（逐 `commit` 判·⛔ 非逐批）

| `commit` | 內容 | 生產碼判法之輸出 | 處置 |
|---|---|---|---|
| `1` | `N-1` ＋ `N-2` ＋ `A-1`（本單） | 命中 `0` 行 | 🟢 **逕行 `push`** |
| `2` | `N-3`（`app.py`） | 命中 `1` 行（`app.py`） | 🟢 **逕行 `push`**（KL 預先核可·射程見 `§四-2`） |
| `3` | `A-2`（執行報告） | 命中 `0` 行 | 🟢 逕行 `push` |

判法逐字（🛑 逐 `commit` 各跑一次·**含空輸出亦須出艙**）：
```
git diff --name-only <前一已 push 之 HEAD>..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

### 四-2　🔒 **KL 預先核可之<u>射程</u>**（🛑 ⛔ 不得外延·⛔ 不得類推）

🔒 **KL 之裁（`2026-09-01`·逐字·已鎖·⛔ 不得重議）**
> **準改動推上去**

🛑 **該核可係<u>對「`§三-2` 所述之那個改動」而發</u>，⛔ 非對「`app.py` 之任何改動」而發。** 其射程之界定如下：

**（一）核可所涵蓋者**（🛑 **座標系** ＝ 「<u>改前</u> → <u>改後</u>」·同 `§三-2` 之宣告·⛔ 非注入實驗方向）
> `§三-2` 逐字之二事——① `for cand in qualified:` → `for cand in group:` ② 該段移至字樣錨 `` if not qualified: `` 之前——**且迴圈本體逐字不動**（三 `score_*` 算式、權重 `0.4`／`0.2`／`0.4`、`round` 位數、四欄欄名皆⛔ 不變）。

**（二）🔴 核可**⛔ **不涵蓋者——下列任一成立即<u>停機上呈發單側</u>，`push` 前之核可於該情形<u>不生效</u>**
| # | 情形 | 何以不在射程內 |
|---|---|---|
| `1` | `D-2` 觸發（落選候選之 `priority_index` **全為 `0.0`**） | ⇒ `§三-3` 之命題 `2`（輸入四欄於分組時已逐候選寫入）**為偽** ⇒ **實際落地之改動⛔ 非 KL 所核可之那個改動**，而係「一個第一排序鍵恆為 `0` 之改動」 |
| `2` | `D-3` 觸發（任一街角之 winner 或其 `priority_index` **有變**） | ⇒ **已生土地後果** ⇒ 逕行牴觸 `§三-3` 之構造保證；🛑 **此情形⛔ 不得續辦**，須連同逐格差異回報 |
| `3` | `S-4` 之 `numstat` 顯示 `app.py` **以外**之生產碼檔有異動 | 超出 `L-3` 之受詞 |
| `4` | `§三-7` 禁改清單之任一項被觸及 | 逕行違反本單明文 |
| `5` | `S-9` 之對帳器出現**類 III**（訊息實質不同） | 既有紅之組成已變 ⇒ ⛔ 非「與凍存名單逐項相同」 |

**（三）處置**
> 上表任一成立 ⇒ CC 將 `commit 2` **備妥但⛔ 不 push**，於報告內出艙 `§五` 全閘之數並**逐字回報該情形**，**回報發單側**（⛔ 不直接問 KL）；由發單側整理為白話後重新上呈 KL。
> ⛔ **不得**以「KL 已說準」為由略過本款——**KL 所準者係一個已被構造證明為零土地後果之改動；一旦該證明之前提被實測推翻，被准的那個東西就已經不是要推的這個東西。**

**（四）🔒 併記**：本款⛔ 不改 `CLAUDE.md` `🔧 常規一` 及其補款一字；其僅就**本單之 `commit 2`** 記錄 KL 之個案核可與射程。

---

## §五　收工閘（🛑 三欄交叉核對：`rc`／逐字結論列／`stderr` 位元組數）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-1` | `git diff --numstat daa618d0952d5d4c7e4d7e273edc5bcc32c6d1fe -- <L-1> <L-2>` | 二列**刪除欄逐列 `0`** |
| `S-2` | `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py` | 依 `常規五` 先判量測器紅抑或受詞紅 |
| `S-3` | `python -m py_compile app.py` ＋ `ast.parse` | 無例外 |
| `S-4` | `git diff --numstat <基座> -- app.py` | **逐字出艙新增／刪除二欄**（🛑 搬移 ⇒ `deletions > 0` 係預期·⛔ 非異常） |
| `S-5` | **形之自證**（`常規四（九）二 ①`）：以自誤寬形掃 `L-1`、以 `GB` 三形聯集掃 `L-2` | `244`／`134` 皆命中 |
| `S-6` | **MAX 之推進**（同款 `②`·🛑 **入倉後重掃**） | 自誤 `243`→`244`；`GB` `133`→`134`；二者皆⛔ 不在缺號集 |
| `S-7` | 本單 `SELF_SHA256` 自驗 | 二值逐位相同（指令同 `W-G.9-193 §五 S-6`，換路徑） |
| `S-8` | **`D-1`／`D-2`／`D-3`**（`§三-4`） | `D-1` 須轉／`D-2` 須⛔ 非全 `0.0`／`D-3` 須逐格不變 |
| `S-9` | `verify/wv_reconcile.py` 之三分類 | 類 III ＝ `0`；類 II **逐項具名確認** |

🛑 **`S-8` 若因長跑而不可得**：依 `CLAUDE.md`「**長跑期間不得動受測物**」，`run_all` 或任何長跑執行期間⛔ 不得修改 `app.py` 與 `verify/` 下任何檔。

---

## §六　執行報告（`A-2`）須含

1. `S-0a`〜`S-9` 逐閘之**三欄**：`rc` ／ **逐字結論列** ／ `stderr` 位元組數。
2. `S-0b` 之 🟡 提示**逐項具名**，標「採納」或「具名豁免（附理由）」。⛔ 不得靜默略過。
3. **取號現查之二數並報**（未扣除／已扣除）＋ 所扣之值逐字 ＋ **意指占用之二形並取** ＋ **對照組**之非零命中。
4. 🛑 **凡出艙任何計數，同段宣告其框係 `列` 抑或 `次`**（`自誤 244` 之一般式·⛔ 不倚賴 `P-2`）。
5. `§三-3` 之 `2` 之**現查坐實**（二字樣錨各得 `1` 列 ＋ 判別力對照之寬字樣列數 `> 1`）。
6. `§三-5` 之**二通道分列**——harness 之數與 KL UI 之數⛔ 不得混列；KL UI 未跑者，明載「**待 KL 實跑**」。
7. 逐 `commit` 之生產碼判法輸出（**含空輸出**）。
8. `git log -1 --format="%h %s"` 之**原樣輸出** ＋ 倉內報告路徑。
9. 🔴 **`§四-2（二）` 之五款逐款具名判**（逐款標「未觸發」或「觸發＋逐字情狀」）——⛔ 不得以「全綠」一語帶過；**五款皆未觸發，方得援用 KL 之預先核可**。
10. 依 `常規二`：發現本單自相矛盾者，**取保守項續辦 ＋ 逐字回報**，⛔ 不上呈 KL。

---

🔒 **只有真觸及域邊界才停機上呈、純技術自行做完。**

SELF_SHA256: ef75f3bdabbe24b698fa18c028863b4cf540bbac7264b375406c8968181bab76
