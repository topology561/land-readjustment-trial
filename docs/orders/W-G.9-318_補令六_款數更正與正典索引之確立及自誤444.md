# `W-G.9-318`　**補令六**：款數之更正／「搜框」之撤回與正典索引之確立（`自誤 444`・恆常附款 `ab`）

> **態**｜**發單側自倉獨立實查**：主線 `wip/s1-endpart` ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（**一字未動**）；`wip/W-G.9-318-preserve` ＝ **`174b0be1841125b14ed31267aebd0d274915c10f`**；`549121b..174b0be` ＝ **`7`** 筆；生產碼 `34` 檔相對 `549121b` 之相異 ＝ **`0`**；恆常附款簿之款名（去重）＝ **`27`**（`a`〜`z` ＋ `aa`）。
> **分級** ＝ **輕**（零生產碼·⛔ 改生產碼一字·⛔ 驅動）。
> **開工態** ＝ `174b0be1841125b14ed31267aebd0d274915c10f`（一切 blob 錨繫於此·恆常附款 `w②`）。
> 🛑 本補令於相衝之點取代原單與補令一〜五。
> 🔑 **KL 已逐筆放行（`2026-09-20`·逐字：「放行」）** ⇒ **`§零-3` 工項零′（主線快轉）為本補令之<u>第一動</u>**，先於工項零（原封入倉）。
> 🔑 **序**：**① `§零-3` 主線快轉** ⇒ ② 工項零（原封入倉）⇒ ③ 輕級開工閘 ⇒ ④ `§一`〜`§三` ⇒ ⑤ `§四` 收工閘 ⇒ ⑥ `push` **主線**（⛔ 側分支）。

---

## `§零`　受領・裁

### `§零-1`　受領（**⛔ 鑄號**）

1. **`W-G.9-318R6` 全批受領**；`push` 射程正確（側分支·⛔ 觸主線·全數零生產碼·發單側自算相異 ＝ `0`）。
2. 🔑 **CC 依恆常附款 `f`（「不符者以列舉為準並具名」）作具名相異而⛔ 追改單一字** ⇒ **判斷完全正確**，二項皆受領。
3. **CC 自捕第 `2` 則受領**：上一批就 `GB` 自建六框皆⛔ 復現 `169`，而倉內早有該器（`probe_WG9267_issuer_measurers.py registry`·一跑即得 `169`／`171`／缺 `[12, 87]`）；根因 ＝ **恆常附款 `k③` 未施行**（新立器前須以功能之名查既有器）。🩸 **同一根因亦及於發單側**——補令四 `裁 3` 之二框係自建，而未先以「恆常附款之查找」為功能名查既有器或索引（見裁 `2`）。
4. **恆常附款 `z` 之首次施行受領**：二哨兵皆先列舉定義域（自誤已鑄集 `432` 個成員／簿內款名 `25` 個）再證 ∉，⛔ 以「執行期組出」充證。

### `§零-2`　裁（**`3` 項**）

**裁 `1`　款數之更正（受領 CC 之列舉）**
補令五 `§一` 閘載「`23` → `25`」**誤**——其未計**補令四 `§四` 所立且已錄於簿**之 `x`／`y`。**正為 `25` → `27`**。發單側自倉獨立重算（框 ＝ 簿內 `^\|\s*\`([a-z]{1,2})\`\s*\|` 之去重）⇒ **`27`**（`a`〜`z` ＋ `aa`），與 CC 逐位相符。

**裁 `2`　🔴 「書寫形 `2` 種·後之搜框須二形並取」<u>全部撤回</u> ⇒ 鑄 `自誤 444`**

三次量測、三個數：

| 量測者 | 分類器 | 得 |
|---|---|---|
| 發單側（補令五 `§一(b)`） | 目視·二分（指向形／條文形） | **`2`** 形 |
| CC（`W-G.9-318R6`） | 機械量測 | **`4`** 形 |
| 發單側（本補令·自倉覆核） | 以簿之 `出處` 欄逐款取列後五分類 | **`5`** 形（`形②` `11` 款／`形③` `10` 款／`形④` `4` 款／引用區塊內之 `**\`x\`**：條文` `2` 款） |

⇒ 🔑 **「書寫形之種數」本身即<u>框依存</u>** ⇒ **⛔ 釘定其數**、**⛔ 以任何形數定搜框**。
⇒ 🔒 **結構解**：**恆常附款簿之 `出處` 欄（檔:列）即正典索引**。此後查任一款之條文，**一律經本簿取其 `檔:列` 後以檔案管線取之**，**⛔ 以文字搜框搜之**。搜框之射程問題於索引存在後**自始不生**。
⇒ 補令五 `§一(b)` 之「其倉內書寫形計 `2` 種……後之搜框須二形並取」**撤回**；CC 已落地之該節**⛔ 改上文一字**，以 `§一` 之末端追加更正。

**裁 `3`　🟢 KL 已逐筆放行 ⇒ 主線快轉（工項零′）**
發單側於落地前最後一次自倉重導（恆常附款 `y②`·框 ＝ `git show --numstat --format= <h> -- app.py verify/stepg_pipeline.py`·母體 ＝ `4037570..174b0be` **`40`** 筆）⇒ 動生產碼者恰 **`6`** 筆，與呈 KL 之表**逐列相符**：`8e056b4`(2 檔)／`b1141a3`(2)／`6cc51e6`(2)／`7c07a36`(1)／`7c346b1`(1)／`7f43924`(1)。
`git merge-base --is-ancestor 4037570 174b0be` ⇒ `rc` ＝ **`0`** ⇒ **真快轉·⛔ 需 `--force`**。

---

## `§零-3`　工項零′：主線快轉（**第一動**·🛑 ⛔ `--force`）

```
git push origin 174b0be1841125b14ed31267aebd0d274915c10f:refs/heads/wip/s1-endpart
```

🛑 **三禁**：⛔ `--force`／`--force-with-lease`（其為真快轉·若被拒即表示主線已被他動 ⇒ **停機上呈**，⛔ 加力）；⛔ 改目標值（**逐字 `174b0be1841125b14ed31267aebd0d274915c10f`**·⛔ 用 `HEAD`／`preserve` 之當下值，其可能已後移）；⛔ 刪 `wip/W-G.9-318-preserve`（保留為歷史·其已全數併入主線）。

🔒 **快轉後即出艙（⛔ 延後）**：
① `git ls-remote origin refs/heads/wip/s1-endpart` 全 `40` 碼 ⇒ **`174b0be1841125b14ed31267aebd0d274915c10f`**；
② `git merge-base --is-ancestor 4037570 <新主線>` ⇒ `rc` `0`；
③ 遠端分支總數 ⇒ **`25`**（⛔ 減·preserve 支保留）；`refs/heads/verify/*` ⇒ **`20`**；
④ 生產碼 `34` 檔於**新主線** vs `174b0be` 之相異 ＝ **`0`**；判別力[必非零]：對 `4037570` 之 `app.py` 須相異 ＝ `True`；
⑤ 六筆生產碼 `commit` 逐筆 `git branch -r --contains <h>` ⇒ 皆含 `origin/wip/s1-endpart`；判別力[必不命中] `4037570^`（`f181f9cc…`）⛔ 落於 `4037570..新主線` 內。

---

## `§一`　工項一：恆常附款簿之末端追加更正節（**⛔ 改上文一字**）

節題：
`## 🔧 補令五「書寫形二種·二形並取」之撤回 ＋ 正典索引之確立 ＋ 恆常附款 ab 之立（W-G.9-318 補令六·⛔ 上文一字不刪·純末端追加）`

內容三項：
`(a)` **裁 `2` 之三次量測表逐字**（含各自之分類器），並具名「**書寫形之種數為框依存·⛔ 釘定·⛔ 據以定搜框**」；
`(b)` **正典索引之確立**：本簿 `出處` 欄即正典索引；此後查款之條文一律經本簿取 `檔:列` 後以檔案管線取之，⛔ 文字搜框。🛑 **其維護款**：此後每立一款，**同批**須補其 `出處`（檔:列）；⛔ 留空、⛔ 待後補；
`(c)` 恆常附款 **`ab`** 之條文（`§二` `自誤 444` 之攔法逐字）。

🔒 閘：款數 `27` → **`28`**（增 `ab`·此 `→` ＝ 期初→期末之**時序方向**·⛔ 幾何方向·⛔ 涉座標系）；逐款 `出處` 欄**非空**者 ＝ `28`（空者 ⇒ 🔴）；抽驗 `5` 款以檔案管線復現其條文逐字相符；判別力[必不命中] 一哨兵款名——**依恆常附款 `z` 先列舉定義域（`a`〜`ab`·`28` 個）並證 ∉**。

---

## `§二`　工項二：`自誤 444` 之鑄（自誤簿檔末·標題形**自最近一則逐字複刻**·恆常附款 `v①`）

> ### 🩸 `自誤 444`　**於一個專為修正「框之射程不足」而立之款中，再度以未經取樣驗證之框斷言書寫形之種數——且該款（恆常附款 `aa③`）出單前未對本單自身施行**
>
> **母體與框（本則之全稱否定所繫）**：恆常附款簿 `docs/reports/W-G.9波_恆常附款登記表.md` @ `174b0be` 之全部 `27` 款（框 ＝ 簿內 `^\|\s*`([a-z]{1,2})`\s*\|` 之去重·發單側當場實跑）；書寫形之量測母體即該 `27` 款之 `出處` 欄所指之 `檔:列`。
> **根因**：恆常附款 `aa③`（「凡受詞為某簿／某族之全部成員者，須先自已知成員取樣其書寫形之分佈，**至少 `3` 個不同批次**，再據以定框」）於**同一份單內**立，而**未對該單自身之斷言施行**——發單側以目視二分即斷「計 `2` 種」，CC 機械量測得 `4`，發單側覆核得 `5`。與 `自誤 443` **同輪、同受詞、同族**，且為本波第 **`4`** 度「沿用／自擬一個未經驗證之框」。
> **第二根因**：恆常附款 `k③`（新立器前須以功能之名查既有器）亦未施行——補令四 `裁 3` 之二框係自建，而倉內既有之 `probe_WG9267_issuer_measurers.py` 與其後所立之登記表，皆為同一功能之既有物。
> **後果之框**：⛔ 偽數入倉（CC 依恆常附款 `f` 具名相異、⛔ 追改、上呈）；其害為**一條「二形並取」之指令將使後窗漏 `12`／`27` 款**（CC 之量）⇒ 正典之檢索能力受損。其攔點 ＝ 恆常附款 `f` 與 CC 之機械量測，**各生效一次**。
> **攔法（二款·立為恆常附款 `ab`）**：
> ① **凡單內新立或修訂一款者，出單前須以該款對<u>本單自身</u>逐條施行一次並出艙其結果**——恆常附款 `a`（攔法須回掃本單全文）之射程於此**明文擴及「本單所新立之款」**；⛔ 以「該款係為後批而設」為由免之；
> ② **凡可由一份正典索引直接取得之物，⛔ 以文字搜框取之**；索引不存在者，**先立索引，⛔ 先定框**——「該用什麼框去搜」之問題，在索引存在後自始不生。

🔒 落地之框：`payload` bytes ⛔ 含其與上文之分隔換行；`append-only` 前綴逐位相符（**三值化**·恆常附款 `x`）；`CR` ＝ `0`；界列⛔ 入簿。
🔒 立後四簿（正典框）：自誤相異 `433`→**`434`**、MAX `443`→**`444`**；`GB`／`VR`／`K-9` ⛔ 變（`GB` 之數依補令五裁 `2` 以 `probe_WG9267_issuer_measurers.py registry` 出艙並具名其框未釘定）。

---

## `§三`　工項三：交接文之再補正（末端追加·⛔ 改上文一字）

載：款數正為 `27`→`28`；「二形並取」已撤回；**查款之條文一律經恆常附款簿之 `出處` 欄**，⛔ 文字搜框。

---

## `§四`　收工閘（輕·報告入倉後量·⛔ 沿用他批之判準字串與錨值）

`1` 刪除欄全 `0`｜`2` 生產碼 `34` 檔相對**開工態 `174b0be`** 之相異 ＝ `0`（判別力[必非零]：對主線 `4037570` 之 `app.py` 須相異）｜`3` `.md` 之 `CR` 全 `0`（受檢集 `0` ⇒ 器紅）｜`4` 四簿二框並報（正典框 自誤 **`434`**／**`444`**；`GB` 依補令五裁 `2`）｜`5` `append-only` **三值化**｜`6` 自限（`baselines` `a898f4e1…`·`298`；`verify/*` `20`；遠端總數 `25`；主線 `4037570…`）｜`7` 母體三數｜`8` `ls-remote` 全 `40` 碼。
🔒 **`push` 之目標改為主線**：本批全數零生產碼 ⇒ 依 `常規一` 補款 `🔧 一` 逕推 **`wip/s1-endpart`**（工項零′ 後主線即本波之現行碼面）。⛔ 再推 `wip/W-G.9-318-preserve`（其任務已了·保留為歷史·⛔ 刪）。

---

## `§五`　⛔ 為之事

1. ⛔ 改生產碼一字；⛔ 驅動；⛔ 開新分支。
1′. 工項零′ 之三禁：⛔ `--force`；⛔ 改目標值；⛔ 刪 `preserve` 支。快轉被拒 ⇒ **停機上呈**，⛔ 加力、⛔ 改法。
2. ⛔ 釘定「書寫形之種數」；⛔ 以任何形數定搜框；⛔ 以文字搜框查款之條文（裁 `2`）。
3. ⛔ 改恆常附款簿、交接文之上文一字（更正一律末端追加）。
4. ⛔ 於本波釘定 `GB` 之定義列式（補令五裁 `2`）。
5. ⛔ 解除或收窄 `GB-168`／`169`／`170`／`171`。
6. ⛔ 代擬呈 KL 之放行格（`序 11`）。

---

## 附錄`甲`　KL 裁示之落款（**逐字**·`2026-09-20`）

> **放行**

**其義（發單側之解讀·CC ⛔ 自解）**：對補令六附錄`甲`所列 **`6`** 筆生產碼 `commit` 之**逐筆放行**，並核可主線 `wip/s1-endpart` 自 `4037570…` **快轉至 `174b0be1841125b14ed31267aebd0d274915c10f`**。

| # | 雜湊 | 內容 | 性質 |
|---|---|---|---|
| `1` | `7f43924` | `app.py` 區塊上移（純位移 ＋ 縮排） | 現行 |
| `2` | `7c346b1` | `stepg` 同構上移 | 現行 |
| `3` | `7c07a36` | `solve_G_binary` 新增 `w0_start`（預設不傳則行為逐位不變） | 現行 |
| `4` | `6cc51e6` | `_solve_G_one`／`_solve_one` 薄殼直通 | 現行 |
| `5` | `b1141a3` | 六呼叫端傳之（舊作法·兩側皆傳） | 🔴 **中間態·已被 `8e056b4` 完全取代** |
| `6` | `8e056b4` | `c7′` forced 側⛔ 傳（`GB-170` 之避讓·KL `2026-09-20` 所核可之作法） | 現行 |

🛑 **落地之受詞**（KL `2026-09-20`「問2：放行」所核可者·⛔ 逾此）：**街角第 `1` 宗非強制抵費地之側，側街負擔起算點校回側面道路邊線（`0` 公尺）；第 `1` 宗為強制抵費地之側維持不動。**
🛑 **⛔ 及於**：`GB-168`／`169`／`170`／`171` 之解除（四項皆 🔴 未解除）；`GB-170` 取錯線之修；`GB-138` 處置 ③；`K-9-41 ②`〜`⑥` 之落地。

---

SELF_SHA256: 65c559dd597210c781398c5fb353efe6412674f53d55fb76e1b21a503858ad18
