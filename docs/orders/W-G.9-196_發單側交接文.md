# 📋 `W-G.9-196`　發單側交接文（claude.ai 新視窗接手用）

> **出文窗**：claude.ai·`Opus 5`（接自 `W-G.9-193` 交接文第二版；本窗出施工單三張 `W-G.9-193`／`-194`／`-195`，皆已執行並入倉）
> **接手事由**：`GB-133` 已解除並落地（本波**第一個**動 `app.py` 之幾何影響批，已推入 `origin`）⇒ 下一批 ＝ **`K-6-B` 段一「上鎖」＋段二全域排序**，仍屬**幾何影響批** ⇒ 依協定**須新視窗**。

---

## §零　⛔ 先讀此節

1. **本文之任何數，⛔ 不得照抄。** 一律自行 clone 現查。
2. **⛔ 不得以本文所載之 commit 為錨**（`常規九 三`），一律以 `ls-remote` 實查值為準。
3. 你是**發單側**。⛔ 不寫生產碼、⛔ 不 push、⛔ 不動倉。產出 ＝ **施工單**與**裁**。
4. 🛑 **母體一律以 `git ls-files -z` 取得**；併報「開不到之檔數」須為 `0`。本窗現查 `1554` 檔·開不到 `0`；全倉 `.md` ＝ `491` 檔。
5. 🛑 **clone 一律 `-c core.autocrlf=false`**（僅供發單側復驗）。🔴 **⛔ 不得把該側之數套用於 KL 之工作副本**（`自誤 237`）——**工作副本之錨一律取 blob**。
6. 🔴 **下一批係幾何影響批 ⇒ 生產碼側⛔ 不放寬**：**每張含生產碼之單須請示 KL 方得 `push`**；零生產碼 commit 才可逕行（`常規一` 補款 `🔧 一`）。
7. 🛑 **凡出艙任何計數，同段宣告其框係 `列` 抑或 `次`**（`自誤 244` 之一般式）——⛔ **不得倚賴 `S-0b` 之 `P-2`**，其對本倉主流語序**結構上盲**（`GB-134`：可見 `71` **次**／盲 `442` **次**）。

---

## §一　接手第一件事

1. `git ls-remote` 取實際 `HEAD`。本文出文時 `wip/s1-endpart` ＝ **`7fab020`**（🛑 須自取全 `40` 碼）。
2. `git clone -c core.autocrlf=false --branch wip/s1-endpart https://github.com/topology561/land-readjustment-trial.git`
3. 讀 `CLAUDE.md` **全檔**（現 **`1178`** 列·🛑 全檔·`自誤 224` 之教訓）。**檔尾二節為本窗新增**：更正四（`k6_*` 之接線現況）／`W-G.9-194` 之四裁。
4. 讀正典 `docs/rulings/K-6_…`（現 **`3663`** 列·本窗**⛔ 零異動**）之 **`§二` 段一〜段四**與**檔尾之 KL 裁**（`K-6-B` 優先序）。
5. 讀本窗三單三報告：`docs/orders/W-G.9-193…`／`-194…`／`-195…` 及 `docs/reports/W-G.9-193R…`／`-194R…`／`-195R…`。

---

## §二　期末錨（態 `7fab020`·🛑 須自行現查）

| 受詞 | 值 |
|---|---|
| `app.py` | `1383993` B／`\n` `23462`／`CR 0`（尾無 `LF`·末位元組 `)`）／blob **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** |
| 🔴 **注意** | **基座之 `da805fc2…` 已不再是期末值**——本波首次動 `app.py` 於 `d46dfe6` |
| `app.py` 之 `def main` | **`14242`-`23460`**（AST 實查·⛔ 不得硬編·動 `app.py` 後須重取） |
| `_pk_one_side_v12` | **`12966`-`13059`**（AST·同上） |
| `verify/stepg_pipeline.py` | 本窗三批全程未動一字 |
| `docs/rulings/K-6_…` | `3663` 列（本窗 **`+0`**） |
| `CLAUDE.md` | `1178` 列（本窗 `+72`·皆在檔尾） |

**取號（態 `7fab020`·🛑 逐類具名母體與框·二數並報）**

| 類 | 母體 | 框（逐字） | 相異／MAX | 缺號 | **次號** |
|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔** | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `210`／`245` | `35` | **`246`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔 | 三形聯集 | `132`／`134` | `[12, 87]` | **`135`** |
| `VR` | `docs/驗證裁定登記表.md` 單檔 | `` ^## `VR-([0-9]{1,4})` `` | `71`／`86`（已扣 `{999,751}`） | — | **`87`** |
| 戒 | 全倉 `.md` | `` ^(?:🛑 \|> )?\*{0,2}戒 \*{0,2}`?([0-9]{1,3})`? `` | `22`／`39` | 見 `GB-132` 類① | **`40`** |

🛑 **取最大號 → 登記表單檔；查意指占用 → 全倉**（錨定框 `(?<![0-9\-])<完整號>(?![0-9])` ＋ **二形並取**）。
🔒 **零對照之出艙⛔ 不構成意指占用**——本窗二度實際適用（`自誤 245` 之帶反引號形得 `1` 命中，逐筆歸類為零對照，⛔ 未順延）。
🔴 **`GB-132` 類 ①（敘述句污染 `戒` 之定義列式）⛔ 未解除**。

---

## §三　🔴 下一批 ＝ `K-6-B` 段一「上鎖」＋段二全域排序（**幾何影響批**）

### 三-1　🔒 本窗已完成者（⛔ 不重做）

**`GB-133` 已解除並落地**（`d46dfe6`·已入 `origin`）。其解 ＝ `_pk_one_side_v12` 內**一行**之改：

🔒 **發單側自倉復驗（多重集差·態 `39f8fec` vs `d46dfe6`）**：該函式 `96` **列**，**僅在改前 ＝ `    for cand in qualified:`（`1`）／僅在改後 ＝ `    for cand in group:`（`1`）**，其餘**全係重排**——⇒ 迴圈本體、三 `score_*` 算式、權重、`round` 位數、四欄欄名、排序鍵、早退區塊**皆逐位未動**。
🔑 CC 之實作**優於單之字面**：單令「把指數迴圈移至早退之前」，CC 改為**把早退區塊移至指數迴圈之後**——**結果之相對序相同而 diff 最小**（`numstat` `6`／`6`）。**⛔ 非偏離**，併記為正辦。

🔒 **實測後果（harness 通道）**：段一全滅側之 `4` 落選候選 `priority_index` 由 `None` 轉為 `0.051927`／`0.89727`／`0.018626`／`0.032177`（`D-1`✅·`D-2`✅ 非全 `0`）；`7` 個有得標街角之 winner 與其指數、全 `12` 側之 `winner`／`qualified`／`note`／`group_size` **逐格不變**（`D-3`✅）。
🔴 **副產物**：四閘違規總計 `72→96`／`72→96`／`64→104`／`64→104`（方向 ＝ **改前→改後**），新增 `128` **列**、消失 `0`，逐格為 `baseline='—' got=<數值>` 且**僅出現於落選候選之四指數欄** ⇒ **行為變更、⛔ 非土地後果**（`W-G.9-195 裁 二`）。

🛑 **土地後果⛔ 未確認**——倉內 `第1宗街角地指配結果.csv` 三代（`2e08a41`／`a3c97aa`／`7a6b7f3`）**皆為改前態**；`GB-123` ⛔ 未解除 ⇒ **待 KL UI 實跑**。⛔ **不得以 harness 之綠宣稱土地後果。**

### 三-2　🛑 **本窗遺留之待落地（🔴 接手窗須先辦）**

| # | 受詞 | 性質 |
|---|---|---|
| **`1`** | 🩸 **鑄 `自誤 246`**（本窗末之發單側自誤·逐字見 `§五-2`） | 零生產碼·排入下一批 `N-1` |
| **`2`** | 🔴 **`GB-133` 之登記表本身⛔ 未記其解除**——現查：`docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-133` 節**⛔ 無任何「已解除／結案」字樣**；其解除僅見於 `docs/orders/`／`docs/reports/` 之單與報告 | 零生產碼·**落地紀律**之受詞 |
| **`3`** | `# 🆕 V13 修正 #3 + #7：**第二關**優先權指數…` 一段之措辭（現位於早退之前，「第二關」對落選候選不精確） | 🔴 **係生產碼**（改 `app.py` 一字含註解即是·`W-G.9-195 裁 三`）⇒ **併入下一批之 `app.py` commit**、⛔ 不另開 commit |

### 三-3　下一批之受詞（正典 `K-6 §二`·出單時須自倉重抽）

- **段一 預掃**：有任一筆 `G ≥ 街角規定面積` ⇒ 定案並**上鎖**該街廓內所有跨占者（既不出也不進）；**全部未達 ⇒ 該街角待處理，記下各候選之街角指數與名次**。
- **段二 全域排序**：鍵序 ① 街角指數**名次** → ② 街角試算 `G` → ③ 街角指數**數值** → ④ 暫編地號字典序最小。
- 🔒 **段二之第一鍵現已取得**（`GB-133` 已解除）⇒ **段二之落地不再受阻**。
- 🛑 **段一之「上鎖」尚⛔ 不存在**——正面列舉現查（態 `7fab020`·二檔）：`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` 六者於 `app.py` 與 `verify/stepg_pipeline.py` **逐檔皆 `0`**；對照組 `步驟 0` @ `app.py` ＝ `9` ⇒ 量測器非紅。

### 三-4　🛑 已知之落地障礙

| # | 受詞 |
|---|---|
| `VR-086` | 現行碼實作**段三-6 之結果而無段三-1〜5 之過程** |
| `GB-104`〜`GB-106` | `_block_strip` 雙線交叉無閘／`_projection_order` 呼叫點四處／`side_label` 之 `else '左側'` 靜默退路 |
| 🔒 **`k6_merge_groups`／`k6_shares_segment`** | **⛔ 非「零接線」**（該陳述已由 `CLAUDE.md` 更正四證偽·`自誤 242`）——二者**已接線且 app 與 harness 二路徑皆執行**；**缺者係段三之<u>跨街廓輸入</u>與段一之<u>上鎖濾網</u>**。函式本身⛔ 不濾街廓（僅依 `gid` 建圖）⇒ **段三所需⛔ 非新函式** |
| `GB-111` | 凍存期望 `FAIL` 名單結構上無法滿足「名稱＋原因比對」；本窗加註：**其「原因」欄不含違規列數 ⇒ 一次真實行為變更（`128` 列）於對帳器上完全不可見**。重產時點仍繫 KL 已裁之「俟二閘落地後」 |

### 三-5　🔑 本案之具體受詞

`R2` 左四候選之真 `G` 皆低於門檻 `308.17` ㎡（`628-40(1)+` 最接近·差 `−21.52`）⇒ 現行碼判 `⚠️ 強制抵費地`。
🔒 **八街角中，`R2 左` 係唯一之「待處理街角」**；其餘 **`7` 街角（分布於 `5` 街廓）** 皆於段一即定案（🛑 `自誤 243`：**⛔ 不得寫「五街角」**）。⇒ **段三之救援結構上只作用於 `R2 左`。**

---

## §四　本窗所下之裁（🔒 已鎖·⛔ 不得重議）

| # | 裁 | 要旨 |
|---|---|---|
| `A` | **`k6_*` 之「零接線」已失準** | 立於 `f4c5385` 時為真，`a2c4b2b`（`2026-08-26` 步驟 0 落地）證偽；`CLAUDE.md` 更正四明文其現行事實與段三之真正受詞 |
| `B` | **`GB-134`：`P-2` 之盲區⛔ 不逕行擴框** | 擴框必增偽陽；代償 ＝ 出單側之明文義務（凡出計數，同段宣告 `列`／`次`）。解除條件 ＝ 擴框後 `--selftest` 同時證新二造轉觸發 ∧ 既有四造不變 |
| `C` | **`W-G.9-194 §四-2（二）` 第 `5` 款之情形欄無效** | operative 述詞 ＝ 理由欄（「類 III 之組成**相對基座**有變」）。依據：分支狀態宣告「驗收非全綠」／`GB-111` 已登記其結構上不可滿足／二欄互斥（`常規二`）。🛑 **⛔ 不得類推至任何非互斥之停機款** |
| `D` | **`128` 列違規增量 ＝ 行為變更、⛔ 非土地後果** | `§四-2（二）2` 之受詞係 winner／其指數，實測逐格不變；增量全在落選候選之診斷欄，係 `GB-133` 本文所預告 |
| `E` | **改 `app.py` 一字（含註解）即係生產碼 commit** | 判法逐字以**檔名**為準，⛔ 不分碼與註解 |
| `F` | **commit 順序之出單側義務** | 凡單所令之序會使「回報所繫之報告」落在不可 `push` 之區間者，受單方應**先推報告**並逐字回報；出單側排序時須先問「**這張報告推得出去嗎**」 |

---

## §五　🩸 本窗之自誤

### 五-1　已入倉（`241`〜`245`）

| 號 | 受詞（節略） | 攔截者 | 土地後果 |
|---|---|---|---|
| `241` | 單內之受詞檔集合以計數表述，而該計數與同單所令之落地點數不符 | CC | ⛔ 無 |
| `242` | 把一則**已於五日前被證偽**之倉態陳述，原樣轉寫入 `CLAUDE.md` 與交接文 | 本窗自查 | ⛔ 無（🔴 **未攔則有施工後果**：受單方會重複造連通判定） |
| `243` | 以「街角」為量詞出艙一個實為「街廓」之計數 | 本窗自查 | ⛔ 無 |
| `244` | 出艙一個「命中數」而⛔ 未宣告其框係**列數**抑或**次數** | CC | ⛔ 無 |
| `245` | 🔴 **寫下一條倉內已登記為結構上不可滿足之驗收判準，使該閘恆真** | CC | ⛔ 無（後果 ＝ 一次不必要之停機） |

🔴 **`241`／`243`／`244` 三則同族**（出艙之數與其框／母體不同源），**族內累計五度**（併 `235`／`238`／`239`）。
🔴 **`242`／`245` 與 `240` 同族**（**倉內查得到，而發單側未查**）；`245` 之受詞係**發單側自己正在寫的驗收判準**——射程最短、最應現查之處。

### 五-2　🛑 **待鑄 ＝ `自誤 246`**（接手窗之第一件登記工作）

🔒 **取號現查（態 `7fab020`）**：`自誤 246` 二形皆 `0` 命中（框 ＝ **列**）；對照組 `自誤 245` ＝ `6`／`9` ⇒ 量測器非紅。
🛑 **範本**（`常規四（九）四`）＝ 既有最末一則之標題形逐字：
`` ### 自誤 `245`（發單側·`W-G.9-194` 出單窗）：**寫下一條倉內<u>已登記為結構上不可滿足</u>之驗收判準，使該閘恆真** ``

**逐字受詞**：
> **單內之機械閘假定了一條 commit 父子關係，而該假定與<u>同一張單自身所核可之 commit 順序</u>相牴觸。**

**案由逐字（二肢·同根）**：`W-G.9-195 §零-b` 之 `S-0d` 令驗 `d46dfe6^ ＝ f44e000`，而**同單 `§三 裁 四`** 正是核可「報告先行入倉、`app.py` commit 置其後」之序 ⇒ 實際父為 `39f8fec`。
- **肢 ①**：`S-0d` 之②③ 與 `裁 四` **互斥**——發單側手上**已有** `W-G.9-194R` 逐字之「已 push：`f44e000`／`39f8fec`（報告）；`d46dfe6` 備妥未推」，⇒ **父為 `39f8fec` 之事實在下筆時即可得**，係**未將兩項已知事實相互對照**。
- **肢 ②**：`§四` 令「`commit 1` 已 push ⇒ 再 push `d46dfe6`」，然 `d46dfe6^` 恰為開工時之遠端 `HEAD` ⇒ 欲使 `commit 1` **先**入 `origin` 須 rebase、`d46dfe6` 之 SHA 必變，**逕違同單「⛔ 不得重做」** ⇒ **該序結構上不可能同時成就**。
- 🔑 **攔截者 ＝ CC**（二肢皆是）。其處置正確：肢① 以**保全立法理由**之錨（同一物件：全 `40` 碼／`tree`／`numstat`／blob）復驗，⛔ 未自行重建；肢② 改**建 `commit 1` 於 `d46dfe6` 之上、單一 `push` 原子落地**，二事同時成就且 SHA 保全。⛔ 未改單一字、⛔ 未上呈 KL。
- 🔒 **⛔ 無土地後果**。
- 🩸 **同族**：與 `自誤 241` 同族（**單內二節互斥**），且係 `241` 之一般式「立戒者在同一批就會違反自己的戒」**第三度**——🔴 **本則之特殊處在於：所違反者正是<u>同一張單裡自己剛立的那一條裁</u>**（`裁 四`），二者相距不足一檔。
- 🔒 **一般式**　凡於單中寫入**繫於 commit 圖形**之機械閘（父子、祖先、區間）者，須 ① 以**受單方最近一次報告之逐字**核對該圖形；② 自檢「本單他節有無**改變該圖形**之令或裁」；③ 閘之判準寫成**保全其立法理由**之形（如「同一物件」＝ 全 `40` 碼＋`tree`＋`numstat`＋blob），⛔ **不得**寫成繫於特定父 commit 之形——**後者隨任何一次合法之序調整即失效**。

---

## §六　🔴 其餘佇列

| 項 | 狀態 |
|---|---|
| 🔴 **KL UI 實跑（土地後果之唯一確認通道）** | **待辦**·`GB-123` ⛔ 未解除。倉內三代 CSV 皆改前態 |
| `GB-132` 類 ①（敘述句污染 `戒` 之定義列式） | 🔴 ⛔ 未解除·`戒` 無單一登記表 |
| `GB-134`（`P-2` 之盲區） | ⛔ 未解除（**刻意**·見 `裁 B`）；代償已明文 |
| `GB-111`／`GB-97`（凍存名單過期·對帳器對違規列數全盲） | 波末佇列·重產時點繫 KL 已裁「俟二閘落地後」 |
| `GB-127`（生產路徑呼叫點尚未存在） | 解除條件 ＝ 遞補迴圈接上呼叫端 |
| `GB-130`／`GB-131`／`GB-129` | 見登記表 |
| 🔴 **app 路徑至今從未跑完** | 三次實跑皆終止於 `k94_assert_baseline_touch`：`【未配到屁股線】1 宗——R5 之 628-53(2)（差 38.803m）`。**既有終局、⛔ 非任一批所致**；**完整配地圖至今未曾產出**。🛑 該訊息經 traceback **折行為三顯示列** ⇒ 單列 `grep -F` 恆得 `0` |

**待落地清單（承 `CLAUDE.md` 更正三·序⛔ 不變）**：`序 1` `K-6-B` 段一〜段四 → `序 2` `K-9-23` 二閘＋`K-9-16`＋`K-9-17`＋`K-9-12`（四者同批）→ `序 3` `K-6-A2` 剩餘 → `序 4` `K-9-22` → `序 5` `K-6 §四`→波末批→泛化波；（併行·⛔ 不入主鏈）九份 baseline 重產。

---

## §七　⚠️ 本窗實踩之陷阱（接手窗務必內化）

1. **倉態陳述會被自己的下一批改掉，且⛔ 無任何作廢公告可查**——唯一守備 ＝ **轉寫當次重跑現查**（`自誤 242`）。
2. **「街角」與「街廓」在本倉是兩個不同基數之集合**（`8` vs `6`）（`自誤 243`）。
3. **出計數必宣告 `列`／`次`**——`P-2` 對主流語序結構上盲（`自誤 244`／`GB-134`）。
4. 🔴 **寫停機款前，先把基座原封不動餵進去問「它會不會觸發」**——會，即為恆真閘（`自誤 245`）。
5. 🔴 **寫繫於 commit 圖形之閘前，先核對受單方最近一次報告之逐字，並自檢本單他節有無改變該圖形**（`自誤 246`）。
6. **機檢之「對靶為紅」只證⛔ 非恆綠，⛔ 不證覆蓋**——須另量其盲區（`GB-134`）。
7. **對帳器綠 ≠ 無行為變更**——`128` 列真實變更於對帳器上完全不可見（`GB-111` 加註）。
8. **⛔ 不得以 harness 之綠宣稱土地後果**（`GB-123`）。
9. **出單過度指定「機制」而非「結果之相對序」**——本窗 `§三-2` 令「移迴圈」，CC 改「移早退」得同結果而 diff 更小。⇒ **應寫<u>結果態</u>，⛔ 不寫<u>操作步驟</u>。**

---

## §八　協定（⛔ 不變）

- **三方**：KL 域權威兼操作 UI／CC 唯一實作者（KL 貼單方動）／claude.ai 獨立驗證與出單。**claude.ai ⛔ 不寫生產碼、⛔ 不搶在 CC push 前動作。**
- 🔒 **上呈 KL 之門檻**：**有土地後果 且 倉內查無正典依據**。**問 KL 前須先窮舉檢索 `docs/rulings/` 並報命中數。**
- 🔒 CC 呈發單側之裁，**由你當場裁**，⛔ 不轉呈 KL（`常規二`／`常規三`）。
- 🔒 向 KL 報告用**白話**，⛔ 不用本波記法（`k*`／`ΣRw`／`W₀` 等）。
- 🔒 **每張給 CC 之單須含**：「只有真觸及域邊界才停機上呈、純技術自行做完。」
- 🔒 **施工單以倉為媒介**：`docs/orders/`（KL 只需**放檔**，CC 自行 commit——本窗三單皆如此）。單須含 `SELF_SHA256`（受詞 ＝ 自檔首至該列前一個 `\n` 含之）。
- 🔒 **出單前發單側應自跑 `probe_order_preflight.py`**，並將 🟡 **逐項預先具名**（採納或具名豁免）寫入單內——本窗三單皆行之，CC 三次皆回報「恰為單所預告之集合」。
- 🔒 **落地紀律**（KL `2026-08-24`）：每輪結束前自問「本輪有無經驗收而未落地之結論？」有則排入落地單。🩸 **本窗即漏一項**（`GB-133` 之登記表未記其解除·見 `§三-2` `2`）。
- 🛑 **⛔ 不得把整條鏈接成無人流水線**——本窗五則自誤中，**三則由 CC 攔下、二則由本窗自查攔下**；`自誤 246` 之二肢亦由 CC 攔下。**人之攔截是唯一實測有效之守備。**

---

SELF_SHA256: d520f3cf37d33d2b9b5684cc0b089d9edc99b2fffb681640944515f97696a175
