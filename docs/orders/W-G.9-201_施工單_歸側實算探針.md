# 🔧 `W-G.9-201` 施工單：**歸側實算探針**——步驟 0 合併後同時跨占左右兩街角者，逐宗算出左右三分項、總分、`G`、達標與 winner

> **性質**：🟢 **零生產碼**（新增 `verify/probes/` 之**唯讀探針**·⛔ 動 `app.py` 一字·⛔ 動任何既有 `verify/**.py` 一字）⇒ **CC 可逕行 `push`**。
> 🔒 **依據**：新增 `verify/probes/*.py` 屬零生產碼——倉內既有先例（`probe_WG9190R_t2diag_full.py`／`probe_WG9190R_t3gate2.py` 入 `7a6b7f3`、`probe_order_preflight.py` 入 `69daed5`，二 commit 訊息皆逐字標「⛔ 零生產碼」）。
> **出單窗**：claude.ai　**基座**：`wip/s1-endpart` 之現時 `HEAD`（判準 ＝ `S-0a`·綁性質）
> 🛑 **序**：本單須於 `W-G.9-200` 入倉**之後**開工（其量測所依之判準即 `K-9-24` 之逐字）。

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **事由**：`K-9-24` **二**之四分支須知「`M` 在左右兩側各自拿不拿得下」。KL 舉例假設 `R4` 之 `628(1)+` **歸左**，而以現行**分別計算**之總分為參考（左 `0.9916`／右 `0.9925`·差 `0.0009`）**可能歸右** ⇒ 左街角將依 **二·5** 成為強制抵費地 `226.01 ㎡`。🛑 **差距僅 `0.0009`，⛔ 得靠推定** ⇒ 本單以真資料實算。

---

## §零　落地點（單一 commit）

| # | 檔／物 | 動作 |
|---|---|---|
| `L-1` | `verify/probes/probe_WG9201_side_assign.py` | **新增**唯讀探針（結果態見 `§二`） |
| `L-2` | `verify/out/probe_WG9201_side_assign_<態8碼>.log` | 探針之 `stdout` 全文存檔入倉（⛔ 得截斷） |
| `A-1` | `docs/orders/W-G.9-201_施工單_歸側實算探針.md` | 本單以原名入倉 |
| `A-2` | `docs/reports/W-G.9-201R_歸側實算_執行報告.md` | 執行報告（**同一 commit**） |

🛑 **⛔ 動**：`app.py` 一字（含註解）；任何**既有** `verify/**.py` 一字；任何 `docs/rulings/**`；任何登記表。
🛑 **本單⛔ 鑄任何號**（⛔ 自誤、⛔ `GB`、⛔ `VR`、⛔ `K-9`）。

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之**性質** | ① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` 逐列⛔ 含 `app.py`、⛔ 不含任何 `verify/**.py`。任一不符 ⇒ 停機上呈 |
| `S-0b` | 🔴 **`K-9-24` 已在倉** | `grep -n "^### 🔒 K-9-24" docs/rulings/K-6_街角地分配程序與可分配判準.md` ＝ **`1`【列】**。⛔ 為 `1` ⇒ **停機**（本單之判準取自該節逐字，⛔ 得先於其入倉而跑） |
| `S-0c` | `probe_order_preflight.py` 對本單 | `P-3`／`P-5` 不紅；🟡 逐項具名（`§五`） |
| `S-0d` | 本單 `SELF_SHA256` 自驗 | `declared` ＝ `recomputed` |
| `S-0e` | 量測與動作之分離 | `§三` 之驗收與 `commit`／`push` **分開呼叫**（`CLAUDE.md` 常規補款 `一`） |

---

## §一　本單之寫法

🛑 **只寫<u>結果態</u>，⛔ 寫操作步驟**；達成手段由 CC 自定。
🛑 **本探針係<u>唯讀診斷</u>**：⛔ 得改變 app 或 harness 之任何既有輸出、⛔ 得被任何既有流程呼叫、⛔ 得寫入任何既有檔案。其存在對現行分配結果之影響須為**零**（`V-2` 驗之）。

---

## §二　結果態

| # | 落地後須成立之性質 |
|---|---|
| `R-1` | 🔒 **資料驅動·⛔ 硬編**：探針自 `data/V6_1.dxf`（`K-9-20` 之權威版本）與現行讀取路徑取得全部街廓與宗地，**逐街廓掃描**，⛔ 得出現任何街廓名（`R1`〜`R6`）、側標（`左`／`右`／`p1`／`p2`）、或本案特有常數（`308.17`／`226.01`／`3.5` 等）之硬編分支 |
| `R-2` | **合併集合之導出**：依 `K-6:57-60` 逐字（同街廓 ∧ 同歸戶 ∧ 相鄰）**無條件**求出全部步驟 0 合併群及其合併宗；🛑 **⛔ 得施用 `k6_step0_block_locked` 之保守替代**（其已隨 `K-9-21` 之取代而失效）。合併宗之面積相加、幾何取聯集、名稱 `{投影序在前者之暫編地號}+`、佔單一原位次 |
| `R-3` | **跨占之判定**：對每一宗（含合併宗與未合併宗），逐街廓判定其是否跨占該街廓**左、右**兩街角之規定範圍。**同時跨占兩側者**列為 `K-9-24` **二**之受詞 `M` |
| `R-4` | 🔴 **主出艙**：對每一個 `M`，**分別**以左、右街角**第 1 宗位置**試算並逐側報出：`真交集(㎡)`／`範圍面積(㎡)`／`門檻(㎡)`／`真G(㎡)`／`正街角分`／`側街分`／`跨占分`／**`總分`**／`達標(是否 G ≥ 門檻)`／**`該側 winner(是否為該側全部達標候選中指數最大者)`**／**`拿得下(達標 ∧ winner)`** |
| `R-5` | 🔴 **四分支之判讀**：依 `K-9-24` **二** 逐宗輸出其歸屬，並具名所走之分支：`2`（兩側皆拿得下 ⇒ 歸指數大者·並列取 `p1` 側）／`3`（僅一側拿得下 ⇒ 歸該側）／`4`（兩側皆拿不下 ⇒ 辦原位次）。🛑 **另一側之結果亦須報**：扣除 `M` 後該側**其餘跨占者**之候選清單、有無 winner、若無 ⇒ **強制抵費地及其範圍面積** |
| `R-6` | 🔴 **反靜默**：任一量取不到值（無 `FRONT_LINE`、無 `SIDE_LINE`、幾何退化、`G` 迭代不收斂等）⇒ **具名報錯並停機**，⛔ 得以預設值、⛔ 得跳過、⛔ 得靜默視為「未跨占」或「未達標」 |
| `R-7` | **對照組（判別力自證）**：同時報出**未合併態**下各宗之同一組量（即現行 `第 1 宗街角地逐候選評點明細` 之對應欄），使「合併前 → 合併後」之差可逐格比對。🛑 二態須於**同一次執行**內產生，⛔ 得跨執行拼接 |
| `R-8` | **全域覆蓋自證**：報出「街廓總數／可建築街廓數／合併群數／合併宗數／`M` 之個數」，並逐一列出**每一個**街廓之判定（含「該街廓⛔ 有 `M`」者），⛔ 得只報有 `M` 者 |
| `R-9` | `stdout` 全文須可直接存檔為 `L-2`，**⛔ 得截斷**；各數皆自載其**母體**與**框**（`列`／`次`／`格`／`檔`／`宗`／`㎡`） |

---

## §三　驗收

| 閘 | 受詞 | 判準 |
|---|---|---|
| `V-1` | 新增檔以外零變動 | `git diff --name-only <基座>..HEAD` **逐列⛔ 含** `app.py`、⛔ 不含任何**既有** `verify/**.py`；`HEAD:app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` |
| `V-2` | 🔴 **唯讀自證** | 探針執行前後，`--selftest` 與現行 harness 之輸出**逐格不變**；且**正面列舉**：新探針之符號於 `app.py`／既有 `verify/**.py` 之出現處 ＝ **`0`【列】**（對照組：任一既有探針名之出現處為非零 ⇒ 量測器非紅） |
| `V-3` | append-only | `git diff --numstat <基座>..HEAD` 之 `deletions` **逐檔 `0`** |
| `V-4` | 🔴 **判別力（⛔ 綁計數）** | `M` 之個數**⛔ 為 `0`**（⇒ 探針未咬到任何東西）；且 `R-7` 之「合併前 vs 合併後」**至少一格有差**（⇒ 合併確實改變了某個量）。二者任一不成立 ⇒ **停機上呈**，⛔ 得自行歸因 |
| `V-5` | 🔴 **`R-6` 之反靜默自證** | 逐項列出探針中**每一個**取值點及其取不到時之行為；須**逐點皆為報錯停機**，⛔ 得有任何 `else` 靜默預設 |
| `V-6` | `L-2` 在倉 | `git ls-files` 含該 log；其 bytes／`\n`【次】／`CR`【次】／`sha256` 逐項回報；🛑 若為純 CRLF 須走 `-c core.autocrlf=false` |
| `V-7` | `S-0e` 之自證 | 貼出量測與 `commit`／`push` 分開呼叫之界線 |

🛑 **⛔ 得以「探針跑完沒報錯」為驗收判準**——`V-4`／`V-5` 之性質判準為 operative。
🛑 **本探針之數⛔ 得逕作為任何裁之依據**：其係**診斷**，須經發單側獨立復驗、並由 KL 認可後方得進入 `K-9-24` 之實例段或任何生產碼。

---

## §四　commit 與 `push`

- **單一 commit**。訊息建議：`W-G.9-201：歸側實算探針（K-9-24 二之四分支）＋ 實測 log 入倉 ⛔ 零生產碼`
- 🟢 零生產碼 ⇒ CC 逕行 `push`。
- 🛑 執行報告 `A-2` 併入同一 commit；其量測須與 `push` **分開呼叫**。
- 🔒 **CC 回報須含**：`R-4`／`R-5` 之**逐宗逐側全表**（⛔ 得只報 `R4`）；`R-8` 之全域覆蓋；`V-1`〜`V-7` 逐項。

---

## §五　`S-0c` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6`）

| 檢 | 觸發點（字樣錨·`grep -F` 固定字串·⛔ 不用行號） | 處置 |
|---|---|---|
| `P-4` | `` \| # \| 落地後須成立之性質 \| ``（`§二` 表頭） | 🔒 **具名豁免**：表頭無方向性量；各結果態之母體、框與方向（合併前 → 合併後）逐列自載於該列本文，並於 `R-7`／`V-4` 逐字宣告二態之比較方向 |

🔒 **發單側已實跑**（`probe_order_preflight.py` 對本單單檔）：🔴 機械停機款 **`0`【項】**；🟡 **`1`【項】**，即上表（`P-4` `1`）；`P-5` `SELF_SHA256` ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。
🔑 **判別力自證**：同器對 `W-G.9-197`／`-198`（改版）／`-199`／`-200` 分得 `5`／`6`／`0`／`2`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。
🛑 CC 實跑若出現**表外**之 🟡 ⇒ 逐項具名回報；🔴 ⇒ 停機上呈。
SELF_SHA256: 6de9298f1ce3dda0b125ecf4ee14bcc761157d5dae97030dc70298cc65762471
