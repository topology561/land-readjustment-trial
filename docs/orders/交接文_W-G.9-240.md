# 交接文_W-G.9-240（發單側 · `claude.ai` 窗轉換）

> **受詞 ＝ 下一個 `claude.ai` 窗**，非 CC。
> CC 收此文之處置 ＝ **原封入倉 `docs/orders/`，⛔ 執行其所列之任何工項**。
> 入倉前依現行條款查號占用（**宣告框**）。

---

## §一　態錨（`2026-09-05`·本窗最後一次 `fetch`）

```
origin/wip/s1-endpart = 8c698bd525df4617120b0a0c697e56b7fca8d9df
追蹤 1650 檔 ／ .md 575 檔 ／ left-right = 0 0
```

生產碼五檔（**內容 sha256** 前 16 位·本窗於 `8c698bd` 實測）：

| 檔 | 內容 sha256 |
|---|---|
| `app.py` | `e3e464ea2493d046` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` |
| `verify/run_verification.py` | `e48f24c84079f365` |
| `verify/stepg_pipeline.py` | `0260273350021311` |
| `verify/run_all.py` | `4ba89fef90979491` |

🔒 **五檔自 `bf27645`（`-238` 交接文之錨）起<u>逐位未變</u>**——本波三批（`-237`／`-238R`／`-239`）皆零生產碼。

自誤簿（**正字框 `A` ＋ 範圍框**·母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔·列框）：
相異 `282` ／ `MAX` `289` ／ `MIN` `7` ／ 缺號 `[106]`。

號占用（**宣告框**·母體 ＝ 全 `docs/` **553** 檔·其中 `19` 檔為 `.png`／`.jpg` 二進位讀不到
且檔名皆⛔ 含單號·框 ＝ 檔框＋列框）：`239` ＝ `1/1`（**已占**）／**`240` ＝ `0/0`（未占）**／`241`–`249` 皆 `0/0`。

**⛔ 採信上表任一值**——接手窗須當場 `clone` 並以 `git cat-file blob` 復現。

---

## §二　接手窗必須先知道的三件事

**一　`claude.ai` 側可以直接讀倉，也可以跑幾何。**

```
git -c core.autocrlf=false clone https://github.com/topology561/land-readjustment-trial.git
git checkout wip/s1-endpart
```

**惟界限須守**：只看得到已推送者；CC 之工作樹看不到；容器不留，每窗須重 clone；**⛔ 推送**。

**二　`git ls-tree` 之預設 `core.quotepath` 會把 CJK 檔名跳脫成 `\NNN` 形。**
本窗首次算 `.md` 檔數得 `30`（真值 `574`）——**差 `544` 檔**，成因即此。
🔒 **凡對倉內檔名之任何計數，一律加 `-c core.quotepath=false`。**

**三　出單前必須重新 `fetch`。**
本窗於 `a3e141c` 查得 `W-G.9-239` 為 `0/0`，出單前重新 `fetch` 方發現 CC 已於 `8c698bd`
以 `docs/reports/W-G.9-239_CC交接文.md` 占用之。**`自誤 288` 之戒在其下一次施行即攔下一次誤用。**

---

## §三　本窗鎖定之域裁（KL 逐字·全部已答）

### 一　五級之射程與選塊法（`2026-09-05`）

> **`§7-1` 級1/2/3 與 `§7-4`（R 軌）之區位順序 ＝ 八鍵詞典序。**（KL 逐字：「`§三` 確定改用八鍵詞典序」）

- **⛔ 適用**：`§7-1` 級 0／級 0'（同街廓·無選塊問題）；`§7-4` 之 `RD`／`PF` 軌（維持距離優先）；`§7-5`。
- 八鍵 `r1`–`r8` 之全文見 `W-G.9-240 §二 b ③`。

### 二　`§7-5` 第2梯（增配）**維持現行最佳化**

KL 逐字：「a，重劃後盡量能讓地主拿得到土地」。
現行行為即：資格 `深度淺於原街廓 ∪ 後價低於原街廓`；選塊 `min Σ(增配面積×目標後價)`；
tie ＝ Σ增配面積 → Σ質心起迄距 → 暫編字典序；配額 `max(G(a′), MinA)`、增配 `max(0, MinA−G(a′))`。
🔒 **此裁之效果 ＝ 零變更**（與碼側現行一致）。

### 三　協調合併（`§31-1-2` 前段）

- **初步成果⛔ 需協調輸入** —— 既有域裁 `docs/W-D.4_域裁鎖定.md`「協調不成（試分配預設不成）→ 增配」即 KL 所要者。**零變更。**
- **事後指定「A 併入 B」須回頭重算 `G` 與配地位置** —— **新項**，射程如下。

### 四　重算射程：**受影響街廓相依**（affected-block dependency）

🛑 KL 明文排除「每接受一筆合併即自第一個街廓全區重跑」。

| 層 | 判 |
|---|---|
| 全區（`B`／`C`／`mina_qu`） | **恆不變**（其入參全為全區聚合量·⛔「通常不變」）⇒ 變動即停機 |
| 街廓 | 受影響集 ＝ `{A 原街廓, B 目標街廓}` 之**傳遞閉包**；傳遞邊自**調配帳**讀（⛔ 幾何鄰接） |
| 宗地 | 自 `p*` ＝ min{投影序 \| `a`／位次／街角身分／臨路條件 任一改變之宗} 向後重算；`p*` 之前由構造不變 |

🔒 **`§7-5` 窮舉母體 ＝ 受影響集之池；已公告而未受影響者之配置鎖定為約束**
（條文文字係發單側所擬、**KL 逐字閱後點頭確認** ⇒ **為 KL 裁**·⛔ 轉述為「claude.ai 之建議」）。

---

## §四　本窗**更正**之誤診（接手窗⛔ 重蹈）

| # | 前窗／本窗之斷言 | 實情（自倉復現） |
|---|---|---|
| 1 | 「`a′` 模式二未落地」 | **偽**——`wf_f2.a_prime_mode1/2` 皆在；`§7-4` 無同歸戶軌走 `wf_f4` 之 `74avg·{blk}` 合成 zone（模式二） |
| 2 | 「`mina_qu` 之定義與手冊『最淺街廓』不合」 | **非缺陷**——`docs/W-C_細部plan.md` 係 KL 拍板 `argmin 乘積`；殘留問題已登記為 `裁定N` 之 `U-B` 泛用化項 |
| 3 | 「八鍵詞典序 vs 現行最佳化 ＝ 同一條法規之二設計」 | **偽**——二者落點不同（`§7-1`／`§7-4` vs `§7-5`）、受詞不同、⛔ 互斥 ⇒ `自誤 291` |
| 4 | 「五級之『或／且』連接詞未有答案」 | **偽**——`wf_f4` 之 `∪` 早已是現行行為 ⇒ `自誤 290` |
| 5 | 交接文 `-238 §四` 以「正面路寬往小」代理「重劃後地價較低」 | **代理非必要**——`post_price_by_block`（`app.py:19635`）已有逐街廓後地價且已四處消費 |

🔒 **`1`／`2` 係本窗<u>出艙前</u>以 `grep` 自捕；`3`／`4` 係 KL 反問／答案落地後方發現。**
⇒ **自捕率 2/4。** 接手窗須把「先窮查再開口」提到出艙前，⛔ 靠 KL 當偵測器。

---

## §五　🔴 發單側之失敗模式（本窗實況）

本窗之二鑄號（`290`／`291`）**同屬交接文 `-238 §六` 所列之「憑設想給錨」族**，受詞分別為
「問題本身之必要性」與「既有碼區之性質」（後者為該族第 `7` 次、且受詞與第 `6` 次相同）。

**強制紀律（承 `-238 §六`·本窗加二款）**

1.–7. 承 `W-G.9-238` 交接文 `§六` 全部七款，⛔ 稀釋。
8. 🆕 **凡擬向 KL 出艙之域問，出艙前須以至少三組同義詞查 `verify/**.py` ∪ 全 `docs/`，
   並於出艙時同格載明所查之同義詞集與命中數**；⛔ 只查 `docs/` 而不查碼側。
9. 🆕 **凡出艙「二機制之對照表」，須於同表載明各自之規格節次**（如 `v3:§7-4` / `v3:§7-5`）；
   ⛔ 以「同一條法規」為欄名而不指其節次。

---

## §六　CC 側現況

- `PreToolUse` heredoc 硬閘**活體攔截已成立**（`W-G.9-238R`）。本窗以八造契約測**獨立復驗 guard 側**全綠，
  且全部阻斷造之 `stdout` 皆 `0` B ⇒ 第三缺陷之修於碼側坐實。
- 🔒 **界限**：`claude.ai` ⛔ 驗 harness 之接線；其唯一證據係 CC 之活體實測。**⛔ 以發單側之推論壓過它。**
- 🔴 **`GB-143` 補款 `③` 之條文仍載已被證偽之「二法並用」**——`W-G.9-240` 工項二即其更正，**尚未落地**。

---

## §七　待落地（**接手窗須自倉重生此表·⛔ 沿用本表**）

| 項 | 狀態 | 依賴 |
|---|---|---|
| `W-G.9-240` 之四工項 | ⬜ | 已出單、待 KL 貼給 CC |
| `K-9-1` 最小建築面積 UI 欄 | ⬜ | **`r2` 之前提**·有土地後果 |
| 區外道路清單 ＋ 正面道路識別符暴露 | ⬜ | **`r3` 之前提**·有土地後果（`app.py` 內「區外道路」命中 `0`） |
| 排序準據之可替換點（seam） | ⬜ | 純技術（現內嵌於 `wf_f4` 之 `級1相鄰/級2鄰近/級3非鄰近`） |
| **五級實作** | ⬜ | 須上三項齊備 |
| 事後協調合併重算 | ⬜ | 須在五級實作之後 |
| `K-6-A2` 存續四項 | ⬜ | 有土地後果 |
| `GB-150` 引擎硬限 `{0,3.5}` 拆除 | ⬜ | 通用化波阻塞項 |
| `GB-67`／telescoping 結構閘 | ⬜ | 二者致 `R4` 逐值迴歸不可得 |
| `GB-142`／`GB-139`／`GB-147`／`GB-149`／`GB` 簿之 `MIN`／缺號／框 | ⬜ | 期末批 |
| 宣告框 `D3` 與 `②` 之互斥 | ⬜ | 純技術·本窗新發現（實例 `W-G.9-235R2` 之表列同時被二款咬中） |
| 通用化波 | ⬜ | 未起 |

---

## §八　接手窗之建議首三步

1. **clone 並復現 `§一` 全部態錨**（⛔ 採信本文任一值·記得 `core.quotepath=false`）。
2. **查 `W-G.9-240` 之執行結果**（號占用宣告框、四工項之 `deletions`、自誤 `290`／`291` 之落號）。
3. **就 `§七` 之依賴序向 KL 取排序**——五級條文入典後，實作被 `K-9-1`／區外道路清單／seam 三項卡住，
   前二項有土地後果。⛔ 逕自決定順序。

---

## §九　未答之題（⛔ 待 KL）

**無。** 本窗所提之四題（五級選塊法、`§7-5` 維持、事後合併重算、重算射程）KL 已全數答畢。

接手窗第一次需要 KL 之時點，預期為：
- `§七` 依賴序之排序確認
- `K-9-1` 落地後之生產碼放行（逐 commit 逐字放行）
- 五級實作動工前之條文複核

---

*本文受詞 ＝ 下一個 `claude.ai` 窗。CC 之處置 ＝ 原封入倉，⛔ 執行。*
