# W-G.9-263　交接文 `-262` 入倉 ＋ `自誤 322`〜`324` 落簿（**全批零生產碼·⛔ 執行交接文所列工項**）

> 🛑 **全批零生產碼** ⇒ 依 `常規一` 補款 `🔧 一`，CC **得逕推主線**，⛔ 候 KL 逐 commit 放行。
> 🛑 本單**只辦入倉與落簿二事**。⛔ 開啟 `R4`–`R6`／`②-宗@R5`／`GB-67`／`_b_L0` 任一工項——**該等屬新窗**。

---

## §零　開工閘（**逐條當場復現·任一不符 ⇒ 停機上呈**）

| 閘 | 應得 |
|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `e9d1b9c3e6e46580da28392e1ed7495d5b34638f` |
| `2` 受詞 | `app.py` ＝ `1e15d45ae84b02ca`／`verify/stepg_pipeline.py` ＝ `cb0bbaaa0f1eb8bc`（sha256 前 `16`）🔴 **舊值 `4860f108…`／`c8d46d2e…` 已於 `-261` 失效** |
| `3` 號占用 | `W-G.9-262` 與 `W-G.9-263` 之宣告框皆 `D1 0`／`D2 0`／`D3 0`（母體全 `docs/`·載讀不到之檔數與副檔名分布）|
| `4` 次號 | 三簿前值 ＝ **自誤 `321`／`GB 154`／`K-9 29`** ⇒ 本批**只鑄 `自誤 322`／`323`／`324`**，`GB`／`K-9`／單號**⛔ 鑄** |
| `5` `S-0b` | `probe_order_preflight.py` `rc=0`、`P-5` 與 `§末` `SELF_SHA256` 相符 |

🩸 **`3` 之自指款**：若本單已入倉致 `263 ≠ 0`，**⛔ 改號**；改判「占用者是否全為本單自身之產物」，是則過閘（`-260` 補令一所立之同形第四例）。

---

## §一　工項零：**二檔原封入倉**（`docs/orders/`）

a. **本單**（`W-G.9-263`）。
b. **交接文 `W-G.9-262`**（`SELF_SHA256: d9a832565952b99b67570be3bd6d0167c8094c92808804f4d4cd3722a1f102ea`）
　🛑 **原封入倉，`⛔` 執行其所列之任何工項。** 其受詞係下一個 `claude.ai` 窗，非 CC。

出艙：來源／落檔／入倉 blob 三者之 `sha256` 逐位相符，並載位元組數與 `LF`／`CR` 計數（防 CRLF 正規化）。
🔒 二檔皆**新建**，⛔ 動任何既有檔一字。

---

## §二　工項一：`自誤 322`〜`324` 落簿（**檔末追加**·`docs/reports/W-G.9波_claude.ai側自誤登記.md`）

🔒 標題形依 `自誤 321` 之既有形（`### 🩸 \`自誤 NNN\`　**…**`），須可被 `VR-091 四` 之定義框命中（**機驗後出艙**）。
🔒 三則之**責皆 ＝ 發單側（本窗）**，鑄於 `W-G.9-263`·`2026-09-09`。

> ### 🩸 `自誤 322`　**在窮盡搜索前即向 KL 宣告「此題需你裁」——而該題 KL 已於 `2026-08-12` 裁畢**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-263`·`2026-09-09`。
>
> **受詞**：發單側於 `2026-09-09` 對 KL 逐字：「留給 CC 補完碼面追溯後，再以一句實務語言呈你」「**那一句才需要你裁**，且它動土地（`R2 left` 首宗那 `55.59%` 的側街負擔該不該收）」。
>
> **真值**：該題 KL **已於 `2026-08-12` 裁畢**——`K-9-5-12（四）`「併同之既有裁示」逐字（`docs/rulings/K-6_…md:1980`–`:1981`）：「無 WINNER 而街角為**強制抵費地**時，**第 2 宗**之起算為該抵費地之**遠側境界線**；抵費地本身不計 `G`、不負擔。」另見 `docs/reports/W-G.9-22_前置登記.md:38` 之對照表列。
>
> **攔截**：發單側自捕——續搜索（`632` 檔 `.md`·同義詞組「無 WINNER／無 winner／無私有街角勝者」× 「第 2 宗／遠側境界」）後尋獲，**⛔ 實際向 KL 提出該題**。
>
> **共相**：「**宣告某題需 KL**」與「**向 KL 提問**」係二個動作，而**前者亦以窮盡搜索為前提**。本則之錯在於：搜索停在 `K-6:1988` 之「⛔ 未撤·列為未決」一行，未讀其下之 `K-9-5-13` 失效加註，亦未查 `（四）`。
> 🔑 **與 CC 同輪之錯同源**：CC 亦只讀該一處而報「未決」。**二側同時只讀一處**——⇒ 該處之「未決」字樣具強誘導性。
>
> 🔒 **通則（本則所立）**：凡**宣告**一題需 KL，該宣告本身即受「⛔ 再問已答之題」之禁；其前置 ＝ 全 `docs/` 之窮盡搜索（**多組同義詞 ＋ 具名檔數**），與實際提問同一標準。**且凡見「未決／待裁」字樣，須續讀其後之全部加註至節末**——`⛔` 於該行止步。

> ### 🩸 `自誤 323`　**出艙閘之基準值時未套該閘母體所含之濾（`F>0`）**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-263`·`2026-09-09`。
>
> **受詞**：`W-G.9-261 §二 A-3` 之 🔒 逐字：「`R2 right` 兩情境皆『否』且 `W ＝ 0.0`／`Rw ＝ 0.0`」。
>
> **真值**：`R2 right` **無鏈頭**——`_has_right_corner ＝ False`（`R2/right` ⛔ 在 `corner_range_keys` 內），該側 `F>0` 者 `0` 宗，總量閘於該側 `continue` 而無記錄。由 CC 於 `-261R` 具名。
>
> **共相**：telescoping 鏈之母體逐字 ＝ 「`推進側別` ＝ 該側 **∧** `F(m) > 0`」；發單側取基準值時**只套前半**，遂以未濾之首列充當「鏈頭」。⇒ **濾條件亦屬母體之一部分**。
>
> 🔒 **通則（本則所立）**：凡出艙某閘之基準值，其母體須與**該閘之母體逐字同一**；⛔ 以「同街廓同側之首列」代「該閘之鏈頭」。出艙時須併載所套之濾。

> ### 🩸 `自誤 324`　**`V-1` 綁在代理量而非其所欲保證之性質，致該閘結構上不可達**
>
> 🔒 **責 ＝ 發單側。** 鑄於 `W-G.9-263`·`2026-09-09`。
>
> **受詞**：`W-G.9-261 §五 V-1` 逐字：「兩情境**共同落檔宗**中，除 `3.5m·R2 left·628-42(1)` 外，`Rw(%)`／`G(㎡)`／`宗地寬度(m)` **逐位不變**」；併同 `§四` 之預測「**其餘一宗不動**」。
>
> **真值**：該閘**結構上不可達**。鏈頭 `Rw` 由 `55.59%` 降為 `0.58%` ⇒ 其 `G` 由 `0` 變 `4.77㎡` ⇒ 其宗地寬度由 `0` 變 `0.11m` ⇒ **同側其後六宗齊一後移 `0.11m`**，其 `G` 必變（最大一筆 `+0.19㎡`）。發單側漏了 `Rw → G → 宗地寬度 → 後續宗之位置` 之回饋圈。實測：`3.5m` 共同鍵中 `Rw` 相異 `3` 宗、`G` 相異 `2` 宗。
>
> **攔截**：**CC ⛔ 自行放寬判準或改判，而照實出艙「`V-1` 未達」並呈上**——程序正確，且係本則得以現形之唯一原因。
>
> **共相**：閘綁在**代理量**（「其餘各宗之值恆等」）而非其所欲保證之**性質**（「無重新分配、僅整體平移」）。
> 🔑 **與 `自誤 319` 之別**：`319` 係「立驗收而未跑該路徑一次」；本則係「**跑了也不可能過**」——閘本身不可達，非執行之缺。
>
> 🔒 **通則（本則所立）**：`V-1′` 之形 ＝ 「**該側除鏈頭外，`S` 與宗地寬度逐宗不變**」（⇒ 整體平移、非重新分配）**＋** 「`ΣG ＋ 池 ＝ 街廓`」。凡改動落在一條**有順序依賴之鏈**上，⛔ 立「其餘各項恆等」為閘——須先問：本量之改變會不會沿鏈傳遞？

🛑 **⛔ 追改 `W-G.9-261` 單或 `-261R` 報告一字**（`常規四（九）五`）。`V-1` 之「未達」定讞，其更正之唯一載體為本節之 `自誤 324`。

---

## §三　【驗收】

| 閘 | 判準 | 發單側是否已實跑該路徑 |
|---|---|---|
| `V-A` | 十檔 ＋ 夾具之倉側 blob **前後逐位不變**；`git status --porcelain` ＝ `0` 列 | ✅ |
| `V-B` | `run_verification.py` 於本倉現態實跑 ⇒ `RESULT: FAIL`、**紅 `24`**，併出艙**計數式**（`grep -c "🔴 FAIL"`）。🔒 `grep -c "^🔴 FAIL"` 得 `0` 係**框之別、⛔ 態之別** | ✅ 發單側於 `023765c` 實跑得 `24`；`023765c → e9d1b9c` 僅增一份報告 ⇒ 生產碼逐位相同 |
| `V-C` | 自誤登記表之 `deletions` ＝ `0`（`git show --numstat`）| ✅ |
| `V-D` | 落地後 自誤 `MAX 321 → 324`／**相異 `314 → 317`**／`MIN 7`／缺號仍 `[106]`／恆等式 `True`；`GB MAX 154`、`K-9 MAX 29` **不變**。🔒 框 ＝ 定義框 ∪ 範圍框展開（`8` 條攢批標題），母體 ＝ 登記表單檔 | ✅ 已實作並實跑（現態 相異 `314`／`MAX 321`）|
| `V-E` | 三則之標題形皆可被 `VR-091 四` 定義框命中（機驗·出艙命中列號）| 🟡 ⛔ 已跑（其需落地後之檔）——故判準為「機驗後出艙」，⛔ 立具體列號 |
| `V-N` | 號占用出艙依宣告框 `①`–`⑦`：三數分列 ＋ 雙屬列數 ＋ 列框 ∪ ＋ 檔框 ∪ ＋ 讀不到之檔數與副檔名分布 | ✅ 已實作並實跑（現態 `612` 檔·讀不到 `19` ＝ `jpg 9`＋`png 10`）|

🩸 **發單側出艙前自檢**：①本單所書之框（`V-D`／`V-N`）與發單側自己產數之框**同一** ⇒ ✅ 二器皆當場實作；②本單所立之驗收路徑**是否實際執行過一次** ⇒ `V-A`／`V-B`／`V-C`／`V-D`／`V-N` **皆已跑**，`V-E` **未跑**故其判準已寫為過程式。

---

## §四　出艙

- 報告：`docs/reports/W-G.9-263R_交接文入倉與自誤322至324.md`
- commit message 須載：單號 `-263`、**零生產碼**、及所鑄之三號。
- 🛑 **全批零生產碼** ⇒ CC 逕推主線，⛔ 候 KL。
- 🛑 **本批落地後 `262` 與 `263` 皆已占，次可用單號 ＝ `264`**；交接文 `§一` 之「`262` `0/0/0`／`263` `0/0/0`」與 `§二 四`／`§六` 首列隨之成為舊態——**接手窗本即受令「⛔ 採信本文任一值·須自倉重生此表」**，⛔ 追改交接文一字。
- 🛑 **推畢即停機**，⛔ 逕啟 `R4`–`R6`／`②-宗@R5`／`GB-67`／`_b_L0` 任一工項——候新窗之發單側。

SELF_SHA256: 0135ece2c6f0a1cff7211970f6cba6ae31b1f9fcf83074e0817d67a4650611ed
