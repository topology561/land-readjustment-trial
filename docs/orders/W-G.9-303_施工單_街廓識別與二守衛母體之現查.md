# `W-G.9-303`　施工單（**中**）：**街廓識別之取得路徑** ／ **二守衛母體之關係**（現查·⛔ 修批）

> **態**｜開工 ＝ `4bc9c77347416ff474f778d24dcc7dc23132f9a0` **或其後裔**（發單側自倉 `ls-remote` 實查·🛑 CC 須自行重跑·**全 `40` 碼**·⛔ 短前綴）
> **單** ＝ `docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`（工項零 ＝ 本單原封入倉）
> **分級** ＝ **中**（現查 ＋ **`run_verification` 全量一次**·**零生產碼**·**⛔ 開分支**·**⛔ 修批**）
> 🛑 **本批⛔ 鑄任何號**（自誤／`GB`／`VR`／`K-9`／坑 皆⛔）——凡自捕**照實併記於報告**，鑄號**候發單側**。
> 🔴 **KL 之放行於本批<u>仍不動用</u>**；⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；**⛔ 辦修批**。
> 🔑 **本批為修批前之<u>最後一批現查</u>**：其二工項即 `W-G.9-302R` 所遺之**全部**未決前提。
> 辦畢無論其判為何，**修批之受詞即告確定**（`W-G.9-304`），⛔ 再開現查批。
> 🛑 **窗之附款**：`W-G.9-304`（修批）之受詞**直接影響配地面積幾何** ⇒ **⛔ 於飽和窗內為之**。
> 本批辦畢時若本窗 context **≥ `70%`**，須於報告**首行**照實具名其百分比並具名「**建議換窗**」；⛔ 續辦 `304`。
> 🛑 **辦畢 ⇒ 出報告 ⇒ 停**。⛔ 呈 KL。

---

## `§零`　開工閘（十一項·**每列同格載其產生指令**·`自誤 392`·任一不符 ⇒ 停、照實具名、⛔ 續）

| # | 受詞（指令逐字） | **期值／判準** |
|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `4bc9c77347416ff474f778d24dcc7dc23132f9a0` **或其後裔**（全 `40` 碼·坑 `bh`） |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**·`自誤 405`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`·`rc` **緊接該指令**） |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符；`P-3` 不紅；🟡 **逐項具名處置**（預告見 `§零-1`）；判定組為空時**照實具名** |
| `4'` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-303` ＝ `True`／[必偽] 含 `W-G.9-302` ＝ `False` |
| `5` | 同 `4` ＋ `--selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`；preflight 四靶逐項成立 |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 自誤 **`396`**／**`406`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `28`／`29`（缺 `[]`） |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`89`／`89`**·末標籤 **`bk`** ⇒ 下一 **`bl`**（🛑 **等待綁行程結束**·⛔ `\| tail -N`） |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；判別力[必不命中] 子層 **須 `> 0`**（**逐批漂移**·⛔ 定值）；🔑 **本數即本批一切母體之外部錨**（坑 `bj`） |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 `298` |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`** |
| `10` | **六物證落檔**（🔒 **框 ＝ blob `sha1`**·`git rev-parse <rev>:<path>`·**先 `cat-file -e` 驗存在**·三態分列·含 CJK 路徑者**取 bytes**·坑 `bk`） | `WG9295R_joint_v1_selfpolluted.log`／`WG9296R_w0rec_v1_blknone.log`／`WG9297R_w0caliber_v1_blkabsent.log`／`WG9298R_gate3_v1_firstcall.log`／`WG9300R_static_v1_popcollapse.log`／`WG9300R_dynamic_v1_wrongframe.log` ⇒ **逐位未動** |

### `§零-1`　`P-4` 之逐項處置（**發單側已實跑·⛔ 靜默略過**）

🟡 之受詞若屬**三類**⇒ **受具名豁免**：**(甲)** `→` 係**賦值鏈／消費鏈／查表鏈／流程節序**之串接；
**(乙)** 方向性之詞係**序之詞而非幾何**（`前綴`／`後裔`／`首`／`末`／`前`／`後`）；**(丙)** `⇒` 係**推論之連詞**，⛔ 空間之指向。
🛑 三類皆⛔ 幾何向量之方向性轉引 ⇒ **無座標系可具名**。
🛑 **凡實跑所得之 🟡 項不在此三類內者，⛔ 受本豁免，須停、照實具名、⛔ 續**。
🛑 本批凡出艙**幾何向量**之夾角者，一律**釘其槽**（線向／法向）並以**實邊**機驗——該禁⛔ 在本豁免射程內。

🛑 **恆常附款逐項適用·⛔ 省**（坑 `bh`／`bi`／`bj`／`bk`／`bc`／`bd`／`be`／`bf`／`bg`／`g`／`3`、`GB-158` **三形分列**、
簡繁誤字機檢、`rev` 釘死、**先跑再寫**、[必為零]之造須**先自證可滿足**且**判定組為空時 loud 拒測**、
函式名**二形並取**、頂層全域取 `t.body`、`rc` **緊接指令**、**逐閘出艙執行次數**、**等待綁行程結束**、
**`id(·)` ⛔ 作錨**、**⛔ `\| tail -N`**、**讀 blob 取 bytes**、**量測器之母體⛔ 含其自身輸出**、
**帶號與絕對值並列**、**物證落檔保全⛔ 覆寫**、`f_locals` 鍵須**自證存在**並**增第二鍵**、
`git rev-parse <rev>:<路徑>` 未加 `--` 之**回退** ⇒ 先 `cat-file -e`·**三態分列**、
**簿之數一律取器之輸出**、**母體之基數須綁外部錨**、**含 CJK 路徑之 `git` 輸出取 bytes**、
**摘要值一律逐字具名其產生指令**、🆕 **母體一律以<u>產生指令</u>界定·⛔ 取單內之列舉**（`自誤 406`）、
自捕為零者須併具「**⛔ 讀為『本批無誤』**」）。

---

## `§零′`　前批（`W-G.9-302`）之核可（**發單側已辦·⛔ CC 重辦**）

五 `commit`（`3f15fc17`／`d13b33fa`／`7ca8e3ad`／`798df526`／`4bc9c773`）經發單側自倉獨立復驗：
工項零之單 blob 內容 `sha256` ＝ `e226bcf99bca2e5d3a2328942e3b74269e428d4fd49fb04e67d6d1ab527b04d8`
（**＝ 發單側所交付之檔逐位相同**·blob `sha1` ＝ `b1a7d42ed193d267…`·**二框並報**）；
生產碼判法 **`0`** 行（異動 `7` 檔·對照組[必非零] ＝ `2`）；逐 `commit` 刪除欄**全 `0`**；
四簿 自誤 相異 **`396`**／MAX **`406`**（`395 + 1`）·`GB`／`VR`／`K-9` 一字未動；
`app.py` `4379108a…`／`baselines` `298`·`a898f4e1…`／`p3a` `rc = 1`／`299-gb170` 命中 `0` ⇒ **✅ 全批核可**。

🔑 **本批之現查品質，發單側逐項具名其功**：
① 款 `3` 之「其鍵是否含街廓層」**⛔ 以讀來像是判**，而以**讀取處之第二層鍵逐字**（`blk_label` ×`2`／`_lbl` ×`2`）＋ **消費端之 `for` target** 雙路坐實；
② 款 `5` 之**三態分列**精確區辨「`_st_fc` **此名**⛔ 可達」與「其**所指之物**可經 `ns` 之鍵 `st` 解析」，並**⛔ 由此推可施**、明言⛔ 新增 `import` 之禁仍在；
③ `停五` 二合取項皆為偽後，**主動併記**其所防之形於本表結構下不成立之**由**，並**⛔ 由此推可施**，指回 `W-G.9-301R` 五-5 之受阻處；
④ 款 `2` 三形對 `ast.Dict` 鍵字面（`4` 列）之**結構性盲區照實具名**且**⛔ 逕改該框**（單未授權）——此為**框之自限之正確體例**；
⑤ 款 `6` 之**併記**（`_first_corner_alloc_dir` 之唯一呼叫端守衛係 `is_corner`、**⛔ `forced` 族**）係本批**最有價值之出艙**——本單工項二即以之為受詞。

---

## `§一`　裁（**發單側·⛔ CC 自裁**）

| # | 事 | **裁** |
|---|---|---|
| `1` | `停五`／`停六` 皆未成就 | ✅ **判成立·CC 之處置完全正確**。二者皆繫於性質、合取項分列、⛔ 由未成就推「可施」——**逐項如令** |
| `2` | 🔑 **所遺之未決前提** | 經發單側逐項盤點，`W-G.9-302R` 後**僅餘二事**：**(一)** 於 `_corner_buffer_S` 之體內，**層① 之鍵（街廓識別）**能否**可信**取得（`W-G.9-301R` 五-5 已坐實 `7` 處中 `3` 處之 `_label` 為複合字串·且受詞函式之 docstring 逐字禁以字串標籤查表）；**(二)** `_first_corner_alloc_dir` 之守衛 `is_corner` 與 `_corner_buffer_S` 之 `7` 處**全為 `forced`** 二母體之**關係**。⇒ **本單二工項即此二事·⛔ 逾** |
| `3` | 🔑 **(二) 之土地意義（發單側具名·⛔ CC 推定·⛔ 據以擬改法）** | 路丙之取線係「**街角第 `1` 宗之臨街向**」——其**預設**該強制抵費地所在之街廓**有**街角第 `1` 宗。而現行碼中求該向之函式，其唯一呼叫端之守衛係 `is_corner`、⛔ `forced`。⇒ **二母體若不相等，則於「有強制抵費地而無街角第 `1` 宗」之街廓，路丙<u>無線可取</u>**。🛑 **此係<u>域</u>之問題**（該情形下裡側界線應取哪一條），**⛔ 由發單側或 CC 裁**；其呈 KL 與否候本批之數 |
| `4` | 🛑 **本批之界** | **⛔ 判「可施」或「不可施」**、⛔ 擬任何改法或 diff、⛔ 呈 KL。二工項**只出艙**；孰為修批之受詞由**發單側**依本批之數裁定 |
| `5` | `停四` 之地位 | **於其母體下仍成立且留倉不改**；⛔ 判其為誤 |
| `6` | `GB-170` | **⛔ 解除、⛔ 收窄、⛔ 提修法主張**；失效條件 `(2)`／`(3)` 仍未成就。本批**⛔ 追加登記**（其登記候 `W-G.9-304`） |

---

## `§二`　工項零：**本單原封入倉**（主線）

落點 ＝ `docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`；**⛔ 改一字**。
出艙：**受詞自證**（坑 `bf`·[必真] 含 `W-G.9-303`／[必偽] 含 `W-G.9-302`）；檔數 ＝ `1` 之自證（造甲[必≥1] ＝ `W-G.9-302` 之單／造乙[必為0] ＝ **執行期組出**之人造名·字面⛔ 出艙）；
`CR` ＝ `0`（**讀 blob 取 bytes**；判別力[必非零] ＝ `data/V6.dxf`）；**blob `sha1` ＋ blob 內容 `sha256` ＝ 磁碟 ＝ 源**（🔒 **二框並報**）；`deletions` ＝ `0`。

---

## `§三`　工項一：**街廓識別於 `_corner_buffer_S` 內之可信取得**（主線·**零生產碼**）

### 三-1　逐款出艙（**母體一律以<u>產生指令</u>界定**·`自誤 406` 攔法 ②·⛔ 取本單之列舉）

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | 🔑 **層① 之鍵之<u>寫入端</u>**：二寫入處（`app.py::main`／`verify/run_verification.py::build_pipeline`）所建之 `f3_cad_side_lines_by_side` 表，其**層① 鍵之值自何而來**——逐處溯其 AST（`Dict` 之鍵字面／推導式之 `target`／變數之 `Store` 來源），**逐層出艙至<u>其源頭之名</u>** | 🛑 **款 `2` 之三形於 `W-G.9-302R` 已具名其對 `ast.Dict` 鍵字面之結構性盲區** ⇒ **本款之框須<u>另立</u>並涵蓋 `ast.Dict`／`ast.DictComp`**（**四形分列並報**：`Subscript`／`Assign`／`.get`／`Dict`-`DictComp`）；⛔ 沿用該盲框 |
| `2` | 🔑 **`_corner_buffer_S` 體內可解之名中，語義為「街廓識別」者之全量**：母體 ＝ **`ns` 之全部鍵**（＝ `app.py` 之 `t.body` 頂層名·**基數由指令得**·🛑 **本批自行重得**·外部錨見 `§零` 閘 `7`）＋ `_corner_buffer_S` 之**八個形參**。框 ＝ 名之字樣（`blk`／`label`／`lbl`／`block_id`·**四字樣分列並報**·⛔ 合併） | **全量列舉**其命中之名（⛔ 只報基數）；逐名出艙其**為何物**（頂層敘述逐字／形參）；判別力[必為零] ＝ 一**執行期組出**之人造字樣命中 `0`（字面⛔ 出艙） |
| `3` | 🔑 **`_label` 七處實參之<u>溯源</u>**：`W-G.9-301R` 五-5 所載之四個實參式（`blk_label` ×`4`／`f'{lbl}·F.1'`／`f'{blk}·E3'` ×`2`）——逐處於**其所在函式內**溯 `blk_label`／`lbl`／`blk` 之**賦值來源**（AST `Store`·含 `For` 之 `target`／解包），出艙其**是否即層① 之鍵之同一物** | 🛑 **⛔ 以名同判物同**——須出艙其賦值之**產生式逐字**並與款 `1` 之源頭比對；不可判者一律 **loud 具名為「⛔ 可判」**·⛔ 判為「是」或「否」 |
| `4` | 🔑 **既有之「幾何 → 街廓識別」映射之全量**：母體同款 `2`（`ns` 全部鍵）。框 ＝ 各鍵若為 `FunctionDef`，其**形參名序**含 `block_poly`／`poly`／`geom` 類且其**回傳**（`Return` 之 `value` 逐字）含款 `2` 之字樣者 | **全量列舉**（⛔ 只報基數）；🛑 **⛔ 自寫第二套幾何**——只現查既有者；凡無命中者 **loud 具名為「母體內無此映射」**並出艙母體基數 |
| `5` | **判別力四造** | [必為 `34`] 檔母體之外部錨（坑 `bj`）／[必非零] `ns` 鍵基數 `≥ 1`（併報其值）／[必為零] **執行期組出**之人造字樣於款 `2` 之框下命中 `0`／[必真] `st` 在 `ns` 之鍵 ＝ `True`（`W-G.9-302R` 款 `5` 之獨立復現） |

🛑 **本工項⛔ 改生產碼一字、⛔ 開分支、⛔ 擬任何改法或 diff、⛔ 判「可信」或「不可信」**——只出艙逐名之來源與其比對結果。

---

## `§四`　工項二：**二守衛母體之關係**（主線·**零生產碼**·🔑 **含 `run_verification` 全量一次**）

🔑 **本工項<u>明文授權</u>二項手段**（`自誤 384` 攔法·射程逐字·⛔ 逾之）：
① **執行 `run_verification` 全量一次**（供款 `3` 之動態量測·**⛔ 改生產碼一字**·**等待綁行程結束**·⛔ `| tail -N`）；
② **於 `verify/probes/` 新增量測器**（**⛔ 動既有探針一字**）。

### 四-1　逐款出艙

| 款 | 受詞 | 附款 |
|---|---|---|
| `1` | **`is_corner` 之定義與求值來源**：母體 ＝ 生產碼 **`34`** 檔（**正面列舉**·外部錨）。框 ＝ AST（該名之 `Store` 全量 ＋ 其 `Load` 全量·**二形分列並報**）＋ 文字層列框以資對照 | **二數並報**·相異即 loud 具名其成因；逐處出艙 `檔 :: 函式` ＋ **字樣錨**（⛔ 行號為錨）＋ 其**賦值產生式逐字**；🔑 **明確回答一問**：其受詞係**逐宗**抑或**逐街廓**（以其賦值式所消費之名判·出艙該名之逐字） |
| `2` | **「街角第 `1` 宗」之判準於碼面之載體**：母體同款 `1`。框 ＝ 名之字樣（`corner`／`first`／`_fc`·**三字樣分列並報**）之 `FunctionDef`／頂層 `Assign` | **全量列舉**；⛔ 由發單側預列其名（`自誤 406`）——字樣由本款之框生成 |
| `3` | 🔑 **二母體之關係（動態·本案）**：於授權之 `run_verification` 全量跑中，逐出艙 **(甲)** `_corner_buffer_S` 實走之 `7` 處中各處之**街廓×側**（`W-G.9-300R` 已得 `F = {S3}`·`side = ['left']`·`_label = ['R2','R4']`·🛑 **本批獨立重得**）；**(乙)** 同一跑中 `is_corner` 為**真**之**街廓×側**之全量；**(丙)** **(甲) ∖ (乙)** 與 **(乙) ∖ (甲)** 逐項列舉 | 🛑 **量測器之母體⛔ 含其自身輸出**（生產錄／器自身**二欄分列**·後者須 `0`）；🛑 **`id(·)` ⛔ 作錨**（以 `co_name`／`co_filename` 序辨呼叫端）；🛑 **(乙) 若取法不可得，一律 loud 具名其<u>不可得之分項成因</u>·⛔ 判為零、⛔ 判為空**（`自誤 401` 攔法） |
| `4` | 🔑 **(甲) ⊆ (乙) 之判**（**只出艙·⛔ 判其後果**） | 三態分列：① **(甲) ⊆ (乙)**（本案無「有強制抵費地而無街角」之格）／② **(甲) ⊄ (乙)**（有之·**逐格列舉**）／③ **(乙) 不可得** ⇒ 依款 `3` 之附款 loud 具名。🛑 **⛔ 判其對土地之後果、⛔ 擬任何 fallback、⛔ 呈 KL** |
| `5` | **判別力四造** | [必為 `34`] 外部錨（坑 `bj`）／[必非零] 款 `1` 之 `Store` 處數 `≥ 1`／[必為零] **執行期組出**之人造名於款 `1` 之框下命中 `0`／[必相異] 款 `1` 之 AST 數與文字層數**各自出艙**（⛔ 只報其一） |

🛑 **本工項⛔ 改生產碼一字、⛔ 開分支、⛔ 動用 KL 之放行、⛔ 辦修批、⛔ 擬任何改法或 diff。**
🔒 **四禁仍在**（`W-G.9-301 §六-3` 逐字）：**⛔ 新增形參、⛔ 改簽章、⛔ 新增 `import`、⛔ 自寫第二套幾何**。

---

## `§五`　🛑 **⛔ 為之事**

🔑 **二項手段<u>已明文授權</u>**（射程逐字見 `§四` 首段·⛔ 逾之）：`run_verification` 全量**一次**；`verify/probes/` **新增**量測器。
其餘：
⛔ 改生產碼 `34` 檔**一字**；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 辦修批；
⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；**⛔ 引任何自 baseline 取得之數**；
⛔ 改任何函式簽章、⛔ 新增任何形參、⛔ 新增任何 `import` 於生產碼、⛔ 改任何 `*_EXPECT` 一字；
⛔ 自寫第二套幾何或第二份 `W`／`side_mid`／側界線／街廓識別之定義（`GB-48` 族）；
⛔ 動 `ALPHA_LABELS` 一字；⛔ 動 `probe_WG9269_pit_index.py`／`probe_WG9292_pathC.py`／`probe_WG9295_joint.py`／`probe_WG9298_gate3.py`／`probe_WG9300_contact_surface.py`／`probe_WG9301_selfderive.py`／`probe_WG9302_sidesrc.py`／任何**既有**探針一字；
⛔ 鑄自誤／`GB`／`VR`／`K-9`／坑之**任何號**（本批**零鑄號**）；⛔ 追加任何 `GB` 之末端登記（候 `W-G.9-304`）；
⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 逕修 `side_mid` 之在域性；⛔ 逕修坑之定義框；⛔ 逕改 `W-G.9-302` 款 `2` 之盲框（本單另立新框於 `§三` 款 `1`）；
⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項；
⛔ 追改 `W-G.9-299`〜`-302`／補令一／交接註／`W-G.9-300R`〜`-302R` 或任何已入倉文件一字；
⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文；
⛔ 判 `GB-170` 可否解除、⛔ 判路丙之落點可施與否、⛔ 判 `停四` 為誤、⛔ 判「(甲) ⊄ (乙)」之土地後果、⛔ 擬任何 fallback、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張；
⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制；
⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述）。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加）

| # | 受詞（指令） | 期／判準 |
|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | 刪除欄**全 `0`** |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `17fd981^..17fd981` ＝ `≥ 1` |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·含 CJK 路徑者取 bytes·坑 `bk`） | `0`；判別力[必非零] ＝ `data/V6.dxf`（`CR` `12308`／全長 `88341` B） |
| `4` | 四簿期末（**器之輸出**） | 🔑 **零鑄號**：自誤 相異 **`396`**／MAX **`406`**；`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **皆一字未動** |
| `5` | 坑之期末（**器之輸出**） | **`89`／`89`**·末標籤 **`bk`** ⇒ 下一 **`bl`**（⛔ 鑄坑）；`ALPHA_LABELS` 之 `git diff --numstat` 輸出**空**；母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 檔數（**自洽檢**） |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必相異] ＝ 本批確有異動之檔）；`app.py` ＝ `4379108a…`（**框 ＝ blob `sha1`**）；`baselines` `298` 檔·`a898f4e1…`；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**六物證落檔 ＋ 本批新增者**之 blob **逐位未動**（**先 `cat-file -e` 驗存在**·三態分列·**框逐字具名**） |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**（[必為零]）；判別力[必存在] `p3a` 命中 **`1`** |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** |

---

## `§七`　報告之格

🛑 **報告之<u>首行</u>**：本窗 context 之百分比（照實）＋ 其是否 `≥ 70%` 之判；若是，具名「**建議換窗**」（單首段之窗附款）。
其次：`§零` 開工閘（**每列同格載其產生指令**）→ `§零′` 之⛔ 重辦 → 逐工項（逐格表以落檔**全量**出艙·⛔ `\| tail -N`）→
`§四` 款 `4` 之**三態分列**判 → 各判別力造之判 → 一句話推導 → 收工閘（**入倉後末端追加**·閘 `7` **出艙於回報·⛔ 入倉**）→
`⛔ 為之事` 之逐項自檢 → 逐 `commit`（**全 `40` 碼**）。
🔒 凡出艙之數，同格載其**框、母體與產生指令**；**母體一律以產生指令界定**（`自誤 406`）；**摘要值一律逐字具名其產生指令**（`自誤 405`）；
框無 `^` 錨者**二數並報**；**帶號與絕對值並列**；**母體之基數綁外部錨**（坑 `bj`）。
🔒 判定組為空一律 **loud 拒測**並**照實具名**；自捕為零者須併具「**⛔ 讀為『本批無誤』**」。
🔒 凡自捕一律於出艙前攔下、照實併記（**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）；物證落檔**保全⛔ 覆寫**。
🔒 **全量列舉之款（`§三` 款 `2`／`4`·`§四` 款 `2`）⛔ 只報基數**——須逐名出艙；無命中者 **loud 具名**並出艙母體基數。

---

【驗收】本批之驗收固定項（**⛔ 推後·⛔ 以「未觸」充綠**）：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`deletions`／`cat-file`／
`merge-base`／`ALPHA_LABELS`／`run_verification`／`co_filename`／
`f3_cad_side_lines_by_side`／`_corner_buffer_S`／`_first_corner_alloc_dir`／`is_corner`／`side_mid`／
`blk_label`／`block_poly`／`DictComp`／`GB-170`／`baselines`／`numstat`／`sha256`／`ls-tree`／`WV_BAKE`。

【單止】本單受詞 ＝ CC 窗。發單側 ＝ `claude.ai`。**⛔ 呈 KL。**

SELF_SHA256: 37c6c848bc9dd757f69cf3178a02b5ce55d52b4aa2bdd1b60bc4dc074dad4d19
