# 🔧 `W-G.9-199` 施工單：**四自誤（`249`〜`252`）＋ 裁 `H`（`V-6` 型判準之形）＋ `GB-135` 編號現查之更正加註 ＋ 常規補款（閘與其守護之動作須分離）**

> **性質**：🟢 **全單零生產碼** ⇒ **CC 可逕行 `push`**。
> **出單窗**：claude.ai（接 `W-G.9-197R`）
> **基座**：`wip/s1-endpart`（🛑 開工時自行 `git ls-remote` 實查全 `40` 碼並記錄；判準見 `S-0a`）

🔒 **給 CC 之通則**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

🔑 **事由**：`W-G.9-197R` 之二項「與單載不符」與一項「呈裁」，經發單側**自倉獨立復驗全數坐實**，並據以定位**四則發單側自誤**。🛑 **CC 之處置逐項正確**——其**未自鑄號**、未追改單一字、以「逐筆歸類」定 `GB-135` 可取，皆合 `常規二`／`常規三`。

---

## §零　發單側之獨立復驗（⛔ 非採信 `W-G.9-197R` 自報·態 `79cfa79`）

| 受詞 | 發單側自倉實測 | 對 `197R` |
|---|---|---|
| `HEAD` | `79cfa794b2059855448c51e547d9ac53ab4b20fe` | ✅ |
| `HEAD:app.py` blob | `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`（＝ `7fab020` 之值） ⇒ **生產碼未動** | ✅ |
| `git diff --numstat 7fab020..HEAD` | 六檔·`deletions` **逐檔 `0`** | ✅ |
| **`V-3`** | 自本單前身 `§二-1`〜`§二-7` 抽出之**七** fence，**逐則逐位落入**其目標檔 ＝ `7`／`7` | ✅ |
| `V-4` | 自誤 相異 `213`／**MAX `248`**；`GB`（扣哨兵）**MAX `136`** | ✅ |
| `V-5` | 二證據檔在 `ls-files`；🔑 **CSV 與 KL 上傳原檔<u>逐位元組相同</u>**（`2841` B／`\n` `21`【次】／`CR` `0`／`sha256` `a2d8ffd6…`）；`stdout.log` `141512` B／`\n` `2527`／**`CR` `2527` 保全**／`sha256` `ae1463bd…` | ✅ |
| `V-7` | `KL_UI_3.5m_*_stdout.log` ＝ **`4`【檔】**，`StreamlitValueBelowMinError` 逐檔 **`1`【列】** | ✅ |
| 🔴 `GB-134` 意指占用 | 母體 ＝ `git ls-tree -r 7fab020` 之 `.md` **`491`【檔】**（追蹤檔 `1554`）⇒ **`15`／`15`【列】** | 🔴 **單載 `18` ⇒ 單誤** |
| 🔴 `GB-135` 意指占用 | 同母體 ⇒ **`3`／`3`【列】**；三筆逐筆 ＝ `W-G.9-195_施工單:59`／`W-G.9-194R_執行報告:32`／`W-G.9-195R_執行報告:123`，**逐筆皆為判別力對照／零對照** ⇒ 依 `CLAUDE.md`「🔧 二」⛔ 不構成占用 ⇒ **`135` 可取** | 🔴 **單載 `0`／`0` ⇒ 單誤**；CC 之歸類與結論 ✅ |
| 🔴 `V-6` 恆假 | 單 `:269` 之 `V-6` 宣告列逐字含 `` 未鑄之 `自誤 249` 與 `GB-137` ``**⇒ 該列自身即命中源** | 🔴 CC 所指成立 |

---

## §零-b　開工閘

| 閘 | 受詞 | 判準 |
|---|---|---|
| `S-0a` | 基座之同一性 | 🔒 **綁性質·⛔ 不綁特定 commit**（承 `自誤 246` 之一般式）：① `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**；② `git diff --name-only 7fab020..HEAD` **逐列⛔ 不含** `app.py`、⛔ 不含任何 `verify/**.py`。二者任一不符 ⇒ 停機上呈。🛑 `ls-remote` 之全 `40` 碼**須記錄並回報**，但⛔ **不作為判準** |
| `S-0b` | `probe_order_preflight.py` 對本單 | `P-3`／`P-5` 須不紅；🟡 逐項具名（`§零-c`） |
| `S-0c` | 本單 `SELF_SHA256` 自驗 | `declared` ＝ `recomputed` |
| `S-0d` | 🔴 **量測與動作之分離** | 🛑 **`§四` 之各驗收閘與 `commit`／`push` ⛔ 不得置於同一不可中斷之命令塊**（本單 `§五 常規補款 一` 之逐字）。CC 須回報二者係分開呼叫 |

## §零-c　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6`）

（見 `§六`：發單側於出單前實跑，逐項具名於該節。）

---

## §一　取號現查（發單側·態 `79cfa79`）

**母體**　`git ls-tree -r --name-only HEAD` 之 `.md` ＝ **`493`【檔】**（含本波新入之單與報告二檔）。
**自誤**　登記表單檔·框 `` ^#+ 自誤 `?([0-9]{1,4})`? `` ⇒ 相異 `213`／MAX **`248`** ⇒ 次四號 ＝ **`249`／`250`／`251`／`252`**。

**意指占用**（錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·二形並取·框 ＝ **列**）：

| 號 | 無引號形 | 帶引號形 | 逐筆歸類 |
|---|---|---|---|
| `自誤 249` | **`4`** | **`1`** | 🔒 **五筆逐筆皆為判別力對照／零對照**：`docs/orders/W-G.9-197_…:269`（`V-6` 宣告列本身）；`docs/reports/W-G.9-197R_…:139`／`:140`／`:145`／`:149`（皆為 `V-6` 之實測與其扣除後受詞）⇒ 依 `CLAUDE.md`「🔧 二」**⛔ 不構成占用** ⇒ **`249` 可取** |
| `自誤 250` | `0` | `0` | — |
| `自誤 251` | `0` | `0` | — |
| `自誤 252` | `0` | `0` | — |

🔑 **對照組** `自誤 245` ＝ `6`／`9`【列】、`GB-134` ＝ `15`／`15`【列】 ⇒ 量測器非紅。
🛑 **本單⛔ 鑄任何 `GB` 號。**

**範本**（`常規四（九）四`·現查之最末一則標題形逐字）：
`` ### 自誤 `248`（發單側·`W-G.9-197` 出單窗）：**出艙一項「落地須同時滿足二個生效時點」之要求，而該二時點之一，倉內<u>生產碼註解</u>早已載明其係「循環依賴·待 KL 裁」並已裝有保守替代** ``

---

## §二　payload（🛑 逐字追加·⛔ 不改一字）

### §二-1　`L-1`　`自誤 249`　🔴 **本輪最重之一則**

```
### 自誤 `249`（發單側·`W-G.9-197` 出單窗）：**出艙一個計數，其<u>受詞</u>⛔ 非產生該數之那一次量測的受詞——即以「量過 A」之數，冠上「量過 B」之名**

**案由**　`docs/orders/W-G.9-197_施工單_三自誤與GB133補記與GB135_136與KLUI證據.md` `§一` 逐字 ＝ 「`GB-135` ＝ `0`／`0`；`GB-136` ＝ `0`／`0`」。
🔴 **現查（`W-G.9-199` 發單側·母體 ＝ `git ls-tree -r 7fab020` 之 `.md` `491`【檔】·錨定框·二形並取·框 ＝ **列**）**：`GB-135` ＝ **`3`／`3`**；`GB-136` ＝ `0`／`0`。⇒ **`GB-135` 之出艙值⛔ 不可能來自任何一次對 `GB-135` 之量測**。

🔒 **證（反證·⛔ 不依賴回憶）**：發單側當時之工作樹**額外含二份未入倉之草稿單**（`W-G.9-197`／`-198`），而該二檔**各自含 `GB-135` 之命中** ⇒ 任何在該工作樹上對 `GB-135` 之量測**只會多於 `3`、⛔ 不可能得 `0`**。⇒ 該 `0`／`0` **必來自另一個受詞**（同批確曾實測且確為 `0`／`0` 者 ＝ `GB-136`／`GB-137`）。

🔴 **其危害⛔ 非「數字錯了」**：該數**以已量測者之形出艙**（同段並列於真實量測值之間、附對照組），使受單方⛔ 無從自單面辨識其未經量測。🔑 **攔截者 ＝ CC**——其於開工閘後之取號現查中實測得 `3`／`3`，逐筆歸類為零對照，判 `135` 仍可取、⛔ 未自鑄號、⛔ 未追改單一字，並具名回報「與單載不符」。**若 CC 逕採單載，此數將以「已驗」之姿長存於 `GB` 登記表。**

🔒 **⛔ 無土地後果**（受詞為編號占用）。⛔ **結論⛔ 不變**：三筆皆零對照 ⇒ `GB-135` 可取。

🩸 **同族**　與 `自誤 247` 同族（**出艙之數與其宣告不同源**），惟 🔴 **本則更重一階**：`247` 之數係**真有量過、只是框／母體標錯**；本則之數係**根本未對該受詞量過**。⇒ `247` 之一般式（「同時宣告母體與框」）**⛔ 不足以攔下本則**——母體與框寫得再全，若該次量測之受詞根本不是它，仍不可復現。

🔒 **一般式（`247` 之補強·三重）**　凡出艙任何計數者，須 ① 宣告**母體**；② 宣告**框**；③ 🔴 **宣告產生該數之<u>命令逐字</u>**（可貼可重跑者），且該命令之**受詞須與該數之標籤字面相同**。🛑 **自檢法**：把每一個出艙之數與其命令並排，逐則問「這條命令的受詞，是不是就是這個標籤」。⛔ **不得**在同一段落內、以同一批次量得之數填入未量之欄。
```

### §二-2　`L-2`　`自誤 250`

```
### 自誤 `250`（發單側·`W-G.9-197` 出單窗）：**出艙一個對照組計數，其母體含<u>本窗自身尚未入倉之草稿檔</u>——與同單所宣告之母體（`git ls-files`）不同源**

**案由**　`W-G.9-197` `§一` 逐字 ＝ 「對照組 `GB-134` ＝ `18`【列】」，而同段所宣告之母體逐字 ＝ 「全倉 `.md` `491`【檔】」。
🔴 **現查**：母體 ＝ `git ls-tree -r 7fab020` 之 `.md` `491`【檔】 ⇒ `GB-134` ＝ **`15`／`15`**【列】。差 `3` ＝ 發單側工作樹內**二份未入倉之草稿單**所含之命中。

🔒 **⇒ 該 `18` 之母體實為「`491` 追蹤檔 ＋ `2` 未追蹤草稿」＝ `493`【檔】**，⛔ 非同段所宣告之 `491`。🔑 **本則之形**：**發單側在自己的工作樹上量倉**——草稿檔對量測器而言與已入倉者無異，而受單方之工作樹⛔ 無該二檔 ⇒ **同一條命令在二端必得不同數**。

🔑 **攔截者 ＝ CC**（實測 `15`／`15` 並具名回報）。🔒 **結論⛔ 不受影響**（非零 ⇒ 量測器非紅仍成立）。🔒 ⛔ 無土地後果。

🩸 **同族**　`自誤 241`／`243`／`244`／`247`（**出艙之數與其框／母體不同源**）⇒ **族內第七度**。

🔒 **一般式**　發單側凡於**自己的工作樹**上量倉者，其母體**須以 `git ls-tree -r <態>` 或 `git ls-files` 取得**，⛔ **不得**以 `grep -r … .`（該形恆含未追蹤檔）。若必須用後者，須於同段**同時報未追蹤檔清單**（`git status --porcelain`）並自宣告其已計入。
```

### §二-3　`L-3`　`自誤 251`

```
### 自誤 `251`（發單側·`W-G.9-197` 出單窗）：**寫下一條<u>恆假</u>之判別力對照閘——宣告「此二號之命中須為 `0`」的那一句話，自身即是它們的命中**

**案由**　`W-G.9-197` `§四` 之 `V-6` 逐字 ＝ 「未鑄之 `自誤 249` 與 `GB-137` 之錨定框全倉 `.md` 命中**皆 `0` 列** ⇒ `False`。若非 `False` ⇒ 量測器紅、停機」。
🔴 **該列自身含 `` `自誤 249` `` 與 `` `GB-137` `` 二字樣，且該列隨本單入倉** ⇒ 落地之際命中即 ≥ `1` ⇒ **`V-6` 結構上不可能成立**。實測（`197R`）：`自誤 249` ＝ `1`／`0`、`GB-137` ＝ `1`／`1`。

🔴 **根因⛔ 是母體，而是判準之<u>形</u>**：`V-6` 欲保證之**性質**係「該號**尚無實質占用**」，而所寫之**判準**係「命中 ＝ `0`」——**後者係前者之代理**。倉內早有正解：`CLAUDE.md`「🔧 二」令**零對照／判別力對照⛔ 不構成占用**，`W-G.9-195` `§一` 即照此寫（報 `1` 命中 ＋ 逐筆歸類 ＝ 零對照 ⇒ 判可取）。**發單側棄該正解不用，改寫成裸計數。**

🔑 **攔截者 ＝ CC**（呈裁「`V-6` 型之母體應否明文扣除本單」）。🛑 **CC 之呈裁受詞須予更正**——見**裁 `H`**：⛔ **不扣母體**，**改判準之形**。

🔒 ⛔ 無土地後果。

🩸 **同族**　`自誤 245`（**寫下一條結構上不可滿足之驗收判準，使該閘恆真**）之**鏡像**——`245` 恆真、本則恆假；且二者之根因同一：**把判準綁在代理上，而非綁在它要保證的性質上**。⇒ **`245` 之一般式第二度**。
🔴 **本則之特殊處**：發單側在**同一張單**內既引 `245` 為範本、又同時犯其鏡像。

🔒 **一般式**　凡寫「某物之命中須為 `N`」之閘者，須先問「我要保證的**性質**是什麼」，並將判準直接綁在該性質上。**判別力對照之正確形** ＝ 「該號之命中**逐筆歸類**皆為零對照／判別力對照」，⛔ **不得**寫成「命中 ＝ `0`」。🛑 **自檢法**：把該判準之文字本身放進它自己的母體，問「這句話會不會使自己為假／為真」。
```

### §二-4　`L-4`　`自誤 252`

```
### 自誤 `252`（發單側·`W-G.9-198` 出單窗）：**在<u>登記 `自誤 246` 之同一批</u>內，於下一張單寫下一條繫於特定 commit 之開工閘——即 `246` 一般式所明文禁止者**

**案由**　`W-G.9-198`（🛑 出單時⛔ 未入倉）`§零-b` 之 `S-0a` 逐字 ＝ 「須與 `W-G.9-197R` 所報之 commit **同一物件**（判準 ＝ 全 `40` 碼 ∧ `tree` ∧ `numstat` ∧ `app.py` blob …）」。
🔴 **該判準之第一合取項繫於一個特定 commit**；而 `自誤 246` 之一般式（同批鑄入）逐字令：「閘之判準寫成**保全其立法理由**之形…⛔ **不得**寫成繫於特定父 commit 之形——後者隨任何一次合法之序調整即失效」。
🔴 **且其失效已<u>實際發生</u>**：本批（`W-G.9-199`）係一次**合法之零生產碼插入** ⇒ `HEAD` 已⛔ 非 `W-G.9-197R` 所報之 commit ⇒ 若 `-198` 依原文開工，`S-0a` **必紅**，而其所欲保全之性質（**生產碼自 `7fab020` 起未動**）**實際上完好無損**（`HEAD:app.py` blob 仍 ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`）。

🔒 **立法理由逐字** ＝ 「`W-G.9-197` 係零生產碼 ⇒ blob 須未變」——⇒ **正確之判準 ＝ 該 blob ＋「`7fab020..HEAD` 之變更檔逐列不含 `app.py`／`verify/**.py`」**，⛔ 與 commit 之身分無關。

**攔截者 ＝ 本窗自查**（於起草 `W-G.9-199` 而察覺其將使 `-198` 之 `S-0a` 失效時）。🔒 ⛔ 無土地後果（`-198` ⛔ 未入倉、⛔ 未開工）。`-198` 已改版重發，`S-0a` 改綁性質。

🩸 **同族**　`自誤 241` 之一般式「**立戒者在同一批就會違反自己的戒**」⇒ **第四度**。
🔴 **本則之特殊處**：所違反者係**同一批剛鑄入之自誤本身之一般式**，且違反處**距其鑄入不足一檔**（`-197` 與 `-198` 同時交付）。⇒ **一般式寫在紙上⛔ 不等於它在下一張紙上生效**。

🔒 **一般式（`246` 之施行細則）**　凡出單者，於**交付前**須對本單之**每一個開工閘**逐條問：「若在本單開工前，倉內合法地多了一個**零生產碼** commit，本閘會不會紅？」**會紅者即為繫於 commit 身分之閘**，須改綁性質。🛑 該自檢須於**同時交付多張單**時逐張執行——後單之基座恆有被前單以外之批插入之可能。
```

### §二-5　`L-5`　`GB` 登記表**檔尾**追加：`GB-135` 編號現查之更正加註（🛑 ⛔ 不刪原句一字·⛔ 鑄號）

```
## 🔴🔴 `GB-135` 編號現查之更正（`W-G.9-199`·⛔ 上文原句一字未刪）

🛑 **本節⛔ 鑄任何號。** 受詞 ＝ `GB-135` 本則「編號現查」段之末句。

**原句逐字** ＝ 「錨定框 `` (?<![0-9\-])GB-135(?![0-9]) `` 全倉 `.md`（`491` 檔）二形並取皆 `0`【列】 ⇒ ⛔ 無占用；對照組 `GB-134` ＝ `18`【列】 ⇒ 量測器非紅。」

🔴 **二數皆誤**（`W-G.9-197R` 實測 ＋ `W-G.9-199` 發單側獨立復驗·母體 ＝ `git ls-tree -r 7fab020` 之 `.md` **`491`【檔】**·追蹤檔 `1554`·錨定框·二形並取·框 ＝ **列**）：

| 受詞 | 原載 | **實測** |
|---|---|---|
| `GB-135` 意指占用 | `0`／`0` | **`3`／`3`** |
| 對照組 `GB-134` | `18` | **`15`／`15`** |

🔒 **`GB-135` 三筆命中之逐筆歸類**：`docs/orders/W-G.9-195_施工單_四裁與d46dfe6放行.md:59`／`docs/reports/W-G.9-194R_GB133解除與攢批登記_執行報告.md:32`／`docs/reports/W-G.9-195R_四裁與d46dfe6放行_執行報告.md:123` ⇒ **逐筆皆為判別力對照／零對照** ⇒ 依 `CLAUDE.md`「🔧 二」**⛔ 不構成意指占用**。

🔒 **⇒ 本則之結論（`135` 可取·⛔ 不順延）⛔ 不變；`GB-135` 之阻塞項內容、觸發條件、現況處置、失效條件⛔ 全部不變。** 更正者**僅為該二個計數及其「⛔ 無占用」之推導形式**——正確之推導係「**三筆逐筆歸類皆零對照**」，⛔ 非「命中為 `0`」。
🩸 成因已登記為 `自誤 249`（受詞錯置之量測）與 `自誤 250`（母體含未入倉草稿）。
```

### §二-6　`L-6`　`CLAUDE.md` 檔尾追加

```
## 🔒 `W-G.9-199` 接手窗：裁 `H` ＋ 常規補款（`2026-09-01`）

### 裁 `H`：**判別力對照之判準⛔ 得寫成「命中 ＝ `0`」——須綁「逐筆歸類」**

🔴 **事由**：`W-G.9-197` `§四` 之 `V-6` 逐字宣告「未鑄之 `自誤 249` 與 `GB-137` … 命中皆 `0` 列」，**該宣告列自身即是二者之命中** ⇒ 閘**恆假**（`自誤 251`）。CC 呈裁「`V-6` 型之母體應否明文扣除本單」。

🔒 **裁**：
1. 🛑 **母體⛔ 得扣除本單。** 扣除將產生新盲區——若某新號**恰好只在本單被實質使用**（如誤植、誤引），扣除後即⛔ 不可見。母體恆為 `git ls-tree -r <態>` 之 `.md` 全集。
2. 🔒 **改判準之形**：判別力對照之 operative 判準 ＝ 「**該號之命中<u>逐筆歸類</u>皆為零對照／判別力對照**」（依「🔧 二」）。**命中數本身⛔ 非判準，係<u>待歸類之材料</u>**——其為 `0` 或非 `0` 皆⛔ 不直接致紅。
3. 🛑 **⛔ 得只報數而不歸類**：凡命中非 `0` 而未逐筆具名其歸類者 ⇒ **停機**。
4. 🔒 **量測器非紅之自證仍須保留**（對照組須為非零），且其對照組亦須逐筆可歸類。

🔒 **一般式**：凡驗收閘欲保證某**性質**者，判準須直接綁該性質；⛔ 不得綁其**計數代理**。承 `自誤 245`（恆真）與 `自誤 251`（恆假）之同根。

### 常規補款 `一`：**閘之量測與其所守護之動作⛔ 得置於同一不可中斷之命令塊**

🔴 **事由**（`W-G.9-197R` `二項自陳 ①`·**CC 自陳·⛔ 非發單側所指**）：CC 將 `V-6` 之量測與 `commit`／`push` 置於同一命令塊 ⇒ 該閘**結構上無從阻擋**——`V-6` 實測不成立時推送已完成。事後查明其受詞成立（自指假象）⇒ ⛔ 無須回退。

🔒 **採納並立為常規**：**閘之量測與其所守護之動作，須為<u>分開之呼叫</u>**；量測結果須先出艙、經判讀，方得執行被守護之動作。🛑 「事後為真」⛔ 使「當時未被閘擋住」變成正確。
🔑 **本款由 CC 自陳並自立一般式，發單側逐字採納。** CC 於同批之報告 commit 即已照此分開呼叫 ⇒ **已有一次成功之施行實例**。

### 常規補款 `二`：**發單側量倉之母體須以 `git` 取得**

🛑 發單側凡於自己的工作樹上量倉，母體須以 `git ls-tree -r <態>` 或 `git ls-files` 取得，⛔ 不得以 `grep -r … .`（該形恆含**未入倉之草稿**）。必須用後者時，須於同段併報 `git status --porcelain` 並自宣告其已計入。承 `自誤 250`。

### 常規補款 `三`：**同時交付多張單時，後單之開工閘須通過「零生產碼插入」自檢**

🛑 凡出單者於交付前，須對本單每一個開工閘問：「若在本單開工前，倉內合法地多了一個**零生產碼** commit，本閘會不會紅？」會紅者即為**繫於 commit 身分**之閘，須改綁性質。承 `自誤 246` 之一般式與 `自誤 252`。
```

---

## §三　`W-G.9-198` 之改版（🛑 ⛔ 屬本單之落地點·僅為告知）

🔴 `W-G.9-198` 因 `自誤 252` **已改版重發**（`S-0a` 改綁性質：`app.py` blob ＋ 變更檔清單，⛔ 不綁 commit 身分）。其舊版⛔ 未入倉 ⇒ ⛔ 無 append-only 之拘束。
🛑 **CC 收到改版檔後，須以新檔<u>覆蓋</u>工作樹內同名之舊檔**（比對 `SELF_SHA256` 確認係新版），**⛔ 本單⛔ 將 `-198` 入倉、⛔ 開工**。

---

## §四　驗收（🛑 承 **裁 `H`**·判別力對照改綁逐筆歸類）

| 閘 | 受詞 | 判準 |
|---|---|---|
| `V-1` | 零生產碼 | `git diff --name-only <基座>..HEAD` **逐列⛔ 不含** `app.py`、⛔ 不含任何 `verify/**.py`；且 `HEAD:app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` |
| `V-2` | append-only | `git diff --numstat <基座>..HEAD` **逐檔 `deletions` ＝ `0`** |
| `V-3` | 🔑 **payload 逐位落地** | 自本單 `§二-1`〜`§二-6` **六**個 fence 抽出之文字，與其落入之檔尾對應區段**逐位元組相同**（各報 `sha256`） |
| `V-4` | 取號 | 落地後自誤登記表之框 MAX ＝ **`252`**；`GB` 登記表（扣哨兵）MAX **仍 ＝ `136`**（🛑 本單⛔ 鑄 `GB` 號） |
| `V-5` | 🔒 **判別力對照（裁 `H` 之形）** | 取 `自誤 253` 與 `GB-137` 之錨定框全倉 `.md` 命中，**逐筆歸類**：須**逐筆皆為零對照／判別力對照**（含本單自身之宣告列）。🛑 **命中數⛔ 非判準**；🔴 **命中非 `0` 而未逐筆具名歸類 ⇒ 停機**。對照組 `自誤 245` ＝ `6`／`9`、`GB-134` ＝ `15`／`15` ⇒ 量測器非紅 |
| `V-6` | 🔒 **`GB-135` 更正之閉合** | 落地後 `GB` 登記表內，`GB-135` 之**原句仍逐字在**（append-only 自證）∧ 檔尾之更正加註逐位落入 |
| `V-7` | `S-0d` 之自證 | 回報 `§四` 各閘之量測與 `commit`／`push` **係分開呼叫**（貼出二者之呼叫界線） |

🛑 **⛔ 得以「計數相符」為單獨之驗收判準**——`V-3` 之逐位比對為 `L-1`〜`L-6` 之唯一 operative 判準。

---

## §五　commit 與 `push`

- **單一 commit**。訊息建議：`W-G.9-199：自誤 249/250/251/252 ＋ 裁 H ＋ GB-135 編號現查更正 ＋ 常規補款三則 ⛔ 零生產碼`
- 🟢 零生產碼 ⇒ CC 逕行 `push`。
- 🛑 **執行報告 `A-2` 併入同一 commit**（`W-G.9-197R` `自陳 ②` 之偏離於本單⛔ 再重演）；🛑 惟其**量測須與 `push` 分開呼叫**（`S-0d`）。

---

## §六　`S-0b` 之已知 🟡（發單側已實跑·母體 ＝ 本單單檔·所查範圍 ＝ `P-1`〜`P-6`）

🔒 **發單側已實跑**（`probe_order_preflight.py` 對本單單檔）：🔴 機械停機款 **`0`【項】**；🟡 **`0`【項】**；`P-5` `SELF_SHA256` ✅ 相符；`P-3` `def main` ＝ `14242`-`23460`。

🔑 **`0` 🟡 之判別力自證**（承檢查器自身之戒「**恆綠 ⇒ 檢查器紅**」）：同一支檢查器對前二單實得 `W-G.9-197` ＝ **`5`【項】**（`P-1`×3／`P-4`×2）、`W-G.9-198` ＝ **`7`【項】**（`P-1`×4／`P-4`×3）⇒ **該器於本批⛔ 恆綠、判別力非零**；本單之 `0` 係**實得**，⛔ 非器失效。
🛑 CC 實跑若出現**任何** 🟡（即⛔ 為 `0`）⇒ 逐項具名回報並與本節對拍；🔴 ⇒ 停機上呈。

SELF_SHA256: 762a17f2ea7eb71445e7598ca387755a56e095f17442f3c3abbacac71fe2afc8
