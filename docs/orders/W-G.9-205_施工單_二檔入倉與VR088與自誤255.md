# 🧾 `W-G.9-205` 施工單　二檔入倉 ＋ `VR-088` ＋ `自誤 255`

> **性質**：🟢 **零生產碼**（⛔ 動 `app.py` 一字含註解、⛔ 動任何**既有** `verify/**.py` 一字）⇒ **CC 逕推，不必請示 KL**。
> **開工態**：`wip/s1-endpart` ＝ `829bdc98132792807067cb7e201fc615827ed72b`
> **通則逐字**：**只有真觸及域邊界才停機上呈、純技術自行做完。**

---

## §零　開工閘 `S-0a`（綁性質·⛔ 綁 commit 身分）

1. `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**。
   🔒 若 `HEAD` 已前進但此 blob 仍相同 ⇒ **視為合法之零生產碼插入，閘綠**，僅須於報告載明實際 `HEAD` 全 `40` 碼。
2. 本單之**變更檔清單**恰為下列 `6` 檔（新增 `3` ＋ 追加 `3`），⛔ 得多、⛔ 得少：
   ```
   docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md   （新增·原封）
   docs/orders/W-G.9-204_發單側交接文.md                              （新增·原封）
   docs/orders/W-G.9-205_施工單_二檔入倉與VR088與自誤255.md          （新增·本單）
   docs/驗證裁定登記表.md                                              （追加 `VR-088`）
   docs/reports/W-G.9波_claude.ai側自誤登記.md                        （追加 `自誤 255`）
   CLAUDE.md                                                           （末端追加一節）
   ```
   🔒 其中**新增 `3` 檔**（`198`／`204`／`205`）、**檔尾追加 `3` 檔**（`驗證裁定登記表`／`自誤登記`／`CLAUDE.md`）。
3. 🛑 全程 `git -c core.autocrlf=false -c core.quotepath=false`。

---

## §一　工項 `1`：二檔原封入倉

工作樹現有二筆 `??`（未追蹤），逐字檔名：

```
docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md
docs/orders/W-G.9-204_發單側交接文.md
```

**做法（🛑 逐條）**

1. **⛔ 重打、⛔ 重新編碼、⛔ 改行尾、⛔ 補尾端換行、⛔ 就地修任何一字。** 原封 `git add`。
2. 入倉**前**先記錄二檔之工作樹位元組指紋：
   ```
   python -c "import hashlib,sys;b=open(sys.argv[1],'rb').read();print(len(b),b.count(b'\n'),b.count(b'\r'),hashlib.sha256(b).hexdigest())" <檔>
   ```
3. `git add -- <二檔>`；入倉**後**以 `git cat-file blob HEAD:<檔>` 取回，重算同四值，**逐值與步驟 2 相同**方為過。
4. **`W-G.9-204` 之自簽自驗**：受詞 ＝ 自檔首至 `SELF_SHA256` 該列**前一個 `\n` 含之**。重算值須與該列宣告之 `64` 碼相同。
   🔒 交接文出文窗宣稱其值前綴為 `3ecd03ec…`；**⛔ 得照抄，須自算**。若不符 ⇒ **停機上報，⛔ 自行修檔**。
5. `W-G.9-198` 舊本若亦帶 `SELF_SHA256`，同法自驗並報值；若無該列，據實報「無自簽列」。

🔑 **入倉之附帶效果（S-2 會驗）**：下列 `3` 列既有引用將由「查無此檔」轉為可驗證——
`docs/reports/W-G.9-197R_三自誤與GB133補記與GB135_136_執行報告.md`、
`docs/reports/W-G.9-199R_CC交接文.md`、
`docs/reports/W-G.9-199R_四自誤與裁H與V6判準更正_執行報告.md`。
🛑 **⛔ 得為此去改那三檔一字**（append-only）。

---

## §二　工項 `2`：鑄 `VR-088`（追加於 `docs/驗證裁定登記表.md` **檔尾**）

🔒 取號現查（發單側自倉·態 `829bdc9`）：框 `` ^#+ `?VR-([0-9]{1,4})`? ``、母體 ＝ `docs/驗證裁定登記表.md` 單檔、扣哨兵 `VR-999`／`VR-751` ⇒ 相異 `72`／MAX `87` ⇒ **次號 `088`（三位補零形）**。

**payload（逐字追加）**

```markdown
## `VR-088`　舊 `W-G.9-198` 入倉係**存查**，⛔ 得作施工依據（claude.ai 裁 `2026-09-02`·`W-G.9-205`）

**受詞** ＝ `docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md`（本單入倉之原封舊本）。

**裁**：該檔入倉之目的**僅為使既有三列路徑引用可驗證、並使重發本得以逐字比對可沿用段落**。
🛑 **它⛔ 是現行施工依據。** 其下列前提已被 `K-9-24` 消滅：
1. `§零` 之「⛔ 不得動保守替代」——`k6_step0_block_locked` 之保守替代已裁定須死碼化（`K-9-24 一`）。
2. 舊版 `S-0a`——其形⛔ 承受本波已發生之合法零生產碼插入（`自誤 252` 之一般式）。
3. 凡以「段一上鎖可作用於步驟 0」為讀法之段落——`KL 2026-09-02` 已裁上鎖之受詞 ＝ **段三之合併群操作**。

**效力**：任何窗引用該檔時，須同格載明本裁號；**⛔ 得以「倉內有此檔」推論其內容現行有效**。
重發本（`W-G.9-198R`）落地後，本檔即降為史料。
```

---

## §三　工項 `3`：鑄 `自誤 255`（追加於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**）

🔒 取號現查：框 `` ^#+ 自誤 `?([0-9]{1,4})`? ``、母體 ＝ 該單檔 ⇒ 相異 `219`／MAX `254` ⇒ **次號 `255`**。

**發單側之實測（本單之證據·CC 須自行復算一次，不得照抄）**
母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔、扣 `GB-997`：

| 框 | 相異 | MAX |
|---|---|---|
| `` ^## `GB-([0-9]{1,4})` `` | `3` | **`83`** |
| `` ^#{2,4} `?GB-([0-9]{1,4})`? `` | `52` | `138` |
| `` ^#+ .*?GB-([0-9]{1,4}) `` | `61` | `138` |
| `GB-([0-9]{1,4})`（全文） | `141` | `883` |

**payload（逐字追加）**

```markdown
## 自誤 `255`　對 `GB` 之 `MAX` 出艙「恆／皆」之全稱，而未窮舉框族

**兩則同形之出艙**
1. `CLAUDE.md` 檔尾（`W-G.9-203` 落筆）逐字：「`MAX` ⛔ 受框差影響（**四框皆 `137`**）」。
   ⇒ 於**同一 commit `829bdc9`** 內，`GB-138` 之 payload 已使標題錨定框之 `MAX` ＝ `138`。**該數在落筆之刻即為偽。**
2. `W-G.9-204` 交接文 `§二` 逐字：「`MAX` 在**所有標題錨定框**下恆 ＝ `138`」。
   ⇒ 框 `` ^## `GB-N` ``（H2 ＋ 緊反引號）亦係標題錨定框，其 `MAX` ＝ `83`。**「所有」為偽。**

**一般式（須內化）**
> **凡出艙「恆」「皆」「所有 X 下均如此」之全稱，其量詞之定義域須先被**<u>窮舉列出</u>**；⛔ 得以「我試過的幾個」代「所有」。**
> **且凡出艙之數，若其分母（框／母體）⛔ 經正典釘定，該數僅得以「在框 F 下 ＝ N」之形出艙，⛔ 得省略 `F`。**

**族** ＝ 與 `自誤 240`／`248`／`254` 同族之**射程不足**，惟此則之射程缺口在**量詞**而非**檔案母體** ⇒ 記為該族之**第四度**、且為**首度發生於量詞面**。
**攔截者** ＝ 本窗（`W-G.9-205` 起草時，為引用 `GB-138` 之結論而自行復算四框所自捕）。
**土地後果** ＝ 無（`GB` 取號之 MAX 於既採之標題框下仍 ＝ `138`，次號 `139` ⛔ 受影響）。
```

---

## §四　工項 `4`：`CLAUDE.md` **末端追加**一節（🛑 append-only·⛔ 就地改任何既有一字）

🛑 **⛔ 得去修改 `CLAUDE.md` 內既有之逐字「四框皆 `137`」**——append-only。以下節追加於檔尾。

```markdown

### 🔒 `W-G.9-205` 更正註　`GB` 取號之框與 `MAX`

本檔前文載有逐字「**`MAX` ⛔ 受框差影響（四框皆 `137`）**」——該句**已於 `2026-09-02` 由 `自誤 255` 更正**：
- 於態 `829bdc9`，標題錨定框 `` ^#{2,4} `?GB-N`? `` 與 `` ^#+ .*?GB-N `` 之 `MAX` 皆 ＝ **`138`**（非 `137`）。
- 框 `` ^## `GB-N` ``（H2 ＋ 緊反引號）之 `MAX` ＝ **`83`**、相異 ＝ `3` ⇒ **`MAX` 確實受框差影響**。
- ⇒ **`GB` 之取號，須同格載明所用之框**；現行採標題錨定框 ⇒ MAX `138` ⇒ 次號 **`139`**。
🛑 `GB` 之定義列式仍**⛔ 經正典釘定**（`GB-138` 處置③ 攢批中），在其釘定前，⛔ 得出艙 `GB` 之「相異／缺號集」，亦⛔ 得對 `MAX` 作「恆／皆」之全稱。
```

---

## §五　驗收閘（🛑 逐項須在報告中具名並附產生它的命令）

| 閘 | 判準 | 產生它的命令 |
|---|---|---|
| **S-0a** | `HEAD:app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`（**push 後複驗一次**） | `git rev-parse HEAD:app.py` |
| **S-1** | 二新檔之 `bytes/LF/CR/sha256` **入倉前 ＝ 入倉後**，逐檔四值全同 | 見 `§一-2/3` |
| **S-2** | `git ls-tree` 對 `W-G.9-198`／`W-G.9-204` 各命中 `1` 檔；且全倉 `.md` 中含 `docs/orders/W-G.9-198` 路徑之 `3` 列，其所指之檔**現已存在** | `ls-tree \| grep` ＋ `git cat-file -e` |
| **S-3** | 追蹤檔 `1571` → **`1574`**；`.md` `504` → **`507`**（＝ 新增三檔：`198`／`204`／`205`） | `ls-tree -r --name-only HEAD \| wc -l` ／ `\| grep -c '\.md$'` |
| **S-4** | `VR` 於框 `` ^#+ `?VR-N`? ``、扣 `999`／`751`：相異 `72`→**`73`**、MAX `87`→**`88`** | 見 `§二` |
| **S-5** | 自誤 於框 `` ^#+ 自誤 `?N`? ``：相異 `219`→**`220`**、MAX `254`→**`255`** | 見 `§三` |
| **S-6** | `GB` **⛔ 得變**：MAX 仍 ＝ `138`（本單⛔ 鑄 `GB`） | 框 `` ^#{2,4} `?GB-N`? `` |
| **S-7** | 🔴 **判別力自證**：對 `CLAUDE.md` 之框 `四框皆` 命中 **`1`** 列（既有偽句·⛔ 得消失）＋ 新增節之框 `W-G.9-205 更正註` 命中 **`1`** 列 ⇒ **二者並存**方證「append-only 而非就地改」 | `git cat-file blob HEAD:CLAUDE.md \| grep -c` |
| **S-8** | `docs/rulings/K-6_…md` 之 `bytes/列` **⛔ 得變**（`269196` B／`3738` 列） | `git cat-file -s` ／ `tr -cd '\n' \| wc -c` |
| **S-9** | 出單前必跑：`PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-205_施工單_二檔入倉與VR088與自誤255.md`，**逐項以字樣錨具名於報告**（⛔ 用行號）；紅項須逐筆歸類為「零參照」或「判別力參照」（裁 `H`） | 同左 |

🛑 **S-7 之用意**：本單刻意**不**修那句偽述。若報告顯示 `四框皆` 命中 `0`，代表你就地改了正典 ⇒ **紅，須 revert**。

---

## §六　報告要求（`docs/reports/W-G.9-205R_…md`·同批入倉）

1. 實際 `HEAD` 全 `40` 碼（**push 後之 `git ls-remote`**，非本機）＋ 量測時刻。
2. `S-0a`〜`S-9` 逐閘：判準、實測值、產生它的命令逐字。
3. `§一-4` 之 `W-G.9-204` 自簽自驗：宣告值與重算值**二者並列**。
4. `§三` 四框之**自行復算**結果（⛔ 照抄本單之表）。
5. 🔴 **凡宣告「`Δ` 恰 ＝ payload」者，須同格報二端之位元組數**（承 `W-G.9-203R` 之不精確；本單即為該戒之首次適用）。

---

## §七　停機條件

- `§一-4` 自簽不符 ⇒ **停機上報**，⛔ 自行修檔。
- 二檔之入倉前後四值有任一不同（含僅 `CR` 不同）⇒ **停機上報**。
- 任一閘紅且非上述二者 ⇒ 依通則：**純技術自行做完**，於報告載明。

✅ 全綠 ⇒ **逕行 push**（零生產碼），⛔ 請示 KL。
SELF_SHA256: e4951e249e758c3996068dae2699e7f0b4fe77c2476170eaf28b284731735d51
