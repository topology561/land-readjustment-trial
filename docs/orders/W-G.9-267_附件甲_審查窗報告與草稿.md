## 一、機械驗證報告

### §0　三軸與開工閘

| 軸 | 值 |
|---|---|
| **(1) 來源 commit** | `3b44a34d308ff046c0099dc098cd56bb5bd0a89d` |
| **(2) 母體** | 全 `docs/` ＝ **624** 檔／讀到 **605**／讀不到 **19**（`png` 10 ＋ `jpg` 9） |
| **(3) 粒度框** | 逐表載明（列框／檔框／集合基數／bytes） |

**開工閘**

| 閘 | 實測 | 判 |
|---|---|---|
| `left-right`（`HEAD...origin/wip/s1-endpart`） | `0　0` | ✅ |
| `ls-remote origin refs/heads/wip/s1-endpart`（**實查·⛔ 採信本地 ref**） | `3b44a34d308ff046c0099dc098cd56bb5bd0a89d` | ✅ 與態錨逐位相符 |
| 工作區為 wip 態（非 main 態陷阱） | `CLAUDE.md` 工作區 `2641` 列／blob `2641` 列（Δbytes ＝ `2641` ＝ 列數 ⇒ CRLF 簽章） | ✅ |
| 硬閘檔存在且生效 | `wg9237_heredoc_guard` **當場攔下**本窗首次 heredoc 落檔 ⇒ 改 `Write` ＋ 路徑呼叫 | ✅ 活體實測（⛔ 只 `test -f`） |

🔴 **併呈（`常規九` 之對偶·非本窗所致）**：**主 checkout 之本地 `wip/s1-endpart` ＝ `1e5a87c`，較 `origin` 落後 `31` 個 commit**（`git rev-list --left-right --count` ＝ `0　31`；`merge-base --is-ancestor 1e5a87c 3b44a34` ⇒ **是** ⇒ **⛔ 分岔、⛔ 有未 push 之工作**）。
⇒ 主 checkout `docs/orders/` 內 `16` 檔顯示為 `??`，係**該落後態下尚未入倉**之顯示，**⛔ 新單**。
🔒 **`W-G.9-267` 之單於全磁碟（含未追蹤）命中 `0`** ⇒ 本窗**無施工單**，合於其審查窗身分。

### §1　四簿取號量

🔒 **框之出處（一律自倉逐字讀·⛔ 憑開場文）**

| 簿 | 正典母體（**單檔**） | 框（逐字） | 出處 |
|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 定義框 `` ^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`? `` ∪ 範圍框 | `VR-091 補款二 ①`（候選 `A`） |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 式表 ∪ 式A ∪ 式B，扣哨兵 `997` | `VR-093` |
| `VR` | `docs/驗證裁定登記表.md` | `` ^#+ `?VR-([0-9]{1,4})`? ``，扣哨兵 `999` | `VR-092 就地加註` |
| `K-9` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `` ^#{2,4}[^\n]*?K-9-([0-9]{1,3}) `` | `W-G.9-266 工項四 四-1` |

**甲部：正典母體（單檔）·列框**

| 簿 | 母體 bytes／列 | 相異 | MAX | MIN | 缺號 `[MIN..MAX]` | vs 開場文 |
|---|---|---|---|---|---|---|
| 自誤 | `667709` B／`7819` | **323** | **330** | **7** | **`[106]`** | ✅ 全符 |
| `GB` | `727042` B／`5626` | **153** | **155** | **1** | **`[12, 87]`** | ✅ 全符 |
| `VR` | `389336` B／`5045` | **79** | **94** | **14** | **`[73, 75]`** | ✅ 全符 |
| `K-9` | `295061` B／`4057` | **28** | **29** | **2** | **`[]`** | ✅ 全符 |

**乙部：同框但母體 ＝ 全 `docs/`（＝開場文所<u>宣告</u>之母體）**

| 簿 | 相異 | MAX | MIN | 缺號數 |
|---|---|---|---|---|
| 自誤 | 330 | **999** | 1 | 669 |
| `GB` | **154** | 155 | 1 | 1（`[87]`） |
| `VR` | **80** | 94 | 14 | 1（`[73]`） |
| `K-9` | **29** | 29 | **1** | 0 |

🔴 **findings（四組數全部只在單檔母體下成立）**：開場文之三軸 `(2)` 逐字宣告「母體 全 `docs/` 624 檔·讀不到 19」，且該**檔數與讀不到數本身正確**；然其所報之四組「相異／MAX／MIN／缺號」**逐簿皆等於單檔母體之值**，與全 `docs/` 母體之值**四簿皆相異**。
⇒ **三軸 `(2)` 誤標**（數對、母體標錯）。所報之值本身**合於正典**（`W-G.9-192-PRE-b 🔧 一`／`VR-093`／`VR-092 就地加註`皆令單檔），⛔ 值錯、⛔ 取號受影響。列為擬鑄之 `自誤`（§四）。

🔒 **`VR-092` 就地加註所令之低端狀態（逐簿）**

| 簿 | `MIN` | 低端已鑄而框外 | 狀態 |
|---|---|---|---|
| 自誤 | `7` | `1`–`6`（**6** 號） | 靜態清單**已立**（`CLAUDE.md` 檔末） |
| `K-9` | `2` | `K-9-1`（**1** 號） | 靜態清單**已立**（`CLAUDE.md` 檔末·`-266`） |
| `GB` | `1` | 無（`< 1` 不存在） | ✅ 無低端盲區 |
| `VR` | `14` | **`1`–`13`（13 號全數）** | 🔴 **清單未立·候發單側授權**（`-266` 明載本單⛔ 授權） |

**對照組（每簿二造·量測器非紅）**

| 簿 | 必非零 | 得數 | 必為零（人造·執行期組出·字樣⛔ 入倉） | 得數 |
|---|---|---|---|---|
| 自誤 | 定義框 | 324 ✅ | 人造號形 | 0 ✅ |
| `GB` | 式表 | 131 ✅ | 同 | 0 ✅ |
| `VR` | 定義列式 | 80 ✅ | 同 | 0 ✅ |
| `K-9` | 定義框 | 106 ✅ | 同 | 0 ✅ |

🔒 **併呈（`自誤` 二框之差·⛔ 影響取號）**：候選 `A` 單筆列命中 **324**／舊框 **328**（Δ `−4`）；**相異／MAX／MIN／缺號 四者二框全同**（`323`／`330`／`7`／`[106]`）。
⚠️ `VR-092` 之 CC 復算段所**引**之框為**舊框**（`[^0-9０-９\n]`），非 `VR-091 補款二` 所裁之候選 `A`；因四數相同故**無後果**，惟屬**引框失準**，一併列入 §四。

🔒 **`CLAUDE.md` 之一句已過期**：其逐字「`GB` 之定義列式仍**⛔ 未經正典釘定**（`GB-138` 處置③ 攢批中）」——`VR-093`（`W-G.9-265 §三 三-4`·`2026-09-09`）**已釘定**為 `GB-116` 三框聯集。⇒ 典側不同步（⛔ 追改原句，末端追加即可）。

### §2　號占用閘（宣告框·六數分列·補款⑤⑥⑦）

母體 ＝ 全 `docs/` **624** 檔（讀到 605）；`列框 ＝ |D2 ∪ D3|`、`檔框 ＝ |D1 ∪ D2檔 ∪ D3檔|`；`D1` ⛔ 產生列命中；`*.py` 檔名含號者⛔ 計入 `D1`（補款③）。

| 號 | `D1`檔 | `D2`列 | `D3`列 | 雙屬 | **列框** | **檔框** | 鬆框(檔/列) | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-267`** | 0 | 0 | 0 | 0 | **0** | **0** | 0／0 | 受詢 ⇒ ✅ **未占用·可用** |
| `W-G.9-266` | 3 | 7 | 5 | 0 | **12** | **5** | 6／30 | 對照甲（須 `≥1`）✅ |
| 一人造號（代稱·字樣⛔ 入倉） | 0 | 0 | 0 | 0 | 0 | 0 | 0／0 | 對照乙（須 `=0`）✅ |

⇒ **次可用單號 ＝ `267`**，與開場文相符（六數逐位相符）。

### §3　生產碼 blob（正面列舉·⛔ 全倉排除式）

母體 ＝ **`app.py` ＋ `verify/` 頂層 `*.py`** ＝ **34** 檔。二態 ＝ `3b44a34`（A）vs `43a668a`（B ＝ `HEAD^`）。

| 檔 | bytes | `sha256[:16]`（二態同） |
|---|---|---|
| `app.py` | 1454937 | **`1e15d45ae84b02ca`** |
| `verify/stepg_pipeline.py` | 109245 | **`cb0bbaaa0f1eb8bc`** |
| `verify/run_all.py` | 24681 | `4ba89fef90979491` |
| `verify/run_verification.py` | 99802 | `d0d531f7dc051b65` |
| `verify/wf_f0.py` | 32299 | `b0722355c1978635` |
| `verify/wf_f1.py` | 23671 | `30e19048dfd781bc` |
| `verify/wf_f2.py` | 16220 | `f974724d7e694ad7` |
| `verify/wf_f3.py` | 14138 | `226c7c5fa00464f3` |
| `verify/wf_f4.py` | 101754 | `b36aeca6c4f6b364` |

**全 34 檔二態相異 ＝ `0`**。對照組：必非零 受檢檔數 `34` ✅／**判別力** `CLAUDE.md` 二態**相異** ✅（證此檢⛔ 恆判同）／必為零 一不存在之檔 `None` ✅。

⚠️ **照實**：開場文令出艙「**十一檔**」，CC 之正面列舉得 **34** 檔。「十一檔」之**成員從未於倉內釘定**——`docs/reports/W-G.9-247R_段乙_harness量測基建.md` 內容錨「`V-C` 之母體：取**十檔**（`3`／`7`）抑或**十一檔**（`4`／`7`）？」逐字登記其為**未決**。`34 ⊇` 任何 `11` ⇒ 「相異 `0`」之結論 **a fortiori 成立**；惟二數相異，具名之（承 `-265R`／`-266R` 之同一具名）。

### §4　`run_verification.py`　二態

**計數式（逐字·源自 `verify/run_verification.py` 之字面 `print(f"  {'✅ PASS' if ok else '🔴 FAIL'}  {name}")`）**
```
FAIL: re.match(r'^  🔴 FAIL  (.+)$', line)
PASS: re.match(r'^  ✅ PASS  (.+)$', line)
```

| 態 | `rc` | `stderr` | `RESULT` | 紅項 | 綠項 | 名目 |
|---|---|---|---|---|---|---|
| **A** `3b44a34` | **1** | **0** B | `FAIL` | **24** | **10** | **34** |
| **B** `43a668a` | **1** | **0** B | `FAIL` | **24** | **10** | **34** |

**FAIL 名目集合 逐位相同 ＝ `True`；PASS 名目集合 逐位相同 ＝ `True`；差集 A∖B ＝ `[]`、B∖A ＝ `[]`。**
（態 B 於**拋棄式 detached worktree**（短路徑·`core.autocrlf=false`）跑畢即 `git worktree remove`。）

🔒 `3.5m` 中止閘逐字 ＝ `②-宗 圍堵閘破[R5]：宗-宗重疊 = 56.8186 > 上界 1.5999（(宗數8−1)×0.005×深度45.71）`；`telescoping` 命中 **0** ⇒ **與 `-266R` 之典側不同步登記一致**（`CLAUDE.md` 序 `12`／序 `14` 仍載 telescoping 為現行阻斷）。**本窗⛔ 就其作成任何處置。**

對照組：`FAIL` 須非零 `24` ✅／`PASS` 須非零 `10` ✅／人造標記須為零 `0` ✅。

### §5　🩸 本窗之量測器紅（**四次·照實·⛔ 隱去**）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | Bash 迴圈以 `git ls-tree`（未 `-z`／未 `quotepath=false`）＋ `\|\| true` 掃 `GB-138` | 得 **`0`** | CJK 檔名被八進位跳脫 ⇒ `git cat-file` 失敗被 `\|\| true` 吞。改 Python ＋ `-z` ＋ `quotepath=false` |
| `2` | `PASS` 之標記憑記憶寫 `🟢 PASS` | 得 **`PASS = 0`** | 「`0` 最該懷疑」⇒ 讀 `run_verification.py` 之 `print` 字面，實為 `✅ PASS` ⇒ 真值 **10** |
| `3` | AST 取 `run_step_g` 找街廓迴圈 | `AssertionError` | `run_step_g` 係 **36 列薄殼**，實作在 `_run_step_g_impl`（`277..1614`）。**loud 失敗·⛔ 靜默** |
| `4` | 扁平 AST walk 判跨迭代相依 | `tp`／`r` 誤判為 🔴 | 二者皆 **comprehension／genexp 變數**（Py3 自有 scope）⇒ 逐處直讀源碼否證 |

🔒 `1` 與 `2` 若未攔，將分別產出「`GB-138` 全倉不存在」與「`run_verification` 零綠項」二個**假結論**。

---

## 二、本波受詞之判（甲〜己）

### 甲　`628-18(1)` @ `3.5m·R6·right` ⇒ **① 幾何巧合·⛔ 缺陷·⛔ 土地後果**

**自我驗證閘（先過方下結論）**：本探針之「共有頂點取邊」重建須復現 `-266R` 之「第 1 宗以後 ∥ALLOCLINE」⇒ 實測**必綠組 22 宗 ∥ALLOC ／ ⛔∥ 0** ⇒ ✅ 過。

**三候選方向（來源層 ＝ 生產解析器 `cad["alloc_dir_by_block"]` 與 `session_state["f3_cad_side_lines_by_side"]`·⛔ 自寫第二份解析）**

| 街廓/側 | ALLOC∠ | SIDE∠ | `\|∠SIDE−∠ALLOC\|` |
|---|---|---|---|
| R1/left | 48.0498 | 47.4919 | 0.5580° |
| R1/right | 48.0498 | 48.7407 | 0.6908° |
| R2/left・R3/right・R5/left | 133.4463 | 137.7683 | 4.3220° |
| R4/left | 46.0270 | 48.7407 | 2.7137° |
| R4/right | 46.0270 | 43.1590 | 2.8680° |
| **R6/right** | **137.7683** | **137.7683** | **0.0000027168°** |

**高精度**：`cross(ALLOC_R6, SIDE_R6right)` ＝ **`4.742e-08`**、`|dot|` ＝ `0.99999999999999889` ⇒ **平行至 `2.7` 微度**。判別力對照：`cross(ALLOC_R6, ALLOC_R1)` ＝ `−9.999879e-01` ⇒ 此檢**⛔ 恆判平行**。

**逐街角第 1 宗之實測遠側界**

| 街廓/側 | 宗地 | 實測∠ | ALLOC∠ | SIDE∠ | 相符者 |
|---|---|---|---|---|---|
| **R1/right** | `628(5)` | 48.7407 | 48.0498 | **48.7407** | **SIDE**（⛔ ALLOC） |
| **R6/right** | `628-18(1)` | 137.7683 | 137.7683 | 137.7683 | ALLOC ＋ SIDE（**二者重合**） |

🔒 **判 ＝ ①**。決定性者為 **`R1/right`**：該處 `SIDE ≠ ALLOC`（差 `0.6908°`），而實測遠側界**取 SIDE** ⇒ **`K-9-5-4 ②` 之 `allocation_dir` 換軸<u>確在生效</u>** ⇒ 假說 ② **否證**。`R6/right` 之遠側界同樣取 SIDE，只因該街廓之 SIDE 與 ALLOC 在**源 CAD 內即繪為平行**（殘差 `4.7e-08` ＝ DXF 座標捨入量級），故亦讀為「∥ALLOC」。
🔒 碼面佐證：`app.py` 之 `if is_corner: allocation_dir = _first_corner_alloc_dir(side_mid)` 係**無條件**單一決定點，且 `_first_corner_alloc_dir` 於查無時 **loud raise**（⛔ 靜默退回 ALLOC）。

🔴 **本判所<u>新捕</u>之結構缺陷（⛔ 甲之三選一·屬 `d-1` 閘本身）**
`d-1` 之必紅組判準為「遠側界是否 ∥ALLOCLINE」。**於任何 `SIDE ∥ ALLOC` 之街廓，「∥ALLOC」與「∥SIDE」是<u>同一述詞</u>** ⇒ 該閘於該類街廓**鑑別力為零**，既不能證違反、亦不能證未違反。全 `8` 個街廓側中**恰 `R6/right` 一組**落此類（`自誤 330` 之「同族全母體逐簿檢查」已施於本項：上表即全母體）。
⇒ `d-1` 須逐街廓出艙 `SIDE∥ALLOC` 之判，並將該類標 **「不可判」**，⛔ 併入「∥ALLOC ⇒ 違反」之計數。

🔒 **`ΔW ＝ +0.00` 之澄清（⛔ 獨立佐證）**：角退探針之 `ΔW` 取自 CSV 欄 `W(m)`，而該欄**入檔前已捨入**（`628-18(1)` 與 `628-53(1)` 之字面**皆為 `7.2`**）⇒ `ΔW = +0.00` 係**捨入解析度下之讀數**，其真值僅知 `|ΔW| < 0.05`。**⛔ 與角度量測（`2.7e-06` 度解析度）等量齊觀**，⛔ 構成「二個獨立量測同落臨界值」。併：該組 `角退? ＝ 否`、`兩兩重疊 ＝ 0.0000 ㎡`。

⇒ 🔒 **⛔ 須備 KL 題**（無土地後果、無缺陷）。

### 乙　逐街廓獨立呼叫 ⇒ **① 無狀態相依**（靜態 ＋ 經驗·二路皆證）

**(a) 靜態（AST·母體 ＝ `_run_step_g_impl` 之街廓迴圈 `524..1605`）**
外綁 ∧ 內寫 ∧ 內讀之候選 `8` 個，逐一判：

| 名 | 資料性讀 | 判 |
|---|---|---|
| `_pcap`／`_stage2_placed`／`detail_trace`／`g_rows`／`pool_diag` | **0** | ✅ **純輸出累加器**（keyed write-only，僅於 `return` 被讀） |
| `_v` | 4（皆後於 Store） | ✅ 逐迭代重綁 |
| `tp`／`r` | — | ✅ **comprehension／genexp 變數**（Py3 自有 scope）——扁平 walk 之誤判，逐處直讀否證 |

對照組：必紅（注入 `acc = acc + 1`）⇒ 偵得 ✅／必綠（注入 `out.append(1)`）⇒ 判無相依 ✅。

**(b) 經驗（跨行程·SB ＝ `3.5m`）**　口徑乙於**全新行程**跑，對拍 `run_verification`（**獨立行程**）之落檔 `verify/out/got_G值_退縮3.5m_partial.csv`。比較欄 ＝ `G(㎡)`／`W(m)`／`累積S(m)`／`幾何面積(㎡)`／`街角地`／`第1筆街角`／`推進側別`。

| 街廓 | 共有宗 | 逐格相同 | 相異 | 鑑別力 |
|---|---|---|---|---|
| R1 | 8 | 56 | **0** | 首街廓·⛔ 鑑別力 |
| R2 | 16 | 112 | **0** | **非首·具鑑別力** |
| R4 | 5 | 35 | **0** | **非首·具鑑別力** |
| R5 | 8 | 56 | **0** | **非首·具鑑別力** |
| R6 | 10 | 70 | **0** | **非首·具鑑別力** |
| **合計** | **47** | **329** | **0** | 判定集基數 `47 > 0` ✅ |

對照組：必綠（CSV 自比）✅／必紅（人造 `G +1`）偵得相異 ✅。
🔒 街廓處理序 ＝ `R1→R2→R4→R6→R5→R3`；口徑甲止於 `R5` ⇒ **`R3` 為全區連跑<u>唯一</u>不可達者**，而口徑乙得其 **11** 宗（含 `628-30(3)+` 街角地·`G ＝ 627.57`）。

🔒 **判 ＝ ①（無狀態相依）**，靜態與經驗二路一致，且經驗面於**四個非首街廓**逐位相同。
🛑 **惟本窗⛔ 據以改 `W-G.9-265 §三 三-5` 一字、⛔ 建立該途徑為常設**——三-5 之由（「該終態無獨立真相源」）是否因此不成立，係**意思決定** ⇒ 入【未決清單】。
⚠️ **射程界限（照實）**：上證所及者為「二口徑於**皆可產出**之街廓逐位相同」；`R3` 之值**⛔ 有第二來源可對拍**，故其正確性係**歸納支持**、⛔ 已證。

### 己　`d-2` 之阻斷 ⇒ **可解**（阻斷點係<u>字樣層</u>之偽陰性）

`-266R` 之阻斷點逐字為「`K-9-10`／`有效垂線` 於二生產檔命中皆 `0`（對照組 `14`）」。**該二數本窗復現無誤**（對照組 `14` ＝ `帶底` @ `app.py`）。

🔒 **然其為<u>域語言</u>字樣；程式語言之同義符號俱在**（正面列舉·列框）：

| 組 | 字樣 | `app.py` | `stepg_pipeline` |
|---|---|---|---|
| **域** | `K-9-10`／`有效垂線`／`K-9-9`／`垂線` | **0** | **0** |
| **域** | `帶底` | 14 | 0 |
| **域** | `後緣連續段`／`最淺` | 9／7 | 0／0 |
| **程式** | `_n14_band_hi`／`t_hi`／`_t_hi`／`_rear` | **9／24／18／4** | 0 |

對照組：必非零 `def ` ＝ `333`／`23` ✅；必為零 `NOSUCH_ZZZ` ＝ `0`／`0` ✅。

🔒 **正典之可實作對比（`K-9-10 六` 逐字）**：舊 `t_hi = min(近側後角之 t , 遠側後角之 t)`／新 `t_band = min(最淺有效垂線之深度 , 近側後角之 t)`。
🔒 **碼面現況**：`_n14_band_hi` 之 `t_hi ＝ min(後緣連續段角點之 t)`（`K-9-6-b (c)-1` 定式）⇒ **`K-9-10` ⛔ 實作**，生產仍走**舊式**。
⇒ 🔒 **`d-2` 之受詞可得**：以生產 `_n14_band_hi` 之 `t_hi` 為一端，探針側依 `K-9-10 一` 計 `有效垂線`（整條落在宗內之 FRONTLINE 法向線段）得 `t_band` 為另一端，差異宗必落於「**遠側後角較淺**」之形狀。

🔒 **所需之新閘（`探針還原內部幾何須設自我驗證閘` 所令）**
① `_n14_band_hi` 之碼面保證 `t_hi > t_lo`（其 `raise` 訊息 `帶底 … ≤ 帶頂 …`）逐宗印出為重建閘；
② `K-9-10 五` 之**斷點法**（`w(t)` 分段線性 ⇒ 極小落於 `{0, t_band} ∪ 頂點 t`）須逐宗印斷點集，⛔ 兩點法（正典明令其於非凸失效）；
③ 判別力：注入一個**遠側後角較淺**之人造宗須使 `t_band ≠ t_hi`（必紅），一個**遠側後角較深**者須相等（必綠）。

🛑 **本窗⛔ 逕辦 `d-2`**（單明令）；只判其可解並具名所需之閘。

### 丁　`D3`「列首即該號」是否含前導 markdown 標記 ⇒ **二框逐框實算·⛔ 自裁**

| 號 | **嚴格式**（列首**即**該號） | **寬式**（允前導 `` ` ``／`*`／`_`／`>`／`-`／空白） |
|---|---|---|
| `W-G.9-265` | `D3 5`／列框 **15**／檔框 5 | `D3 6`／列框 **16**／檔框 5 |
| `W-G.9-266` | `D3 5`／列框 **12**／檔框 5 | `D3 6`／列框 **13**／檔框 5 |
| `W-G.9-267` | `D3 0`／列框 **0** | `D3 0`／列框 **0** |

⇒ **`-265` 單載 `D3 6`／列框 `16` ＝ 寬式；`-266R` 實測 `5`／`15` ＝ 嚴格式。二者皆復現，差之成因坐實為前導反引號。**

**差之列（逐字具名·⛔ 行號為錨·以內容錨）**

- `docs/orders/W-G.9-265_自解款與開工閘三級_五則未決之裁_覆蓋帳與逐款對位.md`　內容錨：
  `` `W-G.9-265` ＝ `D1 0`／`D2 0`／`D3 0`／雙屬 `0`／列框 `0`／檔框 `0`；`W-G.9-264` ＝ … ``
- `docs/orders/W-G.9-266_三自誤_V1prime對照組更正_三簿低端盲區_56.3293來源.md`　內容錨：
  `` `W-G.9-266` ＝ `0/0/0/0/0/0`（鬆框亦 `0/0`）；`W-G.9-265` ＝ `D1 3`／… ``

🔴 **本窗新增之機械論據（供發單側裁·⛔ 本窗之裁）**：二差之列**皆為該單自身之 `S-1` 號占用表**，其內容即「本號占用 ＝ `0`」。
⇒ 於**寬式**下，**「出艙『本號未占用』此一行為，其自身即使該號變為已占用」** —— 閘**恆假**，與 `自誤 251`（`W-G.9-197 V-6` 恆假）**同形**、與 `GB-132`（敘述句污染其所量集合）**同族**。
⇒ **嚴格式係二者中唯一自洽者**；且該形**每批必再現**（每張單皆含其 `S-1` 表），⛔ 個案。
🛑 **⛔ 自裁**——`常規四（七）補款`之修改屬發單側；本窗只出艙二框實算、差之列逐字、與上開自洽性論據。

### 丙　`VR-1`–`VR-13` 靜態清單 ⇒ **只擬形·⛔ 立·⛔ 擬內容**

🔒 **CC 拒立之依據**：立之即「**新訂一條準據**」⇒ `自解款` **黑名單第 `4` 項** ⇒ **恆停機**；且 `-266R` 逐字載「本單⛔ 授權」。本窗承之。

**擬定之落地形**（比照 `CLAUDE.md` 檔末既有二節之體例：「自誤簿首六則之靜態清單」／「`K-9` 簿之靜態清單」）

- **落點**：`CLAUDE.md` **檔末純末端追加**（⛔ 落 `docs/驗證裁定登記表.md`——`GB-132` 風險結構性較高：該簿即 `VR` 定義框之母體，清單敘述句易被自身框咬中；體例同 `-266` 之「⛔ 落 `K-6`」）。
- **節題**：⛔ 得為定義列式形。
- **內容列**：一律**表列式**（列首 `|`）。
- **必載**：`13` 號逐號之「全檔命中／定義框命中／命中列之形逐字」；「⛔ 得視為缺號、⛔ 得重用」；「取號⛔ 受影響（`MAX` 仍 `94`／次號仍 `95`）」；「⛔ 追改任何既有標題形」。

**`GB-132` 迴避之機驗（本窗已實跑·候用）**——候選節題 × **六框**（`VR` 定義框／自誤定義框／`GB` 式表・式A・式B／`K-9` 定義框）：

| 候選節題 | 六框命中 |
|---|---|
| `` ## 🔧 `VR` 簿之靜態清單：**簿內低端已鑄而定義框結構上撈不到**（`W-G.9-267` 工項三） `` | **全 0** ✅ |
| `## 🔧 VR 簿低端之靜態清單（`W-G.9-267`）` | **全 0** ✅ |
| 內容列 `` \| `VR-1` \| **已鑄·定義框撈不到** \| … \| `` | **全 0** ✅ |

**判別力自證（證此檢⛔ 恆綠）**：三反例 `` ## `VR-001`　靜態清單 ``／`` ### `GB-156` 反例 ``／`## 🔒 K-9-30　反例` **皆被對應框咬中** ⇒ ✅。

**驗收閘（擬）**：立後 `VR` 之**相異／MAX／缺號集**須與立前**逐位相同**（`79`／`94`／`[73, 75]`）；且**四框並掃 `CLAUDE.md` 立前/立後差集**須為 `∅`（本倉既有戒）。

### 戊　`自誤 331` 之受詞逐字（**擬·⛔ 鑄**·責 ＝ 發單側）

> **受詞**：發單側凡出艙一個由**自擬量測器**產生之數，而該器之**可跑逐字⛔ 在倉**者，受單方於其數與 CC 實測相異時**結構上不可歸因**——⛔ 能判其為「框之別」「母體之別」「態之別」抑或「器之瑕」。
>
> **通則（擬）**：**凡出艙自擬器所產生之數，須同格出艙該器之<u>可跑逐字</u>**（或其入倉路徑）。⛔ 附者，該數**⛔ 得作為任何閘之通過條件**，且受單方於相異時**⛔ 負歸因義務**，逕記「成因⛔ 可得（發單側量測器⛔ 在倉）」。
>
> **併（承 `常規七 二`）**：三數（列數／位元組／`sha256`）相符⛔ 保證器相同；器之**逐字**與其**輸出**須出自同一次執行。
>
> 🩸 **活體實例（本窗二則）**：① **丁項** —— `-265` 之 `D3 6`／`-266R` 之 `5`，本窗證其為**寬式 vs 嚴格式**之別；在二器逐字皆不在倉之前，該歸因**只能靠 CC 重造二框**。② **§1 之母體誤標** —— 開場文四簿之數皆為單檔母體之值而宣告為全 `docs/`；CC 須**重造二種母體**方能定其為「標錯」而非「值錯」。

---

## 三、`W-G.9-267` 施工單草稿

> 🔒 **本草稿⛔ 入倉、⛔ 為正典**；其所需之款已於單內逐字重述，使其自足（`施工單必須自足`）。
> 🛑 **本窗⛔ 執行其任一工項。**

### §零　本批之級

**級 ＝ 中（純診斷 ＋ 純登記）**

**由**：本批受詞為「唯讀探針讀落檔」與「append-only 登記」，**⛔ 動生產碼一字（含註解）**、**⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何**。依 `開工閘三級`，中級 ＝ 輕級全部 ＋ `run_verification.py` **全批跑一次**（落檔全批共用·⛔ 逐工項各跑）。
🛑 **級之升不降**：若中途觸及須動生產碼一字者 ⇒ **停機上呈**，⛔ 就地升級續辦。

### §一　開工閘

| 閘 | 受詞 | 停機款 |
|---|---|---|
| `S-0` | `left-right` ＝ `0 0`；`ls-remote origin refs/heads/wip/s1-endpart` 全 `40` 碼**當場實查**（⛔ 以本單所載為錨·`常規九 三`） | 🔴 |
| `S-0b` | `python verify/probes/probe_order_preflight.py <本單>`；`P-3`／`P-5` 為機械停機款，`P-1`／`P-2`／`P-4`／`P-6` 逐項具名採納或豁免（⛔ 靜默略過） | 🔴（`P-3`／`P-5`） |
| `S-1` | 號占用（**六數分列** `D1`檔／`D2`列／`D3`列／雙屬／列框／檔框 ＋ 二對照）；受詢 `W-G.9-267` 須全 `0` | 🔴 |
| `S-2` | 四簿取號現查，**母體逐簿為單檔**（⛔ 全 `docs/`），框逐字列出；並依 `VR-092 就地加註` 載其低端狀態 | 🔴 |
| `S-3` | append-only 嚴格前綴（`verify/tools/wg942_append_audit.py`·`PYTHONIOENCODING=utf-8`） | 🔴 |
| `S-4` | 生產碼 **34 檔**（正面列舉 `app.py` ＋ `verify/` 頂層 `*.py`）blob 期初 ＝ 期末；`app.py` ＝ `1e15d45ae84b02ca` | 🔴 |

### §二　工項

**工項零　本單原封入倉**
`docs/orders/`，⛔ 改一字、⛔ 改行尾；驗入倉 blob 內容 `sha256` ＝ 落檔前 `sha256`。**不過即停，⛔ 續辦其餘工項。**

---

**工項一　`d-1` 閘之結構盲區（甲之落地）**

- **(a) 受詞**（具名到街廓／側／宗序／宗地號）：`3.5m`·`R6`·`right`·第 `0` 宗·`628-18(1)`；對照受詞 `3.5m`·`R1`·`right`·第 `0` 宗·`628(5)`。
- **(b) 驗收綁性質**：閘之受詞 ＝ **「`d-1` 必紅組之判準於 `SIDE ∥ ALLOC` 之街廓側上是否具鑑別力」**，⛔ 綁「∥ALLOC 之宗數」此一計數代理。
- **(c) 否證對照（同格載判定集基數）**：
  - **必紅** ＝ `R6/right`（`|∠SIDE−∠ALLOC| = 2.7e-06°`）⇒ 判準須輸出 **「不可判」**。判定集基數 **1**。
  - **必綠** ＝ `R1/right`（`0.6908°`）⇒ 判準須輸出 **「可判·且遠側界取 SIDE」**。判定集基數 **1**。
  - 基數 `0` ⇒ **loud 拒測**，⛔ 計為通過。
- **(d) 陰性對照（受詞須為已證未動者·附二態逐位比對）**：`43a668a` vs `3b44a34` 之生產碼 **34/34 blob 逐位相同**（本報告 §3）⇒ 二態之 `d-1` 輸出須逐位相同。
- **(e) 每數之來源層**：`∠ALLOC` ＝ 生產解析器 `cad["alloc_dir_by_block"]`；`∠SIDE` ＝ `session_state["f3_cad_side_lines_by_side"]`（＝ `_first_corner_alloc_dir` 所查之同一表）；實測遠側界 ＝ **harness 落檔** `verify/out/got_G值_退縮3.5m_partial.csv` 之 `cut_coords`；`cross`／`dot` ＝ 本探針計算。
- **(f) 每框之來源**：`∥` 之容差 `0.5°` ＝ **既有 `d-1` 之框**（承 `probe_WG9266_d1_d3.py`，⛔ 新造）；`SIDE∥ALLOC` 之判用 `|cross| < 1e-9`（**自擬**，⛔ 以待驗值為停止條件——其閾值獨立於本案任何實測值，且以 `R1`／`R4`／`R2` 之 `1e-2` 量級為必紅對照）。
- **(g) 結構性缺陷 ⇒ 同族全母體逐簿檢查**（`自誤 330`）：**全 `8` 個街廓側之 `|∠SIDE−∠ALLOC|` 表**須整表出艙（本報告 §甲 已備），並具名落此類者恰 `R6/right` 一組。
- **產出**：鑄 **一個 `GB` 號**（次號依當場現查·⛔ 憑本單）登記該盲區；`d-1` 探針改為逐街廓出艙 `SIDE∥ALLOC` 判，該類標「不可判」且**⛔ 併入違反計數**。
- 🛑 **⛔ 修配地幾何、⛔ 動 `app.py`**。

---

**工項二　逐街廓獨立呼叫之無狀態相依（乙之登記·⛔ 改任何裁）**

- **(a) 受詞**：`SB = 3.5m`；街廓 `R1`／`R2`／`R4`／`R5`／`R6` 之全 `47` 宗（逐宗地號見落檔）。
- **(b) 驗收綁性質**：「二口徑於皆可產出之街廓，其逐宗逐欄值**相同**」，⛔ 綁「相異格數 ＝ 0」此一計數（須逐格出艙）。
- **(c) 否證對照**：**必紅** ＝ 任取一宗之 `G` 人造 `+1` 後對拍須偵得相異（判定集基數 **1**）；**必綠** ＝ CSV 自比逐格相同（判定集基數 **47**）。基數 `0` ⇒ loud 拒測。
- **(d) 陰性對照**：口徑乙須於**全新行程**跑，對拍 `run_verification`（**獨立行程**）之落檔 ⇒ 去除同行程狀態外洩之混淆。
- **(e) 來源層**：口徑甲 ＝ harness 落檔 CSV；口徑乙 ＝ 本探針之 `run_step_g` 回傳／`e.partial`；欄名同 CSV。
- **(f) 框**：比較欄集 `G(㎡)`／`W(m)`／`累積S(m)`／`幾何面積(㎡)`／`街角地`／`第1筆街角`／`推進側別`（**自擬**·須逐字列出）；數值正規化 `'%.6f'`。
- **(g)**：靜態面須併附 AST 之跨迭代候選逐一判（含 comprehension 變數之排除**及其誤判之具名**）。
- **產出**：於 `docs/reports/` 出執行報告；**⛔ 改 `W-G.9-265 §三 三-5` 一字、⛔ 建立該途徑為常設、⛔ 據以宣稱 `R3` 之射程已解**。
- 🛑 **停機款**：若任一非首街廓出現**相異格 `≥ 1`** ⇒ 停機上呈（其蘊含跨街廓狀態相依，可能有土地後果）。

---

**工項三　`d-2` 阻斷點之解除登記（己）**

- **(a) 受詞**：`_n14_band_hi` 之 `t_hi`（生產·舊式）vs `K-9-10 一・三` 之 `t_band`（正典·新式），母體 ＝ 二情境全部分配宗。
- **(b)** 驗收綁「**同義詞集之完備性**」：域語言組 `K-9-10`／`有效垂線`／`帶底`／`後緣連續段`／`最淺`；程式語言組 `_n14_band_hi`／`t_hi`／`_t_hi`／`_rear`／`band_hi`。⛔ 綁單一字樣之命中數。
- **(c) 否證對照**：**必紅** ＝ 注入一「遠側後角較淺」之人造宗 ⇒ `t_band ≠ t_hi`；**必綠** ＝ 「遠側後角較深」者 ⇒ 相等。二組須同格載判定集基數；基數 `0` ⇒ loud 拒測。
- **(d) 陰性對照**：`K-9-10` 於二生產檔命中 **`0`**（已證未動）⇒ 二態逐位相同。
- **(e)(f)** 同工項一之體例；重建 `有效垂線` 之**自我驗證閘** ＝ `_n14_band_hi` 之碼面保證 `t_hi > t_lo`（其 `raise` 訊息 `帶底 … ≤ 帶頂 …`）逐宗印出，**閘不過⛔ 得據以下任何結論**。
- **產出**：只**登記**阻斷可解與同義詞集；🛑 **⛔ 逕辦 `d-2` 本體、⛔ 實作 `K-9-10`**（後者有土地後果，屬另單）。

---

**工項四　攢批登記（純登記·append-only）**

1. **`自誤`（責 ＝ 發單側）**：§一 之**母體誤標**（四簿之數皆單檔母體之值而宣告為全 `docs/` `624` 檔）＋ `VR-092` CC 復算段**引框失準**（引舊框而非 `VR-091 補款二` 之候選 `A`；四數相同故無後果）。
2. **`自誤`（責 ＝ 發單側·戊）**：自擬量測器之逐字⛔ 在倉 ⇒ 受單方結構上不可歸因（受詞逐字見本報告 §戊）。
3. **典側不同步之就地加註**：`CLAUDE.md` 之「`GB` 之定義列式仍⛔ 未經正典釘定」一句，已由 `VR-093`（`2026-09-09`）釘定 ⇒ **末端追加**具名，**⛔ 追改原句一字**。
4. **本窗量測器紅四則**（本報告 §5）入 `failure-archaeology` 或自誤簿，由發單側定落點。

🛑 **號之鑄造**：一律**當場現查次號**（定義列式 ＋ 單檔母體 ＋ 扣哨兵），⛔ 以本單所載之數為錨；新號標題形須**自證可被該類定義列式命中**，且入倉後該類 `MAX` 須由 `N−1` 推進為 `N`（`常規四（九）二`）。

---

**工項五　候發單側裁之三項（⛔ CC 自裁·本批只備形）**

| # | 受詞 | 本窗已備 | 停機款 |
|---|---|---|---|
| `5-a` | **丁**：`D3`「列首即該號」是否含前導 markdown 標記 | 二框實算 ＋ 差之列逐字 ＋ 恆假自洽性論據 | 發單側裁前 ⛔ 落地 |
| `5-b` | **丙**：`VR-1`–`VR-13` 靜態清單 | 落地形 ＋ 驗收閘 ＋ `GB-132` 迴避機驗（含判別力自證） | **⛔ 授權 ⇒ 立之即黑名單 4 ⇒ 恆停機** |
| `5-c` | **乙**：`三-5` 之由是否不成立 | 靜態 ＋ 跨行程經驗二路證據 | ⛔ 改任何裁 |

### §三　驗收

| # | 受詞 | 判準（綁性質） | 否證對照（含判定集基數） |
|---|---|---|---|
| `V-1` | 工項零 | 入倉 blob `sha256` ＝ 落檔前 `sha256` | 必紅：任改一位元組須不符（基數 1）／必綠：未改須相符（基數 1） |
| `V-2` | `d-1` 盲區 | 見工項一 (b)(c) | 見工項一 (c) |
| `V-3` | 二口徑對拍 | 見工項二 (b)(c) | 必紅 1／必綠 47 |
| `V-4` | `d-2` 同義詞集 | 見工項三 (b)(c) | 見工項三 (c) |
| `V-5` | 零生產碼 | `git diff --name-only <基座>..<commit>` 對四檔清單命中 `0` **逐 commit 出艙**（含空輸出） | 必紅：人造改 `app.py` 須命中 1（基數 1）／必綠：本批須 `0` |
| `V-6` | 零行為變更 | `run_verification` **全批跑一次**：`rc` ／`stderr` ／`RESULT` ／名目集合與基座**逐位相同**（基座 ＝ `24` FAIL／`10` PASS／`34` 名目·**當場復算·⛔ 內嵌本單之數**） | 必紅：人造刪一名目須偵得差集非空／必綠：本批差集 `∅` |
| `V-7` | append-only | `wg942_append_audit`（嚴格前綴 ＝ `True`）＋ `git diff --numstat <基座>` 刪除欄逐列 `0` | 必紅：人造刪一列須轉紅／必綠：本批全 `0` |
| `V-8` | 鑄號 | 每一新號：① 定義列式掃 payload 須命中 ② 入倉後該類 `MAX` 由 `N−1` → `N` 且 `N` ⛔ 在缺號集 | 必紅：以不合形之標題須①不過／必綠：合形者須過 |

🛑 **`V-6` 之款依 `CLAUDE.md` 之分支狀態宣告**（**准紅碼**）：判準為「**與基座之名目集合逐位相同**」，⛔ **「全綠」**。
🛑 **閘之量測與其所守護之動作⛔ 置於同一命令塊**（`W-G.9-199 常規補款一`）——量測結果須先出艙、經判讀，方得 `commit`／`push`。

### §四　`push`

依 `常規一 🔧 一`（KL `2026-08-31`）：**零生產碼 commit 逕行 `push`**。本批**逐 commit** 出艙
`git diff --name-only <前一已 push 之 HEAD>..<本 commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'`
之輸出（**含空輸出**），⛔ 以「我判斷是零生產碼」代之。命中 `≥ 1` ⇒ **請示**。

### §五　候辦池之取捨（含**所棄者**及其由）

**取（本批）**：甲（工項一）／乙（工項二）／己（工項三）／攢批登記（工項四）／三項候裁之備形（工項五）。
**由**：五者**同族**（皆為 `-266R` 遺留之診斷與登記受詞·一批掃）、**皆零生產碼**、**皆⛔ 動配地幾何** ⇒ 合於「純登記攢批」且⛔ 違「動配地幾何者⛔ 與純登記同批」。

**棄（⛔ 本批）**

| 所棄者 | 由 |
|---|---|
| `②-宗@R5 56.8186`／`0m telescoping 6.157`／**`GB-67` 之修** | 開場文明令 ⛔ 本波；且其**有土地後果**（`GB-67` 現行結論 `2` 宗／`4.7950 ㎡`）⇒ ⛔ 與純登記同批 |
| **`c2`（廢步驟 0·`R4 226.01 ㎡`）** | 開場文明令 ⛔ 開工；**動配地幾何** |
| **`K-9-29` 碼側落地** | **有土地後果**·須逐 commit 放行·**另單** |
| **`K-9-1` 之實作** | 🔴 **有土地後果**（`CLAUDE.md` 逐字）·屬 `K-6-A2` 存續四項 |
| **五級實作** | 繫 `K-9-29` 碼側落地之下游 |
| **`R3` 射程** | **繫乙之判**，而乙之判屬**意思決定** ⇒ 候發單側，⛔ 由 CC 逕啟 |
| **通用化波** | 波末／泛化波·序在末 |
| **`K-9-10` 之實作** | 己只登記阻斷可解；實作**變更帶底定式** ⇒ **有土地後果**·另單 |
| **九份 baseline 重產** | **須 KL 決時點**·⛔ 入主鏈 |

---

## 四、二清單

### 【未決清單】

| # | 項 | 現況 | 讀法（`≥2`） | 各讀法後果 | **CC 傾向 ＋ 機械證據** |
|---|---|---|---|---|---|
| `U-1` | **丁**：`D3`「列首即該號」是否含前導 markdown 標記 | `-265` 用寬式（`D3 6`／列框 `16`）、`-266R` 用嚴格式（`5`／`15`）；二者本窗**皆復現** | ① **嚴格式**：列首**即**該號 ② **寬式**：允前導 `` ` `` 等 | ①：單自身之 `S-1` 表⛔ 構成占用 ②：**每張單皆自占其號** ⇒ 「出艙未占用」之行為自身使其已占用 ⇒ 閘**恆假** | 🔒 **傾向 ①**。證據：二差之列**皆為該單自身之 `S-1` 號占用表**（內容錨見 §丁），其與 `自誤 251`（恆假）**同形**、`GB-132` **同族**；且依補款 `②`，「計算式」明文**⛔ 構成占用**。**⛔ 自裁** |
| `U-2` | **乙**：`W-G.9-265 §三 三-5` 之由（「該終態無獨立真相源」）是否仍成立 | 三-5 逐字裁「⛔ 另闢只算 `R3` 之臨時途徑」 | ① 無狀態相依 ⇒ 逐街廓為**合法取值途徑** ⇒ 三-5 之由不成立 ② 仍不合法（另有非「真相源」之理由，如口徑一致性） | ①：`R3` 之 `11` 宗即可取值，`R3` 射程⛔ 待 `GB-67` ②：維持現狀，`R3` 待 `GB-67`／telescoping | 🔒 **傾向「三-5 之<u>由</u>不成立，惟其<u>裁</u>是否變更係意思決定」**。證據：靜態（`8` 候選全為 write-only 累加器）＋ 跨行程經驗（**5 街廓／47 宗／329 格／相異 `0`**，其中**4 個非首街廓**）。⚠️ **射程界限**：`R3` 之值⛔ 有第二來源，係歸納支持。**⛔ 自裁** |
| `U-3` | **丙**：`VR-1`–`VR-13` 靜態清單之立 | `13` 號皆「已鑄而框外」（`-266R` 已證）；清單**未立** | ① 立（比照自誤首六則／`K-9-1` 之既例） ② 不立（維持只登記） | ①：`VR` 缺號集⛔ 再誤列該 `13` 號 ②：後窗可能重用或誤判其未鑄 | 🔒 **傾向 ①**，惟**⛔ 授權** ⇒ `自解款` **黑名單 4（新訂準據）** ⇒ **恆停機**。本窗只備形＋`GB-132` 迴避機驗（含判別力自證：三反例皆被咬中）。**⛔ 立** |
| `U-4` | **「十一檔」之成員** | 從未釘定；CC 正面列舉得 **34** | ① 釘定為某 `11` 檔 ② 改稱「生產碼 34 檔（正面列舉）」 | ①：需發單側給出清單 ②：⛔ 再有「十一 vs 34」之年年具名 | 🔒 **傾向 ②**。證據：`-247R` 內容錨逐字載其為未決（「取**十檔**（`3`／`7`）抑或**十一檔**（`4`／`7`）？」）；`-265R`／`-266R`／本窗**三度**具名同一差。⛔ 自裁 |
| `U-5` | `CLAUDE.md` 序 `12`／序 `14` 與活體實測之不同步（telescoping `3.5m` 已不破） | `-266R` **已登記**該不同步；`CLAUDE.md` **未同步** | ① 末端追加加註 ② 待 `GB-67` 一併處置 | ①：清單可信度回復 ②：續留不同步 | 🔒 **傾向 ①**（純登記·零土地後果）。本窗實測復現：`telescoping` 命中 `0`、中止改為 `②-宗@R5 56.8186`。⛔ 自裁（`-266R` 明載其處置候發單側／KL） |

### 【須 KL 域裁清單】

> 🔒 **空。**

**其為空之逐項理由（⛔ 以「沒有」了事）**

| 曾疑為 KL 題者 | 何以⛔ 入清單 |
|---|---|
| **甲**（`628-18(1)` 遠側界） | 判 ＝ **① 幾何巧合**，⛔ 缺陷、⛔ 土地後果（`R1/right` 之判別力對照證換軸在生效；`R6/right` 之 `SIDE∥ALLOC` 殘差 `4.7e-08`）⇒ **無域裁題可生**。開場文所令之「若判②須備 KL 題」之**條件未成就**。 |
| **乙**／`U-2` | 受詞為**取值途徑之合法性**，屬 `W-G.9-265` 之**發單側裁**，⛔ 域裁（不動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何）。 |
| **丁**／**丙**／**戊**／`U-4` | 皆為**框／編號／登記體例** ⇒ `常規三`「⛔ 無土地後果之事項」⇒ **⛔ 上呈 KL**。 |
| **己** | 只**登記阻斷可解**；`K-9-10` 之**實作**方有土地後果，已列為所棄者（另單）。 |
| `GB-67`／telescoping／`c2`／`K-9-29` 碼側／`K-9-1` | 皆**有土地後果**且皆為**既有之待落地項**，開場文明令 ⛔ 本波、⛔ 由本窗決定是否開修 ⇒ ⛔ 本窗新生之域裁題。 |

🔒 **本清單為空係「本波受詞無一觸及域邊界」之結果，⛔ 因未檢索**。依 `自誤 327` 之【已裁檢索】三軸，**⛔ 有受詞入清單 ⇒ 三軸⛔ 適用**（三軸係入清單之**前置要件**，非空清單之義務）。凡日後任一受詞轉為域裁題（如 `U-2` 之 ① 被採而觸及 `R3` 之配地結果），須先補齊三軸方得入清單。

---

### 本窗之出艙自限

**⛔ 執行草稿任一工項**・**⛔ 鑄任何號**・**⛔ 推 commit**（`HEAD` 期初＝期末＝`3b44a34…`，工作樹內容實異 `0`）・**⛔ 動生產碼**（34 檔 blob 未動）・**⛔ 決定 `GB-67`／telescoping 是否開修**・**⛔ 開工 `c2`**・**⛔ 自裁 丁／丙／`U-2`**。

**須發單側先行者 `3` 項**：`U-1`（丁之框裁）、`U-3`（丙之授權）、`U-2`（乙是否改裁）——三者未裁前，草稿之**工項五**⛔ 落地。
