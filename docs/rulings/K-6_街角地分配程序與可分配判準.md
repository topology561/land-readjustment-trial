# 裁定 K-6：街角地分配程序與可分配判準（KL 裁 2026-07-30·canonical）

## 🔖 節號體系對照表（**本檔起統一·禁互代**·CC 施工單 K-8段三本體 §0 立）

| 記法 | 意義 | 現有者 |
|---|---|---|
| **`§`** | **裁定節** | K-8 §一 BASELINE 不變式／§二 N-19′／**§三 最小寬度帶**／**§四 ALLOC_LINE 定位**／**§五 街角規定範圍與面積**／**K-9 §一〜§五**（`grep -n "^## K-9 " 本檔`） |
| **`段`** | **施工階段** | 段一（配對存檔）／段二（N-19′ 落地）／**段三＝實作 §三〜§五**／**段四＝K-9 正典與登記**（行為零變動） |

⚠️ 倉內 `docs/reports/W-G.5_K8_新session交接文.md` §四之表把 §三〜§五 併列為「§三」、
又把 K-7 記為「§四」——**該表述已由本批取代**，以本檔為準。
⚠️ **段四之內容已於 2026-08-01 更換**：原定為「K-7 落地」，現為「**K-9 正典與登記**」
——K-7 已整條撤銷（見 `grep -n "^## K-9 " 本檔`）。凡見「段四＝K-7 落地」之敘述皆為舊版。

---

## 零、四個判準（取代舊 MinA 體系）
1. **寬深雙檢**（該街廓可否分配之判準）：該宗原位次分配範圍之最小寬度 ≥ 法定最小
   寬度 **且** 最小深度 ≥ 法定最小深度。寬度依 N-14（`parcel_min_width_n14`，深度帶內
   沿 FRONTLINE 取 min）；深度依既鎖定義（FRONTLINE 法向垂距取 min）。**兩者皆須滿足。**
2. **街廓最小建築面積**：使用者輸入，per 街廓，**預設無**（未輸入即不適用）。
3. **街角規定面積**：跨占街角規定範圍者適用。街角規定範圍之寬度 ＝ 退縮寬 ＋ 畸零地
   最小寬，即街角第 1 宗之最小寬度；故「G ≥ 街角規定面積」蘊含街角第 1 宗寬度合格
   （K-4 既鎖，本裁定補上其寬度來源之解釋）。
4. **重劃區內最小分配面積 `region_min`** ＝ 各可建築街廓 D_avg×min_width 之最小值
   （現行算式**保留不動**）。用途**收斂為單一**：判定「原投影分配後未達該街廓最小
   寬深、經合併後」之土地係應**現金補償**、抑或**得申請增配至其他街廓調配池**。
   **不得**再用於 F.0／F.2／F.4 之達標宗判定，**不得**用於池三則。
   - ⚠️ **防混一（KL 2026-08-03 確認·詳見 `grep -n "^### 🔒 K-9-6-0 " 本檔`）**：
     **「最淺街廓」≠「最淺乘積街廓」**——`min_width` 逐街廓查表，寬不同時二者指向
     不同街廓、給出不同 `region_min`。本案六街廓寬皆 3.50 ⇒ 同解；**換案會分家**。
   - ⚠️ **防混二**：**附表深度（14.00／12.00）永不進本乘積**。深度項係 N-19′ 實測
     平均深度或 KL 逐街廓覆寫值；附表深度只供 **K-9-2** 之可建築門檻用（K-9-6-a）。

   **§零-4 補件（KL 裁 2026-07-30·定稿）**：1/2 門檻之**被除數統一為 `region_min`**，
   全區單一標準；per-block `MinA/2`（`_half_min`）與 `MinA_區/2`（`wd4_tier_list`）
   **兩個被除數合併為一**。三段：
   - `< region_min ÷ 2` ⇒ **現金補償**（本人申請保留分配者除外）
   - `≥ region_min ÷ 2` 且 `< region_min` ⇒ **得申請增配至其他街廓調配池**
   - `≥ region_min` ⇒ 回一般程序（**能否分配仍看該街廓寬深雙檢**）

同一街廓內有**兩種**最小寬度，禁互代：
- 有 sideline 側之**街角第 1 宗**：退縮寬 ＋ 畸零地最小寬
- 街廓內**其餘各宗**：畸零地最小寬

## 一、合併群述詞
**合併群 ＝ 同歸戶 ∧ 幾何連通**
- 幾何連通 ＝ 重劃前地籍上共用**線段**（兩端完全共點；**單點相接不算**）
- 連通路徑得經 RD 片或其他使用分區之片（公園、市場、廣場等公設），但**路徑上每一片
  皆須同歸戶**
- 街廓分配線兩側算**兩個街廓**
- **母地號不作判準**：都市計畫公告後釘樁逕割會使同母地號分屬不同地主
- 「同一筆土地」僅為說明用語，**不進碼**

## 二、街角地分配程序（四段）

### 步驟 0：同街廓相鄰合併（前置·無條件）
同街廓 ∧ 同歸戶 ∧ 相鄰 ⇒ 視為同一筆：面積相加、幾何取聯集、佔**單一原位次**
（位次由聯集幾何依 `_projection_order` 導出，禁另訂）。命名 `{投影序在前者之暫編地號}+`；
清冊增「合併組成」「合併依據」兩欄。

### 段一：預掃（街廓內先評一輪，完全不看外部）
每一街角各自：只取該街廓內、步驟 0 後、跨占該街角規定範圍之土地，各以**該街角第 1 宗
位置**試算 G。
- 有任一筆 G ≥ 街角規定面積 ⇒ **該街角定案**（達標者中比街角指數取 winner）；且該街廓
  內**所有**跨占該街角規定範圍之土地（含落選者）**一律上鎖**——不得被任何合併群抽走，
  亦不得作為合併標的（**既不出也不進**）。
- 全部未達 ⇒ 該街角**待處理**，記下各候選之街角指數與名次。
未跨占街角規定範圍者，其 G 以**原投影位次**計算，不得用街角位置。
**本段所有 G 與指數一次算定，全程不再重算。**

### 段二：全域排序（僅排待處理街角之候選）
鍵序：① 街角指數**名次** → ② 街角試算 **G** → ③ 街角指數**數值** →
④ 暫編地號**字典序最小**（最終回退）。

### 段三：逐一嘗試（每候選只試一次）
1. 取該合併群中**未上鎖**之其他片（他街廓片、RD 片、其他公設片）
2. 集中規則：
   - 群中各片**全部未達**其所在街角規定面積 ⇒ 全部併往「**有跨占街角且 G 最大**」之片；
     RD 片**整筆**併入
   - 群中**有一側已達標**（該片原位次分配範圍通過寬深雙檢）⇒ **不集中**；RD 片以
     **道路中心線切半、各歸各側**；該達標片並得併入其側之相鄰公設片（手冊 p.192）
3. 重算 G
4. **成功**（≥ 街角規定面積 ∧ 為該街角 winner）⇒ 定案；該群其餘片全數歸入；該群不再
   參與其他街角
5. **失敗** ⇒ **還原至步驟 3 之前**（含 RD 半邊一併還原）；該群各片以原面積在原街廓辦
   原位次；RD 與公設改走 F.3 手冊五則；換下一名
6. 某街角全部候選失敗 ⇒ **強制留設抵費地**，範圍**嚴格等於街角規定範圍**

### 段四：重排位次
winner（或強制抵費地）佔住街角位置；跨占左街角者靠左、跨占右街角者靠右，各自維持
**原相對順序**；不跨占任何街角者**留在原投影位置不動**；池 ＝ 扣除所有已定位宗後之
中段（單一連續段）。
範例（投影序左→右：3、5 跨左角，2、4、1 跨右角；左角從缺、右角 winner=2）：
`抵費地, 3, 5, 池, 4, 1, 2`

### 特例：一片同時跨占同街廓左右兩街角
以垂直於前緣線之分割線，取「使街廓內該筆面積恰好對半」之位置**真切為兩片**，兩片
**各自**計算所在側之街角指數與 G。
- 兩片各自拿下左右兩街角 ⇒ 保持**兩宗**
- 僅一片拿下 ⇒ **合回一片**，置於得標之街角
- 兩片皆未拿下 ⇒ 全往指數高之一側 ⇒ **合回一片**
- 兩側指數並列 ⇒ 取**前緣線 p1 側**（決定性，**禁隨機**）

## 三、與七級調配之分界
- 進入七級調配者 ＝ 街角定案並辦原投影分配後，**未通過寬深雙檢** 或 **未達街廓最小
  建築面積**（若有輸入）
- F.0 級0/0′ 判「達標宗」之門檻 ＝ **寬深雙檢**（**不是** `region_min`）
- 跨街廓同歸戶「小往較大集中」一律歸七級調配（F.0／F.2）；**街角不再有專屬救援機制**

## 四、作廢與置換
- **K-4-3 整套街角救援作廢**（M-5 ①②③、來源母體 (a)(b)(c)、A4 registry 硬閘），碼與
  裁定一併封存
- `app.py` 之「同原地號合併」述詞置換為**同歸戶 ∧ 幾何連通**
- `MinA = D_avg × min_width` 作為**逐街廓達標判準**之角色廢除；`region_min` 算式保留、
  用途收斂
- **K-5-3 失效條款**：K-5-3（分配面積 2dp tie 規則）原掛於 M-5 之 (c) 比較，
  M-5 封存後**失去適用對象**；若 F.0／F.2 出現同分需裁，**須另裁，勿逕行套用**。

## 五、收斂性
本程序為**貪婪且不回頭**：每街角處理一次、每候選試一次、失敗即還原、已定案者永不
重開；末趟新產生之不合格宗交七級調配、不回頭觸發搬移。試算次數上限 ＝ 所有待處理
街角之候選數總和。**保證停機、結果唯一可重現。**


---

# 裁定 K-8（段一·段二）：BASELINE 不變式與 N-19′ 街廓平均深度（KL 裁 2026-07-31）

## K-8 §一、🔒 BASELINE 無限直線不變式

**BASELINE 一律以「無限直線」參與量測與範圍構造，不得以線段端點截斷。**

**理由（本案實證）**：`#0`／`#2` 為**同一條無限直線之兩段**
（夾角 0.00000°、端點離線 0.00000m、端對端間隔 8.03m）。
若以**線段**為準，R1／R4 之實體選擇會產生**假差異**；以**無限直線**為準則其平均深度**嚴格相同**。
⇒ 任何以「相異直線數」或「實體數 vs 街廓數對帳」為前提之邏輯**皆會壞**。

### 配對機制：**不動**（既有 C-1／C-2／C-3／C-5·KL 2026-07-26 第二批）
其判準為**三個結構條件**——(a) 近平行、(b) 位於屁股側、(c) 深度域內
——**垂距僅為其後之收斂鍵，非距離判準**。
⛔ **禁**以「共線重疊長 ≈ 實體全長」取代之：該判準於本案碰巧同解，但
**一條 BASELINE 橫跨兩並排街廓時會雙雙給出 0.50 ⇒ 假紅**。該量**僅得為診斷欄**。

**驗證機器化**：`verify/probes/probe_ruling_K8_baseline_pairing.py`（已掛 `run_all`）。
斷言為**集合**（R1／R4 ∈ `{#0,#2}`）——**禁寫死單一實體編號**，
因二段共線且 C-5 已判等價（垂距差 5.169e-07／5.795e-06）。

## K-8 §二、N-19′ 街廓平均深度（取代 N-19，並取代「面積÷跨幅」）

**定義**：自前緣線上任一點作**垂直 BASELINE** 之直線，交 BASELINE 於一點；
該兩點之距離為此點之深度。沿前緣線取平均。BASELINE 取**無限直線**、**不受側界截斷**。

**實作**：前緣線與 BASELINE 皆直線 ⇒ 垂距沿弦**線性**
⇒ 平均 ＝ **中點垂距** ＝ **兩端垂距之平均**。**用解析式，禁取樣**。

**已驗證之對照靶**（`probe_ruling_K8_baseline_pairing.py` §3·逐格重現至小數三位）

| 街廓 | N-19′ | 兩端垂距 | 現行值 | Δ |
|---|---|---|---|---|
| R1 | 33.146 | 33.164 / 33.128 | 32.974 | +0.172 |
| R2 | 44.468 | 44.954 / 43.981 | 43.901 | +0.567 |
| R3 | 44.335 | 43.772 / 44.898 | 43.643 | +0.692 |
| R4 | 33.105 | 33.126 / 33.084 | 32.587 | +0.518 |
| R5 | 45.707 | 43.510 / 47.904 | 45.044 | +0.663 |
| R6 | 45.509 | 47.657 / 43.361 | 44.591 | +0.918 |

### 🔒 K-8 §二-1 補件（KL 更正 2026-07-31·**取代下方原「連帶」段之 115.87 版**）

**街廓平均深度以 2dp 記載後方進入乘積。**
法源＝市地重劃實施辦法 §3（長度記至二位小數）。現行 `_compute_block_depth_alloc` 之
`round(D_avg, 2)` 即此規則之實作，**保留、不得移除**。

- 逐街廓乘積 ＝ `round(D_avg, 2) × min_width`
- `region_min` ＝ 各**可建築街廓**乘積之最小值（N-16(b)）
- **顯示**：`region_min` 與 1/2 門檻皆 2dp（`Decimal(str)` ROUND_HALF_UP）；
  **比較**：用**未 round 之乘積原值**（`wd4_tier_list` WARNING-B），`round()` 只在輸出層

**本案值（實跑·`verify/fixture_block_depth_n19p.py` T8/T9）**

| 街廓 | N-19′ 全精度 | 2dp | × 3.5 |
|---|---|---|---|
| R1 | 33.1460880149 | 33.15 | 116.025 |
| R2 | 44.4678768610 | 44.47 | 155.645 |
| R3 | 44.3350248667 | 44.34 | 155.190 |
| **R4** | **33.1046288875** | **33.10** | **115.850** ← min |
| R5 | 45.7071870059 | 45.71 | 159.985 |
| R6 | 45.5093305253 | 45.51 | 159.285 |

⇒ **`region_min` 114.07 → 115.85**（R4 仍為最淺乘積街廓）；
**1/2 門檻 ＝ 115.85 ÷ 2 = 57.925 → 顯示 57.93**。

⚠️ **作廢**：本補件前之「`region_min` = 115.87／½ = 57.94」係以**全精度深度**相乘所得，
係**移除 `round(D_avg,2)` 之結果**、非法定記載鏈。**二者不得混用**；
夾具 T9c 以「≠ 115.87／57.94」為反例錨看守。

**施工狀態（K-8 段二·已落地）**：`app._compute_block_depth_alloc` 之取值法已置換為 N-19′
（`grep -n "def _compute_block_depth_alloc" app.py`）。方法 A（沿 alloc_dir 剖面差值）與
方法 B（面積÷n_alloc 跨幅）**皆降為診斷、禁刪**：A → `D_min/D_max`、B → `D_avg_B`；
`note` 之告警比較對象由「A vs B」改為「**N-19′ vs B**」。
缺 FRONT_LINE／BASELINE ⇒ **loud raise**（禁 fallback 回方法 B 或 `area/front_len`）。
⚠️ 舊註所載「方法 A 主用會被合成 BASELINE 角度污染、R2 曾算出 D_avg=500」**不得刪除**，
已改寫為「對**值**之污染風險已由 K-8 段一之配對閘＋無限直線不變式消滅；
方法 A 因此得以留作診斷，其取樣敏感性對**診斷欄**依然成立（故合成 BASELINE 半長仍須裁到
街廓 bbox 對角線）」。

### 🔴 K-8 §二-2 更正（U-K8-2·KL 裁 2026-07-31·**取代段二施工單 §三 之表述**）

段二施工單 §三 稱「D_avg **僅**經 `街廓分配深度_m` **一個**快照鍵傳播」——**不成立**。
實為**兩條路徑、兩種鍵拼法**：

| 鍵 | 產 | 耗 |
|---|---|---|
| `街廓分配深度_m`（快照鍵） | 快照 `case_params_UC9898.json`（**手維檔·無碼寫入**） | `stepg_pipeline` → `wf_f0~f4`／`wd4_tier_list`／`run_verification`／諸探針 |
| `街廓分配深度(m)`（**參數表欄**） | `app.py` `_corner_rows_init` → `f3L_corner_min_table`；harness 為 `run_verification.build_param_table` | `app.py` 之街角 corner_polygon 深度（Patch E-1.8）、街角地最小分配面積 |

且 app-live 之 `_compute_block_depth_alloc` **不進快照**（`verify/` 零呼叫）。
成因＝母體不全（失敗考古 #33 再犯、#36 ②）。**引錨一律當場 grep、禁跨 commit 抄錄**（#36 ①）。

### 🔴 K-8 §二-3 停機上呈（U-K8-5·K-8 前置 C 之前提不成立）

前置 C 令「`街廓分配深度(m)` 缺值 ⇒ loud raise、刪 `14.0` 兜底」，前提為「兜底係休眠安全網」。
**實測：兩處皆為恆常被走到的實際路徑**——

- `app.py` 街角 PK 之 `_legal_depth_B4`：讀 `f3L_sb_rows_by_label`，該 session 鍵
  **全倉無人寫入**；次源 `_candidates` 之 15 鍵集**不含**深度欄 ⇒ **每趟每塊皆落 `14.0`**。
- `app.py` 之 `_min_area_by_block`：讀 `f3_sb_rows`（臨街負擔列），其欄位**不含**該欄
  （該欄在**另一張表** `f3L_corner_min_table`）⇒ **恆走 `else: _info['min_area']`**。

⇒ 逕改 raise ＝ app 與 harness 之 PK 路徑每次必炸；改讀真來源則 corner_polygon 深度
由 `14.0` 變為 `33.15〜45.71`，**直接改動跨占街角面積 → 三指數 → PK 勝負**
——屬 **K-8 §三 本體**（街角規定範圍新構造）之範圍。
**本批不改行為、僅具名登記**；修法與 delta 量測隨本體同批。

🚩 **未收斂項**：harness／baseline 之深度**另有一條路**——快照
`verify/case_params_UC9898.json` 之 `街廓分配深度_m`（本補件時仍為舊值）。
換快照＝必須同批重烤 v3 baseline，**待授權**（U-K6-1／U-K8-1）。
制度看守見夾具 T10。

---

## K-8 §三〜§五：街角規定範圍新構造（KL 裁 2026-07-31·canonical·逐字）

**法源**：花蓮縣畸零地使用規則 §4③④及末段、§3① 附表。

### §三 最小寬度帶（虛擬）

前緣線沿 **BASELINE 法向**往 BASELINE 方向平移 `round(D_avg, 2)`，二線所夾之帶。
**純量測工具**：不截斷實體、不參與面積、不定義任何宗地邊界。

（立法理由：法規第四條的最小寬度與最小深度互相依賴，此帶為 KL 定義以打斷該循環。）
> 🛑 **失效加註（`K-9-5-13`·KL 裁定六·2026-08-12·⛔ 上列原句一字未刪）**
>
> **本節自本批起失效**——KL 裁定六逐字：「第2題：改成平移之後，「最小寬度帶」一併不需要了」。
>
> 🔒 **失效之範圍條件（⛔ 不得無條件擴張）**：**僅就其服務於 `§四` 滑動求解之用途而失效。**
> `W-G.9-17` 乙組 ① 現查：本帶之實作即在 `_build_corner_range_v3` 之 `block_depth`
> （＝`round(D_avg, 2)`）參數，其用途即該滑動；**⛔ 查無其他運算用途**
> （字樣語意窮舉 5 種；`app.py` 含「寬度帶」之 3 行皆為註解／字串）。
> ⚠️ **⛔ 與 `_n14_band` 不同**：本節為**街廓層**（`D_avg` 平移）；
> `_n14_band_geom` 為 **`K-9-6-b` 宗地層**（逐宗後緣角點·KL 圖示裁定 2026-08-05）
> ⇒ **後者不在本次失效範圍內**。
> 🔴 **若日後查出本帶另有用途，該用途<u>不在</u>本次失效範圍**，須具名待裁。
>
> 🔴 **性質 ＝ 出處無法確認**（同 `§四` 之加註）：本節引文行 **0**；
> ⚠️ 本節內文自稱「此帶為 **KL 定義**」，惟**未以 KL 逐字之引文格式記載**；
> KL 於 2026-08-12 答「記不清」。⛔ 不記為「變更」亦⛔ 不記為「移除」。

### §四 ALLOC_LINE 定位

取平行 ALLOC_LINE 之直線，起始通過該街角之「前緣線 ∩ **截角前**側街境界線」交點，
往街廓內側平行滑動，直到**帶內**二側界間、**平行前緣線**之距離**最小值**
＝ **退縮寬 ＋ 畸零地最小寬**為止。

🔒 量測用之側界取**截角前**（§4 末段：角地應截角時，其寬度及深度係指截角前之寬度及深度）。
> 🛑 **失效加註（`K-9-5-13`·KL 裁定四、五·2026-08-12·⛔ 上列原句一字未刪）**
>
> **本節之構造（平行 ALLOC_LINE 之直線往內滑動、以最小距離為停止條件）自本批起失效。**
> 新構造：街角第 1 宗之**遠側境界線 ＝ SIDELINE 平移 `S1`**（裁定四、五）。
>
> 🔴 **性質 ＝ 出處無法確認**（⛔ **不記為**「KL 變更自己之裁示」，亦⛔ **不記為**
> 「移除從未經裁示之作法」）：倉內查無本節之 KL 逐字記載
> （`W-G.9-17` 機械掃描：本節引文行 **0**；判別力自檢：全檔在 KL 逐字區塊內之行 **96**
> ⇒ 非檢查抓不到）；KL 於 2026-08-12 就其出處答「**記不清**」。
> ⚠️ **⛔ 上述之不確定，僅及於<u>本節之出處</u>；新裁（裁定四、五）之效力<u>不受影響</u>。**
> 依據見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。

### §五 街角規定範圍與面積

範圍 ＝ 前緣線、**截角後**側界、上開 ALLOC_LINE、**BASELINE** 所圍之多邊形；
**面積扣除截角三角形**。

街角規定面積 ＝ **該多邊形之面積**。
手冊「寬 × 街廓分配深度 − 截角面積」為**示例近似式**，非定義。

### 作廢

現行「SIDE_LINE 中點沿 ALLOC 法向平移 退縮＋最小寬」構造**作廢**。

KL 指出它保證不了寬度：**錨在中點**、且**平移沿 ALLOC 法向而寬度沿前緣線量**，兩者不是同一個量。

**(Ⅱ) 側街負擔範圍完全不動**，且二者**不再共用同一幾何原語**。

---

## K-8 §五-1 街角規定面積修正之面積歸屬確認（KL 裁 2026-07-31·canonical·逐字）

> 依法定最小寬修正街角規定面積，並**接受**由此產生之街角地歸屬變動。
>
> 退縮 0m 情境三筆翻盤：
> - **R5 左**：`G005` → `G022`
> - **R2 左**：`G023` → `G010`
> - **R3 右**：`G007（628-45）` → **強制留設抵費地**（該側合格候選 1 → 0）
>
> 退縮 3.5m 情境 winner 全部不變。
>
> 立法事實（實測·非推論）：舊構造在八街角中**七角**所保證之帶內最小寬**低於法定 3.50m**
> （最劣 R5 左 **1.706m**，不足法定值之半）。唯一合格者 R6 右（3.511m）為
> ∠(SIDE,ALLOC)＝0.000° 之角，亦為唯一面積下降者。

## K-8 §五-2 DXF 預埋 146.50㎡ 基準退場（KL 裁 2026-07-31·canonical·逐字）

> `V6.dxf` 預埋圖層 `R5左側街角地最小面積`＝**146.50㎡**（退縮 0m）係以**舊構造**繪製，
> 隨 K-8 §三〜§五 之作廢**一併退場**，不再作為任何驗收對拍基準。
>
> 新構造 R5 左 0m ＝ **225.24㎡**。

### 🔒 K-6-B 必查項登記（commit C 立·**勿沿用本批結論**）

> `628-45`（原 `G007`＠R3 右）於 0m 情境落入強制留設抵費地，係**現行調配鏈**之結論。
> K-6 §二 段三之「合併群跨街廓集中」（同歸戶、得經 RD 或公設連通）**尚未實作（K-6-B）**。
> ⇒ **K-6-B 上線後必須重新確認 `628-45` 之最終歸屬**，不得沿用本批結論。

### 🗄️ `GSA_EXPECT` 重錨登記（commit C）

K-8 §五-1 裁定後，`verify/wf_f0.py` 之 `GSA_EXPECT` 依新構造重錨
（**重錨 ≠ 重烤**：baseline CSV 一律不動·U-K8-1＝乙 照舊）。
逐 gid 歸因表（雙情境）見
`docs/reports/W-G.5_K8-段三_commitC_重錨解封.md`。

---

### 🔬 實作附記（CC·段三 commit B·實測·非裁定本文）

- 落點＝`app._build_corner_range_v3`（`grep -n "def _build_corner_range_v3" app.py`）；
  舊 `_build_corner_range_v2` **已整個刪除·不留 fallback**。
- **解析解**：二側界皆直線 ⇒ 平行前緣線之間距沿深度線性；且滑動只使該距離**整體平移**
  （斜率與 `t` 無關）⇒ `t* = (T − min(W(0,0), W(D,0))) / k`，**一次除法**。禁二分／取樣／迭代。
- **實測坐實裁定之診斷**：八街角之 ∠(SIDE_LINE, ALLOC) ＝ **0.000°〜4.322°**
  ⇒ 舊構造（錨中點）於斜交側必有一端寬度低於法定最小寬。例：R2 左 深度 44.47m、
  ∠4.322° ⇒ 兩端寬差 3.36m；舊值 151.44㎡ ÷ 44.47 ＝ 平均寬 **3.41m < 3.50m**。
- **`面積扣除截角三角形` 於本案為 no-op**：街廓多邊形本身即**截角後**
  （八側逐一實測 街廓 ∩ 截角三角形 ＝ **0.0000㎡**）。條文仍照實作（他案之 BLOCK
  圖層可能畫到未截角尖角）。⚠️ 舊 `_shift_cut_block_range` docstring 所載
  「不扣截角 ⇒ R3 右差 6.2㎡」**係陳舊敘述**，已同批更正。
- **BASELINE 即街廓屁股邊**：六塊各有 2–3 個頂點落於 BASELINE 無限直線 1cm 內
  ⇒ §五之「四線圍成」可逕以「街廓多邊形 ∩ L(t*) 半平面」實作。

---

## K-9 街廓層深度檢查與街角寬度實量（KL 裁 2026-08-01·canonical·逐字·**K-7 撤銷**）

> **本節取代 K-7 全條。** K-7（街角面積門檻之寬深豁免·KL 裁 2026-07-31）**整條撤銷**，
> 寬度豁免與深度豁免**俱廢**，由 **K-9-3／K-9-5** 取代。
> K-7 原文移入本節末之**考古段**、**標作廢、勿刪**
> （理由：`CLAUDE.md` 與碼註解仍有其痕跡，刪除會使後人無從追溯）。

### K-9 §一、五則裁定

| # | 裁定 | 對前案之效力 |
|---|---|---|
| **K-9-1** | 街角第 1 宗**不豁免** §零-2「街廓最小建築面積」 | U-K7-1 定案（原為未裁） |
| **K-9-2** | 深度改**街廓層**檢查：前緣線上每一點沿 **BASELINE 法向**至 BASELINE 之垂距，**任一點 ≤ 畸零地最小深度即警示**（**等於也警示**，因係逐點比較而非平均） | 新設 |
| **K-9-3** | 街廓層通過 ⇒ **宗地層一律不量深度**（含街角第 1 宗） | 新設 |
| **K-9-4** | **每宗必須配到 BASELINE**；未達即**停機報錯**。**不支援同一街廓前後配兩排**（補充四則見 **K-9-4-1**：`grep -n "^### 🔒 K-9-4-1 " 本檔`）✅**已實作**（K-6-A2 段二-1(c)） | 新設 |
| **K-9-5** | 街角第 1 宗**寬度要實量**，門檻 ＝ **退縮寬 ＋ 畸零地最小寬**；其餘各宗門檻 ＝ 畸零地最小寬 | **K-7 撤銷**；K-6 §零 之兩門檻自此生效 |

⚠️ **上表 `K-9-3` 列之射程更正（`W-G.9-42` 加註·⛔ 表內原文一字不刪·⛔ 非改裁定）**：
該列之「**宗地層一律不量深度**」**過寬**——正確為「**量、但不比附表深度**」。
依據 **`K-9-3-1`**（KL 裁 2026-08-05·canonical·本檔內）；⛔ 此處不重述，錨
`grep -n "^### 🔒 K-9-3-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
🔒 **本註⛔ 不改上表之歷史逐字**——該列係 **2026-08-01 當日裁示之紀錄**，
其為當日之真實內容；更正者為**後續裁示對其射程之限縮**，⛔ 非該列寫錯。

### K-9 §一-1、合併後之分配成果判準

- **街角第 1 宗**（有 SIDELINE 之該側）：
  `宗地寬度（截角前）≥ 退縮寬 ＋ 畸零地最小寬` **且** `G ≥ 街廓最小建築面積`
- **第 2 宗以後（含末端塊）**：
  `宗地寬度 ≥ 畸零地最小寬` **且** `G ≥ 街廓最小建築面積`
- **深度**：兩者**皆不逐宗量**，由 K-9-2 街廓層剖面檢查 ＋ K-9-4 BASELINE 臨接閘保證。

**街廓最小建築面積**係使用者輸入、per 街廓、**未輸入即不適用**（§零-2 既鎖）。

### K-9 §二、K-9-2 之封閉性證明（**勿改寫**）

**① 剖面最小值落在兩端·解析·禁取樣。**
前緣線與 BASELINE 皆直線 ⇒ 垂距沿弦**線性** ⇒ 最小值必落於前緣線兩端之一。
（與 `_compute_block_depth_alloc` 之「平均＝兩端平均」同一線性性質，
**同一組數字、一個取平均一個取最小**。）

**② 軸不同但方向對我方有利。**
K-9-2 沿 **BASELINE 法向**；N-12′ 宗地深度沿 **FRONTLINE 法向**。
到一直線之**垂距即所有方向中之最小者** ⇒
`沿 BASELINE 法向之垂距 ≤ 沿 FRONTLINE 法向之距離`
⇒ **K-9-2 通過則宗地深度必然通過**。此為幾何必然、非估算。**K-9-2 係保守側判準。**

**🔴 ②-1 更正（KL 裁 2026-08-02·因 K-9-8 而生）**
②之原敘述為「街廓剖面最小深度 ≥ 畸零地最小深度 ⇒ 該街廓每宗深度必過」。
**前提已變**：依 **K-9-8 (5)**，街角第 1 宗之深度**自 `PQ` 起算**，
而 `PQ` 較 FRONT_LINE **內縮**，內縮量之**上界 ＝ 截角股長**。
⇒ 更正後之敘述為：

> **街廓剖面最小深度 −（該街廓最大截角股長）≥ 畸零地最小深度 ⇒ 每宗深度必過。**

**本案實測**（自 `data/V6_1.dxf` 自算·段七）：
最淺剖面 **R4 `33.084m`**；**全區最大截角股長 `3.7114m`**（R4 左）
⇒ `33.084 − 3.7114 = 29.3726m` ≥ 門檻 `14.00m`，**餘裕 `15.3726m`**
⇒ **六街廓照樣全過、零影響**。

（逐角股長：R1 左 `3.6680/3.6682`／R1 右 `3.3826/3.4270`／R2 左 `3.5439/3.5442`／
R3 右 `3.4787/3.5659`／**R4 左 `3.7114/3.7114`**／R4 右 `3.5439/3.5440`／
R5 左 `3.4633/3.5480`／R6 右 `3.4015/3.4015` m。）

⛔ **僅改證明之敘述，不改 K-9-2 之判準本身**（仍為「剖面最小 ≤ 畸零地最小深即警示」）。

**③ 前提由 K-9-4 保證。**
②之封閉性要求每宗自前緣線配至 BASELINE。K-9-4 之停機閘即此前提之守衛。

> ✅ **落地（K-6-A2 段二-1(c)·2026-08-03）**：本前提之守衛**已實作**。
> 量測 `grep -n "def k94_baseline_touch_by_parcel" app.py`（只量不判·段二-1(a)）；
> 上閘 `grep -n "def k94_assert_baseline_touch" app.py`（雙向咬·停機）；
> 落點 `grep -n "K-9-4 BASELINE 臨接閘（KL 裁 2026-08-03" app.py`。
> 本案實測**零違例、零橫跨**（`verify/out/probe_ruling_K9_4_baseline_touch.log`）
> ⇒ **②之封閉性自此有守衛，非僅紙上前提。**

### K-9 §二-1、本案實測

（數字源＝**本檔 K-8 §二 兩端垂距表**·已逐格驗至小數三位；門檻 14.00）

| 街廓 | 兩端垂距 | 剖面最小 | 餘裕 |
|---|---|---|---|
| R1 | 33.164 / 33.128 | 33.128 | +19.13m ✅ |
| R2 | 44.954 / 43.981 | 43.981 | +29.98m ✅ |
| R3 | 43.772 / 44.898 | 43.772 | +29.77m ✅ |
| R4 | 33.126 / 33.084 | **33.084**（全區最淺） | +19.08m ✅ |
| R5 | 43.510 / 47.904 | 43.510 | +29.51m ✅ |
| R6 | 47.657 / 43.361 | 43.361 | +29.36m ✅ |

⇒ **六街廓零警示。K-9-2 對本案零影響，係換案防呆。**

### K-9 §三、K-7 與 §零 之既存牴觸（**考古價值高**）

§零 早已鎖定「同一街廓內有**兩種**最小寬度，禁互代：
街角第 1 宗＝**退縮寬 ＋ 畸零地最小寬**；其餘各宗＝**畸零地最小寬**」，
而 K-7 之**寬度**豁免與之**直接牴觸**——K-7 令街角第 1 宗逕認寬度合格，
等同令 §零 之第一種最小寬度**永不被援用**。

⇒ **K-9-5 並非新增規則，而是撤除牴觸、令 §零 生效。**

### ✅ K-9 §三-1、§零-3 之殘留「蘊含」語（**已裁·非牴觸**·KL 裁 2026-08-03）

> 🔒 **結案為 (乙)**（K-6-A2 段三·依 **K-9-5-2 ⑤**）：
> §零-3 末句**確認有效、不作廢**；**K-9-5 之實量係保守複檢、非牴觸**。
> 依據＝K-9-5-2 ①③④ 之**構造論**（範圍即以「退縮＋畸零地寬」定位 ⇒ 構造保證最小寬 ≥ 該值；
> winner 之 G ≥ 範圍面積且其 ALLOC_LINE 與範圍之 ALLOC_LINE 平行 ⇒ 面積夠即寬度夠）。
> 詳見 `grep -n "^### 🔒 K-9-5-2 " 本檔`。
> ⛔ **下方原文保留為考古**（其「待 KL 裁」與「須逐角驗」之敘述**已失效**·勿再據行動）。

---

### 🗄️ 〔考古·已由上方結案〕K-9 §三-1、§零-3 之殘留「蘊含」語（~~上呈 KL·本批只登記不改~~）

§零-3 現行條文末句為：

> 「故『G ≥ 街角規定面積』**蘊含**街角第 1 宗寬度合格（K-4 既鎖，本裁定補上其寬度來源之解釋）。」

該句與 **K-9-5「街角第 1 宗寬度要實量」不相容**：若寬度確為面積門檻所蘊含，則實量恆為多餘。

- **本批處置：不改 §零-3 一字**（改之即動判準之正典表述、逾本批「正典＋註解＋登記」之界）。
- **待 KL 裁**：§零-3 末句係
  **(甲)** 與 K-7 同批作廢（K-9-5 governs、該句應刪或標作廢）；抑或
  **(乙)** 於 K-8 §三〜§五 新構造下仍為真之幾何命題（則 K-9-5 屬**保守複檢**、非牴觸）。
- ~~**技術附記（非裁定·供 KL 參考）**：K-8 §四 之滑動終止條件係「帶內**最小**寬 ＝ 退縮＋最小寬」~~
  ~~⇒ 於**該側界與 ALLOC 平行**之退化情形，面積單調性可使 (乙) 成立；~~
  ~~但八街角實測 ∠(SIDE_LINE, ALLOC) ＝ **0.000°〜4.322°**（見 §五 實作附記）⇒ **非退化**，~~
  ~~(乙) 之成立與否須逐角驗。**本批之 probe 即為此提供實測底料，不代裁。**~~
  🗄️ **【已由 K-9-5-2 之構造保證涵蓋·2026-08-03】**——(乙) 之成立**不再依賴逐角驗**：
  K-9-5-2 ③ 明訂「該街角最小規定範圍**一定**最小深度 ≥ 畸零地深、最小寬度 ≥ 畸零地寬」
  係**構造之保證**（範圍本即以該寬度定位而成），非各角幾何之偶然。
  ⇒ ∠(SIDE_LINE, ALLOC) 是否退化**與 (乙) 之成立無關**。**本附記作廢、存查勿刪。**

### 🔒 K-9-4-1 BASELINE 之來源、兩排之作法、與共用（KL 裁 2026-08-03·canonical·**逐字**）

> **① 上閘**：宗地沒配到屁股線就停機。
>
> **② 圖層來源**：BASELINE、BLOCK 等圖層**一律由使用者於 CAD 繪妥後匯入 DXF**。
> 程式不需要、也不應設計畫設 BASELINE 之功能；使用者繪圖時即已考量宗地如何分配、
> 屁股線該畫在哪。
>
> **③ 兩排之作法**：若欲在一個街廓內配兩排宗地，作法是在該街廓內再畫一條 BASELINE，
> 使原本的大街廓成為兩個小街廓，讓程式知道此處有兩排可分配空間；
> **分配後每宗宗地之屁股線一律須臨接 BASELINE**。
>
> **④ 共用**：**一條 BASELINE 得由二街廓共用**（背對背），
> 且**得兼任鄰街廓末端塊第 1 筆宗地之起始境界線（非 SIDELINE）**。

#### K-9-4-1-a 對實作之直接效力

- **②** ⇒ **禁**在程式內設計「自動畫 BASELINE」「推定 BASELINE」之機制；
  缺 BASELINE ⇒ **loud raise 要求補圖**，不得代使用者臆造。
- **③** ⇒ K-9-4 之「跨過」分支，其**補救是改圖（切街廓）、不是放寬容差**
  ⇒ 訊息須明示此作法。⛔ 不得因「跨過」而調參使其消失。
- **④** ⇒ **禁**以「屁股線實體數 vs 街廓數」對帳、**禁**以實體編號為鍵、
  **禁**假設一街廓一實體（與 K-8 §一 之無限直線不變式同向）。

#### 🔬 K-9-4-1-b 本案圖面之實測結構（**坐實 ④·非推測**）

`data/V6.dxf` 之 `BASELINE` 圖層**僅 4 條 LINE，服務 6 個街廓**
（`data/V6_1.dxf` 之該圖層**逐條相同**）：

| 實體 | 方位 | 長度 | 身分 |
|---|---|---|---|
| #0 | 133.4463° | 95.658 | **R4 屁股線**；兼 R2/R3/R5/R6 之末端境界線 |
| #1 | 46.0270° | 89.214 | **R5 與 R6 共用**（背對背） |
| #2 | 133.4463° | 88.037 | **R1 屁股線**（與 #0 共線） |
| #3 | 48.0498° | 96.702 | **R2 與 R3 共用**（背對背） |

⇒ **④ 之三種情形本案皆有實例**：背對背共用（#1／#3）、共線二段（#0／#2）、
兼任鄰街廓末端境界線（#0/#2 之於 R2/R3/R5/R6）。
（#0 與 #2 方位逐位相同＝**共線** ⇒ 無限直線量測無法分辨二者，與 K-8 §一 C-5 判等價一致。）

---

### 🔒 K-9-5-2 街角最小規定範圍之構成與「面積夠 ⇒ 寬度合格」（KL 裁 2026-08-03·canonical·**逐字**）

> 📌 **沿革註**：本則於 2026-08-03 曾短暫編為 `K-9-5-1`，同日更正為 `K-9-5-2`；
> **`K-9-5-1` 自始為「正面路寬未填 ⇒ 停機」**。
> 更正之由：加撇號（U+2032）之變體會使 `grep "K-9-5-1"` **同時命中兩則**（前綴碰撞），
> 而本專案定位正典靠 grep；且施工單不應有能力改編既有正典編號。

🔧 **本款已由 KL 裁 2026-08-08（D-②）取代**——內側界改為 SIDELINE 平行移動。
原文存查，⛔ 不得再作為前提（失敗考古節 36）。新定義見下則（**`K-9-5-4`**）。

> ⚠️ **射程**：本橫幅所取代者為 **① 之範圍定義**（含其 `ALLOC_LINE(退縮＋畸零地寬)` 之內側界），
> 以及**依附於該定義**之 **③④⑤**。⛔ **② 之「退縮＝0 時可能不含 FRONT_LINE 段」不受影響**
> ——新構造下同樣成立（`S1` 咬在截角側時，`Ps` 即為範圍之前緣端點）。
> ⛔ **以下 ①〜⑤ 逐字保留、一字未刪**（失敗考古節 36：作廢之數字與前提須留原文存查）。

> **①** 街角最小規定範圍之構成：使用者畫好 BASELINE、FRONT_LINE、BLOCK 等圖層後，
> 程式先檢驗各街廓最小深度是否小於畸零地深（街廓深度呈線性）——小於即警示（K-9-2）。
> 若最小深度均大於畸零地深，且最小寬依法規定義之量法量出「退縮 ＋ 畸零地寬」之位置，
> 即據以決定街角第 1 宗之 ALLOC_LINE 位置。此時
> **街角最小規定範圍 ＝ FRONT_LINE → 道路截角 → SIDE_LINE → BASELINE → ALLOC_LINE(退縮＋畸零地寬)**。
>
> **②** **退縮 ＝ 0 之情形**：範圍可能**不含 FRONT_LINE 段**，成為
> **道路截角 → SIDE_LINE → BASELINE → ALLOC_LINE(畸零地寬)**。
>
> **③** **構造保證**：該街角最小規定範圍**一定**最小深度 ≥ 畸零地深、最小寬度 ≥ 畸零地寬。
>
> **④** 街角 **WINNER 之 G 值一定 ≥ 街角最小規定範圍**（手冊之街角第 1 宗要件）；
> 依 G 值畫出之 ALLOC_LINE 與該範圍之 ALLOC_LINE **平行**。
>
> **⑤** ⇒ **「面積夠 ⇒ 街角第 1 宗寬度自動合格」為真。**
> §零-3 末句**確認有效、不作廢**；**K-9-5 之實量係保守複檢**。

#### K-9-5-2-a 對實作之直接效力

1. **§零-3 蘊含語結案為 (乙)**——見 `grep -n "^### ✅ K-9 §三-1" 本檔`（已改標「已裁·非牴觸」）。
2. **「(乙) 須逐角驗」之技術附記作廢**——由 ①③④ 之**構造論**取代（同節·已標作廢存查）。
3. 🔴 **K-9-5 實量若出現「面積過、寬不足」⇒ loud raise**（**構造被破壞**）；
   ⛔ **不得判該宗不合格**、⛔ **不得調參使其消失**。
   （此效力屬**段五**·本批只入正典、碼面未實作。）
   > 🛑 **本款與 `K-9-5-3`（KL 裁 2026-08-06）逐字互斥·待 KL 協調**
   > （`grep -n "^### 🔒 K-9-5-3 " 本檔`）：該則令「量得寬 < 門檻即**判不合格**」，
   > 本款令「⛔ **不得判該宗不合格**、應 **loud raise**」。
   > **已坐實之實例**：`3.5m R1/right 628(5)`——G ≥ 街角規定面積（`232.79`·碼面強制
   > `grep -n "cand_G < cand.get('min_area_to_apply'" app.py`）**且**量得寬 `6.847031` < `7.00`
   > ⇒ **正是本款所指之「面積過、寬不足」**。
   > 辨明與上呈見 `docs/reports/W-G.7_K6-A2-段六_接入點偵察.md` §〇。
   > ⛔ **二則之逐字皆未刪改**；⛔ CC 未自裁孰先。
4. **② ⇒ 範圍之邊界組成隨退縮變動** ⇒ 須以**一條規則涵蓋兩種構成**，
   ⛔ **禁分立兩式**（比照 K-9-8 (6) 之體例）。

#### 🔬 K-9-5-2-b ② 之實證

`R4/right` 於 `0m` 與 `3.5m` 兩情境之 `P` 皆落**截角邊**
（`verify/out/probe_ruling_K9_corner_width.log:72`／`:78`），
而 log `:23-26` 載其成因為「退縮 0m 時該範圍可能**整個落在截角區內**」
⇒ **② 所述「範圍可能不含 FRONT_LINE 段」確有實例**。
（⚠️ 惟該角**兩情境皆**落截角邊，成因不止於退縮值——見 K-9-7-d 之更正註。）

---

### 🔒 K-9-5-1 正面路寬未填 ⇒ **停機要求補填**（KL 裁 2026-08-03·canonical·逐字）

> **正面路寬未填者，停機要求補填；不得降為警示。**

**案由**（碼面實況·錨 `9880164`）：畸零地附表係以「分區 × **正面路寬**」查得最小寬／最小深
（`grep -n "def get_min_lot_size" app.py`）。其實作為

- `w = float(front_road_width_m or 0.0)`（`grep -n "w = float(front_road_width_m" app.py`）
- `for upper, mw, md in table: if w <= upper: return ...`（`grep -n "if w <= upper:" app.py`）

⇒ **`w = 0`（未填）恆中附表第一列**。住宅區第一列為「未達七公尺」⇒ 落 **`(3.00, 12.00)`**
——即「**路寬未填**」被靜默當成「**路寬 ≤7m**」，**無 raise、無警示**。

**土地後果（本案實算）**：`min_width` 由 `3.50` 掉為 `3.00`
⇒ `region_min` 由 `33.10 × 3.50 = 115.85` 掉為 `33.10 × 3.00 = 99.30`
⇒ **½ 門檻由 `57.93` 掉為 `49.65`**
⇒ **G 值介於 `49.65 〜 57.93㎡` 之地主，會在「現金補償」與「得申請增配」之間換邊**
（用途見 §零-4：`region_min` 已收斂為單一用途＝½ 現金補償／增配門檻）。
⇒ 此非顯示瑕疵，係**權利歸屬之改變** ⇒ **故不得降為警示。**

**落地狀態**：
- ✅ **`k92_block_depth_check` 之 loud raise 即為正解**
  （`grep -n "正面路寬\*\*缺值" app.py`）——**已符本則、不動。**
- ⬜ **既有消費端仍未依本則**（`_mw_d` → `f3_min_alloc_area_by_label`／`f3_min_width_by_label`；
  `_size_info` → 街角參數表「法定最小寬/深」欄）⇒ 登記於 **GB-20**、**待波末批**。

---

### 🔒 K-9-5-3 街角寬度門檻為**硬門檻**·⛔ 不設容差（KL 裁 2026-08-06·canonical·**逐字**）

> 🛑 **本則與 `K-9-5-2-a` 第 3 款逐字互斥·待 KL 協調**
> （`grep -n "^#### K-9-5-2-a " 本檔`）：該款（KL 裁 **2026-08-03**）令
> 「面積過、寬不足 ⇒ **loud raise**（構造被破壞）；⛔ **不得判該宗不合格**」，
> 而本則令「量得寬 < 門檻即**判不合格**」。
> **本則所舉之 `R1/right 628(5)` 恰為該款所指之情形**（其 G ≥ 街角規定面積 `232.79`
> 係碼面強制、且量得寬 `6.847031` < `7.00`）。
> ⇒ **二則須擇一或另立協調條款**；辨明與上呈見
> `docs/reports/W-G.7_K6-A2-段六_接入點偵察.md` §〇。⛔ **二則之逐字皆未刪改**、⛔ CC 未自裁。

> 街角寬度門檻為硬門檻、⛔ 不設容差。量得寬 < 門檻即判不合格，
> 無論短少量多小。3.5m 退縮下 R1/right 628(5) 量得 6.847031 < 7.00、
> 短少 0.152969 m，判不合格。

**併記一（射程）**：本則為**案外通則**——⛔ **不綁 UC9898、不綁短少量級**。
凡「量得寬 < 門檻」即不合格，短少 `0.15 m` 與短少 `2.83 m` **同一判定**。

**併記二（土地後果之出處）**：`docs/reports/W-G.7_K6-A2-段六_GB40寬度攤表.md`
（**GB-40** 之逐格攤表；實料凍存於 `verify/out/probe_K9_seg6_w_delta_切換前凍存.log`）。
該表之量係**切換為新量法後**之值——3.5m 情境下低於門檻 `7.00` 者
**僅 `R1/right 628(5)` 一格**（`R6/right 628-18(1)` 已翻為 `7.221163`）。

⚠️ **編號說明（CLAUDE.md 🔒 編號衝突以倉內為準·慣例出處見 `K-9-6-g` 之編號說明段）**：
動手前 `grep -oE "K-9-5-[0-9a-z]+" 本檔` ⇒ 僅 `K-9-5-1`／`K-9-5-2` 占用
⇒ 取下一個未占用號 **`K-9-5-3`**；⛔ 未改編任何既有正典編號。

🛑 **本則只裁「判不合格」，⛔ **未**裁其**去向**——該宗其後如何處置
（併宗／改配／現金補償／強制留設抵費地）仍為**待裁**，見
`docs/reports/W-G.7_K6-A2-段六_接入點偵察.md` 之 §7-3。

---

### 🔒 K-9-5-4 街角第 1、2 宗之境界線 ＝ **SIDELINE 平行移動**（KL 裁 2026-08-08·D-②·canonical·**逐字**）

> **本則取代 `K-9-5-2` ① 之範圍定義**（該款已加取代橫幅、原文存查）。

- 街角**第 1 宗與第 2 宗之境界線** ＝ **SIDELINE 平行移動**（⛔ **不是** ALLOCLINE 方向）。
- 平移量 **`S1 = max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)`**
  - `P0` ＝ FRONTLINE 與 SIDELINE 之**未截角理論交點**
  - `Ps` ＝ FRONTLINE 與**道路截角線段**之交點
- 🔒 **`S1` 為「與道路境界線平行」之距離**（花蓮縣畸零地使用規則**第四條第三款**逐字）
  ——⛔ **非垂距**。
- 🔒 **`W`（供 Rw 查表）為垂距**（104 手冊逐字：向宗地分配線作垂直線）
  ——與 `S1` **同名不同量·⛔ 不得互相代用**。
- **新街角最小規定範圍 ＝ `Ps → 道路截角 → SIDELINE → BASELINE → 平移後SIDELINE`**

**實證（GEO-1·`63ae86d`）**：

| 項 | 結果 | 出處 |
|---|---|---|
| 硬約束：平移後交點落在 `Ps` 或其內側（⇒ **分配線永不切截角**） | **16/16 過** | `verify/out/probe_K9_geo1_S1_construction.log` §二 |
| 閘 H3：新範圍兩側界之**路平行距 ≡ `S1`**·全深度 21 取樣（⇒ **定寬成立**） | **16/16 過** | 同上·【A】 |
| 閘 H1（`P0` ≡ FRONT∞ ∩ SIDE∞）／H2（`Ps` 辨別）／H4（範圍非空） | 各 **16/16 過** | 同上·【A】 |

**併記（土地後果·⛔ 只陳述不裁）**：新構造使街角最小規定範圍
**降 15 格／升 1 格**（唯一升＝`0m R1/left` `+0.2326`；最劣降 `−75.99`），
成因＝舊構造內側界 ∥ALLOC 與 SIDE 斜交成**楔形**，新構造為**等寬帶**。
逐格表見 `docs/reports/W-G.7_K6-A2-GEO1_S1構造圖上驗證.md` §三。

⚠️ **編號說明（`CLAUDE.md` 🔒 編號衝突以倉內為準）**：動手前
`grep -oE "K-9-5[-0-9a-zA-Z]*" 本檔` ⇒ 占用者為
`K-9-5-1`／`K-9-5-2`／`K-9-5-2-a`／`K-9-5-2-b`／`K-9-5-3`
⇒ 取下一個未占用號 **`K-9-5-4`**（`CLAUDE.md`／`app.py`／`docs/` 各 **0** 命中）；
⛔ **未改編任何既有正典編號**。

🔴 **實作狀態（更新於 D-2b-0·2026-08-08）**：
**① 街角最小規定範圍 ＝ 已切換**（D-2·`f8b42fc`）。
🔴 **② 之「街角第 1、2 宗之實配境界線」尚未實作**——資格閘之真G 與實配仍走 ∥ALLOC 幾何，
見登記表 **`GB-44`**（中間態幾何口徑不一致）。
⛔ **在 `D-2b` 落地前，任何文件不得記載 `D-②` 已完成。**

> 🔧 **就地加註｜上開實作狀態已過期（`W-G.9-34` 現查·2026-08-15·⛔ 上文原句一字未刪）**
>
> 上段之「**尚未實作**」與「資格閘之真G 與實配仍走 ∥ALLOC 幾何」**均已被後續實作推翻**：
>
> 1. **`②` 之另一半（第 2 宗之近側界）已實作**——`solve_G_binary` 新增 `near_dir` 參數
>    （簽章字樣錨 `near_dir=None) -> dict:`；說明字樣錨
>    `D-2b-23【甲】` `near_dir`：一個界面只有一條線），落地 commit **`79bf2d7`**（2026-08-09）。
>    逐檔正面列舉（`W-G.9-34` 現查）：`app.py` ⇒ **24 命中**、`verify/stepg_pipeline.py` ⇒ **9 命中**，
>    **兩路徑**皆具備初始化・傳入・交遞更新三類點。
> 2. **`GB-44` 早已自標關閉**（其「現況處置」欄逐字：「✅ **已關閉（D-2b-1·2026-08-08）**」）
>    ⇒ 上段所引之「見登記表 `GB-44`」係指向一則**已關閉**之項。
> 3. `n_hat_far` 之**生產端傳參呼叫**現查 ＝ **1**（字樣錨 `allocation_dir=_near_ad, n_hat_far=_far_nhat`）
>    ⇒ `GB-51` 所載之「＝ 0」亦過期（該列已就地加註）。
>
> ⚠️ **⛔ 本加註不主張 `D-②` 已全部完成**——上段末句之禁令（「在 `D-2b` 落地前，任何文件不得記載
> `D-②` 已完成」）**其受詞為完成與否**，本加註**僅更正「尚未實作」四字之現時真偽**，
> ⛔ 未認定其整體完成度；`②` 之殘餘項（若有）仍依既有登記。
>
> **出處**：`docs/reports/W-G.9-34_更正入倉與深度量法掛號.md`（字樣錨 `A-2 三處實作狀態註記`）。

🔒 **釋示（KL 裁 2026-08-09·D-2b-21 上呈之答復·canonical·⛔ 上文一字不刪）**：

1. **`②` 係<u>既裁事項</u>、⛔ 不另需新授權**——本則逐字「街角**第 1 宗與第 2 宗之境界線**」
   ＝ **一條**線、**一個**方向；上欄逐字「⛔ 在 `D-2b` 落地前，任何文件不得記載 `D-②` 已完成」
   ⇒ **正典已指派 `D-2b` 落地**。⇒ 令第 2 宗之**近側**界與第 1 宗之**遠側**界
   **共用同一條 ∥SIDELINE 界線**，屬本則既有射程。
   🔴 **現行半實作狀態係<u>缺陷</u>、⛔ 非基準**：第 1 宗之遠側界已 ∥SIDELINE
   （`grep -n "K-9-5-4 ② 之唯一決定點" app.py`），第 2 宗之近側界仍 ∥ALLOC
   ⇒ **同一界面雙定義** ⇒ 登記表 **`GB-51`**。
2. **「只改池側、不動第 2 宗」之案已剔除**（兩個**獨立**理由）：
   ① 與本則「**一條**線」牴觸；
   ② `_block_partition` 之 `lines` 介面逐字為「**有序**序列，每筆 `(q, n_hat)`……
   自第二條起，每條線切下一片」（`grep -n "def _block_partition" app.py` 之 docstring）
   ⇒ **一個界面只能有一條線** ⇒ 宗0／宗1 界面之**兩個方向**使切線帳無從建構。
3. **落地時點**：待 `D-2b-22【丙】`（池側殘差之逐界面全額歸因·⛔ 零生產碼）之結果。

> 🔒 **釋示補（KL 裁 2026-08-14·canonical·⛔ 上文原句一字未刪）**
>
> 🔴 **授權：街角<u>第 2 宗之近側界</u>一併改為「平行側街」。**
> ⇒ 上款 `3.` 之「待 `D-2b-22【丙】` 之結果」**待決狀態解除**；
> 　 釋示 `1.` 所述「第 2 宗之近側界與第 1 宗之遠側界**共用同一條 ∥SIDELINE 界線**」
> 　 **得予實作**。
>
> **本則之受詞為「界線<u>方向</u>」。**
> ⚠️ `D` 波開場文字之「⛔ 不授權 #3（第 2 宗 5 格為另案·界線 #3）」，其受詞為
> **寬度不足之處置**、⛔ **非界線方向**（出處【倉】`docs/reports/W-G.8_D波_新session交接文.md:186`）
> ⇒ 二者 ⛔ **不相牴觸**，該不授權**仍然有效**。
>
> **已知土地後果（裁定時已告知 KL·⛔ 非事後補述）**：第 2 宗由**平行四邊形變梯形**
> ⇒ 同深度下面積不同 ⇒ 求解收斂至不同之寬度 ⇒ **寬度／G 值／優先購買資格皆可能變動**。
>
> **連動義務 ⛔ 不因本則而免除**：下款 `4.` 之「D 波之範圍界線第 3 項 5 格中**四格須重新量測**」
> **仍須辦理**；⛔ 不得記為已解決、⛔ 不得以「D 之副作用」帶過。
>
> **送達途徑**：**經對話側送達**·⛔ 倉內無從覆核。出處批號：施工單 `W-G.9-29` §2-1。

4. **連動義務**：落地後，「D 波之範圍界線」**第 3 項**之 5 格中**四格須重新量測**
   （`grep -n "第 2 宗最小寬低於畸零地最小寬" 本檔`），另案標的隨之更新。
   ⛔ **不得記為已解決**、⛔ **不得以「D 之副作用」帶過**。

⛔ 以下為 D-2 之前之舊註（存查）：現行碼仍走 `_build_corner_range_v3` 之舊構造
（`grep -n "def _build_corner_range_v3" app.py`）；本則之落地屬 **D-1／D-2**。
⛔ 閱讀現行碼時勿誤以為已生效。

---

### 🔒 K-9-5-5 街角第 1 宗之側面道路負擔**允許收不滿**·差額吸收於抵費地（KL 裁 2026-08-08·D-2b-3·canonical·**逐字**）

> # 🛑 **部分撤回橫幅（KL 撤 2026-08-08·D-2b-4·⛔ 原文不刪·存查）**
>
> **撤回者**：KL　**撤回日**：2026-08-08（同日·D-2b-4）
>
> **被撤回之逐字範圍**（⛔ 以下三項**自即日起不得引用為裁定**）：
> 1. 標題與本則要旨中之「**允許收不滿**」「**差額吸收於抵費地**」；
> 2. 「⇒ **抵費地減少、地主配地增加**」之**土地後果**敘述；
> 3. 「本則為 `ΣRw < 100%` 之<u>第二種</u>豁免」之**全部對照表與結論**
>    （含「飽和時亦允許不閉合」一列）。
>
> **撤回理由（成因·非推測）**：該「短收」係**帳務漏項**、⛔ 非誠實短收——
> 同一條 1|2 **共用界線被兩端各自量測**：第 1 宗以自己的軸量得 `6.4600`（該軸恰為新界線之法向
> ⇒ 正確垂距），第 2 宗仍以**自己的舊軸斜投影**量得 `6.6200`，中間 `0.16 m` **兩邊都沒收**
> ⇒ `ΣRw` 短少 `0.912`。右側同理 `7.0200` vs `7.2300` ⇒ 短少 `1.134`。
> 依 **`K-9-5-6`** 之鏈定式（每條界線只量一次），**該缺口不存在** ⇒ 無需豁免。
> ⇒ 登記為 **`GB-46`**（共用界線被兩端各自量測）。
>
> ✅ **保留之半條（⛔ 未撤回·與 `K-9-5-6` 一致）**：
> 「其**臨側面道路寬度隨新界線量測**（垂直於 ∥SIDELINE 之遠側界）·**不回退**。」
>
> 📌 **原載實測數字保留為歷史實料**（`99.10`／`98.88`／`0.900`／`1.120`），
> 但其性質**更正為「漏項」**、⛔ **非「短收」**。修復後實測：`0m R1 left` 之
> `ΣRw` 由 `99.10` → **`100.02`**（`verify/out/probe_D2b4_gates.log`）。
>
> ⛔ **本則餘下仍有效者**：僅上述「✅ 保留之半條」。引用本則前**必讀本橫幅**。

**裁定逐字**：

> 🔒 **街角第 1 宗之側面道路負擔，允許在該側「收不滿」，差額由抵費地吸收。**
> ⇒ 其「臨側面道路寬度」隨新界線量測（**垂直於 ∥SIDELINE 之遠側界**），**不回退**。

🔒 **範圍**：**限街角第 1 宗**。⛔ **不得推廣至其他宗、其他界面。**

**與 `K-9-5-4 ②` 之關係**：`K-9-5-4 ②` 令街角第 1 宗之遠側界改 **∥SIDELINE**。
碼面 `solve_G_binary`（`grep -n "def solve_G_binary" app.py`）之 `allocation_dir` 係**一槽兩用**
——① 切帶方向、② `W` 量測軸（`_n_alloc = normalize(allocation_dir)`／`_ahat = ±_n_alloc`／
`_W_near = dot(bp − mp, â)`）⇒ 改①必同時改② ⇒ 該宗之 `W` 改以**新界線之垂距**量測。
本則即**追認②之改動為正**（⛔ 非 bug、⛔ 不回退），並容許其致 `ΣRw_側 < 100%`。

**土地後果（⛔ 只陳述不裁）**：該側**負擔短收** ⇒ 該宗 `G` 增 ⇒
**抵費地減少、地主配地增加**。差額**全額**吸收於調配池（守恆式不變：`ΣG ＋ 池 ＝ 街廓`）。

**實測起點（D-2b-2·實料 `verify/out/probe_D2b2_exposure.log`）**：

| 情境 | 側 | `ΣRw_實跑` | `W_final` | `W₀` | `R(W_final)−R(W₀)` | `Δ` |
|---|---|---|---|---|---|---|
| `0m` | R1 left | **99.10** | 24.66 | −0.00 | 100.00 | **0.900** |
| `3.5m` | R1 right | **98.88** | 34.76 | −0.40 | 100.00 | **1.120** |

（`W_final ≥ 18m` ⇒ `rw_from_width` 飽和 100% ⇒ 期望恆為 `100.00`。）

🔒 **本則為 `ΣRw < 100%` 之<u>第二種</u>豁免·⛔ 不得與第一種合併敘述**：

| | 第一種（既有） | **第二種（本則）** |
|---|---|---|
| 逐字出處 | `grep -n "ΣRw<100% 於負δ" app.py` | 本則 |
| 範圍 | **負 δ／forced 塊** | **街角第 1 宗** |
| 成因 | 缺口內含吸收於池（`ΔΣ池 = −ΔΣG`） | 遠側界改 ∥SIDELINE ⇒ `W` 量測軸隨之改 |
| 對飽和特例 | 明言「『ΣRw 閉合 100%』**僅飽和特例**」⇒ 飽和時**仍須閉合** | **飽和時亦允許不閉合**（本則之要旨） |

⇒ 二者**範圍不相交、成因不同**；⛔ 第一種**不覆蓋**本情形（本案 `W_final ≥ 18` 正屬其所稱之
「飽和特例」，依第一種**應閉合**而實測未閉合）——故須獨立立則。

**碼面守衛（D-2b-3 §二）**：三道結構閘改以**逐界面鏈斷量 `Δ_i`** 表述
（`ΣRw_側 == [R(W_final) − R(W₀)] − Σ_界面 Δ_i`）。
**授權界面之判準＝方向**（「該宗遠側分配界線方向 ≠ 該街廓 ALLOC 方向」·⛔ 禁以街廓名／側別／
宗序號硬編），且**斷言**符合者恰為街角第 1 宗；不符 ⇒ **loud raise**（＝本則範圍限制之碼面守衛）。
⛔ **未放寬 `0.1` 容差**、⛔ 未整側豁免、⛔ 未刪任何閘。

⚠️ **編號說明**：動手前 `grep -oE "K-9-5[-0-9a-zA-Z]*" 本檔` ⇒ 占用者為
`K-9-5-1`／`K-9-5-2`／`K-9-5-2-a`／`K-9-5-2-b`／`K-9-5-3`／`K-9-5-4`
⇒ 取下一個未占用號 **`K-9-5-5`**（本檔／`CLAUDE.md`／`app.py`／`docs/` 各 **0** 命中）；
⛔ **未改編任何既有正典編號**。

---

### 🔒 K-9-5-6 W 鏈定式：每條界線只量一次·Rw 逐宗累積（KL **追認** 2026-08-08·D-2b-4·canonical·**逐字**）

> 🔧 **具名更正（D-2b-5·CC 自查·⛔ 不刪原文）**：本則初載為「**KL 新裁**」——**該定性有誤**。
> 本則實為**追認並回歸既有規格**，⛔ **非新裁**。三項證據：
>
> 1. **規格早載同式**（`grep -rn "R(W_i)−R(W_{i−1})\|R(W_i) − R(W_{i−1})" docs/specs/`）：
>    - `docs/specs/W-G.4_規格_v3.md:289` 逐字：
>      「給定 S → 遠側界線 → **直接量 W 垂距** → `Rw=R(W_i)−R(W_{i−1})`」；
>    - `docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md:10` 逐字：
>      「直接量垂距 → `Rw ＝ R(W_i) − R(W_{i−1})`，**無循環**、**與 S 累積器脫鉤**」。
> 2. **偏離者僅為幾何解算器**：`grep -n "def iterate_G_S" app.py`（代數）之路徑**一直合規**
>    （`rw_increment(W_prev, W_cur)`）；`grep -n "def solve_G_binary" app.py`（幾何）之路徑偏離
>    （`rw_increment(_W_near, W)`）⇒ **同一檔內兩個解算器互不一致**，而**無任何檢測捕捉之**。
> 3. ⇒ **本則之教訓 ＝「規格已載、碼面偏離、長期無檢測捕捉」**，
>    ⛔ **不得只寫成「裁定落地」**——那會把一次**檢測缺口**記成一次**新政策**。
>    同族：`GB-46`（共用界線兩端各自量測）。**新增檢測**＝逐界面 `Δ_i == 0`（D-2b-4 §四(b)）。

**KL 逐字（⛔ 不得改寫成程式語言）**：

> 第 1 宗土地的 W ＝ 由 MP 至**平移後 SIDELINE** 之垂距（第 1、2 宗共用邊）⇒ `Rw_1`
> 第 2 宗土地的 W ＝ 由 MP 至 **ALLOCLINE** 之垂距（第 2、3 宗共用邊）⇒ `Rw_2`
> 第 2 宗之 G 值所用之 Rw ＝ `Rw_2 − Rw_1`
> 依此 W 規定量法與 Rw 累積之精神，**一路到最後一宗（非抵費地）**。

**展開式**：`Rw_i = R(W_i) − R(W_{i−1})`；中間項全對消 ⇒ `ΣRw_側 = R(W_最後一宗) − R(W_0)`。

🔑 **每一條界線只被量一次**——由**它前面那一宗**量；下一宗**直接取用該值**當起點，
⛔ **不得自行再量一次**。

- 🔒 **`W_i` 之量測不變**：仍為 **intrinsic 直量**（MP → 本宗**遠側**分配界線之垂距）
  ——⛔ 非以寬度累加堆出（「虛胖陷阱」`grep -n "虛胖陷阱" app.py` ⇒ `:9344`）。
  **累積的是 `Rw`、不是 `W`**（`grep -n "def rw_increment" app.py` 本即此設計·⛔ 無衝突）。
- 🔒 **首宗起點**：`W_0` ＝ MP→corner（**照實可負·⛔ 禁 clamp**）；forced 時為其 buffer 起算點。
  > 🛑 **撤回加註（`K-9-5-12`·KL 裁 2026-08-12·⛔ 上行原句一字未刪）**
  >
  > **上行前半（`W_0` ＝ MP→corner）自本批起<u>失效</u>。**
  > 首宗起點一律為 **0**（即自 MP 起量）。依據見 `grep -n "^### 🔒 K-9-5-12 " 本檔`。
  >
  > 🔴 **性質 ＝ 移除一句從未經裁示之敘述，⛔ 非推翻 KL 之裁定。**
  > 本行經 `W-G.9-15` 甲組以**機械規則**判定為**助手側補充**（**不在任何引文區塊內**）；
  > 其上之 KL 逐字區塊僅四行，內容為「第 1 宗土地的 W ＝ 由 MP 至平移後 SIDELINE 之垂距…」
  > ——**其中並無「`W_0` 起算點」此一概念**。
  > 出處 ＝ `664e4f5`（2026-08-08）之「§二 唯一行為變更：首宗起點不動（`is_corner` ⇒
  > **自量 `W_near`＝mp→corner**…）」⇒ **碼面自述被寫進本檔**（方向相反）。
  >
  > ⚠️ **上行後半（`forced` 時為其 buffer 起算點）⛔ 未一併撤，列為<u>未決</u>**：
  > 現查 `強制抵費地 ⟺ 無 winner`（單一三元式），而 KL 另裁「無 WINNER 時**第 2 宗**之起算
  > ＝ 該抵費地之**遠側境界線**；抵費地本身不計 G、不負擔」⇒ 該半句之受詞（第 1 宗之 `W_0`）
  > 是否因此消滅，**須 KL 確認**，⛔ 本批不順手撤。
  >
  > 🛑 **失效加註（`K-9-5-13`·KL 裁定二·2026-08-12·⛔ 上文原句一字未刪）**
  >
  > 上開「後半句」**自本批起失效**。KL 裁定二逐字：
  > 「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
  > ⇒ `forced` 時之起算**不再取「其 buffer 起算點」**（該點即「面積換算出來的替身線」）。
  > 依據見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。
- 🔒 **止於最後一宗（非抵費地）**——抵費地不入鏈（見 `K-9-5-7`）。

**碼面唯一決定點**：`grep -n "K-9-5-6 W 鏈之唯一決定點" app.py`
（`solve_G_binary` 內 `_rw_start = _W_near if is_corner else float(W_prev)`）。
⛔ 未於六個 `_solve_one` 呼叫端或兩份薄殼各寫一份。
📌 **代數 fallback `iterate_G_S` 本即此式**（`grep -n "rw_increment(W_prev, W_cur)" app.py`）
⇒ 本則係令**幾何解回到與代數解同一定義**、⛔ 非新造。
📌 規格早載同式：`docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md:10`
（「直接量垂距 → `Rw ＝ R(W_i) − R(W_{i−1})`」）——**碼曾偏離、本則校回**。

**實測（`verify/out/probe_D2b4_gates.log`）**：全界面 `Δ_i = +0.0000`；
`0m R1 left` 之 `ΣRw` 由 `99.10` → **`100.02`**（回收漏項 `Δ_self = +0.9120`）。

> 🔎 **【`MP` 之定義指向·`W-G.9-3`·⛔ 上文一字不刪·⛔ 非新裁】**
>
> 本節通篇使用符號 `MP` 而**未於本級（權威序第 1 級）界定之**。其定義在**下位層級**，
> 三處**互相一致**（皆為 SIDE 線之中點）：
>
> | 權威序 | 出處（內容 grep 可自癒） | 逐字 |
> |---|---|---|
> | ② `CLAUDE.md` | `grep -n "SIDE_LINE 中點到該筆" CLAUDE.md` | 「W（累積）：**SIDE_LINE 中點**到該筆靠街角側地界線的垂距」 |
> | ③《配地計算總規格_v3.md》 | `grep -n "取該側 SIDE_LINE 中點" docs/配地計算總規格_v3.md` | 「取該側 **SIDE_LINE 中點 `M`**」 |
> | ③′ `docs/specs/`（v3 補丁鏈） | `grep -n "mp＝SIDE_LINE" docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md` | 「`mp` ＝ SIDE_LINE（FRONTLINE∩SIDELINE 理論交點→末端全段）**中點**」 |
>
> ⇒ **本節之 `MP` ＝ SIDE 線之中點**，係由**下位層級供給之定義**，
> ⛔ **本註不創設新規、不改本節一字**。
>
> ⚠️ **一項<u>受詞</u>差異（⛔ 非衝突）**：第 ②③ 級作「靠**街角側**（＝**近**側）地界線」，
> 而**本節（第 1 級）作「遠側」** ⇒ **依權威序上位優先，以本節之「遠側」為準**。
> 🔒 二者所差者為**量到哪一條界線**（受詞），⛔ **非量測原點**——原點三級一致、皆為 `MP`。
> （同型先例：本檔之「🔒 權威序已自動解決（`docs/rulings/` → `CLAUDE.md`）⇒ 非正典衝突、不停機」。）
>
> 🩸 **案由**：`W-G.9-2` 曾判「`MP` 於正典從未定義」而據以停機（`P1` 破）
> ——**係只查第 1 級、且字樣之符號項僅含大寫 `MP`**（倉內作 `M`／`mp`）所致之**雙重射程不足**。
> 該誤判已於 `W-G.9-3` 具名更正（原句一字未刪·詳失敗考古**節 64**）。

---

### 🔒 K-9-5-7 抵費地係配餘：不計 G、不負擔、不入 Rw 鏈（KL 裁 2026-08-08·D-2b-4·canonical·**逐字**）

**KL 逐字**：

> **抵費地係配餘**：抵費地為該街廓原位次配地完成後**剩下的**街廓空間，
> **不計 G**、**不負擔正街／側街**。⛔ 不入 Rw 鏈。

**與既有池面積定式互為表裡（⛔ 不得合併敘述）**：

| | 既有（2026-08-05·本檔 `:1322` 起） | **本則（`K-9-5-7`）** |
|---|---|---|
| 所答 | **量**如何算 ＝ 街廓多邊形扣除各宗配地多邊形後之**幾何剩餘** | **性質**為何 ＝ 配餘·不計 G·不負擔·不入鏈 |
| 面向 | 幾何／面積 | 負擔／帳務 |

⇒ 二者**一體兩面**：前者說「池是扣完剩下的**形**」，後者說「池不參與**負擔之帳**」。
⛔ 不得以其一代替另一。

**碼面現況符合（⛔ 本則不需改碼·D-2b-4 已正面確認）**：
- `grep -n "def _place_pool_parcels" app.py` 內 `_W_prev_left = float(adv_final.get('Wf_left', 0.0) or 0.0)`
  ——鏈自**階段1 末**起算、僅由**已落位之宗**推進；抵費地為餘域、**不推進**。
- `grep -n "池窗不足" app.py` 之 `需 G=` 係**階段2 池內遞補宗**（私有地·**有** G），
  ⛔ **非抵費地本身** ⇒ 與本則不衝突。

---

### 🔒 K-9-5-8 `SIDE ∥ ALLOC` 之退化係**使用者畫設選擇**·合法且預期（KL 裁 2026-08-08·D-2b-8·canonical·**逐字**）

**KL 逐字（⛔ 不得改寫成程式語言）**：

> **有可能使用者於該街廓就是依據某側的 SIDELINE 方向畫設 ALLOCLINE。**

**要旨**：

- 分配線（ALLOCLINE／宗地分配線）之方向係**使用者畫設之選擇**
  ——`SIDE ∥ ALLOC` 為**合法組態**，⛔ 非錯誤、非巧合。
- 該組態下 `K-9-5-4 ②` 之 `rot90_cw(SIDE)` **恰等於** `allocation_dir_block`（＝`rot90(ALLOC_cad)`）
  ⇒ **方向「套用了但等值」** ⇒ **不動作** ⇒ **無楔形** ⇒ **配地結果與未套用相同**，屬**正常**。
- ⛔ **任何檢查不得將其視為異常。** ⛔ **不得據以判定圖資有誤。**

**本案實測（`verify/out/probe_D2b7_dirscope.log`【A】·`verify/out/probe_D2b8_guard.log`【A-2】）**：

| 街廓／側 | `\|cross(SIDE, ALLOC_cad)\|` | 夾角 | 判定 |
|---|---|---|---|
| **R6 / right** | **`0.0000000474`** | **`0.000003°`** | 🔵 **退化（本則涵蓋）** |
| R1 left／right | `0.0097379216`／`0.0120569928` | 0.558°／0.691° | 非退化 |
| R2 left・R5 left・R3 right | `0.0753…` | 4.322° | 非退化 |
| R4 left／right | `0.0473452131`／`0.0500347024` | 2.714°／2.868° | 非退化 |

⇒ **全案僅 R6/right 一處**。

**碼面守衛（D-2b-8 §A-2）**：`_guard_auth_scope`（`grep -n "def _guard_auth_scope" verify/stepg_pipeline.py`）
原以「方向 ≠ ALLOC」為街角第 1 宗之**充要**條件 ⇒ 於本則之退化組態**誤報**。
已改為判準 ＝「該宗 `_alloc_dir_used` ≠ 街廓 ALLOC 方向」**或**
「該側 `SIDE` 與 `ALLOC` 幾何上退化為平行」（`_side_alloc_degenerate`）。

- 🔒 **二者皆由幾何算出** ⇒ **仍非套套邏輯**；⛔ **未**改成「是否為街角第 1 宗」（那才是套套邏輯）。
- 🔒 退化判定之容差**與 `_dir_is_alloc` 同源**（`_PAR_TOL = 1e-6`）——⛔ 未另立門檻。
- 🔒 退化命中時**印出該側夾角實測值**（`[K-9-5-8]` 前綴），⛔ 未靜默略過。

**驗收（D-2b-8 §A-3·實料 `verify/out/probe_D2b8_guard.log`）**：
其餘五街廓之終態**逐項不變**（呼叫數 10／30／4／24／28 與終止例外逐字皆同基準
`verify/out/probe_D2b6_split.log`【III】）；R6 之守衛**不再 raise**。

---

### 🔒 D 波之範圍界線（KL 裁 2026-08-08·D·claude.ai 補件 2026-08-08）

本裁定（`D-①` F 槽修正 ＋ `D-②` `K-9-5-4`）**未授權**下列六項。
⛔ 於 D 波（D-0～D-3）內一律不得觸及；欲動須另案取得 KL 裁定。

1. **末端塊／`N0-20`**：其範圍定義（`R_end = (p1 之外未臨正街土地) ∪ (p1→pz 帶)`）、
   勝者規則、`strip_area(S) = G − wedge_area` 常數項 —— **一律不動**
   （KL 2026-08-07 明示「末端塊仍沿用之前裁示的作法」）。
2. **第 n−1 宗之兩側界方向**：仍為兩側皆 ∥`ALLOC_LINE`，**不動**。
3. **第 2 宗最小寬低於畸零地最小寬之 5 格**（GEO-1 §五 實測·以現況右界量得）：
   `0m R3/right −1.088138`／`0m R5/left −0.877844`／`3.5m R1/right −1.057060`／
   `3.5m R3/right −0.994436`／`3.5m R6/right −3.272963`。
   ⇒ **`D` 未解決此項**，須另案。⛔ 不得於 D 波內順手處理、亦不得以「D 之副作用」帶過。

   🔒 **加註（KL 裁 2026-08-09·D-2b-21 上呈之答復·⛔ 上文一字不刪·⛔ 不待落地後才加）**：
   ⚠️ **量測基礎將因 `K-9-5-4 ②` 落地而作廢**——本列五格中之**四格須重量**；
   `3.5m R6/right` 不受影響。⛔ **過期前之數字不得被下游引用為現況。**
   - **射程辨明**（⛔ 不得只寫「與界線方向無關」）：本項之**受詞**係「5 格**如何處置**」，
     與界線方向無關 ⇒ **不涵蓋**「第 2 宗近側界方向」之裁題；**然**本項五格逐字係
     「**以現況右界量得**」，而 `K-9-5-4 ②` 改的正是那條界 ⇒ **量測基礎作廢**。
     🔒 **構造性佐證**（⛔ 非僅字面）：同清單**第 2 項**（`第 n−1 宗之兩側界方向……不動`）
     示本清單**知道**如何凍結界線方向，且**只對第 n−1 宗**為之、⛔ 未對第 2 宗為之；
     同清單**第 5 項**逐字「**第 2 宗轉為楔形後**可能沿用同一套量測機具」
     ⇒ 本清單**自身即預設第 2 宗會變形** ⇒ 「第 2 宗整體禁區」之讀法與第 2、5 項**互相矛盾**，⛔ 不採。
   - **須重量者**（其側之 `SIDE` 與 `ALLOC` **不平行**·夾角【倉】`verify/out/probe_D2b7_dirscope.log`）：
     `0m R3/right`（`4.321958°`·`:21`）／`0m R5/left`（`4.321968°`·`:18`）／
     `3.5m R1/right`（`0.690832°`·`:11`）／`3.5m R3/right`（`4.321958°`·`:21`）
   - ⛔ **不受影響者**：`3.5m R6/right`（`−3.272963`·**五格中最劣者**）——該側判 **∥**
     （`|cross|=0.0000000474`／夾角 `0.000003°`【倉】同檔 `:17`、另見
     `verify/out/probe_D2b8_guard.log:23`）⇒ 落地**既不使其惡化、亦不使其改善**。
   - 🔒 **本項仍為另案、仍未結**——⛔ `K-9-5-4 ②` 之落地**不得**被記為解決本項。
4. **`K-9-5-3` 與 `K-9-5-2-a`-3 之條文互斥**：仍在。
   `K-9-5-3` **暫停適用不解除**、雙向互斥標記**不動**。
   爭議實例（`3.5m R1/right 628(5)`）可能因 D 而消失，
   但**實例消失 ≠ 條文相容**，須另案收。
5. **`K-9-6`／`K-9-7`／`K-9-8`／`GB-36`／`GB-40`／`k98_virtual_measure_block`**：
   對街角第 1 宗失效，但 ⛔ **禁刪除、禁停用、禁標作廢**
   —— 第 2 宗轉為楔形後可能沿用同一套量測機具。
6. **baseline／凍存錨之重烤**：⛔ 不得於 D 波內順手為之。
   須另批，且須逐 `gid` 因果鏈（重錨 ≠ 重烤 ≠ 移除錨）。

---

### 🔒 D-3 重烤授權（KL 裁 2026-08-08）

「D 波之範圍界線」**第 6 項**（baseline／凍存錨之重烤，⛔ 不得於 D 波內順手為之）
之禁令，**僅對 `D-3` 一批放寬**。次序：**D-2b 先做完 → D-3 一次重烤**。

放寬之邊界（⛔ 逾越即停機上呈）：
1. **僅限 `D-3` 批次**。D-2b 及其之前一律不得重烤：
   ⛔ 不得設 `WV_BAKE`、⛔ 不得改任何錨值、⛔ 不得放寬 GSA 檢。
2. 重烤須**逐 `gid` 因果鏈**：每一筆錨值變動須歸因至 `D-1`／`D-2`／`D-2b`
   之某一**具名**變更。**無法歸因之錨值變動 ⇒ 停機上呈。**
3. 重烤範圍**限於因 D-1/D-2/D-2b 之分配變更而過期者**；
   ⛔ 不得順手重烤其他錨、⛔ 不得擴及未受本波影響之名目。
4. **重烤 ≠ 移除錨、≠ 放寬閘**。`WV_BAKE` 用畢須正面確認已解除並貼出證明。
5. 本授權**不延伸至任何後續波次**；D-3 結束即失效。

---

### 🔒 K-9-3-1 補正：「宗地層不量深度」之**射程**（KL 裁 2026-08-05·canonical·逐字）

街廓層通過 ⇒ 宗地層**不再以宗地深度比對畸零地附表最小深度**（該判定已由街廓層蘊含）。

**理由**：前緣線與 BASELINE 皆為直線 ⇒ 深度沿前緣線**線性**
⇒ 街廓層最小深度 ≥ 附表深度，即**蘊含**該街廓各宗深度 ≥ 附表深度。

⛔ **「量測各宗自身之最小深度」不在拆除之列**——依畸零地規則 §4，
最小寬度須**先有最小深度範圍**方能界定，故各宗最小深度仍為**必要中間量**，**須實量**。

⇒ **宗地層之判定僅餘一項：帶內最小寬度 ≥ 畸零地最小寬度。**

#### K-9-3-1-a 落地狀態（K-6-A2 段五(a)）

**已裁定；`parcel_min_width_n14` 之量測帶已備 opt-in 之新式**
（`grep -n "def _n14_band_hi" app.py`·帶上緣改自 BASELINE 法向量得之該宗最小深度）
——🔴 **生產尚未切換**（`grep -n -A2 "^    _w = parcel_min_width_n14" app.py` 之四行內
**不含** `baseline_pts`）；切換與 **E-2′ 閘之拆除**屬 **(c)**、須 KL 另行放行。
⚠️ **E-2′ 閘**（`grep -n "本宗深度 {max(_tv):.4f}m" app.py`）於切換後將變成
「拿該宗**最大**深度比該宗**最小**深度」⇒ **由構造恆不成立（空閘）**
⇒ (c) 同批拆除並於拆除處留註記指向 **K-9-2** 之街廓層檢查。

---

#### 🌐 K-9-3 之泛化待查（**不設編號·入泛化波清單**·K-6-A2 段五(a) 登記）

上開「理由」以 **BASELINE 為單一直線段**為前提（**K-8 §一**之公理）。
泛化至任意案件時，若使用者於 CAD 將 BASELINE 畫成**折線**，
則「垂距沿弦線性 ⇒ min 必落於兩端之一」
（`grep -n "min 必落於兩端之一" app.py`）**只取兩端會漏掉中間低點**
⇒ **K-9-3 之蘊含關係無聲失效**。

⚠️ **本倉是否已有閘驗證 BASELINE 之直線性，段五(a) 未查證**
——⛔ **不得寫成「無此閘」**（否定性存在主張須附窮盡搜尋·CLAUDE.md 行號衛生 3.）；
**泛化波再窮舉**。

---

### 🔒 K-9-6-0 `region_min` 定義確認（KL 2026-08-03·**確認·非變更**·canonical）

`region_min` **維持**「各可建築街廓 `round(D_avg,2) × min_width` 之**最小值**」
（＝**最淺乘積**街廓）。**碼與正典皆不動**；本則僅加**防混註兩條**：

**⚠️ 防混一：「最淺街廓」≠「最淺乘積街廓」。**
畸零地最小寬係**逐街廓查表**（分區 × 正面路寬）⇒ 各街廓 `min_width` 可不同時，
「深度最淺者」與「乘積最小者」**會指向不同街廓、給出不同 `region_min`**。
本案六街廓皆住宅區、正面路寬 8／12m **同落附表「超過七公尺至十五公尺」列**
⇒ `min_width` 一律 `3.50`、二者**同解**（皆 R4）；**換案即分家**。
⇒ 敘述 `region_min` 時**一律說「最淺乘積街廓」**，勿簡稱「最淺街廓」。

**⚠️ 防混二：附表深度（`14.00`／`12.00`）永不進 MinA 之乘積。**
MinA 之**深度項**係 **N-19′ 實測平均深度**，或 KL 之**逐街廓覆寫值**
（`grep -n "_depth_use_by_blk\[_lbl\] = float" app.py`）。
**附表深度只供 K-9-2 之可建築門檻用**——此即 **K-9-6-a 同名不同量之戒**：
「量測值」（33〜47m·進乘積）與「門檻值」（14.00·不進乘積）**嚴禁互代**。

---

### K-9-6 最小深度與最小寬度之法定定義（KL 裁 2026-08-01·canonical·逐字）

**法規原文**（花蓮縣畸零地使用規則 §4·**逐字·勿改寫勿濃縮**）：

> 最小深度係指臨接之道路境界線至該基地後側境界線垂直距離之最小值。
> 最小寬度係指最小深度範圍內基地二側境界線間與道路境界線平行距離之最小值。
> 但道路境界線為曲線者，以該曲線與基地兩側境界線交點之連線視為道路境界線。

#### 🔒 K-9-6-a 兩個「最小深度」**同名不同量·嚴禁互代**

（體例比照 WARNING-C 之兩個「最小分配面積」）

| # | 名 | 是什麼 | 用途 | 本案值 |
|---|---|---|---|---|
| **1** | **量測值**：逐宗（逐範圍）之**最小深度** | 該宗**後緣連續段之全部端點**（二側境界線 ∩ BASELINE）中，**沿 BASELINE 法向至前緣線距離取 min** | **定義最小寬度之量測帶** | **33〜46m**（逐宗不同） |
| **2** | **門檻值**：畸零地**附表**之最小深度 | `get_min_lot_size(分區, 正面路寬)['min_depth']`（`HUALIEN_MIN_LOT_TABLE`） | **可否建築之判準** | **14.00**（住宅區·路寬 8/12m） |

> 🔴 **本表第 1 列已於 2026-08-05 更正**（K-6-A2 段五(c)-1）。
> **原述**為「該宗／該範圍**臨街段每點**至 BASELINE 垂距之 min」——其**受詞與同檔上方
> 逐字法規原文相反**（原文之受詞為**後側境界線**：「臨接之道路境界線至該基地**後側境界線**
> 垂直距離之最小值」`grep -n "最小深度係指臨接之道路境界線" 本檔`）。
> **KL 於 2026-08-05 以圖示裁定**：受詞為**後側境界線** ⇒ 量測點取**後緣角點**、非前緣端點。
> ⚠️ 該誤述已致碼面兩度修錯地方，見**失敗考古 §42**
> （`grep -n "^## 42\. " .claude/skills/failure-archaeology/SKILL.md`）。

🔴 **互代即失敗考古 #36 之形狀**（以「門檻」充當「量測量」）。
⇒ **最小寬度之量測帶深度＝第 1 種（量測值）**，**不是** `14.00`。

#### K-9-6-b 最小深度範圍之構造（**KL 定式·逐字**）

前緣線、與「**通過後緣連續段之最淺端點、平行前緣線**」之直線，二線所夾之**帶**；
再與**二側境界線**相交所圍之**封閉多邊形**。二側境界線依宗別：

> 📌 **「最淺端點」＝ 後緣連續段之全部端點取 min**，⛔ **非「二個角點」**
> ——後緣得由**二段以上共線邊**構成（實測端點數分布 `[2, 3]`·
> `verify/out/probe_K9_6_parcel_band.log:275`）。（K-6-A2 段五(c)-2 字面補正。）

> 🔴 **帶底之定義已於 2026-08-05 更正**（K-6-A2 段五(c)-1·KL 圖示裁定）。
> **原述**為「前緣線沿 **BASELINE 法向**平移 `min(垂距)`」——該式係 K-9-6-a 表列第 1 列
> **誤述之衍生**（受詞倒置），其帶底**恰切 BASELINE 而未必落在宗內**。
> **現行**帶底由**後緣角點**定位 ⇒ **恆通過宗地之一個頂點**
> （K-9-6-g「量測點須落在宗地範圍內」·⚠️ **該則僅適用非街角宗**·見 `K-9-6-g-1`；
> 惟**帶底**恆在宗內對街角第 1 宗**亦成立**——伸出宗外者為帶頂 `PQ`）。
> ⚠️ **投影係數不再參與帶底計算**：新式直接取該角點於前緣法向之深度分量。

| 宗別 | 二側境界線 |
|---|---|
| **第 2 宗以後** | 二條 **ALLOC_LINE** |
| **街角第 1 宗** | **SIDE_LINE** 與 **ALLOC_LINE** |
| **末端塊** | **BLOCK 邊界** 與 **ALLOC_LINE** |

**最小寬度** ＝ 該封閉多邊形內，**所有平行前緣線之線段距離取 min**。

> 🔧 **指標（`W-G.9-41`·⛔ 上文原句一字未刪）**：本節之**帶底定式**已由
> **KL 裁 2026-08-16** 變更，新定式見本檔 **`K-9-10`**。
> ⛔ 引用本節之帶底時須併讀該新節；⛔ 不得再以本節之定式作為帶底。（⛔ 此處不重述內容。）

#### 🔴 K-9-6-b-1 更正：量測用之「道路境界線」＝ K-9-8 之延伸線段（KL 裁 2026-08-02）

**§4 原文之引述逐字不動**（見上）；其**於本倉之落地構造**更正如下：

> 量測用之「**道路境界線**」＝ **K-9-8 (3) 之延伸線段**（平行 FRONT_LINE、通過 `P`）；
> **當 `P` 在 FRONT_LINE 上時退化為 FRONT_LINE 本身**。

⇒ 舊敘述將「臨接之道路境界線」逕讀為 `FRONT_LINE`，**於 `P` 落在截角邊時不成立**。
**§4 於本倉之落地構造見 `grep -n "^### K-9-8 " 本檔`。**

#### 🔒 K-9-6-b-2 裁定：量測帶之**兩條邊界線**皆自 PQ 起算（KL 裁 2026-08-05）

K-9-6-b 之帶原述為「前緣線、與『前緣線沿 BASELINE 法向平移 min(垂距)』之平行線」。
依 K-9-6-b-1 之更正，其中**兩處**「前緣線」皆應讀為 **K-9-8 (3) 之延伸線段 `PQ`**
⇒ 帶 ＝ `[PQ, PQ 沿 BASELINE 法向平移 min(垂距)]`。
**當 `P_block` 落在 FRONT 重疊段時 `PQ ≡ FRONT_LINE`，退化回原式。**

> 🔒 **本裁定於 2026-08-05 之帶底更正後維持不變**（K-6-A2 段五(c)-1）：
> **本次改動者為帶底之定位點（改由後緣角點定位），非其起算線**。
> 帶 ＝ `[PQ, 通過後緣連續段之最淺端點且平行 PQ 之直線]`；
> ⛔ 起算線仍為 `PQ`、不得回退為 FRONT_LINE。

⚠️ **只改帶的兩條邊界線，不改量測方向**——最小寬度仍為「平行**前緣線**之線段距離取 min」。
  因 `PQ ∥ FRONT_LINE`，二者等價；⛔ 不得順手把量測方向改成平行 `PQ` 以外的任何軸，
  亦不得動 K-9-6-b 之「二側境界線依宗別」表。

🔒 **(c) 切換之放行**（KL 裁 2026-08-05）：生產自 **K-6-A2 段四(c)** 之 commit 起改走乙式。
   放行時之土地後果：最大位移 `0.004846 m`、候選資格餘裕／擾動比 **142×**、
   PK 決定性欄位**零翻盤**。
   （餘裕之最小格 ＝ `R3/右 628-28(1)` 之真交集 `13.79 ㎡` 距資格線 `1.0 ㎡` ⇒ `12.79 ㎡`；
    最大擾動 `0.09 ㎡`。實料：`verify/out/probe_K9_7d_pk_impact.log`【A2】。）

##### K-9-6-b-2-a 落地狀態（K-6-A2 段四(c)-2(a′) 立·**段四(c) 已切換**）

✅ **已裁定；已落地並已切換為生產**。第二層求解器
（`grep -n "K-9-7-d \*\*第二層分支\*\*" app.py`·以 `block_vertices` 啟用·**生產已傳**）
**與夾具**（`grep -n "def _band_min_width" verify/fixture_corner_range_k8.py`）。
✅ **生產 `_build_corner_range_v3` 已切換**（K-6-A2 段四(c)·KL 放行 2026-08-05）。
推導：`docs/reports/W-G.5_K6-A2-段四c2a2_乙式八支推導.md`；
攤表：`docs/reports/W-G.5_K6-A2-段四c2a2_乙式實作與攤表.md`；
切換：`docs/reports/W-G.5_K6-A2-段四c_切換乙式.md`。
🗄️ **甲式**（帶 ＝ `[FRONT, FRONT + min(垂距)]`·只改帶深不改上緣）**已作廢**
（原推導 `docs/reports/W-G.5_K6-A2-段四c2a_第二層分支推導.md` §三／§四 已加註·原文存查勿刪）。

#### 🔒 K-9-6-g 量測點與量測範圍（**KL 圖示註記·逐字**·裁 2026-08-05）

> ⚠️ **讀本則前必先讀 `K-9-8` 實作紅線**
> （`grep -n "^#### ⛔ K-9-8 實作紅線" 本檔`）——本則之適用範圍已因該紅線而限縮，見下。

> 最小深度、最小寬度之量測點一定要落在宗地範圍內。
> 最小寬度量測的範圍：前緣線、二側境界線、最小深度範圍線。

##### 🔴 K-9-6-g-1 適用範圍之限縮（**KL 補充裁定 2026-08-06·逐字**）

> **適用範圍：非街角第 1 宗。**
> 該圖示所繪為前緣落在 FRONT_LINE、二側皆為 ALLOC_LINE 之情形。
>
> **⛔ 例外——街角第 1 宗**：其往街廓內部推進之 ALLOC_LINE，交點可能落在**道路截角邊**上；
> 自該交點平行 FRONT_LINE 延伸至 SIDE_LINE，延伸段會通過**截角被切掉的區域**。
> ⇒ 街角第 1 宗之最小寬度與最小深度**得蘊含未截角之部分範圍**，
> **不以落在該宗地籍範圍內為必要**。依 `K-9-8`（虛擬還原塊）為準。
>
> ⚠️ 帶之**兩端不同號**：帶底（後緣角點）恆在宗內；伸出宗外者為帶頂（`PQ`）。

**案由**：本則原文係**自單一圖示歸納**（非街角宗）而寫成**全稱**條款，
與同檔 `K-9-8` 實作紅線「虛擬塊在街角處會伸出實際街廓多邊形之外，**此為本裁定之本意**」
**逐字互斥**。⇒ 教訓入**失敗考古 節 47**（`grep -n "^## 47\. " .claude/skills/failure-archaeology/SKILL.md`），
並據以補強節 42 之戒 **(d)**（**寫入**新條款前須對讀同檔既有逐字／紅線條款）。

##### K-9-6-g-2 對街角第 1 宗之代入（**CC 判定·自洽·非新條款**）

K-9-6-g 之「量測範圍三線」對街角第 1 宗**無須另立條款**——逐項代入既有正典即自洽：

| 三線 | 依何則讀 | 街角第 1 宗之值 |
|---|---|---|
| 前緣線 | **K-9-6-b-1** | **`PQ`**（K-9-8 (3) 之延伸線段·`P` 在 FRONT 上時退化為 FRONT_LINE） |
| 二側境界線 | **K-9-6-b** 之依宗別表 | **SIDE_LINE ＋ ALLOC_LINE** |
| 最小深度範圍線 | **K-9-6-b**（(c)-1 更正） | 通過後緣連續段最淺端點、平行 `PQ` 之直線 |

⇒ 代入後之量測範圍 ＝ **`K-9-8` 虛擬還原塊 ∩ 帶**：
`K-9-8 (4)` 供**多邊形**（二側界為何、且**禁與街廓多邊形取交集**），
`K-9-6-b` 供**帶之深度區間**（`PQ` → 帶底）。二者**分工、非重疊定義** ⇒ **自洽**。
✅ **碼面已如此組合·已更正並已切換**（K-6-A2 段六前置·**KL 放行 2026-08-06**）：
`k98_virtual_measure_block` 現以**虛擬塊為多邊形、`P_block` 為 `front_pt`、
且傳入 `baseline_pts`** 呼叫 `parcel_min_width_n14`
（`grep -n "min_width = float(parcel_min_width_n14" app.py`）
⇒ **帶底由 `_n14_band_hi` 定**（＝後緣連續段之最淺端點·同上表第 3 列）。

> 🗄️ **沿革記載（存查勿刪）**：本段初版（K-6-A2 段五(c)-2 commit 1）寫
> 「以…**`depth_from_PQ` 為帶深**呼叫」——**與上表第 3 列互斥**（相距 7 行·**節 42 之同一病灶**）：
> `depth_from_PQ = min(_dep(P_block), _dep(Q))`，而 `P_block`／`Q` **皆在 `PQ` 上**
> ⇒ 其**帶深受詞 ＝ 臨街段**，非本則所定之**後緣**。該敘述於本批切換後**已不再成立**。

##### 🔒 K-9-6-g-2-a 裁定：放行 k98 之帶底切換（KL 裁 2026-08-06·「照新量法切換」）

**先算後切**（KL 裁「寬度影響先算完再切」）之攤表：
`docs/reports/W-G.7_K6-A2-段六_GB40寬度攤表.md`／
實料（**凍存**）`verify/out/probe_K9_seg6_w_delta_切換前凍存.log`。

**土地後果（⛔ 非「已放行」四字了事）**：

| 情境 | 門檻 | 結果 |
|---|---:|---|
| 退縮 **0m** | 3.50 | **七宗全通過·翻盤 0** |
| 退縮 **3.5m** | 7.00 | **翻盤 1 宗** ＝ `R6/right 628-18(1)`：`4.810794 → 7.221163`、餘裕 `−2.189206 → +0.221163` ⇒ **由強制留設抵費地改為可分配**；`R1/right 628(5)` **仍不足**（餘裕 `−2.827283 → −0.152969`） |

`max|Δ寬|` ＝ **`2.674315 m`**（`0m R1/right`）。登記：**GB-40**（已切換）。

⚠️ **編號說明（CLAUDE.md 🔒 編號衝突以倉內為準）**：施工單原標 `K-9-6-c`，
惟 `K-9-6-c` 於倉內**已被占用**（「與 K-9-2 之分工」·見下節），且 `a`〜`f` 全數占用
⇒ 依「新則取下一個未占用號、⛔ 禁改編既有正典編號」取 **`K-9-6-g`**。

🔴 **本則即帶底更正之判準來源**：舊式帶底（前緣線沿 BASELINE 法向平移）**恰切 BASELINE**、
其量測點**未必落在宗內**（實測 34/118 宗落在宗外 ⇒ 弦長 0 而停機——**凍存**實料
`verify/out/probe_K9_6_parcel_band_段五a凍存.log`【D】段；⚠️ **非**同名之現行檔，
後者已由 (c)-1 重跑覆寫）；新式帶底**恆通過宗地之後緣角點** ⇒ 本則成立。

#### K-9-6-c 與 K-9-2 之分工（**不重複管**）

**附表深度之合規**（宗地最小深度 ≥ 附表深度）由 **K-9-2 街廓層剖面警示**管；
**K-9-6 不重複管**。⇒ **K-9-2 不受本次更正影響。**

#### K-9-6-d 落地狀態

**已裁定；實作屬 K-6-A2。段五僅完成正典與登記，碼面行為未變。**
碼面現況之兩處定義錯誤已登記為 **GB-13／GB-14**（不改）。

#### 🔒 K-9-6-h 最小深度之量法修正（KL 裁 2026-08-15·canonical·**逐字**·五款）

⚠️ **編號現查**：`K-9-6-*` 已占用 `0`／`a`〜`g` ⇒ `h` 為下一未占用（`W-G.9-34` 現查）。

── KL 裁 2026-08-15（五款·canonical）──
① 最小深度之定義：以正街道路境界線為起算基準，沿**垂直於正街道路境界線**之
   方向量至後側境界線；取所有合格量測之最小值。
   合格之條件：該垂線須**整條落在該宗地內**，且與**後側境界線之實體線段**相交
   （⛔ 非其延長線）。不合格之量測點**排除**，⛔ 不計入最小值。
② 臨後側境界線之判準：宗地與後側境界線須有交點（⛔ 不得無交點）；
   且於「兩側界線交後側境界線之二點」所夾線段範圍內，**至少須有一條**合格垂線。
   **一條都沒有 ⇒ ⛔ 不算臨後側境界線 ⇒ 停機報錯**
   （KL 裁：此係配地時即不應產生之形狀，⛔ 非產生後再判不合格）。
   > 🔴🔴 **本款之「停機報錯」已由 KL 於 2026-08-17 變更**（`W-G.9-43` 加註·⛔ 上行原文一字不刪）：
   > 改為**不停機報錯 ⇒ 該宗不配地 ⇒ 由下一投影序號之土地遞補**。
   > ⛔ 此處不重述，逐字與六款見錨
   > `grep -n "^### 🔒 K-9-11 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
   > 🔒 **⛔ 變更者僅「後果」**——本款之**判準**（至少須有一條合格垂線）、①之**合格垂線定義**、
   > 及括號內之**判定時點**（配地時判）**均未變更**。
③ 最小寬度之計算域：於「基地最小深度範圍內」計算。
④ 平行街廓之例外：若該街廓之正街道路境界線與後側境界線**平行**，
   則該街廓所有宗地之深度固定 ＝ 兩線間距
   ⇒ **⛔ 停用①②之垂線容納條件**；
   寬度改取「兩側界線交正街道路境界線之線段長」與
   「兩側界線交後側境界線之線段長」二者之**最小值**，須 ≥ 畸零地最小寬。
   **平行之判準**：兩線之 X、Y 向量分量於**小數點後第 3 位**皆相同，即視為平行。
   原則性依據：KL 繪圖系統之座標顯示精度為小數點後第 3 位。
   ⛔ **不綁夾角條件**——「夾角 <60° 或 >120°」係說明該問題何時會發生，
   ⛔ **非判準**（KL 裁：乙）。
⑤ 街角第 1 宗：其最小深度與最小寬度**均以未截角前之形狀量測**
   （畸零地使用規則 §4 末段：角地應截角時，其寬度及深度係指截角前者）。
── 送達途徑：**經對話側送達**·⛔ 倉內無從覆核。⛔ 本批不實作。──
── 示意：見 B-0 所入倉之座標檔（⛔ 以該檔為準，⛔ 不另繪）。──

**示意圖之倉內落點**（B-0·`W-G.9-34` 入倉）：
`docs/specs/figures/最小深度示意圖＿座標.cnt`／`….lin`
（PROVENANCE ＝ `docs/specs/figures/PROVENANCE_最小深度示意圖.md`）。
🔴 **該圖為拓樸之權威、⛔ 非度量之權威**（圖面自身不精確之最大實測 ＝ `0.055803` > `1e-3`）
⇒ ⛔ **不得作為 `1e-3` 級數值對拍之 fixture**（逐筆實測見該 PROVENANCE §四）。

**與本則相關之待決登記**（`W-G.9-34`·⛔ 本則不預判）：
· **`GB-65`**：④ 之平行判準有**兩種讀法**（單位向量分量比 vs 夾角上界），其擇一尚未定；
  且讀法甲之角度容差**隨方向而變**（非等向）。
· **`GB-66`**：④ 之平行／不平行係**分岔閘**（兩側走不同量法），其**兩側連續性尚未證明**。

**與現行碼之落差**（`W-G.9-34` 現查·⛔ 本批不實作）：
| 款 | 現行碼 | 錨 |
|---|---|---|
| ① 量測軸 | 已為**正街（前緣線）法向** ⇒ **與①一致** | `app.py:6447`「帶底 ＝ …**平行前緣線**之直線」·`:6566` ⛔ 無投影係數 |
| ① 垂線容納條件 | **未實作** | 現行 `t_hi = min(後緣角點之 t)`·⛔ 不檢查垂線是否整條落在宗內 |
| ② 臨後側判準 | **僅有無交點**（`not touch`／`crosses`） | `app.py:6257` 表列 |
| ⑤ 截角前 | **未實作**——寬度量測收 `cut_coords` ＝ **截角後** | `app.py:6968` 逐字·入口 `:7012`；`:6925`「現行碼**尚未實作實量**」 |

---

### K-9-7 街角規定範圍之 ALLOC_LINE 解析定位（KL 定式·claude.ai 驗算·canonical）✅**已實作**（K-6-A2 段三(c)·KL 放行 2026-08-04）

> ✅ **落地錨**：求解器 `grep -n "def k97_solve_alloc_t" app.py`；
> 生產接線 `grep -n "_k97 = k97_solve_alloc_t" app.py`（**舊斜率式已移除·生產只有一條路徑**）。
> **對拍靶**：切換後生產實算之街角規定面積與段三(b′) 探針「新」欄**十六格逐格相符**
> （`verify/out/K6A2_seg3c_16cell.log`）。
> **土地後果**：8 格位移（最大 R5 左 −16.56cm ⇒ −7.27㎡）；
> **PK 零翻盤**（候選／合格／winner／`forced_offset` 十六格不變·
> `verify/out/probe_ruling_K9_7_pk_impact.log`）。
>
> ⚠️ **第二層分支（`P_block` 落截角邊 vs FRONT 重疊段）仍屬 K-9-8·段四**
> ——本次只實作 K-9-7 單獨即可完全決定之**第一層四分支（①②③④）**；
> 現行對 `P_front` **只偵測、不臆造 `P_block`**（claude.ai 裁 2026-08-04·交接文待裁 B）。

令 `t` ＝ ALLOC_LINE 自街角往街廓內推之距離。

**【深度】** `D(t)` ＝ 範圍臨街段（街角 `Q0` → `ALLOC_LINE ∩ 前緣線` 之 `P(t)`）
各點至 BASELINE 垂距之 **min**。垂距沿前緣線**線性** ⇒ min 必落於兩端之一：

- 深度往內**遞增** ⇒ min 恆在 `Q0` ⇒ **`D` 為常數**
  （＝前緣線 ∩ SIDE_LINE 交點至 BASELINE 之垂距）
- 深度往內**遞減** ⇒ min 在 `P(t)` ⇒ **`D(t) = D₀ − m·t`**（線性）

**【寬度】** 二側界皆直線 ⇒ 沿深度線性；ALLOC_LINE 平移只令其**整體平移** ⇒

```
w(d, t) = w₀ + c·t + k·d
```

**【最小寬度】** `W(t) = min( w(0,t), w(D(t),t) )`，四分支：

| 分支 | 條件 | `W(t)` |
|---|---|---|
| ① | `k ≥ 0` | `w₀ + c·t`（**深度完全不影響**） |
| ② | `k < 0`、`D` 常數 | `(w₀ + k·D₀) + c·t` |
| ③ | `k < 0`、`D(t)=D₀−m·t` | `(w₀ + k·D₀) + (c − k·m)·t` |
| ④ | 分支切換點 | 見下 |

⇒ **四種情形 `W(t)` 皆為 `t` 之一次式** ⇒ `W(t*) = 退縮 ＋ 畸零地最小寬`
**一次除法解出**。深度與寬度互相依賴，但兩邊皆線性、**耦合後仍線性**。

🔴 **禁二分／禁取樣／禁迭代兜底**（同 K-8 §四 既有鐵律）。

🔴 **須補分支判斷**：`t` 變動時「深度 min 在哪一端」與「寬度 min 在哪一端」**皆可能切換**
⇒ 嚴格為**分段線性**，原為最多 `2×2 ＝ 4` 支。

🔴 **K-9-7-d 分支擴充（KL 裁 2026-08-02·因 K-9-8 而生）**：**新增一層分支條件**
——`P` 落在**截角邊**，抑或已滑過該截角端點落到 **FRONT_LINE 重疊段**
⇒ **最多 `2×2×2 ＝ 8` 支**。
**線性性不受破壞**：`P` 沿截角邊移動時，`PQ` 之平移量對 ALLOC_LINE 位移仍為**一次式**
⇒ **仍為解析解、仍禁二分／禁取樣／禁迭代兜底**。
每支仍**各解一次除法**並檢查 `t*` 是否落在該支**有效區間**；**分支切換點須明列**
（切換點 ＝ `P` 通過截角端點之 `t` 值）。
🔬 **本案實測（🔧 K-6-A2 段三 更正·2026-08-03）**：**10 格落在「FRONT 重疊段」之退化支、
2 格落在「截角邊」支**——即 **`R4/right` 於 `0m` 與 `3.5m` 兩情境皆落截角邊**
（`t_P = +0.013039m`）。
實料：`verify/out/probe_ruling_K9_corner_width.log:72`（0m）／`:78`（3.5m）。

> ⚠️ **前版敘述「12 格全部落在 FRONT 重疊段、截角邊支零實例」與已入庫實料不符，已作廢。**
> **成因**（同 log `:23-26`·**非圖面畫得準不準**）：
> 「退縮 `0m` 時該範圍可能**整個落在截角區內**，其極值遂落在**截角邊**上……
> 且同一街角於退縮 `3.5m` 時回到 ~1e-5（範圍夠大、已越過截角）。」
> ⇒ 此正是 **K-9-5-2 ②「退縮 ＝ 0 之情形，範圍可能不含 FRONT_LINE 段」之實證**
> （`grep -n "^### 🔒 K-9-5-2 " 本檔`）。
> ⚠️ 惟 `R4/right` **兩情境皆**落截角邊（非僅 0m）⇒ 成因不止於退縮值，
> 尚與該角之截角股長／範圍尺度有關；**本批不臆測其完整機制**（禁把未驗證之機制寫成因）。

**每支各解一次除法**，再檢查 `t*` 是否落在該支之**有效區間**；不落入則換支。**仍為解析解。**

🔴 現行 §三 之斜率用 `c`（假設帶深固定）；**正解為 `(c − k·m)`**。**本批只寫規格、不改碼。**

🔴 **退化情形**（`k`、`m`、分母為零／範圍不可構造）一律 **loud raise**，禁兜底。

#### ⚠️ K-9-7-a 記號衝突警語（**碼面對照時必讀**）

**衝突一：`c`／`k`（同名異義）。**
本節之 `c` ＝ `∂w/∂t`、`k` ＝ `∂w/∂d`。
而 `app._build_corner_range_v3` 之**區域變數** `k = sigma / den_a`
（`grep -n "k = sigma / den_a" app.py`）**係本節之 `c`**，**非**本節之 `k`。
**同名異義·勿混**（登記見 GB-14）。

**🔴 衝突二：`P`（同名異物·K-6-A2 段三補記·2026-08-03）。**
正典內有**兩個 `P`**，指涉**不同交點**：

| 出處 | `P` 之定義 | 與誰相交 |
|---|---|---|
| **K-9-7**（本節·`P(t)`） | **`ALLOC_LINE ∩ 前緣線`** | 前緣線 |
| **K-9-8 (2)**（`P`） | **`L_in ∩ BLOCK 邊界`** | BLOCK 邊界（可能落截角邊） |

⚠️ **二者於「`P` 落在 FRONT 重疊段」時重合**，而本案 **12 格中 10 格如此**
（見 K-9-7-d 之更正註）⇒ **多數格次分辨不出**，極易誤以為同一物。

🔒 **實作時必須分開命名**（如 `P_front`／`P_block`）
——否則做 **K-9-8** 時會踩雷（該處之 `P` 決定延伸線段之位置，取錯即整個虛擬量測塊錯位）。

#### K-9-7-b 與 K-8 §三〜§五 之關係

⛔ **本節不改動 K-8 §三〜§五 原文**。
**K-8 §三之「帶深來源」已由本節（K-9-6-b／K-9-7）取代**；
K-8 §三〜§五 之**其餘構造仍有效**（§四 之滑動語意、§五 之範圍與面積、截角前／後之二分）。

#### K-9-7-c 本案實測（段五 probe·**只量不判**）

實料：`verify/out/probe_ruling_K9_corner_width.log`
（`python verify/probes/probe_ruling_K9_corner_width.py`·rc 恆 0）。
斜率係以**擾動 `_build_corner_range_v3` 之輸入、觀測其輸出**取得（app 為 oracle）。

| 街廓/側 | `c` | `k` | `m` | 分支 |
|---|---|---|---|---|
| R1/left | −1.003270 | 0.000000 | （非線性·見下） | ① |
| R1/right | +1.003270 | 0.000000 | −0.000000 | ① |
| R2/left | +1.002499 | −0.075547 | +0.009750 | ③ |
| R3/right | +1.004284 | 0.000000 | （非線性·見下） | ① |
| R4/left | +1.001034 | −0.047599 | +0.000266 | ③ |
| R4/right | −1.001034 | −0.050087 | 0.000000 | ② |
| R5/left | +1.004284 | −0.075610 | −0.000000 | ② |
| R6/right | −1.003245 | 0.000000 | 0.000000 | ① |

⇒ **三支皆有實例**（① 4 個／② 2 個／③ 2 個）；`k ≥ 0` 者 4 個
——即**半數街角之深度定義對其寬度完全無影響**（分支 ①）。

> ⚠️ **本表之 `k`／`m` 係擾動觀測值（有效 `∂W/∂D`／釘住後 `−∂D/∂t`），非解析偏導，禁當解析靶。**
> 與 K-9-7 規格之 `k = ∂w/∂d`、`m = −∂D/∂t` **同名不同量**（體例同 K-9-6-a）。
> **實測數字一字未改**；本註僅正名其量之身分。（K-6-A2 段三(b′) §二-3 補）
>
> **何以不符卻自洽**（八格逐格可解釋·claude.ai 2026-08-03 查明）：
> - **`k` 欄實為 `∂W/∂D`**：解析 `k ≥ 0` 時最小寬取在 `d=0` ⇒ `W` 與深度無關
>   ⇒ **觀測必得 `0`**。此解釋全部三格不符（R1 左／R1 右／R3 右）；
>   `k < 0` 之四格觀測＝解析、**全符** ⇒ **八格自洽**。
> - **`m` 欄實為「釘住後」之 `−∂D/∂t`**：分支 ①② 之 `D` 釘在 `Q0` ⇒ **觀測 `m = 0`**，
>   而解析之原始梯度不為零 ⇒ 解釋 R6 右／R5 左／R4 右。
>   R1 左／R3 右之「（非線性）」則為**擾動跨過拗點**之假象——
>   解析值重現本節末段自載之**次段斜率**（R3 右 `+0.012109` **逐位相同**）。
>
> 解析值見 `verify/out/probe_ruling_K9_7_alloc_t.log`【A】段
> （`k97_solve_alloc_t`·`grep -n "def k97_solve_alloc_t" app.py`）。

🔴 **`D(t)` 之分段線性已由實測坐實**（＝本節「須補分支判斷」之實證）：
**R1/left** 與 **R3/right** 之 `D(t)` 三點量得
`29.5051 → 33.1623 → 33.1621`（R1 左）／`41.4195 → 44.8499 → 44.8439`（R3 右）
——**首段跳 3.66m／3.43m、次段方回穩**（次段斜率 `−0.000413`／`+0.012109`）
⇒ **`D(t)` 於該區間有拗點**，非單一直線。**⇒ 實作 K-9-7 時分支判斷為必要、非保險。**

---

### K-9-8 虛擬量測塊之構成（KL 裁 2026-08-02·canonical·逐字）✅**已實作**（K-6-A2 段四(c)·KL 放行 2026-08-05）

量測宗地最小寬度／最小深度時，所用之多邊形為**虛擬還原塊**，構成如下：

1. 取該宗「**往街廓內部那一側**」之 ALLOC_LINE，記為 `L_in`。
2. `P` ＝ `L_in` 與 **BLOCK 邊界**之交點。
   `P` 可能落在**截角邊**上，也可能落在**與 FRONT_LINE 重疊的那段**上。
3. 自 `P` 作**平行 FRONT_LINE** 之線段，往街角方向延伸，
   **止於該宗靠街角那一側之境界線**（KL 裁「甲」·2026-08-02）：
   - 街角第 1 宗 → 止於 **SIDE_LINE**
   - 第 2 宗以後 → 止於**前一宗之 ALLOC_LINE**（必要時將該線延伸）
   - 末端塊 → 止於 **BLOCK 邊界**

   ⛔ 不得一律延伸至 SIDE_LINE——第 2 宗若延伸至 SIDE_LINE 會把第 1 宗整塊包入，
   量得之最小寬度將是**兩宗合計**之寬度。
4. 虛擬塊 ＝ 該延伸線段、該宗二側境界線、**BASELINE** 所圍之多邊形。
5. **深度自該延伸線段起算**，非自 FRONT_LINE 起算。
6. **規則退化**：當 `P` 落在與 FRONT_LINE 重疊之段時，延伸線段與 FRONT_LINE 重疊
   ⇒ 退化為「自 FRONT_LINE 起算」之原式。**一條規則涵蓋兩種情形，禁分立兩式。**
7. 虛擬塊**僅供量測**。地籍、面積、G、街角規定面積一律取**截角後**，
   **不含虛擬還原部分**。

#### ⛔ K-9-8 實作紅線（**兩條·逐字**）

> ⚠️ 虛擬塊在街角處會**伸出實際街廓多邊形之外**（進入截角被切掉的區域），
> 此為本裁定之本意。
> ⛔ **禁與街廓多邊形取交集**——取交集即把還原部分裁掉，等於沒還原。
> ⛔ 虛擬塊之面積**禁進入任何面積帳**（`ΣG ＋ 抵費地 ＝ 街廓` 之守恆不得受擾）。

⚠️ **與 `K-9-6-g` 之關係（KL 補充裁定 2026-08-06·雙向對讀錨之一）**：
`K-9-6-g` 之「量測點須落在宗地範圍內」**不適用本則**——二者**非衝突、而是分工**：
**非街角宗**適用 `K-9-6-g`（量測點須在宗內）；**街角第 1 宗**適用本則
（虛擬塊得伸出街廓外·**此為本意**）。見 `grep -n "^##### 🔴 K-9-6-g-1 " 本檔`。
⚠️ 帶之**兩端不同號**：帶底（後緣角點）恆在宗內；伸出宗外者為帶頂（`PQ`）。

#### K-9-8-a 落地狀態

🔧 **更新（K-6-A2 段四(c)·2026-08-05）**：
- **GB-19**（`_build_corner_range_v3` §三 之量測起算線）**✅ 已落地**
  ——生產已改走 K-9-7-d 第二層（`grep -n -A4 "_k97 = k97_solve_alloc_t" app.py`
  之四行內含 `block_vertices`），量測帶依 **K-9-6-b-2（乙式）** 為 `[PQ, PQ + D]`。
  獨立驗收：`verify/fixture_corner_range_k8.py`（**寫在前批、本批一字未動**）
  由 **FAIL → PASS**（16 格 T1 殘差 `~1e-10`）。
- **GB-18**（`parcel_min_width_n14` 之量測多邊形為 `cut_coords`）**仍未修**
  ——已重標**段六**（服務全部**非街角**宗）。
🗄️ 原文（段七當時之狀態）：「已裁定；實作屬 K-6-A2。段七僅完成正典與登記，碼面行為未變。」

#### 🔬 K-9-8-b 本案實測（段七 probe·**只量不判**）

實料：`verify/out/probe_ruling_K9_corner_width_V6_1.log`（以 `data/V6_1.dxf` 跑）。

- **於 `V6_1`**：12 格（8 街角 × 2 情境，扣除無 winner 者）之 `P` **全部**落在「與 FRONT 重疊之段」
  ⇒ 全部走 K-9-8 (6) 之退化分支（內縮量 ≤ `6e-6 m`）。
  （實料 `verify/out/probe_ruling_K9_corner_width_V6_1.log`·`grep -c "截角邊（t_P"` ⇒ **0**）

  🔴 **更正（K-6-A2 段四 §一·2026-08-04）：「本案零實例」之敘述僅對 `V6_1` 成立，
  對【現行生產圖 `V6.dxf`】不成立。**
  於 `V6`：**10 格落 FRONT 重疊段、2 格落截角邊**——`R4/right` 於 `0m` 與 `3.5m` 兩情境皆然
  （`t_P = +0.013039m`；實料 `verify/out/probe_ruling_K9_corner_width.log:72`／`:78`·
  **該表表頭逐字載明**「`P ＝ L_in（該宗往街廓內側之 ALLOC_LINE）∩ BLOCK 邊界`」·同檔 `:65`
  ⇒ 確為 **K-9-8 (2) 之 `P_block`**、非 K-9-7 之 `P_front`）。

  ⇒ **兩處敘述之差別在【量測所用 DXF】**，非其一有誤；二者之實料引用**皆保留**。
  ⇒ **(a) 段四 ⛔ 不得以「本案零實例」為由略過截角邊支**——在現行生產圖上它是**活的**。
  ⇒ **(b) 波末換 `V6_1`（GB-16）後，截角邊支將失去其唯一真資料覆蓋。**
  ⚠️ 該唯一實例出現於 `R4`，而 GB-15 記 `R4` 圖面落差 `+0.014826`、與此處內縮量 `0.013039`
  **同階**。**⛔ 本註不主張二者有因果**——同階只是同階。**成因欄留白，待段四查。**
- 虛擬塊 − 實際宗地之差集，**其「在街廓外」之部分與該側截角三角形面積逐格相符**
  （例：R1 右 `5.771147` vs 截角 `5.771148㎡`）⇒ **還原確實發生、且僅還原截角**。
- 三代寬度落差（截角後·0m）：`R1 右 0.7855 → 4.1725`／`R5 左 0.6706 → 3.8531`／
  `R6 右 1.3999 → 4.8108` ⇒ **K-9-8 之還原使街角宗寬度顯著回升**。

---

### K-9 §四、落地狀態

K-9 全七則**已裁定；實作屬 K-6-A2**。
**段四／段五僅完成正典與登記，現行碼行為未變。**

| 則 | 實作落點（K-6-A2） |
|---|---|
| K-9-1 ⬜ **未實作**（段六） | §零-2 街廓最小建築面積之 UI 欄與判準（街角宗不豁免） |
| K-9-2 ✅**已實作**（K-6-A2 段一·`9880164`） | 街廓層剖面警示（解析·兩端取 min·禁取樣）。**不受 K-9-6 更正影響** |
| K-9-3 ⬜ **未實作**（**段五**·與 K-9-6 同批） | 拆除宗地層深度量測（含 `parcel_min_width_n14` 之 E-2′ 閘之角色改變·見 GB-10） |
| K-9-4 ✅**已實作**（K-6-A2 段二·GB-24 之閘） | BASELINE 臨接閘（停機報錯） |
| K-9-5 ⬜ **未實作**（段六） | 街角第 1 宗寬度實量（**截角前**幾何）＋逐宗兩門檻分流 |
| **K-9-6** 🔶 **部分**（`b-1`／`b-2` ✅ 已落地於街角規定範圍·段四(c)；**宗地層之 `min_depth`（GB-13）⬜ 未實作·段五**） | **量測帶深改用「逐宗／逐範圍之自身最小深度」**——`parcel_min_width_n14` 之 `min_depth` 引數（**GB-13**）＋ `_build_corner_range_v3` §三 之帶深（**GB-14**） |
| **K-9-7** ✅**已實作**（段三(c)·`0429772`） | **ALLOC_LINE 之分段線性解析定位**（四支·每支一次除法·**禁二分／取樣／迭代**）；斜率由 `c` 改為 `(c − k·m)`（**GB-14**） |
| **K-9-8** ✅**已實作**（段四(c)） | **虛擬還原塊**取代 `cut_coords` 為量測多邊形（**GB-18**·🔴 仍未修·已重標段六）＋量測起算線改為「過 `P` 之平行 FRONT 線段」（**GB-19**·✅ **已落地** 2026-08-05）＋深度自該線段起算（✅ 乙式 K-9-6-b-2） |

**K-6-A2 之前置**：`verify/probes/probe_ruling_K9_corner_width.py` 之實測表
須先經 **KL 過目**。
🔴 **該 probe 已於段五整支改寫**——**前版（`b061b00`）之八格寬度數字全部作廢**
（帶深誤用附表 `14.00`）。

🔴 **前置未決項（段四 probe 實測·三項·待 KL 域裁）**——登記見
`docs/reports/W-G.4_泛用阻塞項登記表.md` §一-B（`grep -n "^## 一-B" 該檔`）：

| # | 未決項 | 一句話 |
|---|---|---|
| **U-K9-1** | 截角前幾何之**點接合**情形 | 宗僅以**一點**接觸截角三角形 ⇒ 聯集為 `MultiPolygon` ⇒ **截角前幾何不良定義**（⛔ 禁 `buffer(±ε)` 合攏·實測非單調）。🔬 **段五補測**：R1 左與 R4 右之頂點距 **絕對框／局部框皆 `0.000e+00`**（＝**頂點完全重合**）⇒ 「次微米級座標不合之假點接合」假說**已否證**·**兩者皆為真點接合** |
| **U-K9-2** ✅**已裁：修圖**（2026-08-02·見 K-9-6-e·待波末批生效） | `R4/right 628-1(1)` 連**截角後**寬度都量不出 | `parcel_min_width_n14` 之臨接 FRONT 閘 raise（−0.014826m > 容差 0.01m）⇒ K-9-5 上閘後**必停機**。🔬 **段五已溯源定讞：CAD 圖面既有**（見 K-9-6-e） |
| **U-K9-3** | §零-3 末句之「蘊含」語 | 與 K-9-5「實量」不相容（詳見上開 **K-9 §三-1**）。🔬 **段五補測**：包含關係 `範圍 ⊆ 宗` **並非恆成立**（見 K-9-6-f） |

> 🔴 **本節原載之「土地後果預告」已作廢（段五·2026-08-02）**：
> ~~可量之 8 格中 1 格會翻盤——3.5m R1/right 628(5) 截角前寬 6.8470m < 門檻 7.00m。~~
> 該組數字係以**畸零地附表之 `14.00`** 為量測帶深所得，**帶深定義錯誤**（K-9-6）
> ⇒ **全部作廢，不得引用**。改寫後之實測見 **K-9-7-c** 與
> `verify/out/probe_ruling_K9_corner_width.log`。
> ⚠️ **U-K9-1／U-K9-2 之前提已隨帶深更正而改變 ⇒ 兩題暫緩、段五不代裁。**

### 🔬 K-9-6-e `628-1(1)` 之 `−1.4826e-2m` 溯源（段五·**已定讞**）

**結論：CAD 圖面既有、非程式產生。** 逐段追跡（實料同上 log 之【G】段）：

1. 街廓 `R4` 多邊形（`cb_by['R4']['vertices']`·W-A.3 方案B **直讀** DXF BLOCK 閉合多邊形）
   之 `t` 最大頂點 ＝ `(310582.658400, 2651871.934270)`，`t = +0.014826m`。
2. 宗 `628-1(1)` 之 `cut_coords` 之 `t` 最大頂點 ＝ **同一座標**，距離 **`0.000e+00 m`**。
3. 原始 `data/V6.dxf` **BLOCK 圖層**（舊式 `POLYLINE`）距該頂點最近之點 ＝ **同一座標**，
   距離 **`0.000e+00 m`**（entity handle **178**）。

⇒ **BLOCK 圖層之該頂點本身即位於 FRONT_LINE 外側 `+0.014826m`**；
配地引擎只是把它原樣帶下來。**修圖或改判準係 KL 之裁量，非碼面可自決。**

> ✅ **KL 已裁：修圖**（2026-08-02·段六）。新檔 `data/V6_1.dxf`
> ——**移動 `FRONT_LINE`／`SIDE_LINE` 端點使其通過該街廓頂點**，**非**移動街廓頂點
> （**街廓多邊形係地籍事實，街線係規劃線；令線就地籍**）。
> **本節所載之 `+0.014826m` 係舊圖 `V6.dxf` 之值；`V6_1.dxf` 已修**
> （平行對照實測 → **`+0.000006m`**，且該角之截角接合由**點接合**轉為**邊接合**）。
> 🔴 **新圖尚未進生產路徑**——現行碼與全部 baseline 仍讀 `V6.dxf`；
> **換圖須與換快照併入波末批同一次重烤**（GB-16·驗收見
> `verify/baselines/PROVENANCE.md` 附錄 A）。**數字不刪，作廢傳播照既有體例。**

### 🔬 K-9-6-f 包含關係 `街角規定範圍 ⊆ 街角第 1 宗` 之實測（段五）

KL 之推論為「兩者皆為『街廓 ∩ 平行 ALLOC 半平面』⇒ `G ≥ 範圍面積` 應蘊含包含關係」。
**實測：12 格中 9 格成立、3 格不成立**（閾＝**面積法定粒度 `0.01㎡`**，
以區辨真溢出與 shapely 差集之浮點噪訊 `~1e-10㎡`）：

| 情境 | 街廓/側 | 宗 | 範圍−宗 溢出 |
|---|---|---|---|
| 0m | R1/left | `628-37(1)` | **5.325538㎡** |
| 3.5m | R1/left | `628-36(1)` | **5.325538㎡** |
| 3.5m | R1/right | `628(5)` | **5.067912㎡** |

⇒ **包含關係非恆真** ⇒ 「`G ≥ 範圍面積` 蘊含寬度合格」**不可由包含關係逕推**。
（此為 **U-K9-3** 之實測底料；**段五不代裁**。）

### 🗄️ K-9 §五、K-7 原文（**已於 2026-08-01 撤銷·存查·勿刪**）

> ~~**K-7 街角面積門檻之深度豁免（KL 裁 2026-07-31）**：「G ≥ 街角規定面積」者，
> 其寬度與深度**逕認合格**，不適用寬深雙檢。此為**裁定豁免、非幾何蘊含**。~~
>
> **【作廢】** 2026-08-01 KL 五則裁定 ⇒ 整條撤銷。
> 寬度部分由 **K-9-5**（實量·門檻＝退縮寬＋畸零地最小寬）取代；
> 深度部分由 **K-9-2／K-9-3**（街廓層檢查·宗地層不量）取代。
> 原文逐字保留於本段，供追溯 `CLAUDE.md` 與碼註解之殘留痕跡。

---

## 🔒 K-8 §二 補記：R1 全精度之**等價類內差異**（施工單 §2.5·勿當成兩個真值）

R1 之 N-19′ 全精度在兩份交接文分別記為 `33.1460885310` 與 `33.1460880149`（差 **5.16e-7**）
——**係 BASELINE `#0`/`#2` 等價類內之差異、非錯誤**（K-8 §一 判二者等價、禁寫死單一實體）。
二者之 2dp 皆為 **`33.15`**，**法律效果零**。

⇒ **下一手勿於二者間「挑一個當真值」**。凡需比對深度者，閘寬須 ≥ 該等價類散布
（R4 實測 **5.80e-06 > 1e-6**）；現行 A-4 同源閘取 **0.005**（2dp 記載鏈之無差別半寬·
辦法 §3），**有機制依據、非實測殘差**。

---

# 裁定：抵費地池之面積定式（KL 裁 2026-08-05·canonical）

#### 🔒 抵費地池之面積採「量」而非「算」（KL 裁 2026-08-05·canonical）

池（配餘地／調配池／抵費地池）之面積 ＝ **街廓多邊形扣除各宗配地多邊形後之幾何剩餘**，
實測其面積，取兩位小數（第三位四捨五入）。
⛔ **非**「街廓 DXF 面積 − ΣG」之帳算餘數。

⇒ 現行實作即為本裁定（`grep -n "def _pool_strips_for_block" app.py`
   → `grep -n "_pool_total_blk = float(sum" app.py`
   → `grep -n "'池總=幾何剩餘(㎡)'" app.py`），**本條為追認·零碼面變更**。

🔴 **必須同時載明之推論**（KL 2026-08-05 說明·claude.ai 由構造複驗）：
  各宗之 `G` 係自**登記面積 `a`**（tab1 匯入·與該筆重劃前土地之 DXF 面積多不相符）
  經 `G`／`S` 反覆迭代至**小數點後兩位相符**（第三位四捨五入）方止；
  而配地多邊形之面積受街廓邊界拘束，一般**無法恰等於**該兩位小數之值。
  又池係幾何量、**不吸收** `ΣG` 之進位，故有恆等式：

      ΣG ＋ Σ池 − Σ街廓DXF面積  ＝  Σ(G − 配地幾何)   ≢ 0

  ⇒ **守恆殘差恆非零**，且其身分即 `F.3／F.2／F.4` 所印之 `|Σ(G−幾何)|`。
  ⛔ **任何要求該殘差「回到精確零」之驗收條款皆屬誤設**，不得再訂。

##### 落地確認（K-6-A2 段四收官·`3c6a184` 當場 grep·⛔ 零碼面變更）

**兩條路徑皆為「量」**（正面列舉受檢檔）：

| 路徑 | 錨 |
|---|---|
| app（live） | `grep -n "_pool_total_blk = float(sum" app.py` ⇒ 1 命中；`grep -n "'池總=幾何剩餘(㎡)'" app.py` ⇒ 1 命中 |
| harness（`run_all` 實走） | `grep -n "_pool_total_blk = float(sum" verify/stepg_pipeline.py` ⇒ 1 命中；殘差式 `grep -n "_resid_wd2 = round" verify/stepg_pipeline.py` |

**殘差之判準皆為「在容差內」、無任何「精確零」之斷言**
（`grep -n "_verdict_wd2 = " verify/stepg_pipeline.py` ⇒ `abs(_resid_wd2) <= _tol_blk`；
 `grep -rn --exclude-dir=.claude "resid == 0" --include="*.py" .` ⇒ **零命中**）。

⚠️ **與 `CLAUDE.md` 之守恆式字面之關係（上呈·本批不改 `CLAUDE.md`）**

`CLAUDE.md` 之「守恆式（最高鐵律）」載
`ΣG(街廓內所有分配地) + 調配池(Ri) = 街廓 DXF 面積(Ri)  恆成立`
（`grep -n "恆成立" CLAUDE.md`）。該式**僅在「池 ＝ 帳算餘數」之定義下恆真**；
依本裁定池採「量」⇒ 其**字面之「恆成立」不再精確成立**，正確表述為
「**在容差內成立**，殘差 ＝ `Σ(G − 配地幾何)`」。

🔒 **權威序已自動解決**（`docs/rulings/` → `CLAUDE.md`）⇒ **非正典衝突、不停機**；
惟 `CLAUDE.md` 之字面是否隨之修訂，**屬 KL 之文件決定**，本批**不自行改 `CLAUDE.md`**。

---

## 🗄️ K-8 波末遷移項登記（**本批只登記不改·改了就是假紅**）

### 🆕 K-6-A2 段三(c)-3 追加之波末項（claude.ai 2026-08-04·**只登記不改**）

| # | 波末須確認之事 | 現況 | 為何現在不動 |
|---|---|---|---|
| **甲**（🔧 **2026-08-05 改寫**） | **`F.3／F.2／F.4` 守恆殘差之「上界模型」** ——建立 `Σ(G − 配地幾何)` 之量級估計，確認現行容差 `_acct_geom_tol_block`（`grep -n "def _acct_geom_tol_block" app.py`）之導出**是否涵蓋該上界** | 現值：`F.3 0m` `0.03`／`F.3 3.5m` `0.01`（容差 `0.585`·`verify/out/K6A2_seg4c_runall.log`） | 🔴 **原題「是否回到**精確零**」已判為誤設並作廢**（KL 裁 2026-08-05·池採「量」之必然推論·見本檔 `grep -n "^#### 🔒 抵費地池之面積採" 本檔`）——`ΣG ＋ Σ池 − Σ街廓 ＝ Σ(G−配地幾何) ≢ 0`，殘差**恆非零**，⛔ **不得**以「殘差為零」作為驗收線。<br>🗄️ **原文存查**：「`F.3 結構永久閘3.5m` 之守恆殘差是否回到「精確零」／本批由 `0.0` → `0.01`；同時 `F.2 跨街廓守恆3.5m` 由 `0.03` → `0.0`／……惟『精確零』比『在容差內』是更強之性質，而 F.3 離開了精確零 ⇒ 重烤後須確認 F.3 是否回到精確零；若否，須有解釋」 |
| **乙** | **「容差 vs 欄位粒度」之全倉對照** | **GB-27** 已坐實 `GSA_EXPECT` 一族：容差 `>0.01` 恰等於被比欄 `round(...,2)` 之粒度 ⇒ **有效解析度 `0.02㎡`**、對「一個法定粒度」為盲 | 同一形狀**可能存在於其他錨族**（`k*` 六塊經驗錨／v3 財務錨／W-D.4 清單）——**該三族本波未掃過**。⚠️ `GSA_EXPECT` 六格**現確知乾淨**（量測顯示僅 G007 卡在 1 個粒度、已重錨）；**其餘錨族無此保證** ⇒ 波末做一次全倉「容差 vs 欄位粒度」對照 |


三者皆須與快照 `verify/case_params_UC9898.json` ＋ v3 baseline **同批**更新
（KL 裁 U-K8-1 ＝ 乙：快照留到 K-8 全案完成後一次換）。行號為 `262e5a0` 當場 grep 值，
**引用時請重 grep**（🔒 行號衛生）。

| 落點（符號名／查法） | 現值 | N-19′ 後應為 |
|---|---|---|
| `grep -n "114\.07" verify/wf_f4.py`（docstring `:20`／CSV 欄名 `:551`／`:1257`） | `114.07` | **`115.85`** |
| `grep -n "_ok_der = " verify/run_verification.py`（`:717`）＋**其下一行之測項名目字串** | `114.07` / `57.04` | **`115.85` / `57.93`** |
| `grep -n "MINA_QU_EXPECT\|HALF_EXPECT" verify/wd4_tier_list.py` | `114.07` / `57.04` | **`115.85` / `57.93`** |
| `grep -n "WIDTH_VERDICT_CORNER_K4 = " app.py`（`:6343` @ `1e632a1`） | `'合格（K-4 面積門檻蘊含）'` | 改措辭（**K-7 已撤銷 ⇒ 改依 K-9-5**）＋常數名 `_K4` 正名；🔴 **本列原載之「該字串綁 E 系列 baseline」係誤載·見下方更正** |
| `grep -n "街角·K-4 面積門檻蘊含" verify/probes/probe_ruling_N_e1_touch.py`（`:194` @ `1e632a1`） | `'街角·K-4 面積門檻蘊含（N-14 不適用）'`（**硬編字面·非引用 app 常數**） | 改依 K-9-5 之措辭；⛔ **此字串才是綁 baseline 者**——落 `判定` 欄，`E系列實測快照_退縮0m.csv` **8 列**／`退縮3.5m.csv` **5 列**，改字串＝重烤假紅 |

> 🔴 **更正（段四·2026-08-01·實測）**：上表原將「綁 8/5 列 baseline」歸因於 `app.WIDTH_VERDICT_CORNER_K4`，
> **與倉態不符**。實測：
> - `grep -rn "合格（K-4 面積門檻蘊含）" verify/baselines/` ⇒ **0 命中**；
>   `grep -rln "寬度判定" verify/baselines/` ⇒ **0 命中**（**無任何 baseline 有 `寬度判定` 欄**）
>   ⇒ **app 常數之字串值不綁任何 baseline**。
> - 8/5 列實綁者為 `probe_ruling_N_e1_touch.py` 之**另一個硬編字面**
>   （`grep -c "街角·K-4 面積門檻蘊含（N-14 不適用）" "verify/baselines/E系列實測快照_退縮0m.csv"` ⇒ **8**；
>   `…_退縮3.5m.csv` ⇒ **5**）。該探針**比較**時讀 `ns["WIDTH_VERDICT_CORNER_K4"]`
>   （`grep -n 'ns\["WIDTH_VERDICT_CORNER_K4"\]' verify/probes/probe_ruling_N_e1_touch.py`），
>   但**寫入 CSV 時用自己的字面**（`:194`）⇒ 二者**同名異體、改一個不動另一個**。
>
> ⇒ **波末重烤批若只改 app 常數，baseline 不會紅、而探針輸出與 app 語意會靜默脫鉤**；
> 反之只改探針字面則必翻 13 列。**兩者須同批改。**
> （本批依紅線**兩者皆不動**，僅更正登記。）

### 🔒 K-8 §三 開工前之既有事實（已實測·免下一手重複懷疑）

1. **門檻與跨占同源**：`_corner_poly_p1_B4 = _corner_range_left`
   （`grep -n "_corner_poly_p1_B4 = " app.py`）而 `_corner_range_left = _build_corner_range_v2(...)`
   ⇒ **改一處即兩處同步**，不會產生兩套互相矛盾的街角形狀。
2. **兩套側界資料層本來就分得開**：SIDE_LINE 實體與街廓多邊形邊界之重疊率 **0.8884–0.9225**、
   缺口 **3.40–3.71m**（≒截角股長）⇒ **SIDE_LINE ＝ 截角前側界；街廓邊界 ＝ 截角後側界**
   （`verify/probes/probe_ruling_K8_sideline_pairing.py`·已掛 run_all）。
3. **Patch E-1.8 已刪**（K-8 前置 E）：其 `_legal_depth_B4` 鏈為死碼、零讀取，
   刪除不影響任何結果（失敗考古 #37）。**U-K8-5 上半已撤銷。**
4. **Patch E-1 §6 未生效**（K-8 前置 F）：`_min_area_by_block` 恆走 `else`；
   唯一活消費端係 K-6 §五待置換之合併述詞 ⇒ **由 K-6 §五 一併處理，段三不動**。

---

### 🔒 K-9-5-9 KL 裁示八條（KL 裁 2026-08-10·D-2b-25·canonical·**逐字轉錄**）

> ⛔ **本節為逐字轉錄**：⛔ 不得改寫、不得概括、不得補充未說之語。
> 標示【裁】者為裁定；【知會·未見異議】者為 KL 知會而 CC 未提異議之事項。

**【裁】1.** 街廓 R1 之「選槽依據與實際落地不符」（結構閘 理論＝實跑）列為**另案**。
⛔ **未裁事項**：本波是否因它停機、D-3 重烤時點——⛔ 二者**不得由「另案」推定**，須另行請裁。

> 🔒 **【未裁項結清·`D-2b-28`】（KL 裁 2026-08-10·逐字加註·⛔ 上二行原句不刪）**
>
> **(一) 波不因結構閘停機**——結構閘既為另案，`D-2b` 不因它停止。
> ⚠️ 此為**波之停機**；**程式之停機**（跑起來出不出成果）另由**【裁】2／3** 決定，
> ⛔ 二者不得混為一談。
>
> **(二) `D-3` 重烤等 `R1` 之卡點修好之後才做。**
>
> ⇒ 本節【裁】1 所保留之兩未裁項自此**全部結清**，⛔ 不再列為待裁。

**【裁】2.** 業主宗互疊超出捨入容許量者，係**程式錯誤**。一律停機、**不得產出分配成果**、
⛔ **不得降級為警告**。

**【裁】3.** 檢查**一律跑完全部街廓**、把問題一次列齊；但只要有任何一處未過，
**分配成果一概不產出**。

**【裁】4.** `N0-20 §二.2` 之「自末端 ALLOCLINE 之 mp 往街廓內量 W」**失效**。
mp／W 之量法**僅**用於有 SIDELINE 側計算 W 以對 Rw 表
（側街 18m 內各宗地均須負擔臨側街地之精神）。
末端塊之寬度（≥ 畸零地寬）**仍依畸零地使用規則所定之寬度量法**。

**【裁】5.** 第 2 宗與第 3 宗共用之境界線**平行 ALLOCLINE 方向**。
第 2 宗最小規定範圍 ＝ 過 `Ps` 之 ∥ALLOCLINE 直線向街廓內平移畸零地寬所夾之範圍；
此情形（第 1 種）之**最小寬度落在 FRONTLINE 交點**。

**【裁】6.** 另一情形（第 2 種）之**最小寬度落在 BASELINE 端**。FRONTLINE 與 BASELINE 不平行、
但二者皆為直線 ⇒ 距離沿深度呈線性遞增／遞減 ⇒ 須於最小深度帶下解出畸零地寬之位置。

**【裁】7.** 第 2 宗最小規定範圍**另開一波**；D-2b 不動。

**【知會·未見異議】8.** 第 2 宗最小規定範圍用**街廓層**最小寬度帶
（前緣線沿 BASELINE 法向平移 `round(D_avg,2)` 所夾之帶）；
事後寬度驗收仍用**該宗自己**之帶。兩把尺不同係刻意（打斷寬度／深度互相依賴）。

---

🛑 **圖證待補（D-2b-25 §2·CC 2026-08-10）**：第 5、6 條所指之**三張示意圖**
（第 1 種分配線方向兩版、第 2 種分配線方向）**本批未入倉**——
成因：**該批訊息未附任何圖檔**，依 `CLAUDE.md`「🔒 施工單必須自足」之判準
（⛔ 禁編造、⛔ 禁以倉內近似物代替、照實回報並請補·失敗考古節 52）⇒ 照實記載、未辦。
⇒ **補入前，第 5、6 條之幾何細節以本節文字為準**，⛔ 不得以任何倉內近似圖代替。
⚠️ **`P0`／`Ps`／`S1` 之定義本檔<u>已載</u>**（見 `K-9-5-4` 段：
`S1 = max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)`／`P0` ＝ FRONTLINE 與 SIDELINE 之未截角理論交點／
`Ps` ＝ FRONTLINE 與道路截角線段之交點）⇒ 補送時**只需補未載之差額**，⛔ 已載者不得重寫。

> 🔒 **【圖證補入·`D-2b-30`】（KL 補送 2026-08-10·⛔ 上段原文不刪）**
>
> 三張示意圖已入倉於 `docs/specs/figures/`，與本段所載「第 1 種兩版、第 2 種」逐一對應
> （md5 對帳見 `D-2b-30` 報告）。
>
> ⚠️ **本結清僅解除「圖未入倉」之缺件**；第 5、6 條之幾何細節**尚未重測**
> ⇒ ⛔ 仍不得據以作任何第 1 種／第 2 種之對應判定。
>
> 🔎 **圖／正典差額（現查·CC 已自行開圖逐字核對）**：
> **(a)** 圖 `_1` 之 `S1 >= max(…)` 與 `K-9-5-4` 之 `S1 = max(…)` 之表面歧異，
> **已由 `K-9-5-11` 裁定解消**（⛔ 見該節，非本段判定）。
> **(b)** 圖 `_1` 另載「`S1` 定義：街角第 1 宗土地的臨正街寬度（未截角）」——正典未載之差額，照實登記。
> ⚠️ 圖 `_1` 對 `P0` 之描述為「FRONTLINE **左側起點**與 SIDELINE 交點」，係該圖所繪**左街角實例**之描述；
> `K-9-5-4` 逐字為「FRONTLINE 與 SIDELINE 之**未截角理論交點**」（側別中立）。
> ⛔ **不得因該圖把 `P0` 改寫為側別相依之定義**（⛔ 禁硬編側別）。
>
> 🔎 **圖面原字（⛔ 逐字·未正規化）**，供日後比對：
> `P0=FRONTLINE左側起點與SIDELINE交點、` ／ `Ps=FRONTLINE與道路截角線段的交點` ／
> `S1>=max(線段P0-Ps),(退縮+畸零地寬))` ／ `S1定義：街角第1宗土地的臨正街寬度(未截角)`
> ⚠️ 上開 (b) 之引文係按本倉排版慣例補空格與全形括號；**字義相同、字形有別**，
> 以本列之原字為準。⚠️ 圖作「**畸零地寬**」而正典作「**畸零地最小寬**」，亦屬字形差額。

**實作歸屬**：第 5／6／8 條之實作**另開一波**（【裁】7）——見
`docs/W-G.9_第2宗最小規定範圍_範圍界定.md`。⛔ **D-2b 不動。**

---

#### 🔒 原話對照（KL 2026-08-10·**D-2b-26 補入**·⛔ 上方八條一字未動）

> 🔒 **地位**：上方八條係 **claude.ai 之形式化文字**；本小節為 **KL 原話**。
> **二者衝突時，以<u>原話</u>為準。**

**(a)【裁】2 之限定語「超出捨入容許量者」係 claude.ai 依閘之設計所加**——
**KL 原話**（逐字）：

> 「有宗地互疊就不產出成果⋯這個不就是程式錯誤不能用了，這配地程式不應該有這樣的情況」

⚠️ **差額**：原話為「**有**宗地互疊」（⛔ 未設量之門檻）；
形式化文字加了「**超出捨入容許量者**」之限定。
⇒ **以原話為準時，射程較形式化文字為寬**——⛔ 實作時不得逕以現行閘之上界
（`(宗數−1)×0.005×深度`）當作「容許量」之正當性依據；該上界之地位須另行請裁。

**(b)【裁】4 之 KL 原話另含理由一句**（八條轉錄時**漏列**·於此補回·逐字）：

> 「現在末端塊土地因為深度呈現線性遞增或遞減」

⇒ 該句為【裁】4 之**理由**，與【裁】6 之「FRONTLINE 與 BASELINE 不平行、但二者皆為直線
⇒ 距離沿深度呈線性遞增／遞減」**同一機制**。⛔ 前次轉錄漏列，非 KL 未說。

---

### 🔒 K-9-5-10 KL 裁示二條（KL 裁 2026-08-10·D-2b-29·canonical·逐字轉錄）

> ⛔ **本節為逐字轉錄**：⛔ 不得改寫、不得概括、不得補充未說之語。
> **現查依據另立於次節 `K-9-5-10-a`**，⛔ 不得混入本區。

**【裁】1.** `K-9-5-9`【裁】1 之加註 (二) 所稱「`R1` 之卡點」，指**該【裁】1 所列之另案本身**
（街廓 R1「選槽依據與實際落地不符」·結構閘 理論＝實跑）。⇒ **D-3 重烤等該另案修好之後才做。**
⛔ 不得解為 `GB-54`（`app` 警告 vs `stepg` raise 之對齊）。

**【裁】2.** 現用頂層基準中已過期之 **7 份**（街角地指配 ×2、`W-D.1.2` 診斷 ×2、
`W-D.1.3-a` 診斷 ×2、`W-D.1.3-d` 抵費地驗收 ×1）採**丙案：由 KL 以 UI 重跑**。
⛔ 不採甲（明認過期仍不重烤）、⛔ 不採乙（由 harness 重烤）。
**時點與【裁】1 同：另案全部修好之後才重跑。**

#### 🔎 K-9-5-10-a 現查依據（`D-2b-29` §1-2·⛔ 非裁示·⛔ 不得混入上區逐字轉錄）

四點皆由 CC **重跑指令複驗**，**全部相符**（任一不符即應停機，未發生）：

| # | 命題 | 現查結果 |
|---|---|---|
| 1 | `R1` 之中止因為**結構閘**、⛔ 非抵費地計算失敗 | ✅ `grep -c "結構閘 理論＝實跑 破" app.py verify/stepg_pipeline.py` ⇒ `app.py` **0**／`stepg` **2**；`grep -c "抵費地計算失敗" verify/out/D2b24_runall.log` ⇒ **0**；該 log 之中止因**全部**為結構閘（六處） |
| 2 | 該 log 即**最新碼態**所產 | ✅ `git log --format=%h 78079fa..HEAD -- app.py verify/stepg_pipeline.py` ⇒ **0 行** |
| 3 | 那 7 份之產出鏈**遞移可達**結構閘之「實跑」側算子 | ✅ **跳數 3**：`_corner_block_true_G → _corner_first_lot_G → _solve_G_one → solve_G_binary`／`iterate_G_S`（探針 `verify/probes/probe_D2b29_reach_closure.py`·log `verify/out/probe_D2b29_reach_closure.log`） |
| 4 | 「卡點」一詞於正典**無先前定義** | ✅ `grep -rn "卡點" docs/rulings/` ⇒ **僅 1 命中**＝`K-9-5-9` 加註 (二) 本身 ⇒ 本【裁】1 即其**首次**定義 |

🔒 **第 3 點之意義（此即【裁】2「時點與【裁】1 同」之碼面依據）**：
該 7 份既**遞移可達**結構閘之實跑側算子 ⇒ **結構閘未修之前，其重跑結果不具穩定性**
⇒ 縱採丙案（KL 以 UI 重跑），**亦須待另案修畢**，否則重跑所得將再度過期。

⚠️ **第 3 點之射程聲明**（⛔ 否定性宣稱之戒·見失敗考古本波新節）：
種子＝`verify/selection_pipeline.py` 之**全部** `ns["…"]` 取名（AST 取·14 個）；
圖＝`app.py` **全部** FunctionDef，邊涵蓋 `func.id`／`func.attr`／`ns["…"](…)` 三形式；
閉包 BFS 至不動點（觸及 154 名·差集自證 0）。
🔴 **一層對照為「否」**——若只做一層即下「零交集」之結論，**將與遞移閉包之結果相反**。

---

### 🔒 K-9-5-11 KL 裁示：`S1` 之**三分口徑**（KL 裁 2026-08-10·D-2b-30·canonical·**逐字轉錄**）

> ⛔ **本節為逐字轉錄**：⛔ 不得改寫、不得概括、不得補充未說之語。

**KL 原話（逐字）**：
「街角第1宗土地寬度=等於 max（P0 到 Ps 的線段長，退縮＋畸零地最小寬），這是指街角規定範圍的寬度
但若經街角地評選機制選出的WINNER，一定會>=max（P0 到 Ps 的線段長，退縮＋畸零地最小寬），
或沒WINNER強制抵費地時，該抵費地寬度才=max（P0 到 Ps 的線段長，退縮＋畸零地最小寬）」

⇒ **`=` 與 `>=` 並無衝突，係<u>三個不同對象</u>之三種口徑**：

| 對象 | 寬度 |
|---|---|
| **街角規定範圍** | **＝** `max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)` |
| **評選機制選出之 WINNER 所配得之宗地** | **≥** 同式 |
| **無 WINNER ⇒ 強制抵費地** | **＝** 同式 |

🔒 `K-9-5-4` 之 `S1 = max(…)` 屬**第一列**（規定範圍），⛔ 不因本節而修改一字。
🔒 圖 `_1` 之 `S1 >= max(…)` 屬**第二列**（WINNER 實配），非與正典衝突。


---

### 🔒 K-9-5-12 首宗起點一律為 0：撤回「`W_0` ＝ MP→corner」（KL 裁 2026-08-12）

> 🔴 **本節之三段來自<u>不同的人</u>，⛔ 不得合併閱讀。**
> 本輪失敗之根因即為分層不明——一句**助手側補充**被當成 KL 裁示用了十幾輪、並據以翻案三次。

#### （一）所問　**⚠️ claude.ai 擬·⛔ 非 KL 之語**

> 那句「從側面道路中點量到街角點」，是不是應該整條拿掉，一律改成從側面道路線的中點起算（也就是 0）？

#### （二）所答　**✅ KL 逐字**

> **是**

#### （三）裁示句　**⚠️ 經對話側送達·⛔ 本批無法自倉內覆核其為 KL 逐字抑或施工單方之複述**

> 「從側面道路中點量到街角點」，整條拿掉，側街負擔計算的 W 一律改成從側面道路線的中點起算（也就是 0）

🔒 **標記之由（考古 61／75）**：對話側非倉內事實。上段之**送達途徑**已就地標明，
⛔ 不得逕標為「KL 逐字」，亦⛔ 不得與（二）合併成一段敘述。

#### （四）併同之既有裁示　**KL 2026-08-12**

無 WINNER 而街角為**強制抵費地**時，**第 2 宗**之起算為**該抵費地之遠側境界線**；
**抵費地本身不計 G、不負擔**。

#### （五）效力

1. **首宗起點一律為 `0`**（即自 **MP** 起量）。
2. `K-9-5-6` 之 `- 🔒 **首宗起點**：`W_0` ＝ MP→corner …` 一行之**前半自本批起失效**
   （該行原句**一字未刪**，已就地加註並指向本節）。
3. ⚠️ 該行**後半**（`forced` 時為其 buffer 起算點）**⛔ 未撤，列為未決**——見該處加註。

#### （六）性質　🔴 **移除一句從未經裁示之敘述，⛔ 非推翻 KL 之裁定**

`W-G.9-15` 甲組以**機械規則**（引文行＝`^>`／區塊＝極大連續段／引言＝區塊起點往上最近之
非空白非引文行）盤點本檔含 `🔒` 之 **67** 行：**KL 逐字 2 條／助手側補充 57 條／無法判定 8 條**。
其中該「首宗起點」行判為**助手側補充**——**不在任何引文區塊內**。

⇒ 故本節**不是** KL 撤回自己的裁示，而是**移除一句從未經他裁示的話**。
（若當初判為「在 KL 逐字區塊內」，本批依施工單 `D-1` **須先停機上呈**，⛔ 不得逕自入倉。）

#### （七）⛔ 本節<u>未</u>做之事

- ⛔ **未做選位側之校回**——校回會**改變分配面積**，須**先估後改**（下一批只估、再下一批才動碼）。
- ⛔ **未刪本檔任何一字**；⛔ 未改任何既有編號。
- ⛔ 未處置後半句之 `forced` 情形（列為未決）。
> 🔗 **指路加註（`K-9-5-13`·2026-08-12·⛔ 上文原句一字未刪）**
>
> 上開「後半句之 `forced` 情形」之未決，**已由 `K-9-5-13`（KL 裁定二）失效**
> ——見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。


---

### 🔒 K-9-5-13 街角第 1 宗遠側界改為 SIDELINE 平移 `S1`；`§三`／`§四` 失效（KL 裁 2026-08-12）

> 🔴 **本節之各段來自<u>不同的人</u>，⛔ 不得合併閱讀。**
> ⚠️ **送達途徑**：下列 KL 之語**經對話側送達**；⛔ 本則無法自倉內覆核其為 KL 之逐字
> 抑或施工單方之複述（**考古 61**）。

#### （一）所問　**⚠️ claude.ai 擬·⛔ 非 KL 之語**

> 1. 平移的距離是多少？
> 2. 改成平移之後，「最小寬度帶」還需要嗎？
> 3. 「最小寬度帶」與「平行分配線定位」那兩節的作法，是您以前裁示的嗎？

#### （二）所答　**✅ KL 逐字**

> **裁定二**：「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
> **裁定三**：「落位那一邊當然要跟著以起改用真正的地界(第1宗土地是用SIDELINE平移)」
> **裁定四**：「街叫第1宗土地的遠測境界線一定是不同條，因為我以前確實是裁示用"平行ALLOCLINE"方向，但是現在改成街角地的遠糙境界線為平行SIDELINE平移」
> **裁定五**：「第1題：平移的距離，就是 `S1`」
> **裁定六**：「第2題：改成平移之後，「最小寬度帶」一併不需要了」
> **出處之答**：「**記不清**」

---

#### （三）**新裁之效力**　🔒 **已定案**（⛔ 本段與（四）分開讀）
> 🔧 **性質更正（`W-G.9-18`·2026-08-12·⛔ 上行原句一字未刪）**
>
> 🔴 **裁定四〜六 ⛔ 不得記為「新裁」或「變更」——其性質為<u>重申</u>。**
> **現查**：`K-9-5-4` 之定義式**已在本檔內**（`grep -c "^### 🔒 K-9-5-4"` ⇒ 1），
> 且碼面已於 **`f8b42fc`（2026-08-08）** 與 **`79bf2d7`（2026-08-09）** 兩次 commit 切換
> （字樣錨：`git log -S"SIDELINE 平行移動" -- app.py`）。
> ⇒ 正確之敘述為：**重申 `K-9-5-4`（KL 裁 2026-08-08），並補正 `§四` 之過期記載。**
>
> 🔒 **（四）「舊節出處無法確認」<u>不受本更正影響</u>**——`§三`／`§四` 當年由誰寫入，仍未定。
> ⛔ 兩件事分段寫。

1. **街角第 1 宗之遠側境界線 ＝ SIDELINE 平移 `S1`**（裁定四、五）。
2. **起算一律用真正的地界**；「面積換算出來的替身線」**在側街負擔上不再有角色**（裁定二）。
3. **落位側亦改用真正的地界**（裁定三）。
4. **`§三 最小寬度帶` 一併失效**（裁定六·範圍條件見該節加註）。
5. **`§四 ALLOC_LINE 定位` 之滑動求解隨之失效**。

#### （四）**舊節之出處**　🔴 **無法確認**（⛔ 本段與（三）分開讀）

> **出處無法確認。** 倉內查無該二節之 KL 逐字記載（`§三` 引文行 0／`§四` 引文行 0，
> 判別力自檢：全檔引文行 > 0）；KL 於 2026-08-12 答「記不清」。
> ⇒ **本則⛔ 不記為「KL 變更自己之裁示」，亦⛔ 不記為「移除從未經裁示之作法」。**

🔒 **⛔ 上開不確定，僅及於<u>舊節之出處</u>；（三）之效力<u>不受影響</u>。**
⛔ 合併寫會使後人誤以為新裁亦不確定。
**證據**：`verify/out/probe_WG917_B1.log`（機械規則·母體釘於 commit）。

#### （五）`S1` 之口徑　⚠️ **一項未決**

`K-9-5-11`（KL 逐字轉錄）定 `S1` 之**三分口徑**（＝／>=／強制抵費地）。
裁定五定「平移的距離**就是** `S1`」。
🔴 **未決**：`K-9-5-11` 之 `S1` 係「`P0` 到 `Ps` 之線段長」，而平移量須沿何方向量、
與該線段長是否同一個數，**倉內未載** ⇒ **具名待裁**，⛔ 本則不自行認定。
> 🔧 **射程更正 ＋ 待裁項撤回（`W-G.9-18`·2026-08-12·⛔ 上行原句一字未刪）**
>
> 🔴 **上行之「倉內未載」<u>射程寫錯了</u>**——該次**只查了本裁定文件、未查碼面**
> （**考古 60** 之形狀：否定性宣稱之射程未寫明母體）。
>
> **現查 `app.py` 已載（逐字·字樣錨 `S1_par`）**：
> > `S1_par` ＝ max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)（**沿 FRONTLINE 量·路平行**）
> > 🔒 `S1_par` 為「與道路境界線**平行**」之距離（**畸零地使用規則第四條第三款**）
> > ⛔ **非垂距**；`W`（供 Rw 查表）為垂距，**二者同名不同量·禁互代**
>
> ⇒ 正確之敘述為：**裁定文件未載；碼面已載，且引畸零地使用規則第四條第三款為據。**
> ⇒ 🔒 **上行之「具名待裁」<u>撤回</u>**——⛔ **不需 KL 裁**。

#### （六）⛔ 本則<u>未</u>做之事

- ⛔ **未估任何面積**（含 `§五` 範圍多邊形之面積改變量級）。
  > 🔧 **結案加註（`W-G.9-18`·⛔ 上行原句一字未刪）**：該未估之**問題本身已消失**
  > ——碼面早已是新構造（`f8b42fc`／`79bf2d7`）⇒ **面積不因裁定四而變**。
- ⛔ **未改落位**、⛔ 未改 `_build_corner_range_v3`、⛔ 未實作新構造。
- ⛔ **未刪本檔任何一字**；⛔ 未改任何既有編號。
- ⛔ **未追那 57 條未標記條目之出處**（屬後批）。

---

### 🔒 K-9-9 宗地形成之起算垂線約束（KL 裁 2026-08-16·canonical·逐字）

**前置定義**（⛔ 全部由量得之幾何取得·⛔ 禁以街廓名／側別／宗序號硬編）：
- 沿 FRONTLINE 之**推進方向** ＝ 自該側之起點（街角端）往街廓中間；以下之
  「之前／之後」「max」皆依此方向定序。
- `P_prev` ＝ **前一宗**遠側境界線 × FRONTLINE
- `B_prev` ＝ **前一宗**遠側境界線 × BASELINE
- `B⊥` ＝ 自 `P_prev` 沿 **FRONTLINE 法向**至 BASELINE 之垂足
- `P⊥` ＝ 自 `B_prev` 沿**同一法向**至 FRONTLINE 之垂足
- **起算垂線** ＝ 自 `max(P_prev, P⊥)` 出發、沿 FRONTLINE 法向之線段；
  其另一端為對應之垂足。

**一、方向**：本宗遠側境界線 **∥ ALLOCLINE**。

**二、位置下限**：本宗遠側境界線之 **FRONTLINE 交點**與 **BASELINE 交點**，
**兩者皆須在起算垂線之對應端點之後**。分兩情形（由前一宗遠側界之傾斜方向決定）：

| 情形 | 判準 | 起算垂線 | 下限落在 |
|---|---|---|---|
| **甲** | `P⊥` 在 `P_prev` **之前** | `P_prev` → `B⊥` | **BASELINE** 上之 `B⊥` |
| **乙** | `P⊥` 在 `P_prev` **之後** | `P⊥` → `B_prev` | **FRONTLINE** 上之 `P⊥` |

🔒 舊敘之「條件2（兩交點皆在前一宗之後）」**由本則吸收**，⛔ 不另立。
🔒 甲乙之判別**由資料決定**，⛔ 禁硬編。第 2 宗看 SIDELINE 之傾斜、
  第 3 宗以後看 ALLOCLINE 之傾斜 ⇒ **同一街廓同一側之第 3 宗以後必然同屬一種**。

**三、本則之性質**：係**宗地形成之約束**，🔴 **⛔ 非深度閘**。
深度仍依 **`K-9-2`／`K-9-3`** 走**街廓層**；**宗地層一律不量深度**（含街角第 1 宗）。
⇒ 本則與 `K-9-2`／`K-9-3` **互不取代、互不衝突**。

> ⚠️ **射程更正（`W-G.9-42` 加註·⛔ 上段原句一字不刪）**：本款之「**宗地層一律不量深度**」
> **過寬**，正確為「**量、但不比附表深度**」——`K-9-3-1`（KL 裁 2026-08-05）；⛔ 不重述，錨
> `grep -n "^### 🔒 K-9-3-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
> 🔒 **本款之主張不因此動搖**：本則係**形成端**約束、⛔ 非深度閘，
> 與**量測端**之「量、但不比附表深度」值域不相交（同 `W-G.9-41` 更正一之末句）。

**四、不合格之處置**：依該宗 G 值算出之遠側界位置若在起算垂線**之前**
⇒ **該宗不配地**，走**合併／調配機制**；重劃前投影順序中**下一位**之土地
**遞補該位次**；騰出之地**入調配池**。
🔴 ⛔ **不得強制配到下限**（⛔ 不得超配）。

**五、街角第 1 宗⛔ 不適用本則**：其遠側境界線**恆 ∥ SIDELINE**；
- **無 WINNER**（強制抵費地）⇒ 位置 ＝ **街角規定範圍之寬度**
- **有 WINNER** ⇒ 依該宗**應分配之 G 值**決定
街角地適用**街角規定面積**，⛔ 不走一般寬深檢。

**六、兩側推進**：左右兩側**各自往街廓中間**推進，本則於各側**鏡像成立**。
依 G 值公式原理，**必然產生配餘地**（＝調配池），⛔ 不會發生「塞不下」。
⚠️ 既有之單向示意圖僅為**單一方向**之說明，⛔ 不得據以認定只推一側。

**七、有效深度之名詞釐清**（⛔ 非新閘·僅釐清）：
「自 FRONTLINE 沿其法向至 BASELINE 之**完整線段**、**整條落在該宗範圍內**」者，
方計為該宗之**有效深度**。若欲計該宗之最小深度，其**母體僅限有效深度**
（穿出宗外之垂線⛔ 不列入母體、⛔ 非「較短之深度」）；若僅一條有效，
則該宗最小深度 ＝ 該條之長。
⚠️ 依 **`K-9-3`**，宗地層**一律不量深度** ⇒ 本款**僅供名詞釐清**，⛔ 不構成判準。

> 🔴🔴 **本款末句已失效（`W-G.9-42` 加註·⛔ 上句原文一字不刪）**
>
> 1. **射程更正**：上句之「一律不量深度」**過寬**，正確為「**量、但不比附表深度**」
>    ——`K-9-3-1`（KL 裁 2026-08-05）；⛔ 不重述，錨
>    `grep -n "^### 🔒 K-9-3-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
> 2. 🔴 **「⛔ 不構成判準」⛔ 已不成立**：`K-9-10 二`（KL 裁 2026-08-16）令
>    **最淺之「有效」垂線**與 BASELINE 之交點決定**帶底**，而「有效」之定義**即本款**
>    ⇒ **本款自該日起為承重款**，⛔ 非僅名詞釐清。錨
>    `grep -n "^### 🔒 K-9-10 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
> 3. ⚠️ **一併具名之未裁項**：本款逐字只寫「**若僅一條有效**，則該宗最小深度 ＝ 該條之長」
>    ——**「零條有效」之情形⛔ 未裁**（`W-G.9-42` 現查：`零條`／`一條也沒有`／`無有效`
>    於本檔、`CLAUDE.md`、`docs/W-C_細部plan.md` **命中皆 0**）。
>    🔒 **量測框（⛔ 不得省）**：上開命中數之母體 ＝ **`b771d22` 之 blob**，
>    ⛔ **非** working tree——本註自身即含該三字樣，於 working tree 量會得 `1/1/1`
>    （已實測·＝「量測器之母體含其自身輸出」之自我污染·`W-G.9-14` 同族）。
>    判別力自檢（同母體）：`K-9-10` 於本檔 **4**、`僅一條有效` **1** ⇒ 該計數非恆 0。
>    ⇒ 遇該情形者一律**停該宗之判定**、出艙碼 **「正典未裁·待 KL」**（停機碼 `S-9`），
>    ⛔ 不得自行以「僅取近側界終止深度」代替、⛔ 不得判為不合格、⛔ 不得跳過。

> 🔴🔴 **上開第 3 點之「零條有效⇒正典未裁」⛔ 撤回**（`W-G.9-43` 加註·⛔ 上文原句一字不刪）
>
> **該情形正典早已裁**——`K-9-6-h ②`（**KL 裁 2026-08-15**）逐字：
> 「**一條都沒有 ⇒ ⛔ 不算臨後側境界線 ⇒ 停機報錯**」（錨
> `grep -n "一條都沒有" docs/rulings/K-6_街角地分配程序與可分配判準.md`）；
> 其**後果**復由 **`K-9-11`**（KL 裁 2026-08-17）變更為**不配地＋遞補**。
> ⇒ 上開第 3 點所稱之「未裁」**自始不成立**，其所附之現查亦然。
>
> 🔴 **該現查為何全 0**：所用同義詞集為 `零條`／`一條也沒有`／`無有效`，
> 而正典逐字作「一條**都**沒有」——**差一個副詞**（「也」vs「都」）。
> ⇒ 三詞於 `b771d22` 命中 `0/0/0`（**屬實**），而 `一條都沒有` 命中 **1**。
> **⛔ 三詞並非三份獨立佐證，係同一次查詢之三份複本。**
> 修法見失敗考古之「同義詞集之單字差」節與自誤登記 **26**。
>
> 🔒 **⛔ 未撤回者具名**：本加註區塊之第 1、2 點（射程更正／「⛔ 不構成判準」已失效）
> **均不受影響**——其依據為 `K-9-3-1` 與 `K-9-10 二`，與本撤回無涉。

**八、參考線之身分（⛔ 禁互代）**：
- 本則之後緣參考線 ＝ **BASELINE ＝ 後側境界線**
  （R2/R3 共用一條、R5/R6 共用一條 ⇒ 全案 4 條·見 `docs/W-C_細部plan.md`）
- **帶底** ＝ **最小深度範圍**，用途為「界定在哪一段深度範圍內取寬度之 min」
  ⇒ 🔴 **⛔ 非本則之參考線**。
- 逐宗計算帶底（`t_hi = min(後緣角點之 t)`·讀頂點座標）
  🔒 **⛔ 不構成 `K-9-3` 所禁之「宗地層量深度」**——`K-9-3` 禁的是
  **拿宗地深度當可分配判準**，⛔ 非禁止計算中出現與深度相關之中間量。

**編號現查**（`W-G.9-40`）：`K-9` 系列標題級已占用 `K-9-2`〜`K-9-8`；
`^### .*K-9-9` 實體命中 **0** ⇒ 取 **`K-9-9`**。⛔ 未改任何既有編號。

**示意圖之倉內落點**（D-2·`W-G.9-40` 入倉·⛔ 僅指路不重述內容）：
`docs/specs/figures/宗地分配線邏輯.cnt`／`….lin`／
`docs/specs/figures/宗地分配線邏輯_2.cnt`／`….lin`
（PROVENANCE ＝ `docs/specs/figures/PROVENANCE_宗地分配線邏輯.md`）。
🛑 **`.jpg` 二檔待補件**——KL 本次以 `@` 送達者僅 `.cnt`／`.lin` 四檔，
依施工單 `D-1` 之「⛔ 禁自行至本機取用」，⛔ 未取用同名 `.jpg`。
🔴 該圖係**拓樸**之權威，⛔ **非度量**之權威 ⇒ ⛔ 不得作為數值對拍之 fixture。

---

🔴🔴 **本節第七款與第八款已更正**（`W-G.9-41`·⛔ 上文原句一字未刪）

**更正一（第七款·射程過寬）**
上文「依 `K-9-3`，宗地層**一律不量深度**」**過寬**，係 claude.ai 之誤。
`K-9-3-1`（KL 裁 2026-08-05·canonical）逐字載明：
> ⛔「量測各宗自身之最小深度」**不在拆除之列**——依畸零地規則 §4，
> 最小寬度須**先有最小深度範圍**方能界定，故各宗最小深度仍為**必要中間量**，**須實量**。
> ⇒ **宗地層之判定僅餘一項：帶內最小寬度 ≥ 畸零地最小寬度。**
⇒ 正確表述為「**量、但不比附表深度**」，⛔ 非「不量」。
🔒 本節（起算垂線約束）仍係**形成端**約束，與上開**量測端**規定互不取代。

**更正二（第八款·帶底定式已被 KL 變更）**
上文所載「逐宗計算帶底（`t_hi = min(後緣角點之 t)`）」係 `K-9-6-b` 之舊定式。
**KL 已於 2026-08-16 變更之**，新定式見本檔 **`K-9-10`**。
⇒ 引用本節第八款時**須併讀該新節**；⛔ 不得再以舊定式作為帶底。
🔒 **不受影響者具名**：本節第一〜六款、`K-9-9 二` 之違反實測結果
（`W-G.9-40`·35 宗）**皆不因本更正而動搖**——其依據為形成端幾何，⛔ 非帶底。

---

### 🔒 K-9-10 帶底（最小深度範圍）之新定式（KL 裁 2026-08-16·canonical·逐字）

🔴 **本節變更 `K-9-6-b`**（KL 圖示裁定 2026-08-05）之帶底定式。
⛔ `K-9-6-b` 原文一字不刪；其定式自本節生效日起**不再適用**。

**一、有效垂線**（承 `K-9-9 七`）：自 FRONTLINE 沿其法向至 BASELINE 之**完整線段**、
**整條落在該宗範圍內**者，方計為該宗之有效深度。

**二、帶底之位置**：取該宗**最淺之有效垂線**與 BASELINE 之交點，
過該點作**平行 FRONTLINE** 之直線 ⇒ 此即帶底線。
🔒 帶底線之**方向不變**（仍平行前緣線）；⛔ 變更者僅其**位置**。

**三、帶之上限（KL 裁·⛔ 本款為第二款之上蓋）**：
帶**止於近側境界線終止之深度**。
⇒ **`t_band` ＝ min( 最淺有效垂線之深度 , 近側境界線終止之深度 )**
🔒 **理由**：本款以下之深度，該宗之近邊已非近側境界線（而係後側境界線），
第四款之寬度定義於該處**不成立** ⇒ 該段**不計入**。
⚠️ ⛔ 不得改以後側境界線充當近邊、⛔ 不得就該段量任何寬度。

**四、寬度之量法**：**沿 FRONTLINE 方向**，自**近側境界線**至**遠側境界線**之距離。
⛔ 非垂距、⛔ 非 MBR 短邊、⛔ 非內接矩形寬。

> 🔧 **指標（`W-G.9-47` 加註·⛔ 上行原文一字不刪·⛔ 非撤銷）**
>
> **KL 已於 2026-08-17 裁定 `K-9-12`**：可建築之判定改為「**四至範圍內能否完整容納
> `W × D` 矩形**」。錨 `grep -n "^### 🔒 K-9-12 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。
>
> 🔒 **⛔ 不得寫成「上行已撤銷」——二者受詞不同**：
> - **上行**係對「**寬度此一量之定義**」而發（寬度 ＝ 沿 FRONTLINE 自近側界至遠側界之距離，
>   ⛔ 非垂距／MBR 短邊／內接矩形寬）⇒ **該定義未變**。
> - **`K-9-12`** 變更者係「**可建築之判定方式**」——不再取 `w(t)` 於帶內之 min 與門檻相比，
>   改以**矩形容納**判之。
>
> ⇒ 「寬度怎麼量」與「拿它判什麼」是兩件事；`K-9-12` 動的是後者。

**五、最小寬度**：於 **帶 ∩ 宗地範圍** 內取第四款之 min。
🔒 求值法**維持現況之斷點法**（`w(t)` 為**分段線性** ⇒ 極小值必落於斷點：
`{0, t_band} ∪ 各頂點之 t ∩ [0, t_band]`）。
🔴 ⛔ **不得簡化為「僅取帶之上下兩端二次求值」**——該簡化倚賴宗地為**凸**；
倉內已登記「BASELINE 畫成**折線**」之泛化待查（見 `K-9-3` 泛化待查段），
折線 BASELINE 會夾出腰身使宗地非凸 ⇒ 兩點法在泛化後失效。

**六、與舊定式之差異（供實作對照）**：
- 舊：`t_hi = min(近側後角之 t , 遠側後角之 t)`
- 新：`t_band = min(最淺有效垂線之深度 , 近側後角之 t)`
⇒ 差異出現於 **遠側後角較淺** 之形狀：舊帶較淺（寬鬆）、新帶較深（**較嚴**）。

**編號現查**（`W-G.9-41`）：`K-9` 標題級已占用 `K-9-2`〜`K-9-9`；
`^### .*K-9-10` 實體命中 **0** ⇒ 取 **`K-9-10`**。⛔ 未改任何既有編號。
**指標**：`K-9-6-b` 之原處已就地加註指向本節（⛔ 原文一字未刪）。

---

### 🔒 K-9-11 零條合格垂線之後果：⛔ 不停機報錯·改不配地＋遞補（KL 裁 2026-08-17·canonical·逐字）

**編號現查**（`W-G.9-43`）：`K-9` 標題級已占用 `K-9-2`〜`K-9-10`；
`^#{3,4} .*K-9-11` 實體命中 **0** ⇒ 取 **`K-9-11`**。⛔ 未改任何既有編號。

── KL 裁 2026-08-17（**逐字轉錄**·⛔ 一字不改·⛔ 未順稿·⛔ 未補標點）──

> 有關那15宗土地，無一條合格垂線產生⇒ 停機報錯，但其實我看了15宗最大面寬3.2754 m，
> 這樣分配後宗地既然有機率會形成畸零地，而使得最終分配後成果的地主，領回了一塊地
> 結果申請建築時，被建管單位判定不合格，所以這樣G值所形成的面寬⇒一條合格垂線都沒有
> ⇒ ⛔ 不算臨後側境界線 ⇒ 停機報錯，但不用停機報錯，就視為該投影序號土地的G值位符合
> 該街廓可以分配情形，而由下依序號土地遞補

── 逐字段止 ──

#### 【讀法】🛑 一處未定之字（**待 KL 點頭**·⛔ 非裁示之一部）

上開逐字第五行為「G值**位**符合」。**claude.ai 之讀法 ＝「G 值<u>不</u>符合」**，
其據為**其後之「而由下依序號土地遞補」**——若讀為「符合」則無遞補之理。
🔒 **逐字段已保留原字**；本讀法⛔ **不得寫回逐字段**，且**待 KL 點頭**方為定讞。
⚠️ 下開六款係**依該讀法**導出；若 KL 更正讀法，六款須重寫。

> ✅ **KL 已確認（2026-08-17·逐字·`W-G.9-44` 加註·⛔ 上文原句一字不刪）**
>
> > 對，"不"符合的理解對。我寫成"位"是我筆誤，應該是"未"符合。
>
> ⇒ 上開讀法 **成立**：該字為 **「未」**（＝**不**符合）；原「位」係 **KL 自陳之筆誤**。
> ⇒ 本【讀法】段自本註起由「**待 KL 點頭**」改為 **「已確認」**；下開六款**⛔ 無須重寫**。
> 🔒 ⛔ **不得改寫上方逐字轉錄段之「位」字**——逐字段係當日之原樣，更正一律以**註**承載
> （`CLAUDE.md`：KL 會自陳筆誤 ⇒ 逐字段保留原字）。
> 🔒 **`U-K911-1` 結案**——⛔ 不再上呈 KL。

#### K-9-11 之定式（⛔ 由逐字導出·⛔ 未增加逐字未載之條件）

**一、本節變更 `K-9-6-h ②` 之後果。**
⛔ `K-9-6-h ②` 原文一字不刪；其**後果**自本節生效日起不再適用。
🔒 ⛔ **未變更者（逐項具名）**：
- ② 之**判準**（「兩側界線交後側境界線之二點所夾線段範圍內，**至少須有一條**合格垂線」）；
- ① 之**合格垂線定義**（整條落在該宗地內，且與**後側境界線之實體線段**相交）；
- KL 原註「此係**配地時**即不應產生之形狀，⛔ 非產生後再判不合格」之**判定時點**
  ——本節仍於**配地時**判，⛔ 非配出後再判不合格。

**二、後果之變更**：某宗依其 G 值所形成之面寬，使該宗**一條合格垂線都沒有**時 ⇒
**⛔ 不停機報錯**；改視為**該投影序號之土地其 G 值不符合該街廓可分配之情形**
⇒ **該宗不配地**，由**投影序號中下一位之土地遞補該位次**。

**三、遞補之機制⛔ 不另訂**：逐字之「由下依序號土地遞補」與 **`K-9-9 四`** 同構
（錨 `grep -n "遞補該位次" docs/rulings/K-6_街角地分配程序與可分配判準.md`）⇒
**一律沿用 `K-9-9 四`**：走**合併／調配機制**；**騰出之地入調配池**；
🔴 ⛔ **不得強制配到下限（不得超配）**。
🔒 ⇒ 調配池、合併調配、超配三事**皆已裁**，⛔ **不得再上呈 KL**。
> ⚠️ **CC 之射程具名（`W-G.9-43`·⛔ 不改上款一字）**：本款之「入調配池」「不得超配」
> **⛔ 不見於本節之 KL 逐字**，係**自 `K-9-9 四` 援引**而來，其正當性繫於
> 「**由下依序號土地遞補**」與 `K-9-9 四` 之**同構**。
> ⇒ 若 KL 認二者**非**同構，本款須重訂；⛔ 不得以「逐字已載」稱之。

> ✅ **出處之更正：二元素⛔ 非無據，且⛔ 不需上呈 KL（`W-G.9-44`·⛔ 上文一字不刪）**
>
> 上註稱該二元素「⛔ 不見於本節之 KL 逐字」——**該陳述屬實**；惟其**依據**⛔ 不止於
> `K-9-9 四` 之援引，二者**各自另有正典來源**：
>
> | 元素 | 出處 | 錨（`58c9939` 現查·各 **1 命中**） |
> |---|---|---|
> | 騰出之地**入調配池** | **守恆式之定義蘊含**：`ΣG + 調配池 = 街廓 DXF 面積` **恆成立** ⇒ 未配之地**依定義即池**、⛔ 非選擇 | `grep -n "ΣG(街廓內所有分配地)" CLAUDE.md` ⇒ `:363` |
> | ⛔ **不得超配** | **調配池必然存在、必 > 0** ⇒ 超配令池 < 0 ⇒ **違反守恆式** | `grep -n "調配池必然存在、必 > 0" CLAUDE.md` ⇒ `:355` |
>
> ⇒ 二元素之性質 ＝ **「`K-9-9 四` 之援引 ＋ `CLAUDE.md` 守恆式之蘊含」**，
> ⛔ **非 KL 本次逐字**、亦⛔ **非 CC 自創**。
> 🔒 **`U-K911-2` 結案**——正典已足以回答，⛔ 不上呈 KL。

**四、遞補者須重測**：遞補之土地占該位次後，**須以其自身之 G 值重跑本則與 `K-9-9 二`**。
🔒 **本款係第二款之直接蘊含**（若不重測，遞補者可能同樣領到不能建築之地，
與 KL 逐字之立則之由「地主……領回了一塊地結果申請建築時，被建管單位判定不合格」
**直接相違**）⇒ 記為**蘊含**、⛔ 非新裁。
> ✅ **CC 之判（`W-G.9-43`·`S-5` 未觸發）**：本蘊含**成立**——遞補者之 G 值與被替下者不同，
> 其所形成之面寬因而不同 ⇒ 不重測即無從知其是否同樣零條合格垂線。

**五、與 `K-9-9 二` 之關係**：二者**同為形成端約束、同一後果**，但**⛔ 受詞不同**
（`K-9-9 二` ＝ 遠側界位置在起算垂線之前；本則 ＝ 零條合格垂線）
⇒ 🔴 **⛔ 不得相加、⛔ 不得互代**；欲取聯集須**先在同一母體上重測**。

**六、`K-9-6-h ④` 之優先**：若該街廓經 ④ 判為**平行**，則 `①②` 之垂線容納條件**停用**
⇒ **本則於該街廓不適用**（無「零條合格垂線」可言）。

#### K-9-11-a 落地狀態（`W-G.9-43` 現查）

🔴 **已裁定、⛔ 生產碼未實作**（實作屬 `K-6-A2`）：現行 `parcel_min_width_n14`／
`_n14_band_hi` **⛔ 不檢查垂線是否整條落在宗內**（同 `K-9-6-h` 之「與現行碼之落差」表 ① 列）
⇒ 現行碼**既不停機、也不遞補**，而是逕以 `min(後緣角點之 t)` 為帶底繼續量寬度。
⚠️ 閱讀現行碼時勿誤以為本則已生效。

---

### 🔒 K-9-12 可建築之判定改為「四至範圍內能否完整容納 `W × D` 矩形」（KL 裁 2026-08-17·canonical·逐字）

**編號現查**（`W-G.9-47`）：`^### 🔒 K-9-` 占用 `K-9-3`〜`K-9-6`／`K-9-9`〜`K-9-11`；
`^### .*K-9-12` 實體命中 **0** ⇒ 取 **`K-9-12`**。⛔ 未改任何既有編號。

── KL 裁 2026-08-17（**逐字轉錄**·⛔ 一字不改·⛔ 未潤飾·⛔ 未併段）──

> 是，確定改成「四至範圍內能否完整容納 3.5m × 14m 矩形」，但是文字說明內的3.5m*14m
> 是因為對應附表的正面臨路寬的級距及該街廓的使用分區後的數字，若不同路寬級距或不同
> 使用分區則就不是3.5*14，提醒注意一下。

> 矩形不能旋轉，依照畸零地使用規則規定，寬度係依FRONTLINE(道路境界線)方向、深度係依
> 道路境界線法向的方向

> 但是分配街廓的最小深度未達畸零地深的警示功能仍然需要，供畫設街廓分配線的使用者了解

── 逐字段止 ──

#### K-9-12 之定式（**以註承載**·⛔ 上開逐字段一字不改）

**一、判準**：宗地之**四至範圍**（FRONTLINE／BASELINE／近側界／遠側界所圍）內，
能否**完整容納**邊長 `W × D` 之矩形 ⇒ **能 ⇒ 非畸零地·可建築**。

**二、`W`／`D` 之來源（🔴 ⛔ 不得硬編）**：
- `W = get_min_lot_size(該街廓分區, 該街廓正面路寬)['min_width']`
- `D = get_min_lot_size(該街廓分區, 該街廓正面路寬)['min_depth']`

🔴 **⛔ 不得硬編 `3.5`／`14`**——KL 逐字提醒：「**若不同路寬級距或不同使用分區則就不是 3.5*14**」。
⚠️ 本案（住宅區·正面路寬 10.0 m）恰為 `3.50 × 14.00`，**係巧合於本案**、⛔ 非定義。

**三、矩形之姿態：⛔ 不旋轉**（KL 逐字）——
`W` 沿 **FRONTLINE 方向**、`D` 沿 **FRONTLINE 法向**。
🔒 該二軸**即倉內現有 `st_frame` 之 `(s, t)` 框**（＝ `K-9-10 四` 之量測軸）
⇒ **⛔ 不另立座標框**（錨 `grep -n "def st_frame" verify/probes/probe_WG941_bandbase.py`）。

#### 🔴 K-9-12-a 附表值之**兩個用途自本裁起分列**（⛔ 禁互代）

| 層 | 用途 | 依據 | 落點 |
|---|---|---|---|
| **街廓層** | **警示**——分配街廓之最小深度未達畸零地深時，**警示畫設街廓分配線之使用者** | **`K-9-2`**（⛔ **原封不動**） | `app.py:18274`／`:18284`（UI） |
| **宗地層** | **矩形之邊長來源**（`W`／`D`） | **`K-9-12`** | 本節 |

🔴 **⛔ 不得**因「附表值不再是宗地層之**寬度門檻**」而**移除、降級或改寫**街廓層之警示
——KL 逐字：「**分配街廓的最小深度未達畸零地深的警示功能仍然需要**」。
🔒 **`K-9-2` 於本裁後⛔ 未變更一字。**

⚠️ **待實作批處理之登記（`W-G.9-47` 具名·⛔ 本批零生產碼、未改之）**：
`app.py:6019` 現有註解稱附表值為「**可建築門檻**」（逐字：`K-9-6：附表值＝**可建築門檻**`）
——該語**已部分過期**：附表值於**宗地層**自本裁起係**矩形之邊長**、⛔ 非門檻本身。
⇒ **登記待改**，⛔ 不得於零生產碼批次改動。

#### K-9-12-b 落地狀態（`W-G.9-47` 現查）

🔴 **已裁定、⛔ 生產碼未實作**：現行宗地層判定仍走 `parcel_min_width_n14` 之
「帶內 `w(t)` 之 min ≥ 法定最小寬」，**⛔ 不做任何矩形容納測試**。
⚠️ 閱讀現行碼時勿誤以為本則已生效。實作屬 **`K-6-A2`**。

---

#### 🔴 K-9-12-c KL 之**自陳更正**：矩形**可旋轉、可平移**（KL 2026-08-17·canonical·逐字）

⛔ **上開逐字段之「矩形不能旋轉」原文一字不刪**——其為當日之原樣；本節為 KL **事後之自陳更正**。

── KL 逐字（⛔ 一字不改）──

> 我看了你的幾何示意圖，我發現我之前表示的不對，矩形可以旋轉、矩形不一定要貼著(緊鄰)道路
> 境界線，例如你的圖，我將綠色矩形微旋轉，該矩形就會全部落在宗地範圍內了，所以矩形是可以
> 在宗地範圍內旋轉、移動的

── 逐字段止 ──

**新判準（⛔ 非 KL 逐字·係 claude.ai 之整理）**：
∃ 旋轉角 `θ` 與平移 `(u, v)`，使 `W × D` 矩形**整個落在宗地四至範圍內** ⇒ **可建築**。
**⛔ 姿態無約束**——可旋轉、可平移、**⛔ 不須貼齊 FRONTLINE**。

#### 🔒 K-9-12-d 本更正之**射程界定**（⛔ 必讀·防後批誤刪）

**本更正僅及於「矩形之姿態」。⛔ 不影響下列三者：**

| # | ⛔ 不受影響者 | 錨 |
|---|---|---|
| ① | **`K-9-10 四` 之寬度定義**（沿 FRONTLINE 方向、自近側界至遠側界·**⛔ 非垂距**） | `grep -n "⛔ 非垂距、⛔ 非 MBR 短邊" docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| ② | **`K-9-2` 街廓層深度警示**（⛔ 原封不動·KL 逐字「警示功能仍然需要」） | `grep -n "^### 🔒 K-9-12 " …` 之 `K-9-12-a` |
| ③ | **`K-9-12` 之 `W`／`D` 查表來源**（一般建築基地·`get_min_lot_size`） | 同上之定式第二款 |

⚠️ **一處必須就地寫明以防誤讀**：自由旋轉後，**兩側界平行**之宗其決定量**實質為兩側界之垂距**
（`W-G.9-49` 實測：26 宗中 **23 宗**兩側界平行，閉式 `垂距 ≥ W ∧ 帶長 ≥ D` 與侵蝕掃描
**23／23 逐宗一致**）；而 `K-9-10 四` 明文「**⛔ 非垂距**」。
🔒 **二者受詞不同 ⇒ ⛔ 不衝突**：
- `K-9-10 四` 管**寬度此一量之定義**（該量另有下游：四梯達標、`wf_f1`／`wf_f4` 調配、畸零旗標）；
- 本節管**可建築之判定**。
⇒ 「**同一個幾何量在兩處各有其名，而兩處問的不是同一件事**」——⛔ 不得以其一改寫另一。
