# `W-G.9-297R`　執行報告：`W_0` 之**口徑釘死** ／ 負擔計算之**實際消費者** — 🛑 **停機上呈**

> **單** ＝ `docs/orders/W-G.9-297_施工單_W0之口徑釘死與實際消費者.md`（工項零已原封入倉）
> **分級** ＝ **中**（現查 ＋ 量測 ＋ 登記·**零生產碼**）
> **開工態** ＝ `454d5e3dadb860308dfd5e425e645486c15dc76f`（`ls-remote` 自查·全 `40` 碼）
> 🛑 **本批之結局 ＝ 停機上呈**——`§三-2` 款 `7` 之 **[必相異] 不成立**，依單逐字「任一不成立 ⇒ **判器紅、停、上呈**」。
> 🛑 ⇒ **工項二（`自誤 400`／`401` 之鑄）⛔ 續辦**；四簿於本批**逐位未動**。
> 🔒 **KL 之放行仍在·仍⛔ 動用**；生產碼 `34` 檔一字未動。

---

## `§零`　開工閘（九項·**逐項自跑**·⛔ 採單之數）

| # | 產生指令（逐字） | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `454d5e3dadb860308dfd5e425e645486c15dc76f`（＝ `git rev-parse HEAD`） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`（`rc` 緊接該指令） | `rc = 1` ⇒ **非祖先** | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`·🔴 紅 `0` 項·**`SELF_SHA256` ✅ 相符**（受詞 `15763` B·實算 `038756861de46ce6…` ＝ 單載 `038756861de46ce66b372e2744314fce70256a16eaa784f4e8a1dc695b9c709e`） | ✅ |
| `4'` | 受詞自證（坑 `bf`）＝ 器所印之檔名列 | 含 `W-G.9-297` ＝ `True`／含 `W-G.9-296` ＝ `False` | ✅ |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0`·**四靶逐項成立**；逐字「判別力造：✅ 全數成立 ⇒ 本器非恆綠亦非恆紅」 | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`；逐字「必命中字樣 ⇒ **1**（須 > 0）／必不命中之人造字樣 ⇒ **0**（須 0）⇒ ✅」 | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `389`／`399`（缺 `[106,355,356,357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·末標籤 `bi` ⇒ **下一 `bj`**；母體 ＝ `HEAD` 之追蹤 `.md` **`792`** 檔；器自證 造甲 `6 ≥ 1`／造乙 `0`／`0` ⇒ 器非紅（**等待綁行程結束**·⛔ `\| tail -N`） | ✅ |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py`（**正面列舉**） | **`34`** 檔（`33 + 1`）；判別力[必不命中] 子層 ＝ **`311`**（須 `> 0`·前批 `310`·其值**逐批漂移**·⛔ 定值） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **未設 ⇒ 預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`git ls-files verify/baselines \| wc -l` ＝ `298` | ✅ |

🔒 **`core.quotePath` 之口徑具名**（同格載其二側）：本倉三層皆**未設** ⇒ 取預設（八進位跳脫·實例
`"docs/orders/W-G.9-189_\346\226\275…"`）；`-c core.quotePath=false` 則得原字（`docs/orders/W-G.9-189_施工單_幾何本體批第二批.md`）。

---

## `§一`　工項零：本單原封入倉

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/orders/W-G.9-297_施工單_W0之口徑釘死與實際消費者.md` |
| **受詞自證**（坑 `bf`） | 器所印之檔名 ＝ 該路徑；[必真] 含 `W-G.9-297` `True`／[必偽] 含 `W-G.9-296` `False` |
| 所指之檔數 ＝ `1`（框 ＝ 磁碟 `os.path.isfile`·母體 ＝ 工作區） | 受詢 `True`／造甲[必≥1] `W-G.9-296` 之單 `True`／造乙[必為0] 人造名（**執行期組出**·字面⛔ 出艙）`False` |
| `CR`（框 ＝ `bytes.count(b'\r')`·**讀 blob 取 bytes**） | **`0`**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf` 之 `CR` ＝ **`12308`**（全長 `88341` B） |
| **三方逐位** | 源 `15841` B `sha256` `77cc9a2d7e98ccb2daac6cdd311c3ac4e75c10b73bf4f3ee68aae55dcf8766d3` ＝ 磁碟 ＝ **blob 內容**；blob `sha1` ＝ `c58dfa62a2364dfbb6ea40dae341a8867d6ed8c2` |
| `numstat` | `176`／`0`（**`deletions` 欄 `0`**） |

---

## `§二`　工項一（**主檢**）：`W_0` 之口徑釘死與實際消費者

**器** ＝ `verify/probes/probe_WG9297_w0caliber.py`（新建·**單一 `sys.settrace`**·`co_name` 比對·⛔ 改生產碼一字）
**落檔** ＝ `verify/out/WG9297R_w0caliber.log`（`15112` B）·`rc = 5`
**物證** ＝ `verify/out/WG9297R_w0caliber_v1_blkabsent.log`（`11482` B·**⛔ 覆寫**）

### 二-1　逐閘執行次數與鍵之自證

| `SB` | `solve_G_binary` | `_mp_base_W0` | `_advance_block_with_split` | `_build_corner_range_v3`（段內） | `_corner_buffer_S`（段內） | `run_step_g` 終局 |
|---|---|---|---|---|---|---|
| `0.0` | **`149`** | **`0`** | `11` | `24`（`8`） | `1`（`1`） | （未拋） |
| `3.5` | **`149`** | **`1`** | `11` | `24`（`8`） | `2`（`2`） | （未拋） |

🔒 **`f_locals` 鍵之自證**（`§零` 新附款）：`blk_label` 存在率 ＝ **`97`／`149`**（甲·**經上溯**）·
`0`／`0` 與 `1`／`1`（乙）·`11`／`11`（守衛）；`_degenerate_order` ＝ `11`／`11`；`_N` ＝ `11`／`11`。
🔒 **`(甲)` 之上溯**：深度相異值 ＝ `[3, None]`（`None` ＝ `15` 層內查無）；
呼叫鏈（首筆·節略 `8` 層）＝ `('_solve_G_one', '_corner_first_lot_G', '_corner_block_true_G', 'run_corner_pk', 'drive', 'drive_traced', 'main', '<module>')`。
`solve_G_binary` 所見之 `side_label` 相異值 ＝ `['右側', '左側']`。

### 二-2　款 `1`〜`2`：`(甲)` 與 `(乙)` 之產生式與當場值

| 量 | 產生式（逐字） | 所在 |
|---|---|---|
| **`(甲)`** `_W_near_out` | `_W_near = float(np.dot(_bp_w - _mp_w, _ahat))` → `_W_near_out = _W_near`　（其註逐字：「W 正典：本宗**近側**界線之 KL W（mp→近側·供 stepg telescoping 閘 W₀ 用）」） | `app.py` `solve_G_binary` |
| **`(乙)`** `_b_L0`／`_b_R0` | `_bp0 = _gs + _buf·_du`；`return float(_k956_W_from_mp(_bp0, _mp, _adir, _dv))`　（其註逐字：「自此**純委派**至 `k956_W_from_mp`」） | `verify/stepg_pipeline.py` `_mp_base_W0` |

| `SB` | 格 | 鍵之一致 | **`(甲)`** | 鏈頭之旗標 | **`(乙)`** |
|---|---|---|---|---|---|
| `0.0` | `R4/left` | 甲鍵 `1`／乙鍵 `2`·**不一致 ⇒ 採交集（loud）** | **`4.4804447557822895`** | `is_corner` `True`／`is_chain_head` `True`／`W_prev` `0.0` | 🟡 **不可得（loud）** |
| `3.5` | `R2/left` | 甲鍵 `28`／乙鍵 `28`·一致 `True` | **`6.98048044737404`** | `is_corner` `False`／`is_chain_head` `True`／`W_prev` `0.0` | **`6.98048044737404`**（二鍵 `1`／`1`·一致） |
| `3.5` | `R4/left` | 甲鍵 `1`／乙鍵 `2`·**不一致 ⇒ 採交集（loud）** | **`7.785015718090257`** | `is_corner` `True`／`is_chain_head` `True`／`W_prev` `0.0` | 🟡 **不可得（loud）** |

`3.5 R2/left` 之 `(乙)` 實參：`_gs` ＝ `(310361.51526, 2651912.18005)`／`_buf` ＝ `8.696079893776137`／
`_dv` ＝ `(0.6756855318673246, 0.7371899768887057)`／`_mp` ＝ `(310378.15828, 2651897.072295)`。
各格之 `_rw_start`／`W`（遠側）／`Rw`：`4.4804447557822895`／`65.14394336216473`／`0.5922124251414981`；
`6.98048044737404`／`7.0864779911520355`／`0.005782426021889577`；
`7.785015718090257`／`68.62440261125273`／`0.40060915122312607`。

### 二-3　款 `3`：負擔計算之**實際消費者**（逐處具名·⛔ 推定）

| # | `檔:函式` | 賦值（逐字） | 受詞 |
|---|---|---|---|
| `1` | `app.py:solve_G_binary` | `_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)` | **鏈頭取 `(甲)` `_W_near`**；非鏈頭取 `W_prev`（＝前一宗之 `W_far`） |
| `2` | `app.py:solve_G_binary` | `Rw = rw_increment(_rw_start, W)` | `Rw` 之**唯一**產生處 |
| `3` | `app.py:solve_G_binary` | `G_target = max(0.0, (a * (1.0 - A * B) - Rw * F * l_side …` | `G` 消費 `Rw` |
| `4` | `verify/stepg_pipeline.py:_run_step_g_impl` | `_b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block) if _fo_left else 0.0` | **`(乙)` 之唯一消費端 ＝ `_select_pool_slot` 之 `'b'` 槽** |
| `5` | `verify/stepg_pipeline.py:_run_step_g_impl` | `_slot_res = _select_pool_slot(…, {'has': …, 'F': …, 'l1': …, 'b': _b_L0}, …)` | 同上 |

⇒ **消費處 `5` 處**（`(甲)` 之鏈 `3` 處／`(乙)` 之鏈 `2` 處）；`_rw_start` 實際賦值次數（全情境）＝ **`298`**。
🔑 **三格之鏈頭其 `_rw_start` 皆 ＝ `(甲)`**（`is_corner ∨ is_chain_head` 為真）
⇒ **`Rw`／`G` 之鏈實取 `(甲)`**；`(乙)` 只進**選槽理論**。二鏈**互不相交**。

🔒 **字樣錨之唯一性（`uniq`·坑 `bi`·母體 ＝ `app.py` 單檔·列框）**：
`_W_near_out = _W_near` ＝ **`1`**／`_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)` ＝ **`1`**／
`Rw = rw_increment(_rw_start, W)` ＝ **`1`**；判別力 ＝ 寬字樣 `_rw_start` ＝ **`5`** 列（`> 1` ⇒ 上錨**非恆為 `1`**）；
對照組[必為零] ＝ 人造錨 **`0`**。

### 二-4　款 `4`：守衛 `not _degenerate_order and _N > 1` 之**各分項**（⛔ 只報合取）

| `SB` | 街廓 | `_degenerate_order` | `_N` | `not _degenerate_order` | `_N > 1` | **合取** | 呼叫列 |
|---|---|---|---|---|---|---|---|
| `0.0` | `R4` | `False` | **`1`** | `True` | **`False`** | **`False`** | `1017` |
| `3.5` | `R2` | `False` | **`14`** | `True` | `True` | **`True`** | `1019`／`1063` |
| `3.5` | `R4` | `False` | **`1`** | `True` | **`False`** | **`False`** | `1017` |

⇒ 🔑 **`(乙)` 於二格不可得之分項成因 ＝ `_N > 1` 為偽**（**⛔ `_degenerate_order`**）。

### 二-5　款 `6`：`R(·)`（一律取 `ns["rw_from_width"]`·⛔ 器內另算）與二組差

| `SB` | 格 | `S1_perp` | `R((甲))` | `R((乙))` | `R(S1_perp)` | `R((甲))−R(S1)` 帶號／絕對值 | `R((乙))−R(S1)` 帶號／絕對值 | `(甲)−(乙)` |
|---|---|---|---|---|---|---|---|---|
| `0.0` | `R4/left` | `3.6954999940774775` | `40.77875748585019` | 🟡 | `35.8207499615036` | `4.958007524346591`／`4.958007524346591` | 🟡 | 🟡 |
| `3.5` | `R2/left` | `7.0` | `55.58873855003203` | `55.58873855003203` | `55.7` | `-0.11126144996796938`／`0.11126144996796938` | `-0.11126144996796938`／`0.11126144996796938` | **`0.0`** |
| `3.5` | `R4/left` | `7.0` | `59.93908487768739` | 🟡 | `55.7` | `4.2390848776873895`／`4.2390848776873895` | 🟡 | 🟡 |

### 二-6　款 `7`：判別力三造之判 — 🛑 **[必相異] 不成立**

| 造 | 判準 | 實測 | 判 |
|---|---|---|---|
| （母體） | `forced` 格數 `≥ 1` | **`3`** | ✅ |
| **[必相異]** | `(甲)` 與 `(乙)` 於至少一格須**相異**（⇒ 證二者⛔ 同物） | `6.98048044737404` vs `6.98048044737404` ⇒ **全同**（**判定組 `1`／`3` 格**） | 🔴 |
| **[必非零]** | 款 `3` 之消費處數 `≥ 1` | 消費處 **`5`** 處·`_rw_start` 賦值 **`298`** 次 | ✅ |
| **[必為零]** | 同一量自比之差 ＝ `0`（**先自證可滿足**） | `0.0`／`0.0`／`0.0`（判定組 **`3`**） | ✅ |

🛑 **依單 `§三-2` 款 `7` 逐字「任一不成立 ⇒ 判器紅、停、上呈」⇒ 本批停。**

### 二-7　🔴 上呈之要點（⛔ 判孰是·⛔ 判孰誤·**候發單側**）

**（一）單 `§三-1` 與 `自誤 400` payload 所載之二值，經實測<u>確為 `(甲)`</u>。**
`4.4804447557822895` ＝ `(甲)`＠`0.0 R4/left`；`7.785015718090257` ＝ `(甲)`＠`3.5 R4/left`。
而 `6.98048044737404` 於 `3.5 R2/left` **同時**為 `(甲)` 與 `(乙)`（**逐位相同**）。
⇒ 🔴 **`W-G.9-296` 所報之「⛔ 相符」，其二端係<u>不同格</u>**（`R4` 之 `(甲)` vs `R2` 之 `(乙)`），
**⛔ 必然是「同一格之二個相異量」**。此與 payload 之**徵候敘述**有出入。

**（二）前批呈 KL 之「負擔率差」係取 `(甲)`。** 本批實測 `R((甲)) − R(S1_perp)`（帶號·個百分點）
＝ `+4.958007524346591`／`-0.11126144996796938`／`+4.2390848776873895`。

**（三）二者於 `3.5 R2/left` 逐位相同之<u>機制</u>⛔ 未驗。** 其一為 `dot(_bp_w − _mp_w, _ahat)`、
其一為 `k956_W_from_mp(_gs + _buf·_du, _mp, _adir, _dv)`——**⛔ 提假說、⛔ 判其成因**。

### 二-8　🩸 入倉前自捕**三則**（皆**量測器紅**·⛔ 頂替）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `1` | 工項零之自證**逕跑前批（`-296`）之器** | 造甲欄印出「`W-G.9-295` 之單」而與本批無涉 | 受詞為他批之單 | **受詞自證**（坑 `bf`）：器須先印其所量之檔名並與本批單號對拍二造 |
| `2` | v1 `rc = 5`：三格皆報「無鏈頭之錄」 | **`blk_label` 存在率 `0`／`149`** | `solve_G_binary` **⛔ 由 `_run_step_g_impl` 直接呼叫**（實測呼叫鏈 `_solve_G_one` → `_corner_first_lot_G` → `_corner_block_true_G` → `run_corner_pk`）⇒ `f_back` 無該名 | **逐層上溯**（≤ `15` 層·出艙深度與呼叫鏈）＋**第二鍵** `side_mid` 逐位相等·**二鍵並報**。修後命中率 **`97`／`149`** |
| `3` | v2 `rc = 1`（`TypeError: '<' not supported between 'int' and 'NoneType'`） | **落檔 bytes 與 v1 相同（`11482`）** ⇒ 只看落檔會誤讀為「修法無效」 | 器拋出 ⇒ **未落檔** | 以 **`rc` ＋ stdout 之 traceback** 為判，⛔ 以落檔存在充「已跑完」。🔑 該例外**反證**上溯確有部分命中（全落空則集合僅含 `None`、⛔ 拋此例外） |

🔑 **自捕 `2` 係由本單 `§零` 之新附款（鍵存在自證）當場攔下**——若無該自證，v1 之表將以三格
「🟡 不可得」出艙，**與 `W-G.9-296` 之結構性不可得無從分辨**。

---

## `§三`　工項二：🛑 **⛔ 續辦**（停機之射程）

依單 `§三-2` 款 `7` 之停機令，本批**⛔ 鑄 `自誤 400`／`401`**、**⛔ 追加任何 payload**。
⇒ 🔒 **四簿於本批逐位未動**（`§五` 閘 `4` 所期之「自誤 相異 `391`／MAX `401`」**⛔ 達成**）
——其相異係**停機之應然**，於此 **loud 具名**，⛔ 以「未達成」判為缺失。
🛑 其處置（payload 是否因 `二-7（一）` 而須修訂後重發）**候發單側**；⛔ 由 CC 代擬、⛔ 由 CC 自裁。

---

## `§四`　`⛔ 為之事` 之逐項自檢（單 `§六`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔一字 | ✅ 逐 `commit` 之生產碼判法皆 **`0` 行**（見 `§五`）；對照組[必非零] `17fd981^..17fd981` ＝ `2` |
| `2` | ⛔ 開任何分支 | ✅ 未開 |
| `3` | ⛔ 動用 KL 之放行 | ✅ 未動用 |
| `4` | ⛔ 擬任何實作形／⛔ 設計驗算 | ✅ 本批**⛔ 含任何設計驗算**（無假設輸入之複算） |
| `5` | ⛔ 調任何界、⛔ 事後放寬任何門檻 | ✅ 未調；停機款照單觸發 |
| `6` | ⛔ 自寫第二套幾何或**第二份 `W` 之定義**（`GB-48` 族） | ✅ `R(·)` 一律取 `ns["rw_from_width"]`；`(甲)`／`(乙)` 皆**自生產路徑攔取**、器內⛔ 另算 |
| `7` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp`／任何函式簽章 | ✅ 未改（`app.py` blob 期初＝期末） |
| `8` | ⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未動（`git diff --numstat` 對 `probe_WG9269_pit_index.py` 輸出空） |
| `9` | ⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**、⛔ `WV_BAKE`、⛔ 併線 `p3a`、⛔ 引任何自 `baselines` 取得之數 | ✅ `baselines` `298` 檔·框 `sha256` 未變；三物證落檔 blob 逐位未動；`WV_BAKE` 未設；`p3a` 仍非祖先；本報告之數無一取自 `baselines` |
| `10` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號 | ✅ 未鑄；`自誤 400`／`401` 亦因停機而**⛔ 鑄** |
| `11` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`） | ✅ 未解除、未收窄 |
| `12` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ 由生產碼 blob 相異 `0` 機械蘊含 |
| `13` | ⛔ 追改任何已入倉文件一字 | ✅ `append-only` 機檢見 `§五` |
| `14` | ⛔ 判 `A′` 之受詞、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | ✅ `二-7` 三項**並列出艙而⛔ 判**，明載候發單側 |
| `15` | ⛔ 刪任何 ref | ✅ 未刪 |
| `16` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ 收工閘之數係**報告入倉後**以末端追加補入；閘 `7` **出艙於回報·⛔ 入倉** |
