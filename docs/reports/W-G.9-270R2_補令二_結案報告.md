# `W-G.9-270R2`　補令二（結清裁）之執行 ＋ **`W-G.9-270` 結案報告**

> **受詞 ＝ CC**｜**零生產碼批**（`app.py` 與 `verify/` 生產路徑一字未動·含註解）
> **期初態** ＝ `9406275f5b940efa95f3ecc6da61d2ee767ca97b`（`git ls-remote` 實查·`left-right` ＝ `0　0`）
> **單** ＝ `docs/orders/W-G.9-270_補令二_三阻塞項之結清裁.md`（工項零已原封入倉）
> **KL 之序改**（`2026-09-12`·對話側·逐字要旨）：**補令二 `§八` 序 `3`（辦補令一）撤除**——
> 補令一（`file sha256 af2ba742d2f93a77…`）**改由新窗辦，本窗⛔ 接**；序改為 `1`〜`5`（見 `§零-2`）。
> 🛑 **本報告⛔ 載**：`K-9-12` 之任何修法主張／`GB-164` 之落地／`GB-165` 之落地／
> `GB-162` `(b)(c)` 之落地／`R5·left` 閘 `γ` 受詞之改／抵費地之任何**處置主張**。

---

## `§零`　態、序、與 `§六` 預設之續行

### 零-1　開工閘（當場復現）

| 閘 | 實測 |
|---|---|
| `ls-remote`（⛔ 本地追蹤 ref） | `wip/s1-endpart` ＝ `9406275f5b940efa95f3ecc6da61d2ee767ca97b`；`verify/W-G.9-269-p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（**未動**） |
| `left-right` | **`0	0`** ✅（⇒ `CLAUDE.md` 係 wip 態） |
| 生產碼 `34` 檔 | `app.py` blob `sha1` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`（期初＝期末·工作區 `hash-object` 同值） |
| `S-0b` 收單機檢 | `probe_order_preflight.py` ⇒ `rc = 0`（`0` 紅）；🟡 `P-5` **本補令⛔ 載 `SELF_SHA256`** ⇒ 具名豁免（其位由三端 `sha256` 承擔） |

### 零-2　**工項零**：補令二原封入倉

| 端 | `sha256` |
|---|---|
| `1` 來源（主 checkout·未追蹤） | `5e04a2b2ad08fb5beb902a79576c82df8ee54fdc7967390201aa7534a9bc9e06` |
| `2` 落檔（本 worktree） | `5e04a2b2ad08fb5beb902a79576c82df8ee54fdc7967390201aa7534a9bc9e06` |
| `3` **入倉 blob 之內容** | `5e04a2b2ad08fb5beb902a79576c82df8ee54fdc7967390201aa7534a9bc9e06` |

🔒 **三端逐位相同**；blob `sha1` ＝ `882df8bc04ac9798aec76d146fdd1a729c60b298`／`9988` B ＝ 落檔 bytes
（**hash 二式並報**·坑 `21`）；`CR = 0`／`LF = 124`；`core.autocrlf` 有效值 `false` ⇒ ⛔ 行尾正規化。

### 零-3　🔴 **補令一之處置（照實·⛔ 隱去）**

🔒 **現查（正面列舉·三處）**：主 checkout 之 `docs/orders/` 含 `W-G.9-270_補令一` 者 ＝ **`1`** 檔；
本 worktree 之**追蹤檔** ＝ **`0`**；對照組（`W-G.9-270_施工單`）＝ **`1`** ⇒ 器非紅。
⇒ 🛑 **補令一之檔確存在於主 checkout，惟 KL 已明令「改由<u>新窗</u>辦，本窗⛔ 接」**
⇒ **本窗⛔ 接、⛔ 入倉**（其工項零屬**新窗**）。
🛑 ⇒ 本報告**⛔ 列 `GB-164`**（其授權在補令一）——KL 序改令逐字。

### 零-4　`§六` **開工即設之預設**之續行（承 `W-G.9-270R` `§零`）

本補令批之全部文字落檔**逐檔具名其工具**：

| # | 落檔 | 工具 |
|---|---|---|
| `1` | `verify/probes/probe_WG9270R2_pool_dist.py` | **`Write`** ＋ **`Edit`** ×`1` |
| `2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` 之三處末端追加 | **`Edit`**（append-only） |
| `3` | 本報告 | **`Write`** ＋ **`Edit`** |
| `4` | `commit message` | **`Write`** ＋ `git commit -F <檔>` |
| `5` | 工項零之檔案搬移 | **`cp` ＋ glob 展開**（字面全 ASCII·⛔ 令 CJK 穿過引號層） |

🔒 **本批 heredoc ＝ `0` 次**（前一批 `3` 次·全由硬閘攔下）。

---

## `§一`　序 `1`：鑄 `GB-165` ＋ 三準則入交接註

### 一-1　`GB-165` 之號占用查（`常規四（七）`）

**器** ＝ `verify/probes/wg9268_gate6_occupancy.py`（倉內既有）｜態 ＝ `blob@9406275…`｜
母體 ＝ **全 `docs/` `701` 檔**（讀不到 `19` ＝ git 失敗 `0` ＋ 解碼失敗 `19`）｜粒度框 ＝ **檔框 ＋ 列框**（宣告框）

| 號 | 形 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | 列框(∪) | 檔框 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`GB-165`** | 嚴格 | `0` | `0` | `0` | `0` | **`0`** | **`0`** | 受詢 ⇒ **未占用** |
| **`GB-165`** | 寬式 | `0` | `0` | `0` | `0` | **`0`** | **`0`** | 〃 |
| `GB-163` | 嚴格／寬式 | `0`／`0` | `9`／`9` | `0`／`1` | `0` | `9`／`10` | `5`／`6` | 對照甲（須 `≥1`）✅ |
| `GB-160` | 嚴格／寬式 | `0`／`0` | `10`／`10` | `0`／`4` | `0` | `10`／`14` | `5`／`8` | 對照甲 2 ✅ |
| `⟨NEG⟩` | 嚴格／寬式 | `0` | `0` | `0` | `0` | `0` | `0` | 對照乙（須 `=0`）✅ |

⇒ **六數全 `0`** ⋀ 二對照組如預期 ⇒ **器非紅**、`GB-165` **可取**。

🩸 **一既有器之瑕（坑 `t`：<u>具名而不修</u>）**：`wg9268_gate6_occupancy.py` **將對照乙之哨兵字面印入其輸出**
（違坑 `14`：「哨兵之號寫成字面隨檔入倉 ⇒ 次批其真零失效」）
⇒ 🔒 **本窗⛔ 將該 log 入倉**，其數改以 **`⟨NEG⟩`** 載於上表；**⛔ 修該器**（與本批受詞無涉）。

### 一-2　`GB-165` 已鑄（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔尾）

其受詞、解除條件三款與「⛔ 逕刪該片」之禁，**逐字轉錄自補令二 `§四-5`**（⛔ 改寫）。
🛑 **本批⛔ 落地之**（補令二 `§八` 明令）——僅**鑄之並登記**。

🔴 **取號之照實併記**：`GB` 之 `MAX` 由 **`163`** 直接轉為 **`165`**，**`164` 成為缺號**。
其**非漏鑄**——`GB-164` 之授權在**補令一**，而該補令⛔ 送達本窗（KL 明令改由新窗辦）。
🛑 **`常規四（九）二` 閘 `②`（「MAX 須由 `N-1` 轉為 `N`」）於本批<u>結構上不可滿足</u>**
⇒ 依 `自解款` **白名單類 `7`**（框慣例之別 ⇒ 取較嚴框續辦·⛔ 據此作成明文），
改以「**MAX `163 → 165`** ⋀ **`164` 係<u>已授權而待他窗鑄</u>**」出艙，並列入 `§七` 之**自解清單**。
🛑 凡出艙 `GB` 之缺號集者，**須同格具名此情狀**；**⛔ 由缺號集推論 `164` 為「未鑄」**。

### 一-3　三準則（`§一`／`§四-3`／`§六`）⇒ 見 `§六` 收工交接註

---

## `§二`　序 `2`：`GB-163` 之結清 ＋ `GB-160` 之射程收窄（皆已入倉·末端追加）

| 落點 | 內容 | ⛔ 追改前文 |
|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` 檔尾 | `## 🔧 \`GB-163\` 之**結清**`（判定集補全表／「留存宗」＝**配地宗**之定義逐字／`🟢 可結清` 之裁逐字／**射程之界**） | ✅ 一字未動 |
| 〃 | `## 🔧 \`GB-160\` 之**射程收窄**`（二側之成因與其路逐字／二答之實測值／`3` 側母體 `0` 維持「⛔ 靜默綠」／**⛔ 結清**） | ✅ 一字未動 |

🔒 **二節之節題 `#` 後皆夾 `🔧 `** ⇒ `VR-093` 三框**皆不命中** ⇒ **`GB` 之 MAX／相異逐位未受影響**
（`GB-132` 之避讓·已由 `§七` 之四簿復查坐實）。

---

## `§三`　序 `3`（🆕 KL 序改）：**抵費地面積分布表**

**器** ＝ `verify/probes/probe_WG9270R2_pool_dist.py`｜落檔 `verify/out/WG9270R2_pool_dist.log`｜`rc = 0`｜`stderr` `0` B
🛑 **本節⛔ 作任何處置主張**——其**實務性質候 KL 答**（KL 序改令逐字）。
🛑 **⛔ 逕刪任何片**（`GB-165` 明令：其為量測之受詞，刪之即滅證）。

### 三-1　框與真相源（⛔ 憑記憶·⛔ 憑 baseline）

- **母體** ＝ 記憶體側 `to_csv_like(g_rows)`（`0m` **`56`** 列／`3.5m` **`59`** 列·**靜默丟列 `0`**）。
- **框（坑 `y`：先印值分布再定框）**：`推進側別` 之全體值分布 ＝
  `0m` `right 22`／`left 21`／**`抵費地 13`**；`3.5m` `right 23`／`left 22`／**`抵費地 14`**
  ⇒ 取值為「抵費地」者；其片數與值分布**逐數相符**。
- **面積二源並報（⛔ 單源）**：`(A)` 生產所寫之 `幾何面積(㎡)` 欄 ↔ `(B)` 自 `cut_coords` 以 `shapely` **現算**。
  容差**具名** ＝ `0.005 ㎡`（＝ 落檔 `2dp` 之半個單位）⇒ **相異或不可得者 `0`／`13`・`0`／`14`** ✅。
- **`MinA_區`**：逕呼生產側 **`verify/wd4_tier_list._mina_by_block(ns, snapshot, cb_by, build_blocks)`**
  （**單一真相源**·其 `build_blocks` 實參**逐字沿用生產呼叫點**＝ 僅**可建築土地**）
  ⇒ **`MinA_區` ＝ `115.88`**；逐街廓 `MinA_i` ＝ `R1 116.02`／`R2 155.64`／`R3 155.19`／
  **`R4 115.88`**／`R5 159.99`／`R6 159.28`。🛑 **此值與 `CLAUDE.md` 所載相異——見 `§五-1`**。

### 三-2　情境 `退縮 0m`（`13` 片·**依面積升冪**）

| # | 暫編地號 | 街廓 | `(A)` 幾何面積(㎡) | `(B)` 現算(㎡) | 解法 | 類 |
|---|---|---|---|---|---|---|
| `1` | `R1-抵費地-2` | `R1` | **`0.000000`** | **`0.004600`** | 幾何剩餘 | **零面積** |
| `2` | `R4-抵費地-4` | `R4` | `26.090000` | `26.085762` | 幾何剩餘 | 非零但 `< MinA_區` |
| `3` | `R4-抵費地-3` | `R4` | `26.110000` | `26.111604` | 幾何剩餘 | 非零但 `< MinA_區` |
| `4` | `R6-抵費地-2` | `R6` | `85.710000` | `85.706388` | 幾何剩餘 | 非零但 `< MinA_區` |
| `5` | `R4-抵費地-2` | `R4` | `116.080000` | `116.079994` | 幾何剩餘 | 其餘 |
| `6` | `R5-抵費地-2` | `R5` | `145.170000` | `145.170260` | 幾何剩餘 | 其餘 |
| `7` | `R2-抵費地-2` | `R2` | `152.360000` | `152.355130` | 幾何剩餘 | 其餘 |
| `8` | `R4-抵費地-1` | `R4` | `1022.050000` | `1022.046201` | 幾何剩餘 | 其餘 |
| `9` | `R1-抵費地-1` | `R1` | `1175.490000` | `1175.493745` | 幾何剩餘 | 其餘 |
| `10` | `R6-抵費地-1` | `R6` | `1543.010000` | `1543.006306` | 幾何剩餘 | 其餘 |
| `11` | `R5-抵費地-1` | `R5` | `1621.800000` | `1621.801031` | 幾何剩餘 | 其餘 |
| `12` | `R2-抵費地-1` | `R2` | `1682.740000` | `1682.744453` | 幾何剩餘 | 其餘 |
| `13` | `R3-抵費地` | `R3` | `1795.130000` | `1795.132785` | 幾何剩餘 | 其餘 |

**三分類**：零面積 **`1`** ／ 非零但 `< MinA_區`(`115.88`) **`3`** ／ 其餘 **`9`** ⇒ 和 ＝ **`13`** ✅ ＝ 片數。
**Σ抵費地面積**（`(A)` 源）＝ **`9391.74 ㎡`**。

### 三-3　情境 `退縮 3.5m`（`14` 片·**依面積升冪**）

| # | 暫編地號 | 街廓 | `(A)` 幾何面積(㎡) | `(B)` 現算(㎡) | 解法 | 類 |
|---|---|---|---|---|---|---|
| `1` | `R1-抵費地-3` | `R1` | **`0.000000`** | **`0.004575`** | 幾何剩餘 | **零面積** |
| `2` | `R1-抵費地-2` | `R1` | `5.380000` | `5.383510` | 幾何剩餘 | 非零但 `< MinA_區` |
| `3` | `R4-抵費地-4` | `R4` | `26.080000` | `26.084297` | 幾何剩餘 | 非零但 `< MinA_區` |
| `4` | `R4-抵費地-3` | `R4` | `26.110000` | `26.110214` | 幾何剩餘 | 非零但 `< MinA_區` |
| `5` | `R6-抵費地-2` | `R6` | `85.710000` | `85.706388` | 幾何剩餘 | 非零但 `< MinA_區` |
| `6` | `R5-抵費地-2` | `R5` | `146.180000` | `146.180629` | 幾何剩餘 | 其餘 |
| `7` | `R4-抵費地-2` | `R4` | `226.010000` | `226.009994` | 幾何剩餘 | 其餘 |
| `8` | `R2-抵費地-2` | `R2` | `308.170000` | `308.170000` | 幾何剩餘 | 其餘 |
| `9` | `R4-抵費地-1` | `R4` | `906.320000` | `906.324680` | 幾何剩餘 | 其餘 |
| `10` | `R1-抵費地-1` | `R1` | `1169.710000` | `1169.708187` | 幾何剩餘 | 其餘 |
| `11` | `R2-抵費地-1` | `R2` | `1466.260000` | `1466.258357` | 幾何剩餘 | 其餘 |
| `12` | `R6-抵費地-1` | `R6` | `1543.010000` | `1543.006306` | 幾何剩餘 | 其餘 |
| `13` | `R5-抵費地-1` | `R5` | `1620.790000` | `1620.789249` | 幾何剩餘 | 其餘 |
| `14` | `R3-抵費地` | `R3` | `1795.130000` | `1795.132785` | 幾何剩餘 | 其餘 |

**三分類**：零面積 **`1`** ／ 非零但 `< MinA_區`(`115.88`) **`4`** ／ 其餘 **`9`** ⇒ 和 ＝ **`14`** ✅ ＝ 片數。
**Σ抵費地面積**（`(A)` 源）＝ **`9324.86 ㎡`**。

### 三-4　**二數並報**（坑 `an`：⛔ 逕採令所引之數）

| 量 | KL 序改令所引 | **當場復算** | 判 |
|---|---|---|---|
| `0m` 零面積／`< MinA_區`／其餘 | `1`／`3`／`9` | **`1`／`3`／`9`** | ✅ 逐數相符 |
| `3.5m` 零面積／`< MinA_區`／其餘 | `1`／`4`／`9` | **`1`／`4`／`9`** | ✅ 逐數相符 |
| `0m` Σ抵費地（補令二 `§四-2` 之分母） | `9391.74` | **`9391.74`** | ✅ 相符（`2dp`） |
| `3.5m` Σ抵費地 | `9324.86` | **`9324.86`** | ✅ 相符（`2dp`） |

### 三-5　判別力三造（證該分類法**⛔ 恆歸一類**）

造甲［人造一零面積片 `A = 0.0`］⇒ 歸 **零面積** ✅；
造乙［一 `5.38 ㎡` 之真實小片］⇒ 歸 **非零但 `< MinA_區`**（**⛔ 零面積**）✅；
造丙［`MinA_區 + 1` ＝ `116.88 ㎡`］⇒ 歸 **其餘** ✅ ⇒ **器非紅**。
（🔒 造乙即補令二 `§四-2` 併記之 `5.38 ㎡` 真實小片——本表 `三-3` 第 `2` 列坐實其**通過**、⛔ 在不成立之列。）

---

## `§四`　序 `4`：`W-G.9-270` **結案報告**

### 四-1　三項之現況

| 項 | 現況 | 依據 |
|---|---|---|
| **`GB-163`** | 🟢 **已結清** | 補令二 `§四-4`；判定集 `56/56`・`59/59`、配地宗 `43/43`・`45/45` 全稱成立、不成立 `0`、⛔ 可量 `0`；「留存宗」＝ **配地宗**（發單側之裁·`§四-3`） |
| **`GB-160`** | 🔴 **射程收窄·未結** | 補令二 `§三`；`R6·right` **繫於 `K-9-12`**（俟其批落地後**重量**）／`R5·left` **閘 `γ` 之受詞須另定**（「無共用頂點」應判 `不適用` 抑或 `不過`）——**由發單側另單** |
| **`GB-162`** | 🔴 **未結** | 補令二 `§五`；三條件僅 `(a)` 之**歸因**已辦（全量 `1656` 格 ⇒ `(i)` `1656`／`(ii)` `0`），`(b)`／`(c)` 未辦。**其三禁令維持** |

### 四-2　**未結阻塞項清單**（🛑 `GB-164` **⛔ 列入**——其授權在補令一·本窗未收到）

| 號 | 受詞（節略） | 解除條件之現況 |
|---|---|---|
| **`GB-159`** | `W-G.9-268′` 併線後之**中間態配地表⛔ 對外** | 🔴 **仍有效**——其缺口**繫於 `K-9-12`**（矩形容納仍在觀測模式）；受影響之宗之**配地圖與負擔表⛔ 對外**（含三戶補償數） |
| **`GB-160`** | `V-1′` 之器於某類側無從建框（閘 `γ` 不過） | 🔴 **射程收窄為二側**·未結（見 `四-1`） |
| **`GB-162`** | `verify/baselines` 與現行輸出之差**成因未歸因**；baseline 係**應然值** | 🔴 `(a)` 已辦／`(b)`(`c`) 未辦。**三禁令維持**：⛔ 設 `WV_BAKE`／⛔ 覆寫 `verify/baselines`／⛔ 併線 `p3a` |
| **`GB-165`** | **零面積之幾何剩餘片**入「宗」之母體 ⇒ 母體虛增 ⋀ 產出偽「不成立」 | ⬜ **本批新鑄·未落地**（落地⛔ 本批辦）。三款解除條件見其本文 |

🔒 **`GB-161`／`GB-163` ⛔ 在本清單**：前者非本批受詞；後者**已結清**。

---

## `§五`　🛑 **上呈項**（`§七` 明令：⛔ 自行鑄號·⛔ 自裁）

### 五-1　🔴 **`MinA_區` 之典側不同步**：生產側實算 `115.88` ／ `CLAUDE.md` 載 `115.85`

🔒 **現查（三軸）**：來源 ＝ 態 `blob@9406275…` 之**活體實跑**（`_mina_by_block` 直呼）／
母體 ＝ 六可建築街廓／粒度框 ＝ **值**。

| 側 | 值 | 出處（`檔:行`） |
|---|---|---|
| **生產側實算** | **`115.88`** @ `R4` | `verify/wd4_tier_list._mina_by_block` 之回傳（落檔 `verify/out/WG9270R2_pool_dist.log`） |
| **典側（`CLAUDE.md`）** | `115.85` @ `R4`（`round(D_avg,2) = 33.10`）；½ 顯示 `57.93` | `CLAUDE.md:408` |

🔒 **量測器非紅之自證**：全倉 `.md` 之 **`115.88` 命中 `0` 檔**；對照組 `115.85` ＝ **`13`** 檔；
執行期組出之哨兵 ＝ **`0`** 檔 ⇒ **器非紅**。
⇒ 🔴 **`115.88` 此值於倉內⛔ 任何記載** ⇒ **典側與碼側不同步**（「碼已動而典未更新」之族·
＝ `CLAUDE.md` 之「🔧 待落地正本表之增欄：典側陳述是否與碼同步」所治者）。

🔒 **其成因之機械線索（⛔ 推定·照實）**：`_mina_by_block` 之算式逐字為 `round(depth * mw, 2)`，
其 `depth` 取自 `snapshot["blocks"][lbl]["街廓分配深度_m"]`；
而本窗 `W-G.9-270R` 之對帳輸出逐字載 `[參數0m] ('R4',) 欄「街廓分配深度(m)」: baseline='32.59' got=33.11`
⇒ **現行深度為 `33.11`**（⛔ `CLAUDE.md` 所載之 `33.10`）；`33.11 × 3.5 = 115.885` ⇒ `round` ⇒ `115.88`。

🛑 **其後果之射程（照實·⛔ 誇大亦⛔ 縮小）**：依 `CLAUDE.md` 之 `K-6 §零-4`，
`region_min` 之用途**已收斂為單一**——僅供 **½ 現金補償／增配門檻** ⇒ **有金額後果**。
🛑 **本窗⛔ 改 `CLAUDE.md` 一字、⛔ 鑄號、⛔ 自裁其應以何者為正本**——**上呈發單側**。

### 五-2　🔴 **「零面積」係 `2dp` 落檔欄之產物；其現算面積實為 `0.0046 ㎡`**

🔒 **實測**：二片之 `(A)` 幾何面積(㎡) 欄皆為 `0.000000`，而 `(B)` **自 `cut_coords` 現算**為
**`0.004600`**（`0m` `R1-抵費地-2`）／**`0.004575`**（`3.5m` `R1-抵費地-3`）。
🔒 **該值於倉內<u>已有</u>記載**：`GB-120` 之逐字實例
`[T2-DIAG] 街廓 R1｜…｜池帶面積 [1169.7082  5.3835  0.0046]｜Σ池 1175.0963`
（`W-G.4_泛用阻塞項登記表.md`），`W-G.9-182R` 之判別力對照亦逐位復現之
⇒ **`0.0046` 係 `R1` 第三池帶之已知量**（全倉 `.md` 命中 **`13`** 檔·對照組 `5.38` ＝ `30` 檔 ⇒ 器非紅）。

🛑 **⇒ 補令二 `§四-2` 之前提「二者之幾何面積**皆為 `0.000000 ㎡`**」係<u>`2dp` 欄之框</u>所致**，
**其真值為 `0.0046 ㎡`（約 `46 cm²`）、⛔ 字面零**。
🔒 **本窗⛔ 判其結論是否因此改變**——該結論（「其內⛔ 有土地、⛔ 涉任何地主」⇒ 域問題要件不成立
⇒ ⛔ 上呈 KL）**係發單側之裁**；本節**只照實出艙其前提之框**，**⛔ 自裁**、**⛔ 上呈 KL**。
🔒 **同族** ＝ 坑 `ar`（**二數相等⛔ 證明其框相同**）之**鏡像**：本例係**一數之零⛔ 證明其量為零**。
🔒 **併記（⛔ 弱化 `GB-165`）**：`GB-165` 之受詞係「其被列入以**宗**為母體之量測中 ⇒ 母體虛增」，
該受詞**⛔ 繫於「面積是否字面為零」** ⇒ 本節**⛔ 動搖 `GB-165` 一字**。

---

## `§六`　序 `5`：**收工交接註**

### 六-1　本批新立之三準則（補令二 `§一`／`§四-3`／`§六`·逐字）

**準則 `A`（補令二 `§一`·收單之第一動作）**
> **凡收一單，第一動作為驗其與<u>已入倉之同名／同號 blob</u> 是否逐位相同；相同者 ＝ 同一單 ⇒ **⛔ 重跑**、⛔ 以重跑充作新工作，並請交付方指明其意。相異者方為新版本，須具名其差。**

🩸 **併記（發單側之疏漏·⛔ 鑄號）**：發單側於前輪覆 KL「`-270` 照常發」而**未交代其可能已發過**
⇒ KL 之再貼係可預期之後果。**爾後發單側於交付一單時，須同格具名「此單此前是否已交付」。**

**準則 `B`（補令二 `§四-3`·`P-3` 之「留存宗」之定義）**
> **`P-3` 所稱之「留存宗」＝ <u>配地宗</u>**（分配予土地所有權人者·`推進側別 ∈ {left, right}`）。
> 抵費地係主管機關保留供標售者，其幾何要求**另有其源**，⛔ 由 `P-3` 承擔。
> 🔒 日後若須問「真實之抵費地是否亦須臨二線」，**其時方為域問題**、方上呈 KL。

**準則 `C`（補令二 `§六`·坑 `ap` 之最險處）**
> **母體之虛增會使閘看起來更強** ⇒ **凡母體之基數變大而閘轉綠者，先疑母體。**

### 六-2　🆕 本波新增之坑 `ap`〜`ar`（補令二 `§六` **全數核可**·標籤續 `ao` 之後·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `ap` | 🔴 **以量測器之<u>呼叫次數</u>充母體，而真母體為其<u>輸出</u>** | `p1` 首版以間諜所錄之 `_lot_gate` 呼叫（**`91`** 次）為母體，而留存宗實為 **`56`**（`g_rows`）⇒ 三分類之和 `91 ≠ 56`。🔴 **其最險處**：以呼叫次數為母體時，**自我驗證閘反而更好看**——判定集由 `6` 膨脹為 `18`（同一宗被二呼叫點各錄一次），而 `18/18` 逐位相同**讀起來比 `6/6` 更有力**，該 `12` 之增量卻**全是重複計數** | **母體須取自受詞之<u>定義</u>，⛔ 取自量測手段之副產物**。凡以包裹／掛鉤取數者，須**另取一個獨立之母體定義**並**出艙二數之差及其成因**。🔒 **發單側之併記**：**凡母體之基數變大而閘轉綠者，先疑母體。** |
| `aq` | 🔴 **以<u>命名慣例</u>推二集合之配對關係** | `p3` 首版假定 `verify/out/got_<X>.csv` ↔ `verify/baselines/<X>.csv`，實測 **`266` 支 baseline 中配對成功 `0` 支**（`got_G值_退縮0m.csv` vs baseline `G 值計算結果_退縮0m.csv`）。🟢 由坑 `al` 之攔法當場擋下（母體 `0` ⇒ `rc=3` loud 拒測）——**若當初寫成「跳過不存在者」，將得格差 `0` 之假綠** | **配對關係須取自<u>碼面之實際呼叫</u>，⛔ 自命名推之**；此處之單一真相源 ＝ `diff_rows(got_rows, baseline_path, …)` 之**實參** ⇒ 改以**間諜法**錄之。🔒 併：母體 `0` 一律 **loud 拒測**（坑 `al`）——本坑之所以⛔ 成災，全賴該款 |
| `ar` | 🔴 **二數相等而<u>組成完全不同</u>**（「被截斷之顯示」與「全量」之混用） | `run_verification.py:1497-1505` 只印 `viol[:12]`。自 log 取「本閘違規總計」相加得 Σ ＝ **`1656`**，**恰等於**間諜所錄之全量 **`1656`** ⇒ 讀起來像「log 已足以承載全量」。🔴 **實測：二集合之組成完全不同**——log 多含一個**非 `diff_rows`** 之閘（`13` 列）、又漏掉三個**未被截斷**之閘（`3`＋`4`＋`6` ＝ `13`）⇒ **其差恰相抵** | **凡以 log 之違規數為量者，須先問「這是<u>顯示數</u>抑或<u>總計數</u>」**；**全量須自 `diff_rows` 之<u>回傳</u>取**（間諜法），⛔ 自 log 重建。🔒 **要害：二數相等⛔ 證明其框相同**——須**逐項對拍其組成**，⛔ 以 Σ 相等推「甲可代乙」 |

### 六-3　坑之**定義處清單自證**

**器** ＝ `verify/probes/probe_WG9269_pit_index.py`（`PYTHONIOENCODING=utf-8`）
⇒ 其結果載於 `§七-3`（**數字系 `26` ＋ 字母系 `44` ＝ `70`／`70`**·判別力二造 ✅）。

### 六-4　**起手清單**（予接手窗）

1. **開窗第一動作**：`git ls-remote` 實查二 ref（**⛔ 本地追蹤 ref**·坑 `23`）；
   動主 checkout 前另驗 `left-right` ＝ `0　0`（否則 `CLAUDE.md` 係 main 態·**缺數百列規則** ⇒ 停機）。
2. **🛑 補令一（`file sha256 af2ba742d2f93a77…`）尚未入倉**——其檔在**主 checkout 之 `docs/orders/`**，
   本窗依 KL 之令**⛔ 接**。**新窗之工項零即其原封入倉**；其 `§五` 授權 **`GB-164`／`自誤 368`**。
3. **次號（本批期末實查）**：自誤 **`368`**／`GB` **`166`**（🔴 **`164` 係已授權而待新窗鑄之缺號**·
   ⛔ 由缺號集推論其「未鑄」）／`VR` **`96`**／`K-9` **`30`**；**坑之下一標籤 ＝ `as`**。
4. **未結阻塞項**：`GB-159`／`GB-160`／`GB-162`／`GB-165`（＋新窗將鑄之 `GB-164`）。
5. **🛑 二上呈項待發單側之答**（見 `§五`）：`MinA_區` `115.88` vs `CLAUDE.md` `115.85`；
   「零面積」之真值 `0.0046 ㎡`。
6. **`§六` 之預設仍在效**：一律以 `Write`／`Edit` 落文字；**⛔ heredoc**（其攔法 ＝ `wg9237_heredoc_guard`，
   前批 `3` 次活體攔截坐實其**⛔ 賴記憶**）。

---

## `§七`　自限復驗 ＋ 收工閘

### 七-1　自限復驗

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| 生產碼 `34` 檔 | 期初 ＝ 期末 | `app.py` blob `sha1` ＝ `4379108a…`（工作區 `hash-object` 同值）；`git status` 之非 `??` 列 ＝ `0` | ✅ 一字未動 |
| `verify/baselines` 樹 | ⛔ 覆寫 | 聚合 `sha256` 期初 ＝ 期末（見 `§七-2`） | ✅ |
| `p3a` 側支 | ⛔ 併線·⛔ 刪除 | `ls-remote` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`；`merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | ⛔ 設任何值 | 本批**⛔ 跑 `run_verification`**（序 `3` 之器走記憶體側）⇒ 結構上未觸 | ✅ |
| `deletions` | 逐檔 `0` | 見 `§七-2` | ✅ |

### 七-2　入倉之異動（`numstat`·刪除欄逐列須 `0`）

見 commit 之 `git show --numstat`；**Σdeletions ＝ `0`**。

### 七-3　四簿期末復查 ＋ 坑之清單自證

見 `verify/out/WG9270R2_closegate.log` 與 `verify/out/WG9270R2_pit_index.log`。

### 七-4　🔒 **本批之自解清單**（`自解款` 款 `五`：未寫入者視同未自解）

| # | 疑義逐字 | 二以上之讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | `常規四（九）二` 閘 `②`：「MAX 須由 `N-1` 轉為 `N`，且 `N` ⛔ 在缺號集」 | `(甲)` 嚴守字面 ⇒ 本批⛔ 得鑄 `GB-165`（因 `164` 未鑄）／`(乙)` 依補令二 `§七` 之明文授權鑄 `GB-165`，並具名 `164` 係「已授權而待他窗鑄」 | **`(乙)`** | 補令二 `§七` 逐字「本補令得鑄者 ＝ `GB-165` 一號」⋀「連同補令一 `§五` 所授之 `GB-164`／`自誤 368`，**尚未鑄者合計三號**」⇒ **`164` 之缺係發單側所明知且明文安排**；本批之 `GB` MAX `163 → 165` 已當場實測 | `(甲)` 將使一條**已明文授權**之鑄號⛔ 得執行，且其所守護之性質（⛔ 漏鑄）已由**具名**承擔 |
| `2` | 序 `3` 之「`MinA_區`」未具名其取得方式 | `(甲)` 取 `CLAUDE.md:408` 之 `115.85`／`(乙)` 逕呼生產側 `_mina_by_block` | **`(乙)`** | `數字錨引倉檔`⋀`⛔ 憑記憶出艙`；二值**當場並報**於 `§五-1`，⛔ 擇一頂替 | `(甲)` 係**憑文件之值**，而本批之受詞（片之分類）繫於**活體之值** |

🔒 **二者皆**：零土地後果 ⋀ 零生產碼 ⋀ 可機驗（各附實測）⋀ 可逆（append-only）⋀ 出艙具名。
🛑 **⛔ 落入黑名單任一項**（⛔ 動宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字；
二讀法之後果在土地上⛔ 相異；⛔ 新訂準據；⛔ 放寬任何既有之閘）。

---

## `§八`　🛑 **停**

KL 序改令序 `5` 逐字：「收工交接（交接註 ＋ 坑之定義處清單自證 ＋ 起手清單）⇒ **停**」。
🔒 **`GB-159` 仍有效**：受影響之宗之**配地圖與負擔表⛔ 對外**。
🛑 **待發單側之答 ＝ `§五` 之二上呈項**；**待新窗之工項 ＝ 補令一之全部**。
