# `W-G.9-264R`　🛑 **停機上呈**：開工閘 `5`（`S-0b` 之 `P-5`）不符

> 🛑 **本批停於開工閘**——依單 `§零` 標頭逐字「**逐條當場復現·任一不符 ⇒ 停機上呈**」
> 與 `§九 5` 逐字「**CC 停機並回報，恆為正確程序。⛔ 自行放寬判準而呈上。**」
> ⇒ **`§二`〜`§七` 之六個工項<u>一律未辦</u>**（`工項零` 除外——其為 `追加四` 款一所令之第一工項，且其自身通過）。
> 🔒 **零生產碼**：`app.py` 與 `verify/` **一檔未動**（`V-g` 之實測見 `§五`）。
> 🔒 **⛔ 含任何 `app.py` diff、⛔ 含任何修法主張**（單 `§十 f`）。
> 🔒 **⛔ 以行號（含 log 列號）為錨**（單 `§十` 之行號衛生款）。

---

## §零　新單之發現處（🔴 **第八度**）

本單於**主 checkout 工作區** `…/land-readjustment-trial/docs/orders/` 發現，**本 worktree 之倉內查無**
⇒ 記憶所載「開窗第一動作 `④`」之形**第八度**再現。
`docs/` 全域差集（主 ∖ 本）除本單外僅二 `__pycache__/*.pyc` ⇒ **⛔ 有他處之單**。

---

## §一　開工閘（`§零`）之逐條實測

| 閘 | 單所載期望 | 實測 | 判 |
|---|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `2d1eaf47c326fd0209270c7aa6210d62e6870305` | `ls-remote` 得**同值**；本地 `HEAD` 同 | ✅ |
| `2` 受詞 | `app.py` `1e15d45ae84b02ca`／`stepg` `cb0bbaaa0f1eb8bc` | **逐位相符**（十檔全表見 `§一-1`）| ✅ |
| `3` 號占用 | `264` ＝ `0/0/0`；`263` ＝ `D1 2`／`D2 2`／`D3 1`／列框 `3`／檔框 `2` | **逐位相符**（`§一-2`）| ✅ |
| `4` 次號 | 三簿前值 ＝ 自誤 `324`／`GB 154`／`K-9 29` | **逐位相符**（`§一-3`）| ✅ |
| **`5`** | `probe_order_preflight.py` **`rc=0`**、`P-5` 與 `§末` `SELF_SHA256` **相符** | **`rc = 1`**；`P-5` **🔴 不符** | 🔴 **不符 ⇒ 停機** |

### 一-1　閘 `2`：十檔之倉側 blob（**正面列舉**·來源層 ＝ **倉 blob**·`git cat-file`）

| 檔 | `sha256` 前 `16` |
|---|---|
| `app.py` | `1e15d45ae84b02ca` |
| `verify/stepg_pipeline.py` | `cb0bbaaa0f1eb8bc` |
| `verify/run_all.py` | `4ba89fef90979491` |
| `verify/run_verification.py` | `d0d531f7dc051b65` |
| `verify/wf_f0.py` | `b0722355c1978635` |
| `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` |
| `CLAUDE.md` | `7c25e981e5004fbe` |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `7a0edfe53d65790a` |

### 一-2　閘 `3`：號占用（宣告框 `①`–`⑦`·**入倉前**）

**三軸**：來源 ＝ `blob@2d1eaf47c326fd0209270c7aa6210d62e6870305`（**倉 blob 層**）／
母體 ＝ **全 `docs/` `615` 檔**（單載 `615` ✅）／粒度框 ＝ **檔框 ＋ 列框**。
**讀不到 `19` 檔 ＝ `jpg 9` ＋ `png 10`**（單載同）；可讀 `596` 檔。
**器自檢**：含字面反斜線之路徑 `0`／含 CJK 之路徑 `594`（`> 0` ⇒ 該檢有判別力）。

| 號 | `D1` | `D2` | `D3` | 雙屬 | 列框 | 檔框 | 單載 | 判 |
|---|---|---|---|---|---|---|---|---|
| `W-G.9-264` | `0` | `0` | `0` | `0` | `0` | `0` | `0/0/0` | ✅ **未占用** |
| `W-G.9-263` | `2` | `2` | `1` | `0` | `3` | `2` | 同 | ✅ 對照組甲（`≥ 1`）|
| 一人造號（字樣**⛔ 入倉**·`GB-147` 代稱·執行期組出）| `0` | `0` | `0` | `0` | `0` | `0` | `0` | ✅ 對照組乙 |

🔒 **框之來源 ＝ 正典逐字**（`CLAUDE.md` 之宣告框 `①`–`⑦`）**⛔ 自擬**、⛔ 以單載值為停止條件
（`自誤 325` 之通則·本報告先行遵之）。

### 一-3　閘 `4`：三簿前值

| 簿 | 母體（來源層 ＝ 倉 blob）| 框之來源 | 相異 | `MAX` | `MIN` | 缺號集 | 恆等式 |
|---|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔 | **正典逐字**（`VR-091 四` 定義框 ∪ 範圍框·**展開**）| `317` | **`324`** | `7` | `[106]` | `318 == 317 + 1` **`True`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔 | 標題錨定框 | `68` | **`154`** | — | — | — |
| `K-9` | `docs/rulings/K-6_…md` 單檔 | 正典定義列式 | `24` | **`29`** | — | — | — |

⇒ 三簿前值**逐位符單載**。**本批未鑄任何號**（停機 ⇒ `自誤 325`／`GB-155` **皆未落地**）。

---

## §二　🔴 閘 `5` 之不符：**逐框之機械分析**

### 二-1　工具跑之三欄（單 `§十 a`）

| 跑 | `rc` | 逐字結論列 | `stderr` |
|---|---|---|---|
| `probe_order_preflight.py` ＠ **`-264`** | **`1`** | `[P-5] SELF_SHA256 🔴 **不符**（取檔內**最末**一個·受詞 18149 B）｜實算 90c3711a5c73d2cd…／單載 0429b3a1546f043f…` | **`0` B** |
| 同上 ＠ `-263`（**判別力對照**）| **`0`** | `── 🔴 機械·停機款：0 項 ──` | `0` B |

⇒ 🔒 **該 probe ⛔ 恆紅**（同工具、同框，對 `-263` 得 `0`）。

### 二-2　六框並列之獨立復算（**來源層 ＝ 工作副本**·全檔 `18272` B／`LF 253`／`CR 0`）

單之 `SELF_SHA256` 列（檔內最末一個）逐字：
`SELF_SHA256（＝ 本行以上全文之 sha256sum 值）: 0429b3a1546f043f45b94ac5996d0e3c586c9909e391c36a95f3f3e2a368ef52`
其起始 byte offset ＝ `18149`。

| 框 | 受詞長度 | `sha256` 前 `24` | 判 |
|---|---|---|---|
| 框 `1`　`[0, off)` ＝ 自檔首至該列**前一個 `\n`（含）** ← **`P-5` 之受詞** | `18149` B | `90c3711a5c73d2cdcdbc9603…` | 🔴 不符 |
| **框 `2`　`[0, off−1)` ＝ 同上但<u>不含</u>該 `\n`** | **`18148` B** | **`0429b3a1546f043f45b94ac5…`** | ✅ **命中單載** |
| 框 `3`　全檔 | `18272` B | `f3897326198dba28c33c328b…` | 🔴 |
| 框 `4`　全檔去末尾 `\n` | `18271` B | `756af6cf04b1ffd7cbf1db84…` | 🔴 |
| 框 `5`　框 `1` 去尾部全部空白 | `18147` B | `73f2bb960b9a2129451c6522…` | 🔴 |
| 框 `6`　框 `5` 補一 `\n` | `18148` B | `0429b3a1546f043f45b94ac5…` | ✅（與框 `2` 同值·同一 bytes）|

🔒 **判**：**不符係<u>框之別</u>，⛔ 內容之別**。
單所載之 `0429b3a1…` **在框 `2` 下逐位命中其自身內容** ⇒ **該單之內容完整、未遭截斷或竄改**。
🔒 **二框之差 ＝ 恰一個 `\n`**（`18149` vs `18148`）。

### 二-3　判別力（證框 `1` 本身無誤）

對 **`W-G.9-263`** 施**同一框 `1`**：實算 `0135ece2c6f0a1cff7211970…` ／ 自載 `0135ece2c6f0a1cff7211970…`
⇒ **✅ 相符** ⇒ 🔒 **`P-5` 之框對既有之單為真** ⇒ 本次之差**⛔ 出於 `P-5`**。

### 二-4　傳輸保真**未破**（⛔ 與閘 `5` 之不符混同）

| 層 | 值 |
|---|---|
| 來源（主 checkout）`sha256` | `f3897326198dba28c33c328b6942490a1d8181ad4ccd06025829e22d1bffb49d` |
| 落檔（本 worktree 工作副本）`sha256` | **同上** |
| 入倉 blob `sha256` | **同上** |

⇒ 🔒 **三者逐位相符** ⇒ 「發單側之檔 → CC 之倉」之保真**成立**；
破者僅為「單自載之 `SELF_SHA256` 與 `P-5` 之框**不同框**」。

### 二-5　🛑 待發單側裁之點（**⛔ CC 自裁**）

單自載之定義逐字 ＝ 「`SELF_SHA256`（**＝ 本行以上全文之 `sha256sum` 值**）」。
🔒 **「本行以上全文」於字面上⛔ 界定該分隔 `\n` 是否計入** ⇒ 二框皆為其合理讀法。
🛑 **是否 (a) 以框 `2` 為準而更正 `P-5` 之框、(b) 以框 `1` 為準而由發單側重算單之 `SELF_SHA256`、
或 (c) 判為框之別而放行本批 ＝ <u>意思決定</u>** ⇒ **候發單側**。
**本報告⛔ 就此作成任何條文、⛔ 放寬閘 `5`、⛔ 更動 `P-5` 或本單任一字。**

---

## §三　工項零（**已完成**·`追加四` 款一之第一工項）

| 項 | 值 |
|---|---|
| 落檔 | `docs/orders/W-G.9-264_V1prime之立_角退判準登記_GB155_自誤325.md` |
| **【工作副本層】** | `18272` B ／ `LF 253` ／ **`CR 0`** |
| **【blob 層】** | **`18272` B** ⇒ 二層相等 ⇒ **無 CRLF 正規化** |
| 來源／落檔／入倉 blob `sha256` | 三者逐位相符（見 `§二-4`）|

🔑 **層之具名（單 `§一` 之硬性款）已遵行**——上表逐列標明其為 **blob 層**抑或**工作副本層**。
🩸 **併記（承 `-263R` 之待加註項）**：`-263R` 所載之 `661258`／`665761` 係**工作副本層**（CRLF）之長度，
而同報告工項零段之 `B` 係 **blob 層** ⇒ 該報告內「B」確指二層。
🛑 **本報告⛔ 追改 `-263R` 一字**；單 `§六` 所令之二則加註**屬工項五、本批未辦**（見 `§四`）。

---

## §四　未辦之工項（**逐項具名·⛔ 以「未查」了事**）

| 工項 | 單之節 | 狀態 | 阻斷點 |
|---|---|---|---|
| 工項一　鑄 `自誤 325` | `§二` | ⬜ **未辦** | 開工閘 `5` 不符 ⇒ `§零` 令停機 |
| 工項二　`V-1′` 之立（含 `§三 a` 之二態否證量測）| `§三` | ⬜ **未辦** | 同上 |
| 工項三　鑄 `GB-155` ＋ `Q-a`〜`Q-d` | `§四` | ⬜ **未辦** | 同上 |
| 工項四　角退判準登記 ＋ `Q-e`〜`Q-h` | `§五` | ⬜ **未辦** | 同上 |
| 工項五　`-263R` 之二則就地加註 | `§六` | ⬜ **未辦** | 同上 |
| 工項六　唯讀探針入倉 | `§七` | ⬜ **未辦** | 同上 |

🔒 **⛔ 部分辦理**——`§零` 之停機令未分工項；且工項二／三／四之答皆須**二態實跑**，
其結論若於閘未解時出艙，將與「本批之受詞是否為發單側所送之單」耦合。

---

## §五　【驗收】（**本批可判者**）

| 閘 | 判準 | 實測 | 判 |
|---|---|---|---|
| `V-a` | `§零` 閘 `3` 三列由 CC 自測復現；對照甲 `≥ 1`、對照乙 `= 0` | 見 `§一-2`：甲 檔 `2`／列 `3`；乙 `0`／`0` | ✅ |
| `V-g` | 本批 `app.py` 與 `verify/` **生產路徑** `deletions ＋ insertions ＝ 0` | `git status --porcelain` 於 `app.py`／`verify/` 之命中 ＝ **`0`**；十檔 blob 與 `HEAD` 逐位相同 | ✅ |
| `V-h` | 三簿末值 ＝ 自誤 `325`／`GB 155`／`K-9 29` | 🔴 **未達·因停機而未鑄** ⇒ 末值 ＝ **前值**（自誤 `324`／`GB 154`／`K-9 29`）；缺號集 `[106]` 不變、恆等式 `True` | ⬜ **不適用（未辦）** |
| `V-b`／`V-c`／`V-d`／`V-e`／`V-f` | — | ⬜ **未跑**（其受詞為未辦之工項）| ⬜ |

---

## §六　出艙

- 本批 commit **全批零生產碼** ⇒ 依 `常規一` 補款 `🔧 一` 逕推主線（**工項零之入倉 ＋ 本停機報告**）。
- 🛑 **停機候發單側**就 `§二-5` 之三選一裁示；**⛔ 逕辦 `§二`〜`§七` 任一工項**、
  ⛔ 更動 `P-5`、⛔ 更動本單、⛔ 放寬閘 `5`。
- 🔒 **`§十` 出艙形之遵行**：`a` 三欄見 `§二-1`；`c` 各數之來源層已逐處標明；
  `d` 框之來源已逐處載明（正典逐字／自擬）；`e` 未辦者之阻斷點見 `§四`；
  `f` **本報告⛔ 含任何 `app.py` diff、⛔ 含任何修法主張**。

---

## §七　`W-G.9-264 補令一` 之執行（⛔ 上文原句一字不刪·純末端追加）

> 🛑 **本節⛔ 追改上文任一字**（`常規四（九）五`）——上文之停機記述係**當時之態**，其判由補令一 `二` 全數採認。
> 🔒 **零生產碼**：`app.py` 與 `verify/` **生產路徑**之 `insertions ＋ deletions ＝ 0`（`V-g` 實測見 `§七-9`）。
> 🔒 **⛔ 含任何 `app.py` diff、⛔ 含任何修法主張**（單 `§十 f`）。⛔ 以行號（含 log 列號）為錨。

### 七-1　閘 `5` 之復算（補令一 `三` 所令·**⛔ 採信補令所載之任一值**）

**框之來源 ＝ 正典逐字**：`verify/probes/probe_order_preflight.py` 之 `P-5`，內容錨二項於該檔**各命中 `1`**
（`ls = raw.splitlines(keepends=True)` ／ `got = hashlib.sha256(b"".join(ls[:i])).hexdigest()`）；
對照乙（一必不存在之同族字樣·執行期組出）命中 **`0`**。

| 單 | 受詞長度（**倉 blob 層**）| `P-5` 實算 | 判 |
|---|---|---|---|
| **`-264`** | `18149` B | **`90c3711a5c73d2cdcdbc96034f59ce20c2027e7e512a4197ed7d93e48438ea1a`** | ✅ **與補令一 `三` 所載逐位相符** |
| `-263`（判別力）| `9398` B | `0135ece2c6f0a1cf…` ＝ 其自載 | ✅ 器能亮綠 |

🔒 **器非恆真亦非恆假**（`-263` 綠／`-264` 紅）⇒ **閘 `5` 過**。

### 七-2　工項一 ＋ 補令一 `四`：`自誤 325`／`326` 落簿

**框之來源 ＝ 正典逐字**（`VR-091 四` 之定義框 ∪ 範圍框·**展開**）；母體 ＝ 登記表**單檔**。
payload 依 `常規七` **自倉內二檔機械抽取**（⛔ 憑記憶重寫）：`325` 自 `-264 §二` 之 blockquote
（起錨命中 `1`·迄錨逐字 `---`·抽得 `14` 列）；`326` 自補令一 `四` 之四空格縮排段
（起錨命中 `1`·迄錨逐字 `五　閘 5 過後…`·抽得 `20` 列）；**非該前綴且非空白之列 ＝ `0`**。
標題形取**既有最末一則**（`自誤 324`）為範本（`常規四（九）四`）。

**檔末追加之機驗**（來源層 ＝ **工作副本層**·該檔以 CRLF 呈現）：
改前 `665761` B → 改後 `669094` B（**新增 `3333` B／`38` 列**）；**嚴格前綴 `True`** ⇒ append-only。

### 七-3　工項二 `a`：否證量測之獨立復現（⛔ 採信單載）

**二態**：改前 ＝ `70ce7d46b27a1c9f5fd057f697ba3b7f382af7ad`（`app.py` `4860f10828fb8ae9`）／
改後 ＝ `e9d1b9c3e6e46580da28392e1ed7495d5b34638f`（`app.py` `1e15d45ae84b02ca`）；二者皆**原態**（⛔ 注入）。
**來源層 ＝ harness 落檔** `got_G值_退縮3.5m_partial.csv`。**受詞** ＝ `3.5m·R2 left` 之**非鏈頭六宗**。

| 宗 | `ΔS` | `Δ宗地寬度` | `cut_coords` 逐位同 | 質心位移 `m` | `ΔG ㎡` |
|---|---|---|---|---|---|
| `628-41(1)` | `0.0000000000` | `0.0000000000` | 否 | **`0.1067`** | `+0.06` |
| `628-27(1)` | `0.0000000000` | `0.0000000000` | 否 | **`0.1074`** | `+0.00` |
| `628-40(1)+` | `0.0000000000` | `0.0000000000` | 否 | **`0.1096`** | `+0.19` |
| `628-30(2)` | `0.0000000000` | `0.0000000000` | 否 | **`0.1117`** | `+0.00` |
| `628-39(1)` | `0.0000000000` | `0.0000000000` | 否 | **`0.1117`** | `+0.00` |
| `628-31(2)` | `0.0000000000` | `0.0000000000` | 否 | **`0.1120`** | `+0.00` |
| **合計** | 十位皆零·六宗全數 | 十位皆零·六宗全數 | **`0 / 6`** | `0.1067`–`0.1120` | `+0.19`／`+0.06`／餘四宗 `< 0.005` |

⇒ 🔒 **五量逐項與單 `§三 a` 所載相符**。鏈頭 `628-42(1)`：`ΔS ＝ +0.1100`／`Δ寬度 ＝ +0.1100`／
`ΔG ＝ +4.77`；鏈序（宗之排列）**改前改後逐位相同**。

🔑 **本批另得一項單所未列之量（同一器·同一二態）**：以最大面積之非鏈頭宗定單位向量後，
**六宗之質心位移於該向量上之垂距分量<u>全為 `0.0000`</u>** ⇒ **六宗之位移共線且同向**
⇒ **純平移之直接證據**（單 `§三 a` 之「非鏈頭整排沿推進向平移」由此得機械佐證）。

🩸 **CC 側器紅一度（入倉前自捕·照實）**：本窗首版之質心以**自寫之 shoelace 式**計，得
`0.1929`–`2.4895`；依「外部重建須設自我驗證閘」以 **`shapely`（生產路徑同源庫）**復算，
**`14 / 14` 逐筆不符** ⇒ 判**自寫式紅**、採 `shapely` 之值（即上表）。
🔑 **⛔ 該閘，此紅不可見**——自寫式之輸出「面積 ≈ `G`、皆四頂點」**內部自洽**，
其唯一外顯症狀是與單載之範圍相斥。

### 七-4　🛑 工項二 `b`：**停機上呈**（單 `§九 2`·⛔ 自裁）

單 `§三 b` 令 `V-1′` 落於 `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末，並逐字附
「若家法要求閘定義另有落點，CC **停機上呈**其依據，⛔ 自裁」。

**CC 所查得之依據（二檔各自之射程宣告·逐字·來源層 ＝ 倉 blob）**

| 檔 | 其檔首之射程宣告（逐字節略）| 與 `V-1′` 之關係 |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 「本表所列係**『本案（UC9898）不觸發、但換一個案子會硬停或算錯』之已知結構性阻塞**」；判準「**入本表 ＝ 換案即硬停／錯值**」 | 🔴 **`V-1′` 係<u>驗收閘之定義</u>、⛔ 阻塞項** ⇒ ⛔ 合其判準 |
| `docs/驗證裁定登記表.md` | 「🔒 **本檔之範圍 ＝ 驗證紀律層之裁定**（施工單方／`claude.ai` 所下、關於**如何驗證**之裁定）」 | 🟢 **`V-1′` 正屬「關於如何驗證之裁定」** |

🔒 **`CLAUDE.md` 對「閘之定義落點」之直接命中 ＝ `0`**（框：`閘之定義` ／ `驗收閘.*落`）
⇒ **無明文家法**，惟**二登記表各自之射程宣告指向相異之落點**。
🛑 **併記一連帶**：落於 `驗證裁定登記表` 須鑄一 **`VR`** 號，而本單 `§零` 閘 `4` 與補令一 `五`
所授權之鑄號**僅 `自誤 325`／`326` 與 `GB-155`**——**`VR` ⛔ 在授權之內**。

⇒ 🛑 **本工項未辦，停機上呈**。**⛔ 自裁落點、⛔ 鑄 `VR`、⛔ 將 `V-1′` 逕落於任一檔。**
🔒 **工項二 `a`（否證量測）已辦畢**（`§七-3`）——其為 `V-1′` 之**證據**，⛔ 其**落地**。

### 七-5　工項三：`GB-155` ＋ `Q-a`〜`Q-d`

落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末追加（**⛔ 鑄 `GB` 以外之號**）。
🩸 **標題形之偏離（`常規二`·同形第二度）**：單 `§四` 所給之 `## 🆕 \`GB-155\`　…`
經機驗**⛔ 可被 `GB` 之定義列式命中**（該形命中 `0`／既例之形命中 `1`）⇒ 依 `常規四（九）四／二`
改採既例形 `### \`GB-155\` 🆕　…`（範本 ＝ `GB-154`）。**⛔ 改單一字**；逐字回報。

四答之要旨（全文見登記表檔末）：
`Q-a` **復現**（`3.5m R4 left` 之 `b0` 與 `R2` 逐位同 `6.980480447374` 而 `buf` 相異）／
`Q-b` **射程 ＝ 僅 `R4 left` 二情境**（全 `24` 組中 `forced` 者 `3`、`b0 ≠ 0` 者 `2`；`R1`／`R3`／`R5`／`R6` 之 `_fo_*` 皆 `False` ⇒ 其 `b0` 由式中 `else 0.0` 當場賦值·⛔ 受殘值影響）／
`Q-c` **⛔ 依賴**（二項機械證據）⇒ 單 `§九 1` 之停機款**未觸發**／
`Q-d` **觸發條件** ＝ `if _degenerate_order or _N <= 1:`（內容錨於二檔各命中 `1`·判別力寬字樣各 `7`）。

### 七-6　工項四：角退判準之登記 ＋ `Q-e`〜`Q-h`

落點 ＝ 同檔檔末追加，以 **`GB-67` 之補款** ＋ **`GB-154` 之補款**二節（⛔ 動二者原文一字、⛔ 鑄新號）。
`Q-e` **全 `13` 組獨立復現**（角退二組 `0m R2 left −0.95／45.9766`・`3.5m R5 left −1.29／56.8186`；
二值與二中止閘**逐位相同**·差 `0.0000`）／`Q-f` 判 **①**（於現可達母體雙向蘊含成立·⛔ 反例）／
`Q-g` **`R3` 於原態不可得**·阻斷點逐字具名（併記一**條件式**之閘關觀測，且已具名其**⛔ 得替代原態**）／
`Q-h` 判 **結論仍足、舊二項證據不足** ⇒ 以本批新得之**反向反例**（`3.5m R5 left` 有 `②-宗` 破而無 `telescoping` 破）為新載體。

### 七-7　工項五：`-263R` 之二則就地加註

落點 ＝ `docs/reports/W-G.9-263R_交接文入倉與自誤322至324.md` 檔末追加（⛔ 鑄號）。
加註一之四數已由 CC 自倉 blob 獨立復算並與單 `§六` 逐位相符
（`e9d1b9c` `653571`/`7687`/`0`/`661258`；`2bc0429` `658030`/`7731`/`0`/`665761`）。
🩸 **併就受詞之實況作一更正**：單 `§六` 之標題為「位元組數之層**未具名**」，而 `-263R` 該二列
**已逐字載「（工作區 CRLF）」** ⇒ 實際之缺為「**同一報告內『B』指二層且未互相對照**」——已逐字載於加註內。

### 七-8　工項六：唯讀探針入倉

`verify/probes/probe_corner_retro_overlap.py`（**新檔·`177` 列·唯讀**）。
`py_compile` 過；只 `import` `ast`／`csv`／`io`／`itertools`／`os`／`sys`／`shapely.geometry`
——**⛔ import 生產路徑之寫入面**。
**陰性對照**（街廓名於**執行期組出**·⛔ 使其字面落入該檔）：輸出 **`0` 列** ✅。
**`git status --porcelain` 之復現**：於實跑 `run_verification.py` 之拋棄式 clone 內
全部 `porcelain` ＝ **`2`** 項，二項皆為**本窗自建之重導 log**（`verify/out/p2.log`／`p2.err`）；
**扣除該二者後 ＝ `0`** ⇒ 落檔確為 ignored（`git check-ignore` 逐字回 `.gitignore:30: verify/out/got_*`）
⇒ 🔒 **單所令之 `0` 復現**（其差額之唯一來源已具名）。

### 七-9　【驗收】（單 `§八`·一字不變；`V-h` 依補令一 `五` 之末值）

| 閘 | 實測 | 判 |
|---|---|---|
| `V-a` | `§零` 閘 `3` 三列自測復現：`264` `0/0/0`／對照甲 `263` `D1 2`・`D2 2`・`D3 1`・列框 `3`・檔框 `2`（`≥ 1`）／對照乙 `0` | ✅ |
| `V-b` | `自誤 325`／`326` 之標題被該檔現行定義框命中（`:7735`／`:7750`）；未鑄之 `327` 命中 **`0`** | ✅ |
| `V-c` | 五量二態獨立復現；`Δ宗地寬度`／`ΔS` **十位皆零**且 `cut_coords` 逐位相同數 ＝ **`0/6`**（否證對照：得 `6/6` ⇒ 器讀錯態——未發生） | ✅ |
| `V-d` | 🔴 **不適用**——其受詞為 `V-1′` 之 `(5)`，而 `V-1′` 因 `§七-4` 之停機而**未立** | ⬜ **未跑（具名）** |
| `V-e` | `13` 組全數復現；二非零組與二中止閘**逐位相同**（差 `0.0000 < 1e-6`）；否證對照「`13` 組全非零」**未發生**（`11` 組得 `0`） | ✅ |
| `V-f` | 三 clone root 長度 `5`／`24`／`62` ⇒ `RESULT` 列序 `312`／`312`／**`311`**（**相異**）而三者落檔 `sha256` **逐位相同**。🩸 首二長度皆得 `312` ⇒ 依其否證對照逐字**重設**，第三次方跨換行閾 | ✅ |
| `V-g` | `app.py` 與 `verify/` **生產路徑**之 `insertions ＋ deletions ＝ 0`（四檔命中 `0`；`verify/` 下唯一異動為新探針·另列 `177/0`）；十一檔 blob 前後**逐位不變** | ✅ |
| `V-h` | 三簿末值 ＝ 自誤 **`326`**（相異 `319`・`MIN 7`・缺號 **`[106]`** 不變・恆等式 `320 == 319 + 1` **`True`**）／`GB` **`155`**（相異 `69`）／`K-9` **`29`** | ✅ |

**逐工具跑之三欄（單 `§十 a`）**

| 跑 | `rc` | 逐字結論列 | `stderr` |
|---|---|---|---|
| `probe_order_preflight.py` ＠ `-264`（**閘 5 復算前**）| `1` | `[P-5] SELF_SHA256 🔴 **不符**…實算 90c3711a5c73d2cd…／單載 0429b3a1546f043f…` | `0` B |
| 同 ＠ `-263`（判別力）| `0` | `── 🔴 機械·停機款：0 項 ──` | `0` B |
| `run_verification.py` ＠ `70ce7d4`（改前）| `1` | `RESULT: FAIL` | `0` B |
| `run_verification.py` ＠ `e9d1b9c`（改後·短路徑）| `1` | `RESULT: FAIL` | `0` B |
| `run_verification.py` ＠ `e9d1b9c`（改後·長路徑）| `1` | `RESULT: FAIL` | `0` B |
| `run_verification.py` ＠ `2d1eaf4`（**閘關**·注入探針）| `1` | `RESULT: FAIL`（二情境皆跑完六街廓·中止街廓 `None`）| `0` B |
| `probe_corner_retro_overlap.py` ＠ 原態落檔 | `0` | `組數 = 13 ／ 重疊非零之組數 = 2` | `0` B |
| `py_compile` ＠ 新探針 | `0` | `py_compile OK` | `0` B |

**計數之第二態同式結果（單 `§十 b`）**：`run_verification.py` 之紅項數
`grep -c "🔴 FAIL"` 於 `8fcd472` 與 `2d1eaf4` **皆得 `24`**，且**名目差集雙向皆空·共同 `24`**；
對照甲 `grep -c "✅"` ＝ `16`（`> 0`）／對照乙（代稱）＝ `0`。

### 七-10　出艙

- 本節之全部 commit **零生產碼**（逐 commit 之判見 commit message）⇒ 依 `常規一` 補款 `🔧 一` 逕推主線。
- 🛑 **工項二 `b` 停機上呈**（`§七-4`）——候發單側裁 `V-1′` 之落點；**⛔ 逕落、⛔ 鑄 `VR`**。
- 🛑 **⛔ 逕啟 `R4`–`R6`／`②-宗@R5`／`GB-67` 之修／`_b_L0` 之修 任一工項**（單 `§六` 明令·屬新窗）。
