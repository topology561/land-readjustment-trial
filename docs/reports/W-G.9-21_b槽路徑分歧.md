# 【W-G.9-21 全單報告】🛑 `N-1` 觸發：同一個「起算點」，生產路徑上**同時有三個答案**

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`c55c1ec`　**事前登記**：`docs/reports/W-G.9-21_前置登記.md`（commit `7c80fe1`）
**證據**：`verify/out/probe_WG921_b_slot_divergence.log`（commit `a331c3c`）
**字樣錨（倉規 A）**：`【W-G.9-21 全單報告】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行
　🔒 判別力自檢：改用寬字樣 `W-G.9-21` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> # 🛑 **停機碼 `N-1` 觸發——上呈 KL**
>
> **「側街負擔從哪裡起算」這一題，生產路徑上<u>同時有三個不同的答案在跑</u>。**
>
> ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 未觸；全程**只讀**。
> ⛔ **未估上限**（施工單 §1 已裁押後）、⛔ 未取任何代表值。

---

## §一 🔴 甲組：三個答案，兩條路徑，一道破掉的閘

### 一-1 構造判定（🔒 **由 AST 之祖先鏈證**·⛔ 非檔名／註解）

| 路徑 | 落點 | 祖先鏈（節錄） |
|---|---|---|
| **A** | `app.py` | `def main` → `elif selected_tab == TAB_BLOCK_INTERACT:` → `if temp_parcels:` → `if not build_parcels:` → **`if _btn_clicked or _auto_recalc:`** |
| **B** | `verify/stepg_pipeline.py` | **`def run_step_g`** → `if _degenerate_order or _N <= 1:` |

🔒 **判別力自檢**：對**已知答案**之第三符號（`rw_from_width`·已知為 **module 級**）
施同一判法 ⇒ 鏈 ＝ `['rw_from_width']`、**不含 `main`** ⇒ ✅ 判法得出已知結果。

### 一-2 UI 入口（🔴 **兩條皆在生產路徑，且為<u>接力</u>**）

`B` 之入口 ＝ `if st.button("🔷 執行七級調配（wf_f0 → f4 全鏈）", key="wg_run_seven"…)`
→ `_build_wf_ctx(_wg_ss, _tag)` → `wf_f0`…`wf_f4` → `run_step_g`。

其**外層守衛**為 `if not _wg_ss.get('f3_G_values'):` ／ `elif not _is_uc9898(…)`
⇒ 🔴 **`B` 以 `A` 之產物 `f3_G_values` 為前置**。

⇒ **二者同在 `main()` 之<u>同一個分頁</u>（🗂️ 街廓互動分析）分支內，先 A 後 B。**
⛔ **不是「一條是測試、一條是生產」。**

### 一-3 🔴 三個答案

| 誰 | 「起算點」取什麼 | 自洽？ |
|---|---|---|
| **A 之選槽** `'b'` | `buf · _cos_dn` | ✅ **A 自洽** |
| **A 之推進種子** `_W_prev_*` | `buf · _cos_dn` | （同上） |
| **B 之選槽** `'b'` | `_mp_base_W0(群起點, buf)` ＝ `buf · _cos_dn + W_0` | 🔴 **B 不自洽** |
| **B 之推進種子** `_W_prev_*` | **硬 `0.0`** | （同上） |

🔴 **`B` 自身之不自洽<u>就是</u>結構閘 `理論＝實跑` 破掉的那件事**
（`W-G.9-14` 已分解：起點項 **+2.8167 百分點**）。

---

## §二 逐格二值與差（🔒 **驗收 #4 咬到我上一批**）

### 二-1 🔴 這些值**早就在倉裡**

`verify/out/probe_WG91_buf_metric_and_candidates.log` 之 `Q2` 表**已含全 16 格**，
欄名**逐字寫著**「族②③現行 ＝ `buf·cos_dn`」「族④現行 ＝ `_mp_base_W0(群起點, buf)`」。

🔴 ⇒ **`W-G.9-20` 之「本批未量得下界之值」為<u>過早之宣稱</u>**——我以「這次沒跑到」
替代了「倉裡沒有」。⇒ 已立**考古節 77**。
🔒 依驗收 #11：**⛔ 未重跑產生該 log 之工具**，只讀其表。

### 二-2 逐格表（鍵＝**街廓側字面**·單位 **公尺**）

🔒 **解析所得 ＝ 16 格**（母體＝該 log 之 `Q2` 表·判準＝行首符 `情境 街廓 側別` 且含 ≥4 個小數），
其中標 `← forced` 者 ＝ **3 格**。

| 鍵 | `B − A`（＝ `W_0`·**與 `buf` 無關**） | `forced` |
|---|---|---|
| `R1/left` | `+0.161877` | |
| `R1/right` | `+0.200569` | |
| **`R2/left`** | **`−1.693924`** | ✅ |
| **`R3/right`** | **`+1.691801`** | ✅ |
| `R4/left` | `−0.787529` | |
| `R4/right` | `+0.651126` | |
| **`R5/left`** | **`−1.640252`** | ✅ |
| `R6/right` | `+0.000001` | |

🔒 **自我驗證閘**（兩端至少一端來自受詞之外·驗收 #8）：
`B − A` 應**與 `buf` 無關**（∵ `_mp_base_W0(gs, buf) = buf·cos_dn + W_0`）
⇒ 同一格之 `0m` 與 `3.5m`（`buf` 不同）之 `B−A` **逐格最大差 ＝ `0.000001`** ⇒ ✅ 恆等成立。
🔒 **判別力自檢**：同式改比**不同格**（`R1/left` vs `R3/right`）⇒ 差 ＝ `1.529925` ⇒ ✅ 該閘**非恆過**。

### 二-3 🔴 射程：**13 格不是生產實值**

`W-G.9-1` 之表對全 16 格皆代入 `buf`（其自陳「⛔ 含不觸發格與退化格」），
而生產側 `_left_buffer_S` **僅在 `forced_offset` 為真時非零**
⇒ **非 `forced` 之 13 格，生產實值為 `buf = 0`** ⇒ ⛔ **該 13 列不得充作生產差異**。

### 二-4 百分點欄（⛔ **與公尺分欄**·**生產實值**）

`R` ＝ 生產側 `rw_from_width`（⛔ 未自行實作替代）。
🔒 判別力自檢：`R(18.0)` ＝ `100.0000`／`R(0.0)` ＝ `0.0000` ⇒ 該函式**非常數**。

| 鍵 | 情境 | 狀態 | `b_A`（生產） | `b_B`（生產） | `R(b_B) − R(b_A)` |
|---|---|---|---|---|---|
| **`R2/left`** | `3.5m` | **forced** | `8.6742` | `6.9803` | **`−9.0183`** |
| **`R3/right`** | `3.5m` | **forced** | `8.6655` | `10.3573` | **`+8.1830`** |
| **`R5/left`** | `3.5m` | **forced** | `8.6215` | `6.9812` | **`−8.7387`** |
| `R3/right` | `0m` | 非 forced | `0.0000` | `1.6918` | `+22.2426` |
| `R4/right` | 兩情境 | 非 forced | `0.0000` | `0.6511` | `+11.3296` |
| `R1/right` | 兩情境 | 非 forced | `0.0000` | `0.2006` | `+3.4899` |
| `R1/left` | 兩情境 | 非 forced | `0.0000` | `0.1619` | `+2.8167` |
| 其餘 5 列 | — | — | — | — | `0.0000`（**兩義·見下**） |

🔒 **帶號合計 ＝ `+47.9409` 百分點**｜**絕對值合計 ＝ `+83.4549` 百分點**（倉規·並列）。

⚠️ 🔴 **上表之 `0.0000` <u>有兩義</u>**（⛔ 不得一律讀成「無分歧」）：
· `R6/right`：`W_0 ≈ 0` ⇒ **真的無分歧**
· `R2/left`／`R4/left`／`R5/left`（非 forced 情境）：`W_0 < 0` ⇒ `R(負) = 0` 且 `R(0) = 0`
　⇒ **兩端同被地板夾成 0** ⇒ 🔴 **分歧仍在**，只是**在起算項這一欄看不見**
　　——它會經**終點項** `R(b + Σw_群)` 顯現。⛔ 不得讀作「該格無影響」。

### 二-5 🔴 受詞聲明（⛔ 不得誤讀）

上欄為 `ΣRw_側` 之**<u>起算項</u>之差**，⛔ **不是 `ΔΣRw`**。
碼面逐字：`ΣRw_側 = R(b + Σw_群) − R(b)` ⇒ 完整之 `ΔΣRw` 尚需**終點項**，
而 `Σw_群` 於三格**取不到**（§四）⇒ 🔴 **無從判定**，⛔ 非「為 0」。

⚠️ **條件式（⛔ 未斷言）**：**若**兩終點項皆飽和於 `100`，**則** `ΔΣRw = −(起算項之差)`。
⛔ **本批未證其飽和。**

### 二-6 🔴 兩族之分歧**射程不同**（⛔ 不得混為一談）

| 族 | A | B | 分歧範圍 |
|---|---|---|---|
| **③ 選槽 `b`** | `buf·cos_dn` | `buf·cos_dn + W_0` | 凡 `W_0 ≠ 0` 即分歧 ⇒ **8／8 街廓側**（母體＝該 log 之 8 個街廓側·判準＝`\|W_0\| > 1e-6`） |
| **② 推進種子 `_W_prev`** | `buf·cos_dn` | **硬 `0.0`** | 僅 `buf ≠ 0`（即 `forced`）⇒ **3 格**（母體＝16 格·判準＝標 `← forced`） |

---

## §三 甲-4 分歧自何時起

| 側 | 字樣 | `git log -S` 之結果 |
|---|---|---|
| **A** | `_left_buffer_S * _cos_dn`（`app.py`） | **2** commit：`f2dab43`（2026-07-05）／`1b290e4`（2026-07-05 初始遷移） |
| **B** | `_b_L0`（`verify/stepg_pipeline.py`） | **1** commit：**`d0d450d`（2026-07-19）**「…`_select_pool_slot` **理論 `b` 改 mp-based**…」 |
| **B** | `_W_prev_left = 0.0`（同上） | **1** commit：**`687b5d4`（2026-07-19）**「W 脫鉤打樣…」 |

🔒 **判別力自檢**：對**必然存在**之字樣 `def main` ⇒ **1** commit ⇒ ✅ 非恆 0。

⇒ 🔴 **分歧起於 `2026-07-19`**（`W-G.4 S1 W 脫鉤打樣`那一批）——**引擎側改了、`app.py` 側沒改**，
**至今 25 天**。⚠️ 該 commit 訊息**自己寫著**「理論 `b` 改 mp-based」⇒ ⛔ 非無意；
**無意的是「另一側沒跟著改」**，而**沒有任何機制看得到**（`GB-62` 方向③）。

### 三-1 ⚠️ 「曾否使配地結果不同」——**分項判**

| 項 | 判 |
|---|---|
| 二者之**輸入**是否不同 | ✅ **證得**，逐格具名（最大 `1.69` m／`22.24` 百分點） |
| 該輸入是否**直接進入負擔計算** | ✅ **證得**（碼面逐字 `ΣRw_側 = R(b + Σw_群) − R(b)`；`_W_prev` 為 thread 種子） |
| **配地面積**之實際差為多少 | 🔴 **無從判定**——需跑完推進迴圈，被結構閘擋住（§四）。⛔ **非「為 0」** |

---

## §四 丙組：`G4` ✅ 成立，但**射程有限**

✅ **存在不經 `run_step_g`（故不經結構閘）之只讀路徑**：
`verify/out/probe_WG91_buf_metric_and_candidates.log` 已涵蓋 **16 格**
（母體＝該 log·判準＝行首符 `情境 街廓 側別` ⇒ **48** 行命中）。

🔒 **判別力自檢**（`G4` 期望為「存在」·屬**順利**型 ⇒ 尤須自檢）：
以**必不命中**之樣式（`^0m\s+R9\s`）於同一母體試 ⇒ **0** 行 ⇒ ✅ 上式**非恆命中**。

🔴 **射程**：該路徑取得**街廓級**之 `b`／`buf`／`cos_dn`／`S1_par`，
⛔ **未取** `widths`（`Σw_群`）——後者須跑推進迴圈 ⇒ **仍被結構閘擋住**
⇒ `ΔΣRw` 之**終點項**仍**無從判定**（⛔ 非為 0）。

---

## §五 乙組：`8.9 個百分點` ⇒ **維持未證**

候選（＝三 `forced` 格之起算項差·母體＝**3 格**）：`−9.0183`／`+8.1830`／`−8.7387`。
三格帶號合計 ＝ `−9.5740`｜絕對值合計 ＝ `25.9399` 百分點。

🔒 判準：`| |值| − 8.9 | < 0.05` ⇒ **命中 0 項**。
🔒 判別力自檢：同判準改試 `8.7` ⇒ **命中 1 項** ⇒ 該判準**非恆 0 亦非恆命中**。

⇒ 🔴 **無從判定**：`8.9` 對不上任何一格、亦對不上二式合計
⇒ 依施工單乙組：**該數維持<u>未證</u>**，⛔ **不得為求有數而擇一**。
⚠️ 併看倉規：**兩個近似值相符 ≠ 相互驗證** ⇒ ⛔ 即使他日有值落在附近，亦不得據以認定其為真。

---

## §六 `G1`–`G5` 逐項判

| # | 判 | 依據 |
|---|---|---|
| **G1** | ✅ | 兩餵入點之服務流程**由 AST 祖先鏈證**（§一-1·附判別力自檢） |
| **G2** | ✅ **且更省** | 三格之二值差**逐格算出**——且**取自倉內既有產物**，⛔ 未重跑（§二） |
| **G3** | ✅ | 分歧起點定為 `d0d450d`／`687b5d4`（`2026-07-19`·§三·附判別力自檢） |
| **G4** | ✅ | 存在不經結構閘之只讀路徑（§四）；⚠️ **射程有限**，⛔ 未取 `widths` |
| **G5** | ✅ | 本批未觸及 `run_verification` 之任何輸入（§七-7） |

🛑 **`N-1` 觸發** ⇒ 上呈（§白話稿）。⛔ `N-2`／`N-3` 均未觸發。

---

## §七 ⛔ 本批未做之事（⛔ 逐項）

1. ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 零觸；全程**只讀**。
2. ⛔ **未估上限**（施工單 §1 已裁押後）、⛔ 未取任何代表值、⛔ 未取任何近似。
3. ⛔ **未改任何閘或容差**——結構閘**原封不動**（丙組只查、不改）。
4. ⛔ **未判「哪一條是對的」**——那是域裁（`W-G.9-14` 已上呈之是非題）。
5. ⛔ 未量 `ΔΣRw` 之終點項、⛔ 未量配地面積之實際差。
6. ⛔ 未動 `docs/rulings/`／`CLAUDE.md`／`run_verification.py`／`wv_reconcile.py`／
   `baselines/`／既有夾具／三支複本；⛔ 未改期望 FAIL 名單、未重凍、未重烤。
7. ⛔ **未跑長跑**——`git diff --numstat 7c80fe1 -- app.py verify/stepg_pipeline.py
   verify/run_verification.py verify/wv_reconcile.py verify/baselines/ docs/rulings/ CLAUDE.md`
   ⇒ **空輸出**；🔒 判別力自檢：同式對 `docs/reports/` ⇒ **非空** ⇒ 上式非恆空。
   ⇒ 判定層在構造上不可能變動。
8. ⛔ 未追那 57 條、未追 `X5` 之 6 條、⛔ 未掛 `wg915` 盤點器。

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。

---

## §八 丁組：更正之追認

施工單稱「你自撰之**六處**加註」；🔧 **現查為 <u>3 處加註區塊</u>**
（涵蓋 6 個已知為偽之落點：卷首／`D3` 表列／`D5`／§3／§9／§10）。
⇒ 已於**該 3 處**就地補入：**「施工單第 1 版未達，本加註係 CC 自撰；`W-G.9-21` 追認之。」**
🔒 計數指令：`grep -c "【W-G.9-21 追認】" docs/reports/W-G.9-19_下界估算未達與同源前置.md` ⇒ **3**。
⛔ **未改施工單之敘述**，照實報差異。

---

## §白話稿 呈 KL（依 `重劃白話` 之呈送段）

### 【現況】

**同一份圖、同一個分頁、同一次操作——「側街負擔從哪裡開始算」這一題，程式裡<u>同時有三個答案</u>。**

您在那個分頁上做兩件事，它們是**接力**的：

```
   ① 按「重新計算」  → 排出原位次的地  ─┐
                                        ├─ 同一個分頁，先①後②
   ② 按「執行七級調配」→ 做調配、算負擔 ─┘

   「側街負擔從哪裡開始算」——三個答案：
   ┌────────────────────┬──────────────────────────┬────────┐
   │ ① 排地時           │ 從「替身線」量過來        │ 自洽   │
   ├────────────────────┼──────────────────────────┼────────┤
   │ ② 選池子位置時     │ 從「街角點」量過來        │ 🔴     │
   │ ② 實際算負擔時     │ 從「零」開始              │ 不自洽 │
   └────────────────────┴──────────────────────────┴────────┘
   單位：三者皆為「一段距離」（公尺）；⚠️「起算點」是一個位置，⛔ 不是面積
```

**②自己內部那兩個對不起來**——這就是您先前看到「程式跑到第一個街廓就停住」的原因。

**差多少（實際會發生的三個街廓）**：

| 街廓 | 兩個算法差幾公尺 | 換成負擔率差幾個百分點 |
|---|---|---|
| R2 左 | **1.69** 公尺 | **9.0** |
| R3 右 | **1.69** 公尺 | **8.2** |
| R5 左 | **1.64** 公尺 | **8.7** |

**這是什麼時候開始的**：**2026 年 7 月 19 日**，改程式的那一批**只改了一邊**，
另一邊留在原地——**到今天 25 天，沒有任何檢查看得到這件事**。

### 【對土地的影響】

1. **兩邊吃的數字確定不一樣**——上表逐格列出，⛔ 不是「差不多」。
2. **這個數字直接進負擔計算**——負擔算出來不一樣，**每一筆的配地面積就會不一樣**。
3. ⛔ **但「面積實際差多少」這一批還是沒量到**——要量得跑完整個推進流程，
   而它卡在第一個街廓。**⛔ 我不會給您一個猜的數字。**

### 【要你判斷】

🛑 **這一題您已經被問過一次了**（先前那個是非題：「側街負擔之累積，應自**街角點**起算——是或否？」）。

**現在查出來的是：它的影響比當時報的更大。** 當時報的是「一個地方算錯」；
**實際上是三個地方各算各的**，而且**分屬您在畫面上做的兩個不同動作**。

> **那個是非題還是同一題，但現在還要多決定一件事：
> 三個答案要統一成哪一個？**

（① 排地時用的／② 選池子時用的／② 算負擔時用的——**⛔ 我不替您選**。）

**標記：真需地政專業判斷**（因為「負擔應該從哪裡起算」是法規與裁量問題，不是程式問題）。

### 【裁完之後會接著冒出來的問題】

1. 🔴 **統一之後，配地面積會變**——變多少，得等程式跑得過第一個街廓才知道。
2. 🔴 **「只改一邊沒人看得到」這件事還在**——這次是碰巧撞到才發現；
   ⚠️ **同類的分歧可能還有別處，本批⛔ 未查。**
3. ⚠️ 施工單上寫的那個數（`8.9` 個百分點）**對不上我算出來的任何一格**
   ⇒ 我把它標成**未經查證**，⛔ 不當成事實用。
4. ⚠️ 「排位置那一半的上限」**仍未起手**——依您上次的意思，它押後到三個街廓跑得到之後。
