# CC 新 session 交接文：**K-6-A2 段六**（K-9-5 ＋ K-9-1 ＋ GB-18 ＋ GB-39）

**產出**：CC／2026-08-06　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`　**交接時 HEAD**：`9a58bb5`（已 push·已驗）

> 🛑 **新 session 第一件事：讀 §二（停機上呈中之三項）**——其中兩項**擋住段六本體**。
> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前**重跑該指令**（行號／位元組數皆易腐）。
> 🔴 **§三 列有一批「已作廢·不得引用」之數字**——**開工前必讀**，否則會拿作廢值當前提（節 36）。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD                                  # 應 ≥ 9a58bb5
sed -n '1,80p' CLAUDE.md                                    # ⚠️ 本波新增 4 條常設紀律
grep -n "^### 🔒 K-9-5-3 \|^#### K-9-5-2-a \|^##### 🔴 K-9-6-g-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md   # 現值 40
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # 現值 節 49
sed -n '1,60p' docs/reports/W-G.7_K6-A2-段六_接入點偵察.md   # 段六之接入點分析
sed -n '1,40p' docs/reports/W-G.7_K6-A2-S6-2B_資格閘運算元.md
```

⛔ **禁 `git reset --hard`**。⛔ **禁對 `verify/out/` 用 `git add -A`／`.`**（CLAUDE.md 新條）。

---

## 一、現況

| 項 | 值 | 現查指令 |
|---|---|---|
| 分支／HEAD | `wip/s1-endpart` ／ `9a58bb5` | `git rev-parse --short HEAD` |
| **`run_all` 最新靶** | `verify/out/K6A2_seg6_gb40_runall.log` | — |
| 計數 | **86 PASS／46 FAIL** | `grep -c '✅ PASS' <靶>`／`grep -c '🔴 FAIL' <靶>` |
| 🔴 **驗收方式** | **「與凍存之期望 FAIL 名單**逐項**相同」**，⛔ **非「全綠」、⛔ 非比總數** | 名單：`verify/out/K6A2_期望FAIL名單_902f5d1.txt`（**46 列**） |
| 登記表 | **40 列**（`GB-1`〜`GB-40`＋`GB-7b`·**缺 `GB-12`**） | 見上 |
| 失敗考古末節 | **節 49** | 見上 |

🔒 **本分支為「准紅碼」**（KL 裁 2026-07-21）——F.0 六格錨係 W脫鉤(07-19)＋S0d(07-20) 後之
**預期新值**，非 regression、非資料壞；錨待**波末重烤**。

### 1-1 K-6-A2 之完成度

| 段 | 狀態 |
|---|---|
| 段一〜段四 | ✅ 已收官 |
| **段五（K-9-6 宗地層）** | ✅ **已收官**·生產已走 `baseline_pts` 分支 |
| **GB-40（k98 帶底切換）** | ✅ **已切換**（KL 放行 2026-08-06）·登記表已標 ✅ |
| **段六（K-9-5＋K-9-1＋GB-18＋GB-39）** | ⬜ **未開工**·🛑 **被 §二 之停機項擋住** |

---

## 二、🛑 停機上呈中（**開工前必讀·兩項擋住段六**）

### 2-1 🛑 **正典互斥：`K-9-5-3` vs `K-9-5-2-a`-3**（擋住段六之判定層）

| 則 | 逐字要旨 | 對 `3.5m R1/right 628(5)` |
|---|---|---|
| `K-9-5-2-a`-3（KL 裁 **08-03**） | 「面積過、寬不足」⇒ **loud raise**（構造被破壞）；⛔ **不得判該宗不合格** | loud raise |
| `K-9-5-3`（KL 裁 **08-06**） | 量得寬 < 門檻即**判不合格**，無論短少多小 | 判不合格 |

- **實例已坐實**：該格 **真G `240.57` ≥ 門檻 `232.79`**（`app.py:11255` 之閘·2dp 來自 `:11252`）
  **且** k98 量得寬 **`6.847031` < `7.00`** ⇒ **確為「面積過、寬不足」**。
- 🔴 **`K-9-5-3` 現經 KL 令「暫停適用」** ⇒ ⛔ **段六不得依任一則寫判定碼**。
- 正典**兩處**已加 🛑 互斥標記（雙向對讀錨·各 1 命中）⇒ ⛔ **不得擅自解除**。
- 詳：`docs/reports/W-G.7_K6-A2-段六_接入點偵察.md` §〇。

### 2-2 🛑 **寬度不足之成因「仍未明」**（S6-2-B 分支 A 之後半）

已排除者（皆有實據）：

| 假說 | 結論 | 據 |
|---|---|---|
| 兩端量測帶不同 | ❌ 排除 | k97 `_Dn = min(_dPb,_dQq)` 與 k98 `depth_from_PQ = min(_dep(P_block),_dep(Q))` **同一構造** |
| 面積也不足 | ❌ 排除 | 真G `240.57` ≥ 門檻 `232.79`（差 `+7.78`·達標） |
| 弦被截角削（k98 端） | ❌ 排除 | 該格 **(a) 線距 ＝ (b) 弦 ＝ `6.847031`**·弦**未被削** |
| 閘被繞過 | ❌ 未觸發 | 分支 B 未成立 |

⇒ **剩下的是：winner 實配之 `L_in` 與 `SIDE_LINE` 之線距本身就 < `T`。為什麼？未明。**
⇒ `K-9-5-2` ⑤（面積夠 ⇒ 寬度自動合格）之推論**於此格不成立**，而**斷在哪一步未定**。
   已列之候選斷點（⛔ 未挑主因）：④ 之前提／「同面積可窄而深或寬而淺」。

### 2-3 🛑 **S6-1 (b)「去向」未裁**

`K-9-5-3` 只裁「判不合格」，**未裁其後如何處置**（併宗／改配／現金補償／強制留設抵費地）。
⚠️ 該格**候選 1／合格 1** ⇒ **無下一名可換**（`verify/out/probe_K9_7d_pk_impact.log:29`）。

### 2-4 ⛔ 未決項（**CC 不得自決**）

`data/r3.dxf` 之去留／`data/地籍資料來源_匿名版.xlsx` 之未 commit 異動是否入庫。

---

## 三、🔴 **已作廢·⛔ 不得引用之數字**（本節最容易害到下一手）

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| `docs/reports/W-G.7_K6-A2-S6-2_弦vs線距.md` | **§二-3／§二-4／§三 之全部數字**（含「`1.36 vs 5.08`」之複現） | `verify/out/probe_K9_s62_chord_vs_linedist.log`（重跑後）＋ `W-G.7_K6-A2-S6-2B_資格閘運算元.md` §四 |
| 同上 | 其結論「⑤ 於規定範圍上已證偽·與 winner 無關」 | **已由 claude.ai 自撤**（違反「兩套側界·禁混用」） |
| `W-G.7_K6-A2-S6-2_主測_線距vs弦.md` §〇-2 | 「G ＝ 227.72 < 232.79 ⇒ 面積也不足」 | **已撤回**——取錯運算元（`g_rows` 之 G vs 閘用之 `cand_G`） |
| `docs/reports/W-G.5_K6-A2-段五c0_量測對象偵察.md` | 全文之診斷框架 | 已加作廢註（KL 圖示裁定推翻） |
| `docs/reports/W-G.6_K6-A2-段五ab_宗地帶_攤表.md` | 「寬度_新」欄 | `W-G.6_K6-A2-段五c1_宗地帶_攤表.md` |

### 3-1 ⚠️ **仍在倉內但「閘未過·不可據」之格**

| 檔 | 不可據者 |
|---|---|
| `verify/out/probe_K9_s62_k98_line_vs_chord.log` | `0m R1/left`（自我驗證閘差 `1.370e-05`·已標 `🔴 閘未過`） |
| `verify/out/probe_K9_s62_chord_vs_linedist.log` | **6 端**（皆 `d≈0`·近共線·解析 vs GEOS 差 > `1e-4`·已逐端列出） |

🔒 二檔皆**已把該條件寫進輸出**——⛔ 引用前先看該格有無標記。

---

## 四、段六之前置清單（`W-G.7_K6-A2-段六_接入點偵察.md` §七 ＋ 本波新增）

| # | 前置 | 性質 |
|---|---|---|
| 1 | 🛑 **2-1 之互斥協調** | **待 KL** |
| 2 | 🛑 **2-2 之成因查明** | 待查（技術·可續做） |
| 3 | `on_chamfer` 等契約旗標於 `_build_corner_range_v3` 之**存活** | 查證（未查） |
| 4 | **§零-2「街廓最小建築面積」UI 欄**（K-9-1 所需） | 新增 UI。⚠️ **與 `setback` 勿混**：`setback` 欄**早已存在** |
| 5 | 撤銷 K-4 豁免之早退 return ＋ 路由至 k98 ＋ **加判定層**（k98 只量不判） | 實作 |
| 6 | `_require_baseline` 之落點移至 k98（或確認既有 `_stop` 已足） | 實作 |
| 7 | GB-39 之防護（本案零觸發·仍須接對路徑） | 實作 |
| 8 | 🛑 **S6-1 (b) 去向** | **待 KL** |
| 9 | GB-18 契約旗標之攜出：**三改法已列選項與代價·⛔ 未擇一** | 設計層（`接入點偵察` §四） |

🔒 **好消息**：`k98_virtual_measure_block` 簽章**已含** `baseline_pts`、**生產呼叫點 0**；
`_n14_band_geom` 於虛擬塊上 **12/12 成立**；合成門檻 `T = setback + min_width` **碼面已有二處**
⇒ ⛔ **段六不需新造該式、亦不得寫第三份**。

### 4-1 波末批（不變）

換快照（U-K8-1＝乙）／重烤 v3・wf・E 系列 baselines／改遷移項／**U-K9-2 之修圖**／
`K-9-6-d` 落地狀態條文已過期／**`k98_virtual_measure_block` docstring 之法源併記**
（花蓮縣畸零地使用規則第四條末句·因「零碼面」而**留待下一次動 `app.py` 之批**）。

---

## 五、本波之常設紀律（**新增 4 條·全在 `CLAUDE.md`**）

| 條 | 要旨 | 案由 |
|---|---|---|
| 🔒 長跑期間不得動受測物 | `run_all` 執行中 ⛔ 不改 `app.py`／`verify/` 任何檔 | 段五(c)-2：commit 3 之靶被 commit 4 之改動污染，**且不會報錯** |
| 🔒 位元組數／校驗碼須聲明量測框 | worktree ≠ blob；「逐字節相同」以 `diff` 為據 | 節 48 |
| 🔒 `verify/out/` ⛔ 禁 `git add -A`／`.` | 混放入庫證據與本機臨時 log；commit 後必跑 `git show --name-status` 對照 | 節 49-1（誤入 33 檔） |
| 🔒 **探針還原內部幾何須以碼面自身之保證當自我驗證閘** | ⛔ 閘不過不准下結論·且須寫進輸出 | 見 §六 |

---

## 六、🔑 本波最貴的一條：**自我驗證閘**（三次擋下真錯）

| # | 錯 | 若無閘會怎樣 |
|---|---|---|
| 1 | σ（形心翻號）以「量測線是否切到多邊形」**試探**求得 | `δ_P<0` 之 6 格其多邊形**跨越 `PQ`**、**兩個號都會切到** ⇒ 表照印，值全錯 |
| 2 | 弦以 **GEOS 於世界座標**求 | **E-10**：與多邊形邊**共線**時，TWD97 量級（~3.1e5）回 **3.684**（真值 **6.847**） |
| 3 | 解析框寫成 `t = (P−F1)·b̂n` | `d̂` 與 `b̂n` **不正交** ⇒ 該座標在量測線上**不為常數**；正解 `t = ((P−F1)·n̂)/h` |

🔑 **三次都是「不擋就會印出一張漂亮但錯的表」**——外部重建**沒有型別可擋**。

### 6-1 其餘可複用之戒（本波所立）

- **同名不同量**已第三度咬人：`帶底 t_hi` vs `depth_from_PQ`／`cand_G` vs `g_rows` 之 `G(㎡)`／
  `線距` vs `弦`。⇒ **見兩量不一致，先 grep 正典有無已定式之組合條款**，
  再判斷是「兩個合法選項」還是「一舊一新」。
- **節 42／47／48／49 是同一根**：**未與同倉既有逐字紀錄對讀即下斷言**。
  ⇒ 寫入新條款前**必 grep 同檔既有之逐字／紅線／⛔ 條款**（節 42 戒 (d)）。
- **窮舉之揭示**：宣稱窮舉須**連過濾管線一併揭示**，最後跑**裸 grep**（節 44 戒 (b)）。
- **提機制時做單調性檢查**：「若成因變強，觀測量該變大還變小？」（節 49-2）。

---

## 七、收官前置（不變）

`git rev-parse origin/wip/s1-endpart` 已含該 commit——**不綠不推、不推不報**。
⚠️ `run_all` 約 **15–20 分鐘** ⇒ 背景跑＋等通知；`run_verification` 會寫 CSV ⇒ **兩支長跑不可併跑**。
⚠️ 驗收以**期望 FAIL 名單逐項相同**為準（§一）。
