# W-G.5 — **K-6-A2 段三-0：正典與紀律（零行為）**

> **基準**：`7e54160`　**分支**：`wip/s1-endpart`
> **性質**：**零行為**——`app.py` 與 `verify/` 之 `.py` **一字未動**。
> 本檔涵蓋施工單之 **§一（KL 裁定入正典）／§四-2（`P` 同名異物）／§五（正典更正）／§七（併辦兩項）**。
> **§三(a)(b)(c) 不在本 commit**（K-9-7 求解器另批）。
> ⚠️ 所引行號**全部當場重 grep**；差異見 §六。

---

## 一、KL 裁定入正典（施工單 §一·**逐字**）

新增 **K-9-5-1「街角最小規定範圍之構成與『面積夠 ⇒ 寬度合格』」**
（`grep -n "^### 🔒 K-9-5-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`），
①〜⑤ 五則逐字收錄，並補 **K-9-5-1-a**（對實作之直接效力·四項）與
**K-9-5-1-b**（② 之實證）。

### 1-1 ⚠️ 編號衝突之處置（**施工單未預見·以倉內為準**）

**`K-9-5-1` 一號已被占用**——K-6-A2 段二-0 一-6 之「正面路寬未填 ⇒ 停機補填」
即編為 `K-9-5-1`（`648bbb5` 入倉）。而本單 §一 亦指定編為 `K-9-5-1`。

**處置**：**新裁定取得 `K-9-5-1`**（施工單明令），
**舊則改編為 `K-9-5-1′`**（`grep -n "^### 🔒 K-9-5-1′ " docs/rulings/K-6_街角地分配程序與可分配判準.md`）
——**內容一字未改**，僅加編號註。
**全倉引用同批更新**（各附「原號→新號」之說明，避免日後指涉不明）：

- `docs/reports/W-G.4_泛用阻塞項登記表.md` GB-20 條目之正典指標
- `docs/reports/W-G.5_K6-A2-段二-0_補正批.md` 一-6

⛔ **未沿用「後到者加撇」之直覺**——施工單既明令新裁定編為 `K-9-5-1`，
故由**舊則**讓號；此舉使**施工單所寫之編號在倉內即為真**。

## 二、對實作之直接效力（施工單 §一·已入正典）

| # | 效力 | 落地 |
|---|---|---|
| 1 | **§零-3 蘊含語結案為 (乙)** | 正典 `### 🔴 K-9 §三-1` 改標 **`### ✅ K-9 §三-1 …（已裁·非牴觸）`**；原文保留為考古、明標「待 KL 裁／須逐角驗之敘述**已失效**」 |
| 2 | **「(乙) 須逐角驗」技術附記作廢** | 該段全文加刪除線＋標「已由 K-9-5-1 之構造保證涵蓋」，並載明 **∠(SIDE_LINE, ALLOC) 是否退化與 (乙) 之成立無關**（③ 係構造保證、非各角幾何之偶然） |
| 3 | **K-9-5 實量「面積過、寬不足」⇒ loud raise** | 入正典 K-9-5-1-a 第 3 項，**明標效力屬段五、本批碼面未實作** |
| 4 | **② ⇒ 一條規則涵蓋兩種構成、禁分立兩式** | 入正典 K-9-5-1-a 第 4 項（比照 K-9-8 (6) 之體例） |

---

## 三、§四-2 `P` 同名異物（補進 K-9-7-a）

正典原 K-9-7-a 只載 `c`／`k` 之衝突。本批補**衝突二**：

| 出處 | `P` 之定義 |
|---|---|
| **K-9-7**（`P(t)`） | `ALLOC_LINE ∩ 前緣線` |
| **K-9-8 (2)**（`P`） | `L_in ∩ BLOCK 邊界`（可能落截角邊） |

⚠️ 二者於「`P` 落 FRONT 重疊段」時**重合**，本案 **12 格中 10 格如此** ⇒ 多數格次分辨不出。
🔒 已載明**實作時必須分開命名**（`P_front`／`P_block`），否則段四做 K-9-8 時會踩雷。

---

## 四、§五 正典更正（**實料與正典不符·已改**）

### 4-1 事實

正典 K-9-7-d 原稱「**12 格全部落在 FRONT 重疊段之退化支，截角邊支零實例**」。

**已入庫實料**（`verify/out/probe_ruling_K9_corner_width.log`）：

```
:72   0m    R4/right   628-1(1)   -0.013039   14.8480   截角邊（t_P=+0.013039m）
:78   3.5m  R4/right   628-1(1)   -0.013039   14.8480   截角邊（t_P=+0.013039m）
```

本批自行複核該表全 12 列：**FRONT 重疊段 10 列、截角邊 2 列**
（`grep -c "FRONT 重疊段"` ⇒ 10；`grep -c "截角邊"` ⇒ 2）。

⇒ **原敘述與實料不符，已更正**為「10 格 FRONT 重疊段／2 格截角邊」，
並標明前版敘述**已作廢**。

### 4-2 成因（**引 log 原文·未自行詮釋**）

同 log `:23-26`：

> 「退縮 0m 時該範圍可能**整個落在截角區內**，其極值遂落在**截角邊**上
> ——此與圖面畫得準不準**無關**……且同一街角於退縮 3.5m 時回到 ~1e-5
> （範圍夠大、已越過截角）。」

⇒ 此正是 **K-9-5-1 ②「退縮 ＝ 0 之情形，範圍可能不含 FRONT_LINE 段」之實證**。

### 4-3 🔴 **但施工單之成因敘述不完全·本批不臆測**

施工單謂該成因即「退縮 0m ⇒ 落截角；退縮 3.5m ⇒ 越過截角」。
**惟實料顯示 `R4/right` 於 `0m` 與 `3.5m` 兩情境皆落截角邊**
（`-0.013039` 兩格逐位相同）——**3.5m 並未如該成因所述回到 FRONT 重疊段**。

⇒ 成因**不止於退縮值**，尚與該角之截角股長／範圍尺度有關。
**本批只記錄此事實、不臆測其完整機制**（§〇-3：禁把未驗證的機制寫進報告當成因）。
已於正典該處以警語標明。

### 4-4 分支覆蓋之正確敘述（供 §六 用·**本批只更正敘述、不做合成案**）

以 K-9-7-c 表之 ①②③ 交叉 `P` 落點：

| 交叉 | 本案實例 |
|---|---|
| ①×FRONT／②×FRONT／②×截角／③×FRONT | ✅ **有** |
| ①×截角／③×截角／切換點支（④） | ❌ **無**——須以**合成案**補（構造 `t` 使 `P` 恰通過截角端點） |

⚠️ **合成案屬 §三(a) 之交付**（求解器落地後方能構造），**不在本 commit**。

---

## 五、§七 併辦兩項（零行為）

### 5-1 F-1：GB-24 條目補記

已於 GB-24「真資料覆蓋」欄補：

> ⚠️ **併記**：K-9-4 之母體界定 `k94_partition_land_rows` 於本案**零觸發**（無地宗 0 宗），
> 其**排除邏輯**與 **`_bad_no` 自檢分支從未被執行** ⇒ **不得記為「已驗」**，
> 與閘本身同屬**換案防呆**。

### 5-2 常設紀律入 `CLAUDE.md`（**逐字**）

已入「工作紀律」節、緊接「自評 gate 綠不可替代獨立復現」之後
（`grep -n "main() 內之敘述，\`run_all\` 不得單獨作為驗收依據" CLAUDE.md`）：

> **凡新增或修改落在 `main()` 內之可執行敘述，`run_all` 之名目集合結果不得單獨作為
> 驗收依據，必須另附合成案。**
> **理由**：`harvest()` 以 AST 跳過 UI、harness 走 `verify/stepg_pipeline.run_step_g`，
> `main()` 內之敘述從不被 `run_all` 執行（已兩度坐實：GB-24 之 K-9-4 閘、GB-25 之 F-2 狀態機）。

---

## 六、施工單與倉內實況之差異（§八-3）

| 施工單所載 | 倉內實況（`7e54160` 當場 grep） | 處置 |
|---|---|---|
| `_build_corner_range_v3` `app.py:9568` | 屬實 | §三(a) 用 |
| `k = sigma / den_a` `app.py:9691` | 屬實 | §三(a) 用 |
| 正典 `:589-636` K-9-7 本體／`:623-632` K-9-7-d／`:634` 斜率／`:636` 退化 | 屬實 | §三(a) 用 |
| 正典 `:638-643` K-9-7-a／`:657-675` K-9-7-c | 屬實 | 已補 `P` 衝突 |
| 正典 `:427-438` 蘊含語待裁／`:439-442` 技術附記 | 屬實 | 已結案／已作廢 |
| 正典 `:630-631` 「12 格全部 FRONT」 | 屬實（**且與實料不符**） | 已更正 |
| log `:72`／`:78` R4/right 截角邊／`:23-26` 成因 | 屬實 | 已引 |
| §一 令新裁定編 `K-9-5-1` | **該號已被段二-0 之裁定占用** | **舊則改編 `K-9-5-1′`**、新裁定取得 `K-9-5-1`（§1-1） |
| §五 成因「0m 落截角／3.5m 越過」 | **R4/right 兩情境皆落截角邊** ⇒ 成因不完全 | **只記事實、不臆測機制**（§4-3） |
| §三 順序更正（K-9-7→K-9-8→K-9-6→K-9-5）之依據 `:571`／`:683` | 屬實 | 依此序 |
