# `W-G.9-209R`　執行報告——`commit 2` 之放行 ＋ `GB-140` 更正註 ＋ `GB-141` ＋ `自誤 262`

> 施工單：`docs/orders/W-G.9-209_施工單_commit2放行與GB140更正註與GB141與自誤262.md`
> （`13737` B／`175` 列／`CR 0`·`SELF_SHA256` `1fea1d8b69f8f6ec598f50efc0f9e3fdb6b84e599aa9c96fe8cd015627ad8eaa` 自驗相符）
> 三段皆已辦畢：`§一` rebase ＋ 側分支重推 ＋ 停機 ／ `§二` 主線推進（發單側逐字放行）／ `§三` 零工碼登記批。

---

## §零　開工閘（四閘全綠）

| 閘 | 實測 | 判 |
|---|---|---|
| `S-0a` | `ls-remote wip/s1-endpart` ＝ `be1b326a9526c704a37a495474b04aa0694cb1e0`；五檔 blob 逐檔相符（`app.py` `6f0317e4…`／`selection_pipeline.py` `10f90c50…`／`run_verification.py` `d07955c5…`／`stepg_pipeline.py` `fef5865a…`／`run_all.py` `e5cf9a22…`） | ✅ |
| `S-0b` | `verify/W-G.9-198R-c2` ＝ `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b`；其父 ＝ `c746d2dd…` ⇒ **⛔ 能快轉·須 rebase** | ✅ |
| `S-0c` | 追蹤檔 **`1583`**／`.md` **`516`**；`GB` 相異 `54`／MAX `140`；自誤 `226`／`261`；`VR` `74`／`89`；`K-9` `13`／`26` | ✅ |
| `S-0d` | `SELF_SHA256` 相符（受詞 **`13659`** B ＝ 去 `SELF_SHA256` 列及其後 ＋ 補尾換行）；`probe_order_preflight` 🔴 **`0`**／🟡 **`1`** | ✅ |

**取號之框（🛑 `GB-138`：`GB` 之定義列式⛔ 經正典釘定 ⇒ 同格載框）**

| 號 | 框 | 母體 | 哨兵扣除 |
|---|---|---|---|
| 自誤 | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 無 |
| `GB` | `` ^#{2,4} `?GB-([0-9]{1,4})`? `` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `997` |
| `VR` | `` ^#+ `?VR-([0-9]{1,4})`? `` | `docs/驗證裁定登記表.md` | `751`／`999` |
| `K-9` | `` ^### 🔒 K-9-([0-9]{1,3})　 ``（全形空白 U+3000 結尾） | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `99` |

### `S-0d` 之 🟡 逐筆歸類（裁 `H`：命中數⛔ 判準·母體⛔ 扣本單）

探針旗出 **`1`** 項：`[P-4] :154　🟡 方向性轉引而同段⛔ 未具名座標系：| 閘 | 判準 |`。
字樣錨 `` | 閘 | 判準 | `` 於本單命中 **`3`** 列，**逐筆歸類**：

| 列 | 所屬節 | 歸類 |
|---|---|---|
| `18` | `## §零　開工閘` | **索引表頭**——其下 `S-0a`〜`S-0d` 各具名母體與期望值（探針未旗出） |
| `31` | `## §零　開工閘` | **豁免宣告本身之引文**（錨置於反引號內）⇒ **自指、非受詞**（探針未旗出） |
| `154` | `### 三-4　§三 之驗收` | **索引表頭**——其下 `T-1`〜`T-6` 各具名母體／框／方向（＝ 單所預告並具名豁免者） |

⇒ 三筆**無一為「方向性轉引而未具名座標系」之實質缺** ⇒ **採納單之豁免**。

**判別力自證（⛔ 另造·同器）**：`-203` ＝ `0`／`-205` ＝ `1`／`-206` ＝ `3`／`-207` ＝ `0`／
`-208` ＝ `1`／`-198R` ＝ `5`【項】——**六靶全中** ⇒ 本器⛔ 恆綠亦⛔ 恆紅。

**本機分支之處置（`自誤 261` 之一般式）**：開工時 `git rev-parse HEAD` ＝ `be1b326a…`、
`git symbolic-ref -q HEAD` ＝ `refs/heads/wip/s1-endpart` ⇒ **⛔ 停在 `0d88e1a5`**、⛔ 需退回；
`keep/W-G.9-198R-c2` 之本地保全已在（`0d88e1a5…`）。

---

## §一　rebase ＋ 側分支重推

**`0d88e1a5′` ＝ `a1107a17825c37430f69349afaceef4d09c6f5f8`**
（作法：`checkout -B tmp/c2-rebase 0d88e1a5` → `rebase --onto be1b326 c746d2dd` → 驗 → 推 → 復位 `wip/s1-endpart` → 刪 `tmp`）

| # | 判準 | 實測 | 判 |
|---|---|---|---|
| `E-1` | 父 ＝ `S-0a` 所實查之主線頭 | `be1b326a9526c704a37a495474b04aa0694cb1e0` | ✅ |
| `E-2` | 恰二檔 | `app.py`／`verify/selection_pipeline.py`；`verify/run_verification.py` 命中 **`0`** | ✅ |
| `E-3` | `numstat` | `219  11  app.py`／`9  0  verify/selection_pipeline.py` | ✅ |
| `E-4` | 二 blob 逐檔與 `0d88e1a5` 相同 | `app.py` `144349c834c2289434e1b0edbcfc8d0a27d88435`／`verify/selection_pipeline.py` `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c` | ✅ |

commit 訊息：舊 **`7468`** B ／ 新 **`7468`** B（`戒 40` 二端並報）·**逐位相同** ✅。
`tree` `d4ef8fc706eacaaa7455bbd3b89423f1184434d7` → `39f192d552c9fa0472884539575d260d9aa23466`。

### 🔴 判別力自證＝**量測器紅**（⛔ 受詞紅）——已依 `常規五` 處置

**步一（證器紅）**：單所「**建議**」之見證檔 `docs/reports/W-G.4_泛用阻塞項登記表.md`
於 `0d88e1a5`／`0d88e1a5′`／主線頭三態 blob **皆 `c09d01168a93393164dc5e1436923920c8beac12`**。
其成因為**構造性**——rebase 之二基座差（`c746d2dd` → `be1b326`）**恰 `1` 檔**：

```
docs/reports/W-G.9-208R_commit2_步驟0無條件與歸側四分支_執行報告.md
```

而該建議檔**不在其中**（其末次異動係 `1712aed`，二基座皆已含 `GB-140`）
⇒ 其於二基座**必然同值**、**與 rebase 是否生效無涉** ⇒ **判別力 ＝ `0`**。

**步二（以合判準之見證檔證受詞綠）**：單之判準逐字為「取**未參與本 commit** 之一檔」
（檔名係「建議」、非指定）。改取 `docs/reports/W-G.9-208R_commit2_…md`
（於本 commit 之 `--name-only` 命中 `0` ⇒ 確未參與）：

| 態 | blob |
|---|---|
| `0d88e1a5` | **（該樹內不存在）** |
| `0d88e1a5′` | `526247aad009ed423c51daf842e09497bdbe3027` |
| 主線頭 | `526247aad009ed423c51daf842e09497bdbe3027` |

⇒ **二者相異 ✅**，且**後者恰 ＝ 主線頭之同檔 ✅** ⇒ 判別力自證**綠**。

**併證（三獨立法）**：① `0d88e1a5 → 0d88e1a5′` 之檔差恰 `1`（＝二基座之差）
② 主線頭 `→ 0d88e1a5′` 之檔差恰 `2`（＝ `E-2`）
③ `commit`／`tree`／`parent` 三身分皆變。

**側分支重推**：`--force-with-lease=refs/heads/verify/W-G.9-198R-c2:0d88e1a5…`
⇒ `+ 0d88e1a...a1107a1 (forced update)`。
`§一-5` 主線**⛔ 前進**：現查 ＝ `S-0a` 實查值；關係式判準（`a1107a17` 之 `parent` ＝ 現主線頭）✅；
主線⛔ 含 `a1107a17` ✅。**⛔ 寫死舊常數**（承 `W-G.9-208` 之量測器紅）。

---

## §二　主線推進（發單側逐字放行「`0d88e1a5′` 等值已驗·准推主線」）

推前五閘全綠（放行之受詞——**二 blob ＋ 恰二檔**——逐項與已復驗之內容相符，**⛔ 某一 SHA**）。

`git push origin a1107a17825c37430f69349afaceef4d09c6f5f8:refs/heads/wip/s1-endpart`
⇒ `be1b326..a1107a1`

| # | 受詞 | 實測（量測時刻 `2026-09-02 17:32:23 +0800`） | 判 |
|---|---|---|---|
| ① | `ls-remote wip/s1-endpart` | `a1107a17825c37430f69349afaceef4d09c6f5f8` | ✅ |
| ② | `<新主線頭>:app.py` | `144349c834c2289434e1b0edbcfc8d0a27d88435`（併：`selection_pipeline.py` ＝ `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c`） | ✅ |
| ③ | `left-right` | `0	0` | ✅ |
| ④ | 側分支 `verify/W-G.9-198R-c2` | `a1107a17825c37430f69349afaceef4d09c6f5f8` **保留⛔ 刪** | ✅ |

🔴 **此推之土地意義**：本波**第一次分配結果改變**——`R4` 之 `628(1)` ＋ `628-1(1)` 合併為 `628(1)+`
並歸**右**街角，**左街角成為強制抵費地**（退縮 `3.5` m ＝ `226.01` ㎡／退縮 `0` m ＝ `116.08` ㎡）。

---

## §三　零生產碼登記批

### 三項 payload 之抽出

🔴 **節框用 `^### 三-\d`、⛔ 裸 `^### `**——`三-1`／`三-2` 之 payload **首列本身即 `### \`GB-…`**；
實測裸框於本單命中 **`6`**、正確框命中 **`4`** ⇒ 裸框會截斷區間（`W-G.9-208` 實踩之同族）。
三段皆**無巢狀圍欄**。

| 項 | payload | `sha256` | 落地檔 |
|---|---|---|---|
| `三-1` `GB-140` 更正註 | **`1878`** B | `01b33d9f969703121a37337904054b4407a991b20ca7b6fc6a9a31362490b169` | `docs/reports/W-G.4_泛用阻塞項登記表.md` |
| `三-2` `GB-141` | **`1304`** B | `d5270f1a214f49c3a79abb23bb02a72d3a2154b09a573918cbcdd3bebfd5b639` | 同上（置於 `三-1` 之後） |
| `三-3` `自誤 262` | **`1525`** B | `b26aea10118cb2f80db556ab359d13d22eba321f17d215c1a84eab00985bb131` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` |

### 🔴 落地中之量測器紅（⛔ 落地紅·已自捕並修）

首版驗證器以 `git hash-object`（**未帶 `-w`**）取 blob 之 `sha`，再以 `git cat-file blob <sha>` 讀其內容
——**物件未入庫** ⇒ `cat-file` 回**空**（`0` B）⇒ 嚴格前綴誤判為 `False`、Δ 誤報 `-580822` B。
**實際落地正確**（工作樹 `584387` → `586267`·Δ `1880` ＝ payload `1878` ＋ 補 `2`）。
🔒 **修法**：比較端改由**本地正規化**求得（`wt.replace(b"\r\n", b"\n")` ＝ `autocrlf=true` 之 clean filter），
並與 `git hash-object` 之輸出**交叉驗**（自算 `sha1("blob %d\0" % len + content)` ＝ git 之值 ⇒ 器可信）。
本例二法皆得 `b3a0ee54eb8185fc9566aa6e43ff53da5eafcd85` ⇒ 器綠。

### `T-1`　append-only 之位元組級自證（受詞取**倉側** blob·`戒 40` 二端並報）

| 項 | 工作樹 before → after（Δ） | **倉側 blob** 基座 → 現（Δ） | 嚴格前綴 | 檔尾 ≡ payload | 哨兵 | `CR` |
|---|---|---|---|---|---|---|
| `三-1` | `584387` → `586267`（`+1880`） | `580822` → `582702`（`+1880`） | ✅ | ✅ | `1` | `0` |
| `三-2` | `586267` → `587573`（`+1306`） | `580822` → `584008`（`+3186`＝`1880`＋`1306`） | ✅ | ✅ | `1` | `0` |
| `三-3` | `478501` → `480028`（`+1527`） | `473522` → `475049`（`+1527`） | ✅ | ✅ | `1` | `0` |

- 所補逐項具名：三項皆**前導換行 `1` B ＋ 收尾換行 `1` B**。
- `三-2` 之嚴格前綴以**基座（batch base ＝ 本批之 `HEAD`）**為比較端（`W-G.9-200` 之課），
  併驗「本次追加前之全文亦為前綴」（前 `582702` B）＝ ✅。
- 🔒 **行尾註**：`W-G.4_泛用阻塞項登記表.md` 之工作樹**本即非均質**（落地前 `CR 3565`／`LF 3584`，
  差 `19` 列係既有）⇒ 追加採**原樣 LF**；正規化後 `586267 − 3565 = 582702` ＝ 現 blob 長 ✅。
  `W-G.9波_claude.ai側自誤登記.md` 同（`CR 4979`／`LF 4993`）。
- 落地後 blob：`W-G.4_泛用阻塞項登記表.md` ＝ `2d69e09211dba4f96d0d33f7d62e955dafa17d5a`；
  `W-G.9波_claude.ai側自誤登記.md` ＝ `04399c10cbee92ad7a70de627b39313a142efc2d`。

### `T-2`　`GB` 之取號自證

| 態 | 相異 | MAX |
|---|---|---|
| 落地前（`HEAD` blob） | **`54`** | **`140`** |
| 落地後 | **`55`** | **`141`** |

**新增之號 ＝ `{141}`（恰一）** ⇒ `三-1` 之更正註標題（含既有之 `GB-140`）**⛔ 使二數再增** ✅
（單警：若相異得 `56` ⇒ 判紅）。`140` 於落地後之定義列**恰 `2` 列**（正面列舉）：

```
### `GB-140` 🆕　同一量（`priority_index`）於段一與段二採**相異精度**（`4dp` vs `6dp`）
### `GB-140` 更正註（`W-G.9-209`·`2026-09-02`）　第三處已現，且**已有土地後果**
```

### `T-3`　自誤 之取號自證

落地前 相異 **`226`**／MAX **`261`** → 落地後 相異 **`227`**／MAX **`262`** ✅。

### `T-4`　五檔生產碼 blob ⛔ 動

`app.py` `144349c8…`／`verify/selection_pipeline.py` `f878276b…`／`verify/run_verification.py` `d07955c5…`／
`verify/stepg_pipeline.py` `fef5865a…`／`verify/run_all.py` `e5cf9a22…`——**工作樹與 `HEAD` 逐檔皆同** ✅。

### `T-5`　母體

落地前 追蹤檔 **`1583`**／`.md` **`516`**。本批**新增恰二檔且皆 `.md`**（本單 ＋ 本報告），
另修既有二檔（二登記表）⇒ **機械推導** `1585`／`518`。
🛑 依 `自誤 251` 之戒（自指之數⛔ 綁「入倉後」），**實測值繫於提交後之具名量測態**，
載於本波之推後復驗（聊天回報），⛔ 於本檔內自宣。

### `T-6`　判別力自證　🟡 **依 `VR-089` 判**

| 錨 | 落地前 | 落地後 |
|---|---|---|
| `**現況之土地後果 ＝ 無**` | `1` | **`2`** |
| `` `GB-140` 更正註（`W-G.9-209` `` | `0` | **`1`** |

🛑 `T-6` **字面**作「仍命中 `1`」，而其**同格立法理由**逐字為
「**⛔ 得消失——證係 append 而非就地改**」。二者互斥，因 `三-1` 之 payload **本身即引用**該句
（`` `GB-140` 前文逐字載「**現況之土地後果 ＝ 無**」 ``）⇒ 落地後必為 `2`。
依 **`VR-089`（閘之字面與其同格立法理由互斥者 ⇒ **以立法理由為準**）**判之：

1. **原出處逐字仍在** —— 由 `T-1` 之嚴格前綴（前 `580822` B ≡ 基座全文）**蘊含** ✅
2. **落地後 ＝ 落地前（`1`）＋ `三-1` payload 內之數（`1`）＝ `2`** ⇒ 實得 `2` ⇒ **可預期、非任意** ✅
3. **⛔ 消失** ＝ `True`（此即 `T-6` 立法理由之直接受詞）✅
4. 錨② 落地後恰 `1` ✅ ⇒ **二者並存** ✅
5. 對照組（證器非紅）：`現況之土地後果 ＝ 有` 落地後命中 **`0`** ✅

⇒ **`T-6` 綠**。惟該閘之**字面**須由發單側更正（本報告⛔ 逕改單）。

---

## §四　🛑 上呈事項

1. 🔴 **`§一-3` 之見證檔須換**：單所建議之 `docs/reports/W-G.4_泛用阻塞項登記表.md` 於本次 rebase
   **判別力 ＝ `0`**（構造性·見 `§一`）。一般式 ＝ **見證檔須取「未參與本 commit」∧「參與基座之前進」之交集**；
   僅取前者不足。⛔ 由 CC 鑄號。
2. 🔴 **`T-6` 之字面須更正**：「仍命中 `1`」與其立法理由互斥（payload 自引該句）。
   建議改為「**⛔ 得為 `0`**，且總命中 ＝ 落地前 ＋ payload 內之數」。⛔ 由 CC 鑄號。
3. 🟡 **`GB-140` 處三 ＋ `GB-141` 須同批上呈 KL**（泛化波前置·本單⛔ 辦）——
   前者係「歸側之並列判定用 `4dp`」（🔴 **有土地後果**），後者係「`|_dual| ≥ 2` 之處理次序相依」
   （🔴 **有土地後果**·正典⛔ 載）。三種收法之土地後果相異 ⇒ **須先量測後裁**。
4. 🔵 **`抵費地0m(應空)` 之凍存期望**：依單 `§四-2` **本批⛔ 改**，登記為**已知紅**、繫 `GB-111`
   （KL 已裁「俟二閘落地後」重產）。
5. 🛑 **`commit 3`（段一上鎖）之開工條件已成就**（`§二`／`§三` 皆完成）：依 `-198R §五`
   （`R3-1`〜`R3-6`／`D3-a`〜`D3-e`），錨換代——`app.py` 判準值 ＝ `144349c834c2289434e1b0edbcfc8d0a27d88435`、
   基座 ＝ `a1107a17825c37430f69349afaceef4d09c6f5f8`。**`D3-a` 之基準 ＝ `commit 2` 落地後之態**，
   改前基線須**第三度重做**、檔名帶該態之短碼。走側分支 `verify/W-G.9-198R-c3`、備妥後停機。

---

## §五　次號（本批落地後）

自誤 **`263`**／`GB` **`142`**／`VR` **`090`**（三位補零）／戒 **`41`**／`K-9` **`27`**。
