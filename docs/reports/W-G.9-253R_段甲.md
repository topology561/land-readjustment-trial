# `W-G.9-253R`　段甲：工項零〜三（入倉二檔／`K-9-29` 入典／宣告框 `⑦`／自誤 `314`・`315`）

> 🔒 **⛔ 零生產碼**（十檔 ＋ 夾具與 `bf5980a` 逐位同·`app.py` ＝ `4860f10828fb8ae9`）⇒ **直推主線**。
> 🛑 段乙（`-251` `c1`／`c2`）與段丙（`-249` plan v2）**未辦**——側分支·見 `§七`。

---

## §零　開工閘（單 `§零` 四項·**全過**）

| 閘 | 判準 | 實測 |
|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart @ bf5980a` | `git ls-remote` ⇒ **`bf5980ab6ce717f71b59c505d734ec0554272eaa`**·本地 `HEAD` 同·工作區 clean ✅ |
| `2` 號占用 | `W-G.9-253` ＝ `D1 0`／`D2 0`／`D3 0` | **逐項相符** ✅（三軸 ＝ `blob@bf5980a`／全 `docs/` **`590`** 檔／檔框 ＋ 列框；鬆框亦 `0`／`0`）。對照甲 `-249` ＝ 檔 `5`／列 `6`（須 `≥1`）✅；對照乙 一人造號（⛔ 入倉·`GB-147`·代稱）＝ `0`／`0` ✅ ⇒ **器兩造皆能** |
| `3` 次號 | 自誤 `MAX 313`；`K-9 MAX 28` | **當場復算相符** ✅（框 ＝ `VR-091 四` 定義框＋範圍框·母體 ＝ 自誤簿單檔 `634299` B・`7460` LF：定義框列命中 `307`／重咬 `3`／純單筆 `304`／相異 `306`／`MIN 7`／**`MAX 313`**／缺號 `[106]`／恆等式 `True`）。`K-9` 框 `^### 🔒 K-9-[0-9]+`·母體 `K-6` 單檔 ⇒ **`MAX 28`**；對照組 `K-9-29` ＝ `0`（器須紅）／`K-9-28` ＝ `1`（器須綠）✅ |
| `4` `S-0b` | `P-3`／`P-5` 須綠 | **機械停機款 `0` 項**（`rc=0`）✅；`P-5` `SELF_SHA256` **相符**（受詞 `13818` B·實算 `149b573ef6038cf8…` ＝ 單載）⇒ **`S-0` 併綠** |

🟡 **`S-0b` 提示 `1` 項之逐項處置**（⛔ 靜默略過）：`P-2` ＠ `:62`——其命中之「框」係
**檔名字樣**（`段甲_工項零與工項四.md:1`），**⛔ `grep` 框** ⇒ **具名豁免（偽陽）**。

---

## §一　工項零：入倉二檔（`cp` 原封·⛔ 重打）

🔑 **單之所在**：二檔皆在**主 checkout 之工作區**（`…\docs\orders\`）而**⛔ 在倉**——**本窗第四度**。

| 檔 | 大小 | 來源 `sha256[:16]` | 落檔 | 判 |
|---|---|---|---|---|
| `W-G.9-253_K-9-29入典_廢步驟0與a′合併_249改單_宣告框⑦_251放行.md` | `13896` B | `42d4953a2cda3b94` | **同** | ✅ |
| `交接文_W-G.9-252.md` | `9318` B | **`bcaf32a74b88f89b`** | **同** | ✅ **與單 `§一 b` 所載期望逐位符** |

🔒 **`交接文_W-G.9-252.md` 係 KL 一併貼給 CC** ⇒ 單 `§一 b` 之條件成就，**已入倉**；
🛑 **⛔ 執行其所列任何工項**（單逐字）。
🛑 本閘只證「落檔 → 入倉」，**⛔ 證「聊天原文 → 落檔」**（`追加四` 逐字）。

---

## §二　工項一：`K-9-29` 入典 ＋ 更正標記

**落點**：`docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔末追加**——
`### 🔒 K-9-29　**重劃前地價逐筆評定；廢前置合併；一切合併恆走 \`a′\`；五級之入池閘**（KL 裁 `2026-09-08`·`W-G.9-253`）`
（標題形取既有 `K-9-28` 為範本·九款逐字入典·`> **KL 逐字**` 引文原樣保留）。落地後 `K-9 MAX ＝ 29` ✅

### 🩸 單內自相矛盾之回報（`常規二`·**取保守項續辦**·⛔ 上呈 KL）

單 `§二` 末逐字令「**同批更正標記（皆末端追加**·⛔ 動原句）」，其下卻逐字令
「`K-6:57-60` 步驟 0 **節末**追加一列」「`K-9-21`／`K-9-24 一`／`K-9-22` 各**節末**同形追加」
——🔴 **「皆末端追加」與「各節末追加」互斥**（節末係**檔中間**插入，破壞 `wg942_append_audit`
之嚴格前綴，並使其後全體行號位移）。
🔒 **CC 依 `常規二` 取保守項（檔末追加）**，於 `K-6` 檔末立一節，內以**字樣錨**逐則具名四受詞，
**⛔ 動任一原節一字**。**該互斥請發單側當場裁並具名為發單側自誤**（`常規二`：⛔ 上呈 KL）。

### 🔑 唯一性之判別力自檢（`行號衛生` 第 `4` 款·**當場實測**）

| 母體 | 錨 | 鑄前 | 落地後 | 寬字樣（須 `>` 鑄前）| 對照組（須 `0`）|
|---|---|---|---|---|---|
| `K-6` | 四錨（`### 步驟 0：同街廓相鄰合併（前置·無條件）`／`### 🔒 K-9-21`／`### 🔒 K-9-24`／`### 🔒 K-9-22`）| **各 `1`** | **各 `3`** | `步驟 0` `25`／`K-9-2` `76`／`### 🔒 K-9-` `66` | `0` ✅ |
| `CLAUDE.md` | `` `K-9-22` 落地（原地號保留 ＋ 區段回退） `` | **`2`** 🔴 **⛔ 唯一** | `3` | `K-9-22` ＝ `4` | `0` ✅ |
| `v3` | `**a' 折算雙模式**：`／`7-1 級0/0'→**wf_f0**` | 各 `1` | 各 `2` | `a'` ＝ `25` | `0` ✅ |

🔴 **`CLAUDE.md` 之錨命中 `2`（⛔ 唯一）**：二列逐字相同、分屬「更正二」與「更正三」之待落地表
⇒ 本標記**一併**指該二列（已於標記內具名）。

### 其餘三處更正標記（**皆檔末追加**）

| 檔 | 內容 |
|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 依賴序於 `4  五級實作` 之**前**插入 `3′ K-9-29 碼側落地（廢步驟 0·a′·入池閘）`（🔴 有土地後果·另單·逐 commit 放行）|
| `docs/配地計算總規格_v3.md` | `a′` 式升為一切合併之唯一式；級 `0`／`0′` 之 `Σa` 改 `a + a′` |
| `CLAUDE.md` | `K-9-22` **作廢·無須落地**之標記（`§八` 令一併加）|

🩸 **`v3` 之逐字與單所引之別（CC 現查·⛔ 頂替）**：單載 `a′ = a × p(原位置) / p(標的)`，
而 `v3` 該款之**倉內逐字**為**雙模式**（目標街廓該歸戶地／`p_average` 之別）
⇒ 單所引係其**簡寫**；**實作時以 `v3` 之雙模式逐字為準**（已於標記內具名）。

---

## §三　工項二：宣告框補款 `⑦`（`CLAUDE.md` 檔末追加）

`⑦ 列框之聯集`：`列框 ＝ |D2 之列 ∪ D3 之列|`；`D2`／`D3` 仍分列出艙，並明載雙屬列數；`檔框` 一字不動。

---

## §四　工項三：自誤 `314`／`315`（自誤簿檔末追加·標題形取 `313` 為範本）

`314`　宣告框之列框私自實作為聯集而出艙時載 `⑥` 之名（`293`／`295`／`313` 同族·第四度）
`315`　重問 KL 已答之題（`290` 同族·第二度）

🔒 **CC 之復算佐證（⛔ 轉述單）**——`blob@bf5980a`／全 `docs/` `590` 檔：
CC 之器依 `⑥` **逐字**實作（`D2`／`D3` 為**獨立 `if`**）⇒ `-251` 得 `D2 11`／**`D3 2`**／列框 **`13`**，
而發單側呈 KL 者為 `D3 1`／列框 `12` ⇒ **差 `1` 恰為雙屬列** ⇒ 受詞坐實。
`315` 之受詞亦坐實：CC 之排程解讀確已於 **`6ce7017`** 入倉於 `-251R` 停機報告檔尾。

---

## §五　【驗收】

| 閘 | 判準 | 實測 |
|---|---|---|
| `V-A` | 十檔＋夾具與 `bf5980a` 逐位同 | ✅ **十一項全同**（`app.py 4860f10828fb8ae9`）；生產碼五檔之判法指令**空輸出**（`rc=1`）|
| `V-B` | 七檔 `numstat` deletions 皆 `0` | ✅ `CLAUDE.md 45 0`／`-253 單 148 0`／`交接文 160 0`／`W-G.4 27 0`／`自誤簿 54 0`／`K-6 58 0`／`v3 41 0` |
| `V-C` | 自誤 `MAX` 前 `313` 後 **`315`**、缺號仍 `[106]`、恆等式 `True`；`K-9 MAX` 前 `28` 後 **`29`** | ✅ 全符（後態：定義框列命中 `309`／重咬 `3`／純單筆 `306`／相異 `308`／恆等式 `True`·次號 `316`）|
| `V-D` | `-248`／`-249`／`-251` 各雙屬 `1`、聯集列 ＝ `D2＋D3−1`；`-253` 三數 `0` | ✅ `-248` `7→6`／`-249` `6→5`／`-251` `13→12`；`-253` 與人造號皆 `0` ⇒ **器兩造皆能** |

---

## §六　🩸 CC 之入倉前自捕**二度**（⛔ 出艙·⛔ 鑄號）

| # | 事由 |
|---|---|
| `1` | 首版之四片段載「錨於本檔命中 `1`」，係**鑄前**值而**未具名自指增長**；且 `CLAUDE.md` 之錨**鑄前實為 `2`**（我寫 `1`·**數字錯**）⇒ 還原五檔重做 |
| `2` | 🔴 **同形第二度**：修正版寫「落地後各為 `2`」，實測 **`3`**——因修正版之**表**與**自檢句**各含該四錨**一次**（增 `2`，非 `1`）⇒ **在修正自指增長時又製造了新的自指增長**（`CLAUDE.md`「立戒者在下一批最容易違反自己的戒」）⇒ 再還原 `K-6` 重做 |

🔒 **通則（自本則起·⛔ 鑄號）**：出艙「落地後之命中數」前，須**先對 payload 自身逐錨計數**，
**⛔ 以「本節含該錨」逕推增量為 `1`**。
🔒 二度皆於**入倉前**捕獲並替換 ⇒ **⛔ 出艙**（體例同 `W-G.9-150′R`）。

---

## §七　待辦

| 段 | 內容 | 狀態 |
|---|---|---|
| 乙 | `-251` `c1`（工項二＋三·路名管理／`RD` 下拉）／`c2`（工項一丁案）| ⬜ **未辦**·側分支 `verify/W-G.9-251-c1`·`V-F` 由 KL 目檢後候放行 |
| 丙 | `-249` plan v2（六裁已下·`B-1`〜`B-6` 依 `-249R` `§三` 之修正形辦·`B-6` 插入行號 `:13674` 准）| ⬜ **未辦**·plan v2 完成即送 reviewer·reviewer 綠後方得動生產碼·側分支 `verify/W-G.9-249-c1` |
| （登記）| `K-9-29` 碼側落地（🔴 **有土地後果**·`R4` `226.01 ㎡`）／`-249` 觀測於步驟 0 廢除後重跑 | 單 `§八` 已登記·**另單** |

🛑 **乙／丙互不相待**（單 `§七` 逐字）；同一 session 內**⛔ 混同 commit**。
