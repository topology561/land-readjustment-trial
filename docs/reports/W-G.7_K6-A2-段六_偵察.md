# W-G.7／K-6-A2 **段六 開工偵察**（K-9-5 ＋ K-9-1 ＋ GB-18 ＋ GB-39）

**基準**：`wip/s1-endpart` @ `1f7e84b`（段五收官 commit 1 後）。
⛔ **零碼面變更**——`app.py`／`verify/wf_*.py`／`verify/fixture_*.py`／`verify/run_all.py` **一行未動**；
本波唯一新增之可執行檔 ＝ `verify/probes/probe_K9_seg6_recon.py`（**不接 `run_all`**）。
**實料**：`verify/out/probe_K9_seg6_recon.log`。

> 🔴 **全篇通則**：斷言「X 不存在」一律附**窮舉搜尋清單**（含大小寫變體與別名）
> ——節 44 之教訓（`baseline` 命中 0 而 `BASELINE` 命中 1）。⛔ 本文無裸斷言。

---

## 〇、六題之結論一覽

| 題 | 結論 | 是否阻塞段六 |
|---|---|---|
| **4-1** K-9-5 落地 | 碼面 **13 處**提及、**全數為註解或他則之程式化**；K-9-5 本體（實量＋兩門檻分流）**零實作** | 否（＝段六本體） |
| **4-2** K-9-1 | ✅ **存在**——在 K-9 五則**表列**（非 `###` 標題，故標題 grep 未命中）＋落地矩陣 | 否·**不需 KL 補立** |
| **4-3** 兩門檻 | 「退縮寬」**已是既有案件參數**（`setback`·UI 已有欄）；合成門檻 `T = setback + min_width` **碼面已有二處在算** | 否·**不需新 UI 欄** |
| **4-4** GB-18 接線前提 | ✅ `_n14_band_geom` 於虛擬塊上 **12/12 成立**（raise 0）——前提比預期好 | 否；🔧 **但 `depth_from_PQ` 係待改正之陳舊量**（受詞錯·**非待裁**·見 4-4-3） |
| **4-5** GB-39 可觸發性 | `on_chamfer` ＝ **False 12 格／`—` 4 格**⇒ **本案零觸發** | 否（惟**不得**外推至換案） |
| **4-6** 土地後果量級 | 3.5m 情境下 **2 格**低於街角門檻（`R1/right −2.83`、`R6/right −2.19`）——**與 S6-1 逐格相符** | 否·屬 **S6-1**（已在待裁清單） |

---

## 一、4-1 K-9-5 逐條落地對照

### 1-1 正典逐條（標題 grep：`grep -n "K-9-5" docs/rulings/K-6_*.md | grep "^[0-9]*:#"`）

| 條 | 正典行 | 內容 | 碼面 |
|---|---:|---|---|
| **K-9-5**（五則之一） | `:349` | 街角第 1 宗**寬度要實量**，門檻＝退縮寬＋畸零地最小寬；其餘＝畸零地最小寬 | ⬜ **零實作** |
| **K-9-5-2** | `:501` | 街角最小規定範圍之構成與「面積夠 ⇒ 寬度合格」 | 🔶 **③ 已程式化**（見下） |
| **K-9-5-2-a** | `:525` | 對實作之直接效力 | — |
| **K-9-5-2-b** | `:535` | ② 之實證 | — |
| **K-9-5-1** | `:545` | 正面路寬未填 ⇒ **停機要求補填** | ⬜ 未查（不在本單範圍） |

### 1-2 碼面 13 處逐處判讀（`grep -n "K-9-5" app.py`）

| 行 | 性質 | 內容 |
|---:|---|---|
| `2004` | **註解** | 「K-9-5 已撤銷本豁免」之提示 |
| `6909`／`6911` | **註解** | 同上＋門檻定義之逐字 |
| `6944`／`6946` | **註解**（docstring） | `evaluate_parcel_width_n14` 之過渡態說明 |
| `7028` | **註解**（docstring） | 同上 |
| `9583`／`9585` | **註解** | 同上 |
| `20564` | **註解** | 生產區塊之過渡態說明 |
| `10041`／`10043` | **註解**（docstring） | K-9-5-2 ③ 之二半歸屬表 |
| `10199`／`10205` | 🔴 **實作** | **後置斷言**（K-9-5-2 ③ 之程式化）·`k97_solve_alloc_t` 內 |
| `10424`／`10435` | 🔴 **實作** | 第二層之解以**同一組後置斷言**重驗 |

⇒ **判讀**：碼面之 K-9-5 實作**僅有 K-9-5-2 ③ 之後置斷言**（屬街角規定範圍之構造保證）；
**K-9-5 本體**（街角第 1 宗寬度實量 ＋ 兩門檻分流）**完全未實作**——與正典落地矩陣一致。

---

## 二、4-2 K-9-1 之正典位置與落地狀態

🔴 **施工單稱「以標題 grep 未命中」屬實，但 K-9-1 確實存在**——它是**表列**、不是 `###` 標題。

**窮舉清單**（正面列舉受檢檔·⛔ 非單一 regex）：

| 識別字 | `docs/rulings/K-6_*.md` | `app.py` | 判讀 |
|---|---:|---:|---|
| `K-9-1` | **2**（`:345` 五則表／`:1017` 落地矩陣） | **1**（`:6949` 註解） | ✅ 存在 |
| `K9-1` | 2（同上之子字串命中） | 0 | — |
| `街廓最小建築面積` | **6**（`:21`／`:345`／`:354`／`:356`／`:359`／`:1017`） | 1（`:6949`） | 其**判準本體**在 `:354`〜`:359` |
| `不豁免` | 2 | 1 | — |

**正典逐字**（`:345`）：

> **K-9-1**｜街角第 1 宗**不豁免** §零-2「街廓最小建築面積」｜U-K7-1 定案（原為未裁）

**判準本體**（`:353`〜`:359`·K-9 §一-1「合併後之分配成果判準」）：

- 街角第 1 宗：`宗地寬度（截角前） ≥ 退縮寬 ＋ 畸零地最小寬` **且** `G ≥ 街廓最小建築面積`
- 第 2 宗以後（含末端塊）：`宗地寬度 ≥ 畸零地最小寬` **且** `G ≥ 街廓最小建築面積`
- 深度：**兩者皆不逐宗量**（K-9-2 ＋ K-9-4 保證）
- **街廓最小建築面積**係使用者輸入、per 街廓、**未輸入即不適用**（§零-2 既鎖）

**落地狀態**（`:1017`）：⬜ **未實作（段六）**——「§零-2 街廓最小建築面積之 UI 欄與判準」。

⇒ ✅ **不觸發 §八-1 之停機**（K-9-1 存在、無須 KL 補立或作廢）。
⚠️ 惟其**與 §零-2 UI 欄綁定**：未輸入即不適用 ⇒ 本案若不輸入，K-9-1 之判準**恆真**、無土地後果。

---

## 三、4-3 兩門檻分流

### 3-1 「退縮寬」之資料來源：**已存在·非新欄**

| 問 | 答（附錨） |
|---|---|
| 碼面何處？ | **`setback`** ＝ 既有**案件級參數**。UI 欄已有：`grep -n "key='f3L_setback_default'" app.py`（`:17479`·`st.number_input`），預設 `3.5` |
| 其他消費點 | **正面列舉**：`grep -n "f3L_setback_default" app.py` ⇒ **9 命中**——`:10891`／`:10893`／`:10896`（側街退縮·**已有 no-silent-fallback 之 loud raise**「禁靜默 3.5 兜底」）、`:12053`（`ctx["setback"]` 之 `_need(...)`）、`:17474`／`:17479`（UI 欄本體）、`:19920`／`:20409`（下游消費）、`:21371`（W-G 標籤） |
| 驗證側 | `rv.build_param_table(..., setback)`／`run_corner_pk(..., setback)`／`run_step_g(..., setback)`；兩情境即 `setback ∈ {0.0, 3.5}` |

⇒ 🔴 **§八-2 之停機條件不成立**——「退縮寬需使用者輸入而現行 UI 無此欄」**與倉內事實不符**：**欄已存在**。
⚠️ **真正待補之 UI 欄是另一個**：**§零-2「街廓最小建築面積」**（per 街廓·K-9-1 所需）
——`grep -n "街廓最小建築面積" app.py` ⇒ **1 命中且為註解**（`:6949`）⇒ **UI 欄確不存在**。
⇒ **二者勿混**：`setback` 已有；`街廓最小建築面積` 沒有。

### 3-2 合成門檻 `T = 退縮寬 ＋ 畸零地最小寬`：**碼面已有二處在算**

`grep -n "退縮寬＋畸零地最小寬 ＝ {T}" app.py` ⇒ **`:10111`**／**`:10588`**
（`k97_solve_alloc_t` 與 `_build_corner_range_v3`·二處皆 `T = float(setback or 0.0) + float(min_width or 0.0)`，
且皆 `T <= 0` ⇒ `_stop`「⛔ 禁靜默 3.5」）。
⇒ 段六**不需新造該式**；⛔ 亦**不得**再寫第三份（GB-8）。

### 3-3 `_min_width_by_block` 之語意與兩門檻之歸屬

`grep -n "_min_width_by_block" app.py` ⇒ `:20543` 建、`:20551` 填、`:20591` 用（**正面列舉·三處**）。
其值 ＝ `get_min_lot_size(...)['min_width']` ＝ **畸零地最小寬**（**不含**退縮寬）。

| 宗別 | 門檻 | 現行碼 |
|---|---|---|
| 第 2 宗以後（含末端塊） | 畸零地最小寬 | ✅ `_min_width_by_block` **正是此值** ⇒ 段六**不需改** |
| 街角第 1 宗 | 退縮寬 ＋ 畸零地最小寬 | ⬜ 現行**根本不到**（K-4 面積門檻豁免提前 return） |

⇒ **分流之作法**：街角第 1 宗改走 `k98_virtual_measure_block` 之量得寬（GB-18／GB-39），
其門檻取 `setback + min_width`（碼面已有式）；`_min_width_by_block` **語意不變、續供非街角宗**。

---

## 四、4-4 GB-18 落地面盤點（**本題最重**）

### 4-4-1 接線前提：**比預期好**

| 項 | 現況 | 錨 |
|---|---|---|
| `k98_virtual_measure_block` 簽章 | **已含** `baseline_pts`（第 3 位） | `grep -n "def k98_virtual_measure_block" app.py` ⇒ `:9820` |
| 生產呼叫點 | **0**——呼叫者僅**二支探針**（`probe_k98_virtual_block.py:240`／本波新增之 `probe_K9_seg6_recon.py:161`），餘為定義與註解 | `grep -rn --exclude-dir=.claude "k98_virtual_measure_block" --include="*.py" .` |
| 回傳契約 | `label`／`pid`／`lot_kind`／`poly`／`P_block`／`Q`／`inset_from_front`／`on_front`／`on_chamfer`／`depth_from_PQ`／**`min_width`**／`area_DO_NOT_USE` | `app.py:9992`〜`:10001` |
| `evaluate_parcel_width_n14` 輸出契約 | `寬度判定`／`實際寬度(m)`／`_below_min_width`／`_width_by_area_gate`／`_width_k4_G`／`_width_k4_threshold` | 該函式各 `return`／`_out` |
| **差在哪些欄** | k98 **不產** `寬度判定`／`_below_min_width`（它**只量不判**）；`evaluate_*` **不產** `poly`／`P_block`／`on_chamfer` | ⇒ 段六須加一層**判定**把 k98 之 `min_width` 轉為 `寬度判定`，⛔ 不得由 k98 代判 |

**街角第 1 宗之早退點（正面列舉·`corner_min_area` 之完整消費鏈）**：

```
grep -n "corner_min_area" app.py
```
⇒ `evaluate_parcel_width_n14` 之簽章與其 K-4 分支（`WIDTH_VERDICT_CORNER_K4`／
`_width_k4_threshold`），以及生產呼叫端之 `corner_min_area=_thr_jw`
（`_thr_jw` ← `st.session_state['f3_corner_range_areas'][街廓][側]`）。
⇒ **撤銷豁免＝拆掉該 `if str(row.get('街角地')).strip() == '是':` 之提前 return**，
並改路由至 k98。

### 4-4-2 🔴 `_n14_band_geom` 於虛擬塊上**適用**（12/12·逐格實跑）

實料 `verify/out/probe_K9_seg6_recon.log`【A】：

- **成立 12 格／raise 0 格／未達 k98 4 格**（後者皆為 `無 winner（強制留設抵費地）`）。
- `t_lo` **全 12 格 ＝ 0.000000** ⇒ `_fe` 以 `PQ` 為 `t=0` 之判準成立（`front_pt` 傳 `P_block`）。
- 後緣 `|dep|` 上界 **9.506e-11**（遠小於容差 `0.01`）⇒ `_re` 判準成立；`n後緣` 全格 ＝ 2。
- ⇒ ✅ **§八-3 之停機條件不成立。**

### 4-4-3 🔴 `depth_from_PQ` 係**待改正之陳舊量**（⚠️ **非**待裁之「二選一」）

> 🔧 **本節已更正（KL 2026-08-06）**。初版記為「前置決策項·須擇一」，**判讀有誤**：
> **判準已在倉內、非待裁項。** 原文保留於下方 4-4-3-a 以存查。

逐格 `max|Δ|` ＝ **`0.120267 m`**（log【A】之 `Δ(帶底−k98深)` 欄與其下之 A-2 段）
——⚠️ **大於法定粒度 `0.01 m`**，⇒ **不是浮點噪訊**。

**依據（三錨·當場重 grep 皆命中）**：

| # | 錨 | 逐字 |
|---|---|---|
| 1 | `app.py:9954` | `depth_from_PQ = min(_dep(P_block), _dep(Q))` |
| 2 | `app.py:9934` | `P_block = max(_cands, key=_dep)　# 臨街側＝離 BASELINE 最遠` |
| 3 | `app.py:9941` | `Q = _x2(PQ_inf, L_cor_inf, "PQ ∩ 靠街角側境界線")` |

⇒ `P_block` 與 `Q` **皆在 `PQ` 上** ⇒ `depth_from_PQ` 之 min **取自臨街段之端點**
⇒ 其**受詞為「臨街段」**——**正是 KL 於 2026-08-05 推翻之受詞**
（法規原文受詞為**後側境界線**·見 **失敗考古 節 42**）。

而正典早已逐字定式（`grep -n "通過後緣連續段之最淺端點且平行 PQ" docs/rulings/K-6_*.md` ⇒ `:703`）：

> 帶 ＝ `[PQ, 通過後緣連續段之最淺端點且平行 PQ 之直線]`

⇒ 🔴 **二者不是「兩個合法選項」，而是「已被推翻者」與「現行定式」**
——`depth_from_PQ` 與舊帶底 `t_lo + D·(b̂n·h)` **是同一個受詞錯誤的兩個落點**。
⇒ **段六隨 GB-18 一併改正**；⛔ **不必上呈、不必擇一**。

**現行 k98 係把 `depth_from_PQ` 當 `min_depth` 餵給 `parcel_min_width_n14`**
（`grep -n "min_width = float(parcel_min_width_n14" app.py`）⇒ **走舊帶分支**
⇒ 段六改走新帶時，帶底由 `_n14_band_geom` 定，`depth_from_PQ` 之**該用途即消滅**。

#### 4-4-3-a 🗄️ 初版原文（**存查勿刪**）與其教訓

> 初版寫：「⇒ **二者須擇一**。⛔ **本文不代判**——此為量測定義之選擇，
> 體例同 **K-9-6-a**（兩個「最小深度」同名不同量）。」

**錯在哪**：見到「兩個量不一致」即歸入 **K-9-6-a 之「同名不同量·嚴禁互代」**體例
（該體例之二量**皆為合法量、各有用途**），**未先 grep 正典有無已定式之組合條款**。
🔑 **可執行之戒**：遇「兩個量不一致」時，**先查正典是否已定式**，
再判斷是「兩個合法選項」還是「一舊一新」。
⚠️ **謹慎地往上呈仍優於自行代判**——但**先查正典比判斷「像不像兩個合法選項」快**。

### 4-4-4 `_require_baseline` 在此路徑之落點

現行 k98 呼叫 `parcel_min_width_n14` **未傳** `baseline_pts` ⇒ 走舊帶。
若段六改走新帶，`_require_baseline` 應落在 **k98 之呼叫端**（即 `k98_virtual_measure_block` 內），
⛔ **不在 `evaluate_parcel_width_n14`**——因街角第 1 宗撤銷豁免後**不再經過該早退鏈**，
其「查無 BASELINE」之停機責任移轉至 k98。
🔒 k98 之簽章**已收** `baseline_pts` 且已用於建虛擬塊（K-9-8 (4) 之遠側界）
⇒ 該處**本就必須有** BASELINE，缺件已由既有 `_stop` 擋下 ⇒ **可能無須新增旗標**（段六確認）。

---

## 五、4-5 GB-39 之可觸發性：**本案零觸發**

實料 log【B】。🔒 **資料源 ＝ k98 之既有契約旗標 `on_chamfer`**，⛔ 本檔未新造判定式。

- **分布：`False` 12 格／`—`（未達 k98）4 格** ⇒ **無任何一格之 `P_block` 落在道路截角邊上**。
- ⇒ **GB-39 之假停機於本案不會發生**；⛔ 但**不得**外推——該旗標為 per-case 幾何。
- 📌 **附帶觀測**：`R4/right 628-1(1)` 之 `on_front = False` 且 `inset = +0.013039`（> 容差 `0.01`）
  ——即 `P_block` **既不在 FRONT 重疊段、也不在截角上**。此即 **U-K9-2**／**K-9-6-e** 所溯之
  `−1.4826e-2m` 同一格；其處置**已裁為「修圖」**、待波末批生效。

⚠️ **契約旗標之存活未查**：本文之 `on_chamfer` 係**直接呼叫** k98 取得。
其於生產路徑（`_build_corner_range_v3`）是否被靜默丟棄（已登記之事），**本批未查證**
——⛔ 不得以本節之有值推斷生產路徑亦有值。**列為段六前置。**

---

## 六、4-6 土地後果量級（⛔ 只鋪數字·**不設門檻**）

> 🔧 **本節之數已由 GB-40 切換取代（K-6-A2 段六前置收官批·KL 放行 2026-08-06）**
> ——⛔ **下表為切換前之舊圖像·存查勿刪，但不得再作為前提**（失敗考古 **節 36**）。
> **`S6-1` 之圖像隨之改變**：退縮 3.5m 下低於門檻 `7.00` 者
> **由二宗（`R1/right`＋`R6/right`）減為一宗（僅 `R1/right`·餘裕 `−0.152969`）**；
> `R6/right` 已翻為 `7.221163`（餘裕 `+0.221163`）。
> **現行數**：`docs/reports/W-G.7_K6-A2-段六_GB40寬度攤表.md`
> ／實料 `verify/out/probe_K9_seg6_recon.log`【C】（**已隨切換重跑**）。

實料 log【C】。門檻欄僅為**對照**，⛔ 非判定。

| 情境 | 街廓/側 | 街角第 1 宗 | k98 量得寬 | 街角門檻（退縮＋最小寬） | 差 |
|---|---|---|---:|---:|---:|
| 0m | R1/left | 628-37(1) | 4.142051 | 3.50 | +0.642051 |
| 0m | R1/right | 628(5) | 4.172583 | 3.50 | +0.672583 |
| 0m | R2/left | 628-40(1) | 4.758958 | 3.50 | +1.258958 |
| 0m | R4/left | 628(1) | 43.847789 | 3.50 | +40.347789 |
| 0m | R4/right | 628-1(1) | 13.190253 | 3.50 | +9.690253 |
| 0m | R5/left | 628-7(2) | 3.853085 | 3.50 | +0.353085 |
| 0m | R6/right | 628-18(1) | 4.810794 | 3.50 | +1.310794 |
| 3.5m | R1/left | 628-36(1) | 15.232700 | 7.00 | +8.232700 |
| **3.5m** | **R1/right** | **628(5)** | **4.172717** | **7.00** | **−2.827283** |
| 3.5m | R4/left | 628(1) | 43.847789 | 7.00 | +36.847789 |
| 3.5m | R4/right | 628-1(1) | 13.190253 | 7.00 | +6.190253 |
| **3.5m** | **R6/right** | **628-18(1)** | **4.810794** | **7.00** | **−2.189206** |

🔬 **與既有登記之交叉複驗**：交接文 §二 之 **S6-1** 載「3.5m 下 `R1/right`（三代寬 **4.1727**）與
`R6/right`（**4.8108**）低於 K-9-5 門檻 7.00」——**本次逐格相符**
（`4.172717` / `4.810794`·差在末位·異源同值）⇒ S6-1 **獨立復現成立**。

⛔ **本表不是判定表**：K-9-5 尚未上閘、街角宗現走 K-4 豁免。門檻之裁定屬 KL（S6-1 已在待裁清單）。
⚠️ **0m 情境下 7 格全數為正**，⇒ 土地後果**集中於 3.5m 情境**；⛔ 不得由 0m 之全綠外推 3.5m。

---

## 七、段六之前置清單（本偵察之產出）

| # | 前置 | 性質 |
|---|---|---|
| 1 | 🔧 **`depth_from_PQ` 之改正**（受詞為臨街段·節 42 之同族·Δ 達 `0.120267 m`） | **待改正之陳舊量**·⛔ **非待裁項**·隨 GB-18 一併改正（見 4-4-3） |
| 2 | `on_chamfer` 等契約旗標於 `_build_corner_range_v3` 之存活 | 查證（本批未查） |
| 3 | **§零-2「街廓最小建築面積」UI 欄**（K-9-1 所需·`setback` 已有、此欄沒有） | 新增 UI |
| 4 | 撤銷 K-4 豁免之早退 return ＋ 路由至 k98 ＋ 加判定層 | 實作 |
| 5 | `_require_baseline` 之落點移至 k98（或確認既有 `_stop` 已足） | 實作 |
| 6 | **S6-1**（3.5m 之 2 格不足）之去向 | 🛑 **待 KL 裁** |

---

## 八、本批之驗收（§六）

| # | 項 | 結果 |
|---|---|---|
| 1 | `git diff --numstat e73a6c5..HEAD -- app.py verify/wf_*.py verify/fixture_*.py verify/run_all.py \| wc -l` | **0** |
| 2 | 新探針不接 `run_all` | 正面列舉：`probe_K9_seg6_recon` 於 `verify/run_all.py`／`verify/stepg_pipeline.py`／`verify/run_verification.py` 命中皆 **0**；⛔ 本批**未跑** `run_all` |
| 3 | 每題皆有窮舉 grep 或明確「查無」 | §二（K-9-1 四識別字表）／§三-1（`f3L_setback_default` 4 命中）／§三-3（`_min_width_by_block` 三處）／§四-4-1（k98 呼叫點）／§五（`on_chamfer` 分布） |
