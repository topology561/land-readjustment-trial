# W-G.4 CC 自驗：「守恆 <6 不存在」坐實有誤·守恆實為真碼閘·上呈 claude.ai

- 日期：2026-07-15；規格＝`W-G.4_規格_v3.md` N0-17-b（claude.ai 裁）
- 性質：**CC 禁採信·自驗 grep·抓到 claude.ai 坐實與倉態衝突**（對稱紀律雙向——前數輪 claude.ai 抓 CC 漏，本輪 CC 抓 claude.ai 誤）
- 處理：依 CLAUDE.md「數字錨引倉檔·與倉態衝突以倉為準·回報」——規格 ground truth 全文入倉，但 N0-17-b 實證據實標記衝突、上呈 claude.ai 覆核

---

## 一、claude.ai 之陳述（規格 N0-17-b／交辦「守恆<6」段）

> 「守恆 <6」不是閘——僅見於 `verify/run_verification.py:833` **註解**與 `PROVENANCE_F4` **敘述**。**碼中無此閘。**
> 全區守恆從未設閘：3.5m 全區殘差 1.19 > 1.0，然因閘為逐街廓（各 <1.0）→ 全綠。

## 二、CC 自驗 grep（禁採信·倉態坐實）

```
grep -n "cons_resid < 6" verify/run_verification.py
```

| 檔:行 | code | 性質 |
|---|---|---|
| `run_verification.py:833` | `# …F.4 守恆殘差<6，`（**註解**） | 散文（claude.ai 對此正確） |
| `run_verification.py:888/889` | `results.append((f"F.2 跨街廓守恆{tag}（…全區殘差 {_a2['cons_resid']}<6㎡）", **_a2["cons_resid"] < 6.0**, …))` | **真 code 閘** |
| `run_verification.py:951/952` | `… **_a3["cons_resid"] < 6** …`（F.3） | **真 code 閘** |
| `run_verification.py:1009/1010` | `… a4["verdict_all_green"] and **a4["cons_resid"] < 6** …`（F.4） | **真 code 閘** |

**逐街廓守恆閘**（另一組·claude.ai 對此正確）：`stepg_pipeline.py:813` `abs(_resid_wd2) < 1.0`／`:846` 停機③「殘差 ≥1㎡」／`app.py:15341/15379` 同構。

## 三、坐實結論（與 claude.ai 衝突處）

1. **「守恆<6」是真 code 閘·非散文閘**：`run_verification.py:888/951/1009` 之 `cons_resid < 6.0` 作 `results.append` 之 bool 判定，直接進 run_all 綠/紅。claude.ai「碼中無此閘」**有誤**（僅 grep 到 833 註解，漏 888/951/1009）。
2. **全區守恆有閘·非「從未設閘」**：`cons_resid<6.0` 就是**全區/跨街廓守恆閘**（F.2/F.3/F.4）。3.5m F.4 全區殘差 1.19 < 6.0 → 全綠——**因閘寬 6.0，非因無閘**。claude.ai「全區守恆從未設閘」**有誤**。
3. **兩個守恆閘並存**：全區 `<6.0`（run_verification）＋逐街廓 `<1.0`（stepg/app.py）。

## 四、N0-17 主論點仍成立（僅定性須修）

- **N0-17 核心成立**：閘寬 6.0（全區）與 1.0（逐街廓）**皆無原理依據·皆殘餘定閘**。6㎡=**432,352 元**、1㎡/街廓=72,059 元——「43 萬元以內不保證」不是可交付產品規格。**甲-2 守恆改必立（6.0→0.01、1.0→0.01）仍完全正確**，只是理由從「散文閘不存在」更正為「**真閘·但閘寬無依據**」。
- **N0-17-b「散文閘」定性不適用 <6**：<6 有碼（888/951/1009），非散文。N0-17-b 之現行演示（claude.ai 舉「守恆<6」為散文閘例）**須換例**——「守恆<6」反而是「**真閘·閘寬無依據**」之例（屬 N0-17 主論而非 N0-17-b）。
- **N0-17-b 本身仍成立**（作為正典）：白縫舊閘 `<1㎡`（W-D plan·**確為散文·grep run_all/tests/CLAUDE.md 空**·CC 前已自驗）才是真正的散文閘例。

## 五、教訓（雙向對稱紀律）

- **CC 前數輪被 claude.ai 抓漏**（#20 病灶兩處漏兩處、甲全掃漏處）——CC 收。
- **本輪 CC 自驗抓 claude.ai 坐實有誤**（「守恆<6 不存在」實為真碼閘）——上呈 claude.ai。
- **N0-17-b 之精神正是「須指出碼中位置（檔:行）」**——CC 據此 grep 到 888/951/1009 碼位置，證 <6 有碼。**用 N0-17-b 的方法·抓到 N0-17-b 的舉例錯誤**——正典自洽性之壓力測試。
- **CC 亦曾誤**：CC 前版甲-2 把「守恆<6」列入 12 閾值清單時未標碼位置（沿 claude.ai 說法）——CC 未當場自驗，此次補驗才發現。**兩造都須以「指出碼位置」為入倉前提**（N0-17-b 之實踐）。

## 六、上呈 claude.ai 待覆核

1. N0-17-b 之「守恆<6 不存在·散文閘」實證**換例**（改用白縫 `<1㎡` 為散文閘例·守恆<6 移至 N0-17 主論「真閘·閘寬無依據」）。
2. 甲-2 守恆閘收緊之描述據實：全區 `cons_resid<6.0`（run_verification:888/951/1009）→ 0.01；逐街廓 `abs(_resid)<1.0`（stepg:813）→ 0.01。
3. 規格 N0-17-b 已入倉 CC 自驗更正段（本報告 §三/四），待 claude.ai 覆核後定稿。

**未 push 不得報收官**（最高紀律）：本更正屬 S0b plan 前之閾值盤點校正，非收官宣稱。
