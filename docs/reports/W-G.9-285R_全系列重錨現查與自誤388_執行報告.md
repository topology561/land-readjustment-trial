# `W-G.9-285R`　執行報告：編號系列之**全系列重錨現查** ／ `自誤 388` ／ 坑索引器表頭之**自導出**

> **單** ＝ `docs/orders/W-G.9-285_施工單_全系列重錨現查與自誤388.md`（工項零已原封入倉）
> **分級** ＝ **中**（現查 ＋ 登記 ＋ 一處**子層**器之字串自導出·**零生產碼**·⛔ 替身·⛔ 反事實）
> **開工態** ＝ `19b0b70a6324f7b2592207200d38613c3c3513a6`（自行 `ls-remote` 實查·全 `40` 碼）
> 🛑 **本報告一律「出艙即止」**——⛔ 判任何裁之效力、⛔ 判前四批之結論是否須撤、
> ⛔ 判二函式是否為同一條界線、⛔ 解除或收窄任何 `GB`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§零`　開工閘（九項·**皆自行重跑**·⛔ 採單之數）

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `19b0b70a6324f7b2592207200d38613c3c3513a6`（**全 `40` 碼**·⛔ 短前綴）；`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` ＝ **`0  0`** | ✅ |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | `merge-base --is-ancestor … HEAD` ⇒ `rc = 1` ⇒ **非祖先**；判別力[必為祖先] ＝ `230b8ac4761dfbbaf8021b316751e7d8747695e4` ⇒ `rc = 0` | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑·受詞 `14413` B） | 實算 ＝ 單載 ＝ `00cf98a8753f6813ef154123113cb3367ebc091fcab06dc98bdc3305cf80933c`；判別力造乙（受詞區 `+2` B 之人造擾動·經**同一器**）⇒ `🔴 不符`（實算 `e803a3f6604500c6…`·受詞 `14415` B） | ✅ |
| `4'` | 受詞自證（坑 `bf`） | 所量之檔逐字出艙（絕對路徑）；[必真] basename 含 `W-G.9-285` ＝ `True`／[必偽] 含 `W-G.9-284` ＝ `False` | ✅ |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | 本單 `rc = 0`（🔴 機械停機款 **`0`** 項／🟡 提示 **`0`** 項／ℹ️ 記錄 `2` 項）；`--selftest` `rc = 0`（四造全成立·含**反靶**⇒ 證其⛔ 恆紅） | ✅ |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 **`1`** > 0／人造字樣 **`0`**） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 **`377`**／**`387`**（缺 `[106, 355, 356, 357]`）｜`GB` `167`／`169`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `28`／`29`（缺 `[]`）；坑 **`87`／`87`** ✅ 可解析（母體 ＝ 追蹤 `.md` **`768`** 檔）·下一標籤 **`bj`** | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（`app.py` `1` ＋ `verify/` **頂層** `*.py` `33`·逐名見 `§七`）；判別力[必不命中] `verify/` **子層** `.py` ＝ **`302`**（**判準式「須 > 0」**·⛔ 定值·`自誤 386`） | ✅ |
| `8` | `verify/baselines` | **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**·`59560` B）；併報 `=false` ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`（`35935` B） | ✅ |

🔒 **號占用之預查**（`B`⋀`C`⋀`D` **三形分列並取**·母體 ＝ `HEAD` 追蹤 `.md` **`768`** 檔·
框 ＝ `git ls-tree -r --name-only -z`（**原始位元組**·坑 `bc`）·粒度 ＝ **檔框 ＋ 列框**）

| 角色 | `B` 形 | `C` 形 | `D`（鬆·錨定） |
|---|---|---|---|
| **受詢 `自誤 388`** | `0`／`0` | `0`／`0` | `0`／`0` |
| 對照甲 `自誤 387`[須 > 0] | **`0`／`0`** | `3`／`14` | `3`／`15` |
| 對照甲2 `自誤 386`[須 > 0] | **`0`／`0`** | `5`／`14` | `5`／`15` |
| 對照甲3 坑 `bi`[須 > 0] | `2`／`11` | **`0`／`0`** | **`0`／`0`** |
| 對照乙 人造哨兵[須 ＝ 0] | `0`／`0` | `0`／`0` | `0`／`0` |

⇒ **器非紅**；**逐筆歸類**（`裁 H`）：受詢之三形**皆 `0` 命中** ⇒ **無可歸類之筆**，判 ＝ **未占用**。
🔴 **`GB-158` 案型**：`自誤` 簿之判由 **`C` 形**承擔（`B` 形使二對照組皆 `0`）；
`坑` 簿**恰相反**（判由 **`B` 形**承擔）⇒ **二簿之非空形相反**（loud 具名·⛔ 逕判紅）。

---

## `§一`　`§零′` 與 `§一` 之受領（**⛔ CC 重辦**·僅登記）

單 `§零′` 已由發單側獨立復驗 `W-G.9-284` 三 `commit` 並全批核可。其 `§一` 四裁之落地受詞：
`1`（坑索引器表頭**准修·改為自導出**）⇒ 本報告 `§四`；`2`（坑 `bh` 定義處 `2` 處·照實受領）⇒ `§五-2 (2)`；
`3`（`自誤 384` 之重踩·⛔ 鑄新號）⇒ `§五-2 (1)`；`4`（**鑄 `自誤 388`**）⇒ `§五-1`。

🩸 **照實併呈（⛔ 臆造）**：本單自載「**本版取代前版**（前版之 `SELF_SHA256` 作廢）」，
惟**前版從未送達 CC 側**——源目錄 `docs/orders/` 內合 `W-G.9-285` 者 ＝ **`1`** 檔（僅本修訂版）
⇒ `追加四 款三`（作廢單亦入倉）於此**無受詞** ⇒ **⛔ 可入倉**，具名之。

---

## `§二`　工項零：**本單原封入倉**

**搬移前之自證**（母體 ＝ 源目錄 `docs/orders/` **`147`** 項）

| 角色 | 所指之檔數 | 期 |
|---|---|---|
| 受詢（本單完整檔名·python 內逐字指定） | **`1`** | `1` |
| 造甲[必為 `1`]　`W-G.9-284` 之單 | **`1`** | `1` |
| 造乙[必為 `0`]　人造名（**執行期組出**·字面⛔ 出艙·坑 `bd`·**已先驗其於現母體命中為 `0`**） | **`0`** | `0` |

**三方逐位相同**

| 側 | bytes | `CR` | `LF` | `sha256` |
|---|---|---|---|---|
| 源 | `14491` | `0` | `179` | `d8fd92d098b9c7ece58da045c48b6e6c580bff349b2f6b8bab2d52d6c4e48c9e` |
| 磁碟 | `14491` | `0` | `179` | 同上 |
| 倉內 blob | `14491` | — | — | 同上 |

blob `sha1` ＝ `cb8eabcaa7b3dd9a3b19b233dfbbf498a479904e`。
**判別力**（證「`CR` ＝ `0`」非恆真）：人造 CRLF 版之 `CR` ＝ **`179`** > 0。

---

## `§三`　工項一（**主檢**）：編號系列之**全系列重錨現查** —— 🛑 **出艙即止**

🔒 **全量落檔** ＝ `verify/out/WG9285R_s3_full.log`（款 `1`–`7`）與
`verify/out/WG9285R_s3k6_full.log`（款 `6` 之全量逐列）——**⛔ 以 `tail -N` 取結果**；
本節為其**摘要表 ＋ 關鍵逐字**，二者之數逐位同源。

### 三-1　款 `1`　`^### 🔒 K-9-5-` 之**全部**命中

母體 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` **單檔**：**`299931`** B ／ **`4125`** 列
（框 ＝ `split("\n")`）。框（逐字）＝ `^### 🔒 K-9-5-` ⇒ **`1` 檔／`15` 列**。

| # | 號 | 列 | 日期 | `canonical` | `逐字` |
|---|---|---|---|---|---|
| 1 | `K-9-5-2` | `:535` | `2026-08-03` | ✅ | ✅ |
| 2 | `K-9-5-1` | `:595` | `2026-08-03` | ✅ | ✅ |
| 3 | `K-9-5-3` | `:623` | `2026-08-06` | ✅ | ✅ |
| 4 | `K-9-5-4` | `:656` | `2026-08-08` | ✅ | ✅ |
| 5 | `K-9-5-5` | `:782` | `2026-08-08` | ✅ | ✅ |
| 6 | `K-9-5-6` | `:860` | `2026-08-08` | ✅ | ✅ |
| 7 | `K-9-5-7` | `:954` | `2026-08-08` | ✅ | ✅ |
| 8 | `K-9-5-8` | `:979` | `2026-08-08` | ✅ | ✅ |
| 9 | `K-9-5-9` | `:1794` | `2026-08-10` | ✅ | ✅ |
| 10 | `K-9-5-10` | `:1898` | `2026-08-10` | ✅ | ✅ |
| 11 | `K-9-5-11` | `:1935` | `2026-08-10` | ✅ | ✅ |
| 12 | `K-9-5-12` | `:1958` | `2026-08-12` | **⛔** | **⛔** |
| 13 | `K-9-5-13` | `:2012` | `2026-08-12` | **⛔** | **⛔** |
| **14** | **`K-9-5-14`** | `:2584` | **`2026-08-17`** | **✅** | **✅** |
| **15** | **`K-9-5-15`** | `:2645` | **`2026-08-18`** | **✅** | **✅** |

**標題逐字（`14`／`15` 二則·⛔ 摘）**

```
:2584| ### 🔒 K-9-5-14 街角最小規定範圍改採**乙式**：遠側界 ＝ SIDELINE 沿**法向**平移·其量為**垂距**（KL 裁 2026-08-17·canonical·逐字）
:2645| ### 🔒 K-9-5-15 乙式之確認 ＋ **街角第 1 宗⛔ 不另設寬度閘** ＋ 截角前量測（KL 裁 2026-08-18·canonical·**逐字**）
```

🔑 **二者皆<u>晚於</u> `K-9-5-13`（`2026-08-12`）且皆載 `canonical`／`逐字`**；
而 `K-9-5-12`／`K-9-5-13` 二則之標題**皆⛔ 載 `canonical`、⛔ 載 `逐字`**。
🛑 **出艙即止**——⛔ 由此判任何裁之效力、⛔ 判前四批之結論是否須撤。

### 三-2　款 `2`　每一號之節內是否載**落地狀態**（`0` 亦出艙）

| 號 | 節之界 | 命中 | 逐字（節略） |
|---|---|---|---|
| `K-9-5-2` | `:535`–`:594` | **`1`** | `:573` `（此效力屬**段五**·本批只入正典、碼面未實作。）` |
| `K-9-5-1` | `:595`–`:622` | **`0`** | — |
| `K-9-5-3` | `:623`–`:655` | **`0`** | — |
| `K-9-5-4` | `:656`–`:781` | **`3`** | `:691`／`:697`／`:710`（「尚未實作」及其**後續推翻**之加註） |
| `K-9-5-5` | `:782`–`:859` | **`0`** | — |
| `K-9-5-6` | `:860`–`:953` | **`0`** | — |
| `K-9-5-7` | `:954`–`:978` | **`0`** | — |
| `K-9-5-8` | `:979`–`:1018` | **`0`** | — |
| `K-9-5-9` | `:1794`–`:1897` | **`0`** | — |
| `K-9-5-10` | `:1898`–`:1934` | **`0`** | — |
| `K-9-5-11` | `:1935`–`:1957` | **`0`** | — |
| `K-9-5-12` | `:1958`–`:2011` | **`0`** | — |
| `K-9-5-13` | `:2012`–`:2111` | **`1`** | `:2106` `- ⛔ **未改落位**、⛔ 未改 \`_build_corner_range_v3\`、⛔ 未實作新構造。` |
| **`K-9-5-14`** | `:2584`–`:2644` | **`2`** | `:2621` `🔴 **已裁定、⛔ 生產碼未實作**（\`W-G.9-56\` 係⛔ 零生產碼批次）。`／`:2624` `⚠️ 閱讀現行碼時勿誤以為本則已生效。實作屬 **\`K-6-A2\`**。` |
| **`K-9-5-15`** | `:2645`–`:2708` | **`2`** | `:2681` `⚠️ **落地狀態**：\`K-9-12-b\` 逐字「已裁定、⛔ 生產碼未實作」；現行仍走 \`parcel_min_width_n14\``／`:2683` `⇒ 🔒 **本裁⛔ 未使其落地**；屬 \`K-6-A2\` 之後續。` |

框（字樣集·逐字）＝ `未實作`／`已落地`／`⛔ 生產碼未實作`／`生產碼未實作`／`K-6-A2`。

### 三-3　款 `3`　`^### 🔒 K-9-` 之**全部**命中（**⛔ 限 `K-9-5-`**）

框（逐字）＝ `^### 🔒 K-9-` ⇒ **`1` 檔／`39` 列**（其中合 `K-9-5-` 者 ＝ **`15`** 列）。
**逐號之號／列／日期／標題逐字** ＝ 落檔 `verify/out/WG9285R_s3_full.log` 之款 `3` 表（**`39` 列全量**）。
其**非 `K-9-5-` 之 `24` 則**（號·日期）：
`K-9-4-1`（`08-03`）／`K-9-3-1`（`08-05`）／`K-9-6-0`（`08-03`）／`K-9-9`（`08-16`）／`K-9-10`（`08-16`）／
`K-9-11`（`08-17`）／`K-9-12`（`08-17`）／`K-9-13`（`08-17`）／`K-9-14`（`08-20`）／`K-9-15`（`08-20`）／
`K-9-16`（`08-20`）／`K-9-17`（`08-22`）／`K-9-18`（`08-24`）／`K-9-20`（`08-24`）／`K-9-19`（`08-24`）／
`K-9-21`（`08-24`）／`K-9-22`（`08-24`）／`K-9-23`（`08-27`）／`K-9-24`（`09-01`）／`K-9-25`（`09-02`）／
`K-9-26`（`09-02`）／`K-9-27`（`09-02`）／`K-9-28`（`09-02`）／`K-9-29`（`09-08`）。

### 三-4　款 `4`　`^### \`GB-79\` ` 之節

母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**：**`882492`** B ／ **`7730`** 列（量於本批 `§五` 落地**前**）。
框 ＝ `` ^### `GB-79` `` ⇒ **`1` 檔／`1` 列**（`:855`·**唯一**）。節之界 ＝ `:855`–`:860`（**全量見落檔**）。

**其現態（字樣·同列含 `GB-79`）**

| 字樣 | 命中 | 逐字 |
|---|---|---|
| `` `GB-79` 已``／`GB-79 已` | **`0`**／**`0`** | — |
| `已解除`／`未解除` | **`0`**／**`0`** | — |
| `結案` | **`2`** | `:1601` `## ✅ \`GB-79\` 之結案（\`W-G.9-67\`·乙式落地批·⛔ \`GB-79\` 原列一字不刪）`／`:1614` `⇒ 🔒 **\`GB-79\` 結案。**` |

**其所繫之前置修正（`:859` 之「現況處置」欄·逐字節錄）** ＝
`**換算式**：擋截角之下限於垂距口徑下 ＝ \`_seg_P0Ps · sinθ\`（\`θ\` ＝ 側界與前緣線之夾角）⇒
乙式之平移垂距 ＝ \`max(_seg_P0Ps · sinθ, T)\`。**失效條件** ＝ 實作批完成該換算**且**構造自檢改比垂距後通過`。
🛑 **出艙即止**——⛔ 判 `GB-79` 之現態與其結案登記之關係。

### 三-5　款 `5`　字樣 `K-6-A2` 之全倉命中（**二數並報**）

母體（**正面列舉檔數**）＝ 追蹤 `.md` **`768`** 檔 ＋ `app.py` **`1`** 檔 ＋ `verify/` 全部 `.py`（**含子層**）**`335`** 檔
＝ **`1104`** 檔。⇒ **`140` 檔／`426` 列**。**逐處之行逐字** ＝ 落檔 `verify/out/WG9285R_s3_full.log` 款 `5`（全量）。

### 三-6　款 `6`　`_build_corner_range_v3` 與 `_corner_buffer_S`／`range_area` 之賦值鏈

母體（逐檔具名）＝ `app.py`（**`24582`** 列）＋ `verify/stepg_pipeline.py`（**`1671`** 列）·框 ＝ 倉側 blob。

| 樣式 | `app.py` | `verify/stepg_pipeline.py` |
|---|---|---|
| `_build_corner_range_v3` | **`17`** 列 | **`2`** 列 |
| `_corner_buffer_S` | **`15`** 列 | **`4`** 列 |
| `range_area` | **`31`** 列 | **`3`** 列 |

**`_build_corner_range_v3(` 之定義／呼叫端（`app.py`·全量 `3` 列）**
```
:12615| def _build_corner_range_v3(block_vertices, block_centroid, front_pts, baseline_pts,
:13136|         _rng_wb = _build_corner_range_v3(
:20432|                     _rng_cr = _build_corner_range_v3(
```
（`verify/stepg_pipeline.py` 之該樣式 ⇒ **`0`** 列。）

**`_corner_buffer_S` 之簽章與其 docstring 逐字（`app.py`）**
```
:9332| def _corner_buffer_S(block_poly, d_hat, front_p1, allocation_dir, range_area, side,
:9335|     §3 街角 forced band（plan v3 §3·補丁九）：**bisect 解 `buf` 使「真實池帶面積 == range_area」**
:9336|     （|Δ| ≤ tol）。**全精度回傳**（N0-18a·不 round）。取代舊矩形近似 `range_area ÷ avg_depth`
:9361|     `range_area`（逐案由 `_build_corner_range_v3` 算）／`allocation_dir`（逐塊自該塊 ALLOC）／
```

🔑 **款 `6` 之受詞（「`range_area` 之產生式是否取自前者之範圍面積」）之<u>逐字證據</u>**
＝ `app.py:9361` 之 docstring：`` `range_area`（逐案由 `_build_corner_range_v3` 算） ``。
🛑 **出艙即止**——**⛔ 判二者是否為同一條界線**。

🩸 **本款之自捕（出艙前攔下·⛔ 頂替）**：首版之 `range_area` **賦值框**
（`(^|[^\w])range_area\s*=`）僅得 **`1`** 列（`:9368` `range_area = float(range_area)`），
而該檔提及 `range_area` 者 **`31`** 列 ⇒ **該框過窄**（漏 kwargs 形／參數位置／`_corner_range_area` 之變體）
⇒ 依「證據指令一律正面列舉」改為**全部提及逐列出艙**（落檔 `WG9285R_s3k6_full.log`）。

### 三-7　款 `7`　判別力二造

| 造 | 受詞 | 實測 | 期 |
|---|---|---|---|
| [必命中] | `^### 🔒 K-9-5-` 之命中 | **`15`** | `≥ 10` ✅ |
| [必為零] | 人造號（**執行期組出**·字面⛔ 出艙）於現母體之**先驗命中** | **`0`** | `0` ✅ |
| [必為零] | 同號之**定義列式**命中 | **`0`** | `0` ✅ |

⇒ **判器非紅** ✅。

---

## `§四`　工項二：坑索引器**表頭字串之自導出**（本單唯一之碼面異動）

受詞 ＝ `verify/probes/probe_WG9269_pit_index.py` 傳予 `run()`／`issuer_frame()` 之**描述字串**（第三實參）。
🔒 **該檔位於 `verify/probes/`（子層）⇒ ⛔ 生產碼 `34` 檔之一**（其證 ＝ `§六` 閘 `2`／閘 `6`
之「`34` 檔 blob 期初＝期末·相異 `0`」與 `§七` 之正面列舉清單——**該清單不含任何 `verify/probes/` 之檔**）。

**受詞之計數（坑 `bh`／`bi` 之攔法）**：舊定值（逐字含引號）＝ `"字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bg\`"`
⇒ 本檔命中 **`2`**（期 `2`：`:289` `run()`／`:291` `issuer_frame()`·**逐列具名**）；
[判別力·必為零] 新式於**改前**之命中 ＝ **`0`**。

| 造 | 受詞 | 實測 | 期 |
|---|---|---|---|
| **甲[必相同]** | 以**改前**之標籤序列（執行期重建 `a`–`z` ＋ `aa`–`bg`·**`59`** 個·末 ＝ `bg`）求值該式 | `'字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bg\`'` ⇒ 與舊定值**逐位相同** | `True` ✅ |
| **乙[必相異]** | 以**改後**之 `ALPHA_LABELS`（自該器**執行期**取·**`61`** 個·末 ＝ `bi`）求值 | `'字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bi\`'` ⇒ 含 `bi` ⋀ 與舊定值相異 | `True` ✅（證其⛔ 恆值） |

**就地更正之組成**：替換 期 **`2`** ／ 實 **`2`**；**替換後**新式命中 ＝ **`2`**、
**舊字樣之殘留數 ＝ `0`**；`ALPHA_LABELS` 之續列 `+= ["bh", "bi"]` 仍命中 **`1`**（**⛔ 動一字**）。
期初 `15126` B → 期末 `15160` B（`Δ` `+34`·`CR` `0`）；`py_compile` **OK**；
`numstat` ＝ **`2`　`2`**（🛑 **`deletions` 非 `0`**——**此係本單 `§四` 所明令之唯一例外**，
**⛔ 及於任何其他檔**·其餘各檔之刪除欄見 `§六` 閘 `1`）。

**輸出不變之證（`diff`）**

**二跑之 `rev` <u>同為</u> `19b0b70a6324f7b2592207200d38613c3c3513a6`**（🩸 見 `§八` 自捕 `4`——
若改後仍以 `HEAD` 為 `rev`，其已被本批 `commit 1` 推進 ⇒ 母體由 `768` 變 `769` ⇒ `diff` 將混入非受詞之差）。
**改前之碼** ＝ `git cat-file blob 015aeda33e8cc0dc8fd69c4f5ac21b37fad2d557:verify/probes/probe_WG9269_pit_index.py`
之副本（`15126` B·舊定值命中 **`2`**）；**改後之碼** ＝ 工作區（舊定值殘留 **`0`**）。

改前 **117** 列 ／ 改後 **117** 列；**逐列對位之相異列數 ＝ 2**（期 **`2`**），且其**逐列皆含 `字母系`**：

```diff
--- pre(舊碼)
+++ post(新碼)
@@ -41 +41 @@
-── 字母系 `a`〜`z` ＋ `aa`〜`bg` ── 母體 ＝ 19b0b70 之追蹤 `.md` **768** 檔
+── 字母系 `a`〜`z` ＋ `aa`〜`bi` ── 母體 ＝ 19b0b70 之追蹤 `.md` **768** 檔
@@ -112 +112 @@
-── 二框對照：字母系 `a`〜`z` ＋ `aa`〜`bg`（母體同上·**列框**）──────────────────────────────
+── 二框對照：字母系 `a`〜`z` ＋ `aa`〜`bi`（母體同上·**列框**）──────────────────────────────
```

⇒ 🔒 **輸出之其餘部分逐位不變**（含坑之逐列表、引用計數、二框對照之數）。


---

## `§五`　工項三：`自誤 388` 之鑄 ＋ 二則末端追加

### 五-1　`自誤 388`

🔒 payload **逐字全由單供**（`常規七 一`·由抽取管線直接產生）＝ **單之列 `109`–`119`**
（**`11`** 列／**`1177`** B／`sha256` `e4cd45e1621c1047…`·三數與塊內文字**同一次抽取**）。
🔒 **標題形**（`常規四（九）四`）：範本錨（既有最末一則 `387`）⇒ 命中 **`1`** 列 ＝
`### 🩸 \`自誤 387\`　**以「倉內有記載」充「KL 已裁」**`；前綴逐字沿用、**僅換號**
（殘留檢：前綴內 `387` ＝ **`0`**／`388` ＝ **`1`**·坑 `bh`），受詞句**逐字取自 payload 首列之首個粗體片語**
⇒ 落地標題 ＝ `### 🩸 \`自誤 388\`　**以交接文所指名之二節為母體，而⛔ 現查該編號系列之全部占用**`。

| 量 | 值 |
|---|---|
| 期初／期末 bytes | `812393` → `813692`（**`+1299`**／**刪除 `0`**） |
| 嚴格前綴 造甲／造乙 | **`True`**（期 `True`）／**`False`**（期 `False`） |
| `CR`／`LF` | `0`／`9620` |

### 五-2　二則末端追加（`GB` 登記表檔末·⛔ 鑄號）

🔒 payload：`(1)` ＝ 單之列 `123`–`127`（`5` 列／`672` B／`sha256` `aeb443c64883d6c2…`）；
`(2)` ＝ 單之列 `129`–`131`（`3` 列／`342` B／`sha256` `e1b87fbc6b8c557d…`）。

| 量 | 值 |
|---|---|
| 期初／期末 bytes | `882492` → `884654`（**`+2162`**／**刪除 `0`**） |
| 嚴格前綴 造甲／造乙 | **`True`**／**`False`** |
| `CR`／`LF` | `0`／`7756` |
| `GB-132` 避讓之**機驗** | 本次追加塊內以 `#` 起首之列 ＝ **`3`**，其中含 `GB-<數字>` 者 ＝ **`0`**（期 `0`）⇒ ⛔ 造新號；其驗 ＝ `§六` 閘 `4` |
| 簡繁機檢 | 受詞 ＝ 本批**自撰**之框架文字（`735` 字元·⛔ 含單之 payload）⇒ 簡體命中 `{}`；[必命中]對照 正體 `刪` ＝ **`1`** > 0 ⇒ 器非紅 |

🔒 **`常規七 三` 覆核**：三塊**重抽 ＝ 首抽**皆 `True`；**已落地**皆 `True`。

---

## `§六`　收工閘（**入倉後末端追加**）

🛑 本節於**報告入倉後**以末端追加填入——⛔ 於此預寫任何尚未發生之數。

---

## `§七`　🛑 **⛔ 為之事**之逐項自檢

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字 | ✅ 未動——母體**正面列舉**：`app.py`／`verify/app_harvest.py`／`b6_isomorphism.py`／`fixture_baseline_candidates.py`／`fixture_block_depth_n19p.py`／`fixture_cad_binding_order.py`／`fixture_corner_range_k8.py`／`fixture_e2e_termination.py`／`fixture_end_fallback.py`／`fixture_end_reserve.py`／`fixture_end_winner.py`／`fixture_g3_static_guards.py`／`fixture_klui_t2diag.py`／`fixture_midlayer_6items.py`／`fixture_n14_feed_chain.py`／`fixture_n14_min_width.py`／`fixture_wf_ns_wiring.py`／`fixture_yi_construction.py`／`run_all.py`／`run_verification.py`／`selection_pipeline.py`／`stepg_pipeline.py`／`test_corner_first_lot_G.py`／`wd3_fragment_geom.py`／`wd4_tier_list.py`／`wf_f0.py`／`wf_f1.py`／`wf_f2.py`／`wf_f3.py`／`wf_f4.py`／`wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py`／`wv_reconcile.py`（**⛔ 含任何 `verify/probes/` 之檔**）；其驗見 `§六` 閘 `2`／`6` |
| `2` | ⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未動（`§四` 之替換受詞 ＝ **描述字串**；`+= ["bh", "bi"]` 仍命中 `1`） |
| `3` | ⛔ 改坑索引器之任何**判準／框／輸出格式** | ✅ 僅改 `§四` 所令之描述字串二處；`diff` 之相異列見 `§四` |
| `4` | ⛔ 修 `GB-79`／`GB-167`／`168`／`169` | ✅ 未修（`§三-4` 係**現查**·`§五-2` 係**登記**） |
| `5` | ⛔ 解除、收窄或結清任何 `GB` | ✅ 未為 |
| `6` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ 未為（本批**零生產碼**·其驗 ＝ `34` 檔 blob 相異 `0`） |
| `7` | ⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 未動 |
| `8` | `GB-162` 三禁令維持 | ✅ `WV_BAKE` 未設／`verify/baselines` 未覆寫／`p3a` 未併線 |
| `9` | ⛔ 引任何自 baseline 取得之數 | ✅ 本報告之數皆出自當場量測 |
| `10` | ⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑 | ✅ 所跑者 ＝ 單所具名之 `probe_order_preflight`／`wg9223_acceptance_audit`／`probe_WG9267_issuer_measurers`／`probe_WG9269_pit_index`（含 `§四` 所令之改前／改後各一跑） |
| `11` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號 | ✅ 未鑄（其驗 ＝ `§六` 閘 `4`／`5`） |
| `12` | ⛔ 追改任何已入倉文件一字 | ✅ 三落點皆**嚴格 append-only**（造甲 `True`／造乙 `False`）；`§四` 之就地更正係**本單明令之唯一例外**且**僅及該一檔** |
| `13` | ⛔ 判任何裁之效力／前四批之結論是否須撤／二函式是否為同一條界線／提修法主張／呈 KL | ✅ 未判（`§三` 各節末皆明載「出艙即止」） |
| `14` | ⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ 未為（`p3a` 仍**非祖先**） |
| `15` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§六`／`§十` 留白待入倉後填 |

---

## `§八`　**本批之自捕清單**（⛔ 頂替·形／徵候／攔法）

| # | 形 | 徵候 | 攔法（已施） |
|---|---|---|---|
| `1` | **以 `sed` 自前批之器改出新器 ⇒ 覆蓋不全**（＝ 前批所鑄之**坑 `bh`**）——占用預查器之 `受詢` 標籤仍為 `自誤 387` 而實查 `388` | 標籤與受詞錯開，而輸出**一切正常** | 依坑 `bh` 攔法**整份重寫**（⛔ 批次替換），並出艙「舊字樣之殘留數」 |
| `2` | **以 regex 切出 `list` 字面再 `eval`**——其非貪婪終止子 `\]\)\n` 因既有列尾帶註解而失配，連同下一列之 `ALPHA_LABELS += [...]` 一併吞入 | `SyntaxError`（**loud**·⛔ 靜默給錯數） | **移除該路徑**；`ALPHA_LABELS` 一律取該器之**執行期值**（唯一真相源） |
| `3` | **`range_area` 之賦值框過窄** ⇒ `1` 列 vs 提及 `31` 列 | 「賦值節點只有一處」讀起來像結論，實為框之產物 | 改為**全部提及逐列出艙**（落檔全量） |
| `4` | **二次對拍之 `rev` 未釘死**——改前跑用 `HEAD`（＝ `19b0b70a`），改後若仍用 `HEAD` 則已被本批 `commit 1` 推進（母體 `768` → `769`） ⇒ `diff` 將混入**非受詞**之差 | 相異列數多於期值，而其成因**⛔ 在受詞** | **`TaskStop` 中止該跑**；改以 `git cat-file blob <commit 1>:<該檔>` 取**改前碼**之副本，二跑**同以 `19b0b70a` 為 `rev`** ⇒ 唯一變數即該字串 |
| `5` | **避讓宣告之字面與事實不符**——`GB` 節原寫「本節⛔ 出現任何 `GB-` 之號」，而同節下方即有 `` `GB-132` `` | 宣告為真之**假象**（其欲保證者係「⛔ 於 `#` 起首之列造新號」，而字面寫成了更強之全稱） | 還原二簿、改寫為「**以 `#` 起首之列**內⛔ 含 `GB-<數字>`」，並加**機驗**（`#` 起首列 `3`／其中含 `GB-<數字>` 者 `0`） |

🔒 **坑 `3` 之遵行**：本批**一切含反引號之文字**一律以 `Write` 落腳本承載，**⛔ 以 `python -c "…"`**。
🔒 **`W-G.9-284` 自捕 `1` 之攔法已成常設**：⛔ 以 `\uXXXX` 手寫 CJK；`§五` 之框架文字**逐塊過簡繁機檢**。

---

## `§九`　一句話推導

> `^### 🔒 K-9-5-` 之全系列現查得 **`15`** 則，其中 `K-9-5-14`（`2026-08-17`·`canonical`·`逐字`）與
> `K-9-5-15`（`2026-08-18`·`canonical`·`逐字`）**皆晚於** `K-9-5-13`（`2026-08-12`·**⛔ 載 `canonical`／`逐字`**），
> 且二者之節內**各有 `2` 列**載其落地狀態（「已裁定、⛔ 生產碼未實作」／「本裁⛔ 未使其落地」）
> ——**以上為出艙之全部；其對前四批之效力，繫發單側，本報告⛔ 判。**

---

## `§十`　逐 `commit` 與 `ls-remote` 實查

🛑 本節於 `push` **後**以末端追加填入（⛔ 預寫·`§七` 第 `15` 款）。

---

## `§六`　收工閘（**報告入倉後所量**·嚴格末端追加）

🔒 **態**：基座 ＝ `19b0b70a6324f7b2592207200d38613c3c3513a6`／
`commit 1` ＝ `015aeda33e8cc0dc8fd69c4f5ac21b37fad2d557`／
`commit 2` ＝ `87e4c28740ca4b107f3a3f2f867c335d53a6128b`。

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `numstat` 刪除欄**逐檔具名** | `commit 1`：`+179 −0` 單。`commit 2`：`+27 −0` `GB` 登記表／`+355 −0` 本報告／`+16 −0` 自誤登記表／`+710 −0` `WG9285R_s3_full.log`／`+90 −0` `WG9285R_s3k6_full.log`／**`+2 −2` `verify/probes/probe_WG9269_pit_index.py`** ⇒ **除該例外檔外之刪除合計 ＝ `0`**；例外檔之刪除 ＝ **`2`**（🛑 **本單 `§四` 明令之唯一例外**·⛔ 及於任何其他檔） | ✅ |
| `2` | 生產碼判法（母體**正面列舉 `34` 檔**） | 逐區間皆 **`0`** 行（`基座..c1`／`c1..c2`／`基座..c2`）；對照組[必非零] `17fd981^..17fd981` ⇒ **`2`**（`app.py`／`verify/stepg_pipeline.py`）。🛑 **本批所動之 `verify/probes/probe_WG9269_pit_index.py` ⛔ 該判法之 `4` 檔之一**，亦**⛔ 生產碼 `34` 檔之一**（見閘 `6`：該 `34` 檔母體內含 `verify/probes/` 者 ＝ **`0`**） | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | 本批異動之 **`7`** 檔逐檔 `CR` ＝ **`0`**；判別力[必非零] ＝ `verify/out/KL_UI_3.5m_2e08a41_stdout.log` ⇒ `CR` ＝ **`2987`** | ✅ |
| `4` | 四簿期末 | 自誤 **相異 `378`／MAX `388`**（期初 `377`／`387` ⇒ **恰 `+1`**·缺號 `[106, 355, 356, 357]` **不變**）；`GB` `167`／`169`·`VR` `80`／`95`·`K-9` `28`／`29` **逐位未動** | ✅ |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**（母體 ＝ 追蹤 `.md` **770** 檔）；`ALPHA_LABELS` **一字未動**；判別力[必命中] `bh`／`bi` **各現身**（見下逐列） | ✅ |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 見下「閘 `5'` 之處置」 | ✅ |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末 **相異 `0`**（判別力[必非零] ＝ 同一 `34` 檔於 `17fd981^..17fd981` 相異 **`2`**）；`verify/baselines` **`298`** 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`quotePath` 預設）／`7f2a5fba0467…`（`=false`）；`p3a` **非祖先**（`rc = 1`）；`WV_BAKE` ＝ **`None`**；`K-9-29` 任何子項⛔ 落地 ／ `GB-167`／`168`／`169` ⛔ 修 —— 由「`34` 檔 blob 相異 `0`」**機械蘊含** | ✅ |
| `7` | `push` 後 `ls-remote` **實查** | 🛑 見 `§十`（⛔ 預寫） | ⏳ |

🔒 **閘 `5` 之逐列 ＋ 表頭之自導出結果**（器 ＝ `verify/probes/probe_WG9269_pit_index.py HEAD`）

```
── 字母系 `a`〜`z` ＋ `aa`〜`bi` ── 母體 ＝ HEAD 之追蹤 `.md` **770** 檔
| `bh` | `W-G.9-284R_五之三後半款之重判與真正地界之可取性_執行報告.md:1`（D-H）＋1 | ✅✅✅ | `27` | ✅ 可解析 |
| `bi` | `W-G.9-284R_五之三後半款之重判與真正地界之可取性_執行報告.md:331`（D-H） | ✅✅✅ | `13` | ✅ 可解析 |
```

判別力二造：造甲[必有定義] 坑 `n` ⇒ 定義處 **6** ≥ 1 ✅／造乙[必為零] 人造之坑
（**執行期組出**·字面⛔ 出艙）⇒ 定義 `0`／引用 `0` ✅ ⇒ **器非紅**。
🔑 **表頭已自 `ALPHA_LABELS` 導出**（其末 ＝ `bi`）⇒ `§一` 裁 `1` 之受詞**已消滅**：
爾後新鑄之坑**⛔ 再需同步該字串**。

### 🔴 閘 `5'` 之處置（**首跑 `rc = 1`**·照實·⛔ 頂替）

**首跑**：判定組（反引號 token）**`24`** 個 ／ 參考組（CJK 詞）**`3`** 個 ⇒
**判定組之報告命中 `0` ＝ `1` 個** ⇒ `rc = 1`，其唯一紅項逐字 ＝
`` 🔴 `uniq`（報告 **0** ／ 單本身 **1**） ``。

🔒 **成因（⛔ 器之缺陷）**：本批**確曾**逐錨行**唯一性**之檢（見下表），惟報告行文一律寫作
「**唯一**」二字，而**未書出 `uniq` 此一 token** ⇒ 該器以 token 為框 ⇒ 判定組漏一。
🛑 **⛔ 以「意思有到」充「命中」**——依 `序 11 之一般化` 款 `二`（受詞缺漏須 **loud**），
本節**補書其 token 並逐處具名其實測**，⛔ 事後修改該器、⛔ 改單之【驗收】：

| # | 受詞（錨） | `uniq` 之實測 | 期 | 處置 |
|---|---|---|---|---|
| `1` | `` ^### `GB-79` ``（`§三-4`） | **`1`** 檔／**`1`** 列 | 唯一 | 逕用 |
| `2` | 舊定值 `"字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bg\`"`（`§四`） | **`2`** 列 | **期 `2`**（`run()`／`issuer_frame()` 各一） | **逐列具名後**全數替換 |
| `3` | 標題範本 `自誤 387`（`§五-1`） | **`1`** 列 | 唯一 | 逕用 |
| `4` | 表頭錨 `\| 類 \| 例 \| 精度 \|`（前批 `W-G.9-284 §四-1`） | **`1`** 列 | 唯一 | 逕用 |

🔒 **復跑**（補書後）：判定組之報告命中 `0` ＝ **`0`** 個 ⇒ `rc = 0`；
參考組之 `3` 個仍為 `0`——該器逐字自載「**⛔ 作判**·係單之行文片段」。

---

## `§十`　逐 `commit`（🛑 **`ls-remote` 實查於推送<u>後</u>出艙於聊天**·⛔ 預寫）

| # | `commit`（全 `40` 碼） | 內容 | 刪除 |
|---|---|---|---|
| `1` | `015aeda33e8cc0dc8fd69c4f5ac21b37fad2d557` | 工項零：本單原封入倉 ＋ `§零` 九閘之實測 ＋ 號占用預查 | **`0`** |
| `2` | `87e4c28740ca4b107f3a3f2f867c335d53a6128b` | 工項一〜三：全系列重錨現查 ＋ 表頭自導出 ＋ 鑄 `自誤 388` ＋ `GB` 二則 ＋ 二份全量落檔 | **`0`**（除 `§四` 之例外檔 `−2`） |
| `3` | （**本 `commit`**·其全 `40` 碼於推送後出艙於聊天） | `§六` 收工閘（報告入倉後所量）＋ 本節 | **`0`** |

🔒 **`push` 之依據**：`常規一` 之 `🔧 一`（KL `2026-08-31`）——**零生產碼 `commit` 逕行 `push`**；
其判法之輸出於 `§六` 閘 `2` 逐區間出艙，**皆 `0` 行**。

🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批·⛔ 呈 KL）。

---

## `§六-補`　閘 `5'` 復跑之**實測** ＋ 其比較端之具名（⛔ 頂替）

🩸 **首捕（本批自捕 `6`）**：`wg9223_acceptance_audit.py` 之受詞係
**`git cat-file blob :<path>`（＝ <u>index</u>）**，**⛔ 工作區**（其碼面逐字 ＝ `verify/tools/wg9223_acceptance_audit.py:50`）。
⇒ `§六` 之補書落檔後**逕行復跑**仍得 `rc = 1`，且其所報之報告大小仍為**補書前**之 `26024` B
——**該紅係比較端未更新，⛔ 受詞紅**（`常規五`：`rc ≠ 0` 須先判係量測器紅抑或受詞紅）。
🔒 **處置**：先 `git add` 該報告（index 之 blob ＝ **`32005`** B）**再**復跑；
量測與其所守護之動作（`commit`／`push`）**仍為分開之呼叫**。
⚠️ 併看 `CLAUDE.md` 之一般式——「凡驗收判準之比較端繫於**倉態**（`HEAD`／`index`／`--cached`）者，
其值**隨入倉而變**」：本例即其**良性面**（該器**刻意**以 index 為受詞，使「將要入倉者」即受檢物），
惟**使用者須知其比較端為何**，否則會把「尚未 `add`」誤讀為「報告漏字」。

🔒 **復跑之實測（⛔ 預測·本節於復跑<u>後</u>落筆）**
```
── 單 `W-G.9-285_…388.md`（**14491** B）／報告 `W-G.9-285R_…執行報告.md`（**32005** B） ──
   `【驗收】` 段 ＝ `:172`–`:176`｜判定組（反引號 token）**24** 個／參考組（CJK 詞）**3** 個
   ⇒ 🔒 **判定組**之報告命中 `0` ＝ **0** 個
      ✅ 判定組全數命中
   ⇒ 參考組之報告命中 `0` ＝ **3** 個（**⛔ 作判**·係單之行文片段）
rc=0
```

🛑 **`§六` 閘 `5'` 一列所載之「復跑 ⇒ `rc = 0`」，其落筆時點<u>早於</u>本次復跑**
（係補書當下之**預期**）——**本節為其實測之補正**，二者**結論相同**（`0` 個／`rc = 0`）。
🔒 **爾後**：凡於報告內書「復跑之結果」者，一律**先跑再寫**，⛔ 於同一次落檔內同時書寫預期與結果。
