# `W-G.9-260R2`　補令一之執行（`Q-2` 獨立差集／`Q-4` 正典互斥之具名／工項五復現）

> 🛑 **本倉三受詞一字未動**：`app.py` `4860f10828fb8ae9`／`verify/stepg_pipeline.py` `c8d46d2e5fb79dc5`／
> `verify/wf_f0.py` `b0722355c1978635`（倉側 blob·前後逐位相同·當場復現）。
> 🔒 注入落**三拋棄式 clone**（`C:/t26b` 原態全跑／`C:/t26c` 原態＋探針／`C:/t26d` 閘關＋探針）
> ·**目錄名同長度**（記憶 ㊲）·跑畢整份刪除。
> 🔒 **本文⛔ 含任何修法主張**（`V-S`·受檢三字系以代稱行之·其逐字見原單 `§六 V-S` 欄）。
> 🔒 **零生產碼**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`
> 於本批之主倉異動命中 ＝ `0`·見 `§六`）。

---

## §零　工項零：收單原封入倉 ＋ 入倉前之號占用閘

### 零-1　號占用閘（**宣告框** `D1`／`D2`／`D3` 之款 `①`–`⑦`·錨 ＝ `CLAUDE.md` 之 `宣告框` 節）

**三軸**：來源 ＝ `blob@4cad4e7f9da88892180b4f90d386b509ac4431e3`／母體 ＝ **全 `docs/` 608 檔**／
粒度框 ＝ **檔框 ＋ 列框**（`D1`／`D2`／`D3` 分列·`列框 ＝ |D2 ∪ D3|`）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 鬆框 檔/列 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-260`** | **3** | **5** | **1** | **0** | **6** | **4** | 5／15 | 受詢 ⇒ 🔴 **已占** |
| `W-G.9-258` | 2 | 3 | 0 | 0 | 3 | 3 | 4／9 | 對照組甲（須 `≥ 1`）✅ |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱·執行期組出） | 0 | 0 | 0 | 0 | **0** | **0** | 0／0 | 對照組乙（須 `= 0`）✅ |

⇒ **器非紅**。占用者逐檔具名（**全為本波自身之產物**）：
`docs/orders/W-G.9-260_交接文入倉_自誤321_GB153歸因更正_GB154_telescoping定位.md`〔`D1+D2+D3`〕／
`docs/reports/W-G.9-260R_CC交接文.md`〔`D1+D2`〕／`docs/reports/W-G.9-260R_telescoping定位.md`〔`D1+D2`〕／
`docs/reports/W-G.4_泛用阻塞項登記表.md`〔`D2`·＝ `GB-154` 之標題列〕。

🔴 **量測器紅一（本節當場自捕·⛔ 出艙前已修）**：首版之 `D1` 得 **`0`**，而該三檔之**檔名確含該號**。
成因 ＝ `core.quotepath` 對 CJK 檔名輸出八進位跳脫，其**字面反斜線**被 Windows 之
`os.path.basename` 當作路徑分隔符 ⇒ `D1` 對 **CJK 檔名靜默歸零**。
修法 ＝ `git -c core.quotepath=false` ＋ POSIX `rsplit("/")`；並加**器自檢**：
含字面反斜線之路徑 ＝ **`0`**（須 `0`）／含 CJK 之路徑 ＝ **`587`**（須 `> 0`·證該檢有判別力）。
🔒 同族 ＝ 記憶 `git-quotepath-cjk-trap`。**⛔ 該自檢，此紅不可見**——`D1 = 0` 讀起來像「檔名沒有該號」。

### 零-2　🛑 已占之處置（⛔ 自行變更單號·逐字回報）

`CLAUDE.md`「入倉前之號占用閘」款 `一` 之要旨 ＝ **「已占即停並回報」** ＋ 其後之禁令
（**⛔ 自行變更單號後續辦**，並載明該變更屬**意思決定**、歸發單側／KL）。
🔒 該款之**逐字**含 `V-S` 之受檢字系 ⇒ 依 `V-S` **以代稱行之**；其逐字見 `CLAUDE.md` 該款。

**CC 之處置**：
1. 🔒 **⛔ 自行變更單號**（該款之明文禁令·本批一字未違）。
2. 🔒 **續辦**，其逐字理由 ＝ 本閘之受詞係「**該單所鑄之單號**」，而**本單首行逐字自載
   「`W-G.9-260 補令一（⛔ 鑄號·戒 36）`」** ⇒ **⛔ 有新號可占**；其與 `W-G.9-260` 之共號
   係**發單側明示之補令關係**，⛔ 撞號。
3. 🔴 **具名為<u>同形之第三例</u>·候發單側／KL 裁**：倉內既載二例——
   **作廢單**（「宣告框⛔ 排除『作廢單』⇒ 同號重發之單永遠過不了自己的閘」·`W-G.9-237` 節末）
   與 **零對照之出艙**（`W-G.9-192-PRE-b`「🔧 二」）。**補令**為第三形。
   🛑 **本節⛔ 就此作成任何條文、⛔ 放寬本款**——是否於宣告框增設「補令⛔ 計入」一款
   ＝ **意思決定** ⇒ 候發單側／KL。

### 零-3　原封入倉 ＋ `sha256` 驗

| 項 | 值 |
|---|---|
| 落檔 | `docs/orders/W-G.9-260_補令一_Q1Q2Q3判_Q4唯一開放題.md` |
| 位元組 | **`1502` B**／`LF 26`／**`CR 0`** |
| 工作區 `sha256` | `46994e427304eb0be95a9fef36183b3885a78c56e27584774a6dada1bec4493c` |
| 入倉 blob `sha256`（內容） | `46994e427304eb0be95a9fef36183b3885a78c56e27584774a6dada1bec4493c` |
| 判 | ✅ **逐位相符**（blob size ＝ `1502` ＝ 工作區 ⇒ 無 CRLF 正規化） |

🛑 **射程界限（照實·承 `作業常規之追加四` 之既載）**：本驗所證者為「**落檔 → 入倉**」之保真，
**⛔ 證「發單側送出之原文 → 落檔」之保真**——來源係聊天訊息、無獨立 `sha256` 可比。

🔒 **檔名（`常規三`·無土地後果·CC 提案·候發單側核可）**：候選三者 ＝
① `W-G.9-260_補令一_Q1Q2Q3判_Q4唯一開放題.md`（**採**）／② `W-G.9-260_補令一.md`／
③ `W-G.9-260補令一_Q4正典互斥.md`。**理由**：① 承 `docs/orders/` 既例之
`<單號>_<摘要>.md` 形，且摘要載本單之四個受詞，於清單中可自名而不必開檔。

---

## §一　補令 `一`：`Q-2` 二母體差集之**獨立出艙**（⛔ 採信發單側自報）

### 一-1　二母體之定義（自碼面直取·⛔ 轉述）

| 母體 | 定義（`verify/stepg_pipeline.py` 倉側 blob·字樣錨） | 元素之鍵 |
|---|---|---|
| **A**（`ΣRw` 母體） | `def _rw_real_wd2(_side_tag)` ⇒ `_adv_final['rows']` 中 `推進側別 == side` **且** `F(m) > 0` 者 | `暫編地號` |
| **B**（鏈母體） | `def _chain(_side_tag, _stage=None)` ⇒ 走 `_adv_final[f'{side}_results']`，逐筆以 `暫編地號` 回查 A，`_row is None or F<=0` 者略過 | 節點之 **`'id'`** |

🩸 **前批阻斷點之成因至此閉合**：前批探針以 `n.get('lot')` 抽 `_chain` 節點 id 而全得 `None`
——`'lot'` 係 **`_iface_deltas` 輸出**之鍵，**⛔ `_chain` 節點之鍵**（後者為 `'id'`）。
⇒ **⛔ 「node 結構非該形」，而是取錯了同名概念之另一支結構**。

### 一-2　量測法（探針·三 clone）

注入點 ＝ `verify/stepg_pipeline.py` 之 `_ifs_a = _iface_deltas(_nodes_a)` 之**次列**
（錨命中 **`1`**·判別力自檢：寬字樣 `_iface_deltas(` ＝ **`4`** ⇒ 該檢**非恆為 `1`**）。
探針**只讀不判**、例外一律 `raise`（⛔ 靜默退路），並附**反靜默退路計數器** `_PZ_HITS`。
`diff --numstat`：`t26c` ＝ `45/0`（僅 `stepg`）／`t26d` ＝ `1/1`（`app.py`）＋ `45/0`（`stepg`）。

🔴 **量測器紅二（當場自捕）**：首版注入之錨含 `\n`，而 clone 工作區為 **CRLF** ⇒ 錨命中 **`0`**。
修法 ＝ 行尾無關之逐列比對（`ln.strip() == 錨`）＋ 偵得行尾後以同行尾組塊。
🔒 同族 ＝ `CLAUDE.md`「位元組數／校驗碼須聲明量測框」（CRLF vs blob LF）。

🛑 **`0m·R2 left` 於<u>原態</u>結構上不可達**：`t26c`（原態閘開）之 `9` 筆記錄中**無** `0m·R2 left`
——`0m` 之 `R2` 於 `②-宗 圍堵閘`（`app.py` 之 `raise`）即中止，**⛔ 抵達 telescoping 閘**。
⇒ 另建 `t26d`，將該 `raise` **降級為 `print`**（**量測手段**·⛔ 修法主張·同前批 `t260` 之作法），
方得二情境齊備。**降級之錨**：訊息列命中 `1`（判別力自檢：寬字樣 `圍堵閘` ＝ `4`），
其上一列逐字為 `raise RuntimeError(`，`py_compile` 過。

### 一-3　🔒 差集（**本節即補令 `一` 所令之獨立出艙**）

**三軸**：來源 ＝ `t26d`（clone HEAD ＝ `4cad4e7f9da88892180b4f90d386b509ac4431e3`·三受詞 blob 逐位同主倉）／
母體 ＝ `verify/out/pz.jsonl` **`9`** 筆記錄（`_PZ_HITS` ＝ `9` ⇒ 反靜默退路計數器 `> 0`）／
粒度框 ＝ **宗**（`暫編地號` 之集合）。

| # | 情境 | 街廓·側 | `W₀` | `W_f` | `nA` | `nB` | **`A ∖ B`** | **`B ∖ A`** | `A` 重複 | `B` 重複 | 逐宗序逐位同 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `0m` | `R1` left | `-0.0000` | `24.4900` | 2 | 2 | **∅** | **∅** | 0 | 0 | ✅ |
| 2 | `0m` | `R1` right | `-0.4000` | `27.5500` | 3 | 3 | **∅** | **∅** | 0 | 0 | ✅ |
| **3** | **`0m`** | **`R2` left** | **`0.000000`** | **`26.030000`** | **7** | **7** | **∅** | **∅** | 0 | 0 | ✅ |
| 4 | `3.5m` | `R1` left | `-0.0000` | `17.5200` | 1 | 1 | **∅** | **∅** | 0 | 0 | ✅ |
| 5 | `3.5m` | `R1` right | `-0.4000` | `34.5500` | 4 | 4 | **∅** | **∅** | 0 | 0 | ✅ |
| **6** | **`3.5m`** | **`R2` left** | **`6.980000`** | **`34.390000`** | **7** | **7** | **∅** | **∅** | 0 | 0 | ✅ |
| 7–9 | `0m` | `R1` L／`R1` R／`R2` L（後續呼叫） | 同 1／2／3 | | | | **∅** | **∅** | 0 | 0 | ✅ |

🔒 **`R2 left` 之逐宗序（二情境·⛔ 取樣）**
- `3.5m`：`628-42(1)` → `628-41(1)` → `628-27(1)` → `628-40(1)+` → `628-30(2)` → `628-39(1)` → `628-31(2)`
- `0m`：**`628-41(1)`** → **`628-42(1)`** → `628-27(1)` → `628-40(1)+` → `628-30(2)` → `628-39(1)` → `628-31(2)`
  （🔑 **首二宗互易**——即 `Q-3` 之 `W` 倒退所在）
- 二情境之 `A`／`B` **逐位同序**（⛔ 僅集合相等）。
- 該側 `rows` **全體**（未濾 `F`）二情境皆 `n = 7`、`F ≤ 0` 者 **`[]`** ⇒ **`F > 0` 之濾網於本案未汰任何宗**。

🔒 **判**：**發單側之實測成立**——`A ∖ B ＝ ∅` 且 `B ∖ A ＝ ∅`，二情境 `R2 left` 各 `7` 宗、全 `F > 0`。
🔴 **前批 CC 之強假說（「有宗在 `rows` 而不在 `results`」）<u>確被推翻</u>**，本節係其**獨立復現**，⛔ 轉述。

🛑 **本節之界限（照實）**：`B ⊆ A` 於 `_chain` 之**構造上即成立**（其逐筆自 A 回查且濾 `F ≤ 0`）
⇒ `B ∖ A ＝ ∅` 係**結構必然**、其為空**⛔ 構成證據**；**有證據力者為 `A ∖ B ＝ ∅`**
（即「`rows` 中無任何宗未進入 `{side}_results`」）。二者⛔ 等值，出艙時⛔ 併為一句。

---

## §二　補令 `二`／`三`：`Q-1`／`Q-3` 之獨立佐證（**照實·含與補令所載數之差**）

### 二-1　`Q-1`（`3.5m`）——**獨立坐實**

`rw_from_width` 之表（`app.py` 倉側 blob·逐字）：`W=6 → 50.0`／`W=7 → 55.7`，中間值線性內插。
⇒ **`R(6.98) ＝ 50.0 + (55.7 − 50.0) × 0.98 ＝ 55.586`**（本窗獨立算得·⛔ 引前批）。

| 判準 | 值 | 判 |
|---|---|---|
| 鏈首宗 `628-42(1)` 之 `Rw`（實測） | **`55.59`** | ＝ `round(55.586, 2)` |
| 若自 `W₀ = 6.98` 起算 ⇒ `R(6.98) − R(6.98)` | `0` | ✗ |
| 若自 `W = 0` 起算 ⇒ `R(6.98) − R(0)` | **`55.586`** | ✅ |

🔑 **逐界面之佐證（`t26d` log 之 `[Δ_i·R2·left·總量]` 列·逐字）**：`3.5m` 之六個界面
`Δ` **全為 `+0.0000`**、`Δ_self` 亦全 `+0.0000` ⇒ **鏈逐界面全未斷**。
⇒ 🔒 **差不在界面之間，在鏈首宗自身之起算點**——`_iface_deltas` 於鏈首**⛔ 產生界面**
（其自 `_prev_far = _w0_prev`（總量段為 `None`）起跑）⇒ 首宗之起點**結構上不在該閘之視野內**。

🔴 **與補令 `二` 所載之數之差（照實·`數字錨引倉檔`）**
補令 `二` 逐字載「鏈自 `W=0` 起累、**`Σ ＝ 100.000`**」。**本窗實測**：

| 量 | 值 | 出處 |
|---|---|---|
| `Σ Rw_實跑`（逐宗 `2dp` 相加） | **`100.02`** | `55.59+20.34+2.84+21.25+0+0+0`（本窗 PZ 逐宗表） |
| `R(W_f = 34.39) − R(0)`（理論） | **`100.000`** | `W ≥ 18 ⇒ 100`；`R(0) = 0` |
| 差 | `0.02` | 逐宗 `Rw(%)` **`2dp` 捨入累積**（前批已具名） |

⇒ 🔒 **二數係<u>不同的量</u>（理論 vs 實跑），⛔ 相斥**；補令所載之 `100.000` ＝ **理論值**。
⚠️ 併記：閘之期望 `R(34.39) − R(6.98) ＝ 44.414` ⇒ `Δ ＝ 100.02 − 44.414 ＝ 55.606` ≈ `R(6.98) ＝ 55.586`（殘差 `0.020`）。

### 二-2　`Q-3`（`0m`）——**獨立坐實**

`R(4.06) ＝ 37.8 + (44.0 − 37.8) × 0.06 ＝ 38.172`；`R(3.11) ＝ 31.3 + (37.8 − 31.3) × 0.11 ＝ 32.015`
⇒ **原始增量 ＝ `32.015 − 38.172` ＝ `−6.157`**（本窗獨立算得）⇒ 經
`rw_increment` 之 `return max(0.0, …)`（`app.py` 逐字）**夾為 `0`**。
**實測**：`628-42(1)` 之 `Rw ＝ 0.0`（PZ 逐宗表）✅

| 量 | 值 |
|---|---|
| `Σ Rw_實跑` | **`106.16`**（`38.14+0.0+4.18+36.18+8.46+17.41+1.79`） |
| `R(W_f = 26.03) − R(W₀ = 0.00)`（閘期望） | **`100.000`** |
| `Δ` | **`6.160`** |
| 夾斷所吞之量（獨立算得） | **`6.157`** |
| 殘差 | `0.003`（逐宗 `2dp` 捨入） |

⇒ 🔒 **補令 `三` 之機制獨立坐實**：`628-41(1)`(`W=4.06`) → `628-42(1)`(`W=3.11`) 之 `W` 倒退，
其負增量 `−6.157` 遭夾為 `0`，該段遂被計二次。
🔑 **逐界面之交叉**：該界面之 `Δ ＝ +0.0000` 而 **`Δ_self ＝ −11.3570`**，且經標記為**方向變動**
（該標籤於 log 之逐字含 `V-S` 受檢字系 ⇒ 以代稱行之）
⇒ **逐界面閘對此全盲**（其量 `W_start` 對 `W_far`，二者皆 `4.0600` ⇒ 恆 `0`）。

⚠️ **精度之界（照實）**：`W` 欄係 `2dp` 顯示值。以顯示值算得之 `R(4.06) − R(0) ＝ 38.172`
而實測 `Rw ＝ 38.14` ⇒ 真值 `W ≈ 4.0555`。⇒ **上開各式之末二位⛔ 逐位可信**，
其**符號與量級**則不受影響（`−6.157` vs `Δ 6.160` 之相符即其佐證）。

---

## §三　補令 `四`：`Q-4` **正典互斥之具名**（⛔ 主張何者為準）

### 三-1　行號之現查（`行號衛生`·單所載三處**皆準**，態 ＝ `4cad4e7`）

| 單所載 | 現查 | 逐字 | 字樣錨（窄／寬命中） |
|---|---|---|---|
| `app.py:5757` | ✅ `app.py:5757` | `側街負擔總量 = Σ 各筆 = (R(W_last)−R(0))/100 = 100%（telescoping，§3 主驗收）。` | `側街負擔總量 = Σ 各筆` **1**／`側街負擔` **26** |
| `stepg:1437` | ✅ `verify/stepg_pipeline.py:1437` | `_tele_exp = _rw_from_width(_sd_Wf) - _rw_from_width(_sd_W0)` | `_tele_exp = _rw_from_width(_sd_Wf)` **1**／`_tele_exp` **3** |
| `stepg:836-837` | ✅ `:836`–`:837` | `if not _W0_left_set:` ／ `_W0_left = float(res.get('W_near', 0.0)); _W0_left_set = True` | `_W0_left = float(res.get('W_near'` **1**／`_W0_left` **5** |

對照組乙（必不存在·代稱·執行期組出）＝ **`0`** ⇒ 器非紅。
🛑 **`:835` 逐字為 `if _has_left_corner:`** ⇒ **`W₀` 之捕獲受該旗標所閘**（`b1` 之交會點·見 `§四`）。

### 三-2　🔒 立法出處之時序（**全部自 `git log -S` 之逐 commit 取得·⛔ 憑記憶**）

| 序 | 日 | commit | 單號 | 受詞 |
|---|---|---|---|---|
| `1` | **`2026-07-05`** | `1b290e4` | ⛔ **無**（`初始遷移:市地重劃試分配系統(淨土倉,零歷史)`） | **(a)** `app.py:5757` docstring 入倉。🔒 **`-S` 全史僅此 `1` 筆** ⇒ 此後**⛔ 曾變動** |
| `2` | **`2026-07-10`** | `40e747f` | `v3 財務接線波` | **(b)** 之閘引入；其 commit message 逐字列 `telescoping(ΣRw_側 = R(W_final) − R(W₀_buffer))` |
| `3` | **`2026-07-19`** | **`687b5d4`** | **`W-G.4 S1`** | 🔑 **`stepg` 之 `W₀` 捕獲式落為 `res['W_near']`（mp-based）**。commit message 逐字自載「**未完(待 `_select_pool_slot` 理論同步＋`ΣRw<100%` 正典裁)**」 |
| `4` | `2026-07-19` | `f2fd44f` | `W-G.4 S1` | 上呈報告入倉：`docs/reports/W-G.4_S1_W脫鉤打樣_樣張＋ΣRw正典裁上呈.md`（Q ＝ **(甲) clamp** vs **(乙) 照實**·「**CC 不自裁**」） |
| `5` | **`2026-07-19`** | `26f9c02` | **`W-G.4 補丁八`** | 🔴 **KL 裁（乙定案）**：`W-G.4 補丁八入倉（乙定案）：首宗 W₀=mp 照實·ΣRw<100% 池吸收…` |
| `6` | **`2026-08-08`** | `664e4f5` | `K-6-A2 D-2b-4` | **`K-9-5-6`（KL 追認）**入典：`ΣRw_側 = R(W_最後一宗) − R(W_0)`；`_tele_exp` 之**現形**亦於此批定 |
| `7` | **`2026-08-12`** | `af8501b` | **`W-G.9-15`** | 🔴 **`K-9-5-12`（KL 裁 `2026-08-12`）**：撤回「`W_0 ＝ MP→corner`」，**首宗起點一律為 `0`（自 MP 起量）** |

### 三-3　🔒 **何者較晚**（單所令之具名·⛔ 主張何者為準）

> 🔒 **(a) 較早**：`2026-07-05`（`1b290e4`·**⛔ 單號**·淨土倉初始遷移）。
> 🔒 **(b) 較晚**：其**引入** ＝ `2026-07-10`（`40e747f`·單號 ＝ `v3 財務接線波`）；
> 其**現形** ＝ `2026-08-08`（`664e4f5`·單號 ＝ `K-6-A2 D-2b-4`·裁定 ＝ **`K-9-5-6`**）。
> ⇒ **無論取引入日抑或現形日，(b) 皆晚於 (a)。**

🔑 **併具名（⛔ 單所問，惟為同一互斥之必要環節）**：**典側最晚之裁 ＝ `K-9-5-12`（KL 裁 `2026-08-12`·`W-G.9-15`·`af8501b`）**，
其 `（五）效力` 逐字第 `1` 款 ＝ **「首宗起點一律為 `0`（即自 **MP** 起量）」**。

🛑 **其第 `3` 款逐字**：
> `⚠️ 該行**後半**（`forced` 時為其 buffer 起算點）**⛔ 未撤，列為未決**——見該處加註。`

🔴 **本案恰落於該未決之射程內**：`3.5m·R2 left` 係 **forced 帶**
（`docs/reports/W-G.4_S1_W脫鉤打樣_樣張＋ΣRw正典裁上呈.md` 逐字：
「**3.5m forced 帶**（R5左/R2左/R3右）：buffer 占低 W 域·lots 自 buffer 遠緣起 → ΣRw 再降（…**R2左 54**…）」）
⇒ 其 `W₀ ＝ 6.98`（實測·＝ 退縮 `3.5` ＋ 畸零寬 `3.5` 之量級）**⛔ `0`**。
而 `0m·R2 left`（**非** forced·有私有街角勝者）之 `W₀ ＝ 0.000000`（實測）。
⇒ 🔒 **二情境之 `W₀` 之別，與「該側有無私有街角勝者」逐格對位**（`§四` 表）。

🛑 **本節⛔ 述其應為何**（單 `§五 b Q-4` 與補令 `四` 之逐字）。以上皆**時序與逐字之具名**。

---

## §四　補令 `四 (b1)`：`_has_left_corner` 之來源與其與「第1筆街角」欄之關係

### 四-1　二受詞之來源（碼面直取·倉側 blob）

| 項 | 產生式（逐字） | 位置 | **層** | **受詞** |
|---|---|---|---|---|
| `_has_left_corner` | `_has_left_corner = _side_mid_left is not None`；其中 `_side_mid_left = _sl_left.get('mid')`、`_sl_left = (ss['f3_cad_side_lines_by_side'] or {}).get(blk_label, {}).get('left')` | `stepg:681`（`:679`–`:681`） | **街廓層** | 該街廓**左側之 `SIDE_LINE` 中點是否存在**（＝該側有無側街線·**幾何存在性**） |
| 「第1筆街角」欄 | `'第1筆街角': '是' if _is_first_corner else '否'` | `stepg:487`（`app.py:21521` 同構） | **宗地層** | 該宗**是否為該側之街角第 1 宗**（**角色**） |
| 其上游 | `is_first_corner_l = (bool(entry.get('is_first_corner_marker', False)) and not first_corner_used_left)` | `stepg:803`–`:806` | 宗地層 | `is_first_corner_marker` ＝ 街角 PK 之產物 |

⇒ 🔒 **二者不同層、不同受詞、⛔ 互為充要**：
`_has_left_corner` 問**街廓有無側街線**；「第1筆街角」問**某宗是否為該側街角第 1 宗**。

### 四-2　🔒 實測之對照（**二情境·同一街廓同一側**·判別力自證）

| 情境 | `_has_left_corner` | 「第1筆街角＝是」筆數 | `is_first_corner_marker ＝ True` 筆數 | 「街角地＝是」 | `W₀` |
|---|---|---|---|---|---|
| **`3.5m`·`R2` left** | **`True`** | **`0` ／ `7`** | **`0` ／ `7`** | `0` ／ `7` | **`6.980000`** |
| **`0m`·`R2` left** | **`True`** | **`1` ／ `7`**（`628-41(1)`·街角側別 `左側`） | **`1` ／ `7`**（`628-41(1)`） | `1` ／ `7` | **`0.000000`** |

🔒 **判別力自證**：同一街廓、同一側、同一旗標值（`True`），而「第1筆街角」欄 `0` vs `1`
⇒ **該對照非恆同**、⛔ 恆綠。
⇒ 🔒 **「七宗全否 ∧ `_has_left_corner ＝ True`」在結構上<u>相容</u>**，⛔ 矛盾。

### 四-3　倉內既載之成因（**逐字·⛔ 轉述**）

`docs/reports/W-G.4_v3偵察補查報告.md:23`：
> `- R2左/R3右/R5左**無私有街角勝者**（原本就 forced·抵費地充當第1宗）→ F.0_G值 無其「第1筆街角=是」列。`

⇒ 🔒 **`R2 左`於 `3.5m` 經倉內既載為「無私有街角勝者·抵費地充當第 1 宗」** ——與本窗實測逐格相符。

### 四-4　🔒 二者於 `W₀` 之**交會點**（本節之核心具名）

`stepg:835` 逐字 `if _has_left_corner:` ⇒ **`W₀` 之捕獲以<u>街廓層</u>旗標為閘**；
其所取之值為 `res.get('W_near')`，即**該側推進鏈<u>首宗</u>之近側 `W`**，
⛔ 論該首宗是否為「第1筆街角」。
而該捕獲式之 docstring（`stepg:775`–`:778`）逐字含：
> `首宗解出後捕獲（首宗＝角落宗·其近側＝buffer 之遠緣）`

⇒ 🔒 **具名（⛔ 主張其應為何）**：**閘之受詞（街廓層·`_has_left_corner`）
與 docstring 所述之情形（首宗＝角落宗·宗地層）⛔ 同一**；
二情境之實測（`§四-2`）證該二者**確可分離**——`3.5m` 之首宗 `628-42(1)`
其「第1筆街角」為**否**、`marker` 為 **`False`**，而 `W₀` 仍被捕獲為其 `W_near ＝ 6.98`。

🛑 **本節⛔ 述其應為何**（補令 `四 (b1)` 之逐字）。

---

## §五　補令 `五`：`t26a` 型（**原態·閘開**）之紅項數與其計數式

🔒 **本節係 `GB-153` 更正「二」之 CC 側<u>第二次</u>獨立復現**（第一次見
`docs/reports/W-G.9-260R_telescoping定位.md` `§三 a`·紅 `24`）。

**受測物之同一性**：`1531916..4cad4e7` 之異動**全在 `docs/`**、
生產碼四檔命中 **`0`**；`t26b` clone 之三受詞 blob 內容 `sha256`
（`app.py 4860f10828fb8ae9`／`stepg c8d46d2e5fb79dc5`／`wf_f0 b0722355c1978635`）
與前批報告**逐位相符** ⇒ **同一受測物**。

**三軸**：來源 ＝ `t26b`（clone HEAD `4cad4e7f9da88892180b4f90d386b509ac4431e3`·**原態·⛔ 注入**）／
母體 ＝ `verify/out/t26b_runall.log`（**`49162` B ／ `543` 列**）／粒度框 ＝ **列框**。

| 計數式（逐字） | 值 |
|---|---|
| **框 A**　`grep -c "🔴 FAIL" verify/out/t26b_runall.log` | **`24`** |
| 框 B　`grep -c "^🔴 FAIL" …` | `0`　⚠️ 列首錨·**因該類列皆有縮排而為 `0`** |
| 框 C　`grep -c "^ *🔴 FAIL  " …` | **`24`** |
| 對照組（器須綠·須 `> 0`）　`grep -c "✅ PASS" …` | **`10`** ✅ |
| 對照組乙（必不存在·代稱·執行期組出） | **`0`** ✅ |
| `RESULT:` 之末筆 | `RESULT: FAIL`（`:423`）／`W-V run_all: FAIL` |
| `rc` | **`1`**（**准紅碼**之預期·⛔ 新紅） |

⇒ 🔒 **紅項數 ＝ `24`**，與前批**逐位相符**；
🛑 **框 B 之 `0` 係<u>框之別</u>、⛔ 態之別**——⛔ 以其單獨出艙（`VR-091` 之三軸即為此立）。

---

## §六　【驗收】

| 閘 | 實測 |
|---|---|
| 工項零·占用 | ✅ 三數分列出艙（`D1 3`／`D2 5`／`D3 1`／雙屬 `0`／列框 `6`／檔框 `4`）＋二對照組；🔴 **已占** ⇒ 逐字回報、**⛔ 自行變更單號**、具名為同形第三例·候發單側／KL |
| 工項零·入倉 | ✅ 工作區 `sha256` ＝ 入倉 blob `sha256` ＝ `46994e42…`（`1502` B·`CR 0`） |
| 補令 `一` | ✅ 差集**獨立出艙**：二情境 `R2 left` 各 `7` 宗、`A∖B ＝ ∅`、`B∖A ＝ ∅`、逐宗序逐位同；併具名 `B ⊆ A` 係構造必然（⛔ 證據） |
| 補令 `二` | ✅ `R(6.98) ＝ 55.586` 獨立算得；`Σ_實跑 ＝ 100.02` vs 補令所載理論 `100.000` **二數之別已具名** |
| 補令 `三` | ✅ `−6.157` 獨立算得並與 `Δ ＝ 6.160` 對上（殘差 `0.003` ＝ `2dp` 捨入） |
| 補令 `四` | ✅ 三處行號現查**皆準**；七項時序逐 commit 出艙；**(b) 較晚**已具名；⛔ 主張何者為準 |
| 補令 `四 (b1)` | ✅ 二受詞之產生式、層、受詞逐字具名；二情境對照（`0` vs `1`）證判別力；⛔ 主張其應為何 |
| 補令 `五` | ✅ 紅 **`24`** ＋ 三框計數式 ＋ 二對照組 ＋ 三軸 |
| `V-A`（受詞不動） | ✅ 主倉 `app.py`／`stepg_pipeline.py`／`wf_f0.py` 之 blob 前後逐位相同 |
| **零生產碼** | ✅ `git diff --name-only <前一已 push 之 HEAD>..<本 commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'` ⇒ **`0` 行**（見 `§七`） |
| `deletions` | ✅ 本批 `git diff --numstat` 之刪除欄逐列 `0`（純新增檔） |
| `V-S`（射程閘） | 見下 |

🔒 **`V-S` 之自檢（以代稱行之·受檢字樣⛔ 逐字寫入本文·其逐字見原單 `§六 V-S` 欄）**
本報告內**⛔ 含修法主張**；該欄所列之**三字系**於**終稿**之命中 ＝ **`0`／`0`／`0`**
（本窗以程式對本檔全文實測·受檢字樣於執行期由碼位組出、⛔ 落入倉內任何檔）。
**判別力對照（同一支量測器）**：`具名` ＝ `18` 列／`實測` ＝ `13` 列（須 `> 0`·⚠️ 此二數含**本節自身**之貢獻
＝ `GB-132` 族之自指增長，故其為**終稿**之值）✅；
一必不存在之同族字樣（代稱）＝ `0` 列（須 `= 0`）✅ ⇒ **器非紅**。

🩸 **本閘之首跑為<u>紅</u>（照實·入倉前自捕）**：初稿之字系一命中 **`6` 列**，
而該時 `§六` 已書「命中 ＝ `0`」⇒ **該宣稱當時為偽**。
🔑 **其得以被擋下之唯一原因** ＝ **閘之量測與其所守護之動作（入倉）係分開之呼叫**
（`W-G.9-199` 常規補款 `一`）。若併於同一命令塊，該偽宣稱即隨報告入倉。
🔒 **逐筆歸類（依 `W-G.9-199` 裁 `H`：命中數係<u>待歸類之材料</u>、⛔ 判準本身）**：
`2` 列 ＝ **倉內既有文字之逐字引用**（`CLAUDE.md` 之節題與款文）／
`3` 列 ＝ **否定式**（其語義為「⛔ 為之」·非祈使性主張）／
`1` 列 ＝ **log 之逐字標籤**。
⇒ **`6` 列皆非祈使性修法主張**（性質面未破）；惟依 `V-S` 之「**代稱行之**」明令，
六處已逐一以代稱重寫，終稿之字面命中歸 `0`。**⛔ 以「反正性質沒破」略過重寫。**

🩸 **本窗之量測器紅二度（皆於出艙前自捕·逐條具名）**
1. `core.quotepath` × CJK 檔名 × Windows `os.path.basename` ⇒ **`D1` 靜默歸零**（`§零-1`）。
2. clone 工作區 **CRLF** × 含 `\n` 之錨 ⇒ **注入錨命中 `0`**（`§一-2`）。
🔒 **二者皆由「該數理應非零」之預期擋下**——⛔ 由任何自動閘。
⇒ 併記為 `§零-1`／`§一-2` 之戒：**凡新寫之量測器，其首次輸出若含 `0`，一律先當作器紅。**

---

## §七　出艙

- 施工單（原封）：`docs/orders/W-G.9-260_補令一_Q1Q2Q3判_Q4唯一開放題.md`
- 報告：`docs/reports/W-G.9-260R2_補令一_Q2差集_Q4互斥具名.md`（本檔）
- 🔒 三 clone（`C:/t26b`／`C:/t26c`／`C:/t26d`）**跑畢整份刪除**（`GB-94`）。
