# 📐 `W-G.9-167R`　`VR-074` 落地**前置量測** ＋ 預測 diff 集合 ＋ `GB-114` ＋ 自誤 `200`

**受單**：`W-G.9-167`（`SELF_SHA256 = 81ca56b03f5c04da7ca22b531d07e54a59b1791ec83d5d9d2d6edd7a643fa00b`·CC 覆算相符·四反引號 `0`）
**基座**：`6b77b4a0dca0ffe72069295b7e47d0c59a9afaf1`
**性質**：**只讀量測 ＋ 純新增**（⛔ 零生產碼·⛔ 未改 `app.py`／`verify/*.py` 既有一字·⛔ 未落地 `VR-074`）

## 📊 §零　結論（先講）

| # | 事 | 值 |
|---|---|---|
| `1` | `M-1 d`　三判準合取 vs `_is_ghost_sliver` 之**對稱差** | **空**（二層皆然）⇒ `X-1` ✅ **符** |
| `2` | 🔴 `M-1 e`　②③ 於二層之**鍵存在性** | **`0`／`0`** ⇒ ②③ **恆真** ⇒ 合取 **≡ ①**（⛔ 非更嚴） |
| `3` | `M-2a`　位次改變之真實宗地 | **`5`** 筆（`R1` `4` ＋ `R4` `1`）·差值**皆 `−1`** ⇒ `X-2` ✅ **符** |
| `4` | `M-2b`　winner | **二情境皆零改變**（指配列差異之鍵 ＝ `0`）⇒ `X-3` ✅ **符**·**⛔ 無土地後果** |
| `5` | `M-2c`　預測 diff | **每情境 `4` 格**，皆為 `原位次(投影序)`；**對拍靶 `4` 檔 × `4` 格 ＝ `16` 格** |
| `6` | 🔴 `X-4` | **不符**——受影響之**對拍靶為 `4` 檔**（⛔ 非「現行二份」）·凍存為 `6`（⛔ 非八）⇒ 鑄 **`GB-114`** ＋ **自誤 `200`** |
| `7` | `M-2d`　`_GHOST` 於 CSV | **`0`**（`baselines` `266` 檔／`out` `10` 檔）⇒ `X-5` ✅ **符** |
| `8` | 🔴 `M-3 d`　改動點數 | **⛔ 非 `1`**（忠於宣告表之作法 ≥ **`8`** 處；`1` 處之作法須犧牲宣告之真實性）⇒ `X-6` **不符** |

🛑 **本批⛔ 未出任何落地規格**（單 `§零之零` 逐字）；唯一產出 ＝ 可機械複驗之預測集合
`verify/out/WG9167_predicted_diff.json`（**已入倉**）。

---

## §一　開工閘

| 錨 | 期望 | 實測 | |
|---|---|---|---|
| `HEAD` 全 `40` 碼 | `6b77b4a0dca0ffe72069295b7e47d0c59a9afaf1` | 同 | ✅ |
| `app.py` blob（期初＝期末） | `f285b91c9e5f97f13dd74508e1a403833bc75cb3` | 同 | ✅ |
| 工作樹 | 淨 | `git status --porcelain` ＝ `0` 列 | ✅ |

clone ＝ `/c/w167`（**拋棄式**）。🔒 **本批⛔ 未跑 `run_all`**（只跑二支新探針）⇒ `verify/out/**` 之**已追蹤** log 零異動（`GB-94` 之副作用未觸）。

### 🔒 量測法（**執行期層疊**·⛔ 零生產碼）

`VR-074` 態之模擬 ＝ 把 `ns["_projection_order"]` 包一層，於排序**之前**濾掉三判準合取為真者。
🔒 **射程自證**：`ns is _projection_order.__globals__` ＝ **`True`** ⇒ 層疊亦及於 `app.py` 內部之直呼。
🔒 **反靜默退路計數**（`CLAUDE.md` 之戒）：層疊共**呼叫 `12` 次／濾掉 `4` 列** ⇒ **確有咬到**（⛔ 非「零變化」之假結論）。

---

## §二　取號現查（定義列式 ＋ 錨定框 ＋ 二形並取）

🔒 母體 ＝ `docs/**/*.md` ＋ `CLAUDE.md` ⇒ **`395` 檔**；態 ＝ `blob@6b77b4a`。

| 待鑄 | 定義列式（逐字） | 相異 | MAX | 錨定框 `X N` | `` X `N` `` | 對照組 |
|---|---|---|---|---|---|---|
| `W-G.9-167` | `^docs/reports/W-G\.9-(\d{1,4})` | — | **`166`** | `0` | — | `W-G.9-166` ＝ `10` |
| `GB-114` | 三框聯集（母體 ＝ 登記表**單檔**） | `111` | **`113`** | `0` | — | `GB-113` ＝ `11` |
| 自誤 `200` | `` ^#+ 自誤 `?([0-9]{1,4})`? ``（母體 ＝ 自誤登記**單檔**） | `164` | **`199`** | `0` | `0` | `自誤 199` ＝ `2`／`` 自誤 `199` `` ＝ **`4`** |

🔒 **`GB` 算術自檢（常規八·外部錨已具名）**：`113 − |{12, 87}|(2) == 111` ⇒ **True**；
**外部錨** ＝ `docs/reports/W-G.9-144_開工閘與二項現查.md:81` 之**式A**（`` ^## `GB-N` ``·二級標題·`3` 則）。

---

## §三　`M-1`　三判準合取 vs `_is_ghost_sliver`（逐層·逐街廓）

| 層 | `a` 總列數（**對照組**） | `b` 三判準合取 | `c` `_is_ghost_sliver` | `d` **對稱差** |
|---|---|---|---|---|
| `TEMP_LAYER`（`temp_parcels`） | **`114`** | **`8`** | **`8`** | **空** ✅ |
| `BUILD_LAYER`（`build_parcels`） | **`49`** | **`2`** | **`2`** | **空** ✅ |

**逐街廓**（僅列有 ghost 者）：`TEMP_LAYER` ＝ `G1` `2`／`R1` `1`／`R4` `1`／`RD1` `1`／`RD2` `1`／`RD3` `2`；
`BUILD_LAYER` ＝ `R1` `1`（`_GHOST_(R1)`）／`R4` `1`（`_GHOST_(R4)`）。

### 🔴 `M-1 e`　三判準之單獨命中 ＋ **鍵存在性**（本批之關鍵發現）

| 層 | ① `原地號=='_GHOST'` | ② `G(㎡)==0` | ③ `a 面積(㎡)==0` | 有 `G(㎡)` 鍵者 | 有 `a 面積(㎡)` 鍵者 |
|---|---|---|---|---|---|
| `TEMP_LAYER` | `8` | **`114`**（＝全體） | **`114`**（＝全體） | **`0`** | **`0`** |
| `BUILD_LAYER` | `2` | **`49`**（＝全體） | **`49`**（＝全體） | **`0`** | **`0`** |

🔴 **合取是否比任一單項更嚴？** vs① **`False`**／vs② `True`／vs③ `True`。
🔒 **成因（碼面）**：`G(㎡)`／`a 面積(㎡)` 係 **`g_row` 之欄**（`_build_g_row`·`grep -n "'a 面積(㎡)': _a_m2," app.py`），
⛔ **非 parcel dict 之鍵**（ghost 之建構處 `app.py:4636-4649` 只寫 `原地號`／`暫編地號`／`幾何面積_m2`／`面積_m2`／`_is_ghost_sliver` 等）
⇒ 於本二層 `dict.get(..., 0)` 恆回 `0` ⇒ **②③ 恆真**。
🛑 **⇒ 本層施用 `K-9-19 一` 之三判準，其效果<u>恰等於單條 ①</u>**（`原地號 == '_GHOST'`）。
⚠️ 此**⛔ 非主張正典有誤**——`K-9-19 一` 之受詞係 `g_row`（`K-9-4` 之母體），本層係 parcel；
🛑 **落地時須明示「三判準於 parcel 層之等價化」，⛔ 不得讓讀者誤以為②③在此層有鑑別力**（交發單側落筆）。

---

## §四　`M-2a`　位次之變動

⚠️ **受詞更正**：`_rank_by_tpid` 係 **per-block**（`verify/selection_pipeline.py:339`／`app.py:19135`），
⛔ **非 per-side** ⇒ 單 `§三 M-2a` 之「逐街廓**逐側**」於碼面**無受詞**，本節逐**街廓**報。

| 街廓 | `n`（現況） | `n`（`VR-074`） | 位次改變 | 逐筆（現況 → `VR-074`） |
|---|---|---|---|---|
| `R1` | `6` | `5` | **`4`** | `628-37(1)` `3→2`／`628-36(1)` `4→3`／`628-34(3)` `5→4`／`628(5)` `6→5` |
| `R2` | `14` | `14` | `0` | — |
| `R3` | `10` | `10` | `0` | — |
| `R4` | `3` | `2` | **`1`** | `628-1(1)` `3→2` |
| `R5` | `8` | `8` | `0` | — |
| `R6` | `8` | `8` | `0` | — |

🔴 **全區 `差 ≠ 0` ＝ `5` 筆**；**差值集合 ＝ `[−1]`**（⛔ 無他值）。
🔒 **對拍錨（⛔ 非判準·單 `§三` 逐字）** `W-G.9-148R M-3`：`R1` `n−r = 4`／`R4` `n−r = 1`（合計 `5`）
⇒ 本批逐街廓實測 `{'R1': 4, 'R4': 1}` ⇒ **逐街廓相符** ⇒ `X-2` ✅ **符**。

---

## §五　`M-2b`　PK winner（**二態實跑對拍**·⛔ 非假定）

| 情境 | 指配列數（現況／`VR-074`） | 鍵集相同 | 🔴 **指配列有差異之鍵** | 群數 | 含 4dp 同分之群 |
|---|---|---|---|---|---|
| `0m` | `6` ／ `6` | `True` | **`0`** | `8` | `5` |
| `3.5m` | `6` ／ `6` | `True` | **`0`** | `8` | `5` |

**同分群之相對序**（每群同分值皆為 `0.0`）：`0m` 與 `3.5m` 各 `5` 群、逐群逐組比對，
**現況序與 `VR-074` 序⛔ 全部未翻轉**（逐字見 log）。

⇒ 🔒 **`X-3` ✅ 符：winner 全數不變 ⇒ ⛔ 無土地後果**（單 `§五` 之停機條件未觸）。
🔒 **機制**：移除 ghost 只令其**後方**之宗各 `−1`（`§四` 實測差值集合 ＝ `[−1]`）⇒ 真實宗之**相對序保持**
⇒ 任何以 `(-round(pi,4), rank)` 為鍵之排序（`app.py:12482-12485`）其結果不變。**本批係求證、⛔ 非假定。**

---

## §六　`M-2c`　🔴 預測 diff 集合（**本批主產出**·已入倉）

**檔** ＝ `verify/out/WG9167_predicted_diff.json`（機器可讀·**窮舉**）。

### 六-1　逐格（**二情境相同**）

| 情境 | 有差之格 | 涉及欄 | 逐格 |
|---|---|---|---|
| `0m` | **`4`** | `原位次(投影序)`（**唯一**） | `R1·右·628(5)` `6→5`／`R1·左·628-36(1)` `4→3`／`R1·左·628-37(1)` `3→2`／`R4·右·628-1(1)` `3→2` |
| `3.5m` | **`4`** | 同上 | **與 `0m` 逐字相同** |

⇒ `X-7`（二情境得同一組）✅ **符**。診斷列數 `0m` `17`／`3.5m` `20`，二態鍵集**相同**。

### 六-2　🔴 映射至 baseline CSV（**角色以 `run_verification` 之常數判**·⛔ 非字串猜測）

| 檔 | 列數 | 預測有差之格 | **角色** |
|---|---|---|---|
| `verify/baselines/v3/W-D.1.2 診斷_退縮0m.csv` | `18` | **`4`** | **對拍靶·全欄逐格**（`run_verification.py:606`） |
| `verify/baselines/v3/W-D.1.2 診斷_退縮3.5m.csv` | `21` | **`4`** | 同上 |
| `verify/baselines/W-D.1.2 診斷_退縮0m.csv` | `18` | **`4`** | **對拍靶·無串聯**（`:613`·`skip_cols` 對本欄**失效**） |
| `verify/baselines/W-D.1.2 診斷_退縮3.5m.csv` | `21` | **`4`** | 同上 |
| `PRE-S0b_S0c_幾何漏歸零_凍存/v3/…0m`／`…3.5m` | `18`／`21` | `4`／`4` | 凍存·⛔ 非對拍靶 |
| `PRE-比率更新_凍存/v3/…0m`／`…3.5m` | `18`／`21` | `4`／`4` | 凍存·⛔ 非對拍靶 |
| `PRE勘誤_期日/v3/…0m`／`…3.5m` | `18`／`21` | `4`／`4` | 凍存·⛔ 非對拍靶 |

🔒 **窮舉之另一半（單 `§三 M-2c` 逐字要求）**：`verify/baselines/**` 之 CSV 共 **`266`** 份（**對照組**·必然非零），
其中含 `原位次(投影序)` 欄者 **`10`**、**⛔ 不含者 `256`** ⇒ 後者**逐檔判為不變**、檔名清單已列於 JSON 之 `unchanged_files`。
⇒ 🔒 **`168` 之驗收**：期初 vs 期末之差**恰為對拍靶 `4` 檔 × `4` 格 ＝ `16` 格**；**多一格或少一格皆停機**。

### 六-3　🔴 `X-4` **不符**之逐項

| 單之預測 | 實測 |
|---|---|
| 「受影響 ＝ **現行二份**（`verify/baselines/W-D.1.2 診斷_退縮{0m,3.5m}.csv`）」 | ⛔ 不符——**全欄逐格**之靶係 `verify/baselines/**v3/**` 二份（`:606`·`V3RUN`）；頂層二份係**另一支閘**（`:613`·`BASELINES`·無串聯） |
| 「凍存**八份**⛔ 不受影響」 | ⛔ 不符——凍存實為 **`6`** 份（`PRE-*` 三組）；`v3/` 二份係**現行對拍靶** |
| 依據：`:616` `skip_cols={"原位次(距角序·暫行)", …}` | 🔴 **該欄名於四檔之欄集命中皆 `0`**（**對照組**：新欄名 `原位次(投影序)` 命中皆 `True`）⇒ **豁免失效** ⇒ 頂層二份亦逐格比對本欄 |

⇒ 鑄 **`GB-114`**（豁免清單指向不存在之受詞）＋ **自誤 `200`**（以宣告之豁免代替欄集之現查）。

---

## §七　`M-2d`　`_GHOST` 於 CSV 之命中

| 母體（框 ＝ 子字串 `_GHOST`） | 檔數 | 命中檔數 | **對照組**（`街廓`） |
|---|---|---|---|
| `verify/baselines/**/*.csv` | `266` | **`0`** | `222` 檔 |
| `verify/out/**/*.csv` | `10` | **`0`** | `10` 檔 |

⇒ `X-5` ✅ **符**；**⛔ 無輸出結構之變動** ⇒ 單 `§五` 之該停機條件未觸。

---

## §八　`M-3`　落地接點之現查（⛔ 只列不寫）

### 八-1　`a`／`b`（`b` ＝ **CC 依 `§一` 拘束推得**·⛔ 非裁定）

| tag | kind | source | `a` 現行 filter | `b` 落地後**應為**（CC 推得） |
|---|---|---|---|---|
| `app:v2/pre_seq` | POP_SYNC | BUILD_LAYER | `passthrough` | `passthrough ∧ ¬ghost3` |
| `app:main/v2_caller` | POP_SYNC | BUILD_LAYER | `stage1` | `stage1 ∧ ¬ghost3` |
| `stepg:v2_caller` | POP_SYNC | BUILD_LAYER | `stage1` | `stage1 ∧ ¬ghost3` |
| `app:main/_rank_by_tpid` | POP_SYNC | BUILD_LAYER | `identity` | `identity ∧ ¬ghost3` |
| `sp:_rank_by_tpid` | POP_SYNC | BUILD_LAYER | `identity` | `identity ∧ ¬ghost3` |
| `stepg:_proj_rank` | POP_SYNC | BUILD_LAYER | `stage1` | `stage1 ∧ ¬ghost3` |
| `wf_f0/f2/f3/f4:_proj_order` ×4 | ORDER_INVARIANCE | BUILD_LAYER | `no_ghost` | **述詞換源**：`_is_ghost_sliver` ⇒ `ghost3`（名可留） |
| `wf_f1:_order`／`wf_f4:_order_fb`／`wf_f4:_order` ×3 | ORDER_INVARIANCE | RESHAPE | `pseudo_of` | **⛔ 不變**——母體係 pseudo polygon、⛔ 無 `原地號` 鍵 ⇒ `ghost3` 恆假 |
| `app:k6step0/_ordered` | NAME_DERIVATION | TEMP_LAYER | `group_members` | 【**未量**】其母體 `_mem` 由 `own_map` 之群成員構成、ghost ⛔ 無地主 ⇒ **推定⛔ 無 ghost**；🛑 本批**未實測**，⛔ 不得當已驗 |

**理由（`b` 欄）**：`§一` 之 `VR-074` 逐字為「ghost **⛔ 不入投影序母體**」——受詞係**母體**，
而每一 tag 之 `filter(source)` 即該點之母體宣告 ⇒ 凡其母體可能含 ghost 者皆須加 `¬ghost3`。
**實測佐證**（`M-3` 補測）：`identity(BUILD_LAYER)` 出 `49` 列**殘留 ghost `2`**；`stage1(BUILD_LAYER)` 亦 `2`；
`identity(TEMP_LAYER)`／`stage1(TEMP_LAYER)` 各**殘留 `8`**；`no_ghost` 二層皆殘留 **`0`**。

### 八-2　`c`　濾式之**實作處**（逐字·自基座 blob 重抽）

```python
def _proj_pop_filter(fname, base):
    """🔒 濾式之**唯一定義處**（`L-1′`）。⛔ 其內容與既有碼面**逐字相同**。"""
    if fname == "identity":
        return list(base or [])
    if fname == "stage1":
        return [tp for tp in (base or []) if '配地階段' not in tp]
    if fname == "no_ghost":
        return [tp for tp in (base or []) if not tp.get("_is_ghost_sliver")]
    raise RuntimeError("🔴 [_proj_pop] 未知濾式 %r（`L-2′`）" % (fname,))
```

🔴 **`passthrough`／`pseudo_of`／`group_members` ⛔ 不在該函式內**——實測三者皆 `raise`
（逐字 `🔴 [_proj_pop] 未知濾式 'passthrough'（\`L-2′\`）`）⇒ 其**由他支斷言器承載**
（`_proj_pop_assert_passthrough` `app.py:7757`／`_proj_pop_assert_diff` `:7767`／`_proj_pop_assert_subset` `:7803`）
⇒ 🛑 **`filter` 欄之語意於此三者係<u>描述性</u>、⛔ 非可執行之濾式**。

**呼叫點（`12` 處·`grep -n "_projection_order(\|\[\"_projection_order\"\]" app.py verify/*.py`）**：
`app.py:7938`／`:11609`／`:19138`；`selection_pipeline.py:342`；`stepg_pipeline.py:505`；
`wf_f0.py:256`／`wf_f1.py:376`／`wf_f2.py:95`／`wf_f3.py:78`／`wf_f4.py:1047`／`:1437`／`:1512`。

### 八-3　🔴 `d`　改動點數（對 `W-G.9-161 L-2′` 承諾之驗證）

| 作法 | 改動點數 | 代價 |
|---|---|---|
| **(A)** 忠於宣告表：改 `_proj_pop_filter` ＋ 同步各呼叫端之**實參** | **≥ `8`**：`ghost3` 述詞定義 `1` ＋ 濾式三分支（`:7733`／`:7735`／`:7737`）`3` ＋ 呼叫端實參 ≥ `4`（`app.py:7938`／`:19138`／`sp:342`／`stepg:505`） | 無（宣告與實效一致） |
| **(B)** 於 `_projection_order` 入口（`app.py:7593`）濾 | **`1`** | 🔴 `_PROJ_POP_DECL` 之 `filter` 欄與**實效母體不符**（宣告 `identity` 而實際排除 ghost）⇒ **宣告表失真**；`no_ghost` 分支變冗餘 |

🔒 **本批之執行期層疊即 (B) 之模擬**，且**全程 `POP_SYNC` 斷言零紅**（探針 `rc=0`·`stderr` `0` B）
⇒ **(B) 於斷言層可行**（⛔ 非「行不通」）。
⇒ 🔴 **`X-6` 不符**：`L-2′` 之「**本表一處修改**」於 (A) 為 `≥ 8`、於 (B) 為 **本表 `0` 處**（改在表外）
⇒ **二讀法皆⛔ 非「一處」**。**判由發單側落筆**（單 `§五` 末：`X-6` 不符⛔ 非停機）。

---

## §九　`§五` 八項預測逐項對拍（⛔ 未改預測）

| # | 預測 | 實測 | 判 |
|---|---|---|---|
| `X-1` | `M-1 d` 對稱差 ＝ 空（單自評「很可能不符」） | 二層皆**空** | ✅ **符** |
| `X-2` | `差 ≠ 0` ＝ `5` 筆（`R1` `4` ＋ `R4` `1`）·差值皆 `−1` | `5` 筆·`{R1:4, R4:1}`·差值集合 `[−1]` | ✅ **符** |
| `X-3` | winner 全數不變 | 二情境指配列差異之鍵 ＝ `0`；`5` 同分群逐組未翻轉 | ✅ **符** |
| `X-4` | 受影響 ＝ 現行**二**份；凍存**八**份不受影響 | **對拍靶 `4` 份**（`v3/` 二 ＋ 頂層二）／凍存 **`6`** 份 | 🔴 **不符** |
| `X-5` | `_GHOST` 於 baseline CSV 命中 ＝ `0` | `266` 檔中 `0`（對照組 `222`） | ✅ **符** |
| `X-6` | `M-3 d` 改動點數 ＝ `1` | (A) ≥ `8`／(B) 本表 `0` | 🔴 **不符** |
| `X-7` | 二情境得同一組 `5` 筆 | 二情境之 `4` 格逐字相同；`M-2a` 與情境無關（`build_build_parcels` ⛔ 不吃 setback） | ✅ **符** |
| `X-8` | 受檔 `deletions` 逐列 ＝ `0` | `numstat` 刪除欄逐列 `0` | ✅ **符** |

**計數對拍**：符 **`6`** ／ 不符 **`2`**（`X-4`／`X-6`）／不可判 `0` ⇒ 和 ＝ `8` ＝ 預測總數 ✅。
🔒 **`X-3`／`X-5` 皆符 ⇒ 單 `§五` 之二停機條件<u>均未觸</u>**；`X-1`／`X-2`／`X-6`／`X-7` 依單⛔ 非停機。

---

## §十　入倉清單（⛔ 零生產碼·純新增／純附加）

| # | 檔 | 內容 | 刪除欄 |
|---|---|---|---|
| `1` | `verify/probes/probe_WG9167_vr074_pre.py` | 主探針（`M-1`／`M-2a`〜`M-2d`／`M-3`·**新檔**） | — |
| `2` | `verify/probes/probe_WG9167_m3.py` | `M-3` 補測 ＋ `X-4` 覆核（**新檔**） | — |
| `3` | `verify/out/probe_WG9167_vr074_pre_6b77b4a.log` | 主探針 log（**新檔**） | — |
| `4` | `verify/out/probe_WG9167_m3_6b77b4a.log` | 補測 log（**新檔**） | — |
| `5` | 🔴 `verify/out/WG9167_predicted_diff.json` | **預測 diff 集合**（機器可讀·窮舉·含 `roles`／`unchanged_files`·**新檔**） | — |
| `6` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | **`GB-114`** | `0` |
| `7` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **自誤 `200`** | `0` |
| `8` | 本報告 | `docs/reports/W-G.9-167R_VR074前置量測與預測集合.md`（**新檔**） | — |

🔒 **`§六` 之不辦事項全數遵守**：⛔ 未落地 `VR-074`、⛔ 未出落地規格；⛔ 未改 `app.py`／`verify/*.py` 既有一字；
⛔ 未改任何濾式；⛔ 未改 `_PROJ_POP_DECL`；⛔ 未重產 baseline／凍存名單／全量基準 log；
⛔ 未修 `GB-104`／`110`／`111`／`112`／`113`；⛔ 未落地 `VR-075`／`VR-078`／任何 `K-9-*`；⛔ 未動 `data/`；⛔ 未改任何既有 `docs/` 文字。

---

## §十一　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | `HEAD` 全 `40` 碼；`app.py` blob 逐位；`verify/*.py` 既有檔零改 | ✅ `app.py` ＝ `f285b91c9e5f97f13dd74508e1a403833bc75cb3`（**絕對值**）；`numstat` 中既有 `verify/*.py` 零命中 |
| `2` | `numstat` 刪除欄逐列 `0`；`wg942_append_audit` | ✅ 見 `§九 X-8` 與檔尾 |
| `3` | `M-1`／`M-2a`〜`M-2d`／`M-3` 逐項有數；二情境皆報 | ✅ `§三`〜`§八` |
| `4` | 預測集合入倉（機器可讀·窮舉·含判為不變之檔名） | ✅ `verify/out/WG9167_predicted_diff.json`（`changed_files` `40` 格／`unchanged_files` `256` 檔／`roles` `10`） |
| `5` | 每一命中數標框 ＋ 具名母體之態；列數採 `splitlines()` | ✅ 全報告；母體之態 ＝ `blob@6b77b4a` |
| `6` | 凡 `N/N` 型自證，同格具名**外部錨** | ✅ `M-2a` 之外部錨 ＝ `W-G.9-148R M-3`（`R1` `4`／`R4` `1`·逐街廓相符）；`GB` 算術自檢之外部錨 ＝ `W-G.9-144:81` |
| `7` | 取號：錨定框 ＋ 二形並取 ＋ 形自證 ＋ MAX 推進 | ✅ `§二`；入倉後 `GB` MAX `113`→`114`、自誤 MAX `199`→`200`（形皆由該類定義列式命中·`114` ⛔ 不在缺號集） |
| `8` | 所引之倉內逐字自基座 blob 重抽並逐位 `diff` | ✅ `§八-2` 之 `_proj_pop_filter` `:7730-7738` 九列；`§六-3` 之 `:616` `skip_cols` 逐字 |
| `9` | `§五` **八項**預測逐項對拍 | ✅ `§九`（符 `6`／不符 `2`／不可判 `0`） |
| `10` | 探針⛔ 無絕對路徑；一 clone 一支長跑 | ✅ 二探針之絕對路徑樣式命中 **`0`**（**對照組** `verify/tools/wg960_z_selfcheck.py` ＝ `1` ⇒ 量測器非紅）；本批**⛔ 未長跑** |
| `11` | 報告命名 ＝ `W-G.9-167R` | ✅ |

---

## §十二　🛑 交發單側落筆（`W-G.9-168` 之設計輸入·⛔ CC 不自裁）

1. 🔴 **`M-1 e` 之等價化**：三判準於 parcel 層**恰等於單條 ①**（②③ 之鍵不存在 ⇒ 恆真）。
   落地時之判準文字**須明示此事**，否則讀者會誤以為②③在此層具鑑別力（`GB-108` 之「無判別力」同族）。
2. 🔴 **`M-3 d` 之二作法擇一**（`§八-3`）——(A) `≥ 8` 處而宣告誠實／(B) `1` 處而宣告失真。
   🛑 **CC ⛔ 不自裁**；若採 (B)，建議同批修 `_PROJ_POP_DECL` 之 `filter` 語意說明。
3. 🔴 **`168` 之驗收預測閘**：差**恰為 `16` 格**（對拍靶 `4` 檔 × `4` 格·逐格見 JSON）；
   🛑 **`v3/` 二份與頂層二份<u>須同批重產</u>**（`自誤 200` 之後果欄）。
4. ⚠️ **`app:k6step0/_ordered`（`group_members`）本批⛔ 未量** ⇒ `168` 之前置須補量，⛔ 不得沿用本報告之「推定」。
