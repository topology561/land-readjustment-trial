# 🧾 `W-G.9-184R`　🔓 `GB-123` 守備之建置（候選 (a)）＋ `GB-125`：算術自檢係**恆真**

**受單**：CC　**施工單**：`W-G.9-184`（`SELF_SHA256` ＝ `5fb6d1538290f46220dfc3e59f5aeaa3d13a8fa0420c5aa48daf647b41c8afe4`·**CC 實算逐位相符** ✅）
**期初態**：`5a5e150b5624f2b484337a5fded43772a196c48e`
**性質**：🔒 ⛔ **零生產碼**（`app.py`／`stepg_pipeline.py`／`run_verification.py` 皆逐位未動）·🔓 **`run_all.py` 之掛載清單新增一項**（單明示授權·`§二 X-1`）·鑄 `自誤 221` ＋ 戒 `37` ＋ `GB-125`

> 🔒 **報告命名（`常規三`·CC 提三候選·**已採甲**）**
> **甲（採）** `W-G.9-184R_GB123守備建置與GB125恆真自檢.md` — 沿用施工單之二主軸，利於單↔報告逐字對位。
> 乙 `W-G.9-184R_KLUI凍存夾具與取號三紀律補正.md` — 以交付物命名，惟漏 `GB-125` 之立。
> 丙 `W-G.9-184R_值之直接對拍守備上線.md` — 以主結論命名，惟 `N-1`／`N-2`／`N-3` 全落在名外。

---

## `§一`　停機款之逐字引錄與逐款作答（🔒 `GB-121` 施行式·⛔ 非摘要）

**來源管線**：施工單 `§二` 之表（`C:\Users\admin\Downloads\W-G.9-184_施工單_授權建置GB123守備與GB125恆真自檢.md`·**`【單】`**）。

| 款 | 逐字原文 | CC 之判 |
|---|---|---|
| `X-1` | 凡須修改 `app.py`／`verify/stepg_pipeline.py`／`verify/run_verification.py` 一字（含註解）方能取得之量 ⇒ **停機上呈**。`verify/run_all.py` **僅授權於掛載清單新增一列**，改其任何既有列 ⇒ **停機**。 | ✅ **⛔ 未觸**——三檔逐位未動；`run_all.py` 之新項**插於既有末項<u>之前</u>**（見 `§五-3`），`before` 之 `365` 列**逐列原樣**出現於 `after`（純插入自證 ＝ `0` 未原樣出現）。 |
| `X-2` | 凡須跑 `run_all` 全案／任何長跑／跨態重跑，或須請 KL 實跑者 ⇒ **停機上呈**。🔓 **單獨跑新夾具**係授權範圍內。 | ✅ **⛔ 未觸**——只單獨跑新夾具（`rc=0`）；⛔ 未跑 `run_all`、⛔ 未跨態、⛔ 未請 KL 實跑。 |
| `X-3` | `M-I-2` 之注入擾動後夾具**仍綠** ⇒ **先判夾具紅並停機上呈**。 | ✅ **⛔ 未觸**——判別力**七造全部如預期**（`§五-2`）。 |
| `X-4` | 取號現查之三數皆須 `0`／`0`／`0`；凡非零者，須出艙其**唯一來源列**並證其屬**本案自身之配給**（`§零` 之通則），否則 ⇒ 先判量測器紅並停機。對照組依預設判準。 | ✅ **⛔ 未觸**——非零之三號逐號出艙其唯一來源列（`§四-2`），皆屬本案自身之次號表／呈裁段；對照組三號皆非零、人造受詞 `0/0/0`。 |
| `X-5` | 本批所鑄之號，其標題形須**逐字取自倉內既有最末一則**；**非鑄號之加註／補款須採 `🔬`／`🔧` 起首之形並逐字聲明其⛔ 不命中何框**（`戒 36`）。入倉後須實測各類 MAX 之變化**恰如預期**；不符 ⇒ **停機**。 | ✅ **⛔ 未觸**——入倉前標題形自證 **`28/28`**（`§三-1`）；入倉後 MAX 推進 **`8/8`**（`§六 9`）。 |
| `X-6` | `CLAUDE.md` 與二登記表之改動**僅限末端追加**。凡 `deletions ≠ 0` 或既有列被改一字 ⇒ **停機**。 | ✅ **⛔ 未觸**——四檔皆 binary append·**嚴格前綴 `True`**·`deletions` 逐列 `0`。 |
| `X-7` | 受檔 `numstat` `deletions` **逐列 `0`**；`app.py` 期末 blob ＝ `68f7bfab610ec78688b9d7322ae4bc2aac74493e`；`verify/out/KL_UI_3.5m_*` 十檔之 `sha256` 期末與期初**逐檔相同**。 | ✅ **⛔ 未觸**——`§六 10`／`§六 12`。 |
| `X-8` | 🛑 **停機款觸發一律先停機**：⛔ 不得先辦後呈；於停機回報中併陳擬辦之意見。 | ✅ **⛔ 未觸**——無款觸發；`§七` 之二事係**呈裁**、⛔ 非停機款。 |

---

## `§二`　開工閘（`S-1`〜`S-4`）

### `S-1`　🔒 **八項逐位全符**（CC 自 clone 實測·⛔ 不以單所載為錨）

| 項 | 實測 | 判 |
|---|---|---|
| `origin/wip/s1-endpart` | `5a5e150b5624f2b484337a5fded43772a196c48e` | ✅ |
| `app.py` blob | `68f7bfab610ec78688b9d7322ae4bc2aac74493e`／`1352132` B／`22922` 列／`7c290b6f55f83a24` | ✅ |
| `CLAUDE.md` | `71926` B／`771` 列／`a225037671f8241f` | ✅ |
| 自誤登記表 | `391433` B／`4272` 列／`d587ee3a8fdffe9d` | ✅ |
| `W-G.4` 泛用阻塞項登記表 | `510551` B／`3146` 列／`b639611ad2695658` | ✅ |
| `docs/驗證裁定登記表.md` | `330984` B／`4229` 列／`bc60869c80aa4858` | ✅ |
| `W-G.9-183R` 報告 | `26937` B／`344` 列／`18f01ea1b1833a84` | ✅ |
| **`verify/run_all.py`** | `23286` B／`365` 列／`680b4f7dd6868dd2` | ✅ |

`CRLF` 八檔皆 `0`。**施工單 `SELF_SHA256` 實算 ＝ `5fb6d153…` ⇒ 與單所載逐位相符**。

### `S-2`／`S-3`／`S-4`

- `S-2`：⛔ 未改任何框／母體／容差。**二處不符照實回報**（`§三-4` 之差集成因／`§三-2` 之 `P-2` 缺欄）。
- `S-3`：clone `C:\w184a`；自檢器與 payload 置 `C:\w184x` ⇒ 母體自扣。跑畢整份刪除（`§六 12`）。
- `S-4`：**先補齊清單再取**（`N-2 1` 所令）——本批人造受詞 ＝ **`自誤 4173`**（⛔ 不在補齊後之 `12` 項內·二形皆 `0`／`0`／`0`）🛑 **本報告出艙後即作廢**。

---

## `§三`　`N-1`／`N-2`　登記與三項紀律之補正

### `§三-1`　payload 之取得與標題形自證（`常規七 一`＋`戒 36`·**入倉前**）

| payload | 來源 | 列數／bytes／`sha16` | 落點 |
|---|---|---|---|
| `P-1`（`自誤 221` ＋ 戒 `37`） | **自單抽取**（block `:111-120`） | `10`／`1753`／`d14b1da1d328287a` | 自誤登記表檔末 |
| `P-2`（`GB-125`） | **自單抽取**（block `:126-134`）＋ **程式化組裝**（`§三-2`） | 原 `9`／`2111`／`86ee4a72c7e59c31` | `W-G.4` 檔末 |
| `P-3`（清單補齊·**CC 自擬**） | `§三 委辦式` | `26` 列 | `docs/驗證裁定登記表.md` 檔末 |
| `P-4`（自誤正典框錨定·**CC 自擬**） | `§三 委辦式` | `33` 列 | `CLAUDE.md` 檔末 |

**標題形自證（七框 × 四 payload ＝ `28` 格）**：

| payload | 應命中 | 實測 | 判 |
|---|---|---|---|
| `P-1` | 自誤寬形／甲形 各 `['221']`，餘五框 `[]` | 同 | ✅ |
| `P-2`（組裝後） | `GB` 式表／式B 各 `['125']`，餘五框 `[]` | 同 | ✅ |
| `P-3` | **七框皆 `[]`**（`🔬` 起首） | 同 | ✅ |
| `P-4` | **七框皆 `[]`**（`## 🔧` 起首） | 同 | ✅ |

⇒ **`28/28` 全符**。

### `§三-2`　🔴 **`P-2` 之二事照實回報**

1. **`P-2` 之 payload ⛔ 無標題形**——其九列係 `GB-124` 表格**各欄之內容**（以 `案由：`／`觸發條件：`／`現況處置：`／`真資料覆蓋：`／`需 KL 域裁：` 為前綴）。單 `§三` 令「`GB` 形取 `GB-124` 之二形」⇒ CC 以 `C:\w184x\build_p2.py` **程式化切分並拼接**為二形（`` ### `GB-125` 🆕 `` ＋ `` | **`GB-125`** 🆕 | ``），⛔ 未重寫任何一字。
   🔒 **逐字守恆自證**：`P2.txt` 之 `8` 個非空列，其內容（去欄名前綴後）**逐列完整出現**於組裝後之文字 ⇒ **`8/8` ✅**。
2. 🔴 **`P-2` ⛔ 未供「倉內出處」欄**（`GB-124` 表共 `7` 欄）⇒ CC 依現查自填，**逐字出艙**如下：
   > `` `docs/reports/W-G.9-184R_*.md` `§三-3`（本批**三組人造號集**之實測：組一連續無缺／組二大量缺號／組三**刻意含 `0`**·另附組四含負數）／`app.py:9462-9464`（同族①「構造恆真」之逐字）／`docs/reports/W-G.9-183R_VR取號污染處置與式ii擴大驗證.md` `§四-3 ②`（同族② 式(ii) 之殘差 ≡ Σ退化帶寬）／`docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-116` 起各則之「算術自檢 True」出艙 ``

### `§三-3`　`N-3`　`GB-125` 之恆真性 — **四組人造號集實測**（單令三組·CC 加測含負數之第四組）

| 組 | 號集 | MAX | 相異 | 缺號數 | `MAX−缺` | 自檢 |
|---|---|---|---|---|---|---|
| 組一：連續無缺 | `[1,2,3,4,5]` | `5` | `5` | `0` | `5` | **True** |
| 組二：大量缺號（仿真） | `[1,7,14,220]` | `220` | `4` | `216` | `4` | **True** |
| 組三：**刻意含 `0`** | `[0,1,2,5]` | `5` | `4` | `2` | `3` | **False** 🔴 |
| 組四（附）：含負數 | `[-3,1,2]` | `2` | `3` | `0` | `2` | **False** 🔴 |

🔒 **判**：組一／組二（皆 `S ⊆ {1..MAX}`）**恆 True**，⛔ 與量測是否正確無關 ⇒ **`GB-125` 之案由成立**。
🔒 組三／組四轉 `False` ⇒ 坐實其**⛔ 非恆為 `True`**，故依 `app.py:9465-9467` 之戒 **⛔ 不得刪除／停用**；惟本倉各定義列式皆取 `[0-9]{1,4}` 且⛔ 無 `0` 號 ⇒ **實務上恆真、判別力為零**。

### `§三-4`　`N-2 2`　自誤二形之差集 — 🔴 **成因與單所預期<u>不同</u>，照實回報**

**三數（母體 ＝ 自誤登記表單檔·期初態）**：

| 形（逐字） | 命中列 | 相異 | MAX | 缺號數 |
|---|---|---|---|---|
| 甲形 `` ^### 自誤 `([0-9]{1,4})` `` | `38` | **`38`** | `220` | **`182`** |
| **寬形** `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `188` | **`185`** | `220` | **`35`** |

⇒ 差集 **寬∖甲 ＝ `147`**／**甲∖寬 ＝ `0`** ——**與單 `§六 三` 所載逐位相符**。

🔴 **惟單 `N-2 2` 令「以證其確係<u>層級之差</u>而⛔ 非他因」，實測<u>否證</u>之**——成因係**二維**：

| 成因 | 號次數 | 逐字樣本（`3` 則·單所令） |
|---|---|---|
| 層級非 `###`（多為 `##`） | `102` | `## 自誤 43（W-G.9-60）`＠`:859` ／ `## 自誤 165〜169（**五則·一批入倉**·claude.ai `-103` 窗自犯·W-G.9-134）`＠`:3403` |
| 層級 `###` 但**⛔ 無反引號** | **`48`** | `### 自誤 7｜**交付一個在法上不可能成立的取捨**`＠`:50` |
| 其他 | `0` | — |

🔒 **反向自證**：甲形之 `38` 個命中中，⛔ 不合「`###` ＋反引號」者 ＝ **`0`** ✅（證分類器非紅）。
🔒 **⛔ 不影響本單之裁**：採寬形之理由係「**涵蓋既有全部鑄號**（甲∖寬 ＝ `0`）」，該理由與成因是一維或二維**無關**。`P-4` 已按實測之二維成因落筆，⛔ 未沿用單之單一歸因。

### `§三-5`　`N-2 1`　清單之補齊與**算式**

🔒 **三數出艙（`N-2 1` 所立之算式）**：**前批 `8` ＋ 本批出艙 `5` ＝ 本批 `13`**。
- 前批項數 ＝ **`8`**（倉內機械抽取·框 `` ^\|\s*(\d+)\s*\|\s*`([^`]+)`\s*\| ``）
- 本批出艙數 ＝ **`5`** ＝ 補列 `4`（`自誤 4907`／`GB-357`／`自誤 3182`／`自誤 5741`）＋ 本批 `S-4` `1`（`自誤 4173`）
- 本批項數 ＝ **`13`**
⚠️ **與單所載 `12` 之差 ＝ `1`，來源已具名**：單之 `12` 係**補列後、本批 `S-4` 出艙前**之數；`P-3` 依補款四（**同批即列**）併列 `自誤 4173` ⇒ 期末 `13`。⛔ 非漏列、⛔ 非多列。

---

## `§四`　取號現查（🛑 **⛔ 不以「算術自檢 True」為憑**·`N-2 3`／`GB-125`）

**母體** ＝ clone 內全倉 `.md` **`462`** 檔（自檢器置 `C:\w184x` ⇒ 自扣）。

### `§四-1`　三數本身 ＋ 未扣除／已扣除二數（`常規四（八）` 補款）

| 類 | 框 | 未扣除（相異／MAX／缺號數） | 所扣 | 已扣除（相異／MAX／缺號數） |
|---|---|---|---|---|
| 自誤·寬形·單檔 | `` ^#+ 自誤 `?N`? `` | `185`／`220`／`35` | `[]` | `185`／`220`／`35` |
| 自誤·甲形·單檔 | `` ^### 自誤 `N` `` | `38`／`220`／`182` | `[]` | `38`／`220`／`182` |
| 自誤·寬形·全倉 | 同寬形 | `189`／`220`／`31` | `[]` | `189`／`220`／`31` |
| `GB`·三框聯集 | 式表 `119` 列／式A `3`／式B `36` | `122`／`124`／`2` | `[]` | `122`／`124`／`2`（缺 `[12, 87]`） |
| **`VR`**·定義列式 | `` ^##\s*`VR-N` `` | `70`／**`999`**／`929` | **`[999]`** | `69`／**`84`**／`15` |
| 戒·裸框·全倉 | `` 戒\s*`?N`? `` | 相異 `24`／MAX `36` | — | — |

### `§四-2`　次號之占用 — 🔒 **逐號出艙其唯一來源列**（`§零` 通則所令）

| 號 | 裸形 | 反引號形 | 唯一來源（逐字） | 判 |
|---|---|---|---|---|
| **`自誤 221`**（本批取） | `1`／`1`／`1` | `1`／`1`／`1` | `W-G.9-183R…md:264` ＝ `` \| `自誤 221` \| `0`／`0`／`0` \| `0`／`0`／`0` \| — \| ✅ 未占用 \| `` | 🔒 **本案自身之配給** ⇒ ⛔ 不順延 |
| **戒 `37`**（本批取） | `0`／`0`／`0` | `0`／`0`／`0` | — | ✅ 未占用 |
| **`GB-125`**（本批取） | `6`／`6`／`2` | `0`／`0`／`0` | 交接文 `:76` 之次號表 ＋ `-183R` 之 `:262`／`:267`（次號表）與 `:301`／`:304`（呈裁段） | 🔒 **本案自身之配給** ⇒ ⛔ 不順延 |
| `VR-085`（本批⛔ 不鑄） | `8`／`8`／`2` | `0`／`0`／`0` | 同上族 | 🔒 同上·本批⛔ 不鑄 |
| `自誤 222`（備） | `0`／`0`／`0` | `0`／`0`／`0` | — | ✅ 未占用 |

🔒 **對照組（已知必非零）**：`自誤 220` ＝ `7`／`7`／`3`；`GB-124` ＝ `20`／`22`／`5`；戒 `36` ＝ `5`／`5`／`2` ⇒ **整體非零**。
🔒 **人造受詞**：`自誤 4173` 二形皆 `0`／`0`／`0`。
⇒ **兩造自證俱備 ⇒ 量測器非紅**（`X-4` 之處置前提不成立）。

---

## `§五`　🔑 `N-4`　主受詞：`GB-123` 守備之建置（候選 (a)）

### `§五-1`　`M-I-1`　夾具之建置 — **四項設計要求逐項具名其落實方式**

**交付物**：`verify/fixture_klui_t2diag.py`（`432` 列·名稱依既有 `fixture_*` 體例·⛔ 未調整）
**凍存**：`verify/out/WG9184_KLUI_T2DIAG_凍存.txt`（`21` 列 ＝ `4` 列 `#` 檔頭 ＋ `2` 列 `#META` ＋ **`15` 列資料**）

| # | 設計要求（單逐字） | 落實方式 |
|---|---|---|
| `1` | **凍存<u>整列</u>，⛔ 非只 Σ池** | 🔒 **凍存 `[T2-DIAG]` 之<u>整列原文</u>**，`observe()` **⛔ 不解析欄位、⛔ 不正規化** ⇒ 街廓／宗數／池帶片數／逐帶 `s` 寬／逐帶面積／`Σ池`／覆蓋殘差／`Σ 殘差`／宗-宗疊／內部縫／退化帶數與寬／`s` 域**全在受詞內**。判別力已就 `宗數` 單獨實測（`§五-2` 之 ⑤：`宗數 5→6` 而 `Σ池` 不動 ⇒ **仍紅**）。 |
| `2` | **凍存「`R5` 缺席」為<u>期望事實</u>** | 🔒 `#META` 列記各 log 之**列數 ＋ 街廓序列**；`compare()` 比對序列 ⇒ **`R5` 出現即紅**（已實測·`§五-2` 之 ⑥）。docstring 逐字載其根因（`stdout.log:2985` 之 `K-9-4` BASELINE 臨接閘 `RuntimeError`）與「⛔ 屆時不得逕自重凍了事」。 |
| `3` | **期望值須凍存為<u>獨立檔</u>，⛔ 不得由夾具自 log 現算** | 🔒 期望值一律自 `WG9184_KLUI_T2DIAG_凍存.txt` **讀入**；`freeze()` 係 `--freeze` 之**獨立入口**、**⛔ 非夾具主路徑之一部**。**產生過程**（`M-I-1 3` 所令之逐字記錄）：`2026-08-30`、於 clone `C:\w184a`、命令 `python verify/fixture_klui_t2diag.py --freeze`、來源 ＝ `verify/out/KL_UI_3.5m_*_stdout.log`（`glob`）、框 ＝ **含字面 `[T2-DIAG]` 之列**、產出 `15` 列（`2e08a41` `5` 列／`a3c97aa` `10` 列）、本體 `sha256`（扣 `#` 檔頭）＝ `51b492af8cab0e4e07157c8dd3a7e58116d7c50d1e7255fe5652ef8c40ba4685`。 |
| `4` | **⛔ 不得對既有 `KL_UI_3.5m_*` 之任何一列做任何修改** | 🔒 夾具**唯讀**該十檔；`X-7` 實測十檔 `sha256` **期末與期初逐檔相同**（`§六 10`）。 |

🔒 **docstring 已逐項列「轉紅之因」`6` 項與「射程外」`5` 項**（`VR-082`·單令至少三項·CC 出艙五項）：
① 只守**已入倉之那一趟**（KL 下次實跑須人工入倉 ⇒ 仍非全自動·**`GB-123` ⛔ 未消滅**）；
② 該 log 之態⛔ 不可自證（`GB-119`）；③ 守**變動**⛔ 非守**正確**（凍存值本身若已錯則恆綠）；
④ ⛔ 不涵蓋 `R5`（其池於 KL UI 側從未產生·無值可守）；⑤ ⛔ 不比對 app-vs-shim（二端**皆 app 側**）。

### `§五-2`　`M-I-2`　判別力之證明 — 🔒 **七造全部如預期**（單令兩造·CC 加證現值側與 `R5`）

| # | 擾動（⛔ 全程只動**記憶體複本**） | 結果 | 期望 |
|---|---|---|---|
| ① | **凍存**複本注入：首列 `Σ池` 末位 `+0.0001` | 🔴 紅 | 紅 ✅ |
| ② | 移除該擾動（還原） | ✅ 綠 | 綠 ✅ |
| ③ | **現值**複本注入：首列 `Σ池` 末位 `+0.0001` | 🔴 紅 | 紅 ✅ |
| ④ | **現值**複本注入：首列**逐帶面積**首值 `+0.0001` | 🔴 紅 | 紅 ✅ |
| ⑤ | **現值**複本注入：`宗數 5`→`6`（**`Σ池` 不動**·`M-I-1 1` 之案由） | 🔴 紅 | 紅 ✅ |
| ⑥ | **現值**複本**新增一列 `街廓 R5`** | 🔴 紅 | 紅 ✅ |
| ⑦ | **現值**複本**刪去一列**（列數 `5`→`4`） | 🔴 紅 | 紅 ✅ |

🔒 **凍存檔 `sha256` 期初 ＝ 期末**（`703d2c88879da98d…`）⇒ 擾動確施於複本、⛔ 未改倉內凍存檔。
🔒 ③④⑤ 係**夾具真正要守之方向**（app 產物變動 ⇒ 紅）——單只令凍存側，CC 加證之。
⇒ **`X-3` ⛔ 未觸**。夾具亦內建同型自檢（每次執行**兩態皆印**），單獨跑 `rc=0`。

### `§五-3`　`M-I-3`　掛入 `run_all` 與其後果

🔒 **`numstat` ＝ `12`／`0`**（`deletions` **`0`**）＝ **`2` 列 tuple ＋ `10` 列註解**。
⚠️ **列數之具名**（單期望「`1/0` 或**其體例所需之最小列數**」）：既有體例每項皆附「掛入本清單之由」之註解區塊（`fixture_yi_construction` `9` 列／`tools/wg973_gb_integrity` `6` 列）⇒ `10` 列註解在體例內；tuple 本身之**最小**為 `2` 列（檔名列 ＋ 說明列）。
🔴 **新項插於既有末項<u>之前</u>之理由（⛔ 非任意）**：Python tuple 之末項結尾為 `")):`，若插於其後**必須**將其改為 `"),` ⇒ **即觸 `X-1`「改其任何既有列 ⇒ 停機」**。⇒ CC 插於 `("tools/wg973_gb_integrity.py",` 之前，**`before` 之 `365` 列逐列原樣出現於 `after`**（純插入自證 ＝ `0` 未原樣出現）。
🔒 **AST 覆核**：掛載清單 **`15` → `16`** 項，新項在第 `15` 位；`py_compile` 通過。
🛑 **⛔ 未跑 `run_all` 全案**（`X-2`）——只單獨跑該夾具：**`rc=0`**、逐位比對 `15` 列中 `0` 列不符、判別力自檢「注入態紅 ∧ 還原態綠」。

---

## `§六`　收工閘（`12` 項·逐項）

| # | 項 | 結果 |
|---|---|---|
| `1` | `S-1` 八項期初錨逐位對拍 | ✅ **八項全符**（`§二`） |
| `2` | `N-1` 形之自證 ＋ MAX 推進 `220 → 221`（**三框並報**·出艙三數本身） | ✅ 寬形／甲形／全倉 **三框 MAX 皆 `220→221`**；三數見 `§六 9` |
| `3` | `N-2 1` 清單補齊 ＋ **算式出艙** | ✅ `8 ＋ 5 ＝ 13`（與單載 `12` 之差已具名·`§三-5`） |
| `4` | `N-2 2` `CLAUDE.md` 純末端追加 ＋ 差集 `147` 覆核 ＋ 樣本 `3` 則逐字 | ✅ 差集 `147`／`0` 逐位相符；🔴 **成因為二維**、照實回報（`§三-4`） |
| `5` | `N-3` `GB-125` 取號（未扣除／已扣除二數）＋ 恆真性三組實測 | ✅ `§四-1`／`§三-3`（**四組**·加測含負數） |
| `6` | `N-4` `M-I-1` 四項設計要求逐項具名 | ✅ `§五-1` |
| `7` | `N-4` `M-I-2` 判別力兩造 | ✅ **七造**全部如預期（`§五-2`） |
| `8` | `N-4` `M-I-3` `numstat` ＋ 單獨跑 `rc` ＋ 射程外三項 | ✅ `12/0`／`rc=0`／**射程外五項**（`§五-3`／`§五-1`） |
| `9` | `X-5` 入倉後 MAX 之變化恰如預期 | ✅ **`8/8`**：自誤寬形 `221`（相異 `186`／缺 `35`）·甲形 `221`（相異 `39`／缺 `182`）·全倉 `221`（相異 `190`／缺 `31`）·`GB` `125`（相異 `123`／缺 `[12,87]`）·`VR` 未扣除 `999`／已扣除 `84`（**不變**）·戒 `37`（相異 `25`）·常規 `12` 列（**不變**） |
| `10` | `X-7` `KL_UI_3.5m_*` **十檔** `sha256` 期末＝期初 | ✅ **逐檔相同** |
| `11` | `wg942_append_audit.py`（**commit 前**）＋ memory ㉞ 母體驗證 | ✅ `rc=0`／`stderr` `0` B／受檢 **`77`**／`嚴格前綴＝True` ×`77`／`False` ×`0`／「無此檔」`0`；**四受檔皆在母體內且 `True`** |
| `12` | 全新檔 `CRLF ＝ 0`；逐檔 `git add`；`numstat` `deletions` 逐列 `0`；`push` 後三重驗證；clone 整份刪除 | ✅ 見完工回報 |

🔒 **絕對路徑機檢**：本批新增命中 **`0`**；既存 `4`（`probe_WG971_wrap.py:203`／`:247`／`tools/nameset_diff.py:19`／`tools/wg960_z_selfcheck.py:12`）；判別力兩造皆如預期。

---

## `§七`　🛑 呈發單側裁（⛔ 不上呈 KL）

### `一`　🔴 **`N-2 2` 之「層級之差」預期被實測否證**（⛔ 非停機款·照實回報）

單 `N-2 2` 逐字令「以證其確係**層級之差**而⛔ 非他因」，實測成因為**二維**：`102`（層級非 `###`）＋ `48`（`###` 但⛔ 無反引號）。
🔒 CC 之處置：**⛔ 未改框使其相符**（`S-2`），`P-4` 依實測之二維成因落筆；**⛔ 不影響本單之裁**（採寬形之理由係「涵蓋既有全部鑄號」，與成因維度無關）。
🛑 **請發單側**追認 `P-4` 之逐字（其與單 `§六 三` 之歸因不同），並判是否須就此鑄號。

### `二`　🟡 **`P-2` 未供「倉內出處」欄**（`GB-124` 表共 `7` 欄）

CC 依現查自填並於 `§三-2 2` 逐字出艙。🛑 **請發單側**追認該欄逐字；若不採，CC 可依指示以末端加註更正（⛔ 不追改已入倉之列）。

---

## `§八`　積欠之更新

🔒 **`W-G.9-183` 交接文 `§五` 之 `11` 項續行**；`-183R` 新增之 `12`／`13` 兩項**本批已辦結**：

| # | 項 | 狀態 |
|---|---|---|
| `12` | 已作廢人造受詞清單之補列 | ✅ **已辦結**（`P-3`·`8`→`13` 項＋算式） |
| `13` | 自誤定義列式之正典框未定 | ✅ **已辦結**（`P-4`·採寬形並錨定） |
| `5` | `GB-112`〜`GB-120` | ⬜ 只登記 |
| `7` | `GB-121`／`GB-122`／`GB-123`／`GB-124` | 🔶 **`GB-123` 部分**：本批已建守備並掛入 `run_all`，**惟其 `⬜` ⛔ 未消滅**——射程外五項（`§五-1`）中之 ① 使其**仍非全自動** |
| `14` | 🆕 **`GB-125`**（算術自檢恆真） | ⬜ 只登記（本批新立） |

---

## `§九`　回報紀律之遵行

- 🔒 **數字錨引倉檔**：本報告一切數字皆出自本窗實測；`【單】` 者僅 `§一` 之停機款逐字。
- 🔒 **`S-4`**：`自誤 4173` **本報告出艙後即作廢**（已於 `P-3` 第 `13` 列同批落地）。
- 🔒 **`常規一 ①` 之受詞具名**：本批 `app.py`／`stepg_pipeline.py`／`run_verification.py` **逐位未動**；`run_all.py` 之 `12/0` 係**單明示授權範圍內之例外**（`§二 X-1` 逐字）。
- 🔒 **拋棄式 clone**：`C:\w184a`／`C:\w184x` 跑畢整份刪除（見完工回報）。
