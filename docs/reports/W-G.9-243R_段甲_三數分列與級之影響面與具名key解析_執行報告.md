# W-G.9-243R（段甲）　宣告框三數分列 ＋ 級之影響面清單 ＋ 具名 `key` 解析表 ＋ 自誤 `295`／`296`　執行報告

> **段甲 ＝ 零生產碼**（工項零–四·一 commit）。`app.py`／`verify/**.py` **一字未動**（十檔雜湊·§七）。
> **段乙（工項五 ＝ seam 抽出）於本報告<u>尚未動工</u>**——其為生產碼、須先出完整 diff 上呈、俟 **KL 逐字放行**方得 push。
> 🔒 一切數字**當場現查於倉**，⛔ 採單所載之值；宣告框之數一律依補款 `⑥` **三數分列**。

---

## §零　態錨

| 項 | 值（現查於 `2026-09-06`）|
|---|---|
| 開工態 `origin/wip/s1-endpart` | `135316e19402e2cbc72333e7862912d80cb98af8`（`ls-remote` 現查）|
| 施工副本 | 短路徑拋棄式 clone `C:/Users/admin/AppData/Local/Temp/w240`（`core.autocrlf=false`）|
| 掃描器所在 | `C:/Users/admin/AppData/Local/Temp/w240t/`（**倉外**·⛔ 入倉）|

---

## §一　開工閘

### `S-0`（單之完整性）✅ **綠**

payload **16423** B（全檔 **16501** B・**274** LF・**CR 0**）；`SELF_SHA256` 列命中 **1**；
宣告 ＝ 實算 ＝ `99272dfd5ab110b70cfd7ef6342a40b60aab831e3a89a135668701b879d7060b`。

### `S-1`（號占用·宣告框·**三數分列**）✅ **六欄逐一相符**

🔒 **三軸**：來源 `blob@135316e19402e2cbc72333e7862912d80cb98af8`／
母體 **全 `docs/` 560 檔**（其中 **19** 檔為 `.png`／`.jpg` 二進位而讀不到·可解碼 **541**）／
粒度框 **檔框 ＋ 列框**。
🔒 **框**：`D1` 係**檔框**之款、**⛔ 產生任何列命中**；**`列框 ＝ D2 ＋ D3`**；
`檔框 ＝ |D1 ∪ D2 之檔 ∪ D3 之檔|`；`D3` 之「本單」款依**補款 `⑤`**（僅於該號 ＝ 該檔檔名之號時成立）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 宣告框 檔／列 | 鬆框 檔／列 | 單所載 | 判 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-243`** | **0** | **0** | **0** | **0**／**0** | **2**／**2** | 全同 | ✅ **未占用** |
| `W-G.9-242` | **2** | **5** | **3** | **3**／**8** | **6**／**27** | 全同 | ✅ 對照組 甲（`≥ 1`）|
| `W-G.9-244` | **0** | **0** | **0** | **0**／**0** | **0**／**0** | 全同 | ✅ 對照組 乙（次號）|

🔒 **`243` 之鬆框 `2／2`** 之逐筆落點 ＝ `-242` 單／`-242R` 報之**前瞻委派**（`⛔ 抽 seam……順延為次一單`），
依 `②`／`⑤` **⛔ 構成占用** ⇒ 與單所述一致。
🔒 **`242` 之 `D3 = 3`** 逐筆：`-242 單:35`／`:79`／`-242R 報:54`——三者皆「同列含『本單』且該號 ＝ 該檔檔名之號」⇒ 依 `⑤` 成立。

### `S-2`（append-only）✅

段甲受詞之既有二檔（`CLAUDE.md`／自誤簿）皆**純末端追加**，上文一字未改 ⇒ `deletions` 全 **`0`**。

### `S-3`（生產路徑之錨）✅ **五檔前 16 位逐一相符**

| 檔 | 實測 `sha256` 前 16 位 | 單所載 | 判 |
|---|---|---|---|
| `verify/wf_f0.py` | `6758ea766b001b95` | `6758ea766b001b95` | ✅ |
| `verify/wf_f1.py` | `30e19048dfd781bc` | `30e19048dfd781bc` | ✅ |
| `verify/wf_f2.py` | `f974724d7e694ad7` | `f974724d7e694ad7` | ✅ |
| `verify/wf_f3.py` | `226c7c5fa00464f3` | `226c7c5fa00464f3` | ✅ |
| `verify/wf_f4.py` | `c40295c3f8cf8686` | `c40295c3f8cf8686` | ✅ |

---

## §二　工項零：本單原封入倉 ✅

`docs/orders/W-G.9-243_排序準據seam抽出與級之影響面清單.md`
**16501** B／**274** LF／**CR 0**／`sha256 3625c73bc4520f5b…`；落檔 ＝ 入倉 blob 逐位相同。
🛑 該閘只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。

---

## §三　工項一：宣告框出艙之**三數分列**補款 `⑥`（`CLAUDE.md`）✅

| 項 | 值 |
|---|---|
| payload 來源 | 本單**第 `66`–`88` 列**（四反引號圍欄 `65`／`89`·邊界已逐列印出坐實）|
| 追加量 | **1891** B／**26** LF／**CR 0** |
| 改前 | 186140 B・2259 LF・`sha256 a9f40e68f59980bbc2c435d77c6db4e7292e036798da987824eb92a4af20379d` |
| 改後 | 188031 B・2285 LF・`sha256 bd8f13e7a42096956f02f5475287299cb35d541f9f045689219cb6963e021fe8` |
| `numstat` | **`26 / 0`** ⇒ `deletions ＝ 0` ✅ |

**字樣錨（二形同格併載·列框·母體 `CLAUDE.md` 單檔）**

| 字樣 | 改前 | 改後 | 判 |
|---|---|---|---|
| `號占用閘之框`（受詞錨）| **2** | **2** | 單只令「報改前數」——**改後未增**（新節標題作「宣告框**出艙之形**」，⛔ 含該字面）|
| `宣告框`（判別力自檢·寬字樣）| **11** | **17** | ✅ **恆 `>` 受詞錨**（`11 > 2`／`17 > 2`）⇒ 該檢**非恆為 `1`** |

🛑 **併記（事實·⛔ 條文）**：`號占用閘之框` 現為 **2** 且本批未增 ⇒ 其**已非唯一錨**、亦不指向本節；
新節之可用唯一錨為 `三數分列`（本批新入·⛔ 於本報告出艙其數以免自指增長）。

---

## §四　工項二：**級之影響面清單**（另設框·⛔ 併入 AST 排序準據框）✅

### `a`　框與母體

🔒 **三軸**：來源 `blob@135316e`／母體 **`app.py` ∪ `verify/*.py`（頂層·全部）＝ 34 檔**／粒度框 **列框**。
🔒 字樣三組 ＝ `_level`／`LV`／`級別`；對照組 ＝ 級字面 `級1相鄰`／`級2鄰近`／`級3非鄰近`。
🔒 **⛔ 併入 AST 框**（受詞不同：AST 框問「誰在排序」，本框問「級被生產／寫到哪」）。

### `b`　逐點表（**14** 列·全表）

| 檔:列 | 字樣 | 逐字原文 | 角色 |
|---|---|---|---|
| `app.py:3986` | `級別` | `1. area < 0.1 ㎡    → 一律 sliver（浮點誤差級別）` | 字面／其他 |
| `verify/wf_f0.py:324` | `級別` | `#   級別＝全達標·無須併），舊訊息對之為**錯**。改為**印實際逐塊決策**、不猜原因。` | 字面（註解／docstring） |
| `verify/wf_f0.py:360` | `級別` | `"級別": d["kind"], "標的宗": "—", "被併宗": "",` | 字面（欄名字串） |
| `verify/wf_f0.py:371` | `級別` | `"情境": tag, "歸戶": d["gid"], "街廓": d["blk"], "級別": d["kind"],` | 字面（欄名字串） |
| `verify/wf_f2.py:19` | `級別` | `**距離級別**（規格 §2）：相鄰＝共享 BLOCK 邊或隔 RD 相望；鄰近＝質心距≤中位；非鄰近＝其餘。` | 字面／其他 |
| `verify/wf_f2.py:173` | `級別` | `"雙模式Δ(㎡)": round(abs(m1 - m2), 2), "級別": lvl,` | 字面（欄名字串） |
| `verify/wf_f4.py:16` | `級別` | `質心（一次定錨）。級別：相鄰＝錨所在公設塊共邊 R 塊（RD）或 G1 隔 RD 相望（F.3 裁定③）；` | 字面／其他 |
| `verify/wf_f4.py:406` | `級別` | `"目標塊": tr["所屬街廓"], "級別": "級1(同歸戶)",` | 字面（欄名字串） |
| `verify/wf_f4.py:483` | `_level` | `def _level(gid, blk):` | **生產者**（定義） |
| `verify/wf_f4.py:487` | `LV` | `LV = {0: "級1相鄰", 1: "級2鄰近", 2: "級3非鄰近"}` | **生產者**（定義） |
| `verify/wf_f4.py:488` | `_level` | `border = {gid: sorted(mina, key=lambda b: (_level(gid, b), dists[(gid, b)], b))` | **消費者**（呼叫 `_level`） |
| `verify/wf_f4.py:798` | `_level+LV+級別` | `"級別": LV[_level(gid, blk)],` | **消費者**（呼叫 `_level`） |
| `verify/wf_f4.py:1274` | `級別` | `"級別": "7-5批次最佳化", "起迄距(m)": round(cm["dist"], 1),` | 字面（欄名字串） |
| `verify/wf_f4.py:1558` | `級別` | `chain[r["歸戶"]].append(f"F.0{r['級別']}→{r['去向']}" if r["級別"] != "全達標·無須併"` | 字面（欄名字串） |

### `c`　分布與二錨

| 檔 | 級之落點列數 | 對照組（級字面）|
|---|---|---|
| `app.py` | **1** | **0** |
| `verify/wf_f0.py` | **3** | 1 |
| `verify/wf_f2.py` | **2** | 3 |
| `verify/wf_f4.py` | **8** | 1 |
| 其餘 **30** 檔 | 0 | 0 |
| **合計** | **14** | **5** |

🔒 **角色分布**：**生產者 2**（`wf_f4.py:483` `def _level(gid, blk):`／`:487` `LV = {…}`）／
**消費者 2**（`wf_f4.py:488` **決策鍵**／`:798` **輸出欄**）／**字面 10**。

🔴 **本節之要害（碼面事實）**：**`_level` 之呼叫點恰 `2` 處**——
`:488`（進 `sorted` 之 `key` 首鍵 ⇒ **決策**）與 `:798`（`LV[_level(…)]` ⇒ **輸出欄**）。
⇒ **抽 `:488` 之 key 時，`:798` 之輸出語意不受影響**（其自行呼叫 `_level`，⛔ 讀 `border`）。

🩸 **一則偽陽已具名**：`app.py:3986` `1. area < 0.1 ㎡ → 一律 sliver（浮點誤差級別）`
——其「級別」二字與本框**偶然同字**、與調配之級**無關** ⇒ 判 `字面／其他`。
⇒ 🔒 **`app.py` 側之級之真實落點 ＝ `0`**。

**二錨之復現（單 `§三 c` 令）**

| 錨 | 單所載 | 實測 | 判 |
|---|---|---|---|
| `app.py` 之級字面命中 | **0** | **0** | ✅ 相符 ⇒ 級之機制**全在 `verify/` 側** |
| `app.py:23844` | `import wf_f4 as _wf4` | 逐字相同 | ✅ |
| （併證）`app.py:14095` | —— | `#   app 路徑（「執行七級調配」→ \`_build_wf_ctx\` → wf_f4.compute）取不到即 **KeyError**。` | ✅ |

⇒ 🔒 **`verify/wf_f4.py` 在 app 之生產路徑上，⛔ 僅為 harness 之複刻。**

---

## §五　工項三：**具名 `key` 之解析表**（⛔ 擴 AST）✅

🔒 **三軸**：來源 `blob@135316e`／母體 `-242R` 之 AST 站點集（**367** 站·本批**當場復現該數**）／粒度框 **節點**。
🔒 **篩準**：站點之 `key=` 實參為 `ast.Name` 者（`ast.Lambda` 者排除）。
🔒 **對照組**（同一 code path）：人造樣本之具名 `key` 命中 **1**（期 1）、`lambda` `key` 命中 **1**（期 1·須被排除）⇒ **GREEN**。

**量**：具名 `key` 之站點 ＝ **5**（**⛔ 單所引之「預期只有二站」**——該數係 `-242R §八 5` 之引述，
本批依單之明令**自行掃出全集**，實得 **5**，多 **3** 站）。**`⛔ 可解析` 者 ＝ `0`。**

| 站點 檔:列 | `key` 實參名 | 站點逐字 | 定義之 檔:列 | 定義原文全文 |
|---|---|---|---|---|
| `verify/wf_f0.py:218` | `_key` | `sorted(qual, key=_key)` | `verify/wf_f0.py:209–210` | `def _key(r):   # G 最大；並列取暫編字典序最小 / return (-float(r["G(㎡)"]), r["暫編地號"])` |
| `verify/wf_f0.py:222` | `_key` | `sorted(lots, key=_key)` | `verify/wf_f0.py:209–210` | `def _key(r):   # G 最大；並列取暫編字典序最小 / return (-float(r["G(㎡)"]), r["暫編地號"])` |
| `app.py:7619` | `_proj_of` | `sorted(parcels, key=_proj_of)` | `app.py:7609–7617` | `def _proj_of(tp): / cs = tp.get('polygon_coords') or [] / if len(cs) < 3 or line is None: / return 0.0 / try: / cen = _P_po(cs).representative_point() / return float(line.project(_SP_po(cen.x, cen.y))) / except Exception: / return 0.0` |
| `app.py:10756` | `_dep` | `max(_cands, key=_dep)` | `app.py:10721–10723`（同名定義共 **4** 處·取呼叫點之前最近者） | `def _dep(p): / """點至 BASELINE 之垂距（沿共用法向·恆非負）。""" / return abs((float(p[0]) - b0x) * bnx + (float(p[1]) - b0y) * bny)` |
| `app.py:12404` | `_d_front` | `_cv.sort(key=_d_front)` | `app.py:12396–12398` | `def _d_front(p): / """點至 FRONT∞ 之距離（解析·⛔ 非 GEOS·E-10）。""" / return abs((p[0] - F1[0]) * (-dy) + (p[1] - F1[1]) * dx)` |

🩸 **本表之量測器自捕（⛔ 隱去）**
初版以「該名於同檔之**首個**定義」解析 ⇒ `app.py:10756` 之 `key=_dep` 被指向 `app.py:6623` `_dep = []`（**錯**）。
當場另跑多重性檢查得 **`_dep` 於 `app.py` 有 `4` 個定義**（`:6623` 賦值／`:10721–10723` `def`／`:12483` 賦值／`:22466` 賦值）
⇒ 改判準為「取**呼叫點之前最近**之定義」，`:10756` 正解 ＝ `app.py:10721–10723` `def _dep(p)`。
🔒 **通則（本報告之自加·⛔ 鑄號）**：**凡以「名 → 定義」解析者，須先報該名之定義處數**；
`> 1` 者，其解析須綁**呼叫點之前最近**，且該多重性須同格出艙。
🔒 **界**：本判準仍屬**靜態近似**——⛔ 涵蓋跨模組匯入、閉包遮蔽、動態指派；
本批 **5** 站之定義**皆在同檔且皆可解析**，故該界於本批**未被觸及**。

---

## §六　工項四：自誤 `295`／`296` ✅

### `a`　末號之獨立復算（⛔ 採單所載）

🔒 框 ＝ 定義框 `A` **＋ 範圍框**（範圍框二端**已展開**併入相異集）；母體 自誤簿**單檔**；粒度框 **列框**。

| 量 | 改前 | 單所載 | 改後 |
|---|---|---|---|
| 一 定義框列命中 | **288** | —— | **290** |
| 二 重咬列 | **3**（`:3338`／`:3373`／`:3403`）| —— | **3** |
| 三 純單筆列命中 | **285** | —— | **287** |
| 範圍框條數 | **8** | —— | **8** |
| 相異 | **287** | **287** ✅ | **289** |
| `MIN` | **7** | **7** ✅ | **7** |
| `MAX` | **294** | **294** ✅ | **296** |
| 缺號清單 | **`[106]`** | **`[106]`** ✅ | **`[106]`** |
| 恆等式 `MAX−MIN+1−缺號數 == 相異` | **True** | **True** ✅ | **True** |

⇒ **`N` ＝ `295`、`N+1` ＝ `296`**（與單相符）。

### `b`　鑄號之形（依 `常規四（九）四`）

既有最末一則之標題形 ＝ `## 自誤 \`294\`　**…**` ⇒ 本批二則採同形：

```
## 自誤 `295`　**宣告框之量測器把 `D1`（檔名之款）併入列計**
## 自誤 `296`　**單內收工閘之受詞與同單工項相斥**
```

🔒 內文逐字未改（機驗：`自誤 N` 佔位符出現數 **皆 ＝ 0**·assert 通過）。

### `c`　鑄號收工閘（單 `§五` 三項）

| 項 | 判準 | 實測 | 判 |
|---|---|---|---|
| ① | `MAX` 自 `294` → `296` | `294` → **`296`** | ✅ |
| ② | 缺號恆 `[106]` | `[106]` → **`[106]`** | ✅ |
| ③ | **號**之零污染（`GB` 簿本批未動）| **段甲** `git diff --numstat -- GB簿` ⇒ **空** | ✅（其界見 §八 `2`）|

🔒 另：定義框 `A` 之**盲區探針**（前綴文字非空者）改前改後**皆 `0`**；`> MAX` 之落號**皆 `0`**；
`295`／`296` 於改後命中集**皆 present**。

### `d`　落地量

改前 578450 B・6664 LF ／ `295` 追加 **953** B・**18** LF ／ `296` 追加 **1203** B・**20** LF ／
改後 **580606** B・**6702** LF・**CR 0**・`sha256 2ebcbf63e71c54db60bf432b3789c3f3b0c8d5abd59a16338a3aae1310fe177d`；
`numstat` **`38 / 0`** ⇒ `deletions ＝ 0` ✅。

---

## §七　閘之總表（段甲）

| 閘 | 判準 | 實測 | 判 |
|---|---|---|---|
| `S-0` | 最末 `SELF_SHA256` 逐位相符 | `99272dfd…` | ✅ |
| `S-1` | 宣告框**六欄**逐一相符 ＋ 兩造對照組 | 三列六欄全同（`243` `0/0/0`·`242` `2/5/3`·`244` `0/0/0`）| ✅ |
| `S-2` | append-only | 二既有檔 `deletions` 全 `0` | ✅ |
| `S-3` | `wf_f*.py` 五檔雜湊前 16 位 | 五檔全同 | ✅ |
| **十檔逐位不變** | `wf_f0`–`wf_f4` ＋ 閘 `P` 五檔 | **10/10** 全同（段甲後重測）| ✅ |
| `deletions` | append-only ＝ `0` | `26/0`・`38/0`；新檔二張（本單、本報告）亦 `0` | ✅ |
| 鑄號收工閘 | ①②③ | 三項全綠 | ✅ |
| AST 站點數之復現 | `-242R` 之 `367` | **367** | ✅ |
| ⛔ 執行 harness | 段甲⛔ 跑 `run_all`／`run_verification` | **未跑**（`verify/out/` 無新增）| ✅ |
| 掃描器⛔ 入倉 | 置於倉外 | `git status` 無其蹤 | ✅ |

**十檔之 `sha256` 前 16 位（段甲後）**：`wf_f0 6758ea766b001b95`／`wf_f1 30e19048dfd781bc`／
`wf_f2 f974724d7e694ad7`／`wf_f3 226c7c5fa00464f3`／`wf_f4 c40295c3f8cf8686`／
`app.py e3e464ea2493d046`／`selection_pipeline 6c6783420dbc635c`／`run_verification e48f24c84079f365`／
`stepg_pipeline 0260273350021311`／`run_all 4ba89fef90979491`。

---

## §八　🔴 上呈（**⛔ 由 CC 自裁**）

1. **工項一之受詞字樣錨未增**：`號占用閘之框` 改前 **2** → 改後 **2**
   ——單只令「報改前數」，故**⛔ 未達標**；惟該錨**已非唯一、亦不指向新節**，
   下一批若須以字樣錨指認本節，須另擇（如 `三數分列`）。
2. 🔴 **本單 `§五` 鑄號收工閘 `③` 之括號說明與同單 `§六 f` 相斥**——
   `③` 逐字作「**號**之零污染（`GB` 簿本批未動·**本單無 `GB` 追加** ⇒ 本款受詞成立）」，
   而**同單 `§六 f` 明令追加於 `GB` 簿**（閘 `P` 涵蓋缺口之待落地登記）。
   🔒 **其操作性受詞（號之零污染）於段甲成立**（段甲之 `GB` 簿 diff 為空·已給證）；
   **不成立者僅括號內之「本單無 `GB` 追加」一語**。
   🛑 **此與本單所鑄之 `自誤 296`（單內收工閘之受詞與同單工項相斥）<u>同族</u>**，
   且係在**登記該族之同一單內**再現。**是否構成新一則自誤 ＝ 意思決定 ⇒ 候發單側／KL**；
   **本報告⛔ 逕鑄號、⛔ 改該閘一字。**
3. **工項三之量之落差**：單載「量之預期……已具名 `wf_f0:218`／`:222`」，實掃得 **5** 站（多 `app.py` 三站）。
   單已預先禁止「以預期二站為由略過全掃」⇒ **本批依令全掃**；此處只登記其差。
4. **具名 `key` 解析之靜態界**（§五 之自捕所暴露）：`_dep` 於 `app.py` 有 **4** 個定義。
   **是否把「定義處數 `> 1` 須同格出艙」升為正字條款 ＝ 意思決定。**
5. 🛑 **CC 側違規自陳**：本窗 `Bash` 呼叫**第三度**誤含 heredoc（`<<`），違 `GB-143` 補款二。
   本 session 之 project dir 為自 `main` 開之舊態 worktree ⇒ `.claude/settings.json` 與 guard **皆不存在**
   ⇒ **`PreToolUse` 硬閘於本 session 結構性失效**（`-240R §八 2` 已具名·本批**第二次**坐實）。
   後果實測為零（該命令未寫任何檔、未動倉態，已 `TaskStop` 終止）。
   **計數器之處置 ＝ 意思決定 ⇒ 候發單側／KL。**

---

## §九　段乙（工項五 ＝ seam 抽出）之界（**本報告尚未動工**）

- 射程 ＝ **乙**（四站·全在 `verify/wf_f4.py`）：`:433`／`:488`–`:489`／`:662`–`:663`／`:689`。
- **⛔ 及於** `-242R` `A` 桶其餘 `15` 站（標的宗選定、`§7-5` 第2梯、F.2／E0 目標塊宗選定、分攤剔除 `:703`）。
- **⛔ 動** `_level`（`:483`–`:486`）／`LV`（`:487`）／`dists`／`med`／`mina`／`adj_pub` 之算法；
  **⛔ 動** `wf_f0`／`wf_f1`／`wf_f2`／`wf_f3`／`app.py` 與閘 `P` 五檔；**⛔ 新增檔**。
- **⛔ 於本批新增任何替代準據**（registry 每鍵只有一個實作·⛔ 開關／參數化／條件分支／預留 `if` 位）。
- 驗收 ＝ `V-1` 靜態等價逐行人閱 ／ `V-2` 單元級逐位對拍（含 tie 情形·拋棄式·⛔ 入倉）／
  `V-3` 十檔雜湊前後各報一次（**只有 `wf_f4.py` 得變**）。
- 🛑 **段乙之 commit ⛔ 逕行 push**——須先出完整 diff ＋ `V-1`／`V-2`／`V-3` 上呈，俟 **KL 逐字放行**。
