# `W-G.9-263R`　交接文 `-262` 入倉 ＋ `自誤 322`〜`324` 落簿

> 🛑 **全批零生產碼**（逐 commit 之判見 `§五`）⇒ 依 `常規一` 補款 `🔧 一` **逕推主線**，⛔ 候 KL。
> 🛑 本批**只辦入倉與落簿二事**；**⛔ 開啟 `R4`–`R6`／`②-宗@R5`／`GB-67`／`_b_L0` 任一工項**（單 `§四` 明令·屬新窗）。
> 🛑 **交接文 `-262` 係原封入倉，⛔ 執行其所列之任何工項**（其受詞係下一個 `claude.ai` 窗、非 CC）。
> 🔒 一切數字皆**當場實測**（⛔ 憑記憶·⛔ 採信單載）。

---

## §零　新單之發現處（🔴 **第七度**）

本單與交接文 `-262` 皆於**主 checkout 工作區**（`…/land-readjustment-trial/docs/orders/`）發現，
**本 worktree 之倉內查無** ⇒ 記憶所載「開窗第一動作 `④`」之形**第七度**再現。
主 checkout 之 `HEAD` ＝ `1e5a87c`（落後於主線 `e9d1b9c`），二檔以 **untracked** 之姿置於該處。

🗄️ **前批之未決項已由 KL 結案**：`R2左側街角第1宗_推進超跑_幾何示意.svg` **已不在**
「主 checkout ∖ 本 worktree」之差集內 ⇒ **KL 所稱之手動刪除已執行**。本項⛔ 再提。

---

## §一　開工閘（五項·**逐條當場復現**）

| 閘 | 單所載期望 | 實測 | 判 |
|---|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `e9d1b9c3e6e46580da28392e1ed7495d5b34638f` | `ls-remote` 得**同值**；本地 `HEAD` 同 | ✅ |
| `2` 受詞 | `app.py` `1e15d45ae84b02ca`／`stepg` `cb0bbaaa0f1eb8bc` | **逐位相符**（🔴 舊值 `4860f108…`／`c8d46d2e…` 確已於 `-261` 失效）| ✅ |
| `3` 號占用 | `262`／`263` 之宣告框皆 `D1 0`／`D2 0`／`D3 0` | 見 `§一-1` | ✅ |
| `4` 次號 | 三簿前值 ＝ 自誤 `321`／`GB 154`／`K-9 29` | **逐位相符**（框逐字見 `§三-1`）| ✅ |
| `5` `S-0b` | `rc=0`、`P-5` 相符 | 二檔皆 `rc=0`；`P-5` 本單 `0135ece2c6f0a1cf…`／交接文 `d9a832565952b99b…` **皆與單載逐位相符** | ✅ |

### 一-1　號占用（宣告框 `①`–`⑦`·**入倉前**）

**三軸**：來源 ＝ `blob@e9d1b9c3e6e46580da28392e1ed7495d5b34638f`／母體 ＝ **全 `docs/` `612` 檔**／
粒度框 ＝ **檔框 ＋ 列框**（`列框 ＝ |D2 ∪ D3|`·`檔框 ＝ |D1 ∪ D2檔 ∪ D3檔|`）。
**讀不到 `19` 檔 ＝ `jpg 9` ＋ `png 10`**；可讀 `593` 檔。
**器自檢**：含字面反斜線之路徑 ＝ `0`（`core.quotepath` 已關）／含 CJK 之路徑 ＝ `591`（`> 0` ⇒ 該檢有判別力）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 判 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-262`** | **0** | **0** | **0** | **0** | **0** | **0** | ✅ 未占用 |
| **`W-G.9-263`** | **0** | **0** | **0** | **0** | **0** | **0** | ✅ 未占用 |
| `W-G.9-261` | 2 | 2 | 0 | 0 | 2 | 2 | 對照組甲（須 `≥ 1`）✅ |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱·執行期組出）| 0 | 0 | 0 | 0 | 0 | 0 | 對照組乙（須 `= 0`）✅ |

⇒ **器非紅**；`3` 之**自指款未觸發**（二檔於量測時尚未入倉）。

### 一-2　`S-0b` 之 🟡 提示三項（**逐項具名·⛔ 靜默略過**）

`P-1`／`P-2`／`P-4` 係**文字啟發式、必有偽陽**（`W-G.9-190R §九（乙）` 之性質款：**⛔ 列為停機款**）。

| 檔 | 項 | 命中處 | 判 |
|---|---|---|---|
| 本單 | `P-4` | `:65` ＝ `自誤 324` 之**標題列** | **具名豁免（偽陽）**——標題文字，非方向性轉引 |
| 交接文 | `P-1` | `:137` ＝ `\| 項 \| 狀態 \| 依賴 \|`（**表頭列**）| **具名豁免（偽陽）** |
| 交接文 | `P-2` | `:60` ＝ 一敘述句（「三度／二度器紅皆入倉前自捕」）| **具名豁免（偽陽）**——敘述、非計數出艙 |

🛑 交接文係**原封入倉** ⇒ 上開提示**⛔ 得作為改其一字之理由**。

---

## §二　工項零：二檔原封入倉

| 檔 | 位元組 | `LF` | `CR` | 來源 `sha256` | 落檔 | 入倉 blob | blob size |
|---|---|---|---|---|---|---|---|
| `docs/orders/W-G.9-263_交接文262入倉_自誤322至324.md` | `9476` | `107` | **`0`** | `77646f7fd31fda83e324fe7a9f9e20bdbd2d90ce2a4e27019a8e509d0c78c1c7` | 同 | 同 | `9476` |
| `docs/orders/交接文_W-G.9-262.md` | `11852` | `177` | **`0`** | `0d7b4044999ae08138ce0da37082f8aeada292418b572401fc23ffefebf29d82` | 同 | 同 | `11852` |

⇒ 🔒 **三者逐位相符**；**blob size ＝ 位元組數** ⇒ **無 CRLF 正規化**。
🛑 **射程界限（承 `追加四` 之既載·照實）**：本驗所證者為「**來源檔 → 落檔 → 入倉**」之保真，
**⛔ 證「發單側送出之原文 → 主 checkout 之落檔」之保真**。

---

## §三　工項一：`自誤 322`〜`324` 落簿

### 三-1　取號之框（`常規四（八）三`·逐字列出）

**母體** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**（`W-G.9-192-PRE-b 一`）。
**框** ＝ `VR-091 四` 之**定義框 ∪ 範圍框（<u>展開</u>）**：

```
定義框  ^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`?
範圍框  ^#+[^\n]*?自誤[\s　`]*`?([0-9]{1,4})`?[\s　`]*[〜～\-–~]+[\s　`]*`?([0-9]{1,4})`?
```

**範圍標題實測 `8` 條**（單載 `8` 條·逐條出艙）：
`143〜156`／`157〜164`／`165〜169`／`170〜179`／`180〜182`／`183〜184`／`187〜188`／`191〜192`。

🩸 **CC 側之器紅一度（入倉前自捕·照實）**：本窗**首版**之框**未展開範圍標題**，得
**相異 `305`／缺號 `10` 個 `[106,171,…,178,181]`**——其**恆等式仍為 `True`**（`315 ＝ 305 ＋ 10`）
⇒ 🔴 **恆等式本身⛔ 足以判框之對錯**（二框皆自洽）。
**其得以被擋下之唯一原因** ＝ 單 `V-D` 逐字載「相異 `314`」與「缺號仍 `[106]`」二數，
與我之 `305`／`10` **相斥** ⇒ 回頭查框，發現單之框逐字含「**範圍框展開**」而我未展開。
🔑 **通則（本則所立·CC 側）**：**自洽 ⛔ 蘊含 正確**——恆等式只證「該框之集合與其缺號互補」，
⛔ 證「該框涵蓋全部既有鑄號」。凡取號框，須另以**單載值或第二框**作外部錨。

### 三-2　落簿之法（`常規七`·**由檔案管線直接產生**）

payload **自倉內之單檔機械抽取**（⛔ 憑記憶重寫）：
以 `> ### 🩸 \`自誤 322\`` 為起錨（命中 `1`）、`🛑 **⛔ 追改` 為迄錨（命中 `1`），
去 blockquote 前綴後得 **`41` 列／`4453` B**；**非引文且非空白之列 ＝ `0`**；
三標題各命中 `1`。標題形取**既有最末一則**（`自誤 321`）為範本（`常規四（九）四`）。

**檔末追加之機驗**：

| 量 | 值 |
|---|---|
| 改前 | `661258` B ／ `CR 7687` ／ `LF 7687`（工作區 CRLF） |
| 改後 | `665761` B ／ `CR 7731` ／ `LF 7731` |
| 新增 | **`4503` B ／ `44` 列** |
| **嚴格前綴**（改前為改後之前綴 ∧ 長度增加）| **`True`** ⇒ **append-only·⛔ 改既有一字** |

### 三-3　三則之受詞（**摘·全文見登記表 `:7691`／`:7706`／`:7718`**）

| 號 | 受詞（一句）| 責 |
|---|---|---|
| `自誤 322` | 在窮盡搜索前即向 KL **宣告**「此題需你裁」——而該題 KL 已於 `2026-08-12`（`K-9-5-12（四）`）裁畢 | 發單側 |
| `自誤 323` | 出艙閘之基準值時**未套該閘母體所含之濾**（`F>0`）⇒ 以未濾之首列充當「鏈頭」 | 發單側 |
| `自誤 324` | `V-1` 綁在**代理量**而非其所欲保證之**性質**，致該閘**結構上不可達** | 發單側 |

🔒 **`323`／`324` 之攔截者皆為 CC 之 `-261R`**：前者由 CC 具名「`R2 right` 無鏈頭」；
後者因 **CC ⛔ 自行放寬判準而照實出艙「`V-1` 未達」並呈上**——單逐字載其為「本則得以現形之唯一原因」。
🛑 **⛔ 追改 `W-G.9-261` 單或 `-261R` 報告一字**（`常規四（九）五`）；`V-1` 之「未達」定讞，
其更正之唯一載體為 `自誤 324`。**本報告亦⛔ 追改之。**

---

## §四　【驗收】

| 閘 | 判準 | 實測 | 判 |
|---|---|---|---|
| `V-A` | 十檔 ＋ 夾具之倉側 blob 前後逐位不變；`git status --porcelain` ＝ `0` 列 | 見 `§四-1` | ✅ |
| `V-B` | `run_verification.py` 於本倉現態實跑 ⇒ `RESULT: FAIL`、紅 `24` ＋ 計數式 | 見 `§四-2` | ✅ |
| `V-C` | 自誤登記表之 `deletions` ＝ `0` | `git show --numstat` ⇒ 三檔皆 `<n>　0` ⇒ **刪除欄逐列 `0`** | ✅ |
| `V-D` | 自誤 `MAX 321 → 324`／相異 `314 → 317`／`MIN 7`／缺號仍 `[106]`／恆等式 `True`；`GB 154`・`K-9 29` 不變 | 見 `§四-3` | ✅ |
| `V-E` | 三標題皆可被 `VR-091 四` 定義框命中（機驗·出艙命中列號）| `:7691`（`322`）／`:7706`（`323`）／`:7718`（`324`）；判別力對照（一必不存在之號·代稱）＝ **`0`** | ✅ |
| `V-N` | 號占用依宣告框 `①`–`⑦`：三數分列 ＋ 雙屬列數 ＋ 列框 ∪ ＋ 檔框 ∪ ＋ 讀不到之副檔名分布 | 見 `§一-1` | ✅ |

### 四-1　`V-A`（**正面列舉·⛔ 以「全倉排除 X」界定母體**）

| 檔 | `HEAD^` blob（前 16）| 工作區 | 判 |
|---|---|---|---|
| `app.py` | `1e15d45ae84b02ca` | 同 | OK |
| `verify/stepg_pipeline.py` | `cb0bbaaa0f1eb8bc` | 同 | OK |
| `verify/run_all.py` | `4ba89fef90979491` | 同 | OK |
| `verify/run_verification.py` | `d0d531f7dc051b65` | 同 | OK |
| `verify/wf_f0.py` | `b0722355c1978635` | 同 | OK |
| `verify/wf_f2.py` | `f974724d7e694ad7` | 同 | OK |
| `verify/wf_f4.py` | `b36aeca6c4f6b364` | 同 | OK |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | 同 | OK |
| `CLAUDE.md` | `7c25e981e5004fbe` | 同 | OK |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `7a0edfe53d65790a` | 同 | OK |

**夾具**：`git status --porcelain verify/` 之命中 ＝ **`0`** ⇒ `verify/` 下**一檔未動**。
🔒 **本批之異動清單（definitive）**：`git show --name-status` ⇒ `A`／`A`／`M` **恰三檔**，全在 `docs/`。

### 四-2　`V-B`（**於本倉現態 `2bc0429` 實跑·⛔ 以「生產碼逐位相同」推定**）

**三軸**：來源 ＝ `blob@2bc0429386347153e88fcbf05771ab23d4e067ee`（拋棄式 clone·**原態**·⛔ 注入）／
母體 ＝ `verify/out/v263_rv.log`（**`28300` B ／ `313` 列**·`stderr` **`0`** B）／粒度框 ＝ **列框**。

| 量 | 值 |
|---|---|
| `RESULT` | **`RESULT: FAIL`**（`:312`）·`rc = 1`（**准紅碼**之預期）|
| **紅項數** | **`24`** |
| 計數式 框A　`grep -c "🔴 FAIL"` | **`24`** |
| 計數式 框B　`grep -c "^🔴 FAIL"` | **`0`** 🔒 **係<u>框之別</u>、⛔ 態之別**——該類列皆有縮排（單 `V-B` 逐字所令之併記）|
| 計數式 框C　`grep -c "^ *🔴 FAIL  "` | `24` |
| 對照組甲（器須綠·須 `> 0`）`grep -c "✅"` | `16` ✅ |
| 對照組乙（必不存在·代稱·執行期組出）| `0` ✅ |

🔒 **第二態之同式結果**（`自誤 321` 所立之通則）：同式於 **`e9d1b9c`**（本批之父）亦得
`RESULT: FAIL`／紅 **`24`**／母體 `28300` B・`313` 列 ⇒ **二態逐位相同**，
與「`e9d1b9c → 2bc0429` 僅增三份文件、生產碼 blob 逐位相同」相符。

### 四-3　`V-D` 之逐值（**二框並列**·`戒 29`：一張表只准一個框 ⇒ 分列）

| 框 | 相異 | `MAX` | `MIN` | 缺號數 | 缺號集 | 恆等式 |
|---|---|---|---|---|---|---|
| **展開**（`VR-091 四` 正式）改前 | `314` | `321` | `7` | `1` | `[106]` | `315 == 314 + 1` **`True`** |
| **展開** 改後 | **`317`** | **`324`** | `7` | `1` | **`[106]`** | `318 == 317 + 1` **`True`** |
| 未展開（**⛔ 正式框**·僅列以具名器紅）改前 | `305` | `321` | `7` | `10` | `[106,171–178,181]` | `315 == 305 + 10` `True` |
| 未展開 改後 | `308` | `324` | `7` | `10` | 同上 | `318 == 308 + 10` `True` |

`GB`（母體 ＝ `W-G.4_泛用阻塞項登記表.md` 單檔·框 ``^#{2,4} .?`?GB-([0-9]{1,4})`?``）：
相異 `68`／**`MAX 154`**（前後不變）。
`K-9`（母體 ＝ `K-6` 正典單檔·框 ``^#{2,4} \s*🔒?\s*K-9-([0-9]{1,3})(?![0-9-])``）：
相異 `24`／**`MAX 29`**（前後不變）。
⇒ 🔒 **本批只鑄 `自誤 322`／`323`／`324`；`GB`／`K-9`／單號⛔ 鑄。**

---

## §五　出艙

- 逐 commit 之生產碼判（`常規一` 補款之逐字指令）：**命中 `0` 行 ⇒ 零生產碼 commit** ⇒ **逕推主線**。
- 🛑 **推畢即停機**——⛔ 逕啟 `R4`–`R6`／`②-宗@R5`／`GB-67`／`_b_L0` 任一工項，候新窗之發單側。
- 🛑 **本批落地後 `262` 與 `263` 皆已占，次可用單號 ＝ `264`**；交接文 `§一`／`§二 四`／`§六` 首列之
  占用表隨之成為**舊態**——接手窗本即受令「⛔ 採信本文任一值·須自倉重生此表」，
  ⛔ 追改交接文一字。
