# `W-G.9-301R`　執行報告：**實作形之重釘**登記 ／ `自誤 404`・`405` ＋ 坑 `bj`・`bk` 之鑄 ／ 🛑 **停四成就**

> **單** ＝ `docs/orders/W-G.9-301_施工單_實作形之重釘與自導可行性之現查.md`（工項零已入倉·`d65b2511`）
> **態**｜開工 ＝ `e5c9924694a57ce3831873393119cb9104c8c1a2`（`ls-remote` 實查·全 `40` 碼）
> **分級** ＝ **中**（登記 ＋ 現查·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**·**⛔ 修批**）
> 🛑 **本批鑄號 ＝ `自誤 404`／`405` ＋ 坑 `bj`／`bk`**（**四號**·逐字由單供）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔴 **`§六` 之 `停四` 成就 ⇒ 判器紅、停、上呈**——**函式體內自導⛔ 可施於現有簽章下**。
> 🛑 **⛔ 呈 KL。** ⛔ 擬任何改法或 diff、⛔ 判 `GB-170` 可否解除、⛔ 提修法主張。

---

## `§零`　開工閘（十一項·**每列同格載其產生指令**·`自誤 392`）

| # | 受詞（**指令逐字**） | 期值／判準 | 實測 | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `e5c9924694a57ce3831873393119cb9104c8c1a2` 或其後裔 | `e5c9924694a57ce3831873393119cb9104c8c1a2	refs/heads/wip/s1-endpart`（全 `40` 碼） | ✅ |
| `1′` | `git merge-base --is-ancestor e5c99246… FETCH_HEAD`；`rc` **緊接指令** | `rc = 0` | `rc = 0` | ✅ |
| `2` | `git rev-parse HEAD:app.py`（🔒 **框 ＝ blob `sha1`**·`自誤 405`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`；`rc` 緊接指令 | 須**非**祖先（`rc = 1`） | `rc = 1` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `P-5` `SELF_SHA256` 逐位相符·`P-3` 不紅·🟡 **逐項具名處置** | `P-5` **`SELF_SHA256` ✅ 相符**（取檔內**最末**一個·受詞 `36488` B·實算 `f4d55500c0712788…`／單載 `f4d55500c0712788…`）·`P-3` **不紅**（`def main` ＝ `14975`-`24579`）·🔴 **`0`** 項·🟡 **`2`** 項（逐項處置見 `§零-1`） | ✅ |
| `4′` | 受詞自證（坑 `bf`） | [必真] 含 `W-G.9-301`／[必偽] 含 `W-G.9-300` | 器印 `【order pre-flight】W-G.9-301_施工單_實作形之重釘與自導可行性之現查.md（36566 B／351 列）` ⇒ `True`／`False` | ✅ |
| `5` | 同 `4` ＋ `--selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`·preflight 四靶逐項成立 | `rc = 0`／`rc = 0`；靶① `P-3` 紅 ✅／靶② `P-4` 提示 ✅／靶③ `P-6` 提示 ✅／**反靶** `-190R` 之 `P-3` 不紅 ✅ | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 四簿之數 | 自誤 相異 `393`／MAX `403`／缺 `[106, 355, 356, 357]`｜`GB` `168`／`170`／缺 `[12, 87]`｜`VR` `80`／`95`／缺 `[73, 75]`｜`K-9` `28`／`29`／缺 `[]` | ✅ |
| `6′` | `python verify/probes/probe_WG9269_pit_index.py`（🛑 **等待綁行程結束**·⛔ `\| tail -N`） | `87`／`87`·末 `bi` ⇒ 下一 `bj` | **`87` 標籤／`87` 可解析**·末標籤 **`bi`**·母體 ＝ 追蹤 `.md` **`801`** 檔；造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造坑 `0`／`0` ✅ | ✅ |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔；子層須 `> 0` | **`34`**（`1 + 33`）；子層 `^verify/.+/.+\.py$` ＝ **`314`** `> 0` ✅（**逐批漂移**·`313`→`314` ＝ `W-G.9-300` 所增之一器·⛔ 定值） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8…`·`298` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·**`298`** 檔（`git config --get core.quotePath` ⇒ `rc = 1` ⇒ 未設） | ✅ |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | 族 `20`；`…-299-gb170` ＝ `0`；[必存在] `…-269-p3a` ＝ `1` | **`20`**／**`0`**／**`1`** | ✅ |
| `10` | **六物證落檔**（🔒 **框 ＝ blob `sha1`**·`git rev-parse <rev>:<path>`·先 `git cat-file -e` 驗存在·三態分列） | 四者逐位未動 ＋ 二者存在 | 六者皆 **態1:存在**；`1a35dc168655`／`f93eb2fcb7e6`／`16742f3805e0`／`5cef4c8dfe7a` ⇒ **逐位未動**；`WG9300R_static_v1_popcollapse.log` `476aac14eda5`／`WG9300R_dynamic_v1_wrongframe.log` `b3ab88b73dda`（本批新增者·⛔ 單載）。**二框並報**：其內容 `sha256` ＝ `956bf39f56c6`／`000ae17f6f4b`／`46b4e9e96779`／`11fb216c6608`／`0ec3fc839805`／`6743f2a7e1e6`。[必存在] `app.py` ＝ 態1／[必為零] 執行期組出之人造路徑 ＝ 態2／人造 `rev` ＝ 態3 | ✅ |

### `§零-1`　`P-4` 之**逐項處置**（⛔ 靜默略過）

🟡 **`2`** 項（判定組**非空** ⇒ ⛔ 拒測）：

| 列 | 受詞（節略） | 歸類 | 處置 |
|---|---|---|---|
| `:3` | `**態**｜開工 ＝ \`e5c9924…\` **或其後裔**（…·⛔ 短前綴）` | **(乙)** 方向性之詞係**序之詞而非幾何**（`後裔`／`前綴`） | ✅ **受具名豁免** |
| `:333` | `\`§零\` 開工閘（…）→ \`§零′\` 之⛔ 重辦 → 逐工項（…）→` | **(甲)** **流程節序之串接** | ✅ **受具名豁免** |

🔒 **二項皆在單 `§零-1` 所定之二類內** ⇒ 受豁免；**⛔ 幾何向量之方向性轉引** ⇒ 無座標系可具名。
🛑 本批**⛔ 出艙任何幾何向量之夾角** ⇒ 「釘其槽並以實邊機驗」之禁**無受詞**（照實具名·⛔ 以「未觸」充綠）。

---

## `§零′`　前批（`W-G.9-300`）之核可　🔒 **⛔ 重辦**（單明定「發單側已辦」）

---

## `§一`　工項零：**本單原封入倉**（主線·`commit d65b2511`）

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/orders/W-G.9-301_施工單_實作形之重釘與自導可行性之現查.md`（新檔·`numstat` ＝ **`351` 增／`0` 刪**） |
| **二框並報**（`自誤 405`·**產生指令逐字**） | blob `sha1`（`git rev-parse HEAD:<path>`）＝ `b27a2cc05161f3d9b7daac984339c4562e9a76a7`；內容 `sha256`（`sha256(檔之全部 bytes)`）＝ `7d0ff35a163ae9f75d8dcf5b71c5e44c4ad31a37764a3a3c2c43dae81bbda935` |
| **三方逐位對拍** | 倉內 blob ＝ 磁碟 ＝ 源 ⇒ **`True`**（`36566` B） |
| `CR`（框 ＝ `bytes.count(b'\r')`·**讀 blob 取 bytes**） | **`0`**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf` `CR` **`12308`**（全長 `88341` B） |
| **受詞自證**（坑 `bf`） | 含 `W-G.9-301` ＝ `True`[必真]／含 `W-G.9-300` ＝ `False`[必偽] |
| 檔數自證（框 ＝ `os.path.isfile`） | 受詞 ＝ `1`；造甲[必≥1] `W-G.9-300` 之單 ＝ `True`／造乙[必為0] **執行期組出**之人造名 ＝ `False` |

---

## `§二`　工項一：`自誤 404`／`405` 之鑄（主線·`commit bbf2ba5e`）

🔒 **payload 由檔案管線直接產生**（`常規七 一`）——自**倉內 blob** 抽 `ZW404` 二哨兵之間，
**⛔ 剝層、⛔ 加 `> `、⛔ 改一字**、⛔ 憑記憶重寫。

| 受詞 | 實測 |
|---|---|
| 哨兵唯一性（母體 ＝ **本單單檔**·**二數並報**） | `<!-- ZW404-BEGIN -->`：**整列逐位相等框 ＝ `1`**／子字串框 ＝ `2`；`<!-- ZW404-END -->`：**`1`**／`2`（單行文另引該二字串各一次） |
| payload | **`42`** 列／**`3845`** B／`sha256` ＝ `45f300ed91aa4cdc…`；首列 ＝ `` `### 🩸 \`自誤 404\`　**未先現查改之<u>接觸面與守衛分布</u>…**` ``；末列 ＝ `` `③ 二框之值凡並存者**一律並列出艙**、**⛔ 只報其一**。` ``；payload 內含哨兵字樣 ＝ `False` |
| 落點 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`（**`43` 增／`0` 刪**·嚴格末端追加） |

### 二-1　落地後之機驗（**繫於性質**·`自誤 402` 攔法）

| 款 | 實測 | 判 |
|---|---|---|
| `1` **鑄號框之當場現查**（**自器碼取**·⛔ 轉引本單） | `RE_ZW_DEF` 逐字 ＝ `` r'^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：]' ``（自 `verify/probes/probe_WG9267_issuer_measurers.py` 當場取） | ✅ |
| `2` **以器本身量落地後之簿** | 自誤 相異 ＝ **`395`**（`393 + 2`）／MAX ＝ **`405`**；`400`／`401`／`402`／`403`／`404`／`405` 之定義列命中**各 `1`**（⛔ `> 1`）。`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** | ✅ |
| `3` **判別力二造** | [必命中] `自誤 399`（本批鑄成前即存在）＝ `True`（`1` 列）／[必為零] 一**執行期組出**之人造號（字面⛔ 出艙）＝ `0` 列 | ✅ |
| `4` **哨兵⛔ 入簿** | 簿內 `ZW404`／`ZW404-BEGIN`／`ZW404-END` 之命中（列框·母體 ＝ 該簿單檔）**皆 `0`** | ✅ |
| `5` **append-only** | 期初 `836361` → 期末 `840207` B（**＋`3846`**·`+43`／`−0`）；**嚴格前綴 ＝ `True`**／`new.endswith(payload)` ＝ `True`；`CR` ＝ `0`；`wg942_append_audit.py`（`PYTHONIOENCODING=utf-8`）**`rc = 0`**·逐字「**結論：✅ 全部成立**」 | ✅ |

🔒 **款 `2` 全數成立** ⇒ ⛔ 判器紅。

---

## `§三`　工項二：坑 `bj`／`bk` 之鑄 ＋ `ALPHA_LABELS` 之擴（主線·`commit 8e3b0d32`）

🔑 **本工項之一項手段係單 `§四` 首段<u>明文授權</u>**：擴 `ALPHA_LABELS` 至 `bk`——
射程 ＝ **僅** `verify/probes/probe_WG9269_pit_index.py` 內**新增一列**。
🔒 該檔屬 `verify/` **子層**、**⛔ 在生產碼 `34` 檔母體內**（收工閘 `2` 併報其 `numstat`）。

| 受詞 | 實測 |
|---|---|
| 哨兵唯一性（**二數並報**） | `<!-- PIT-BEGIN -->`／`<!-- PIT-END -->`：**整列逐位相等框各 `1`**／子字串框各 `2` |
| payload | **`28`** 列／**`2526`** B／`sha256` ＝ `dcf323afa603b8a7…`；首列 ＝ `` `### 🕳 坑 \`bj\`　**量測器之母體<u>靜默塌陷</u>，而其判別力造全盲**` ``；payload 內含哨兵字樣 ＝ `False` |
| 落點 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`（**`29` 增／`0` 刪**·**與 `§二` 之 payload 分列 `commit`**） |

### 三-1　落地後之機驗

| 款 | 實測 | 判 |
|---|---|---|
| `1` **坑之定義框之當場現查**（**自器碼取**·⛔ 轉引本單） | `D-H` 標題形逐字 ＝ `` hrx = re.compile(r"^(#{1,6})[^\n]*?坑[\s　]*" + BT + r"?([0-9a-z][0-9a-z\-]*)" + BT + r"?") ``（`BT` ＝ `chr(96)`·**⛔ 令其經 bash 一層**）。🔒 該器之形**僅 `D-H`／`D-T` 二形**（⛔ 三形） | ✅ |
| `2` **以器本身量落地後之態** | **`89`／`89`**（`87 + 2`）·字母系末標籤 **`bk`** ⇒ 下一 **`bl`**；`bj`／`bk` 之**可解析** ＝ `True`（三項齊備 `✅✅✅`）·⛔ 可解析之列數 ＝ `0`；母體 ＝ 追蹤 `.md` **`802`** 檔 | ✅ |
| `3` **`ALPHA_LABELS` 之擴** | 成員數 **`61` → `63`**（**＋`2`**）·尾元 ＝ `'bk'`（🛑 **先實測後填**）；該檔 `git diff --numstat` ＝ **`1` 增／`0` 刪**。🔒 **框字串之處數<u>現查</u>**（⛔ 假定）：`ALPHA_LABELS[-1]` ＝ **`2`** 處（`:290`／`:292`·**皆 f-string** ⇒ **自動齊一**）／字面 `"bi"` ＝ **`1`** 列（係**資料列本身**·⛔ 框）／硬編標題字串 `〜\`bi\`` ＝ **`0`** ⇒ **⛔ 須另改任何框** | ✅ |
| `4` **定義處清單** | 🩸 **見下「三-2」——與單所載之預期<u>相異</u>·照實具名** | 🩸 |
| `5` **哨兵⛔ 入簿** | 簿內 `PIT-BEGIN`／`PIT-END` 之命中**皆 `0`** | ✅ |
| `6` **判別力二造** | 造甲[必≥1] 坑 `n` 之定義處 ＝ **`3`**（本器獨立量·`≥ 1`）／器自身之造甲 ＝ `6`；造乙[必為0] **執行期組出**之人造坑 ＝ `0`／`0` | ✅ |

### 三-2　🩸 **款 `4` 之照實具名：`bj` 之定義處為 `4`（⛔ 單所載之 `2`）**

單 `§四` 逐字預告「二坑之**定義處各為 `2`**（本單 ＋ 簿）」並令「**照實具名為 `2`**，⛔ 判為紅、⛔ 逕修」。
🔒 **本窗實測（框 ＝ `D-H` 標題形·自器碼當場取·母體 ＝ 追蹤 `.md` 全檔·⛔ `text=True`）**：

| 坑 | 定義處 | 逐處之 `檔` ＋ **字樣錨** |
|---|---|---|
| **`bj`** | 🔴 **`4`**（⛔ `2`） | ① `docs/orders/W-G.9-301_…md`　`` `# \`W-G.9-301\`　施工單（**中**）：路丙**實作形之重釘** ／ \`自誤 404\`・\`405\` ＋` ``（**單之 H1 標題**）／② 同檔　`` `## \`§四\`　工項二：坑 \`bj\`／\`bk\` 之鑄 ＋ \`ALPHA_LABELS\` 之擴（主線）` ``（**節標題**）／③ 同檔　`` `### 🕳 坑 \`bj\`　**量測器之母體…**` ``（**真定義**）／④ `docs/reports/W-G.9波_claude.ai側自誤登記.md`　同 ③ |
| `bk` | **`2`**（與單所載**相符**） | ① `docs/orders/W-G.9-301_…md`　`` `### 🕳 坑 \`bk\`　**以 \`text=True\` 取含 CJK 路徑之 \`git\` 輸出…**` ``／② 簿內同形 |
| （對照）`bh` | **`3`**（既有·亦⛔ `2`） | `W-G.4_泛用阻塞項登記表.md` 之受領節標題／`W-G.9-284R_…md` 之 **H1**／同檔 `#### 坑 \`bh\`` |

🔒 **成因（機械·⛔ 推定）**：`D-H` 之框係「`^#{1,6}` … 含『坑』… 標籤」⇒ **凡標題列提及該坑者皆被計為定義處**。
本單之 **H1 標題**與 **`§四` 節標題**各含一次「坑 `bj`」⇒ `bj` 多計 `2`；`bk` 因該二標題以 `・`／`／` 分隔而
**⛔ 緊接於「坑」之後**，故未被該框咬中。
🔒 **其與坑 `bh` <u>同形</u>**——`bh` 亦為 `3`（其 `W-G.9-284R` 之 H1 亦含之），
即 `W-G.9-285 §五-2` **已受領**之現象。
🛑 **⇒ 依單明令：照實具名、⛔ 判為紅、⛔ 逕修該框。**
🔒 **二獨立量測相符**：器本身之輸出亦載 `bj` ＝ `…md:1`（D-H）**＋3**、`bk` ＝ `…md:185`（D-H）**＋1**
⇒ 與本窗之 `4`／`2` **逐數相符**（**⛔ 同一支求值器**·`§零` 閘 `6′` 之器 vs 本窗之獨立掃描）。

---

## `§四`　工項三：`GB-170` 之末端追加（主線·`commit 6a3c87c3`）

🔑 本 payload **⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**（`> ` 起首之列 ＝ **`23`**）。

| 受詞 | 實測 |
|---|---|
| 哨兵唯一性（**二數並報**） | `<!-- GB301-BEGIN -->`／`<!-- GB301-END -->`：**整列逐位相等框各 `1`**／子字串框各 `2` |
| payload | **`29`** 列／**`3085`** B／`sha256` ＝ `e74b34a0c9edf1b5…`；首列 ＝ `` `---` ``；末列 ＝ `` `> **失效條件 \`(2)\`／\`(3)\` 仍未成就**——⛔ 解除、⛔ 收窄、⛔ 提修法主張。` `` |
| 落點 | `docs/reports/W-G.4_泛用阻塞項登記表.md`（**`30` 增／`0` 刪**·⛔ 上文一字不刪） |
| **`GB-132` 之避讓** | payload 內 `^#` 起首而**列內含 `GB-`** 者 ＝ **`0`** ✅ |
| append-only | 期初 `904710` → 期末 `907796` B（**＋`3086`**）；嚴格前綴 ＝ `True`／`endswith(payload)` ＝ `True`；`CR` ＝ `0` |

### 四-1　**反向機驗**（`自誤 402` 攔法 ④·**三形分列並報**·`GB-158`）

| 形（**自器碼當場取**·逐字） | 期初 相異／MAX | 期末 相異／MAX |
|---|---|---|
| `RE_GB_F1` ＝ `` r'^#{2,4} [^\w`]{0,8}`?GB-(\d{1,3})`?(?![0-9])' `` | `93`／`170` | `93`／`170` |
| `RE_GB_F2` ＝ `` r'^\| *\*\*`?GB-(\d{1,3})`?\*\*' `` | `131`／`137` | `131`／`137` |
| `RE_GB_F3` ＝ `` r'^#{1,6} .*?`?GB-(\d{1,3})`?(?![0-9])' `` | `95`／`170` | `95`／`170` |

🔒 **聯集（扣哨兵 `997`）**：相異 **`168` → `168`**／MAX **`170` → `170`**／缺號 **`[12, 87]` → `[12, 87]`**
⇒ **⛔ 鑄號之<u>性質</u>成立**（⛔ 以「payload 內無 `###`」或「未被框命中」充證）。
判別力[必命中] `GB-170` 之既有定義列之命中形 ＝ `['RE_GB_F1', 'RE_GB_F3']`（`≥ 1` 形）；
判別力[必為零] 一**執行期組出**之人造號於三形之命中形 ＝ `[]`。
🔒 **三形皆非空**（`93`／`131`／`95`）⇒ 「二形以上皆 `0` 須 loud 具名」之款**無受詞**（照實具名）。

---

## `§五`　工項四：**自導可行性之現查**（主線·**零生產碼**）

🔒 **本項之器** ＝ `verify/probes/probe_WG9301_selfderive.py`（**新檔**·`verify/` **子層**·**⛔ 在生產碼 `34` 檔母體內**）。
🛑 **本器⛔ 改生產碼一字、⛔ 驅動 `run_verification`、⛔ 擬任何改法或 diff**——**只現查**。
【倉】`verify/out/WG9301R_selfderive.log`

### 五-1　款 `1`：二函式之**定義處**與**簽章逐字**（框 ＝ AST `FunctionDef`／`AsyncFunctionDef`·**二形並取**）

母體 ＝ 生產碼 **`34`** 檔（**正面列舉**）·🆕 **其基數綁外部錨**（`§零` 閘 `7` ＝ `34`）⇒ **相符**（坑 `bj`）。

| 函式 | 檔／處**二數並報** | **簽章逐字** | 形參名序 | 體之列數 |
|---|---|---|---|---|
| `_corner_buffer_S` | `1`／`1`（`app.py`·`FunctionDef`） | `` `def _corner_buffer_S(block_poly, d_hat, front_p1, allocation_dir, range_area, side, tol=0.01, _label='')` `` | `['block_poly', 'd_hat', 'front_p1', 'allocation_dir', 'range_area', 'side', 'tol', '_label']` | **`104`** |
| `_first_corner_alloc_dir` | `1`／`1`（`app.py`·`FunctionDef`） | `` `def _first_corner_alloc_dir(side_mid)` `` | `['side_mid']` | **`36`** |

### 五-2　款 `2`：二函式體內之**自由名**（框 ＝ `Name.Load` 扣形參／扣體內 `Store`／扣 `builtins`）

| 函式 | 自由名 | 於 `ns` 之鍵框內 |
|---|---|---|
| `_corner_buffer_S` | **`2`** 個：`_strip_s_range`／`_block_strip` | `True`／`True` |
| `_first_corner_alloc_dir` | **`1`** 個：`np` | `True` |

🔒 `ns` 之鍵框 ＝ `app.py` 之**頂層**名（`t.body`）·基數 ＝ **`286`**
（⛔ 模組 `__globals__`——`W-G.9-298R` 已證 `ns` 係純 `dict`）。

### 五-3　款 `3`：🔑 **所需輸入集 `R`**（⛔ 判「可導」·**只出艙逐名之來源**）

`R` ＝ `_first_corner_alloc_dir` 之**形參** `['side_mid']` ＋ 其體所消費之**自由名** `['np']`。

| `R` 之名 | 於 `_corner_buffer_S` 之形參 | 於 `ns` | **來源** |
|---|---|---|---|
| 🔴 **`side_mid`** | **`False`** | **`False`** | 🔴 **二者皆非** |
| `np` | `False` | `True` | `ns` |

### 五-4　款 `4`：🔑 `side_mid` 之等價量於 `_corner_buffer_S` 內之可得性（**只現查既有原語**·⛔ 自寫第二套幾何）

`_corner_buffer_S` 體內**已調用**之名 ＝
`['float', 'np.asarray', '_strip_s_range', 'max', '_band_area', 'range', 'abs', 'RuntimeError', 'print', '_block_strip', 'np.linalg.norm']`。
其中屬 `ns` 之鍵者（＝ **既有原語**）＝ `_strip_s_range`／`_block_strip`：

| 原語 | **簽章逐字** | **回傳結構**（逐字·去重） |
|---|---|---|
| `_strip_s_range` | `` `def _strip_s_range(geom, d_hat, corner_pt, allocation_dir=None)` `` | `['(min(ts), max(ts))', 'None']` |
| `_block_strip` | `` `def _block_strip(block_poly, d_hat, baseline_pt, S, allocation_dir=None, n_hat_far=None)` `` | `['(None, 0.0)', "(cut, float(cut.area) if hasattr(cut, 'area') else 0.0)"]` |

🔴 **既有原語中，其體內含 `side_line`／`SIDE_LINE`／`f3_cad_side_lines_by_side` 者 ＝ `[]`**
⇒ **側界資料⛔ 在既有原語之回傳結構中**（二者之回傳僅為 **`s` 域之二元組**與 **`(幾何, 面積)`**）。

🔒 **`_first_corner_alloc_dir` 所消費之側界資料**（其 `Attribute`／`Subscript` 鏈·逐字）＝
`_st_fc.session_state.get`／`_st_fc.session_state`／`_slbs.get`／`_sd.get`／`_sd['p1']`／`_sd['p2']`；
其**查表之鍵**（字串常數·逐字）含 `'f3_cad_side_lines_by_side'`／`'left'`／`'right'`／`'mid'`／`'p1'`／`'p2'`。

### 五-5　款 `4′`：`_label`（`_corner_buffer_S` **唯一之「標籤狀」形參**）之**實參逐字**

🔑 **其由**：`_first_corner_alloc_dir` 之 docstring **逐字**令
「🔒 **以 `side_mid` 幾何反查 SIDE_LINE**——⛔ **不靠任何字串標籤**。
案由（D-2b-0 實測）：`_corner_first_lot_G` 之 `_label` 實為複合字串 `'R4·628(1)·左'`、**非街廓標籤**；
以其查表必 `None` ⇒ 靜默退回。」
⇒ **「以 `_label` 查表」之禁，已寫在受詞函式自身之 docstring 內**。本款**只現查該實參實際為何**。

| # | 檔 | `_label` **實參逐字** |
|---|---|---|
| `S1`／`S2` | `app.py` | `` `blk_label` `` |
| `S3`／`S4` | `verify/stepg_pipeline.py` | `` `blk_label` `` |
| 🔴 `S5` | `verify/wf_f1.py` | `` `f'{lbl}·F.1'` `` |
| 🔴 `S6`／`S7` | `verify/wf_f4.py` | `` `f'{blk}·E3'` `` |

⇒ 🔴 **`7` 處中 `3` 處之 `_label` 為<u>複合字串</u>**（⛔ 街廓標籤）
——以其查 `f3_cad_side_lines_by_side` 必 `None` ⇒ **即 docstring 所具名之「靜默退回」**。

### 五-6　款 `5`：**非 `forced` 呼叫端之復驗**（🔒 本批**獨立重得**·⛔ 轉引 `W-G.9-300R`）

`S` ＝ **`7`** 處／**`4`** 檔（框 ＝ AST `Call`·**二形並取**）。逐處命中之外圍 `test`：

| # | 檔 | 命中之外圍 `test`（逐字） |
|---|---|---|
| `S1` | `app.py` | `If` `` `_fo_left` `` |
| `S2` | `app.py` | `If` `` `_fo_right` `` |
| `S3` | `verify/stepg_pipeline.py` | `If` `` `_fo_left` `` |
| `S4` | `verify/stepg_pipeline.py` | `If` `` `_fo_right` `` |
| `S5` | `verify/wf_f1.py` | `If` `` `fo.get('left_forced_offset')` `` |
| `S6` | `verify/wf_f4.py` | `IfExp` `` `fo.get('left_forced_offset')` `` |
| `S7` | `verify/wf_f4.py` | `IfExp` `` `fo.get('right_forced_offset')` `` |

⇒ **非 `forced` 之呼叫端 ＝ `0`／`7`** ✅（與 `§一` 裁 `3`（一）所據者**本批獨立復現**）。
判別力[必為否] ＝ `def _corner_buffer_S` 之定義列：外圍 `If`／`IfExp` ＝ **`0`** 個 ⇒ 判 **否** ✅
⇒ 該判準**非恆真**。

### 五-7　款 `6`：判別力四造

| 造 | 期 | 實測 | 判 |
|---|---|---|---|
| [必非零] 二函式之定義處 `≥ 1` | `≥ 1` | `1`／`1` | ✅ |
| 🆕 [必為 `34`] **母體基數之外部錨**（坑 `bj`） | `34` | `34` | ✅ |
| [必為零] 一**執行期組出**之人造函式名之定義處 | `0` | `0` | ✅ |
| [必真] `_first_corner_alloc_dir` 在 `ns` 之鍵 | `True` | `True` | ✅ |

---

## `§六`　🛑 **`停四` 之判**

> **停四**（單 `§六-3` 逐字）：款 `3` 之 `R` 中**有任一名**既⛔ 在 `_corner_buffer_S` 之現有形參內、
> 亦⛔ 可自其現有形參經**既有原語**導出（款 `4`），⇒ 函式體內自導**不可施**於現有簽章下 ⇒ **停、上呈**。

| 合取項 | 受詞 | 實測 | 判 |
|---|---|---|---|
| `(i)` `R` 中有一名⛔ 在現有形參內 | `side_mid` | 現有形參 ＝ `['block_poly', 'd_hat', 'front_p1', 'allocation_dir', 'range_area', 'side', 'tol', '_label']` ⇒ `side_mid` **`False`** | 🔴 **成立** |
| `(ii)` 該名亦⛔ 可自現有形參經**既有原語**導出 | 既有原語 ＝ `_strip_s_range`／`_block_strip` | 二者之回傳結構 ＝ `(min(ts), max(ts))`／`None` 與 `(None, 0.0)`／`(cut, area)`；其體內含側界字樣者 ＝ **`[]`** ⇒ **側界資料⛔ 在其回傳結構中** | 🔴 **成立** |

⇒ 🛑 **`停四` 成就**（二合取項皆成立）⇒ **判器紅、停、上呈**。

### `§六-1`　**第二項獨立之由**（照實併記·⛔ 據以擬改法）

縱使改走「以 `_corner_buffer_S` 之現有形參 `_label` 查 `f3_cad_side_lines_by_side`」之路，其亦**不可施**：
① `_first_corner_alloc_dir` 之 docstring **逐字**令「⛔ **不靠任何字串標籤**」，並具名其案由（D-2b-0）；
② 本批實測 **`7` 處中 `3` 處**之 `_label` 實參為**複合字串**（`f'{lbl}·F.1'`／`f'{blk}·E3'`）
⇒ 以其查表必 `None` ⇒ **靜默退回**（`no-silent-fallback` 之受詞）。
🛑 **本節⛔ 提任何改法**——照實出艙二由，**處置候發單側**。

### `§六-2`　🛑 **四禁於停機後仍在**（單 `§六-3` 逐字）

**⛔ 新增形參、⛔ 改簽章、⛔ 新增 `import`、⛔ 自寫第二套幾何**
——本批**⛔ 以「為使其可施」為由逾之**，亦⛔ 擬任何改法或 diff。

---

## `§七`　一句話推導

> `_first_corner_alloc_dir` 之所需輸入集 `R` ＝ `{side_mid, np}`，其中 `np` 於 `ns` 可得而
> **`side_mid` 既⛔ 在 `_corner_buffer_S` 之八個現有形參內、亦⛔ 在 `ns` 內**；
> 而其體內**僅有**之二既有原語（`_strip_s_range`／`_block_strip`）之回傳結構為 **`s` 域二元組**與
> **`(幾何, 面積)`**、**⛔ 含任何側界資料**（含側界字樣者 ＝ `[]`）
> ⇒ **函式體內自導⛔ 可施於現有簽章下** ⇒ **停四成就 ⇒ 停、上呈**；
> 其唯一之「標籤狀」旁路（以 `_label` 查表）復由受詞函式自身之 docstring **明文禁止**，
> 且 `7` 處中 `3` 處之 `_label` 實為複合字串 ⇒ **該旁路必靜默退回**。

---

## `§八`　本批之**自解清單**（`常規九 一`·五項齊備·⛔ 略）

| # | 疑義（逐字） | 二以上之讀法 | **所取者** | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§七` 只明文授權**一項**手段（擴 `ALPHA_LABELS`），而 `§六` 之現查**須有量測器**方能出艙 | (甲) 於 `verify/probes/` **新增**一器；(乙) 只以 session 內之臨時腳本量測、⛔ 入倉 | **(甲)** | ①**零土地後果**（純 AST 唯讀·⛔ 觸配地）；②**零生產碼**——新檔屬 `verify/` **子層**，`34` 檔母體 blob 期初＝期末**相異 `0`**；③**可機驗**——本批當批實跑其否證對照（[必為零] 人造函式名 ＝ `0`／[必非零] 定義處 ＝ `1`·二者皆如預期）；④**可逆**——**新建檔**、⛔ 編修任何既有檔既有一字；⑤**出艙具名**——即本列 | (乙) 使該現查**⛔ 可自倉復現**，與「數字宣稱一律附 log `file:line`」及【倉】之體例相斥；且 `§七` 之禁列**未含**「新增探針」，而 `W-G.9-300 §三` 之同形手段係**明文授權**之先例 |

🔒 **黑名單之逐項自檢**：⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何（**未為**）；
⛔ 動生產碼一字含註解（**未為**·`34` 檔相異 `0`）；二讀法之後果**在土地上相同**（皆零）；
**⛔ 新訂任何準據、⛔ 廢止或放寬任何既有之閘**；⛔ 涉 KL 已裁之款。⇒ **五項齊備·⛔ 落黑名單** ⇒ 得自解。

---

## `§九`　⛔ **為之事**之逐項自檢（單 `§七`·**逐項**·⛔ 略）

| 受詞 | 自檢 |
|---|---|
| ⛔ 改生產碼 `34` 檔**一字**（含 `_corner_buffer_S` 函式體） | ✅ **未為**——`34` 檔 blob 期初＝期末**相異 `0`**·`app.py` ＝ `4379108a…` |
| ⛔ 開任何分支 | ✅ **未為**——`refs/heads/verify/*` 期末 **`20`**·`gb170` ＝ `0` |
| ⛔ 動用 KL 之放行 | ✅ **未為** |
| ⛔ 驅動 `run_verification` 或任何長跑 | ✅ **未為**——本批**⛔ 執行** `run_verification`／`run_all`（`verify/out/got_*` 之 `mtime` 未因本批而動） |
| ⛔ 辦修批 | ✅ **未為**（`停四` 成就 ⇒ 修批候 `W-G.9-302`） |
| ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref | ✅ **未為**——`p3a` 對主線 `merge-base --is-ancestor` `rc = 1` |
| ⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一 | ✅ **未為**——`baselines` `298` 檔·`a898f4e1…` 未變；六物證 blob 逐位未動 |
| ⛔ `WV_BAKE` | ✅ **未設**（`os.environ.get('WV_BAKE')` ＝ `None`） |
| ⛔ 引任何自 baseline 取得之數 | ✅ **未為**——本報告之數皆出自 AST 現查與二器之輸出 |
| ⛔ 改任何函式簽章、⛔ 新增任何形參、⛔ 新增任何 `import` 於生產碼、⛔ 改任何 `*_EXPECT` 一字 | ✅ **未為** |
| ⛔ 自寫第二套幾何或第二份 `W`／`side_mid` 之定義（`GB-48` 族） | ✅ **未為**——`§五-4` **只現查既有原語**之簽章與回傳結構 |
| ⛔ 動 `probe_WG9269_pit_index.py` 之**任何既有列**（僅准新增一列） | ✅ **未為**——其 `numstat` ＝ **`1` 增／`0` 刪** |
| ⛔ 動 `probe_WG9292_pathC.py`／`probe_WG9295_joint.py`／`probe_WG9298_gate3.py`／`probe_WG9300_contact_surface.py`／任何**其他既有**探針一字 | ✅ **未為**——本批於 `verify/probes/` **只新增一檔**（`probe_WG9301_selfderive.py`） |
| ⛔ 鑄 `GB`／`VR`／`K-9` 之任何號 | ✅ **未為**——三簿期末**一字未動**（`168`／`170`·`80`／`95`·`28`／`29`） |
| ⛔ 解除或收窄任何 `GB`（含 `GB-170`） | ✅ **未為** |
| ⛔ 逕修 `side_mid` 之在域性（⛔ 移賦值列） | ✅ **未為**——其只登記於 `§四` 之 `GB-170` 末端追加 |
| ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ **未為** |
| ⛔ 追改 `W-G.9-299`／`-300`／補令一／交接註／`W-G.9-300R` 或任何已入倉文件一字 | ✅ **未為**——本批 `deletions` 全 `0` |
| ⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文 | ✅ **未為** |
| ⛔ 判 `GB-170` 可否解除、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ **擬任何改法或 diff** | ✅ **未為**——`§六` 只出艙 `停四` 之二合取項**與其產生指令** |
| ⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制 | ✅ **未為** |
| ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ **未為**——`push` 後之 `ls-remote` 實查值**出艙於回報**（收工閘 `7`） |

---

## `§十`　逐 `commit`（**全 `40` 碼**）

| # | `commit` | 題 |
|---|---|---|
| `1` | `d65b2511e2a66d396ed5646b1f79c390c13cfb02` | `W-G.9-301 工項零：本單原封入倉（三方逐位對拍·二框並報·受詞自證）⛔ 零生產碼` |
| `2` | `bbf2ba5ed5e2813d7c1cccfc8acb743ce9dc0563` | `W-G.9-301 工項一：自誤 404／405 之鑄（哨兵界·嚴格 append-only·⛔ 行號為錨）⛔ 零生產碼` |
| `3` | `8e3b0d324aebc8e4cc1c13ace027b65b5061c204` | `W-G.9-301 工項二：坑 bj／bk 之鑄 ＋ ALPHA_LABELS 之擴（哨兵界·嚴格 append-only）⛔ 零生產碼` |
| `4` | `6a3c87c317fadcd9ef896a6caad4b952eca92269` | `W-G.9-301 工項三：GB-170 之末端追加（⛔ 鑄任何號·⛔ 解除⛔ 收窄）⛔ 零生產碼` |
| `5` | （本報告 ＋ 新器 ＋ 二物證落檔·其 `sha` 見回報） | `W-G.9-301 工項四：自導可行性之現查 ＋ 🛑 停四成就（⛔ 辦修批）⛔ 零生產碼` |

---

## `§十一`　收工閘（**報告入倉後量**·嚴格末端追加）

（本節於報告入倉後以**純末端追加**補之。）

### `§十一-1`　逐閘（**報告入倉後量**·態 ＝ `fdd3b69cc3a6e94bff0a1468fb66248fa5ec2e2e`）

| # | 受詞（**指令**） | 期／判準 | 實測 | 判 |
|---|---|---|---|---|
| `1` | `git show --numstat <各 commit>` **逐檔具名** | 刪除欄**全 `0`** | `d65b2511` `351`／**`0`**｜`bbf2ba5e` `43`／**`0`**｜`8e3b0d32` `29`／**`0`** ＋ `1`／**`0`**｜`6a3c87c3` `30`／**`0`**｜`fdd3b69c` `352`／**`0`** ＋ `118`／**`0`** ＋ `122`／**`0`** ＋ `421`／**`0`** ⇒ **全 `0`** | ✅ |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 **`0` 行**；對照組[必非零] `≥ 1` | 五 `commit` 逐區間**皆 `0` 行**；對照組 `17fd981^..17fd981` ＝ **`2`** ⇒ 器非紅。🔒 `probe_WG9269_pit_index.py` 屬**子層**、⛔ 在母體內 ⇒ 其 `numstat` ＝ `` `1	0	verify/probes/probe_WG9269_pit_index.py` `` | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·**取 bytes**·🆕 **含 CJK 路徑者一律取 bytes**·坑 `bk`·`-z` ＋ `core.quotePath=false`） | `0` | 本批異動 **`8`** 檔，其 `.md`／`.py` 共 `6` 檔（`36566`／`907796`／`30322`／`842734`／`15236`／`19404` B）`CR` 合計 ＝ **`0`**；判別力[必非零] `data/V6.dxf` `CR` ＝ **`12308`**／全長 **`88341`** B | ✅ |
| `4` | 四簿期末（**器之輸出**·⛔ 自擬單框） | 自誤 相異 **`395`**／MAX **`405`**（＋**二**）；餘三簿一字未動 | 自誤 相異 **`395`**／MAX **`405`**／缺 `[106, 355, 356, 357]`；`GB` `168`／`170`／缺 `[12, 87]`｜`VR` `80`／`95`／缺 `[73, 75]`｜`K-9` `28`／`29`／缺 `[]` ⇒ **餘三簿與期初逐格相同** | ✅ |
| `5` | 坑之期末（**器之輸出**） | **`89`／`89`**（＋**二**）·末標籤 **`bk`** ⇒ 下一 **`bl`**；`ALPHA_LABELS` 成員 `+2`·尾元 `bk`；`.md` 增量自洽 | **`89` 標籤／`89` 可解析**（⛔ 可解析之列 ＝ **`0`**）·字母系末標籤 **`bk`** ⇒ 下一 **`bl`**；母體 ＝ 追蹤 `.md` **`803`** 檔；`ALPHA_LABELS` **`61` → `63`**·尾元 `'bk'`；`.md` **`801` → `803`**（**＋`2`** ＝ 本批新增之 `.md` 檔數：單 `1` ＋ 報告 `1`）⇒ **自洽**；器二造：造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造坑 `0`／`0` ✅ | ✅ |
| `5′` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；🛑 受詞係 `index` ⇒ 復跑前先 `git add` | 判定組（反引號 token）**`32`** 個 ⇒ 報告命中 `0` 者 ＝ **`0`** 個（`rc = 0`）；參考組 `3` 個中 `1` 個命中 `0`（**⛔ 作判**·係單之行文片段）。🔒 **本節追加後已重跑**（先 `git add`） | ✅ |
| `6` | 自限復驗 | 見右 | 生產碼 `34` 檔 blob 期初＝期末**相異 `0`**·`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`（**框 ＝ blob `sha1`**）；判別力[必相異] ＝ 本批確有異動之 `verify/probes/probe_WG9269_pit_index.py`（`7d174ac28e90` → `08cca5878977` ⇒ 相異 `True`）⇒ 該比對**確能偵出相異**；`baselines` **`298`** 檔·`a898f4e1bba8…` **未變**；`p3a` `rc = 1`；`WV_BAKE` ＝ `None`；`k6_step0_merge(` `2`→`2`／`rect_fits` `7`→`7`／`_blue_shadow_tri` `7`→`7` ⇒ `K-9-29`／`K-9-5-*`／`K-9-12` **⛔ 落地任何子項**；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；**六物證落檔**逐位未動 ＋ 本批新增二檔 `F/T` **新入倉**（**三態分列**·**框逐字具名**）；對照組[必為零] 執行期組出之人造路徑期末存在 ＝ `False` | ✅ |
| `6′` | `git ls-remote origin 'refs/heads/verify/*'` | `…-299-gb170` ＝ **`0`**；[必存在] `p3a` ＝ **`1`** | 該族共 **`20`** 個；`gb170` ＝ **`0`**（**⛔ 開**）；`p3a` ＝ **`1`** | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** | （其 `sha` 於本節自身落地前不存在·**結構上遞迴** ⇒ 逐字見聊天回報） | — |

### `§十一-2`　本批之自捕　🔒 **零則**（照實具名·⛔ 以「未觸」充綠）

本批**⛔ 於出艙前攔下任何自捕**——五工項之量測皆一次成立。
🛑 **其⛔ 得讀為「本批無誤」**：本批係**中**級（登記 ＋ 純 AST 現查·**⛔ 長跑**·⛔ 改生產碼），
其受詞面遠小於 `W-G.9-300`（該批六則自捕皆出於**動態量測**與**雜湊框**）。
🔒 **併記一項<u>與單所載相異</u>之實測**（已於 `§三-2` 逐處具名）：坑 `bj` 之定義處 ＝ **`4`**（單載 `2`）
——**⛔ 自捕**（其非本窗之誤），係**單之預期與框之實際行為相異**；依單明令**照實具名、⛔ 判紅、⛔ 逕修**。

---

## `§十二`　🛑 本批之終結

> **`§六-3` 之 `停四` 成就 ⇒ 判器紅、停、上呈。**
> 修批**⛔ 辦**（候 `W-G.9-302`）；KL 之放行**仍未動用**；`verify/W-G.9-299-gb170` **⛔ 開**；
> `GB-170` **⛔ 解除、⛔ 收窄**，其失效條件 `(2)`／`(3)` **仍未成就**。
> 🔒 **路丙之<u>取線之設計</u>⛔ 變**（`W-G.9-299 §一` 裁 `3` 之受領仍在）——本批所證者僅其**落點**
> 「`_corner_buffer_S` 之**函式體內自導**」**於現有簽章下不可施**。
> 🛑 **⛔ 呈 KL**；**⛔ 擬任何改法或 diff**。候發單側裁者三項：
> ① `停四` 之處置（含四禁是否鬆動——🛑 **本報告⛔ 就此表任何意見**）；
> ② 坑 `bj` 定義處 ＝ `4` 之處置（與 `bh` ＝ `3` 同形·`W-G.9-285 §五-2` 已受領之現象）；
> ③ `side_mid` 在域性不足（已登記於 `GB-170` 末端追加·**⛔ 逕修**）。
