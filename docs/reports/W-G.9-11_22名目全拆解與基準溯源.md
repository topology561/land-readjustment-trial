# `W-G.9-11`：22 名目全拆解 ＋ 基準溯源與判準預登

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg911-dissect`）
**基準**：`cd9569f`　**事前登記**：`docs/reports/W-G.9-11_前置登記.md`（commit `ead6a08`）

> # ✅ **`M1`〜`M6` 全部成立；`Y-1`〜`Y-4` 皆未觸發。**
>
> 🔑 **兩個核心結果**：
> ① **`M3` 成立**——`G3` 三項以外**確有**可回收之主判定，
> **新發現者為 `F.2 靜態閘` 與 `F.4 靜態閘`**（與已立之 `F.3 靜態閘` 同型同構）。
> ② **`M5` 不只「至少一個」，而是<u>全部 16 個</u>**——每一份 v3 基準自烘焙以來
> 皆有生產側 commit 觸及其產生路徑 ⇒ **「基準過期」對每一檔皆為活假說**。
>
> 🛑 **一項須裁**：現查發現**註冊判準未涵蓋之中間層**（需 CAD 管線但**不需** `run_step_g`·6 項）
> ——⛔ 本批**另格呈現、未併入**，判準之擴充**待 KL／claude.ai 裁**（§3-1）。

---

## §1 `M1`〜`M6` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **M1** | ✅ **成立** | `WG96_coverage.txt` 產於 `20f3446`，其母體 `verify/run_verification.py` 自彼時起 `git diff --numstat` **空輸出**。🔒 判別力自檢：同式對 `verify/run_all.py` 得 `57 2` ⇒ **非恆為空** |
| **M2** | ✅ **成立** | 泛化工具**逐項重現**舊工具對 `G3` 三項之結論（`try` 範圍／主判定集合／`except` 歸屬皆 ✅）。🔒 判別力自檢：對其集合人造一筆 ⇒ **會判不符** |
| **M3** | ✅ **成立** | `G3` 以外確有可回收者——**新發現 `F.2 靜態閘`／`F.4 靜態閘`**（§4-1） |
| **M4** | ✅ **成立** | 16 檔皆可定烘焙 commit／日期（14 檔 `2466faf` 2026-07-25／2 檔 `6830642` 2026-07-10） |
| **M5** | ✅ **成立（且為全部）** | **16／16** 自烘焙以來皆有生產側 commit 觸及（72 或 117 個）⇒「過期」為**活假說** |
| **M6** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`；**全檔非路徑差異為空**（本批未掛任何東西）。🔒 判別力自檢：對前一批之 log 重算得 **1** 差異 ⇒ **非恆為空** |

---

## §2 22 名目全拆解（考古 70 之收割）

**工具**：`verify/tools/wg911_all_dissect.py`（母體自證＋母體規模＋判別力自檢）。
**產物**：`verify/out/WG911_22名目拆解.log`。

```
【母體】verify/run_verification.py：1462 行｜`results.append` 站點 107 處
【覆蓋】verify/out/WG96_coverage.txt：已執行行 519
【名單】verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt：22 名目

⇒ 22 名目合計：主判定 **85**｜未執行 **80**
   ｜只被順序擋住 3｜需CAD管線（不需 run_step_g）0｜需人判 12｜真需管線 65
```

🔒 **獨立交叉驗證**：「未執行 **80**」與 `W-G.9-6` 所量之**全檔死站點 80** 吻合
——⚠️ 本工具**首版報 85／90**（高於 80），成因為**多個名目共用同一 `try` 而重複計數**
（如 `…0m`／`…3.5m` 之 f-string 變體）；已改為**按行號去重**。

### 2-1 🔴 分類器之三次修正（⛔ 皆已具名·非事後美化）

| # | 症狀 | 根因 | 後果（若未修） |
|---|---|---|---|
| 1 | 首版**全部**落「需人判」 | 把 `results.append` **自身**之 `append` 算進呼叫集 | 分類器完全失能 |
| 2 | 總數 90／85 **高於** 全檔死站點 80 | 共用 `try` 之名目**重複計數** | 出艙一個**大於母體**之數 |
| 3 | 🔴 `W-G G.1` 被判「**可回收**」 | `_vgN = []` 後以 `.append(...)` 填值，**變異未入相依追蹤** | **偽陽性、且是危險方向**——`W-G.9-4b` 已證 `G.1` 崩於 `_ctx["0m"]` |

⇒ 修正 3 之後，`G.1` 正確落「真需管線」。
🔒 **校準**（vs `W-G.9-10` 之三項已證可回收者）：**偽陰性為零**
（⛔ 無任何已證可回收者被判為「真需管線」……惟見下之 `G.2` 例外）。

### 2-2 🔴 分類器之**已知界限**（⛔ 正面聲明）

| 界限 | 說明 |
|---|---|
| **複合命題不可原子分類** | `W-G G.2` 之**單一站點含兩子命題**（(a) 曝出契約·需管線／(b) 只寫不讀·純靜態）⇒ 分類器只能給**一個**答案（現為「真需管線」）。**其 (b) 部實可回收**——`W-G.9-10` 已立閘守之、現行實跑綠。**⇒ 校準之 `G.2` 一格為「偽陰性」，係此界限所致，非分類器出錯。** |
| **跨模組呼叫之輸入不可解** | 如 `wf_f3.fixture_cross_zone_f3(snapshot)`——語法層看不出其內部只吃 `snapshot` ⇒ 落「需人判」，**交人裁**。 |

⇒ 本分類器之定位 ＝ **保守篩子**：其「**真需管線**」之判定可信（偽陰性為零），
「需人判」者**一律人裁並附證據**。⛔ 不得把它當終審。

---

## §3 12 個「需人判」之逐項人裁（⛔ 附現查證據）

| # | 名目 | 相依證據（工具所印） | 人裁 |
|---|---|---|---|
| 1 | `v3 reverse-test① 單位工程費 3500→3550 ⟹ C 隨動` | 呼叫 `_rerun_fin`；**現查其定義內呼 `run_step_g`** | **真需管線** |
| 2 | `v3 reverse-test② 前均價 ×1.1 ⟹ B 隨動且 C 不動` | 同上 | **真需管線** |
| 3 | `v3 重劃總負擔率錨` | 未解析名 `fake_st`／`snapshot`；率值之寫入點 ＝ `ss["f3_total_burden_rate_from_finance"] = _rate`（**CAD 管線層**·`run_verification.py`） | 🔵 **中間層**：需 CAD 管線、**不需 `run_step_g`** |
| 4 | `v3 率分項錨 公設負擔比＋費用負擔比` | 未解析名僅 `fake_st` | 🔵 **中間層** |
| 5 | `v3 率用 DXF 公設實值` | 未解析名僅 `fake_st` | 🔵 **中間層** |
| 6 | `v3 reverse-test③ ⟹ 重劃總負擔率隨動` | 呼叫 `compute_total_burden_rate`；未解析名含 `ns`／`cb_by`／`snapshot`（**無** `_ctx`／`stepg_ctx`） | 🔵 **中間層** |
| 7 | `W-D.3 碎片幾何/三分類（v3）` | 呼叫 `_frag_compute` ＝ `wd3_fragment_geom.compute`（**現查其內呼 `run_step_g`**） | **真需管線** |
| 8 | `W-D.3 碎片逐邊CAD（v3）` | 同上（**同一次** `compute()`） | **真需管線** |
| 9 | `W-D.4 MinA_區 reverse-test(改深度→隨動·非寫死)` | 呼叫 `_mina_by_block`；未解析名含 `_w4`／`cb_by`／`ns`／`snapshot`。⚠️ **`_w4` 之來源本批未查明** | 🟡 **未定**（⛔ 具名·不猜） |
| 10 | `F.2 跨區段 fixture` | 呼叫 `wf_f2.fixture_cross_zone`；未解析名含 `build_parcels`（**CAD 管線產物**）、`snapshot` | 🔵 **中間層** |
| 11 | `F.3 跨區段 fixture` | 呼叫 `wf_f3.fixture_cross_zone_f3`；未解析名僅 `snapshot`（**`W-G.9-10` 已證·現行夾具實跑綠**） | 🟢 **只被順序擋住** |
| 12 | `F.4 模式二 p_avg 母體＝原始 build_parcels` | 呼叫 `wf_f4.fixture_mode2_hand`；未解析名含 `build_parcels`、`snapshot` | 🔵 **中間層** |

### 3-1 🔴 一個**註冊判準未涵蓋之中間層**（⛔ 具名上呈·本批不擅自併入）

事前登記 §2 之「只被順序擋住」定義為「輸入**只有**原始碼／常數／`snapshot`」。
**現查發現第三類**：**需 CAD 管線（`build_pipeline`／`build_build_parcels`／
`compute_total_burden_rate`）但<u>不需</u> `run_step_g`**。

🔑 **關鍵事實**：擋路之另案**只在 `run_step_g`**（結構閘 raise）——
`wd3_fragment_geom.compute()` 即**先跑完** `build_pipeline`／`run_corner_pk`，
**才死在 `run_step_g`** ⇒ **CAD 管線本身跑得通**。
⇒ 這 **5 項（＋1 未定）在實務上同樣可回收**，只是**不在註冊判準之字面內**。

🔒 **CC 之處置**：**另格呈現、⛔ 不併入「只被順序擋住」**——
判準之擴充**須由 KL／claude.ai 裁**，⛔ 非 CC 事後放寬自己的登記。

---

## §4 可回收清單與立閘優先序（⛔ 只排序·本批不實作）

### 4-1 可回收清單

| 層 | 數 | 項 |
|---|---|---|
| 🟢 **只被順序擋住**（註冊判準內） | **4** | `F.2 靜態閘`／`F.3 靜態閘`／`F.4 靜態閘`／`F.3 跨區段 fixture` |
| 🟢 **半可回收**（複合站點之一半） | **1** | `W-G G.2` 之 **(b) 只寫不讀**（⚠️ **已由 `W-G.9-10` 立閘守**） |
| 🔵 **中間層**（需 CAD 管線·不需 `run_step_g`） | **6** | `v3 重劃總負擔率錨`／`v3 率分項錨`／`v3 率用 DXF 公設實值`／`v3 reverse-test③`／`F.2 跨區段 fixture`／`F.4 模式二 p_avg` |
| 🟡 **未定** | **1** | `W-D.4 MinA_區 reverse-test`（`_w4` 來源未查明） |

⇒ **`M3` 成立**：`G3` 三項以外**確有**可回收者——
**新發現者為 `F.2 靜態閘` 與 `F.4 靜態閘`**（`F.3 靜態閘`／`F.3 跨區段 fixture`／`G.2 (b)`
係 `W-G.9-10` 已知）。

### 4-2 立閘優先序（**排序準則**：① 土地後果之近距離 ② 現況可計算之確定性 ③ 立閘成本）

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | `F.2 靜態閘`／`F.4 靜態閘` | 與已立之 `F.3 靜態閘` **同型同構**（讀 `wf_fN.py` 找四個函式名）⇒ **成本近零**、確定性最高。守「引擎不得呼叫已廢函式」 |
| **2** | 🔵 `v3 率分項錨`／`v3 率用 DXF 公設實值` | 只吃 `fake_st` 之率分項；**財務負擔率直接決定 A/B/C ⇒ 土地後果最近**。⚠️ 須先裁中間層是否納入 |
| **3** | 🔵 `v3 重劃總負擔率錨`／`reverse-test③` | 同為率之守備；後者證「率會隨動、非寫死」 |
| **4** | 🔵 `F.2 跨區段 fixture`／`F.4 模式二 p_avg` | 跨區段 `a′` 之比例守備；需 `build_parcels` |
| **5** | 🟡 `W-D.4 MinA_區 reverse-test` | **須先查明 `_w4` 之來源**（本批未做） |

⚠️ 序 2〜4 之前置 ＝ **中間層判準之裁定**（§3-1）；⛔ 未裁不得開工。

---

## §5 16 個 v3 基準之溯源表（乙組）

**產物**：`verify/out/WG911_基準溯源表.log`（工具：`verify/tools/wg911_baseline_trace.py`）。

### 5-1 ④ 之三項射程（⛔ 逐項）

- **【目錄射程】** 生產側**一層直接參與者** 10 項：`app.py`／`verify/stepg_pipeline.py`／
  `verify/selection_pipeline.py`／`verify/run_verification.py`／`verify/wd3_fragment_geom.py`／
  `verify/wd4_tier_list.py`／`verify/app_harvest.py`／`verify/case_params_UC9898.json`／
  `data/V6.dxf`／`data/地籍資料來源_匿名版.xlsx`。
  🔴 **⛔ 未做 import 遞移閉包**——具名聲明，⛔ 不宣稱完備。
- **【字樣射程】** `git log <烘焙commit>..HEAD -- <上列路徑>`（⛔ **非內容比對**·僅 commit 觸及）。
- **【連接方式】** 逐檔獨立查詢（⛔ 無距離窗）。

### 5-2 結果

| 烘焙 | 檔數 | 自烘焙以來觸及生產側之 commit 數 | ⑤「過期」為活假說 |
|---|---|---|---|
| `2466faf`（2026-07-25） | **14** | **72** | 🔴 **是** |
| `6830642`（2026-07-10） | **2**（`W-D.1.2 診斷_退縮{0m,3.5m}`） | **117** | 🔴 **是** |

⇒ **`M4` 成立**（16 檔皆可定烘焙 commit／日期）；
⇒ **`M5` 成立**——且**不是「至少一個」，而是<u>全部 16 個</u>**：
**「基準過期」對每一檔皆為活假說。**

### 5-3 ② 逐檔消費者（AST 現查·⛔ 非由檔名推想）

**查無消費者之檔 ＝ 0**。
⚠️ **一項自誌**：本工具**首版只做字面比對** ⇒ 14 個以 **f-string 命名**之基準
（`f"…{tag}.csv"`）被誤報「**查無消費者**」。已改為**樣式比對**（`{…}` → 萬用）後歸零。
⇒ 若當時出艙，會得到一個「**14 檔無人使用**」之**完全錯誤**結論。

---

## §6 「基準過期 vs 算錯」之判準預登（⛔ 標「現在就能取得」者本批**已取**）

| # | 證據類型 | 何時可取 | 本批狀態 |
|---|---|---|---|
| 1 | **烘焙 commit ／ 日期** | 現在 | ✅ **已取**（§5-2） |
| 2 | **自烘焙以來觸及生產側之 commit 清單** | 現在 | ✅ **已取**（每檔之 commit 數；清單可由該工具重跑列出） |
| 3 | **該基準之消費者（哪些主判定）** | 現在 | ✅ **已取**（§5-3·查無消費者 0） |
| 4 | **產生路徑之 import 遞移閉包** | 現在（但**成本較高**） | 🔴 **本批未取**——⛔ 具名列為射程外（§5-1） |
| 5 | **解阻後之實際差異逐格內容** | **須解阻後** | ⛔ 本批不取；取得方式＝另案修好後跑 `run_all`、取該主判定之 `diff_rows` 違規列 |
| 6 | **差異之方向性**（是否與已知幾何變更同向） | **須解阻後** | ⛔ 同上；⚠️ 判別要點 ＝ 與 `2466faf`〜`HEAD` 之幾何相關 commit 對位 |
| 7 | **重烤後是否收斂**（重烤 vs 不重烤之二次對拍） | **須解阻後** | ⛔ 同上 |

🔒 **判準之要旨**（供解阻當天使用）：
- 若差異**可對位**到 ②／⑥ 之已知生產側變更 ⇒ 傾向「**基準過期**」⇒ 重烤。
- 若差異**無法對位**任何生產側變更 ⇒ 傾向「**算錯**」⇒ ⛔ **不得重烤**，須查因。
- ⚠️ **二者皆須 KL 裁**——⛔ CC 不自行判定重烤與否。

---

## §7 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未立任何閘、未掛任何東西入 `run_all`**（`M6` 之全檔非路徑差異為空即其證）。
2. ⛔ **未重烤任何基準**（`git diff --numstat -- verify/baselines/` **空輸出**）。
3. ⛔ **未修任何既有 FAIL**、⛔ 未解除明細截斷。
4. ⛔ **未動** `app.py`／`verify/run_verification.py`／`verify/wv_reconcile.py`／
   既有三支夾具／三支端到端複本（皆 `git diff --numstat` **空輸出**）。
5. ⛔ **未做**基準產生路徑之 **import 遞移閉包**（§5-1 具名列為射程外）。
6. ⛔ **未查明** `W-D.4 MinA_區 reverse-test` 之 `_w4` 來源（§3 #9·具名列為**未定**）。
7. ⛔ **未擅自擴充**「只被順序擋住」之判準以納入中間層（§3-1·待裁）。
8. ⛔ 未追改歷史記錄之舊行號錨與舊雜湊。

---

## §8 後批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **0** | 🛑 **兩項待裁**：① `W-G.9-9` 之 `K5` 判準（「未新增」取代「歸零」）② 本批之**中間層判準**（§3-1） | 皆為**判準**問題，⛔ 非 CC 可自裁；不裁則後批之分類與驗收皆懸空 |
| **1** | **立 `F.2 靜態閘`／`F.4 靜態閘` 之閘** | 與已立之 `F.3 靜態閘` 同型同構、成本近零、確定性最高 |
| **2** | **`W-G G.1` 之上游 `_ctx["0m"]`／結構閘另案** | **一修即解 65＋ 項之阻塞**；且三支端到端複本會跑更遠 ⇒ 其夾具依設計轉紅 ⇒ 屆時一併更新凍存 |
| **3** | **中間層 6 項之立閘**（裁定納入後） | 其中率分項／DXF 公設實值**土地後果最近**（率直接決定 A/B/C） |
| **4** | **查明 `_w4` 來源並歸類** `W-D.4 MinA_區 reverse-test` | 本批未定之唯一項 |
| **5** | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」 |
| **6** | **解除明細截斷（12 列上限）** | 必使現行名單全面失效、須重凍 ⇒ 與序 2 之重烤時點協調 |
| **7** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

⚠️ 序 2／6 之前置含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §9 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ python verify/tools/wg911_all_dissect.py --verify-g3 | tail -2
  ✅ **M2 成立**：泛化工具逐項重現舊工具之結論（try 範圍／主判定集合／except 歸屬）
  🔒 判別力自檢：對 `W-D.3 碎片` 之集合人造一筆 ⇒ ✅ 會判不符

$ python verify/tools/wg911_all_dissect.py | grep "22 名目合計"
⇒ 22 名目合計：主判定 **85**｜未執行 **80**｜只被順序擋住 3｜需CAD管線 0｜需人判 12｜真需管線 65

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG910_runall.log)        <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG911_runall.log); echo rc=$?
rc=0
$ diff 全檔 | grep "^[<>]" | grep -v worktrees   ⇒ （空·本批未掛任何東西）
$ grep -c '✅ PASS' verify/out/WG911_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG911_runall.log ⇒ 22

$ git diff --numstat -- app.py verify/run_verification.py verify/wv_reconcile.py       verify/fixture_*.py verify/wg_g*.py verify/baselines/   ⇒ （空輸出）
```

⚠️ **依倉規（不可變文件不記可變值），本報告⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**

### 9-1 🔴 四項自誌（⛔ 皆當場更正·皆為「壞掉的檢查器」族）

| # | 症狀 | 若未察覺之後果 |
|---|---|---|
| 1 | 分類器把 `results.append` **自身**算進呼叫 ⇒ **全部**落「需人判」 | 分類器完全失能、無任何結論 |
| 2 | 共用 `try` 之名目**重複計數** ⇒ 報 90／85 | 出艙一個**大於母體（80）**之數 |
| 3 | 🔴 `_vgN=[]` 後之 `.append` 變異未入相依 ⇒ `W-G G.1` 判為「**可回收**」 | **偽陽性·危險方向**——會據以對一個實需管線之判定立閘 |
| 4 | 溯源工具**只做字面比對** ⇒ 14 個 f-string 命名之基準報「查無消費者」 | 得出「**14 檔無人使用**」之完全錯誤結論 |
| 5 | 報告草稿之「中間層 **5**」與其所列 **6** 項不符 | 出艙一個與自身清單矛盾之計數 |

🔒 **共通**：①②④⑤ 皆由**與另一組數對拍**才發現（80 之交叉驗證／清單長度自對）；
③ 由**與既有結論校準**才發現。⇒ **考古 69 之修法「搬家必對拍」與「母體規模同時出艙」在本批各救一次。**

---

## §10 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `ead6a08` — 事前登記（⛔ 零量測、零碼變更）
- **B** `43a205b` — 泛化拆解器 ＋ 22 名目掃描 ＋ 16 檔溯源表 ＋ 長跑 log（⛔ 零文件結論）
- **C** 本報告 ＋ `GB` 登記
