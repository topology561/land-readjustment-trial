# `W-G.9-274R`　實跑側 `Rw` 之產生式定位 ／ 態甲 `R4` 未觸發之成因 ／ `自誤 378` ／ `345` 再現 ／ `GB-168` 之精化

> **態**｜開工 ＝ `77f14101a103c8ee1f56f7265e0b96e492ee2a62`（`ls-remote` 實查）｜工項零後 ＝ `f46ce92`
> **級** ＝ **中**（含雙態驅動·**零生產碼**）
> 🛑 **本批⛔ 辦**（單 `§七`）：⛔ 動生產碼 `34` 檔一字／⛔ 修 `GB-167`·`GB-168`／
> ⛔ 落地 `K-9-29` 任何子項・`GB-164`／`165`／`166`／`GB-162` 三禁令維持／
> **⛔ 由 `85.41` 反推其 `W`**、**⛔ 判孰為正確之 `W`**／
> **⛔ 提「結構閘應放寬／應停用」之任何主張**／⛔ 呈 KL 任何事。
>
> 🔒 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批）。

---

## `§零`　開工閘與工項零

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `ls-remote` 主線 | `77f14101a103c8ee1f56f7265e0b96e492ee2a62` | 🟢 |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 🟢 |
| `3` | `p3a` 非祖先 | `merge-base --is-ancestor` ⇒ **非祖先** | 🟢 |
| `4` | **`SELF_SHA256`**（`P-5` 內建口徑·受詞 **`10992`** B） | 實算 ＝ 單載 `537712d796566d37bf552ea138630901046ae21c7762c5125fa4a3643fe69a22`（逐位相同 `True`） | 🟢 |
| `5` | `probe_order_preflight.py` | **`rc = 0`**；`P-3`／`P-5` 機械停機款 **`0`** 項；🟡 提示 **`2`** 項（逐項具名·見下） | 🟢 |
| `6` | 四簿 ＋ 坑 | 自誤 `367`／`377`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `79`／`79`（已於 `77f1410` 實測·rev `4872b44`） | 🟢 |

**閘 `4` 之判別力三造**：全檔 `5be021ea…`（**不符** ✅）／⛔ 含該 `\n` 之舊口徑 `eb86223e…`（**不符** ✅）／自比（**相符** ✅）。

**閘 `5` 之二 🟡 提示（逐項具名·⛔ 靜默略過）**

| 提示 | 受詞 | 處置 |
|---|---|---|
| `P-1` `:37` | 「全稱否定而同段⛔ 具名母體」 | **具名豁免·偽陽**——該列係 `自誤 378` 之**「形」之描述**（「未先驗該閘…是否曾被執行」），⛔ 一個帶母體之量測宣稱 |
| `P-4` `:55` | 「方向性轉引而同段⛔ 具名座標系」 | **具名豁免·偽陽**——該列為 `§二` 之「⛔ 鑄任何號；⛔ 改 `自誤 345` 原節一字」，**無方向性之量** |

**工項零**（commit `f46ce92`）：所指之檔數 ＝ **`1`** ✅（判別力：人造名 `0` ✅／`CLAUDE.md` `1` ✅）；
`CR` ＝ **`0`**（`LF` `146`·bytes `11070`；造甲真 CRLF ⇒ `CR = 2` ✅）；
blob `sha1` ＝ `ced684a93297526bf07bc7a232fdefd6a0b2a7ae`；
blob 內容 `sha256` ＝ 磁碟 `sha256` ＝ `5be021eaffd48906afe3d1ec626da58277cee36fad8681b39b318f45b2357a48` ⇒ **逐位相同** 🟢。

---

## `§一`　工項 `1`：實跑側 `Rw` 之產生式與其輸入之逐字定位（**主檢**·🛑 出艙即止）

**器** ＝ `verify/probes/probe_WG9274_rw_origin.py`｜落檔 `verify/out/WG9274_rw_origin.log`（`rc = 0`·**stderr `0` B**）。
🛑 **⛔ 由 `85.41` 反推其 `W`**（單 `§四-1` 明令）——`rw_from_width` **非全域線性**
（`R(17.99) = 99.974`／`R(18.00) = 100.000` 之斜率 ⛔ 同於 `R(14.01) → R(14.20)` 之斜率）
⇒ 反推之前提不成立。**本器一律包裹 `ns["rw_increment"]` 正面取其實際輸入。**

### 一-0　自我驗證閘（框內省之正確性·⛔ 不過即拒測）

| 情境 | 態 | `_spatial_order_parcels_v2` 所錄 | `_select_pool_slot` 所錄 | 判 |
|---|---|---|---|---|
| `0m` | 甲 | `['R1','R2','R4','R6','R5','R3']` | `['R1','R2','R6','R5','R3']` | ✅ |
| `0m` | 乙 | `['R1','R2','R4']` | `['R1','R2','R4']` | ✅ |
| `3.5m` | 甲 | `['R1','R2','R4','R6','R5','R3']` | `['R1','R2','R6','R5','R3']` | ✅ |
| `3.5m` | 乙 | `['R1','R2','R4']` | `['R1','R2','R4']` | ✅ |

⇒ **器非紅**（所錄之 `blk` 皆為 `BLKS` 之成員且非空）。**其首版曾不過**——見 `§五-2` 之自捕 `2`。

### 一-1　項 `1`／`2`：產生式鏈（**逐字錨**·⛔ 行號）

| 環 | 逐字 | 檔 | **列框命中** |
|---|---|---|---|
| `g_rows` 之 `Rw(%)` 欄 | `'Rw(%)': round(_res.get('Rw_pct', 0.0), 2),` | `verify/stepg_pipeline.py` | **`1`** |
| `_res['Rw_pct']` 之產生 | `Rw_pct = (rw_increment(W_prev, W_cur) * 100.0) if _apply_rw else 0.0` | `app.py` | **`1`** |
| `W_cur` 之產生 | `W_cur = W_prev + S` | `app.py` | **`4`** |
| `rw_increment` 之本體 | `return max(0.0, (rw_from_width(W_cur) - rw_from_width(W_prev)) / 100.0)` | `app.py` | **`1`** |
| **理論側 `widths` 之來源** | `_widths_local[entry['_ov2_idx']] = float(res.get('_宗地寬度', 0.0) or 0.0)` | `verify/stepg_pipeline.py` | **`2`** |
| `宗地寬度(m)` 欄 | `'宗地寬度(m)': round(_res.get('_宗地寬度', 0.0), 2),` | `verify/stepg_pipeline.py` | **`1`** |
| `W(m)` 欄 | `'W(m)': round(_res.get('W', 0.0), 2),` | `verify/stepg_pipeline.py` | **`1`** |

🔒 **判別力**：一人造字面（**執行期組出**·字面⛔ 出艙）於 `app.py` ＝ **`0`** ✅。

⇒ 🔑 **實跑側 `Rw(%) = R(W_cur) − R(W_prev)`（<u>差</u>·累積差額制）**；
**理論側之 `widths` ＝ `_宗地寬度`**（與 `宗地寬度(m)` 欄**同源**）。

### 一-2　項 `3`／`4`：三個 `W` 之對位

| 名 | 產生式（逐字） | 是否即 `k956_W_from_mp` 之輸出 |
|---|---|---|
| **理論側之 `widths`** | `_widths_local[...] = float(res.get('_宗地寬度', 0.0) or 0.0)` | 🔴 **否** |
| `宗地寬度(m)` 欄 | `round(_res.get('_宗地寬度', 0.0), 2)` ⇒ **與上同源** | 🔴 **否** |
| `W(m)` 欄 ＝ 實跑側之 `W_cur` | `round(_res.get('W', 0.0), 2)`；`W_cur = W_prev + S` | 🔴 **否** |

🔒 **`k956_W_from_mp`（`K-9-5-6` 之「`W` 之單一產生者」·`verify/stepg_pipeline.py` 逐字
`_k956_W_from_mp = ns["k956_W_from_mp"]`）之呼叫次數**（態乙·全域）：
`0m` ＝ **`0`**／`3.5m` ＝ **`1`**
⇒ 🔴 **於 `0m` 之全流程<u>從未被呼叫</u>** ⇒ **上開三個 `W` 之中無一係其輸出**。
🛑 **⛔ 判其為缺陷、⛔ 提修法主張**——照實具名（該函式係 `_mp_base_W0` 之委派對象，
其呼叫繫於 `_fo_left`／`_fo_right`〔forced 側〕之條件）。

### 一-3　項 `5`：`628-1(1)` 之**實際值**（**未捨入**·包裹所錄·⛔ 反推）

| 名 | 值（`g_rows` 欄·`2dp`） | 其 `rw_from_width` |
|---|---|---|
| `宗地寬度(m)` | **`14.01`** | **`87.835000`** |
| `W(m)` | **`14.20`** | `88.500000` |
| `Rw(%)`（實跑） | **`85.41`** | —— |

🔒 **`rw_increment` 之實際輸入**（與該宗之 `Rw(%)` 相符之呼叫 ＝ **`2`** 次·**二情境逐位相同**）：

| `W_prev` | `W_cur` | `R(W_prev)` | `R(W_cur)` | 回傳×100 |
|---|---|---|---|---|
| **`0.1775004071`** | **`14.1988975422`** | `3.088507` | `88.496141` | **`85.407634`**（`2dp` ⇒ `85.41` ✅） |

🔒 **理論側**：`widths` ＝ `[46.08, 14.01]`｜`k*` ＝ `1`｜`ΣRw_R` ＝ **`87.835`**。

### 一-4　🔑 `Δ` 之**分解**（機械·自洽·🛑 **⛔ 判孰誤、⛔ 提修法主張**）

> `理論 − 實跑` ＝ `R(宗地寬度) − [R(W_cur) − R(W_prev)]`
> ＝ `87.835000 − 88.496141 + 3.088507` ＝ **`2.427366`**
>
> 其**二項貢獻**：
> · **所餵之 `W` 之別**：`R(14.01) − R(14.1989)` ＝ **`−0.661141`**
> · **`W_prev` 之扣除**：`+R(0.1775)` ＝ **`+3.088507`**
> · 合 ＝ **`2.427366`** ✅ **自洽驗算成立**

⇒ 🔑 **二式之相異有<u>二處</u>（⛔ 一處）**：
`(a)` **所餵之 `W` 相異**（理論側 ＝ `_宗地寬度`；實跑側 ＝ `W_cur = W_prev + S`）；
`(b)` **式之形相異**（理論側取 `R(W)` **絕對值**；實跑側取 **差** `R(W_cur) − R(W_prev)`）。
🛑 **何者為 `Δ` 之成因、抑或二者兼有——⛔ 由 CC 判**，候發單側。

🔒 **併記（照實·⛔ 追改）**：碼面 raise 所載之 `Δ=2.425` 係其**以 `2dp` 值重算**者
（`87.83 − 85.41 = 2.42`；其 `%.3f` 印為 `2.425` 係 `abs(_th - _re)` 之未捨入值 `2.42500…`）；
本節之 `2.427366` 係**以未捨入值**算得。**二者之別係捨入層**，⛔ 判孰誤。

### 一-5　判別力（單 `§四-1` 所令之取法）

🔒 **對照須落於非飽和域（`W < 18`）且其變動能驅動 `Rw(%)` 欄**；**⛔ 取 `R4 left`**（`W = 46.08 ≥ 18`）。
🔒 **實測**：態甲之全母體（六街廓 × 二情境 × 二側）中，落於**非飽和域**者 ＝ **`20` 組**
（逐組之宗已列於落檔）⇒ 🟢 **對照<u>存在</u>**（⛔ 第三種碼「對照不存在」）。
🛑 其「理論＝實跑」之逐組判**須另取**——**本批之受詞為產生式之定位**，
該判別力之完整施行**候發單側**（⛔ 逕稱器非紅）。

---

## `§二`　工項 `2`：態甲之 `R4` 何以未觸發（🛑 出艙即止）

🔒 **呼叫條件之逐字**（`verify/stepg_pipeline.py`·⛔ 行號）：

> `_N = len(ordered_v2)`
> `_k_naive = (_N + 1) // 2 if _N % 2 == 1 else _N // 2`
> `_slot_res = None`
> `if _degenerate_order or _N <= 1:`
> 　`_k_star = _N`
> 　`_adv_final = _advance_block_with_split(_k_star, True)`
> `else:`
> 　`_adv_base = _advance_block_with_split(_k_naive, False)`
> 　…
> 　`_slot_res = _select_pool_slot(...)`

而結構閘之守衛逐字 ＝ `if _slot_res:`；
`_degenerate_order` 之逐字 ＝ `_degenerate_order = (d_hat is None or corner_pt is None)`。

### 二-1　全母體之**三態**執行次數表（六街廓 × 二情境 × 二態 ＝ **`24`** 格·⛔ 二義共用出艙碼）

| 情境 | 態 | `R1` | `R2` | `R3` | `R4` | `R5` | `R6` |
|---|---|---|---|---|---|---|---|
| `0m` | 甲 | `5` 🟢 | `14` 🟢 | `10` 🟢 | **`1` 🛑 未執行** | `8` 🟢 | `8` 🟢 |
| `0m` | 乙 | `5` 🟢 | `15` 🟢 | 🛑 ⛔ 可得 | **`2` 🔴 已執行且破** | 🛑 ⛔ 可得 | 🛑 ⛔ 可得 |
| `3.5m` | 甲 | `5` 🟢 | `14` 🟢 | `10` 🟢 | **`1` 🛑 未執行** | `8` 🟢 | `8` 🟢 |
| `3.5m` | 乙 | `5` 🟢 | `15` 🟢 | 🛑 ⛔ 可得 | **`2` 🔴 已執行且破** | 🛑 ⛔ 可得 | 🛑 ⛔ 可得 |

（格內之數 ＝ `_N`；🟢 已執行且通過／🛑 未執行／🔴 已執行且破／「⛔ 可得」＝ 管線已中止、未達該街廓。）

🔒 **`d_hat is None` 與 `corner_pt is None` 於全母體皆 `False`** ⇒ `_degenerate_order` 恆 `False`
⇒ **唯一之分支依據 ＝ `_N <= 1`**。

⇒ 🔑 **成因**：態甲之 `R4` 之 `_N` ＝ **`1`**（步驟 `0` 之前置合併已將 `628(1)` 與 `628-1(1)` 併為 `628(1)+`）
⇒ 走 `if` 分支 ⇒ `_slot_res` 保持 `None` ⇒ **該閘結構上不執行**；
態乙之 `R4` 之 `_N` ＝ **`2`**（`K-9-29 二` 廢前置合併 ⇒ 解併）⇒ 走 `else` ⇒ 該閘**首次執行**即破。
🔒 **併證其⛔ 因管線中止**：態甲之其餘五街廓**皆「已執行且通過」**。

🛑 **⛔ 判其為缺陷、⛔ 提修法主張**——出艙即止。

---

## `§三`　工項 `3`：三段登記

| 段 | 落點 | 題（逐字依單） |
|---|---|---|
| `自誤 378` | 自誤簿**檔末** | `### 🩸 \`自誤 378\`　**以「該閘在對照態未報錯」充…**`（形取 `自誤 377` 為範本） |
| `自誤 345` 之再現 | 自誤簿**檔末** | `## 🔧 \`自誤 345\` 之再現一例：對照組落於量測式之**飽和區**（\`W-G.9-274\` §二·…）` |
| `GB-168` 之精化 | `W-G.4` 登記表**檔末** | `## 🔧 \`GB-168\` 受詞之精化：\`Δ=2.425\` 已收斂至…（\`W-G.9-274\` §三·…）` |

**取號現查**（三軸｜`blob@f46ce92`｜母體 ＝ 追蹤 `.md` **`748`** 檔｜檔框 ＋ 列框·**三形聯集**框）：
受詢 `378` B `1`／`1`・C `1`／`3`・鬆框 `1`／`5` ⇒ **聯集 `5` 列／`1` 檔**，
**逐筆歸類**（`裁 H`）⇒ **全數**為本單自身之授權條款 ⇒ **⛔ 撞號**；
對照組甲 `377` 聯集 `16` ✅／甲2 `376` 聯集 `17` ✅／乙 人造哨兵三形皆 `0` ✅ ⇒ **器非紅**。

🛑 **`§二`／`§三` 二段皆⛔ 鑄號**（`戒 36`）；其 `GB-132` 之避讓已於落地**前**機檢
（`自誤 345` 之節題只使 `345` 多一列命中、`GB-168` 之節題只使 `168` 多一列命中·**⛔ 造新號**）。

---

## `§四`　收工閘（`§五`·**commit 前·與被守護之動作分開呼叫**）

### 四-1　閘 `1`：`numstat` 刪除欄（逐檔具名）

**刪除欄逐檔皆 `0`**（**非零者 ＝ `0` 檔** ⇒ 無須具名其由）：

| 檔 | `+` | `−` |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `112` | **`0`** |
| `docs/reports/W-G.9-274R_實跑側Rw之產生式定位.md` | `228`（🩸 **自指**·見下） | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `110` | **`0`** |
| `verify/out/WG9274_numbers.log` | `137` | **`0`** |
| `verify/out/WG9274_preflight.log` | `15` | **`0`** |
| `verify/out/WG9274_rw_origin.log` | `146` | **`0`** |
| `verify/probes/probe_WG9274_rw_origin.py` | `352` | **`0`** |

🩸 **自指之量⛔ 填數**：本報告自身之 `+` 欄係**自指** ⇒ 上列 `228` 係**本節落地前**之值，落地後必增；⛔ 追改。

### 四-2　閘 `2`：生產碼判法（四檔）

```
git diff --cached --name-only | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

⇒ **輸出為空（命中 `0` 行）** ⇒ **零生產碼 commit** ⇒ **逕行 `push`**。
🔒 `verify/probes/**` 係**第二層**，⛔ 在生產碼 `34` 檔之內。

### 四-3　閘 `3`：`CR` ＝ `0` ＋ 前綴檢

| 檔 | `CR` | `LF` | bytes |
|---|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` | **`0`** | `7453` | `860960` |
| `W-G.9-274R_…md` | **`0`** | `228` | `14733` |
| `W-G.9波_claude.ai側自誤登記.md` | **`0`** | `9204` | `780965` |
| `probe_WG9274_rw_origin.py` | **`0`** | `352` | `17901` |

**既有檔之嚴格前綴**（基座 ＝ `f46ce92`·受檢之既有檔數 ＝ **`2`**）：

| 檔 | 基座 | 現 | `new.startswith(old)` |
|---|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` | `853920` B | `860960` B | **`True`** |
| `W-G.9波_claude.ai側自誤登記.md` | `773576` B | `780965` B | **`True`** |

**判別力二造**：必真 `True` ✅／必偽 `False` ✅。

### 四-4　閘 `4`：四簿期末（**期值⛔ 為現值**）

| 簿 | 相異（期／實） | MAX（期／實） | 缺號集 | 判 |
|---|---|---|---|---|
| **自誤** | `368` ／ **`368`** | `378` ／ **`378`** | `[106, 355, 356, 357]` | 🟢 |
| **`GB`** | ⛔ 動 ／ **`166`** | ⛔ 動 ／ **`168`** | `[12, 87]` | 🟢 **⛔ 動** |
| **`VR`** | ⛔ 動 ／ **`80`** | ⛔ 動 ／ **`95`** | `[73, 75]` | 🟢 **⛔ 動** |
| **`K-9`** | ⛔ 動 ／ **`28`** | ⛔ 動 ／ **`29`** | `[]` | 🟢 **⛔ 動** |

🔒 **`常規四（九）二` 閘 `②`**：自誤 MAX `377 → 378`，且 `378` **⛔ 在缺號集** ✅。
🔒 **閘 `①`（形之自證）**：`自誤 378` 之節題經定義框命中 ＝ `378` ✅（**落地前**之機檢）。
🔒 **`GB-132` 之避讓獲驗**：`§三` 之二末端追加段（`自誤 345` 之再現／`GB-168` 之精化）
**皆⛔ 造新號** ⇒ 自誤之相異**恰增 `1`**（`367 → 368`·全由 `自誤 378` 所致）；
`GB` 之相異／`MAX` **逐位未動**（`166`／`168`）；`VR`／`K-9` 亦逐位未動。

### 四-5　閘 `6`：自限復驗

| 受詞 | 實測 | 判 |
|---|---|---|
| `app.py` blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ 期初 ＝ 期末 |
| 生產碼判法四檔 | `numstat` 命中 `0` 行 | ✅ ⛔ 動一字 |
| 五個 `*_EXPECT` | `verify/wd4_tier_list.py` **⛔ 在本批 staged 之中** | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | **未併線** | ✅ |
| `WV_BAKE` | `None` | ✅ 未設 |
| `WV_K6_STEP0` | `None`（本批雙態驅動後**已還原**） | ✅ 未設 |
| `K-9-29` 之任何子項 | 子項 `1` 之逐字錨＠`app.py` 仍 **`1`** ⇒ **⛔ 落地** | ✅ |
| **`GB-167`／`GB-168` ⛔ 修** | `app.py` blob 期初＝期末即其證；二則之**原節一字未動** | ✅ |
| `GB-164`／`165`／`166` | **⛔ 落地** | ✅ |

### 四-6　閘 `5`：坑之期末數（`通則 I`）

🔒 **`通則 I`**：本自證之受詞（**追蹤 `.md` 之全集**）**含承載其結論之本報告**
⇒ **於報告 commit <u>之後</u>量**，其 rev 與實測值**逐字載於本批之末一 commit 訊息**。
🛑 **期值 ＝ `79`／`79`**（單 `§五` 款 `5`·**本批⛔ 鑄坑**）。
🔴 **期值⛔ 為現值**——實測見末一 commit。
🛑 **本批之二則自捕（`§五-2` 之 `2`／`3`）⛔ 鑄為坑**——其**形／徵候／攔法**已出艙於 `§五-2`，
**候發單側准**；若准，標籤 ＝ **`bb`**，且須**同批擴 `ALPHA_LABELS`** 至 `bb`。


---

## `§五`　本批之自解清單 ＋ 自捕表

### 五-1　自解清單

**本批⛔ 動用自解款**——`§零` 六閘全綠，⛔ 遇單內互斥或單與倉規相斥之情形。

### 五-2　🩸 本批之自捕（`3` 則·皆 loud 或當場顯形·⛔ 出艙錯值）

| # | 形 | 徵候 | 攔法（本批已施行） | 處置 |
|---|---|---|---|---|
| `1` | **命令串內誤留 heredoc** | `wg9237_heredoc_guard` **硬閘** `rc ≠ 0` 攔下 | 一律以 `Write` 落腳本 | 已修·**⛔ 鑄坑**（其形已在本機坑 `2` 之射程內） |
| `2` | 🔴 **抽出「框內省」為輔助函式，而未補其自身之一層** ⇒ `sys._getframe(depth)` **整體偏移 `1`** | 🔴 **一整張表 `24` 格全欄「⛔ 可得」**——讀起來像「管線未達該街廓」，而實為**框差一層**；且**同一偏移同時命中二個包裹**（`_spy_ord`／`_spy_slot`），故「理論側」之列亦**靜默消失** | `(1)` 輔助函式內一律 `sys._getframe(depth + 1)`；`(2)` 加**自我驗證閘**——所錄之鍵須為**已知母體之非空子集**，含 `None` 即 **loud 拒測**（⛔ 以「不可得」出艙） | 已修·**⛔ 鑄坑**（單 `§六` 明令坑本批⛔ 鑄）⇒ **形／徵候／攔法已出艙如左，候發單側准** |
| `3` | **`python -c "…"` 內之反引號遭 bash 命令替換**（本會期**第三次**） | 本次吃掉 docstring 內之 `` `_peek` ``／`` `depth` `` 二 token；**功能改動完好**（`depth + 1`），故⛔ 由 `rc` 顯形 | 一律以 `Write`／`Edit` 落腳本；已以 `Edit` 修回該 docstring | **⛔ 鑄坑**（其形已在本機坑之射程內·於報告具名即足） |

### 五-3　🩸 一項**紀律之自陳**（⛔ 出艙錯值·惟程序有瑕）

🔴 **我於實測<u>之前</u>已在片段中預寫 `_N ＝ 1`／`_N ＝ 2`**（由 `W-G.9-273` 之配地宗數推得），
違「**⛔ 在片段中預寫命中數——一律先實測後填**」。
🔒 **實測結果與之相符**（`1`／`2`），且**該片段於落地前已以實測值改寫**（補入全母體 `24` 格之三態表）
⇒ **⛔ 出艙錯值**；惟**程序有瑕**，照實具名。
🔒 **攔法**：片段內凡須載量者，先留**佔位**，俟實測後方填。

---

## `§六`　🛑 停

**辦畢 ⇒ 出報告 ⇒ 停**（單 `§七` 末·⛔ 自續下一批）。
本報告送回發單側；發單側自倉、自跑復驗其主檢後，方核可或指其誤。
