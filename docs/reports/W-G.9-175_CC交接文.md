# 🧾 `W-G.9-175`　**CC 交接文**（新 session 接手用·`W-G.9-174 補正①` 收束時出）

**出文窗**：CC（context 逼近上限 ⇒ 換窗）
**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ 零登記（⛔ 不鑄任何號）

---

## ✅ `§零`　**最要緊之一件事**（⛔ 讀完再動任何東西·**常規九**）

🔒 **與 `W-G.9-170` 之交接時<u>相反</u>：本窗之全部成果<u>皆已 push</u>，⛔ 無任何未 push 之 commit。**

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart` | **`e2970c2fffad5b01b3afcaf6f8dc71bd51789cf9`** |
| 本地未 push 之 commit | **無**（`left-right` ＝ `0 0`·工作 clone 已刪） |
| **`app.py` blob** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`**（`22922` 列／`1352132` B／`sha16 7c290b6f55f83a24`） |

🛑 **接手者之第一判斷**（**常規九 三**：開工閘 ⛔ 不得以本檔所載之 commit 為錨）：
`git ls-remote origin refs/heads/wip/s1-endpart` ⇒
- ＝ **`e2970c2f…` 或其後裔** ⇒ 照 `§一` 正常開工。
- **其他值** ⇒ 🛑 **停機**上呈（本窗收束時⛔ 無未 push 之物，故任何落後皆非本窗所留）。

### 🛑 唯一之待辦交接（**⛔ 非 CC 可自辦**）

`C:\kl173`（KL 實跑用之獨立 clone·`HEAD` ＝ `a3c97aa`）**未能刪除**——
`rm -rf` 得 `Device or resource busy`（自該目錄啟動之 streamlit 仍佔用）。
🔒 **其「⛔ 不刪」之戒<u>已解除</u>**（`W-G.9-174 補正① §六` 之條件 ＝ 本批 push·已達成）。
⇒ **請 KL 關閉該視窗後自行執行 `rmdir /s /q C:\kl173`**；⛔ CC 不得強殺 KL 之行程。
⚠️ 該目錄之存在**⛔ 不影響**任何倉態或驗收；⛔ 不得因其未刪而阻塞下一波。

---

## `§一`　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（態 ＝ `e2970c2f`：**`751`** 列／**`69552`** B／`sha16 5c013ba334464def`）。
   🔴 **末端已增**：`常規六`〜`常規九` ＋ `常規四（七）(八)(九)` ＋ `（七）補款一`／`補款二`。
   🔒 **`CLAUDE.md` 在 `wg942` 之 `TARGETS` 內** ⇒ 一律**末端追加**、⛔ 不得就地改字。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （**`3632`** 列／**`258295`** B／`sha16 6a5cada2901a10e8`·`grep -c "^#"` ⇒ **`196`**）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**：`git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNNa` ⇒
   `cd` ⇒ `git config core.autocrlf false` ＋ `user.name diag` ／ `user.email diag@local`
   ⇒ **對拍 `HEAD` 全 `40` 碼與 `app.py` blob**。
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、⛔ 不得放 scratchpad；跑畢刪除。
   🔴 **若本批將做全文對拍或長跑**，clone 路徑須為 **`8` 字元**（`C:\wNNNa` 之形·`自誤 203` 之戒 `18`）。
4. 🛑 **長跑（`run_all`）一律於<u>另一個</u>拋棄式 clone**（`GB-94`：`run_all` 會改寫 `verify/out/**` 之
   **已追蹤** log ⇒ 於工作 clone 內跑會使「`verify/` 零動」之驗收格必不成立）；**入倉自未跑長跑之 clone**。

---

## `§二`　倉態與取號現查（態 ＝ `e2970c2f…`·CC 現查·量測時點 `2026-08-30`）

| 項 | 值 |
|---|---|
| `origin` tip ＝ 本地 tip | `e2970c2fffad5b01b3afcaf6f8dc71bd51789cf9` |
| **`app.py` blob** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`**（`22922` 列／`1352132` B／`sha16 7c290b6f55f83a24`） |
| `CLAUDE.md` | `751`／`69552`／`5c013ba334464def` |
| `K-6` 正典 | `3632`／`258295`／`6a5cada2901a10e8`（`196` 節標題） |

🔴 **`app.py` 於本波已<u>四</u>度變更**：`b4cc3083…` →（`-161R`）→ `8daca97f…` →（`-163R`）→
`f285b91c…` →（`-169R`）→ `60b550b7…` →（`補正① W-5` 純註解）→ **`68f7bfab…`**。
🛑 **`-169R` 係本波唯一<u>有輸出後果</u>之生產碼批**（`16` 格·**已於 app 路徑實測坐實**·見 `§三`）。

### 🔒 取號現查（**常規四（八）** 定義列式 ＋ **逐格三數框**·母體已扣量測器自身）

母體 ＝ **全倉 `.md`**（`rglob("*.md")`·排除 `.git`）⇒ **`452`** 檔；態 ＝ `e2970c2f…`。

| 類 | 定義列式（逐字） | 相異號 | **MAX** | 次號 | 錨定框（對照組） |
|---|---|---|---|---|---|
| `docs/reports` 之 `W-G.9-NNN` | 檔名 `^W-G\.9-(\d{1,4})` | `125` | **`174`** | **`175`** | `W-G.9-175` ＝ `0`／`0`／`0`（對照 `W-G.9-174` ＝ `16`／`17`／`4`） |
| `VR` | `` ^##\s*`VR-([0-9]{1,4})` `` | `68`（單檔）／`69`（全倉） | **`83`** | **`84`** | 缺號 `15` 個·⚠️ 單檔／全倉之差集 ＝ `{VR-075}`（落於 `K-6`）·MAX 二者皆 `83` |
| `GB` | 三框聯集（式表 `115`／式A `3`／式B `32`·母體 ＝ `W-G.4` 登記表**單檔**） | `118` | **`120`** | **`121`** | 缺號 `[12, 87]`（自檢 `120−2==118` True） |
| 自誤 | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `176` | **`211`** | **`212`** | 缺號數 `35`（自檢 `211−35==176` True） |
| 常規 | `^\*\*常規(.)`（**寬框**） | 命中 `12` 列 | **九** | — | 🔴 窄框 `[一-十]` **⛔ 不含「四」**（`U+56DB > U+5341`）⇒ 靜默漏抓 |

🛑 **取號之四戒**（⛔ 缺一不可）：
1. **定義列式**取 MAX（常規四（八））；🛑 裸框⛔ 不得用於取號。
2. **錨定框** `(?<![0-9A-Za-z_\-])<N>(?![0-9A-Za-z_])`（`VR-083 三` 之**收緊框**）查意指占用；
   🛑 **收緊⛔ 不免逐列人工判**——其仍命中小數（`205.27`）與冒號式列號（`:205`）。
3. **二形並取**：**形① `X N`**／**形② `` X `N` ``**（＝ **意指框**·⛔ 非裸數字之二寫法）。
4. 🔴 **量測器須自母體扣除自身**（`VR-083 二`）——payload 暫存檔／探針原始碼／其 log 皆含待鑄號之字面；
   未扣除時錨定框**必得非 `0`**（`W-G.9-173` 實測：母體 `452` 而非 `450`、`VR-083` 得 `1/1/1`）。

---

## `§三`　本波之位置（`-164` 〜 `-174`·**全數已 push**）

| 單 | commit | 要旨 |
|---|---|---|
| `-164` | `0e4b508` | CC 交接文 |
| `-165R` | `001f60a` | 🛑 停機：第四類正規化入倉·`GB-112` |
| `-166R` | `6b77b4a` | `VR-078` ＋ `GB-113` ＋ 🔓 `GB-105` 解除 ＋ 自誤 `199` |
| `-167R` | `a98a99f` | `VR-074` 前置量測 ＋ **預測 diff 集合**（`16` 格）＋ `GB-114` ＋ 自誤 `200` |
| `-168R` | `a3c97aa` | `k6step0` 補量 ＋ 期初錨 log ＋ `VR-079`／`GB-115`／自誤 `201` |
| 🔴 `-169R` | `6a4a0da` | 🛑 **`VR-074` 落地（唯一生產碼批）** ＋ `VR-080`／`GB-116`／自誤 `202`-`204` |
| `-170` | `b9cb3cf` | CC 交接文（前一份） |
| `-169 補正①` | `926a0de` | app↔harness 同構證明 ＋ `W-5` 註解補記（`app.py` `+7/-0` 純註解） |
| `-169 補正②` | `acd50eb` | 🔓 `ns[…]` 間接層計入同構（規則 ④）＋ **AST 零行為證** |
| `-169 收官` | `2e08a41` | `E-3`／`E-4` 就地加註（🛑 當時之 `E-3` 停機，後由 `VR-081` 解除） |
| `-171` | `fcad7fd` | 🔓 `E-3` 停機解除 ＋ `VR-081`／`GB-117`／自誤 `206`-`207` |
| `-172` | `0c88cc8` | 池面積 `+0.01` **未歸因**（量測器紅）＋ 步驟 0 二裁前置量測 ＋ `VR-082`／`GB-118`-`119`／自誤 `208` |
| `-173` | `d4f02fc` | 🛑 `G.3` 射程之停機 ＋ `R2` 池差判 (甲) ＋ 備妥 `C:\kl173` ＋ `VR-083`／自誤 `209` |
| ✅ `-174`＋`補正①` | **`e2970c2`** | 🔑 **`+0.01` 歸因完成** ＋ KL 三趟 UI 證據入倉 ＋ `GB-120`／自誤 `210`-`211` |

### 🔑 本波之**五條結論鏈**（下一單之全部前提）

**① `VR-074` 已落地且其 app 路徑後果<u>已實測坐實</u>**
ghost（`K-9-19 一` 三判準合取）⛔ 不入投影序母體。碼面 ＝ `_proj_pop_ghost3`（唯一定義處）／
`_proj_pop_key_guard`／`_proj_pop_filter` 之複合原子名／四處實參同步／`_WF_NS_NAMES` 補列。
🔒 **土地後果（KL 三趟 UI 實跑·`3.5m`·證據已入倉）**：
`R1` 宗數 `6→5`／Σ池 `1175.0918→1175.0963`（**`+0.0045`**）；`R4` 宗數 `3→2`／Σ池 `1228.3533→1228.3568`（**`+0.0035`**）；
`R2`／`R3`／`R6` **逐位相同**。顯示層之 `+0.01` 係**捨入邊界**所放大。
🔒 **配地結果零變動**——三份匯出 CSV **位元組全等**；唯一相異欄 ＝ `原位次(投影序)` 之 **`4` 格**
（`R1/628(5)` `6→5`／`R1/628-36(1)` `4→3`／`R1/628-37(1)` `3→2`／`R4/628-1(1)` `3→2`）
⇒ 與 `verify/out/WG9167_predicted_diff.json` **逐格相同**。
🔒 **機制**：ghost 之 `5.28 ㎡` **舊態即已**經 `K-9-5-7` 配餘流入抵費地（二態該列 `5.3835` 逐位相同）；
`L-4` 只使該空間**由「配餘」改歸「池帶」** ⇒ 面積幾乎不動。**方向係修正**（app 自此與 harness 之 `1175.096297168` 逐位相同）。

**② 三批之「零行為變更」結論之射程已被具名**（`VR-082`）
凡宣稱「零行為變更／後果僅為 X」者，須同格具名其**射程**（本批之驗收**看不見**什麼）＋ 至少一個射程外之具名量；
⛔ 不得以 harness 之綠涵蓋 app 路徑。

**③ `Δ +0.0045`／`+0.0035` 之處置 ＝ 波末重烤**（`GB-111`·KL 已裁時點「俟二閘落地後」）——本波⛔ 不重產。

**④ 步驟 0 `裁 A`／`裁 B` 之前置量測已全備、⛔ 未落地**
`裁 B`（命名依**面積最大**·平手回落投影序首員·**⛔ 不設容差**）⇒ **改名 `4` 群**：
`R5` `628-21(2)+`→`628-22(2)`／`R5` `628-45(1)+`→`628-20(2)`／`R6` `628(2)+`→`628-1(2)`／`R6` `628-23(1)+`→`628-21(1)`。
🔴 **`R3` 第三群 `628-47(1)+` 可判**（`246.50` vs `246.48`·差 **`0.02`**）⇒ **不改名**。
🛑 **脆弱性清冊（top-2 差 `< 1 ㎡`）＝ `2` 群**（差 `0.02`／`0.35`）——`幾何面積_m2` 係 **2dp**，`0.02` ＝ **兩個最小刻度**。
🔒 `裁 A` 新增欄宜採 **`_` 前綴**（`temp_parcels` `20` 鍵中僅 `3` 鍵流入 baseline 欄集；`_` 前綴之 `4` 鍵流入數 ＝ `0`；
⚠️ 惟 `合併組成`／`合併依據` 無前綴亦未流入 ⇒ `_` 係**充分之慣例、⛔ 非必要條件**）。
🔒 改名波及面：母體 ＝ 全倉 `1402` 檔；`628-45(1)+` 落於**六份全量 log 各 `7` 列** ＋ 二份 `viol_full` 各 `16` 列
⇒ 落地時**對拍靶與 log 須同批重產**。

**⑤ `_rank_by_tpid` 之 `float('inf')` 退路現況 `0` 宗**（結構性之由 ＝ `_candidates` 與 rank 同源於 `_all_in_blk`）
——🛑 落地時仍宜加 loud 斷言（守衛之覆蓋繫於構造）。

---

## 🛑 `§四`　下一單之受詞（由發單側落筆·**依優先序**）

1. 🔴 **`R2` 之 `0.1171 m` 未解**（本波唯一之真分歧）——`Σ(宗帶 s 寬)` app `58.3674` vs shim `58.2503`。
   **已知**：街廓 `s` 域 `[0.2677, 100.0274]` 二路徑**逐位相同**；第一池帶 `8.4283`／`308.1700` **逐位相同**；
   第二池帶 `s` 寬 `32.9640` vs `33.0811`、面積 `1466.0294` vs `1471.2568`（差 `5.2274`）；退化帶 `3` vs `2`。
   🔒 **分解已定**：`5.2274 ＝ 寬差之貢獻 5.2079 ＋ 深度差（`0.000590536`）之貢獻 0.0195`（**二個變因**）。
   **受詞 ＝ 該 `0.1171` 落於 `14` 宗之哪一宗，抑或係累積。**
2. 🛑 **`G.3` 射程之停機**（`W-G.9-173 W-1` 判 (ii)·後由 `自誤 210` 補列第三支 **(iii) baseline 刻意過期**）
   ——`R1`／`R4`／`R6` 之差已歸 (iii)；**`R2` 之差方為真受詞**（同上第 `1` 項）。
3. 🔒 **波末重烤**（baseline 九份 ＋ 凍存名單 ＋ `GB-111`／`GB-113`）——**須 KL 決時點**（已裁「俟二閘落地後」）。
4. 🔒 **待落地清單**（`CLAUDE.md` 之「待落地清單之更正二」）：`K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17`
   重排／遞補（**幾何影響批·須另開新窗**）→ `K-9-12` 矩形容納（**同批**）→ 步驟 0 `裁 A`／`裁 B` 落地
   → `K-6-A2` 剩餘六項 → `K-9-22` 落地 → `K-6-B` 段一〜段四 → `K-6 §四` 合併述詞置換 → 波末批 → 泛化波。

---

## `§五`　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104`（`_block_strip` 雙線⛔ 無交叉閘之 loud 斷言） | ⬜ 未辦 |
| `2` | `GB-107`（`wg960_z_selfcheck.py:12` 寫死絕對路徑·該 worktree 已不存在） | ⬜ 未修 |
| `3` | `GB-108`（`K-9-23-a` 八格表對「`σ` 由形心導出」⛔ 無判別力） | ⬜ 未辦 |
| `4` | `GB-110`（八格表對閘一之關係詞 `>` vs `>=` ⛔ 無判別力） | ⬜ 未解除 |
| `5` | `GB-111`（凍存期望 FAIL 名單已失效·**須 KL 決重產時點**） | ⬜ |
| `6` | `GB-112`（`_WF_NS_NAMES` 反向閘寄生於既已 FAIL 之 `try`·**死碼**） | ⬜ 未修 |
| `7` | `GB-113`（`traceback.format_exc()[-700:]` 定長字元尾切致偽差） | ⬜ 未修 |
| `8` | `GB-114`（`無串聯` 閘之 `skip_cols` 指向**不存在**之欄名 ⇒ 豁免失效） | ⬜ 未修 |
| `9` | `GB-115`（驗收通道之 `12` 列**顯示上限**·驗收力阻塞） | ⬜ 未修 |
| `10` | `GB-116`（同一正典述詞於倉內 `≥ 4` 處各自實作·⛔ 無單一定義處） | ⬜ 只登記 |
| `11` | `GB-117`（靜態層檔集⛔ 不含 `selection_pipeline.py` ⇒ **`16` 名** `ns[]` 消費只由動態層守護） | ⬜ 只登記 |
| `12` | `GB-118`（主幹遇錯即停 ⇒ 其後街廓之量**靜默缺席**·`R4`/`R5`/`R6` 之 `T2-DIAG` 一筆皆無） | ⬜ 只登記 |
| `13` | `GB-119`（`app.py` ⛔ 不顯示自身 commit ⇒ 人工實跑之產物無法自證其態） | ⬜ 只登記 |
| `14` | `GB-120`（所需之量已由既有輸出印出而三張單皆⛔ 未索取） | ⬜ 只登記 |
| `15` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `16` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `17` | `K-9-22` 落地／`VR-074` 之他層／`VR-075` 落地 | ⬜ |

---

## `§六`　🔑 本波所立之家法與陷阱（🛑 接手者必讀）

1. 🔴 **量測器須自母體扣除自身**（`VR-083 二`·**同形已三度**）——第三度更是出現在**受詞**上而非僅對照組：
   payload 暫存檔含待鑄號之字面 ⇒ 母體 `452`／`VR-083` 得 `1/1/1`；扣除後始得 `0`（母體 `450`）。
   🛑 **判準**：凡此類量測須附**人造不存在之受詞**作對照，其得數**必須為 `0`**；非 `0` 即**先判量測器紅**。
2. 🔴 **他方環境之識別符須標其來源管線**（`自誤 211` 之戒 `27`）——檔名經上傳管線被置換即**非逐字**
   （八份 CSV 之 `.`／空白／`：` 全被換成 `_`）。🛑 **⛔ 不得以識別符為唯一同一性判準**，
   須併附 `sha256`／位元組／首末列，並**以內容錨配對**。
   🩸 **實例**：同目錄之 `退3.5_ …` 一組係**另一趟**之產物，其 `W-B` 表 `sha` 竟**與本批同**
   ⇒ 🔒 **「另一趟」之判據是<u>另二份</u>相異、⛔ 非全組相異**；依檔名批次配對即會撈錯而無從察覺。
3. 🔴 **以 2dp 顯示值相除得「相同」⇒ ⛔ 不得斷「全由單一變因解釋」**——須以**未捨入**值算殘差。
   實例：`44.475` vs `44.475` 之「相同」，未捨入實為 `44.473650042` vs `44.474240578`，殘差 `0.019536`。
4. 🔴 **`stdout` 之量往往早就印出來了**（`GB-120`）——`[T2-DIAG]` 之未捨入 Σ池／逐池帶面積／`s` 寬
   於 KL 首二趟即已印出，而三張單卻各自提出「解 `GB-118`」「令 app 輸出中間量」等**須動碼**之方案。
   🛑 **凡令他方執行取量，出單當下須逐一列舉<u>全部</u>輸出管道**（UI 訊息區／`stdout`＋`stderr`／匯出檔／log 檔）。
5. 🔴 **跨態量測須用<u>該態自身之 `verify/`</u>**（`VR-083 一`）——新 harness 跑舊 `app.py` 必 `KeyError`
   ⇒ 用 `git worktree` 逐態各開一份。
6. 🔴 **硬寫 `BASE_REF` 之探針，其產物須改名入倉**（`VR-083 三`）——沿用舊名即**覆寫既有錨、使其成假錨**
   （實例：`probe_WG9168_viol_full.py` 未改一字而產物 json `61187`→`62795` B）。
7. 🔴 **判準之母體／規則未涵蓋「本情境<u>必然</u>存在之項」⇒ 判準恆不成立**（`自誤 203`／`205`／`207` 三度共形）
   ——三度之「必然項」依序為 **環境字串**（clone 路徑）／**制度層**（接線 `ns[…]`）／**自身之寫入**。
8. 🔴 **「必有一為真」之選項集須自問是否窮盡**（`自誤 210` 之戒 `26`）——特別是「**已由域權威裁定而刻意維持之現況**」
   極易被漏（其外觀與「失效」不可區分，唯裁定紀錄可辨）。出單前須現查**積欠表／已裁事項**。
9. 🔴 **讀 session／全域狀態之判斷，須先現查其<u>寫入點</u>之時序**——探針曾於 `run_step_g` **之前**讀
   `ss['f3_corner_winners']` 得 `None` 而險判為路徑差異；實則 `stepg:282-283` 係**函式內部**才種入。
10. 🔴 **列數之框須具名**（`VR-057`）——`splitlines()` vs `split("\n")` 於「檔尾有換行」時差恰 `1`。
11. 🔴 **`in` vs `==` 之子字串偽陽**／**欄名⛔ 不得以關鍵字猜測**（winner 欄真名 `【左/右】第1宗指配`）／
    **層疊「濾掉 `0` 列」⛔ 不等於失效**。
12. 🛑 **`shell` 之反引號與 `\\` 會吃字**——`python -c "…"` 內含反引號 ⇒ **命令替換**吞字；
    **引號式 heredoc 亦曾把 `\\1` 吃成 `\x01`**（本波實測·寫進 memory 之正規式被毀）。
    ⇒ **交付塊／正規式一律由檔案管線產生，或用 Write 工具落檔**（**常規七**）。
13. 🛑 **`常規七` 之硬界**：交付塊⛔ 不得手寫——本波曾因「`~2900` 列 `stdout` 無法手打轉錄」而**停機**，
    改請 KL **存檔**（⛔ 非重跑）⇒ 此係**正辦**，⛔ 不得自行寬免。

---

## `§七`　已知 harness 限制

- 🔴 **`run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止** ⇒ `wf_f0`〜`f4` **主路徑從不執行**。
- 🔴 **`app.py:main()` 內之敘述從不被 `run_all` 執行**（`CLAUDE.md` 逐字）
  ⇒ **app 路徑之池呼叫端 `app.py:20892` 在 `main()` 內**、被 `app_harvest._filter_module` 濾除
  ⇒ **shim 結構上到不了**；唯一可執行之驅動器 ＝ `stepg_pipeline.run_step_g`。
- 🔴 **`run_step_g` 於 `R2` 之「結構閘 telescoping 破」終止** ⇒ **`R4`／`R5`／`R6` 之池永不可達**
  （吞掉 `②-宗 圍堵閘` 之例外**無濟於事**·`GB-118`）。
- 🔒 **`run_all` 於基座即 `FAIL`**（**准紅碼**）：`RESULT: FAIL`／`EXIT=1`。
- ⚠️ `run_all` 會改寫 `verify/out/**` 之**已追蹤** log（`GB-94`·實測 `3` 檔）。
- 🔒 **KL 之 UI 係目前唯一可達 `R4`／`R6` 之儀器**；其 `[T2-DIAG]` 印出**未捨入**之全部池量。

---

## `§八`　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**：`wg9143-handoff-entry`（本波主線·已更新至 `-174`）／
`push-before-shipping-claim`（🔒 未 push 不得報收官）／`grep-multibyte-charclass-trap`（量測器靜默給錯數之諸面）／
`run-all-side-effects`／`harness-worktree-crlf-trap`／`app-vs-harness-stepg-divergence`／`wg9161-projpop-landed`。

---

## `§九`　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit
  （**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值、⛔ 不得改框／母體／容差使其相符。
- 🔒 **拋棄式 clone 跑畢整份刪除**並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push（① 零生產碼 ② `deletions = 0` 或既有檔純附加
  ③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠）。
  🛑 **惟施工單另令「須請示」者，以單為準**；🛑 **生產碼批⛔ 不得逕行**。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`
  （期望 `rc=0`·`stderr` `0` B·逐字「結論：✅ 全部成立」·受檢 **`77`** 筆·`嚴格前綴＝True` ×`77`）。
- 🔒 **新探針入倉前必過** `verify/run_all.py:73` 之「禁寫死本機絕對路徑」全倉機檢（命中須 `0`·附判別力對照）。
- 🔒 **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
