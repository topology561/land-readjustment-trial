# `W-G.9-270R`　三技術阻塞項之結清（`GB-163`／`GB-160`／`GB-162`）·執行報告

> **受詞 ＝ CC**｜**零生產碼批**（`app.py` 與 `verify/` 生產路徑一字未動·含註解）
> **期初態** ＝ `78dc3f955d9d463745535955ce3642a96b5c31de`（`git ls-remote` 實查）
> **單** ＝ `docs/orders/W-G.9-270_施工單_三阻塞項之結清.md`（工項零已原封入倉·`b2146e4`）
> 🛑 **本報告⛔ 載**：`K-9-12` 之任何修法主張／三戶補償**適用式**之任何主張／`GB-159` 解除之任何判。
> 🛑 **三項之結清裁歸發單側**——本報告**只出艙其答**，⛔ CC 自裁。

---

## `§零`　`§六` **開工即設之預設**之自證（先於 `S-1`）

單 `§六` 款 `2` 逐字令「出艙一則**自證**：本批之全部文字落檔皆由 `Write`／`Edit` 所為（**逐檔具名其工具**）」。

| # | 落檔 | 工具 | 備註 |
|---|---|---|---|
| `1` | `verify/probes/probe_WG9270_S1_occupancy.py` | **`Write`** | — |
| `2` | `verify/probes/probe_WG9270_pit_digest.py` | **`Write`** ＋ **`Edit`**（母體擴為 `docs/` 全樹） | — |
| `3` | `verify/probes/probe_WG9270_p2_gb160.py` | **`Write`** ＋ **`Edit`** × `2` | — |
| `4` | `verify/probes/probe_WG9270_p1_gb163.py` | **`Write`** ＋ **`Edit`** × `6` | — |
| `5` | `verify/probes/probe_WG9270_p3_spy.py` | **`Write`** | — |
| `6` | `verify/probes/probe_WG9270_p3_gb162.py` | **`Write`** ＋ **`Edit`** × `2` | 🗄️ 首版之檔名配對法作廢（見 `§五` 坑 `aq`）；其分析端已由 `7` 取代 |
| `7` | `verify/probes/probe_WG9270_p3_analyze.py` | **`Write`**（先落 scratchpad·長跑期間⛔ 動 `verify/` 下任何檔）| — |
| `8` | 本報告 `docs/reports/W-G.9-270R_三阻塞項.md` | **`Write`** ＋ **`Edit`** | — |
| `9` | 各 `commit message` | **`Write`** ＋ `git commit -F <檔>` | ⛔ 令 CJK／反引號穿過 `-m` 之引號層 |
| `10` | 工項零之檔案搬移 | **`cp` ＋ glob 展開** | 🔒 **⛔ 文字落檔**（逐位複製）；glob 之字面全為 ASCII ⇒ ⛔ 令 CJK 檔名穿過引號層 |

🔒 **⛔ 以 `python -c`／`sed`／`echo` 傳遞任何含反引號、全形字元或多行之文字**——全批零次。
🩸 **惟本批仍二度誤帶 heredoc**（由硬閘當場阻斷）⇒ **照實記於 `§五` 自捕 `1`**：
**宣告預設⛔ 等同已施行該預設**；其攔下者是**閘**，⛔ 是我。

---

## `§一`　開工閘 `S-1`〜`S-8`（逐條當場復現·各載母體與粒度框）

### `S-1`　號占用（**二形並取** ⋀ **逐筆歸類** ⋀ 二對照組）

**器** ＝ `verify/probes/probe_WG9270_S1_occupancy.py`｜落檔 `verify/out/WG9270R_S1.log`｜`rc = 0`（**直取**·⛔ 經管線）
**框（逐字）** ＝ 宣告框 `D1` 檔名／`D2` 列首 `#+` 之列／`D3` 自稱形（**嚴格式**·補款 `⑧`）；
錨定框 `(?<![0-9\-])<完整號>(?![0-9])`；**列框 ＝ `|D2 ∪ D3|`**（補款 `⑦` 之聯集）。
**二形** ＝ **裸形** `W-G.9-270` ⋀ **C形** `` `W-G.9-270` ``（`GB-158`：`B` 形於本倉⛔ 單獨採信）。

| 態 | 母體 | 形 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | 列框 | 檔框 |
|---|---|---|---|---|---|---|---|---|
| **期初** `78dc3f9` | 全 `docs/` **`699`** 檔（讀不到 `19`） | 裸形 | `0` | `0` | `0` | `0` | **`0`** | **`0`** |
| 〃 | 〃 | C形 | `0` | `0` | `0` | `0` | **`0`** | **`0`** |
| **現態** `b2146e4` | 全 `docs/` **`700`** 檔（讀不到 `19`） | 裸形 | `1` | `1` | `1` | `0` | `2` | `1` |
| 〃 | 〃 | C形 | `0` | `0` | `0` | `0` | `0` | `0` |

🔒 **期初六數全 `0`**，且母體 **`699` 檔／讀不到 `19`** 與**發單側所載逐位相符**。
🔒 **現態之 `2` 筆逐筆歸類**（`裁 H`：命中數本身⛔ 判準，係待歸類之材料）：

1. `docs/orders/W-G.9-270_施工單_三阻塞項之結清.md:1`（標題列）⇒ **本單自身之產物**
2. 同檔 `:134`（`§八` 工項零之敘述）⇒ **本單自身之產物**

⇒ **第三方 `0` 筆** ⇒ ⛔ 停機（`自解款` 白名單類 `1`：「占用者全為本單自身之產物」）。
🔒 **二對照組**：甲 `W-G.9-269` 列框 裸形 `58`／C形 `14`（期初）⇒ `≥1` ✅；
乙 一人造哨兵（**執行期組出**·字面⛔ 出艙·`GB-147`）⇒ 六數全 `0` ✅ ⇒ **器非紅**。

### `S-2`　`ls-remote` 實查 ＋ `left-right`

```
05a11bd665f6c2790ff821c9e3a1606651e345d4	refs/heads/verify/W-G.9-269-p3a
78dc3f955d9d463745535955ce3642a96b5c31de	refs/heads/wip/s1-endpart
```
🔒 二值與開場文**逐位相符**；`git rev-list --left-right --count HEAD...78dc3f9` ＝ **`0	0`** ✅
⇒ 本 worktree 之 `CLAUDE.md` 係 **wip 態**（`2944` 列·⛔ main 態之 `190` 列）。
🔒 **⛔ 採信本地追蹤 ref**（坑 `23`）。

### `S-3`　生產碼 `34` 檔 blob（**當場現查·全表**）

落檔 `verify/out/WG9270R_S3_prodcode.txt`｜母體 ＝ **正面列舉** `app.py` ＋ `verify/` 頂層 `*.py`（⛔ 全倉排除式）
| 量 | 值 |
|---|---|
| 檔數 | **`34`**（＝ 期 `34`）|
| `app.py` blob `sha1` | **`4379108a4856714078c410ff4e6291ecfdf6d2c1`** ＝ 單 `S-3` 所載期初 ✅ |
| `app.py` blob bytes | `1464014` |
| `git hash-object app.py`（工作區） | `4379108a…`（**與 blob 同** ⇒ `core.autocrlf` 有效值 `false` 之證） |

🔒 **hash 二式並報**（坑 `21`）：上為 **git blob `sha1`**，⛔ `sha256`。

### `S-4`　`run_verification` 期初實跑

🔒 **跑前 `rm -f verify/out/got_*`**（坑 `aj`：母體須可證）——清前 `18` 支 ⇒ 清後 **`0`** 支。
| 量 | 值 |
|---|---|
| `rc`（**直取**·⛔ 經管線·坑 `b`） | **`0`** |
| `stdout` | **`161064`** B（落檔 `verify/out/WG9270R_S4_runverif.out`） |
| `stderr` | **`0`** B |
| 起跑／收跑 | `15:16:07` ／ `15:20:45` |
| 名目集合 | **`64`** ＝ **`24` PASS ＋ `40` FAIL** |
| `got_*` | **`18`** 支（`mtime` 皆於起跑後·逐支列於 `§四`） |

🔒 **`PASS`／`FAIL` 之字面自碼面取**（坑 `2`／`ak-1`）：`run_verification.py:1497` 逐字
`print(f"  {'✅ PASS' if ok else '🔴 FAIL'}  {name}")` ⇒ 框 ＝ `✅ PASS`／`🔴 FAIL`。

### `S-5`　四簿取號量（**每簿具名其<u>各自</u>之框及其出處**·⛔ 冠單一框名）

**器** ＝ `verify/probes/wg9268_gate5_tally.py`（倉內既有）｜態 `b2146e4`｜粒度框 **列框**

| 簿 | 母體（**單檔**） | 框之出處（逐字） | 相異 | `MAX` | 缺號集 | 次號 |
|---|---|---|---|---|---|---|
| **自誤** | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `VR-091` 補款二 `①` ＋ **範圍框** | `356` | **`366`** | `[106, 355, 356, 357]` | **`367`** |
| **`GB`** | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `VR-093`（`GB-116` **三框聯集**·扣哨兵 `997`） | `161` | **`163`** | `[12, 87]` | `164` |
| **`VR`** | `docs/驗證裁定登記表.md` | `VR-092` 就地加註所引（扣哨兵 `999`） | `80` | **`95`** | `[73, 75]` | `96` |
| **`K-9`** | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `W-G.9-266` 工項四 `四-1` | `28` | **`29`** | `[]` | `30` |

🔒 靜態清單（已鑄而定義框結構上撈不到·⛔ 計缺號）：自誤 `1`–`6`／`VR` `1`–`13`／`K-9-1`。
🔒 **自誤 `MAX 366`／次號 `367`** 與單 `S-5` 所載**逐位相符**。

### `S-6`　硬閘活體生效（`--selftest` **⋀** 活體攔截實測）

| 器 | `--selftest` | 活體攔截 |
|---|---|---|
| `verify/tools/wg9237_heredoc_guard.py` | `rc=0`·三造皆 `OK`·判 `PASS` | 🔒 **本批 `2` 次當場阻斷**（`PreToolUse:Bash hook`）⇒ **已接線** |
| `verify/probes/probe_order_preflight.py` | `rc=0`·**四造判別力全數成立**（靶①②③ ＋ **反靶**） | 對本單實跑（見 `S-0b`） |

🔒 **自檢綠⛔ 蘊含 hook 已接線**——該器自身逐字如此聲明；其接線由**活體攔截實測**判定，本批**二度正面坐實**。
⚠️ 該 hook 之 `stderr` 於 `cp950` 主控台呈亂碼 ＝ **顯示層**之事，**⛔ 攔截失效**（`常規五` 之判：先分量測器紅抑或受詞紅）。

### `S-0b`　收單機檢（`probe_order_preflight.py` 對本單）

`rc = 0`（**`0` 紅**）｜🟡 提示 `1` 項，**逐項具名並處置**：
- `[P-5]` **本單⛔ 載 `SELF_SHA256` 列** ⇒ 該自證鏈不可用。
  🔒 **具名豁免**：其位由開場文所載之 `file sha256 d990f87c54ea5dee…` 與**三端對拍**承擔（已逐位相符·見 `§二`）。

### `S-7`　探針定位普查（🛑 **須先於任何實跑**——本批確於一切實跑之前）

**框** ＝ `worktrees\|REPO *=`（**BRE 交替**·`GB-34`）｜**母體以 `git ls-files -z` 取**（⛔ 八進位跳脫·`GB-88`）
| 量 | 值 |
|---|---|
| 母體 | 追蹤檔 `2035`／其中 `*.py` **`302`** |
| 命中 | **`221`** 列／**`196`** 檔 |
| 對照甲（須 `>0`）`import` | **`301`** 檔 ✅ |
| 對照乙（須 `=0`）執行期組出之哨兵 | **`0`** 檔 ✅ ⇒ **器非紅** |

🔒 **本批所用之五支既有器**逐支查其 `REPO` 之取得方式：
`probe_WG9269_pit_index.py`（⛔ `REPO`·以 `argv` 之 `rev` ＋ `git show` 取倉側 blob）／
`probe_WG9269_c4_gamma.py`（`argv[1]` 否則自 `__file__` 上溯）／
`probe_WG9266_V1prime_3groups.py`（由呼叫端給 `root`）／
`wg9268_gate5_tally.py`（`argv[1]` 之 `rev` ＋ `git show`）／
`wv_reconcile.py`（`os.path.dirname(HERE)`）
⇒ **皆⛔ 硬編他樹**。🔒 全倉唯一硬編絕對 worktree 路徑者 ＝ `probe_WG971_wrap.py:203`（**本批⛔ 跑**）。

### `S-8`　坑索引（`26` ＋ `41` ＝ **`67`／`67`**）

**器** ＝ `verify/probes/probe_WG9269_pit_index.py`（`PYTHONIOENCODING=utf-8`·`常規五`）｜`rc = 0`｜`stderr` `0` B
| 量 | 值 |
|---|---|
| 母體 | `b2146e4` 之追蹤 `.md` **`722`** 檔 |
| 數字系 `1`〜`26` | **`26`** 坑·**全數 ✅ 可解析** |
| 字母系 `a`〜`ao` | **`41`** 坑·**全數 ✅ 可解析** |
| 合計 | **`67`／`67`** ＝ 單所載之期 ✅ |
| `形／徵候／攔法` 三字樣齊備 | **`67`／`67`**（`✅✅✅`） |
| 判別力二造 | 造甲 坑 `n` 定義處 `6` ≥ `1` ✅／造乙 人造之坑 `0` ✅ ⇒ **器非紅** |

🔒 **逐坑覆述之施行（⛔ 猜、⛔ 略過）**：索引器**只證三字樣齊備**、⛔ 出艙其內容
⇒ 另立 `verify/probes/probe_WG9270_pit_digest.py`，依索引之 `檔:列` **逐處取其逐字**，
產 `verify/out/WG9270R_pit_digest.log`（**`55058` B／`452` 列**·`rc = 0`·**`67`／`67` 皆取得**）。
**全 `67` 坑之徵候與攔法已逐條讀畢**；其於本批之施行逐則見 `§五`。

---

## `§二`　工項零：本單原封入倉

| 端 | `sha256` |
|---|---|
| `1` 來源（主 checkout·未追蹤·`mtime 2026-09-12 15:07:58`） | `d990f87c54ea5deeecd6510db6aeecba67ea8d9085f770363ea43f4de807ed75` |
| `2` 落檔（本 worktree） | `d990f87c54ea5deeecd6510db6aeecba67ea8d9085f770363ea43f4de807ed75` |
| `3` **入倉 blob 之內容** | `d990f87c54ea5deeecd6510db6aeecba67ea8d9085f770363ea43f4de807ed75` |

🔒 **三端逐位相同**，且 ＝ 開場文所載之 `file sha256 d990f87c54ea5dee…`。
🔒 blob `sha1` ＝ `3898581231881149c3e80624e9bbbd571af7f145`／`12054` B ＝ 落檔 bytes（**hash 二式並報**·坑 `21`）。
🔒 **行尾**：來源 `CR=0`／`LF=147`；`core.autocrlf` **三層分列** ＝ system `true`／global **未設**／local **`false`**
⇒ **有效值 `false`** ⇒ ⛔ 行尾正規化 ⇒ **⛔ 動一字、⛔ 改行尾**。
🔒 `git status --porcelain` ＝ **空**（`0`）✅｜`numstat` ＝ **`147	0`**（`deletions = 0`·純新增檔）。
🔒 **生產碼判法**（`常規一` 補款）：`git diff --name-only 78dc3f9..HEAD | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'` ⇒ **命中 `0` 行** ⇒ **零生產碼 commit ⇒ 逕行 `push`**。

---

## `§三`　`p2`：`GB-160` 之逐側**處置判**（⛔ 逐側之修）

**器** ＝ `verify/probes/probe_WG9270_p2_gb160.py`｜落檔 `verify/out/WG9270R_p2.log`｜`rc = 0`｜`stderr` `0` B
🔒 判準來源 ＝ 倉內既有 `V1.shared_edge`／`V1.QK` **逐字**（⛔ 改一字·⛔ 另寫第二份定義）｜`QK` ＝ **`1e-06`**
🛑 **本批⛔ 調 `QK` 或任何容差**（放寬既有之閘 ＝ 黑名單·恆停機）。

### 三-1　`R5·left`：共用頂點 `0` 之成因 ⇒ 🟢 **`(乙)` 幾何之實**

母體 ＝ 記憶體側 `g_rows` **`59`** 列（情境 `3.5m`·⛔ 落檔·⛔ baseline）；`R5·left` 之宗 **`5`**（依 `累積S(m)` 序）：
`628-45(1)+`／`628-18(2)`／`628-7(2)`／`628-31(1)`／`628-32(1)`。受詞對 ＝ 序 `0` ─ 序 `1`（**相鄰**）。

| 宗 | 頂點集（`cut_coords` 逐字） |
|---|---|
| **A** `628-45(1)+`（`6` 點） | `(310466.201470, 2651817.150800)`／`(310436.597150, 2651844.024220)`／`(310436.254000, 2651849.012430)`／`(310439.284863, 2651852.467334)`／`(310471.798917, 2651822.952594)`／`(310466.201470, 2651817.150800)` |
| **B** `628-18(2)`（`5` 點） | `(310441.476572, 2651854.965680)`／`(310445.666945, 2651859.742319)`／`(310476.196379, 2651827.510596)`／`(310474.107186, 2651825.345132)`／`(310441.476572, 2651854.965680)` |

| 量 | 值 |
|---|---|
| **頂點↔頂點之最近點距** | **`3.323450107879 m`**（`A3 ↔ B0`） |
| 其 `|Δx|`／`|Δy|` | `2.191708938801` ／ `2.498345962260`（`shared_edge` 之判為**此二者各自** `< QK`） |
| **頂點↔對方<u>邊</u>之最近距** | `A→B` **`3.322971495193 m`** ／ `B→A` **`3.323450107879 m`** |
| 與 `QK` 之比 | **`3.32e+06` 倍** |

🔑 **判 ＝ 🟢 `(乙)` 幾何之實**——距為 `QK` 之 `3.32` 百萬倍，**⛔ 任何合理容差可及**；
且**頂點↔對方邊**之距亦為 `3.323 m`（⛔ 近 `0`）⇒ **⛔ 「頂點錯位而邊相接」**，係**二宗真不相接**。
🔒 **判別力二造**（證本距量測⛔ 恆大亦⛔ 恆小）：
造甲［已知共用頂點 `2` 之對 `628-18(2)` ─ `628-7(2)`］⇒ **`0.000000000000 m`**（`< QK`）✅；
造乙［受詞對］⇒ `3.323450107879 m`（`≥ QK`）✅ ⇒ **器非紅**。

### 三-2　`R6·right`：`628-53(1)` 🔴 **繫於 `K-9-12`**

🔑 **`K-9-12` 之欄 ＝ `驗_A幾何`**——機械定位（⛔ 推定）：
`app.py` `_lot_gate` 之段註逐字「`── A 幾何（K-9-12 矩形·可旋轉可平移）──`」，其實作為 `_A, _Ad = _k923_gate2(...)`；
而 `_k923_gate2` 之 docstring 逐字「**閘二（可建築）** ＝ `K-9-12`」。
🛑 **`驗_rejected` ⛔ 為受詞**——`app.py` 逐字 `_rejected = any(_v is False for _v in (_A, _B, _C))`，
且 `app.py:9291` 逐字警示其「含 `K-9-12`（閘二·**射程外**）」⇒ **複合欄⛔ 單獨證繫屬**。

🔒 **坑 `ao` 法① 之施行**——該欄之**全體值分布**（母體 ＝ `got_G值_退縮3.5m.csv`·`50` 欄／`59` 列）：

| `驗_A幾何` | 列數 |
|---|---|
| **不合格** | **`20`** |
| 合格 | `19` |
| （空） | `14` |
| 不適用 | `6` |

⇒ 其**確具鑑別力**（⛔ 對全體標同值）。
🔒 **坑 `al` 之施行**（⛔ 以「檔在」推「欄在」）：`驗_A幾何` 非空 **`45`／`59`** 列（⛔ `0` ⇒ ⛔ loud 拒測）。

🔒 **受詞之逐欄值**（`628-53(1)`＠`R6·right`）：

| 欄 | 值 |
|---|---|
| `G(㎡)` | **`0.26`** |
| `宗地寬度(m)` | **`0.01`** |
| `街角地` | `否` ⇒ **走閘二**（街角地⛔ 走閘二·`K-9-12-e`） |
| **`驗_A幾何`** | 🔴 **`不合格`** |
| `驗_A_W`／`驗_A_D` | `3.5`／`14.0` ⇒ 需 **`49.0 ㎡`** |
| `驗_A_理由` | **`面積 < W×D（必要條件不成立）`** |
| （併陳·⛔ 受詞）`驗_總判`／`驗_rejected` | `剔除`／`True` |

🔑 **答**：`628-53(1)` 之 **`K-9-12` 閘二判為 `不合格`**，其理由為**面積必要條件不成立**（`0.26 ㎡` < `49.0 ㎡`）
⇒ 🔴 **繫於 `K-9-12`** ⇒ **矩形容納轉主動後，該宗離開配地母體**。
🔒 **坑 `ao` 法② 之施行**（正典之落地狀態·正面列舉）：`K-9-12` 於 `app.py` **`18`** 列／
`verify/stepg_pipeline.py`・`run_verification.py`・`selection_pipeline.py` **逐檔 `0`** 列；
`app.py:11932` 逐字「🆕 **K-9-12 矩形容納判定**（`W-G.9-187` 段 A·⛔ **純函式**·**觀測模式**）」
⇒ **現為觀測模式·未接生產**（對照組：`app.py` 之 `K-9-5` ＝ `66` 列 `>0`／執行期組出之哨兵 ＝ `0` ⇒ 器非紅）。
🛑 ⇒ 依單 `§三`：**該側本批⛔ 結清**，並登記於 `GB-160` 之末端追加。

### 三-3　`3` 側母體 `0` 者之標

🔒 承 `W-G.9-269R_c4` 之另診：`R1·left`／`R4·left` 各 `1` 宗、`R4·right` `0` 宗 ⇒ **母體 `0`**
⇒ **須維持「⛔ 靜默綠」之標**、**⛔ 計入「全過」**（單 `§三` 🛑 明令·本報告照辦）。

🔒 **`GB-160` 之結清或其解除條件之更新，裁歸發單側**；CC **只出艙上開二答**。

---

## `§四`　`p1`：`GB-163` —— `P-3` 判定集**擴至全部留存宗**

**器** ＝ `verify/probes/probe_WG9270_p1_gb163.py`｜落檔 `verify/out/WG9270R_p1.log`｜`rc = 0`｜`stderr` `0` B

### 四-1　量測之獨立性（單 `§二-1`）

🔒 **⛔ 自 `驗_B_臨正街`／`驗_B_臨屁股` 欄取**（該二欄正為 `—` 之來源）。
🔒 **幾何之真值自生產碼取**：`ns['_k923_touch_len']`（`app.py`·`#20` 單一真相源·⛔ 器內重導）。
🔒 **輸入以間諜法取自生產**：包裹 `ns['_lot_gate']`，逐次錄其實參之 `blk_ctx`
（`front_p1`／`front_p2`／`baseline_pts`），**⛔ 改生產碼一字**（包裹逕行委派並原樣回傳）。
🔑 **其線即第一關所用之線**（機械依據·⛔ 推定）：`app.py` `_blue_shadow_tri` 逐字
`_o = np.asarray(front_p1…)`／`_d = _k923_uhat(front_p2 − _o)`／`_bp = base_pt`／`_bd = _k923_uhat(base_dir)`，
而 `_lot_gate` 以 `blk_ctx['baseline_pts'][0]` ⋀ `[1]-[0]` 餵之 ⇒ **二線皆街廓級、⛔ 繫於街角**。
🔒 **街廓級之機械驗證**（⛔ 推定）：同街廓左右側之二線 `repr` 逐位比對——可比之左右對 **`5`** 組
⇒ **逐位相同 ✅**；判別力：對其一線注入 `+1.0` ⇒ **相異 ✅**（證該比對⛔ 恆真）。

### 四-2　**自我驗證閘（硬）**——單 `§二-2`

🔒 **容差之具名**：既有欄經 `app.py` 之 `_n6` ＝ `round(x, 6)` 落檔 ⇒ 比較端取 `round(mine, 6)`，
判準 ＝ **相等**（容差 **`0`**·**⛔ 放寬**）。

| 情境 | 判定集（第一關已評估者） | 逐位相同 | 判 |
|---|---|---|---|
| `0m` | **`6`** 宗 | **`6`／`6`** | ✅ |
| `3.5m` | **`5`** 宗 | **`5`／`5`** | ✅ |

🔒 其宗數 **`6`／`5`** 與 `GB-163` 所載之判定集**逐位相符** ⇒ **新器之量與第一關同源可比**。
（`0m` 逐宗：`628-35(2)`／`628-34(3)`／`628-40(1)+`／`628-53(1)`／`628-7(2)`／`628-42(2)`；
`3.5m` 逐宗：`628-34(3)`／`628-41(1)`／`628-53(1)`／`628-18(2)`／`628-42(2)`。）
🛑 **不過 ⇒ loud 拒測·停機上呈**——本批**未觸發**。

### 四-3　**判別力二造**——單 `§二-3`

| 情境 | 造甲［二者皆 `>0`·實宗］ | 造乙［同宗沿 FRONTLINE **法向**平移 `+1000 m`］ | 判 |
|---|---|---|---|
| `0m` | `628-37(1)`＠`R1·left` ⇒ 臨正街 `2.813767217`／臨屁股 `6.481421062` ⇒ 🟢 | ⇒ `0.000000000`／`0.000000000` ⇒ 🔴 | **異色** ✅ |
| `3.5m` | `628-36(1)`＠`R1·left` ⇒ `13.899098443`／`17.566427694` ⇒ 🟢 | ⇒ `0.000000000`／`0.000000000` ⇒ 🔴 | **異色** ✅ |

⇒ **器非紅**（⛔ 恆綠亦⛔ 恆紅）。🛑 二造同色 ⇒ 閘失能·停機——**未觸發**。

### 四-4　**三分類**與逐宗具名——單 `§二-4`

🔒 **母體 ＝ 留存宗 ＝ `to_csv_like(g_rows)`**（**靜默丟列 `0`** ⇒ ✅）。
🛑 **間諜所錄之 `91` 次呼叫⛔ 母體**——其含被 `K-9-17` 剔除者 ⋀ 二呼叫點之重複（**自捕 `3`**·`§五` 坑 `ap`）。

| 情境 | 留存宗 | 成立 | 不成立 | ⛔ 可量 | 和 | **判定集** |
|---|---|---|---|---|---|---|
| `0m` | **`56`** | **`55`** | **`1`** | **`0`** | `56` ✅ | **`56`／`56`** |
| `3.5m` | **`59`** | **`58`** | **`1`** | **`0`** | `59` ✅ | **`59`／`59`** |

🔑 **判定集已達<u>全部留存宗</u>**（`GB-163` 之受詞「判定集僅 `6`／`56`・`5`／`59`」**已補全**）；
**⛔ 可量 ＝ `0`** ⇒ 單 `§二` 之「⛔ 可量者非空 ⇒ 判 `GB-163` ⛔ 結清」一款**未觸發**。

🔒 **二類分列**（🛑 「抵費地是否落於 `P-3` 之受詞內」＝ **域問題**·⛔ CC 自裁）：

| 情境 | 類 | 母體 | 成立 | 不成立 | ⛔ 可量 |
|---|---|---|---|---|---|
| `0m` | **配地宗** | `43` | **`43`** | **`0`** | `0` |
| `0m` | 抵費地 | `13` | `12` | **`1`** | `0` |
| `3.5m` | **配地宗** | `45` | **`45`** | **`0`** | `0` |
| `3.5m` | 抵費地 | `14` | `13` | **`1`** | `0` |

🔒 **不成立者逐宗具名**（地號 ⋀ 歸戶·單 `§二-4` 明令）：

| 情境 | 街廓 | 側 | 暫編地號 | 歸戶 | 臨正街(m) | 臨屁股(m) |
|---|---|---|---|---|---|---|
| `0m` | `R1` | 抵費地 | **`R1-抵費地-2`** | ⛔ 可得（抵費地⛔ 在 `Gxxx` 清單內） | **`0.000000000`** | `0.002011201` |
| `3.5m` | `R1` | 抵費地 | **`R1-抵費地-3`** | ⛔ 可得（同上） | **`0.000000000`** | `0.002011201` |

🔒 **歸戶之取**（坑 `ai`：⛔ 憑字形猜鍵）：自**當次實跑**之 `got_W-D.4_清單_退縮{0m,3.5m}.csv` 之
`宗清單`→`歸戶鍵Gxxx` 建映射（`43`／`45` 筆·非空列 `33`／`33`）；抵費地**⛔ 在該映射內** ⇒ 一律印「⛔ 可得」（坑 `z`：⛔ 印 `0`）。

🔑 **本節之二項照實回報（⛔ 頂替）**
1. **每一<u>配地宗</u>皆臨二線且臨接長 `> 0`**（`43`／`43`・`45`／`45`）⇒ `P-3` 於配地宗**全稱成立**。
2. 🔴 **各情境另有 `1` 片<u>抵費地</u>之臨正街長 ＝ `0`**（`R1-抵費地-2`／`-3`）。
   🛑 **「抵費地是否為 `P-3` 所稱之『留存宗』」係<u>域問題</u>** ⇒ **⛔ CC 自裁**、**⛔ 發單側自裁**；
   本報告**只分列出艙**，其判候發單側（若涉土地後果則上呈 KL）。

🔒 **`GB-163` 之結清裁歸發單側**——單 `§二` 逐字「判定集達全部留存宗且不成立為 `0` 方得判其**可結清**
（**其結清之裁歸發單側**·⛔ CC 自裁）」。**本報告⛔ 作成該裁**：判定集已達全部留存宗 ✅，
而「不成立為 `0`」**繫於上開域問題之答**（僅配地宗 ⇒ `0`；含抵費地 ⇒ `1`）。

---

## `§五`　`p3`：`GB-162` 條件 `(a)` 逐格歸因 ＋ `(b)(c)` 所需之物

🛑 **三禁令維持·逐條出艙其證**
| 禁 | 證 |
|---|---|
| ⛔ 設 `WV_BAKE` 任何值 | 間諜器起跑前**顯式斷言** `os.environ.get("WV_BAKE")` ＝ `None` ⇒ 印 `✅ 未設`（設則 `rc=9` 即停） |
| ⛔ 覆寫 `verify/baselines` 任一檔 | `verify/baselines` 之 `.csv` 全樹**聚合 `sha256`**：期初 ＝ 期末 ＝ **`0cacf509c02db410376fe80bcd52165804b0859b5552894f37895b1617098f90`**；`git status --porcelain -- verify/baselines` ＝ **空** |
| ⛔ 併線 `p3a` | `git ls-remote` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（**未動**）；`git merge-base --is-ancestor 05a11bd HEAD` ⇒ **✅ 未併線** |

### 五-1　母體之取得（🛑 **⛔ 自 log 取**）

🩸 `run_verification.py:1497-1505` 逐字只印 `viol[:12]`，餘以「另 N 列未顯示·本閘違規總計 N 列」帶過
⇒ **逐格歸因⛔ 得自 log 取**（`CLAUDE.md` 之「引用違規列數須註明**顯示數抑或總計數**」即此族·**新坑 `ar`**）。
⇒ 另立 `verify/probes/probe_WG9270_p3_spy.py`，**以間諜法包裹 `rv.diff_rows`** 取其**全量**違規列。

🔒 **包裹之無害性（機械·⛔ 推定）**：其**逕行委派原函式並原樣回傳** ⇒
**名目集合與 `S-4` 逐位相同**（`diff` ＝ 空·`24 PASS`／`40 FAIL`）⇒ **對行為零影響**。
🔒 **母體可證（坑 `aj`）**：跑前 `rm -f verify/out/got_*`（清後 `0` 支）⇒ 收跑後 **`18`** 支，
其 `mtime` **晚於起跑者 ＝ `18`／`18`** ⇒ ✅。
🔒 間諜 `rc = 0`｜`diff_rows` 呼叫 **`25`** 次｜**違規列總計 `1656`**｜產物 `verify/out/WG9270R_p3_viol.tsv`（`273935` B）。

### 五-2　`label → 名目` 之映射 ＋ **自我驗證閘**

🔒 **機械依據**：`diff_rows` 之每一違規列皆冠 `[label]`（其實作逐字 `f"[{label}] …"`）
⇒ 自各名目**所印之前 `12` 列**取 `[label]` 建映射；
**自我驗證閘 ＝ 「該 `label` 之全量列數 ＝ 該名目所報之總計數」**。

| 量 | 值 |
|---|---|
| 可對拍之名目 | **`20`** 項 |
| 其中**逐位相同** | **`20`／`20`** ✅ ⇒ 映射可信 |
| ⛔ 可對拍者 | **`44`** 項（名目未被截斷 ⇒ 無總計數，或其印出之 `label` 非唯一）——其映射仍由 `[label]` 建立，**惟無計數之自證**（**照實·⛔ 頂替**） |

### 五-3　🔑 **逐格歸因之結果**

| 類 | 格數 |
|---|---|
| **`(i)` 已為現行紅項所涵蓋** | **`1656`** |
| **`(ii)` 未涵蓋** | **`0`** |
| 和 | `1656` ✅ ＝ 全量違規列數 |

⇒ 🟢 **全量違規列皆已為現行紅項所涵蓋**，`(ii)` 為**空** ⇒ ⛔ 有「逐格具名」之受詞。
**`23` 個 `label` 之逐個對應**（格數降冪·全數歸 `(i)`）：

| `label` | 格數 | 名目（其判皆 `🔴 FAIL`） |
|---|---|---|
| `v3·G值0m` | `235` | `v3·G值0m` |
| `v3·G值3.5m` | `234` | `v3·G值3.5m` |
| `v3·J表3.5m` | `139` | `v3·J表3.5m` |
| `v3·診斷3.5m` | `100` | `v3·診斷3.5m（全欄逐格·無豁免）` |
| `無串聯3.5m` | `100` | `率接線無串聯3.5m（vs v1 原錨，豁免 G估 後逐格全等）` |
| `v3·J表0m` | `96` | `v3·J表0m` |
| `v3·診斷0m` | `92` | `v3·診斷0m（全欄逐格·無豁免）` |
| `無串聯0m` | `92` | `率接線無串聯0m（…）` |
| `碎片幾何` | `84` | `W-D.3 碎片幾何/三分類（v3）` |
| `v3·滑池槽3.5m` | `80` | `v3·滑池槽3.5m` |
| `W-D.4清單0m` | `71` | `W-D.4 四梯清單0m（v3 半實算）` |
| `v3·滑池槽0m` | `65` | `v3·滑池槽0m` |
| `W-D.4清單3.5m` | `61` | `W-D.4 四梯清單3.5m（v3 半實算）` |
| `碎片逐邊` | `52` | `W-D.3 碎片逐邊CAD（v3）` |
| `W-D.4跨占0m` | `35` | `W-D.4 跨占分配線0m` |
| `W-D.4跨占3.5m` | `35` | `W-D.4 跨占分配線3.5m` |
| `指配0m` | `22` | `指配0m` |
| `指配3.5m` | `22` | `指配3.5m` |
| `參數0m` | `14` | `參數0m` |
| `參數3.5m` | `14` | `參數3.5m` |
| `W-D.4遞補3.5m` | `6` | `W-D.4 碎片遞補3.5m` |
| `抵費地3.5m` | `4` | `抵費地3.5m` |
| `W-D.4遞補0m` | `3` | `W-D.4 碎片遞補0m` |

🔒 **判別力（款 `3`：證該歸因法⛔ 恆歸一類）**：
造甲［已知必屬 `(i)` 之 `label` ＝ `v3·G值0m`］⇒ 歸 `(i)` ✅；
造乙［**人造之 `label`**·執行期組出·字面⛔ 出艙］⇒ 命中名目 `0` 個 ⇒ 歸 `(ii)` ✅
⇒ **器非紅**（二造異類）。

### 五-4　🛑 **與單所引之 `1137` 二數並報**（坑 `an`）

| 數 | 其框（二端） | 檔集 |
|---|---|---|
| **`1137`**（單／`GB-162` 所引） | **前態側烤產物**（`WV_BAKE` 所生）↔ `verify/baselines`·**檔對檔**之格差 | `GB-162` 逐字載「前態側烤集 **`11`** 檔 vs 現態 **`51`** 檔」 |
| **`1656`**（本窗當場復算） | `verify/baselines` ↔ **記憶體側 `got_rows`**·`diff_rows` 之**全量違規列** | `diff_rows` 實際比較之 **`23`** 支 baseline |

🔴 **二數之框相異**（側烤 vs 原始比對·檔集亦異）——本窗**⛔ 側烤**（`GB-162` 禁 `WV_BAKE`）
⇒ **結構上⛔ 復現 `1137` 之框**。🛑 **⛔ 以其一頂替其他**；亦**⛔ 讀為單之誤**——
單之數於其自身之框內為真。
### 五-4-1　🔴 **一個「二數相等而組成完全不同」之實例**（新坑 `ar` 之案由·**當場實測**）

自 log 取「本閘違規總計」逐閘相加：**`21`** 閘、Σ ＝ **`1656`**；
間諜所錄之 `diff_rows` 全量亦 ＝ **`1656`**。
⇒ 讀起來像「log 已足以承載全量」。🔴 **實測否證之**——二集合之**組成完全不同**：

| 差之來源 | 內容 | 列數 |
|---|---|---|
| log 有而間諜無 | `率接線 G估 欄變動3.5m 12 格（期 0·Y 波後 baseline 同源）` ——其**非 `diff_rows` 之閘**（`23` 個 `label` 中⛔ 其名） | **`13`** |
| 間諜有而 log 無 | 三個**未被截斷**（`≤12` 列）之 `diff_rows` 閘，其總計行**依碼面⛔ 印**：`W-D.4遞補0m` `3`／`抵費地3.5m` `4`／`W-D.4遞補3.5m` `6` | **`3+4+6 = 13`** |

🔑 **二側之差恰皆為 `13` ⇒ Σ 相等係<u>巧合</u>**（`1656 − 13 + 13 ＝ 1656`）。
🛑 ⇒ **二數相等⛔ 證明其框相同**；若逕以「Σ 相等」推「log 可代全量」，
即會把一個**非 `diff_rows`** 之閘算進格差、又漏掉三個真 `diff_rows` 之閘。
🔒 **本節之初稿曾寫「真 Σ ＝ `1643`」——該推論亦為偽**，已於**入倉前**由當場實測更正（自捕 `6`）。

### 五-5　條件 `(b)`／`(c)` **各需何物**（🔒 **⛔ 本批辦**·單 `§四-1` 末款）

#### `(b)`　「本分支之紅項降至『其餘皆為**已裁之准紅**』」

**器** ＝ `verify/wv_reconcile.py`（倉內既有）｜名單 ＝ `verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt`
（＝ `CLAUDE.md` 所指之現行凍存名單）｜`rc = **1**`（**對帳 FAIL**）

| 量 | 值 |
|---|---|
| 名目：**凍存** ／ **現況** | **`22`** ／ **`40`** |
| 凍存有而現況無 | **`7`** 項 |
| **現況有而凍存無** | **`25`** 項 |
| 類 `I`（正規化後相同·自動視為同） | **`0`** 項 |
| 類 `II`（**僅 traceback 行號**不同·⛔ 自動視為同） | **`4`** 項（`W-F F.1`／`F.2`／`F.3`／`F.4`·皆 `run_verification.py` 之 `main` 內位移 `+54` 列） |
| **類 `III`（訊息實質不同·紅）** | **`11`** 項 |

🔑 ⇒ **`(b)` 所需之物（逐項）**
1. **`25` 項「現況有而凍存無」**之新名目——逐項須經**裁為准紅**或**修復**；
2. **`11` 項類 `III`**（同名目而**根因整個換掉**）——逐項須重新裁其准紅性（`W-G.9-7` 之立法理由即此）；
3. **`4` 項類 `II`**——須**逐項具名確認**其係「上方插入行位移」抑或「`raise` 換了位置」（⛔ 由工具代判）；
4. **`7` 項「凍存有而現況無」**——其為**改善**，惟名單須據以更新；
5. ⇒ 綜之：**凍存名單須經 KL／發單側重新裁定並重凍**。
🛑 **本項之紅係<u>既存</u>、⛔ 本批所致**——凍存名單立於 `W-G.9-7`（`2026-08-11`），
其後 `K-9-29`／`GB-67` 病一等多批已落地；本批**零生產碼**、**零 baseline 變動**。

#### `(c)`　「重烤之受詞集、其效力判與其**不可逆性之處置**」

🔒 **重烤之受詞集 ＝ `diff_rows` 實際比較之 baseline 檔 ＝ `23` 支**（**正面列舉**·⛔ 全倉排除式；
**⛔ `verify/baselines` 之 `.csv` 全集 `266` 支**——此差即「重烤之射程」之要害）：

`verify/baselines/退縮{0m,3.5m}參數.csv`／`第 1 宗街角地指配結果_退縮{0m,3.5m}.csv`／
`W-D.1.2 診斷_退縮{0m,3.5m}.csv`／`W-D.1.3-d 驗收_退縮3.5m.csv`／
`v3/G 值計算結果_退縮{0m,3.5m}.csv`／`v3/W-D.1.2 診斷_退縮{0m,3.5m}.csv`／
`v3/W-D.2 §3 滑池槽診斷_退縮{0m,3.5m}.csv`／`v3/W-D.4_四梯分級清單_退縮{0m,3.5m}.csv`／
`v3/W-D.4_碎片遞補對照_退縮{0m,3.5m}.csv`／`v3/W-D.4_跨占分配線_退縮{0m,3.5m}.csv`／
`v3/wd3_fragment_edges.csv`／`v3/wd3_fragment_geom.csv`／`v3/逐槽 J 表_退縮{0m,3.5m}.csv`

🛑 **不可逆性之處置之候選形**（🔒 **⛔ 本批擇一**·候發單側）：

| 形 | 內容 | 凍存物之可達性 |
|---|---|---|
| **甲** | 先行凍存現行 `verify/baselines` **全樹**至具名之凍存目錄再覆寫（倉內既有此體例：`verify/baselines/PRE-S0b_S0c_幾何漏歸零_凍存/`） | 🟢 **自主線可達** |
| **乙** | **側支凍存**（體例 ＝ `verify/W-G.9-269-p3a`·⛔ 併線） | 🟡 須逐字附側支名與 `commit`（坑 `k`／`x`） |
| **丙** | 側烤至 `verify/out/<具名目錄>/` 後**只對拍不覆寫**（＝ `GB-162` 之現行狀態） | 🔴 其產物屬 `.gitignore` 之族 ⇒ **自倉觀之無推導紀錄**（坑 `26` 款 `2`） |

🛑 **`(b)`／`(c)` 本批⛔ 辦**——上開僅**出艙其各需何物**，供發單側另單（單 `§四-1` 逐字）。

### 五-6　`§四-2` 三戶表（**三項必載**）

🛑 **`GB-159` 之「⛔ 對外」不受 `§四-2` 之更正影響**——**下表⛔ 對外，僅得內部呈報**（單 `§四-2` 🛑 逐字）。
🔒 **`(2)` 時態錨**：`commit` ＝ `b2146e4d9a37bd02642f5ec849e9ec42b5a2d840`；
落檔 `verify/out/got_W-D.4_清單_退縮{0m,3.5m}.csv`／`mtime` **`2026-09-12 15:31:29`**／`6332`・`6417` B
（**本窗當次實跑所寫**·`mtime` 晚於起跑之證見 `五-1`）。
🔒 **`(1)` `估算性質` ＝ `半實算(試分配·非正式)`**（**⛔ 稱其為「最終補償數」**）——六格逐格自落檔取得。
🔒 **`(3)` 二式併陳·⛔ 擇一**。

| 歸戶 | 軌別 | 梯次 | 情境 | 增配 `a′(㎡)` | §52-1 差額地價(元) | §53-2 放棄改領(元) | §53-1 **本文式**(元) | **但書式**(元) | `估算性質` |
|---|---|---|---|---|---|---|---|---|---|
| **`G011`** | 建地軌 | `2` | `0m` | **`25.92`** | **`1976296`** | **`6516134`** | ⛔ 可得 | ⛔ 可得 | 半實算(試分配·非正式) |
| **`G011`** | 建地軌 | `2` | `3.5m` | **`21.15`** | **`1612602`** | **`6861642`** | ⛔ 可得 | ⛔ 可得 | 半實算(試分配·非正式) |
| **`G025`** | 建地軌 | `3` | `0m` | ⛔ 可得 | ⛔ 可得 | ⛔ 可得 | **`30193`** | **`16422`** | 半實算(試分配·非正式) |
| **`G025`** | 建地軌 | `3` | `3.5m` | ⛔ 可得 | ⛔ 可得 | ⛔ 可得 | **`30193`** | **`16422`** | 半實算(試分配·非正式) |
| **`G030`** | 建地軌 | `3` | `0m` | ⛔ 可得 | ⛔ 可得 | ⛔ 可得 | **`3381204`** | **`1742230`** | 半實算(試分配·非正式) |
| **`G030`** | 建地軌 | `3` | `3.5m` | ⛔ 可得 | ⛔ 可得 | ⛔ 可得 | **`6902853`** | **`3657639`** | 半實算(試分配·非正式) |

🔒 **「⛔ 可得」係<u>該欄於該情境為空</u>**（增配軌之戶無補償欄、補償軌之戶無增配欄）
⇒ **⛔ 印 `0`**（坑 `z`）。
🔒 **與單 `§四-2` 之表逐格對拍** ⇒ **六組數逐位相符**（`25.92`／`1,976,296`／`6,516,134`／
`21.15`／`1,612,602`／`6,861,642`／`30,193`／`16,422`／`3,381,204`／`1,742,230`／`6,902,853`／`3,657,639`）。
🛑 **其適用式之裁屬 KL**——CC **⛔ 主張**、發單側**⛔ 自裁**（單 `§四-2` 逐字）。

---

## `§六`　🩸 本批之自捕（照實·各載 形／徵候／攔法）

### 六-1　既有坑之重演（⛔ 新坑·逐則具名其既有標籤）

| # | 形 | 徵候 | 既有坑 | 攔法（已施行） |
|---|---|---|---|---|
| `1` | 🔴 **宣告 `§六` 預設<u>之後</u>仍用 heredoc**（**三度**） | `PreToolUse:Bash hook` **硬阻斷**（本批 **`3`** 次·⛔ `2` 次） | 坑 `g`／`7`／`ak-8` | 改以 `Write` 落檔、以**路徑**呼叫。🔒 **攔下者是閘、⛔ 是我**——本例正坐實單 `§六` 款 `3`「⛔ 待被咬後再援引」之所指：**宣告一個預設⛔ 等同已施行它**。🛑 **照實**：三次皆為**反射性**之 `python - <<'PY'`／`cat <<'EOF'`，⛔ 一次是「忘了有這條規則」——⇒ **該預設之施行⛔ 賴記憶，全賴該硬閘** |
| `2` | **CJK 檔名經 shell 引號層** | `sed: can't read <檔>`（二檔名被併為一串） | 坑 `g` 族／`GB-88` | 改 **glob 展開**（字面全 ASCII）或 **Python 側 `os.walk`** 定位 |
| `3` | **`grep -c` 回 `0` 時 `rc=1` ⇒ `&&` 鏈中斷** | 首次探查之 `A` 印出後**整串靜默結束**，`B`〜`F` **從未執行**——其徵候與「後續皆無輸出」相同 | 坑 `b`／`18` 族 | 改 `;` 分隔 ＋ `\|\| true`；**⛔ 以 `&&` 串接含 `grep -c` 之證據鏈** |
| `4` | 🔴 **判準之常數憑記憶**——`p3` 初稿之 `norm` 寫 `6dp`，而 `run_verification._norm` 逐字為 **`2dp`**（「能轉 float 就比到 2dp」） | 未對照則格差之數**必偏大**，而其輸出格式完好 | 坑 `2`／`ak-1` | **逕用生產碼之 `rv._norm`**（單一真相源·⛔ 另寫第二份定義）。🔒 **入倉前自捕** |
| `5` | `NameError: G4`（`p1` 首跑）／`TypeError`（`p3` 分析器之 `%` 格式位） | **loud** ⇒ ⛔ 靜默 | 坑 `f` 族 | loud 即其攔法；補 `import`／修格式位後復跑 |
| `6` | 🔴 **自「二 Σ 相等」推出一個未實測之結論**——初稿據此推「未截斷之閘其違規數為 `0`」，續推「真 Σ ＝ `1643`」 | **二推皆偽**，而其**讀起來完全自洽**（算術對得上） | 坑 `an`／`ak-5`（判準綁**計數代理**⛔ 其性質）之族 | **當場逐閘列出二側之成員並取差集** ⇒ 查得二側各差 `13` 而**恰相抵**（見 `五-4-1`）。🔒 **入倉前自捕並整段以實測值替換** ⇒ ⛔ 出艙 |

### 六-2　🆕 **新坑之提案**（`常規三`·**無土地後果**·CC 提案 ＋ 具名其由·🛑 **候發單側核可**）

🔒 **標籤之取**：既有字母系 MAX ＝ **`ao`**（`S-8` 索引器實查·`67`／`67`）⇒ 續取 **`ap`**〜**`ar`**
（⛔ 跳號·承「缺號永不重用」之精神）。
🛑 **坑之標籤⛔ 四簿之號**——本批鑄號之授權僅 **`自誤 367` 一號**（單 `§五`），坑之標籤⛔ 在其列。

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| **`ap`** | 🔴 **以量測器之<u>呼叫次數</u>充母體，而真母體為其<u>輸出</u>** | `p1` 首版以間諜所錄之 `_lot_gate` 呼叫（**`91`** 次）為母體，而留存宗實為 **`56`**（`g_rows`）⇒ 三分類之和 `91 ≠ 56`。🔴 **其危險在於自我驗證閘反而更好看**——判定集由 `6` 膨脹為 `18`（同一宗被二呼叫點各錄一次），而 **`18/18` 逐位相同**讀起來**比 `6/6` 更有力**，該 `12` 之增量卻**全是重複計數** | **母體須取自受詞之<u>定義</u>，⛔ 取自量測手段之副產物**。間諜法所錄者係「**誰被呼叫過**」，⛔ 「**誰留存**」——二者之差 ＝ 被剔除者 ＋ 重複呼叫。⇒ 凡以包裹／掛鉤取數者，須**另取一個獨立之母體定義**並**出艙二數之差及其成因** |
| **`aq`** | 🔴 **以<u>命名慣例</u>推二集合之配對關係** | `p3` 首版假定 `verify/out/got_<X>.csv` ↔ `verify/baselines/<X>.csv`，實測 **`266` 支 baseline 中配對成功 `0` 支**（`got_G值_退縮0m.csv` vs baseline `G 值計算結果_退縮0m.csv`）。🟢 **本次由坑 `al` 之攔法當場擋下**（母體 `0` ⇒ `rc=3` loud 拒測）——**若當初寫成「跳過不存在者」，將得格差 `0` 之假綠** | **配對關係須取自<u>碼面之實際呼叫</u>，⛔ 自命名推之**。此處之單一真相源 ＝ `diff_rows(got_rows, baseline_path, …)` 之**實參** ⇒ 改以**間諜法**錄之。🔒 併：母體 `0` 一律 **loud 拒測**（坑 `al`）——本坑之所以⛔ 成災，全賴該款 |
| **`ar`** | 🔴 **二數相等而<u>組成完全不同</u>**（「被截斷之顯示」與「全量」之混用） | `run_verification.py:1497-1505` 只印 `viol[:12]`。本批自 log 取「本閘違規總計」相加得 Σ ＝ **`1656`**（`21` 閘），**恰等於**間諜所錄之 `diff_rows` 全量 **`1656`** ⇒ 讀起來像「log 已足以承載全量」。🔴 **實測：二集合之組成完全不同**——log 多含一個**非 `diff_rows`** 之閘（`率接線 G估 欄變動3.5m` ＝ `13`），又漏掉三個**未被截斷**之 `diff_rows` 閘（`3`＋`4`＋`6` ＝ **`13`**）⇒ **其差恰相抵** | **凡以 log 之違規數為量者，須先問「這是<u>顯示數</u>抑或<u>總計數</u>」**（`CLAUDE.md` 既載此戒）；**全量須自 `diff_rows` 之<u>回傳</u>取**（間諜法），⛔ 自 log 重建。🔒 **併（本坑之要害）：二數相等⛔ 證明其框相同**——須**逐閘列出二側之成員並取差集**，⛔ 只比 Σ。⚠️ 本批之初稿據「Σ 相等」推出「真 Σ ＝ `1643`」，**該推論亦為偽**，由當場實測於**入倉前**更正 |

---

## `§七`　自限復驗

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| **生產碼 `34` 檔** | 期初 ＝ 期末 | `git status --porcelain -- app.py verify/` 之非 `??` 列 ＝ **`0`**；`app.py` blob `sha1` ＝ `4379108a…`（＝ 單所載期初·工作區 `hash-object` 同值） | ✅ **一字未動**（含註解） |
| **`verify/baselines` 樹** | ⛔ 覆寫任一檔 | 聚合 `sha256` 期初 ＝ 期末 ＝ `0cacf509c02db410376fe80bcd52165804b0859b5552894f37895b1617098f90`；`git status` 該路徑 ＝ **空** | ✅ |
| **`p3a` 側支** | ⛔ 併線·⛔ 刪除 | `ls-remote` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（**未動**）；`merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| **`WV_BAKE`** | ⛔ 設任何值 | 間諜器起跑前顯式斷言 ＝ `None` | ✅ |
| **四簿及其框** | 期初 ＝ 期末（僅 `自誤` `+1`） | 見 `§八-2` 之期末復查 | ✅ |
| **`deletions`** | 逐檔 `0`（純新增·`常規一` 補款 `②`） | `git diff --cached --numstat` 之刪除欄逐列 `0`；Σ ＝ **`0`**（`25` 檔） | ✅ |

🔒 **併記：二跑之 `got_*` 逐位相同（管線之決定性·⛔ 本批所令，惟其結果有用）**
本批共跑 `run_verification` **二次**（`S-4` ＋ `p3` 間諜跑），二次之 `got_*` **逐檔 `sha256` 全同（`18`／`18`）**
⇒ ① `p2` 所據之 `got_G值_退縮3.5m.csv`（`S-4` 態）與現態**逐位相同** ⇒ 其答**⛔ 因覆寫而失效**；
② 間諜之包裹**確對產物零影響**（與名目集合之 `diff` 為空**互為獨立之二證**）。
🔒 **判別力**（證該比對⛔ 恆同）：取二支相異之檔對照 ⇒ `e040af7e49cac62a…` vs `105df35a41cfdc67…` **相異** ✅。
⇒ 🔒 `S-4` 之凍存目錄 `verify/out/WG9270R_S4_got/` 經此證為**冗餘**，**⛔ 入倉**（⛔ 以 `git add -A` 掃入·`失敗考古節 49-1`）。

---

## `§八`　鑄 `自誤 367` ＋ `GB-160` 之末端追加

🔒 **本批得鑄者 ＝ `自誤 367` 一號；⛔ 逾此**（單 `§五`）。其**受詞與通則逐字承單 `§四-2`**，
落於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔尾**（append-only·`deletions = 0`）。
🔒 **`GB-160` 之末端追加**係單 `§三` 明令（「若是 ⇒ 判其**繫於 `K-9-12`**、本批⛔ 結清，
**並登記於 `GB-160` 之末端追加**」），落於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔尾**。
🛑 **二者皆⛔ 改任何既有一字**。

### 八-1　**收工閘**（`常規四（九）二`）——器 ＝ `verify/probes/probe_WG9270_closegate.py`·`rc = 0`

| 閘 | 判準 | 實測 | 判 |
|---|---|---|---|
| **①** 形之自證 | 以自誤之**定義列式**掃該簿，須命中新號 `367` | 命中；其標題形 ＝ `### 🩸 \`自誤 367\`　**立禁時，其受詞較該禁之<u>由</u>為廣**`（`:8667`·**取既有最末一則之形為範本**·`常規四（九）四`） | ✅ |
| **②** MAX 之推進 | 由 `366` 轉為 `367`，且 `367` ⛔ 在缺號集 | MAX ＝ **`367`**；缺號集 ＝ `[106, 355, 356, 357]`（MIN ＝ `7`·已扣靜態清單 `1`–`6`）；`367` **⛔ 在其中** | ✅ |

### 八-2　四簿期末復查（**逐簿具名其<u>各自</u>之框及其出處**·⛔ 冠單一框名）

| 簿 | 母體（單檔） | 框之出處 | 相異 | **MAX** | 缺號集 | 期初→期末 |
|---|---|---|---|---|---|---|
| **自誤** | `W-G.9波_claude.ai側自誤登記.md` | `VR-091` 補款二 `①` ＋ 範圍框 | `357` | **`367`** | `[106, 355, 356, 357]` | `366 → 367`（**＋1·本批唯一之鑄**） |
| **`GB`** | `W-G.4_泛用阻塞項登記表.md` | `VR-093`（`GB-116` 三框聯集·扣哨兵 `997`） | `161` | **`163`** | `[12, 87]` | **`163 → 163`**（⛔ 動） |
| **`VR`** | `docs/驗證裁定登記表.md` | `VR-092` 就地加註所引（扣哨兵 `999`） | `80` | **`95`** | `[73, 75]` | **`95 → 95`**（⛔ 動） |
| **`K-9`** | `docs/rulings/K-6_…md` | `W-G.9-266` 工項四 `四-1` | `28` | **`29`** | `[]` | **`29 → 29`**（⛔ 動） |

🔒 **⇒ 本批得鑄者僅 `自誤 367` 一號，實鑄亦僅此一號**（單 `§五`·⛔ 逾此）。
🔒 **`GB-160` 之末端追加⛔ 鑄號**——其節題為 `## 🔧 \`GB-160\` 之末端追加…`，
`#` 後夾 `🔧 ` ⇒ **`VR-093` 三框皆不命中** ⇒ `GB` 之 MAX／相異**逐位未動**（上表已坐實·`GB-132` 之避讓）。

---

## `§九`　🛑 本報告**⛔ 載**之事（單 `§七` 明令·逐條自檢）

| ⛔ 載之事 | 自檢 |
|---|---|
| `K-9-12` 之任何**修法主張** | ✅ **零**——`§三` 只出艙 `628-53(1)` 之 `驗_A幾何 ＝ 不合格` 與 `K-9-12` 之**現行落地狀態**（觀測模式），**⛔ 主張其應如何改** |
| 三戶補償**適用式**之任何主張 | ✅ **零**——`§五-6` **二式併陳**，逐格照錄，**⛔ 擇一**、**⛔ 評述** |
| `GB-159` **解除**之任何判 | ✅ **零**——`§五-6` 逐字重申其「⛔ 對外」仍有效；本批⛔ 觸其解除 |

---

## `§十`　🛑 **停**——候發單側復驗與三項之結清裁

單 `§八` 序 `5` 逐字：「出艙報告 ⇒ **停**，候發單側復驗與三項之結清裁」。
🔒 **三項之結清或其解除條件之更新，一律裁歸發單側**；**CC 只出艙其答，⛔ 自裁**。

**本批所出艙之三答（摘要·其判歸發單側）**

| 項 | CC 之答 |
|---|---|
| **`GB-163`** | 判定集**已補全至全部留存宗**（`56/56`・`59/59`·⛔ 可量 `0`）；**配地宗全數成立**（`43/43`・`45/45`）；**不成立各 `1`，皆為抵費地**（臨正街 ＝ `0`）。🛑 「抵費地是否為 `P-3` 之『留存宗』」＝ **域問題**，⛔ CC 自裁 |
| **`GB-160`** | `R6·right` 之 `628-53(1)` 🔴 **繫於 `K-9-12`**（閘二 `不合格`·理由「面積 < W×D」）⇒ **本批⛔ 結清**；`R5·left` 之共用頂點 `0` ＝ 🟢 **`(乙)` 幾何之實**（最近點距 `3.3234 m` ＝ `QK` 之 `3.32e6` 倍）。`3` 側母體 `0` 者**維持「⛔ 靜默綠」之標** |
| **`GB-162`** | 條件 `(a)` **已辦**：全量 `1656` 格**逐格歸因**，`(i)` `1656`／`(ii)` **`0`** ⇒ ⛔ 有未涵蓋者。`(b)`／`(c)` **⛔ 本批辦**，其**各需何物**已逐項出艙（`五-5`）。🛑 三禁令全程維持，逐條有證 |
