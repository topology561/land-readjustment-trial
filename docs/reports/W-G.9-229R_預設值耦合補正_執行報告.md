# W-G.9-229R　`:19720` 之預設值耦合補正（執行報告·🛑 **候 KL 逐字放行**）

**批次性質** ＝ **含生產碼**（`app.py` `1` 增／`1` 刪·**單列取代**·純重構）。
**基座** ＝ `2b78a6ae24742b32a393f485499f0de4671df06a`。
🛑 **本批⛔ 推入主線**——依單「完成【驗收】所列後停，候 KL 逐字放行方得推入主線」。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
payload 一律 `Write` 落檔、⛔ heredoc（`GB-143` 補款二）。

---

## 工項零　✅ PASS

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **4367** | 69 | **0** | `d9bb3675a9aad9722f21178b8247360b530d3601147e023ef88807f8a39e603d` |
| 入倉（索引側 blob） | **4367** | 69 | **0** | `d9bb3675a9aad9722f21178b8247360b530d3601147e023ef88807f8a39e603d` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）。對照組：翻首位元組 ⧧ blob ＝ `True`；blob 自比 ＝ `True`。

---

## 前置閘

### `G1`　態錨 ✅
`HEAD` ＝ `2b78a6ae24742b32a393f485499f0de4671df06a`；`left-right` ＝ **`0 0`**；追蹤 **1629** 檔／`.md` **557** 檔。
生產碼五檔之**改前** `sha256` 見下「閘 `P`」表之左半。

### `G2`　✅ PASS（依 `W-G.9-228` 之**標記法新框**）
母體 ＝ `app.py` **1417936** B／**23942** 列·**列框**。
標記列 ＝ **1** 個；最新者（**`git` 祖先序**·⛔ 位置／字典序）＝ `GB-144` 節之 `:3959`@`d2d3692`。
現態 **13** ＝ 新框 **13**；**雙向差集皆 `[]`**。

### `G3`　✅ PASS（二者現值相同）

| 端 | 檔列 | 逐字 |
|---|---|---|
| 顯示端常數 | `app.py:19686` | `_SB_DEFAULT_M = 3.5   # 顯示端預設；與下方輸入欄之預設**同值**（集中為具名常數列通用化波）` |
| 輸入欄之初值 | `app.py:19720` | `_setback_init = float(st.session_state.get('f3L_setback_default', 3.5))` |

🔒 二者現值 ＝ `3.5` ／ `3.5` ⇒ **相同 ＝ `True`**（不同即停——既有實害）。
🔒 二者**相距 34 列**、**無任何語法連結** ⇒ 本單所欲補正者。
🔧 **本閘之量測器當場更正**：首版以「含 `_setback_init` 且含 `=`」取，誤中 `value=_setback_init, step=0.1,`
（命中 `2`）⇒ 改**賦值列錨** `^\s*_setback_init\s*=` 後命中 `1`。

---

## 改動摘要（何處、幾行、為何）

| # | 位置 | 增／刪 | before → after | 為何 |
|---|---|---|---|---|
| `P1` | `app.py:19720`（**單列取代**）| **+1／−1** | `…get('f3L_setback_default', 3.5))` → `…get('f3L_setback_default', _SB_DEFAULT_M))` | 使**輸入欄之預設**改讀**顯示端所定之具名常數** ⇒ 二者**綁定**；⛔ 保留其字面 |

🔒 **hunk 標頭（機驗）** ＝ `@@ -19720 +19720 @@ def main():` ⇒ **同列取代**、⛔ 位移。
🔒 **`戒 40` 二端並報**：舊列 **92** B → 新列 **102** B；檔 Δ ＝ **+10** B、`LF` 列數 Δ ＝ **0**；
`Δ − (新 − 舊) = 0`。
🔒 `py_compile` 之 `rc` ＝ **0**。
🔒 **落地器之列錨**（承 `-219R2` 之陷阱）：`str.count` 係**子字串**比對 ⇒ 一律前綴換行；
本例去列錨之命中亦為 `1`（該列縮排最深）⇒ **二者同**、已並報。

---

## 工項一

### a　補正之範圍 ✅
**限於該二處**（`19686` 定義／`19720` 使用）；⛔ 順手擴及他處——見下 `d` 之「他處變動 ＝ `[]`」。

### b　🔒 作用域可及性 ＝ **實測坐實**（⛔ 以推理充證）✅

**證據形** ＝ `verify/probes/probe_WG9229_default_coupling.py`（`streamlit.testing.v1.AppTest`）。
🔒 **⛔ 鏡寫**——受測之四段碼**逐字自 `app.py` 抽出後執行**：
常設顯示區塊（`:19671`–`:19694`·**定義 `_SB_DEFAULT_M`**）／`on_change`（`:19708`–`:19719`）／
輸入欄（`:19720`–`:19727`·**使用 `_SB_DEFAULT_M`**）／下游 `_need`（`:14176`–`:14179`）與
`_wf_tag_of`（`:14271`–`:14277`）之定義原文。
探針之組裝**與 app 同構**：常數定義在 `with st.expander(...)` **之外**、使用在**其內**。

🛑 **實測結果 ＝ 可及**——三情境**皆有輸出且輸入欄取到常數值**；
**若 `with` 造出新作用域，`①` 即 `NameError` 而無任何輸出** ⇒ 本判係**實測**、⛔ 推理。

### c　🔴 `:22395`／`:22884` **本批⛔ 動**（理由）

| 項 | 逐字（當場復現）|
|---|---|
| `app.py:22395` | `_yd_setback = float(st.session_state.get('f3L_setback_default', 3.5))`（其後 `:22398` ＝ `elif abs(_yd_setback - 3.5) < 1e-9:`）|
| `app.py:22884` | `_p7_sb = float(st.session_state.get('f3L_setback_default', 3.5))`（其後 `:22887` ＝ `elif abs(_p7_sb - 3.5) < 1e-9:`）|

**何以⛔ 於本批併辦**
1. **位於不同區段、不同函式脈絡**——`22395` 在 **Y 波診斷 JSON 下載**區、`22884` 在 **Excel 匯出**區；
   二者與顯示端（步驟 L）**⛔ 同一渲染輪次之耦合**（本單之受詞係「同輪次內顯示與輸入之分歧」）。
2. 二處之 `3.5` **各出現二次**：一為 `.get` 之預設，一為 `abs(v − 3.5) < 1e-9` 之**情境判別分支**；
   只改前者而不改後者 ⇒ **半重構**（更壞）；二者皆改 ⇒ **逾越本單之「最小形」**。
3. `_SB_DEFAULT_M` 係**步驟 L 之區域名**，於該二處**不可及** ⇒ 併辦須先提升為**模組級常數**
   ⇒ **⛔ 純重構之最小形**。
🛑 ⇒ **列通用化波**（集中為**單一具名常數**之全面重構·與 `GB-144` 之「改為資料驅動」並排程）。

### d　`3.5` 字面之前後對照（三軸·**列框**）✅

母體：改前／改後皆 `app.py` **23942** 列（`1417936` → `1417946` B）·來源 ＝ 索引側 blob。

| 框 | 改前 | 改後 | Δ |
|---|---|---|---|
| 全檔列框（含 `3.5`）| **44** | **43** | **−1** |
| 退縮相關（`-227R` 之框）| **11** | **10** | **−1** |
| 🔴 **取值用之字面** | **4** | **3** | **−1** |

🔒 **取值用之字面**：改前 `[19686, 19720, 22395, 22884]` → 改後 `[19686, 22395, 22884]`。
🛑 **減少者 ＝ `[19720]`（恰 `1`）／他處變動 ＝ `[]`（空）** ⇒ 符工項一 `d` 之令。

---

## 工項二　自驗

### a　三情境實測 ✅（不符項 `0`）

| 情境 | 顯示之值 | 顯示之來源 | **輸入欄之值** | `E1` `:12677` 主路徑 | `E2` `:14259` 引擎接線 | `E3` `:23846` tag |
|---|---|---|---|---|---|---|
| ① 首次渲染（key 未存在） | `3.5` | **預設** | `3.5` | `3.5` | `3.5` | `3.5m` |
| ② 輸入 `0` | `0` | **使用者輸入** | `0.0` | `0.0` | `0.0` | `0m` |
| ③ 輸入 `2`（他值） | `2` | **使用者輸入** | `2.0` | `2.0` | `2.0` | 🔴 loud raise |

🔒 **三者一致**（顯示之值 ＝ 輸入欄之值 ＝ 下游逐入口實收之值）。
🔒 `③` 之 `E3` loud raise 係 **`GB-150`** 之受詞（`_wf_tag_of` 硬限 `{0,3.5}`）——**⛔ 本批所生**；
本單逐字令「⛔ 動 `_wf_tag_of` 及其呼叫點（`GB-150` 之射程，待 KL）」⇒ **⛔ 觸**。
🔒 **期望之出處**：`①` 之期望值係**由抽出之 `_SB_DEFAULT_M` 導出**（⛔ 硬寫 `3.5`）
⇒ 該常數一改，期望隨之改 ⇒ 本探針**即是**分歧偵測器。

### b　🔴 分歧偵測 ✅ PASS（**含負對照**）

三態皆改**工作樹**（**⛔ 入索引**），末了還原：

| 態 | 探針 `rc` | 抽出之常數 | 顯示之值 | 顯示之來源 | **輸入欄之值** |
|---|---|---|---|---|---|
| `A`｜**補正後** ＋ 常數 `3.5`（現態）| **0** | `3.5` | `3.5` | 預設 | **`3.5`** |
| `B`｜**補正後** ＋ 常數 `9.9` | **0** | `9.9` | `9.9` | 預設 | **`9.9`** |
| `C`｜🔴 **負對照：補正前**（字面 `3.5`）＋ 常數 `9.9` | 🔴 **1** | `9.9` | `9.9` | 預設 | **`3.5`** |

🔒 **`B`：輸入欄初值<u>隨常數改變</u>（`3.5` → `9.9`）** ⇒ **二者確已綁定**。
🔒 **`C`（負對照）：補正前之輸入欄初值<u>不隨</u>常數改變**（顯示 `9.9`／輸入欄 `3.5`）
⇒ 探針 `rc` ＝ **1** ⇒ **器能紅**。
🛑 ⇒ **⛔ 只證「現值相同」**（其於補正前即成立·`A` 與 `C` 之顯示值同為常數即為證）；
**二態判然可分** ⇒ 本測**有判別力**。

🔒 **還原後之逐位驗**：工作樹 **1417946** B·`sha256 e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca`
⇒ **與索引側 blob 逐位相同 ＝ `True`**；`git status -- app.py` ＝ `M  app.py`（**僅已暫存**·⛔ 未暫存之改）。

### c　迴歸 ✅（**行為逐位相同**）＋ 🔴 界限之載明

**證據形** ＝ `verify/probes/probe_WG9219R2_regression_R4.py`，於**拋棄式 clone**（`C:\w229`·自 `2b78a6a` clone）
跑二次：改前 ＝ clone 之 `HEAD` `app.py`；改後 ＝ 覆蓋本批之 `app.py`。

| 量 | 改前 | 改後 | 判 |
|---|---|---|---|
| 結果物正規化序列化之 `sha256` | `2e0f54bd9578fd3091698fb6b97c89006bb4182c31dd261e411a6bd12fb7413b`（**1380** B）| **同左** | ✅ |
| 全 `stdout`（去 `app.py`-sha 一列）`sha256` | `d32cb4c213ee16bd92b6c089cdf10e9d07874c3bce1dba7bc96ce0e1effeda85`（**5278** B）| **同左** | ✅ |
| `stderr` | **0** B | **0** B | ✅ |
| 🔒 **對照組（器須紅）** `app.py` 自身 `sha256` | `d0fe5c99fb2e6c535ee94a8699b61b9b351745cd92ed1bc197a5d0c11a908167` | `e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca` | 🔴 **不同** |

🔒 **對照組之意義**：二輪之 `app.py` **確為不同物** ⇒「相同」**⛔ 因跑了同一份碼**。

🔴 **界限之載明（⛔ 省·單逐字所令）**

> **`R4` 逐值迴歸<u>不可得</u>**——兩情境各有未決閘：`0 m` 之 **`GB-67`**（`②-宗 圍堵閘`·`app.py`·**未結案**）／
> `3.5 m` 之 **telescoping 結構閘**（`verify/stepg_pipeline.py`·harness·**既存紅·准紅碼**）；
> 見 `W-G.9-228` 工項五之二鏈（`CLAUDE.md` 正本表 `序 13`／`序 14`）。
> **本批所出者係<u>替代證據</u> ＝ 行為逐位相同（含<u>同點 raise</u>）**，
> 🛑 **⛔ 讀為「已證 `R4` 逐值不變」**——二者射程不同：前者證「同一鏈之行為未變」，
> 後者需「`R4` 之逐格數值」而該數值**現態產不出**。

🔒 **結構性佐證**：本批之改動在 `main()` 之 Streamlit 區塊內，而 `harvest` **從不呼叫 `main()`**
（`CLAUDE.md` §7 逐字）⇒ 期望本即逐位相同，實測相符。

### d　`numstat` 逐檔逐行 ＋ `deletions` 逐行具名

| 檔 | 增 | **刪** |
|---|---|---|
| `app.py` | **1** | 🔴 **1** |
| `docs/orders/W-G.9-229_預設值耦合補正.md` | 69 | **0** |
| `verify/probes/probe_WG9229_default_coupling.py` | 199 | **0** |

**`deletions` 之逐行具名（`VR-069` 補款二之體例）**

| 檔·列 | 舊逐字 | 取代式 | 所修之受詞 |
|---|---|---|---|
| `app.py:19720` | `                    _setback_init = float(st.session_state.get('f3L_setback_default', 3.5))` | `                    _setback_init = float(st.session_state.get('f3L_setback_default', _SB_DEFAULT_M))` | 顯示端常數與輸入欄字面**相隔 34 列且無連結** ⇒ 二者日後分歧時，**首次渲染所示之預設值將與輸入欄實採者不同**（恰為 `W-G.9-219R2` 所欲防止者）。取代後**綁定**，其判別力已由工項二 `b` 之 `B`／`C` 二態實測坐實 |

🔒 **⛔ 及 `verify/tools/`**；`app.py` 屬**生產碼五檔** ⇒ 依 `VR-069` 補款其 `deletions` 本應為 `0`。
🛑 **本列係例外，其正當性繫於 KL 之放行**——⛔ 由 CC 自行認定合格。

---

## 閘 `P`（生產碼五檔·改前／改後逐檔 `sha256`）

| 檔 | 改前 B | 改前 `sha256` | 改後 B | 改後 `sha256` | 判 |
|---|---|---|---|---|---|
| `app.py` | 1417936 | `091efa9b77c288082edea9cce809c0bb53c8b927f7cd0ea2e646a962c6409628` | **1417946** | **`e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca`** | 🔴 **已動（本單所令）** |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` | 35367 | 同左 | ✅ ⛔ 動 |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` | 96923 | 同左 | ✅ ⛔ 動 |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` | 96261 | 同左 | ✅ ⛔ 動 |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` | 24681 | 同左 | ✅ ⛔ 動 |

🔒 `left-right`（推前·對 `origin/wip/s1-endpart`）＝ **`0 0`**；追蹤 ＝ **1630** 檔／`.md` ＝ **557** 檔。
🔒 未暫存之追蹤檔異動 ＝ **0** 列。

---

## 🛑 停：候 KL 逐字放行

依單【回報後之處置】：**完成【驗收】所列後停**。本批**⛔ 推入主線**；
其 commit 已推至**側分支** `verify/W-G.9-229-c1`（⛔ `wip/s1-endpart`）供逐位復驗。
🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。

**🟡 併呈（本批⛔ 觸·⛔ 由 CC 處置）**
1. 🔴 **`GB-150`**（`_wf_tag_of` 硬限 `{0,3.5}`）——待 KL 覆「七級調配是否每案須能跑」；
   本批之 `③` 情境已再度見證其 loud raise。
2. 🟡 **`:22395`／`:22884` 之集中化**——列**通用化波**（理由見工項一 `c`）。
3. 🟡 **`序 12` 之 `R4` 逐值迴歸**——⟸ `序 14`（telescoping 閘）轉綠。
4. 🔴 **放行後必辦**：本批⛔ 使 `app.py` 行號位移（`@@ -19720 +19720 @@`·`LF` 列數 Δ ＝ `0`）
   ⇒ `GB-144` 之**現態命中集⛔ 變**（`13` 個檔列全同）⇒ **⛔ 須新增現態命中集列**
   （`W-G.9-228` 工項一 `d` 之維護義務**於本批無受詞**）。已機驗：改後之命中集與 `現態命中集@d2d3692` **逐項相同**。
