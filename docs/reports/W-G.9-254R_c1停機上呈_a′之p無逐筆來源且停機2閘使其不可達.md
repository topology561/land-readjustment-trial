# `W-G.9-254R`　`c1` **停機上呈**：`a′` 之 `p` 無逐筆來源，且 `停機#2` 閘使該分支結構上不可達

> 🛑 **⛔ 改一字生產碼**——`verify/wf_f0.py` blob 前後 ＝ `6758ea766b001b95`；`app.py` ＝ `4860f10828fb8ae9`。
> 🔒 **⛔ 鑄任何號。** 本文係 `W-G.9-254 §四 c1` 之施工前偵察結果與停機判。
> 🔒 依單 `§二 d` 之同型（`-249`）：**受詞之資料源不足以支撐所令之算式 ⇒ ⛔ 自行擴充 ⇒ 停機上呈並逐項具名何者不可得。**

---

## §一　單所令之受詞（`§四 c1` 逐字）

> 受詞：`verify/wf_f0.py:241`–`:246`（`add` 之累加 ＋ `t["面積_m2"]` 之寫入）…
> 改為：`a′ = a × p(被併宗原位置) / p(標的宗)`，`p` ＝ **重劃前地價（元/㎡）逐筆**（`K-9-29 一`）。
> 🛑 **`p ≤ 0` ⇒ `raise`**（⛔ 靜默退 `1.0`·⛔ 靜默退 `a′ = a`）。

🩸 **行號之差（`行號衛生`·具名·⛔ 追改單）**：單載 `:241`–`:246`，**實查**為
`_transform` ＠ **`:233`**、`add` 之累加 ＝ **`:239`–`:242`**、`t["面積_m2"]` 之寫入 ＝ **`:244`**
（態 ＝ `blob@92e4244`·`wf_f0.py` ＝ `6758ea766b001b95`）⇒ **差 `2` 列**。受詞本身**⛔ 誤**，僅行號漂移。

---

## §二　三環現查（**機械證據·三軸** ＝ `blob@92e4244`／逐檔正面列舉／**列框**）

### 環 `1`　🔴 **逐筆重劃前地價於現行全鏈<u>結構上不存在</u>**

| 受檢欄名 | `app.py` | `verify/stepg_pipeline.py` | `verify/wf_f0.py` | `verify/run_verification.py` |
|---|---|---|---|---|
| `原地號_單價` | `0` | `0` | `0` | `0` |
| `原地號_地價` | `0` | `0` | `0` | `0` |
| `pre_price_by_lot` | `0` | `0` | `0` | `0` |
| `逐筆地價` | `0` | `0` | `0` | `0` |
| **`原地號_區段`** | `0` | `0` | **`1`** | `0` |
| 🔒 對照組（器須綠）`pre_price_by_zone` | **`9`** | **`7`** | **`0`** | — |
| 🔒 對照組（器須紅）一必不存在之欄名 | `0`（`rc=1`） | — | — | — |

🔒 **唯一之逐原地號欄係 `原地號_區段`**（`wf_f0:199` `zone_of = snap["財務接線_v3"]["原地號_區段"]`）
——其值係**區段代碼**，**⛔ 價**。
🔒 **價之最細粒度 ＝ 區段**：`verify/stepg_pipeline.py:177`–`:178` 逐字
`pre_price_by_zone = {z: float(v["單價_元每m2"]) for z, v in fin["重劃前區段_面積單價"].items()}`
——鍵為 **`z`（zone）**；其上游 `重劃前區段_面積單價` 全倉命中 `app.py 1`／`stepg 1`／`run_verification 0`。
`stepg:175` 之註解逐字：「地價（v3：後街廓單價／**重劃前區段單價**；A 逐宗＝post/pre）」。

⇒ 🔴 **`K-9-29 一`（「重劃前地價係逐筆重劃前地號評定，⛔ 依歸戶、⛔ 依區段預設同價」）
所需之逐筆 `p`，其資料結構<u>尚未落地</u>**（`K-9-29 一` 自載「UI 步驟 D **逐筆點選**」係待落地項）。

### 環 `2`　🔴 **`wf_f0` 內⛔ 有任何地價資料** ⇒ 取 `p` 須擴充簽章（單未授權）

`pre_price_by_zone` 於 `wf_f0.py` 命中 **`0`**（對照：`app.py 9`／`stepg 7` ⇒ **器非紅**）。
`_transform(build_parcels, decisions)`（`:233`）之入參**僅二**，**⛔ 含 `snap`／價表**。
其唯一生產呼叫點 `:288` 位於 `compute(ctx_by_tag)` 內（該作用域**有 `snap`**）
⇒ **技術上可傳入**，惟**擴充 `_transform` 之簽章⛔ 在單 `§四 c1` 之授權內**。

### 環 `3`　🔴 **`停機#2` 閘使 `a′ ≠ a` 之分支<u>結構上不可達</u>**

`verify/wf_f0.py:272`–`:276` 逐字：

```
        for d in decisions:
            if d["target"] and len(d["zones"]) != 1:
                raise RuntimeError(
                    f"🔴 停機#2 區段不一致：{d['gid']}/{d['blk']} zones={d['zones']}"
                    "（A 折算係數≠1，須域裁 7-4 雙模式）")
```

🔑 **該閘之括號逐字即本題之受詞**——「**A 折算係數≠1，須域裁 7-4 雙模式**」。
⇒ 現行設計**明文假設**：合併群恆同區段 ⇒ `A` 折算係數恆 `1` ⇒ 可直接 `Σa`；
**異區段之情形被該閘擋在門外，並註明「須域裁」。**

🔴 **後果（若依單逕行實作而不動該閘）**：
1. 以**區段價**為 `p` ⇒ 該閘保證 `zone(被併) = zone(標的)` ⇒ **`p(被併) = p(標的)` 恆真**
   ⇒ **`a′ = a` 恆真** ⇒ 新增之折算分支**永不生效**（dead code）。
2. **⛔ 只在本案為真**——只要該閘在，**他案亦然** ⇒ 單 `§六` 所稱之立法理由
   「本批於本案逐值零差，**係為他案可攜**」**⛔ 成立**。
3. 單 `§四 c1` 所令之**判別力對照組**（「餵一組 `p(被併) ≠ p(標的)` 者，該閘須紅」）
   須以**繞過 `停機#2`** 之人造測資為之 ⇒ 即 `-249R` 所具名之「**靶未涵蓋受詞**」同形
   ——測到的是新寫的折算式，**⛔ 是它在生產路徑上的可達性**。

---

## §三　🛑 停機之判（**⛔ CC 自裁**）

**⛔ 施工 `c1`。** 三環之任一環未解，`c1` 之落地即為
「**新增一段結構上不可達之碼，並以繞過既有閘之人造測資宣稱其已驗**」。

### 候裁三問（呈 KL＋發單側）

| # | 問 | 性質 |
|---|---|---|
| `1` | 🔴 **`停機#2` 閘（`wf_f0:273`–`:275`）如何處置？** 其逐字理由即「A 折算係數≠1，**須域裁** 7-4 雙模式」——**`K-9-29 三` 是否即為該域裁？** 若是，該閘應**廢止**（否則 `a′` 分支不可達）；若否，`c1` 應俟該域裁 | 🔴 **域裁**（其解除使異區段合併成為可能 ⇒ **有土地後果**）|
| `2` | 🔴 **`p` 之粒度**：`K-9-29 一` 令**逐筆**，現行最細 ＝ **區段**且逐筆結構未落地。`c1` 係 (甲) 以**區段價**暫行（並登記逐筆為待落地）／(乙) **俟逐筆資料結構（UI 步驟 D）落地後再辦**／(丙) 其他 | 🔴 **工序 ＋ 域**（甲案與 `K-9-29 一`「⛔ 依區段」字面相斥）|
| `3` | 🟡 **`_transform` 之簽章擴充**（傳入價表或 `snap`）是否在 `c1` 射程內 | 🟡 **工程**（單 `§四 c1` 未授權）|

🔒 **CC 之建議（⛔ 自裁·僅供裁參）**：問 `1` 與問 `2` **同源**——二者皆繫於「異區段合併是否成立」。
若 KL 裁「`K-9-29 三` 即為 `停機#2` 所待之域裁」，則 `c1` 之正解為
**同批廢 `停機#2` ＋ 以區段價暫行 ＋ 逐筆登記為待落地**，且其**土地後果須以 `V-H` 同形之差異表出艙**
（⛔ 再以「逐值零差」為預期——廢閘後本案是否仍零差係**未知**，須實測）。

---

## §四　本批之態（**⛔ 動生產碼**）

| 項 | 值 |
|---|---|
| `app.py` | `4860f10828fb8ae9`（未動）|
| `verify/wf_f0.py` | **`6758ea766b001b95`**（未動）|
| 其餘九檔 ＋ 夾具 | 與 `92e4244` 逐位同 |
| 側分支 `verify/W-G.9-254-c1`／`-c2` | **未推**（⛔ 有生產碼 commit）|

🟢 **`c2`（廢步驟 0）⛔ 受本停機影響**——其受詞係 `app.py:11633` `k6_step0_enabled()` 之預設值，
與 `a′` 無涉。**惟單 `§六` 令 `c1`／`c2` 逐一候 KL 放行、⛔ 同一 commit** ⇒
**是否先辦 `c2`，候發單側指示**（⛔ CC 逕為）。
🟢 **`-251` 段乙（純 UI）⛔ 受影響**，可辦。
