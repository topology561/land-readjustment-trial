# `W-G.9-255R`　工項零〜三：入倉／`c1` 三環之處置／`K-9-29` 補款 `十`／自誤 `317`・`318`

> 🔒 **⛔ 零生產碼**（十一檔與 `70a676d` 逐位同·`wf_f0.py` ＝ `6758ea766b001b95`）⇒ **直推主線**。
> 🛑 **`c1` 復工（工項四）未辦**——生產碼·側分支 `verify/W-G.9-255-c1`·候 KL 放行。

---

## §零　開工閘（五項·**全過**）

| 閘 | 實測 |
|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ **`70a676d12b62fc6508c5723e98b234827f95ee9a`**·本地 `HEAD` 同·工作區 clean ✅ |
| `2` 號占用 | `W-G.9-255` ＝ `D1 0`／`D2 0`／`D3 0`／雙屬 `0`／檔框 `0`／列框 `0`／鬆框 `0`／`0` ✅（三軸 ＝ `blob@70a676d`／全 `docs/` **`596`** 檔／檔框＋列框）。對照甲 `-254` ＝ `D1 3`／`D2 6`／`D3 0`／檔框 **`5`** ⇒ **與單載逐項相符**；對照乙 一人造號（⛔ 入倉·`GB-147`·代稱）＝ 全 `0` ⇒ **器兩造皆能** |
| `3` 次號 | 自誤 **`MAX 316`**／缺號 `[106]`／恆等式 `True` ✅（與單載相符）⇒ 鑄 `317`／`318`；`K-9 MAX ＝ 29`（本單⛔ 鑄 `K-9` 號）|
| `4` `S-0b` | **停機款 `0` 項**（`rc=0`）✅；`P-5` 相符（受詞 `13065` B·實算 `72e929ebe5c3ac2b…` ＝ 單載）|
| `5` 前置 | worktree 已在 **wip 態**（`GB-152`）✅ |

🟡 **`S-0b` 提示 `2` 項之逐項處置**：`P-1` ＠ `:27` 係**引述 CC 停機主張並判其偽**之節標題
（⛔ 單自身之全稱否定宣稱）／`P-4` ＠ `:92` 係**函式簽章之改寫箭頭**（`_transform(a,b)` → `(a,b,snap)`·
⛔ 幾何方向）⇒ **二者皆具名豁免（偽陽）**。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-255_c1停機撤回_K-9-29補款十_自誤317_318_c1c2續辦.md`（`13143` B）。
來源 `sha256[:16]` ＝ **`b51580b902e02059`** ＝ 入倉 blob ⇒ **逐位相同**。

---

## §二　工項一：`c1` 停機三環之處置（**CC 之獨立復現·⛔ 照單全收**）

| 環 | 發單側之判 | **CC 當場復現** | 判 |
|---|---|---|---|
| **一**「逐筆重劃前地價於全鏈結構上不存在」| 🔴 偽（框錯） | KL 逐字已定義「逐筆」之受詞 ＝ **每一原地號各自被指定一個區段碼**（⛔ 各有獨立價格欄）⇒ CC 所窮查之十二組同義詞**本就不該存在**，其命中 `0` **⛔ 構成不可得之證** | 🔴 **CC 之框錯·撤回** |
| **二**「`wf_f0` 內⛔ 有任何地價資料」| 🔴 偽（框錯） | **CC 實查快照**（`verify/case_params_UC9898.json`）：`財務接線_v3` 內確有 `重劃前區段_面積單價` ＝ `{"a": {…"單價_元每m2": 43909.2308486259…}, "b": {…37322.846221332016…}}`，而 `wf_f0:199` 已逐字讀**同一 dict** 之鄰鍵 `原地號_區段` ⇒ **價一直在手上** | 🔴 **CC 之框錯·撤回** |
| **三**「`停機#2` 使 `a′≠a` 不可達」| 🟢 成立 | `停機#2` ＠ `:273`／`:275`（`grep -n` 實查）⇒ 其括號逐字「（A 折算係數≠1，須域裁 7-4 雙模式）」正指本題；KL 已於 `2026-09-08` 裁畢 | 🟢 **成立** |

### 🩸 CC 之具名撤回（單 `§二` 附帶所令·`V-M`）

🔴 **撤回**：`-254R` `§一` 所報之「**單載 `:241`–`:246`，實查差 `2` 列**」——**該項為偽，責在 CC**。
**CC 當場以 `grep -n` 機械實查**（態 ＝ `blob@70a676d`／`verify/wf_f0.py`）：

```
233:def _transform(build_parcels, decisions):
241:        add = 0.0
243:            add += (float(by_id[m].get("分攤登記面積_m2", 0) or 0)
244:                    + float(by_id[m].get("面積_m2", 0) or 0))
246:        t["面積_m2"] = round(float(t.get("面積_m2", 0) or 0) + add, 2)
```

⇒ **單載 `:241`–`:246` 逐位正確**；CC 所報之 `:239`–`:242`／`:244` **係目視誤算**
（以 `sed -n '230,255p'` 之輸出**目測推算**行號，⛔ 用 `grep -n`）⇒ **量測器紅**。
🛑 **⛔ 追改 `-254R` 既出一字**（`常規四（九）五`）——`V-M` 已驗其二檔 blob **未動**
（`ddf67fab86b86ce8`／`6b2d8ee912a08bb0`）；本節係其**唯一**撤回載體。

### 🔒 CC 側之二通則（自立·⛔ 鑄號）

1. 🔑 **欲證某量「不可得」，其框須為<u>受詞之可達性</u>**（自現有入參能否走到該值），
   **⛔ 為某一<u>變數名</u>之字面命中**——**變數名係實作之偶然，鍵名／資料結構才是受詞**。
   （環二之根因：以 `pre_price_by_zone` 為框，而快照側鍵名為中文 `重劃前區段_面積單價`。）
2. 🔑 **行號一律 `grep -n` 機械取**，**⛔ 自 `sed` 之輸出目測推算**——
   後者無型別可擋，錯了照樣印出一個看似精確的行號。

🔒 **CC 之停機行為本身⛔ 有過**（單 `§二` 逐字追認：「停機是正確的紀律；錯的是其中二環之框」）。

---

## §三　工項二：`K-9-29` 補款 `十`（`K-6` **檔末**追加）

`## 🔧 \`K-9-29\` 之補款 \`十\`：**同歸戶相鄰之跨地價區段合併<u>准予為之</u>，以 \`a′\` 折算**（KL 裁 \`2026-09-08\`·\`W-G.9-255\`…）`
——`停機#2` 閘**廢止**；合併一律以 `a′ = a × p(自身) / p(標的)` 折算；`p ≤ 0` ⇒ `raise`。
🔴 **本款有土地後果**（KL 逐字「會動配地面積」）。
🔒 併附 KL 對「逐筆」受詞之逐字定義，及**真正待修者係<u>時序</u>**（UI 點選在前置合併之後）之具名。

---

## §四　工項三：自誤 `317`／`318`（自誤簿檔末追加·標題形取 `316` 為範本）

| 號 | 受詞 |
|---|---|
| `317` | 令 CC 取「逐筆地價」而未定義其受詞（`受詞未定義` 族）·**責 ＝ 發單側**；CC 併記其二環框錯之根因與通則 |
| `318` | 以截圖為證斷言「倉內快照已與 KL 實際標註脫節」而未查倉（`proxy vs property` 族）·**責 ＝ 發單側** |

🔒 **`318` 之 CC 當場復現**：倉內快照為 **`a`／`b` 二區段**，⛔ 截圖所示之 `a`／`e`／`b` 三區段
⇒ **快照確未動**。

---

## §五　【驗收】（本批可判者）

| 閘 | 實測 |
|---|---|
| `V-B` append-only | ✅ **刪除欄非 `0` 之列數 ＝ `0`**（三檔：單／`K-6`／自誤簿）|
| `V-C` 取號 | ✅ 自誤 `MAX 316→318`、缺號仍 `[106]`、恆等式 `True`（`311+1 == 312`）、次號 `319`；**`K-9 MAX` 仍 `29`** |
| `V-L` 補款落於檔末 | ✅ 落於 `K-6` **檔末**（`append_frag` 之機械證：既有 `296381` B **逐位不動**，新增 `2736` B）|
| `V-M` 撤回且 `-254R` 未動 | ✅ 已於 `§二` 具名撤回；`-254R` 二檔 blob **逐位未動** |
| `V-A` | ✅ 十一項與 `70a676d` 全同（`wf_f0.py` ＝ `6758ea766b001b95`）|
| `V-G`／`V-G′`／`V-H`／`V-I`／`V-J`／`V-K` | ⬜ **本批未辦**（`c1`／`c2`／`-251` 段乙）|

---

## §六　待辦

| 批 | 受詞 | 狀態 |
|---|---|---|
| **`c1`** | `wf_f0`：`_transform` 收 `snap`（`:288` 傳 `c` 之 `snap`）／`p_of()` 自快照二鍵取值·`p ≤ 0` ⇒ `raise`／`add` 改 `Σ_m (a_m × p_of(m) / p_of(target))`／**廢 `停機#2`**（`:273`–`:275`·留逐字註）／`:22` docstring 同批更正 | ⬜ 側分支 `verify/W-G.9-255-c1`·`V-G`（逐值零差）＋ **`V-G′` 三組判別力**（⛔ 省）|
| `c2` | 廢步驟 0（`app.py:11633` 預設 `on→off`）·**序在 `c1` 後** | ⬜ 側分支 |
| `-251` 段乙 | 純 UI·⛔ 受影響 | ⬜ 側分支 |

🛑 三者**⛔ 混同 commit**；`c1` 未放行前 `c2` **⛔ 開工**（單 `§六` 之序）。
🔒 **發單側於 `70a676d` 實測**：現態（步驟 0 仍在）之 `F.0` 六組級 `0`／`0′` 成員**跨區段組數 ＝ `0`**
⇒ `c1` 之 `V-G` 預期**逐值零差**——🛑 **該數係發單側之現查，CC 將於 `c1` 批自行復現**。
