# `W-G.9-254R`　工項零（入倉）＋ 工項一（自誤 `316`／`GB-152`／`§三` 之裁）

> 🔒 **⛔ 零生產碼**（十一檔與 `4e6f00f` 逐位同·`app.py` ＝ `4860f10828fb8ae9`）⇒ **直推主線**。
> 🛑 **`c1`／`c2`（`K-9-29` 碼側落地·🔴 有土地後果）與 `-251` 段乙均未辦**——各走側分支、逐一候 KL 放行。

---

## §零　開工閘（單 `§零` 五項·**全過**）

| 閘 | 判準 | 實測 |
|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart @ 4e6f00f` | `ls-remote` ⇒ **`4e6f00f54ccb8dccd183e4b3f5dbfcf5028b1f87`**·本地 `HEAD` 同·工作區 clean ✅ |
| `2` 號占用 | `W-G.9-254` ＝ `D1 0`／`D2 0`／`D3 0`／雙屬 `0` | **逐項相符** ✅（三軸 ＝ `blob@4e6f00f`／全 `docs/` **`593`** 檔／檔框＋列框·依 `⑦` 併載雙屬）。檔框 `0`／列框 `0`／鬆框 `0`／`0`。對照甲 `-253` ＝ `D1 2`／`D2 7`／`D3 0`／雙屬 `0`／檔框 **`6`**／列框 `7`（須 `≥1`）✅；對照乙 一人造號（⛔ 入倉·`GB-147`·代稱）＝ 全 `0` ✅ ⇒ **器兩造皆能** |
| `3` 次號 | 自誤 `MAX 315` ⇒ `316`；`GB MAX 151` ⇒ `GB-152` | **當場復算相符** ✅。自誤（框 ＝ `VR-091 四` 定義框＋範圍框·母體 ＝ 簿單檔 `638325` B・`7514` LF）：定義框列命中 `309`／重咬 `3`／純單筆 `306`／相異 `308`／`MIN 7`／**`MAX 315`**／缺號 `[106]`／恆等式 `True`。`GB`（母體 ＝ `W-G.4` 單檔·**二框並取**：`^#{2,4} \`?GB-N\`?` 與 `^#+ .*?GB-N`）⇒ **二框 `MAX` 皆 `151`**；對照組 `GB-152` ＝ `0`（器須紅）／`GB-151` ＝ `1`（器須綠）✅ |
| `4` `S-0b` | `P-3`／`P-5` 綠 | **機械停機款 `0` 項**（`rc=0`）✅；`P-5` **相符**（受詞 `10731` B·實算 `56a0437348b9b3e5…` ＝ 單載）|
| `5` 開窗前置 | worktree 須在 wip 態，硬閘方生效 | ✅ **已在 wip 態**（`HEAD ＝ 4e6f00f`）；且硬閘**已於 `-253R` 段甲末段實際攔下 CC 一次**（見 `§二 GB-152`）⇒ **生效之活體證據** |

🟡 **`S-0b` 提示 `1` 項之逐項處置**（⛔ 靜默略過）：`P-1` ＠ `:33`——其命中之句係 **`GB-152` 之節標題**
（描述其受詞），**⛔ 全稱否定之宣稱** ⇒ **具名豁免（偽陽）**。

---

## §一　工項零：本單原封入倉

`docs/orders/W-G.9-254_K-9-29碼側落地_a′與廢步驟0_249延後_自誤316_GB-152.md`（`10809` B／`131` 列）。

| 驗 | 值 |
|---|---|
| 來源（主 checkout 工作區）`sha256[:16]` | `4e6923d3a875744c` |
| 入倉 blob `sha256[:16]` | **`4e6923d3a875744c`** ⇒ **逐位相同** |
| `numstat` | `131	0` ⇒ deletions ＝ `0` |

🔑 **單之所在**：仍在**主 checkout 之工作區**而⛔ 在倉——**本窗第五度**。
🛑 本閘只證「落檔 → 入倉」，⛔ 證「聊天原文 → 落檔」。

---

## §二　工項一：自誤 `316` ＋ `GB-152` ＋ `§三` 之裁

### `a`　`自誤 316`（自誤簿檔末追加·標題形取 `315` 為範本）

`## 自誤 \`316\`　**單內同節二款相斥：\`§二\` 令「皆末端追加」而其下逐款令「節末追加」（…同族·第五度）**`
——**責 ＝ 發單側**；其逐字入倉，並附 CC 側之落地紀錄（`K-6` 檔末四則字樣錨標記·原節一字未動）。
🔒 **CC 於 `-253R` 依 `常規二` 取保守項（檔末追加）之處置，經本單追認為<u>正解</u>。**

### `b`　`GB-152`（`W-G.4` 檔末追加·標題形取 `GB-151` 為範本）

`### \`GB-152\` 🆕　\`GB-143\` 硬閘之生效係**條件式**——繫於 worktree 是否在 wip 態`

🔒 **本則之案由即 CC 於本窗之親歷**：開窗時 worktree 停在 `main` 態、閘之二檔**結構上不存在**
⇒ 活體實測報「未生效」（**當時為真**）；CC 施工前 `git switch --detach` 至 wip 態後二檔隨工作區出現，
其後 CC 誤送一個 heredoc **即被同一硬閘攔下且攔對**。
🛑 **本則⛔ 解除 `GB-143`**——規則（⛔ heredoc）**不因 backstop 生效而放寬**；
亦**⛔ 計入 `GB-143` 之發生次數計數器**。
🔑 **通則**：凡閘之生效繫於**工作區檔案是否存在**者，其「是否生效」**⛔ 於 session 內恆定**
⇒ 出艙該類判須同格載明**量測當時之工作區態**，⛔ 外推至全 session。

### `c`　`§三` 之裁：`-249` 段丙**延後**（`W-G.4` 檔末追加更正段二）

依賴序更正為：`3′ K-9-29 碼側落地` → **`3″ -249 段三觀測（重跑）`** → `4 五級`。
🔒 plan v2 **得先撰、先送 reviewer**（零生產碼），惟**⛔ 產出 `V-S` 表、⛔ 呈 KL**，
俟 `c2` 經 KL 放行併線後以**落地後母體**重跑方得出艙。

🛑 **待 CC 復現之三數（⛔ 得作為倉內事實出艙）**：單 `§三` 案由所載
`628-40(1)+`（`G010`·成員 `628-40(1)`＋`628-43(1)`·`Σa 513.93`）係**發單側之現查**，
**CC 於本批⛔ 復現** ⇒ 依 `三方分工鐵律` 之補正，已於更正段內**標為待復現**。

### `d`　🟢 單 `§七` 之一項**已辦**（⛔ 重辦）

單 `§七` 列「`K-9-22` 作廢之 `CLAUDE.md` ⬜ 表更正標記｜本單工項一可一併加」——
**已於 `W-G.9-253R` 段甲落地**（`CLAUDE.md` 檔末之 `## 🔧 \`K-9-22\` 之作廢標記（\`W-G.9-253 §八\`…）` 一節·
現查命中 `1`）⇒ **本批⛔ 重辦**（重辦即二處標記同受詞，違「一個受詞一個載體」）。

---

## §三　判別力自檢（字樣錨·`行號衛生` 第 `4` 款·**於本報告出艙**，⛔ 寫入 payload 以免自指遞迴）

| 母體 | 錨 | 鑄前 | 落地後 | 寬字樣 | 對照組（須 `0`）|
|---|---|---|---|---|---|
| `W-G.4` | `3′　K-9-29 碼側落地` | **`1`** | **`2`**（本批之更正段二含 `1` 次）| `K-9-29` ＝ `9` | `0` ✅ |

🔒 **本批 payload ⛔ 含任何「命中數」之陳述** ⇒ **⛔ 有自指增長之遞迴**
（本窗曾二度於此踩坑·見 `W-G.9-253R §六`）。

---

## §四　【驗收】（單 `【驗收】` 表中**本批可判者**）

| 閘 | 判準 | 實測 |
|---|---|---|
| `V-A` | 十一檔 `sha256[:16]` 與 `4e6f00f` 逐位同 | ✅ **十一項全同**（`app.py 4860f10828fb8ae9`）|
| `V-B` | `numstat` deletions 全 `0` | ✅ **刪除欄非 `0` 之列數 ＝ `0`**（三檔：單 `131 0`／自誤簿 `55 0`／`W-G.4` `24 0`）|
| `V-C` | 自誤 `MAX 315→316`、缺號仍 `[106]`、恆等式 `True`；`GB MAX 151→152` | ✅ 全符（後態：定義框列命中 `310`／重咬 `3`／純單筆 `307`／相異 `309`／恆等式 `True`／次號 `317`；`GB` 二框 `MAX` 皆 `152`·對照組 `GB-153` 不在）|
| `V-G`／`V-H`／`V-I`／`V-J` | `c1`／`c2` 之閘 | ⬜ **本批未辦**——見 `§五` |
| `V-K` | `-251` 段乙 KL 目檢 | ⬜ **本批未辦** |

---

## §五　待辦（各走側分支·逐一候 KL 放行）

| 批 | 受詞 | 狀態 |
|---|---|---|
| `c1` | `verify/wf_f0.py` 級 `0`／`0′` 之 `Σa` → `a + Σa′`（`K-9-29 三`）·`p` 依 `v3:432`–`:435` **雙模式逐字**（本工項恆為模式一）·`p ≤ 0` ⇒ `raise` | ⬜ 側分支 `verify/W-G.9-254-c1`；**`V-G` ＝ 逐值零差閘** ＋ **二判別力對照組**（`p` 相異須紅／`p = 0` 須 `raise`）|
| `c2` | `app.py` `k6_step0_enabled()` 預設 `"on"` → `"off"`（🔴 **有土地後果**）·**旗標翻轉·⛔ 刪碼** | ⬜ 側分支 `verify/W-G.9-254-c2`；`V-H` 差異表 ＋ `V-I` `K6_STEP0_ENV=on` 逐位復現 |
| `-251` 段乙 | `c1`（路名管理／`RD` 下拉）／`c2`（工項一丁案）·純 UI | ⬜ 側分支 `verify/W-G.9-251-c1`·`V-K` 由 KL 目檢 |
| `-249` 段丙 | plan v2 得先撰、先送 reviewer；**⛔ 產出 `V-S` 表、⛔ 呈 KL** | ⬜ 依賴 `c2` 併線 |

🛑 `c1` 與 `c2` **⛔ 同一 commit**；`c2` 未經 KL 放行前**⛔ 併入主線**。
🔒 **呈 KL 之形**：CC 先自倉逐位復驗，再以**一句實務語言**呈 KL——**KL 放行的是那句話、⛔ 那段程式碼**。
