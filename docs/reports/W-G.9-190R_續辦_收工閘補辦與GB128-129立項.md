# 🧾 `W-G.9-190R` **續辦**：收工閘 `5` 補辦 ＋ `GB-128`／`GB-129` ＋ 自誤 `230`／`231`

**出文窗**：CC　**依據**：發單側 `2026-08-31`【`W-G.9-190R` 族③ 對拍之裁 ＋ 次一 commit 開工令】
**基座**：`80cf3ef2b7fa71708c745dbe3f4aee128b0f001e`（`app.py` blob `da805fc2…`／`1383997` B／`23463` 列）
**性質**：🛑 **零生產碼**（`§二-2` 判法輸出空 ⇒ 依 `§二-1` 逕行 push·⛔ 未請示）
🔒 **⛔ 未鑄新單號**——本檔係 `W-G.9-190R` 之續辦（體例同 `W-G.9-186R_續辦_…`）。

---

## `§一`　五受詞之判

| # | 受詞 | 判 |
|---|---|---|
| `1` | `[T3-GATE]` 補跑·`SB = 0.0 m` 與 `3.5 m` **二情境各一次**·逐宗具名其變動 | ✅ `§二`（**二情境皆變動 `0` 宗**） |
| `2` | `[T2-DIAG]` 未截斷版落檔 ＋ 鑄 `GB-128` | ✅ `§三`／`§五` |
| `3` | `§九-1` 之口徑就地加註（⛔ 不改原句一字） | ✅ `§四` |
| `4` | 鑄 `GB`（次號現查）＝ app 獨有之 `d_hat`／`corner_pt` BASELINE fallback | ✅ `§五`（＝ **`GB-129`**） |
| `5` | 鑄發單側自誤**二則** | ✅ `§六`（＝ **自誤 `230`／`231`**） |

---

## `§二`　受詞 `1`：`[T3-GATE]` 二情境補跑

### `§二-1`　作法（⛔ 未改既有證據鏈）

新增 `verify/probes/probe_WG9190R_t3gate2.py`（**純新增**·`deletions = 0`）——
以 `import` 取 `probe_WG9187_t3gate`，僅覆寫其**模組級** `SB`（自 `WV_T3_SB` 取），再呼叫其 `main()`。
🔒 **⛔ 未複製該檔任何一行、⛔ 未改該檔一字**（其為 `W-G.9-187R` 之既有證據鏈）。
🔒 覆寫後**即刻自驗**（`t3.SB != sb` 即 `raise`）⇒ ⛔ 不致二情境得同一結果之假綠。
🔒 `WV_T3_SB` **⛔ 非數即 `raise`**（no-silent-fallback·⛔ 不以預設兜底）。

### `§二-2`　🔑 主判：**基座 vs 期末**（同情境對拍）

| 情境 | 基座（`69daed5`·`app.py` blob `d756d9ad`） | 期末（`80cf3ef`·blob `da805fc2`） | 鍵序逐位相同 | 僅基座有 | 僅期末有 | 共有但整列相異 | 判 |
|---|---|---|---|---|---|---|---|
| **`SB = 0.0 m`** | `39` 列 | `39` 列 | `True` | `0` | `0` | **`0`** | ✅ **變動 `0` 宗** |
| **`SB = 3.5 m`** | `25` 列 | `25` 列 | `True` | `0` | `0` | **`0`** | ✅ **變動 `0` 宗** |

🔒 **判別力對照（證本對拍⛔ 非恆綠）**：於期末側注入一字元後重判 ⇒ **共有但整列相異 ＝ `1`** ⇒ `False` ✅。
🔒 **旁證**：【倉】`verify/out/probe_WG9187_t3gate.log`（`187R`·`0m`·`39` 列）vs 本批期末 `0m` ⇒
**逐宗逐位全同** ⇒ **收工閘 `5`（「`[T3-GATE]` 期末仍為 `39` 列」）✅ 成立**。
⇒ 🔒 **族②③ 之切換對 `[T3-GATE]` 之逐宗判定：二情境皆零後果。**

🛑 **基座態之取得**：拋棄式 clone `C:\w190b` @ `69daed5`。
🩸 **CRLF 陷阱實踩並修正**（`harness-worktree-crlf-trap`）：新 clone 繼承全域 `core.autocrlf=true`
⇒ `app.py` 得 **`1402119` B**／CRLF `23411`（≠ 基座 `1378708` B）；經 `core.autocrlf=false` ＋ 強制重簽出後
**`1378708` B**／CRLF `0`，且四框現查（舊式各 `1`／新式各 `0`）坐實其為**切換前**之態。
🔒 驅動器以 `sha256` 驗二 clone **逐位同源**（`95c9ccfc600d2cb5…`）。

### `§二-3`　逐宗具名（期末態·二情境之綜合判分布）

| 情境 | 總列 | 不適用 | 配地（就本批已檢之二閘而言） | **不配地** | 逐街廓 |
|---|---|---|---|---|---|
| `0.0 m` | `39` | `8` | `13` | **`18`** | `R1 5`／`R2 14`／`R3 5`／`R4 2`／`R5 8`／`R6 5` |
| `3.5 m` | `25` | `7` | `8` | **`10`** | `R1 5`／**`R2 0`**／`R3 5`／`R4 2`／`R5 8`／`R6 5` |

**`0.0 m` 之「不配地」`18` 宗**（逐宗·街廓／側／位次／暫編地號／`G`）：

`R1 right 1 628-34(3) 86.00`／`R2 left 1 628-42(1) 3.82`／`R2 left 2 628-27(1) 29.08`／
`R2 left 4 628-30(2) 86.66`／`R2 left 7 628-35(1) 58.95`／`R2 left 9 628-49(1) 65.45`／
`R2 left 10 628-50(1) 147.18`／`R2 left 11 628-51(1) 146.59`／`R2 left 12 628-52(1) 71.60`／
`R6 right 1 628-53(1) 0.26`／`R6 right 2 628-38(1) 117.73`／`R5 left 1 628-53(2) 0.99`／
`R5 left 4 628-31(1) 10.46`／`R5 left 5 628-32(1) 3.08`／`R5 left 6 628(3) 148.72`／
`R3 right 1 628-42(2) 89.96`／`R3 right 2 628-27(2) 26.54`／`R3 right 3 628-29(1)+ 131.61`

**`3.5 m` 之「不配地」`10` 宗** ＝ 上列**扣除 `R2` 之 `8` 宗**後之其餘十宗
（逐宗與 `0m` **同名、同位次、同 `G`**）。

🛑 **`39 → 25` 之差 `14` 已具名**：`R2` 於 `3.5 m` **整街廓 `0` 列**，成因由 log **自身**載明——
`🔴 R2/left：winner None 不在母體 ⇒ 射程外` ⇒ `R2` 無街角 winner ⇒ 其側不入八格、亦不出 `[T3-GATE]` 列；
`14` 恰為 `R2` 於 `0m` 之列數（`39 − 25 = 14`）✅ 收支相符。
⚠️ **⛔ 不得以此推論「`3.5m` 之配地較寬鬆」**——係**覆蓋**之差、⛔ 非**判定**之差。

### `§二-4`　`M-L-5 ①` 八格 ＋ `rc`（照實·⛔ 未調容差）

| 情境 | 格數 | 跨態不符格 | `X-3` | `rc` |
|---|---|---|---|---|
| `0.0 m` | `8` | **無**（八格 `\|Δ\| ≤ 5.2e-09`） | ✅ 未觸發 | `0` |
| `3.5 m` | **`7`**（`R2/left` 缺·見 `§二-3`） | **`2`**：`R1/left` `\|Δ\|=1.482e-03`／`R5/left` `\|Δ\|=5.043e-01` | ✅ 未觸發 | `0` |

🔒 `3.5m` 之二跨態不符格係**期望事實**——`REF_BLUE`（【倉】`K-9-23-a` 八格）係 **`0m` 情境之錨**
（`probe_WG9187_t3gate.py:32` 逐字：「與【倉】`K-9-23-a` 同情境（該表逐字：『情境 `0m`／`STEP0=on`』）」）。
🔒 **⛔ 未改 `REF_BLUE`、⛔ 未調容差 `REF_TOL`、⛔ 未略過任何格。**

### `§二-5`　🔧 原探針之表頭字串於 `3.5m` 成偽 ⇒ 以**驅動器尾記**具名（⛔ 不改原檔）

`probe_WG9187_t3gate.py:155-156` 之 `say()` 硬寫「情境 SB = %.1f m（**⛔ 非 X-T 之 3.5m**）」——
該括號係為 `SB = 0.0` 而寫；本驅動器令 `SB = 3.5` 時，該列成**自相矛盾之偽述**。
⇒ 🔒 **⛔ 不留偽述於倉**：驅動器於落檔後**末端追加尾記**，逐字具名該列係原檔硬寫、⛔ 非本趟之事實，
並載本趟情境**正是** `3.5 m`。🔒 ⛔ 未改原檔既有任何一列（`deletions = 0`）。

---

## `§三`　受詞 `2`：`[T2-DIAG]` 未截斷版落檔

新增 `verify/probes/probe_WG9190R_t2diag_full.py`（**純新增**）——量法**逐字同**
`probe_WG9190R_fam3.py:157-171`，**僅移除其 `:200` 之 `[:110]` 截斷**；⛔ 未改該檔一字。

| 街廓 | 切換前（【倉】`S-5` 基準量·未截）字元 | 切換後（本趟）字元 | 逐位相同 |
|---|---|---|---|
| `R1` | `201` | `201` | `True` |
| `R2` | `211` | `211` | `True` |
| `R3` | `193` | `193` | `True` |
| `R4` | **`235`** | **`235`** | `True` |
| `R6` | `185` | `185` | `True` |

🔒 **五列全數逐位相同 ＝ `True`**；**判別力對照**（`R2` 之 `33.0811`→`33.0812`）⇒ `False` ✅。
🔒 `R5` **`0` 列**係**期望事實**（`②-宗 圍堵閘破[R5]`）。`rc = 0`。
🛑 **射程限（`§七`·⛔ 不得省）**：`[T2-DIAG]` 係 **shim 側**，對 `app.py` 之切換**結構上不敏感**
⇒ 所證者 ＝「⛔ 無任何街廓因本批而變動」、⛔ **非**「切換有後果」。殘餘風險歸 `GB-123`，本檔⛔ 不解除。

🔒 **`GB-128` 之實害錨**：`R4` 整列 `235` 字元 ⇒ `[:110]` 截去 **`125` 字元**；倉內舊版
`verify/out/probe_WG9190R_fam3.log` 之 `R1` 止於「覆蓋」、`R4` 止於「26.093」——**⛔ 無截斷標記**。

---

## `§四`　受詞 `3`：`§九-1` 之就地加註

已於 `docs/reports/W-G.9-190R_族二三切換.md` **末端追加**（⛔ 上文原句一字不刪、一字不改）。

| 原文之數 | 實際口徑 | 獨立復現值 |
|---|---|---|
| `diff` 得 **`48`** 列差 | `diff` 之**輸出列數** ＝ `12` 個孤立變更塊 × `4` 列 | 原始**逐列相異 `12` 列**（全部含 clone 路徑） |
| 各 **`32972` B** | 🔴 ⛔ 非位元組——係 `len(''.join(splitlines()))`（**去行尾之字元數**）；`33488 − 516 = 32972` | 正規化後 **`33488` 字元**／**`46895` B**（原始各 `46907` B） |

🔒 **原句未動之機檢**：三句以 `grep -F` 各命中 **`2`**（＝ 原句 `1` ＋ 加註內 fence 引錄 `1`；**顯示數即總計數**）。
🔒 **引文之框（`戒 39` 之宣告）**：加註內之 fence 係自本檔 `:360`／`:362`／`:363` **逐列讀出後原樣寫入**
（⛔ 非手抄、⛔ 無轉義）——首版曾以 `> ` 引錄並轉義反斜線，經機檢（命中 `1` 而非 `2`）**當場捕獲並改正**。

---

## `§五`　受詞 `2`／`4`：`GB-128`／`GB-129` 之鑄造

**編號現查**（態 `80cf3ef`·**二母體並報**·`GB-88` 族·🛑 ⛔ 不以「算術自檢 True」為憑〔`GB-125`〕）：

| 母體 | 框 | 相異 | MAX | 缺號 |
|---|---|---|---|---|
| 登記表**單檔** | 式A `` ^## `GB-N` `` | `3` | `83` | — |
| 同上 | 式B `` ^### `GB-N` `` | `38` | `127` | — |
| 同上 | 式表 `` ^\| *\*{0,2}`?GB-N`?\*{0,2} `` | `122` | `127` | — |
| 同上 | **聯集** | **`125`** | **`127`** | `2` |
| **全倉**（`.md` `478` 檔·開不到 `0`） | `` ^(?:## \|### \|\| *)`GB-([0-9]{1,4})` `` | `63` | **`127`** | `64` |

🔒 **扣除集**（`常規四（八）` 補款）＝ **`GB-997`**——其定義列（`W-G.9-177R:93`）逐字
`` | `GB-997`（**`S-4` 人造不存在之受詞**） | `0`／`0`／`0` | ✅ 必須為 `0`·符 `` ⇒ **判別力哨兵**、⛔ 非實體占用。
⇒ **二母體 MAX 同為 `127`** ⇒ 次號 **`128`**、次次號 **`129`**（⛔ 不受母體差影響）。
🛑 **錨定框**（停機款 `12`）：`GB-128` 命中 **`0`** ✅／`GB-129` 命中 **`0`** ✅；
**對照組** `` (?<![0-9\-])GB-127(?![0-9]) `` 命中 **`25`** ⇒ 量測器非紅。
🔒 **標題形**逐字取既有最末一則 `GB-127` 之二形（`常規四（九）四`）。

- **`GB-128`** ＝ 「驗收探針**自身**之顯示截斷，使其所落之 log ⛔ 非整列逐位**而讀者不可知**」。
  🔒 **與 `GB-115` 之別**：`GB-115` 之截斷**印總數**（非靜默）；本則**⛔ 無任何截斷標記**。
  同族 `GB-108`／`GB-110`／`GB-115` ⇒ **第四度**。⛔ 不得以「把 `110` 改大」為處置。
- **`GB-129`** ＝ 「`app.py` 獨有之 `d_hat`／`corner_pt` BASELINE fallback（`app.py:20725-20733`·`_mbl_block`），
  其射程因族③ 落地而由『推進迴圈』擴及『**選槽起算點**』」
  （`_oblique_s_max` → `_end_pt_o` → `_b_R0` → `k*` → 配地位置）。
  🔒 `_mbl_block` 於 `verify/stepg_pipeline.py` 命中 **`0`**。本案**⛔ 不觸發**（六街廓皆有 `FRONT_LINE`；守衛 ＝ `d_hat is None`），
  已由本波二情境 `[T3-GATE]` 零變動 ＋ `[T2-DIAG]` 五列全同**實測坐實**。
  **失效條件**（逐字）＝「二側之 `d_hat`／`corner_pt` 取得路徑統一，或 app 側之 fallback 改為 loud」。
  ⚠️ **觸發態⛔ 無真資料覆蓋**（倉內⛔ 無 `FRONT_LINE` 缺之案）；🛑 本則**⛔ 未上呈 KL**（本案不觸發）。

---

## `§六`　受詞 `5`：自誤 `230`／`231`

**編號現查**：自誤（正典寬形 `` ^#+ 自誤 `?([0-9]{1,4})`? ``）相異 `198`／MAX `229`；
窄框（`` ^###\s*自誤\s*`?([0-9]{1,4})` ``）相異 `47`／MAX `229` ⇒ **二框 MAX 同** ⇒ 次號 **`230`**／次次 **`231`**。
🛑 錨定框命中皆 **`0`** ✅（對照組「自誤 `229`」命中 `6` ⇒ 量測器非紅）。
🔒 **標題形**逐字取既有最末一則 **自誤 `229`** 之形：``### 自誤 `NNN`（發單側·落地窗）：**<受詞>**``。
🔒 **入倉後機檢**：二框 MAX 皆 `229` → **`231`**，且 `230`／`231` **皆在二框之集合內**；
判別力對照（行中提及之形）命中**空** ✅。

- **自誤 `230`** ＝ 「單所載之取號框與同節之敘述互斥，且該框使一個**已知須撈到之號**撈不到」（由 CC 於 `§九-4` 指出）。
- **自誤 `231`** ＝ 「對一個**有土地後果**之受詞，只立**字樣閘**而未立**數值閘**」——
  🔴 係 **自誤 `229` 之同族第二度**（把閘綁在**症狀**上而非**性質**上）。
  🔒 併載發單側事後補證（AST 對拍逐位相同 `True`／判別力 `_dn > 1e-9`→`1e-8` 判不同）⇒ **該缺已解除**。

---

## `§七`　🛑 框污染機檢（前窗踩過·⛔ 不假設）

入倉**後**全倉四類重掃（母體 `.md` `478`）：

| 類 | 相異 | MAX | 次號 |
|---|---|---|---|
| 自誤（寬形） | `200` | **`231`** | `232` |
| `GB`（扣哨兵 `997`） | `65` | **`129`** | `130` |
| `VR`（扣哨兵 `999`） | `70` | `84` | `85` |
| **`戒`（正典框）** | `22` | **`39`** | **`40`** |

🔒 **`戒` 之 `24` 個命中列逐列現查** ⇒ **⛔ 無一列落於本 commit 之新增區**
（自誤登記之新增自 `:4472` 起、登記表自 `:3239` 起；`戒` 之最大命中列 ＝ `:4343`）
⇒ **本 commit ⛔ 未污染 `戒` 框**，次號 `40` 不動。
⚠️ **既有污染仍在**（前窗已報·本 commit ⛔ 未處置）：`docs/orders/W-G.9-190R_族二三切換.md:155`
與 `docs/orders/W-G.9-191_發單側交接文.md:71` 之 `🛑 戒 1〜15…` 被框命中而捕獲 `戒 1`。

---

## `§八`　🛑 呈裁（⛔ 未自鑄號·本裁僅授二號，已用畢）

🔴 **新出之阻塞候選**：**`SB = 3.5 m`（＝ 本案之實作情境）下，`R2` 之 `[T3-GATE]` 覆蓋 `0` 宗。**

- **實測**：`0.0 m` 時 `R2` 出 `14` 列（其中「不配地」`8` 宗）；`3.5 m` 時 `R2` 出 **`0`** 列。
- **成因（log 自載·⛔ 非推定）**：`🔴 R2/left：winner None 不在母體 ⇒ 射程外`
  ⇒ `R2` 於 `3.5m` **無街角 winner**。
- **後果**：`W-G.9-190R` 全部量測皆在 `3.5 m`，而 `[T3-GATE]`（`K-9-23` 藍影＋二閘）於該情境下
  **對 `R2` 之 `14` 宗完全無守備**——而 `R2` 正是本波族②③ 之**唯一實測受詞街廓**。
- 🛑 **CC ⛔ 未自鑄號**：本裁逐字僅授二號（`128` ＋「其次一號」），且 `W-G.9-188 裁 C` 之先例
  逐字為「本批僅此一號可鑄」⇒ **呈發單側裁**是否立為新 `GB`（次號將為 `130`）。
- ⛔ 本則**⛔ 非**本 commit 之受詞、⛔ 未作任何處置。

---

## `§九`　異動清單（逐檔 `numstat`·**基座檔 `deletions` 須 `0`**）

見 commit 之 `git diff --numstat`。🔒 **`§二-2` 判法輸出 ＝ 空 ⇒ 零生產碼**：
`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` **全數未動**。
🔒 `probe_WG9187_t3gate.py`／`probe_WG9190R_fam3.py`／`probe_WG9189_s6_replica.py` **皆未動一字**。
🔒 二登記表與 `W-G.9-190R_族二三切換.md` 皆為**末端追加**（`deletions = 0`）。
