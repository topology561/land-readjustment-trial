# `W-G.9-80`　`t_X` 之**精確式** ＋ **真正之閉式** ＋ `s` 漂移之候選證路 ＋ 身分鍵比對（⛔ 只量不修）

**施工單**：`W-G.9-80`　**基座**：`55e26ba`（`origin/wip/s1-endpart`·`W-G.9-79b`）
**探針**：`verify/probes/probe_WG980_closedform.py`　**log**：`verify/out/probe_WG980_closedform_55e26ba.log`
**零 `app.py` 變更**（blob 仍 `a9e5671d64d254907a0396f898f046d9d85e8283`·`git diff` 0 行）

---

## 🔴 §0　本批之三句結論

1. ✅ **`P3` 成立——「真正之閉式」已得，且其判別力經負對照坐實。**
   謂詞 `t_lo − t(p_j) ≤ (d_signed/sinα)·⟨û_j,n̂⟩ ≤ t_hi − t(p_j)`（**⛔ 全部輸入在白名單內**）
   與 `block.contains(X)` 於 **86 真實對**之**對稱差 ＝ 空集**；
   **負對照**（關掉「張開側之號」）恰於 `0m R3` 之 **4 對**判錯，含施工單指名之 `(7,9)`——
   其 `|s*·⟨û_j,n̂⟩| − 張開側可用長度` ＝ **`3.4192`**，與 `VR-037` 二-甲**逐位相符**。

2. 🔴 **`P2`／`P4`／`P6` 三項逐字不成立**（⛔ 未調任何判準·逐字保留）：
   `P2` 之絕對殘差極大 `2.384e-07 > 1e-9`——**28 對全部落在 `|t_X| ≥ 1e6`**（`R6` 之 `sinα ≈ 0`），
   而 `|t_X| < 1e6` 之 **58 對**其絕對殘差極大僅 `1.787e-10` ✅；
   `P4` 之 `⟨û_j,m̂⟩/sinα ∈ [0.9,1.1]` **0/62**，**改取 `|比值|` 亦僅 40/62**，
   `VR-038` 之候選式 `d/cosα` 同樣 **|比值| 40/62** ⇒ 🔴 **二式皆不成立**
   ⇒ 依施工單，`VR-037` 二-丙之「**構造必然**」四字**須撤回**；
   `P6` 於掃描 **144 列**不一致 **34 列**——**全部為「閉式預測有交叉、實測無」**
   ⇒ 🔒 **正是 A-0 事前選定之錯誤方向（多判在內 ⇒ 會吵）**，設計如預期生效。

3. 🔴 **一處須更正我上批**：`W-G.9-79` §D-2 之「A-2 表身 **67** 對」係**列印數**
   （該探針設 `show2 = [...][:40]` 之上限），**⛔ 非母體**；真母體 ＝ **86 對**。
   ⇒ 該節「越界量_s > 0 之對數 ＝ 0（67/67）」之**分母須更正為 86**，
   且該結論**本批未直接重測**（僅間接·§J-2）。

---

## §A　既有工作之擴搜（四組 ＋ **CC 增補之第五組**）

### A-1　逐字出艙之指令與範圍

| 組 | 指令（逐字） | 範圍 | 命中（僅 `probes/*.py`） |
|---|---|---|---:|
| ① 受詞名 | `grep -rlF "t_X" verify/probes/`；同式 `"閉式"`／`"投影因子"`／`"公垂"` | `probes/` | **7** |
| ② 現象名 | `grep -rlF "殘差" verify/probes/`；同式 `"sin"`／`"夾角"`／`"點距"`／`"張開"` | `probes/` | **71** |
| ③ 檔名樣式 | `ls -1 verify/probes/probe_WG9[78][0-9]*`／`probe_D2b*` | `probes/` | **32** |
| ④ 構造端符號名 | `grep -rlF "_strip_axis" verify/probes/`；同式 `"_line_cross"`／`"_bitsame"`／`"faces_of"`／`"alloc_normal_axis"` | `probes/` | **18** |
| ⑤ 🔒 **CC 增補**（⛔ 非施工單所列） | `grep -rlF "rot90" verify/probes/`；同式 `"np.cross"`／`"恆等式"`／`"unit("` | `probes/` | **25** |

🔒 排除式：`__pycache__` 一律排除；母體僅計 `verify/probes/*.py`。

| 母體 | 檔數 |
|---|---:|
| `①∪②∪③∪④` | **87** |
| ⑤ 之**淨增量** | **2**（`probe_K9_6_parcel_band.py`／`probe_WG922_three_vs_target.py`） |
| 五組聯集（**排除自身**） | **89** |

🔒 **第五組具名為「CC 增補」**：受詞 ＝ **向量原語之名**（旋轉／外積／恆等式／單位化）。
淨增 **2**——與 `-78`（2）／`-79`（2）同 ⇒ ⚠️ **⛔ 不得據此推論規格已窮舉**（節 101）。

### 🛑 A-2　停機條款之判定：**未觸發**（⛔ 逐項具名）

停機條款：「已有一支出艙過**以 `d`／`α`／投影因子<u>預測</u> `t_X` 並給出殘差**」。

| 現查 | 實得 | 判 |
|---|---|---|
| `grep -rlF "投影因子" verify/probes/ verify/out/` | **0 檔** | 無 |
| `grep -rlF "公垂" verify/probes/ verify/out/` | **0 檔** | 無 |
| `grep -rlF "t_X" verify/probes/ verify/out/` | **1 檔** ＝ `probe_WG979_crosspoint.py`（本波前批） | 其出艙者為**實測** `t_X`，**⛔ 非預測式＋殘差** |

⇒ 🔒 **未觸發**。⚠️ 施工單已明言 `probe_WG978_interface` 之「通過點距／方向夾角」**不構成停機**
（係 `d`／`α` 之**來源**）——本批依令**逕引用其產生式並以同式重算對拍**（§C-1），⛔ 未重造該量測。

### A-3　逐支「已答／未答」（🔒 分層·每層出艙分母·判準逐字可否證）

**層 A ＝ 同時含「預測」與「殘差」者 ＝ 3 支**

| 探針 | 其閉式預測之受詞（逐字引） | 對本批受詞 |
|---|---|---|
| 🔒 `probe_K9_7d_stageA.py` | 「偏離量有**閉式預測**：`殘差 = W_甲(t*_乙) − T = −k · δ_P(t*_乙)`」 | **未答**——其受詞為**最小寬 `W`**、⛔ 非 `t_X`。🔒 **惟可引為家法先例**：本倉已有「閉式預測 ＋ 殘差」之成例 |
| `probe_WG91_buf_metric_and_candidates.py` | 「`buf` 之**度量識別**……⛔ **只量不判**」 | **未答**（受詞為 `buf` 之度量） |
| `probe_WG943_parallel_recount.py` | 「`K-9-6-h ④` 之『平行』——受詞 ＝ **兩條線本身**」 | **未答**（受詞為平行判測） |

**層 B ＝ 含 `_line_cross`（求交點）而不在層 A ＝ 2 支**
`probe_WG978_interface.py`（出艙 `d`／`α` 之**來源**·⛔ 非預測式）／
`probe_WG979_crosspoint.py`（出艙**實測** `t_X`·⛔ 非預測式）⇒ **皆未答**（施工單已明言不構成停機）。

**層 C ＝ 其餘 ＝ 84 支** ⇒ **未答**（不含任一預測／交點符號）。
🔒 **對帳：`3 + 2 + 84 = 89 ＝ 排除自身後之母體`** ✅。

---

## §B　§Z **四塊**自檢

**入倉前現查**：四個目標檔之現況 bytes（LF 正規化後）＝
`331345`（`W-G.4_泛用阻塞項登記表.md`）／`114116`／`125783`／`306141`
——🔒 後三者與 `W-G.9-79b` §B 所載之追加後檔長**逐位相同** ⇒ 基座未被他人動過。

| 塊 | 目標檔 | bytes | 期望 | LF | 期望 | sha256 前 16 | 期望 | **追加後檔長** | 期望 | 判 |
|---|---|---:|---:|---:|---:|---|---|---:|---:|:--:|
| `Z-1` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | **4048** | 4048 | **64** | 64 | `0b316f25f4612156` | 同 | **335393** | 335393 | ✅ |
| `Z-2` | `docs/驗證裁定登記表.md` | **5923** | 5923 | **83** | 83 | `aeffa81e126b01a1` | 同 | **120039** | 120039 | ✅ |
| `Z-3` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **2719** | 2719 | **41** | 41 | `b25b9fbe06eed497` | 同 | **128502** | 128502 | ✅ |
| `Z-4` | `.claude/skills/failure-archaeology/SKILL.md` | **3254** | 3254 | **45** | 45 | `b91a70a8ee4eeb8d` | 同 | **309395** | 309395 | ✅ |

**入倉後自檢**：① 末 N bytes 逐位 == 片段 ✅（四塊）② 檔長增量 == N ✅
③ `numstat` 刪除欄全 0 ✅（`+64/0`、`+83/0`、`+41/0`、`+45/0`·二法 ＋ 陽性對照）
④ 四個新編號之**定義節標題**各 **1** ✅（🩸 首版以 shell grep 量得二個 `0`·係跳脫寫壞·見 §I-2）。

**編號現查（入倉前）**

| 待取編號 | **期望** | 實得 | 判別力對照 | 對照**期望** | 對照實得 | 判 |
|---|---:|---:|---|---:|---:|:--:|
| `VR-038` | 0 | **0** | `VR-037` | ≥1 | **27** | ✅ |
| `自誤 80` | 0 | **0** | `自誤 79` | ≥1 | **4** | ✅ |
| `^## 104\.` | 0 | **0** | `^## 103\.` | ≥1 | **1** | ✅ |
| `GB-67` 之新加註標題 | 0 | **0** | 既有 `GB-67` 列 | ≥1 | **15** | ✅ |

🔒 併記：考古之既有最大節號 ＝ **103** ⇒ `104` 為次號（⛔ 非跳號）。

---

## 🔴 §C　`A-1` `t_X` 之精確式

### 🔴 C-1　`A-1-0`：「通過點距」之定義比對 ⇒ **不相等**（⛔ 已二者併出艙）

🔒 **逐字現查**：`probe_WG978_interface.py:442` ＝ `dist = _dist_pt_line(near_k[1], far_j[1], far_j[0])`，
而 `_dist_pt_line(p,q,d)` 之實作為 `abs(w[0]*(-dd[1]) + w[1]*dd[0])` ＝ `|cross(p−q, d)|`
⇒ **`WG978` 式 ＝ `|cross(p_k − p_j, û_j)|`**（`p_k` 到**線 j** 之垂距·**無號**）；
**本單式 ＝ `cross(p_k − p_j, û_k)`**（`p_j` 到**線 k** 之垂距·**帶號**）。
⇒ 🔴 **受詞互換（哪個點對哪條線）＋ 一有號一無號 ⇒ 不相等。**

| 情境 | 街廓 | j | k | `WG978 |d_j|` | 本單 `d_signed` | `|差|` |
|---|---|---:|---:|---:|---:|---:|
| 0m | R3 | 0 | 7 | 95.368072 | −95.762881 | **0.394809** |
| 0m | R3 | 1 | 7 | 89.675537 | −90.046780 | 0.371243 |
| 0m | R3 | 0 | 8 | 87.640175 | −88.002991 | 0.362817 |
| 0m | R5 | 0 | 7 | 87.937052 | 87.574505 | 0.362546 |
| …（前 8 大·全表見 log） | | | | | | |

⇒ **分母 86 對**：`|差|` 之**最大 `0.394809` ／ 中位 `0.024497` ／ 最小 `0.000000`**
⇒ 🔴 **不相等**（最大差 ≫ `1e-9`）⇒ 依 A-1-0 **二者已併出艙**，⛔ 未逕代用。

### 🔴 C-2　`A-1-1`：恆等式之逐對驗證 ⇒ **`P2` 逐字不成立**

🔒 **恆等式（⛔ 非擬合·⛔ 非模型·其推導載於探針 docstring）**：
`s* = cross(p_k−p_j, û_k) / cross(û_j, û_k)`；`t_X = t(p_j) + s*·⟨û_j, n̂⟩`。

| 量 | 判準 | 實得 | 判 |
|---|---|---|:--:|
| 殘差**絕對值極大** | ≤ `1e-9 m` | **`2.384e-07 m`** | 🔴 **不成立** |

🔒 **補量（⛔ 不改判準·只加維度）——依 `|t_X|` 量級分層（逐層出艙分母）**：

| `|t_X|` 區間 | 對數 | 絕對殘差極大 | 判 |
|---|---:|---:|:--:|
| `[0, 1e2)` | 9 | `1.425e-10` | ✅ ≤ 1e-9 |
| `[1e2, 1e4)` | 47 | `1.787e-10` | ✅ ≤ 1e-9 |
| `[1e4, 1e6)` | 2 | `1.164e-10` | ✅ ≤ 1e-9 |
| 🔴 `[1e6, 1e12)` | **28** | **`2.384e-07`** | 🔴 > 1e-9 |

⇒ 🔒 **`|t_X| < 1e6` 之 58 對全部通過**；**不通過之 28 對全部落在 `|t_X| ≥ 1e6`**，
且其 `sinα` 皆印為 `−0.000000`／`0.000000`（＝ `R6` 之 `SIDE ∥ ALLOC` 退化）。
**相對殘差** `|res| / max(|t_X|,1)` 之極大 ＝ **`1.582e-11`**。
⚠️ **⛔ 未證其成因**（推定為 `s* = d/sinα` 於 `sinα → 0` 之數值條件；**本批未做條件數分析**·§J-5）。

🔒 **負對照（令 `⟨û_j,n̂⟩ ≡ 1`·＝退回 `W-G.9-79` §D-4 之量級式）**：
殘差絕對值極大 ＝ **`3603043232.59 m`**（判準 ≥ `0.1`）⇒ ✅ **本檢查非恆真**
（中位 `2052.97` ／ 最小 `0.000000`）。

---

## 🔴 §D　`A-2` **真正之閉式** ⇒ **`P3` 成立**

🔒 **謂詞（⛔ 全部輸入在白名單內·⛔ 不用 `t_X`／`s_X`／`X`／`contains`）**：

> **`在內 ⟺ t_lo − t(p_j) ≤ (d_signed / sinα)·⟨û_j, n̂⟩ ≤ t_hi − t(p_j)`**

### D-1　**問 1**：與 `block.contains(X)` 之對稱差

| 量 | 期望 | 實得 | 判 |
|---|---|---|:--:|
| 分母（可算交點之真實對） | — | **86** | — |
| 一致 ／ 不一致 | 對稱差 ＝ 空集 | **86 ／ 0** | ✅ |

### 🔴 D-2　**負對照**（關掉「張開側之號」·改取二側之**較大**可用長度）

| 量 | 期望 | 實得 | 判 |
|---|---|---|:--:|
| 與 `contains` 之一致 ／ 不一致 | **須判錯**（否則該項無判別力） | **82 ／ 4** | ✅ **有判別力** |
| 判錯之對 | 含施工單指名之 `0m R3 (7,9)` | `0m R3` 之 `(7,9)`／`(7,10)`／`(7,11)`／`(7,12)` | ✅ |

### 🔴 D-3　**問 2**：`0m R3 (7,9)` 之逐項

| 量 | 實得 |
|---|---:|
| `t(p_j)` | `8.2161` |
| `s*·⟨û_j,n̂⟩` | `3.4927` |
| **張開側** | **`t_hi` 側** |
| 張開側**可用長度** | `0.0735` |
| 🔴 **差**（`|s*·⟨û_j,n̂⟩| − 可用長度`） | **`3.4192`** |
| **另一側**可用長度 | `52.1297`（🔒 負對照即取此 ⇒ 誤判「在內」） |

🔒 **`3.4192` 與 `VR-037` 二-甲所載之「`0m R3` 距翻面僅 `3.4192 m`」<u>逐位相符</u>**
——二者**由不同路徑取得**（前者為 `t_X` 對 `t_hi` 之越界量·後者為本批之**閉式**由 `d`／`α` 預測）
⇒ 🔒 **一次獨立復現。**

### D-4　**問 3**：於掃描 144 列（＝ `P6`）

⇒ 🔴 **不一致 34 列**（詳 §F-2）。

---

## 🔴 §E　`A-3` `s` 漂移之候選證路 ⇒ **`P4` 不成立**

### E-1　施工單之判準（`⟨û_j,m̂⟩ / sinα ∈ [0.9, 1.1]`）

| 量 | 實得 |
|---|---:|
| 分母（全部真實對） | **86** |
| 例外類（`宗k` 亦為街角宗·⛔ 不併入判準） | **24** |
| **非例外類**：在 `[0.9,1.1]` 內 ／ 外 | **0 ／ 62** |

🔒 **補量①（⛔ 非施工單所令·不改判準）——`|比值|` 之分布**（非例外類·分母 62）：
最小 `0.0000` ／ **中位 `1.0000`** ／ 最大 `1.0000`；`|比值| ∈ [0.9,1.1]` 者 **40**；
**比值為負者 ＝ 42 ／ 62**。
⇒ 🔒 施工單之 `[0.9,1.1]` 係**帶號**區間、**⛔ 未涵蓋 `−1`** ⇒ 「號」須先具名；
🔴 **惟即使改取 `|比值|`，仍只有 40／62** ⇒ **22 對真正偏離** ⇒ **`P4` 仍不成立**。

### 🔴 E-2　`VR-038` 四-I-3 之候選證路（`s 漂移 ≈ d / cos α`·🔒 補量②·⛔ 非施工單所令）

| 量 | 實得 |
|---|---:|
| 帶號 `實測漂移 / (d/cosα)` ∈ `[0.9,1.1]` 者 ／ 外 | **29 ／ 33** |
| `|比值|` 最小 ／ 中位 ／ 最大 | `0.0000` ／ **`0.9996`** ／ `1.0032` |
| `|比值| ∈ [0.9,1.1]` 者 | **40 ／ 62** |
| 比值為負者 | **19 ／ 62** |

⇒ 🔴 **號係主要差異**（`|比值|` 合格 40 ≫ 帶號合格 29），
🔴 **惟 `|比值|` 亦僅 40／62** ⇒ **`VR-038` 之候選式亦不成立**·具名。

### 🔴 E-3　判（`P4`·依施工單逐字）

> ⛔ 若非例外類亦大幅偏離 ⇒ **`α` 不相消** ⇒ `VR-037` 二-丙之「構造必然」**撤回**。

⇒ 🔴 **二式（施工單之 `⟨û_j,m̂⟩/sinα` 與 `VR-038` 之 `d/cosα`）皆不成立**
⇒ 🔒 **「`s_X` 恆在域內」之地位<u>永久停留於觀察</u>；`VR-037` 二-丙之「構造必然」四字須撤回。**
⚠️ 🔒 **`s` 恆等式本身仍成立**（`s_X = s(p_j) + s*·⟨û_j,m̂⟩/denom`·殘差極大 `1.479e-07`，
與 `t` 同樣落在 `|t_X| ≥ 1e6` 之退化列）——**不成立者係「`α` 相消」之<u>近似論證</u>**，⛔ 非恆等式。

---

## 🔴 §F　`A-4` 身分鍵比對 ＋ `P6`

### F-1　`A-4`：母體變異之**身分鍵自動比對** ⇒ **`P5` 成立**

🔒 身分鍵 ＝ `(n, 街角宗索引之 tuple)`。

| 情境 | 街廓 | 相異鍵數 | 不恆定者 |
|---|---|---:|---|
| 0m | R3 | 2 | `20°: (14, (8,))` |
| 3.5m | R1 | 2 | `16°: (5, (0, 2))`／`20°: (5, (0, 2))` |
| 3.5m | R6 | 2 | `16°: (11, ())`／`20°: (11, ())` |
| 其餘 9 格 | | 1 | **恆定** |

⇒ **不恆定之列數 ＝ 5** ⇒ 🔒 **正對照：比對器<u>恰抓 5 列</u>** ✅（`P5` 成立）。

🔒 **併答（`W-G.9-79` §E-2 未具名者）**：`3.5m R6` 於 `θ ≥ 16°` 之街角宗集合為 `()`
⇒ 其「未取得正例」係 **shim 未觸發**、**⛔ 非「交點在外」**——本批補正。

### 🔴 F-2　`P6`：閉式謂詞於掃描 144 列 ⇒ **不成立**

| 量 | 實得 |
|---|---:|
| 一致 ／ 不一致 | **110 ／ 34** |

🔒 **34 列之方向<u>完全一致</u>**：**全部為「閉式預測有交叉（≥1）而實測 `contains` 為 0」**
——⛔ 無一列為反向。逐列（前 12）：

| θ | 情境 | 街廓 | 實測交叉(不鄰) | 閉式預測 |
|---:|---|---|---:|---:|
| −8° | 0m | R2 | 0 | **2** |
| −8° | 0m | R5 | 0 | **1** |
| −6° | 0m | R2 | 0 | **2** |
| −6° | 0m | R5 | 0 | **1** |
| −4° | 0m | R2／R5／R3 | 0 | **1** 各 |
| −2° | 0m | R2／R5 | 0 | **1** 各 |
| 2° | 0m／3.5m | R6 | 0 | **1** 各 |
| 4° | 0m | R6 | 0 | **1** |

🔒 **判讀（⛔ 觀察·⛔ 非成因結論）**：閉式係 `t` 之**外接盒**測試，
而 `contains` 係**真多邊形**測試 ⇒ **外接盒為必要非充分**（`W-G.9-79` §A 已先驗於非凸 U 形）。
⇒ 🔒 **於 86 真實對二者全一致，而於合成掃描出現 34 列分歧** ⇒ **分歧係合成角度所致**，
承 `probe_D2b12_bridge` 之「**非凸街廓 ⇒ 可切出不連通片**」缺口（⛔ 未證·§J-3）。
🔒 **⇒ 方向恰為 A-0 事前選定之「多判在內」⇒ 會吵** ⇒ **事前選定之設計如預期生效。**

---

## §G　`P1`〜`P7` 逐項判 ＋ 閘

### G-1　預測（🔴 ⛔ 不得調整·逐字保留）

| # | 預測（逐字） | 期望 | 實得 | 判 |
|---|---|---|---|:--:|
| `P1` | §Z **四塊**之 bytes／LF／sha256 逐塊相符 ⇒ **4／4** | 4／4 | 4／4 ＋ **追加後檔長四塊全中** | ✅ |
| 🔴 `P2` | 精確式殘差**絕對值極大 ≤ `1e-9 m`**（全部真實對）；負對照 ≥ `0.1 m` | ≤1e-9 | **`2.384e-07`** 🔴（分層：`|t_X|<1e6` 之 58 對 ≤ `1.787e-10` ✅／`≥1e6` 之 28 對 🔴）；**負對照 `3.60e9` ✅** | 🔴 **逐字不成立·具名** |
| `P3` | 閉式謂詞與 `contains` 於 **86**（原書 67）真實對**對稱差 ＝ 空集**；負對照應於 `0m R3` 判錯 | 空集 | **86／0 ✅**；負對照判錯 **4 對**含 `(7,9)` ✅ | ✅ |
| 🔴 `P4` | `⟨û_j,m̂⟩/sinα ∈ [0.9,1.1]`，例外類 ＝ `宗k` 亦街角宗 | 在內 | **0／62**（`|比值|` 亦僅 **40／62**；`VR-038` 之 `d/cosα` 同為 **40／62**） | 🔴 **不成立** ⇒ 「構造必然」**撤回** |
| `P5` | 身分鍵比對器**恰抓 5 列** | 5 | **5** | ✅ |
| 🔴 `P6` | 閉式謂詞於掃描 144 列亦與 `廓內交叉(不鄰) ≥ 1` 一致 | 一致 | **110／34**·34 列**方向全為「預測有、實測無」** | 🔴 **不成立·具名** |
| `P7` | `app.py` blob 仍 `a9e5671…`·`diff` 0 行 | 相同 | 同左 | ✅ |
| `P7` | `docs/rulings/` 變更 **0**／`verify/baselines/` **0 檔** | 0 | 0／0 | ✅ |
| `P7` | `verify/**.py` 變更檔數 ＝ **2** | 2 | **2**（改 `wg942_append_audit.py` ＋ 新 `probe_WG980_closedform.py`） | ✅ |
| `P7` | 🔒 `run_all` 清單 ＝ **15**（**第五法**覆量） | 15 | **15**（見下） | ✅ |
| `P7` | `run_all.py` blob 與基座逐位相同 | 相同 | `fcc7f0111ee40a85f8544fb993eb1251fcce0180` | ✅ |

🔒 **`run_all` 15 之第五法（節 100 所令·⛔ 與前四法皆不同族）**：

| 法 | 解析層 | 假設 | 實得 |
|---|---|---|---:|
| 甲 | 字面層（`re` 字元序） | 字面形如 `("....py"` | 16 − 1（`:80` 假陽性）＝ 15 |
| 乙 | 語法層（`ast.For` + `Tuple`） | 清單係一個 `for` 之元組迭代體 | 15 |
| 丙 | 詞法層（`tokenize` token 流） | `STRING` 之前一 token 為 `(` | 16 − 1（同一假陽性）＝ 15 |
| 戊 | 肉眼列舉（claude.ai·`VR-037`） | 人工 | 15 |
| 🆕 **丁** | **編譯產物層**（`compile()` 之 `co_consts` **遞迴**） | 常數摺疊後之**元組物件** | **15**（🔒 **無假陽性**——`endswith(".py")` 係方法呼叫、⛔ 非元組常數） |

🔒 **丁⛔ 不經原始碼文字**（看的是 CPython 摺疊後之常數）⇒ **與前四法皆不同族**。

### G-2　閘（🔒 序：1 → 4 → 2 → 2′ → 入倉 → 1′）

| # | 閘 | 期望 | 實得 | 判 |
|---|---|---|---|:--:|
| 1 | 純追加稽核（入倉前·基座 `55e26ba`） | `rc=0` | `rc=0` | ✅ |
| 4 | `TARGETS` 追加（先現查·只補未在列者） | 只追加 | **四個 `Z` 目標檔皆已在列**（命中各 1·施工單 §三 已明載 `Z-1` 已在列）⇒ ⛔ 不重登；新報告檔命中 **0** ⇒ **追加 1 項** | ✅ |
| 2 | 變異測試（對象先證在母體內·舊文行中插字） | `rc=1`·具名 | 對象 `docs/reports/W-G.4_泛用阻塞項登記表.md`（母體命中 **1**·`:38`）⇒ `rc=1`·S-5 觸發 | ✅ |
| 2′ | 變異復原 | `rc=0`·`diff` 0 行 | `diff` **0 行**·`rc=0` | ✅ |
| 1′ | 純追加稽核（入倉後） | `rc=0` | `rc=0` | ✅ |
| 3 | `numstat` 刪除欄全 0（二法＋陽性對照） | 全 0 | 甲 0（列數 4）／乙 0（`rc=0`）／陽性對照 1 | ✅ |
| 5 | `app.py` blob ＋ `verify/**.py` 變更檔數 | `a9e5671…`／**2** | 同左 | ✅ |
| 6 | `py_compile` | 二檔全過 | 全過 | ✅ |
| 6′ | 位元組層複查·**附對照組** | 0 ＋ 對照須命中 | 二檔皆 **0**；對照命中 `(3,'0x8')` | ✅ |
| 7 | 🔒 `run_all` ⛔ 不跑（逸出條款逐項現查） | 未跑·未觸發 | `app.py` 0 行／`baselines` 0 檔／`run_all.py` blob 逐位相同／`docs/rulings` 0／`grep` **rc=1** ⇒ 未觸發 | ✅ |
| 8 | `git status --porcelain` 全樹（前後各一次） | 無非預期 | **前 2 列**（新探針＋新 log）／**後 7 列**（＋4 個 `Z` 目標檔 `M` ＋ 稽核器 `M`）；連同本報告共 **8 列** | ✅ |

---

## §H　施工單項數對帳（🔒 **量詞三對齊自校**）

| 節 | 項數 | 逐項 | 已辦 | 未辦 |
|---|---:|---|---:|---:|
| §零 | 2 | ① `TARGETS` 追加 ② 新增探針 | **2** | 0 |
| §一 | 7 | ①〜④ 四組併搜 ⑤ 指令與範圍逐字 ⑥ 命中清單＋逐支已答/未答（分層·分母·可否證） ⑦ 編號現查 4 列＋判別力對照 | **7**（另 ⑧ **CC 增補第五組**·具名·⛔ 不計入） | 0 |
| §二 | 6 | `A-0`／`A-1-0`／`A-1-1`／`A-2`（三問）／`A-3`／`A-4`；`A-5` 五款全守 | **6** | 0 |
| §三 | 7 | `Z-1`〜`Z-4`／前置三道／入倉後四項自檢／檔長期望四塊 | **7** | 0 |
| §四 | 7 | `P1`〜`P7` | **7**（`P2`／`P4`／`P6` 不成立·皆具名） | 0 |
| §五 | 11 | 1／4／2／2′／1′／3／5／6／6′／7／8 | **11** | 0 |
| §六 | 13 | §0／§A／§B／§C／§D／§E／§F／§G／§H／§I／§J／§K／§L | **13** | 0 |
| §七 | 8 | 1〜8 條 | **8** | 0 |
| **合計** | **61** | — | **61** | **0** |

🔒 **量詞三對齊自校**：結論句 N ＝ **61**（＝ 施工單 §八之合計·逐字相符）；
「項數」欄逐列加總 `2+7+6+7+7+11+13+8 = 61`；「已辦」欄 ＝ **61**；「未辦」＝ **0**。三者對齊。
✅ **§六 之項數係由「§ 標題之裸列舉」現數後填入**（🔒 `W-G.9-79b` §F-1 之修法首次施行·**本批未再誤植**）。

**§七 邊界逐條自檢**

| 條 | 內容 | 實得 |
|---|---|---|
| 1 | ⛔ 零 `app.py`；`verify/**.py` 限二項 | ✅ |
| 2 | ⛔ 只量不修；⛔ 不下「這是 bug」 | ✅ §D/§E/§F 皆標「觀察」「對應」 |
| 3 | ⛔ 不重烤／不換圖／不接入 `run_all` | ✅ |
| 4 | ⛔ 不得以擬合代替恆等式 | ✅ §C-2 之式係**解析推導**（docstring 載其推導）·⛔ 未做最小二乘 |
| 5 | ⛔ 不改編既有編號／不改寫歷史 | ✅ |
| 6 | 🔒 ⛔ 不得 shim `S` | ✅ **本批未 shim `S`**（第二軸仍以 `θ` 取得） |
| 7 | 🔒 表頭計數可逐筆指認／`%.3e`／二門檻併標；🆕 「完全分離」須附最小餘裕 | ✅ §C-1／§C-2 逐層出艙分母；§D-3 即最小餘裕（`3.4192`） |
| 8 | 🆕 🔒 **凡出艙一個檢查，須舉出會使它為否之具體輸入及實得值** | ✅ **本批四處施行**：A-1-1 負對照（`3.60e9`）／A-2 負對照（判錯 4 對）／A-4 正對照（恰 5 列）／自檢 ②③（合成已知偽） |

---

## 🩸 §I　CC 本批之自誤

### 🩸 I-1　`W-G.9-79` §D-2 之分母 **67 係列印數、⛔ 非母體**（真母體 **86**）

**錯**：`W-G.9-79` §D-2 寫「🔒 全表統計（A-2 表身 **67** 對·⛔ 出艙分母）」，
並據以出艙「越界軸 `t` 63／「無」4」「`越界量_s > 0` 之對數 ＝ **0**」「二口徑不一致 ＝ 0」。
**實**：該 `67` 係 `probe_WG978`／`WG979` 之**表身列印數**（`show2 = [...][:40]` 之上限），
**真母體 ＝ 86 對**（全部非逐位平行者）。
🔴 **要害**：我在該處**字面上標了「表身」**，卻**把它當母體用**——
⇒ 「`s_X` 恆在域內」之結論其**分母錯了 67／86**。
🔒 **本批已於 log 逐字更正**，並列為 §J-2 之未答（該結論**本批未直接重測**）。
🔑 **形狀 ＝ 節 102／95 之同族**：**印出來的那個子集，被當成了全部。**
🔒 **修法（自用）**：**凡引用一個計數為分母，須同批寫出「該計數之產生指令」**——
若其產生式含 `[:N]`／`if` 過濾，**一律標為「列印數」並另出艙母體**。

### 🩸 I-2　shell 單引號內之 `\`` 使兩個標題計數誤報為 **0**

**錯**：閘之四個標題計數以 `grep -c '^## \`VR-038\`'` 量 ⇒ `GB-67 加註` 與 `VR-038` 皆回 **0**。
**實**：bash **單引號內** `\`` 係**字面反斜線＋反引號**，⛔ 非跳脫 ⇒ 樣式對不上。
以 Python `re`（無 shell 層）重量 ⇒ **四個皆 1** ✅（附已知偽對照 `VR-999` ⇒ 0）。
🔴 **若照信，我會判「入倉失敗」而去動已正確之檔。**
🔒 **修法（自用）**：**凡樣式含反引號／反斜線／跳脫序列，⛔ 不得經 shell** ——一律以 Python `re` 為之。
⚠️ 與記憶中之「轉義層自傷」同族（`W-G.9-78` 寫 `0x08` 一則）——**本則之方向相反**：
前者**寫進去**了不該有的字元；本則**讀不到**該有的字元。**共同病根 ＝ 多了一層會解讀反斜線的東西。**

### I-3　近失（⛔ 未出艙錯數·列此留痕）

`A-1-1` 之絕對殘差判準對 `|t_X| ~ 1e9` 之列在物理上不適用（`2.4e-7 / 1.6e9 ≈ 1.5e-16`）。
🔒 **我⛔ 未調判準**（逐字保留 `P2` 不成立），改以**分層 ＋ 相對殘差**補量並具名其未證之成因（§J-5）。

---

## ⛔ §J　本批未涵蓋之事

1. ⛔ **成因未下結論**（施工單 §七-2 明令）。
2. 🔴 **「`s_X` 恆在域內」未於 86 對直接重測**——本批 A-2 之謂詞僅測 `t` 軸。
   🔒 **可間接推得之部分**：閉式（`t`）與 `contains` 於 86 對全一致 ⇒
   **凡閉式判「在內」之對，其 `contains` 亦為真 ⇒ `s` 必在域內**；
   ⛔ **但閉式判「在外」之對，本批對其 `s` 一無所知。**
3. 🔴 **`P6` 之 34 列不一致未歸因**（推定為外接盒 vs 真多邊形於非凸街廓之差·承 `probe_D2b12_bridge` 之缺口·**⛔ 未證**）。
4. 🔴 **`s` 漂移之機制仍未解**——施工單之 `⟨û_j,m̂⟩/sinα` 與 `VR-038` 之 `d/cosα` **二式皆不成立**（各 40／62）。
5. ⛔ **`A-1-1` 之 28 對（`|t_X| ≥ 1e6`）絕對殘差之成因未證**（未做條件數分析）。
6. ⛔ **極端 `θ` 之 `S0d 異常` 訊息未逐項歸因**（於 stderr·⛔ 未入 log）。
7. ⛔ **未跑 `run_all`**（閘 7 明令）⇒ 閘數現況本批不出艙。
8. ⛔ **未上呈 KL**。
9. ⛔ **層 B 之 2 支與層 C 之 84 支未逐支讀 docstring**——以判準分層，判準已逐字出艙、可被否證。

---

## 🔒 §K　波末佇列現況

| # | 項 | 現況（本批後） |
|---|---|---|
| **0** | **`GB-67`：`②-宗` 破量之成因** | 🔴 **仍未下結論**。**本批新增**：🔒 **閉式已得且經負對照坐實**（86 對對稱差空集·負對照判錯 4 對）；`0m R3` 之 `3.4192` 由**閉式獨立復現**；🔴 **`s` 漂移之機制二式皆不成立**（`P4`）；🔴 **閉式於合成掃描有 34 列多判「在內」**（`P6`） |
| 1 | `K-8` 遷移項**乙** | 要件 1／2 ✅；要件 3 與重烤**同批**·繫於第 0 項 |
| 2 | `K-8` 遷移項**甲** | ⛔ 未辦 |
| 3 | 換圖 `V6_1` ＋ 換快照**同一次**重烤 | ⛔ 未辦·`GB-16`／`U-K8-1`。⚠️ **`GB-67` 之加註（本批 `Z-1`）已載其失效條件**：重烤後須**重量** `0m R3` 之最小 `t` 越界量；若 **≤ 0** ⇒ 破閘母體由 2 增為 3 |
| 4 | `format_exc()[-300:]` 二處之修 ＋ 名單正當重生 | ⛔ 未辦（`GB-86`） |
| 5 | `K-9-12` 宗地層矩形容納之實作 | ⛔ 未辦 |

---

## 🔴 §L　交下一窗之三件

### ① 本批之**未答項**（⛔ 逐項具名·承 §J）

| # | 未答 | 錨 |
|---|---|---|
| 1 | **`s_X` 恆在域內** 未於 86 對直接重測（僅間接·且只涵蓋「閉式判在內」之對） | §J-2 |
| 2 | **`P6` 之 34 列不一致未歸因**（外接盒 vs 真多邊形·非凸缺口） | §J-3 |
| 3 | **`s` 漂移之機制**——二式皆 40／62·**仍未解** | §E／§J-4 |
| 4 | `|t_X| ≥ 1e6` 之 28 對絕對殘差成因未證 | §J-5 |

### ② `GB-67` 之**現況一句話**

> **`②-宗` 破量 ＝ 街角宗遠側界與其後宗近側界**在街廓內交會**所生之楔形；
> **是否交會已可由閉式<u>預測</u>**——`t_lo − t(p_j) ≤ (d/sinα)·⟨û_j,n̂⟩ ≤ t_hi − t(p_j)`
> （⛔ 全部輸入為構造量·⛔ 不需先算交點），於 **86 真實對**與 `block.contains` **對稱差空集**，
> 且其**張開側之號**經負對照證實**有判別力**（關掉即於 `0m R3` 判錯 4 對）。
> ⛔ 此為**條件之陳述**，⛔ 非成因之結論——是否為缺陷、如何修，下一窗決定。**

### ③ 下一窗**第一批之建議受詞**（⛔ 建議·非指令）

🔒 **受詞 ＝ 閉式之<u>射程</u>**（⛔ 非再求新式）：

1. **測 `P6` 之 34 列**：逐列出艙其閉式判「在內」之交點 `X`，並判 `block.contains(X)` 為何為否
   ——是**非凸凹槽**（`probe_D2b12_bridge` 之缺口）抑或**其他**？⛔ 解釋不出即具名。
2. **補 `s` 軸**：以同一恆等式 `s_X = s(p_j) + s*·⟨û_j,m̂⟩/denom` 對 **86 對**出艙
   `s_X` 與街廓 `s` 域之對照 ⇒ 直接回答 §J-2（⛔ 不再以「構造必然」之近似論證代替）。
3. **`s` 漂移之機制**：`⟨û_j,m̂⟩` 之分布已知（`0.0000`／`±0.0754`／`±0.0097`…）
   ⇒ 建議改問「**`⟨û_j,m̂⟩` 何時為 0**」（40／62 之 `|比值| ≈ 1` 與 22／62 之偏離，其分界為何）。
4. ⛔ **仍不修**——修法**必涉配地幾何** ⇒ 屆時**須以五段式呈 KL**。

🔒 **一句話交下一窗**：
`W-G.9-79` 把「交點落在哪裡」量成了座標；**本批把它變成了一個<u>在算交點之前就能算出來</u>之條件**，
並以負對照證明該條件之每一項都在做事；**剩下的是這個條件的<u>射程</u>——它在合成角度下會多判 34 列。**
