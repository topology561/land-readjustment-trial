# W-G.8／**D-2b-31**：`GB-58` 登記 ＋ 面積差欄作廢 ＋ 考古增節（⛔ 全單零生產碼·**零探針**）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**起點**：`88afdc2`
**本單實際編號 ＝ `D-2b-31`**（`grep -rn "D-2b-31" . --exclude-dir=worktrees` ⇒ **0 命中**·未占用）

> # 🔴 最先讀
>
> **§1／§2／§3 全部落地，⛔ 無停機**（§6 三項停機條件逐項未觸發·見 §4-7）。
>
> 🔴 **本批多出一個 commit `A′`：CC 自己的具名更正。**
> commit A 之 W-G.9 補充宣稱「`_first_corner_alloc_dir` **僅 1 處呼叫**」並引
> `grep -c "_first_corner_alloc_dir(side_mid)" app.py`——**該指令實跑為 `3`**。
> **結論對、引據錯**；已於 `A′` 具名更正（⛔ 原句不刪）。詳 §1-4。

---

## §0 動手前（⛔ 未憑本單轉抄）

| 項 | 現查 |
|---|---|
| 編號 `D-2b-31` | **0 命中** ⇒ 未占用 |
| `HEAD` | `88afdc2`（＝ `origin/wip/s1-endpart`·工作樹乾淨） |
| **零生產碼·零探針** | `git diff --name-only 88afdc2 HEAD \| grep '\.py$'` ⇒ **空**（`rc=1`） |

---

## §1 ✅ commit A：`GB-58` 登記（`dd75f18`）

### 1-1 §1-2 六道複驗（⛔ 原樣實跑·**全部相符**·任一不符即停機·未發生）

```
$ grep -n "_left_min = round(float(_rng_cr.area), 2)" app.py
18120:                                    _left_min = round(float(_rng_cr.area), 2)
⇒ 命中 1

$ grep -n "S1_par = max(_seg_P0Ps, T)" app.py
11080:    S1_par = max(_seg_P0Ps, T)
⇒ 命中 1

$ grep -n "n_hat = rot90" app.py
5741 / 8419 / 8736 / 8852 / 11829   ⇒ 命中 5（皆註解）
$ grep -n "n_hat = np.array(\[-ad\[1\], ad\[0\]\])" app.py
8371 / 8457 / 8767                  ⇒ 命中 3（活碼·`_block_strip` 內）
⇒ 帶之切邊由 `allocation_dir` 決定 ⇒ ∥ALLOC（兩種寫法皆坐實）

$ git log --format="%h %ad" --date=short -S "_corner_buffer_S" --reverse -- app.py | head -1
5b80f9d 2026-07-21          ⇒ 與單所載 2026-07-21 相符

$ git log --format="%h %ad" --date=short -S "S1_par" --reverse -- app.py | head -1
f8b42fc 2026-08-08          ⇒ 與單所載 2026-08-08 相符

$ git show 021bca3 -- app.py | grep -c "_corner_buffer_S"
0                           ⇒ 與單所載 0 相符
（021bca3 ＝「K-6-A2 D-2b-1: K-9-5-4 ② 實配境界線 ∥SIDELINE」·2026-08-08）
```

### 1-2 `GB-58` 入表

⚠️ **本批之錨為預期變動**：`grep -c "^| \*\*GB-" 該檔` **由 `57` → `58`**
——⛔ **未為維持 57 而避開 `| **GB-` 起首**（與 `D-2b-30` 之處置相反，係本單 §1-1 明令）。
新列 **7 欄**（與表頭一致·`sed -n '36p'` 之分隔列 ⇒ 7）、**末格無裸豎線**（`D-2b-30` 已踩過）。
置於 `GB-57` 之前以維持表首之遞降序。

### 1-3 `W-G.9` 增列「七、併入本波之項目」

⛔ 既有各節一字不動。載明 **KL 原話逐字**、**連動理由**與**既有可資參照之機具**。

### 1-4 🔴 CC 之補充現查（⛔ 施工單未載·具名）

`GB-58` 之碼面形狀可再收緊一層：**「同一裁示、兩條路徑，只切換了一條」**。

- 有 WINNER 之路徑已切換——落點為 `_first_corner_alloc_dir`
  （`grep -c "def _first_corner_alloc_dir" app.py` ⇒ **1**），
  其將街角第 1 宗之 `allocation_dir` 覆寫為 `rot90_cw(SIDE 方向)`，使 `_block_strip` 之界線 ∥SIDELINE。
- **該覆寫不在強制抵費地路徑上**——`_corner_buffer_S` 之兩處呼叫點傳的是
  `allocation_dir_block`（＝街廓之 ALLOC 方向）。

🔴 **具名更正（commit `A′`·`6409341`）**：commit A 原寫
「`grep -c "_first_corner_alloc_dir(side_mid)" app.py` ⇒ **1**」——**實跑為 `3`**
（def `:11826` ＋ 註解 `:9690` ＋ 真呼叫 `:11886`）。

- 🔒 **實質結論不變**：真呼叫點確為 **1** 處。**錯的是我所引之指令產不出我所寫之數。**
- **可產出該數之正確指令（皆已原樣實跑）**：
  `grep -c "= _first_corner_alloc_dir(side_mid)" app.py` ⇒ **1**；
  AST 窮舉 `ast.Call` 之 `func.id == "_first_corner_alloc_dir"` ⇒ **1**（`:11886`）。
- ⚠️ **本錯即節 60 之形狀**（工具射程 ≠ 命題射程）：命題為「**呼叫**點數」，
  而 `grep` 之射程為「**字串出現**數」——且係在**立該戒之次二批**再犯。
- 🔒 **通則之增量（已入考古節 62 之續發段）**：凡宣稱「某符號只有 N 處呼叫」，
  ⛔ 不得以裸字串 `grep -c` 為據，須以 **AST `ast.Call`** 或至少排除 def／註解之式為據。

---

## §2 ✅ commit B：`D-2b-30` 報告之 D 欄作廢（`51569ad`）

### 2-1 §2-1 現載複驗 ⇒ **與單所載逐行相符**（`:13`／`:128`／`:130`–`:132`）⇒ 未觸發停機條件 2

### 2-2 加註（置於該表之後·⛔ 未刪表、未改任一數字）

**D 欄（`range_area − band_area(A)`）⛔ 不得引為土地後果**——其所量者為
「維持 ∥ALLOC 帶形、僅把帶寬壓成 `S1_par`」之面積短少，**而該構造從未被任何一方主張**。
`K-9-5-4` 之正解係**改用 ∥SIDELINE 多邊形**，該多邊形**寬度 ＝ `7.000 m` 且面積 ＝ `range_area`，
二者同時成立、無取捨** ⇒ 「三格合計 `224.97 ㎡`」係**題目設錯所生之數**。

🔒 **A／B／C／E 欄不受影響**——`buf`（`8.695857`／`8.702662`／`8.658415`）與
`S1_par`（皆 `7.000000`）之實測值及其差，仍為 `GB-58` 之**有效實測命中**。

🔎 **連帶失效（CC 現查·⛔ 施工單未載）**：`D-2b-30` §8 次步第 2 項
「若 KL 欲檢定 `P2` ⋯需另設母體」**一併失效**——`P2` 係在**同一個錯的題目**下提出；
正解既為「改用 ∥SIDELINE 多邊形」，`|C|` 隨夾角如何變化**已不構成待答問題**。
⛔ 不得再據以另設母體。

---

## §3 ✅ commit C：失敗考古**增節 62**

節號**以現查為準**：`grep -n "^## [0-9]" 該檔 | tail -1` ⇒ 既有最末 **61** ⇒ 新增為 **62**。
⛔ 既有各節一字不動。

節 62 ＝「**量測題目設在錯的構造上 ⇒ 交付一個不存在的取捨**」（claude.ai 犯·**KL** 現查抓出）。

🔴 **CC 自誌已一併入節（⛔ 不得只記 claude.ai）**：`D-2b-30` §3-2 四項前提由 CC 逐項複驗、
全部相符，且 CC **在同一批讀過** `_build_corner_range_v3` 回傳多邊形與
`_corner_buffer_S` 只收 `range_area`（**面積**）——**兩個事實都在手上**，卻仍照單量完。
⇒ **「前提全部相符」不蘊含「題目問對」**：前提查的是**單所述是否屬實**，
⛔ 不查「**單所問的是不是該問的問題**」。
🔒 **給 CC 之機械修法**：複驗前提之後、動手量測之前，須額外書面回答一句——
「若現行構造與正典不符，正解是否為『**直接改用正典構造**』？若是，本量測是否已含該格？」

🩸 **同批續發亦已入節**：本批 commit A 之 `grep -c` 引據錯（§1-4）⇒ 與**節 60** 同型。

---

## §4 全單驗收（⛔ 原樣貼）

### 4-1 `git diff --numstat 88afdc2 HEAD`（**刪除欄全 0**；**無任何 `.py` 異動**）

```
49	0	.claude/skills/failure-archaeology/SKILL.md
41	0	docs/W-G.9_第2宗最小規定範圍_範圍界定.md
1	0	docs/reports/W-G.4_泛用阻塞項登記表.md
19	0	docs/reports/W-G.8_D-2b-30_三圖入倉與K-9-5-11.md
（＋本報告）
$ git diff --name-only 88afdc2 HEAD | grep '\.py$'   ⇒ 空（rc=1）——本批零探針
```

### 4-2 `grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md` ⇒ **58**

⚠️ **由 57 變 58 係本批之預期變動**（§1-1 明令）——⛔ 不得與 `D-2b-29`／`D-2b-30`
之「錨須維持 57」混為一談：**彼二批加的是註解列、本批加的是<u>真阻塞項</u>**。

### 4-3 `grep` 自癒

| 落點 | 指令 | 結果 |
|---|---|---|
| §1-2 `GB-58` | `grep -n "^| \*\*GB-58\*\*" docs/reports/W-G.4_泛用阻塞項登記表.md` | 1 命中 |
| §1-3 `W-G.9` §七 | `grep -n "^## 六、動工前之已知前置\|^## 七、併入本波之項目" docs/W-G.9_第2宗最小規定範圍_範圍界定.md` | 二節同時命中 |
| §2-2 作廢註記 | `grep -n "76.162621\|72.536113\|D 欄作廢" docs/reports/W-G.8_D-2b-30_三圖入倉與K-9-5-11.md` | **原表數字（`:130`／`:132`）與作廢註記（`:134`）同時存在** ✅ |
| §3 考古 62 | `grep -n "^## 61\.\|^## 62\." .claude/skills/failure-archaeology/SKILL.md` | 二節同時命中 |

### 4-4 §1-2 六道複驗之指令與輸出 ⇒ **已於 §1-1 原樣貼出**

### 4-5 `env | grep WV_BAKE` ⇒ **空**（`rc=1`）

### 4-6 commit 分立 ＋ 機械時序證明

```
$ git log --oneline 88afdc2..HEAD
51569ad D-2b-31 commit B【零碼】D-2b-30 報告之「面積差」D 欄作廢註記
6409341 D-2b-31 commit A′【零碼】具名更正：commit A 之 grep 計數引錯
dd75f18 D-2b-31 commit A【零碼】GB-58 登記 ＋ W-G.9 §七 併入項目
$ git merge-base --is-ancestor dd75f18 6409341 ; echo rc=$?   ⇒ rc=0（A → A′）
$ git merge-base --is-ancestor 6409341 51569ad ; echo rc=$?   ⇒ rc=0（A′ → B）
$ git merge-base --is-ancestor 51569ad dd75f18 ; echo rc=$?   ⇒ rc=1（反向不成立）
```

### 4-7 §6 停機條件逐項

| # | 條件 | 判 |
|---|---|---|
| 1 | §1-2 六道複驗任一與所載不符 | ❌ 未觸發（**六道全部相符**·含兩個日期與一個 0） |
| 2 | §2-1 現載與實跑不符 | ❌ 未觸發（逐行相符） |
| 3 | 觸及任何實作（改構造／改表示法／改 `_corner_buffer_S`） | ❌ 未觸發（**本批零 `.py` 異動**·構造性證明見 §4-1） |

### 4-8 §0 不授權之逐項自證

| 項 | 狀態 |
|---|---|
| ⛔ 不新增或修改任何 `.py` | ✅ `--name-only \| grep '\.py$'` ⇒ 空 |
| ⛔ 不設 `WV_BAKE`／不改錨值／不放寬閘 | ✅ `env` 空；`--numstat` 無 `verify/` 列 |
| ⛔ 不刪不停用不標作廢任何既有機具 | ✅ 刪除欄全 0（構造性證明） |
| ⛔ 本批不實作任何修正 | ✅ 只登記與更正；`GB-58` 之處置欄逐字載「併入 `W-G.9` 一起做」 |

---

## §5 次步

1. 🔒 **`GB-58` 之實作已由 KL 裁定併入 `W-G.9`**——⛔ 不另開批次先改。
2. ⚠️ **`W-G.9` 動工前之已知前置現為 5 項**（原 4 項 ＋ 本批 §七 之 1 項）；
   其中 §2-1 之判讀仍標「**地基不成立、結論未定**」（`D-2b-30` §4）、**禁令未解**。
3. ⛔ **`D-3` 仍受 `K-9-5-10`【裁】1／2 拘束**（等結構閘另案修好；頂層 7 份採丙案）。
