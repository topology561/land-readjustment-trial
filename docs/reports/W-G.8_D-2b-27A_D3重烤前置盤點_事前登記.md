# W-G.8 D-2b-27 commit A：`D-3` 重烤前置盤點 —— **事前登記**（⛔ 零盤點輸出·零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**基準**：`cac03fb`
**編號現查**：`grep -rn "D-2b-27" .`（排除巢狀 worktree）⇒ **0 命中** ⇒ 可用。

> ⛔ **本 commit 之性質**：**只登記三個待驗命題**，
> ⛔ **零盤點輸出**（一份基準檔都還沒讀）、⛔ 零生產碼。
> 盤點結果與對帳於 commit C。**事前登記之全部價值在於它早於結果。**

---

## §0 本單為何存在（**轉述施工單·非本 CC 之判斷**）

`D-3` 重烤授權（KL 裁 2026-08-08）第 2、3 條要求「逐 `gid` 因果鏈歸因」且
「範圍限於因 `D-1`／`D-2`／`D-2b` 之分配變更而過期者」。
**本單只做盤點**，供 KL 據以裁定 `D-3` 之時點與範圍。
⛔ **本單不重烤、不判定該不該重烤**（施工單 §4-6 逐字：「那是 KL 之裁，本單只供事實」）。

---

## §1 事前登記（⛔ **事後不得修改**）

🔒 **P1／P3 係 claude.ai 現查所得，⛔ 不得引為事實；本單即為驗之。**（施工單 §1 逐字）

### P1

> **現用基準之產出者為 KL 之 Streamlit UI 實跑、非 harness 生成**
> （`verify/baselines/PROVENANCE.md` 自載）。
> ⇒ 若查得**任一現用基準實為 harness 產出**，即 **P1 部分證偽**，如實列出。

**本 CC 之預先承諾**：
⛔ 不得因「PROVENANCE.md 這樣寫」就判 P1 成立——**須以檔案 git 歷史或欄名對應獨立佐證**；
佐證不足者一律標「**不可判定**」並具名理由（施工單 §2-a 逐字：⛔ 不得留白、⛔ 不得猜）。

### P2

> **自 `D-1` 起至現行 HEAD，`verify/baselines/` 之現用檔<u>未被任何 commit 修改過</u>**
> （`D-3` 重烤授權第 1 條：`D-2b` 及其之前一律不得重烤）。
> ⇒ 查得**任一現用基準曾被修改** ⇒ **🛑 停機上呈**。

**本 CC 之預先承諾**：P2 之驗證須以 **`git log --follow` 逐檔**為之，
⛔ 不得以「我沒改過」或「本 session 未動」代替——**本 session 之外之 commit 亦在射程內**。

### P3

> **`WV_BAKE` 之 dump 來源為驗證程式自身**（`verify/run_verification.py`），
> 與 P1 之產出路徑**不同源**。
> ⇒ 若二者實為同一路徑，**P3 證偽**，如實列出。

**本 CC 之預先承諾**：⛔ **不得設 `WV_BAKE` 來驗 P3**（施工單 §4-1 之不授權）；
須以**讀碼／AST**判定其 dump 之寫出點，並以 `env | grep WV_BAKE` 之空輸出正面自證未設。

---

## §2 三個命題之處置矩陣（**事前約定**·⛔ 事後不得改判準）

| 命題 | 成立時 | 證偽時 |
|---|---|---|
| **P1** | 於報告載明佐證來源（⛔ 不只引 `PROVENANCE.md` 自載） | **如實列出**（⛔ **非**停機條件·施工單 §5 逐字） |
| **P2** | 於報告載明逐檔 `git log` 之輸出 | 🛑 **停機上呈**（施工單 §5-1） |
| **P3** | 於報告載明 dump 寫出點之碼面指認 | **如實列出**（⛔ **非**停機條件） |

---

## §3 commit C 將回答之四問（**事前列出·⛔ 屆時不得減項**）

母體 ＝ `find verify/baselines -type f` 之**全集**；凍存子目錄**具名列出並標為凍存**。
對每一份**現用**基準之**每一欄**：

| 問 | 內容 | 事前約定之嚴格性 |
|---|---|---|
| **(a)** | **產出者**：KL UI 實跑／harness `WV_BAKE` dump／手造／**不可判定** | 判定須附證據；⛔ 不可判定者照實標並具名理由、⛔ 不得留白、⛔ 不得猜 |
| **(b)** | **相對 StepG 之位置**：上游／下游 | 須由**產出鏈**指認（哪一段程式寫出該欄）＋ **grep 指認**；⛔ 不得以欄名字面推定 |
| **(c)** | **是否因本波過期**：受 `D-1`／`D-2`／`D-2b` 之**具名變更**影響？ | 受影響者須指名到**具體變更**（commit 或裁示編號）；⛔「受本波影響」一語不算具名。**未受影響者亦須具名說明何以不受影響** |
| **(d)** | **重烤路徑之影響**：循 `WV_BAKE` 重烤是否改變其產出者 ⇒ 是否破壞 `PROVENANCE.md` 所賴之「與 driver 非同源」性質 | — |

**另須答之一問（施工單 §3）**：現行 harness 之 StepG 於首個街廓失敗即整趟中止
（`GB-52`／`GB-54`）⇒ 在此狀態下執行 `WV_BAKE`，**哪些欄會拿不到值**、**哪些欄仍拿得到值**。
⛔ **不得以「應該都拿不到」概括，須逐欄由產出鏈判定。**

---

## §4 不授權逐項（**事前自我約束**）

| # | 項 | 本 commit A 之狀態 |
|---|---|---|
| 1 | ⛔ **不得設 `WV_BAKE`** | ✅ 未設（commit C 將貼 `env \| grep WV_BAKE` 之空輸出正面自證） |
| 2 | ⛔ 不得修改 `verify/baselines/` 下任何一個檔（含凍存） | ✅ 未動 |
| 3 | `app.py`／`verify/**` 一字不動 | ✅ 未動 |
| 4 | ⛔ 不改任何錨值／不放寬任何閘／不改期望 FAIL 名單 | ✅ 未動 |
| 5 | ⛔ 不實作 `GB-53`〜`GB-57` 之任何修法 | ✅ 未動 |
| 6 | ⛔ 不判定「該不該重烤」「何時重烤」 | ✅ 本單只供事實 |

---

## §5 生產碼零觸自證（**原樣貼**）

```
$ git diff --stat -- app.py verify/
```
（**無輸出**。）

---

## 異動清單（commit A）

| 檔 | 異動 | 性質 |
|---|---|---|
| `docs/reports/W-G.8_D-2b-27A_D3重烤前置盤點_事前登記.md` | 新增（本檔） | 文件·**零盤點輸出** |
