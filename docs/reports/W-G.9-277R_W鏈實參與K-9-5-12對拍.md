# `W-G.9-277R`　`W` 鏈之逐宗實參與 `K-9-5-12` 之對拍 ／ 二解算器之不一致 ／ `_宗地寬度` 之受詞

> **態**｜開工 ＝ `52cf3a39d2b9b543b71cada1452ca3c1dc3a5075`（`ls-remote` 實查）｜工項零後 ＝ `b763d694b74ac21c80c75ce10bc5b7c9b3b538a4`
> **單** ＝ `docs/orders/W-G.9-277_施工單_W鏈實參與K-9-5-12之對拍.md`（工項零已原封入倉）
> **分級** ＝ **中**（含雙態驅動·**零生產碼**）｜**生產碼 `34` 檔一字未動**
> 🛑 **本報告⛔ 判實跑側違反 `K-9-5-12`、⛔ 判孰為正確之 `W_0`／`W`、⛔ 判二解算器孰誤、⛔ 判該檢是否退化為缺陷、⛔ 提修法主張。**

---

## `§零`　開工閘與工項零

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `52cf3a3…` 或其後裔 | `52cf3a39d2b9b543b71cada1452ca3c1dc3a5075` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 逐位相同 | ✅ |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須**非**祖先 | **非祖先** | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `49e3f93e205f37bd1a3fef5a6445bd7c221ef5b72b2cb17805c93d28b19319a5` | **逐位相符**（受詞 `11765` B·全檔 `11843` B／`CR` `0`） | ✅ |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 不紅 | **停機款 `0`**；🟡 **提示 `3` 項**（逐項處置見下） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `369`／`379`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `81`／`81` | **逐位相同**（坑判別力二造 造甲 `6 ≥ 1` ✅／造乙 `0`／`0` ✅） | ✅ |
| `7` | `verify/baselines` | `298` 檔·`a898f4e1…`（口徑 ＝ `core.quotePath` **預設**） | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |

🔒 **閘 `4` 之判別力**：對**全檔**施同口徑 ⇒ **相異**；受詞多 `1` byte ⇒ **相異** ⇒ 非恆綠。

### 零-1　🟡 **`3` 項提示之逐項處置**（⛔ 靜默略過·`W-G.9-190R §九（乙）` 所令）

| 項 | 列 | 受詞（節略） | **處置** |
|---|---|---|---|
| `P-1` | `:94` | 「全稱否定而同段⛔ 具名母體／所查範圍」——其所咬者為 `**已知（K-9-5-6 之 D-2b-5 具名更正·逐字）**：` 之後所引之「**無任何檢測捕捉之**」 | 🔒 **具名豁免**：該句係**逐字引用**倉內既有正典（`K-6:872` 之 `D-2b-5` 段），**⛔ 本單之新宣稱** ⇒ 其母體屬**被引文之原脈絡**。**併採納**：本報告 `§二-1` 對該述詞**另行自碼面現查**（AST），⛔ 採其轉述 |
| `P-4` | `:75` | 「方向性轉引而同段⛔ 具名座標系」——`W_0` ＝ **MP→corner** | 🔒 **採納**：其箭頭之座標系 ＝ **幾何量測之起訖**（自 `MP` 量至 `corner`），**⛔ 「改前→改後」之時序座標系**。本報告凡引該形皆同格具名 |
| `P-4` | `:106` | 同上——`W_i` ＝ **MP → 本宗遠側分配界線**之垂距 | 🔒 **採納**：同上，其為**量測之起訖**、⛔ 時序 |

**工項零**（commit `b763d69`·`§二-B` 搬移法）：
所指之檔數 ＝ **`1`**（判別力：造乙 人造名 ⇒ `0`／造甲 `W-G.9-276` 之單 ⇒ `1`）；
`CR` ＝ **`0`**（`LF` `141`·bytes `11843`；判別力：真 CRLF 人造檔 ⇒ `CR = 2` ✅）；
blob `sha1` ＝ `03fe3023c6e06ef72d24dba11517c0688f598111`；
**blob 內容 `sha256` ＝ 磁碟 ＝ 源 ＝ `913e0a47f3d7b6c27f43f0b1f3a7a0a1fd3469abd40309a8178425ccda1bf119`** ⇒ **逐位相同** 🟢。

---

## `§一`　工項 `1`：`W` 鏈之逐宗實參 ＋ `W_prev` 之受詞（**主檢**）

**器** ＝ `verify/probes/probe_WG9277_wchain.py`｜**落檔** ＝ `verify/out/WG9277R_wchain.log`
**辦法** ＝ 包裹 `ns["rw_increment"]`，錄其**實參 `(W_prev, W_cur)`／回傳**，並以**框內省**
錄其呼叫端之**解算器名／街廓／側別／宗**及 `is_corner`／`is_chain_head`／`_W_near`／`_rw_start`。

### 一-1　判別力（**逐態各備**·承 `§二` 之通則）

| 情境 | 態 | 必命中對照 `rw_increment` 呼叫數 | 判 | 必為零對照（人造實參）之命中 | 判 |
|---|---|---|---|---|---|
| `0m` | 甲 | **`1834`** | ✅ `>0` | **`0`** | ✅ |
| `0m` | 乙 | **`1590`** | ✅ `>0` | **`0`** | ✅ |
| `3.5m` | 甲 | **`1813`** | ✅ `>0` | **`0`** | ✅ |
| `3.5m` | 乙 | **`1560`** | ✅ `>0` | **`0`** | ✅ |

⇒ 🔒 **四態之必命中對照皆 `>0`** ⇒ **器非紅（逐態）**——即 `§二` 通則之**首次施行**。
🔒 **自我驗證閘（坑 `bb`）**：`run_step_g` 段所錄之 `blk_label` ＝
態甲 `['R1','R2','R3','R4','R5','R6']`／態乙 `['R1','R2','R4']`，**皆為 `BLKS` 之成員且非空** ⇒ 器非紅。
🔒 **確定性**：本器**跑二輪**（初版／修正版），其**全部表列**逐位 `diff` **相同**。

### 一-2　項 `1`：`R4` `right` 之宗序列與逐宗 `W_i`／`Rw_i`（**未捨入**）

| 情境 | 態 | `R4` `right` 定案筆數 | # | 宗 | side | solver | `is_corner` | `is_chain_head` | `_W_near` | `_rw_start`（＝實參 `W_prev`） | `W`（＝實參 `W_cur`） | `Rw_i`(%) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `0m` | 乙 | **`1`**（`R4` 全側 `2`） | 1 | `628-1(1)` | 右側 | `solve_G_binary` | `True` | `True` | `0.1775004071` | **`0.1775004071`** | **`14.1988975422`** | `85.407634` |
| `3.5m` | 乙 | **`1`**（全側 `2`） | 1 | `628-1(1)` | 右側 | `solve_G_binary` | `True` | `True` | `0.1775004071` | **`0.1775004071`** | **`14.1988975422`** | `85.407634` |
| `0m` | 甲 | 🟡 **`0`**（**⛔ 靜默退路**：下列為 `R4` **全側** `1` 筆） | 1 | `628(1)+` | **左側** | `solve_G_binary` | `True` | `True` | `4.4804447558` | `4.4804447558` | `65.1439433622` | `59.221243` |
| `3.5m` | 甲 | 🟡 **`0`**（同上·全側 `1` 筆） | 1 | `628(1)+` | **左側** | `solve_G_binary` | `True` | `True` | `7.7850157181` | `7.7850157181` | `68.6244026113` | `40.060915` |

🛑 **態甲之 `R4` 無 `right` 側**（步驟 `0` 前置合併 ⇒ `628(1)` 與 `628-1(1)` 併為 `628(1)+`·單一宗於**左側**）
——本器**明載其退為全 `R4`**，⛔ 靜默頂替。

### 一-3　項 `2`／`3`：`W_prev = 0.1775004071` 之**序位**

🔒 **於定案筆中之命中 ＝ `2` 筆**（`0m` 態乙／`3.5m` 態乙·皆 `R4`·`右側`·宗 `628-1(1)`）。
🔒 **其序位 ＝ <u>鏈頭之 `W_0`</u>，⛔ 前一宗之 `W_i`**——機械依據三項（**⛔ 推定**）：

| 依據 | 實測 |
|---|---|
| `is_corner` | **`True`** |
| `is_chain_head` | **`True`** |
| 本側 `_lg_idx_right` | **`0`** |
| `_W_near`（該宗自量之近側垂距） | `0.17750040709932335` |
| `_rw_start`（＝實參 `W_prev`） | `0.1775004071` ⇒ **`_rw_start ← _W_near`** |

🔒 **碼面之決定式逐字**（`app.py:10244` 之上一列·AST 所在 ＝ `solve_G_binary`）：
`_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)`
⇒ `is_corner ∨ is_chain_head` 為 `True` ⇒ **取 `_W_near`**（⛔ 外層 thread 之 `W_prev`）。

🔒 **項 `3` 所令之同格並列**——`K-9-5-12（五）` 之逐字
（由 `sed -n '1983,1988p'` 之輸出**逕行落檔**·`常規七 一`｜列數 **`6`**｜位元組 **`388`**｜
`sha256` **`bf40f65019573eed89f3db423be048cc17c035599f72d9148b8dd80db079a19f`**｜**覆核**：與真 `sed` 逐位相同）：

<!-- ===== BEGIN 逐字塊（K-6 :1983-:1988）===== -->
#### （五）效力

1. **首宗起點一律為 `0`**（即自 **MP** 起量）。
2. `K-9-5-6` 之 `- 🔒 **首宗起點**：`W_0` ＝ MP→corner …` 一行之**前半自本批起失效**
   （該行原句**一字未刪**，已就地加註並指向本節）。
3. ⚠️ 該行**後半**（`forced` 時為其 buffer 起算點）**⛔ 未撤，列為未決**——見該處加註。
<!-- ===== END 逐字塊 ===== -->

🔒 **同格具名之 `_fo`**（`§三-1` 項 `3` 所令）：該側之 **`_fo_right` ＝ `False`**
（`0m` 態乙／`3.5m` 態乙 皆然·源 ＝ `f3L_forced_offset`）⇒ **該側非 `forced`**。
🛑 **⛔ 判其是否違反 `K-9-5-12`**（單 `§六-4` 明令）——**並列即止**。

### 一-4　項 `4`：`0.1775004071` 之**產生式逐字**（字樣錨·⛔ 行號為錨）

其被賦值之運算式（於 `solve_G_binary` 內·**字樣錨** `grep -F` 固定字串）：

| # | 逐字 |
|---|---|
| `1` | `_ahat = _n_alloc if float(np.dot(_dhu, _n_alloc)) >= 0.0 else -_n_alloc` |
| `2` | `_bp_w = np.asarray(baseline_pt, dtype=float)` |
| `3` | `_mp_w = np.asarray(side_mid, dtype=float)` |
| `4` | **`_W_near = float(np.dot(_bp_w - _mp_w, _ahat))`** |
| `5` | `_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)` |

⇒ 🔒 **`0.1775004071 ＝ (baseline_pt − side_mid) · â`**——其**座標系 ＝ 幾何量測之起訖**
（自 `side_mid`〔＝ `MP`〕量至 `baseline_pt`，投影於 `â`），**⛔ 「改前→改後」之時序**（`P-4` 之採納）。

### 一-5　項 `5`：**鏈頭之 `W_0`** 全母體值表（六街廓 × 二側 × 二情境 × 二態）

| 街廓 | 側 | `0m` 態甲 | `0m` 態乙 | `3.5m` 態甲 | `3.5m` 態乙 |
|---|---|---|---|---|---|
| `R1` | left | `-0.0000000000`（`628-37(1)`） | `-0.0000000000`（`628-37(1)`） | `-0.0000000000`（`628-36(1)`） | `-0.0000000000`（`628-36(1)`） |
| `R1` | right | `-0.4007179780`（`628(5)`） | `-0.4007179780`（`628(5)`） | `-0.4007179780`（`628(5)`） | `-0.4007179780`（`628(5)`） |
| `R2` | left | `0.0000000002`（`628-41(1)`） | `0.0000000002`（`628-41(1)`） | `6.9804804474`（`628-42(1)`） | `6.9804804474`（`628-42(1)`） |
| `R2` | right | — | — | — | — |
| `R3` | left | — | — | — | — |
| `R3` | right | `-3.3976107559`（`628-30(3)+`） | — | `-3.3976107559`（`628-30(3)+`） | — |
| `R4` | left | `4.4804447558`（`628(1)+`） | `-0.0000000002`（`628(1)`） | `7.7850157181`（`628(1)+`） | `-0.0000000002`（`628(1)`） |
| `R4` | right | — | `0.1775004071`（`628-1(1)`） | — | `0.1775004071`（`628-1(1)`） |
| `R5` | left | `-0.0000000002`（`628-18(2)`） | — | `-0.0000000002`（`628-45(1)+`） | — |
| `R5` | right | — | — | — | — |
| `R6` | left | — | — | — | — |
| `R6` | right | `-0.0000020570`（`628-18(1)`） | — | `-0.0000020570`（`628-18(1)`） | — |

🔒 **有值之格 ＝ `24`**｜**嚴格 `== 0` ＝ `0`**｜**`≈ 0`（`0 < |W_0| ≤ TOL0`）＝ `10`**｜**`|W_0| > TOL0` ＝ `14`**
（**零之判準 ＝ 具名常數 `TOL0 = 1e-06`**·⛔ 字面·⛔ 二分）。
🔒 **判別力**：二類**並存**（`10` vs `14`）⇒ **該欄具鑑別力** ✅。
🩸 **本批之自捕**：初版以 `abs(v) < 1e-12` 為零判 ⇒ `-2.057e-06`／`2e-10` 等**全被歸入「非 `0`」**
⇒ 得「`0`／`24`·🟡 單一值」之**偽單一類**；改為**三分類 ＋ 具名常數**後方顯出二類並存。
🛑 **⛔ 判「`W_0` 應否為 `0`」**（單 `§六-4`）——**出艙即止**。
🔒 **`_fo_left`／`_fo_right` 全表**（同格具名）見落檔 `verify/out/WG9277R_wchain.log`；
其中 `True` 者 ＝ `R2 left`（`3.5m` 二態）／`R3 right`（`3.5m` 態乙）／`R4 left`（`0m`／`3.5m` 態甲）／
`R5 left`（`3.5m` 態乙），合計 **`6`**。

---

## `§二`　工項 `2`：二解算器之不一致 ／ 本案之 `理論@k*` 出自何者

### 二-1　項 `1`：二解算器內 `rw_increment(...)` 之**呼叫式逐字**（**自碼面現查**·⛔ 採轉述）

🔒 取法 ＝ **AST `Call` 節點**（⛔ 文字層啟發式·`自誤 377`）。

| 解算器 | 區間 | `app.py:` | `rw_increment` 之**實參**（`ast.unparse`） | 該列逐字 |
|---|---|---|---|---|
| `solve_G_binary` | `:10083`–`:10352` | `:10244` | **`_rw_start, W`** | `Rw = rw_increment(_rw_start, W)` |
| `iterate_G_S` | `:6401`–`:6492` | `:6458` | **`W_prev, W_cur`** | `Rw = rw_increment(W_prev, W_cur) if _apply_rw else 0.0` |
| `iterate_G_S` | 同上 | `:6486` | **`W_prev, W_cur`** | `Rw_pct = (rw_increment(W_prev, W_cur) * 100.0) if _apply_rw else 0.0` |

🔒 **呼叫節點數**：`solve_G_binary` ＝ **`1`**／`iterate_G_S` ＝ **`2`**。
🩸 **二法之別（本批自捕）**：**文字層**之同一計數於 `iterate_G_S` 得 **`3`**
——其多咬一列 `app.py:6428`（**docstring** 之 `第 k 輪：W_cur = W_prev + S_k；Rw = rw_increment(W_prev, W_cur)`）
⇒ **AST 與文字層相異** ⇒ 本表一律以 **AST** 為準。
🔒 **判別力**：一人造字面（執行期組出）於 `app.py` ＝ **`0`**；對照組 `rw_increment(` ＝ **`7`** 列 ⇒ 器非紅。

🔒 **與 `D-2b-5` 具名更正之對照（照實·⛔ 判孰誤）**：該更正逐字稱
「`iterate_G_S`（代數）之路徑**一直合規**（`rw_increment(W_prev, W_cur)`）；
`solve_G_binary`（幾何）之路徑偏離（`rw_increment(_W_near, W)`）」。
**現行碼面**：`iterate_G_S` 之實參**仍為** `W_prev, W_cur`（**與該述相符**）；
`solve_G_binary` 之實參**已非** `_W_near, W`，而係 **`_rw_start, W`**
（`_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)`）
⇒ 🔒 **該「偏離」之描述係<u>改前</u>之態**；`K-9-5-6` 之校回**已落地於碼面**。
🛑 **⛔ 判二者現是否一致、⛔ 判孰誤**（單 `§六-4`）——**出艙即止**。

### 二-2　項 `2`：本案之 `理論@k*` 係經**哪一解算器**產生

🔒 **框內省之實測**（`rw_increment` 之呼叫端·逐情境 × 逐態）：

| 情境 | 態 | `solve_G_binary` | `iterate_G_S` | 其他 |
|---|---|---|---|---|
| `0m` | 甲 | **`1834`** | **`0`** | （無） |
| `0m` | 乙 | **`1590`** | **`0`** | （無） |
| `3.5m` | 甲 | **`1813`** | **`0`** | （無） |
| `3.5m` | 乙 | **`1560`** | **`0`** | （無） |

⇒ 🔒 **全母體之 `rw_increment` 呼叫<u>無一</u>出自 `iterate_G_S`**；
本案（`0m` 態乙 `R4 right`·宗 `628-1(1)`）之該筆，其 solver 欄實測為 **`solve_G_binary`**。
🔒 **`理論@k*` 之輸入鏈**：`理論@k*` 由 `_select_pool_slot(widths, …)` 算得，
其 `widths` 之來源逐字 ＝ `_widths_local[entry['_ov2_idx']] = float(res.get('_宗地寬度', 0.0) or 0.0)`，
而 `res` 即 `_solve_one` 之回傳 ⇒ **本案之 `res` 出自 `solve_G_binary`**（上表·`iterate_G_S` 計數 `0`）。
🔒 **實測之 `widths`**（`0m` 態乙·`R4`）＝ **`[46.08, 14.01]`**｜`_N` ＝ `2`｜**`k*` ＝ `1`**。

### 二-3　項 `3`：`逐界面 Δ_i == 0` 之檢於**界面 `0`** 時之行為

🔒 **逐字（列框命中皆 `1`·母體 ＝ `verify/stepg_pipeline.py` 單檔）**：

| 錨（逐字） | 列 |
|---|---|
| `_bad_a = [d for d in _ifs_a if abs(d['delta']) > _IFACE_TOL]` | `:1461` |
| `if _bad_a:` | `:1462` |
| `return (f"[Δ_i·{blk_label}·{_tag}] " + ("（無界面）" if not _ifs else "　".join(` | `:1347` |

🔒 **機械事實（⛔ 判）**：`_iface_deltas` 僅於 `_prev_far is not None` 時 `append`
⇒ **`len(_nodes) <= 1` 時回傳 `[]`** ⇒ `_bad_a` ＝ 空列表之過濾 ＝ `[]`
⇒ `if _bad_a:` **不成立** ⇒ 該 `raise` **不執行**；`_fmt_ifaces` 印「（無界面）」。
🔒 **本案之實測**：`R4 right` 之**定案宗數 ＝ `1`**（`§一-2`）⇒ **界面數 ＝ `0`**
⇒ 該檢之判定組為空 ⇒ **空集之全稱判定恆真**。
🔒 **實際拋出者係其後之<u>第二閘</u>**，其逐字 ＝
`🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）｜參考 ΣΔ_i=+0.000（**⛔ 未用於本式**·實測反證見碼註）｜逐界面：[Δ_i·R4·right] （無界面）`
🛑 **⛔ 判該檢是否退化為缺陷**（單 `§三-2`／`§六-4` 明令）——**出艙即止**。

---

## `§三`　工項 `3`：`_宗地寬度` 之產生式 ＋ 「虛胖陷阱」之逐字對拍

**器** ＝ `verify/probes/probe_WG9277_width.py`｜**落檔** ＝ `verify/out/WG9277R_width.log`

### 三-1　產生式（**AST `Assign` 實查**·母體 ＝ `app.py` ＋ `verify/*.py` 單層）

| 檔:列 | 逐字 | RHS（`ast.unparse`） | 包層 |
|---|---|---|---|
| `app.py:22013` | `_res['_宗地寬度'] = round(_pw, 2)` | `round(_pw, 2)` | `main > _mark_zaling` |
| `verify/stepg_pipeline.py:735` | `_res['_宗地寬度'] = round(_pw, 2)` | `round(_pw, 2)` | `_run_step_g_impl > _mark_zaling` |

⇒ **賦值節點總數 ＝ `2`**（**唯一之二處**·app 路徑與 harness 路徑各一）。
🔒 **其 `_pw` 之逐字** ＝ `_pw = float(_res.get('S', 0.0)) * _cos_dn`
（`verify/stepg_pipeline.py:734`·字樣錨 `grep -F`）。
🔒 **`_cos_dn` 之逐字** ＝ `_cos_dn = abs(float(_np_d.dot(_dh0 / _dl0, _np_d.asarray(_n_alloc_blk, dtype=float))))`
（`:727`；其前後之 `_cos_dn = 1.0` 為二處預設·`:721`／`:730`）。

### 三-2　🔴 其**受詞**

> 🔒 **`_宗地寬度` ＝ `S × |cos(d̂, n_alloc)|`**
> ——其係「**沿 `FRONT_LINE` 之推進量 `S`**」之<u>投影</u>，
> 🛑 **⛔ 「何二物之距」**（⛔ 垂距形式、⛔ 以二界線為端）。

🔒 **對比（同格並列·⛔ 判）**：同一宗（`0m` 態乙·`R4`·`628-1(1)`）之三量

| 量 | 值 | 其產生式 |
|---|---|---|
| `宗地寬度(m)` | **`14.01`** | `round(S × \|cos(d̂, n_alloc)\|, 2)`（**`S` 之投影**） |
| `W(m)` | **`14.2`** | `W = float(np.dot(_bp_w + S_guess * _dhu - _mp_w, _ahat))`（**`MP` → 遠側界之垂距**·`intrinsic` 直量） |
| `Rw(%)` | **`85.41`** | `rw_increment(_rw_start, W)`·未捨入 `85.407634` |

🔒 **`理論@k*` 所餵者 ＝ `宗地寬度(m)`**（`widths ＝ [46.08, 14.01]`·`k* = 1`）
⇒ **`理論@k* = R(14.01) = 87.835`**；**實跑 ＝ `R(14.1989) − R(0.1775) = 85.407634`**。

### 三-3　「虛胖陷阱」之**逐字**（正面列舉受檢檔·⛔ 全倉 grep 再排除）

| 檔 | 列數 | 逐字 |
|---|---|---|
| `app.py` | **`2`** | `:10191` `#   W_i ＝ mp → 本宗**遠側**分配界線之垂距（**intrinsic 直量**·非 telescoping〔虛胖陷阱〕）。`<br>`:10222` ``#     不觸「虛胖陷阱」`:9344`）——**累積的是 `Rw`、不是 `W`**。`` |
| `docs/rulings/K-6_…md` | `1` | ``:890  ——⛔ 非以寬度累加堆出（「虛胖陷阱」`grep -n "虛胖陷阱" app.py` ⇒ `:9344`）。`` |
| `docs/配地計算總規格_v3.md` | `1` | `:40  >    判畸零地用 W 不用 S（S 斜切虛胖陷阱，如 170°/10° 平行四邊形狹長地）。` |
| `docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md` | `1` | ``:10  2. **脫鉤**：現碼 `W = Σ S·cos_dn`（自 p2 切線起算·寄生於 S）**廢**——違反「W 用垂距不用 S（虛胖陷阱）」正典。`` |
| `docs/specs/W-G.4_規格_v3.md` | `2` | ``:294  **🔴 脫鉤（補丁六 §一.2·鎖定）**：現碼 `W=Σ S·cos_dn`（…**寄生於 S**）**廢**——違反正典「W 用垂直 ALLOC 之距離·不用 S（虛胖陷阱）」…``<br>`:384`（`T1` 段） |
| `.claude/skills/cad-layer-semantics/SKILL.md` | `1` | `:28  - W（含街角 W_i）**⊥ALLOC 量測**、非 ∥側街：…（虛胖陷阱）。` |

🔒 **判別力**：一人造同形字樣（執行期組出）於 `app.py` ＝ **`0`**；對照組 `虛胖` @ `app.py` ＝ **`3`** 列 ⇒ 器非紅。

🔴 **二項照實併記（⛔ 判、⛔ 動生產碼一字）**
1. **`app.py` 內⛔ 有「虛胖陷阱」之<u>定義段</u>**——其 `2` 列**皆為引用**；該詞之定義文字在
   `docs/配地計算總規格_v3.md:40`／`docs/specs/…補丁六…:10`／`.claude/skills/…SKILL.md:28`。
2. 🩸 **`app.py:10222` 與 `K-6:890` 所載之錨 `` `:9344` `` <u>已腐</u>**——`app.py:9344` 現為
   `_corner_buffer_S` 之**方位契約**段（逐字 ＝ `故本契約靠**呼叫端正確**＋下列 assert 之粗篩，**不可倚賴函式自行糾正**。`），
   **與「虛胖陷阱」⛔ 相關**。此即 `CLAUDE.md` `行號衛生` 第 `3`／`5` 款之形
   （**既有之失準行號永遠留在原地作為引述**）。
   🛑 **本批⛔ 修**（`§六-1`：⛔ 動生產碼 `34` 檔一字；`K-6` 亦⛔ 在本批授權內）——**只登記**。
🛑 **⛔ 判 `_宗地寬度` 是否落入虛胖陷阱**（單 `§三-3`／`§六-4`）——**並列即止**。

---

## `§四`　工項 `4`：`自誤 380` ＋ `§二` 之末端追加

### 四-1　`自誤 380`

**落點** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（嚴格前綴 ＝ `True`；
判別力：`old + b'X'` 為前綴 ⇒ `False` ✅）｜長度 `785957` → `793242`（**`+7285` B**·含 `§四-2`）｜`CR` ＝ `0`。
**受詞逐字塊**由 `sed -n '35,44p'` 之輸出**逕行落檔**（`常規七 一`）：
列數 **`10`**｜位元組 **`1599`**｜`sha256` **`a8faffc949ab9fcb58343e6cf60d777c93e4f57ac1630b47fa4a2f1ec35998ab`**；覆核逐位相同 ✅。

**取號現查**（三軸｜`(1)` `blob@b763d694b74ac21c80c75ce10bc5b7c9b3b538a4`｜
`(2)` 取最大號 ＝ 登記表**單檔**（`785957` B／`9268` 列）·查占用 ＝ 全倉追蹤檔 **`2172`** 檔（讀不到 `0`）｜
`(3)` **檔框 ＋ 列框**）：
**取最大號** ＝ 命中列 `381`／相異 `369`／**MAX `379`** ⇒ **次號 `380`**；
**查占用**（**B 形 ⋀ C 形並取**·`GB-158`）：受詢 `380` ＝ 鬆 `1`／`4`·B `1`／`1`·C `1`／`4`；
對照甲 `379` ＝ `4`／`15`·`3`／`3`·`4`／`14`；甲2 `378` ＝ `8`／`25`·`3`／`3`·`8`／`23`；
乙 人造哨兵 ＝ `0`／`0` ⇒ **器非紅**。
**逐筆歸類**：受詢之命中**全數集中於本單自身** ⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號。

### 四-2　`§二` 之末端追加（**⛔ 鑄任何號**）

節題由 `sed -n '53,53p'` 之輸出**逕行落檔**（列數 `1`／位元組 `189`／
`sha256` `8192e848e4e1bd92203e93f0c55d3dc3f7b03400a98fa1e0c13a41f43dab71ae`）。
其內載 `自誤 345` 再現之**第二例**、通則之補，及**本批之首次施行**
（改以 `rw_increment` 自身為必命中對照·四態皆 `>0`）。

🩸 **同族之新捕（已登記於該節·⛔ 鑄號）**：`CLAUDE.md` 之「零對照之出艙⛔ 構成意指占用」一款
解決者為**取號側**；其**量測器側**之副作用仍在——本批實測 **坑 `bz`**（前批之「必為零」對照，
其字面已隨 `W-G.9-276R` 出艙入倉）於同一檢**得 `1` 檔／`1` 列** ⇒ 該對照**已失其「必為零」之性質**
⇒ **量測器紅**。🔒 **攔法** ＝ 必為零之對照一律**執行期組出**且字面⛔ 出艙（`GB-147`）。
🛑 **只登記**；是否立為明文，候發單側。

---

## `§五`　本批之**自解清單**（`作業常規之追加五 §一` 款 `5`）

| # | 疑義逐字 | 二個以上之讀法 | **所取者** | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§三-1` 項 `1`「**`R4 right`** 之該側宗序列」——而**態甲之 `R4` 無 `right` 側** | `(a)` 靜默退為全 `R4`；`(b)` **明載其退路**並逐列具名側別；`(c)` 出艙「⛔ 可得」 | **`(b)`** | 態甲 `R4` `right` 定案筆數 ＝ `0`、全側 ＝ `1`（`628(1)+`·**左側**）；步驟 `0` 前置合併之後果 | `(a)` 違「無靜默退路」；`(c)` 過嚴——該態確有 `R4` 之資料可出艙，具名即可 |
| `2` | 單 `§三-2` 項 `2`「`理論@k*` 係經**哪一解算器**產生」——`理論@k*` 實由 `_select_pool_slot` 算得、⛔ 由解算器直接產生 | `(a)` 答「皆非」；`(b)` 以其**輸入鏈**定之（`widths ← res['_宗地寬度'] ← _solve_one`） | **`(b)`** | `widths` 之來源逐字 ＝ `_widths_local[entry['_ov2_idx']] = float(res.get('_宗地寬度', 0.0) or 0.0)`；全母體 `iterate_G_S` 計數 ＝ `0` | `(a)` 字面為真而**無資訊**——單所問者實為「該 `res` 出自何者」 |
| `3` | 單 `§三-3`「『虛胖陷阱』**該處註解**之全段逐字」——`app.py` 內⛔ 有定義段、且其錨 `:9344` 已腐 | `(a)` 停機請補；`(b)` **照實具名其⛔ 存在 ＋ 錨已腐**，並補引其**真正之定義處**逐字 | **`(b)`** | `app.py` 之 `虛胖陷阱` ＝ `2` 列（**皆引用**）；`app.py:9344` 現為 `_corner_buffer_S` 方位契約段；定義文字在 `docs/配地計算總規格_v3.md:40` 等 `4` 檔 | `(a)` 過度——所需之逐字**倉內確有**，只是不在單所指之處；照實具名即滿足其受詞 |

🔒 **三項皆滿足自解五要件**：零土地後果／零生產碼（`34` 檔一字未動）／可機驗（證據欄·皆附必紅必綠二組）／
可逆（新建檔 ＋ append-only·**本批⛔ 就地更正任何既有檔**）／出艙具名（本表）。
🔒 **黑名單之逐項自檢**：⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字（含註解）；
二讀法之後果**⛔ 在土地上相異**；**⛔ 新訂準據**；⛔ 廢止或放寬任何既有之閘。

## `§六`　🔴 **出艙即止之事**（⛔ 判、⛔ 提主張·單 `§六-4`）

1. `W_prev = 0.1775004071` ＝ **鏈頭之 `W_0`**（`is_corner`／`is_chain_head` 皆 `True`·`_lg_idx_right = 0`），
   其 ＝ `_W_near = (baseline_pt − side_mid) · â`；該側 `_fo_right = False`。
2. `K-9-5-12（五）1` 逐字 ＝ 「**首宗起點一律為 `0`**（即自 **MP** 起量）」——**已同格並列**。
3. 全母體 `24` 格之鏈頭 `W_0`：`≈ 0`（`|W_0| ≤ 1e-06`）**`10`** 格／`> 1e-06` **`14`** 格。
4. 本案之 `res` 出自 **`solve_G_binary`**；全母體 `iterate_G_S` 之 `rw_increment` 計數 ＝ **`0`**。
5. `逐界面 Δ_i` 之檢於**界面 `0`** 時**判定組為空 ⇒ 該 `raise` 不執行**；本案界面數 ＝ `0`。
6. `_宗地寬度 ＝ S × |cos(d̂, n_alloc)|`（**`S` 之投影**），而 `W ＝ MP → 遠側界之垂距`；
   `理論@k*` 所餵者為**前者**（`14.01`）、實跑所用者為**後者**（`14.1989`／`0.1775`）。
7. `app.py:10222`／`K-6:890` 之錨 `` `:9344` `` **已腐**。

🛑 **上七事皆⛔ 得讀為「某側違反某裁」「某解算器有誤」「某檢為缺陷」**——該判**⛔ 在本批之授權內**。

---

*本報告受詞 ＝ `W-G.9-277`。⛔ 呈 KL。*
