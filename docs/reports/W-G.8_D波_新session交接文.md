# CC 新 session 交接文：**K-6-A2 D 波**（D-0 〜 D-2b-1 已落地·D-3 待開）

**產出**：CC／2026-08-08　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`　**交接時 HEAD**：`8a1a901`（已 push·已驗·工作樹追蹤檔異動 **0**）

> 🛑 **新 session 第一件事：讀 §二（停機上呈中之四項）**——其中一項擋住 D 波繼續。
> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前**重跑該指令**（行號／計數皆易腐）。
> 🔴 **§三 列有一批「已作廢·不得引用」之數字**——開工前必讀（失敗考古節 36）。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD                                   # 應 ≥ 8a1a901
sed -n '1,60p' CLAUDE.md                                     # ⚠️ 本波新增 4 條常設紀律
grep -n "^### 🔒 K-9-5-4 \|^### 🔒 D 波之範圍界線\|^### 🔒 D-3 重烤授權" docs/rulings/K-6_街角地分配程序與可分配判準.md
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md    # 現值 44
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # 現值 節 52
grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_D2b1切換後.txt  # 現值 22（⛔ 非 wc -l）
```

⛔ **禁 `git reset --hard`**。⛔ **禁對 `verify/out/` 用 `git add -A`／`.`**。

---

## 一、現況

| 項 | 值 | 現查指令 |
|---|---|---|
| 分支／HEAD | `wip/s1-endpart` ／ `8a1a901` | `git rev-parse --short HEAD` |
| **`run_all` 最新靶** | `verify/out/D2b1_runall.log` | — |
| 計數 | **12 PASS／22 FAIL**（名目 **34**） | `grep -c '✅ PASS' <靶>`／`grep -c '🔴 FAIL' <靶>` |
| 🔴 **驗收方式** | **「與凍存之期望 FAIL 名單**逐項**相同」**，⛔ **非「全綠」、⛔ 非比總數** | `verify/out/K6A2_期望FAIL名單_D2b1切換後.txt`（**22 名目行**） |
| 登記表 | **44 列** | 見上 |
| 失敗考古末節 | **節 52** | 見上 |

### 1-1 D 波之逐批 SHA（全部已 push）

| 批 | SHA | 性質 |
|---|---|---|
| S6-2-C（成因） | `73e4dfb` → `e9bcaa6` | ⛔ 零生產碼·查明 `F` 槽餵錯量 |
| GEO-1／GEO-2 | `63ae86d` → `e45f85a` | ⛔ 零生產碼·`S1` 構造驗證＋`W1` 口徑更正 |
| D-0／D-0b／D-0c | `027a06d`／`6f6450e`／`2213f1e` | ⛔ 零碼面·正典＋凍存表＋哨兵 |
| **D-1** | `c56067f` | 🔴 **行為變更**：`F` 槽 路寬 → SIDE_LINE 全長 |
| **D-2** | `f8b42fc` | 🔴 **行為變更**：街角規定範圍改 ∥SIDELINE（`K-9-5-4` ①） |
| D-2b-0 | `b4ccaf5` | ⛔ 零生產碼·② 之期望表凍存＋D-3 重烤授權 |
| **D-2b-1** | `021bca3` ＋ `8a1a901` | 🔴 **行為變更**：實配境界線改 ∥SIDELINE（`K-9-5-4` ②） |

### 1-2 `run_all` 之衰減軌跡（**全部已逐項歸因·無法歸因 ＝ 0**）

| 批 | PASS/FAIL/名目 | 新阻塞 |
|---|---|---|
| D-1 之前 | 86／46／132 | — |
| **D-1** | 74／49／**123** | `W-F F.1` 右鷹架未實作（`GB-43`） |
| **D-2** | 37／27／**64** | `W-F F.0` GSA 硬錨 |
| **D-2b-1** | **12／22／34** | **`①' 覆蓋閘破`**（§二-1） |

🔑 **三次衰減皆為「切換正確 ⇒ 撞到既有之凍結物／未實作路徑」**，
⛔ **非 regression**。每一批之報告皆附逐項歸因表。

---

## 二、🛑 停機上呈中（**開工前必讀**）

### 2-1 🛑 **`①' 覆蓋閘破`**（**擋住 D 波繼續**·D-2b-1 之發現）

```
🔴 ①' 覆蓋閘破[R1]：|union(宗+池) − 街廓| = 12.0568 > 0.01     ← app.py:8789
⇒ 街廓 R1 抵費地計算失敗                                        ← verify/stepg_pipeline.py:972
```

**根因**：第 1 宗之遠側界已改 **∥SIDELINE**（D-2b-1），而**池片**
（`_place_pool_parcels`·`grep -n "def _place_pool_parcels" app.py` ⇒ `:8886`）
**仍以 `allocation_dir`（∥ALLOC）切帶** ⇒ 二者間出現**楔形空隙 `12.0568㎡`**。

- **本批新破**：`覆蓋閘破` 於 `D1_runall.log`／`D2_runall.log` 命中 **各 0**、本批 **7 次**。
- **修復須動池片切線方向** ——⛔ 不在 D-2b 清單、不在授權；
  且它正是「**第 2 宗轉楔形**」之實作，而 D-2b 施工單明令**第 2 宗遠側界不動**。
- **後果**：`run_step_g` 中止 ⇒ D-2b-1 之 **§三(c)(d)(e) 與 §五 合成案無法量測**。

⇒ **待 KL 裁**：① 擴授權於本波修池片／② 另開波次／③ 其他。

### 2-2 🛑 **`W-F F.0` GSA 硬錨**（D-2 之發現·**已有歸屬**）

```
G010 G(Σa)=293.72 ≠ 錨 283.87；G014 G(Σa)=127.05 ≠ 錨 128.63   ← verify/wf_f0.py:342
```

⇒ **歸屬已定 ＝ `D-3`**（正典「🔒 D-3 重烤授權」·`K-6_*.md:692`）。
⛔ **D-2b 及其之前一律不得重烤**：⛔ 不得設 `WV_BAKE`、⛔ 不得改任何錨值、⛔ 不得放寬 GSA 檢。

### 2-3 🛑 **`GB-43`：`W-F F.1` 右鷹架未實作**（D-1 之發現·**仍開放**）

`verify/wf_f1.py:219`（逐字：「右支架 deferred·**KL 2026-07-20 裁**」）。
**失效條件（GB-43 逐字）**：`right_forced_offset` 回 False **且** run_all 名目數回到 132。
- 第一半 ✅ **已成立**（D-2 之後 `3.5m R1 右 = False`）
- 第二半 ❌ **未成立**（名目 34）
⇒ 依逐字**不得關閉**。⚠️ **但其直接成因已解除**——右鷹架**不再被觸發**；
現在的阻塞是 §二-1 與 §二-2。

### 2-4 ⛔ 未決項（**CC 不得自決**）

`GB-42`：`E-1.7` 之 `1.0㎡` 地板（**KL 裁定之工程門檻**，唯一逐字出處為 `app.py:11145` 之註解，
**未入正典、未入 `CLAUDE.md`**）⇒ ① 位階追認 ② 數值去留，**皆屬 KL**。
併記脆弱性：`0m R5/left 628-7(2)` 之真交集 `1.070652`，對地板**餘裕僅 `0.070652㎡`**。

---

## 三、🔴 **已作廢·⛔ 不得引用之數字**

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| `W-G.7_K6-A2-GEO1_S1構造圖上驗證.md` §三 | `0m R4/right` 原載 `111.0225`／`−25.9816` | **`111.0215`／`−25.9827`**（報告內已加具名更正註） |
| `W-G.8_D_期望影響表_凍存.md` §二 四象限表 | **⛔ 不得用於 D-2b 以後**——其幾何口徑為 **D-2 之前** | `verify/out/probe_D2b_expect_凍存.log`（D-2b-0 凍存表·**38 列**） |
| D-2b-0 §四 之「兩份 `_solve_one` 都要改、非字面上的恰好一個」 | **判斷有誤** | 兩份皆薄殼、皆委派同一 `_solve_G_one` ⇒ **決定點恰 1 處** |
| D-2b-0 §四 之清單「`_place_pool_parcels` 非第 1 宗 ⇒ 排除」 | **判斷不完整** | 它與第 1 宗**共用覆蓋不變量** ⇒ 被波及（§二-1） |
| S6-2-C 之前 `W-G.7_K6-A2-S6-2_弦vs線距.md` §二-3／§二-4／§三 | 全部數字（含 `1.36 vs 5.08`） | 見該檔之作廢橫幅 |

⚠️ **`probe_E17_intersection.py` 切換後不可再跑**——它以 `_build_corner_range_v3` 取「舊範圍」，
而該函式已是新構造 ⇒ 其 `E2`（舊）閘必假紅。**已入庫之 log 係切換前量測、仍有效。**

---

## 四、生產碼之現況（D 波已改動處·**共 3 處**）

| # | 落點 | 內容 | 批 |
|---|---|---|---|
| 1 | `app.py:17986-17987` ＋ `verify/selection_pipeline.py:436-437` | `F` 槽：路寬 → **側長度**（`sb_rows_by_label` 同源） | D-1 |
| 2 | `_build_corner_range_v3` 內（`grep -n "def _build_corner_range_v3" app.py`） | 街角規定範圍：`L(t*)`（∥ALLOC）→ **平移後 SIDELINE**；`k97` 呼叫整段移除 | D-2 |
| 3 | `grep -n "def _first_corner_alloc_dir" app.py` ⇒ `:11458`；`grep -n "K-9-5-4 ② 之唯一決定點" app.py` ⇒ `:11508` | 新 helper ＋ `_solve_G_one` 內**唯一決定點** | D-2b-1 |

🔒 **未刪未停用**（D 波界線 #5）：`k97_solve_alloc_t`、`k98_virtual_measure_block`、
`K-9-6/7/8`、`GB-36`／`GB-40` —— 皆仍在，8 支探針／夾具仍呼叫 `k97`。
🔒 `verify/wf_f1.py`／`verify/wf_f0.py`／`_place_pool_parcels` **一字未動**。

---

## 五、D 波之範圍界線（**六項不授權**·正典 `K-6_*.md:667`）

末端塊／N0-20 不動｜第 n−1 宗側界方向不動｜**第 2 宗 5 格未解決·須另案**｜
`K-9-5-3` 與 `K-9-5-2-a`-3 互斥仍在（`K-9-5-3` **暫停適用不解除**）｜
`K-9-6/7/8`·`GB-36/40`·`k98` **禁刪禁停用禁標作廢**｜baseline／凍存錨**禁於 D 波內重烤**。

🔒 **唯一放寬**：`D-3 重烤授權`（`K-6_*.md:692`）——**僅對 D-3 一批**，五條邊界逐字在案。

---

## 六、本波新增之常設紀律（**全在 `CLAUDE.md`**·各 1 命中）

| 條 | 要旨 | 案由 |
|---|---|---|
| 🔒 施工單必須自足 | ⛔ 禁「本訊息／上文／前述」等指向 block 外之引用 | 交付物落單外 ⇒ CC 連續兩批收不到（考古節 52） |
| 🔒 凡列合計，帶號與絕對值**並列** | `Σ\|X\|` 抓漏項／值錯、`ΣX` 抓**符號錯／抄錯欄** | `Σ\|ΔG\|` 兩邊全符而只有帶號合計抓到抄錯欄 |
| 🔒 **修完之後那個閘要再看一次，且看它的「數字」** | ⛔ 不得以「這次沒報錯／`rc=0`」代替 | D-2b-0 patch 定位錯**兩次**、**兩次 `rc=0`**、結論兩次皆假 |
| 🔒 **立戒者在下一批最容易違反自己的戒** | 本波新立之戒，次批須逐條自檢並正面列出「已套用」 | 反靜默計數器係 CC 自立於 GEO-1，**下一批即漏設** |

**失敗考古新增**：節 **50**（節 40 之再現）／**51**（施工單設下正典早已裁過之停機條件）／
**52**（交付物落在施工單之外）。

---

## 七、🔑 本波最貴的三條（**給下一手**）

1. **零 diff／零異動有兩種相反解釋** ⇒ 一切 patch／wrapper 必附**咬到／退回計數器**，
   且**計數為 0 而結論為「無變化」者，一律先當作 patch 沒生效**。
   （D-2b-0 因此擋下一個「漂亮但全錯」的凍存表。）
2. **受影響清單不能只問「這個函式是不是我要改的對象」**，
   還要問「**有沒有任何不變量把它和我要改的對象綁在一起**」。
   （`_place_pool_parcels` 之漏列 ⇒ §二-1。）
3. **對帳器本身沒有對帳器** ⇒ 三項不變量須內建並印出：
   **框內重複鍵 ＝ 0**（否則 raise）／**母體列數**／**比對欄以欄名指名**。
   （D-1 之同鍵覆蓋、D-2 之比錯欄，兩次都是「數字看起來正常」才最難察覺。）

---

## 八、次步（**待 KL 裁定後方可動**）

| 序 | 事 | 前置 |
|---|---|---|
| 1 | **§二-1 覆蓋閘破之歸屬** | 🛑 待 KL（擴授權／另開波次） |
| 2 | D-2b-1 之 **§三(c)(d)(e) ＋ §五 合成案** | 隨 1 解除 |
| 3 | **D-3 重烤** | 授權已在案（`K-6_*.md:692`）·**須逐 `gid` 因果鏈**·⛔ 無法歸因之錨值變動 ⇒ 停機 |
| 4 | `GB-42`（E-1.7 地板）之位階與去留 | 🛑 待 KL |
| 5 | 第 2 宗 5 格低於最小寬 | ⛔ **D 未解決·須另案**（界線 #3） |

---

## 九、收官前置（不變）

`git rev-parse origin/wip/s1-endpart` 已含該 commit——**不綠不推、不推不報**。
⚠️ `run_all` 約 **15–20 分鐘** ⇒ 背景跑＋等通知；⚠️ **長跑期間 ⛔ 不得改 `app.py` 與 `verify/`**。
⚠️ 驗收以**期望 FAIL 名單逐項相同**為準（§一）；⛔ 非全綠。
⚠️ **四份期望 FAIL 名單並存**（`902f5d1`／`D1切換後`／`D2切換後`／`D2b1切換後`）
——**現行者為 `D2b1切換後`**，其餘三份**刻意保留供考古**、⛔ 勿誤用。
