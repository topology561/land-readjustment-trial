# CC 新 session 交接文：**K-6-A2 段五收官 → 段六**

**產出**：CC／2026-08-06　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`

> 🛑 **新 session 第一件事**：跑 §五 之開工檢查清單，**再讀 §四**（待辦與驗收點）。
> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前請**重跑該指令**——
> 行號與位元組數在 wip 支是**易腐資產**（`CLAUDE.md` 行號衛生／失敗考古節 48）。

---

## 一、現況（**一律現查·⛔ 禁自本文抄**）

| 項 | 值 | 現查指令 |
|---|---|---|
| 工作分支 | `wip/s1-endpart` | `git branch --show-current` |
| 段五收官後 HEAD | 見 §五-1 之核對 | `git rev-parse --short HEAD` |
| **`run_all` 最新靶** | `verify/out/K6A2_seg5c2_c4_runall.log` | — |
| 靶之計數 | **86 PASS／46 FAIL**（＝ `CLAUDE.md` 所載之現行驗收基線·本波未動） | `grep -c "✅ PASS" <靶>`／`grep -c "🔴 FAIL" <靶>` |
| 靶之位元組數 | **工作樹 `1688283`**／**blob `1682070`**（差 `6213` ＝ CRLF 數） | `wc -c <靶>`／`git cat-file -s $(git rev-parse HEAD:<靶>)` |
| 登記表 | **39 列**（`GB-1`〜`GB-39`·另有 `GB-7b`·**缺 `GB-12`**） | `grep -c "^\| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md` |
| 失敗考古末節次 | **節 48** | `grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md \| tail -1` |

🔴 **位元組數必須聲明量測框**：本倉 `core.autocrlf=true`，`.gitattributes` 之 `-text` **僅涵蓋 `*.dxf`／`*.dwg`**
⇒ `*.log` 入庫時 CRLF→LF ⇒ **工作樹與 blob 恆不相等**。
**claude.ai 自倉內看到的是 blob 側。**「逐字節相同」之宣稱**一律以 `diff` 為據**（框無關）。

---

## 二、本波鎖定之裁定（段五 (a)〜(c)-2·**引用前先看正典逐字**）

| # | 裁定 | 正典錨 |
|---|---|---|
| 1 | **K-9-3 補正**：宗地層**不再以宗地深度比對附表深度**（該判定由街廓層 K-9-2 蘊含）；⛔ **但各宗最小深度仍須實量**（§4 之最小寬度須先有最小深度範圍） | `grep -n "^### 🔒 K-9-3-1 " docs/rulings/K-6_*.md` |
| 2 | **帶底改由「後緣連續段之最淺端點」定位**（KL 圖示裁定 2026-08-05）——⛔ **無平移、無投影係數**；舊式 `t_lo + D·(b̂n·n̂)` 已作廢 | `grep -n "^#### K-9-6-b " 同檔`（含更正註）＋ `grep -n "^#### 🔒 K-9-6-a " 同檔` |
| 3 | **`K-9-6-g` 限縮**（KL 補充裁定 2026-08-06）：「量測點須落在宗地範圍內」**僅適用非街角第 1 宗**；街角第 1 宗依 **K-9-8** 得**伸出地籍範圍**（虛擬還原塊·此為本意） | `grep -n "^##### 🔴 K-9-6-g-1 " 同檔` |
| 4 | **GB-36 乙式維持**：帶之**起算線仍為 `PQ`**；本波改的是**帶底之定位點**、非起算線 | `grep -n "^#### 🔒 K-9-6-b-2 " 同檔` |
| 5 | **`K-9-6-g-2`**：K-9-8 供**多邊形**（且禁與街廓取交集）、K-9-6-b 供**深度區間** ⇒ **分工、非重疊定義** | `grep -n "^##### K-9-6-g-2 " 同檔` |

**碼面落點**：`_n14_band_geom`（單一真相源）／`_n14_band_hi`（薄殼·五元組）／
`bl_pts_by_label`（BASELINE 端點之單一構造點）／`evaluate_parcel_width_n14` 之 `_require_baseline`。
**GB-13／GB-33 已標「✅ 已落地（段五 c-2）」**；⚠️ **GB-33 不得結案**——其屁股側判號之同軸用法本波未觸。

---

## 三、誤判更正（**逐字·勿改寫**）

> 節 42／47／48 為**同一根**——未與同倉既有逐字條款對讀即下斷言；
> **三次皆由 KL 或 CC 抓到，claude.ai 零次自抓。**

- **節 42**：抄用衍生表列（`K-9-6-a` 表列第 1 列）未與同檔上方**逐字法規原文**對讀**受詞**
  ⇒ 帶底取錯；下一輪並把該錯誤**誤診為另一個錯誤**（量測對象），在錯的地方修了兩次。
- **節 47**：把**單一情形之圖示**升格為**全稱條款**（`K-9-6-g`），與同檔 `K-9-8` 實作紅線逐字互斥。
  ⇒ 已補**節 42 之戒 (d)**：**寫入**新條款前須 grep 同檔既有逐字／紅線條款對讀。
- **節 48**：據自身容器之讀數斷定他方數字有誤，而二者只是**量測框**不同
  （工作樹 `1,688,154` vs blob `1,681,942`·差 `6,212` ＝ 行數）。**兩個數字都對。**
  ⇒ 已補戒 **(d)**：指控他方數字有誤前，先問「**我們是不是在量同一側**」。

---

## 四、待辦與驗收點

### 4-1 段六（K-9-5 ＋ K-9-1 ＋ GB-18 ＋ GB-39 **同批**）

**偵察已完成** ⇒ `docs/reports/W-G.7_K6-A2-段六_偵察.md`（**開工前必讀**）。其六項前置：

| # | 前置 | 性質 |
|---|---|---|
| 1 | 🔧 **`depth_from_PQ` 之改正**（實測 Δ 達 **`0.120267 m`** ＞ 法定粒度）——其 min 取自 `P_block`／`Q`，二者**皆在 `PQ` 上** ⇒ **受詞為臨街段**，正是節 42 被推翻之受詞；正典 `:703` 早已定式帶底為「後緣連續段之最淺端點」 | **待改正之陳舊量**·⛔ **非待裁項**·隨 GB-18 一併改正 |
| 2 | `on_chamfer` 等契約旗標於 `_build_corner_range_v3` 之**存活**未查 | 查證 |
| 3 | **§零-2「街廓最小建築面積」UI 欄**（K-9-1 所需）——⚠️ 與 `setback` **勿混**：`setback` **已有欄**、此欄**沒有** | 新增 UI |
| 4 | 撤銷 K-4 豁免之早退 return ＋ 路由至 k98 ＋ **加判定層**（k98 只量不判） | 實作 |
| 5 | `_require_baseline` 之落點移至 k98（或確認既有 `_stop` 已足） | 實作 |
| 6 | **S6-1** 之去向 | 🛑 **待 KL 裁** |

🔒 **好消息**：`k98_virtual_measure_block` 簽章**已含** `baseline_pts`、生產呼叫點 **0**；
且 `_n14_band_geom` 於虛擬塊上 **12/12 成立**（raise 0）⇒ 接線前提比預期好。

### 4-2 GB-33 專批（⛔ 不得因段五而結案）

其**另一半**未觸：`grep -n "屁股側：垂距沿 FRONT 法向" app.py`／
`grep -n "FRONT 法向·取號指向街廓內" app.py` 之屁股側判號同軸用法。

### 4-3 波末批

換快照（U-K8-1＝乙）／重烤 v3・wf・E 系列 baselines／改遷移項／
**U-K9-2 之修圖**（`R4/right 628-1(1)`·已裁「修圖」·待生效）／
`K-9-6-d` 落地狀態條文已過期（宜隨波末批改寫）。

### 4-4 🛑 未決項（⛔ **CC 不得自決**）

| 項 | 狀態 |
|---|---|
| `data/r3.dxf` 之去留 | KL 未決 |
| `data/地籍資料來源_匿名版.xlsx` 之未 commit 異動是否入庫 | 🚩 自 W-G G.2 起上呈中 |
| **S6-1**：3.5m 下低於門檻 `7.00` 者 | 🔧 **(a) 部分已裁**（KL 2026-08-06·**`K-9-5-3`**：門檻為**硬門檻·不設容差** ⇒ `R1/right 628(5)` 之 `6.847031` **判不合格**）。<br>⚠️ **數已更新**（GB-40 切換後）：低於門檻者**僅 `R1/right` 一格**（`R6/right` 已翻為 `7.221163`）。<br>🛑 **(b)「去向」仍未裁**——併宗／改配／現金補償／強制留設抵費地，見接入點偵察 §7-3 |

---

## 五、開工檢查清單

### 5-1 可直接貼之一段

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD          # 應 ≥ e73a6c5（段五(c)-2 收官）
sed -n '1,60p' CLAUDE.md
grep -n "^### K-9\|^#### 🔒 K-9-6\|^##### 🔴 K-9-6-g-1" docs/rulings/K-6_街角地分配程序與可分配判準.md
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md          # 現值 39
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # 現值 節 48
sed -n '1,40p' docs/reports/W-G.7_K6-A2-段六_偵察.md
```

### 5-2 grep／數字衛生（**每一條都付過學費**）

| 戒 | 案由 |
|---|---|
| 全倉 grep 一律加 `--exclude-dir=.claude` | **GB-35**：巢狀 worktree 使命中**整整多一倍**，而 `git status` 看不到它 |
| 證據指令**正面列舉受檢檔**，⛔ 禁「全倉 grep ＋ `grep -v` 排除」 | 後者**隨倉庫長大自動過期** |
| 多 pattern 用 **BRE 交替**（`\|`），⛔ 禁多個 `-e` | **GB-34**：本機 grep 曾 abort 而偽造「乾淨的 0」 |
| **否定性存在主張**須附窮舉清單，含**大小寫變體** | **節 44**：`baseline` 命中 0 而 `BASELINE` 命中 1；**節 33** 同族 |
| **行號一律當場重 grep** | 節 48 併看：`:238` 在**同一 session 內**因後續 edit 位移為 `:239` |
| **位元組數須聲明量測框**；「逐字節相同」以 `diff` 為據 | 節 48（見 §一） |
| **長跑期間 ⛔ 不得改 `app.py` 與 `verify/` 任何檔** | 段五(c)-2：commit 3 之 `run_all` 跑到一半時編輯了它的**受測夾具** ⇒ 靶被下一 commit 污染，**且不會報錯** |

### 5-3 收官前置

`git rev-parse origin/wip/s1-endpart` 已含該 commit——**不綠不推、不推不報**。
⚠️ **`run_all` 約 15–20 分鐘** ⇒ 背景跑＋等通知；`run_verification` 會寫 CSV ⇒ **兩支長跑不可併跑**。
