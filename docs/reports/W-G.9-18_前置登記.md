# 【W-G.9-18 前置登記】降維落點之偵察 ＋ 正典同步缺口之登記

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`git rev-parse --short HEAD`（⛔ 本文不寫死·現查為準）
**字樣錨（倉規 A·唯一命中）**：`【W-G.9-18 前置登記】`

> 🔒 **本檔為 commit A：⛔ 零生產碼、⛔ 零碼變更、⛔ 零 `C1`–`C6` 之量測。**
> 撰寫時**尚未**追降維鏈、**尚未**加註任何一處。

---

## §0 登記時已現查（⛔ 以**字樣錨**自行定位·⛔ 未採信施工單所引之行號）

| 錨 | 現查結果 |
|---|---|
| `git grep -l "W-G.9-18" \| wc -l` | `0` |
| `git grep -c "^### 🔒 K-9-5-4" -- docs/rulings/` | **1**（定義式·已在權威序**第 1 級**之裁定文件內） |
| `git log -S"SIDELINE 平行移動" -- app.py` | 🔴 **2 個 commit**：`f8b42fc`（2026-08-08）＋ `79bf2d7`（2026-08-09）<br>⚠️ **施工單 §0 只列了前者**——⛔ 本批以現查之 **2** 為準 |
| 降維落點（字樣錨 `_rng_cr.area`） | `_left_min = round(float(_rng_cr.area), 2)` ／ `_right_min = …` **各 1 處** |
| `_corner_buffer_S(` | **定義 1 ＋ 呼叫 2**（left／right） |
| 🔒 判別力自檢（不存在之符號 `ZZZ_不存在(`） | `0` ⇒ 上列各式**非恆命中** |

---

## §1 前批之三處更正（⛔ 先辦·**加註**·原句一字不刪）

### 1-1 裁定四〜六之性質　⇒ **重申**，⛔ 非「新裁」或「變更」

**現查**：`K-9-5-4` 之定義式**已在裁定文件內**（命中 1），且碼面同期已切換
（`f8b42fc` 2026-08-08 ／ `79bf2d7` 2026-08-09）。
⇒ **裁定四〜六 ⛔ 不得記為「新裁」或「變更」**，須加註更正為：

> **重申 `K-9-5-4`（KL 裁 2026-08-08）**，並補正 `§四` 之**過期記載**。

🔒 **「舊節出處無法確認」該段<u>不受影響</u>**——`§三`／`§四` 當年由誰寫入，仍未定。
⛔ **兩件事分段寫。**

### 1-2 否定性宣稱之射程　⇒ **我把射程寫錯了**

前批（`K-9-5-13` §五）寫「平移量之量法**倉內未載**」。
**現查 `app.py` 已載**（含其法源）⇒ 該宣稱之**射程實際只涵蓋裁定文件**，
卻寫成「倉內」（**考古 60 之形狀**）。須加註更正為：

> **裁定文件未載；碼面已載，且引畸零地使用規則第四條第三款為據。**

⚠️ **併同：該待裁項<u>撤回</u>**——⛔ 不需 KL 裁。

### 1-3 乙②結案

`§五` 面積量級之未估 ⇒ **問題本身已消失**（碼面已是新構造 ⇒ 面積不因裁定四而變）。加註結案。

---

## §2 待證命題（⛔ 現查·⛔ 不採信施工單）

- **H-1**：`_build_corner_range_v3` 之回傳**多邊形**，除 `.area` 外，其**遠側界**在**生產路徑**上是否還有任何消費者。
- **H-2**：`_corner_buffer_S` 之**兩個呼叫點**，其輸入之 `range_area` 是否即為 `_left_min`／`_right_min`。
- **H-3**：**不丟棄遠側界**所需之**最小改動範圍**——⛔ **只描述、不實作**。

---

## §3 `C1`–`C6` 可證偽預測（⛔ 於任何量測之前寫定）

| # | 預測 | 證偽方式 ⇒ 觸發 |
|---|---|---|
| **C1** | `H-2` 成立（`range_area` ＝ 該多邊形面積） | 不成立 ⇒ **C1 破 ⇒ `J-3`**（`GB-58` 受詞須重新定式） |
| **C2** | 遠側界在**生產路徑**上除面積外**無其他消費者** | 有 ⇒ **C2 破 ⇒ `J-1`**（⚠️ **合法結果**） |
| **C3** | `H-3` 之最小改動範圍**可由構造列出** | 不可列 ⇒ **C3 破**，具名，⛔ 不猜 |
| **C4** | 下游**確以 `s` 座標為介面** ⇒ 「∥SIDELINE 界非 `s` 區間」為真前置 | 不是 ⇒ **C4 破**（**好消息**，須查是何介面） |
| **C5** | 倉內**查無** `S1_par` 與 `W` 互代之實例 | 查得到 ⇒ **C5 破**，具名逐處列出 |
| **C6** | 本批**零判定層變動** | 出現任何差 ⇒ **C6 破 ⇒ `J-4`** |

🔒 每項附**判別力自檢**。
🔴 **`C5` 之期望為「查無」**——依**考古 69** ＋ `W-G.9-16` 之 `X5` 教訓，
**須先證明所查之鍵在母體中確實可命中**（⛔ 否則「查無」可能只是鍵壞掉）。

---

## §4 停機條件

| 碼 | 條件 |
|---|---|
| **J-1** | `H-1` 查出遠側界**另有生產消費者** ⇒ 🛑 上呈（「不丟棄」之影響面比預期大） |
| **J-2** | `H-3` 之最小改動範圍**觸及配地順序或選位分數** ⇒ 🛑 上呈 |
| **J-3** | `H-2` 為偽 ⇒ 停機（`GB-58` 之受詞須重新定式） |
| **J-4** | `run_verification.results` 之 PASS/FAIL 層改變 ⇒ 停機 |

---

## §5 ⛔ 明確未授權（⛔ 逐項）

1. ⛔ **零生產碼**：不動 `app.py`、`verify/stepg_pipeline.py`。
2. ⛔ 不估任何面積、不改落位、不改任何一族消費者、⛔ **不實作 `H-3` 之任何一項**。
3. ⛔ 不實作 `GB-58`、不解「保留區換表示法」、⛔ **不為其定式**。
4. ⛔ 不刪 `docs/rulings/` 任何一字；`K-9-5-13` 之更正一律**加註**。
5. ⛔ 不動 `run_verification.py`／`wv_reconcile.py`／`baselines/`／既有夾具／三支複本／`CLAUDE.md`。
6. ⛔ **不追那 57 條之出處、不追 `X5` 之 6 條**（皆屬後批）。
7. ⛔ 文件刪除欄一律 **0**。

---

## §6 🔒 本批自我加課

| 驗收 | 本批如何遵守 |
|---|---|
| **#1** 降維鏈「該段丟掉什麼」**不得留空** | 若答「什麼都沒丟」須附證據 |
| **#2** 凡出艙計數必寫**母體 ＋ 判準** | 承 `3043f56` 之 `2` vs `96` 混寫自誤 |
| **#3** 否定性宣稱之**射程須寫明母體** | 🔴 **本批 §1-2 即因此更正**——⛔ 不得籠統寫「倉內」 |
| **#4** `H-3` **只描述**、⛔ 不評優劣、⛔ 不排序 | 一旦比較優劣即會往「哪個好做」滑，而該選哪個取決於 KL 尚未看到的面積影響 |
| **#8/#9** 母體釘 commit／證據產生後⛔ 不重跑／閘之兩端至少一端在受詞外 | 承前四批之自誤 |
| **#10** 凡「查無」須先證鍵可命中 | 承 `W-G.9-16` 之 `X5` |

---

## §7 commit 分立

- **A** ＝ 本檔（⛔ 零碼、零量測）
- **B** ＝ 降維鏈追蹤 ＋ `H-3` 描述 ＋ 丁組查證（⛔ 零文件結論）
- **C** ＝ 三處更正加註 ＋ `GB` 新條 ＋ 報告 ＋ 考古節（若有）

⚠️ **依倉規（不可變文件不記可變值），本檔 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本檔之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
