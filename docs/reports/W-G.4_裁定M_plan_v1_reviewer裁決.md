# W-G.4 裁定 M — plan v1 reviewer 裁決與**停機上呈**

> reviewer（redistribution-reviewer·獨立復現）判 **BLOCKED×6 ＋ WARNING×8**。
> **plan v1 不得依現狀施工**。基準 `513f920`。
> 本檔＝裁決紀錄＋域裁上呈；**未寫 plan v2**（理由見 §四）。

## 〇、結論

- reviewer 之 **B-1～B-4 我已逐條親自復驗，全部成立**（§一）——皆為 plan v1 之**實質錯誤**，非文字瑕疵。
- **B-5 守恆風險**（救援池雙重消費）以真資料算出三組對稱互救，**M-3(ii) 解不了**。
- **四題屬域邊界**（§三），依 CLAUDE.md「必答題未裁未施工＝停機」→ **停機上呈**。
- 其中 **W-8 最關鍵**：實測顯示 **Q1（真 G）之方向與本波目的相反**——可能**新增** forced 端。

---

## 一、我親自復驗成立者（非採信 reviewer 文字）

| # | 復驗證據（我實跑） | 對 plan v1 之影響 |
|---|---|---|
| **B-1** | `tests/test_corner_priority_golden.py:63,69` ＝`"G_value": 1e9`；`:79` ＝`g_values_map={}`。同法見 `verify/app_harvest.py:174` | P-C 之「缺即 raise」**直接炸掉圖 8 golden**（`run_all.py` 之 **[1/3] 第一道閘**）。⚠️ **與 plan 自身 §七.4「golden 變動＝實作 bug、非合法重定錨」互斥** |
| **B-2** | `_estimate_G_for_qualification` 三真消費端：`app.py:14870`（**live Streamlit UI**）／`verify/selection_pipeline.py:290`／`verify/app_harvest.py:150` | plan §三「換源後成孤兒→contract 整段刪」**錯**。且 `app.py:14870` 在 `main()` 內，harvest 保留 `main()` 但從不呼叫 ⇒ 刪後 `NameError` **py_compile 與 harness 皆抓不到**，只有 KL 在 localhost 按下按鈕才爆 |
| **B-3** | `app.py:14927` 自有 `select_corner_lots_both_sides_v12` 呼叫；`:15050` 寫 `f3_corner_winners`、`:15081` 寫 `f3L_forced_offset`——**正是引擎 ctx 之來源**（`app.py:9106-9107`） | **致命**：plan P-C 只列 `selection_pipeline.run_corner_pk`。只改 harness ⇒ **harness 綠、真 app 之 R5左/R2左/R3右 仍是強制抵費地、918.50㎡ 仍鎖住、「9 戶差 2 戶」在 app 側原封不動**。⇒ 本波首要驗收讀數在 KL 的 UI 上不會成立 |
| **B-4** | `verify/wf_f2.py:40` `F2_GROUPS=["G009","G001","G021","G026","G027"]` 硬編；`:143-144` **只對這 5 群**呼叫 `_decide` | M-1 母體＝「多街廓**均未達**」＝`qual` 空，而該 5 群 qual 全非空（否則現行 126 PASS 不可能綠）⇒ **plan P-E 落點是死碼**。且 `F2_GROUPS` 為案例常數，**違反 plan §五自訂之「禁案例常數」** |

**B-5／B-6 我未逐位復跑**（成本高），但其論證鏈與碼面一致、且 reviewer 附可復跑命令；**以其為準並列入必辦**。

---

## 二、plan v1 之事實錯誤清單（我的·一併更正）

| plan 所寫 | 正確值（reviewer 實測·我抽驗一致） |
|---|---|
| `run_corner_pk` **6** 呼叫端 | **7**（漏 `verify/tools/y_dump_diff.py:59`）；**另加 app.py:14927 自有 PK ＝第 8 條路徑** |
| P-A 左端 `S_max_limit = s_max_blk`（`_oblique_s_max`） | 左端實為 **`S_block_max`**（stepg:575）；**只有右端**用 `_oblique_s_max`（stepg:613-614）。二者非同值（`app.py:6925-6927` 自述 R3 差 **3.4571m**） |
| P-A「首宗可用全幅」 | 左端**還扣 `_right_buffer_S`**（stepg:575）；右端扣**整個左組** `left_cum_S`（stepg:646）⇒ 右端「全幅」係**純虛構**、會使閘比實配**寬鬆**（再違 Q1「口徑一致」） |
| P-A `baseline_pt` 之「無 forced⇒buf=0」 | **只涵蓋本端**。**現成反例**：3.5m R3【右】＝強制抵費地(308.93) ⇒ 評 R3 **左**端假設首宗時 `_right_buffer_S>0` 依然存在 |
| 我引之 `stepg:512-513`／`:425-442`／`:528-532` | 實為 **`:540-541`／`:439-470`／`:556-560`(左)、`:627-631`(右)、`:260-264`** |
| `fixture_end_reserve.py` **10** 斷言 | 實為 **12** 檢核項（左 6＋右 6·全 Δ=0） |
| 盤點表 `_end_gate` 落點 `wf_f4:1311-1324` | `_cond1` 在 **:1324-1325**、`_end_gate` 在 **:1338** |

**另**：`docs/specs/W-G.4_CC交辦_v3.md:105` 之「3.5m forced 擴為 5（含 R1右 226.88、R6右 299.13）」「628(5) 短 3.44／628-18(1) 短 3.54」**已過期**——現況 baseline 為 **3 端**，該兩宗現為 **+0.84／+8.65 過關**（S0b/S0c/N1 之後）。引用前須標作廢。

---

## 三、⛔ 域裁上呈（四題·CC 不自裁）

### Q-M1【最關鍵】W-8：Q1（真 G）之方向與本波目的**相反**

reviewer 實測 3.5m 全部 13 個街角候選之 `G真 − G估`：**全 13 筆為負**（−0.65 ～ −14.75）
⇒ **真 G 一律低於粗估 ⇒ 閘一律變嚴**。

現行 5 個地主 winner 在真 G 下之餘裕：

| winner | 端 | 真 G vs 街角規定面積 | 餘裕 |
|---|---|---|---|
| 628-36(1) | R1左 | 576.44 vs 226.18 | +350.26 |
| **628(5)** | **R1右** | **227.72 vs 226.88** | **+0.84** ⚠️ **刀口** |
| 628(1) | R4左 | 1521.05 vs 225.25 | +1295.80 |
| 628-1(1) | R4右 | 457.76 vs 225.67 | +232.09 |
| **628-18(1)** | **R6右** | **307.78 vs 299.13** | **+8.65** ⚠️ 薄 |

⇒ 若「假設第 1 宗」口徑（全幅 S／W_prev=0／buf=0）與 trunk A 實值有**任何位移**，
**R1右 0.84㎡ 極易翻** ⇒ **Q1 本身可能新增 forced 端（每端 226.88㎡）**，
與本波「釋回 918.50㎡、消除 9戶差2戶」之**目的相反**。

> **請裁**：Q1 是否仍於本波施作？或
> (a) 本波**先只做 M-2(c) 合併自救**（沿用現行 G估口徑）、Q1 另立波；
> (b) Q1 與 M-2(c) 同波但**先量後裁**（先出「真 G 下各端 winner 是否翻」之探針數據再定）；
> (c) 照原裁全做（接受可能新增 forced 端）。
> **CC 傾向 (b)**——本波已具備探針基建（`probe_capacity_decomp` 家族），可先量再定，不必盲賭。

### Q-M2｜B-6：「假設第 1 宗」對**他端 forced** 之約定

他端 forced 未知係**循環相依**（forced 正是 PK 之輸出）。三選項：
樂觀（兩端皆假設無 forced·全幅）／悲觀（含對端 buffer）／兩情境並列。
涉附件二公式選擇與法規本意 → **請裁**。

### Q-M3｜W-3：三條硬閘落地後**必紅**

| 閘 | 檔:行 | 落地後 |
|---|---|---|
| 率接線「無串聯」逐格全等（僅豁免 `原位次(距角序·暫行)`/`G估(㎡)`） | `verify/run_verification.py:441-444` | **必紅**·該閘註解**自帶「停機上呈」** |
| 「G估 欄變動 N 格（期 0）」 | `:451-456` | **必紅**（0m 18 格／3.5m 21 格·13 候選真G−G估 無一為 0） |
| 「v3·診斷{tag}（全欄逐格·無豁免）」 | `:433-435` | **必紅** |

改其 `skip_cols` 或退役屬**域裁** → **請裁處置**。

### Q-M4｜W-1：`_solve_one` 之 `except Exception: pass`

`verify/stepg_pipeline.py:316-317` 實配路徑**靜默退** `iterate_G_S`。
plan 同時要求「與實配**同一條** solve 路徑」＋「失敗＝**loud raise**」——**二者不可兼得**
（raise 版會在實配算得出 G 時擋掉候選、閘比實配嚴，仍是口徑不一致）。**請裁採哪一側**。

---

## 四、為何**未寫 plan v2**

**W-8（Q-M1）之裁決會決定 plan v2 之骨架**：若裁 (a)「先只做 M-2(c)」，則 P-A/P-B/P-C
（真 G、snapshot 補入、7+1 呼叫端、estG 處置）**整段不需要**，plan v2 形狀完全不同。
先寫 v2 ＝ 高機率白工，且違「必答題未裁未施工」。

**已可先行之技術修正**（裁決回來後併入 v2·不需再議）：

1. **R1 採 reviewer 裁：抽共用 helper**（B/C 單一真相源），**前提**＝純位移、不改算式/求值順序/
   兩處斷言位置，以三張 v3 baseline **byte 級零 diff** 為唯一證明、**先於任何行為改動單獨 commit**。
2. **R3**：`_corner_first_lot_G` 之 `a` 須明釘 `round(分攤登記面積_m2 + 面積_m2, 2)`
   （防 KeyError 慣例 `tp.get('分攤登記面積_m2', tp.get('面積_m2', 0))`），
   輸入來源由 `g_rows(r)` 改 `temp_parcels(_tp)`。實測 Δa ∈ [−2.53, +0.72]。
3. **B-1 修法**：raise **不得**無條件加在 `_pk_one_side_v12`；改加在保證有 map 之呼叫層，
   或以顯式旗標 `require_g_map: bool=False`。**禁改兩顆 golden fixture**（手冊語意錨）。
4. **B-2**：estG **不刪**（3 消費端）；若日後 contract，三處同 commit ＋ 靜態閘（掃 `Name` 節點）。
5. **B-3**：`app.py:14860-14939` **必須列入同 commit**，並補「app `_candidates` 欄集 ==
   selection_pipeline `_candidates` 欄集」結構閘。
6. **B-4**：P-E 落點上移至「F.2 群體選取層」——`F2_GROUPS` 換為 `byg`+`mina` 現算分流
   （`qual 非空 → F.2 既有`／`qual 空 → M-1 新路徑`），同步處理 `wf_f2.py:146` 之 raise。
7. **B-5**：需**跨階段單一結算帳**（consumed-lot registry·PK→F.0→F.2→F.4）＋
   **涵蓋街角-vs-街角之全域決定性序**，並明寫 materialisation 落點與源宗移除時機。
   （reviewer 真資料：G011 `628-42(1)@R2左` ↔ `628-42(2)@R3右` 對稱互救 ⇒ 幻影 **+161.80㎡**；
   G030 同構 ⇒ **+98.00㎡**。M-3(ii) 定的是「街角 vs M-1」序、**非**「街角 vs 街角」⇒ 無效。）
8. **W-2**：Q3「只取所需」不可用 `缺口 ÷ 固定比率`（G 對 a **次線性**·實測 G/a ∈ 0.5594–0.5828）；
   須改「注入 a′ 後**重跑** `_corner_first_lot_G` 迭代至 G ≥ 門檻」。
9. **W-5**：PK 時段 `f3_alloc_depth_by_label`／`f3_min_width_by_label` **尚未鋪底**
   （`stepg:196-199` 才寫）⇒ 須顯式自 snapshot `街廓分配深度_m` 取、缺值 loud
   （fallback 差額：R6 **6.05㎡**、R3 4.89㎡）。
10. **W-6**：Q2 測項落點須上移至 `select_corner_lots_both_sides_v12`
    （E-1.7 在 `app.py:8402-8403`、**先於** `_pk_one_side_v12` ⇒ 原測項為套套邏輯）。
11. **W-7**：首要驗收讀數須標注——「12 ≥ 9」係**鬆弛上界且 log 自述未窮舉**，
    且 M-1/M-2 會改變「需求群」集合本身（9 會動）⇒ **前後不可當獨立對比**。

**PASS（reviewer 實查通過·無須改）**：Q2/R5 不需停機（E-1.7 結構上先行·自救不可能放寬它）；
M-4 `_cond1` 不變之斷言正確（`has_side` 由「無此側」導出、與 winner 無關）；
盤點表 §〇 之 Σ918.50 三端 forced 覆核逐位吻合。

---

## 五、次步

1. **等裁 §三 四題**（Q-M1 最優先·其裁決決定 v2 骨架）。
2. 裁後寫 **plan v2**（併入 §四 之 11 項技術修正）→ reviewer → 施工。
3. **本波至今零碼異動**——spec／盤點／plan／本裁決皆為文件；`app.py`、`verify/*.py` 未觸。
