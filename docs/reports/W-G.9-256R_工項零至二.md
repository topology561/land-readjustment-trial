# `W-G.9-256R`　工項零〜二：二單入倉／自誤 `319`／`GB-153`／`V-G` 改令之落地

> 🔒 **⛔ 零生產碼**（十一檔與 `8a52dbd` 逐位同·`wf_f0.py` ＝ `6758ea766b001b95`）⇒ **直推主線**。
> 🔒 本報告併含 **`W-G.9-257` 之工項零**（入倉）；其工項一（`GB-67` 定位·`Q-1`〜`Q-7`）**未辦**，見 `§五`。
> 🛑 **`c1`（本地 `dacd354`·已保全於 `keep/W-G.9-255-c1`）尚未推任何分支**——待 `V-G1`〜`V-G3`＋`V-G′` 四閘全綠。

---

## §零　開工閘（二單共用·**全過**）

| 閘 | 實測 |
|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ **`8a52dbd5ec9fa8490c6ec6e9d3a64e9755ac2817`** ✅。🔑 **本地 `HEAD` 原為 `dacd354`（`c1` 生產碼·未推）⇒ 先 `git branch keep/W-G.9-255-c1 dacd354` 保全，再 `git switch --detach 8a52dbd`**，使本零生產碼批⛔ 挾帶 `c1`。復驗：`wf_f0.py` blob 回 `6758ea766b001b95`、工作區 clean |
| `2` 號占用 | `W-G.9-256` ＝ 全 `0`；`W-G.9-257` ＝ 全 `0` ✅（三軸 ＝ `blob@8a52dbd`／全 `docs/` **`598`** 檔／檔框＋列框·依 `⑦` 併載雙屬）。對照甲 `-255` ＝ `D1 2`／`D2 5`／`D3 0`／雙屬 `0`／檔框 **`4`**／列框 `5` ⇒ **與 `-256` 單載逐項相符**；對照乙 一人造號（⛔ 入倉·`GB-147`·代稱）＝ 全 `0` ⇒ **器兩造皆能** |
| `3` 次號 | 自誤 **`MAX 318`**／缺號 `[106]`／恆等式 `True` ✅ ⇒ 鑄 `319`；`GB`（二框並取·母體 `W-G.4` 單檔）**`MAX 152`** ✅ ⇒ 鑄 `GB-153`；對照組 `GB-153` ＝ `0`（器須紅）／`GB-152` ＝ `1`（器須綠）|
| `4` `S-0b` | **二單皆 `rc=0`**、機械停機款 `0` 項、🟡 提示 **`0` 項**；`P-5` 皆相符（`-256` 實算 `2582ca5a8e0d7589…`／`-257` `0394745232cb7e7c…` ＝ 各該單載）|
| `5` 前置 | worktree 在 **wip 態**（`GB-152`）✅ |

---

## §一　工項零：二單原封入倉

| 單 | 大小 | 來源 `sha256[:16]` ＝ 入倉 blob | 判 |
|---|---|---|---|
| `W-G.9-256_VG改令_代數恆等_a′寫法硬性款_自誤319_GB-153.md` | `8868` B | **`004cdee081ab88b0`** | ✅ 逐位同 |
| `W-G.9-257_GB-67定位波_純診斷.md` | `8237` B | **`c2f28e2532e45768`** | ✅ 逐位同 |

---

## §二　工項一：自誤 `319` ＋ `GB-153`

| 號 | 受詞 | 責 |
|---|---|---|
| `319` | **立驗收於未查之輸出路徑**——`V-G`／`V-H` 所需之 `F.0`–`F.4` 表在 `GB-67` 下**結構上產不出**（`300`／`301`／`306`／`310`／`312` 同族·第六度）| **發單側** |
| `GB-153` | `GB-67`／telescoping 使 `F.0`–`F.4` **全世代之逐值迴歸不可得**（`c1`／`c2`／`-249` 皆受其限）| — |

🛑 **CC 於本批⛔ 復現該實跑**（`RESULT: FAIL`／紅 `32`／`F.0`〜`F.4` 全紅）——環境須另裝
`ezdxf`／`shapely`／`plotly`／`streamlit`／`pandas`／`openpyxl`，屬 `W-G.9-257` 之射程。
⇒ 依 `三方分工鐵律` 之補正，**該數係發單側之現查，已於 `GB-153` 內標為「⛔ 得作為倉內事實出艙、須俟 CC 復現」**。

🩸 **CC 側之併記（⛔ 卸責·已入 `自誤 319`）**：CC 於 `-255R §六` 已受 `V-G` 之令並排入下輪，
**⛔ 於受令當時查其可執行性**——其成本僅一次 `RESULT` 行之讀取。
🔒 **通則（CC 側自立）**：受令執行某閘時，**排程之前**應先問「此閘之產生路徑今日跑得動嗎」。
🛑 **本則⛔ 減輕發單側之責**（立驗收者之義務在立時；受令者之義務在執行前）。

---

## §三　工項二：`V-G` 改令之落地（`-255 §五`【驗收】一款）

🔒 **`V-G` 原款（逐值迴歸）作廢**（`GB-153`）。**代之以三款**；`V-G′`（判別力三組＋反靶）與 `V-A` **一字不動、全效**。

| 閘 | 判準 | 本批狀態 |
|---|---|---|
| `V-G1` 代數恆等 | `zone(m) == zone(target)` ⇒ `a′_m == a_m`（**bitwise**·⛔ 容差）。母體 ＝ `verify/baselines/wf/f0/F.0_G值_退縮{0m,3.5m}.csv` 之 `a 面積(㎡)` 全欄 × 全部區段鍵。**反例須 `0`**；🛑 反靶：同器對**左結合式** `(a*p)/p` 須得**反例 `9`** | ⬜ **未辦**（下一步）|
| `V-G2` `a′` 寫法硬性款 | **必須** `a * (p_自身 / p_標的)`（**先除後乘·括號不得省**）；⛔ 左結合形 | 🔴 **`dacd354` 之碼面現為左結合** `a_m * p_of(m) / p_t` ⇒ **須改**（見下）|
| `V-G3` 射程未逾 | `停機#2` `0`／`停機#3` `1`／`zones` `1`／`_key` `1`／`py_compile` rc `0`；十一檔中除 `wf_f0.py` 外逐位不變 | 🟢 已於 `-255` 批實測全過·`c1` 改寫後須**重跑具名** |

🛑 **四閘（`V-G1`–`V-G3` ＋ `V-G′`）全綠**方得推側分支並呈 KL。
🛑 **本組閘所證者<u>僅</u>「同區段時不動」**；「跨區段時算得對」僅由 `V-G′` 之**人造測資**支撐
⇒ **出艙時須逐字具名此界**，⛔ 以「`c1` 已驗證」概括。

🔴 **`V-G2` 之實查（CC·態 ＝ `keep/W-G.9-255-c1` ＝ `dacd354`）**：現行碼面逐字為
`add += a_m * p_of(m) / p_t`（**左結合 ＝ `(a*p)/p`**）⇒ **違 `V-G2`**，
須改為 `add += a_m * (p_of(m) / p_t)`。**改前改後之逐字將於 `c1` 批具名。**

---

## §四　【驗收】（本批可判者）

| 閘 | 實測 |
|---|---|
| `V-B` append-only | ✅ **刪除欄非 `0` 之列數 ＝ `0`** |
| `V-C` 取號 | ✅ 自誤 `MAX 318→319`、缺號仍 `[106]`、恆等式 `True`；`GB MAX 152→153`；**`K-9 MAX` 仍 `29`** |
| `V-N` | ✅ `-254`／`-255` 二單之 blob **逐位未動**（本單係改令·⛔ 追改既出）|
| `V-P1`（`-257`）| ✅ `app.py` ＝ `4860f10828fb8ae9`；十一檔全部逐位不變 |
| `V-P2`（`-257`）| ✅ scratchpad 之器⛔ 入 commit |
| `V-G1`／`V-G2`／`V-G3`／`V-G′`／`V-A` | ⬜ 見 `§三`·`c1` 批 |
| `V-P3`〜`V-P6`（`-257` 定位）| ⬜ **未辦**·見 `§五` |

---

## §五　待辦

| 批 | 受詞 | 狀態 |
|---|---|---|
| **`c1`** | `V-G2` 之寫法改正（左結合 → 括號式）＋ `V-G1` 代數恆等（`114` 組窮舉·⛔ 採信發單側之數）＋ 反靶（左結合式須得反例 `9`）＋ `V-G3` 重跑 ＋ `V-G′` 重跑 | ⬜ 本地 `dacd354`·保全於 `keep/W-G.9-255-c1`·**四閘全綠後**推 `verify/W-G.9-255-c1` 並呈 KL `-256 §四` 之句 |
| **`-257`** | `GB-67` 定位（`Q-1`〜`Q-7`）·**純診斷·⛔ 修·⛔ 動生產碼**·器落 scratchpad | ⬜ 環境須另裝六套件方能跑 harness |
| `-251` 段乙 | 純 UI·並行 | ⬜ |
| `c2` | 🛑 **`-256 §六` 明令本單⛔ 開工**——其 `V-H` 已由 `GB-153` 判為不可得，放行證據形須發單側另議並候 KL | 🔴 **⛔ 開工** |
