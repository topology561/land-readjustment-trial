# `W-G.9-70`　比對覆蓋率之和解 ＋ `q` 提升為檔案級

**基座**：`21159c9`（`wip/s1-endpart`·`W-G.9-69`）　🔴 **本批有生產碼變更**
`app.py` blob：`083d382adc5fc18844ce94a055bc86730ed8cf28` → **`a9e5671d64d254907a0396f898f046d9d85e8283`**

---

## 🔴 §0　本批之三句結論

1. 🔴 **`A` 組坐實了施工單之指控，且比其所估更嚴重**——舊工具之靜默覆寫致
   **全 11 檔 117 列中 54 列從未進入比對（覆蓋率 53.8%）**。以真主鍵重跑後
   變動格由 **165 → 387**。⇒ **`P4` 成立**：`R3` 之「候選翻盤」係**工具偽影**
   （winner 兩側同為 `628-45(2)`·⛔ 未翻盤）；**`P5` 成立**：新之未歸因 ＝ **0**
   ⇒ 閘 `G-A` **放行** `B`／`C`／`D` 組。
2. ✅ **`B` 組（`q` 提升為檔案級）已落地**——先證紅（逐字訊息已出艙）⇒ 修 ⇒ 三格全符
   （檔案級生效／街廓級回退仍在／二者皆無仍 loud raise）；乙式耦合斷言**回歸仍綠 16／16**；
   AST 變更範圍**未逸出**施工單閘 5′ 之允許集。**`D` 組**（`GB-80` 碼面標記）
   以 `ast.dump` **逐位元組相同**證其為純註解。
3. 🛑 **`C` 組（`W-D.4` 路徑正規化）⛔ 未執行·停機上呈**——其唯一可行作法係
   **擴張 `wv_reconcile` 之正規化規則作用域至 `File "…"` 之外**，而該清單於倉內
   **已封**：`docs/reports/W-G.9-7_前置登記.md` §2 標題逐字「正規化規則清單
   （⛔ **事後不得增列**·施工單 §5）」，且其內文逐字「作用域**皆限於** traceback 之
   `File "…"` 引號內之路徑 token，⛔ **不及於訊息之其他部分**」。⇒ 依 `§八-6` 停、報、等裁。

---

## §A 組　§Z 五塊之逐位元組自檢與入倉（`P1`）

### A-1　前置三道（⛔ 入倉前）

| 塊 | 目標檔 | bytes【單】／【實】 | 換行【單】／【實】 | sha256[:16]【單】／【實】 | 判 |
|---|---|---|---|---|:--:|
| `Z-1` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 3691／**3691** | 60／**60** | `184a075013c09b03`／**同** | ✅ |
| `Z-2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 3213／**3213** | 54／**54** | `7106ce28f1ec2a09`／**同** | ✅ |
| `Z-3` | `docs/驗證裁定登記表.md` | 4485／**4485** | 73／**73** | `8a4a40d1785a4057`／**同** | ✅ |
| `Z-4` | `.claude/skills/failure-archaeology/SKILL.md` | 3382／**3382** | 51／**51** | `452ca2b0aeb05da2`／**同** | ✅ |
| `Z-5` | `docs/reports/W-G.9-69_重烤與乙式耦合斷言.md` | 2279／**2279** | 39／**39** | `efdfbdffcd42aa97`／**同** | ✅ |

**無 BOM** 5／5 ✅（首三位元組 ＝ `0a 2d 2d`）／**無 CRLF**（`CR` 位元組數 ＝ **0**）5／5 ✅／**末字元 ＝ LF** 5／5 ✅；
**目標檔末字元 ＝ LF** 5／5 ✅。

### A-2　入倉（檔末追加·位元組層）

| 目標檔 | 追加前 | 塊 | 追加後 | 判 |
|---|---:|---:|---:|:--:|
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 90517 | 3691 | **94208** | ✅ |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 320098 | 3213 | **323311** | ✅ |
| `docs/驗證裁定登記表.md` | 64606 | 4485 | **69091** | ✅ |
| `.claude/skills/failure-archaeology/SKILL.md` | 278230 | 3382 | **281612** | ✅ |
| `docs/reports/W-G.9-69_重烤與乙式耦合斷言.md` | 30712 | 2279 | **32991** | ✅ |

### A-3　§一 前置現查

| 待取編號 | 期【單】 | 實得 | 判別力對照（須 ≥1） | 入倉後定義節標題 |
|---|---:|---:|---|---:|
| `自誤 61` | 0 | **0** | `自誤 60` ＝ **5** | `## 自誤 61` ＝ **1** ✅ |
| `自誤 62` | 0 | **0** | `自誤 59` ＝ **7** | `## 自誤 62` ＝ **1** ✅ |
| `VR-027` | 0 | **0** | `VR-026` ＝ **7** | `` ## `VR-027` `` ＝ **1** ✅ |
| `^## 95\.` | 0 | **0** | `^## 94\.` ＝ **1** | `^## 95\.` ＝ **1** ✅ |

### A-4　§一「另須現查」（⛔ 實得照報·⛔ 單未預告期望值）

| 受詞 | 實得 |
|---|---|
| `_match` 於 `app.py` | **72** |
| `f3_manual_baseline` 於 `app.py` | **24** |
| `_q_cr` 於 `app.py` | **2**（`:18211` 讀／`:18223` 傳入·**修前行號**） |
| `_q_wb` 於 `app.py` | **2**（`:11460` 讀／`:11472` 傳入·**修前行號**） |
| `if not dxf_quantum:` 於 `app.py` | **2**（`:10189` `_corner_range_eps` 內／`:11154` `K-9-5-14` 停機·**修前行號**） |
| `DEFAULT_LIST` 於 `verify/wv_reconcile.py` | **2**（`:231` 賦值／`:234` 作 `reconcile_text` 之預設引數） |

---

## 🔴 §B 組　A 組：比對覆蓋率之和解

### B-1　逐檔覆蓋率普查（⛔ 11 檔全查·⛔ 不抽樣）

| 檔 | 列數 | 第 1 欄 | 相異鍵 | 🔴 被靜默覆寫 | 真主鍵（**實測** 列數 ＝ 相異鍵數） |
|---|---:|---|---:|---:|---|
| `W-D.1.2 診斷_退縮0m.csv` | 18 | `街廓` | 6 | **12** | `街廓＋端＋候選地號` |
| `W-D.1.2 診斷_退縮3.5m.csv` | 21 | `街廓` | 6 | **15** | `街廓＋端＋候選地號` |
| `W-D.1.3-a 診斷_退縮0m.csv` | 6 | `街廓` | 6 | 0 | `街廓` |
| `W-D.1.3-a 診斷_退縮3.5m.csv` | 6 | `街廓` | 6 | 0 | `街廓` |
| `W-D.1.3-d 驗收_退縮3.5m.csv` | 3 | `街廓` | 3 | 0 | `街廓`（＋`端` 亦唯一） |
| `v3/W-D.1.2 診斷_退縮0m.csv` | 18 | `街廓` | 6 | **12** | `街廓＋端＋候選地號` |
| `v3/W-D.1.2 診斷_退縮3.5m.csv` | 21 | `街廓` | 6 | **15** | `街廓＋端＋候選地號` |
| `第 1 宗街角地指配結果_退縮0m.csv` | 6 | `街廓` | 6 | 0 | `街廓` |
| `第 1 宗街角地指配結果_退縮3.5m.csv` | 6 | `街廓` | 6 | 0 | `街廓` |
| `退縮0m參數.csv` | 6 | `街廓` | 6 | 0 | `街廓` |
| `退縮3.5m參數.csv` | 6 | `街廓` | 6 | 0 | `街廓` |

⇒ 🔴 **總列數 117／被靜默覆寫 54 列 ⇒ `W-G.9-69` 之比對僅涵蓋 63 列（覆蓋率 53.8%）**。
🔒 **與施工單自算三檔逐項相符**：`18／街廓／6／12`、`6／街廓／6／0`、`6／街廓／6／0` ✅
⚠️ **真主鍵⛔ 未逕採施工單之建議**——本表之 `街廓＋端＋候選地號` 係**實測**確認
（`W-D.1.2 診斷_退縮0m.csv` 之單欄 `候選地號` 亦恰唯一 18／18，惟語意上**不可倚賴**
——同一地號若兩端皆入候選即碰撞 ⇒ 取語意正確之三欄鍵）。

### B-2　工具之修（`verify/tools/wg969_bake_diff.py`·⛔ 非生產碼）

1. 🔴 **同鍵重複即 `raise`**（`index_rows`）——訊息含**檔名／鍵欄／列數／相異鍵／被覆寫數／逐鍵列數**。
2. **鍵欄取交集**：`--keys` 所給之欄若該檔沒有則略過；略過後仍重複，一樣 `raise`
   ⇒ **一組鍵（`街廓,端,候選地號`）可服務全部 11 檔**。
3. 另加**覆蓋率普查**於報表首段（⇒ 每次執行都先證「每一列都進得了比對」）。

**判別力（⛔ 二項全附）**

| # | 受詞 | 實得 |
|---|---|---|
| (a) | 未給 `--keys` 對 `W-D.1.2 診斷` ⇒ 須 `raise` | ✅ **`rc=1`**·逐字：`🔴 同鍵重複 ⇒ 比對會漏列（⛔ 禁靜默覆寫）：W-D.1.2 診斷_退縮0m.csv（baseline）｜鍵欄=['街廓']｜列數=18·相異鍵=6·被覆寫=12 列｜鍵=R1 共 4 列；鍵=R2 共 3 列；鍵=R3 共 4 列；鍵=R4 共 2 列；鍵=R5 共 3 列…` |
| (b) | 給真主鍵 ⇒ 須完成且「列數 ＝ 相異鍵數」 | ✅ **`rc=0`**·**總列數 117／被覆寫 0（覆蓋率 100.0%）** |

### B-3　以真主鍵重跑之因果鏈（`A-3`）

**變動格 165 → 387**（欄名普查：**17 欄**·計數合計 387 ＝ 逐格列總數 387·**三對齊 ✅**）
＋ **缺列 2**（`W-D.1.2 診斷_退縮0m.csv` 與其 `v3/` 同名檔各 1）。

| 成因 | 欄 | 格數 |
|---|---|---:|
| 🔴 **`N-19′` 深度注入**（⛔ 非乙式） | `街廓分配深度(m)` | **12** |
| **乙式**（`K-9-5-14`）之範圍多邊形變動及其下游 | `範圍面積(㎡)` 68／`門檻(㎡)` 68／`真交集(㎡)` 62／`項三比(≤1)` 60／`跨占分(0.4)` 26／`總分` 26／`截角邊(range)` 14／`【左】最小面積(㎡)` 8／`【左】街角最小面積(㎡)` 8／`臨截角` 6／`【右】最小面積(㎡)` 6／`【右】街角最小面積(㎡)` 6／`【左】優先權指數` 5／`【右】優先權指數` 5／`正街角分(0.4)` 4／`抵費地面積＝range(㎡)` 3 | **375** |
| **乙式 ⇒ E-1.7 門檻** | **缺列**（見 B-5） | **2** |
| — | **未歸因** | **0** |

🔒 **二族之分離仍為實跑所證**（`verify/probes/probe_WG969_attrib.py`·`W-G.9-69` §C-3）：
`Δ深度` 逐格 **0.0000（16／16）**；機制 ＝ `block_depth` 於 `_build_corner_range_v3`
**只進構造自檢之取樣**（`for _d_chk in (0.0, 0.5*D, D)`）、⛔ 不參與多邊形構造。
**結構佐證（正面列舉受檢檔）**：`街廓分配深度_m` 於 `app.py` **2**／`verify/run_verification.py` **3**／
`verify/selection_pipeline.py` **1**（`avg_depth=` 之財務路徑）／`verify/stepg_pipeline.py` **2**
⇒ **街角 PK 診斷表之欄中，深度之出現處僅 `街廓分配深度(m)` 欄自身**。

### B-4　🔴 A-3-1／A-3-2：`R3` 之逐側候選清單並列 ⇒ **翻盤不存在·係工具偽影**

**`W-D.1.2 診斷_退縮0m.csv`｜`R3`（端＝右）**

| # | baseline 候選地號 | 真交集 | 達標 | 選中 | ‖ | # | side 候選地號 | 真交集 | 達標 | 選中 |
|---:|---|---:|---|:--:|---|---:|---|---:|---|:--:|
| 1 | `628-45(2)` | 36.27 | **達標** | **✅** | ‖ | 1 | `628-45(2)` | 53.66 | **達標** | **✅** |
| 2 | `628-42(2)` | 102.31 | 未達標 | — | ‖ | 2 | `628-42(2)` | 95.75 | 未達標 | — |
| 3 | `628-27(2)` | 11.96 | 未達標 | — | ‖ | 3 | `628-27(2)` | 4.40 | 未達標 | — |
| 4 | **`628-28(1)`** | 2.29 | 未達標 | — | ‖ | — | **（無此列）** | — | — | — |

🔒 **A-3-1 之答 ＝ 「候選地號 `628-28(1)` → `628-27(2)`」<u>不存在</u>。**
🔒 **A-3-2 之答（具名）**：**baseline 之 `R3` 最後一列 ＝ `628-28(1)`；side 之 `R3` 最後一列 ＝ `628-27(2)`**
——舊工具以 `街廓` 為鍵、**後列覆寫前列** ⇒ 各只留該街廓之**最後一列** ⇒ 二側之「最後一列」不同，
即被呈現為「候選地號翻盤」。**實則 `選中` 者兩側同為 `628-45(2)`、⛔ 未翻盤。**
⚠️ `3.5m` 情境二側**皆 5 列、逐列對得上**（`628-42(2)`／`628-45(2)`／`628-27(2)`／`628-28(1)`／`628-29(1)`），
且**兩側皆無達標宗**⇒ 該情境本無翻盤可言。

### B-5　🔒 缺列 2 之因果鏈（⛔ 非未歸因）

`628-28(1)` 於 `0m` 之 side 側**整列消失**（`R3` 由 4 列 → 3 列；全表 18 → 17）。
**鏈**：`乙式(K-9-5-14) ⇒ S1_perp 改 ⇒ 街角規定範圍多邊形改變（R3/右 面積 152.82 → 153.80·且位置移動）
⇒ 628-28 與該範圍之真交集由 2.29㎡ 降至 ≤ 1.0㎡ ⇒ 觸 E-1.7 之絕對地板
（`_has_p2 = _inter_p2_area > 1.0`·`app.py` 之 `🆕 W-D.1.3-b E-1.7 門檻 0.01→1.0㎡（KL 絕對地板）`）
⇒ 該宗被排除出 PK 候選池 ⇒ 診斷表無其列`。
**旁證（同向）**：同塊 `628-27(2)` 之真交集 `11.96 → 4.40`、`628-42(2)` `102.31 → 95.75`（皆降），
而 `628-45(2)` `36.27 → 53.66`（升）⇒ 範圍係**移動**、非單純放大。
🔒 **土地意義**：`628-28(1)` 於 baseline 即**未達標**、⛔ 非 winner ⇒ **本項不改變任何分配**。

---

## §C 組　B 組：`q` 提升為檔案級（生產碼）

### C-1　先證紅（`B-1`·⛔ 此步之後方動碼）

**情境**：`f3_cad_alloc_dir` 有值（已匯 DXF）＋ `f3_manual_baseline[R1]` ＝ `{'point','angle_deg','enabled'}`（**手動鎖邊**·⛔ 無 `_match`）。
**現行讀法**（`app.py` 之 `_q_wb`／`_q_cr` 逐字同式）`(_mb.get('_match') or {}).get('q_detected')` ⇒ **`None`**
（對照：CAD 解析所得之 `_match.q_detected` ＝ **`1e-05`**）。

**逐字 raise 訊息**：

> 🔴 _build_corner_range_v3[R1·left]：K-9-5-14：`q`（DXF 量化步長）取不到 ⇒ `sinθ` 退化門檻無從導出（放大率 1/sinθ 之誤差放大無界）⇒ ⛔ 禁硬編、禁兜底（K-8 §三〜§五·停機條件·⛔ 禁兜底）

**判別力**：同一呼叫改傳 `dxf_quantum=1e-05` ⇒ ✅ 正常回多邊形（`area = 114.939719 ㎡`）
⇒ **該紅係 `q` 所致、⛔ 非其他**。

### C-2　受詞（實作）

| # | 受詞 | 作法 |
|---|---|---|
| 1 | 檔案級存放 | `parse_cad_precision_layers` 於 `return` 前加 `result['dxf_quantum']`（**頂層鍵**）；`main()` 之 CAD 匯入段加 `st.session_state['f3_cad_dxf_quantum'] = _cad_layers.get('dxf_quantum')` |
| 2 | 讀取序 | **新增 module 級 `_cad_dxf_quantum(session_state, block_baseline)`**：先檔案級、**回退**街廓級 `_match.q_detected`（🔒 **既有讀法⛔ 未刪**·封裝於 helper 內）。`_q_cr`／`_q_wb` 皆改呼叫之 |
| 3 | ⛔ 不改 `if not dxf_quantum:` 之停機 | ✅ 未改（`app.py` 之二處 `if not dxf_quantum:` 逐字未動） |
| 4 | ⛔ 不改 `_corner_range_eps` 之三常數與其 `q=None` 分支 | ✅ 未改 |

🔒 **抽為 module 級之由**（已寫入 docstring）：其唯二呼叫端**皆在 `main()` 內或其下游**，
而 `harvest()` 以 AST 跳過 UI、`run_all` 從不執行 `main()` 內之敘述
（`CLAUDE.md`：`main()` 內之敘述，`run_all` 不得單獨作為驗收依據）
⇒ **判準若內聯於呼叫端即無從被任何自動流程觸及**；抽出後方可由合成案直接餵——本批即如此驗（§C-3）。

### C-3　修後之量（`B-3`）

**受測者 ＝ `app.py` 之 `_cad_dxf_quantum` 本尊**（由 `harvest` 取真符號·⛔ 非複本）
**⇒ 已入倉為 `verify/probes/probe_WG970_q_scope.py`**（🔒 **驗的是生產碼變更後之行為 ⇒ 須倉內可重現**；
⚠️ B-1 之**修前**腳本依施工單 §三 B-1 之明令留在**倉外**，其逐字訊息存於
`verify/out/wg970_B1_prove_red_21159c9.log`）：

| 格 | 情境 | `_cad_dxf_quantum(...)` | 期 | 實得 |
|---|---|---|---|---|
| (1) | 檔案級有 ＋ 手動鎖邊（無 `_match`） | `1e-05` | 成功 | ✅ 回多邊形 `114.939719 ㎡`（＝ C-1 之紅**已解**） |
| (2) | 檔案級無 ＋ `_match` 有（**回退**·舊 session／既有探針之路） | `1e-05` | 成功 | ✅ 回多邊形 `114.939719 ㎡` |
| (3) | 二者皆無 | `None` | **raise** | ✅ **仍 loud raise**（⇒ ⛔ 未兜底·該停機是對的） |

**回歸**：乙式耦合斷言（`verify/fixture_yi_construction.py`）⇒ **`rc=0`·不變式 16／16**、
二式變異判別力**皆仍會咬** ⇒ `P7` ✅。

### C-4　逐行 diff ＋ blob ＋ 閘 5′（AST 判定之變更範圍）

`app.py` `git diff --numstat` ＝ **`59  2`**（B 組 `43  2` ＋ D 組 `16  0`）。
blob：`083d382a…` → **`a9e5671d64d254907a0396f898f046d9d85e8283`**。

| AST 判定 | 實得 |
|---|---|
| module 級定義數 | 170 → **171** |
| 🆕 新增之定義 | **`_cad_dxf_quantum`** |
| 🗑 刪除之定義 | **（無）** |
| 🔧 AST 有變之既有定義 | `main`／`parse_cad_precision_layers`／`select_corner_lots_both_sides_v12` |
| 逸出施工單允許集者 | **（無）** |

🔒 **允許集之對位**：`_q_wb` 段之外層函式 ＝ `select_corner_lots_both_sides_v12`；
`_q_cr` 段之外層 ＝ `main`。⚠️ **`_build_corner_range_v3` 在允許集內但<u>本批未改其 AST</u>**
（D 組僅加註解·見 §E）⇒ 實際變更**嚴格小於**允許集。

---

## 🛑 §D 組　C 組：`W-D.4` 路徑正規化——**未執行·停機上呈**

### D-1　受詞已坐實（⛔ 與施工單所載一致）

`run_all` 之對帳明細（逐字·`verify/out/wg969_runall_after_f5c4175.log`）：

* 凍存：`n\Desktop\land-readjustment-trial\.claude\worktrees\frosty-agnesi-fa87af\verify\stepg_pipeline.py", line 995, in run_step_g`
* 現況：`top\land-readjustment-trial\.claude\worktrees\w-g-9-68-cc-handoff-f0515c\verify\stepg_pipeline.py", line 995, in run_step_g`

⇒ **差異 ＝ worktree 絕對路徑前綴**（`line 995` 兩側**相同**）⇒ 🔒 **受詞 ＝ 路徑、⛔ 非數值**。

### D-2　🔴 為何現有規則抓不到（成因·⛔ 非猜測）

`wv_reconcile` 之 R1／R2 均以 `_FILE_TOKEN = re.compile(r'File "([^"]*)"')` 為作用域。
而該明細行係 `run_verification.py:921-922` 之
`traceback.format_exc()[-300:]` ——一個**尾切片** ⇒ `File "` 之開頭被切掉
（`C:\Users\admin\Desktop\…` 只剩 `top\…`／`n\Desktop\…`）
⇒ `_FILE_TOKEN` **不匹配** ⇒ R1／R2 **不觸發** ⇒ 落**類 III（紅）**。

### D-3　🛑 停機理由（⛔ 具名·⛔ 非「做不到」）

要讓它變綠，唯二作法**皆撞倉內明令**：

| 作法 | 撞何者（逐字） |
|---|---|
| (a) 增列一條規則（或放寬 R2 之作用域）至 `File "…"` 之外 | `docs/reports/W-G.9-7_前置登記.md` §2 標題：「正規化規則清單（⛔ **事後不得增列**·施工單 §5）」；內文：「作用域**皆限於** traceback 之 `File "…"` 引號內之路徑 token，⛔ **不及於訊息之其他部分**」。`verify/wv_reconcile.py` 檔頭亦逐字複述「⛔ 事前登記已封……事後不得增列」 |
| (b) 加大 `traceback.format_exc()[-300:]` 之切片使 `File "` 現形 | 會使**該名目之凍存原因全文整個改變** ⇒ 由「路徑差」變成「訊息實質不同」⇒ **更紅**，且動到凍存名單之比較基準（施工單 §八-1「⛔ 不刪任何名單」之同族） |

🔴 **且本情形正落在該封條所要防的形狀上**：R3 若成立，其效果是**把一個<u>已知</u>之紅變綠**
——`W-D.4` 之紅早於 `W-G.9-69` 報告中具名。事前登記之全部意義即在**先於見到資料**訂規則。
⇒ 🔒 依 `§八-6`（明令之 ⛔ 與所讀之意圖衝突時，取**可逆**之一支）**停、報、等裁**。

🔒 **待裁之問（⛔ CC 不自裁）**：**`W-G.9-70` 施工單 §四 C 組，是否構成對 `W-G.9-7` §2 封條之
<u>明示解封</u>？** 若是，請於下一單**逐字**寫明「解封」二字並補該規則之
①非冗餘自證 ②「這條會讓我看不見什麼」（＝ 該 §2 對 R1／R2 所要求之同一格式）。
⚠️ **本批⛔ 未動 `verify/wv_reconcile.py` 一字**；⛔ 未刪任何名單、⛔ 未改 `DEFAULT_LIST`。

---

## §E 組　D 組：`GB-80` 恆真式守衛之碼面標記（⛔ 純註解）

就地標記 `_build_corner_range_v3` 內之 `if S1_par + 1e-9 < _seg_P0Ps:` 為**「防未來重構之斷言」**，
並**逐字**寫出其恆真之理由：

```
S1_perp = max(_seg_P0Ps·sinθ, T)            （乙式·K-9-5-14）
S1_par  = S1_perp / sinθ = max(_seg_P0Ps·sinθ, T) / sinθ
        = max(_seg_P0Ps, T / sinθ)           （sinθ > 0 ⇒ 除法保序）
        ≥ _seg_P0Ps                          （max 之定義）
```

⇒ `S1_par < _seg_P0Ps` 在乙式下**不可能成立**（浮點餘裕由 `+1e-9` 吸收）。
併寫入：⛔ **不得因恆真而刪除**（它守的是「構造被改回甲式或其他式」之未來），
以及 `W-G.9-69` §B-3 之結論（構造自檢對「只改 `S1_perp` 取值」之回退**零判別力**
⇒ 該類回退之可紅之物 ＝ 乙式耦合斷言夾具）。

🔒 **AST 同構證明（`P8`）**：

| | sha256（`ast.dump(ast.parse(src))`） |
|---|---|
| **B 組完成後** | `3c899d67ee5a3736c317839ee89fe7eca5ec05b1c7c5f47a6f2cedd3999d7880` |
| **D 組完成後** | `3c899d67ee5a3736c317839ee89fe7eca5ec05b1c7c5f47a6f2cedd3999d7880` |

⇒ **逐位元組相同**（長度 `2762989` 對 `2762989`）⇒ **D 組為純註解** ✅（⛔ 未以 `git diff` 行數充當）。

---

## §F 組　`P1`〜`P8` 逐項判 ＋ 閘 `G-A`（⛔ 原預測逐字保留）

| # | 施工單之預測（逐字節錄） | 實得【倉】 | 判 |
|---|---|---|:--:|
| `P1` | §Z 五塊之 bytes／換行／sha256 逐塊相符 | 5／5 逐塊相符（§A-1） | ✅ |
| `P2` | `W-D.1.2 診斷_退縮0m.csv` 之「被靜默覆寫列數」＝ **12**（18 − 6）；`第 1 宗街角地指配結果_退縮0m.csv` 與 `退縮0m參數.csv` 皆 **0** | **12**／**0**／**0**（三檔逐項相符） | ✅ |
| `P3` | A-2 判別力：未給 `--keys` 對 `W-D.1.2 診斷` ⇒ **`raise`**；給真主鍵 ⇒ **列數 ＝ 相異鍵數** | 皆成立（§B-2·給真主鍵後 **117／117·覆蓋率 100.0%**） | ✅ |
| `P4`（弱） | `R3` 之候選翻盤於真主鍵下 **不再存在**（＝工具偽影） | **不存在**·成因具名（各取該街廓最後一列）·winner 兩側同為 `628-45(2)` | ✅ |
| `P5`（弱） | 新的「未歸因」格數 ＝ **0** | **0**（387 格 ＋ 缺列 2 全數歸因·§B-3／B-5） | ✅ |
| `P6` | B-1：`dxf_quantum=None` ⇒ loud raise（訊息含 `K-9-5-14`）；`=1e-05` ⇒ 正常回多邊形 | 皆成立·逐字訊息已出艙（§C-1） | ✅ |
| `P7` | B-3：修後同情境不再 raise；乙式耦合斷言**仍綠 16／16** | 三格全符；夾具 `rc=0`·16／16 | ✅ |
| `P8` | D 組之 `ast.dump` 前後**逐位元組相同**；`docs/rulings/` 變更檔數 ＝ **0**；`verify/baselines/` 異動 ＝ **0 檔** | 逐位元組相同 ✅；`docs/rulings/` ＝ **0** ✅；`verify/baselines/` ＝ **0 檔** ✅ | ✅ |

🔒 **閘 `G-A`**：`P4` ✅ ∧ `P5` ✅ ⇒ **放行 `B`／`C`／`D` 組**；`Z-2` 得入倉（其繫於 B 組·B 組已成）。
⇒ **八項預測全部成立**（`W-G.9-69` 之 `P3`／`P4`／`P6` 三項不成立，本批 0 項）。

---

## §G 組　閘

| 序 | # | 閘 | 實得 | 判 |
|:--:|---|---|---|:--:|
| 1 | 1 | 純追加稽核（入倉前·基座 `21159c9`） | **`rc=0`**·48 項全過 | ✅ |
| 2 | 4 | `TARGETS` 追加（先現查·⛔ 只補未在列者） | 現查命中 **0** ⇒ 追加 **1** 項；48 → **49**（檔 25 → **26**／段 23 不變） | ✅ |
| 3 | 2 | 變異測試（對象先證在母體內：`docs/reports/W-G.9波_claude.ai側自誤登記.md` 命中 ＝ **1**） | **`rc=1`**·具名該檔·`S-5 觸發` | ✅ |
| 4 | 2′ | 變異復原 | **`rc=0`**·該檔 `git diff --numstat` **0 列** | ✅ |
| 5 | 1′ | 純追加稽核（入倉後重跑） | **`rc=0`** | ✅ |
| — | 3 | `numstat` 刪除欄：`docs/` ＋ `.claude/` 全 0 | `+51/-0`、`+54/-0`、`+39/-0`、`+60/-0`、`+73/-0` ⇒ **刪除欄非 0 之檔數 ＝ 0**（`+N` 逐檔 ＝ §Z 各塊之換行數） | ✅ |
| — | 5 | `app.py` blob | 🔴 **變**：`083d382a…` → **`a9e5671d…`**；逐行 diff `59 2`（§C-4） | ✅（本批預期內） |
| — | 5′ | 變更範圍之限縮（AST 判定） | 逸出允許集者 **（無）**（§C-4） | ✅ |
| — | 6 | `py_compile`（`app.py` ＋ 工具 ＋ 夾具 ＋ 探針） | 5／5 `rc=0` | ✅ |
| — | 7 | `run_all` | ⚠️ **本批<u>有跑</u>**（⛔ 偏離施工單之「不跑」·理由見 §I 自誤 3）：`rc=1`（准紅碼）；末端夾具 **✅13／🔴1**（唯一紅 ＝ `fixture_corner_range_k8.py`·**既存紅**）；名目 **12 PASS／22 FAIL**。🔒 **與 `W-G.9-69` 收官態逐字對拍：`PASS` 名目集合 `diff` 0 行、`FAIL` 名目集合 `diff` 0 行** ⇒ **本批生產碼變更對 `run_all` 之淨行為影響 ＝ 零** | ✅ |

---

## §H 組　施工單項數對帳（⛔ 逐項打勾）

| 項 | 受詞 | 辦 |
|---|---|:--:|
| §一 | 四個待取編號 ＋ 四項判別力對照 | ✅ §A-3 |
| §一 | 另須現查六項（⛔ 不預告期望值） | ✅ §A-4 |
| §二 A-1 | 逐檔覆蓋率普查（11 檔全查） | ✅ §B-1 |
| §二 A-2 | 工具之修（同鍵 raise ＋ 逐檔真主鍵 ＋ 判別力二項） | ✅ §B-2 |
| §二 A-3 | 真主鍵重跑 ＋ 重出因果鏈 ＋ 核心判定四問 | ✅ §B-3〜B-5 |
| §三 B-1 | 先證紅（scratchpad·逐字訊息 ＋ 判別力） | ✅ §C-1 |
| §三 B-2 | 檔案級存放 ＋ 讀取序 ＋ 二項 ⛔ | ✅ §C-2 |
| §三 B-3 | 修後之量 ＋ 回歸 ＋ 逐行 diff ＋ 新 blob | ✅ §C-3／C-4 |
| §四 C 組 | `W-D.4` 路徑正規化 | 🛑 **未辦·停機上呈**（§D） |
| §四 D 組 | `GB-80` 碼面標記 ＋ AST 同構證明 | ✅ §E |
| §五 | `P1`〜`P8` ＋ 閘 `G-A` | ✅ §F |
| §六 | 閘 1／4／2／2′／1′／3／5／5′／6／7 | ✅ §G（閘 7 具名偏離） |
| §七 | 報告 §0〜§J | ✅ 本檔 |
| §八 | 邊界七條 | ✅ 逐條遵行（§J） |

**合計 14 項：✅ 13／🛑 1（C 組）**

---

## 🩸 §I 組　CC 本批之自誤

### 自誤 1　🔴 **我把「事後不得增列」之封條讀成待辦事項之前，差一點就直接改了**

C 組之受詞明確、成因我也查清楚了（`traceback.format_exc()[-300:]` 之尾切片使 `File "` 消失），
**修法只要三行**。我已經在腦中寫好那個 regex 才去讀 `verify/wv_reconcile.py` 之檔頭
——那裡逐字寫著「⛔ 事前登記已封……事後不得增列」。
🔑 **通則**：**動一個共用判準之前，先讀它自己的檔頭**——那裡通常寫著誰封的、為什麼封。
⚠️ 併記：`W-G.9-69` 我立過「⛔ 不得為那個差編一個聽起來合理的成因」；本則是它的**孿生**
——**⛔ 不得為那個紅編一條聽起來合理的規則**。

### 自誤 2　🔴 **`TARGETS` 差點把「本批正在修的工具」登為純追加受詞**

閘 4 我一次寫了三項（報告 ＋ `probe_WG969_attrib.py` ＋ `wg969_bake_diff.py`），
其中 `wg969_bake_diff.py` **正是本批 A-2 就地修改之檔** ⇒ 若登檔級，閘 1′ **必紅**，
而那個紅是**假的**（碼本來就是就地改的）。發現後改為只登報告檔，並就地寫下判準：
**本器之受詞係「純追加之受詞」（正典／登記表／報告／交接文／skills），⛔ 不含 `verify/` 下之工具與探針。**
⚠️ **併記一件既存不一致**：`verify/fixture_yi_construction.py` 已於 `W-G.9-69`（**我上一批**）登為檔級
——同一個錯我上批就犯了，只是那批沒改到它所以沒現形。⛔ 本批**不移除**（不得移除任何錨），
留待施工單裁其受詞層級。

### 自誤 3　⚠️ **偏離施工單之「`run_all` ⛔ 不跑」（⛔ 具名·非默默）**

施工單 §六 閘 7 令 `run_all` **不跑**，理由逐字為「**未重烤態之紅未變·跑之無新資訊**」。
🔴 **該理由於本批不成立**——本批**改了 `app.py`**（`P8` 亦明載 blob 會變）。
⇒ 我**跑了**，理由：`CLAUDE.md` 之波循環要求生產碼變更後驗；且「無新資訊」之前提已被本批自身推翻。
🔒 **這是偏離，不是解釋**——若 claude.ai 認為不該跑，請於下一單具名糾正。
（跑之期間⛔ 未動 `app.py` 與 `verify/` 任何檔·`CLAUDE.md` 長跑期間不得動受測物。）

---

## ⛔ §J 組　本批未涵蓋之事（§八 邊界之逐條遵行）

1. 🛑 **C 組（`W-D.4` 路徑正規化）未辦**——理由 §D-3。**待裁**：施工單 §四 C 組是否構成對
   `W-G.9-7` §2 封條之**明示解封**。
2. ⚠️ **`Z-3`（`VR-027`）§三 之處置與本批實況不一致**——該節逐字裁「`B-4` **改判**……
   **正解 ＝ 於比對工具層將 worktree 前綴正規化**」，而該處置**即 C 組、本批未執行**。
   🔒 **`Z-3` 仍逐字入倉**（⛔ 不改施工單之交付物、⛔ 不改既有裁定之文字）；
   **其執行落差由本節具名承載**，⛔ 不以「已辦」帶過。
3. **`W-G.9-69` §C-3／§C-4 之上文⛔ 未改**（更正由 `Z-5` 就地加註承載·§八-3）。
   ⚠️ 惟本批 §B-3 之**新數**（387 格／17 欄／缺列 2）與 §C-3 之**舊數**（165 格／22 欄）並存於倉內
   ⇒ 🔒 **引用時須認明批號**；`Z-5` 已於該報告尾具名其母體不足。
4. **`B-3` 重烤／`B-4` 重錨**——仍停機（`VR-027` 二**維持** `B-3` 之停機）。
5. **§八 逐條**：①不重烤／不動 `verify/baselines/`（**0 檔**）／不重錨／不刪名單 ✅
   ②未上呈 KL；`P4` 已判明為工具偽影 ⇒ **本報告⛔ 未陳述任何「土地後果」** ✅
   ③未改 `W-G.9-69` 上文 ✅　④`K-9-5-3`／`K-9-5-2-a`-3 逐字未動、暫停適用未解除 ✅
   ⑤未改編任何既有編號、未改寫 commit 歷史 ✅　⑥衝突取可逆之一支 ✅（§D-3）
   ⑦三問：**受詞在碼裡嗎**（`_cad_dxf_quantum` 已 module 級·可由合成案直接餵）／
   **所引之款仍有效嗎**（`W-G.9-7` §2 封條**現查仍有效** ⇒ 即 C 組停機之由）／
   **是同一種量嗎**（`q` 之**作用域＝檔案級** vs 其**存放＝街廓級**，本批之受詞正是此不一致）✅
6. 🔒 **§八-7 之「凡彙總數字，同批出艙其分母」（考古節 95）**：本報告之
   `54／117`（覆蓋率）、`387 格`（分母 ＝ 11 檔 117 列 × 其欄）、`16／16`（八格 × 二情境）、
   `5／5`（§Z 塊數）、`49`（`TARGETS` 項數 ＝ 檔 26 ＋ 段 23）**皆已同批載其分母**。
