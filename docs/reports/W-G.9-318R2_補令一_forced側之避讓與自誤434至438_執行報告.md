# `W-G.9-318R2`　執行報告（**補令一**）：forced 側⛔ 傳 `_b_*0`（`GB-170` 之避讓）／`自誤 434`〜`438`

> **態**｜開工 ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（遠端未動）。**分級** ＝ **中**。
> 🛑 **本批⛔ `push`**——`c3`／`c4`／`c5`／`c6` 維持待放行；`c7`（`b1141a3`）之放行**已由補令一暫緩**，
> 其受詞由 **`c13`（`8e056b4`）取代**並重提。⛔ 開新分支（`refs/heads/verify/*` 維持 `20` 支）。
> 🛑 **`常規九`**：本報告所載之 `commit` **皆未 push** ⇒ ⛔ 得記為倉態；接手者之開工第一動作須驗
> `git ls-remote origin refs/heads/wip/s1-endpart`——若 ＝ `4037570…` 則本批**不在 `origin`**。
> 🔒 **檔名之提案**（`常規三`·無土地後果）：本報告取 `W-G.9-318R2_補令一_…`（候選二 ＝ `…-318R_補令一_…`、
> 候選三 ＝ `…-318pR_…`）；理由 ＝ 與原單之報告 `W-G.9-318R_…` **同號而可辨**，且⛔ 另取新單號。候發單側核可。

---

## `§零`　本批（補令一）之 `commit` 串（**皆未 push**）

| # | `commit` | 受詞 | `numstat`（新增／刪除） |
|---|---|---|---|
| `c12` | `c6d6561fccb8746c0794ef50108385e2e76c55af` | 工項零：**本補令**原封入倉 | `187` ／ **`0`** |
| **`c13`** | `8e056b4c792bdbe164e94bd16601e58c608e9515` | **`§二` `c7′`**：forced 側⛔ 傳 `_b_*0` | `6` ／ **`6`** |
| `c14` | `b5f398fd20b5e06ec721683d3a6b41ed08a52fd3` | `§二` 六閘 ＋ `§三` 對帳（器 ＋ log） | `1905` ／ **`0`** |
| `c15` | `f6be00aaca1da0d09ffa0936a84a2b9b261081d7` | `§四`：`自誤 434`〜`438` ＋ `GB` 簿末端追加 | `103` ／ **`0`** |

🛑 **`c13` ＝ 生產碼 `commit`**，其 `push` 須 **KL 放行**；其**取代** `c7`（`b1141a3`）。
🩸 **`c13` 之刪除欄 ＝ `6`（⛔ `0`）之預先具名豁免**（`自誤 437` 攔法 `t`）：
補令 `§二` 明令「`c7` 須依本補令 `§二` **改寫**後重提」——**改寫既有六列，結構上⛔ 純插入**
⇒ 於相衝之點**取代**原單 `§七` 閘 `1`「其餘全 `0`」一款。列數不變（`app.py` `24608`／`stepg` `1676`）。

🛑 **`push` 之粒度（照實回報·候發單側／KL）**：`c7`（`b1141a3`）在 `c13` **之前**且已入本地歷史
⇒ **凡 `push` 至 `c13` 或其後裔者，`c7` 必隨之進入 `origin` 之歷史**（其樹已被 `c13` 完全取代）。
CC **⛔ 自行改寫歷史**（rebase／drop `c7`）——該處置係**意思決定**且不可逆，候發單側／KL 明示。
現有二法：`(甲)` 受領 `c7` 為**被取代之中間態**（歷史留痕·同倉內「作廢單不刪」之體例）；
`(乙)` 授權 CC rebase 去除 `c7`（其**代價** ＝ `c8`〜`c11` 之雜湊全變，
而 `W-G.9-318R` 之 `§零` 表已逐列載其雜湊 ⇒ 須另以末端追加更正）。

---

## `§一`　工項零（`c12`）：本補令原封入倉

`SRC` ＝ `DST` **逐位相同**：`sha256` ＝ `6345f2384494209741780f2d2fec9a32a9b617faa805863651eb00aa5573c828`
（`17855` B·`CR` `0`·末換行有）。
判別力二造：必綠 `6345f23844942097…`／必紅 改一位元組之副本 `31e51a238a8cf196…`（副本置 scratchpad·**⛔ 入倉**）。
`probe_order_preflight.py` ⇒ `P-5` ✅ 相符（實算 `e5d9a980a5511c59…`／單載同·受詞 `17777` B）；
🔴 ＝ **`0`**；🟡 ＝ **`0`**；`rc` ＝ `0`。

🔒 **入倉前之號占用閘**（宣告框·補款 `⑨` 已施行·母體 ＝ 全 `docs/` 追蹤 **`828`** 檔）
`W-G.9-318` ⇒ `D1` 檔 **`2`**／`D2` 列 **`2`**／`D3` 列（**嚴格式**）**`1`**／雙屬列 `0`
⇒ 列框 `|D2 ∪ D3|` ＝ **`3`**／檔框 ＝ **`2`**；鬆框（僅漏框偵察·⛔ 判準）列 ＝ `22`。
`D1` 逐檔 ＝ `docs/orders/W-G.9-318_施工單_…md`／`docs/reports/W-G.9-318R_…執行報告.md`
⇒ 占用者**全為本批自身之產物**（自解白名單 `1`）；且本補令由**發單側**指定共用原單號
（其檔首逐字「本補令於相衝之點**取代**原單 `W-G.9-318`」「**二者皆留倉**」）⇒ **⛔ 有取號之行為**
⇒ CC **未改號**、照實回報後續辦（該款所禁者為「**自行改號**」）。

---

## `§二`　工項一（`c13`）：`c7′`——forced 側⛔ 傳 `_b_*0`

**六呼叫端逐字**（左群 `3`／右群 `3`·縮排隨各呼叫端）
```
_w0_start=(0.0 if not _fo_left else None),   # 🆕 K-9-41 ①前段（W-G.9-318 補令一）
_w0_start=(0.0 if not _fo_right else None),  # 🆕 K-9-41 ①前段（W-G.9-318 補令一）
```
**其義**：非 forced 側 ⇒ `0.0`（覆寫**觸發**·`K-9-41 ①` **前段**）；forced 側 ⇒ `None`（覆寫**不觸發**·
該側行為**逐位不變**）。`_b_L0`／`_b_R0` 之**產生式一字未動**（其仍供選槽側用）；`c3`／`c4` 之上移**維持**。

### 閘（逐項出艙·彙整落檔 `verify/out/WG9318p_gates.log`·`151` 列）

| # | 閘 | 實得 | 判 |
|---|---|---|---|
| `c7′-1` | `_w0_start=` 之列框 ＋ 左右群之 `test`（AST·包層 `for` 之 `iter` 為判據） | 六呼叫端 ＝ `app.py:22184` `left_group` `not _fo_left`／`:22299`・`:22316` `right_group` `not _fo_right`／`stepg:823` `left_group`／`:925`・`:941` `right_group`；**真枝皆 `0.0`／偽枝皆 `None`**·**⛔ 交叉**。🩸 二數並報：呼叫端之列框 ＝ **`6`**（補令載之 `6`）／全字樣 `_w0_start=` ＝ **`8`**（另含 `c6` 之二簽章預設）。判別力[必非零] 同框 `_is_chain_head=` ＝ **`8`** | ✅ |
| `c7′-2` | `_b_L0`／`_b_R0` 之**讀取節點** | ＝ **`4`**（`app.py:22461`／`:22463`／`stepg:1058`／`:1060`），逐一皆在 `_select_pool_slot` 之實參內；出現於任何 `_solve_one` 呼叫端者 ＝ **`0`**。判別力[必非零] `_b_L0` 之全檔字樣命中 ＝ **`5`** | ✅ |
| `c7′-3` | **forced 側逐位不變**（態甲·全量·對拍改前基座） | `got_*.csv` **`18` 檔 `sha256` 逐檔相同（相異 `0`）**；名目序列（判＋名目）**逐位相同**（`64`）；`wv_reconcile` 類 I `40`／類 II `0`／類 III `0` ⇒ **對帳 PASS**。補令所載「唯一得非零者 ＝ `0m` 態甲 `R2 left`（`ΔG < 1e-6 ㎡`）」⇒ 實得 **`ΔG` ＝ `0`**（滿足） | ✅ |
| `c7′-4` | **態乙之受詞維持** | `R4` right `628-1(1)`：`ΔRw` ＝ **`+2.99`** pp／`ΔG` ＝ **`−0.90`** ㎡（`0m` 與 `3.5m` 同值）。**三者對拍**：`c7′` ＝ `c7`（`b1141a3`）＝ 替身 `α` ⇒ **逐值相符** | ✅ |
| `c7′-5` | 二結構閘之**三值判**（`telescoping` **先**／`理論＝實跑` **後**） | 態甲之**十二格於基座與 `c7′` 逐格相同**；態乙 `R4 right`：`telescoping` 🟢（**基座即已 🟢**）／`理論＝實跑` 🔴·`Δ` 由 **`2.425`**（基座）轉 **`0.676`**（`c7′`·與 `c7` **同值**）。⛔ 追改期值 | ✅（照實） |
| `c7′-6` | 守恆 | `R4` 街廓面積 `3207.87` **不變**（`ΣG` `1979.5200` → `1978.6200` ⇒ 調配池 **`+0.9000`** ㎡）；強制抵費地三列 `ΣΔ` ＝ **`0.0000`**／`Σ|Δ|` ＝ **`0.0000`** | ✅ |

🔒 **改前基座之取得**：`c7′-3` 之基座 ＝ `A_pre_got`（`c5` 之態），其與**拋棄式 clone @ `4037570`** 之
`got_*.csv` **`18` 檔 `sha256` 逐檔相同**（閘 `c5-2` 已機驗）⇒ 得代基座；
態乙之基座則以**新建之拋棄式 clone @ `4037570`** 實跑取得（`verify/out/WG9318p_BASE_B_runverif.log`），
跑畢**整份刪除**（`GB-94`）。
🛑 **⛔ 靜默退 `0`**：`w0_start` 為 `None` 以外之不可解析值仍一律 loud raise（原單 `c5` 之閘**一字未動**）。

---

## `§三`　工項二（`c14`）：對帳

**器** ＝ `verify/probes/probe_WG9318p_bl0_recon.py`（唯讀·態甲·**⛔ 驅動全量**·`rc` `0`·`stderr` `0` B）
**落檔** ＝ `verify/out/WG9318p_bl0_recon.log`

🔒 **二自我驗證閘皆過**
`(i)` `_select_pool_slot` 之實參 `b` ＝ 本器自 `_mp_base_W0` spy 所捕之 ①（`5` 街廓 × `2` 側·逐格相符）；
`(ii)` 承 `probe_WG9279_rw0.py` 之同一閘：以**鏈頭實參**重建之 `W` ＝ 碼面之 `W_0`（＝ ②）**逐位相符**。

| 情境 | ① `_b_L0` | `R(①)` | ② 改前鏈頭 `W_0` | `R(②)` | ③ `W(遠側)` | ④ `R(①)−R(②)` | ⑤ `R(②)−R(③)` |
|---|---|---|---|---|---|---|---|
| `0m` | `3.7076106246` | `35.8994690601` | `4.4804447558` | `40.7787574859` | `4.4804447558` | **`−4.8792884257`** | **`0.0000000000`** |
| `3.5m` | `7.0230161898` | `55.8242874248` | `7.7850157181` | `59.9390848777` | `7.7850157181` | **`−4.1147974529`** | **`0.0000000000`** |

🔒 **對拍 `GB-169` 收窄 `§三`**【倉】：其所載 `buf`／`W(遠側)`／`W_0`（碼面）三者與本器實得之 `Δ`
皆 `≤ 3.9e-11` ⇒ **逐位復現**。
🔒 **判別力二造**：甲[必為零] ⑤ ＝ `0.0000000000` ✅／乙[必非零] ④ ≠ `0` ✅ ⇒ **二造異色 ⇒ 器非紅**。

🔴 **判（⛔ 判孰誤·補令 `§三` 明令）**
`|④|` ＝ `4.8792884257`／`4.1147974529`；`GB-170` 倉載之 `R(W_0) − R(S1_perp)` ＝
`4.955250038496402`／`4.265999999999998` ⇒ **`Δ` ＝ `−0.0759616128`／`−0.1512025471`**
⇒ **⛔ 逐位相符** ⇒ **具名其差**，⛔ 判孰誤。

🔑 **併記之新查（只登記·⛔ 判孰誤·⛔ 據以改任何判）**
① 與 ③ 係**同一點**（`corner_pt + buf · d̂_單位`）之 `W`，其**相異之唯一源 ＝ 量測軸**——
① 之軸 ＝ `_mp_base_W0` 之 `_adir`（＝ `allocation_dir_block`·**街廓軸**）；
②③ 之軸 ＝ **鏈頭那一筆**之 `allocation_dir`（`K-9-5-4 ②` 之換軸 `_first_corner_alloc_dir(side_mid)`）
⇒ **`|cos|` ＝ `0.9988785831`**。
而 `GB-170` 之受詞係**線之別**（其所載 `|cos|` ＝ `0.04734528804692001` 等）⇒ **二者係不同之相異源**。
🛑 本器**⛔ 舉證其非** ⇒ 依 `自誤 438` 攔法 `u ③`（同量級同格者**一律先判為該項之引入**·
舉證責任在「主張其非」之一方），**仍先依該款判**；上開軸別之實測**併呈**，候發單側。

---

## `§四`　工項三（`c15`）：登記

### `§四-1`　`自誤 434`〜`438`

`payload` ＝ 補令 `:106`–`:129`（**`24`** 列·**逐字自檔案管線抽取**·`常規七 一`）；
`bytes` **`4856`**／`sha256` **`4d2c353784002dbb275a930c69afddab9e432986ba9be20389c31aa7fb235b36`**／`CR` `0`；
期初 **`922836`** → 期末 **`927693`**（＝ `922836` ＋ `4856` ＋ 分隔 NL `1`）；
`append-only` 嚴格前綴**逐位相符**；`payload` 尾段**逐位相符**。

🔴 **二框並報（`常規四（九）二` 之形之自證 ＝ <u>框之別</u>·⛔ 形之誤）**

| 框 | 相異 | `MAX` | 缺號 | `434`〜`438` |
|---|---|---|---|---|
| **正典框**（`VR-091 四` 定義框 ＋ 範圍框·`CLAUDE.md`「`:779` 寬形之廢止標記」明定 operative） | `424` → **`428`** | `433` → **`438`** | **`[106, 355, 356, 357]`**（未變） | **皆在集合中** ✅ |
| 發單側自擬框（`verify/probes/probe_WG9267_issuer_measurers.py` 之 `RE_ZW_DEF`） | `424` | `438` | `[106, 355, 356, 357, **434, 435, 436, 437**]` | 僅 `438` 命中 |

🔒 **差因（機械·逐字）**：`RE_ZW_DEF` ＝ `` ^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：] ``
——其**要求號後須接 `｜（　:：` 之一**。補令之 `434`〜`437` 標題形為 `` ### `自誤 NNN` ``
（號後**⛔ 分隔符**）⇒ 該框失配；`438` 之形為 `` ### `自誤 438`（🔴 …） `` 有 `（` ⇒ 命中。
🛑 **CC ⛔ 改補令之 `payload` 一字**（`常規七 一`／⛔ 改單一字）⇒ **該形之處置候發單側**
（其選項含：於自擬框加一分支、或由發單側以末端追加補一可被自擬框命中之索引錨）。
🔒 **`常規四（九）二` 之二項機檢，於 operative 之正典框下<u>皆過</u>**：
① 形之自證 ＝ 定義框於 `payload` 命中 `['434','435','436','437','438']`；
② `MAX` 由 `433` 轉 `438`，且五號**皆⛔ 在缺號集**。

### `§四-2`　`GB` 簿末端追加（⛔ 鑄號·⛔ 解除·⛔ 收窄）

節題 ＝ `## 🔧 forced 側起算點接入實跑側之退步與其避讓 ＋ 二態之土地後果逐態並列（W-G.9-318 補令一·⛔ 上文一字不刪·純末端追加）`
內容三項 ＝ `(a)` 對帳五量與二造 ＋ **軸別之併記**／`(b)` `c7` 與 `c7′` 之土地後果**逐態並列**
（母體·可比性判·未變之宗數）／`(c)` 避讓形及其與三則失效條件之關係（**皆未成就**）。
`payload` **`6712`** B／`sha256` **`489d0c5f170ca48e2446f8e6ddc6b6976d4f1f00851b02c9f0b8df8252ef8558`**；
期初 **`935764`** → 期末 **`942477`**；`CR` `0`；嚴格前綴逐位相符。
**`GB-132` 避讓之機驗**：`payload` 內以 `#` 起首且含 `GB-N` 之列 ＝ **`0`**
（判別力[必非零]：`#` 起首列總數 ＝ `4`）⇒ 立後 `GB` 之相異／`MAX` 與立前**逐位相同**（`168`／`170`）。

### `§四-3`　四簿期末

自誤 **`428`**／**`438`**（正典框·缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `40`／`41`。
`append-only` 機檢（`verify/tools/wg942_append_audit.py 4037570`）：受檢 **`77`** 筆／結論 **✅ 全部成立**／`rc` `0`。

---

## `§五`　⛔ 為之事（逐條答）

| # | 條 | 答 |
|---|---|---|
| `1` | ⛔ push 任何 `commit`，直至 KL 逐筆放行 | ✅ **未 push**（`origin/wip/s1-endpart` 仍 ＝ `4037570…`） |
| `2` | ⛔ 改原單 `W-G.9-318` 一字 | ✅ 未改（`c1` 之 blob `sha256` 未動） |
| `3` | ⛔ 動 `_b_L0`／`_b_R0` 之產生式 | ✅ 未動（`_mp_base_W0` 一字未改·`c3`／`c4` 之上移維持） |
| `4` | ⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170` | ✅ 三則皆逐則載「**未解除**」；`GB` 簿只登記其量 |
| `5` | ⛔ 修 `GB-170` 之取錯線 | ✅ 未修（其候 `W-G.9-309`） |
| `6` | ⛔ 代擬呈 KL 之放行格 | ✅ 未代擬——補令已自附 `附錄甲′`，CC 只補其**待實測坐實之欄**（見 `§七`） |
| `7` | ⛔ 追改任何期值；相異一律照實出艙 | ✅ `Δ` `0.676`（帶外 `0.0049`）與 `telescoping` 之基線態，皆照實出艙 |
| `8` | ⛔ 以「量測器綠」代「受詞綠」；⛔ 以空集充綠 | ✅ 二自證閘 ＋ 判別力二造逐項出艙；受檢集基數逐處具名（`18`／`4`／`5` 街廓 × `2` 側） |

---

## `§六`　本批之**自解清單**與**自捕**

### 🔧 自解清單

| # | ①疑義逐字 | ②讀法 | ③所取 | ④機械證據 | ⑤所棄之由 |
|---|---|---|---|---|---|
| `1` | `入倉前之號占用閘` 款一「已占即停並回報」 | 甲：停機／乙：照實回報後續辦 | **乙** | 占用者 `D1` `2` 檔**全為本批自身之產物**（自解白名單 `1`）；補令自載「二者皆留倉」⇒ **⛔ 有取號之行為**；該款所禁者為「**自行改號**」而 CC 未改號 | 甲將使「補令共用原單號」之既定體例結構上不可行 |
| `2` | `c13` 之刪除欄 ＝ `6` vs 原單 `§七` 閘 `1`「其餘全 `0`」 | 甲：拒辦／乙：依補令「改寫後重提」之明令為之並**預先具名豁免** | **乙** | 補令 `§零`／`§二` 逐字令「`c7` 須依本補令 `§二` **改寫**後重提」；改寫既有六列**結構上⛔ 純插入**；補令自載「於相衝之點**取代**原單」 | 甲與補令之明令直接衝突 |
| `3` | `自誤 434`〜`437` 之標題形於**發單側自擬框**失配（落入缺號集） | 甲：判形之誤並停機／乙：判**框之別**，以 operative 之正典框出艙並二數並報 | **乙** | `CLAUDE.md`「`:779` 寬形之廢止標記」逐字：「求 `自誤` 之 **MAX／相異／缺號集**一律用 `VR-091 四` 之**定義框 ＋ 範圍框**」；該框於 `payload` 命中 `434`–`438` **全五號**；自擬框自名「發單側自擬」⇒ **⛔ 正典** | 甲須改補令之 `payload`（違 `常規七 一`／⛔ 改單一字） |
| `4` | 本報告之檔名（`常規三`·無土地後果） | 三候選（見檔首） | **`W-G.9-318R2_補令一_…`** | 與原單報告同號可辨、⛔ 另取新單號 | 其餘二者或與原單報告混淆、或引入新後綴體例 |

### 🩸 自捕（**皆於入倉前**·`2` 則）

| # | 形 | 攔法 |
|---|---|---|
| `1` | `§三` 器內以 `None not in (numpy 陣列, …)` 判缺值 ⇒ `ValueError`（numpy 之 `in` 走**逐元素比較**） | 該例外 loud 現形（`stderr` 非空）⇒ 改 `all(... is not None)` |
| `2` | `§三` 之 ③ **首版誤取 `_mp_base_W0` 之實參**（街廓軸）⇒ ③ 與 `GB-169 §三`【倉】差 **`0.77`**、判別力甲[必為零] 不過 ⇒ **器紅** | 依 `CLAUDE.md`「探針還原內部幾何須以碼面自身之保證為閘」，改取**鏈頭那一筆**之實參（＝ `probe_WG9279_rw0.py` 之同一取法·⛔ 另寫第二份判準），並補**自證閘 `(ii)`**；重跑後 ③ 與倉載**逐位相符**、⑤ ＝ `0` ⇒ 器非紅。**該誤若未攔，將以「兩條不同線」之錯誤歸因出艙** |

---

## `§七`　🛑 呈 KL 與候發單側之事項

### `§七-1`　補令 `附錄甲′`【對土地的影響】之**待實測欄已坐實**

補令 `附錄甲′` 自載「上表之**態甲值為預期**，須以 `§二` 之閘 `c7′-3` 實測坐實後方為定數」。
🔒 **實測結果**：

| 狀態 | 受影響之宗 | 負擔率 | 分配到的土地 | 其餘地主 |
|---|---|---|---|---|
| **現行預設**（未廢前置合併·態甲） | **無**（`got_*.csv` `18` 檔 `sha256` 逐檔相同；名目序列逐位相同；對帳類 III `0`） | — | — | **全部分毫未變** |
| **廢前置合併後**（`K-9-29 二` 所令·態乙） | `R4` 街廓**右**側 `628-1(1)`（**一宗**） | ＋**`2.99`** 個百分點 | **減少 `0.90` 平方公尺** | 其餘 `28`／`31` 宗**分毫未變** |

⇒ 🔒 **補令 `附錄甲′` 之該表<u>已由實測坐實</u>**（態甲之「唯一得非零者 `R2 left`·`ΔG < 1e-6 ㎡`」
⇒ 實得 **`ΔG` ＝ `0`**）。減少之 `0.90` ㎡ **進入調配池**（守恆已驗：`R4` 街廓面積 `3207.87` 未變；
強制抵費地三列合計變動 `0.0000`）。
🛑 **CC ⛔ 代擬放行格**；補令 `附錄甲′`【要你判斷】一問**原樣待覆**。

### `§七-2`　待 KL 放行之生產碼 `commit`（**五筆 → 四筆 ＋ 一筆重提**）

| 筆 | `commit` | 狀態 |
|---|---|---|
| `c3` | `7f43924c53f5c4f8b98bd9a48f5ffeff3506e0a0` | **維持待放行**（純位移） |
| `c4` | `7c346b19120744242d964c8c178d5e81ff6c663a` | **維持待放行**（純位移） |
| `c5` | `7c07a360c9ee00bcc4b61982c287fa774c23785a` | **維持待放行**（預設不變之新關鍵字） |
| `c6` | `6cc51e6183c534d4bc410f544082a2ae38a1ec99` | **維持待放行**（薄殼直通） |
| ~~`c7`~~ | ~~`b1141a3f95e6aff7f5646882e35f234e2f555198`~~ | 🗄️ **放行已暫緩**·其樹由 `c13` 完全取代 |
| **`c13`** | `8e056b4c792bdbe164e94bd16601e58c608e9515` | 🔴 **重提·待放行**（forced 側之避讓） |

### `§七-3`　候**發單側**之事項（⛔ 上呈 KL·`常規二`／`常規三`）

| # | 事項 |
|---|---|
| `1` | 🔴 **`push` 之粒度**：`c7` 在 `c13` 之前且已入本地歷史 ⇒ 推 `c13` 必連帶推 `c7`。二法（受領為中間態／授權 rebase）見 `§零`。**CC ⛔ 自行改寫歷史。** |
| `2` | 🔴 **`§三` 之對帳不符**：`|④|` 與 `GB-170` 倉載之量 **⛔ 逐位相符**（差 `0.0760`／`0.1512`）；且併查得 ①③ 之相異源係**量測軸**（`|cos|` `0.9988785831`）而 `GB-170` 之受詞係**線之別**（`|cos|` `0.0473`）⇒ **不同之相異源**。CC 依 `自誤 438` 攔法 `u ③` **仍先判為該項之引入**（未舉證其非），惟該歸因**候發單側覆核** |
| `3` | `自誤 434`〜`437` 之標題形於**發單側自擬框**失配（`§四-1`）⇒ 其處置候發單側（CC ⛔ 改單之 `payload`） |
| `4` | 本報告檔名之核可（`常規三`·`§六` 自解 `4`） |

---

🔑 **本報告之數皆為【倉】**（來源 `commit`、母體、粒度框逐處同格具名）；
凡僅見於施工單／補令而倉內未復現者一律標 **【單】**，⛔ 下游文件引用為事實。

---

## `§八-實測`　收工閘（**報告入倉後量**·⛔ 上文一字不刪·嚴格末端追加）

🔒 **期初** ＝ `40375702378af4cefd2d11515fdf3f98c0791580`／**期末** ＝ `43753e059660cdf2d672368e8d1bb34f7739210f`（`c16`）。
🔒 **本節表內之 `→` 一律為「期初 → 期末」之<u>時序方向</u>**·⛔ 幾何方向，⛔ 涉任何座標系。
🔒 逐字落檔 ＝ `verify/out/WG9318p_exit_gates.log`（`98` 列）。判準逐條**自條文重新錄入**（⛔ 沿用他批）。

| # | 閘 | 實得 | 判 |
|---|---|---|---|
| `1` | 逐 `commit` `numstat` 之**刪除欄** | 十六筆中非零者恰 **三**：`c3` `36`／`c4` `30`／**`c13` `6`**（補令 `§二` 所明令之改寫·已於 `§零` **預先具名豁免**）；其餘十三筆全 `0` | ✅ |
| `2` | 生產碼 `34` 檔（正面列舉·⛔ 空 `pathspec`） | 相異 **恰 `2`** ＝ `['app.py', 'verify/stepg_pipeline.py']`；其餘 `32` 檔 blob 逐位相同；`app.py` blob `4379108a48567140…` → `8466f7493082bf84…` | ✅ |
| `3` | 本批異動之 `.md` 之 `CR`（倉內 blob·取 bytes） | 受檢集 ＝ **`6`** 檔（二單 ＋ 二報告 ＋ 二簿）·**`CR` 全 `0`**；已**先解跳脫再判**（`core.quotepath=false`）；判別力[⛔ 空集充綠] 受檢集基數 ＝ `6`（`> 0`） | ✅ |
| `4` | 四簿期末（🔴 **二框並報**） | **正典框**（`VR-091 四`·operative）：自誤 相異 **`428`**／`MAX` **`438`**／缺 `[106, 355, 356, 357]`｜發單側自擬框：相異 `424`／`MAX` `438`／缺 `[106, 355, 356, 357, 434, 435, 436, 437]`。`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `40`／`41`（三者皆未變） | ✅（正典框）·🟡 具名 |
| `5` | `append-only`（逐檔） | 自誤簿 `919874` → `927693` B·嚴格前綴 `True`·判別力（改末位元組之竄改本）`False`；`GB` 簿 `928354` → `942477` B·同上。機檢器 `wg942_append_audit.py 4037570`：受檢 **`77`** 筆／**✅ 全部成立**／`rc` `0` | ✅ |
| `6` | 自限復驗 | `baselines` `sha256` ＝ `a898f4e1…`·**`298`** 列；`refs/heads/verify/*` ＝ **`20`** 支（框 ＝ `shell=False` 之 argv 直傳）；`verify/out/` `928` → `946`（新增 `18`／移除 `0`）·**既有 `928` 檔 blob 相異 ＝ `0`**（判別力[必非零] ＝ `18`）；`data/` `3` → `3`·相異 **`0`** | ✅ |
| `7` | 母體 | 全 `docs/` **`826` → `830`**（＋`4`）／`verify/out/` **`928` → `946`**（＋`18`）／全倉追蹤 **`2447` → `2471`**（＋`24`）／全倉 `.md` **`848` → `852`**（＋`4`） | ✅ |
| `8` | 標籤對照（當場以程式自資料列計數·⛔ 轉引） | `probe_WG9269_pit_index.py`：資料列 **`94`**／「✅ 可解析」**`94`**／非之者 **`0`**／`rc` `0` | ✅ |
| `9` | `ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | **【⛔ 可得】**——現態（`push` **前**）＝ `40375702378af4cefd2d11515fdf3f98c0791580`。其由 ＝ **本批⛔ `push`** | 🛑 **未執行** |
| `10` | **KL 放行之逐筆記錄** | **【⛔ 可得】**——五列皆【未放行】。🩸 **落檔之該表仍列 `c3`〜`c7`**（原單之受詞）；**依補令一，其正確之受詞 ＝ `c3`／`c4`／`c5`／`c6` ＋ `c13`**（`c7` 之放行已暫緩·見 `§七-2`） | 🛑 **未執行**·具名 |

🛑 **閘 `9`／`10` 之「未執行」⛔ 讀為「不過」**（`序 11` 之一般化 款 `三`：「**無從回測**」⛔ 等同「回測不過」）
——二者之受詞**結構上繫於 KL 之放行**，而本批依補令 `§五-1` 之明令**止於放行之前**。

🩸 **本節之自捕（第 `3` 則·入倉前）**：收工閘之器於 `閘 4` 之**判準字串**首版沿用原單之
`自誤 423／433`（未隨本批更新）⇒ 與其**實得**並列時將誤導讀者。
攔法 ＝ 落檔後逐列覆讀，發現判準列與實得列不一致 ⇒ 改判準列為 `428`／`438` 並**加列二框並報**，重跑。
🔑 **通則**：閘之**判準**與其**實得**若由二處分別維護，前者必隨批次漂移 ⇒ **判準須與受詞同批重新錄入**
（本單 `§七` 之「判準逐條自本單條文重新錄入·⛔ 沿用他批」即此；本次係其**於補令批之延伸**）。

---

## `§九`　`c7`（`b1141a3`）之性質具名（`W-G.9-318 補令二` 裁 `2` 附款·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為**性質之具名**（`戒 36`）。

🔒 **裁 `2`（發單側·`2026-09-20`）逐字要旨**：`push` 粒度 ＝ **甲**——**受領 `c7` 為被取代之中間態**（歷史留痕）。
其由三：① 與「作廢單⛔ 刪·補令與原單並存」之體例同構；② `W-G.9-318R` 已**逐列載其雜湊**，
`rebase` 將使倉內自相矛盾，其代價高於歷史不整；③ 改寫歷史係為掩一項**發單側之誤**而行之不可逆動作——⛔ 為此而行。

🔴 **逐字具名（依裁 `2` 附款所令）**

> **`c7`（`b1141a3f95e6aff7f5646882e35f234e2f555198`）為被 `c13`（`8e056b4c792bdbe164e94bd16601e58c608e9515`）
> <u>完全取代</u>之中間態**；
> **⛔ 為現行碼面**；
> **凡 `checkout` 該 `commit` 者，將取得一含<u>土地後果退步</u>之樹。**

🔒 **其退步之量（【倉】·`W-G.9-318R2 §三`／`verify/out/WG9318p_gates.log`）**：
於 `c7` 之樹，**態甲**（現行預設）之 `R4` **左** `628(1)+` 一宗其 `ΔRw` ＝ `+4.88`（`0m`）／`+4.12`（`3.5m`）個百分點、
`ΔG` ＝ `−1.48`／`−1.25` ㎡；而於 `c13` 之樹，**態甲之 `got_*.csv` `18` 檔 `sha256` 逐檔相同**（無任何宗受影響）。
⇒ 二樹於**現行預設之態**下**土地後果相異**；`c13` 為 KL 已放行之受詞（補令一 `附錄甲′`）之落地，`c7` **⛔ 是**。

🛑 **⇒ 後窗之處置**：凡引用本波之生產碼態者，一律以 **`c13`（`8e056b4…`）或其後裔**為錨；
**⛔ 以 `c7`（`b1141a3…`）為錨**、**⛔ 據其樹作任何量測或結論**。
🔒 本具名**⛔ 改任何既有 `commit`**、**⛔ 改寫歷史**（裁 `2` ③）。
