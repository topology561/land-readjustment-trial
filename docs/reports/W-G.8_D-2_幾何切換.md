# W-G.8／**D-2**：街角規定範圍幾何切換（`K-9-5-4`）—— 🔴 行為變更 ＋ 🛑 撞 GSA 硬錨

**錨**：`c56067f`（D-1）。**§八-1**：碼面變更**僅限 `_build_corner_range_v3` 內**（逐處見 §一）。

> **一句話**：幾何切換正確——範圍面積哨兵 **14/14 命中新值**、對帳 **41/41 全符**、
> `3.5m R1 右 forced_offset` **回 False**、winner **13／強制 3** 與凍存表逐格相同；
> 🛑 **但 0m 之 winner 三格異動使 F.0 撞上 GSA 硬錨**，而**硬錨更新在 D 波授權之外**（六項界線 #6）。

---

## §〇 前置登記：**GB-43**（登記表 42 → **43** 列）

`W-F F.1` 右鷹架未實作 —— D-1→D-2 之間的**已預期過渡態**。
逐字轉錄 `verify/wf_f1.py:219`（含「KL 2026-07-20 裁」）；⛔ 本批**未修改該檔任何字元**。

🔑 **其失效條件（GB-43 逐字）**：「D-2 落地後 `right_forced_offset` 回 False
**且** run_all 名目數回到 132 ⇒ 本列關閉。**若未回復 ⇒ 升級為擴授權議題、停機上呈 KL。**」

**本批結果（⛔ 兩半分開陳述）**：

| 半 | 結果 |
|---|---|
| `right_forced_offset` 回 False | ✅ **成立**（實測 `3.5m R1 右 = False`；forced 為真者僅剩 `3.5m R2左／R3右／R5左` 三個既有強制抵費地側） |
| run_all 名目數回到 132 | ❌ **不成立**（實得 **64**） |

⇒ 依逐字，**GB-43 不得關閉 ⇒ 升級·停機上呈**。
⚠️ **但升級之標的已改變**：右鷹架**不再被觸發**（其直接成因已解除），
**新阻塞是 F.0 之 GSA 硬錨**（§六）。⛔ 二者**不可混為一談**。

---

## §一 切換（碼面變更**逐處**·皆在 `_build_corner_range_v3` 內）

| # | 變更 | 說明 |
|---|---|---|
| 1 | `k97_solve_alloc_t` 呼叫 ＋ `t_star`／`D_at`／乙式帶／自檢①／①′ **整段移除** | ⛔ 不留 fallback（CLAUDE.md：舊邏輯整個刪） |
| 2 | 新增 `Ps` 之取得 | 截角三角形中**非 `P0`** 且**最靠 `FRONT∞`** 之頂點 |
| 3 | 新增 **辨別性檢**（沿用 GEO-1 閘 H2） | `d(Ps,FRONT∞) < 0.05` **且** 另一頂點遠 10 倍以上。⛔ **非絕對 `1e-6`**——BLOCK 邊與 FRONT_LINE 係兩圖層、實測相距達 1.48 cm |
| 4 | 新增 `S1_par = max(\|s(Ps) − s(P0)\|, T)` ＋ 硬約束斷言 | **沿 FRONTLINE 量·路平行**；斷言 `S1_par ≥ 線段 P0–Ps` ⇒ 分配線永不切截角 |
| 5 | 新增自檢：三深度以 **shapely 獨立實作**覆算路平行距 ≡ `S1_par` | ⛔ 非閉式自證（那是套套邏輯、無鑑別力） |
| 6 | 切線由 `L(t*)`（∥ALLOC）改為**平移後 SIDELINE** | ⇒ 範圍 ＝ `Ps → 道路截角 → SIDELINE → BASELINE → 平移後SIDELINE` |
| 7 | 死碼 `_s_alloc`／`_W` **整段刪**；`alloc_dir` 參數與其守衛**刻意保留**並加註 | 參數仍由呼叫端傳入，二守衛為街廓幾何健全性檢查 |
| 8 | `_t_override` 改為 **loud stop** | 舊式 `t*` 之注入口，新構造無 `t*` ⇒ ⛔ 不靜默忽略（no-silent-fallback） |

🔒 **`k97_solve_alloc_t` 本身未刪、未停用、未標作廢**（D 波界線 #5）——`app.py` 內定義仍在，
8 支探針／夾具仍呼叫（`grep -ln "k97_solve_alloc_t" verify/probes/*.py verify/fixture*.py`）。
🔒 `verify/wf_f1.py` **0 行未動**。

---

## §二 切換前凍存（**執行於切換之前**）

`verify/out/probe_D2切換前凍存.log`（⛔ 一字不得改）：

| 項 | 凍存值 | 對施工單期望 |
|---|---|---|
| 街角規定範圍面積 16 格 | 舊構造值（＝ GEO-1 §三 (7)） | ✅ |
| `3.5m R1` 之 `right_forced_offset` | **True** | ✅ |
| run_all 名目／PASS／FAIL | **123／74／49** | ✅ 三項皆相符 |

---

## §三 切換後哨兵

| # | 受測 | 結果 |
|---|---|---|
| **(a)** | 範圍面積 16 格 → GEO-1 §三 (8) 新值 | ✅ **命中新值 14/14**（**有效鑑別格 14/16**·`R6/right` 兩格標「🔴 無鑑別力·非通過」並自分母剔除·⛔ 未記為 16/16）。切換前態 🔴／切換後態 ✅ ⇒ **具鑑別力** |
| **(b)** | `3.5m R1/right 628(5)`：真G `228.42`／門檻 `226.13`／**達標**、winner 回復 | ✅（見 §四 對帳之該列） |
| **(c)** | 全案 winner `11 → 13`；強制抵費地 `5 → 3` | ✅ **實測 13／3** |
| **(d)** | `right_forced_offset` → False；`W-F F.1` 恢復；名目數 `123 → 132` | ⚠️ **部分**：`forced_offset` ✅ **False**；`W-F F.1` ❌ **仍 FAIL**（**但成因已換**：現為「F.0 未成功，F.1 跳過（W5 守衛）」·**非**右鷹架）；名目數 ❌ **64** |
| **(e)** | winner 異動 3 格 | ✅ `0m R2/left`→`628-41(1)`／`0m R5/left`→`628-18(2)`／`0m R3/right` 由無人→`628-45(2)`（見 §四 對帳） |

🛑 **(d) 未全回復 ⇒ 依 §三 之停機條件上呈**（詳 §六 之新阻塞）。

### 3-1 🔧 (a) 曾報一格不符——**是我的抄錯，不是實作分歧**

`0m R4/right` 實測 `111.0215` vs fixture `111.0225`（差 `0.000987` > 容差 `1e-4`）。
現查 GEO-1 log 第 66 行為 **`111.0215`／`−25.9827`**
⇒ **我在撰寫 GEO-1 報告時抄成 `111.0225`／`−25.9816`**，兩支 fixture 繼承之。

**全 16 格對拍：僅此 1 格抄錯**，其餘 15 格逐位相符。
生產式與 GEO-1 探針式在該格**完全相同**（`S1` 皆 `3.543883384`；`|P0 − Q0| = 0.000000000`）。

**處置**：修 `probe_D2_corner_range.py` 與 `probe_E17_intersection.py` 之 `EXP`；
對已 commit 之 GEO-1 報告 §三 加**具名更正註**（⛔ 未默默改）。
🔒 **凍存檔 `W-G.8_D_期望影響表_凍存.md` 未受影響、未改一字**——其 §二 四象限表**不含面積欄**。

---

## §四 對帳（**41/41 全符**）

`verify/out/probe_reconcile_col3.log`：比對欄 ＝ **`F改正×新門檻`**（`RECON_COL=3`）。
解析框 ＝ 凍存檔 §二 四象限表（第 24〜69 行）、**框內重複鍵 0**、讀入 **41** 候選列。
三欄皆比（真G／門檻／達標含 ★）。

**⇒ 相符 41／不符 0 ⇒ 未觸發 §四 之停機條件。** ⛔ 凍存表全程唯讀、未改一字。

### 4-1 🔧 對帳器第一次報「37 格不符」——**是我比錯欄**

D-2 應比 `F改正×**新**門檻`（`COL=3`），我沿用 D-1 之 `COL=2`（舊門檻）。
**徵候很明確**：不符列之**真G 逐格相同**（`584.91` vs `584.91`），**只有門檻欄是舊值**。
⇒ 已改為 `RECON_COL` 環境變數 ＋ 於碼註記錄此坑；log 檔名帶欄序（`probe_reconcile_col3.log`）。

---

## §五 守恆入帳（🔴 **本批無法對帳·照實陳述**）

D-0 §三 立帳：`W` 口徑連動 `ΣΔG ＝ 0m +0.0423 ㎡／3.5m −0.0449 ㎡`
（`Σ|ΔG| ＝ 0.5243／0.3275`）。

🛑 **本批無法執行「逐項列名扣除」**——因 **F.0 世代中止**（§六），
`F.3 結構永久閘` 之守恆殘差**根本未產出**（該名目已自 run_all 消失）。
⇒ ⛔ **不得聲稱「已對帳」**、⛔ 亦不得以「殘差未變」帶過（沒有殘差可看）。
⇒ **帳目仍掛在 D-0 §三，待 F.0 可跑之後補做。**

⚠️ 併記：D-2 之幾何切換**本身**亦會產生 `ΔG`（範圍改變 ⇒ winner 改變 ⇒ 實配鏈改變），
**該筆尚未立帳**——與 D-0 之 Rw 口徑帳**是兩筆**，⛔ 不得混計。

---

## §六 `run_all`（🛑 **新阻塞：F.0 撞 GSA 硬錨**）

| | D-1（`D1_runall.log`） | D-2（`D2_runall.log`） |
|---|---:|---:|
| PASS | 74 | **37** |
| FAIL | 49 | **27** |
| 名目總數 | 123 | **64** |

### 6-1 根因（**單一**）

```
🔴 FAIL  W-F F.0
      [F.0] 🔴 GSA 錨檢破（0m）——**一次列出全部**（F-7-3：禁首個即 raise 致打地鼠）：
【值不符 2 項】G010 G(Σa)=293.72 ≠ 錨 283.87；G014 G(Σa)=127.05 ≠ 錨 128.63
                                          （`verify/wf_f0.py:351` raise）
```

⇒ **F.0 世代中止** ⇒ F.1（「F.0 未成功，F.1 跳過（W5 守衛）」）／F.2／F.3／F.4／E／G 連鎖不產出。

### 6-2 逐項歸因（**無法歸因 ＝ 0**）

| 變動 | 歸因 |
|---|---|
| `W-F F.0` FAIL（GSA 硬錨 2 項不符） | **0m winner 三格異動**（`R2/left`／`R5/left`／`R3/right`）⇒ 分配鏈改變 ⇒ `G010`／`G014` 之 `G(Σa)` 改變 ⇒ 撞硬錨 |
| 新增 `W-F F.1`／`W-F F.2`／`W-F F.3`（世代級 FAIL） | F.0 中止之連鎖（各自訊息皆明載「上游未成功」） |
| 消失之 F.0 以降全部細項名目（**59 項**） | 同上（世代中止 ⇒ 細項不產出） |
| `F.0 釋池對象＝梯3 二群 …Σ=57.37㎡` → `…Σ=56.88㎡` | 同一名目、數字入字串（**GB-38**）；差 `0.49㎡` ⇒ 0m 分配鏈改變 |
| `率接線 G估 欄變動0m 1 格` | 0m 之 G估 隨新門檻改變 |
| `S0d 異常` 診斷行 **12 次** | ⚠️ **D-1 亦為 12 次** ⇒ **既有·非本批造成**（⛔ 不歸入本批變動） |

🔑 **全部變動歸因至凍存期望表 `F改正×新門檻` 之 0m 三格 winner 異動。無法歸因 ＝ 0。**

**期望 FAIL 名單已更新**：`verify/out/K6A2_期望FAIL名單_D2切換後.txt`
（計數用 `grep -c "^🔴 FAIL"`·⛔ 非 `wc -l`）。⛔ 前兩份名單**未刪未改**。

---

## 🛑 §六-3 停機上呈（**本批唯一之停機項**）

**GSA 硬錨（`G010` `283.87`／`G014` `128.63`）之更新，不在 D 波授權範圍內。**

- `CLAUDE.md` §7 明載：引擎 UC9898-凍結含**硬寫錨 `GSA_EXPECT`**；
  且「**重錨 ≠ 去錨 ≠ 重烤**」——**重錨**係 claude.ai 層之技術動作，**須附逐 `gid` 因果歸因表**。
- **D 波之範圍界線 #6** 逐字：「baseline／凍存錨之重烤：⛔ **不得於 D 波內順手為之**。
  須另批，且須逐 `gid` 因果鏈（重錨 ≠ 重烤 ≠ 移除錨）。」

⇒ ⛔ **CC 未改任何錨值、未加豁免、未放寬 GSA 檢**。
⇒ **上呈 KL 裁定**：① 併入 D 波（須擴授權並附逐 `gid` 歸因表）／② 另開重錨批／③ 其他。

⚠️ **與 D-1 之 F.1 右鷹架是同一形狀**：**切換本身正確，撞到的是既有之凍結物**。
D-1 撞「未實作路徑」、D-2 撞「硬寫錨」。

---

## §七 六項不授權——**未觸及**（正面舉證）

| # | 項 | 舉證 |
|---|---|---|
| 1 | 末端塊／N0-20 | 本批 diff 僅 `_build_corner_range_v3`；未 import／未呼叫 `wf_f4`／`_end_region_R`／`_end_band` |
| 2 | 第 n−1 宗兩側界方向 | `allocation_dir` 傳遞未動；`_block_strip` 未改 |
| 3 | 第 2 宗 5 格 | 未處理、未提及；⛔ 未以「D 之副作用」帶過 |
| 4 | `K-9-5-3` 與 `K-9-5-2-a`-3 互斥 | 正典二則**未動一字**；`K-9-5-3` 暫停適用**未解除** |
| 5 | `K-9-6/7/8`／`GB-36`／`GB-40`／`k98` | **未刪、未停用、未標作廢**——`k97_solve_alloc_t` 定義仍在且 8 支探針/夾具仍呼叫 |
| 6 | baseline／凍存錨重烤 | ⛔ **未為之**——正因如此才有 §六-3 之停機 |

⚠️ **§七 之特別提醒已遵守**：第 2 宗轉楔形後 `K-9-6/7/8` 量測機具將被用到 ⇒ **更未刪未停**。

---

## §八 驗收

| # | 項 | 結果 |
|---|---|---|
| 1 | 碼面變更逐處列出且僅限 §一 | ✅ 8 項·皆在 `_build_corner_range_v3` 內 |
| 2 | §二 凍存已產出並入版控（切換前） | ✅ |
| 3 | §三 五項；(a) 標明 14/16；(d) 正面陳述 | ✅（(d) 為部分回復·已正面陳述並上呈） |
| 4 | §四 對帳逐候選；解析框與重複鍵斷言輸出 | ✅ 41/41·框 24〜69 行·重複鍵 0 |
| 5 | §五 帳目逐項列名扣除 | 🛑 **無法執行**（F.3 未產出）·已照實陳述 |
| 6 | §六 無法歸因之變動 ＝ 0 | ✅ **0** |
| 7 | push 後回報附 SHA | ✅ 見聊天回報 |

## §九 ⛔ 本批**未**做者

1. **GSA 硬錨之更新** —— 🛑 停機上呈（§六-3）。
2. **`K-9-5-4` 之第二半：街角第 1、2 宗之*實配*境界線 ∥SIDELINE** ——
   **未做**。理由：其期望值**從未凍存**，而 §四 之硬性驗收係比對凍存表之
   `F改正×新門檻` 欄，該欄係 GEO-1 **只 patch `_build_corner_range_v3`** 產生；
   若同批連實配境界線一起換，§四 **必然不符**，而那個「不符」不是發現、是受測物被換掉。
   ⇒ **上呈 KL**：需另立凍存表後另批為之。
3. **§五 之守恆對帳** —— 待 F.0 可跑。
4. ⚠️ **`probe_E17_intersection.py` 切換後不可再跑** —— 該檔以 `_build_corner_range_v3` 取「舊範圍」，
   而該函式現已是新構造 ⇒ 其 `E2`（舊）閘必假紅。**已入庫之 log 係切換前量測、仍有效。**
