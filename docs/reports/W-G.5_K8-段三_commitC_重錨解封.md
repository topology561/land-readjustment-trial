# W-G.5 K-8 段三 **commit C**：重錨解封 ＋ 舊基準退場

> 施工單：《CC 施工單_K-8段三_commitC》（錨定 `65ffd17`）。
> 唯一行為變動＝**C-1 `GSA_EXPECT` 重錨**；C-2／C-3／C-4 皆零行為。
> **重錨 ≠ 重烤**：baseline CSV 與快照**一律不動**（U-K8-1＝乙 照舊）。

---

## 一、C-1：`GSA_EXPECT` 重錨（唯一行為變動）

### 1.1 取值方法（**BAKE 僅側錄·不認證**）

`wf_f0` 於 `WV_BAKE` 下把 GSA raise 降為 print ⇒ 以之**取值與取因果資料**
（`verify/out/K8_seg3_C_bake.log:118`／`:126`），**認證另跑普通模式**
（`verify/out/K8_seg3_C.log`）。`WV_BAKE` 指向 scratchpad，**baseline 未被寫入**
（`git status -- verify/baselines/` 零輸出）。

### 1.2 🔴 逐 gid 歸因表（**0m**·2 格移動）

| gid | 舊錨 | 新值 | Δ | 街廓·側 | PK 變動 | 決策變動 (kind/target) | 一行因果 |
|---|---|---|---|---|---|---|---|
| G006 | 365.84 | 365.84 | — | R3 | 無 | 級0'／628-48(1)（不變） | 未移動·**已評估且相符** |
| G007 | 359.43 | 359.43 | — | R5 | 無 | 級0／628-20(2)（不變） | 同上 |
| G009 | 153.19 | 153.19 | — | R6 | 無 | 級0'／628-1(2)（不變） | 同上 |
| **G010** | 293.72 | **283.87** | **−9.85** | **R2 左** | **G023→G010** | **全不變**（級0／628-40(1)／Σa 513.93） | 街角規定面積 151.44→225.69 ⇒ 門檻升 ⇒ 原 winner `628-41(1)` 失格 ⇒ 標的宗**自身**成該側街角第 1 宗 ⇒ 改吃側街負擔 ⇒ G 降 |
| **G014** | 127.05 | **128.63** | **+1.58** | **R3 右** | **G007→強制抵費地**（合格候選 1→0） | **全不變**（級0'／628-29(1)／Σa 228.0） | 該側無街角第 1 宗 ⇒ 推進序起點重排 ⇒ `W` 增、`Rw` 降 ⇒ 負擔降 ⇒ G 升 |
| G017 | 433.68 | 433.68 | — | R6 | 無 | 級0／628-21(1)（不變） | 未移動·**已評估且相符** |

**逐欄實測**（`verify/out/got_F.0_G值_退縮0m.csv` vs `verify/baselines/wf/f0/F.0_G值_退縮0m.csv`）：

- `R2 628-40(1)`：`街角地 否→是`｜`第1筆街角 否→是`｜`街角側別 —→左側`｜
  `W 10.61→6.44`｜`Rw 35.58→52.51%`｜`S 6.54→8.16`｜`累積S 12.34→8.16`｜
  `負擔比率 0.4285→0.4476`｜**`G 293.72→283.87`**
- `R3 628-29(1)`：`街角地` 欄**未動**（非翻盤宗）｜`累積S 10.86→16.21`｜
  `W 9.13→14.45`｜`Rw 15.19→11.16%`｜`負擔比率 0.4428→0.4358`｜**`G 127.05→128.63`**

### 1.3 🔴 逐 gid 歸因表（**3.5m**·3 格移動·**winner 全不變**）

| gid | 舊錨 | 新值 | Δ | 街廓·側 | PK 變動 | 決策變動 | 一行因果 |
|---|---|---|---|---|---|---|---|
| G006 | 365.84 | 365.84 | — | R3 | 無 | 不變 | 已評估且相符 |
| **G007** | 369.41 | **370.02** | **+0.61** | **R5 左** | **無** | 全不變（級0／628-20(2)） | 強制抵費地 300.52→**378.47** ⇒ s 帶加寬 ⇒ `累積S 27.44→29.27`／`W 25.69→27.50`／`Rw 1.52→0.00%` ⇒ 負擔降 ⇒ G 升 |
| G009 | 153.19 | 153.19 | — | R6 | 無 | 不變 | 已評估且相符 |
| **G010** | 298.61 | **301.12** | **+2.51** | **R2 左** | **無** | 全不變（級0／628-40(1)） | 抵費地 309.05→**382.85** ⇒ `累積S 19.57→21.29`／`W 17.82→19.54`／`Rw 23.07→16.66%` ⇒ G 升 |
| **G014** | 129.20 | **129.74** | **+0.54** | **R3 右** | **無** | 全不變（級0'／628-29(1)） | 抵費地 308.93→**383.67** ⇒ `累積S 18.08→19.79`／`W 16.31→18.02`／`Rw 9.69→8.31%` ⇒ G 升 |
| G017 | 433.68 | 433.68 | — | R6 | 無 | 不變 | 已評估且相符 |

**🔑 五格皆非「跑出什麼寫什麼」**：`級別`／`標的宗`／`被併宗`／`Σa` **全部未變**
（`verify/out/got_F.0_合併決策_退縮{0m,3.5m}.csv` vs baseline 逐欄 diff ⇒ 僅
`G(Σa)`／`MinA_街廓`／診斷欄 `Δ非線性` 三者有異）。⇒ **不是合併決策改了，
而是同一決策下標的宗之 G 因街角幾何／抵費地佔位改變而改變**——因果鏈皆可指名。

### 1.4 F-1 覆蓋率閘輸出（§2.2-3「沒人檢查 ≠ 相符」）

`grep -c "未被評估" verify/out/K8_seg3_C_bake.log` ⇒ **0**（雙情境皆無）。
⇒ 兩則 `GSA 錨檢破` 訊息**僅含【值不符】、無【未被評估】**
⇒ 六個 gid 於兩情境**全數進入 `gsa` 被實際比對**；G006／G009／G017 係
**比對後相符**，非「沒被評估」。

### 1.5 兩個 delta 源之可分離性（本批歸因之前提）

commit A 已實測「G 閘 442 列違規、**100% 只落在 `平均深度(m)` 顯示欄**、G 值 delta＝0」
⇒ 深度欄之同步變動**不參與 G**。故 §1.2／§1.3 之五格 G 位移可**全部歸因於 commit B
之街角幾何**。此註已寫入 `wf_f0.py` 重錨註解。

### 1.6 舊值 provenance

依 `wf_f0.py:50-58` 既有體例，新增一代註記
「K-8 §三〜§五 街角規定範圍新構造（KL 裁 2026-07-31·K-8 §五-1）」，
並將**前一代（P-0c）之六格值逐格凍存於註解**。

---

## 二、驗收（`verify/out/K8_seg3_C.log`·全母體 `K8_seg3_C_viol.csv`）

### 2.1 名目恢復（**本批首要目的·達成**）

| | commit B | **commit C** |
|---|---|---|
| 名目 | 64 | **132**（雙向 diff vs commit A：消失 6／新增 6，**全為名目字串內嵌計數者**） |
| PASS／FAIL | 33／31 | **86／46** |
| F.0–F.4＋G.2 | **未產生**（68 名目被截斷） | **全部恢復可測** |

「消失 6／新增 6」逐項＝同一測項因名稱內嵌值而改名，例：
`F.0 六格 G(Σa) 錨 0m {…'G010': 293.72, 'G014': 127.05…}` →
`{…'G010': 283.87, 'G014': 128.63…}`；
`率接線 G估 欄變動0m 0 格` → `2 格`；
`F.0 釋池對象＝梯3 二群 [('G025',1.79),('G030',55.09)] Σ=56.88` → `55.58 / Σ=57.37`；
`F.2 跨街廓守恆0m 0.03` → `0.01`；`F.3 結構永久閘{0m,3.5m} 0.03/0.02` → `0.01/0.0`。
**無測項被刪或憑空新增。**

### 2.2 紅集合三分（**兩源分開歸因·無不明紅**）

```
既有 2 ＋ 源A(門檻) 22 ＋ 源B(街角幾何) 22 − (A有C無) 0 ＝ 46   （實測 46·逐格相符）
```

- **源 A ⊆ 段二預測集合＝True**（靶 `verify/out/K8_seg2_cascade_ifsnapshot.log`）
- **源 B 22 項**（皆為 baseline diff·**該紅照紅**）：
  `指配{0m,3.5m}`／`抵費地0m(應空)`／`抵費地3.5m`／`v3·診斷{0m,3.5m}`／
  `率接線無串聯{0m,3.5m}`／`率接線 G估 欄變動0m 2 格`／`v3·滑池槽{0m,3.5m}`／
  `v3·J表{0m,3.5m}`／`F.0·滑池槽{0m,3.5m}`／`F.0·J表{0m,3.5m}`／
  `F.0·池差閉合{0m,3.5m}`／`F.0 釋池對象＝梯3 二群`／`F.2·跨街廓調配3.5m`／
  `W-D.3 碎片逐邊CAD（v3）`
- **既有 2**：`W-F F.4`（3.5m E2 結構性不可行·早已待域裁）＋`W-G G.2`（F.4 純連坐）
- **「A 有、C 無」＝0** ⇒ 重錨**未把任何既有紅洗綠**

🔑 `抵費地0m(應空)` 之紅，即 **K-8 §五-1「R3 右→強制留設抵費地」之機檢簽名**
（0m baseline 原設「全側有 winner ⇒ 抵費地表應空」）。

### 2.3 golden 段 16 支逐支 rc

| # | 支 | rc |
|---|---|---|
| 1 | B-3 欄集結構閘 | ✅ |
| 2 | 圖8 golden | ✅ |
| 3 | 滑池槽 golden 8/8 | ✅ |
| 4 | 禁寫死絕對路徑閘（0 命中） | ✅ |
| 5 | `fixture_cad_binding_order.py` | rc=0 |
| 6 | `fixture_baseline_candidates.py` | rc=0 |
| 7 | `fixture_n14_min_width.py` | rc=0 |
| 8 | `fixture_block_depth_n19p.py` | rc=0 |
| 9 | **`fixture_corner_range_k8.py`（本波新增）** | **rc=0** |
| 10 | `fixture_end_reserve.py` | rc=0 |
| 11 | `fixture_end_fallback.py` | rc=0 |
| 12 | `fixture_end_winner.py` | rc=0 |
| **13** | **E 系列實測快照閘** | 🔴 **rc=1** |
| 14 | K-4 S 起算點閘 | rc=0 |
| 15 | K-8 SIDE_LINE 配對驗證閘 | rc=0 |
| 16 | K-8 配對驗證閘 | rc=0 |

**🔴 第 13 支之更正與歸因**：該閘**自 commit B 起即 rc=1**
（`grep -n "E 系列實測快照閘" verify/out/K8_seg3_B.log` ⇒ `:30`），
**commit B 報告誤載為「16 支全綠」，已於本批更正該報告**（成因：對帳輸出被 `head -60`
截斷、第 ③ 層未印出，卻據夾具自身 rc 推斷全段——「沒看到」當成「沒有」·#35 再犯）。
其 diff 逐格指向 `R2·628-40(1) 街角地 否→是`／`R2·628-41(1) 街角地 是→否`
＝**與 PK 翻盤 G023→G010 同一事件**，屬**源 B**；該快照身分係**回歸快照**
（探針 docstring 自載「非真值錨」）⇒ **隨其他 baseline 同批重烤**，本波不重烤。

### 2.4 §6.3 不得倒退之既有綠

| 項 | 實測 |
|---|---|
| 快照 `case_params_UC9898.json` 零變更 | ✅ `git status` 零輸出 |
| baseline 零變更（不重烤） | ✅ 同上 |
| A-4 同源閘 | ✅ 全程未 raise |
| 構造自檢（16 格 \|Δ\|≤2.84e-10）＋單調性自檢 | ✅ `fixture_corner_range_k8.py` rc=0 |
| `_build_burden_range`（Ⅱ）零變更 | ✅ `git diff 65ffd17 -- app.py \| grep -c _build_burden_range` ⇒ **0** |

---

## 三、C-2：146.50㎡ 退場（零行為）

### 3.1 fresh grep 復驗（施工單清單之**增補一處**）

`grep -rn "146\.50\|146\.5\|R5左側街角地最小面積" --include="*.py" --include="*.md" .`

| 落點 | 性質 | 處置 |
|---|---|---|
| `CLAUDE.md`「驗收對拍基準」節 | 街角基準 | **刪該列**＋加 §五-2 退場註 |
| `app.py` UI caption（`grep -n "驗收基準：R5 左側" app.py` ⇒ 已無此字串） | 公務員畫面 | **改寫**為新構造敘述＋明記 146.50 已退場 |
| `docs/W-B_細部plan.md`（:11/193/207/264/280） | 歷史文件 | 檔頭加退場註·**內文不改** |
| `docs/配地計算總規格_v3.md`（:197/320/534/**562**） | 歷史文件 | 同上 |
| **`docs/配地計算總規格_v3.md:562`** | — | 🆕 **施工單清單未列·本批 grep 增補** |

### 3.2 ⚠️ 同號不同物（**未動·勿誤刪**）

`146.5` 之多數命中實為 **`12,146.5579㎡ 公設共同負擔`**，與街角基準無關：
`docs/W-F_細部規格.md:24`、`verify/baselines/**/PROVENANCE_v3.md`（六處）、
`verify/run_verification.py:764`、`verify/stepg_pipeline.py:50`、`baselines/v2/PROVENANCE_v2.md:18`。
**全部未動。**

### 3.3 🚩 未處置一處（**具名·非靜默略過**）

`AGENTS.md:170`「R5 左側街角規定範圍 ≈ 146.50㎡（退縮 0m）」——該檔為
**untracked**（`git status` ⇒ `?? AGENTS.md`）、非本倉受控檔，施工單清單亦未列。
**本批未動**，具名上呈由 KL 決定是否納管或自行更新。

---

## 四、C-3：A-4 同源閘誠實標籤（零行為）

依 claude.ai 獨立複驗（`run_step_g` **19 個呼叫端** snapshot 全部來自 `load_snapshot()`；
母體用同一個 `F3_CATEGORY_BURDEN` 過濾故恆等），
`stepg_pipeline.assert_depth_same_source` docstring 已加註：

- 本閘**在所有現行呼叫點恆真**，與 K-4-4 之 `s_max_left`／`front_p2` 閘**同族**（第三次出現）
- **其價值在「凍結未來呼叫點」**，不在「現在咬到了什麼」
- ⚠️ 人工餵原始快照之反例測試，證明的是「**若**有人這樣寫會被咬」，**不是**「現在有人這樣寫」
  ——**不得充作判別力證據**
- 🗄️ 登記泛化波觀察項：**恆真閘家族**（A4 registry／K-4-4／本閘）宜統一標註體例

> 📌 **本批亦更正我先前之回報用語**：commit A 回報曾以「Discriminative power tested」
> 列為判別力證據，依本節標準**應降格為「載具自檢」**。

---

## 五、C-4：裁定檔補寫（零行為）

`docs/rulings/K-6_街角地分配程序與可分配判準.md` 新增：
**K-8 §五-1**（逐字）／**K-8 §五-2**（逐字）／**🔒 K-6-B 必查項登記**
（`628-45` 之最終歸屬須於 K-6-B 上線後重新確認·不得沿用本批結論）／
**`GSA_EXPECT` 重錨登記**（指向本報告）。

---

## 六、🚩 上呈 KL

1. **`AGENTS.md:170`** 之 146.50 未處置（untracked·§3.3）。
2. **E 系列實測快照**（`verify/baselines/E系列實測快照_退縮{0m,3.5m}.csv`）
   ＋ **全部 v3／wf baselines** 之重烤，仍待 U-K8-1＝乙 之波末批次；
   在此之前 46 紅中之 **44 紅（源A 22 ＋ 源B 22）** 係**該紅照紅**、非缺陷。
3. **既有 2 紅**（`W-F F.4` 3.5m E2 結構性不可行＋`W-G G.2` 連坐）仍待域裁·未擴大。
4. **停機條件三項（施工單 §7）皆未觸發**：
   五格重錨全部追出因果鏈｜F-1 覆蓋率閘無未評估錨鍵｜`SNAP_WAVG`／`TIER3` 未 raise。

### 未做或做不到者（禁靜默略過）

- **F.0–F.4 之 baseline 對拍仍紅**——重錨只解封「名目產生」，**未**使 baseline 相符；
  相符須待重烤批（U-K8-1＝乙）。
- **`AGENTS.md`** 未動（§3.3）。
