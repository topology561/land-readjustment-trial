# `W-G.9-4b`：`W` 單一產生者之**接線修復** ＋ 接線閘**射程補齊**（**事前登記**·⛔ 零碼變更）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本 worktree 之本地支 `wg94b-wiring`）
**基準**：`1041874af0e215e0e82cd5ca16274c72eaf757e3`
**批號現查未占用**：`grep -rn "W-G.9-4b" . --exclude-dir=worktrees --exclude-dir=.git | wc -l` ⇒ `0`

> 🔴 **本批之最高原則仍為：<u>生產行為不得改變</u>。**
> 本批係 `W-G.9-4`（expand）之**收尾**——`W-G.9-4` 只完成了「引擎側取用」，
> **未完成 app 側之名單登錄** ⇒ app 生產路徑之 `ns` 取不到該名。
> 🔒 **本批仍屬 expand 側：加路、不換路。** 族②③ 之生產消費**不切換**（屬 `W-G.9-5`）。

---

## §0 「已跑／未跑」聲明（⛔ 指令與輸出並列·**皆本登記落筆當次重跑**）

```
$ git rev-parse --short HEAD
1041874

$ python -c "<讀 app.py 之 _WF_NS_NAMES 字面·印(名目數, 是否含 k956_W_from_mp)>"
25 False

$ grep -n 'ns\["k956_W_from_mp"\]' verify/stepg_pipeline.py
334:    _k956_W_from_mp = ns["k956_W_from_mp"]

$ grep -rn "W-G.9-4b" . --exclude-dir=worktrees --exclude-dir=.git | wc -l
0

$ git log --format="%h %s" 78079fa..HEAD -- app.py verify/stepg_pipeline.py
2e6afc6 W-G.9-4 commit B（expand）：W 之單一產生者 ＋ stepg 委派 ＋ app 併行診斷（🔴 生產行為零變化）

$ ls .claude/worktrees 2>/dev/null || echo "(無巢狀 worktrees 目錄)"
(無巢狀 worktrees 目錄)
```

⇒ **本 worktree 內無巢狀 worktree** ⇒ `grep -rn . --exclude-dir=worktrees` 之母體未受污染
（`git-quotepath-cjk-trap`／巢狀污染二陷阱之現查排除）。

**另兩項狀態現查**（⛔ 非本登記之預測·僅為受詞之背景）：

```
$ grep -n "G.1" verify/out/WG94_runall.log | head
114:… W-G G.1 接線層 ctx-builder 同源（app _build_wf_ctx vs native _ctx；0m）
297:  🔴 FAIL  W-G G.1 接線層 ctx-builder 同源
298:        [G.1] '0m'

$ grep -rn "wg_g1_smoke" --include=*.py . | grep -v worktrees
./verify/probes/probe_corner_trueG.py:55:from wg_g1_smoke import _reconstruct_sb_rows
./verify/wg_g1_smoke.py:6:用法：python verify/wg_g1_smoke.py    退出 0＝跑通。
./verify/wg_g2_smoke.py:18:from wg_g1_smoke import _reconstruct_sb_rows
./verify/wg_g3.py:24:from wg_g1_smoke import _reconstruct_sb_rows
```

⇒ ① W-8 反向閘所在之 `W-G G.1` 項於基準 log 即以 `'0m'` 崩於**差集檢查之前**
（施工單 §2-3 之「死閘」敘述**經現查相符**）；
⇒ ② 🔴 **本登記新增之現查**：`verify/wg_g1_smoke.py`（其 docstring 自述
「app `_build_wf_ctx` → wf_f0→f4 全鏈端到端跑通」＝**真 app 路徑之複本**）
**不被任何自動流程執行**——三處命中皆僅 `from wg_g1_smoke import _reconstruct_sb_rows`
（取其 helper），`verify/run_all.py` 對 `smoke` 之命中為 **0**。
⇒ 此**恰為** `run_all.py:99` 註解所指之同一覆蓋率洞（「檔存在於 `verify/` 卻不在任何自動流程內」）。

🔴 **未跑（本登記之受詞）**：

- ⛔ 尚未改動任何一行碼（生產碼與 `verify/` 皆然）。
- ⛔ 尚未以 AST 判定 `stepg:334` 是否落於 `Try`／`If` 內。
- ⛔ 尚未以 `_wf_ns()` 建出之 ns 實際呼叫過 `run_step_g`。
- ⛔ 尚未跑本批之 `run_all`。

---

## §1 本批之受詞：命題 **P**

> **P**：於 app 生產路徑（UI 鈕「執行七級調配」`app.py:21931` → `_build_wf_ctx`
> → `_wf_ns()` → `wf_f0.compute` → `run_step_g`），`ns["k956_W_from_mp"]`
> 拋 `KeyError` ⇒ 七級調配全鏈無法產出。

**P 之判定 ⛔ 不得由施工單之敘述推定**，須**由構造**雙證（見 §2 之 `F1`）。

---

## §2 可證偽預測（⛔ 事後不得回頭修改）

| # | 可證偽預測 | 證偽方式 |
|---|---|---|
| **F1** | 命題 **P** 為真：`stepg:334` 之 `ns[...]` **無條件執行**（AST 證其非落於 `Try`／`If`／`ExceptHandler` 內），且以 `_wf_ns()` 建出之 ns 呼叫 `run_step_g` **實拋** `KeyError: 'k956_W_from_mp'` | **未拋**（或 AST 顯示其為條件式／被包覆）⇒ **F1 破 ⇒ `S′-1` 停機上呈**，全單作廢 |
| **F2** | 接線修復後，W-8 反向閘之差集（引擎實耗 − `_WF_NS_NAMES`）為 **空**，且與 `6ab9e86` 同 | 差集非空 ⇒ **F2 破** |
| **F3** | 修復後 app 路徑之七級調配**可完成**，且其輸出與 `6ab9e86` **相同** | 任一項不同 ⇒ **F3 破 ⇒ `S′-2` 停機** |
| **F4** | 本批之 `run_all` 判定層與 `verify/out/WG94_runall.log` 相同，**唯一容許之差 ＝ 新夾具之列**（＋已具名之 worktree 路徑／traceback 行號類差） | 出現其他判定改變 ⇒ **F4 破** |
| **F5** | 新夾具之竄改自檢**具鑑別力**：自 `_WF_NS_NAMES` 抽掉一名 ⇒ 夾具轉**紅**；還原 ⇒ 轉**綠** | 抽名後仍綠 ⇒ **F5 破 ⇒ `S′-3` 停機**，該夾具不得計入交付 |

### 2-1 `F3` 之對拍方法（⛔ 事前寫死·鍵為 **(情境 × 街廓 × 側別)**）

- **兩端**：`6ab9e86`（變更前·app 路徑當時可跑）vs 本批修復後之 `HEAD`。
- **取值路徑**：⛔ **必經 `_wf_ns()`**（即受限 ns）；⛔ `harvest()` 之全 globals 路徑
  **不得**充作 `F3` 之證據（施工單 §6 閘 3）。
- **鍵**：`(tag, 街廓, 側別)`；⛔ **不用宗序號**。每格內以「按 `暫編地號` 排序之
  `G(㎡)`／面積列」之校驗和比對（`暫編地號` 為**格內資料**、非鍵）。
- **`6ab9e86` 端之取得**：於 scratchpad 另開 **detached worktree** 執行，
  ⛔ 不在本工作樹 checkout、⛔ 不用 `git reset --hard`。

### 2-2 新夾具之**必要性質**（⛔ 事前寫死·⛔ 事後不得放寬）

新夾具（掛入 `verify/run_all.py` 之末端夾具清單）須**同時**具備下列，⛔ 缺一不可：

1. **靜態層**：以 **AST**（⛔ 非固定距離窗之正則）取引擎檔之 `ns["X"]` 取用點，
   與 `_WF_NS_NAMES` 取差集，差集非空即**紅**。
2. **靜態層自述三項射程**：夾具**自身輸出**須逐項印出【目錄射程】【字樣射程】【連接方式】；
   ⛔ 字樣不得預設符號名（須為「任何 `ns[<字面字串>]`」之泛式）。
3. **動態層**：以 `_wf_ns()`（⛔ **非** `harvest()` 全 globals）建出之 ns **實際呼叫**
   `run_step_g` 至少一次；⛔ **不得**以 `try/except` 吞掉例外。
4. **竄改自檢**：暫抽一清單名 ⇒ 夾具轉紅；還原 ⇒ 轉綠。**二態皆印入夾具自身輸出**。
   ⛔ **空集合不得算通過**（母體為 0 時須自判紅——「空真假綠」病灶）。

---

## §3 停機條件（⛔ 逐字採施工單 §4）

**🛑 真域邊界 ⇒ 停機上呈 KL**：本批**無**預期之域邊界。若觸及，以
【現況】→【要改成】→【對土地的影響】→【要你判斷】格式上呈，⛔ 禁程式術語。

**🛑 立即停機（紅線·非域邊界）：**

- **`S′-1`**：§1 之命題 **P 為偽** ⇒ **全單作廢**，把反證（指令＋輸出）寫入報告，
  ⛔ 不得逕自繼續。
- **`S′-2`**：接線修復後，`app.py` 生產輸出（G 值／面積／抵費地）與 `6ab9e86`
  **仍有任一項不同** ⇒ 停機，⛔ 不得以「新值才是對的」保留。
- **`S′-3`**：新夾具之竄改自檢**無鑑別力**（抽名後仍綠）⇒ 停機，該夾具不得計入交付。

**純技術項 ⇒ 自行做完至 push**：探針、夾具、log、報告、加註、考古節撰寫。

---

## §4 🔒 `S-2` 之裁定引用（⛔ 對事前登記文字之偏離·須逐字記明）

`W-G.9-4` 事前登記（`docs/reports/W-G.9-4_前置登記.md` §3）之 `S-2` **逐字原文**：

> - **S-2**：**E3 或 E4 破**（生產行為改變）⇒ **立即停機**並**回退該變更**；
>   ⛔ 不得以「新值才是對的」為由保留——**本批之最高原則是行為不變**。

🔒 **裁定（claude.ai·施工單 `W-G.9-4b` §2-1）**：准以**前推修復**（roll-forward）結案，
**不回退** `2e6afc6`。**理由與代價逐條**：

1. **判準未放寬**——仍為「生產行為與 `6ab9e86` 相同」，且本單**加重**其舉證
   （§2-2 之動態層與竄改自檢為**新增**要求）。
2. **所異者僅為結案手段**（roll-forward vs revert），**非判準**。
3. ⚠️ **代價**：此為對事前登記文字之**偏離**。故本批須：
   ① 於本登記**逐字引用** `S-2` 原文並記明本裁定（＝本節）；
   ② 於 `docs/reports/W-G.9-4_expand報告.md` **加註**（⛔ 原句一字不刪）
   記明 `S-2` 曾被觸發、而當批**未偵測到**。

---

## §5 驗收（⛔ 逐字採施工單 §6·**禁以計數或列數為證據**）

| # | 閘 |
|---|---|
| 1 | 命題 **P** 之判定由 **AST ＋ 實際呼叫**雙證，⛔ 非僅 grep |
| 2 | `F3` 之對拍**以 (情境 × 街廓 × 側別) 為鍵**，⛔ 不用宗序號；列出對拍方法與殘差 |
| 3 | `F3` 之證據**來自 `_wf_ns()` 路徑**，⛔ `harvest()` 路徑之結果不得充作其證據 |
| 4 | `F4` 之對照為**逐項**，且新夾具之列**正面標示為本批新增**，⛔ 不得混入「逐字相同」之措辭 |
| 5 | 新夾具之**竄改自檢兩態**（紅／綠）皆印入其自身輸出 |
| 6 | 夾具之靜態層**自述三項射程**；字樣**語意窮舉**、⛔ 不預設符號名、⛔ 無距離窗 |
| 7 | 每一出艙之數，其指令於**落筆當次重跑**且指令與輸出並列 |
| 8 | `git diff --numstat` 對 `verify/wf_f*.py` 為**空輸出**；對文件之**刪除欄為 0** |
| 9 | 凡書寫「正典」二字，就地標明**權威序層級**（`CLAUDE.md:4`·本倉共 **4 級**） |
| 10 | 施工單 §2-4 之三處加註皆已落地，且**原句一字未刪** |

---

## §6 已預先承諾之判準（⛔ 事後不得放寬）

- 🔴 **生產行為不變為最高原則**：⛔ 不得為「順手修對」而改變任何生產輸出。
- ⛔ **不切換族②③ 之生產消費**（`_W_prev_left/right`、`_select_pool_slot` 之 `'b'`
  仍取舊式 `buf · _cos_dn`）——屬 `W-G.9-5`。
- ⛔ 不動 `buf`／`_corner_buffer_S`／`_build_corner_range_v3`／`GB-58`。
- ⛔ 不動 `verify/wf_f*.py`；⛔ 不重烤任何基準 CSV；⛔ 不設 `WV_BAKE`。
- ⛔ 不改動現行 W-8 反向閘、⛔ 不改 `W-G G.1` 之錨以求其不崩
  （上游 `KeyError: '0m'` 屬**另案**，本批不修）。
- ⛔ 不於 `k956_W_from_mp` 或 `_mp_base_W0` 內加入任何 fallback／`ns.get` 靜默退路
  （`no-silent-fallback`；此即 `GB-60` 已登記之病灶族）。
- ⛔ 不改期望 FAIL 名單；⛔ 禁 `git reset --hard`；⛔ 禁對 `verify/out/` 用 `git add -A`／`.`。
- ⛔ 為使 `run_all` log 相同而**不把新夾具掛入** `run_all` ＝ 明確禁止。
- 否定性宣稱須**同時**寫【目錄射程】【字樣射程】【連接方式】；字樣須語意窮舉、
  ⛔ 不預設符號名；⛔ 不以固定距離窗 `.{0,N}` 連接兩概念。
- 凡書寫「正典」須就地標**權威序層級**（`CLAUDE.md:4` 共 **4 級**）。
- 🔴 **`rc=1` 不構成「命題為偽」**（考古 64）。

🔒 **自誌（`CLAUDE.md`：「立戒者在下一批最容易違反自己的戒」）**：

`W-G.9-4` 新立之戒為「⛔ 不得由參數名推定語意，須追至消費端」。
**本批所修之缺陷正是同一戒的另一面**——由「我只給 `stepg` 用」推定接線範圍，
而**未追 `stepg` 之全部呼叫端**。

⇒ **本批凡下任何「某物只被 Y 用」之判斷，須現查其全部呼叫端並逐一列出**，
⛔ 不得以用途推定。本登記 §0 之第二項現查（`wg_g1_smoke` 之三處命中逐一列出、
而非斷言「只有 X 用它」）即為本戒之首次自套用。
