# `W-G.9-268′` 補令九 序 `1′`：人造擾動之判別力 ＋ 款 `(2)` 逐側鏈序 —— ✅ **通過**

> 🔒 **結論**：`§三-2` 判別力 ✅ ⋀ `§三-3` 款 `(2)` 逐側鏈序不變 ✅
> ⇒ 補令九 `§七` 序 `1′` 之三停機款（人造擾動不轉紅／二輪同色／任一側鏈序改變）**皆未觸發**。
> 🛑 **序 `2`〜`5` 一律未辦**——序 `2` ＝ 發單側逐位元組復驗，係其職。
> 🔒 **主線 `wip/s1-endpart` 原地不動**（`683b7fa`）；本批之 `commit` 全落側支 `verify/W-G.9-268p-c2`。

---

## §零　三軸之總宣告（`VR-091`）

> `(1)` **來源 `commit`**：主線 ＝ `683b7fa49e4e20f53593c363b7d7050c91218d20`（`ls-remote` 實查·原地不動）；
> 側支期初 ＝ `d1aebebff074ce15c090e3ea50f1e6a7598d53a0`（＝ 本地 HEAD·實查相符）。
> `(2)` **母體**：逐節具名。　`(3)` **粒度框**：逐節具名（側／宗／格／公尺／㎡）。

🔒 **工項零**：補令九原封入倉（`73d70f4`）——`154` 列／**`13182`** B／`CR 0`；
來源與落檔 `sha256` 皆 `0649674dc355af8143cfed8d9cbfa673c16c91f87bc5d0f2edf5f2c46c91bea1`／`cmp rc = 0`。

**器** ＝ `verify/probes/probe_WG9268p_c2_v1prime_pert.py`（新建）。`rc = 0`／`stderr` **`0`** B。
**落檔** ＝ `verify/out/WG9268pR_c2_v1prime_pert.log`（`13200` B）。
🔒 **判準之來源**：倉內既有 `probe_WG9266_V1prime_3groups`——**只換資料源**，
五款與 `1e-6` **一字不動**（`TOL_K1 ＝ 1e-06`／`TOL_K3 ＝ 1e-06`·**自該模組現讀並出艙**）。

---

## §一　🩸 補令九 `§三-2` 之字面與器之**二處結構性不相容**（`常規二`·逐字回報）

🔒 **二者皆由<u>讀碼</u>確定，⛔ 臆測**；並已由本批**實測坐實**（`§二-2`）。

| # | 不相容 | 機械依據 | 實測 |
|---|---|---|---|
| `(甲)` | 「單獨平移**一宗** `1e-3 m`」⇒ **閘 `γ` 必先不過** | `V1.shared_edge` 以 `QK = 1e-6` 認共用頂點；平移 `1e-3`（`> QK` 三個量級）⇒ 該宗與鄰宗**⛔ 再有共用頂點** ⇒ `V1.run` 回 **`refuse`**、**⛔ 回紅** | 閘 `γ` 由 `6/6` 轉 **`4/6`** ⇒ **loud 拒測** ✅ 坐實 |
| `(乙)` | 「`(5)` 須 `> 0`」⇒ **結構上不可能** | `(5)` 之受詞 ＝ `ΔG ＝ B['G(㎡)'] − A['G(㎡)']`，取自**列之欄位**；**剛體平移⛔ 動該欄** ⇒ `ΔG ≡ 0` ⇒ `(5) ≡ 0` | 式 `甲` 之全側 `|ΔG|max ＝ 0.0000 ㎡` ✅ 坐實 |

🛑 **⇒ 單所定之「不轉紅 ⇒ 閘失能·停機」對式 `甲` <u>⛔ 適用</u>**
——其未轉紅之因係**拒測**（`無從驗`），**⛔ 不敏感**（`驗不過`）。
二者⛔ 共用出艙碼（`序 11 之一般化` 款 `三`／`自誤 338`／`341`）。

🔒 **本批之處置（`常規二`：取保守項續辦 ＋ 逐字回報·⛔ 上呈 KL）**
**二式並跑、二式皆出艙**，量級**固定 `1e-3 m`**、判準**一字不動**、容差**一字不動**：

> **式 `甲`（單之字面）**：單獨平移一非鏈頭宗 ⇒ 照實出艙其結果。
> **式 `乙`（保守項）**：平移**一條共用界線之二頂點**（於**相鄰二宗內同步**移動，使共用邊仍恰 `2` 頂點）
> ＋ 將該二宗之 `G(㎡)` 欄改設為其**擾動後之多邊形面積**（使閘 `β` 仍過、且 `ΔG ≠ 0`）。

🛑 **式 `乙` 所更改者 ＝ <u>擾動之施加方式</u>，⛔ 判準、⛔ 容差、⛔ 量級**
（⛔ 觸 `VR-094` 二「⛔ 調框以就結果」）。**其是否採為正式判別力，候發單側。**

---

## §二　`§三-2` 人造擾動之判別力（組 ＝ `3.5m`·`R2`·`left`·`V-1′-a` 三組之一）

🔒 `d̂` ＝ 生產解析器之 `FRONT_LINE` `p1→p2` ＝ `(0.675685532, 0.737189977)`（⛔ 質心導出）。
🔒 該側宗數 ＝ `7`／**判定集基數 `n` ＝ `6`**（非鏈頭）。擾動量 **固定 `0.001 m`**（⛔ 調至恰好轉紅）。
🔒 **比較端 `OFF` ＝ `3.5m`／`off`**（`47` 列·⛔ 混入 `0m` 之列·本批明文化並出艙其列數）。

### 二-1　輪 `1`〔**陰性**·未擾動〕

| 閘 | 態A／態B |
|---|---|
| 閘 `γ` | `6/6` ✅／`6/6` ✅ |
| 閘 `α` | `5/5` ✅（最大 `|Δ| 0.003677`）／同 |
| 閘 `β` | `7/7` ✅／`7/7` ✅ |

`(1a)` ✅／`(1b)` ✅（`Δ ∈ [−1e-9, +1e-9]`）／`(2)` ✅／`(3)` ✅（極差 `1e-9`）／`(4)` ✅／`(5)` **`0` 宗**
⇒ 🟢 **陰性全綠**。

### 二-2　輪 `2`〔式 `甲`·**單之字面**〕

受擾之宗 ＝ `628-41(1)`（非鏈頭）｜⛔ 動其 `G(㎡)` 欄（剛體平移）。

> 閘 `γ`（態B）＝ **`4/6`** 🔴 ⇒ `V1.run` **loud 拒測**（逐字：「🔴 自我驗證閘不過 ⇒ ⛔ 據本表下任何結論」）。

🛑 **⇒ 該輪⛔ 產生「轉紅／不轉紅」之判**；其為 `§一 (甲)` 之**實測坐實**。

### 二-3　輪 `3`〔式 `乙`·**保守項**〕

受擾之界 ＝ `628-41(1)` ↔ `628-27(1)`｜**共用頂點數 ＝ `2`**（須 `2`·⛔ 靜默改選）。
`G` 之同步更新：`628-41(1)` `183.2300 → 183.2678`／`628-27(1)` `29.6100 → 29.5705`（＝ 擾動後多邊形面積）。

| 閘 | 態A／態B |
|---|---|
| 閘 `γ` | `6/6` ✅／**`6/6`** ✅ |
| 閘 `α` | `5/5` ✅／`5/5` ✅ |
| 閘 `β` | `7/7` ✅／**`7/7`** ✅ |

| 款 | 判 | 實測 |
|---|---|---|
| `(1a)` | ✅ | 六宗逐字相同 |
| **`(1b)`** | 🔴 **紅** | `Δ ∈ [−0.001000000, +0.001000000]`——`628-41(1)` `+0.001000000`／`628-27(1)` `−0.001000000`，餘四宗 `≤ 1e-9` |
| `(2)` | ✅ | 鏈序二態逐位相同 |
| **`(3)`** | 🔴 **紅** | **極差 ＝ `0.001000000`**（判準 `≤ 1e-6`）——`628-27(1)` `δ ＝ +0.001000000`，餘 `≤ 1e-9` |
| `(4)` | ✅ | 態A／態B 殘差皆 `−0.0200`（`2dp` 捨入內） |
| **`(5)`** | 🔑 **`2` 宗** | `628-41(1)` `ΔG ＝ +0.0400 ㎡`·`δ ＝ +0.000000000`；`628-27(1)` `ΔG ＝ −0.0400 ㎡`·`δ ＝ +0.001000000` |

🔒 **擾動之傳播恰如其構造**：`(1b)` 之 `Δ` 與 `(3)` 之極差**精確為 `0.001000000`**
（⛔ 近似·⛔ 依賴容差之邊緣）⇒ 其對 `1e-3` 之敏感度**確為三個量級之餘裕**。

### 二-4　判（`§三-2` 逐字）

| 項 | 應然 | 實測 | 判 |
|---|---|---|---|
| 陰性（未擾動）須全綠 | 全綠 | 全綠 | ✅ |
| 式 `乙` `(1b)`／`(3)` 須轉紅 ⋀ `(5) > 0` | 紅·紅·`> 0` | 紅·紅·`2` 宗 | ✅ |
| **二輪同色？** | 須 `False` | **`False`** | ✅ **閘⛔ 失能** |
| 式 `甲`（單之字面） | —— | **loud 拒測** | 🟡 **⛔ 讀為閘失能**（`§一 (甲)`） |

---

## §三　`§三-3` `V-1′-b` 款 `(2)`：逐側**鏈序不變**

🔒 **⛔ 需共用邊框**——以序列直接比對（`暫編地號` 序 ⋀ `驗_宗序` 序，二者皆比）。
🔒 **已動／未動之判⛔ 用 `cut_coords` 逐位相異**（`§三-1` 明令：末位浮點差⛔ 真實位移）；
改用**無框可得**之二量：全側 **`|ΔG|max`** 與 **`max|Δ累積S|`**（皆 `> 1e-6` 方判已動）。

**母體基數（二態共有之側）＝ `13`｜其中已動 ＝ `3`**

| 情境 | 街廓·側 | 宗 | `|ΔG|max` | `max|Δ累積S|` | 已動？ | **鏈序** |
|---|---|---|---|---|---|---|
| `0m` | `R1·left` | `2` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `0m` | `R1·right` | `3` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `0m` | **`R2·left`** | `7` | **`3.5900`** | **`3.3900`** | 🔴 **已動** | ✅ **不變** |
| `0m` | `R2·right` | `7` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R1·left` | `1` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R1·right` | `4` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R2·left` | `7` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R2·right` | `7` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | **`R4·left`** | `1` | `0.0000` | **`1.5800`** | 🔴 **已動** | ✅ **不變** |
| `3.5m` | **`R5·left`** | `6` | **`3.6000`** | **`3.3200`** | 🔴 **已動** | ✅ **不變** |
| `3.5m` | `R5·right` | `2` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R6·left` | `3` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |
| `3.5m` | `R6·right` | `5` | `0.0000` | `0.0000` | ✅ 未動 | ✅ **不變** |

🟡 **僅 `on` 有之側**（**新可達**·⛔ 判·loud）＝ **`9`**：
`0m·R3·left`／`0m·R3·right`／`0m·R4·left`／`0m·R5·left`／`0m·R5·right`／`0m·R6·left`／`0m·R6·right`／
`3.5m·R3·left`／`3.5m·R3·right`。**⛔ 靜默略**——其於 `off` 態⛔ 存在，鏈序**無比較端**。

> 🟢 **全 `13` 側鏈序 `✅ 不變`** ⇒ **`V-1′-b` 款 `(2)` 通過**；
> ⛔ 觸「任一側鏈序改變 ⇒ 停機」。

### 三-1　🔑 併記：`§三-1` 之「⛔ 以 `cut_coords` 判已動」**經本批坐實**

以 `cut_coords` 逐位相異為判準者，`3.5m` 得**七側**已動（序 `1` 報告 `§四-1` 之診斷）；
以本節之 frame-free 二量為判準者，`3.5m` 僅 **`R4·left`／`R5·left` 二側**已動。
🔴 **⇒ 舊判準將 `5` 個<u>未動之側</u>誤報為已動**——即 `§三-1` 所戒之「末位浮點差⛔ 真實位移」。

---

## §四　`§三-4` 必答第 `2′` 項之對答

| `2′` 之三合取 | 出處 | 判 |
|---|---|---|
| `V-1′-a` **全綠** | 序 `1` 報告 `§三`（三組 `n ＝ 6`／`3`／`6`·五款全綠·自我驗證閘六輪全過） | ✅ |
| **人造擾動轉紅** | 本報告 `§二-3`／`§二-4`（式 `乙`·`(1b)`／`(3)` 紅·`(5)` `2` 宗·二輪⛔ 同色） | ✅ |
| `V-1′-b` 款 `(2)` **逐側鏈序不變** | 本報告 `§三`（`13` 側全不變） | ✅ |

> 🟢 **必答第 `2′` 項 ＝ 成立**（惟式 `甲` 之不相容須由發單側裁·`§一`）。

---

## §五　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| `73d70f4`（工項零） | `d1aebeb..73d70f4` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 |
| 本報告之 `commit` | `73d70f4..HEAD` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 |

🔒 **`c2` 之生產碼內容逐位未變**——`git diff --numstat c1376af..HEAD -- app.py` **空輸出**
（本批**⛔ 動 `app.py` 一字**）。

---

## §六　🩸 本批之**自捕**

**`KeyError: 'OFF'`（首跑·loud·⛔ 靜默）**：本器將二態存入 `ST` 而**未填** `ROWS["OFF"]`，
致 `V1.load("OFF")` 拋 `KeyError` ⇒ `rc = 1`／`stderr` `1278` B。
🔒 **量測器紅、⛔ 受詞紅**；已修（併明文化「比較端 `OFF` ＝ `3.5m`／`off`·⛔ 混情境」並出艙其列數）。
🔒 **其為 loud** ⇒ ⛔ 落靜默退路；修後 `rc = 0`／`stderr` **`0`** B。

---

## §七　續辦之界

🛑 **本批⛔ 辦者**：⛔ 併線、⛔ 推主線、⛔ 動 `app.py` 一字、⛔ 辦 `c3`／`c4`、
⛔ 鑄任何號（`自誤 343`／`344`／`GB-158`／`GB-159`／`GB-160` 皆屬**序 `4`**·在序 `3` 之後）、
⛔ 改 `V-1′` 五款之判準一字、⛔ 調任何框以就結果。

🔒 **候發單側**：`§一` 之式 `甲` 不相容之裁（式 `乙` 是否採為正式判別力）；
其後序 `2`（逐位元組復驗）→ 序 `3`（快轉併線）→ 序 `4`（五號之鑄）→ 序 `5`（`c3`／`c4`）。
