# W-G.9-122　CC 側交接文（`-111`〜`-121` 波·供下一個 CC session 接手）

**性質**：🔒 純新增 `1` 檔·⛔ 零生產碼·⛔ 零管線·⛔ 不落地·**`verify/` 零動**
**⛔ 非任何施工單所令**——KL 交辦（同 `W-G.9-111`／`-102`／`-68` 之先例）。

---

## §一　三句話現況

1. **`HEAD` ＝ `6ee8d91d9413d596fa2c4761436af6b509a9ffed`**（`origin/wip/s1-endpart` 同·工作樹 `0` 筆）。
   `app.py` blob ＝ `94c1d2b1d3290af2104e91d6a405f04abfe69cd4`（`1310936` B／`22206` 列）。
2. 🔴 **本波最重之發現 ＝ 圖資分歧**：KL 之 app 用 **`V6_1.dxf`**、`verify/` 全鏈硬寫死 **`V6.dxf`**
   ⇒ **CC 之驗證側與 KL 之 UI 側從來不是同一份圖**。此已由 `W-G.9-121` 逐格坐實（見 `§五`）。
3. 🛑 **停機仍在**：KL 之 `G` 值迭代仍卡在 `K-9-4`（`_GHOST_(R4)`·差 `32.727m`·`1` 宗）。
   `W-G.9-119` 之修法已 push、`W-G.9-120` 之診斷已 push，**下一步 ＝ 等 KL 重跑並截圖回報那段
   `🔍 K-9-4 ghost 判準診斷` 之 `st.warning`**。

---

## §二　`A-0` 開場必查（下一窗**⛔ 其後受詞在此之前一律未開辦**）

| # | 受詞 | 實得 |
|---|---|---|
| `1` | `git rev-parse HEAD` | `6ee8d91d9413d596fa2c4761436af6b509a9ffed` |
| `2` | `git ls-remote origin refs/heads/wip/s1-endpart` | 同上（⇒ 本地與 origin 一致） |
| `3` | `git status --porcelain` | `0` 筆 |
| `4` | `git rev-parse HEAD:app.py` | `94c1d2b1d3290af2104e91d6a405f04abfe69cd4`（🔒 git blob **`SHA-1`**·`40` 碼·⛔ 非 `SHA-256`） |
| `5` | `app.py` 內容之 `sha256` | `bb36efe58cb50207a7999dce935bce90…`（`1310936` B·`22206` 列·`splitlines()`） |
| `6` | `data/V6.dxf` | `88341` B·`sha256 a3de6fcefc0c9e4f35ba48dac7fed561…` |
| `7` | `data/V6_1.dxf` | `88453` B·`sha256 33ed09a16b0cd5e11e503121ae7cd285…` |
| `8` | `verify/` 已追蹤檔數 | `936`（其中 `verify/baselines/` ＝ `298`） |
| `9` | `wg942_append_audit.py <HEAD>` | `rc = 0`／`結論：✅ 全部成立`／受檢 `77` |
| `10` | `wg9103_target_coverage.py <HEAD>` | `rc = 0`／`結論：✅ 本批受詞全數受涵蓋` |
| `11` | `wg973_gb_integrity.py` | `rc = 0`／`懸空 ＝ 母體 97 − 有定義列 95 − 缺號 2 ＝ 0` |
| `12` | 三器跑畢之 `git status` | `0` 筆（⇒ 該三器⛔ 不寫倉） |

🔴 **短路徑硬要求**：拋棄式 clone **⛔ 不放 scratchpad**（其路徑 `183` 字元·破 `MAX_PATH`）。
本波一律用 `C:\wg1NN\{w,m,…}`。且 **系統級 `core.autocrlf = true`**（`file:C:/Program Files/Git/etc/gitconfig`）
⇒ clone 必帶 `-c core.autocrlf=false` 並再 `git config core.autocrlf false`。

---

## §三　commit 鏈（本波·近 `10` 筆）

| commit | 日 | 題（節） |
|---|---|---|
| `6ee8d91` | 08-25 | `W-G.9-120` `K-9-4` ghost 判準自報「哪一條沒過」（診斷·⛔ 零行為變更·`19` 增／`0` 刪） |
| `deb1cd9` | 08-25 | `W-G.9-119` `K-9-4` 母體排除「無地主之未覆蓋殘餘」（**首個動生產碼之波**·`28` 增／`1` 刪） |
| `8a9f6ae` | 08-24 | `W-G.9-115` `32` 格定案測 ＋ `GB-97` 補正①〜④ ＋ 自誤 `142` |
| `5ff02cb` | 08-24 | `W-G.9-114` `R4` `34` 列逐列時序判定 ＋ `GB-97` ＋ 自誤 `141` |
| `27a2816` | 08-24 | `W-G.9-112` 撤銷「`R6` 街角地消失」之全部斷言 ＋ 積欠登記一次入倉 |
| `ce24777` | 08-24 | `W-G.9-111` CC 回報入倉（`§二` 六數量測 ＋ `§零 閘二` 停機） |
| `7d3407d` | 08-24 | `W-G.9-111` CC 側交接文入倉 |
| `99e200a` | 08-24 | `W-G.9-108` 修 `wg942_append_audit.py`（`GB-92`） |
| `a83c28c` | 08-24 | `W-G.9-106` 丙-0：`run_all` 雙機對照 ＋ `VR-069`／`070`／`GB-93` |
| `d593d50` | 08-24 | `W-G.9-105` 乙：修 `wg973_gb_integrity.py`（`GB-90`） |

⚠️ **`W-G.9-113`／`-116`／`-118`／`-121` ⛔ 無 commit**（皆純量測單·結果只在聊天）。
⚠️ **`W-G.9-109`／`-110` 已作廢**（`W-G.9-112 §零之前` 逐字：其 `GB-96` 本於一個已證偽之斷言）。

---

## §四　🔴 本波之三個實質結論（皆已坐實）

### 4-1　`R6` 右街角地**從未消失**（`W-G.9-111`／`-112`·已入倉 `27a2816`）

`GB-96` 之**舊義**（「乙式使 `R6` 右街角地消失」）**已證偽並全部撤銷**。
六數實測：`T1` 之 `_corner_range_right` **⛔ 非 `None`**、面積 `145.88 → 146.38`（`0m`）／`298.14 → 299.13`（`3.5m`）**皆變大**、
真交集四格 `16.81`／`129.13`／`98.02`／`200.61` **皆遠高於 `1.0` 門檻**、`_has_p2 = True`、
winner `T0 ＝ T1 ＝ G005（628-18）[628-18(1)]`、街角側二 rev 皆 `8`。

🔑 **病根**：「`26 → 2` 活列」量到的是掛在 **`🔴 FAIL`** 標頭下之**違規報告列**，
其**極性與「存否」相反**（列愈多愈紅）⇒ 真相是 `4df19f4` 把 `R6` 右**修對了**。
⇒ `GB-96` **新義** ＝ 乙式使八格中七格趨近 `baseline`、**僅 `R4` 左 `3.5m` 反向**。

### 4-2　全部違規列皆係**舊基準**、程式⛔ 無誤（`W-G.9-113`〜`-116`）

以**各 baseline 之凍存 `rev_f` 重跑**（`rev_f` 須**逐檔**決定·九檔收斂為三 rev）：

| `rev_f` | `app.py` blob | 服務之表 | 結果 |
|---|---|---|---|
| `1b290e4691ac5c8e327573259aa2cc20adf5de28` | `c640f436c133…` | `指配{0m,3.5m}`／`抵費地3.5m` | `ALL GREEN`·`FAIL 0` |
| `6830642d66eb571682b0684c4d8500507e72ed7c` | `dfa8792328eb…` | `v3·診斷{0m,3.5m}` | `ALL GREEN`·`FAIL 0` |
| `7ce98e6adbf1b298f274ede064cd7970a93ab1ae` | `063ae07ee3b6…` | `參數{0m,3.5m}`／`無串聯{0m,3.5m}` | `ALL GREEN`·`FAIL 0` |

⇒ 🔒 **該九份 baseline 係純舊快照** ⇒ 處置 ＝ **KL 於 UI 重產基準**、⛔ 非修程式。
**`G-6` KL 行動清單（該重產之 `9` 份·皆在 `verify/baselines/`）**：
`退縮0m參數.csv`／`退縮3.5m參數.csv`／`第 1 宗街角地指配結果_退縮0m.csv`／`第 1 宗街角地指配結果_退縮3.5m.csv`／
`W-D.1.2 診斷_退縮0m.csv`／`W-D.1.2 診斷_退縮3.5m.csv`／`v3/W-D.1.2 診斷_退縮0m.csv`／`v3/W-D.1.2 診斷_退縮3.5m.csv`／
`W-D.1.3-d 驗收_退縮3.5m.csv`。

### 4-3　`K-9-4` 母體排除「無地主之未覆蓋殘餘」（`W-G.9-118`／`-119`／`-120`·已入倉）

**KL 域裁 `2026-08-24` 逐字**：「`R4` 街廓內的碎片是重劃範圍邊界的細碎塊? **如果是就不算一宗土地**。
但如果是有地主的地…**若這筆碎塊土地是有地主的，無法分配土地就是要給補償**」

判準綁**三個權利屬性之合取**（`app.py:6261-6265`）：
`原地號 == '_GHOST'` ∧ `float(G(㎡)) == 0.0` ∧ `重劃前區段 == ''` ⇒ 落 `_no`；
同批調整 `_bad_no` 自檢之受詞（`:6288-6292`）。`-120` 另加**純增印**之診斷（`:6267-6285`）。

---

## §五　🔴🔴 **最重之未解項：圖資分歧**（`W-G.9-121`·⛔ 無 commit）

| 側 | 讀哪一份 | 出處逐字 |
|---|---|---|
| `verify/` 全鏈 | **`data/V6.dxf`**（硬寫死） | `verify/run_verification.py:33` `V6DXF = os.path.join(REPO, "data", "V6.dxf")`；全倉 `173` 命中中 **`138` 列／`108` 檔**經 `rv.V6DXF` 繼承之 |
| `app.py`（KL 之 UI） | **KL 當下上傳者** | `:16067` `st.file_uploader("選擇分配街廓 DXF 檔（例：test.dxf）", type=["dxf"], key="f3_block_upl")` ⇒ `:16085` `parse_block_dxf(up_block.getvalue() ...)`。🔒 **`app.py` ⛔ 無任何內建 `data/` 路徑** |

🔒 **KL 匯入者確為 `V6_1.dxf`**（KL 裁 `2026-08-24` 逐字：「用 `V6_1.dxf`　因為這份檔案應該是
前幾次 CC 有叫我修改 `R1` 及 `R4` 街角部分的修改後圖檔」），CC 側實測坐實：

| 街廓 | `V6.dxf` 全精度／2dp | `V6_1.dxf` 全精度／2dp | KL 之 `§7-0a` |
|---|---|---|---|
| `R1` | `33.146088` / `33.15` | `33.145954` / `33.15` | `33.15` |
| `R2`〜`R3`／`R5`〜`R6` | （四塊全精度**逐位不變**） | 同 | 相符 |
| **`R4`** | `33.104629` / **`33.10`** | `33.112192` / **`33.11`** | **`33.11`** |

🔑 **只有 `R4` 跨過 `33.105` 之進位門檻**（二圖相距 `7.563` 公釐）⇒ 這解釋了為何六塊只有 `R4` 差、且只差 `0.01`。

⚠️ **十六格街角範圍面積在二圖下<u>完全相同</u>**（且皆與 KL 實得相符）⇒ **該十六格⛔ 無鑑別力**，
分不出 KL 用哪一份。**真正有鑑別力者只有 `R4` 深度一格。**

🔒 **倉內早已載明此事**：`verify/baselines/PROVENANCE.md:45` 逐字
「🔴 **本檔尚未成為任何 baseline 之輸入**——現行全部 baseline 仍以 `data/V6.dxf` 產出」、
`:47`「⛔ **不得覆蓋或刪除 `data/V6.dxf`**」。
⚠️ 惟 `:46`「生產路徑（`app.py`／`verify/run_verification.py::V6DXF`）一律未改」對 **`app.py` 之描述⛔ 不精確**
——`app.py` 根本**沒有**該路徑可言。

🛑 **未決**：圖資之取捨（`verify/` 是否改讀 `V6_1`／九份 baseline 以哪一份重產）**⛔ 尚未有裁**。

---

## §六　🩸 CC 自我更正（`W-G.9-116` 之量詞）

`run_verification.py:1451` 逐字
`print(f"        …（**另 {len(viol) - 12} 列未顯示**·本閘違規總計 {len(viol)} 列）")`
⇒ **每閘只印前 `12` 列**。

⇒ 我在 `W-G.9-116` 數的「`91` 列」是**顯示列數**、⛔ 非違規總數。
自 `V6_1` 之趟以截斷標記算真數（九表）：`14`／`14`／`72`／`72`／`11`／`65`／`65`／`7`／`3` ＝ **`323`**
（全部 `FAIL` 閘合計 `335`，顯示僅 `105`）。

🔒 **但 `-116` 之結論仍成立、且理由更強**：三趟凍存 rev 之該九表皆 `✅ PASS` 且 `FAIL = 0`，
而 `PASS` 之定義即 `not viol`（**整表零違規**）⇒ **含被截斷之隱藏列在內、全表逐列重現**。
⇒ 受影響者僅**量詞**（應寫「九表之全部違規」而非「`91/91`」），**`G-6` 行動清單⛔ 不受影響**。

🔑 **通則**：凡自 `run_verification`／`run_all` 之 stdout 取「違規列數」者，
**須先確認該閘是否截斷**（`grep -E "未顯示|本閘違規總計"`），⛔ 不得逕以顯示列數為母體。

---

## §七　🛑 待 KL 之三件事

| # | 事 | 為何非 CC 可代 |
|---|---|---|
| `1` | **重跑 `G` 值迭代並截圖那段 `🔍 K-9-4 ghost 判準診斷`** | CC 之重建**⛔ 未達 app 等價**（`-118 C-1`：重建得 `4` 宗、KL 得 `1` 宗）⇒ CC 所讀之三條件值⛔ 不可推及 KL 之態 |
| `2` | **九份 baseline 之重產** | 重產係 **KL 於 UI 之動作**；且須先定圖資（`§五`） |
| `3` | **圖資之取捨** | `verify/` 改讀 `V6_1`？九份 baseline 以哪一份重產？`data/V6.dxf` 是否保留為溯源基準？ |

---

## §八　取號現況（🔒 每格逐字帶其正規式·態 ＝ `blob@6ee8d91`）

| 號 | 母體 | 樣式（逐字） | 相異 | `max` |
|---|---|---|---|---|
| `VR` 裸 | `docs/驗證裁定登記表.md` | `VR-(\d+)` | `72` | `72` |
| `VR` 窄 | 〃 | `^## ` ＋ 反引號 ＋ `VR-(\d+)` ＋ 反引號 | `59` | `72` |
| 自誤 裸 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `自誤\s*(\d+)` | `135` | `142` |
| 自誤 窄 | 〃 | `^## 自誤 (\d+)` | `99` | `142` |
| `GB` 裸 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `GB-(\d+)` | `97` | `97` |
| `GB` 定義列 | 〃 | `^\|\s*\*\*` ＋ 選擇性反引號 ＋ `GB-(\d+)` ＋ 選擇性反引號 ＋ `\*\*` | `92` | `97` |
| `K-9` 裸 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `K-9-(\d+)(?!-)` | `18` | `18` |
| `K-9` 窄 | 〃 | `^###\s*(?:🔒\s*)?` ＋ 選擇性反引號 ＋ `K-9-(\d+)` ＋ 字界錨 | `16` | `18` |

⚠️ **`K-9` 二框同名不同量**（裸 `18` vs 窄 `16`）⇒ ⛔ 不得同表混框。

**四檔之三數**：`VR` 表 `288362` B／`3792` 列；自誤表 `262822` B／`3334` 列；
`GB` 表 `423133` B／`2720` 列；`K-6` `209357` B／`3063` 列。

---

## §九　🔴 積欠之登記（**下一批須一次入倉**）

發單側已列於 `-121 §四`：`自誤 143`〜`146`／`-116` 之 `F-3` `⛔` 誤植／`-113` 之 rev 分派瑕疵／
CC 工作區落後未上呈之常規／深度刀鋒之泛用阻塞項／`K-9-19`（ghost 非宗之域裁）／`K-9-20`（圖資權威版本）／
圖資分歧之泛用阻塞項。

🩸 **CC 側另有二項應併入**：
1. **`§六` 之截斷量詞**（`run_verification.py:1451`）——建議立為泛用阻塞項（同族 `GB-95`「綠時丟棄明細」，
   惟此度係**紅時亦只印前 `12` 列**）。
2. **`W-G.9-121 C-2` 之新事實**：`V6_1` 下 `R4` 多一格違規 ＝ **`【右】截角(㎡)`**
   （`V6` `6.25` 相符 → `V6_1` `6.28` 違規·二情境各 `+1`）。

---

## §十　環境與陷阱（本波實付學費者·下一窗直接可用）

1. 🔴 **`rc = 124` ＝ `timeout` 殺掉**，⛔ 非「跑完且無違規」。判別法：先 `grep -E "^RESULT:"`；無該列即未跑完。
2. 🔴 **跨 rev 比對前必驗該 rev 之 baseline 與 `HEAD` 是否位元組相同**
   （`退縮0m參數.csv` 於 `S1`／`S2` ＝ `569B` 而 `HEAD` ＝ `593B`）⇒ 不同即射程不同。
3. 🔴 **`⛔` 誤植已連三單**（`-118 C-1`／`-116 F-3`／`-114 §零閘二`）：條件句以 `⛔` 起首者，
   須自問「去掉 `⛔` 後與前句是否互斥」；互斥即誤植。CC 之處置 ＝ 取更正讀法＋**逐字回報備查**。
4. 🔴 **markdown 縮排歧義**（自誤 `145`）：施工單以 markdown 給程式碼時，`4` 空白究係圍欄抑或實際縮排
   ⛔ 無從分辨 ⇒ CC 須依「置於何處」自裁，並**逐字回報所裁**。`-120` 已改用 `[N]` 標註法。
5. 🔴 **轉義層吃 `\n`**：工具呼叫之 JSON 編碼會把 `\\n` 塌成 `\n`。凡需寫出字面反斜線者，
   一律改用 `chr(10)`／`chr(92)` 組字串，**⛔ 不寫反斜線**。本波踩了三次。
6. 🔴 **Windows 主控台 `cp950`**：CJK 直寫 stdout/stderr 會 `UnicodeEncodeError`
   ⇒ 一律 `export PYTHONIOENCODING=utf-8`，或探針改寫 utf-8 檔案。
7. 🔒 **`git rev-parse HEAD:app.py` 回的是 git blob `SHA-1`（`40` 碼）**，⛔ 非檔內容之 `SHA-256`（`64` 碼）。
   自誤 `146` 即因此製造一次假警報。**位數先數。**
8. 🔒 **harness 結構上到不了 `k94`**（`-118` 坐實三層分歧）：
   ①ghost 過濾（app ⛔ 不排／harness 排·`stepg_pipeline.py:317-318`）
   ②抵費地例外（app `:20186-20187` `st.warning` 續行／harness `:994-995` `raise`）
   ③`run_step_g` 內 **`11` 條 harness 專屬結構閘**（訊息字樣於 `app.py` 命中 `0`）。
   ⇒ 要重建 app 路徑須以**記憶體 exec** 中和該三層（⛔ 不寫任何檔）。
9. 🔒 **`_is_ghost_sliver` ⛔ 不隨 `g_row` 傳遞**（`g_row` 於 `:19340-19374` 新建·只帶列舉之鍵）
   ⇒ `K-9-4` 側讀不到該旗標，只剩 `暫編地號` 之 `_GHOST_` 前綴可辨。
10. 🔒 **記憶體代換之法**：改讀圖只需 `rv.V6DXF = <path>`（重綁模組屬性）⇒ ⛔ 無須改任何檔。
11. 🔒 **跑 `run_all`／`run_verification` 必於拋棄式 clone**（`VR-071 一`／`GB-94`）——
    三支 probe 以文字模式寫 log 且⛔ 無 `newline=""`，於 CRLF 平台重跑即改寫已追蹤產出。
12. ⚠️ **`run_verification` 全跑之耗時**：`1b290e46` `59s`／`6830642d` `505s`／`7ce98e6a` `1413s`／
    現況 `HEAD` 約 `20` 分 ⇒ **⛔ 不得設 `timeout`**，一律背景跑並掛終態守望。

---

## §十一　回報紀律（本波實行者）

- **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
- **數字錨引倉檔**，⛔ 禁憑記憶出艙；與倉態衝突以倉為準。
- **🔒 未 push 不得報收官**：前置硬條件 ＝ `git ls-remote origin refs/heads/wip/s1-endpart` 已含該 commit
  ＋ `is-ancestor rc = 0` ＋ `rev-list --left-right --count` ＝ `0　0`。
- **拋棄式 clone 跑畢整份刪除**，並於回報中逐字列其路徑與刪除確認。

---

## §十二　付印前機檢（本檔）

| # | 檢 | 實得 |
|---|---|---|
| `1` | 本文所引之倉內路徑是否存在 | `7/7` 存在·⛔ 不存在者 `0` |
| `2` | `CR` 計數 | `0` |
| `3` | 控制碼（排除 `LF`） | `0` |
| `4` | `W-G.9-122` 於入倉前之全倉命中 | `0` |
| `5` | `git status --porcelain` | 恰 `1` 列 `??` |
| `6` | `app.py`／`verify/` 對基座 | 皆零動 |

🔴 **本檔⛔ 不在 `TARGETS`** ⇒ 日後任一批對其**附加**（狀態 `M`）⇒ `wg9103` 必 `rc = 1`
（`GB-88 四` 之設計行為·⛔ 非 bug）；處置 ＝ 該批補登 `TARGETS`。本次係 `A`（新檔）⇒ 該閘現仍綠。
