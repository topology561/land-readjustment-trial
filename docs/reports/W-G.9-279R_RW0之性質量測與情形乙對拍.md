# `W-G.9-279R`　`R(W_0)` 之性質量測 ／ 情形乙與 `K-9-5-12（四）` 之對拍 ／ `自誤 381`·坑 `be`

> **態**｜開工 ＝ `2bf34586b1e8e1b080fc1d6d37591a2ac3c15a03`（`ls-remote` 實查）｜工項零後 ＝ `20737ddbe20f9e602523a50727f28bf2ad742c37`
> **單** ＝ `docs/orders/W-G.9-279_施工單_受詞改繫於R(W0)與情形乙之對拍.md`（工項零已原封入倉）
> **分級** ＝ **中**（量測·**⛔ 反事實**·**零生產碼**）｜**生產碼 `34` 檔一字未動**｜**本批⛔ 施任何替身**
> 🛑 **本報告⛔ 判任何側是否違反 `K-9-5-12`、⛔ 判（四）之射程、⛔ 提修法主張、⛔ 解除 `GB-169`。**

---

## `§零`　開工閘與工項零

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `2bf3458…` 或其後裔 | `2bf34586b1e8e1b080fc1d6d37591a2ac3c15a03` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 逐位相同 | ✅ |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須**非**祖先 | **非祖先** | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `bfb31aa5f0c320f7038bfbdb6cb98f3f6b8ac12ec7a0fac12bf6f7db153a5386` | **逐位相符**（受詞 `12526` B·全檔 `12604` B／`CR` `0`） | ✅ |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 不紅 | **停機款 `0`**；🟡 **提示 `4` 項**（皆 `P-4`·逐項處置見 `§零-1`） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `370`／`380`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `82`／`82` | **逐位相同**（坑判別力二造 造甲 `6 ≥ 1` ✅／造乙 `0`／`0` ✅） | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（⛔ pathspec·坑 `be`） | **`34`** 檔；**判別力[必不命中]**：`git ls-files -- 'verify/*.py'` 所多咬之**子層**檔 ＝ **`297`** 檔（首三例 `verify/archive/m_rescue.py`／`verify/archive/probe_ruling_K4_3_source.py`／`verify/fixtures/case_D2b15_tilegate_intersection.py`）⇒ **坑 `be` 之形當場坐實** | ✅ |

🔒 **閘 `4` 之判別力**：對**全檔**施同口徑 ⇒ 相異；受詞多 `1` byte ⇒ 相異 ⇒ 非恆綠。

### 零-1　🟡 **`4` 項提示之逐項處置**（⛔ 靜默略過）

四項**皆為 `P-4`**（方向性轉引而同段⛔ 具名座標系）：`:33`（裁之 `R(W_0) > 0`）／`:42`（`自誤 381` 之形）／
`:62`（`§二` 內載）／`:105`（`§四-2` 須出艙）。
🔒 **一律採納**：本報告凡引含箭頭之形（`MP→corner`／`→ return 0.0`）者，**同格具名其座標系**——
`MP→corner` ＝ **幾何量測之起訖**（自 `MP` 量至 `corner`）；`→ return 0.0` ＝ **控制流之去向**（⛔ 時序）；
`corner_pt + buf · d̂` ＝ **沿推進向 `d̂` 自 `corner_pt` 量起**（⛔ 「改前→改後」）。

**工項零**（commit `20737dd`·`§二-B` 搬移法）：
🔒 **受詞之自證（本批新加·⛔ 令讀者推定）**：所搬之單 ＝ `W-G.9-279_施工單_受詞改繫於R(W0)與情形乙之對拍.md`；
其號含 `W-G.9-279`？**`True`**；對照組[必為偽] 含 `W-G.9-278`？**`False`**。
所指之檔數 ＝ **`1`**（判別力：造乙 人造名 ⇒ `0`／造甲 `W-G.9-278` 之單 ⇒ `1`）；
`CR` ＝ **`0`**（`LF` `150`·bytes `12604`；判別力：真 CRLF 人造檔 ⇒ `CR = 2`）；
blob `sha1` ＝ `56bf4a9724bbb05043661ab1d54724b8ba84bc7d`；
**blob 內容 `sha256` ＝ 磁碟 ＝ 源 ＝ `fa2561b74b21bb79306e0e06c0fb468bfc8069cb7ad71a90b6a4459f5bb6525d`** ⇒ **逐位相同** 🟢。

---

## `§一`　工項 `1`：`R(W_0)` 之性質量測（**主檢**）

**器** ＝ `verify/probes/probe_WG9279_rw0.py`｜**落檔** ＝ `verify/out/WG9279R_rw0.log`｜**`rc = 0`**
🔒 **替身一律關閉·⛔ 反事實**（單 `§七-2`）；`R(·)` 一律以 `ns["rw_from_width"]` 取值·⛔ 器內另算。

### 一-1　自我驗證閘（框內省·坑 `bb`）

`run_step_g` 段所錄之鏈頭 `blk_label`：`0m` 態甲 `['R1','R2','R3','R4','R5','R6']`／態乙 `['R1','R2','R4']`；
`3.5m` 態甲 `['R1','R3','R4','R5','R6']`／態乙 `['R1','R4']` ⇒ **皆為 `BLKS` 之成員且非空** ⇒ **器非紅**。

### 一-2　主表（母體 ＝ `W-G.9-278` 之**同一** `24` 格定義·⛔ 另定）

🔒 **格數**：`6` 街廓 × `2` 側 ×（`2` 情境 × `2` 態）＝ **`48`** 列；其中**有值**（該側有鏈頭之記錄）＝ **`22`** 格，
其餘 **`26`** 格判【**⛔ 可得**】（該側於該態未產生鏈頭）。
🔒 **`R(W_0) > 0` 者 ＝ `6` 格**（逐格見下）；其餘 `16` 格之 `R(W_0)` **嚴格 `== 0`**。

| 情境 | 態 | 街廓 | 側 | `W_0`（未捨入·帶號） | **`R(W_0)`** | 情形 | `_fo` | 宗 |
|---|---|---|---|---|---|---|---|---|
| `0m` | 甲 | `R2` | left | `0.0000000002` | **`0.0000000033`** | **甲** | `False` | `628-41(1)` |
| `0m` | 甲 | `R4` | left | `4.4804447558` | **`40.7787574859`** | 乙 | `True` | `628(1)+` |
| `0m` | 乙 | `R2` | left | `0.0000000002` | **`0.0000000033`** | **甲** | `False` | `628-41(1)` |
| `0m` | 乙 | `R4` | right | `0.1775004071` | **`3.0885070835`** | **甲** | `False` | `628-1(1)` |
| `3.5m` | 甲 | `R4` | left | `7.7850157181` | **`59.9390848777`** | 乙 | `True` | `628(1)+` |
| `3.5m` | 乙 | `R4` | right | `0.1775004071` | **`3.0885070835`** | **甲** | `False` | `628-1(1)` |

🔒 **全 `48` 列之逐格表**（含 `is_corner`／`is_chain_head`）見落檔 `verify/out/WG9279R_rw0.log` 之「`§四-1` 主表」節。

### 一-3　判別力二造（單 `§四-1`）

| 造 | 受詞 | 格數 | 符者 | 判 |
|---|---|---|---|---|
| 甲[必為零] | `W_0 ≤ 0` ⇒ `R(W_0) == 0` | **`16`** | **`16`** | ✅ |
| 乙[必非零] | `W_0 > 0` ⇒ `R(W_0) > 0` | **`6`** | **`6`** | ✅ |

⇒ **二造異色 ⇒ 器非紅** ✅。

### 一-4　🔑 **情形甲諸側之 `R(W_0)`**（⛔ 判其是否違反）

**態甲 ∩ 情形甲**之有值格 ＝ **`11`**；其 `R(W_0) == 0` 者 ＝ **`10`**、`R(W_0) > 0` 者 ＝ **`1`**
（`0m` `R2 left`·`W_0` ＝ `+0.0000000002`·`R(W_0)` ＝ `0.0000000033`）。
⇒ 🛑 **「行為上等同『自 MP 起量』」於該母體<u>成立於 `10`／`11`</u>，⛔ 全部**——照實出艙即止。

---

## `§二`　工項 `2`：情形乙與 `K-9-5-12（四）` 之對拍

**正典逐字**（`K-9-5-12（四）`）：「無 WINNER 而街角為**強制抵費地**時，**第 2 宗**之起算為**該抵費地之遠側境界線**；**抵費地本身不計 G、不負擔**。」

### 二-1　情形甲／乙之判別（逐情境 × 逐態·源 ＝ `run_corner_pk` 之第三回傳 `_off` 之 `指配` 欄）

| 情境 | 態 | `_off` 列數 | 乙之格 |
|---|---|---|---|
| `0m` | 甲 | `1` | `R4 left`（`116.08 ㎡`） |
| `0m` | 乙 | **`0`** | （**無**） |
| `3.5m` | 甲 | `2` | `R4 left`（`226.01 ㎡`）／`R2 left`（`308.17 ㎡`） |
| `3.5m` | 乙 | `3` | `R2 left`（`308.17`）／`R3 right`（`307.79`）／`R5 left`（`299.73`） |

🔒 **與 `verify/out/got_抵費地_退縮*.csv` 之逐列對拍**（該檔**⛔ 在倉**·`.gitignore:30` 之 `verify/out/got_*`；
本批沿用 `W-G.9-278` 所產者·**追蹤檔零異動**）：`0m` **`1`** 列／`3.5m` **`2`** 列，
**與態甲之 `_off` 列數逐情境相同** ✅（該 CSV 係**生產態**之產物）。

### 二-2　🔑 **還原之自我驗證閘**（`CLAUDE.md`：探針還原內部幾何須以碼面自身之保證為閘·⛔ 不過不准據以下結論）

**碼面之保證** ＝ `_W_near = float(np.dot(_bp_w - _mp_w, _ahat))`（`solve_G_binary`），其 `_bp_w` ＝ `baseline_pt`、`_mp_w` ＝ `side_mid`。
⇒ 以 `ns["k956_W_from_mp"](baseline_pt, side_mid, allocation_dir, d_hat)` 重建之 `W` 須與碼面之 `W_0` **逐位相符**。
**實測：所驗之格 ＝ `22`｜逐位相符 ＝ `22`** ⇒ **閘過** ✅（逐格見落檔）。

### 二-3　對拍（`W(·)` 一律以 `ns["k956_W_from_mp"]` 取值·⛔ 器內另算）

🔒 **「遠側境界線」之構造逐字**（`app.py` `_corner_buffer_S` docstring）：
`side='left'` → 帶 `s ∈ [max(s_min, 0), buf]` ← **上界＝`buf`**；消費端 `left_cum_S = buf`。
⇒ 其上之點 ＝ `corner_pt + buf · d̂_單位`（**座標系 ＝ 沿推進向 `d̂` 自 `corner_pt` 量起**·⛔ 時序）。

| 情境 | 態 | 街廓 | 側 | `buf` | `range(㎡)` | `W(遠側)` | `W_0`（碼面） | `Δ` | **判** |
|---|---|---|---|---|---|---|---|---|---|
| `0m` | 甲 | `R4` | left | `4.4997522400` | `116.0800` | `4.4804447558` | `4.4804447558` | `+0.000e+00` | **逐位相符** |
| `3.5m` | 甲 | `R4` | left | `7.8185635189` | `226.0100` | `7.7850157181` | `7.7850157181` | `+0.000e+00` | **逐位相符** |
| `3.5m` | 甲／乙 | `R2` | left | `8.6960798938` | `308.1700` | 【**⛔ 可得**】 | — | — | 【**⛔ 可得**】（該側無鏈頭之記錄） |

🔒 **併錄**：該側之 `left_cum_S` 與 `buf` **逐位相同**（`4.49975224003872`／`7.8185635189090075`）。
🔒 **判別力二造**：甲[必不符] 同片之**近側**境界線（`corner_pt`·`s ≈ 0`）⇒ `W` ＝ `-0.0000000002` ⇒ **⛔ 相符** ✅；
乙[必相符] `W_0` 與其自身 ⇒ **逐位相符** ✅ ⇒ **器非紅**。
🛑 **⛔ 判其合致之法律效果、⛔ 判（四）是否及於本案、⛔ 提修法主張**——出艙即止。

---

## `§三`　工項 `3`：態乙 `R4 right` 之 `W_0 = 0.1775004071` 之歸屬

**態乙全母體之有值格 ＝ `9`**（`0m` `5`／`3.5m` `4`）；其 `R(W_0) > 0` 者 ＝ **`3`**
（`0m` `R2 left`／`0m` `R4 right`／`3.5m` `R4 right`）。

🔑 **`R4 right` 之四項**（`0m` 與 `3.5m` **逐項相同**）

| 項 | 值 |
|---|---|
| 情形 | **甲**（該態該情境之 `_off` ⛔ 含 `R4 right`） |
| `_fo_right` | **`False`** |
| **`R(W_0)`** | **`3.0885070835`**（`W_0` ＝ `0.1775004071`·宗 `628-1(1)`） |
| `is_chain_head` | **`True`** |

🔒 **判別力**：態乙各格之 `R(W_0)` **相異值數 ＝ `3`**（若全同值 ⇒ 先判器紅）⇒ ✅ 非全同。
🛑 **⛔ 判其為違反、⛔ 提修法主張**——出艙即止。

---

## `§四`　工項 `4`：`自誤 381` ／ `GB-169` 收窄 ／ 坑 `be` ／ `ALPHA_LABELS`→`be`

### 四-1　`自誤 381`

**落點** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 檔末追加（嚴格前綴 `True`；判別力 `old+b'X'` ⇒ `False`）｜
`793242` → `798921`（**`+5679` B**）｜`CR` ＝ `0`。
**受詞逐字塊**由 `sed -n '42,50p'` 之輸出**逕行落檔**：列數 **`9`**｜位元組 **`1624`**｜
`sha256` **`44b0f0e36b1c38d26be6dcb0c1658668caa050526d2dd622af2006deea188884`**；覆核逐位相同 ✅。

**取號現查**（三軸｜`(1)` `blob@20737ddbe20f9e602523a50727f28bf2ad742c37`｜
`(2)` 取最大號 ＝ 登記表**單檔**（`793242` B／`9360` 列）·查占用 ＝ 全倉追蹤檔 **`2182`** 檔（讀不到 `0`）｜
`(3)` **檔框 ＋ 列框**）：命中列 `383`／相異 `370`／**MAX `380`** ⇒ **次號 `381`**；
查占用（**B ⋀ C**·`GB-158`）：受詢 `1`／`3`·`1`／`1`·`1`／`3`；甲 `380` `4`／`10`·`3`／`3`·`4`／`10`；
甲2 `379` `5`／`17`·`3`／`3`·`5`／`16`；乙 人造哨兵（**執行期組出**·坑 `bd` 之攔法）`0`／`0` ⇒ **器非紅**。
**逐筆歸類**：受詢之命中**全數集中於本單自身** ⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號。

🔴 **本則之事實欄與實測相異二處（已於該節照實併記·⛔ 判孰誤、⛔ 改單一字）**
本則載「`W_0 > 0` 者 **`3` 格恰全落情形乙**」。CC 於**全母體 `24` 格**實測 ＝ **`6` 格**
（**情形甲 `4`**／**情形乙 `2`**）。其別有二：`(1)` 本則之 `3` 格係 `W-G.9-278` 之**態甲 ∩ 情形甲**母體所餘者；
`(2)` 本則所列第三格 `3.5m R2 left`（`6.9804804474`）於本批**⛔ 在鏈頭之記錄內** ⇒ 其 `R(W_0)`【**⛔ 可得**】。

### 四-2　`GB-169` 受詞之收窄（末端追加·**⛔ 鑄號·⛔ 解除**）

**落點** ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末追加｜`872208` → `876204`（**`+3996` B**）｜`CR` ＝ `0`。
節題由 `sed -n '59,59p'` 之輸出**逕行落檔**（列數 `1`／位元組 `199`／
`sha256` `4c7f40129f341f942161a88cfa747d4fb79736d9b145c56b1aa14095fdf56220`）。
其內載四事，皆當場復現；**收窄之裁逐字** ＝ 「**`GB-169` 之射程收窄為 `R(W_0) > 0` 之側**」。

🔴 **單 `§二-4` 之事實欄與實測相異（已照實併記）**：單載「生產態 ⇒ **僅情形乙之 `3` 格**」；
實測生產態（態甲）之 `R(W_0) > 0` ＝ **`3` 格**，惟其歸屬為 **情形乙 `2`**（`0m`／`3.5m` `R4 left`）
＋ **情形甲 `1`**（`0m` `R2 left`·`R(W_0)` ＝ `3.3e-9`）；且第三格 `3.5m R2 left`【**⛔ 可得**】。

### 四-3　坑 `be` ＋ `ALPHA_LABELS`→`be`

🔒 **其定義處 ＝ 本單 `§三` 之坑表**（`docs/orders/W-G.9-279_…md:71`–`:73`），表頭列逐字 ＝ `| 標籤 | 形／徵候／攔法 |`
⇒ 同時含 `徵候` ⋀ `攔法` ⇒ 合於 `D-T` 之形。
**取號現查**（全倉·態 `20737dd`）：坑 `be` ＝ **`1`** 檔／**`7`** 列（**全數為本單自身**）；
對照組 甲 坑 `bd` ＝ `5`／`18`／甲2 坑 `bc` ＝ `4`／`14`（皆須 `>0`）／
**乙 ＝ 一未鑄之標籤（字面<u>執行期組出</u>·⛔ 出艙·坑 `bd` 之攔法）＝ `0`／`0`** ⇒ **器非紅**。

**`ALPHA_LABELS` 之擴**（受詞 ＝ `verify/probes/probe_WG9269_pit_index.py`·**⛔ 生產碼 `34` 檔之一**）：
末元 `"bd",` ＋ 新列 `"be"])`；二處框字串 `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`be\``。
🔒 **判別力（先實測後填·以 `compile` + `exec` 單一 `Assign` 取語法真值·⛔ `ast.literal_eval`）**：
**成員數 `56` → `57`（`+1` ✅）**｜**尾元 `'be'` ✅**｜前一元 `'bd'`｜**重複元 `0`** ✅；
新框字串命中 `2`／舊形（`bd`）殘留 `0` ✅。**總數 ＝ `NUM 26` ＋ `ALPHA 57` ＝ `83`** ⇒ 與單 `§五-5` 之期值 **`83`／`83`** 一致。

---

## `§五`　本批之**二則量測器紅之自捕** ＋ **自解清單**

### 五-1　🩸 二則自捕（皆於出艙**前**攔下）

| # | 形 | 徵候 | 攔法 ／ 處置 |
|---|---|---|---|
| `1` | **閘器之字串替換未生效，遂量到<u>他批之單</u>** | 輸出之標題仍作 `W-G.9-277`、所量之檔名仍作 `…-278…`，**而其餘各數看起來一切正常**（`SELF_SHA256` 相符、preflight `rc=0`）——**徵候只在標題之號** | 🔒 **受詞之自證**：閘器**印出所量之檔名**並與本批號對拍（含[必為偽]之對照：其號含前一批號？須 `False`）。⛔ 令讀者自行推定 |
| `2` | **框內省取錯<u>層</u>** ⇒ 還原之幾何錯 | `§二` 之還原自我驗證閘 **`0`／`22`** 逐位相符（全紅） | 🔒 二根因：`(a)` `_side_mid_left` 於 `_corner_buffer_S` 之呼叫時點**尚未賦值**（在同一街廓迴圈之**後段**）⇒ 取到**前一街廓**之值；`(b)` `app.py:10194`–`:10196` 逐字「**街角第 1 宗之 `allocation_dir` 已換為 `_first_corner_alloc_dir(side_mid)`**」⇒ 外層之 `allocation_dir_block` 於**鏈頭**恰為錯者。⇒ **一律改取 `solve_G_binary` 之<u>參數本身</u>**（`baseline_pt`／`side_mid`／`allocation_dir`／`d_hat`）⇒ 閘轉 **`22`／`22`** |

🔑 **`2` 之教訓**：若無該還原閘，其所印之 `W(遠側)` ＝ `73.8689182960`（vs `W_0` `4.4804447558`）
**是一張漂亮而錯的表**——且其「⛔ 相符」之結論**看起來比真值更像在說明什麼**。
⇒ 正是 `CLAUDE.md`「探針還原內部幾何時，須以碼面自身之保證當自我驗證閘」之受詞。

### 五-2　自解清單（`作業常規之追加五 §一` 款 `5`）

| # | 疑義逐字 | 二個以上之讀法 | **所取者** | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§四-2` 項 `1`「該側強制抵費地之列逐字（`got_抵費地_退縮*.csv`·**須先以 `run_verification.py` 產之**）」——該檔已於 `W-G.9-278` 產出且**內容未變** | `(a)` 再跑一次 `run_verification.py`；`(b)` **沿用既有之檔**並與**同一產生者**（`run_corner_pk` 之 `_off`）逐列對拍 | **`(b)`** | 二者列數逐情境相同（`1`／`1`·`2`／`2`）；`git status --porcelain` 之非 `??` 列 ＝ `0`（追蹤檔零異動·單 `§七-4` 所令） | `(a)` 之受詞（生產態之 `_off`）本批已於 `drive` 內**當場重得**並逐列出艙 ⇒ 再跑僅重複同一產生者、⛔ 增證據力 |
| `2` | 單 `§四-2` 項 `2`「以 `ns["k956_W_from_mp"]` 計『MP → 該抵費地之**遠側**境界線』之垂距」——其**點**之構造單內未定 | `(a)` 取 `corner_pt`（`s≈0`）；`(b)` 取 `corner_pt + buf · d̂`（`s = buf`） | **`(b)`** | `_corner_buffer_S` docstring 逐字「`side='left'` → 帶 `s ∈ [max(s_min, 0), buf]` ← **上界＝`buf`**」；且實測 `left_cum_S` 與 `buf` **逐位相同** | `(a)` 係該片之**近側**——單自身將其列為**判別力甲[必不符]**之對照 ⇒ ⛔ 得同時為受詞 |
| `3` | 單 `§四-3`「態乙之全母體」——`R4 right` 於 `0m` 與 `3.5m` **各一格** | `(a)` 只報 `0m`；`(b)` **二格皆報** | **`(b)`** | 二格之四項**逐項相同**（情形 甲／`_fo_right` `False`／`R(W_0)` `3.0885070835`／`is_chain_head` `True`） | `(a)` 將使「唯一之疑似點」之計數失真 |

🔒 **三項皆滿足自解五要件**：零土地後果（純量測·**⛔ 施任何替身**）／零生產碼（`34` 檔一字未動）／
可機驗（證據欄·皆附必紅必綠二組）／可逆（新建檔 ＋ append-only ＋ 一處**非生產碼**探針之就地更正）／出艙具名（本表）。
🔒 **黑名單之逐項自檢**：⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字（含註解）；
二讀法之後果**⛔ 在土地上相異**；**⛔ 新訂準據**；⛔ 廢止或放寬任何既有之閘。

## `§六`　🔴 **出艙即止之事**（⛔ 判·單 `§七-5`／`-6`）

1. `R(W_0) > 0` 之格 ＝ **`6`**／`22`（全母體 `24` 格定義·有值 `22`）：**情形甲 `4`**／**情形乙 `2`**。
2. **態甲 ∩ 情形甲**之 `11` 格中，`R(W_0) == 0` 者 ＝ **`10`**、`> 0` 者 ＝ **`1`**（`0m R2 left`·`3.3e-9`）。
3. 情形乙之二可量格，其 `W(遠側)` 與 `W_0` **逐位相符**（`Δ = +0.000e+00`）；第三格【**⛔ 可得**】。
4. **態乙 `R4 right`**（`0m`／`3.5m` 皆然）＝ **情形甲**·`_fo_right` `False`·`R(W_0)` `3.0885070835`·`is_chain_head` `True`。
5. `_off` 之乙格**逐態相異**：`0m` 態乙 ＝ **`0` 列**（⛔ 任何強制抵費地）；`3.5m` 態乙 ＝ `3` 列（含 `R3 right`／`R5 left`·**態甲所無**）。

🛑 **上五事皆⛔ 得讀為「某側違反 `K-9-5-12`」或「（四）及於本案」**——該判**⛔ 在本批之授權內**。

---

*本報告受詞 ＝ `W-G.9-279`。⛔ 呈 KL。*
