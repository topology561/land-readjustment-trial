# `W-G.9-307R2`　補令一（**舊窗收工批**）執行報告

> **主線**｜開工 ＝ `e2d26b1ac6c15391d6dc4001811184754d6f6d52`（`ls-remote` 實查·全 `40` 碼）。
> **補令** ＝ `docs/orders/W-G.9-307_補令一_舊窗收工批.md`（已原封入倉）。
> **分級** ＝ **輕**（交接註一檔·**⛔ 長跑**·**零生產碼**·⛔ 開分支·**⛔ 鑄任何號**）。
> 🔒 凡出艙之數，同格載其**框、母體與產生指令**。

---

## `§零`　開工閘（**⛔ 略**·**⛔ 驅動任何長跑**）

| # | 受詞（產生指令逐字） | 實得 | 期／判準 | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `e2d26b1ac6c15391d6dc4001811184754d6f6d52`；`rc = 0` | `e2d26b1a…` **或其後裔** | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1`；`rc = 0` | 同左 | ✅ |
| `3`存 | `git cat-file -t 05a11bd665f6c2790ff821c9e3a1606651e345d4` | `commit`；`rc = 0` | 存在 | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd6… HEAD` | `rc = 1` | **非**祖先 | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本補令>` | 🔴 機械停機款 **`0`** 項；🟡 **`0`** 項；`P-5` ✅ 相符（受詞 **`7610`** B·`be4fc16456b42a2d…`）；`P-3` **不紅**（`app.py` 之 `def main` 區間 ＝ `14975`-`24579`）；`rc = 0` | `P-5` 相符·`P-3` 不紅 | ✅ |
| `4'` | 受詞自證（以器所印之檔名列） | `【order pre-flight】W-G.9-307_補令一_舊窗收工批.md（7688 B／103 列）` ⇒ 含 `補令一` ＝ `True`[必真]／含 `W-G.9-308` ＝ `False`[必偽] | 同左 | ✅ |
| `5` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`406`**／**`416`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `29`／`30`（缺 `[]`）；`rc = 0` | 同左 | ✅ |
| `6` | `git status --porcelain` ／ `git rev-list --left-right --count origin/wip/s1-endpart...HEAD` | **空** ／ **`0 0`** | 空 ／ `0 0` | ✅ |

🔒 **`P-5` 之判別力二造**（`§零` 閘 `4` 之佐證）：
[必綠] 原檔相符 ＝ `True`；[必紅] 改一位元組之副本相符 ＝ `False`（實算 `67b99401f1ad3046…`·**該副本⛔ 入倉**·落於 scratchpad）。

### `§零-1`　🔴 **本補令與常設 `常規` 之互斥**（`常規二`·**取保守項 ＋ 逐字回報**·⛔ 上呈 KL）

| 受詞 | 逐字 |
|---|---|
| **常設 `常規`**（`CLAUDE.md`「作業常規之追加四：**施工單之入倉**」款 `一`） | 「凡發單側出予 CC 之**施工單**（含交接文、含 `R` 版與作廢本），CC 收單後**第一工項**即**原封入倉** `docs/orders/`」；款 `二`：「🔴 **該工項不過即停**，**⛔ 續辦其餘工項**」 |
| **本補令 `§三` 閘 `1`** | 「`git show --numstat <本批各 commit>` 逐檔具名 ｜ 刪除欄**全 `0`**；**檔數 ＝ `1`（交接註）**」 |

🔒 **互斥之點**：依常設 `常規`，本補令自身須入倉 ⇒ 本批之異動檔**至少 `2`**（補令 ＋ 交接註），
而 `§三` 閘 `1` 載「檔數 ＝ `1`（交接註）」。
🔒 **CC 之處置（取保守項）**：**依常設 `常規` 將本補令原封入倉**（其款 `二` 為**停機款**·位階較高），
並使**二者各成一 `commit`** ⇒ **逐 `commit` 之檔數皆 ＝ `1`**（`§三-1` 實測）。
🛑 **⛔ 改補令一字、⛔ 自裁孰為正**；**其裁屬發單側**。
🔒 **併記**：本補令 `§四` 之七款**⛔ 禁**「本補令自身入倉」；且 `§二` 之交接註**仍為單獨 `commit`**（補令明文）。

---

## `§一`　態之判（🛑 **自倉現查·⛔ 憑記憶**）

### 一-1　框甲之**全量輸出**

**產生指令** ＝ `git log --format='%H %s' e2d26b1ac6c15391d6dc4001811184754d6f6d52..HEAD`

| 時點 | 全量輸出 |
|---|---|
| **收單時**（`HEAD` ＝ `e2d26b1ac6c15391d6dc4001811184754d6f6d52`·`left-right` ＝ `0 0`） | **（空）** |
| **現時**（本補令入倉後） | **`1`** 列 ＝ `1d519572765e0acd774123534325745876bddabb W-G.9-307 補令一：本補令原封入倉（docs/orders/·⛔ 改一字·作業常規追加四 款一）⛔ 零生產碼` |

### 一-2　框乙之**全量輸出**

**產生指令** ＝ `git ls-tree -r --name-only -z HEAD docs/orders/` ⋂ 檔名前綴 `W-G.9-308`（**取 bytes**）
⇒ **命中 `0`（空）**
**併查**（補令 `§一` 末段）：檔名含 `K-9-30依據之更正` 之 `W-G.9-308`（發單側已作廢之版）⇒ **命中 `0`（無）**
🔒 **對照組·必非零**：同一母體內檔名含 `W-G.9-307` 者 ＝ **`2`**（本單 ＋ 本補令）⇒ 量測器非紅。

### 一-3　🔒 **態之判**（照實·**⛔ 判**）

- 依**收單時**之框甲（空）＋ 框乙（空）⇒ **`§一` 之態甲之條件於收單時成立** ⇒ 逕辦 `§二`。
- 依**現時**之框甲（`1` 列）⇒ 其唯一列係**本補令自身之入倉 `commit`**（`§零-1` 之取保守項所生），
  **⛔ `W-G.9-308` 之工項**；框乙仍空。
- 🔒 **`W-G.9-308` 之 `commit` 與檔案皆 `0`** ⇒ 態乙之**實質受詞**（「已辦之 `W-G.9-308` 工項」）**不存在**
  ⇒ 無「照實留倉之 `W-G.9-308` `commit`」可具名。
- 🛑 **二時點之輸出併呈**，⛔ 以其一代其二。

---

## `§二`　交接註之落點與逐位

**落點** ＝ `docs/reports/W-G.9-307_交接註_舊窗收工.md`（**新檔入倉·單獨 `commit`**·`open(p,'wb')`）

| 受詞 | 實得 |
|---|---|
| blob `sha1` | `2a85fbee8fd615d5f827da7b8e74447e9c4ca5dd` |
| blob 內容 `sha256` | `084bd233330a8a6898e4086ec1e470c3013aa725d9d94efb77973cddfa5d6de7` |
| bytes ／ 列數 | **`17083`** ／ **`264`** |
| `CR` | **`0`** |
| `blob == disk` | `True` |
| `numstat` | `264` 增／**`0`** 刪；該 `commit` 之**檔數 ＝ `1`** |

🔒 **補令 `§二` 之二末款之機驗**
- 「交接註內**凡以 `#` 起首之列⛔ 含『坑』字**」⇒ 實測 **`0`** 列
  （[對照組·必非零] 以 `#` 起首之列總數 ＝ **`19`**；全檔含「坑」字之列 ＝ **`2`**·皆⛔ 以 `#` 起首）⇒ ✅
- 「交接註內之數**⛔ 冠『已交付』『已核可』之詞**」⇒ 含「已交付」之列 ＝ **`0`**；含「已核可」之列 ＝ **`0`** ⇒ ✅

🔒 **交接註所載之十五項**：`1` 主線 ref／`2` `app.py` blob／`3` `p3a` 祖先關係／`4` 四簿（含缺號）／
`5` 生產碼母體 `34` 檔（正面列舉）＋ 子層 `*.py` `327`／`6` `baselines`／`7` `refs/heads/verify/*`／
`8` 態與其框之全量輸出／`9` `W-G.9-307` 之全部 `commit`（`10` 列）／`10` 環境事實（`8` 項）／
`11` 未入倉之物（`20` 條目）／`12` 倉外之寫入（`4` 項）／`13` 自捕（`13-甲` `3` 則·`13-乙` `4` 則·`13-丙` `0` 則）／
`14` 已辦與未辦之工項／`15` 交接予新窗之未決項。

### `§二-1`　交接註內**須逐字之產生指令**（節錄·其全文見該註）

| 交接註之項 | 產生指令（逐字） | 其於該註所載之值 |
|---|---|---|
| `10-b` | `git config --system --get core.autocrlf`／`--global`／`--local`（**三層分列**） | `--system` `rc=0` `'true'`／`--global` `rc=1` `''`（**未設**）／`--local` `rc=0` `'false'` ⇒ **有效值 `false`**。🔒 `autocrlf` 之三層須分列，⛔ 只報有效值 |
| `10-d` | —（構造上之事實·⛔ 指令） | `ast` 之 `col_offset`／`end_col_offset` 係 **UTF-8 位元組**偏移（⛔ 字元）⇒ 自建之原始碼切片須**先 `encode` 再切**；其自我驗證閘（vs `ast.get_source_segment`）⇒ 相同 `837`／相異 `0` |
| `11` | `git status --porcelain --ignored -z` | 條目數 ＝ **`20`**（`verify/out/got_*.csv` `18` ＋ 二 `__pycache__/`）。🔒 `--ignored` 係**必要之旗標**——⛔ 加之則 `.gitignore` 所忽略之 `got_*` 族**⛔ 現形**；🛑 **⛔ 刪、⛔ 入倉** |

🔒 **`SELF_SHA256` 之併記**：本補令之 `SELF_SHA256`（`P-5` 之受詞 ＝ 自檔首至**最末**一個 `SELF_SHA256` 列之前一個 `\n`（含））
＝ `be4fc16456b42a2d7239f0c022a9c1ec64f715834fc511592099f59d547f075d`（受詞 `7610` B）——與補令末列所載**逐位相符**。

---

## `§三`　收工閘（**交接註入倉後量**）

| # | 受詞（產生指令） | 實得 | 期／判準 | 判 |
|---|---|---|---|---|
| `1` | `git show --numstat -z <本批各 commit>` 逐檔具名 | **`2`** 個 `commit`·見 `§三-1` | 刪除欄**全 `0`**；檔數 ＝ `1`（交接註） | ✅（逐 `commit` 之檔數皆 `1`·見 `§零-1` 之互斥回報） |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 二區間逐一 **`0`** 行 ⇒ 合計 **`0`** | 本批區間 `0` 行 | ✅ |
| `2`造 | 對照組[必非零] `17fd981^..17fd981` | **`2`** 行 ＝ `['app.py', 'verify/stepg_pipeline.py']` | `≥ 1` ⇒ 器非紅 | ✅ |
| `3` | 交接註之 `CR`（倉內 blob·**取 bytes**） | **`0`** | `0` | ✅ |
| `3`造 | 判別力[必非零] `data/V6.dxf` | **`12308`** | `> 0` | ✅ |
| `4` | 四簿期末（器之輸出） | 自誤 `406`／`416`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `29`／`30`（缺 `[]`） | 與 `§零` 閘 `5` **逐字相同** | ✅ |
| `5` | `python verify/probes/probe_WG9269_pit_index.py a57c64e…`（`rev` **釘死**·**全量落檔**） | 數字系 **`26`** ＋ 字母系 **`68`** ⇒ **`94`／`94`**·末 **`bp`**；母體 ＝ `a57c64e` 之追蹤 `.md` **`827`** 檔；`rc = 0`；器自印判別力二造 ✅（造甲 定義處 `6` ≥ `1`／造乙 `0`／`0`）⇒ **器非紅** | `94`／`94`·末 `bp` | ✅ |
| `5`母體 | 母體 `.md` 檔數之增量 ＝ 本批新增之 `.md` 數 | 期初 `825` → `a57c64e` **`827`** ⇒ 增量 **`2`**；`git diff --name-only --diff-filter=A -z` 之 `.md` 數 ＝ **`2`**（補令 ＋ 交接註） | 二者相符 | ✅ |
| `6` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 **`20`** 支；`W-G.9-299-gb170` 命中 **`0`**；`W-G.9-269-p3a` 命中 **`1`** | 同左 | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉**（補令 `§三` 項 `7` 明文） | —— | 🔒 |

### `§三-1`　閘 `1` 之逐檔具名

| `commit`（全 `40` 碼） | 檔 | ＋ | － | 該 `commit` 之檔數 |
|---|---|---|---|---|
| `1d519572765e0acd774123534325745876bddabb` | `docs/orders/W-G.9-307_補令一_舊窗收工批.md` | `103` | **`0`** | **`1`** |
| `a57c64e871f5de118a38c1c552a1ad99a4ea7ee9` | `docs/reports/W-G.9-307_交接註_舊窗收工.md` | `264` | **`0`** | **`1`** |

🔒 **刪除欄合計 ＝ `0`**（`git show --numstat` 之第二欄·即 `deletions`；二 `commit` 之 `deletions` 皆 `0`）。
🛑 **本批之異動檔合計 ＝ `2`**（⛔ `1`）——其由已於 `§零-1` 逐字回報（`常規二` 之互斥·取保守項）。

---

## `§四`　🛑 `⛔ 為之事` 之逐項自檢（補令 `§四` 逐項）

| # | 受詞（補令 `§四` 逐字） | 自檢 |
|---|---|---|
| `1` | **⛔ 辦 `W-G.9-308` 之任何版本、任何工項**；**⛔ 驅動任何長跑**（`run_verification`、任何管線驅動） | ✅ **未為**——`W-G.9-308` 之 `commit` `0`／檔 `0`（`§一-2`）；本批**⛔ 驅動 `run_verification` 或任何管線**；所跑者僅 `probe_order_preflight`／`probe_WG9267_issuer_measurers`／`probe_WG9269_pit_index`（**補令 `§零`／`§三` 明令之閘**·皆為登記掃描器·⛔ 管線驅動） |
| `2` | ⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支（含 `verify/W-G.9-299-gb170`）；⛔ 動用 KL 之放行 | ✅ **未為**——生產碼判法二區間皆 `0` 行（閘 `2`）；`refs/heads/verify/*` 仍 `20` 支·`gb170` 命中 `0`（閘 `6`） |
| `3` | ⛔ 鑄任何號（自誤／`GB`／`VR`／`K-9`／坑）；`ALPHA_LABELS` ⛔ 動一字 | ✅ **未為**——四簿期末與 `§零` 閘 `5` **逐字相同**（閘 `4`）；坑 `94`／`94`·末 `bp` **未動**（閘 `5`）；`ALPHA_LABELS` 所在之 `probe_WG9269_pit_index.py` **⛔ 在本批之異動檔內**（`§三-1` 僅二檔） |
| `4` | ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或任一物證落檔；⛔ `WV_BAKE` | ✅ **未為**——`p3a` `is-ancestor` `rc = 1`（**非**祖先·閘 `3`）；`baselines` 與物證落檔**⛔ 在本批之異動檔內**；`WV_BAKE` 未設（本批⛔ 跑任何需其之器） |
| `5` | ⛔ 追改任何已入倉文件一字 | ✅ **未為**——二 `commit` 之刪除欄**全 `0`**；二檔**皆新增檔**（`--diff-filter=A` ＝ `2`） |
| `6` | ⛔ 判任何域事、⛔ 提修法主張、⛔ 呈 KL | ✅ **未為**——交接註**⛔ 載任何判**（其 `15` 之未決項係**照實轉錄** `W-G.9-307R` 已具名者）；本報告⛔ 提任何修法主張 |
| `7` | ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述） | ✅ **未為**——閘 `7`（`push` 後之 `ls-remote`）**⛔ 入倉**、僅出艙於回報 |

---

## `§五`　🩸 本批之自捕

**`0` 則**　🛑 **⛔ 讀為「本批無誤」**。
🔒 **併記**：本批所出艙之唯一異常係 `§零-1` 之**補令與常設 `常規` 之互斥**——
其**⛔ 受單側之誤**，故⛔ 列為自捕；依 `常規二` 已**取保守項 ＋ 逐字回報**，**其裁屬發單側**。

---

## `§六`　逐 `commit`（**全 `40` 碼**）

| # | `commit` | 主旨 |
|---|---|---|
| `1` | `1d519572765e0acd774123534325745876bddabb` | `W-G.9-307` 補令一：本補令原封入倉（`作業常規追加四` 款一） |
| `2` | `a57c64e871f5de118a38c1c552a1ad99a4ea7ee9` | `W-G.9-307` 補令一 `§二`：交接註入倉（單獨 `commit`·`CR` `0`） |

🔒 **本報告自身另成一 `commit`**，其全 `40` 碼**出艙於回報**
——⛔ 於本報告內宣稱一件尚未發生之事（補令 `§四` 項 `7`）。

🛑 **辦畢 ⇒ 停 ⇒ 本窗封存。**
