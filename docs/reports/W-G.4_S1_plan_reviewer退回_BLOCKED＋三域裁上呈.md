# W-G.4 S1 plan：reviewer 退回 **BLOCKED** ＋ 三域裁題上呈

- 日期：2026-07-17；plan＝`docs/specs/W-G.4_S1_plan.md`（`a74fc28`+）；**未動工**
- reviewer＝`redistribution-reviewer`（獨立·**以真 harness `app_harvest` 真函式＋V6.dxf 實跑量測**·不採信 plan/偵察之任何數字）
- **總判：BLOCKED**——**plan 之 bisect 方程式量錯了它要控制的量**；右側 forced 塊（R3）修不好，且閘② 會**自我確認地**宣告「由破轉綠」。

---

## 0. 一句話（本次退回之全部價值）

**我的通式完全複現了規格 §N1 自己的錨（R2 `buffer_S′=8.7157` → 實切 `309.0499`），而它仍然是錯的。**
因為 **R2 是左側塊**——錨恰好落在通式正確的那一半。**右側塊（R3）用同一式解出 388.92，對 range 308.93 超額 79.99㎡（≈5,763,723 元）**，
而閘② 若照 plan 落點量測，**會宣告 N0-10「由破轉綠」**。

> **新失敗模式：錨驗證了錯的通式。**
> 錨是**單點**，通式是**全域**。單點命中**不證**全域正確——尤其當該點恰好落在通式退化為正確的子域（此處：左側）。
> **判準**：錨之**維度**（本例：side）若未被通式參數化，該錨對此維度**零舉證力**。

---

## 1. 🔴 BLOCKED-1（致命）：bisect 目標函式對**右側** forced 塊量錯端點

**plan 之式**（§2 helper 簽章·**無 `side` 參數**）：
```
_corner_buffer_S(block_poly, d_hat, corner_pt, allocation_dir, range_area, ...)
  bisect: _block_strip(block_poly, d_hat, corner_pt, S).area == range
```

**但右側推進根本不從 `corner_pt` 起算**（`app.py:15478-15479`／`stepg_pipeline.py:573-576`）：
```python
end_pt = corner_pt + actual_max_proj * d_hat
d_hat_rev = -d_hat
baseline_pt = end_pt + right_cum_S * d_hat_rev
```

**reviewer 實跑三塊（全走 `ns["_block_strip"]` 真函式）**

| 塊 | side | range | plan 式解出 `buffer_S′` | 該 S′ 下之**真實池片** | 殘差 |
|---|---|---|---|---|---|
| R5 | left | 300.52 | 8.6774 | 300.52 | **0.00** ✅ |
| R2 | left | 309.05 | **8.7157** | 309.05 | **0.00** ✅ |
| **R3** | **right** | 308.93 | 7.0582 | **388.92** | **+79.99** ❌ |

→ **修完 R3 仍違 N0-10 達 79.99㎡ ≈ 5,763,723 元**；**且 plan §2.1 之錨 R2=8.7157 完全複現**（f=309.0499）——**危險正在於此**。

**且側別修正仍不夠**——R3 池帶跑到 `s_max` 而非 `amp`：
```
s_min=-0.0000   s_max=96.8032   actual_max_proj=93.3461   → s_max − amp = 3.4571
strip(end_pt, -d, 7.0791) = 311.6124
+ p2 楔形                 =  78.2363
= 389.8488  ≈ baseline 389.85 ✅（逐位複現）
```
**`actual_max_proj` 係<u>正交</u>投影 `dot(v−corner_pt, d_hat)`；`s_max` 係 `_strip_axis` 之<u>斜交</u>切線座標——R3 差 3.4571m。**
→ **推進域與池域用不同座標系＝第四源同族之<u>既存</u>錯配**（非 S1 引入）。

**正解式（reviewer 實測皆命中 ±0.01）**
- left → `area(block ∩ s∈[s_min, buf]) == range`
- right → `area(block ∩ s∈[amp − buf, s_max]) == range`
- → **R5 8.6774／R2 8.7157／R3 5.2719**

## 2. 🔴 BLOCKED-2：「S2 擴為 5 時無縫承接」為**偽**

S2 所加之 R1右(226.88)、R6右(299.13)——**皆右側**。plan 式實測：

| 塊 | plan 式 S′ | 真實帶 | range | 誤差 | 正解 S′ |
|---|---|---|---|---|---|
| R1右 | 7.0421 | **132.04** | 226.88 | **−94.84** | 9.9050 |
| R6右 | 6.3151 | **116.92** | 299.13 | **−182.21** | 10.5087 |

**且 `verify/wf_f1.py:213-216` 只有 `left_buffer_S`、無 right 分支** → **S2 之 R1右 於 wf_f1 完全無處理**。

## 3. 🔴 BLOCKED-3：閘② 與 `f(0)=0` 斷言皆為**套套邏輯**

- **閘②**：若以 helper **內部收斂檢查**量測 → bisect 收斂 ⇒ `f(S′)==range` **by construction**、**恆真必綠**，
  而 R3 真實池片仍 388.92。→ **閘② 須錨在 `F.0_G值` 之抵費地列「幾何面積(㎡)」**（`_pool_strips_for_block` 之**真實產物**）；**plan 未指定量測源**。
- **`f(0)=0` 斷言**：`app.py:6750-6751` `if S <= 0: return None, 0.0` ——**無條件早退、測不到任何幾何**（恆真）；
  **且右側正解式下 R3 之 `f_true(0)=78.24 ≠ 0`** → **該斷言會誤停**。→ **刪**。

---

## 4. ✅ PASS（reviewer 自驗坐實·採認）

| 項 | 結論 |
|---|---|
| **A 病灶四處·無第五處** | 多路 grep 全庫掃 → `÷avg_depth` 機制僅 `app:15291/15298`／`stepg:421/428`／`wf_f1:216`／`wf_f4:1121/1128`。`app:5299/5319`（`S=a(1-tb)/avg_depth`）為 `iterate_G_S` 之 S 迭代·非街角 buffer（排除正確·plan 未記錄此排除→已補）。`selection_pipeline:500-502` **確為產生端**（鏡射 `app:14467-14472`·不除深度）→ 非病灶 |
| **B forced 域＋閘①** | 3.5m 恰三塊；0m 全 cornered 側 `達資格候選 ≥1`、零強制；`F.0_滑池槽診斷_退縮0m.csv` 角落抵費地 L/R 全 0.0 佐證。碼路徑：病灶行位於 `if _fo_left:`（app:15287）內，而 `left_forced_offset = bool(_l_forced and _l_has_side)`（app:14460）於 0m 恆 False → **病灶行不執行 → 0m 零行為變更**。**閘①（0m 一字不變）作為絕對閘成立** ✅ |
| **D 單調性** | **成立**——strip ＝ `[0,S]×[-big,big]` 經基底 `(d_hat, n_hat)` 之像，`big` 只依 `block_poly.bounds`、**與 S 無關** → `strip(S₁) ⊆ strip(S₂)` → 交集面積單調不減。**係集合包含論證·不需凸性·斜交亦成立**。數值：6 塊 × 4000 點 → **遞減點 0、最大負增量 0.000e+00** |
| **E WATCH 界線** | plan 判斷**正確**：`app:16215` 出口為 `st.dataframe`＋判定字串、**無 raise/st.stop → 確為診斷非閘**；真 telescoping 閘在 `stepg:801-812`、用 `_adv_final['W0_left']`＝`_left_buffer_S * _cos_dn`（raw）✅ → **「不改」正確** |
| **F N0-18a 引用** | 與規格原文**實質相符** ✅；`left_cum_S = float(_left_buffer_S)`（app:15396／stepg:500）＝raw ✅ |

---

## 5. ⚠️ WARNING（施工前宜修·部分須裁）

- **W-1 🔴 閘⑤（Σ池恆定＝2299.26）為偽，且與閘④ 自相矛盾**（**須裁·見 §6-a**）
  `solve_G_binary` 實碼：`G_target = (a(1−A·B) − Rw·F·l_side − S·l_front)(1−C)`，`Rw = rw_increment(W_prev, W)`，
  首筆 `W_prev = buffer_S × cos_dn` → **`buffer_S → W₀ → Rw → G` 是<u>硬接線</u>**。
  reviewer 之模型對倉驗證（飽和域 `ΣRw = 100 − R(W₀)`）：預測 R5 46.33／R2 44.18／R3 44.04
  vs baseline 實跑 **46.3／44.18／44.07** —— **逐塊命中**。
  量化：**ΔΣG ＝ +3.62㎡ → Σ池 2299.26 → 2295.64（−260,852 元）**。
  → plan 之理由「forced 帶漲/縮由中央池吸收·全區池總量不變」**僅在 ΣG 不變時成立，而 ΣG 必變（閘④ 自己承認）**。
  → 附帶：`grep 2299 --include=*.py` → **0 命中 ＝ 散文閘**（N0-17-b）→ **不會讓 run_all 紅、只會靜默錯**。
- **W-2 「<600 候選」判準不穩健（我自陳最不確定處——確實不安全，但結論恰對）**
  R1(5.33)、R6(85.71) 皆 <600 卻**非 forced**；R3@0m 有 78.24 <600 亦非 forced → **該判準會產生偽陽性**。
  reviewer **改以結構複現證明歸屬**：R5 `strip(p1,+d,6.6723)=212.8359`≈212.84；R2 `=233.7880`≈233.79；
  R3 `311.6124+78.2363=389.8488`≈389.85。→ **§0 之 243.86㎡／17,572,213 元 算術與歸屬皆正確** ✅（獨立重算一致）
  ——**但論證方法須換**（結構複現 ≫ 面積門檻）。
- **W-3 副產物：碎片清單之真因已定位**（**須裁·見 §6-b**）
  **R1 5.33／R6 85.71／R3 78.24（W-D.3 §4 backlog）<u>全部</u>是 `s 域 ⊄ [0, actual_max_proj]` 之楔形**：
  R1 `s_min=−0.3248`→楔 5.3255；R6 `s_min=−3.6068`→楔 85.7064；R3 `s_max−amp=3.4571`→楔 78.2363。**三者與 baseline 逐位相符。**
  → **楔形該由 §N1 吸收或屬 §N5，是波次邊界判斷**。**若 §N5 先消滅楔形，plan 之簡式即自動變對——順序是實質問題、非文書問題。**
- **W-4 S1 單獨施工將可預期地破 N0-6 池三則**
  `docs/reports/W-G.4_v3偵察補查報告.md:110` 自載：R5 池 376.54 − 300.52 = **76.02 ∈ (0, MinA=157.64)**。
  `grep 池三則 --include=*.py` → **0 命中（散文閘）** → run_all 不會紅。**plan 未預告此後果。**
- **W-5 規格未入倉（已修）**：N0-18a 與 §N1 改寫原在工作區未 commit → **違反「數字錨引倉檔·禁憑記憶出艙」**。**本 push 已入倉。**
- **W-6 🔴 §N1／N0-18a 已載入錯誤歸屬**（**須裁·見 §6-c**）
  規格（KL 2026-07-17 落章）現寫「R3右 389.85（超額 80.92）」「R3 右須由 389.85 回切至 308.93」。
  **數字對，但「回切」之操作在 plan 之式下做不到**（做出來是 388.92）。
  **KL 落章所據之 `389.80→389.85` 錨重定，量的是「<u>池片</u>」而非「<u>buffer 帶</u>」——兩者在 R3 差 78.24（楔形）。**

**NOTE（行號·已修）**：`stepg:751`→**748**（`'S(m)': 0.0` 之真行號；**且它是 N0-18a 全部裁定之唯一舉證**·實質為真·僅行號差 3）；
`run_verification:1100`→**1111**（`_WF_NS_NAMES` 實數 **16** ✅·plan 之 16→17 正確）。
**未修**：`app:15399` 變數名為 `_W_prev_left` 非 `_W0_left`；**`_W0_left/Wf_left` 僅 stepg 有 → app 根本沒有 telescoping 閘**（既存 app/stepg 分歧·非 S1 造成·列 backlog）。
`_wf_ns()` docstring（`app.py:8403`）仍寫「13 app 真符號」·實為 16 → 動 `_WF_NS_NAMES` 時順手改 17。
**第五<u>消費端</u>（非病灶·不需改·但 scope guard 未列·已補）**：`app:6053/6056` → `left/right_corner_offset_area`
→ `app:15739/15741`／`stepg:782/784` → pool_diag 之 `角落抵費地L/R(㎡)`、`中央池(㎡)`（**入 baseline CSV**）。
**該 ledger 現以 range 記帳而幾何是 212.84 → 記帳已假設「forced 片＝range」**——**反向佐證 piece 讀法**；S1 後 `中央池` 值會動。
池片回傳依面積遞減排序（`app:6934`）→ `{blk}-抵費地-{i}` 序號 S1 後不變（300.52<1530.93、309.05<1572、308.93<1407.43）——**恰安全**。

---

## 6. 上呈 KL／claude.ai（**三題·reviewer 明言不裁·CC 亦不自裁**）

- **(a) N0-7（Σ池恆定＝2299.26）vs §N1 之張力**
  `buffer_S → W₀ → Rw → G` 為硬接線 → **§N1 修 buffer 必動 ΣG（+3.62㎡）→ Σ池 2299.26 → 2295.64**。
  **N0-7 曰「全案池總量恆定＝總負擔＝財務分析定值」**——二者不可兼得。
  **且 2299.26 為散文閘（碼中 0 命中）** → 不裁則**靜默錯**。
  → 請裁：①N0-7 之「恆定」是否容 §N1 之內生變動（若是，其量須可歸因＋立**碼閘**）；②抑或 §N1 須額外約束以保 Σ池。
- **(b) R3 之 78.24 楔形歸 §N1 或 §N5？（決定 S1／S5 之順序）**
  R1 5.33／R6 85.71／R3 78.24 **全部**係 `s 域 ⊄ [0, amp]` 之楔形（正交 `amp` vs 斜交 `s_max` 之差）。
  **若 §N5 先消滅楔形，S1 之簡式即自動變對**；**若 S1 先行，則須自行處理楔形（跨入 §N5）**。
  → **順序是實質問題、非文書問題**（reviewer 語）。請裁 S1／S5 之先後。
- **(c) §N1／N0-18a 之 R3 錨敘述須更正**
  「R3右 389.85 → 回切至 308.93」之 **389.85 量的是<u>池片</u>（= buffer 帶 311.61 ＋ 楔形 78.24），非 buffer 帶本身**。
  → 該句之操作在任何「只解 buffer 帶」之式下皆做不到。請更正錨之**歸屬敘述**（數字本身無誤）。
  **併記**：CC 之 S1 偵察報告 §4「真正微移者＝§N1 自身之 buffer 錨 389.80→389.85」——**該句之「buffer 錨」亦為誤稱**，應為「**池片**」。

---

## 7. CC 之處置

- **plan 不改、不動工**——俟 §6 三題裁定（(b) 尤其決定 S1 是否此刻該開）。
- **已修**：W-5（規格入倉）＋NOTE 之兩行號。
- **已採認**：reviewer 之「最小修正方向」1~4（側別參數化／刪 `f(0)=0`／閘② 錨改 `F.0_G值` 抵費地列幾何面積／閘⑤ 撤或改可歸因）
  → **俟裁後一併改 plan**（避免依未裁前提改兩遍）。
- **失敗考古候選（待入 #25）**：**「錨驗證了錯的通式」**——錨之維度若未被通式參數化，該錨對此維度零舉證力。
  本例：規格 §N1 唯一之 buffer 錨（R2 8.7157）恰為**左側**，遂使一個**無 side 參數**之通式完全複現該錨而仍錯。
  **與 #23（挾持）互補**：#23 是「錯的物件冒充對的身分」；本條是「**對的單點掩護錯的全域**」。
