# 【W-G.9-21 前置登記】`'b'` 槽之路徑分歧（⛔ 只查·不改·不估上限）

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`c55c1ec`（§0 現查所得·⛔ 非施工單所引）
**字樣錨（倉規 A）**：`【W-G.9-21 前置登記】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行（⛔ 無第三處）
　🔒 判別力自檢：改用寬字樣 `W-G.9-21` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> 🔒 **本檔為 commit A：⛔ 零生產碼、⛔ 零碼變更、⛔ 零 `G1`–`G5` 之量測。**
>
> # 🔴 本批之最高原則
>
> **⛔ 不得取任一條路徑代表全部。**
> 本批之受詞是**分歧本身**；凡取用某量之前，須先現查**生產路徑實際吃的是哪一個**。

---

## §0 登時已現查（⛔ 施工單所引一律不信·⛔ 逐項附母體與判準）

| # | 指令／母體 | 判準 | 結果 | 對照組（證判法非壞掉） |
|---|---|---|---|---|
| ① | `git rev-parse --short HEAD` | — | **`c55c1ec`** | — |
| ② | `git grep -l "W-G.9-21"`（母體＝git index 全部追蹤檔） | 含該字樣之**檔數** | **0**（＝期望） | `W-G.9-20` ⇒ **5 檔** |
| ③ | `grep -n "'b':" app.py verify/stepg_pipeline.py` | 含該字樣之**行** | **5 行** | — |
| ④ | `git grep -c "^### 🔒 K-9-5-13" -- docs/rulings/` | **定義式**（行首）之命中 | **1**（已入倉） | `K-9-5-12` ⇒ **1** |

### §0-1 🔴 ③ 之更正：**是 4 個餵入點，不是 2 個**

施工單 §0 曰「期望 **≥ 2** 個餵入點」。現查為 **5 行**，其中：

| 檔 | 行之字面 | 性質 |
|---|---|---|
| `app.py` | `left_side / right_side：{'has': bool, 'F': float, 'l1': float, 'b': float}` | **docstring**（⛔ 非餵入） |
| `app.py` | `'l1': _lside_left, 'b': _left_buffer_S * _cos_dn},` | **餵入**（左） |
| `app.py` | `'l1': _lside_right, 'b': _right_buffer_S * _cos_dn},` | **餵入**（右） |
| `verify/stepg_pipeline.py` | `'l1': _lside_left, 'b': _b_L0},` | **餵入**（左） |
| `verify/stepg_pipeline.py` | `'l1': _lside_right, 'b': _b_R0},` | **餵入**（右） |

⇒ **實際餵入 ＝ 4 個**（2 檔 × 左右各 1）。⚠️ **⛔ 不得說「兩個餵入點」**
——正確表述為「**兩條路徑**，各有**左右兩個**餵入點」。

⚠️ **射程**：本項之母體**僅 `app.py` ＋ `verify/stepg_pipeline.py`**（施工單所指定者）。
⛔ **不宣稱**全倉只有這些——⛔ 未查其他檔（`G1` 之量測將正面列舉受檢檔）。

---

## §1 🔒 上限估算**押後**（⛔ 本批不做）

施工單 §1 之裁定：**上限估算押後至三格跑得到之後。**
事由：`§2-2 ②`（母體涵蓋）現況不可能滿足——三格從未被跑到 ⇒ 觀測數 **0**
⇒ 連「已觀測最大值」之退路都不成立（`W-G.9-20` §四-3 已具名）。

⇒ **本批⛔ 不估上限、⛔ 不取任何近似、⛔ 不取任何代表值。**
⚠️ 已向 KL 說明 ⇒ ⛔ 本批不再問。

---

## §2 `G1`–`G5` 可證偽預測（⛔ 於任何量測之前寫定）

| # | 預測 | 證偽方式 ⇒ 觸發 | **判別力自檢之作法**（⛔ 於量測時附） |
|---|---|---|---|
| **G1** | 兩餵入點各自之**服務流程可由構造判定** | 不可判 ⇒ **G1 破**，具名，⛔ 不猜 | 對**已知答案**之第三個符號施同一判法，須得**已知之**結果 |
| **G2** | 三格之二值差**可逐格算出** | 算不出 ⇒ **G2 破** | 先證**倉內既有產物中無現成之量**（含**看欄名**·驗收 #4），再談算不算得出 |
| **G3** | 分歧之**起始 commit 可定** | 定不出 ⇒ **G3 破**，具名 | 以 `git log -S` 對**必然存在**之字樣試，須得非空 |
| **G4** | 存在**不經結構閘**之只讀路徑可跑到三格 | 無 ⇒ **G4 破**（⚠️ **合法結果**） | ⛔ **不採信**施工單所指之 log；須**自行現查該檔是否存在、其欄名為何** |
| **G5** | 本批**零判定層變動** | 出現任何差 ⇒ **G5 破 ⇒ `N-3`** | 同式對 `docs/reports/` 須**非空** ⇒ 證該式非恆空 |

🔴 **`G4` 之期望為「存在」——期望值為<u>順利</u>者尤易假成立**（驗收 #9）
⇒ 須證「**若不存在，該判法會給出不同結論**」。

🔴 **`G1` 之判定⛔ 不得由檔名或註解推定**（驗收 #2）——須由**構造**證：
即「**誰呼叫它**／**它被誰 import**／**該路徑之進入點是什麼**」，⛔ 非「它在 `verify/` 底下所以是測試」。
🔒 依 `CLAUDE.md`「倉內本體論優先於外部常識」：本倉之**引擎 ＝ `verify/wf_f0~f4`**，
`app.py` 是**接線層** ⇒ ⛔ 不得以外部直覺覆蓋。

---

## §3 停機條件

| 碼 | 條件 | 性質 |
|---|---|---|
| **N-1** | **兩條皆在生產路徑且結果不同** ⇒ 🛑 上呈 KL | **域邊界**——那表示同一份圖跑兩個功能會得到**不同的配地** |
| **N-2** | 動任一**生產函式** ⇒ 停機。⛔「只是暫時改回來」**不成立** | 紅線 |
| **N-3** | `run_verification.results` 之 **PASS/FAIL 層**改變 ⇒ 停機 | 紅線 |

---

## §4 ⛔ 明確未授權

1. ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 不動——一律**只讀包裝**。
2. ⛔ 不改任何閘或容差、⛔ **不估上限**、⛔ 不取任何代表值。
3. ⛔ 不動 `run_verification.py`／`wv_reconcile.py`／`baselines/`／既有夾具／三支複本／
   `docs/rulings/`／`CLAUDE.md`。
4. ⛔ 不改期望 FAIL 名單、不重凍、不重烤、不修任何既有 FAIL。
5. ⛔ **文件刪除欄一律 0**。
6. ⛔ 不追那 57 條、不追 `X5` 之 6 條、⛔ 不掛 `wg915` 盤點器。

---

## §5 🔒 本批自我加課（對應驗收 13 條）

| # | 規矩 | 本批如何遵守 |
|---|---|---|
| 1 | 🔴 **凡取用某量前，現查生產路徑實際吃哪一個** | ⛔ 不得取任一條代表全部——**這是本批之受詞本身** |
| 4 | 🔴 **宣稱「算不出來」前，先證倉內無現成之量** | **含看欄名**——⛔ 不得只看檔名 |
| 5 | 🔴 **「無從判定」≠「判定為偽」** | 出艙碼**分開**（`W-G.9-20` 已違反一次·**本批逐處自檢**） |
| 6 | 凡出艙一個計數，同時寫出**母體 ＋ 判準** | 逐表附 |
| 7 | 否定性宣稱之**射程**寫明母體 | ⛔ 不得籠統寫「倉內」 |
| 8 | 凡掛閘，**兩端至少一端來自受詞之外** | ⛔ 避免 `W-G.9-16` 之套套邏輯閘（考古 76） |
| 9 | 期望值為 **0／空／順利／否定**者附判別力自檢 | `G4`（期望順利）、`G5`（期望空）尤須 |
| 11 | 掃描器母體**釘在明確 commit**；證據產生後、落筆前⛔ 不重跑 | ⛔ 避免 `W-G.9-15` 之自我污染（**第四度**） |
| 13 | 凡書寫「正典」，就地標明**權威序層級**（4 級） | `docs/rulings/` ＝ **第 1 級** |

🔒 **立戒者在下一批最容易違反自己的戒**（`CLAUDE.md` 常設）
⇒ 本批**新近之戒**（`W-G.9-20` 之「無從判定 ≠ 判定為偽」）**逐處正面列出「已套用」**。

---

## §6 commit 分立

- **A** ＝ 本檔（⛔ 零碼、零量測）
- **B** ＝ 甲乙丙三組之量測（⛔ 零文件結論）
- **C** ＝ 報告 ＋ `GB-62` 之本實例登記 ＋ **丁組追認** ＋ 考古節

⚠️ **依倉規（不可變文件不記可變值），本檔 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本檔之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
