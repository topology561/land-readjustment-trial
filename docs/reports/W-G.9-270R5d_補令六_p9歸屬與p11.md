# `W-G.9-270R5d`　補令六之執行（坑 `ax` ／ `自誤 373`・`374` ／ `p9′` 結案登記 ／ `p11`）

> **態**｜開工 ＝ `2cf4a55968c59975783fc311c3aa0d5d9d58b59b`（`ls-remote` 實查·`left-right ＝ 0　0`）｜
> 工項零後 ＝ `2dd3ba7d0167c5e0500ea5df198fef370f39e2ef`
> **級** ＝ **中**（純登記 ＋ 純診斷·**零生產碼**）
> 🛑 **本批⛔ 辦**（補令六 `§六`）：⛔ 動 `34` 檔一字（**尤其⛔ 改 `TIER_EXPECT`／`FLAGGED_EXPECT`／
> `MINA_QU_EXPECT`／`HALF_EXPECT` 任一字**）／⛔ 落地 `K-9-29` 之任何子項／⛔ 落地 `GB-164`・`GB-166`／
> `GB-162` 三禁令維持／⛔ 以 commit 空間二分跨越洞／⛔ 呈 KL 任何事。

---

## `§零`　開工閘 ＋ 工項零

| 受詞 | 實測 | 判 |
|---|---|---|
| `ls-remote` 主線／`p3a` | `2cf4a55968…`／`05a11bd665…` | ✅ ＝ 補令六 `§零` |
| `left-right` | `0　0` | ✅ |
| **條款一** `CR` | **`0`**（`LF` `118`·bytes `9224`）；判別力造甲（真 CRLF）得 `2` | ✅ |
| **條款一** `core.autocrlf` | 有效值 **`false`** | ✅ |
| **條款二** blob `sha1` | `dff5828922f159a959fa783329adb9031426689a`（`index` 時 ＝ `HEAD:` 時） | ✅ |
| **條款二** blob 內容 `sha256` | `b71cda373a0d22283189dc01264fa8fcdbc40002a720fdf77bf6d871b822857a` ＝ 磁碟值；逐位相同 `True` | 🟢 **往返逐位保存** |
| **條款三** 來源逐字 | 主 checkout `docs/orders/` 之 untracked 檔·`9224` B·⛔ 由 KL 於本回合貼入 | ✅ |

🔒 **對照組**：補令五 blob 內容 `sha256` ＝ `56d42db906bbe2c4…`（相異且逐位相符）；空串 ＝ `e3b0c44298fc1c14…`。

### 零-1　🩸 **搬移法之二度失效**（皆 loud 攔下·⛔ 出艙錯值）

| # | 形 | 症狀 | 處置 |
|---|---|---|---|
| `1` | **全 ASCII glob 之唯一性<u>於同波多單下失效</u>** | `W-G.9-270_*p9*.md` 命中 **`2`**（補令五「…`p9`停機之裁…」與補令六「…`p9`歸屬…」皆含 `p9`），其差異**只在 CJK** | ⇒ 前此以「唯一性自檢 `== 1`」為據之搬移法，**在同波單號密集時⛔ 再可靠** |
| `2` | **「差集」法亦不唯一** | 「主 checkout 有而本 worktree 無」之差集實得 **`6`**（⛔ `1`）⇒ **loud `rc=7`** 攔下 | 改以 python 內之**完整檔名**逕指（CJK ⛔ 穿過 shell 引號層），並以**逐位 `sha256` 比對**收尾 |

🔴 **併帶之照實發現（⛔ 逕辦·逾令·具名候發單側）**：主 checkout 之 `docs/orders/` 有 **`6`** 個 `.md`
未入本 worktree，其中 **`5`** 個非本補令——`W-G.9-268prime_施工單_GB67病二_起算垂線錨點更正.md`（`20407` B）／
`W-G.9-269_補令四_安全烤法與P1改繫.md`（`10799` B）／`W-G.9-269_補令五_驗乙之重定與二檔無從驗.md`（`9366` B）／
`W-G.9-270_新施工窗開場文.md`（`12777` B）／`發單側交接文_claude.ai側換手.md`（`12915` B）
⇒ 屬 `作業常規之追加四`（施工單須原封入倉）之**積欠**。**本批⛔ 補入倉**（逾令）。

---

## `§一`　序 `1`：鑄坑 `ax` ＋ `自誤 373`／`374` ＋ `ALPHA_LABELS`→`ax` ＋ **三處**框字串齊一

### 一-1　🆕 本波新增之坑 `ax`（補令六 `§三` 款 `1` **准鑄**·標籤續 `aw`·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `ax` | 🔴 **以<u>寬於受詞</u>之樣式取結果** ⇒ 命中含**非受詞之列**而**⛔ 可辨** | 本批以 `grep -E 'REBUILD-OK\|自我驗證閘\|🛑\|·'` 取重建器之結果，而 `·`（分隔點）**咬中 `compute()` 之逐宗診斷列** ⇒ 輸出被淹；🔴 **其最險處**：命中數看起來「有結果」，而所命中者全非受詞 | 樣式須**窄至只能命中受詞**（例：唯一標記、完整逐字），並以**必不命中之造乙**（執行期組出之人造字樣）自證其非恆命中 |

🔒 **`ax` 與 `av` 之別**（承發單側對 `av`／`al` 之分判）：`av` 之受詞為**輸出之取法**（`tail -N` 截斷），
`ax` 之受詞為**樣式之寬窄**（命中非受詞之列）——二者**可同時發生而互不掩蔽**。

### 一-2　`ALPHA_LABELS` 之擴充（`aw` → `ax`）＋ **三處**框字串齊一

🔒 **逐字自證**：`aa`〜`ax` 命中 **`3`**（期 `3`·逐處 `:40`／`:281`／`:283`）；`aa`〜`aw` 殘留 **`0`**；
判別力（寬字樣 `字母系`）＝ **`4`** ⇒ 該檢⛔ 恆為 `3`。`numstat` ＝ `5`／`4`；`py_compile` OK；`CR` ＝ `0`。

🩸 **本項之二自捕（皆當場顯形）**

| # | 形 | 症狀 | 處置 |
|---|---|---|---|
| `3` | **以 `chr(N)` 組出字元而碼位記錯** | 我以 `chr(12295)` 組波浪號，而 `12295` ＝ **`〇`（U+3007）**、⛔ `〜` ⇒ 替換**靜默為空操作** | 🔒 **攔法 ＝ 同格並報「期 `N` 實 `M`」**——其得「期 `3` **實 `0`**」⇒ **當場顯形**（檔案未被改）；改以 `Write` 落腳本，字面**直接寫出**（⛔ 經 shell 之引號層） |
| `4` | **診斷列之標籤與其所取之索引不符** | 印「波浪號之碼位 ＝ `U+0060`」，而 `U+0060` ＝ **反引號**（我取 `OLD[2]` 而波浪號在 `OLD[3]`） | ⛔ 影響結果（替換用完整字面）；照實具名 |

### 一-3　`自誤 373`／`374` 之鑄（責 ＝ **發單側**）

- **`373`**：**列舉一判準之翻轉路時，只拆其合取項，而漏列其<u>集合型自由變元</u>**
  ⇒ 通則：**凡列舉一判準之翻轉路，須先把該判準之全部自由變元逐一列出**（含集合型、含 `bool(集合)` 這類存在量詞）。
- **`374`**：**於單中指定一落點，而未先自倉實查其存在**
  ⇒ 通則：**錨只能是逐字**（承 `自誤 372`：行號⛔ 為錨、他文之指涉亦⛔ 為錨）。

🔒 **取號現查**（態 `blob@2dd3ba7`·母體 追蹤 `.md` **`734`** 檔·二形並取·錨定框）：
`自誤 373` B 形 `1` 檔／`3` 列・C 形 `1`／`3`；`自誤 374` B 形 `1`／`1`・C 形 `1`／`1`；
**逐筆歸類**（`裁 H`）——**全數**為**補令六自身之授權條款** ⇒ **⛔ 撞號**。
**對照組**：甲 `自誤 372` `5`／`14`・`5`／`14` ✅；甲2 `自誤 371` `5`／`8`・`5`／`8` ✅；
乙 人造哨兵（執行期組出·字面⛔ 出艙）`0`／`0` ✅ ⇒ **器非紅**。

🔒 **閘 `①②`**（本次為**次號與次次號** ⇒ 閘 `②` 適用）：
自誤 相異 **`362 → 364`**（`+2`）／**MAX `372 → 374`** ✅／缺號集 `[106,355,356,357]` **⛔ 動** ✅；
其餘三簿（`GB` `164`／`166`・`VR` `80`／`95`・`K-9` `28`／`29`）**⛔ 動** ✅
——🔑 其中 `GB` 之⛔ 動即證本批之 GB 表追加**確⛔ 鑄新 `GB`**（補令六 `§四-2` 明令）。

---

## `§二`　序 `2`：`p9′` 之結案登記（`W-G.4_泛用阻塞項登記表` 末端追加·⛔ 鑄新 `GB`）

🔒 **落點** ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔尾（`+82`／`−0` 列·`828856 → 836234` B）。
🔒 **`append-only` 機驗**（基座 `2dd3ba7`·⛔ `--cached`）：嚴格前綴 **OK**；
判別力［必 `FAIL`］`old.startswith(new)` ＝ `False`；`numstat` 刪除欄 `0`；`CR` ＝ `0`。

**其載**：`§一` 之五步逐字鏈（**CC 逐步當場復現**）＋ `§二` 之三紅錨分流表。

🔒 **五步之末（決定性者）**：`app.py` 之逐字錨 `temp_parcels, _diag_k6 = k6_step0_merge(` ⇒ **命中 `1`**
（判別力：寬字樣 `k6_step0_merge` @ `app.py` ＝ **`4`** ⇒ 該檢⛔ 恆為 `1`）⇒ 🔴 **該廢止確未落地**。

🔒 **歸屬（發單側之裁·CC 採納）**：`ok_t`／`ok_f` 之紅**⛔ 為「合法變動·錨未同步」**、**⛔ 為「新破損」**，
**係第三族：已裁未落地**（`K-9-29 二` 已由 KL 於 `2026-09-08` 廢止步驟 0，而碼側尚未撤除）。

🛑 **⛔ 以「重烤消化」處置 `ok_t`／`ok_f`** ——KL 已逐字確認廢止之後果為「`9` 組／`13` 宗**解併**」
⇒ 解併後二錨之值**可能回復**；此刻重烤 ＝ **`GB-97` 之覆轍**。

### 二-1　🔑 一項**受詞已對齊**之二數（照實·⛔ 判孰誤）

依賴序更正節之案由段載發單側現查「`628-40(1)+`（`G010`·`628-40(1)`＋`628-43(1)`·**`Σa 513.93`**）」，
並自載「**CC 於本批⛔ 復現** ⇒ 其⛔ 得作為倉內事實出艙」。
🔒 **CC 已復現其二**（`W-G.9-270R5c` 款 `2`）：`G010` 之宗清單 `628-40(1);628-43(1)` → **`628-40(1)+`**
⇒ **歸戶、成員、命名逐字相符** ✅
🔑 **而 `a2c4b2b` 之 commit message 逐字為**
`八群逐群結果（成員／命名／Σ幾何面積_m2）＝R2 628-40(1)+628-43(1)→628-40(1)+ 513.79`
⇒ 🔒 **`513.79` 之受詞 ＝ `Σ幾何面積_m2`**，而 `513.93` 之受詞 ＝ **`Σa`（重劃前面積）**
⇒ 🟢 **二者確⛔ 同一量** ⇒ **`0.14` 之差⛔ 構成矛盾**。
🛑 **`Σa 513.93` 之復現仍待**（本批所測者為 `ΣG_戶` 與 `Σ幾何面積`，**皆非 `Σa`**）⇒ 其【倉】化候後續批次。

---

## `§三`　序 `3`：**`p11` —— 待落地清單與依賴序之現態出艙**

🛑 **本節⛔ 提落地之順序主張、⛔ 動任何碼、⛔ 判任何項之優先**——**出艙即止**（`§四-3` 款 `4`）。
**器** ＝ `verify/probes/probe_WG9270R5d_p11.py`｜落檔 `verify/out/WG9270R5d_p11.log`｜`rc = 0`。

### 三-1　款 `1`：`3′` 一項之逐字 ⇒ 🔴 **其已被<u>更正</u>取代**

🔒 **`3′` 之立節** ＝ `## 🔧 依賴序之更正：插入 \`3′　K-9-29 碼側落地\`（\`W-G.9-253\` 工項一·⛔ 上文一字不刪·純末端追加）`
🔴 **其後另有一節** ＝ `## 🔧 依賴序之更正二：插入 \`3″　-249 段三觀測（重跑）\`（\`W-G.9-254 §三\`…）`
⇒ 其內之逐字錨 `更正後之依賴序（以本節為準`（**命中 `1`**·判別力：寬字樣 `依賴序` ＝ **`11`**）載：

```
3′  K-9-29 碼側落地（廢步驟 0·a′·入池閘）    🔴 有土地後果·逐 commit 候 KL
3″  -249 段三觀測（重跑）                      ⇐ 依賴 3′ 之 c2 併線後
4   五級實作                                   （r1–r8 齊備後）
```

🔒 **依賴之方向（原節逐字）**：「`3′` **支配** `4 五級實作`——五級之入池閘（`K-9-29 六`）係 `3′` 之一部，
⇒ **`3′` 未落地前，五級實作之入池判準⛔ 完整**。」

### 三-2　款 `2`：`CLAUDE.md` 之 `⬜` 清單（**二框並報**）

| 框 | 列數 |
|---|---|
| 窄框 `⬜ **未落地**` | **`10`** |
| 寬框 `⬜` | **`76`** |

🛑 **二數相異 ⇒ ⛔ 以其一代另一**（`VR-091 一`：計數須同格載其框）。
**對照組**：必命中 `待落地` ＝ **`48`**（`>0` ✅）／必為零（執行期組出）＝ `0` ✅ ⇒ **器非紅**。
🔒 **逐列之全文**（`檔:導引行號` ＋ 逐字）已出艙於落檔 `verify/out/WG9270R5d_p11.log`
——🛑 **行號僅作導引、⛔ 為錨**（`自誤 372`）。
🩸 **併記（`⬜` 之自指增長·`GB-132` 族）**：`CLAUDE.md` 自身已三處載明「凡以 `⬜` 之全檔命中數為量者，
須自扣本節之增量」⇒ 上開 `76` **含該等自指之列**，**⛔ 等於待落地項數**。

### 三-3　款 `3`：`K-9-29` 碼側落地之**四子項**之現態（逐項以**逐字**證·⛔ 以「已讀過」代「已執行」）

| 子項 | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `app.py` 之 `k6_step0_merge` 呼叫（KL 已令廢） | 逐字錨 `temp_parcels, _diag_k6 = k6_step0_merge(` ⇒ **命中 `1`**（判別力：寬 `k6_step0_merge` ＝ `4`） | 🔴 **仍在** |
| `2` | `wf_f0` 之 `Σa` vs `a + a′` | @ `verify/wf_f0.py`：`a′` **`14`**／`Σa` **`9`**／`a + a` **`0`**／`_mina_by_block` `11` | 🛑 **⛔ 由字樣命中逕判其已否落地**——僅出艙字樣之在否 |
| `3` | 步驟 D 是否逐筆 | `步驟 D` @ `app.py` **`3`**／`verify/wf_f0.py` `0`／`verify/stepg_pipeline.py` `0`；`步驟D` @ `app.py` `1`；`逐筆` @ `app.py` **`12`**／`wf_f0` `1`／`stepg_pipeline` `3` | 同上 |
| `4` | 入池閘是否存在 | `入池閘` @ 四檔**皆 `0`**；`入池` @ `app.py` **`2`**、餘三檔 `0` | 同上 |

**對照組（證器非紅）**：`調配池` @ `app.py` ＝ **`4`**（`>0` ✅）。

🛑 **本節⛔ 判四子項之落地與否**——子項 `1` 之「仍在」係**逐字錨之直證**；
子項 `2`〜`4` **僅出艙字樣之在否**，⛔ 由字樣命中逕推其已否落地（`§四-3` 款 `3` 明令）。

---

## `§四`　收工閘

### 四-1　自限復驗

| 受詞 | 實 | 判 |
|---|---|---|
| 生產碼 `34` 檔 | `app.py` blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`；聚合 `c6320ce6def9a3c6b5a2c4281e30b84bf754c8ad44a3e603f42aa4c208c87458` | ✅ 期初 ＝ 期末 |
| **五個 `*_EXPECT`** | `verify/wd4_tier_list.py` 之非 `??` 列 ＝ `0` | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·聚合 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` 側支 | `merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | 未設 | ✅ |
| `K-9-29` 之任何子項 | ⛔ 落地（`app.py` blob 未動即其證） | ✅ |

### 四-2　四簿期末

| 簿 | 相異（期初 → 期末） | MAX | 缺號集 |
|---|---|---|---|
| **自誤** | **`362 → 364`**（`+2`） | **`372 → 374`** | `[106,355,356,357]`（⛔ 動） |
| `GB` | `164`（⛔ 動） | `166` | `[12,87]`（⛔ 動） |
| `VR` | `80`（⛔ 動） | `95` | `[73,75]`（⛔ 動） |
| `K-9` | `28`（⛔ 動） | `29` | `[]`（⛔ 動） |

### 四-3　🛑 **上呈項**（⛔ CC 自裁）

1. 🔴 **`docs/orders/` 之積欠 `5` 件**（`§零-1`）——屬 `作業常規之追加四`；本批⛔ 補（逾令）。
2. 🟡 **`Σa 513.93` 之復現仍待**（`§二-1`）——受詞已對齊（**⛔ 同一量**），惟該數本身未復現。
3. 🟡 **候選新坑二則**（`§一-2` 之自捕 `3`／`4`：`chr(N)` 碼位記錯／診斷列索引取錯）——⛔ 鑄（授權僅 `ax`）。
4. 🟡 **搬移法之二度失效**（`§零-1`）——全 ASCII glob 與差集法**皆⛔ 再唯一**；其常規化候發單側。
5. **承前批而仍待者**：`p5`／`p6` 之事實；`GB-166` 之 `(1)(2)`；`ok_t`／`ok_f` **待 `K-9-29` 落地後重量**。

### 四-4　坑之**定義處清單自證** ＋ `通則 I`

🔒 **其實測見本批之<u>末一 commit</u> 之訊息**——本報告含 `D-T` 形之坑表（`§一-1`）
⇒ 其為**受詞之一員**（`GB-132` 族·`通則 I`）。

---

## `§五`　🛑 **停**

候發單側之令，⛔ 自行續辦。
