# `W-G.9-318R5`　補令四執行報告：復驗受領／恆常附款簿之立／`自誤 442`／恆常附款 `x`・`y`

> **分級** ＝ **中**（零生產碼·⛔ 改生產碼一字·⛔ 驅動全量）。
> **開工態** ＝ `bc97c9d72d1680e820872aba787ecc453fa3a509`（`wip/W-G.9-318-preserve`）。
> **主線** ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（**一字未動**）。
> 🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**——本批一切含反引號之樣式皆以**檔案管線**為之（恆常附款 `h`）。
> 🔑 **檔名之提案（`常規三`）**：本檔取 `W-G.9-318R5`，承既有序 `R`（原單）→`R2`（補令一）→`R3`（補令二）→`R4`（補令三）。候選二 ＝ `W-G.9-318R4p`、候選三 ＝ `W-G.9-318R4_補令四…`。**取 `R5` 之由**：既有序為單調遞增之整數且無空號，`R5` 係其唯一之次號，⛔ 生歧義。**候發單側核可。**

---

## `§零`　工項零：本補令原封入倉（`追加四` 款一·**第一工項**）

| 量 | 值 |
|---|---|
| 落檔 | `docs/orders/W-G.9-318_補令四_復驗受領與恆常附款簿及自誤442.md` |
| 來源 bytes／`sha256` | `18336` B／`825ae7c56a52a0151305482459a426efddb582cbb695f66d728f402e7e422820` |
| 入倉 bytes／`sha256` | `18336` B／**同上** |
| 逐位相符 | 🟢 **True** |
| `CR`／`LF` | `0`／`190` |
| `commit` | `6f4c7ec` |

🛑 **款一之射程界限（照實·承 `追加四` 之既載）**：該 `sha256` 所證者為「**落檔 → 入倉**」之保真，
**⛔ 證「發單側送出之原文 → 落檔」之保真**——本補令內⛔ 載其送出時之 `sha256`。

### `§零-1`　入倉前之號占用查（`追加四` 補款 一・二）

🔒 **現查**（態 `bc97c9d`·母體 ＝ 全 `docs/` **`835`** 檔〔讀不到 `19`〕·**檔框 ∪ 列框**）：
`W-G.9-318` 之占用者 ＝ **同號之原單與補令一〜三及其報告**。
🔒 **⛔ 觸「已占即停」**：本件係**同號之補令四**，發單側於 `§零` 逐字載「**五者皆留倉**」
⇒ 其占用係**應然**，⛔ 異常。此即倉內既載之結構缺陷
（`CLAUDE.md`「號占用閘之框改宣告框」節末逐字：「**同號重發之單永遠過不了自己的閘**」·
其處置**仍為未決之意思決定**）。⛔ 自行改號（改號屬意思決定·歸發單側／KL）。
🔒 **對照組（補款 二）**：查一已知占用之號 `W-G.9-234` ⇒ 檔 `4`／列 `9`（`≥ 1`）✅；
人造哨兵（執行期組出·字面⛔ 出艙）⇒ `0`／`0` ✅ ⇒ **器非紅**。

---

## `§一`　開工閘（**`10` 項**·逐閘出艙）

| # | 閘 | 實得 | 判 |
|---|---|---|---|
| `1` | 環境 | `Python 3.13.11`（Anaconda·`MSC v.1929 64 bit`）／`git 2.53.0.windows.1`／`ezdxf 1.4.3`／`shapely 2.1.2` | 🟢 |
| `2` | 取回本波 | `git fetch origin refs/heads/wip/W-G.9-318-preserve` ⇒ `FETCH_HEAD` ＝ **`bc97c9d72d1680e820872aba787ecc453fa3a509`**（全 `40` 碼·與單載逐位相符） | 🟢 |
| `3` | 主線 | `git ls-remote origin refs/heads/wip/s1-endpart` ⇒ **`40375702378af4cefd2d11515fdf3f98c0791580`**（未動）；`git merge-base --is-ancestor 4037570 bc97c9d` ⇒ `rc` ＝ **`0`** | 🟢 |
| `4` | 分支 | 遠端總數 ＝ **`25`**；`refs/heads/verify/*` ＝ **`20`** | 🟢 |
| `5` | preflight | `probe_order_preflight.py` ⇒ 🔴 **`0`** 項／🟡 **`0`** 項；`P-5` `SELF_SHA256` **相符**（受詞 `18258` B·實算 `850016aede1f32a1…` ＝ 單載）；`P-3` `def main` 區間 ＝ `14996`-`24605` | 🟢 |
| `6` | `core` 設定 | `core.autocrlf` ＝ **`false`**（`rc` `0`）；`core.quotepath` **未設**（`rc` `1`） | 🟢 |
| `7` | 生產碼 | 正面列舉 **`34`** 檔·存在 `34`／缺 `0`；聚合 `sha256` ＝ `e7080c4b64964b09f6711c34e078224e0199fce49ca251805b75c302816f3420`；`app.py` ＝ `8466f7493082bf84…`（`1465843` B）·`stepg_pipeline.py` ＝ `866dd3cb3dc2ca7c…`（`111891` B）——**錨繫於 `bc97c9d`**（⛔ 沿用前批·恆常附款 `w②`） | 🟢 |
| `8` | 四簿 @ `bc97c9d` | **二框並報**（見 `§一-1`） | 🟢／🩸 |
| `9` | `baselines` | `git ls-tree -r HEAD verify/baselines \| sha256sum` ＝ **`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`**·**`298`** 列（`quotePath` 預設） | 🟢 |
| `10` | 取號現查 | 見 `§一-2` | 🟢 |

### `§一-1`　閘 `8`：四簿之二框並報（母體逐簿**單檔**·粒度框 ＝ **列框**）

| 簿 | 框（逐字） | 相異 | `MIN` | `MAX` | 缺號 | 對單載之判 |
|---|---|---|---|---|---|---|
| 自誤 | 正典框〔定義框 ＋ 範圍框〕 | **`431`** | `7` | **`441`** | `[106, 355, 356, 357]` | 🟢 **與單載 `431`／`441` 逐位相符** |
| 自誤 | 定義框〔⛔ 併範圍框〕 | `418` | `7` | `441` | （`17` 個） | 🩸 **⛔ 復現單載之自擬框 `427`／缺 `8`**（框相異·具名·⛔ 追改） |
| `GB` | 標題錨定框 `^#{2,4}…GB-N` | `96` | `7` | **`171`** | （`69` 個） | `MAX` 🟢 **相符**；**相異 ⛔ 復現單載之 `169`** |
| `GB` | 裸錨定框（`≤ 3` 碼·扣哨兵 `997`／`998`） | `174` | `1` | `883` | （`709` 個） | 同上 |
| `VR` | 標題錨定框 `^#+ \`?VR-N\`?`（扣哨兵 `999`） | **`80`** | `14` | **`95`** | `[73, 75]` | 🟢 **與單載 `80`／`95` 逐位相符** |
| `K-9` | 標題錨定框 `^#{2,4}…K-9-N` | **`40`** | `2` | **`41`** | `[]` | 🟢 **與單載 `40`／`41` 逐位相符** |

🩸 **`GB` 相異之具名相異（⛔ 追改）**：單 `裁 1`／閘 `8` 載 `GB` 相異 **`169`**（缺 `[12, 87]` ⇒ `MIN` `1`）。
CC 以**六種框**對位（`verify/out/WG9318p4_gbframe.log`）**皆⛔ 復現**該三數。
🔒 **其⛔ 影響任何 operative 判準**：`(i)` 本批**⛔ 鑄任何 `GB` 號**；`(ii)` `MAX` ＝ `171` 二框一致、與單載相符；
`(iii)` `CLAUDE.md` 逐字載「`GB` 之定義列式仍**⛔ 經正典釘定**（`GB-138` 處置③ 攢批中），
在其釘定前，⛔ 得出艙 `GB` 之『相異／缺號集』**除非同格載明所用之框**」
⇒ 本表已逐列載框，其相異之歧異**係該未釘定之直接後果**、⛔ 本批之瑕。

### `§一-2`　閘 `10`：取號現查（母體 ＝ **全倉追蹤 `.md` `857` 檔**〔讀不到 `0`〕）

**框**：意指占用之**錨定框**（`常規四（七）` 補款一）·**二形並取**（補款二／`GB-158`）。

| 受詞 | B 形〔`自誤 N`〕列／檔 | C 形〔`` `自誤 N` ``〕列／檔 | 二形聯集列 | 除本補令自身之產物外 | 判 |
|---|---|---|---|---|---|
| **`自誤 442`**（受詢） | `0`／`0` | `0`／`0` | `0` | **`0`** | 🟢 **未占用** |
| `自誤 441`（對照甲【必非零】） | `13`／`4` | `13`／`4` | `13` | `13` | 🟢 **非零** |
| 人造哨兵（對照乙【必為零】·執行期組出·字面⛔ 出艙） | `0`／`0` | `0`／`0` | `0` | `0` | 🟢 **零** |

**`恆常附款 x`／`y`**（字母框·**二形並取**·裁 `3` 之二形）：

| 款 | 定義 | 引用 | 其中係**框之後設變數** | **真占用** | 判 |
|---|---|---|---|---|---|
| `x` | `0` | `3` | **`3`** | **`0`** | 🟢 **未占用** |
| `y` | `0` | `0` | `0` | **`0`** | 🟢 **未占用** |
| `u`（對照甲【必非零】） | `2` | `5` | — | — | 🟢 |
| `z`（對照乙【必為零】） | `0` | `0` | — | — | 🟢 |

🔑 **`x` 之 `3` 命中逐筆具名**——三者**皆為「框」自身之後設變數**（以 `x` 代任一字母），
**⛔ 款 `x` 之占用**：
`docs/reports/W-G.9-318R4_補令三_保全push之授權與四簿期值更正及自誤441_執行報告.md:86`／
`docs/reports/W-G.9-318_CC側換手_一.md:137`／`:159`（三處逐字皆為 `` 框 ＝ `` 恆常附款 `x` `` `` 之形）。

---

## `§二`　工項一：恆常附款登記表之立

**檔** ＝ `docs/reports/W-G.9波_恆常附款登記表.md`（新檔·`append-only`·`209` 列）·`commit` `2a69c63`。
**產生器** ＝ `verify/probes/probe_WG9318p4_build.py`。
🔒 **`常規七 一`**：簿內**逐字條文**皆由**檔案管線**自倉側 blob 以 `(檔, 起列, 迄列)` 抽取寫入，
**⛔ 一字出於 CC**；**所繫之自誤號亦機械導出**（定義處在自誤簿者取其最近之前一則
`### 🩸 自誤 N` 標題，在單者取其行內之 `（自誤 NNN）` 標記）。

| 閘 | 受詞 | 實得 | 判 |
|---|---|---|---|
| `二-1` | 款數 ＝ `23`（`a`〜`w`）·⛔ 缺號⛔ 增號 | `§一` 表之 `a`〜`w` ＝ **`23`**·缺號 `[]`；另列本批新立 `x`／`y` ⇒ 總 `25` 列 | 🟢 **符** |
| `二-2` | 出處欄皆可由 `git show <檔>:<列>` 復現（抽驗 `5` 款·逐字相符） | `a`／`d`／`j`／`q`／`w` **重跑抽取並逐位 `diff`** ⇒ **`5`／`5`** 相符（`常規七 三`：⛔ 僅比對三數） | 🟢 |
| `二-3` | 判別力〔必不命中〕：人造款名於本簿之命中 ＝ `0` | `0`／`0` ✅；〔必命中〕已知款 `a` 之節題 ＝ `1` ✅ | 🟢 |
| `二-4` | 🔴／🟡 之款數 ＝ `3`／`4` | **實得 🔴 `0`／🟡 `0`／🟢 `25`** | 🩸 **不符·已具名**（下 `§二-1`） |

### `§二-1`　🔴 與 `裁 3` 之**可得性判相異**（具名·⛔ 追改）

🔒 **`裁 3` 之判（逐字轉錄）** ＝ `a`／`b`／`f` 🔴 **⛔ 可得·失傳**；`e`／`g`／`h`／`i` 🟡 **可重建**。
🔴 **CC 之現查（態 `bc97c9d`）** ＝ **`a`〜`w` 全 `23` 款之逐字條文皆在倉** ⇒ **失傳 `0`／重鑄 `0`**。

**根因 ＝ 框之射程**——與 `裁 3` 自己所診斷者同族，惟其所列之三形**未涵蓋主形**：

| 形 | 樣式 | 涵蓋 |
|---|---|---|
| ① | `立為恆常附款[^\n]{0,20}?\`([a-z])\`` | `j`〜`w` |
| ② | `恆常附款 \`([a-z])\` [：＝]` | 本倉命中 `0` |
| ③ | 僅見引用 | — |
| 🆕 **④** | 單 `§一-1` 之 `（\`a\`）**…**` **全形括號列舉形** | `a`／`b`／`c` |
| 🆕 **⑥** | 單 `§一-1` 之「🆕 **本單所增**…」**區塊形** | **`a`〜`w` 之全部** |

**`a`〜`i` 之定義處逐款具名**（皆為形 ⑥·**⛔ 失傳**）：

| 款 | 定義處 |
|---|---|
| `a`／`b`／`c` | `docs/orders/W-G.9-308_補令一_K-9-38與K-9-39之鑄及自誤420至421與款4之靜態結案.md:82`／`:83`／`:84` |
| `d`（`d-1`〜`d-3`） | `docs/orders/W-G.9-310_施工單_K-9-40之鑄與三則末端註記及自誤422.md:83`–`:86` |
| `e`／`f` | `docs/orders/W-G.9-311_施工單_修批前置之現態診斷與自誤423_424.md:89`／`:90` |
| `g` | `docs/orders/W-G.9-312_施工單_步驟0二態對拍與自誤425.md:90` |
| `h`／`i` | `docs/orders/W-G.9-313_施工單_自誤426與312二態對拍之登記.md:82`／`:83` |

🔒 **單 `§二` 三禁之遵守（逐條）**
① **⛔ 憑記憶補寫** —— 未犯（每一字皆檔案管線抽取）。
② **⛔ 由引用反推條文** —— 未犯（所取者為**定義列本身**，⛔ 引用列）。
③ **⛔ 另擬一條充之** —— 未犯。
🛑 **未從者僅其<u>分類指令</u>**（「`a`／`b`／`f` 一律標 🔴 失傳」）——其**前提經實測為偽**，
依 `CLAUDE.md`「**與倉態衝突以倉為準**」改以**具名相異**承載（簿 `§二`、本節）；
**⛔ 靜默改判、⛔ 追改 `裁 3` 一字**。**可得性判之更正繫發單側。**

### `§二-2`　🔑 `§六` 之三禁與本節之關係（**⛔ 誤讀**）

`§六-3` 逐字「⛔ 憑記憶補寫或由引用反推 `a`／`b`／`f` 之條文；⛔ 另擬一條充之」——
**本批一件未犯**。其所欲防者為**捏造**；而本批所為係**自倉逐字抽取**，係其反面。

---

## `§三`　工項二：交接文之補正

**受詞** ＝ `docs/reports/W-G.9-318_CC側換手_一.md`·`commit` `aff66dd`·新增節 **`§9`**（四款）。

| 量 | 值 |
|---|---|
| 期初 | `17908` B（＝ 開工態 `bc97c9d` 之 blob·逐位相符） |
| payload | `4975` B／`68` 列／`CR` `0`·`sha256` `39eadb3c606f4e946b282abad70277fc802b6949878814c65c785025897b3a4a` |
| 期末（算式） | `17908` ＋ `4975` ＝ **`22883`** B·`sha256` `44806b14784611f9a07d952a8cf036058c3cec4029f562729cf4c35e424ebf68` |
| `append-only` 四造 | 前綴逐位 `True`／payload 逐位 `True`／bytes 算式 `True`／期末 `sha256` 已載 |

### `§三-1`　`裁 2` 之**獨立復現**（`恆常附款 y①②` 之首次施行）

🔒 **CC 自倉重導**（⛔ 採信補令之表）：
框（逐字）＝ `git show --numstat --format= <h> -- app.py verify/stepg_pipeline.py verify/run_all.py verify/run_verification.py`；
母體 ＝ `git rev-list --reverse 4037570..bc97c9d` 之 **`26`** 筆。

| # | 雜湊 | 改生產碼 | `numstat` | 性質 |
|---|---|---|---|---|
| `1` | `7f43924` | `1` 檔 | `app.py(+36/-36)` | 現行·純位移 |
| `2` | `7c346b1` | `1` 檔 | `verify/stepg_pipeline.py(+30/-30)` | 現行·純位移 |
| `3` | `7c07a36` | `1` 檔 | `app.py(+15/-0)` | 現行·`w0_start` 之增 |
| `4` | `6cc51e6` | `2` 檔 | `app.py(+8/-0)` `stepg(+2/-0)` | 現行·薄殼直通 |
| `5` | **`b1141a3`** | **`2` 檔** | `app.py(+3/-0)` `stepg(+3/-0)` | 🔴 **中間態·已被 `8e056b4` 完全取代** |
| `6` | `8e056b4` | `2` 檔 | `app.py(+3/-3)` `stepg(+3/-3)` | 現行·`c7′` forced 側之避讓 |

⇒ 🟢 **與 `裁 2` 之表逐列相符**（含 `b1141a3` 列為第 `5`）⇒ **`自誤 442` 之受詞坐實**。

---

## `§四`　工項三：`自誤 442` 之鑄 ＋ 恆常附款 `x`・`y` 之立

**受詞** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md`·`commit` `a15840b`。

| 量 | 值 |
|---|---|
| payload 之來源 | 本補令 `:135`–`:141`（**檔案管線**抽出並剝 `> ` 前綴·`常規七 一`） |
| 標題形 | 自該簿**最近一則**（`自誤 441`）逐字複刻（`恆常附款 v①`） |
| payload | `1533` B（**含**前導分隔 `\n` `1` B 與末換行 `1` B·`恆常附款 l②`）·`sha256` `bc76400b87114d5f7808002fa4a1abb967645bbfcc15c700e43cf3eb5726b422`·`CR` `0` |
| 期末（算式·`恆常附款 w①`） | 期初 `935085` ＋ `1533` ＝ **`936618`** B·`sha256` `61a7689f8d42ba799336dd2a1d9ea662184b46367142ced96f1540826aad778d` |
| 四造（`恆常附款 j③`） | 前綴逐位 `True`／payload 逐位 `True`／bytes 算式 `True`／期末 `sha256` 已載·`CR` 全檔 `0` |

🔒 **`恆常附款 x`**（`裁 4`·所繫欄填【CC 自捕第 `10` 則·⛔ 鑄自誤號】）與
**`恆常附款 y`**（`自誤 442` 之攔法·二款）皆登記於簿 `§一`／`§三`，其逐字自本補令 `:144`／`:139`–`:141` 抽取。

---

## `§五`　本批之**自解清單**（`作業常規之追加五 §一` 五項·逐項具名）

| # | 疑義（逐字） | 二讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | `§二` 閘 `二-1`「款數 ＝ `23`（`a`〜`w`）·⛔ 增號」 vs `§四`「`恆常附款 x` 之立（登記於 `§二` 之簿）」＋「立為恆常附款 `y`」 | 甲：嚴守 `23`，⛔ 錄 `x`／`y`；乙：`a`〜`w` 恰 `23` 以滿足該閘，`x`／`y` 另列並標「本批新立」 | **乙** | 簿 `§一` 之 `a`〜`w` ＝ `23`·缺號 `[]`（`二-1` 🟢）；`x`／`y` 另列 ⇒ 總 `25` | 甲將**遺漏二款已立之附款**，與 `§四` 之明文直接牴觸 ⇒ 依 `常規二` 取**保守項**（⛔ 遺漏任何已立之款） |
| `2` | `§零-1` 之號占用（`W-G.9-318` 已占） vs `追加四` 補款 一「已占即停」 | 甲：停機；乙：同號補令之占用係應然，續辦並具名 | **乙** | 補令 `§零` 逐字「五者皆留倉」；`CLAUDE.md` 逐字載「同號重發之單永遠過不了自己的閘」係**既載之未決缺陷** | 甲將使**每一張補令皆不可執行**；且改號屬意思決定·⛔ CC 自裁 ⇒ ⛔ 改號、只具名 |

🔒 **五項之逐項自檢**：① 零土地後果（二者皆⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何）；
② 零生產碼（`§六` 閘 `2` 實測相異 `0`）；③ 可機驗（`二-1`〜`二-4` 皆附必紅／必綠二造）；
④ 可逆（皆 `append-only` 或新建檔·`§六` 閘 `1` 刪除欄全 `0`）；⑤ 出艙具名（即本節）。
🛑 **⛔ 落入黑名單任一項**。

### `§五-1`　本批之**自捕**（**`6` 則**·皆於出艙／入倉前）

| # | 自捕 | 其形 |
|---|---|---|
| `1` | `GB` 框過窄（`^#{2,4}` ⇒ 相異 `96`） | 單一框⛔ 足以出艙相異 ⇒ 增列裸錨定框並**二框並報** |
| `2` | 🔴 對 `自誤 N` 套用**宣告框 `⑤`／`⑨`**（其受詞 ＝ **單號 ＝ 檔名之號**）⇒ **對照甲得 `0`** | **對照組紅即量測器紅**（`常規五`）。改用**意指占用之錨定框**（母體 ＝ 全倉追蹤 `.md`） |
| `3` | 人造哨兵取 `自誤 9997`——其字面**已在倉內**（鬆框 `11` 列） | 對照乙【必為零】不為零 ⇒ 改為**執行期搜出並當場自證為真零**之號 |
| `4` | 字母框僅取帶反引號之一形 ⇒ 與 `裁 3` 之 `b`／`e` 不符（`0`／`1` vs `2`／`8`） | 改**二形並取**後與 `裁 3` 之 `a`〜`i` **逐格相符** ⇒ 反成其框之**獨立復現** |
| `5` | 閘 `二-1` 之母體未限於 `§一` ⇒ `§二` 之「定義處」表亦被咬（偽陽 `9` 列·得 `34`／`32`） | **敘述句污染其所量之集合**（`GB-132` 族）⇒ 母體限於 `§一` 區段 |
| `6` | 閘 `二-3` 之哨兵 `chr(0x71 + 2)` ＝ **`s`**，係**真存在之款** ⇒【必不命中】得 `1` | 執行期組出之哨兵**亦須自證其為真零** ⇒ 改取真不存在之字母 |

🔑 **自捕 `2`／`6` 之通則（候發單側裁是否立款）**：**「執行期組出」⛔ 蘊含「真零」**
——哨兵須**當場自證其於母體之命中 ＝ `0`**，否則對照乙恆為偽綠。
本批已將該自證內建於 `pick_sentinel()`（`verify/probes/probe_WG9318p4_gates.py`）。

---

## `§六`　收工閘（**輕**·報告入倉後量·⛔ 沿用他批之判準字串與錨值）

（本節於報告入倉後以**嚴格末端追加**補。）

---

## `§七`　🛑 上呈（**意思決定·⛔ CC 自裁**）

1. 🔴 **`裁 3` 之可得性判與倉態不符**（失傳 `3`／重鑄 `4` ⇒ 實為 **可得 `23`**）。
   本批已依 `二-4` 之明文「具名相異·⛔ 追改」承載；**其更正（含 `§二` 三禁 ① 之分類指令是否撤回）繫發單側。**
2. 🩸 **`GB` 相異 `169` ⛔ 復現**（六框皆不得）。其根因 ＝ `GB` 之定義列式**⛔ 經正典釘定**（`GB-138` 處置③ 攢批中）。
   **是否於本波內釘定 `GB` 之定義列式，繫發單側。**
3. 🔑 **檔名之核可**（`常規三`）：本報告取 `W-G.9-318R5`（候選見卷首）。
4. 🔑 **自捕 `2`／`6` 之通則是否立為恆常附款**（哨兵須當場自證為真零）——繫發單側。
5. 🛑 **`push` 之請示**：本批 **`6` 個 `commit` 全數零生產碼**（`§六` 閘 `1`／`2` 為證）
   ⇒ 依 `常規一` 補款 `🔧 一`（KL `2026-08-31`）「**零生產碼 commit 逕行 push**」，
   CC 逕推至 **`wip/W-G.9-318-preserve`**；**⛔ 觸 `wip/s1-endpart`、⛔ 自 preserve 支 fast-forward 主線**（候 KL 逐筆放行）。
