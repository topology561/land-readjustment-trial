# 🧾 `W-G.9-173R`　二路徑之 `R2` 池差 `5.23 ㎡`（主）＋ `R1` `+0.01` 之二分歸因

**施工單**：`W-G.9-173_R2池差與R1二分歸因.md`
（`SELF_SHA256 = 11ed24da1cf8d4899331962e45a48be77d5092c5d3aa27c1083997654f7f36d8`
——**CC 現算逐位相符**·計法 ＝ 扣除最末一行·`198` 列／`17234` B；
四反引號界線實查 ＝ `[123, 162, 166, 183]` ⇒ **`2`** 塊·與單載相符）
**基座（`S-1` 實查）**：`0c88cc85fef117131375e6555717efc8aac53be0`
**`app.py` blob（期初 ＝ 期末）**：`68f7bfab610ec78688b9d7322ae4bc2aac74493e`
**性質**：只讀量測 ＋ 純附加登記 ＋ 一項待 KL 實跑　🛑 ⛔ 零生產碼·⛔ 未重產 baseline

🛑 **總判**：`§三` 六項預測 ⇒ **符 `3`／不符 `3`／不可判 `0`**（和 ＝ **`6`** ✅）。
🔴 **`W-1` 不符 ⇒ 依停機條件上呈**：`G.3` 之受詞集合**確含**逐街廓池面積、**確含** `3.5m`
⇒ 判為 **(ii)**（其後某批使之失效）。定位見 `M-A-1` 末。

---

## 🔴 `§一`　`M-A`　二路徑於 `R2` 之池差

### `M-A-1`　`G.3`（`335f5f3`）射程之現查　🔴 **判 (ii)**

| 受詞 | 出處（`檔:行 @commit`） | 逐字 |
|---|---|---|
| 情境迴圈 | `verify/wg_g3.py:118` @`0c88cc8` | `    for setback, tag in ((0.0, "0m"), (3.5, "3.5m")):` |
| 受詞表（`f0`） | `verify/wg_g3.py:28-35` | `("g_tab", "G值", ["所屬街廓", "暫編地號"])`／`("diag_tab", "滑池槽診斷", ["街廓"])`／`("slot_tab", "逐槽J表", …)`／`("dec_rows", …)`／`("flag_rows", …)`／`("pool_rows", "池差", ["情境", "街廓"])` |
| 受詞表（`f1`／`f2`／`f3`／`f4`） | `verify/wg_g3.py:36-59` | `f1`：`reshape_rows`／`frag_rows`／**`("pool_rows", "池驗證", ["情境","街廓","池片"])`**；`f2`／`f3`：`conv_rows`／`g_tab`／**`("pool_rows","池流向",["情境","街廓"])`**；`f4`：`conv_rows`／`exit_rows`／`g_tab`／**`pool_rows`**／`reshape_rows`／`ledger_rows` |
| 判準 | `verify/wg_g3.py:134-139` | `ok_cell, viol = rv.diff_rows(rows, bpath, kcols, …)`／`base_bytes = open(bpath, "rb").read().replace(b"\r\n", b"\n")`／`ok_byte = (got_bytes == base_bytes)` |
| commit 訊息之自陳射程 | `git log -1 335f5f3` | `scope(誠實)：證 ctx-builder 分歧為零·app-path 輸出==baseline byte級；引擎/區段價合法性由 run_all+法規+claude.ai 把關·非本波所證` |

🔒 **`g_tab` 之列確含抵費地**——`verify/baselines/wf/f0/F.0_G值_退縮3.5m.csv` 現查含
`R1-抵費地-1`／`R1-抵費地-2`／`R2-抵費地-1`／`R2-抵費地-2`／`R4-抵費地`／`R5-抵費地-1/-2`／`R3-抵費地-1/-2`／`R6-抵費地-1/-2`
（欄 `幾何面積(㎡)`）。

⇒ 🔴 **`G.3` 之受詞集合<u>含</u>逐街廓之抵費地池面積、且<u>含</u> `3.5m`** ⇒ **判 (ii)**：
`W-1`（預測「⛔ 不含」⇒ 判 (i)）**不符** ⇒ 依停機條件**上呈**。

🛑 **定位（現查·⛔ 非推定）**：`G.3` 所比之 baseline 係 **`wf/f0`〜`f4`** 之**世代**表，
其 `f0` 之 `3.5m` 池 Σ 為 `R1 1174.40`／`R2 1805.11`／`R4 1229.06`／`R6 1628.99`；
而 **Step-G 階段**之基準另在 `verify/baselines/v3/G 值計算結果_退縮3.5m.csv`
（`R1 1174.40`／`R2 1774.40`／`R4 1229.06`／`R6 1628.69`）。
🔒 **KL 所報之四數（`1175.10`／`1774.20`／`1228.36`／`1628.71`）與 <u>`v3` Step-G 基準</u>之量級相符
（差 `+0.70`／`−0.20`／`−0.70`／`+0.02`），與 `f0`〜`f4` 任一世代皆不符**
⇒ 🔒 **KL 之顯示值係 <u>Step-G 階段</u>之池、⛔ 非 `f0`〜`f4` 世代之池。**
⇒ 🛑 **`G.3` 之 `pool_rows`／`g_tab` 所護者係「世代」之池；<u>Step-G 之池</u>由 `v3` 基準所護
（`run_verification` 之 `v3·G值` 對拍），而該對拍於現基座之 trunk A **`②-宗 圍堵閘破[R2]` 即中止**
⇒ `R2` 以後之 Step-G 池<u>從未被比到</u>**（`GB-118`）。**此即 (ii) 之機制。**

### `M-A-2`　二路徑輸入之逐項對拍（🛑 **僅 shim 一側可量**）

🛑 **可量性之界**（承 `W-G.9-172 M-A-1`）：app `main()` 之池呼叫端（`app.py:20892`）被
`app_harvest._filter_module` 濾除 ⇒ **app 側之輸入⛔ 不可量**。
本節出艙 **shim 側之全部輸入**，與 KL 實跑之**四則逐字事實** ＋ **`v3` Step-G 基準**三方對拍。

**shim 側之 `R2` 輸入（情境 `3.5m`·態 `68f7bfab…`）**：

| 項 | 值 |
|---|---|
| `block_poly.area` | `4368.603346850` |
| `_depth` | **`44.47`**（🔴 vs `case_params_UC9898.json` 之 `R2.街廓分配深度_m` ＝ **`43.9`**·差 `0.57`） |
| `d_hat` | `[0.6756855318673246, 0.7371899768887057]` |
| `corner_pt` | `[310361.51526, 2651912.18005]` |
| `allocation_dir` | `[0.7260188428756796, 0.6876748067142342]` |
| `biz_n` | **`14`**（＝ KL 側逐字之 `14`·**同**） |
| `Σbiz.area` | **`2589.176526798`** |
| 逐宗 | `0.007822`／`183.174430`／`29.607754`／`299.319090`／`89.976052`／`236.537153`／`391.660631`／`58.949875`／`432.113341`／`65.445691`／`147.188795`／`146.590175`／`71.592234`／`437.013484`（各附 `npts` 與座標 `sha16`·見證據檔） |
| **強制抵費地** | 🔴 **`forced_map['R2']['left_forced_offset'] = True`**／`left_corner_min_area = **308.17**`（⇒ **shim 側<u>有</u>強制抵費地**） |
| 池片 | `[1471.256820020, 308.170000011]` ⇒ **Σ ＝ `1779.426820031`** |
| `telescoping` 閘 | 🔴 **破**：`ΣRw_實跑=100.02 ≠ R(W_final=34.39)−R(W₀=6.98)=44.41（Δ=55.606）` ⇒ `run_step_g` 於 `R2` **之後**終止 |

🔒 **恆等式自檢（逐位）**：`4368.603346850 − 2589.176526798 = 1779.426820052`
⇒ 與實得 `1779.426820031` 差 `2.1e-8`（浮點）⇒ **池 ＝ 街廓 − Σ宗** 成立
⇒ 🔒 **池之差<u>完全等於</u> Σ宗之差（符號相反）**。

**🔴 三方對拍（`3.5m`·Step-G 階段）**：

| 街廓 | `v3` baseline Σ | KL app（2dp） | shim Σ | `KL − base` | `shim − base` |
|---|---|---|---|---|---|
| `R1` | `1174.4000` | `1175.10` | `1175.096297` | **`+0.70`** | **`+0.6963`** |
| `R2` | `1774.4000` | `1774.20` | `1779.426820` | **`−0.20`** | 🔴 **`+5.0268`** |
| `R4` | `1229.0600` | `1228.36` | ⛔ 不可達 | `−0.70` | — |
| `R6` | `1628.6900` | `1628.71` | ⛔ 不可達 | `+0.02` | — |

逐片：`R1` base `[1169.07, 5.33]` vs shim `[1169.7082, 5.3835, **0.0046**]`（shim 多一片 ＝ `K-9-5-7` 配餘）；
`R2` base `[1465.35, 309.05]` vs shim `[1471.2568, **308.17**]`。

🔴 **第一個產生差異之輸入 ＝ `R2` 左側之「街角規定範圍面積」**（＝強制抵費地帶）：
`verify/baselines/v3/W-D.1.2 診斷_退縮3.5m.csv` 之 `R2` `範圍面積(㎡)` ＝ **`309.05`**
（逐列四筆皆同）＝ baseline 之池片 2；shim 之 `left_corner_min_area` ＝ **`308.17`** ＝ shim 之池片 2
⇒ **差 `0.88`**，且**該量本身即池之一片** ⇒ 其為**輸入層**之差、⛔ 非輸出層之巧合。
**第二個差異 ＝ `Σ宗地幾何`**：shim `2589.1765`；由恆等式反推 baseline `≈ 2594.20`、KL `≈ 2594.40`
⇒ **shim 之 Σ宗較 baseline 少 `≈ 5.03`**（＝ 池之差）。
**第三個具名差異 ＝ `_depth`**：shim 現算 `44.47` vs 快照 `43.9`（`R1`：`33.15` vs `32.97`）
——`avg_depth` 進 `G` 之計算 ⇒ 直接影響 Σ宗（`K-8 §三 A-2` 之「現算 N-19′ 深度注入」）。

🩸 **CC 自捕一則（量測器之時序誤·⛔ 非路徑差異）**：本探針曾於 `run_step_g` **之前**讀
`ss['f3_corner_winners']`／`ss['f3L_forced_offset']` 而得 `None`，並含 `f3_g_iter_params`
之 `is_corner=True` 筆數 ＝ `0`（KL 側逐字「步驟 L 覆寫 `7` 筆」）。
🔒 **現查後更正**：`verify/stepg_pipeline.py:282-283` 逐字
`ss["f3_corner_winners"] = winners_state`／`ss["f3L_forced_offset"] = forced_map`
——二鍵係**在 `run_step_g` 內部**才被種入 ⇒ 前述 `None` 係**讀取時序**所致、**⛔ 非二路徑之差異**。
🛑 `f3_g_iter_params` 之 Step-L 覆寫（`app.py:20015-20041`）於 harness **確無對應**，
惟其唯一消費點 `stepg:473` 落於 `if _degenerate_order:` 分支（`UC9898` **不可達**）
⇒ **⛔ 不列為本案之差異**。

### `M-A-3`　`5.23` 之歸屬　⇒ **判 (甲)**（harness 側之量已漂離）

🔑 **判別之關鍵在 `R1` 之交叉對照**：
`R1` 之 `KL − base = +0.70` 與 `shim − base = +0.6963` **幾乎相同**
⇒ 於 `R1`，**app 與 shim <u>同步</u>漂離 baseline**（⇒ 該處係 **baseline 過期**、二路徑一致）；
而於 `R2`，**KL 僅 `−0.20` 而 shim `+5.03`** ⇒ **二路徑於 `R2` 確有實質分歧，且離群者係 shim**。

⇒ 🔒 **判 (甲)：harness／shim 於 `R2` 少算 Σ宗（＝多算池）約 `5.03 ㎡`**。
其**具名之候選成因**（皆已現查、按證據強弱排序）：
1. **街角規定範圍面積 `308.17` vs `309.05`**（差 `0.88`·**直接**是池之一片）；
2. **`_depth` 現算 `44.47` vs 快照 `43.9`**（差 `0.57`）⇒ 經 `G` 影響全部 `14` 宗之幾何；
3. **`R2` 之 `②-宗 圍堵閘` 於 `0m` 破**（宗-宗重疊 `45.9766` ≫ 上界 `2.8906`）
   ＋ **`telescoping` 閘於 `3.5m` 破**（`Δ=55.606`）⇒ `R2` 之宗地幾何在 harness 側**確有既存之病**。

🛑 **⛔ 判為 (乙)**（app 算錯）——故**⛔ 不觸 `W-3` 之停機**。
🛑 **⛔ 判為 (丙)**——二路徑之「抵費地池」定義**同一**（`池 ＝ 街廓 − Σ宗`·恆等式於 shim 側逐位成立），
差異在**輸入之值**、⛔ 非定義。
⚠️ **反面之併陳（⛔ 不隱去）**：`v3` baseline 係**凍存且已知過期**（`GB-111`／九份 baseline 待重產）
⇒「離 baseline 遠」⛔ **不等於**「錯」。本判之**唯一**支撐係 `R1` 之交叉對照（app 與 shim 同步、`R2` 不同步），
⛔ 非「baseline 為真」之假定。

---

## `§二`　`M-B`　`R1` `+0.01` 之二分歸因（**待 KL 實跑**）

### `M-B-1`　參數風險之消除　🔴 **⛔ 不可自動載入**

**現查**：`app.py` 之 `case_params` 字樣 **`5`** 處——
`:6004`／`:12196`／**`:13319-13322`**／`:20756`／`:22752`。
其中唯一之**載入器**係 `app.py:13319-13323` 逐字：

```
def _wf_load_snapshot(app_file):
    """β：財務 ctx 之凍結來源＝verify/case_params_UC9898.json（裁定②）。回傳完整 snapshot dict。"""
    import os
    import json
    p = os.path.join(os.path.dirname(os.path.abspath(app_file)), "verify", "case_params_UC9898.json")
```

🔒 其射程逐字為「**財務 ctx 之凍結來源**」（`裁定②`），且其唯一消費者係 `_build_wf_ctx`
⇒ 🔴 **app ⛔ 無「以快照填 UI 側街參數」之機制**；`:20756` 之字樣僅係**錯誤訊息中之提示**
（逐字 `"（尺度=0 多因未勾新闢；參數正典見 verify/case_params 快照）後重跑。"`）、⛔ 非載入器。
⇒ `W-4`（預測「可載入」）**不符**（⛔ 非停機）⇒ **改出手動設定清單**：

🔒 **KL 須手動設定之全部欄位與正典值**（直取自 `verify/case_params_UC9898.json`）

**全域**：分區 ＝ `住宅區`／法定最小寬 ＝ `3.5 m`／法定最小深 ＝ `14 m`／重劃區總面積 ＝ `34950 m²`／`C% = 8.96`
**情境**：`退縮_m ∈ {0.0, 3.5}` ⇒ 本趟只跑 **`3.5m`**

| 街廓 | 正面路寬 | 正面負擔尺度 | 既成道路 | 街廓分配深度 | 法定最小寬 | 左側路寬／尺度 | 右側路寬／尺度 |
|---|---|---|---|---|---|---|---|
| `R1` | **`12`** | **`0`** | `是` | `32.97` | `3.5` | `8` ／ `1` | `8` ／ `1` |
| `R2` | `8` | `2` | `否` | `43.9` | `3.5` | `8` ／ `1` | `8` ／ `0` |
| `R3` | `8` | `2` | `否` | `43.64` | `3.5` | `8` ／ `0` | `8` ／ `1` |
| `R4` | **`12`** | **`0`** | `是` | `32.59` | `3.5` | `8` ／ `1` | `8` ／ `1` |
| `R5` | `8` | `2` | `否` | `45.04` | `3.5` | `8` ／ `1` | `8` ／ `0` |
| `R6` | `8` | `2` | `否` | `44.59` | `3.5` | `8` ／ `0` | `8` ／ `1` |

🔒 **四則正典裁示（逐字·`case_params.rulings`）**：
> ① `R1、R4 正面道路寬=12m（系統預設 8m）；該二路為既成道路、非重劃新闢 → 正街臨街地特別負擔=0（正街尺度=0）`
> ② `12m 與 8m 路寬對應相同之畸零地最小寬（3.5m）→ 街角 range/T 不因此差異而變（W-D.1.3 各波結果不受影響之因）`
> ③ `側街尺度=正街之半（8m 新闢：正街 2、側街 1）；尺度 0=既成/無新闢負擔`
> ④ `良性不一致（記錄）：退縮參數表之 R1 正面路寬顯示 8（街角模組殘留），正典=12；因同最小寬故街角結果無影響。harness 一律設 12。`

🛑 **自檢**：設畢須確認輸出 **⛔ 無** `F×l₁ = …×0.00` 之警告（八個側街參數皆已生效）。
⚠️ **併記**：上表之「街廓分配深度」係**快照之凍值**；harness 現行改採 **N-19′ 現算**
（`R1` `33.15`／`R2` `44.47`·`K-8 §三 A-2`）⇒ 🛑 **若 app 之 UI 亦現算深度，KL ⛔ 不必手動輸入該欄**；
該欄之**實際來源**須於實跑時由 KL 順手回報（見操作單 ⑤-b）。

### `M-B-2`　供 KL 實跑之工作副本　✅ **已備妥**

| 項 | 值 |
|---|---|
| **路徑** | **`C:\kl173`** |
| `HEAD`（全 `40` 碼） | **`a3c97aa0ada17389c465550e04d90388c346f670`** |
| `app.py` blob | **`f285b91c9e5f97f13dd74508e1a403833bc75cb3`**（＝ `W-5` 之期望值·**逐位相同** ✅） |
| `git status --porcelain` | 空（clean） |
| `data/` | `V6.dxf`／`V6_1.dxf`／`地籍資料來源_匿名版.xlsx` 皆在 |

🛑 **一處具名之偏離**：單令「建 `git worktree`」，CC 改以**獨立 clone**（`git clone` ＋ `checkout --detach`）。
🔒 **理由**：`git worktree` 須自某個既有 repo 派生，而唯一持久之 repo 係 **KL 之工作副本**——
派生 worktree 會寫入其 `.git/worktrees/`，違 `§六` 之「⛔ 不動 KL 之工作副本」。
獨立 clone **完全不觸** KL 之副本，且對 KL 之操作**完全等價**（同 `HEAD`／同 blob／同 `data/`）。
🔒 **⛔ 未動 KL 工作副本一字**。

### `M-B-3`　交 KL 之操作單　⚠️ **待發單側核可·⛔ 未交 KL**

> **①** 開一個新的命令列視窗，執行：`cd /d C:\kl173`
> **②** 執行 `git log -1 --oneline` 與 `git rev-parse HEAD` 與 `git status --porcelain`，
> 　　**把三者之輸出原樣貼回**（🔒 `戒 24` 之態三證：`HEAD` 全 `40` 碼／與 `origin` 之關係／工作樹淨否）。
> 　　期望：`HEAD` ＝ `a3c97aa0ada17389c465550e04d90388c346f670`、`git status` **空**。
> 　　（本副本係**獨立 clone**、⛔ 非 KL 原工作副本 ⇒ ⛔ 不必 `git pull`。）
> **③** 啟動：`streamlit run app.py`（或 KL 慣用之啟動方式）。
> **④** 依 `M-B-1` 之表設定參數；**只跑 `3.5m` 情境**。
> 　　設畢先確認畫面**⛔ 無** `F×l₁ = …×0.00` 之警告。
> **⑤** 執行至 Step G（抵費地／池計算）後，記下並貼回：
> 　　**a.** `R1`／`R2`／`R4`／`R6` 之**抵費地池面積**（各街廓之總和，二位小數）；
> 　　**b.** 畫面上 `R1`／`R2` 之**街廓分配深度**顯示值（供判定深度來源·見 `M-B-1` 併記）；
> 　　**c.** **中止點**（若有 `圍堵閘破[…]` 或其他紅字，逐字貼回）；
> 　　**d.** 「步驟 L 覆寫 N 筆」「強制抵費地」之逐字訊息（若有）。
> **⑥** 跑畢再執行一次 `git status --porcelain`，**原樣貼回**（🔒 態三證之收尾·證該趟未改動任何檔）。
> **⑦** 全部貼回後，該副本即可刪除：`rmdir /s /q C:\kl173`（⛔ 在 ⑤⑥ 貼回**之前**不要刪）。

### `M-B-4`　歸因之判準（單載·預先寫死·CC ⛔ 未更動一字）

| `a3c97aa` 之 `R1` 實得 | 結論 |
|---|---|
| `1175.09` | ⇒ 變動產於 **`-169R`**（＝ `L-4` 對 `app.py:20165` 之 ghost 排除）🔴 **`VR-074` 之落地有未登記之土地後果** |
| `1175.10` | ⇒ 變動產於 **`-161R`** 或 **`-163R`** 🔴 **該批之「零行為變更」結論須就地加註更正** ⇒ 須再二分一次 |
| 其他值 | 🛑 **停機上呈**——三態非單調，另有變因 |

---

## `§四`　取號現查之自行複現

母體 ＝ **全倉 `.md`** ⇒ **`450`** 檔；**態 ＝ `0c88cc85fef117131375e6555717efc8aac53be0`**（期初 tree·`VR-081 一`）。
🔒 **量測器須自母體扣除自身**（`VR-083 二`）——本批之二 payload 暫存檔（`_p1.md`／`_p2.md`）
已含待鑄號之字面；**未扣除時實測** `VR-083` ＝ `1`／`1`／`1`、自誤 `209` 形① ＝ `1`／`2`／`1`、形② ＝ `2`／`2`／`1`
（母體 `452`）⇒ 扣除後始得 `0`（母體 `450`）。🔴 **同形第三度·⛔ 於受詞上而非僅對照組上。**

| 待鑄 | 定義列式（逐字） | 相異號 | MAX | 錨定框：命中列數／出現次數／檔數 | 對照組（同框） |
|---|---|---|---|---|---|
| `VR-083` | `` ^##\s*`VR-([0-9]{1,4})` `` | `67`（單檔）／`68`（全倉） | **`82`** | `0`／`0`／`0` | `VR-082` ＝ `4`／`4`／`4` |
| 自誤 `209` | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `173` | **`208`**·缺號數 `35` | 形① `自誤 209` ＝ `0`／`0`／`0`；形② `` 自誤 `209` `` ＝ `0`／`0`／`0` | 形① `自誤 208` ＝ `2`／`3`／`2`；形② `` 自誤 `208` `` ＝ `3`／`4`／`2` |

🔒 **二形之字面**：**形① `X N`**／**形② `` X `N` ``**。
🔒 **算術自檢**：`208 − 35 == 173` True。🔒 **與單載逐項相符**（含二對照組之三數）。
⚠️ `VR` 相異號之單檔／全倉之別承 `W-G.9-171R §五-3 A`（差集 `{VR-075}` 落於 `K-6`）·**MAX 皆 `82`** ⇒ ⛔ 不影響取號。

### `§五-6`　形之自證 ＋ MAX 推進

| 類 | 期初@`0c88cc8` 相異／MAX | 期末 相異／MAX | **新號** | 新號 ∈ 缺號集？ | 算術自檢 |
|---|---|---|---|---|---|
| `VR` | `67`／`82` | `68`／**`83`** | **`[83]`** | `False` ✅ | `83−15==68` True |
| 自誤 | `173`／`208` | `174`／**`209`** | **`[209]`** | `False` ✅ | `209−35==174` True |

---

## `§三`　六項預測之逐項對拍

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `W-1` | `G.3` 之受詞集合**⛔ 不含**逐街廓池面積（⇒ 判 (i)） | 🔴 **含**（`g_tab` 之 `*-抵費地*` 列 ＋ 四代之 `pool_rows`），且**含 `3.5m`**（`wg_g3.py:118`）⇒ **判 (ii)** | 🔴 **不符** ⇒ **停機上呈**；定位已附（`G.3` 護「世代」池·Step-G 池由 `v3` 基準護，而該對拍於 `R2` 中止 ⇒ `R2` 以後從未被比到·`GB-118`） |
| `W-2` | 第一個差異出現於**強制抵費地 buffer**（shim 側**無**） | 🔴 shim 側**有**（`left_forced_offset=True`·`left_corner_min_area=308.17`）；第一個差異係該帶之**面積值** `308.17` vs baseline `309.05`（差 `0.88`） | 🔴 **不符**（「shim 無」證偽），🟡 惟「差異出現於強制抵費地帶」**成立** ⇒ 以實查為準·⛔ 未改框 |
| `W-3` | 判為 **(甲)** 或 **(丙)** | **(甲)**——`R1` 之交叉對照證 app 與 shim 同步漂離 baseline，而 `R2` 僅 shim 漂 | ✅ **符**（⛔ 未判 (乙) ⇒ ⛔ 不觸該停機） |
| `W-4` | app **可**載入 `case_params` 快照 | 🔴 **不可**——唯一載入器 `app.py:13319` 之射程逐字係「財務 ctx 之凍結來源」 | 🔴 **不符**（⛔ 非停機）⇒ 已出手動設定清單 |
| `W-5` | `a3c97aa:app.py` blob ＝ `f285b91c…` | **逐位相同** ✅ | ✅ **符** |
| `W-6` | 受檔 `deletions` 逐列 `0`；`app.py` blob 期末 ＝ `68f7bfab…` | `39/0`／`17/0` ＋ 新增檔；`app.py` ⛔ 不在 `numstat` 內 | ✅ **符** |

**和 ＝ `6`**（符 `3` ／ 不符 `3` ／ 不可判 `0`）✅
🔒 **`S-2` 遵守**：⛔ 未為使預測相符而改探針／框／容差／母體。
🔒 **`S-4` 遵守**：本批之產物以**態**命名（`WG9173_r2inputs_68f7bfab.json`）。

---

## `§五-1`／`§五-2`　倉態 ＋ `numstat`

| 項 | 值 |
|---|---|
| 開工閘 `ls-remote` | `0c88cc85fef117131375e6555717efc8aac53be0`（＝ `S-1`） |
| clone `HEAD`（全 `40` 碼） | `0c88cc85fef117131375e6555717efc8aac53be0` |
| **`app.py` blob（期初 ＝ 期末）** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`** |

| 受檔 | 新增 | **刪除** |
|---|---|---|
| `docs/驗證裁定登記表.md`（`P-1`） | `39` | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`P-2`） | `17` | **`0`** |
| 新增檔（本報告 ＋ `1` 探針 ＋ `verify/out/**` 之證據） | — | **`0`** |

🔒 **二 payload 皆末端追加**（`new.startswith(old)` ＝ `True`·`crlf` ＝ `0`）：
`docs/驗證裁定登記表.md` `4134`／`322017`／`ea70bc8d13c9c3db` → `4173`／`324997`／`05a027249e26479a`；
`docs/reports/W-G.9波_claude.ai側自誤登記.md` `3970`／`350566`／`6e2aeaa0b38e2cd6` → `3987`／`353127`／`02d93651fa2e3261`。
🔒 **payload 之取法（常規七）**：`sed -n 'A,Bp'` 自單身直取；`sha16` ＝ `2e102810f86675ca`（`P-1` `38` 列）／`0fd21fd5dccc747b`（`P-2` `16` 列）。

---

## `§五-8`　報告檔名之擇一（常規三）

| 候選 | 檔名 |
|---|---|
| **甲（採）** | `docs/reports/W-G.9-173R_R2池差與R1二分歸因.md` |
| 乙 | `docs/reports/W-G.9-173R_G3射程與三方對拍.md` |
| 丙 | `docs/reports/W-G.9-173R_VR083_自誤209.md` |

**一句理由**：採**甲**——與施工單同名尾（承 `-171R`／`-172R` 之體例），單與報告以單一框即可成對取得。

---

## 🔒 收工聲明

1. 🔴 **`W-1` 判 (ii) ⇒ 停機上呈**：`G.3` 之射程**確含**池面積與 `3.5m`；其失效之機制已定位為
   「**Step-G 之池由 `v3` 基準所護，而該對拍於 `②-宗 圍堵閘破[R2]` 中止 ⇒ `R2` 以後從未被比到**」（`GB-118`）。
   🛑 **本報告⛔ 未指認任何一批為肇因**——`M-A-3` 之判為 **(甲)**（harness 側），
   而「哪一批使 `G.3` 失效」須另批以**逐 commit 二分**定位；⛔ 不推定。
2. 🔴 **`5.23` 判 (甲)**（harness 於 `R2` 少算 Σ宗 ≈ `5.03`），三候選成因已具名並排序；
   🛑 **⛔ 判 (乙)** ⇒ ⛔ 不觸 `W-3` 之停機；反面（baseline 過期）已併陳。
3. ✅ **`M-B` 已備妥**：`C:\kl173`（`a3c97aa`·blob 逐位相符）＋ 手動參數清單 ＋ 操作單
   ——🛑 **操作單⚠️ 待發單側核可·⛔ 未交 KL**。
4. 🔒 **⛔ 零生產碼**；⛔ 未解 `GB-118` 之中止、⛔ 未改任何閘、⛔ 未落地 `裁 A`／`裁 B`、
   ⛔ 未改 `probe_WG9168_viol_full.py`、⛔ 未重產 baseline／凍存名單、
   ⛔ 未追改 `-161R`／`-163R`／`-169R`／`-172R` 之報告一字、⛔ 未動 KL 之工作副本、⛔ 未動 `data/`。
5. 🔒 **常規一四項自評齊備** ⇒ 依 `§三` 共通**逕行 push**。
   🛑 **`C:\kl173` ⛔ 不刪**（供 KL 實跑；其刪除指令已寫在操作單 ⑦）；工作 clone `C:\w175a` 跑畢刪除。
