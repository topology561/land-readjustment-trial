# W-G.8 D-2b-23【甲】commit A：**事前預測表** ＋ 街角資格否決原因 ＋ `ca8cf6c` 報告加註

**產出**：CC／2026-08-09　**分支**：`wip/s1-endpart`　**⛔ 生產碼零觸**
**實料**：`verify/out/probe_D2b23A_pre.log`（本批新產·⛔ 未覆寫任何既有 log）

> # 🛑 一件事須 KL 裁：**§3-3 之封閉式保留只複現了一半**
>
> - ✅ **`0m R4 右` 之保留<u>完全複現</u>**：差 **`+5.8404` ㎡**、實測為模型之 **`1.220` 倍**
>   ——與施工單「約 `5.8㎡`／`1.22` 倍」**逐位相符**。⇒ 亦據此判定 claude.ai 所用之
>   「街廓深」＝ **街廓面積 ÷ s 域跨距**（`D_ratio`），⛔ 非生產路徑之 `_depth`。
> - 🔴 **惟「其餘六處落在實測的 `0.92`–`1.02` 倍」<u>不成立</u>**：0m 之七處中
>   **五處在帶內、<u>兩處</u>在帶外**——除 `0m R4 右` 外，尚有 **`0m R5 左`**
>   （比值 `1.053`·模型**高出**實測 `3.8222` ㎡·**方向與 R4 相反**）。
>   兩種「街廓深」定義下皆在帶外（`1.053`／`1.094`）。
> - ⇒ 依施工單 §9-1（「無法複現 ⇒ 停機上呈」）**上呈**。
>   ⚠️ **CC 之判讀**：該保留之**核心**（R4 之殘量豁免）**成立且已複現**；不成立者為其**周邊敘述**。
>   本檔已將 `R5 左` **事前登記為第二項保留**（§四-2）⇒ ⛔ 事後不得補寫。

---

## 一、A-1：封閉式 `½·D²·tan(θ)` 之複現

### 1-1 量測定義（⛔ 每個選擇皆寫明·不藏於碼）

- **θ**：該側 `SIDE_LINE` × 該街廓 `ALLOC_cad` 之 `|cross|` → `arcsin`（判準同
  `verify/probes/probe_D2b7_dirscope.py`【A】）。
- **街廓深 D**：⚠️ 有**兩個所指**，二者皆算、⛔ 不擇一：
  `D_prod` ＝ 生產路徑實傳之 `_depth`（`avg_depth_default`）／
  `D_ratio` ＝ **街廓面積 ÷ s 域跨距**。
- **實測楔形**：缺口型取該側之 `宗·帶內` 分段；重疊型取該街角側之宗與其後各宗交集總和。
  側別由 s 定（`d_hat` ＝ FRONT `p1→p2`、`p1` 為左 ⇒ s 遞增即由左而右）。
- **比值** ＝ 模型 ÷ 實測（與施工單同向）。

### 1-2 結果（母體 ＝ **13** 列·⛔ 非空·【倉】`verify/out/probe_D2b23A_pre.log:10-23`）

| 情境 | 街廓 | 側 | θ(deg) | D_prod | D_ratio | 實測楔形 | 模型(D_ratio) | 比值 | 判 |
|---|---|---|---|---|---|---|---|---|---|
| 0m | R1 | 左 | `0.557951` | `33.1500` | `32.8662` | `5.3896` | `5.2596` | `0.976` | ✅ 帶內 |
| 0m | R1 | 右 | `0.690832` | `33.1500` | `32.8662` | `6.6671` | `6.5124` | `0.977` | ✅ |
| 0m | R2 | 左 | `4.321972` | `44.4700` | `43.7913` | `76.2616` | `72.4652` | `0.950` | ✅ |
| 0m | R4 | 左 | `2.713695` | `33.1000` | `32.5536` | `26.0938` | `25.1149` | `0.962` | ✅ |
| **0m** | **R4** | **右** | `2.867975` | `33.1000` | `32.5536` | `32.3854` | `26.5450` | **`0.820`** | 🔴 **帶外**（施工單已預告） |
| **0m** | **R5** | **左** | `4.321968` | `45.7100` | `44.8521` | `72.1964` | `76.0187` | **`1.053`** | 🔴 **帶外**（⛔ 施工單**未**預告） |
| 0m | R3 | 右 | `4.321958` | `44.3400` | `43.4572` | `76.0261` | `71.3636` | `0.939` | ✅ |
| 3.5m | R1 | 左／右 | 同 0m | 同 | 同 | `5.3881`／`6.6671` | 同 | `0.976`／`0.977` | ✅ |
| 3.5m | R4 | 左 | 同 0m | 同 | 同 | `26.0938` | `25.1149` | `0.962` | ✅ |
| **3.5m** | **R4** | **右** | 同 0m | 同 | 同 | `32.3854` | `26.5450` | **`0.820`** | 🔴 同上 |
| 0m／3.5m | R6 | 右 | `0.000003` | `45.5100` | `44.4470` | `0.0000` | `0.0000` | — | ⛔ **退化·不計入** |

**帶內／帶外計數**（【倉】同檔 `:27-34`）：`D_prod` 帶內 **8**／帶外 **3**；`D_ratio` 帶內 **8**／帶外 **3**
（帶外三列 ＝ `0m R4 右`、`3.5m R4 右`、`0m R5 左`；前二者為同一 (街廓,側) 之兩情境）。

### 1-3 判定

1. ✅ **R4 右之保留完全複現**（`D_ratio`：差 `+5.8404`、`1/0.820 = 1.220`）
   ⇒ ⛔ 我**未照抄**，係以自量之 θ／D／實測獨立算出後相符。
2. 🔴 **「六處在 `0.92`–`1.02`」不成立**：實為**五處**（0m 七處中）。第二個帶外者
   `0m R5 左` 之偏差**方向相反**（模型 > 實測）⇒ ⛔ 不可與 R4 併為同一現象。
3. ⚠️ **鑑別力**：兩種 D 定義使比值變動 `0.820 ↔ 0.847`（R4 右）、`1.053 ↔ 1.094`（R5 左）
   ⇒ **模型對 `0.92`–`1.02` 帶之判定<u>對 D 之定義敏感</u>** ⇒ ⛔ 該帶不得當精密判準用。

---

## 二、A-2：街角資格之否決原因（甲前基準）

### 2-1 實測（母體逐格 **非空**·【倉】`verify/out/probe_D2b23A_pre.log`【A-2】段）

三格之 `is_corner = 0` 皆因**同一道閘**：`_pk_one_side_v12`（`grep -n "def _pk_one_side_v12" app.py`）
之第一關 `cand_G < cand['min_area_to_apply']` ⇒ 全體候選 `達標=未達標`
⇒ `note = 🚫 無人達 G 值門檻 → 該端保留為抵費地`。

| 格 | 診斷 rows | 最佳候選 | 真G(㎡) | 門檻(㎡) | 短少 | 門檻之身分 |
|---|---|---|---|---|---|---|
| 3.5m R2 左 | 4（⛔ 非空） | `628-40(1)` | `286.39` | `308.16` | `21.77` | `範圍=門檻? ✅` |
| 3.5m R5 左 | 4（⛔ 非空） | `628-7(2)` | `236.28` | `299.69` | `63.41` | `範圍=門檻? ✅` |
| 3.5m R3 右 | 5（⛔ 非空） | `628-45(2)` | `195.17` | `307.75` | `112.58` | `範圍=門檻? ✅` |

⇒ **門檻 ＝ 街角最小規定範圍面積**（`K-9-5-4 ①` 之新構造·D-2 已切換）。

### 2-2 🔴 claude.ai 假說之處置：**實測不支持**（施工單 §4 已預留「量出來若否，如實列出即可」）

**假說**（施工單 §4·明標未實測）：否決係因楔形吃掉面積 ⇒ 資格被缺陷奪走 ⇒ 甲會還回去。

**不支持之理由（兩項·皆碼面可查）**：

1. **真G 之產生路徑不含第 2 宗**：真G 走 `_corner_first_lot_G → _solve_G_one → solve_G_binary`
   （`grep -n "K-9-5-4 ② 之唯一決定點" app.py`），其 `is_corner=True` ⇒ **已**走 ∥SIDELINE 幾何。
   甲所改者為**第 2 宗之近側界**，⛔ 不進入第 1 宗之 G 解算。
2. **門檻亦不受甲影響**：門檻 ＝ 街角規定範圍面積，由 `_build_corner_range_v3` 產出（`K-9-5-4 ①`），
   與第 2 宗界線無關。
3. **量級**：短少 `21.77`／`63.41`／`112.58` ㎡；縱使甲能動 G（實際不能），亦非該量級。

⇒ **事前預測**：`3.5m` R2／R5／R3 之**街角資格不翻轉**、`is_corner` 仍為 **0**、其幾何**逐字不變**。
⚠️ 此**推翻**施工單 §3-2 該列之「**可能翻轉**」。依 §4 逐字授權（「量出來若否，如實列出即可」）
於**實作前**登記，⛔ 非事後修改預測。

### 2-3 🔒 KL 之強化依據（KL 裁 2026-08-09 ①）—— **CC 已原樣現查、⛔ 非照抄**

| KL 之主張 | 現查指令與結果 | 判 |
|---|---|---|
| `_corner_first_lot_G`（`app.py:11833`）以 `W_prev=0.0`／`is_corner=True` 呼叫 `_solve_G_one` | `grep -n "def _corner_first_lot_G" app.py` ⇒ **`:11833`**；docstring `:11843` 逐字「`is_corner=True`、`W_prev=0.0`（第 1 宗）」；**實際呼叫** `grep -n "W_prev=0.0)" app.py` ⇒ **`:11922`** | ✅ **屬實** |
| `_build_corner_range_v3` 內唯一之「第 2 宗」字樣係**註解** | AST 取其行域 ⇒ **`app.py:10817-11047`**；域內含「第 2 宗」之行 ＝ **恰 1 行（`:10957`）**，該行以 `#` 起始 | ✅ **屬實** |

⇒ 🔑 **街角第 1 宗位於 W 鏈之<u>上游</u>（`W_prev=0.0` ＝ 鏈首）**。
**上游不受下游影響係<u>構造事實</u>**，⛔ 非「查了沒看到」——此較 §二-2 之兩項理由更強，
**已併入 §三 預測表之機制欄**。

### 2-4 ⚠️ 唯一殘餘條件（KL 裁 2026-08-09 ②·**須量·⛔ 不得推定**）

上游性只擋住「**經第 2 宗幾何回饋**」一條路。`_solve_G_one` 之實參尚含
**`S_max`／`A`／`B`／`C`／`tab6_burden`**；此五者若於甲前甲後**有變**，本預測即**不在射程內**。

⇒ **甲前基準已於本批捕捉**：`verify/probes/probe_D2b23A2_params.py`
→ 實料【倉】`verify/out/probe_D2b23A2_params_before.log`
（逐 (情境, 街廓, **路徑**) 收相異值集合 ＋ `sha256` 摘要；**資格路徑**與**實配路徑**分開記
——`3.5m` R2／R5／R3 之 `真G` 出自**資格路徑**，⛔ 不在 `run_step_g` 內）。

🔒 commit C 以**同一支探針**記甲後值逐格對拍：**相同** ⇒ 預測成立；
**不同** ⇒ 本預測**作廢**並依 §9-3 **停機上呈**。
⛔ KL 逐字：「逐街廓分跑下『應該相同』不算證據，**須有印出之值**。」

---

## 三、事前預測表（🔒 **commit A·先於實作**·⛔ 事後不得修改）

判定規則（施工單 §3-1）：有街角第 1 宗 ∧ 該側非 ∥ ⇒ 會變；有街角但 ∥（退化）⇒ 逐字不變；
無街角第 1 宗 ⇒ 不套用。

| # | 情境·街廓 | **現況**【倉】`probe_D2b22_attrib.log` | **預測** | **機制** | **若不符之處置** |
|---|---|---|---|---|---|
| 1 | 0m R1 | 缺口 `12.0568`（`:36-41`） | **歸零** | 兩側街角之帶內楔形被第 2 宗吸收（共用單線） | 停機上呈（§9-3） |
| 2 | 0m R2 | 重疊 `76.2616`（`:43-49`） | **歸零** | 左街角宗不再斜跨進宗1/2/3 | 同上 |
| 3 | 0m R4 | 缺口 `58.4792`（`:51-56`） | **歸零**；⚠️ 惟右側容有 `≈4.94`–`5.84` 殘量 ⇒ **保留一**（§四-1） | 同 #1 | 落於保留一之量 ⇒ **不判失敗**；逾之 ⇒ 停機 |
| 4 | 0m R6 | 缺口 `0`／重疊 `0`（`:58-61`） | **逐字不變** | 該側 ∥（`0.000003°`）⇒ 委派回單線 | **變即停機**（§9-2·⛔ 無例外） |
| 5 | 0m R5 | 重疊 `72.1964`（`:63-69`） | **歸零**；⚠️ 模型與實測差 `3.82` ⇒ **保留二**（§四-2） | 同 #2 | 落於保留二 ⇒ 不判失敗；逾之 ⇒ 停機 |
| 6 | 0m R3 | 缺口 `76.0262`（`:71-76`） | **歸零** | 右街角之帶內楔形 | 停機上呈 |
| 7 | 3.5m R1 | 缺口 `12.0553`（`:78-83`） | **歸零** | 同 #1 | 停機上呈 |
| 8 | **3.5m R2** | 缺口 `0`／重疊 `0`·`is_corner=0`（`:85-88`） | 🆕 **逐字不變**（⛔ **非**「可能翻轉」） | 🔒 **主依據（構造事實）**：街角第 1 宗於 W 鏈**上游**——`_corner_first_lot_G` 以 `W_prev=0.0`（鏈首）呼叫（`app.py:11922`）⇒ **上游不受下游影響**（§二-3）。**次依據**：真G 與門檻之產生路徑皆不含第 2 宗界線（§二-2）。⚠️ **射程**：以 §二-4 之五實參不變為條件 | **翻轉即停機上呈**（§9-3·與本預測不符） |
| 9 | 3.5m R4 | 缺口 `58.4792`（`:90-95`） | **歸零**；同保留一 | 同 #3 | 同 #3 |
| 10 | 3.5m R6 | `0`／`0`（`:97-100`） | **逐字不變** | 同 #4 | **變即停機** |
| 11 | **3.5m R5** | `0`／`0`·`is_corner=0`（`:102-105`） | 🆕 **逐字不變** | 同 #8 | 同 #8 |
| 12 | **3.5m R3** | 缺口 `0.0001`（退化帶）／重疊 `0`（`:107-110`） | 🆕 **逐字不變**（含該 `0.0001`） | 同 #8 | 同 #8 |

🔴 **具名作廢（KL 裁 2026-08-09 ③·⛔ 上文不刪·追記）**：施工單 §3-2 之
「`3.5m` R2／R5／R3 **可能翻轉**」一列，由 CC 於 commit A 之實測**推翻** ⇒ **具名作廢**，
以本表 #8／#11／#12 之「**逐字不變**」取代。

🔒 **驗收鍵 ＝ 側別 ＋ s 區間**（施工單 §7·⛔ 禁以宗序號為鍵）
——案由：同一條帶於 `[0m] R1` 標「宗2」、於 `[3.5m] R1` 標「宗1」
（同 s 區間、同 `6.6671`）【倉】`verify/out/probe_D2b22_attrib.log:38` 與 `:80`。

---

## 四、兩項事前保留（⛔ 事後不得補寫）

### 4-0 🔒 §3-3 更正本（claude.ai 2026-08-09 重發·**已取代原 §3-3**）

- **原「其餘六處落在 `0.92`–`1.02`」⇒ 具名作廢**，⛔ 原帶寬不得再被引用。
- 🔒 **本封閉式之地位（逐字要旨）**：**僅為機制之旁證**（「量級隨 `深²·tanθ` 走」）
  ——⛔ **不得寫入任何閘**、⛔ **不得列入 §7 驗收之任一項**、⛔ **不得作為停機條件之依據**。
  §7 驗收一律以**實測歸零與否 ＋ 側別＋s 區間之具名歸屬**為準。
- ⇒ 🔑 **施工單 §9-1 之停機事由於此解除**（該條所繫之保留已改為旁證）。

⚠️ **一項<u>未完全複現</u>（併記·⛔ 不隱藏）**：更正本稱 `0m R5 左` 之比值
「隨深度定義**反轉**（`0.914` ↔ `1.094`）」。CC 現查之兩種深度定義
**皆 > 1**：`D_prod` ⇒ **`1.094`**（✅ 與更正本相符）／`D_ratio` ⇒ **`1.053`**
【倉】`verify/out/probe_D2b23A_pre.log:18`。
⇒ 產生 `0.914`–`0.916` 之**第三種深度定義，CC 未能複現**（反解需 `D ≈ 41.79`，
而 CC 兩定義為 `45.7100`／`44.8521`）。
🔒 **不構成阻礙**：更正本已將本式降為旁證、非判準 ⇒ 依 §9 之「只有真觸及域邊界才停機」，
本項**如實記載即可**，⛔ 不停機。**保留二仍照登記**（其效力不繫於偏差方向）。

### 4-1 保留一（**KL 提出·CC 已複現**）：`0m／3.5m R4 右`

封閉式模型 `½·D²·tan θ` 於該處低於實測：`D_ratio` 下差 **`+5.8404` ㎡**（`1.220` 倍）／
`D_prod` 下差 **`+4.9418` ㎡**（`1.180` 倍）【倉】`verify/out/probe_D2b23A_pre.log:16`／`:23`／`:28-33`。
⇒ **甲後若 R4 右留下該量級殘量，屬本保留、⛔ 不得判為接線失敗**；
若一併歸零，則該偏差記為模型之深度代理誤差、已解釋。

### 4-2 🆕 保留二（**CC 於本批發現·施工單未載**）：`0m R5 左`

模型 **高於** 實測 `3.8222` ㎡（`D_ratio` 下比值 `1.053`）／`6.7579` ㎡（`D_prod` 下 `1.094`）
【倉】同檔 `:18`／`:29`／`:33`。**偏差方向與保留一相反** ⇒ ⛔ 不得與之併為同一現象。
⇒ 甲後若 R5 左之重疊未完全歸零而留下 `≤ 4` ㎡ 量級殘量，屬本保留範圍，須具名記錄；
逾之或方向相反 ⇒ 停機上呈。

### 4-3 ⚠️ 兩項保留之共同限制

封閉式模型**對「街廓深」之定義敏感**（§一-3-3）⇒ 其 `0.92`–`1.02` 帶
**只堪作量級檢查、⛔ 不得作為驗收判準**。**驗收判準仍為 §三 之「歸零／逐字不變」**。

---

## 五、施工單 §5：`ca8cf6c` 報告之加註（**已完成**）

加註已寫入 `docs/reports/W-G.8_D-2b-22_池側殘差逐界面全額歸因.md`
（卷首紅框下方 ＋ §3-2 尾）——逐字要旨：**殘差 `±0.000000` 係構造恆等式、⛔ 非檢定**，
只證切片鋪滿、不證宗別歸屬掛對；真對拍為 `|甲−乙|`（`0.0022`–`0.0350`）與逐段 `Δ` 欄。
⛔ **上文一字未刪**（`git diff --numstat` 之刪除欄可驗）。

---

## 六、異動清單

| 檔 | 異動 | 性質 |
|---|---|---|
| `verify/probes/probe_D2b23A_pre.py` | 新增（探針） | ⛔ 非生產碼 |
| `verify/out/probe_D2b23A_pre.log` | 新增（實料） | 實料 |
| `docs/reports/W-G.8_D-2b-23A_甲前預測表與否決原因.md` | 新增（本檔） | 文件 |
| `docs/reports/W-G.8_D-2b-22_池側殘差逐界面全額歸因.md` | **純加性**加註（§5） | 文件 |

⛔ `app.py`／`verify/stepg_pipeline.py`／`verify/wf_*.py` **零觸**。
⛔ 12 格全保真基準**未重烤**。
