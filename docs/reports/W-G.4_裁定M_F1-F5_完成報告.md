# W-G.4 裁定 M · F-1~F-5（P-F 前置修補）— 完成報告

> claude.ai 交辦五項（2026-07-25·全屬純技術）。基準 `63553d6`。
> 閘 b 已判「**預測錯、機制對**」⇒ M-5 機制未動；k*/E0 重錨准入 P-H。
> **措辭鐵律**：一律「forced 街角解鎖 → **面積回歸中央調配池** → 可及容量上升」；
> **禁「釋回／減少抵費地」**。

---

## 〇、兩份 run_all 摘要（claude.ai 異機對拍用）

| 模式 | 讀數 | log |
|---|---|---|
| **非 BAKE** | **53 PASS ／ 14 FAIL** | `verify/out/M_PF_runall_nobake.log` |
| **WV_BAKE** | **157 PASS ／ 1 FAIL**（僅 `k* 六塊經驗錨3.5m`·准入 P-H） | `verify/out/M_PF_runall_bake.log` |

非 BAKE 之 14 FAIL ＝ 3.5m baseline 全紅（**M-5 連動·待 P-H 重烤**）＋ F.0 raise 級聯；**0m 全綠**。

---

## 一、F-1 修「六格錨消失即靜默放行」（最高優先·**已咬到**）

**問題**：`wf_f0.py` 之 GSA 自檢**只走 `decisions`**，某 gid 若無合併決策（`len(lots)<2`）
其 `GSA_EXPECT` 鍵**永不被評估** ⇒ **沒人檢查 ≠ 相符**。M-5 後 `G007@3.5m` 正是此況。

**改法**：迴圈後加**覆蓋率硬閘**（`set(GSA_EXPECT[tag]) − set(gsa)` 非空 ⇒ loud raise；
WV_BAKE 下印 ⚠️），訊息列 gid ＋「本情境已無合併決策（回復單獨？被併宗遭上游消費移出？）」
＋「請確認係合法連動（如 M-5 提前合併）並於 P-H 重錨，或係上游漏配之 bug」＋已評估鍵清單。
另 `run_verification.py` 之 `_g0[k]` 裸 `KeyError` → 缺鍵 loud（可讀 FAIL 列）。

**實測（閘確實咬到）**：
```
⚠️ [WV_BAKE] 🔴 GSA 覆蓋率破（3.5m）：錨鍵 ['G007'] **本情境已無合併決策**…
   已評估鍵＝['G006', 'G009', 'G010', 'G014', 'G017']
```
⇒ 與 claude.ai 之觀察（「BAKE 只有 G014 一則⚠️、G007 一則都沒有」）**完全對上**：
G007 從「沒人檢查」變成「**顯式紅**」。0m 不誤報（0m 六鍵全被評估）。

---

## 二、F-2 M-5 三閘＋一帳（`_m5_by_tag` 原寫入後零讀取 → 已有消費者）

| 閘 | 判準 | 實測 |
|---|---|---|
| **M-5 定點閘3.5m** | 每個 award 端趟1 **確實不再 forced**；否則 loud raise（訊息含 blk/side/winner/趟0補足後真G/趟1真G） | ✅ PASS（1 award 端） |
| **M-5 無新生閘3.5m** | 趟1 forced 集合 **⊆** 趟0（現行僅一輪救援·新生端不會被救） | ✅ PASS |
| **M-5 結算閘3.5m** | Σ出資a′ == Σ注入a′（1e-6）＋**歸戶 a 總量守恆**（tol 0.02） | ✅ PASS（**Σ=270.00**；G007 a 2500.52→2500.52） |

> 📌 **定點閘之靶＝W-4 型 bug**：真 G 讀錯 `a` 源致 winner **假性維持 forced**。
> 該 bug 當時係靠 G014 錨**偶然**撞出；換個案子同型 bug 會全綠出貨。此閘使其**必響**。

**稽核帳**（`verify/out/got_M5合併帳_退縮3.5m.csv`·公務員稽核／P-H 歸因表引用）：

| 街廓端 | winner | 門檻 | ①補足a′ | ③餘額a′ | 源宗 | 出資a | a′ | 階段 | 合格候選 | 丟棄規劃 |
|---|---|---|---|---|---|---|---|---|---|---|
| R3right | 628-45(2) | 308.93 | 179.32 | 90.68 | 628-30(2) | 150.30 | 150.30 | ①補足 | 1 | 0 |
| R3right | 628-45(2) | 308.93 | 179.32 | 90.68 | 628-45(1) | 29.02 | 29.02 | ①補足 | 1 | 0 |
| R3right | 628-45(2) | 308.93 | 179.32 | 90.68 | 628-45(1) | 63.69 | 63.69 | ③餘額回街角 | 1 | 0 |
| R3right | 628-45(2) | 308.93 | 179.32 | 90.68 | 628-30(1) | 26.99 | 26.99 | ③餘額回街角 | 1 | 0 |

---

## 三、F-3 `E0_EXPECT` 改 per-tag（＋E2_NAMED 覆核）

`wf_f4.py:60` → `{"0m": 三對, "3.5m": 兩對（去 `628-30(2)→628-45(2)`）}`；`:355`／訊息取 `[tag]`。
**註解載明**：3.5m 少一對係 **M-5 已於 PK 階段提前完成同一對合併**
（`628-30(2)` 為 M-5 ① 之出資源、`628-45(2)` 為街角 winner／受贈 target——**原就是 E0 之既有配對**）
⇒ **階段位移（E0→PK）·非行為改變**：同一對地仍合併、僅時點提前。0m 無 award ⇒ 三對不變。

**E2_NAMED 覆核（grep 坐實·claude.ai 指定）**：其 5 宗
`{628-29(1), 628-46(1), 628-50(1), 628-51(1), 628-52(1)}` 與 M-5 消費集
`{628-30(2), 628-45(1), 628-30(1)}` **交集為空** ⇒ **不需 per-tag**（與 claude.ai 判斷一致）。

---

## 四、F-4 清反向註解（失敗考古 #26-3）

`app.py:15093-15094` 原寫「組裝失敗 ⇒ loud st.warning ＋ 退 estG（Q1 中性·零翻盤⇒winner 不變）」，
與 D-1 實碼（`:15158 st.error`／`:15164 st.stop()`）**直接矛盾** ⇒ 改為
「**st.error＋st.stop() 硬停**——**無 estG 降級路徑**（Q1：取不到真 G ＝loud·禁 fallback）」。
全倉 grep「退 estG／st.warning／降級」：其餘命中皆為**歷史記述**（「**前版** st.warning→退 estG」
「**禁**退 estG」），非描述現行路徑 ⇒ 保留。

---

## 五、F-5 名目集復原之準備（P-H plan 用·不改機制）

**名目集對照**（`M_P0c_main_verify.log` vs 現況）：

| 基準 | 名目數 | 相對 P-0c |
|---|---|---|
| **P-0c**（S1 重烤後） | **131** | — |
| **現況·非 BAKE** | 66 | **缺 72**（F.0 raise 級聯掐掉）⇒ 「不新增名目」紀律**現無法評估** |
| **現況·WV_BAKE** | **157** | **缺 2 ／ 新增 28** |

**缺 2**＝`W-F F.4`／`W-G G.2`——**非消失，而是「粗名目 → 細名目」**：
P-0c 時 F.4 於 E2 不可行處 raise，只報一個粗 FAIL；M-5 解 E2 後 **F.4 跑到底**，
改報其**28 個內部細閘**（`F.4 E2 窮舉最優性{0m,3.5m}`／`F.4 池終態三則{…}`／`F.4 Q3＋裁示1`／
`F.4 E0 級1殘餘三對錨`／`F.4 ½<½錨…`／`F.4 模式二 p_avg 母體…` 等）。
⇒ **28 新名目係「F.4 跑通後解鎖之既有閘」、非新增檢查項**；G.2 同理（隨 F.4 產出而恢復）。

**⇒ P-H plan 須明列**：
1. 重烤後**目標名目數 ≈ 157**（131 − 2 粗名目 + 28 解鎖細名目 ＝ 157）；
2. 逐項歸因表：3.5m baseline 全紅之每一格 → **M-5**（與 P-0c 之 **S1** 嚴格分列）；
3. `k* 3.5m`（R2 8→7／R5 7→5·M-5 移除 R2/R5 源宗致宗數變）＋ `GSA G007 鍵`（刪鍵或改值）
   ＋ `E0_EXPECT` 已於 F-3 先行 per-tag ⇒ P-H 不重複。

**G.2 紅字改善**：`_f0["0m"]` 之裸 `TypeError: 'NoneType' object is not subscriptable`
→ 上游世代缺件時 raise **有意義訊息**（列出未產出之世代＋「G.2 係純呈現層契約檢·
其資料前提為各代皆成功；上列世代失敗 ⇒ G.2 **無從檢查**、非 G.2 自身破」）。

---

## 六、誠實更正（claude.ai 指出·我原述有誤）

前報告稱 R3 左組「**byte-unchanged／逐位全等**」——**不成立**。實測
`628-47(1)`／`628-48(1)`／`628-49(2)` 之**幾何面積各差 0.01㎡**、`628-47(1)` **累積S 差 0.01m**。
正確表述為 **「左組 G 零動」**（G 欄逐宗不變），非全欄逐位全等。
我原僅比對 G 欄即推及全列，係**過度宣稱**；已於本報告全面改用「G 零動」。

---

## 七、泛用四約束自查（F-1/F-2 新碼）

| 約束 | 自查 |
|---|---|
| 禁塊名 | F-1 走 `set(GSA_EXPECT[tag])`／F-2 走 `_m5_plan["awards"]` 之 `blk`/`side` 變數·**無 R3/R2 字面** |
| 禁 gid | 訊息中 gid 皆**執行期值**（`_uncov`／`_gid_viol`）·**無 G007 硬編** |
| 禁常數 | 容差 `1e-6`（結算）／`0.02`（歸戶 a）明列；門檻/MinA 全現算 |
| 換案成立 | 三閘皆由 `awards`／`forced_map` 現算驅動·空 award ⇒ 三閘不掛列（0m 即此況） |

## 八、次步
P-F（M-4 交互驗證·實測非 prose）→ P-G（容量拆解重跑·措辭依鐵律）→ P-H（重烤·歸因 M-5／S1 分列）。
