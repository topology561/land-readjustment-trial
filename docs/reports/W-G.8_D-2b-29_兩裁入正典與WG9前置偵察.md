# W-G.8／**D-2b-29**：兩裁入正典 ＋ 三處加註 ＋ 考古增節 ＋ `W-G.9` 前置偵察（⛔ 全單零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**起點**：`5973a4a`
**本單實際編號 ＝ `D-2b-29`**（`grep -rn "D-2b-29" . --exclude-dir=worktrees` ⇒ **0 命中**·未占用）

> # 🔴 最先讀
>
> **§1／§2／§3 已落地。§4 三問已<u>做完</u>，但依 §4-4-3 與 §4-1 <u>停機上呈</u>。**
> 停機二由：**(甲)** `Q2` 之「二端 ↔【裁】5／6 第 1 種／第 2 種」之對應係
> **【裁】5／6 之幾何細節** ⇒ §4-4-3 明令停機；
> **(乙)** `Q2` 之**第 3 項預測（幾何指認不可判定）被推翻** ⇒ §4-1 明令「預測與結果不符 ⇒ 停機上呈」。
> ⚠️ **`Q1`／`Q3` 及 `Q2` 之前二項預測<u>全部相符</u>**，⛔ 非全案退回。
>
> 🔴 **另有一項倉態發現（非本單所交辦·具名上呈）**：
> `GB-14` 列所引之結案錨 `grep -n "_k97 = k97_solve_alloc_t" app.py` **現為 0 命中**（已失效）。
> ⛔ 本批**未修**（§4 為只查不改·§2 亦未授權動 `GB-14`）。

---

## §0 動手前（⛔ 未憑本單轉抄）

| 項 | 現查 |
|---|---|
| 編號 `D-2b-29` | **0 命中** ⇒ 未占用、取用 |
| `HEAD` | `5973a4a`（＝ `origin/wip/s1-endpart`·工作樹乾淨） |
| 零生產碼 | `git diff --stat -- app.py verify/stepg_pipeline.py verify/selection_pipeline.py verify/run_verification.py verify/wf_f*.py` ⇒ **空**（全單） |

---

## §1 ✅ commit A：`K-9-5-10` 兩裁入正典（`7b43d64`）

**落點**：`docs/rulings/K-6_街角地分配程序與可分配判準.md`，以
`grep -n "^### 🔒 K-9-5-9" 該檔` 定位後置於**該節全文之後**（該節延伸至原檔尾 ⇒ 實為檔尾附加）。
⛔ `K-9-5-9` **一字未動**（`--numstat` 刪除欄 0 為其構造性證明）。

### 1-2 §1-2 四點現查依據（**全部相符**·任一不符即停機·未發生）

| # | 命題 | 現查結果 |
|---|---|---|
| 1 | `R1` 之中止因為**結構閘**、⛔ 非抵費地計算失敗 | ✅ `grep -c "結構閘 理論＝實跑 破" app.py verify/stepg_pipeline.py` ⇒ `app.py` **0**／`stepg` **2**；`grep -c "抵費地計算失敗" verify/out/D2b24_runall.log` ⇒ **0**；該 log 之中止因**全部**為結構閘（`:56`／`:196`／`:240`／`:255`／`:257`／`:260`） |
| 2 | 該 log 即**最新碼態**所產 | ✅ `git log --format=%h 78079fa..HEAD -- app.py verify/stepg_pipeline.py` ⇒ **0 行** |
| 3 | 那 7 份之產出鏈**遞移可達**結構閘之實跑側算子 | ✅ **跳數 3**（見下 1-3） |
| 4 | 「卡點」於正典**無先前定義** | ✅ `grep -rn "卡點" docs/rulings/` ⇒ **僅 1 命中**＝`K-9-5-9` 加註 (二) 本身 |

### 1-3 §1-2 #3 之遞移閉包（⛔ 未只做一層）

探針 `verify/probes/probe_D2b29_reach_closure.py`／log `verify/out/probe_D2b29_reach_closure.log`。

**射程宣告**：種子 ＝ `verify/selection_pipeline.py` 之**全部** `ns["…"]` 取名（**AST 取**·14 個）；
圖 ＝ `app.py` **全部** FunctionDef，邊涵蓋 `func.id`／`func.attr`／`ns["…"](…)` **三形式**；
每個 `Call` 歸**最內層**封閉 FunctionDef；BFS 至不動點。

- **結果**：`solve_G_binary` 與 `iterate_G_S` **皆可達·跳數 3**
  路徑 ＝ `_corner_block_true_G → _corner_first_lot_G → _solve_G_one → solve_G_binary`／`iterate_G_S`
- 🔴 **一層對照為「否」**（同一探針內建）⇒ 若只做一層即下「零交集」之結論，
  **將與遞移閉包之結果相反**——非程度之差。
- **自我驗證閘**：② 目標名於 `app.py` 皆有定義（`:9580`／`:6312`）；
  ③ 差集自證：閉包 154 ＝ 有定義 51 ＋ 外部名 103，**差 0**。

🔒 **此即【裁】2「時點與【裁】1 同」之碼面依據**：該 7 份既遞移可達結構閘之實跑側算子
⇒ **結構閘未修之前重跑，所得將再度過期**——縱採丙案（KL 以 UI 重跑）亦然。

---

## §2 ✅ commit A 同批：三處加註（⛔ 原句一律不刪·純附加）

### 2-1／2-2 🔴 **施工單所稱之「現載」句，倉內查無**

施工單 §2-1／§2-2 稱兩檔現載「`GB-54`：R1 卡點。它同時是 `D-3` 之前置」。**現查**：

```
$ grep -rn "卡點" . --exclude-dir=worktrees --exclude-dir=.git
docs/reports/W-G.8_D-2b-28_未裁項結清與考古節59增補.md:23,103,195
docs/reports/W-G.8_D-2b-29_WG9前置偵察_事前登記.md:15
docs/rulings/K-6_街角地分配程序與可分配判準.md:1609
⇒ 5 命中，全部出自 D-2b-28 之報告與 K-9-5-9 加註 (二) 本身
$ grep -n "GB-54" docs/reports/W-G.4_泛用阻塞項登記表.md | grep -c "前置"                    ⇒ 0
$ grep -n "GB-54" docs/reports/W-G.8_D-2b波_新session交接文_D2b21-27.md | grep -c "前置"   ⇒ 0
```

⇒ **無錯句可更正** ⇒ 二處均寫為**「補記事實」**、⛔ **未寫成「更正既有錯句」**
（依 `CLAUDE.md`「⛔ 禁編造、禁以倉內近似物代替」）。

**所補之事實**（§1-2 #1 現查）：`GB-54` 之觸發點 `抵費地計算失敗` **現行未發動**（log 命中 0）；
`R1` 之實際中止因為結構閘 ⇒ **單修 `GB-54` 不使 `StepG` 抵達 `R2`–`R6`**。
🔒 **`GB-54` 本身仍為真阻塞項、⛔ 未撤銷**——只是**它不是 R1 停在 Gate C 的原因**。

⚠️ **2-2 之技術處置**：`GB-54` 列在長表中段，**改該列會產生刪除**（違 §5-1）
⇒ 改**插入一列**，且首格刻意**不匹配** `^| \*\*GB-`
⇒ 既有錨 `grep -c "^| \*\*GB-" 該檔` **仍為 57**（加註前後同值·已現查）。

### 2-3 ✅ `W-G.9` §二之加註（⛔ 未改寫 §2-1 之判讀一字）

`grep -n "w_lo = min" app.py` ⇒ **全檔僅 1 命中**＝`:11031`，且**係 `#` 註解**。
其自載逐字：「⛔ **舊式已移除** …… 即 **`GB-14` ①②** 所登記之二誤」。
`GB-14` 現況 ✅ **已結案**（`K-6-A2 段三(c)`·2026-08-04）。
⇒ 加註記明：**§2-1 之判讀係建立在已移除之舊式上、⛔ 坐實前不得引用**；
活碼之對應結構指向本報告 §4。🔒 **判讀之存廢屬 KL**，本批只加事實。

---

## §3 ✅ commit B：失敗考古**增節 60**（`add79f3`）

節號**以現查為準**：`grep -n "^## [0-9]" 該檔 | tail -1` ⇒ 既有最末 **59** ⇒ 新增為 **60**。
⛔ 既有各節之標題與內文**一字未動**。

節 60 ＝「**否定性結構宣稱，以射程小於命題之工具作成**」（claude.ai 犯·同一 session 三例·皆自糾）。
⛔ 與 `59-9` 不同族：`59-9` 錯在**解析器壞掉**；本節錯在**解析器沒壞、但射程比命題小**。

**CC 已獨立復現其例 2**（⛔ 非轉抄）——見 §1-3：同一探針之**一層對照為「否」、遞移閉包為「可達」**。

---

## §4 🛑 commit C：`W-G.9` 前置偵察三問（做完·**停機上呈**）

**事前登記**：`4943910`（`docs/reports/W-G.8_D-2b-29_WG9前置偵察_事前登記.md`）
——⛔ **於任何 `w_lo`／`_W`／`k97_solve_alloc_t`／`app.py:11031` 查詢之前**入庫。
**機械時序證明**見 §5-2。

探針 `verify/probes/probe_D2b29_wg9_q1q3.py`／log `verify/out/probe_D2b29_wg9_q1q3.log`。

### 4-0 射程與閘（依本批新立之節 60 自檢·⛔ 立戒者次批須自證）

- 母體 ＝ 倉內全部 `*.py` **108 檔**（⛔ 已排除 `.claude/worktrees/`）。
- 🔒 **閘⓪ 母體非空 ＋ 生產碼全在母體內**：`108 > 0`、生產碼 9 支中不在母體者 **0**。
- 🩸 **本探針首版當場自糾**：首版以**絕對路徑**判排除巢狀 worktree，
  而本 session 之 `ROOT` **自身**即位於 `…/.claude/worktrees/<name>/`
  ⇒ **每個目錄都命中該式** ⇒ 母體 **0** 檔、三問全部印出漂亮的「0」。
  ⇒ 係 `D-2b-2` 已登記之「**空真假綠（母體＝0）**」之再現，且與**節 60** 同族。
  🔒 已改為**相對 `ROOT`** 判斷並加閘⓪ fail-fast（`rc=2`）。
  ⚠️ **此即 `CLAUDE.md`「立戒者在下一批最容易違反自己的戒」之當批實例**——
  節 60 於 commit B 落地，**同一 session 之次一支探針即犯之**。

### 4-1 `Q1`　`w_lo = min(_W(0,0), _W(D,0))` 是否存在為**活碼**

**答：❌ 否**——於 `_build_corner_range_v3` 內**活碼命中 0**。

- 文字命中 **11**；AST **活碼命中 3 行**（`app.py:5913`／`:5915`／`:5919`），
  **全部**位於 **`_compute_block_depth_alloc`**，其式為 `w_lo = max(min(...), min(...))`
  ⇒ **同名異義**、與 `_W` **無關**。
- `app.py:11031` 經三分歸為 **`#` 註解**。
- **差集自證**：AST 3 ＋ 註解 2 ＋ 字串常數 6 ＝ **11 ＝ 文字命中**，**殘差 0**。

### 4-2 `Q2`　活之「min 取二端」結構位於哪些函式；其二端幾何各是什麼線

**活之 `min(_W(...), _W(...))` 共 3 處，<u>全部</u>位於 `k97_solve_alloc_t`**（`app.py`）。
`_W` 之定義亦在該函式內（`:10545`）：`_W(d, t) = σ·(_s_alloc(d,t) − _s_side(d))`
——`d` ＝ **自 FRONT_LINE 沿 BASELINE 法向之深度**，`t` ＝ ALLOC_LINE 之平移參數。

| 落點 | 二端之 `d` | **二端之幾何**（碼註自載） | 碼註自稱 |
|---|---|---|---|
| `min(_W(0.0, t_star), _W(_D_at, t_star))` | `0` ／ `_D_at` | `d=0` ⇒ **前緣線 FRONT_LINE**；`_D_at = D0 − max(0, m·t*)`，而 `D0` 碼註逐字「**＝ Q0 至 BASELINE 之垂距**」⇒ 該端抵 **BASELINE** | **第一層**之後置斷言（`K-9-5-2 ③`）·未自稱甲／乙 |
| `min(_W(_l2['d_top'], t_star), _W(_l2['d_bot'], t_star))` | `d_top` ／ `d_bot` | **帶之二端**（`d = δ_P` 與 `d = δ_P + D_new`） | 🆕 **乙式**；碼註逐字「⛔ **不再是** `d = 0` 與 `d = D_new`（**甲式·已作廢**）」 |
| `min(_W(0.0, 0.0), _W(_Db, 0.0))` | `0` ／ `_Db` | `d=0` ⇒ **前緣線**；`_Db = block_depth` ＝ **街廓平均深度** | **舊式**——碼註逐字「**舊式**之 `t*`（**只供段三(b) 對照·同源同框**）」 |

🔴 **關鍵對位**：`W-G.9 §2-1` 所引之解析捷徑
`w_lo = min(_W(0,0), _W(D,0)); t_star = (T − w_lo)/k`
與**第三列**（`_t_legacy`·`app.py:10858`）**逐字同構**
——即該判讀所據者，**正是碼面自稱之「舊式」**，其帶深 `block_depth` ＝ 街廓平均深度
**恰為 `GB-14 ①` 所登記之誤**。

⚠️ **且活碼之分支數與 §2-1 之分類數不合**：`k97_solve_alloc_t` 為**三分支**
（① `k≥0`：深度不影響、min 恆在 `d=0`；② `k<0 ∧ m·t≤0`：min 在 `Q0`；
③ `k<0 ∧ m·t>0`：min 在 `P_front(t)`——皆碼註自載），
而 `§2-1` 之表為**二分類**。**分支 ③ 於該表中無對應項。**

🛑 **停機（§4-4-3）**：上開「二端 ↔【裁】5 第 1 種／【裁】6 第 2 種」之**對應判別**
係**【裁】5／6 之幾何細節** ⇒ ⛔ **本批不作此判定、不代為另立判讀**。

### 4-3 `Q3`　`k97_solve_alloc_t` 之**生產呼叫數**

**答：0。**

- 文字命中 **38**；三形式窮舉（`func.id`／`func.attr`／`ns["…"](…)`）得**全倉呼叫 7 處**，
  **全部**位於 `verify/probes/*.py`（量測件）、**全部**經 `ns["…"]` 間接形式。
- **生產碼內呼叫 ＝ 0**（生產碼定義 ＝ `app.py` ＋ `stepg_pipeline` ＋ `selection_pipeline`
  ＋ `run_verification` ＋ `wf_f0`〜`wf_f4`；⛔ `probes`／`fixtures` 為量測件不計）。
- **差集自證**：AST 活節點 8 ＋ `#` 註解 ＋ 字串常數 ＝ 38 ＝ 文字命中，**殘差 0**。
- **碼面成因**（`app.py` 註解逐字）：`K-6-A2 D-2`（2026-08-08）令
  「⛔ **舊式（`k97_solve_alloc_t` → `t_star` → `L(t*)`）於本函式整段移除**·不留 fallback」，
  而「🔒 `k97_solve_alloc_t` **本身不刪、不停用、不標作廢**（D 波範圍界線 #5）」。

🔴 **附帶發現（非交辦·具名上呈·本批未修）**：`GB-14` 列所引之結案錨
`grep -n "_k97 = k97_solve_alloc_t" app.py` ⇒ **0 命中**（`rc=1`）——**該錨已失效**，
成因即上開 `D-2` 之整段移除。⚠️ 屬 `CLAUDE.md`「**行號／錨在 wip 支是易腐資產**」之實例。

### 4-4 🔴 預測對帳（⛔ 未靜默改預測）

| 題 | 登記之預測 | 結果 | 判 |
|---|---|---|---|
| `Q1` | 有文字命中，但於 `_build_corner_range_v3` 內**非活碼** | 文字 11／該函式內活碼 **0** | ✅ **相符** |
| `Q2`-1 | 活結構之主要落點為 `k97_solve_alloc_t` | **3 處全部**在該函式 | ✅ **相符** |
| `Q2`-2 | 另有**同名異義**之 `w_lo` | `_compute_block_depth_alloc` 之 `w_lo = max(min…)` | ✅ **相符** |
| `Q2`-3 | 二端之**幾何指認為「不可判定」**（三張示意圖未入倉） | 🔴 **被推翻**——**碼註自載**足以定二端幾何（見 4-2 表） | ❌ **不符** |
| `Q3` | 生產呼叫數 ＝ **0** | **0**（7 處全在 probes·全走 `ns["…"]`） | ✅ **相符** |

🛑 **`Q2`-3 不符 ⇒ 依 §4-1「預測與結果不符 ⇒ 停機上呈」一併上呈。**
🔒 **登記原文已載其為「好的推翻，須據實改答」**，本報告即據實改答（4-2 表），
⛔ **未回頭修改登記檔一字**（登記 commit `4943910` 早於本結果·時序證明見 §5-2）。

⚠️ **仍屬不可判定者（⛔ 具名·不猜）**：**二端幾何 ↔【裁】5／6 第 1 種／第 2 種之對應**。
**缺件**＝ KL 2026-08-10 之**三張示意圖**（第 1 種分配線方向兩版、第 2 種）
——依 `K-9-5-9` 節末之 🛑 段，該三圖**未入倉**（成因：該批訊息未附任何圖檔）。
⛔ **不得以倉內近似圖代替。**

---

## §5 全單驗收（⛔ 原樣貼）

### 5-1 `git diff --numstat 5973a4a`（刪除欄全 **0**；`.py` 僅出現於 `verify/probes/`）

```
48	0	.claude/skills/failure-archaeology/SKILL.md
19	0	docs/W-G.9_第2宗最小規定範圍_範圍界定.md
1	0	docs/reports/W-G.4_泛用阻塞項登記表.md
69	0	docs/reports/W-G.8_D-2b-29_WG9前置偵察_事前登記.md
19	0	docs/reports/W-G.8_D-2b波_新session交接文_D2b21-27.md
37	0	docs/rulings/K-6_街角地分配程序與可分配判準.md
41	0	verify/out/probe_D2b29_reach_closure.log
188	0	verify/probes/probe_D2b29_reach_closure.py
```
（commit C 另加 `verify/probes/probe_D2b29_wg9_q1q3.py`＋`verify/out/probe_D2b29_wg9_q1q3.log`
＋本報告；⛔ **無任何 `.py` 落在 `verify/probes/` 以外**。）

### 5-2 commit 分立 ＋ **機械時序證明**

```
$ git log --oneline 5973a4a..HEAD
add79f3 D-2b-29 commit B【零碼】失敗考古增節 60…
7b43d64 D-2b-29 commit A【零碼】K-9-5-10 兩裁入正典 ＋ 三處加註…
4943910 D-2b-29 §4-1 事前登記：W-G.9 前置偵察 Q1〜Q3 之可證偽預測…
```
時序證明（登記 → 結果）之指令與輸出見本報告 commit（C）之提交訊息與 §7。

### 5-3 `grep` 自癒（原文與加註**同時**命中）

| 落點 | 指令 |
|---|---|
| `K-9-5-10` 兩裁 | `grep -n "^### 🔒 K-9-5-9\|^### 🔒 K-9-5-10 " docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| 交接文加註 | `grep -n "GB-54\` 之修法方向已於\|【\`D-2b-29\` §2-1 加註" docs/reports/W-G.8_D-2b波_新session交接文_D2b21-27.md` |
| 登記表加註 | `grep -n "^| \*\*GB-54\*\*\|^| ↳ 🔒 \*\*加註" docs/reports/W-G.4_泛用阻塞項登記表.md` |
| `W-G.9` 加註 | `grep -n "w_lo = min\|§2-3 加註" docs/W-G.9_第2宗最小規定範圍_範圍界定.md` |
| 考古節 60 | `grep -n "^## 59\.\|^## 60\." .claude/skills/failure-archaeology/SKILL.md` |

### 5-4 `env | grep WV_BAKE` ⇒ **空**（`rc=1`·未設）

### 5-5 報告所引之 log **皆已入倉**

`verify/out/probe_D2b29_reach_closure.log`（commit 登記）／
`verify/out/probe_D2b29_wg9_q1q3.log`（commit C）。

### 5-6 §4-5 不授權之逐項自證

| 項 | 狀態 |
|---|---|
| ⛔ 不接線 `k97_solve_alloc_t` | ✅ 生產呼叫數**仍為 0**（本批未加任何呼叫；`git diff --stat -- app.py` 空） |
| ⛔ 不加入 `_WF_NS_NAMES` | ✅ `git diff --stat -- app.py verify/` ⇒ 僅 `verify/probes/`＋`verify/out/` 新檔 |
| ⛔ 不動 `app.py` 任何一行 | ✅ `git diff --stat 5973a4a -- app.py` ⇒ 空 |
| ⛔ 不設 `WV_BAKE`／不改錨值／不放寬閘 | ✅ `env` 空；`--numstat` 無 `verify/baselines/` 列 |
| ⛔ 不刪不停用不標作廢任何既有機具 | ✅ 刪除欄全 0（構造性證明） |
| ⛔ 不於本波實作第 2 宗最小規定範圍 | ✅ 本批僅文件加註＋兩支唯讀探針 |

---

## §6 次步

1. 🛑 **KL 裁 §4-2 之「二端 ↔【裁】5／6」對應**——前置＝**三張示意圖入倉**（§4-4 具名缺件）。
2. 🛑 **`W-G.9 §2-1` 判讀之存廢**（其所據為碼面自稱之「舊式」`_t_legacy`；且活碼為**三分支**、
   該表為**二分類**，**分支 ③ 無對應項**）——⛔ 屬 KL，本批未改一字。
3. **`GB-14` 之結案錨已失效**（`_k97 = k97_solve_alloc_t` ⇒ 0 命中）⇒ 建議另批具名更正。
4. ⛔ **`D-3` 仍受 `K-9-5-10`【裁】1 拘束**：等該另案（結構閘 理論＝實跑）修好之後才做；
   已過期之頂層 7 份依【裁】2 採**丙案（KL 以 UI 重跑）**、時點同。
