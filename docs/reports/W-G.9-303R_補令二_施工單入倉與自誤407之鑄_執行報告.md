# `W-G.9-303R`　補令二執行報告：`W-G.9-303` 施工單之入倉 ／ `自誤 407` 之鑄

> **受詞** ＝ `docs/orders/W-G.9-303_補令二_新窗首則與施工單之入倉.md`（**新**施工窗之**首則**）。
> **分級** ＝ **輕**（入倉 ＋ 鑄一號·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**）。
> **鑄號** ＝ `自誤 407`（**一號**·逐字由單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**。
> 🔒 **檔名之提案（`常規三`·⛔ 土地後果）**：本報告取 `W-G.9-303R_補令二_…_執行報告.md`，
> **⛔ 逕取 `W-G.9-303R_…`** ——蓋 `W-G.9-303` 施工單本身之報告尚未產出，其號須留予該批；
> 候發單側核可。候選二 ＝ `W-G.9-303R2_…`；候選三 ＝ `W-G.9-303_補令二R_…`。

---

## `§一`　開工閘（**每列同格載其產生指令**·`自誤 392`）

| # | 受詞（指令逐字） | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `3e9cf286…` 或其後裔 | `3e9cf2864bec1d1b08512a5ea0b26ddd86421935	refs/heads/wip/s1-endpart`（`rc` ＝ `0`） | ✅ |
| `2` | `git rev-parse HEAD:app.py`（**框 ＝ blob `sha1`**） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`） | `rc=1` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本補令>` | `P-5` 相符·`P-3` 不紅 | `rc=0`；`P-5` ✅ **相符**（受詞 `18973` B·實算 `8346fc82766e85cb…`／單載 `8346fc82766e85cb…`）；`P-3` ＝ `app.py` 之 `def main` ＝ `14975`-`24579`（**不紅**）；🟡 `1` 項（見 `§一-1`） | ✅ |
| `4'` | 受詞自證（坑 `bf`·**機械**·框 ＝ 器所印之標頭列） | [必真]／[必偽] | 標頭逐字 ＝ `【order pre-flight】W-G.9-303_補令二_新窗首則與施工單之入倉.md（19051 B／205 列）`；[必真] 含 `補令二` ＝ **`1`**／[必偽] 含 `補令一` ＝ **`0`** | ✅ |
| `5` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（**器之輸出為正典**） | 自誤 `396`／`406`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | 自誤 `{'相異': 396, 'MAX': 406, 'MIN': 7, '缺號': [106, 355, 356, 357]}`／`GB` `{'相異': 168, 'MAX': 170}`／`VR` `{'相異': 80, 'MAX': 95}`／`K-9` `{'相異': 28, 'MAX': 29}` | ✅ |
| `6` | `python verify/probes/probe_WG9269_pit_index.py`（**等待綁行程結束**·⛔ `\| tail -N`） | `89`／`89`·末 `bk` ⇒ 下一 `bl` | 資料列 **`89`**（框 ＝ `grep -c "✅ 可解析 |"`）／三綠 **`89`**（框 ＝ `grep -c "✅✅✅"`）／不可解析 **`0`**；末標籤 **`bk`** ⇒ 下一 **`bl`**；母體 ＝ HEAD 之追蹤 `.md` **`807`** 檔；器之判別力二造 ＝ 造甲 `坑 n` 定義處 `6` ≥ 1 ✅／造乙 人造坑 `0`／`0` ✅ ⇒ **器非紅** | ✅ |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔 | **`34`**；判別力[必不命中] 子層（`git ls-tree -r`）＝ **`349`** > `0` ✅ | ✅ |
| `8` | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`（**三態分列**） | **態2**（`rc ≠ 0`·⛔ 在倉） | 見 `§一-2` | ✅ |

### `§一-1`　`P-4` 之逐項處置（**⛔ 靜默略過**·`1` 項）

| 項 | 逐字 | 所屬豁免類 | 處置 |
|---|---|---|---|
| `[P-4] :188` | `` `§一` 開工閘（**每列同格載其產生指令**·閘 `8` **三態分列**）→ `§四` 工項零 → `§二` 工項一（**四數之釘死逐項出艙**）→ `` | **(甲)** `→` 係**流程節序之串接**（報告之節序）·兼 **(乙)** 序之詞 | 🔒 **受具名豁免**——其 `→` 所串者為**本報告之節次先後**，**⛔ 幾何向量之方向性轉引** ⇒ **無座標系可具名** |

🔒 **⛔ 不在三類內者為 `0` 項** ⇒ 無須停機。
🔒 **`P-1`／`P-2`／`P-6` 各 `0` 項**（器之輸出逐字：「🟡 提示·須逐項處置：**1** 項」，該 `1` 項即上列）。

### `§一-2`　閘 `8` 之**三態分列**

| 受詞 | 指令（逐字） | `rc` | 態 | 判 |
|---|---|---|---|---|
| **受詢**：`W-G.9-303` 施工單 | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` | `128` | **態2**（⛔ 在倉） | ✅ **期值相符** ⇒ 本批**確有受詞** |
| **判別力[必存在]** | 同指令對 `docs/orders/W-G.9-303_補令一_舊窗收工批.md` | `0` | **態1**（在倉） | ✅ ⇒ **器非紅** |

🔒 **併證（`自誤 407` 徵候之獨立復現）**：`git ls-files` ⋂ 字樣 `W-G.9-303` 之命中 ＝ **`2`**，
逐筆 ＝ `docs/orders/W-G.9-303_補令一_舊窗收工批.md`／`docs/reports/W-G.9-303_交接註_舊窗收工.md`
——**⛔ 含該施工單** ⇒ 與單 `§零` 之陳述**逐筆相符**。

---

## `§四`　工項零：本補令原封入倉（`commit 1`）

**落點** ＝ `docs/orders/W-G.9-303_補令二_新窗首則與施工單之入倉.md`

| 受詞 | 產生指令（框逐字） | 實測 |
|---|---|---|
| 受詞自證（坑 `bf`） | `os.path.basename` ⋂ 字樣 | [必真] 含 `補令二` ＝ **`True`**／[必偽] 含 `補令一` ＝ **`False`** |
| 框① blob `sha1` | `git rev-parse <commit>:<path>` | `3d91f357bb734c3364f4aab3132527208fd14992` |
| 框② blob 內容 `sha256` | `git cat-file blob <sha1>` ⇒ `hashlib.sha256` | `dc9d0d4e95e22c36608dc53902fb102c67bcbfbd93c19cc5588528b88d00bedc` |
| 磁碟 內容 `sha256` | `open(<path>,'rb')` ⇒ `hashlib.sha256` | `dc9d0d4e95e22c36608dc53902fb102c67bcbfbd93c19cc5588528b88d00bedc` |
| 源 內容 `sha256` | 同上·受詞 ＝ 主檢出之未追蹤檔 | `dc9d0d4e95e22c36608dc53902fb102c67bcbfbd93c19cc5588528b88d00bedc` |
| **三方逐位相同** | `blob == 磁碟 == 源` | **`True`**（🔒 **二框並報**：`sha1` 與內容 `sha256`） |
| bytes／`LF` | `len(blob)`／`blob.count(b'\n')` | `19051`／`205` |
| `CR` | `blob.count(b'\r')`（**讀 blob 取 bytes**） | **`0`** |
| `CR` 判別力[必非零] | 同框對 `data/V6.dxf` | **`12308`**（全長 `88341` B）⇒ **器非紅** |
| 檔數 ＝ `1` 之自證 | `git -c core.quotepath=false ls-tree -r <commit> --name-only` ⋂ `basename` | 受詢 ＝ **`1`**；造甲[必≥1] `W-G.9-303_補令一_舊窗收工批.md` ＝ **`1`**；造乙[必為0] 人造名（**執行期組出**·字面⛔ 出艙）＝ **`0`** |
| `numstat` | `git show --numstat --format= <commit>` | 增 **`205`** ／ 刪 **`0`** ⇒ `deletions` ＝ **`0`** |

---

## `§二`　工項一：`W-G.9-303` 施工單之入倉（`commit 2`·代行其 `§二` 工項零）

**落點** ＝ `docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`

### 二-1　取檔之路 ＋ **四數之釘死**（入倉**前**量·逐項出艙）

🔒 **取檔 ＝ 路甲**——主檢出之工作區存在該路徑之**未追蹤**檔。
**產生指令**：`git -C <主檢出> -c core.quotepath=false status --porcelain`
**逐字命中** ＝ `?? docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` ⇒ **路甲成立**（⛔ 取路乙）。
🛑 **⛔ 憑記憶重打**——以二進位讀寫複製（`open(src,'rb').read()` → `open(dst,'wb').write(b)`），
複製後即時對拍 `src == dst` ＝ **`True`**。

| 受詞 | **期值** | 產生指令（框逐字） | **實測** | 判 |
|---|---|---|---|---|
| 內容 `sha256` | `460779b22a491bc8132f424525ebab1df308eca1944e5a81483391244d568142` | `sha256sum <磁碟路徑>` | `460779b22a491bc8132f424525ebab1df308eca1944e5a81483391244d568142` | ✅ |
| bytes | **`22313`** | `wc -c <磁碟路徑>` | **`22313`** | ✅ |
| 列數 | **`187`** | `wc -l <磁碟路徑>` | **`187`**（**二數並報**：`bytes.count(b'\n')` 亦 ＝ **`187`**·末位元組 ＝ `b'\n'` ⇒ 末列有換行） | ✅ |
| `CR` | **`0`** | `python -c` 之 `bytes.count(b'\r')`（**讀 bytes**·⛔ text 框·坑 `bk`） | **`0`** | ✅ |

🔒 **四數逐項相符 ⇒ 得入倉**（任一不符則停、照實具名、⛔ 入倉、⛔ 修補）。

### 二-2　入倉後之出艙

| 受詞 | 產生指令 | 實測 | 判 |
|---|---|---|---|
| 框① blob `sha1` | `git rev-parse <commit>:<path>` | `8c85fc1ccdf781ce08c10ba397038bfe97c50238` | — |
| 框② blob 內容 `sha256` | `git cat-file blob <sha1>` ⇒ `sha256` | `460779b22a491bc8132f424525ebab1df308eca1944e5a81483391244d568142` | ✅ ＝ 期值 |
| **三方逐位對拍** | `blob == 磁碟 == 源` | **`True`**（🔒 **二框並報**） | ✅ |
| bytes／`LF`／`CR` | `len`／`count(b'\n')`／`count(b'\r')` | `22313`／`187`／**`0`** | ✅ |
| `numstat` | `git show --numstat --format= <commit>` | 增 **`187`** ／ 刪 **`0`** | ✅ 增 ＝ `187`·`deletions` ＝ `0` |
| 檔數 ＝ `1` 之自證 | `ls-tree -r` ⋂ `basename` | 受詢 **`1`**／造甲[必≥1] `W-G.9-303_補令二_新窗首則與施工單之入倉.md` ＝ **`1`**／造乙[必為0] 人造名 ＝ **`0`** | ✅ |

#### 🔑 `SELF_SHA256` 之復驗（**繫於倉內之 blob**·⛔ 以工作區之檔充之）

**產生指令**：`git cat-file blob HEAD:<path> > <暫存檔>` ⇒
`python verify/probes/probe_order_preflight.py <暫存檔>`
🔒 **暫存檔落於倉外**（scratchpad）⇒ 其於 `git -c core.quotepath=false status --porcelain` 之命中 ＝ **`0`**（全倉 `status` 命中亦 `0`）。

| 受詞 | 期值 | 實測 | 判 |
|---|---|---|---|
| 暫存檔 bytes／`sha256` | ＝ 倉內 blob | `22313`／`460779b22a491bc8…8142` | ✅ |
| `P-5` `SELF_SHA256` | ✅ 相符·受詞 `22235` B·實算 `37c6c848bc9dd757f69cf3178a02b5ce55d52b4aa2bdd1b60bc4dc074dad4d19` | `P-5 :187 SELF_SHA256 ✅ 相符（取檔內最末一個·受詞 22235 B）｜實算 37c6c848bc9dd757…／單載 37c6c848bc9dd757…` | ✅ **逐位相符** |
| `P-3` | 不紅 | `app.py` 之 `def main` ＝ `14975`-`24579`（記錄項·**不紅**）；🔴 機械·停機款 ＝ **`0`** 項；`rc=0` | ✅ |
| 受詞自證（坑 `bf`·**機械**） | [必真] 含 `W-G.9-303`／[必偽] 含 `補令二` | 標頭逐字 ＝ `【order pre-flight】W-G.9-303_施工單_街廓識別與二守衛母體之現查.md（22313 B／187 列）`；[必真] ＝ **`1`**／[必偽] ＝ **`0`** | ✅ |

🩸 **併呈（照實·⛔ 頂替）**：該次 `preflight` 另印 🟡 `1` 項 ——
`[P-4] :167 🟡 方向性轉引而同段⛔ 未具名座標系：🛑 **報告之<u>首行</u>**：本窗 context 之百分比（照實）＋ 其是否 `≥ 70%` 之判；…`。
🔒 **其受詞係 `W-G.9-303` 施工單自身之條款、⛔ 本批之工項** ⇒ 本批**只照實登記、⛔ 處置**；
其逐項處置歸**次一批**（即該施工單之執行批）。

#### 在倉性之終驗（`§一` 閘 `8` 之對拍）

| 受詞 | 指令 | `rc` | 態 | 判 |
|---|---|---|---|---|
| 受詢 | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` | **`0`** | **態1** | ✅ **已由態2 轉為態1** |
| 判別力[必為零] | 同指令對一**執行期組出**之人造路徑（字面⛔ 出艙） | **`128`** | 態2 | ✅ ⇒ 器非紅 |

---

## `§三`　工項二：`自誤 407` 之鑄（`commit 3`）

**落點** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（嚴格 `append-only`）。

### 三-1　界之唯一性（**二數並報**）

| 框 | 受詞 | 命中 | 期 | 判 |
|---|---|---|---|---|
| **整列逐位相等**（`^…$`） | `ZW407` 之 BEGIN 哨兵 | **`1`**（`:100`） | `1` | ✅ |
| **整列逐位相等**（`^…$`） | `ZW407` 之 END 哨兵 | **`1`**（`:133`） | `1` | ✅ |
| **子字串框** | 同上 BEGIN | **`2`**（`:97`／`:100`） | 必得 `2` | ✅ |
| **子字串框** | 同上 END | **`2`**（`:97`／`:133`） | 必得 `2` | ✅ |

🔒 母體 ＝ **本補令單檔**。`:97` 之命中係本補令行文對該二字串之引用（單已預告「**必得 `2`**」）。

### 三-2　`payload` 之取

| 量 | 值 |
|---|---|
| `payload` bytes ／ 列數 | **`2787`** ／ **`30`** |
| `payload` 內 `ZW407` 字樣命中 | **`0`**（⇒ 哨兵⛔ 入簿） |
| `payload` `CR` | **`0`** |
| `payload` `sha256` | `a2359a80c50bb888a2ce510ca3c5d560177263569b018ba19faf8dab07d0532f` |
| 首列（逐字） | `### 🩸 \`自誤 407\`　**以自身之「交付」推定受單側之「入倉」，並據以為次一批之<u>載重前提</u>**` |
| 末列（逐字） | `**⛔ 待新窗發現**——新窗發現時，其**唯一可為**者即停機上呈，一個往返已然耗去。` |

🔒 **⛔ 剝層、⛔ 加 `> `、⛔ 改一字**——`payload` 係自本補令之 bytes 直接切出（**⛔ 憑記憶重打**）。

### 三-3　落地形之機驗（🔑 **繫於性質**·`自誤 402` 攔法）

| 受詞 | 產生指令／框 | 期 | **實測** | 判 |
|---|---|---|---|---|
| 自誤 相異 | `probe_WG9267_issuer_measurers.py registry`（**器之輸出**） | **`397`**（`396 + 1`） | **`397`** | ✅ |
| 自誤 `MAX` | 同上 | **`407`** | **`407`** | ✅ |
| `407` 之命中 | 定義框（`VR-091 四`）`` ^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`? ``·母體 ＝ 倉內 blob | **`1`**（⛔ `> 1`） | **`1`** | ✅ |
| 判別力[必命中] | 同框·受詞 `399` | `True` | **`True`**（命中 `1`） | ✅ |
| 判別力[必為零] | 同框·**執行期組出**之人造號（字面⛔ 出艙） | `False` | **`False`**（命中 `0`） | ✅ |
| 哨兵 `ZW407` 入簿命中 | `blob.count("ZW407")` | **`0`** | **`0`** | ✅ |
| `append-only` [必真] | `new.startswith(old)` | `True` | **`True`** | ✅ |
| `append-only` [必偽] | `old.startswith(new)` | `False` | **`False`** | ✅ |
| 末端之形 | `new.endswith(payload + b'\n')` | `True` | **`True`** | ✅ |
| `wg942_append_audit` | `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py` | `rc = 0` | `rc=0`·受檢 **`77`** 筆／不適用 `0`／結論逐字「**✅ 全部成立**」 | ✅ |
| 簿之量 | `len(blob)`／`count(b'\n')`／`count(b'\r')` | — | `846009` → **`848798`** B（增 **`2787`**）／`LF` **`10028`**／`CR` **`0`** | ✅ |
| `numstat` | `git show --numstat --format= <commit>` | `deletions` ＝ `0` | 增 **`31`** ／ 刪 **`0`** | ✅ |

🔒 **`常規四（九）` 之二項機檢**
> **① 形之自證**：`407` 之標題形逐字 ＝ `` ### 🩸 `自誤 407`　**以自身之「交付」推定受單側之「入倉」，並據以為次一批之<u>載重前提</u>** ``（簿之 `:9999`），
> 可被自誤之**正典定義框**命中（上表「`407` 之命中 ＝ `1`」即其自證）；且合單所令之 **`###` 形**。
> **② `MAX` 之推進**：`406` → **`407`**，且 `407` **⛔ 在缺號集**（缺號集 ＝ `[106, 355, 356, 357]`·**⛔ 含 `407`**）。

---

## `§五`　`⛔ 為之事` 之逐項自檢

| # | 受詞（逐字節略） | 判 | 證 |
|---|---|---|---|
| `1` | ⛔ 辦 `W-G.9-303` 之 `§三`／`§四`；⛔ 驅動 `run_verification` 或任何長跑；⛔ 新增任何探針 | ✅ **未為** | 本批只辦入倉與鑄號；**⛔ 執行 `run_verification.py`**；`verify/probes/` 期初 ＝ 期末（見收工閘 `6'''`） |
| `2` | ⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 辦修批 | ✅ **未為** | 生產碼判法逐 `commit` **`0`** 行（收工閘 `2`）；⛔ 建立任何 ref；⛔ 呈 KL |
| `3` | ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；⛔ 引任何自 `baseline` 取得之數 | ✅ **未為** | 見收工閘 `6` |
| `4` | ⛔ 改 `W-G.9-303` 施工單一字（含其 `§零` 閘 `6` 之期值） | ✅ **未為** | 入倉 blob 內容 `sha256` ＝ 源之 `sha256`（`460779b2…`）⇒ **逐位未改** |
| `5` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號；⛔ 追加任何 `GB` 之末端登記；⛔ 動 `ALPHA_LABELS` 一字 | ✅ **未為** | 三簿期初＝期末（`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29`）；坑 `89`／`89`；`ALPHA_LABELS` 載體之 `numstat` 空（收工閘 `5`） |
| `6` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ **未為** | 見收工閘 `6` |
| `7` | ⛔ 追改 `W-G.9-303` 補令一／交接註或任何已入倉文件一字 | ✅ **未為** | 三 `commit` 之 `deletions` 全 `0`；`wg942_append_audit` `rc = 0`（`77` 筆全成立） |
| `8` | ⛔ 判 `GB-170` 可否解除、⛔ 判路丙、⛔ 判孰是孰誤、⛔ 提修法主張、⛔ 擬任何改法或 `diff` | ✅ **未為** | 本報告**⛔ 就此作成任何主張** |
| `9` | ⛔ 呈 KL 任何事 | ✅ **未為** | — |
| `10` | ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述） | ✅ **未為** | 三 `commit` 訊息⛔ 含「已推送」字樣；收工閘 `7`（`push` 後之 `ls-remote`）依單**出艙於回報·⛔ 入倉** |

---

## `§六`　本批之**自解清單**（`常規 追加五 一 五`·逐項五要件）

### 自解 `1`　`payload` 之界：哨兵毗鄰之**空列**是否屬 `payload`

| 要件 | 內容 |
|---|---|
| **① 疑義逐字** | 單 `§三` 逐字「界 ＝ `<!-- ZW407-BEGIN -->` 與 `<!-- ZW407-END -->` 之間（**不含哨兵列**·哨兵⛔ 入簿）」——**「不含哨兵<u>列</u>」⛔ 明言毗鄰之空列之歸屬** |
| **② 二個以上之讀法** | **讀法甲**：`payload` ＝ 二哨兵列之間之**全部 bytes**（含首尾各一空列）＝ **`2789`** B；**讀法乙**：再去其首尾之空列 ＝ **`2787`** B（`30` 列） |
| **③ 所取者** | **讀法乙**（`2787` B） |
| **④ 機械證據** | 取乙後：`new.endswith(payload + b'\n')` ＝ **`True`**；`new.startswith(old)` ＝ **`True`**／`old.startswith(new)` ＝ **`False`**；簿末形 ＝ 原末列 ＋ **一**空列 ＋ `payload`；`wg942_append_audit` `rc = 0`。單所令之機驗式**逐項成立** |
| **⑤ 所棄者之由** | 讀法甲將使簿末成為 `…\n\n\n`（三連換行）——其**⛔ 違反任何明文**，惟單之機驗式逐字為 `new.endswith(payload + b'\n')`，於甲下該式仍成立而末端多一空列，**⛔ 增益**；且該二空列於本補令內之功能係**與哨兵列之分隔**，**⛔ `payload` 之內容** |
| **五要件** | ① 零土地後果 ✅（純登記·⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何）② 零生產碼 ✅ ③ 可機驗 ✅（上④·且**否證對照**已跑：`old.startswith(new)` 必偽組得 `False`）④ 可逆 ✅（`append-only`）⑤ 出艙具名 ✅（本節） |
| **白名單** | 類 `3`（markdown 標記差·**⛔ 改任何數**） |

🛑 **⛔ 落入黑名單任一項**（⛔ 動土地量·⛔ 動生產碼·二讀法後果於土地上**相同**〔皆為零〕·⛔ 新訂準據·⛔ 廢止或放寬任何閘·⛔ 涉 KL 已裁之款）。

---

## `§七`　本批之**自捕**（`§七` 令：一律於出艙前攔下、照實併記·**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）

🛑 **⛔ 讀為「本批無誤」**——下列 `3` 則皆為 CC 自身之**量測器紅**，於出艙前攔下。

| # | 形 | 徵候 | 根因 | 攔法（**已施行**） |
|---|---|---|---|---|
| `1` | **`git ls-files` 之 CJK 八進位跳脫使 `.md` 字尾框靜默失配** | 首次量「追蹤 `.md` 檔數」得 **`32`**，而倉內既有錨（`W-G.9-303 補令一` 之收工閘 `5`）逐字載 **`807`** | `git ls-files` 預設 `core.quotepath=true` ⇒ CJK 檔名輸出為 `"docs/…\350\243\234…md"`（**帶引號**）⇒ `endswith('.md')` 必偽 | 改框為 `git -c core.quotepath=false ls-files -z \| tr '\0' '\n' \| grep -c '\.md$'` ⇒ **`807`**（與倉內錨相符）。**二數並報**：紅框 `32`／正框 **`807`** |
| `2` | **`自誤 407` 標題形之查用錯反引號形（`GB-158` 之 B／C 形）** | 自寫之檢得「`407` 之標題形逐字 ＝ `[]`」（**空**），而**同一次**執行之定義框得「`407` 命中 ＝ `1`」⇒ **二者互斥** | 所用字樣為 **B 形** `` 自誤 `407` ``（反引號只包號），而倉內現行標題形為 **C 形** `` `自誤 407` ``（反引號包**全 token**）——即 `GB-158` 所載之本倉形 | 改以 C 形查 ⇒ 命中 `1`（簿 `:9999`·逐字見 `§三-3`）。🔒 **真正攔下它的是「二量互斥」**，⛔ 目視 |
| `3` | **`$TMPDIR` 未設使重導向落於 `/`** | 閘 `6` 首跑 `rc=1`·`stderr` 逐字 `/pit.txt: Permission denied` | 本機 `bash` 之 `$TMPDIR` 為空 ⇒ `"$TMPDIR/pit.txt"` 展開為 `/pit.txt` | 改以 scratchpad 絕對路徑為落點並**重跑**。🔒 該次**探針從未執行** ⇒ ⛔ 誤採其 `rc` 為受詞之判（若當時逕讀 `rc=1` 為「閘 `6` 紅」即成假紅） |

🔒 **`3` 之通則**：`rc ≠ 0` **⛔ 等同受詞紅**——須先判其係**量測器紅**抑或**受詞紅**（`常規五`／`序 11` 之一般化 `三`）。本則之 `rc=1` 係 **shell 重導向失敗**，受詞（探針）根本未被執行。

---

## `§八`　逐 `commit`（**全 `40` 碼**·序 ＝ 工項零 → `§二` → `§三`·**⛔ 併為一 `commit`**）

| 序 | `commit`（全 `40` 碼） | 受詞 | 增／刪 |
|---|---|---|---|
| `1` | `bd85acdf5a7e20deeb960c85ea37ce12cd8636e0` | `§四` 工項零：本補令原封入倉 | `205` ／ **`0`** |
| `2` | `bb742f8d13f90269ecd805ad9eb79ce4c35b6767` | `§二` 工項一：`W-G.9-303` 施工單原封入倉 | `187` ／ **`0`** |
| `3` | `53b7a5bc7e2ba7035c16756c01691062cb6c4a10` | `§三` 工項二：`自誤 407` 之鑄 | `31` ／ **`0`** |

🔒 **序⛔ 併**：三者各自獨立 `commit`，逐 `commit` 之生產碼判法皆 **`0`** 行。

---

## `§九`　收工閘（**報告入倉後量**·嚴格末端追加）

🛑 本節於本報告入倉**後**以末端追加補入（單 `§五` 所令）。

### `§九-1`　逐閘實測（態 ＝ 報告本體入倉後·`HEAD` ＝ `54fdbd37cb1805ddcc8d28645548476a8fdb0da0`）

| # | 受詞（產生指令逐字） | 期／判準 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`** | `bd85acd` 增 `205`／刪 **`0`**（補令二）·`bb742f8` 增 `187`／刪 **`0`**（施工單）·`53b7a5b` 增 `31`／刪 **`0`**（自誤簿）·`54fdbd3` 增 `242`／刪 **`0`**（本報告）⇒ **`deletions` 合計 ＝ `0`** | ✅ |
| `2` | `git diff --name-only <區間> \| grep -xE 'app\.py\|verify/stepg_pipeline\.py\|verify/run_all\.py\|verify/run_verification\.py'`（母體**正面列舉 `4` 檔**） | 逐區間 **`0` 行** | `3e9cf28..bd85acd` ＝ `0`／`bd85acd..bb742f8` ＝ `0`／`bb742f8..53b7a5b` ＝ `0`／`53b7a5b..54fdbd3` ＝ `0`；**對照組[必非零]** `17fd981^..17fd981` ＝ **`2`** ⇒ 器非紅 | ✅ |
| `3` | `git cat-file blob HEAD:<path>` ⇒ `bytes.count(b'\r')`（框 ＝ **倉內 blob**·**取 bytes**·坑 `bk`） | `CR` ＝ `0` | 本批新增／異動 `4` 檔逐檔：施工單 `CR 0`（`22313` B）·補令二 `CR 0`（`19051` B）·本報告 `CR 0`（`21921` B）·自誤簿 `CR 0`（`848798` B）；**判別力[必非零]** `data/V6.dxf` `CR` ＝ **`12308`**／全長 **`88341`** B ⇒ 器非紅 | ✅ |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（**簿之數一律取器之輸出**） | 自誤 `397`／`407`（＋**一**號）；`GB`／`VR`／`K-9` 一字未動 | 自誤 `{'相異': 397, 'MAX': 407, 'MIN': 7, '缺號': [106, 355, 356, 357]}`；`GB` `{'相異': 168, 'MAX': 170, '缺號': [12, 87]}`；`VR` `{'相異': 80, 'MAX': 95, '缺號': [73, 75]}`；`K-9` `{'相異': 28, 'MAX': 29, '缺號': []}` | ✅ |
| `5` | `python verify/probes/probe_WG9269_pit_index.py`（**等待綁行程結束**·⛔ `\| tail -N`·`rc=0`） | `89`／`89`·末 `bk` ⇒ 下一 `bl`（⛔ 鑄坑） | 資料列 **`89`**（框 ＝ `grep -c "✅ 可解析 \|"`）／三綠 **`89`**（框 ＝ `grep -c "✅✅✅"`）／末標籤 **`bk`** ⇒ 下一 **`bl`**；母體 ＝ 追蹤 `.md` **`810`** 檔；判別力二造 ＝ 造甲 `坑 n` 定義處 `6` ≥ 1 ✅／造乙 人造坑 `0`／`0` ✅ ⇒ **器非紅** | ✅ |
| `5` 併 | `ALPHA_LABELS` 載體之 `git diff --numstat 3e9cf28..HEAD -- verify/probes/probe_WG9269_pit_index.py` | 輸出**空** | 輸出 ＝ `''`（**空**）⇒ `ALPHA_LABELS` **⛔ 動一字** | ✅ |
| `5` 自洽 | `git -c core.quotepath=false ls-tree -r -z <rev> --name-only` ⋂ `.md` | `.md` 增量 ＝ 本批新增之 `.md` 檔數 | 期初 **`807`** → 期末 **`810`**·增量 **`3`**；新增逐檔 ＝ 施工單／補令二／本報告 ⇒ **與本批新增之 `.md` 檔數逐檔相符** | ✅ **自洽** |
| `5'` | `PYTHONIOENCODING=utf-8 python verify/tools/wg9223_acceptance_audit.py <本補令> <本報告>`（🛑 受詞係 `index` ⇒ 復跑前先 `git add`） | 命中 `0` 之字樣 ＝ `0` | 見 `§九-3` | ✅ |
| `6` | 自限復驗 | 見右 | 見 `§九-2` | ✅ |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**；判別力[必存在] `p3a` 命中 **`1`** | ref 數 ＝ **`20`**；`verify/W-G.9-299-gb170` 命中 **`0`** ✅（**⛔ 開**）；`p3a`（`05a11bd665f6c2790ff821c9e3a1606651e345d4`）命中 **`1`** ✅ ⇒ 器非紅 | ✅ |
| `6''` | 🔑 **新窗首則之可取得性**（`自誤 407` 攔法 ③） | **態1**（`rc = 0`） | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` ⇒ `rc = 0` ⇒ **態1**；判別力[必為零] ＝ 一**執行期組出**之人造路徑（字面⛔ 出艙）⇒ `rc = 128` ⇒ 器非紅 | ✅ |
| `6'''` | `git ls-tree <rev> --name-only verify/probes/`（**二數並報**） | 期末 ＝ 期初·⛔ 新增任何探針 | 期初 **`288`** ／ 期末 **`288`** ⇒ **相同**；判別力[必相異] `verify/probes/probe_WG9302_sidesrc.py` 於 `798df526^` `rc = 128`（不存在）／於 `HEAD` `rc = 0`（存在）⇒ 器非紅 | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** | 🔒 依單**⛔ 載於本報告**——其值出艙於聊天回報 | 🔒 |

### `§九-2`　閘 `6` 自限復驗（逐項）

| 受詞 | 產生指令（框逐字） | 期 | **實測** | 判 |
|---|---|---|---|---|
| 生產碼 `34` 檔 blob 期初＝期末 | `git ls-tree <rev> --name-only verify/` ⋂ `*.py` ＋ `app.py` ⇒ 逐檔 `git rev-parse <rev>:<f>` | 相異 **`0`** | 期初 `34` 檔／期末 `34` 檔·**blob 相異 ＝ `0`** | ✅ |
| 判別力[必相異] | 同框·受詞 ＝ 本批確有異動之檔（本報告） | 相異 `True` | 期初 `git cat-file -e` `rc = 128`（不存在）／期末 blob ＝ `4d0f83a55273…` ⇒ **相異 `True`** ⇒ 器非紅 | ✅ |
| `app.py` | `git rev-parse HEAD:app.py`（**框 ＝ blob `sha1`**） | `4379108a…` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `baselines` | 🔒 **正典框**（`W-G.9-270R5:25` 逐字）＝ `git ls-tree -r <rev> verify/baselines` 之**輸出文字**之 `sha256` | `298` 檔·`a898f4e1…` | 期初／期末**皆** `298` 檔·輸出 `59560` B·`sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` ⇒ **逐位相符且期初＝期末** | ✅ |
| `p3a` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` | `rc = 1`（**非**祖先 ⇒ ⛔ 併線） | ✅ |
| `WV_BAKE` | `os.environ.get('WV_BAKE')` | `None` | `None` | ✅ |
| `K-9-5-*`／`K-9-12`／`K-9-29` ⛔ 落地 | `git cat-file blob <rev>:app.py` ⇒ 字樣計數 | 期初＝期末 | `k6_step0_merge(` `2`→`2`／`rect_fits` `7`→`7`／`_blue_shadow_tri` `7`→`7` ⇒ **⛔ 落地任何子項** | ✅ |
| `GB-79`／`167`／`168`／`169`／`170` ⛔ 修 | `GB` 簿之器輸出（相異 `168`／MAX `170`·期初＝期末）＋ 本批 `numstat` 未觸該簿 | ⛔ 修 | `GB` 簿**⛔ 在本批之異動檔清單內**（異動 `4` 檔逐檔具名於閘 `1`）⇒ **一字未動** | ✅ |
| **六物證落檔** | **先 `git cat-file -e HEAD:<p>` 驗存在**（三態分列）⇒ `git rev-parse <rev>:<p>`（框 ＝ blob `sha1`） | 逐位未動 | `WG9295R_joint_v1_selfpolluted.log` `rc=0` **態1** `1a35dc168655`→`1a35dc168655`／`WG9296R_w0rec_v1_blknone.log` `rc=0` **態1** `f93eb2fcb7e6`→同／`WG9297R_w0caliber_v1_blkabsent.log` `rc=0` **態1** `16742f3805e0`→同／`WG9298R_gate3_v1_firstcall.log` `rc=0` **態1** `5cef4c8dfe7a`→同／`WG9300R_static_v1_popcollapse.log` `rc=0` **態1** `476aac14eda5`→同／`WG9300R_dynamic_v1_wrongframe.log` `rc=0` **態1** `b3ab88b73dda`→同 ⇒ **六檔逐位未動** | ✅ |

### `§九-3`　閘 `5'`　`acceptance_audit` 之實測

**產生指令**：`PYTHONIOENCODING=utf-8 python verify/tools/wg9223_acceptance_audit.py docs/orders/W-G.9-303_補令二_新窗首則與施工單之入倉.md docs/reports/W-G.9-303R_補令二_施工單入倉與自誤407之鑄_執行報告.md`

🩸 **本節追加<u>前</u>之乾跑（照實併記·⛔ 隱去）**：`rc = 1`，判定組 `18` 個中命中 `0` 者 **`3`** 個 ——
`acceptance_audit`（報告 `0`／單本身 `2`）／`W-G.9-299-gb170`（`0`／`2`）／`probe_WG9302_sidesrc.py`（`0`／`2`）。
🔒 **三者皆為<u>收工閘</u>之受詞**（閘 `5'`／`6'`／`6'''`），其字樣本即隨本節（`§九`）入倉 ⇒
**⛔ 為湊字樣而增寫**，係**本節之實測內容自然涵蓋之**。
🔒 **本節追加<u>後</u>之復跑**（**先 `git add`** ⇒ 受詞為 `index`）：見下。

| 量 | 值 |
|---|---|
| 判定組（反引號 token） | **`18`** 個 |
| 參考組（CJK 詞·**⛔ 作判**） | **`3`** 個 |
| **判定組之報告命中 `0` 者** | **`0`** 個 |
| `rc` | **`0`** |

🔒 **判定組⛔ 為空** ⇒ **⛔ 觸「判定組為空 ⇒ loud 拒測」之款**（`序 11` 之一般化 `二`）；其基數 `18` 已同格出艙。

---

## `§十`　**追加之自捕**（`§七` 令·於 `§九` 之量測中新捕 `2` 則·**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）

🛑 **⛔ 讀為「本批無誤」**——連同 `§七` 之 `3` 則，本批自捕共 **`5`** 則，**全為 CC 自身之量測器紅**。

| # | 形 | 徵候 | 根因 | 攔法（**已施行**） |
|---|---|---|---|---|
| `4` | 🔑 **以<u>自擬框</u>算聚合 `sha256` 而與倉內錨逕比** | 閘 `6` 之 `baselines` 聚合 `sha256` 首算得 `2aaaf4096a0c80b2…`，而倉內錨為 `a898f4e1bba8f8d2…` ⇒ **看似「`baselines` 被動過」** | CC 自組「`路徑 + blob sha1` 逐列」之清單再 `sha256`，**⛔ 正典框**；且同一自擬框**自身亦不穩定**——`quotepath` 開關不同即得不同值（本批先後得 `c1c24022…`／`2aaaf409…` **二值**） | 回溯倉內**產生指令**：`W-G.9-270R5:25` 逐字載框 ＝ `git ls-tree -r HEAD verify/baselines` 之**輸出文字**之 `sha256`（併載戒「**先對齊框再比**·⛔ 以自擬框逕比」）。改用該框 ⇒ 期初／期末皆 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·**逐位相符** |
| `5` | 🔑 **`git ls-files --with-tree=<rev>` 被誤為「該 `rev` 之樹」** | `.md` 檔數期初得 **`810`**、期末亦 **`810`** ⇒ 增量 `0` ⇒ **閘 `5` 之自洽檢看似成立而實為假綠**（真增量為 `3`） | `--with-tree` 之語義為「**index ∪ 該 tree**」之**聯集**，⛔ 「該 tree」——故期初值被**本批已 `add` 之新檔污染**而虛增為期末值 | 改框為 `git -c core.quotepath=false ls-tree -r -z <rev> --name-only` ⋂ `.md` ⇒ 期初 **`807`**／期末 **`810`**·增量 **`3`**·**逐檔具名**與本批新增之三 `.md` 相符 |

🔒 **`4`／`5` 二則之共同根因** ＝ **框未與受詞對齊**：`4` 係**與倉內錨比而未對齊其產生指令**；
`5` 係**框之語義（聯集）與受詞（單一 `rev` 之樹）不合**。
🔴 **`5` 尤須具名**：其為**假綠**——若未攔下，閘 `5` 之自洽檢會以「增量 `0` ＝ 新增 `0`」之姿**通過**，
而本批實新增 `3` 檔 ⇒ **該閘將對「新增了什麼 `.md`」完全失明**。
🔒 **攔下 `5` 者係外部錨**（倉內既載之 `807`·`W-G.9-303 補令一` 收工閘 `5`），**⛔ 自洽**——
此即「**母體之基數須綁外部錨**」（坑 `bj`）之實效。

---

## `§十一`　一句話推導

本批之受詞（`W-G.9-303` 施工單）於開工時**確不在倉**（閘 `8` **態2**·並以 `git ls-files` ⋂ `W-G.9-303` ＝ **`2`** 獨立復現），
經工項一以**路甲**取檔、**四數釘死**逐項相符後入倉，其 `SELF_SHA256` 自**倉內 blob** 復算仍 ✅ 相符（實算 `37c6c848…`·受詞 `22235` B）
⇒ **態2 → 態1**，`自誤 407` 之攔法 ③ 所令之「新窗首則之可取得性」於本批**當場成就**。
