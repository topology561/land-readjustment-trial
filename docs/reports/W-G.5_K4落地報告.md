# W-G.5 — **K-4 落地報告**（任務 1／3／4／5 完成·任務 2 停機上呈）

> 起點 `80a1853`。上游＝KL 域裁 K-4（街角地資格·2026-07-27）＋ claude.ai 任務 1–5。
> **行號一律當場 grep**；本文只給符號名。

---

## 〇、先講兩件程序上的事

**(1) 任務 5 所稱「上一則」，本 session 未收到。** 本 session 收到的上一則是最初那份
（BLOCKED-1／-2／-3 ＋ 新立 E-8 ＋ 夾具補格 ＋ N-17 ＋ 順帶查證 ＋ 失敗考古），
其中**沒有** BLOCKED-A／BLOCKED-B／`app.py:8190`／E-9 連續性／E-6 機制註記這五項。
⇒ 下列處置中**字面能確定者已做**；**BLOCKED-A／-B 之原文要求無從核對**——
若與我的推定不符，請把原文再貼一次。

**(2) 本報告曾被我自己寫壞一次並重寫。** 用了
`io.open(p,"w").write(io.open(p).read().replace(...))` ——`"w"` 模式**先截斷再求值內層 read**
⇒ 檔案歸零。已重寫。（記此以免下一手重蹈：**就地改檔一律先讀進變數、再開寫**。）

---

## 一、任務 1：右側 S 起算點（K-4 第 5 條）

### 1.1 您的前提【坐實】、歸因【推翻】

**坐實**（我獨立重算·未抄）：八側 |FRONT×SIDE **無限直線**交點 − FRONT 對應端點|
**全為 0.000000000 m** ⇒ **FRONT_LINE 之 p1／p2 即該側未截角理論角**。

**推翻**：您稱 R3 `_oblique_s_max = 89.8392`、**短** 3.5659 ＝ 截角腿長。
我用管線自己的 `cad['alloc_dir_by_block']` 量到：

| 街廓 | \|FRONT\| | `_oblique_s_max` | **Δ右** | s_min(左端) | 右截角腿 |
|---|---|---|---|---|---|
| R1 | 87.3130 | 87.7155 | **−0.4024** | −0.3248 | 3.4273 |
| R2 | 100.0274 | 100.0274 | −0.0000 | 0.2677 | — |
| R3 | 93.4051 | **96.8032** | **−3.3981** | −0.0000 | 3.5659 |
| R4 | 98.8938 | 98.7170 | **+0.1767** | 0.1759 | 3.5439 |
| R5 | 92.7995 | 92.7995 | 0.0000 | 0.2685 | — |
| R6 | 85.8549 | 85.8549 | −0.0000 | −3.6068 | 3.4015 |

- **方向相反**：四個右街角塊中三塊之 `_oblique_s_max` **長於** `|FRONT|`（非短）。
- **量值不合**：|Δ − 右截角腿| 落在 **3.37～6.96 m** ⇒ **Δ 不等於截角腿**。
- **交叉驗證**：本表之 `s_min／s_max` 與引擎自身 `T2-DIAG` 之「s 域」輸出**逐塊吻合**
  （R1 [−0.3248, 87.7155]／R3 [−0.0000, 96.8032]／R6 [−3.6068, 85.8549]…）。

**真成因**：`_oblique_s_max` 取街廓**全體頂點**於 `_strip_axis` **斜交軸**之極大 s。
ALLOC 與 FRONT 斜交 2.6°–5.3° ⇒ 勝出者常為 **BASELINE 側（後緣）頂點**。
機制核對：R3 之 3.3981 ÷ 深度 43.64 ＝ **tan 4.455°**；R6 之 3.6068 ÷ 44.59 ＝ **tan 4.62°**
——皆落在正典之 ALLOC-FRONT 偏角域（84.706°–87.395°，即偏 2.605°–5.294°）內。
（您的 89.8392 應係**正交軸**下之 R3 截角點 s，非斜交軸之 `_oblique_s_max`。）

⇒ **歸因要改，但「該修」的判斷成立**：R1／R3／R4 右側起算點確實不在理論角上（R3 差 3.40 m）。

### 1.2 兩消費者分開評估（KL 任務 1(b)）

以 patch `ns['_corner_first_lot_G']` 跑三變體（**內部仍呼原 `_solve_G_one`**·非另寫平行式），
並以 **A′ 載具自檢**（用 patch 重現未 patch 之語意）證明比較忠實——A′ vs A 逐格全等。

| 消費者 | 判定 | 依據 |
|---|---|---|
| `baseline_pt`（起算點） | **改** ⇒ `corner_pt + s_max_left·d̂`（＝FRONT p2） | K-4 第 5 條之標的 |
| `S_max`（bisect 上界） | **不改** ⇒ 續用 `s_max_right` | 該值係街廓於斜交軸之**真實 s 域上界**，正是解 S 之正確搜尋上界；且實測**惰性**（變體 B vs C 兩情境合計僅 1 格差 0.01㎡） |

### 1.3 衝擊（KL 任務 1(c)：逐側列表·不自行接受翻盤）

**兩情境全鏈實跑：winner 集合 0 異動、強制抵費地側 0 異動 ⇒ 無翻盤可言。**

| 情境 | winner 側 | forced 側 | winner 異動 | forced 異動 | 真G 異動 |
|---|---|---|---|---|---|
| 0m | 12 | 0 | **0** | **0** | 5 格 |
| 3.5m | 12 | 3 | **0** | **0** | 6 格 |

真G 逐格（K-4 前 → 後）：

| 情境 | 街廓·端·候選 | 前 | 後 |
|---|---|---|---|
| 兩情境 | R1·右·628(5) | 240.39 | 240.57 |
| 兩情境 | R3·右·628-27(2) | 24.54 | **26.85** |
| 兩情境 | R3·右·628-28(1) | 62.17 | **65.61** |
| 3.5m | R3·右·628-29(1) | 62.17 | **65.61** |
| 兩情境 | R3·右·628-42(2) | 84.86 | **88.36** |
| 兩情境 | R3·右·628-45(2) | 205.27 | **209.06** |

### 1.4 🔴 一個我預測錯、且因此揭出覆蓋率洞的地方

我原判「真G 變動 ⇒ `v3·診斷0m`／`率接線無串聯0m` 由 PASS 轉 FAIL、需重烤 baseline」。
**實跑否證：FAIL 名目 diff 為空、56 PASS／14 FAIL 不動。**

**真因**：`diff_rows` 之比對迴圈為 `for col in b`（**只走 baseline 之欄**·
`grep -n "for col in b:" verify/run_verification.py`），而 `W-D.1.2 診斷` 之 baseline
**根本沒有 `真G(㎡)` 欄**（該欄係 P-C 後才加於 got 表；baseline 欄序止於
`…G估(㎡),門檻(㎡),範圍=門檻?…`）。

⇒ **`真G(㎡)` 不在任何 baseline 閘之比對母體內**。`diff_rows` 會咬
「缺列／多列／baseline 有而 got 缺之欄」，但**不咬「got 多出來的欄」**。

**後果（誠實記載）**：K-4 第 5 條這個**確實改了數字**的改動，在既有 `run_all` 下**完全隱形**
——若非本波把 `probe_ruling_K4_s_origin.py` 掛進 `run_all`，它將**無任何閘覆蓋**。
與倉內 F-2 之教訓同族：**沒人檢查 ≠ 相符**。

**未修之理由**：若加「got 欄集 ⊆ baseline 欄集」結構閘，`真G(㎡)` 會**立即**咬紅
⇒ 需重烤 v3 baseline ⇒ **P-H 凍結·需 KL 授權** ⇒ 列 **U-K2**，本波不動。

---

## 二、任務 3：E-7「待判（截角）」框架作廢 → K-4 蘊含

| 項 | 處置 |
|---|---|
| (a) | 新增 `WIDTH_VERDICT_CORNER_K4 = '合格（K-4 面積門檻蘊含）'`；街角宗帶 `_width_by_area_gate=True` ＋ `_width_k4_G`／`_width_k4_threshold`（門檻取自 `f3_corner_range_areas` 之該側值＝winner 選拔所用者） |
| (b) | `_need_merge` 寬度分支對街角 winner 恆 False，**理由改 K-4 蘊含**；`_width_pending_unchecked` 旗標**已移除**；舊 `_ok_mark`（為待判筆加註）**整段刪**（K-4 後為死碼） |
| (c) | 四態機制保留（非街角宗仍走 N-14）；**`待判（截角）` 計數須恆為 0**——步驟 J 與 `merge_subparcels_by_parent` **雙處 loud raise** |
| (d) | 探針 T7 改 K-4 版：**T7①** 街角 winner 皆 `G ≥ 門檻`（0m 8 筆／3.5m 5 筆全過）；**T7②** 無 PENDING 態產出；**T7③ 鑑別力**——刻意壓一筆 winner 之 G 至門檻下 ⇒ ① 確實咬到（0m 壓 `628-37(1)`→108.79、3.5m 壓 `628-36(1)`→225.18） |
| (e) | **K-2(Ⅲ) 消費者盤點**：全倉唯一消費者即 E-7 之街角寬深量測（`app.py` 兩處註解＋探針一處），**已由 K-4 退役** ⇒ **無其他消費者** ⇒ 續留 backlog、**不建**（禁死碼） |

---

## 三、任務 2：🛑 **停機上呈**（真域邊界）

K-4 第 3 條與既有 **M-5 §七①**（KL 2026-07-25 裁·已實作於 `verify/m_rescue.py`）
是**同一操作的兩個版本**。

| 面向 | M-5 ①（已實作） | K-4 第 3 條（待實作） |
|---|---|---|
| 觸發時機 | 趟0 PK **完成後**（`_fo_ends`＝forced_map 中已成 forced 之端） | PK **資格判定當下**（`cand_G < min_area_to_apply` 之每一候選） |
| 觸發母體 | **僅 forced 端** | **每個**未達門檻候選，不論該端是否 forced |
| 合併來源 | 同歸戶 ∧ **其他可建築街廓**（M-2 要件·`sr.所屬街廓 != 目標所屬街廓`）∧ ∈U₀（未達 MinA） | 字面僅「同歸戶中**較小者**」——**未排除同街廓、未限 U₀** |
| 判準 | 補至街角規定面積（`min_a_prime_for_corner` 迭代求**最小 a′**） | 「是否為同歸戶中應分配面積**最小者**」；非最小 ⇒ 併較小者再試算 |
| 消費帳 | `ConsumedRegistry`（源→target **對稱凍結**） | 若在 `_pk_one_side_v12` 內做 ⇒ **在 registry 之外** |

**三處衝突**：
1. **順序**：K-4 在 PK 內生效 ⇒ `_fo_ends` 縮小甚至清空 ⇒ M-5 ①②③ 整條規劃鏈
   （registry／稽核帳／F-2 三閘）失去輸入。UC9898 3.5m 現有 **3 個 forced 端**。
2. **重複計**：兩者都從「同歸戶其他土地」取 a′ 併入街角候選。K-4 若在 PK 內合併而不入
   `ConsumedRegistry`，同一宗可能**先被 K-4 消費、再被 M-5 當源**⇒ 面積增生。
   registry 之對稱凍結**只保護 M-5 內部**。
3. **來源母體不同**：照 K-4 字面實作 ⇒ 違反 M-2「其他可建築街廓」要件（KL 2026-07-25 明定不變）；
   照 M-5 限制實作 ⇒ 又不是 K-4 之字面。

⇒ **待裁**：K-4 第 3 條與 M-5 ① 之**適用序**與**合併來源範圍**。
裁定前**不實作**（實作即改面積歸屬）。K-4 第 1／2／4 條為既有行為、未動。

---

## 四、任務 4：`18.0` 之身分 ＋ 手冊逐句對條

- **`RW_SATURATION_WIDTH_M`（模組級具名常數）維持**——您已撤回「改案件參數」之指令，本波不動。
- **逐句對條已寫入 `_build_burden_range` docstring**。法源＝《市地重劃實施辦法》
  **第 29 條附件二**之 `W` 定義（倉內引錄：`grep -n "W 表示分配土地寬度" docs/W-C_細部plan.md`）：

  > 「W 表示分配土地寬度（**宗地側街臨街線實際長度之中點，向宗地分配線作垂直線**所量其間之距離）。」

| 條文子句 | 構造對應 |
|---|---|
| 「側街臨街線實際長度之**中點**」 | `side_mid`（SIDE_LINE 中點） |
| 「向**宗地分配線**作**垂直線**」 | 平移方向取 `alloc_dir` 之法向 `(-uy, ux)`；**非** MBR 較長邊（用 MBR 即違反條文） |
| 方向指向宗地內 | 以 `block_centroid` 定號 |
| `W ≥ 18m ⇒ Rw = 100%`（Rw 累積表末格） | `burden_width = RW_SATURATION_WIDTH_M` ⇒ 範圍即 `W ≤ 18m` 之帶 |
| 「其間之**距離**」＝垂距 | 切線取 **∥ALLOC_LINE** ⇒ 與基準線之距即垂距 |
| 取含 `side_mid` 之一側 | `min(pieces, key=centroid.distance(side_mid))`；`block ∩ 半平面` |
| **不扣截角** | 附件二 `W` 僅定義垂距、**未含**截角；截角係**畸零地規則**下 (Ⅰ) 之構件 |

⇒ **構造與條文相符**（逐句可對）⇒ **不停機**。

---

## 五、任務 5：字面能確定者

| 項 | 處置 |
|---|---|
| `app.py:8190` 訊息殘留 | ✅ 已修——`_build_burden_range` 之失敗仍印舊符號 `_build_corner_range_v2`、且訊息硬寫「18m」；已改印真符號＋`RW_SATURATION_WIDTH_M` |
| 快照 `min_t` 零判定 **1e-12** | ✅ 已加 `EPS_SNAP_ZERO`。理由入註記：快照係**跨機回歸**，`%.3e` 會把 −4.6e-14 與 +1.2e-14 印成不同字串 ⇒ 假紅。1e-12 ＝ 噪訊上界 100 倍，且**低於本案最小真實 \|min t\|（1.04e-08）四個數量級** ⇒ 不損鑑別力 |
| **E-9 前緣邊連續性斷言** | ✅ 已加——前緣諸邊須於環上構成**單一連續段**（可跨環首尾），否則 loud。<br>⚠️ **可達性已實證非空**：新夾具 ⑭ 以**微凹口**（面積 **0.002㎡ < E-8a 之 0.01㎡**、凹口頂 t=0.02 > 容差）**越過凸性閘**而觸發之，raise 訊息確為「前緣邊於環上分裂為 **2 段**（邊索引 [0, 3]／共 7 邊）」——**非**被 E-8a 攔下 |
| **E-6 閘寬機制註記** | ✅ 已寫入 `parcel_min_width_n14` docstring（機制導出 `~(min_depth/L)·q < q`；理論 4.2e-6 vs 實測 4.574e-6；並註明 KL 之 `1e-6` 低於本檔量化步長 1e-5、原理不可達成） |
| **E-10 歸因改述** | ✅ 已改——原寫「GEOS 會少算」讀起來像 bug report，會把下一手導向「換函式庫」。改述為**責任在呼叫方式**（絕對大地座標 ~3.1e5 ＋ ±1e5 超長切線 ⇒ 要求雙精度在 ~1e11 動態範圍保住 1e-2 絕對精度），故正解是改呼叫方式 |
| **失敗考古 #32** | ✅ 已立：「工具算錯 vs 工具用錯」。判別法＝**同幾何換呼叫條件**跑一次（平移 1.078e-2→2.6e-7、閉式解→~1e-11 ⇒ 鐵證是呼叫條件）。與 #31 互為對照：#31 把工具誤差推給**資料**、#32 推給**函式庫**，同一病根 |
| BLOCKED-A／BLOCKED-B 之原文 | 🚩 **未收到**（見 §〇）。上表處置係依任務 5 之標題字面推定 |

---

## 六、驗收

- `python -m py_compile app.py` → OK
- `verify/fixture_n14_min_width.py` → **23 格**（新增 ⑭）rc=0
- `verify/probes/probe_ruling_N_e1_touch.py` → rc=0（T1–T7 全綠·T7 已改 K-4 版含鑑別力格）
- `verify/probes/probe_ruling_K4_s_origin.py` → rc=0（**新增·已掛 run_all `[1/3]` 段**）
- `python verify/run_all.py` → **56 PASS／14 FAIL**，FAIL 名目對 K-4 前（`E_after_B2.log`）**diff 為空**
  ⚠️ 這**不是**「改動無影響」，而是**該欄不在比對母體**——見 §1.4。

---

## 七、🚩 上呈

| # | 題 | 性質 |
|---|---|---|
| **U-K1** | **K-4 第 3 條 vs M-5 ① 之適用序與合併來源範圍** | **域邊界·停機中**（§三） |
| **U-K2** | **`diff_rows` 不比對 got-only 欄**（`for col in b`）⇒ `真G(㎡)` 等後加欄**不在任何 baseline 閘之比對母體**。補「got 欄集 ⊆ baseline 欄集」結構閘須先重烤 v3 baseline（P-H 凍結） | 需 KL 授權 |
| **U-E3** | 對照清冊 12→13 欄（`寬度判定`） | 交付物格式異動·仍待知會 |
| ~~U-E1~~ | 「待判」筆可否算合格 | **已由 K-4 解消**（不再有待判態） |
| ~~U-E4~~ | `18.0` 之身分 | **已由您撤回指令解消**（維持具名常數） |

**既有 🚩（未動）**：`data/地籍資料來源_匿名版.xlsx` 未 commit 異動。
