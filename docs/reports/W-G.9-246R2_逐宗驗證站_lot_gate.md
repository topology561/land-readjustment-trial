# `W-G.9-246R2`　逐宗驗證站 `_lot_gate`（觀測模式）執行報告

> 🛑 本文 **⛔ 鑄任何號**。所有數字**現查於倉／當場實測**；三軸逐處載明（`VR-091`）。
> 🔴 **生產碼 ⛔ 推主線**——已推側分支 `verify/W-G.9-246-c1`，候發單側逐位復驗與 KL 逐字放行。

---

## §零　態錨

| 項 | 值 |
|---|---|
| 主線（本批基座） | `033f2811ff98a2e4a4a5e0e0a4bb0b2f9b0e04b0`（工項零·`W-G.9-246′` 原封入倉）|
| 拋棄式 clone | `p246a`（PRE）／`p246b`（POST）／`v0a246b`／`v0b246b`（`V-0`）——皆自 **origin** clone·`core.autocrlf=false`·HEAD `033f281` |
| 🔒 所驗即所交 | 工作區二檔（LF 正規化）`sha256[:16]` **＝** `v0b246b` 內二檔：`app.py 5693242b19d970c1`／`stepg_pipeline.py c8d46d2e5fb79dc5` |

🛑 **⛔ 於主 checkout 跑長跑**（`CLAUDE.md` `常規六 三`）。

🩸 **側分支之基座偏離（`常規二`·逐字回報）**：單 `§零` 逐字令「側分支 `verify/W-G.9-246-c1`（**自 `61e503b` 起**）」，
惟 `61e503b` 係**出單當時**之主線；本批之**工項零**（本單原封入倉）已於其後推進主線至 `033f281`。
⇒ 依「工項零優先於一切」，側分支自 **`033f281`** 起（其**含**本單自身）。**⛔ 回退主線。**

---

## §一　開工閘（`§零`）

| 閘 | 結果 |
|---|---|
| `S-0` | ✅ 逐位相符（受詞 `5498` B·出現 `1` 次）；判別力：全檔口徑 `043fb5d5…` ≠ 單載 ⇒ 非恆綠 |
| `S-1` | ✅ 對照乙 `W-G.9-248` `D1 0`／`D2 0`／`D3 0`（鬆框 `1`／`1` ＝ `-247` 單之對照表列 ⇒ 依補款 `②` ⛔ 占用）。併查：本號帶撇形 `W-G.9-246′` **`0`／`0`／`0`·鬆框 `0`／`0`**；母號 `W-G.9-246` `2`／`2`／`0`（與單稱「已占」相符）；對照甲 `W-G.9-247` `2`／`3`／`0` |
| `S-3` | ✅ **11/11**（十檔 ＋ 夾具）|
| `S-0b` | ✅ `rc=0`·🔴 `0` 項／🟡 `0` 項·`stderr` `0` B |

🔒 **`S-1` 之框補正**：錨定框加排除接續之 `′`（U+2032）⇒ 查 `246` 時 **⛔ 咬 `246′`**；⛔ 加此排除，母號之數會把帶撇形一併計入。

---

## §二　落地（施工形由 CC 定·單 `§零` 逐字授權）

### `a`　`_lot_gate`（`app.py` module 級·`I-1`）

置於 `_t3_gate_emit` 之後（`app.py` 之 `@@ -12283,0 +12284,194 @@`）。
**⛔ 讀 `st`、⛔ 副作用**（⛔ 改 `res`／`tp`／`blk_ctx`、⛔ 寫任何 session／檔案）。
四態**沿用既有基建**——`_lg_fmt` 對 `_T3_NA`／`None` **委派 `_t3_fmt_gate`**（得 `不適用`／`無從判定`），
⛔ 另立第二套詞彙。🔒 **`無從判定` ＝ 單所稱之「不可判」**。

| 項 | 判準 | 射程（`I-3`）|
|---|---|---|
| `A` 幾何 | `_k923_gate2(poly, W, D)` ＝ `K-9-12` 矩形（可旋轉平移）；`W`／`D` 自 `get_min_lot_size(分區, 正面路寬)` **查表**（⛔ 硬編）| 街角第 1 宗 ⇒ `不適用`；`cut_coords < 3` ⇒ `無從判定`（具名 `res.cut_coords`）|
| `B` 藍影 | `_blue_shadow_tri` → `_k923_gate1` ＝ 閘一（`G > 藍影` 嚴格大於 ∧ 臨正街 `>0` ∧ 臨屁股 `>0`）| **僅有 sideline 之鏈之第 2 宗**（碼側 index `1`）；末端塊之鏈 ⇒ `不適用`；缺輸入／構造 raise ⇒ `無從判定`（具名）|
| `C` 面積 | `e > 0` ⇒ `G ≥ e`；**街角第 1 宗** ⇒ `G ≥ max(街角規定範圍面積, e)`（`v3` ⑩）| `e ＝ 0` 且非街角第 1 宗 ⇒ `不適用`；`G` 或街角範圍面積缺 ⇒ `無從判定`（具名）|
| 總判 | `rejected ＝ 任一為「不合格」`（⛔ 計「不適用」與「無從判定」）| — |

### `b`　`_WF_NS_NAMES` 補列（`I-1`）

**`31 → 33` 筆**：`"_lot_gate"`（單所令）＋ 🔴 **`"k91_effective_min_build_area"`**（見 `§四` 之新捕）。
🔒 **AST 窮舉**：`stepg` 之 `ns[…]` **相異名 `27`**，**仍漏列者 `0`**；對照組（一必不在清單之名判為漏 ＝ `True`）⇒ 器非紅。

### `c`　街廓層 ctx ＋ 四站 ＋ 四處寫欄

- **街廓層 ctx**（每街廓組一次·二檔同構）：`app.py` 於 `_lside_right` 後；`stepg` 同位。
  `corner_range_polys`／`eff_min_build` 皆取自 **`W-G.9-247` 所鋪**（`I-7`）——⛔ 自建第二源。
- **四站**（`I-4`）：`res` 定案後、`_W_prev_* = res['W_far']` **之前**；右鏈掛於**定案之 `res`**（⛔ 各 solve 後各掛）。
  站只做二事（`I-5`）：`res['_lg_cols'] = _lot_gate(...)` ＋ 鏈索引 `+1`。**⛔ 依 verdict 寫任何 `if`。**
- **寫欄**：`_lg_row = _build_g_row(...)` → `_lg_row.update(res.get('_lg_cols') or {})` → `append`。
  🔒 **既有欄一字不改**（`I-6`）；新欄 **22** 個，皆 `驗_` 前綴。

🩸 **施工形之具名**：`I-5` 令「站只做二事：呼叫、寫欄」，惟 `I-4` 令掛載點在 `_W_prev_*` **之前**，
而 row 之 `append` 在其**後** ⇒ 二者**⛔ 同一列**。取：站呼叫並暫存於 `res['_lg_cols']`（純加性·同 `res['_alloc_dir_used']` 之既例），
`append` 處 `update` 寫欄。⇒ **每鏈二處**、**四站仍為四**。

### 🔴 `d`　`B` 之受詞 `G` —— **典間歧異·依權威序取·⛔ CC 自裁**

| 典 | 逐字 | 依其自身之宗序對照所指 |
|---|---|---|
| `docs/rulings/K-6:3261`（**權威序第 1 級**）| 「`G(第 1 宗) > 藍影面積`（**嚴格大於**）」 | 該檔以**碼側索引**行文（同檔「`B1` ＝ **第 0 宗**遠側界與 BASELINE 之交點」）⇒ **碼側 index `1` ＝ 受檢宗自身** |
| `docs/配地計算總規格_v3.md` ⑪（第 3 級）| 「`G(街角第1宗) > area(藍影)`」 | 依其宗序對照（KL 語「街角第 1 宗」＝ 碼側 `0`）⇒ **winner** |

🔒 **本實作取 `docs/rulings/`**（權威序）＝ **受檢宗自身之 `G`**。
🔒 **內部自洽之佐證**：`K-6` 同節「嚴格大於」之由逐字「`G ＝ 藍影` 時**臨 BASELINE 長 ＝ 0**」
——臨接長係**受檢宗**之幾何性質 ⇒ 該推理唯有在 `G` 為受檢宗自身時成立。
🛑 **⛔ CC 自裁孰為誤**；已於碼內 docstring 具名。**接閘（`-248`）前須定案。**

---

## §三　【驗收】逐項

**三軸**：來源 ＝ 上開四 clone（`blob@033f281` ＋ 二檔 patch）／母體 ＝ `run_all` 全 `stdout` 與 `verify/out/got_*.csv`／粒度框 ＝ **bytes**（`sha256`／`cmp`）與**列**。

### `V-0`　`run_all` 全名目 `rc` 前後相同 ✅

| 量 | PRE | POST |
|---|---|---|
| 整體 `rc` | **`1`** | **`1`** |
| `stdout` | **`49049` B／`542` 列** | **`49049` B／`542` 列** |
| 名目數（**放寬框**）| **`71`** | **`71`** |
| 判之分佈 | `{'✅': 15, '🔴': 28, '✅/rc=0': 13, '🔴/rc=1': 7, '🟡': 8}` | **逐鍵相同** |
| **判翻轉** | — | **`0`** ✅ |

🔒 **決定性檢**：二 log **僅正規化 clone 目錄名**（`v0a246b`／`v0b246b` → `<CLONE>`）後，
相異**恰 `3` 列**，且**皆為同一處 traceback 行號** `stepg_pipeline.py:1127 → :1181`（**`+54`**）。
**自洽驗算**：`stepg_pipeline.py` LF 淨增 ＝ **`+54`**（`numstat` `58` 增／`4` 刪）⇒ **相符** ✅。
⇒ 🔒 **無類 III 之實質差**；`43` 項既有紅（PRE 非 ✅ 者）於 POST 皆仍非 ✅。
🔒 **判別力**：未正規化之 `cmp` 為**異** ✅；於 PRE 末注入一字元後再正規化仍**異** ✅。

🔴 **框之更正（`自誤` 之受詞·具名）**：本報告首版之 `[A]` 框為 `^\s*(✅|🔴)\s+(PASS|FAIL)\s+`
⇒ **漏抓 `末端夾具` 之名目形**（`  ✅ 末端夾具 xxx.py（…）rc=0`），
致 `fixture_wf_ns_wiring.py` 由 ✅ 翻 🔴 時仍報「全名目逐項相同」＝ **偽綠**。
已改**放寬框**（任何 `  ✅|🔴|🟡 ` 起首之列）；**判別力自證**：窄框 `34` ／放寬框 **`71`** ⇒ 多抓 **`37`** 項。

### `V-A`　既有輸出逐位不變 ＋ `驗_*` 非全空 ✅

| 量 | `0m` | `3.5m` |
|---|---|---|
| PRE 列／欄 | `21`／`31` | `24`／`31` |
| POST 列／欄 | `21`／**`53`** | `24`／**`53`** |
| 新增 `驗_*` 欄 | **`22`** | **`22`** |
| `PRE 欄集 == POST 去 驗_* 之欄集（含序）` | **`True`** | **`True`** |
| **去 `驗_*` 後之 `sha256`** | PRE `ffe70fca3db7648c` ＝ POST | PRE `002b7127c6e0af05` ＝ POST |
| `驗_*` 有值之列 | `19`／`21` | `19`／`24` |

🔒 **「`R1` 全部宗皆有 verdict」之精確判**：無 verdict 者**恰為 `抵費地` 列**
（`推進側別 = 抵費地`·`解法 = 幾何剩餘`·`G = 0.0`·逐列具名：`R1-抵費地-1`／`-2`／`-3`、`R2-抵費地-1`／`-2`），
其**⛔ 經四站**（`I-4` 之掛載點限左右鏈）⇒ **`R1` 之全部<u>鏈宗</u>皆有 verdict（`5/5`）** ✅。

### `V-S`　觀測統計（次一單之地基）

**情境 `0m`（`21` 列）**

| 街廓 | 鏈 | n | `A` | `B` | `C` |
|---|---|---|---|---|---|
| `R1` | left | `2` | 不適用 1·合格 1 | 不適用 1·合格 1 | 不適用 1·合格 1 |
| `R1` | right | `3` | 不合格 1·不適用 1·合格 1 | 不適用 2·合格 1 | 不適用 2·合格 1 |
| `R1` | 抵費地 | `2` | —（⛔ 經站）| — | — |
| `R2` | left | `7` | 不合格 3·不適用 1·合格 3 | 不合格 1·不適用 6 | 不適用 6·合格 1 |
| `R2` | right | `7` | 不合格 5·合格 2 | 不適用 7 | 不適用 7 |

**情境 `3.5m`（`24` 列）**

| 街廓 | 鏈 | n | `A` | `B` | `C` |
|---|---|---|---|---|---|
| `R1` | left | `1` | 不適用 1 | 不適用 1 | 合格 1 |
| `R1` | right | `4` | 不合格 1·不適用 1·合格 2 | 不適用 3·合格 1 | 不適用 3·合格 1 |
| `R1` | 抵費地 | `3` | — | — | — |
| `R2` | left | `7` | 不合格 3·合格 4 | 不適用 6·合格 1 | 不適用 7 |
| `R2` | right | `7` | 不合格 5·合格 2 | 不適用 7 | 不適用 7 |
| `R2` | 抵費地 | `2` | — | — | — |

🔴 **不合格（剔除）宗 ＝ 二情境各 `9`**（`不可判` **`0`**）——逐宗具名：

**`0m`**：`628-42(1)`（left·第2宗·A 不合格 ∧ B 不合格·`G 3.82` vs 藍影 `76.222438`·Δ `-72.402438`）／
`628-34(3)`（right·第2宗·A 不合格·B **合格** `G 86.0` vs 藍影 `6.666544`）／
`628-27(1)`／`628-30(2)`（left·其後·A 不合格）／
`628-35(1)`／`628-49(1)`／`628-50(1)`／`628-51(1)`／`628-52(1)`（right·其後·A 不合格）。

**`3.5m`**：`628-34(3)`（right·第2宗·A 不合格·B 合格 藍影 `6.667126`）／
`628-42(1)`／`628-27(1)`／`628-30(2)`（left·其後·A 不合格）／
`628-35(1)`／`628-49(1)`／`628-50(1)`／`628-51(1)`／`628-52(1)`（right·其後·A 不合格）。

🔒 **`A` 之失敗皆自洽**（`W×D ＝ 3.5×14.0`）：其 `G ÷ 街廓平均深度 < 3.5 m`
——例 `147.18 / 44.47 ＝ 3.31`／`146.59 / 44.47 ＝ 3.30`／`71.60 / 44.47 ＝ 1.61`／`29.08 / 44.47 ＝ 0.65`。
🔒 **`C` 之具名（單所令）**：主跑 `eff ＝ {}` ⇒ **非街角第 1 宗**之 `C` 恆 `不適用`——**⛔ 讀為全過**。
⚠️ **街角第 1 宗**之 `C` **仍為可判**——依 `v3` ⑩ 其門檻 ＝ `max(街角規定範圍面積, 0)` ＝ 範圍面積
（實測 `R1/left 114.939693`／`R1/right 110.676142`）⇒ 得 `合格`。

### `V-S′`　`C` 之端到端（拋棄式）✅

| `X` | `R1` 鏈宗 `5` 之 `C` 分佈 |
|---|---|
| `0` | `合格 2`（**街角第 1 宗**·走 ⑩ 之 `max`）／`不適用 3` |
| **`300`** | **`不合格 3`／`合格 2`** ✅（`628-37(1)` `208.16 < 300`·`628(5)` `227.73 < 300`·`628-34(3)` `86.0 < 300`；`628-35(2)` `599.91`·`628-36(1)` `596.26` 合格）|

🩸 **與單之對照組不符（照實·⛔ 湊）**：單令「`V-S′` 對照：`X ＝ 0` 須**全** `不適用`」，
惟**街角第 1 宗**依 ⑩ 之 `max` 仍有門檻 ⇒ 實測 `合格 2`。
⇒ **「全不適用」之全稱僅對<u>非街角第 1 宗</u>成立**（實測 `不適用 3/3` ✅）。
🔒 同理，`I-3` 之「`e ＝ 0` ⇒ `C` `不適用`」與 ⑩ 於街角第 1 宗**互斥**；本實作依 ⑩（`v3` 之明文式）並具名。

### `V-D`　純函式對拍 ✅ **18／18**

自 `harvest()` 取**真函式** `ns["_lot_gate"]`（⛔ 抄本）；`'_lot_gate' ∈ _WF_NS_NAMES` ＝ `True`。

`A` 四態 ✅／`B` 四態（含二種「不適用」）✅／`C` 四態（含「街角缺範圍 ⇒ 無從判定」）✅；
邊界：`G ＝ area(藍影)` ⇒ **不合格** ✅／`G ＝ e` ⇒ **合格** ✅／⑩ `max` **二臂各一例** ⇒ 皆不合格 ✅
（臂一 門檻 `114.939693`·臂二 門檻 `164.939693`）。

🩸 **二次靶誤（皆 CC 之測試靶·⛔ 受詞）**
① `B` 之合成矩形置於原點 ⇒ **臨接長恆 `0`**、閘一必假 ⇒ 改用**真街角範圍多邊形**
（`K-8 §五`：其邊即 FRONTLINE 與 BASELINE；修後臨正街 `0.002366`／臨屁股 `3.670102`）。
② 邊界造以**經 `6dp` 捨入**之欄值充當「相等」（`5.385368` > 真值 `5.385367527840`）⇒ **偽綠**
⇒ 改**直呼 `_blue_shadow_tri`** 取全精度。

### `V-B`／`V-C` ✅

`V-B`：**刪除列 ＝ 每檔 `4` 列**，皆為 `_rows_local.append(_build_g_row(` 之**開合括號**
（其取代物 ＝ `_lg_row = _build_g_row(...)` ＋ `)` ＋ `update` ＋ `append(_lg_row)`）
⇒ **⛔ 有任何算式被刪而未以等價物取代**。`numstat`：`app.py` `269`／`4`；`stepg` `58`／`4`。
`V-C`：**十檔 ＝ 變 `2`／不變 `8`** ✅（與令相符）；**夾具 `b17826a6f7b98bf5` 不變** ✅。

| 檔 | `033f281` → 本批 |
|---|---|
| `app.py` | `445585e238c26154` → **`5693242b19d970c1`** |
| `verify/stepg_pipeline.py` | `508708b9c792f6a8` → **`c8d46d2e5fb79dc5`** |
| 餘八檔 ＋ 夾具 | **逐位不變** |

### `V-K`（KL 目檢·app 側）

⬜ **未辦**——其為 KL 之人工目檢（單載「由發單側轉呈」）。
🔒 **本批已備妥其受詞**：app 側四站已落地，`驗_*` `22` 欄將出現於 Step G 表。

---

## §四　🔴🔴 施工中捕得之三誤（皆已修·逐一具名）

### `a`　**判定式輸入錯 `90°`**（由單 `§三`「不合格宗數異常 ⇒ 先疑判定式」所觸發）

首跑 `V-S` 得**藍影面積 `6832.803047`／`12534.739056`／`6866.318414 ㎡`**——**大於整個街廓**。
🔒 **根因**：ctx 之 `alloc_dir` 我傳 `allocation_dir_block`，而其 ＝ `alloc_normal_axis(_alloc_dir_cad)` ＝ **`rot90`**
（`app.py:21304`／`stepg:563`）；`_blue_shadow_tri` 所需者為 **CAD 原始宗地分配線方向**
（`app.py:9681` 逐字「`alloc_dir` 參數＝**CAD 原始宗地分配線方向**（`f3_cad_alloc_dir`）」，
且三處 `_build_corner_range_v3` 呼叫端皆傳原始值）。
🔒 **修後**：藍影 `6.666544`／`76.222438 ㎡`（合理量級）；不合格宗 `10 → 9`；`628-34(3)` 之 `B` 由 不合格 轉 **合格**。
🔒 **⇒ 該防呆條款（「先疑判定式」）實測有效**。

### `b`　🔴 **CC 自犯 `自誤 302`**：`ns[…]` 之消費未補列 `_WF_NS_NAMES`

`V-0` 首跑捕得 `verify/fixture_wf_ns_wiring.py` 由 **`rc=0` 轉 `rc=1`**，逐字：

> `實拋 KeyError @ stepg_pipeline.py:699｜'k91_effective_min_build_area'`
> `🔴 ns 解包段拋 KeyError ⇒ app 生產路徑必崩（KeyError @ stepg_pipeline.py:699）`

我於 `stepg` 之 ctx 以 `ns['k91_effective_min_build_area']` 取有效值，**只補列了 `I-1` 所令之 `_lot_gate`**。
🔒 **修**：補列之（`31 → 33` 筆）＋ **AST 窮舉**（`stepg` 之 `ns[…]` 相異名 `27`·仍漏列 `0`·對照組非紅）。
🔑 **攔截者 ＝ `W-G.9-247` 採 `(甲)` 所修之那支夾具**——若未修它（其時因段乙之薄殼拆分而 `IndexError`），
**本紅不會被任何閘咬到**，而 `V-A`／`V-S`／`V-D`／`V-C` **四證全綠**。

### `c`　**CC 之 `V-0` 比對框偽綠**（見 `§三 V-0` 之框更正）

---

## §五　候發單側裁

| # | 受詞 |
|---|---|
| `1` | 🔴 **`B` 之受詞 `G`**：`docs/rulings/K-6:3261`（受檢宗自身）vs `v3` ⑪（winner）之典間歧異。本實作依權威序取前者；**接閘（`-248`）前須定案**，其**有土地後果** |
| `2` | `V-S′` 對照「`X ＝ 0` 須全 `不適用`」與 `v3` ⑩ 於**街角第 1 宗**互斥（`I-3` 同）；本實作依 ⑩ |
| `3` | 是否鑄號登記 `§四` 之三誤（`a` 責 ＝ CC·`b` 責 ＝ CC·`c` 責 ＝ CC）|
| `4` | `V-K`（KL 目檢）之安排 |
| `5` | 側分支基座自 `033f281`（⛔ `61e503b`）之偏離（`§零` 已具名）|

🛑 **本批⛔ 辦**：⛔ 依 verdict 動任何控制流／⛔ 動 `_solve_G_one`／`_corner_first_lot_G`／
`_run_step_g_impl` 之既有敘述（其**唯一**被改者為四處 `append` 之開合括號·已具名）／
⛔ 實作 `r2`／五級／⛔ 動 `verify/` 其他檔。
