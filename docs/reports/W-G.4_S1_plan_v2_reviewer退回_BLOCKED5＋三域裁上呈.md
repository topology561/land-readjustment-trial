# W-G.4 S1 plan **v2**：reviewer 退回 **BLOCKED ×5** ＋ 三域裁題上呈

- 日期：2026-07-17；plan v2＝`docs/specs/W-G.4_S1_plan.md`（`bc13cbc`）；**未動工**
- reviewer＝獨立·**真 harness（`app_harvest.harvest()` 真函式＋V6.dxf）實跑**·不採信 plan／補丁四任一數字
- **總判：BLOCKED（5 項）**
- **一句話**：**v2 把 step 0 當成純幾何清潔工，但它是一次 G 重定**（實測各塊右組首宗 **ΣΔG ＝ +6.51㎡**）。
  BLOCKED-1 與 BLOCKED-3 同根於此。

---

## 0. 本次退回之最重教訓（**我剛寫完 #25 就再犯同型**）

**閘③'（三楔形消滅）之理由是<u>套套邏輯</u>**——plan §5 寫「（`s 域 ⊂ [s_min,s_max]` **by construction**）」。
但 `app.py:6984-6990` 之 `_pool_strips_for_block` **現在、楔形還在的狀態下，就已無條件把 `pool_iv` 夾在 `[s_min, s_max]`**：
```python
cur = s_min
for a, b in merged:
    if a > cur: pool_iv.append((cur, min(a, s_max)))
    cur = max(cur, b)
if cur < s_max: pool_iv.append((cur, s_max))
```
→ **該句今天就是真的，對「楔形是否消滅」零舉證力。**
**此即 v1 閘②（量 helper 內部收斂 ⇒ 恆綠）之<u>同一失效模式復發</u>**——**我在 v2 修掉了 v1 的套套邏輯閘，同時新造了一個。**

---

## 1. 🔴 BLOCKED-1（致命）：step 0 **不會**消滅三楔形之二——R1／R6 之楔形在**左端**，`actual_max_proj` 碰不到

**reviewer 實測（`_strip_s_range` 真函式·逐位複現 plan §1 之三數）**

| blk | s_min | s_max | amp（正交） | **p1 楔形** `[s_min,0]` | **p2 楔形** `[amp,s_max]` |
|---|---|---|---|---|---|
| R1 | −0.3248 | 87.7155 | 90.3965 | **5.3255** | 0.0000 |
| R3 | −0.0000 | 96.8032 | 93.3461 | 0.0001 | **78.2363** |
| R6 | −3.6068 | 85.8549 | 89.3411 | **85.7064** | 0.0000 |

對照 `F.0_G值_退縮3.5m.csv`：`R1-抵費地-2 = 5.33`／`R6-抵費地-2 = 85.71` → **與 p1 楔形四位吻合；p2 楔形為 0.0000**。

**碼證（`actual_max_proj` 之全部消費端·CC 已覆核）**
- **定義於 `app.py:15475`**——而**左組迴圈已於 `15462` 結束** → **物理上不可能影響左組**。
- 消費端**僅二**：`app:15478`（`end_pt`·**右**）、`app:15512`（`S_remain`·**右**）。
- 左組起點＝`app:15430` `baseline_pt = corner_pt + left_cum_S * d_hat` → 0m 時 `left_cum_S=0` → **s=0**，
  而 **R6 街廓延伸至 s=−3.6068**。

→ **閘③' 將 2/3 紅**（只有 R3 修得掉）。

**🔴 且 plan 把<u>兩種不同的病</u>併成一種**：`s_max − amp` 之符號——**R3 +3.4571**，但 **R1 −2.68／R6 −3.49／R4 −0.16（amp <u>超出</u> s_max）**。
plan §1 以 R3 之 `+3.4571` 為例、寫成「`s 域 ⊄ [0, amp]`」——**6 塊中 3 塊符號相反**。
- **p1 楔形**（`s_min < 0`）＝**起點**錯 → 修法＝左組起點 `corner_pt → corner_pt + s_min·d_hat`——**不在 plan 範圍內**。
- **p2 楔形**（`amp ≠ s_max`）＝**終點**錯 → 即 plan 之 step 0。

## 2. 🔴 BLOCKED-2：閘③ 之 R3 錨 `5.2719` 被 plan **自己的 step 0** 作廢

**side 參數化式本身正確**（reviewer 實跑·**五錨全中**）：

| blk | side | range | 錨 | 字面式解 | f(S) |
|---|---|---|---|---|---|
| R5 | left | 300.52 | 8.6774 | 8.6774 | 300.52 |
| R2 | left | 309.05 | 8.7157 | 8.7157 | 309.05 |
| R3 | right | 308.93 | 5.2719 | 5.2719 | 308.93 |
| R1 | right | 226.88 | 9.9050 | 9.9050 | 226.88 |
| R6 | right | 299.13 | 10.5087 | 10.5087 | 299.13 |

**但 step 0（`amp := s_max`）後錨即位移**：

| blk | PRE-step0 | **POST-step0** | 位移 |
|---|---|---|---|
| R3右 | 5.2719 | **8.7290** | **+3.4571** |
| R1右 | 9.9050 | **7.2240** | **−2.6810** |
| R6右 | 10.5087 | **7.0226** | **−3.4861** |

**plan §3 之註解自己寫著「← step 0 後 amp == s_max」，§5 閘③ 卻仍以 5.2719 驗收** → **必紅，且紅的是閘、不是碼**。
**⚠️ reviewer 之警告**：下一步很可能是「回頭把 step 0 拿掉」或「改式去湊 5.2719」——**兩者都是回到 #25**。

## 3. 🔴 BLOCKED-3：閘①「業主宗 G 一字不變（絕對）」為**偽**，且與閘④ **直接矛盾**

**reviewer 以真 `solve_G_binary` 對拍**（兩次呼叫 a/A/B/C/l_front/l_side/F/side_mid/W_prev **完全相同**，唯一差異＝`end_pt` 與 `S_max_limit`）：

| blk | lot | amp | s_max | G_pre | G_post | **ΔG** |
|---|---|---|---|---|---|---|
| R1 | 628(5) | 90.3965 | 87.7155 | 213.50 | 216.17 | **+2.67** |
| R3 | 628-45(2) | 93.3461 | 96.8032 | 198.50 | 194.03 | **−4.47** |
| R6 | 628-18(1) | 89.3411 | 85.8549 | 295.61 | 303.80 | **+8.19** |
| R2/R4/R5 | — | amp≈s_max | | | | +0.00／+0.12／+0.00 |
| | | | | | **ΣΔG（僅各塊右組首宗）** | **+6.51** |

**機制**（碼證）：`amp → S_conv → W = W_prev + S·cos_dn`（`app:7147`）→ `Rw = rw_increment(...)`（`7148`）→ `G_target`（`7153-7155`）。
→ **即 v1 reviewer 坐實之 `buffer_S → W₀ → Rw → G` 硬接線，只是這次由 <u>amp</u> 觸發。**

**三推論**
1. **閘① 與閘④ 互斥**：①說每宗 ΔG=0；④說 ΔΣG=+3.62。**不可同時為真。**
2. **🔴 CC 於 plan §7-3 提議之替代絕對閘（「0m 之 `G(㎡)` 欄逐格 byte 相同——G 不隨幾何動」）<u>立刻紅</u>**
   ——上表即 **0m 態**（W_prev=0、零 forced），**G 仍動到 +8.19**。**G 經 Rw 隨幾何動。**
3. plan §5 註「閘①-0m 之範圍收縮」**方向對但收縮不夠**：不只池片幾何動，**0m 之 G 欄本身就動**。

## 4. 🔴 BLOCKED-4：step 0 之範圍漏 `wf_f4:1124-1126`（正交 amp 之**第三處**）

**reviewer 窮盡搜（N0-17-c）**：`max(_proj_pts)`→`app:15475`／`stepg:573`；**`max(proj)`→`wf_f4:1126`**；
**`corner = p1 +`→`wf_f4:1126`**（CC 已覆核命中）。
```python
# verify/wf_f4.py:1124-1127 ← 與 app:15469-15478／stepg:567-576 同型
proj = [float(np.dot(np.array([v[0] - p1[0], v[1] - p1[1]]), d_hat)) for v in blkm["vertices"]]
corner = p1 + max(proj) * d_hat
```
**plan §1 只寫「`stepg:563-569`／`app` 同構」→ 改兩處不改第三處 ＝ #20 抄寫複本漂移**
——**正是 plan §3 那張「四處同步表」要根絕的東西；step 0 也需要一張同樣的表。**

## 5. 🔴 BLOCKED-5：閘④ 之基準 `2299.26` 與倉態衝突 → **以倉為準：2298.80**

- **F.4（trunk E）Σ池 3.5m 實算 ＝ 2298.80**（CC 已覆核）；plan §5／規格 N0-7／**補丁四 §三-(a)** 皆稱 **2299.26**。
- **2299.26 溯源＝`docs/reports/W-G.4_v3偵察補查報告.md:111`，係 S0b/S0c <u>之前</u>之量**
  （其池片 R1 5.30／R3 389.80／R5 212.79／R6 85.66；**現行倉態** 5.33／389.85／212.84／85.71）。
- **且 ΔΣG +3.62 係 S1-only 之預測**（v1 reviewer 於 **S0c 態**所量）；v2 併的是 **S0d＋step 0＋S1**
  ——**光 step 0 各塊首宗就 +6.51**。→ **「2299.26 → 2295.64」三個數在 v2 下全部失效。**
- **方向正確**（`grep "2299.26" --include=*.py` → **0 命中**＝散文閘·轉真閘正確；落點 `run_verification` 重烤驗收段正確）
  ——**惟基準須重取自倉**。
- **🔴 CC 之責**：**我自己剛做完重烤**（`d9a8b05`），卻仍引 pre-重烤之 2299.26 入 plan——**且該數已隨我的引用寫進補丁四**。

---

## 6. ⚠️ WARNING（reviewer 提·CC 已覆核）

- **W-1 🔴 §2 行號全數過時，且 `app.py:6821` 會導向<u>錯的函式</u>**（CC 已實測）：
  `_S_cut = round(S_conv, 2)` 實在 **`app.py:7209`**；**`6821` 現為 `_acct_geom_tol_per_lot` 之 docstring 一行**
  ——而該函式**貌似相關**（帳對幾何閘寬）→ **照 plan 字面改必動錯函式**。
  同理 `stepg:541/632`→**545/636**；`547/638`→**551/642**；規格之 `6792/6804/6836`→**7171/7183/7239**。
  **CC 之責**：行號自規格（`100cfa3` 基準）抄入，**未依自己插入 helper 後之位移重驗**。
- **W-2 `_WF_NS_NAMES` 16→17 少算 → 應為 16→18**：`_strip_s_range` **不在** `_WF_NS_NAMES`，而 step 0（`stepg:573`／`wf_f4:1124`）與 helper 皆需之；stepg 一律走 `ns["..."]`。
- **W-3 S0d 之「閘① 不受影響」只對半**：實切回復 `S_conv` 確在 G 定值之後 → 本宗 G 不動 ✓；
  但「推進改吃未捨入 S」→ 改 `cum_S` → **下一宗 `baseline_pt` 移動 ≤0.005·n → 下游宗 S/W/Rw/G 全動**。量級小，**但陳述錯**。
- **W-4 `W_far` 仍是 `round(W_conv, 2)`（`app:7240`）→ 一個長度量化<u>仍留在</u> W→Rw→G 路徑上**
  → **「S0d 後同源於全精度」之<u>通稱</u>不成立**，宜改「**S 之量化退出路徑**」。
  （惟 **tol 算術逐項核過<u>是對的</u>**：0.015／宗數×0.015／E3 整形宗數×0.005——因 W 之量化對「帳」與「幾何」**同向**作用、不開帳-幾何縫。）
- **W-5 🔴 `_acct_geom_tol_per_lot` docstring（`app:6818-6819`）仍載<u>已作廢</u>之域框架**
  （「本差＝對照清冊『增減(㎡)』欄、由差額地價找補之法定常態」）；**同樣的話在 `stepg:770`**。
  **單一真相源函式內留作廢法理＝下一輪的引用來源**（§2 只說改係數、未說清此段）。
- **W-6 閘② 之身分鍵**：錨源 `F.0_G值` 抵費地列**確為真實產物、非套套邏輯**（R5 212.84／R2 233.79／R3 389.85 全與 range 不等 → **閘現在就紅、有鑑別力**）✓；
  **惟 plan 未指定「怎麼認出哪一列是 forced 帶」**——`{blk}-抵費地-{i}` 係**面積遞減序**（`app:6934`）＝**artifact**
  → **違反 plan 自己引之正典補條（數值錨須帶身分鍵）**。
- **W-7 `wf_f1` 補 right 分支不只是加變數**：`wf_f1:195` 寫死 `lbl="R1"`、`204` `corner_pt=p1`、`220`
  → **整個右側鷹架（`end_pt`／`d_hat_rev`／`max(proj)`）都不存在**。plan §0「side 參數化即解」**低估**。
- **W-8 §N1 左側帶 `[s_min, buf]` 會把 p1 楔形吞進 forced 角落抵費地**（當 `s_min<0`）：
  現行 forced 左側僅 R5(+0.2685)／R2(+0.2677) → **不觸發**；**S2／泛化會**。

## 7. ✅ PASS（reviewer 自驗·採認）

四處行號（逐行 sed 全中）／**side 參數化式命中五錨**／`wf_f1` 無 right（`grep right_buffer_S` **0 命中**）／
閘② 錨源非套套邏輯＋`f(0)=0` 該刪正確（`app:6750-6751` 早退恆真；R3右 `f_true(0)=78.24≠0`）／
0m 零 forced（`F.0_滑池槽診斷_退縮0m.csv` 角落抵費地 L/R **六塊全 0.0**）／
scope guard `_pool_strips_for_block`「值變、法不變」正確（`app:6960-6961` 自註不倚賴 `actual_max_proj`）／
`app:16215` 診斷非閘／**app 無 telescoping 閘**（`W0_left/Wf_left/_tele_exp/ΣRw_實跑` 於 app **各 0 命中**）／
`app:6053/6056` range 記帳 ✓／守恆（F.0·3.5m 逐街廓全 <1㎡·Σ=+0.11）／N0-18b 等引用實質相符。

---

## 8. 📮 上呈 KL／claude.ai（**reviewer 明言域外·CC 亦不裁**）

1. **左組起點該在 `s=0`（FRONT_LINE p1）還是 `s=s_min`（街廓 s 極值）？**
   **R6 街廓在 FRONT_LINE p1 之後仍延伸 3.6068m** → 該 **85.71㎡** 該配、該入池、**還是根本是製圖問題（FRONT_LINE 沒畫到底）**？
   → **法規／製圖 SOP 判斷**。**BLOCKED-1 之修法完全取決於此。**
2. **forced 角落抵費地是否應吞併 p1／p2 楔形？**
   §N1 左帶 `[s_min, buf]`／右帶 `[amp−buf, s_max]` 之字面式**會把楔形算進 range 帶**；
   而 N0-10 要求「嚴格等於 range」、range 係 `_build_corner_range_v2` 之真實多邊形
   → **兩者是不是同一塊地，是域問題**。
3. **step 0 改 amp 會改 G（實測 ΣΔG +6.51／首宗）→ 這是「修好」還是「新的重定」？**
   現行 R1/R6 之 **W 從街廓外 2.7~3.5m 起算**（Rw **68.56 vs 55.01**）——照附件二「**向宗地分配線作垂線**」之本意，哪個對？
   → **法規本意·交 KL＋claude.ai。**

**⚠️ 併請裁**：BLOCKED-5 之基準——**Σ池 應以倉態 2298.80 為準**（補丁四 §三-(a) 之 2299.26 係 pre-S0b/S0c 量·請更正）。

---

## 9. CC 之處置與自評

- **plan v2 不動工**，俟 §8 三題（尤其 1、3 決定 step 0 是否成立、以何形式成立）。
- **reviewer 採認 v2 相對 v1 之真實進步**（side 參數化命中五錨／閘② 錨源／刪 `f(0)=0`／`ΔΣ池=−ΔΣG` 轉真閘）**建議保留** → CC 採認。
- **CC 本輪自評（四則·皆由 reviewer 實跑照出）**
  1. **把 step 0 當純幾何清潔工，實為 G 重定**（ΣΔG +6.51／首宗）——**根**。BLOCKED-1/3 同源於此。
  2. **套套邏輯閘復發**：我在 v2 修掉 v1 之閘②，**同時新造閘③'**（`s 域 ⊂ [s_min,s_max]` by construction 今天就真）
     ——**#25 之墨未乾即再犯**（#24 亦載「墨未乾即再犯」之前科）。
  3. **兩種病併成一種**：p1 楔形（`s_min<0`·起點）vs p2 楔形（`amp≠s_max`·終點）；且 **6 塊中 3 塊 `s_max−amp` 符號相反**
     ——**我以 R3 之單例（+3.4571）推全域**，正是 **#25**（單點對未參數化維度零舉證力）**之同型**。
  4. **引用未重驗**：`app.py:6821`（自規格 `100cfa3` 基準抄·**未依自己之插入位移重驗**·且 6821 現為貌似相關之函式 docstring）；
     **2299.26**（**我自己剛做完重烤**卻引 pre-重烤值·且該數已隨我的引用寫進補丁四）。
     → **通則：跨波引用之行號與錨，須以<u>當前倉態</u>重驗——尤其在自己剛動過該檔之後。**
