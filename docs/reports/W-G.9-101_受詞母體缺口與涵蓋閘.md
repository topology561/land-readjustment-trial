# `W-G.9-101`　純追加稽核器之**受詞母體缺口** ＋ 涵蓋閘

**基座**：`origin/wip/s1-endpart` ＝ `65f575bd5c95c301eeec83ddffe764beebe25dda`
**性質**：⛔ 零生產碼・⛔ 零管線・⛔ 不落地・新增 `4` 檔 ＋ 三檔純附加 ＋ 一檔純插入
**探針**：`verify/probes/probe_WG9103_targetcov.py` ＋ `verify/out/probe_WG9103_targetcov_65f575b.log`
**閘**：`verify/tools/wg9103_target_coverage.py`

> 🔒 **一切量之框與態皆逐項具名**（`VR-057 一`／`VR-058 一`／`三`）。
> 🔒 土地三數（`ΣΔ ＝ +1.0300`／池增量 `84.0800 ㎡`／`R1 ΣΔ ＝ +0.000000`）係
> **依守恆式導出·⛔ 非幾何實量**之**未重算之轉述**（`VR-054 三`）——本批**零管線**、⛔ 未重算。

---

## §零　四句結論

1. **`wg942_append_audit` 之「綠」⛔ 不蘊含「本批之受詞皆已受檢」**——其受詞由人工維護之硬編清單 `TARGETS` 界定；
   機制錨（引被檢物逐字）＝ `verify/tools/wg942_append_audit.py:485@65f575b` ＝ `    for path, kind, head, tail in TARGETS:`
   ⇒ 未列者迴圈**永不到訪** ⇒ `bad` ⛔ 不可能因之遞增。
2. **缺口已量化**：母體 ＝ commit 區間 `a331c3c..65f575b`（**`99`** 個 commit）；
   被修改之 `.md` 實例 **`262`**／⛔ 不在涵蓋內 **`20`**（**`7.6%`**）／含缺口之 commit **`17 / 99`**／相異缺口檔 **`12`**。
3. **二器異判已實測**（同一輸入·⛔ 非推論）：`shim.md` 被附加而 `TARGETS` 未補登時，
   `wg942_append_audit` ⇒ `rc = 0`·`結論：✅ 全部成立`；新閘 `wg9103_target_coverage` ⇒ `rc = 1`·`結論：🔴 1 項未涵蓋`。
4. **本批處置**：`TARGETS` 補登 `2` 項 ＋ 新增涵蓋閘（受詞 ＝ **本批**·附 `SELFTEST-1`／`SELFTEST-2` 強制自檢）；
   ⛔ **本批不掛 `run_all`**（涉其 `rc` 語意與期望 FAIL 名單之耦合·`GB-88 五`）。

---

## §一　`S-1`〜`S-8` 逐格（態 ＝ `blob@65f575b`·⛔ 非工作區）

> 🔒 探針之 `TARGETS` **自 `blob@65f575b` 以 `ast` 解析**（⛔ 不自工作區 import）
> ⇒ 本批之補登**不使 `S-1`／`S-8` 漂移**（`VR-058 三` 之自我適用；其直檢見 `§三` 驗收 `11`）。

| # | 項 | 實得 | 判 |
|---|---|---|---|
| `S-1` | `TARGETS` 結構 | 項數 `75`／檔級 `52`（相異 `52`·重複登錄 `0`）／段級 `23`（相異檔 `8`）／**涵蓋相異檔 `60`**；`kind` 值集 ＝ `['檔', '段']` | ✅ |
| `S-2` | 受詞缺口 | commit `99`／被修改之 `.md` 實例 **`262`**／⛔ 不在涵蓋 **`20`**（**`7.6%`**）／含缺口之 commit **`17 / 99`** | ✅ |
| `S-3` | 缺口逐檔 | 相異 **`12`** 檔（框 ＝ **該檔出現於幾個 commit**·⛔ 非出現次數）；見下表 | ✅ |
| `S-4` | 🔒 判別力對照（`core.quotepath` 二框） | `quotepath=true` ⇒ **`0`**／`false` ⇒ **`3`**；**擾動量 ＝ `3` > `0`** ⇒ ⛔ 未退化 | ✅ |
| `S-5` | 🔒 否證通道 | 布林「缺口集為空」＝ **`False`** | ✅ |
| `S-6` | 本系列逐 commit | `02964ba 1/1/0`｜`3103ab5 2/1/1`｜`dea0ba3 3/2/1`｜`84bb7a6 0/0/0`｜`d488970 3/2/1`｜`b0d0db8 3/2/1`｜`65f575b 3/2/1`；缺口實例合計 **`5`**；`gap` 逐 commit 皆恰為 `shim.md` | ✅ |
| `S-7` | 🔒 機制歸因（引實作逐字） | 見下 `§一-3` | ✅ |
| `S-8` | 具名之單一受詞 | `shim.md` ∈ 涵蓋 ＝ **`False`**；其於本系列被修改之 commit 數 ＝ **`5`** | ✅ |

### 一、`S-3` 缺口逐檔（⛔ 全列·框 ＝ 該檔出現於幾個 commit）

| 檔 | commit 數 |
|---|---|
| `docs/reports/W-G.9-99_乙之遞補鏈shim.md` | **`5`** |
| `docs/reports/W-G.9波_新session交接文_WG913-WG931.md` | `4` |
| `docs/design/W-G.9-30_∥SIDELINE界線之接線設計.md` | `2` |
| `docs/reports/W-G.9-19_下界估算未達與同源前置.md` | `1` |
| `docs/reports/W-G.9-22_三值對目標偏差.md` | `1` |
| `docs/reports/W-G.9-23_僅修非forced之B選槽.md` | `1` |
| `docs/reports/W-G.9-25_Read自報行數量測.md` | `1` |
| `docs/reports/W-G.9-35_破量世代校正.md` | `1` |
| `docs/reports/W-G.9-38_相鄰性判準更正與楔形幾何.md` | `1` |
| `docs/reports/W-G.9-97_重排後遞補之入裁與現查.md` | `1` |
| `docs/reports/W-G.9-97補正②_逐字補記與K-9-17入倉.md` | `1` |
| `docs/reports/W-G.9-97補正③_自誤101-105與補款③.md` | `1` |

`POPULATION=12 PRINTED=12 SUPPRESSED=0`

### 二、`S-4` 之逐字樣本（二框並列·`VR-058 二` 之要求）

| 框 | `M(.md)` | 逐字樣本（首列） |
|---|---|---|
| `quotepath=true`（git 預設） | **`0`** | `M\t"docs/reports/W-G.9-99_\344\271\231\344\271\213\351\201\236\350\243\234\351\217\210shim.md"` |
| `quotepath=false` | **`3`** | `M\tdocs/reports/W-G.9-99_乙之遞補鏈shim.md` |

🔒 **後果之形**：母體歸零 ⇒ 迴圈零次 ⇒ `bad` 停留於初值 ⇒ **閘恆綠**（此「恆綠」係承 `GB-61`／考古 `65` 族之既有字樣）。

### 三、`S-7` 機制歸因（**引被檢物實作逐字**·⛔ 非數值吻合·家法 `12`）

`verify/tools/wg942_append_audit.py:485@65f575b` 逐字 ＝

    for path, kind, head, tail in TARGETS:

同檔 `:524@65f575b` 逐字（結論列）＝

    print(f"  結論：{'✅ 全部成立' if bad == 0 else f'🔴 {bad} 項不成立 ⇒ S-5 觸發'}")

🔒 **反例搜尋**（`bad` 之全站點·母體 ＝ `blob@65f575b`·框 ＝ 命中列數）：

| 列 | 縮排 | 角色 |
|---|---|---|
| `:482` | `4` | 初始化 `bad = 0`（迴圈**外**·迴圈於 `:485` 縮排 `4`） |
| `:513` | `16` | `bad += 1`（迴圈**內**） |
| `:519` | `12` | `bad += 1`（迴圈**內**） |
| `:524` | `4` | 讀取（結論列） |
| `:525` | `4` | 讀取（`return`） |

⇒ **遞增站點恰 `2`、無一在迴圈外** ⇒ **反例 ＝ `0`**
⇒ 「未列者之附加⛔ **不可能**使 `bad` 遞增」係**機械可證**、⛔ 非修辭。
⛔ **會使本判為否之輸入**：`bad += 1` 出現於 `:485` 迴圈之外（縮排 `≤ 4`）。

---

## §二　`T-1`〜`T-3` 逐格

| # | 項 | 實得 | 判 |
|---|---|---|---|
| `T-1` | 涵蓋閘（本批·補登後） | `rc = 0`；結論列逐字 `結論：✅ 本批受詞全數受涵蓋`；**涵蓋相異檔 ＝ `62`**；受詞 **`3`** 筆皆 `✅ 已涵蓋` | ✅ |
| `T-2` | 判別力自檢 | `SELFTEST-1` ＝ **`True`**；`SELFTEST-2` ＝ **`True`** | ✅ |
| `T-3` | 🔒 反證重演（⛔ 未入倉·⛔ 已還原） | 二器**異判**：新閘 `rc = 1`／`wg942` `rc = 0` | ✅ |

### 一、`T-1` 之受詞三筆（狀態 `M` ∧ 尾綴 `.md`·態 ＝ 工作區）

    ✅ 已涵蓋  docs/reports/W-G.4_泛用阻塞項登記表.md
    ✅ 已涵蓋  docs/reports/W-G.9波_claude.ai側自誤登記.md
    ✅ 已涵蓋  docs/驗證裁定登記表.md

🔒 **具名排除集**（本批恰 `1` 項·`VR-059 二`：⛔ 不得以「其餘沿用」一句帶過）：
前綴 `verify/` —— 理由逐字 ＝「碼與 log：本器受詞係『純追加之受詞』，而 `verify/` 下之碼係**就地改**的（`W-G.9-70` 所立之判準）」。
⇒ 本批之 `verify/tools/wg942_append_audit.py`（`+15/-0`·**檔中插入**）即落入此排除。

### 二、`T-2` 之逐字（性質直檢·⛔ 不綁版本字串·家法 `4`）

`SELFTEST-2`（`core.quotepath` 框已生效之直檢）：

    `quotepath=true`  首列 ＝ 'M\t"docs/reports/W-G.4_\346\263\233\347\224\250\351\230\273\345\241\236\351\240\205\347\231\273\350\250\230\350\241\250.md"'
    `quotepath=false` 首列 ＝ 'M\tdocs/reports/W-G.4_泛用阻塞項登記表.md'
    ⇒ 二框輸出相異 ＝ True ；併呈 `M(.md)`：true ⇒ 0 ／ false ⇒ 3

`SELFTEST-1`（判別力）：注入合成路徑 `docs/reports/__WG9103_SYNTHETIC_UNCOVERED__.md` ⇒ 判為未涵蓋 ＝ **`True`**。

### 三、`T-3` 反證重演（🔒 ⛔ 未入倉·跑畢已 `git checkout --` 還原）

**情境**：`TARGETS` **未補登** ∧ `shim.md` 被純附加（暫時哨兵一列）。

| 器 | `rc` | 結論列逐字 |
|---|---|---|
| `verify/tools/wg942_append_audit.py`（既有） | `0` | `結論：✅ 全部成立` |
| `verify/tools/wg9103_target_coverage.py`（新） | `1` | `結論：🔴 1 項未涵蓋` |

新閘並印出應補之 `TARGETS` 列逐字（可直接貼用）：

    ("docs/reports/W-G.9-99_乙之遞補鏈shim.md", "檔", None, None),

🔒 **還原之證**：`shim.md` 之 `git hash-object` 與 `git rev-parse HEAD:<該檔>` 逐位相同
（`e3a28fe780074e34fedc2b9be672e0493cd28167`）⇒ 工作樹無殘留。
⇒ **擾動量 > `0`**（二器異判）⇒ 坐實「綠⛔ 不蘊含母體完備」（家法 `5`／`VR-053 五`）；停機⑨ **未觸發**。

---

## §三　`§Z` 入倉三塊（本報告只留指標·⛔ 不重述其內容）

| 塊 | 落點 | 期末口徑（`splitlines()` 框） |
|---|---|---|
| `VR-059`（母體缺口之定義／涵蓋須自證／`core.quotepath` 須明綁／歷史⛔ 不追溯為紅／保留哨兵識別字） | `docs/驗證裁定登記表.md`（`+59/-0`） | `3228` 列／`243316` B／`eabedc259f1118d1efd3e5f127a438cc5207cba168ad681794b0b5a76f582567` |
| `GB-88`（受詞母體缺口·含 `GB-87` 讓號之逐字理由） | `docs/reports/W-G.4_泛用阻塞項登記表.md`（`+87/-0`） | `2485` 列／`371727` B／`23c3b6e563f8302736371d5cf9062398533f9842f798cb083a98240dc3a43a1c` |
| 自誤 `117`（自陳母體與量測母體錯配·發單側付印前自捕） | `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`+12/-0`） | `2861` 列／`197565` B／`64173f5d197238e693b9131a23f38a871f0d3d3cbd955f37a1939d7554430699` |
| `TARGETS` 補登 `2` 項（**檔中插入**·於 `:431` 錨後、`:432` `]` 前） | `verify/tools/wg942_append_audit.py`（`+15/-0`） | `544` 列／`46204` B／`2317a3b17881c893ae15b0cb819707b5ac55161daf1c38bb2dd5dd3d58bef115` |

**補登之生效性（逐項具名·⛔ 不含糊）**：

- `docs/reports/W-G.9-99_乙之遞補鏈shim.md` 於基座**已存在**（`912` 列）⇒ **本次登錄即生效**。
  實證：期末 `wg942_append_audit 65f575b` 之輸出**新增**一列
  `✅ [檔] docs/reports/W-G.9-99_乙之遞補鏈shim.md`（`35217 → 35217`·嚴格前綴 `True`·判別力自檢 `False`）
  ——🔒 **此列於基座⛔ 不存在**，係本批之增益。
- `docs/reports/W-G.9-101_受詞母體缺口與涵蓋閘.md`（即本檔）於基座為**新檔** ⇒ 本器之處置為「略過」
  ⇒ **本次登錄不產生實質檢查、自下一批起方生效**（⛔ 未為此放寬任何判準）。

**取號之現查**：`GB-87` ⛔ 不取——其全倉**命中列數 ＝ `3`**，三筆全係「保證不存在之對照組」用例
（`.claude/skills/failure-archaeology/SKILL.md`／`docs/reports/W-G.9-72_GB86訂正入倉.md`／
🔴 `docs/驗證裁定登記表.md`——**其一落在 `VR` 之裁示本文內**）⇒ 定義之將使該三處由真變假 ⇒ **讓號**、改取 `GB-88`。
🔒 讓號已以逐字 `為缺號` 登記 ⇒ `wg973_gb_integrity` 期末：`rc = 0`、缺號集 ＝ `['GB-12', 'GB-87']`、
逐字 `懸空 ＝ 母體 88 − 有定義列 86 − 缺號 2 ＝ 0`。

---

## §四　⛔ 本批未涵蓋之事（逐項具名·⛔ 不以「其餘沿用」帶過）

1. ⛔ **不掛 `run_all`**——涵蓋閘之掛載涉 `run_all` 之 `rc` 語意與期望 FAIL 名單（`GB-52`／`GB-61`）之耦合，
   須**同批裁決**（`GB-88 五`）⇒ **指定為下一單之受詞**。
2. ⛔ **不回溯補登其餘 `11` 檔之缺口**——⛔ 未逐檔查其是否仍為活受詞、⛔ 不臆造；
   涵蓋閘生效後，該類漏登於**下批即變紅** ⇒ ⛔ 不需回溯補齊。
3. ⛔ **不普查其餘既有工具是否受 `core.quotepath` 之害**——現查 `quotepath` 於 `verify/` 命中檔數 **`0`**
   （即**無一明綁**）⇒ 只登記（`GB-88 一`）、本批⛔ 不逐一追究。
4. ⛔ **不落地**（`GB-67` 之 `R2 45.9766`／`R5 56.3293`、`R4` 結構閘 `Δ = 2.415` 皆未修）；
   ⛔ 不改任何生產碼；⛔ 不跑管線；⛔ 不重算土地三數。
5. ⛔ **不動** `VR-053`〜`VR-058` 之裁示本文一字；⛔ 不動 `K-6`／`K-9` 任一字；
   ⛔ 不改任何既有探針或 log 一字。
6. ⛔ **本報告未設模態詞盤點**——本批之盤點受詞係**四段 payload**（其逐段實得見 `§五-3`），
   ⛔ 非本報告；⛔ 不以「合計」代「逐段」（自誤 `117` 之修法）。

---

## §五　🩸 CC 側併呈（本批施工中之三則·⛔ 皆非被檢物之錯）

### 一、量測器踩「含空格檔名」⇒ 假紅

首版之 `A0-13` 掃描器以 `ls-tree` 輸出之 `.split()` 取檔名，而 `verify/baselines/` 下有**含空格之路徑**
⇒ 路徑被切斷、`git show` 報 `fatal: path ... does not exist` 而中止。
🔒 **改正**：一律 `splitlines()`；本項與倉內既登之「`splitlines()` vs `.split()` 使母體由 `298` 灌水為 `365`」同族。
⇒ **紅的是量測器、⛔ 不是被檢物**。

### 二、`恆` 之分層器過窄 ⇒ 自誤段誤判

首版分層器之「被引受詞」只認 `` `恆` ``（單字反引號），漏了 `` `恆 3` ``／`` `恆 2` ``（多字 code span）
⇒ 自誤段得 `名詞 3／被引 2／恆綠 0／模態 2`，與單載之 `3/4/0/0` 不符（**總數 `7` 二者相同**）。
🔒 **改正**：「被引受詞」＝ 落在**任一反引號跨度** `` `[^`\n]*` `` 內者；改正後四段逐格與單載相同，
且全批**模態新斷言合計 ＝ `1`**（＝ `GB` 段「`bad` 恆 `0`」，其反例搜尋見 `§一-3`）。
🔒 已附**四造判別力對照**（純名詞／純被引／純恆綠／純模態各一，`4/4` 皆如預期）。

### 三、🩸 驗收 `11` 之「log `61` 列」⛔ 非本探針之不變量

單載 `驗收 11` 併記「log **`61`** 列」；本探針之 log 實得 **`96`** 列（`splitlines()` 框／`8222` B）。
🔒 該 `61` 係**發單側自撰之同名探針**之輸出格式所致，⛔ 非本探針之不變量——
**二側之探針係各自實作、⛔ 非同一檔**。
🔒 該格之**實質半**（「補登前後重跑之輸出逐位相同」⇒ 證 `TARGETS` 確自 `blob@65f575b` 取）**已驗**：
基座態與補登後之 log `sha256` 皆 ＝ `5f16e6da77c35a0f12cacbf6be9b5f086cc9c9e77514db276a84153067582df3`（逐位相同）。
⇒ 依 `VR-058 三`，列數之出艙須具名其**態與所量之檔**；⛔ 我未調整輸出去湊 `61`（家法 `6`）。

### 四、🩸 `A0-7` 之哨兵 `VR-999` 有二讀

該格逐字為「`VR-059` **全倉**命中 `0`；哨兵 `VR-999` 命中 `0`」——「全倉」只標在 `VR-059` 上。
🔒 實得：母體 ＝ **本列受詞**（`docs/驗證裁定登記表.md`）⇒ `VR-999` 出現次數 **`0`** ✅；
母體 ＝ **全倉** ⇒ `VR-999` 命中於 `7` 檔（皆為既有報告內之哨兵用例）。
⇒ 我採**本列受詞**之讀法（與同列 `窄樣式` 之母體一致）並判為符；
⛔ 未擴讀為全倉、⛔ 未改樣式去湊。該格之母體宜依 `VR-058 三` 補具名。

---

🔒 **本報告之一切數皆現查於本批**；⛔ 無轉引項。土地三數係**未重算之轉述**（見文首）。
