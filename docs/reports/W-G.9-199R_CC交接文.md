# 🧾 `W-G.9-199R` CC 交接文（換 session）

> **出文窗**：CC（本 session）　**出文時態**：`origin/wip/s1-endpart` ＝ `945d216c895eb31abe840a80c8a2378af8d08748`
> **性質**：⛔ **零生產碼**·⛔ **未鑄任何號**
> **下一單**：🛑 **`W-G.9-198`（`K-6-B` 段一上鎖 ＋ 名次 ＋ 段二全域排序）——⛔ 本窗未開工，俟新對話開之。**

🔒 **本檔一切計數皆於同段具名 ① 母體 ② 框（`列`／`次`／`格`／`檔`）③ 產生該數之命令受詞**（`自誤 244`／`247`／`249` 之三重一般式）。

---

## `§零`　接手窗第一動作（🛑 **⛔ 得跳過**）

```
git ls-remote https://github.com/topology561/land-readjustment-trial.git refs/heads/wip/s1-endpart
git rev-parse HEAD:app.py
git rev-list --left-right --count origin/wip/s1-endpart...HEAD
git config --get core.autocrlf
```

| 受詞 | 出文時之值 |
|---|---|
| `origin/wip/s1-endpart`（全 `40` 碼） | **`945d216c895eb31abe840a80c8a2378af8d08748`** |
| `HEAD:app.py` blob | **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** |
| `left-right` | `0  0`；`git status --porcelain` 之**已追蹤異動 ＝ 空** |
| `core.autocrlf`（主 checkout） | `true`（🟡 出艙用·⛔ 非判準） |

🛑 **⛔ 得以 commit 身分為開工閘之判準**（`自誤 246`／`252` 之一般式）——判準須綁**性質**：
① `HEAD:app.py` blob ＝ `b9a50a2e…`；② `<基座>..HEAD` 之變更檔**逐列⛔ 不含** `app.py`／`verify/**.py`。

🔴 **工作副本 ＝ 主 checkout** `C:\Users\admin\Desktop\land-readjustment-trial`
（⛔ **非** session 開窗時之 worktree——`-190R` 以降之全部施工單與證據檔僅存在於主 checkout；一切 `git` 操作以 `git -C` 對其執行）。

---

## `§一`　取號次號（🛑 **現查值·⛔ 不得硬編**·母體與框如欄）

| 類 | 母體 | 框（逐字） | 相異 | MAX | 缺號 | **次號** |
|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔** | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `217`【列】 | `252` | — | **`253`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**·三形聯集·**扣哨兵 `GB-997`** | 式A／式B／式表 | `134`【列】 | `136` | `[12, 87]` | **`137`** |
| `VR` | `docs/驗證裁定登記表.md` **單檔**·扣哨兵 `VR-999` | `` ^## `VR-([0-9]{1,4})` `` | `71`【列】 | `86` | — | **`87`** |
| 戒 | 全倉 `.md` | `CLAUDE.md:826` 之正典框 | `22`【列】 | `39` | — | **`40`** |

🛑 **取最大號 → 登記表單檔；查占用 → 全倉**（`CLAUDE.md`「🔧 一」·`W-G.9-192-PRE-b` 落地）。
🛑 **意指占用之判準 ＝ 裁 `H`**：命中數係**待歸類之材料**、**⛔ 非判準**；operative 判準 ＝ 「**逐筆歸類皆為零對照／判別力對照**」。**命中非 `0` 而未逐筆具名歸類 ⇒ 停機**；母體**⛔ 得扣除本單**。

---

## `§二`　倉態量（出文時·母體 ＝ `git ls-files`／`git show HEAD:<檔>`）

| 受詞 | 值 |
|---|---|
| 追蹤檔 | **`1560`**【檔】／`.md` **`495`**【檔】 |
| `CLAUDE.md` | **`1236`**【列】 |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | **`3663`**【列】 |
| `app.py`（blob） | `1383993` B／`\n` `23462`【次】／`CR` `0`【次】 |
| `app.py`（工作樹·`autocrlf=true` 展開） | `1407455` B／`CR` `23462`【次】——**正常、⛔ 非污染** |

---

## `§三`　本 session 之全部 commit（`7fab020..945d216`）

| rev | 訊息首列 |
|---|---|
| `9fc36cc` | `W-G.9-197：自誤 246/247/248 ＋ GB-133 補記 ＋ 鑄 GB-135/136 ＋ KL UI 實跑證據二檔入倉 ⛔ 零生產碼` |
| `79cfa79` | `W-G.9-197R 執行報告入倉（A-2）⛔ 零生產碼` |
| `945d216` | `W-G.9-199：自誤 249/250/251/252 ＋ 裁 H ＋ GB-135 編號現查更正 ＋ 常規補款三則 ⛔ 零生產碼` |

🔒 **三 commit 皆零生產碼**；`app.py` blob 自 `d46dfe6`（`W-G.9-194` `GB-133` 解除）起**未再變動**。

---

## `§四`　🛑 下一單 ＝ `W-G.9-198`（**⛔ 本窗未開工**）

**檔**：`docs/orders/W-G.9-198_施工單_K6B段一上鎖與名次與段二全域排序.md`
**態**：🛑 **未追蹤**（`git status` 為 `??`）——**⛔ 未 `git add`、⛔ 未入倉**。
**量**：`18865` B／`175`【列】；`SELF_SHA256` `declared` ＝ `recomputed` ＝ **`f16bdb9e5c52b4040fd7ebb73dcb9776c9c19b8f98908f94be8caea1e88695ec`** ⇒ **係改版後之新版**
（舊版 `17976` B 因 `自誤 252` 已作廢；其 `S-0a` 已改綁性質——現查：`b9a50a2e…` 命中 `1`【列】、`同一物件` `2`【列】）。

🔴 **性質預告**：`-198` 係 **`K-6-B` 段一上鎖 ＋ 名次 ＋ 段二全域排序** ⇒ **幾何影響批·含生產碼** ⇒
🛑 **依 `CLAUDE.md` `🔧 三`：生產碼批⛔ 不得逕行 `push`，須請示**（`W-G.9-194` 之 KL 預先核可**⛔ 不及於本單**）。

**其前置之現況（本窗已現查·態 `945d216`）**
- `GB-133` **部分成就·⛔ 未解除**——指數已算並出艙（`d46dfe6`），**名次⛔ 不存在**（`名次`／`排名`／`priority_rank` 於 `app.py` 皆 `0`【列】）。
- `K-6-B` 段一之「上鎖」、段二之全域排序**皆⛔ 不存在**——正面列舉（母體 ＝ `app.py` 與 `verify/stepg_pipeline.py` **二檔**·框 ＝ **列**）：`預掃`／`上鎖`／`未上鎖`／`集中規則`／`合併群跨街廓`／`K-6-B` **逐檔皆 `0`**；對照組 `步驟 0` @ `app.py` ＝ `9`【列】。
- `步驟 0` 之上鎖**已落地惟係保守替代**（`k6_step0_block_locked`）；其正典解繫於**待 KL 域裁之循環依賴** ⇒ 已鑄 **`GB-135`**。

---

## `§五`　本 session 新增之家法（🛑 對下一單即生效）

| # | 出處 | 內容 |
|---|---|---|
| **裁 `H`** | `CLAUDE.md` 檔尾（`945d216`） | 判別力對照之判準**⛔ 得寫成「命中 ＝ `0`」**——須綁**逐筆歸類**；母體**⛔ 得扣除本單**；**命中非 `0` 而未逐筆具名歸類 ⇒ 停機** |
| **常規補款 一** | 同上 | 🔴 **閘之量測與其所守護之動作⛔ 得置於同一不可中斷之命令塊**——量測須先出艙、經判讀，方執行動作。（本款源自 CC 於 `W-G.9-197R` 之**自陳過失**，發單側逐字採納） |
| **常規補款 二** | 同上 | 發單側量倉之母體須以 `git ls-tree -r <態>` 或 `git ls-files` 取得，⛔ 不得以 `grep -r … .`（恆含未入倉草稿） |
| **常規補款 三** | 同上 | 同時交付多張單時，後單之開工閘須通過**「零生產碼插入」自檢**：「若開工前倉內合法地多了一個零生產碼 commit，本閘會不會紅？」會紅者須改綁性質 |

---

## `§六`　⚠️ 本窗實踩之陷阱（供接手窗）

1. 🔴 **閘與動作同塊**（`常規補款 一` 之由來）——`W-G.9-197` 之 `V-6` 量測與 `commit`／`push` 同塊 ⇒ 該閘**無從阻擋**。`W-G.9-199` 起已分開呼叫。
2. 🔴 **heredoc 之轉義層**——`python - <<'PY'` 內之 `\\-`／`\\n` 會塌陷（`SyntaxWarning: invalid escape sequence`）⇒ 正則與測試字串失真。**依 `常規五` 先判量測器紅抑或受詞紅**；比對用之原句應**自 blob 取出**，⛔ 不手抄。
3. 🔴 **含大量反引號之 `.md` 用 heredoc 會 `unexpected EOF`** ⇒ 改用 Write 工具。
4. 🔴 **母體隨批次而變**——對照組之數（`自誤 245`／`GB-134` 等）逐批增長，係**新報告引用所致**、⛔ 非異常；出艙時須具名其態與檔數。
5. 🔴 **行尾分流**（`GB-17` 族）：`KL_UI_*_stdout.log` 係**純 CRLF** ⇒ 須 `git -c core.autocrlf=false add`（保 `CR`）；`.md` 於本工作副本為 CRLF ⇒ 走**常規 `add`**（正規化為 `LF`），⛔ 用 `false` 否則整檔偽異動、`deletions` 爆表。
6. 🛑 **⛔ 用 `git add -A`／`.`**——`verify/out/` 混放入庫證據與本機臨時 log（現有 `50` 餘個未追蹤）。

---

## `§七`　尚未解除之阻塞（本窗⛔ 未動）

`GB-111`（凍存名單無結構化原因欄·重產時點繫 KL 已裁之「俟二閘落地後」）／`GB-123`（`main()` 無自動守備·**惟本波 KL UI 實跑已首次走通該通道**）／`GB-127`／`GB-129`／`GB-130`／`GB-131`／`GB-132` 類 ①（`戒` 之敘述句污染·`戒` 無單一登記表 ⇒ 母體對齊之解不適用）／**`GB-133` 部分成就**／`GB-134`／**`GB-135`**（待 KL 裁之循環依賴·唯一存在處係碼註解）／**`GB-136`**（`_ni` 還原不驗 `min_value` ⇒ UI 全頁崩潰·四代皆現）。

🔴 **`app` 路徑仍未跑完**——終止於 `k94_assert_baseline_touch`：`【未配到屁股線】1 宗——R5 之 628-53(2)（差 38.803m）`（既有終局·⛔ 非本波所致）。
