# 🛑 `W-G.9-145R`　**停機回報**：`payload 3` 之 `常規二`／`常規三` 與倉內**撞號**

**出文窗**：CC　**基座**：`27d0861d100154865b47e705cd34ba7b56d94af6`（`W-G.9-144【乙】`）
**性質**：🔒 **純追加一新檔**·⛔ **零生產碼**·⛔ **三受檔一字未追加**·⛔ 未改任何既有編號
**依據**：`W-G.9-145` 施工單 `§零 閘六`／`§一`「🛑 任一不符 ⇒ **停機回報實得**，⛔ 不得自行改號」

---

## §一　停機之由（**`§一-4` 不符**）

| # | 現查 | 單所擬 | CC 實得 | 判 |
|---|---|---|---|---|
| `1` | 自誤登記表既有最大號 | `179` | **`179`** | ✅ |
| `2` | `自誤 18[01]` 全倉落點 | 全在本單自身 | **`0`**（倉內⛔ 無·對照組 `自誤 17[0-9]` ＝ `1` 檔 ⇒ 量測器非紅） | ✅ |
| `3` | `K-9-23-a` 全倉命中 | `0` | **`0`**（對照組 `K-9-23` ＝ `3` 檔 ⇒ 量測器非紅） | ✅ |
| `4` | `grep -c '常規二' CLAUDE.md`／`grep -c '常規三' CLAUDE.md` | **皆 `0`**（「現行僅常規一」） | 🔴 **皆 `1`** | 🛑 **不符** |

### 🔴 實況：`常規二`／`常規三` **早已存在且受詞完全不同**

倉內 `CLAUDE.md`（`blob@27d0861`）之「🆕 作業常規（KL `2026-08-22` 同意·`W-G.9-97` 入倉）」節：

| 錨 | 既有之則（逐字節題） | 受詞 |
|---|---|---|
| `CLAUDE.md:500` | **常規一（`push` 之自辦）** | push 自辦四項 |
| `CLAUDE.md:504` | **常規二（施工單自相矛盾）** | 單內條款互斥之處置 |
| `CLAUDE.md:507` | **常規三（無土地後果之事項）** | 檔名／欄名／節號／序號之提案核可 |

而 `payload 3` 擬追加者為：
- **常規二　`W-G.9-NNN` 之取號與占用**
- **常規三　登記／稽核工具之編碼**

⇒ 🔴 **若照追加，同一檔內將各有二則同名之「常規二」「常規三」而受詞互異**
——即 `CLAUDE.md` 之「🔒 編號衝突一律以倉內為準」所指之**前綴碰撞**
（其案由逐字：「撇號使 `grep` **同時命中兩則**（前綴碰撞·而本專案定位正典靠 grep）」）。

🔒 **該鐵律之逐字處置**：「**一律以倉內既有編號為準；新則取下一個未占用號。⛔ 禁為配合施工單而改編既有正典編號。**……**正確處置是改新則之號並回報，不是改舊則。**」
⇒ 而 `§零 閘六`／`§一` 又明令 CC **⛔ 不得自行改號**（payload 內文已嵌該號且受 `sha256` 硬閘）
⇒ **二者合取之唯一相容解 ＝ 停機回報、由發單側改號重發。**

### 🛑 何以**不部分執行**（`payload 1`＋`2` 亦未追加）

`payload 2` 之 `自誤 181` **逐字**載「**加註見 `CLAUDE.md` 末端**」。
`payload 3` 未落 ⇒ 該引用即成**倉內查無之指涉** ⇒ **正是本批所要登記之 `自誤 180`（倉內不可覆核之數）之同型再犯**。
⇒ 🔒 **三 payload 為一不可分之整體**，本批**全數未追加**。

---

## §二　三 payload 之三數：**全中**（⇒ 問題純在「號」，⛔ 非內容）

| payload | 目標檔 | 列數 | 位元組 | `sha256`（前 16） | 判 |
|---|---|---|---|---|---|
| `1` | `docs/rulings/K-6_…md` | `34`／`34` | `3493`／`3493` | `c7bd4c1412030007` | ✅ MATCH |
| `2` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `8`／`8` | `2032`／`2032` | `903bfd253681fca1` | ✅ MATCH |
| `3` | `CLAUDE.md` | `34`／`34` | `2873`／`2873` | `8bb68ac6c7e4bddd` | ✅ MATCH |

（取法 ＝ `sed -n 'A,Bp' <施工單> > <檔>` 自單身**直取**·⛔ 未重打字·⛔ 未經 heredoc→python。）

⇒ 🔒 **`payload 1`／`payload 2` 可<u>原樣</u>重發**（三數不變）；**僅 `payload 3` 須改號並重算其三數**。

---

## §三　CC 之提案（依既有 **常規三**「節號／序號 ＝ ⛔ 無土地後果 ⇒ 有倉之一方提案、發單側核可」·⛔ 未上呈 KL）

| 候選 | 內容 | 理由 |
|---|---|---|
| **（甲）✅ CC 傾向** | 改為 **常規四（`W-G.9-NNN` 之取號與占用）** ／ **常規五（登記／稽核工具之編碼）** | 逐字符合 `CLAUDE.md` 鐵律「**新則取下一個未占用號**」；⛔ 不動既有二則；`grep "常規四"` 唯一命中 |
| （乙） | 併為既有 **常規三之補款④**（取號）與 **常規一之補款④**（工具編碼） | 受詞不合——既有常規三之受詞為「**無土地後果之事項之核可流程**」，非「取號序之占用」；強併將使受詞漂移 |
| （丙） | 另立體系（如 `PR-1`／`PR-2`「程序則」） | 引入第二套編號體系，違「節號體系（禁互代）」之精神、成本最高 |

🔒 **併請一併處理**：`payload 3` 內文另有二處嵌「常規二」「常規三」字樣需同步改（`§零之前 二` 之「並立家法斷根（`payload 3` 之常規二）」與 `payload 2` 之 `自誤 181` 之「🔒 **家法（`W-G.9-145` 立·詳見 `CLAUDE.md` 末端）**」一句）
⇒ ⚠️ **`payload 2` 之 `sha256` 若因此變動，須一併重宣告**（CC 未擅改，故此處只提示、⛔ 不代擬）。

---

## §四　為重發所備之現查（發單側可直接引用）

### 期初三數（態 ＝ `blob@27d0861`）

| # | 檔 | 列數（`\n` 計） | 位元組 | `sha256` | 末端是否已為空行 |
|---|---|---|---|---|---|
| `1` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `3328` | `232635` | `cd233e120cc89b2d9ba5c64427ca2701b55269eea9456ddb6b62c9dad25b7a2b` | **否**（末端為 `\n`·⛔ 非 `\n\n`） |
| `2` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `3451` | `284658` | `2cd0dbc3e20cb1fde2756492fc25b3dfb60557b391993e5431fde35a028dba9a` | **否** |
| `3` | `CLAUDE.md` | `600` | `53321` | `ee22d25caf409a51d81db1ae44ce2334fe6d69c0d62f04ba8252fbed2adc259b` | **否** |

⇒ 🔒 依 `§二`「追加前須先在目標檔末補一空行分隔」，**三檔皆須補 `1` 空行** ⇒ 重發之 `V-3` 列數增量應為 **`35`／`9`／`35`**（＝宣告列數 ＋ `1`）。

### 🩸 CC 自捕：`§一-1` 之**框誤**（⛔ 非單之誤）

CC 首測以裸框 `自誤\s*([0-9]{1,3})` 得 **`169`**、與單擬 `179` 不符；覆查後證係**框漏收**——
`W-G.9-144【乙】` 所入之 `170`〜`179` 係**表列式** `` | `170` | ``，其號**⛔ 不接於「自誤」二字之後**。
改以三框並列後定案：

| 框 | 式 | 相異 | max |
|---|---|---|---|
| A 標題式 | `^#{2,6}\s*.*?自誤\s*` + 選擇性反引號 + `(\d{1,3})` | `163` | `170` |
| **B 表列式** | `^\|\s*` + 反引號 + `(\d{1,3})` + 反引號 + `\s*\|` | `10` | **`179`** ✅ |
| C 裸式 | `自誤\s*` + 選擇性反引號 + `(\d{1,3})` | `165` | `298` ❌ |

🔴 **框 C 不可用作取號**：其 `298` 係 `` 自誤 `2980` ``（**一個列數**）遭量詞 `{1,3}` **截頭**所致
（錨：`docs/reports/W-G.9波_claude.ai側自誤登記.md:3004`）——即 memory 既載之
「量詞下限／上限靜默吃號」之再現。
⇒ 🔒 **既有最大號 ＝ `179`（框 B）**，與單擬**相符**。

---

## §五　⛔ 本批未辦之事（逐項）

- ⛔ **三受檔一字未追加**（`git status --porcelain` 對該三檔為空）。
- ⛔ 未改任何既有編號、⛔ 未改 payload 一字、⛔ 未自行改號。
- ⛔ 未跑 `V-5`／`V-6`／`V-8`——受詞未變，跑之無意義（`V-8` 之資料源 `K-9-23-a` 尚未入倉）。
- ⛔ 未落地 `K-9-23`／`K-9-16`／`K-9-17`／`K-9-12`；⛔ 未改 `app.py`／`verify/`／`data/`。
- ⛔ 未動 `distracted-tu-19e45b` worktree。

🔒 `app.py` 期末 blob ＝ **`b4cc30835feaad03dfc37f394a669bbb45988b8b`**（⛔ 零觸）。
🔒 **常規一四項齊備**（① 零生產碼 ② 純新增檔·`deletions = 0` ③ `app.py` blob 逐位＝期望 ④ 開工閘 `一`／`二`／`三` 全綠）⇒ 本停機報告**逕行 `push`**。
