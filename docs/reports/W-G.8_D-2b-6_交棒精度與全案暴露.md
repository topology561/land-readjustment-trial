# W-G.8 D-2b-6：逐街廓分跑實證 ＋ **首次取得全六街廓診斷**

**產出**：CC／2026-08-08　**分支**：`wip/s1-endpart`　**基準 commit**：`e82af35`
**狀態**：**§一 PASS**（分跑保值性實證）＋ **全案首次暴露**；§二／§三／§四／§六／§七 **未達**

> ⚠️ 行號錨定本批 commit；⛔ 引用前重跑 grep。⛔ 檢索排除 `| grep -v "^\./\.claude/worktrees/"`。
> 🔑 **本批之價值**：連續四批被「分跑不保值」之**錯誤判斷**擋住的全案暴露，**今日打開**。

---

## 一、§五 全案暴露 —— **首次取得 R1–R6**（照 §十 置最前）

**實料**：`verify/out/probe_D2b6_split.log`【III】　**探針**：`verify/probes/probe_D2b6_split.py`

### 1-1 逐街廓診斷（solo 分跑·`cb` 全集不變）

| 情境 | 街廓 | `_solve_G_one` 次數 | 中止之閘與**數值** |
|---|---|---|---|
| 0m | **R1** | 10 | `①' 覆蓋閘破` `12.0568`（union 2881.4963／街廓 2893.5531） |
| 0m | **R2** | 30 | `②-宗 圍堵閘破` 宗-宗重疊 **`76.2616`** > 上界 `3.1129`（(15−1)×0.005×44.47） |
| 0m | **R4** | 4 | `①' 覆蓋閘破` **`58.4792`**（union 3149.3873／街廓 3207.8665） |
| 0m | **R6** | 28 | 🔴 **`K-9-5-5 範圍守衛破`**（本波自立之閘·見 §二） |
| 0m | **R5** | 24 | `②-宗 圍堵閘破` 宗-宗重疊 **`72.1964`** > 上界 `2.5141`（(12−1)×0.005×45.71） |
| 0m | **R3** | 28 | `①' 覆蓋閘破` **`76.0262`**（union 4130.7734／街廓 4206.7996） |
| 3.5m | **R1** | 10 | `①' 覆蓋閘破` `12.0553` |
| 3.5m | **R2** | 30 | `結構閘 telescoping 破` ΣRw `100.02` ≠ R(34.39)−R(**6.98**) `44.41`（Δ **55.606**） |
| 3.5m | **R4** | 4 | `①' 覆蓋閘破` `58.4792` |
| 3.5m | **R6** | 28 | 🔴 **`K-9-5-5 範圍守衛破`** |
| 3.5m | **R5** | 24 | `結構閘 telescoping 破` ΣRw `99.99` ≠ `44.41`（Δ **55.576**） |
| 3.5m | **R3** | 28 | `結構閘 telescoping 破` ΣRw `100.06` ≠ `44.47`（Δ **55.589**） |

🔑 **`12.0568` 遠非全案** ——覆蓋殘差另有 **R4 `58.4792`／R3 `76.0262`**，
且另有**兩類從未見過**之破（`②-宗 圍堵` 與 `範圍守衛`）。

### 1-2 ⛔ 尚未產生之項（誠實聲明）

§五 要求之 `Σ殘差`／`Σ|殘差|` **並列**、各側宗數 `n`、種子修正前後值 —— **未產出**：
每一街廓**皆於閘中止**，`|union(宗+池) − 街廓|` 只有**撞覆蓋閘之三塊**（R1／R4／R3）有值，
R2／R5／R6 中止於**更早或不同**之閘 ⇒ **無 union 可取** ⇒ 逐街廓殘差表**不完整**。
⛔ 依 §五「記錄逐值後續跑下一街廓」已照辦（未中止全案），但**殘差閘無法計算**（母體不全）
⇒ ⛔ **不宣稱殘差閘通過**。

### 1-3 🔒 誠實界限（§五 要求逐字）

> 楔形空隙**尚未修復** ⇒ `停機③（守恆-帳幾何級）` 之殘差**被該空隙污染**
> ⇒ ⛔ **不得宣稱守恆已驗**、⛔ 不得放寬其上界。該項延至**鋪滿機制一般化之後**。

---

## 二、🛑 我自己立的閘擋下了 R6：`K-9-5-5 範圍守衛破`

```
🔴 K-9-5-5 範圍守衛破：街廓 R6 right 側 方向判準所得授權界面 [] ≠ 街角第 1 宗 ['628-18(1)']
```

- **來源**：D-2b-3 §二-3 之 `_guard_auth_scope`（`grep -n "def _guard_auth_scope" verify/stepg_pipeline.py`）。
- **語意**：R6 right **有**街角第 1 宗（`628-18(1)`），但**方向判準找不到任何授權界面**
  ——即該宗之 `res['_alloc_dir_used']` **∥ALLOC**，與「街角第 1 宗遠側界應 ∥SIDELINE」矛盾。
- **⇒ 依該閘之設計即停機上呈**（其存在目的正是攔截此情形）。
- ⚠️ **⛔ 未診斷、⛔ 未修**：R6 從未執行過（連續四批），本批**首次**觸及；
  成因（`_first_corner_alloc_dir` 於 R6 之回傳？該側 SIDE_LINE 幾何？）**需另查**。
  ⛔ **不在本單授權**（本單 §二／§三 為交棒精度與種子）。

---

## 三、🛑 3.5m 之 `Δ ≈ 55.6`：疑為 `GB-47` 種子（**假說·未證**）

R2／R5／R3 於 3.5m 皆 `ΣRw ≈ 100` 而期望 `R(W_final) − R(W₀=6.98) ≈ 44.41`。
`100 − 44.41 = 55.59 ≈ R(6.98)` ⇒ **實跑之鏈像是自 `0` 起算、而非自 `W₀ = 6.98`**。
`W₀ = 6.98 ≠ 0` 正是 **forced buffer** 情境 —— 即 `GB-47` 所記之種子分歧
（`verify/stepg_pipeline.py:615` `_W_prev_left = 0.0`）**首次被實際消費**。

⚠️ **此為假說**：⛔ 未以實驗證實（證實需執行 §三 之種子修正）。
⛔ 不當作已證結論；但它**正面支持 §三 之必要性**，且說明 `GB-47` 之影響**遠大於**前批所估
（前批稱「本案未觸發」⇒ D-2b-5 已更正為「R1 可及範圍內未觸發」⇒ **本批證實 R2/R3/R5 觸發**）。

---

## 四、§一 分跑保值性 —— **PASS**（含鑑別力界限）

| 檢 | 結果 |
|---|---|
| 【I】R1：A 全跑 vs B 單跑，`_solve_G_one` **10 次 × 29 欄**（全精度 `S_raw`／`W_far_raw`／`Rw_raw`／`W_rw_start_raw` 等） | **不符 0** ✅ 逐欄 `==`（0m／3.5m 皆然） |
| 【I】③ 終止例外逐字 | ✅ `==` |
| 【II】R2 solo vs (R2+R4) 同跑之 **prefix 30 次** | **不符 0** ✅ ⇒ R2 不受 R4 在列影響 |

### 4-1 ⚠️ 鑑別力界限（⛔ 照實聲明·不得含糊）

1. 【I】② `stdout` 比對：**A／B 皆 0 字**（覆蓋閘於 `_pool_strips_for_block` 內先 raise、
   早於 `[T2-DIAG]` 之列印）⇒ 該項係**空真**、**無鑑別力**，⛔ 不列為佐證。
2. 【II】同跑於**第一塊即中止** ⇒ **到不了第二塊** ⇒ 只證「**前塊不受後塊影響**」；
   「**後塊不受前塊影響**」在**每塊皆中止**之現狀 **無法直接測**。
3. ⇒ 分跑保值性目前建立於三支柱：①【I】R1 全跑==單跑；②【II】prefix ==；
   ③ **唯一之跨塊輸入 `cb`／`snapshot` 於兩模式皆全集不變**（`compute_total_burden_rate`
   之母體為 `cb`，⛔ 與 `parcels_by_block` 無關）。

### 4-2 🔧 具名更正：D-2b-2 §1-6

D-2b-2 「自 `cb` 剔除 R1 ⇒ 非保值 ⇒ 排除」**錯誤·已加更正橫幅**（⛔ 原文未刪）。
**錯在把兩個母體當成同一個**：負擔率之母體是 `cb`，迴圈之母體是 `parcels_by_block`
——**限縮後者、保持前者**即可。**代價：連續擋住四批**。
⇒ `GB-45` 族之教訓延伸：**「排除某方案」之理由同樣必須現查**。

---

## 五、未達之項（具名·⛔ 非遺漏）

| 節 | 狀態 | 理由 |
|---|---|---|
| §二 `W` 交棒全精度 | **未達** | 本批預算用於 §一 之實證與全案掃描（此二者為其餘各節之前置）。§二 之落點已定位：6 處消費點（`app.py:9169/9174/19265/19365`、`stepg:656/743`）＋ 第 7 處 `stepg:1097`（🔴 **施工單稱 6 處·實為 7 處**·`GB-45` 族） |
| §三 作廢式殘存 | **未達** | 已完成定位：`app.py:19223/19224`（種子）＋ **`19427/19429`（`_select_pool_slot` 之 `b` 參數）** ⇒ 🔴 **施工單稱 2 處·實為 4 處**。且 stepg **已用** `_mp_base_W0`（`:786`／`:806-807`）⇒ `GB-47` 之真身係「**app 殘存已廢止式**」而非「兩版分歧」——**stepg 早已遷移、app 未遷** |
| §四 選位對照表 | **未達** | 手段已具（§一 已解鎖分跑）；但 R6 之範圍守衛破與 3.5m 之 Δ55.6 使 `k*`／`ΣRw` **現值不可信** ⇒ 先出表等於出一張建立在破閘上的表 |
| §六 作廢式殘存檢查 | **未達** | 依賴 §三 之母體定義 |
| §七 run_all | **未跑** | 六街廓皆中止 ⇒ 靶無法逐項歸因 |

---

## 六、異動清單

| 檔 | 性質 |
|---|---|
| `verify/probes/probe_D2b6_split.py` | 🆕 §一 實證探針（⛔ 生產碼零觸） |
| `verify/out/probe_D2b6_split.log` | 🆕 §一＋全案掃描實料 |
| `docs/reports/W-G.8_D-2b-2_鋪滿機制一般化.md` | 🔧 §1-6 具名更正橫幅 |
| `docs/reports/W-G.8_D-2b-6_交棒精度與全案暴露.md` | 🆕 本報告 |

**生產碼**：`app.py`／`verify/*.py` 引擎層 —— **本批一字未動**。
**§八 六項不授權**：全未觸及。**§九 停機條件**：1 ⇒ 未觸發（§一 PASS）；
2／5 ⇒ 未達該節；3 ⇒ 未達該節；4（殘差格不含街角第 1 宗）⇒ **未能評估**（殘差母體不全·§一-2）。

---

## 七、🛑 上呈

1. **R6 `K-9-5-5 範圍守衛破`**（0m 與 3.5m 皆然）——本波自立之閘首次咬到，
   代表 R6 right 之街角第 1 宗其遠側界**未**走 ∥SIDELINE。⛔ 未診斷、未修、不在本單授權。
2. **3.5m R2/R5/R3 之 `Δ ≈ 55.6`** —— 疑為 `GB-47` 種子（假說·未證）；
   ⇒ **§三 之優先度應提高**（其影響已證遠大於前批所估）。
3. **施工單兩處計數偏低**（`GB-45` 族·⛔ 非指摘·供次批更正）：
   §二 稱消費點 6 處、實為 **7**（多 `stepg:1097`）；§三 稱殘存 2 處、實為 **4**（多 `app.py:19427/19429`）。
4. **建議次批序**：先修 §三 種子（可能一併解 3.5m 三塊）→ 再 §二 交棒精度 → 再診 R6 → 最後 §四 出表。
