# W-G.5 — **執行環境基準**（GB-31 之實體化）＋ 換機確認實跑

> 立於 KL 交辦「K-6-A2 段四 換機基準確認」（2026-08-04）。
> ⛔ 本文**零碼面變更**；⛔ 段四(c) **未開始**。
> **基準 commit**：`5ef7de3`（fresh clone·`git status --porcelain` 全空）。

---

## ⚠️ 先講本次量測之**效力上限**（勿誤讀）

本次 fresh clone 落在 **與桌機工作區同一台主機**（同 OS build、同 `C:\Users\admin\…` 路徑根）。
⇒ 本文所證者為 **「乾淨重製可復現」**（clone→install→run 得同結果），
**⛔ 並未**檢驗 GB-30／GB-31 真正關切的 **跨主機**全精度浮點分歧。
**⇒ 下列「`.full.log` 亦逐位元相同」一項，不得引為「跨機穩定」之證據。**
真正的跨機驗證須在**另一台主機**上重跑本文之三步。
🆕 **已由 claude.ai 於其主機補上** ⇒ 見 **§一-2**（出處為他方實跑·⛔ 非 CC 自驗）。

---

## 一、環境解析版本（GB-31「執行環境未鎖」之實測快照）

> ⚠️ 施工單指示寫入「GB-31 之**本機環境**欄」，惟**倉內登記表無此欄**
> （欄頭實為 7 欄：`# / 阻塞項 / 觸發條件（換案）/ 現況處置 / 倉內出處 / 真資料覆蓋 / 需 KL 域裁？`
> ——`grep -n "^| # | 阻塞項" docs/reports/W-G.4_泛用阻塞項登記表.md`）。
> 依「以倉內為準並在報告載明差異」，**不新增欄**（會動到全表每一列），
> 改立本檔為環境之常駐落點，並自 GB-31 之「倉內出處」欄指來。

| 項 | 解析值 |
|---|---|
| Python | **3.13.11**（CPython·AMD64）|
| OS | **Windows 11 · 10.0.26200** |
| 路徑分隔符 | `os.sep = '\\'`／`os.altsep = '/'`／`os.pathsep = ';'` |
| **shapely** | **2.1.2** |
| **GEOS** | **3.13.1** |
| numpy | 2.4.4 |
| pandas | 2.3.3 |
| openpyxl | 3.1.5 |
| ezdxf | 1.4.3 |
| streamlit | 1.56.0 |
| plotly | 6.7.0 |
| pyproj | 3.7.2 |
| geopandas | 1.1.3 |
| xlrd | 2.0.2 |

🔴 **GB-31 之成立條件未變**：`requirements.txt` 仍為 11 條全 `>=`、無 lockfile、無 GEOS 版本紀錄。
本表只是**記錄某一次解析結果**，**不是鎖**。⛔ 依 GB-31 現況處置，**禁現在改 `requirements.txt`**（屬波末批）。

---

## 一-2、🆕 跨主機實測（**出處：claude.ai 2026-08-04 於其自身主機實跑**）

> 🔴 **出處紀律**：本節全部數據係 **claude.ai 於其主機**實測，
> **CC 無該主機之存取、無法獨立重現** ⇒ ⛔ **不得寫成 CC 自驗**。
> 本節之效力等同「他方回報之實料」，非本機復現。
> ⇒ 這正好補上 §零 所述之效力上限（本機 fresh clone 不檢驗跨主機分歧）。

### (甲) **判定層跨主機穩定——已實證**

| | CC 機（§一 快照） | claude.ai 機 |
|---|---|---|
| OS | Windows 11 10.0.26200 | **Linux 6.18.5-fc-v18** |
| Python | 3.13.11 · AMD64 | **3.12.3 · x86_64** |
| pandas | 2.3.3 | **3.0.2** |
| ezdxf | 1.4.3 | **1.4.4** |
| shapely / GEOS | 2.1.2 / 3.13.1 | **2.1.2 / 3.13.1（同）** |
| `os.sep` | `\` | `/` |

對同一靶 `verify/out/K6A2_seg3c3_runall.log` 正規化對拍：
**名目 157 vs 157／出 1／進 1／判定翻轉 0**；嚴格 `✅ PASS`／`FAIL` 計數 **86／46**，兩機相同。

⚠️ 「157」係 claude.ai 之正規化較寬（連 `🔴` 診斷行一併掃入），與本檔嚴格 **132 名目**
**不衝突**——兩種讀法結論一致。（本倉入庫之量尺 `verify/tools/nameset_diff.py`
**刻意只掃判定行**、不掃診斷行，理由見該檔 docstring。）

唯一那組出／進即 `S0d` 全精度浮點末位：

```
CC 機（Win）      ：S_cut(未捨入)=0.19536602438260764
claude.ai（Linux）：S_cut(未捨入)=0.1953660243826076
```

⇒ 🔑 **分歧只到診斷層，未進判定層。**
對泛化波是好消息：不同機器上之公務員／廠商**不會跑出不同的土地判定**。

### (乙) 🔴 **鎖版本必要但不充分**

兩機之 **shapely 與 GEOS 同版**（皆 `2.1.2 / 3.13.1`）而全精度值**仍不同**
⇒ 即使補上 lockfile、把 shapely/GEOS 鎖死，**此分歧不會消失**。

剩餘嫌犯（**未收斂**·⛔ **禁擇一寫成結論**）：
Python 3.12 vs 3.13｜Linux vs Windows 之 libm｜`ezdxf 1.4.3 vs 1.4.4`（DXF 座標解析）。

### ⚠️ GB-31 **不因本節而解除**

(甲) 是**量化了風險邊界**（分歧止於診斷層），**不是**解除登記。
GB-31 之成立條件**未變**：`requirements.txt` 仍全 `>=`、無 lockfile、無 GEOS 版本紀錄。
且由 (乙) 可知，**單靠鎖版本也解不掉**。

---

## 二、`run_all` 名目集合對拍（⛔ 不以計數驗收）

正規化：只取 `✅ PASS`／`🔴 FAIL` 之**判定＋名目**，剝除絕對路徑、`line N`、`0x…` 位址。

> 🆕 **§0-5 已將量尺與實料一併入庫**（初版只 commit `.md` ⇒ 本項倉內既無工具亦無 log，
> 正是 `run_all.py:157`／BLOCKED-3／GB-29／GB-30 反覆咬到之形狀）：
> - 量尺：`verify/tools/nameset_diff.py`（可攜·路徑由 `__file__` 導出·⛔ 無寫死絕對路徑）
> - 實料：`verify/out/K6A2_seg4_換機確認_runall.log`
> ⇒ **任何人可逕自複驗**：
> `python verify/tools/nameset_diff.py verify/out/K6A2_seg3c3_runall.log verify/out/K6A2_seg4_換機確認_runall.log`
> （rc=0 綠／1 有差異；⛔ 該器**不以計數驗收**，輸出為集合差）
> 🔒 該器已驗**會咬**：餵入非 `run_all` 之 log ⇒ **loud raise**，不靜默回空集合。

| | 名目 | PASS | FAIL |
|---|---:|---:|---:|
| 靶 `verify/out/K6A2_seg3c3_runall.log` | 132 | 86 | 46 |
| 本次 fresh clone | 132 | 86 | 46 |

**出（靶有·本次無）＝ 0**／**進（本次有·靶無）＝ 0**／**判定翻轉 ＝ 0**

⇒ **零進零出零翻轉**。`RESULT: FAIL` 係 46 紅之既有基線（非回歸）。

---

## 三、三支段四探針

### §0-1 之查核：`.log` 與 `.full.log` **本來就是同一次跑**（施工單前提不成立）

施工單 §0-1 稱二檔之 `對照錨` 行分隔符不同（`.log` 為 `verify/out/…`、`.full.log` 為 `verify\out\…`）
⇒ 推論非同一次跑。**倉內實況並非如此**：

- 二檔之該行**皆為反斜線**：`grep -n "對照錨" verify/out/probe_k98_virtual_block.log`
  ⇒ `verify\out\probe_ruling_K9_corner_width_V6_1.log:115`（`.full.log` 同）。
- 逐行比對：`.log`（62 行）為 `.full.log`（78 行）之**精確子序列**（62/62 全配對）；
  多出之 16 行分類為 `[T2-DIAG]`×12 ＋ `🔴 S0d`×2 ＋ 空行×1 ＋ **寫檔宣告**×1。
- **重跑一次**（同一行程同時產出二檔）後 `git status --porcelain` **為空**
  ⇒ 與倉內版本**逐位元相同** ⇒ 反證二檔本就同源。

⇒ 依「以倉內為準並在報告載明差異」，**未依 §0-1 之推論改動任何東西**；
但仍照做「重跑一次」以消除疑慮（結果：零異動）。
⚠️ 施工單所見之正斜線，推測係報告 §三 之**手寫路徑**（markdown 內文一律寫正斜線）
——**⛔ 此為推測、非查證**，僅備一說。

| 探針 | rc | 策展 `.log` vs 倉內 |
|---|---:|---|
| `probe_k98_virtual_block` | 0 | ✅ **逐位元相同**（md5 `446b0f32fd231a95384fd5953ebd0dfd`）|
| `probe_ruling_K4_s_origin` | 0 | ✅ **逐位元相同**（md5 `e620c7ab8ec3c05e23bee3619dbb7029`）|
| `probe_ruling_N_e1_touch` | **1** | ✅ **逐位元相同**（md5 `03cb120493daec3c0b945aa907eb6209`）|

md5 為**獨立於 `git diff`** 之複核；`git diff --stat -- verify/out/` 亦為空。

### `probe_ruling_N_e1_touch` 之 `rc=1` **係設計如此，非換機所致**

- rc 約定見其 docstring：`# 量測＋比對（rc=0 綠／1 紅）`
  （`grep -n "rc=0 綠／1 紅" verify/probes/probe_ruling_N_e1_touch.py`），
  收尾為 `return 1 if viol else 0`（`grep -n "return 1 if viol else 0" verify/probes/probe_ruling_N_e1_touch.py`）。
- **倉內既有之策展 log 本身就載著那些 viol**（快照 vs 實跑之欄差，例 `[0m] R2·628-41(1)`）。
- 且本次輸出與倉內**逐位元相同** ⇒ 紅之內容一字未變。

⇒ **既有紅、未因換機而動**。⚠️ 併記：本檔**不主張**該紅該不該修——那不是本單範圍。

### `.full.log`

`probe_k98_virtual_block.full.log`（倉內已入庫）與本次 fresh 全文**逐位元相同**（diff 0 行），
含 `🔴 S0d 異常` 之全精度浮點。
🔴 **但見本文開頭之效力上限**——同主機下相同是**應然**，**⛔ 不得**用以反駁 GB-30
所記之跨機浮點分歧（`0.1953660243826076` vs `0.19536602438260764`）。

---

## 四、`data/r3.dxf` — **不是未登記之相依**

三重證據：

1. **fresh clone 根本沒有它**（`data/` 僅 `V6.dxf`／`V6_1.dxf`／`地籍資料來源_匿名版.xlsx`），
   而 `run_all` 在其缺席下**跑完並得零進零出** ⇒ 實證不需要它。
2. **全倉碼面零引用**：`grep -rn "r3\.dxf\|r3_dxf" --include="*.py" .` ⇒ **0 命中**；
   僅數份報告把它列為 untracked 檔。碼面實讀之 DXF 只有 `data/V6.dxf` 與 `data/V6_1.dxf`。
3. 桌機那顆 **檔案大小 0 bytes**（`ls -la data/r3.dxf`·2026-07-22）。

⇒ **遊離檔案，非相依，無須登記為相依。**
⚠️ 其**去留**仍是既有未決項（`docs/reports/W-G.5_裁定N_偵察.md:211`「須 KL 確認來源後再定去留·本批不自決」）
——本文**不自決**，僅解除「它可能是隱藏相依」之疑慮。

---

## 五、結論

| 施工單項 | 結果 |
|---|---|
| 基準 `5ef7de3`／工作區乾淨 | ✅ |
| 1. 環境記錄 | ✅ 見 §一（惟登記表無「本機環境」欄，改立本檔·已載明差異）|
| 2. `run_all` 名目對拍 | ✅ **132／86／46·零進零出零翻轉**（量尺＋實料已入庫·§0-5）|
| 3. 分流 | ⇒ **換機無影響** |
| 4. 三支探針 | ✅ 策展 log **三支皆逐位元相同**；`N_e1_touch` 之 rc=1 為既有設計紅 |
| 5. `data/r3.dxf` | ✅ **非相依**（三重證據）|

⇒ 依施工單分流條款，**可開段四(c)**。⛔ 本文未開始 (c)、未動任何碼。
