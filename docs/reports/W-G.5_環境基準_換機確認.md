# W-G.5 — **執行環境基準**（GB-31 之實體化）＋ 換機確認實跑

> 立於 KL 交辦「K-6-A2 段四 換機基準確認」（2026-08-04）。
> ⛔ 本文**零碼面變更**；⛔ 段四(c) **未開始**。
> **基準 commit**：`5ef7de3`（fresh clone·`git status --porcelain` 全空）。

---

## ⚠️ 先講本次量測之**效力上限**（勿誤讀）

本次 fresh clone 落在 **與桌機工作區同一台主機**（同 OS build、同 `C:\Users\admin\…` 路徑根）。
⇒ 本文所證者為 **「乾淨重製可復現」**（clone→install→run 得同結果），
**⛔ 並未**檢驗 GB-30／GB-31 真正關切的 **跨主機**全精度浮點分歧。
**⇒ 下列「`.full.log` 亦逐位元相同」一項，不得引為「跨機穩定」之證據。**
真正的跨機驗證須在**另一台主機**上重跑本文之三步。

---

## 一、環境解析版本（GB-31「執行環境未鎖」之實測快照）

> ⚠️ 施工單指示寫入「GB-31 之**本機環境**欄」，惟**倉內登記表無此欄**
> （欄頭實為 7 欄：`# / 阻塞項 / 觸發條件（換案）/ 現況處置 / 倉內出處 / 真資料覆蓋 / 需 KL 域裁？`
> ——`grep -n "^| # | 阻塞項" docs/reports/W-G.4_泛用阻塞項登記表.md`）。
> 依「以倉內為準並在報告載明差異」，**不新增欄**（會動到全表每一列），
> 改立本檔為環境之常駐落點，並自 GB-31 之「倉內出處」欄指來。

| 項 | 解析值 |
|---|---|
| Python | **3.13.11**（CPython·AMD64）|
| OS | **Windows 11 · 10.0.26200** |
| 路徑分隔符 | `os.sep = '\\'`／`os.altsep = '/'`／`os.pathsep = ';'` |
| **shapely** | **2.1.2** |
| **GEOS** | **3.13.1** |
| numpy | 2.4.4 |
| pandas | 2.3.3 |
| openpyxl | 3.1.5 |
| ezdxf | 1.4.3 |
| streamlit | 1.56.0 |
| plotly | 6.7.0 |
| pyproj | 3.7.2 |
| geopandas | 1.1.3 |
| xlrd | 2.0.2 |

🔴 **GB-31 之成立條件未變**：`requirements.txt` 仍為 11 條全 `>=`、無 lockfile、無 GEOS 版本紀錄。
本表只是**記錄某一次解析結果**，**不是鎖**。⛔ 依 GB-31 現況處置，**禁現在改 `requirements.txt`**（屬波末批）。

---

## 二、`run_all` 名目集合對拍（⛔ 不以計數驗收）

正規化：只取 `✅ PASS`／`🔴 FAIL` 之**判定＋名目**，剝除絕對路徑、`line N`、`0x…` 位址。
（工具為 scratchpad 之比對腳本，**未入倉**——它不是受測者，是量尺。）

| | 名目 | PASS | FAIL |
|---|---:|---:|---:|
| 靶 `verify/out/K6A2_seg3c3_runall.log` | 132 | 86 | 46 |
| 本次 fresh clone | 132 | 86 | 46 |

**出（靶有·本次無）＝ 0**／**進（本次有·靶無）＝ 0**／**判定翻轉 ＝ 0**

⇒ **零進零出零翻轉**。`RESULT: FAIL` 係 46 紅之既有基線（非回歸）。

---

## 三、三支段四探針

| 探針 | rc | 策展 `.log` vs 倉內 |
|---|---:|---|
| `probe_k98_virtual_block` | 0 | ✅ **逐位元相同**（md5 `446b0f32fd231a95384fd5953ebd0dfd`）|
| `probe_ruling_K4_s_origin` | 0 | ✅ **逐位元相同**（md5 `e620c7ab8ec3c05e23bee3619dbb7029`）|
| `probe_ruling_N_e1_touch` | **1** | ✅ **逐位元相同**（md5 `03cb120493daec3c0b945aa907eb6209`）|

md5 為**獨立於 `git diff`** 之複核；`git diff --stat -- verify/out/` 亦為空。

### `probe_ruling_N_e1_touch` 之 `rc=1` **係設計如此，非換機所致**

- rc 約定見其 docstring：`# 量測＋比對（rc=0 綠／1 紅）`
  （`grep -n "rc=0 綠／1 紅" verify/probes/probe_ruling_N_e1_touch.py`），
  收尾為 `return 1 if viol else 0`（`grep -n "return 1 if viol else 0" verify/probes/probe_ruling_N_e1_touch.py`）。
- **倉內既有之策展 log 本身就載著那些 viol**（快照 vs 實跑之欄差，例 `[0m] R2·628-41(1)`）。
- 且本次輸出與倉內**逐位元相同** ⇒ 紅之內容一字未變。

⇒ **既有紅、未因換機而動**。⚠️ 併記：本檔**不主張**該紅該不該修——那不是本單範圍。

### `.full.log`

`probe_k98_virtual_block.full.log`（倉內已入庫）與本次 fresh 全文**逐位元相同**（diff 0 行），
含 `🔴 S0d 異常` 之全精度浮點。
🔴 **但見本文開頭之效力上限**——同主機下相同是**應然**，**⛔ 不得**用以反駁 GB-30
所記之跨機浮點分歧（`0.1953660243826076` vs `0.19536602438260764`）。

---

## 四、`data/r3.dxf` — **不是未登記之相依**

三重證據：

1. **fresh clone 根本沒有它**（`data/` 僅 `V6.dxf`／`V6_1.dxf`／`地籍資料來源_匿名版.xlsx`），
   而 `run_all` 在其缺席下**跑完並得零進零出** ⇒ 實證不需要它。
2. **全倉碼面零引用**：`grep -rn "r3\.dxf\|r3_dxf" --include="*.py" .` ⇒ **0 命中**；
   僅數份報告把它列為 untracked 檔。碼面實讀之 DXF 只有 `data/V6.dxf` 與 `data/V6_1.dxf`。
3. 桌機那顆 **檔案大小 0 bytes**（`ls -la data/r3.dxf`·2026-07-22）。

⇒ **遊離檔案，非相依，無須登記為相依。**
⚠️ 其**去留**仍是既有未決項（`docs/reports/W-G.5_裁定N_偵察.md:211`「須 KL 確認來源後再定去留·本批不自決」）
——本文**不自決**，僅解除「它可能是隱藏相依」之疑慮。

---

## 五、結論

| 施工單項 | 結果 |
|---|---|
| 基準 `5ef7de3`／工作區乾淨 | ✅ |
| 1. 環境記錄 | ✅ 見 §一（惟登記表無「本機環境」欄，改立本檔·已載明差異）|
| 2. `run_all` 名目對拍 | ✅ **132／86／46·零進零出零翻轉** |
| 3. 分流 | ⇒ **換機無影響** |
| 4. 三支探針 | ✅ 策展 log **三支皆逐位元相同**；`N_e1_touch` 之 rc=1 為既有設計紅 |
| 5. `data/r3.dxf` | ✅ **非相依**（三重證據）|

⇒ 依施工單分流條款，**可開段四(c)**。⛔ 本文未開始 (c)、未動任何碼。
