# `W-G.9-8`：引用錨**去行號化** ＋ 三支端到端複本**掛入自動流程**

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg98-anchors`）
**基準**：`dc619a4`　**事前登記**：`docs/reports/W-G.9-8_前置登記.md`（commit `e853fa7`）

> # ✅ **`J1`〜`J5` 全部成立；`V-1`〜`V-4` 皆未觸發。**
>
> 🔒 **本批之核心**：三支端到端複本掛入時，判準寫成**受詞本身之命題**
> （「終止點 ＋ 終止原因與凍存相符」），**⛔ 非「三支是否跑通」**
> ——後者會新增**三個永紅項** ＝ 把 `W-G.9-6`／`W-G.9-7` 剛修好的病再造一次。
> **此為失敗考古節 65 第四層之首次實戰。**

---

## §1 `J1`〜`J5` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **J1** | ✅ **成立** | 三支皆 `rc=1`，且**終止於同一處**：`verify/stepg_pipeline.py:1362`（`run_step_g` 之結構閘 raise），終止訊息**逐字相同**（正規化後） |
| **J2** | ✅ **成立** | 竄改自檢**兩態皆印**：竄改態（`wg_g1_smoke.py` 之終止記錄換成更早階段）⇒ **紅**；還原態 ⇒ **綠** |
| **J3** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`。全檔**非路徑差異恰一行** ＝ 新夾具之列 |
| **J4** | ✅ **成立** | `W-G.9-7` 之對帳於本批 log 內仍 **PASS**（類 II **0**、類 III **0**） |
| **J5** | ✅ **成立** | 兩新錨皆**唯一命中**（各附判別力自檢·見 §2-2） |

### 1-1 三支之**凍存終止點**（⛔ 非 `rc`·含原因全文）

| 腳本 | `rc` | 落點（traceback 末幀） | 終止原因（節錄·全文入凍存） |
|---|---|---|---|
| `verify/wg_g1_smoke.py` | `1` | `File "verify/stepg_pipeline.py", line 1362, in run_step_g` | `RuntimeError: 🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）…` |
| `verify/wg_g2_smoke.py` | `1` | 同上 | 同上 |
| `verify/wg_g3.py`（**W-G 收官判準 ①** 之終驗工具） | `1` | 同上 | 同上 |

**凍存檔**：`verify/out/WG98_端到端複本_終止點凍存.txt`
（`git hash-object` ⇒ `8ac9aa7eef177c3012bb7e5caf9c1a0bf83191e7`）。
**記錄範圍**（⛔ 正面聲明）：`rc` ＋ **stderr 全部非空行**（traceback ＋ 終止例外），
經 `R1`／`R2` 正規化；⛔ **stdout 不入記錄**（進度輸出、噪音大）。
🔒 traceback **行號未正規化** ⇒ raise 換位置即落**類 II**，由人具名確認。

### 1-2 🔒 設計意圖（**特性，不是缺陷**·已寫入碼註與 `run_all` 註解）

現況三支皆終止於**結構閘另案**。**該另案修好後，三支會跑得更遠 ⇒ 終止點改變
⇒ 本夾具轉紅 ⇒ 逼人來看並更新凍存。**
⛔ 屆時**不得**逕自重凍了事——須先確認「跑得更遠」確為預期。

### 1-3 沿用（⛔ 未另寫第二套正規化·`#20` 族）

`normalize`／`classify`／`report`／`render_list`／`parse_list` **一律取自**
`verify/wv_reconcile.py`。⇒ 本夾具**未新增任何**正規化規則或解析碼（`V-4` 未觸發）。
⚠️ 凍存檔沿用該格式，故每筆以 `🔴 FAIL  <腳本名>` 起頭——
**此處該標記之義為「終止記錄」**，⛔ 非「該腳本是一道失敗之閘」（已於檔頭與 docstring 標明）。

---

## §2 甲組：引用錨**去行號化**

### 2-1 受位移影響之逐項清單（⛔ 由程式原文逐行 `finditer` 讀出·非 `grep -A/-B/-C` 之窗）

**【目錄射程】** 全倉（⛔ 排除 `.git`／`worktrees`／`__pycache__`）·**⛔ 不限副檔名**。
**【字樣射程】** 正則 `CLAUDE\.md:(\d+)`。**【連接方式】** 逐行 `finditer`。

**母體（本批登記檔寫出前之倉態）**：引用 **39**｜`N≤75`（未受影響）**24**｜**`N≥76`（已失準）15**。

| 類 | 處 | 處置 |
|---|---|---|
| 🔴 **活文件·交接文** | `docs/reports/W-G.9波_新session交接文_WG91-94.md:129` → `CLAUDE.md:328` | 🔧 **已加註字樣錨** |
| **活文件·skill** | `.claude/skills/failure-archaeology/SKILL.md:1072`／`:1075` → `CLAUDE.md:207` ×2 | 🔧 **已加註字樣錨**（同一課節區塊·一併） |
| ⛔ **批次報告**（6 處） | `W-G.7_K6-A2-GEO2_W1口徑更正.md:143/:146`／`W-G.8_D-2b-21…:15`／`W-G.8_D-2b-22…:29`／`W-G.9-2_定式批報告.md:49`／`W-G.9-7_對帳準則升級.md:164` | ⛔ **未動** |
| ⛔ **凍存 log**（6 處） | `verify/out/probe_WG93_mp_definition_scope.log:29/31/34/35/36/37` | ⛔ **未動** |

⇒ **加註 3 處、未動 12 處**。⛔ 改歷史記錄等於**事後修史**。
**閘 7 之證**：`git diff --numstat` 對上開 6 份批次報告與 `verify/out/` 之既有物 **皆空輸出**。

### 2-2 兩個新字樣錨（⛔ 各附 grep 指令、輸出與**判別力自檢**）

| 錨 | 指令 | 輸出 | 命中 |
|---|---|---|---|
| **A**（`MP` ＝ SIDE 線中點·**正典·權威序第 2 級**） | `grep -n "SIDE_LINE 中點到該筆靠街角側地界線的垂距" CLAUDE.md` | `339:- W（累積）：SIDE_LINE 中點到該筆靠街角側地界線的垂距 → 查 Rw（累積差額制）` | **1** |
| **B**（CAD 圖層表之 `BASELINE` 列） | `grep -n "^\| BASELINE \|" CLAUDE.md` | `282:\| BASELINE \| 每街廓必畫 \| **屁股線**…` | **1** |

🔒 **判別力自檢**（⛔ 計數型宣稱不得逕以數字出艙）：
`grep -c "BASELINE" CLAUDE.md` ⇒ **7** ——寬字樣多命中
⇒ 「唯一命中」之檢查**非恆為 1**、確有鑑別力。

### 2-3 🔴 現查所得之**額外事實**（⛔ 施工單未載）

`.claude/skills/failure-archaeology/SKILL.md` 之「**行號衛生之現場示範**」課節，
其**自身舉例已再度漂移**：該課逐字稱「`CLAUDE.md:207` **現為 CAD 圖層表之一列**」，
而 `sed -n '207p' CLAUDE.md` ⇒ 「**先 `git clone`／`git pull` 對現況**。公倉可直接 clone。」
——CAD 圖層表之 `BASELINE` 列現落於 `:282`。
⇒ 該課寫著「本則自身即踩到」，**兩批之後又踩了一次**。加註已就地補上（見 §2-2 之錨 B）。

---

## §3 截斷盲點之**聲明**（⛔ 只聲明·未解除）

`verify/run_verification.py` 之顯示層將每閘明細**截於 12 列**
（字樣錨：`grep -n "未顯示" verify/run_verification.py` ⇒ **唯一命中**）。
⇒ `W-G.9-7` 之名單所存者 ＝ log 所印者 ⇒ **第 13 列以後之內容改變，若列數不變則對帳看不見。**

| 名目 | 隱藏明細列 | 該閘違規總列 | 已入凍存之列 |
|---|---|---|---|
| `參數0m` | 2 | 14 | 12 |
| `參數3.5m` | 2 | 14 | 12 |
| `v3·診斷0m（全欄逐格·無豁免）` | 66 | 78 | 12 |
| `率接線無串聯0m（vs v1 原錨…）` | 66 | 78 | 12 |
| `v3·診斷3.5m（全欄逐格·無豁免）` | 59 | 71 | 12 |
| `率接線無串聯3.5m（vs v1 原錨…）` | 59 | 71 | 12 |
| **合計（受截斷 6／22 項）** | **254** | **326** | **72** |

⚠️ **判別力自檢**：以**不存在**之字樣（`**另 N 欄未顯示**`）重算 ⇒ **0**
⇒ 上開抽取式**非恆命中**。
🔒 首版正則曾把 `**` 放錯邊（`另 **N` vs 實際 `**另 N`）⇒ 得 0 而被斷言擋下（考古 67 修法 ①）。

🔒 **緩解**：截斷提示行本身（`…（**另 N 列未顯示**·本閘違規總計 M 列）`）**有**入凍存
⇒ **列數改變仍會被抓**（`N`／`M` 任一變動即落類 III）。
**不可見者僅為「列數不變而內容變」。**

⚠️ **與失敗考古節 67 之關聯（就地點明）**：本盲點即該節之同族
——「**比較器之一端根本不在輸入中，而比對照樣回 PASS**」。
**差別**在：本處之缺席是**已知且已量化**的，且**有一個會隨列數變動而變紅之代理**
（截斷提示行）；節 67 之三例則是**無人知情**之缺席。

⛔ **本批未解除**：解除須改 `run_verification.py` 之 12 列上限 ⇒ log 內容全面變動
⇒ 現行名單全面失效、須重凍 ⇒ **屬另批**。

⚠️ **具名副作用**：上開聲明已依施工單 §2-3 加註於名單**檔頭**（⛔ 純插入·`numstat 26 0`）
⇒ 該檔之 `git hash-object` 由 `f2520e51e15750a222621a13398486799d4e7295`
變為 **`c8a2b43adf97eb10bc82f42dd4ae07d380994cf4`**
（`W-G.9-7` 報告 §10 所載為前者）。**加註後對帳仍 PASS**、解析筆數 22 → 22 不變。

---

## §4 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未修任何既有 FAIL**——含結構閘 `理論＝實跑`、`W-G G.1` 之 `KeyError: '0m'`。
2. ⛔ **未解除截斷**（`run_verification.py` 之 12 列上限未動；該檔 `git diff --numstat` 為**空輸出**）。
3. ⛔ **未立 `G3` 三項之閘**（`W-D.3 碎片`／`W-F F.3`／`W-G G.2`）。
4. ⛔ **未動任何凍存物之行號錨**（批次報告 6 處＋凍存 log 6 處）。
5. ⛔ **未改三支複本原檔**——其「全鏈端到端跑通」之原始命題**保留**，
   作為結構閘另案修好後之終驗。
6. 🔴 **洞 A（寄生失聲）仍在**：`W-G.9-6` 所量之「從未執行之 `results.append` 站點」
   本批**未動**（本批未改 `run_verification.py`）。
7. ⛔ **未動生產碼**（`git diff --numstat -- app.py` ⇒ 空輸出）。

---

## §5 下一批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | **`W-G G.1` 之上游 `_ctx["0m"]`** | 一崩即使 **69 行**判定式（含 W-8 反向閘）全數失聲；修好上游 ⇒ 一項換回一整片。**且**三支端到端複本亦將因此跑得更遠 ⇒ 新夾具依設計轉紅 ⇒ 屆時一併更新凍存 |
| **2** | **`W-F F.3` 之閘** | `W-G.9-6` 六式全 0 命中、**確定無人接手**；死站點含守恆／位次序類命題 ⇒ 土地後果直接 |
| **3** | **`W-D.3 碎片` 之閘** | 確定無人接手；碎片三分類直接影響 W-D.4 四梯與遞補 |
| **4** | **`W-G G.2` 之閘** | 確定無人接手；屬呈現層契約，土地後果較間接 |
| **5** | **洞 A 之通則處置** | 「一項入期望 FAIL 名單即使其後判定式全數失聲」——須逐項登記「原守何命題／現由誰守」，無人接手者另立**不受該上游影響**之閘 |
| **6** | **解除明細截斷（12 列上限）** | 可消滅 §3 之盲點，但**必使現行名單全面失效、須重凍** ⇒ 須與序 1 之重烤時點協調 |
| **7** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

⚠️ 序 1／6 之前置含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §6 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**·雜湊一律 `git hash-object`）

```
$ git hash-object <各產物>
8ac9aa7eef177c3012bb7e5caf9c1a0bf83191e7  verify/out/WG98_端到端複本_終止點凍存.txt
6a1b955cb560d25e46fc7e37a332412fadbc8c07  verify/out/WG98_runall.log
7466af7c3b7239f9afe5e3714f8857ca2ff3166d  verify/fixture_e2e_termination.py
c8a2b43adf97eb10bc82f42dd4ae07d380994cf4  verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt

$ grep -n "fixture_e2e_termination" verify/out/WG98_runall.log
43:  ✅ 末端夾具 fixture_e2e_termination.py（🆕 W-G.9-8 三支端到端複本·終止點凍存對帳…）rc=0

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG97_runall.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG98_runall.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG98_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG98_runall.log ⇒ 22

$ grep -n "SIDE_LINE 中點到該筆靠街角側地界線的垂距" CLAUDE.md ⇒ 339:…（命中 1）
$ grep -n "^| BASELINE |" CLAUDE.md                              ⇒ 282:…（命中 1）
$ grep -c "BASELINE" CLAUDE.md                                   ⇒ 7   ← 判別力自檢

$ git diff --numstat -- app.py                     ⇒ （空輸出）
$ git diff --numstat -- verify/run_verification.py ⇒ （空輸出）
$ git diff --numstat  （commit B 之前態）
14  0  .claude/skills/failure-archaeology/SKILL.md
1   0  docs/reports/W-G.9波_新session交接文_WG91-94.md
12  1  verify/run_all.py
```

🔒 **`verify/run_all.py` 之刪除欄 `1`**：唯一被刪之行為前一末項之**收尾標點**
（`…竄改自檢）"))` → `…竄改自檢）"),`）；**該項內容逐字未變**。文件之刪除欄**皆 0**。

### 6-1 ⚠️ 兩項自誌（⛔ 皆當場更正）

1. 🔴 **`Edit` 工具回報「updated successfully」而檔案<u>未變</u>**：
   對 `.claude/skills/failure-archaeology/SKILL.md`（2000+ 行）之加註，工具回報成功，
   而 `grep -c "W-G.9-8" <該檔>` ⇒ **0**。改以程式插入 ＋ **寫後斷言**始落地。
   ⇒ 已立**失敗考古節 68**。
2. 🔴 **截斷量之首版正則把 `**` 放錯邊** ⇒ 母體 0 ⇒ **被 `assert` 擋下**
   （考古 67 修法 ① 之首次生效）。⛔ 若無該斷言，會出艙一個「無截斷」之假結論。

---

## §7 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `e853fa7` — 事前登記（⛔ 零碼、零量測；含位移清單與三支之預測終止點）
- **B** `ebd1141` — 錨之加註 ＋ 新夾具 ＋ 掛入 ＋ 長跑 log（⛔ 零文件結論）
- **C** 本報告 ＋ 截斷盲點聲明（名單檔頭加註）＋ `GB` 登記 ＋ 考古節 68
