# `W-G.9-6`：期望 FAIL 名單之**守備盤點**（**事前登記**·⛔ 零量測·零碼變更）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg96-audit`）
**基準**：`82d44bde821e49bde73cc01816a66219b43e30c2`
**批號現查未占用**：`grep -rn "W-G.9-6" . --exclude-dir=worktrees --exclude-dir=.git | wc -l` ⇒ `0`
**批號非序**：`W-G.9-5`（contract 側切換）經裁定押後於本批之後；⛔ 不回頭補號。

> 🔴 **本批性質：純量測。** ⛔ 零生產碼、⛔ 不立新閘、⛔ 不修任何既有 FAIL、
> ⛔ 不改任何一份期望 FAIL 名單、⛔ 不把任何檔掛入 `run_all`。

---

## §0 「已跑／未跑」聲明（⛔ 指令與輸出並列·**皆本登記落筆當次重跑**）

```
$ git rev-parse --short HEAD
82d44bd

$ grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_D2b1切換後.txt
22

$ grep -c "results\.append" verify/run_verification.py
107

$ grep -n "run_verification\|v.main()" verify/run_all.py
261:    import run_verification as v
267:    print("### [3/3] W-V headless 對拍（verify/run_verification.py）")
268:    rc2 = v.main()

$ grep -rn "W-G.9-6" . --exclude-dir=worktrees --exclude-dir=.git | wc -l
0
```

### 0-1 🔴 **與施工單 §0 不符之一項（具名·⛔ 不默默吸收）**

```
$ python -V
Python 3.13.11
$ python3 -V
(無 python3)
```

施工單 §0 記 `python3 -V` ⇒ `Python 3.12.3`。**本機實為 3.13.11**，且無 `python3` 命令。
⇒ 該 §0 區塊**非於本機所產**。其餘 §0 諸項經本機重跑**皆相符**（見上）。
⚠️ 記此非為挑錯，而是：**`sys.monitoring` 之可用性與行為依版本而異**，
本批之工具選擇須以**實機版本**為據（見 §2）。

### 0-2 「倉內無任何 `.py` 讀取該名單」之**否定性宣稱**（三項射程齊備）

- **【目錄射程】** 全倉（`.` 遞迴），⛔ 排除 `.git/`／`worktrees/`；不限副檔名。
- **【字樣射程】** 五式、⛔ 不預設符號名：
  ① `期望FAIL`（.py）② `期望`（`verify/` 全樹 .py）③ `K6A2`（.py）
  ④ `期望FAIL名單`（**不限副檔名**）⑤ `對帳`（`.py/.sh/.ps1/.bat/.yml/.yaml/.toml/.cfg`）
- **【連接方式】** 各式**獨立裸 grep**、⛔ 無管線過濾、⛔ 無距離窗；rc 逐式記錄。

**結果**：① rc=1（0 命中）；② 命中皆為探針／夾具之「期望**值**」語意，**無一讀該名單**；
③ 僅 `app.py` 三處，指向 `K6A2_seg5c2_bindproof.txt`（**另一檔**）；
④ 命中皆為 `.md` 文件 ＋ 名單檔自身；⑤ 命中皆為**他案對帳**探針（凍存影響表等）。
⇒ **對帳確為人工程序**，⛔ 倉內無可執行實作。

### 0-3 🔒 對帳準則之**正典定義**（權威序**第 2 級**·`CLAUDE.md:71-72`·逐字）

> ⇒ 期間之驗收改為 **「與凍存之期望 FAIL 名單逐項相同」**，⛔ **非「全綠」**。
> 凍存名單：`verify/out/K6A2_期望FAIL名單_902f5d1.txt`（**逐項名目**·⛔ 非計數）。

⇒ `Q2` 所測之準則**有獨立真相源**（⛔ 非 CC 自撰之稻草人）。

### 0-4 🔴 **新發現之具名不一致**（⛔ 本批不改·僅登記）

| 項 | 正典（`CLAUDE.md:71-72`·第 2 級） | 現行實務（施工單 §0／兩份交接文） |
|---|---|---|
| 凍存名單檔 | `K6A2_期望FAIL名單_902f5d1.txt` | `K6A2_期望FAIL名單_D2b1切換後.txt` |
| 名目數 | **46** | **22** |

二檔**名目 diff 非空**（`902f5d1` 之約 30 個細名目——`v3·G值0m`／`v3·滑池槽0m`／`v3·J表0m`／
`F.0·合併決策0m`／`F.0·池差閉合0m`／`F.2·池流向0m`… ——於 `D2b1切換後` 被
**8 個粗名目**取代：`v3·StepG0m（結構閘/看守觸發）`／`W-F F.0`〜`F.3`／`W-D.3 碎片`／`W-D.4` 等；
另新增 `W-G G.1 接線層 ctx-builder 同源`）。

⚠️ **此現象本身即疑似「洞 A 之名目層表現」**——上游一 raise，其後數十道判定式塌縮為一個名目。
🔒 **本批之工作即量它**，⛔ 不在本登記預判其判定。

### 0-5 🔴 未跑（本登記之受詞）

- ⛔ 尚未做任何覆蓋量測；⛔ 尚未跑本批之 `run_all`。
- ⛔ 尚未盤點任一名目之三欄；⛔ 尚未判定任一道閘是否失聲。
- ⛔ 尚未寫任何探針。

---

## §1 §8-3 之**逐條反查**（⛔ 施工單指定·先於登記預測）

> 施工單 §8-3：「claude.ai 於 `W-G.9-4b` 自犯之錯（`F3` 前半：把一個**前提已被既有另案否證**的
> 子句寫進可證偽預測）⇒ 本單之 `G1`〜`G5` 你須**逐條反查其前提是否已被既有另案否證**；
> 若發現任一條與 `F3` 同型，**停機上呈**。」

**`F3` 同型之判準**（CC 定義，⛔ 事前寫死）：
**該預測之主詞或其成立所需之前置，在本批動工前即已因<u>本批不得處置之另案</u>而不可能成立**
⇒ 照做亦不可能產出所宣稱之證據。

| # | 前提 | 反查結果 |
|---|---|---|
| **G1** | ① 可對 `run_verification.py` 取行覆蓋；② 其於 `run_all` 內**同進程**執行 | ✅ **前提成立**：`run_all.py:261/268` 現查為 `import` ＋ `v.main()`。⛔ 非 `F3` 同型 |
| **G2** | 存在一個可被檢定之「現行對帳準則」 | ⚠️ **近失（near-miss）·已解消**：倉內**無可執行實作**（§0-2）⇒ 若逕依施工單 §2-2 之「**複製**對帳邏輯」照做，被複製之對象不存在，結果將是**自撰稻草人＋自證其盲**＝套套邏輯（`fixture-provenance` 所禁）。**解消之道**：準則之**定義**取 `CLAUDE.md:71-72`（權威序第 2 級·§0-3 逐字），輸入取**真實** log 與**真實**名單 ⇒ 僅「規則之程式表達」為 CC 所寫，其**語義出處獨立**。⇒ **不構成 `F3` 同型**（前提未被另案否證，只是受詞在文件而非碼中） |
| **G3** | 「原守命題」可由碼註與訊息文字判定 | ✅ **前提成立**：`results.append` 站點 **107** 處、皆帶訊息字串 |
| **G4** | `WG94b_runall.log` 為有效對照基準 | ✅ **前提成立**：該 log 產於**本 worktree**（`W-G.9-4b` commit B′），⇒ worktree 路徑類差異不介入 |
| **G5** | 「覆蓋量測是否改變被測物」可與 `G4` 分離 | ✅ **前提成立**：施工單已規定分離法（若 `G4` 比對不同，另跑一次**無覆蓋**之 `run_all`） |

🔒 **結論：無任一條為 `F3` 同型 ⇒ ⛔ 不停機，續施工。**
⚠️ `G2` 之近失**已於此具名**，並已**改變作法**（⛔ 非「默默調整措辭後照做」）：
本批**不複製不存在之碼**，改為**以正典條文為準則之出處**。此偏離已正面登記。

---

## §2 工具選擇與其**射程上界**（⛔ 事後不得放寬）

### 2-1 工具

```
$ python -c "import coverage"
ModuleNotFoundError: No module named 'coverage'
$ python -c "import sys;print(hasattr(sys,'monitoring'))"
True
```

⇒ `coverage.py` **不可得**（⛔ 不為本批安裝——改動環境非本批授權範圍）。
採 **stdlib `sys.monitoring`**（Python 3.13.11）之 `LINE` 事件，
**過濾至 `verify/run_verification.py` 之 code object**。

**為何足以支撐本命題**：`G1` 之命題為「**某行執行次數為 0**」——
`sys.monitoring` 之 `LINE` 事件係直接由直譯器於**每一行執行時**觸發，
其所產生之「已執行行號集合」即為該命題之**直接觀測量**；
`coverage.py` 7.x 於 Python ≥3.12 亦以同一機制取行覆蓋。
⇒ 二者對**本命題**等價；⛔ 本批不主張二者在其他命題（分支覆蓋等）上等價。

### 2-2 🔴 射程上界（⛔ 逐字寫死·事後不得放寬）

1. **subprocess 段不在射程內**：`run_all` 之末端夾具與部分工具以 `subprocess` 起
   ⇒ 子進程**不受本 `sys.monitoring` 追蹤**。本量測之射程**僅及於同進程執行之
   `verify/run_verification.py`**。⛔ 不得靜默截斷。
2. **行覆蓋只能證「未執行」，⛔ 不能證「執行了就有在守」**——
   一道執行了但**恆真**之閘（如斷言其自身剛填入之值）同樣不守任何事。
   🔒 **本批⛔ 不量恆真性**，須於報告**正面記為未涵蓋**（⛔ 非附註）。
3. **母體限 `run_verification.py`**：`app.py`／`wf_f*.py`／`stepg_pipeline.py`
   之內部斷言**不在本批母體內**。

---

## §3 可證偽預測（⛔ 事後不得回頭修改）

| # | 預測 | 證偽方式 |
|---|---|---|
| **G1** | 存在**至少一條** `run_verification.py` 之閘判定式，於一次完整 `run_all` 中**執行次數為 0** | 覆蓋資料顯示全部判定式皆執行 ⇒ **G1 破** ⇒ W-8 反向閘之失聲另有機制，本批定式錯誤，須重新定式 |
| **G2** | 正典對帳準則（§0-3·逐項**名目**相同）**無法**察覺任一期望 FAIL 項之**失敗訊息**改變 | 合成竄改被察覺 ⇒ **G2 破**（須查是何機制察覺的） |
| **G3** | 22 名目中**至少一項**，其原守命題現**無**任何仍活之閘覆蓋 | 逐項皆有接手者 ⇒ **G3 破**（須以三項射程證之，⛔ 不得以「找不到」代替「不存在」） |
| **G4** | 本批**零觸**：覆蓋跑之**判定層**與 `verify/out/WG94b_runall.log` 相同 | 出現任何判定改變 ⇒ **G4 破** |
| **G5** | 覆蓋量測**不改變被測物** | 若 `G4` 之比對**不同**，⛔ 不得逕判 `G4` 破——須**另跑一次無覆蓋之 `run_all`** 以分離二者，兩份 log 皆入庫 |

---

## §4 停機條件（⛔ 逐字採施工單 §4）＋ 一項**事前寫死之解讀**

**🛑 真域邊界 ⇒ 停機上呈 KL**：本批**無**預期之域邊界（純驗證基礎建設）。
若觸及，以【現況】→【要改成】→【對土地的影響】→【要你判斷】格式上呈，
⛔ 禁程式術語、⛔ 一句是非題只綁一個命題。

**🛑 立即停機（紅線）**：

- **`T-1`**：覆蓋量測**改變了判定層**（見 `G5`）⇒ 停機，全部覆蓋資料作廢。
- **`T-2`**：`Q2` 之**反向**自檢無鑑別力（改名目仍綠）⇒ 停機，該探針不得計入交付。
- **`T-3`**：任一名目之**當前失敗原因，與其入名單時之原因不同** ⇒ 🔴 **停機上呈**。
  ⛔ 不得逕自更新名單。

### 4-1 🔒 `T-3` 之**事前寫死解讀**（⛔ 非事後調整）

⚠️ 由 §0-4 觀之，`T-3` **有相當機率會觸發**。故其處置須**事前**定明，⛔ 不得事後裁量：

> **`T-3` 觸發時，CC 之行為 ＝**
> ① **⛔ 立即停止一切「動作類」工項**——本批之 §3 未授權清單已涵蓋全部可能動作
>    （不改名單、不修 FAIL、不立閘、不掛檔），故**實際上無動作可停**；
> ② **✅ 完成已在途之「純觀測」工項**（`Q1` 覆蓋表、`Q2` 探針、`Q3` 三欄盤點）——
>    **理由**：`T-3` 之受詞（「當前原因 vs 入名單時之原因」）**正是 `Q3` ② 欄之產物**；
>    於盤點途中停止觀測，將使 KL 收到一份**不完整**之證據而無從裁決，
>    與 `T-3`「上呈」之目的相違；
> ③ **🔴 於報告與聊天 ping 皆以最高優先序具名上呈**，⛔ 不埋在內文。

⇒ 本解讀**現在**寫死。若 KL 認為 ② 過寬，該判由 KL 為之，⛔ 非 CC 事後解釋。

**純技術項 ⇒ 自行做完至 push**：探針、覆蓋跑、盤點表、報告、考古節。

---

## §5 驗收（⛔ 逐字採施工單 §6·**禁以計數或列數為證據**）

| # | 閘 |
|---|---|
| 1 | **22 名目逐項**皆有三欄之填答，含「②③ 為空」者亦正面列出 |
| 2 | ② 之判定**來自覆蓋資料**，⛔ 不得只由 `try` 結構之閱讀推得 |
| 3 | 每一「無其他閘覆蓋」之答，皆附【目錄射程】【字樣射程】【連接方式】三項 |
| 4 | `Q2` 之探針**兩態皆印**（改訊息／改名目），且其反向自檢具鑑別力 |
| 5 | `Q1` 之**射程自述**齊備：subprocess 段不在射程內、行覆蓋不量恆真性——⛔ 皆正面寫 |
| 6 | `G4`／`G5` 之比對以**判定層逐項**為據，⛔ 不得以整檔雜湊或行數代替 |
| 7 | 每一出艙之數，其指令於**落筆當次重跑**且指令與輸出並列 |
| 8 | `git diff --numstat` 對 `app.py` 為**空輸出**；對既有文件之**刪除欄為 0** |
| 9 | 凡以雜湊作證一律記 **`git hash-object`**（⛔ 非 `md5sum`——本倉 `core.autocrlf=true`） |
| 10 | 凡書寫「正典」二字，就地標明**權威序層級**（本倉共 **4 級**） |

---

## §6 已預先承諾之判準（⛔ 事後不得放寬）

- ⛔ **零生產碼**：不動 `app.py`。
- ⛔ 不立新閘、不改任何既有閘之判定式或門檻。
- ⛔ 不改期望 FAIL 名單（四份皆然）、⛔ 不新增名單。
- ⛔ 不修任何既有 FAIL（含 `W-G G.1` 之 `KeyError: '0m'`、結構閘 `理論＝實跑`）。
- ⛔ 不重烤基準 CSV、⛔ 不設 `WV_BAKE`。
- ⛔ 不把 `wg_g1_smoke.py` 或任何檔掛入 `run_all`（§2-4 只盤不掛）。
- ⛔ 長跑期間不動 `app.py` 與 `verify/`。
- ⛔ 不為本批安裝任何套件（`coverage.py` 改以 stdlib 替代·§2-1）。
- ⛔ 不覆寫任何既有 log；新產物檔名與既有基準**分立**。
- 否定性宣稱須**同時**寫【目錄射程】【字樣射程】【連接方式】；字樣須語意窮舉、
  ⛔ 不預設符號名；⛔ 不以固定距離窗連接兩概念。
- 🔴 **`rc=1` 不構成「命題為偽」**（考古 64）。

🔒 **自誌（⛔ 逐條自檢·施工單 §8）**

1. **`W-G.9-4b` 立戒「加白名單制之消費點時，須現查供給端之全部上游」**
   ⇒ 本批凡下任何「某閘由 X 守」之判斷，須現查該閘之**全部執行前置**，
   ⛔ 不得由閘名或其所在區塊推定。**已套用之首例**：§0-2 之五式窮舉
   （⛔ 未以「應該沒有碼在讀」推定，而是五式裸 grep 並逐式記 rc）。
2. **考古 60（工具射程 < 命題射程）**：本批核心工具（行覆蓋）之射程**嚴格小於**
   命題（「有沒有在守」）⇒ §2-2 已逐字寫死，**且報告須置於顯眼處**，⛔ 非附註。
3. **考古 65 第五層（竄改自檢之受詞須保證可觀測）**：`Q2` 之反向自檢
   ⛔ 不得選一個「不論改不改都不會被比到」之受詞。
