# W-G.8 D-2b-23【甲】commit C：甲後逐格對帳 —— 🛑 **§9-3 停機上呈**

**產出**：CC／2026-08-09　**分支**：`wip/s1-endpart`　**⛔ 本 commit 生產碼零觸**
（實作在 commit B `79bf2d7`）
**實料**：`verify/out/probe_D2b23C_attrib_after.log`／`verify/out/probe_D2b23A2_params_after.log`
（皆新檔·**與甲前並存**·⛔ 未覆寫任何既有 log）

> # 🛑 結論：**12 格中 5 格與事前預測不符 ⇒ 依施工單 §9-3 停機上呈**
>
> ⛔ **未改預測**（§7-C 逐字：「不得修改事前預測以就結果」）。
> ✅ **實作本身與 §1 規格相符**——`0m R2` 之 `宗0×宗1` 由 `4.9205` **歸零**，
> 證明該界面確已只剩**一條線**。
> 🔴 **然規格<u>必要而不充分</u>**：三類殘餘（§三）皆為 §1 未涵蓋之情形。
> ⚠️ **`run_all` 尚未跑**（§7-F 未完成·理由見 §五）。

---

## 一、§7-A 時序（機械證明）

```bash
git merge-base --is-ancestor acc3eac 79bf2d7   # commit A  → B　⇒ rc=0
git merge-base --is-ancestor ea9fdc4 79bf2d7   # commit A′ → B　⇒ rc=0
```

⇒ **事前預測表確實早於實作**（⛔ 非事後補寫）。

---

## 二、§7-C 逐格對帳（母體 ＝ **12** 格·⛔ 非空）

現況／甲後皆為【倉】`verify/out/probe_D2b23C_attrib_after.log`（各格 `══` 段）；
甲前為【倉】`verify/out/probe_D2b22_attrib.log`。

| # | 情境·街廓 | 甲前 | **預測** | **甲後** | Δ | 判 |
|---|---|---|---|---|---|---|
| 1 | 0m R1 | 缺口 `12.0568` | 歸零 | 缺口 **`0.0046`** | `−12.0522` | ✅ **符**（< ①' 閘 `0.01`） |
| **2** | 0m R2 | 重疊 `76.2616` | 歸零 | 重疊 **`45.9766`** | `−30.2850` | 🔴 **不符** |
| **3** | 0m R4 | 缺口 `58.4792` | 歸零（容保留一 `4.94`–`5.84`） | 缺口 **`58.4792`** | **`0.0000`** | 🔴 **不符**（逐位不變） |
| 4 | 0m R6 ⛔退化 | `0`／`0` | 逐字不變 | `0`／`0` | `0` | ✅ 符（⛔ 不計入憑據） |
| **5** | 0m R5 | 重疊 `72.1964` | 歸零（容保留二 `≤4`） | 重疊 **`56.3293`** | `−15.8671` | 🔴 **不符** |
| 6 | 0m R3 | 缺口 `76.0262` | 歸零 | 缺口 **`0.0001`** | `−76.0261` | ✅ **符** |
| **7** | 3.5m R1 | 缺口 `12.0553` | 歸零 | 缺口 **`5.3881`** | `−6.6672` | 🔴 **不符** |
| 8 | 3.5m R2 | `0`／`0` | 逐字不變 | `0`／`0` | `0` | ✅ 符 |
| **9** | 3.5m R4 | 缺口 `58.4792` | 歸零 | 缺口 **`58.4792`** | **`0.0000`** | 🔴 **不符** |
| 10 | 3.5m R6 ⛔退化 | `0`／`0` | 逐字不變 | `0`／`0` | `0` | ✅ 符（⛔ 不計入） |
| 11 | 3.5m R5 | `0`／`0` | 逐字不變 | `0`／`0` | `0` | ✅ 符 |
| 12 | 3.5m R3 | 缺口 `0.0001` | 逐字不變 | 缺口 **`0.0001`** | `0` | ✅ 符 |

**計數**：符 **7**／不符 **5**。⛔ **扣除 R6 之 2 格退化後**：鑑別性 **10** 格中 **5 符／5 不符**。

**總量**：甲前 Σ（缺口＋重疊）＝ `365.5548` ㎡ → 甲後 `224.6572` ㎡ ⇒ **降 `140.8976` ㎡（`38.5%`）**。
🔒 ⛔ **「降了很多」不是通過**——判準為 §三 之預測類別，⛔ 非改善幅度。

**閘之後果**：`0m R1`（`0.0046`）與 `0m R3`（`0.0001`）之 `①' 覆蓋閘` **由破轉過**；
其餘破者仍破（`②-宗` 上界 R2 `3.1129`／R5 `2.5141`；`①' ` `0.01`）。

---

## 三、成因（**三類結構不同之殘餘**·⛔ 非同一件事）

### 3-1 類 A：**傾斜跨越第 2 宗之整個寬度**（`0m R2`／`0m R5`）

【倉】`verify/out/probe_D2b23C_attrib_after.log`（`[0m] R2` 段）：

| 對 | 甲前 | 甲後 | 判 |
|---|---|---|---|
| `宗0×宗1` | `4.9205` | **`0.0000`** | ✅ **界面已只剩一條線**（＝ §1 之標的，達成） |
| `宗0×宗2` | `25.2873` | `19.8142` | 🔴 殘 |
| `宗0×宗3` | `46.0539` | `26.1624` | 🔴 殘 |

⇒ 街角第 1 宗之遠側界（∥SIDELINE）之傾斜量**大於第 2 宗之寬度** ⇒ 其仍切進**第 3、4 宗**。
🔒 §1 只規範「第 1 宗與第 2 宗**之間**」之界面；第 3 宗以後之近側界仍 ∥ALLOC
（且 §8 逐字**不授權**動之）⇒ **殘餘係規格射程之外，⛔ 非實作瑕疵**。

### 3-2 類 B：**該側根本沒有第 2 宗**（`0m／3.5m R4`·逐位不變）

R4 全塊**宗數 ＝ 2**【倉】同檔（`[0m] R4` 段之「宗數 2」），而其**左右兩側皆有街角**
（`is_corner ＝ 4` ＝ 2 趟 × 2 側）⇒ **每側各只 1 宗** ⇒ **不存在第 2 宗**
⇒ `near_dir` 鏈**從未觸發** ⇒ 幾何逐位不變。
⇒ 該塊之楔形（`宗1·帶內 32.3854` ＋ `宗0·帶內 26.0938`）**無下一宗可承接**；
其正確歸屬應為**池**，而現行池帶由「已被佔用之 s 區間」排除之 ⇒ 仍成缺口。
🔒 **此情形 §1 完全未涵蓋**。

### 3-3 類 C：`3.5m R1` 左側（殘 `宗0·帶內 5.3881`）

甲後該格**重疊 ＝ `0.0000`** ⇒ 其後無宗與之相交 ⇒ **型態同類 B**（該側無承接者）。
🔴 **⚠️ 未證**：本批**未量逐側宗數**，故「該側只有 1 宗」係**推論**、⛔ 非實測
（`is_corner` 欄只給側別、不給該側宗數）。⇒ 列為**待量**。
（對照：同一街廓於 `0m` 之兩個楔形**皆已吸收** ⇒ `0m` 時該側有第 2 宗 ⇒ 退縮改變了分側。）

---

## 四、KL ②「唯一殘餘條件」之對拍 —— **預測在射程內·且成立**

【倉】`verify/out/probe_D2b23A2_params_before.log` vs `..._after.log`（各 **14** 組·`sha256` 逐組）：

| 路徑 | 組數 | sha256 甲前 vs 甲後 | 判 |
|---|---|---|---|
| **資格**（`(全案)／資格`·兩情境） | 2 | **完全相同**（`10522ebb…`） | ✅ **五實參未變** |
| 實配 | 12 | 7 組同／**5 組異**（`0m` R1・R2・R3・R5；`3.5m` R1） | ⇒ 見下 |

**異動之內容 ＝ 只有 `S_max`**：案級四參數 `A`／`B`／`C`／`tab6_burden` 於**全檔**逐位相同
（`A` 之 6 個相異值、`B=0.171042711`、`C=0.091318931`、`tab6_burden=0.40712387`
甲前甲後**集合相同**）。`S_max` ＝ `S_block_max − cum_S`，`cum_S` 隨宗地幾何改變
⇒ **本批改動之下游後果**、⛔ 非預測之前提破。

🔑 ⇒ **「街角資格不翻轉」之預測成立**：`is_corner` 計數甲後與甲前**逐格相同**
（`0m` `4/2/4/2/2/2`；`3.5m` `4/0/4/2/0/0`）【倉】`probe_D2b23C_attrib_after.log`（§2-3 表）。

### 4-1 🔒 射程之依據 ＝ **構造**（KL 裁 2026-08-09·⛔ 非「原意如此」）

被守護者係「**街角資格（`is_corner`）不翻轉**」之預測。一個量**唯有在其為該預測之
<u>因果祖先</u>（＝資格計算之輸入）時，方可能推翻之**。

- 資格由 `_corner_first_lot_G` 以 **`W_prev=0.0`／`is_corner=True`** 之**樂觀口徑**算出
  （`grep -n "W_prev=0.0)" app.py` ⇒ `:11922`；docstring `:11843` 逐字）
  ⇒ 該處 **`cum_S = 0`** ⇒ **`S_max = S_block_max`**，**結構上不含累積項** ⇒ 不受下游影響。
- 實配路徑之 `S_max`（`= S_block_max − cum_S`）**生於資格判定之後、且不回饋**
  ⇒ **在因果祖先之外**。

⇒ **射程 ＝ 資格路徑**。⛔ **非**因「當初想的是那個」，係因**別的路徑進不去**。

### 4-2 🔴 具名更正原 ②（KL 裁 2026-08-09·⛔ 上文一字不刪·追記）

原 ② 只點名**變數**、未點名**讀取位置** ⇒ 逐字讀確實涵蓋兩條路徑，**CC 之質疑正當**。
**更正後之 ②**（KL 逐字）：

> 逐格記錄**資格路徑**（`_corner_first_lot_G` → `_solve_G_one`）之五實參甲前／甲後值：
> 相同 ⇒ 預測成立；不同 ⇒ 作廢並依 §9-3 停機。
> 實配路徑之同名實參**不在本條射程**——其為**結果**、非**輸入**。

🔒 **一般規則**（KL 令入常駐規則與失敗考古）：
**不變條件須指明其所守護之預測，並指明該量於何處被讀。⛔ 只點變數名不足以界定射程。**
（本則由 claude.ai 犯、CC 現查抓出；已入 `CLAUDE.md` 與失敗考古**節 58**。）

### 4-3 🔑 5 組異動係**正面證據**（KL ③·⛔ 不得只寫「屬下游後果」）

**CC 獨立現查之對位**（⛔ 非引 KL 之敘述；以兩對 log 現算集合後比對）：

| 集合 | 內容 | 集合相等？ |
|---|---|---|
| 實配路徑 **`sha256` 異動**之組 | `0m R1`・`0m R2`・`0m R3`・`0m R5`・`3.5m R1`（**5**） | ✅ **True** |
| §二對帳表 **`Δ≠0`** 之格 | **同一 5 格** | 同上 |
| 實配路徑 **`sha256` 未異動**之組 | **7** 組 | ✅ **True** |
| §二對帳表 **`Δ=0`** 之格 | **同一 7 格** | 同上 |

⇒ **12/12 對位、零例外。**
🔑 **其中 `0m R4`／`3.5m R4`（甲後幾何逐位不變）之 `S_max` <u>亦未變</u>**
——若該變動係**不受控之擾動**，R4 沒有不動之理。
⇒ `S_max` **恰好且僅在**幾何改變處變動 ⇒ **本批之下游因果得到正面坐實**。

### 4-4 KL ④：異動 5 組 ＋ 其 `cum_S` 差值（**入庫留存**·⛔ 不得屆時才回頭重建）

`cum_S 差 = −ΔS_max`（∵ `S_max = S_block_max − cum_S`）。全部 `ΔS_max` **為正**
⇒ `cum_S` **減少** ⇒ 累積推進量變小，與「梯形吸收楔形 ⇒ 同一 G 所需之 S 變小」一致。

| 組（實配） | `S_max` 值數 | 相異對數 | `max\|ΔS_max\|` | `Σ\|ΔS_max\|` |
|---|---|---|---|---|
| `0m R1` | 8 | 6 | `0.356057279` | `1.170869193` |
| `0m R2` | 15 | 13 | `0.689073528` | `8.769696897` |
| `0m R3` | 14 | 5 | `1.637598046` | `7.888786443` |
| `0m R5` | 18 | 16 | `0.359656699` | `5.689462353` |
| `3.5m R1` | 9 | 5 | `0.196211896` | `0.875649520` |

**逐值明細（`0m R1`·全 6 對）**：
`37.423175859→37.586529638`（`Δcum_S −0.163353779`）／
`44.874396259→45.037313168`（`−0.162916909`）／
`53.103618069→53.459675348`（`−0.356057279`）／
`55.691198953→55.854117217`（`−0.162918264`）／
`62.739774250→62.902585731`（`−0.162811481`）／
`63.142224204→63.305035685`（`−0.162811481`）。
其餘四組之逐值明細見【倉】兩份 `probe_D2b23A2_params_*.log`（逐組 `×次數 S_max=…` 列）。

🔒 **⛔ 不得就此結案**：本表係 §7-F 之**必要輸入**——日後跑 `run_all` 時，
期望 FAIL 名單之每一列變動須能**指回這 5 組之哪一組**。

---

## 五、§7 其餘各項

| 靶 | 判 | 證 |
|---|---|---|
| **A 時序** | ✅ | §一（`rc=0` ×2） |
| **B 涵蓋** | ✅ | 受測 12 格名目與【倉】`verify/out/probe_D2b8_guard.log:8-19` 逐一相同 |
| **C 逐格對帳** | 🛑 | §二：7 符／5 不符 ⇒ **§9-3 停機**。⛔ 預測一字未改 |
| **D 歸屬** | ✅ | 殘餘皆以 **s 區間**具名（如 `宗0·帶內 s∈[-0.3248, 17.5649]`），兩量測器並列；`|甲−乙| ≤ 0.0350`，最小具名分項 `5.3881` ⇒ 鑑別力 ≈154×（同 `ca8cf6c` §3-4） |
| **E 退化隔離** | ✅ | R6 兩格單獨標示，已自「10 格」計數扣除 |
| **F 下游 `run_all`** | 🔴 **未執行** | 見下 |

🔴 **§7-F 未完成之逐字說明**（施工單 §10 要求）：`run_all` **本批未跑**。
**理由**：§9-3 之停機條件已於 §二 觸發；`run_all` 約 15–20 分鐘，而其期望 FAIL 名單之
逐項歸因**須以最終幾何為準**——若 KL 依 §9-3 裁定調整規格或撤回 commit B，
現在跑得之名單即刻作廢。⇒ **待裁後再跑**。
⛔ **本項<u>不得</u>被記為「已處理」**；⛔ 亦未先改名單（§7-F 逐字禁止）。

---

## 六、🛑 上呈 KL（三題）

1. **§9-3 之處置**：5 格不符係因 §1 規格**必要而不充分**（§三之三類）。
   請裁：(甲-2) 擴充規格至「類 A：第 3 宗以後之近側界亦承接」／
   (甲-3) 擴充至「類 B：無第 2 宗時楔形歸池」／
   (乙) 維持 §1 現狀並接受殘餘／(丙) 撤回 commit B。
   ⚠️ 甲-2／甲-3 皆須動 §8 之不授權項（第 3 宗以後界線方向／池帶構造）。
2. **§四之射程解讀**：KL ② 之「本預測」是否僅指**資格**路徑（CC 之解讀）。
3. **`run_all` 之時點**：是否於裁定後再跑（CC 建議）。

---

## 八、`run_all` 唯讀量測（KL 裁 2026-08-09·**⛔ 名單一列不動**）

> 🔒 **歸因標的 ＝ `commit B` <u>單一變更</u>**（`K-9-5-4 ②` 之實作）。
> ⛔ **非**最終 §7-F 交付；最終交付仍以**最終幾何**為準。
> 🔒 **本次係兩次跑之第一次**；類 B（R4 楔形歸池）落地後再跑第二次，
> 兩次之差 ＝ 類 B 之爆炸半徑。**每次跑各攤一個變更，⛔ 不得混。**
> ⛔ 本次結果**不得作為任何閘或停機條件**（§9-3 之停機已由 §二 對帳表觸發）。

### 8-1 基準之取得（⛔ 未用倉內舊 log 當基準）

倉內最近之 `run_all` ＝ `verify/out/D2b1_runall.log`（`D-2b-1`），其間隔 8 批生產碼變更
⇒ 以之為基準即「**比錯基準向量**」（失敗考古節 53）。
⇒ **另於獨立 worktree 檢出 `79bf2d7^`（＝`ea9fdc4`·pre-B）跑一份**：
`verify/out/D2b23B_runall_preB.log`。
**該樹確為甲前之碼**：`grep -c "near_dir" <preB樹>/app.py` ⇒ **0**（本樹為 25）。

### 8-2 名目層級 —— **零新增、零消失**

| 項 | 值 | 指令 |
|---|---|---|
| 甲後 FAIL 名目 | **22** | `grep -c "🔴 FAIL" verify/out/D2b23B_runall.log` |
| pre-B FAIL 名目 | **22** | 同上（`_preB` 檔） |
| 現行期望名單 | **22** | `grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_D2b1切換後.txt` |
| 三者名目集合 `diff` | **全空** | `diff <(...|sort) <(...|sort)` |

⇒ **差異清單 ＝ 空**；逐項歸因**不適用（無列可歸因）**。
🔒 **期望 FAIL 名單一列未動**（§7-F 逐字維持有效）。

### 8-3 值層級 —— 實質差異 **52** 行（排除 worktree 路徑雜訊後），**全部為 R1**

| 類 | 內容 | 歸因 |
|---|---|---|
| **①** | `0m R1`：`①' 覆蓋閘破 12.0568` ⇒ **通過**（`[T2-DIAG] … 覆蓋殘差 0.0046`），**改破 Gate C**：`🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）` | 🔴 **12 格外之新發現** |
| **②** | `3.5m R1`：`①' 覆蓋閘破` 之值 `12.0553` → **`5.3881`**（仍破） | ✅ **12 格內已歸因**——與 §二對帳表 #7 之甲後值**逐位相同**（兩條獨立路徑：`run_all` 之 StepG vs 探針之 replicate） |
| **③** | 新增之 `[T2-DIAG]`／`[Δ_i·R1·…]` 診斷列；下游 `[碎片]`／`[W-D.4]` 之失敗訊息隨根因轉述而改 | 皆為 ①／② 之**從屬**（執行推進更遠 ⇒ 印出更多；根因字串隨之改寫） |

#### 🔴 8-3-1 類 ① 之精確表述（⛔ 不得寫成「新缺陷」）

Gate C 於 `0m R1` **甲前係「沒到」**——該塊早死於池閘（`stepg_pipeline.py:972` 之包裝重拋前綴
「抵費地計算失敗」可辨），Gate C 在其後、從未執行。甲後池閘通過 ⇒ **首次抵達** ⇒ 破。
⇒ **係「新抵達」、⛔ 非「新產生」**（失敗考古§七-1「0 有三義」）。
⚠️ Gate C（`結構閘 理論＝實跑`）本即**已登記之待裁項**——交接文 §2-3
「🛑 Gate C（D-2b-3 起·**循環相依**）」，⛔ 非本批新開之問題。

### 8-4 🔴 **覆蓋盲區（⛔ 必讀·否則 §8-3 會被誤讀為「其餘皆未變」）**

`run_all` 之 StepG **只到 R1**：

| 量 | 甲後 | pre-B |
|---|---|---|
| `T2-DIAG] 街廓 …` 之命中（依街廓） | **R1 × 3**（⛔ 無其他街廓） | **0**（R1 早死於池閘、未印） |
| `StepG…] 街廓 …` 之街廓 | **R1 × 2** | 同為 R1 |

⇒ **R2–R6 之 StepG 於 `run_all` 從未執行**（R1 先失敗即中止）
⇒ 其「零差異」係「**沒到**」、**⛔ 非「到了且沒變」**。
🔒 **R2–R6 之覆蓋<u>只由 12 格 solo 探針提供</u>**（§二對帳表）——該表已測得
R2／R3／R5 皆有值變動。⇒ ⛔ **不得以本次 `run_all` 之「零差異」宣稱本批影響僅及 R1。**

### 8-5 §9-3 停機狀態 —— **不變**

本次為唯讀量測，⛔ 未改變停機狀態；亦**未順手修**任何 12 格外之新破（KL ⑥）。

---

## 七、異動清單

| 檔 | 異動 | 性質 |
|---|---|---|
| `verify/out/probe_D2b23C_attrib_after.log` | 新增（甲後實料） | 實料 |
| `verify/out/probe_D2b23A2_params_after.log` | 新增（甲後五實參） | 實料 |
| `verify/probes/probe_D2b22_attrib.py`／`probe_D2b23A2_params.py` | `WV_OUT_NAME` 覆寫 ＋ **拒絕覆寫**守衛（fail-fast） | 探針·⛔ 非生產碼 |
| `docs/reports/W-G.8_D-2b-23C_甲後逐格對帳.md` | 新增（本檔） | 文件 |

⛔ `app.py`／`verify/stepg_pipeline.py` 於**本 commit** 零觸（其變更在 commit B `79bf2d7`）。
⛔ 12 格全保真基準**未重烤**。
