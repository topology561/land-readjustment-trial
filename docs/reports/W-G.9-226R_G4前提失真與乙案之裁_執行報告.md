# W-G.9-226R　`G4` 前提失真之登記 ＋ 乙案之裁（執行報告）

**批次性質** ＝ 零生產碼·append-only。**基座** ＝ `11c9216315d540cc985b1dddb26670dccd7a81c2`（`left-right` `0 0`）。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；一切 `rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
payload 一律 `Write` 落檔、⛔ heredoc（`GB-143` 補款二）。

---

## 工項零　本單原封入倉 ✅ PASS

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **4722** | 64 | **0** | `aba79ed4577a151e9507e0234725c2187a8a8db22a3532d6a9e427e0bfa0f331` |
| 入倉（索引側 blob） | **4722** | 64 | **0** | `aba79ed4577a151e9507e0234725c2187a8a8db22a3532d6a9e427e0bfa0f331` |

🔒 **逐位相同 ＝ `True`**（位元組數差 ＝ `+0`·`戒 40` 二端並報）；`cat-file blob` 之 `rc` ＝ **0**。
🔒 對照組（器須紅）：翻首位元組 ⧧ blob ＝ `True`；對照組（器須綠）：blob 自比 ＝ `True`。
🛑 射程界：只證「**落檔→入倉**」，**⛔ 證「聊天原文→落檔」**。
入倉路徑 ＝ `docs/orders/W-G.9-226_G4前提失真與乙案之裁.md`。

---

## 工項一　鑄 `自誤 284`（`G4` 前提失真·⛔ 併入 `283`）

### 一、號之推導式（落地後當場復算·`VR-091` 補款二之三數同格併載）

| 量 | 值 |
|---|---|
| 母體 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`　**551600** B／**6210** 列（索引側·**列框**）|
| ① 定義框之**列命中** | **276** |
| ② 其中被**範圍框**重咬 | **3** |
| ③ **純單筆列命中** | **273** |
| 相異號 | **278** |
| `MAX` | **285** |
| 範圍框列 | **8**（恆定期望·符）|
| 缺號清單 | `[106]`（`VR-091` 補款五之恆定期望·符）|

⇒ 前次末號 ＝ **283** ⇒ **本批鑄 `自誤 284`（工項一）＋ `自誤 285`（工項三）**；**次號 ＝ 286**。
🔒 逐號之定義框列命中：`自誤 284` ＝ **1**（`:6109`）／`自誤 285` ＝ **1**（`:6165`）。
🔒 `GB` 簿 `MAX` ＝ **148**（本批**⛔ 鑄 `GB`**）；`VR` 真 `MAX` ＝ **91**（本批**⛔ 鑄 `VR`**）。

### 二、`:16120`／`:16136` 逐字之當場復現（母體 `app.py` **1414673** B／**23906** 列·**列框**）

| 列 | ind | 逐字 |
|---|---|---|
| `app.py:16120` | 12 | `# ---------- 偵測開發年期變動：自動重置各年度比例 ----------` |
| `app.py:16136` | 20 | `if _k.startswith('const_ratio_') or _k.startswith('replot_ratio_'):` |

🔒 機驗：`:16120` 含「開發年期」＝ `True` ∧ 含「重置各年度比例」＝ `True`；
`:16136` 含 `const_ratio_` ＝ `True` ∧ 含 `replot_ratio_` ＝ `True` ∧ **含 `f3L` ＝ `False`**。
🔒 `自誤 284` 之表另載 `:16135`／`:16138`／`:16139`／`:16140` 之逐字與 ind（共六列）。

### 三、受詞與族之具名（要旨）

受詞 ＝ **單對既有碼區之性質作概括誤述**（稱 `:16135`–`:16140` 為「整批 session_state 清除碼」，
實為財務分析頁「開發年期變動 ⇒ 重置各年度比例」之**局部**殘留鍵清理）。
🔴 **後果之重點已逐字入登記**：該誤述驅動了一個**結構上不成立之設計**（持久旗標依賴清除路徑同步），
而真失效機制係 Streamlit widget 生命週期，與 `:16135` 之觸發時點不同
⇒ **即使 `G4` 為真該設計亦不足** ⇒ **危害⛔ 止於「閘之錨誤」，而及於「設計前提誤」**。
🔒 族：與 `自誤 283` **非同族**（`283` ＝ 應被改而**不存在**者；`284` ＝ **存在而被說錯**者·受詞互斥）；
與 `自誤 272` **同度而受詞不同**（`272` ＝ **倉之態**；`284` ＝ **碼之語義**）。
🔒 通則：**凡單中對既有碼區之性質作概括描述（「整批」「全域」「所有」等），須於同單當場出艙該區之逐字與其判準；⛔ 以概括語入單。**
**責** ＝ 發單側。

---

## 工項二　`G4` 四案之裁：**採乙案**（⛔ 鑄號·`戒 36`）

### 一、落地處與檔列

| 項 | 值 |
|---|---|
| 落地檔 | `docs/reports/W-G.9-219R_停機_前置閘G4.md`（**17001** B／**257** 列）|
| 裁之節首 | `:220`（該標題全檔命中 **1**）|
| 其**受詞**（四案表）之節首 | `:170`（全檔命中 **1**）·四案表列 `:174`–`:177`·⛔ 追改一字 |

🔒 **「既有正字處」之擇定理由（`grep` 定位·⛔ 新創檔）**
四案之**唯一**在倉載體 ＝ 上開 `-219R` 報告之 `### 候議之最小改動` 節（全倉 `.md` **549** 檔中命中 **1**）。
🔒 **體例之出處（⛔ 自創）**：本倉既有「報告內含**他批**之就地加註／裁決」共 **23** 例，
形如 `docs/reports/W-G.9-62_閘之判別力與GB-83裁示.md:328`
「`## 🔧 就地加註｜\`W-G.9-62\` 之裁決（\`W-G.9-63\`·claude.ai·⛔ 上文原句一字不刪）`」。

### 二、四案理由之逐案出艙（自落地文當場取·⛔ 憑記憶）

| 案 | 判 | 理由 |
|---|---|---|
| `甲`（改 `:16136` 之前綴條件）| 🔴 **⛔** | 觸發點 ＝ 財務頁年期變動，與「街角地區塊未渲染」**時點不同** ⇒ **接不上**；且 `-219R` 已逐字禁「⛔ 自行修改該清除路徑」 |
| `丙`（旗標存**值**·比對相等）| 🔴 **⛔** | 使用者輸入值**恰等於預設（`3.5`）**時，key 被丟棄後比對**仍相等** ⇒ **誤報「使用者輸入」**；而 `3.5` 即預設值 ⇒ **最常出現之情形、⛔ 邊角** |
| `丁`（於未渲染分支就地清旗標）| 🔴 **⛔** | 僅堵**已列舉之一條**未渲染路徑；其他成因不觸發清除 ⇒ 旗標**再度失真** |
| `乙`（**衍生旗標**）| 🟢 **採** | 旗標之有效性**綁於 key 之存在性**——被丟棄者**即該 key** ⇒ 旗標**不可能活得比值久**；🔒 **係不變式、⛔ 成因列舉** |

🔒 機驗：`乙` 列含「🟢 **採**」＝ `True`；四案逐列皆命中 **1**。
🔒 **`丙` 之否決理由⛔ 為「多一個鍵」**——`-219R:176` 備註欄之「同為自我修復」一語**於本裁被推翻**，
其真缺陷係**在最常見情形下失效**。此點已於落地文逐字載明。

### 三、乙案之附加約束（二項·機驗各命中 **1**）

1. **丟棄過期旗標須發生於顯示處讀取<u>之前</u>**（「**先調和、後顯示**」，⛔ 先顯示後調和）。
2. **該路徑須以<u>實測</u>坐實，⛔ 以推理充證**——`-219R` 工項三 `a` 之五情境須實跑，
   其中「key 被丟棄」一情境須**真被觸發**。

🛑 **本裁⛔ 解除 `W-G.9-219R` 工項一／二／三之停機**——實作仍待新單或補款明載後方得動工。

---

## 工項三　鑄 `自誤 285`（單缺【驗收】段）＋ 通則入正字

### 一、`grep` 結果（**落地前**之數·即鑄號決策所據）

母體 ＝ 索引側四簿（自誤簿／`GB` 簿／`VR` 簿／`CLAUDE.md`）·**列框**：

| 字樣 | 落地前命中 | 其中**同受詞之號** |
|---|---|---|
| `【驗收】段` | **5** | **0**（`CLAUDE.md:1869`/`:1875`/`:1882`/`:1887` ＝ `序 11` 節·其節首逐字「🛑 本節⛔ 鑄號」；`驗證裁定登記表.md:4665` ＝ `VR-069` 補款之案由）|
| `判定組` | **4** | **0**（皆同一 `序 11` 節內）|
| `不可回測` | **1** | **0**（`CLAUDE.md:1878`·所指係「單⛔ 在倉」之**另一成因**）|
| 自誤簿含「驗收」之**標題列** | **9**（`118`／`128`／`134`／`151`／`196`／`201`／`202`／`245`／`276`）| **0**（`245` ＝ 判準結構上不可滿足；`276` ＝ 所令之項未出艙·責 CC）|
| `GB` 簿含「驗收」之標題列 | **0** | **0** |

⇒ 🔒 **無同受詞之既有號 ⇒ 依單之令<u>鑄一號</u>**（`自誤 285`），**⛔ 以併記行之**。

### 二、🔒 自指增量之機械核對（`GB-147` 處置三／`自誤 258`・`264` 之要求）

本批 payload 自身含上開字樣 ⇒ 落地後之數**必**高於落地前。逐字樣核對：

| 字樣 | 落地前（`HEAD`）| 落地後（索引）| 增量 | payload `A`＋`B`＋`D` 內之列命中 | 核 |
|---|---|---|---|---|---|
| `【驗收】段` | **5** | **15** | **+10** | **10** | ✅ |
| `判定組` | **4** | **8** | **+4** | **4** | ✅ |
| `不可回測` | **1** | **3** | **+2** | **2** | ✅ |

🔒 payload `C` 落於 `-219R` 報告、本單落於 `docs/orders/` ——二者**皆⛔ 在四簿母體內** ⇒ ⛔ 計入增量。
🔒 ⇒ **增量全由本批自身解釋，⛔ 有未歸屬之殘差**。

### 三、判與併記

🔒 **⛔ 為 `W-G.9-219R` 批之失敗**——`rc` ＝ `1` 之義為「**無從回測**」，**⛔ 「回測不過」**。**責 ＝ 發單側。**
🔒 **CC ⛔ 代擬該段係正確處置**——該判理**⛔ 本則所創**：`CLAUDE.md:1887`–`:1888` 已逐字載
「⛔ 由 CC 代擬其【驗收】段（代擬即使該閘之受詞與其守護者同源 ⇒ 判別力歸零，同 `GB-147` 之理路）」，
係 `W-G.9-225` 工項三 `c` 所落。

### 四、通則之入正字（一般化）

落地處 ＝ `CLAUDE.md:1895`（檔尾新節·**⛔ 追改 `:1887`–`:1888` 一字**；機驗其逐字仍在 ＝ `True`）。
四款：**一** 受詞之獨立性（受測方提供 ⇒ 判別力為零）／**二** 缺漏時須 **loud 拒測**、⛔ 靜默綠／
**三** `rc ≠ 0` 之**二義須逐次辨明**（「無從回測」⛔ 等同「回測不過」）／
**四** 射程僅及「閘與受詞之**來源關係**」、⛔ 及受詞之**內容**是否正確。

---

## 閘 `P`（生產碼五檔·前後逐檔 `sha256`）✅ PASS

| 檔 | B | `sha256`（改前 ＝ 改後·逐位相符） |
|---|---|---|
| `app.py` | 1414673 | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

🔒 改前值之出處 ＝ `docs/reports/W-G.9-219R_停機_前置閘G4.md` 之 `G1` 態錨（同基座鏈）。

---

## `numstat`／`left-right`／檔數／`戒 40`

| 檔 | 增 | **刪** |
|---|---|---|
| `CLAUDE.md` | 27 | **0** |
| `docs/orders/W-G.9-226_G4前提失真與乙案之裁.md` | 64 | **0** |
| `docs/reports/W-G.9-219R_停機_前置閘G4.md` | 41 | **0** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 105 | **0** |
| **四檔小計（穩定值）** | **237** | **0** |
| `docs/reports/W-G.9-226R_…_執行報告.md`（**本報告**）| 🔒 **⛔ 於本報告內釘死**（自指）| **0** |

🛑 **`deletions` ＝ `0`（全檔）**——本批**⛔ 動任何工具檔** ⇒ 工項三 `b`（`VR-069` 補款）所令之
「工具檔 `deletions` 逐行具名」之義務**於本批無受詞**。
🔒 `left-right`（推前）＝ **`0 0`**。追蹤 ＝ **1618** 檔／`.md` ＝ **549** 檔（`ls-files -z`）→ 含本報告後 **1619**／**550**。
🔒 本批各檔之索引側 `CR` 皆 ＝ **0**（`GB-139`）。

**`戒 40`（二端位元組數同格並報）**

| 檔 | payload B | 分隔符 B | 工作樹 `Δ` B | 核 |
|---|---|---|---|---|
| 自誤簿 | `A` **3420** ＋ `B` **3271** | `(6+1)` ×2 | **+6705** | `3420+3271+14 = 6705` ✅ |
| `-219R` 停機報告 | `C` **2873** | `6+1` | **+2880** | `2873+7 = 2880` ✅ |
| `CLAUDE.md` | `D` **1880** | `6+1` | **+1887** | `1880+7 = 1887` ✅ |

🔒 **`Δ` − (payload ＋ 分隔符) ＝ `0`（逐檔·落地器之 `assert` 守之）**。

## 🟡 仍待（⛔ 本批處置）

1. 🛑 **`W-G.9-219R` 之實作仍停**——本批僅裁四案之取捨；動工須**新單或補款**明載「工項一 `a` 改依乙案」。
2. 🟡 **`GB-148` 之碼面處置**（顯式根參數 ＋ `--selftest` 走同一路徑）——併期末批。
3. 🟡 **`自誤 282` 補款之候議三案**（轉述入倉與否）——候發單側／KL 裁。
4. 🟡 **`G2` 解析器之敏感度**（`-225R` 工項一 `c` 之出艙候裁）——候發單側裁。
5. 🟡 已失效哨兵殘留（`哨兵-Ｑ` **1** 檔／`哨兵-Ｒ` **6** 檔）之逐處處置——併期末批。
6. 🛑 `K-9-5-2-a`-3 與 `K-9-5-3` 之逐字互斥（休眠·復活即**真域邊界**）——候 KL。
