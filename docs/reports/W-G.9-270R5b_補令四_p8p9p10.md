# `W-G.9-270R5b`　補令四之執行（坑 `as`／`at`／`au` ／ `p8` ／ `p9` ／ `p10`）

> **態**｜開工 ＝ `3d3aa80d66ac1b8a5022ca24c199cd8028e39a86`（`ls-remote` 實查·`left-right ＝ 0　0`）｜
> 工項零後 ＝ `630d80a9390afbc1804607b10bc0f203a50a5f80`
> **級**（作業常規追加五 `§二`）＝ **中**（純登記 ＋ 純診斷·**零生產碼**）
> 🛑 **本批⛔ 辦**（補令四 `§七`）：⛔ 動 `34` 檔一字（含 `MINA_QU_EXPECT`／`HALF_EXPECT`／`TIER_EXPECT`）／
> ⛔ 落地 `GB-164`・`GB-166`／`GB-162` 三禁令維持／⛔ 判 `p6` 之交錯為缺陷／⛔ 呈 KL 任何事／
> ⛔ 以「現態無一戶落帶」弱化 `GB-164`。
>
> 🔒 **命名（`常規三`·發單側 `§一-1` 已准）**：落檔前綴一律 `WG9270R5_`；本報告屬 **`R5` 系**，
> 取名 `W-G.9-270R5b`（⛔ 撞已入倉之 `W-G.9-270R5_…`）——**提案並續辦**，候核可。

---

## `§零`　開工閘 ＋ 工項零

### 零-1　開工閘（當場復現）

| 受詞 | 實測 | 判 |
|---|---|---|
| `ls-remote` 主線 | `3d3aa80d66ac1b8a5022ca24c199cd8028e39a86` | ✅ ＝ 補令四 `§零` 所載 |
| `ls-remote` `p3a`（凍存·⛔ 併線） | `05a11bd665f6c2790ff821c9e3a1606651e345d4` | ✅ |
| `left-right`（`HEAD…origin`） | `0　0` | ✅ 工作區為 **wip 態** |
| 生產碼 `34` 檔 | `app.py` blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`／`verify/` 單層 `*.py` `33` 檔 | ✅ |
| `verify/baselines` | `298` 檔·聚合 `sha256` `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |

### 零-2　工項零（補令四之原封入倉·commit `630d80a`）

| 量 | 值 |
|---|---|
| **條款一** `CR` | **`0`**（`LF` `158`·`CRLF` `0`）——位元組層**二法並取**；判別力造甲（真 CRLF）得 `2` ⇒ ⛔ 偽陰 |
| **條款一** `core.autocrlf` | system `true` ／ global **未設**（`rc=1`）／ local `false` ⇒ **有效值 `false`** |
| **條款二** blob `sha1` | `80496b2bf59b457ee68a434a18e238e65944aca3`（`index` 時 ＝ `HEAD:` 時·同一物件） |
| **條款二** blob 內容 `sha256` | `6fe2c71ae05d40239734f54ec19b94f2fe8243c984f56529a803bbcdf2e864fc` ＝ **磁碟值** ⇒ 🟢 **往返逐位保存** |
| bytes | `13173` ＝ `13173`；逐位 `diff` ＝ **DIFF-EMPTY** |
| **條款三** 來源逐字 | 主 checkout `docs/orders/` 之 untracked 檔·`13173` B·⛔ 由 KL 於本回合貼入 |

🔒 **對照組（證此量測非恆真）**：補令一 blob `8e55b176…` 之內容 `sha256` ＝ `af2ba742…`（**相異且逐位相符**）；
空串之 `sha256` ＝ `e3b0c442…`（當場印出·以坐實「管線斷而輸出為空」之徵候）。
🔒 **號占用閘**（`追加四` 補款 `一`）：本補令之單號 ＝ `W-G.9-270`，係**已在辦之單之補令**、**⛔ 新號**
⇒ 其占用本即存在（宣告框 `D1`：`docs/` 內檔名含該號者 **`8`** 檔；對照組 `W-G.9-269` ＝ **`27`** ⇒ 器非紅）
⇒ 本閘之受詞（新單號之撞號）於此**⛔ 適用**；照實具名，**⛔ 停機**。

---

## `§一`　序 `1`：鑄坑 `as`／`at`／`au` ＋ `ALPHA_LABELS` 擴充 ＋ 框字串之改正

### 一-1　🆕 本波新增之坑 `as`〜`au`（補令四 `§一-2` **准鑄**·標籤續 `ar` 之後·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `as` | 🔴 **`cat -A \| grep '\^M'` 於 UTF-8 CJK 文本恆有<u>偽陽</u>**：位元組 `0x8D` 之轉義形為 `M-^M`，其**尾二字元即 `^M`** ⇒ 命中數 ≈ 含該位元組之列數，**與 CR 無關** | 補令一之受詞檔實測得 **`47`**，而其真值 `CR ＝ 0`；🔴 **`47` 讀起來像一個正常的行尾統計**——樣本逐字 `-8M-^MM-eM`／`^GM-^MM-eM`，須**印出命中片段**方見其為 `M-^M` | 以**位元組層**直讀（`od -An -tx1` 之 `0d` 計數 ⋀ Python `rb.count(b'\r')`）**二法並取**，且**須對真 CRLF 造甲得非零**（本批造甲得 `2`） |
| `at` | 🔴 **`grep -c $'\r'` 對真 CRLF 檔得 `0`（<u>偽陰</u>）** ⇒ ⛔ 得用於 CR 之量測 | 本批造甲（`printf 'a\r\nb\r\n'`·真值 `2`）實測得 **`0`**；🔴 **其偽陰之方向最險**——「`0`」恰是「此檔乾淨」之期望值 ⇒ **與正解同形** | 同 `as`：位元組層二法並取 ⋀ 造甲須得非零。🔒 **本坑係由 `as` 之交叉驗證撞見**——若只換一支工具而不設造甲，二紅皆不可見 |
| `au` | 🔴 **Python `io.open(p, 'w')` 於 Windows 預設 `newline=None` ⇒ `\n` <u>靜默</u>寫成 `\r\n`**；寫回既有 LF 檔即**全檔換行改寫**，而**無任何出艙** | 本批之報告初落得 `CR ＝ 305`（＝ 其列數），而 `docs/reports/*.md` 之既有體例為 blob 側 `CR ＝ 0`；🔴 **徵候完全正常**——內容逐字無誤、渲染無異、`git status` 僅顯示 `A` | **入倉前逐檔量 `CR`**（同 `as` 之二法）並**與該類檔之既有體例對拍**；寫回時明寫 `newline=''`（或以 `'wb'` 寫 bytes）。🔒 併：`verify/out/*.log` 含 `CR` 係**既有體例**（前窗 blob 側實測 `22`／`171`／`89`）⇒ ⛔ 判其為破損 |

🔒 **三則皆已以<u>對照組</u>坐實**（真 CRLF 檔之二造），⛔ 推斷——此即補令四 `§一-2` 所稱之鑄坑要件。

### 一-2　`ALPHA_LABELS` 之擴充（`ar` → `au`）＋ 框字串之改正

| 受詞 | 改前 | 改後 |
|---|---|---|
| `ALPHA_LABELS`（`verify/probes/probe_WG9269_pit_index.py`） | 止於 `"ar"` | 續加 `"as", "at", "au"` |
| `run(...)` 之標題字串 | `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`ak\`` | `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`au\`` |
| `issuer_frame(...)` 之標題字串 | 同上 | 同上 |

🔒 **照實一句（補令四 `§一-3` 所令）**：**此前每一次坑索引之出艙，其所具名之框字串皆與其實作之框相異**
——實作已達 `ar` 而字串仍書 `ak`。**⛔ 追改既有報告**（`常規四（九）五`）。

🔴 **CC 之照實併呈：陳舊處實為<u>三</u>處，而單只令改二處**
——第三處 ＝ 同檔之**註解**（其逐字 ＝ `# 字母系：\`a\`〜\`z\` ＋ 二字母形 \`aa\`〜\`ak\``）。
🛑 **本批⛔ 改之**（⛔ 逾核可射程·體例同 `W-G.9-194 裁三`），**具名候發單側**。

🔒 **`verify/probes/` ⛔ 在生產碼 `34` 檔之列**（正面列舉 ＝ `app.py` ＋ `verify/*.py` **單層**）
⇒ 本項為**零生產碼**。

### 一-3　🩸 對 `W-G.9-270R5` 一數之**更正**（補令四 `§零` 所指之框差）

🩸 **被更正者**：`W-G.9-270R5` 之聊天回報與其 `§七-4`／`§九` 所載
「`probe_WG9270R4_*` **`5`** 件·`WG9270R4_*.log` `5` 件」⇒ 合計 **`10`**。
🔒 **⛔ 追改該報告一字**（`常規四（九）五`）。

🔒 **當場復算（二框並報）**

| 框 | `probe_WG9270R4_*` | `WG9270R4_*` | 合計 |
|---|---|---|---|
| 甲：`git ls-files` ＠ `HEAD`（**追蹤**） | **`3`** | `5` | **`8`** |
| 乙：`ls` 工作區（含 untracked） | **`3`** | `5` | **`8`** |

對照組（須非 `0`）：`WG9270R2_` 之追蹤件數 ＝ `4` ⇒ 器非紅。
逐件（框甲）：`probe_WG9270R4_p6_interleave.py`／`_p7_mina.py`／`_ziwu_occ.py`；
`WG9270R4_closegate.log`／`_p6_interleave.log`／`_p7_mina.log`／`_pit_index.log`／`_ziwu_occ.log`。

🔴 **⇒ 發單側之 `8` 為真，我所報之 `10` 為<u>誤</u>，且其成因<u>⛔ 框之別</u>**（二框皆得 `8`）
——係我**逕書「探針 `5` 件」而未實查**（真為 `3`；當時所見之 `5` 係 `WG9270R*` 全體探針中之一部分）。
🔒 **形** ＝ **以一眼掃過之印象充實測**（`數字錨引倉檔·禁憑記憶出艙` 之違反）。
🔒 **⇒ 補令四 `§零` 之令「凡出艙件數須同格具名其框」已於本節施行**；
**結論⛔ 受影響**（二框下 `R4` 前綴皆已被占用 ⇒ `§一-1` 之核可不動）。

---

## `§二`　序 `2`：`GB-7` 末端追加 ＋ `W-D.4` 規格末端追加（commit `86bc3dd`）

| 落點 | 受詞 | 增量 |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md`（檔尾） | **`GB-7` 之末端追加**（`§二-2`） | `+72`／`−0` 列·`812179 → 817392` B |
| `docs/W-D.4_合併分配規格.md`（檔尾） | **但書式之描述已為 KL 二裁所取代**（`§四-4`） | `+58`／`−0` 列·`16620 → 20356` B |

🔒 **`append-only` 之機驗**（基座 `01c61a9`·⛔ `--cached`）：二檔皆 `new.startswith(old)` ⋀ `len(new) > len(old)` ＝ **OK**；
判別力［必 `FAIL`］`old.startswith(new)` ＝ `False`（期 `False`）；`numstat` 刪除欄逐列為 `0`；二檔 `CR` 皆 `0`。

🔒 **`GB-7` 之三款（`§二-2` 所令）已載於其登記節**：`(a)` 前提已被真資料走過、現不成立／
`(b)` 本批之實測值與其角色（六街廓二形逐塊表）／`(c)` 時態錨 `01c61a9`·`2026-09-13`。
🛑 **⛔ 改 `GB-7` 之受詞一字、⛔ 解除、⛔ 重鑄**。

🔒 **`W-D.4` 之更正**：其 `:78` 之「僅標式不算值」**⛔ 再適用**；現行正解 ＝ **實算而⛔ 出艙**
（KL `2026-09-12` 裁二 ＋ 補令一 `§一-2`）⇒ **碼面行為即正解、⛔ 缺陷**（補令四 `§四-4` 逐字）。
🛑 **⛔ 刪或停算但書式欄**（KL 裁令其保留）；**`自誤 368` 之本文⛔ 追改**。

---

## `§三`　序 `3`：**`p8` —— `R4` 深度 `33.10 → 33.11` 之歸因**（commit `8356f09`）

🛑 **本節⛔ 主張其為缺陷、⛔ 提任何處置、⛔ 改任何常數**（補令四 `§四-1` 款 `5`）。

### 三-1　款 `1`：候選集之縮小（**框須具名**）

**框** ＝ `git log -S'V6_1' -- <正面列舉之路徑>`——路徑逐一為 `app.py`／`verify/run_verification.py`／`verify/`；
`rev` 範圍 ＝ 預設（全史）。
**對照組**：`git log -S"'D_avg': round(D_avg, 2)" -- app.py` ⇒ **`1`** 命中（`1b290e4` 初始遷移）⇒ 器非紅。
⇒ **候選 ＝ `4214b3a`**（`W-G.9-123 驗證側改讀 V6_1.dxf`·`K-9-20` 落地·KL 裁 `2026-08-24`）。

### 三-2　款 `3`：**判別力對照（必辦·先於二分）**

於 **`K-8 段三 commit A` ＝ `7fa957e`** 實跑（**拋棄式 worktree**·`git worktree add --detach`）
⇒ **`R4 = 33.1`** ✅（期 `33.10`）⇒ **量測器非紅** ⇒ 得續二分。
併：該態六街廓深度 ＝ `[R1 33.15, R2 44.47, R3 44.34, R4 33.1, R5 45.71, R6 45.51]`。

### 三-3　款 `2`：二分定位

| rev | `run_verification.py:33` 逐字 | `V6DXF` | `R4` | 其餘五塊 |
|---|---|---|---|---|
| `7fa957e`（對照） | `V6DXF = os.path.join(REPO, "data", "V6.dxf")` | `V6.dxf` | **`33.1`** | `[33.15, 44.47, 44.34, 45.71, 45.51]` |
| `a47e758`（＝ `4214b3a^`） | 同上 | `V6.dxf` | **`33.1`** | **逐值相同** |
| **`4214b3a`** | `V6DXF = os.path.join(REPO, "data", os.environ.get("WV_K9_DXF_NAME", "V6_1.dxf"))` | **`V6_1.dxf`** | **`33.11`** | **逐值相同** |

⇒ 🔒 **首次為 `33.11` 之 commit ＝ `4214b3a`**。

### 三-4　🔑 分離實驗（現態 × 二圖資·⛔ `checkout`）

**同一份現態之碼**（`rev 86bc3dd`），只換 `WV_K9_DXF_NAME`：

| `WV_K9_DXF_NAME` | `V6DXF` | `R4` | 其餘五塊 | `MinA_區` |
|---|---|---|---|---|
| `V6.dxf` | `V6.dxf` | **`33.1`** | `[33.15, 44.47, 44.34, 45.71, 45.51]` | **`115.85`** |
| `V6_1.dxf` | `V6_1.dxf` | **`33.11`** | **逐值相同** | **`115.88`** |

⇒ 🔑 **因 ＝ 圖資，⛔ 碼**（碼逐位相同而結果相異 ⋀ 其餘五塊逐值不動）。

### 三-5　款 `4`：出艙

| 項 | 值 |
|---|---|
| **commit `sha`** | `4214b3a6a36b1efc3b877863289c9569cc9a4f73`（父 `a47e758a10504e152652dc6c74d44fcf57d4d72f`） |
| **其訊息逐字**（節錄·⛔ 改寫） | 「六街廓深度**僅 `R4` 由 `33.10` 變 `33.11`**」 |
| **其所改之受詞** | ① `verify/run_verification.py:33` 之 `V6DXF` 預設（`V6.dxf` → `V6_1.dxf`·保留 `WV_K9_DXF_NAME` 覆寫出口）；② `verify/baselines/PROVENANCE.md` 之末端追加 |
| **`D_avg@R4`（`round` 後·本批實測）** | `33.10` → `33.11` |
| **`D_avg@R4`（未捨入）** | `33.104629` → `33.112192` |
| **`mina_qu`（本批實測）** | `115.85` → `115.88` |

🔴 **未捨入值⛔ 可自現行管線取得**——`app.py:5966` 逐字 `return {'D_avg': round(D_avg, 2), 'D_min': …}`
⇒ **於回傳當下即捨入**；`verify/run_verification.py:128` 取 `float(v["D_avg"])` 已是 `2dp`。
⇒ 🔒 該二值係引 **`GB-102` 之倉內錨**（當場 `grep` 實測：`33.104629` 命中 `2` 列／`33.112192` 命中 `2` 列；
對照組 `33.999999` ＝ `0` ⇒ 器非紅），**角色 ＝ 倉內既有實測值**（發單側以 `ezdxf` 自二檔逐點實算）、**⛔ 本批實測**。
🔒 **交叉驗證**：`round(33.104629, 2) = 33.1` ⋀ `round(33.112192, 2) = 33.11`
⇒ **與本批管線實測逐位相符** ⇒ 二源獨立而一致。

### 三-6　🔑 副產物：**`GB-7` 之前提於 `V6` 態確實成立**（⛔ 誤）

| 圖資 | `R4` 之 `D` | 正典形 `round(D,2)×w` | 碼面形 `round(D×w,2)` | `Δ` | `MinA_區` |
|---|---|---|---|---|---|
| `V6.dxf` | `33.1` | **`115.850000`** | **`115.850000`** | **`0`** 🟢 | `115.85` |
| `V6_1.dxf` | `33.11` | `115.885000` | `115.880000` | `−0.005` 🔴 | `115.88` |

⇒ 🔒 **`GB-7` 所載之「`R4` 兩形同值」於 `V6` 態<u>確實成立</u>、⛔ 誤**；
其於 `V6_1` 態方破裂 ⇒ **前提之破裂與圖資切換（`4214b3a`）同源**。
🛑 **本節⛔ 就此提任何處置**——`GB-7` 之末端追加已於 `§二` 落地，其受詞一字未動。

---

## `§四`　序 `4`：**`p9` —— `TIER_EXPECT` 之翻紅點** ⇒ 🛑 **停機上呈**

🛑 **本節⛔ 改 `TIER_EXPECT` 一字、⛔ 判其為缺陷、⛔ 提遷移主張**（補令四 `§四-2` 款 `4`）。

### 四-1　款 `2`：**判別力對照（必辦·此對照即本題之答之半）** ⇒ 🟢 **通過**

**`TIER_EXPECT` 最後一次更新之 commit** ＝ **`7ce98e6`**
（框 ＝ `git log -L '/^TIER_EXPECT/,+1:verify/wd4_tier_list.py'`·得二 commit：`7ce98e6`〔更新〕／`9061c84`〔引入〕；
**對照組**：`git log -S'TRACK_EXPECT' -- verify/wd4_tier_list.py` ⇒ `1` ✅／一必不存在之符號 ⇒ `0` ✅ ⇒ 器非紅）。
其訊息 ＝ `W-G Y 波收官：財務快照第 3 版比率更新＋全鏈重烤＋Y 波機器判準 45/45 綠`
——**與 `TIER_EXPECT` 註解所載之 `2026-07-14` 相符**。

**於其上實跑**（拋棄式 worktree）：

| 情境 | `mina_qu` | `tiers` | 判 |
|---|---|---|---|
| `0m` | **`114.07`** | `{'0': 13, '1': 7, '—': 8, '2': 3, '3': 2}` | ✅ **＝ `TIER_EXPECT`** |
| `3.5m` | `114.07` | 同上 | ✅ |

⇒ 🟢 **該錨<u>曾為真</u>**（且 `mina_qu` ＝ `MINA_QU_EXPECT` ⇒ 該態全綠）
⇒ 依款 `2` **第一分支**：「其後有一次**漂移**，二分定位之」。
🔒 **⇒ `TIER_EXPECT` ⛔ 「自登記之日即為紅」** ——第二分支不成立，**⛔ 停機於此**。

### 四-2　三項排除（皆**當場實跑**·⛔ 推斷）

| # | 候選源 | 實測 | 判 |
|---|---|---|---|
| ① | **圖資**（`4214b3a` ＝ `p8` 之答） | 現態 × 二圖資 ⇒ `mina_qu` `115.85` ↔ `115.88` 而 **`tiers` 逐格相同** `{'0':11,'1':9,'—':8,'2':3,'3':2}`（二情境皆然） | 🔒 **⛔ 是因** |
| ② | **深度鏈之變**（`32.59 → 33.10`） | `7fa957e` 之 `mina_qu` 已為 **`115.85`**（⛔ `114.07`）而其 `tiers` 仍 ＝ `TIER_EXPECT` | 🔒 **⛔ 是因** |
| ③ | **宗之組成**（步驟 0·`a2c4b2b`） | 其 commit message 逐字自載「片數 **`R2 15→14`**／`R3 14→10`／`R5 12→8`／`R6 11→8`·合計 `−12` ＝ 吸收數」；本批實測其 `R2` 宗數確為 `14`（父態 `15`） | 🔶 **不可求值**（見 `四-4`） |

🔑 **①② 皆坐實補令四 `§三` 之方向併記**——「`0`／`1` 之界由 **`all_qual`（逐宗達標）** 所定，**⛔ 由 `mina_qu`**」。
🔒 **碼面之因果鏈（當場現查·⛔ 猜）**：
`all_qual = bool(lots) and all(_qual(r) for r in lots)`；`_qual` 比 `r["G(㎡)"]` 與 `mina[r["所屬街廓"]]`；
`sumG = round(sum(float(r.get("G(㎡)", 0) or 0) for r in lots), 2)`
⇒ 🔒 **`mina` 之變⛔ 改 `tiers`（已二度坐實）⇒ 其因必在 `G(㎡)` 或 `lots` 之組成。**

### 四-3　款 `1`：全序列二分 ⇒ 🛑 **於步 `1` 即撞「洞」**

**器** ＝ `verify/probes/probe_WG9270R5_p9_bisect.py`｜落檔 `verify/out/WG9270R5_p9_bisect.log`｜
區間 ＝ `7fa957e..HEAD` ＝ **`692`** commits。

**二端之前提（⛔ 假定·當場復現）**

| 端 | `mina_qu` | `0m tiers` | 期 | 判 |
|---|---|---|---|---|
| `7fa957e` | `115.85` | `{'0':13,'1':7,'—':8,'2':3,'3':2}` | 相符 | ✅ |
| `8356f09` | `115.88` | `{'0':11,'1':9,'—':8,'2':3,'3':2}` | 紅 | ✅ |

**步 `1`（`idx = 345`·`8a9f6ae` ＝ `W-G.9-115`·`2026-08-24`）**：`compute()` **求值失敗**（`rc = 1`），
其逐字 ＝ `RuntimeError: 街廓 R2 抵費地計算失敗：🔴 ②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 3.1129（(宗數15−1)×0.005×深度44.47）`
⇒ 器依其設計 **loud 拒測**（`rc = 5`）、**⛔ 視為紅**、**⛔ 續二分**
（承「**無從判定**⛔ 與**判定為偽**共用同一出艙碼」）。

### 四-4　款 `1` 之**正解**：先縮小候選集 ⇒ 🛑 **三候選<u>皆</u>求值失敗**

🔒 **自捕**：我原直接對 `692` 個 commit **全序列**二分，而單逐字為「**同 `四-1` 之法**」，
`四-1` 款 `1` 明令「**縮小候選集（⛔ 全序列實跑）**：以 `git log -S`／`git log -L` 取候選」
⇒ **我未施行該款** ⇒ 改依單已令之法續辦（**⛔ 新訂準據、⛔ 停機於此**）。

**候選集之縮小**（框 ＝ `git log -S` 於 `tiers` 之因果祖先·**正面列舉**）：
`k6_step0_merge` @ `app.py` ⇒ `6e67722`／`a1107a1`／`a2c4b2b`；同 @ `verify/stepg_pipeline.py` ⇒ **`0`**；
`k6_merge_groups` @ `app.py` ⇒ `a2c4b2b`／`f4c5385`。
**對照組**：`run_corner_pk` @ `verify/wd4_tier_list.py` ⇒ `1` ✅／一必不存在之符號 ⇒ `0` ✅ ⇒ 器非紅。

**於候選上實跑**（器 ＝ 一次性之逐 rev 測試器·落檔 `verify/out/WG9270R5_p9_revs.log`）：

| rev | `R2` 宗數 | 上界 | 宗-宗重疊 | 判 |
|---|---|---|---|---|
| `2b49340`（＝ `a2c4b2b^`） | **`15`** | `3.1129` | `45.9766` | 🛑 **求值失敗** |
| `a2c4b2b`（步驟 0 落地） | **`14`** | `2.8906` | `45.9766` | 🛑 **求值失敗** |
| `a1107a1`（步驟 0 回歸無條件） | **`14`** | `2.8906` | `45.9766` | 🛑 **求值失敗** |
| `8356f09`（現態·已測） | **`12`** | `2.4459` | **`0.0000`** | ✅ 可求值 |

🔑 **宗數 `15 → 14` 坐實 `a2c4b2b` 之 commit message 逐字**（「片數 `R2 15→14`」）
⇒ **步驟 0 之效確已生**，惟該態**仍不可求值** ⇒ ⛔ 判其是否為翻紅點。
🔑 **現態之 `12`** 係 `a1107a1` **之後**另有變 ⇒ 其亦在洞之出口之後。

### 四-5　🔒 「洞」之界定與其成因（⛔ 推斷·當場實測）

| 受詞 | 實測 |
|---|---|
| **洞之閘** | `verify/stepg_pipeline.py` 之 `run_step_g` 內 `raise RuntimeError(f"街廓 {blk_label} 抵費地計算失敗：{_eOff}")` |
| **洞之成因** | `②-宗 圍堵閘破[R2]`·**宗-宗重疊 `45.9766`** ——**即 `GB-67` 之受詞**（`CLAUDE.md` 逐字載其破量 `R2 45.9766 ㎡`·**⛔ 尚未修**） |
| **⛔ 錯誤處理之變** | 二態之呼叫處**逐字相同**：`sg = run_step_g(ns, fake_st, list(cb_by.values()), cad, snapshot, params, build, _pk[3], _pk[4], setback)`（**皆無 `try`**）；`run_step_g` 之 `-S` @ `verify/wd4_tier_list.py` ⇒ 僅 `808c2e9`（引入）⇒ **依賴鏈⛔ 變** |
| **⇒ 真因** | **上界隨宗數變**（`(宗數−1) × 0.005 × 深度`），而**重疊量**在洞內恆為 `45.9766`、於現態為 `0.0000` ⇒ **幾何之變**使洞被填補 |

### 四-6　🛑 **停機上呈**（⛔ CC 自裁）

🔒 **所呈之題**：**`p9` 之翻紅點於單所令之法<u>結構上不可得</u>**——
款 `1`（縮小候選集）與款 `2`（二分）之**全部受測態皆落於「洞」內**（`compute()` 拋 `GB-67` 之閘）。
續辦須**新訂一條準據**（例：遇求值失敗之態時如何處置——跳過？改受詞？改以他量為代理？），
而依 `自解款` 之**黑名單**「**須新訂一條準據**」⇒ **恆停機** ⇒ 🛑 **⛔ CC 自解，呈發單側。**

🔒 **款 `3`（併判同源否）之<u>已可判部分</u>**：
`p8` 之答 ＝ `4214b3a6a36b1efc3b877863289c9569cc9a4f73`；`p9` 之答 ＝ **未得**。
🔒 **惟 `四-2` ① 已<u>機械證明</u>**：`4214b3a` 之受詞（圖資）**⛔ 改 `tiers`**
（現態 × 二圖資之 `tiers` 逐格相同）⇒ 🔒 **二者⛔ 同源**——此判**⛔ 待 `p9` 之答**，
且係以**受詞之效**（⛔ 以 sha 相異）證之；**其最終之 sha 逐位對待 `p9` 答畢後補**。

🔒 **本節已排除者（供發單側續裁·⛔ 結論）**：圖資（`4214b3a`）／深度鏈（`32.59→33.10`）**皆⛔ 是因**；
剩餘候選 ＝ **`G(㎡)` 之變**或 **`lots` 之組成之變**（步驟 0 係後者之一，惟其態不可求值）。

---

## `§五`　序 `5`：**`p10` —— 登記紅之遮蔽** ⇒ 鑄 **`GB-166`**（commit `8356f09`）

### 五-1　款 `1`：**先查既有號**（全倉·八字樣·逐筆歸類·二造對照）

**三軸**：`(1)` `blob@86bc3dd216947e9c2d6936465ff89a230942fd01`／`(2)` 追蹤 `.md` **`731`** 檔／`(3)` **檔框 ＋ 列框**。

| 字樣 | 檔／列 | 逐筆歸類 |
|---|---|---|
| `遮蔽` | `19`／`26` | 同列含 `GB-` 者 ＝ `1`（`GB-61`） |
| `allok` | `4`／`5` | `2` 列 ＝ **本單之 payload**；`3` 列 ＝ 引用其碼面形（`W-G.9-265R:478`／`-266R:131`／`W-G.9-7:48`） |
| `恆紅` | `60`／`71` | 同列含 `GB-` 者 ＝ `3`（`GB-86`／`GB-93`／`GB-97`） |
| `單一聚合`／`聚合出艙`／`RESULT: W-D.4`／`逐錨分列` | 各 `1`／`1`・`1`／`1`・`1`／`1`・`1`／`2` | **全數落於本單自身** |
| `失明` | `11`／`13` | 同列含 `GB-` 者 ＝ `0` |

**四候選之逐一判**（⛔ 以「相關」代 operative 述詞）：`GB-61` 之受詞係**期望 FAIL 名單之對帳欄位粒度**
（洞 A 寄生失聲／洞 B 同色遮蔽）、`GB-86` traceback 尾切片、`GB-93` Python 版本相依、`GB-97` baseline 時序
⇒ **皆⛔ 同受詞**（本則之受詞係 `main()` 之**聚合布林** `allok`）。

**二造對照**：必命中 `GB-165` ⇒ B 形 `7`／`51`・C 形 `7`／`49` ✅；
必為零 人造哨兵（**執行期組出**·字面⛔ 出艙·代稱 `⟨NEG⟩`）⇒ `0`／`0` ✅ ⇒ **器非紅**。
⇒ 🟢 **查無既有同受詞號** ⇒ 依款 `2` **鑄之**。

### 五-2　`GB-166` 之取號現查與閘

`GB-166` B 形 `2` 檔／`6` 列・C 形 `2`／`6`；**逐筆歸類**（`裁 H`）＝ `5` 列為補令四之授權條款、
`1` 列為本報告骨架之引用 ⇒ **⛔ 一列為定義列式** ⇒ **⛔ 撞號**。
對照 `GB-164`（已鑄）⇒ `10`／`45`・`10`／`44` ✅。

**閘 `①②`**（`常規四（九）二`·本次**⛔ 補鑄缺號** ⇒ 閘 `②` **適用**）：
`GB` 相異 `163 → 164`（`+1`）／**MAX `165 → 166`** ✅／缺號集 `[12, 87]` **⛔ 動**（`166` ⛔ 在其中）✅；
其餘三簿⛔ 動 ✅。

### 五-3　🩸 照實併呈（⛔ 追改補令四一字）

其 `§四-3` 逐字載聚合式於 `verify/wd4_tier_list.py:449`，CC 當場實測其在 **`:450`**（差 `1`）。
🔒 本批**⛔ 動該檔一字**（`git status` 對其之非 `??` 列 ＝ `0`）⇒ 成因係**行號之易腐**、**⛔ 本批所致**
⇒ 本則一律以**字樣錨**指之。
🔑 **`ok_t` 之紅係<u>純由 `tiers`</u>（⛔ `tracks`）**——`tracks` 逐格 ＝ `TRACK_EXPECT`。

🛑 **本則本批⛔ 落地**（補令四 `§四-3` 款 `4`）。

---

## `§六`　收工閘

### 六-1　自限復驗

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| 生產碼 `34` 檔 | 期初 ＝ 期末 | `app.py` blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`；聚合 `c6320ce6def9a3c6b5a2c4281e30b84bf754c8ad44a3e603f42aa4c208c87458` | ✅ |
| `verify/baselines` | ⛔ 覆寫 | `298` 檔·聚合 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` 側支 | ⛔ 併線·⛔ 刪除 | `merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | ⛔ 設任何值 | 未設 | ✅ |
| `GB-162` 三禁令 | 維持 | ⛔ `WV_BAKE`／⛔ 覆寫 `baselines`／⛔ 併線 `p3a`／**本批之數皆自當次實跑** | ✅ |
| **拋棄式 worktree** | 跑畢整份刪除（`常規六 三`） | `p8a` 已 `git worktree remove --force`；`git worktree list` 已無之 | ✅ |

### 六-2　本批之**自捕**（皆當場攔下·⛔ 出艙錯值）

| # | 形 | 症狀 | 處置 |
|---|---|---|---|
| `1` | `git rev-parse :<glob>` 之 `:` 前綴使 bash ⛔ 展開 glob | `sha256sum` 印出 `e3b0c442…` ＝ **空串之 `sha256`**（形上與正常值⛔ 可辨） | 路徑以變數承接；當場印出空串值以坐實 |
| `2` | `cat -A \| grep '\^M'` 於 UTF-8 CJK 偽陽 | 得 `47` 而真值 `0` | ⇒ **已鑄坑 `as`** |
| `3` | `grep -c $'\r'` 對真 CRLF 檔偽陰 | 造甲得 `0`（期 `2`） | ⇒ **已鑄坑 `at`** |
| `4` | `io.open(p,'w')` 於 Windows 靜默轉 CRLF | 報告初落 `CR ＝ 305` | ⇒ **已鑄坑 `au`** |
| `5` | **以 `tail -N` 取長跑之結果** | `compute()` 之逐宗診斷**淹沒**結果列；例外之 traceback 亦同遭截 ⇒ **受詞靜默失去，而輸出讀起來完全正常** | 唯一標記 ＋ `grep -F`；落空即 **loud** |
| `6` | **標記含 `%` 而以 `%` 格式化** ⋀ **`2>&1` 合流後 `grep` 命中 traceback 之<u>源碼列</u>** | `rc=1` 而「命中」之竟是**我自己的源碼** ⇒ 偽陽 | 標記**執行期組出**（`"RES"+"MARK"+str(7*13)`）＋ **分離二流**（只搜 stdout） |
| `7` | **宣告之字樣錨命中數未實測** | 片段宣告 `GB-7 ＝ 9`／`但書式 ＝ 3`，實測 **`57`**／**`6`**（窄錨與對照組皆如宣告） | **附加前**以全檔計數實測（⛔ 以 `head` 之顯示列數代） |
| `8` | **heredoc 誤帶 `2` 次** | `wg9237_heredoc_guard` 硬閘 `rc ≠ 0` | 改 `Edit`／`Write`（⛔ 賴記憶·全賴該硬閘） |
| `9` | **未施行單所令之款 `1`**（縮小候選集） | 直接對 `692` commit 全序列二分 ⇒ 步 `1` 即撞洞 | 改依單已令之法；**⛔ 新訂準據** |
| `10` | `python -c "…"` 之**雙引號內含反引號** | bash 解為命令替換 ⇒ `rc=2`（loud） | 改以 `Write` 落腳本，反引號以 `chr(96)` 於執行期組出 |

🛑 **`5`／`6` 尚未鑄坑**（本批授權僅 `as`／`at`／`au` 三號）⇒ **具名候發單側核可**。

### 六-3　本批之**自解清單**（`自解款` 款 `五`：未寫入者**視同未自解**）

| # | 疑義逐字 | 二以上之讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 報告與落檔之命名（`R4` 抑或他） | 甲 `W-G.9-270R5b` ＋ 落檔前綴 `WG9270R5_`／乙 逕用 `R5`（撞已入倉之 `W-G.9-270R5_…`） | **甲** | `W-G.9-270R5b` 之全倉命中 `0`；`§一-1` 已准「落檔前綴一律 `WG9270R5_`」 | 乙必撞已入倉之報告名 |
| `2` | `§一-3` 令改「二處」框字串，而陳舊處實為**三**處 | 甲 只改單所令之二處並具名第三處／乙 一併改三處 | **甲** | `aa`〜`au` 命中 `2`（期 `2`）·`aa`〜`ak` 殘 `1` ＝ 第三處註解 | 乙**逾核可射程**（體例同 `W-G.9-194 裁三`） |
| `3` | 二分撞洞後之續辦 | 甲 逕停機／乙 **先施行單所令之款 `1`**（縮小候選集）再判 | **乙** | 單逐字「同 `四-1` 之法」，而 `四-1` 款 `1` 明令「縮小候選集（⛔ 全序列實跑）」⇒ **我未施行該款** | 甲將使一條**單已明令之法**未被施行即停機 |

🛑 **三者皆**：零土地後果 ⋀ 零生產碼 ⋀ 可機驗 ⋀ 可逆 ⋀ 出艙具名。
🛑 **⛔ 落入黑名單任一項**；而 `p9` 之續辦**確落入黑名單**（須新訂準據）⇒ **已停機上呈**（`§四-6`）。

### 六-4　🛑 **上呈項**（⛔ CC 自裁）

1. 🛑 **`p9` 之翻紅點於單所令之法<u>結構上不可得</u>**（`§四-6`）——續辦須**新訂準據**。
2. 🟡 **候選新坑二則**（`§六-2` 之 `5`／`6`）——⛔ 鑄（無授權·下一標籤 `av`）。
3. 🟡 **`§一-3` 之第三處陳舊框字串**（同檔註解）——⛔ 改（逾射程）。
4. 🩸 **補令四 `§四-3` 之 `:449` 與實測 `:450` 差 `1`**（行號易腐·⛔ 本批所致）。
5. 🟡 **命名之提案**（`W-G.9-270R5b`）候核可。
6. **承前批而仍待者**：`MinA_區` 四值之擇一（發單側 `§二-3` 已裁「暫⛔ 呈 KL·俟 `p8` 之歸因」——
   🔒 **`p8` 已辦畢**⇒ 其呈 KL 之時點由發單側定）／`p5`・`p6` 之事實／前批自解 `3`（發單側 `§一-3` 已准）。

### 六-5　坑之**定義處清單自證** ＋ 四簿期末

🔒 **其實測見本批之<u>末一 commit</u> 之訊息**（承 `W-G.9-270R5` 之體例）——
本報告自身入倉後方可量（器讀倉側 blob），且本報告含 `D-T` 形之坑表（`§一-1`）
⇒ 其為**受詞之一員**（`GB-132` 族·`通則 I`）。
🔒 **序 `1` 之中間態實測**（態 `01c61a9`·報告骨架已入倉）：**`73`／`73`** 可解析・三字樣齊備 `73`／`73`・
母體 追蹤 `.md` **`731`** 檔・`as`（`:1` `D-H`＋3）／`at`（`:53` `D-T`）／`au`（`:54` `D-T`）皆 ✅・
判別力二造 ✅（造甲定義處 `6 ≥ 1`／造乙 `0`／`0`）。

---

## `§七`　🛑 **停**

候發單側之令，⛔ 自行續辦。
