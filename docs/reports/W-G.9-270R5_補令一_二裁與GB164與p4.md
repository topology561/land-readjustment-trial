# `W-G.9-270R5`　補令一之執行（KL 二裁入倉 ／ 鑄 `GB-164`・`自誤 368` ／ `p4` 全戶落位）

> **態**｜開工 ＝ `dccef82d2c7800a3da915734855acdb9c346036a`（`ls-remote` 實查）｜
> 工項零後 ＝ `946a60d036844565cc11f351f2ed3ee80752b8c2`｜本報告之基座 ＝ `260b0fdbf31de3c12ee3086aa39702122381b765`
> **級**（`常規五`／作業常規追加五 `§二`）＝ **中**（純登記 ＋ 純診斷·**零生產碼**）
> 🛑 **本批⛔ 辦**：`GB-164` 之落地／`K-9-12` 之任何修法主張／三阻塞項之結清裁／
> `MinA_區` 四值之擇一／任何生產碼一字。

---

## `§零`　起手清單（開場文 `§五`·七項）

| # | 項 | 實測 | 判 |
|---|---|---|---|
| `1` | `ls-remote` 三 ref | 主線 `dccef82…`／`p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4`／`c2` `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d` | ✅ 與開場文 `§二` 逐位相符 |
| `2` | `fetch --prune` ＋ `reset --hard` | `HEAD` ＝ `dccef82…`；`HEAD~1` ＝ `51d6a931bced5d3c5665f7de92ebfe6e83065360`；**`left-right` ＝ `0　0`**（⇒ 工作區確為 **wip 態**·⛔ main 態）；`git status --porcelain` ＝ `0` 列 | ✅ |
| `3` | 生產碼 `34` 檔 | `app.py` blob ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；`verify/` **單層** `*.py` ＝ `33` 檔 ⇒ 合計 `34` | ✅ |
| `4` | 四簿 | 見 `§八-2` 之期初欄 | ✅ 與開場文 `§三` 逐項逐值相符 |
| `5` | 坑索引 | `70`／`70` 可解析·`形／徵候／攔法` 三字樣齊備 `70`／`70`·母體 ＝ 追蹤 `.md` **`727`** 檔·判別力二造 ✅ | ✅ |
| `6` | 讀 `R3`／`R2` | 已讀（`R3` `§五`–`§七`／`R2` `§零-3`・`§三′-6`・`§七-4`） | ✅ |
| `7` | 讀補令三 `§四`／`§五` | 已讀 | ✅ |

🔒 **加辦一項（具名為加辦·⛔ 在七項內）**：`verify/baselines` ＝ **`298`** 檔；
**用開場文 `§二` 所載之逐字框**（`git ls-tree -r HEAD verify/baselines` 之**輸出文字**之 `sha256`）
⇒ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`
⇒ **與該節所載逐位相符** ✅（**先對齊框再比**·⛔ 以自擬框逕比）。

---

## `§一`　條款一（`git add` 之前·發單側 `2026-09-13` 令）

**受詞** ＝ 主 checkout `docs/orders/` 之 untracked 檔 `W-G.9-270_補令一_KL二裁與GB164.md`（`9440` B）。

| 量 | 值 | 法 |
|---|---|---|
| **CR（`0x0D`）** | **`0`** | `od -An -tx1 -v` ⋀ `python rb.count(b'\r')`（**二法一致**） |
| LF（`0x0A`） | `124` | 同上 |
| CRLF | `0` | `python rb.count(b'\r\n')` |
| **`core.autocrlf` 有效值** | **`false`** | `git config --get` |

**`core.autocrlf` 之三層分列**（⛔ 只報有效值）

| 層 | 檔 | 值 | `rc` |
|---|---|---|---|
| system | `C:/Program Files/Git/etc/gitconfig` | `true` | `0` |
| global | （`~/.gitconfig`） | 🔴 **未設** | **`1`** |
| local | `C:/Users/admin/Desktop/land-readjustment-trial/.git/config` | **`false`** | `0` |

🔒 **`.gitattributes` 之 `-text` ⛔ 及於 `*.md`**（發單側所令之前提·CC 復現）：
`git check-attr -a` 對本檔**無任何屬性輸出**；對照組 `data/V6_1.dxf` ⇒ `text: unset` ⇒ **器非紅**
⇒ `.md` 確受行尾正規化管轄；惟 `core.autocrlf` 有效值為 `false` ⋀ CR 本即 `0`
⇒ **雙重保證：入倉⛔ 發生行尾轉換**。

🩸 **本節之量測器紅二則（皆當場坐實·⛔ 出艙錯值）**

| 法 | 本檔 | 造甲（真 CRLF·期 `2`） | 判 |
|---|---|---|---|
| `od` `0x0D` | `0` | `2` | ✅ 可用 |
| `python` `rb.count(b'\r')` | `0` | `2` | ✅ 可用 |
| `cat -A \| grep -c '\^M'` | **`47`** | — | 🔴 **偽陽**——命中實為 **`M-^M`**（位元組 `0x8D` 之轉義形·UTF-8 CJK 之續位元組）；樣本逐字 `-8M-^MM-eM`／`^GM-^MM-eM` |
| `grep -c $'\r'` | `0` | **`0`** | 🔴 **偽陰**（對真 CRLF 檔亦得 `0`）⇒ ⛔ 可用 |

🔒 **⇒ 真值 `CR ＝ 0`**（二可用之法一致，且二者對造甲皆得 `2` ⇒ 判別力成立）。
🛑 **二者係<u>候選新坑</u>**——本批**⛔ 授權鑄坑號**（補令一 `§五` 僅授二號·坑之下一標籤 `as`）
⇒ **具名候發單側核可**，⛔ 逕鑄。

🩸 **候選新坑第三則（本節之法於本批之另一面·`§五` 落地時捕獲）**：
**`io.open(p, 'w', encoding='utf-8')` 於 Windows 之預設 `newline=None` 會把 `\n` 轉為 `\r\n`**
——本報告初落時因此得 `CR ＝ 305`（＝ 其列數），而**倉內既有體例**為
`docs/reports/*.md` 之 blob 側 `CR ＝ 0`（實測：`W-G.9-270R3` ＝ `0`／`W-G.9-270R2` ＝ `0`）。
🔒 **其徵候完全正常**——檔案內容逐字無誤、`markdown` 渲染無異、`git status` 只顯示 `A`。
**攔法** ＝ **入倉前逐檔量 `CR`**（以 `§一` 所定之位元組層法），並**與該類檔之既有體例對拍**。
🔒 **已於本批 commit 前修正為 `LF`**（⛔ 追改任何已入倉之物）。
⚠️ **併記（照實·⛔ 判其為缺陷）**：`verify/out/*.log` 之含 `CR` 係**既有體例**
（前窗 blob 側實測 `WG9270R4_closegate.log` ＝ `22`／`WG9270R3_p5_pool_conn.log` ＝ `171`／
`WG9270R2_pool_dist.log` ＝ `89`）⇒ 本批之 log 含 `CR` **合體例**，**⛔ 新破損**；
其成因同為 Windows 下 `python` stdout 之 text mode。**本批⛔ 改其行尾**。

---

## `§二`　工項零：補令一之**原封入倉**

| 量 | 值 |
|---|---|
| 落檔前來源 `sha256`（磁碟） | `af2ba742d2f93a77576a25b5c4e7728851c13e0271007517cc8a895ec5eb1d4f` |
| 落檔後 `sha256`（本 worktree） | **同上** |
| 逐位 `diff`（來源 vs 落檔） | **DIFF-EMPTY** |
| bytes | `9440` ＝ `9440` |
| CR | `0` ＝ `0` |

🔒 **搬移之法**（承前窗體例·⛔ 令 CJK 穿過引號層）＝ `cp` ＋ **全 ASCII glob** `W-G.9-270_*GB164.md`；
**glob 唯一性自檢**：命中 `1`（判別力：寬 glob `W-G.9-270_*.md` ＝ `5`；必不命中之 glob ＝ `0` ⇒ 器非紅）。
🔒 **`add` 逐檔**（⛔ `-A`／`.`）⇒ staged ＝ `A  docs/orders/W-G.9-270_補令一_KL二裁與GB164.md`·`numstat` `124／0`。

---

## `§三`　條款二（入倉之後）

🔒 **逐字形**（`git rev-parse HEAD:<path>` ⇒ `git cat-file blob … | sha256sum`），態 ＝ `946a60d…`：

| 量 | 值 |
|---|---|
| `blob sha1`（`HEAD:`） | `8e55b1760aa73868a07fb12882f07800eb587307` |
| `blob sha1`（`add` 時之 index·同一物件） | **同上** |
| **倉內 blob 內容之 `file sha256`** | `af2ba742d2f93a77576a25b5c4e7728851c13e0271007517cc8a895ec5eb1d4f` |
| **磁碟 `sha256`（同格並列）** | **同上** |
| blob bytes ／ 磁碟 bytes | `9440` ／ `9440` |

⇒ 🟢 **往返逐位保存**。**自此以<u>倉內 blob</u> 為補令一之唯一指涉錨。**

🔒 **對照組（發單側已實測·證此量測非恆真）**：補令三之 blob `sha1` ＝ `6849f7b4bb62a4180a8cd71e248669ce27035000`；
其 blob 內容 `sha256` ＝ `a31574d9d476415f4d8e34ef16fa54449539ac83791fdbda3b3ea408146a9770`
⇒ **與發單側所載逐位相符**，且 ＝ 其磁碟值、`CR ＝ 0` ⇒ **器非紅**。

🩸 **本節之自捕（量測器紅·已攔下·⛔ 出艙錯值）**：首次取值寫作 `git rev-parse ":$F"` 而 `$F` 為 **glob**
——`:` 前綴使 bash **⛔ 展開** glob（檔案系統上無以 `:` 起首之路徑）⇒ git 收到字面 `*` 而 `fatal`；
`git cat-file` 輸出為空，`sha256sum` 遂印出 **`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`**
——🔴 **＝ 空串之 `sha256`**，一個**看起來完全正常**之值。
**攔法**：① `fatal` 已 loud；② 路徑改以變數承接（`F=$(ls …)`）；③ **當場印出空串之 `sha256` 以坐實其來源**。

---

## `§四`　條款三（指涉之釘死）

已載於 commit `946a60d…` 之訊息（`git log -1 --format=%B` 可復現）與本報告 `§三`：
`(a)` blob `sha1` `8e55b1760aa73868a07fb12882f07800eb587307`／
`(b)` `file sha256` 全長 `af2ba742d2f93a77576a25b5c4e7728851c13e0271007517cc8a895ec5eb1d4f`（取自倉內 blob 內容）／
`(c)` 來源**逐字** ＝ 「主 checkout `docs/orders/` 之 untracked 檔·`9440` B·⛔ 由 KL 於本回合貼入」。

🔒 **發單側之更正之採納**：開場文 `§四` 之「補令一尚未送達任何窗」與 CC 之現查相斥；
發單側已自承其為**前窗交接文之語**，並自倉驗「`docs/orders/` 無其追蹤本、`GB-164` 與 `自誤 368` 皆仍在缺號集」
⇒ **工項零確未辦**。CC 本批之期初實測**逐項復現該三事** ⇒ 🟢 **一致**。
🔒 **全長 `sha256` 之角色（照實）**：發單側稱倉內 `8` 處記載**盡為 `16` 碼截斷形**、⛔ 存全長本
⇒ `§二`／`§三` 之全長值於**本報告落地前**係「**CC 實測值**」；
**條款二辦畢後，倉內 blob 所算者即成為<u>可復驗之全長錨</u>**，自此取代之。

---

## `§五`　序 `6`：鑄 `GB-164`・`自誤 368` ＋ `§一` 二裁入倉

| 落點 | 受詞 | 增量 |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md`（檔尾） | **`GB-164`** ＋ `p4` 之全量測 | `+119`／`−0` 列·`802161 → 812179` B |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md`（檔尾） | **`自誤 368`** | `+48`／`−0` 列·`749705 → 753894` B |
| `docs/W-D.4_合併分配規格.md`（檔尾） | **KL 二裁**（`§一-1`／`§一-2`） | `+100`／`−0` 列·`9321 → 16620` B |

🔒 **`append-only` 之機械驗**（⛔ `--cached`——其於入倉後清空 ⇒ 恆綠·`常規一 §二 補款③`）：
以**基座 blob**（`946a60d…`）對工作區逐位 ⇒ **三檔皆 `new.startswith(old)` ⋀ `len(new) > len(old)`** ✅；
**判別力**［必 `FAIL`］：`old.startswith(new)` ＝ `False`（期 `False`）✅；
`git diff --numstat 946a60d… -- docs/` 之**刪除欄逐列為 `0`** ✅。

🔒 **鑄號之取號現查**（器 ＝ `verify/probes/probe_WG9270R5_numscan.py`·落檔 `verify/out/WG9270R5_numscan.log`）
——母體 ＝ `946a60d…` 之追蹤 `.md` **`728`** 檔·**二形並取**（`GB-158`）·錨定框：

| 號 | B 形 檔／列 | C 形 檔／列 | 定義框（實體） | 判 |
|---|---|---|---|---|
| **`GB-164`** | `6`／`26` | `6`／`26` | **`0`**（`164` 在缺號集） | ⛔ 撞號 |
| **`自誤 368`** | `6`／`13` | `6`／`12` | **`0`**（`368` 在缺號集） | ⛔ 撞號 |
| 對照甲 `GB-165`（已鑄） | `6`／`48` | `6`／`46` | — | `≥1` ✅ |
| 對照甲 `自誤 371`（已鑄） | `3`／`4` | `3`／`4` | — | `≥1` ✅ |
| 對照乙 人造哨兵（**執行期組出**·字面⛔ 出艙·代稱 `⟨NEG⟩`） | `0`／`0` | `0`／`0` | — | `＝0` ✅ |

⇒ **器非紅**。**逐筆歸類**（`裁 H`）已載於二簿之鑄造節內，其歸類**全數**為
「其授權在補令一·待新窗鑄」之**明文安排**與引用 ⇒ ⛔ 一列為實體占用。

---

## `§六`　序 `7`：`p4` 四款

器 ＝ `verify/probes/probe_WG9270R5_p4_tier_band.py`｜真相源 ＝ `verify.wd4_tier_list.compute(fixture=False)`
（**記憶體側**·⛔ 寫任何檔 ⇒ 合 `GB-162` 三禁令）｜落檔 `verify/out/WG9270R5_p4_tier_band.log`｜
`rc = 0`｜`stderr` `0` B｜**判別力二造** ✅（造甲必落帶／造乙必不落帶）。

**全量測已入倉於 `GB-164` 之登記節**（`docs/reports/W-G.4_泛用阻塞項登記表.md` 檔尾）。摘：

| 款 | 結果 |
|---|---|
| `1` `ε` | 實測值 `0.04 ㎡`（`GB-101` 原文「差達 `0.04 ㎡`」·逐值 ＝ `628(1)` `1521.09` vs `1521.05`）／**所取保守值 `0.05 ㎡`**（`GB-101` 之**過渡紀律逐字**「容差一律 `±0.05 ㎡`」·**倉內明文**·⛔ CC 自訂） |
| `2` 全戶落位 | 建地軌 **`25`** 戶 × 二情境 × **三 `MinA_區` 候選** ⇒ **落帶戶數皆 `0`**；最緊 ＝ `0m` `G004`（`273.2×`@活體）／**`3.5m` `G030`（`17.9×`@活體·`8.8×`@`114.07`）** |
| `3` 最壞 | 宗數最多 ＝ **`G009`（宗數 `5`）** ⇒ **帶寬上界 `±0.50 ㎡`（`ε=.05`）／`±0.40`（`ε=.04`）** |
| `4` 提案 | 判準形／出艙形（`梯次 = "不可判"`·**⛔ 沿用 `—`**——其已為公設軌所用）／上呈之路／判別力 ⇒ 見登記節 |

🔒 **二源之獨立復現**：`114.07` 欄（`+29.13`／`291.3×`／`−1.77`／`8.8×`）＝ **補令一 `§三-1`** 逐位相符；
`115.88` 欄（`−3.58`／`17.9×`）＝ **自誤簿檔尾之更正** 逐位相符。
🔒 **公設軌 `8` 戶之排除之由**已逐戶具名（`路徑標註` 逐字）；**自洽驗算** `Σ軌別 ＝ Σ梯次 ＝ 33` ✅。

---

## `§七`　🛑 上呈項（**⛔ CC 自裁**）

1. 🔴 **梯次分布之斷言現為紅**：實跑得 `{'0': 11, '1': 9, '—': 8, '2': 3, '3': 2}`（二情境同），
   而 `verify/wd4_tier_list.py` 之 `TIER_EXPECT` ＝ `{"—": 8, "0": 13, "1": 7, "2": 3, "3": 2}`
   ⇒ **`0` 與 `1` 各差 `2`**（梯 `2`／`3`／`—` 三欄相符）。**其歸屬（既有紅／待重烤／新破損）候發單側。**
2. 🔴 **但書式已實算，而規格之描述稱其「僅標式不算值」** ⇒ 二者不同步（⛔ 追改該句·已於二裁登記節具名）。
3. 🟡 **候選新坑三則**（`cat -A ^M` 偽陽／`grep $'\r'` 偽陰／`io.open(…,'w')` 於 Windows 靜默轉 CRLF）
   ——⛔ 鑄坑號（無授權·下一標籤 `as`）。
4. 🟡 **產物命名之提案**（`常規三`）：本窗取 `W-G.9-270R5`／`WG9270R5_`／`probe_WG9270R5_*`
   ——因 **`R4` 之產物前綴已由前窗佔用**（`probe_WG9270R4_*` `5` 件·`WG9270R4_*.log` `5` 件·皆已入倉）
   而 `docs/reports/` 之報告號止於 `R3`。**取保守（⛔ 撞）並逐字回報**，候核可。
5. **承前批而仍待發單側／KL 者**：`p7` 之停機上呈（`MinA_區` `115.85` vs `115.88`·**⛔ 屬 `GB-7`**）／
   `GB-7` 之「零後果」前提已不再成立／`MinA_區` 四值之擇一（½ 門檻 `57.93` vs `57.94` ⇒ **直達土地**）／
   `p5`・`p6` 之事實（⛔ 判其為缺陷）／**前批自解 `3`** 仍待覆核。
   🔒 **本批對 `4` 之新增事實（照實·⛔ 逾越）**：本案**⛔ 任一戶之 `2ΣG_戶` 落於 `[114.07, 115.88]`**
   ⇒ 四值之擇一於**本案**⛔ 改任一戶之梯次；⚠️ **⛔ 外推至他案**。

---

## `§八`　自限復驗 ＋ 收工閘

### 八-1　自限復驗

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| 生產碼 `34` 檔 | 期初 ＝ 期末 | 見下表 | ✅ |
| `verify/baselines` | ⛔ 覆寫 | 聚合 `sha256` 期初 ＝ 期末（框 ＝ 開場文 `§二` 逐字） | ✅ |
| `p3a` 側支 | ⛔ 併線·⛔ 刪除 | `ls-remote` ＝ `05a11bd…`·`merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | ⛔ 設任何值 | 本批**⛔ 跑 `run_verification`**（`compute()` 走記憶體側） | ✅ |
| `GB-162` 三禁令 | 維持 | ⛔ `WV_BAKE`／⛔ 覆寫 `verify/baselines`／⛔ 併線 `p3a`／**本批之數皆自當次實跑**（⛔ 自 baseline 取得） | ✅ |

### 八-2　四簿期末（逐簿具名其**各自**之框·⛔ 冠單一框名）

| 簿 | 框之出處 | 相異（期初 → 期末） | MAX | 缺號集（期初 → 期末） |
|---|---|---|---|---|
| **自誤** | `VR-091` 補款二 `①`（候選 `A`）＋範圍框 | **`360 → 361`** | `371`（⛔ 動） | `[106,355,356,357,368] → [106,355,356,357]` |
| **`GB`** | `VR-093` 三框聯集（扣哨兵 `997`） | **`162 → 163`** | `165`（⛔ 動） | `[12,87,164] → [12,87]` |
| **`VR`** | `VR-092` 就地加註所引（扣 `999`） | `80 → 80` | `95` | `[73,75]`（⛔ 動） |
| **`K-9`** | `W-G.9-266` 工項四 `四-1` | `28 → 28` | `29` | `[]`（⛔ 動） |

🔴 **閘 `②` 之結構不適用及其替代（已列入自解清單）**：`常規四（九）二` 閘 `②` 令「`MAX` 須由 `N-1` 轉為 `N`」，
而本批所鑄之二號皆為**補鑄缺號**（`164 < 165`／`368 < 371`）⇒ **`MAX` 結構上⛔ 推進**。
⇒ 改以**二判準**：`(a)` 相異數 `+1`；`(b)` 該號自缺號集**移除**。**二者皆當場實測且皆綠**
——其亦**同時證成閘 `①`**（形之自證）：若標題形⛔ 符定義框，則相異⛔ `+1`、缺號⛔ 移除。

### 八-3　坑之**定義處清單自證**

**器** ＝ `verify/probes/probe_WG9269_pit_index.py`（`PYTHONIOENCODING=utf-8`）｜
**母體取法** ＝ `git ls-tree -r <rev>` ＋ `git show <rev>:<path>` ⇒ **讀倉側 blob**（⛔ 工作區）。

| 量 | 期初（`rev = dccef82…`） | 期末（`rev = 260b0fd…`） | 判 |
|---|---|---|---|
| 母體（追蹤 `.md`） | **`727`** 檔 | **`728`** 檔 | ＝ `727 ＋ 1`（補令一）⇒ **自洽** |
| `✅ 可解析` 之資料列 | **`70`** | **`70`** | ⛔ 動 ✅ |
| `形／徵候／攔法` 三字樣齊備 | **`70`** | **`70`** | ⛔ 動 ✅ |
| 數字系 `1`〜`26` ／ 字母系 `a`〜`ar` | `26` ／ `44` | `26` ／ `44` | ⛔ 動 ✅ |
| 判別力二造 | 造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造之坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ | **同上** | **器非紅** |

🔒 **本批⛔ 新增任何坑**（補令一 `§五` 之授權僅二個號·坑之下一標籤仍為 `as`）
⇒ `70` 與前批**相同**。落檔 `verify/out/WG9270R5_pit_index.log`（期末）／`verify/out/WG9270R5_pit_index_pre.log`（期初·**當場重跑**）。

🔑 **`GB-132` 之當場驗（⛔ 推斷·實測為據）**：本批之 payload 內有**三處**表頭形為
`| # | 形 | 徵候 | 攔法 |`（`GB-164` 節·`自誤 368` 節·本報告 `§十-1`），其資料列首欄為 `` `1` ``、`` `2` ``…
——**恰與該器 `D-T` 之判準（表頭含 `徵候` ⋀ `攔法`）及數字系坑標籤之形相合** ⇒ 有污染之虞。
🔒 **二態之數字系 `1`–`7` 逐列對拍：定義處（含 `＋N` 之增量）與引用處<u>逐位相同</u>**
（`1` `…:135（D-T）＋24`／引用 `13`；`7` `…:230（D-T）＋5`／引用 `9`；餘同）
⇒ 🟢 **未發生污染**。⚠️ 惟**期末之 `260b0fd` 尚⛔ 含本報告自身** ⇒ 本節之數係
「**登記節已入倉、報告未入倉**」之態；報告入倉後若該數變動，即為本節所慮之污染，**須以末端追加更正**。

---

## `§九`　🔒 本批之自解清單（`自解款` 款 `五`：未寫入者**視同未自解**）

| # | 疑義逐字 | 二以上之讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | `常規四（九）二` 閘 `②`：「`MAX` 須由 `N-1` 轉為 `N`，且 `N` ⛔ 在缺號集」 | `(甲)` 嚴守字面 ⇒ 本批⛔ 得鑄（`MAX` 不動）／`(乙)` 於**補鑄缺號**之情形改以「相異 `+1` ⋀ 該號自缺號集移除」為替代判準 | **`(乙)`** | 二簿之期初／期末實測：自誤 `360→361`・`368` 移除；`GB` `162→163`・`164` 移除。且該二判準**同時證成閘 `①`** | `(甲)` 將使**補令一之明文授權**結構上⛔ 得執行——而該閘所守護之性質（形之自證·⛔ 漏鑄）已由替代判準承擔 |
| `2` | 產物之命名（`R4` 抑或 `R5`） | 甲 `R5`（⛔ 撞·報告序跳號）／乙 `R4`＋落檔後綴（序連續·前綴二義）／丙 `R4c1…` | **甲** | `probe_WG9270R4_*` `5` 件、`WG9270R4_*.log` `5` 件**皆已入倉**（`git ls-files` 實查）；`W-G.9-270R5`／`WG9270R5_` 之全倉命中 `0` | 乙使同一前綴**二義** ⇒ 後人定位靠 `grep` 時必歧義 |
| `3` | 本機 `python3` ⛔ 可用（`rc = 49`），而開場文 `§五` 之指令逐字為 `python3 …` | 甲 停機／乙 改用 `python`（`3.13.11`）並具名 | **乙** | `python --version` ⇒ `Python 3.13.11`；二器之 `rc = 0`、`stderr` `0` B | 甲之疑義**零土地後果·零生產碼**，且其解**當場可機驗** |

🔒 **三者皆**：零土地後果 ⋀ 零生產碼 ⋀ 可機驗（各附實測）⋀ 可逆 ⋀ 出艙具名。
🛑 **⛔ 落入黑名單任一項**（⛔ 動宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字；
二讀法之後果在土地上⛔ 相異；⛔ 新訂準據；⛔ 放寬任何既有之閘）。
🛑 **`2` 特請發單側核可**（`常規三`：命名須發單側核可·本窗僅得提案）。

---

## `§十`　交接註

### 十-1　🩸 本批之自捕（各載 形／徵候／攔法）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | **`git rev-parse :<glob>` 之 `:` 前綴使 bash ⛔ 展開 glob** | `fatal` ＋ `sha256sum` 印出 `e3b0c442…`（**空串之 `sha256`**）——一個看起來完全正常之值 | 路徑以變數承接（`F=$(ls …)`）；**當場印出空串之 `sha256` 以坐實其來源** |
| `2` | **`cat -A \| grep '\^M'` 對 UTF-8 CJK 檔偽陽** | 得 `47`，與 `od` 之 `0` 相斥 | 命中實為 `M-^M`（`0x8D` 之轉義形）；以 `od` ⋀ `python rb` **二法** ＋ 造甲（真 CRLF 得 `2`）坐實 |
| `3` | **`grep -c $'\r'` 對真 CRLF 檔偽陰** | 造甲得 `0`（期 `2`） | 同上二法取代之；⛔ 再用該法 |
| `4` | **`^#+[^0-9０-９]*?自誤` 之 CJK 字元類陷阱** | 該 grep 只抓到 `320`，而 probe 之真 `MAX` 為 `371` | 一律以 `probe_WG9267_issuer_measurers.py` 為準；取範本改用 `grep -F` |
| `5` | **heredoc 誤帶 `1` 次** | `wg9237_heredoc_guard` 硬閘 `rc ≠ 0` | 改以 `Edit` 落改（本會期第 `1` 次·前窗 `0` 次·再證該預設⛔ 賴記憶） |
| `6` | **對照組乙之哨兵字面經「框逐字」欄出艙** | 框欄印出完整字面（違 `GB-147`） | 其僅落於聊天與 scratchpad·**⛔ 入倉**；器改為 `hide_pat` ⇒ 落檔之完整字面命中 `0`（判別力：同檔 `GB-` 命中非 `0`） |
| `7` | **`$TMPDIR` 於本 shell 未設** | `> "$TMPDIR/x.log"` 展開為 `/x.log` ⇒ `Permission denied`（**loud**） | 改用 scratchpad 絕對路徑 |
| `8` | **`io.open(p,'w')` 於 Windows 靜默把 `\n` 轉為 `\r\n`** | 本報告初落得 `CR ＝ 305`（＝ 列數）；🔴 **徵候完全正常**——內容逐字無誤、渲染無異、`git status` 僅顯示 `A` | **入倉前逐檔量 `CR`** 並**與該類檔之既有體例對拍**（`docs/reports/*.md` blob 側 `CR ＝ 0`；`verify/out/*.log` 含 `CR` 係既有體例）⇒ 於 commit 前改寫為 `LF` |

### 十-2　本批新立之通則（候發單側採納·⛔ CC 自立為家法）

> **通則 `G`（源 ＝ 本批 `§三` 之自捕）**
> **凡一量測管線之末端為雜湊／校驗碼者，其「空輸入」之值須<u>當場印出為對照</u>**
> ——`sha256` 之空串值 `e3b0c442…` 與任何正常輸出**形上⛔ 可辨**，
> ⇒ 其為「管線斷而輸出為空」之**唯一可見徵候**。

> **通則 `H`（源 ＝ 本批 `§一` 之自捕）**
> **凡量行尾／不可見字元者，須以<u>位元組層</u>之法（`od`／`rb.count`）為之，
> 且須以「真 CRLF 之造」證其非偽陰**——`cat -A` 之 `^M` 於 UTF-8 CJK 母體**必偽陽**
> （`0x8D` 轉義為 `M-^M`），`grep $'\r'` 於本機**必偽陰**。

### 十-3　起手清單（予接手窗·**取收工後之倉態**）

1. **開窗第一動作**：`git ls-remote origin refs/heads/wip/s1-endpart` 實查（⛔ 本地追蹤 ref）；驗 `left-right` ＝ `0　0`。
2. **主線** ＝ **本報告之 commit**（其父 ＝ `260b0fdbf31de3c12ee3086aa39702122381b765`）——🛑 `常規九 三`：**⛔ 以本文所載為錨**，一律以 `git ls-remote origin refs/heads/wip/s1-endpart` **實查**｜**凍存 `p3a`** ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
3. **生產碼 `34` 檔** ＝ `app.py`（blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`·**本批未動**）＋ `verify/*.py` **單層** `33` 檔。
4. **四簿（本批期末實查）**：自誤 相異 `361`／MAX `371`／缺號 `[106,355,356,357]`；
   `GB` 相異 `163`／MAX `165`／缺號 `[12,87]`；`VR` 相異 `80`／MAX `95`／缺號 `[73,75]`；`K-9` 相異 `28`／MAX `29`／缺號 `[]`。
   ⇒ **次號**：自誤 **`372`**／`GB` **`166`**／`VR` **`96`**／`K-9` **`30`**；**坑之下一標籤 ＝ `as`**。
5. **未結阻塞項**：`GB-159`／`GB-160`／`GB-162`／**`GB-164`（本批新鑄·⬜ 未落地）**／`GB-165`。
6. **待發單側／KL 者**：見 `§七` 之 `1`–`5`。
7. **⛔ heredoc**（本批誤帶 `1` 次·全由 `wg9237_heredoc_guard` 攔下）；⛔ `python3`（本機無·用 `python`）。
8. 🛑 **`GB-164` 之帶與距**：`p7` 尚未答畢 ⇒ **二值並報之令仍在效**；本批已**三值並報**（`115.85`／`114.07`／活體 `115.88`）。

---

## `§十一`　🛑 **停**

候發單側之令，⛔ 自行續辦。
