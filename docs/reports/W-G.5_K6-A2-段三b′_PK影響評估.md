# W-G.5 — **K-6-A2 段三(b′)：PK 影響評估（只算不換）＋三筆補正**

> **基準**：`d8f97db`　**分支**：`wip/s1-endpart`
> **性質**：**只算不換**——`k97_solve_alloc_t` 維持**零生產呼叫點**、
> `_build_corner_range_v3` **一字未改**、生產路徑**未切換**。
> ⚠️ 所引行號**全部當場重 grep**；差異見 §六。

---

## 一、PK 影響評估（施工單 §一）

實料：`verify/out/probe_ruling_K9_7_pk_impact.log`
（`python verify/probes/probe_ruling_K9_7_pk_impact.py`·**rc 恆 0**）

### 1-1 三條路徑**同時**變動之作法

施工單 §一-2 已指明：只換 `min_corner_area_p*` 只測了 (c)。
本批之作法——**於 harvest 命名空間 patch `_build_corner_range_v3`**：

`select_corner_lots_both_sides_v12` **於函式內自行呼叫**該符號
（`grep -n "_rng_wb = _build_corner_range_v3" app.py`），`build_param_table` 亦然
⇒ 換掉該符號即令 **(a)(b)(c) 三條路徑同時**改用新範圍多邊形。
**生產碼一字未改**；包裝只多傳 `_t_override=`（段三(a) 已入庫之純加性參數）。
**patch 命中 32 次**（新輪注入 `t*` 之呼叫數）。

#### 🔬 三條路徑**確實被走到**之佐證（若只換 (c)，下欄必逐格不變）

| 情境 | 街廓/端 | 候選地號 | **(a)** 真交集(㎡) | **(b)** 範圍面積(㎡) | **(b)** 項三比 | **(b)** 總分 |
|---|---|---|---|---|---|---|
| 0m | R2/左 | 628-41(1) | 199.71→**200.72** | 225.69→227.11 | 0.8849→0.8838 | — |
| 0m | R2/左 | 628-40(1) | 8.66→**8.93** | 225.69→227.11 | 0.0384→0.0393 | 0.0153→**0.0157** |
| 0m | R5/左 | 628-18(2) | 199.89→**193.80** | 225.24→217.97 | 0.8875→0.8891 | — |
| 0m | R5/左 | **628-7(2)** | **1.58→1.07** | 225.24→217.97 | 0.0070→0.0049 | 0.0028→**0.0020** |
| 0m | R4/左 | 628(1) | 135.05→135.08 | 135.15→135.19 | 0.9993→0.9992 | 0.9917→0.9917 |

⇒ **(a) 真交集有變之列 16、(b) 範圍面積／項三比／總分有變之列 20**
⇒ **(a)(b) 確實隨新範圍變動、非僅換 (c) 門檻。**

### 1-2 逐格結果（16 格＝8 位移組 ＋ 8 對照組）

| 項目 | 結果 |
|---|---|
| **候選集合** | **16/16 格逐格相同**（無任何進／出） |
| **合格集合** | **16/16 格逐格相同**（無任何進／出） |
| **winner** | **16/16 格逐格相同** |
| **`forced_offset`** | **16/16 格逐格相同** |
| **優先權指數** | **2 格數值微變**（見下）·**名次未翻轉** |

**指數微變之兩格**：

| 格 | 候選 | 指數(舊→新) | 名次 |
|---|---|---|---|
| 0m R2/left | `628-40(1)` | 0.0153 → **0.0157** | 唯一合格者·**未翻轉** |
| 0m R5/left | `628-7(2)` | 0.0028 → **0.0020** | 唯一合格者·**未翻轉** |

二格之次位者皆為**未達標**（指數 `—`）⇒ **無名次可翻**。

**對照組（零位移之四角）**：R1 左右／R3 右／R6 右 × 兩情境
⇒ **八格逐項完全相同**（含指數逐位相同）——與預期一致，**未觸發停機**。

### 1-3 `forced_offset` 之現況（施工單 §一-4 指為最重要輸出）

本案**既有** `forced_offset = True` 之格共 **4**：
`0m R3/right`、`3.5m R2/left`、`3.5m R3/right`、`3.5m R5/left`。
**新舊逐格相同、無一改變。**

⚠️ 特記 **`3.5m R5/left`**：門檻雖自 `378.47` 降至 `371.17`（−7.30㎡），
**仍無候選達標** ⇒ `forced_offset` 維持 `True`。
即：**該角之門檻下修並不足以使任何人合格**，街角第 1 宗仍留設為抵費地。

### 1-4 結論

> **✅ 零翻盤。** 八格位移組與八格對照組之**候選集合、合格集合、winner、
> `forced_offset` 皆逐格相同**；僅二格之優先權指數數值微變，**且名次未翻轉**。

⇒ **切換 K-9-7 不會改變本案任何一個街角第 1 宗之歸屬，亦不改變任何抵費地狀態。**

### 1-5 🔴 惟有一項須 KL 知悉：**R5 左之候選資格餘裕大幅縮小**

`0m R5/left` 之 winner `628-7(2)`，其與街角規定範圍之**真交集**
自 `1.58㎡` 降至 **`1.07㎡`**。
而**候選資格門檻為 `> 1.0㎡`**（`grep -n "_has_p1 = _inter_p1_area > 1.0" app.py`）
⇒ **餘裕自 `0.58㎡` 縮為 `0.07㎡`**（僅存 12%）。

**今日仍在門檻之上、故未翻盤**；惟**該角之 winner 距「連候選資格都失去」僅 0.07㎡**。
⚠️ 若失去候選資格，該角將**無人可選 ⇒ 轉為 `forced_offset`（抵費地）**
——後果遠大於換人。

⛔ **本批不判定其可忽略**、不調參。**僅據實上呈。**
（⚠️ 本項係**觀察**，非預測：本案實測仍合格。本批**不臆測**換案或換圖時之行為。）

### 1-6 受控比較之佐證（施工單 §一-3）

`v12` 之**實際傳入引數**已逐次攔截列於 log【C】段。
兩輪各 6 次呼叫（每情境），**除門檻外逐項相同**：

```
0m #2: 門檻 p1 225.24→217.97／p2 inf→inf｜其餘引數 ✅ 逐項相同（require_g=True·候選 12·gmap 12）
0m #3: 門檻 p1 225.69→227.11／p2 inf→inf｜其餘引數 ✅ 逐項相同（require_g=True·候選 15·gmap 15）
```

⇒ `base_front_len_m`／`base_side_len_m_p*`／`cutoff_*`／`require_g_map`／
候選數／`g_values_map` 筆數**兩輪全等** ⇒ **唯一變因確為範圍多邊形**。
（兩輪皆 `require_g_map=True`、走同一 `g_values_map` ⇒ 無 `G_value`／`G_estimated` fallback 之分歧。）

### 1-7 ⚠️ 本批之過程失誤（自記）

探針**初版**以 `側`／`暫編地號` 為欄名讀 `diag`，**實際欄名為 `端`／`候選地號`**
⇒ 全 16 格讀成「候選 0→0、合格 0→0、winner —→—」，
**首度輸出竟自動得出「✅ 零翻盤」之結論**。

🔴 **若未複查，本批會以「假零翻盤」結案**——而該結論恰與最終真結論**字面相同**，
**極難由結果反查出錯**。
發現方式：`v12` 攔截器顯示候選數為 `2/11/12/15/14/5`，與「候選 0」**自相矛盾**。
⇒ **教訓：`0` 這種「乾淨」的結果最該懷疑；交叉來源自相矛盾是唯一咬出它的機制。**
（已於探針 `_pick` 之 docstring 就地記錄。）

---

## 二、三筆補正（施工單 §二）

### 2-1 🔴 `K-9-5-1` 編號還原

**已辦**（施工單 §二-1 四項全做）：

| 項 | 落地 |
|---|---|
| 「正面路寬未填 ⇒ 停機」**改回 `K-9-5-1`** | `grep -n "^### 🔒 K-9-5-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| 「街角最小規定範圍之構成」**改編 `K-9-5-2`** | `grep -n "^### 🔒 K-9-5-2 " docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| 全倉引用同批更新 | `app.py` 之 `K-9-5-1 ②③` 三處 → `K-9-5-2`；正典互參 6 處；GB-20 指標；段三-0 報告 |
| **撤除**段二-0 報告之改編註記、回復原文 | `docs/reports/W-G.5_K6-A2-段二-0_補正批.md` 一-6 |
| `K-9-5-2` 節首加沿革註 | 已加（含「`K-9-5-1` 自始為『正面路寬未填 ⇒ 停機』」） |

**§四-3 驗收**：以「`K-9-5-1` ＋ 撇號（U+2032）」之連寫字串全倉 `grep -rn` ⇒ **零命中**。

> 🔬 **本驗收自身之陷阱（已避開）**：若在報告裡**逐字寫出**該待查字串，
> 該行本身即會成為命中 ⇒ **驗收永遠不可能通過**。
> 故本報告與段三-0 之說明文字**一律以「加撇號（U+2032）之變體」描述**、不留該連寫。
> **重跑指令**：`grep -rn "K-9-5-1$(printf '′')" . --include="*.md" --include="*.py"`

### 2-2 `CLAUDE.md` 編號原則（逐字）

已入「工作紀律」節
（`grep -n "編號衝突一律以倉內為準" CLAUDE.md`）：

> **施工單所載編號與倉內衝突時，一律以倉內既有編號為準；新則取下一個未占用號。
> ⛔ 禁為配合施工單而改編既有正典編號。**

並載明案由三點（撇號前綴碰撞／動到已凍結報告／權威方向倒置）。

### 2-3 正典 K-9-7-c 表加標（**實測數字一字未改**）

已於該表下加註（`grep -n "本表之 \`k\`／\`m\` 係擾動觀測值" docs/rulings/K-6_街角地分配程序與可分配判準.md`）：

> ⚠️ 本表之 `k`／`m` 係**擾動觀測值**（有效 `∂W/∂D`／釘住後 `−∂D/∂t`），
> **非解析偏導**，**禁當解析靶**。與 K-9-7 規格之 `k = ∂w/∂d`、`m = −∂D/∂t`
> **同名不同量**（體例同 K-9-6-a）。

並附「八格逐格自洽」之說明（`k` 三格／`m` 三格＋二格拗點假象）。

---

## 三、§三 標記留待段四

**GB-26** 已登記（`grep -n "GB-26" docs/reports/W-G.4_泛用阻塞項登記表.md`）：

> `R4/left` 係唯一「`m` 真正進入斜率（分支 ③）、且解析與正典觀測相差約 60%」之格
> （解析 `0.000424` vs 正典 `+0.000266`）。`R2/left` 同為 ③ 但兩者一致（`0.009750`）
> ⇒ **不能與 R2 一併結案**。分支 ①② 之 `m` 不進斜率 ⇒ 其差異已由「觀測 vs 解析」解釋完畢；
> **唯 ③ 之 `m` 真正乘進 `(c − k·m)`** ⇒ **段四須單獨確認**。

**本批不查因**（KL 明令）。

---

## 四、`run_all`

實料：`verify/out/K6A2_seg3b_runall.log`（`EXIT=1`·一般模式·未用 `WV_BAKE`）
⇒ `132 名目／86 PASS／46 FAIL`。

### 4-1 名目集合 diff 之實際輸出行

```
$ diff <(K8_seg3_C 靶 FAIL 名目 sort) <(段三b′ 輪 FAIL 名目 sort)
  (無輸出)
$ diff <(K8_seg3_C 靶 PASS 名目 sort) <(段三b′ 輪 PASS 名目 sort)
  (無輸出)
$ diff <(段三a 輪 FAIL 名目 sort) <(段三b′ 輪 FAIL 名目 sort)
  (無輸出)
$ diff <(段三a 輪 PASS 名目 sort) <(段三b′ 輪 PASS 名目 sort)
  (無輸出)
```

⇒ **四組皆空**、逐字零進零出（本批只算不換 ⇒ 此為正確結果）。

### 4-2 ⚠️ 依 `CLAUDE.md` 常設紀律，本項不得單獨作為驗收依據

`app.py` 本批之改動**全在註解／訊息字串**（`K-9-5-1`→`K-9-5-2` 四行），
且 `_build_corner_range_v3`／`select_corner_lots_both_sides_v12` 經 **AST 比對與 HEAD 逐字相同**
⇒ `run_all` 零進零出係**必然**，**不證明 PK 評估正確**。
**正確性由 §一 之受控比較承擔**（同一 `v12`、同一引數、唯一變因為範圍多邊形，
且 §1-1 已實測證明 (a)(b) 路徑確被走到）。

---

## 五、只算不換之確證

```
$ grep -rn "k97_solve_alloc_t" --include="*.py" .   ⇒ 僅命中定義（app.py）與二支探針
$ ast 掃 main() 內之 k97_solve_alloc_t 呼叫          ⇒ 無
$ git diff -- app.py                                ⇒ 僅 K-9-5-1→K-9-5-2 之註解字串（三處）
```

⇒ `_build_corner_range_v3` **一字未改**；生產路徑**未切換**；
patch **僅存在於探針行程內**，結束前已還原。

---

## 六、施工單與倉內實況之差異（§四-5）

| 施工單所載 | 倉內實況（`d8f97db` 當場 grep） | 處置 |
|---|---|---|
| (a) 候選資格 `app.py:10339` | 屬實（`_has_p1 = _inter_p1_area > 1.0`） | 已納入 |
| (c) `min_area_to_apply` `:10346`／`:10364`·閘 `:10449` | 屬實 | 已納入 |
| `select_corner_lots_both_sides_v12` `:10039` | 屬實 | 已納入 |
| **(b) 優先權指數 `app.py:16678`** | 🔴 **該行係 UI 說明文字（markdown），非計算碼**。實際計算在 `grep -n "score_corner_cut = 0.4" app.py`（`:10479-10486`） | **以倉內為準**·已改用實際計算處 |
| **(b) 「跨占街角面積」權重 0.4** | 🔴 **不止項三**——**三分項之分母皆取自範圍多邊形**（`_cc_den` 截角邊／`_sl_den` 側街邊／`_tj_range` 範圍面積） ⇒ 改範圍**三項全動** | **以倉內為準**·評估已涵蓋三項（實測項三比與總分皆變） |
| §二-1 令 `app.py:9602/:9719/:9725/:9743` 之 `K-9-5-1` 引用更新 | 屬實（四處·`:9602`／`:9719`／`:9725` 為 ③、`:9743` 為 ②） | 已全數改 `K-9-5-2` |
| §一-1 母體「有位移之 8 格」 | 屬實 | 位移組 8＋對照組 8＝16 全跑 |
