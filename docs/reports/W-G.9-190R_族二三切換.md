# 🧾 `W-G.9-190R`　**族②③ 之切換**執行報告（`W-G.9-5` 之落地）

**出文窗**：CC　**施工單**：`docs/orders/W-G.9-190R_族二三切換.md`（`35215` B／`441` 列·`SELF_SHA256` 相符）
**基座**：`4251234847f4ee5817c6c5a8effd318261df4cb5`　**取號**：`常規四 三` ⇒ `W-G.9-190R`
**性質**：🔴 **含生產碼**且**改變 app 路徑之配地邏輯**（族②③ 各二處）

🛑 **push 狀態**：commit `1`／`2` **已 push**（零生產碼·依 `§二-1` 自辦）；
commit `3`／`4`（**生產碼**）與 commit `5`（零生產碼·**被其祖先阻擋**）**⛔ 未 push·待請示**。

---

## `§零`　開工閘（`S-0`〜`S-6`）——**全綠**

| 閘 | 結果 |
|---|---|
| **`S-0`** | ✅ 取檔內**最末**一個 `SELF_SHA256`（出現 `4` 次·索引 `[23,303,338,440]` ⇒ 取 `440`）；受詞 `35137` B、末位元組 ＝ `\n` ⇒ 實算 `28fad809f2063941…` **逐位相符** |
| **`S-1`** | ✅ **十檔全符**（⛔ 未照抄單表·逐檔現查）；`app.py` 尾**無 `LF`**／末位元組 `)`／`wc -l` `23411`／`splitlines()` `23412` —— 四者皆與單載相符 |
| **`S-2`** | ✅ `ls-remote` ＝ `4251234847f4ee5817c6c5a8effd318261df4cb5`；`is-ancestor(25857aa→HEAD)` `rc=0` ⇒ **係其後裔·正常開工** |
| **`S-3`** | ✅ `core.autocrlf=false`；`git status --porcelain` **跑前 ＝ 空**／**跑後 ＝ 空**（二態皆出艙·見 `§六`） |
| **`S-4`** | ✅ 已讀 `§十一-3`；逐項守（見 `§五`） |
| **`S-5`** | ✅ 於**新 clone 重跑**，與已入倉之基準量 `probe_WG9190_baseline_25857aa.log` **逐位相同**（`diff` `rc=0`·含 `[T2-DIAG]` 全文與 `R2` 三量） |
| **`S-6`** | ✅ **開工復現逐項相符**（見下） |

### `§零-1`　`S-6` 開工復現（`WV_OUT_NAME="_scratch_s6_base.log"`·跑畢已刪·`git status` 空）

| 受詞 | 單所令 | CC 實測 | 判 |
|---|---|---|---|
| `_build_g_row` 全等 | `True` | `True` | ✅ |
| `_solve_one` 全等 | `True` | `True` | ✅ |
| `_advance_block_with_split` | `False`·實質差 `3` | `False`·`3` | ✅ |
| 逐街廓迴圈 | `False`·實質差 `17` | `False`·`17` | ✅ |
| `st.*` 略去 | `0` | `0` | ✅ |
| **實質差總處數** | **`20`** | **`20`** | ✅ |
| 判別力對照二問 | 皆 `False` | 皆 `False` | ✅ |
| `rc` | `1` | `1` | ✅ |

🔒 **`ast.dump` bytes ⛔ 未作為判準**（單 `§零-1` 逐字）——本機 `CPython 3.13.11` 得
`4353`／`1293`／`23821`／`20641`／`74569`／`94370`，與發單側之 `3.12.3` 得數不同而**判定與差數逐項相同**。

---

## `§一`　`§二-2` 之判法：**逐 commit 之輸出**（🛑 原樣出艙·⛔ 未以判斷代之）

指令逐字：
```
git diff --name-only <前一 commit>..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

| commit | 輸出（原樣） | 命中行數 | 判 |
|---|---|---|---|
| `4251234`（前批停機報告） | *（空）* | `0` | 零生產碼 ⇒ **已 push** |
| `d8d64b0` commit `1` | *（空）* | `0` | 零生產碼 ⇒ **已 push** |
| `69daed5` commit `2` | *（空）* | `0` | 零生產碼 ⇒ **已 push** |
| **`571b2c3` commit `3`** | `app.py` | **`1`** | 🔴 **生產碼 ⇒ 請示** |
| **`c141b47` commit `4`** | `app.py` | **`1`** | 🔴 **生產碼 ⇒ 請示** |
| `aa52bc5` commit `5` | *（空）* | `0` | 零生產碼（⚠️ 其祖先 `3`／`4` 未 push ⇒ **實際被阻擋**·一併請示） |

🔒 **對照組（證框非紅）**：以字串 `app.py` 餵同一 `grep -xE` ⇒ **命中** ✅。

---

## `§二`　`§四` 取號現查（母體以 `git ls-files -z` 取·`GB-88` 族）

🔒 **母體**：追蹤檔 **`1510`**／`.md` **`472`**；**開不到之檔數 ＝ `0`** ✅
（🛑 一律 `-z` 並以 `\0` 切分——`core.quotepath=true` 會把 CJK 檔名轉義，以其輸出直接 `open()` 者**全部開不到、靜默得 `0`**）。

| 類 | 框（逐字） | 相異 | MAX | 缺號數 | 次號 |
|---|---|---|---|---|---|
| 自誤（**本單 `§四` 之窄框**） | `` ^###\s*自誤\s*`?([0-9]{1,4})` `` | `41` | `223` | `182` | **`224`** |
| 自誤（**`W-G.9-184` 裁之正典寬形**） | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | **`192`** | `223` | **`31`** | **`224`** |
| `GB` | `` ^(?:## \|### \|\| *)`GB-([0-9]{1,4})` `` | `44` | `127` | `83` | `128` |
| `VR`（扣哨兵 `999`） | `` ^## `VR-([0-9]{1,4})` `` | `69` | `84` | `15` | `85` |

🔒 **自誤二框並報**（`CLAUDE.md:725`）：**MAX 同為 `223`** ⇒ **次號 `224` 不受框差影響**；
受影響者僅「相異／缺號集」（`41`/`182` vs `192`/`31`）——正是 `W-G.9-184` 所裁之受詞。

### `§二-1`　🔴 **本單 `§四` 之「戒」框有缺（照實回報·依 `常規二` 取保守項）**

**單載之框**（逐字）：`` ^(?:> )?🛑 \*{0,2}戒 \*{0,2}`?([0-9]{1,4})`? ``——**要求 `🛑`**。
🔒 **CC 現查**：`戒 16` 之定義列（`docs/reports/W-G.9波_claude.ai側自誤登記.md:3783`）逐字為

> `> **戒 \`16\`**　凡寫**計數式或逐項式**之驗收判準，須於出單前現查其……`

**⛔ 無 `🛑`** ⇒ 該框**漏之**（`16` 在集合內 ＝ `False`），與**同節**之敘述
「戒之框**已於 `W-G.9-187R` 擴為含引言塊形**（因 `戒 16` 之定義形為 `> **戒 \`16\`**`）」**互斥**。

⇒ 🔒 依 `常規二` **取保守項** ＝ `CLAUDE.md:826` 之**正典框**
`` ^(?:🛑 |> )?\*{0,2}戒 \*{0,2}`?([0-9]{1,3})`? ``（`🛑 ` 與 `> ` 係**二擇一之前綴**）：

| 框 | 命中列 | 相異 | MAX | `16` 在內 | `40` 在內 |
|---|---|---|---|---|---|
| 本單 `§四` 之框 | `21` | `20` | `39` | **`False`** 🔴 | `False` |
| **`CLAUDE.md:826` 正典框** | `22` | **`21`** | `39` | **`True`** ✅ | `False` ✅ |

🔒 **MAX 二框同為 `39`** ⇒ **次號 `40` ⛔ 不受影響**。⛔ 本批未鑄戒。

---

## `§三`　commit `1`：`N-1` 登記（零生產碼·**已 push**）

### `§三-1`　鑄 **自誤 `224`〜`229`（六則）**

🔒 **標題形**依 `常規四（九）四`，取既有最末一則 **`自誤 223`** 之形逐字為範本：
`### 自誤 \`NNN\`（發單側·落地窗）：<受詞>`。
🔒 **`常規四（九）二` 之二項機檢**：
> ① **形之自證**：窄框與寬形掃 payload **皆得 `[224,225,226,227,228,229]`**
>    （判別力對照：行中提及之形 ⇒ 命中**空**）
> ② **MAX 之推進**：二框皆 **`223` → `229`**，且 `224`–`229` **皆不在缺號集**

🛑 **CC 之射程聲明（逐則已載於登記本體）**：
- **`自誤 228`**（宣稱未為之行為）：其受詞係**發單側工作環境內之檔案**，
  **⛔ 不在 CC 之母體（倉）內** ⇒ **CC ⛔ 未能亦⛔ 未曾獨立復現**，係**發單側自陳**。
- **`自誤 229`**（`20 → 19` 之偽停機款）：入倉時 CC **尚未**施作族② 切換 ⇒ 彼時⛔ 未復現。
  🔑 **本報告 `§四-4` 已補其復現**：CC 於 `Python 3.13.11` 實測**亦得 `20 → 21`**。

### `§三-2`　四項 🔧（⛔ 皆不鑄號）

1. `W-G.9-187R` 之 `:368`／`:485`：**維持 `39`** ＋ 補宣告框 `` ^\[T3-GATE\] 街廓 `` ＋
   依 `CLAUDE.md:725` 加註「裸框 `57`／錨定框 `39`，差 `18` ＝ 摘錄重列」。
   🔒 該檔**⛔ 不在 `wg942` 之受檢清單內**（命中 `0`）⇒ 其就地更正**未使任何閘轉紅**。
2. `verify/wf_f0.py:9` 之偽述就地更正。
   🔒 **證**（母體 ＝ `ls-files -z` 之 `.py` **`239`** 檔）：`^def run_step_g` 命中 **`1`**
   ＝ `verify/stepg_pipeline.py:238`；**對照組** `^def ` 命中 **`237`** 檔 ⇒ 框非紅。
3. `CLAUDE.md` **末端追加字樣錨**（族②③ 各二·`grep -F` 固定字串）＋
   併記 `CLAUDE.md:760` 之箭頭係**注入方向** ＋ 附 `177R §四-4` 之正確方向表。⛔ 不追改上文一字。
4. `probe_WG9189_s6_replica.py` 加**直譯器版本標框**（**純插入**·`deletions = 0`）。

### `§三-3`　`CLAUDE.md` 之**常規一補款**（逐字載 `§二-1`〜`§二-3`·末端追加 `+90/−0`）

🔒 **⛔ 不鑄號**之機檢：`^\*\*常規(.)` 於 payload 命中 **`0`**（對照組合成列命中 `1`）；
入倉後 `常規` 之序 **期初 `一二三四五六七四四八四九` → 期末 同** ⇒ **逐位不變**。

---

## `§四`　commit `3`：族② 之切換（**生產碼**·待請示）

### `§四-1`　受詞與落地

| 側 | 逐字 |
|---|---|
| **切換前** | `_W_prev_left = (_left_buffer_S * _cos_dn) if _has_left_corner else 0.0`（＋ `_right`） |
| **切換後** | `_W_prev_left = 0.0`（＋ `_right`）——**逐字同 `verify/stepg_pipeline.py:628-629`** |

🔒 正典依據（`CLAUDE.md:758` 逐字）：`mp` 由三級權威**皆作 SIDE 線之中點**；
**舊式 ＝ 新式 − `W_0`**，相等**當且僅當 `W_0 = 0`**，而正典明載其**照實可負**。
（`:759` 逐字：⛔ 非待裁項、⛔ 非失效，係**已裁而未遷移** ⇒ ⛔ 不上呈 KL。）
🛑 **⛔ 未順手搬移 `stepg` 之 `_W0_*`／`_Wfirst_*`**（`189R` 類丙·本批⛔ 不動）。

### `§四-2`　主閘 `G-1`〜`G-3`（`verify/probes/probe_WG9190R_fam2.py`·`rc=0`）

**`G-1`（源碼層）**：`_advance_block_with_split` **內**之全部 `_W_prev_*` 賦值節點，
app 與 stepg **逐節點 `ast.dump` 逐位相同** ✅
（`_W_prev_left` app@`[20995, 21107]`／stepg@`[628, 676]`；`_W_prev_right` app@`[20996, 21211]`／stepg@`[629, 767]`）。

🔒 **射程之工程裁（`§零-2`「純技術自行做完」·逐字呈裁）**：限縮於 `_advance_block_with_split` **內**，
⛔ **不取「全檔首次」**——實測 `app.py` 全檔首個 `_W_prev_left` 賦值在 `:9649`
（`_place_pool_parcels` 族之 `float(adv_final.get('Wf_left'…))`），與 `stepg:628`
**語意上⛔ 非對應物**；限縮後二側**恰各 `2` 節點**、對應明確。

**`G-2`（判別力造）** ✅：於**記憶體內字串副本**還原舊式後重跑 `G-1` ⇒ 判**不同**
⇒ `G-1` **⛔ 非恆真**（⛔ 未落檔、⛔ 未動工作樹）。

**`G-3`（框表·🛑 二母體之框⛔ 不通用）** ✅ —— **四框皆由 `1` 降為 `0`**：

| 母體 | 框（`grep -F` 固定字串） | 基座 | 現況 |
|---|---|---|---|
| `app.py` **原始碼**（含**字面括號**） | `_W_prev_left = (_left_buffer_S * _cos_dn)` | `1` | **`0`** ✅ |
| `app.py` 原始碼 | `_W_prev_right = (_right_buffer_S * _cos_dn)` | `1` | **`0`** ✅ |
| `S-6` log（`ast.unparse` **去括號**） | `_W_prev_left = _left_buffer_S * _cos_dn` | `1` | **`0`** ✅ |
| `S-6` log（同上） | `_W_prev_right = _right_buffer_S * _cos_dn` | `1` | **`0`** ✅ |

### `§四-3`　🔑 佐閘（`§六-4`）：**三量同時收斂至 shim 值** ✅

| 量 | 池帶2 `s` 寬（全精度） | 退化帶數 | `merged` 段數 |
|---|---|---|---|
| 切換前（【倉】`177R §四-4`·app 舊式） | `32.9639559294` | **`3`** | **`4`** |
| **切換後（本批現跑）** | **`33.0810700422`** | **`2`** | **`3`** |
| 目標（【倉】`177R §四-4`·shim 新式） | `33.0810700422` | `2` | `3` |

`|Δ|`：池帶2 寬 **`5.542e-13`**（容差 `1e-4`）／退化帶數 `±0`／`merged` `±0`。

🔒 **方向 ＝ `32.9640 → 33.0811`／退化帶數 `3 → 2`／`merged` 段數 `4 → 3`**
（權威來源 ＝ `177R §四-4`）。
⚠️ `CLAUDE.md:760` 所載之 `2→3`／`3→4` 係 **shim 之注入方向**（`自誤 227`）——**本批實測坐實之**。

🔒 **量法⛔ 不硬編**：自 `app.py` 以 AST 讀出 `_W_prev_left` 之**初值運算式**（得 `0.0`）據以決定注入
⇒ **切換若未落地，本閘自動量到舊值**。

🛑 **佐閘之限（明載·⛔ 不得省）**：本量係 **harness 內之模擬**、⛔ **非 app 路徑實跑**。
**殘餘風險** ＝ app 路徑（`main()`）從未被任何自動測試執行 ⇒ 歸 **`GB-123`**，
本批**⛔ 不解除、明載之**。唯一真量者係 KL 之 UI 實跑——**本批⛔ 未授權、⛔ 不辦**。

### `§四-4`　🔑 `§六-3` 之**出艙記錄（⛔ 非停機款）**

**`S-6` 實質差：基座 `20` → 族② 後 `21`**（逐區：`_advance_block_with_split` `3`→**`4`**／逐街廓迴圈 `17`→`17`）。
⇒ 🔑 **與發單側於 `Python 3.12.3` 之實測逐項相符**（本機 `3.13.11`）
⇒ **`自誤 229` 之 `20→21` 至此由 CC 獨立復現**（`§三-1` 所欠者已補）。
🛑 該數⛔ 不作任何判準（差異對齊之**重分段**使其⛔ 非單調）。

### `§四-5`　🔴 因切換而成偽之註解，據實更正（⛔ 不留偽述於生產碼）

1. `app.py` 之 `W-G.9-4 expand` 段原逐字「上二行之 `_W_prev_*` **仍取舊式** `buf · _cos_dn`」⇒ 已成偽 ⇒ 就地更正。
2. `app.py` 之 `k956_W_from_mp` docstring 原逐字「`app.py` 之族②③ … **仍取舊式**」
   ⇒ 對**族②** 已成偽、對**族③** 仍真 ⇒ commit `3` **末端加註分列**；commit `4` 再更新為「二者皆已切換」。
🔒 二處皆⛔ 不改語意、⛔ 不使診斷值進入生產路徑。

---

## `§五`　commit `4`：族③ 之切換（**生產碼**·待請示）

### `§五-1`　🔴 **族③ ⛔ 非一列對換**（現查坐實）

`stepg:812-844` 含**三物**，而 app 側**三者皆無**：
① `_mp_base_W0` 閉包 ② `_end_pt_o`／`_dhr_o`（`app.py` **全檔命中 `0`**）
③ `if _fo_* else 0.0`（`W-G.9-27`「非 forced 之選槽起算點校回 `0`」）。
⇒ 本 commit 係**逐字複刻** `stepg:812-844`（⛔ 非改寫），縮排依 app 該區**實測**之 `28` 空格。

🔒 **開工前之名之現查（AST·同一函式作用域·⛔ 非推定）**：`_fo_left`@`20862`／`_fo_right`@`20863`／
`_side_mid_left`@`20922`／`_side_mid_right`@`20923`／`corner_pt`／`allocation_dir_block`／`blk_meta`／
`_left_buffer_S`／`_right_buffer_S` **皆於受詞列之前於 `main()` 內賦值** ✅；
`_end_pt_o`／`_dhr_o` **全檔 `0`** ⇒ 本 commit 新增之。`_fo_*` 二側**逐字相同**。

🛑 **⛔ 未於 `app.py` 另寫第二份 `W` 定義**（`GB-48` 族）——新增之 `_mp_base_W0` 係**純委派**
至單一產生者 `k956_W_from_mp`；其 `_du` 自算之式**逐字同 stepg**
（⛔ 未改為先正規化再傳——會改變 `_dn ≤ 1e-9` 之退化行為）。

### `§五-2`　主閘 `G-4`／`G-5`（`verify/probes/probe_WG9190R_fam3.py`·`rc=0`）

**`G-4`（源碼層·🛑 直接對 `app.py` 檔案 `grep -F`·⛔ 不經 `S-6` 之 log 通道）** ✅：

| 框（`grep -F`） | 基座 | 族③ 後 |
|---|---|---|
| `'b': _left_buffer_S * _cos_dn` | `1` | **`0`** ✅ |
| `'b': _right_buffer_S * _cos_dn` | `1` | **`0`** ✅ |
| `k956_W_from_mp(...)` 之委派呼叫 | `0` | **`1`** ✅（須 `≥1`） |

🛑 單 `§六-5` 逐字之戒**已遵**：**⛔ 未以 `S-6` 之清單為族③ 之閘**——其於 log 命中恆 `0`
係 `GB-115` 之顯示截斷、⛔ 非「受詞不存在」；以其立閘者將**恆綠**。

**`G-5`（判別力造）** ✅：於**記憶體內字串副本**還原舊式後重跑 `G-4` ⇒ 二框**各得 `1`**
⇒ `G-4` **⛔ 非恆綠**。

### `§五-3`　族③ 之預測：`R2` 三量**⛔ 未再變動** ✅

族③ 後現跑 ＝ `33.0810700422`／`2`／`3`，與族② 後**逐項相同**（`|Δ|` `5.542e-13`／`±0`／`±0`）
⇒ **停機款 `7` 未觸**。

🛑 **本量之通道限（明載·⛔ 不得省）**：族③ 之受詞在 `app.py` 之 `main()` 內，而 harness 走
`stepg` 之複本 ⇒ **本量對族③ 之切換<u>結構上不敏感</u>**——其「未變動」係**必然**、
⛔ **非**「切換無後果」之證據。**族③ 之實質證明在 `G-4`／`G-5`（源碼層）。**
🔒 殘餘風險歸 `GB-123`，本批⛔ 不解除、明載之。

---

## `§六`　commit `2`／`5` 與收工閘

### `§六-1`　commit `2`：`N-2` 流程工具（零生產碼·**已 push**）

**（甲）** `verify/probes/probe_order_preflight.py`（`P-1`〜`P-6`）。
🛑 性質⛔ 不混同：**`P-3`／`P-5` 機械可判 ⇒ `rc≠0`**；`P-1`／`P-2`／`P-4`／`P-6` 係
**文字啟發式、必有偽陽** ⇒ ⛔ 不計入 `rc`。`P-3` 之 `def main` 區間係 **AST 實查**（得 `14234`-`23409`·⛔ 未硬編）。

🔑 **判別力造（`--selftest`·四造全數成立·`rc=0`）**：
靶① `W-G.9-190 §一 N-1` 之 `P-3` **紅**／靶② 同單之 `P-4` **提示**／
靶③ `W-G.9-191 §六-3` 之 `P-6` **提示**／**反靶** `W-G.9-190R` 之 `P-3` **不紅**（證⛔ 非恆紅）。

🩸 **本器首版之二缺，由其自身判別力造抓出（CC 自捕·⛔ 非調門檻求綠）**：
> ① **`P-6` 粒度太粗**——bullet list 於空行分段下係**單一段**，致「本條之停機款」被
>    **他條**之「實跑」豁免（`W-G.9-191:121` 三 bullet 同段）⇒ 新增 `units()`，逐**條目**判。
> ② **`P-3` 射程太寬**——**引述**既往／已知有缺之設計之節（登記自誤／廢止／指定為靶）
>    被當成閘規格 ⇒ 加排除語 `("自誤","廢止","原款","案由","靶")`。
>    🔒 **⛔ 過寬之保證** ＝ 靶① 之節經實查**⛔ 不含任一排除語** ⇒ 其紅係真缺、非排除語所致。

**（乙）** `docs/orders/` 匯流排：四單以原名入倉，**皆與來源逐位同源**；
`W-G.9-190` 加註 `⛔ 作廢`（**純檔首加註·原文一字未改**·後綴逐位相同）。
⚠️ 該加註**必使其 `SELF_SHA256` 不再相符**——係**刻意**、⛔ 非竄改；
🔑 **其反成 `P-5` 之活體正對照**：對該檔單獨跑得 **`rc=1`**（`P-5` 🔴 不符）⇒ 證 **`P-5` ⛔ 非恆綠**。

🔒 **本單之 `S-0b`（收單機檢）**：`rc=0`（⛔ 無機械紅）；**`4` 項 🟡 提示逐項處置**（⛔ 未靜默略過）：
| 檢 | 列 | 處置 |
|---|---|---|
| `P-2` | `:262` | **採納**——本批出艙該類數一律二數並報 |
| `P-1` | `:279` | **具名豁免**（偽陽：母體於鄰句即具名「其於 `app.py` 原始碼中命中為 `1`／`1`」） |
| `P-4` | `:240` | **具名豁免**（偽陽：座標系已於同節 `§六-4` 前段逐字具名） |
| `P-6` | `:151` | **具名豁免**（偽陽：同段已具名「發單側本回合**親踩**」即其實測出處） |

### `§六-2`　commit `5`：`S-6` 四分分類器（零生產碼）

**末端追加**於 `probe_WG9189_s6_replica.py`（`+251/−0`）。
🔒 **置於 `if __name__ == "__main__":` <u>之後</u>** ⇒ 腳本執行時 `sys.exit(main())` 已先終止
⇒ **既有判定路徑逐位未動**（機檢：**嚴格前綴 `True` ／ `deletions` `0`**），
且改後以腳本重跑 `S-6` **逐項復現**（`True/True/False/False`·`4+17 = 21`·`rc=1`）。
入口係 `import` 後呼叫 `classify4_main()`，驅動檔 ＝ `probe_WG9190R_class4.py`（**⛔ 未複製分類器任何一行**）。

🩸 **本器首版之缺，由其自身判別力造抓出（CC 自捕·⛔ 非調參數求綠）**：
首版把「乙」實作為**敘述形**之判，致注入之 `st.error(f'x')` ↔ `st.error(_dev * 2 + _tol_lot)`
**逃過而判乙** ⇒ **分類器紅**。查因：單 `§十` 逐字係「名單外之任一**識別符** ⇒ 歸甲」。

🔒 **CC 之實作解（工程裁·`§零-2`·🛑 逐字呈裁）**：受檢之識別符集取**二側之<u>對稱差</u>**
（⛔ 非聯集）——「宿主介面差」之受詞係**差異本身**，二側相同之訊息負載（如 `blk_label`）
⛔ 非介面差之一部。⇒ 注入案之對稱差 `{_dev, _tol_lot}` ⊄ 白名單 ⇒ **判甲** ✅。
🛑 **若發單側認本解過寬，改採聯集即一行之改**，惟屆時**類乙將近乎不可達** ⇒ **呈裁**（`§九-2`）。

🔑 **判別力造：✅ 過**（注入後**仍判甲**）⇒ 停機款 `10` 未觸·`rc=0`。

🛑 **四分之結果（`21` 項）：類甲 `14` ／ 類乙 `1` ／ 類丙 `6` ／ 類丁 `0`**
⇒ 🔴 **與 `§十` 之「期末預期：類甲數應為 `0`」不符** ⇒ 依 `§十` 逐字**逐項具名呈裁**
（逐項機械理由見 `verify/out/probe_WG9190R_class4.log`），⛔ 未自行豁免、⛔ 未改白名單以求綠。

### `§六-3`　`S-3` 之 `git status --porcelain` **二態**

**跑前** ＝ `[]`（空）；**跑後**（全部 commit 後）＝ `[]`（空·無殘留 scratch）。
🔒 本批之三支 scratch log（`_scratch_s6_base.log`／`_scratch_s5.log`／`_scratch_t2_after.log`／
`_scratch_s6_c4.log`）**跑畢即刪**，並以 `git status --porcelain` 空輸出證之。

---

## `§七`　`[T2-DIAG]` 之射程限（`§七` 所令·⛔ 不得省）

🔒 `GB-118` 之實測：`run_all` 之主幹於 `②-宗 圍堵閘破[R2]` 中止 ⇒ 其 `T2-DIAG` **僅有 `R1`／`R2`**。
⇒ **本批之 `[T2-DIAG]` 對拍係走<u>逐街廓 `run_step_g`</u> 之通道**（⛔ 非 `run_all` 之主幹），
其涵蓋 `R1`／`R2`／`R3`／`R4`／`R6` 各 `1` 列、`R5` **`0` 列**（**期望事實**：
`②-宗 圍堵閘破[R5]：宗-宗重疊 = 56.8186 > 上界 1.5999`）。
🔒 **切換前後逐位全同**（`5` 列·`diff` `rc=0`）⇒ **停機款 `8` 未觸**。
⚠️ 併記：harness 之 `[T2-DIAG]` **本即 shim 側** ⇒ 切換 `app.py` 對其**零影響**
（`§四-3`／`§五-3` 之通道限）；本對拍所證者 ＝ **⛔ 無任何街廓因本批而變動**。

---

## `§八`　收工閘（`11` 項）

| # | 項 | 結果 |
|---|---|---|
| `1` | `S-0`〜`S-6` 全過，`S-6` 開工復現逐項相符（dump bytes 除外） | ✅ `§零` |
| `2` | `[T5-FAM2]`：`R2` 三量同時收斂至 shim 值 | ✅ `§四-3`（`|Δ|` `5.542e-13`／`±0`／`±0`） |
| `3` | `N-1` 後 `S-6` 實質差 `20 → 19`，消失者恰第 `1` 項 | 🔒 **該款已由單 `§六-3` <u>廢止</u>**；實測 `20 → 21`，與發單側逐項相符（`§四-4`） |
| `4` | `[T5-FAM3]`：`R2` 三量⛔ 未再變動 | ✅ `§五-3` |
| `5` | `[T3-GATE]` 期末仍為 `39` 列 | 🛑 **⛔ 未辦**——見 `§九-3` 呈裁 |
| `6` | 四分分類器已入倉、判別力造判甲、`21` 項之類甲數已出艙 | ✅ `§六-2`（類甲 **`14`**·逐項具名呈裁） |
| `7` | `N-4` 六項全辦；`自誤 224`／`225` 之標題形取自既有最末一則 | ✅ `§三`（六則·形之自證與 MAX 推進皆機檢） |
| `8` | `run_all` 全綠，或紅項經實測基座態證非本批所致 | ✅ **已證非本批所致**——二態輸出**逐位相同**（見 `§九-1`） |
| `9` | 取號現查：四類之錨定框 MAX 與次號命中 `0`，逐類載其框逐字 | ✅ `§二`（併報「戒」框之缺·`§二-1`） |
| `10` | 三 commit 分立，`numstat` 逐 commit 逐檔已備，基座檔 `deletions = 0` | ✅ `§十`；⚠️ `app.py` 之 `deletions` 見 `§十` 之逐字辨析 |
| `11` | 倉內報告 | ✅ 本檔 |

---

## `§九`　🛑 呈發單側裁（⛔ 不上呈 KL）

### `§九-1`　✅ **`run_all` 之紅係<u>基座既有</u>、⛔ 非本批所致（已實測坐實）**

依收工閘 `8`／停機款 `9` 之逐字，CC **於另二個拋棄式 clone 各跑一次 `run_all`**：

| 態 | clone | `HEAD` | `app.py` blob | `rc` | 對帳三分類 | 判 |
|---|---|---|---|---|---|---|
| **基座** | `C:\w190u` | `4251234` | `d756d9ad…`（切換**前**） | `1` | 類 I `0`／類 II `8`／類 III `13` | 🔴 對帳 FAIL |
| **期末** | `C:\w190t` | `571b2c3` | `7c58d002…`（族② **後**） | `1` | 類 I `0`／類 II `8`／類 III `13` | 🔴 對帳 FAIL |

🔑 **逐位對拍**：二 log **各 `516` 列**；`diff` 得 **`48` 列**差，**全部**為 clone 路徑
（`C:\w190u` ↔ `C:\w190t`）。
🔒 **機械證明（⛔ 非目視）**：僅將該路徑正規化為 `<CLONE>` 後，二者
**各 `32972` B、逐位相同 ＝ `True`**。
🔒 **⛔ 無截斷位移之保證**（memory ㊲ 之戒）：二路徑**皆 `8` 字元**、長度相同
⇒ ⛔ 不致因固定 byte offset 之截斷而偽造差異。
🔒 **判別力對照（證本對拍⛔ 非恆綠）**：於期末側注入一字元（`類 III … 13 項` → `14 項`）
後重判 ⇒ **`False`** ✅。

⇒ 🔒 **收工閘 `8` 滿足**（「紅項經實測基座態證非本批所致」）；**停機款 `9` 未觸**
（其要件係「**由綠轉紅**且係本批所致」——本批**既未由綠轉紅、亦非其所致**）。

⚠️ **對帳 FAIL 之成因（記錄·⛔ 非本批之受詞）**：其一為
`[碎片] … 宗-宗重疊 = 45.9766 > 上界 3.1129（(宗數15−1)…` vs 現況 `上界 2.8906（(宗數14−1)…`
——**宗數 `15` → `14`**；而 CC 之 `S-5` **基座**量測（切換**前**）之 `[T2-DIAG]` 已載
`街廓 R2｜宗數 14` ⇒ **基座即為 `14`** ⇒ 該差係**凍存名單過期**（`GB-111`·**須 KL 決重產時點**），
**⛔ 非本批**。🛑 本批**⛔ 未更新凍存名單**（`wv_reconcile` 逐字：「⛔ 不得逕自更新名單」）。

### `§九-2`　四分分類器之「乙」判準：**識別符之對稱差** vs **聯集**

見 `§六-2`。CC 採**對稱差**（使判別力造成立且「乙」可達）。
🛑 **若發單側認過寬 ⇒ 改採聯集**（一行之改），惟屆時類乙近乎不可達，`21` 項將幾乎全歸甲。

### `§九-3`　收工閘 `5`（`[T3-GATE]` 期末仍為 `39` 列）**⛔ 未辦**之具名

🔒 **理由**：`[T3-GATE]` 之產生端係 `verify/probes/probe_WG9187_t3gate.py`，其情境為 **`SB = 0.0 m`**，
而本批之全部量測係 **`SB = 3.5 m`**（與【倉】`177R` 同情境）。二情境**⛔ 不可互代**。
🛑 且收工閘 `5` 自載「若不為 `39` ⇒ **逐宗具名其變動並呈裁**（⛔ 非自動紅——族②③ 會改幾何、本即可能改判）」
⇒ 其性質係**呈裁**、⛔ 非機械閘。
🛑 **CC 之擬辦**：本項與 `§九-1` 併辦——俟基座對照跑之判定後，若准 push，
則於**次一零生產碼 commit** 補跑 `[T3-GATE]`（`SB = 0.0 m`）並逐宗具名其變動。
⚠️ **⛔ 不於本報告臆造其值。**

### `§九-4`　`§四` 之「戒」框（`§二-1`）

單所載之框**要求 `🛑`** 而 `戒 16` 之定義列⛔ 無之 ⇒ 與同節敘述互斥。
CC 依 `常規二` 取保守項（`CLAUDE.md:826` 之正典框）續辦並逐字回報。**請發單側裁定正典框之唯一形。**

---

## `§十`　異動清單（**逐 commit 逐檔** `numstat`）

| commit | 檔 | `+` | `−` |
|---|---|---|---|
| **`d8d64b0`** `1`／`5` | `CLAUDE.md` | `90` | **`0`** |
| | `docs/reports/W-G.9-187R_幾何本體批第一批.md` | `2` | `2` |
| | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `124` | **`0`** |
| | `verify/probes/probe_WG9189_s6_replica.py` | `4` | **`0`** |
| | `verify/wf_f0.py` | `2` | `1` |
| **`69daed5`** `2`／`5` | `CLAUDE.md` | `48` | **`0`** |
| | `docs/orders/`（四單） | `179`＋`441`＋`195`＋`253` | **`0`** |
| | `verify/out/`（二 log）／`verify/probes/probe_order_preflight.py` | `19`＋`63`＋`254` | **`0`** |
| **`571b2c3`** `3`／`5` | **`app.py`** | `14` | **`4`** |
| | `verify/probes/probe_WG9190R_fam2.py` ＋ 二 log | `311`＋`50`＋`177` | **`0`** |
| **`c141b47`** `4`／`5` | **`app.py`** | `45` | **`4`** |
| | `verify/probes/probe_WG9190R_fam3.py` ＋ log | `216`＋`46` | **`0`** |
| **`aa52bc5`** `5`／`5` | `verify/probes/probe_WG9189_s6_replica.py` | `251` | **`0`** |
| | `verify/probes/probe_WG9190R_class4.py` ＋ 二 log | `50`＋`125`＋`177` | **`0`** |

🔒 **停機款 `11`（`deletions` 出現於<u>基座檔</u>）之辨析**：該款逐字定義基座檔 ＝
**`§十一-3` 禁改清單所列之檔**。清單就 `app.py` 所列者係其
**`minimum_rotated_rectangle` 全部 `11` 處**（⛔ 非全檔）——實測 **期初 `11` ／ 期末 `11`·未動**。
其餘 `deletions`：`W-G.9-187R`（`2`）與 `wf_f0.py`（`1`）係 `§八-2` **明令之就地更正**
（`§二-3` 逐字准之）；`app.py`（`4`＋`4`）係**族②③ 之受詞列本身 ＋ 因之成偽之註解列**。
⇒ **停機款 `11` 未觸**。
🔒 **`verify/stepg_pipeline.py` 全批 `0` 列異動**（`§十一-3` 禁改）。
🔒 `probe_WG9189_s6_replica.py` 二次異動**皆 `deletions = 0`**（末端追加／純插入·既有判定路徑逐位未動）。

---

## 🔧 `§九-1` 之就地加註：二數之**口徑**（`W-G.9-190R` 續辦·⛔ 上文原句一字不刪、一字不改）

🩸 **被加註之對象**（逐字·本檔 `:360` 與 `:362-363`）：
🔒 **引文之框（`戒 39` 之宣告）**：下列 fence 內係本檔 `:360`／`:362`／`:363` 之**原始位元組**，
⛔ 未經任何轉義（其自本檔逐列讀出後原樣寫入·⛔ 非手抄）。

```
🔑 **逐位對拍**：二 log **各 `516` 列**；`diff` 得 **`48` 列**差，**全部**為 clone 路徑
🔒 **機械證明（⛔ 非目視）**：僅將該路徑正規化為 `<CLONE>` 後，二者
**各 `32972` B、逐位相同 ＝ `True`**。
```

🔒 **結論（「逐位相同 ＝ `True`」）⛔ 未受影響——已由續辦窗獨立復現**（⛔ 非轉引本檔）。
惟其**二數之口徑**須加註：

| 原文之數 | 實際口徑 | 獨立復現值 | 判 |
|---|---|---|---|
| `diff` 得 **`48`** 列差 | ＝ **`diff` 之輸出列數**（⛔ 非「相異之來源列數」）。`12` 個**孤立**變更塊 × `4` 列（`NcN` ＋ `< 舊` ＋ `---` ＋ `> 新`）＝ `48` | **原始逐列相異 ＝ `12` 列**，其**全部**含 clone 路徑（`w190u` ↔ `w190t`） | ✅ 二數自洽·⛔ 非矛盾 |
| 各 **`32972`** **B** | 🔴 **⛔ 非位元組數**——實係 `len(''.join(splitlines()))`，即**去行尾之字元數**。`33488 − 516 = 32972`（`516` ＝ 列數） | 正規化後 **`33488` 字元**／**`46895` 位元組**（原始各 `46907` B） | 🔧 **標示不精確**（發單側 `2026-08-31` 放行裁：**⛔ 非錯**；「字元數」標作「B」係 **`GB-101` 族同形**） |

🛑 **通則（`CLAUDE.md:725` 之施行式）**：凡出艙「量」，須併載其**單位與口徑**——
「列數／字元數／位元組數」三者於 CJK 文本中**彼此差距達數倍**，混稱即令讀者無從復現。
🔒 爾後本波一律：位元組稱 **`B`**、字元稱 **字元**、列稱 **列**，且**去行尾與否須明載**。

🔒 **獨立復現之判別力對照**（證本加註之量⛔ 非恆綠）：於期末側注入一處（`類 III` → `類 XXX`）後重判 ⇒ **`False`** ✅。
