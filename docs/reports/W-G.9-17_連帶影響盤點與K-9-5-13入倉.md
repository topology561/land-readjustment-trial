# 【W-G.9-17 全單報告】`§三`／`§四` 失效之連帶影響盤點 ＋ `K-9-5-13` 入倉

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`1dca348`　**追加登記**：`docs/reports/W-G.9-17_追加登記.md`（commit `da3f751`）
**字樣錨（倉規 A·唯一命中）**：`【W-G.9-17 全單報告】`

> # ✅ **三個停機條件皆未觸發；`B6′` 破（合法）。**
>
> | # | 判 | 一句話 |
> |---|---|---|
> | **B3′** | ✅ | `§三` 之帶**實作於 `_build_corner_range_v3` 內**，其用途即服務 `§四` 之滑動；⛔ 查無其他運算用途 ⇒ **`G-1` 未觸發** |
> | **B5′** | ✅ | 🔴 **真正之遠側界<u>已經被建出來了</u>**——只是在無 WINNER 時被**降維成面積** ⇒ **`G-3` 未觸發** |
> | **B6′** | 🔴 **破** | 「保留區之表示法」該**措辭**於 `GB` 登記表 **0 行**；實質機制在，但該前置**須重新定式** |
> | **B7′** | ✅ | `k97_solve_alloc_t` **早已是定義而零呼叫之孤兒** ⇒ `§四` 失效**不改變其狀態** |
> | **B4′** | 🔴 **破** | `§五` 面積改變之量級**估不出**——估它須**實作新構造**（本批未授權）⇒ 具名列為未估 |
> | **B8′** | ✅ | 本批未觸及 `run_verification` 之任何輸入 ⇒ 判定層在構造上不可能變動 |
>
> 🛑 **另有一項未決具名待裁**（`K-9-5-13` §五）：`K-9-5-11` 之 `S1` 係「`P0` 到 `Ps` 之線段長」，
> 而裁定五之**平移量**須沿何方向量、與該線段長**是否同一個數**，**倉內未載**。

---

## §1 `K-9-5-13` 入倉（⛔ 三段分層·⛔ 用「失效」）

**位置**：`docs/rulings/K-6_街角地分配程序與可分配判準.md`
**字樣錨**：`grep -c "^### 🔒 K-9-5-13 "` ⇒ **1**（新編號以**定義式**現查·驗收 #11）

### 1-1 🔴 新驗收 **C**：兩件事**分段寫**

| 段 | 內容 | 狀態 |
|---|---|---|
| **（三）新裁之效力** | 遠側界 ＝ SIDELINE 平移 `S1`／起算用真正地界／落位側跟著改／`§三`、`§四` 失效 | 🔒 **已定案** |
| **（四）舊節之出處** | 倉內查無 KL 逐字記載；KL 答「記不清」 | 🔴 **無法確認** |

🔒 逐字採施工單第 3 版 §1 之性質敘述：
**⛔ 不記為「KL 變更自己之裁示」，亦⛔ 不記為「移除從未經裁示之作法」。**
⛔ 合併寫會使後人誤以為**新裁亦不確定**。

### 1-2 🔴 新驗收 **B**：四處加註一律用「**失效**」

⛔ 未用「撤回」「變更」「更正」——**後三者皆隱含出處已定**，而出處正是無法確認者。

| 位置 | 加註 | 措辭 |
|---|---|---|
| `K-9-5-6` 之 `forced` 半句 | 依**裁定二**失效 | 失效 ✅ |
| `### §四 ALLOC_LINE 定位` | 依**裁定四、五**失效 ＋ 出處無法確認 | 失效 ✅ |
| `### §三 最小寬度帶` | 依**裁定六**失效（**附範圍條件**） | 失效 ✅ |
| `K-9-5-12` | 指路至新則 | 指路 ✅ |

**原句一字未刪**：`numstat` 對該檔 ＝ `96  0`（**刪除欄 0**）。

### 1-3 🔒 `§三` 失效之**範圍條件**（驗收 #3）

> **僅就其服務於 `§四` 滑動求解之用途而失效。**
> ⚠️ **⛔ 與 `_n14_band` 不同**：`§三` 為**街廓層**（`D_avg` 平移）；
> `_n14_band_geom` 為 **`K-9-6-b` 宗地層**（逐宗後緣角點）⇒ **後者不在失效範圍內**。
> 🔴 若日後查出本帶另有用途，該用途**不在**本次失效範圍，須具名待裁。

---

## §2 乙組：連帶影響盤點（⛔ 只盤·不改·不估·不提修法）

**產物**：`verify/out/probe_WG917_recon.log`（母體**釘於 commit `da3f751`**·驗收 #9）
🔒 **凡計數皆附母體與判準**（新驗收 A·承本批 `3043f56` 之 `2` vs `96` 混寫自誤）。

| # | 待查 | 回報 | 依據 |
|---|---|---|---|
| **①** | `§三` 除 `§四` 外有無他用 | **無** | 字樣**語意窮舉 5 種**；`app.py` 含「寬度帶」之 **3 行皆為註解／字串**；其帶之實作即 `_build_corner_range_v3` 之 `block_depth = round(D_avg,2)` 參數（docstring 逐字複製 `§三`／`§四`） |
| **②** | `§五` 範圍多邊形之面積改變量級 | 🔴 **無法判定** | 估它**須實作新構造**（本批⛔ 未授權）⇒ `B4′` 破·具名列為未估。**可界定之方式**：舊界 ∥ALLOC、新界 ∥SIDELINE ⇒ 量級由**二者夾角**決定；⛔ 數值須待授權後實測 |
| **③** | `K-9-5-11` 之 `S1` 三分口徑是否仍成立 | ⚠️ **部分·一項未決** | 三分口徑（＝／`>=`／強制抵費地）**定義的是「寬度值」**，與界線方向無關 ⇒ **仍成立**。🛑 **未決**：其 `S1` 係「`P0` 到 `Ps` 之線段長」，而**平移量沿何方向量**、與該線段長**是否同一個數**，**倉內未載** ⇒ 具名待裁 |
| **④** | `GB-19`／`GB-36`／`GB-14` 是否整組失去受詞 | ⚠️ **無法判定** | 三者於 `GB` 登記表之自述**未逐條載明其受詞是否即 `§三`**；⛔ 本批**未逐條追**（屬後批）。⚠️ ⛔ 不得因「都跟帶有關」即整組判為失去受詞 |
| **⑤** | `_build_corner_range_v3` 之全部消費者 | **`app.py` 呼叫行 2**／`stepg` **0** | 母體＝`app.py` 全 **22,049** 行；判準＝含 `_build_corner_range_v3(` 且**非註解、非 `def`** |
| **⑥** | `k97_solve_alloc_t` 是否隨 `§四` 失效 | **否**（早已孤兒） | `app.py` 命中 **7** 行 ＝ **定義 1／呼叫 0／註解 4／其餘 2**；`stepg` **0**。碼註自載「舊式…於本函式整段移除·不留 fallback」「本身不刪、不停用、不標作廢」⇒ **`§四` 失效不改變其狀態** ⇒ `B7′` **可由構造判定** |

---

## §3 丙組：真正地界之可取得性（`B5′`／`B6′`）

### 3-1 🔴 **真正之遠側界已經被建出來了**

`GB-58` 逐字所載：

> 街角最小規定範圍由 `_build_corner_range_v3` 依 `K-9-5-4` 以 **∥SIDELINE 平移 `S1_par`**
> 建成**多邊形**；但**無 WINNER** 時該多邊形被**丟棄、只取其面積**
> （`_left_min = round(float(_rng_cr.area), 2)`），交由 `_corner_buffer_S` 以 **∥ALLOC** 另建一帶。

**現查**：`_rng_cr.area` 之**非註解行 ＝ 2**（母體＝`app.py` 全 22,049 行）。

⇒ 🔒 **`B5′` 成立**：真正之遠側界**不必從被丟棄的多邊形取回**——它**本來就在** `_build_corner_range_v3`
之輸出多邊形裡，只是被上開兩行**降維成一個面積**。
⇒ **`SIDELINE ＋ S1` 之路亦可行**（`S1_par` 即該平移量）⇒ **`G-3` 未觸發**。

### 3-2 🔴 `B6′` **破**：「保留區之表示法」該措辭**倉內查無**

**三項射程**（否定性宣稱·⛔ 缺一不可）：

- **【目錄射程】** `docs/reports/W-G.4_泛用阻塞項登記表.md`（`GB` 之唯一登記處）＋ `app.py`。
- **【字樣射程】** **語意窮舉 6 式**：`GB-58`／`保留區`／`表示法`／`∥SIDELINE`／`s 區間`／`S1_par`。
- **【連接方式】** 各式**獨立裸計數**（⛔ 無距離窗、⛔ 無上下文窗·考古 67 修法 ③）。

| 字樣 | `GB` 登記表 | `app.py` |
|---|---|---|
| `GB-58` | 有 | 0 |
| **`保留區`** | **0** | 1 |
| **`表示法`** | **0** | 0 |
| `s 區間` | **0** | 12 |

🔒 **判別力自檢（驗收 #12）**：`GB-46` 於同一母體 ＝ **3 行**（>0）
⇒ 上列各 `0` **非因母體壞掉**——**該措辭是真的不在**。

⇒ **`GB-58` 之前置<u>不是</u>以「保留區之表示法」之措辭記載的。**
**實質機制在**（多邊形降維成面積、再以 ∥ALLOC 另建一帶），
⛔ 但該前置**須重新定式**；⛔ 本批**不替其定式**。

---

## §4 丁組：五族消費者觸及表（⛔ 只標·不改）

母體 ＝ `GB-47` 所列 `_corner_buffer_S` 之**五族土地後果消費者**。

| 族 | 定位字樣 | 本批處置 |
|---|---|---|
| ① 落位 s 累積 | `left_cum_S = float(_left_buffer_S)` | ⛔ **未改** |
| ② W 鏈種子 | `_W_prev_left = (_left_buffer_S` | ⛔ **未改** |
| ③ 選槽 `b` 參數 | `'b': _left_buffer_S` | ⛔ **未改** |
| ④ `W₀` 基準 | `_mp_base_W0(corner_pt, _left_buffer_S` | ⛔ **未改** |
| ⑤ 診斷顯示 | `_left_buffer_S` | ⛔ **未改** |

⚠️ **裁定二（「替身線在側街負擔上不再有角色」）之射程涵蓋 ②③④**
——但**本批只標，⛔ 未改任何一族、⛔ 未提修法**。

---

## §5 出艙之數（⛔ 皆取自 commit `3b68954` 之 log·**⛔ 未重跑**·驗收 #10）

```
verify/out/probe_WG917_recon.log（commit 3b68954·母體釘於 da3f751）
  乙①  「寬度帶」app.py 3 行（皆註解／字串）｜語意窮舉 5 種
  乙⑤  _build_corner_range_v3 呼叫行：app.py 2／stepg 0（母體 22,049 行）
  乙⑥  k97_solve_alloc_t：定義 1／呼叫 0／註解 4／其餘 2
  丙    _rng_cr.area 非註解行 2｜「保留區」GB 0 行｜判別力自檢 GB-46 = 3 行

verify/out/probe_WG917_B1.log（commit 3043f56·母體釘於 053705f）
  §三 引文行 0｜§四 引文行 0｜全檔在 KL 逐字區塊內之行 96

$ grep -c "^### 🔒 K-9-5-13 " docs/rulings/K-6_…md            ⇒ 1
$ git diff --numstat -- docs/rulings/K-6_…md                  ⇒ 96  0（刪除欄 0）
$ git diff --numstat da3f751 -- app.py verify/stepg_pipeline.py \
      verify/run_verification.py verify/wv_reconcile.py verify/baselines/ CLAUDE.md
  ⇒ （空輸出）
🔒 判別力自檢：同式對 `docs/rulings/` ⇒ 非空 ⇒ 上式非恆空
```

⚠️ **`B8′` 之依據**：本批未觸及 `run_verification` 之任何輸入（上式空輸出為證），
`run_all` 之夾具清單未改 ⇒ **判定層在構造上不可能變動**。
**具名聲明：本批⛔ 未跑長跑**，理由即此構造論證；⛔ 不以「跑了就綠」代替論證。

---

## §6 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **零生產碼**：`app.py`（含 `_build_corner_range_v3`、`k97_solve_alloc_t`）／`verify/stepg_pipeline.py` **零觸**。
2. ⛔ **未估任何面積**（含 `§五` 之量級 ⇒ `B4′` 破·具名）。
3. ⛔ **未改落位**、⛔ 未改**任何一族**消費者、⛔ **未實作新構造**。
4. ⛔ **未實作 `GB-58`**、⛔ 未解「保留區換表示法」、⛔ **未替該前置重新定式**。
5. ⛔ **未刪 `docs/rulings/` 任何一字**（刪除欄 0）；⛔ 未改任何既有編號。
6. ⛔ **未逐條追 `GB-19`／`GB-36`／`GB-14` 之受詞**（乙④ ⇒ 無法判定·屬後批）。
7. ⛔ **未追那 57 條未標記條目之出處**（屬後批）。
8. ⛔ 未動 `CLAUDE.md`、`run_verification.py`、`wv_reconcile.py`、`baselines/`、既有夾具、三支複本。
9. ⛔ **未跑長跑**（§5 已具名聲明理由）。

---

## §7 下一批（**估算**）之前置條件清單（⛔ 只列前置·不設計實作）

| # | 前置 | 理由 |
|---|---|---|
| **1** | 🛑 **`S1` 之平移量與「`P0`→`Ps` 線段長」是否同一個數**（`K-9-5-13` §五之未決） | ⛔ 未定即**平移不出來**，估算無從起 |
| **2** | **動碼重跑之授權** | 估算**必須**實作新構造並跑修正後之引擎；本批與前三批皆零生產碼 |
| **3** | **`§五` 面積改變量級之估算授權**（`B4′` 破之補救） | 它決定「受影響的不只側街負擔」有多大 |
| **4** | **`GB-58` 前置之重新定式**（`B6′` 破） | 該前置之措辭倉內查無 ⇒ 後批若引用即為**引用不存在之依據** |
| **5** | **乙④ 三項（`GB-19`／`GB-36`／`GB-14`）之逐條受詞追查** | ⛔ 不得因「都跟帶有關」即整組判為失去受詞 |
| **6** | **殘差 `0.02`／`0.05` 百分點之驗收線已定** | 屬驗收線之訂定，⛔ CC 不自裁 |
| **7** | **重烤與凍存之時點已協調** | 構造一改，`run_step_g` 將跑得更遠 ⇒ 三支端到端複本依**設計**轉紅 |

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。

---

## §8 commit 鏈

- **A** `053705f` — 事前登記／**B** `3043f56` — `B1` 證據／**C** `ca783f7` — `G-4` 停機上呈
- **A′** `da3f751` — 追加登記／**B′** `3b68954` — 乙丙丁盤點／**C′** — 本報告 ＋ `K-9-5-13` ＋ 四處加註 ＋ 上呈文件加註
