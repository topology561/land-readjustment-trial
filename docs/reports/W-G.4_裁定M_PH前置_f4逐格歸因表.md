# W-G.4 裁定 M · P-H **前置**——`wf/f4` 12 檔逐格歸因表

> ⛔ **本檔不執行任何重烤**（KL 交辦丙）。理由＝重錨登記 §五 鐵則 3「**重烤不是理解的替代品**」，
> 且 KL 須於烤前目視 D-2。做完 push 後**停機**等 claude.ai 複驗與 KL 看圖。
> 基準 `e45bbb2`＋甲批。比對之 got 側＝ `WV_BAKE` 側錄（`M_0725_PF_bake_diag.log` 該趟·69 檔）。
> **計數口徑＝harness 自身 `run_verification.diff_rows`**（非自造計數·同 `run_all` 判準）。

---

## 〇、違規計數（**與重錨登記 R-9 逐項全等**）

| 檔（`退縮0m`） | 違規 | 缺列 | 多列 | 值不符 | R-9 登記 |
|---|---|---|---|---|---|
| `F.4_G值` | **293** | 2 | 1 | 290 | 293 ✅ |
| `F.4_整形` | **70** | 8 | 0 | 62 | 70 ✅ |
| `F.4_池流向` | **18** | 0 | 0 | 18 | 18 ✅ |
| `F.4_總決算` | **9** | 0 | 0 | 9 | 9 ✅ |
| `F.4_七五雙出口` | **15** | 1 | 0 | 14 | 15 ✅ |
| `F.4_公設調配` | **34** | 1 | 1 | 32 | 34 ✅ |
| **0m 合計** | **439** | | | | |

| 檔（`退縮3.5m`） | 違規 | 缺列 | 多列 | 值不符 |
|---|---|---|---|---|
| `F.4_G值` | 300 | 7 | 5 | 288 |
| `F.4_整形` | 65 | 0 | 2 | 63 |
| `F.4_池流向` | 18 | 0 | 0 | 18 |
| `F.4_總決算` | 10 | 0 | 0 | 10 |
| `F.4_七五雙出口` | 34 | 1 | 0 | 33 |
| `F.4_公設調配` | 55 | 7 | 5 | 43 |
| **3.5m 合計** | **482** | | | |

> 🚩 **交辦文與倉態之數字歧異（不自行調和）**：KL 交辦丙 (a) 載「`F.4_公設調配_退縮0m`（**83 格**）」。
> 倉內 **R-9 登記為 34**，本次以 harness `diff_rows` 實測亦為 **34**（`缺列1／多列1／值不符32`）。
> 另以「純儲存格差」計亦僅 32；含缺列/多列之全欄展開為 32+2×15＝62。**83 於倉內查無出處**。
> 依「數字錨引倉檔」，本表一律採 **34**；83 之出處請 KL／claude.ai 指出後再併。

---

## 一、0m 六檔·逐項差異與歸因

**歸因碼**：`S1-W`＝W 脫鉤(07-19)／`S1-S0d`＝S0d 實切回未捨入 S_conv(07-20)／`S1-率`＝比率更新／
`S1-A4`＝R3 0m 碎片併入池主體（P-0c 錨 A-4）／`連動`＝上列之下游傳遞／
`M-5`＝M-5 重排序／`M-1`＝M-1 搬移／`Q1`＝Q1 口徑。

### 1.1 `F.4_G值_退縮0m`（54→53 資料列·293 違規）

| 差異群 | 內容 | 歸因 | 倉內證據 |
|---|---|---|---|
| **缺列 2／多列 1** | `R3-抵費地-1`(436.14)＋`R3-抵費地-2`(**78.24**) → 併為單一 `R3-抵費地`(**517.35**) | **S1-A4** | P-0c 歸因表 A-4；`run_verification` `_exp_m = 1`（`grep -n "_exp_m = 1" verify/run_verification.py`） |
| `推進側別` ×10 | `right → left`，**全為 `74·G*@*` 合成宗** | **S1-W** | 同族於 P-0c A-1 |
| `F(m)` ×9／`W(m)`(部分)／`l₁ 側面尺度` ×9 | `33.27 → 0.0`／`1.0 → 0.0`（同一組合成宗） | **S1-W**（合成宗不再帶側街負擔） | 同上 |
| `Rw(%)` ×19 | 如 `74·G020@R1 13.59 → 0.0`；`628(5) 83.72 → 71.86` | **S1-W** | 同上 |
| `S(m)` ×28／`累積S(m)` ×33／`宗地寬度(m)` ×28／`幾何面積(㎡)` ×49 | 微移（如 `628-37(1) S 8.43→8.45`） | **S1-S0d** | P-0c A-1（S 8.21→8.15）／A-2（k* 因 S0d 移） |
| `負擔比率` ×26 | 如 `0.3975 → 0.3791` | **S1-率** | P-0c §二「率鋪底（既往）」 |
| `G(㎡)` ×31／`a 面積(㎡)` ×7 | 上列之函數 | **連動** | — |
| `迭代次數` ×31 | 二分收斂步數隨輸入變 | **連動** | — |

### 1.2 `F.4_整形_退縮0m`（**23→15 列·少 8**·70 違規）— KL 指名項

**缺 8 列＝全部 R3**：`628-45(2)`／`628-42(2)`／`628-30(3)`／`628-31(3)`／`628-34(2)`／
`628-48(1)`／`628-32(3)`／`74·G025@R3`。

**歸因＝`S1-A4`（單一成因·非多因）**：
`wf_f4` 之 E3 僅對「恰有 1 片**碎片**」之塊呼叫 `_reshape_block`（`grep -n "S=0 碎片" verify/wf_f4.py`）。
S1 後 R3 0m 之 78.24 碎片**已併入池主體**（寬 40.36·畸零旗標全空）⇒ **R3 無碎片 ⇒ E3 跳過 R3**
⇒ 該 8 列整形項自然消失。此非「整形失效」，而是**上游已無待整形之碎片**。

**已錨證據（非本次新發現）**：`run_verification` P-0c 註解逐字載
「S1 後＝單一池主體 1795.17（寬 40.36·畸零旗標全空）·碎片 78.24 併入·不再標記」，
並已把 `F.1 標記制0m` 之期望由 **2 改 1**；本次實跑該閘 **PASS（1 片·期 1）**。

其餘 62 值不符（`G_E`／`S_E`／`S_new`／`宗地寬度_new`／`新形面積`／`Δ面積`）＝**連動**；
`角色` ×4（`對側·未動 → 前移`·全 R1 之 `74·G*`）＝**S1-W** 之側別翻轉所致（右→左後改由標的側推進）。

### 1.3 `F.4_公設調配_退縮0m`（17→17 列·34 違規）

| 差異 | 內容 | 歸因 |
|---|---|---|
| 缺列 1／多列 1 | `74·G031@R1` 由 `E2·7-5` **改走** `E1·7-4` | **連動**（見下） |
| 值不符 32 | `a″引擎`／`a源`／`模式二a′`／`起迄距`／`配額G` | **連動** |

**`G031` 改道之機制鏈（逐環有據·非臆測）**：`S1-W` 使 `74·G031@R1` 之 `W/Rw/F/l₁` 歸零、
`S1-S0d` 使各塊幾何微移 ⇒ **池容量改變**（`F.4_池流向_退縮0m`：R3 池_D `905.75→908.64`、
R1 `897.47→896.65`）⇒ E1(7-4) 之可規配性改變 ⇒ G031 於 E1 即可安置、不再落入 E2(7-5)。
**佐證**：`F.4_七五雙出口_退縮0m` 之 `G031` 列同步消失（缺列 1）。

### 1.4 `F.4_七五雙出口_退縮0m`（10→9 列·15 違規）
缺列 1＝`G031`（同 1.3）。值不符 14（`G(a′)輪0`／`增配面積`／`差額地價§52-1`／
`放棄改領§53-2`／`配額G`）＝**連動**。

### 1.5 `F.4_池流向_退縮0m`（6→6 列·18 違規）
三欄（`池_D`／`池_E3整形後`／`池_E引擎`）× 六塊。R3 `514.37→517.35`（＝1.1 之碎片併入）＝**S1-A4**；
其餘五塊微移＝**S1-S0d／S1-率 之連動**。

### 1.6 `F.4_總決算_退縮0m`（33→33 列·9 違規）
全部落在 `實走鏈` 單欄——係**文字複述**上游 G 值（如 `G012 … R2 G=156.21 → 157.80`）＝**純連動**。

---

## 二、(b) 判定：**既定引擎變更·非退化**

| 判準 | 結果 |
|---|---|
| 是否有**無法歸因**之格？ | **無**。0m 439 違規全數落入 `S1-W`／`S1-S0d`／`S1-率`／`S1-A4`／`連動` 五類 |
| 是否有 **M-5／M-1／Q1** 之痕跡？ | **無**。0m 無 forced 端 ⇒ `_fo_ends` 空 ⇒ M-5 零 award（BK-4 證據(1)）；本次 `M-5 三列0m` 實跑亦為「forced 端 0 個」 |
| 是否為**退化**？ | **否**。四項成因皆為 **07-19～07-20 已收官之 S1 變更**，且 `F.1 標記制0m` 期望值已於 P-0c 同步改錨（2→1）並**現跑 PASS**；f4 baseline 未同步僅因當年 **F.4 raise 產不出可烤輸出**（＝BK-4 之覆蓋漏洞） |
| 守恆是否仍成立？ | `F.4 結構永久閘`／`池終態三則` 於 BAKE 趟為結構性綠——**該趟不可採信**（BAKE 不比對）。⇒ **列為 P-H 執行時之必驗項**，非本表結論 |

---

## 三、(c) 停機判定

| 條款 | 判定 |
|---|---|
| **S-6**（任一格無法歸因 ⇒ 停機不重烤） | **不觸發**——見 §二第一列 |
| **鐵則 4**（屬末端幾何／整形之域問題 ⇒ 停機上呈 KL） | **觸發·惟係既有旗標之延續、非新旗** |

**🚩 域旗標（＝ P-0c 歸因表 §五之同一旗·仍開啟）**：
「**R3 0m 該不該有一片待 F.4 之碎片**」——亦即 R3 0m 終態幾何是否應含 78.24 碎片。
- 本表 §1.2 之「整形少 8 列」**完全繫於此旗**：旗若判「應含」，則 8 列不該消失、f4 0m 不可照現態烤。
- reviewer 已獨立證：此係引擎 **genuinely 產出**、byte-reproducible、**非 cover-up 非 regression**。
- **CC 不拍板**（域判斷層·CC 與 reviewer 同模型·共盲區）。**本批未問 KL**（依交辦丁「本批不問」），
  但依鐵則 4 於此**明列**：⛔ **P-H 執行重烤前，此旗須有 KL 明確裁示**。

---

## 四、(d) 3.5m 六檔：**併列兩源·不得擇一**（BK-4 裁定）

3.5m 合計 **482 違規**。**構造上不可分離**——pre-M-5 之 F.4 3.5m **從未產出**（E2「上界 7 < 需求 9」
死在前面）⇒ **無 comparand**，無從切出「多少屬 S1、多少屬 M-5」。

可辨識之**兩源指紋**（併列·不作比例分攤）：

| 指紋 | 內容 | 源 |
|---|---|---|
| `R3-抵費地-1`＋`-2` → `R3-抵費地` | 與 0m **同一現象** | **S1**（0m 亦有 ⇒ 必非 M-5） |
| `E0級1 628-45(2)` 列消失 | M-5 winner 已於 PK 前取得 a′ ⇒ 不再走 E0 級1 | **M-5** |
| `街角地`／`第1筆街角`／`街角側別` 欄變動 | R3 右 corner winner 由抵費地→地主宗 | **M-5** |
| E2 批次指派重排（`74·G033@R2→@R4`／`74·G020@R4→@R1`／`74·G004@R3→@R4`／`74·G015@R4→@R1`） | 容量改變後之全域最佳化重解 | **S1＋M-5 混**（兩者皆改容量） |
| `74·G031@R1` E2→E1 | 與 0m **同一現象** | **S1**（0m 亦有） |
| `推進側別`／`F`／`W`／`l₁`／`Rw` 歸零 | 與 0m 同族 | **S1-W** |

⇒ 歸因表於 P-H 執行時，3.5m 六檔一律標 **`S1 ＋ M-5（不可分離）`**，禁擇一。

---

## 五、次步（**本批到此為止**）

1. 本檔 push 後 **停機**，等 claude.ai 複驗與 KL 看圖（D-2）。
2. P-H 執行之**前置硬條件**：§三 之 R3 0m 域旗標須有 KL 裁示。
3. P-H 執行時另須現場複驗（BAKE 趟不可採信者）：`F.4 結構永久閘`／`池終態三則`／守恆殘差。
4. 重錨登記 R-1~R-9 之逐列消化仍為 P-H 之交付物（§三閘），本表**不代替**之。
