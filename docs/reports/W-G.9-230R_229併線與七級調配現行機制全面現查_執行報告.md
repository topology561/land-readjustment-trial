# W-G.9-230R　`W-G.9-229` 併線 ＋ 七級調配現行機制之全面現查（執行報告）

**批次性質** ＝ 一次**合併**（KL 逐字放行）＋ 零生產碼之**現查**。
**合併前主線** ＝ `2b78a6ae24742b32a393f485499f0de4671df06a`；**合併後主線** ＝ `20b04ac8caa137108b24ecc0d27253076f43adcb`。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）。
🛑 **本報告只產生事實**——依單逐字「⛔ 就七級調配提名、建議修正、評價現行機制之優劣」「⛔ 出任何裁定題」。

---

## 工項零　✅ PASS

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **6741** | 99 | **0** | `5f7a8e9c3a5d4888b6a1dec3aaa1dbc75b07cbbb3de853e3921d060e7cc42375` |
| 入倉（索引側 blob） | **6741** | 99 | **0** | `5f7a8e9c3a5d4888b6a1dec3aaa1dbc75b07cbbb3de853e3921d060e7cc42375` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）。對照組：翻首位元組 ⧧ blob ＝ `True`；blob 自比 ＝ `True`。

---

## 工項一　合併 ✅ PASS（①–⑤）

方法 ＝ **純快轉**（依 `W-G.9-227` 工項一 `a` 所證之既例·⛔ merge commit）：
`git merge --ff-only origin/verify/W-G.9-229-c1` ⇒ 輸出逐字 `Updating 2b78a6a..20b04ac` ／ `Fast-forward`；
繼以 `git push origin HEAD:refs/heads/wip/s1-endpart`（`2b78a6a..20b04ac`）。

| 項 | 實測 | 判 |
|---|---|---|
| ① `app.py` blob | 主線 `02cc6d7313789149ba852f997d2d8051a6cefaa0` ＝ 側分支**同值**；內容逐位相同 | ✅ |
| ① 對照組（器須紅）| 合併**前**主線之 blob ＝ `5d45de2013216cedaaa8c65bede2b77b785245e0` ⇒ **相異** | ✅ |
| ② 其餘四檔 `sha256` ＝ 合併前值 | `True` | ✅ |
| ③ 合併未引入 deletion | 合併 **487** 增／**1** 刪 ＝ 側分支自身 **487** 增／**1** 刪 ⇒ **合併新增之 deletion ＝ 0** | ✅ |
| ④ 無 merge commit | 合併後主線頭之**父數 ＝ 1**（父 ＝ 合併前主線）；`2b78a6a..HEAD` 之 merge commit 數 ＝ **0** | ✅ |
| ⑤ `left-right` | **`0 0`** | ✅ |

---

## 工項二　七級調配之**正典側**現查

### a　正典錨與各級之逐字

**正典錨** ＝ `docs/配地計算總規格_v3.md` **§七**（`:354`–`:451`）。其節首逐字：

| 檔·行 | 逐字 |
|---|---|
| `:354` | `## 七、調配池定義 + 三調配順位（v3 核心重寫）` |
| `:364` | `### 7-0. 調配池定義（取代舊「抵費地池」一詞）` |
| `:376` | `### 7-0a. 前置地基檢查（最先跑，調配前）` |
| `:384` | `### 7-1. 第 1 調配順位：可建築街廓內未達分配面積者` |
| `:401` | `### 7-2. 第 2 調配順位：RD 道路用地（手冊 192-193 頁）` |
| `:414` | `### 7-3. 第 3 調配順位：共同負擔公設地（手冊 194-195 頁）` |
| `:420` | `### 7-4. 三類需調配土地的歸戶合併調配（無同歸戶 3 級調配）` |
| `:453` | `### 7-5. 1/2 門檻雙路徑（辦法 §31-1-2、§52、§53；2026-07-08 法規總對點修正版）` |
| `:478` | `### 7-6. 選填介面（正式作業用；試分配顯示預設值）` |
| `:486` | `### 7-8. 法源缺口記錄（法規總對點 2026-07-08；辦法 111.09.01）` |

🔒 **`7-1` 之「同歸戶 4 級合併分配」表（`:388`–`:394`·逐字）**

| 級 | 內容（逐字） |
|---|---|
| 級 0 | `同街廓同歸戶：小塊往**已達標**的最大塊集中合併` |
| 級 0' | `**弱弱聯合**（手冊宗地分配原則(三)）：同街廓同歸戶**皆不達標**者，全部往其中「應分配面積較大者」（實作可用 G 值最大者）合併；合併後 G(Σa) 達標 → 以該最大塊的投影位次參與原位次分配。**不得合併分配於公設地及依法不能建築之土地**` |
| 級 1 | `相鄰街廓同歸戶合併` |
| 級 2 | `鄰近街廓同歸戶合併` |
| 級 3 | `非鄰近街廓同歸戶合併` |

🔒 **`7-4` 之「無同歸戶 3 級調配」（`:429`·逐字）**：
`**3 級順序**：相鄰 → 鄰近 → 非鄰近（自暫定錨點起算距離），且遵守`
`「最近優先；最近街廓調配池不夠或會使該地不達最小建築面積 → 次近或門檻較低街廓」。`

🔒 **另一正典側載體**：`docs/W-D.4_域裁鎖定.md:8`–`:31`「〇、七級 ↔ 四梯對照表（術語永久釘定）」，
其 `:10` 逐字：「v3 第七節『七級』＝**執行動作集**（怎麼把地挪進去）；本波『四梯』＝**法定資格分流**…
一戶先過四梯定路徑，再由七級之一執行。**不重複、不打架。**」

### b　各級之**排序準據**（逐級具名）

| 級 | 排序準據（逐字之依據）| 檔·行 |
|---|---|---|
| 級 0 | **同歸戶** ∧ 「往**已達標**的**最大塊**集中」 | `v3:390` |
| 級 0' | **同歸戶** ∧ 「往『**應分配面積較大者**』（實作可用 **G 值最大者**）」 | `v3:391` |
| 級 1／2／3 | **同歸戶** ∧ **相鄰／鄰近／非鄰近**（距離之離散化）| `v3:392`–`:394` |
| 7-2（RD）| **同歸戶** ∧ CENTERLINE 兩側 ∧ 「一側未達最小分配面積 → 向另一側集中」 | `v3:405`–`:407` |
| 7-3（公設地）| **同歸戶** ∧ 「相鄰住宅區有同歸戶他筆土地 → 整筆合併」 | `v3:416` |
| **7-4**（無同歸戶 3 級調配）| 🔴 **距離**（「自暫定錨點起算距離」·「**最近優先**」）；**多歸戶搶同一最近街廓時 ⇒ 重劃前總地價高者先調配** | `v3:429`–`:430`／`v3:427` |

🔒 **⇒ 「以距離為優先順序」者 ＝ `7-4` 之 3 級調配**（逐字 `v3:429`）。

### c　「距離」之受詞（正典側）

🔒 **暫定位置錨點**（`v3:425` 逐字）：`- **暫定位置錨點** = 重劃前面積相對最大的那筆土地位置（供 3 級調配算距離）`
🔒 **起算**（`v3:429` 逐字）：`（自暫定錨點起算距離）`

| 受詞 | 正典側 |
|---|---|
| 誰到誰 | **暫定錨點** →（目標）**街廓**（`v3:425`／`:429`）|
| 何座標系 | **【查無】** |
| 帶號否 | **【查無】** |
| 單位 | **【查無】**（`v3` 全節未載距離之單位）|
| 容差及其來源 | **【查無】**——`v3:438` 之 `**收斂門檻 0.05㎡**` 係**面積**之收斂門檻、⛔ 距離之容差 |

🔒 **碼側之對應受詞見工項三**（正典側【查無】者，碼側有實作值）。

### d　各級之**終局 tie-break**

**【查無】**——正典側**無**「走完該級仍多候選時」之離散 tie-break 明文。
🔒 其位置由 `7-4` 之**比例分配**條款佔（`v3:436`–`:438` 逐字）：
`**同級多歸戶比例分配**：分母綁 level、逐層消耗 capacity、每層分母獨立；`
`灌入上限 min(G'(g), cap_share)；…**收斂門檻 0.05㎡**。同級皆無同歸戶（戊己情境）→ 平均分配、多輪平均。`
🔒 **對照（他受詞·⛔ 七級）**：全倉「tie-break」**28** 列／「tiebreak」**17** 列／「平手」**31** 列，
經核皆屬**街角 PK**（`SKILL.md:31`「總分同分（4dp）→ 重劃前原位次小者勝」）或 **W-D 選槽**
（`W-D_細部plan.md:78`／`:112`）等**他受詞**；`K-6:103` 之「兩側指數並列 ⇒ 取**前緣線 p1 側**（決定性，**禁隨機**）」
亦屬**街角歸側**、⛔ 七級。

### e　輸入母體之定義與其上游

🔒 **`docs/rulings/K-6_街角地分配程序與可分配判準.md:105`–`:109`（逐字）**

```
## 三、與七級調配之分界
- 進入七級調配者 ＝ 街角定案並辦原投影分配後，**未通過寬深雙檢** 或 **未達街廓最小
  建築面積**（若有輸入）
- F.0 級0/0′ 判「達標宗」之門檻 ＝ **寬深雙檢**（**不是** `region_min`）
- 跨街廓同歸戶「小往較大集中」一律歸七級調配（F.0／F.2）；**街角不再有專屬救援機制**
```

🔒 **各順位自身之轉出條件（＝下一順位之輸入母體·逐字）**
- `v3:399`：`4 級走完仍無可合併者 → 定義為「**無法分配亦無同歸戶的 R**」，進七-4。`
- `v3:409`–`:411`：`觸發轉出條件：「兩側街廓原有土地分配後已無空地（= 調配池已被第 1 順位用完）可供分配道路用地，或兩側本無土地可供合併」→ 定義為「**無法分配亦無同歸戶的 RD**」，進七-4。`
- `v3:417`–`:418`：`但若**整筆合併會超過該街廓剩餘調配池** → 不能硬併，轉出定義為「**無法分配亦無同歸戶的 PF**」，進七-4`
- `v3:422`：`**前置**：將同一歸戶的 R + RD + PF 全部加總`

🔒 **上游（由哪些機制決定）**：`K-6:106` 之**寬深雙檢**與**街廓最小建築面積**；
`K-9-11 二`（`K-6:2342`–`:2344`）之「該宗不配地 ⇒ 由投影序號中下一位遞補」。

### f　`K-9-11 三`／`R2`／`N0-2`〜`N0-5` 之逐字現況與其對七級之拘束

🔒 **`K-9-11 三`（`K-6:2346`–`:2349`·逐字）**
```
**三、遞補之機制⛔ 不另訂**：逐字之「由下依序號土地遞補」與 **`K-9-9 四`** 同構
（錨 `grep -n "遞補該位次" docs/rulings/K-6_街角地分配程序與可分配判準.md`）⇒
**一律沿用 `K-9-9 四`**：走**合併／調配機制**；**騰出之地入調配池**；
🔴 ⛔ **不得強制配到下限（不得超配）**。
```
🔒 其後 `K-6:2350` 逐字：`🔒 ⇒ 調配池、合併調配、超配三事**皆已裁**，⛔ **不得再上呈 KL**。`

🔒 **`R2` ＝ `N0-5`（`docs/specs/W-G.4_規格_v3.md:18`·逐字·全倉「皆僅為優先順位」命中 **1** 列）**
```
- **N0-5**：七級調配之**同歸戶、調配街廓距離，皆僅為優先順位判斷，非必要**。法源：手冊 P.192~195
「共同負擔公共設施用地分配原則」——「**得**於不影響原街廓內土地分配位次下」「**得**視該街廓分配情形，
將公共設施用地土地合併於該街廓之住宅區內分配」，均為「得」。→ 優先順位與鐵律衝突時，**鐵律勝，優先讓位**。
```

🔒 **`N0-2`／`N0-3`／`N0-4`（同檔 `:13`／`:14`／`:15`·逐字）**
- `- **N0-2 可建築**：重劃後**各宗分配土地**（含抵費地）皆須 ≥ 最小寬度 **且** ≥ 最小深度。`
- `- **N0-3 原位次不變**：原街廓內可以分配土地之原位次不變。`
- `- **N0-4 街角評比規範**：見 §N2。`

### g　調配池（配餘地）

🔒 **`v3:364`–`:374`（逐字）**
```
### 7-0. 調配池定義（取代舊「抵費地池」一詞）
調配池(Ri) = 街廓 DXF 面積(Ri) − Σ G(Ri 內得以分配土地)
           ≈ 街廓 DXF 面積 × (公設地平均負擔% + 重劃費用平均負擔%)
組成 = 街角抵費地（若有）+ 街廓中間調配池
用途 = 公設地負擔部分（本來就預留供調配）+ 費用負擔部分（標售自償，必要時亦可調配/增配）
…
**不是每個街廓都必須留設調配池**——可歸零；要留就必須 ≥ 該街廓最小分配面積（可建築）。
```
🔒 **可否為 `0`（`docs/specs/W-G.4_規格_v3.md:21`·`N0-6`·逐字）**
```
- **N0-6 抵費地＝「得」非「應」**：手冊「各街廓兩面**得**酌留抵費地至少 1 宗…」→ **街廓池可為 0
（合法常態，非權宜）**；若有抵費地，則須完全符合鐵律（≥ MinA、街角依街角規則）。池三則精確化：
任一池片 ∈ {0} ∪ [MinA, ∞)，**且街廓池總量為 0 亦合法**。
```
🔒 **與抵費地之關係**：`v3:369` 逐字「`組成 = 街角抵費地（若有）+ 街廓中間調配池`」
⇒ **抵費地係調配池之<u>組成之一</u>**（⛔ 二詞互斥）。

---

## 工項三　七級調配之**碼側**現查

### a　各級之實作處

🔒 **正典自載之對照（`v3:356`–`:362`·逐字）**：
`7-0a→**wf_f0**（前置地基檢查）；7-1 級0/0'→**wf_f0**、級1-3→**wf_f2**（跨街廓 a′ 模式一）；`
`7-2/7-3 公設地→**wf_f3**（RD 五則＋PF、a′ 併入同歸戶建地）；7-4 無同歸戶 3 級調配→**wf_f4 E1**`
`（距離法＋模式二）；7-5 雙出口→**wf_f4 E2**…；終態遞補整形→**wf_f4 E3**；33 群總決算→**wf_f4 E5**。`

🔒 **實測（母體 ＝ 索引側全部 `.py` **252** 檔）**：`wf_f*` 之檔 ＝ **5**
（`verify/wf_f0.py` **449** 列／`wf_f1.py` **419**／`wf_f2.py` **292**／`wf_f3.py` **233**／`wf_f4.py` **1614**）。
各檔之 module 級 `def`：`f0` **5** 個／`f1` **8**／`f2` **9**／`f3` **6**／`f4` **19**；
五檔皆有 `compute(...)` 為入口（`f0:260`／`f1:184`／`f2:114`／`f3:81`／`f4:315`）。

🔒 **級名字樣於<u>可執行層</u>之命中（去註解與 docstring·AST 判 docstring）**

| 檔 | 可執行列 | 命中之級名（行號）|
|---|---|---|
| `wf_f0.py` | 237 | `級0`（`217`／`221`）／`級1`＋`相鄰`（`135`）|
| `wf_f1.py` | 316 | `相鄰`（`282`）|
| `wf_f2.py` | 209 | `級1`＋`相鄰`（`161`）／`級2`＋`鄰近`（`163`）／`級3`＋`非鄰近`（`165`）|
| `wf_f3.py` | 170 | **（無級名字樣）** |
| `wf_f4.py` | 1266 | `級0`（`1524`）／`級1`（`403`,`404`,`406`,`410`,`487`,`1526`,`1541`,`1543`,`1563`）／`級2`・`級3`・`相鄰`・`鄰近`・`非鄰近`（`487`）／`距離`（`689`）|

🔒 **可執行層之逐字（`verify/wf_f2.py:159`–`:165`）**
```
        for (gid, r, sblk, z_src, a_src, tgt_blk, tgt_row, z_tgt, m1, m2, cd) in raw:
            if frozenset((sblk, tgt_blk)) in adj:
                lvl = "級1相鄰"
            elif cd <= med:
                lvl = "級2鄰近"
            else:
                lvl = "級3非鄰近"
```
🔒 **可執行層之逐字（`verify/wf_f4.py:483`–`:488`）**
```
        def _level(gid, blk):
            if blk in adj_pub.get(ginfo[gid]["anchor"]["blk"], set()):
                return 0
            return 1 if dists[(gid, blk)] <= med else 2
        LV = {0: "級1相鄰", 1: "級2鄰近", 2: "級3非鄰近"}
        border = {gid: sorted(mina, key=lambda b: (_level(gid, b), dists[(gid, b)], b))
                  for gid in ginfo}
```
🔒 **可執行層之逐字（`verify/wf_f0.py:213`–`:223`）**
```
        unq = [l for l in lots if l not in qual]
        if not unq:
            kind, target, merged = "全達標·無須併", None, []
        elif qual:
            kind = "級0"
            target = sorted(qual, key=_key)[0]
            merged = unq
        else:
            kind = "級0'"
            target = sorted(lots, key=_key)[0]
            merged = [l for l in lots if l is not target]
```

🔒 **距離之碼側受詞（`verify/wf_f4.py:433`／`:459`–`:481`·可執行層逐字要點）**
- `:433` `anchor = max(lots, key=lambda x: (x["a"], x["pid"]))` ⇒ **暫定錨點 ＝ 分攤登記面積最大者·並列取 `pid`**（合 `v3:425`）
- `:459`–`:467` 註逐字「距離表（E1 開始時一次定錨：各塊**最大池片質心**）」；`pool_cen[blk] = ((p.centroid.x, p.centroid.y), p.area)`
- `:476`–`:479` `ax, ay = gi["anchor"]["cen"]` … `d = ((ax - pool_cen[blk][0][0]) ** 2 + (ay - pool_cen[blk][0][1]) ** 2) ** 0.5`
- `:481` `med = float(np.median(med_all))`

⇒ 🔒 **碼側之距離受詞**：**歸戶暫定錨點宗之質心** → **該街廓最大池片之質心**；
**歐氏距離**、**DXF 平面座標**（`shapely` 之 `centroid.x/y`）、**⛔ 帶號之明文**、
**單位同 DXF**；**級2／級3 之切分門檻 ＝ 全體距離之中位數 `med`**（⛔ 常數）；
**相鄰之判準 ＝ `adj_pub`**（`_pub_adjacency`·`wf_f4.py:127` `polys[pb].distance(polys[r]) < 0.5` ⇒ **`0.5` m**）。

### b　正典各級 ↔ 碼側實作之逐級對拍

| 正典（`v3` §七）| 碼側 | 判 |
|---|---|---|
| `7-0a` 前置地基檢查 | `wf_f0`（正典 `:359` 自載）| **正典有碼有** |
| `7-1` 級0／級0' | `wf_f0.py:217`／`:221` | **正典有碼有** |
| `7-1` 級1／2／3 | `wf_f2.py:161`／`:163`／`:165` | **正典有碼有** |
| `7-2` RD／`7-3` 公設地 | `wf_f3`（正典 `:360` 自載；`wf_f3` 可執行層**無級名字樣**）| **正典有碼有**（其級名⛔ 現於可執行層）|
| `7-4` 3 級調配 | `wf_f4.py:483`–`:488`（`_level`／`LV`／`border`）| **正典有碼有** |
| `7-5` 雙出口 | `wf_f4` `_e2_optimal`（`:1063`）| **正典有碼有** |
| 🔴 `7-4` 之「**多歸戶搶同一最近街廓時，重劃前總地價高者先調配**」（`v3:427`）| 🔴 **碼側⛔ 實作**——`wf_f4.py:689` 逐字 ＝ `gs = sorted(requests[blk], key=lambda g: (dists[(g, blk)], g))   # 裁定K：距離優先` | 🔴 **正典有碼無**（其成因見下）|
| `7-6` 選填介面／`7-8` 法源缺口 | **（本次未查其碼側對應）**| —— |
| 碼有正典無 | **（本次於 `wf_f*` 可執行層未見正典未載之級）**| —— |

🔒 **上開「正典有碼無」之成因（碼側<u>逐字自陳</u>·`verify/wf_f4.py:437`–`:438`）**
```
            # 裁定K（W-7 順解·claude.ai 2026-07-23）：佇列恆為單一類別、純距離優先
            #   ⇒ `value`（Σa×重劃前區段單價）三處消費**全數退場**、不再計算（原行號已朽·勿引）。
```
🔒 **`裁定K` 之正典側**（全倉 `.md` 命中 **3** 列）：
`docs/W-G.4_KL域裁_兩階段落位_部分面積拆分_Rw側選.md:65` 逐字
`**【裁定K＝B-4 結案·道路用地優先分配之本質】**（KL 2026-07-23·claude.ai 倉內坐實）`。
🔒 **事實**：`docs/配地計算總規格_v3.md:427` **仍載舊文**（「重劃前總地價高者先調配」）、
其上下文**未見**指向裁定K 之更正標記。（⛔ 評價·僅記其為事實。）
🔒 **佐證（`pre_price` 之現行用途·`wf_f4` 命中 **11** 列）**：`:326`／`:343`／`:399`／`:409`／`:498`／`:572`／`:793`／`:836`／`:1101`
皆為 **a′ 折算**（模式一／模式二）與**價換算**，**⛔ 排序**。

### c　🔴 排序準據之**可替換性**（本工項要害）

🔒 **窮舉變體之實測（母體 ＝ 索引側全部 `.py` **252** 檔·**可執行列**）**

| 變體 | 命中 |
|---|---|
| ① 級序之**具名常數**（`LEVEL_ORDER`／`LEVELS =`／`級序`／`ORDER =`）| 🔴 **0** |
| ② 距離優先之**具名參數**（`DIST_PRIORITY`／`DISTANCE_ORDER`／`distance_key`／`dist_key`）| 🔴 **0** |
| ③ 排序之 `key`（`sort_key`／`key=lambda`）| **220**（跨 **85** 檔·**通用形**·⛔ 本項之受詞）|
| ④ 「可替換／`policy`／`strategy`」字樣 | （見下註）|
| ⑤ 三級之字面列舉 | （見下註）|

🛑 **⇒ `W-G.9-94 §六` 所要求之「可替換點」＝ 不存在**；
依單之令「**不存在 ⇒ 明載『已寫死』及其逐處位置**」，逐處如下：

| # | 檔·行 | 逐字（可執行層）|
|---|---|---|
| `1` | `verify/wf_f2.py:160`–`:165` | `if frozenset((sblk, tgt_blk)) in adj:` → `lvl = "級1相鄰"` ／ `elif cd <= med:` → `lvl = "級2鄰近"` ／ `else:` → `lvl = "級3非鄰近"` |
| `2` | `verify/wf_f4.py:483`–`:486` | `def _level(gid, blk):` … `return 0` ／ `return 1 if dists[(gid, blk)] <= med else 2` |
| `3` | `verify/wf_f4.py:487` | `LV = {0: "級1相鄰", 1: "級2鄰近", 2: "級3非鄰近"}` |
| `4` | `verify/wf_f4.py:488` | `border = {gid: sorted(mina, key=lambda b: (_level(gid, b), dists[(gid, b)], b))` |
| `5` | `verify/wf_f4.py:689` | `gs = sorted(requests[blk], key=lambda g: (dists[(g, blk)], g))   # 裁定K：距離優先` |
| `6` | `verify/wf_f0.py:217`／`:221` | `kind = "級0"` ／ `kind = "級0'"` |
| `7` | `verify/wf_f0.py:135` | `ROUTE_OUT = {"G009": "轉F.2(同歸戶R4有達標宗·級1相鄰街廓)",`（**含歸戶 id 之字面**）|

🔒 **級序與距離門檻皆以 `if/elif/else` ＋ 區域變數 `med`（中位數）表達**，
**⛔ 具名常數、⛔ 參數、⛔ 設定入口**；`LV`（`4`／`3`）係**內部碼 → 顯示標籤**之映射，
其**序**由 `_level` 之 `return 0/1/2` 決定。（⛔ 評價其優劣。）

### d　現行處理順序（多宗／多歸戶競爭同一街廓時誰先）

| # | 檔·行 | 逐字 | 所決定者 |
|---|---|---|---|
| `1` | `verify/wf_f4.py:689` | `gs = sorted(requests[blk], key=lambda g: (dists[(g, blk)], g))` | **多歸戶競爭同一街廓** ⇒ **距離** → **`gid` 字典序** |
| `2` | `verify/wf_f4.py:488` | `border = {gid: sorted(mina, key=lambda b: (_level(gid, b), dists[(gid, b)], b))` | 各歸戶之**街廓偏好序** ⇒ 級 → 距離 → 街廓名 |
| `3` | `verify/wf_f4.py:663` | `key=lambda g: (min(dists[(g, b)] for b in mina), g))` | 歸戶之處理序 ⇒ 最近距離 → `gid` |
| `4` | `verify/wf_f4.py:703` | `gs.remove(max(small, key=lambda g: (dists[(g, blk)], g)))` | 退出本塊者之選擇 |
| `5` | `verify/wf_f0.py:204` | `for (gid, blk), lots in sorted(cell.items()):` | F.0 之遍歷序 ⇒ `(gid, blk)` 字典序 |
| `6` | `verify/wf_f2.py:85` | `tgt_blk = max(sorted(qual), key=qual.get)               # 並列取字典序（決定性）` | F.2 之目標街廓 |
| `7` | `verify/wf_f2.py:86` | `tgt_row = sorted(blks[tgt_blk], key=lambda r: (-float(r["G(㎡)"]), r["暫編地號"]))[0]` | F.2 之目標宗 ⇒ `G` 降序 → 暫編地號 |

🔒 **⇒ 處理順序由<u>顯式 `sorted(...)` 之 key</u> 決定**（距離／`gid`／街廓名／`G` 值／暫編地號），
**⛔ 實作之遍歷順序**（各處皆有 `sorted` 包覆·`wf_f2.py:85` 更以註解逐字自陳「**並列取字典序（決定性）**」）
⇒ **可複現**。
🔒 **⛔ 由 `_projection_order` 決定**——`_proj_order` 於 `wf_f0.py:252`／`wf_f2.py:91`／`wf_f3.py:74`／`wf_f4.py:1043`
各定義一次，其**用途**係位次序之**不變量斷言**（`wf_f0.py:413` `ns["_proj_pop_assert_diff"]("wf_f0:_proj_order", oA, oB, …)`；
`wf_f2.py:255` 同形），**⛔ 供調配之處理順序**。

### e　容量過濾與超配檢查

| # | 檔·行 | 逐字 |
|---|---|---|
| `1` | `verify/wf_f4.py:691` | `budget = _budget(blk)            # 可灌量＝可達池 − (MinA+餘裕)；rule3 保有效殘池` |
| `2` | `verify/wf_f4.py:693`–`:698` | `while True:` `tot = sum(demG[g] for g in gs)` `shares = ({g: demG[g] for g in gs} if tot <= budget + TOL else {g: budget * demG[g] / tot for g in gs})` `small = [g for g in gs if shares[g] < mina[blk] - 0.01 and shares[g] < demG[g] - 0.01]` |
| `3` | `verify/wf_f4.py:492` | `unreach = _s0_unreachable(ns, snapE, cb_by, cad, eng, c["forced"])`（註逐字「不可達池量（S=0 碎片＋forced 鎖定；吃池歸零之幾何禁區）」）|
| `4` | `verify/wf_f4.py:452`–`:458` | `_rd_bad = sorted(...)` → `raise RuntimeError(f"🔴 [{tag}] 裁定K 閘破：RD 筆入 7-4 佇列，然其歸戶 {_rd_bad} 於可建築街廓仍有可分配之原位次土地 ⇒ 上游手冊道路五則派工已壞…禁靜默續跑——查 wf_f3 五則派工。")` |

🔒 **`K-9-9 四`／`K-9-11 三` 之「⛔ 不得超配」於碼側之對應** ＝ 上表 `1`／`2`
（`budget` 之上限 ＋ 比例分攤 ＋ `share < MinA` 退出）；
**「不得超配」四字之字面**於 `wf_f*` 可執行層 **⛔ 出現**（其為**正典側**之表述）。

---

## 工項四　KL 五級微調所需之資料在否（**只查在否**·⛔ 設計、⛔ 評價）

| 項 | 在否 | 在哪／型別 |
|---|---|---|
| `a` **使用分區** | 🟢 **有** | 鍵 ＝ `category`（`app.py:1295`／`:2404`／`:3316` 等·**153** 列跨 **47** 檔）。**來源 ＝ DXF `BLOCK` 圖層之閉合多邊形直讀**（`app.py:675` 逐字「街廓一律維持『BLOCK 圖層的閉合多邊形』直讀」；別名表 `:691` `_block_layer_aliases = ('BLOCK', 'BLOCKS', 'STREET_BLOCK', …)`）。型別 ＝ `str`（如 `'可建築土地'`·`app.py:1295` `F3_CATEGORY_BURDEN.get(b.get('category', ''), '') == '可建築土地'`）|
| `b` **規定最小建築面積** | 🟡 **字樣有（4 列）·與「最小分配面積」<u>非同一受詞</u>** | 「**最小建築面積**」＝ **4** 列（`app.py:7055` 逐字 `· **K-9-1**：街角第 1 宗**不豁免**街廓最小建築面積。`／`probe_WG996_yi_invariants.py:871`／`:873`／`probe_WG997_reassign_order.py:559`）；「**最小分配面積**」＝ **62** 列（跨 **6** 檔·如 `app.py:10323` `min_area_by_block  {街廓編號: 該街廓最小分配面積(㎡)}`）。🔒 **二者之別已在正字**：`CLAUDE.md` 既載「**兩個『最小分配面積』同名不同量、禁互代**」（街角規定範圍面積 vs `f3_min_alloc_area_by_label`）|
| `c` **臨正面道路寬度之 UI 欄** | 🟢 **有·逐街廓** | `app.py:4827` `fw = float(b.get('front_width', 0.0) or 0.0)`；輸出欄 `:4851` `'正面路寬(m)':  fw, …`；`:6067` 逐字 `front_road_width_by_label  `{label: 正面路寬(m)}``；harness 對應 `:6069` `run_verification.build_param_table` 之 `正面路寬(m)`。型別 ＝ `float`（公尺）|
| `d` **正面道路名稱／編號** | 🔴 **【查無】** | 「道路名稱／路名／`road_name`／`street_name`／道路編號／`road_id`」之命中 ＝ **0** 列（母體 ＝ 索引側全部 `.py` **252** 檔·可執行層 ∪ 註解）|
| `e` **幾何層如何識別「正面道路」** | 🟢 **有**（見下）| —— |
| `f` **「重劃前原位於可建築分配街廓內」之判定** | 🟢 **有** | `app.py:2389` `def overlay_parcels_to_blocks(parcels, classified_blocks)`·其 docstring 逐字「**將重劃前地號坐落街廓 — 以地號中心點測試是否在街廓多邊形內**」「回傳 parcels 清單（新增 `'block_label'`, `'block_category'` 欄位）」；`app.py:4333` `def overlay_polygons_to_blocks(parcel_polys, classified_blocks)`·逐字「**對每筆重劃前宗地多邊形 × 每個街廓多邊形算 intersection**」「若 area > 1 ㎡ 且非 sliver 才產生一筆暫編地號」「回傳：`[{'原地號','暫編地號','所屬街廓','街廓分類','面積_m2','polygon_coords',...}]`」|

### 🔴 `e` 之逐字（本工項要害）

**「哪條是正面道路」由什麼決定** ＝ **DXF `FRONT_LINE` 圖層之線段，與各<u>可建築街廓</u>多邊形之<u>重疊長度</u>**：

```
app.py:1434      def _best_block(_pts_l, _layer_nm, _handle):
app.py:1435          """回 (winner_lbl, overlap, ranked)。無任何重疊 → winner=None。"""
app.py:1437          for _bl, (_b0, _bp0, _c0, _f0) in _buildable_blocks.items():
app.py:1438              _ovl = _line_block_overlap(_pts_l, _bp0)
app.py:1441          _rk.sort(key=lambda x: (-x['overlap_m'], x['block']))   # 決定性：同分按塊名
app.py:1444          if len(_rk) > 1 and (_rk[0]['overlap_m'] - _rk[1]['overlap_m']) <= _BIND_MARGIN_M:
app.py:1445              _ambig.append({'layer': _layer_nm, 'handle': _handle,
app.py:1446                             'candidates': _rk[:4], 'chosen': None})
app.py:1449      for _fr in _front_raw:
app.py:1450          _w, _ov, _rk = _best_block(_fr['pts'], 'FRONT_LINE', _fr['handle'])
app.py:1454          # 同一街廓多條 FRONT_LINE ⇒ 取**重疊最大**者（舊法取最長·對未截角尖角失真）
```

🔒 **已有之可暴露識別值**

| 值 | 檔·行 | 逐字 | 是否已存入結果 |
|---|---|---|---|
| `p1`／`p2`（端點座標）| `app.py:1465`–`:1466` | `'p1': (_fr['pts'][0][0], _fr['pts'][0][1]),` `'p2': (_fr['pts'][-1][0], _fr['pts'][-1][1]),` | 🟢 **是**（`result['front_lines'][_w]`）|
| `angle_deg` | `app.py:1467` | `'angle_deg': float(_ang_fl),` | 🟢 **是** |
| `length` | `app.py:1468` | `'length': _fr['length'],` | 🟢 **是** |
| `front_lengths[label]` | `app.py:1463` | `result['front_lengths'][_w] = _fr['length']` | 🟢 **是** |
| 🔴 **DXF entity `handle`**（線段之唯一 id）| `app.py:1450`（傳入 `_best_block`）／`:1445`（記入 `_ambig`）| `_w, _ov, _rk = _best_block(_fr['pts'], 'FRONT_LINE', _fr['handle'])` ／ `'layer': _layer_nm, 'handle': _handle,` | 🔴 **否**——`handle` **⛔ 存入 `result['front_lines']`**（僅於**歧義診斷** `_ambig` 中出現）|
| 圖層名 | `app.py:1273` | `_layer_map = {'BASELINE', 'CENTERLINE', 'FRONT_LINE', 'SIDE_LINE', 'ALLOC_LINE'}` | 🟢 **是**（為常數集）|
| `centerlines[road_label]` | `app.py:1200`／`:1193` | `'centerlines': {road_label: [(x,y), (x,y)], ...},` ／ `- CENTERLINE   → f3_manual_road_centerlines（中點落於**道路類街廓**）` | 🟢 **是**——🔒 其鍵係 **`road_label`（＝道路類街廓之 label）** |

🔒 **事實之併陳（⛔ 判斷孰優）**：
`front_lines[label]` 之鍵係**可建築街廓之 label**（`_w`）、其值**⛔ 含道路之任何識別符**；
道路側之識別符現存於 **`centerlines` 之鍵 `road_label`**（＝**道路類街廓之 label**·來自 `BLOCK` 圖層之文字）
與 **DXF entity `handle`**（現僅於 `_ambig` 診斷可見）。

---

## 工項五　預宣證偽輸入（各工項共用）

| 項 | 實測 | 判 |
|---|---|---|
| `a` **搜法有判別力** | 一**已知存在**之符號（`_projection_order`）於**可執行層**之命中 ＝ **99**（母體 ＝ 索引側全部 `.py` **252** 檔）| ✅ 須 > 0 |
| `b` **搜法非恆真** | 一**必不存在**之符號（**哨兵**·其字樣**⛔ 入倉**·`GB-147` 處置一）＝ **0** | ✅ 須 0 |
| `c` 【查無】非停機 | 本報告之【查無】＝ 工項二 `c`（座標系／帶號／單位／容差）·工項二 `d`（終局 tie-break）·工項四 `d`（道路名稱／編號）**據實出艙**、⛔ 停機 | ✅ |

---

## 閘 `P` ／ `numstat` ／ `left-right`

| 檔 | B | 合併後 `sha256` |
|---|---|---|
| `app.py` | 1417946 | `e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

🛑 **本批於合併之外⛔ 動生產碼一位元組**（`app.py` 之現值 ＝ 側分支 `20b04ac` 所帶者）。

| 檔（本 commit·vs 合併後之主線）| 增 | **刪** |
|---|---|---|
| `docs/orders/W-G.9-230_229併線與七級調配現行機制全面現查.md` | 99 | **0** |
| `docs/reports/W-G.9-230R_…_執行報告.md`（**本報告**）| 🔒 ⛔ 於本報告內釘死（自指）| **0** |

🛑 **`deletions` ＝ `0`**（append-only 檔全數）。🔒 `left-right`（推前）＝ **`0 0`**。

## 🟡 仍待（**⛔ 本單處置**·⛔ 裁定題）

1. 🔴 `GB-150`（引擎硬限 `{0, 3.5}`）——依 KL 域答已確認為**通用化波之真阻塞項**；本批**僅登記**、⛔ 動碼。
2. 🔴 **`v3:427` 與 `wf_f4.py:689` 之不一致**（「重劃前總地價高者先調配」vs「裁定K：距離優先」）
   ——**本報告只記其為事實**，⛔ 提名、⛔ 建議、⛔ 出裁定題。
3. 🔴 **排序準據之可替換點 ＝ 不存在**（`W-G.9-94 §六` 佇列項）——已明載其**已寫死**之 **7** 處。
4. 🔴 **`e` 之 `handle` ⛔ 存入 `front_lines`**——事實已出艙，⛔ 判斷其應否暴露。
