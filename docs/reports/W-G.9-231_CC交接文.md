# `W-G.9-231`　CC 新 session 交接文（`2026-09-04`）

> **性質**：交接文（零生產碼）。依既例（`docs/reports/W-G.9-111_CC交接文.md`／`-122`／`-134`）取**批次號 `231`**；
> **⛔ 鑄任何 `自誤`／`GB`／`VR`／`戒`／`K-9` 號**。
> **本文之一切數字皆現查於 `6617acc`**（索引側 `git cat-file blob`·`GB-139 ②`）；⛔ 憑記憶。

---

## 一、態錨（**開窗第一動作＝重跑本節，⛔ 採本文之數**）

| 項 | 值（現查於 `2026-09-04`）|
|---|---|
| 主線 | `origin/wip/s1-endpart` ＝ **`6617acc40fbe80a87420b73b42de33e536b12f41`** |
| `left-right` | **`0 0`**（工作樹追蹤檔乾淨）|
| 🔴 `app.py` blob | **`02cc6d7313789149ba852f997d2d8051a6cefaa0`** |
| 追蹤／`.md` | **1633** 檔／**560** 檔（`git ls-files -z`·**⛔ 省 `-z`**·見四之陷阱）|

**生產碼五檔之現態 `sha256`（閘 `P` 之基準）**

| 檔 | B | `sha256` |
|---|---|---|
| `app.py` | 1417946 | `e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

**近 6 commit**

```
6617acc W-G.9-230：229 已純快轉入主線 ＋ 七級調配現行機制全面現查（只產生事實）⛔ 零新增生產碼
20b04ac W-G.9-229：:19720 預設值耦合補正（單列取代·純重構）🔴 含生產碼·候 KL 放行
2b78a6a W-G.9-228：G2 框改標記法（雙向差集空）＋ GB-149 行號錨通則 ＋ GB-150 引擎硬限 ⛔ 零生產碼
d2d3692 W-G.9-227：219R2 commit 1 已純快轉入主線 ＋ 放行後必辦登記 ＋ GB-67 查證判 ① ⛔ 零新增生產碼
8b0534c W-G.9-219R2：退縮值常設顯示（甲案版面 ＋ 乙案來源判別）🔴 含生產碼·候 KL 放行
2fa1590 W-G.9-226：自誤 284（G4 前提失真）＋ 自誤 285（單缺【驗收】段）＋ G4 四案裁採乙案 ⛔ 零生產碼
```

🔒 **側分支（遠端·皆已併入主線·⛔ 刪）**：`verify/W-G.9-198R-c1`／`-c2`／`-c3`／`verify/W-G.9-219R2-c1`／`verify/W-G.9-229-c1`。

🔴 **工作副本 ＝ 主 checkout** `C:\Users\admin\Desktop\land-readjustment-trial`（**⛔ worktree**·KL `2026-09-01` 裁）；
一切 `git` 以 `git -C <絕對路徑>` 下，**⛔ `cd` 進去**。
🔴 **harness 長跑一律於拋棄式 clone**（主 checkout 內跑 `run_all` **會改寫 3 個已追蹤 log**）；clone **須短路徑**（如 `C:\w2r2`）。

---

## 二、次號（現查·⛔ 採本文之數，開工前重算）

| 簿 | 現況 | 次號 |
|---|---|---|
| 自誤（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）| 相異 **278**／`MAX` **285**／缺號 `[106]`／範圍框 **8** | **286** |
| `GB`（`docs/reports/W-G.4_泛用阻塞項登記表.md`）| 相異 **64**／`MAX` **150** | **GB-151** |
| `VR`（`docs/驗證裁定登記表.md`）| 真 `MAX` **91**（`999` 係裸框佔位·**⛔ 真末號**）| **VR-092**（三位補零）|
| 戒 | —— | **41** |
| `K-9` | —— | **29** |

🔒 **自誤之定義框（正字＝候選 `A`·`VR-091` 補款二）**：
`` ^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`? ``；**範圍框**僅既有 **8** 條；
**缺號清單恆為 `[106]`**（`VR-091` 補款五·`≠` 即器紅或新漏 ⇒ **停**）。
🔒 **出艙命中數之三軸（`VR-091`）**：來源 `commit`／母體檔數・檔長／**粒度框**（字元／列／檔），**三者缺一即違規**；
「單筆列命中」須**三數同格併載**（定義框列命中／重咬／純單筆）。

---

## 三、工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——收官前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**⛔ `origin/main`**）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**。
3. 🔒 **倉內報告＝唯一本體**；聊天＝ping。push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑，**別無他物**。
4. 🔒 **數字錨一律引倉檔**，**⛔ 憑記憶出艙**；與倉態衝突以倉為準。
5. 🔒 **工項零優先於一切工項**——單一律原封入倉 `docs/orders/`，驗「落檔 `sha256` ＝ 入倉 blob `sha256`」。
   🛑 **該閘只證「落檔→入倉」，⛔ 證「聊天原文→落檔」**。
6. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（經發單側）；**⛔ 逕呈 KL**。
7. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 **`deletions` ＝ `0`**。
8. 🔒 **常規補款一**：閘之量測與其所守護之動作（commit／push）**⛔ 置於同一命令塊**。
9. 🔒 **常規五**：每一量測須有**兩造對照組**——器須綠（已知應 > 0）＋器須紅（已知應 ＝ 0）。

### 🔴 生產碼 commit 之流程（**⛔ 逕推主線**）

```
備妥（本地 commit）→ ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c<n> ＋ 本地保全 ref keep/<單號>-c<n>
  → 本地分支 git reset --hard origin/wip/s1-endpart（⇒ 下一批⛔ 可能誤推）
  → 發單側逐位復驗 → 請示 KL → KL 逐字放行
  → 合併：**純快轉**（`git merge --ff-only`）·**⛔ merge commit**
     （既例逐字 ＝ `docs/orders/W-G.9-208_….md:3`；主線全史 merge commit ＝ 4，皆非放行物）
     主線若已前進而快轉不可行 ⇒ 改走 `W-G.9-206 §八` 之 rebase 款（`--force-with-lease` 重推側分支＋重驗）
```
🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。

---

## 四、現行之閘與其框（**開工前逐一重跑**）

| 閘 | 母體／框 | 現況 |
|---|---|---|
| **閘 `P`** | 生產碼五檔之內容 `sha256` | 零生產碼批須**前後逐位相同** |
| **`G2`**（`GB-144` 之取值路徑對拍）| 🔒 **標記法新框（`W-G.9-228` 所立）**：`GB-144` 節內標記為 **`現態命中集@<commit>`** 之列中，其 `<commit>` **最新**者（**`git` 祖先序**·⛔ 字典序／⛔ 位置／⛔ 全節聯集）| 現行標記列 ＝ `GB-144` 節 `:3959`@**`d2d3692`**（**13** 個檔列）|
| **驗收出艙閘** | `verify/tools/wg9223_acceptance_audit.py <單> <報告>`（**須於倉內執行**·`GB-148`）| `rc=1` 且訊息為「該單⛔ 有【驗收】段」⇒ **無從回測**、**⛔ 回測不過** |

🛑 **`G2` 之維護義務（`W-G.9-228` 工項一 `d`）**：每次 `app.py` 之相關檔列**位移**，須於 `GB-144` 節
**新增**一列 `現態命中集@<新 commit>`（**⛔ 修改舊列**）；未新增者 ＝ **已知失效·須於出艙時具名**。
（`W-G.9-229` 之改動係**單列取代**、`LF` 列數 Δ ＝ `0` ⇒ **行號⛔ 位移** ⇒ 該義務**於彼批無受詞**。）

### 🔴 陷阱清單（**已付學費·逐條皆有實例**）

| 號 | 受詞 |
|---|---|
| `GB-139` | `core.autocrlf=true` ⇒ ① ⛔ 對既有檔下 `autocrlf=false` 之 `add`；② **閘之受詞一律取倉側 `git cat-file blob`**（以**工作樹**位元組核 `Δ` 會因 `CRLF` 而少算 CR 數·`W-G.9-227` 實例）|
| `GB-143`（＋補款）| **含反斜線之字面一律 `Write` 落檔**；**凡經 heredoc 者一律⛔ 執行** |
| `GB-145` | 閘之 `rc` **⛔ 取自管線之後**；一律 `subprocess.run(...).returncode` 直取 |
| `GB-147` | **哨兵自落地**——對照組字樣**⛔ 寫入任何入倉文字**；出艙只載命中數與代稱 |
| `GB-148` | 機檢器倉根繫 `cwd`；**倉外 `rc=1`**（⛔ `128`）；🔴 **`--selftest` ⛔ 觸 `_repo()` ⇒ 倉外假綠** |
| `GB-149` | **append-only 簿內行號為錨之閘 ⇒ 母體單調擴張·永久轉紅·⛔ 可以自扣增量解**；處置二 ＝ **錨改符號名** |
| `戒 40` | 凡出艙「`Δ` 恰 ＝ payload」之等同斷言，須**同格並報二端位元組數** |
| `戒 36` | 非鑄號者**⛔ 採該類號之定義列式形**（本倉體例 ＝ `🔬`／`🔧` 起首）|
| 量測器族 | `git ls-files` **未加 `-z`** ⇒ CJK 路徑被 `quotePath` 加引號 ⇒ `.endswith('.md')` **假 0**；`str.count` 係**子字串**比對（深縮排列會被淺縮排樣式命中）⇒ **一律列錨**；`AppTest.session_state` 係 `SafeSessionState`（**⛔ `.get()`／⛔ `dict()`**）|

---

## 五、已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 鏈 | 情境 | 閘 | 所在 | 登記 | 自解條件 |
|---|---|---|---|---|---|
| `序 13` | 退縮 `0 m` | `②-宗 圍堵閘` | **`app.py`**（生產碼）| **`GB-67`·未結案**（破量 `R2` `45.9766 ㎡`／`R5` `56.3293 ㎡`·⛔ 尚未修）| `②-宗` 三項落地介面（`【缺】` 5）＋ 破量歸零 |
| `序 14` | 退縮 `3.5 m` | **結構閘 `telescoping`** | `verify/stepg_pipeline.py`（**harness 專屬**·`app.py` 命中 `0`）| **既存紅·准紅碼（`W-G.9-4` 起）** | 鏈斷成因獲解（**⛔ 放寬容差**）|

🔒 **後果**：`run_step_g` 於 **`R2`** 即 `raise` ⇒ **`R4`／`R5`／`R6` 一筆不產生**
（`docs/reports/W-G.9-172R_…md:27` 逐字·**⛔ `②-宗 圍堵閘`**·同檔 `:59`「中止點 🔴 **相異**」）。
🛑 ⇒ **一切「`R4` 逐值迴歸」現態<u>不可得</u>**（`CLAUDE.md` 正本表 `序 12`）；
生產碼批之迴歸只能出**替代證據 ＝ 行為逐位相同（含同點 `raise`）**，
**⛔ 讀為「已證 `R4` 逐值不變」**。

---

## 六、待辦／待裁（依現態·⛔ 由 CC 自裁）

### 🔴 候 KL／發單側

1. **`GB-150`**（`app.py:14271` `_wf_tag_of` **硬限 `{0, 3.5}`**、其餘 `raise`）——
   KL 域答已確認「**七級調配是每個案都要執行的功能**」⇒ 其為**通用化波之真阻塞項**；本批僅登記。
   🔒 呼叫點**唯一** ＝ `app.py:23846`，在 `st.spinner("執行七級調配…")` 內 ⇒ **⛔ 在 Step G 主分配路徑上**。
2. **`W-G.9-219R2` 之 `③` 語意**（把 `3.5` 設為 `3.5`·值未變 ⇒ 顯示「預設」）——
   已由 `W-G.9-227` 工項二 `c` **正字化並結案**（旗標記「**值曾被改變**」、⛔「欄曾被聚焦」）；⛔ 再議。
3. **`v3:427` 與 `wf_f4.py:689` 之不一致**（「重劃前總地價高者先調配」vs「**裁定K：距離優先**」）——
   `W-G.9-230R` **只記其為事實**；`docs/配地計算總規格_v3.md:427` **仍載舊文、未見更正標記**。
   🛑 **⛔ 由 CC 提名、建議或出裁定題。**
4. **排序準據之「可替換點」＝ 不存在**（`W-G.9-94 §六` 佇列項）——已明載**已寫死之 7 處**
   （`wf_f2:160`–`165`／`wf_f4:483`–`486`／`:487`／`:488`／`:689`／`wf_f0:217`,`221`／`wf_f0:135` 含**歸戶 id 字面**）。
5. **`front_lines` ⛔ 存 DXF entity `handle`**（僅見於 `_ambig` 診斷）——事實已出艙，⛔ 判斷其應否暴露。

### 🟡 併期末批

- `GB-149` 處置二（**錨改符號名**）；行號錨**待復驗之 2 支**
  （`verify/probes/probe_order_preflight.py:134`／`verify/tools/wg99_anchor_audit.py:18`）。
- 🔴 **`G2` 之定義只存於<u>單</u>、⛔ 落地為 `.py`** ⇒ 掃描器取不到（本身即一風險）。
- `GB-148` 之碼面處置（顯式根參數 ＋ `--selftest` 走同一路徑）。
- 已失效**哨兵殘留**（代稱 `Ｑ` **1** 檔／`Ｒ` **6** 檔·計 **13** 列）之逐處處置。
- 層二 `[1, 188]` 之 `188` 號單原文未入倉；正典 `K-6` 之未實作型陳述之復核。
- `:22395`／`:22884` 之 `3.5` 集中化（其各含 `abs(v−3.5)` **情境判別分支**·`_SB_DEFAULT_M` 於彼處**不可及**）⇒ **通用化波**。
- 🛑 `K-9-5-2-a`-3 與 `K-9-5-3` 之逐字互斥（**休眠**·復活即**真域邊界**）。

---

## 七、本波（`-219R2` → `-230`）之新增摘要

| 批 | 產物 |
|---|---|
| `-219R2`／`-227` | **退縮值常設顯示**（甲案版面 ＋ **乙案**來源判別）；`G4'` 實測坐實 **Streamlit 分支未渲染 ⇒ 丟棄 widget key**（非 widget 鍵存活 ＝ 判別力自證）|
| `-225`／`-226` | `GB-144` 補款一（補登三處）；`自誤 283`（單所令之修改對象不存在）／`284`（對既有碼區之性質誤述）／`285`（單缺【驗收】段）；`VR-069` 補款（`deletions=0` 之受詞⛔ 及 `verify/tools/`）|
| `-228` | **`G2` 框改標記法**；`GB-149`（行號錨通則）／`GB-150`（引擎硬限 `{0,3.5}`）；`CLAUDE.md` 正本表 `序 13`／`序 14` |
| `-229` | `:19720` 預設值耦合補正（**單列取代**·`_SB_DEFAULT_M`）；**分歧偵測含負對照**（`B` 常數 `9.9` ⇒ 輸入欄隨之改；`C` 補正前 ⇒ 輸入欄仍 `3.5`·探針 `rc=1`）|
| `-230` | **七級調配之全面現查**（正典側 ＋ 碼側 ＋ 資料在否）——**正典錨 ＝ `docs/配地計算總規格_v3.md` §七 `:354`–`:451`** |

### 🔒 七級調配之現查要旨（`W-G.9-230R` 之濃縮·⛔ 取代該報告）

- **距離優先者 ＝ `7-4` 之 3 級調配**（`v3:429`「相鄰 → 鄰近 → 非鄰近（自暫定錨點起算距離）」；
  暫定錨點 ＝ **重劃前面積相對最大之筆**·`v3:425`）。
- **碼側之距離受詞**：**歸戶暫定錨點宗之質心 → 該街廓最大池片之質心**（歐氏·DXF 平面座標·**⛔ 帶號明文**）；
  **級2／級3 之門檻 ＝ 全體距離之中位數 `med`**；**相鄰 ＝ `_pub_adjacency` 之 `0.5` m**。
- **處理順序由<u>顯式 `sorted` key</u> 決定**（距離→`gid` 等）、**⛔ 遍歷順序**、
  🔒 **⛔ 由 `_projection_order` 決定**（後者係**位次序不變量之斷言**·`_proj_pop_assert_diff`）。
- **正典側【查無】三處**：距離之**座標系／帶號／單位／容差**；**終局 tie-break**；**道路名稱／編號欄**。
- **資料在否**：使用分區 🟢（`category`·**DXF `BLOCK` 圖層直讀**）／正面路寬 🟢 **逐街廓**／
  道路名稱編號 🔴【查無】／**正面道路之識別 ＝ `FRONT_LINE` × 街廓之<u>重疊長度</u>**（`app.py:1434` `_best_block`）／
  「重劃前坐落街廓」判定 🟢（`overlay_parcels_to_blocks`／`overlay_polygons_to_blocks`）。

---

## 八、開窗前三件事

1. **重跑一之態錨**（`rev-parse`／`left-right`／五檔 `sha256`／`ls-files -z`）——**⛔ 採本文之數**。
2. **重算二之次號**（自誤／`GB`／`VR`）——並驗**缺號清單 ＝ `[106]`**、**範圍框 ＝ 8**。
3. **讀 `CLAUDE.md`**（其**工作紀律**與**待落地正本表**為正字；本文只是索引）。
