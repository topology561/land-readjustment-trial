# W-G.5 K-6／K-8 — **新 CC session 交接文**

> 基準 `HEAD == origin/wip/s1-endpart == 1b8ab9a`（本文寫入時）。
> 紀律：凡「已修／已驗／已量」之宣稱**均附實測輸出**；無輸出即視為未驗。
> **行號一律當場 grep 復驗**——本文只給符號名（`CLAUDE.md` 🔒 行號衛生）。

---

## 〇、一分鐘定位

本 session 自 `8237214` 起共 **7 枚 commit**。最重要的一件事：

> **`run_all` 由 `56 PASS／14 FAIL`（70 項）→ `130 PASS／2 FAIL`（132 項）。**
> 不是「12 個紅變綠」，而是 **F.0–F.3 四代原本一 raise 即整代收攤，其下 62 個子檢從未被執行**
> ——它們既不是綠也不是紅，是**不存在**。封存 M-5 後整代跑完，73 個新名目首次執行且全綠。

**剩餘 2 紅皆非本 session 造成**：
`W-F F.4` ＝ 3.5m E2 7-5 批次全域最佳化不可行（**既有·早已上呈待域裁**，先前被 F.0 GSA 錨破遮住）；
`W-G G.2` ＝ F.4 之純連坐。

---

## 一、必讀（依序·勿跳）

1. `CLAUDE.md`（🔒 未 push 不得報收官／🔒 行號衛生／回報鐵律）
2. **`docs/rulings/K-6_街角地分配程序與可分配判準.md`** ——**本波正典主場**
   （K-6 §零 四判準／§一 合併群／§二 四段程序／§三 分界／§四 作廢／§五 收斂性
   ＋ **K-8 §一 BASELINE 無限直線不變式**／**K-8 §二 N-19′ 定義與對照靶**）
3. `.claude/skills/validation-runbook/SKILL.md` —— **驗收判準已升級**（見 §三）
4. `.claude/skills/failure-archaeology/SKILL.md` **#30–#33**（本 session 新增四條）
5. `docs/reports/W-G.5_K6-A1_封存與清單報告.md`（本波主報告·含 §八 K-8 段一）
6. `docs/reports/W-G.5_K6-A_MinA消費點清單.md`（282 點·**含更正附錄**）

---

## 二、倉態

- `HEAD == origin/wip/s1-endpart == 1b8ab9a`
- **既有 🚩·勿動勿 commit**：`data/地籍資料來源_匿名版.xlsx` 未 commit 異動
- 另有 `verify/out/probe_capacity_decomp.log` 未 commit 異動（既有）
- 既存 untracked（**本 session 未動**）：`.agents/`／`.codex/`／`.tmp.driveupload/`／
  `AGENTS.md`／`data/r3.dxf`／大量 `verify/out/*.log`
- `python verify/run_all.py` 約 15–20 分鐘 ⇒ **背景跑＋等通知**
- ⚠️ **收尾一律 `exit $rc`**——`; rc=$?; echo "REAL_EXIT=$rc"` 會讓 harness 收到 `echo` 的 0

---

## 三、🔴 驗收判準已升級（**本 session 起生效·最重要之流程變更**）

**`FAIL 名目 diff 為空` 不再作為單獨驗收判準。**

它只證明「本批沒新增紅」，**看不見長期紅同源**，更**看不見「62 個子檢不存在」**
（它們不在任何名目裡，diff 當然是空的）。我用它當驗收用了整整數批。

**現行判準**（已入 `validation-runbook`）：
1. 取**測項名目集合**（`✅ PASS` 與 `🔴 FAIL` 兩者，非只 FAIL）
2. 算**雙向 diff**：消失名目 ＋ 新增名目
3. **逐項歸因**——消失與新增各自都要理由，禁「預期會變」一句帶過
4. 驗算 `前總數 − 消失數 + 新增數 = 後總數` 須成立
5. 數字型名目（「N 列」「N 格」「N 對」）之變動須附 log `file:line` **自證**

**併記**：`grep -v "T2-DIAG"`／`grep -v "S0d"` 等**過濾引擎自報診斷之做法已明文禁止**
（同 runbook）——要縮短輸出改用 `sed -n '/段標記/,/下段/p'` 取段。

---

## 四、本 session 七枚 commit

| commit | 內容 |
|---|---|
| `71b33d1` | E 系列 BLOCKED-1~3 ＋ 新立 **E-8／E-9／E-10**（弦長改解析·起點改前緣邊·寬度判定三態） |
| `80a1853` | N-17 前置：三 bare except → loud raise；(Ⅰ)/(Ⅱ) 分家；`18.0` → `RW_SATURATION_WIDTH_M` |
| `8a697c2` | **K-4 第5條**右側錨改理論角；E-7「待判」框架作廢改 K-4 蘊含；第3條停機 |
| `e583075` | **K-4-3** 來源母體去街廓限制＋(c) 小往大；E-10 歸因三度更正；U-K2 extras 帳 |
| `de64885` | **K-6-A1**：裁定檔＋282 點清單＋**M-5 封存**（56P/14F → 130P/2F） |
| `d366d97` | K-6-A1 補丁：名目集合補帳＋清單更正附錄＋K-5-3 護欄＋三陳舊指標 |
| `1b8ab9a` | **K-8 段一**：BASELINE 配對「驗證並存檔」＋N-19′ 對照靶（`app.py` 零變更） |

**新增探針（皆掛 `run_all` `[1/3]` 段·不動 PASS/FAIL 計數）**：
`probe_ruling_N_e1_touch.py`（E 系列 T1–T7）／`probe_ruling_K4_s_origin.py`（K-4 第5條）／
`probe_ruling_K8_baseline_pairing.py`（K-8 段一＋N-19′）。
**封存**：`verify/archive/m_rescue.py`／`probe_ruling_K4_3_source.py`（**禁再接線**）。

---

## 五、🔴 下一批要做的（依賴序·KL 指定）

### 5.1 K-8 段二 —— **N-19′ 施工**（定義與對照靶**已在裁定檔·可直接照做**）

- 置換 `_compute_block_depth_alloc` 之取值法（現為「方法 B：面積÷n_alloc 跨幅」）
  為 **N-19′ 解析式**＝兩端垂距之平均（垂直 BASELINE·**無限直線**·**禁取樣**）。
- **對照靶（已逐格驗證·`probe_ruling_K8_baseline_pairing.py` §3）**：
  R1 33.146／R2 44.468／R3 44.335／R4 33.105／R5 45.707／**R6 45.509**
- **連帶（已實測）**：`region_min` **114.07 → 115.87**（R4 仍最淺）；
  1/2 門檻精確值 **57.935** ⇒ `Decimal(str)` 慣例顯示 **57.94**。🚩 2dp 落點待 KL 確認。
- ⚠️ 舊註解所載「方法 A 主用會被合成 BASELINE 角度污染、R2 曾算出 D_avg=500」之風險
  **已由 K-8 段一之配對閘消滅**——但**該註解不得刪除**，改寫保留考古。
- **此改動會動街廓分配深度 ⇒ 連帶 MinA／G／街角面積／PK 勝負全數要重量並逐項歸因。**
  這正是我上一批**不動碼**的理由（避免半套狀態）。**排足時間再開工。**

### 5.2 K-8 段三 —— 街角規定範圍**新構造**
虛擬最小寬度帶（前緣線沿 BASELINE 法向平移 N-19′）＋ ALLOC_LINE 滑動求解
（量測用側界取**截角前**、範圍用**截角後**）。必報八個街角新舊面積逐一列出＋成因方向。
⚠️ `app.py` 之「二者共用同一幾何原語·單一真相源」註解**因本裁定失效**，須改寫；
**(Ⅱ) `_build_burden_range` 完全不動。**

### 5.3 K-8 段四 —— K-7 註解豁免（**行為零變動**）
⛔ **一律不動** `WIDTH_VERDICT_CORNER_K4` 之常數名與字串值——該字串綁
`verify/baselines/E系列實測快照_退縮{0m,3.5m}.csv`，改字串＝重烤假紅。列波末重烤遷移項。

### 5.4 K-6-A2 —— 深度函式提升 ＋ 接線 ＋【B】＋【C】＋ E 類 1/2 門檻
- **深度函式**：`_bl_perp_depth`（巢狀於 `parse_cad_precision_layers`）**提升為 module 級
  ＋ 加逐宗取 min 之外圈**，照 `evaluate_parcel_width_n14` 檔頭之抽出先例
  （headless 可測、+1 gate）。**這不是新建幾何運算**（我原判高估·已更正）。
- 【B】89 點逐街廓達標判定 ＋【C】14 點池三則 → **寬深雙檢**（K-6 §零-1）
- 【D】`region_min` 用途收斂＋立閘；【E】15 點 1/2 門檻改吃 `region_min`（K-6 §零-4 定稿）

### 5.5 K-6-A1【D】 —— 「同原地號合併」述詞置換（**未動·見 §六 U-K6-2**）

---

## 六、🚩 上呈 KL·未裁

| # | 題 | 現況 |
|---|---|---|
| **U-K6-1** | 12 長期 FAIL 真因已定位為 M-5 未重烤 ⇒ K-6 全案落地後是否**統一重烤 v3 baseline（P-H 解凍）** | 需授權·**現仍不重烤** |
| **U-K6-2** | 【D】容差依據：`wf_f2.adj` 係**街廓級**鄰接、**不可沿用**於宗地級共線段連通；須另定（N0-17-a 地籍量測誤差源）且**禁由觀測殘差回推** | A2 批開工前先報 grep 與導出 |
| **K-6 §零-3** | 「面積閘蘊含深度」**幾何上不成立**（`_shift_cut_block_range` 只有寬度項、深度向由街廓多邊形界定）⇒ **未逕改**。KL 所要效果達得到，但理由須改寫為「由街廓分配深度之下限（E-2′）保證」 | 停機中 |
| **1/2 門檻 2dp** | `115.87 ÷ 2 = 57.935` ⇒ `Decimal(str)` 顯示 **57.94**（同 `114.07→57.04` 先例）；KL 文字寫「約 57.93」 | 待確認鎖入 |
| **U-E3** | 對照清冊 12→13 欄（加 `寬度判定`）＝交付物格式異動 | 待知會 |
| **F.4 3.5m E2** | 批次全域最佳化不可行（上界 < 需求·成因既有） | 早已上呈待域裁 |

**已裁畢·勿再問**：U-K3／U-K4／U-K5（**K-6 取代**·K-4-3 整套作廢）／GB-7／GB-8（因作廢消滅）／
U-K2（extras 帳已建）／U-E1（K-4 解消）／U-E4（KL 撤回改案件參數之令）。

---

## 七、🔑 本 session 血訓（**都是實際踩過的**·新 session 先讀）

### 7.1 「既有紅」是待歸因項，不是背景噪音
14 個 FAIL 被當背景擱置了整整數批，其中 **12 個同源於一個可拆除的機制**。
更糟：**紅被別的紅遮住時，它連「既有」都不會被登記**——K-6-A1 解除 F.0 後才曝出
F.4 之 3.5m E2 不可行（**既有**·早已上呈）。
⇒ 長期紅集合應定期做**一次源頭歸因**，否則它會變成一塊誰都不看的地毯。

### 7.2 歸因連錯三次，每次都少做「只變一個變因」的對照（失敗考古 **#32**）
同一個現象（弦長差 1.08e-2 m）我給過三個歸因：
① 資料性質（ALLOC 非平行）→ 錯　② TWD97 座標量級（GEOS 少算）→ 錯
③ **量測線與前緣邊近乎共線** → 對。
決定性實驗：**同宗·同絕對座標·只換 T** ⇒ 只有 E-9 量測點差 −1.0779e-02，其餘深度全 ~1e-10。
**② 版做了對照但同時變了兩個變因**（換量測點＋換座標框）⇒ 把功勞記給錯的那個。
**併記**：修正歸因的那一段最容易犯下一個歸因錯——我在 ③ 版收尾又寫「解析弦是硬前置」，
被 reviewer 用我自己表格的最後一行擋下（局部框同一 T 得 2.22e-16）。

### 7.3 母體錯了，「我量過了」就是一句沒有效力的話
- **#30**：以「**會觸發的子集**」之上界冒充「**全體**」之上界（E-1′ 註解「低容差 40 倍」，
  真全體上界 −1.48e-2 **比容差還大**）。**統計母體**錯。
- **#33**：`wf_f0_mina` 是 `mina` 之複合縮寫，字集有 `_mina_by_block`／`mina_by_blk`／`mina[`
  **卻獨漏它** ⇒「窮盡清單」之窮盡性建立在漏字的 regex 上。**搜尋母體**錯。
  清單本體 278 點**逐點正確、完全可複現**——**正確的內容 ＋ 不完整的母體 ＝
  一個看起來無懈可擊的錯誤結論**。沒被翻盤是運氣不是方法。
⇒ 窮盡型任務**禁以人腦列字集**：對每個概念 grep 其**定義處**，由符號名回頭補字集、迭代到不動點。

### 7.4 我差點取代掉一個正確的機制（K-8 段一）
施工單要我把 BASELINE 配對改結構判準，我六格對靶全中、正要改碼 ⇒ 被撤銷。
被取代者是 KL 2026-07-26 之 C-1/C-2/C-3/C-5，**六塊全部正確**。
誤讀：把「近平行＋屁股側＋深度域＋垂距收斂」整包當距離判準；前三者是**結構條件**。
**⚠️ 我自己表上就有反證**：`#0` 對 R5/R6 皆 0.50、`#2` 對 R2/R3 皆 0.50——
那是「一條 BASELINE 橫跨兩並排街廓」，若某案兩邊皆需配對，該判準**雙雙 0.50 ⇒ 假紅**。
**同一張表既是我的證據也是我的反例，我只看了前半。**
⇒ **對照靶全中不等於判準正確**——還要問「這個判準在什麼情況下會壞」。

### 7.5 reviewer 之全部價值＝獨立復現，且它會打到你剛寫下的正典
本 session reviewer 兩度判 BLOCKED，其中**兩個錯是我當批剛寫進正典的文字**
（「解析弦是硬前置」、docstring 之 `±1e5`／`1.015735` ——那組數字是**我 recon 腳本的組態、
從未上線**，卻被我當成舊碼特徵值寫進註解）。
**自標風險點連續多輪都不是真風險點**——真 BLOCKED 都在沒插旗處。

### 7.6 工具鏈兩雷
- `cmd; rc=$?; echo "REAL_EXIT=$rc"` ⇒ harness 收到 `echo` 的 **0**。一律 `exit $rc`。
- `io.open(p,"w").write(io.open(p).read().replace(...))` ⇒ **`"w"` 先截斷再求值內層 read**
  ⇒ 檔案歸零（我把報告寫成 0 bytes）。**就地改檔一律先讀進變數、關檔、再開寫。**
  倉內自查三種樣式**皆 0 命中**。

---

## 八、機制速查（符號名·**用前當場 grep**）

| 概念 | 查法 |
|---|---|
| N-14 宗地最小寬度（含 E-1′~E-10） | `grep -n "def parcel_min_width_n14" app.py` |
| 寬度判定四態（K-4 蘊含） | `grep -n "def evaluate_parcel_width_n14\|def width_verdict_of" app.py` |
| 街角第 1 宗假設 G（K-4 第5條錨） | `grep -n "def _corner_first_lot_G" app.py` |
| (Ⅰ) 街角規定範圍／(Ⅱ) 側街負擔範圍 | `grep -n "def _build_corner_range_v2\|def _build_burden_range\|def _shift_cut_block_range" app.py` |
| Rw 飽和寬具名常數 | `grep -n "RW_SATURATION_WIDTH_M" app.py` |
| BASELINE 配對（C-1~C-5·**不動**） | `grep -n "def parse_cad_precision_layers" app.py` |
| 深度量測原語（**待提升**） | `grep -n "_bl_perp_depth" app.py` |
| 街廓平均深度（**待換 N-19′**） | `grep -n "def _compute_block_depth_alloc" app.py` |
| MinA per-block（B 類主源） | `grep -n "def _mina_by_block" verify/wf_f0.py`／`grep -n "def wf_f0_mina" verify/run_verification.py` |
| `region_min` 推導／7-0a | `grep -n "_region_min\|_valid_mins" app.py` |
| declared extras 帳（U-K2） | `grep -n "_DECLARED_EXTRAS" verify/run_verification.py` |

---

## 九、驗收與停機

**每枚 commit 前跑 `run_all`**：
```bash
python verify/run_all.py > verify/out/<tag>.log 2>&1; rc=$?; echo "REAL_EXIT=$rc" >> verify/out/<tag>.log; exit $rc
```
須維持 **130 PASS／2 FAIL**（FAIL＝`W-F F.4`／`W-G G.2`），
且**測項名目集合雙向 diff ＋ 逐項歸因**（§三）。
另須：禁寫死絕對路徑閘 0 命中、六夾具 rc=0、**三探針 rc=0**。

**停機準則**：只有**真觸及域邊界**（法規解釋、土地分配後果、面積歸屬）才停機上呈；
純技術項一路 plan→reviewer→施工→量測→push 做完，**不要回問**。
⚠️ **plan 不是交付物**——不得只交 plan 不交碼。
