# `W-G.9-249R`　段甲：工項零（本單原封入倉）＋ 工項四（鑄 `自誤 313`）

> 🔒 **取號之由**：依 `常規四（三）`（CC 對某單之報告、產物一律「該單號 ＋ 後綴 `R`」）⇒ `W-G.9-249R`。
> ⛔ 零生產碼（十檔 ＋ 夾具前後逐位不變）。⛔ 鑄 `GB`／`VR`／戒／`K-9` 任何號。
> 🛑 **段乙（工項一〜三）未辦**——其係生產碼批，須走側分支 `verify/W-G.9-249-c1`；
> 且本窗尚有 `-251` 工項一之四案裁**停機中**（見 `§五`）。

---

## §零　本窗開窗四件事（交接文 `W-G.9-251R §八`·⛔ 跳）

| # | 受詞 | 實測 |
|---|---|---|
| `1` | 驗 `left-right` | 🔴 **`1 656`**（**非 `0 0`**）——harness 自 `main` 開 worktree，**已多度重踩**。工作區 `CLAUDE.md` 僅 **`190`** 列／`15200` B，wip 態為 **`2354`** 列／`193298` B ⇒ **缺 `2164` 列規則**。**處置**：`git switch --detach 5d054f2` ⇒ 復驗 `left-right ＝ 0 0`、`CLAUDE.md ＝ 2354` 列 |
| `2` | 驗硬閘是否生效 | 🔴 **`.claude/settings.json` 與 `.claude/hooks/` 於 main 態工作區<u>皆不存在</u>**；**活體實測**（送一含 `<<` 之無害命令）⇒ **未被攔** ⇒ **`PreToolUse` heredoc 硬閘於本 session ⛔ 生效**。**⛔ 以 `test -f` 代活體實測**（交接文 `§八-2` 逐字）⇒ 本窗全程自律：`Bash` 內⛔ 出現 `<<` 任何形 |
| `3` | 重跑 `§一`／`§二` 每一數 | 見 `§一`。**十檔 ＋ 夾具 ＋ `CLAUDE.md` 之 `sha16` 十二項逐一相符**；檔數各 `+1`（恰為交接文自身）；四簿 B／LF 逐項相符；`keep/*` **`11`** 個逐一相符 |
| `4` | 查新單／未併側分支 | 🆕 **`W-G.9-249` 新單已到**（見 `§二`）；遠端 `verify/*` **`11`** 支之內容**皆已併線** |

🩸 **`§八-4` 之「無未併側分支」一驗，本窗初判誤紅**：以 `git merge-base --is-ancestor` 為框，
`verify/W-G.9-244-c1`（`6b5fe71`）判「未併」。**真因 ＝ 框錯**——`-244` 之併線走 **cherry-pick**
（至 `2bfaa25` 上，成 `1a6a357`）⇒ **commit 物件非祖先，而<u>內容</u>已併**。
🔒 **當場給證**（交接文 `§三` 之最強同一性閘）：`sha256(git diff <父> <該 commit>)` 二端相等 ＝ **`e904b07bf19ab07e`**；
且 `app.py` blob 二端相等 ＝ **`ad7a91dba08119f5`** ⇒ **內容確已併**。
🔑 **通則（本窗第一次自捕）**：**「已併」之受詞係<u>內容</u>，⛔ commit 物件之祖先關係。**

---

## §一　態錨（**現查·⛔ 採交接文之值**）

| 項 | 值（現查 `2026-09-08`·`git ls-remote`）|
|---|---|
| 主線 `origin/wip/s1-endpart` | **`5d054f2fcd4b3ab97f9828dc8a9c36fa53afe65a`** |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/**.md` ／ `docs/orders/` | **1685** ／ **608** ／ **586** ／ **558** ／ **62** 檔 |
| `CLAUDE.md` | `193298` B／`2354` LF／`sha16 663e334b78b45548` |
| 主 checkout 之工作樹 | 🟡 停於 **`1e5a87c`**（落後 `3` 個 commit）——其差 **`5` 檔全為 `docs/`**、**零生產碼**（`grep -xE` 生產碼五檔 `rc=1`）⇒ KL 之 localhost ⛔ 受影響 |

**十檔 ＋ 夾具之 `sha256[:16]`（`blob@5d054f2`·十二項逐一與交接文 `§一` 相符）**

| 檔 | 前 16 位 | 檔 | 前 16 位 |
|---|---|---|---|
| `app.py` | `4860f10828fb8ae9` | `verify/wf_f0.py` | `6758ea766b001b95` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/run_verification.py` | `d0d531f7dc051b65` | `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/stepg_pipeline.py` | `c8d46d2e5fb79dc5` | `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/run_all.py` | `4ba89fef90979491` | `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| 夾具 `verify/fixture_wf_ns_wiring.py` | `b17826a6f7b98bf5` | | |

🩸 **本窗第二次自捕（量測器紅·非受詞紅）**：自誤簿以工作區 `wc -c` 量得 **`637463`** B，
與交接文 `§二` 之 `630066` **不符**，差恰 **`7397`** ＝ **該檔之 LF 數** ⇒ **CRLF vs LF 之量測框錯**
（`autocrlf=true`·交接文 `§四` 陷阱 `2` 明載）。改以 `git cat-file blob` 量 ⇒ 四簿 B／LF **逐項相符**：
自誤 `630066`／`7397`；`GB` `681606`／`5036`；`VR` `364971`／`4695`；`K-6` `285233`／`3972`。

---

## §二　工項零：本單原封入倉

🔑 **單之所在（交接文 `§八` 明載·本窗<u>第三度</u>）**：單在**主 checkout 之工作區**
（`C:\Users\admin\Desktop\land-readjustment-trial\docs\orders\`）而**⛔ 在倉** ⇒ 尋單之母體須含該處。
🩸 **本窗第三次自捕**：首次 `ls` 得 `62` 檔（與倉同）⇒ 判「無新單」；一分鐘後 `comm` 得工作區多 `1`。
**真因 ＝ 受詞於二次量測之間改變**（該檔 `mtime ＝ 2026-09-08 01:04:32`，首次 `ls` 在其之前）——
**非量測器錯**。復驗：二獨立器（`ls -1`／`find -type f`）皆得 **`63`**。

### `a`　開工閘（**⛔ 顛倒·先查占用、後入倉**）

**`S-1` 宣告框（三數分列 ＋ 檔框載其聯集式·補款 `⑥`）**
**三軸** ＝ 來源 `blob@5d054f2`／母體 **全 `docs/` `586` 檔**（`③`·`*.py` ⛔ 構成占用）／粒度框 **檔框 ＋ 列框**。
框之逐字 ＝ `CLAUDE.md`「號占用閘之框改宣告框」`①`–`④` ＋「`D3` 自稱形之邊界」`⑤` ＋「三數分列」`⑥`。

| 號 | `D1` | `D2` 列 | `D3` 列 | **檔框**〔`|D1 ∪ D2之檔 ∪ D3之檔|`〕 | **列框**〔`D2＋D3`〕 | 鬆框 檔／列 | 角色 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-249`** | `0` | `0` | `0` | **`0`** | **`0`** | `2`／`2` | 受詢 ⇒ ✅ **未占用** |
| `W-G.9-251` | `4` | `8` | `2` | **`5`** | **`10`** | `5`／`20` | 對照甲（須 `≥1`）✅ |
| `W-G.9-252` | `0` | `0` | `0` | `0` | `0` | `2`／`2` | 參考 |
| 一人造號（字樣⛔ 入倉·`GB-147`·**代稱**）| `0` | `0` | `0` | `0` | `0` | **`0`／`0`** | 對照乙（須 `=0`）✅ 器能歸零 |

🔒 **與單 `§零` 之對拍**：單於 `blob@c524cb8`／`585` 檔載 `-249` ＝ `0/0/0`、檔框 `0`、列框 `0`、鬆框 `2/2`
⇒ **與本表逐項相符**。對照甲之差（單 `3/5/0`·檔框 `4` ⇒ 本表 `4/8/2`·檔框 `5`）**恰為交接文自身一檔**
（`docs/reports/W-G.9-251R_CC交接文.md`·`D1 +1`／`D2 +3`／`D3 +2`）⇒ **自洽**。
🔒 **`D3` 二列逐列具名**（防偽陽）：`…W-G.9-251R_CC交接文.md:44`（commit 序之引用列）／
`…W-G.9-251R_段甲_工項零與工項四.md:1`（`# ` 標題列）。

**`S-0`／`S-0b`**　`python verify/probes/probe_order_preflight.py <單>`（**於倉內**·`GB-148`）
- **`--selftest`**：✅ 四靶全成立（靶① `P-3` 紅／靶② `P-4` 提示／靶③ `P-6` 提示／反靶 `P-3` 不紅）⇒ **器非恆綠亦非恆紅**。
- **對本單**：🔴 **機械停機款 `0` 項**（`rc=0`）。
  **`P-5` `SELF_SHA256` ✅ 相符**（口徑 ＝ 自檔首至最末一個 `SELF_SHA256` 列之前一個 `\n`（含）·受詞 `11547` B；
  實算 `53654fb5c370d5c7…` ＝ 單載）⇒ **`S-0`（單之完整性）同此一驗，併綠**。
- **🟡 提示 `2` 項之逐項處置**（⛔ 靜默略過）：

| 項 | 列 | 處置 |
|---|---|---|
| `P-4` | `:3` | **具名豁免（偽陽）**——命中之「——」係破折號，該段⛔ 涉幾何方向、無座標系可言 |
| `P-4` | `:60` | **具名豁免（偽陽）**——同上；該段係 KL 裁乙之逐字引，⛔ 涉方向 |

**`S-3`**　閘 `P` 十檔 ＋ 夾具（index 側）**逐一與 `§一` 相符** ⇒ 本批零生產碼。

### `b`　入倉（原封）

`docs/orders/W-G.9-249_K-6段三落地_第一分支_觀測模式.md`（`11625` B／`154` 列／LF）。

| 驗 | 值 |
|---|---|
| 發單側原檔 `sha256` | `a278fbcc062f7a7779c47661fd8af984e5e35b792135b1ae86acbebf558232a5` |
| **入倉 blob（index 側）`sha256`** | **`a278fbcc062f7a7779c47661fd8af984e5e35b792135b1ae86acbebf558232a5`** ⇒ **逐位相同** |
| 對照組（器須紅·任取他檔）| `f6d477b6981d0f6c…` ⇒ 器能紅 |
| `numstat`（原始輸出·⛔ 加工）| `154	0` ⇒ **deletions ＝ `0`** |

🛑 **本閘只證「落檔 → 入倉」，⛔ 證「聊天原文 → 落檔」**（`CLAUDE.md` `追加四` 逐字）。
🔒 落檔以 `cp` 為之（⛔ 經 CC 重打）；原檔行尾為 **LF** ⇒ `autocrlf` ⛔ 改其入庫位元。

---

## §三　工項四：鑄 `自誤 313`

**號之復算**（**三軸** ＝ `blob@5d054f2`／母體 `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔
`630066` B・`7397` LF／粒度框 **列框**；框 ＝ `VR-091 四` 之**定義框（候選 `A`）＋ 範圍框**逐字·⛔ 裸框）

| 量 | 值 | 交接文 `§二` 所載 | 判 |
|---|---|---|---|
| 一、定義框列命中 | **`306`** | `310` | 🔴 **不符** ⇒ 見 `§四 1` |
| 二、重咬列 | `3` | —— | —— |
| 三、純單筆列命中 | `303` | —— | —— |
| 範圍框列命中 | `8`（其中重咬 `3`）| `8` | ✅ |
| 相異〔集合基數〕 | **`305`** | `305` | ✅ |
| `MIN` ／ `MAX` | `7` ／ **`312`** | `7` ／ `312` | ✅ |
| 缺號清單 | **`[106]`** | `[106]` | ✅ |
| 恆等式（相異＋缺號數 ＝ `MAX`−`MIN`＋`1`）| `True`（`305＋1 ＝ 306`）| `True` | ✅ |
| **次號** | **`313`** | `313` | ✅ |

🔒 **三數同格併載**依 `VR-091` 補款二之補充（⛔ 以「單筆列命中」一詞單獨出艙任一數）。
🔒 **`③` 盲區機驗**：`A` 之 `306` 列命中，其「`#+` 與『自誤』之間」去裝飾後之文字部分
**非空者 ＝ `0` 列** ⇒ 現態仍**零實例**、⛔ 觸補款二 `④` 之復審條件。
🔒 **兩造對照組**：`MAX 312` ∈ 命中集 `True`（器須綠）／一人造號 ∈ 命中集 `False`（器須紅·代稱）／
`106` ∈ 缺號清單 `True`。

**落地**：`docs/reports/W-G.9波_claude.ai側自誤登記.md` 末端追加一則
`## 自誤 \`313\`　**宣告框之檔框逕取 \`D1\` 之數（\`293\`／\`295\` 同族·第三度）**`。

| 驗 | 值 |
|---|---|
| `append-only` | ✅ 既有 `637463` B（工作區 CRLF 口徑）**逐位不動**；新增 `4296` B |
| `numstat`（原始輸出）| `63	0` ⇒ **deletions ＝ `0`**（`VR-069`）|
| **標題形之自證** | ✅ 新號 `313` **可被定義框（候選 `A`）命中** ＠ `:7401`；命中總數 `306 → 307` |
| 標題形之範本 | 取既有最末一則 `## 自誤 \`312\`　**…**`（`常規四（九）四`）|

🔒 **內容**依單 `§五` 逐字（⛔ 改寫），惟**標題形改依簿之既有體例**（`##` ＋ `　` ＋ 粗體），
⛔ 照抄單之 `### \`自誤 N\`｜…` 形——其⛔ 可被定義框命中。

---

## §四　本批新捕（**事實登記·⛔ 鑄號·⛔ 由 CC 自裁**）

### `1`　🔴 交接文 `§二` 之「列命中 `310`」係**舊框**所得

當場辨明（**同一母體·同一 blob**）：**舊框**（前綴字元集⛔ 禁 `（`／`(`）得 **`310`**；
**正字框 `A`**（`W-G.9-223` 工項一已裁採）得 **`306`**；`舊 ∖ A` ＝ **`4` 列**，逐列具名：

| 列 | 逐字（截） |
|---|---|
| `:5363` | `### 族之具名（依 \`自誤 257\` 之要求）` |
| `:6142` | `### 族之具名（依 \`自誤 257\` 一系·⛔ 採單所述之族譜——下表係本批自倉標題逐字所配）` |
| `:6394` | `### 族之具名（依 \`自誤 257\` 一系）` |
| `:6477` | `### 族之具名（依 \`自誤 257\` 一系）` |

四列之形皆為**括號內之引用形**——正係補款二 `①` 所指「引用形之特徵係『`自誤` 落於**括號內**』」。
🔒 **相異／`MAX`／缺號／次號⛔ 受污染**（四列所引之 `257` 係**已鑄之號** ⇒ 後向引用），
合補款二 `③` 之逐字。🛑 **⛔ 追改交接文之 `310` 一字**（`常規四（九）五`）——已記於 `自誤 313` 之併記。

### `2`　🔴 宣告框之 `列框 ＝ D2 ＋ D3` 對**同列雙屬**者**重複計數**

**實例（本批當場捕得）**：`docs/reports/W-G.9-251R_段甲_工項零與工項四.md:1`
＝ `# \`W-G.9-251R\`　段甲：工項零（**本單**原封入倉）＋ …`
——該列**同時**滿足 `D2`（列首為 `#+` 且含該號）**與** `D3`（同列含「本單」＋ 該號，且該號 ＝ 檔名之號 ⇒ 依 `⑤` 成立）。
🔒 **後果**：`⑥` 之 `列框 ＝ D2 ＋ D3` 使該列**計二次**，與 `①` 之**聯集**語意（`D1 ∪ D2 ∪ D3`）相斥。
🔒 **本批之影響 ＝ 零**——受詢號 `-249` 三數皆 `0`；本則僅係對照甲之精確分項。
🛑 **⛔ 由 CC 自裁**：是否於 `⑥` 增設「同列雙屬者列框計一次」一款，係**意思決定** ⇒ 上呈發單側／KL。
🔒 併記：`⑥` 已明令「`D1` 係<u>檔框</u>之款、⛔ 產生任何列命中」，已處理 `D1`×`D2` 之重複；
**`D2`×`D3` 之重複⛔ 在其射程**。

### `3`　🟡 單之工項編號自相斥（**與 `-251` 同形·第<u>五</u>度**）

`§前言`「段甲（工項零／**五**）」vs `§五` 標題「工項**四**」。
🔒 **處置**：依 `常規二` 取**節標題**為準（工項零 ＋ 工項四）並續辦——與 `-251` 之處置一致。
🛑 **⛔ 另鑄號**（單 `§五` 已就 `-251` 之同形明令「併記·⛔ 另鑄號」）；⛔ 追改既出。

---

## §五　待辦／上呈

| # | 受詞 | 狀態 |
|---|---|---|
| `1` | 🔴 **`W-G.9-251` 工項一之四案裁**（甲／乙／丙／**丁**）| **停機中·尚未下**——倉內⛔ 有新裁、`docs/orders/` ⛔ 有 `-251` 之後續單 |
| `2` | 🟢 **`W-G.9-249` 段乙（工項一〜三）** | **未辦**——生產碼批（`app.py` 單檔），須走側分支 `verify/W-G.9-249-c1`。**候發單側／KL 指示其與 `1` 之排程先後** |
| `3` | `§四 1`／`§四 2` 二則 | **上呈**（⛔ CC 自裁）|
| `4` | 承前波 | `GB-67`（退縮 `0` 之 `②-宗 圍堵閘`·`R2` 重疊 `45.9766 > 2.8906`）／telescoping（退縮 `3.5` 之 `ΣRw`·`R2 left 100.02 ≠ 44.41`）**皆未解** |

🛑 **本報告⛔ 宣稱段乙已辦**；`§三` 之驗收（`V-0`〜`V-6`）**一項未跑**——其受詞係段乙之生產碼。

**次號（本批後）**：自誤 **`314`**（`MAX 313`）／`GB-152`／`VR-092`／`K-9-29`／戒 `41`。
