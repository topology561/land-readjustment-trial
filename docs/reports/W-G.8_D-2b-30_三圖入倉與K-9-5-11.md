# W-G.8／**D-2b-30**：三圖入倉 ＋ `K-9-5-11` ＋ 判讀標記 ＋ 錨更正 ＋ 考古增節 ＋ 強制抵費地帶量測

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**起點**：`a2e483d`
**本單實際編號 ＝ `D-2b-30`**（`grep -rn "D-2b-30" . --exclude-dir=worktrees` ⇒ **0 命中**·未占用）

> # 🔴 最先讀
>
> **§1〜§6 全部落地，⛔ 無停機。** §9 四項停機條件**逐項未觸發**（見 §7-7）。
> **§3-3 之量測結果單列於 §3，標「⛔ 待 KL 裁（附數字）」**，⛔ 未混入已結清項。
>
> 🔴 **三項須 KL 注意者**：
> **(a)** `§3-3` 之 `C = buf − S1_par` 於**三個適用格**皆為 **+1.66〜+1.70 m**、
> 面積差 **+72.5〜+76.3 ㎡**——⛔ **本批不判其為缺陷**（`§3-4`），**待裁**。
> **(b)** 登記之 **`P2`（|C| 與夾角正相關）於正表無法檢定**——三適用格夾角幾乎相同
> （`4.3219°`·差 `1.4e-5°`）；且**儀器層出現一組逆序**。
> **(c)** 登記之 **`P3`（R6 退化自驗）於正表落空**（`R6` 無適用格）
> ⇒ 另設**儀器自驗**補之（`R6` `|C| = 7.8e-05` ✅）——🔒 **⛔ 非土地事實**。

---

## §0 動手前（⛔ 未憑本單轉抄）

| 項 | 現查 |
|---|---|
| 編號 `D-2b-30` | **0 命中** ⇒ 未占用 |
| `HEAD` | `a2e483d`（＝ `origin/wip/s1-endpart`·工作樹乾淨） |
| 零生產碼 | `git diff --stat a2e483d -- app.py verify/stepg_pipeline.py verify/selection_pipeline.py verify/run_verification.py verify/wf_f*.py` ⇒ **空** |
| 新增 `.py` | **僅** `verify/probes/probe_D2b30_forced_band_width.py`（§7-1 已列） |

---

## §1 ✅ commit A：三張示意圖入倉（`45657e3`）

入 `docs/specs/figures/`（沿用倉內既有慣例·同目錄已有 `W圖.jpg` 等三檔）。
**檔名逐字保留 KL 原名**，⛔ 未改名、未轉檔、未壓縮。

**md5 對帳（§7-3·三檔逐檔相符）**：

```
4a2edf6513e6be399e93ea5ea8e020ff *docs/specs/figures/第2宗示意圖_第1種分配線方向.jpg
4a6081b921a2a6a3152982c2017c61af *docs/specs/figures/第2宗示意圖_第1種分配線方向_1.jpg
91e972d917efd9d7bb61b85d9ec6c52a *docs/specs/figures/第2宗示意圖_第2種分配線方向.jpg
```

🔒 **量測框聲明**（`CLAUDE.md`「位元組數／校驗碼須聲明量測框」）：
`.gitattributes` **未涵蓋 `*.jpg`**（`grep -c "jpg" .gitattributes` ⇒ `0`），
故先驗既有同目錄檔之兩側是否一致——`docs/specs/figures/W圖.jpg` 之
**工作樹 md5 ＝ blob md5**（`968e4c59…`）⇒ git 對 jpg **不做行尾正規化**。
本批入庫後複驗三檔 **blob md5 與上表逐檔相同** ⇒ **兩側量測框一致、無歧義**。

### 1-1 圖面引文之逐字核對（⛔ CC 自行開圖·未憑本單轉抄）

我開啟 `第2宗示意圖_第1種分配線方向_1.jpg` 確認其為**含標題框**之版本，逐字比對：

| # | 圖 `_1` 之原字（⛔ 未正規化） | `K-9-5-4` 逐字（`docs/rulings/K-6_*.md`） | 判 |
|---|---|---|---|
| (a) | `S1>=max(線段P0-Ps),(退縮+畸零地寬))` | `S1 = max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)` | ✅ 表面歧異存在（由 `K-9-5-11` 解消） |
| (b) | `S1定義：街角第1宗土地的臨正街寬度(未截角)` | **正典未載** | ✅ 差額·已照實登記 |
| (c) | `P0=FRONTLINE左側起點與SIDELINE交點、` | `P0` ＝ FRONTLINE 與 SIDELINE 之**未截角理論交點** | ✅ 圖為左街角實例之描述·正典側別中立 |

⚠️ **字形差額已一併入倉**（⛔ 不當作「不符」）：全形／半形括號、`第 1 宗` vs `第1宗` 之空格、
以及圖作「**畸零地寬**」而正典作「**畸零地最小寬**」。加註內已附**圖面原字**供日後比對。

🔒 正典「圖證待補」段之結清加註**已載明**：本結清**僅解除「圖未入倉」之缺件**，
第 5、6 條之幾何細節**尚未重測** ⇒ ⛔ 仍不得據以作第 1 種／第 2 種之對應判定。

---

## §2 ✅ commit A 同批：`K-9-5-11` 入正典

落點以 `grep -n "^### 🔒 K-9-5-10" 該檔` 定位，置於該節**及其 `-a` 子節全文之後**（現為檔尾）。
`S1` 之**三分口徑**：規定範圍「**＝**」／WINNER 實配「**≥**」／無 WINNER 強制抵費地「**＝**」。
🔒 `K-9-5-4` **未因本節修改一字**（`--numstat` 刪除欄 0 為構造性證明）。

---

## §3 ⛔ **待 KL 裁（附數字）**：強制抵費地帶之寬度量測（commit B `71264c3`）

> 🔒 **本節與已結清項分列**（§8 要求）。⛔ 本批**不判定此差是否為缺陷、不改構造、
> 不登記 `GB-xx`、不提修法建議**（§3-4）。**量完即止。**

**事前登記**：`660b9cf`（`docs/reports/W-G.8_D-2b-30_強制抵費地帶量測_事前登記.md`）
——⛔ 於任何 `_corner_buffer_S`／`_block_strip`／`S1_par` 之閱讀或量測**之前**入庫。
**時序證明**見 §7-6。

### 3-1 §3-2 四項前提（CC 自行複驗·**全部相符**·任一不符即停機·未發生）

| # | 命題 | 現查 |
|---|---|---|
| 1 | 規定範圍遠側邊 ∥SIDELINE、寬度定為 `S1_par = max(_seg_P0Ps, T)` | ✅ `grep -n "S1_par = max(_seg_P0Ps, T)" app.py`；碼註逐字「**沿 FRONTLINE 量·路平行**」；另有自檢「二側界之**路平行距**恆 ＝ `S1_par`·全深度」 |
| 2 | `_corner_buffer_S` 以 bisect 解 `buf` 使**帶面積 == 規定範圍面積** | ✅ 函式本體：`_band_area(mid)` 二分至 `\|band − range\| ≤ tol`（`tol=0.01`） |
| 3 | 帶之切邊方向由 `allocation_dir` 決定 ⇒ **∥ALLOC** | ✅ `grep -n "n_hat = np.array(\[-ad\[1\], ad\[0\]\])" app.py`（`_block_strip` 內） |
| 4 | 兩處呼叫點傳街廓 ALLOC 方向、輸入為**面積** | ✅ 傳 `allocation_dir_block`（＝`alloc_normal_axis(cad alloc dir)`）＋`【左/右】街角最小面積(㎡)`，並由 `_fo_left`／`_fo_right` 閘控 |

🔑 **讀碼另得（⛔ 施工單未載·CC 加）**：`A` 與 `B` **同為「沿 FRONTLINE」之量**
（`S1_par` 為路平行距；`buf` 為 `s` 座標量——左側為**絕對 `s` 上界**、右側為**自 `s_max` 起之寬**）。
**二者之別在切線方向**：規定範圍界線 **∥SIDE**、強制抵費地帶切線 **∥ALLOC**
⇒ **等面積不蘊含等寬**。

### 3-2 🔒 射程聲明（§3-3 要求）

**街廓 ↔ SIDE 線 ↔ ALLOC 方向之關聯**取自 `cad["side_lines_by_side"][blk][side]` 與
`cad["alloc_dir_by_block"][blk]`（由 `run_verification.build_pipeline` → `parse_cadastral_geofile` 產出）
——⛔ **未以幾何鄰近、buffer 相交或最近距離自行配對**。
`range_area` 取自 `build_param_table` 之 `【左/右】街角最小面積(㎡)`、
強制抵費地之有無取自 `run_corner_pk` 回傳之 `forced_map`——**二者皆與生產同源**。

### 3-3 自我驗證閘

| 閘 | 結果 |
|---|---|
| **H1** `P0` ≡ FRONT∞ ∩ SIDE∞ | ✅ max `5.821e-11`（16 格） |
| **H2** `Ps` 為明確之 FRONT 側頂點 | ✅ **16/16**；最劣辨別比 `238.0×`（須 > 10） |
| **R** `band_area(B) == range_area` | ✅ max`\|Δ\|` `0.000000`（3 格） |

🩸 **本檔首版把 H2 寫成絕對 `< 1e-6` ⇒ 立即閘破**（max `1.483e-02`）。
該值 ＝ **`R4` 之 CAD 圖面既有落差 `0.014826m`**（`verify/baselines/PROVENANCE.md` §A.2
逐字「係 **CAD 圖面既有**（非程式產生）」）⇒ **係閘設錯、非資料有問題**。
🔒 改採既有 GEO-1 探針之**辨別性閘**逐字（`grep -n "H2 係辨別性閘" verify/probes/probe_K9_geo1_S1_construction.py`）
——該處亦逐字記載「**前版寫成絕對 `< 1e-6` ⇒ 16 格中 14 格未過，係閘之判準設錯**」。
⇒ **我重演了倉內已記載之同一個錯**；⛔ 未自訂門檻，改沿用既有判準。

### 3-4 🔴 逐格結果（鍵 ＝ 側別 ＋ `s` 區間·⛔ 非宗序號、⛔ 非行號）

**母體 16 ＝ 街角側 8 × 情境 2**；**差集自證：適用 3 ＋ 不適用 13 ＝ 16，殘差 0**。
⚠️ **非空集合** ⇒ 未觸發 §3-3 之「全部不適用即標未取得證據」之退化條件。

| 情境 | 街廓/側 | `s` 區間 | **A**＝`S1_par` | **B**＝`buf` | **C**＝`B−A` | **D**＝面積差(㎡) | **E**＝夾角° |
|---|---|---|---|---|---|---|---|
| 3.5m | R2/left | `[0.2677, 100.0274]` | `7.000000` | `8.695857` | **`+1.695857`** | **`76.162621`** | `4.321972` |
| 3.5m | R3/right | `[-0.0000, 96.8032]` | `7.000000` | `8.702662` | **`+1.702662`** | **`76.275228`** | `4.321958` |
| 3.5m | R5/left | `[0.2685, 92.7995]` | `7.000000` | `8.658415` | **`+1.658415`** | **`72.536113`** | `4.321968` |

**不適用之 13 格**（⛔ 已逐格具名於 log·未留白）：`0m` 全 8 格 ＋ `3.5m` 之
`R1/left`、`R1/right`、`R4/left`、`R4/right`、`R6/right`
——成因**一律**為「該側有 WINNER ⇒ 無強制抵費地」（`forced_map` 之 `{side}_forced_offset` 為 `False`）。

### 3-5 儀器自驗（🔒 **⛔ 非土地事實**·⛔ 不得引為土地後果）

**成因**：`R6` 無適用格 ⇒ 登記之 `P3` 自驗閘於正表落空。
故另對**全部 16 格**試算 `buf`（含無強制抵費地者），**僅驗量測鏈本身**：

- 🔒 **`R6`（E ＝ `0.000003°`·`SIDE ∥ ALLOC`）之 `|C|` ＝ `7.786e-05`**
  ⇒ ✅ **量測鏈通過退化自驗**（二帶同向時等面積即等寬）。
  ⛔ 依 §3-3，該格**不得作任何結論之通過憑據**。

### 3-6 🔴 預測對帳（⛔ 未回頭修改登記檔一字）

| 預測 | 結果 | 判 |
|---|---|---|
| **P1** `C ≠ 0` 於 `SIDE ∦ ALLOC` | 三適用格 `C` ＝ `+1.66`〜`+1.70` m | ✅ **相符** |
| **P2** `\|C\|` 與夾角 `E` 正相關 | 🔴 見下 | 🔴 **部分不符** |
| **P3** `R6` 之 `C ≈ 0`（自驗閘） | 正表**落空**；儀器層 `7.8e-05` ✅ | 🔴 **正表未取得證據**（由儀器層補得） |
| **P4** `C` 符號於同側別內一致 | 三適用格**全為正** | ✅ **相符** |
| **P5** 多數格「不適用」 | `13/16` 不適用；**非全部** | ✅ **相符** |

**P2 之二項具名事實**：
1. 🔴 **正表無鑑別力**——三適用格之夾角 `4.321972°`／`4.321958°`／`4.321968°`，
   **極差僅 `1.4e-5°`** ⇒ **在正表上無從檢定「隨夾角變化」之命題**。
2. 🔴 **儀器層出現一組逆序**（⛔ 非土地事實）：
   `R4/left` `E=2.713695°` → `|C|=0.788` **大於** `R4/right` `E=2.867975°` → `|C|=0.651`
   （**兩情境皆然**，惟係**同一幾何對**量兩次）。
   併看：`R1/left` `E=0.557951°` → `|C|=0.0018` vs `R1/right` `E=0.690832°` → `|C|=0.2010`
   ——**夾角僅差 `0.13°` 而 `|C|` 差 112 倍**。
   ⇒ **`|C|` 顯非 `E` 之單變數函數**；登記之「正相關」作為**趨勢**大致成立
   （`E` 由 `0.000003°` 增至 `4.32°` 時 `|C|` 由 `7.8e-05` 增至 `~1.70`），
   但**作為法則不成立**。⛔ 本批**不推論其另一自變數為何**。

---

## §4 ✅ commit C：`W-G.9 §2-1` 判讀改標（`1d96ea5`）

以 `grep -n "待實測坐實" 該檔` 定位，於該標記段**之後**加註；⛔ 原標記與判讀表**一字不動**。
改標為「**地基不成立、結論未定**」，並載明引用禁令，及
「**三圖已入倉 ≠ 已重測**；圖到位**不解除**本禁令」。

---

## §5 ✅ commit C 同批：`GB-14` 失效錨更正

- **失效**：`grep -n "_k97 = k97_solve_alloc_t" app.py` ⇒ **0 命中**（`rc=1`）。
- **成因**：`K-6-A2 D-2`（2026-08-08）將舊式「**於本函式整段移除·不留 fallback**」
  （`grep -n "於本函式整段移除" app.py` ⇒ **1**）；`k97_solve_alloc_t` 本身依 **D 波範圍界線 #5** 保留。
- 🔒 **`GB-14` 之結案結論不因錨失效而改變**，僅**憑據需另指**。
- **替代憑據（CC 自行現查·現存且穩定·四道指令均已原樣實跑）**：

```
grep -c "_D_at = D0 - max(0.0, m \* t_star)" app.py   ⇒ 1   （① 帶深＝範圍自身最小深度 D(t)）
grep -c "_den3 = c - k \* m" app.py                    ⇒ 1   （② 斜率正解 (c − k·m)）
grep -c "_k97 = k97_solve_alloc_t" app.py              ⇒ 0   （失效之舊錨）
grep -c "於本函式整段移除" app.py                       ⇒ 1   （成因）
```

⚠️ **反面提醒（防後人誤讀）**：`grep -c "k = sigma / den_a" app.py` ⇒ **3**，
然三處**全在註解／docstring**（活碼已改名為 `c`）⇒ ⛔ **不得當作「舊誤式仍活」之證據**。

⚠️ **技術處置**：`GB-14` 列在長表中段，改該列必產生刪除 ⇒ 改**插入一列**，
首格刻意**不匹配** `^| \*\*GB-` ⇒ 既有錨 `grep -c` **仍為 57**（§7-2 已驗）。

---

## §6 ✅ commit D：失敗考古**增節 61**

節號**以現查為準**：`grep -n "^## [0-9]" 該檔 | tail -1` ⇒ 既有最末 **60** ⇒ 新增為 **61**。
⛔ 既有各節一字不動。

節 61 ＝「**把 chat 側素材當作倉內事實，並據以要求 CC 更正**」。
**CC 側之正確處置**（`D-2b-29` 實作）已併記為對照：查無該句時**未編造、未以近似物代替**，
而是把加註寫成「**補記事實**」並貼出 0 命中之現查指令。

🩸 **同批續發之同族已併記**：本批 §1 之「KL 已於本則訊息附上」**為真**（三檔 md5 相符）
⇒ **本族之判別不在「單怎麼說」，而在「跑一次查得到與否」**；
⛔ **不得因前批被抓過，就預設本批之同型陳述亦為假——兩次都要查。**

---

## §7 全單驗收（⛔ 原樣貼）

### 7-1 `git diff --numstat a2e483d HEAD`（**刪除欄全 0**；`.py` 僅在 `verify/probes/`）

```
35	0	.claude/skills/failure-archaeology/SKILL.md
13	0	docs/W-G.9_第2宗最小規定範圍_範圍界定.md
1	0	docs/reports/W-G.4_泛用阻塞項登記表.md
63	0	docs/reports/W-G.8_D-2b-30_強制抵費地帶量測_事前登記.md
44	0	docs/rulings/K-6_街角地分配程序與可分配判準.md
-	-	docs/specs/figures/第2宗示意圖_第1種分配線方向.jpg
-	-	docs/specs/figures/第2宗示意圖_第1種分配線方向_1.jpg
-	-	docs/specs/figures/第2宗示意圖_第2種分配線方向.jpg
91	0	verify/out/probe_D2b30_forced_band_width.log
353	0	verify/probes/probe_D2b30_forced_band_width.py
```

⚠️ 三個 `.jpg` 之 numstat 為 `-　-`（**二進位**）⇒ 其「刪除欄 0」由 **git 之二進位標記**成立，
**非**逐行計數；⛔ 不得以行數論之。**圖檔具名已列於上表。**

### 7-2 `grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md` ⇒ **57**（未撐值）

### 7-3 md5 ⇒ 與 §1 對帳表**逐檔相符**（上引）

### 7-4 `grep` 自癒（原文與加註**同時**命中）

| 落點 | 指令 |
|---|---|
| §1 圖證結清 | `grep -n "圖證待補\|【圖證補入" docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| §2 `K-9-5-11` | `grep -n "^### 🔒 K-9-5-10 \|^### 🔒 K-9-5-11 " docs/rulings/K-6_街角地分配程序與可分配判準.md` |
| §4 判讀改標 | `grep -n "待實測坐實\|本判讀改標" docs/W-G.9_第2宗最小規定範圍_範圍界定.md` |
| §5 `GB-14` | `grep -n "^| \*\*GB-14\*\*\|^| ↳ 🔒 \*\*錨更正" docs/reports/W-G.4_泛用阻塞項登記表.md` |
| §6 考古 61 | `grep -n "^## 60\.\|^## 61\." .claude/skills/failure-archaeology/SKILL.md` |

### 7-5 `env | grep WV_BAKE` ⇒ **空**（`rc=1`）

### 7-6 commit 分立 ＋ 機械時序證明

```
$ git log --oneline a2e483d..HEAD
1d96ea5 D-2b-30 commit C【零碼】W-G.9 §2-1 判讀改標 ＋ GB-14 失效錨更正
71264c3 D-2b-30 commit B【零碼】強制抵費地帶寬量測（⛔ 只量不判·§3-4 四不）
660b9cf D-2b-30 §3-1 事前登記：強制抵費地帶寬度量測之可證偽預測
45657e3 D-2b-30 commit A【零碼】三圖入倉 ＋ K-9-5-11 入正典 ＋ 圖證待補段結清
$ git merge-base --is-ancestor 660b9cf 71264c3 ; echo rc=$?    ⇒ rc=0（登記先於結果）
$ git merge-base --is-ancestor 71264c3 660b9cf ; echo rc=$?    ⇒ rc=1（反向不成立）
```

### 7-7 §9 停機條件逐項

| # | 條件 | 判 |
|---|---|---|
| 1 | 三檔 md5 任一不符 | ❌ 未觸發（逐檔相符·blob 側亦同） |
| 2 | 圖面引文與圖不逐字相符 | ❌ 未觸發（§1-1 三項全部坐實；字形差額已具名、⛔ 未調整引文求相符） |
| 3 | §3-2 四項前提任一不符 | ❌ 未觸發（四項全部相符） |
| 4 | 觸及【裁】5／6 幾何細節或第 1 種／第 2 種之對應判定 | ❌ 未觸發（**本批明令不作·亦確未作**） |

### 7-8 §3-4／§0 不授權之逐項自證

| 項 | 狀態 |
|---|---|
| ⛔ 不動生產碼 | ✅ `git diff --stat a2e483d -- app.py verify/stepg_pipeline.py verify/selection_pipeline.py verify/run_verification.py verify/wf_f*.py` ⇒ 空 |
| ⛔ 新增 `.py` 只在 `verify/probes/` | ✅ 僅 `probe_D2b30_forced_band_width.py` |
| ⛔ 不設 `WV_BAKE`／不改錨值／不放寬閘 | ✅ `env` 空；`--numstat` 無 `verify/baselines/` 列 |
| ⛔ 不刪不停用不標作廢任何既有機具 | ✅ 刪除欄全 0（構造性證明） |
| ⛔ 不判缺陷／不改構造／不登記 `GB-xx`／不提修法 | ✅ §3 只出數字並標「待 KL 裁」；`GB-` 錨仍 57 |

---

## §8 次步

1. 🛑 **KL 裁 §3-4 之三格數字**（`C ≈ +1.7 m`／`D ≈ +72〜76 ㎡`）——是否為缺陷、如何處置。
2. ⚠️ 若 KL 欲檢定 §3-6 之 `P2`，**現有母體不足**（三適用格夾角幾乎相同）
   ⇒ 需**另設母體**方能檢定；⛔ 本批未代為設計。
3. 🛑 **`W-G.9 §2-1` 之重測**——圖已入倉但**禁令未解**；重測屬另批。
4. ⛔ **`D-3` 仍受 `K-9-5-10`【裁】1／2 拘束**：等結構閘另案修好之後才做，
   已過期之頂層 7 份採**丙案（KL 以 UI 重跑）**。
