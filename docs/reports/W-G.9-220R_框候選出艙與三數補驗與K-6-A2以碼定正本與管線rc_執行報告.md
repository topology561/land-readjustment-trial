# `W-G.9-220` 執行報告：框候選出艙 ＋ 三數補驗 ＋ `K-6-A2` 以碼定正本 ＋ 管線 `rc` 之 `GB`

> 🔒 **本檔＝本波唯一本體**；聊天僅為 ping。
> 🔒 **態**：落地前 `HEAD ＝ 821f8afd5b0481f9009c76fdadd6652f7b67cbf2`。
> 🔒 **⛔ 採用單所載之任何數為錨**——`259`／`262`／`272`／`275`／`143`／`144`／`26`／`19` 等**全數當場復算**，
> 復算值逐處以三軸出艙；與單所載不符者於本報告具名。
> 🔒 **零生產碼**：閘 P 五檔 `sha256` 落地前後逐位相符（`§八`）。
> 🔒 **本批鑄號 ＝ `GB-145` 一則；零自誤鑄號**（工項二 `c` 判「不構成」⇒ 結案；工項二 `d` 判「重疊」⇒ 停）。

---

## §一　工項一：三候選框之逐字出艙（唯讀·候發單側擇一）

### a　三候選之**逐字框定義**（正字形·`repr`·⛔ 敘述）

🔒 **三軸**：`(1)` 態 `821f8af…`／`(2)` 母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md`
倉側 blob **`515383`** B／**`5591`** 列／`(3)` 粒度框逐量載於表。

**基準（現行正字·`VR-091 四`）**
```
'^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`?'
```
**候選 `A`**（前綴另禁 `（`／`(`）
```
'^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`?'
```
**候選 `B`**（終結符集 `∪ {　, 半形空白, 〜}`）
```
'^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`?(?:[｜（　 〜]|$)'
```
**候選 `C`**（`A ∧ B`）
```
'^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`?(?:[｜（　 〜]|$)'
```
**範圍框**（四者共用）
```
'^#+[^\n]*?自誤[\s　`]*`?([0-9]{1,4})`?[\s　`]*[〜～\-–~]+[\s　`]*`?([0-9]{1,4})`?'
```

### 五數對照（現態·⛔ 引 `-218R` 之數）

| 候選 | 相異〔集合基數〕 | 單筆列命中〔列框〕 | 範圍條數〔列框〕 | `MAX` | 缺號清單〔相異之補集〕 |
|---|---|---|---|---|---|
| 基準 | **`268`** | **`262`** | **`8`** | **`275`** | **`[106]`** |
| `A` | **`268`** | **`261`** | **`8`** | **`275`** | **`[106]`** |
| `B` | **`268`** | **`262`** | **`8`** | **`275`** | **`[106]`** |
| `C` | **`268`** | **`261`** | **`8`** | **`275`** | **`[106]`** |

🛑 **與 `-218R` 之數不同係態之差、⛔ 矛盾**：`-218R` 之量測於 `73537ee`（落地前），本表於 `821f8af`（`273`／`274`／`275` 落地後）
⇒ 相異 `265→268`、列命中 `259→262`、`MAX` `272→275`（皆恰 `+3`）。⛔ 追改 `-218R` 之既出數。

### b　四款驗收之逐款實測

| 候選 | `①` 相異∧MAX∧缺號同基準 | `②` 列命中 < 基準 | `③` 新∖舊 ＝ `∅` | `④` 對照組 | 總判 |
|---|---|---|---|---|---|
| `A` | ✅ | ✅（`Δ −1`） | ✅ | 定義·U+3000 `1`／定義·｜ `1`／定義·（ `1`／**引用·括號 `0`**／🔴 **引用·無括號 `1`** | 🔴 **未全過**（`④`） |
| `B` | ✅ | 🔴（`Δ +0`） | ✅ | 定義三形皆 `1`／🔴 引用·括號 `1`／🔴 引用·無括號 `1` | 🔴 **未全過**（`②④`） |
| `C` | ✅ | ✅（`Δ −1`） | ✅ | 定義·U+3000 `1`／定義·｜ `1`／定義·（ `1`／**引用·括號 `0`**／🔴 **引用·無括號 `1`** | 🔴 **未全過**（`④`） |

🔒 **對照組之期望**（人造列·⛔ 取自倉）：定義三形須 `1`；**引用二形須 `0`**。

🛑 **與 `-218R` 之「`A`／`C` 四款全過」不符——成因具名**：`-218R` 之 `④` 只跑**四**條對照列
（定義×3 ＋ 引用·括號×1），**未含**「引用·**無括號**」一形。本單 `工項一 c` 令具名該形
⇒ 本報告以**五**條對照列重跑 ⇒ `A`／`C` 於 `④` **不過**。
🔒 **⛔ 追改 `-218R` 之既出判**（`常規四（九）五`）；其判之**射程**即該四條對照列。

**`A`／`C` 之差之逐列具名**（基準有而候選無·**`1`** 列·二候選同）：

| 列 | 擷取 | 逐字 |
|---|---|---|
| `:5363` | `257` | `### 族之具名（依 \`自誤 257\` 之要求）` |

### c　殘餘漏洞之分辨：**無括號引用形**⛔ 實存 ⇒ **僅為構造**

🔒 **判別法（機械·⛔ 人工挑）**：取候選 `A` 之全部命中，切出「`#+` 與『自誤』之間」之字串，
去裝飾（空白／全形空白／反引號／emoji／符號）後**僅留中日韓文字與英數** ⇒ 依該「前綴文字」分群。
**定義形之特徵 ＝ 前綴文字為空**（`## 自誤 …`／`### 🩸 自誤 …`／`## 🔧 自誤 …`）；
**引用形之特徵 ＝ 前綴含文字**（`### 族之具名（依 …`）。

| 前綴文字 | 列 | 判 |
|---|---|---|
| （空） | **`261`** | ✅ **定義形**（純裝飾前綴） |
| 非空 | **`0`** | — |

⇒ 🔒 **候選 `A` 之 `261` 列命中<u>全部</u>為定義形** ⇒ **「無括號引用形」於現態自誤簿內⛔ 實存**。

🔒 **該構造字串本身（⛔ 取自倉·人造）**：
```
### 併看 `自誤 999` 之形
```
其於四框之命中：基準 **`1`**／`A` **`1`**／`B` **`1`**／`C` **`1`**（**四者皆命中**）。

🛑 ⇒ **`④` 之不過係對<u>零實例</u>之形而言**——`A`／`C` 於**現態母體**上與「定義形／引用形」之分辨
**完全正確**（`261` 定義形全中、唯一引用形 `:5363` 全不中）。**⛔ 由 CC 據此擇一或調框**：
「是否要求框對零實例之形亦具判別力」屬**發單側之裁**。

🛑 **停·候發單側裁**（⛔ 落地任一候選·⛔ 擇一·⛔ 調框）。

---

## §二　工項二：落地後三數之補驗（`VR-091` 補款五之自我適用）

### a　自誤簿之三數

🔒 `(1)` 來源 commit ＝ `821f8afd5b0481f9009c76fdadd6652f7b67cbf2`
🔒 `(2)` 母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 倉側 blob **`515383`** B／**`5591`** 列（單檔·無哨兵）
🔒 `(3)` 粒度框 ＝ 相異〔去重集合基數〕／`MAX`〔集合最大元〕／缺號〔相異之補集·**集合框**〕

⇒ 相異 **`268`**【號】／`MAX` **`275`**／缺號 **`[106]`**／（併）單筆列命中 **`262`**【列】／範圍 **`8`**【列】

🔒 **期望關係之當場復算**（⛔ 採單之數）：`-218R` 落地前為 相異 `265`／`MAX` `272`（本報告 `§一` 之三軸同源復算）；
本批所鑄之自誤號數 ＝ **`3`**（`273`／`274`／`275`）⇒ 期望 相異 `265+3 = 268` ✅／`MAX` `272+3 = 275` ✅／缺號不變 `[106]` ✅。
🛑 **`VR-091` 補款五**：缺號清單 ＝ `[106]` ⇒ ✅（⛔ 停機）。**次號 ＝ `276`。**

### b　`GB` 側之三數

🔒 `(1)` `821f8af…`／`(2)` `docs/reports/W-G.4_泛用阻塞項登記表.md` **`597974`** B／**`3813`** 列／
`(3)` 框 `'^#{2,4} \`?GB-([0-9]{1,4})\`?'`·扣 `[997]`

⇒ 相異 **`58`**【號】／`MIN` **`72`**／`MAX` **`144`**／缺號 **`15`** 個 ＝
`[87, 88, 89, 91, 93, 94, 95, 96, 97, 98, 99, 100, 101, 102, 103]`／列命中 **`61`**【列】
⇒ **次號 ＝ `145`**（落地前 `GB-145` 標題命中 ＝ **`0`** ✅）。

### c　`-218R` 是否構成**出艙不全**（違 `VR-091 一`）⇒ 🟢 **不構成**

🔒 母體 ＝ `docs/reports/W-G.9-218R_定義框與清單正本與退縮三層與側別敏感度_執行報告.md`
倉側 blob **`25055`** B／**`415`** 列。四量之列命中：
`相異` **`7`**【列】`[34, 38, 46, 71, 111, 173, 361]`／`列命中` **`11`**【列】／
`MAX` **`7`**【列】`[37, 46, 52, 71, 111, 173, 361]`／`缺號` **`8`**【列】`[38, 40, 46, 71, 111, 174, 366, 367]`。

**四量同列並出之列（＝ 出艙之所在）**：

| 列 | 逐字（節錄） |
|---|---|
| `:71` | `\| 候選 \| 式之特徵 \| 相異 \| 單筆列命中 \| \`MAX\` \| 缺號 \| 四款總判 \|` |
| `:361` | `\| 族 \| 落地前 相異／列命中／\`MAX\` \| payload 內 相異／列命中 \| 落地後 相異／列命中／\`MAX\` \| 期望（機械算） \| 判 \|` |
| `:40` | `缺號清單：舊 \`[106]\` ／ 新 \`[106]\`。` |
| `:366` | `🛑 **\`VR-091\` 補款五**：\`自誤\` 缺號清單 ＝ **\`[106]\`** ⇒ ✅（器紅／新漏皆不成立）。` |

⇒ 🟢 **報告本體已於 `:34`–`:40`（五數對照表）、`:71`（候選表）、`:361`–`:366`（閘 7 表）
逐處並出 `相異`／`單筆列命中`／`MAX`／`缺號` 四量** ⇒ **⛔ 構成出艙不全**。
🔒 **⛔ 鑄號、⛔ 回改 `-218R` 一字** ⇒ **結案**。
（併記：`-218R` 之**聊天回報**依 `工作紀律`「完工聊天回報」條所令，其內容**限於** `git log -1` 之原樣輸出 ＋ 倉內報告路徑，
**⛔ 承載任何數** ⇒ 該處⛔ 出艙「列命中」或任何其他數。）

### d　鑄號受詞對照表 ＋ 與 `自誤 272` 之射程重疊 ⇒ 🛑 **重疊·停**

| 號 | 標題列 | 受詞（一句） |
|---|---|---|
| `自誤 273` | `:5445` | `自誤` 之**定義框**咬中「小節標題內對他號之引用」⇒ 出艙「單筆列命中」恆偏高 |
| `自誤 274` | `:5496` | `CLAUDE.md` 待落地清單之**四列 `⬜` 標記本身為誤** |
| `自誤 275` | `:5536` | 以**測試參數下之輸出**（`226.01 ㎡`）為案件事實而⛔ 載情境軸 |

🟢 **`273` 與 `275` 之射程與 `272` ⛔ 重疊**——`272` 之受詞為「單之前提未經 grep」，
其附表⛔ 涉「框之判別力」（`273`）亦⛔ 涉「情境軸」（`275`）。

🔴 **`274` 與 `272` <u>射程重疊</u>——機械坐實**：於 `272` 節內（`:5309` 起）含 `⬜` 且含
`不符`／`清單`／`未開工` 之列 ＝ **`10`**【列】，其中：

| 列 | 逐字（節錄） |
|---|---|
| `:5391` | `> **四項係「部分落地」而清單載為「⬜ 未開工」**（見下表）。` |
| `:5410` | `…🔴 清單 \`:591\` 載「⬜ 未開工」⇒ **與實測不符** \|` |
| `:5413` | `…🔴 清單 \`:594\`／\`:1098\` 載「⬜ 未開工」⇒ **與實測不符** \|` |
| `:5419` | `🔒 **本表之核心發現**：主鏈六項中，**\`1\`／\`2\`／\`3\`／\`5\` 四項係「部分落地」而清單載為「⬜ 未開工」**` |

⇒ 🔴 **`272` 已以「四列 `⬜` 與實測不符」為其<u>核心發現</u>**，而 `274` 之受詞逐字即該四列
⇒ **事實層完全重疊**。`274` 自載之區辨（`:5505` 受詞欄／`:5507` 成因-結果欄／`:5510` ⛔ 重複計數）
係**成因／結果**與**存在期間**之框，**⛔ 受詞之相異**。

🛑 **依單「發現重疊 ⇒ 停並回報，⛔ 自行合併或作廢任一號」⇒ 停。**
⛔ 本批未合併、未作廢、未回改 `274` 或 `272` 一字。
🔒 **CC 之自陳**：`274` 係 CC 於 `-218R` 依單所鑄，鑄時之區辨理由已逐字載於 `:5505`–`:5510`；
本次以**機械**（`272` 節內 `⬜`＋`不符`／`未開工` 之列命中）復驗，該區辨**於事實層不成立**。
🔒 **對照組（器非紅）**：`272` 節內含 `⬜` 之列 ＝ **`14`**（須 > `0`）／含 `NOSUCHMARK` ＝ **`0`**（須 `0`）。

---

## §三　工項三：`K-6-A2` 以碼定正本（發單側已裁）

### a　正典所令（逐字錨）

| 錨 | 逐字要旨 |
|---|---|
| `K-6:18`–`:20`（§零-1） | **寬深雙檢** ＝ 最小寬度 ≥ 法定最小寬度 **且** 最小深度 ≥ 法定最小深度；寬依 N-14（`parcel_min_width_n14`），深依既鎖定義（FRONTLINE 法向垂距取 min）。**兩者皆須滿足** |
| `K-6:1081`–`:1089`（**`K-9-3-1`**·KL 裁 `2026-08-05`） | 街廓層通過 ⇒ 宗地層**不再以宗地深度比對附表最小深度**；⛔「量測各宗自身最小深度」在拆除之列（仍為**必要中間量·須實量**）⇒ **宗地層之判定僅餘一項：帶內最小寬度 ≥ 畸零地最小寬度** |
| `K-6:2645`／`:2670`–`:2673`（**`K-9-5-15 二`**·KL 裁 `2026-08-18`） | 街角規定範圍由**平移規則單獨決定**、**與 `G` 無關**、構造保證最小寬 ≥ 畸零地最小寬 ⇒ 🔴 **街角第 1 宗⛔ 不另設寬度閘、⛔ 不另跑矩形容納測試** |
| `K-6:2680`–`:2683`（**`K-9-5-15 四`**） | 第 2 宗與宗地層採**最小矩形**（`K-9-12`）；`K-9-12-b` 逐字「已裁定、⛔ 生產碼未實作」；**現行仍走 `parcel_min_width_n14`** ⇒ **本裁⛔ 未使其落地** |
| `K-6:2685`–`:2690`（**`K-9-5-15 五`**） | 角地寬深**一律以截角前形狀量測** ⇒「截角過大放不下最小矩形」⛔ 存在 |

### b　碼面實測（受詞 ＝ 倉側 blob·**列框**·母體 ＝ 生產碼五檔 `23906`／`628`／`1474`／`1454`／`377` 列）

| 受詞 | 實測 | 判 |
|---|---|---|
| 寬檢函式 | `def parcel_min_width_n14` @ `app.py:6675`（`def …` 之列命中 `4`·餘三為註／錨） | ✅ 存在 |
| 寬檢**接線** | `app.py:7107`–`:7109` `_w = parcel_min_width_n14(… , baseline_pts=baseline_pts)`；外層 `evaluate_parcel_width_n14` 之**生產呼叫點** ＝ `app.py:23026`（該符號 `app.py` `7` 列／`verify/*` 皆 `0`） | ✅ **接於主分配路徑** |
| 寬檢之**消費端** | `WIDTH_VERDICT_BAD` @ `:7003`·設於 `:7116`；`_below_min_width=True` @ `:7117`；讀於 `:10394` `_below_width = (_wv == WIDTH_VERDICT_BAD)` 與 `:23041`；唯一活消費端 `merge_subparcels_by_parent` @ `:10315`·呼叫於 `:23078` | ✅ **有下游後果**（⛔ 觀測模式） |
| `K-9-3-1-a` 之新式帶**生產切換** | `app.py:7109` 逐字含 `baseline_pts=baseline_pts`（`_n14_band_hi` **`9`** 列／`baseline_pts` **`55`** 列） | ✅ **已切** |
| **街角第 1 宗**之寬度判定 | `app.py:7074` `if str((row or {}).get('街角地', '')).strip() == '是':` ⇒ 逕回 `WIDTH_VERDICT_CORNER_K4`（`:7020` ＝ `'合格（K-4 面積門檻蘊含）'`）·**⛔ 量測** | ✅ 合於 `K-9-5-15 二` |
| **街角 PK 之資格門檻** | `_pk_one_side_v12` @ `app.py:12975`–`:13087`（**`113`** 列）內 `parcel_min_width_n14` **`0`**／`min_depth` **`0`**／`寬` **`0`**／`深` **`0`**；唯一門檻 `:13016` `if cand_G < cand.get('min_area_to_apply', 0):`；契約逐字 `:13377` | ✅ 合於 `K-9-5-15 二` |
| **深軸**（宗地層比對附表深度） | 碼面⛔ 存在該比對 | ✅ 合於 `K-9-3-1` |
| **最小矩形**（`K-9-12`） | `_rect_fits_free_pose` @ `app.py:11867` 存在（`rect_fits` **`7`**／`rect_fit` **`15`**／`K-9-12` **`14`**／`fits_at` **`0`**）；`app.py:11755` 逐字「⛔ 純函式·**觀測模式**」 | 🔶 **建置未接線** |

🔒 **對照組（證器非紅）**：`k6a2_width_depth_dual_check` **`0`**／`parcel_min_depth_n14x` **`0`**／
`_n14_band_hi_nosuch` **`0`**（三者須 `0`）；`def parcel_min_width_n14` **`4`**／`min_area_to_apply` **`11`**（須 > `0`）⇒ ✅。

### 🛑 `d` 之停機條件 ⇒ 🟢 **⛔ 觸發**（三條·⛔ 缺一）

1. 街角第 1 宗⛔ 設寬度閘 ＝ **KL 裁 `2026-08-18`（`K-9-5-15 二`）之明令** ⇒ 碼面行為與**最新**正典一致、⛔ 缺失。
2. 宗地層⛔ 比附表深度 ＝ **KL 裁 `2026-08-05`（`K-9-3-1`）之明令** ⇒ ⛔ 缺失。
3. 餘之未落地（第 2 宗與宗地層改採最小矩形）＝ **既具名待落地項**（更正五正本表 `序 2`），
   且 `K-9-5-15 四` 逐字自載「本裁⛔ 未使其落地」⇒ ⛔ 新缺陷。

🔒 **曾具名之唯一工作例已作廢**：`3.5m R1/right 628(5)` 之 `6.847031 < 7.00`——
其數字已於 `2026-08-02` 明令「**全部作廢，不得引用**」（帶深定義錯誤·`K-9-6`），
`GB-84` 並據此結案（KL 裁 `2026-08-19`·`docs/reports/W-G.4_泛用阻塞項登記表.md:1701`–`:1710`）。

### c　落地（`CLAUDE.md` 末端追加·⛔ 回改 `:1277`／`:564` 本文一字）

已追加 `## 🔧 \`K-6-A2\` 寬深雙檢之**以碼定正本**（\`W-G.9-220\`·⛔ 上文一字不刪·純末端追加）`，
其節內載：正典五錨 ＋ 碼面九列實測 ＋ 對照組 ＋ 六條結論，並逐字指向
**清單側** `CLAUDE.md:1277`（`- ⬜ \`K-6-A2\` 寬深雙檢`）與**更正節** `CLAUDE.md:564`
（`🔒 **\`K-6-A2\` ＝ 🔶 <u>部分已落地</u>，⛔ 非「未開工」。**`）。

🔒 **本裁之理由（一句）**：`K-6-A2` 之在否係**碼之事實**；碼面實測顯示寬檢**存在且接線且有下游後果**
⇒ `:564` 之「🔶 部分已落地」為真、`:1277` 之「`⬜`」為誤。

### 🔴 併呈之過期陳述二處（**具名·⛔ 追改**）

| 處 | 逐字（節錄） | 現況實測 |
|---|---|---|
| `app.py:7047`–`:7057` docstring | 「🔴 **`K-9-5` 已撤銷本豁免**（K-7 作廢·KL 裁 2026-08-01）」「⚠️ 本函式**尚未實作實量**」 | 該段係 `2026-08-01` 之態，**已由 `K-9-5-15`（`2026-08-18`）取代**——後裁令**⛔ 另設寬度閘** ⇒ **註解過期**（屬波末批） |
| `K-6:1095`（`K-9-3-1-a`） | 「🔴 **生產尚未切換**（`grep -n -A2 "^    _w = parcel_min_width_n14" app.py` 之四行內**不含** `baseline_pts`）」 | 🔴 **與現態不符**——`app.py:7109` 逐字含 `baseline_pts=baseline_pts` ⇒ **已切** |

🛑 **二者皆⛔ 由 CC 逕改**（一為生產碼、一為正典）；本報告僅具名。

### 🔒 「面積門檻過 ⇒ 寬度自動滿足」之二軸辨（單所令）

- **正典所保證者 ＝ 僅寬軸**：`K-9-5-15 二` 逐字「面積夠 ⇒ 街角第 1 宗**寬度**合格」。
- **碼面之蘊含 ＝ 二軸**：`app.py:7044`–`:7046` docstring 逐字「『G ≥ 街角最小分配面積』**蘊含**『寬度、**深度**皆合格』」。
- ⇒ 🔴 **碼面之蘊含比正典寬<u>一軸</u>**（多了深軸）。
- 🔒 **惟結論不變、理由不同**：深軸之「宗地層不比附表深度」另由 **`K-9-3-1`** 所令
  ⇒ 屬**註解措辭**問題、**⛔ 行為缺陷**。**二軸⛔ 混**——寬軸靠**構造保證**，深軸靠**街廓層蘊含**。

---

## §四　工項四：管線 `rc` 之 `GB` ＋ 回溯掃描

### a　所鑄之號 ＝ `GB-145`（推導式）

**受詞**：閘之 `rc` 取自**管線之後**（或 `;` 串接之尾端）⇒ 其判別力為**零**。
**推導式**：`cmd | head; echo "rc=$?"` 之 `$?` ＝ `head` 之碼，而 `head` 正常結束**恆回 `0`**
⇒ 前段失敗被靜默吞掉 ⇒ **無論受測物成敗皆綠**。
**族**：與 `GB-143` 同族（**閘恆綠、判別力為零**），**失真層不同**——`143` 在**字面落地層**、本則在 **`rc` 取值層**。
**⛔ 重複鑄之自證**：本受詞於**自誤側**已登記二處（`自誤 37` @ 自誤登記 `:695`；`自誤 78` @ `:1917`–`:1937`，
其通則 `67.` 逐字「退出碼⛔ 不得取自管線或以 `;` 串接之尾端」），惟該二者係「**已發生之誤**」。
🔒 **`GB` 側⛔ 有本受詞之任何覆蓋**——當場復算（母體 ＝ `GB` 登記表倉側 blob **`597974`** B／**`3813`** 列·**列框**）：
`$?` **`0`**／`退出碼` **`0`**／`exit code` **`0`**／`PIPESTATUS` **`0`**／`pipefail` **`0`**；
對照組 `GB-` ＝ **`588`**（須 > `0`）／`GB-NOSUCH-XX` ＝ **`0`**（須 `0`）⇒ 器非紅。
⇒ **新值域 ＝ 由「個案之誤」升為「泛用阻塞項之【必觸】通則」**，並具名倉內未用過之二手段。

**🔒 判別力自證（活體工作例·本窗當場發生·⛔ 歷史記述）**：
本窗工項六之前置量測中曾以 `git grep -c -F '226.019999' … | head -3; echo "rc=$?"` 取零對照之 `rc`，得 **`rc=0`**；
改為**不經管線直取**後實得 **`rc=1`** ⇒ **同一受詞、同一時刻，二形給出相反之 `rc`**。

### b　回溯窮舉掃描

🔒 **母體** ＝ `docs/` ＋ `verify/` 之**非二進位追蹤檔** **`1264`**【檔】／Σ **`94257323`** B·**列框**。

**🔒 變體之窮舉（先窮舉再斷言）**

| # | 變體 | 列 |
|---|---|---|
| `V1` | `rc=$?` | **`35`** |
| `V2` | `echo rc=$?`（含引號變體） | **`19`** |
| `V3` | `$?` 之一切出現 | **`63`** |
| `V4` | `if [ $?` | **`0`** |
| `V5` | `$? -eq/-ne/-gt/-lt` | **`0`** |
| `V6` | `; echo $?` | **`10`** |
| `V7` | `\|\| echo` | **`1`** |
| `V8` | `PIPESTATUS` | **`0`** |
| `V9` | `pipefail` | **`1`** |
| `V10` | `returncode`（Python 側之對應形） | **`37`** |
| `V11` | `check=False`（Python 側吞 rc 之形） | **`0`** |

**濾網（含 `$?` 且同列或上一列有管線 `|`）** ⇒ 命中 **`13`**【列】／**`11`**【檔】。

🛑 **該濾網有<u>偽陽</u>——當場自陳**：`13` 列中 **`9`** 列之 `|` 落於**引號內之 regex 交替**
（`diff <(grep -E "✅ PASS|🔴 FAIL" …) <(…); echo rc=$?`）⇒ **非 shell 管線**，
且該形之 `$?` 由 shell 於 `echo` 執行**前**展開 ⇒ 取到的正是 `diff` 之碼、**⛔ 誤**。
真管線者 ＝ **`4`** 列（`自誤登記:700` `| tail -6`／`:1917` `| tail -5`／
`docs/reports/W-G.9-58_CC交接文.md:38` `cmd | tail`／`docs/驗證裁定登記表.md:1389` `git clone … | tail`），
另 `W-G.9-143_CC交接文.md:125` 與 `W-G.9-81b_CC交接文.md:42` 之 `\|` 係 md 表格轉義之管線（**描述形**）。

**🔴 逐處判「是否曾作為某閘之判準」** ⇒ **`63` 列全數為<u>描述／警語／歷史報告之記述</u>**，
⛔ 有任何一處係**現行某閘之判準**。逐族具名：

| 族 | 代表列 | 性質 |
|---|---|---|
| 既有登記之警語 | `自誤登記:695`（`自誤 37` 標題）／`:710`／`:1937`（通則 `67.`） | 警語 |
| 交接文之陷阱條 | `W-G.9-58:38`／`W-G.9-100:183`／`W-G.9-143:125`／`W-G.9-81b:42`／`W-G.9-89b:32`／`W-G.9-63:28`／`W-G.9-68:35`／`W-G.9-75:42` | 警語 |
| **正寫法**之範本 | `W-G.5_*:209`／`:265`／`docs/specs/W-G.5_裁定N_E系列_BLOCKED_plan.md:282`（`… > log 2>&1; rc=$?; … ; exit $rc`） | ✅ 已合規 |
| 歷史閘之調用（`diff <(…) <(…); echo rc=$?`） | `W-G.9-4b:187`／`-6:267`／`-7:214`／`-8:176`／`-9:212`／`-10:215`／`-11:230`／`-12:206` | ✅ **無管線** ⇒ `$?` 取到 `diff` 之碼·**正確** |
| `merge-base --is-ancestor …; echo $?` | `W-G.8_D-2b-30:280`／`:281`／`W-G.8_D-2b-31:171`–`:173`／`W-G.9-97補正③:39`／`-98:41`／`-98補正①:20` | ✅ **無管線**·正確 |

⇒ 🟢 **「判別力待復驗之閘」清單 ＝ <u>空</u>**（**`0`** 項）。
🔒 **零對照（⛔ 經管線·`returncode` 直取）**：`rc=$?NOSUCH` ＝ **`0`**（須 `0`）／
`$?` ＝ **`63`**／`PIPESTATUS` ＝ **`0`**／`pipefail` ＝ **`1`** ⇒ 器非紅。

### c　⛔ 於本批重跑任何歷批之閘 ✅（本批僅產出上開清單·清單為空 ⇒ ⛔ 併期末批之處置項）

---

## §五　工項五：探針之歸位驗算

| 量 | 值 |
|---|---|
| 路徑 | `verify/probes/probe_WG9218R_setback_sensitivity.py` |
| 倉側 blob | **`14018`** B |
| 內容 `sha256` | `5f989dab14dcece632f97580b91f168c6c68dc30db136b69d1e169a7f434365b` |
| 行尾 | `CR` ＝ **`0`** ／ `LF` ＝ **`272`** ⇒ **純 LF**（合 `verify/probes/` 既有 `8` 檔之既例·皆 `CR=0`） |
| blob `sha1`（自算 `sha1(b"blob %d\\0" + content)`） | `b0e17c05a792a0a44dbd1a65c3417841d6fdfa15` |
| `git rev-parse 821f8af:<path>` | `b0e17c05a792a0a44dbd1a65c3417841d6fdfa15` ⇒ ✅ **交叉相符** |

**`import`／`from` 之命中**（母體 ＝ 生產碼 **`5`** 檔 ＋ 全倉 `*.py` **`247`** 檔·扣探針自身）：

| 字樣 | 五檔 | 全倉 |
|---|---|---|
| `import probe_WG9218R_setback_sensitivity` | `0` | `0` |
| `from probe_WG9218R_setback_sensitivity` | `0` | `0` |
| `probe_WG9218R` | `0` | `0` |
| `setback_sensitivity` | `0` | `0` |
| `probes.probe_WG9218R` | `0` | `0` |
| `verify.probes` | `0` | `0` |
| **合計** | **`0`** ✅ | **`0`** ✅ |

🔒 **零對照（器非紅）**：`import subprocess` 於全倉 `*.py` ＝ **`90`**【列】（須 > `0`）⇒ ✅。

---

## §六　所鑄各號之推導式（彙總）

| 號 | 受詞 | 推導式（一句） |
|---|---|---|
| `GB-145` | 閘之 `rc` 取自管線之後 ⇒ 判別力為零 | `cmd \| head; echo "rc=$?"` 之 `$?` ＝ `head` 之碼、`head` 恆回 `0` ⇒ 前段失敗被吞 ⇒ 閘恆綠 |

🔒 **本批零自誤鑄號**——工項二 `c` 判「不構成出艙不全」⇒ 結案·⛔ 鑄；工項二 `d` 判「射程重疊」⇒ 停·⛔ 處置。

---

## §七　閘（`常規補款一`：閘之量測與其所守護之動作**分屬不同命令塊**）

### 閘 `1`〜`6`：append-only 位元組級自證

| 檔 | `HEAD` B | 索引 B | `Δ` | payload B | `Δ−payload` | 嚴格前綴 | 檔尾 `≡` payload | `CR` | 哨兵 |
|---|---|---|---|---|---|---|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` | **`597974`** | **`601930`** | **`+3956`** | **`3956`** | **`0`** | ✅ | ✅ | **`0`** | ✅ `1` |
| `CLAUDE.md` | **`130188`** | **`137408`** | **`+7220`** | **`7220`** | **`0`** | ✅ | ✅ | **`0`** | ✅ `1` |

🔒 **`戒 40` 之二端**：`HEAD B` 與 `索引 B` 同格並列；`Δ−payload` 全 **`0`** ⇒ 純末端追加。

### 閘 `7`：反向框重算（期望**由機械算出**·三者同源）

| 族 | 落地前 相異／列命中／`MAX` | payload 內 相異／列命中 | 落地後 相異／列命中／`MAX` | 缺號 | 期望 | 判 |
|---|---|---|---|---|---|---|
| `自誤` | `268`／`262`／`275` | **`0`（∅）／`0`** | `268`／`262`／`275` | `[106]` | `268`／`262` | ✅ |
| `GB` | `58`／`61`／`144` | `1`（`[145]`）／`1` | **`59`／`62`／`145`** | `15` 個·同落地前 | `59`／`62` | ✅ |

🛑 `VR-091` 補款五：`自誤` 缺號清單 ＝ `[106]` ⇒ ✅。
🔒 **本批零自誤鑄號之機驗**：payload 內 `自誤` 相異 ＝ **`0`**、列命中 ＝ **`0`** ⇒ ✅。
🔒 **次號**：`自誤` **`276`**／`GB` **`146`**。

### 閘 `8`：`⬜` 之自指增長（`CLAUDE.md`）

| | 字元框 | 列框 |
|---|---|---|
| 落地前 | `56` | `50` |
| **payload 內** | **`5`** | **`3`** |
| 落地後 | `61` | `53` |

### 閘 `Q`：🔒 `GB-145` 之**自我適用**

本閘之一切 `rc` **⛔ 經管線**——`numstat` 之取法 ＝ `subprocess.run(...).returncode` **直取**
（⛔ shell、⛔ 管線）⇒ `rc` ＝ **`0`**；`deletions ≠ 0` 之檔 ＝ **`0`** ✅。

---

## §八　閘 P：生產碼五檔之 `sha256`（落地**前後**逐位相符）

| 檔 | `HEAD` B ＝ 索引 B | `sha256`（前 ＝ 後） | 判 |
|---|---|---|---|
| `app.py` | `1414673` | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` | ✅ 未動 |
| `verify/selection_pipeline.py` | `35367` | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` | ✅ 未動 |
| `verify/run_verification.py` | `96923` | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` | ✅ 未動 |
| `verify/stepg_pipeline.py` | `96261` | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` | ✅ 未動 |
| `verify/run_all.py` | `24681` | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` | ✅ 未動 |

🔒 **`deletions ＝ 0`**（`git diff --cached --numstat -z HEAD`·`returncode` 直取）：
`60 0 CLAUDE.md`／`63 0 docs/reports/W-G.4_泛用阻塞項登記表.md`。

---

## §九　🛑 候裁／停機（照實·⛔ 頂替）

1. 🛑 **工項一 `d`／`e`：停**（承 `-218R`）——`A`／`C` 於**現態母體**上分辨完全正確（`261` 定義形全中·唯一引用形全不中），
   惟於「**無括號引用形**」（**零實例**·純構造）之對照列上仍命中 `1` ⇒ `④` 不過。
   **是否要求框對零實例之形亦具判別力，屬發單側之裁。** ⛔ 由 CC 擇一／調框／落地。
2. 🛑 **工項二 `d`：停**——`自誤 274` 與 `自誤 272` **射程於事實層重疊**（機械坐實·`272` 節內 `10` 列）。
   ⛔ 自行合併或作廢任一號。**候發單側裁。**
3. 🟢 **工項三 `d`：⛔ 觸發** ⇒ 已辦 `c`（`CLAUDE.md` 末端追加）。
   併呈**過期陳述二處**（`app.py:7047`–`:7057` docstring／`K-6:1095`），**⛔ 由 CC 逕改**。
4. 🟢 **工項四 `b`：「判別力待復驗之閘」清單 ＝ 空**（`0` 項）⇒ ⛔ 併期末批之處置項。
5. 🔴 **`K-9-5-2-a`-3 與 `K-9-5-3` 之逐字互斥**——`GB-84` 雖已結案，其結案**⛔ 蘊含**該互斥已解決
   （`W-G.4:1712`–`:1714` 逐字：「⛔ **`K-9-5-3` <u>未</u>失效**」「⛔ **二則之逐字互斥<u>未</u>解決**」）
   ⇒ **仍待 KL 協調**·⛔ CC 自裁孰先。
