# W-G.5 — **K-6-A2 段二-1(c)：K-9-4 BASELINE 臨接閘上閘**

> **基準**：`ead148b`　**分支**：`wip/s1-endpart`
> **性質**：**上停機閘·有行為後果**（本案零觸發，惟閘已生效）。
> **前置**：段二-1(a) 已完成（只量不判·零違例·`ead148b`）；KL 已就 §二(b) 放行。
> ⚠️ 本單所引行號**全部當場重 grep**，未沿用施工單數字；差異見 §七。

---

## 一、KL 四則裁定入正典（施工單 §一·**逐字**）

新增 **K-9-4-1**（`grep -n "^### 🔒 K-9-4-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`），
四則逐字收錄：① 上閘、② 圖層來源、③ 兩排之作法、④ 共用。

並補 **K-9-4-1-a**（對實作之直接效力）與 **K-9-4-1-b**（本案圖面之實測結構）。
K-9 §一 五則表之 K-9-4 列已加指標與「✅已實作」。

---

## 二、上閘（**雙向咬**）

### 2-A 新增二函式（module 級·app 與 harness 同源）

| 符號 | 職掌 |
|---|---|
| `k94_partition_land_rows`（`grep -n "def k94_partition_land_rows" app.py`） | 界定 K-9-4 之母體＝**受配土地之宗** |
| `k94_assert_baseline_touch`（`grep -n "def k94_assert_baseline_touch" app.py`） | **上閘**·雙向咬·停機 |

⛔ 量測函式 `k94_baseline_touch_by_parcel` 與量測軸 `_baseline_normal_axis`
**判準、欄位、回傳一字未動**（施工單 §〇-3）。

### 2-B 母體之界定（**一個施工單未預見、但必須處理之情形**）

`g_rows` 除受配土地之宗外，另含 **`💰 現金補償`** 與 **`🟠 孤立公設地`** 二類，
其 `cut_coords` **依構造為 `None`**（`grep -n "'推進側別': '💰 現金補償'" app.py`／
`grep -n "'推進側別': '🟠 孤立公設地'" app.py`），法定敘明為
「無法調配至其他街廓，建議領錢及申請合併分配」⇒ **未受配土地**。

⇒ 若逕行納入母體，閘會對「**依法本就不配地者**」報「未配到屁股線」＝**假停機**。
施工單 §二 母體寫「全部**分配宗地**（含抵費地宗）」——**抵費地宗有地、現金補償無地**
⇒ 排除該二類**與施工單原文一致**，非擅自縮母體。

🔒 **以「推進側別」正面分類，⛔ 不以 `cut_coords is None` 反面篩**——
後者會把**真正的幾何缺失**（上游 bug）一併靜默吸收成「無地宗」。
🔒 **並附雙向自檢**：有地者須有幾何、無地者須無幾何，任一違反即 **loud raise**
⇒ `K94_NO_LAND_SIDES` 之字面若與上游脫鉤，**會被自檢咬出、不致靜默**。

**本案實測**：受配土地之宗 **68**、無地宗 **0**（本案無現金補償／孤立公設地）。

### 2-C 🔴 雙向咬之**互斥處理**（§四 (ii) 案實測發現之缺陷·已修）

初版把 `_miss`（`not touch`）與 `_cross` 各自獨立判斷、**先報 `_miss`**。
§四 (ii) 案實跑顯示：**跨過屁股線之宗，其 `gap` 必 > 容差 ⇒ 同時落入 `not touch`**
⇒ 初版對「跨過」之宗報「**你沒配到屁股線**」——

- **誤導**：它不是構不到，是**走過頭了**；
- 且**吞掉唯一可行之補救**（「街廓需先切成兩個小街廓」）。

**修正**：`_miss` **扣除** `_cross`；二者**併存時一次把兩段都報出**（跨過在前）。
⇒ 修正後 (ii) 案正確顯示【跨過屁股線】段與切街廓之指引。

> 🔴 **此即 §四 三案之價值**——若只調容差，此缺陷**不會被發現**（調容差時兩分支同時動，
> 看不出誰蓋掉誰）。

### 2-D 落點

`grep -n "K-9-4 BASELINE 臨接閘（KL 裁 2026-08-03" app.py`
——置於 **Step G 配地成果落定之當下**（`f3_G_values` 寫入**前**）。

- ✅ **不在任何 `st.expander` 之內**（以 `ast` 確證，見 §六-3）——守段二-0 一-8 之戒。
- ✅ **不在任何 `try` 之內**（以 `ast` 確證）——停機閘被 `try` 包住即等於沒上閘。

---

## 三、⚠️ 落點與施工單所載不同（施工單 §六-5·**以倉內為準**）

施工單 §二 令落點「比照段一 K-9-2（`app.py:16631` 之呼叫點附近；
BASELINE 設定完成後、**開始配地之前**）」。**該落點對 K-9-4 不可行**：

| 事實 | 查證 |
|---|---|
| K-9-4 之主詞是**分配宗地** | 正典：「**每宗**必須配到 BASELINE」 |
| 宗地幾何（`cut_coords`）最早產生於 **Step G** | `grep -n "'cut_coords': _res.get('cut_coords'" app.py` |
| K-9-2 落點在 Step G **之前** | `grep -n "_k92_rows = k92_block_depth_check" app.py` vs 上行 |

⇒ 於 K-9-2 落點**尚無任何宗地存在**，K-9-4 **無從評估**。
⇒ 改置於 **K-9-4 可被評估之最早時點**＝配地成果落定之當下。
**「開始配地之前」之要求對 K-9-4 在邏輯上不可滿足**，故以倉內實況為準並載明於此。

---

## 四、鑑別力證明（三案·**缺一不可**）

實料：`verify/out/K9_4_gate_discrimination.log`

⛔ **未調容差**（施工單明禁「只調容差不算數」）。三案皆以**擾動輸入**驅動
（宗地幾何／BASELINE 位置／BASELINE 誤派），**生產碼一字未改**
⇒ `git diff` 天然零殘留（見 §六-4）。

| | 情境 | 結果 |
|---|---|---|
| **對照組** | 未擾動 | ✅ **不 raise**（零違例） |
| **(i) 未達** | `R1/628-37(1)` 後緣頂點沿 BASELINE 法向推 `0.5m` | 🔴 raise ✅　`gap = 0.499997m`　訊息＝**【未配到屁股線】1 宗**（差 0.500m） |
| **(ii) 跨過** | `R1` 之 BASELINE 沿法向往街廓內平移 `16.575m`（≈半個進深 `33.15m`） | 🔴 raise ✅　該街廓 **7 宗**實質橫跨　訊息＝**【跨過屁股線】7 宗**＋「街廓需先切成兩個小街廓」 |
| **(iii) 屁股線誤派（主案）** | `R2` 之 BASELINE 換成 `#0/#2` 那條（＝R1/R4 屁股線·亦即 R2 之末端境界線） | 🔴 raise ✅　R2 受配 16 宗中 **15 宗**未配到（差 71〜92m）、**1 宗**仍碰得到 |

### 4-1 (iii) 案之附帶收穫：**正面坐實裁定 ④**

誤派後 R2 之 16 宗中**恰有 1 宗仍碰得到**該線。
該線與 R2 原屁股線之**方位夾角 85.3965°**（近乎垂直）——若二線無關，
理應**零宗**碰得到；實測「恰 1 宗」正是**末端塊第 1 筆**，
即裁定 ④「**得兼任鄰街廓末端塊第 1 筆宗地之起始境界線**」之**實測坐實**。

⇒ 本案「一條線兩種身分」確實存在 ⇒ **誤派是活風險**，本閘咬得到。

---

## 五、§三 實作紅線之自查（BASELINE 對 BLOCK 係多對一）

**倉內實測**（本批自算·`ezdxf`）：`data/V6.dxf` 之 `BASELINE` 圖層 **4 條 LINE**
（`data/V6_1.dxf` 逐條相同）：

| 實體 | 方位 | 長度 |
|---|---|---|
| #0 | 133.4463° | 95.658 |
| #1 | 46.0270° | 89.214 |
| #2 | 133.4463° | 88.037 |
| #3 | 48.0498° | 96.702 |

⇒ 與施工單 §三 之方位／長度**逐位相符**。（#0 與 #2 方位逐位相同＝**共線**。）

**紅線遵循**：

| 紅線 | 自查 |
|---|---|
| 禁以屁股線實體編號為鍵 | ✅ 本批新碼一律走 `baseline_pts_by_label`（**街廓** label 為鍵）；`grep -n "k94_" app.py \| grep -iE "#0\|#2\|實體\|entity\|idx"` ⇒ **0 命中** |
| 禁假設一街廓一實體 | ✅ `_k94_bl_by` 由 `f3_manual_baseline` 逐街廓取值，**容許多鍵指向同一幾何** |
| 禁以「實體數 vs 街廓數對帳」為前提 | ✅ 本批**未新增任何對帳**。既有 `baselines_matched_count` 語意早於 C-4 改為「配到 BASELINE 之**街廓**數」（`grep -n "C-4：\`baselines_matched_count\`" app.py`），且**未被當作閘**（`grep -rn "baselines_matched_count" --include="*.py" . \| grep -iE "assert\|raise"` ⇒ **0 命中**） |

⚠️ **未複驗者（誠實聲明）**：施工單 §三 之**逐街廓邊界落線長度表**（R1 88.037…）
係 claude.ai 之量測，本批**未逐格重算**——本批之結論不依賴該表，
僅依賴「4 條服務 6 街廓」之結構，而該結構已由本批自算之實體表
＋ §四 (iii) 案之實測（R2 恰 1 宗碰得到 #0/#2）**雙重坐實**。

---

## 六、驗收

### 6-1 `run_all` 名目集合 diff

實料：`verify/out/K6A2_seg2_1c_runall.log`（`EXIT=1`·一般模式·未用 `WV_BAKE`）
⇒ `132 名目／86 PASS／46 FAIL`。

```
$ diff <(K8_seg3_C 基線輪 FAIL 名目 sort) <(段二-1c輪 FAIL 名目 sort)
  (無輸出)
$ diff <(K8_seg3_C 基線輪 PASS 名目 sort) <(段二-1c輪 PASS 名目 sort)
  (無輸出)
$ diff <(段二-1a輪 FAIL 名目 sort) <(段二-1c輪 FAIL 名目 sort)
  (無輸出)
$ diff <(段二-1a輪 PASS 名目 sort) <(段二-1c輪 PASS 名目 sort)
  (無輸出)
```

⇒ **四組皆空**、逐字零進零出。

#### 🔴 6-1-a **但 `run_all` 對本閘之覆蓋＝零**（**不得誤讀上表**）

`k94_assert_baseline_touch` 之**唯一呼叫點**在 `main()` 之 Step G 分支
（`grep -n "k94_assert_baseline_touch(_k94_touch)" app.py` ⇒ **1 命中**）；
harness 走 `verify/stepg_pipeline.run_step_g`（**另一套 Step G**），**不呼叫該閘**
（`grep -rn "k94_assert_baseline_touch" --include="*.py" . | grep -v "^./app.py"` ⇒ **0 命中**）。

⇒ **上表之「零進零出」證明的是「本批之非閘改動為零行為」，
**不是**「K-9-4 閘已被驗過」。**
本閘之鑑別力**全部**由 §四 三案承擔。
（登記為 **GB-24**——⚠️ 此為「`run_all` 綠不代表 K-9-4 已驗」之制度性缺口。）

### 6-2 上閘後重跑量測 probe

實料：`verify/out/probe_ruling_K9_4_baseline_touch.log`（**已入庫**·`rc=0`）

| 情境 | 母體 | 最大 gap | 逾容差 | 實質橫跨 |
|---|---|---|---|---|
| 0m | 68 宗 | `0.000008 m` ＠ `R4/R4-抵費地` | **0** | **0** |
| 3.5m | 70 宗 | `0.000008 m` ＠ `R4/R4-抵費地` | **0** | **0** |

⇒ **仍為零違例、零橫跨**（與段二-1(a) 逐格相同）⇒ **上閘對本案零觸發**。

### 6-3 落點確證（`ast`·非縮排目測）

```
上閘呼叫行 = k94_assert_baseline_touch(_k94_touch)
  包住之 st.expander：無 ✅
  包住之 try（會吞 raise）：無 ✅
```

### 6-4 §四 三案之零殘留

三案以**擾動輸入**驅動（宗地幾何／BASELINE 位置／BASELINE 誤派），**未改生產碼一字**
⇒ **無「還原」可言，本就沒動過**。實際輸出：

```
$ git status --porcelain -- app.py verify/probes/
 M app.py                                   ← 僅本批之上閘實作，無三案痕跡

$ git diff -- app.py | grep -cE '40\.00|臨時|discrim|TMP|debug'
0                                           ← 三案未在生產碼留任何痕跡

$ git ls-files | grep -c "tmp_k94_discrim"
0                                           ← 三案腳本在 scratchpad，倉內 0 命中
```

⇒ **零殘留**、無開關、無容差旁路。
三案之輸出行已入庫至 `verify/out/K9_4_gate_discrimination.log`。

---

## 七、施工單與倉內實況之差異（§六-5）

| 施工單所載 | 倉內實況（`ead148b` 當場 grep） | 處置 |
|---|---|---|
| `k94_baseline_touch_by_parcel` `app.py:6090` | 屬實 | 未動其一字 |
| `_baseline_normal_axis` `app.py:5769` | 屬實 | 未動 |
| `_EPS_TOUCH_FRONT` `app.py:5766` | 屬實 | 沿用·未另立 |
| `touch` `app.py:6158`／`crosses` `app.py:6155` | 屬實 | 兩者皆咬 |
| K-9-2 呼叫點 `app.py:16631` | 屬實 | — |
| `app.py:6024` docstring「尚未實作」 | 屬實 | 已改實作錨點 |
| 正典 `:393` ③ 前提由 K-9-4 保證 | 屬實 | 已標落地 |
| 正典 `:348` K-9-4 條目 | 屬實 | 已加指標＋已實作 |
| GB-23 錨 `app.py:1762` | 屬實（`result['baselines'][_lbl] = `） | 已登記 |
| §三 BASELINE 4 條、方位／長度 | **屬實**（本批自算逐位相符） | — |
| §二 **落點「開始配地之前」** | 🔴 **對 K-9-4 不可行**——該時點尚無宗地 | **改置於配地成果落定之當下**（§三） |
| §二 母體「全部分配宗地」 | 另有**無地宗**（現金補償／孤立公設地·`cut_coords=None`） | **正面分類排除＋雙向自檢**（§2-B） |
| §三 引 `data/V6_1.dxf` | 生產路徑讀 **`data/V6.dxf`**（`grep -n "V6DXF = " verify/run_verification.py`）；二檔該圖層**逐條相同** | 以 `V6.dxf` 為準·結論不變 |
