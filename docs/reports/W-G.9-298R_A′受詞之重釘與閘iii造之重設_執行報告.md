# `W-G.9-298R`　執行報告：`A′` 受詞之**重釘** ／ 閘 `(iii)` 造之**重設** ／ `自誤 400`・`401` 之修訂重發

> **單** ＝ `docs/orders/W-G.9-298_施工單_A′受詞之重釘與閘iii造之重設.md`（工項零已原封入倉）
> **分級** ＝ **中**（現查 ＋ 量測 ＋ 登記·**零生產碼**）
> **開工態** ＝ `2df61769b467fceeb68b297d74d87c49138426a0`（`ls-remote` 自查·全 `40` 碼）
> 🔴 **本批含一項單內互斥之逐字回報**（`常規二`·取保守項續辦）——見 `§三-7`。
> 🔒 **KL 之放行仍在·仍⛔ 動用**；生產碼 `34` 檔一字未動。

---

## `§零`　開工閘（九項·**每列同格載其產生指令**·`自誤 392`）

| # | 產生指令（逐字） | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `2df61769b467fceeb68b297d74d87c49138426a0`（＝ `git rev-parse HEAD`·全 `40` 碼） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` ⇒ **非祖先**（`rc` 緊接該指令取之） | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`·🔴 **`0`** 項·🟡 **`3`** 項·`P-5` **`SELF_SHA256` ✅ 相符**（受詞 `27227` B·實算 `33b8f7cc43190e26…` ＝ 單載 `33b8f7cc43190e26d919bfddac70d6d0cfd6ba90fec1a86dc8e553ef8e444d3b`） | ✅ |
| `4'` | 受詞自證（坑 `bf`）＝ 器所印之檔名列 | 含 `W-G.9-298` ＝ `True`／含 `W-G.9-297` ＝ `False` | ✅ |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0`·**四靶逐項成立**；逐字「判別力造：✅ 全數成立 ⇒ 本器非恆綠亦非恆紅」 | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`；逐字「必命中字樣 ⇒ **1**（須 > 0）／必不命中之人造字樣 ⇒ **0**（須 0）⇒ ✅」 | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `389`／`399`（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `28`／`29`（缺 `[]`） | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·末標籤 `bi` ⇒ **下一 `bj`**；母體 ＝ `HEAD` 之追蹤 `.md` **`794`** 檔；造甲 `6 ≥ 1`／造乙 `0`／`0` ⇒ 器非紅（**等待綁行程結束**·⛔ `\| tail -N`） | ✅ |
| `7` | `git ls-files \| grep -E '^verify/[^/]+\.py$'` ＝ `33`；`git ls-files -- app.py` ＝ `1` | **`34`** 檔（**正面列舉**）；判別力[必不命中] `git ls-files \| grep -E '^verify/.+/.+\.py$'` ＝ **`312`**（須 `> 0`·前批 `311`·**逐批漂移**⇒ ⛔ 定值·`自誤 386`） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **未設 ⇒ 預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`；`git ls-files verify/baselines \| wc -l` ＝ `298` | ✅ |

🔒 **`P-4` 三項之逐項處置**（⛔ 靜默略過）：我方實跑得 🟡 `3` 項，列號 **`:66`**（`§一` 裁表）／**`:116`**（`§四` `自誤 400` payload）／**`:157`**（`§五` 登記 payload），
與單 `§零` 所預告之三處**逐一相符** ⇒ **受該具名豁免**（其 `→` 皆係賦值鏈／消費鏈之串接·⛔ 幾何向量之方向性轉引 ⇒ 無座標系可具名）。
🛑 其射程：本批凡出艙**幾何向量**之夾角者（`§三` 款 `5`）一律**釘槽並以實邊機驗**——該禁**⛔ 在豁免內**（見 `§三-5`）。

## `§零′`　前批之核可

發單側已辦 ⇒ **⛔ CC 重辦**（本批未重跑其五列）。

---

## `§一`　工項零：本單原封入倉

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/orders/W-G.9-298_施工單_A′受詞之重釘與閘iii造之重設.md` |
| **受詞自證**（坑 `bf`） | 器所印之檔名 ＝ 該路徑；[必真] 含 `W-G.9-298` `True`／[必偽] 含 `W-G.9-297` `False` |
| 所指之檔數 ＝ `1`（框 ＝ 磁碟 `os.path.isfile`·母體 ＝ 工作區） | 受詢 `True`／造甲[必≥1] `W-G.9-297` 之單 `True`／造乙[必為0] 人造名（**執行期組出**·字面⛔ 出艙）`False` |
| `CR`（框 ＝ `bytes.count(b'\r')`·**讀 blob 取 bytes**） | **`0`**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf` 之 `CR` ＝ **`12308`**（全長 `88341` B） |
| **三方逐位** | 源 `27305` B `sha256` `6ecaa3d1d46e3c136a16a016ea9093e35e7a650c558c56a7cb2451678620f83e` ＝ 磁碟 ＝ **blob 內容**；blob `sha1` ＝ `5df70201a19dc4a2229f70b34b858914d677c0c3` |
| `numstat` | `231`／`0`（**`deletions` 欄 `0`**） |

---

## `§二`　工項一（**主檢**）：閘 `(iii)` 重設造之機驗 ＋ `A′`／`B′` 之當場重得

**器** ＝ `verify/probes/probe_WG9298_gate3.py`（新建·**單一 `sys.settrace`**·⛔ 改生產碼一字）
**落檔** ＝ `verify/out/WG9298R_gate3.log`（`14874` B）·`rc = 0`
**物證** ＝ `verify/out/WG9298R_gate3_v1_firstcall.log`（`13723` B·**⛔ 覆寫**）

### 二-1　逐閘執行次數

| `SB` | `solve_G_binary` | `_mp_base_W0` | `_advance_block_with_split` | `_build_corner_range_v3`（段內） | `_corner_buffer_S`（段內） | `k956_W_from_mp` 生產錄 | `run_step_g` 終局 |
|---|---|---|---|---|---|---|---|
| `0.0` | `149` | **`0`** | `11` | `24`（`8`） | `1`（`1`） | **`0`** | （未拋） |
| `3.5` | `149` | **`1`** | `11` | `24`（`8`） | `2`（`2`） | **`1`** | （未拋） |

`(甲)` 上溯命中率 ＝ `97`／`149`（二 `SB` 同）；`blk_label` 存在率：守衛 `11`／`11`。

### 二-2　款 `1`：**同一性**（`is` 比對·🛑 **`id(·)` ⛔ 作錨**）與 `ns` 之來源

| 受詞 | 實測 |
|---|---|
| `ns["k956_W_from_mp"]` 之 `__name__`／`__qualname__` | `'_spy_k'`／`'drive.<locals>._spy_k'` |
| **器所調用者**（`settrace` 之 `call` 事件·**全部**·⛔ 只取第一個）其中含 `ns["k956_W_from_mp"].__code__`（`is`） | 逐格 `[True, True, True]`（**`3`／`3`**） |
| 呼叫鏈之 `co_name`（序·節略 `8`·三格逐格相同） | `['_w0', '_norm_dispatcher', 'norm', 'isComplexType', '_spy_k', 'k956_W_from_mp', '_norm_dispatcher', 'norm']` |
| 其中 `co_name == 'k956_W_from_mp'` 者 | 各 **`1`** 個·`co_filename` ＝ **`'<app.py:harvest>'`** |
| 判別力[必為真] | `ns` 之項與其自身 `is` ⇒ **`True`** |
| 判別力[必為偽] | 一**執行期組出**之同式包裝物（字面⛔ 出艙）`is` `ns` 之項 ⇒ **`False`** |
| **`ns` 之來源** | `type(ns)` ＝ `'dict'`；`ns is globals()` ⇒ **`False`**；`sys.modules` 中 `__dict__ is ns` 之模組數 ＝ **`0`** ⇒ **⛔ 任何模組之 `__globals__`** |

🔒 **照實併記**：`ns["k956_W_from_mp"]` 現為 `drive.<locals>._spy_k`——**本器（`P.drive`）之包裹**；
其**委派**至原函式由上列呼叫鏈（含 `co_filename` ＝ `'<app.py:harvest>'` 之 `k956_W_from_mp`）**機驗**、**⛔ 推定**。

### 二-3　款 `2`：守衛 `not _degenerate_order and _N > 1` 之**各分項**（⛔ 只報合取·⛔ 轉引前批）

| `SB` | 街廓 | `_degenerate_order`（鍵存在） | `_N`（鍵存在） | `not _degenerate_order` | `_N > 1` | **合取** | 呼叫列 |
|---|---|---|---|---|---|---|---|
| `0.0` | `R4` | `False`（`True`） | **`1`**（`True`） | `True` | **`False`** | **`False`** | `1017` |
| `3.5` | `R2` | `False`（`True`） | **`14`**（`True`） | `True` | `True` | **`True`** | `1019`／`1063` |
| `3.5` | `R4` | `False`（`True`） | **`1`**（`True`） | `True` | **`False`** | **`False`** | `1017` |

### 二-4　款 `3`：可得之格之**逐位對拍**（母體 ＝ `drive()` 期間之**前綴**·⛔ 含器自身之輸出）

| `SB` | 格 | 生產錄 | 器自身已追加 | 器以現行 `buf` 重建之 `W_0` | 判 |
|---|---|---|---|---|---|
| `0.0` | `R4/left` | **`0`** | `1` | `3.7076106246376597` | 🟡 **結構上不可得（loud）**·分項成因 ＝ `_N > 1` 為偽·**⛔ 判為 `0`、⛔ 判為「回測不過」** |
| `3.5` | `R2/left` | **`1`** | `1` | `6.98048044737404` | ✅ **逐位相同**（命中 `1` 筆·最小差 `0.0`） |
| `3.5` | `R4/left` | `1`（**本格無**） | `3` | `7.023016189779138` | 🟡 **結構上不可得（loud）**·同上 |

### 二-5　款 `4`：`A′`／`B′` 之**當場重得**（**【設計驗算·⛔ 非實測】**·⛔ 轉引 `W-G.9-295`）

🔑 **射程** ＝ 於 `forced` 消費端以 `_first_corner_alloc_dir(side_mid)` 取代當場之 `allocation_dir` 複算路丙之帶，**⛔ 逾此一處**；
原語全委派 `ns`（`_corner_buffer_S`／`_block_strip`／`_strip_s_range`／`k956_W_from_mp`），帶之構造復用 `probe_WG9292_pathC.band_poly`（⛔ 動該檔一字）。

| `SB` | 格 | 現行 `buf` | `buf′` | `buf′ − buf` 帶號／絕對值 | `W_0′` | `S1_perp` | `\|W_0′ − S1_perp\|` | 跨距（`L_su`／`L_edge` ⇒ 取較大） | `A′` 界 | 比值 | 判 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `0.0` | `R4/left` | `4.49975224003872` | `3.7114402919876444` | `-0.7883119480510761`／`0.7883119480510761` | `3.6955152872177086` | `3.6954999940774775` | `1.5293140231076308e-05` | `33.26627503242344`／`33.26627503226274` ⇒ `33.26627503242344` | `0.00015030237064795143` | `0.10174916180728083` | ✅ |
| `3.5` | `R2/left` | `8.696079893776137` | `7.000183533577529` | `-1.6958963601986081`／`1.6958963601986081` | `7.000102486246391` | `7.0` | `0.00010248624639075388` | `44.93777987966314`／`44.88675333792245` ⇒ `44.93777987966314` | `0.00011126495375137079` | `0.9211008762001237` | ✅ |
| `3.5` | `R4/left` | `7.8185635189090075` | `7.030272552440337` | `-0.7882909664686704`／`0.7882909664686704` | `7.000107141448771` | `7.0` | `0.00010714144877077558` | `33.572497547604144`／`33.265390173978915` ⇒ `33.572497547604144` | `0.00014893142796154046` | `0.7194012052207236` | ✅ |

| `SB` | 格 | `甲∖乙` | `乙∖甲` | **`B′` 實測** | `B′` 界 | 比值 | 判 |
|---|---|---|---|---|---|---|---|
| `0.0` | `R4/left` | `0.0` | `0.000502592537319901` | `0.000502592537319901` | `0.005` | `0.1005185074639802` | ✅ |
| `3.5` | `R2/left` | `4.777744422150305e-10` | `0.004600270698404074` | `0.004600271176178517` | `0.005` | `0.9200542352357033` | ✅ |
| `3.5` | `R4/left` | `7.014238515912815e-11` | `0.003564108231212022` | `0.003564108301354407` | `0.005` | `0.7128216602708813` | ✅ |

🛑 **界值逐格由 `§一` 裁 `4` 所釘之算式當場導出·⛔ 調界、⛔ 事後放寬**；🛑 **⛔ 因此判 `A′` 已通過或路丙成立**（裁 `4`／`6`）。

### 二-6　款 `5`：**切向之釘槽**機驗（⛔ 報「向量之夾角」而不釘其槽·以**實邊**）

| `SB` | 格 | 二鍵（甲／乙·一致） | 槽 ＝ `rot90(allocation_dir)` vs 該宗 `cut_coords` **實邊** | 槽 ＝ `su`（SIDE **線向**） vs 遠側境界線**實邊** |
|---|---|---|---|---|
| `0.0` | `R4/left` | `1`／`2`·**不一致（loud）** | `allocation_dir` ＝ `(-0.7517327519688664, 0.6594678685253096)`；`5` 邊之最大 `\|cos\|` ＝ `1.0000000000000002`（與 `1` 之差 `2.220446049250313e-16`） | `\|cos\|` ＝ `1.0`（差 `0.0`·長 `33.26627503226274`） |
| `3.5` | `R2/left` | `28`／`28`·一致 | `allocation_dir` ＝ `(0.7260188428756796, 0.6876748067142342)`；`4` 邊之最大 `\|cos\|` ＝ `1.0`（差 `0.0`） | `\|cos\|` ＝ `1.0`（差 `0.0`·長 `44.88675333792245`） |
| `3.5` | `R4/left` | `1`／`2`·**不一致（loud）** | `allocation_dir` ＝ `(-0.7517327519688664, 0.6594678685253096)`；`5` 邊之最大 `\|cos\|` ＝ `1.0`（差 `0.0`） | `\|cos\|` ＝ `1.0000000000000002`（差 `2.220446049250313e-16`·長 `33.265390173978915`） |

🔒 `allocation_dir` 之出處 ＝ `solve_G_binary` 之回傳鍵，其碼註逐字「**§0.5-B：實際使用之臨街向（`rot90(f3_cad_alloc_dir)`）**」。

### 二-7　🔴 **單內互斥之逐字回報**（`常規二`·取保守項續辦·**責之歸屬⛔ 由 CC 判**）

**互斥之二端（逐字）**
- `§四` 末：「🛑 本 payload **⛔ 含 `###` 標題列** ⇒ 依 `自誤 393` 之判準 **原樣保留 `> `**（⛔ 剝層）」
- `§六` 收工閘 `4`：「自誤 相異 **`391`**／MAX **`401`**（＋**二**號）」

**機驗三項（當場·⛔ 推定）**

| # | 受詞 | 實測 |
|---|---|---|
| `1` | 鑄號之框（`verify/probes/probe_WG9267_issuer_measurers.py:28` 之 `RE_ZW_DEF`·逐字） | `^#{1,6} [^\w\`]{0,6}\`?自誤 \`?(\d+)\`?\`?\s*[｜（　:：]` ⇒ **須 `^#` 起首** |
| `2` | 本單 `§四` payload 之形（`30` 列） | `^>` 起首 ＝ **`29`**／`^#` 起首 ＝ **`0`** ⇒ **無論剝層與否皆不可能被命中**（剝一層後為 `` **`自誤 400`（… ``，仍無 `#`） |
| `3` | payload **落地後**之簿（母體 ＝ 該檔單檔·列框） | `RE_ZW_DEF` 命中 **`376`** 筆·**MAX ＝ `399`**；`400` 在其中 ⇒ **`False`**／`401` ⇒ **`False`**；判別力[必命中] `399` 在其中 ⇒ **`True`**（器非紅） |

🔒 **併記**：簿內既有鑄號形為 `` ### 🩸 `自誤 397`　**…** ``（**`^###` 標題列**·`:9725`／`:9738`／`:9751`），與本單 payload 之形相異。
🛑 **保守項之選取**：遵 `§四` 明文**原樣保留 `> `**、**⛔ 改 payload 一字**（`⛔ 改一字` 係更基本之拘束）。
⇒ 其後果（自誤簿 MAX 仍 `399`、收工閘 `4` 之 `401` **結構上不可達成**）為**該互斥之應然**，於 `§五` 閘 `4` **loud 具名**，**⛔ 判為缺失**·**候發單側**。

### 二-8　款 `6`：判別力四造之判

| 造 | 判準 | 實測 | 判 |
|---|---|---|---|
| **[必為真]** | 款 `1` 之 `ns` 項與其自身 `is` | **`True`** | ✅ |
| **[必為偽]** | 款 `1` 之人造包裝物 `is` | **`False`** | ✅ |
| **[必非零]** | 現行 `buf` 之對稱差 `> 0`（判定組 **`3`**／`3`） | `13.055879754480518`／`38.06818855324997`／`13.05518976183967` | ✅ |
| **[必為零]** | 範圍多邊形自比 ＝ `0`（判定組 **`3`**／`3`·**先自證可滿足**） | `0.0`／`0.0`／`0.0` | ✅ |

🔒 **`rw_from_width` 之受詞（照實·⛔ 以「未觸」充綠）**：本器於 `:150` 逐字 `R = ns["rw_from_width"]` 取其委派，
惟**本批⛔ 呼叫之**（`R(·)` 之逐格值係 `W-G.9-297` 款 `6` 之受詞、本批⛔ 重量）。
其碼面現查（**正面列舉**·列框）：定義處 `app.py:5728` `def rw_from_width(W: float) -> float:`；
命中 `app.py` **`11`**／`verify/stepg_pipeline.py` **`6`**／`verify/wf_f0.py` **`0`**／本器 **`2`**；對照組[必為零] 人造名 ＝ **`0`**。

### 二-9　🩸 入倉前自捕**二則**（皆**量測器紅**·⛔ 頂替）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `1` | v1 之款 `1` 報同一性 `[False, False, False]` | **與四造全綠並存** ⇒ 二者互斥即其徵候 | 我取「**第一個** `call` 事件」，而 `J._w0` 於呼 `k956_W_from_mp` **之前**先呼 `numpy` 之 `asarray`／`linalg.norm` ⇒ 攔到 `numpy`、**⛔ 受詞** ⇒ **偽紅** | 蒐集**全部** `call` 事件、以 `is` 判受詞之 `code` 是否在其中，並出艙呼叫鏈之 `co_name` 序。修後 **`3`／`3`** 為真 |
| `2` | 工項二之字樣錨**判別力對照**取寬字樣 `自誤 400`，命中 ＝ `1`（須 `> 1`） | 「寬」字樣之命中**不大於**錨 | 該號**本批方鑄** ⇒ 該字樣實不比錨寬·**⛔ 判別力** | 改取真寬字樣 `自誤 `（含尾空格）＝ **`898`** 列（`> 1`）⇒ 該檢**非恆為 `1`** |

🔒 **修法射程之機驗**：扣除款 `1` 區塊後，`v1` 與 `v2` **逐位相同**（`diff` 無輸出）⇒ 修法**只動款 `1`**。

### 二-10　一句話推導

> 依裁 `5` 之重設造，本器不再求一個**結構上不可得**之生產錄對拍，改驗「器所調用者**即** `ns` 之項」
> （三格 `3`／`3`·且呼叫鏈機驗其委派至 `co_filename` ＝ `'<app.py:harvest>'` 之原 `k956_W_from_mp`）
> 與「**唯一可得之格**逐位相同」（`3.5 R2/left`·最小差 `0.0`）——二者皆成立
> ⇒ `W_0′` 之重建有**機制上之依據**；🛑 惟**⛔ 據此判路丙成立或 `A′` 已通過**（裁 `4`／`6`·受領候發單側）。

---

## `§三`　工項二／三：二 payload 之末端追加

| 受詞 | 工項二（`§四`） | 工項三（`§五`） |
|---|---|---|
| 落點 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` 檔末 | `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末 |
| payload 之產生（`常規七 一`·**由檔案管線直接產生**·⛔ 憑記憶重寫） | 本單第 `116`–`145` 列（`30` 列／`3544` B／`CR` `0`） | 本單第 `157`–`170` 列（`14` 列／`1884` B／`CR` `0`） |
| 落地形機驗（`自誤 393`） | `^###` 起首 ＝ **`0`**（⇒ **⛔ 剝層**）；`^>` 起首 ＝ `29`／`^#` 起首 ＝ `0`（**二數並報**） | `^###` ＝ **`0`**；`^>` ＝ `14`／`^#` ＝ `0` |
| 對照組[必非零] | 本單全檔 `^##` 起首列 ＝ **`12`**（須 `> 0`） | 同 |
| 期初／期末 bytes | `824558` → `828109`（**`+3551`**·`numstat` `33`／`0`） | `900377` → `902268`（**`+1891`**·`numstat` `17`／`0`） |
| 嚴格前綴檢**二造** | `new.startswith(old)` `True`／`old.startswith(new)` `False` | 同 |
| `new.endswith(payload + b'\n')` | `True` | `True` |
| **字樣錨唯一性**（`uniq`·坑 `bi`·列框·母體 ＝ 該檔單檔） | 錨 `` > **`自誤 400`（修訂重發 `` ＝ **`1`**；判別力 寬字樣 `自誤 `（含尾空格）＝ **`898`**（`> 1`）；對照組[必為零] 人造錨 ＝ **`0`** | 錨 `` > **`GB-170` 之修：`A′` 受詞之重釘 `` ＝ **`1`**；寬字樣 `GB-170` ＝ **`21`**（`> 1`）；對照組 ＝ **`0`** |
| `append_audit` | `python verify/tools/wg942_append_audit.py` ⇒ `rc = 0`（先設 `PYTHONIOENCODING=utf-8`）·受檢 **`77`** 筆·逐字「結論：✅ 全部成立」 | 同（同一次執行） |

---

## `§四`　`⛔ 為之事` 之逐項自檢（單 `§七`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔一字 | ✅ 逐 `commit` 之生產碼判法皆 **`0` 行**（見 `§五`）；對照組[必非零] `17fd981^..17fd981` ＝ `2` |
| `2` | ⛔ 開任何分支 | ✅ 未開 |
| `3` | ⛔ 動用 KL 之放行 | ✅ 未動用 |
| `4` | ⛔ 擬任何實作形 | ✅ 未擬 |
| `5` | ⛔ 調任何界、⛔ 事後放寬任何門檻 | ✅ `A′` 界逐格由裁 `4` 之算式當場導出；`B′` ＝ `0.005` 未動 |
| `6` | ⛔ 自寫第二套幾何或第二份 `W` 之定義（`GB-48` 族） | ✅ 原語全委派 `ns`；帶之構造復用 `band_poly`；`W_0` 重建式復用 `probe_WG9295_joint._w0` |
| `7` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp`／`_mp_base_W0`／任何函式簽章 | ✅ 未改（`app.py` blob 期初＝期末） |
| `8` | ⛔ 動 `ALPHA_LABELS` 一字；⛔ 動 `probe_WG9292_pathC.py` 一字 | ✅ 二者之 `git diff --numstat` 輸出皆**空**（見 `§五`） |
| `9` | ⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**、⛔ `WV_BAKE`、⛔ 併線 `p3a`、⛔ 刪 `p3a`、⛔ 引任何自 `baselines` 取得之數 | ✅ 見 `§五` 閘 `6`（**先 `cat-file` 驗存在**·三態分列） |
| `10` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號 | ✅ 未鑄（見 `§五` 閘 `4`／`5`） |
| `11` | ⛔ 解除或收窄任何 `GB` | ✅ `§五` payload 逐字自載「⛔ 解除、⛔ 收窄」 |
| `12` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ 由生產碼 blob 相異 `0` 機械蘊含 |
| `13` | ⛔ 追改任何已入倉文件一字 | ✅ `append-only` 機檢 `77` 筆全成立 |
| `14` | ⛔ 判路丙成立與否、⛔ 判受領、⛔ 判孰是、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | ✅ 本報告只出艙；`二-7` 之互斥**逐字並列而⛔ 判責** |
| `15` | ⛔ 驗 `(甲)`／`(乙)` 逐位相同之機制（裁 `3`） | ✅ 未驗（本批⛔ 開此工） |
| `16` | ⛔ 刪任何 ref | ✅ 未刪 |
| `17` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ 收工閘之數係**報告入倉後**以末端追加補入；閘 `7` **出艙於回報·⛔ 入倉** |

---

## `§五`　收工閘（**報告入倉後量**·嚴格末端追加）

### 閘 `1`　`git show --numstat <各 commit>` ⇒ **刪除欄全 `0`**

| `commit`（全 `40` 碼） | 逐檔 `numstat` |
|---|---|
| `fa0790728d018ab3c01509f550c5335a44b60498` | `231`／`0`　`docs/orders/W-G.9-298_施工單_A′受詞之重釘與閘iii造之重設.md` |
| `05fe5c526f8dfc03756f2536873947c136153de5` | `17`／`0`　`docs/reports/W-G.4_泛用阻塞項登記表.md`；`33`／`0`　`docs/reports/W-G.9波_claude.ai側自誤登記.md`；`113`／`0`　`verify/out/WG9298R_gate3.log`；`109`／`0`　`verify/out/WG9298R_gate3_v1_firstcall.log`；`401`／`0`　`verify/probes/probe_WG9298_gate3.py` |
| `de843d34a033bad1a3d4765e2f52ef33e1d1795b` | `211`／`0`　`docs/reports/W-G.9-298R_A′受詞之重釘與閘iii造之重設_執行報告.md` |
| （本 `commit`） | 收工閘之末端追加（其 `sha` **⛔ 預寫**） |

### 閘 `2`　生產碼判法（母體**正面列舉 `34` 檔**）⇒ 逐區間 **`0` 行**

| 區間 | 命中行數 |
|---|---|
| `2df6176..fa07907` | **`0`** |
| `fa07907..05fe5c5` | **`0`** |
| `05fe5c5..de843d3` | **`0`** |
| `2df6176..de843d3`（全批） | **`0`** |
| **對照組[必非零]** `17fd981^..17fd981` | **`2`** |

### 閘 `3`　`.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**·**取 bytes**·框 ＝ `git diff --name-only -z` ＋ `core.quotePath=false`）

母體 ＝ 本批異動 **`7`** 檔：`W-G.9-298_施工單…md`（`27305` B）／`W-G.4_泛用阻塞項登記表.md`（`902268` B）／
`W-G.9-298R_…執行報告.md`（`20163` B）／`W-G.9波_claude.ai側自誤登記.md`（`828109` B）／
`WG9298R_gate3.log`（`14874` B）／`WG9298R_gate3_v1_firstcall.log`（`13723` B）／
`probe_WG9298_gate3.py`（`21483` B）⇒ 逐檔 `CR` ＝ `0`·**合計 `0`**。
判別力[必非零] ＝ `data/V6.dxf` 之 `CR` ＝ **`12308`**（全長 `88341` B）。

### 閘 `4`　四簿期末 — 🛑 **與單所期相異（單內互斥之應然·loud 具名）**

| 簿 | 期初（`§零` 閘 `6`） | **期末** | 單 `§六` 閘 `4` 所期 | 判 |
|---|---|---|---|---|
| 自誤 | 相異 `389`／MAX `399` | 相異 **`389`**／MAX **`399`** | 相異 `391`／MAX `401` | 🛑 **⛔ 達成·係 `§二-7` 互斥之應然** |
| `GB` | `168`／`170` | `168`／`170` | 一字未動 | ✅ |
| `VR` | `80`／`95` | `80`／`95` | 一字未動 | ✅ |
| `K-9` | `28`／`29` | `28`／`29` | 一字未動 | ✅ |

🔒 **其相異之由**：`§四` 之 payload 逐列以 `> ` 起首（`^#` 起首 ＝ `0`），而鑄號之框 `RE_ZW_DEF` 須 `^#` 起首
⇒ **無論剝層與否皆不可能被鑄**；單既明令 **⛔ 剝層**，保守項即**原樣保留** ⇒ 自誤簿 MAX 不動。
**⛔ 判為缺失**；其處置（payload 是否改為 `### 🩸 \`自誤 400\`　…` 之鑄號形後重發）**候發單側**。

### 閘 `5`　坑之期末

**`87` 標籤／`87` 可解析**（數字系 `26` ＋ 字母系 `61`）·末標籤 `bi` ⇒ **下一標籤 `bj`**（⛔ 鑄坑）；
母體 ＝ `HEAD` 之追蹤 `.md` **`796`** 檔——**自洽檢**：期初 `794` ⇒ **`+2`**，而本批新增之 `.md` 檔數 ＝ **`2`**
（**單**與**報告**；二登記表係**既有檔之追加**、`verify/**` 三檔非 `.md`）⇒ **相符**。
器自證二造：造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ ⇒ **器非紅**。
`ALPHA_LABELS`：`git diff --numstat 2df6176..HEAD -- verify/probes/probe_WG9269_pit_index.py` ⇒ **輸出空**；
併驗 `probe_WG9292_pathC.py` ⇒ **空**、`probe_WG9295_joint.py` ⇒ **空**（單 `§七` 之禁）。

### 閘 `5'`　驗收出艙閘（受詞係 `index` ⇒ 復跑前先 `git add`）

`python verify/tools/wg9223_acceptance_audit.py <本單> <本報告>` ⇒ **`rc = 0`**；
判定組（反引號 token）**`29`** 個·**命中 `0` 之字樣 ＝ `0`**；參考組命中 `0` ＝ `1` 個（**⛔ 作判**）。
本節追加後之**再復跑**見下「閘 `5'` 再復跑」。

### 閘 `6`　自限復驗

| 受詞 | 實測 | 判 |
|---|---|---|
| 生產碼 `34` 檔 blob 期初＝期末 | **相異 `0`** 檔（**正面列舉**）；判別力[必相異] ＝ `verify/probes/probe_WG9298_gate3.py` 期初存在 `False`／期末 `True` | ✅ |
| `app.py` 期末 blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1`（＝ 期初） | ✅ |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**） | ✅ |
| `p3a` | `git merge-base --is-ancestor 05a11bd6… HEAD` ⇒ `rc = 1`（非祖先·⛔ 併線·⛔ 刪） | ✅ |
| `WV_BAKE` | `None` | ✅ |
| `K-9-29`／`K-9-5-*`／`K-9-12` 任何子項⛔ 落地；`GB-79`／`167`／`168`／`169`／`170` **⛔ 修** | 由生產碼 `34` 檔 blob **相異 `0`** 機械蘊含 | ✅ |

🔒 **四物證落檔⛔ 覆寫**（**先 `git cat-file -e` 驗存在**·三態分列·`§零` 新附款）

| 物證 | 期初存在 | 期末存在 | blob | 判 |
|---|---|---|---|---|
| `verify/out/WG9295R_joint_v1_selfpolluted.log` | `True` | `True` | `1a35dc168655` → `1a35dc168655` | ✅ **逐位未動** |
| `verify/out/WG9296R_w0rec_v1_blknone.log` | `True` | `True` | `f93eb2fcb7e6` → `f93eb2fcb7e6` | ✅ **逐位未動** |
| `verify/out/WG9297R_w0caliber_v1_blkabsent.log` | `True` | `True` | `16742f3805e0` → `16742f3805e0` | ✅ **逐位未動** |
| `verify/out/WG9298R_gate3_v1_firstcall.log` | **`False`** | `True` | — → `5cef4c8dfe7a` | 🔒 **本批新入倉** ⇒ ⛔ 覆寫之受詞·**⛔ 適用** |

判別力[必存在] ＝ `app.py` 於二態皆 `True`；對照組[必為零] ＝ 一人造路徑（**執行期組出**·字面⛔ 出艙）於期末存在 ＝ `False`。

### 閘 `6'`　`git ls-remote origin 'refs/heads/verify/*'`

該 ref 族共 **`20`** 個；`verify/W-G.9-289-gb170` 命中 **`0`**（⇒ **⛔ 開**）；
判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`**。

---

## `§六`　逐 `commit`（**全 `40` 碼**）

| # | `commit` | 題 |
|---|---|---|
| `1` | `fa0790728d018ab3c01509f550c5335a44b60498` | 工項零：本單原封入倉 |
| `2` | `05fe5c526f8dfc03756f2536873947c136153de5` | 工項一／二／三：閘 `(iii)` 重設造之機驗 ＋ `A′`／`B′` 當場重得 ＋ 二 payload |
| `3` | `de843d34a033bad1a3d4765e2f52ef33e1d1795b` | 執行報告入倉 |
| `4` | （本 `commit`·收工閘之末端追加·其 `sha` **⛔ 預寫**） | 收工閘 |

🛑 **閘 `7`（`push` 後之 `ls-remote`）⛔ 寫入本檔**（單 `§六` 閘 `7` 明令「**出艙於回報·⛔ 入倉**」）
——本 `commit` 之 `sha` 於其自身落地前尚不存在，事後補入**結構上遞迴**。
