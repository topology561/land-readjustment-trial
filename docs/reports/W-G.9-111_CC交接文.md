# `W-G.9-111`　CC 側交接文（倉態）——供**下一個 CC session** 接手

> 🔒 **本文之性質**：**倉態 ＋ 陷阱 ＋ 已現查之事實**。
> ⛔ **非施工單、⛔ 非 plan、⛔ 不含任何實作指示與排程建議**——依分工鐵律（`CLAUDE.md`）：
> claude.ai 寫規格與 plan；Claude Code 照 plan 施工。下一窗 CC ⛔ 不得以本文代替施工單開工。
>
> 🔒 **本文所有數字皆現查於 `99e200a`**；下一窗**仍須自行重跑 `§二` 之 `A-0`**，
> ⛔ 不得轉引本文之錨（`CLAUDE.md`「數字錨引倉檔·禁憑記憶出艙」）。
>
> 🔒 **檔名之提案（常規三）**：`W-G.9-111_CC交接文.md`（與 `W-G.9-102_CC交接文.md` 同族·
> `W-G.9-111` 全倉命中 `0`）；候選另二 ＝ `W-G.9-110後_CC交接文.md`／`W-G.9波_CC交接文_110後.md`
> ——**待發單側核可**；該事⛔ 無土地後果、⛔ 不上呈 KL。

---

## §一　三句話現況

1. **`W-G.9-108`（修 `wg942`·`GB-92`）已入倉並 push**，`HEAD` ＝ `origin/wip/s1-endpart` ＝ `99e200a87b74724d99f9093c1bdc55f51c8406e4`，工作樹 `0` 筆。
2. 🛑 **其後之二張施工單（`W-G.9-109`／`W-G.9-110`）皆<u>停機</u>、payload 全數⛔ 未入倉**——
   停機之由**皆為發單側之期望字樣之錯**，⛔ 非被檢物之錯（詳 `§四`）。
3. 🔴 **土地後果已隔離**：`R6` 右街角地之消失 ＝ **單一 commit `4df19f4…`（乙式落地）**所致；
   且**例外⛔ 非肇因**（八處 `except` 探針 `0` 命中·而該函式確有執行）⇒ 下一步應查**選取條件**（詳 `§六`）。

---

## §二　開場必查（`A-0`·⛔ 其後受詞在此之前一律未開辦）

🔴 **開場第一件事（⛔ 不可省·省之則後續逐格皆紅而紅的是環境）**：

```
git -c core.autocrlf=false clone --branch wip/s1-endpart <url> <短路徑>
cd <短路徑> && git config core.autocrlf false
```

🔴 **「短路徑」係硬要求·⛔ 非潔癖**：本機 `core.autocrlf` 之 **system** 層 ＝ `true`
（`C:/Program Files/Git/etc/gitconfig`·⛔ 非 local／global），且 **session scratchpad 路徑長 `183` 字元**
⇒ `git show <40 碼 rev>:<CJK 路徑>` 之串長破 Windows `MAX_PATH` **`260`** ⇒ `wg942` 會把
**`62` 個受詞檔中之 `24` 個**靜默判為「無此檔（新檔）」而跳過，**閘仍印 `結論：✅ 全部成立`**。
🔒 **實用值**：`C:/Users/admin/AppData/Local/Temp/w1xx`（**`38` 字元**）⇒ 和 ＝ `38 + 43 + 40 = 121` ＜ `260`。

| # | 項 | 期望（現查於 `99e200a`） |
|---|---|---|
| 1 | `origin` | `99e200a87b74724d99f9093c1bdc55f51c8406e4` |
| 2 | 本機 `HEAD` | 同上；工作樹 `0` 筆 |
| 3 | `app.py` | `22160` 列／`1307679` B／`sha256` 前 16 `263a527c2a27be6e`／**git blob `a9e5671d64d254907a0396f898f046d9d85e8283`** |
| 4 | `docs/驗證裁定登記表.md` | `3745` 列／`284384` B／`8be78544f7229243` |
| 5 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `3235` 列／`241638` B／`b268c8f0cc1aedec` |
| 6 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `2658` 列／`403971` B／`04570b085cf7794b` |
| 7 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `3037` 列／`207289` B／`b533700407165efe` |
| 8 | `.claude/skills/failure-archaeology/SKILL.md` | `4791` 列／`351531` B／`6e760c5798f1518c` |
| 9 | `CLAUDE.md` | `543` 列／`48354` B／`4c45501353d2c4be` |
| 10 | `verify/tools/wg942_append_audit.py` | `622` 列／`50750` B／`b362ab5dbff01260`（🆕 `W-G.9-108` 已修） |
| 11 | `verify/tools/wg973_gb_integrity.py` | `263` 列／`14783` B／`b9aad7684900a334`（`W-G.9-105` 已修） |
| 12 | `verify/tools/wg9103_target_coverage.py` | `223` 列／`10288` B／`e897e576c796cb08` |
| 13 | `verify/probes/probe_WG9104_pathframe.py` | `309` 列／`14952` B／`1ac24eb3abba8b74` |
| 14 | `verify/run_all.py` | `365` 列／`23286` B／`680b4f7dd6868dd2` |
| 15 | `TARGETS` | 項數 `77`／涵蓋相異檔 `62`／檔級 `54`／段級 `23`；最長受詞相對路徑 ＝ `.claude/skills/failure-archaeology/SKILL.md`（**`43`** 字元） |
| 16 | 四器 | 見 `§七`（四者於淨工作樹皆 `rc = 0`·`stderr` 皆 `0` B） |
| 17 | 環境（**記錄項**·⛔ 非閘） | `python 3.13.11`（Anaconda·MSC v.1929）・`ezdxf 1.4.3`・`shapely 2.1.2`・`GEOS (3, 13, 1)`・`numpy 2.4.4` |

🔒 **量測框**：列數一律 `s.splitlines()`；**`sha256` 與 git blob hash 是兩個量**（第 3 列已並呈二者）⇒ ⛔ 不得互代。

---

## §三　commit 鏈（近 `10` 筆）

| commit | 摘要 |
|---|---|
| `99e200a` | `W-G.9-108`：修 `wg942_append_audit.py`（`GB-92`）＋ `VR-072`（**本文之基座**） |
| `a83c28c` | `W-G.9-106 丙-0`：`run_all` 雙機對照 ＋ `VR-069`／`VR-070`／`GB-93`／自誤 `135` |
| `d593d50` | `W-G.9-105 乙`：修 `wg973_gb_integrity.py`（`GB-90`）＋ `GB-92` ＋ `VR-068` ＋ 自誤 `133`／`134` |
| `c1719da` | `W-G.9-104 甲`：`GB-91` ＋ `VR-067` ＋ 自誤 `131`／`132` |
| `62b1e1c` | `W-G.9-103 補正①`：`VR-066` ＋ 自誤 `129`／`130` ＋ `GB-89`／`GB-90` |
| `7e56a9e` | `W-G.9-103`：`VR-065` ＋ 自誤 `128` |
| `f3ae9cf` | `W-G.9-101 補正⑤`：`VR-064` ＋ 自誤 `127` |
| `7d824b8` | `W-G.9-102`：CC 側交接文（**前一份**） |
| `aa15b79` | `W-G.9-101 補正④`：`VR-063` ＋ 自誤 `125`／`126` |
| `b9fa481` | `W-G.9-101 補正③`：涵蓋閘 `v3` ＋ `VR-060`〜`062` |

---

## §四　🛑 二張停機中之施工單（**最重要·下一窗之直接受詞**）

### 四-1　`W-G.9-109`（純查因·`GB-61` 加註 ＋ `VR-073` ＋ 自誤 `137`）——**停機於 `Q4`**

- **停機之格**：`Q4` 稱「`8c8f223a…` 於 `app.py` 之新增列 `16`、其中⛔ **非註解者 ＝ `0`**」；
  CC 之**具名框**（`strip()` 非空 ∧ ⛔ 不以 `#` 起）得 **`8`**（該 `8` 列係**三引號 docstring 之續行**）。
- 🔒 **其所欲保證之性質已由 CC 以正交量坐實**：`ast.parse` 後逐節點去首個 docstring `Expr`，再比 `ast.dump()`
  ⇒ **逐位相同 ＝ `True`**（判別力自檢：注入一 `import` ⇒ `False`）⇒ `8c8f223a` **零行為變更成立**。
- 🔒 **`W-G.9-110` `§零之前 2` 已裁**：原式**作廢**、改採 CC 之 `AST` 正交量；`Q3` 之 `31/19/12` 改採 CC 之 **`45/19/26`**。
- 🛑 **然其 payload（`GB-61` 就地加註／`VR-073`／自誤 `137`）⛔ 仍未入倉。**
- ✅ 其餘 `Q1`（`8/8` 逐位相同）／`Q2`（`Δ<0 = 0`）／`Q5`／`Q6` 皆符（見 `§六`）。

### 四-2　`W-G.9-110`（`R6` 隔離 ＋ `K-9-18` ＋ `GB-94`〜`96` ＋ `VR-071` ＋ 自誤 `136`〜`138`）——**停機於 `R4`**

- **停機之格**：`R4` 要求「`T1`／`T2` 之 `fixture_yi_construction.py` 皆須 `rc=0`」；
  🔒 現查 `git ls-tree`：該夾具於 **`2de3ea7`（T0）與 `4df19f4`（T1）皆⛔ 不存在**，係 `f13b1c92`（T2）才加入
  （`run_all.py@4df19f4` 之 `fixture_yi_construction` 命中 ＝ `0`）⇒ **`T1` 之「須 `rc=0`」⛔ 不可能成立**。
  `T2` ✅ 逐字 ＝ `✅ 末端夾具 fixture_yi_construction.py（…八格×二情境…）rc=0`。
- 🔴 **`R3` 之列舉亦不完整**（⛔ 非停機格，惟須併呈）：其稱「`T1`／`T2` 二者之一必等於 `26`」，
  實得**二者皆 `2`**；其 ⚠️ 分支之結論「肇因⛔ 不在此二 commit」**為假**——真轉折在 **`T0 → T1`**，
  該支⛔ 未被列舉。**隔離仍完全成立且更強（單一 commit）**。
- 🛑 **其 payload（`K-9-18`／`GB-94`／`GB-95`／`GB-96`／`VR-071`／自誤 `136`〜`138`）⛔ 全未入倉。**

### 四-3　🔴 二單之 payload 有**重疊**——下一窗須先解

自誤 `137`（「以登記表之散文充現況」）**同時出現於** `W-G.9-109` 之 `payload-C` 與 `W-G.9-110` 之 `payload-C`，
逐字內容相同。⇒ 🔒 **若二單先後照套，`自誤 137` 會重複入倉**。
併呈：`W-G.9-109` 獨有者 ＝ `GB-61` 就地加註 ＋ `VR-073`；`W-G.9-110` 獨有者 ＝ 其餘全部。

---

## §五　🔴 懸空引用（`W-G.9-107` 從未入倉）——現查坐實

`W-G.9-107` 之 payload（`VR-071`／`GB-94`／`GB-95`／自誤 `136`）**從未入倉**，
而**已 push 之 `VR-072` 逐字引用之** ⇒ 倉內現存**懸空引用**。現查（態 ＝ `blob@99e200a`）：

| 號 | 全倉命中 | 其定義節／表列 | 判 |
|---|---|---|---|
| `VR-071` | `1` 檔／`1` 列（`docs/驗證裁定登記表.md`·即 `VR-072` 文中） | `^## ` ＋ 反引號 ＋ `VR-071` ⇒ **`0`** | 🔴 懸空 |
| `GB-94` | `1` 檔／`1` 列（同上） | GB 登記表之定義列 ⇒ **`0`** | 🔴 懸空 |
| `GB-95` | `1` 檔／`1` 列（同上） | GB 登記表之定義列 ⇒ **`0`** | 🔴 懸空 |
| `GB-96`／`K-9-18`／`VR-073` | 全倉 `0` | — | ⛔ 未鑄（在停機之 payload 內） |
| 自誤 `136`／`137`／`138` | `^## 自誤 N` ⇒ 皆 `0` | — | ⛔ 未鑄 |

🔒 **`wg973` ⛔ 未抓到之由（⛔ 非其漏）**：其母體**僅 GB 登記表**，而該三處引用落在 **VR 登記表**。

---

## §六　🔴 土地後果：`R6` 右街角地之消失（**已單 `commit` 隔離**）

🛑 **域裁前提**：`K-9-18`（KL 裁 `2026-08-24`·逐字「`R6` 右側有 `SIDELINE`，他有街角地是確定的」）
⇒ 該側街角地之存在**⛔ 非待判**，消失即**迴歸**。⚠️ 該裁**⛔ 尚未入倉**（在 `W-G.9-110` 之 `payload-D`）。

**CC 實跑之隔離（三趟 `run_all`·量詞 ＝ 含 `R6` ∧ ⛔ 不以 `凍存:`／`現況:` 起之列數）**：

| 趟 | rev | `R6` 活列 | `[指配]` 街角側數 | 涉及街廓 |
|---|---|---|---|---|
| `T0` | `2de3ea7bd00fa54d15b1c2f3ba2fb80766269926`（乙式前） | **`26`** | **`8`** | `R1`–`R6` |
| `T1` | `4df19f4998da422c731e107d86c22df52654c7c0`（**只套乙式**） | **`2`** | **`7`** | `R1`–`R5` |
| `T2` | `f13b1c92ecea6e624df34873eec35484322e6aaf` | `2` | `7` | `R1`–`R5` |
| 現況 | `99e200a…` | `2` | `7` | `R1`–`R5` |

🔒 **⇒ 轉折在 `T0 → T1` ⇒ 肇因 ＝ `4df19f4…` 單一 commit（＝ 乙式落地 `K-9-5-14`／`K-9-5-15`）；`f13b1c92` ⛔ 非因。**
消失之二列逐字 ＝ `[指配0m] ('R6',) 欄「【右】最小面積(㎡)」: baseline='146.38' got='145.88'`／
`[指配3.5m] … baseline='299.13' got='298.14'`。

🔴 **例外⛔ 非肇因（假說已否證）**：於 `T1` 之拋棄式 clone，對 `select_corner_lots_both_sides_v12`
（該 rev `:11309`–`:11677`）之 **`8` 處** `except Exception:`（`:11452`／`:11483`／`:11507`／`:11544`／
`:11562`／`:11574`／`:11585`／`:11595`·**⛔ 非沿用 `99e200a` 之列號**）各插一列 `stderr` 探針後重跑：
`run_all` 一趟 ⇒ `EXC@` 命中 **`0`**；**直呼 `verify/run_verification.py`**（繞過 `GB-95` 之吞噬）⇒ 亦 **`0`**。
🔒 **該 `0` ⛔ 非空真**：同趟 `R1`–`R5` 仍產出 `7` 個街角側 ⇒ 該函式**確有執行**。
⇒ 🔒 **`R6` 右側係「未被選為街角」，⛔ 非「構造拋例外被吞」** ⇒ **下一步應查選取條件**。

🔒 **構造層⛔ 非病因**：`fixture_yi_construction.py`（八格×二情境）於 `T2`／現況 `rc=0`。

---

## §七　四器之受詞與判準（皆現查於 `99e200a`·淨工作樹）

| 器 | 受詞 | 判準 | 實得 |
|---|---|---|---|
| `verify/tools/wg942_append_audit.py` | `TARGETS` 各項之純追加性 | 嚴格前綴 ＋ 判別力自檢 | `rc 0`·`結論：✅ 全部成立`·`受檢 ＝ 77／已具名 0／⛔ 未宣告 0／合計 77` |
| `verify/tools/wg9103_target_coverage.py` | **本批**變更之 `M(.md)` | `受詞 ⊆ TARGETS ∪ 具名排除集['verify/']` ＋ `SELFTEST-1`／`2a`／`2b` | `rc 0`·`結論：✅ 本批受詞全數受涵蓋`·**淨樹下受詞 ＝ `0` 筆（⇒ 該綠⛔ 無判別力）** |
| `verify/tools/wg973_gb_integrity.py` | `GB` 之懸空號 | 母體 − 有定義列 − 缺號 ＝ `0` ＋ 不交／分割／恆等式／判別力自檢 | `rc 0`·`**懸空 ＝ 母體 93 − 有定義列 91 − 缺號 2 ＝ 0**`·缺號集 `['GB-12', 'GB-87']` |
| `verify/probes/probe_WG9104_pathframe.py` | 路徑解析之框 | `P-1`〜`P-8` | `rc 0`（🔴 **其 `rc` 恆 `0`·判別力 `0`·⛔ 不得入驗收合取項**·自誤 `134`）·逐字 `✅ 本檔未觸任一停機條件。` |

🔒 **`wg942` 新增之 `--new <path>`**（`W-G.9-108`／`VR-072`）：具名宣告之新檔⛔ 不判紅，
惟**判決字⛔ 不得為 `✅`**、且「**受檢 ＝ `0`**」⛔ 不得由任何宣告救免、宣告只能逐字列**單一路徑**。

🔴 **本檔（`W-G.9-111_CC交接文.md`）⛔ 不在 `TARGETS`** ⇒ **日後任一批對其附加（狀態 `M`）⇒ `wg9103` 必 `rc = 1`**
（`GB-88 四` 之設計行為·⛔ 非 bug）；處置 ＝ 該批補登 `TARGETS`。

---

## §八　取號（🔒 **每格逐字帶其正規式**·`VR-066 二`·母體逐項具名·態 ＝ `blob@99e200a`）

| 號 | 母體 | 樣式（逐字） | 相異 | `max` |
|---|---|---|---|---|
| `VR` 裸 | `docs/驗證裁定登記表.md` | `VR-(\d+)` | `72` | `72` |
| `VR` 窄 | 〃 | `^## ` ＋ 反引號 ＋ `VR-(\d+)` ＋ 反引號 | `58` | `72` |
| 自誤 裸 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `自誤\s*(\d+)` | `128` | `135` |
| 自誤 窄 | 〃 | `^## 自誤 (\d+)` | `92` | `135` |
| `GB` 裸 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `GB-(\d+)` | `93` | `93` |
| `GB` 定義列 | 〃 | `^\|\s*\*\*` ＋ 選擇性反引號 ＋ `GB-(\d+)` ＋ 選擇性反引號 ＋ `\*\*` | `88` | `93` |
| `K-9` 窄 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `^###\s*(?:🔒\s*)?` ＋ 選擇性反引號 ＋ `K-9-(\d+)` ＋ 字界錨 | `15` | `17` |
| 考古節 | `.claude/skills/failure-archaeology/SKILL.md` | `^##\s*(\d+)\.\s` | `124` | `124` |

⚠️ **`VR` 裸 `72` vs 窄 `58`** 之差含 `VR-071`（**懸空引用·⛔ 無定義節**·見 `§五`）。

---

## §九　環境與陷阱（本波實付學費者·**下一窗直接可用**）

1. 🔴 **`core.autocrlf` 之 system 層 ＝ `true`** ⇒ harness 新開之 worktree 之 `.md` **全檔 CRLF**；
   ⛔ 不得以 `git checkout --`／`git stash pop` 復原（皆經 smudge）⇒ **另行 clone（`-c core.autocrlf=false`）**。
2. 🔴 **短路徑係硬要求**（見 `§二`）：`183` 字元之 scratchpad ⇒ `wg942` 靜默跳過 `24/62` 檔而仍印綠。
   🔒 **判法**：跑 `wg942` 後查「`無此檔（新檔）`」之列數，須 `0`。
3. 🔴 **`run_all` 必於<u>拋棄式 clone</u> 內跑**：其會**改寫 `3` 個已追蹤 log**
   （`verify/out/probe_ruling_K4_s_origin.log`／`…K8_baseline_pairing.log`／`…K8_sideline_pairing.log`），
   另產 `6` 個 `.gitignore` 涵蓋之 `got_*.csv`。根因 ＝ 三支 probe 之
   `with open(LOG, "w", encoding="utf-8") as f:` **⛔ 無 `newline` 參數**（`GB-94`·⛔ 未入倉）。
   🔒 **平台相依**：Linux 側跑完 `status` ＝ `0` 筆。
4. 🔴 **`run_all` 一趟 ＝ `250`〜`520` s**（本機實測）；`run_verification.py` 直呼 ＝ `~222` s。
5. 🔴 **`GB-95`（⛔ 未入倉）**：`run_all:189` 使子器 `stdout` **只在紅時被印**、且僅末 `10` 列
   ⇒ 探針輸出**必被吞** ⇒ 欲取子器之逐字輸出，**須直呼該子器**。
6. 🔴 **`run_all` 之 stdout 於本機為 CRLF** ⇒ 逐列比對前須去 `CR`；
   `s.split` 之「換行切」＝ `419`／`s.splitlines()` ＝ `418`（現況趟）。
7. 🔴 **Python `3.13.11` ≥ `3.13`** ⇒ traceback 之多列語句壓縮渲染使
   `W-G G.2 世代幾何曝出契約` 落**類 I**（Linux `3.12.3` 落**類 III**）⇒ 類 III ＝ **`11`**（Linux `12`）。`GB-93`。
8. 🔴 **`rc` ⛔ 不是證據**（`VR-063 一`）：須 `rc` ＋ `stdout` 結論列逐字 ＋ `stderr` 三段式對拍
   （硬底線四詞 `Traceback`／`Error`／`Exception`／`Fatal` 命中 `0` ＋ 事先具名之無害類 ＋ 未歸類者 `0`）。
9. 🔴 **驗收格之每一合取項須各自具負對照**（`VR-068 四`）：`probe_WG9104` 之 `rc` **恆 `0`** ⇒ ⛔ 不得入合取項。
10. 🔴 **量詞須與其樣式同格綁定**（`VR-066 二`）：「換行切」與 `splitlines()` 差恆為 `1`；
    「非註解者」⛔ 無判定式時 docstring 續行兩面不是人 ⇒ **證「零行為變更」須比<u>去 docstring 之 `AST`</u>**。
11. 🔴 **`{N,M}` 之上下限係靜默過濾器**（`VR-066 四`）：`自誤\s*([0-9]{2,3})` 吃掉個位數號（差集恰 `{7,8,9}`）。
12. 🩸 **本機 Git Bash 之 Python 預設 `cp950`** ⇒ 設 `PYTHONUTF8=1`／`PYTHONIOENCODING=utf-8`。
13. 🩸 **`git grep` 無命中時 `rc = 1`**——`rc` 為 `0` 或 `1` 皆正常。
14. 🩸 **`ls-tree` 輸出⛔ 不可以空白切**（倉內有 `45` 條含空白之路徑）；一律 `splitlines()` ＋ `-c core.quotepath=false`。
15. 🔴 **常規一 `②` 之「等效更強」款（`VR-069`）**：碼檔就地改（`deletions > 0`）在
    「期末全檔 `sha256` 事前具名 ∧ 實得逐位相符 ∧ `numstat` 增／刪二欄事前具名且相符」三者齊備時
    **視為滿足 `②`** ⇒ ⛔ 不必請示。⚠️ ⛔ 無事前 `sha256` 者仍須請示。

---

## §十　未解項

| # | 未解 | 狀態 |
|---|---|---|
| 1 | 🛑 **`W-G.9-109`／`W-G.9-110` 之 payload 入倉** | 二單皆停機；停機格皆為**發單側期望字樣之錯**；`§四-3` 之 `自誤 137` 重疊須先解 |
| 2 | 🔴 **懸空引用** `VR-071`／`GB-94`／`GB-95` | 見 `§五`；`W-G.9-110` 之 payload 即為補之 |
| 3 | 🔴 **`R6` 右街角地之修法** | 肇因已隔離至 `4df19f4`、例外已否證 ⇒ **下一步查選取條件**（`select_corner_lots_both_sides_v12`） |
| 4 | 🛑 **期望 FAIL 名單之正當重生** | `K-9-18` 已裁 `R6` 街角地確定存在 ⇒ **⛔ 不得重記**（重記即追認已知為錯之狀態）；`GB-61` 之「名單失效後如何處置」仍待 KL |
| 5 | ⛔ **`run_all` 掛載**（`GB-88 五`／`丙-1`） | `W-G.9-107` 已裁**⛔ 不出**（三前提全數證偽·`VR-071 三`）——惟該裁⛔ 未入倉 |
| 6 | ⛔ `GB-94` 之 `158` 站（`newline` 參數） | 只登記·⛔ 不修（受詞逾任一驗收批之正交範圍） |
| 7 | ⛔ `GB-95`（`run_all` 輸出政策）／`GB-86`（`[-300:]`）／`GB-93` | 皆須與**名單正當重生**同批 |
| 8 | 🛑 `GB-67`／`R4` 結構閘 `Δ=2.415`（落地前置） | 未修 |

---

## §十一　回報紀律（本波實行者）

- **倉內報告＝唯一本體**；聊天回報 ＝ `git log -1 --format="%h %s"` **原樣貼** ＋ 倉內路徑。
- **push 後三重驗證**：`ls-remote` 直查／`merge-base --is-ancestor`／`rev-list --left-right --count` ＝ `0 0`。
- **常規一**：四項齊備 ⇒ 逕行 `push`、⛔ 不必請示；任一項不齊 ⇒ **須請示**（`②` 另見 `§九-15`）。
- **停機**：照字面停機、⛔ 不自行套用寬免；報紅時先判「錯的是**被檢物**、**期望字樣**、抑或**量測器**」，
  ⛔ 不改被檢物去湊、⛔ 不改期望值去湊。
- 🔒 **開工第一件事（`VR-063 二`）**：算施工單自身之 `sha256` 並與其 `§零`／`§八` 之自載值比對；
  不符 ⇒ 手上不是最新版 ⇒ 索取正確版本、本單不辦。

---

## §十二　付印前機檢

- 本文所引之 `commit`／`sha256`／列數／取號**皆現查於 `99e200a`**（⛔ 無一轉引）。
- `W-G.9-111` 於本倉之現查命中 ＝ **`0`**（⇒ 本文係首用該編號）。
- 本文係**新檔**（狀態 `A`·⛔ 非 `M`）⇒ `wg9103` 之受詞（`M(.md)`）⛔ 不含本檔 ⇒ 該閘仍綠。
- 本文⛔ 不含 `wg973` 之缺號觸發字樣；且其母體僅 GB 登記表，本檔⛔ 不入其母體。
- 本文 `CR` 位元組 ＝ `0`。
