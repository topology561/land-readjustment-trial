# 【W-G.9-24 前置登記】交接前補登（⛔ 零生產碼·純入倉）

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`87aeeba`（§0 現查所得·⛔ 非施工單所引）
**字樣錨（倉規 A）**：`【W-G.9-24 前置登記】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行
　🔒 判別力自檢：改用寬字樣 `W-G.9-24` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> 🔒 **本檔為 commit A：⛔ 零生產碼、⛔ 零碼變更、⛔ 零量測（本批本即無量測）。**

---

## §0 登時已現查（⛔ 逐項附母體與判準）

| # | 指令／母體 | 判準 | 結果 | 對照組 |
|---|---|---|---|---|
| ① | `git rev-parse --short HEAD` | — | **`87aeeba`** | — |
| ② | `git grep -l "W-G.9-24"`（母體＝git index） | 檔數 | **0** | `W-G.9-23` ⇒ **6 檔** |
| ③ | `docs/驗證裁定登記表.md` 是否已存在 | `ls` | **已存在**（建檔於 `W-G.9-12`） | — |
| ④ | 既有 `VR` 編號（母體＝該檔·判準＝`\| **編號** \|` 之行） | 已占用之號 | `VR-001`／`VR-002`／`VR-003` | 全倉 `git grep -o "VR-0[0-9][0-9]"` ⇒ **同三號** |
| ⑤ | 既有交接文（母體＝`git ls-tree -r HEAD docs/reports/`·判準＝檔名含「交接文」） | 檔數 | **12 檔** | — |

⇒ 🔒 **下一個未占用號 ＝ `VR-004`**（倉規：**編號衝突一律以倉內為準；新則取下一個未占用號**）。

---

## §1 🔴 甲組之射程更正：**自行現查所得**（⛔ 不採信施工單之轉述）

施工單 §1 曰「依**上文一**」——🔴 **該「上文一」不在單內**（⛔ 自足鐵律·考古 **61**）。
⇒ 依判準（給 CC）：**⛔ 禁編造、禁以倉內近似物代替**
⇒ **改由本人自倉內現查該射程錯在哪裡**：

**現查**（母體＝`verify/out/probe_WG923_bslot_only.log`·判準＝含「到達之街廓序」之行）：

| 行 | 內容 |
|---|---|
| 6 行命中 | `['R1']` ×2（兩情境之「改前」）／`['R1','R2']` ×4（「改後」與「只改R1」各二情境） |

⇒ 🔴 **實際到達之街廓 ＝ `R1`／`R2` <u>兩個</u>；`R3`／`R4`／`R5`／`R6` <u>從未進入</u>。**

而 `W-G.9-23` 報告卷首寫「**本批只涵蓋 13 格（非 `forced`）**」
——🔴 **該句把「本批之受詞範圍」寫成了「本批之量測涵蓋」**：
`13` 係「**非 `forced` 之格數**」（受詞），而**實測涵蓋者遠少於此**。

🔒 **更正之界限（⛔ 施工單明定）**：**結論（`k*` 未變）⛔ 不撤，只縮射程。**
⇒ 加註曰：**該結論之射程限於<u>實際到達之街廓</u>（`R1`／`R2`）**，
⛔ **不宣稱** `R3`〜`R6` 之 `k*` 亦不變（**無從判定**·⛔ 非判定為偽）。

---

## §2 本批之交付（⛔ 純入倉·無量測）

| 組 | 交付 | 落點 |
|---|---|---|
| **甲** | 射程更正（**加註**·⛔ 原句一字不刪·**兩處**） | `docs/reports/W-G.9-23_僅修非forced之B選槽.md` 之**報告本文**與**呈 KL 稿** |
| **乙** | 對話側裁決 **5 則**（`VR-004`〜`VR-008`） | `docs/驗證裁定登記表.md`（⛔ **非** `docs/rulings/`） |
| **丙** | 施工單方之自誤 **5 則**（逐則具名） | 新交接文之專節（⛔ 不入 `驗證裁定登記表.md`——該檔之範圍為**裁定**、非**錯誤**） |
| **丁** | **新視窗交接文**（🔴 本批之主交付） | `docs/reports/W-G.9波_新session交接文_WG913-WG924.md` |

---

## §3 ⛔ 明確未授權

1. ⛔ **零生產碼**；⛔ 不改任何閘、容差、凍存物。
2. ⛔ **不撤 `k*` 未變之結論**（**只縮射程**）。
3. ⛔ **不寫入 `docs/rulings/`**（乙組屬**驗證紀律層**，⛔ 非域裁）。
4. ⛔ 不動 `app.py`／`verify/`／`CLAUDE.md`。⛔ 文件刪除欄一律 **0**。

---

## §4 🔒 本批自我加課（承本波之戒）

| # | 規矩 | 本批如何遵守 |
|---|---|---|
| 1 | **文件插入之錨須為<u>行尾或整行</u>** | ⛔ 不以行中片語為錨（`W-G.9-22` 曾據此剖斷 `GB` 表之一列） |
| 2 | **每次寫入後以獨立指令驗其落地** | 含 `numstat` 之刪除欄與**逐列淨管線數** |
| 3 | **交接文之考古清單⛔ 不摘要成通則** | **逐節列號 ＋ 一句話根因**（施工單 §4 明定） |
| 4 | **交接文所引之錨皆為<u>字樣錨</u>** | ⛔ **無行號**（倉規·行號衛生第 4 款）＋**附唯一性自檢** |
| 5 | **每一個數附受詞** | **母體 ＋ 判準**（⛔ 裸數不出艙） |
| 6 | 凡書寫「正典」**就地標明權威序層級** | `docs/rulings/` ＝ **第 1 級**；`docs/驗證裁定登記表.md` ＝ **⛔ 不在四級之內** |

---

## §5 commit 分立

- **A** ＝ 本檔（⛔ 零碼、零量測）
- **B** ＝ **無量測 ⇒ 併入 C**（施工單 §7 明定「可併」）
- **C** ＝ 射程更正 ＋ 乙丙補登 ＋ 交接文 ＋ 考古節（若有）

⚠️ **依倉規（不可變文件不記可變值），本檔 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本檔之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
