# W-G.9-31：換窗前入倉

**基座**：`wg925-handoff` @ `270acab`（＝`origin/wip/s1-endpart`）
**性質**：⛔ 零生產碼 · ⛔ 零凍存物更新 · **純追加／純新建**
**結論**：四處落地；`R-1`〜`R-6` 全部未觸發（`§4-6` 之判別力對照曾失效、已重選並重驗）。

---

## §1 編號現查（🔴 依**考古 82** 逐筆看落點·⛔ 不憑命中數）

| 受詞 | 字樣命中 | **逐筆落點之判** | 判 |
|---|---:|---|---|
| `W-G.9-31` | **0 檔** | 無實體、無任何引用 | **未占用**·⚠️ 命名式與前批 `W-G.9-30` 一致（已對照） |
| 考古下一節號 | `^## 82\. ` ⇒ **0** | 最大節號 **81**；`^## 81\. ` ⇒ **1**（判別力自檢·證該式非恆 0） | **`82` 未占用** |
| `L1` 之行首錨 `^\| \*\*GB-51\*\* 🆕 \|` | **1** | —— | ✅ 唯一 |
| 判別力自檢：寬錨 `GB-51` | **2** | ⇒ 證「行首唯一」**≠**「字樣唯一」 | ✅ 非恆 1 |

---

## §2 佔位符之填值與其現查式（考古 81）

| 佔位符 | **所填之值** | 現查式與結果 |
|---|---|---|
| `<下一未占用節號>` | **`82`** | `grep -o "^## [0-9]*\. " <考古檔> \| grep -o "[0-9]*" \| sort -n \| tail -1` ⇒ **81**；`grep -c "^## 82\. "` ⇒ **0**；判別力 `grep -c "^## 81\. "` ⇒ **1** |
| `<起批>` | **`WG913`** | 承接前一份交接文之涵蓋起點（其字樣錨 `【W-G.9 交接文 WG913-WG924】`） |
| `<迄批>` | **`WG931`** | ＝ 本批號 |
| `<改前行數>`（考古檔） | **`2821`** | `wc -l` 於本批寫入前 |

### 🔒 §2-1 逐字內所載之數已獨立覆核（⛔ 未採信單內數）

單之 `L1` 列載「**現查為 4 命中，其中 1 個是<u>真呼叫</u>**」。**原樣執行其現查式** ⇒ **4 命中**，逐筆：

| 落點 | 性質 |
|---|---|
| `app.py` 之 `🔒 提出後 \`_block_strip\` 之行為**逐位不變**` 所在行 | 註解 |
| `app.py` 之 `                 n_hat_far=None):` | `def` 預設值 |
| `app.py` 之 `       \`n_hat_far=None\`（現行全部呼叫端之預設）` | 交叉引用註解 |
| `app.py` 之 `allocation_dir=_near_ad, n_hat_far=_far_nhat)` | 🔴 **真呼叫** |

⇒ **與單逐格相符**（判別力自檢：偽字樣 `n_hat_ZZZ=` ⇒ **0**）。

---

## §3 四處落地

| # | 檔 | 手法 | 前 → 後 |
|---|---|---|---|
| **L1** | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 插一整列於行首錨列之後 | 307 → **308**；`^\| ` **116 → 117** |
| **L2** | `.claude/skills/failure-archaeology/SKILL.md` | 檔末追加考古 **82** | 2821 → **2851**（**+30**） |
| **L3** | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **新建**（逐字全文） | — |
| **L4** | `docs/reports/W-G.9波_新session交接文_WG913-WG931.md` | **新建**（CC 之倉態交接文·十一節） | — |

---

## §4 落地自證（六項·考古 68／63）

**1. `git show --numstat`**——刪除欄全 `0`（見 commit）。

**2. 禁動清單** `git diff --numstat HEAD -- app.py verify/*.py verify/**/*.py docs/rulings/ CLAUDE.md verify/baselines/ verify/out/ docs/design/` ⇒ **空** ⇒ **`R-4` 未觸發**。
🔒 判別力自檢：同式對本批異動檔 ⇒ **非空**（`30 0` 考古檔／`1 0` 登記表）。

**3. `W-G.4` 之 `^\| ` 列數**：**116 → 117**（**＋1**）✅

**4. 考古檔純追加**：`^## 82\. ` ⇒ **1**、`^## 83\. ` ⇒ **0**。
🩸 **一項量測框自誤（已更正）**：首次以 `head -2821 <工作樹> | diff - <(git show HEAD:<檔>)` 比對，得「**前段被動**」。
**診斷**：工作樹前 2821 行 `wc -c` ＝ **222,358**、blob 同段 ＝ **219,621**，差 **2,737**
⇒ **行尾框差**（該檔為**混合行尾**·⛔ 非本批所致）。
依 `VR-011` **先正規化行尾**後重驗 ⇒ **逐字相同** ⇒ **純追加、既有 81 節零觸**。
🔒 判別力自檢：同式對一個本批**未動**之檔 ⇒ 判為**同** ⇒ 該式**非恆紅**。

**5. 兩份新建檔**：各 1；`L3` 之六則表列 `^\| \*\*1\*\* \|`〜`^\| \*\*6\*\* \|` ⇒ **各 1**。

**6. `L4` 之自足性自檢之實測**（母體 ＝ 扣除 `§11` 起至檔末）

| 式 | `L4` | 判別力對照 | 判 |
|---|---:|---|---|
| 行號錨樣態 `:[0-9][0-9]` | **0** | `docs/reports/W-G.9-29_R2破量歸因與授權入倉.md` ⇒ **9** | ✅ |
| 對話側引用樣態 | **0** | 🔴 對 `W-G.9-29` 報告 ⇒ **0**（**對照失效**）⇒ **重選** `docs/reports/W-G.9-25_Read自報行數量測.md` ⇒ **1** | ✅ |

🩸 **具名**：施工單 §4-6 指定之對照檔（`W-G.9-29` 報告）對「對話側樣態」**回 0**
⇒ 依**考古 80 修法 ③**（對照組回 0 而該 0 正好支持你想證的命題 ⇒ **先疑對照組**），
**該次判別力自檢作廢**、重選一個**已知含該樣態**之檔方成立。⛔ 未以「對照也 0」充作通過。

**`R-6` 之判**：`L4` 內含「自誤」之行 ⇒ **4**，逐行查為**聲明與指路**
（`⛔ 不載驗證側之自誤內容，僅於 §7 指其路徑`／節題／`⛔ 本檔不轉述其內容`／該檔之字樣錨）
⇒ **⛔ 無內容轉述** ⇒ **`R-6` 未觸發**。

---

## §5 停機條件之逐條判

| 碼 | 判 | 依據 |
|---|---|---|
| **R-1**（佔位符未填） | ⛔ 未觸發 | §2 四項皆已填並附現查式 |
| **R-2**（僅憑命中數） | ⛔ 未觸發 | §1 逐筆看落點 |
| **R-3**（任何刪除行） | ⛔ 未觸發 | 兩異動檔之 `git diff` 刪除行皆 **0**；兩新建檔無前身 |
| **R-4**（禁動清單） | ⛔ 未觸發 | §4-2 空（含 `docs/design/`） |
| **R-5**（`L4` 自足性未過） | ⛔ 未觸發 | §4-6 兩式皆 0·對照皆非 0 |
| **R-6**（`L4` 載自誤內容） | ⛔ 未觸發 | §4 末 |

---

## §6 ⛔ 本批未做之事

1. ⛔ **未寫任何碼**、⛔ 未動 `app.py`／`verify/` 下任何檔／`docs/rulings/`／`CLAUDE.md`／`verify/baselines/`／`verify/out/`／`docs/design/`。
2. ⛔ **未更新任何凍存物或 baseline**（`VR-012`）。
3. ⛔ **未跑** `run_all`、⛔ 未跑任何探針（本批之現查全為靜態）。
4. ⛔ **未改 `GB-51` 之原列一字**——其「現況欄過期」由新增之 `↳` 列承載。
5. ⛔ **未處置** `L3` 所載之任何一則（該檔為**登記**、⛔ 非處置）。
6. ⛔ **未修正** `.claude/skills/failure-archaeology/SKILL.md` 之混合行尾（既有狀態·⛔ 非本批所致；修它會產生大量偽異動）。

---

## §7 逐條自檢

| # | 戒 | **已套用** |
|---|---|---|
| 1 | **考古 82**（查到 ≠ 已占用·本批所立） | §1 兩處編號現查皆**逐筆看落點**；`W-G.9-31` 之 0 命中另附「命名式一致」之確認 |
| 2 | **考古 81**（逐字區塊 ⛔ 不含須現查之值） | §2 四佔位符之填值與現查式具名 |
| 3 | **考古 80 ＋ 增量** | §4-4 之量測框自誤已依 `VR-011` 更正；§4-6 之失效對照已依修法 ③ 重選 |
| 4 | **考古 68**（已完成 ≠ 已落地） | §4 六項獨立指令 |
| 5 | **考古 63**（同一次執行） | 本報告之數皆為本批當次輸出 |
| 6 | **紀律第 8 條 ＋ `VR-011`** | `L1` 之錨為**整列**·插入器比對前正規化行尾 |
| 7 | **交接文兩份 ⛔ 不共用** | `R-6` 已驗 |
