# W-G.8 D-2b-25 commit A：**逐字轉錄清單**（⛔ 尚未寫入正典·⛔ 全程零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**基準**：`78079fa`
**性質**：本檔**只列出將要寫入之文字與其落點**；實際寫入於 commit C，屆時逐段對帳。

---

## §0 編號與波名（現查·⛔ 不引單內之數）

| 項 | 現查指令 | 結果 | 判 |
|---|---|---|---|
| 本單編號 | `grep -rn "D-2b-25" . --include=*.md --include=*.py \| grep -v worktrees` | **0 命中** | ✅ `D-2b-25` 可用 |
| 新波名 | `ls docs/reports/ \| sed 's/_.*//' \| sort \| uniq -c` ⇒ 前綴族 `W-G.4`(52)／`W-G.5`(47)／`W-G.6`(2)／`W-G.7`(12)／`W-G.8`(22) | 序列最大 ＝ **W-G.8**（本波） | ⇒ 新波取 **`W-G.9`** |
| `W-G.9` 是否已占用 | `grep -rn "W-G\.9" . --include=*.md \| grep -v worktrees \| wc -l` | **0** | ✅ 不相撞 |
| 登記表現行最大 | `grep -o "^\| \*\*GB-[0-9]*" <登記表> \| grep -o "[0-9]*" \| sort -n \| tail -1` | **52** | ⇒ 新項自 **`GB-53`** 起 |
| 考古末節 | `grep -n "^## [0-9]" <SKILL.md> \| tail -1` | **58** | ⇒ 新節 **59** |

**波名依據**：`docs/reports/` 之波名採 `W-G.<n>` 序列（`.4`→`.8` 連號），本波為 `W-G.8`
⇒ 次波順序取 `W-G.9`，且全倉 0 命中、不相撞。

---

## §1 KL 裁示八條（**逐字**·落點：`docs/rulings/K-6_街角地分配程序與可分配判準.md` 末尾新節）

🔒 每條均註「**KL 裁 2026-08-10**」；標示【裁】或【知會·未見異議】。
⛔ **以下文字為將寫入之逐字內容**，⛔ commit C 不得改寫、不得概括、不得補充未說之語。

> ### 🔒 K-9-5-9 KL 裁示八條（KL 裁 2026-08-10·D-2b-25·canonical·**逐字轉錄**）
>
> **【裁】1.** 街廓 R1 之「選槽依據與實際落地不符」（結構閘 理論＝實跑）列為**另案**。
> ⛔ **未裁事項**：本波是否因它停機、D-3 重烤時點——⛔ 二者**不得由「另案」推定**，須另行請裁。
>
> **【裁】2.** 業主宗互疊超出捨入容許量者，係**程式錯誤**。一律停機、**不得產出分配成果**、
> ⛔ **不得降級為警告**。
>
> **【裁】3.** 檢查**一律跑完全部街廓**、把問題一次列齊；但只要有任何一處未過，
> **分配成果一概不產出**。
>
> **【裁】4.** `N0-20 §二.2` 之「自末端 ALLOCLINE 之 mp 往街廓內量 W」**失效**。
> mp／W 之量法**僅**用於有 SIDELINE 側計算 W 以對 Rw 表
> （側街 18m 內各宗地均須負擔臨側街地之精神）。
> 末端塊之寬度（≥ 畸零地寬）**仍依畸零地使用規則所定之寬度量法**。
>
> **【裁】5.** 第 2 宗與第 3 宗共用之境界線**平行 ALLOCLINE 方向**。
> 第 2 宗最小規定範圍 ＝ 過 `Ps` 之 ∥ALLOCLINE 直線向街廓內平移畸零地寬所夾之範圍；
> 此情形（第 1 種）之**最小寬度落在 FRONTLINE 交點**。
>
> **【裁】6.** 另一情形（第 2 種）之**最小寬度落在 BASELINE 端**。FRONTLINE 與 BASELINE 不平行、
> 但二者皆為直線 ⇒ 距離沿深度呈線性遞增／遞減 ⇒ 須於最小深度帶下解出畸零地寬之位置。
>
> **【裁】7.** 第 2 宗最小規定範圍**另開一波**；D-2b 不動。
>
> **【知會·未見異議】8.** 第 2 宗最小規定範圍用**街廓層**最小寬度帶
> （前緣線沿 BASELINE 法向平移 `round(D_avg,2)` 所夾之帶）；
> 事後寬度驗收仍用**該宗自己**之帶。兩把尺不同係刻意（打斷寬度／深度互相依賴）。
>
> 🛑 **圖證待補**：第 5、6 條所指之三張示意圖，**本批未入倉**——理由見
> `docs/reports/W-G.8_D-2b-25A_逐字轉錄清單.md` §2。⛔ 補入前，本二條之幾何細節
> **以文字為準**，⛔ 不得以任何倉內近似圖代替。

**§8-1 停機條件檢查**：本八條於倉內**未查得更晚之相反裁示**
（`grep -rn "2026-08-1[0-9]" docs/rulings/` ⇒ 除本批外無其他 2026-08-10 之後裁示）⇒ **未觸發**。

---

## §2 🛑 圖證：**單內無圖檔·待補**（⛔ 未辦·非 CC 之選擇）

施工單 §2 令將「KL 於 2026-08-10 提供之三張示意圖」入 `figures/`。

**現查**：
1. **本次訊息未附任何圖檔**（無 image、無附件、無路徑）。
2. `figures/` 目錄**不存在**；`docs/figures/` 亦不存在。
3. 倉內未有 2026-08-10 之新增圖檔。

⇒ 依 `CLAUDE.md`「🔒 **施工單必須自足**」之判準（給 CC）逐字：
**「收到『將本訊息之 X 入倉』而單內查無 X 時，⛔ 禁編造、禁以倉內近似物代替；
照實回報『單內無 X』並請補」**（同族：失敗考古節 52）。

⇒ **§2 全項未辦**，包含其附帶之「§1-5／§1-6 行內註檔名」與
「`P0`／`Ps`／`S1` 定義文字之差額補寫」。**請 KL 補送圖檔後另批處理。**

⚠️ **併記（供補送時省一步）**：`P0`／`Ps`／`S1` 之定義**正典已載**——
`docs/rulings/K-6_街角地分配程序與可分配判準.md` 之 `K-9-5-4` 段逐字：
「平移量 **`S1 = max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)`**／`P0` ＝ FRONTLINE 與 SIDELINE 之
**未截角理論交點**／`Ps` ＝ FRONTLINE 與**道路截角線段**之交點」
⇒ **已載者不得重寫**（施工單 §2 逐字）；補送時**只需補未載之差額**。

---

## §3 `N0-20 §二.2` 之作廢加註（⛔ 不刪原句）

**落點**：`docs/specs/W-G.4_規格v3補丁七_N0-20幾何定義正典.md`，**第 18 行**之後
（該行逐字現查：
`2. **末端帶＝末端 ALLOCLINE 向街廓內平移「畸零地寬」之平行帶**；量法（KL 裁三）＝**自末端 ALLOCLINE 之 mp 往街廓內量 W**；兩線平行 → **W＝S＝畸零地寬**（本案 3.5）。`）

**將加註之文字（逐字）**：

> 🔴 **【作廢·勿復原】上行之量法句「自末端 ALLOCLINE 之 mp 往街廓內量 W」自 `K-8`
> （2026-07-31）起失效**（KL 裁 2026-08-10·`D-2b-25`）。
> **矛盾之出處**：`K-8` 作廢欄逐字（現查 `grep -n "保證不了寬度" app.py` ⇒ `:10108`／`:10940`）：
> 「舊 `_build_corner_range_v2`（SIDE_LINE 中點沿 ALLOC 法向平移 退縮＋最小寬 → 切 BLOCK）已刪。
> KL 指其保證不了寬度：錨在中點，且平移沿 ALLOC 法向、而寬度沿前緣線量，**兩者不是同一個量**。」
> **更正（依 KL 裁 2026-08-10 第 4 條）**：mp／W 之量法**僅**用於有 SIDELINE 側計算 W 以對 Rw 表；
> **末端塊之寬度（≥ 畸零地寬）仍依畸零地使用規則所定之寬度量法。**
> ⛔ **原句保留供對照，⛔ 不得復原為現行判準。**

🔒 **§9-2 之可驗性**：加註後，原句與更正註**同時存在**，
可由 `grep -n "往街廓內量 W" docs/specs/W-G.4_規格v3補丁七_N0-20幾何定義正典.md` **一次同時命中**。

**§8-2 停機條件檢查**：本更正**不使**任何既有已通過之閘或既有實作與正典衝突
——它**明文承認**現行 `_end_band` 之實作與更正後之正典**不一致**，
並將該不一致**登記為 `GB-53`**（§4-a）而非改碼 ⇒ **未觸發**。

---

## §4 缺陷登記五項（落點：`docs/reports/W-G.4_泛用阻塞項登記表.md`·**接續 `GB-53`〜`GB-57`**）

**§8-3 檢查**：登記表現有 `GB-40`〜`GB-52`，其中**無** `st.warning`／app-vs-harness 偏離之項
（`grep -c "st.warning" <登記表>` ⇒ **0**）⇒ **無重複登記** ⇒ **未觸發**。

| 新編號 | 要旨（將寫入） | **內容 grep 錨（`78079fa` 上實際命中）** |
|---|---|---|
| **`GB-53`** | (a) `_end_band` 仍以 **⊥ALLOC 垂距**平移畸零地寬構帶 ⇒ 即 `K-8` 已作廢量法之**同型**；與畸零地使用規則之寬度量法**不是同一個量**（§1-4 之碼面對應） | `grep -n "def _end_band" app.py` ⇒ `:9510`；其 docstring 逐字 `:9514` 「向街廓內 **⊥垂距** `min_width` 之平行帶」；作廢欄 `grep -n "保證不了寬度" app.py` ⇒ `:10108`／`:10940` |
| **`GB-54`** | (b) `verify/stepg_pipeline.py` 於「街廓抵費地計算失敗」處 `raise`，而 `app.py` 同一結構位置為 `st.warning` ⇒ **驗證複刻件與生產偏離**；此係 `GB-52` 盲區之**真因**（⛔ 非結構閘 理論＝實跑） | `grep -n "抵費地計算失敗" app.py verify/stepg_pipeline.py` ⇒ `app.py:19962`（`st.warning`）／`verify/stepg_pipeline.py:987`（`raise RuntimeError`） |
| **`GB-55`** | (c) 三道閘（覆蓋／池不重疊／宗-宗互疊）之失敗**一律冠以「抵費地計算失敗」**，其中**宗-宗互疊與抵費地無關**；且該閘為**全對加總**、**不指認宗對** | `grep -n "①' 覆蓋閘破\|②-池 不重疊閘破\|②-宗 圍堵閘破" app.py`；加總式 `grep -n "biz_x += float" app.py`；冠名處同 `GB-54` 之兩行 |
| **`GB-56`** | (d) 檢查破時：業主宗之列**已先寫入成果**、抵費地之列**因寫在檢查內故未寫成** ⇒ 成果表「**該擋的沒擋、不該少的少了**」（§1-2／§1-3 之碼面對應） | `grep -n "g_rows.append" verify/stepg_pipeline.py` ⇒ `:957`（業主宗列·在池呼叫**之前**）；池呼叫與其後之抵費地列寫入皆在 `grep -n "_pool_strips_for_block(" verify/stepg_pipeline.py` 之 `try` 內（`raise` 於 `:987`） |
| **`GB-57`** | (e) `docs/reports/W-G.8_D-2b-23C_甲後逐格對帳.md` 載 `near_dir` 命中 **25**；倉內現查為 **24** ⇒ **以倉為準**，該報告數字更正並註明 | `grep -c "near_dir" app.py` ⇒ **24**（`78079fa` 現查）；報告內誤載處 `grep -n "本樹為 25" docs/reports/W-G.8_D-2b-23C_甲後逐格對帳.md` |

⚠️ **`GB-57` 附帶之文件更正**：commit C 須於 `W-G.8_D-2b-23C_甲後逐格對帳.md` §8-1
加註「🔴 具名更正：本文載 `25`，倉內現查為 `24`；以倉為準」——⛔ **原句不刪**。

---

## §5 開波文件（落點：`docs/W-G.9_第2宗最小規定範圍_範圍界定.md`·⛔ 只建文件·零實作）

**內容須含且僅含五項**（施工單 §5）：

1. **目標**：第 2 宗最小規定範圍（KL 裁 2026-08-10 第 5／6／8 條）。
2. **已知可重用之機具**：`K-8 §三〜§五` 之解析構造——「取平行 ALLOCLINE 之直線往街廓內
   平行滑動，直到帶內二側界間、平行前緣線之距離**最小值**＝門檻」；解析捷徑逐字
   `k = sigma / den_a; w_lo = min(_W(0,0), _W(D,0)); t_star = (T - w_lo) / k`
   （現查 `grep -n "w_lo = min" app.py` ⇒ **`:11031`**）。
   🔒 **判讀**：該 `min()` 取到前緣端即第 1 種、取到 BASELINE 端即第 2 種
   ⇒ **同一構造之兩解、非兩套構造**。
   🔴 **標記**：「claude.ai 現查所得·**待實測坐實**」⛔ **不得逕列為事實**。
3. **算子現況（CC 自行 AST 現查·⛔ 未引施工單之數）**：
   - `k97_solve_alloc_t` **定義 1 處**（`app.py:10398`）
   - **`app.py` 內呼叫 ＝ 0**（AST `ast.Call` 窮舉）
   - **`_WF_NS_NAMES` 未納入**（AST 取該 `Assign` 之 `elts` ⇒ 長度 **25**、不含該名）
   - 全倉非 `app.py`／非 `verify/probes/` 之命中**僅** `verify/fixture_corner_range_k8.py:68`／`:81`／`:148`，
     且三處皆為**否定敘述**（「⛔ 不自 `k97_solve_alloc_t` … 取任何回傳值」）⇒ **非呼叫**
   - 正典「D 波之範圍界線」第 5 項明載其「**不刪、不停用、不標作廢**——第 2 宗轉為楔形後
     可能沿用同一套量測機具」（`grep -n "第 2 宗轉為楔形後" docs/rulings/K-6_街角地分配程序與可分配判準.md`）
4. **套用時須換之兩個參數**：門檻由「退縮寬＋畸零地最小寬」→「畸零地最小寬」；
   固定之一側界由「截角前之側街境界線」→「街角第 1 宗之遠側界」。
5. **本波不授權**：⛔ 不得於本波實作；⛔ 不得動 `D-2b` 已落地之任何一行。

---

## §6 失敗考古新節（落點：`.claude/skills/failure-archaeology/SKILL.md` 末尾·**節 59**）

**標題（將寫入）**：
`## 59. 在讀實作／查正典時序之前就先下結構性斷言（W-G.8 D-2b-25·**claude.ai 犯·KL 域裁與 CC 現查各抓出一部分**）`

**八個實例（逐條·將逐字寫入）**：
1. **複合是非題**——三個命題綁進一句，KL 只答其一，其餘二項一度被誤記為已裁。
2. **未查生產側 handler 即斷言「結構閘是盲區之唯一阻塞」**——現查生產為警告後續跑，斷言被推翻。
3. **把「第 2 宗轉為楔形」當成缺陷**——KL 圖示該形狀本即正常（與既有第 57 節同型·方向錯）。
4. **誤稱「目前 R1 係抵費地算不出來」**——現行版本該訊息出現 0 次。
5. **問「該不該量整宗全深」**——後緣為斜線時最深角點寬度趨近 0，該問法會使幾乎每宗量到 0。
6. **斷言「第 2 種之最窄處落在量測帶外」**——帶底逐宗跟著自己後緣走，該斷言錯。
7. **以末端塊類比推斷第 2 宗最小規定範圍之內側界平行側街**——實為平行分配線。
8. **引 `N0-20 §二.2` 未察其已被 12 天後之 `K-8` 推翻**——**未做正典時序檢查**。

**共同根因（逐字）**：
> **在讀實作／查正典時序之前就先下結構性斷言。**

**機械修法（逐字）**：
> 任何結構性斷言落筆前，須先 (i) AST／grep **現查實作**、
> (ii) 對所引正典條文**查其後是否有更晚之裁示推翻之**。

---

## §9-6 生產碼零觸自證（**原樣貼**）

```
$ git diff --stat -- app.py verify/
（空·無輸出）
```

---

## §7 不授權逐項自檢

| # | 不授權項 | 本 commit 之狀態 |
|---|---|---|
| 1 | `app.py`／`verify/**` 一字不動（含註解） | ✅ 未動（§9-6） |
| 2 | 不接線 `k97_solve_alloc_t`／`_block_partition`／不加入 `_WF_NS_NAMES` | ✅ 未動 |
| 3 | 不改任何閘之寬度或處置、不刪除／停用／標作廢任何既有閘 | ✅ 未動 |
| 4 | 不重烤 baseline、不設 `WV_BAKE`、不改期望 FAIL 名單 | ✅ 未動 |
| 5 | 不於本單實作 §5 之任何一項 | ✅ 僅建文件（commit C） |
| 6 | 不刪除既有正典文字 | ✅ §3 採「作廢·勿復原」加註 |

---

## 異動清單（本 commit A）

| 檔 | 異動 | 性質 |
|---|---|---|
| `docs/reports/W-G.8_D-2b-25A_逐字轉錄清單.md` | 新增（本檔） | 文件·**尚未寫入正典** |
