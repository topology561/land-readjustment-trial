# ✅ `W-G.9-166R`　`VR-078` 具名例外 ＋ `GB-113` ＋ 🔓 `GB-105` **解除** ＋ 自誤 `199`

**受單**：`W-G.9-166`（`SELF_SHA256 = 7b979a8ca3b061833ef256383ba10765001e9311f5cc96e8df1d9d5fea8136a8`·CC 覆算相符·四反引號區塊 `0`）
**基座**：`001f60a9afe36f24ed6e9ffeb2e8180faeba0bf2`
**性質**：量測 ＋ **純附加**（⛔ 零生產碼·⛔ 未改 `app.py`／`verify/*.py` 既有一字·**⛔ 未長跑**）

## ✅ §零　結論（先講）

| # | 事 | 值 |
|---|---|---|
| `1` | `VR-078` 五項判準 | **①②③④⑤ 全部成立** |
| `2` | ⇒ `§四`（`GB-105` **解除**） | ✅ **已入倉** |
| `3` | `W-8` 判別力自檢（人造**診斷文字**差·⛔ 非空白） | ✅ **`③` 轉紅** ⇒ 該例外**⛔ 非放行一切** |
| `4` | `§七` 十項預測 | 符 **`10`** ／ 不符 `0` ／ 不可判 `0`（和 ＝ `10` ＝ 預測總數） |

🔴 **惟 `M-1 ①` 之「`2`」須辨框**（見 `§三-1`）：單 `§一` 逐字「變更列數 **`2`**（＝ CC `165R` 所報）」——
二者**同為 `2` 而所指不同**：`165R` 之 `2` ＝ **框A** 之「只在期初 `1` ＋ 只在期末 `1`」之**合計相異**；
單之 `2` ＝ **框B** 之**變更列數**。二框並報後 `W-3` 於**框B** ✅ 符（`2` ／ `4`），**⛔ 非改預測**。

---

## §一　開工閘

| 錨 | 期望 | 實測 | |
|---|---|---|---|
| `HEAD` 全 `40` 碼 | `001f60a9afe36f24ed6e9ffeb2e8180faeba0bf2` | 同 | ✅ |
| `app.py` blob（期初＝期末） | `f285b91c9e5f97f13dd74508e1a403833bc75cb3` | 同 | ✅ |
| 工作樹 | 淨 | `git status --porcelain` ＝ `0` 列 | ✅ |

clone ＝ `/c/w166`（**拋棄式**·跑畢整份刪除）。🔒 **本批⛔ 未長跑**——四份全量 log 皆在倉（單 `§九` 末逐字）。

---

## §二　取號現查（定義列式 ＋ 錨定框 ＋ 二形並取）

🔒 母體 ＝ `docs/**/*.md` ＋ `CLAUDE.md` ⇒ **`394` 檔**；**態 ＝ `blob@001f60a`**（＝本批基座）。

| 待鑄 | 定義列式（逐字） | 相異 | MAX | 錨定框 `X N` | `` X `N` `` | 對照組 |
|---|---|---|---|---|---|---|
| `W-G.9-166` | `^docs/reports/W-G\.9-(\d{1,4})` | `116` | **`165`** | **`1`**（見下） | — | `W-G.9-165` ＝ `9` |
| `VR-078` | ``^##\s*`VR-([0-9]{1,4})` `` | `63` | **`77`** | `0` | — | `VR-077` ＝ `23` |
| `GB-113` | 三框聯集（母體 ＝ 登記表**單檔**） | `110` | **`112`** | `0` | — | `GB-112` ＝ **`7` 列**／**`8` 次**（見下） |
| 自誤 `199` | `` ^#+ 自誤 `?([0-9]{1,4})`? ``（母體 ＝ 自誤登記**單檔**） | `163` | **`198`** | `0` | `0` | `自誤 198` ＝ `0`／`` 自誤 `198` `` ＝ **`4`** |

🔒 **`W-G.9-166` 之錨定框 `1` 命中 ＝ `docs/reports/W-G.9-165R_停機_第四類未涵蓋行內位移.md:233`**
（`## §七　🛑 上呈發單側（W-G.9-166 之受詞·⛔ CC 不自裁）`）——即單 `§前置` 所宣告之**前瞻引用**，
其意指正為本單受詞 ⇒ 🔒 **⛔ 非衝突**（常規四（七）一②之反面：意指與本單同一，⛔ 非他批占用）。

🔒 **`GB` 算術自檢（常規八·外部錨已具名）**：`MAX(112) − |缺號|(2) == |已定義|(110)` ⇒ **True**；
缺號 ＝ `[12, 87]`。**外部錨** ＝ `docs/reports/W-G.9-144_開工閘與二項現查.md:81` 逐字所具名之**式A**
（``^## `GB-N` ``·二級標題·`3` 則 ＝ `GB-81`／`-82`／`-83`），本批實測**式A 專有成員 ＝ `{81, 82, 83}`** ⇒ 逐項相符。

### 🩸 `GB-112` 對照組之**框辨正**（單載 `8`·CC 得 `7`·⛔ 二者皆對）

| 框（逐字） | 得數 |
|---|---|
| **命中列數**（`grep -c` 框·錨定框與裸框**同得**） | **`7`** |
| **出現次數**（`str.count` 框·錨定框與裸框**同得**） | **`8`** |

⇒ 🔒 **⛔ 非相異之量測，係 `VR-058 一` 之二框**（一列含該號二次）。單之 `8` ＝ **出現次數**框。
🛑 二數並報，⛔ 不同表混框。

---

## §三　`M-1`：`VR-078` 五項判準之機械檢驗

**受詞**：`runall_77ebc49_full.log`（`506` 列／`46005` B／`sha256 f7bc0d78…a0d1`）
vs `runall_0e4b508_full.log`（`506` 列／`46006` B／`sha256 13cc1b3a…92d2`）·框 ＝ `splitlines()`。

### `M-1 ①`　變更列數（🛑 **二框並報**·`VR-058 一`）

| 框（逐字） | 只在期初 | 只在期末 | **變更列數** | `unified_diff` `±` 列數 |
|---|---|---|---|---|
| **框A**：四類正規化 ＋ 比較前 `line.strip()`〔`W-G.9-163R`／`-165R` 之**正典實作**〕 | `1` | `1` | **`1`** | **`2`** |
| **框B**：四類正規化·⛔ **無** `line.strip()`〔單 `§一`／`§五 ①` 所用之框〕 | `2` | `2` | **`2`** | **`4`** |

🔴 **二框之差恰為那一列「只差<u>行首</u>空白」者**（期初 `#327`）——`strip()` 使其**等價**，故框A 看不見。
🔒 **`VR-078 ①`（變更列數 ≤ `2`）於二框皆成立**（`1 ≤ 2`／`2 ≤ 2`）⇒ 判準之成立**⛔ 不繫於框之選擇**。
🩸 **單 `§一` 之「`2`（＝ CC `165R` 所報）」係<u>框之混同</u>**：`165R` 之 `2` 係**框A 之合計相異**
（`1 + 1`），而單之 `2` 係**框B 之變更列數**；框A 之變更列數實為 `1`、框B 之合計相異實為 `4`。
🛑 二者恰同為 `2` ⇒ **巧合**，⛔ 非同一量。本報告一律**標框並報二數**。

### `M-1 ②③④`　逐列（母體 ＝ **框B** 之變更列·為二框之上界）

| 列 | 位置 | ② 含特徵字樣 | ③ 摺疊空白後逐位相同 | ④ 空白 run（期初→期末） | ④ 非零差 |
|---|---|---|---|---|---|
| `1` | 期初 `#327` ／ 期末 `#327` | **True** | **True** | `[23,1,1,1,1]` → `[22,1,1,1,1]` | `[1]` ⇒ **恰差 `1`** |
| `2` | 期初 `#497` ／ 期末 `#497` | **True** | **True** | `[8,24,1,1,1,1]` → `[8,23,1,1,1,1]` | `[1]` ⇒ **恰差 `1`** |

**逐字**（自管線抽出·⛔ 未手打·常規七）：

```text
── 只在期初 #327 ──
                       params, build, winners, forced, setback)
── 只在期末 #327 ──
                      params, build, winners, forced, setback)
── 只在期初 #497 ──
        現況:                        params, build, winners, forced, setback)
── 只在期末 #497 ──
        現況:                       params, build, winners, forced, setback)
```

🔒 **④ 之方向一致性**：所涉檔 ＝ `verify/stepg_pipeline.py`，其 `raise` 由 `:995`（`3` 位）移至 `:1001`（`4` 位）
⇒ 行號位數 **`+1`**；而空白 run 之長度 **`−1`**（`23→22`／`24→23`）
⇒ 🔒 **定長尾切窗整體<u>後移</u> `1` 字元 ⇒ 首列被多切一空白**，**方向一致** ✅。

### `M-1 ⑤`　截斷式逐字（**自基座 blob 重抽**·常規七）

```python
    except Exception as _e_g1:                                              # :1390
        import traceback                                                    # :1391
        results.append(("W-G G.1 接線層 ctx-builder 同源", False,            # :1392
                        [f"[G.1] {_e_g1}", traceback.format_exc()[-700:]]))  # :1393
```

| 檢 | 結果 |
|---|---|
| `:1393` 含 `traceback.format_exc()[-700:]` | **True** ✅ |
| **正對照**（已知應為真）：`:1390` 含 `except Exception` | **True** |
| **負對照**（已知應為偽）：`:1393` 含 `format_exc()[-999:]` | **False** |
| 基座 blob ＝ 工作區 bytes | **True**（`96652` B） |

⇒ 🔒 **⑤ 成立**；二造對照皆如預期 ⇒ **量測器非紅**（`CLAUDE.md` ㉑ 之戒）。

### 🔒 `VR-078` 五項總判

| 項 | ① 列數 ≤ 2 | ② 字樣錨 | ③ 空白等價 | ④ 位移恰 1 且方向一致 | ⑤ 機制錨 |
|---|---|---|---|---|---|
| 判 | ✅（框A `1`／框B `2`） | ✅ 二列皆含 | ✅ 二列皆同 | ✅ 二列皆 `[1]`·方向一致 | ✅ |

⇒ 🔒 **五項<u>全部</u>成立** ⇒ 該二列判為 **⛔ 非行為差** ⇒ `§四` 得入倉。

---

## §四　`W-8`　判別力自檢（**已實跑**·⛔ 非推測）

**注入**：於**期末**第 `327` 列把 `setback)` 改為 `setback)_SENTINEL`（**診斷文字**之差·⛔ 非空白）。

| 量 | 注入前 | 注入後 |
|---|---|---|
| 變更列數（框B） | `2` | `2` |
| `②` 轉紅之列 | — | `[]`（特徵字樣仍在·合理） |
| **`③` 轉紅之列** | — | **`[1]`** |
| ⇒ 「`②` 或 `③` 轉紅」 | — | ✅ **True** |

⇒ 🔒 **`VR-078` 之例外⛔ 非放行一切**：一個**非空白**之字元差即使 `③`（摺疊空白後逐位相同）**轉紅**
⇒ 該差**必被判為行為差**。`W-8` ✅ **符**。

---

## §五　入倉清單（⛔ 零生產碼·純附加）

| # | 檔 | 內容 | 刪除欄 |
|---|---|---|---|
| `1` | `docs/驗證裁定登記表.md` | `§二`　**`VR-078`**（定長截斷所生之空白位移·**具名例外**五項判準） | `0` |
| `2` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `§三`　**`GB-113`**（`traceback.format_exc()[-700:]` 定長字元尾切致偽差） | `0` |
| `3` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `§四`　🔓 **`GB-105` 解除** | `0` |
| `4` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `§六`　**自誤 `199`** | `0` |
| `5` | 本報告 | `docs/reports/W-G.9-166R_VR078具名例外與GB105解除.md`（**新檔**） | — |

🔒 **`§八` 之不辦事項全數遵守**：⛔ 未增第五類正規化；⛔ 未修 `GB-112`／`GB-113`；
⛔ 未查 `G.1 ctx-builder 同源` FAIL 之成因；⛔ 未改 `app.py`／`verify/*.py` 既有一字；
⛔ 未解除 `GB-104`／`-110`／`-111`／`-112`／`-113`；⛔ 未落地 `VR-074`／`VR-075`／`VR-078`／任何 `K-9-*`；
⛔ 未重產 baseline／凍存名單／全量基準 log；⛔ 未動 `data/`；⛔ 未改任何既有 `docs/` 文字。

---

## §六　`§七` 十項預測逐項對拍（⛔ 未改預測）

| # | 預測 | 實測 | 判 |
|---|---|---|---|
| `W-1` | 自誤 `198`⇒`199`；`VR` `77`⇒`78`；`GB` `112`⇒`113` | 入倉後：自誤 MAX `199`／`VR` MAX `78`／`GB` 聯集 MAX `113`（缺號仍 `[12, 87]`·算術自檢 `113−2==111` True） | ✅ **符** |
| `W-2` | `自誤 198` ＝ `0`／`` 自誤 `198` `` ＝ `4` | `0` ／ `4` | ✅ **符** |
| `W-3` | `M-1 ①`：變更列數 ＝ `2`；`unified_diff ±` ＝ `4` | **框B**：`2` ／ `4`（框A：`1` ／ `2`·二框並報） | ✅ **符**（於單所用之框B） |
| `W-4` | `M-1 ②`：二列**皆**含 `params, build, winners, forced, setback)` | 二列皆 **True** | ✅ **符** |
| `W-5` | `M-1 ③`：二列摺疊空白後**皆逐位相同** | 二列皆 **True** | ✅ **符** |
| `W-6` | `M-1 ④`：空白 run `23`→`22` 與 `24`→`23`（皆差 `1`） | `[23,…]`→`[22,…]`／`[8,24,…]`→`[8,23,…]`·非零差皆 `[1]` | ✅ **符** |
| `W-7` | `M-1 ⑤`：`:1393` 含 `traceback.format_exc()[-700:]` | **True**（正／負對照皆如預期） | ✅ **符** |
| `W-8` | 判別力自檢：人造診斷文字差 ⇒ `②` 或 `③` 轉紅 | **`③` 轉紅**（列 `[1]`）⇒ True | ✅ **符** |
| `W-9` | `runall_0e4b508_full.log` ＝ `506` 列（`splitlines()`） | **`506`** | ✅ **符** |
| `W-10` | 受檔 `deletions` 逐列 ＝ `0`；`app.py`／`verify/*.py` 既有檔零改 | `numstat` 刪除欄逐列 `0`；期末 `app.py` blob ＝ `f285b91c9e5f97f13dd74508e1a403833bc75cb3` | ✅ **符** |

**計數對拍**：符 `10` ／ 不符 `0` ／ 不可判 `0` ⇒ 和 ＝ `10` ＝ 預測總數 ✅。

---

## §七　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | `HEAD` 全 `40` 碼；`app.py` blob 逐位；`verify/*.py` 既有檔零改 | ✅ `app.py` ＝ `f285b91c9e5f97f13dd74508e1a403833bc75cb3`（**絕對值**）；`numstat` 中 `verify/` 零命中 |
| `2` | `git diff --numstat 001f60a -- .` 刪除欄逐列 `0`；`wg942_append_audit` | ✅ 見 `§六 W-10` 與下 |
| `3` | `M-1` 五項逐項報數；判別力自檢**須實跑** | ✅ `§三`／`§四`（**已實跑**·⛔ 非推測） |
| `4` | `§四` 之入倉繫於五項全部成立 | ✅ 五項全成立 ⇒ **已入倉** |
| `5` | `VR-078`／`GB-113`／自誤 之形自證 ＋ MAX 推進，二數同格報 | ✅ 形自證：`VR-078` 由 ``^##\s*`VR-N` `` 命中、`GB-113` 由 ``^### `GB-N` `` 命中、`自誤 199` 由 `` ^#+ 自誤 `?N`? `` 命中；MAX `77→78`／`112→113`／`198→199`，且 `113` ⛔ 不在缺號集 |
| `6` | 錨定框 ＋ 二形並取、二數並報；對照組得 `0` 先判量測器紅 | ✅ `§二`（`自誤 198` ＝ `0`／`4` 即二形之實證；`GB-112` 之 `7`／`8` 為二框之實證） |
| `7` | `§一`／`§三` 所引之 `run_verification.py:1390-1393` 逐字自基座重抽並逐位 `diff` | ✅ 四列逐位相符（`§三 M-1 ⑤`）；基座 blob ＝ 工作區 bytes ＝ True |
| `8` | 每一命中數標框 ＋ 具名母體之態；列數採 `splitlines()` | ✅ 全報告；母體之態 ＝ `blob@001f60a` |
| `9` | 凡 `N/N` 型自證，同格具名外部錨 | ✅ `GB` 算術自檢之外部錨 ＝ `W-G.9-144:81` 逐字；`M-1` 之外部錨 ＝ `W-G.9-165R` 之決定性錨（同碼跨 clone ＝ `0`） |
| `10` | `§七` 十項逐項對拍 | ✅ `§六`（符 `10`／不符 `0`／不可判 `0`） |
| `11` | 報告命名 ＝ `W-G.9-166R` | ✅ |

---

## §八　🔒 併記（⛔ 非本批受詞）

1. 🔒 **`GB-105` 已解除**；仍開者 ＝ `GB-104`／`GB-110`／`GB-111`／`GB-112`／**`GB-113`**（本批新鑄）。
2. 🔒 **`VR-078` ⑤ 之自動失效機制已載於裁文**：一旦 `GB-113` 修畢（改行對齊截斷），`⑤` 不成立
   ⇒ 本例外自動失效，屆時全文對拍應為**真 `0`**。
3. ⚠️ **框之教訓（承 `VR-058 一`）**：本批出現**二數皆為 `2` 而所指不同**（框A 合計相異 vs 框B 變更列數）
   與**二數 `7`／`8` 同指一物**（命中列數 vs 出現次數）之**兩種**框現象
   ⇒ 🛑 凡出艙任何計數，**框須逐字同格**，⛔ 不得以「數字相同」推論「量相同」。
