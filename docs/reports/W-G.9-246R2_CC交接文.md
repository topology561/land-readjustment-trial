# `W-G.9-246R2`　CC 新 session 交接文（`2026-09-07`）

> 🔑 **開窗前四件事見 `§八`**。本文一切數字**現查於倉**；⛔ 採本文之數——**每一節皆須重跑**。
> 🛑 本文 **⛔ 鑄任何號**（自誤／`GB`／`VR`／戒／`K-9` 一律不鑄）。
> 🔒 **取號之由**：依 `常規四（三）`（CC 對某單之報告、log、產物一律「該單號 ＋ 後綴 `R`」·**⛔ 另取新號**）
> ⇒ 本文取 **`W-G.9-246R2`**（本窗最後一單 ＝ `W-G.9-246′`）。
> ⛔ **取 `W-G.9-248`**——其已於 `W-G.9-246′ §三` 被指定為**接閘單**（`常規四（七）②` 之意指占用）。

---

## §一　態錨（**開窗第一動作 ＝ 重跑本節**）

| 項 | 值（現查 `2026-09-07`）|
|---|---|
| 主線 `origin/wip/s1-endpart` | **`d7610b09c8aa606d52a546614dcce26e622433af`** |
| 🔴 **未併之側分支（候放行）** | **`verify/W-G.9-246-c1` ＝ `200dc53896d0e3ea62d0b827022d2bfdb111c9e2`**（`_lot_gate`·生產碼·本地保全 ref `keep/W-G.9-246-c1`）|
| 已併之側分支（⛔ 刪·復驗痕跡）| `verify/W-G.9-198R-c1`／`-c2`／`-c3`／`-219R2-c1`／`-229-c1`／`-243-c1`／`-244-c1`／`-244-c2`／**`-247-c1`** |
| 舊分支（⛔ 工作分支）| `main` ＝ `6cb77b7`／`claude/wg9-95z-v3-resume-hgpzfs`／`wip/W-G.4-S0b-S0c` |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/**.md` ／ `docs/orders/` | **1672** ／ **597** ／ **575** ／ **547** ／ **60** 檔 |
| `CLAUDE.md` | **193298** B／**2354** LF／`sha16 663e334b78b45548` |

**生產路徑十檔 ＋ 夾具之 `sha256[:16]`（`blob@d7610b0`）**

| 檔 | 前 16 位 | 檔 | 前 16 位 |
|---|---|---|---|
| `app.py` | `445585e238c26154` | `verify/wf_f0.py` | `6758ea766b001b95` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/run_verification.py` | `d0d531f7dc051b65` | `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/stepg_pipeline.py` | `508708b9c792f6a8` | `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/run_all.py` | `4ba89fef90979491` | `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| 🆕 `verify/fixture_wf_ns_wiring.py` | **`b17826a6f7b98bf5`** | | |

🛑 **十檔之定義 ＝ 閘 `P` 五檔 ＋ `wf_f*` 五檔**（`docs/reports/W-G.9-245_CC交接文.md:19` 逐字）。
🔴 **`fixture_wf_ns_wiring.py` ⛔ 屬十檔**——`W-G.9-247` 曾因誤計而生 `自誤 307`；**引用時須分清**。

🔴 **側分支 `-246-c1` 之十檔**：`app.py` **`5693242b19d970c1`**／`stepg_pipeline.py` **`c8d46d2e5fb79dc5`**；餘八檔 ＋ 夾具**逐位同主線**（**變 `2`／不變 `8`**）。

**近 10 commit**

```
d7610b0 W-G.9-246R2 執行報告：逐宗驗證站 _lot_gate（觀測模式）⛔ 零生產碼 ⛔ 零號鑄造
033f281 W-G.9-246′ 工項零：本單原封入倉 ⛔ 零生產碼 ⛔ 零號鑄造
61e503b W-G.9-247 併線後即辦：鑄 自誤 307（V-C 母體）＋ 報告加註三 ⛔ 零生產碼
910d5e7 W-G.9-247R 就地加註二：採 (甲) ＋ rebase ＋ V-0 全綠 ⛔ 零生產碼 ⛔ 零號鑄造
e76a3ad W-G.9-247 段甲追加：鑄 自誤 306（迴歸母體不足）⛔ 零生產碼
8a633ea W-G.9-247R 採 (甲)：fixture_wf_ns_wiring.py 限改二事 🔴 生產路徑檔·⛔ 推主線
7da3fb0 W-G.9-247R 工項二：街角規定範圍多邊形進 ctx（KL 令 2026-09-07·依候裁 (A)）🔴 生產碼
2391c1f W-G.9-247R 段乙：harness 量測基建（工項一 部分落檔 ＋ 工項三 eff 管線）🔴 生產碼
28d62f3 W-G.9-247R 就地加註：工項二已落地（依 (A)）＋ 🔴 新捕之紅 ⛔ 零生產碼
97eec90 W-G.9-247 段甲追加：鑄 自誤 303／304／305 ＋ GB-151（min_width 三源）⛔ 零生產碼
```

---

## §二　次號（**現查·⛔ 採本文之數·開工前重算**）

| 簿 | 現況（`blob@d7610b0`）| 次號 |
|---|---|---|
| 自誤（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）| **615897** B／**7172** LF；定義框列命中 **301**／重咬 **3**／純單筆 **298**／範圍框 **8**；相異 **300**／`MIN 7`／`MAX 307`／缺號 **`[106]`**；恆等式 `True` | **`308`** |
| `GB`（`docs/reports/W-G.4_泛用阻塞項登記表.md`）| **標題錨定框** `MAX` **151**（窄框 68／寬框 153）| **`GB-152`** |
| `VR`（`docs/驗證裁定登記表.md`）| 標題形相異 **77**（哨兵 `999` 已扣）·`MAX` **91** | **`VR-092`**（三位補零）|
| `K-9`（`docs/rulings/K-6_…md`）| `MAX` **`K-9-28`** | **`K-9-29`** |
| 戒（全倉 `.md`·`CLAUDE.md` 之正典定義列式）| 相異 **23**／`MAX` **40** | **戒 `41`** |
| 單號 | `248` 宣告框 `0/0/0`（鬆框 檔 `3`／列 `3`）**惟已由 `-246′ §三` 意指占用**；`249`／`250` 宣告框與鬆框**皆 `0`** | 🛑 **`248` 保留予接閘單**；CC 自起事者取 **`249`**（須先向發單側取得其最後單號·`常規四（四）`）|

🔒 **自誤之框** ＝ `VR-091` 補款二 `①` 定義框 `A` ＋ 範圍框展開；**母體 ＝ 登記表單檔**（`🔧 一`）。
🛑 **`GB` 之「相異／缺號集」⛔ 出艙**（`CLAUDE.md` 字樣錨 `在其釘定前，⛔ 得出艙`）。
🔒 **鑄號前須取該類<u>既有最末一則</u>之標題形逐字為範本**（`常規四（九）四`）。

---

## §三　工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——前置 ＝ `git ls-remote origin wip/s1-endpart` 已含該 commit（**⛔ `origin/main`**）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**；**`--selftest` 綠亦⛔ 蘊含閘已生效**（須**活體實測**）。
3. 🔒 **倉內報告 ＝ 唯一本體**；聊天 ＝ ping。push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
4. 🔒 **數字錨一律引倉檔**，⛔ 憑記憶出艙；與倉態衝突以倉為準。
5. 🔒 **工項零優先於一切**——收單後 **① 查宣告框占用（三數分列）→ ② 原封入倉 `docs/orders/`**（⛔ 顛倒）。
   驗「落檔 `sha256` ＝ 入倉 blob」。🛑 該閘只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。
6. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（經發單側）。
7. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 `deletions ＝ 0`。
8. 🔒 **常規補款一**：閘之量測與其所守護之動作（`commit`／`push`）**⛔ 置於同一命令塊**。
9. 🔒 **常規五**：每一量測須有**兩造對照組**（器須綠 ＋ 器須紅）。
10. 🔒 **零生產碼 commit 逕推**；**生產碼 commit 走側分支**（見下）。生產碼檔 ＝
    `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`
    （判法逐 commit：`git diff --name-only <前一已 push>..<本 commit> | grep -xE …`·**空輸出亦須出艙**）。

### 🔴 生產碼 commit 之流程（**⛔ 逕推主線**）

```
報告先行入倉主線（零生產碼·逕推）——依 W-G.9-194 裁四：置後之報告將落於不可 push 之區間
  → 生產碼本地 commit → ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c<n> ＋ 本地保全 ref keep/<單號>-c<n>
  → 本地分支回退至 origin/wip/s1-endpart（git update-ref ＋ git read-tree --reset -u）
  → 發單側逐位復驗 → 請示 KL → KL 逐字放行
  → 主線若於其間前進 ⇒ 依 W-G.9-206 §八 rebase ＋ --force-with-lease 重推側分支 ＋ 重驗
  → 合併：**純快轉**（git push origin <sha>:refs/heads/wip/s1-endpart）·⛔ merge commit
  → 併後報：主線 SHA／十檔雜湊／全部遠端 heads 逐一列示；**⛔ 刪側分支**
```

🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。
🔒 **`-247` 之既例**：rebase 後同一性閘（三檔 blob 逐位相同）＋ `is-ancestor` 綠 ＋ `--merges ＝ 0` ＋ 父數逐一皆 `1`，方快轉。

---

## §四　現行之閘與其框（**開工前逐一重跑**）

| 閘 | 母體／框 | 現況 |
|---|---|---|
| **閘 `P`** | **五檔**之內容 `sha256`（基準見 `§一`）| 零生產碼批須前後逐位相同 |
| **`S-3` 生產路徑錨** | `verify/wf_f*.py` 五檔 ＋ 🆕 夾具 | 閘 `P` ⛔ 涵蓋 ⇒ 逐批併報 |
| **號占用閘（宣告框·三數分列）** | 母體 ＝ **全 `docs/`**；`D1` 檔名 ∪ `D2` 列首 `#+` ∪ `D3` 自稱形；皆帶數字邊界 | 收單後、入倉**前**；**已占即停·⛔ 自行改號** |
| **`S-0` `SELF_SHA256`** | 自檔首至**最末**一個 `SELF_SHA256` 列之前一個 `\n`（含）| 判別力：對全檔施同一口徑須**不符** |
| **`S-0b`** | `python verify/probes/probe_order_preflight.py <單>`（**須於倉內**·`GB-148`）| `P-3`／`P-5` 為機械停機款 |
| **`V-0`** | `run_all` 全名目 `rc` 前後相同 | 🔴 **框須含「末端夾具」形**——見下 |
| **`PreToolUse` heredoc 硬閘** | `.claude/settings.json` ＋ `verify/tools/wg9237_heredoc_guard.py` | 🟢 **本窗實測生效並攔下一次** |

### 🆕 本窗新捕之陷阱（**逐條皆有實例·付過學費**）

| # | 受詞 |
|---|---|
| `1` | 🔴 **`V-0` 之比對框須含「末端夾具」形**。舊框 `^\s*(✅｜🔴)\s+(PASS｜FAIL)\s+` **漏抓** `  ✅ 末端夾具 xxx.py（…）rc=0` ⇒ 該夾具由 ✅ 翻 🔴 時仍報「全名目逐項相同」＝ **偽綠**。改**放寬框**（任何 `  ✅｜🔴｜🟡 ` 起首之列）；**判別力自證**：窄框 `34` ／放寬框 **`71`**（多抓 `37`）|
| `2` | 🔴 **凡於 `verify/**` 新增 `ns[<字面>]` 之消費，必補 `app.py` 之 `_WF_NS_NAMES`**。本窗 **CC 自犯**（漏 `k91_effective_min_build_area`）⇒ 夾具 `rc 0→1`、app 生產路徑必崩。**查法 ＝ AST 窮舉**：抽 `stepg` 全部 `ns[…]` 之名，與清單作差集（現況：相異 `27`／漏列 `0`）|
| `3` | 🔴 **`allocation_dir_block` ＝ `alloc_normal_axis(f3_cad_alloc_dir)` ＝ `rot90`**。`_blue_shadow_tri`／`_build_corner_range_v3` 所需者為 **CAD 原始 ALLOC 方向**（`app.py:9681` 逐字）。誤傳 `rot90` ⇒ 藍影面積 `6832`–`12534 ㎡`（**大於整個街廓**）；正解 `6.67`／`76.22 ㎡` |
| `4` | 🔴 **`app.py` 之 `_SP` ＝ `Point`、⛔ `Polygon`**（`app.py:1207` `from shapely.geometry import LineString as _SL, Point as _SP, Polygon as _SP_poly`）。需 Polygon 者一律區域 import 別名 |
| `5` | 🔴 **含非 ASCII（如 `′` U+2032）之檔名⛔ 經 shell 命令替換**——`cp950` 下會毀損（實測 `cp: cannot stat …`）。一律**於 Python 內**開檔／雜湊／`git add` |
| `6` | 🔴 **邊界造須取全精度**——以**已 `round(·,6)`** 之欄值充當「相等」時，`G > blue` 於浮點上為 `True` ⇒ **偽綠**（實測 `5.385368` vs 真值 `5.385367527840`）。改直呼受測函式取原值 |
| `7` | 🔴 **合成幾何靶須臨接受詞所需之線**——`K-9-23` 閘一含「臨 FRONTLINE `>0` ∧ 臨 BASELINE `>0`」；置於原點之合成矩形臨接長恆 `0` ⇒ 閘必假，係**靶之誤**。用**真街角範圍多邊形**（`K-8 §五`：其邊即該二線）|
| `8` | 🔴 **`run_all` 之名目輸出為固定 `14` 列尾窗**（`verify/run_all.py:315` 逐字 `splitlines()[-14:]`）⇒ traceback 增列會**擠掉頂端**，看似「例外鏈滅失」。判法：比二側該名目之輸出列數是否**相等** |
| `9` | 🔒 **`GB-143`（heredoc）之硬閘於本窗<u>實測生效</u>**——CC 誤送 `<<'PYEOF'` 遭當場攔下。⇒ `Bash` 內**⛔ 出現 `<<` 任何形**；腳本一律 `Write` 落檔後以路徑呼叫 |
| `10` | 🔒 承前波：`GB-139`（`autocrlf` ⇒ 受詞取倉側 blob）／`GB-145`（`rc` ⛔ 取自管線之後）／`GB-147`（哨兵自落地）／`GB-148`（機檢器倉根繫 `cwd`）／`GB-149`（append-only 簿內行號為錨）／`GB-132`（自指增長）|

---

## §五　已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 鏈 | 受詞 | 現況 |
|---|---|---|
| `GB-67` | 退縮 `0` 之 `②-宗 圍堵閘`（`app.py`）| **未解**：`R2` 宗-宗重疊 `45.9766 > 上界 2.8906` |
| telescoping | 退縮 `3.5` 之 `ΣRw`（`stepg_pipeline.py`·**harness 專屬**）| **未解**：`R2 left ΣRw_實跑 100.02 ≠ 44.41`（`Δ=55.606`）|

🔒 **二者係二事**（中止點相異）；**皆使 Step G 於 `R2` 中止** ⇒ `R4`／`R5`／`R6` **一筆不產生**。
🟢 **`W-G.9-247` 已使中止時之逐宗表可得**——`got_G值_退縮{tag}_partial.csv`
（`run_verification.py:727`–`:728` 於 `except` 內）⇒ **`R1` 全部鏈宗 ＋ `R2` 部分**之逐宗量現已可觀測。

---

## §六　待辦／待裁

### 🔴 最優先：`W-G.9-246-c1` 之放行鏈（**未完**）

- 側分支 **`origin/verify/W-G.9-246-c1` ＝ `200dc53896d0e3ea62d0b827022d2bfdb111c9e2`** 已推；
  `wip/s1-endpart` **一字未動**（現查 `d7610b0`）；本地保全 ref ＝ `keep/W-G.9-246-c1`。
- 其報告 ＝ `docs/reports/W-G.9-246R2_逐宗驗證站_lot_gate.md`（**已在主線**）。
- **`app.py` 改後 `sha256[:16]` ＝ `5693242b19d970c1`**；`stepg_pipeline.py` ＝ **`c8d46d2e5fb79dc5`**。
- **下一步 ＝ 發單側逐位復驗 → 請示 KL → 逐字放行 → 純快轉併線**。
- 🛑 **主線已於其後前進**（`033f281` → `d7610b0`）？**否**——側分支自 `033f281` 起而主線現為 `d7610b0`
  ⇒ **已不可純快轉**，須依 `W-G.9-206 §八` **rebase 至 `d7610b0` 後重推並重驗**（同一性閘）。

### 🔴🔴 候 KL 之域裁（**有土地後果·接閘前須定案**）

> **`K-9-23` 閘一之受詞 `G` 究為何宗？**
> `docs/rulings/K-6:3261` 逐字「`G(第 1 宗) > 藍影面積`」——該檔以**碼側索引**行文
> （同檔「`B1` ＝ **第 0 宗**遠側界與 BASELINE 之交點」）⇒ **碼側 index `1` ＝ 受檢宗自身**。
> `docs/配地計算總規格_v3.md` ⑪ 逐字「`G(街角第1宗)`」——依其自身之宗序對照 ⇒ **winner**。
> 🔒 **現行實作依權威序取 `docs/rulings/`**（第 1 級 > v3 第 3 級）＝ **受檢宗自身之 `G`**；
> 內部自洽亦支持（「嚴格大於」之由逐字「`G ＝ 藍影` 時**臨 BASELINE 長 ＝ 0**」繫於**受檢宗**之臨接長）。
> 🛑 **⛔ CC 自裁孰為誤。** 其影響 ＝ 哪些宗會被 `-248` 接閘剔除。

### 🛑 待裁清單（**⛔ 由 CC 自裁**）

| # | 受詞 |
|---|---|
| `1` | 上開 `K-9-23` 閘一之受詞 `G`（**有土地後果**）|
| `2` | `v3` ⑩（街角第 1 宗 `max(街角規定範圍面積, e)`）與 `-246′` `I-3`（`e ＝ 0` ⇒ `C` 不適用）於**街角第 1 宗互斥**；現行依 ⑩。連帶：`V-S′` 之「`X ＝ 0` 須全 `不適用`」僅對**非街角第 1 宗**成立 |
| `3` | 本窗 CC 之三誤是否鑄號（`alloc_dir` 錯 `90°`／自犯 `自誤 302`／`V-0` 比對框偽綠·責皆 ＝ CC）|
| `4` | **`V-K`（KL 目檢·app 側）之安排**——`_lot_gate` 之 `B`／`C` 於 app 側活著之**唯一**證據 |
| `5` | 側分支基座自 `033f281`（⛔ 單所載之 `61e503b`）之偏離 |
| `6` | 承前波未決：`min_width` 三源（`GB-151`）之處置時點／`GB-67` 與 telescoping 之排程 |

### ⬜ 次一單（KL 據 `V-S`／`V-S′`／`V-K` 裁出）

**`W-G.9-248`：接閘**——依 verdict 剔宗、整包回滾、左右雙鏈（`-246′ §三` 逐字）。
🛑 **`248` 之號已由該節意指占用**（`常規四（七）②`）⇒ CC 自起事者⛔ 取之。

---

## §七　本窗（`-245` 交接 → `-246R2`）之摘要

| 批 | 要旨 | 落點 |
|---|---|---|
| `-246` | 工項零入倉；**停機上呈**（`V-A` 之前提結構上不可得／`B`、`C` 之輸入於 harness 不可得／單漏令補列 `_WF_NS_NAMES`）| 主線 `34230bb`／`a313ebd` |
| `-247` | **harness 量測基建三件**：① 中止時**部分落檔**（`e.partial`·薄殼／`_impl` 分離）② 街角規定範圍多邊形進 ctx（`app.py` `_WF_NS_NAMES` `+2`）③ `eff_min_build_by_blk` 管線。另修 `fixture_wf_ns_wiring.py`（採 `(甲)`·限二事·判別力三造）| 已 **KL 放行併線**（`910d5e7`）|
| `-247` 段甲 | 鑄 `自誤 303`／`304`／`305`／`306`／`307` ＋ **`GB-151`**（`min_width` 三源） | 主線 |
| `-246′` | **逐宗驗證站 `_lot_gate`**（觀測模式·`app.py` module 級·四站掛載·`22` 個 `驗_*` 欄）| 側分支 `200dc53`·**候放行** |

### 🔑 碼面要害（**下一波必知**）

- **`_lot_gate`（`app.py` module 級·`_t3_gate_emit` 之後）** ＝ 判定式**單一真相源**；⛔ 讀 `st`、⛔ 副作用。
  四態**沿用** `_T3_NA`／`_t3_fmt_gate`（`_lg_fmt` 委派之）——**`無從判定` ＝ 單所稱之「不可判」**。
- **四站** ＝ app 左／右鏈 ＋ harness 左／右鏈，掛於 `res` 定案後、`_W_prev_* = res['W_far']` **之前**；
  右鏈掛於**定案之 `res`**（⛔ 各 solve 後各掛）。站只做二事：呼叫 ＋ 鏈索引；
  **寫欄於 `append` 處**（`res['_lg_cols']` 暫存·純加性·同 `res['_alloc_dir_used']` 之既例）。
- **`_WF_NS_NAMES` 現為 33 筆**（本波 `+4`：`_make_chamfer_tri_wb`／`_baseline_pts_from_manual`／
  `_lot_gate`／`k91_effective_min_build_area`）。
- **`ctx['corner_range_polys'][(blk, side)]`**（`-247`）＋ **`_eff_mba`**（`-247`）＝ harness 之 `B`／`C` 輸入。
- **`partial` 之鍵**：`g_rows`／`aborted_blk`／`aborted_gate`／`eff_keys`／`corner_range_keys`／
  `corner_range_areas`／`corner_range_errors`。
- **`_run_step_g_impl`**：`run_step_g` 現為**薄殼**（只包不吞·bare `raise`）；
  🛑 凡以 AST 解析「`run_step_g` 內之 `ns[…]`」者，受詞須含 **`_run_step_g_impl`**。

### 🔒 `V-S` 觀測統計（**次一單之地基**·態 ＝ `200dc53`）

二情境（`0m`／`3.5m`）**各不合格 `9` 宗**、**`不可判 0`**。
其中 `8` 宗為 **`A` 幾何**不合格（`W×D ＝ 3.5×14.0`；其 `G ÷ 街廓平均深度 < 3.5 m`·自洽），
`1`–`2` 宗涉 **`B` 藍影**。`C` 於主跑（`eff ＝ {}`）**恆 `不適用`**——**⛔ 讀為全過**；
惟**街角第 1 宗**依 ⑩ 仍可判（門檻 ＝ 街角規定範圍面積）。
🔒 **抵費地列⛔ 經四站** ⇒ 其 `驗_*` 為空係**構造使然**、⛔ 缺漏。

---

## §八　開窗前四件事（**依序·⛔ 跳**）

1. **驗 `left-right`**：`git ls-remote origin wip/s1-endpart` ＋ `git rev-list --left-right --count origin/wip/s1-endpart...HEAD`。
   **非 `0 0` 即工作區為舊態** ⇒ ⛔ 直接 `cat` 工作區之 `CLAUDE.md`／`app.py`／`docs/**`；一律 `git show <rev>:<path>`。
   **harness 逐窗自 `main` 開 worktree ＝ 已多度重踩。**
2. **驗硬閘是否生效**：`test -f .claude/settings.json` ＋ `test -f verify/tools/wg9237_heredoc_guard.py`。
   **二者缺一即閘不存在** ⇒ 該 session 內 `Bash` **⛔ 出現 `<<` 任何形**。
   🟢 **本窗二檔皆在且實測攔下一次**——惟「閘已落地 ⇏ 閘於本 session 生效」，仍須現查。
3. **讀本文 `§一`／`§二` 並<u>重跑</u>其每一數**（⛔ 採本文之值）。
4. **查 `W-G.9-246-c1` 之放行進度**：`git ls-remote --heads origin 'refs/heads/verify/*'`。
   若已放行 ⇒ **先 rebase 至當時之主線**（其已非 `033f281`）、重推、重驗同一性閘，**再**純快轉併線。

🔒 **收單後首二步之順序**：① 查宣告框占用（三數分列）→ ② 原封入倉。**⛔ 顛倒。**
🔒 **長跑一律於拋棄式 clone**（自 origin·`core.autocrlf=false`·短路徑）——`run_all` 會改寫已追蹤 log。
