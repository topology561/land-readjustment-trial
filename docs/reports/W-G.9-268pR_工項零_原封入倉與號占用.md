# W-G.9-268pR　工項零：本單原封入倉與號占用（**審查窗**·⛔ 零生產碼·⛔ 零號鑄造）

> **本報告之射程 ＝ 單 `§十` 工項零，止此。**
> ⛔ 執行 `§二` 任何工項（`c0`〜`c4`）、⛔ 鑄任何號、⛔ 載任何關於**病一**之修法主張。
> 級 ＝ **輕級**（純登記·`作業常規之追加五 二`）⇒ ⛔ 跑 `run_verification.py`／`run_all.py`。

---

## §零　態錨與環境框（逐條當場現查）

| 項 | 實測 |
|---|---|
| 期初態（單 `§零` 所載） | `95882de570fe840cf5ae8069ef6237eb5e83991c` |
| `git ls-remote origin refs/heads/wip/s1-endpart` | `95882de570fe840cf5ae8069ef6237eb5e83991c` ⇒ **全 `40` 碼逐位相符** ✅ |
| `git ls-remote origin refs/heads/main` | `6cb77b7f0c08e2edf62f8e9cd239efa774a98d95` |
| `left-right`（`FETCH_HEAD...HEAD`） | `0　0` ✅（`harness-worktree-crlf-trap` 之開窗第一動作） |
| 本窗 worktree 分支 | `claude/gb67-anchoring-correction-8736c9`（其 tip ＝ 期初態） |
| `core.autocrlf` | 全域 `true`（`C:/Program Files/Git/etc/gitconfig`）／**本倉 `.git/config` ＝ `false`** ⇒ **有效值 `false`** |
| `git check-attr text eol -- docs/orders/x.md` | `text: unspecified`／`eol: unspecified` |

🔒 **⇒ 量測框之結論**：`*.md` 於本倉**⛔ 受行尾正規化** ⇒ **工作區 bytes ＝ 倉內 blob bytes**。
本項係 `CLAUDE.md`「位元組數／校驗碼須聲明量測框」之受詞——⛔ 得憑該條所述之 `autocrlf=true` 逕推，
**須現查**；現查所得與該條之描述**相異**（該條立於全域值，本倉 config 已覆蓋之）。

---

## §一　入倉保真（`作業常規之追加四` 款 `一`）

**三軸**：`(1)` 來源 ＝ KL 交付檔（`Downloads` 側·⛔ 倉內）／`(2)` 母體 ＝ 該單**單檔**／`(3)` 粒度框 ＝ **bytes ＋ `sha256`**。

| 階 | `sha256` | bytes |
|---|---|---|
| 來源（落檔前） | `037538f136b51a4ec7efdc4ae80471906f62575d9a3c8d860c20253500447ab9` | `20407` |
| 落檔（工作區） | 同上（**逐位相符**） | `20407` |
| index blob（`git cat-file blob :<檔>`） | 同上（**逐位相符**） | `20407` |
| `HEAD` blob（`git cat-file blob HEAD:<檔>`） | 同上（**逐位相符**） | `20407` |

- 行尾：`CR` ＝ **`0`**／`LF` ＝ **`225`** ⇒ 純 LF ⇒ 與 `§零` 之量測框一致。
- 落點：`docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md`（單 `§十` 所指定之逐字檔名）。
- 落檔前該路徑**⛔ 存在**（`ls` 回 `No such file or directory`）⇒ ⛔ 覆蓋任何既有檔。

🛑 **射程界限（照實·⛔ 頂替·承 `追加四` 之既有明文）**
本閘所證者為「**落檔 → 入倉**」之保真，**⛔ 證「發單側送出 → 落檔」之保真**。
本單**⛔ 載 `SELF_SHA256`**（見 `§二`）⇒ 全鏈保真之另一半在本窗**結構上不可驗**，
**具名為「不可驗」**（`序 11 之一般化` 款 `二`：受詞缺漏時須 loud 拒測，⛔ 靜默綠）。
🔒 併記：`W-G.9-268` 前批（`95882de`）之 `S-0` 曾以「KL 交付檔 vs 倉內 blob 逐位相同」閉合該缺口，
其可行之由係**該單自載 `SELF_SHA256`**；本單無之 ⇒ **⛔ 得沿用該結論**。

---

## §二　`S-0b`　收單時之機檢（`verify/probes/probe_order_preflight.py`）

**判別力造（`--selftest`·四造·⛔ 可省）**：`rc = 0`，逐字「判別力造：✅ 全數成立 ⇒ 本器非恆綠亦非恆紅」
——靶① `P-3` 紅／靶② `P-4` 提示／靶③ `P-6` 提示／反靶 `P-3` 不紅，**四者皆如預期**。

**本單**：`rc = 0`／🔴 **機械停機款 `0` 項**／🟡 提示 **`3` 項**（⛔ 計入 `rc`）。
🔒 **🟡 之逐項處置（`CLAUDE.md` 明令「逐項具名，採納或具名豁免」·⛔ 靜默略過）**

| # | 項 | 逐字 | 處置 |
|---|---|---|---|
| `1` | `P-5` | 「⛔ 無 `SELF_SHA256` 列 ⇒ ⛔ 不可對拍（🟡 非紅：非所有文件皆須有）」 | 🔴 **採納·具名為「不可驗」**，其後果已載於 `§一` 之射程界限。⛔ 以 `§一` 之三值相符代之 |
| `2` | `P-4` `:87` | 「於 `c0-1` 所定位之**全部**碼位（二檔）加入新錨，置於具名旗標之後（預設 `off`）。」 | **具名豁免·偽陽**——該列之「後」係**碼位之序**，⛔ 方向性轉引，⛔ 有座標系可具名 |
| `3` | `P-4` `:144` | 「` ``` `」（code fence 起首列） | **具名豁免·偽陽**——命中者為 fence 標記本身，⛔ 語意 |

🔒 `P-1`／`P-2`／`P-6` 命中 **`0`**。`P-3` 之記錄項：`app.py` 之 `def main` 區間（AST 實查）＝ `14887`-`24459`。

---

## §三　號占用（🛑 **入倉前查**·宣告框六數分列·補款 `①`〜`⑧`）

**三軸**：`(1)` 來源 ＝ `blob@95882de570fe840cf5ae8069ef6237eb5e83991c`／
`(2)` 母體 ＝ **全 `docs/`** 之倉側 blob **`631`** 檔·Σ **`19834847`** B（補款 `③`）／
`(3)` 粒度框 ＝ **檔框 ＋ 列框**（`D1` 檔數／`D2` 列數／`D3` 列數／雙屬列數／`列框 = |D2 ∪ D3|`／`檔框 = |D1 ∪ D2檔 ∪ D3檔|`·補款 `⑥`／`⑦`）。
`D3` 取**嚴格式**（列首**即**該號·⛔ 前導 `` ` ``／`*`／`_`／`>`／`-`／空白·補款 `⑧`）＋ 自稱形（補款 `⑤`）。

| 受詞 | `D1` | `D2` | `D3` | 雙屬 | **列框** | **檔框** | 〔漏框偵察〕鬆框 檔／列 | 判 |
|---|---|---|---|---|---|---|---|---|
| **受詢 ＝ 本單之號（U+2032 形）** | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0`／`0` | ✅ **六數全 `0` ⇒ 未占用** |
| 對照組甲 ＝ `W-G.9-268` | `3` | `6` | `4` | `0` | `10` | `3` | `7`／`37` | ✅ 須 `≥ 1` |
| 對照組乙 ＝ 一必不存在之人造號（**執行期組出**·字樣⛔ 入倉·`GB-147`） | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` | ✅ 須 `= 0` |

🔒 **對照組甲與單 `§一 S-1` 所載發單側實測【`D1 3`／`D2 6`／`D3 4`／列框 `10`／檔框 `3`】逐位相符**
⇒ 本窗之框實作與發單側同源（`W-G.9-243` 補款 `⑥` 之案由所欲防者，於本次**未發生**）。

### 🔒 二號之可辨（單 `§八` 停機款之受詞）

**結構之證（⛔ 僅憑實測）**：受詢之錨定框**要求 U+2032**，而 `W-G.9-268` 之字樣**⛔ 含之**
⇒ **`W-G.9-268` 之任何出現，結構上⛔ 可能進入受詢之六數** ⇒ 受詢之計數**⛔ 可被污染**。
**實測之證**：於期初態，`W-G.9-268` 之框**加排除 U+2032** 後之鬆框 ＝ **`7` 檔／`37` 列**，
與**未排除**者（`7`／`37`）**逐位相同** ⇒ 期初倉內**⛔ 有任何 U+2032 形之出現**。
⇒ 🟢 **二號可辨**，`§八` 之「`S-1` 之二號不可辨」**未觸發**。

🩸 **反向之非對稱（照實）**：`W-G.9-268` 之框**會**咬中 `W-G.9-268′`（`′` 非數字 ⇒ 過 `(?!\d)` 邊界）
⇒ **甲之數會被受詢之出現灌大**（見 `§四` 之入倉後值）。此非對稱**⛔ 影響本閘**
——本閘之受詞係**受詢之六數**，而該方向已由上開結構之證封死。

---

## §四　入倉後之復查（`GB-132` 自指增長之具名）

**三軸**：`(1)` `blob@7d7112f2682e140811c749c94391de2b0fdb8f8c`／`(2)` 全 `docs/` **`632`** 檔·Σ **`19855254`** B／`(3)` 檔框 ＋ 列框。

| 受詞 | `D1` | `D2` | `D3` | 雙屬 | 列框 | 檔框 | 鬆框 檔／列 |
|---|---|---|---|---|---|---|---|
| 受詢（U+2032 形） | `0` | **`1`** | `0` | `0` | **`1`** | **`1`** | `1`／`3` |
| 對照組甲 ＝ `W-G.9-268` | `4` | `7` | `4` | `0` | `11` | `4` | `8`／`47` |
| 對照組乙 | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` |

🔒 **受詢由 `0` 轉 `1` 之唯一來源 ＝ 本單自身之標題列**（入倉檔之 `:1`，其形為 `# ` 起首 ⇒ 合 `D2`）。
含 U+2032 形之列逐列 ＝ 該檔之 `:1`（標題）／`:52`（`S-1` 之表列）／`:211`（`§十` 之工項二）。
🔒 **自洽驗算**：甲之鬆框由 `37` 增為 `47`（`+10`，皆落於該檔）；其中「加排除 U+2032」後為 `45`，
差 **`2`** ＝ 上開 `3` 列中**⛔ 同列另含無 `′` 形**者之數（`3 − 1 = 2`；`:52` 同列兼含二形）✅。

🩸 **`D1` 之照實登記**：入倉檔之**檔名**為 `…W-G.9-268p…`（ASCII `p`），**⛔ 含 U+2032**
⇒ 受詢之 `D1` 於入倉後**仍為 `0`**；其占用**由 `D2`（標題列）承擔**。
同時，`W-G.9-268` 之 `D1` 由 `3` 增為 `4`（`p` 非數字 ⇒ 過邊界）。
🛑 **此係事實登記，⛔ 條文、⛔ 由 CC 自裁**；是否令檔名與號之形一致，候發單側／KL。

### 🔑 交接註（**對施工窗之 `S-1` 有直接後果·⛔ 得漏讀**）

單 `§一 S-1` 逐字令「受詢 ＝ `W-G.9-268′`（**須六數全 `0`**）」。
🔴 **工項零入倉之後，該受詞之實測已為 `D1 0／D2 1／D3 0／雙屬 0／列框 1／檔框 1`，⛔ 全 `0`。**
🔒 其**唯一**占用者為**本單自身**（工項零所入倉之該檔之標題列）
⇒ 合 `作業常規之追加五` 一-1 **白名單 `1`**（「號占用非零，而占用者全為本單自身之產物」）
⇒ **施工窗得自解續辦、⛔ 改號、⛔ 停機**，惟須逐筆歸類並寫入其自解清單。
🔒 **同形之前例**：`95882de`（`W-G.9-268R`）之 `S-1` 即以同一款自解（其時 `7` 筆命中全落於工項零所入倉之二檔）。

---

## §五　四簿 MAX（期初 ＝ 期末·各以其**正典母體單檔**）

| 簿 | 母體（單檔） | 檔長 | 框（逐字） | 命中列 | **MAX** 期初 → 期末 |
|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `671849` B／`7883` 列 | `` ^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`? `` ＋範圍框 | `330` | **`332` → `332`** ✅ |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `743313` B／`5880` 列 | `` ^#{2,4}[^\n]*?GB-([0-9]{1,4}) `` | `169` | **`156` → `156`** ✅ |
| `VR` | `docs/驗證裁定登記表.md` | `394392` B／`5124` 列 | `` ^#+ `?VR-([0-9]{1,4})`? ``（扣哨兵 `999`） | `81` | **`95` → `95`** ✅ |
| `K-9` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `295061` B／`4057` 列 | `` ^#{2,4}[^\n]*?K-9-([0-9]{1,3}) `` | `106` | **`29` → `29`** ✅ |

🔒 **相異／缺號集**（依 `VR-091 一` 同格載框；母體與框如上表）
- 自誤：相異 **`325`**／`MIN` `7`／缺號集 **`[106]`** ⇒ 與單 `§七` 所載期初【`325`／`332`／缺 `[106]`】**逐位相符** ✅
- `VR`：相異 **`80`**／缺號集 **`[73, 75]`** ⇒ 與單 `§七` 所載【`80`／`95`】**逐位相符** ✅
- `K-9`：相異 **`28`**／缺號集 **`[]`** ⇒ 與單 `§七` 所載【`28`／`29`】**逐位相符** ✅
- `GB`：🛑 **其「相異／缺號集」⛔ 出艙**——`CLAUDE.md`（`W-G.9-205` 更正註）逐字令「`GB` 之定義列式仍⛔ 經正典釘定，在其釘定前，⛔ 得出艙 `GB` 之『相異／缺號集』」。
  🔒 ⇒ 本窗只對拍其 **MAX ＝ `156`**，與單 `§七` 所載第二數**逐位相符** ✅；
  單所載之第一數（`154`）與本窗之框**⛔ 同源**，依上開明令**⛔ 於此出艙本窗之對應值**。

🔒 **量測器非紅之二造自證**：四框之命中列各為 `330`／`169`／`81`／`106`（皆 `> 0`）；
一必不存在之標題形（**框於執行期組出**·字樣⛔ 入倉）命中 **`0`** ⇒ 器能歸零、⛔ 恆綠亦⛔ 恆紅。

🔒 **結構之併證**：四簿之母體**皆為單檔**且**皆⛔ 在 `docs/orders/` 之下**
⇒ 本批之入倉**結構上⛔ 可能改動任一簿之 MAX**；上表之期初＝期末係其**實測復現**，⛔ 其替代。

---

## §六　`commit` 級之判（`7d7112f`）

| 項 | 實測 |
|---|---|
| `git show --name-status` | `A␉docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md`（**唯一一列**） |
| `git show --numstat` | `225␉0␉<同上>` ⇒ **`deletions` 欄 ＝ `0`** ✅（`常規一 ②` 補款：判準在有無刪除／改寫） |
| 生產碼判（`§五` 逐字指令·`95882de..7d7112f`） | **空輸出**（`grep rc = 1` ⇒ 零命中）⇒ **零生產碼 `commit`** |
| 　└ 該 `grep` 之對照組 | 同一 `grep` 餵 `app.py` ⇒ 命中 `app.py` ⇒ **器非紅、⛔ 恆空** |
| `git status --porcelain` | **空**（`0` 列）✅ |

🔒 ⇒ 依 `常規一 🔧 一`（零生產碼 `commit` 逕行 `push`）＋ 單 `§十`（零生產碼 ⇒ 得逕推），
本批**⛔ 須請示**即得 `push`。

---

## §七　`push` 與其復驗

🛑 **本節於 `push` 後以<u>末端追加</u>承載**（`常規補款 一`：閘之量測與其所守護之動作須為**分開之呼叫**
⇒ 復驗之數⛔ 得與 `push` 同塊產生）。
🔒 其受詞須逐字寫為「**為 `origin/wip/s1-endpart` 之祖先**」，⛔ 寫為「在 `origin`」（單 `§五` 之明文）；
對照組 ＝ `6cb77b7f0c08e2edf62f8e9cd239efa774a98d95`（`origin/main` 之 tip·**在 `origin` 而⛔ 為該分支之祖先**）。

---

## §八　本批之自解清單（`作業常規之追加五` 一·款 `五` 出艙具名）

### 自解 `1`　本報告之檔名與落點

- **①疑義逐字**：單 `§九` 指定之出艙形為 `docs/reports/W-G.9-268pR_錨點更正.md`，
  而其內容係**施工窗**之批次報告（開工閘八閘／`c0` 四項／`§三` 十閘／差異表／併線前必答五項）；
  單 `§十` 之工項零**⛔ 指定**其自身之報告檔名。
- **②二個以上之讀法**：
  `(a)` 逕寫入 `W-G.9-268pR_錨點更正.md`（與 `§九` 同名·由施工窗續補）；
  `(b)` 另立 `W-G.9-268pR_工項零_原封入倉與號占用.md`（保留 `常規四（三）` 所令之「單號 ＋ `R`」前綴，另加受詞後綴）。
- **③所取者**：`(b)`。
- **④機械證據**：`(b)` 為**純新增檔**（`deletions = 0`）、⛔ 動任何既有檔一字 ⇒ 可逆；
  其檔名合 `常規四（三）` 之「該單號 ＋ 後綴 `R`」（前綴 `W-G.9-268pR` 逐字命中）。
- **⑤所棄者之由**：取 `(a)` 將使**施工窗**之整份批次報告與本窗之工項零同居一檔
  ⇒ 施工窗若以 `Write` 整檔落檔即**覆寫**本窗之內容，與 `append-only` 之倉規相衝；
  另立一檔則二者**結構上⛔ 可能互毀**。
- 🔒 **五項自檢**：零土地後果 ✅／零生產碼 ✅（`§六` 之空輸出）／可機驗 ✅（檔名前綴之命中·`deletions = 0`）／
  可逆 ✅（新建檔）／出艙具名 ✅（本節）。
- 🛑 **類之具名**：本項係**白名單外之新類**（白名單 `4` 之受詞為「落點之選擇**而目標檔之射程宣告逐字可機械判定**」，
  本項之目標檔**尚⛔ 存在** ⇒ 無射程宣告可判）⇒ 依 `追加五` 一 之界之判準具名之。
- 🔒 **併記**：依 `常規三`，檔名屬**⛔ 無土地後果**之事項，其正辦為「有倉之一方**提案**、發單側**核可**」
  ⇒ 上開 `(a)`／`(b)` 即本窗之提案，**候發單側核可**；本窗**⛔ 上呈 KL**。

### 自解 `2`　`Bash` heredoc 之阻斷

- **①疑義逐字**：本窗首次落腳本時以 `Bash` heredoc 為之，遭 repo hook 阻斷
  （`verify/tools/wg9237_heredoc_guard.py`·`GB-143` 補款二「⛔ 於 Bash 之 command 內出現該引入符」）。
- **②讀法**：`(a)` 改寫命令以規避 PATTERNS；`(b)` 依該 hook 之明令**一律以 `Write` 工具落檔，再以路徑呼叫**。
- **③所取者**：`(b)`。**④證據**：其後之三支腳本皆以 `Write` 落於 scratchpad、以絕對路徑呼叫，
  hook **⛔ 再觸發**。**⑤所棄者之由**：`(a)` 逐字為該 hook 所禁（「⛔ 得繞、⛔ 得改 PATTERNS」）。
- 🔒 五項自檢：零土地後果 ✅／零生產碼 ✅／可機驗 ✅／可逆 ✅／出艙具名 ✅。

---

## §九　事實登記（🛑 **⛔ 條文·⛔ 由 CC 自裁**·候發單側／KL）

1. **號之形與檔名之形不一致**：本單之號為 U+2032 形，而其入倉檔名採 ASCII `p` 形
   ⇒ 受詢之 `D1` 於入倉後恆為 `0`，其占用僅由 `D2` 承擔；同時 `W-G.9-268`（**已作廢之前批**）之 `D1` 因而 `+1`。
   是否令二形一致、或於宣告框增設「`p` 變體視同該號」一款 ＝ **意思決定**。
2. **`CLAUDE.md` 之 `autocrlf` 描述與本倉現況相異**：該檔逐字載「本倉 `core.autocrlf=true`」，
   而本倉 `.git/config` 現為 `false`（覆蓋全域之 `true`）。其對「工作區 bytes vs blob bytes」之結論**方向相反**。
   🛑 **本窗⛔ 追改該處一字**（`append-only`）；是否以末端追加更正之 ＝ **意思決定**。
3. **本單⛔ 載 `SELF_SHA256`** ⇒ `追加四` 款 `一` 之全鏈保真在本窗結構上不可閉合（`§一` 之射程界限）。
   是否令發單側於單內同格載其送出時之 `sha256`（該款自身已預留之延伸）＝ **意思決定**。

---

## §十　本報告⛔ 載者（自限）

- ⛔ 載 `§二` 任何工項（`c0`〜`c4`）之執行、結論或預測。
- ⛔ 載任何關於**病一**（`K-9-23` 閘一轉主動／`K-9-17` 遞補）之修法主張——其為 `W-G.9-269`。
- ⛔ 鑄任何號：單 `§七` 所授權之二號**由施工窗鑄**；於工項零鑄之將使自誤 `MAX` 由 `332` 推進，
  與單 `§十` 之「四簿 MAX（**須未動**）」**直接互斥** ⇒ 本窗據此判其⛔ 屬工項零之射程。
- ⛔ 跑 `run_verification.py`／`run_all.py`（輕級之明文）。

---

## 🔧 `§七` 之末端追加：`push` 與其復驗（⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為 `§七` 所預告之承載（`常規補款 一`：閘之量測與其所守護之動作須為**分開之呼叫**）。

### 一　推前之閘（**於 `push` <u>之前</u>、以獨立之呼叫量得**）

| 閘 | 受詞 | 實測 | 判 |
|---|---|---|---|
| `A` | `git ls-remote origin refs/heads/wip/s1-endpart` ＝ 本批基座 | `95882de570fe840cf5ae8069ef6237eb5e83991c` ＝ 基座 | ✅ 快轉成立 |
| `B` | 基座**為 `HEAD` 之祖先** | `rc = 0` | ✅ |
| `B′` | **對照組**：`6cb77b7f0c08e2edf62f8e9cd239efa774a98d95` 為 `HEAD` 之祖先 | `rc = 1` | ✅ 該檢⛔ 恆真 |
| `C` | 四簿 `MAX` 期末 | 自誤 `332`／`GB` `156`／`VR` `95`／`K-9` `29` | ✅ 未動 |
| `D` | 受詢之六數期末（`GB-132` 避讓） | `D1 0`／`D2 1`／`D3 0`／雙屬 `0`／列框 `1`／檔框 `1` | ✅ 報告⛔ 新增 `D2`／`D3` |

🔒 `D` 之併記：期末鬆框為 **`2` 檔／`5` 列**（本報告之散文二列所增）——依補款 `②`，
**引用／散文⛔ 構成占用** ⇒ 宣告框之六數**⛔ 因之而動**，其差正是二框並列之用（補款 `④`）。

### 二　推之逐字輸出

```
To https://github.com/topology561/land-readjustment-trial.git
   95882de..6cdc4af  HEAD -> wip/s1-endpart
```

### 三　推後之復驗（**受詞逐字 ＝ 「為 `origin/wip/s1-endpart` 之祖先」**·⛔ 寫為「在 `origin`」）

| 受詞 | 實測 |
|---|---|
| `git ls-remote origin refs/heads/wip/s1-endpart`（**實查**·⛔ 採信本地 ref） | `6cdc4af407b6b3c02e39ed0c3cb7bdab7b94c32d` |
| `7d7112f2682e140811c749c94391de2b0fdb8f8c` 為該分支之祖先 | `rc = 0` ✅ |
| `6cdc4af407b6b3c02e39ed0c3cb7bdab7b94c32d` 為該分支之祖先 | `rc = 0` ✅ |
| **對照組** `6cb77b7f0c08e2edf62f8e9cd239efa774a98d95`｜**在 `origin`** | **是**（`refs/heads/main` 之 tip·`ls-remote` 實查） |
| **對照組**｜**為該分支之祖先** | `rc = 1` ⇒ 🔒 **證該述詞⛔ 為「在 `origin`」** |

🔒 **倉側端之保真復驗**：自 `FETCH_HEAD`（＝ 推後之 origin 側）取入倉檔之 blob，
其 `sha256` ＝ `037538f136b51a4ec7efdc4ae80471906f62575d9a3c8d860c20253500447ab9`
⇒ 與 `§一` 之**來源**逐位相符 ⇒ 「落檔 → 入倉 → **origin**」全段保真成立
（🛑 其**射程界限**仍如 `§一` 所載，⛔ 及於「發單側送出 → 落檔」）。

### 四　逐 `commit` 之異動清單與生產碼判（`§五` 逐字指令·**含空輸出**）

| `commit` | `git show --numstat` | 生產碼判（`grep -xE` 四檔） |
|---|---|---|
| `7d7112f` | `225␉0␉docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md` | **空輸出**（`rc = 1`） |
| `6cdc4af` | `232␉0␉docs/reports/W-G.9-268pR_工項零_原封入倉與號占用.md` | **空輸出**（`rc = 1`） |

🔒 該 `grep` 之對照組：同式餵 `app.py` ⇒ 命中 `app.py` ⇒ **器非紅、⛔ 恆空**。
🔒 全批 `deletions` 逐列為 `0`；`git status --porcelain` 為**空**。

### 五　🛑 收工即停

單 `§十` 逐字「辦畢即推即停。`§二` 之工項由**新 CC 施工窗**辦。」
⇒ **本窗止於此**，⛔ 開工 `c0`〜`c4`、⛔ 鑄任何號。
