# `W-G.9-268pR`　`c0-5`：新靶重得（**全閘綠**）＋ 七號鑄造 ＋ `GB-67` 更正註

> **受詞** ＝ `W-G.9-268′` **補令二**（`docs/orders/W-G.9-268p_補令二_受詞精確化與新靶重得.md`）。
> 🔒 本檔與 `W-G.9-268pR_錨點更正.md`（`c0` 之停機上呈）／`W-G.9-268pR_工項零_原封入倉與號占用.md`
> （審查窗）**並存·互不覆寫**。
> 🛑 **本批⛔ 動生產碼一字**（逐 `commit` 之判見 `§八`·**含空輸出**）。
> 🛑 **本報告⛔ 載**任何關於**病一**（`K-9-23` 閘一轉主動／`K-9-17` 遞補）之修法主張——其為 `W-G.9-269`。

---

## §零　三軸之總宣告（`VR-091`）

| 軸 | 值 |
|---|---|
| `(1)` 來源 `commit` | **期初** ＝ `4ff8b35523ddd43448330f63e264ca565d95bf50`（`ls-remote` 實查·與補令二 `§首` 逐位相符）／**工項零後** ＝ `c02d593499671fd9abb4ef8f800e9db5098ba8b3` |
| `(2)` 母體 | 逐節分列（各節自載） |
| `(3)` 粒度框 | 逐節分列（**檔框**／**列框**／**blob bytes**／**面積（㎡）**／**長度（公尺）**） |

---

## §一　工項零：補令二原封入倉

| 量 | 值 |
|---|---|
| 落點 | `docs/orders/W-G.9-268p_補令二_受詞精確化與新靶重得.md` |
| 落檔前來源 `sha256` | `e9c9feee8bf4e9e792d867ca7a4faab366dc9289dca6716e05caaa49da068a90` |
| 入倉 blob `sha256` | **同上**（逐位相符）｜`bytes 11555`／`CR 0`／`LF 151`｜`cmp` 空 |
| `numstat` | `151	0` ⇒ **`deletions ＝ 0`**（純新增檔）|
| `commit` | `c02d593` |

🔒 **開工閘（補令二⛔ 另令，沿用原單 `§一` 之受詞）**
> `S-2`：`ls-remote` ＝ **`4ff8b35523ddd43448330f63e264ca565d95bf50`** ⇒ 與補令二 `§首` **逐位相符**；
> `left-right` ＝ `0	0`；`porcelain` ＝ `0`。
> `S-1`（補令一 `§一` 之性質式）：受詢二形聯集 **`D1 4／D2 4／D3 7／雙屬 1／列框 10／檔框 4`**；
> **逐筆歸類之第三方 ＝ `0` 筆**（全落於本單族之四檔：原單／補令一／工項零報告／`錨點更正` 報告）；
> 對照甲 `W-G.9-268` 檔框 `7`／列框 `22`（須 `≥ 1`）✅；對照乙（人造·執行期組出）全 `0` ✅。
> 落檔 ＝ `verify/out/WG9268pR_occ_pre2.log`。

---

## §二　`c0-5`：新器 ＋ 六閘（**全閘綠·`rc = 0`**）

**器** ＝ `verify/probes/probe_WG9268p_anchor.py`（**新建·唯讀·⛔ 動生產碼一字**）
**母體** ＝ `verify/out/got_G值_退縮{0m,3.5m}_partial.csv`（`15979` B／`21` 列；`33670` B／`47` 列）
⇒ **`18` 組**（配地鏈 `12` ＋ 抵費地 `6`）
**落檔** ＝ `verify/out/WG9268pR_c05_anchor.log`（`21422` B）／`.err`（**`0`** B）
🔒 **二跑逐位相同** ⇒ 其數可復現。

### 二-1　框之來源（逐項具名·**⛔ 另立幾何定義**）

| 量 | 來源 |
|---|---|
| `cut_coords → Polygon`（`is_valid` 否則 `buffer(0)`）| **沿用** `probe_WG9265_coverage_account.py` 之逐字 |
| 宗序 | 組內 `累積S(m)` 昇冪之 `0-based` 位次 ＝ **正典逐字**（`K-9-15 二`）|
| `d̂` ／ `corner_pt` | **FRONT_LINE** 之單位向量／其 `p1`（生產碼字樣錨 `d_hat = _np_d.array([_dx_fl / _L_fl, _dy_fl / _L_fl])`·其上註解逐字「**FRONT_LINE 為 d_hat + corner_pt 之主來源**」）|
| `n̂` | **ALLOC 方向之法向**（生產碼註解逐字「使 `_block_strip` 之 `n_hat` **∥ 地界線**」）|
| `s_錨_現行(j+1)` | ＝ `累積S(j)` ＝ **生產碼之錨**（`baseline_pt = corner_pt + left_cum_S * d_hat` 等四處）|
| `s_錨_幾何(j)` | ＝ 宗 `j` 沿**帶軸框**之極值 ＝ `W-G.9-84` 之 `S_req`（**框乙**·⛔ 框甲）|
| 重疊之判準 | 逐對 `intersection.area > 1e-6` ＝ **自擬**（同 `probe_WG9265` 之 `Σ−∪ > 1e-6` 之逐對形）|

### 二-2　六閘之判（**逐閘各載其母體與兩造對照**）

| 閘 | 受詞 | 判定集基數 | 結果 |
|---|---|---|---|
| **否證對照** | 幾何原語 | `2` | 人造相疊 `1.0000000000` ✅／人造相離 `0.0000000000` ✅ |
| **閘甲** | 框之標定殘差 ≤ `0.010 m` | **`15`** 組（可標定）| **全綠**；殘差 max ＝ `5.23e-03 m`（`3.5m·R2·left`）|
| **閘乙** | **以現行錨（`δ=0`）重建之重疊 ＝ `S-6` 覆蓋帳**（對數 ⋀ 逐對面積·4dp）| **`18`** 組 | **`18／18` ✅**（`0m·R2·left` `[19.8142, 26.1624]`；`3.5m·R5·left` `[56.8186]`；其餘 `[]`）|
| **閘丙**（**外錨**）| `s_錨_幾何` ＝ 倉內既有落檔之 `S_req`（`\|Δ\| ≤ 0.02 m`）| **`4`** | **`4／4` ✅**（`0m·R1·left j=0` `6.4800` vs `6.4796`；`0m·R2·left j=0` `7.4479` vs `7.4487`）|
| **兩造對照** | 陽性須增·陰性須減·二造⛔ 同值 | **`5`** | **全綠** |
| **新靶** | 以幾何終點為錨（**逐 `j` 累積套用**）後，全部可達組之重疊 | **`18`** 組 | 🔴 **全部為 `0` 對** ✅ |

🔒 **閘甲之判準之由（⛔ 任意）**：`TOL_FIT ＝ 0.010 m` ＝ `累積S(m)`／`S(m)` 於落檔為 **2dp**
⇒ 捨入上界 `0.005 m` 之 **2 倍**。⛔ 以「跑得過」回頭調之。
🔒 **`A` 由候選 FRONTLINE 方向定**（`A·dot(d̂,n̂) ＝ ±1` **構造保證**）⇒ `consist` 恆 `±1`、**⛔ 為判別者**；
判別者 ＝ **三項殘差**（`r_pt` 共用界線／`r_sl` 斜率約束／`r_ord` 次序約束·**皆以公尺計**）。
🩸 **本閘之界（照實）**：`S-6` 與本器讀**同一落檔** ⇒ 閘乙之獨立性在**算法**
（`Σarea − area(∪)` vs **逐對** `intersection`），**⛔ 在資料來源**。
**⇒ 閘丙（外錨·`S_req` 出自<u>他器他時</u>之落檔）方為真正之外部錨。**

### 二-3　🔴 **核心結果**（逐格·⛔ 只出合計）

| 組 | `j` | 暫編地號 | `s_錨_現行` | `s_錨_幾何` | **`δ`(m)** | 現況重疊 | **新錨後重疊** |
|---|---|---|---|---|---|---|---|
| `0m·R2·left` | `0` | `628-41(1)`〔街角地〕| `4.0600` | `7.4479` | **`+3.3879`** | `2` 對｜`0×2 19.8142`／`0×3 26.1624` | **`0` 對** |
| `3.5m·R5·left` | `0` | `628-45(1)+`〔街角地〕| `8.0600` | `11.3822` | **`+3.3222`** | `1` 對｜`0×2 56.8186` | **`0` 對** |

🔒 **`δ` 之彙總**（母體 ＝ **配地鏈** `left`／`right` 之 **`42`** 個 `(組, j)`；
🩸 **抵費地 `6` 組⛔ 在母體內**——其 `累積S` 恆 `0`、⛔ 具推進語意·已於器內逐組具名）

> **`Σδ`（帶號）＝ `+7.1133 m`** ／ **`Σ|δ|` ＝ `7.1878 m`**（**帶號與絕對值並列**）
> `min ＝ -0.0052`／`max ＝ +3.3879`／中位 ＝ `+0.0004`
> `left`：`n = 20`·`Σδ = +6.7104`·`Σ|δ| = 6.7533`｜`right`：`n = 22`·`Σδ = +0.4029`·`Σ|δ| = 0.4346`

🔴 **`|δ| > 0.01 m` 者僅 `3` 列／`42`**：
`0m·R2·left j=0` `+3.3879`／`3.5m·R5·left j=0` `+3.3222`／`0m·R1·right j=1` `+0.3969`。
🔒 **其餘 `39` 列之 `|δ| ≤ 0.01 m`** ＝ `累積S(m)` 之 2dp 捨入量級（`0.005 m`）
⇒ 🔴 **錨之偏差實質上集中於 `j = 0`（街角第 1 宗）**——與 `GB-67` 成因鏈之逐字
（「現行實作將錨取在**第 0 宗**之起點」）**同指**。

🛑 **`§四-4` 之停機款未觸發**：新錨後**⛔ 任一組之重疊 `> 0`**。
🔒 **⇒ 新靶成立**：「以幾何終點為錨時，全部可達組之重疊對數 ⋀ 逐對面積皆為 `0`」。

### 二-4　🩸 **逐 `j` 個別套用⛔ 等於累積套用**（照實·⛔ 隱去）

以**單一 `j`** 套用 `δ(j)` 時，`j ≥ 1` 之數格會**新生**微小重疊
（`0m·R2·left j=3` 生 `3×4 0.1671`；`j=4` 生 `4×5 0.0964`；`3.5m·R5·left j=2` 生 `2×3 0.0340`；`j=4` 生 `4×5 0.1543`）。
🔒 **其成因 ＝ `累積S(m)` 之 2dp 捨入**（該處 `|δ| ≤ 0.004 m`，係捨入噪聲、⛔ 真偏差）。
🔒 **累積套用⛔ 有此現象**——其以**當下之近側界**（`min strip`）為基準，⛔ 以落檔之 `累積S`
⇒ **`0` 對**。**⇒ `c1` 之實作須採累積形，⛔ 逐格獨立套用落檔之 2dp 值。**

---

## §三　`GB-157` 之鑄造 ＋ 全倉波及面掃描（補令二 `§二`）

🔒 **`GB-157` 已鑄**於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（受詞逐字沿用補令二 `§二`）。
🔒 **全倉波及面掃描**（落檔 `verify/out/WG9268pR_a4_scan.log`）
母體（**正面列舉·⛔ 全倉排除式**）＝ `docs/` ＋ `verify/` 之追蹤檔 **`1756`**（非 utf-8 解出者 `19`）·**檔框 ＋ 列框**：

| 受詞 | 檔框 | 列框 |
|---|---|---|
| `probe_WG985_grouphead` | `18` | `45` |
| `A-4`（裸框·含無關命中）| `134` | `410` |
| `0.2163` | `13` | `26` |
| `7.2 公尺` | **`0`** | **`0`**（**含命中 `0` 之情形·單明令**）|
| 對照組（人造·執行期組出·字樣⛔ 入倉）| **`0`** | **`0`** |

🔒 **共現列（`A-4` 或該器名 ⋀ 已停用之數·同列）＝ `2` 列**，已於 `GB-157` 內**逐處具名**並標
「**已停用·須於現態重得**」：`docs/orders/W-G.9-268p_施工單…md:78`（`5／5`／`3／5`／`3／3`）／
`docs/reports/W-G.4_泛用阻塞項登記表.md:2137`（`≤ 1e-6·5／5`）。
🔒 **其現態之值已由閘丙重得**（`4／4`·`|Δ| ≤ 1.01e-3 m`）。

---

## §四　`GB-67` 之更正註（補令二 `§一-1`）

已落於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**，含四項：
`(i)` 原述逐字 ＋ 字樣錨（`現行實作將錨取在第 0 宗之`·鑄前命中 `1`·判別力自檢已附）／
`(ii)` `0.2163` 之倉內受詞（**「CC 所算之『`K-9-9 二` 下限』」**·二處出處逐字）／
`(iii)` `c0-1`（丙）之現態語意 ＋ **八個字樣錨**與其唯一性自檢／
`(iv)` 時態錨 `c02d593`。
🛑 **⛔ 追改 `GB-67` 原列與其歷次加註一字。**

---

## §五　七號之鑄造（補令二 `§六`·**⛔ 逾此**）

🔒 **鑄前之實體占用現查**（**定義列式**·母體 ＝ 各該登記表**單檔**·態 `c02d593`·落檔 `verify/out/WG9268pR_occ_defline.log`）：
`自誤 333`〜`339` 與 `GB-157` 之**實體命中皆無**；對照組 `自誤 332` ∈ 集合 ＝ `True`、`GB-156` ∈ 集合 ＝ `True` ⇒ 器非紅。
🩸 **裸框之非零命中已逐筆歸類**：`GB-157` `4` 列／`自誤 333` `1` 列落於 `W-G.9-267R`，
**逐筆皆為〔零對照〕或〔判別力對照〕** ⇒ 依 `W-G.9-192-PRE-b 🔧 二`「**零對照之出艙⛔ 構成意指占用**」
⇒ **⛔ 構成占用**。其餘命中全落於本單族之 `8` 檔（〔授權〕或〔本單之產物〕）。

| 號 | 責 | 受詞（首句節略）|
|---|---|---|
| `GB-157` | — | 既有探針之內部模型與生產碼之對應量為不同之量，其自我驗證閘於現態不過 |
| `自誤 334` | 發單側 | 以未經自行復驗之他方報告表格充作開工閘之通過條件 |
| `自誤 335` | 發單側 | 取一受詞之方法時只讀其最新補款所列，未回溯同一簿內該受詞自身之成因鏈 |
| `自誤 336` | 發單側 | 立閘時，其通過條件於該閘被評估之時點結構上不可能成立 |
| `自誤 337` | 發單側 | 號之字樣與其檔名之字樣不同形，致占用閘之一軸結構性失明 |
| `自誤 338` | 發單側 | 立停機款時，其受詞繫於該器結構上⛔ 出艙之量 |
| `自誤 339` | 發單側 | 引一數為土地量級而未查該數之受詞 |

🔒 **`常規四（九）二` 之二項機檢（落地後當場復算）**
> ① **形之自證**：`自誤 334`〜`339` **逐項**皆被定義框命中 ✅；`GB-157` 亦然 ✅。
> ② **MAX 之推進**：自誤 `332` → **`339`** ✅（相異 `318`）；`GB` `156` → **`157`** ✅（相異 `81`）；
> 新號**⛔ 在缺號集** ✅。
🔴 **`自誤 333` ⛔ 在本批之授權內**（其屬已停機之 `W-G.9-268`）⇒ **本批⛔ 鑄之**，其仍列缺號 ✅。

---

## §六　補令二各條之施行對照

| 條 | 內容 | 本窗之辦理 |
|---|---|---|
| `§一` | 題 `A` 之裁：受詞精確化（標稱終點 → 幾何終點）| ✅ 採納·`c0-5` 全依新受詞量測 |
| `§一-1` | `GB-67` 加註之更正註（末端追加）| ✅ 已落地 |
| `§二` | shim 停用 ＋ 鑄 `GB-157` ＋ 全倉波及面掃描 | ✅ 三項皆辦（`§三`）|
| `§三` | 既有預測與量級**一併作廢**，新靶由 `c0-5` 重得 | ✅ 新靶已重得（`§二-3`）|
| `§四` | 新增 `c0-5`（零生產碼·得逕推）| ✅ `rc = 0`·全閘綠 |
| `§五` | 題 `D` 核可 ＋ 停機款 `3′` | ✅ 沿用·⛔ 再自解；`3′` **三肢皆未觸發** |
| `§六` | 鑄號授權七號 | ✅ 七號全鑄·**⛔ 逾此** |
| `§七` | `c1` 之依據改繫 | 🔒 **已記錄**（見 `§七`）·**本批⛔ 開工 `c1`** |
| `§八` | 續辦 | `c0-5` 已推 ⇒ **`c1`〜`c3` 候 KL 逐 `commit` 放行** |

🔒 **停機款 `3′` 之逐肢自檢**（補令二 `§五`）
| 肢 | 判 |
|---|---|
| `c0-5` 之自我驗證閘（`§四-2`）不過 | 🟢 **未觸發**（閘乙 `18／18`）|
| 兩造對照同值（`§四-3`）| 🟢 **未觸發**（判定集 `5`·全綠）|
| 新錨後仍有任一組之重疊 `> 0`（`§四-4`）| 🟢 **未觸發**（全部為 `0` 對）|

---

## §七　`c1` 之依據（補令二 `§七`·**本批⛔ 開工**）

🔒 `c1` 之設計依據 ＝ **`c0-1`（丙）之實測語意** ＋ **`c0-5` 之 `s_錨_幾何` 定式與其自我驗證閘**。
🔒 **保留原單 `c1` 之其餘全部逐字**：只改錨點與沿 `d̂` 平移一條界線之位置；⛔ 改面積目標、⛔ 解 `S`；
`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 一字不動；
`expand` 並存段、旗標預設 `off`、`off` 態逐位不變、`on` 態須相異。
🔒 **二檔須同改**（`c0-2` 已坐實錨之二列於二檔逐字相同）。
🔴 **`c0-5` 對 `c1` 之一項新增約束**（見 `§二-4`）：**累積形**，⛔ 逐格獨立套用落檔之 2dp 值。

🛑 **`c1`〜`c3` 係生產碼批 ⇒ 依 `W-G.9-206 §一` 之側分支流程 ＋ KL 逐 `commit` 放行**；
**本批⛔ 備妥任何 `c1` 之 `commit`。**

---

## §八　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | `numstat`（逐檔）| 生產碼判之輸出 | 判 |
|---|---|---|---|
| `c02d593` | `151	0	docs/orders/W-G.9-268p_補令二_受詞精確化與新靶重得.md` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 得逕推 |
| （本批之 `c0-5` commit）| 見 `git show --numstat`（`deletions` 逐列為 `0`）| **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 得逕推 |

🔒 **判別力**（證該判⛔ 恆 `0`）：`67cd1a4..HEAD` 之同一指令得 **`4`** 行
（`app.py`／`verify/run_all.py`／`verify/run_verification.py`／`verify/stepg_pipeline.py`）。

---

## §九　🩸 本批之**自捕**（照實·⛔ 隱去·各載 形／徵候／攔法）

| # | **形** | **徵候** | **攔法** |
|---|---|---|---|
| `1` | **以「各宗臨街段之端點」標定帶軸框** ⇒ 對**街角第 1 宗為偽** | 閘甲殘差 **`10.75 m`** | 逐邊列印後發現宗 `0` 之臨街段 `t∈[0,4.0554]` **內含**宗 `2` 之 `t∈[1.989,2.636]`（⛔ 相接）⇒ 改以**共用 ∥ALLOC 界線**（帶軸機自身之保證）標定 |
| `2` | **`A` 之符號於 `known == 1` 時二解皆得 `r_pt ＝ 0`** ⇒ `sort` 任取其一 | 閘丙紅：`0m·R1·left j=0` 得 `12.9596`（＝ `2 × 6.4798`·**鏡射**）而倉內 `S_req ＝ 6.4796` | 新增**次序約束** `r_ord`（每宗之 `max strip` 須 ≥ 其 `累積S`）——對錯號之解必然巨大 |
| `3` | **斜率約束⛔ 適用於街角第 1 宗** | 閘甲 `0m·R1·right` 之 `r_sl ＝ 0.4015 m` | `slopes` 之母體排除 `驗_宗序 == '街角第1宗'` 者 |
| `4` | **兩造對照之母體全掃 `j`** ⇒ 平移⛔ 波及之 `j` 必得「陰性 ＝ 現況」 | `0m·R2·left j=5`／`3.5m·R5·left j=3` **偽紅** | 母體改限「∃ 重疊對 `(a,b)` 使 `a ≤ j < b`」 |
| `5` | **抵費地組之 `累積S` 恆 `0`** ⇒ 其 `δ` 混入彙總 | `Σδ ＝ +253.41 m`／`max ＝ +135.0051 m`（**量級離譜**）| 逐列查其來源 ⇒ `|δ| > 0.01` 之 `8` 列中 `5` 列為抵費地 ⇒ `δ` 之母體限**配地鏈** |
| `6` | **`sed` 之替換端把反斜線讀為轉義**（承前批自捕 `2`）| 路徑變 `C:SERS\x07DMIN…` | 一律以 `python re.sub` 落檔並印相異列之新舊逐字 |
| `7` | **命令中殘留 heredoc 之引入符** | `PreToolUse:Bash hook` **阻斷**（`wg9237_heredoc_guard`·位移 `378`）| 改以 `Write` 落檔、以路徑呼叫（`GB-143` 補款二）⇒ 🔒 **該硬閘於本批<u>再度活體生效</u>** |

🔒 **`1`〜`5` 皆為<u>閘自身把本器擋下</u>**——五者**全部**發生在 `rc ≠ 0` 之狀態，
**⛔ 一項係事後回頭發現**。🔒 **此即新器之設計目的**（補令二 `§四-2`：新器之存在理由即在過此閘）。

---

## §十　自限復驗

| 項 | 值 | 判 |
|---|---|---|
| `git status --porcelain`（收工）| `0` | ✅ |
| `left-right`（推畢後）| `0	0` | ✅ |
| **生產碼 `34` 檔**：期初（`4ff8b35`）vs 期末 | 受檢 `34` 檔／**相異 `0`** 檔 | ✅ |
| ↑ **判別力**（證⛔ 恆同）| `4ff8b35` vs `67cd1a4`：相異 **`12`** 檔 | ✅ |
| 四簿 MAX | 自誤 `332`→**`339`**（**本批授權之鑄**）／`GB` `156`→**`157`**（同）／`VR` `95`（**未動**）／`K-9` `29`（**未動**）| ✅ 與補令二 `§六` 之授權**逐號相符** |
| append-only（二登記表）| `W-G.4_泛用阻塞項登記表.md` `743313`→`750897`（`+7584`·`numstat 114	0`）／`W-G.9波_…自誤登記.md` `671849`→`678929`（`+7080`·`numstat 87	0`）；二者 `new.startswith(old)` ＝ `True`、逆向 `False` | ✅ |

---

## §十一　本批之落檔（**逐檔 `git add`**·⛔ `git add -A`／`.`）

| 檔 | bytes | 內容 |
|---|---|---|
| `verify/probes/probe_WG9268p_anchor.py` | — | `c0-5` 之器（**新建·唯讀**）|
| `verify/out/WG9268pR_c05_anchor.log`／`.err` | `21422`／`0` | `c0-5` 之全輸出（**二跑逐位相同**）|
| `verify/out/WG9268pR_a4_scan.log` | `2208` | 全倉波及面掃描 |
| `verify/out/WG9268pR_occ_defline.log` | `2153` | 鑄號前之實體占用現查 ＋ 逐筆歸類 |
| `verify/out/WG9268pR_occ_pre2.log` | `13332` | `S-1` 號占用（入倉前）|
