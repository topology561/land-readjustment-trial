# `W-G.9-210R-c3`　`commit 3` 執行報告——`K-6 §二 段一`「上鎖」

> 施工單：`docs/orders/W-G.9-210_施工單_自誤263與264與VR090與補款三與commit3授權.md` `§二`／`§三`
> 結果態與驗收逐字沿用 `docs/orders/W-G.9-198R_…md` `§五`（自簽 `b291070e2bf1ee3aaf2c20200f385f64aef5c62e99016e8550548ba81d3dab54`·受詞 `26798` B·已自倉重抽驗）
> 🛑 **本報告⛔ 綁於 `commit 3`、⛔ 置於其後**（單 `§三-2`）——`commit 3` 走側分支候放行，報告走主線。

---

## §零　落地態（全 `40` 碼·量測時刻 `2026-09-02 18:29:55 +0800`）

| 受詞 | 值 |
|---|---|
| **`commit 3`** | **`cead6449854ba43a950d63ee2f16041c022e505c`** |
| 其 `parent` | `7e29eab30b9de6f3ca0f414549abd40e0f66dac0` |
| 其 `tree` | `a1f63af87e5eae9f9a76fff10bd160ea4941988a` |
| **側分支** `origin/verify/W-G.9-198R-c3` | `cead6449854ba43a950d63ee2f16041c022e505c` |
| 側分支 `origin/verify/W-G.9-198R-c2` | `a1107a17825c37430f69349afaceef4d09c6f5f8`（⛔ 刪） |
| 側分支 `origin/verify/W-G.9-198R-c1` | `b772f8aa09820f01312081588fd8c8d718c8ac86`（⛔ 刪） |
| **主線** `origin/wip/s1-endpart` | `7e29eab30b9de6f3ca0f414549abd40e0f66dac0` |

**主線⛔ 因 `commit 3` 前進之證**（🛑 期望值取**當時實查**、⛔ 寫死常數——承 `-208` 之量測器紅）：
① `commit 3` 之 `parent` **恰等於**現查主線頭；② `merge-base --is-ancestor` ⇒ **非**祖先；
③ 主線之二生產檔 blob 仍係 `commit 2` 之態（`app.py` `144349c8…`／`selection_pipeline.py` `f878276b…`）。

**`commit 3` 之三路保全**：本地 `keep/W-G.9-198R-c3` ＋ 遠端側分支 ＋ `reflog`（各 `1`）。

---

## §一　異動總表

| 檔 | `+`／`−` | 舊 blob | **新 blob** |
|---|---|---|---|
| `app.py` | **`+127`／`−0`** | `144349c834c2289434e1b0edbcfc8d0a27d88435` | **`a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190`** |
| `verify/selection_pipeline.py` | **`+11`／`−0`** | `f878276b70ff5244b71f0d688ce7ffe5bb8fee2c` | **`167a7beb8e9f7176ea70a53e6ad3bcb37c130b38`** |

- **`deletions ＝ 0`——本 commit 純加性**（`commit 1` 刪 `1` 列、`commit 2` 刪 `11` 列，本 commit `0`）。
- 🔒 **`verify/run_verification.py` 未觸**——本 commit **未增任何診斷欄**（`got_診斷_退縮3.5m`／`0m`
  二 CSV 之**首列逐位相同**、欄數皆 `24` 未增）⇒ `U-K2` 未觸發 ⇒ ⛔ 需 `_DECLARED_EXTRAS` 之申報。
- 🔒 **`補三-4` 之界**：`staged` 檔 `2`，**逾所准者 `0`**；二 blob `CR` 皆 `0`
  （`app.py` `1414673` B／`selection_pipeline.py` `35367` B）。
- 補丁自證：`4＋3` 處替換**逐處 `count==1`**；寫入後行尾均質（`CR == LF`）。

### 新 `AST` 區間（🛑 ⛔ 沿用 `S-0c` 之值）

| 函式 | **新** | （`S-0c` 舊值） |
|---|---|---|
| `k6_step0_block_locked` | `11567-11589` | `11567-11589`（未動） |
| `k6_step0_merge` | `11592-11752` | `11592-11752`（未動） |
| `select_corner_lots_both_sides_v12` | `12603-12972` | `12603-12972`（未動） |
| `_pk_one_side_v12` | `12975-13085` | `12975-13085`（未動） |
| `k6b_stage2_global_order` | `13088-13156` | `13088-13156`（未動） |
| `k6b_resolve_dual_crossing_sides` | `13159-13322` | `13159-13322`（未動） |
| **`k6b_stage1_locks`** | **`13325-13406`** | （本 commit 新增） |
| `main` | **`14589-23904`** | `14505-23777` |

`app.py`（`LF` 正規化）＝ **`1414673`** B ／ `LF` **`23906`** ／ `CR` **`0`**
（`S-0c` 舊值 `1406177` B／`23779`）。

---

## §二　結果態（`R3-1`〜`R3-8`）

| # | 落地內容 |
|---|---|
| `R3-1` | 新增模組級純函式 `k6b_stage1_locks(v13_result, block_label=None)`。上鎖之判準 ＝ 某街角**定案**（該側存在任一筆 `G ≥ 該街角規定面積`，即 `_pk_one_side_v12` 之 `qualified` 非空）⇒ 該街廓內**所有**跨占該街角規定範圍之土地（**含落選者**，即 `qualified ∪ eliminated`）一律上鎖 |
| `R3-2` | 效力於出艙欄與 docstring 逐字載明：**既不出也不進**（⛔ 被任何合併群抽走、⛔ 作為合併標的） |
| `R3-3` | 🛑 **⛔ 作用於步驟 0**：本函式係於 `select_corner_lots_both_sides_v12` 與 `k6b_resolve_dual_crossing_sides` 之**後**被呼叫，⛔ 位於 `k6_step0_merge` 及其被呼叫鏈上（機械證見 `D3-c`） |
| `R3-4` | 出艙 → `f3_k6b_stage1_locks`（逐筆：`街廓`／`暫編地號`／`所跨占之街角`／`達標`／`上鎖`／`上鎖之緣由`）＋ `f3_k6b_stage1_locked_by_block`（逐塊之上鎖集）；app 側 expander 渲染 |
| `R3-5` | 🔒 **唯讀**：⛔ 改動 `v13_result` 及其內任何候選物件之一字；其產出⛔ 被任何既有分配邏輯消費 |
| `R3-6` | 側無關·案無關：以 `p1_end`／`p2_end` 二**幾何端點**鍵識別；`block_label` 僅作出艙欄之資料 |
| `R3-7`（`補三-5`） | ⛔ 收斂 `GB-140`／`GB-141`；⛔ 動任何 `round` 位數；⛔ 動 `_dual` 次序；**⛔ 用到 `priority_index`** |
| `R3-8`（`補三-6`） | ⛔ 改動 `抵費地0m(應空)` 之凍存期望（繫 `GB-111`） |

🔑 **母體取「歸側調整後」之 `_v13`**——`K-9-24 二·5` 逐字「一經歸定，另一側**視為未被 `M` 跨占**」
⇒ `M` 於另一側已**非**「跨占該街角規定範圍之土地」、⛔ 因該側定案而上鎖。

`verify/selection_pipeline.py` 同構之三處：二累加器初始化（`_k6b_lock_rows`／`_k6b_locked_by_block`）
／同一函式之呼叫（`ns["k6b_stage1_locks"]`）／二 `ss` 曝鍵。**⛔ 改 `run_corner_pk` 之回傳元數。**

---

## §三　`D3-a` 〜 `D3-e` 逐項

> 基準 ＝ **`commit 2` 落地後之態 `6c8d8ea`**（依 `補三-3` **第三度**重做；
> ⛔ 沿用 `6a3f5fe`／`b772f8a`）。二態 `run_all` 皆於**拋棄式 clone** 內跑。
> 🔒 併證三態（`a1107a17`／`6c8d8ea`／`7e29eab`）之生產碼 blob **逐檔同一**
> ⇒ 基線可於任一態烤、以 `6c8d8ea` 命名。

### `D3-a`　🟢 **土地欄零變——且遠強於閘所求**

| 受詞 | 改前（`6c8d8ea`） | 改後（`commit 3`） | 判 |
|---|---|---|---|
| `got_診斷_退縮3.5m.csv` | `2885` B | `2885` B | **逐位相同** ✅ |
| `got_診斷_退縮0m.csv` | `2459` B | `2459` B | **逐位相同** ✅ |
| `got_指配_退縮3.5m.csv` | `835` B | `835` B | **逐位相同** ✅ |
| `got_抵費地_退縮3.5m.csv` | `113` B | `113` B | **逐位相同** ✅ |
| `34` 閘之 `PASS`／`FAIL` 集 | `10 PASS`／`24 FAIL` | `10 PASS`／`24 FAIL` | `diff` **`0` 列** ✅ |
| `run_all` stdout **全文** | `535` 列 | `535` 列 | `diff` **`0` 列** ✅ |

⇒ **土地欄逐格有變 `0` 格**；且**全部輸出零變化** ⇒ `R3-5`（唯讀）之最強證。

### `D3-b`　上鎖集合非空且非全集

候選（去重）**`19`** 宗 ／ 上鎖 **`15`** 宗 ／ 未上鎖 **`4`** 宗
⇒ 基數 **⛔ 為 `0`** ∧ **⛔ 為全集** ⇒ ✅。

**未上鎖者逐宗具名**（⛔ 只報數）：`R2` 之 `628-27(1)`／`628-40(1)+`／`628-41(1)`／`628-42(1)`
——其 `p1_end` 端**達標者 `0` 筆** ⇒ 該街角**待處理** ⇒ ⛔ 上鎖。

**`R3-1` 之逐塊復算**（定案 ⇒ 該側全體上鎖；待處理 ⇒ 該側全體不上鎖）：

| 街廓／端 | 候選 | 達標 | 上鎖 | 定案 | 判 |
|---|---|---|---|---|---|
| `R1`／`p1_end` | `3` | `2` | `3` | 是 | ✅ |
| `R1`／`p2_end` | `1` | `1` | `1` | 是 | ✅ |
| `R2`／`p1_end` | `4` | `0` | `0` | 否 | ✅ |
| `R3`／`p2_end` | `4` | `1` | `4` | 是 | ✅ |
| `R4`／`p2_end` | `1` | `1` | `1` | 是 | ✅ |
| `R5`／`p1_end` | `4` | `1` | `4` | 是 | ✅ |
| `R6`／`p2_end` | `2` | `1` | `2` | 是 | ✅ |

逐塊之上鎖集：`R1` `['628(5)','628-35(2)','628-36(1)','628-37(1)']`／`R2` `[]`／
`R3` `['628-27(2)','628-29(1)+','628-30(3)+','628-42(2)']`／`R4` `['628(1)+']`／
`R5` `['628-18(2)','628-45(1)+','628-53(2)','628-7(2)']`／`R6` `['628-18(1)','628-38(1)']`。

🔑 **「含落選者」之逐字確已落地**：`R1`／`p1_end` 之 `628-37(1)`（＝ `D2-e` 所名之
「第 1 名而**未達標**」者）**亦上鎖**；`R3`／`R5` 各有 `3` 筆落選者上鎖、`R6` 有 `1` 筆。

### `D3-c`　`R3-3` 之機械證（正面列舉·逐處具名產出/消費）

上鎖之值（`k6b_stage1_locks`／`_k6b_lk_set`／`_k6b_lk_rows_blk`／`_k6b_lock_rows`／
`_k6b_locked_by_block`／`f3_k6b_stage1_locks`／`f3_k6b_stage1_locked_by_block`／`_lk_n`／`_lk_p`）
於 `app.py` ＋ `verify/**.py` **五檔**之**全部**出現處 ＝ **`38`** 列。

| 類 | 數 | 說明 |
|---|---|---|
| 定義 | `1` | `def k6b_stage1_locks(...)` |
| **產出**（賦值） | `8` | 二累加器初始化 `×2`／函式回傳解構 `×2`／`_lk_n`・`_lk_p` `×2`（app）＋ 同構 `×2`（engine） |
| **產出**（累加） | `2` | `_k6b_lock_rows.extend(...)` |
| **產出**（曝值·唯讀出艙） | `6` | 四 `session_state`／`ss` 鍵之寫入 |
| 產出（渲染） | `4` | `expander` 標題 f-string／`_pd.DataFrame` |
| **讀** | `17` | 逐處判如下 |

**「讀」之逐處歸類**（🛑 **無一餵入任何分配邏輯**）：
`_k6b_locked_by_block[_lbl] = sorted(_k6b_lk_set)`（app `:20249`／engine `:484`）＝ **組裝唯讀出艙 dict**；
`_lk_n`／`_lk_p` 之 comprehension（app `:20451`／`:20452`）＝ **渲染用計數**；
`if _k6b_lock_rows:`（app `:20458`）＝ **渲染分支**（於 `expander` 內、二支皆只呼叫 `st.*`）。

🔴 **位於 `k6_step0_merge` 區間（`11592-11752`）者 ＝ `0`**（須 `0`）✅
🔴 **`k6_step0_merge` 內所呼叫之本檔函式**（正面列舉）＝
`['_proj_pop_assert_subset', '_proj_pop_ghost3', '_proj_pop_key_guard', '_projection_order', 'k6_merge_groups', 'k6_shares_segment', 'k6_step0_merge']`
——其中 ＝ `k6b_stage1_locks` 者 **`0`** ✅
其唯一被呼叫處 ＝ `app.py:4292` 於 `_annotate_temp_parcel_cut_type`（⛔ 觸上鎖之任何符號）。

🔒 **對照組（證器非紅）**：同法查 `k6_step0_merge` 區間內之 `_diag` ＝ **`13`** 列（非 `0`）。

### `D3-d`　反靜默退路（母體 ＝ `k6b_stage1_locks` `13325-13406`）

| 樣式 | 命中 | 判 |
|---|---|---|
| `raise RuntimeError` | **`2`** | ✅ |
| `except`／`return None`／`return False` | `0`／`0`／`0` | ✅ |
| **AST** `Try`／`ExceptHandler`／`Pass` | `0`／`0`／**`0`** | ✅ |
| `else:`（區塊） | `0` | ✅ |
| 三元 `if…else` | `3` | 逐處判（下） |
| `.get(` | `3` | 逐處判（下） |

🔴 **量測器自捕**：裸子字串 `pass` 得 **`2`** 列——經逐列具名，二者皆為**區域變數 `_pass`**
（`for _c, _pass in [...]`／`'達標': ('✅' if _pass else '')`）；**詞界式 `^\s*pass\s*$` 得 `0`**、
**AST 之 `Pass` 節點得 `0`**（全 `app.py` 對照組 `48` 列 ⇒ 器非紅）⇒ **裸子字串係假命中**。

三個三元 `else` **皆為顯示用**：`'✅' if _pass else ''`／`'🔒' if _lk else ''`／緣由字串之二擇一。
**上鎖之決定本身** ＝ `_settled = {_s: bool(_grp[_s][0]) for _s in _SIDES}`——
直取 `qualified` 之布林，**無任何 fallback**。

三處 `.get(`：`_c.get('暫編地號')` → 緊接 `if not _v: raise` ✅；
`v13_result.get(_s)` → 緊接 `if _r is None: raise` ✅；
`_r.get('qualified')／('eliminated')` → 契約鍵，`or []` ＝「**該側無候選**」之正解（非靜默預設）。

### `D3-e`　四閘違規全集／`--selftest`／既有終局

1. **四閘違規全集**：二態逐列相同——`率接線 G估 欄變動0m` **`8` 格**、`3.5m` **`12` 格**、
   `v3·診斷0m`／`3.5m`、`率接線無串聯0m`／`3.5m` 之閘列**逐字相同**。
2. **`--selftest`**：於生產碼四檔（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／
   `verify/run_verification.py`）**皆 `0`**（對照組 `verify/probes/probe_order_preflight.py` ＝ `6`）
   ⇒ 該旗標於本倉**不存在** ⇒ 改以**結構證**（`D3-c` 之 `38` 處逐處具名 ＋ `D3-d` 之 AST 級反靜默）代之。
3. **既有終局**：`k94_assert_baseline_touch`／`628-53(2)`／`38.803` 二態皆 `0` 命中；
   `②-宗 圍堵閘破[R2]` **`12`** 列、破量 `45.9766` **`12`** 列，二態相同。
   🔒 此三項由 **`run_all` stdout 全文 `diff` ＝ `0` 列**所**蘊含**（更強之陳述）。

### `R3-6` 之機械證（母體 ＝ **去 docstring** 之 AST）

字串常數 **`19`** 個（正面列舉）：`'p1_end'`／`'p2_end'`／`'暫編地號'`（`×2`）／二則 `raise` 訊息／
`'qualified'`／`'eliminated'`／`'街廓'`／`'所跨占之街角'`／`'達標'`／`'上鎖'`／`'上鎖之緣由'`／
`'✅'`／`''`（`×2`）／`'🔒'`／二則緣由字串。數值常數僅 **`0`**（一個）。

| 類 | 樣式 | 命中 | 判 |
|---|---|---|---|
| 街廓名 | `R1\|R2\|R3\|R4\|R5\|R6\|RD` | `0` | ✅ |
| 側標字面 | `左\|右` | `0` | ✅ |
| 本案地號 | `628` | `0` | ✅ |
| 本案常數 | `3.5\|226.01\|116.08` | `0` | ✅ |

🔒 對照組（證器非紅）：同法查 `'p1_end'` ＝ `1` 個（非 `0`）。

### `R3-7` 之機械證

`priority_index` 之**裸子字串**於本函式命中 `1` 列（`:13345`）——經查係
**docstring 內之否定宣告**（「**⛔ 用到 `priority_index`**——上鎖之判準只有…」，
docstring 佔 `:13326-13358`）。**去 docstring 之 AST 命中 ＝ `0`**
（對照組：同法於 `_pk_one_side_v12` 得 **`3`** ⇒ 器非紅）。`round(` 於本函式 ＝ **`0`** 列。

---

## §四　🛑 停機（單 `§三-1-2`）

`commit 3` **⛔ 在主線**，候**發單側自側分支 `verify/W-G.9-198R-c3` 逐位元組復驗 → 請示 KL → 放行**；
放行後依 `-207`／`-209` 之等值四判準（`E-1`〜`E-4`）rebase 並推進主線。

🔒 **判別力自證之見證受詞依 `VR-090` 肢一之機械測試選出**（`-210 §三-1-3`）：
其選法 ＝ `S ＝ {f : git diff --name-only <舊基座> <新基座> 命中 f} ∖ {f : git show --name-only <本 commit> 命中 f}`。
🛑 **本次尚無「舊基座→新基座」之推進**（`commit 3` 之 `parent` 即現主線頭、⛔ 經 rebase）
⇒ 該自證於**本階段⛔ 適用**（`S` 之左項為空集）；俟放行後之 rebase 階段方適用，屆時逐項回報。

**其後**（單 `§四`）：`commit 3` 放行後之下一件 ＝ **`GB-140` 處三 ＋ `GB-141` 同批上呈 KL**
（泛化波前置）。本批⛔ 辦。

---

## §五　次號（本批落地後）

自誤 **`265`**／`GB` **`142`**／`VR` **`091`**（三位補零）／戒 **`41`**／`K-9` **`27`**。
