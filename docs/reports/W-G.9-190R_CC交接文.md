# 🧾 `W-G.9-190R`　**CC 交接文**（新 session 接手用·KL 令換窗）

**出文窗**：CC　**性質**：🔒 **交接文**·⛔ 非正典·⛔ 非施工單·⛔ **未鑄任何號**
**取號**：`常規四 三`（CC 之產物 ＝ 該單號 ＋ 後綴 `R`）⇒ 用 `W-G.9-190R`；
⛔ **未取 `W-G.9-192`**——該序屬**發單側**（`常規四 四`），且施工單 `§零` 已將其配給**遞補批**。
🔒 檔名占用現查：`W-G.9-190R_CC交接文.md` 之全倉命中 **`0`** ⇒ 未占用。

---

## 🛑 `§零`　**最要緊之一件事：本窗有 `4` 個 commit <u>未 push</u>**（`常規九 二`）

🔴 **⛔ 不得把下列 commit 記為倉態。** 其僅存在於**本機拋棄式 clone**：

```
C:\w190r
```

🛑🛑 **該 clone ⛔ 不得刪除，直到 `push` 完成。** 刪除即**滅失**四個 commit（含二個生產碼 commit）。

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart`（**實查**） | **`69daed5e74335bbc3320a6bfcbb202bdec19fd44`** |
| `C:\w190r` 之本地 `HEAD` | **`ba4e39f4b787ab800c6bcd036a7faf3e402c64e3`** |
| `left-right`（領先／落後） | **`4  0`** |

**未 push 之四 commit（由舊而新）**：

| commit | 內容 | `§二-2` 判法 |
|---|---|---|
| `571b2c3` | `W-G.9-190R` commit `3`／`5`：**族② 之切換**（`app.py`） | 🔴 **生產碼** |
| `c141b47` | `W-G.9-190R` commit `4`／`5`：**族③ 之切換**（`app.py`） | 🔴 **生產碼** |
| `aa52bc5` | `W-G.9-190R` commit `5`／`5`：`S-6` 四分分類器 | 零生產碼（**被祖先阻擋**） |
| `ba4e39f` | `W-G.9-190R` 收工：倉內報告 ＋ `run_all` 二態證據 | 零生產碼（同上） |

### 🛑 接手者之**第一動作**（`常規九 三`：⛔ 不得以本檔所載之 commit 為錨）

`git ls-remote origin refs/heads/wip/s1-endpart` ⇒ **逐分支之處置**：

- **＝ `69daed5`** ⇒ 🔴 **上開四 commit 仍未入倉**。
  ① 先確認 `C:\w190r` 是否仍在（`ls /c/w190r/.git`）。
  ② **在** ⇒ 其 `HEAD` 應為 `ba4e39f`；**待 KL 核可後** `git -C C:/w190r push origin HEAD:refs/heads/wip/s1-endpart`。
  ③ **不在** ⇒ 🛑 **停機上呈**：四 commit 已滅失，須向 KL 確認是否重做。**⛔ 不得假設其已落地。**
- **＝ `ba4e39f` 或其後裔** ⇒ 已 push，照 `§一` 正常開工。
- **＝ `69daed5` 之祖先** ⇒ 🛑 **停機**。

### 🛑 為何未 push（⛔ 非疏漏）

施工單 `W-G.9-190R §二` 逐字：**生產碼 commit「`push` 前一律請示，⛔ 無自辦款」**。
CC 已於前一則聊天完整請示（附 `§四` 全項、二主閘全文、`S-6` 前後差數、逐 commit 逐檔 `numstat`），
**KL 尚未回覆核可**即令換窗。⇒ **⛔ 非 CC 未辦，係待裁。**

---

## `§一`　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（其於本波已由 `807` → **`1041`** 列）。
2. 🛑 讀 **本檔** ＋ `docs/reports/W-G.9-190R_族二三切換.md`（執行報告·`426` 列）
   ＋ `docs/orders/W-G.9-190R_族二三切換.md`（施工單·`441` 列）。
3. 🛑 **開工閘**：短路徑拋棄式 clone（`MAX_PATH`），`git -c core.autocrlf=false clone --branch wip/s1-endpart`
   ＋ `git config core.autocrlf false`／`user.name diag`／`user.email diag@local`
   ⇒ 對拍 `HEAD` 全 `40` 碼與 `app.py` blob。自檢器置於**另一**短路徑 ⇒ 母體 `rglob` 自然自扣。
4. 🛑 **長跑（`run_all`）一律於<u>另一個</u>拋棄式 clone**（`GB-94`：其會改寫 `verify/out/**` 之**已追蹤** log）。
5. 🆕 **收單機檢 `S-0b`**（本波新立·`CLAUDE.md` 末端已載）：
   `python verify/probes/probe_order_preflight.py <單.md>` ⇒ `P-3`／`P-5` 紅即停機退單。
6. 🔴 **harness worktree 之陷阱（每窗必撞）**：Claude Code 之 worktree **自 `main` 開分支**
   ⇒ 工作區落後數百 commit，**自動載入之 `CLAUDE.md` 係 `main` 態之 `190` 列**（工作態 `1041` 列）。
   **判準**：`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` **非 `0 0` 即工作區為錯態**。
   ⇒ 🔒 **⛔ 不得直接 `cat` worktree 之任何檔**；一律 `git show <工作分支 rev>:<path>` 或於 clone 內讀。
   ⚠️ 其**具體數字逐窗變動**，⛔ 不得照抄——**恆真者只有判準本身**。

---

## `§二`　倉態與取號現查（態 ＝ 本地 `ba4e39f`·CC 現查）

### `§二-1`　三數（框 ＝ blob 位元組／`splitlines()`／`sha256` 前 `16`·CRLF 皆 `0`）

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| **`app.py`** | `1383997` | `23463` | **`ef698be4ed43bc01`** |
| `CLAUDE.md` | `89295` | `1041` | `d530f27f97ede9ad` |
| `verify/stepg_pipeline.py` | `96261` | `1454` | `0260273350021311`（**全波 `0` 列異動**） |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `410707` | `4471` | `569d83d42c6d0b48` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `529494` | `3238` | `499ce534e43f332e` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| `docs/rulings/K-6_…判準.md` | `258295` | `3632` | `6a5cada2901a10e8` |
| `docs/reports/W-G.9-190R_族二三切換.md` | `28054` | `426` | `2df0a91974f153d1` |
| `docs/orders/W-G.9-190R_族二三切換.md` | `35215` | `441` | `4917a0a99bd82876` |
| `verify/probes/probe_WG9189_s6_replica.py` | `26327` | `535` | `c99716f5c9457c05` |

⚠️ `app.py` 檔尾**⛔ 無 `LF`** ⇒ `wc -l` 得 `23462`、`splitlines()` 得 `23463`（`VR-057`）。

### `§二-2`　取號（母體 ＝ `git ls-files -z` 之全倉 `.md` **`477`** 檔·**開不到之檔數 `0`**）

🛑 **⛔ 不以「算術自檢 True」為憑**（`GB-125`）⇒ 出艙 `相異`／`MAX`／`缺號數` 三數本身。

| 類 | 框（逐字） | 相異 | MAX | 缺號數 | **次號** |
|---|---|---|---|---|---|
| 自誤（窄框） | `` ^###\s*自誤\s*`?([0-9]{1,4})` `` | `47` | **`229`** | `182` | **`230`** |
| 自誤（**`W-G.9-184` 之正典寬形**） | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `198` | **`229`** | `31` | **`230`** |
| `GB`（三框聯集） | `` ^(?:## \|### \|\| *)`GB-([0-9]{1,4})` `` | `44` | `127` | `83` | **`128`** |
| `VR`（扣哨兵 `999`） | `` ^## `VR-([0-9]{1,4})` `` | `69` | `84` | `15` | **`085`** |
| 戒（`CLAUDE.md:826` 正典框） | `` ^(?:🛑 \|> )?\*{0,2}戒 \*{0,2}`?([0-9]{1,3})`? `` | `22` | `39` | `18` | **`40`** |

🔒 **自誤二框並報**（`CLAUDE.md:725`）：**MAX 同為 `229`** ⇒ 次號 `230` 不受框差影響。

### `§二-3`　🔴 **新發現之框污染：`docs/orders/` 之入倉使施工單散文進入母體**

🩸 **本波 commit `2` 立 `docs/orders/` 匯流排**（施工單以原名入倉）⇒ **施工單之散文亦成為各框之母體**。
🔒 **實測（態 `ba4e39f`）**：

| 框 | 全母體 相異 | 扣 `docs/orders/` 後 相異 | 污染命中 | 差集 |
|---|---|---|---|---|
| 本單 `§四` 之戒框 | `21` | `20` | `2` 列 | **`[1]`** |
| **`CLAUDE.md:826` 正典戒框** | `22` | `21` | `2` 列 | **`[1]`** |

**污染之二列（逐字）**：
- `docs/orders/W-G.9-190R_族二三切換.md:155` ＝ `🛑 戒 \`1\`〜\`15\`／\`21\`〜\`23\` 係**倉外已鑄**…`
- `docs/orders/W-G.9-191_發單側交接文.md:71` ＝ 同形

⇒ 🔴 **`戒 1` 被誤判為「已鑄且可現查」**，而其實係 **prose 引用**（且該號本係**倉外已鑄**·見 `CLAUDE.md` 之類②清單）。
🔒 **MAX 二框皆仍 `39`** ⇒ **次號 `40` ⛔ 不受影響**；受影響者僅**相異／缺號集**之斷言。
🔴 **同族**：`VR` 裸框得 `999`／`自誤` 裸框得 `298`／`戒 40` 被**次號之書寫**污染
——本則係**第四度**，且其成因是**新立之匯流排本身**。
🛑 **列 `§四-2` 呈裁**（⛔ CC 未自行處置：可能之解含「取號之母體排除 `docs/orders/**`」，屬**判準之變更**）。

---

## `§三`　本波之位置（`W-G.9-187` 〜 `W-G.9-190R`）

| 單 | commit | 要旨 |
|---|---|---|
| `W-G.9-187` | `1796e70`／`761a4fa` | **幾何本體批第一批**：`K-9-12` 矩形容納（可旋轉平移）＋ `K-9-23` 藍影骨架 ＋ 二閘（**觀測模式**·配地零變動）。`[T3-GATE]` `39` 列判**不配地 `18` 筆**；規格書預言之**假綠三格**（`R1右`／`R3右`／`R6右`）逐格坐實 |
| `W-G.9-189` | `25857aa` | 🛑 **停機**：`S-6` 複刻區同步閘觸發（**實質差 `20` 處**）。🔑 遞補所在之二迴圈 `28/28`·`29/29` **僅 `1`〜`2` 處分歧** ⇒ 污染源**收斂為單一具名項**（族②） |
| `W-G.9-190`（**已廢止**） | `4251234` | 🛑 **停機**：主閘 `[T5-FAM2]` **不可量**（受詞在 `main()` 內而以 `run_step_g` 量之）＋ 判準**方向倒置**（三量中二量） |
| **`W-G.9-190R`** | `d8d64b0`／`69daed5`／**`571b2c3`**／**`c141b47`**／`aa52bc5`／`ba4e39f` | **族②③ 之切換**（`W-G.9-5` 落地）＋ 自誤 `224`〜`229` ＋ pre-flight 機檢器 ＋ `docs/orders/` 匯流排 ＋ `S-6` 四分分類器 |

### 🔑 本波之結論鏈（下一單之全部前提）

**①** `S-6` 證：迴圈層之人工複本，除**一個起始值**（族②）外**逐句同步**；遞推式
`_W_prev_left = float(res.get('W_far', _W_prev_left))` 在 `app:21097` 與 `stepg:676` **逐位相同**。

**②** 🔑 **族②③ 已切換且主閘全過**：`G-1`（AST 逐位同）／`G-2`（還原後判不同 ⇒ 非恆真）／
`G-3`（四框 `1→0`）／`G-4`（二框 `1→0` ＋ 委派呼叫 `0→1`）／`G-5`（還原後二框各得 `1` ⇒ 非恆綠）。

**③** 🔑 **佐閘：`R2` 三量同時收斂** `32.9639559294`／`3`／`4` → **`33.0810700422`／`2`／`3`**
（`|Δ|` `5.542e-13`）。**方向 ＝ `3→2`／`4→3`**——⚠️ `CLAUDE.md:760` 所載之 `2→3`／`3→4`
係 **shim 之注入方向**（`自誤 227`），**⛔ 不得再照抄**。

**④** 🔑 **`run_all` 之紅係基座既有**：基座 `4251234` 與期末 `571b2c3` 各跑一次，
二 log 各 `516` 列，`diff` `48` 列**全部**為 clone 路徑；正規化後**各 `32972` B、逐位相同**。
（判別力：注入一字元後重判 `False`；二路徑皆 `8` 字元 ⇒ ⛔ 無截斷位移。）

**⑤** 🔴 **`GB-123` ⛔ 未解除**：族②③ 之受詞**皆在 `app.py` 之 `main()` 內**，
而 `main()` **從不被 `run_all` 執行** ⇒ **harness 之任何量測對其結構上不敏感**。
本波之佐閘係 **harness 內之注入式模擬**、⛔ **非 app 路徑實跑**；
**唯一真量 app 路徑者係 KL 之 UI 實跑**——本波⛔ 未授權、⛔ 未辦。

**⑥** 🔑 **三個工具，其缺皆由<u>其自身之判別力造</u>抓出**（⛔ 非事後回頭看）：
pre-flight（`P-6` 粒度／`P-3` 射程）／四分分類器（「乙」誤實作為**敘述形**而非**識別符**）／
`W-G.9-187` 之 `_rect_fits_free_pose`（掃描域誤取 `[0,90°)`）。
🔒 **通則**：**判別力造不是形式**——本波三度是它、而非人眼，抓到實作紅。

---

## 🛑 `§四`　下一單之受詞

### `§四-1`　**待 KL 之裁 ＝ `push`**（🛑 最優先）

見 `§零`。**⛔ 未獲核可前，⛔ 不得刪除 `C:\w190r`、⛔ 不得重跑覆蓋。**

### `§四-2`　🛑 **四項呈發單側裁**（已寫入執行報告 `§九`）＋ **本檔新增之第五項**

1. **四分結果：類甲 `14`／乙 `1`／丙 `6`／丁 `0`** ⇒ 與 `§十` 之「期末預期類甲 `0`」不符，
   依其逐字**逐項具名呈裁**（機械理由見 `verify/out/probe_WG9190R_class4.log`）。
2. 「乙」之識別符集採**二側對稱差**（⛔ 非聯集）——若認過寬，改聯集係一行之改，
   惟屆時**類乙近乎不可達**。
3. 收工閘 `5`（`[T3-GATE]` 期末仍 `39` 列）**⛔ 未辦**：其情境係 `SB = 0.0 m` 而本批全部量測係
   `SB = 3.5 m`，二情境⛔ 不可互代；擬於次一零生產碼 commit 補跑並逐宗具名其變動。
4. 施工單 `§四` 之「戒」框**要求 `🛑`** 而 `戒 16` 之定義列（`自誤登記表:3783`）⛔ 無之
   ⇒ 與同節敘述互斥。已依 `常規二` 取保守項（`CLAUDE.md:826` 正典框）。
5. 🆕 **`docs/orders/` 之框污染**（`§二-3`）——**新立之匯流排使施工單散文進入取號母體**。

### `§四-3`　`W-G.9-192` ＝ **遞補批**（`K-9-17`）

施工單 `§零` 逐字：「⛔ **不含遞補**——遞補順延 `W-G.9-192`」。
🔒 其**前置已由本波備妥**：`W-G.9-187R` 之二閘（`_k923_gate1`／`_k923_gate2`）已落地、
`S-6` 之污染源（族②）已拔除、四分分類器已入倉。
🛑 **惟 `GB-127` 仍未解除**——`_rect_fits_free_pose`／藍影骨架／二閘於**生產路徑尚無呼叫端**，
其解除條件逐字 ＝「**第二批之遞補迴圈接上呼叫端**」。

---

## `§五`　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | **`GB-127`**（生產路徑無呼叫端） | ⬜ 待 `W-G.9-192` 之遞補接線 |
| `2` | **`GB-123`**（app 路徑從未被自動測試執行） | 🔴 **⛔ 未解除·本波明載之** |
| `3` | **`GB-111`**（凍存期望 FAIL 名單已失效·**須 KL 決重產時點**）——本波實測其**確已過期**（凍存載「宗數 `15`」而基座即為 `14`） | ⬜ |
| `4` | `GB-104` 之交叉 loud 斷言（**獨立第三批**） | ⬜ 未辦 |
| `5` | `GB-107`／`GB-108`／`GB-110`／`GB-112`〜`GB-126` | ⬜ 只登記 |
| `6` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `7` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `8` | `K-9-22` 落地／`VR-074` 之他層／`VR-075` 落地 | ⬜ |
| `9` | `K-6-A2` 剩餘（`K-9-1`／`K-9-3`／`K-9-5`／`K-9-6` 宗地層 `min_depth`／`K-9-8` 之 `GB-18`／`K-9-5-2` ①⑤） | ⬜ |
| `10` | `K-6-B` 段一〜段四（步驟 `0` 已落地） | ⬜ |
| `11` | 🆕 `§四-2` 之**五項呈裁** | ⬜ **待發單側** |
| `12` | 🆕 `[T3-GATE]` 於 `SB = 0.0 m` 之期末復量（收工閘 `5`） | ⬜ |

---

## `§六`　🔑 本波所立之家法與陷阱（🛑 接手者必讀）

### `§六-1`　新入 `CLAUDE.md` 者（皆末端追加·⛔ 未鑄號）

1. **`常規一` 之補款**（`W-G.9-190R §二`·KL `2026-08-31` 新增款逐字）：
   > **零生產碼 commit 逕行 push，生產碼 commit 才請示。**
   其「生產碼」之**機械界定** ＝ 四檔清單（`app.py`／`stepg_pipeline.py`／`run_all.py`／`run_verification.py`），
   **逐 commit** 以 `git diff --name-only <前一 commit>..<本 commit> | grep -xE '…'` 判之，
   🛑 **該指令之輸出（含空輸出）須逐 commit 出艙**，⛔ 不得以「我判斷是零生產碼」代之。
   🔒 零生產碼側⛔ 不再受 `常規一` ②③④ 之拘束（**含 `deletions > 0` 之就地更正**）；
   🔒 **⛔ 放寬的是「誰批准 push」，⛔ 不是「什麼可以改」**——禁改清單⛔ 不因此鬆動。
2. **施工單與報告之傳遞媒介 ＝ 倉**（`docs/orders/`）＋ 收單機檢 `S-0b` ＋
   🛑「**⛔ 不得據此把整條鏈接成無人流水線**」＋ **KL 側之「見檔案卡方採信」**。
3. **`戒` 之定義列式與其三類受詞**（`W-G.9-188 裁 B`·於 `W-G.9-187R` 落地）：
   類① 倉內已鑄而舊框漏（`戒 16`）／類② **倉外已鑄 `18` 號**（⛔ 結構上撈不到·靜態清單）／
   類③ **無編號之戒 `9` 則**（⛔ 不占號、⛔ 不得回頭給號）。
4. **族②③ 之字樣錨**（⛔ 不用行號）＋ 併記 `CLAUDE.md:760` 之箭頭係**注入方向**。

### `§六-2`　🩸 陷阱（本波實測·⛔ 不重踩）

1. **`heredoc` 吃反斜線／全形括號** ⇒ 含 `\` 或全形括號之內容**改用 `Write` 直寫檔**（本波踩 `3` 次）。
2. **Git Bash 之 `/tmp` 與 Windows Python 之 `/tmp` ⛔ 非同一目錄** ⇒ 跨二者一律用 `/c/xxx`。
3. **`git ls-files` 預設 `core.quotepath=true`** ⇒ CJK 檔名被轉義，以其輸出直接 `open()` 者
   **全部開不到、靜默得 `0`** ⇒ **一律 `-z` 並以 `\0` 切分**，且須併報「開不到之檔數」（須 `0`）。
4. **`bytes` vs `str` 之 `endswith`**：`ls-files -z` 之輸出係 `bytes`，`x.lower().endswith(".md")` 會 `TypeError`。
5. **哨兵勿入倉**：`W-G.9-187` 之段 A 曾把 `minimum_rotated_rectangle` 之**字面**寫進註解
   ⇒ 會污染全倉計數 ⇒ 改以 **AST** 機檢，且字面⛔ 不入倉。
6. 🆕 **入倉施工單會污染取號母體**（`§二-3`）——**新立之匯流排本身即污染源**。
7. **二母體之框⛔ 不通用**：`app.py` **原始碼**含**字面括號**，而經 `ast.unparse` 正規化之輸出**去括號**。
8. **`ast.dump` 之 bytes ⛔ 非跨環境錨**（`CPython 3.12.3` 與 `3.13.11` 得數不同而判定相同）
   ⇒ **只對拍判定與差數**。
9. **兩趟 log 逐位對拍**：先確認二工作目錄名**長度相同**（本波 `C:\w190t`／`C:\w190u` 皆 `8` 字元），
   否則固定 byte offset 之截斷會偽造差異。

---

## `§七`　已知 harness 限制

- 🔴 **`app.py:main()` 內之敘述從不被 `run_all` 執行**（`CLAUDE.md`）
  ⇒ `def main` 之 AST 區間 ＝ **`14234`-`23409`**（**須實查·⛔ 不得硬編**，其隨 `app.py` 變動）。
  **族②③、`_advance_block_with_split`／`_solve_one`／`_build_g_row` 皆在其內。**
- 🔴 `run_verification` 於 `②-宗 圍堵閘破[R2]` **中止** ⇒ `run_all` 主幹之 `T2-DIAG` **僅有 `R1`／`R2`**（`GB-118`）。
- 🔧 **逐街廓 `run_step_g` 可繞過之**：`run_step_g(ns, …, [tp for tp in build_p if tp.get("所屬街廓")==<blk>], …)`
  ⇒ 六街廓皆可攔到（`R2`／`R4`／`R5` 雖各拋 `RuntimeError`，**攔取在前**）。
  範本 ＝ `verify/probes/probe_WG9190_baseline.py`。
- 🔒 `run_all` 於基座即 `FAIL`（**准紅碼**）；驗收改為與凍存名單「**名稱＋原因**」對帳
  （`verify/wv_reconcile.py`·三分類）。**本波實測基座與期末之對帳輸出逐位相同。**
- 🔒 `run_all` 會改寫 `verify/out/**` 之**已追蹤** log（`GB-94`）⇒ **一律於另一拋棄式 clone 跑**。
- 🔒 探針之拒絕覆寫：合法出口 ＝ 環境變數 **`WV_OUT_NAME`**（⛔ 不得 `WV_ALLOW_OVERWRITE=1` 覆寫已追蹤者）。

---

## `§八`　memory 之指引

🛑 `MEMORY.md` 之主題行係**併看入口**；查具體主題時**須循主檔之併看清單再讀該檔**。
🔒 **每窗必讀之四條**：`push-before-shipping-claim`／`grep-multibyte-charclass-trap`／
**`wg9143-handoff-entry`（主線入口）**／`harness-worktree-crlf-trap`。
⚠️ **memory 所載之具體數字皆易腐**（逐批前進即變）——⛔ 不得照抄，**一律當場現查**。

---

## `§九`　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：三重驗證 ＝ `ls-remote` 直查／`is-ancestor` `rc=0`／`left-right` ＝ `0 0`。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值、⛔ 不得改框／母體／容差使其相符。
- 🔒 **停機款觸發一律先停機**（⛔ 不先辦後呈），於停機回報中**併陳擬辦之意見**。
- 🔒 **`GB-121` 施行式**：施工單之**停機款**須於報告 `§一` **逐字引錄**其原文（⛔ 非摘要）。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`
  （期望 `rc=0`·`stderr` `0` B·受檢 **`77`** 筆·`嚴格前綴＝True` ×`77`）。🛑 **於 `commit` 前跑**。
- 🔒 **新探針入倉前必過**「禁寫死本機絕對路徑」機檢（**本批新增**命中須 `0`·附判別力對照）。
  ⚠️ 全倉現有 **`3`** 個**既存**命中（`probe_WG971_wrap.py:203`／`:247`／`tools/wg960_z_selfcheck.py:12`）
  ——⚠️ 交接文歷來載 `4`，其差係**框相異**（`tools/nameset_diff.py:19` 係**中文註解**、⛔ 非寫死路徑）。
- 🔒 **拋棄式 clone 跑畢整份刪除**並於回報中確認——🛑 **本窗之 `C:\w190r` 係例外，見 `§零`**。
- 🔒 **逐檔 `git add`**（⛔ 禁 `-A`／`.`·`verify/out/` 之戒）。
- 🔒 **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
