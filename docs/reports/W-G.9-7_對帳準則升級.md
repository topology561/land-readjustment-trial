# `W-G.9-7`：對帳準則升級為「**名目 ＋ 失敗原因**」（KL 裁 2026-08-11）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg97-recon`）
**基準**：`49f40f9`　**事前登記**：`docs/reports/W-G.9-7_前置登記.md`（commit `7287205`）

> # ✅ **`H1`〜`H5` 全部成立；`U-1`〜`U-4` 皆未觸發。**
>
> **洞 B 已修**：對帳自此比「名目 ＋ 失敗原因」，且**於每次 `run_all` 自動發生**。
> 🔴 **洞 A 未修**：`results.append` 站點 **107** 處中 **80 處從未執行**（現算·見 §7）
> ——本批**未**動之。

---

## §1 授權（逐字·⛔ 本批之唯一授權來源）

> **KL 2026-08-11**：同意把驗收核對方式改成「**名稱＋原因一併比對**」＝**是**。

⇒ 僅及於**對帳方式**。⛔ 未修任何既有 FAIL、⛔ 未立 `G3` 三項之閘、⛔ 未掛三支端到端複本。

---

## §2 `H1`〜`H5` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **H1** | ✅ **成立** | **真實歷史資料為靶**（⛔ 非合成竄改）：`D2b1_runall.log` 端 類 II／III **皆空**；`WG94b_runall.log` 端 類 III **恰 4 項**且逐項 ＝ `W-G.9-6` 所具名者。**雙向閘皆過**（不多不少） |
| **H2** | ✅ **成立** | 移除 `R1` ⇒ 類 I/II/III 由 `(18,0,4)` 變 `(10,0,12)`；移除 `R2` ⇒ 同變 `(10,0,12)` ⇒ **兩條皆非冗餘** |
| **H3** | ✅ **成立** | 本批 log × 新名單 ⇒ 類 II **0**、類 III **0**（🔒 **但此不構成鑑別力之證據**·見 §4-3） |
| **H4** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`。全檔非路徑差異**恰為**：① 移除之 `[WG96-cov]` 包裝行（前批之覆蓋跑專有）② **本批新增之對帳段** |
| **H5** | ✅ **成立** | **證據 ＝ 本批 log 內之對帳輸出本身**（§5 逐字引錄），⛔ 非「我有掛」之敘述 |

---

## §3 三分類（⛔ 含為空之類亦正面列出）

### 3-1 本批驗收之對帳（`WG97_runall.log` × 新名單）

| 類 | 項數 | 逐項 |
|---|---|---|
| **類 I**（正規化後相同·自動視為同） | **22** | 全部 22 名目；其中 **raw 即相同 14 項**、**經正規化始相同 8 項** |
| **類 II**（僅 traceback 行號不同） | **0** | **（空）**——⛔ 無須具名確認之項 |
| **類 III**（訊息實質不同） | **0** | **（空）** |

### 3-2 鑑別力之靶（`WG94b_runall.log` × **歷史**凍存）

| 類 | 項數 | 逐項 |
|---|---|---|
| **類 I** | **18** | （其中 raw 即相同 10 項） |
| **類 II** | **0** | **（空）** |
| **類 III** | **4** | `v3·StepG0m（結構閘/看守觸發）`／`v3·StepG3.5m（結構閘/看守觸發）`／`W-D.3 碎片`／`W-D.4` |

🔒 **類 II 於兩端皆空** ⇒ 施工單 §6 閘 4 之「逐項具名確認」**本批無受詞**；
⛔ 該欄為空係**量出來的**（對帳器對每一項皆判過），非未查。

---

## §4 正規化規則（⛔ 清單於事前登記已封·事後未增列）

作用域**皆限於 traceback 之 `File "…"` 引號內之路徑 token**，⛔ 不及於訊息其他部分
（避免誤改非路徑用途之反斜線）。

| # | 規則 | ① 非冗餘（實測） | ② 🔴 **這條會讓我看不見什麼** |
|---|---|---|---|
| **R1** | `File "…"` 內 `\` → `/` | 移除後 `H1-b` 之 `(I,II,III)` 由 `(18,0,4)` 變 `(10,0,12)` | **看不見「路徑分隔符本身改變」** ⇒ 無法分辨 log 產於 Windows 或 POSIX。⚠️ 若某日某失敗只在單一 OS 出現，本條會抹掉該線索 |
| **R2** | `File "…"` 內絕對前綴 → 倉相對 | 同上，由 `(18,0,4)` 變 `(10,0,12)` | **看不見「這份 log 產於哪個工作樹／哪個倉」** ⇒ **誤用他倉基準無法由對帳發現**。⚠️ **本倉已有前例**：`W-G.9-4b` 之 `E4` 基準 log 產於 `k6a2-d2b21-wiring-6ef500`。**補償**：名單檔頭載來源 log 之倉相對路徑與 `git hash-object` |

### 4-1 ⛔ traceback 行號**未**列入正規化

其差異一律入**類 II**，由人**逐項具名確認**。
**案由**：行號位移**既可能**無害（`W-G.9-4b` 之 `stepg_pipeline.py 1357→1362`）、
**也可能**是 raise 換了位置（＝真原因改變）——⛔ 二者不可由工具代判。

🔒 對帳器內雖有一個「抹行號」之函式，**其身分為<u>分類器</u>而非正規化規則**：
只用於判斷「差異是否全落在行號」，**⛔ 不寫入名單、不改變所存之原因全文**。

### 4-2 新名單存**原因全文**（⛔ 非雜湊）

`verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt`：22 名目、**154 行原因全文**。
**⛔ 不用雜湊之由**（施工單 §2-2）：雜湊不同時**看不出變在哪**，人無從判斷該不該接受。

### 4-3 🔒 `H3` 為何**不是**鑑別力之證據

以**現況 log 凍結**、再以**現況 log 對帳** ⇒ **必然相符**（恆真）。
⇒ 鑑別力之唯一證據是 **`H1`**（以**真實歷史資料**為靶、且為**雙向閘**）。
此點於事前登記 §4 即已寫死，⛔ 非事後補述。

---

## §5 `H5` 之證據：**本批 log 內之對帳輸出本身**（逐字引錄）

```
【對帳】期望 FAIL 名單：**名目 ＋ 失敗原因**（`verify/wv_reconcile.py`）
  名目：凍存 22／現況 22
  ✅ 名目逐項相同
  類 I  （正規化後相同·自動視為同）＝ 22 項　其中 raw 即相同 14 項
  類 II （僅 traceback 行號不同·⛔ 須逐項具名確認）＝ 0 項　（**空**）
  類 III（訊息實質不同）＝ 0 項　（**空**）
  ✅ **對帳 PASS**——名目與失敗原因皆與凍存相符
```

**自動發生之機制**（⛔ 不得再造「檔在 `verify/` 卻不在任何自動流程內」之洞）：
`run_verification.main()` 之 `results` 為函式內區域變數（`:502`）、且只回 `0/1`
⇒ 對帳器取不到結構化結果物件。故於 `run_all` `[3/3]` 以 **tee**（寫穿真 stdout ＋ 同時累積）
擷取其輸出後餵給對帳器。
⇒ **⛔ 零修改 `run_verification.py`、⛔ 未觸 `results`、⛔ 未新增任何 `results.append`**
⇒ **PASS/FAIL 層不變**（`H4` 已證）。

**`rc` 之處置**：`rc = rc or rc_對帳`。
- **今日可觀測影響 ＝ 零**（本分支為准紅碼、`rc` 本即 `1`）。
- **理由**：若**只印不併**，即新造一道**永遠不能使任何東西變紅**之閘 ＝ 本波所修之病灶本身（考古 65／66）。
- ⚠️ **代價**：未來全綠時，`ALL GREEN` 一行將受對帳左右。**已知並接受**（事前登記 §3-2）。

---

## §6 4 項之**退役**逐項記載（施工單 §2-4）

**退役依據（三項合證）**：

1. 🔒 **名單自身條款**——`…_D2b1切換後.txt` 檔頭逐字：
   「⚠️ 本名單於**覆蓋閘修復後即失效**——本批之紅係**單一阻塞之連鎖**、非各自獨立之問題。」
2. 其所指之根因 `①' 覆蓋閘破` 之出現次數：**入單時 7 → 本批 0**
   （`grep -c "覆蓋閘破" verify/out/D2b1_runall.log` ⇒ `7`；
   　`grep -c "覆蓋閘破" verify/out/WG97_runall.log` ⇒ `0`）。
3. ⇒ 這是**執行該名單自己的條款**，⛔ **不是推翻它**。

| # | 名目 | 入單時之原因 | 現況之原因 |
|---|---|---|---|
| 1 | `v3·StepG0m（結構閘/看守觸發）` | `街廓 R1 抵費地計算失敗：🔴 ①' 覆蓋閘破[R1]：\|union(宗+池) − 街廓\| = 12.0568 > 0.01（union 2881.4963 vs 街廓 2893.5531）` | `🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）` |
| 2 | `v3·StepG3.5m（結構閘/看守觸發）` | `…①' 覆蓋閘破[R1]：… = 12.0553 > 0.01（union 2881.4978 vs 街廓 2893.5531）` | `🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=96.30 ≠ 實跑(階段1)=98.75（Δ=2.446 >0.1）` |
| 3 | `W-D.3 碎片` | `[碎片] compute 失敗：街廓 R1 抵費地計算失敗：🔴 ①' 覆蓋閘破[R1]：… 12.0568 …` | `[碎片] compute 失敗：🔴 結構閘 理論＝實跑 破：… Δ=2.837 …` |
| 4 | `W-D.4` | `[W-D.4] compute 失敗：… ①' 覆蓋閘破[R1] … 12.0568 …` | `[W-D.4] compute 失敗：🔴 結構閘 理論＝實跑 破：… Δ=2.837 …` |

⇒ 新名單所存者為**現況原因**；舊原因即於此**退役**並留檔於本表。
⚠️ **四份舊名單一份未刪**（`902f5d1`／`D1切換後`／`D2切換後`／`D2b1切換後`），僅不再作為驗收基準。

---

## §7 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未修任何既有 FAIL**——含結構閘 `理論＝實跑`、`W-G G.1` 之 `KeyError: '0m'`。
2. 🔴 **洞 A（寄生失聲）未修，且仍在**：現算 —— `results.append` 站點 **107** 處，
   **曾執行 27**、**從未執行 80**（自 `verify/out/WG96_coverage.txt` ＋ AST 現算，⛔ 非憑記憶）。
   ⇒ 本批只修**洞 B**（同色遮蔽）。
3. ⛔ **未立** `G3` 三項（`W-D.3 碎片`／`W-F F.3`／`W-G G.2`）之新閘。
4. ⛔ **未掛** `wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py` 入任何流程。
5. ⛔ **未刪任何舊名單**（四份）、⛔ 未刪 `CLAUDE.md` 任何原句。
6. ⛔ **未量任何閘之恆真性**——已執行之 27 處是否恆真，仍**未涵蓋**。
7. ⛔ **未動生產碼**（`git diff --numstat -- app.py` ⇒ 空輸出）。
8. ⛔ **未代裁** `CLAUDE.md` 原指名之 `902f5d1`（46 名目）與實務所用者之關係——
   本批僅**加註**指向新名單，**原兩行留存**。

---

## §8 ⚠️ 一項具名之副作用：`CLAUDE.md` 行號位移

本批於 `CLAUDE.md:75` 之後**純插入 11 行**（`numstat 11 0`）
⇒ 原行號 **>75** 者一律 **+11**。

```
$ wc -l CLAUDE.md            ⇒ 436（原 425）
$ 受位移影響之引用（行號 >75）⇒ **14 處**
```

**分布**：`CLAUDE.md:207`（4 處：一份報告 ×2、`failure-archaeology/SKILL.md` ×2）、
`:85`（2 處·報告）、`:328`（2 處·報告）、`:290/291/292/327/328/329`（6 處·
**凍存 log** `verify/out/probe_WG93_mp_definition_scope.log`）。

🔒 **本批未改動之**，理由二：
① **凍存 log 與歷史報告描述其產出當時之狀態**，改之即為事後修史；
② 施工單 §6 閘 10 要求「對既有文件之**刪除欄為 0**」，而更新行號錨**必然**修改既有行。
⇒ **具名上呈**，處置請 KL／claude.ai 裁（建議：下一批將**活文件**之錨——
`SKILL.md` 2 處與三份報告 6 處——一次更正；⛔ 凍存 log 不動）。

⚠️ **先例**：施工單所引之 `63bc889` 亦為**就地純插入**（`CLAUDE.md` `numstat 16 0`）
並同樣造成位移 ⇒ 本作法與倉內既有實務一致。

---

## §9 下一批**優先序**（⛔ 只排序·不實作）

承 `W-G.9-6` §8 之排序，扣除本批已完成之序 1（對帳準則），其餘上移：

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | **`W-G G.1` 之上游 `_ctx["0m"]`** | 一崩即使 **69 行**判定式（含 W-8 反向閘）全數失聲；修好上游 ⇒ 一項換回一整片 |
| **2** | **`W-F F.3`（公設地調配 trunk D）之閘** | `W-G.9-6` 六式全 0 命中、**確定無人接手**；死站點含**守恆／位次序**類命題 ⇒ 土地後果直接 |
| **3** | **`W-D.3 碎片` 之閘** | 確定無人接手；碎片三分類直接影響 W-D.4 四梯與遞補 |
| **4** | **`W-G G.2 世代幾何曝出契約` 之閘** | 確定無人接手；惟屬呈現層契約，土地後果較間接 |
| **5** | **掛入三支端到端複本**（`wg_g1_smoke`／`wg_g2_smoke`／`wg_g3`） | 覆蓋率洞同族；`wg_g3` 為 `CLAUDE.md` 收官判準 ① 之工具。⚠️ **掛入必改判定層** ⇒ 須與重烤時點協調 |
| **6** | **`v3·StepG` 之 5 個死站點** | 土地後果最近，但其復活**取決於結構閘另案** ⇒ 不宜先於該另案 |
| **7** | **恆真性之量測**（已執行之 27 處） | 行覆蓋之射程外；需另一種工具（如變異注入） |

⚠️ 序 5／6 之前置均含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §10 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**·雜湊一律 `git hash-object`）

```
$ git hash-object <各產物>
f2520e51e15750a222621a13398486799d4e7295  verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt
9e2742b217a0695c32ff2644e78e67c6b5f50796  verify/out/WG97_歷史凍存_D2b1_名目加原因.txt
2c35e01e2940d50aa7421adc2ba1bde33064abc8  verify/out/WG97_runall.log
109068db48ae39f70ddd60ce663aff77e3454979  verify/out/probe_WG97_rules.log
b833fb0fb8da3c393f4eb59818aa7911dd9c0439  verify/wv_reconcile.py

$ grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt   ⇒ 22
$ grep -c "^    | " verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt     ⇒ 154（原因全文行數）

$ grep -c "覆蓋閘破" verify/out/D2b1_runall.log   ⇒ 7
$ grep -c "覆蓋閘破" verify/out/WG97_runall.log   ⇒ 0

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG96_runall_cov.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG97_runall.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG97_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG97_runall.log ⇒ 22

$ <自 WG96_coverage.txt ＋ AST 現算>
  results.append 站點 107｜曾執行 27｜從未執行 80

$ git diff --numstat -- app.py        ⇒ （空輸出）
$ git diff --numstat -- "*.md"        ⇒ 11  0  CLAUDE.md      （刪除欄 0）
$ git diff --numstat -- verify/run_all.py ⇒ 37  1
```

🔒 **`verify/run_all.py` 之刪除欄 `1` 之具名**：唯一被刪之行為
`    rc2 = v.main()`，其於新碼中作 `        rc2 = v.main()`
——**同一敘述、僅縮排入 `with` 區塊**；⛔ 無任何邏輯被刪。
**文件（`*.md`）之刪除欄為 0。**

⚠️ **一項自誌**：§6 之「重凍前後本體一致」首次比對時，備份指令之 `||` 連鎖短路
致比對之**一端檔案不存在**，`diff` 遂輸出「全部新增」並回 `rc=1`——**假警報**。
補作**判別力自檢**（人造一行差異 ⇒ `rc=1`）後重測，實得 **`rc=0`**。
🔒 同族第三次（見考古節 67）。

---

## §11 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `7287205` — 事前登記（⛔ 零碼、零量測；含已封之正規化規則清單）
- **B** `ad56351` — 對帳器 ＋ 掛入 ＋ 新名單 ＋ 長跑 log（⛔ 零文件結論）
- **C** 本報告 ＋ `CLAUDE.md` 加註 ＋ `GB` 登記 ＋ 考古節 67
