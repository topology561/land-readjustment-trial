# `W-G.9-269R`　本體階　執行報告（**重級批**·🔴 有土地後果）

> **主線期初態** ＝ `3958b92fef0e1f6215405d239dc105e3b63b189d`（`git ls-remote` 實查·⛔ 採信本地追蹤 ref）
> ——係單 `§零` 所載 `0e835f6…` 之**後裔**（`0e835f6 → 280f816 → 54616fd → 3958b92`）✅
> **工項零後** ＝ `8677e96…`（主線·零生產碼·已 push）
> **側支** ＝ `verify/W-G.9-269-p3a`（`p3a` ＋ 證據落檔·**⛔ 推主線**·候 KL 放行）
> **結論** ＝ 工項零 ✅／開工閘 `S-1`〜`S-9` ✅·**`S-10` 🛑 loud 拒測**／`p3a` ✅ 備妥候放行／
> **`p3b` 🛑 停機上呈**（§六 停機款「任一逐檔差異歸因為『其他』」·二獨立理由）／`c1`〜`c4` **未開工**。
> 🛒 **本報告⛔ 載**：`K-9-12` 之任何修法主張；病一之任何修法主張（其屬 `c1`〜`c3`·尚未開工）。

---

## §零　工項零：本補令原封入倉

**受詞** ＝ `docs/orders/W-G.9-269_補令三_本體階施工令.md`

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付·主 checkout） | `424f2882514b0f27b58e4778f514463fac1195e5786bfb6c527f77ea9963b5dc` | `15198` | `0` | `190` |
| 落檔（本 worktree） | **同** | `15198` | `0` | `190` |
| **blob**（`8677e96`） | **同** | `15198` | — | — |

🔒 **三端逐位相同** ✅。落檔以**二進位式**（`rb`／`wb`·⛔ 文字層讀寫·坑 `a`）；`numstat` ＝ `190	0`（`deletions ＝ 0`）。

### `S-0b`　`probe_order_preflight.py`（`rc` **直取** ＝ `0`）

🔴 **機械·停機款 ＝ `0` 項** ✅
🟡 **提示 `2` 項·逐項處置（⛔ 靜默略過）**

| 項 | 受詞 | 處置 |
|---|---|---|
| `P-5` | 本單⛔ 載 `SELF_SHA256` 列 ⇒ ⛔ 可對拍 | **具名豁免**——其傳輸保真已由上表**三端 `sha256` 鏈**承擔（來源／落檔／blob 逐位相同）。器自載「非紅：非所有文件皆須有」 |
| `P-4` `:153` | 含方向箭頭而⛔ 具名座標系 | **具名豁免·偽陽**——`:153` 係 `§七` 之**側支流程圖**（`備妥 → 推側支 → 復驗 → 放行 → 併線`），係**程序之序**、⛔ 方向性轉引之量。`P-4` 屬文字啟發式（**必有偽陽**·⛔ 停機款） |

ℹ️ 記錄 `1` 項：`P-3` — `app.py` 之 `def main` 區間（**AST 實查**）＝ `14917`-`24497`。

---

## §一　開工閘 `S-1`〜`S-10`（逐條當場復現·各載母體與粒度框）

| # | 閘 | 實測 | 判 |
|---|---|---|---|
| `S-1` | 號占用 | `D1 4`／`D2 11`／`D3 5`／雙屬 `1`／**列框 `15`**／**檔框 `7`**（必非零 ✅） | ✅ |
| `S-2` | `ls-remote` ⋀ `left-right` | `3958b92f…`（期初）→ `8677e96…`（工項零後）；`left-right` ＝ `0	0`；`porcelain` 空 | ✅ |
| `S-3` | 生產碼 `34` 檔 blob 全表 | **`34`** 檔（正面列舉）；`app.py` 三值全符 | ✅ |
| `S-4` | `run_verification` 期初實跑 | `24 PASS`／`40 FAIL`／`64` 名目·`RESULT: FAIL`·`rc 1`·**`stderr 0` B** | ✅ |
| `S-5` | `run_all` 期初對帳態 | 見 `§一-4`（**只取值**·⛔ 以「與凍存相同」為通過條件） | ✅ |
| `S-6` | 四簿取號量 | 自誤 `347`／**`354`**／`[106]`；`GB` `159`／**`161`**／`[12, 87]`；`VR` `80`／**`95`**／`[73, 75]`；`K-9` `28`／**`29`**／`[]` | ✅ |
| `S-7` | 硬閘活體生效 | `--selftest` 四造＋三造全成立 **⋀ 活體攔截本窗實測二次** | ✅ |
| `S-8` | 探針定位普查 | 見 `§一-3`（**先於任何實跑**） | ✅ |
| `S-9` | 🔴 觀測模式現查 | `驗_rejected` ＝ **`1`**；`_lg_cols` 寫 `4` 站（`app 2`＋`stepg 2`）／讀 `4`；判別力 寬字樣 `8 > 4` | ✅ |
| `S-10` | 覆蓋帳期初 | 🛑 **loud 拒測（`rc = 4`）**——見 `§一-5` | 🛑 **無從量** |

### 一-1　`S-1`　號占用（六數分列 ＋ 逐筆歸類 ＋ 二對照組）

**三軸**：`(1)` `blob@3958b92…`／`(2)` 母體 ＝ **全 `docs/`** `676` 檔（讀不到 `0`·宣告框補款 `③`）／
`(3)` 粒度框 ＝ **檔框 ＋ 列框**（宣告框 `D1`／`D2`／`D3`·`D3` 取**嚴格式**·補款 `⑤⑧`；列框 ＝ `|D2 ∪ D3|`·補款 `⑦`）。
🔒 **錨定框** `(?<![0-9\-])W-G.9-269(?![0-9])`（補款一）——該框對**帶反引號與不帶反引號二形皆命中**
（反引號非數字亦非連字號），故 `GB-158` 之「二形並取」於本號**已由單一錨定框涵蓋**。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | **列框** | **檔框** | 鬆框（檔／列·⛔ 判準） | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-269`** | `4` | `11` | `5` | `1` | **`15`** | **`7`** | `25`／`97` | 受詢（**必非零** ✅） |
| `W-G.9-268` | `44` | `57` | `15` | `1` | `71` | `46` | `51`／`335` | 對照甲（須 `≥ 1`）✅ |
| ⟨NEG⟩（哨兵·**執行期自倉掃出**·字面⛔ 出艙） | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` | 對照乙（須 `= 0`）✅ |

🔒 **輔檢**：宣告框列 `15` `≤` 鬆框列 `97` ✅（⛔ 以輔檢代主檢·補款 `⑥`）。

🔒 **逐筆歸類（`裁 H`·⛔ 只報數）**——`15` 列**全數**歸類，**無第三方**：

| 類 | 列數 | 逐筆 |
|---|---|---|
| **本單自身之產物** | `11` | `docs/orders/W-G.9-269_施工單_病一_前置階.md:1`／`:36`／`:137`；`…補令一…md:1`；`…補令二…md:1`／`:47`；`docs/reports/W-G.9-269R_前置階.md:1`／`:13`／`:541`／`:570`／`:788` |
| **發單側歷次補令對<u>本批</u>之前瞻引用**（補款 `②`：引用⛔ 構成占用） | `4` | `docs/orders/W-G.9-268p_施工單…md:217`（`§十一 W-G.9-269 之預告`）／`docs/orders/W-G.9-268p_補令十二…md:62`（`排入 W-G.9-269 之前置`）／`docs/reports/W-G.4_泛用阻塞項登記表.md:6042`（`GB-159` 解除條件）／`:6345`（`GB-67` 末端追加） |

⇒ 🔒 **全數白名單類 `1`**（占用者全為本單自身之受詞或其前瞻引用）⇒ **未占用·可用**；`S-1` **未觸停機**。

### 一-2　`S-3`　生產碼 `34` 檔（**當場現查**·⛔ 以任何既有表為比較端·`自誤 334`）

**母體 ＝ 正面列舉** `app.py` ＋ `verify/[^/]+\.py`（⛔ 全倉排除式）⇒ **`34`** 檔。

| 檔 | bytes | **blob `sha1`** | **file `sha256`** |
|---|---|---|---|
| `app.py` | **`1458081`** | **`1d7d7c7324719f13e7d61473a55001170ef44d8e`** | **`b74fd07c3c86a914e602c00aecf90112f543f472bbf11a8b314eb0ce4452f9a4`** |

🔒 **二式並報**（坑 `21`：`app.py` 之 hash **二式並存**·只比一式者必誤停機）。
🔒 餘 `33` 檔之 blob `sha1` 全表落檔於 `verify/out/`（見 `§六`）。
🔒 **`_wg9268p_anchor_advance` 內 `environ` 存取 ＝ `0` 處**（函式 `29` 列·**AST 實查**）✅；
**判別力**：`app.py` 全檔 `environ` ＝ **`3`** 處（須非零，否則該檢恆綠）✅。

### 一-3　`S-8`　探針定位普查（**須先於任何實跑**·坑 `10`／`11`／`12`）

🔒 母體以 `git ls-tree` 取得（`常規補款二`：⛔ `grep -r … .`——該形恆含巢狀 worktree 之倉副本·`GB-35`）。
🔒 框用**裸字樣** `worktrees`（坑 `12`：⛔ 要求其後緊接分隔符）＋ **併查** `os.chdir(`（二族⛔ 互相涵蓋）。

| 框 | 母體 | `worktrees` | `os.chdir(` | 交 | **聯集** |
|---|---|---|---|---|---|
| **甲 `GB-161` 正典框**（`verify/**/*.py`·⛔ `__pycache__`） | `288` 檔 | `10` | `10` | `3` | **`17`** |
| **乙 全倉追蹤 `*.py`** | `293` 檔 | `10` | `12` | `3` | **`19`** |

**三造對照（坑 `13`）**：`(甲)` `import os` ＝ `267` ✅／`(乙)` 哨兵 ＝ `0` ✅／
`(丙)` **必命中之本類已知成員** `verify/probes/probe_WG9268p_c1_synth.py` **在聯集內** ✅ ⇒ **量測器非紅且撈得到本類**。

🔒 **與 `269R 前置階 §三-1` 之差：逐項歸因**（坑 `d`：先對齊母體再判孰誤·⛔ 逕判他方有誤）

| 段 | 量 | 成因（逐支具名） |
|---|---|---|
| 甲框 `21` → **`17`** | −`4` | **恰為前置階 `p2` 所修之 `4` 支**：`wg9268_bing_csv.py`／`wg9268_ingest_verify.py`／`wg9268_nameset_diff.py`／`wg960_z_selfcheck.py` |
| 乙框 `23` → **`19`** | −`4` | 同上 |

**正面覆驗（逐支·⛔ 只看字樣）**——四支現值逐字皆為 `os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`
⇒ 🔒 **`268p §六` 項 `4` 之停機條件（「若其復為字面絕對路徑 ⇒ 該修被回退，停機」）未觸發** ✅。

🔴 **併記（照實）**：`269R 前置階 §三-2` 逐字載 `#1`–`#3` 改前所指之樹 ＝ **`wg9-268-review-window-42a7ad`**
——**即本窗所在之 worktree**。其修已落地 ⇒ 於本窗執行該三支**現指向本樹**，⛔ 再有他樹假綠。

### 一-4　`S-5`　`run_all` 期初對帳態（**拋棄式 clone**·⛔ 以「與凍存相同」為通過條件·**只取值**）

🔒 **clone 之驗**（坑 `18d`）：`git -c core.autocrlf=false clone` → `checkout` **全 `40` 碼** `8677e96…`；
`app.py` **工作區 bytes ＝ blob bytes ＝ `1458081`**（相等 ✅）·`CR 0`·`sha256 b74fd07c…`·`sha1 1d7d7c73…`。
🔒 **坑 `20` 之三項**：`(a)` `RC=1` 已落 ✅／`(b)` 產物時戳晚於啟動 ✅／`(c)` 產物檔數 `> 0` ✅ ⇒ **該跑確已執行**。

| 量 | 期初值 |
|---|---|
| 名目 | 凍存 **`22`** ／ 現況 **`40`** |
| 凍存有而現況無 | **`7`** 項 |
| 現況有而凍存無 | **`24`** 項 |
| **類 I**（正規化後相同·自動視為同） | **`0`** 項 |
| **類 II**（僅 traceback 行號不同·⛔ 須逐項具名確認） | **`4`** 項（`W-F F.1`／`F.2`／`F.3`／`F.4`·皆 `run_verification.py` 之行號） |
| **類 III**（訊息實質不同） | **`11`** 項 |
| 對帳判 | 🔴 **對帳 FAIL** |
| `run_all` | `W-V run_all: FAIL`·`rc 1`·log `192815` B |

🛑 **本節⛔ 作任何「已迴歸／未迴歸」之判**——`S-5` 之受詞係**取期初值以供 `p3b` 後比較**（單 `§一` 逐字）。

### 一-5　🛑 `S-10`　覆蓋帳期初 —— **loud 拒測**（`rc = 4`·`stderr 0` B）

**器** ＝ `verify/probes/probe_WG9268p_mem.py`（S-10 所稱之**記憶體側器**·⛔ `_partial`）。
**其輸出逐字（⛔ 改寫）**：

> ── 選擇器活體檢（`state-selector liveness`·補令十一 `§二-2`）──
> 　旗標 `off`（顯式 `'0'`）⇒ `(1, 1.0)`
> 　旗標 `on` （顯式 `'1'`）⇒ `(1, 1.0)`
> 　🛑 **二態於該量上逐位相同 ⇒ loud 拒測**（⛔ 判綠、⛔ 靜默續跑）
> 　🛑 **二種可能成因**：`(a)` 受測之變更為 **no-op**；`(b)` **態之選擇機制已失效**（如旗標 `WG9268P_ANCHOR_GEOM` 已被 `c3` 移除）。

🔒 **成因已定（機械·⛔ 推定）＝ `(b)`**：`WG9268P_ANCHOR_GEOM` 於**生產碼 `4` 檔命中 ＝ `0`**
（`app.py`／`stepg_pipeline.py`／`run_all.py`／`run_verification.py` 逐檔 `0`）
⇒ 旗標已由 `W-G.9-268′ c3` 移除 ⇒ 該器之二態**結構上同值**。

🔒 **此轉紅係<u>明文預告</u>，⛔ 新破損**——`docs/reports/W-G.9-268pR_補令十二_序1_2_GB161與c1synth定位之修.md:287-288` 逐字：

> 🛑 **`c3` 之後，該七支之活體檢<u>應當轉紅</u>**（旗標消失 ⇒ 二態同值）——
> **其轉紅係預期，⛔ 新破損**；`Q-3` 之受詞為「**⛔ 任一支以『二態逐位相同』判綠**」。

🛑 **⇒ `S-10` 之判 ＝「<u>無從量</u>」，⛔「量得為零」、⛔「受詞紅」**
（`CLAUDE.md`「`序 11` 之一般化」款 `二`〔受詞缺漏須 loud 拒測〕與款 `三`〔「無從回測」⛔ 等同「回測不過」〕；坑 `9`）。
🔒 **該器之行為正確**——其**拒絕判綠**正是 `Q-3` 所欲保證者。

🔴 **其結構性後果（⛔ CC 可裁·候發單側）**：`S-10` 單載「**本閘為 `c2` 之不變量基準**」
⇒ **`c2` 之 `P-1`（覆蓋帳維持歸零）於現態<u>無比較端可取</u>**。
🔒 **比較端之既有值**（`W-G.9-268′ 補令五` 之落檔 `verify/out/WG9268pR_mem.log:49-88`）＝
**`34` 組／逐對總數 `0`／全數 ✅**——惟其產於**旗標仍在**之態，**⛔ 於現態復現**。
🛑 **本報告⛔ 就此作成任何處置**（⛔ 修該器、⛔ 放寬其閘——後者屬 `常規五` 自解款**黑名單**「廢止／放寬任何既有之閘」⇒ 恆停機）。

---

## §二　`p3a`　`_DECLARED_EXTRAS` 之更正（🔴 生產碼·**候 KL 放行**）

**受詞** ＝ `verify/run_verification.py` 之 `_DECLARED_EXTRAS`（`:390`）之 `指數名次` **二則**（`:394`／`:399`），
逐字依 `CLAUDE.md` 之明令（「**九份 baseline 重產（波末）時，須將 `指數名次` 自 `_DECLARED_EXTRAS` 移除**」）。

### 二-1　改動之界（⛔ 動該檔其餘一字·⛔ 動其他 `33` 檔）

| 量 | 值 |
|---|---|
| `numstat` | **`0	3`**（`insertions 0`／`deletions 3`） |
| bytes | `99802` → **`99531`**（`Δ −271`） |
| `file sha256` | `d0d531f7dc051b65ae2e1ed29ec03df2a028fa097094f2182533326ff45b4b52` → `dd5c78cd83961eb46299077c4f1fa01993f0643010f4e923db0bd8021ca56786` |
| `ΔCR`／`ΔLF` | **`0`** ／ `−3`（🔒 **⛔ 行尾污染**·坑 `a` 之攔法：二進位式 `rb`／`replace(bytes)`／`wb`） |
| **反靜默退路計數器** | **咬到 `2` 處**（期 `2`）／**退回 `0` 處**（期 `0`） |
| `py_compile` | `rc = 0`（**直取**） |
| `git status --porcelain` | 僅 `M verify/run_verification.py`（＋未追蹤之證據落檔）⇒ **⛔ 動其他 `33` 檔** ✅ |

### 二-2　驗收 `(a)`

| 項 | 實測 |
|---|---|
| `指數名次` 於該檔命中 | **`2` → `0`** ✅ |
| **判別力**（⛔ 恆為 `0` 之自證） | **父態對照 ＝ `2`** ⇒ 該檢非恆為 `0` ✅ |

### 二-3　驗收 `(b)`：與父態之差異**逐項歸因**

**二態皆 `run_verification` 全跑·分離二流**（⛔ `2>&1`·其對拍不可信）：

| 態 | `stdout` | `stderr` | `rc` | `RESULT` | 綠／紅名目 |
|---|---|---|---|---|---|
| 父（`8677e96`） | `168296` B | **`0`** B | `1` | `FAIL` | `24`／`40` |
| `p3a`（`fe316e6`） | `168296` B | **`0`** B | `1` | `FAIL` | `24`／`40` |

🔒 **綠集合相同 ＝ `True`／紅集合相同 ＝ `True`／新紅 ＝ `[]`／新綠 ＝ `[]`**。

**逐列相異 ＝ `9` 列**（`diff` 之 `<`/`>` 合計 `18` 行 ＝ `9` 對）：

| 類 | 列數 | 逐處 | **歸因** |
|---|---|---|---|
| 「本閘違規總計」 | **`4`** | `92 → 93`（`v3·診斷0m`／`率接線無串聯0m`）；`100 → 101`（`v3·診斷3.5m`／`率接線無串聯3.5m`） | 🔒 **該欄自此被比對（U-K2）** |
| traceback 框行號 | **`5`** | `1071→1068`／`1122→1119`／`1188→1185`／`1255→1252`／`1453→1450` | 🔒 **位移逐處皆 `3` ＝ 本 patch 之刪除列數**（**閉式相等**） |
| **其他** | **`0`** | — | 🔒 **⇒ `§六` 停機款「歸因為其他者」未觸發** ✅ |

🔑 **U-K2 歸因之<u>機械</u>證（⛔ 推定）**
1. `declared_extras_for` 之**消費點恰 `1`**（`verify/run_verification.py:448`·在 `U-K2` 區塊內）——
   ⇒ **`U-K2` 係本 patch 唯一可能影響之處**。
2. `U-K2` 每次 `diff_rows` 呼叫**至多 `append` 一列** violation（其為單一 `f"…"`·⛔ 迴圈）
   ⇒ **每處至多 `+1`**。
3. `_DECLARED_EXTRAS` 之**二鍵** ＝ `W-D.1.2 診斷_退縮{0m,3.5m}.csv`，各被**二個** `diff_rows` 呼叫點消費
   （`:606` `v3·診斷{tag}` → `V3RUN`；`:614` `率接線無串聯{tag}` → `BASELINES`）⇒ **`2 × 2 = 4` 處**。
⇒ 🔒 **`4` 處各 `+1`，與實測逐處相符**；⛔ 其他解釋。

🩸 **併呈（照實·⛔ 隱去）**：該 `+1` violation 之**逐字內容落於 log 之截斷區**
（「…（**另 `81` 列未顯示**·本閘違規總計 `93` 列）」）⇒ **本報告所據者為<u>總計數</u>，⛔ 其逐字**。
其歸因由上開**碼面機械證**承擔，⛔ 由字樣命中承擔。

### 二-4　逐 `commit` 之生產碼判（**逐 `commit`·⛔ 逐批**·`常規一 🔧 二`·**含空輸出**）

指令 ＝ `git diff --name-only <前一 commit>..<本 commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'`

| `commit` | 題 | 該指令之輸出（**逐字**） | 判 |
|---|---|---|---|
| `8677e96` | 工項零（本補令原封入倉） | **（空）** | 零生產碼 ⇒ 已逕推**主線** |
| `fe316e6` | `p3a` | `verify/run_verification.py` | 🔴 **生產碼** ⇒ **側支·候 KL 放行** |
| `305579b` | 證據落檔入倉（`12` 檔） | **（空）** | 零生產碼 ⇒ 側支（`§七`：待併期間亦落側支） |

### 二-5　側支與主線之態

| ref | 值 |
|---|---|
| `refs/heads/wip/s1-endpart`（**主線**） | `8677e9617816ba03c9c9e18e45e32bbda9508279`（**⛔ 前進** ✅·`§七`） |
| `refs/heads/verify/W-G.9-269-p3a`（側支） | `305579bbe00ba0a5c8f6232a84766415a5a5bfed` |

🛑 **候：發單側逐位元組復驗 → 呈 KL → 放行 → 快轉併線。**

---

## §三　🛑 `p3b`　九份 baseline 之重烤 —— **停機上呈**

🛑 **本批⛔ 執行原地覆寫**（`WV_BAKE=BASELINES`）。下列二必答係以**非破壞式捕捉**取得
——`WV_BAKE=<side dir>`（其側 dir 在 scratchpad·**⛔ `verify/baselines`**），即單所引註解逐字之「捕捉用 side dir」。

### 三-1　🔴 必答一（射程）：`WV_BAKE` 之**實際覆寫集**

🔒 **機制之碼面**（`verify/run_verification.py:417-421`）：`WV_BAKE` 掛於 **`diff_rows` 之入口**
⇒ 其覆寫集 ＝ **該次執行所實際到達之每一個 `diff_rows` 呼叫點之 baseline**，**⛔ 九份**。

| 類 | 檔數 | 說明 |
|---|---|---|
| **實際覆寫集（基數）** | **`51`** | ⛔ `9` |
| ┗ **類甲：九份** | **`9`／`9`** | **全數在覆寫集內** ✅（逐檔具名於 `§三-1a`） |
| ┗ **類乙：其餘** | **`42`** | ⛔ 在九份之列**而仍會被覆寫**（`v3/` `14` ＋ `wf/f0` `12` ＋ `wf/f2` `6` ＋ `wf/f3` `8` ＋ `W-D.1.3-a` `2`） |

**判別力對照**：`(甲)` 必在者 `退縮0m參數.csv` ＝ `True` ✅／`(乙)` 哨兵（執行期組出）＝ `False` ✅。

#### 三-1a　類甲：九份之逐檔（清單自 `docs/reports/W-G.9-122_CC交接文.md:87-90` 機械抽出·**本窗自行重得**）

`退縮0m參數.csv`／`退縮3.5m參數.csv`／`第 1 宗街角地指配結果_退縮0m.csv`／`第 1 宗街角地指配結果_退縮3.5m.csv`／
`W-D.1.2 診斷_退縮0m.csv`／`W-D.1.2 診斷_退縮3.5m.csv`／`v3/W-D.1.2 診斷_退縮0m.csv`／
`v3/W-D.1.2 診斷_退縮3.5m.csv`／`W-D.1.3-d 驗收_退縮3.5m.csv`
——**`9`／`9` 皆在覆寫集內**，且**`9`／`9` 皆為倉內追蹤檔且存在** ✅。

### 三-2　🔴 必答二：`W-D.4_四梯分級清單` 是否在覆寫集內

| 檔 | 判 |
|---|---|
| `v3/W-D.4_四梯分級清單_退縮0m.csv` | ✅ **在**覆寫集內 |
| `v3/W-D.4_四梯分級清單_退縮3.5m.csv` | ✅ **在**覆寫集內 |

⇒ 🔒 **判 ＝ `(甲)`**（⛔ `(乙)`）⇒ **`§六` 之「`p3b` 必答二判為 `(乙)`」停機款<u>未</u>觸發**。
🔒 **⇒ 其 `G011`／`G025` 二歸戶之列須於重烤前後並列**——惟該出艙**繫於重烤之執行**，而重烤本身已因 `§三-3` 停機。

### 三-3　🛑 **停機之二獨立理由**（任一即足·`§六`「任一逐檔差異歸因為『其他』」）

#### 理由甲　🔴 **二檔將被覆寫為 `0` B ——<u>資料滅失</u>**

| 檔 | 改前 bytes | 改前 blob `sha1` | **改後 bytes** | 改後 `sha256` |
|---|---|---|---|---|
| `wf/f0/F.0_合併決策_退縮0m.csv` | `947` | `3425308e7c3381c41d62ee38bd14a9aedc6caaf0` | **`0`** | `e3b0c44298fc1c14…`（**空檔之 `sha256`**） |
| `wf/f0/F.0_合併決策_退縮3.5m.csv` | `964` | `20a63e48c7d28a75e20f0475482aec094fb387da` | **`0`** | 同上 |

其**現行內容為真實資料**（首二列逐字）：

```
情境,歸戶,街廓,級別,標的宗,被併宗,Σa(㎡),G(Σa)(㎡),MinA_街廓,達標,去向,Δ非線性(㎡)
0m,G006,R3,級0',628-48(1),628-47(1);628-49(2),627.61,365.84,152.74,✅,達標·留置原位,0.0
```

🔒 **成因**：該二檔之 `diff_rows` 呼叫點**被到達**（故進入覆寫集），惟其 `got_rows` 為**空**
（`F.0` 於現態之 `GSA 錨檢破` 等致其合併決策列未產出）⇒ `_bake_csv` 寫出**空檔**。
🛑 **此歸因⛔ 屬「錨點更正」亦⛔ 屬「`p3a` 之欄被比對」⇒ 歸因為<u>其他</u> ⇒ 停機款成立。**

#### 理由乙　🔴 **`WV_BAKE` <u>改變控制流</u>，其所烤者非「正常跑」之產物**

`WV_BAKE` 於 `diff_rows` 入口即 `return True, []`（**一律回綠·⛔ 比對**）
⇒ 凡以 `diff_rows` 之結果為前提之下游守衛**皆不再中止** ⇒ 管線走得**遠於正常跑**。

| 跑 | 名目 | 綠 | 紅 |
|---|---|---|---|
| 正常跑（`S-4`·父態／`p3a` 態） | **`64`** | `24` | `40` |
| **`WV_BAKE` 捕捉跑** | **`123`** | `105` | `18` |

⇒ 🔒 **`+59` 個名目係「因比對被抑制而多跑出來的」**；其所烤之 `51` 檔**含該 `+59` 段所產之列**。
🛑 **⇒ 「重烤」所寫入者，並非「現行主線正常執行之真值」**——此**⛔ 錨點更正之後果**，
係 `WV_BAKE` 機制自身之性質 ⇒ **歸因為「其他」** ⇒ 停機款成立。

### 三-4　逐檔之改前／改後（`51` 檔全表落檔）

**`51` 檔中：逐位相同 `4` 檔／相異 `47` 檔。**
逐位相同者 ＝ `W-D.1.3-a 診斷_退縮{0m,3.5m}.csv`（`886` B·`ba97d84f…`）／`wf/f3/F.3_轉7-4_退縮{0m,3.5m}.csv`。
🔒 **全 `51` 檔之逐檔 bytes ＋ `file sha256`（改前／改後）** ＝ `verify/out/WG9269R_p3b_bakeset.log`（`10311` B·已入倉）。
🩸 **照實**：其中多檔之 bytes 增幅甚大（如 `wf/f0/F.0_G值_退縮0m.csv` `9089 → 16312`），
**其逐檔歸因本報告⛔ 作成**——因理由乙已使該批數之口徑本身不可採。

🛑 **⇒ `p3b` 停機上呈。⛔ 執行 `WV_BAKE=BASELINES`、⛔ 改任何 `verify/baselines/**` 一字。**

---

## §四　`c1`〜`c4`　**未開工**

🔒 依 `§十` 之序，`c1` 後於 `p3b`。`p3b` 既停機，`c1`〜`c4` **未開工**；
本報告**⛔ 載** `§四` 之差異表（其受詞繫於 `c2`）、**⛔ 載** `§五` 之後置二項。

---

## §五　`§三` 驗收閘之現況

| # | 閘 | 現況 |
|---|---|---|
| `P-0` | **受詞於開工時自行重得，與 `§零-1` 逐宗相符** | ✅ **相符**（見下） |
| `P-1`〜`P-9` | — | **未達其受詞之階段**（`c1`〜`c4` 未開工）⇒ **⛔ 出艙任何判** |

### 五-1　`P-0`：受詞之自行重得（⛔ 採信單 `§零-1` 之表）

**器 ＝ 生產管線自身**（`run_verification`·⛔ 另寫第二份定義）；受詞 ＝ 欄 `驗_B藍影`（`_k923_gate1` 之判）。
**母體**（坑 `h`：CSV 列 ≠ 受詞之母體·須扣抵費地列）：

| 情境 | CSV 列 | 抵費地列 | **配地宗** | 合格 | **不合格** | 不適用 | 無從判定 | 四態以外 |
|---|---|---|---|---|---|---|---|---|
| `0m` | `59` | `13` | **`46`** | `4` | **`2`** | `40` | `0` | **∅** |
| `3.5m` | `60` | `14` | **`46`** | `4` | **`1`** | `41` | `0` | **∅** |

🔒 **恆等式自檢**：`4+2+40+0 = 46` ✅／`4+1+41+0 = 46` ✅ ＝ 配地宗（坑 `h` 之攔法）。
🔒 **四態⛔ 併三**（`_lg_fmt` docstring：「『不適用』與『無從判定』**是兩件事**」）。

| 情境 | 受詞之宗 | 歸戶 | 街廓 | `G(㎡)` | 宗地寬度(m) | 單 `§零-1` 所載 | 判 |
|---|---|---|---|---|---|---|---|
| `0m` | `628-42(1)` | `G011` | `R2` | `0.23` | `0.18` | `0.23`／`0.18` | ✅ **相符** |
| `0m` | `628-53(2)` | `G025` | `R5` | `0.00` | `0.02` | `0.00`／`0.02` | ✅ **相符** |
| `3.5m` | `628-53(2)` | `G025` | `R5` | `0.00` | `0.02` | `0.00`／`0.02` | ✅ **相符** |

⇒ 🔒 **`P-0` 成立**（`§六` 之「`P-0` 受詞相異」停機款**未**觸發）。

---

## §六　🩸 本批之自捕（照實·⛔ 隱去·各載 形／徵候／攔法）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | **heredoc 二度誤帶**（坑 `7′`） | `wg9237_heredoc_guard` **硬阻斷**（⛔ 靜默） | 改以 `Write` 落檔 ＋ **路徑**呼叫；本窗其後全程如此。🔒 **此二次即 `S-7` 之「活體攔截實測」**（⛔ 只 `test -f`） |
| `2` | **`rc` 經管線取得**（坑 `18b`） ＋ **未讀器之 `argv` 契約**（坑 `18c`） | `wg9268_gate5_tally.py \| tail` 得 `rc = 0`，**而該器實為 `IndexError`（缺 `argv[1]`）** ⇒ 二坑**同時現形** | `rc` **直取**（重導向落檔後 `echo $?`）；先讀其 docstring 之 `argv` 契約再跑 |
| `3` | **錨之唯一性命中 `> 1`**（坑 `c`） | 抽坑 `24`／`25` 時以「檔名片段 ＋ `§四`」為框 ⇒ 命中 **`3` 列**；**取首筆即取到空的 stub**（其內容僅「*（見下 `§四-1`）*」） | 改以**坑標題形**（`### 坑 \`24\``）窄化；⛔ 取首筆 |
| `4` | **二數偶然相等，險被讀為「相符」** | `S-8` 首版母體含 `docs/reports/probes/` ⇒ 聯集得 `19`，而 `GB-161` 定稿亦為 `19`（**框相異而數相同**） | 逐框**分列**母體與其基數（`§一-3` 之甲／乙二列）；`§一-3` 逐項歸因，⛔ 以偶合作證 |
| `5` | **`git ls-files` 之 `core.quotepath` CJK 陷阱** | `git ls-files verify/baselines \| grep 四梯` 得 **`0`**（偽 `0`）——CJK 檔名被轉義為八進位 | 改以 `git ls-files -z` ＋ Python 解碼；重查得 **`10`** 檔。**對照組**：`v3/` 下 `68` 檔（須非零）✅ |
| `6` | **違規列被顯示截斷** | 「…（**另 `81` 列未顯示**·本閘違規總計 `93` 列）」⇒ `U-K2` 之逐字 violation **不在可見區** | **照實具名**「本報告所據者為總計數，⛔ 其逐字」，並改以**碼面機械證**承擔歸因（`§二-3` 之三點） |

---

## §七　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | 見 `§七-1`（本報告本身為新增檔） |
| 生產碼 `34` 檔 blob：**工項零 `8677e96` ＝ 期初 `3958b92`** | **相異 `0`／`34`**（該 commit 僅新增一 `.md`；`.py` 異動檔數 ＝ **`0`**）✅ |
| 生產碼 `34` 檔 blob：**`p3a` 後** | 相異 **`1`／`34`** ＝ `verify/run_verification.py`（**即本批之受詞**·`numstat 0	3`） |
| 四簿 MAX | 自誤 `354`／`GB` `161`／`VR` `95`／`K-9` `29` —— **一號未鑄** ✅（`§八` 之授權 ＝ 無） |
| `deletions` | `8677e96` `0`／`305579b` `0`／`fe316e6` **`3`**（＝ `p3a` 之受詞本身·⛔ 逾此） |
| 主線 | **⛔ 前進**（仍 `8677e96`·`§七` 之保護一字不減） |

---

## §八　🛑 呈發單側／KL

### 八-1　候 KL 放行

**`p3a`**（側支 `verify/W-G.9-269-p3a` ＝ `305579b`·其生產碼 commit ＝ `fe316e6`）
——`§二` 之驗收 `(a)`／`(b)` 全過，`§六` 停機款未觸發。

### 八-2　🛑 停機上呈（二項·⛔ CC 可裁）

| # | 項 | 受詞 | 何以⛔ 自解 |
|---|---|---|---|
| `1` | **`p3b` 之停機** | 重烤將 `(甲)` 使 `wf/f0/F.0_合併決策_退縮{0m,3.5m}.csv` **由 `947`／`964` B 變為 `0` B**；`(乙)` 所烤者係 **`WV_BAKE` 抑制比對後多跑 `59` 個名目**之產物 ⇒ 二者皆**歸因為「其他」** | `常規五` 自解款**黑名單**：「動任何一宗地之 `G`／面積／…／配地幾何」（baseline 係 `GB-159` 補償數之所繫）⋀「須**新訂**一條準據」 |
| `2` | **`S-10` 之無從量** | `c2` 之 `P-1`（覆蓋帳維持歸零）**無比較端可取**——記憶體側器之選擇器活體檢因 `c3` 已移除旗標而 loud 拒測 | 其解須**修該器或放寬其閘** ⇒ **黑名單**「廢止／放寬任何既有之閘」⇒ 恆停機 |

🔒 **`2` 之併記**：該器之**拒絕判綠係正確行為**（`Q-3` 之受詞）；所缺者為**一個於單態世界仍成立之覆蓋帳量測管道**。
🔒 **既有比較端之值**（`verify/out/WG9268pR_mem.log:49-88`）＝ `34` 組／逐對總數 `0`／全數 ✅——
**惟其產於旗標仍在之態**，依 `坑 f` 之理路，其**數**是否仍有效**繫於「該器之覆蓋帳計算是否與旗標無關」**，
**本報告⛔ 就此作成判**（其須逐碼確認，而確認之後果將指向修器 ⇒ 黑名單）。

### 八-3　續辦（俟裁）

| 序 | 事 | 狀態 |
|---|---|---|
| `1` | 工項零 ＋ 開工閘 `S-1`〜`S-10` | ✅ 辦畢（`S-10` loud 拒測·已具名） |
| `2` | `p3a`（側支·呈 KL） | ✅ **備妥·候放行** |
| `3` | `p3b` | 🛑 **停機上呈** |
| `4`〜`7` | `c1`／`c2`／`c3`／`c4` | ⬜ **未開工**（後於 `p3b`） |

🔒 **`GB-159` 仍有效**：二宗近乎零之配地，其配地圖與負擔表**⛔ 對外**。
🔒 **`p3b` 完成前**，凡自 baseline 取得之數皆帶舊錨，引之須同格標「**舊錨態·待重烤**」。

*本報告受詞 ＝ CC。本批⛔ 鑄任何號（`§八` 之授權 ＝ 無）；⛔ 改 `verify/baselines/**` 一字；⛔ 推主線。*
