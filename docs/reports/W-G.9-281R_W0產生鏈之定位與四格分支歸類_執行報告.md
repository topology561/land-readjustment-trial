# `W-G.9-281R`　執行報告：態乙 `R4 right` 之 `W_0` **產生鏈**定位 ／ 情形甲 ∧ `R(W_0) > 0` 四格之**分支**歸類

> **單** ＝ `docs/orders/W-G.9-281_施工單_W0產生鏈之定位與四格分支歸類.md`（工項零已原封入倉）
> **分級** ＝ **中**（純量測·**零生產碼**·⛔ 替身·⛔ 反事實）
> **開工態** ＝ `cb4fac535fe27d0c529149735625ac670d9f2a2c`（`ls-remote` 實查·⛔ 本地追蹤 ref）
> 🛑 **本批⛔ 鑄任何號**（`自誤`／`GB`／`VR`／`K-9`／坑 **皆⛔**）。
> 🛑 **出艙即止**：⛔ 判任何側是否違反 `K-9-5-12`、⛔ 判碼註為誤、⛔ 判 `（五）3.` 之效力、
> ⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§零`　開工閘（八項·**全綠**）

三軸：`(1)` 來源 ＝ `blob@cb4fac535fe27d0c529149735625ac670d9f2a2c`（工項零後為 `f24e1d7a…`）／
`(2)` 母體逐項具名於下／`(3)` 粒度框逐項具名於下。

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `cb4fac53…` 或其後裔 | `cb4fac535fe27d0c529149735625ac670d9f2a2c`（**`ls-remote` 實查**·本窗 `HEAD` 同值） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | **逐位相同** | ✅ |
| `3` | `p3a` 非祖先 | 須**非**祖先 | `merge-base --is-ancestor 05a11bd6… HEAD` ⇒ `rc = 1`（非祖先）｜**判別力[必為祖先]** `00db723…` ⇒ `rc = 0` | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `b9ec1b8c887a54e170c95dc734d5b6cae9d34a874db83545bfba1f60e352d050` | 受詞 `18547` B（自檔首至末列 `SELF_SHA256:` 前一 `\n` 含之）⇒ **逐位相符**｜**判別力二造**：全檔同口徑 `19178dfa…` **相異**／受詞多 `1` byte `02dbac04…` **相異** ⇒ 該閘**非恆綠** | ✅ |
| `4'` | **閘器之受詞自證**（坑 `bf`） | 印檔名；[必真] 含 `W-G.9-281` ＝ `True`；[必偽] 含 `W-G.9-280` ＝ `False` | 所量之檔名逐字出艙；`True`／`False` | ✅ |
| `5` | `probe_order_preflight.py <本單>` | `P-3`／`P-5` 不紅 | `rc = 0`·`stderr` `0` B｜🔴 停機款 **`0`** 項／🟡 提示 **`0`** 項／ℹ️ 記錄 `2` 項（`P-5` ✅ 相符；`P-3` ＝ `def main` 區間 `14975`-`24579`） | ✅ |
| `5'` | `--selftest`（⛔ 以「提示 `0`」逕充綠） | `rc = 0`·三靶皆觸·反靶不觸 | `rc = 0`·`stderr` `0` B｜靶① `W-G.9-190` `P-3` **紅** ✅／靶② 同單 `P-4` **提示** ✅／靶③ `W-G.9-191 §六-3` `P-6` **提示** ✅／**反靶** `W-G.9-190R` `P-3` **不紅** ✅ ⇒ 本器**非恆綠亦非恆紅** | ✅ |
| `6` | 四簿（器 ＝ `probe_WG9267_issuer_measurers.py registry`） | 自誤 `372`／`382`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29` | **逐位相符**（`rc = 0`·`stderr` `0` B）；缺號 自誤 `[106, 355, 356, 357]`／`GB` `[12, 87]`／`VR` `[73, 75]`／`K-9` `[]` | ✅ |
| `6'` | 坑（器 ＝ `probe_WG9269_pit_index.py`） | **`85`／`85`**·下一標籤 `bh` | `rc = 0`·`stderr` `0` B·母體 ＝ 追蹤 `.md` **`760`** 檔·**可解析 `85`／`85`**（「不可解析」`0`）·末二標籤 `bf`／`bg` ⇒ 下一 ＝ `bh`｜判別力二造：造甲 坑 `n` 定義處 `6`（期 `≥1`）✅／造乙 人造之坑（**執行期組出**·字面⛔ 出艙）定義 `0`／引用 `0` ✅ | ✅ |
| `7` | 生產碼母體（**正面列舉**） | `34` 檔 | `app.py` `1` ＋ `verify/` **頂層** `.py` **`33`** ＝ **`34`**｜**判別力[必不命中]** pathspec `verify/*.py` 所多咬之**子層** ＝ **`298`**（期 `> 0`·坑 `be`） ✅（逐檔清單見探針落檔） | ✅ |
| `8` | `verify/baselines` | `298` 檔·框 `sha256` `a898f4e1…7ec9`（`quotePath` **預設**） | `298` 檔｜預設口徑 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`｜`quotePath=false` `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`（`自誤 379` 之二口徑並報） | ✅ |

### `零-1`　🟡 preflight 提示之處置

**提示 ＝ `0` 項** ⇒ **無可處置之項**。🛑 依單之明令，本批**⛔ 以「提示 `0`」逕充綠**，
另跑 `--selftest`（見上 `5'`）⇒ `rc = 0`、三靶皆觸、反靶不觸 ⇒ **本器於本單非恆綠**。

### `零-2`　🔴 **本單⛔ 含【驗收】段**（照實登記·**責 ＝ 發單側**·🛑 本批⛔ 鑄號）

現查（母體 ＝ 本單單檔·**列框**）：`【驗收】` 命中 **`0`**；對照組 `§六` ＝ **`4`** ⇒ **量測器非紅**。
⇒ `verify/tools/wg9223_acceptance_audit.py` 於本單之**判定組為空** ⇒ 依
`CLAUDE.md` 之「`序 11` 之一般化」通則 **二**，該器須 **loud 拒測**；其 `rc ≠ 0` 之義為
**「無從回測」，⛔ 等同「回測不過」**（同通則 **三**）。
🛑 依同通則 **一**，**⛔ 由 CC 代擬其【驗收】段**（代擬即使閘之受詞與其守護者同源 ⇒ 判別力歸零）。
🔒 本項**同形於 `自誤 285`**；其歸屬與鑄號**候發單側**。

---

## `§一`　鑄號：**本批⛔ 鑄任何號**

| 簿 | 本批 | 次號（**留空·⛔ 取**） |
|---|---|---|
| `自誤` | **⛔ 鑄** | `383` |
| `GB` | **⛔ 鑄** | `170` |
| `VR` | **⛔ 鑄** | `96` |
| `K-9` | **⛔ 鑄** | `30` |
| 坑 | **⛔ 鑄** | `bh`（`ALPHA_LABELS` **一字未動**） |

---

## `§二`　工項零：本單原封入倉　✅

| 款 | 受詞 | **實測** |
|---|---|---|
| `(1)` | 完整檔名逐字指定下「所指之檔數 ＝ `1`」 | 受詢（本單）＝ **`1`**｜造甲 `W-G.9-280_補令一_舊窗收工批.md` ＝ **`1`**（須 `1`）✅｜造乙 人造名（**執行期組出**·字面⛔ 出艙）＝ **`0`**（須 `0`）✅ |
| `(2)` | `CR` ＝ `0` | 源／磁碟／blob 之 `CR` 皆 **`0`**；`bytes` 皆 **`18625`**；`LF` ＝ **`235`**｜**判別力** 人造真 CRLF（記憶體內·⛔ 落檔）`CR` ＝ `2`（期 `> 0`）✅ ⇒ 該量**非恆 `0`** |
| `(3)` | blob `sha1` ＋ 三方內容 `sha256` 逐位相同 | blob `sha1` ＝ `359c1a5c06232fa9de55b19894ba0f0bcfbbd19e`｜`sha256` 源 ＝ 磁碟 ＝ blob ＝ **`19178dfa0a92b1666db8212ade44ba1f4c1e91a23fcc867d86aac30498520ce2`** ✅｜**判別力** 源之 blob `sha1` **預算**與 `git` 實值相符 ⇒ 該欄⛔ 抄錄 |

`git status --porcelain` 列數 ＝ **`1`**（逐檔 `git add`·⛔ `-A`／⛔ `.`）。

---

## `§三`　工項一（**主檢**）：`W_0` 之加性分解與產生鏈

**器** ＝ `verify/probes/probe_WG9281_w0chain.py`（新建·`rc = 0`·`stderr` **`0`** B）
**落檔** ＝ `verify/out/WG9281R_w0chain.log`（`55015` B／`450` 列·**全量出艙**·⛔ `tail -N` 取結果）

### 三-0　器之結構（單 `§三-2` 所令·坑 `bg` 之攔法）

**包裹 `ns["solve_G_binary"]` 本身**，取其**參數本身**（`baseline_pt`／`side_mid`／
`allocation_dir`／`d_hat`／`is_corner`／`is_chain_head`／`W_prev`／`side_label`），**⛔ climb 外層**。
🔑 **其必要性經本批實測坐實**：`allocation_dir` 於 `_solve_G_one` 內、呼叫 `solve_G_binary` **之前**
已由 `if is_corner: allocation_dir = _first_corner_alloc_dir(side_mid)` **換過**
⇒ 外層之 `allocation_dir_block` 於鏈頭**恰為錯者**（`R4 right` 二格：外層 `[0.7196670499348666, -0.694319333763684]`
vs 所採者 `[-0.6840255720972312, 0.7294580294417599]`）。
🔒 **非參數之符號**（`corner_pt`／`end_pt`／`*_cum_S`／`blk_label`／`actual_max_proj`／`allocation_dir_block`）
仍自呼叫端框內省取回——此係單 `§三-3` 所**明令**（「該式中每一符號於本格之當場值」）。

**自我驗證閘**（⛔ 不過即 loud 拒測）

| 態 | `solve_G_binary` 呼叫次數（`run_step_g` 段） | 鏈頭所錄之 `blk` | 包裹器錯誤 | 判 |
|---|---|---|---|---|
| `0m` 態乙 | `110`（`44`） | `['R1', 'R2', 'R4']` | `0` | ✅ |
| `0m` 態甲 | `149`（`97`） | `['R1', 'R2', 'R3', 'R4', 'R5', 'R6']` | `0` | ✅ |
| `3.5m` 態乙 | `110`（`44`） | `['R1', 'R4']` | `0` | ✅ |
| `3.5m` 態甲 | `149`（`97`） | `['R1', 'R3', 'R4', 'R5', 'R6']` | `0` | ✅ |

判別力[必非零]：四態合計呼叫 **`518`**（期 `> 0` ⇒ 包裹確實生效）✅ ⇒ **器非紅**。

### 三-1　情形甲／乙（源 ＝ `run_corner_pk` 之第三回傳 `_off` 之 `指配` 欄）

| 態 | `_off` 列數 | **乙之格** |
|---|---|---|
| `0m` 態乙 | `0` | （無） |
| `0m` 態甲 | `1` | `[('R4', 'left')]`（`抵費地面積＝range(㎡)` ＝ `116.08`） |
| `3.5m` 態乙 | `3` | `[('R2','left'), ('R3','right'), ('R5','left')]`（`308.17`／`307.79`／`299.73`） |
| `3.5m` 態甲 | `2` | `[('R2','left'), ('R4','left')]`（`308.17`／`226.01`） |

判別力[必命中]：四態之乙格合計 **`6`**｜`_off` 列合計 **`6`** ⇒ ✅（**⛔ 以空集充綠**）。

### 三-2　全母體與分解（**於現母體重量·⛔ 轉引前批**）

**母體之列數 ＝ `48`**（`6` 街廓 × `2` 側 × `2` 情境 × `2` 態）｜**有值格 ＝ `22`**。
🔒 該 `22` 係**本批於現母體之重量**（前批亦得 `22`·二者**獨立同值**）。

分解式（碼面逐行複刻·⛔ 另寫第二份判準）：

```
â         = _n_alloc  若 d̂·_n_alloc ≥ 0，否則 −_n_alloc      （_n_alloc = normalize(allocation_dir)）
W_0       = (baseline_pt − side_mid)·â                        ← 碼面 `_W_near`
T_corner  = (錨 − side_mid)·â
T_adv     = (baseline_pt − 錨)·â
恆等檢     = W_0 − (T_corner + T_adv)
```

🔴 **「錨」有二讀，本批<u>二者並列出艙</u>**（見 `三-3` 之互斥登記）：
**錨甲 ＝ `corner_pt`**（單 `§三-2` 之字面）／**錨乙 ＝ 該呼叫端之實際錨**（左鏈 `corner_pt`／右鏈 `end_pt`）。
**恆等檢之差於二錨、全 `22` 格皆為 `0.0`**（代數上為 telescoping·⛔ 讀為「分解已驗證」）。

### 三-3　`baseline_pt` 之產生式：**逐呼叫端各自取回**（🔴 **二式逐字相異**）

母體 ＝ `verify/stepg_pipeline.py` **單檔**（`111526` B／`1671` 列）·粒度框 ＝ **列框**
框甲（**有 `^` 錨**）＝ `^\s*baseline_pt = ` ⇒ **命中檔數 `1` / 命中列數 `2`**
框乙（**無 `^` 錨·二數並報**）＝ `_solve_one(` ⇒ **命中檔數 `1` / 命中列數 `3`**
🔴 **二框之數相異** ⇒ `_solve_one` 之呼叫端實為 **`3`** 處（列 `821`／`922`／**`937`**），
而 `baseline_pt` 之賦值式僅 **`2`** 式——第 `3` 處（列 `937`）所餵者為 `_try_pt = baseline_pt + _adj * d_hat_rev`
之**再解**（`area_geom < 0.5` 時之調整），**⛔ 另一個 `baseline_pt` 賦值**。

**呼叫端甲 @ 列 `819`（左鏈·列號作佐證·⛔ 作錨）**
```
                baseline_pt = (corner_pt + left_cum_S * d_hat
                               if (d_hat is not None and corner_pt is not None) else None)
                    _is_chain_head=(_lg_idx_left == 0),   # 🆕 W-G.9-261：本側鏈頭
```

**呼叫端乙 @ 列 `920`（右鏈·列號作佐證·⛔ 作錨）**
```
                baseline_pt = (end_pt + right_cum_S * d_hat_rev
                               if (d_hat_rev is not None and end_pt is not None) else None)
                    _is_chain_head=(_lg_idx_right == 0),   # 🆕 W-G.9-261：本側鏈頭
```

🔴 **⇒ 右鏈之錨係 `end_pt`、⛔ `corner_pt`**；且 `end_pt = corner_pt + actual_max_proj * d_hat`、
`d_hat_rev = -d_hat`（字樣錨 `grep -nE '^[[:space:]]*(end_pt|d_hat_rev) = ' verify/stepg_pipeline.py`）。
⇒ 單 `§三-3` 所預告之「**⛔ 以『`corner_pt` ＋ `cum_S`』為既定式**」**經本批實測坐實**。

`(c)` 該式中每一符號於本格之當場值（`repr` 全位）：逐格見落檔 `§三-2` 主表之
`corner_pt`／`end_pt`／`cum_S`／`actual_max_proj` 欄；`R4 right` 二格另見下 `三-4` 第 `4` 項。

🛑 **單內二處之互斥（依 `常規二` 逐字回報·⛔ 自裁·⛔ 改單一字）**
單 `§三-2` 以 **`corner_pt`** 定 `T_corner`／`T_adv`；單 `§三-3` 令**⛔ 以 `corner_pt ＋ cum_S` 為既定式**、
且**⛔ 以其一概括另一**。二者於**右鏈**互斥。
⇒ CC 取**保守項** ＝ **二錨並列出艙**（⛔ 擇一、⛔ 以「兩者實質相同」代替逐字），
其於 `§四` 之歸類後果亦二者並列（見 `§四`）。**歸屬與擇定候發單側。**

### 三-4　`R4 right` 二格之定位（`0m`／`3.5m` **態乙**各一格·二格逐值相同）

暫編地號 ＝ **`628-1(1)`**

| # | 受詞 | **實測** |
|---|---|---|
| `1` | `is_corner` 之實參真值 | **`True`** ⇒ **觸**「街角第 1 宗換軸」之分支（決定式錨 ＝ `grep -n "_first_corner_alloc_dir" app.py`·**列框**·命中檔數 `1`／命中列數 `3`；其唯一決定點 ＝ `if is_corner:` 之次列） |
| `2` | `is_chain_head` ／ `_fo_right` | `True` ／ **`False`** |
| `3` | `allocation_dir` 之**實際來源**（座標系 ＝ 平面直角座標之**單位向量**·⛔ 時序） | `allocation_dir_block` ＝ `[0.7196670499348666, -0.694319333763684]`｜`_first_corner_alloc_dir(side_mid)` ＝ `[-0.6840255720972312, 0.7294580294417599]`｜**所採者**（＝ `solve_G_binary` 之參數）＝ `[-0.6840255720972312, 0.7294580294417599]` ⇒ **`_first_corner_alloc_dir(side_mid)`** |
| `4` | `T_corner`／`T_adv` 及 `T_adv` 式中各因子 | **[錨甲 `corner_pt`]** `T_corner` ＝ `98.89271740492607`／`T_adv` ＝ `-98.71521699782676`｜**[錨乙 `end_pt`]** `T_corner` ＝ `0.17750040709932335`／`T_adv` ＝ **`0.0`**｜因子：`corner_pt` ＝ `[310517.10709, 2651941.1775]`／`end_pt` ＝ `[310584.97277032473, 2651869.489542534]`／`d_hat_rev` ＝ `[-0.6874818007775385, 0.7262016067179091]`／`right_cum_S` ＝ **`0.0`**／`actual_max_proj` ＝ `98.71633001483886`／`baseline_pt` ＝ `[310584.97277032473, 2651869.489542534]` |
| `5` | **當場重得**（⛔ 轉引前批） | **`W_0` ＝ `0.17750040709932335`**｜**`R(W_0)` ＝ `3.088507083528226`** |
| `6` | 三項前提之真值（🛑 **⛔ 由 CC 作歸屬之判**） | 是否 `forced`（該側為強制抵費地）＝ **`False`**（`_fo_right` ＝ `False`）｜是否街角勝者（`is_corner`）＝ **`True`**｜是否鏈頭（`is_chain_head`）＝ **`True`** |

🔒 **產生鏈之逐環（皆當場值·⛔ 推定）**
```
corner_pt        = np.array(FRONT_LINE.p1)                     = [310517.10709, 2651941.1775]
actual_max_proj  = 98.71633001483886
end_pt           = corner_pt + actual_max_proj * d_hat         = [310584.97277032473, 2651869.489542534]
right_cum_S      = 0.0                                          （鏈頭 ⇒ 未推進）
baseline_pt      = end_pt + right_cum_S * d_hat_rev            = end_pt（逐位相同）
side_mid         = SIDE_LINE 之中點（MP）
allocation_dir   = _first_corner_alloc_dir(side_mid)            （因 is_corner ＝ True）
â                = 依 d̂·â ≥ 0 定號之 normalize(allocation_dir)
W_0              = (baseline_pt − side_mid)·â                   = 0.17750040709932335
R(W_0)           = ns["rw_from_width"](W_0)                     = 3.088507083528226
```
🔒 **`baseline_pt` 逐位等於 `end_pt`**（`right_cum_S ＝ 0.0`）⇒ **[錨乙] 之 `T_adv` ＝ `0.0`**
⇒ 於該讀法下 `W_0` **全部**來自 `T_corner`（＝ `end_pt` 相對 `side_mid` 之 â 分量）。
🛑 **⛔ 由 CC 判其落於 `K-9-5-12（四）`、`（五）1.` 抑或二者皆不及**——僅出艙上開三項前提之真值。

### 三-5　碼註**二支**之窮舉檢（**出艙即止**）

碼註錨 ＝ `grep -n "K-9-5-6 W 鏈之唯一決定點" app.py`（**列框**·命中檔數 `1`／命中列數 `1`）。

| 造 | 受詞 | **實測** |
|---|---|---|
| `A` | 有值格中 `is_corner` 為真者，`W_0` **嚴格 `== 0`** 之格數／`!= 0` 之格數 | **`0`** ／ **`22`**（逐格具名見落檔；`22` 格之 `is_corner` **皆為真**） |
| `B` | `is_chain_head` 真 ∧ `is_corner` 偽 ∧ 該側 `_fo` 偽 之格數 | **`0`** ⇒ ⛔ 有「第三支之候選」 |

`A` 之 `!= 0` 者中，`R(W_0) > 0` 者 **`4`** 格；其餘 `18` 格之 `R(W_0)` 皆為 `0.0` 或 `3.34e-09`。
🛑 **出艙即止**：⛔ 判碼註為誤、⛔ 判其「窮舉不成立」、⛔ 提修法主張。

---

## `§四`　工項二：情形甲 ∧ `R(W_0) > 0` 之**分支**歸類

**零判之具名常數** ＝ `TOL0`；判法逐字 ＝ `|x| <= TOL0 ⇒ 判為零`（`TOL0 = 1e-12`）。
**當場重得之成員 ＝ `4` 格**（⛔ 轉引前批之 `4`）——其與單所具名之四格**逐格相同**。

| # | 格 | 暫編地號 | `W_0` | `R(W_0)` | **[錨甲 `corner_pt`]** `(T_corner, T_adv)` ⇒ 支 | **[錨乙 呼叫端之錨]** `(T_corner, T_adv)` ⇒ 支 |
|---|---|---|---|---|---|---|
| `1` | `0m` 態甲 `R2/left` | `628-41(1)` | `1.9195844913610927e-10` | `3.340077014968301e-09` | `(1.9195844913610927e-10, 0.0)` ⇒ **`P`** | 同左 ⇒ **`P`** |
| `2` | `0m` 態乙 `R2/left` | `628-41(1)` | `1.9195844913610927e-10` | `3.340077014968301e-09` | `(1.9195844913610927e-10, 0.0)` ⇒ **`P`** | 同左 ⇒ **`P`** |
| `3` | `0m` 態乙 `R4/right` | `628-1(1)` | `0.17750040709932335` | `3.088507083528226` | `(98.89271740492607, -98.71521699782676)` ⇒ **`R`** | `(0.17750040709932335, 0.0)` ⇒ **`P`** |
| `4` | `3.5m` 態乙 `R4/right` | `628-1(1)` | `0.17750040709932335` | `3.088507083528226` | `(98.89271740492607, -98.71521699782676)` ⇒ **`R`** | `(0.17750040709932335, 0.0)` ⇒ **`P`** |

🔒 **無任何格落於三支之外** ⇒ ⛔ 觸單 `§四` 之 loud 停機款。

🔴 **歸類之結果<u>因錨而異</u>（⛔ 由 CC 擇一）**
- **[錨甲 `corner_pt`]**（單 `§三-2` 之字面）：`4` 格落於 **二支**（`P` × `2`／`R` × `2`）
  ⇒ 依單之「歸類之效力 ＝ **分屬不同支者即分列**」，**「應分列」為機械事實**。
- **[錨乙 呼叫端之錨]**（單 `§三-3` 之明令）：`4` 格**全落於 `P`** ⇒ **⛔ 分列**。

🛑 **⇒ 本批⛔ 作成「應否分列」之判**——其繫於錨之擇定，而錨之擇定係單內二處之互斥（見 `三-3`）。
**照實並列出艙，候發單側裁。**

---

## `§五`　工項三：`K-9-5-12（五）3.` 之倉內現態（**逐字出艙·⛔ 判其效力**）

**落檔** ＝ `verify/out/WG9281R_j5_ruling_state.log`（`rc = 0`·`stderr` `0` B）
三軸：`(1)` 來源 ＝ `blob@f24e1d7a10dac4da352469f91a2d5a833d6922ab`／
`(2)` 母體 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` 單檔（`299931` B／`4125` 列／`CR 0`）
 ＋ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔（`876204` B／`7647` 列／`CR 0`）／`(3)` **列框**。
對照組：[必命中] `K-9-5` ⇒ 檔數 `2`／列數 `245`；[必為零] 人造字樣（**執行期組出**·字面⛔ 出艙）⇒ `0`／`0` ⇒ **器非紅**。

### 款 `1`　`K-9-5-12` 之 `（五）效力` 三項**逐字**（框 ＝ `^### 🔒 K-9-5-12 ` 至下一 `^### ` 前一列·命中列 `1`·節 `54` 列）

```
#### （五）效力

1. **首宗起點一律為 `0`**（即自 **MP** 起量）。
2. `K-9-5-6` 之 `- 🔒 **首宗起點**：`W_0` ＝ MP→corner …` 一行之**前半自本批起失效**
   （該行原句**一字未刪**，已就地加註並指向本節）。
3. ⚠️ 該行**後半**（`forced` 時為其 buffer 起算點）**⛔ 未撤，列為未決**——見該處加註。
```

**其後之加註區塊逐字**（同節 `（七）` 內）：
```
> 🔗 **指路加註（`K-9-5-13`·2026-08-12·⛔ 上文原句一字未刪）**
>
> 上開「後半句之 `forced` 情形」之未決，**已由 `K-9-5-13`（KL 裁定二）失效**
> ——見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。
```

### 款 `2`　`K-9-5-13` **存在**（框甲 `^### 🔒 K-9-5-13 ` 命中列 `1`；框乙 字樣 `K-9-5-13` ⇒ 檔數 `2`／列數 `11`；節 `100` 列）

**`（二）所答　✅ KL 逐字`**
```
> **裁定二**：「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
> **裁定三**：「落位那一邊當然要跟著以起改用真正的地界(第1宗土地是用SIDELINE平移)」
> **裁定四**：「街叫第1宗土地的遠測境界線一定是不同條，因為我以前確實是裁示用"平行ALLOCLINE"方向，但是現在改成街角地的遠糙境界線為平行SIDELINE平移」
> **裁定五**：「第1題：平移的距離，就是 `S1`」
> **裁定六**：「第2題：改成平移之後，「最小寬度帶」一併不需要了」
> **出處之答**：「**記不清**」
```

**`（三）新裁之效力`　逐條逐字**
```
1. **街角第 1 宗之遠側境界線 ＝ SIDELINE 平移 `S1`**（裁定四、五）。
2. **起算一律用真正的地界**；「面積換算出來的替身線」**在側街負擔上不再有角色**（裁定二）。
3. **落位側亦改用真正的地界**（裁定三）。
4. **`§三 最小寬度帶` 一併失效**（裁定六·範圍條件見該節加註）。
5. **`§四 ALLOC_LINE 定位` 之滑動求解隨之失效**。
```
（其上另有 `W-G.9-18` 之**性質更正**引言塊：「🔴 **裁定四〜六 ⛔ 不得記為「新裁」或「變更」——其性質為<u>重申</u>。**」）

### 款 `3`　`失效加註`／`指路加註`（**二框皆無 `^` 錨 ⇒ 二數並報**）

| 框（逐字） | 命中檔數 | 命中列數 |
|---|---|---|
| `失效加註` | **`1`** | **`3`** |
| `指路加註` | **`1`** | **`1`** |

| 所指之節（回溯最近標題） | 所載之效力逐字 |
|---|---|
| `### §三 最小寬度帶（虛擬）` | `> **本節自本批起失效**——KL 裁定六逐字：「第2題：改成平移之後，「最小寬度帶」一併不需要了」。` |
| `### §四 ALLOC_LINE 定位` | `> **本節之構造（平行 ALLOC_LINE 之直線往內滑動、以最小距離為停止條件）自本批起失效。**` ／ `> 新構造：街角第 1 宗之**遠側境界線 ＝ SIDELINE 平移 \`S1\`**（裁定四、五）。` |
| `### 🔒 K-9-5-6 W 鏈定式…（canonical·**逐字**）` | `> 上開「後半句」**自本批起失效**。KL 裁定二逐字：` ／ `> 「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」` |
| `#### （七）⛔ 本節<u>未</u>做之事` | `> 上開「後半句之 \`forced\` 情形」之未決，**已由 \`K-9-5-13\`（KL 裁定二）失效**` |

### 款 `4`　`GB-169` 之**解除條件**（框 `解除條件` 無 `^` 錨 ⇒ 二數並報：檔數 **`1`**／列數 **`27`**；`GB-169` 原節內 **`1`** 列）

```
> **解除條件**：`(1)` `W_0 → 0` 之**土地後果**經量測具名（本單工項一）；`(2)` 其修入生產碼 ＋ **KL 放行**；`(3)` 判別力二造；`(4)` `（五）3.` 之「未決」後半款經 KL 裁定。
```

### 🔴 **倉與交接文之相斥（照實登記·🛑 出艙即止）**

單 `§五` 逐字：「發單側交接文載該『未決後半款』為**待 KL**；倉內是否另有記載，**由本項逐字出艙定之**。
凡交接文與倉相斥，**以倉為準**。」

⇒ **倉內三處逐字**（上開款 `1` 之指路加註、款 `3` 之 `K-9-5-6` 失效加註、款 `2` 之裁定二）
與「待 KL」之記載**相斥**。
🛑 **本批⛔ 判該款是否已了、⛔ 解除或收窄 `GB-169`、⛔ 改 `GB-169` 原節一字、⛔ 追改任何已入倉文件一字。**
**歸屬與後續由發單側於復驗後定之。**

---

## `§六`　判別力**三造**之判（`§三-6`·🛑 逐態各自成立）

| 造 | 受詞 | **實測** | 判 |
|---|---|---|---|
| 甲[必逐位相符] | 還原之 `W_0` vs 碼面之 `W_rw_start_raw`（鏈頭 ⇒ `_rw_start` 即 `_W_near`） | **分母 ＝ 全有值格 `22`**｜該閘之**執行次數 ＝ `22`**｜`repr` 逐位相符 ＝ **`22`** | ✅ |
| 乙[必相異] | 以 `corner_pt` 取代 `baseline_pt` 之同式於 `R4 right` 二格 | `0m`／`3.5m` 態乙：`W_0` ＝ `0.17750040709932335` vs 代以 `corner_pt` ＝ `98.89271740492607` ⇒ **相異** | ✅ |
| 丙[必為零] | `T_adv` 於「`baseline_pt` 與 `corner_pt` 逐位相同」之格（**態甲／態乙各自成立**） | **態甲 `5` 格**（`0m` `R1/left`・`R2/left`・`R5/left`；`3.5m` `R1/left`・`R5/left`）｜**態乙 `5` 格**（`0m` `R1/left`・`R2/left`・`R4/left`；`3.5m` `R1/left`・`R4/left`）｜**皆 `T_adv == 0.0`** ⇒ 二態子母體**皆非空** | ✅ |

🔒 **三造全綠** ⇒ ⛔ 觸單 `§三-6` 之停機款。
🔒 造甲之**執行次數 `22` ＝ 分母 `22`** ⇒ **⛔ 以「未執行」充「已通過」**（開場文 `§三-10`）。

---

## `§七`　🩸 本批之**自捕**（⛔ 頂替·⛔ 鑄號·單 `§八` 所令）

### 自捕 `1`｜**坑 `9` 之重踩**：`\uXXXX` 碼位記錯 ⇒ 情形乙偵測**結構上恆空**

| 項 | 內容 |
|---|---|
| **形** | 探針以 `\uXXXX` 寫 CJK 鍵名，`廓`（`U+5ED3`）誤寫為 `廣` ＝ **`廣`**（`U+5EE3`）⇒ `r.get("街廣")` 恆 `None` ⇒ `b` 為空 ⇒ 條件恆偽 |
| **徵候** | ① 表頭印出「`6` **街廣** × `2` 側」——**唯一之肉眼徵候**；② 情形乙之格於**四態皆得 `0`**，而 `_off` 實有 `1`／`2`／`3` 列 |
| **後果（若未攔下）** | `§四` 之受詞「情形甲 ∧ `R(W_0) > 0`」由 **`4`** 格膨脹為 **`6`** 格（多出 `0m`／`3.5m` 態甲之 `R4/left` 二格·該二格實為**情形乙**）⇒ 整張歸類表錯 |
| **攔法（已施於器內）** | ① **逐替換報「期／實」**並印該字元本身（`廣 U+5EE3` vs `廓 U+5ED3` ⇒ `MISMATCH`）；② 情形乙偵測補**必命中對照**（全態合計須 `> 0`；`_off` 有列而乙格為零 ⇒ **先判量測器紅**、`return 5`）；③ `_off` **原列逐列出艙** |
| **併記** | 落檔工具於寫檔時**將 `\uXXXX` 解碼為字面 CJK** ⇒ 該誤在**檔內**即為字面 `街廣`，`grep "5ee3"` 得 **`0`**——**錯誤之痕跡⛔ 在轉義層、在字面層** |

🛑 本自捕**於出艙前攔下**（首版探針之數**⛔ 出艙**）；🛑 **本批⛔ 鑄坑**（`ALPHA_LABELS` 一字未動）
⇒ 其鑄號**候發單側**。

### 自捕 `2`｜**單內二處互斥**（`§三-2` 之 `corner_pt` vs `§三-3` 之「⛔ 以 `corner_pt ＋ cum_S` 為既定式」）

依 `常規二`：**取保守項**（二錨並列出艙·⛔ 擇一）**＋ 逐字回報**（見 `三-3`／`§四`）；**⛔ 改單一字**。

### 自捕 `3`｜落檔之 `CRLF`（坑 `6` 之變體·**歸正後入倉**）

`python <probe> > <log>` 於本機之重導向使 `stdout` 以**文字模式**寫出 ⇒ 二份 `log` 帶 `CRLF`。
**實測（字元框·⛔ 列框——`grep -c` 於單一長行內恆得 `1`／`0`）**：
`WG9281R_w0chain.log` `CR = 451`／`WG9281R_j5_ruling_state.log` `CR = 218`；
其餘四檔（`.py`／`.md`／二份 `.err`／`.rc`）`CR = 0`。
**處置**：二份 `log` 以**二進位**就地歸正（`CRLF → LF`）⇒ `CR 451 → 0`（`55130 → 54679` B）／
`CR 218 → 0`（`17056 → 16838` B）；**內容一字未改**。
**判別力[必非零]**：人造真 `CRLF`（記憶體內·⛔ 落檔）`CR = 2` ⇒ 該量**非恆 `0`**。
🔒 **⛔ 改 log 之任何一個字元**——所改者僅行尾位元組。

---

## `§八`　一句話推導（驗收固定項）

> 態乙 `R4 right` 之 `W_0 = 0.17750040709932335` **並非**「`corner_pt` ＋ `cum_S`」之產物——
> 其呼叫端（`verify/stepg_pipeline.py` 右鏈）之賦值式逐字為 `baseline_pt = end_pt + right_cum_S * d_hat_rev`，
> 而該格 `right_cum_S = 0.0` ⇒ `baseline_pt` **逐位等於** `end_pt`；
> 又因 `is_corner = True` 使 `allocation_dir` 已換為 `_first_corner_alloc_dir(side_mid)`，
> 故 `W_0 = (end_pt − side_mid)·â = 0.17750040709932335`，`R(W_0) = 3.088507083528226`。

---

## `§九`　🛑 `⛔ 為之事`之逐項自檢

| # | 受詞 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字（含註解） | ✅ 未動（`§十` 收工閘 `6` 之 blob 期初＝期末） |
| `2` | ⛔ 改任何 `*_EXPECT` | ✅ 未改 |
| `3` | ⛔ 修 `GB-167`／`168`／`169` | ✅ 未修 |
| `4` | ⛔ 解除、收窄或結清 `GB-168`／`GB-169` | ✅ 未為 |
| `5` | ⛔ 落地 `K-9-5-12`／`K-9-29` 之任何子項 | ✅ 未為 |
| `6` | ⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 未改 |
| `7` | `GB-162` 三禁令（⛔ `WV_BAKE`／⛔ 覆寫 `verify/baselines`／⛔ 併線 `p3a`） | ✅ 皆守；**⛔ 引任何自 baseline 取得之數** |
| `8` | ⛔ 施替身、⛔ 反事實、⛔ 未經具名之長跑（含 `run_verification`） | ✅ 未為（包裹器**先原樣呼叫**再記錄·⛔ 改回傳值一位元） |
| `9` | ⛔ 鑄任何號、⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未為 |
| `10` | ⛔ 追改任何已入倉文件一字 | ✅ 未為（本批之異動全為**新建檔**） |
| `11` | ⛔ 判違反／⛔ 判碼註為誤／⛔ 判 `（五）3.` 之效力／⛔ 判孰誤／⛔ 提修法主張 | ✅ 未為（`§四`／`§五`／`三-5` 皆**出艙即止**） |
| `12` | ⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ 未為 |
| `13` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ 本報告之 `§十`（收工閘）與 `§十一`（`ls-remote`）皆**入倉後末端追加** |

---

## `§十`　收工閘之實測（**入倉後末端追加**）

> 🛑 本節於本報告入倉**之後**追加——⛔ 預寫。

---

## `§十一`　逐 `commit` 與 `push` 之 `ls-remote` 實查

> 🛑 本節於推送**之後**追加——⛔ 預寫。

---

## `§十`（本體）　收工閘之實測

> 🔒 上二節之標頭係 `commit 2` 所立之**佔位**（其時該數尚未量得）；本節與次節為其**本體**，
> 於 `commit 2`（`85c58147…`）**推送並 `ls-remote` 實查後**方量得，以**嚴格末端追加**寫入（⛔ 改既有一字）。
> 三軸：`(1)` 來源 ＝ `blob@85c581476913dc06cb851e86c3eb9780d4b43330`／`(2)` 母體逐項具名／`(3)` 粒度框逐項具名。

| # | 受詞 | 期 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `numstat` **刪除欄逐檔具名** | 全 `0` | `commit 1`：`+235／−0`（單）｜`commit 2` 七檔：`+396／−0`（報告）・`+0／−0`（`j5 .err`）・`+218／−0`（`j5 .log`）・`+0／−0`（`w0chain .err`）・`+451／−0`（`w0chain .log`）・`+1／−0`（`.rc`）・`+708／−0`（探針）⇒ **刪除欄全 `0`** | ✅ |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間命中 `0`；對照組 `≥ 1` | `commit 1` 變更檔 `1`・∩ `34` 檔 **`0`**・∩ `常規一` 之 `4` 檔 **`0`**｜`commit 2` 變更檔 `7`・∩ `34` 檔 **`0`**・∩ `4` 檔 **`0`**｜**判別力[必非零]** 一含 `app.py` 之歷史區間 `17fd9818` ⇒ ∩ `34` 檔 ＝ **`2`** | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力須 `> 0` | 本批所觸 `.md`／`.py` ＝ **`1`** 檔（`probe_WG9281_w0chain.py`）·`CR` ＝ **`0`**｜**判別力[必非零]** 一已知含 `CR` 之倉內檔 `verify/out/KL_UI_3.5m_2e08a41_stdout.log` ⇒ `CR` ＝ **`2987`** | ✅ |
| `4` | 四簿期末 | 四簿相異與 MAX 皆⛔ 動 | 自誤 `372`／`382`・缺號 `[106, 355, 356, 357]`｜`GB` `167`／`169`・缺號 `[12, 87]`｜`VR` `80`／`95`・缺號 `[73, 75]`｜`K-9` `28`／`29`・缺號 `[]` ⇒ **與 `§零` 閘 `6` 之期初逐位相同** ⇒ 坐實 `§一` 之⛔ 鑄號 | ✅ |
| `5` | 坑之期末 | **`85`／`85`**·下一標籤 `bh`；`ALPHA_LABELS` 一字未動 | `rc = 0`·`stderr` **`0`** B·母體 ＝ `85c5814` 之追蹤 `.md` **`762`** 檔（期初 `760` ＋ 本批新增之 `2` 份 `.md` ⇒ **自洽**）｜可解析 **`85`／`85`**（「不可解析」`0`）·末二標籤 `bf`／`bg` ⇒ **下一 ＝ `bh`**｜判別力二造：造甲 坑 `n` 定義處 `6` ✅／造乙 人造之坑（**執行期組出**·字面⛔ 出艙）定義 `0`／引用 `0` ✅｜`probe_WG9269_pit_index.py` 於 `cb4fac53..85c58147` 之變更檔數 ＝ **`0`** ⇒ `ALPHA_LABELS` **一字未動** | ✅ |
| `6` | 自限復驗 | 相異 `0` 等五款 | `(a)` **`34` 檔 blob 期初（`cb4fac53`）＝期末（`85c58147`）·相異 ＝ `0`**；**判別力[必非零]** 同法於 `17fd9818` ⇒ 相異 ＝ **`2`**｜`(b)` `verify/baselines` **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（口徑 ＝ `core.quotePath` **預設**）／`7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`（`quotePath=false`）｜`(c)` `p3a` **非祖先**（`rc = 1`；**判別力[必為祖先]** `f24e1d7a` ⇒ `rc = 0`）｜`(d)` `WV_BAKE` ＝ `None`｜`(e)` `K-9-29` 任何子項**⛔ 落地**、`GB-167`／`168`／`169` **⛔ 修**——二者皆由 `(a)` 之「`34` 檔 blob 相異 `0`」**機械蘊含**（其落地與修法皆須動該 `34` 檔之一） | ✅ |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit` | 見次節 | ✅（`commit 1`／`2`） |

🔒 **收工閘 `1`〜`6` 全綠**；`7` 見次節。

---

## `§十一`（本體）　逐 `commit` 與 `push` 之 `ls-remote` 實查

| # | `commit` | 內容 | `push` 後 `ls-remote origin refs/heads/wip/s1-endpart`（**實查·⛔ 本地追蹤 ref**） |
|---|---|---|---|
| 基座 | `cb4fac535fe27d0c529149735625ac670d9f2a2c` | `W-G.9-280` 收工（前批） | 開工閘 `1` 實查 ＝ 同值 |
| `1` | `f24e1d7a10dac4da352469f91a2d5a833d6922ab` | `§二` 工項零：本單原封入倉 | **`f24e1d7a10dac4da352469f91a2d5a833d6922ab`** ✅ |
| `2` | `85c581476913dc06cb851e86c3eb9780d4b43330` | `§三`〜`§五` 工項一〜三 ＋ 本報告 `§零`〜`§九` | **`85c581476913dc06cb851e86c3eb9780d4b43330`** ✅ |
| `3` | （本節所在之 `commit`） | `§十`／`§十一` 本體之末端追加 ＋ 收工閘 `5` 之落檔 | 🛑 **其 `ls-remote` 實查於推送<u>之後</u>出艙於聊天**——⛔ 預寫、⛔ 於入倉文件宣稱一件尚未發生之事（單 `§七` 末款） |

🔒 **偏離之逐字回報（承 `W-G.9-194` 裁四之體例）**：`commit 3` 之受詞（收工閘）**結構上必在其自身之 `push` 之前量得**，
故其自身之 `ls-remote` **⛔ 能寫入該 `commit`**；本表以「候聊天出艙」載之——⛔ 留空、⛔ 預寫。

---

## `§十二`　交付清單

| 檔 | 性質 |
|---|---|
| `docs/orders/W-G.9-281_施工單_W0產生鏈之定位與四格分支歸類.md` | 本單原封入倉（`commit 1`） |
| `verify/probes/probe_WG9281_w0chain.py` | 主檢之器（新建·`708` 列） |
| `verify/out/WG9281R_w0chain.log`／`.err`／`.rc` | 主檢之**全量**落檔（`54679` B／`0` B／`rc = 0`） |
| `verify/out/WG9281R_j5_ruling_state.log`／`.err` | `§五` 工項三之逐字出艙（`16838` B／`0` B） |
| `verify/out/WG9281R_pitindex_post.log`／`.err`／`.rc` | 收工閘 `5` 之落檔 |
| `docs/reports/W-G.9-281R_…_執行報告.md` | 本報告 |

---

## `§十三`　🔺 **候發單側裁之項**（⛔ 由 CC 自裁·⛔ 呈 KL）

| # | 項 | 現態 |
|---|---|---|
| `1` | **單 `§三-2`（錨 ＝ `corner_pt`）與 `§三-3`（⛔ 以「`corner_pt` ＋ `cum_S`」為既定式）之互斥** | 已依 `常規二` 取保守項（**二錨並列出艙**）＋ 逐字回報；🔴 **`§四` 之歸類因錨而異**（[錨甲] `P × 2`／`R × 2` ⇒ 分屬**二支**；[錨乙] 全 `P` ⇒ **一支**）⇒ **「應否分列」候裁** |
| `2` | **本單⛔ 含【驗收】段** | `wg9223_acceptance_audit.py` 於本單 `rc = 1` ＝ **「無從回測」**（⛔ 等同「回測不過」）；**⛔ 由 CC 代擬** ⇒ 歸屬與鑄號候裁（同形於 `自誤 285`） |
| `3` | **坑 `9` 之重踩**（本批自捕·見 `§七` 自捕 `1`） | 已於**出艙前**攔下並補三項攔法；🛑 本批⛔ 鑄坑 ⇒ **其鑄號候裁** |
| `4` | **`K-9-5-12（五）3.` 之倉態與交接文相斥** | 倉內**三處逐字**載其「**已由 `K-9-5-13`（KL 裁定二）失效**」，而交接文／`GB-169` 解除條件 `(4)` 載「**待 KL**」⇒ **出艙即止**，歸屬候裁 |
| `5` | **`§三-5` 造 `A`／造 `B` 之數** | `is_corner` 真且 `W_0` 嚴格 `== 0` ＝ **`0`** 格；`!= 0` ＝ **`22`** 格；造 `B` ＝ **`0`** 格 ⇒ **出艙即止**，⛔ 判碼註為誤 |
