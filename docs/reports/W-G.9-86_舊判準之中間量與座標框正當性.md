# `W-G.9-86`　🛑 舊判準何以在平行二界上判違反 ＋ `_PAR_TOL` 之射程 ＋ 二座標框之正當性 ＋ `D-0` 結案

**施工單**：`W-G.9-86`（claude.ai·發單依據 `VR-044` 六）
**基座**：**`8b6165e`**（`origin/wip/s1-endpart`·`W-G.9-85`）——⛔ 無偏離
**性質**：🔒 **只量不修**·⛔ 零 `app.py` 變更·⛔ 不落地／不建遞補介面／不換圖／不重烤·⛔ `data/` 零變更·⛔ `docs/rulings/` 零變更
**探針**：`verify/probes/probe_WG986_oldjudge.py`　**log**：`verify/out/probe_WG986_oldjudge_8b6165e.log`
🔒 **本報告之每一個 ≥4dp 之數皆附其 log 行號**（常設 10）；行號以 `:n` 表之，皆指上開 log。

---

## §0　三句結論

1. 🔴🔴 **舊判準之缺陷⛔ 與「二界是否平行」無關——它比錯了兩個端點**：
   逐宗中間量顯示，甲乙二情形之**起算垂線二端被壓成同一個 `s`**（皆 ＝ `max(s(P_prev), s(B_prev))`），
   於是判準實際在比「**本宗二交點之 `min`**」對「**前一宗二交點之 `max`**」。
   🔒 **恆等式乙（判準之精確形）**：`min(Δfront, Δbase) ≡ min(s(P_cur), s(B_cur)) − max(s(P_prev), s(B_prev))`
   ——逐宗殘差極大 **0.000e+00**（殘差/ulp **0.00**·`:81`）
   ⇒ 🔒 **只要界線有「斜跨距」，本宗須推進<u>至少一整個斜跨距</u>方能過關**（`:74`–`:79`）。
2. 🔴 **該缺陷<u>亦</u>咬受詞內（`P2` ✅）**：`K-9-14` 受詞內之 5 宗中，
   `min(本宗) < max(前宗)` 者 ＝ **3 ／ 5**（`:134`）——即受詞內之 3 宗違反**與受詞外 27 宗同一機制**。
   以正典 `_PAR_TOL = 1.0e-06` 重判得違反 **3** 宗（`R1右4`／`R3右9`／`R5左1`·`:150`–`:154`），
   面積合計 **623.1843 ㎡**（`:155`）⇒ `P3` 之數與名單皆符。
3. 🔴 **`P4` 實得<u>相反</u>·而 `K-6:2426` **未被違反****：`框甲` ＝ `st_frame` 之 `s`（**58 ／ 58** 宗逐位相同·
   全體極大差 **2.842e-14**·`:183`）；`框乙` ＝ **同一座標框施於<u>另一個點</u>**（`:186`–`:189`）
   ⇒ 🔒 **本波⛔ 從未另立座標框**；`-85` §I 之「二框」係**措辭不當**，本批更正（§I-5）。
   ⇒ ⛔ **停機條件（「二框皆非 `st_frame`」）不成立** ⇒ 不停機。

---

## §A　五組擴搜（含出處分類）＋ 🛑 反向檢之逐字覆核 ＋ 停機判定

### A-1　指令與範圍逐字出艙（§一-⑥）

🔒 一律 Python `re`／`str.count`，⛔ **不經 shell 傳字樣**（常設 11·**適用動作 ＝ 讀 ＋ 寫**——
本批之探針、§Z 三塊、本報告**皆以 `Write` 落盤**）。
⛔ **排除式**逐字：`dns[:] = [d for d in dns if d != '__pycache__']`。
母體：`rulings` **1**／`specs` **41**／`figures` **18**／`probes` **127**／`verify/**.py` **183**／
`out` **405**／`reports` **280**。

### A-2　五組之命中（🆕 每一字樣標其**出處分類**·節 109）

| 組 | 【出處分類】 | 字樣 → 命中檔／總次數 |
|---|---|---|
| **⓪** | **【正典款號】**@`docs/rulings/` 全庫 | `K-9-9` **1／24**；`K-9-14` **1／4**；`K-9-10 四` **1／4**；`起算垂線` **1／8**；`_PAR_TOL` **1／1**；`_dir_is_alloc` **1／1** |
| **⓪′** | **【土地行話】**@`rulings`+`specs` | `平行` **17／100**；`容差` **10／39**；`退化` **12／56**；`境界線` **5／98**；`方向` **16／102**；`門檻` **17／99** |
| **⓪″** | **【對方原詞】**@`figures`+`rulings` | `藍色影線` **0／0**；`不能落在` **1／1**；`遞補試算` **0／0**；`P2_temp` **2／3** |
| **①** | **【我方·可】**@`verify/**.py`+`reports` | `st_frame` **6／15**；`座標框` **12／17**；`far_side_dir_and_pt` **8／25**；`faces_of` **13／35**；`取邊` **1／1** |
| **②** | **【碼面】**@`verify/**.py`+`app.py` | `_side_alloc_degenerate` **1／2**；`_guard_auth_scope` **2／5**；`_PAR_TOL` **4／13**；`groups_of` **1／5** |

🔴 **⓪〜⓪″ 之判別力自檢（節 109·⛔ 不可省）**：三組**皆非全 0**；回 0 之字樣逐項具名
（⓪ 無；⓪′ 無；⓪″：`藍色影線`／`遞補試算`）。另掃**已知非零之同類**：
⓪ `K-9-2` **26**／`K-9-5-4` **23**；⓪′ `面積` **452**／`街廓` **541**；⓪″ `宗地分配線` **24**／`BASELINE` **86**
⇒ 🔒 **該現查式非恆 0 ⇒ 上開 0 皆為<u>真 0</u>**。
⚠️ 承 `-85` §A-2 之教訓：`藍色影線`／`遞補試算` 係 **KL 於對話側之口語、倉內從未寫下**
⇒ 🔒 **⛔ 不得據其 0 推論任何事**。

### A-3　命中清單之逐支「已答／未答」（§一-⑦·分層·每層出艙分母）

| 層 | 分母 | 已答 | 未答（具名） |
|---|---:|---|---|
| **正典款號** | 6 | `K-9-9`／`K-9-14`／`K-9-10 四`／`起算垂線`／`_PAR_TOL`／`_dir_is_alloc` **皆現查到定義處**（`_PAR_TOL` 見 §D-1） | — |
| **我方受詞** | 5 | `st_frame`（§E-1）／`座標框`（§E-2）／`far_side_dir_and_pt`（§F-1）／`取邊`（§F-1） | `faces_of`：**13 檔**皆為 `-78`〜`-83` 之界面族·⛔ 非本批受詞 |
| **碼面** | 4 | `_PAR_TOL`（§D-1·**4 檔／13 次**逐處出艙） | `_side_alloc_degenerate`／`_guard_auth_scope`／`groups_of` ⛔ 本批未動 |

### 🛑 A-4　反向檢之逐字覆核（§一-⑨·常設 12 ②·節 108）——**二款仍有效**

| 款 | 逐字命中 | 行號 | 判 |
|---|---:|---:|:--:|
| **`K-6:1010`**「退化判定之容差**與 `_dir_is_alloc` 同源**（`_PAR_TOL = 1e-6`）——⛔ 未另立門檻。」 | **1** | **1010** | ✅ |
| **`K-6:2426`**「該二軸**即倉內現有 `st_frame` 之 `(s, t)` 框**…⇒ **⛔ 不另立座標框**」 | **1** | **2426** | ✅ |

🔒 **節 92 之落實（命中唯一 ≠ 該款仍有效）**：逐字掃 `撤回`／`失效`／`取代`／`已不適用`／`不再適用`／`作廢`，
且其**上下 40 行內**提及 `_PAR_TOL`／`st_frame`／`座標框`／`不另立` 者 ⇒ **風險列 ＝ 5**，逐列判：

| 行 | 風險列逐字（節錄） | 判 |
|---:|---|---|
| `:1034` | 「量測基礎將因 `K-9-5-4 ②` 落地而作廢——本列五格中之**四格須重量**」 | ⛔ 受詞 ＝ **該五格之夾角量測值**·⛔ 非 `_PAR_TOL` 本身 |
| `:1038` | 「『以現況右界量得』，而 `K-9-5-4 ②` 改的正是那條界 ⇒ 量測基礎作廢」 | 同上 |
| `:2098` | 「⛔ 本註不動搖該段之結論：其『⛔ 不需 KL 裁』之撤回**仍然成立**」 | ⛔ 受詞 ＝ 該段之**待裁狀態**·⛔ 非座標框 |
| `:2140` | 「本則與 `K-9-2`／`K-9-3` **互不取代、互不衝突**」 | ⛔ 明文「互不取代」 |
| `:2169` | 「本款末句已失效（`W-G.9-42` 加註）」 | ⛔ 受詞 ＝ **該款末句**·⛔ 非 `:2426` |

⇒ 🔒 **二款皆未被後裁取代 ⇒ ⛔ 不停機**；本單得逕採 `A-2`／`A-3` 之門檻與框。
⚠️ 🔒 **注意 `:1034`／`:1038`**：該二列宣告「**四格須重量**」，而本批 §F-2 所引之 `K-6:1043–1045`
之三個夾角值（`4.321958°`／`4.321968°`／`0.690832°`）**正在該五格之列**
⇒ 🔒 **本報告引之，係作為「該差角<u>早已載明且為真實幾何</u>」之佐證，⛔ 非作為現值**（§F-1 已具名）。

### 🛑 A-5　停機條款之判定：**部分觸發·⛔ 不停機·具名上呈**

**停機條款【單】**：「若已有一支探針**出艙過『舊判準於二界平行時之逐步中間量』** ⇒ 停機上呈」。

現查 @`verify/probes/`+`verify/out/`：`P⊥` **4 檔**／`B⊥` **3**／`起算垂線` **8**／`甲乙判別` **4**／
`中間量` **0**／`逐步` **4**（後者皆 `D-2b` 波之切帶推進·⛔ 非本受詞）。

🔴 **⛔ 不以字樣數作判——逐欄對拍**（錨【倉】`verify/out/probe_WG983_k99prep_bee1abf.log:71` vs 本批 `:31`）：

| | `-83:71` 之欄 | 本批 `:31` 之欄 |
|---|---|---|
| 共有（**9**） | 街廓／側／`i`／情形／起算front／起算base／`本宗P s`／`Δfront`／`Δbase` | 同（`本宗P s` ＝ `s(P_cur)`） |
| `-83` 獨有（3） | `本宗B s`／面積／出艙碼 | — |
| **本批獨有（4）** | — | **`前`（前一宗索引）／`s(P_prev)`／`s(B_prev)`／`斜跨距`** |

⇒ 🔒 **9 ／ 13 欄已於 `-83` 出艙 ⇒ 條款<u>部分觸發</u>**。⛔ 不停機之三由（逐項具名）：
① `-83` 該表之**母體 ＝ 35 宗違反名單**，⛔ **未以「二界平行」界定**（其 `|sin|` 在**另一表**·`-83:52`–`:66`）；
② 缺 `s(P_prev)`／`s(B_prev)`／`斜跨距` 三欄 ⇒ 🔒 **⛔ 無從導出 §C 之恆等式**（缺的正是「號何以為負」所需之量）；
③ `中間量` 字樣全倉 **0 檔**。
⚠️ 🛑 **本判定係 CC 之判、⛔ 非機檢所得** ⇒ **併呈 claude.ai／KL 覆核**；
若判為「應停機」，本批 §C 之產出仍可保留（其為 `-83` 所無之四欄與二恆等式）。

🔒 **⛔ 不得重造之落實**：`w40`／`w82`／`w81`／`w83`／`w84`／`w941` **皆原樣 import**
（`w83`／`w84`／`w85` 之量測邏輯在 `main()` 內⛔ 不可 import ⇒ 逐字具名；本檔 import 其**模組級**符號）。
🔒 **逐位對拍證⛔ 非重造判定式**：本檔自 `P_prev`／`B_prev` 重取之
`(情形, 起算二端, Δ 二值, 出艙碼)` 與 `w40.eval_lot` **全部相同 ＝ 47 ／ 47**（`:26`）。
閘 7-⑤ 機檢：新探針中 `def eval_lot`／`def chord_interval`／`def far_side_dir_and_pt`／
`def analyse_cell`／`def pred_chord`／`def st_frame` 命中皆 **0**；陽性對照 `w40.eval_lot(` 命中 **5**。

### A-6　編號現查 3 列 ＋ 判別力對照（§一-⑧）

| 檔 | 樣式 | 實得 | 期望 | 判 |
|---|---|---:|---|:--:|
| `docs/驗證裁定登記表.md` | ``^## `VR-044` `` ／ ``^## `VR-043` `` | **0** ／ **1** | =0 ／ ≥1 | ✅ |
| `…自誤登記.md` | `^## 自誤 89` ／ `^## 自誤 88` | **0** ／ **1** | =0 ／ ≥1 | ✅ |
| `W-G.4_泛用阻塞項登記表.md` | `錨點更正坐實` ／ `GB-67` | **0** ／ **29** | =0 ／ ≥1 | ✅ |

全倉字樣（`verify/**.py`＋`app.py`＋`docs/**.md`＋`skills`）：`VR-044` **0 檔**／`自誤 89` **0**／`W-G.9-86` **0**
⇒ 🔒 三個新編號皆為**真新**。

---

## §B　§Z 三塊之入倉與自檢

### B-1　前置三道（⛔ 入倉前）

| 塊 | 目標檔 | bytes | LF | CR | sha256 前 16 | 起訖 | 目標檔長 |
|---|---|---:|---:|---:|---|---|---|
| `Z-1` | `docs/驗證裁定登記表.md` | **7986**／7986 ✅ | **104**／104 ✅ | **0** ✅ | `d6cfbf469ee0f00a` ✅ | 起 `LF+"---"+LF+LF+"## "` ✅／收單一 `LF` ✅ | 157760 ＋ 7986 ＝ **165746** ✅ |
| `Z-2` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **2958**／2958 ✅ | **39**／39 ✅ | **0** ✅ | `56796cbb88f3991a` ✅ | 同上 ✅ | 148571 ＋ 2958 ＝ **151529** ✅ |
| `Z-3` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | **3226**／3226 ✅ | **46**／46 ✅ | **0** ✅ | `12c596a11938f25c` ✅ | 同上 ✅ | 345546 ＋ 3226 ＝ **348772** ✅ |

🔒 **控制字元檢之陽性對照**（人工植入 `0x08`）⇒ 命中 **1**（期望 1）：PASS
——⛔ 否則「控制字元 0」係量測器沒跑成功之同義詞（常設 8）。

### B-2　入倉後自檢四道（🔒 全部對 **blob**·⛔ 非工作區）

| 塊 | ① 末 N bytes 逐位 == 片段 | ② blob 檔長 | ④ 定義節標題 | 判 |
|---|:--:|---:|---|:--:|
| `Z-1` | True | **165746** ✅ | ``^## `VR-044` `` → **1** | PASS |
| `Z-2` | True | **151529** ✅ | `^## 自誤 89` → **1** | PASS |
| `Z-3` | True | **348772** ✅ | `^## .*錨點更正坐實` → **1** | PASS |

🔒 **三個新編號之定義節標題合計 ＝ 3**（期望 3）：PASS
③ `numstat` 刪除欄：`+104／−0`（`驗證裁定登記表`）／`+39／−0`（`自誤登記`）／`+46／−0`（`泛用阻塞項登記表`）
⇒ **刪除合計 ＝ 0**；法二 `--shortstat` ⇒ `3 files changed, 189 insertions(+)`；
🔒 **陽性對照** `HEAD~40..HEAD` 刪除 ＝ **307**（須 > 0）：PASS。

🩸 **首版之誤（記入 §I-4）**：`Z-3` 之標題錨我原寫 `錨點更正坐實`（**非行首錨**）⇒ 命中 **2**
（`## 🔧 …` 與其 `### 一、…` 小標各 1）⇒ 判 FAIL；改 `^## .*錨點更正坐實` ⇒ **1** ⇒ PASS。

---

## §C　A-1　舊判準之逐步中間量（27 宗**全部**·⛔ 非抽樣）＋ 受詞內 3 宗之對照

### C-1　母體之界定（⛔ 先界定後出艙）

🔒 母體 ＝ `|sin(前一宗遠側界, 本宗遠側界)| ≤ 1.0e-06`（**正典容差**）⇒ **二界平行**；
其中**出艙碼 ＝ 不合格**者 ＝ **32 宗**（`:30`）。
表末：`POPULATION=42 PRINTED=32 SUPPRESSED=10`（`:64`）
⚠️ 🔒 **本表之結論一律以 `POPULATION` 為分母**（⛔ 表身列數不得作為母體·節 105）。
🔒 **量詞三對齊**：施工單稱「27 宗」係 `-85` 之數；本批以**正典容差**重界後得 **32**——
🔒 **⛔ 非同一母體**（`-85` 用 `1e-9` 得受詞外 27；本批用 `1e-6` 得受詞外不合格 32）·具名。

### C-2　🔴 必答：二界平行時，`Δfront`／`Δbase` 之號何以為負？（逐步指認·⛔ 純算術）

| 步 | 逐字 |
|---|---|
| ① | `P⊥` ＝ `s(B_prev)`、`B⊥` ＝ `s(P_prev)`——**法向投影 ⇒ `s` 不變**（`w40.eval_lot` 逐字） |
| ② | 甲乙判別 ＝ `s(B_prev)` vs `s(P_prev)`；🔴 **二情形之起算二端<u>皆</u> ＝ `max(s(P_prev), s(B_prev))`**<br>（甲：`(s_Pprev, s_Bperp) = (s_Pprev, s_Pprev)`／乙：`(s_Pperp, s_Bprev) = (s_Bprev, s_Bprev)`） |
| ③ | ⇒ 🔒 **起算垂線之二端被壓成<u>同一個 `s`</u>**，而前一宗之遠側界本身跨越 `\|s(P_prev) − s(B_prev)\|`（＝ **斜跨距**） |
| ④ | 本宗之二交點相差 `s(B_cur) − s(P_cur)`（＝ 本宗之斜跨距） |

🔒 **恆等式甲（二情形皆成立）**：`Δbase − Δfront ≡ s(B_cur) − s(P_cur)`
　逐宗殘差極大 ＝ **0.000e+00**（殘差/ulp **0.00**·`:80`）⇒ ✅ 成立
🔒 **恆等式乙（判準之精確形）**：`min(Δfront, Δbase) ≡ min(s(P_cur), s(B_cur)) − max(s(P_prev), s(B_prev))`
　逐宗殘差極大 ＝ **0.000e+00**（殘差/ulp **0.00**·`:81`）⇒ ✅ 成立

⇒ 🔒 **判準實際在比：<u>本宗二交點之 `min`</u> vs <u>前一宗二交點之 `max`</u>**（`:76`）
⇒ **違反 ⟺ `min(本宗二交點) < max(前宗二交點) − 1e-6`**（`:77`）
⇒ 🔴 **⛔ 與二界是否平行無關**——只要界線有斜跨距（`BASELINE ∦ FRONTLINE` 亦使其**隨位置變**），
**本宗須推進「至少一整個斜跨距」方能過關** ⇒ 平行二界之相鄰宗**系統性**被判違反（`:78`–`:79`）。

🩸 **首版之誤（記入 §I-1）**：我原寫「`Δbase − Δfront ≡ −(s(P_prev) − s(B_prev))`」（用**前一宗**之斜跨距）
⇒ 逐宗殘差極大 **4.850e-02**（`:82`）⇒ **不成立**；其因 ＝ `BASELINE ∦ FRONTLINE`
⇒ 斜跨距**隨 `s` 位置而變**，⛔ **二平行線之斜跨距不相等**（`:83`）。

### C-3　🔒 判別力（常設 8·⛔ 不可省）

| 群 | 分母 | `min(本宗) − max(前宗)` |
|---|---:|---|
| **受詞外·已知合格** | **10** | **極小 ＝ 8.084667e-01**（`:121`） |
| **受詞外·不合格** | **32** | **極大 ＝ −4.777013e-01**（`:122`） |

⇒ ✅ **二群完全分離** ⇒ 🔒 **中間量與結論逐項對應**（⛔ 未出現「中間量無差而結論相反」之情形）。
🔒 **節 103（最接近翻面之二筆及其餘裕）**：**−4.777013e-01**（不合格側極大）／**8.084667e-01**（合格側極小）
——🔒 **單一門檻 `0`** 之分離宣稱（常設 14 ①）⇒ ⛔ **無未定義帶**。
🔒 **常設 14 ③（判別力為零者）**：二群之值域無交集、⛔ 無同值筆 ⇒ **判別力為零者 ＝ 0 筆**。

### C-4　🔴 併答（A-0 之受詞）：該機制**是否亦出現於受詞內**？

| 街廓 | 側 | `i` | 情形 | `\|sin\|` | 本宗斜跨距 | 前宗斜跨距 | `min(本)−max(前)` | 出艙碼 |
|---|---|---:|---|---|---|---|---|---|
| R1 | 左 | 1 | 乙 | 9.738e-03 | 2.683093e+00 | 2.358909e+00 | **+1.557194e+01** | 合格 |
| R1 | 右 | 4 | 乙 | 1.206e-02 | 2.681325e+00 | 3.083718e+00 | **−5.478160e+00** | 🔴 不合格 |
| R2 | 左 | 1 | 乙 | 7.536e-02 | 3.179012e+00 | 2.163233e-01 | **+1.418602e+00** | 合格 |
| R3 | 右 | 9 | 甲 | 7.536e-02 | 4.151098e+00 | 7.610639e-01 | **−4.416196e+00** | 🔴 不合格 |
| R5 | 左 | 1 | 甲 | 7.536e-02 | 4.041788e+00 | 7.426365e-01 | **−3.655290e+00** | 🔴 不合格 |

`POPULATION=5 PRINTED=5 SUPPRESSED=0`（`:133`·**全列**）

⇒ 🔒 **`P2` 之判**：受詞內之宗中，`min(本宗) < max(前宗)`（＝ **同一機制**）者 ＝ **3 ／ 5**（`:134`）
⇒ ✅ **同一成因亦出現於受詞內**（`P2` 成立）。
🔒 **A-0 之落實**：本節之切分沿用**正典 `_PAR_TOL = 1e-6`**（⛔ 不取 `-85` 之保守 `1e-9`）
——即 A-0 (乙) 所預選之「使受詞內亦受影響」之解讀·⛔ 未事後改口。

---

## §D　A-2　`_PAR_TOL = 1.0e-06` 之射程現查 ＋ 以之重判（⛔ 不另立門檻）

### D-1　定義處與**全部**使用點（自碼面現查·⛔ 不抄寫）

🔒 **取法**：`_PAR_TOL` 係 `verify/stepg_pipeline.py` 內之**函式區域變數**（⛔ 非 module 級）
⇒ ⛔ 不可 `import` 後取屬性；本檔以 **`ast` ＋ `regex` 二法對拍**自碼面取，**皆得 `1.0e-06`**（`:14`），
且與 `K-6:1010` 逐字之 `1e-6` 相符（`:15`）。

| 檔:行 | 逐字 | 受詞 |
|---|---|---|
| `verify/stepg_pipeline.py:1054` | `_PAR_TOL = 1e-6  # 方向平行判定（\|cross\| of unit vectors）` | **定義處** |
| `verify/stepg_pipeline.py:1065` | `return abs(float(_np_d.cross(_u / _nu, _v / _nv))) <= _PAR_TOL` | `_dir_is_alloc` 之判定式 |
| `verify/stepg_pipeline.py:1144` | 「🔒 **容差與 `_dir_is_alloc` 同源**（`_PAR_TOL`）——⛔ 不另立門檻。」 | 註 |
| `verify/stepg_pipeline.py:1159` | `return (_c <= _PAR_TOL), _c, float(...arcsin(min(1.0, _c)))` | `_side_alloc_degenerate` |
| `verify/stepg_pipeline.py:1182` | 「此斷言**可證偽**，能抓到：`_alloc_dir_used` 來源錯誤／`_PAR_TOL` 與…」 | 註 |
| `verify/stepg_pipeline.py:1196` | `f"（\|cross\|={_cx:.10f}／夾角={_ang:.6f}°　≤ 容差 {_PAR_TOL:g}）"` | 訊息 |
| `app.py:8399`（**2 次**） | 註：「與 `verify/stepg_pipeline.py` 之 `_PAR_TOL`…」 | **註·⛔ 非碼路徑** |
| `verify/probes/probe_D2b12_bridge.py:20/101/111/116` | 容差同源交叉檢查（`re.search(r"_PAR_TOL\s*=\s*…")`） | 探針·**倉內既有之 regex 法** |
| `verify/probes/probe_D2b7_dirscope.py:46` | `PAR_TOL = 1e-6  # ＝ stepg _dir_is_alloc 之 _PAR_TOL（同源·⛔ 不另訂）` | 探針·**先例** |

### D-2　射程之判

判定式逐字 ＝ `abs(cross(u/|u|, v/|v|)) <= _PAR_TOL`（`_dir_is_alloc`·`:1065`）
⇒ **受詞 ＝ 二單位向量之 `|sin|`** ⇒ 🔒 **與本批「前一宗遠側界是否 ∥SIDELINE」所用之量<u>同一</u>** ⇒ **射程涵蓋** ✅
⚠️ ⛔ **不涵蓋者具名**：`_PAR_TOL` 之**原受詞**為「該側 `SIDE` 與 `ALLOC`」／「本宗 `_alloc_dir_used` 與街廓 `ALLOC`」，
⛔ **非**「相鄰二宗之遠側界」——🔒 惟三者皆為「二方向是否平行」之**同一物理量** ⇒ 沿用之·具名（`:147`）。

### D-3　🔴 以 `1.0e-06` 重判（母體 ＝ **47** 宗·＝ `-85` 之母體）

受詞內 ＝ **5**／受詞外 ＝ **42**（`:150`）；**受詞內之違反宗數 ＝ 3**：

| 街廓 | 側 | `i` | 前 | `\|sin\|` | 夾角 | 面積 |
|---|---|---:|---:|---|---|---:|
| R1 | 右 | 4 | 3 | 1.206e-02 | **0.6908°** | **596.2550 ㎡**（`:151`） |
| R3 | 右 | 9 | 8 | 7.536e-02 | **4.3220°** | **25.9425 ㎡**（`:152`） |
| R5 | 左 | 1 | 0 | 7.536e-02 | **4.3220°** | **0.9867 ㎡**（`:153`） |

`POPULATION=3 PRINTED=3 SUPPRESSED=0`（`:154`）／**面積合計 ＝ 623.1843 ㎡**（`:155`）
⇒ 🔒 **`P3` 之判**：實得 **3** 宗、名單 `R1右4`／`R3右9`／`R5左1` ⇒ ✅ **數與名單皆符**（`:156`）。

### D-4　🔒 節 103：門檻上下最接近之二筆及其餘裕

| 側 | 值 | 餘裕 |
|---|---|---|
| 下側極大（判為平行者中最大） | **7.521221e-08** | **9.248e-07** |
| 上側極小（判為不平行者中最小） | **9.737922e-03** | **9.737e-03** |

（皆 `:158`）🔒 **二者之比 ＝ 1.2947e+05**（`:159`）⇒ ✅ **≥ 10 ⇒ ⛔ 非擦邊**。
🔒 **常設 9**：被測量 `|sin|` ∈ `[0, 1]`；`math.ulp(1.0) = 2.220e-16` ⇒ **門檻/ulp ＝ 4.504e+09**（`:21`）
⇒ 門檻遠高於機器解析度（常設 9 之下閘）。
🔒 **常設 14 ①**：本節之分離宣稱以**單一門檻 `1.0e-06`** 表述 ⇒ ⛔ **無未定義帶**
（⛔ 非 `-85` `P4` 之 `1e-9`／`1e-3` 二門檻·`:160`）。

---

## §E　A-3　二座標框之正當性（`K-6:2426`「⛔ 不另立座標框」）

### E-1　`st_frame` 之逐字構成式（錨 `verify/probes/probe_WG941_bandbase.py:87`）

```
d̂ = d_hat / |d_hat| ；  n̂ = (−d̂_y, d̂_x)
(s, t) = ((p − front_pt)·d̂ , (p − front_pt)·n̂)
其後依形心翻號 σ（t < 0 ⇒ 全環 t → −t）
```
🔒 **`σ` 只作用於 `t`，⛔ 不動 `s`**（`:166`）。
🔒 **比對器可用之證（判別力）**：`st_frame` 以二法（原樣 `import` ／ 依定義式重算）取同一格之 `s`
⇒ **極大差 ＝ 0.000e+00**（殘差/ulp **0.00**·`:16`）⇒ ✅ 比對器可用。

### E-2　框甲／框乙 vs `st_frame`（逐宗逐位對拍·⛔ 非字面比較）

🔒 `w40.s_of` 之逐字 ＝ `(pt[0]−origin[0])*d[0] + (pt[1]−origin[1])*d[1]` ＝ `(pt − origin)·d̂`，
且本波各探針之 `origin` **一律 ＝ `cad['front_lines'][lbl]['p1']` ＝ `front_pt`**（`:168`）
⇒ 🔴 **`框甲` ＝ `w40.s_of(點, front_pt, d̂)` ＝ `st_frame` 之 `s`（逐字同式）**（`:169`）。

| 街廓 | 宗 | 頂點數 | 極大差 `\|Δs\|` | 殘差/ulp | 判 |
|---|---:|---:|---|---:|---|
| R1 | 0 | 5 | 4.441e-16 | 0.25 | ✅ 逐位相同 |
| R1 | 1 | 4 | 0.000e+00 | 0.00 | ✅ |
| R1 | 2 | 6 | 1.421e-14 | 1.00 | ✅ |
| R1 | 3 | 4 | 1.421e-14 | 1.00 | ✅ |
| R1 | 4 | 4 | 0.000e+00 | 0.00 | ✅ |
| R2 | 0 | 6 | 1.193e-15 | 1.34 | ✅ |

`POPULATION=59 PRINTED=6 SUPPRESSED=53`（`:180`）
⚠️ 🔒 **本表之結論一律以 `POPULATION` 為分母**（⛔ 表身列數不得作為母體·節 105）。
🔒 **母體之界定（⛔ 不得以 0 為母體·空真假綠）**：可比對之宗 ＝ **58**；`st_frame` `raise` 者 ＝ **1**
（其因 ＝ 該宗與 FRONTLINE **無前緣邊**·⛔ 非本檔之誤·`:182`）。
🔒 `eps_touch` ＝ **生產常數 `ns['_EPS_TOUCH_FRONT']` ＝ 1.000e-02**（⛔ 不另立·同 `w941`／`w947` 之取法·`:172`）。
🔒 **逐位相同 ＝ 58 ／ 58**；**全體之極大差 ＝ 2.842e-14**（`:183`）⇒ ✅

### E-3　🔴 `P4` 之判：**實得相反**

`P4` 預測「**框乙** ＝ `st_frame`；**框甲**為另立」——**實得 `框甲` ＝ `st_frame.s`（58／58）**（`:185`）。

🔒 **`框乙` 之正確定性（⛔ 非「另立座標框」）**：
`框乙(X)` ＝「過 `X` 且 ∥ALLOC 之直線與 FRONTLINE 之交點」之 `s`
＝ **`st_frame.s` 施於<u>另一個點</u>** ⇒ 🔒 **同一座標框、二個<u>受詞點</u>**（`:186`–`:188`）
⇒ ✅ **⛔ 未另立座標框 ⇒ `K-6:2426` 未被違反**（`:189`）。
⇒ ⛔ **停機條件（「框甲與框乙**皆**非 `st_frame`」）不成立** ⇒ **不停機**。

### E-4　🔴 `-79`〜`-85` 各探針**實際所用之框**（逐支現查）

| 探針 | `w40.s_of` | `s_front_of_line` | `strip_axis` | `st_frame` |
|---|---:|---:|---:|---:|
| `w40`（`-40`） | **12** | 0 | 0 | 0 |
| `w81`（`-81`） | 6 | 0 | **7** | 0 |
| `w82`（`-82`） | 2 | 0 | **5** | 0 |
| `w83`（`-83`） | 9 | 0 | **3** | 0 |
| `w84`（`-84`） | 15 | **8** | 3 | 0 |
| `w85`（`-85`） | 8 | **10** | 6 | 0 |

（皆 `:191`–`:196`）⇒ 🔒 **各支之 `s` 皆經 `w40.s_of`（＝ `st_frame.s`）或其派生（`s_front_of_line` ＝ 框乙·同框異點）**
⇒ ⛔ **本波無一支另立座標框**。⚠️ `strip_axis` 係 `-81` 起之**帶軸**受詞，其為 `t` 向之量、⛔ 非另一個 `s` 框。

---

## §F　A-4　`D-0` 二源之結案 ＋ `R1 左 j=0` 之雙重異常

### F-1　A-4-1｜二源之取邊規則（測 `VR-043` 四之假說·母體 ＝ **全體 59 宗**）

🔒 **`|sin|(甲, 乙) > 1e-5` 者逐宗具名**：

| 街廓 | 側 | `i` | `sign` | `w40` 取邊 | `\|sin\|` | 夾角 |
|---|---|---:|---:|---:|---|---|
| R1 | 右 | 3 | **−1** | 0 | 1.206e-02 | **0.6908°**（`:203`） |
| R3 | 右 | 8 | **−1** | 3 | 7.536e-02 | **4.3220°**（`:204`） |

`POPULATION=59 PRINTED=2 SUPPRESSED=57`（`:205`）
⚠️ 🔒 **本表之結論一律以 `POPULATION` 為分母**（節 105）。

🔒 **`P5` 之判**：`sign = −1` 之側（**29** 宗）中 `|sin| > 1e-5` 者 ＝ **2**（期望 ≥1）；
`sign = +1` 之側（**30** 宗）中 `> 1e-5` 者 ＝ **0**（期望 0·`:207`）；
且 `sign = +1` 之側中 `|sin| > _PAR_TOL(1e-06)` 者 ＝ **0**，名單 `[]`（`:208`）
⇒ ✅ **`P5` 假說成立**（`w40` 於 `sign = −1` 之側取到**近**側界）。
🔒 **常設 14 ③（判別力為零者）**：`sign = +1` 側 30 宗**全部**落於 `≤ _PAR_TOL` ⇒
🔒 **該 30 筆對「取邊是否有誤」之判別力為零**（其二源本即重合、⛔ 無從區辨）·具名。

🔴 **`('R2','左',1)` 之另查（施工單令「已知⛔ 不受涵蓋者」）**：其 `sign = +1` 而判定曾翻。
本批 §C-4 已出艙其中間量：`|sin| = 7.536e-02`、`min(本)−max(前) = +1.418602e+00` ⇒ **合格**。
⇒ 🔒 **其翻面⛔ 非取邊所致**，而係 §C-2 之機制（**前宗斜跨距 2.163233e-01 特小** ⇒ 門檻低 ⇒ 過關）
——🔒 **即「同一機制、不同號」**，⛔ 非另一成因·具名。

🔴 **正典之獨立佐證（CC 自補·⛔ 施工單未令）**：`K-6:1043–1045` 逐字載
「須重量者（其側之 `SIDE` 與 `ALLOC` **不平行**）：`0m R3/right`（**4.321958°**）／
`0m R5/left`（**4.321968°**）／`3.5m R1/right`（**0.690832°**）」（`:209`–`:211`）
⇒ 🔒 **`D-0` 之二真差角<u>正典早已載明</u>，且其受詞 ＝ 該側之 `SIDE` 與 `ALLOC` 之夾角**
⇒ 🔴 **該 `|sin|` ⛔ 非「取錯邊」所生之偽差，係<u>真實幾何</u>**（`:212`–`:213`）。
⚠️ **⛔ 二數不得互相驗證**（`wg8-cang-dan-provenance`）：本批之 `4.3220°`（2dp 顯示）與正典之 `4.321958°`
係**同一物理量之二次量測**，其相符係**佐證來源真實**、⛔ **非精度之相互驗證**·具名。
⚠️ 併具名：`K-6:1034`／`:1038` 已宣告該五格「**量測基礎將因 `K-9-5-4 ②` 落地而作廢**」
⇒ 🔒 **本處引之僅為「差角早已載明且為真實幾何」，⛔ 不作為現值**。

### F-2　🔴 A-4-2｜`R1 左 j=0` 之雙重異常 ＋ `P6` 之判

**① 何以「現況本即 0 對」**：`R1` `j=0` 之可算對 ＝ **3**（其中 `d ≥ 2` ＝ 3）：

| 對 | `d` | `s*` | `contains` | `s* ∈ λ`？ |
|---|---:|---|---|---|
| (0,2) | 2 | **−3711.4620** | False | False |
| (0,3) | 3 | **−3371.0484** | False | False |
| (0,4) | 4 | **−7307.2952** | False | False |

弦區間 `λ = [0.000021, 33.244114]`（皆 `:216`–`:220`）
⇒ 🔒 **三對之 `s*` 皆為<u>大負數</u>、遠在 `λ` 之外** ⇒ 交點落在街廓**之後方甚遠** ⇒ 現況即 0 對
⇒ **該格對 `P5` 之判別力 ＝ 0**（⛔ 不論錨點如何改，其重疊恆 0）·具名。

**② `殘差/ulp = 2.9e8` 之量級**（🔒 **`2.9e8`／其餘四格之 `5e3`〜`9e4` 皆為【單】之引數**·⛔ 非本批量測）：
其 `S_req` ＝ **6.4796**、`ulp = 8.882e-16`；
其餘四格之 `S_req` 為 **89.04**／**78.63** 級、`ulp = 1.421e-14`（`:221`）
⇒ 🔒 `ulp(6.4796) = 8.882e-16` vs `ulp(89.0422) = 1.421e-14` ⇒ **比 ＝ 16.0**（`:222`）。

🔒 **`P6` 之判（二異常是否同源）：🔴 不成立 ⇒ 具名為未答**，逐項：

| 問 | 判 |
|---|---|
| ① 之機制 | `s*` 為大負數（最小 **−7307.2952**／最大 **−3371.0484**·`:218`–`:219`）⇒ 落於 `λ` 外 ⇒ 該對**幾何上不相交於街廓內** |
| ② 之機制 | 分母 `ulp` 小 **16.0 倍**（`:222`） |
| 二者是否同源 | 🔴 **⛔ 否**——①係 **`s*` 之值域**、②係 **`ulp` 之分母**，⛔ 二量無共同前提 |
| ② 之殘差為何仍差 3〜4 個數量級【單】 | 🔴 **`ulp` 之 16.0 倍⛔ 不足以解釋**——🔒 **餘量來自<u>殘差之分子</u>，而該分子本批⛔ 未量**（⛔ 不手算 `2.9e8` 與 `9e4` 之比·常設 10） |

⇒ 🛑 **具名為未答**：「`R1 左 j=0` 之 `殘差/ulp` 何以高出其餘四格【單】——扣除 `ulp` 之 **16.0** 倍後之餘量從何而來」
——🔒 **⛔ 不以『分母小』一句吸收之**（常設 11：稱二失敗「同源」須出艙「把共同前提補回去」之對照，本批**做不到** ⇒ 不稱同源）。

---

## §G　`P1`〜`P7` 逐項判 ＋ 閘

### G-1　預測（🔒 逐字保留·⛔ 事後未調整）

| # | 判 | 實得 |
|---|:--:|---|
| `P1` | ✅ | §Z 三塊之 bytes／LF／sha256 **逐塊相符 3／3**（§B-1）；`docs/rulings/` 變更 **0 檔** |
| `P2` | ✅ **成立** | 27 宗之成因**逐宗可指認**（§C-2 之恆等式乙·殘差 0.000e+00）；**同一成因亦出現於受詞內 3／5**（`:134`） |
| `P3` | ✅ **成立** | 以 `_PAR_TOL = 1.0e-06` 重判 ⇒ 違反 **3** 宗、名單 `R1右4`／`R3右9`／`R5左1` **逐字相符**（`:156`）；門檻二側之比 **1.2947e+05** ⇒ ⛔ 非擦邊 |
| `P4` | 🔴 **不成立（實得相反）** | **`框甲` ＝ `st_frame.s`（58／58）**（`:185`）；`框乙` ＝ **同框異點**（`:188`）⇒ ⛔ 二者皆非「另立」⇒ **停機條件不成立** |
| `P5` | ✅ **成立** | `sign = −1` 側 29 宗中 `> 1e-5` 者 **2**；`sign = +1` 側 30 宗中 **0**（`:207`–`:208`） |
| `P6` | 🔴 **不成立** | 二異常**⛔ 不同源**；且 `ulp` 之 **16.0** 倍⛔ 不足以解釋量級差【單】⇒ 🛑 **具名為未答**（§F-2） |
| `P7` | ✅ | `app.py` blob `a9e5671d64d254907a0396f898f046d9d85e8283`·`diff` **0 行**；`docs/rulings/` **0**；`verify/baselines/` **0**；`data/` **0**；`verify/**.py` 變更 **2**；`run_all` **第十二法 ＝ 15**（`:246`） |

⚠️ `P2`〜`P6` 為 claude.ai 之預測——其否證⛔ 不記 CC 之錯（施工單 §四明文）。

### G-2　`run_all` 之**第十二法**（🔒 位元組碼之控制流定位法·⛔ 與十一法不同族）

母體 ＝ `run_all.main` 之**位元組碼指令流**；受詞 ＝ **餵給 `GET_ITER`／`FOR_ITER` 之常數元組**（`:227`）。
🔒 **⛔ 未 `exec`**（僅 `compile`）⇒ ⛔ 未執行 `run_all`（閘 7·`:228`）。
命中之候選元組數 ＝ **1**；其長度 ＝ **[15]**（`:229`）；`POPULATION=15 PRINTED=15 SUPPRESSED=0`（`:245`）。
🔒 **其獨有之判別力已於 `selfcheck` ⑤ 實證**：合成模組（**迭代 2 筆 ＋ 誘餌 3 筆未被迭代**）
⇒ **本法得 2**（期望 2）、**④⑥ 族（掃全部常數）得 3**（期望 3·`:19`）
⇒ 🔒 **二法之受詞確實不同 ⇒ ⛔ 非同族**（`:20`）。

### G-3　閘（**11 項**·序：`1 → 4 → 2 → 2′ → 入倉 → 1′`）

| # | 閘 | 期望 | 實得 | 判 |
|---|---|---|---|:--:|
| 1 | 純追加稽核（入倉前·基座 `8b6165e`） | `rc=0` | `rc=0`·「結論：✅ 全部成立」 | ✅ |
| 4 | `TARGETS` 追加（先現查·只補未在列者） | 只追加 | `numstat` `+7／−0`；`TARGETS` **67 → 68**（相異 68）；🔒 §Z 三塊之目標檔**皆已在列** ⇒ ⛔ 不重複登錄 | ✅ |
| 2 | 變異測試（對象先證在母體內·舊文**行中**插字） | `rc=1`·具名 | ① 對象 ∈ `TARGETS` **True**（68 筆·相異路徑 53）② 變異前 `rc=0` ③ 第 2 行插 `U+24C2`（54 → 57 bytes）④ **`rc=1`**·具名 `docs/驗證裁定登記表.md`（`86367 → 86368`·嚴格前綴 False） | ✅ |
| 2′ | 變異復原 | `diff` 0 行·重跑 `rc=0` | `git diff --numstat` 列數 **0**；重跑 `rc=0` | ✅ |
| 入倉 | §Z 三塊 | — | `+104`／`+39`／`+46`·`git add rc=0` | ✅ |
| 1′ | 純追加稽核（入倉後） | `rc=0` | `rc=0`·「結論：✅ 全部成立」 | ✅ |
| 3 | `numstat` 刪除欄全 0（二法 ＋ 陽性對照） | 全 0 | 法一 刪除合計 **0**；法二 `3 files changed, 189 insertions(+)`；陽性對照 `HEAD~40..HEAD` 刪除 **307** | ✅ |
| 5 | `app.py` blob ＋ `verify/**.py` 變更檔數 ＋ `data/` ＋ 🔒 `docs/rulings/` | `a9e5671…`／2／0／0 | blob `a9e5671d64d2…`（工作區 == `HEAD`）／**2**（`probe_WG986_oldjudge.py`＋`wg942_append_audit.py`·且為 §零 二項之子集）／`data/` **0**／`docs/rulings/` **0**／`verify/baselines/` **0** | ✅ |
| 6 | `py_compile` | 二檔全過 | 二檔 `rc=0` | ✅ |
| 6′ | 位元組層複查·附對照組 | 0 ＋ 對照命中 | 二檔控制字元 **0**；🔒 **陽性對照**（人工 `0x08`）命中 **1** | ✅ |
| 7 | 🔒 `run_all` ⛔ 不跑（逸出條款逐項現查） | 未跑 | ① `app.py` diff **0 行** ② `verify/baselines/` **0** ③ `run_all.py` blob 未變（`fcc7f0111ee4`）④ `docs/rulings/` **0** ⑤ 探針**未重寫** 6 個 `def`（陽性對照 `w40.eval_lot(` 命中 **5**）⑤b `exec(` 命中 **0** | ✅ |
| 8 | `git status --porcelain` 全樹（前後各一次） | 無非預期 | **6 列**：`M` ×3（§Z 三目標檔）／` M` ×1（`wg942_append_audit.py`）／`??` ×2（新探針 ＋ 其 log） | ✅ |

⇒ **11 ／ 11 綠**。

---

## §H　施工單項數對帳（🔒 逐項打勾·量詞三對齊自校）

| 節 | 項數 | 逐項 | 打勾 |
|---|---:|---|:--:|
| **§零** | **2** | ① `wg942_append_audit.py` 之 `TARGETS` 追加 ② 新增 `probe_WG986_oldjudge.py` | ✅✅ |
| **§一** | **9** | ⓪ 正典款號 ／ ⓪′ 土地行話 ／ ⓪″ 對方原詞 ／ ① 我方受詞 ／ ② 碼面 ／ ⑥ 指令逐字 ／ ⑦ 已答未答分層 ／ ⑧ 編號現查 3 列 ／ ⑨ 反向檢逐字覆核 | ✅×9（§A-1〜A-6） |
| **§二** | **6** | A-0 錯誤方向事前選定 ／ A-1 舊判準中間量 ／ A-2 `_PAR_TOL` 射程 ／ A-3 座標框 ／ A-4 `D-0`＋`R1左j0` ／ A-5 不得做六款 | ✅×6（§C／§D／§E／§F） |
| **§三** | **6** | 🔒 **本報告採之讀法**：`Z-1` 入倉／`Z-2` 入倉／`Z-3` 入倉／前置① 編號現查／前置② 目標檔 bytes 現查／前置③ 片段 `CR`＝0 —— ⛔ 「入倉後自檢四道」另計於 §B-2（⛔ 不重複計數） | ✅×6（§B） |
| **§四** | **7** | `P1`〜`P7` | ✅×7（§G-1） |
| **§五** | **11** | 閘 1／4／2／2′／入倉／1′／3／5／6／6′／7／8 —— 🔒 **施工單表列 11 行、含「入倉」一行**；本報告 §G-3 表**列 12 行**（多出之 1 行 ＝ 「入倉」，⛔ 非新增閘） | ✅×11（§G-3） |
| **§六** | **13** | `§0`／`§A`／`§B`／`§C`／`§D`／`§E`／`§F`／`§G`／`§H`／`§I`／`§J`／`§K`／`§L` | ✅×13（本檔） |
| **§七** | **14** | 1 零 `app.py`／2 只量不修／3 不重烤／4 不擬合／5 不改編號／6 不另立門檻與框／7 表頭可逐筆指認／8〜14 常設七條 | ✅×14（§G-3 閘 5／7 ＋ 全文） |
| **合計** | **68** | — | **68 ／ 68** |

🔒 **量詞三對齊自校**（`grep-exhaustiveness-pipeline` 第三維度）：
① **結論句 N** ＝ 68；② **表列數** ＝ 2+9+6+6+7+11+13+14 ＝ **68**；③ **裸列舉母體** ＝ 上表「逐項」欄逐一列名者 ＝ **68**
⇒ ✅ **三者一致**。⚠️ 🔒 **二處讀法已具名**（§三之 6 之切分／§五之 11 含「入倉」行）——
⛔ 若 claude.ai 之原意不同，請以其為準並回退本節之切分。

---

## §I　CC 本批之自誤（**5 件**·🔒 逐件附其被抓到之機制）

| # | 自誤 | 抓到它的是什麼 | 修法 |
|---|---|---|---|
| **1** | **恆等式之受詞取錯**：原寫 `Δbase − Δfront ≡ −(s(P_prev) − s(B_prev))`（用**前一宗**之斜跨距）⇒ 逐宗殘差極大 **4.850e-02**（`:82`） | 🔒 **「應為零之量一律 `%.3e`」**（§七-7）——`4.850e-02` 以 `%.3e` 印出方顯其非零；若以 `%.3f` 印出，⛔ 極易被讀成「≈0」 | 改為**恆等式甲／乙**（受詞 ＝ **本宗**之斜跨距）⇒ 殘差 **0.000e+00**·`BASELINE ∦ FRONTLINE` ⇒ 斜跨距**隨位置變** |
| **2** | **A-3 之空真假綠**：首版 `eps_touch = 1e-9` ⇒ **6 街廓全 `raise`** ⇒ 印出「**0 ／ 0 逐位相同**」 | 🔒 **常設 10 之「⛔ 不得以 0 為母體」** ＋ `POPULATION=` 行 | 改用**生產常數** `ns['_EPS_TOUCH_FRONT'] = 1.000e-02`、母體改**全 59 宗** ⇒ **58 ／ 58**·`raise` 者 **1** 具名 |
| **3** | **`_PAR_TOL` 之取法**：首版寫 `sgp._PAR_TOL` ⇒ `AttributeError`——它是**函式區域變數**、⛔ 非 module 級 | 執行時當場 `AttributeError`（⛔ 非我事先查出） | 改 **`ast` ＋ `regex` 二法自碼面取**（後者即 `probe_D2b12_bridge.py:101` 之**倉內既有法**）⇒ 二法皆 `1.0e-06` |
| **4** | **`Z-3` 之標題錨非唯一**：`錨點更正坐實` 命中 **2**（`## 🔧 …` 與其 `### 一、…` 各 1）⇒ 自檢判 FAIL | 入倉後自檢④ 之**逐塊計數**（⛔ 非我目視） | 改 `^## .*錨點更正坐實` ⇒ **1** ⇒ PASS |
| **5** | **`-85` §I 之措辭不當**（本批更正·⛔ 非本批新犯）：稱框甲／框乙為「**二框**」 | 本批 A-3 之逐位對拍（58／58） | 🔒 正確表述 ＝ **同一座標框（`st_frame`）、二個<u>受詞點</u>** ⇒ `K-6:2426` **未被違反** |

🔒 **五件之共通形狀**：**①③ ＝ 「我以為某符號是什麼」與碼面不符**；**②④ ＝ 「量測器回了一個數，而那個數不是我要的量」**。
⇒ 🔒 **本批之家法**：**凡取生產碼之常數，一律自碼面二法對拍**（⛔ 不 `import` 屬性、⛔ 不抄寫）；
**凡以樣式計數，一律先驗其在<u>已知母體</u>上之命中數**（⛔ 不逕用）。

---

## §J　未涵蓋之事（⛔ 逐項具名·⛔ 不以「未涵蓋」吸收漏辦項）

1. 🔒 **情境母體僅 `0m`**（`:8`）——⛔ **未擴 `3.5m`**。理由：`-79`〜`-85` 全線以 `0m` 為母體，擴充須換錨·⛔ 非本單受詞。
2. 🛑 **`P6` 之②：殘差**分子**未量**（§F-2）——`ulp` 之 **16.0** 倍⛔ 不足以解釋【單】所載之量級差·**未答**。
3. ⚠️ **§A-5 停機條款之「部分觸發」係 CC 之判、⛔ 非機檢**——**併呈 claude.ai／KL 覆核**。
4. **左側三組跳號之機制**（承 `-85` §未結 4）——本批⛔ 未再查。
5. **`VR-043` 四之 `22 ／ 22` 簽名成因**（承 `-85` §未結 3）——本批 A-4-1 只證**取邊假說成立**，⛔ 未解釋簽名。
6. **`S_req` 之<u>面積</u>後果**（承 `-84`）——須解 `S`·⛔ 本批未量。
7. **`GB-67` 之修法**⛔ 未擬（屬 `K-6-A2`·須五段式呈 KL）——🔒 本單明文「⛔ 只量不修、⛔ 不下『這是 bug』之結論、⛔ 不擬正典條文」。
8. **`faces_of` 之 13 檔**（§A-3）⛔ 非本批受詞·未逐支追。

---

## §K　波末佇列現況（錨【倉】`docs/reports/W-G.4_泛用阻塞項登記表.md:1868` 起之更正表）

| # | 項 | 現況（🔒 本批之增量） |
|---|---|---|
| **0** | **`GB-67`：`②-宗` 破量之成因** | 🔴 **機制已封閉**（`-82` 弦區間閉式 ＋ `-85` 錨點更正 ＋ **本批之恆等式乙**）；🔒 **剩「修法未擬」**（§J-7） |
| 1 | `K-8` 遷移項**乙** | 要件 1／2 ✅；要件 3 與**重烤同批** ⇒ 仍繫於第 0 項之**落地**（⛔ 非其成因） |
| 2 | `K-8` 遷移項**甲**（守恆殘差上界模型） | ⛔ 未辦 |
| 3 | 換圖 `V6_1` ＋ 換快照 **同一次**重烤 | ⛔ 未辦·`GB-16` 禁分兩次烤；🔒 `-83` §排程已備料 |
| 4 | `format_exc()[-300:]` 二處之修 ＋ 名單正當重生 | ⛔ 未辦（`GB-86`） |
| 5 | `K-9-12` 宗地層矩形容納之實作 | ⛔ 未辦（已裁未實作） |

🔒 **本批對佇列之實質變動**：第 0 項之「**成因未知**」欄——
`-83` 撤回舊歸因後之新受詞為「**不相鄰之宗之間為何會重疊**」，
本批 §C-2 之**恆等式乙**給出其**算術上之精確形**（判準比錯端點 ⇒ 須推進一整個斜跨距）
⇒ 🔒 **第 0 項自「成因未知」轉為「成因已封閉·修法未擬」**。
⚠️ 🔒 **此為 CC 之定性、⛔ 非裁定**——`GB-67` 之狀態變更須由 claude.ai／KL 為之（本批 §Z-3 已就地加註·⛔ 未改原列）。

---

## §L　交下一窗之三件

1. 🛑 **`§A-5` 停機條款之「部分觸發」覆核**：`-83:71` 已出艙 **9 ／ 13** 欄；
   CC 判**不停機**之三由見 §A-5。**請 claude.ai 裁**：該判是否成立？若不成立，本批之定性須回退至「重複量測」。
2. 🔴 **`P6` 之未答**（§F-2）：`R1 左 j=0` 之 `殘差/ulp` 扣除 `ulp` 之 **16.0** 倍後之**餘量未解**。
   🔒 **建議之量**：直接出艙該五格之**殘差分子**（⛔ 非其比值），並與各格之 `S_req` 分層對照。
3. 🔴 **`K-6:1034`／`:1038` 之「四格須重量」與本批 `§F-1` 之佐證之關係**：
   正典已宣告該五格之量測基礎將因 `K-9-5-4 ②` 落地而作廢，而本批以其中三個夾角值作為
   「差角係真實幾何」之佐證。🔒 **請裁**：`K-9-5-4 ②` 落地後，`D-0` 之定性（⛔ 非偽差）是否須重驗？

---

🔒 **一句話**：舊判準把**起算垂線之二端壓成同一個 `s`**，於是它比的是「本宗二交點之 `min`」對「前宗二交點之 `max`」
——**與二界是否平行無關**，而該毛病**同樣咬到剩下那 3 宗**（`R1右4`／`R3右9`／`R5左1`·**623.1843 ㎡**）。
