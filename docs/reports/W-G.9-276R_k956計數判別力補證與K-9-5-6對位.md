# `W-G.9-276R`　`k956` 計數之判別力補證 ／ `K-9-5-6` 之受詞與三個 `W` 之對位

> **態**｜開工 ＝ `fe1d8653df052f9e572402bee1b7faa0d156d3aa`（`ls-remote` 實查）｜工項零後 ＝ `0249e42f7de2bd2b393d732095aab6143ed971e2`
> **單** ＝ `docs/orders/W-G.9-276_施工單_k956計數之判別力補證與K-9-5-6之對位.md`（工項零已原封入倉）
> **分級** ＝ **中**（含雙態驅動·**零生產碼**）｜**生產碼 `34` 檔一字未動**
> 🛑 **本報告⛔ 判孰為正確之 `W`、⛔ 判 `k956` 是否未被消費、⛔ 判 `K-9-5-6` 之射程、⛔ 鑄 `GB`。**

---

## `§零`　開工閘與工項零

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `fe1d865…` 或其後裔 | `fe1d8653df052f9e572402bee1b7faa0d156d3aa` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 逐位相同 | ✅ |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須**非**祖先 | **非祖先** | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `30392726265554bafee6fa24af0bb34a7aa5b2cd6428073a9b8a3ec185c68455` | **逐位相符**（受詞 `11424` B） | ✅ |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 不紅 | **停機款 `0`／提示 `0`**；`P-3` ＝ `app.py` `def main` `14975`-`24579` | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `368`／`378`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `80`／`80` | **逐位相同**（坑 `80`／`80`·判別力二造 造甲 `坑 n` 定義處 `6 ≥ 1` ✅／造乙 人造坑 `0`／`0` ✅） | ✅ |

🔒 **閘 `4` 之判別力（證非恆綠）**：對**全檔**施同一口徑得 `28d6b437fa7b8eed…` ⇒ 與單載**相異**；
受詞多含 `1` byte 得 `09ab8f700fd91cc5…` ⇒ 亦**相異**。

**工項零**（commit `0249e42`·`§二-B` 搬移法）：
所指之檔數 ＝ **`1`**（判別力：造乙 人造名 ⇒ `0`／造甲 `W-G.9-275` 之單 ⇒ `1`）；
`CR` ＝ **`0`**（`LF` `146`·bytes `11502`；**判別力**：造甲 真 CRLF 人造檔 ⇒ `CR = 2` ✅）；
blob `sha1` ＝ `c9e3bb379906e4a5845abfac4693b60109063c6e`；
**blob 內容 `sha256` ＝ 磁碟 ＝ 源 ＝ `28d6b437fa7b8eedde6e61cbe33b06c6425fb12ca46cb52a61ece8463b9d8824`** ⇒ **逐位相同** 🟢
（判別力：對 `blob + 1 byte` 施同法 ⇒ 相異 ✅）。

🔒 **`§二-B` 搬移法之出處**（⛔ 憑記憶）＝ `docs/orders/W-G.9-270_補令七_舊窗收工批.md` 之
`### 二-B　搬移法之常規（**自此為常規·⛔ 再以 glob 定唯一**）`。

---

## `§一`　工項 `1`：`k956_W_from_mp` 計數之判別力補證（**主檢**）

**器** ＝ `verify/probes/probe_WG9276_k956_count.py`（底本 ＝ `probe_WG9274_rw_origin.py` 之 `drive`／`_spy_*`）
｜**落檔** ＝ `verify/out/WG9276R_k956_count.log`

### 一-1　🔴 法乙之標的：**本倉⛔ 存在名為 `app` 之模組物件**

`verify/app_harvest.py` 之 `harvest()` 逐字為 `ns = {...}` ＋ `exec(code, ns)`
⇒ **`ns` 即 app 碼之 `__globals__`**，⛔ 另有模組物件。
**正面列舉**（態 `0249e42`·框 ＝ 列框）：`^\s*(import app|from app )` 於 `app.py` ＋ `verify/*.py` 單層
之命中 ＝ **`0`**（唯一近似者 ＝ `verify/fixture_yi_construction.py:57` 之 `import app_harvest`）；
對照組 `^\s*import ast` @ `verify/app_harvest.py` ＝ **`1`** ⇒ 器非紅。

⇒ 🔒 **法乙之唯一可實作形** ＝ 自一**已取得之函式物件**取其 `__globals__` 並於其上賦值
（⛔ 假定其即 `ns`）。**機驗**：全 **`8`** 次 drive 之 `probe_fn.__globals__ is ns` ⇒ **全為 `True`**
⇒ **法甲與法乙之標的為同一個 dict**。
🛑 **此係實測所得之<u>結構事實</u>，⛔ CC 對單之改寫**；準據一字未動。

### 一-2　主表：三函式 × 二法 ＝ `6` 格計數

| 情境 | 態 | 函式 | 角色 | 法甲 `ns[...]` | 法乙 `__globals__` | 判 |
|---|---|---|---|---|---|---|
| `0m` | 甲 | `k956_W_from_mp` | **受測** | **0** | **0** | 相同 |
| `0m` | 甲 | `_pool_strips_for_block` | 對照甲[必命中] | **6** | **6** | 相同 |
| `0m` | 甲 | `k6_step0_merge` | 對照乙[必命中·於態甲] | **1** | **1** | 相同 |
| `0m` | 乙 | `k956_W_from_mp` | **受測** | **0** | **0** | 相同 |
| `0m` | 乙 | `_pool_strips_for_block` | 對照甲 | **3** | **3** | 相同 |
| `0m` | 乙 | `k6_step0_merge` | 對照乙 | **0** | **0** | 相同 |
| `3.5m` | 甲 | `k956_W_from_mp` | **受測** | **1** | **1** | 相同 |
| `3.5m` | 甲 | `_pool_strips_for_block` | 對照甲 | **6** | **6** | 相同 |
| `3.5m` | 甲 | `k6_step0_merge` | 對照乙 | **1** | **1** | 相同 |
| `3.5m` | 乙 | `k956_W_from_mp` | **受測** | **1** | **1** | 相同 |
| `3.5m` | 乙 | `_pool_strips_for_block` | 對照甲 | **3** | **3** | 相同 |
| `3.5m` | 乙 | `k6_step0_merge` | 對照乙 | **0** | **0** | 相同 |

🔒 **二法之 `k956` 計數<u>逐格相同</u>** ⇒ 單 `§三-1` 之「二法相異 ⇒ 並報二值」一款**未觸發**。
🔒 **確定性**：本器**跑二輪**（初版／字面修正版），其**全部表列**與**回傳值集合**逐位 `diff` **相同**。

### 一-3　準據之逐格判（**發單側定·⛔ CC 自訂**）

| 情境 | 態 | 法 | 對照甲 `_pool_strips_for_block` | 對照乙 `k6_step0_merge` | `k956` 計數 | 該法之計數 |
|---|---|---|---|---|---|---|
| `0m` | 甲 | 甲 | `6` | `1` | **`0`** | **可信** |
| `0m` | 甲 | 乙 | `6` | `1` | **`0`** | **可信** |
| `0m` | 乙 | 甲 | `3` | `0` | **`0`** | 部分（只一對照 `>0`） |
| `0m` | 乙 | 乙 | `3` | `0` | **`0`** | 部分 |
| `3.5m` | 甲 | 甲 | `6` | `1` | **`1`** | **可信** |
| `3.5m` | 甲 | 乙 | `6` | `1` | **`1`** | **可信** |
| `3.5m` | 乙 | 甲 | `3` | `0` | **`1`** | 部分 |
| `3.5m` | 乙 | 乙 | `3` | `0` | **`1`** | 部分 |

⇒ 🔒 **二對照皆 `>0` 之格 ＝ `4`／`8`**（皆為**態甲**）⇒ 依準據第二款，
**`k956` 於態甲之計數得出艙為事實**：**`0m` ＝ `0`／`3.5m` ＝ `1`**。
🔒 態乙之 `k6_step0_merge` ＝ `0` 係 `WV_K6_STEP0=off` 之**設計後果**（步驟 `0` 關閉）
⇒ 其格判「部分」，其 `k956` 計數**⛔ 出艙為受詞之事實**（照實·⛔ 以「大概也一樣」頂替）。

### 一-4　🔑 正面第三造：**模組全域名解析確可被攔**（⛔ 僅賴二法同值）

**器** ＝ `verify/probes/probe_WG9276_k956_alignment.py`｜**落檔** ＝ `verify/out/WG9276R_k956_alignment.log`

**AST 實查**：`app.py:4292` 之 `temp_parcels, _diag_k6 = k6_step0_merge(` 其**包層 ＝ `_annotate_temp_parcel_cut_type`**
（**頂層 def**·⛔ 在 `main()` 內），且 `k6_step0_merge` **⛔ 為該 def 之區域變數／參數**
⇒ 其解析走**模組全域**。
**實測**：態甲之 `k6_step0_merge` 計數 ＝ **`1`**（二法皆然）。
⇒ 🔒 **模組全域名解析之呼叫確被間諜攔下** ⇒ 該法**⛔ 對此類呼叫失明**。
🛑 **⇒ 開場文 `§六` 所載之由（「間諜掛於 `ns[...]` 而受測路徑以模組全域名解析 ⇒ 其呼叫構造上看不見」）
<u>經實測否證</u>**——照實回報，**⛔ 判其為誰之誤、⛔ 鑄號**（授權僅及 `自誤 379` ＋ 坑 `bc`）。

### 一-5　併出艙 `1`：綁定時點 vs 安裝時點

- 逐字 `_k956_W_from_mp = ns["k956_W_from_mp"]` 於 `verify/stepg_pipeline.py` 之**列框命中 ＝ `1`**（列 `394`）。
- **AST 實查**：該列之**最內層 def ＝ `_run_step_g_impl`（`277`–`1643`）**；`def run_step_g` 於列 `238`。
- ⇒ 其綁定於 **`_run_step_g_impl` 之入口段**（**每次呼叫皆重新綁定**）。
- 間諜之安裝於 `harvest()` 後、`run_step_g` 呼叫**之前** ⇒ **安裝於綁定之前**
  ⇒ 該綁定所取者**即間諜**（⛔ 原函式）。

### 一-5′　併出艙 `1'`：`k956_W_from_mp` 各呼叫點之**包層 def 鏈**（AST 實查·⛔ 硬編）

| 檔:列 | 逐字（節略） | **包層 def 鏈** |
|---|---|---|
| `app.py:22090` | `_W0_l = k956_W_from_mp(_gs_l, _side_mid_left,` | `main > _advance_block_with_split` |
| `app.py:22092` | `_Wn_l = k956_W_from_mp(_gs_l + float(_left_buffer_S) * _du_diag,` | `main > _advance_block_with_split` |
| `app.py:22102` | `_W0_r = k956_W_from_mp(_gs_r, _side_mid_right,` | `main > _advance_block_with_split` |
| `app.py:22104` | `_Wn_r = k956_W_from_mp(` | `main > _advance_block_with_split` |
| `app.py:22415` | `return float(k956_W_from_mp(_bp0, _mp, _adir, _dv))` | `main > _mp_base_W0` |
| `verify/stepg_pipeline.py:1036` | `return float(_k956_W_from_mp(_bp0, _mp, _adir, _dv))` | `_run_step_g_impl > _mp_base_W0` |

🔒 **照實之射程陳述（⛔ 判其為缺陷）**：`app.py` 之**五個**呼叫點**全數落於 `main()` 內**
（`main` ＝ `14975`-`24579`·`P-3` AST 實查），而 `CLAUDE.md` 之 `§7` 逐字載
「harvest 保留 `main()` 但**從不呼叫**（走 stepg）」
⇒ **本批之計數所量者 ＝ harness 路徑（`verify/stepg_pipeline.py:1036`）**，
**⛔ 及於 app 路徑**。**⛔ 由此推論 app 路徑之任何事**。

### 一-6　併出艙 `2`：`_fo_left`／`_fo_right` 之實際值（**全母體 `24` 格**）

源 ＝ `fake_st.session_state['f3L_forced_offset'][<街廓>]`（＝ `_fo_block` 之同一來源）。

| 街廓 | `0m` 態甲 | `0m` 態乙 | `3.5m` 態甲 | `3.5m` 態乙 |
|---|---|---|---|---|
| `R1` | L=False R=False | L=False R=False | L=False R=False | L=False R=False |
| `R2` | L=False R=False | L=False R=False | **L=True** R=False | **L=True** R=False |
| `R3` | L=False R=False | L=False R=False | L=False R=False | L=False **R=True** |
| `R4` | **L=True** R=False | L=False R=False | **L=True** R=False | L=False R=False |
| `R5` | L=False R=False | L=False R=False | L=False R=False | **L=True** R=False |
| `R6` | L=False R=False | L=False R=False | L=False R=False | L=False R=False |

⇒ 格數 ＝ **`24`**（期 `24`）｜`True` 之總數 ＝ **`6`**
🔒 **判別力**：若全母體皆 `False` ⇒ 該欄⛔ 具鑑別力；實測 `True` 總數 ＝ `6` ⇒ **非全 `False`·具鑑別力** ✅

🔒 **框內省之交叉驗**（`_select_pool_slot` 呼叫端·**自我驗證閘**：所錄 `blk_label` 共 **`32`** 筆·
**皆為 `BLKS` 之成員**且非空 ⇒ **器非紅**·坑 `bb` 之攔法已施 `sys._getframe(depth + 1)`）：
其 `_fo_left`／`_fo_right` 與上表**逐格一致**；併錄 `_N` 與 `_degenerate_order`
（`_degenerate_order` **全母體恆 `False`**；態甲之 `_select_pool_slot` 呼叫序 ＝
`['R1','R2','R6','R5','R3']`（**⛔ 含 `R4`**）／態乙 ＝ `['R1','R2','R4']`·`R4` 之 `_N` ＝ `2`）。

### 一-7　併出艙 `3`：`left_forced_offset` 之來源逐字

| 錨 | 逐字 |
|---|---|
| `verify/stepg_pipeline.py:341` | `ss["f3L_forced_offset"] = forced_map` |
| `verify/stepg_pipeline.py:646` | `_fo_block = (ss.get('f3L_forced_offset', {}) or {}).get(blk_label, {})` |
| `verify/stepg_pipeline.py:647` | `_fo_left = bool(_fo_block.get('left_forced_offset', False))` |
| `verify/stepg_pipeline.py:648` | `_fo_right = bool(_fo_block.get('right_forced_offset', False))` |
| `verify/selection_pipeline.py:617` | `'left_forced_offset': bool(_l_forced and _l_has_side),` |
| `verify/selection_pipeline.py:618` | `'right_forced_offset': bool(_r_forced and _r_has_side),` |
| `app.py:20953` | `'left_forced_offset': bool(_l_forced and _l_has_side),` |
| `app.py:20967` | `st.session_state['f3L_forced_offset'] = _forced_offset_map` |

🔒 **判別力**：一人造字面（**執行期組出**·字面⛔ 出艙）於 `verify/stepg_pipeline.py` ＝ **`0`**（須 `0`）。
🔒 **逐字全表**（`18` 列）見落檔 `verify/out/WG9276R_k956_count.log` 之「併出艙 `3`」節。

### 一-8　終止態（照實·⛔ 判、⛔ 提修法主張）

| 情境 | 態 | `run_step_g` 之中止 |
|---|---|---|
| `0m`／`3.5m` | **甲** | **`None`**（⛔ 拋 `RuntimeError`） |
| `0m`／`3.5m` | **乙** | 🔴 `結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）` |

🛑 **本節只登記**；**⛔ 判 `GB-168` 之任何事、⛔ 解除、⛔ 收窄**（單 `§六` 明令）。

---

## `§二`　工項 `2`：`K-9-5-6` 之受詞 ＋ 三個 `W` 之對位

### 二-1　該節**全文逐字**（`sed -n '860,953p'` 之輸出**逕行落檔**·`常規七 一`·⛔ 摘要⛔ 改寫）

🔒 **抽取之三數（與塊內文字出自<u>同一次</u>抽取·`常規七 二`）**：
來源 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之 `:860`–`:953`｜
**列數 ＝ `94`**｜**位元組 ＝ `7198`**｜**`sha256` ＝ `640f2784e47576057bf916f640de6213964c907e0723bdf30efb7fc0fcf8fbaa`**
🔒 **框之逐字** ＝ `sed -n '860,953p'`（**每列含其結尾換行**）；下塊即該次抽取之**輸出本身**。
🔒 **覆核（`常規七 三`）**：另跑一次真正之 `sed -n '860,953p'`，與塊內文字**逐位相同**；`CR` ＝ `0`。
🩸 **本批之自捕**：初版之三數取自 shell 之 `sed`（`7198` B）而塊內文字取自 python 之
`"\n".join(...)`（**`7197` B**·**缺末尾換行**）⇒ 二者**出自不同次抽取** —— 即 `常規七 二`
所戒之形（「三數相符⛔ 保證塊內文字相符」之對偶）。已於入倉**前**改為同一次抽取。
🔒 **落點之現查**：`K-9-5-6` 於該檔之全檔命中 ＝ **`5`** 列；其**節題形**（`^### .*K-9-5-6`）＝ **`1`** 列（`:860`）；
其後首個 `^#{1,3} ` 之列 ＝ `:954`（`K-9-5-7`）⇒ 節之界 ＝ `[860, 953]`。

<!-- ===== BEGIN 逐字塊（K-6 :860-:953·sed -n 之輸出本身）===== -->
### 🔒 K-9-5-6 W 鏈定式：每條界線只量一次·Rw 逐宗累積（KL **追認** 2026-08-08·D-2b-4·canonical·**逐字**）

> 🔧 **具名更正（D-2b-5·CC 自查·⛔ 不刪原文）**：本則初載為「**KL 新裁**」——**該定性有誤**。
> 本則實為**追認並回歸既有規格**，⛔ **非新裁**。三項證據：
>
> 1. **規格早載同式**（`grep -rn "R(W_i)−R(W_{i−1})\|R(W_i) − R(W_{i−1})" docs/specs/`）：
>    - `docs/specs/W-G.4_規格_v3.md:289` 逐字：
>      「給定 S → 遠側界線 → **直接量 W 垂距** → `Rw=R(W_i)−R(W_{i−1})`」；
>    - `docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md:10` 逐字：
>      「直接量垂距 → `Rw ＝ R(W_i) − R(W_{i−1})`，**無循環**、**與 S 累積器脫鉤**」。
> 2. **偏離者僅為幾何解算器**：`grep -n "def iterate_G_S" app.py`（代數）之路徑**一直合規**
>    （`rw_increment(W_prev, W_cur)`）；`grep -n "def solve_G_binary" app.py`（幾何）之路徑偏離
>    （`rw_increment(_W_near, W)`）⇒ **同一檔內兩個解算器互不一致**，而**無任何檢測捕捉之**。
> 3. ⇒ **本則之教訓 ＝「規格已載、碼面偏離、長期無檢測捕捉」**，
>    ⛔ **不得只寫成「裁定落地」**——那會把一次**檢測缺口**記成一次**新政策**。
>    同族：`GB-46`（共用界線兩端各自量測）。**新增檢測**＝逐界面 `Δ_i == 0`（D-2b-4 §四(b)）。

**KL 逐字（⛔ 不得改寫成程式語言）**：

> 第 1 宗土地的 W ＝ 由 MP 至**平移後 SIDELINE** 之垂距（第 1、2 宗共用邊）⇒ `Rw_1`
> 第 2 宗土地的 W ＝ 由 MP 至 **ALLOCLINE** 之垂距（第 2、3 宗共用邊）⇒ `Rw_2`
> 第 2 宗之 G 值所用之 Rw ＝ `Rw_2 − Rw_1`
> 依此 W 規定量法與 Rw 累積之精神，**一路到最後一宗（非抵費地）**。

**展開式**：`Rw_i = R(W_i) − R(W_{i−1})`；中間項全對消 ⇒ `ΣRw_側 = R(W_最後一宗) − R(W_0)`。

🔑 **每一條界線只被量一次**——由**它前面那一宗**量；下一宗**直接取用該值**當起點，
⛔ **不得自行再量一次**。

- 🔒 **`W_i` 之量測不變**：仍為 **intrinsic 直量**（MP → 本宗**遠側**分配界線之垂距）
  ——⛔ 非以寬度累加堆出（「虛胖陷阱」`grep -n "虛胖陷阱" app.py` ⇒ `:9344`）。
  **累積的是 `Rw`、不是 `W`**（`grep -n "def rw_increment" app.py` 本即此設計·⛔ 無衝突）。
- 🔒 **首宗起點**：`W_0` ＝ MP→corner（**照實可負·⛔ 禁 clamp**）；forced 時為其 buffer 起算點。
  > 🛑 **撤回加註（`K-9-5-12`·KL 裁 2026-08-12·⛔ 上行原句一字未刪）**
  >
  > **上行前半（`W_0` ＝ MP→corner）自本批起<u>失效</u>。**
  > 首宗起點一律為 **0**（即自 MP 起量）。依據見 `grep -n "^### 🔒 K-9-5-12 " 本檔`。
  >
  > 🔴 **性質 ＝ 移除一句從未經裁示之敘述，⛔ 非推翻 KL 之裁定。**
  > 本行經 `W-G.9-15` 甲組以**機械規則**判定為**助手側補充**（**不在任何引文區塊內**）；
  > 其上之 KL 逐字區塊僅四行，內容為「第 1 宗土地的 W ＝ 由 MP 至平移後 SIDELINE 之垂距…」
  > ——**其中並無「`W_0` 起算點」此一概念**。
  > 出處 ＝ `664e4f5`（2026-08-08）之「§二 唯一行為變更：首宗起點不動（`is_corner` ⇒
  > **自量 `W_near`＝mp→corner**…）」⇒ **碼面自述被寫進本檔**（方向相反）。
  >
  > ⚠️ **上行後半（`forced` 時為其 buffer 起算點）⛔ 未一併撤，列為<u>未決</u>**：
  > 現查 `強制抵費地 ⟺ 無 winner`（單一三元式），而 KL 另裁「無 WINNER 時**第 2 宗**之起算
  > ＝ 該抵費地之**遠側境界線**；抵費地本身不計 G、不負擔」⇒ 該半句之受詞（第 1 宗之 `W_0`）
  > 是否因此消滅，**須 KL 確認**，⛔ 本批不順手撤。
  >
  > 🛑 **失效加註（`K-9-5-13`·KL 裁定二·2026-08-12·⛔ 上文原句一字未刪）**
  >
  > 上開「後半句」**自本批起失效**。KL 裁定二逐字：
  > 「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
  > ⇒ `forced` 時之起算**不再取「其 buffer 起算點」**（該點即「面積換算出來的替身線」）。
  > 依據見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。
- 🔒 **止於最後一宗（非抵費地）**——抵費地不入鏈（見 `K-9-5-7`）。

**碼面唯一決定點**：`grep -n "K-9-5-6 W 鏈之唯一決定點" app.py`
（`solve_G_binary` 內 `_rw_start = _W_near if is_corner else float(W_prev)`）。
⛔ 未於六個 `_solve_one` 呼叫端或兩份薄殼各寫一份。
📌 **代數 fallback `iterate_G_S` 本即此式**（`grep -n "rw_increment(W_prev, W_cur)" app.py`）
⇒ 本則係令**幾何解回到與代數解同一定義**、⛔ 非新造。
📌 規格早載同式：`docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md:10`
（「直接量垂距 → `Rw ＝ R(W_i) − R(W_{i−1})`」）——**碼曾偏離、本則校回**。

**實測（`verify/out/probe_D2b4_gates.log`）**：全界面 `Δ_i = +0.0000`；
`0m R1 left` 之 `ΣRw` 由 `99.10` → **`100.02`**（回收漏項 `Δ_self = +0.9120`）。

> 🔎 **【`MP` 之定義指向·`W-G.9-3`·⛔ 上文一字不刪·⛔ 非新裁】**
>
> 本節通篇使用符號 `MP` 而**未於本級（權威序第 1 級）界定之**。其定義在**下位層級**，
> 三處**互相一致**（皆為 SIDE 線之中點）：
>
> | 權威序 | 出處（內容 grep 可自癒） | 逐字 |
> |---|---|---|
> | ② `CLAUDE.md` | `grep -n "SIDE_LINE 中點到該筆" CLAUDE.md` | 「W（累積）：**SIDE_LINE 中點**到該筆靠街角側地界線的垂距」 |
> | ③《配地計算總規格_v3.md》 | `grep -n "取該側 SIDE_LINE 中點" docs/配地計算總規格_v3.md` | 「取該側 **SIDE_LINE 中點 `M`**」 |
> | ③′ `docs/specs/`（v3 補丁鏈） | `grep -n "mp＝SIDE_LINE" docs/specs/W-G.4_規格v3補丁六_W正典_S1v3範圍.md` | 「`mp` ＝ SIDE_LINE（FRONTLINE∩SIDELINE 理論交點→末端全段）**中點**」 |
>
> ⇒ **本節之 `MP` ＝ SIDE 線之中點**，係由**下位層級供給之定義**，
> ⛔ **本註不創設新規、不改本節一字**。
>
> ⚠️ **一項<u>受詞</u>差異（⛔ 非衝突）**：第 ②③ 級作「靠**街角側**（＝**近**側）地界線」，
> 而**本節（第 1 級）作「遠側」** ⇒ **依權威序上位優先，以本節之「遠側」為準**。
> 🔒 二者所差者為**量到哪一條界線**（受詞），⛔ **非量測原點**——原點三級一致、皆為 `MP`。
> （同型先例：本檔之「🔒 權威序已自動解決（`docs/rulings/` → `CLAUDE.md`）⇒ 非正典衝突、不停機」。）
>
> 🩸 **案由**：`W-G.9-2` 曾判「`MP` 於正典從未定義」而據以停機（`P1` 破）
> ——**係只查第 1 級、且字樣之符號項僅含大寫 `MP`**（倉內作 `M`／`mp`）所致之**雙重射程不足**。
> 該誤判已於 `W-G.9-3` 具名更正（原句一字未刪·詳失敗考古**節 64**）。

---

<!-- ===== END 逐字塊 ===== -->

### 二-2　受詞之具名（**逐字引其句**）

**`(a)` 該「`W`」係何物之寬度**——🔴 **其⛔ 為「某物之寬度」，而係一<u>垂距</u>**。逐字：

> 🔒 **`W_i` 之量測不變**：仍為 **intrinsic 直量**（MP → 本宗**遠側**分配界線之垂距）
> ——⛔ 非以寬度累加堆出（「虛胖陷阱」`grep -n "虛胖陷阱" app.py` ⇒ `:9344`）。
> **累積的是 `Rw`、不是 `W`**（`grep -n "def rw_increment" app.py` 本即此設計·⛔ 無衝突）。

其**原點 `MP`** 之定義由**下位層級**供給，該節之加註逐字載三處一致「皆為 SIDE 線之中點」，
並載一項**受詞差異**逐字：

> ⚠️ **一項<u>受詞</u>差異（⛔ 非衝突）**：第 ②③ 級作「靠**街角側**（＝**近**側）地界線」，
> 而**本節（第 1 級）作「遠側」** ⇒ **依權威序上位優先，以本節之「遠側」為準**。

**`(b)` 射程及於何處**——逐字：

> 依此 W 規定量法與 Rw 累積之精神，**一路到最後一宗（非抵費地）**。

> 🔒 **止於最後一宗（非抵費地）**——抵費地不入鏈（見 `K-9-5-7`）。

其**碼面唯一決定點**逐字：

> **碼面唯一決定點**：`grep -n "K-9-5-6 W 鏈之唯一決定點" app.py`
> （`solve_G_binary` 內 `_rw_start = _W_near if is_corner else float(W_prev)`）。
> ⛔ 未於六個 `_solve_one` 呼叫端或兩份薄殼各寫一份。

**`(c)` 該節是否逐字令 `Rw` 之式為「差」或「絕對值」**——🔒 **逐字令為「<u>差</u>」**：

> **展開式**：`Rw_i = R(W_i) − R(W_{i−1})`；中間項全對消 ⇒ `ΣRw_側 = R(W_最後一宗) − R(W_0)`。

🔒 **機械佐證（三軸｜`(1)` `blob@0249e42`｜`(2)` 母體 ＝ 上開 `94` 列之抽取塊**單檔**｜`(3)` **列框**）**：
`絕對值` 於該塊之命中 ＝ **`0`** 列；`R(W_i) − R(W_{i−1})` 形之命中 ＝ **`3`** 列（塊內 `:6`／`:10`／`:25`／`:65` 之四處中三處為該式之逐字形）。
**對照組（必非零）**：`W` 於該塊之列命中 ＝ **`32`** ⇒ **器非紅**。
🛑 **⇒ 該節通篇⛔ 出現「絕對值」三字**；⛔ 由此推論任何修法主張（單 `§六-4`）。

### 二-3　三個 `W`（＋ `W_prev`）是否即 `k956_W_from_mp` 之輸出（**逐位**）

**判法**（單 `§三-2` 項 `3`）＝ 以 `§三-1` **法乙**之間諜所錄之**回傳值集合**與三值逐位比對。

🔒 **回傳值集合**（法乙·`__globals__`）：
`0m` 態甲 ＝ `[]`（n `0`）／`0m` 態乙 ＝ `[]`（n `0`）／
`3.5m` 態甲 ＝ `[6.98048044737404]`（n `1`）／`3.5m` 態乙 ＝ `[6.98048044737404]`（n `1`）
⇒ **全母體之聯集 ＝ `[6.98048044737404]`**。

🔒 **判別力二造**（單逐字·二造同判 ⇒ 器紅）：
造甲[必相符] ＝ 集合中任一元與其自身比 ⇒ **逐位相符** ✅；
造乙[必不符] ＝ 一人造值（**執行期組出**·字面⛔ 出艙）⇒ **⛔ 相符** ✅ ⇒ **器非紅**。

| 名 | 值（單所載） | `0m` 態甲 | `0m` 態乙 | `3.5m` 態甲 | `3.5m` 態乙 |
|---|---|---|---|---|---|
| `宗地寬度(m)` | `14.01` | **⛔ 相符** | **⛔ 可得**（器不可信） | **⛔ 相符** | **⛔ 可得** |
| `W(m)` | `14.20` | **⛔ 相符** | **⛔ 可得** | **⛔ 相符** | **⛔ 可得** |
| `W_cur` | `14.1988975422` | **⛔ 相符** | **⛔ 可得** | **⛔ 相符** | **⛔ 可得** |
| `W_prev` | `0.1775004071` | **⛔ 相符** | **⛔ 可得** | **⛔ 相符** | **⛔ 可得** |

🛑 **三態⛔ 共用出艙碼（單 `§三-2` 項 `4`）**：
**「⛔ 相符」** ＝ **器可信**（態甲·二對照皆 `>0`）**且**比對不中；
**「⛔ 可得」** ＝ **器不可信**（態乙·`k6_step0_merge` ＝ `0`）⇒ **該格未作比對**。
🛑 **⛔ 判孰為正確之 `W`、⛔ 提修法主張、⛔ 判 `K-9-5-6` 之射程是否及於該閘**——**出艙即止**。

---

## `§三`　工項 `3`：`自誤 379`（`§一`）＋ 坑 `bc`（`§二`）＋ `ALPHA_LABELS`→`bc`

### 三-1　`自誤 379`

**落點** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**（append-only·嚴格前綴 ＝ `True`；
**判別力**：以 `old + b'X'` 為前綴 ⇒ `False` ✅）｜長度 `780965` → `785957`（**`+4992` B**）｜`CR` ＝ `0`。
**受詞逐字塊**由 `sed -n '41,46p'` 之輸出**逕行落檔**（`常規七 一`）：
單 `:41`–`:46`｜列數 **`6`**｜位元組 **`1037`**｜`sha256` **`68e5dab3d4fad054d001b786ade5483effe15bf9e8b9739ad61f138f768e6991`**；
**覆核**：重跑抽取逐位相同 ✅。
**節題之形** ＝ 取既有最末一則（`自誤 378`）之標題形逐字為範本（`常規四（九）四`）。

**取號現查**（三軸｜`(1)` `blob@0249e42f7de2bd2b393d732095aab6143ed971e2`｜
`(2)` 取最大號 ＝ 登記表**單檔**（`780965` B／`9205` 列）·查占用 ＝ 全倉追蹤檔 **`2166`** 檔（讀不到 `0`）｜
`(3)` **檔框 ＋ 列框**）：

- **取最大號**（框 ＝ `VR-091 四` 定義框 ＋ 範圍框·母體 ＝ 單檔）：命中列 **`380`**／相異 **`368`**／**MAX `378`** ⇒ **次號 `379`**。
- **查占用**（全倉·**B 形 ⋀ C 形並取**·`GB-158`）：

| 角色 | 鬆框 檔／列 | B 形 檔／列 | C 形 檔／列 |
|---|---|---|---|
| 受詢 `自誤 379` | `1`／`6` | `1`／`1` | `1`／`5` |
| 對照組 甲 `378`（須 `>0`） | `7`／`23` | `3`／`3` | `7`／`21` |
| 對照組 甲2 `377`（須 `>0`） | `9`／`36` | `6`／`15` | `9`／`34` |
| 對照組 乙（人造哨兵·執行期組出·`GB-147`） | `0`／`0` | `0`／`0` | `0`／`0` |

⇒ **器非紅**；且 **B 形於二對照皆 `>0`** ⇒ `GB-158` 之「B 形使對照組全 `0`」**於本則未發生**。
**逐筆歸類**（`裁 H`）：受詢之命中**全數集中於單一檔** ＝ 本單自身（工項零已入倉）
⇒ 依**自解款白名單 `1`** ⇒ **⛔ 撞號、⛔ 順延**。

### 三-2　坑 `bc`

🔒 **其定義處 ＝ 本單 `§二` 之坑表**（`docs/orders/W-G.9-276_…md:53`–`:55`），
其表頭列逐字 ＝ `| 標籤 | 形／徵候／攔法 |` ⇒ 同時含 `徵候` ⋀ `攔法` ⇒ 合於 `D-T` 之形。
**取號現查**（全倉·態 `0249e42`）：坑 `bc` ＝ **`1`** 檔／**`5`** 列（**全數為本單自身**）；
對照組 甲 坑 `bb` ＝ `3` 檔／`15` 列（須 `>0`）／甲2 坑 `ba` ＝ `8` 檔／`37` 列（須 `>0`）／
乙 坑 `bz`（未鑄）＝ **`0`** 檔／`0` 列（須 `0`）⇒ **器非紅**。

### 三-3　`ALPHA_LABELS` → `bc` ＋ **三處**框字串齊一

**受詞** ＝ `verify/probes/probe_WG9269_pit_index.py`（**⛔ 生產碼 `34` 檔之一**）。

| # | 處 | 改後 |
|---|---|---|
| `1` | `ALPHA_LABELS` 之末元 | `"bb",` ＋ 新列 `"bc"])  # 🆕 \`W-G.9-276\` §二 所准之一坑` |
| `2` | `run(rev, ALPHA_LABELS, …)` 之題 | `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bc\`` |
| `3` | `issuer_frame(rev, ALPHA_LABELS, …)` 之題 | 同上 |

🔒 **判別力（單 `§二` 所令·先實測後填）**——以 **`compile` + `exec` 單一 `Assign`** 取其**語法真值**
（⛔ `ast.literal_eval`：其**只及字面**，而該 RHS 含 comprehension ＋ 串接）：
**成員數 `54` → `55`（`+1` ✅）**｜**尾元 ＝ `'bc'` ✅**｜前一元 ＝ `'bb'`｜**重複元 ＝ `0`** ✅。
**三處框字串**：新形（`aa`〜`bc`）＝ **`2`** 列；舊形（`aa`〜`bb`）**殘留 ＝ `0`** 列 ✅
（對照組[必非零] `字母系 ` ＝ `2` 列／對照組[必零] 人造字面 ＝ `0` 列 ⇒ **器非紅**）。
🔒 **總數之算式**：坑之總數 ＝ `NUM_LABELS` **`26`** ＋ `ALPHA_LABELS` **`55`** ＝ **`81`**
（改前 ＝ `26 + 54 = 80`）⇒ 與單 `§四-5` 之期值 **`81`／`81`** 一致。

---

## `§四`　收工閘

（本節之數係**報告入倉後**所量者，逐項見下；閘之量測與其所守護之動作**分開呼叫**·`常規補款一`。）

---

## `§五`　本批之**自解清單**（`作業常規之追加五 §一` 款 `5`）

| # | 疑義逐字 | 二個以上之讀法 | **所取者** | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§三-1`「**法乙** ＝ 改寫 **app 模組之全域屬性**」 | `(a)` 存在 `app` 模組物件 ⇒ `setattr(mod, …)`；`(b)` ⛔ 存在該物件 ⇒ 取一已 harvest 函式之 `__globals__` 並於其上賦值 | **`(b)`** | `^\s*(import app\|from app )` 於 `app.py` ＋ `verify/*.py` 單層 ＝ **`0`**（對照組 `import ast` ＝ `1`）；`harvest()` 逐字 ＝ `exec(code, ns)`；八次 drive 之 `__globals__ is ns` 全 `True` | `(a)` 之標的**不存在** ⇒ 依之即無法執行；且 `(b)` **⛔ 假定** 其即 `ns`，而是**機驗**之 |
| `2` | 單 `§三` 工項 `0`「`§二-B` 搬移法」（單內無該節） | `(a)` 停機請補；`(b)` 自倉取其**正典定義** | **`(b)`** | `docs/orders/W-G.9-270_補令七_舊窗收工批.md:48` 之 `### 二-B　搬移法之常規` ＝ **唯一定義處**（全倉 `搬移法` 之命中逐處已列舉） | 其**⛔ 為單內查無之交付物**，係**倉內已成文之常規**之指涉 ⇒ 非 `自誤 52` 之形 |
| `3` | 「搬移」是否須**刪除**主 checkout 之源檔 | `(a)` 移動（刪源）；`(b)` 複製（留源） | **`(b)`** | `§二-B` 之三款**全為「如何唯一指認」**，⛔ 一字及於刪源；`W-G.9-275R` 之工項零亦未載刪源 | `(a)` **⛔ 可逆**（違自解款第 `4` 項）；且開場文亦留於同目錄 ⇒ 留源為既例 |

🔒 **三項皆滿足自解五要件**：零土地後果／零生產碼（`34` 檔一字未動）／可機驗（上表證據欄·皆附必紅必綠二組）／
可逆（新建檔 ＋ append-only ＋ 一處非生產碼探針之就地更正）／出艙具名（本表）。
🔒 **黑名單之逐項自檢**：⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字（含註解）；
二讀法之後果**⛔ 在土地上相異**；**⛔ 新訂準據**（單之準據一字未動）；⛔ 廢止或放寬任何既有之閘。

## `§六`　🔴 **出艙即止之三事**（⛔ 判、⛔ 提主張·單 `§六-4`）

1. **`k956_W_from_mp` 於 harness 路徑之呼叫次數**（器可信之態甲）＝ `0m` **`0`**／`3.5m` **`1`**。
2. **開場文 `§六` 所載之由經實測否證**——同型之模組全域名解析（`k6_step0_merge`）**確被攔下**。
3. **`app.py` 之五個呼叫點全數落於 `main()` 內**，而 harness **從不呼叫 `main()`**（`CLAUDE.md §7` 逐字）。

🛑 **上三事皆⛔ 得讀為「`k956` 未被消費」或「其為缺陷」**——該判**⛔ 在本批之授權內**。

---

*本報告受詞 ＝ `W-G.9-276`。⛔ 呈 KL。*
