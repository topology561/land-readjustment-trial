# `W-G.9-9`：凍存受詞**去呈現層** ＋ 倉規二則**升格**

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg99-projection`）
**基準**：`8892aae`　**事前登記**：`docs/reports/W-G.9-9_前置登記.md`（commit `31c04c2`）

> # ✅ `K1`／`K2`／`K3`／`K4`／`K6` 成立
> # 🛑 **`K5` 字面未達成 ⇒ 依 `W-3` 停機上呈**（詳 §4）
>
> **要旨**：`K5` 之字面判準（活文件之引用失準 **＝0**）**在本倉之規則體系下不可達**
> ——「⛔ 原句不刪」保證每一個被更正之行號**永遠留在原地作為引述**。
> **實測**：加註**前 5 處、加註後 5 處、且為同一集合** ⇒ **本批未新增任何失準**；
> 且該 5 處**在本批之前即已失準**、其所在活文件**皆已附可執行字樣錨**。
> ⇒ 判準應為「**未新增**」，⛔ 非「歸零」。**此一改寫須由 KL／claude.ai 裁，⛔ 非 CC 自裁。**

---

## §1 `K1`〜`K6` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **K1** | ✅ **成立** | **未投影**時：現行凍存 × 呈現層合成變動 ⇒ **類 III ＝ 3**（三支全中）⇒ 對帳器對呈現層**敏感**、舊受詞過寬 |
| **K2** | ✅ **成立** | 兩途徑（**甲**：投影既有凍存·⛔ 不重跑／**乙**：重跑三支後投影）之**本體逐字相同**（`diff rc=0`），且**本體 sha1 相同**（值見各檔檔頭·倉規 B）。附**判別力自檢**：人造一字差異 ⇒ `rc=1` |
| **K3** | ✅ **成立** | 竄改自檢**兩態皆印**：`raise` 幀改為 `wg_g1_smoke.py:58`（終止點提前）⇒ **紅**；還原 ⇒ **綠** |
| **K4** | ✅ **成立** | **同一份**合成輸入於**投影後** ⇒ 類 I **3**、類 II **0**、類 III **0** ⇒ 呈現層變動**不再致紅** |
| **K5** | 🛑 **字面未達成**（見 §4） | 活文件之失準：加註**前 5**／加註**後 5**、**同一集合** ⇒ **淨新增 0**；惟字面要求為 **0** |
| **K6** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`。全檔**唯一非路徑差異** ＝ 夾具列之**描述文字**（`rc` 仍 `0`） |

### 1-1 `K1`／`K4` 之**單變因**構造（⛔ 倉內合成·不依賴任何外部觀測）

同一份合成輸入，差別**只在有無投影**：

| 合成變動 | 內容 | 命中 |
|---|---|---|
| **M1** | 錯誤位置標記行 `~~~~^^` → `^^^^^^` | 3 |
| **M2** | 摺疊 `...<4 lines>...` → 展開為兩行原始碼列 | 3 |
| **M3** | 上游幀之原始碼列重排 | 3 |

⛔ **未改動** `raise` 幀、例外型別、例外訊息 ⇒ **真受詞逐字未變**。

**判別力自檢（⛔ 投影不得過窄）**：投影後
- 只改**例外訊息** ⇒ 類 III **1** ✅
- 只改 **`raise` 幀** ⇒ 類 III **1** ✅
⇒ 投影**未把真受詞一併吃掉**。

### 1-2 投影後之凍存記錄（三支·各 **2 行**）

```
File "verify/stepg_pipeline.py", line 1362, in run_step_g
RuntimeError: 🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）…
```

（三支**逐字相同**；投影前為 13／13／15 行。）

---

## §2 投影之**放棄清單**（⛔ 逐條·**永不比對**·非「呈現層」四字了事）

| # | 放棄 | 後果 |
|---|---|---|
| 1 | `Traceback (most recent call last):` 標題行 | 該行措辭改變不再致紅 |
| 2 | **上游各幀**之 `File` 行 | **呼叫鏈形狀改變不再致紅**（只要 `raise` 點不變） |
| 3 | 各幀之**原始碼列** | 呼叫處寫法（換行、參數重排）改變不再致紅 |
| 4 | **錯誤位置標記行**（`~~~~^^`／`^^^^`） | 直譯器標記樣式改變不再致紅 |
| 5 | **摺疊標記**（`...<N lines>...`） | 摺疊行數改變不再致紅 |
| 6 | **`rc` 值** | ⚠️ 單看 `rc` 不再致紅——惟 `rc=0` 幾必伴隨「無幀、無例外行」⇒ 投影變 `["(無 traceback 幀)"]` ⇒ **仍紅** |
| 7 | **stdout**（`W-G.9-8` 已放棄·本批延續） | 進度輸出之任何變動不再致紅 |
| 8 | traceback 之**中段空行** | 無實質後果 |

🔒 **投影 ≠ 正規化**：正規化（`R1`／`R2`）是**兩端都變形**、代價是**看不見**；
投影是**縮小受詞**、代價是**永不比對**。⇒ 二者聲明義務不同，⛔ 不得混同。
**本批未新增任何正規化規則**（`R1`／`R2` 照舊，套用於投影**後**之 `File` token）。

---

## §3 凍存之**環境耦合聲明**（⛔ 逐項·已同步寫入夾具 docstring）

| # | 綁死 | 說明／緩解 |
|---|---|---|
| 1 | **`raise` 所在檔之行號** | **刻意保留**：raise 換位置須被看見 ⇒ 落**類 II**、由人具名確認 |
| 2 | **例外訊息中之一切數值** | 綁死當前 CAD／地籍／快照；資料一變即類 III——**這是要的** |
| 3 | **例外型別名** | 型別改變即紅 |
| 4 | **語系與編碼** | 須 `PYTHONIOENCODING=utf-8`（夾具已設）；⛔ 非 UTF-8 環境擷取會失真 |
| 5 | **「最後一幀即 raise 點」之格式假設** | 直譯器若改幀序／加尾幀 ⇒ 受詞位移 ⇒ 落類 III（loud） |
| 6 | **「例外塊 ＝ 尾端連續無縮排行」之格式假設** | ⚠️ 若環境於 traceback **之後**再印無縮排警告，該行**會被納入**受詞 ⇒ 致紅。**已知耦合·本批不處理** |
| 7 | **檔案路徑之倉相對形式** | 由 `R1`／`R2` 保證 ⇒ **不**綁工作樹絕對位置 |
| 8 | **直譯器之 traceback 呈現層** | 🔒 **本批投影已解除** |

**夾具之轉紅之因**已改為**逐項列舉 8 條**（⛔ 非單一）——見
`verify/fixture_e2e_termination.py` 之 docstring「本夾具轉紅之因」節。

---

## §4 🛑 `K5`：字面未達成 ⇒ 依 `W-3` **停機上呈**

### 4-1 實測（⛔ 由程式原文逐行 `finditer` 讀出·非 `grep -A/-B/-C` 之窗）

**稽核器已入倉**：`verify/tools/wg99_anchor_audit.py`（可覆現）。

| | 加註**前**（門檻 `P=76`） | 加註**後**（門檻 `P=130`·本批插入起點） |
|---|---|---|
| 引用總數 | 57 | 57 |
| 失準合計 | 32 | 28 |
| **活文件失準** | **5** | **5** |
| 歷史記錄失準 | 27 | 23 |

🔒 **活文件之 5 處，前後為<u>同一集合</u>**：

| 檔:行 | → 錨 | 性質 |
|---|---|---|
| `.claude/skills/failure-archaeology/SKILL.md:1072` | `CLAUDE.md:207` | 引述施工單之歷史說法（原文即標「⚠️ 行號指不到」） |
| `.claude/skills/failure-archaeology/SKILL.md:1075` | `CLAUDE.md:207` | 課文之舉例（「施工單指 …」） |
| `.claude/skills/failure-archaeology/SKILL.md:1082` | `CLAUDE.md:207` | **`W-G.9-8` 自己之加註**（明言「本則之舉例已再度漂移」） |
| `docs/reports/W-G.9波_新session交接文_WG91-94.md:129` | `CLAUDE.md:328` | 原句（`W-G.9-8` ⛔ 不刪） |
| `docs/reports/W-G.9波_新session交接文_WG91-94.md:130` | `CLAUDE.md:328` | **`W-G.9-8` 自己之加註**（明言「已失準」） |

### 4-2 二項決定性現查

**① 該 5 處在本批<u>之前</u>即已失準**（⇒ 非本批所致）：

```
$ git show HEAD:CLAUDE.md | sed -n '207p'   ⇒   **先 `git clone`／`git pull` 對現況**。公倉可直接 clone。
$ git show HEAD:CLAUDE.md | sed -n '328p'   ⇒ | 面積_m2 | 動態 | a' 累加器 |
```
而引用方所稱者為「CAD 圖層表之一列」與「`MP` ＝ SIDE 線之中點之出處」——**皆不符**。

**② 其所在活文件<u>皆已附</u>可執行字樣錨**（⇒ 讀者仍能定位）：

```
$ grep -c "^| BASELINE |" CLAUDE.md                                   ⇒ 1
$ grep -c "SIDE_LINE 中點到該筆靠街角側地界線的垂距" CLAUDE.md         ⇒ 1
```
（判別力自檢：寬字樣 `grep -c "BASELINE" CLAUDE.md` ⇒ **7** ⇒ 唯一性檢查非恆為 1。）

### 4-3 🔴 判準本身之問題（⛔ 上呈·非 CC 自裁）

`K5` 之字面「活文件失準 ＝ **0**」與本倉之「**⛔ 原句不刪**」**互斥**：
每一次以加註更正行號錨，都會**同時**留下
① 原句中之舊行號、② 加註中對該舊行號之引述 ——**兩者都會被任何 `CLAUDE.md:<N>` 稽核計為失準**。
⇒ **失準數只會單調增加，永遠到不了 0。**

🔒 **CC 之處置**（比照 `W-G.9-6` `T-3` 之先例）：
- ✅ **完成全部純觀測工項**（前後稽核、性質分類、字樣錨覆蓋驗證）；
- ⛔ **未刪除任何原句**、⛔ 未追改任何歷史記錄；
- 🛑 **以最高優先序上呈**，判準之改寫（「未新增」取代「歸零」）**待裁**。

⚠️ **本批已先行把該分辨寫入 `CLAUDE.md` 之升格條文**（「本規只能止血、不能回溯」），
惟**該條文係描述現況與判準建議，⛔ 不等於已裁**。

---

## §5 倉規升格之**具名偏離**（⛔ 與施工單 §2-3 之措辭不同·已現查）

施工單令新增「倉規 A」。**現查**：`CLAUDE.md` **早已有**
「🔒 行號衛生（KL 2026-07-25 立）」一則，其第 1 款即「一律引符號名 ＋ 可自癒 grep 指令」
——與「倉規 A」**大幅重疊**。

⇒ **CC 改為「併入既有規則升格」**（新增第 4／5 款 ＋ 升格案由），
⛔ **未**另立一則平行規則。
**理由**：本倉明文禁「同一述詞兩處定義」（`CLAUDE.md` 之
「⛔ 上條 `數字錨引倉檔` 為…**單一生產者**，本條**不重述**」即其先例）。
**偏離已於此具名。**

「倉規 B」（不可變文件不得記載可變檔之雜湊）**倉內無對應者** ⇒ 獨立新增，
並就地寫明其與既有 `數字錨引倉檔` 之分工（該條管「須現查」，本則管「該寫在哪個檔」）。

**倉規 B 之首次實施**：兩份新凍存檔之檔頭均記其**本體**雜湊
（本體 ＝ 去除全部 `#` 檔頭行後之內容 ⇒ 檔頭加註**不改變**本值）。
**驗證指令**（⛔ 值不入本報告——本報告即「不可變文件」）：

```
$ grep -v "^#" <凍存檔> | git hash-object --stdin      # 應等於該檔檔頭所載
⇒ 兩檔皆「現算 == 檔頭所載」：是
```

---

## §6 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未解除截斷**（`verify/run_verification.py` 之 12 列上限未動；該檔 `git diff --numstat` **空輸出**）。
2. ⛔ **未立 `G3` 三項之閘**（`W-D.3 碎片`／`W-F F.3`／`W-G G.2`）。
3. ⛔ **未追改任何歷史記錄**之舊行號錨與舊雜湊（批次報告／事前登記／凍存 log）。
4. ⛔ **未修任何既有 FAIL**（結構閘另案、`W-G G.1` 之 `KeyError: '0m'`）。
5. ⛔ **未改三支複本原檔**——其「全鏈端到端跑通」之原始命題**保留**，作為另案修好後之終驗
   （`git diff --numstat` 對三檔**空輸出**）。
6. ⛔ **未動生產碼**（`app.py` `git diff --numstat` **空輸出**）。
7. ⛔ **未處理**環境耦合第 6 項（traceback 後之無縮排警告會被納入受詞）。
8. 🔴 **洞 A（寄生失聲）仍在**——本批未改 `run_verification.py`。

---

## §7 後批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **0** | 🛑 **`K5` 判準之裁定**（「未新增」取代「歸零」） | 不裁則此後每批之錨稽核都會卡在同一個不可達之 0 |
| **1** | **`W-G G.1` 之上游 `_ctx["0m"]`** | 一崩即使 **69 行**判定式（含 W-8 反向閘）全數失聲；且三支複本將因此跑得更遠 ⇒ 新夾具依設計轉紅 ⇒ 屆時一併更新凍存 |
| **2** | **`W-F F.3` 之閘** | 確定無人接手；死站點含守恆／位次序類命題 ⇒ 土地後果直接 |
| **3** | **`W-D.3 碎片` 之閘** | 確定無人接手；影響 W-D.4 四梯與遞補 |
| **4** | **`W-G G.2` 之閘** | 確定無人接手；屬呈現層契約 |
| **5** | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」，無人接手者另立不受該上游影響之閘 |
| **6** | **解除明細截斷（12 列上限）** | 可消滅該盲點，但**必使現行名單全面失效、須重凍** ⇒ 須與序 1 之重烤時點協調 |
| **7** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

⚠️ 序 1／6 之前置含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §8 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG98_runall.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG99_runall.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG99_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG99_runall.log ⇒ 22
$ grep -n "fixture_e2e_termination" verify/out/WG99_runall.log
43:  ✅ 末端夾具 fixture_e2e_termination.py（🆕 W-G.9-8/9-9 …受詞已投影去呈現層…）rc=0

$ python verify/tools/wg99_anchor_audit.py 130 25 | head -2
掃描檔數 955｜非 UTF-8 而改以 latin-1 納入者 19 檔（皆具名）
引用總數 57｜以 P=130、N=25 判：**失準 28** ＝ 活文件 **5** ＋ 歷史記錄 23

$ grep -c "^| BASELINE |" CLAUDE.md                            ⇒ 1
$ grep -c "SIDE_LINE 中點到該筆靠街角側地界線的垂距" CLAUDE.md   ⇒ 1
$ grep -c "BASELINE" CLAUDE.md                                 ⇒ 7   ← 判別力自檢

$ git diff --numstat -- app.py verify/run_verification.py \
      verify/wg_g1_smoke.py verify/wg_g2_smoke.py verify/wg_g3.py   ⇒ （空輸出）
$ git diff --numstat -- CLAUDE.md                                    ⇒ 25  0
```

⚠️ **依倉規 B，本報告⛔ 不記任何可變檔之雜湊**——凍存物之本體雜湊記於**其檔頭**，
驗證指令見 §5。

### 8-1 🔴 自誌：**壞掉的稽核器吐出的，正好是我期望的那個值**

把稽核器由 scratchpad 移入 `verify/tools/` 時，`REPO` **少取一層 `dirname`**
⇒ 母體縮成 `verify/`（掃描 680 檔·引用 14）⇒ 輸出 **活文件失準 ＝ 0**
——**恰為 `K5` 之期望值**。若未與移入前之輸出對拍，這會直接變成一則**假綠**。

已修正並加入**母體自證**（`assert os.path.exists(REPO/CLAUDE.md)`），
入倉版現與 scratchpad 版**逐字覆現**（`diff rc=0`）。⇒ 已立**失敗考古節 69**。

---

## §9 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `31c04c2` — 事前登記（⛔ 零碼、零量測；含投影定義、放棄清單、倉規草案）
- **B** `9c1d3e8` — `Q-1` 合成驗證 ＋ 投影實作 ＋ 兩途徑重凍 ＋ 夾具聲明 ＋ 長跑 log
- **C** 本報告 ＋ `CLAUDE.md` 倉規升格 ＋ `GB` 登記 ＋ 考古節 69
