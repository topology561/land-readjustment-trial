# `W-G.9-4` expand 階段：**事前登記**（⛔ 查之前 commit·**零碼變更**）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`
**基準**：`6ab9e86ce449cd3bc1fd288c50ec81a8cb51d41f`
**批號現查未占用**：`grep -rn "W-G.9-4" . --exclude-dir=worktrees --exclude-dir=.git | wc -l` ⇒ `0`

> 🔴 **本批之最高原則：<u>行為不得改變</u>。**
> 任何使生產輸出改變之修改，**即使方向正確，本批一律不得落地**
> ——寫進報告之「`W-G.9-5` 待辦」。
> 🔒 這是 expand-contract 之 **expand** 側：**加路、不換路**。

---

## §0 「已跑／未跑」聲明

**已跑（本登記之前·指令與輸出並列·皆本次重跑）**：

```
$ grep -n "_W_prev_left = (_left_buffer_S \* _cos_dn)" app.py
19571:                            _W_prev_left = (_left_buffer_S * _cos_dn) if _has_left_corner else 0.0
$ grep -n "'b': _left_buffer_S \* _cos_dn" app.py
19788:                                 'l1': _lside_left, 'b': _left_buffer_S * _cos_dn},
$ grep -n "_mp_base_W0(corner_pt, _left_buffer_S" verify/stepg_pipeline.py
821:            _b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block)
$ grep -n "def _mp_base_W0" verify/stepg_pipeline.py
801:            def _mp_base_W0(_gs, _buf, _dv, _mp, _adir):
$ grep -n "MP → 本宗" docs/rulings/K-6_街角地分配程序與可分配判準.md
793:- 🔒 **`W_i` 之量測不變**：仍為 **intrinsic 直量**（MP → 本宗**遠側**分配界線之垂距）
$ git log --format=%h 78079fa..HEAD -- app.py verify/stepg_pipeline.py | wc -l
0
```

⇒ **生產碼自 `78079fa` 起 0 筆變更** ⇒ `verify/out/D2b24_runall.log` **為 `E4` 之有效基準**。

🔴 **未跑（本登記之受詞）**：⛔ 尚未實作任何產生者、尚未改動任何一行生產碼；
⛔ 尚未追 `_select_pool_slot` 之 `'b'` 至其消費端（＝ `E5` 之判定）。

---

## §1 `W_0` 期望表（⛔ 事前寫死·逐格）

`W_0 = dot(gs − mp, â_定向)`（＝正典**權威序第 1 級** `K-9-5-6` 之「首宗起點 `W_0` ＝ MP→corner」）。

| 街廓/側 | `W_0` | 街廓/側 | `W_0` |
|---|---|---|---|
| R1/left | `+0.161877` | R4/left | `−0.787529` |
| R1/right | `+0.200569` | R4/right | `+0.651126` |
| R2/left | `−1.693924` | R5/left | `−1.640252` |
| R3/right | `+1.691802` | R6/right | `+0.000001` |

⚠️ **本表為街廓級**（不隨退縮情境變）⇒ 16 格之 `E2` 判定以此 8 值對兩情境各驗一次。
🔒 **`R6/right` 為退化格**（`SIDE ∥ ALLOC`·`W_0 ≈ +1e-6`）：**須正面列出**，
⛔ **不得以其近零作為任何結論之通過憑據**。

---

## §2 可證偽預測

| # | 預測 | 證偽方式 |
|---|---|---|
| **E1** | 新產生者之輸出 **≡** `stepg` 現行 `_mp_base_W0` 之輸出（逐位） | 合成對拍殘差 > 該處既有探針原式之閾 ⇒ **E1 破** |
| **E2** | 新式 − 舊式（族②③）逐格 **＝** §1 表之 `W_0` | 任一格不符 ⇒ **E2 破** |
| **E3** | 本批落地後 `app.py` 之生產輸出**零變化** | 任一 G 值／面積／抵費地與 `6ab9e86` 不同 ⇒ **E3 破 ⇒ 立即停機** |
| **E4** | 本批落地後 `run_all` 之 PASS/FAIL 層與 `6ab9e86` **逐項相同** | 任一項判定改變 ⇒ **E4 破 ⇒ 立即停機** |
| **E5** | `_select_pool_slot` 之 `'b'` 參數，其語意**確為 W**（垂距·供 Rw 或其等價用途） | 現查其消費端顯示 `'b'` 係他量 ⇒ **E5 破 ⇒ 停機上呈** |

---

## §3 停機條件（⛔ 逐字採施工單 §6）

**真域邊界 ⇒ 停機上呈：**

- **S-1**：**E5 破**（`'b'` 之語意非 W）⇒ 族②③ 之歸類本身有誤 ⇒ **停機**，
  ⛔ 不得逕改該處，並把正確歸類寫入報告待重新定式。

**⛔ 立即停機（非域邊界，但屬紅線）：**

- **S-2**：**E3 或 E4 破**（生產行為改變）⇒ **立即停機**並**回退該變更**；
  ⛔ 不得以「新值才是對的」為由保留——**本批之最高原則是行為不變**。

**純技術項 ⇒ 自行做完至 push：**

- `E1`／`E2` 破 ⇒ 於報告正面列出並附殘差；若 `E2` 破，須逐格列出實測差與期望 `W_0` 之對照。
- `GB-47` 條目加註本批之 expand 進度（⛔ 原列不刪）。
- 探針、log、報告、合成測試之一切撰寫。

---

## §4 驗收（⛔ 逐字採施工單 §7·禁以計數或列數為證據）

| # | 閘 |
|---|---|
| 1 | **16 格**皆正面列出 `E2` 之實測差與期望 `W_0`，含不觸發格與退化格（`R6/right`） |
| 2 | `E1`／`E2` 之證據**來自合成測試**；⛔ `run_all` 結果不得充作 `main()` 內敘述之證據 |
| 3 | `E3` 之證明：`app.py` 生產輸出與 `6ab9e86` 之對拍，須列出**對拍方法**與殘差 |
| 4 | `E4` 之證明：`run_all` PASS/FAIL 層逐項對照，⛔ 不得只報總數 |
| 5 | 新產生者**確為 module 級**（以 AST 證其非巢狀於 `main()`） |
| 6 | 全倉 `cos_dn`／W 直量之定義**未因本批增加第三份**（附語意窮舉之射程三項） |
| 7 | 每一出艙之數，其指令於**落筆當次重跑**且指令與輸出並列 |
| 8 | 每一否定性宣稱附【目錄射程】【字樣射程】【連接方式】 |
| 9 | `git diff --numstat` 對 `verify/wf_f*.py` **為空輸出**；對 `app.py` 之**刪除欄為 0** |
| 10 | 文件之更正一律加註，**刪除欄為 0** |

---

## §5 已預先承諾之判準（⛔ 事後不得放寬）

- 🔴 **行為不變為最高原則**：⛔ 不得為「順手修對」而改變任何生產輸出（§0-6／§2-7）。
- ⛔ **不得切換族②③ 之生產消費**——`_W_prev_left/right` 與 `_select_pool_slot` 之 `'b'`
  於本批**仍取舊式**；⛔ 不得以任何條件分支讓新值進入生產路徑。
- ⛔ 不動 `_corner_buffer_S`／`buf`／`_build_corner_range_v3`／`GB-58`／`verify/wf_f*.py`／基準 CSV。
- 否定性宣稱須**同時**寫【目錄射程】【字樣射程】【連接方式】；字樣須語意窮舉、
  ⛔ 不預設符號名；⛔ 不以固定距離窗 `.{0,N}` 連接兩概念。
- 凡書寫「正典」須就地標**權威序層級**（`CLAUDE.md:4` 共 **4 級**）。
- 判準之鍵一律為 **(情境 × 街廓 × 側別)**；⛔ 不用宗序號。
- `E5` 之判定 ⛔ **不得由參數名推定**——須以內容 grep ＋ AST 追至**消費端**。

🔒 **自誌（`CLAUDE.md`「立戒者在下一批最容易違反自己的戒」）**：
`W-G.9-3` 新立之「否定性宣稱須同附目錄／字樣射程」與「不預設符號名」二戒，
**本批須逐條自檢並正面列出『已套用』**——⛔ 尤其 §4 之閘 6（「未增加第三份定義」）
本身即為一**否定性宣稱**，⛔ 不得以單一字串 grep 交差。
