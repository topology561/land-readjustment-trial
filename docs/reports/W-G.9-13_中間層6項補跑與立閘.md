# 【W-G.9-13 全單報告】中間層 6 項補跑與立閘 ＋ `_w4` 歸類

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`d9f7659`　**事前登記**：`docs/reports/W-G.9-13_前置登記.md`（commit `d61cef5`）
**字樣錨（倉規 A·唯一命中）**：`【W-G.9-13 全單報告】`

> # ✅ **`T1`〜`T7` 全部成立；`A-1`〜`A-5` 皆未觸發。**
>
> 🔑 **三個核心結果**：
> ① **中間層其餘 5 項全部補跑完成**（`5／5`），且**六項斷言現況皆綠**
>    ⇒ 🔴 **`T4` 未破 ⇒ 無紅燈 ⇒ ⛔ 未觸發 §2-4 之停機上呈**。
> ② **6 項已立閘**（`verify/fixture_midlayer_6items.py`·掛入 `run_all`），
>    判準**自原碼 AST 抽取後就地求值**、⛔ 零常數抄寫。
> ③ **`W-D.4 MinA_區 reverse-test` 歸類定案 ＝「真需 `run_step_g`」**
>    （構造 ＋ 實跑雙證，附對照組判別力自檢）⇒ `W-G.9-11` 之**唯一未定項**結案。
>
> 🔴 **兩項現查推翻既有敘述**（詳 §2、§7）：
> ⓐ 施工單 §2-4 稱「這六項之錨**從未被評估過**」——**與倉內不符**，
>    正確為「**曾綠、後因上游結構閘另案破而停止執行**」（82 份歷史 log 之判定行為 `✅ PASS`）。
> ⓑ 六項判準之碼面**第二元素皆為 `Name`** ⇒ 照 `W-G.9-10` 之 `G-2` 內嵌式外推
>    會得 **6 個空閘**——**已實跑實證**，⛔ 非推想。

---

## §1 `T1`〜`T7` 逐項結果

| # | 判 | 依據（皆本報告落筆當次重跑·詳 §8） |
|---|---|---|
| **T1** | ✅ **成立** | 其餘 5 項**逐項各自**實跑，`5／5` **跑得完**；⛔ 未以主樣本外推、⛔ 未以「同在財務段」推定 |
| **T2** | ✅ **成立** | 四型判準之同源抽取皆成功，抽出之受詞**逐型非空**；並**實證**外推式之受詞全空且不可求值 |
| **T3** | ✅ **成立** | 六閘之竄改自檢**兩態異色**（原態綠／竄改態紅） |
| **T4** | ✅ **成立** | 六閘現況**全綠** ⇒ ⛔ **無紅燈**、未取「過期 vs 算錯」證據、未上呈 |
| **T5** | ✅ **成立** | 判準複本逐鍵 **7／7 為 0**（附**注入真複本**之判別力自檢 7／7 抓得到） |
| **T6** | ✅ **成立** | `wd4_tier_list.compute(fixture=False)` 之構造與實跑**一致指向「需 `run_step_g`」** |
| **T7** | ✅ **成立** | 判定行 `diff` **rc=0**（`PASS 12／FAIL 22`／對帳 PASS）；唯一之差 ＝ 新夾具之列（`rc=0`） |

🔒 依**考古 69 修法 ④**，上列**每一項**皆附判別力自檢——⛔ 未只證其成立，**已證其會不成立**。

---

## §2 🔴 一項前提更正：這六項**不是**「從未被評估過」

施工單 §2-4 之前提為「這六項之錨**從未被評估過** ⇒ 首次評估，紅的機率不低」。
**本批於 §0 現查階段即發現其與倉內不符**（已於**事前登記** §2 正面登記，⛔ 非事後發現）：

```
$ grep -rl "v3 率用 DXF 公設實值" verify/out/ | wc -l          ⇒ 84
   逐檔取其判定行 ⇒ 82 檔為 `✅ PASS  v3 率用 DXF 公設實值 12146.5579`
$ grep -rl "v3 重劃總負擔率錨" verify/out/ | wc -l              ⇒ 84
$ grep -rl "v3 率分項錨" verify/out/ | wc -l                    ⇒ 84
$ grep -rl "v3 reverse-test③" verify/out/ | wc -l               ⇒ 85
$ grep -rl "F.2 跨區段 fixture" verify/out/ | wc -l             ⇒ 44
$ grep -rl "F.4 模式二 p_avg" verify/out/ | wc -l               ⇒ 10
🔒 判別力自檢：`✅ PASS` ⇒ 95 檔｜`ZZZQQQ不存在` ⇒ 0 檔（⇒ 上式非恆命中、非恆 0）
```

⇒ **正確之陳述**：這六項**曾被反覆評估且曾為綠**，
是**在上游結構閘另案破掉之後才停止執行**——⛔ **非「從未被評估」**。

🔒 **對本批之影響（⛔ 未改任何工項）**：`T4` 之先驗因此上升；本批實測**確為全綠**，與之相符。
⚠️ **反面之意義更重要**：若 `T4` 曾破，倉內即多一類證據可用
——「**最後一次為綠之 log**」與「**其後之觸及史**」⇒ 可定位**何時翻**，而不只是「不符」。
**該證據自此登記在案**，供後批（尤其解阻後之重烤判定）取用。

---

## §3 甲組：其餘 5 項之逐項補跑（`T1`）

**產物**：`verify/out/probe_WG913_midlayer.log`（⛔ **每項各自一個 `try`**·**考古 70**）。

| 名目字面 | 判準型 | 所需管線層 | 跑得完 | 實跑值 | 斷言 |
|---|---|---|---|---|---|
| `v3 重劃總負擔率錨 == ` | ② | 層 B | ✅ | 錨 `0.40712387`｜現算 `0.40712387`｜Δ `-0.0000000003` | ✅ 綠 |
| `v3 率分項錨 公設負擔比 ` | ① | 層 B | ✅ | 公設負擔比 `34.754211%`／費用負擔比 `5.958176%` | ✅ 綠 |
| `v3 率用 DXF 公設實值 ` | ① | 層 B | ✅ | `12146.5579`｜Δ `+0.000027`（容差 `5e-4`） | ✅ 綠 |
| `F.2 跨區段 fixture（a→b 模式一 a′=` | ③ | 層 C | ✅ | `a=100.0`／`mode1_a_to_b=117.6471`／`expect_m1=117.6471`／`ratio_ne_1=True` | ✅ 綠 |
| `F.4 模式二 p_avg 母體＝原始 build_parcels（R1/R4 可算）` | ④ | 層 C | ✅ | `p_avg` 鍵 `['R1','R2','R3','R4','R5','R6']`｜`R1=41177.19518498675`｜`R4=42340.39024977705` | ✅ 綠 |

⚠️ 名目 4（`v3 reverse-test③`）已於 `W-G.9-12` 實跑 ⇒ **本批不重跑**、亦不計入 5 項。

### 3-1 判別力自檢（⛔ 逐項·考古 69 修法 ④）

| 項 | 自檢 | 結果 |
|---|---|---|
| 1 | 餵壞輸入（`cb=None`）於其生產路徑 | 實拋 `TypeError` ⇒ 「跑得完」非恆真 |
| 2 | 擾動（單位工程費 `3500→3550`）⇒ 分項須**改變** | 費用負擔比 `5.958176% → 6.027564%` ⇒ 證為**現算**、非常數 |
| 3 | 以**截圖 rounded** `12146.56` 代入同一容差式 | `\|Δ\|=0.002100` ⇒ **轉偽** ⇒ 容差非鬆到無鑑別力 |
| 5 | 餵壞 `snap`（空 dict） | 實拋 `KeyError` |
| 6 | 以**必不存在**之塊名代入同一 `all(...)` 式 | **轉偽** ⇒ `all` 非空集恆真（母體規模 `\|p_avg\|=6`） |

### 3-2 🔑 事前登記 §1 之未知，已由**實跑**判定

**`build_build_parcels` ⛔ 不需 `build_ownership` 先行**
（首試即成功、無例外 ⇒ 無須第二次嘗試）。產出 `build_parcels 61 筆`／`temp 126 筆`。
⇒ 事前登記所列「🔴 是否需 `build_ownership`」之未知**結案**，⛔ 未以推定填補。

---

## §4 乙組：6 項立閘（`T2`／`T3`／`T4`／`T5`）

**落點**：`verify/fixture_midlayer_6items.py`（**新檔**）＋ 掛入 `verify/run_all.py` 末端夾具。

> ⚠️ **與事前登記之偏離（⛔ 具名記帳）**：事前登記 §3 之落點寫為
> `verify/fixture_midlayer_v3rate.py`（該處已註明「登記時之設計·若實跑推翻則照實報」）。
> **改名為** `fixture_midlayer_6items.py`，**理由**：該檔實際涵蓋 `F.2`／`F.4` 兩項非率錨之判定，
> 沿用 `v3rate` 之名即**當場製造**一個與 `fixture_g3_static_guards.py` 同型之誤導檔名。
> ⇒ **判準未改、受詞未改、範圍未改**；所異者僅檔名。

### 4-1 🔴 四型判準之處理（⛔ 分型列出·每型附受詞與其非空之證據）

| 型 | 名目 | **抽出之受詞** | 非空 | 處理方式 |
|---|---|---|---|---|
| ① 內嵌數值字面 | `v3 率分項錨` | `[34.754211, 5.958176, 6, 6, 100, 100]` | ✅ | 自 AST 抽取後**就地求值**（⛔ 不抄常數） |
| ① 內嵌數值字面 | `v3 率用 DXF 公設實值` | `[0.0005, 12146.5579]` | ✅ | 同上 |
| ② `_anc` 字典查值 | `v3 重劃總負擔率錨` | `_anc` 之鍵 `['重劃總負擔率']` | ✅ | **同源讀取**：求值時之 `_anc` ＝ `snapshot["財務接線_v3"]["斷言錨"]` **本體**，⛔ 非抄值 |
| ② `_anc` 字典查值 | `v3 reverse-test③` | `_anc` 之鍵 `['重劃總負擔率']` | ✅ | 同上；其**擾動幅度**亦自同一 `try` 同源抽取（⛔ 不寫死） |
| ③ 自參照·無外部錨 | `F.2 跨區段 fixture` | 原式之正規化原始碼（`✅ 與凍存逐字相同`） | ✅ | **原式文字同源自證**（原式一變即 loud 紅）＋ 就地求值 |
| ④ 名單型 | `F.4 模式二 p_avg` | 字串 tuple `('R1', 'R4')` | ✅ | 自 AST 抽取後就地求值 |

🔒 **同源之證（型②·⛔ 非抄值）**：夾具內 `重劃總負擔率` 之命中 ＝ **0**
（扣除必須持有之**名目字面**後；見 §4-3）——該鍵**只存在於 `run_verification.py` 之原碼**，
夾具是**在求值時才從原碼取得它、再拿去索引真快照**。

### 4-2 🔴 `A-4` 陷阱之實證（**第三種碼面形狀**·⛔ 非推想）

`W-G.9-10` 之 `G-2` 受詞為**內嵌式**（`results.append((f"…", <expr>, …))`）；
`W-G.9-12` 現查 `F.2` 之靜態閘多繞一個中介變數（**第二種**形狀）。
**本批現查：這 6 項之第二元素<u>皆為 `Name`</u>**（`_ok_rate`／`_ok_bd`／`_ok_dxf`／`_ok_rt3`／`_ok_fx`／`_m2ok`）
——**第三種**。⇒ 須**再解析其 `Assign`** 才取得判準式。

**若照 `G-2` 形狀外推會怎樣（實跑·`verify/out/probe_WG913_gate_selfcheck.log`）**：

```
▸ 六項之外推式各抽到 '_ok_rate'／'_ok_bd'／'_ok_dxf'／'_ok_rt3'／'_ok_fx'／'_m2ok'
   受詞（常數／`_anc` 鍵／tuple）＝ ([], [], ())   ← **全空**
   就地求值 ⇒ RuntimeError: 自由名未綁定 ['_ok_*']  ← **不可求值**
⇒ 6／6 項 ⇒ 若照抄，這 6 個閘會是 **6 個空閘**
```

🔒 **抽取式非恆成功**：對 `ZZZ_不存在之名目` ⇒ **實拋 `RuntimeError`**；
對**存在**之對照組 `v3 財務錨 B ==` ⇒ 抽到 `round(_fin["B"], 6) == float(_anc["B"])`
⇒ 「抽得到」**非恆真句**。

🔒 **一份抽取邏輯**（⛔ 未產生第二份）：`orig_criterion()` **一份**吃兩種形狀
（內嵌式／`Name` 再解析），其後之型別差異全部落在**讀取器**
（`numbers`／`keys_under`／`string_tuple`／原始碼文字）——**皆讀同一次抽取之節點**。

### 4-3 `T5`：判準複本 ＝ 0（逐鍵 ＋ **兩重**判別力自檢）

| 鍵 | 型 | 夾具內命中 |
|---|---|---|
| `重劃總負擔率` | ② | **0** ✅ |
| `34.754211` | ① | **0** ✅ |
| `5.958176` | ① | **0** ✅ |
| `5e-4` | ① | **0** ✅ |
| `12146.5579` | ① | **0** ✅ |
| `('R1', 'R4')` | ④ | **0** ✅ |
| `"R1", "R4"` | ④ | **0** ✅ |

- 🔒 **判別力自檢①**（母體未壞）：`orig_criterion` ⇒ 2、`eval_criterion` ⇒ 4（皆 >0）。
- 🔒 **判別力自檢②**（真有複本抓得到）：於扣除後之母體**注入各鍵之真複本**
  ⇒ **7／7 鍵**之計數 `+1` ⇒ 扣除步驟**未把母體挖空**。
- 🔴 **母體須先扣除「名目字面」**——本批自檢一度**誤報**：判準鍵 `重劃總負擔率`
  恰為名目字面 `v3 重劃總負擔率錨` 之**子字串**，而名目字面是**定位用 prefix**、夾具必須持有。
  尚須扣其**呈現形**（docstring 內寫 `v3 重劃總負擔率錨`、無尾綴 ` ==`）。**已入考古節 71**。
- ⚠️ **型③ 之明示例外**：`_F2_EXPR_FROZEN` 存有原式之正規化文字
  （施工單 §2-2 型③ 所訂之同源自證手段·與 `fixture_g3_static_guards.py` 之 `G-2` 同型）。
  🔒 其內**無任何外部錨值**——容差 `0.01` 與期望值 `expect_m1` **皆屬原式自身**。

### 4-4 `T3`：竄改自檢（**兩態皆印**·⛔ 每閘皆有）

| 名目 | 原態 | 竄改態 |
|---|---|---|
| `v3 重劃總負擔率錨` | ✅ 綠 | `_rate_live` `+1.0` ⇒ **轉紅** ✅ |
| `v3 率分項錨` | ✅ 綠 | `_bd['公設負擔比']` `×2` ⇒ **轉紅** ✅ |
| `v3 率用 DXF 公設實值` | ✅ 綠 | `_bd['公設共同負擔_DXF']` `×2` ⇒ **轉紅** ✅ |
| `v3 reverse-test③` | ✅ 綠 | `_rate3` 改為與 `_rate_live` 相同（＝率未隨動）⇒ **轉紅** ✅ |
| `F.2 跨區段 fixture` | ✅ 綠 | `_fx['mode1_a_to_b']` 改為 `expect_m1+1.0` ⇒ **轉紅** ✅ |
| `F.4 模式二 p_avg` | ✅ 綠 | `_pavg` 移除 `'R4'` ⇒ **轉紅** ✅ |

🔒 **受詞必被執行之現查**（⛔ 不以「沒報錯」代替）：夾具末列印
`已求值之判準式 ＝ 6／6`；若 `< 6` 即 `rc=1`。

### 4-5 🔒 四項聲明（⛔ 逐條·第 2、4 項不得留空）

**逐字寫於夾具 docstring**（⛔ 本報告不重述其全文，摘其判斷）：

| # | 答 |
|---|---|
| **1. 守哪個子集？** | **六項之判準式全部**——⛔ 未縮受詞；自由名皆由**自建之真管線**產生 |
| **2. 🔴 放棄哪個子集？後果？** | 同 `try` 內**其餘**主判定（`v3 財務錨 B`／`C`／`中繼錨`／`C 兩形等價`／`A 逐宗全查`／`reverse-test①②`）＋ `F.2`／`F.4` 段其餘主判定 ⇒ **完全未守**。其中 `A 逐宗全查`／`reverse-test①②` **實需** `run_step_g` ⇒ 現況**不可能**由本夾具覆蓋 |
| **3. 與 `except` 分支重疊？** | ⛔ **不重疊**。`except` 之受詞 ＝ **compute 失敗之訊息**（`W-G.9-7` 已凍存）；本閘 ＝ **命題本身之真偽** |
| **4. 另案修好後會怎樣？** | **仍綠**（受詞與 `run_step_g` 無關 ⇒ ⛔ 不會假紅）；屆時原判定復活 ⇒ **應併同檢討本夾具是否退場** |

---

## §5 丙組：`_w4` 之歸類（`T6`·⛔ 只歸類·**未立閘**）

**產物**：`verify/out/probe_WG913_w4_class.log`。

- **`_w4` 之來源**：`run_verification.py` 之 `import wd4_tier_list as _w4`（**模組匯入**，⛔ 非管線產物）；
  消費式 ＝ `_w4.compute(fixture=False)`。
- **① 構造**：`compute()` 內具名呼叫 **26** 個，**含 `run_step_g`** ⇒ `True`。
- **② 實跑**：實拋 `RuntimeError`，traceback **落在 `run_step_g` 之內** ⇒ `True`
  （訊息即 §2-1 之結構閘另案：`🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02`）。
- 🔒 **判別力自檢**（對照組 `wf_f2.fixture_cross_zone`·已於甲組證實不需 `run_step_g`）：
  構造 `False`／實跑**跑得完** ⇒ **本判法會區分**，⛔ 非對誰都答「需」。

> ## 【歸類】`W-D.4 MinA_區 reverse-test(改深度→隨動·非寫死)` ＝ **真需 `run_step_g`**
>
> ⇒ ⛔ **非**「只被順序擋住」、⛔ **非**「中間層」
> ⇒ **【裁定 B】之擴充不適用於本項**；⛔ 本批**未為其立閘**（施工單 §3 明文未授權）。
> ⇒ `W-G.9-11` 之**唯一未定項**自此結案。

---

## §6 `T7`：長跑（`verify/out/WG913_runall.log`）

```
$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG912_runall.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG913_runall.log)   ⇒ rc=0（逐字相同）
$ grep -c '✅ PASS' verify/out/WG913_runall.log   ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG913_runall.log   ⇒ 22
$ grep -c '對帳 PASS' verify/out/WG913_runall.log ⇒ 1
$ diff <(grep "末端夾具" …WG912…) <(grep "末端夾具" …WG913…)
  12a13
  >   ✅ 末端夾具 fixture_midlayer_6items.py（🆕 W-G.9-13 中間層 6 項…）rc=0     ← **唯一之差**
```

- 🔒 **判別力自檢**（⛔ 兩式並列·**其一不具鑑別力，據實記**）：
  - ✅ **人造改動**：把 `WG913` 之首行 `🔴 FAIL` 改為 `✅ PASS` ⇒ `diff rc=1` 且逐行指出 ⇒ **具鑑別力**。
  - ⚠️ **對照舊批 log**（`WG94`／`WG97`／`WG98`／`WG99`／`WG910`／`WG911`）⇒ **皆 rc=0**
    ⇒ **該對照不具鑑別力**——⛔ 非「本批無變化」之額外佐證，而是**那幾批本就零行為變更**。
    據實列出，⛔ 不冒充為證據。
- 末端夾具支數 **12 → 13**（`grep -oE '\("fixture_[a-z0-9_]+\.py"' verify/run_all.py | wc -l`）。

---

## §7 🔴 本批之兩項自誌（⛔ 皆由**自查**抓出·⛔ 非事後美化）

| # | 症狀 | 若未察覺之後果 |
|---|---|---|
| **1** | 型④ 之 `all(b in _pavg for b in (…))` 於 `eval` 拋 `NameError: _pavg`，而 `_pavg` **確已綁定**——根因 ＝ `eval` 分傳 `globals/locals` 時，**genexp 另開 scope、自由名只查 `globals`** | 🔴 **最貴**：本閘之受詞為**重劃負擔比率**族，其紅燈依施工單 §2-4 屬**真域邊界 ⇒ 須上呈 KL**。若當時把求值寫成 `except: return False`（最自然之寫法），這個**純 Python scope 故障**會被記成「命題為偽」⇒ 產出一份「**負擔比率算錯、全區所有權人之分配面積受影響**」之**假警報**送到 KL 面前。⇒ **已入考古節 71** |
| **2** | 「判準複本 ＝ 0」檢查**誤報**——判準鍵 `重劃總負擔率` 恰為名目字面 `v3 重劃總負擔率錨` 之**子字串** | 會把**必須持有之定位字串**誤判為複本而去「修」它（結果是把閘的定位能力拆掉）；**反向亦成立**——真複本可藏在定位字串內而不被計數。⇒ 已入考古節 71 之副線 |

🔒 **共通**：二者皆因**「求值／檢查失敗時不得靜默」**之設計而暴露
——① 是 loud `RuntimeError` 而非 `return False`；② 是**逐鍵印出計數**而非只印總計。

🩸 **第三項（方法·非本批交付物）**：本批曾以 `checkout` **之前**所跑之 `grep` 結果
論斷失敗考古之節號，而該結果來自**另一個 commit 之工作樹**。
⇒ 已於現基準重跑覆核（`2231` 行／`70` 節，節 `70` 於 `2184` 行）。
**教訓**：**換基準之後，先前所有現查結果一律作廢**——⛔ 不得跨 `checkout` 沿用。

---

## §8 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ python verify/probes/probe_WG913_midlayer.py        ⇒ rc=0｜5 項中跑得完 ＝ 5／5｜斷言全綠 ＝ 是
$ python verify/fixture_midlayer_6items.py            ⇒ rc=0｜已求值之判準式 ＝ 6／6
$ python verify/probes/probe_WG913_gate_selfcheck.py  ⇒ rc=0｜陷阱實證 ✅｜抽取非恆成功 ✅｜判準複本 0 ✅
$ python verify/probes/probe_WG913_w4_class.py        ⇒ rc=0｜歸類 ＝ 真需 run_step_g

$ git diff --numstat d9f7659 HEAD -- app.py verify/run_verification.py verify/wv_reconcile.py \
      verify/baselines/ verify/wg_g1_smoke.py verify/wg_g2_smoke.py verify/wg_g3.py   ⇒ （空輸出）
$ git diff --numstat d9f7659 HEAD -- verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt  ⇒ （空輸出）
$ git diff --numstat d9f7659 HEAD -- verify/run_all.py                                 ⇒ 13  1
```

### 8-1 字樣錨之唯一性自檢（倉規 A）

```
$ grep -rl "【W-G.9-13 全單報告】" . --exclude-dir=.git --exclude-dir=__pycache__ | wc -l  ⇒ 1
$ grep -rl "【W-G.9-13 前置登記】" . --exclude-dir=.git --exclude-dir=__pycache__ | wc -l  ⇒ 1
🔒 判別力自檢：`全單報告` ⇒ 2 檔（非恆 1）｜`ZZZQQQ不存在之錨` ⇒ 0 檔（非恆 ≥1）
```

⚠️ **上列之「1」係本報告寫出<u>當時</u>之值**（同 `W-G.9-12` 對 `VR-00N` 之加註）：
此後每被一份文件引用即 `+1`。🔒 **這不影響定位**——**定義**該錨者始終只有各該檔一處。
⇒ **計數會長大、定位不會失效**；⛔ 計數型宣稱須連**時點**一起走。

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告**未書「正典」二字**作為斷言之依據；凡引權威層級處已就地標明
（`docs/rulings/` ＝ 權威序**第 1 級**；`docs/驗證裁定登記表.md` **不在**權威序四級之內）。

---

## §9 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未修改任何錨值**（`34.754211`／`5.958176`／`12146.5579`／`_fv3["斷言錨"]` 之任何鍵）。
2. ⛔ **未重烤任何基準**、⛔ 未重凍任何凍存物、⛔ 未改期望 FAIL 名單。
3. ⛔ **未為 `W-D.4 MinA_區 reverse-test` 立閘**（本批只歸類）。
4. ⛔ **未解除**明細截斷（12 列上限）、⛔ 未修任何既有 FAIL。
5. ⛔ **未動** `app.py`／`verify/run_verification.py`／`verify/wv_reconcile.py`／
   `verify/baselines/`／三支端到端複本（皆 `git diff --numstat` **空輸出**）。
6. ⛔ **未把任何裁定寫入** `docs/rulings/`。
7. ⛔ **未追改**歷史記錄之舊錨與舊雜湊；**文件刪除欄 ＝ 0**。
8. ⛔ **未守** §4-5 第 2 項所列之放棄清單（其中 `A 逐宗全查`／`reverse-test①②` 實需 `run_step_g`）。
9. ⛔ **未量測恆真性**（行覆蓋射程外·需變異注入類工具）。

---

## §10 後批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | **`W-G G.1` 之上游 `_ctx["0m"]`／結構閘另案** | **一修即解 65＋ 項之阻塞**；且本批已證 `W-D.4 MinA_區` 亦卡在同一處。⚠️ 修好後三支端到端複本會跑更遠 ⇒ `fixture_e2e_termination` 依**設計**轉紅（⛔ 非 regression） |
| **2** | **解阻後「基準過期 vs 算錯」之判定** | 🔴 **須 KL 裁**（⛔ CC 不自行判定是否重烤）。**本批新增可用證據**：`verify/out/` 之歷史判定行可定位「**最後一次為綠**」⇒ 由「不符」升級為「**何時翻**」 |
| **3** | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」；無人接手者另立**不受上游影響**之閘（本批之夾具即此型之第二例） |
| **4** | **解除明細截斷（12 列上限）** | 必使現行名單全面失效、須重凍 ⇒ 與序 1／2 之重烤時點協調 |
| **5** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |
| **6** | **本夾具與 `fixture_g3_static_guards.py` 之退場檢討** | 序 1 完成後原判定復活 ⇒ 同一命題兩處判定（雖同源）⇒ 應併同檢討 |

⚠️ 序 1／2／4 之前置含**本批不得處置之另案**與**待 KL 裁之項**，⛔ 不得逕自開工。

---

## §11 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `d61cef5` — 事前登記（⛔ 零碼、零量測）　`基準 d9f7659 ⊂ A` ✅
- **B** `2fd19f0` — 5 項補跑 ＋ 立閘 ＋ `_w4` 實跑 ＋ 掛入 ＋ 長跑 log（⛔ 零文件結論）　`A ⊂ B` ✅
- **C** — 本報告 ＋ `GB` 進度列 ＋ 裁定登記表加註 ＋ 失敗考古節 71
