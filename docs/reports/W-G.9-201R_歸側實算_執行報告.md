# 🧾 `W-G.9-201R` 執行報告：歸側實算探針（`K-9-24` **二**之四分支）

> **執行窗**：CC　**基座**：`origin/wip/s1-endpart` ＝ `8020d3d8a00acc82d8e2e1d4f145a2bca050efc4`（`ls-remote` 實跑）
> **性質**：🟢 **零生產碼**（新增 `verify/probes/` 唯讀探針 ＋ 其 `stdout` log·⛔ 動 `app.py` 一字·⛔ 動任何**既有** `verify/**.py` 一字）
> **情境**：退縮 `SB` ＝ `3.50` m（源 ＝ 環境變數 `WV_SB`·⛔ 硬編·`R-1`）／`python 3.13.11`
> **工作副本**：主 checkout（KL 裁 `2026-09-01`）；一切 `git` 以 `git -C` 執行

🛑 **本報告之數⛔ 得逕作為任何裁之依據**（單 `§三` 逐字）——其係**診斷**，須經發單側獨立復驗、並由 KL 認可後方得進入 `K-9-24` 之實例段或任何生產碼。
🛑 **本報告⛔ 鑄任何號**（`自誤`／`GB`／`VR`／`K-9`／`戒` 皆未動）。
🔒 一切計數皆於同段具名 ① 母體 ② 框（`列`／`檔`／`宗`／`群`／`街廓`／`格`／`㎡`／`B`）③ 受詞。

---

## `§零`　開工閘

| 閘 | 實測 | 判 |
|---|---|---|
| `S-0a`① | `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** ＝ 單載 | ✅ |
| `S-0a`② | `git diff --name-only -z 7fab020..HEAD` ＝ **`13`【檔】**（於 `fb494f5` 時之實測）；`^app\.py$`／`verify/.*\.py`／`\.py` 皆 **`0`**【列】；判別力對照 `\.md` ＝ `11`【列】 | ✅ |
| `S-0b` | 🔴 **`K-9-24` 已在倉**：`grep -c '^### 🔒 K-9-24' docs/rulings/K-6_…md` ＝ **`1`**【列】；判別力對照 `^### 🔒 K-9-25` ＝ **`0`**【列】 ⇒ 量測器非紅 | ✅ |
| `S-0c` | `probe_order_preflight.py` 對本單單檔：🔴 機械停機款 **`0`【項】**；🟡 **`1`【項】** ＝ `[P-4]:49`（`§二` 表頭）＝ 單 `§五` 之具名豁免、⛔ 表外之 🟡 | ✅ |
| `S-0d` | `SELF_SHA256` 實算 ＝ 單載 ＝ **`6de9298f1ce3dda0…`**（受詞 `9326` B／全檔 `9404` B·`98`【列】·`CR` `0`） | ✅ |
| `S-0e` | 見 `§五` 之呼叫界線 | ✅ |

🔑 **`S-0c` 之判別力自證**：同器對 `W-G.9-197` ＝ `5`／`-198`（改版）＝ `6`／`-199` ＝ `0`／`-200`（新版）＝ `3`／本單 ＝ `1`【項】 ⇒ ⛔ 恆綠亦⛔ 恆紅。
🛑 **本單⛔ 改版**：於 `W-G.9-200` 改版之同一批次中，`-201` 之 `mtime` 雖同被觸碰（`23:31`），惟其 `bytes`／`列`／`SELF_SHA256` 與收單時**逐位相同** ⇒ **內容未變**。

---

## `§一`　落地物

| # | 檔 | bytes | `\n`【次】 | `CR`【次】 | `sha256`（全檔） |
|---|---|---|---|---|---|
| `L-1` | `verify/probes/probe_WG9201_side_assign.py` | `27704` | `520` | **`0`** | `12bbf06a665dbb8ff673d1840cf814509a40ef7b2adca8946e827784a404d167` |
| `L-2` | `verify/out/probe_WG9201_side_assign_8020d3d8.log` | `12106` | `125` | **`0`** | `b93121cff8dfbe5c1c3a28063b82f9fd741f0857007293b6d6cc58683304e33b` |

🔒 二檔皆**純 `LF`** ⇒ 走**常規 `git add`**（⛔ `-c core.autocrlf=false`·`W-G.9-199R §六-5` 之行尾分流；該旗標僅用於**純 CRLF** 之 `KL_UI_*_stdout.log` 族）。
🔒 **`R-9` 之自證**（`stdout` 全文須可直接存檔·⛔ 得截斷）：`stdout` `12231` B／`CR` `125`／`LF` `125`；`L-2` `12106` B／`CR` `0`。**`12231 − 12106 = 125 = 列數`** ⇒ 純 CRLF 算術簽章。**去 `CR` 後與 `L-2` 逐位相同 ＝ `True`**；**判別力對照**：注入一字元（`M 個數 = 1`→`2`）後判為**不同** ⇒ 對拍器⛔ 恆綠。
🩸 **併記**：本探針曾於態 `fb494f57` 先跑一趟（產 `probe_WG9201_side_assign_fb494f57.log`），其後因 `W-G.9-200` 增量 commit 使基座前移，且探針於其間補強二處（`範圍面積` 改自 `f3_corner_range_areas` 取＋加對拍閘、上鎖之「街廓數 vs 群數」二量分列），故**刪該中間 log 並於 `8020d3d8` 重跑**。該中間 log **從未入倉**（`git status` 之 `??`），⛔ 構成倉內證據之刪除。

---

## `§二`　結果態 `R-1`〜`R-9` 之實現與實測

### `§二-1`　三態（`R-7`：二態於**同一次執行**內產生·⛔ 跨執行拼接）

| 態 | 構造 | `ns is _projection_order.__globals__` |
|---|---|---|
| `A` | 現行倉態（`k6_step0_block_locked` 保守替代照舊生效）＝ **對照組** | `True` |
| `B` | `K-9-24` **一**：層疊 `ns["k6_step0_block_locked"] → 恆 False` ＝ **無條件步驟 0** | `True` |
| `C(M)` | 態 `B` ＋ 於 `v12` 之 `candidates` 扣除該 `M`（`K-9-24` **二·5**） | `True` |

🔒 **層疊點之依據**：`k6_step0_merge` 以**全域查找**呼叫 `k6_step0_block_locked`（`grep -n "if k6_step0_block_locked(side_lines_by_side, _lbl):" app.py`）；`run_corner_pk` 以 `ns["select_corner_lots_both_sides_v12"]` 取值（`grep -n 'v12 = ns\["select_corner_lots_both_sides_v12"\]' verify/selection_pipeline.py`）⇒ 覆寫 `ns[...]` 即生效。

### `§二-2`　🔒 自我驗證閘 ①：層疊生效（反靜默退路之「咬到」計數器）

| 受詞 | 態 `A` | 態 `B` |
|---|---|---|
| `blocks_locked_k921` | **`['R1', 'R4']`**（`2`【街廓】） | **`[]`**（`0`【街廓】） |
| 層疊被呼叫／其中原判為「鎖」而被強制放行 | — | `6`【次】／**`2`**【次】 |
| `groups_found`／`groups_merged` | `8`／`8` | **`9`／`9`** |
| `parcels_absorbed`／`parcels_in`／`parcels_out` | `12`／`126`／`114` | `13`／`126`／**`113`** |
| `blocks_buildable`／`anomalies` | `6`／`0` | `6`／`0` |

⇒ 🔒 **閘 ① ＝ `True`**（`A` 非空 ∧ `B` 空 ∧ 強制放行非零 ∧ `B` 之合併群數 > `A`）。
🔒 **`k6_step0_block_locked` 實際所擋之群**（＝ 僅態 `B` 有者）＝ **`['628(1)+']`**（`1`【群】·街廓 `R4`）；僅態 `A` 有者 ＝ `[]`。
🛑 **「上鎖之街廓數」（`2`）與「實際被擋之群數」（`1`）係二量、⛔ 互代**——`R1` 雖上鎖，其街廓內**無可合併之群**。⇒ `app.py` 之 `k6_step0_block_locked` 註解逐字「本判準…皆只擋 `R4` 之群」係就**群**而言，本探針實測**相符**；惟若讀為「只鎖 `R4` 一個街廓」則**與實測不符**（實鎖 `2` 個）。

**態 `B` 之合併宗（`9`【群】·逐筆）**

| 街廓 | 命名 | 成員 | gid | Σ幾何面積(㎡) | 聯集 |
|---|---|---|---|---|---|
| `R2` | `628-40(1)+` | `628-40(1)`, `628-43(1)` | `G010` | `513.7900` | Polygon |
| `R3` | `628-30(3)+` | `628-30(3)`, `628-45(2)` | `G007` | `1145.2600` | Polygon |
| `R3` | `628-47(1)+` | `628-47(1)`, `628-48(1)`, `628-49(2)` | `G006` | `628.9200` | Polygon |
| `R3` | `628-29(1)+` | `628-28(1)`, `628-29(1)` | `G014` | `228.9500` | Polygon |
| **`R4`** | **`628(1)+`** | **`628(1)`, `628-1(1)`** | **`G009`** | **`3199.7800`** | Polygon |
| `R5` | `628-21(2)+` | `628-21(2)`, `628-22(2)`, `628-23(2)` | `G017` | `2428.3100` | Polygon |
| `R5` | `628-45(1)+` | `628-20(2)`, `628-30(1)`, `628-45(1)` | `G007` | `636.1000` | Polygon |
| `R6` | `628(2)+` | `628(2)`, `628-1(2)` | `G009` | `240.2200` | Polygon |
| `R6` | `628-23(1)+` | `628-21(1)`, `628-22(1)`, `628-23(1)` | `G017` | `725.4500` | Polygon |

### `§二-3`　`R-3`／`R-8`　全域覆蓋與跨占判定（🛑 ⛔ 只報有 `M` 者）

**街廓總數 ＝ `11`**【街廓】／**可建築街廓數 ＝ `6`**【街廓】（母體 ＝ `classified_blocks`·可建築 ＝ `F3_CATEGORY_BURDEN[category] == '可建築土地'`）。
🔒 **跨占之定義（碼面·⛔ 本探針自訂）** ＝ 該宗進入該端之 PK 組，其充要條件 ＝ `_corner_intersection_area > 1.0 ㎡`（`E-1.7` 絕對地板·`grep -n "_has_p1 = _inter_p1_area > 1.0" app.py`）。

| 街廓 | 有無左 | 左跨占數 | 右跨占數 | 同時跨占兩側之宗（＝ `M`） |
|---|---|---|---|---|
| `R4` | 有 | `1` | `1` | **`['628(1)+']`** |
| `R6` | 無 | `0` | `2` | ⛔ 無 `M` |
| `R5` | 有 | `4` | `0` | ⛔ 無 `M` |
| `R2` | 有 | `4` | `0` | ⛔ 無 `M` |
| `R3` | 無 | `0` | `4` | ⛔ 無 `M` |
| `R1` | 有 | `3` | `1` | ⛔ 無 `M` |

⇒ **`M` 之個數 ＝ `1`**【宗】（母體 ＝ 態 `B` 之可建築街廓 `6` 個）。

### `§二-4`　`R-4`　逐 `M` 逐側之實算（分別以左／右街角**第 1 宗位置**試算）

**街廓 `R4`｜`M` ＝ `628(1)+`**（＝ `628(1)` ＋ `628-1(1)`·gid `G009`）

| 欄 | 【左】(`p1`) | 【右】(`p2`) |
|---|---|---|
| 真交集(㎡) | `225.76` | `225.19` |
| 整筆幾何(㎡) | `3199.78` | `3199.78` |
| 範圍面積(㎡) | `226.01` | `225.42` |
| 門檻(㎡) | `226.01` | `225.42` |
| **真G(㎡)** | **`2005.21`** | **`2005.36`** |
| 正街角分(0.4) | `0.392` | `0.3929` |
| 側街分(0.2) | `0.2` | `0.2` |
| 跨占分(0.4) | `0.3996` | `0.3996` |
| **總分** | **`0.9916`** | **`0.9925`** |
| 原位次(投影序) | `1` | `1` |
| 達標（真G ≥ 門檻） | **`True`** | **`True`** |
| 該側 winner | **`True`** | **`True`** |
| **拿得下** | **`True`** | **`True`** |

🛑 **「拿得下」＝ 達標 ∧ 為該側全部達標候選中指數最大者（＝ winner）**；⛔ 得以「第 1 名」單獨代之。

### `§二-5`　🔒 自我驗證閘 ②：扣除 `M` 之獨立性（態 `C(M)` vs 態 `B` 逐格）

`v12` 被呼叫 `6`【次】／實際扣除 **`1`**【列候選】（＝ 反靜默之「咬到」計數器）。
**其餘候選之（`總分`／`達標`／`真G`）逐格相同 `54`【格】／相異 **`0`**【格】** ⇒ **閘 ② 通過**。
🔒 **碼面保證**：三指數之分母皆取自 **range 自身**（`_corner_cut_den`／`_side_line_den`／`_corner_range_area`），⛔ 候選集之極值（`grep -n "score_corner_cut = 0.4 \* min" app.py`）⇒ 扣除 `M` 結構上⛔ 動其餘候選之分數。**本閘即為該碼面保證之機械復驗**，⛔ 以其為假設。

### `§二-6`　`R-5`　四分支之判讀 ＋ 另一側扣除 `M` 後之評選

**街廓 `R4`｜`M` ＝ `628(1)+`**
總分：左 ＝ `0.991600` ／ 右 ＝ `0.992500`（差 **`+0.000900`**）；拿得下：左 `True`／右 `True`
⇒ 🔴 **分支 `2`**：兩側皆拿得下 ⇒ **歸指數大者** ⇒ **歸屬 ＝ 右（`p2` 側）**

| 側 | 角色 | 扣除 `M` 後其餘跨占者 | 達標 | 結果 |
|---|---|---|---|---|
| **左**（`p1`） | 🔴 **operative：另一側** | **`0`**【宗】 | `0`【宗】 | **無 winner（候選集合為空）⇒ 強制留設抵費地·範圍嚴格等於該街角規定範圍 ＝ `226.01` ㎡**（門檻 `226.01` ㎡） |
| 右（`p2`） | 歸屬側·⛔ operative | `0`【宗】 | `0`【宗】 | （同上構造·範圍 `225.42` ㎡）——🛑 **⛔ operative**：`M` 歸此側，故此側之第 1 宗即 `M` |

🔒 **範圍面積之源** ＝ `f3_corner_range_areas['R4']['left'|'right']`（**塊×側級**·⛔ 依候選集）；與態 `B` 之 `M` 列 `範圍面積(㎡)` 對拍 ⇒ **相符 ＝ `True`**（二側皆）。

🔑 **與 `W-G.9-200` `§二-1` 五（實例段）之對照**：該段之**推定**逐字為「左總分 `0.9916`／右總分 `0.9925`（差 `0.0009`，唯一差源為正街角分 `0.392` vs `0.3929`）⇒ **可能歸右**，左街角則因⛔ 有其他跨占者而依**二·5** 成為強制抵費地 `226.01 ㎡`」。
⇒ 🔒 **本探針之實算與該推定<u>逐項相符</u>**（總分二值、差值、唯一差源、歸右、左為強制抵費地 `226.01 ㎡`）。🛑 惟該段自載「**此為推定·⛔ 得作為判準或寫入碼**」，本報告亦同——**實算⛔ 因相符而自動升格為裁**。

### `§二-7`　`R-7`　對照組：態 `A`（未合併·現行）vs 態 `B`（無條件合併）

診斷列總數：態 `A` ＝ **`20`**【列】／態 `B` ＝ **`20`**【列】（母體 ＝ `run_corner_pk` 之 `_corner_cand_diag`）。
`(街廓, 端)` 鍵集：僅 `A` 有 `[]`／僅 `B` 有 `[]`（⇒ 鍵集相同）。

| `(街廓, 端)` | 僅 `A` 有 | 僅 `B` 有 |
|---|---|---|
| `R4`／右 | `['628-1(1)']` | `['628(1)+']` |
| `R4`／左 | `['628(1)']` | `['628(1)+']` |

⇒ **合併前 vs 合併後之相異總格數 ＝ `4`**（`V-4` 之判準：須 **⛔ 為 `0`**）。
🔑 其餘 `4` 個可建築街廓（`R1`／`R2`／`R3`／`R5`／`R6` 之各側）**逐格全同** ⇒ 本批之差**全數集中於 `R4`**、⛔ 波及他塊。

### `§二-8`　各側指配（`run_corner_pk` 之 `_corner_select_results`）

| 街廓 | 態 | 候選數 | 【左】最小面積／達資格／指配／指數 | 【右】最小面積／達資格／指配／指數 |
|---|---|---|---|---|
| `R4` | `A` | `2` | `226.01`／`1`／`G009（628）[628(1)]`／`0.9916` | `225.42`／`1`／`G009（628-1）[628-1(1)]`／`0.9925` |
| `R4` | **`B`** | **`1`** | `226.01`／`1`／**`G009（628）[628(1)+]`**／`0.9916` | `225.42`／`1`／**`G009（628）[628(1)+]`**／`0.9925` |
| `R6` | `A`＝`B` | `8` | 無此側 | `299.13`／`1`／`G005（628-18）[628-18(1)]`／`0.1311` |
| `R5` | `A`＝`B` | `8` | `299.73`／`1`／`G007（628-45）[628-45(1)+]`／`0.4306` | 無此側 |
| `R2` | `A`＝`B` | `14` | `308.17`／**`0`**／**⚠️ 強制抵費地**／— | 無此側 |
| `R3` | `A`＝`B` | `10` | 無此側 | `307.79`／`1`／`G007（628-30）[628-30(3)+]`／`0.6095` |
| `R1` | `A`＝`B` | `5` | `226.01`／`2`／`G029（628-36）[628-36(1)]`／`0.2377` | `227.13`／`1`／`G009（628）[628(5)]`／`0.9915` |

🛑 **態 `B` 之 `R4` 二側指配同為 `628(1)+`** ＝ 現行引擎**尚未實作 `K-9-24` 二之歸側**所致（同一宗被兩側各自選為第 1 宗）⇒ 正是 `K-9-24` **二**所要處置之情形；其歸側由 `§二-6` 之四分支判定，**落地由重發之 `W-G.9-198` 辦**、⛔ 本單。

---

## `§三`　驗收 `V-1`〜`V-7`

| 閘 | 實測 | 判 |
|---|---|---|
| `V-1` | `git diff --name-only -z <基座>..HEAD` 之**生產碼命中 ＝ `0`**【列】（框 ＝ `^app\.py$ ∪ ^verify/.*\.py$` 之**既有**檔）；`HEAD:app.py` blob ＝ `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` ⛔ 變 | ✅ |
| `V-2` | 見 `§三-1` | ✅（附一項**受詞查無**之照實回報） |
| `V-3` | `git diff --numstat <基座>..HEAD` 之 `deletions` **逐檔 `0`** | ✅ |
| `V-4` | 見 `§三-2` | ✅ |
| `V-5` | 見 `§三-3` | ✅ |
| `V-6` | `L-2` 在倉；bytes／`\n`／`CR`／`sha256` 見 `§一`；**純 `LF` ⇒ 走常規 `add`**（⛔ `autocrlf=false`） | ✅ |
| `V-7` | 見 `§五` | ✅ |

### `§三-1`　`V-2`　唯讀自證

🔴 **一項受詞倉內查無（照實回報·⛔ 以近似物頂替·`CLAUDE.md`「施工單必須自足」之判準）**：單 `V-2` 逐字含「`--selftest` 與現行 harness 之輸出**逐格不變**」。**現查**（母體 ＝ `git ls-files '*.py'` ＝ **`245`**【檔】·框 ＝ `grep -F -- '--selftest'`）：

| 受詞 | 命中 |
|---|---|
| `app.py` | **`0`**【列】 |
| `verify/stepg_pipeline.py` | **`0`**【列】 |
| `verify/run_all.py` | **`0`**【列】 |
| `verify/run_verification.py` | **`0`**【列】 |
| **全倉 `245` 個 `.py`** | 命中檔 **`1`**【檔】＝ `verify/probes/probe_order_preflight.py`（`6`【列】·**判別力對照·非零**）|

⇒ 🛑 **`app`／harness 側⛔ 存在名為 `--selftest` 之入口** ⇒ 該受詞**⛔ 可執行**；CC ⛔ 編造、⛔ 以他物頂替，逐字回報並改以下列**結構證明**滿足 `V-2` 之實質（唯讀）：

1. **符號正面列舉**（母體 ＝ `git ls-files 'app.py' 'verify/**.py'` ＝ **`241`**【檔】·⛔ `grep -r … .`）：新探針之符號 `probe_WG9201_side_assign` 命中 **`0`**【檔】。
   **判別力對照**：既有探針名 `probe_WG9190R_fam3` 命中 **`2`**【檔】（`probe_WG9190R_fam3.py:1`／`probe_WG9190R_t2diag_full.py:4`）⇒ 量測器⛔ 恆零。
   ⇒ **⛔ 任何既有流程呼叫本探針。**
2. **零副作用**：探針執行後 `git status --porcelain` 之**已追蹤區為空**（本探針僅寫 `verify/out/` 下之**新** log；其覆寫閘為 `if os.path.exists(path) and WV_ALLOW_OVERWRITE != "1": loud(...)`）。
   ⇒ **任何 harness 之輸入逐位不變 ⇒ 其輸出逐格不變**（結構證明·⛔ 需重跑）。
3. **模組級自檢**：`harvest()` 於本探針之三態各 exec 一次 `app.py`（合計 **`4`** 次，含扣除態），其**模組級自檢**（`k6_merge_groups`／`k6_shares_segment` 之 `S-1`〜`S-7`、`_pk_one_side_v12` 相關斷言等）**逐次皆未 raise** ⇒ 該層自檢仍過。
🛑 **⛔ 跑 `run_all`**：其會改寫倉內**已追蹤**之 log（須於拋棄式 clone 內跑），且與本批之受詞（唯讀性）**無增益**——本項係**照實聲明所未跑者**，⛔ 以「未跑」冒充「已過」。

### `§三-2`　`V-4`　判別力（🛑 ⛔ 綁計數之單獨判準）

| 判準 | 實測 | 判 |
|---|---|---|
| `M` 之個數 **⛔ 為 `0`**（⇒ 探針未咬到任何東西） | **`1`**【宗】 | ✅ |
| `R-7` 之「合併前 vs 合併後」**至少一格有差** | **`4`**【格】（皆在 `R4`） | ✅ |

🛑 二者任一不成立即**停機上呈**；探針內已以 `loud()` 硬編此二閘（`grep -n '「V-4」' 之二處 loud`）⇒ **不成立時⛔ 可能印出綠**。

### `§三-3`　`V-5`　`R-6` 反靜默之自證（🛑 逐點皆須為報錯停機）

**正面列舉**（母體 ＝ `verify/probes/probe_WG9201_side_assign.py` 單檔·框 ＝ 列）：`loud(` 之呼叫點 ＝ **`23`**【列】，涵蓋：`git` 非零 rc／`WV_SB` 未設或非數／`ns is __globals__` 為偽／`ns` 內查無二層疊點／`v12` 之 `candidates` 非具名／`f3_k6_step0_diag` 未產生／`f3_corner_range_areas` 缺街廓或缺側鍵或為 `None`／端值集不合引擎契約／診斷列缺 `候選地號` 或缺欄／`總分` 非數／`達標` 值域外／二自我驗證閘不過／扣除層疊未咬到／對照母體為空／`V-4` 二判準／拒絕覆寫既有 log。

**負面查核**（同母體同框）：

| 字樣 | 命中 | 逐筆歸類 |
|---|---|---|
| 裸 `except:` | **`0`**【列】 | — |
| `except Exception` | `1`【列】 | `:259` 之 `_s.reconfigure(encoding="utf-8")` ⇒ **stdout 編碼設定**、⛔ 取值點（體例同 `probe_WG9190R_t2diag_full.py` 之 `2` 列·**判別力對照·非零**） |
| `return False` | `1`【列】 | `:124` ＝ 層疊函式 `_no_lock` 之**設計回傳值**（其存在目的即回 `False`）、⛔ 失敗值 |
| `return None` | `1`【列】 | `:245` ＝ `pick_winner` 之「**無 winner**」 ⇒ **域值**（`K-9-24` 二·5 之強制抵費地），其消費端逐字處置、⛔ 失敗值 |
| `.get(` | `40`【列】 | 其中**餵判斷者** `6` 列（`:116`／`:129`／`:134`／`:159`／`:182`）**逐列緊接 `if … is None: loud(…)`**；`:302` 之 `groups_merged` 預設 `0` 係**失敗關閉**（缺鍵 ⇒ 閘 ① 判偽 ⇒ `loud`）；`:296`-`:299` 之 `-1` 係**可見哨兵**（⛔ 貌似合法之 `0`）；其餘 `31` 列為**顯示欄**（`m.get('街廓')` 等），⛔ 參與任何判斷。**判別力對照**：既有探針 `probe_WG9168_k6step0.py` 同框 `12`【列】 ⇒ 量測器⛔ 恆零 |

---

## `§四`　🛑 射程限（⛔ 得省）

1. **本探針之數⛔ 得逕作為任何裁之依據**——須經發單側獨立復驗、KL 認可，方得進入 `K-9-24` 之實例段或任何生產碼（單 `§三` 逐字）。
2. **情境限**：全部數字繫於 **`SB = 3.50` m**（`WV_SB`）。換情境須重跑；本報告⛔ 宣稱其對其他退縮值成立。
3. **`GB-123` ⛔ 解除**：本探針走 `run_corner_pk`（harness 通道），**⛔ 經 app `main()`**；其對 app 路徑之**土地後果⛔ 確認·待 KL UI 實跑**。
4. **`K-9-24` 之落地⛔ 於本單**——現行引擎**尚未實作**歸側四分支（`§二-8` 之 `R4` 二側同指配即其表徵）；落地由**重發之 `W-G.9-198`** 辦。
5. **`R-2` 之限**：態 `B` 係以層疊使 `k6_step0_block_locked` 恆偽而得，**⛔ 改 `app.py` 一字**；其與「真正刪除該保守替代」之等價性⛔ 本探針所證。

---

## `§五`　`S-0e`／`V-7`　閘之量測與其所守護之動作**分開呼叫**之界線

| 序 | 呼叫塊 | 性質 |
|---|---|---|
| `1` | `S-0a`／`S-0b` 現查（`git diff`／`grep` `K-9-24`／`K-9-25`） | 量測 |
| `2`〜`6` | 偵察：既有探針清單／`probe_WG9190R_t2diag_full`／`probe_WG9168_k6step0`／`selection_pipeline.run_corner_pk`／`app.py` 之 `k6_step0_*`／`_pk_one_side_v12`／`select_corner_lots_both_sides_v12` | 量測 |
| `7` | **`L-1` 探針寫入** | **動作** |
| `8` | `py_compile` ＋ 缺 `WV_SB` 之 loud 自證 | 量測 |
| `9` | **探針實跑（首趟·態 `fb494f57`）** | **動作** |
| `10` | 首趟輸出判讀 | 量測 |
| `11` | **探針補強二處（範圍面積之源＋二量分列）** | **動作** |
| `12` | **重跑（`WV_ALLOW_OVERWRITE` 未設·先 `rm` 舊 log）** | **動作** |
| `13` | `V-2` 之符號正面列舉 ＋ `git status` 已追蹤區 | 量測 |
| `14` | `--selftest` 之全倉現查（受詞查無） | 量測 |
| `15` | **於 `8020d3d` 重跑並產 `L-2`** | **動作** |
| `16` | `R-9` 之 `stdout` ≡ `log` 對拍 ＋ 注入判別力 | 量測 |
| `17` | `V-5` 之 `loud` 點與負面查核 | 量測 |
| `18` | **本報告寫入** | **動作** |
| `19` | 入倉前之閘（`numstat`／生產碼框／blob `CR`） | 量測 |
| `20` | **逐檔 `git add` ＋ `git commit`** | **動作** |
| `21` | 入倉後覆驗 | 量測 |
| `22` | **`git push`** | **動作** |

🛑 `20`／`22` 皆⛔ 與任何閘之量測同塊；`19`／`21` 之判讀先出艙、經判讀，方進入下一動作。

---

## `§六`　⛔ 本報告不辦

⛔ 不動 `app.py`／`verify/**.py` 之**既有**檔一字。
⛔ 不辦 `K-9-24` 之落地（幾何影響·由重發之 `W-G.9-198` 辦）。
⛔ 不辦 `k6_step0_block_locked` 之死碼化。
⛔ 不鑄任何號；⛔ 追改任何既有已出艙之數。
⛔ 不跑 `run_all`（理由與其未跑之聲明見 `§三-1`）。
