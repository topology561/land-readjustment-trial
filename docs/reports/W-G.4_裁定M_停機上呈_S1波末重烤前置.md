# ⛔ W-G.4 裁定 M — 停機上呈：**P-0c 遠大於計畫·S1 波末重烤係硬前置**

> 施工至 P-0c（F.0 重烤）時實測發現：**plan v4 ＋ 三輪 reviewer 皆低估 P-0c 範圍**。
> 「F.0-only 重烤」**不可行**（22→34 FAIL·新增 16 名目）；M-5 驗證**強制**需先做**完整 S1 波末重烤**。
> 此係**讓綠當過最高紀律**之最大暴露面（57 CSV＋數個經驗錨），且撞 `k*` **不變量結構性破裂**。
> 依 CLAUDE.md「每波動工前回報 KL 確認」＋「意思決定標旗呈 KL」→ **停機上呈**。基準 `334dfb4`。
> **P-0a/P-0b/P-A/P-B 已 push（全零 diff）·本停機不涉已落地碼。**

---

## 〇、一句話

裁定M 之 M-5 需 **F.0→F.4 全跑**才能顯現（改 trunk B→C→D→E）；但 F.0~F.4 之 baseline
**全數 S1 過期**（W脫鉤 07-19＋S0d 07-20 之後未重烤）。**只烤 F.0** 會把下游 16 個 S1 過期
baseline **從 cascade-skip 揭成 baseline-mismatch**（22→34 FAIL·**違「FAIL 集不新增名目」**）。
⇒ **完整 S1 波末重烤（57 CSV＋GSA＋F.0釋池＋k*）係裁定M 之硬前置**·非可選範圍。

---

## 一、實測證據（CC 親驗·三個獨立測）

### 1.1 全 22 BEFORE-FAIL ＝**單一成因**：S1 deferred 波末重烤

| FAIL 群 | 症狀樣本 | 成因 |
|---|---|---|
| `v3·滑池槽{0m,3.5m}` | `J(naive) baseline=60.3553 got=55.3577` | **W脫鉤**改 W → J |
| `v3·J表` | `dev(m) baseline=42.15 got=39.60` | S0d 改 S → 落位 |
| `v3·G值` | `缺列 R2-抵費地-1/2` | 抵費地幾何變 |
| `k* 六塊經驗錨3.5m` | `R1 baseline=2 got=1` | **S0d** 改 S → 最優切點移 |
| `W-D.3／W-D.4` | `ΣG 768.94→768.37` 等 | trunk A 下游 |
| `W-F F.0` | `六格錨 359.43≠362.08` | trunk A G |
| `F.1~F.4／G.2` | 級聯（F.0 raise） | — |

全部 trunk-A 位移之下游。**無一為裁定M 所致**（P-0a/0b/A/B 皆零 diff 證）。

### 1.2 provenance 乾淨：committed xlsx == modified xlsx（F.0 逐格同）

`git worktree` 於 committed xlsx（blob `80f75ee`·12.38MB）跑 F.0：
`0m G007 359.43／G010 293.72／G014 127.05`；`3.5m G007 369.41／G010 298.61／G014 129.20`
——**與我工作樹（modified xlsx）逐位相同**。⇒ **KL 2026-07-21「working xlsx 汙染係誤判」再度坐實**；
六格漂移純由碼（W脫鉤+S0d），與 xlsx 無關。**重烤可自任一 xlsx 產·結果同。**

### 1.3 「F.0-only 重烤」**不可行**：22 → **34 FAIL**（新增 16 名目）

worktree 實測：只套 12 個 F.0 CSV ＋ 更新 GSA_EXPECT（6 格）→ 重跑：
- **消失 4 名目**（cascade→實跑）：`W-F F.0/F.1/F.2/F.3`
- **新增 16 名目**（S1 過期 baseline 被揭）：
  `F.1·遞補整形{0m,3.5m}`／`F.1·碎片處置0m`／`F.1·池驗證{0m,3.5m}`／`F.1 標記制0m`／
  `F.2·跨街廓調配{0m,3.5m}`／`F.2·G值{0m,3.5m}`／`F.2·池流向{0m,3.5m}`／
  `F.3·G值{0m,3.5m}`／`F.3·池流向{0m,3.5m}`
- **淨 +12·違「FAIL 集不新增名目」**。

⇒ 只烤 F.0 把 F.1~F.4 之 trunk-B/C/D 過期 baseline 從「隱」揭成「顯」。
**要不增名目，F.1~F.4（及其上游 trunk C/D）baseline 亦須重烤 ＝ 完整 S1 波末重烤。**

---

## 二、為何是**硬前置**（非可選範圍）

1. **M-5 需 F.0~F.4 全跑**：M-5 改 F.0 級0 合併（628-20(2) 回單獨）→ trunk B→C→D→E 連動；
   驗收預測閘 b（GSA G007）、c（forced）、d（容量）**皆需 F.0~F.4 執行**。
2. **F.0 一跑，F.1~F.4 即揭 S1 過期**（§1.3）⇒ 不重烤 F.1~F.4 即 +12 名目。
3. **P-H 混烤風險**：trunk-A baseline（v3/W-D.3/W-D.4）**既 S1 過期·又 M-5 觸**（R3 winner 變）
   ⇒ 若留至 P-H 重烤，其 diff ＝ S1 漂移 ＋ M-5 增量 **混算**·**破 Q-M3「逐格歸因」核心**。
   唯一解：**先 P-0c 把 S1 漂移烤淨（M-5 前）·P-H 才見純 M-5 增量。**

⇒ plan v4 §一「P-0c ＝ F.0 六格＋12 CSV」**範圍不足**；正解 ＝ **P-0c ＝ 完整 S1 波末重烤**。

---

## 三、完整 S1 波末重烤之**範圍＋合法性**

### 3.1 範圍（harness 全可烤·無 KL UI 缺口）

| 類 | 檔/錨 | 手段 |
|---|---|---|
| CSV baseline | **57 檔**（`v3/*`／`wf/f0~f4/*`／`W-D.1.2`／`W-D.1.3`／W-D.3／W-D.4） | `WV_BAKE` 全產（實測 57 CSV） |
| 碼字面·GSA | `wf_f0.py:55-60 GSA_EXPECT` 6 格（G007/G010/G014 × 雙情境·G006/G009/G017 不動） | 手動·觀測值 |
| 碼字面·F.0釋池 | `run_verification.py:691` `55.18→55.09` | 手動 |
| 碼字面·**k\*** | `run_verification.py:44 K_STAR_EXPECT` | **見 §3.2·須結構改** |

「含 KL UI 錨」疑慮**解除**：上列 57 CSV 全 harness 現算·`WV_BAKE` 全產；碼字面亦由觀測值定。

### 3.2 🔴 **k\* 不變量結構性破裂**（唯一需裁·非機械重烤）

`K_STAR_EXPECT = {"R1": 2, ...}` 係**單一 dict**（跨情境共用·假設 k* 不隨退縮變·註解「本案恰不變」）。
**S0d 打破此假設**：`0m R1 k*=2` 但 `3.5m R1 k*=1`（S0d 改 S → 最優切點移）。

- **合法性已證**（非 bug）：BEFORE **結構不變量永久閘 `J(k*)≥J(naive)` 於 0m/3.5m 皆 PASS**
  ⇒ 引擎所選 k*=1（3.5m R1）**確為最優**·K_STAR_EXPECT 只是**過期**（記錄 S0d 前之最優）。
- **但需結構改**：`K_STAR_EXPECT` 單 dict → **per-tag**（`{"0m":{...R1:2...}, "3.5m":{...R1:1...}}`）；
  且碼註解「本案恰不變」**已被推翻**·須更正。
- 碼註**自帶**「k* 破＝widths 變動使切點翻·**須重驗非直接放行**」⇒ **此改須獨立復現·不得逕烤**。

---

## 四、⛔ 上呈裁決（CC 不自裁·三問）

### Q-S1【範圍】裁定M 是否吸收**完整 S1 波末重烤**為 P-0c？

現況：**硬前置**（不做則 M-5 無法驗·或 +12 名目）。**CC 傾向：是**（吸收·單獨 commit·歸因 S1）。
替代：另立「S1 波末重烤」獨立波先行、裁定M 待其綠後續——但二者強耦合、分波僅增交接成本。

### Q-S2【讓綠當過】57 CSV＋數錨之重烤·**reviewer 獨立復現**程序

最高紀律：「自評 gate 綠不可替代獨立復現」。**CC 擬**：worktree 重烤 → **reviewer 逐錨復現**
（每漂移錨 → git bisect/永久閘 證 ＝ S1 合法後果·非 regression）→ 綠後才 apply＋commit。**請確認此程序足夠。**

### Q-S3【k\*】`K_STAR_EXPECT` 改 **per-tag**（0m R1=2／3.5m R1=1）是否照准？

`J(k*)≥J(naive)` 永久閘已證 k*=1 合法最優（§3.2）。**CC 傾向：准**（per-tag·歸因 S0d·永久閘背書）。
惟「跨情境 k* 不變」原係設計假設·其推翻屬**意思決定邊界** → 標旗呈 KL·**未准不逕改**。

---

## 五、不受本停機阻擋（已 push·全零 diff）

`P-0a`（`_compute_v3_finance` 純位移）／`P-0b`（`_solve_G_one` 純位移）／
`P-A`（`_corner_first_lot_G`·真幾何 A-1/A-2 確立）／`P-B`（`require_g_map`·or→in）
——**四步 PASS/FAIL 集合 vs BEFORE 皆 BYTE-IDENTICAL**·origin/wip/s1-endpart=`334dfb4`。
**Q1 真 G 已與實配同 solve 路徑·待接線（P-C）**。

## 六、次步（待裁）

Q-S1/S2/S3 裁後：完整 S1 波末重烤（worktree→reviewer 復現→apply·單獨 commit·歸因表）
→ P-C（真 G 驅動達標·Q1）→ P-D/E（M-5）→ P-F/G/H。
