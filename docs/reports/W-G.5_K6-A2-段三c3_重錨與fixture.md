# W-G.5 — **K-6-A2 段三(c)-3：重錨（G010＋G007）＋ fixture T1**

> **基準**：`111ce12`　**分支**：`wip/s1-endpart`
> **claude.ai 裁 2026-08-04**：G007 一併重錨；fixture T1 帶深改 `D(t*)`。
> **性質**：重錨（**兩格**）＋ fixture 量測基準更正。體例比照 `W-G.5_K8-段三_commitC_重錨解封.md`。

---

## 一、重錨（`GSA_EXPECT["3.5m"]`·**兩格**）

`grep -n '"3.5m": {' verify/wf_f0.py`

| gid | 舊錨 | 新錨 | 破錨？ |
|---|---|---|---|
| **G010** | 301.12 | **301.17** | ✅ 破（Δ `+0.05` 逾嚴格容差） |
| **G007** | 370.02 | **370.01** | ❌ **未破**（Δ `−0.01` 恰落邊界）·**主動重錨** |

**可執行變更僅上列兩數字**（`git diff` 之非註解行 ⇒ 一組 2 行替換）；
**其餘四 gid 與 `"0m"` 全組一字未動**。

### 1-1 逐 gid 因果歸因表（**六格全列·含未動者及其未動之理由**）

**成因（統一）**＝**K-9-7 接生產**（段三(c)）：街角規定範圍改依 **K-9-6-b 之帶深**
（範圍**自身**之最小深度 `D(t*)`，非街廓平均深度）與 **K-9-7 之 `(c − k·m)` 斜率**定位
⇒ **三側強制留設抵費地（`forced_offset`）之面積變動** ⇒ 其後各宗之 s 帶／W／Rw 隨之微移。

| gid | 標的宗 | 街廓 | 該側 forced？ | 範圍面積(舊→新) | ΔG | 處置 |
|---|---|---|---|---|---|---|
| **G010** | 628-40(1) | **R2** | ✅ R2左 | 382.85→384.15（**+1.30**） | **+0.05** | **重錨 301.12→301.17** |
| **G007** | 628-20(2) | **R5** | ✅ R5左 | 378.47→371.17（**−7.30**） | **−0.01** | **重錨 370.02→370.01** |
| G014 | 628-29(1) | R3 | ✅ R3右 | 383.67→383.67（0） | +0.00 | 不重錨 |
| G006 | 628-48(1) | R3 | ✅ R3右 | 383.67→383.67（0） | +0.00 | 不重錨 |
| G009 | 628-1(2) | **R6** | ❌ R6右非 | 298.14→298.14（0） | +0.00 | 不重錨 |
| G017 | 628-21(1) | **R6** | ❌ R6右非 | 298.14→298.14（0） | +0.00 | 不重錨 |

> 🔎 **gid→街廓係本批實測所得**（`wf_f0.py` 原註解未載 G006／G009／G017 之街廓）：
> 由 `wf_f0.compute` 之 `decisions[*].target` 對回 `g_rows` 之 `所屬街廓`。
> G006／G014 同屬 **R3**；G009／G017 同屬 **R6**。

**未動之理由**：

- **G014／G006（R3）**：R3右 雖 `forced=True`，但其範圍面積**未變**
  ——R3右 屬 **K-9-7 分支 ①**（`k ≥ 0` ⇒ 深度不影響寬度）⇒ 換帶深定義不改 `t*`。
- **G009／G017（R6）**：R6右 **`forced=False`**（不生抵費地帳）**且**範圍面積未變（亦分支 ①）
  ⇒ **雙重不受影響**。

### 1-2 `"0m"` 全組不動之理由

0m **僅 R3右 `forced=True`**，而 R3右 範圍面積 `226.91→226.91` **未變** ⇒ 抵費地帳零變動。

⚠️ **一項須更正之說法**：0m 之 **R2左／R5左 範圍面積確有變動**（`+1.42`／`−7.27`），
惟該二側 **`forced=False`** ⇒ **不生抵費地帳** ⇒ 不影響任何 gid。
⇒ 「**0m 面積沒變**」之說法**不正確**、勿沿用；正確表述為
「**0m 只有 R3右 forced，且該側面積未變**」。
**實測**：0m 六格 ΔG 皆 `+0.00`。

### 1-3 G007 主動重錨之理由（**未破錨仍重錨**）

錨檢為**嚴格**不等式 `abs(G - exp) > 0.01`（`grep -n "abs(G - exp) > 0.01" verify/wf_f0.py`）
⇒ `0.01 > 0.01` 為 **False** ⇒ Δ 恰為 `0.01` 者**不破錨**。

**但實值已變**（370.02 → 370.01）。若留舊錨：

- 段四 K-9-8／波末換 `V6_1` 之**任何再一個 `0.01` 微擾**即累積為 `0.02` 而爆；
- **屆時歸因會指向那一批，真正成因卻在 K-9-7** ⇒ 正是 **F-7-3「禁打地鼠」**所防之形狀。

**傳導佐證**（實料 `verify/out/probe_k97_r5_attribution.log`【3】段）：
標的宗 `628-20(2)` 於 R5 之**累積S ＝ 28.78**，遠在抵費地（`S=0`）**之後**
⇒ 屬機制所及之「**其後各宗**」，傳導路徑成立。

### 1-4 ⚠️ 一項「記錄、不解釋」之觀察

**兩格之敏感度差 27 倍**：R2 `+1.30㎡ ⇒ +0.05`；R5 `−7.30㎡ ⇒ −0.01`。

🔴 **本批如實記下此不對稱，⛔ 未為其編造未經量測之成因**（claude.ai 明令）。
段四或波末如需，屆時再查。

### 1-5 🔴 粒度限制（見 **GB-27**）

被比欄 `G(㎡)` 於來源即 `round(..., 2)`（`grep -n "'G(㎡)': round" verify/stepg_pipeline.py`）
⇒ 上表 ΔG **粒度為 `0.01`**；「`+0.00`」**只能讀作「2dp 下未變」**，
**不得**讀作「真值未變」。

---

## 二、fixture T1（帶深改 `D(t*)`）

### 2-1 改法

`verify/fixture_corner_range_k8.py`：新增 **`_range_min_depth`**
（`grep -n "def _range_min_depth" verify/fixture_corner_range_k8.py`），
T1 之帶深由 `snap["blocks"][lbl]["街廓分配深度_m"]`（街廓平均深度）
改為**該範圍自身之最小深度 `D(t*)`**（K-9-6-b）。

### 2-2 🔒 護欄一：`D(t*)` **由 fixture 獨立量得**

`_range_min_depth` **只吃 `cad` 幾何 ＋ 範圍多邊形之 ALLOC 邊**（`t_line_pts`·＝受測函式之**輸出幾何**）：

```
Q0       = 前緣線 ∩ 側界（皆無限直線）
P_front  = 前緣線 ∩ ALLOC 邊
D(t*)    = min( dist(Q0, BASELINE), dist(P_front, BASELINE) )
```

⛔ **未自 `k97_solve_alloc_t` 或 `_build_corner_range_v3` 之回傳取任何值**
⇒ 本檔 §「T1 之量測係**第三份獨立實作**」之性質**保住**、未退化為恆真閘。

### 2-3 結果

| | 修前 | 修後 |
|---|---|---|
| R5 left `0m` | **3.333687**（Δ `−0.166`）🔴 | **3.500000**（Δ `+1.61e-10`）✅ |
| R2 left `0m` | 3.531524 🔴 | 3.500000 ✅ |
| R4 left／right `0m` | 3.501116／3.499182 🔴 | 3.500000 ✅ |
| 零位移四角 | 3.500000 ✅ | 3.500000 ✅ |
| **T1 十六格** | **4 紅** | **全綠（`~1e-10`）** |
| **fixture rc** | **1** | **0** |

### 2-4 🔒 護欄二：反向鑑別力檢查（**已做·附零殘留證明**）

把帶深**故意改回**街廓平均深度後重跑：

```
R2 left  0.0  3.50  3.531524  +3.15e-02  🔴
R4 left  0.0  3.50  3.501116  +1.12e-03  🔴
R4 right 0.0  3.50  3.499182  -8.18e-04  🔴
R5 left  0.0  3.50  3.333687  -1.66e-01  🔴
R1 left  0.0  3.50  3.500000  +2.84e-10  ✅   ← 零位移角仍綠
R6 right 0.0  3.50  3.500000  +7.21e-11  ✅   ← 零位移角仍綠
rc=1
```

⇒ **四個位移角仍會紅、零位移四角仍綠** ⇒ T1 **保有鑑別力**，非「改測試求綠」。

**零殘留證明**：已還原並複跑 `rc=0`；
`git diff -- verify/fixture_corner_range_k8.py` 之**可執行行**中
**無任何**「臨時反向對照」或舊帶深之殘留
（三處命中皆為**說明文字**：docstring 述其「原以…」、註解述其「不再用…」、
以及 `-` 側之被移除行）。

### 2-5 ⚠️ 段四須複查（**已就地留註＋登記 GB-28**）

K-9-6-b-1 明訂量測用之「道路境界線」＝ **K-9-8 之 `PQ` 延伸線段**、深度自該線段起算。
本案 `P` **多落 FRONT 重疊段而退化**（12 格中 10 格）⇒ 現行自 FRONT_LINE 起算可行；
**K-9-8 落地後 T1 之起算線須一併改**。
註記見 `grep -n "段四須複查" verify/fixture_corner_range_k8.py`；登記見 **GB-28**。

---

## 三、新登記

| GB | 內容 |
|---|---|
| **GB-27** | `GSA_EXPECT` 錨檢容差 `abs(G−exp) > 0.01` **對一個法定面積粒度為盲**——被比欄本身即 `round(...,2)` ⇒ **有效解析度 `0.02㎡`**，而 `0.01㎡` 為法定粒度。**本案已坐實一例**（G007@3.5m）。**只登記不改**（改容差牽動其他錨·波末批） |
| **GB-28** | `fixture_corner_range_k8` T1 之起算線為 FRONT_LINE、未實作 K-9-8 之 `PQ`（見 §2-5） |

---

## 四、`run_all`：**F.0–F.3 已恢復實跑·名目回到 132**

實料：`verify/out/K6A2_seg3c3_runall.log`（`EXIT=1`·一般模式）

| | 切換後（未重錨） | **本輪（重錨後）** | 基線 |
|---|---|---|---|
| 名目 | 64 | **132** ✅ | 132 |
| PASS | 33 | **86** ✅ | 86 |
| FAIL | 31 | **46** ✅ | 46 |
| `W-F F.0/F.1/F.2/F.3` 階段彙總 FAIL | 4 | **0** ✅ | 0 |
| `fixture_corner_range_k8` | `rc=1` | **`rc=0`** ✅ | `rc=0` |

⇒ **級聯已解除**：F.0–F.3 家族之 **77 個子閘全部恢復實跑**。

### 4-1 §三-1 逐格比對（vs `verify/out/K6A2_seg3b_runall.log`＝**切換前最後一輪**）

**FAIL 集合**：

```
$ diff <(前輪 FAIL 名目 sort) <(本輪 FAIL 名目 sort)
  (無輸出)
```

⇒ **零進零出**（46 ↔ 46）。**併對靶 `K8_seg3_C.log` 亦同**。

**PASS 集合**：筆數 `86 ↔ 86`，`diff` 出現**兩處置換**（`39c39`／`49c49`·非增刪）：

| 名目 | 前輪 | 本輪 |
|---|---|---|
| `F.2 跨街廓守恆3.5m` | `\|Σ(G−幾何)\| **0.03**` | `\|Σ(G−幾何)\| **0.0**` |
| `F.3 結構永久閘3.5m` | `\|Σ(G−幾何)\| **0.0**` | `\|Σ(G−幾何)\| **0.01**` |

🔴 **此二處**不是**名目進出**——該二名目之**字串內嵌了實測殘差**，殘差一變、字串就變，
於集合 `diff` 中遂顯示為「一出一進」。**證明**：

```
$ diff <(前輪 PASS 名目 | 將 |Σ(G−幾何)| 之數字正規化) <(本輪同樣正規化)
  (無輸出)   ⇒ 正規化後逐字相同
```

**成因**：本批之 G 微移（`G010 +0.05`／`G007 −0.01`）使守恆殘差隨之微動
（`0.03→0.0`／`0.0→0.01`）。
**二格**均**仍為 `✅ PASS`**、且**遠在容差 `0.585` 之內**（`0.01 ≪ 0.585`）。

⇒ **逐格結論**：132 名目**無一進出**；唯二字面差異係**同一名目之內嵌殘差數字**，
二者仍 PASS 且在容差內，成因已歸因至本批之 G 微移。

### 4-2 ⚠️ `run_all` 不得單獨作為驗收依據（`CLAUDE.md` 常設紀律）

本批之 `app.py` 未再改動；`verify/wf_f0.py` 之改動為**兩個錨值**、
`verify/fixture_corner_range_k8.py` 之改動為**量測基準**。
⇒ `run_all` 恢復 132 證明**級聯已解除**，但**不證明重錨值正確**——
重錨值之正確性由 **§一之逐 gid 因果歸因表**（新舊皆由同一支 `wf_f0.compute` 產出）承擔；
fixture 之正確性由 **§2-4 反向鑑別力檢查**承擔（二者皆不經 `run_all`）。


---

## 五、§四 收尾（**另二枚 commit**）

| 項 | commit | 結果 |
|---|---|---|
| §四-1 三支拋棄式合成案**升格為正式探針** | `d6b7c7f` | 三支皆可攜（零硬編路徑）、實跑 `rc=0` 且結論與升格前相同 |
| §四-2 harness 種子 **xlsx 入庫** | `3fe3cce` | 內容**逐格全等**（本批獨立複驗）·僅容器層差異 |

**§四-1 之移植查核**：逐支比對原稿 `import`——f2 原有 `import re`（移植初版漏之·已補）；
k94 原有 `import math` 而 ported 檔 `grep "math\."` **0 命中** ⇒ 原即冗餘、非移植漏。
路徑：`REPO` 改由 `__file__` 導出、移除 `os.chdir`
（`app_harvest.APP_PY`／`rv.V6DXF`／`rv.ANON_XLSX` 皆已 `__file__` 導出 ⇒ 免 cwd 相依）。

---

## 六、追加登記（claude.ai 2026-08-04 指示·**只登記不改**）

### 6-1 波末項【甲】：`F.3` 之守恆殘差是否回到「精確零」

本批 `F.3 結構永久閘3.5m` 之 `|Σ(G−幾何)|` 由 **`0.0` → `0.01`**，
同時 `F.2 跨街廓守恆3.5m` 由 **`0.03` → `0.0`**。
二者皆**遠在容差 `0.585` 內**，且與本批 ΔG（`+0.05`／`−0.01`）**量級一致**
⇒ 係 **2dp 捨入殘差在兩格之間重新分配**、**非守恆破口**。

🔴 惟「**精確零**」比「在容差內」是**更強之性質**，而 **F.3 離開了精確零**
⇒ **波末重烤後須確認 F.3 是否回到精確零；若否，須有解釋。**
登記落點：正典「K-8 波末遷移項登記」節之追加表
（`grep -n "K-6-A2 段三(c)-3 追加之波末項" docs/rulings/K-6_街角地分配程序與可分配判準.md`）。

### 6-2 波末項【乙】：**GB-27 之延伸——「容差 vs 欄位粒度」全倉對照**

GB-27 所記者為 `GSA_EXPECT` **一族**。**同一形狀**（容差恰等於被比欄之粒度）
**可能存在於其他錨族**：`k*` 六塊經驗錨／v3 財務錨／W-D.4 清單——**該三族本波未掃過**。

⚠️ **`GSA_EXPECT` 六格現確知乾淨**（本批量測顯示僅 `G007` 卡在 1 個粒度、已重錨）；
**其餘錨族無此保證** ⇒ 波末做一次全倉對照。

### 6-3 登記表之編號體例（**⛔ 禁以列數驗收**）

本表新項**採前插** ⇒ `GB-1…GB-21` 升序、其後 `GB-28→GB-22` **降序**；**排序無語意**。
**`GB-12` 為缺號**（沿革未載·本批未查得其是否曾用於他項）。
🔴 **列數會巧合通過**（本表列數曾正確而順序與缺號皆異常）
⇒ 已於表頭載明**應以內容檢查**（重複 ID／缺號／最大號）代之。
本批實跑該檢查：**重複 0、缺號 [12]、最大號 28**。
