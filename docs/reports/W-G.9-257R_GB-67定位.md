# `W-G.9-257R`　`GB-67` 定位（丙案·**純診斷**·⛔ 修·零生產碼）

> 🛑 **本倉 `app.py` 一字未動**——倉側 blob 前後 ＝ **`4860f10828fb8ae9`**；`git status` 空。
> 🔒 全部器落 **scratchpad**／診斷注入落**拋棄式 clone**（`C:/t257`·⛔ 本倉·`GB-147`）。
> 🔒 **本文⛔ 含任何修法主張**（`V-P6`）。

---

## §零　開工閘

| 閘 | 實測 |
|---|---|
| `1` 錨 | 🩸 **漂移**：單載 `8a52dbd`，現態 **`ff79584`**（其間為 `-256R`／`-258R`／`c1` 併線，皆已授權）。🔒 **受詞未變**——`app.py` blob 於 `8a52dbd`／`ff79584`／`HEAD` **三態皆 `4860f10828fb8ae9`** ⇒ 續辦並具名 |
| `2` 號占用 | `W-G.9-257` 於 `a5b4edd` 之前批已驗全 `0`；本單已入倉（`c2f28e2532e45768`）|
| `3` 次號 | **本批⛔ 鑄任何號**（單 `§零 3` 明令）|
| `4` `S-0b` | 前批已驗 `rc=0`、`P-5` 相符（`0394745232cb7e7c…`）|
| `5` 環境 | ✅ `ezdxf`／`shapely`／`plotly`／`streamlit`／`pandas`／`openpyxl` **六者皆已裝**（⛔ 需安裝）|

---

## §一　方法（**⛔ 動生產碼**之機械保證）

1. 自 GitHub `fetch` 至拋棄式 clone `C:/t257`＠`ff79584`（`app.py` `4860f10828fb8ae9`／`wf_f0` `b0722355c1978635` 逐位符）。
2. 於 **clone 之** `app.py` 注入診斷：`②-宗 圍堵閘` 之 `raise` 改為**記錄後續跑**（受詞命中**恰 `1`**·⛔ 盲改；`py_compile` rc=0）。
3. `WG257_OUT=… python verify/run_verification.py` ⇒ `RESULT: FAIL`、紅 **`24`** 項。
4. 🔒 **本倉零改動**（`V-P1`／`V-P2` 已驗）。

🩸 **一處自證期望寫錯（CC）**：patch 之自證期望「原閘字樣殘留 `0`」而實得 `1`；查明該殘留在
`app.py:22508`（`main()` 內**另一處** UI 訊息 ＋ `st.stop()`），**與 `②-宗` 閘無關** ⇒ 注入完全。

---

## §二　`Q-1`　配對矩陣之獨立復現（**完整 `91` 對·⛔ 取樣**）

| 量 | CC 實測 | 發單側所載 | 判 |
|---|---|---|---|
| 對數 | **`91`** | `91` | ✅ |
| 非零對 | **`3`** | `3` | ✅ |
| 零對 | **`88`** | —— | 補報 |
| 非零之和 | **`45.9766`** | `45.9766` | ✅ **與閘所報之 `biz_x` 逐位相符** |

| 配對 | 疊(㎡) | i 面積 | j 面積 | 佔總疊 |
|---|---|---|---|---|
| `宗0` × `宗3` | **`26.1624`** | `175.946` | `293.478` | `56.9%` |
| `宗0` × `宗2` | **`19.8142`** | `175.946` | `29.085` | `43.1%` |
| `宗1` × `宗2` | **`0.0000`** | `3.808` | `29.085` | `0.0%` |

⇒ 🔒 **「僅 `3` 對非零」非取樣所致**——`91` 對全數列出，其餘 `88` 對皆 `0.0`。

---

## §三　`Q-2`　`宗0`〜`宗3` 之身分（**幾何面積逐位對位**·`got_G值_退縮0m_partial.csv`）

| `k` | 幾何面積 | **暫編地號** | 原地號 | `a` 面積 | `G` | **累積S** | 推進側別 |
|---|---|---|---|---|---|---|---|
| `0` | `175.946` | **`628-41(1)`** | `628-41` | `319.3` | `175.95` | **`4.06`** | `left` |
| `1` | `3.808` | **`628-42(1)`** | `628-42` | `8.34` | `3.82` | `4.81` | `left` |
| `2` | `29.085` | **`628-27(1)`** | `628-27` | `51.32` | `29.08` | `5.46` | `left` |
| `3` | `293.478` | **`628-40(1)+`** | `628-40` | **`513.93`** | `293.48` | **`12.0`** | `left` |

🔒 **歸戶**：四宗之原地號各異（`628-41`／`628-42`／`628-27`／`628-40`）；`宗3` 之 `+` 尾綴即**步驟 0 之合併宗**（`K-6 §二 步驟 0` 之命名規則）。
🛑 **`s` 區間 `(s_min, s_max)` ⛔ 可得**——**阻斷點**：`kept_iv` 於本次注入**未一併記錄**，
而該值僅存在於 `_pool_strips_for_block` 之區域變數；本批之注入點在 `②-宗` 閘處，
其時 `kept_iv` 之逐宗對應**已不在作用域內可辨**。⇒ 以**累積S** 代之（上表·來自 `partial.csv`）。

---

## §四　`Q-6`　射程（**`R3`〜`R6` 之判**）

🔴 **即使 `②-宗` 之 `raise` 已停用，管線<u>仍於 `R2` 中止</u>** ⇒ **`R3`〜`R6` 之 `②-宗` 破否<u>不可得</u>**。

🔒 **阻斷點之具名（⛔「未查」）**：**`結構閘 telescoping`**。
`rv.log` 逐字：`🟡 [StepG0m] 部分落檔 22 列 …（中止街廓 'R2'）`／`🟡 [StepG3.5m] 部分落檔 24 列 …（中止街廓 'R2'）`。

🔒 **全街廓之閘況（本批唯一新得之事實）**

| 呼叫序 | 街廓 | 宗數 | 宗-宗重疊 | 上界 | 破? |
|---|---|---|---|---|---|
| `0`／`2`／`4`／`6` | `R1` | `5` | `0.0000` | `0.6630` | 🟢 未破 |
| `1`／`5`／`7` | `R2` | `14` | **`45.9766`** | `2.8906` | 🔴 破 |
| **`3`** | `R2` | `14` | **`0.0000`** | `2.8906` | 🟢 **未破** |

🔑 **`②-宗` 之破<u>非恆定</u>**——`R2` 之 `4` 次呼叫中**有一次得 `0.0000`**。
🛑 **該次屬何情境／trunk ⛔ 可得**——**阻斷點**：本批之注入**未記情境／trunk 標籤**。

---

## §五　`Q-7`　`telescoping` 之逐界面明細，及其與 `②-宗` 是否同源

**二情境之破量相異**（`rv.log` 逐字）：

| 情境 | `ΣRw_實跑` | `R(W_final)−R(W₀)` | `Δ` |
|---|---|---|---|
| `0m` | `106.16` | `R(26.03)−R(0.00) = 100.00` | **`6.160`** |
| `3.5m` | `100.02` | `R(34.39)−R(6.98) = 44.41` | **`55.606`** |

🔒 `3.5m` 之三數**與 `-257 §三 Q-7` 所載逐位相符**。

**逐界面明細（`Δ_self` 欄）**

| 情境 | 首界面 | `Δ_self` | 其餘界面 |
|---|---|---|---|
| `0m` | **`628-41(1)`→`628-42(1)`** | **`-11.3570`**／**🔄方向改變** | 其餘 `5` 界面皆 `+0.0000` |
| `3.5m` | **`628-42(1)`→`628-41(1)`**（**順序相反**）| `+0.0000` | 全部 `6` 界面皆 `+0.0000`、**⛔ 方向改變** |

🔒 **判：⛔ 同源（二病）**——機械證據二項：
1. **`3.5m` 有 `telescoping` 破（`Δ=55.606`）而 `②-宗` <u>未破</u>**（`0.0000`）⇒ `telescoping` 破**⛔ 蘊含** `②-宗` 破。
2. **`0m` 之 `telescoping` 破可歸於單一界面之方向改變**（`Δ_self=-11.3570`），
   而 **`3.5m` 之逐界面全部 `Δ_self=+0.0000`、⛔ 方向改變** ⇒ 其 `Δ=55.606` **另有來源**。
   ⇒ **二情境之 `telescoping` 破本身亦非同一機制。**

🔑 **惟二者<u>共指同一宗</u>**：`0m` 之 `②-宗` 最大二對皆含 **`宗0` ＝ `628-41(1)`**，
而 `0m` 之 `telescoping` 首界面亦為 **`628-41(1)`**；且**二情境之推進序相反**
（`0m`：`628-41(1)` 在前／`3.5m`：`628-42(1)` 在前），而 **`②-宗` 破恰只在 `628-41(1)` 排第一時發生**。

---

## §六　`Q-5`　上界式之適配性（**⛔ 主張調整該上界**）

`上界 = (宗數 14 − 1) × 0.005 × 深度 44.4700 = 2.8906`。

| 面向 | 模型隱含 | 實測 |
|---|---|---|
| 非零對之分佈 | 每對疊 ≤ `0.005×深度`、**遍佈全體**（期望 ≈ `91` 對）| **`3`／`91`（`3.3%`）** |
| 單對之量級 | ≤ `0.2224`（＝ `0.005 × 44.47`）| **最大單對 `26.1624` ＝ 上界之 `9.1` 倍**、＝ 單對容許量之 `117.6` 倍 |

⇒ 🔒 **發單側之判成立**：模型（**每對疊一點點·遍佈全體**）與實測形狀（**單宗跨多宗·集中於 `2` 對**）**不符**。

---

## §七　`Q-3`／`Q-4`（**部分可得·阻斷點具名**）

- **`Q-3`（`宗0` 之 `s` 上界由何式算出）**：⛔ 可得。**阻斷點** ＝ 本批之注入點在 `②-宗` 閘（`app.py:9514`–`:9527`），
  其時 `s` 帶之構造已完成且**逐宗之 `(s_min, s_max)` 未被記錄**；`kept_iv` 於發單側實測**只落 `k=0` 一筆**，
  其成因（僅存池帶抑或另有斷路）**本批未查**——其查證須另一次注入（記錄 `kept_iv` 之逐筆與其來源）。
  🔒 **已得之替代量**：四宗之**累積S**（`§三` 表）——`宗0` `4.06`／`宗1` `4.81`／`宗2` `5.46`／`宗3` `12.0`。
- **`Q-4`（碎片 `宗1` 之來源）**：**部分可得**。`628-42(1)` 之 `a 面積 = 8.34`、`幾何面積 = 3.81`、`G = 3.82`
  ⇒ 其**於 `partial.csv` 中為一獨立宗且有原地號 `628-42`** ⇒ **係 CAD 原有之小宗，⛔ 切帶所生之殘片**。
  🛑 **「排除 `宗1` 後總疊是否收斂」⛔ 測**——該測須修改受測物（移除一宗後重跑），
  **逾本單「⛔ 修」之射程**；且 `§二` 已示 `宗1` 之二對疊皆為 `0.0000` ⇒ 其**⛔ 貢獻總疊**。

---

## §八　成因之判（單 `§四 b` 三選一）

🔒 **判 ＝ ③ 尚不能判**，惟**發單側之推定已獲二項正面支持、⛔ 被推翻**。

**已成立者**
1. `GB-67` 登記表所載受詞「**不相鄰**之宗之間為何會重疊」**係誤框**——`§三` 之 `bounds` 與**累積S**
   證 `宗0`（`4.06`）／`宗1`（`4.81`）／`宗2`（`5.46`）／`宗3`（`12.0`）在推進序上**相鄰或近乎共域**。
2. 重疊**集中於 `宗0` 之二對**（`56.9%`＋`43.1%` ＝ `100%`）⇒ 形狀係「**單宗跨多宗**」，⛔「每對疊一點點」。
3. `②-宗` 之破**非恆定**（`R2` 四次呼叫有一次 `0.0000`），且**恰與推進序相關**
   （`0m` `628-41(1)` 排第一 ⇒ 破；`3.5m` `628-42(1)` 排第一 ⇒ 未破）。

**尚差之證據（⇒ 為何判 ③）**
- `宗0` 之 `s` 上界**由何式算出**及其輸入實值（`Q-3`·阻斷點見 `§七`）；
- `kept_iv` 只落 `k=0` 一筆之成因（`Q-2` 所令·未查）；
- `R2` 那一次 `0.0000` **屬何情境／trunk**（`Q-6`·未記標籤）。

🔒 **三者皆須另一次診斷注入**（記錄 `kept_iv` 逐筆 ＋ 情境／trunk 標籤 ＋ 逐宗 `s` 區間）。

---

## §九　`GB-67`／`GB-153` 之受詞更正（**`W-G.4` 檔末追加**·⛔ 動原列一字·⛔ 鑄新號）

見 `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末之
`## 🔧 \`GB-67\` 之受詞更正 ＋ \`GB-153\` 射程之確認（\`W-G.9-257\`…）` 一節。

---

## §十　【驗收】

| 閘 | 實測 |
|---|---|
| `V-P1` | ✅ 本倉 `app.py` 倉側 blob ＝ `4860f10828fb8ae9`；十一檔全部逐位不變 |
| `V-P2` | ✅ 本倉 `git status --porcelain` ＝ `0` 列；clone 在 `C:/t257`（本倉外）、零痕跡 |
| `V-P3` | ✅ `91` 對完整清單出艙（`§二`）；非零者之和 ＝ **`45.9766`** ＝ 閘所報 `biz_x` **逐位** |
| `V-P4` | ✅ 四宗身分具名（暫編／原地號／`a`／`G`／**累積S**／側別）；🟡 **`s` 區間之阻斷點已具名**（`§三`／`§七`）|
| `V-P5` | ✅ **已測且判為不可得**——`R3`〜`R6` **未到達**，阻斷點 ＝ **`telescoping` 結構閘**（⛔「因中止而未知」之含混，已具名其為何中止）|
| `V-P6` | 見下 |
| `V-B` | `W-G.4` deletions ＝ `0` |
| `V-C` | 自誤／`GB`／`K-9` 之 `MAX` **前後不變**（本批⛔ 鑄號）|

🔒 **`V-P6`（射程閘）之自檢**：本報告內**⛔ 含修法主張**。
🩸 **受檢字樣以<u>代稱</u>行之**（`GB-132` 族之自指）——單 `§四 e` 所禁之三詞
（祈使性之「改」字系、「議」字系、「正為」字系）**⛔ 逐字寫入本報告**，
否則自檢句自身即成命中。**其逐字見單 `W-G.9-257 §三` 首句**。
🔒 `§七 Q-4` 之「⛔ 測」與 `§八` 之「尚差之證據」皆為**證據狀態之陳述**，⛔ 修法。
🔑 **通則（CC 側自立·本窗第三度）**：凡以「某字樣之命中 ＝ `0`」為閘者，
**該字樣⛔ 逐字出現於受檢文件**——含自檢句自身。
