# W-G.9-227R　`W-G.9-219R2` commit 1 併入主線 ＋ 放行後必辦 ＋ `GB-67` 查證（執行報告）

**批次性質** ＝ 一次**合併**（KL 逐字放行）＋ 零新增生產碼之登記與查證。
**合併前主線** ＝ `2fa1590da7ef680c616d85f8b1641fc5d106d20a`；**合併後主線** ＝ `8b0534c451a45a8d066e7bbc98a6ee7d67323432`。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
payload 一律 `Write` 落檔、⛔ heredoc（`GB-143` 補款二）。

---

## 工項零　✅ PASS

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **5896** | 89 | **0** | `4deef92aed3c8dcc9088f73a6565dcc0d201b4e0c41687dce4e2bee8b59e84e9` |
| 入倉（索引側 blob） | **5896** | 89 | **0** | `4deef92aed3c8dcc9088f73a6565dcc0d201b4e0c41687dce4e2bee8b59e84e9` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）。對照組：翻首位元組 ⧧ blob ＝ `True`；blob 自比 ＝ `True`。

---

## 工項一　合併 ✅ PASS

### a　既例之證據（`grep` 母體 ＝ `docs/` 之索引側 `.md` **501** 檔·**列框**）

🔒 **逐字**（`docs/orders/W-G.9-208_施工單_207入倉與自誤261與GB140與補款二與commit2授權.md:3`）：

> **基座**：`wip/s1-endpart` ＝ **`b772f8aa09820f01312081588fd8c8d718c8ac86`**（`W-G.9-198R commit 1` 已落地·**純快轉**·**⛔ merge commit**）

🔒 **史側之佐證（⛔ 只信文件）**：`b772f8aa`／`a1107a17`／`6e677223` 三者**父數皆 ＝ 1**、
且**皆為主線祖先**；主線全史 **687** commit 中 merge commit ＝ **4**（皆非此三者）。
🔒 **`--ff-only` 之既例**：`W-G.9-95Zv3.1_核對與三次退回.md:48`／`:74`／`:353`（其時未執行·惟形已定）。
🔒 **主線若已前進則改走 rebase 款**：`W-G.9-206_施工單_….md:278`「## §八　🔴 `48bad3c` 之 rebase 款」
＋ `W-G.9-207`／`-209`／`-211` 之 `--force-with-lease` 重推側分支。

⇒ 🔒 **既例 ＝ 純快轉（fast-forward）·⛔ merge commit·⛔ cherry-pick**；
rebase 僅於**主線已前進、快轉不可行**時方用。

### b　本批之合併方法

🔒 **快轉可行性之當場驗**：`8b0534c^` ＝ `2fa1590da7ef680c616d85f8b1641fc5d106d20a` ＝ 合併前主線頭
⇒ **純快轉可行**，**⛔ 需 rebase**。
執行 ＝ `git merge --ff-only origin/verify/W-G.9-219R2-c1`（輸出逐字 `Updating 2fa1590..8b0534c` ／ `Fast-forward`），
繼以 `git push origin HEAD:refs/heads/wip/s1-endpart`（`2fa1590..8b0534c`）。

### c　四項逐驗（⛔ 採單所抄之任何 hash）

| 項 | 實測 | 判 |
|---|---|---|
| ① `app.py` blob ＝ 側分支 | 主線 `5d45de2013216cedaaa8c65bede2b77b785245e0`（**1417936** B·`sha256 091efa9b…`）＝ 側分支**同值**；blob id 與內容**皆逐位相同** | ✅ |
| ① 對照組（器須紅）| 合併**前**主線之 `app.py` blob ＝ `a6a6ffaffcfa6c3aa1e4a4d163d069be9ce8e190` ⇒ 與現值**相異** | ✅ |
| ② 其餘四檔 `sha256` ＝ 合併前值 | `selection_pipeline` `6c678342…`／`run_verification` `e48f24c8…`／`stepg_pipeline` `02602733…`／`run_all` `4ba89fef…`——**逐檔相同** | ✅ |
| ③ 合併未引入 deletion | 合併之 `numstat` 合計 ＝ **824** 增／**1** 刪；側分支 commit 自身 ＝ **824** 增／**1** 刪 ⇒ **合併新增之 deletion ＝ 0**；`app.py` 之刪除列逐行 ＝ `-                        on_change=_f3L_invalidate_g_cache,`（**1** 行）| ✅ |
| ④ `left-right` | **`0 0`** | ✅ |

🔒 **合併後主線頭之父數 ＝ 1**（⛔ merge commit），其父 ＝ 合併前主線 `2fa1590…`。

---

## 工項二　放行後必辦之登記

### a　`GB-144` 補款二之落地（`docs/reports/W-G.4_泛用阻塞項登記表.md`·**節內末端**）

落地處 ＝ `GB-144` 節（`:3762`–`:3978`·**217** 列）之末端；Δ ＝ **+6695** B（＝ payload **6693** ＋ 分隔 **2**）。

**① 既登處之位移（當場實算·⛔ 採單之 `+36`）**

以**逐字相同**配對：位移量之**相異值 ＝ `{0, +36}`**（插入點之前 `0`、之後 `+36`）。
`19684`→**`19720`**／`19689`→**`19725`**／`22359`→**`22395`**／`22848`→**`22884`**／`23810`→**`23846`**；
其餘七處（`10947`／`12319`／`12675`／`12677`／`12680`／`12703`／`14259`）**行號未動**。
🔧 **量測器之當場更正**：`10947`／`12319` 二列**彼此逐字全等** ⇒ 配對器回「候選 2 個」而未配對，
遂於首版被誤列為「新增」。**二者實在新舊二集內、行號皆未動**——係**配對器之歧義**、⛔ 事實。

**② 新增之命中處（唯一）** ＝ `app.py:19685` ＝ `_SB_KEY = 'f3L_setback_default'`。

**③ 新增之顯示端與 wrapper**（**裸行號**·⛔ 落入 `G2` 之三群述詞）：
`19686` `_SB_DEFAULT_M`／`19688` 丟棄過期旗標／`19689` `_sb_present`／`19690` `_sb_val`／
`19691` `_sb_src`／`19694` `st.markdown`（顯示列）／`19713` `def _f3L_setback_changed`／
`19716` 設旗標／`19726` `on_change=` 之改綁；旗標鍵 `f3L_setback_user_set` ＝ **4** 處
（`19679`／`19688`／`19692`／`19716`）。

### 🔴🔴 `G2` 之重驗 ＝ **FAIL**——**append-only 與行號位移之結構性不相容**（本批之要害發現）

補款二落地後重跑 `-219` 之 `G2`（解析器**逐字沿用**·⛔ 改器）：

| 端 | 元素數 | 集合 |
|---|---|---|
| `app.py` 現態 | **13** | `[10947, 12319, 12675, 12677, 12680, 12703, 14259, 19685, 19720, 19725, 22395, 22884, 23846]` |
| `GB-144` 節（現行框 ＝ **節內全部** `app.py:NNN` 之聯集）| 🔴 **18** | 上列 ∪ `[19684, 19689, 22359, 22848, 23810]` |

🛑 **多出之 5 個 ＝ 補款一（`W-G.9-225`）之歷史完整命中集列**（本檔 `:3868`）。
🔒 **其⛔ 可撤**——`GB-144` 係 **append-only 正典檔**，歷史列**一字不刪**。
⇒ 🔴 **在現行框下，`G2` 於 `app.py` 行號**一經位移**即<u>永久轉紅</u>，且<u>任何登記皆無法使其復綠</u>**。

🔒 **節內「完整命中集」列之全史（機驗）**：`:3774`（**2** 個·`GB-144` 原文）／`:3868`（**12** 個·補款一）／
`:3965`（**13** 個·**本批補款二**）。
🟡 **候議之收緊框（出艙·🛑 ⛔ 由 CC 採用·⛔ 改任何既落之閘）**：
「取節內**最末**一個完整命中集列」⇒ 得 **13** 個，**與現態相符 ＝ `True`**（已機驗）。
🛑 **`G2` 之框係<u>閘之定義</u>，其修改屬發單側之權**——本批**⛔ 自改**，僅出艙二讀與其證據。
🔒 **本項承 `-225R` 工項一 `c` 之既有出艙**（「`G2` 之母體係節內全部 `app.py:〈行號〉` 字樣 ⇒ 對附帶引用敏感」）
——彼時為**潛在**，今**已實現**且更嚴重（⛔ 附帶引用所致，係**歷史正本**所致）。

### b　🔴 `3.5` 字面之耦合危害（已入補款二 `④`）

**現況**（當場出艙·母體 `app.py` **1417936** B／**23942** 列·**列框**；
命中式 ＝ 含 `3.5` **且**含 `setback`／`退縮`／`_SB_DEFAULT`／`f3L_setback`）＝ **11** 列（⛔ 單所述之四處）：

| 類 | 數 | 檔列（裸行號）|
|---|---|---|
| 🔴 **取值用之字面** | **4** | `19686`（顯示端）／`19720`（輸入欄預設）／`22395`／`22884` |
| **情境判別之字面** | **2** | `14275`（`abs(setback − 3.5)`）／`22398`（`abs(_yd_setback − 3.5)`）|
| 註解／訊息（⛔ 取值）| **5** | `10947`／`12319`／`12680`／`14272`／`14277` |

**處置** ＝ 將 `19720` 改讀顯示端之具名常數（一行·純重構·數值不變）⇒ **屬另單**（見工項四）；
**歸屬** ＝ `GB-144` 之補款、**⛔ 另鑄 `GB`**。

### c　旗標語意之正字化（`docs/reports/W-G.9-219R2R_…_執行報告.md` 檔尾就地加註）

🔒 **既有正字處之 `grep`（母體 ＝ 索引側 `.md` **553** 檔·**列框**）**：
「值曾被」＝ **2** 列／「欄曾被聚焦」＝ **2** 列——**其一為本單自身**，另一為
`docs/reports/W-G.9-219R2R_退縮值常設顯示_執行報告.md:129`（該報告之**上呈**節）
⇒ **唯一之在倉載體** ⇒ 依既例（報告內含他批之就地加註·本倉既有 **23** 例）於其檔尾追加。
Δ ＝ **+1661** B（＝ payload **1654** ＋ 分隔 **7**）。

**正字（⛔ 再議）**：🔒 **旗標所記者係「值曾被<u>改變</u>」，⛔「欄曾被聚焦」**；
Streamlit 僅於值真變動時觸發 `on_change` ⇒ 使用者見預設值而未改者，來源即「預設」。
🔒 **與 KL 裁示一致之理由**：KL 裁示之判準係「**在用之值是否即預設**」（「被沖掉而回落預設者，來源即為預設」），
⛔ 「使用者有無碰過該欄」⇒ `③` 之在用值即預設 ⇒ 判「預設」一致。
⇒ **`-219R2R` 之上呈已結案**（本批**⛔ 改一行碼**——現行實作已合本正字）。

### d　迴歸界限之登記（`CLAUDE.md` 檔尾·正本表增列 `序 12`）

Δ ＝ **+2880** B（＝ payload **2873** ＋ 分隔 **7**）。內容 ＝
「**`R4` 逐值迴歸待補**（俟 telescoping 閘解除後回補）」⬜ 未落地，並附**依賴鏈四環**（見工項三）。
🔒 逐字載明界限：替代證據係「**同一鏈之行為逐位相同**（含同點 `raise`）」，
🔴 **⛔ 讀為「已證 `R4` 逐值不變」**——後者所需之數值**現態產不出**。

---

## 工項三　`GB-67` 之查證 ⇒ **判 ①（已登記之待決閘）**·⛔ 停

### a　`GB-67` 之逐字（母體 `docs/reports/W-G.4_泛用阻塞項登記表.md`·含 `GB-67` 之列 **37**）

**受詞**（定義列 `:140`·截）：「🔴 **`②-宗` 破量之成因＝未知——舊歸因『同一界面雙定義』已撤回、
新受詞為『**不相鄰**之宗之間為何會重疊』**」。
**現況欄**：「**本案即觸發**（⛔ 非換案才觸發）——`②-宗 圍堵閘`（`app.py:9144` 註／`:9155` raise）於本案 `R2`／`R5` 破」。
🔒 **該二錨已位移**——當場復現 ＝ `app.py:9514`（註）／`:9524`–`:9525`（`raise`），其逐字：
`🔴 ②-宗 圍堵閘破[{_label}]：宗-宗重疊 = {biz_x:.4f} > 上界 {bound:.4f}`。
**處置欄**：「🔒 **本批只登記·⛔ 不查**（施工單明訂）。**下一手之最小動作** ＝ 於 `79bf2d7` 之後重跑 `probe_D2b17_dual` 並入倉」。
**結案與否** ＝ 🔴 **未結案**——其**歷次就地加註 11 節**（`:1868`／`:1884`／`:1908`／`:1972`／`:2033`／`:2081`／
`:2129`／`:2175`／`:2232`／`:2281`／`:2327`）末節（`:2327`）逐字為「**備料完結——落地介面清單已備
（`【缺】` 5·`②-宗` 三項全缺）**」，且 `:2334` 逐字載「`②-宗` 圍堵閘之破量 `R2` `45.9766 ㎡`／`R5` `56.3293 ㎡`（**⛔ 尚未修**）」。

### b　二 `raise` 之當場定位（⛔ 採單之 `:1261`）

🔴 **單之前提須更正**：`GB-67` 之受詞與 `W-G.9-219R2` 所遇之 `3.5` raise **係二事**。

| 情境 | 拋出處（當場定位）| 閘 | 是否 `GB-67` |
|---|---|---|---|
| 退縮 `3.5` | `verify/stepg_pipeline.py:1261`（其上尚有 `:1252` 之**鏈斷**式）| **結構閘 telescoping**（`ΣRw_側 == R(W_final) − R(W₀_buffer)`）| 🔴 **否** |
| 退縮 `0` | `verify/stepg_pipeline.py:1003`（外層 `raise RuntimeError(f"街廓 {blk_label} 抵費地計算失敗：{_eOff}")`）| **`②-宗 圍堵閘`**（`app.py:9524`）| ✅ **是** |

🔒 `圍堵閘破` 於 `verify/stepg_pipeline.py` 命中 ＝ **0**；於 `app.py` ＝ **1**（`:9525`）。
🔒 **所屬之結構閘族**（`verify/stepg_pipeline.py` 模組 docstring `:29`–`:32` 逐字）：
「**🆕 結構不變量永久閘（KL 閘裁定 2026-07-09；破即 RuntimeError）**：守恆／⊥（ALLOC⊥FRONT）／
位次＝投影序／J(k\*)≥J(naive)／理論＝實跑／**telescoping**（ΣRw_側 = R(W_final) − R(W₀_buffer)）。」
🔒 該族之閘於 `verify/stepg_pipeline.py` 之 `raise` 共 **多處**；本案所觸者 ＝ telescoping 之**第二式**
（`:1260` `if abs(_sum_rw_side - _tele_exp) > _IFACE_TOL:` → `:1261` `raise`）。

### c　判 ＝ **①（已登記之待決閘）**·逐項證據

| # | 證據（倉內·逐字）| 出處 |
|---|---|---|
| 1 | 「`v3·StepG{0m,3.5m}` 結構閘 `telescoping` 破（`R2 left ΣRw_實跑=100.02 ≠ 44.41`）…｜**既存紅**（**准紅碼·`W-G.9-4` 起**）｜❌ 否」 | `docs/reports/W-G.9-69_重烤與乙式耦合斷言.md:273` |
| 2 | 「`run_step_g` 於 `R2` 之 **`結構閘 telescoping 破`** 拋出而終止（**⛔ 非 `②-宗 圍堵閘`**）⇒ `R4`／`R5`／`R6` **一筆不產生**」 | `docs/reports/W-G.9-172R_池面積歸因與步驟0前置量測.md:27` |
| 3 | 「中止點｜`圍堵閘破[R5]`｜`telescoping 破[R2]`｜🔴 **相異**」 | 同上 `:59` |
| 4 | 「三個擋路 raise 中**二個係 harness 專屬**（`結構閘 telescoping 破`／`結構閘 理論＝實跑 破`·`app.py` 命中 `0`）」 | `docs/reports/W-G.9-134_CC交接文.md:129` |
| 5 | 同一破之**同一數值**（`ΣRw_實跑=100.02` ≠ `44.41`·Δ `55.606`）於歷批**反覆出艙** | `-27:141`／`-29:51`／`-173R:67`／`W-G.8_D-2b-6:26` |

🔒 **⇒ 二 `raise` 皆為①**：`3.5` 者係 **既存紅·准紅碼（`W-G.9-4` 起）**、**harness 專屬**（`app.py` 命中 `0`）；
`0` 者係 **`GB-67`** 之受詞、**未結案**、破量 `R2 45.9766 ㎡`／`R5 56.3293 ㎡`。
🔒 **⛔ ②（新破）**——本批（及 `W-G.9-219R2`）之改動**逐位未使其改變**：`-219R2` 之迴歸已證改前／改後
之**失敗形逐位相同**（同點、同訊息），且該二破之數值**與歷批既載完全一致**。
🔒 **⛔ ③（未經登記）**——上表五條皆為**在倉之既有登記**。

### d　依 ① 之處置：自解條件與待落地依賴鏈（已入 `CLAUDE.md` `序 12`）

| 環 | 內容 | 出處 |
|---|---|---|
| `1` | telescoping 破[R2] ＝ 既存紅（准紅碼·`W-G.9-4` 起）| `-69R:273` |
| `2` | 其後果 ＝ `R4`／`R5`／`R6` 一筆不產生 | `-172R:27` |
| `3` | 該閘 **harness 專屬**（`app.py` 命中 `0`）⇒ **⛔ 影響 app 之配地** | `-134:129` |
| `4` | 退縮 `0` 之中止點係 `②-宗 圍堵閘` ＝ `GB-67`（⛔ 尚未修）| `GB` 簿 `:2334` |

🛑 **`序 12` 之解除條件 ＝ 環 `1` 之閘轉綠**（⛔ 放寬該閘、⛔ 改判準）。
🛑 **⛔ 觸發 `d` 之上呈款**——判為 ①，⛔ ② 或 ③。
🔒 **本批⛔ 修一行碼、⛔ 放寬任何閘**（閘 `P` 五檔於合併之外逐位未動·見下）。

---

## 工項四　`:19720` 補正之最小形（**⛔ 落地、⛔ 入索引**）

**檔列** ＝ `app.py:19720`（當場復現）。

**before**（逐字·含縮排）
`                    _setback_init = float(st.session_state.get('f3L_setback_default', 3.5))`

**after**（逐字·含縮排·⛔ 改其縮排）
`                    _setback_init = float(st.session_state.get('f3L_setback_default', _SB_DEFAULT_M))`

🔒 **等值性**：`_SB_DEFAULT_M` 定義於**同一 `else` 分支內**且**位於本列之前**（裸行號 `19686`）⇒ 執行期必已綁定。
🔒 **數值不變**：`_SB_DEFAULT_M` 之字面即 `3.5` ⇒ **純重構**。
🔒 **未落地之機驗**：`app.py` 內 `_SB_DEFAULT_M))`（補正後之形）命中 ＝ **0**（須 `0`）；
對照組 `_SB_DEFAULT_M` 命中 ＝ **2**（須 > 0）。

---

## 閘 `P` ／ `numstat` ／ `left-right`

| 檔 | B | `sha256`（合併後·＝ 側分支值）| ＝ 側分支？ |
|---|---|---|---|
| `app.py` | 1417936 | `091efa9b77c288082edea9cce809c0bb53c8b927f7cd0ea2e646a962c6409628` | ✅ |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` | ✅ |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` | ✅ |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` | ✅ |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` | ✅ |

🛑 **閘 `P` ＝ PASS**——本批於**合併之外**⛔ 動生產碼一位元組。

| 檔（本 commit·vs 合併後之主線）| 增 | **刪** |
|---|---|---|
| `CLAUDE.md` | 36 | **0** |
| `docs/orders/W-G.9-227_219R2併線與放行後必辦與GB-67查證.md` | 89 | **0** |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 97 | **0** |
| `docs/reports/W-G.9-219R2R_退縮值常設顯示_執行報告.md` | 27 | **0** |
| **四檔小計（穩定值）** | **249** | **0** |
| `docs/reports/W-G.9-227R_…_執行報告.md`（**本報告**）| 🔒 ⛔ 於本報告內釘死（自指）| **0** |

🛑 **`deletions` ＝ `0`**（append-only 檔全數）。`app.py` 之 `1` 刪係**合併所帶**（側分支既有），**⛔ 本 commit**。
🔒 `left-right`（推前）＝ **`0 0`**。追蹤 ＝ **1625** 檔／`.md` ＝ **553** 檔（`ls-files -z`）。
🔒 **`戒 40`（二端並報）**：`GB` 簿 Δ `+6695` ＝ payload `6693` ＋ 分隔 `2`；
`-219R2R` 報告 Δ `+1661` ＝ `1654` ＋ `7`；`CLAUDE.md` Δ `+2880` ＝ `2873` ＋ `7`——**逐檔差 `0`**。
🔧 **落地器之當場更正**：首版以**工作樹**位元組核 Δ，而 `-219R2R` 於快轉後為 **CRLF**
（`243` 列 ⇒ `243` 個 `CR`）⇒ 正規化後之 Δ 少 `243`。**改以索引側 blob 為錨後三檔全符**（`GB-139 ②` 之又一實例）。

---

## 🟡 仍待

1. 🔴🔴 **`G2` 之框須由發單側裁**——現行框（節內全部 `app.py:NNN` 之聯集）與 **append-only ＋ 行號位移**
   **結構性不相容**（今得 **18** vs 現態 **13**）；候議之收緊框（取節內**最末**一個完整命中集列）已機驗**相符**，
   🛑 **⛔ 由 CC 採用**。
2. 🔴 **`:19720` 之補正**（工項四之最小形已備）——含生產碼、須另單並經 KL 放行。
3. 🟡 **`序 12`（`R4` 逐值迴歸待補）** ⟸ telescoping 結構閘轉綠。
4. 🟡 **`GB-67` 未結案**（破量 `R2 45.9766 ㎡`／`R5 56.3293 ㎡`）——其下一手之最小動作已載於 `GB-67` 原文。
5. 🟡 `GB-148` 之碼面處置／已失效哨兵殘留／層二 `188` 號單原文——併期末批。
