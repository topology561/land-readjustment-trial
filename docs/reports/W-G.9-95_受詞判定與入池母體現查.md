# `W-G.9-95`　🔴 `W-D.4 碎片遞補` **不填任何東西**（⇒ 停機③ 解除）＋ 入池母體**可機械枚舉** ＋ 容差錨更正清單 ＋ `raise` 閘**非單點惟從未紅過**

**基座**：`b31f1ff`（`origin/wip/s1-endpart`·`W-G.9-94`）——🔒 與施工單所載**一致**。
**性質**：🔒 **只查不判**·⛔ **零生產碼變更**·⛔ **零裁定**·入倉物 ＝ **本報告**（§Z ⛔ 另批）
**受詞**：`A` `W-D.4` 之受詞判定／`B` 入池母體之全部來源／`C` 容差錨之射程掃／`D` `raise` 閘之單點檢

---

## §0　五句結論

1. 🔴 **`A-2` 決定性**：`W-D.4 碎片遞補` 之「填充對象」＝ **一個 CSV 欄位值**——
   `遞補標的宗` 之**可執行下游窮舉 ＝ 3 處**（寫檔／`diff_rows` 對 baseline／一個錨閘），
   **⛔ 無任何配地被改變**、**⛔ 不填位次、⛔ 不填面積缺口**。
   而 `K-6:2344`／`K-9-11` 填的是 **位次**（＋「騰出之地**入調配池**」）
   ⇒ 🔒 **二者<u>不同受詞</u>** ⇒ **停機③ ⛔ 不觸發**（其解除條件逐字：「`A-2` 判填充對象非位次 ⇒ 不觸發」）。
2. 🔴 **`A-3` 五欄全異 ＋ 逐格 5／6 異，且其<u>機制已坐實</u>**：`累積S` 係**配<u>後</u>**之推進累加器
   （左右**反向**二數線·`round(,2)`），`_projection_order` 係**配<u>前</u>**之單次投影排序。
   **6 ／ 6** 每組「`累積S` 序之首宗」皆為**街角第 1 宗**，而其投影序 **3／4 非該組最小**
   ⇒ 🔒 **街角第 1 宗之上位重排即二序分家之成因**。
3. ✅ **`B-3` 必答句 ＝ `是`**：入調配池之母體**可由現行碼機械枚舉**——
   `_stage2_parcels = [tp for tp in parcels_in_blk if '配地階段' in tp]`
   （`app.py:19407`／`verify/stepg_pipeline.py:417`；旗標由 `verify/wf_f4.py:211-213` `add_syn` 寫入）
   ⇒ 🔒 **§六之 `T3′` <u>可機械判</u>**。
4. **`D-2` ＝ 非單點（有<u>間接</u>冗餘）**，**`D-3` ＝ ⛔ 查無曾實際紅過之記錄**
   ⇒ **停機⑤ ⛔ 不觸發**（解除條件：「任一他物會紅 ⇒ 不觸發」）。
   ⚠️ 🔒 **⛔ 無任何<u>直接</u>之同義檢**——間接冗餘倚賴 baseline，而 **baseline 一重烤即失效**。
5. 🩸 **`-94` 之一處自誤當場捕獲**：baseline CSV 實為 **10 支**（`{v2,v3,PRE-*×3}` ＝ 5 目錄 × 2），
   `-94` 誤記 **12** ⇒ 本批更正。🔒 **⛔ 不追改 `-94`**（append-only）。

---

## §A　`A-0`　先驗錨（**本單第一件事**·⛔ 其後受詞在此之前一律未開辦）

### A-0-1　四錨

| # | 期望 | 實得 | 判 |
|---|---|---|:--:|
| 1 | 基座 ＝ `b31f1ff` | `b31f1ff09ff7f1066aff0f0d5931a6c4deb4c317`（🔒 `git ls-remote origin refs/heads/wip/s1-endpart` 直查）；本地 `HEAD` 同值 | ✅ |
| 2 | `app.py` blob ＝ `a9e5671d64d254907a0396f898f046d9d85e8283` | 同值；工作區 `git hash-object app.py` 亦同值 | ✅ |
| 3 | 本批結束時 `app.py` 逐位與 2 相同 | `git diff b31f1ff -- app.py` ＝ **空**（見 §F） | ✅ |
| 4 | 開工閘全套 | `wg942_append_audit.py` `rc = 0`·「✅ 全部成立」；`git status --porcelain` **空**；`TARGETS` **75 項** | ✅ |

⇒ **停機① ⛔ 不觸發。**

### A-0-2　併驗（`-94` 之轉引錨·⛔ 逐列附產生指令與源）

🔒 **產生指令**：`git show b31f1ff:<path>` → `s.split(chr(10))[n-1]` → NFKC ＋ 去空白／`**`／反引號後之**子字串**比對
🔒 **母體鎖 `commit`**（`-94` 修法）·⛔ **不讀工作區**。

| # | 檔:行 | 判 |
|---|---|:--:|
| 1 | `verify/run_verification.py:903` | ✅ |
| 2 | `verify/wd4_tier_list.py:353` | ✅ |
| 3 | `verify/wd4_tier_list.py:382` | ✅ |
| 4 | `verify/stepg_pipeline.py:498` | ✅ |
| 5 | `verify/stepg_pipeline.py:507` | ✅ |
| 6 | `K-6:2505`（`K-9-13`） | ✅ |
| 7 | `K-6:2149`（「投影順序」） | ✅ |
| 8 | `K-6:106` | ✅ |
| 9 | `K-6:109` | ✅ |
| 10 | `K-6:59` | ✅ |
| 11 | `K-6:2344` | ✅ |
| 12 | `app.py:7518` | ✅ |

`POPULATION=12 PRINTED=12 SUPPRESSED=0` ⇒ ✅ **12 ／ 0**
🔒 **判別力（陽性對照）**：`K-6:59` 原句 ＝ `True`；**僅改一字**（`禁另訂` ⇒ `禁另定`）＝ `False` ⇒ 該對驗**非恆真**。

### A-0-3　🩸 **`-94` 之轉引錨一處不符（本批更正）**

| 轉引（`-94`） | 本批現查 | 判 |
|---|---|:--:|
| baseline CSV **12 支** | **10 支** | 🔴 **不符** |

🔒 **產生指令**：`git ls-tree -r -z --name-only b31f1ff`（母體 **1309 檔**）→ 篩含 `W-D.4_碎片遞補對照`
🔒 **成因（自陳）**：`{v2, v3, PRE-*×3}` ＝ **5 目錄 × 2 情境 ＝ 10**，我於 `-94` 寫成 `12` ——**算術滑失**。
🔒 **併驗自校**：`-94` §B-3 之 17 命中檔 ＝ **10**（CSV）＋ **4**（`PROVENANCE_v3.md`）＋ **3**
（`docs/W-D.4_合併分配規格.md`／`run_verification.py`／`wd4_tier_list.py`）⇒ `10+4+3 = 17` ✅
⇒ 🔒 **17 這個數當時是對的，被拆錯的是它的組成。**
⛔ **不追改 `-94`**（append-only·`CLAUDE.md`「既有報告不追改」）。

---

## §B　受詞 `A`：`W-D.4 碎片遞補` 之**受詞判定**

🔒 **⛔ CC 不判、⛔ 發單側不判**——本受詞只取**判定所需之事實**。

### B-1　`A-1`　觸發條件之比對

🔴 **首須更正施工單之表列**：`K-6:2344` **⛔ 非獨立機制**——它**就在 `K-9-11` 之內**
（`K-9-11` 起於 `K-6:2300`「### 🔒 K-9-11 零條合格垂線之後果…」；`:2342-2344` 為其「**二、後果之變更**」）
⇒ 🔒 **四者實為三**。

| 機制 | 觸發條件（逐字 ＋ 錨） |
|---|---|
| **`W-D.4 碎片遞補`** | `wd4_tier_list.py:359-364`：`if r.get("推進側別") != "抵費地": continue` ∧ `if cat != "碎片": continue`（`cat` ← `frag_cls` ＝ **`W-D.3` 三分類**）⇒ **抵費地列 ∧ 三分類為「碎片」** |
| **`K-9-11`**（含 `K-6:2344`） | `K-6:2342`「某宗依其 G 值所形成之面寬，使該宗**一條合格垂線都沒有**時」 |
| **`K-9-13`**（`K-6:2505`） | `K-6:2513-2514` KL 逐字「凡達不到畸零地寬度及深度，就代表該宗土地不得以建築，也就未達該街廓可以分配標準」 |

**必答句（逐字回覆）**：

> 「四者之觸發條件**是否為同一集合**？」

**答：`否`**（具名差集）。

| 對 | 判 | 差集（逐字） |
|---|---|---|
| `K-9-11` vs `K-6:2344` | **同一** | 🔒 **同一條文之同一款**（`:2344` 為 `:2342` 之續句）⇒ ⛔ 非二事 |
| `K-9-11` vs `K-9-13` | **不同觸發·相同後果** | 觸發：**零合格垂線** vs **寬深不足**；🔒 惟 `K-6:2346-2348` 逐字「**遞補之機制⛔ 不另訂**……**一律沿用 `K-9-9 四`**」⇒ **後果同一** |
| `W-D.4` vs 上列三者 | 🔴 **完全不同** | `W-D.4` 之觸發 ＝ **`推進側別 == "抵費地"` ∧ 三分類「碎片」**；⛔ **與「合格垂線」「寬深雙檢」皆無關**；⛔ 亦不以「不配地」為前提 |

### B-2　`A-2`　填充對象之比對（🔴 **本受詞之決定性一格**）

🔒 **產生指令**：`git ls-tree -r -z --name-only b31f1ff` → 篩 `verify/**.py` ∪ `app.py`（**191 檔**）
→ 逐行 `term in line` → **v2 位置式分層**（`-94` §B-1 之修正後分層器）

| 字樣 | 命中 | **可執行層** | 逐字（⛔ 全列） |
|---|---:|---:|---|
| `遞補標的宗` | 2 行／2 檔 | **1** | `run_verification.py:903` `_ok_anc = (bool(_r6) and _r6[0]["遞補標的宗"] == "628-4(1)"` |
| `跳過失格筆` | 2 行／2 檔 | **1** | `run_verification.py:904` `and _r6[0]["跳過失格筆"] == "628(2);628-1(2);628-23(1)")` |
| `["frag"]` | 6 行／3 檔 | **6** | 見下 |
| `W-D.4_碎片遞補對照` | 2 行／2 檔 | **0** | （無） |
| `got_W-D.4_遞補` | 1 行／1 檔 | **0** | （無） |

**`d["frag"]` 之 6 個可執行消費點（⛔ 全列）**：

```
verify/wd4_tier_list.py:425                       _write(os.path.join(OUTDIR, f"W-D.4_碎片遞補對照_退縮{tag}.csv"), d["frag"])
verify/run_verification.py:816                    _dump_csv(_d4["frag"], os.path.join(OUTDIR, f"got_W-D.4_遞補_退縮{tag}.csv"))
verify/run_verification.py:822                    ok_r, v_r = diff_rows(_d4["frag"],
verify/run_verification.py:902                    _r6 = [f for f in _d0["frag"] if f["碎片"] == "R6-抵費地-2"]
verify/probes/probe_capacity_decomp_solve.py:51   d["pool"][b] = float(m.group(2)); d["frag"][b] = float(m.group(3))
verify/probes/probe_capacity_decomp_solve.py:126  "乙釋forced": {b: max(d["pool"][b] - d["frag"][b], 0.0) for b in blocks},
```

⚠️ 🔒 **具名二個<u>同名不同量</u>（⛔ 不得混入）**：
1. `probe_capacity_decomp_solve.py` 之 `d["frag"]` ＝ **該探針自身之 dict**（值為**面積浮點**），⛔ 非 `wd4_tier_list` 之 `frag` 列表。
2. `verify/wf_f1.py:242-254` 亦有 `frag_rows`（**F.1 碎片處置**·欄名為 `處置`／`標的宗`）
   ——⛔ **與 `W-D.4` 之 `frag_rows` 非同一物**；其處置字串逐字為
   「已整形吞納（域裁(B)：等G重切＋後續宗前移）」／「標記·待 F.4 終態遞補整形（…）」。

🔴 **答（`A-2`）**：

| 機制 | **填的是什麼** | 逐字錨 |
|---|---|---|
| `W-D.4 碎片遞補` | 🔴 **一個 CSV 欄位值**（`遞補標的宗`／`跳過失格筆`）——其可執行下游**僅**：① `_write` CSV ② `_dump_csv` ＋ `diff_rows` 對 baseline ③ **一個錨閘**。**⛔ 不填位次、⛔ 不填面積缺口、⛔ 不改變任何配地。** | 上表 6 行 |
| `K-6:2344`／`K-9-11` | **位次**（序列中之一格）＋「**騰出之地入調配池**」 | `K-6:2149-2150`「重劃前投影順序中**下一位**之土地**遞補該位次**；騰出之地**入調配池**。」 |

⇒ 🔒 **二者填充對象<u>不同</u>** ⇒ 🛑 **停機③ ⛔ 不觸發**。
⚠️ 🔒 **本判之射程**：本節只證「`W-D.4` **現行**不改配地」；⛔ **未判**其可否**被改造**為「乙」之介面
——該決定屬 §附-1（**須在新窗作成**）。

### B-3　`A-3`　定義層比對（⛔ 非逐格比對）

🔒 **產生式之逐字鏈**：
`累積S(m)` ← `app.py:19363` `'累積S(m)': round(float(_res.get('_alloc_cum_S', 0.0)), 2)`
（`verify/stepg_pipeline.py:374` 同式）← `app.py:19856`／`:19960` `res['_alloc_cum_S'] = left_cum_S`／`right_cum_S`
← `app.py:19855` `left_cum_S += _S_actual`／`:19959` `right_cum_S += _S_actual`
← 起點 `app.py:19735-19736` `left_cum_S = float(_left_buffer_S)`／`right_cum_S = float(_right_buffer_S)`

| # | 問 | `_projection_order` | `累積S(m)` |
|---|---|---|---|
| 1 | **產生式** | `app.py:7518-7544`　`sorted(parcels, key=_proj_of)`，`_proj_of(tp) = line.project(Polygon(tp['polygon_coords']).representative_point())` | `app.py:19855-19856`　`left_cum_S += _S_actual; res['_alloc_cum_S'] = left_cum_S`；出艙時 **`round(…, 2)`** |
| 2 | **投影到哪一條 `line`** | `LineString([front_line_p1, front_line_p2])`（**FRONT_LINE**） | 🔴 **⛔ 不投影**——係**累加器**，全鏈無 `line.project()` |
| 3 | **投影哪一個點** | **`representative_point()`** | 🔴 **⛔ 無點**（累加標量） |
| 4 | **「累積」之受詞** | ⛔ **無累積**（單次 `project`） | **同側之逐宗 `S` 累加**；起點 ＝ `_left_buffer_S`／`_right_buffer_S`（⛔ 非 `0`）；**逐街廓、逐側各自重設** |
| 5 | **帶號／重設原點** | 單一數線（`p1` → `p2`）·⛔ 無 `round` | 🔴 **左右二個獨立累加器**，右側自街廓**另一端反向**推進（`d_hat_rev`·`app.py:19915`）⇒ **二側之 `累積S` ⛔ 不在同一數線上**；且 **`round(,2)`** |

🔒 **比對器之判 ＝ 「不同」·差異欄 ＝ 5 ／ 5（全異）**。

### B-4　`A-3` 之逐格比對（🔒 **用途僅為證偽**）

🔒 **⛔ 不跑 pipeline**——母體 ＝ **入倉 baseline CSV**＠`b31f1ff`：
`verify/baselines/v3/G 值計算結果_退縮0m.csv`（**67 列**·`累積S(m)`／`推進側別`）
＋ `verify/baselines/v3/W-D.1.2 診斷_退縮0m.csv`（**18 列**·`原位次(投影序)`）
🔒 **`原位次(投影序)` 之來源鏈（已現查）**：`app.py:18672` ← `_pre_position_rank` ← `app.py:18505`
← `_rank_by_tpid` ← **`app.py:18458-18462`** `{tp.get('暫編地號'): _i_rk + 1 for _i_rk, tp in enumerate(_projection_order(_all_in_blk, _fl_p1_lstep, _fl_p2_lstep))}`
⇒ 🔒 **該欄即 `_projection_order` 之 1-based rank**（⛔ 非近似）。

| 街廓 | 側 | n | 按 `累積S` 之序 | 按 `原位次(投影序)` 之序 | 同? |
|---|---|---:|---|---|:--:|
| R1 | left | 2 | `628-37(1)`, `628-35(2)` | `628-35(2)`, `628-37(1)` | 🔴 **異** |
| R1 | right | 2 | `628(5)`, `628-36(1)` | `628-36(1)`, `628(5)` | 🔴 **異** |
| R2 | left | 3 | `628-41(1)`, `628-42(1)`, `628-27(1)` | `628-42(1)`, `628-41(1)`, `628-27(1)` | 🔴 **異** |
| R3 | right | 4 | `628-45(2)`, `628-42(2)`, `628-27(2)`, `628-28(1)` | `628-45(2)`, `628-28(1)`, `628-27(2)`, `628-42(2)` | 🔴 **異** |
| R5 | left | 3 | `628-18(2)`, `628-53(2)`, `628-45(1)` | `628-53(2)`, `628-18(2)`, `628-45(1)` | 🔴 **異** |
| R6 | right | 2 | `628-18(1)`, `628-38(1)` | `628-18(1)`, `628-38(1)` | ✅ 同 |

`POPULATION=6 PRINTED=6 SUPPRESSED=0` ⇒ **異 5 ／ 同 1**

🔒 **用途之限制（施工單逐字）**：逐格**不同** ⇒ **決定性**（二者確非同一）；
逐格**相同** ⇒ ⛔ **不得據以認定同一**（`-93` 已裁：②③ `10/10` 亦相同而 `representative_point` ≠ 中點）。

⚠️ 🔒 **母體之具名**：`W-D.1.2 診斷` 只涵蓋**街角候選**（18 宗），⛔ **非全宗** ⇒ 上表之 `n` 為**該側候選數**、⛔ 非該側全宗數。

### B-5　🔴 二序分家之**成因坐實**（⛔ 非猜測）

| 街廓 | 側 | `累積S` 序之**首宗** | `累積S` | 投影序 | `街角地` | **`第1筆街角`** | `街角側別` |
|---|---|---|---|---|---|---|---|
| R1 | left | `628-37(1)` | 6.49 | 3 | 是 | **是** | 左側 |
| R1 | right | `628(5)` | 7.25 | 6 | 是 | **是** | 右側 |
| R2 | left | `628-41(1)` | 5.69 | 2 | 是 | **是** | 左側 |
| R3 | right | `628-45(2)` | 6.12 | 11 | 是 | **是** | 右側 |
| R5 | left | `628-18(2)` | 6.44 | 2 | 是 | **是** | 左側 |
| R6 | right | `628-18(1)` | 7.22 | 9 | 是 | **是** | 右側 |

`POPULATION=6 PRINTED=6 SUPPRESSED=0` ⇒ 🔒 **6 ／ 6 皆為街角第 1 宗**

**其投影序是否為該組最小？**

| 組 | 首宗 | 其投影序 | 該組投影序最小 | 判 |
|---|---|---:|---:|:--:|
| R1/left | `628-37(1)` | **3** | 1 | 🔴 **不同** |
| R2/left | `628-41(1)` | **2** | 1 | 🔴 **不同** |
| R5/left | `628-18(2)` | **2** | 1 | 🔴 **不同** |
| R3/right | `628-45(2)` | **11** | 11 | ✅ 相同 |

⇒ 🔒 **成因 ＝ 街角第 1 宗被<u>上位</u>至街角**（`app.py:19562` `ordered_v2.sort(key=lambda e: (not e['is_corner_winner'],))`；
`stepg_pipeline.py:492` 註解逐字：「`ordered_v2` 保留投影序（**僅 PK winner 最小擾動上位**）」）
⇒ **`累積S`（配<u>後</u>）與 `投影序`（配<u>前</u>）之分家係<u>設計內生</u>**，⛔ 非缺陷、⛔ 非浮點。

### B-6　`A-4`　baseline CSV 之射程（⛔ 只量不改·⛔ 不重烤）

🔒 **清單（`commit` ＝ `b31f1ff`·**10 支**·🩸 `-94` 誤記 `12`·見 §A-0-3）**：

| # | 檔 | 列數 |
|---|---|---:|
| 1 | `verify/baselines/PRE-S0b_S0c_幾何漏歸零_凍存/v3/W-D.4_碎片遞補對照_退縮0m.csv` | 3 |
| 2 | `verify/baselines/PRE-S0b_S0c_幾何漏歸零_凍存/v3/W-D.4_碎片遞補對照_退縮3.5m.csv` | 2 |
| 3 | `verify/baselines/PRE-比率更新_凍存/v3/W-D.4_碎片遞補對照_退縮0m.csv` | 3 |
| 4 | `verify/baselines/PRE-比率更新_凍存/v3/W-D.4_碎片遞補對照_退縮3.5m.csv` | 2 |
| 5 | `verify/baselines/PRE勘誤_期日/v3/W-D.4_碎片遞補對照_退縮0m.csv` | 3 |
| 6 | `verify/baselines/PRE勘誤_期日/v3/W-D.4_碎片遞補對照_退縮3.5m.csv` | 2 |
| 7 | `verify/baselines/v2/W-D.4_碎片遞補對照_退縮0m.csv` | 3 |
| 8 | `verify/baselines/v2/W-D.4_碎片遞補對照_退縮3.5m.csv` | 2 |
| 9 | **`verify/baselines/v3/W-D.4_碎片遞補對照_退縮0m.csv`**（**現行**） | **2** |
| 10 | **`verify/baselines/v3/W-D.4_碎片遞補對照_退縮3.5m.csv`**（**現行**） | **2** |

🔒 **各鎖何值（欄名·逐字）**：`情境`／`碎片`／`面積(㎡)`／`沿街s`／`側`／**`遞補標的宗`**／**`跳過失格筆`**／`定序註`
⚠️ 🔒 **具名**：三支 `PRE-*` 與 `v2` 之 `退縮0m` 為 **3 列**、現行 `v3` 為 **2 列** ⇒ **凍存者多一列**（⛔ 本批未查其差之內容）。

**🔒 逐位重現（⛔ 複製未對之前不談差）**——以 `G 值計算結果_退縮{0m,3.5m}.csv` 重跑 `wd4_tier_list.py:369-377` 之走法
（`sorted(key=累積S(m))` → 首個滿足 `S(m) > 0.01 ∧ 宗地寬度(m) >= mw` → `target`；其餘入 `跳過失格筆`；`mw = 3.50`）：

| 情境 | 碎片 | 重現 `target` | 重現 `skip` | baseline | 判 |
|---|---|---|---|---|:--:|
| 0m | `R1-抵費地-2` | `628-37(1)` | （空） | `628-37(1)`／（空） | ✅ 逐位相符 |
| 0m | `R6-抵費地-2` | `628-4(1)` | `628(2);628-1(2);628-23(1)` | 同 | ✅ 逐位相符 |
| 3.5m | `R1-抵費地-2` | `628-36(1)` | （空） | `628-36(1)`／（空） | ✅ 逐位相符 |
| 3.5m | `R6-抵費地-2` | `628-4(1)` | `628(2);628-1(2);628-23(1)` | 同 | ✅ 逐位相符 |

`POPULATION=4 PRINTED=4 SUPPRESSED=0` ⇒ 🔒 **4 ／ 4 ⇒ 複製忠實 ⇒ 方可談差**

**🔴 反事實：若 `W-D.4` 之定序改綁 `_projection_order`（🔒 只量·⛔ 不判該不該改）**

| 情境 | 碎片 | 該側全宗 | 有投影序者 | 覆蓋 | 反事實 `target` | 判 |
|---|---|---:|---:|---|---|---|
| 0m | `R1-抵費地-2` | 2 | 2 | **完整** | 🔴 **`628-35(2)`**（現行 `628-37(1)`） | 🔴 **值會變 ⇒ 該 CSV <u>失效</u>** |
| 0m | `R6-抵費地-2` | 6 | 0 | **不完整** | — | **`【資料不足】`** |
| 3.5m | `R1-抵費地-2` | 1 | 1 | **完整** | `628-36(1)`（不變） | ✅ **不受影響** |
| 3.5m | `R6-抵費地-2` | 6 | 0 | **不完整** | — | **`【資料不足】`** |

🔒 **`【資料不足】` 之具名**：R6 該側之 6 宗（`628(2)`／`628-1(2)`／`628-23(1)`／`628-4(1)`／`628-22(1)`／`628-21(1)`）
**皆非街角候選** ⇒ `W-D.1.2 診斷` 無其 `原位次(投影序)`；補齊須**執行 `_projection_order`**（本批 ⛔ 不跑）。

**⇒ 逐檔判**：

| 檔 | 判 | 理由（一句） |
|---|---|---|
| 1／3／5／7／**9**（`退縮0m` 五支） | 🔴 **失效** | 其 `R1-抵費地-2` 之 `遞補標的宗` 於反事實下由 `628-37(1)` 變為 `628-35(2)` |
| 2／4／6／8／**10**（`退縮3.5m` 五支） | **`【資料不足】`** | `R1` 列不受影響，惟 `R6` 列之反事實**不可算**（該側 6 宗無投影序） |

⛔ **未改動任何 CSV**·⛔ 未重烤。

### B-7　`A-5`　預宣證偽輸入（🔒 **量測器自證在前**）

| # | 檢查 | 預宣之證偽輸入 | 預期 | 實測 | 判 |
|---|---|---|---|---|:--:|
| 1 | 定義層比對器有判別力 | `_projection_order` 與**它自己** | **判為同一** | **同一**（差異欄 ＝ `[]`） | ✅ |
| 2 | 同上·反向 | `_projection_order` 與**組內索引序**（`-93` 候選 ①） | **判為不同** | **不同**（差異欄 ＝ `產生式`／`投影線`／`投影點`／`方向／原點`·**4 欄**） | ✅ |
| 3 | 逐格比對之證偽用途 | `R2左`／`R5左` | **應現出不一致** | 二組皆 **不一致** | ✅ |

🔒 **CC 自加之量測器自證（⛔ 非施工單所令）**：
① 同一序與**它自己**逐格比 ⇒ **全同** ✅（證該比對器**非恆「異」**）；
② **組內索引序** vs `投影序` 逐格 ⇒ 異 **4 ／ 6** 組 ✅（證該比對器**會咬**）。
⇒ 🔒 **雙向皆非恆定** ⇒ **量測器非紅** ⇒ **停機④ ⛔ 不觸發**。

---

## §C　受詞 `B`：入調配池母體之**全部來源**

### C-1　`B-1`　`K-6:106` 與 `K-6:109` 之逐字（前後 ≥5 行·⛔ 不節錄·⛔ 不改標點）

```
K-6:98   以垂直於前緣線之分割線，取「使街廓內該筆面積恰好對半」之位置**真切為兩片**，兩片
K-6:99   **各自**計算所在側之街角指數與 G。
K-6:100  - 兩片各自拿下左右兩街角 ⇒ 保持**兩宗**
K-6:101  - 僅一片拿下 ⇒ **合回一片**，置於得標之街角
K-6:102  - 兩片皆未拿下 ⇒ 全往指數高之一側 ⇒ **合回一片**
K-6:103  - 兩側指數並列 ⇒ 取**前緣線 p1 側**（決定性，**禁隨機**）
K-6:104  
K-6:105  ## 三、與七級調配之分界
K-6:106  - 進入七級調配者 ＝ 街角定案並辦原投影分配後，**未通過寬深雙檢** 或 **未達街廓最小
K-6:107  建築面積**（若有輸入）
K-6:108  - F.0 級0/0′ 判「達標宗」之門檻 ＝ **寬深雙檢**（**不是** `region_min`）
K-6:109  - 跨街廓同歸戶「小往較大集中」一律歸七級調配（F.0／F.2）；**街角不再有專屬救援機制**
K-6:110  
K-6:111  ## 四、作廢與置換
K-6:112  - **K-4-3 整套街角救援作廢**（M-5 ①②③、來源母體 (a)(b)(c)、A4 registry 硬閘），碼與
K-6:113  裁定一併封存
K-6:114  - `app.py` 之「同原地號合併」述詞置換為**同歸戶 ∧ 幾何連通**
K-6:115  - `MinA = D_avg × min_width` 作為**逐街廓達標判準**之角色廢除；`region_min` 算式保留、
K-6:116  用途收斂
```

### C-2　`B-2`　來源之窮舉

🔒 **判準（正面列舉）**：正典內逐字謂「**⇒ 進入七級調配／列入合併調配／入調配池**」者。

| # | 來源 | 正典錨（逐字） | 碼側實作 | 在乙甲下游? |
|---|---|---|---|:--:|
| 1 | **未通過寬深雙檢** | `K-6:106-107`「進入七級調配者 ＝ 街角定案並辦原投影分配後，**未通過寬深雙檢** 或 **未達街廓最小建築面積**（若有輸入）」 | `【查無·K-6-A2 未實作】`（`CLAUDE.md` 逐字：「已裁定、K-6-A2 尚未實作」） | **是**（＝「甲」） |
| 2 | **未達街廓最小建築面積** | 同上 | `【查無】` | **是**（＝「甲」） |
| 3 | **零合格垂線** | `K-6:2342-2344`（`K-9-11 二`） | `【查無】` | **是**（＝「乙」） |
| 4 | **不得建築者** | `K-6:2505`／`:2513-2515`（`K-9-13`） | `【查無】` | **是** |
| 5 | 🔴 **跨街廓同歸戶「小往較大集中」** | `K-6:109`「跨街廓同歸戶「小往較大集中」一律歸七級調配（F.0／F.2）」 | **已實作**：`verify/wf_f0.py`（級0／0′）／`verify/wf_f2.py:140-165`（級1／2／3） | 🔴 **否** |
| 6 | **末趟新產生之不合格宗** | `K-6:122`「末趟新產生之不合格宗交七級調配、不回頭觸發搬移」 | `【查無】` | **是**（惟為**迭代**產物） |
| 7 | **池內遞補合成宗** | `K-6:2150`「騰出之地**入調配池**」 | **已實作**：`verify/wf_f4.py:211-213` `add_syn` 寫 `"配地階段": "池內"` | **是** |

⇒ 🔒 **來源 7 個，其中 `#5` ⛔ 非乙甲之產物** ⇒ **`-94` §E-2 之判（「是，惟母體非僅由此構成」）本批複驗<u>成立</u>**。

### C-3　`B-3`　必答句（逐字回覆）

> 「入調配池之母體（宗清單），**是否可由現行碼機械枚舉**？」

**答：`是`。**

🔒 **枚舉點（逐字·`檔:行`）**：

```
app.py:19406                  _stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp]
app.py:19407                  _stage2_parcels = [tp for tp in parcels_in_blk if '配地階段' in tp]
verify/stepg_pipeline.py:416  _stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp]
verify/stepg_pipeline.py:417  _stage2_parcels = [tp for tp in parcels_in_blk if '配地階段' in tp]
verify/wf_f4.py:211-213       # P1 配地階段旗標（**資料驅動**·禁以 `74·` 名稱前綴或塊名/側別判別·泛用紀律）：
                              "配地階段": "池內",
```

🔒 **產生指令**：`[tp for tp in parcels_in_blk if '配地階段' in tp]`
🔒 **旗標之權威**（`stepg_pipeline.py:861` 逐字）：「`_stage2_ids`＝階段2宗之暫編地號集，**其出處即 `配地階段` 旗標**（資料驅動…）」

**現況清單之宗數**：

| 源 | 數 |
|---|---:|
| `verify/baselines/wf/f4/F.4_G值_退縮0m.csv` | **14**（列 54） |
| `verify/baselines/wf/f4/F.4_G值_退縮3.5m.csv` | **17**（列 59） |
| `verify/baselines/wf/f4/F.4_整形_退縮0m.csv` | **6**（列 23） |
| `verify/baselines/wf/f4/F.4_整形_退縮3.5m.csv` | **4**（列 14） |
| 全 `verify/baselines/**.csv`（**266 支**）之相異合成宗 | **30** |

🔒 **具名（⛔ 不得混用）**：上數 ＝ **baseline 內以 `74·` 為前綴之合成宗**，
⛔ **非**「現行執行期之入池母體宗數」——後者須**執行 `run_step_g`／`run_all`** 方得
⇒ 標 **`【須執行該碼·本批⛔ 不跑】`**。
⚠️ 🔒 **併具名**：`verify/baselines/wf/**.csv`（**44 支**）中含 `配地階段` **欄**者 ＝ **0**
⇒ 該旗標**不入 baseline 欄**，故上表改以 `74·` 前綴代理（**代理·射程已具名**·§零-2 之修法）。

⇒ 🔒 **§六之 `T3′`（「入池母體連續二批逐位不變」）<u>可機械判</u>** ✅

---

## §D　受詞 `C`：容差錨之**射程掃**（⛔ 只出更正清單·不改既有文件）

### D-1　`C-1`　引用處之窮舉

🔒 **母體**：`b31f1ff` 之 `docs/**`（**375**）＋ `verify/out/**`（**418**）＋ `verify/**.py`（**190**）＝ **983 檔**
🔒 **產生指令**：`git ls-tree -r -z --name-only b31f1ff` → 篩前綴 → 逐行 `re.search(r"(?<![0-9.])<N>(?![0-9.])", line)`

| 錨 | 裸命中 | **收斂後**（行內另含 `容差`／`分層`／`可執行層`／`判定鏈`／`樣式命中`／`註解＋字串`） |
|---|---|---|
| `279` | 70 行／25 檔 | **26 行 ／ 9 檔** |
| `342` | 24 行／13 檔 | **17 行 ／ 7 檔** |
| `530` | 14 行／9 檔 | **9 行 ／ 5 檔** |
| `188` | 20 行／13 檔 | **5 行 ／ 3 檔** |

⚠️ 🔒 **具名一個假陽性（⛔ 不計入）**：`verify/out/wg974_tol_vs_gran_4bab9b9.log:136` 之 `279`
係 **`probe_ruling_N_recon.py:279` 之<u>行號</u>**，⛔ 非該錨。

### D-2　`C-2`　逐處之結論判

🔒 **判準**：該處之宣稱若受「v1 分層器之假陰性」影響 ⇒ `被推翻`；若只是引述母體／指令 ⇒ `仍成立`。

| # | `檔:行` | 逐字（節錄） | 判 | 理由（一句） |
|---|---|---|---|---|
| 1 | `W-G.9-91:332` | 「其中**可執行層** \| **342**」 | 🔴 **被推翻** | v2 下為 **453** |
| 2 | `W-G.9-91:335` | 「入判定鏈者 \| **279 ／ 342**」 | 🔴 **被推翻** | v2 下為 **371 ／ 453** |
| 3 | `W-G.9-91:402` | 「入判定鏈 **279 支**（≥2 ✅）」 | **仍成立** | 其宣稱為「**≥ 2**」，`371 ≥ 2` |
| 4 | `W-G.9-91:472` | 「`A-4` 之可執行層由 **355** 改為 **342**」 | 🔴 **被推翻** | 該批之 f-string 修正正確，惟其結果值已由 v2 更正 |
| 5 | `W-G.9-91:542` | 「入判定鏈 **279 支**（`1e-9` 140／`1e-6` 82／`1e-12` 64 為前三）」 | 🔴 **被推翻** | 新分布 `1e-9` **147**／`1e-6` **74**／`1e-12` **70**（⚠️ 舊分布之母體為 `342`·**同名不同量**） |
| 6 | `W-G.9-92:322` | 「樣式命中（全部） \| **530**」 | ✅ **仍成立** | 樣式命中不受分層影響（v2 亦 530） |
| 7 | `W-G.9-92:323` | 「可執行層 \| **342**」 | 🔴 **被推翻** | 同 #1 |
| 8 | `W-G.9-92:324` | 「入判定鏈 \| **279**」 | 🔴 **被推翻** | 同 #2 |
| 9 | `W-G.9-92:326` | 「註解＋字串 \| **188**」 | 🔴 **被推翻** | v2 下為 **77**（純資料）＋ 0 |
| 10 | `W-G.9-92:329` | 「合計自校：`342 ＋ 188 ＝ 530`」 | 🔴 **被推翻**（其**算術**仍對） | v2 之自校式為 `452 ＋ 1 ＋ 77 ＝ 530` |
| 11 | `W-G.9-92:330` | 「`279` 支即波末重烤批之重驗清單」 | 🔴 **被推翻** | 清單應為 **371** |
| 12 | `W-G.9-92:369` | 「`279` 支中入判定鏈 **≥ 1**」 | **仍成立** | 同 #3 之理 |
| 13 | `W-G.9-93:53`／`:285`／`:286`／`:303`／`:309`／`:336` | 三數之身分表與級別分布 | 🔴 **被推翻** | 其母體數已更正（`530` 不變·`342`／`279` 改） |
| 14 | `docs/驗證裁定登記表.md:2765`／`:2786`／`:2873` | 「容差級分類閘 ＝ `279` 支」等 | 🔴 **被推翻** | 同 #2；🔒 **append-only ⇒ ⛔ 不追改**，本清單即其更正 |
| 15 | `W-G.9-94:225`／`:226`／`:227`／`:88`／`:219`／`:221` | 本次更正之出處 | ✅ **仍成立** | 即 v2 之結果本身 |
| 16 | `verify/out/probe_WG991_yishim_2cb0fe9.log`（4 處）／`probe_WG992_blue_451f0f8.log`（8 處） | log 內之同數 | 🔴 **被推翻** | 🔒 **log 為當時之實跑產物** ⇒ ⛔ **不追改**、⛔ 亦不重跑（§附-5） |
| 17 | `verify/probes/probe_WG992_blue.py:11` | docstring「容差級分類閘 **279 支**」 | 🔴 **被推翻** | 🔒 **⛔ 本批不改 `verify/**.py`**（無 §零） |

⛔ **未改動任何既有文件**（append-only）——本受詞只產出**更正清單**。

### D-3　`C-3`　波末佇列項第 3 項之更新料

🔒 **母體鎖 `451f0f8`**（＝ 該錨之出處）·**189 檔**（扣 SELF）·`CHAIN` 逐字轉錄自 `probe_WG991_yishim.py:637-638`

| 項 | 值 |
|---|---|
| **舊母體數**（v1·入判定鏈） | **279** ✅ 逐位重現 |
| **新母體數**（v2·入判定鏈） | **371** |
| **帶號差** | **`+92`** |
| **`Σ|差|`** | **92**（新增 **92** ／ 移除 **0** ⇒ 🔒 **嚴格增**） |

**新增 92 行之分類（🔒 逐檔行號清單見 §附之 scratchpad 具名）**：

| 類 | 行 | 註 |
|---|---:|---|
| `verify/probes/**` | **79** | **探針**·⛔ 非生產路徑 |
| `verify/fixture_*` | **7** | **夾具**·入 `run_all` |
| `verify/archive/**` | **1** | **封存** |
| **`app.py`** | 🔴 **2** | **生產碼**（`:22032`／`:22040`） |
| 引擎（`wf_f*`／`stepg`／`selection`／`run_verification`／`wd4`） | **3** | — |

`POPULATION=92 PRINTED=92 SUPPRESSED=0`（檔數 **50**）

**級別分布之新舊對照（⛔ 全列）**：

| 容差字面 | 舊 | 新 | 差 |
|---|---:|---:|---:|
| `1e-9` | 119 | **147** | +28 |
| `1e-6` | 51 | **74** | +23 |
| `1e-12` | 60 | **70** | +10 |
| `1e-15` | 24 | **30** | +6 |
| `1e-3` | 14 | **18** | +4 |
| `1e-5` | 2 | **9** | +7 |
| `5e-4` | 2 | **8** | +6 |
| `1e-8` | 1 | **3** | +2 |
| `1e-18` | 1 | **3** | +2 |
| `1e-4` | 1 | **2** | +1 |
| `1e-7` | 1 | **2** | +1 |
| `5e-5` | 1 | **2** | +1 |
| `1e-09` | 1 | 1 | 0 |
| `1e-10` | 1 | 1 | 0 |
| `3e-9` | 0 | **1** | +1 |

🔒 **合計自校**：舊 `279` ／ 新 `371` ／ 差 `+92` ⇒ 逐列差之和 ＝ `+92` ✅

🔒 **供發單側之判（⛔ CC 不判排程）**：新增之 92 行中**生產碼僅 2 行**，其餘 90 行落在**探針／夾具／封存**
⇒ **重烤射程之<u>實質</u>增幅遠小於 `+92`**；⚠️ ⛔ **惟母體數確為 `371`**——⛔ 不得以「多數是探針」抹掉該更正。

---

## §E　受詞 `D`：碼面 `raise` 閘之**單點檢**

### E-1　`D-1`　逐字全文 ＋ 觸發母體

🔒 **逐字全文（`verify/stepg_pipeline.py:491-508`＠`b31f1ff`·⛔ 全列）**：

```
        # ── 結構閘 位次＝投影序 ──
        #   ordered_v2 保留投影序（僅 PK winner 最小擾動上位），其 `pre_position` 必逐筆等於
        #   `_projection_order`（純幾何 representative_point 投影）之排名。此為位次價格無關之
        #   機制保證：任何使位次改吃 G/地價之 refactor 立即被逮。
        if not _degenerate_order:
            # 🆕 P2-a／B-2：母體**與 `ordered_v2` 同源**（皆 `_stage1_parcels`）。用未過濾之
            #   `parcels_in_blk` 會使排名把階段2宗一併編號 → 與 `pre_position` 對不上 → 本閘必 raise。
            _proj_rank = {tp['暫編地號']: _i + 1 for _i, tp in enumerate(
                ns["_projection_order"](_stage1_parcels, _cad_fl_blk.get('p1'),
                                        _cad_fl_blk.get('p2')))}
            _pos_bad = [(e['tp']['暫編地號'], e.get('pre_position'),
                         _proj_rank.get(e['tp']['暫編地號']))
                        for e in ordered_v2
                        if e.get('pre_position') != _proj_rank.get(e['tp']['暫編地號'])]
            if _pos_bad:
                raise RuntimeError(
                    f"🔴 結構閘 位次＝投影序 破：街廓 {blk_label} {len(_pos_bad)} 筆 pre_position "
                    f"≠ _projection_order 排名（暫編, pre_position, 投影排名）={_pos_bad[:3]}")
```

**觸發母體（⛔ 逐項）**：

| 項 | 值 |
|---|---|
| **跑哪些宗** | `ordered_v2` 之**每一筆**——其源 ＝ `_stage1_parcels`（`= [tp for tp in parcels_in_blk if '配地階段' not in tp]`）⇒ **階段 1 之全宗**（⛔ 不含池內合成宗） |
| **跑哪些街廓** | **逐街廓**（`blk_label` 於 `run_step_g` 之街廓迴圈內） |
| **何時<u>不</u>執行** | 🔴 `if not _degenerate_order` ⇒ **`_degenerate_order = (d_hat is None or corner_pt is None)`**（`stepg_pipeline.py:455`）＝ **缺 FRONT_LINE** 之街廓 ⇒ 🔒 **該閘於退化街廓<u>整段跳過</u>** |
| **本案是否觸及該跳過** | `stepg_pipeline.py:458` 註解逐字：「**UC9898 全塊皆有 FRONT_LINE·不觸**」 ⇒ 本案 **⛔ 不跳過** |

### E-2　`D-2`　若該閘被刪，何者會紅？

🔒 **產生指令**：`git show b31f1ff:<f> | grep -c "<pat>"`（**正面列舉**受檢檔·⛔ 非「全倉排除」）

| 樣式 | `app.py` | `stepg_pipeline.py` | `run_verification.py` | `selection_pipeline.py` |
|---|---:|---:|---:|---:|
| `pre_position` | 7 | 8 | **0** | 2 |
| `原位次(投影序)` | 2 | **0** | 1 | 1 |
| `位次＝投影序` | **0** | 3 | 1 | **0** |
| `_proj_rank` | **0** | 4 | **0** | **0** |

🔴 **直接之同義檢 ＝ `0`**：全倉**⛔ 無第二處**比較 `pre_position` 與 `_projection_order` 排名。

🔴 **`W-D.1.2 診斷` CSV <u>不會</u>咬（🔒 具名·此為最易誤判之一格）**：
該 CSV 有 `原位次(投影序)` 欄且**入 `diff_rows`**（`run_verification.py:383`／`:386` 之 `_DECLARED_EXTRAS` 表列之），
**然其值另有來源**——`app.py:18672` ← `_pre_position_rank` ← `app.py:18505` ← `_rank_by_tpid`
← `app.py:18458-18462` **直接呼叫 `_projection_order`**，⛔ **不經 `pre_position`**
⇒ 🔒 **改動 `pre_position` 之來源，該欄<u>逐位不變</u>** ⇒ **該 CSV ⛔ 不會紅**。

✅ **會紅者（**間接**·具名）**：
`pre_position` 由 `_spatial_order_parcels_v2` 於**排序時**寫入（`app.py:7658` `{'tp': tp, 'pre_position': i + 1, …}`，
其 `i` 枚舉 `pre_seq = _projection_order(...)`·`app.py:7656`）⇒ **`ordered_v2` 之序即配地推進序**
⇒ 改其來源必動 `累積S(m)`／`G(㎡)` ⇒ **`G 值計算結果_退縮*.csv` 之 `diff_rows`（`run_verification.py:669`）會紅**。

⇒ 🔒 **答：<u>非</u>單點**（有間接冗餘）⇒ **停機⑤ ⛔ 不觸發**（解除條件：「任一他物會紅 ⇒ 不觸發」）。

⚠️ 🔒 **必須併載之弱點（⛔ 不以「非單點」掩之）**：
1. 該冗餘是**間接**的——它偵測的是**結果之改變**，⛔ 非**機制之違反**。
   一個「改了來源但數值恰好不變」之 refactor，**baseline 不會紅、而該 `raise` 閘會紅**。
2. 🔴 **baseline 一重烤即失效**：重烤後新值即成為期望值 ⇒ 該間接冗餘**歸零**。
   ⇒ 🔒 **在波末重烤批中，該 `raise` 閘是<u>唯一</u>守住 `K-6:59`「禁另訂」之物。**

🔒 **發單側之 VR 裁（本單併宣·⛔ 非上呈 KL·CC 逐字轉錄並登記）**：
> 該 `raise` 閘為**正典 `K-6:59` 之代理**。⇒ **⛔ 不得刪除、⛔ 不得放寬**；如需變更，須先有 `VR` 或正典之依據。

⛔ **不上呈 KL**（`K-9-11 三`：遞補之機制已裁）。

### E-3　`D-3`　該閘是否曾**實際紅過**？

🔒 **母體**：`b31f1ff` **全倉 1309 檔**（⛔ 遞迴·⛔ 不限副檔名）

| 字樣 | 命中檔 | 命中總數 |
|---|---:|---:|
| `結構閘 位次＝投影序 破` | **5** | **6** |
| `結構閘 位次＝投影序` | 5 | 10 |
| `位次＝投影序` | 16 | 23 |

**`結構閘 位次＝投影序 破` 之 5 檔逐項（⛔ 全列）**：

| 檔 | 性質 |
|---|---|
| `verify/stepg_pipeline.py` | **碼面自身**（該 `raise` 之字面） |
| `verify/probes/probe_D2b9_guard_degen.py` | **raise-site <u>盤點</u>探針**之 `GATE_FAMILIES` 表（`:92`） |
| `verify/out/probe_D2b9_guard_degen.log` | 上者之 log ⇒ 其內容為「**原始碼第 501 行有此 raise**」之**清單**，⛔ **非該 raise 曾執行之記錄** |
| `verify/out/probe_D2b10_family.log` | 同上族 |
| `docs/reports/W-G.9-94_分層器重量與三詞現查.md` | `-94` 之**引述** |

**`git log` 之側**：
`git log --all --oneline -S "結構閘 位次＝投影序" -- verify/stepg_pipeline.py` ⇒ **1 個 commit**：
`40e747f v3 財務接線波：真值 A/B/C 接線＋結構不變量永久閘＋v3 錨定 baseline`（＝**引入該閘者**）
`git log --all --oneline --grep="位次＝投影序"` ⇒ **僅 `b31f1ff`**（`-94` 之 commit 訊息）。

⇒ 🔒 **答：`【查無】`——⛔ 無任何記錄顯示該 `raise` 曾被執行。**

⚠️ 🔒 **「查無」之射程（memory：「查無」只是簡寫）**：本檢之母體為**入倉**之 `verify/out/**`（**418 檔**）；
而 `CLAUDE.md` 逐字：「`verify/out/*.log` 為 **untracked**」⇒ 🔒 **未入倉之 log 不在母體內**
⇒ **⛔ 不得由本結果推得「該閘從未紅過」**，只能謂「**入倉範圍內查無其紅過之記錄**」。

⚠️ 🔒 **行號漂移之具名**：兩支盤點 log 記該 `raise` 於 `:501`，本批現查為 **`:506`**
（`if _pos_bad:` 在 `:505`）⇒ **`CLAUDE.md` 行號衛生之又一實例**（log 為當時之實跑產物·⛔ 不追改）。

---

## §F　停機款之判（🔒 逐款·⛔ 不略）

| 款 | 觸發條件（逐字） | 本批之判 |
|---|---|---|
| **①** | `A-0` 任一錨不符／開工閘紅／`app.py` 位元變動 | ✅ **不觸發**（四錨全符；`git diff b31f1ff -- app.py` ＝ 空；`status` 開工前空） |
| **②** | 本批**動了**生產碼、CSV、或既有文件之既有文字 | ✅ **不觸發**（本批入倉物僅本報告一檔·**新建**·⛔ 零既有文字變更） |
| **③** | `A-3` 顯示 `累積S` **確非** `_projection_order`，**且** `W-D.4` 之填充對象**確為位次** | ✅ **不觸發** —— 前件**成立**（5 欄全異 ＋ 逐格 5／6 異），惟**後件不成立**：`A-2` 判填充對象 ＝ **CSV 欄位值**、**非位次** ⇒ 🔒 **解除條件逐字命中** |
| **④** | 預宣證偽輸入與實測相反，**且**已先自證量測器非紅 | ✅ **不觸發**（`A-5` 三預宣全 ✅；量測器**雙向**自證通過） |
| **⑤** | `D-2` 判該閘為**單點**，**且** `D-3` 顯示它**從未紅過** | ✅ **不觸發** —— `D-2` 判**非單點**（間接冗餘：`G 值計算結果_退縮*.csv` 會紅）⇒ 解除條件成立。⚠️ 惟 `D-3` **確為 `【查無】`**，且該間接冗餘**於重烤批歸零** ⇒ 🔒 **具名為<u>殘留風險</u>並上呈**（⛔ 非停機） |

---

## §G　🔒 §六 佇列項之**登記**（⛔ 零效力·⛔ 非裁定·CC 只登記不評價）

🔒 **本節係<u>轉錄發單側之修正</u>**，⛔ CC 未提名、未建議、未評價（施工單 §五／§六禁令）。

**廢止**：`T4`（「`D-2` 回是·調配在乙甲下游」）——發單側自誤，⛔ CC 不評。

**修正後之觸發條件**：

| 條件 | 逐字 | CC 之**可機械判性**現查 |
|---|---|---|
| `T1` | 「乙」已落地（不配地 ＋ 遞補之介面已定且通過其不變式） | `【待落地·本批不判】` |
| `T2` | 「甲」已落地（`K-9-12` 矩形容納） | `【待落地·本批不判】` |
| `T3′` | **入調配池之母體（宗清單·含全部來源·⛔ 不問來源為何）連續二批逐位不變** | ✅ **可機械判**——枚舉點 ＝ `配地階段` 旗標（§C-3）；⚠️ 惟須**執行該碼**方得現況清單 |

🔒 **KL 已示意之微調內容（登記·`【條文未作成·⛔ 零效力】`·逐字轉錄）**：
> 依區位順序調配至他街廓：(一) 同類型使用分區 →(二) 由原街廓最小建築面積往次一級 →(三) 臨相同道路 →(四) 相同路寬 →(五) 次一級路寬。

**對本波落地之唯一要求（零土地後果·逐字）**：
> ⛔ **不得將距離優先之參數／級序寫死為不可替換**；須留可替換點並逐字註明本佇列項。

🛑 **早期提醒之現查（安全閥·CC 只報事實）**：`-94` §E-3 已現查——
`verify/wf_f2.py:106`／`:107` 之 `0.5` 與 `wf_f4.py:127`／`:130` 之 `0.5` 為**字面常數**；
級2 之門檻為 `np.median(_dists)`（**動態**）。🔒 **⛔ CC 不評其是否「寫死為不可替換」**
——該判準之受詞（「可替換點」）未定義 ⇒ 標 **`【未驗·待發單側定義】`**。

---

## §H　🩸 CC 自誤登記（本批·**1 則**）＋ 機檢射程之自捕（**1 則**·見 §附）

1. **`-94` 之 baseline CSV 數誤記**（§A-0-3）——寫 **12**、實為 **10**。
   **成因**：`{v2, v3, PRE-*×3}` ＝ 5 目錄 × 2 情境 ＝ 10，我算成 12。
   **抓到它的**：本批 `A-0-2` 之**併驗**（施工單所令之逐列覆核）。
   🔒 **自校可及**：同批之 17 命中檔 ＝ 10＋4＋3 ✅ ⇒ **當時就能自校，我沒做**。
   🔑 **通則**：**把一個總數拆成分項時，須<u>回加驗算</u>**——⛔ 拆解不是複述，它是一次新的運算。

---

## §I　🔒 本批**未涵蓋**（⛔ 具名·⛔ 不以沉默代替）

1. ⛔ **未決定** `W-D.4` 可否充當「乙」之遞補介面（§附-1：**須在新窗作成**）。
2. ⛔ **未辦**「乙」「甲」之落地／換圖／換快照／重烤／`K-8` 遷移項甲。
3. ⛔ **未改動** `W-D.4`／10 支 CSV／任何既有報告文字（append-only）。
4. ⛔ **未重驗**容差 `371` 支之**現值**（§附-5）·⛔ 未跑 `run_all`。
5. ⛔ **未查** 三支 `PRE-*` 與 `v2` 之 `退縮0m` **多出之第 3 列**內容為何。
6. ⛔ **未補齊** R6 該側 6 宗之 `原位次(投影序)`（`A-4` 之 `【資料不足】`·須執行 `_projection_order`）。
7. ⛔ **未查** `wf_f0` 級0／0′ 之逐行實作（`-94` 已具名·仍為 `【本批未逐行查】`）。
8. ⛔ **未就**調配池／合併調配／超配出裁定題（`K-9-11 三`）；⛔ 未就七級調配提名／建議／評價。
9. ⛔ **§Z 未入倉**（§零-3：塊內含受測術語 ⇒ 排在量測後·**另批**）。
10. 🔒 **本報告 ⛔ 未登入 `TARGETS`**：本單**無 §零**（⛔ 未授權 `verify/**.py` 變更）⇒ ⛔ 不逾越授權（`-93`／`-94` 之處置延續）。

---

## §附　🔒 付印前機檢

**乾草堆**：`docs/rulings/K-6_…md`／**`app.py`**／`verify/stepg_pipeline.py`／`verify/wd4_tier_list.py`／
`verify/run_verification.py`／`verify/wf_f1.py`／`verify/wf_f2.py`／`verify/wf_f4.py`／
`verify/probes/probe_WG991_yishim.py`／`verify/baselines/v3/**.csv`／`docs/reports/W-G.9-9{1,2,3,4}_…md`／
`docs/驗證裁定登記表.md`
　＋ 🔒 **本批之量測輸出**（`out_c3.txt` 等·**scratchpad·⛔ 未入倉**·具名）。

| 項 | 值 |
|---|---|
| 報告內 `≥3dp` 之相異數 | **0** —— 🔒 本報告之數**全為計數／行號／地號**（整數）⇒ memory ④ 之小數檢**無受詞** |
| **檢①：逐行引文**（`檔:行` ＋ 逐字 ⇒ 對 blob 之**第 n 行**） | 母體 **23 條**（另 4 條過短·略）；✅ 相符 **23**／🔴 不符 **0** |
| 🔒 檢① 之判別力 | `app.py:7518` 原句 ⇒ **`True`**；**僅改一字**（`_projection_ordeR`）⇒ **`False`** |
| 🆕 **檢②：整塊逐字**（fenced block ⇒ 對 blob 之**連續段**） | 🔒 **檢① 之射程缺口**——`E-1` 之 `raise` 閘全文與 `C-1` 之 `K-6:98-116` **不帶 `檔:行` 前綴** ⇒ **檢① 看不到**。母體 **2 塊**；✅ **2 ／ 2** |
| 🔒 檢② 之判別力 | 原段 ⇒ **`True`**；**僅改一字**（`_pos_baD`）⇒ **`False`** |

🩸 **檢② 之立由（`-94` 之續·CC 自捕）**：`-94` 立檢① 時，其母體恰好每條引文都帶 `檔:行`；
本批之**整塊逐字**不帶前綴 ⇒ 檢① 之母體由 49 掉到 **23**，而**掉出去的那兩塊正是最長、最易抄錯的**。
🔑 **通則**：**一個機檢之母體會隨<u>下一批的寫法</u>而縮小，且它不會告訴你**
——⛔ 須每批問一次「**本批有哪些引文落在該檢之母體<u>外</u>**」。

🔒 **覆核之定式**（同 `-94` §附）：`NFKC` 正規化 ＋ 去空白／`**`／反引號／`<u>` ⇒ **取該 `commit` blob 之第 n 行**作子字串比對；
`commit` ＝ `b31f1ff`。

🔒 **本批⛔ 未逐字寫出任何哨兵／假錨之字樣**（自檢 ⑥·節 `111`）。
