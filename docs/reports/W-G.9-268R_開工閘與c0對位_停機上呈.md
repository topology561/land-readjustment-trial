# `W-G.9-268R`　開工閘八閘 ＋ `c0` 受詞母體之對位 ⇒ 🛑 **停機上呈**（`A ∩ B = ∅`）

> 🔒 **級 ＝ 重級**（單 `§零` 自載）——本報告所辦者為 `§一` 開工閘八閘 ＋ `§二` 之 `c0`（**零生產碼**）。
> 🛑 **`c1`〜`c4` ⛔ 開工**——`c0` 觸發單 `§八` 之停機款「`c0` 之 `A ∩ B = ∅`」。
> 🛑 **零生產碼**：`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` **一字未動**。
> 🛑 **零土地後果**：⛔ 動任何一宗地之 `G`／面積／街角歸屬／抵費地／配地幾何。
> 🛑 **⛔ 鑄任何號**（單 `§七` 所授權之 `自誤 333` 亦**未鑄**——其受詞係發單側之自誤，
> 而本批因停機而未及辦其登記；候發單側指示）。
> 🔒 **行號衛生**：本報告⛔ 以行號（含 log 列號）為錨。

---

## §零　三軸之總宣告（`VR-091`）

| 軸 | 值 |
|---|---|
| **(1) 來源 `commit`** | **`abeb8794fe930a590fe1c27fa2dd10f85d6bfed1`**（`ls-remote` 實查所得之 `origin/wip/s1-endpart`）|
| **(2) 母體** | **逐量各載**（⛔ 以本節之母體代任何個別量之母體·`自誤 332`）|
| **(3) 粒度框** | **逐量各載**（列框／檔框／字元框／格／bytes／面積 ㎡）|

🩸 **單 `§零` 所載之期初態 ＝ `e57d2ef86894472761e1403974ff1f2ce1e6a2df`**，
而實查之 `origin` ＝ `abeb879…`（**領先 `1` 個 commit** ＝ 單 `§十一` 所令之**工項零**）。
其處置見 `§三 自解 1`。

---

## §一　開工閘（八閘·逐條當場復現）

### `S-0`　本單原封入倉（入倉 blob `sha256` ＝ 落檔前來源）

🔑 **本窗以<u>更強之證據</u>代之——全鏈保真已可正面驗證。**
`作業常規之追加四` 款 `一` 之射程界限逐字自載：該閘所證者僅「**落檔 → 入倉**」，
**⛔ 證「發單側送出之原文 → 落檔」**（來源係聊天訊息、無獨立 `sha256` 可比）。
🔒 **本窗<u>持有</u> KL 交付之原始檔** ⇒ 得直接對拍其與倉內 blob：

| 側 | bytes | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| **KL 交付檔**（`Downloads/W-G.9-268_施工單_….md`）| `18244` | `224` | `0` | `16df5696ac773068e370108b9109508530833f34980e7df2bffdfcaedb2b12d8` |
| **倉內 blob**（`abeb879:docs/orders/W-G.9-268_施工單_….md`）| `18244` | `224` | `0` | **同上** |

**逐位相同 ＝ `True`** ✅　**判定集基數 ＝ `1`**
**否證對照**：改末一位元組後相同 ＝ `False` ✅（須 `False`·判定集基數 `1`）
⇒ 🔒 **`追加四` 款 `一` 所具名之全鏈缺口，於本單<u>已閉合</u>**（⛔ 憑豁免）。
🔒 併：`16df5696…` 與工項零 commit 訊息所載**逐位相符**。

### `S-0b`　`probe_order_preflight.py <本單>`

| 欄 | 值 |
|---|---|
| `rc` | **`0`** |
| 🔴 機械·停機款（`P-3`／`P-5`）| **`0`** 項 |
| 🟡 提示 | **`1`** 項 ＝ `[P-5] ⛔ 無 SELF_SHA256 列 ⇒ ⛔ 不可對拍（🟡 非紅：非所有文件皆須有）` |
| ℹ️ 記錄 | `[P-3] app.py 之 def main 區間（AST 實查）＝ 14887-24459` |

🔒 **`P-5` 之逐項處置（⛔ 靜默略過）＝ <u>採納</u>，並以 `S-0` 之全鏈實測<u>代其功能</u>**
——該提示所指之「不可對拍」係就**單內自載之 `SELF_SHA256`** 而言；
本窗改以**來源檔 vs 倉內 blob 之逐位對拍**達成同一保證（且射程更寬）。
`P-1`／`P-2`／`P-4`／`P-6` **命中 `0` 項** ⇒ 無待處置項。

### `S-1`　號占用（宣告框六數分列 ＋ 二對照）

**三軸**：`(1)` `abeb879…`／`(2)` 全 `docs/` **`630`** 檔（**讀不到 `19`** ＝ git 失敗 `0` ＋ 解碼失敗 `19`）／
`(3)` **檔框 ＋ 列框**（宣告框 `①`–`⑧`·`列框 ＝ |D2 ∪ D3|`·`D1` ⛔ 產生列命中）。
**器** ＝ `verify/probes/wg9268_gate6_occupancy.py`（工項零所入倉·母體以 `ls-tree -z` 取·`quotePath` CJK 陷阱已避）。

| 號 | 框 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | 列框(∪) | 檔框 | 鬆框 檔／列 | 角色 |
|---|---|---|---|---|---|---|---|---|---|
| **`W-G.9-268`** | 嚴格 | `2` | `3` | `2` | `0` | **`5`** | **`2`** | `6`／`23` | 受詢 ⇒ 🩸 **非零** |
| **`W-G.9-268`** | 寬式 | `2` | `3` | `2` | `0` | **`5`** | **`2`** | `6`／`23` | 同上 |
| `W-G.9-267` | 嚴格／寬式 | `4` | `13` | `5`／`6` | `0` | `18`／`19` | `6` | `8`／`54` | 對照甲（須 `≥1`）✅ |
| 一人造號（**執行期組出**·字樣⛔ 入倉·`GB-147`）| 嚴格／寬式 | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` | 對照乙（須 `=0`）✅ |

🩸 **受詢非零 ⇒ 依自指款<u>逐筆歸類</u>**（`裁 H`：命中非 `0` 而未逐筆具名者 ⇒ 停機）：

| 款 | 落點 | 歸類 |
|---|---|---|
| `D1` | `docs/orders/W-G.9-268_施工單_….md` | **工項零 `1`**（單 `§十一`）|
| `D1` | `docs/orders/W-G.9-268_附件甲_審查窗報告與草稿.md` | **工項零 `2`** |
| `D2` | 本單標題列 | 本單自身 |
| `D2` | 附件甲標題列 | 附件甲 |
| `D2` | 附件甲之「`# 三、W-G.9-268 施工單草稿`」 | 附件甲 |
| `D3` | 本單之「本單係 `W-G.9-268 附件甲` 之審定版」 | 本單自身（補款 `⑤`：該號 ＝ 檔名之號）|
| `D3` | 本單 `§十一` 之「`1. 本單 ⇒ docs/orders/W-G.9-268_…`」 | 本單自身 |

⇒ 🔒 **檔框 ＝ `2` ＝ 恰為工項零所入倉之二檔**；**占用者<u>全為本單自身之產物</u>**
⇒ 依 `自解款` 白名單 `1` ⇒ **⛔ 改號、`268` 可用**（見 `§三 自解 2`）。

### `S-2`　`left-right` ＝ `0 0`；`ls-remote` 與期初態逐位相符

| 量 | 實測 |
|---|---|
| `git ls-remote origin refs/heads/wip/s1-endpart` | **`abeb8794fe930a590fe1c27fa2dd10f85d6bfed1`** |
| 單 `§零` 所載之期初態 | `e57d2ef86894472761e1403974ff1f2ce1e6a2df` |
| `git rev-list --left-right --count HEAD...origin/wip/s1-endpart`（切至 `abeb879` **前**）| `0　1` |
| `abeb879` 之父 | **`e57d2ef…`**（⇒ 為期初態之**直系後裔**）|
| `git diff --name-only e57d2ef..abeb879` 對四檔清單之 `grep -xE` | **（空輸出）** ⇒ 命中 **`0`** 行 |
| 切至 `abeb879` 後之 `left-right` | **`0　0`** ✅ |

🩸 **字面不符 ⇒ 依 `常規九 二` 之體例判**：`ls-remote` 值 ＝ 期初態**之後裔**且該後裔為
**零生產碼**之**工項零**（單 `§十一` 所令）⇒ **正常開工**。其自解見 `§三 自解 1`。

### `S-3`　生產碼 `34` 檔 blob 期初

**母體（正面列舉·⛔ 全倉排除式）** ＝ `app.py` ＋ `verify/` 頂層 `*.py` ＝ **`34`** 檔（判定集基數 `34`）
**粒度框** ＝ **blob SHA ＋ bytes**

| 量 | 實測 |
|---|---|
| `app.py` | **`c5ec503c740f59e084cedd83531b9d029fe8ddc0`**／`1454937` B ⇒ ✅ **與單 `S-3` 逐位相符** |
| 與附件甲 `§3` 逐檔相異 | **`1`**（見下）|
| 對照乙（一必不可得之檔·**執行期組出**）| `git rev-parse` `rc = 128` ✅（須 `≠ 0`）|

🩸 **唯一相異 ＝ `verify/fixture_yi_construction.py`**：本窗實測 `69f4a18a`／`11562` B；
附件甲 `§3` 載 `69f4a18e`／`11562` B（**bytes 相同·SHA 末一字元 `a` vs `e`**）。
🔒 **機械否證其為倉態之別**：該檔之 blob 於 **`e57d2ef`**（＝ 附件甲量測之態）與 **`abeb879`**
**皆為 `69f4a18ae03f13181e6ca83ba8b291a54ea548ab`**（逐位相同），
且工項零 commit 觸及該檔之次數 ＝ **`0`**
⇒ 🔒 **附件甲之值係<u>單字元抄錄之誤</u>，⛔ 倉態之別**。其自解見 `§三 自解 3`。

### `S-4`　`run_verification` 期初實跑

**計數式（逐字·自生產碼字面讀取**——`verify/run_verification.py` 之
`print(f"  {'✅ PASS' if ok else '🔴 FAIL'}  {name}")`**）**：
`FAIL: ^  🔴 FAIL  (.+)$` ／ `PASS: ^  ✅ PASS  (.+)$`
**器** ＝ `verify/probes/wg9268_gate8_names.py`（工項零所入倉·**⛔ `grep -oP`**·`-267R 自捕 2` 之戒）

| 欄 | 實測 |
|---|---|
| `rc` | **`1`** |
| `stderr` | **`0`** B |
| `RESULT`（逐字·log 末列）| **`RESULT: FAIL`** |
| 🔴 FAIL 名目 | **`24`**（相異 `24`）|
| ✅ PASS 名目 | **`10`**（相異 `10`）|
| 名目合計 | **`34`** |

⇒ ✅ **與單 `S-4` 之判準逐位相符**。落檔 ＝ `verify/out/WG9268R_runverif_pre.log`／`.err`。

### `S-5`　🔴 `run_all` 期初對帳態（本批新增·**只取值·不判過**）

（見 `§四`。）

### `S-6`　覆蓋帳期初

**器** ＝ `verify/probes/probe_WG9265_coverage_account.py`（唯讀·`rc = 0`·`stderr` **`0`** B）
**判定集基數 ＝ `13` 組**　落檔 ＝ `verify/out/WG9268R_cov_pre.log`

| 量 | 實測 |
|---|---|
| 組數 | **`13`**（違反 **`2`**／無違反 **`11`**）|
| 違反組 | `0m·R2·left` ＝ **`45.9766`**／`3.5m·R5·left` ＝ **`56.8186`** |
| **否證對照 三造** | 人造相疊矩形 ⇒ `1.0000000000` ✅／人造相離矩形 ⇒ `0.0000000000` ✅／注入 `+1㎡` 於原綠之宗 ⇒ **原綠→轉紅** ✅ |
| 自我驗證閘 | 街廓面積逐街廓為常數 ✅；位次 `0` 之 `驗_宗序` ＝ `街角第1宗`（受檢 `8`＋`16` 格全符）✅ |

⇒ ✅ **與單 `S-6` 之判準逐位相符**（`13` 組·違反 `2`·二破量·三造對照如期）。

### `S-7`　硬閘活體生效（`--selftest` **⋀** 活體攔截實測·⛔ 只 `test -f`）

| 檢 | 實測 |
|---|---|
| `--selftest` | `rc = 0`；對照甲（含引入符·期望阻斷）＝ `True` ✅／對照乙（不含·期望放行）＝ `True` ✅／對照丙（非 Bash·期望放行）＝ `True` ✅；逐字判 `[selftest] 判 = PASS` |
| 該器之**自載警語**（逐字）| `🛑 本自檢綠 ⛔ 蘊含 hook 已接線——接線與否須由**活體攔截實測**判定。` |
| 掛載現查 | `.claude/settings.json` 之 `PreToolUse`／`matcher: "Bash"` ⇒ `python "$CLAUDE_PROJECT_DIR/verify/tools/wg9237_heredoc_guard.py"` |
| **活體攔截實測** | 本窗**故意**送出一條含 heredoc 之 `Bash` 命令 ⇒ **被阻斷** |
| 🔑 **命中偏移之自證** | 所送命令之前綴為 `echo AAAAAAAAAA; cat `（**`21` 字元**）⇒ `<<` 之位移**應為 `21`**；阻斷訊息逐字回報**「位移 `21`」** ⇒ 🔒 **該器讀到的確係本窗此一真命令**，⛔ 恆綠、⛔ 只 `test -f` |

⇒ ✅ **接線與生效皆經活體坐實**。

### `S-8`　🔴 四站現查（本批新增）

**母體（正面列舉）** ＝ `app.py` ＋ `verify/` 頂層 `*.py` ＝ `34` 檔（判定集基數 `34`）·**列框**

| 受詞 | 實測 | 判 |
|---|---|---|
| `_lg_cols` **寫入點** | `app.py`〔`res['_lg_cols'] = _lot_gate(`〕＝ **`2`** ／ `verify/stepg_pipeline.py`〔`res['_lg_cols'] = ns['_lot_gate'](`〕＝ **`2`** ⇒ 合計 **`4`** | ✅ 須 `4` |
| `_lg_cols` 全命中（寫 ＋ 讀）| `app.py` **`4`** ／ `stepg` **`4`** ⇒ 合計 **`8`** ⇒ 讀出點 **`4`** | ✅ 須 `4` |
| **判別力**：`驗_rejected` 於全 `34` 檔之命中 | **`1`**（逐檔 ＝ `{'app.py': 1}`·即其**自身之定義處**）| ✅ **觀測模式成立**（`> 1` ⇒ 停機·**未觸發**）|
| 對照甲（必非零）`def ` @ `app.py` | `333` | ✅ |
| 對照乙（必為零·**執行期組出**）| `0`（判定集基數 `34` 檔）| ✅ |

---

## §二　工項 `c0`（零生產碼）：受詞母體之對位 ⇒ 🛑 **停機款觸發**

**探針** ＝ `verify/probes/probe_WG9268_c0_align.py`（**唯讀**·本批新增）
**落檔** ＝ `verify/out/WG9268R_c0_align.log`｜`rc` ＝ **`20`**（＝ 停機款之出艙碼）｜`stderr` ＝ **`0`** B
**逐字結論列** ＝ `🛑 **\`A ∩ B\` ＝ ∅ ⇒ 觸發停機款**`

### 一　`A` 母體（地界線不交叉之違反宗集）

🔒 **受詞之正典款** ＝ `K-9-9` 之第 **二** 款（地界線不交叉）。🩸 本節題**⛔ 內含該款號**——其形會被 `K-9` 之定義列式咬中（`戒 36`：非鑄號者⛔ 得採該類號之定義列式形）。

🔒 **判定之權威源 ＝ `verify/probes/probe_WG988_nocross.py`**（`GB-67` 就地加註
`W-G.9-88`／`VR-047` 所裁之權威源）——本窗**⛔ 重判不交叉**（`同源聲明`：⛔ 另造第二份判準），
**當場實跑復現**其結論；`rc = 0`，落檔 `verify/out/WG9268R_nocross_pre.err`。

| 量 | 權威源實測（本窗當場復現）|
|---|---|
| 「必答 1：依『不交叉』重判之違反宗數」 | **`2`**（受詞內 `2 / 5`·受詞外 `0 / 30`）|
| 逐宗 | `R2 左 序1（i=1）` `s* = 10.0407 ∈ [0.0000, 44.9154]`·擦邊 `False`·面積 **`3.8082 ㎡`** |
|  | `R5 左 序1（i=1）` `s* = 5.1067 ∈ [-0.0000, 43.7602]`·擦邊 `False`·面積 **`0.9867 ㎡`** |
| 面積合計 | **帶號 `4.7950 ㎡`／絕對值 `4.7950 ㎡`**（`凡列合計二者並列` 所令）|
| **判定集基數**（`POPULATION`）| **`35`** |
| 「遠側界取不到者」 | **`0`**（⛔ 靜默退路之反面·計數為 `0` 亦印出）|

🔒 ⇒ **與 `GB-67` 現行有效之土地結論（`2` 宗／`4.7950 ㎡`）逐位相符**。

🔴 **該權威源之<u>口徑</u>（本窗新查·決定性）**：其驅動為
**逐街廓獨立呼叫** `run_step_g`（`for lbl in blks: … except Exception: pass`），
且其**情境母體 ＝ 僅 `0m`**（該探針逐字自載）。

🩸 **該權威源之負對照<u>未如預期</u>（照實·⛔ 隱去）**：其常設 `8` 之負對照（舊判準）
實得 **`16` 宗**而其內嵌期望為 **`25`** ⇒ 該探針**自身逐字判「🔴 ≠ 25 ⇒ 同源性斷裂·具名」**。
🔒 **本窗之處置**：**⛔ 以該負對照為 `A` 之通過條件**；`A` 之採信改繫於
**外部錨**——`GB-67` 就地加註所載之 `2` 宗／`4.7950 ㎡`（**逐位相符**·`常規八 二 ①`）。
🛑 該負對照之漂移**本身**係一待決事實（其期望值鑄於 `W-G.9-88` 之態，
而圖資／telescoping／步驟 0 均已變動），**本批⛔ 就其作成任何處置**。

**`A` 之逐宗暫編地號（本窗所補·該權威源之 `暫編地號` 命中 ＝ `0`）**

| 街廓 | 側 | 宗序 | **暫編地號** | `G(㎡)` | 分配範圍面積 ㎡ |
|---|---|---|---|---|---|
| `R2` | left | 第 `1` 宗 | **`628-42(1)`** | `3.8200` | `3.808224` |
| `R5` | left | 第 `1` 宗 | **`628-53(2)`** | `0.9900` | `0.986729` |

🔒 **自我驗證閘**（`探針還原內部幾何須設自我驗證閘` 所令）：本窗以**同口徑同情境**
（逐街廓獨立·`0m`）重建之分配範圍面積，須復現權威源所印之值 ⇒
`3.808224` vs `3.8082`（`|Δ| = 0.000024`）／`0.986729` vs `0.9867`（`|Δ| = 0.000029`）
⇒ ✅ **過**（容差 `±0.05 ㎡` ＝ `GB-101` KL 裁 `2026-09-10`；本測為**同主機**故併報全精度差）。

### 二　`B` 母體（覆蓋帳違反組之逐重疊對）

**口徑 ＝ 全區連跑**（`run_verification` 之落檔）｜判定集基數：組 **`13`**／重疊對 **`3`**

| 情境 | 街廓 | 側 | 宗 A（序） | 宗 B（序） | 重疊面積 ㎡ |
|---|---|---|---|---|---|
| `0m` | `R2` | left | `628-41(1)`（**第 0 宗**·街角地） | `628-27(1)`（第 2 宗） | **`19.8142`** |
| `0m` | `R2` | left | `628-41(1)`（**第 0 宗**·街角地） | `628-40(1)+`（第 3 宗） | **`26.1624`** |
| `3.5m` | `R5` | left | `628-45(1)+`（**第 0 宗**·街角地） | `628-18(2)`（第 2 宗） | **`56.8186`** |

🔒 `B` 之**宗集** ＝ `['628-18(2)', '628-27(1)', '628-40(1)+', '628-41(1)', '628-45(1)+']`（**`5`** 宗）
🔒 二組之 `Σ` 分別為 `45.9766`（＝ `19.8142 + 26.1624`）與 `56.8186` ⇒ **與 `S-6` 逐位相符**。

### 三　🛑 對位之結果

| 集合 | 值 | 基數 |
|---|---|---|
| **`A ∩ B`** | **∅** | **`0`** |
| `A ∖ B` | `['628-42(1)', '628-53(2)']` | `2` |
| `B ∖ A` | `['628-18(2)', '628-27(1)', '628-40(1)+', '628-41(1)', '628-45(1)+']` | `5` |

判定集基數 ＝ `|A| = 2` ＋ `|B| = 5`。

**否證對照（同格載判定集基數）**

| 造 | 受詞 | 判定集基數 | 實得 | 判 |
|---|---|---|---|---|
| **必綠** | `0m·R2·left` 第 `1` 宗於**二口徑**須同名 | **`1`** | 全區連跑 `628-42(1)` ／ 逐街廓獨立 `628-42(1)` | ✅ 如預期 |
| **必紅** | 注入 `B` 之一宗（`628-18(2)`）入 `A` | **`1`** | `|A ∩ B|` 由 **`0`** 轉為 **`1`** | ✅ 如預期 ⇒ **交集運算子確能翻面·本檢⛔ 恆為 ∅** |

### 四　🔑 本窗所補之二項（**單未令·惟為停機上呈之必要材料**）

#### `(a)` 同口徑之對位 `B′`（＝ 於 `A` 之口徑重算覆蓋帳）

🩸 **由**：`A` 之口徑 ＝ **逐街廓獨立·僅 `0m`**；`B` 之口徑 ＝ **全區連跑**
（`0m` 止於 `R2`／`3.5m` 止於 `R5`）⇒ **`0m·R5` 於 `B` 之口徑下<u>結構上不存在</u>**
（`0m` 可達 ＝ `['R1','R2']`）⇒ 二者**⛔ 得逕取交集**。
⇒ 本窗於 **`A` 之同一口徑同一情境**重算覆蓋帳，得 `B′`：

| 街廓 | 側 | 宗 A（序） | 宗 B（序） | 重疊面積 ㎡ |
|---|---|---|---|---|
| `R2` | left | `628-41(1)`（第 0 宗） | `628-27(1)`（第 2 宗） | **`19.8142`** |
| `R2` | left | `628-41(1)`（第 0 宗） | `628-40(1)+`（第 3 宗） | **`26.1624`** |
| `R5` | left | `628-18(2)`（第 0 宗） | `628-7(2)`（第 2 宗） | **`56.3293`** |

判定集基數：組 **`11`**／重疊對 **`3`**。
🔑 **`56.3293` 於本窗獨立復現** ——與 `CLAUDE.md` 之「🔧 `56.3293` 之來源追溯」
（`W-G.9-266` 工項五）所裁之「`0m·R5`·**逐街廓獨立呼叫**口徑」**逐位相符**。

🔒 **`A ∩ B′` ＝ ∅**（`0` 宗）；`A ∖ B′` ＝ `['628-42(1)', '628-53(2)']`；
`B′ ∖ A` ＝ `['628-18(2)', '628-27(1)', '628-40(1)+', '628-41(1)', '628-7(2)']`。
⇒ 🔒 **交集為空<u>⛔ 係口徑之產物</u>——二口徑下皆為 ∅。**

#### `(b)` **組層**之對位（⛔ 只看宗層）

| `A` 之宗 | 落在 `B` 之違反組？ | 落在 `B′` 之違反組？ |
|---|---|---|
| `628-42(1)`（`0m·R2·left` 第 `1` 宗）| **是** | **是** |
| `628-53(2)`（`0m·R5·left` 第 `1` 宗）| **是**（同街廓側）| **是** |

⇒ 🔒 **宗層交集為 ∅，而<u>組層⛔ 為 ∅</u>** ——`A` 之二宗**皆位於違反組之內**，
惟**⛔ 為任一重疊對之成員**。

🔑 **結構之形（本窗之判·⛔ 推定）**：在**全部 `3` 個重疊對**中，重疊恆發生於
**第 `0` 宗（街角地）× 第 `2`／第 `3` 宗**——**恰好跨過第 `1` 宗**，
而第 `1` 宗正是 `A` 之受詞（`3.8082`／`0.9867 ㎡` 之極小宗）。
⇒ **街角宗之斜置切帶自第 `1` 宗<u>上方越過</u>而疊到其後之宗**。

---

## §三　本批之自解清單（`自解款 一 5`·逐項五款）

> 🔒 **凡未寫入本清單之自解，視同未自解。** 本清單**非空**，共 **`3`** 項。

### 自解 `1`　`S-2` 之 `ls-remote` 與單載期初態不符

| 款 | 內容 |
|---|---|
| **① 疑義逐字** | 單 `§零` ＝ 「**期初態** ＝ `e57d2ef86894472761e1403974ff1f2ce1e6a2df`（`origin/wip/s1-endpart`·須 `ls-remote` 實查）」；`S-2` ＝ 「`ls-remote` 與期初態逐位相符」。實查得 `abeb879…`。 |
| **② 二讀法** | **甲** ＝ 依字面「逐位相符」⇒ 不符即停機。**乙** ＝ 依 `常規九 二` 之體例（「`= <該 commit>` **或其後裔** ⇒ 已 push，正常開工」）＋ 單 `§十一`（工項零由審查窗辦·辦畢即推）⇒ 該後裔即單自身所令之產物。 |
| **③ 所取** | **乙**。 |
| **④ 機械證據** | `abeb879` 之父 ＝ `e57d2ef…`（直系後裔）；`git diff --name-only e57d2ef..abeb879` 對四檔生產碼清單之 `grep -xE` ⇒ **空輸出**（命中 `0` 行）；其 `name-status` 逐檔皆 `A`（`11` 檔·`docs/orders/` `2` ＋ `verify/probes/` `9`）；切至該態後 `left-right` ＝ `0　0`；`app.py` blob ＝ `c5ec503c…`（**與單 `S-3` 之期望逐位相符**）。 |
| **⑤ 所棄者之由** | 甲使**單自身 `§十一` 所令之工項零**必然令 `S-2` 轉紅 ⇒ 該閘**繫於 commit 身分**而非性質，正是 `W-G.9-199 常規補款三`（「若在本單開工前，倉內合法地多了一個**零生產碼** commit，本閘會不會紅？會紅者……須改綁性質」）所欲防之形。合 `白名單 6`（單內二處自相矛盾·二讀法後果皆⛔ 動土地 ⇒ 取較嚴者：本窗**加驗**其為零生產碼後裔，較單之字面更嚴）。 |

### 自解 `2`　`S-1` 之號占用六數非零

| 款 | 內容 |
|---|---|
| **① 疑義逐字** | `S-1` ＝ 「`W-G.9-268` 嚴格式與寬式**六數全 `0`**」；實測 `[2, 3, 2, 2, 3, 2]`。 |
| **② 二讀法** | **甲** ＝ 依字面非零即停機。**乙** ＝ 依自指款改判「占用者是否**全為**本單自身之產物」（`-267` 已循此例）。 |
| **③ 所取** | **乙**。 |
| **④ 機械證據** | 逐筆歸類見 `§一 S-1` 之表 ⇒ `D1`／`D2`／`D3` 之**全部 `7` 筆命中**皆落於工項零所入倉之**二檔**內；**檔框 ＝ `2` ＝ 該二檔**；對照甲 `-267` ＝ `18`／`19` 列框（須 `≥1`）✅；對照乙人造號六數全 `0` ✅。 |
| **⑤ 所棄者之由** | 甲與單 `§十一`（令工項零先行入倉）**結構上互斥**——凡照辦者其號必被自己的產物占用。合 `自解款` **白名單 `1`**（號占用非零，而占用者全為本單自身之產物）。 |

### 自解 `3`　`S-3` 之單一 blob 相異（`fixture_yi_construction.py`）

| 款 | 內容 |
|---|---|
| **① 疑義逐字** | 附件甲 `§3` 載 `fixture_yi_construction 69f4a18e/11562`；本窗實測 `69f4a18a`／`11562` B。 |
| **② 二讀法** | **甲** ＝ 倉態之別（該檔於二態間被改）⇒ `S-3` 不符 ⇒ 停機。**乙** ＝ 外部錨之**單字元抄錄之誤**。 |
| **③ 所取** | **乙**。 |
| **④ 機械證據** | `git rev-parse e57d2ef:verify/fixture_yi_construction.py` 與 `abeb879:…` **皆為 `69f4a18ae03f13181e6ca83ba8b291a54ea548ab`**（逐位相同）；`git diff --name-only e57d2ef..abeb879 \| grep -c 'fixture_yi_construction'` ⇒ **`0`**；bytes 二側皆 `11562`（**相符**）；其餘 `33` 檔逐檔相符。 |
| **⑤ 所棄者之由** | 甲被 `git` 直證為偽——該檔於附件甲量測之態（`e57d2ef`）與本窗之態（`abeb879`）blob **完全相同**，⇒ ⛔ 可能是倉態之別。🔒 **具名其為<u>白名單外之新類</u>**（`自解款` 界之判準所令）：「**外部錨之單字元抄錄誤，而受詞之二態逐位相同已由 `git` 直證**」；五款自檢 ＝ ① 零土地後果 ✅ ② 零生產碼 ✅ ③ 可機驗（`git rev-parse` 二態）✅ ④ 可逆（僅出艙）✅ ⑤ 出艙具名（本項）✅；黑名單五項逐項不落入。 |

---

## §四　`S-5`　`run_all` 期初對帳態（**只取值·不判過**）

（本節於長跑結束後以**末端追加**補入。）

---

## §五　🩸 本批之自捕（照實·⛔ 隱去·各載 形／徵候／攔法）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | `c0` 探針之 `parse()` 只吃**字串**，而 `run_step_g` 之**記憶體回傳**中 `cut_coords` 為 **`list`** ⇒ `ast.literal_eval` 拋例外 ⇒ 落入**我自寫之靜默退路** `or [(0,0),(1,0),(1,1)]` | 二宗之分配範圍面積**皆恰為 `0.500000`**（＝ 該三角形之面積）| **自我驗證閘當場轉紅**（`3.8082` vs `0.500000`·`|Δ| = 3.3082`）⇒ ⛔ 得下結論。修法 ＝ `parse()` 兼吃二形 ＋ **移除靜默退路**（取不到即 **loud 拒測**·`rc = 11`）⇒ 重跑得 `3.808224`／`0.986729` |
| `2` | `c0` 之**必紅對照**初版寫成 `... == len(inter) or True` ⇒ **恆真** | 印出「`A ∩ B` 由 `0` 變為 `0` ⇒ 如預期」——**二數相同而判為如預期** | 落地前自捕；改為**真能翻面**之造：注入 `B` 之一宗入 `A` ⇒ `|A ∩ B|` 須由 `0` 轉為 `1`（實測如預期）⇒ **證交集運算子⛔ 恆為 ∅** |
| `3` | 承 `-267R 自捕 6` 之通則（「對照組得數與受詞同者一律先判對照選錯」）之**再一次應用** | 自捕 `2` 即其形 | 已於本批**開工前**即以該通則自檢每一對照；`S-3` 之對照乙改用 `rc ≠ 0`、`S-8` 之對照乙用執行期組出之人造字樣 |

🔒 **`1` 之通則**：本倉既有之「`探針還原內部幾何時，須以碼面自身之保證當自我驗證閘`」
**再一次**於本批擋下一個**會印出漂亮但錯的表**之重建——若無該閘，`c0` 會以
「`A` 之二宗面積各 `0.5000 ㎡`」出艙，而其結論（`A ∩ B = ∅`）**恰好仍為真**
⇒ 🔴 **對的結論 ＋ 錯的證據**，且**⛔ 有任何既有閘會擋下**。

---

## §六　🛑 停機上呈

### 一　停機款（單 `§八`·逐條自檢）

| # | 停機款 | 自檢 |
|---|---|---|
| `1` | 開工閘任一不符 | 🟡 `S-2`／`S-1`／`S-3` 三處字面不符，**皆已依 `自解款` 自解並具名**（`§三`）；餘五閘全符 |
| `2` | **`c0` 之 `A ∩ B = ∅`** | 🛑 **觸發** ——見 `§二 三`。**且於同口徑之 `B′` 下亦為 ∅**（`§二 四(a)`）|
| `3` | 差異表出現街角地歸屬變動 | ⬜ **未及**（`c1`〜`c5` 未辦）|
| `4` | 須動及 `§零` 未列之受詞或未列之方法 | 🟢 未觸發 |
| `5` | `S-8` 之 `驗_rejected` 命中 `> 1` | 🟢 未觸發（實測 `1`）|
| `6` | 單與正典相斥 | 🟢 未觸發 |

### 二　🛑 所呈之題（**⛔ 由 CC 自裁**·候發單側／KL）

> **題**：`GB-67` 之受詞（`A` ＝ `K-9-9 二` 不交叉之違反·`2` 宗／`4.7950 ㎡`）
> 與本波之驗收判準（`B` ＝ 覆蓋帳歸零）**在宗層互不相交**；
> 而單 `§六` 併線前必答 `1` 令「覆蓋帳 `13` 組 ＋ `R3` 組**全部歸零**」。
> ⇒ 🔴 **「`K-9-23` 閘一轉主動剔除 `A` 之二宗」此一方法，<u>結構上⛔ 觸及</u> `B` 之三個重疊對**
> ——重疊之二端恆為**第 `0` 宗（街角地）** 與**第 `2`／第 `3` 宗**，
> 而 `A` 之二宗為**第 `1` 宗**且**⛔ 為任一重疊對之成員**。
> ⇒ **方法與受詞須重新對位**（單 `§二 c0` 之停機款逐字）。

🔒 **本窗所備之材料（供重新對位用）**
1. `A`／`B`／`B′` 三母體之**逐宗**與**逐對**具名（`§二`）。
2. 二口徑之**明確界定**與其**不可通約之處**（`0m·R5` 於全區連跑口徑下結構上不存在）。
3. **組層對位**：`A` 之二宗**皆在**違反組內 ⇒ 二現象**同組而不同宗**。
4. 結構之形：重疊恆為**第 `0` 宗 × 第 `2`／第 `3` 宗**，**跨過第 `1` 宗**。

🛑 **本窗⛔ 作成下列任一**：⛔ 就「方法應如何重新對位」作成任何主張；
⛔ 動 `app.py` 或 `verify/stepg_pipeline.py` 一字；⛔ 開側分支；⛔ 鑄任何號；
⛔ 就 `GB-67` 之修法作成任何主張；⛔ 放寬單之任何判準。

### 三　⬜ 本批**未辦**之項（逐項具名·⛔ 靜默略過）

| 項 | 狀態 | 由 |
|---|---|---|
| `c1`（遞補迴圈骨架 ＋ 旗標 `off`）| ⬜ **未辦** | 停機款 `2` 觸發（單 `§二 c0` 逐字「⇒ **停機上呈**」）|
| `c2`（旗標 `on`·幾何變動·有土地後果）| ⬜ **未辦** | 同上；且須 KL 單獨放行 |
| `c3`（`_block_strip` loud 斷言）| ⬜ **未辦** | 同上 |
| `c4`（`contract` 段）| ⬜ **未辦** | 同上 |
| `c5`（差異表 ＋ 報告）| 🔶 **部分**（本報告即其報告之一部；**差異表⛔ 產出**——其受詞為 `c2` 之前後差異）| 同上 |
| `§三` `P-1`〜`P-7` 七閘 | ⬜ **未辦** | 其受詞為 `c2` 之改前改後；`c2` 未辦 ⇒ **判定集為空** ⇒ 依「`序 11` 之一般化」款 `二` **loud 拒測**，⛔ 以空集之「全數通過」充作綠 |
| `§四` 差異表 | ⬜ **未產出** | 同上 |
| `§六` 併線前必答 `1`〜`4` | ⬜ **未辦** | 同上 |
| `§六` 併線前必答 `5`（`c0` 之 `A`／`B` 對位表）| ✅ **已辦** | 見 `§二`——**且已回答其所令之「須說明覆蓋帳歸零與 `4.7950 ㎡` 之關係究竟為何」**：二者**同組而不同宗**，重疊跨過 `A` 之宗 |
| `§七` `自誤 333` 之鑄 | ⬜ **未鑄** | 本批因停機而未及辦；**⛔ 逾授權**（單只授權此一號）⇒ 候發單側指示 |

---

## §七　自限復驗

（本節於長跑與入倉後以**末端追加**補入。）

---

## 🔧 `§四` 之補入：`S-5` `run_all` 期初對帳態（`W-G.9-268R` 末端追加·⛔ 上文一字不刪）

🛑 **本閘只<u>取值</u>·⛔ 判過**（單 `S-5` 逐字：「⛔ 以『與凍存相同』為通過條件——期初即不相同」）。

🔒 **執行框（`CLAUDE.md`：`run_all` 會改寫已追蹤 log ⇒ 須於拋棄式副本內跑）**
本閘跑於一**拋棄式 detached worktree**（`C:/w268`·短路徑·`core.autocrlf=false`·態 ＝ `abeb879…`），
跑畢即 `git worktree remove --force`（`git worktree list` 之 `w268` 命中 ＝ **`0`** ⇒ 已移除）。
⇒ 🔒 **主工作樹之已追蹤 log 一字未動**（`git status --porcelain` 之 `M` ＝ **`0`** 列）。

| 欄 | 實測 |
|---|---|
| `rc` | **`1`** |
| `stderr` | **`0`** B |
| 逐字結論列（log 末）| **`W-V run_all: FAIL`** |
| `RESULT` | `RESULT: FAIL` |

🔒 **對帳態（`verify/wv_reconcile.py`·於每次 `run_all` 自動執行）**

| 量 | 期初實測 | 單 `S-5` 所載之外部錨 | 判 |
|---|---|---|---|
| 名目：**凍存** | **`22`** | `22` | ✅ 相符 |
| 名目：**現況** | **`24`** | `24` | ✅ 相符 |
| **凍存有而現況無**（`A ∖ B`）| **`1`** 項 | `1` | ✅ 相符 |
| **現況有而凍存無**（`B ∖ A`）| **`3`** 項 | `3` | ✅ 相符 |
| **類 I**（正規化後相同·自動視為同）| **`0`** 項（其中 raw 即相同 `0` 項）| — | 取值 |
| **類 II**（僅 traceback 行號不同·⛔ 自動視為同）| **`8`** 項 | — | 取值 |
| **類 III**（訊息實質不同）| **`13`** 項 | — | 取值 |
| 對帳判 | 🔴 **對帳 FAIL**——名目或失敗原因與凍存不符（⛔ 逕自更新名單）| — | **⛔ 判過**（單所令）|

🔒 **逐項具名（`A ∖ B` ＝ `1`／`B ∖ A` ＝ `3`·逐字）**
> **凍存有而現況無**：`['率接線 G估 欄變動0m 1 格（期 0·Y 波後 baseline 同源）']`
> **現況有而凍存無**：`['率接線 G估 欄變動0m 8 格（期 0·Y 波後 baseline 同源）', '抵費地0m(應空)', '率接線 G估 欄變動3.5m 12 格（期 0·Y 波後 baseline 同源）']`

🩸 **`A ∖ B` 與 `B ∖ A` 之首二項係<u>同一名目</u>而**內嵌計數相異**（`1 格` vs `8 格`）
——即附件甲 `U-4` 所具名之「`名目` 內嵌計數 ⇒ 名目差集閘繫於**代理量**」之形。
🛑 **本批⛔ 就其作成任何處置**（單 `§十` 之裁：`U-4` 採甲 ＝ **只具名·⛔ 立條文**·列入 `-269` 候辦）。

🔒 **本節之用**：此即單 `S-5` 所令之**期初對帳態**，供 `W-G.9-268` 之續辦批
作為 `§六` 併線前必答 `3` 之**比較端**（⛔ 凍存名單之舊態）。
**落檔** ＝ `verify/out/WG9268R_runall_pre.log`（`52735` B）／`.err`（`0` B）。

---

## 🔧 `§七` 之補入：自限復驗（`W-G.9-268R` 末端追加·⛔ 上文一字不刪）

| 項 | 實測 | 判 |
|---|---|---|
| **生產碼 `34` 檔 blob 期初 ＝ 期末** | 逐檔 `sha`／`bytes` 全同（唯一「相異」＝ 附件甲之抄錄誤·見 `自解 3`）；`app.py` ＝ `c5ec503c740f59e084cedd83531b9d029fe8ddc0`／`1454937` B | ✅ **零生產碼** |
| **`git diff --name-only <基座>..<本 commit>` 對四檔清單之 `grep -xE`** | 見下 `§八` | ✅ **空輸出** |
| **`commit` 數** | `1`（本報告 ＋ 探針 ＋ 落檔·**皆零生產碼**）| — |
| **側分支** | **`0`**（`c1`〜`c4` 未辦 ⇒ ⛔ 開側分支）| ✅ |
| **鑄號** | **`0`**——四簿 MAX 於期初／期末**皆未動**：自誤 `相異 325`／`MAX 332`／`缺號 [106]`；`GB` `相異 154`／`MAX 156`／`缺號 [12, 87]`（母體 ＝ **各該單檔**·**列框**·態 `abeb879…`）| ✅ |
| **`app.py`／`verify/stepg_pipeline.py`** | 一字未動 | ✅ |
| **`docs/orders/` 之二檔** | 一字未動（`deletions` 逐列 `0`）| ✅ |

---

## 🔧 `§八`　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**·⛔ 以「我判斷是零生產碼」代之）

🔒 **基座** ＝ `abeb8794fe930a590fe1c27fa2dd10f85d6bfed1`（＝ 前一已 `push` 之 `HEAD`）。
🔒 **指令（逐字）**

```
git diff --name-only <前一已 push 之 HEAD>..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

（其逐 `commit` 之輸出見本節落地後之末端追加；本批之 `commit` 皆為 `c0`／報告／落檔，
依單 `§五` 之「零生產碼 `commit`（`c0`／`c5`）依 `常規一 🔧 一` **逕行 `push`**」。）

🛑 **本節之量測與其所守護之 `push` 係<u>分開之呼叫</u>**（`W-G.9-199 常規補款一`）。
