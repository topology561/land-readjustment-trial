# `W-G.9-248R`　段甲：工項零（本單原封入倉）＋ 工項四（鑄 `自誤 310`）

> 🛑 **本段⛔ 零生產碼**（僅動 `docs/`）·**鑄 `自誤 310`**·⛔ 鑄 `GB`／`VR`／戒／`K-9`。
> 🔒 **取號** ＝ `常規四（三）`（CC 產物一律「該單號 ＋ 後綴 `R`」·⛔ 另取新號）。
> 🔑 **一切數字現查於倉**；三軸（來源 `commit`／母體／粒度框）逐處具名（`VR-091 一`）。
> ⬜ **段乙（工項一–三·生產碼）另辦** ⇒ 側分支 `verify/W-G.9-248-c1`，候發單側復驗、KL 放行。

---

## §零　開工閘

**態** ＝ `origin/wip/s1-endpart` ＝ **`f73ce5eb3befd8ff2ccc53571271083644914bcd`**（`ls-remote` 實查）；
`left-right` ＝ `0	0`；工作樹 `0` 異動。

### `S-1`　宣告框（三數分列·`常規四（七）`＋`W-G.9-243 補款⑥`）

**三軸** ＝ `blob@f73ce5e`／母體 **全 `docs/` `577` 檔**（可讀 `558`／二進位或讀不到 `19`）／
粒度框 **檔框 ＋ 列框**。🔒 母體之 `577`／`19` **與單 `S-1` 自載逐數相符**。

| 號 | `D1` | `D2` | `D3` | 檔框 | 列框(`=D2+D3`) | 鬆框 檔／列 | 判 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-248`** | `0` | `0` | `0` | **`0`** | **`0`** | `5`／`6` | ✅ **未占用**（**逐數與單 `S-1` 自載相符**）|
| `W-G.9-247` | `2` | `3` | `0` | `2` | `3` | `7`／`38` | 對照組甲（須 `≥1`）✅ **逐數與單載相符** |
| `W-G.9-246R3` | `1` | `2` | `0` | `2` | `2` | `3`／`8` | 對照組甲2（須 `≥1`）✅ |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱）| `0` | `0` | `0` | `0` | `0` | `0`／`0` | 對照組乙（須 `=0`）✅ |

🔑 **`檔框 ≠ D1`**：檔框 ＝ `|D1 ∪ D2 之檔 ∪ D3 之檔|`（`W-G.9-243 補款⑥` 逐字）
——`246R3` 之 `D1 1` 而檔框 `2`（其 `D2` 之二列分居二檔）即其例；
🩸 本報告初稿於此格誤寫 `1`，**已於入倉前自捕更正**。

🔴 **併呈（⛔ 停機款·⛔ 由 CC 自裁）：`248` 之<u>意指</u>已被占用**
`常規四（七）一②` 逐字含「**施工單已發而未執行者**」。倉內**二處**逐字將 `248` 指定為**接閘單**：

| 錨 | 逐字 |
|---|---|
| `docs/reports/W-G.9-246R2_CC交接文.md:7` | `> ⛔ **取 \`W-G.9-248\`**——其已於 \`W-G.9-246′ §三\` 被指定為**接閘單**（\`常規四（七）②\` 之意指占用）。` |
| 同檔 `:183` | `**\`W-G.9-248\`：接閘**——依 verdict 剔宗、整包回滾、左右雙鏈（\`-246′ §三\` 逐字）。` |

而**本單之受詞⛔ 接閘**（`K-9-1` UI 掛點／道路名稱欄／混型欄）。
🔒 **CC ⛔ 停機**——`入倉前之號占用閘` 款一之受詞逐字為「**倉內占用（檔框 ∪ 列框）**」＝ **實體占用**，
實測 `0`／`0` ⇒ ⛔ 觸發該款；且款一明令「⛔ **自行改號後續辦**（改號屬**意思決定**）」。
🛑 **⇒ 接閘單日後須另取號**（`249` 現查 `0`／`0`，惟單 `§前言` 已將 `-249` 意指予「街角地指配之處置」）。
**候發單側／KL 裁**；本節⛔ 作成任何處置、⛔ 追改任何既有指定。
🩸 **併記**：單 `§零 S-1` 只報宣告框與鬆框，**未報意指占用**（`常規四（七）二` 令「同格報二事」）。

### `S-3`　生產路徑錨（`blob@f73ce5e`·本段前後須逐位不變）

`app.py 5693242b19d970c1`／`stepg_pipeline.py c8d46d2e5fb79dc5`／`run_all.py 4ba89fef90979491`／
`run_verification.py d0d531f7dc051b65`／`selection_pipeline.py 6c6783420dbc635c`／
`wf_f0 6758ea766b001b95`／`wf_f1 30e19048dfd781bc`／`wf_f2 f974724d7e694ad7`／
`wf_f3 226c7c5fa00464f3`／`wf_f4 b36aeca6c4f6b364`／夾具 `b17826a6f7b98bf5`
⇒ **逐檔與單 `§零 S-3` 所載相符**。

---

## §一　工項零：本單原封入倉

🩸 **單之所在（現查·⛔ 憑記憶）**：該單**⛔ 在倉**——全部 `14` 支遠端 heads 之 `docs/orders/` 皆無 `248` 之檔；
其位於**主 checkout 之工作區**（`C:\…\land-readjustment-trial\docs\orders\`）。
⇒ 工項零**確係未辦**，本節辦之。
🔒 **窮舉之母體與對照組**：層一 倉側追蹤 `1674` 檔（文字類 `939`）／層二 未追蹤 `0` 列／
層三 主 checkout 工作區／層四 scratchpad／層五 全部遠端 heads／層六 意指占用。
對照組甲 `W-G.9-246R3` 檔名命中 `1` ✅；對照組乙（人造號）`0` ✅ ⇒ 器非紅。

### 🔒 閘（`作業常規之追加四` 款一）

**落點** ＝ `docs/orders/W-G.9-248_K-9-1掛點更正與道路名稱欄與混型欄.md`
（`9484` B／`151` LF／**全 LF·CRLF `0`**）。

| 閘 | 實測 | 判 |
|---|---|---|
| 來源 `sha256` | `caadbc1eb373bb5d344fa28a9d15d74f99be4aac82ca7e8522921818de11fd63` | — |
| 落檔 `sha256` | **同上** | ✅ 逐位相同 |
| **入倉 blob 之內容 `sha256`** | **同上**（blob `ead3b45ffd173bce1d65821243308af35218aade`·`9484` B）| ✅ **逐位相同** |
| 判別力自證 | 人造「改一位元組」之變體其 `sha256 == 來源` ⇒ **`False`** | ✅ 非恆真 |

🔒 **binary copy·⛔ 經 shell 命令替換**（陷阱 `5`：含非 ASCII 之檔名於 `cp950` 下會毀損）
⇒ 開檔／雜湊／`git add` **一律於 Python 內**為之。
🛑 **本閘之射程界限（照實·⛔ 頂替）**：所證者為「**落檔 → 入倉**」之保真，
**⛔ 證「發單側送出之原文 → 落檔」之保真**（`作業常規之追加四` 自載之界限）。

### `S-0b`　`probe_order_preflight`（`GB-148`：於倉內跑·`常規五`：`PYTHONIOENCODING=utf-8`）

🔑 **`--selftest` 四造（⛔ 可省）：全數成立** ⇒ 器**非恆綠亦非恆紅**
（靶① `-190` 之 `P-3` 紅／靶② `-190` 之 `P-4` 提示／靶③ `-191 §六-3` 之 `P-6` 提示／反靶 `-190R` 之 `P-3` 不紅）。

**對本單**（`rc = 0`·**直取·⛔ 取自管線之後**·`GB-145`）：

| 級 | 項數 | 內容 |
|---|---|---|
| 🔴 機械·停機款 | **`0`** | — |
| 🟡 提示 | **`0`** | — |
| ℹ️ 記錄 | `2` | `P-5` `SELF_SHA256` ✅ **相符**（受詞 `9406` B·實算 `66639764501ae8a1…` ＝ 單載）；`P-3` `def main` 區間（AST 實查）＝ `14833`-`24311` |

🔒 **CC 側之獨立復算**：`P-5` 之受詞（自檔首至最末一個 `SELF_SHA256` 列之前一個 `\n` 含之）＝ `9406` B，
實算 `66639764501ae8a1a1f4e7f1e1d1922d8a7021429a43f3e2337db19226c27569` ＝ 單載 ✅；
**判別力**：對**全檔**施同一口徑 ⇒ **不符** ✅（證該檢非恆綠）。

🛑 **CC 之實質把關（`P-3` 未紅⛔ 等於本規已滿足）**
`CLAUDE.md` 逐字：「凡新增或修改落在 `main()` 內之可執行敘述，`run_all` 之名目集合結果
**不得單獨作為驗收依據，必須另附合成案**。」
本單工項一／二之受詞**確在 `main()` 內**（`P-3` 記錄之區間 `14833`-`24311`），
而單之 `V-0` 係 `run_all`。🔒 **單已另附 `V-1`〜`V-5` 之合成案** ⇒ **本規已滿足**；
`V-0` 於本批之角色僅為「**未波及其他名目**」之證，⛔ 得單獨作為工項一／二之驗收依據。

---

## §二　工項四：鑄 `自誤 310`

### 🔒 末號當場復算（CC 側·⛔ 採單之數）

**三軸** ＝ `blob@f73ce5e`／母體 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**
（`619457` B／`7229` LF）／**列框**。**框** ＝ `VR-091 四` 定義框 ＋ 範圍框。

| 量 | 值 |
|---|---|
| 定義框列命中 | **`307`**（**含**就地加註節）|
| 範圍框 | `8` 條 ／ 相異 **`302`** |
| `MIN` ／ `MAX` | `7` ／ **`309`** |
| 缺號 | **`[106]`**（已結案·永久空置·`自誤 271`）|
| 恆等式 `(MAX−MIN+1) == 相異＋缺號` | **`True`** |

⇒ **單載 `MAX 309` ✅ 相符** ⇒ **`N` ＝ `310`**。
**對照組**：既有 `309` 命中 `1`（須 `>0`）✅／未鑄 `310` 命中 `0`（須 `0`）✅。

### 🔒 `310` 之意指占用（**二形並取**·`常規四（七）補款二`·母體 ＝ 全 `docs/` `577` 檔）

| 形 | 檔 ／ 列 | 判 |
|---|---|---|
| `自誤 310`（無反引號）| `0` ／ `0` | ✅ |
| `` 自誤 `310` ``（帶反引號）| `0` ／ `0` | ✅ |
| **對照組** `自誤 309` | **`2` ／ `2`** | ✅ 器非紅 |

### 🔴 CC 之自捕（**入倉前**·責 ＝ CC·依 KL `-246R3` 工項三 `4` **⛔ 入 claude.ai 側自誤簿**）

**壞錨**：本則初稿以 `st.form("f3_road_form")`（**含右括號**）為介面甲之字樣錨，
而真碼為 `with st.form("f3_road_form", clear_on_submit=False):`
⇒ 該固定字串實測命中 **`0`**。**若入倉，下一窗據以 `grep` 將得 `0` 而誤判介面甲不存在。**
🔒 **已於入倉前自捕並改為 `st.form("f3_road_form"`（去右括號）⇒ 實測命中 `1`。**
🩸 同批另二宣稱亦不符並已更正：`st.form(` 宣稱 `5` → **實測 `3`**；
`st.expander(` 宣稱 `22` → **實測 `39`**。
🔑 **此即 `自誤 295`／`W-G.9-243 補款⑥` 之形**——「**載框名 ≠ 框實作正確**」；
本次係**立戒者於次一批親踩**（`CLAUDE.md`「立戒者在下一批最容易違反自己的戒」）。
🛑 **通則（CC 自立）**：**以固定字串為錨者，⛔ 含其右界之標點**
——真碼常於該處續接參數（`, clear_on_submit=False`）⇒ 含 `)` 之形必失配。

### 🔒 所鑄之則（標題形取既有最末一則為範本·`常規四（九）四`）

`## 自誤 \`310\`　**\`V-B\` 未就 UI 落點對拍單之意圖（\`306\` 同族）**`
——**責 ＝ 發單側**；單 `§五` 之逐字全文引錄（⛔ 改寫），另附 CC 側之字樣錨實測與判別力自檢。

---

## §三　【驗收】（段甲之部分）

| 證 | 內容 | 判 |
|---|---|---|
| 工項零之閘 | 落檔 `sha256` ＝ 入倉 blob `sha256`（逐位）＋ 判別力自證 | ✅ |
| `S-0b` | `--selftest` 四造成立；本單機械停機款 `0`／提示 `0`；`P-5` 相符 | ✅ |
| 工項四 | 末號復算與單載相符；`310` 二形占用皆 `0`；兩造對照組如預期 | ✅ |
| `V-B`／`V-C`（段甲部分）| 十檔 ＝ **變 `0`／不變 `10`**，夾具不變（本段零生產碼）| 見 `§四` |
| 三軸 | 逐處出艙 | ✅ |

⬜ **`V-0`／`V-1`／`V-2`／`V-3`／`V-4`／`V-5` 屬段乙**，本段⛔ 辦。

---

## §四　收工閘（判準先行載明·⛔ 事後改判準）

| 閘 | 判準 |
|---|---|
| `S-1` | `git diff --numstat <基座>..<本 commit>` 之**刪除欄逐列為 `0`** |
| `S-2` | 生產碼判法逐字指令 ⇒ **空輸出**（`rc` 直取·⛔ 管線後·`GB-145` 二 rc 分報）|
| `S-3` | 十檔 ＋ 夾具 `sha256[:16]` 前後**逐位相同**（變 `0`／不變 `10`）|
| `S-4` | `MAX` 由 `309` → `310`；`310` 在命中集且⛔ 在缺號集；缺號集恆 `[106]`；恆等式 `True` |
| `S-5` | `wg942_append_audit`（嚴格前綴 ＝ `True`）證自誤簿未被改寫 |
| `S-6` | 入倉之單 blob 內容 `sha256` ＝ `caadbc1e…`（**入倉後再驗一次**）|
