# W-G.7／K-6-A2 **S6-2-C**：§二-2 寬度不足之成因 —— **已查明**（⛔ 零生產碼面）

**基準**：`wip/s1-endpart` @ `527dbdd`。⛔ `git diff -U0 527dbdd -- app.py` ⇒ **0 行**。
**碼面異動**：僅 `verify/probes/probe_K9_s62_k98_line_vs_chord.py` 之 **§【C】結論分支**
（一句歸因更正 ＋ 收支表印出）＋ 其重跑之 log。**生產碼零觸。**
**實料**：`verify/out/probe_K9_s62_k98_line_vs_chord.log`（重跑後）
🔴 `K-9-5-3` 仍暫停適用；二則互斥標記**未動**。⛔ 本文**不裁定**、不寫判定碼。

> **一句話**：不是「構造被破壞」。**寬不足與面積不足是同一件事的兩種量法**；
> 閘之所以過，是因為它比的是**樂觀口徑上界** `cand_G`，不是**實配面積**。

---

## 一、成因鏈（五步·每步附碼面錨）

| # | 步 | 值 | 錨（行號綁 `527dbdd`） |
|---|---|---|---|
| 1 | 資格閘比 `cand_G` ≥ `min_area_to_apply` | **240.57 ≥ 232.79**（+7.78）⇒ **過** | `app.py:11255`／左端 `:11238` |
| 2 | `cand_G` 依設計即**上界**（「樂觀口徑」） | — | `app.py:11506–11512` |
| 3 | 但**實配** G（`g_rows`）＝ | **227.72** ⇒ 比門檻**短少 5.07** | log 【E】·⑧ 欄 |
| 4 | 該短少 ⟺ 寬度短少 × 深度 | `0.152969 × 33.126748 ＝ 5.0674` | log 【B】·下 §二 |
| 5 | ⇒ 量得寬 **6.847031 < T ＝ 7.00** | 差 **−0.152969** | log 【B】 |

⇒ **`K-9-5-2` ⑤（面積夠 ⇒ 寬度自動合格）在此格不成立，斷點在第 1 ↔ 3 步之間**：
⑤ 之「面積夠」被閘保證的是 **`cand_G` 夠**，而寬度量的是**實配幾何**。
**二者不同量**（差 **12.85 ㎡**）。⑤ 本身**未被證偽**——若以實配面積代入，
`227.72 < 232.79` 與 `6.847031 < 7.00` **方向一致、無矛盾**。

### 1-1 🔒 「樂觀口徑」係碼面明文，非本文之推測

`app.py:11506–11512`（`grep -n "def _corner_first_lot_G" app.py`）逐字：

> 🆕 P-A（裁定M·M-2/Q1）：**假設第 1 宗**之真 G（**樂觀口徑**）。
> 樂觀口徑（Q-M2·兩端皆不預設 forced·buf=0）

⇒ **`cand_G ≥ 實配 G` 是宣告過的設計意圖**，不是缺陷。

---

## 二、數值收支（本批之核心證據）

| 量 | 值 | 出處 |
|---|---:|---|
| `T` ＝ 退縮 3.5 ＋ 最小寬 3.5 | **7.00** | log 【B】·`app.py:10135`／`:10612` |
| min(a) 線距 ＝ min(b) 弦（帶端 `t=0`） | **6.847031** | log 【B】 |
| (a)(b) @ 帶底 `t=33.126748` | **7.249478** | log 【B】 |
| Δ寬 ＝ `T − min(a)` | **0.152969** | 算 |
| 深度（帶底 `t`） | **33.126748** | log 【B】 |
| **Δ寬 × 深度** | **5.0674** | 算 |
| 街角規定範圍（截角後） | **232.79**（顯示值） | log 【E】·⑥ |
| 實配 G（`g_rows`） | **227.72**（顯示值） | log 【E】·⑧ |
| **規定範圍 − 實配 G** | **5.0731** | **探針全精度相減**·log 【C】 |
| **殘差** | **−0.0057** | 落在 G 之 2dp 捨入帶（±0.01）內 |

⇒ **寬度短少與面積短少數值上是同一個量。**

> 🔒 **戒（本批當場踩到）**：上表之 `232.79`／`227.72` 係 **2dp 顯示值**，
> ⛔ **不可拿顯示值手算差**——手算得 `5.0700`／殘差 `−0.0026`，
> 而探針以**全精度**相減得 `5.0731`／殘差 `−0.0057`（**偏小 0.0031**）。
> 本文初稿即為手算值，經探針覆核後更正。**同族於 `CLAUDE.md`「位元組數／校驗碼須聲明量測框」。**

### 2-1 ⚠️ 本收支表之**限度**（⛔ 勿讀成解析恆等）

上式係**數值相符**，⛔ **未**做解析推導：量寬沿 `d̂`、帶端沿 `b̂n`，二者**不正交**
（ALLOC-FRONT 偏角 2.6°–5.3°·`app.py:11523`）⇒ 嚴格式應含**投影因子 `h`**
（＝交接文 §六-3 所立之 `h = b̂n·n̂`）。本批**未分離 `h` 之貢獻**。
⇒ 宣稱僅止於「**二者相符至 2dp 捨入帶**」，⛔ 不宣稱 `Δ面積 ≡ Δ寬 × 深度`。
**下一批可做**：以解析式重算並驗殘差是否隨 `h` 收斂。

### 2-2 內部一致性檢查（**非獨立證據**·同一線性模型）

以平行平移模型回推截角三角形面積（自規定範圍一路、自 winner 一路），
二者之差 **＝ 上表同一殘差**（`−0.0057`）⇒ 模型內部自洽、二形之截角為同一三角形。
⛔ 因與 §二 同源，**不得**當作第二個獨立佐證。
（虛擬塊（截角前）＝ `(6.847031 + 7.249478)/2 × 33.126748 ＝ 233.4858`；
規定範圍（截角前·推）＝ `(7.000000 + 7.402447)/2 × 33.126748 ＝ 238.5531`。）

---

## 三、🔧 更正：一句**歸因錯誤**（前版探針之結論句）

前版 `probe_K9_s62_k98_line_vs_chord.py` 於 `(a) < T` 分支印：

> ⇒ 🛑 **(a) 亦 < T** ⇒ 假說**不成立**：**構造保證本身即未達 T** ⇒ **停機上呈**（另一種病）。

**後半句錯。** 該探針**自己的 docstring §一**（同檔·第 32 行）已寫明：

> 差別僅在 `_Lt` 之來源：自檢①′ 用**構造解** `t*`，本檔用 **winner 實配之 `L_in`**

碼面覆核（`527dbdd`）：

| 自檢 | 其 ALLOC 線之來源 | 錨 |
|---|---|---|
| 自檢① | `w0, wD = _W(_band_lo, t_star), _W(_band_hi, t_star)` | `app.py:10713`（`grep -n "構造自檢不合" app.py`） |
| 自檢①′ | `_Lt` 由 `Q0 + t_star·an ± au·_LBIG` 生成 | `app.py:10726–10727` |
| 探針 (a) | **winner 實配之 `L_in`**（宗地環上平行 ALLOC 之邊） | 探針 `_lin = _edges[-1]` |

⇒ **構造保證（自檢①／①′）從未涵蓋 (a)** ⇒ ⛔ **不得由 `(a) < T` 推論「構造被破壞」。**
⇒ 交接文 §二-2 所載之「**另一種病**」**不存在**；🛑 該項**應予解除**（待 KL 認可）。

**已修**：該分支改為正確歸因 ＋ 印出 §二 之收支表（依常設條「結論須寫進輸出」）。

### 3-1 🔑 可複用之戒（建議入失敗考古·**本文不逕改技能檔**）

**探針之結論句與其自身 docstring 相矛盾，而無人對讀。**
此為交接文 §六-1 所指「節 42／47／48／49 同一根」（**未與同倉既有逐字紀錄對讀即下斷言**）
之**新變體**：前四例皆為「與**他檔**未對讀」，本例是**與同一檔之上方 40 行未對讀**。
⇒ 戒：**探針之結論分支，須明列它所依據的量之定義出處**（同檔 docstring 亦須引），
⛔ 不得只印判斷詞。

---

## 四、🆕 附帶發現：真G − 實配G 於 winner **恆正且與非-winner 完全分離**

逐格攤開（來源：log 【E】之 ②真G 與 ⑧g_rows G·41 列全取）：

| 類 | 格數 | gap 範圍 |
|---|---:|---|
| **WINNER** | **12** | **+11.51 ～ +22.96** |
| 非-winner | 29 | **−5.48 ～ +6.95** |

⇒ **零重疊**，中間空隙 **4.56 ㎡**。逐塊（winner）：

| 街廓/側 | 街角第1宗 | 真G | 實配G | gap |
|---|---|---:|---:|---:|
| R4/左 | 628(1) | 1544.01 | 1521.05 | +22.96 |
| R1/左（3.5m） | 628-36(1) | 598.50 | 576.44 | +22.06 |
| R4/右 | 628-1(1) | 477.62 | 457.76 | +19.86 |
| R6/右 | 628-18(1) | 325.16 | 307.78 | +17.38 |
| R2/左（0m） | 628-40(1) | 300.40 | 283.61 | +16.79 |
| R5/左（0m） | 628-7(2) | 247.96 | 233.57 | +14.39 |
| **R1/右** | **628(5)** | **240.57** | **227.72** | **+12.85** |
| R1/左（0m） | 628-37(1) | 220.23 | 208.72 | +11.51 |

**符號方向與「樂觀口徑」相符**（§一-1）⇒ **不是新缺陷**。
⛔ **但其量級（11.5–23 ㎡）本批未歸因**——`app.py:11466`／`:11508` 宣稱
「與實配第 1 宗**共用同一 solve 路徑**（Q-M4·禁另寫平行式）」，
而 `_corner_first_lot_G` 明文放寬**兩端皆不預設 forced**、**buf=0**、`W_prev=0.0`（`app.py:11591`）。
⇒ **哪一項放寬貢獻多少，未查。⛔ 不臆測。**
⚠️ 反例存在：R1 兩端於 0m **皆有 winner、無 forced**，故「不預設 forced」**單獨解釋不了該塊之 +11.51／+12.85**。

---

## 五、對 §二-1 互斥案之效力（⛔ 只陳述·不裁定）

`3.5m R1/right 628(5)` **仍確為**：閘過（`cand_G` 240.57 ≥ 232.79）**且**寬不足（6.847031 < 7.00）。
⇒ `K-9-5-2-a`-3 所指之「面積過、寬不足」情形，**以閘之口徑仍成立**。
⚠️ **惟本批新增一個 KL 可能想知道的事實**：**若以實配面積為口徑，它是「面積也不足」**
（227.72 < 232.79·短少 5.07）⇒ 二則之互斥，**可能取決於「面積」採哪一口徑**。
⛔ **口徑之選擇屬域裁**——上呈 KL，本文不代裁。

---

## 六、驗收

| # | 項 | 結果 |
|---|---|---|
| 1 | `git diff -U0 527dbdd -- app.py` | **0 行**（生產碼零觸） |
| 2 | 成因鏈五步逐步附碼面錨 | ✅ §一 |
| 3 | 收支表殘差落在捨入帶內 | ✅ **−0.0057**（帶 ±0.01·探針全精度） |
| 4 | 收支表之**限度**已聲明（未分離投影因子） | ✅ §二-1 |
| 5 | 歸因錯誤已更正並寫進探針輸出 | ✅ §三·探針【C】分支 |
| 6 | 附帶發現之量級**未歸因**已明載 | ✅ §四 |
| 7 | ⛔ 未寫任何判定碼·未動互斥標記 | ✅ |

---

## 七、⛔ 本批**未**查明者（留給下一手·勿誤認已結案）

1. **真G − 實配G 之量級（11.5–23 ㎡）之逐項歸因**（forced／buf／`W_prev` 各佔多少）。
2. **收支式之解析形**（含投影因子 `h`）——本批僅數值相符。
3. §二-3 **S6-1 (b) 去向**：仍**待 KL**（本批未觸）。
4. §二-1 **兩則互斥**：仍**待 KL**；本批只是把「另一種病」排除掉，
   使該案回到**單純的口徑選擇問題**（§五）。
