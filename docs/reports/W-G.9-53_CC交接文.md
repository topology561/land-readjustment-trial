# CC 新 session 交接文（倉態）｜`W-G.9-42` 〜 `W-G.9-53`

## 🔴 本文之地位

**⛔ 本文不是事實來源。** 只做兩件事：**指路**、記錄**倉內已知之壞掉處**。
凡本文提及之檔名、行號、**編號**、數字，**一律現查**。與倉內不符 ⇒ **以倉內為準**。

🔒 **編號一律標「⛔ 現查」而⛔ 不填具體數**（自誤 30 之修法：本檔一旦入倉即凍結，
而編號每批都在動 ⇒ **填了就必然過期**）。

---

## §一 三方分工

- **KL** ＝ 土地重劃域權威。所有域裁由他下。**懂市地重劃分配技術，不懂程式。**
- **CC（你）** ＝ **唯一實作者**。只在 KL 貼施工單時動工。
- **claude.ai** ＝ 獨立驗證 ＋ 規格 ＋ 裁判。⛔ 不寫生產碼。

---

## §二 開場（⛔ 先跑完再說話）

```bash
git fetch origin && git rev-parse origin/wip/s1-endpart
```

⚠️ **三個必查（皆已實測·⛔ 重複發生）**：

1. **session worktree 之起始基座**——自動建立者**起始於 `origin/main`**（距工作分支數百 commit）。
2. **KL 之 localhost 基地**（`C:/Users/admin/Desktop/land-readjustment-trial`）
   ——`W-G.9-42` 實測落後 origin **48 commit**。
3. **背景任務之「完成」通知⛔ 不等於「工作已做」**（`W-G.9-53` 實測：`nohup … &` 報 exit 0
   而輸出檔為空、log 時間戳未變）⇒ 須以**產物之時間戳／內容**驗其真實發生。

**必讀（依序·編號一律現查）**：

| # | 檔 | 是什麼 |
|---|---|---|
| 1 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | 權威序**第 1 級**。`K-9-13` 為最新 |
| 2 | `CLAUDE.md` | 權威序**第 2 級**·工作紀律／編號倉規／**守恆式** |
| 3 | `docs/reports/W-G.9-53_判準鏈定案與街角實測.md` | **最新倉態** |
| 4 | `.claude/skills/failure-archaeology/SKILL.md` | 失敗考古（⛔ 現查節數·**節 87 已累積五個實例**） |
| 5 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | GB 系列（⛔ 現查·`GB-64` 已撤銷） |
| 6 | `docs/驗證裁定登記表.md` | VR 系列（⛔ **不在權威序四級內**） |
| 7 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 對方之自誤（⛔ 現查） |

⚠️ **`K-6` 內有大量過期之實作狀態註記** ⇒ **讀到任何「已／未實作」都要現查碼。**

---

## §三 🔴 本波判準鏈之**完整結論**（最重要一節）

### 判準（`K-9-12`）

**可建築 ⟺ 宗地四至範圍內能完整容納 `W × D` 矩形。**

| 項 | 定案 |
|---|---|
| `W`／`D` | `get_min_lot_size(分區, 正面路寬)` 之 `min_width`／`min_depth` ⇒ ⛔ **不得硬編 `3.5`／`14`** |
| 姿態 | **可旋轉、可平移、⛔ 不須貼齊道路境界線**（`K-9-12-c`·KL 自陳更正） |
| 街角第 1 宗 | 🔴 **⛔ 不走本則**——沿用 `K-9-5-2`／`K-9-5-4` 之**街角最小規定範圍**（`K-9-12-e`／`K-9-13 四`） |
| 附表值之兩用途 | 【街廓層】＝ **警示**（`K-9-2`·⛔ 原封不動）／【宗地層】＝ **矩形之邊長** |

### 後果（`K-9-13`）

**不可建築 ⇒ 未達該街廓可分配標準 ⇒ 列入合併／調配機制 ⇒ 由下一投影序號之重劃前土地遞補。**
🔒 「入調配池」「⛔ 不得超配」＝ `K-9-9 四` 之援引 ＋ `CLAUDE.md` **守恆式之蘊含**
⇒ **已由正典回答·⛔ 不得再問 KL**。

### 🔴 兩套規則之關係（⛔ 此處 claude.ai 兩度誤述·以 KL 逐字為準）

> 「合格垂線是一套遊戲規則，……"可否容納最小矩形"是另一種遊戲規則，……都是要判斷可否建築，
> **26 宗的一樣可能真的只是巧合**……現在判斷方法**一律用**"宗地範圍內可否容納最小矩形"」

⇒ **受詞同一（可否建築）·規則不同·新取代舊** ⇒ **⛔ 不得認為二者等價或可互相驗證**。

### 兩側臨接（`K-9-13 二`）

**分配後宗地須<u>同時</u>臨 FRONTLINE 與 BASELINE**（立法理由：避免末端塊形成「碎塊」）。
🔒 係 `K-9-6-h ②` **前段之補齊**、⛔ 非取代 ⇒ **該款前段⛔ 不得刪**。
⚠️ **FRONTLINE 臨接係<u>新增</u>要求**——現查 `docs/rulings/` 內其**要求**命中 ＝ **0**
（BASELINE 臨接則有明文）。

---

## §四 🔴 已實測之量（母體 ＝ **3／6 街廓·30 宗**·⛔ 現查後才引用）

| 量 | 實得 |
|---|---:|
| 形狀測試母體 | **26** 宗（另 4 宗面積 < `W×D` 先分流） |
| **自由姿態可容納** | **14** 宗 |
| 不可建築（面積分流 4 ＋ 矩形不進 12） | **16** 宗 |
| 軸對齊 → 自由姿態之**翻轉** | **1** 宗（`628-21(1)`·見證 `θ ∈ [2.9870, 3.1350] rad`） |
| 街角規定範圍**本身**可容納矩形 | 🔴 **2／4**（R1/right、R6/right **不進**·皆附證書） |

🔴 **三集合完全同一（⛔ 不得推論法源關係）**：
「面積 < `MinA`」／「零條合格垂線」／「矩形不進」**在本案 26 宗上是<u>同一個 16 宗</u>**。
🔒 **成因為資料之空隙、⛔ 非規則等價**：該 16 宗面積 **0.26〜152.21**，
其餘 14 宗 **189.54〜692.05** ⇒ **`152.21` 與 `189.54` 之間有一道乾淨的空隙**，
而三個門檻（`W×D`=49.00／`MinA`=116.02〜159.28／矩形容納之有效邊界）**皆落在該空隙內**。
⇒ **換一筆資料即可分家**；二判準之**分岔反例已由合成例雙向坐實**。

🔒 **⛔ 不得與 `W-G.9-40` 之 35 宗相加**（受詞與母體皆不同）。
🔒 **⛔ 不得以 16 宗當作最終不配地數**——遞補會改變後續各宗之 G 值與四至範圍。

### 🔴 R2／R4／R5 之 29 宗**全未量**

R2／R5 ＝ `②-宗 圍堵閘破`（宗-宗重疊 45.9766／56.3293）；R4 ＝ 結構閘 理論≠實跑（Δ=2.415）。
🔒 **⛔ 不得以 spy 擷取其 `biz_polys` 充數**——其宗地**彼此重疊**、係上游狀態不合規之產物。

---

## §五 ⛔ 未實作清單（**全部**）

| 項 | 狀態 |
|---|---|
| `K-9-10`（帶底新定式） | ⛔ 未實作 |
| `K-9-11`（零條合格垂線 ⇒ 不配地＋遞補） | ⛔ 未實作 |
| `K-9-12`（矩形容納） | ⛔ 未實作 |
| `K-9-13`（後果＋兩側臨接） | ⛔ 未實作 |
| `K-9-6-h ①②`（合格垂線之定義與判準） | 🔴 **從未落地**——生產碼 `合格垂線`／`垂線容納`／`整條落在`／`K-9-6-h` **命中皆 0** |
| `K-9-6-h ⑤`（截角前形狀） | ⛔ 未實作 ⇒ 街角宗之輸入形狀仍為**截角後** |
| `app.py:6019` 之過期註解（「可建築門檻」） | ⛔ 未改（登記待實作批） |

---

## §六 未結 `GB`（⛔ 現查號·此處只列受詞）

| 受詞 | 狀態 |
|---|---|
| **平行判準之擇一** | 🗄️ **回報「可結案」**——④ 三句之下游**全部命中 0** ⇒ 已無土地後果。🔒 **⛔ CC 不逕結案** |
| **`②-宗` 破量之幾何成因** | 未查（R2／R5） |
| **正典更正只改命中點** | 無人守 |
| **判準已改而訊息字串未同步** | 無人守 |
| **`S-9(二)` 乙丙於本案同值** | ⛔ 不以本案資料問 KL |
| **騎樓分支 (a)** | ✅ 結案（KL 裁「重劃分配街廓一律僅適用一般建築基地附表」）；🔴 **(b) 靜默回退留列** |
| **剪移吃掉寬度** | 🗄️ 立即失效·保留為紀錄 |
| **半格捨入慣例** | 本案不受影響 |
| **`K-9-5-2 ③` ⇏ 矩形容納** | 🆕 本案零土地後果 |
| **「臨」之最小程度未定** | 🆕 本案零土地後果（`628-53(1)` 接觸 0.0059 m） |
| **遞補連鎖之終止性** | 🆕 未形式化·🔴 其「⛔ 不加回」前提**未經現查** |

---

## §七 🔴 下一階段 ＝ **實作與遞補實跑**之前置條件

1. 🛑 **`K-9-12` 與 `K-9-11` 之併存或取代關係**——正典逐字**未見**。
   `K-9-13 一` 已定「現行判斷方法一律用矩形容納」⇒ 可推論 `K-9-11` 之觸發前提（合格垂線）
   **已不再是可建築判準**；惟**其遞補後果是否仍適用於「矩形不進」之宗**，須確認。
2. 🛑 **遞補連鎖之終止性**（`GB-78`）——**「⛔ 不加回」之前提須先證**：
   `K-9-9 四`／`K-9-11 三` 令騰出之地**入調配池**，而調配池之消耗（七級調配）
   **可能把地配回同一街廓**。
3. 🛑 **街角走 `K-9-5-2`** ⇒ 須先實作 `K-9-6-h ⑤`（截角前形狀），否則街角宗之輸入形狀不合規。
4. 🛑 **R2／R4／R5 之三個 raise** ——不解則永遠只有 3／6 街廓可量。
5. ⚠️ **實作即動配地面積** ⇒ 依 `CLAUDE.md`，須先估影響、且**baseline 重烤**須與之協調。

---

## §八 環境與陷阱

- 🔴 **Windows／Git Bash**：CRLF 混合 ⇒ 整行錨比對兩側先 `tr -d '\r'`。
  **cp950**：python 印 emoji 會 `UnicodeEncodeError` ⇒ `export PYTHONIOENCODING=utf-8`。
- ⚠️ **heredoc 內含大量全形／emoji 之 python 易被 bash 誤剖** ⇒ 寫成 scratchpad 之 `.py` 再跑。
- 🔒 **`REAL_rc`**：⛔ 不以背景 exit code 判成敗；**背景完成通知亦不算**（見 §二-3）。
- 🔒 log 檔名須含**產生它之 commit 短碼**，⛔ 不覆寫任何既有 log。
- 🔒 `verify/out/` ⛔ 禁 `git add -A`／`.` ——逐檔 `git add`。
- ⚠️ `run_step_g` **整批呼叫會於 R2 中止** ⇒ 需逐街廓各自一個 `try`。
- 🔒 **浮點**：「等價」⛔ 不等於「逐位元相同」——重現生產碼之框須連 `numpy` 運算次序一起重現。
- 🔒 **`f3_min_alloc_area_by_label` 係 `session_state` 鍵**、⛔ 非 module 級符號 ⇒ `ns` 取不到；
  `MinA` 須自快照現算（`round(街廓分配深度_m, 2) × min_width`）。

---

## §九 本波之工具（⛔ 現查後才用）

| 工具 | 受詞 |
|---|---|
| `verify/tools/wg942_append_audit.py` | 純追加稽核（檔／段兩層·補集判準＝**嚴格前綴**·每項附判別力自檢） |
| `verify/tools/wg942_ast_zero_delta.py` | `app.py` 零可執行異動（AST·⛔ 不以 grep／awk 判） |
| `verify/probes/probe_WG941_bandbase.py` | 帶底新舊定式（已過 `C-1` 驗收閘·與生產碼逐宗差 `0.000e+00`） |
| `verify/probes/probe_WG943_parallel_recount.py` | 平行判測**三讀法並列** |
| `verify/probes/probe_WG947_rect_fit.py` | 矩形容納（**軸對齊**·侵蝕法） |
| `verify/probes/probe_WG949_free_pose.py` | **自由姿態**矩形容納（侵蝕＋**Lipschitz 證書**＋見證逐角驗證） |
| `verify/probes/probe_WG953_corner_and_sets.py` | 街角規定範圍之矩形容納 ＋ 集合對稱差 ＋ `MinA` 三向交叉 |

🔒 **證明義務之不對稱（本波之方法論核心·下一批請延用）**：
- **判「可以」** ⇒ 須具名**見證物**並**逐角／逐項獨立驗證**（⛔ 不以同一次計算當證明）。
- **判「不可以」** ⇒ 掃描找不到 **⛔ 不等於不存在**，須附**證書**（本波用 Lipschitz）。

---

## §十 編號倉規（⛔ **一律現查**·本文不填數）

```bash
grep -rho "GB-[0-9]\+" docs/ .claude/ verify/ | sed 's/GB-//' | sort -n | tail -1
grep -o "VR-[0-9]\+" docs/驗證裁定登記表.md | sort | tail -1
grep -o "自誤 [0-9]\+｜" docs/reports/W-G.9波_claude.ai側自誤登記.md | grep -o "[0-9]\+" | sort -n | tail -1
grep -o "^## [0-9]\+\." .claude/skills/failure-archaeology/SKILL.md | tail -1
grep -oE "^### 🔒 K-9-[0-9]+" docs/rulings/K-6_街角地分配程序與可分配判準.md | sort -u -V | tail -1
```

**入倉紀律**：⛔ **純追加**、⛔ 原文一字不刪、加註只給指標⛔ 不重述內容；
每次入倉須做**嚴格前綴驗證**（含判別力自檢）。同檔同批受兩種受詞層級 ⇒ **分解驗證**。
