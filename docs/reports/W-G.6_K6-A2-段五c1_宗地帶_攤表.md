# W-G.6／K-6-A2 段五(c)-1 — 宗地層量測帶（**帶底更正後**）**攤表**（⛔ 只算不換·不設門檻）

> 基準 `802798a`（commit 1）。三 commit：正典＋登記＋考古／`_n14_band_hi` 改寫／探針重跑＋本表。
> 實料：`verify/out/probe_K9_6_parcel_band.log`（入倉·**已由本批重跑覆寫**）。
> (c)-1 **前**之實料凍存於 `verify/out/probe_K9_6_parcel_band_段五a凍存.log`
> （⛔ **同名目錄陷阱**：二者檔名僅差後綴，引用前先確認引的是哪一份）。
> ⛔ **本表只鋪數字**——放行門檻由 KL 於 **(c)-2**（生產切換）前裁定。

---

## 〇、一句話

**帶底改由「較淺之後緣角點」定位後：34 筆停機全消、判定翻轉 0 筆，
且 106 宗之寬度與今日生產值之最大差為 `0.000002 m`
——`|Δ| > 法定粒度 0.01 m` 者 **0 列**。**

⇒ **GB-13 之解除（帶深自附表 `14.00` 改為逐宗 `33.13〜47.60`）在本案之土地後果為零。**

---

## 一、更正了什麼（⛔ 讀數字前先讀本節）

| | 帶底 `t_hi` | 狀態 |
|---|---|---|
| 生產中 | `min_depth` ＝ 畸零地**附表**值 `14.00` | **GB-13** 所登記之誤·**仍在生產** |
| 段五(a)(b) | `t_lo + D·(b̂n·n̂)`（前緣端點至 BASELINE 垂距之 min·沿 `b̂n` 平移） | 🗄️ **已作廢** |
| **本批(c)-1** | **該宗二個後緣角點中較淺者之 `t`** | ✅ **opt-in 已備·生產未切換** |

**作廢之由**：`K-9-6-a` 表列第 1 列之**受詞**與同檔上方逐字法規原文相反
——原文「臨接之道路境界線至該基地**後側境界線**垂直距離之最小值」，受詞為**後側境界線**；
表列誤寫為「**臨街段**每點至 BASELINE 垂距之 min」。
KL 2026-08-05 以圖示裁定受詞為後側境界線 ⇒ 量測點取**後緣角點**。
🔴 **量測對象未改**（仍量宗地多邊形之弦）——前一輪「正典與碼面構造不一致」之診斷已作廢，
教訓入**失敗考古 節 42**（`grep -n "^## 42\. " .claude/skills/failure-archaeology/SKILL.md`）。
⚠️ ⇒ 舊攤表（`docs/reports/W-G.6_K6-A2-段五ab_宗地帶_攤表.md`）之
**「寬度_新」欄與 §〇／§三 之結構性發現全部作廢**，該檔已加註。

---

## 二、母體（log `:6`〜`:12`）

**118 列**（兩情境合計·`推進側別 ∈ {left,right}`·剔除抵費地 `0m 9`／`3.5m 11`）
＝ 街角 **12**（`N/A·豁免中`·K-4 面積門檻豁免·其量測路徑屬 **GB-39**／K-9-5·**段六**）
＋ **量得 106** ＋ **raise 0**。

🔒 篩準為 `推進側別`，⛔ **非**「有無 `cut_coords`」——抵費地有真幾何卻不量寬度。

---

## 三、數字（⛔ 皆為觀測·非門檻）

🔒 **本節所引之 min/max 一律出自 log 【H】聚合段**（`:269`〜`:277`）
——⛔ **非**由讀者離線聚合後宣稱（該做法無從複驗，且本批已因此犯過一次·見 §六-附記）。

### 3-1 帶（log `:271`／`:275`）

| 量 | 值 | 出處 |
|---|---|---|
| 帶底 `t_hi` 之範圍 | **33.129955 〜 47.600848 m** | log `:271` |
| 相對附表帶深 `14.00` 之倍率 | **2.366× 〜 3.400×** | log `:271` |
| 後緣角點數 `n後緣` 之分布 | **{2, 3}**（3 者＝後緣由二段共線邊構成·2 列·皆 `R2/628-35(1)`） | log `:275` |
| 二後緣角點之 `t` 差 `\|t1−t2\|` 最大 | **0.735923 m** | log `:275` |

### 3-2 寬度（**對今日生產值之影響**·log `:276`〜）

| 量 | 值 |
|---|---|
| `max\|Δ寬度\|`（新帶 − 舊帶） | **`0.000002 m`**（`0m R5/628(3)`·log 印至 6dp） |
| `Δ寬度 ≠ 0` 之列（**全浮點精度**） | **49／106**——⚠️ 逐列列於 log `:277` 起 |
| ↳ 其中 `\|Δ\| ≤ 1e-10`（浮點層） | **47 列**（`1e-13` 2／`1e-11` 25／`1e-10` 20） |
| ↳ 其中 `\|Δ\| ~ 1e-06` | **2 列**——`0m`／`3.5m` 之 `R5/628(3)` |
| **`\|Δ寬度\| > 法定粒度 `0.01 m` 之列** | **0**（log `:276`） |
| **判定翻轉之宗** | **0**（log `:142`） |

⚠️ **「49」與「2」不衝突**：前者為**全浮點精度**之非零計數，後者為**達 1e-06 量級**者。
⛔ 勿以 6dp 顯示欄推斷非零列數——本批初稿即因此誤寫「2／106」，已更正（見 §六-附記）。

🔬 **交叉佐證（不同方法、同一結論）**：段五(c)-0 之探針以**另一條路徑**
（改量測對象為「帶 ∩ 二側無限直線」·該診斷其後被 KL 裁定作廢）
於**同一宗** `R5/628(3)` 得帶深敏感度 `2.243860e-06 m`
（`verify/out/probe_K9_6_measure_object.log:140`）。
⇒ 二個互不相同的更正路徑，指向**同一個量級最大之宗、同一量級**
——此為「本案土地後果為零」之**獨立佐證**，非同一算式抄兩遍。

### 3-3 報表量 `D`（🔴 **不進任何閘或帶**·log `:272`〜`:274`）

`D = t_hi / (b̂n·n̂)` ＝ 該後緣角點**沿 BASELINE 法向**至前緣線之距離（KL 圖示之橘色箭頭）。

| 量 | 值 | 出處 |
|---|---|---|
| `D` 之範圍 | **33.129958 〜 47.654288 m** | log `:272` |
| `\|g\| = \|d̂·b̂n\|` 最大 | **0.050035**（R6） | log `:273` |
| `(b̂n·n̂)` 最小 | **0.998747488** | log `:273` |

📌 **註腳（施工單 §三-④ 之兩讀法）**：法規原文之「垂直距離」亦可讀為 **`t_hi` 本身**
（垂直於**道路境界線**）。二者之相對差 ＝ `1/(b̂n·n̂) − 1`，**本案實測最大 `0.125408%`**
（log `:274`；施工單估「≤ 0.13%」·**實測相符**）。
K-9-3 之後 `D` 不進任何判定 ⇒ 本倉採 **KL 圖示之定義**；⛔ 本註腳不作為裁定請求。

---

## 四、§六 驗收（**可否證預言·逐宗列示於 log 【G】**）

| # | 預言 | 結果 | 錨 |
|---|---|---|---|
| **1** | 34 筆 raise 全消 ⇒ 【D】為 0 筆 | ✅ **0 筆** | log `:144` |
| **2** | 帶底落在宗地頂點上（`\|t_v − t_hi\| < 1e-12` ∧ 該點 `\|dep\| ≤ 0.01`） | ✅ **106／106** | log `:265` |
| **3** | 帶底不越過最深頂點（`t_hi ≤ max(t_v)`） | ✅ **106／106** | log `:266` |
| **4** | 對凍存 log 之 34 筆：新 `t_hi ≤` 舊 raise 訊息之 `t` | ✅ **34／34** | log `:267` |

🔒 **§六-2／3 之 `t_v` 係獨立復現**（探針內 `_ring_ts_oracle`·⛔ 非自 `_n14_band_geom` 取值）
——二者一致與否即受檢項本身；若改為呼叫同一支，該二項即成**恆真閘**（log `:155` 已聲明）。

📌 **§六-2 之附帶觀測**：帶底頂點之 `|dep|` 實測落在 `1e-06` 量級
（例 log `:157`〜`:159`：`1.443e-06`／`1.976e-06`／`1.824e-06`），
遠小於容差 `0.01 m` ⇒ **「後緣角點在 BASELINE 上」是實測事實、非靠容差撐出來的**。

### 附記：本批自身犯過一次「以顯示精度推斷全精度」

初稿之 §3-2 寫「`Δ寬度 ≠ 0` 之列 ＝ **2／106**」——該數係我**離線解析 log 之 6dp 顯示欄**
所得；探針加入【H】聚合段（以**全浮點值**計）後，實為 **49／106**。
⇒ 二者皆非錯，但**「2」回答的是「顯示得出來的有幾列」，不是所問之「有幾列非零」**。
🔒 **處置**：min/max 與計數一律改由**探針於全精度算完後印出**，
⛔ 讀者（含 CC 自己）**不得離線聚合 log 之顯示欄再宣稱**。
（同族：失敗考古 節 41(c)「欲寫任何『N 項』⇒ 先確認 N 的計數單位」。）

---

## 五、⛔ 本批未做之事（逐項聲明）

1. **生產未切換**——`grep -n -A2 "^    _w = parcel_min_width_n14" app.py` 三行內**無** `baseline_pts`。
2. **E-2′ 閘未動**（`grep -n "本宗深度 {max(_tv):.4f}m" app.py`）——新定義下該閘仍不 fire
   （`max(_tv)` 33〜48 vs `_md` 14.00），留待 **(c)-2** 與切換同批處理，避免 delta 混淆。
3. **不設門檻、不作放行建議**——放行屬 KL。
4. **街角 12 宗未量**——GB-39／K-9-5·**段六**。
5. **`_min_depth_by_block` 之餵值鏈在 `main()` 內** ⇒ (c)-2 切換時**須另附合成案**
   （`CLAUDE.md:56`·`run_all` 從不執行 `main()` 內之敘述）。
