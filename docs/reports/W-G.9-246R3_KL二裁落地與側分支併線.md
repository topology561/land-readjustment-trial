# `W-G.9-246R3`　KL 二裁之落地 ＋ `verify/W-G.9-246-c1` 併線（`2026-09-07`）

> 🔒 **取號之由**：依 `常規四（三）`（CC 對某單之報告一律「該單號 ＋ 後綴 `R`」·⛔ 另取新號）
> ⇒ 承 `W-G.9-246R2` 取 **`W-G.9-246R3`**。⛔ 取 `W-G.9-248`（已由 `-246′ §三` 意指占用）。
> 🛑 **本批⛔ 零生產碼**（僅動 `docs/`）·**鑄 `自誤 308`／`309`**·⛔ 鑄 `GB`／`VR`／戒／`K-9`。
> 🔑 **一切數字現查於倉**；三軸（來源 `commit`／母體／粒度框）逐處具名（`VR-091 一`）。

---

## §零　開工閘（接手窗·`W-G.9-246R2 §八` 四件事）

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `left-right`（`HEAD...origin/wip/s1-endpart`）| **`0	0`** | ✅ ⛔ main 舊態 |
| `2` | 硬閘 | `.claude/settings.json` ＋ `verify/tools/wg9237_heredoc_guard.py` 皆在；🟢 **活體實測**送 `cat <<'EOF'` **被當場 deny** | ✅ **⛔ 僅 `test -f`** |
| `3` | `§一`／`§二` 重跑 | 十檔 ＋ 夾具 `sha256[:16]` **逐位相符**；檔數 `1673`／`598`／`576`／`548`／`60`（較文各 `+1/+1/+1/+1/0` ＝ 交接文自身·**自洽**）| ✅ |
| `4` | `-246-c1` 進度 | `origin/verify/W-G.9-246-c1` ＝ `200dc53…`·**未放行** | ✅ |

🩸 **本窗自捕之量測器紅（`常規五`）**：首跑檔數用 `git ls-tree -r --name-only`（**未加 `-z`**），
因 `quotePath` 跳脫 CJK 而得 `.md` **`30`**／`docs/` **`16`**（真值 `598`／`576`；被跳脫列 `897`）。
⇒ 改 `-z` 並**二框並報**以坐實差源。**取母體一律 `-z`。**

---

## §一　KL 裁一之落地：`B` 之受詞 ＝ **受檢宗自身**之 `G`

### 🔒 KL 之裁（`2026-09-07`·**逐字**·二層面）

> * 物理層：街角第 1 宗 ＝ 街角地（街廓最左／最右側第 1 筆）；藍影閘一量第 2 宗（確認街角地後相對順序為第 2 宗之那筆）。
> * 碼層：第 `0` 宗 ＝ 街角地；第 `1` 宗 ＝ 該第 2 宗。

⇒ 🔒 **閘一之受詞 ＝ 受檢宗自身（物理第 2 宗 ＝ 碼側第 1 宗）之 `G`**；
**`docs/rulings/K-6:3261` 正確**；**`v3:893` 誤**；🔒 **⛔ 回改實作。**

### 🔒 落地一：`v3` 末端追加更正標記（⛔ 改 `:893` 一字）

**節名** ＝ `## 🔧 ⑪ 閘一「G(街角第1宗)」之**更正標記**（W-G.9-246R3·KL 裁 2026-09-07·⛔ 上文一字不刪·純末端追加）`
**體例** ＝ 依本檔既有二先例（`§7-4「排序」一款之更正標記`·`K-9-1 域裁六款之 ⑧⑪ 更正標記`）。
**載** ＝ KL 二層面逐字 ＋ 閘一受詞之正字 ＋ `K-6:3261` 為**上位錨**（權威序第 `1` 級 > `v3` 第 `3` 級）
＋ `v3:893` 之「`G(街角第1宗)`」**自本節起停止適用** ＋ 內部自洽佐證（`K-6:3264` 之「嚴格大於」之由繫於**受檢宗**臨接長）。

🔒 **字樣錨**（`行號衛生` 第 `4` 款·活文件⛔ 以行號為錨）
| 受詞 | 字樣錨（`grep -F`）| 命中 |
|---|---|---|
| `v3` 被更正之句 | `G(街角第1宗) > area(藍影)` | `1` |
| `K-6` 上位錨 | `G(第 1 宗) > 藍影面積` | `1` |
| `v3` ⑩ 之 `max` 臂 | `G ≥ max(街角規定範圍面積` | `1` |

🔑 **判別力自檢**：寬字樣 `街角第1宗` 於 `v3` 命中 **`3`** ⇒ 上開唯一性檢**非恆為 `1`**。

### 🔒 落地二／三：鑄 `自誤 308`／`309`

| 號 | 受詞 | 責 |
|---|---|---|
| `308` | `I-3`「`e ＝ 0` ⇒ `C` 不適用」與 `v3` ⑩ 於**街角第 1 宗互斥**（`303` 同族·**第三度**）。⑩ 之 `max` 另一臂（街角規定範圍面積）於 `e ＝ 0` 時仍給非零門檻 ⇒ `C` **仍可判**。**通則**：凡立「某條件 ⇒ 不適用」之款，須逐一對拍該項既有之**全部例外式** | **發單側** |
| `309` | 入典條文之**宗序自違**——`v3` ⑪ 同節 `:896-897` 既明定「條文一律以 KL 語為準」，而 `:893` 沿用**碼側索引**之原文。**通則**：凡條文節內宣告語序對照者，出艙前須對該節**每一處**宗序詞逐一驗；⛔ 只驗新寫之句 | **發單側** |

🔒 **CC 之處置於二則皆正確**——`-246R2` 報告 `:175`／`:244` 已當場具名該互斥並依 `v3` ⑩ 實作。
🔒 **`V-S′` 之「`X ＝ 0` 須全 `不適用`」僅對<u>非街角第 1 宗</u>成立**（`308` 之連帶）。

---

## §二　KL 裁二之落地：`verify/W-G.9-246-c1` 併線

### 🔴 推前閘：**款一／款三 不成立**（逐字出艙·⛔ 隱去）

| 款 | 單所令 | 實測 | 判 |
|---|---|---|---|
| `1` | `fetch` 後主線須仍 `d7610b0` | **`e0c3971e681eaf69ff2efb526e1a7658664365af`** | 🔴 **⛔ 成立** |
| `2` | 側須仍 `200dc53` | `200dc53896d0e3ea62d0b827022d2bfdb111c9e2` | ✅ |
| `3` | `is-ancestor(主線, 側支)` | `rc=1` | 🔴 **⛔ 可純快轉** |
| `4` | `--merges` ＝ `0`／父數 `1` | `0`／`1` | ✅ |

🔒 **款一紅之<u>唯一</u>成因** ＝ 主線於出單後多一個**零生產碼** commit（`e0c3971` ＝ `-246R2` 交接文自身·
其相對 `d7610b0` 之改動 ＝ **僅新增 `docs/reports/W-G.9-246R2_CC交接文.md` 一檔**）。

🔒 **處置之依據（倉內明文·⛔ CC 之意思決定）**
1. `W-G.9-206 §八`：主分支若於其間前進 ⇒ **rebase ＋ `--force-with-lease` 重推側分支 ＋ 重驗**。
2. `W-G.9-246R2 §八-4` 逐字：「若已放行 ⇒ **先 rebase 至當時之主線**、重推、重驗同一性閘，**再**純快轉併線。」
3. `W-G.9-246R2 §三` 逐字：「🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。」

⇒ 款一之紅係**觸發 `§八` 之條件**，⛔ 停機款。

### 🔒 rebase（**純 plumbing**·⛔ 觸工作副本·⛔ 動任何 ref）

**改動集不重疊之前置證**（`git diff --name-only` 之交集 ＝ **空**）：

| 側 | 改動 |
|---|---|
| `d7610b0..200dc53` | `M app.py`（`269`／`4`）·`M verify/stepg_pipeline.py`（`58`／`4`）|
| `d7610b0..e0c3971` | `A docs/reports/W-G.9-246R2_CC交接文.md` |

**造法**：`GIT_INDEX_FILE` 臨時 index ＋ `read-tree e0c3971` ＋ `update-index --cacheinfo`（二檔取 `200dc53` 之 blob）
＋ `write-tree` ＋ `commit-tree -p e0c3971 -F <原 message>`（`GIT_AUTHOR_*` 沿用原 commit）。
⇒ **rebased commit ＝ `29ebc141aab5e1fa49d1712dbb8322c54109497e`**（tree `f4fccefd6276d9631a6380b9db8f09da5c57b7f1`）。

### ✅ 同一性閘（`-247` 既例）——**全綠**

| 閘 | 受詞 | 實測 |
|---|---|---|
| `1` | `sha256(diff)` 二端逐位同一 | `d7610b0..200dc53` ＝ `e0c3971..29ebc14` ＝ **`b711d9ac9c86f49152a71fc7`** ✅ |
| `2` | rebased tree vs 側支 tree 之差 | **僅 `A docs/reports/W-G.9-246R2_CC交接文.md`** ✅ |
| `3` | `is-ancestor(e0c3971, 29ebc14)` | `rc=0` ✅ |
| `4` | `--merges`／父數 | `0`／`1` ✅ |
| `5` | 十檔 ＋ 夾具 | **變 `2`／不變 `8`**；夾具不變 ✅ |

🔒 **閘 `1` 之意義**：`sha256(diff)` 逐位相同 ⇒ **rebase ⛔ 改動任何內容一位元組**
⇒ KL 所放行之**內容**於 `29ebc14` 完全保全。

### ✅ 併後報（KL 所令四項·`ls-remote` 實查）

1. **主線 SHA ＝ `29ebc141aab5e1fa49d1712dbb8322c54109497e`**（`e0c3971..29ebc14`·**無 `+` 前綴 ⇒ 純快轉**）。
2. **十檔 ＝ 變 `2`／不變 `8`**：

| 檔 | 併後 `sha256[:16]` | KL 所令之期望 | 判 |
|---|---|---|---|
| `app.py` | `5693242b19d970c1` | `5693242b19d970c1` | ✅ |
| `verify/stepg_pipeline.py` | `c8d46d2e5fb79dc5` | `c8d46d2e5fb79dc5` | ✅ |
| 夾具 `verify/fixture_wf_ns_wiring.py` | `b17826a6f7b98bf5` | `b17826a6f7b98bf5`（**不變**）| ✅ |
| 其餘八檔 | 逐位同 `d7610b0` | 不變 | ✅ |

3. **`--merges` ＝ `0`**（`d7610b0..29ebc14`）。
4. **全部遠端 heads（`14` 支）**：`wip/s1-endpart` ＝ **`29ebc14`**；
   `verify/W-G.9-246-c1` ＝ **`29ebc14`**（**⛔ 刪·已依 KL 令保留**·並經 `--force-with-lease` 重推為併入之物件）；
   餘 ＝ `verify/W-G.9-198R-c1`／`-c2`／`-c3`／`-219R2-c1`／`-229-c1`／`-243-c1`／`-244-c1`／`-244-c2`／`-247-c1`／
   `main` ＝ `6cb77b7`／`wip/W-G.4-S0b-S0c` ＝ `8866454`／`claude/wg9-95z-v3-resume-hgpzfs` ＝ `a18bb57`。

🔒 **本地保全 ref**：`keep/W-G.9-246-c1-preRebase` ＝ `200dc53`（rebase 前）·
`keep/W-G.9-246-c1` ＝ `29ebc14`（併入之值）——**既例同 `keep/W-G.9-198R-c2-preRebase`**。

---

## §三　工項四：末號當場復算

**三軸**：`blob@29ebc14`／母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔**
（`615897` B／`7172` LF）／粒度框 ＝ **列框**。**框** ＝ `VR-091 四` 之定義框 ＋ 範圍框。

| 量 | 值 |
|---|---|
| 定義框列命中 | **`305`**（**含**就地加註節）|
| 範圍框 | `8` 條 |
| 相異 | **`300`** ／ `MIN 7` ／ **`MAX 307`** |
| 缺號 | **`[106]`**（已結案·永久空置·`自誤 271`）|
| 恆等式 `(MAX−MIN+1) == 相異＋缺號` | **`True`** |

⇒ 🔒 **本批取 `308` 與 `309`**，與 KL 之預算（`d7610b0` 實測 `MAX 307`）**相符**。

🔴 **與 `-246R2 §二` 之分項差（⛔ 影響取號·須具名）**：該文載「列命中 **`301`**／重咬 `3`」，
本批以**正典框**實得**列命中 `305`／重咬 `18`**。多咬 `15` 列經**逐列判讀** ＝
`## 🔧 \`自誤 N\` 之就地加註` 一族（其**⛔ 鑄號**·`戒 36`）。
🔑 **通則**：正典定義框之 `[^0-9０-９\n]*?` 允許任意非數字前綴 ⇒ **必咬就地加註節**
⇒ 凡出艙「列命中」者須**同格具名是否含加註節**；⛔ 只報一數。
🔒 **相異／`MIN`／`MAX`／缺號／恆等式／次號⛔ 受影響。**

🔒 **CC 之三誤（`alloc_dir` 錯 `90°`／自犯 `自誤 302`／`V-0` 比對框偽綠）⛔ 入 claude.ai 側自誤簿**
（KL 工項三 `4` 逐字：簿之受詞不符）——其已於 `-246R2` 報告 `§四` 逐字具名並修畢。

### 🔒 鑄號前之占用查（`常規四（七）` 宣告框·`W-G.9-243 補款⑥` 三數分列）

**三軸**：`blob@29ebc14`／母體 ＝ **全 `docs/` `576` 檔**（讀不到 `0`）／粒度框 ＝ **檔框 ＋ 列框**。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 檔框 | 列框(`=D2+D3`) | 鬆框 檔／列 | 角色 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-246R3`** | `0` | `0` | `0` | **`0`** | **`0`** | `0`／`0` | 受詢 ⇒ **未占用·可用** |
| `W-G.9-246R2` | `2` | `2` | `0` | `2` | `2` | `2`／`5` | 對照組甲（須 `≥1`）✅ |
| 一人造號（其字樣**⛔ 入倉**·`GB-147`·以代稱行之）| `0` | `0` | `0` | `0` | `0` | `0`／`0` | 對照組乙（須 `=0`）✅ |

🔒 **`自誤 308` 之裸數框得 檔 `69`／列 `137`** ——**必偽陽**（裸數 `308` 咬中任何含該三位之數）
⇒ 依 `常規四（八）二`，裸框**僅供漏框偵察**、**⛔ 為判準**；判準 ＝ **定義框之 `MAX ＝ 307`** ⇒ `308` 未鑄 ✅。

---

## §四　工項五：側分支基座之**追認**（🔴 **實測與單所載相異**）

🔒 **KL 工項三 `5` 逐字** ＝ 「側分支基座 `033f281` 追認（工項零先推所致）」。

🔴 **實測（`git log -1 --format=%P 200dc53`）：其父 ＝ `d7610b09c8aa606d52a546614dcce26e622433af`**，
**⛔ `033f281`**。`merge-base(e0c3971, 200dc53)` 亦 ＝ `d7610b0`。

🔒 **單內部之互斥**：KL 裁二之括號逐字自載「**發單側已預算：父恰為 `d7610b0`**」
⇒ 與工項三 `5` 之 `033f281` **相斥**。依 `常規二`，CC **取保守項**（＝ **以倉內現查為準**）續辦
＋ **逐字回報該互斥**；⛔ 上呈 KL。

🩸 **其真來源 ＝ 側分支自身之 commit message**（`200dc53` 逐字）：

> `🛑 側分支 verify/W-G.9-246-c1（自 033f281 起·⛔ 61e503b——單所載者係出單當時之主線，`
> `   而本批工項零已推進主線至 033f281；依「工項零優先於一切」自其起）。`

⇒ 🔴 **該敘述於其自身之 commit 上即已失準**——前窗 CC 於工項零推至 `033f281` 後，
**又推了執行報告 `d7610b0`**（`033f281` 係 `d7610b0` 之父），方 commit 側分支
⇒ **實際基座已是 `d7610b0`**，而 message 沿用了較早之敘述。

🔒 **追認之內容（以本節為準）**：**側分支 `verify/W-G.9-246-c1` 之基座 ＝ `d7610b0`**
（工項零 `033f281` ＋ 執行報告 `d7610b0` **二者皆先推**所致）。
🔒 **⛔ 追改該 commit message 一字**（commit message 不可改·且 `常規四（九）五`）。
🔒 **責 ＝ CC（前窗）** ⇒ 依 KL 工項三 `4` 之明文，**⛔ 入 claude.ai 側自誤簿**；本節即其登記載體。
🟢 **後果 ＝ 零**——基座之敘述誤⛔ 影響任何內容；同一性閘（`§二`）已逐位證明。

---

## §五　【驗收】

| # | 受詞 | 判 |
|---|---|---|
| `A` | 推前閘四款**逐款出艙**（含不成立者·⛔ 隱去）| ✅ |
| `B` | rebase 後 `sha256(diff)` 二端逐位同一 | ✅ `b711d9ac9c86f49152a71fc7` |
| `C` | 併後主線 SHA ／ 十檔 變 `2`／不變 `8` ／ 夾具不變 ／ 全部遠端 heads ／ `-246-c1` ⛔ 刪 | ✅ 四項全報 |
| `D` | `v3` 更正標記已落且**⛔ 改 `:893` 一字** | ✅ 純末端追加 |
| `E` | `自誤 308`／`309` 已鑄，且其標題形**可被定義框命中**（`常規四（九）四`）| ✅ 見 `§六` 收工閘 |
| `F` | 末號當場復算並出艙三軸 | ✅ `§三` |
| `G` | 側分支基座追認（**照實**·含與單之相異）| ✅ `§四` |
| `H` | `deletions ＝ 0`（三檔皆純追加／新增）| ✅ 見 `§六` |
| `I` | 本批**零生產碼** | ✅ 見 `§六` |
| `J` | ⛔ 依 verdict 動任何控制流／⛔ 實作 `r2`／五級（KL 四）| ✅ **本批⛔ 動一行生產碼** |

---

## §六　收工閘

（本節之數於 commit 後補實測；其框與判準先行載明·⛔ 事後改判準）

| 閘 | 判準 |
|---|---|
| `S-1` | `git diff --numstat <基座>..<本 commit>` 之**刪除欄逐列為 `0`**（`常規一 ②` 補款③ 驗法）|
| `S-2` | 生產碼判法：`git diff --name-only <前一已 push>..<本 commit> \| grep -xE 'app\.py\|verify/stepg_pipeline\.py\|verify/run_all\.py\|verify/run_verification\.py'` ⇒ **空輸出**（須逐 commit 出艙·含空）|
| `S-3` | 十檔 ＋ 夾具 `sha256[:16]` 前後**逐位相同**（本批零生產碼）|
| `S-4` | 新鑄之 `308`／`309` **可被自誤定義框命中**，且 `MAX` 由 `307` → `309`、二者⛔ 在缺號集（`常規四（九）四` 二項機檢）|
| `S-5` | `wg942_append_audit`（嚴格前綴 ＝ `True`）證 `v3` 與自誤簿**未被改寫** |

---

## §七　收工閘之**實測**（`cc9cbff` 入倉後補記·⛔ 上文一字不刪）

🩸 **對 KL「一 commit」之偏離（逐字具名·⛔ 隱去）**
KL 工項三逐字令「純文件·**一 commit**·逕推」，而本節係**第二個** commit。
🔒 **其由 ＝ 結構性自指**：收工閘 `S-1`／`S-2`／`S-4`／`S-5` 之受詞**即該 commit 自身**
（`deletions`／生產碼命中／`MAX` 之推進／嚴格前綴），**⛔ 可能在其所驗證之 commit 內完成**
——同形於 `W-G.9-194 裁四`（置後之報告將落於不可 `push` 之區間）。
🔒 **本節⛔ 改 `§六` 之判準一字**（該節已於**入倉前**先行載明·`自誤 267` 之戒）；僅補其數。
🔒 **本 commit 亦為零生產碼**（判法之空輸出見下 `S-2`）。

### 🔒 逐閘實測（`base ＝ 29ebc14` ／ `head ＝ cc9cbff`）

| 閘 | 判準（`§六` 逐字） | 實測 | 判 |
|---|---|---|---|
| `S-1` | `numstat` 刪除欄逐列為 `0` | `228/0`（本報告）·`57/0`（自誤簿）·`48/0`（`v3`）⇒ **刪除欄非 `0` 之列數 ＝ `0`** | ✅ |
| `S-2` | 生產碼判法之逐字指令 ⇒ 空輸出 | **空輸出**（`PIPESTATUS[0]` ＝ `0` ＝ `git diff` 成功／`$?` ＝ `1` ＝ `grep` 無命中·依 `GB-145` **二 rc 分報**）| ✅ |
| `S-3` | 十檔 ＋ 夾具 `sha256[:16]` 逐位相同 | **不符 `0` ／ `11`** | ✅ |
| `S-4` | `MAX 307 → 309`；`308`／`309` ⛔ 在缺號集 | 見下表 | ✅ |
| `S-5` | `wg942_append_audit`（嚴格前綴 ＝ `True`）| 自誤簿 `313716 → 315566` 列·嚴格前綴 `True`·判別力自檢 `False`·`rc=0` | ✅ |

### 🔒 `S-4` 之三軸與逐量（框 ＝ `VR-091 四` 定義框 ＋ 範圍框·母體 ＝ 登記表**單檔**·**列框**）

| 態 | B ／ LF | 列命中 | 相異 | `MAX` | 缺號 |
|---|---|---|---|---|---|
| `blob@29ebc14` | `615897` ／ `7172` | `305` | `300` | **`307`** | `[106]` |
| `blob@cc9cbff` | `619457` ／ `7229` | `307` | `302` | **`309`** | `[106]` |

🔒 **四項機檢**：① `MAX` `307`→`309` ✅／② `308`,`309` 在命中集 ✅／③ 二者⛔ 在缺號集 ✅／
④ 恆等式 `(MAX−MIN+1) == 相異＋缺號` ✅；⑤ **缺號集恆為 `[106]`**（`VR-091` 補款五）✅。
🔒 **對照組（器須紅）**：未鑄之 `310` 在命中集 ＝ **`False`** ✅ ⇒ 該檢非恆綠。

### 🔴 `S-5` 之射程界限（照實·⛔ 頂替）

🛑 **`wg942_append_audit` 之 `TARGETS`（`77` 筆／相異路徑 `62`）⛔ 涵蓋 `docs/配地計算總規格_v3.md`**
⇒ 該工具**對本批之 `v3` 追加全盲**。
🔒 **另驗（倉側 `blob`·⛔ 工作區）**：`v3` `58744` B → `61758` B（`+3014`）·
**嚴格前綴 ＝ `True`**；**判別力自檢**（人造「行中插入」變體）＝ **`False`** ✅ ⇒ 該檢非恆真。
🟡 **候發單側裁**：`v3` 是否應納入 `wg942_append_audit` 之 `TARGETS`
（其已為**末端追加體例**之檔——本檔內既有 `§7-4 排序` 與 `⑧⑪` 二更正標記先例）。
**本節⛔ 就此作成條文、⛔ 逕改該工具。**

### 🟢 收官前置（`未 push 不得報收官`）

`git ls-remote origin refs/heads/wip/s1-endpart` ＝ **`cc9cbffa3b91771621236915d6b1e3b170c209ce`**
（本節之 commit 另計·其 SHA 見本檔 commit message）。
