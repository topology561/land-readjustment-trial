# W-G.8 D-2b-25 commit C：**實際寫入 ＋ 逐段對帳**（⛔ 全程零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**基準**：`78079fa`
**對照**：commit A ＝ `d2015d8`（逐字轉錄清單·尚未寫入正典）

---

## §9-6 生產碼零觸自證（**原樣貼**）

```
$ git diff --stat -- app.py verify/
```
（**無輸出**——即該指令對 `app.py`／`verify/` 之變更為**空**。）

---

## §10 時序（機械證明）

```
$ git merge-base --is-ancestor d2015d8 <commit C>
rc=0
```

⇒ **commit A（清單）確實早於 commit C（寫入）**。

---

## 一、逐段對帳（A 之文字 vs C 之落地文字·⛔ 不得有靜默改寫）

### 1-1 §1 八條裁示 —— **機械對拍 `rc=0`**

```
A：sed -n '/^> \*\*【裁】1\./,/^> 事後寬度驗收仍用/p' <A報告> | sed 's/^>[[:space:]]*//' | sed '/^$/d'
C：sed -n '/^\*\*【裁】1\./,/^事後寬度驗收仍用/p'     <K-6正典>  | sed '/^$/d'
diff ⇒ rc=0（A 19 行／C 19 行·逐字相同）
```

⇒ **八條逐字一致、⛔ 無一字改寫**。落地位置：
`grep -n "^### 🔒 K-9-5-9" docs/rulings/K-6_街角地分配程序與可分配判準.md` ⇒ **`:1595`**。
編號取 `K-9-5-9`（現查 `K-9-5-1`〜`K-9-5-8` 已占用 ⇒ 下一未占用號·⛔ 未改編任何既有編號）。

### 1-2 §3 `N0-20` 作廢加註 —— **原句與更正註同時存在**

`grep -n "往街廓內量 W" docs/specs/W-G.4_規格v3補丁七_N0-20幾何定義正典.md` **一次同時命中**：

| 行 | 內容 |
|---|---|
| `:18` | **原句**（`量法（KL 裁三）＝自末端 ALLOCLINE 之 mp 往街廓內量 W`）·⛔ **一字未刪** |
| `:20` | **更正註**（`🔴 【作廢·勿復原】上款之量法句「自末端 ALLOCLINE 之 mp 往街廓內量 W」…`） |

⇒ **§9-2 滿足**。純加性：`20` 增 `0` 刪。

### 1-3 §4 缺陷登記 —— `GB-53`〜`GB-57`（登記表 **52 → 57** 列）

**每項之 grep 錨於 `78079fa` 上實際命中**（⛔ 非只寫行號）：

| 編號 | 錨（內容 grep） | 實際命中 |
|---|---|---|
| `GB-53` | `grep -c "def _end_band" app.py` | **1**（`:9510`；docstring `:9514` 逐字「⊥垂距」） |
| | `grep -c "保證不了寬度" app.py` | **2**（`:10108`／`:10940`） |
| `GB-54` | `grep -c "抵費地計算失敗" app.py` ／ `… verify/stepg_pipeline.py` | **1**（`app.py:19962` `st.warning`）／**1**（`stepg:987` `raise`） |
| `GB-55` | `grep -c "biz_x += float" app.py` | **1** |
| `GB-56` | `grep -c "g_rows.append" verify/stepg_pipeline.py` | **2**（首處 `:957`·在池呼叫**之前**） |
| `GB-57` | `grep -c "near_dir" app.py` | **24**（報告載 `25` ⇒ 已更正） |

### 1-4 §4-e 之附帶文件更正 —— **原句不刪**

`docs/reports/W-G.8_D-2b-23C_甲後逐格對帳.md:224` 之原句「（本樹為 25）」**保留**，
其後加註「🔴 具名更正（CC 2026-08-10·`D-2b-25` §4-e·⛔ 上句一字不刪）：……正確值為 `24`」。
🔒 **不影響任何幾何或土地結論**——`D-2b-23` 之靶與逐格對帳皆不繫於該計數；
「pre-B 樹之 `near_dir` 命中為 **0**」（＝該樹確為甲前之碼）**不受影響**。

### 1-5 §5 開波文件 —— `docs/W-G.9_第2宗最小規定範圍_範圍界定.md`

**波名 `W-G.9`** 之依據與不相撞證明見該檔卷首（`grep -rn "W-G\.9" .` 排除巢狀 worktree ⇒ 0 命中）。
內容**含且僅含**施工單 §5 所列五項；其第 3 點之三項現況**由 CC 自己之 AST 輸出佐證**
（§9-4）：定義 **1** 處（`app.py:10398`）／`app.py` 內呼叫 **0**／`_WF_NS_NAMES` 長度 **25** 且不含。
§2-1 之判讀已標「**claude.ai 現查所得·待實測坐實**」、⛔ 未列為事實。

### 1-6 §6 失敗考古 —— **節 59**（`:1572`）

八條逐條可辨識；**共同根因**與**機械修法**逐字入檔。純加性：`43` 增 `0` 刪。

---

## 二、🛑 §2 圖證：**未辦**（⛔ 非 CC 之選擇·理由逐字）

**現查**：本次訊息**未附任何圖檔**；`figures/`、`docs/figures/` **皆不存在**。

⇒ 依 `CLAUDE.md`「🔒 **施工單必須自足**」之**判準（給 CC）**逐字：
「收到『將**本訊息**之 X 入倉』而單內查無 X 時，⛔ **禁編造、禁以倉內近似物代替**；
**照實回報『單內無 X』並請補**」（同族：失敗考古**節 52**）。

**未辦之範圍**（逐項）：
1. 三張示意圖入 `figures/`；
2. §1-5／§1-6 之**行內註檔名**；
3. `P0`／`Ps`／`S1` 定義文字之**差額補寫**。

⚠️ **併記（供補送時省一步）**：`P0`／`Ps`／`S1` 之定義**正典已載**於 `K-9-5-4`
（`S1 = max(線段 P0–Ps, 退縮 ＋ 畸零地最小寬)`／`P0` ＝ FRONTLINE 與 SIDELINE 之未截角理論交點／
`Ps` ＝ FRONTLINE 與道路截角線段之交點）⇒ **已載者不得重寫**（施工單 §2 逐字），
補送時**只需補未載之差額**。
🔒 該待補狀態**已寫入正典本文**（`K-9-5-9` 節末之 🛑 段），⇒ 後人讀正典即見，
⛔ 不必回頭翻報告。

---

## 三、§8 停機條件逐項

| # | 條件 | 現查 | 判 |
|---|---|---|---|
| 1 | §1 之任一裁示於倉內查得**更晚之相反裁示** | `grep -rn "KL 裁 2026-08-1[1-9]\|KL 裁 2026-09" docs/rulings/` ⇒ **0** | ❌ 未觸發 |
| 2 | §3 之更正使既有已通過之閘或既有實作與正典衝突 | 該更正**明文承認** `_end_band` 與更正後正典不一致，並**登記為 `GB-53`** 而非改碼；⛔ 未動任何閘 | ❌ 未觸發 |
| 3 | §4 之任一缺陷**倉內已有同一項登記** | `grep -c "st.warning" <登記表>` ⇒ **0**；現有 `GB-40`〜`GB-52` 無同項 | ❌ 未觸發 |
| 4 | 為完成本單而需修改任何生產碼 | §9-6 之輸出為空 | ❌ 未觸發 |

---

## 四、§7 不授權逐項自檢

| # | 項 | 狀態 |
|---|---|---|
| 1 | `app.py`／`verify/**` 一字不動（含註解） | ✅（§9-6 空輸出） |
| 2 | 不接線 `k97_solve_alloc_t`／`_block_partition`／不加入 `_WF_NS_NAMES` | ✅ 未動 |
| 3 | 不改任何閘之寬度或處置、不刪除／停用／標作廢任何既有閘 | ✅ 未動 |
| 4 | 不重烤 baseline、不設 `WV_BAKE`、不改期望 FAIL 名單 | ✅ 未動（`git diff` 該名單 ⇒ 0 行） |
| 5 | 不於本單實作 §5 之任何一項 | ✅ 僅建範圍界定文件 |
| 6 | 不刪除既有正典文字 | ✅ §3／§4-e 皆採「加註·原句不刪」 |

---

## 五、異動清單（commit C）

| 檔 | 異動 | 純加性？ |
|---|---|---|
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | 新增 `K-9-5-9`（八條逐字）＋ 圖證待補段 | ✅ `48` 增 `0` 刪 |
| `docs/specs/W-G.4_規格v3補丁七_N0-20幾何定義正典.md` | `:18` 之後加「作廢·勿復原」註 | ✅ `20` 增 `0` 刪 |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 新增 `GB-53`〜`GB-57`（52→57 列） | ✅ 增列 |
| `docs/reports/W-G.8_D-2b-23C_甲後逐格對帳.md` | `:224` 之後加具名更正註 | ✅ 原句不刪 |
| `docs/W-G.9_第2宗最小規定範圍_範圍界定.md` | 新增（開波範圍界定） | 新檔 |
| `.claude/skills/failure-archaeology/SKILL.md` | 新增節 **59** | ✅ `43` 增 `0` 刪 |
| `docs/reports/W-G.8_D-2b-25C_寫入與對帳.md` | 新增（本檔） | 新檔 |

⛔ `app.py`／`verify/**` **零觸**。⛔ 期望 FAIL 名單零變更。⛔ 未重烤任何 baseline。

---

## 六、未完成（⛔ 逐字·不以「已處理」概括）

1. 🛑 **§2 圖證未辦**——訊息未附圖檔（§二）。**待 KL 補送後另批處理。**
2. 🛑 **`K-9-5-9`【裁】1 之未裁事項仍未裁**：「本波是否因它停機」「D-3 重烤時點」
   ——正典逐字「⛔ 二者**不得由『另案』推定**，須另行請裁」。
3. **`GB-53`〜`GB-56`** 皆為**已登記未修**（本批零生產碼）；其中 `GB-53`／`GB-54`／`GB-56`
   標「🛑 需域裁」（時點與範圍未裁）。
