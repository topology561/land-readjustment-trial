# 🧾 `W-G.9-155`　**CC 交接文**（新 session 接手用·`W-G.9-153′R` 收束時出）

**出文窗**：CC（本 session context 逼近上限）　**倉態基座**：`fa991ff51675e903fafa5605f9ba9cb5711ea132`
**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ 零登記

---

## §一　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（現查 `674` 列／`62233` B／`sha16 a38e2954c6e0ab9d`）。
   🔴 **末端已增三批之常規**：`常規六`／`常規七`（`W-G.9-152`）、`常規四之補款（七）（八）`（`W-G.9-153′`）。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （現查 `3584` 列／`255033` B／`sha16 8a3000d700c58e14`·`grep -c "^#"` ⇒ **`191`**）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判「是否為本受詞之規格」，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**（每張施工單皆同）：
   `git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNN` ⇒ `cd` ⇒ `git config core.autocrlf false`
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、**⛔ 不得放 scratchpad**；跑畢**整份刪除**。

---

## §二　倉態（態 ＝ `blob@fa991ff`·CC 現查·量測時點 `2026-08-27`）

| 項 | 值 |
|---|---|
| `origin/wip/s1-endpart` | `fa991ff51675e903fafa5605f9ba9cb5711ea132` |
| `app.py` blob | **`b4cc30835feaad03dfc37f394a669bbb45988b8b`**（`22612` 列／`1333202` B／`sha16 1f2ae7c0048427c0`） |
| `CLAUDE.md` blob | `e6e96c2e9bee99ab71fba1987fb8ba80488ee0fe`（`674`／`62233`／`a38e2954c6e0ab9d`） |
| `K-6` 正典 blob | `aacf2a745e74455625936c7ff85b02aa0c525c82`（`3584`／`255033`／`8a3000d700c58e14`） |
| `docs/驗證裁定登記表.md` blob | `93952cbf47b5e6933784d879078693165e416b39` |
| `W-G.4_泛用阻塞項登記表.md` blob | `2ff511f1edf55768cc82cc18baee0ecb3e6dcb44` |
| 自誤登記表 blob | `83d159d599ef1576dfdb4f7403544715d9a0e54f` |

🔴 **`app.py` 自 `W-G.9-136`（`a2c4b2b`）起<u>一字未動</u>**——`-143`〜`-153′` 全部批次皆 ⛔ 零生產碼。

### 🔒 取號現查（依**常規四（八）**·一律用**定義列式**·⛔ 不用裸框）

| 類 | 定義列式（逐字） | 相異 | **MAX** |
|---|---|---|---|
| 自誤 | `^###\s*自誤\s*\`?([0-9]{1,4})` | `58` | **`192`** |
| `VR` | `^##\s*\`VR-([0-9]{1,4})\`` | `60` | **`74`** |
| `K-9` 主號 | `^###\s*(?:🔒\|\s)*K-9-([0-9]+)` | `21` | **`23`** |
| `GB` | 三框聯集（定義列／`^## \`GB-N\``／`^### \`GB-N\``） | `107` | **`109`**（缺號 `[12, 87]`） |
| 常規 | `^\*\*常規X（` | 命中 `9` 列 | **七**（另有常規四之補款 `七`／`八`） |
| `docs/reports` 之 `W-G.9-NNN` | 檔名式 | — | **`155`**（＝本檔） |

🛑 **裸框⛔ 不得用於取號**（常規四（八））——已二度坐實偽陽性：自誤裸框得 `298`（真值 `190`）、`VR` 裸框得 `999`（真值 `72`）。裸框**僅**用於「意指占用」之搜尋。

---

## §三　本波之位置

| 案 | 受詞 | 狀態 |
|---|---|---|
| 丙 | `K-6 §一` 幾何連通判定 | ✅ 已落地（`W-G.9-131`·`f4c5385`·零接線） |
| 乙 | `K-6 §二` 步驟 0 | ✅ 已落地（`W-G.9-136`·`a2c4b2b`·**有土地後果**） |
| — | `K-9-21`／`K-9-22`／`K-9-23` | ✅ 已入正典·🔴 **`K-9-22`／`K-9-23` ⛔ 未落地** |
| — | `VR-074`（ghost ⛔ 不入投影序母體） | ✅ 已入登記表（`W-G.9-152`）·🔴 **⛔ 未落地** |
| — | 末端塊門檻 | ✅ **KL 已裁「先沿用 `R_end`」**（`W-G.9-154R`）·**碼面已實作** |
| 甲 | `K-6-A2` 剩餘 | ⬜ 未開工（`K-9-1`／`K-9-3`／`K-9-5`／`K-9-6` 宗地層／`K-9-8` 之 `GB-18`／`K-9-5-2` ①⑤） |
| — | `K-9-16`／`K-9-17`／`K-9-12` | 🔶 量測全完成（`-138`〜`-142`）·🔴 **⛔ 未落地** |

🔒 **`W-G.9-138`〜`-142`／`-146`／`-151` 皆⛔ 不在倉**（純量測·零 commit 或未執行）⇒ 編號不連續係設計如此。

### 🔑 `-147`〜`-154` 之結論鏈（**下一單之全部前提**）

1. 🔴 **`GB-105` 之「`4` 處」嚴重不足** ⇒ `_projection_order` 之**生產鏈呼叫點實為 `12` 處**（漏 `verify/selection_pipeline.py:339` ＋ `wf_f0`／`f1`／`f2`／`f3`／`f4` **七處**）。漏因 ＝ 把 `verify/` 當測試，而 `CLAUDE.md §7` 定 `verify/wf_f0~f4` **即引擎**。
2. 🔴 **母體分 `5` 類·受詞互異** ⇒ **⛔ 無單一 `BASE`**（`def _proj_order` 於四支引擎**各自定義**）⇒ `W-G.9-15X` 落地**須先建 `BASE`，其本身即生產碼變更**。
3. 🔴 **ghost 有位次後果**：`R1` `n−r=4`／`R4` `n−r=1`（合計 **`5`** 筆真實宗地位次改變）；二街廓之**第 1 位皆二態相同**，而**「下一位」恰為 ghost 本身** ⇒ **遞補必然落空**。
4. ✅ **`VR-074` 已裁**（claude.ai 側·**⛔ 非 KL 域裁**）：ghost ⛔ 不入投影序母體；**落地拘束**＝判準須綁 `K-9-19 一` 之**三判準合取**（`原地號=='_GHOST'` ∧ `G==0` ∧ `a 面積==0`），🛑 ⛔ **不得綁 `_is_ghost_sliver`**（`K-6:3106` 逐字：於 `g_row` 層**恆假**）、⛔ 不得綁名稱。**附證偽條件**。
5. ✅ **末端塊已裁**：KL `2026-08-27` 逐字「既有 `R_end` 門檻既已落地，就**先**沿用 `R_end` 門檻」⇒ 門檻維持 **`G >= area(R_end)`**、**藍影式 ⛔ 不落地於末端塊**、`K-9-23` 射程⛔ 未擴張。⚠️ **「先」字 ⇒ 本裁具暫時性**。
6. 🔓 **`K-9-23 五` 八格表之母體<u>充足</u>**（末端塊零格⛔ 非缺口）⇒ 得逕作主批之驗收預測閘；⚠️ **`GB-109` ⛔ 未解除**（**母體充足** 與 **輸入可重算** 係二事）。

---

## §四　下一單之受詞：**落地單**（幾何影響批）

🛑 屬**幾何影響批**，須**另開新窗**（本波自 `-143` 起 `11` 批皆⛔ 零生產碼）。

### 落地之三項硬拘束（`-147`〜`-149` 實測）

1. **⛔ 無單一 `BASE`** ⇒ 須先建；`12` 呼叫點**皆須納入**；不變式須係**導出關係式**（實參 ≡ 濾式(`BASE`)）、**⛔ 非相等式**。
2. **ghost 判準**⛔ 不得綁 `_is_ghost_sliver` 或名稱（見 `§三-4`）。
3. `verify/stepg_pipeline.py` 之**結構閘口徑為「重排前」**，`K-9-17 三【更正】` 已預告落地後**必然轉紅** ⇒ **須同批處置**。

### 落地接點（⛔ 行號須重查·`grep` 現查）

- `_projection_order` 生產鏈 `12` 處（清單見 `GB-105` 之就地加註·`W-G.4_泛用阻塞項登記表.md`）
- `_proj_order` 之四份複製：`wf_f0.py:251`／`wf_f2.py:91`／`wf_f3.py:74`／`wf_f4.py:1039`
  ⚠️ **`wf_f0` 與其餘三份實質分歧**（傳完整 `tp` vs 只傳二鍵），惟 `_proj_of` **只讀 `polygon_coords`** ⇒ 實測 `6/6` 等價。
  🔴 **危害**：若 `_proj_of` 日後改讀第二個鍵，**五份即刻分家**，且其 `except` 回 `0.0` ⇒ **靜默改序·⛔ 不會 raise**。

---

## §五　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104` 之 loud 斷言（`_block_strip` 雙線⛔ 無交叉閘） | ⬜ 未辦 |
| `2` | `GB-107`（`verify/tools/wg960_z_selfcheck.py:12` 寫死絕對路徑·**該 worktree 已不存在**） | ⬜ 未修 |
| `3` | `GB-108`（`K-9-23-a` 八格表對「`σ` 由形心導出」**⛔ 無判別力**·**驗收力**阻塞） | ⬜ 未辦 |
| `4` | `GB-109`（驗收預測閘之**輸入**不可重算·`W-G.9-142` 不在倉） | ⬜ **未解除** |
| `5` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `6` | 九份 baseline 重產（**須 KL 決時點**·建議俟二閘落地後） | ⬜ |
| `7` | `K-9-22` 落地（原地號保留 ＋ 區段回退） | ⬜ |

---

## §六　本波所立之家法與陷阱（🛑 接手者必讀）

| # | 家法／陷阱 | 出處 |
|---|---|---|
| `1` | 🛑 **交付塊之逐字一律自<u>檔案管線</u>產生**（`sed -n` 之輸出）；🔴 **三數相符⛔ 不保證內容相符**——三數係另行自檔案計得、與所寫文字**無因果** | **常規七**（`W-G.9-152`）·CC 自捕 |
| `2` | 🛑 **取號一律用<u>定義列式</u>，⛔ 不用裸框**；裸框僅用於「意指占用」之搜尋 | **常規四（八）** |
| `3` | 🛑 **占用之受詞 ＝ <u>意指</u>，⛔ 非實體**——含**尚未入倉**者（停機 payload／交接文待鑄項／**已發未執行之單**）。取號須**同格報二事** | **常規四（七）** |
| `4` | 🛑 **倉為 public·後續窗自行 clone 現查**；⚠️ **clone ⛔ 非【倉】之替代**（斷言仍須 CC 現查） | **常規六** |
| `5` | 🛑 **受詞不可現查者，報「不可取得」＋框＋對照組並<u>續辦</u>**，⛔ 不停機、⛔ 不以他物代替、⛔ 不推定其內容 | `V-0c`（`W-G.9-150′`） |
| `6` | 🛑 **位置參數之對應亦須 spy 實收**——`_spatial_order_parcels_v2` 之第 `2` 位係 **`d_hat`**，⛔ 非 `front_line_p1`（CC 曾由呼叫名推定位置而得無效輸出） | `W-G.9-149R` 自捕 |
| `7` | 🛑 **量測器紅⛔ 不得上呈**——判 blockquote 須先 `lstrip()`（縮排之 `>` 會漏）；含 emoji 之工具一律先設 `PYTHONIOENCODING=utf-8` | `W-G.9-150′R`／**常規五** |
| `8` | 🛑 **凡斷言二量「非同量」者**，須擇一：① 實測其值（附逐格數與容差）；或 ② 明標 `⚠️ 僅定義層之相異·值未量` | `自誤 190` |
| `9` | 🛑 **⛔ 不得以單一錨之倒塌推定命題回到【查無】**——須同格窮舉該命題之**全部已知錨**並逐一標其存否 | `自誤 189`（末端塊錨自 `:1022` 更正為 `:1026` 即其實例） |
| `10` | 🛑 **新探針入倉前必過 `run_all.py:73` 之禁絕對路徑閘**（樣式 `C:/`／`C:\`／`/Users/`／`Desktop/land`）⇒ `REPO` 由 `__file__` 推導；log 檔名綁**基座**⛔ 非 `HEAD` | `W-G.9-147R` |
| `11` | 🛑 **`git add` 具名逐檔**，⛔ 不用 `-A`；暫存檔／探針／log **一律寫在 clone 之外**（探針本體除外·其入 `verify/probes/`） | `a47e758` 之覆轍 |
| `12` | 🛑 **bash 之反引號插值會吃掉 `python -c` 內含反引號之 CJK 字串**（已三度踩）⇒ **一律用 `Write` 落腳本再跑** | 本波三度 |
| `13` | 🛑 **`grep -c` 得 `0` 時退出碼為 `1`** ⇒ `&&` 鏈會斷；命中 `0` 之宣稱須附**對照組** | 常規四-六 |

---

## §七　已知 harness 限制

- 🔴 **`run_step_g` 於 `R2` 中止**（`②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 2.8906`）⇒ **到不了 `R3`〜`R6`**。
- 🔴 **`app.py:18913` 在 `main()` 內** ⇒ 依 `CLAUDE.md` 逐字「`main()` 內之敘述**從不被 `run_all` 執行**」。
- 🔧 **管線之最短驅動**（本波三支探針所用·可直接複製）：
  `rv.load_snapshot()` → `harvest()` → `rv.build_pipeline` → `rv.build_param_table` → `build_ownership` → `build_build_parcels` → `run_corner_pk` → `run_step_g`。
- 🔧 **攔 `ns["_projection_order"]` 亦攔得到 `app.py` 內部直呼**——因 `ns is _projection_order.__globals__` ＝ `True`（**須當場自證**）。
- 🔧 呼叫端定位用 `inspect.stack()[1]`；`app.py` 經 harvest exec ⇒ 檔名顯示為 `<app.py:harvest>`。
- ⚠️ `run_all`／`run_verification` 須於**拋棄式 clone** 內跑（會改寫 `verify/out/**` 之已追蹤 log·`GB-94`）。

---

## §八　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**：`wg9152-endblock-already-ruled`（**本波主線·含 `-152`〜`-153′` 之全部結論**）／
`wg9150-endblock-otherform`／`wg9149-k919-scope-projorder`／`wg9148-ghost-rank-consequence`／
`wg9147-projorder-callsites`／`push-before-shipping-claim`（🔒 **未 push 不得報收官**）／
`grep-multibyte-charclass-trap`（量測器靜默給錯數之三十七面）／`run-all-side-effects`／
`harness-worktree-crlf-trap`。

---

## §九　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值。
- 🔒 **拋棄式 clone 跑畢整份刪除**，並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push——① 零生產碼變更 ② 純新增檔（或既有檔之**純附加**·`deletions = 0`）③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`（期望 `rc=0`·`stderr` `0` B·逐字「結論：✅ 全部成立」·受檢 `77` 筆）。
