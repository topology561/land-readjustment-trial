# claude.ai 新視窗交接文（`W-G.9-32` 〜 `W-G.9-34`＋在途之 `W-G.9-35`）

## 🔴 本文之地位

**⛔ 本文不是事實來源。** 它只做兩件事：**指路**，以及記錄**你自己的偏誤**。

凡本文提及之倉內內容（檔名、編號、數字、結論），**一律現查**。
若本文與倉內不符 —— **以倉內為準**，並把差異寫進第一批之報告。

🔒 上一個你在本窗犯了 **5 次**（見 §四），其中 **3 則尚未入倉**——把它們入倉是你第一批的必辦事項。

---

## §一 你是誰

三方分工（⛔ 不得互代）：

- **KL** ＝ 土地重劃域權威。所有域裁由他下，UI 由他操作。**懂市地重劃分配技術，不懂程式。**
- **CC（Claude Code）** ＝ 唯一實作者。只在 KL 貼施工單時動工。
- **你（claude.ai）** ＝ 獨立驗證 ＋ 規格 ＋ 裁判。🔴 **⛔ 你不寫生產碼。**

**你的產出只有三種**：施工單／對 CC 回報之裁決／呈 KL 之白話稿。

---

## §二 開場（⛔ 先跑完再說話）

```bash
git clone --branch wip/s1-endpart https://github.com/topology561/land-readjustment-trial.git
cd land-readjustment-trial && git rev-parse --short HEAD
pip install --break-system-packages "shapely==2.1.2" ezdxf streamlit pandas numpy openpyxl xlrd plotly
```

**換窗時之 `origin/wip/s1-endpart` ＝ `cd78097`**（⚠️ 現查·很可能已前進——`W-G.9-35` 在途）。

**必讀（依序·⛔ 不可跳）**：

| # | 檔 | 是什麼 |
|---|---|---|
| 1 | `docs/reports/W-G.9-34_更正入倉與深度量法掛號.md` | CC 所撰·**最新倉態**·含丙組全部現查實得 |
| 2 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **你歷來之自誤**·倉內現有 **8 則** ＋ 三個機械修法 |
| 3 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | 權威序**第 1 級**。**新增 `K-9-6-h`（KL 裁 2026-08-15·深度量法五款）** |
| 4 | `CLAUDE.md` | 權威序**第 2 級**·工作紀律／編號倉規／未 push 不得報收官 |
| 5 | `.claude/skills/failure-archaeology/SKILL.md` | 失敗考古（現有 **83 節**·第 83 節係本窗所立、記你的） |
| 6 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | GB 系列（現最大 **`GB-67`**）。⚠️ **`GB-64` 已撤銷**（你造成的） |
| 7 | `docs/驗證裁定登記表.md` | VR 系列（現 **13 則**）。⚠️ **⛔ 不在權威序四級之內** |
| 8 | `docs/reports/W-G.9波_新session交接文_WG913-WG931.md` | 較舊之 CC 倉態文·**檔末有兩批加註** |

⚠️ **注意 #3**：`K-6` 內有大量**過期之實作狀態註記**（例如「第 2 宗近側界仍 ∥ALLOC」）。
`W-G.9-34` 已就地加註，但**原句一字未刪**（倉規）。⇒ **讀到任何「現行碼尚未……」都要現查**。

---

## §三 現在卡在哪（⛔ 現查後覆核）

### 🔴 A. 一張施工單在途 —— **⛔ 不得重發、⛔ 不得預先做它的工作**

`W-G.9-35`（暫定批號·⛔ 現查）：**「破量受詞之世代校正 ＋ 現況 log 入倉」**。
已交 KL，可能已在 CC 手上。**你的第一件事是等 CC 回報**，⛔ 不是重寫它。
其四項預測為：時序 (a)<(b)<(c)／宗0×宗1 判「減」／相鄰對合計 ＝ 0／殘差 ≤ 0.0001。

### B. 已裁未實作

**`K-9-6-h`（KL 裁 2026-08-15·五款·canonical）** 已逐字入 `K-6`，**⛔ 尚未實作**。
碼面缺口（`W-G.9-34` 丙組實得·⛔ 現查覆核）：

| 款 | 碼面現況 |
|---|---|
| ① 沿垂直正街量 | **軸別已對**（現行帶底即 FRONT 法向） |
| ① 垂線須整條落在宗內 | **未做** |
| ② 臨後側須有合格垂線 | **只檢查有無交點** |
| ③ 寬度於最小深度範圍內算 | **已是現況** |
| ⑤ 街角第 1 宗用截角前形狀 | **未做**（現收截角後） |

⚠️ ⑤ 與 ② 會**改變哪些宗合格** ⇒ **真的土地後果** ⇒ 動碼前須先估。

### C. 三則阻塞項未結

- **`GB-65`**：`K-9-6-h ④` 之平行判準有**兩讀法**（單位向量分量比 1e-3 ／ 夾角上界），未擇一。
  🔒 **已否證而不得再列入者**：以**原始 Δ 分量**比 1e-3——KL 之座標檔中後側線同一直線之三段 Δ 互異。
  🔒 讀法甲之角度容差**非等向**（軸向較嚴、45° 較寬，差約 √2 倍）。
  ⚠️ **先以資料判**：兩讀法若對六街廓分類相同 ⇒ 記「不影響」，⛔ 不上呈 KL。
- **`GB-66`**：④ 之平行／不平行係**分岔閘**，兩側連續性未證。
- **`GB-67`**：`②-宗` 破量之成因 ＝ **未知**。新受詞：**不相鄰**之宗之間為何會重疊。

### D. 破量之世代對比（本窗所裁·⛔ 現查覆核）

| 出處 | R2 之三項（宗0×宗3／宗0×宗2／宗0×宗1） | 世代 |
|---|---|---|
| `verify/out/probe_D2b17_dual.log` | `46.0539`／`25.2873`／**`4.9205`** | `dca0a85` 08-09 13:14 |
| `W-G.9-29` 報告表列（現況） | `26.1624`／`19.8142`／**（無）** | `ed598a2` 08-14 09:45 |

`near_dir` 落地於 `79bf2d7` **08-09 18:37** ⇒ 舊 log 早於落地、報告表晚於落地。

🔒 **裁定**：宗0×宗1 由 `4.9205` → `0` ＝ `near_dir` 落地之效果；
`宗0×宗2`／`宗0×宗3` **仍在** ⇒ **「同一界面雙定義」解釋不了剩下的 `45.9766`**。
⇒ 舊歸因撤回**成立**。⛔ 此裁定尚在 `W-G.9-35` 之施工單中，**未入倉**。

🔴 **根因**：`W-G.9-29` 跑探針覆寫追蹤 log 後以 `git checkout` **還原** ⇒ 新世代之原始輸出**從未入倉**。
⇒ 違反「報告所引之 log 須入倉」。已列入 `W-G.9-35` 之 A-4。

---

## §四 🔴 你自己會犯的錯（本窗實錄·⛔ 不摘要成通則）

倉內已有 **8 則**（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）。以下 **3 則尚未入倉**。

### 🛑 未入倉之三則 ——**你第一批必須把它們入倉**

**自誤 9｜施工單內兩句話互斥（`W-G.9-34` 施工單 `C-4(b)` vs `Q4`）**
`C-4(b)` 令算「正街**法向**與垂線之內積」，`Q4` 卻預期「＝ 0」。
法向 ∥ 垂線 ⇒ 內積恆為 ±1；得 0 者為正街**方向**·垂線。
CC 認出停機條件之受詞是「垂直性不成立」而垂直性成立 ⇒ **未停在假紅燈上**。
🔒 **這是考古 79／81 之同族，你第三次犯。** 出單前自問：**這份單裡有沒有兩句話，照 A 辦會違反 B？**

**自誤 10｜逐字區塊裡寫了一個沒現查的檔名**
單寫半形底線 `_`，實體為**全形底線 `＿`**（U+FF3F·位元組 `EF BC BF`）。
🔒 **考古 81 之規則你自己寫過又自己違反**：逐字區塊 ⛔ 不得含任何須現查之值，
**檔名也是須現查之值**。改用佔位符 `<…>`，現查式寫在區塊外。

**自誤 11｜受詞未綁時間層**
單寫「宗0×宗1……實測亦無」——該陳述**事實上為真**（見 §三-D），
但**沒標明它屬於哪一個量測世代**。倉內有兩份互斥之量測 ⇒ 不標世代即留下陷阱，
CC 因而據**舊 log** 判「前提為偽」。
🔒 **凡引用量測值，必同時載明其產生之 commit 與時刻。** ⛔ 「倉內有這個數」不等於「現況是這個數」。

### 倉內已載之兩則（本窗所犯·⛔ 不重述細節，去讀原文）

- **自誤 7**：交付一個**在法上不可能成立的取捨**（KL 抓出）
- **自誤 8**：以**射程小於命題之工具**作否定性結構宣稱——徹底列舉了 `n_hat_far`，
  卻**從未查同一函式簽章上的 `near_dir`**，即斷言「近側界方向無表達力」⇒ 造出假的 `GB-64`。
  🔒 **考古 83**：一次無瑕的徹底列舉，證出一個假的「做不到」——**因為母體選錯了**。
  ⚠️ 而 **CC 的獨立覆核也沒抓出來**——它把同一個太小的母體逐格重跑，數字一字不差。
  ⇒ **覆核時除了對數字，必須另外問一句「這個範圍對嗎」。**

### 🩸 一則量測器自傷（自查抓出·可不入倉，但要記得）

覆核 `K-9-6-h` 是否含「⛔ 不綁夾角條件」時回 ❌——因為比對字串漏算了 markdown 粗體標記。
重查後 ✅。🔒 **量得的值正好支持「對方漏辦」時 ⇒ 先疑量測器、後疑命題。**

---

## §五 與 CC 之界線（本窗實測）

**CC 這兩窗表現極好，⛔ 不要因為它是實作者就重做**：

- **逐次具名回報對施工單之偏離**，並附理由——本窗三次，**三次都對**（其中兩次是你的單寫錯）
- 自檢時**抓到自己的期望值算錯**（手算 CRLF 位元組），判「錯在期望值、非量測器」
- 判別力自檢第一版壞掉時，認出 `?` 是「物件未寫入」而**非**「被正規化」——自己回到「`0`／空值有多義」那條
- 主動更正自己上一批之量測框錯誤（把位元組寫成「字」）
- 以 `git hash-object --path=` 對不同副檔名套 attributes 作判別力自檢，**⛔ 不污染倉**

**你該攔下的**：

- CC 呈 KL 之題，若 **KL 已裁過** ⇒ ⛔ 攔下
- CC 判「與單不符」時，**先問它查的是哪一個世代**（本窗即因此翻案：CC 沒錯，但只看了舊 log）
- CC 之射程若寫得比實測寬 ⇒ 縮；**⛔ 但也不要反向擴張**

🔴 **CC 跑在 Windows／Git Bash 上。** working tree 為 CRLF、倉內 blob 為 LF，且**同一個檔裡會混合**。
⇒ **凡整行錨之比對，兩側都先 `tr -d '\r'`**（`VR-011`）。
⇒ 它說「該檔為 CRLF／混合」時，**受詞是它的 working tree**，⛔ 不是倉內。
⇒ 它另有 **cp950 輸出陷阱**，會自行加 `PYTHONIOENCODING=utf-8`。

---

## §六 與 KL 之溝通（⛔ 逐條）

- 🔴 **幾何示意 ＝ 地的圖**（街廓、正街、側街、界線、宗地）。⛔ 曲線圖、長條圖、座標圖都不算
- 🔴 **KL 畫的圖，效力高於你的文字推論**
- 🔴 **本窗新證**：KL 會直接給**座標檔**（`.cnt` 點表／`.lin` 連線表·CRLF·全形底線檔名）。
  已入倉於 `docs/specs/figures/`，**逐位元組凍結**（`.gitattributes` 之 `*.cnt -text`／`*.lin -text`）。
  🔒 該圖是**拓樸**之權威，⛔ **非度量**之權威——圖面自身最大不精確 `0.0558`（> 1e-3）。
  ⛔ **不得作為 1e-3 級數值對拍之 fixture。**
- 他說「我還是不懂」時，**換媒介、⛔ 不換說法**
- **一題只綁一個命題**；三個以上候選 ⇒ 列表，⛔ 不硬壓成二選一
- 🔴 **⛔ 不得給他一個「其中一支在法上不可能」的選項**（＝自誤 7）。
  出題前自問：**每一支若他選了，是否都合法、都可執行？**
- 「**記不清**」是合法答案 ⇒ 記「出處無法確認」，⛔ 不替他選邊
- 純技術／知會項目**直接寫進 CC 施工單**，⛔ 不打斷 KL
- 呈 KL 之題須用五段格式：**【現況】→【要改成】→【對土地的影響】（含數字）→【要你判斷】（單一是非）**，
  並標明「**真需 KL 域專業判斷**」抑或「**只是知會、點頭即可**」。⛔ 禁程式術語。
- 🔒 **本窗實證有效**：開工前先現查正典 ⇒ 攔下「保留區遠側界是哪條線」（已裁）。
  🔒 **本窗亦實證**：主動裁掉一題（`GB-64` 之「新增參數 vs 另闢構造」屬技術題）
  ——因兩路線**同一幾何** ⇒ 無差別土地後果 ⇒ ⛔ 不上呈。

---

## §七 換窗之判準（下次輪到你提）

出現任一即提出換窗：

- 重複問 KL 已裁過的事
- 引用某段對話而說不出它在倉裡哪裡
- 一批之內犯第二次「受詞取錯一層」
- **出艙一個你沒實際跑過的現查值**
- 🆕 **同一份施工單裡出現第二處內部矛盾**（本窗所加·見自誤 9）
- 🔴 **KL 要做改變配地面積之決策前**

**換窗前必辦**：把只存在於對話側之裁決與自誤**入倉**，再各寫一份交接文
—— **CC 一份（倉態）、你一份（本文）**，⛔ 不共用。
共用等於讓新的你拿著 CC 的自述去驗 CC。

🔒 **本文亦須入倉**（置於 `docs/reports/`），⛔ 不得只活在對話側。

---

## 🔧 入倉加註（`W-G.9-37`·2026-08-15·⛔ 上文原句一字未刪）

**PROVENANCE**：來源 ＝ **KL**／送達途徑 ＝ **對話側**（以 `@` 附上）／送達日 ＝ **2026-08-15**。
原檔名（本機）＝ `claude_ai_新視窗交接文_2.md`（12141 bytes／6117 字元）；
入倉時**內容逐字未改**（構造證：來源與倉內字元數相同且逐字相等），僅依倉內慣例調整檔名。
⚠️ 本檔係補辦 `W-G.9-36` 施工單 `§4` 之待補件——該批因 KL 當時未送達而標「🛑 待補件」，
⛔ 未以本機任何近似物代替。

🔴 **上文 `§三-D` 之裁定已被推翻**（⛔ 上文原句一字未刪）：

> 上文逐字：「**裁定**：宗0×宗1 由 `4.9205` → `0` ＝ `near_dir` 落地之效果」

該**因果歸屬⛔ 不成立**——兩世代之間，探針所消費之生產碼受動之 commit **不只一處**
（`W-G.9-36` §A-1 正面列舉 ＝ **5 個**；其中 `2dc32ff`「非 forced 之 **B 選槽起算點**校回 0」
直接移動宗地位置）。**量值不變**（`4.9205` → `0` 之事實成立），⛔ 非量值有誤。
更正全文見 `docs/reports/W-G.9-36_兩處過寬表述之更正.md`
與 `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末「五、歸因射程之更正加註」。

🔴 **上文 `§三-C` 之 `GB-67` 受詞亦已被推翻**（`W-G.9-37` 實測）：
上文逐字「新受詞：**不相鄰**之宗之間為何會重疊」——經逐宗量測空間位置，
`宗0`／`宗1`／`宗2`／`宗3` 之沿正街投影區間**兩兩重疊、中間無第三宗隔開**
⇒ **四者全部空間相鄰** ⇒ 「不相鄰」三字之受詞**自始錯誤**。
成因：`biz[i]` 之索引係**分組串接序**（左組 → 右組），⛔ **非**沿正街之空間序。
詳見 `docs/reports/W-G.9-37_相鄰性校正與交集形狀.md`。
