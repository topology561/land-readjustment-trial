# 🧾 `W-G.9-143`　**CC 交接文**（新 session 接手用·`W-G.9-142` 收束時出）

**出文窗**：CC（本 session context 逼近上限）　**倉態基座**：`529fa82da6e51024986309460546916b65f658bd`
**性質**：🔒 **純追加**·⛔ 零生產碼·⛔ 零管線·⛔ 零登記

---

## §一　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md`**（現查 `575` 列／`50854` B／`sha16 f6f272f740aeb100`·末端有 `W-G.9-132` 之進度表更正）。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （現查 `3199` 列·🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判「是否為本受詞之規格」，⛔ 不得只搜關鍵詞）。
3. 🛑 **開工閘**（每張施工單皆同）：
   `git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNN` ⇒ `cd` ⇒ `git config core.autocrlf false`
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、**⛔ 不得放 scratchpad**；二 clone 逐位對拍 log 時**路徑須同長度**。

---

## §二　倉態（態 ＝ `blob@529fa82`·CC 現查）

| 項 | 值 |
|---|---|
| `origin/wip/s1-endpart` | `529fa82da6e51024986309460546916b65f658bd` |
| `app.py` blob | `b4cc30835feaad03dfc37f394a669bbb45988b8b`（`22612` 列／`1333202` B／`sha16 1f2ae7c0048427c0`） |
| `CLAUDE.md` | `575` 列／`50854` B／`sha16 f6f272f740aeb100` |
| `K-6` 正典 | `3199` 列／`221652` B／`sha16 afeddb4f05b94588` |
| 取號（現查） | `K-9` max `22`／自誤 max `169`／`GB` 母體 `103`（缺號 `['GB-12','GB-87']`）／`docs/reports` 之 `W-G.9-NNN` max `134` |

🔴 **本波先前各單所載之 `app.py` 行號<u>全部須重查</u>**——`W-G.9-136` 已使其增 `191` 列。**一律 `grep` 現查。**

### 🩸 二件與倉態無關但接手者必知

1. 🛑 **`W-G.9-135`〜`-137` 之 commit 係自<u>拋棄式 clone</u> 直推 GitHub**；主 checkout（`C:\Users\admin\Desktop\land-readjustment-trial`）**未同步**，其 `HEAD` 停在 `a2c4b2b`（`W-G.9-136`）。
   ⇒ **⛔ 不要以主 checkout 驗基座**——`W-G.9-140` 之 Codex 接手即因此誤判「基座不存在」而停機。**開工閘一之 clone 才是判準。**
2. ⚠️ **工作 worktree `.claude/worktrees/distracted-tu-19e45b` 有 `D data/地籍資料來源_匿名版.xlsx`**（工作區缺檔·`HEAD` blob `80f75eeb…` 仍在物件庫）。**CC ⛔ 未動、⛔ 未還原、⛔ 無法歸因**（該資料夾有 Google Drive 同步）。主 checkout 之同檔完好。**待 KL 指示是否 `git checkout --` 還原。**

---

## §三　本波之位置

| 案 | 受詞 | 狀態 |
|---|---|---|
| 丙 | `K-6 §一` 幾何連通判定 | ✅ 已落地（`W-G.9-131`·`f4c5385`·零接線） |
| 乙 | `K-6 §二` 步驟 0 | ✅ **已落地**（`W-G.9-136`·`a2c4b2b`·`app.py` 純追加 `191/0`·**有土地後果**） |
| — | `K-9-21`（步驟 0 之例外） | ✅ 已入正典（`W-G.9-135`·`2b49340`） |
| — | `K-9-22`（合併後之表示層） | ✅ 已入正典（`W-G.9-137`·`529fa82`）·🔴 **⛔ 未落地** |
| 甲 | `K-6-A2` 剩餘六項 | ⬜ 未開工 |
| — | `K-9-16`／`K-9-17`／`K-9-12`（藍影＋二閘＋遞補） | 🔶 **量測全部完成**（`-138`〜`-142`）·🔴 **⛔ 未落地** |

🔒 **`W-G.9-138`〜`-142` 皆係純量測·零 commit** ⇒ 編號不連續係設計如此、⛔ 非遺失。

### 🔑 `-138`〜`-142` 之結論鏈（**下一單之全部前提**）

1. **`R2`／`R5` 之 `②-宗 圍堵閘破` 與步驟 0 <u>無關</u>**——`on`／`off` 二態之重疊值**逐位相同**（`45.9766`／`56.3293`），交集區域 `symmetric_difference = 2e-9 ㎡`。
2. **重疊之幾何 ＝ 楔形**：`R2`／`R5` 之重疊皆為**三角形**，恰一邊 ∥`SIDELINE`（`0.000000°`）、一邊 ∥`ALLOCLINE`（`0.000000°`）⇒ 即 `K-9-5-4 ②` 所預言者。楔角 ＝ `4.321972°`／`4.321968°`。
3. **成因 ＝ `near_dir` 之 ∥SIDE 只傳一手**：宗2 收到 ∥SIDE、**宗3 起一律退回 ∥ALLOC**（六街廓皆然）。`R2`／`R5` 之宗2 是碎片（`G 3.82`／`G 0.99`）擋不住 winner ⇒ winner 遠側界與**宗3**近側界夾出楔形；`R1` 之宗2 是大宗（`G 599.91`）⇒ 界面為**單一 `LineString`**（長 `33.244092972`）、無楔。
4. **該楔形 ＝ 正典之「藍影」**：`R2` 之 `S_req = 7.448704379` 與 `K-9-16 三` 逐字 `7.4487` 相符；`S_req − S` 恰等於 winner 之向前越界（`3.388704379`／`3.311774572`）。
5. **二閘分立已坐實**：`②-宗` 圍堵閘**只抓得到閘一**；**閘二（`K-9-12` 矩形容納）之違反⛔ 不產生重疊 ⇒ 靜默通過（假綠 `3` 格：`R1右`／`R3右`／`R6右`）**。

---

## §四　下一單之受詞：**落地單**（`K-9-16`／`K-9-17`／`K-9-12`）

🛑 屬**幾何影響批**，依既定紀律**須另開新窗**。`W-G.9-142 §四` 已明令本波⛔ 不落地。

### 已備之定式（KL `2026-08-27` 五項裁示·逐字見 `W-G.9-142 §零之前 二`）

| 項 | 內容 |
|---|---|
| **閘一** | `G(第1宗) > 藍影面積`（**嚴格**）**且** 臨 `FRONTLINE` 長 `> 0` **且** 臨 `BASELINE` 長 `> 0` |
| **閘二** | `K-9-12` 矩形容納（`W × D`·**可旋轉可平移**） |
| **後果** | 任一不過 ⇒ 該宗**不配地**、列入合併／調配、**由下一位遞補**；騰出之地入調配池；⛔ 不得超配 |
| **迴圈** | **單一遞補迴圈·二閘取合取**；遞補宗之 `G` **須重算** |
| **時點** | 🔒 **藍影於 winner `G` 終定後算一次，遞補迴圈內固定⛔ 不重算** |
| **泛化** | `Δs` 之正負由 `SIDELINE`／`ALLOCLINE` 相對傾角決定，**⛔ 與左右側無關** ⇒ 判準寫成一條、對兩向一體適用 |

### 🛑 落地前必知之三個要點（`W-G.9-142` 實測）

1. 🔴 **選邊只能依「落在街角地外」之定義**——`Δs` 正負當選邊規則於 **4/8 格選錯**（`R1左`／`R1右`／`R3右`／`R6右`）。二三角形之**面積亦不相等**（最大差 `R5左 0.2588`），因 `FRONTLINE ∦ BASELINE`（六格夾角 `0.0152°`〜`2.8680°`）。
2. 🔴 **閘二是真正的缺口**：假綠 3 格全在**右側**、全係 `S` 極小之碎片（`2.39`／`0.32`／`0.01`），最大可容納矩形僅 `2.66×10.63`／`2.84×11.36`／`0.0058×0.0232`（門檻 `3.5×14.0`）。
3. 🔴 **`stepg_pipeline.py:493-508` 之結構閘口徑為「重排前」**，`K-9-17 三【更正】` 已預告落地後**必然轉紅** ⇒ **須同批處置**，否則第一跑即停。

### 落地接點（`W-G.9-142 Q-8` 現查·⛔ 行號須重查）

- `_projection_order` 之呼叫點：`app.py` **3 處**（`:7731`／`:11396`／`:18913`）＋ `verify/stepg_pipeline.py:499`
  ⚠️ 施工單曾載「二呼叫點」，**實得 3 處**（`:11396` 係 `W-G.9-136` 步驟 0 新增者）。
- `near_dir` 交遞鏈：`app.py:12493`／`:12508`／`:12510`／`:12525`／`:20299`／`:20403` ＋ harness 鏡射 2 處
- `_block_strip` 雙線分支：`app.py:8510`（參數）／`:8572`（分支）；**生產鏈唯一傳非 `None` 者 ＝ `app.py:9815`**（於 `_cut_strip`·`:9805`）
- `Q-8b`：二呼叫點之「暫編地號集合逐位相同」之**直接不變式 ⇒ 現行<u>無</u>**（`assert` 命中 `0`）

### 建議之新增／須改（⛔ 只列不寫）

**新增**：`k916_blue_shadow()`（三角形＋面積·選邊依定義）／`k913_touch_lengths()`（二臨接長）／`k912_fit_rect()`（凸情形用 `P ⊖ R = ∩(P − r_i)`）／`k917_backfill_loop()`（單一迴圈二閘合取）
**須改**：`stepg_pipeline` 之左右推進迴圈 ＋ `app.py:18913` 對應段／`stepg_pipeline.py:493-508` 結構閘／池之結算

---

## §五　積欠登記（🛑 下一登記批一次入倉·**依序**）

| # | 項 |
|---|---|
| `1` | **KL `2026-08-27` 五項裁示之逐字入正典**（含「院測→遠側」「背叛→判」二讀法註·**編號須現查**·現 `K-9` max `22`） |
| `2` | 自誤 `170`〜`178`（現 max `169`） |
| `3` | `K-9-5-14-a`／`-b` 落地狀態就地加註 |
| `4` | `CLAUDE.md` 進度表**漏列 `K-9-16`／`K-9-17`** 之更正（該檔三詞命中皆 `0`） |
| `5` | `K-9-17 四 現況對照表`（`K-6:2990-2992`）之就地加註——`628-45(1)` 已於 `W-G.9-136` 併為 `628-45(1)+`，`R5` 之遞補宗實為 **`628-7(2)`**（正典載 `628-45(1)`·`W-G.9-141′ P-3d` 已具名） |
| `6` | `GB` 新則：`_block_strip` 雙線分支⛔ 無「兩線於街廓內交叉」之閘 |
| `7` | `GB` 新則：`side_label` 之 `else '左側'` 靜默退路（**咬到 `174` 次**·⛔ **無算術後果**·形式違例） |
| `8` | `W-G.9-130` 偵察報告入倉 ＋ **九份 baseline 重產**（須 KL 決時點） |

---

## §六　本波所立之家法與陷阱（🛑 接手者必讀）

| # | 家法／陷阱 | 出處 |
|---|---|---|
| `1` | 🛑 **量「是否落在某帶內」前，須先自碼面讀出該帶之<u>四個角</u>**，⛔ 不得由 docstring 之 `n_hat` 一詞推定基底。`_block_strip` 單線路徑之平行四邊形 ＝ `[bp−big·n̂, bp+S·d̂−big·n̂, bp+S·d̂+big·n̂, bp+big·n̂]`，`n̂ = rot90(allocation_dir)` ⇒ 帶座標解 `P−bp = t·d̂ + s·n̂`（`det ≈ 0.996`） | `W-G.9-140′`（我錯二次） |
| `2` | 🛑 **投影口徑須全程一致**：`w40.s_of` 係**正交**投影；正典之 `s` 多為 **∥ALLOCLINE** 投影。`SIDE ∦ ALLOC` 時二者不同（`R5`：`4.107` vs `8.162`） | `W-G.9-141′`（我錯一次） |
| `3` | 🛑 **「第 0 宗遠側界」＝ winner 依其 `G` <u>實配</u>之邊**（自 `cut_coords` 取 ∥SIDE 且最往推進方向者），⛔ **非** `_Tp` 之線（那是街角**規定範圍**之邊） | `W-G.9-141′` |
| `4` | 🛑 **`_oblique_s_max` 對參數極敏**：用 `cad['alloc_dir_by_block']` 原值得 `R1 499.70`／`R4 828.79`（荒謬）；須用**生產實收之 `allocation_dir`**（＝`rot90(cad 值)`） | `W-G.9-140′` |
| `5` | 🛑 **守恆自檢之受詞 ＝ Σ幾何面積（含抵費地列）**，⛔ 非 `ΣG`（`G` 不含抵費地）。破閘街廓之抵費地列**從未產生** ⇒ 殘差 ＝ 未算出之抵費地面積、⛔ 非新病 | `W-G.9-142`（我錯一次） |
| `6` | 🛑 **參數值之斷言須 spy 實收值**，⛔ 不得由 docstring／命名推定 | `W-G.9-127` |
| `7` | 🛑 **凡以 `G` 或 `幾何面積` 跨環境對拍者，容差一律 `±0.05 ㎡`** | `GB-101` |
| `8` | 🛑 **「`0` 最該懷疑」**；量得 `0` 或「與前次逐格相同」時先疑欄名錯／沒讀到新資料 | 本波四度自糾 |
| `9` | 🛑 **`grep` 之 `rc` 不可經管線讀**（`grep … \| head; echo $?` 讀到的是 `head` 之 `rc`）；命中 `0` 之宣稱須附**已知應非 0** 之對照組 | 本波再度踩 |
| `10` | 🛑 **`git add` 具名逐檔**，⛔ 不用 `-A`；暫存檔／探針／log **一律寫在 clone 之外** | `a47e758` 之覆轍 |
| `11` | 🛑 **`run_all`／`run_verification` 須於<u>拋棄式 clone</u> 內跑**（會改寫 `verify/out/**` 之已追蹤 log） | `GB-94` |
| `12` | 🛑 **bash heredoc → python stdin 在本機以 `cp950` 解碼** ⇒ 含 CJK／`\u0302` 之 patch 腳本會 `UnicodeEncodeError`；改用 `Write` 工具直接落檔 | 本波新踩 |

---

## §七　已知 harness 限制 ＋ 本波新增之量測工法

- 🔴 **`run_verification` 跑不完整**：於 `R2` 先 raise 而中止 ⇒ **到不了 `R5`**。首版量測若直跑，會全落在 `R2`。
- 🔧 **第四路徑（取任一街廓之 Step-G 態）**：`build_parcels` **重排使目標街廓先跑** ＋ 自 `exc.__traceback__` 之 `run_step_g` frame 讀 `f_locals['g_rows']`（`stepg_pipeline:995`）。
- 🔧 **取內部量之通法**：`sys.settrace` 攔 `_build_corner_range_v3`（`return` 事件讀 `f_locals`）／`_solve_one`／`_solve_G_one`／`solve_G_binary`（`call` 事件讀實參）。
- 🔧 **「重算 `G`」而⛔ 不改碼**：取該位置之**真實** `solve_G_binary` 實參，**只換 `a`**，外部再呼叫一次。
- 🔧 **矩形容納之精確法（凸多邊形）**：`P ⊖ R = ∩_{r∈頂點(R)}(P − r)`，非空即可容納；θ 掃 `0..180°`。
- 🔧 **`S_req` 之單一生產者**：`verify/probes/probe_WG984_gap.py:141` 之 `s_front_of_line(p, u, o, d)`（⛔ 不得另寫平行式）。
- 🔧 **`k94` gap 之單一生產者**：`k94_baseline_touch_by_parcel(parcels, bl_pts_by_label(...))`，入參須 `{'label','pid','coords'}`（⛔ 非 `所屬街廓`／`暫編地號`／`cut_coords`）。
- ⚠️ `solve_G_binary` 之回傳已 `round(_,2)`；`負擔比率` 欄係 `round4` ⇒ ⛔ 不可用以驗算 `G`。
- ⚠️ `_build_corner_range_v3` 只在 **PK 期**被呼叫（stepg 期命中 `0`）；其區域變數名為 **`Q0`**（⛔ 非 `P0`）。

---

## §八　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**：`wg9133p-step0-temp-layer`（**本波主線·含 `-136`〜`-140′` 之全部結論**）／`wg9131-k6-connectivity`／`wg9130-k6b-recon`／`wg9129-corner-pk-per-lot`／`push-before-shipping-claim`（🔒 **未 push 不得報收官**）／`grep-multibyte-charclass-trap`（量測器靜默給錯數之三十七面）／`app-vs-harness-stepg-divergence`／`dxf-source-divergence`／`run-all-side-effects`。

---

## §九　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值。
- 🔒 **拋棄式 clone 跑畢整份刪除**，並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push——① 零生產碼變更 ② 純新增檔（或既有檔之**純附加**·`deletions = 0`）③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠。
