# `W-G.9-239`　CC 新 session 交接文（`2026-09-05`）

> **性質**：交接文（零生產碼）。依既例（`docs/reports/W-G.9-231_CC交接文.md`／`-134`／`-122`／`-111`）
> 取**批次號 `239`**；**⛔ 鑄任何 `自誤`／`GB`／`VR`／`戒`／`K-9` 號**。
> **本文之一切數字皆現查於 `a3e141c`**（索引側 `git cat-file blob`·`GB-139 ②`）；⛔ 憑記憶。
> 🔒 **取號已依現行條款查宣告框**：`W-G.9-239` 宣告框 **0**／**0**（鬆框亦 **0**／**0**）；
> 對照組 甲 `W-G.9-238` 宣告框 **2**／**5** ✅／對照組 乙（人造號·代稱）**0**／**0** ✅。

---

## 一、態錨（**開窗第一動作＝重跑本節，⛔ 採本文之數**）

| 項 | 值（現查於 `2026-09-05`）|
|---|---|
| 主線 | `origin/wip/s1-endpart` ＝ **`a3e141cc512c77fad0673a21fcf9ca533182c402`** |
| `left-right` | **`0 0`**（工作樹追蹤檔乾淨）|
| 🔴 `app.py` blob | **`02cc6d7313789149ba852f997d2d8051a6cefaa0`** |
| 追蹤 ／ `.md` ／ `docs/` | **1649** ／ **574** ／ **552** 檔（`git ls-tree -r -z`）|
| `CLAUDE.md` | blob `ab4e6e4f44fc`·**184114** B／**2229** 列 |

**生產碼五檔之現態 `sha256`（閘 `P` 之基準）**

| 檔 | B | `sha256` |
|---|---|---|
| `app.py` | 1417946 | `e3e464ea2493d0461b629941280c55238633cc57573d0837363d60eb3c85b7ca` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

**近 8 commit**

```
a3e141c W-G.9-238R：🎉 PreToolUse 硬閘活體攔截成立（三缺陷已修）＋ 238 交接文原封入倉 🔧 工具層
982a930 W-G.9-237R：號占用改宣告框 ＋ PreToolUse 硬閘落地 ＋ 自誤 288／289 🔧 工具層·⛔ 生產碼
893f6ba W-G.9-237 工項零：宣告框查號（🔴 咬中·前版作廢）＋ 新版單原封入倉 ⛔ 零生產碼
67fb77c W-G.9-237 工項零：宣告框查號 ＋ 本單原封入倉 ⛔ 零生產碼
bf27645 W-G.9-235R2：三裁之落地 ＋ _best_block 幾何定義出艙 ＋ GB-143 誠實化 ⛔ 零生產碼
08822af W-G.9-235 工項零：本單原封入倉（閘咬中·依單之逐字重送續辦）⛔ 零生產碼
4e80d2f W-G.9-235R：🛑 停機——工項零 a 之號占用查未過（W-G.9-235 檔框 1／列框 1）⛔ 零生產碼
17ccee8 W-G.9-234R：號占用閘之條款補正 ＋ 自誤 286／287 ＋ 區外道路清單現況勘查 ⛔ 零生產碼
```

🔒 **側分支（遠端·皆已併入主線·⛔ 刪）**：`verify/W-G.9-198R-c1`／`-c2`／`-c3`／
`verify/W-G.9-219R2-c1`／`verify/W-G.9-229-c1`。
（另有 `wip/W-G.4-S0b-S0c`／`claude/wg9-95z-v3-resume-hgpzfs`／`main` ＝ **舊分支**·⛔ 工作分支。）

🔴 **工作副本 ＝ 主 checkout** `C:\Users\admin\Desktop\land-readjustment-trial`（**⛔ worktree**·KL `2026-09-01` 裁）。
🛑 **worktree 陷阱**：若 session 之 project dir 為 worktree，其 `CLAUDE.md` 為**舊態**
（`-231` 窗實測 `190` 列 vs 真態 `2229` 列）⇒ **開窗須驗 `left-right`**；非 `0 0` 即在舊態。
🔴 **harness 長跑一律於拋棄式 clone**（主 checkout 內跑 `run_all` **會改寫 3 個已追蹤 log**）；clone 須**短路徑**。

---

## 二、次號（現查·⛔ 採本文之數，開工前重算）

| 簿 | 現況 | 次號 |
|---|---|---|
| 自誤（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）| blob `a7ba8d690882`·**571555** B／**6550** 列；相異 **282**／`MAX` **289**／缺號 **`[106]`**／範圍框 **8**／定義框列命中 **283** | **290** |
| `GB`（`docs/reports/W-G.4_泛用阻塞項登記表.md`）| blob `bbd567fa19b3`·**649103** B／**4572** 列；`MAX` **150**（**標題錨定框**）| **GB-151** |
| `VR`（`docs/驗證裁定登記表.md`）| blob `61dcde76275b`·**364971** B／**4695** 列；未扣除 `MAX` **999**（哨兵）／**已扣除 91** | **VR-092**（三位補零）|
| 戒 | `MAX` **40** | **41** |
| `K-9` | `MAX` **28** | **29** |

🛑 **`GB` 之「相異／缺號集」⛔ 出艙**（`CLAUDE.md` 字樣錨 `在其釘定前，⛔ 得出艙`；
`GB-138` 處置③ 仍攢批未辦），亦⛔ 對其 `MAX` 作「恆／皆」之全稱。
🔒 **自誤之定義框（正字＝候選 `A`·`VR-091` 補款二）**：
`` ^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`? ``；**範圍框**僅既有 **8** 條；
**缺號清單恆為 `[106]`**（`VR-091` 補款五·`≠` 即器紅或新漏 ⇒ **停**）。
🔒 **缺號之母體範圍 ＝ `[min(命中集), MAX]`**（**⛔ `[1, MAX]`**——`自誤 1`〜`6` 記於「`## 六則`」**表列**、非標題形）。
🔒 **出艙命中數之三軸（`VR-091`）**：來源 `commit`／母體檔數・檔長／**粒度框**，**三者缺一即違規**。

---

## 三、工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**⛔ `origin/main`**）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**。🔴 **本波之強化**：**`--selftest` 綠亦⛔ 蘊含閘已生效**——見四之「硬閘」。
3. 🔒 **倉內報告＝唯一本體**；聊天＝ping。push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑，**別無他物**。
4. 🔒 **數字錨一律引倉檔**，**⛔ 憑記憶出艙**；與倉態衝突以倉為準。
5. 🔒 **工項零優先於一切工項**——單一律原封入倉 `docs/orders/`，驗「落檔 `sha256` ＝ 入倉 blob `sha256`」。
   🔴 **其前另有一步（本波新立）**：**入倉<u>前</u>先查該單號之占用（宣告框）**——見四。
   🛑 該 `sha256` 閘只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。
6. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（經發單側）；**⛔ 逕呈 KL**。
7. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 **`deletions` ＝ `0`**。
   🔒 **例外**：`verify/tools/` 之**缺陷修正**免 `deletions = 0`（`VR-069` 補款），**惟須逐行具名理由**。
8. 🔒 **常規補款一**：閘之量測與其所守護之動作（commit／push）**⛔ 置於同一命令塊**。
9. 🔒 **常規五**：每一量測須有**兩造對照組**——器須綠（已知應 > 0）＋器須紅（已知應 ＝ 0）。
10. 🔴 **本波新立**：**凡宣稱某閘「已落地」者，其<u>活體實測</u>為必要條件**，⛔ 以自檢代之（見四）。

### 🔴 生產碼 commit 之流程（**⛔ 逕推主線**）

```
備妥（本地 commit）→ ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c<n> ＋ 本地保全 ref keep/<單號>-c<n>
  → 本地分支 git reset --hard origin/wip/s1-endpart（⇒ 下一批⛔ 可能誤推）
  → 發單側逐位復驗 → 請示 KL → KL 逐字放行
  → 合併：**純快轉**（`git merge --ff-only`）·**⛔ merge commit**
     主線若已前進而快轉不可行 ⇒ 改走 `W-G.9-206 §八` 之 rebase 款（`--force-with-lease` 重推側分支＋重驗）
```
🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。

---

## 四、現行之閘與其框（**開工前逐一重跑**）

| 閘 | 母體／框 | 現況 |
|---|---|---|
| **閘 `P`** | 生產碼五檔之內容 `sha256` | 零生產碼批須**前後逐位相同**（基準見 §一）|
| 🆕 **號占用閘（宣告框）** | **母體 ＝ 全 `docs/`**；框 ＝ `D1` 檔名含該號 ∪ `D2` 列首為 `#+` 之列含該號 ∪ `D3` 列首即該號／同列含「本單」＋該號；皆帶數字邊界 `(?!\d)` | **收單後、入倉<u>前</u>**執行；**已占即停並回報·⛔ 自行改號** |
| 🆕 **`PreToolUse` heredoc 硬閘** | `.claude/settings.json`（`matcher` ＝ `Bash`）＋ `verify/tools/wg9237_heredoc_guard.py` | 🎉 **接線已成立**（`W-G.9-238R` 活體實測綠）|
| **`G2`**（`GB-144` 之取值路徑對拍）| **標記法框**：`GB-144` 節內標記為 `現態命中集@<commit>` 之列中 `<commit>` **最新**者（**`git` 祖先序**·⛔ 位置／字典序／全節聯集）| 現行標記列 ＝ 該節 `:3959`@**`d2d3692`**（**13** 個檔列）|
| **驗收出艙閘** | `verify/tools/wg9223_acceptance_audit.py <單> <報告>`（**須於倉內執行**·`GB-148`）| `rc=1` 且訊息為「該單⛔ 有【驗收】段」⇒ **無從回測**、**⛔ 回測不過** |

### 🆕 號占用閘之**三代演進**（⛔ 混同）

| 代 | 立於 | 框 | 病 |
|---|---|---|---|
| `0` | —— | **無閘** | `W-G.9-232`（原自載 `-231`）出單時該號已被占而無閘攔下 ⇒ `自誤 286` |
| `1` | `W-G.9-234` | **鬆框**（任一列含該號）| **自指增長**（`GB-132` 族）——報告寫「次號 ＝ N」即使 N 被咬中 ⇒ **序號被自身之報告推著跑** ⇒ `自誤 288` 層 `(2)` |
| **`2`（現行）** | `W-G.9-237` | **宣告框**（見上表）| ⛔ 排除**作廢單** ⇒ **同號重發之單永遠過不了自己的閘**（**候裁**·見六）|

🔒 **二框並列、各載框**（`VR-091 一 (3)`）；**判準取宣告框**，**鬆框僅作漏框偵察**。
🔒 **實測效益（`W-G.9-237R`）**：`200`–`237` 二框**全數相異**（`Δ` 列 **752**）；
**`233`／`236` 二號判準<u>翻轉</u>**（舊框已占·新框未占）。

### 🆕🎉 `PreToolUse` heredoc 硬閘（**接線已成立**·`W-G.9-238R`）

> **`Bash` 之 `command` 內含 `<<`（任何形）者<u>會被真的擋下</u>**，且 loud
> （訊息含**命中樣式之名 ＋ 偏移**／**依據**／`MISFIRE_POLICY` **四款全文**）。

🔒 **誤攔之處置（`MISFIRE_POLICY`·⛔ 設任何自助式 bypass 旗標）**
> **一** 首選：改以 `Write` 落檔，再以**路徑**呼叫。
> **二** 確不可行（如位元左移）：**⛔ 停用本器、⛔ 改其 `PATTERNS`**；改寫該命令以避開該字樣。
> **三** 二亦不可行：**停並上呈發單側／KL**，並於該批報告新設「誤攔登記」節（命令以**代稱**登記·`GB-147`）。
> **四** 本器之任何改動須逐行具名理由，並重跑 `--selftest`。

🩸 **其三缺陷皆由<u>活體</u>實測捕得**（自檢與 hook 契約測**皆綠時仍在**）：
`1` 空 `stdin` 落 `{}` ⇒ **靜默放行**（違 `W-G.9-14` 修法 `②`）／
`2` `sys.stdin.read()` 走 `cp950` ⇒ **誤攔一切含 CJK 之命令**／
`3` **`stdout` 之 deny JSON <u>覆蓋</u> `exit 2` ⇒ 回落放行** ⇒ 🔒 **⛔ 二法並用·只走 `stderr` ＋ `exit 2`**。

### 🔴 陷阱清單（**已付學費·逐條皆有實例**）

| 號 | 受詞 |
|---|---|
| `GB-139` | `core.autocrlf=true` ⇒ ① ⛔ 對既有檔下 `autocrlf=false` 之 `add`；② **閘之受詞一律取倉側 `git cat-file blob`** |
| `GB-143`（＋補款）| **含反斜線之字面一律 `Write` 落檔**；**凡經 heredoc 者一律⛔ 執行**——🎉 **自 `W-G.9-238R` 起有真攔截點**（見上）|
| `GB-145` | 閘之 `rc` **⛔ 取自管線之後**；一律 `subprocess.run(...).returncode` 直取 |
| `GB-147` | **哨兵自落地**——對照組字樣**⛔ 寫入任何入倉文字**；出艙只載命中數與代稱 |
| `GB-148` | 機檢器倉根繫 `cwd`；**倉外 `rc=1`**（⛔ `128`）；🔴 **`--selftest` ⛔ 繞過受測路徑** |
| `GB-149` | **append-only 簿內行號為錨之閘 ⇒ 母體單調擴張·永久轉紅**；處置二 ＝ **錨改符號名** |
| `GB-132` | **自指增長**——清單／占用表之敘述句自身污染其所量之集合 |
| `戒 40` | 凡出艙「`Δ` 恰 ＝ payload」之等同斷言，須**同格並報二端位元組數** |
| `戒 36` | 非鑄號者**⛔ 採該類號之定義列式形**（本倉體例 ＝ `🔬`／`🔧` 起首）|
| 🆕 母體之取法 | 取「某 `rev` 之母體」一律 `git ls-tree -r -z --name-only <rev>`，**⛔ `ls-files --with-tree`**（後者併 index ⇒ 母體有洞）|
| 🆕 `行號衛生 4` | **活文件**（`CLAUDE.md`／`.claude/skills/`／交接文）之引用**⛔ 以行號為錨**；用唯一字樣 ＋ **判別力自檢** |
| 量測器族 | `git ls-files` 未加 `-z` ⇒ CJK 路徑被 `quotePath` 加引號 ⇒ `.endswith('.md')` **假 0**；`str.count` 係子字串比對 ⇒ **一律列錨** |

🛑 **字樣錨之腐化（本文自報）**：`入倉前之號占用閘` 於 `CLAUDE.md` 之命中已由 **1** 增為 **4**
（後續節引用之）⇒ **⛔ 再作唯一錨**；改用 `號占用閘之框`（**1**）／`號占用閘之母體`（**3**）等，
且**每次使用前重驗其唯一性 ＋ 判別力自檢**（寬字樣 `號占用` 現為 **9**）。

---

## 五、已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 鏈 | 情境 | 閘 | 所在 | 登記 | 自解條件 |
|---|---|---|---|---|---|
| `序 13` | 退縮 `0 m` | `②-宗 圍堵閘` | **`app.py`**（生產碼）| **`GB-67`·未結案**（破量 `R2` `45.9766 ㎡`／`R5` `56.3293 ㎡`）| `②-宗` 三項落地介面（`【缺】` 5）＋ 破量歸零 |
| `序 14` | 退縮 `3.5 m` | **結構閘 `telescoping`** | `verify/stepg_pipeline.py`（**harness 專屬**）| **既存紅·准紅碼**（`W-G.9-4` 起）| 鏈斷成因獲解（**⛔ 放寬容差**）|

🔒 **後果**：`run_step_g` 於 **`R2`** 即 `raise` ⇒ **`R4`／`R5`／`R6` 一筆不產生**
⇒ 🛑 **一切「`R4` 逐值迴歸」現態<u>不可得</u>**（`CLAUDE.md` 正本表 `序 12`）；
生產碼批之迴歸只能出**替代證據 ＝ 行為逐位相同（含同點 `raise`）**，**⛔ 讀為「已證 `R4` 逐值不變」**。

---

## 六、待辦／待裁（依現態·⛔ 由 CC 自裁）

### 🔴 候發單側／KL

1. 🔴 **`GB-143` 補款之定性更新須入倉**——`W-G.9-237` 所落之節逐字載「在活體實測綠**之前**仍為
   『已知無閘之禁令』」，該條件**已於 `W-G.9-238R` 成就**；其更新宜由**發單側出單**指示
   （**⛔ 由 CC 逕改該節**·`常規四（九）五`）。字樣錨 `已知無閘之禁令`（`GB` 簿命中 **4**）。
2. **宣告框是否增設「作廢單⛔ 計入」一款**——`W-G.9-237` 即其首例（同號重發永遠過不了自己的閘）。
3. **`.py` 以自稱形宣告其為某單時是否構成占用**（現行款 `③` 明列為「另議」）。
4. **guard 之 `stderr` 於 `cp950` 主控台呈亂碼**（活體訊息可讀但字元受損）——是否改 ASCII-safe。
5. **`GB-150`**（`app.py:14271` `_wf_tag_of` **硬限 `{0, 3.5}`**、其餘 `raise`）——KL 已確認
   「七級調配是每個案都要執行的功能」⇒ 其為**通用化波之真阻塞項**；呼叫點**唯一** ＝ `app.py:23846`
   （在 `st.spinner("執行七級調配…")` 內）⇒ **⛔ 在 Step G 主分配路徑上**。
6. **`v3:427` 與 `wf_f4:689` 之不一致**——已由 `W-G.9-232R` 於 `v3` 末端掛**更正標記**；
   🛑 **⛔ 由 CC 提名、建議或出裁定題**。

### 🟡 併期末批

- `GB-149` 處置二（**錨改符號名**）；行號錨待復驗之 **2** 支
  （`verify/probes/probe_order_preflight.py:134`／`verify/tools/wg99_anchor_audit.py:18`）。
- 🔴 **`G2` 之定義只存於<u>單</u>、⛔ 落地為 `.py`** ⇒ 掃描器取不到（本身即一風險）。
- `GB-148` 之碼面處置（顯式根參數 ＋ `--selftest` 走同一路徑）。
- 已失效**哨兵殘留**之逐處處置（`GB-147`）。
- 層二 `[1, 188]` 之 `188` 號單原文未入倉；正典 `K-6` 之未實作型陳述之復核。
- `app.py:22395`／`:22884` 之 `3.5` 集中化（其各含情境判別分支·`_SB_DEFAULT_M` 於彼處**不可及**）⇒ **通用化波**。
- 🛑 `K-9-5-2-a`-3 與 `K-9-5-3` 之逐字互斥（**休眠**·復活即**真域邊界**）。

### 🔒 `CLAUDE.md` 待落地正本表（`序 1`–`14`·**開工前自倉重生·⛔ 沿用本文**）

其正本 ＝ `CLAUDE.md` 之「🔧 待落地清單之更正五」節 ＋ 其後之拆列（`3-a`／`3-b`）與增列（`序 9`–`14`）。
🔴 **有土地後果者**：`序 3-b`（`K-6-A2` 存續四項）／`序 7`（`K-9-27`／`K-9-28` 碼面）／`序 13`（`GB-67`）。

---

## 七、本波（`-231R` → `-238R`）之新增摘要

| 批 | 產物 |
|---|---|
| `-231R` | 開窗態錨復驗 ＋ 次號重算 ＋ 閘重跑；**🩸 CC 側二自陳**（聊天出艙 `GB` 相異／第二次 heredoc）|
| `-232R` | **`v3` §7-4「排序」款之更正標記**（裁定K：純距離優先）；**`wf_f2` ⛔ 有容量／池量／超配過濾**（`_decide` ＝ 達標塊中 `ΣG` 最大者·**級別係事後標記⛔ 選擇準據**）；**`K-9-1` ⛔ 有獨立正典節**（僅表列二處）；**`_k923_gate2` 生產呼叫點 `0`**（建置未接線）|
| `-234R` | **`入倉前之號占用閘`**（鬆框代）＋ `自誤 286`／`287`；**區外道路清單之現況勘查**（`b` 🔴【查無】可新增項之 UI 既例／`c` `front_lines` 四欄⛔ 含 `handle`／`d` 覆蓋率逐街廓實算／`e` 重劃範圍邊界 🟢 二路）|
| `-235R` | 🛑 **停機**——號占用閘**首次咬中**（其占用者即該閘自身之施行紀錄）|
| `-235R2` | **`_best_block` 之幾何定義出艙**（🔴 **⛔ 有分母**·比絕對重疊長·勝差 `≤ 1.0` m·容差 `2.0`／`1.0`；街廓多邊形 ＝ **截角後**·實測 `32` 條短邊）；**`GB-143` 誠實化**；**丙丁戊己編碼現查**（分配情境碼**僅 `戊己` 一組**·`丙丁` 作情境碼者【查無】）|
| `-237R` | **號占用改<u>宣告框</u>**（`200`–`237` 二框全數相異·`233`／`236` 判準翻轉）；**`PreToolUse` 硬閘落地**；`自誤 288`／`289` |
| `-238R` | 🎉 **活體攔截實測 ＝ 接線成立**（三缺陷皆由活體捕得）；`W-G.9-238` 交接文原封入倉 |

### 🔒 `_best_block` 之幾何定義（**道路側推導須對齊之正字**·`CLAUDE.md` 字樣錨 `道路側推導之幾何定義`）

- 重疊量 ＝ `_line_block_overlap(line_pts, block_poly, ang_tol_deg=2.0, perp_tol_m=1.0)`
  （方位角 `mod 180` 差 `≤` 容差 ∧ BLOCK 邊兩端到線垂距 `≤` 容差 ∧ 投影落線 span 內；**逐邊加總之 1-D 重疊長**）。
- 🔴 **⛔ 有分母**；歧義判準 ＝ **勝差 `≤ _BIND_MARGIN_M`（`1.0` m）**；tiebreak ＝ 同分按塊名。
- 街廓多邊形 ＝ **截角<u>後</u>**（`p = _SP_poly(b['vertices'])`·DXF 原始頂點·**實測 `32` 條 `2`–`8` m 短邊**）。
- 🔒 **覆蓋率之逐街廓實算**（倉側·`data/V6_1.dxf`）：`R1` **8.08**／`R2` **96.46**／`R3` **96.18**／
  `R4` **7.34**／`R5` **96.27**／`R6` **96.04**（%）⇒ `R1`／`R4` **臨區外道路**。

---

## 八、開窗前四件事

1. **重跑一之態錨**（`ls-remote`／`left-right`／五檔 `sha256`／`ls-tree -r -z`）——**⛔ 採本文之數**。
2. **重算二之次號**——並驗 `自誤` **缺號清單 ＝ `[106]`**、**範圍框 ＝ 8**。
3. 🆕 **驗 `PreToolUse` 硬閘仍生效**：發一則含 heredoc 引入符之 `Bash` 命令（**字樣以代稱出艙**·`GB-147`），
   **須被攔且 loud**；**未被攔即接線失效 ⇒ 停並回報**（`settings.json` 於 **session 啟動時**載入）。
4. **讀 `CLAUDE.md`**（其**工作紀律**與**待落地正本表**為正字；本文只是索引）。

🛑 **收單後之首二步（順序⛔ 顛倒）**：**① 查該單號之宣告框占用 → ② 原封入倉 `docs/orders/`**。
