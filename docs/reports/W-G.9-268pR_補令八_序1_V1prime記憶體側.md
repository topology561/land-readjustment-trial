# `W-G.9-268′` 補令八 序 `1`：`V-1′` 記憶體側新器 —— 🛑 **停機上呈**（三組同色）

> 🛑 **結論**：`V-1′` 三組於 `c2` 態**全綠 ⇒ 三者同色 `True`**
> ⇒ 依補令八 `§二` 「三組同色 ⇒ **停機上呈**，⛔ 併線」⇒ **序 `2`〜`5` 一律未辦**。
> 🔒 補令八 `§二` 逐字：「**KL 之放行於該情形下⛔ 生效**（其所裁者為土地後果，⛔ 為驗證結果）」。
> 🔒 **主線 `wip/s1-endpart` 原地不動**；本批之 `commit` 全落側分支 `verify/W-G.9-268p-c2`。

---

## §零　三軸之總宣告（`VR-091`）

> `(1)` **來源 `commit`**：主線 ＝ `683b7fa49e4e20f53593c363b7d7050c91218d20`（`ls-remote` 實查）；
> 側支期初 ＝ `2a1da8dc0680248ec0b4806ce168b93ff54df350`（＝ 本地 HEAD·實查相符）。
> `(2)` **母體**：逐節具名。
> `(3)` **粒度框**：逐節具名（列／宗／格／公尺）。

---

## §一　🔴 **補令七 ⛔ 在倉、亦⛔ 送達本窗**（照實·⛔ 編造）

**現查**（母體 ＝ `docs/orders/` ＋ 全倉）：
`docs/orders/` 現有 **施工單 ＋ 補令一〜六 ＋ 補令八**，**⛔ 補令七**；
全倉「`補令七`」之命中 ＝ **`0`**（對照組「`補令六`」＝ 非零 ⇒ 量測器非紅）。

🔒 **處置依 `CLAUDE.md` `施工單必須自足` 之判準（給 CC）**：
「⛔ 禁編造、禁以倉內近似物代替；照實回報『單內無 X』並請補」（失敗考古節 `52`）。

**逐項之影響**

| 補令八 所引 | 是否受阻 | 處置 |
|---|---|---|
| 補令七 `§二`（二宗） | ✅ 否 | 補令八 `§一` **已逐字重述** |
| 補令七 `§四`（`V-1′` 新器之授權與規格） | 🟡 **部分** | 改以**倉內正典**為據：`VR-094` 五款逐字 ＋ 其末端追加之三組界定 ＋ 原單 `§三 P-7`。**若補令七 `§四` 另載規格，本器須據以復驗** |
| 補令七 `§五`（二形並取） | ✅ 否 | 補令八 `§四` 已重述其要求 |
| 補令七 `§六`（`自誤 343`／`GB-158` 之受詞） | 🔴 **是** | **⛔ 可鑄**——其形與內容⛔ 可得。惟二者屬**序 `4`**（序 `3` 併線之後）⇒ **⛔ 阻塞序 `1`** |
| 補令七 `§七`（併線次序） | ✅ 否 | 已由補令八 `§二` **取代** |

---

## §二　器：`verify/probes/probe_WG9268p_c2_v1prime_mem.py`

🩸 **其存在理由（自毀式之第二例）**：舊器 `probe_WG9266_V1prime_3groups.py` 之母體 ＝
二態之落檔 `got_G值_退縮3.5m_partial.csv`，而 `_partial` **只在管線<u>中止</u>時產生**；
`c2` 之受詞正是「**使管線⛔ 再中止**」⇒ 該檔於 `c2` 態⛔ 存在 ⇒ 舊器**結構上量不到 `c2` 態**。

🔑 **本器之作法 ＝ <u>只換資料源</u>**（⛔ 另寫第二份定義·`GB-48` 族）
> 對舊器**只 monkeypatch 其 `load()`**（＝ 讀 CSV 之那一支）；
> `shared_edge`／`s_of_line`／`measure`／`gates`／`run`——**含 `V-1′` 五款之全部判準與 `1e-6`**
> ——**逐字沿用、一字不動**。
> 🔒 `1e-6` **自該模組現讀並出艙**：`TOL_K1 ＝ 1e-06`／`TOL_K3 ＝ 1e-06`（⛔ 本器另定·⛔ 放寬）。

🔒 **二態之取得**：於**同一 process** 內以**顯式旗標值**各驅動一次
（`off ＝ '0'`／`on ＝ '1'`）——⛔ 以 `pop` 取 `off`（`c2` 後預設為 `'1'`·自毀式）。
🔒 **`FRONT_LINE`** ＝ 生產解析器之 `cad['front_lines']`（`6` 街廓·⛔ 質心導出·⛔ 自寫第二份解析）。
🔒 **`cut_coords` 之二形（坑 `1`）**：記憶體側 `list` → 還原為**字串形**餵舊器；
**⛔ `cut_coords` 之列 ＝ `0`**（二態皆）⇒ ⛔ 靜默丟列。

**母體基數**（`3.5m`·`rc = 9`·`stderr` **`0`** B·落檔 `verify/out/WG9268pR_c2_v1prime_mem.log`）

| 態 | `g_rows` | 中止街廓 |
|---|---|---|
| `off` | **`47`** 列 | `R5` |
| `on` | **`60`** 列 | 無 |

---

## §三　🔴 三組之逐款（`c2` 態·**判準一字不動**）

| 組（受詞逐字） | `n` | `(1a)` | `(1b)` | `(2)` | `(3)` | `(4)` | `(5)` |
|---|---|---|---|---|---|---|---|
| `3.5m`·`R2`·`left`〔**陽性**〕 | **`6`** | ✅ | ✅ | ✅ | ✅ | ✅ | **`0` 宗** |
| `3.5m`·`R1`·`right`〔**真陰性**〕 | **`3`** | ✅ | ✅ | ✅ | ✅ | ✅ | **`0` 宗** |
| `3.5m`·`R2`·`right`〔**第三類**〕 | **`6`** | ✅ | ✅ | ✅ | ✅ | ✅ | **`0` 宗** |

🔒 **三組之 `n` 皆 `> 0`** ⇒ ⛔ 觸「任一組 `n = 0`」之停機款（`自誤 329` 之形**未重演**）。
🔒 **自我驗證閘全過**（三組 × 二態 各一輪）：
閘 `γ` `6/6`／`3/3`／`6/6`；閘 `α` `5/5`（最大 `|Δ| 0.003677`）／`2/2`／`5/5`；閘 `β` `7/7`／`4/4`／`7/7`
⇒ 🔒 **本紅⛔ 量測器紅**。

### 三-1　判別力（`VR-094` 末端追加 三·逐字）

| 角色 | 應然 | 實測 | 判 |
|---|---|---|---|
| 陽性 | 須 `(5) > 0` ⋀ `(1b)`／`(3)` **紅** | `(5) = 0`·二款**綠** | 🔴 **不符** |
| 真陰性 | 須 `(5) = 0` ⋀ `(1b)`／`(3)` **綠** | 同 | ✅ 符 |
| 第三類 | 須 `(5) = 0` ⋀ `(1b)`／`(3)` **紅** | `(1b)`／`(3)` **綠** | 🔴 **不符** |
| **三者同色？** | 須 `False` | **`True`** | 🛑 **閘失能·停機** |

---

## §四　🔑 歸因（**已量**·⛔ 斷言）

🔒 **三組之受詞皆為 `3.5m` 之 `R1`／`R2`**，而該三側於 `c2` 之 `off → on` 下，
其量**全在 `1e-6` 判準之下**：

| 組 | 全側 `|ΔG|` 最大 | `(1b)` `Δ` 值域 | `(3)` 極差 |
|---|---|---|---|
| `R2·left`〔陽性〕 | **`0.0000 ㎡`** | `[−1e-9, +1e-9]` | `1e-9` |
| `R1·right`〔真陰性〕 | `0.0000 ㎡` | `[0, 0]` | `0` |
| `R2·right`〔第三類〕 | `0.0000 ㎡` | `[−1e-9, +1e-9]` | `1e-9` |

🩸 **`cut_coords` 逐位相同雖僅 `1/7` 宗（`R2·left`）**，惟其**每一個受量之量皆為 `9` 位小數之零**
⇒ 該「已動」係**末位浮點差**、**⛔ 真實位移**。
🔒 **⇒ 三組係為<u>另一個變更</u>而選**（`VR-094` 末端追加 三 逐字：態A `70ce7d46…` → 態B `e9d1b9c3…`
＝ `W-G.9-261`），其陽性側在**彼**變更下方紅。**對 `c2` 之受詞，三組皆為「未動之側」。**

### 四-1　🛑 診斷之結果 ＝ **不可得**（⛔ 據以下任何結論）

本器另以**同一支 `V1.run`** 量本批**實際動到**之側（`off→on` 之 `cut_coords` 相異宗數：
`R5·left` `5/6`／`R5·right` `2/2`／`R6·right` `3/5`／`R6·left` `1/3`／`R2·left` `6/7`／
`R2·right` `6/7`／`R1·right` `2/4`；未動 ＝ `R1·left`／`R4·left`／`R3` 二側）。

| 診斷側 | `n` | 閘 `γ` | 判 |
|---|---|---|---|
| `3.5m·R5·left` | `5` | **`3/5`** 🔴 | 🛑 **自我驗證閘不過 ⇒ ⛔ 據本表下任何結論** |
| `3.5m·R6·right` | `4` | **`3/4`** 🔴 | 🛑 同上 |

🔴 **⇒ `c2` 實際動到之側，本器<u>量不了</u>**——閘 `γ`（共用邊須恰 `2` 頂點）於該二側不過。
🛑 **⇒ ⛔ 得據此主張「改選 `R5·left` 為陽性即可」**——該主張所需之證據**正是被閘 `γ` 擋下者**。
🔒 **⛔ 調框以就結果**（`VR-094` 二 之明令：本器⛔ 以待驗值為停止條件）。
🩸 併記（照實·⛔ 推定其因）：`R5·left` 含 `628-53(2)`（`G ＝ 0.00 ㎡`·寬 `0.02 m`），
`R6·right` 含 `628-53(1)`（`G ＝ 0.26 ㎡`）——**是否與其退化幾何有關，本批⛔ 量**。

---

## §五　🛑 停機上呈之受詞（**候發單側／KL**·⛔ CC 自裁）

1. 🔴 **三組同色 ⇒ `V-1′` 於 `c2` 態失能**。其解（**重選三組之受詞**）係
   **「須新訂一條準據」** ⇒ 落 `自解款` **黑名單** ⇒ ⛔ CC 自裁。
2. 🔴 **重選亦⛔ 顯然可行**——`c2` 實際動到之二側於閘 `γ` 不過（`§四-1`）
   ⇒ 其**或須先解閘 `γ` 於退化幾何下之適用**，方談得上重選。
3. 🔴 **補令七 `§六` 之 `自誤 343`／`GB-158` ⛔ 可鑄**（其受詞⛔ 可得）⇒ **請補補令七**。

🔒 **本批⛔ 辦者**：⛔ 併線、⛔ 推主線、⛔ 動 `app.py` 一字、⛔ 辦 `c3`／`c4`、
⛔ 鑄任何號（含 `GB-159`——其屬序 `4`）、⛔ 改 `V-1′` 五款之判準一字、⛔ 調任何框以就結果。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| `4be0470`（工項零） | `2a1da8d..4be0470` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 |
| 本報告之 `commit` | `4be0470..HEAD` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 |

🔒 **`c2` 之生產碼內容逐位未變**——側支之 `app.py` 仍為 `c1376af` 所定之內容
（本批**⛔ 動 `app.py` 一字**）。
