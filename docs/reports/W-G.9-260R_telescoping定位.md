# `W-G.9-260R`　`telescoping` 定位（純診斷·⛔ 修）＋ 工項一〜三之落地

> 🛑 **本倉三受詞一字未動**：`app.py` `4860f10828fb8ae9`／`verify/stepg_pipeline.py` `c8d46d2e5fb79dc5`／
> `verify/wf_f0.py` `b0722355c1978635`（倉側 blob·前後逐位相同）。
> 🔒 注入落**二拋棄式 clone**（`C:/t260` 閘關＋診斷／`C:/t26a` 原態）·**目錄名同長度**（記憶 ㊲）·跑畢整份刪除。
> 🔒 **本文⛔ 含任何修法主張**（`V-S`·受檢字樣以代稱行之·⛔ 逐字寫入）。

---

## §一　開工閘（五項全過·**當場復現·⛔ 採信單載**）

錨 ＝ **`1531916`**（`ls-remote` 逐位符）；三受詞 blob 逐一相符；
`W-G.9-260`／`-259` 之宣告框**皆全 `0`**（三軸 ＝ `blob@1531916`／全 `docs/` **`604`** 檔／檔框＋列框；
**讀不到 `19` 檔 ＝ `png 10` ＋ `jpg 9`**，與單 `§六 V-N` 所載逐項相符）；
對照甲 `-257` ＝ `D1 2`／`D2 3`／`D3 1`／檔框 `3`；對照乙 人造號全 `0`。
次號：自誤 `MAX 320`（相異 `313`／`MIN 7`／缺號 `[106]`／恆等式 `True`）⇒ `321`；`GB MAX 153` ⇒ `GB-154`；`K-9 MAX 29`（⛔ 鑄）。
`S-0b` `rc=0`；二檔 `SELF_SHA256` 皆相符（`-260` `a0ca842495407870…`／`-259` 實算 **`f5dc385fe1ba2041`** ＝ 單載）。
🟡 提示 `1` 項：`P-4` ＠ `:36` 係 `自誤 321` 標題中之「載 `32`、真值 `24`」⇒ **具名豁免（偽陽）**。

---

## §二　工項一〜三（落地）

| 工項 | 受詞 | 落點 |
|---|---|---|
| 一 | `自誤 321`（前窗發單側之紅項數量測誤·載 `32`／真值 `24`）| 自誤簿檔末 |
| 二 | `GB-153` 歸因之更正（`②-宗` 停用之相異由「上界 `8`」更正為 **`0`**）| `W-G.4` 檔末 |
| 三 | `GB-154` 鑄（`telescoping` 之獨立登記）| `W-G.4` 檔末 |

---

## §三　CC 之獨立復現（單 `§三 二`／`三` 之受詞）

### `a`　同容器之閘開／閘關對照 — ✅ **完全復現**

二 clone 同 `1531916`、同依賴集、目錄名同長度；`t26a` 為**原態**（⛔ 注入），`t260` 之 `②-宗` `raise` 降為 `print`。

| 態 | 紅項數（計數式 `grep -c '🔴 FAIL'`）| 名稱差集 |
|---|---|---|
| 閘**開**（`t26a` 原態）| **`24`** | —— |
| 閘**關**（`t260`）| **`24`** | **只在閘開 `0`／只在閘關 `0`** |
| 🔒 對照組（器須綠）| 共同項 **`24`** | —— |

⇒ 🔒 **相異 ＝ `0`（恆等）** ✓ 發單側之判**成立**。

### `b`　`R2` 四次呼叫之情境 — 🟡 **部分復現**

本批之注入記錄於 `telescoping` 閘（⛔ `②-宗` 閘）⇒ 得 `R2 left` **四筆**：
**`#2` 為 `3.5m`**（`Wf=34.39`／`W0=6.98`），**`#1`／`#3`／`#4` 為 `0m`**（`Wf=26.03`／`W0=0.00`）。
⇒ 🔒 **與單 `§三 三` 之「那一次 `0.0000` ＝ `3.5m`」一致**（`3.5m` 恰為四筆中唯一之異者）。
🛑 **`②-宗` 之 `biz_x` 逐次值本批⛔ 記錄**（注入點不同）⇒ 該欄之復現**沿用 `-257R`**。

---

## §四　工項四：`telescoping` 之定位（`Q-1`〜`Q-4` **全部有答**）

### `Q-1`　`3.5m`·`R2 left` 鏈首宗之 `Rw` — 🔴 **發單側之推定為<u>真</u>**

鏈首宗 ＝ **`628-42(1)`**，其 **`Rw = 55.59`**、`W = 6.98`（＝ `W₀`）。

| 判準（單 `§五 b`）| 值 | 判 |
|---|---|---|
| 若 ≈ `0`（＝ `R(6.98)−R(6.98)`）⇒ 推定**偽** | —— | ✗ |
| 若 ≈ `55.59`（＝ `R(6.98)−R(0)`）⇒ 推定**真** | **`55.59`** | ✅ |

⇒ 🔒 **全部差額集中於該一宗**：`Σ = 55.59 + 20.34 + 2.84 + 21.25 + 0.0 + 0.0 + 0.0 = 100.02`，
而閘期望 `R(34.39) − R(6.98) = 44.4140` ⇒ `Δ = 55.606` ≈ `R(6.98) = 55.586`（殘差 `0.020`）。

### `Q-2`　`3.5m`·`R2 left` 逐宗表（**完整 `7` 宗·⛔ 取樣**）

| # | 暫編地號 | `W(m)` | `Rw(%)` | `F(m)` | `S(m)` |
|---|---|---|---|---|---|
| `1` | **`628-42(1)`** | `6.98` | **`55.59`** | `44.95` | —— |
| `2` | `628-41(1)` | `11.05` | `20.34` | `44.95` | —— |
| `3` | `628-27(1)` | `11.71` | `2.84` | `44.95` | —— |
| `4` | `628-40(1)+` | `18.37` | `21.25` | `44.95` | —— |
| `5` | `628-30(2)` | `20.38` | `0.0` | `44.95` | —— |
| `6` | `628-39(1)` | `25.65` | `0.0` | `44.95` | —— |
| `7` | `628-31(2)` | `34.39` | `0.0` | `44.95` | —— |

🔒 **`W` 序列單調遞增** `6.98 → 11.05 → 11.71 → 18.37 → 20.38 → 25.65 → 34.39`；
`#5`–`#7` 之 `Rw = 0` 係 `rw_from_width` 於 `W ≥ 18` **飽和 `100%`** 所致（差額為 `0`）。

**二母體之對照（單所令·`:1256` docstring 宣稱對齊）**：
🔒 **`n_rw = 7`／`n_chain = 7`**（四筆紀錄皆同）⇒ **母體大小相同**。
🔴 **CC 之強假說（「有宗在 `rows` 而不在 `results`」）<u>被推翻</u>**——照實出艙。
🩸 **差集之逐宗比對⛔ 可得**：CC 之注入以 `n.get('lot')` 抽 `_chain` 之 node id，實得**全 `None`**
⇒ **量測器紅**（node 結構非該形）。**阻斷點已具名**；`n_rw == n_chain` 之等式仍成立且足以推翻上開假說。

### `Q-3`　`0m`·`R2 left` 逐宗表 ＋ 逾 `100%` 之歸屬

| # | 暫編地號 | `W(m)` | `Rw(%)` |
|---|---|---|---|
| `1` | **`628-41(1)`** | **`4.06`** | **`38.14`** |
| `2` | `628-42(1)` | **`3.11`** 🔻 | `0.0` |
| `3` | `628-27(1)` | `3.75` | `4.18` |
| `4` | `628-40(1)+` | `10.27` | `36.18` |
| `5` | `628-30(2)` | `12.2` | `8.46` |
| `6` | `628-39(1)` | `17.31` | `17.41` |
| `7` | `628-31(2)` | `26.03` | `1.79` |

`Σ = 106.16`；閘期望 `R(26.03) − R(0.00) = 100.00` ⇒ `Δ = 6.160`。

🔴 **逾 `100%` 之機制（本批之核心發現）**：**`W` 序列<u>非單調</u>**——
`#1` `4.06` **→** `#2` **`3.11`**（**下降 `0.95`**）→ `#3` `3.75` → `#4` `10.27` …
而 `Rw` 係**累積差額制**（`R(W_far) − R(W_near)`），其恆等式 `ΣRw = R(W_final) − R(W₀)`
**以 `W` 單調遞增為前提**。`#2` 之 `Rw` 為 `0`（差額為負而未退回），
致 `R(3.11) → R(3.75)` 之區段**被計二次**（一次含於 `#1` 之 `R(0)→R(4.06)`，一次為 `#3` 之 `4.18`）。

🔑 **與 `②-宗` 之交叉**：`0m` 之溢計源頭 **`628-41(1)`**，**正是 `-257R` 所定位之 `②-宗` 重疊源頭**
（`宗0`·其幾何橫跨其後三宗·佔總疊 `100%`）。⇒ 🔒 **二病，共指同一宗。**

### `Q-4`　`W₀` 之產生路徑（**只述其現由何式得**）

| 項 | 逐字 |
|---|---|
| 初值 | `verify/stepg_pipeline.py:779` `_W0_left = 0.0; _W0_right = 0.0` |
| 捕獲 | `:837` `if not _W0_left_set: _W0_left = float(res.get('W_near', 0.0)); _W0_left_set = True`（`right` 同構 ＠ `:940`）|
| 定義（docstring `:775`–`:778`）| 「`W₀` ＝ 該側 `Rw` 累積起算點（telescoping 閘用：`ΣRw_側 = R(W_final) − R(W₀)`）。🆕 `W` 正典（脫鉤 `S`）：`W₀` 改＝**首宗近側 KL `W`**（`mp`→首宗近側界線＝`res['W_near']`）·非舊 `buffer·cos_dn`。首宗解出後捕獲（首宗＝角落宗·其近側＝buffer 之遠緣）」|

⇒ 🔒 **`3.5m` 得 `6.98`** ＝ 首宗 `628-42(1)` 之 `W_near`；**`0m` 得 `0.00`** ＝ 首宗之 `W_near` 為 `0`。
🔑 **與 `Q-1` 之交叉**：`3.5m` 首宗之 `Rw` 若依 `W_near` 起算應為 `R(6.98) − R(6.98) = 0`，
實得 `55.59` ≈ `R(6.98) − R(0)` ⇒ **`W₀` 之捕獲式（`W_near`）與首宗 `Rw` 之實算起點<u>不一致</u>**。
🛑 **本節⛔ 述其應為何**（單 `§五 b Q-4` 逐字）。

---

## §五　成因之判（**二情境二機制·皆已定位**）

| 情境 | `Δ` | 機制 | 源頭 |
|---|---|---|---|
| `3.5m` | `55.606` | **首宗之 `Rw` 自 `W=0` 起算，而閘期望自 `W₀=6.98` 起算** ⇒ 差 ＝ `R(W₀)`（`55.586`·殘差 `0.020` 為逐宗 `2dp` 捨入）| 鏈首宗 `628-42(1)`／`W₀` 捕獲式 |
| `0m` | `6.160` | **`W` 序列非單調**（`4.06 → 3.11`）⇒ 累積差額制之前提破 ⇒ 區段重複計 | **`628-41(1)`**（＝ `②-宗` 之重疊源頭）|

🔒 **`GB-154` 之「二情境二機制」獲實證**；`3.5m` 之「鏈逐界面全未斷而總和仍差」之詭**已解**
——其差**不在界面之間**，而在**鏈首宗自身之起算點**（逐界面 `Δ_self` 只看相鄰二宗之銜接，看不到首宗之起點）。

---

## §六　【驗收】

| 閘 | 實測 |
|---|---|
| `V-A` | ✅ 三受詞倉側 blob 前後逐位不變；`git status --porcelain` ＝ `0` 列 |
| `V-B` | ✅ 二 clone 皆 `RESULT: FAIL`、紅 **`24`**；計數式 `grep -c '🔴 FAIL'` 已出艙 |
| `V-C` | ✅ 三登記表 `deletions` ＝ `0` |
| `V-D` | ✅ 自誤 `MAX 320→321`／`GB MAX 153→154`／`K-9 MAX 29`（不變）；缺號仍 `[106]`、恆等式 `True` |
| `V-N` | ✅ 見 `§一`（三數分列＋雙屬＋讀不到之副檔名分布）|
| `V-P` | ✅ **四題全部有答**；`Q-2`／`Q-3` 之表為**逐宗完整**（各 `7` 宗·⛔ 取樣）；`Q-2` 之差集逐宗比對**阻斷點已具名** |
| `V-S` | 見下 |

🔒 **`V-S` 之自檢（以代稱行之·⛔ 逐字寫入）**：本報告內**⛔ 含修法主張**——
單 `§六 V-S` 所禁之三字系（祈使性之「改」字系／「議」字系／「正為」字系）**⛔ 逐字出現於本文**。
🔑 **通則（`-257R` 所立·本批沿用）**：凡以「某字樣之命中 ＝ `0`」為閘者，該字樣⛔ 逐字出現於受檢文件，含自檢句自身。
