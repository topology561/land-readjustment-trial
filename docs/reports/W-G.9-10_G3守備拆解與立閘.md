# `W-G.9-10`：`G3` 三項之**守備拆解與立閘**

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg910-g3`）
**基準**：`9feb48a`　**事前登記**：`docs/reports/W-G.9-10_前置登記.md`（commit `39931d9`）

> # ✅ **`L1`〜`L6` 全部成立；`X-1`〜`X-5` 皆未觸發。**
>
> 🔑 **本批之核心發現**：15 個從未執行之主判定中，有 **3 個之阻塞<u>不是資料不足，而是順序</u>**
> ——它們與擋路之 `raise` **同在一個 `try` 內、且排在其後**。
> ⇒ 把它們**搬出該 `try`** 即可執行。本批以**獨立夾具**達成，⛔ 未動 `run_verification.py`。
>
> ⚠️ **12＋ 項仍無人守**（守備移交清單見 §4）——⛔ 本批**未**為它們硬立閘。

---

## §1 `L1`〜`L6` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **L1** | ✅ **成立** | 15 個主判定站點於覆蓋資料中**皆未執行**；三項名目**皆出自 `except` 分支**（該三 `except` 站點確有執行）。附判別力自檢：`參數{…}` 站點判為**已執行** ⇒ 該式非恆判未執行 |
| **L2** | ✅ **成立** | 可計算者**非空**：**3 個**，逐項具名（`F.3 靜態閘`／`F.3 跨區段 fixture`／`G.2 只寫不讀`），且**皆經實跑證實**（⛔ 非由名目推想·§2） |
| **L3** | ✅ **成立** | 三閘之竄改自檢**兩態皆印且異色**：`G-1` 合成含 `calc_a_prime(` 之原始碼 ⇒ 轉紅；`G-2` `a′` 偏移 +1.0 ⇒ 轉紅；`G-3` 兩種違規（重複鍵／越界鍵）各造一次 ⇒ 皆轉紅 |
| **L4** | ✅ **成立** | 三閘於現況**皆綠**；🔒 依考古 69，**⛔ 未只證其綠**——每閘之「**會紅**」已由上開竄改自檢逐項證明 |
| **L5** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`。全檔**唯一非路徑差異** ＝ 新夾具之列。`W-G.9-7` 之對帳於本批仍 **PASS**（類 II／III 皆空） |
| **L6** | ✅ **成立** | 新閘之受詞 ＝ **命題本身之真偽**；既有 `except` 分支之受詞 ＝ **compute 失敗之訊息**（已由 `W-G.9-7` 凍存）⇒ **不同述詞、不重疊**。且判準常數**一律自原碼 AST 抽取**（⛔ 未抄寫）：`grep -c "sgC_rows" verify/fixture_g3_static_guards.py` ⇒ **0**——⇒ 本夾具**未持有任何一份判準鍵之複本** |

---

## §2 15 個主判定之四欄拆解（⛔ 逐一·鍵＝名目字面·⛔ 不用行號）

**共同之阻塞鏈**（自倉內 `verify/out/WG99_runall.log` 逐級上溯·⛔ 現查具名）：

```
[StepG0m] 🔴 結構閘 理論＝實跑 破（街廓 R1 left）
   ↓
[F.0] stepg_ctx[0m] 缺（trunk A 未成功？）
   ↓
[F.1] F.0 未成功…／[F.2] F.0 未成功…
   ↓
[F.3] F.2 未成功，F.3 跳過（存在性守衛）
```

**字樣錨**（⛔ 不以行號為錨·倉規「行號衛生」第 4 款；各附唯一性）：

| 錨 | 指令 | 命中 |
|---|---|---|
| `wd3` 之管線入口 | `grep -c "sg = run_step_g" verify/wd3_fragment_geom.py` | **1** |
| `F.3` 存在性守衛 | `grep -c "F.2 未成功，F.3 跳過" verify/run_verification.py` | **1** |
| `G.2` 上游守衛 | `grep -c "上游世代未產出" verify/run_verification.py` | **1** |
| `F.3` 靜態閘站點 | `grep -c "F.3 靜態閘（wf_f3 不呼叫" verify/run_verification.py` | **1** |

🔒 **判別力自檢**：寬字樣 `grep -c "run_step_g" verify/wd3_fragment_geom.py` ⇒ **2**
⇒ 「唯一命中」之檢查**非恆為 1**。

---

### 2-1 `W-D.3 碎片`（2 個）

| # | 名目字面 | ① 命題 | ② 所需資料 | ③ 阻塞源（現查具名） | ④ 可計算？ |
|---|---|---|---|---|---|
| 1 | `W-D.3 碎片幾何/三分類（v3）` | `wd3_fragment_geom.compute()` 之 `geom_rows` 與 v3 基準 `wd3_fragment_geom.csv` **逐格相同**（鍵＝情境、暫編地號） | `compute()` **自建全管線**：`harvest` → `build_pipeline` → `build_ownership` → `build_build_parcels` → `run_corner_pk` → **`run_step_g`**；＋ v3 基準 CSV | `wd3_fragment_geom` 內之 `run_step_g` 呼叫 → `stepg_pipeline` **結構閘 raise**「理論＝實跑 破」。倉內證據：`[碎片] compute 失敗：🔴 結構閘 理論＝實跑 破…` | **否** |
| 2 | `W-D.3 碎片逐邊CAD（v3）` | 同上，`edge_rows` vs `wd3_fragment_edges.csv`（鍵同） | 同上 | 同上（**同一次** `compute()` 呼叫，二判定共命運） | **否** |

### 2-2 `W-F F.3`（12 個）

| # | 名目字面 | ① 命題 | ② 所需資料 | ③ 阻塞源 | ④ 可計算？ |
|---|---|---|---|---|---|
| 3 | `F.3 靜態閘（wf_f3 不呼叫 calc_a_prime／三廢）` | `verify/wf_f3.py` **原始碼**中不出現 `calc_a_prime(`／`_allocate_three_tier_v1(`／`_allocate_tier2_tier3_geometric(`／`_inflate_a_for_orphan(` | **僅** `verify/wf_f3.py` 之原始碼 | 🔴 **非資料不足，係<u>順序</u>**：同一 `try` 之首行守衛 `F.2 未成功，F.3 跳過（存在性守衛）` **先 raise** | ✅ **是** |
| 4 | `F.3·公設調配{tag}` | `wf_f3.compute()` 之 `conv_rows` 與 `F.3_公設調配_退縮{tag}.csv` 逐格相同 | `_ctx`（trunk A）＋ `_f2`（trunk C 終態） | 同 #3 之守衛（`_f2_ok` 為 False ⇐ 上溯至結構閘 raise） | 否 |
| 5 | `F.3·轉7-4{tag}` | `to74_rows` vs 基準逐格相同 | 同上 | 同上 | 否 |
| 6 | `F.3·G值{tag}（trunk D 終態）` | `g_tab` vs 基準逐格相同（鍵＝所屬街廓、暫編地號） | 同上 | 同上 | 否 |
| 7 | `F.3·池流向{tag}` | `pool_rows` vs 基準逐格相同 | 同上 | 同上 | 否 |
| 8 | `F.3 零遺漏{tag}（59＝併入{…}＋轉7-4{…}）` | `anchors` 之 `n_pub==59 ∧ n_in==35 ∧ n_74==24` | 同上（`anchors` 出自 `compute()`） | 同上 | 否 |
| 9 | `F.3 結構永久閘{tag}（trunk D 守恆綠…）` | `verdict_all_green` 為真 **且** `cons_resid ≤ cons_tol` | 同上 | 同上 | 否 |
| 10 | `F.3 位次序不變{tag}` | `pos_viol` 為空 | 同上 | 同上 | 否 |
| 11 | `F.3 五則①切半5筆·{…}灌入項{tag}` | `straddle == sorted(wf_f3.STRADDLE)` 且 `n_inject_items == 40` | 同上 | 同上 | 否 |
| 12 | `F.3 舊帳重測 628-18(2) a={…}==477.91（…）` | `anchors["a_628_18_2"] == 477.91` | 同上 | 同上 | 否 |
| 13 | `F.3 轉7-4 9群（含 G025 邊界…）` | `groups_74 == ["G003","G012","G013","G015","G016","G024","G025","G028","G031"]` | 同上 | 同上 | 否 |
| 14 | `F.3 跨區段 fixture（a→b a′={…}≠a·ratio≠1）` | `wf_f3.fixture_cross_zone_f3(snapshot)` 之 `ratio_ne_1` 為真 **且** `\|a′ − expect\| < 0.01` | **僅** `snapshot["財務接線_v3"]["重劃前區段_面積單價"]`（純算術） | 同 #3 之守衛（**順序**） | ✅ **是** |

### 2-3 `W-G G.2`（1 個·**單一站點含兩子集**）

| # | 名目字面 | ① 命題 | ② 所需資料 | ③ 阻塞源 | ④ 可計算？ |
|---|---|---|---|---|---|
| 15a | `W-G G.2 世代幾何曝出契約＋只寫不讀（…）`<br>之 **(a) 曝出契約** | `v3/f0/f2/f3/E` 各代原始列**有逐宗 `cut_coords`**；`f1.reshape_polys` 非空、`f1.wedge_coords` ≥3、`f4.reshape_polys` 為 `dict` | `_ctx`／`_f0`／`_f1`／`_f2`／`_f3`／`_f4` **六代全部成功** | `上游世代未產出：['trunkA(_ctx)','F.0','F.1','F.2','F.3','F.4']` 之 raise（該 raise 係 `F-5` 刻意所立之**有意義紅字**） | **否** |
| 15b | 同站點之 **(b) 只寫不讀（靜態圍欄②）** | 新曝鍵於 `wf_f1`／`wf_f2`／`wf_f4` **各恰 1 次**；`wf_f0`／`wf_f3`／`stepg_pipeline`／`selection_pipeline` **零越界** | **僅**上開七個引擎檔之原始碼 | 同 15a 之 raise（**順序**：該 raise 在靜態圍欄**之前**） | ✅ **是** |

---

## §3 小結

| 分類 | 數 | 項 |
|---|---|---|
| **可計算**（本批已立閘） | **3** | #3 `F.3 靜態閘`／#14 `F.3 跨區段 fixture`／#15b `G.2 只寫不讀` |
| **不可計算**（守備移交） | **12＋** | #1、#2（`W-D.3` ×2）／#4〜#13（`F.3` ×10）／**#15a**（`G.2` 曝出契約·同站點之另一半） |

🔴 **三個可計算者之共同特徵**：其阻塞**不是資料不足，而是<u>順序</u>**
——它們與擋路之 raise 在**同一個 `try` 內、且排在其後**。
⇒ 把它們**搬出該 `try`** 即可執行；本批以**獨立夾具**達成，⛔ 未動 `run_verification.py`。

---

## §3 三閘之四項聲明（⛔ 逐條·第 2、4 項不得留空）

**已就地寫入** `verify/fixture_g3_static_guards.py` 之 docstring
（字樣錨：`grep -c "四項聲明" verify/fixture_g3_static_guards.py` ⇒ **1**；
🔒 判別力自檢：寬字樣 `grep -c "聲明" …` ⇒ **2** ⇒ 唯一性檢查**非恆為 1**）。

⚠️ **一項自誌**：本節首版曾寫該寬字樣為 `4`——**未驗即出艙**，實測為 `2`，已就地更正。
⇒ 「數字錨引倉檔·禁憑記憶出艙」之再現；⛔ 判別力自檢之**數值本身**亦須現查。

| # | 答 |
|---|---|
| **1. 守的是哪個子集？** | `G-1`／`G-2` ＝ **原判定之全部**（其本身即不依賴管線）；`G-3` ＝ 原判定之 **(b) 只寫不讀靜態圍欄**（原站點含兩子集） |
| **2. 🔴 放棄了哪個子集？其後果？** | 放棄 `G.2` 之 **(a) 曝出契約**——各代原始列**逐宗 `cut_coords` 是否存在**、`f1/f4` 整形座標是否非空。**後果**：若某代**產得出資料但漏曝座標**，本閘**看不見**。另放棄其餘 **12 個**主判定（見 §4） |
| **3. 與 `except` 分支重疊？** | ⛔ **不重疊**（見 `L6`） |
| **4. 另案修好之後會怎樣？** | **不變、仍綠**——三受詞皆與管線無關（純檔案讀取／純快照算術）⇒ ⛔ **不會假紅**（`X-4` 未觸發）。⚠️ 屆時原主判定亦將復活 ⇒ 同一命題有兩處判定，**惟二者同源**（AST 抽取）⇒ **應併同檢討本夾具是否退場** |

---

## §4 守備移交清單（**不可計算之 12＋ 項**·⛔ 未硬立閘）

🔒 **案由**：`W-G.9-7` 之對帳只凍**失敗原因**；下列命題**從未被正面驗證**。
⇒ 本清單是它們**唯一的守備記錄**。

**「④ 在此之前，誰在守」之否定性宣稱**（三項射程·全清單共用）：

- **【目錄射程】** `run_all` 自動流程之執行單元 ＝ `verify/run_verification.py` ＋
  全部 `verify/fixture_*.py` ＋ `verify/run_all.py`，母體 **14 檔**。
  ⚠️ ⛔ 不含 `verify/probes/**`、`verify/wg_g*_smoke.py`、`verify/wg_g3.py`、`verify/tools/**`
  ——現查皆不在任何自動流程內。
- **【字樣射程】** 各命題之**領域語意詞**（⛔ 不預設符號名）：
  `碎片幾何`／`碎片逐邊`／`公設調配`／`轉7-4`／`trunk D`／`零遺漏`／`位次序`／`五則`／`628-18`／`曝出契約`。
- **【連接方式】** 各式**獨立裸 grep**、逐式記命中檔（⛔ 無距離窗）。

**結果**：上列九式於**夾具／`run_all`** 之命中**皆 0**；
唯 `曝出契約` 命中 `verify/fixture_g3_static_guards.py`——**係其 docstring 之放棄清單**
（明載本閘**不守**該子集），⛔ **非**守備。
🔒 **判別力自檢**：以必然命中之字樣 `竄改自檢` 重算 ⇒ **3 檔** ⇒ 上式**非恆 0 命中**。

⇒ **下列 12＋ 項，答案一律為「無人在守」。**

---

| # | ① 原命題 | ② 阻塞源（現查具名） | ③ 解阻後第一件該做的事 | ④ 在此之前誰在守 |
|---|---|---|---|---|
| 1 | `W-D.3 碎片幾何/三分類（v3）`：`geom_rows` vs v3 基準逐格相同 | `wd3_fragment_geom` 內之 `run_step_g` → 結構閘 raise（`grep -c "sg = run_step_g" verify/wd3_fragment_geom.py` ⇒ 1） | 該判定復活後**先看它是綠是紅**——⚠️ 其基準係**波前產物**，結構閘修好後幾何會變 ⇒ **紅是預期**，須與重烤時點協調，⛔ 不得逕自重烤 | 🔴 **無人** |
| 2 | `W-D.3 碎片逐邊CAD（v3）`：`edge_rows` vs 基準逐格相同 | 同上（**同一次** `compute()`·二判定共命運） | 同上 | 🔴 **無人** |
| 3 | `F.3·公設調配{0m,3.5m}` | `F.2 未成功，F.3 跳過（存在性守衛）` ⇐ 上溯 `F.0 → stepg_ctx[0m] 缺 → 結構閘 raise` | F.3 復活後，**四張表（調配／轉7-4／G值／池流向）× 兩情境** 一次全紅或全綠——⛔ 先確認是「基準過期」還是「算錯」，二者處置相反 | 🔴 **無人** |
| 4 | `F.3·轉7-4{0m,3.5m}` | 同上 | 同上 | 🔴 **無人** |
| 5 | `F.3·G值{0m,3.5m}（trunk D 終態）` | 同上 | 同上；**此項有土地後果**（逐宗 G 值） | 🔴 **無人** |
| 6 | `F.3·池流向{0m,3.5m}` | 同上 | 同上 | 🔴 **無人** |
| 7 | `F.3 零遺漏{tag}`：`n_pub==59 ∧ n_in==35 ∧ n_74==24` | 同上 | ⚠️ 此為**計數型不變量**——復活後若不符，**先查是母體變了還是分流變了**，⛔ 不得逕改期望數 | 🔴 **無人** |
| 8 | `F.3 結構永久閘{tag}`：`verdict_all_green ∧ cons_resid ≤ cons_tol` | 同上 | 🔴 **最優先**——守恆式為本專案最高鐵律；復活後**先驗它**，其餘 F.3 判定在它綠之前無意義 | 🔴 **無人** |
| 9 | `F.3 位次序不變{tag}`：`pos_viol` 為空 | 同上 | 同 #8（位相不變＝正典鐵律·權威序第 1 級 `docs/rulings/`） | 🔴 **無人** |
| 10 | `F.3 五則①切半5筆·灌入項{tag}`：`straddle == sorted(wf_f3.STRADDLE) ∧ n_inject_items == 40` | 同上 | 復活後若不符，先查 `STRADDLE` 常數與實際切半集合何者改變 | 🔴 **無人** |
| 11 | `F.3 舊帳重測 628-18(2) a==477.91` | 同上 | 單一錨值；復活後不符即為**土地後果**，⛔ 不得視為顯示差 | 🔴 **無人** |
| 12 | `F.3 轉7-4 9群`：`groups_74 == [G003,G012,G013,G015,G016,G024,G025,G028,G031]` | 同上 | 復活後若群集改變，**先查 G025 邊界**（建地 F.0 梯3 釋池）——該項為已知敏感點 | 🔴 **無人** |
| 13 | `W-G G.2` 之 **(a) 曝出契約**：各代原始列有逐宗 `cut_coords`；`f1/f4` 之整形座標非空 | `上游世代未產出：['trunkA(_ctx)','F.0','F.1','F.2','F.3','F.4']` 之 raise | 六代全部成功後，該子集**自動恢復**；⚠️ 屆時 `fixture_g3_static_guards` 之 `G-3` 與原判定**同守只寫不讀**一半 ⇒ **應併同檢討本夾具是否退場** | 🔴 **無人** |

---

### 4-1 🔴 一項須連帶處置之事實

三個可計算者之阻塞**不是資料不足，而是<u>順序</u>**——它們與擋路之 raise 在**同一個 `try` 內、且排在其後**。
⇒ 同樣之結構**可能存在於其他期望 FAIL 項**（本批只拆了 `G3` 三項）。
**建議**：後批以同一支拆解器（`verify/tools/wg910_g3_dissect.py`）掃**全部 22 名目**，
把「被順序擋住之靜態判定」一次撈完。⛔ 本批未做。

---

## §5 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未動 `verify/run_verification.py`**（`git diff --numstat` **空輸出**）——含三項之
   `try`／`except` 結構與 12 列上限。
2. ⛔ **未修任何既有 FAIL**（結構閘另案、`W-G G.1` 之 `KeyError: '0m'`）。
3. ⛔ **未解除明細截斷**（屬後批）。
4. ⛔ **未為 12＋ 項不可計算者硬立閘**——改出**守備移交清單**（§4）。
5. ⛔ **未重凍任何凍存物**、未改期望 FAIL 名單。
6. ⛔ **未追改**任何歷史記錄之舊行號錨與舊雜湊。
7. ⛔ **未動生產碼**（`app.py` `git diff --numstat` **空輸出**）；
   ⛔ 未動三支端到端複本原檔、⛔ 未動 `verify/wv_reconcile.py`。
8. ⛔ **未掃其餘 19 個期望 FAIL 名目**是否也有「被順序擋住之靜態判定」（見 §4-1 之建議）。

---

## §6 後批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **0** | 🛑 **`W-G.9-9` 之 `K5` 判準裁定**（「未新增」取代「歸零」） | 前批已上呈·未裁則錨稽核永遠卡在不可達之 0 |
| **1** | **`W-G G.1` 之上游 `_ctx["0m"]`／結構閘另案** | **一修即解 12＋ 項之阻塞**（§4 之整張表）；且三支端到端複本將跑得更遠 ⇒ 其夾具依設計轉紅 ⇒ 屆時一併更新凍存 |
| **2** | **以拆解器掃全部 22 名目**，撈出其餘「被順序擋住之靜態判定」 | 本批只拆了 `G3` 三項；同一結構**可能存在於其他名目**（§4-1）。成本低、可立即做 |
| **3** | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」，無人接手者另立不受上游影響之閘 |
| **4** | **解除明細截斷（12 列上限）** | 可消滅該盲點，但**必使現行名單全面失效、須重凍** ⇒ 須與序 1 之重烤時點協調 |
| **5** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

⚠️ 序 1／4 之前置含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §7 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ python verify/tools/wg910_g3_dissect.py | tail -4
⇒ 三項合計：主判定站點 **15** 個｜**從未執行 15** 個
🔒 判別力自檢：`參數{…}` 站點（513）曾執行 ⇒ True　⇒ 上式**非恆判未執行**

$ python verify/fixture_g3_static_guards.py | tail -3
✅ 夾具 PASS（rc=0）

$ grep -n "fixture_g3_static_guards" verify/out/WG910_runall.log
44:  ✅ 末端夾具 fixture_g3_static_guards.py（🆕 W-G.9-10 G3 三項中不依賴管線之主判定…）rc=0

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG99_runall.log)        <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG910_runall.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG910_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG910_runall.log ⇒ 22

$ git diff --numstat -- app.py verify/run_verification.py verify/wg_g1_smoke.py       verify/wg_g2_smoke.py verify/wg_g3.py verify/wv_reconcile.py   ⇒ （空輸出）
```

⚠️ **依倉規（不可變文件不記可變值），本報告⛔ 不記任何可變檔之雜湊。**

---

## §8 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `39931d9` — 事前登記 ＋ 拆解器入倉（⛔ 零立閘、零碼變更於受測物）
- **B** `fc68cf9` — 拆解量測 ＋ 三閘 ＋ 掛入 ＋ 長跑 log（⛔ 零文件結論）
- **C** 本報告 ＋ 守備移交清單 ＋ `GB` 登記 ＋ 考古節 70
