# 🧾 `W-G.9-178R`　五裁落地 ＋ `G.3` 射程停機之解除判斷

**受單**：`W-G.9-178`（發單 claude.ai·`2026-08-30`）　**執行**：CC
**基座（開工閘實測·⛔ 非採信單所載）**：`666e0bd4640be4b2980b189198e5ca86eb7d5022`
**性質**：⛔ **零生產碼**·⛔ 零管線·鑄 自誤 `215`／`216` ＋ `GB-122` ＋ 待落地補列 ＋ `MEMORY.md` 提案·**⛔ 無新探針**

> 🔒 本報告之一切計數，其**框 ＋ 母體 ＋ 母體之態**皆同格標示（`戒 29`：一張表只准一個框）。

---

## `§零`　開工閘（`S-1` 八項·逐位對拍）

| 項 | 期望 | 實得 | 判 |
|---|---|---|---|
| `origin/wip/s1-endpart` | `666e0bd4640be4b2980b189198e5ca86eb7d5022` | 同 | ✅ |
| `app.py` blob | `68f7bfab610ec78688b9d7322ae4bc2aac74493e` | 同 | ✅ |
| `app.py` 三數 | `1352132`／`22922`／`7c290b6f55f83a24` | 同（`wc -l` `22921`·差恰 `1`） | ✅ |
| `CLAUDE.md` | `69552`／`751`／`5c013ba334464def` | 同 | ✅ |
| 自誤登記表 | `371299`／`4144`／`69039dedbfcc70b8` | 同 | ✅ |
| `W-G.4` 泛用阻塞項登記表 | `494687`／`3070`／`2a5238093df26487` | 同 | ✅ |
| `W-G.9-177R` 報告 | `23895`／`284`／`a52d4bb43a731bc7` | 同 | ✅ |
| `-177` 探針 | `23851`／`503`／`32c07131d7b7d225` | 同 | ✅ |
| `-177` 探針 log | `7054`／`89`／`dc22fe5ba3d2f9e7` | 同 | ✅ |

⇒ **七檔三數 `7/7`**（框 ＝ `splitlines()`·CRLF 皆 `0`）。clone ＝ `C:\w178a`（`8` 字元）·`autocrlf=false`·自檢器置於 `C:\w178x`（clone 外）。
🔒 **單身完整性**：`SELF_SHA256` ＝ `42f0d90f1cf8eb6b26b1d6b136964211cfc5c92ee08122bbe0095dc225634f65` ✅ **二框皆 MATCH**。

---

## `§一`　`§二` 七款之**逐字引錄**（`GB-121` 施行式）

🔒 **來源管線**：KL 於聊天視窗**貼入之 `.md` 檔**（`W-G.9-178_施工單_五裁落地與G3停機解除判斷.md`·`21728` B／`214` 列·CRLF `0`）。**標 `【單】`**——該檔⛔ 不在倉內。

| 款 | **逐字原文（【單】·⛔ 非摘要）** | 停機？ | 實得 | 判 |
|---|---|---|---|---|
| `X-1` | 「凡須修改 `app.py` 一字（含新增 `print`／註解）方能取得之量 ⇒ **停機上呈**。本單⛔ 不授權任何生產碼變更。」 | 🛑 是 | 本批**全程純閱讀 ＋ 既有 log**，⛔ 未跑任何探針；`app.py` 期初 ＝ 期末 blob | ✅ **⛔ 未觸** |
| `X-2` | 「凡須請 KL 實跑、或須跑 `run_all`／任何長跑者 ⇒ **停機上呈**。」 | 🛑 是 | `M-C-1`〜`M-C-3` **全以純閱讀 ＋ 倉內既有凍存檔完成**；⛔ 未跑 `wg_g3.py`、⛔ 未跑 `run_all`、⛔ 未請 KL | ✅ **⛔ 未觸** |
| `X-3` | 「自誤 `215`／`216` 與 `GB-122` 之取號現查，**二形分報**之三數皆須 `0`／`0`／`0`，且對照組須非 `0`，且 `S-4` 人造受詞（依 `§零` 補款另取）須 `0`／`0`／`0`。任一不合 ⇒ **先判量測器紅並停機**。」 | 🛑 是（量測器紅時） | 見 `§二`／`§三` | ✅ **⛔ 未觸** |
| `X-4` | 「本批所鑄之號，其標題形須**逐字取自倉內既有最末一則**（`常規四（九）四`）；凡本單所供之形與倉內範本不符 ⇒ **以倉內範本為準**、逐字回報該不符，**⛔ 不停機**。」 | ⛔ 否 | 單所載四出處（`:4108`／`:4129`／`:3059`／`:3067`）經 `sed` 逐字覆核**相符** | ✅ **⛔ 無不符** |
| `X-5` | 「`M-C-2` 之三支若⛔ 不窮盡 ⇒ 具名第四支、續辦，**⛔ 不停機**。」 | ⛔ 否 | 🔴 **⛔ 不窮盡** ⇒ 已具名**第四支 (丁)**（`§四-3`） | ✅ 依款續辦 |
| `X-6` | 「受檔 `numstat` `deletions` **逐列 `0`**；`app.py` 期末 blob ＝ `68f7bfab610ec78688b9d7322ae4bc2aac74493e`。」 | 🛑 是 | 見 `§六-1` | ✅ **⛔ 未觸** |
| `X-7` | 「🛑 **停機款觸發一律先停機**（`W-G.9-177 §六 一` 之施行式）：⛔ 不得先辦後呈；於停機回報中併陳擬辦之意見。」 | 🛑 是 | 本批**⛔ 無任何停機款被觸發** ⇒ 無適用之機會 | ✅ |

---

## `§二`　`N-1`：自誤 `215`／`216` 之落地

🔒 **取號現查**（CC·態 `666e0bd4…`·**期初** tree·母體 ＝ 全倉 `.md` **`455`** 檔·量測器置於 clone 外 ⇒ 母體自然自扣，經現查母體內含 `178` 之檔名 ＝ `[]`）：

| 框 | 定義列式（逐字） | 相異 | MAX | 缺號數 | 自檢 |
|---|---|---|---|---|---|
| ①單檔（自誤登記表） | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `179` | **`214`** | `35` | `214−35==179` True |
| ②全倉 `455` 檔 | 同 | `183` | **`214`** | `31` | `214−31==183` True |

**意指占用（錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·🛑 二形<u>分報</u>·三數 ＝ 命中列數／出現次數／檔數·母體 ＝ 框②）**：

| 號 | `自誤 N` | `` 自誤 `N` `` | 判 |
|---|---|---|---|
| `215` | `0`／`0`／`0` | `0`／`0`／`0` | ✅ |
| `216` | `0`／`0`／`0` | `0`／`0`／`0` | ✅ |
| `214`（對照組） | `1`／`1`／`1` | `2`／`2`／`2` | ✅ 非零 ⇒ 量測器非紅 |
| 🆕 **`8811`**（`S-4` 人造受詞） | `0`／`0`／`0` | `0`／`0`／`0` | ✅ 🛑 **本則出艙後即作廢** |
| 🔴 `9997`（**舊**人造受詞） | `1`／`1`／`1` | — | 🔴 **失效坐實**（`§六 六` 之案由） |

🔒 「未入倉／未鑄／待鑄」共現列：`自誤 215`／`216` 皆 **`0`**。
🔒 **payload 逐字入倉**（自誤登記表**末端追加**·`+4208` B／`+30` 列·**嚴格前綴 True**·CRLF `0`）。塊內文字以 `sed -n '102,111p'`／`'117,126p'` **自單身抽出**（`常規七 一`），並經 **`常規七 三` 覆核**：raw 逐位含於 payload ⇒ `True`／`True`。

### `§二-1`　**常規四（九）** 二項機檢

| 項 | 實得 | 判 |
|---|---|---|
| ① 形之自證 | 定義列式掃 payload ⇒ `['215','216']`；標題形逐字 ＝ `` ### 自誤 `215`（發單側·落地窗）：… ``／`` ### 自誤 `216`（…）：… `` | ✅ |
| ② MAX 推進·框①單檔 | 相異 `181`／MAX **`216`**／缺 `35`；二號⛔ 不在缺號集 | ✅ |
| ② MAX 推進·框②全倉 `455` 檔 | 相異 `185`／MAX **`216`**／缺 `31`；二號⛔ 不在缺號集 | ✅ |

---

## `§三`　`N-2`：`GB-122`

🔒 **正典三框**（逐字·出處 `docs/reports/W-G.4_泛用阻塞項登記表.md:3005`·母體 ＝ **該登記表單檔**）：式表 `` ^\| *\*\*`?GB-N`?\*\* `` **命中列數 `116`**／式A `` ^## `GB-N` `` `3`／式B `` ^### `GB-N` `` **命中列數 `33`**（相異 `32`）⇒ **聯集 相異 `119`／MAX `121`／缺 `[12, 87]`**（`121−2==119` True）。

| 號 | 錨定框（母體 ＝ 全倉 `455` 檔） | 判 |
|---|---|---|
| `GB-122` | `0`／`0`／`0` | ✅ 未占用 |
| `GB-121`（對照組） | `12`／`14`／`2` | ✅ 非零 ⇒ 量測器非紅 |
| 🆕 **`GB-883`**（`S-4` 人造受詞） | `0`／`0`／`0` | ✅ 🛑 **本則出艙後即作廢** |
| 🔴 `GB-997`（**舊**人造受詞） | `2`／`2`／`2` | 🔴 **失效坐實** |

✅ **與單 `§一 N-2` 之預查逐位相符**（三框聯集 相異 `119`／MAX `121`／缺 `[12,87]`；`GB-122` `0/0/0`）。
🔒 **`X-4` 覆核**：單 `§三` 所供之四出處經 `sed` 逐字現查——`:4108`＝`` ### 自誤 `213`（…） ``／`:4129`＝`` ### 自誤 `214`（…） ``／`:3059`＝`` ### `GB-121` 🆕 ``／`:3067`＝`` | **`GB-121`** 🆕 | … `` ⇒ **皆與倉內既有最末一則相符**，⛔ 無不符可報。
🔒 **payload 入倉**（`W-G.4` 登記表**末端追加**·`+3468` B／`+14` 列·**嚴格前綴 True**·CRLF `0`）；塊內七欄之文字以 `sed -n '132,139p'` 自單身抽出，**`常規七 三` 覆核七段逐位含於 payload ⇒ 全 `True`**。

### `§三-1`　**常規四（九）** 二項機檢（`GB`）

| 項 | 實得 | 判 |
|---|---|---|
| ① 形之自證 | 式表 `['122']`／式A `[]`／式B `['122']` ⇒ **二框命中** | ✅ |
| ② MAX 推進 | 三框聯集 相異 `120`／MAX **`122`**／缺 `[12, 87]`；`122` ⛔ 不在缺號集；自檢 `122−2==120` True | ✅ |

---

## `§四`　`N-3`：🔑 **`G.3` 射程停機之判**（主受詞）

🛑 **全節以<u>純閱讀</u>完成**——⛔ 未跑 `wg_g3.py`、⛔ 未跑 `run_all`、⛔ 未請 KL 實跑（`X-1`／`X-2` 未觸）。
🛑 **`grep` 全程⛔ 未接 `head`**（`W-G.9-177R §四-2` 之自捕）——負存在斷言尤然；本節之負存在斷言（`run_all` 對 `wg_g3` 之命中）已附**對照組**。

### `§四-1`　`M-C-1` 射程之逐字現查

🔒 **檔** ＝ **`verify/wg_g3.py`**（⛔ 非單所載之 `verify/tools/wg_g3.py`；`ls verify/tools/ | grep -i 'g3\|g_3'` 得 `k8_seg3_A_recon.py`／`k8_seg3_A_runall.py`／`wg910_g3_dissect.py`·**皆非**）。三數 ＝ `8764` B／`174` 列／`sha16 429c8b6ef5bf4802`。

🔒 **`自誤 161` 防線——先列全部節／函式（AST·⛔ 非關鍵詞搜尋）再逐一判**：

| 行 | 名 | 判（是否界定射程） |
|---|---|---|
| `:17` | `HERE`（常數） | ⛔ 否（路徑） |
| **`:27`** | **`GEN_TABLES`（常數）** | ✅ **是——射程之逐字定義** |
| `:63` | `_serialize` | ⛔ 否（序列化為 CSV bytes） |
| **`:78`** | **`_seed_ctx`** | ✅ **是——界定<u>種子</u>之來源** |
| **`:107`** | **`main`** | ✅ **是——界定<u>比較端</u>與 baseline** |

（巢狀 `def` ＝ **`0`** 個。）

**(1) 射程（`GEN_TABLES` `:27-61`）**：`f0`〜`f4` 五世代共 **`22`** 表 × `2` 情境。其中 **`pool_rows` 於五世代<u>皆</u>在列**（f0「池差」／f1「池驗證」／f2·f3·f4「池流向」）。

**(2) 比較端（`main` `:119-133`）**：`got` ＝ `gens[gkey][tag][rkey]`，即 **`wf_f0.compute(cbt)`〜`wf_f4.compute(...)` 之回傳**；`base` ＝ `os.path.join(bdir, f"{prefix}_{nm}_退縮{tag}.csv")`，`bdir` ＝ `rv.F0DIR`〜`F4DIR`（`run_verification.py:47-51`）。判準 ＝ `rv.diff_rows`（逐格·2dp）**且** `got_bytes == base_bytes`（byte-perfect）。

**(3) 種子（`_seed_ctx` `:78-105`）**：
```
sg = run_step_g(ns, fake_st, list(cb_by.values()), cad, snapshot, params, build, winners, forced, setback)
seed.update({"f3_G_values": sg["g_rows"], …, "f3_wd2_pool_diag": sg["pool_diag"], …})
return ns["_build_wf_ctx"](seed, tag, ns["__file__"])
```

**(4) 🔴 「Step-G 之池」是否在射程內 ＝ <u>是</u>**：`wf_f0.py:425-432` 逐字
```
poolA = {lbl: round(float(v["池總=幾何剩餘(㎡)"]), 2) for lbl, v in c["poolA"].items()}
pool_rows = [{"情境": tag, "街廓": lbl, "池_v3(㎡)": poolA[lbl], "池_F.0(㎡)": poolB[lbl], …}]
```
而 `c["poolA"]` 之來源經 `app.py:13422` 逐字坐實 ＝ `"poolA": _need("f3_wd2_pool_diag")` ⇒ **即 `_seed_ctx` 中 `run_step_g` 之 `pool_diag`**。
⇒ 🔴 **單所擬之候選 (乙)「`G.3` 射程本即不及 Step-G 之池」<u>字面 ⛔ 不成立</u>**——`池_v3(㎡)` 欄確在射程內（惟為 **2dp**）。

**(5) 🔑 但二端<u>皆為 harness 側</u>**：
- `got` 側：種子由 **`run_step_g`**（harness）產生 ⇒ 其池 ＝ **shim 之池**；`wg_g3.py` docstring `:8` 逐字「**種子＝harness native 管線**（禁改 stepg/引擎），組真 app session → harvested `_build_wf_ctx`」。
- `base` 側：`F0DIR`〜`F4DIR` 之 CSV 由 **`run_verification.py:420` 之 `_bake_csv(got_rows, …)`** 烤出（全倉 `_bake_csv` 之呼叫端**正面列舉**僅此 `1` 處）。
- ⇒ 🔑 **`app.py:20892` 之池呼叫端（在 `main()` 內）⛔ 從不參與 `G.3` 之任一端**。
- ⇒ 🔑 **`R2` 之 `0.1171`（＝ <u>app-vs-shim</u> 之差）結構上⛔ 不可能被 `G.3` 抓到——縱使 `G.3` 全綠亦然。**

**(6) 🔑 且 `G.3` 之執行於 `R2` 前即中止**：【倉】`verify/out/WG99_端到端複本_終止點凍存_投影.txt`（`3631` B／`39` 列／`sha16 2234898b216e7b32`）逐字：
```
🔴 FAIL  wg_g3.py
    | File "verify/stepg_pipeline.py", line 995, in run_step_g
    | RuntimeError: 街廓 R2 抵費地計算失敗：🔴 ②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 3.1129（(宗數15−1)×0.005×深度44.47）——**超出捨入量子可解釋範圍＝另有病**，停
```
⇒ 於 `_seed_ctx` 之 `run_step_g` 即 raise ⇒ **⛔ 從未到達 `wf_f0.compute`** ⇒ `GEN_TABLES` **任一格從未被比對**。

**(7) 🔑 且 `wg_g3.py` 本不在任何自動流程內**：`verify/fixture_e2e_termination.py` docstring 逐字
> `verify/wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py` 皆為「app `_build_wf_ctx` → `wf_f0`→`f4` 全鏈」之端到端複本（其中 `wg_g3.py` 係 `CLAUDE.md` 所訂 **W-G 收官判準 ①** 之終驗工具），惟**三支皆不在任何自動流程內**（`W-G.9-6` §6）。若以 `rc=0` 為判準掛入，將新增**三個永紅項**。

🔒 **負存在斷言之證（⛔ 未接 `head`·附對照組）**：`grep -n 'wg_g3' verify/run_all.py` ⇒ **僅 `1` 列且係<u>註解</u>**（`:129`）；對照組 `grep -c 'wg_g1_smoke\|run_verification' verify/run_all.py` ⇒ **`11`**（非零 ⇒ 量測器非紅）。`run_all` 所掛者係 **`fixture_e2e_termination.py`**，其受詞逐字為「**三支之 `raise` 點 ＋ 例外型別 ＋ 例外訊息，與凍存者相符**」，**⛔ 非其綠否**。

### `§四-2`　`M-C-2` 三支之窮盡性自問（`戒 26`）

🛑 **逐字作答：三支 ⛔ <u>不</u>窮盡。**

| 支 | 單所擬之內容 | 判 |
|---|---|---|
| **(甲)** | `G.3` 曾失效（須逐 commit 二分指認肇因） | 🟡 **受詞須先澄清**——若「失效」指「可執行性由綠轉紅」則**成立**（`CLAUDE.md` 載其曾 ALL GREEN ＠`335f5f3`，今 raise 中止）；但**肇因已在倉內具名且已凍存**（`stepg_pipeline.py:995` 之 `②-宗 圍堵閘破[R2]`）⇒ **⛔ 不需二分** |
| **(乙)** | `G.3` 射程本即不及 Step-G 之池 | 🔴 **字面 ⛔ 不成立**——`wf_f0` 之 `pool_rows` 之 `池_v3(㎡)` 欄 ← `ctx["poolA"]` ← `f3_wd2_pool_diag` ⇒ **確在射程內** |
| **(丙)** | 二者並存 | 🟡 承 (甲)(乙) 之界定 ⇒ 亦不足以描述實情 |

🆕 **第四支 (丁)（`X-5` 之具名·⛔ 不停機）**：

> **`G.3` 之射程<u>含</u> Step-G 之池，但其<u>二端皆為 harness 側</u>**（`got` 之種子 ＝ `run_step_g`；`base` ＝ 自同族 harness 管線 `_bake_csv` 烤出）⇒ **`app` 路徑之池⛔ 從不進入其比較端** ⇒ **`R2` 之 app-vs-shim 差<u>結構上</u>不可能被 `G.3` 抓到**；且其**執行於 `R2` 前即中止**（凍存之終止點）、其**本不在任何自動流程內**（`W-G.9-6 §6`）。

⇒ 🔒 **(丁) 之三層互相獨立**：① 射程雖含池但二端同側（**結構性**）；② 執行中止（**現況**）；③ 不在自動流程（**制度**）。**任一層單獨即足以解釋「未擋下」**；三層並存 ⇒ 「`G.3` 失效」之提問**預設了一個⛔ 不存在之因果**。

### `§四-3`　`M-C-3` 判與其代價

🔒 **判 ＝ (丁)**（⛔ 非 (甲)／(乙)／(丙)）。

**逐 commit 二分 ⛔ 不必要之逐字依據（三條·任一獨立成立）**：
1. 🔑 **結構性**：`G.3` 二端皆 harness 側 ⇒ app-vs-shim 之差**本不可能**進入其比較端 ⇒ **⛔ 無「哪一批使其失效」可問**——該提問之預設（「`G.3` 本應抓到而未抓到」）**不成立**。
2. 🔑 **肇因已具名**：其執行中止之點與訊息**已逐字凍存**（`WG99_端到端複本_終止點凍存_投影.txt`·`W-G.9-28` 更新·`stepg_pipeline.py:995`）⇒ **⛔ 不需二分去找**。
3. 🔑 **已知之覆蓋洞**：`wg_g3.py` **本不在任何自動流程內**（`W-G.9-6 §6` 逐字）⇒ 「它沒擋下」係**已具名之現況**、⛔ 非退化；且 `run_all` 已掛 `fixture_e2e_termination.py` 守其**終止點**（設計意圖逐字：「結構閘另案修好後三支會跑得更遠 ⇒ 終止點改變 ⇒ 本夾具**轉紅** ⇒ 逼人來看並更新凍存。**這是特性，不是缺陷。**」）。

🛑 **本單⛔ 不解除停機**——解除之逐字依據已備妥如上，**由發單側裁**。

### `§四-4`　🔒 **`VR-082` 射程外之具名量**（本判**看不見**什麼）

1. ⛔ **看不見**「`G.3` 若能跑完，其 `22` 表 × `2` 情境是否仍逐格全綠」——**⛔ 未跑**（`X-2` 禁長跑）。本判只證「即使全綠亦抓不到 app-vs-shim 之差」，**⛔ 未證其會全綠**。
2. ⛔ **看不見**「`335f5f3` 當時之 ALL GREEN 是否已含 `R2` 之該差」——須跨態重跑（`VR-083 一`：跨態量測須用該態自身之 `verify/`）。
3. ⛔ **看不見**「app 路徑之池是否另有**其他** harness 對拍器在守」——本判之母體**僅 `verify/wg_g3.py` 一檔**；`run_verification` 之 trunk B 亦用同組 baseline，其是否構成另一守備**⛔ 未查**。
4. ⛔ **看不見** `②-宗 圍堵閘破[R2]` 之肇因批次（`宗-宗重疊 45.9766` 之來源）——屬 `GB-118` 族·另批。
5. ⛔ **看不見** `池_v3(㎡)` 之 **2dp** 是否會吞掉更小之差——本案之 `5.2274` 於 2dp 為 `5.23`（不會被吞），但 **`< 0.005`** 之差會被吞，**⛔ 未量**其他街廓是否有此量級之差。

---

## `§五`　`N-4`：待落地補列 ＋ `MEMORY.md` 提案

### `§五-1`　待落地補列（依 `§六 五` 之裁）

🔒 **族②③ 之切換**（`_W_prev_*`／`_select_pool_slot['b']` 由舊式 `buf·cos_dn` 改為 `_mp_base_W0`）**歸 `W-G.9-5`（contract 側）**，其正典依據 ＝ `app.py:9107-9122` 之逐字（三級權威皆作 SIDE 線中點；舊式 ＝ 新式 − `W_0`，相等**當且僅當** `W_0 = 0` 而正典明載其**照實可負**）。
⇒ 🔒 **⛔ 非待裁項、⛔ 非失效，係<u>已裁而未遷移</u>**；⛔ 不上呈 KL（`常規三` 修法：`【查有】`）。
⚠️ 🛑 **本項⛔ 未入倉為清單之一列**——`CLAUDE.md` 之「待落地清單之更正二」在 `wg942` 之 `TARGETS` 內、須**末端追加**方得改，而本單 `§五` 逐字令「⛔ 不改 `app.py` 一字」但**未授權改 `CLAUDE.md`**。⇒ CC **只於本報告登記**，其入 `CLAUDE.md` 之時點**請發單側裁**（見 `§八`）。

### `§五-2`　`MEMORY.md` 之整理（**常規三**·⛔ 只提案·⛔ 未落地）

**現況**：`45018` B；系統警告逐字「Only part of it was loaded」⇒ 接手窗可能讀不到全部索引。條目 ＝ **`40+`** 條，多數為「某波之結論」。

| 候選 | 作法 | 一句理由 | 🛑 **射程外（何種查詢會讀不到）** |
|---|---|---|---|
| **(a) 主題聚合**（**CC 推薦**） | 將 `40+` 條依主題併為 **`~12`** 條（量測器陷阱／取號與倉規／`K-6`·`K-9` 正典／`harness` 與 `G.3`／幾何與街角／池與守恆／交接與紀律…），`MEMORY.md` 一主題一行 | memory 之**可複用價值**在「陷阱與家法」，而「哪一波做了什麼」倉內報告已載 ⇒ 聚合後每行資訊密度更高 | ⚠️ **跨主題之時序關聯被壓平**——查「**哪一波**發生過 X」「X 與 Y 是否同批」將讀不到；須回倉查報告 |
| (b) 分層索引 | `MEMORY.md` 只留「主線入口 ＋ 家法總表」約 `20` 行，其餘移入 `INDEX-archive.md`，由主線入口連結 | 保留全部內容、只降低首屏載入量 | ⚠️ **archive 不會被自動載入**——若接手窗未先讀主線入口，archive 內之一切**永不進入 context**；風險 ＝ 把「載入不全」換成「載入但不讀」 |
| (c) 時序截斷 | 只留最近 `N` 波（如 `-160` 以後）＋ 全部家法類，較舊者移入 archive | 最省事、且近期波之相關度最高 | ⚠️ **早期波之裁定與陷阱**（`W-D`／`W-F`／`K-8` 族）將不在索引內 ⇒ 查「BASELINE 1:N 配對」「四欄面積」等**仍在生效**之早期正典會讀不到 |

🔒 **CC 推薦 (a)**，並建議**保留 `4` 條不聚合**（`push-before-shipping-claim`／`grep-multibyte-charclass-trap`／`wg9143-handoff-entry`／`harness-worktree-crlf-trap`）——此四條係**每窗必讀**且與主題正交。

---

## `§六`　收工閘（`§四` 十一項）

| # | 項 | 實得 |
|---|---|---|
| `1` | `S-1` 八項期初錨逐位對拍 | ✅ `§零`（七檔三數 `7/7`） |
| `2` | `N-1` 常規四（九）二項·二框並報·**對照組二形分報** | ✅ `§二`／`§二-1` |
| `3` | `N-2` `GB-122` 三框現查 ＋ 意指占用 ＋ 對照組 ＋ **新取之 `S-4` 人造受詞**（並標其「出艙後即作廢」） | ✅ `§三` |
| `4` | `M-C-1` 全部節／函式**先列後判**（`grep` ⛔ 未接 `head`·逐字聲明） | ✅ `§四-1`（`5` 個 module 級 ＋ 巢狀 `0`） |
| `5` | `M-C-2` 三支窮盡性自問 ＋ 逐字作答 | ✅ `§四-2`（**⛔ 不窮盡** ⇒ 具名 (丁)） |
| `6` | `M-C-3` 判 ＋ **射程外之具名量** | ✅ `§四-3`／`§四-4`（`5` 項） |
| `7` | `N-4` 待落地補列 ＋ `MEMORY.md` `3` 候選（各具名射程外） | ✅ `§五` |
| `8` | 依 `GB-121` 施行式**逐字引錄** `§二` 七款 ＋ 標 `【單】` 與來源管線 | ✅ `§一` |
| `9` | 新探針入倉前置 | ⛔ **不適用**——本批**⛔ 無新探針**（`M-C-1`〜`M-C-3` 全以純閱讀 ＋ 倉內既有凍存檔完成） |
| `10` | `wg942`（**commit 前**跑）＋ memory ㉞ 母體驗證 | ✅ `§六-2` |
| `11` | 全新檔 CRLF `0`／逐檔 `git add`／`numstat` `deletions` 逐列 `0`／三重驗證／clone 刪除 | ✅ `§六-1` ＋ 聊天回報 |

### `§六-1`　`numstat`（期初 tree ＝ `666e0bd4`）

| 檔 | 新增 | 刪除 | 性質 |
|---|---|---|---|
| `docs/reports/W-G.9-178R_五裁落地與G3射程判.md` | （見 commit） | **`0`** | `A`·新檔 |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `30` | **`0`** | `M`·**純末端追加**（正典檔·`常規一②補款③(2)` 例外清單） |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `14` | **`0`** | `M`·**純末端追加** |

🔒 **異動 ＝ `3` 檔**，與單之 `§三`（payload ×`3`·入二檔）＋ `§四-11`（報告）**逐項對照相符**；⛔ 無單未列之異動。**⛔ 未動 `verify/` 下任何一檔**（本批無探針·無 log）。
🔒 **位元組框之獨立佐證**：自誤登記表 `371299 → 375507`（`+4208` B）／`W-G.4` `494687 → 498155`（`+3468` B）；二者**嚴格前綴 ＝ `after[:len(before)] == before` ⇒ True**；CRLF 期初期末皆 `0`。

### `§六-2`　`wg942_append_audit`

見聊天回報之逐項。🔒 比較端 ＝ `HEAD` ＝ 基座 ⇒ 於 `commit` **前**跑（考古節 `123` 一般式）。
🔒 **框之具名（戒 `29`）**：`wg942` 所報之三數係**字元數**（`len(str)`），⛔ 非位元組；`§六-1` 所報為**位元組數**。二框皆證嚴格前綴。

### `§六-3`　報告命名（**常規三**）

(a) `W-G.9-178R_五裁落地與G3射程判.md`　(b) `W-G.9-178R_自誤215-216_GB122_G3第四支.md`　(c) `W-G.9-178R_G3二端皆harness之判.md`
🔒 **採 (a)**——與施工單標題同構，日後由單號回查最直接。

---

## `§七`　不辦事項之確認（`§五`）

⛔ 未改 `app.py` 一字（期初 ＝ 期末 blob `68f7bfab…`）；⛔ 未改 `verify/stepg_pipeline.py` 一字；⛔ 未切換族②③；⛔ **未解除 `G.3` 停機**（只備依據）；⛔ 未辦逐 commit 二分；⛔ 未解 `GB-118`；⛔ 未重跑 `run_all`；⛔ 未請 KL 實跑；⛔ 未重產 baseline／凍存名單；⛔ 未編修 `verify/out/**` 之任何既有一列；⛔ 未改 `VR` 一字；⛔ 未追改 `W-G.9-173`〜`-177` 之既有一字；⛔ 未落地 `裁 A`／`裁 B`；⛔ 未動 `data/`；⛔ 未追改任何既有已鑄之號；⛔ **未逕行落地 `MEMORY.md` 之整理**（只提案）。

🔒 **拋棄式 clone**：`C:\w178a`／`C:\w178x` 於 `push` 後整份刪除，並於聊天回報中確認。

---

## `§八`　🛑 呈發單側裁（⛔ 非上呈 KL）

1. 🔑 **`G.3` 停機之解除**：依據已備妥（`§四-3` 三條·任一獨立成立），判 ＝ **(丁)**。🛑 **由發單側裁是否解除**；CC ⛔ 未解除。
2. 🟡 **候選 (乙) 之表述須修正**：單所擬之「射程本即不及 Step-G 之池」**字面不成立**（`池_v3(㎡)` 欄確在射程內）；正確者為「**二端皆 harness 側**」。⇒ 若發單側欲將 (丁) 併入 (乙)，須**改其表述**，⛔ 不得逕用原字面。**是否鑄自誤號請裁**——CC ⛔ 未自鑄（`常規四 四`）。
3. 🟡 **`§五-1` 之入倉時點**：待落地補列須改 `CLAUDE.md`（`wg942` `TARGETS` 內·須末端追加），而本單 `§五` **未授權**改該檔 ⇒ CC 只於本報告登記。**請裁其入 `CLAUDE.md` 之時點**。
4. 🟡 **`M-C-1` 之檔路徑**：單載 `verify/tools/wg_g3.py`，實為 **`verify/wg_g3.py`**（`verify/tools/` 下無同名檔）。⛔ 無後果（單已寫「或其現名」），**併報以備稽核**。
5. 🔒 **`MEMORY.md`**：三候選及其射程外見 `§五-2`，CC 推薦 **(a) 主題聚合** ＋ 保留 `4` 條不聚合。**俟核可後方落地**。
