# CC 新 session 交接文：**K-6-A2（K-9 實作波）**

**產出**：CC／2026-08-04·🔧 **更新至 `0f8f35d`（2026-08-05·段五(a)+(b) 完成）**
**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`　**交接時 HEAD**：`0f8f35d`（**已 push·已驗**）

> 🛑 **新 session 第一件事：讀 §九**——段五(a)+(b) 撞到一個**正典與碼面之構造不一致**，
> **(c) 之範圍因此須擴大**，且**該題未裁前不得動 `parcel_min_width_n14` 之生產路徑**。

> 🔧 **本次更新之範圍**：§〇／§一／§二／§六-2／§八 已改寫至 `0f8f35d`；
> 🆕 **§九＝段五(a)+(b) 之現況與待裁**（新 session 之主戰場）。
> §三／§四 為**段三之歷史結論**（已標）；§五／§七 未受段四、段五影響。

> ⚠️ **本文行號一律不寫**——引用一律用**可自癒 grep**。
> 若確需行號，請當場 `grep -n` 後再用（CLAUDE.md 行號衛生）。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD          # 應為 0f8f35d（≠ 則本文所述進度需重新核對）
sed -n '1,60p' CLAUDE.md
grep -n "^### K-9\|^### 🔒 K-9\|^#### 🔒 K-9-6-b-2" docs/rulings/K-6_街角地分配程序與可分配判準.md
grep -n "^| \*\*GB-3[0-9]\*\*" docs/reports/W-G.4_泛用阻塞項登記表.md
sed -n '1,60p' docs/reports/W-G.6_K6-A2-段五ab_宗地帶_攤表.md   # 🛑 段五之攤表·先讀 §〇
sed -n '1,80p' docs/reports/W-G.5_K6-A2-段五_偵察.md            # 段五之開工偵察
```

**⛔ 禁 `git reset --hard`**（理由見 §五）。

---

## 一、K-6-A2 現況（`0f8f35d`）：**八項中已落地五項·一項部分（段五 (a)(b) 已做·(c) 未做）**

| # | 項目 | 正典 | 狀態 |
|---|---|---|---|
| 1 | **K-9-2** 街廓層剖面深度警示 | K-9-2 | ✅ **已上線**（`9880164`＋`648bbb5` 遷位）·本案零警示 |
| 2 | **K-9-4** BASELINE 臨接閘 | K-9-4／K-9-4-1 | ✅ **已上閘**（`dd02b5b`）·停機閘·本案零觸發 |
| 3 | **K-9-5-2** 街角範圍構成之正典 | K-9-5-2 | ✅ 已入正典（`9d6ae21`）·**碼面未實作**（屬第 6 項） |
| 4 | **K-9-7** 解析定位 | K-9-7 | ✅ **已接生產**（段三(c)·`0429772`）·四分支 |
| 5 | **K-9-8** 虛擬量測塊 | K-9-8 | ✅ **已接生產**（段四(c)·`3c6a184`）——第二層＋**乙式帶**（K-9-6-b-2）·**GB-19 已落地**；⛔ **GB-18 仍未修**（已重標段六） |
| 6 | K-9-6 逐宗量測最小深度 | K-9-6 | 🔶 **部分**：`b-1`／`b-2` ✅ 已落地於**街角規定範圍**；**宗地層**＝段五——**(a)(b) 已做**（opt-in `_n14_band_hi`·`9fbce47`／攤表 `0f8f35d`）、**(c) 未做且範圍須擴大**（🛑 **見 §九**）·**GB-13 仍未解除** |
| 7 | K-9-5 街角實量＋兩門檻分流 | K-9-5 | ⬜ 未開工（**段六**·**土地後果最大**） |
| 8 | §零-2 街廓最小建築面積 UI 欄 | K-6 §零-2 | ⬜ 未開工（段六） |

**依賴序（claude.ai 2026-08-03 更正·非原交辦序）**：
**K-9-7 ✅ → K-9-8 ✅ → K-9-6 ⬅️ 下一站（段五） → K-9-5（段六）**
（K-9-6 之量測道路境界線＝K-9-8 之延伸線段；K-9-8 之 `L_in` 位置由 K-9-7 之 `t*` 決定）。
⇒ **前二關已通**。段五 (a)(b) 已完成；**(c) 有前置**——🛑 **§九之構造題未裁前不得切換**。

**另有補正批**：`648bbb5`（段二-0 八項）、`7e54160`（F-2 Step G 停機後成果失效）。

---

## 二、🛑 **待 KL 裁之未決項（開工前先問清楚）**

| # | 待裁項 | 出處 |
|---|---|---|
| ~~**A**~~ ✅ **已裁·已完成** | ~~是否放行 K-9-7 段三(c) 切換~~ ⇒ **KL 放行 2026-08-04·已切換（`0429772`）** | 段三(b′) 報告 |
| ~~**B**~~ ✅ **已解（技術·非域裁）** | ~~K-9-7-d 第二層 ⟷ K-9-8 互為前提之循環怎麼切~~ ⇒ **以「逐邊通式」切開**：`P_block` 通過任一頂點之 `t` ＝ `t_C = (C−F1)·ân − c0`（**一次減法**）⇒ 每邊一區間、每支一次除法，**不需先有 K-9-8 之成品**。見 `docs/reports/W-G.5_K6-A2-段四c2a2_乙式八支推導.md` §二 | 段三(a) 報告 §六 |
| **C** ⬜ **仍未做**（**非阻塞**） | 分支覆蓋合成案（①×截角／③×截角／④切換點）——段四(c)-2 之施工單 §七 明令**本段不做**；**本案真資料已覆蓋 ①②③ 與截角邊支**（`verify/out/probe_K9_7d_layer2.full.log`【G】），⚠️ 惟**換 `V6_1` 後截角邊支將失去唯一真資料覆蓋** ⇒ 合成案為其日後**唯一覆蓋**（GB-19 已載） | 段三(a) 報告 §四 |
| ~~**D**~~ ✅ **已答·無須處置** | ~~`0m R5/left` winner 之候選資格餘裕 `0.07㎡`~~ ⇒ 段四(c) 放行時 KL 已以**全案最小餘裕格**（`R3/右 628-28(1)`：餘 `12.79㎡`、最大擾動 `0.09㎡` ⇒ **142×**）為據裁定放行；`R5/left` 於段四(c) 之 `Δt* = 0.0000`（`k<0` 之格不動） | 段三(b′) 報告 §1-5 |
| **E** | `data/地籍資料來源_匿名版.xlsx` 之未 commit 異動是否入庫？（🚩自 W-G G.2 起上呈中） | 段二-0 報告 §一-3 |
| **S6-1** | **🔴 段六才呈·段四不擋**：退縮 3.5m 下 `R1/right`（三代寬 **4.1727**）與 `R6/right`（**4.8108**）**低於 K-9-5 門檻 7.00 m**——即 K-9-8 把量測改對之後**仍不足**。須 KL 裁 **K-9-5 之落地時程**與**該二格之去向**（併宗／改配／現金補償）。<br>⚠️ **現階段無須決定**：段四(c) 只換量測不出判定，街角宗仍走 K-4 豁免提前 return（`app.py:6738`／`:6784`）、根本不到 N-14 ⇒ 段四無論如何不會判這二格不合格。待段六「兩門檻分流」上桌時**連同完整兩門檻資訊一併呈**。 | 段四(b) 報告 §四 |

🔧 **更新（`3c6a184`）**：**A／B／D 已了結；C 仍未做但非阻塞**（見上表）。
**現仍待 KL 之項 ＝ `E`（xlsx 未 commit 異動）與 `S6-1`（段六才呈）**＋ 🆕 **`data/r3.dxf` 之去留**（KL 未決·⛔ 不得自決）。
⚠️ **S6-1 不是段五之阻塞**——列此僅為**不遺漏**。

---

## 三、🗄️ 段三(b′) PK 影響評估之結論（**歷史·放行 A 之依據**）

> 🗄️ **本節為段三之結論**（`d8f97db`）。段四(c) 之切換另有**其自身之** PK 評估
> （`verify/out/probe_K9_7d_pk_impact.log`·**決定性欄位零翻盤**＋一格非決定性指數變動）
> ⇒ 引用時請分清是哪一次切換。**§三 之「三條路徑」錨仍完全有效。**

實料：`verify/out/probe_ruling_K9_7_pk_impact.log`（已入庫）

- 16 格（8 位移組＋8 對照組）之**候選集合／合格集合／winner／`forced_offset` 皆逐格相同**。
- 僅 2 格指數數值微變，**次位者皆未達標 ⇒ 無名次可翻**。
- ⇒ **切換 K-9-7 不改變本案任何街角第 1 宗之歸屬，亦不改任何抵費地狀態。**

🔑 **改街角規定範圍會動三條路徑，缺一即評估不完整**（KL 2026-08-03 指出）：

| 路徑 | 錨 |
|---|---|
| **(a)** 候選資格（宗地 ∩ 範圍 > `1.0㎡`） | `grep -n "_has_p1 = _inter_p1_area > 1.0" app.py` |
| **(b)** 優先權指數**三分項之分母皆取自範圍多邊形**（**不止「跨占面積」一項**） | `grep -n "score_corner_cut = 0.4" app.py` |
| **(c)** G 值門檻 `min_area_to_apply` | `grep -n "cand_p1\['min_area_to_apply'\]" app.py` |

**一次測全三路之作法**：`select_corner_lots_both_sides_v12` **於函式內自行呼叫**
`_build_corner_range_v3`（`grep -n "_rng_wb = _build_corner_range_v3" app.py`）
⇒ 於 harvest 命名空間 patch 該符號即三路全動、生產碼零改。
（探針已入庫：`verify/probes/probe_ruling_K9_7_pk_impact.py`。）

---

## 四、🗄️ 段三(c) 若放行，要做什麼（**已於 `0429772` 完成·存查**）

> 🗄️ 本節之四項**已全部完成**：① 生產已走 `k97_solve_alloc_t`、舊斜率式已移除；
> ② `_t_override` **保留**（其語意於每次生產換邊時**須反轉**——段三(c)／段四(c) 各反轉一次，
> 漏反轉會使對照兩欄同源、`Δ` 恆 0，**已犯過一次並登記**）；
> ③④ 之名目變動與土地後果已逐格入報告。
> **段四(c) 之對應資訊見** `docs/reports/W-G.5_K6-A2-段四c_切換乙式.md`。

1. 生產路徑改走 `k97_solve_alloc_t`（`grep -n "def k97_solve_alloc_t" app.py`），**移除舊斜率式**
   （`grep -n "k = sigma / den_a" app.py`——⚠️ 該區域變數係正典之 `c`、非正典之 `k`）。
2. `_t_override` 之去留：該參數係**只供對照探針**（`grep -n "_t_override" app.py`）；
   切換後若不再需要，須連同 `probe_ruling_K9_7_alloc_t.py`／`probe_ruling_K9_7_pk_impact.py`
   之用法一併評估，**勿逕刪**。
3. **`run_all` 名目集合此時允許變動**（施工單 §六-1 之條款於 (c) 方適用）
   ⇒ 須**逐格列出進／出之名目並各附成因**，⛔ 不得以「名目數相同」結案。
4. 預期土地後果（段三(a) 已量）：8 格位移，最大 **R5 左 −16.56cm ⇒ 面積 −7.27㎡**。

---

## 五、⚠️ **不會跟到辦公室電腦的狀態**（本機工作樹限定）

新機 `git pull` 只會取得**已 commit** 之內容。下列**只存在於這台家用電腦**：

| 項 | 影響 | 處置建議 |
|---|---|---|
| `data/地籍資料來源_匿名版.xlsx` 之**未 commit 異動**（07-19·−898KB） | 新機將取得 **HEAD 版**（12,380,156 B） | ✅ **無影響·已查明**：兩版 `U_LAND (64453,76)` 與 `重劃區地籍 (53,21)` 經 `assert_frame_equal(check_exact=True)` **雙雙通過**，差異全在容器層（Excel 重存）⇒ **新機跑 `run_all` 應同得 132／86／46**。（段二-0 報告 §一-3） |
| 三支已入庫 log 之工作樹髒污（`probe_capacity_decomp.log`／`probe_ruling_K4_s_origin.log`／`probe_ruling_N_e1_touch.log`） | 新機為乾淨版 | ✅ 無影響。⚠️ 惟 **`run_all` 會就地改寫後二支** ⇒ 新機跑完工作樹亦必髒兩支；**「工作樹乾淨」不可作為驗收訊號** |
| **三支臨時驗證腳本**（`tmp_k94_discrim.py`／`tmp_f2_invalidate.py`／`tmp_k92_edge.py`） | **新機沒有** | ⚠️ 見下 |

### 5-1 臨時腳本之處置（**已查證·非臆測**）

三支皆**硬編本機絕對路徑**（`C:\Users\admin\Desktop\...`）⇒ **原樣入庫在他機會壞**
⇒ 本 session **未入庫**（非遺漏）。

**其證據仍在**——所產出之 log **皆已入庫**：

- `verify/out/K9_4_gate_discrimination.log`（K-9-4 閘之三合成案·§四 鑑別力）
- `verify/out/F2_stepg_invalidate.log`（F-2 之三項驗證）

**若需重跑**：方法已逐項寫在對應報告內（擾動對象、擾動量、期望結果），可據以重寫；
重寫時請**改為相對路徑**並考慮是否升格為正式探針。

🔴 **與常設紀律之關聯**：CLAUDE.md 已立「`main()` 內之敘述，`run_all` 不得單獨作為驗收依據，
**必須另附合成案**」。⇒ 日後凡動 `main()` 內之閘或狀態機，**合成案腳本宜直接寫成
`verify/probes/` 之正式探針**（相對路徑），避免再次落入「證據在、но 重跑器沒了」。

---

## 六、本波已立之紀律與登記（**新 session 必讀**）

### 6-1 CLAUDE.md 新增兩條常設紀律

- **`main()` 內之敘述，`run_all` 不得單獨作為驗收依據**（`grep -n "不得單獨作為驗收依據" CLAUDE.md`）
  ——`harvest()` 以 AST 跳過 UI、harness 走 `stepg_pipeline.run_step_g`
  ⇒ `main()` 內之敘述**從不被 `run_all` 執行**（已兩度坐實：GB-24／GB-25）。
- **編號衝突一律以倉內為準**（`grep -n "編號衝突一律以倉內為準" CLAUDE.md`）
  ——施工單編號與倉內衝突時**改新則之號**，⛔ 禁改既有正典編號。

### 6-2 本波新增之 GB（GB-20〜**GB-39**·登記表共 **39 列**·缺 `GB-12`·另有 `GB-7b`）

`grep -n "^| \*\*GB-2[0-9]\*\*" docs/reports/W-G.4_泛用阻塞項登記表.md`
`grep -n "^| \*\*GB-3[0-8]\*\*" docs/reports/W-G.4_泛用阻塞項登記表.md`

| GB | 一句話 |
|---|---|
| **GB-20** | `get_min_lot_size` 於正面路寬 ≤0 時**靜默取附表最窄列**（KL 已裁：停機補填·新碼已 raise、既有消費端待波末批） |
| **GB-21** | `non_buildable` **名實相反**（裝的是可建築分區）·波末批改名 |
| **GB-22** | BASELINE 法向**兩份實作、號規不同**（`_baseline_normal_axis` vs `_build_corner_range_v3` 內聯）·②屬街角構造所需**禁順手併** |
| **GB-23** | `cad['baselines']` 每街廓單值·無「以 BASELINE 切分街廓」機制（KL 已裁：**說明性登記·非缺件**） |
| **GB-24** | 🔴 **`run_all` 對 app UI 分支之覆蓋＝零**·「零進零出」不得引為「已驗」 |
| **GB-25** | Step G 產物鍵無「進入重算即失效」（**已修**·F-2） |
| **GB-26** | 🔴 **`R4/left` 之解析 `m` vs 觀測差 60%**——段四(c)-2(a) 已以 `V6_1` 坐實成因（`0.000424348 → 0.000266090`·相對差 `59.53% → 0.03%`），**依 KL 明令仍維持開啟**·⛔ 不得結案 |
| **GB-27〜GB-35** | 段三／段四陸續登記（容差 vs 欄位粒度／`.claude/worktrees` 使全倉 grep 加倍／多 `-e` grep 偽造 0 命中 等）·⛔ 引用前逐條讀 |
| **GB-36** | 量測帶「起算線」之字面 vs 推導二式 ⇒ **KL 裁採乙式**（K-9-6-b-2）·**✅ (c) 已切換** |
| **GB-37** | 生產路徑上之**假陰性偵測器**（測 `P_front` 回報 0/16、實情 `P_block` 4/16、且無消費端）·**✅ 段四(c) 已收**（整段移除·判定改由 `P_block_on_chamfer` 承擔） |
| **GB-38** 🆕 | `nameset_diff` **以整行為名目** ⇒ 行文內嵌之實測數值一動即被讀成「出 1／進 1」·**只登記不改工具**；使用時「出／進」非零**一律須逐條具名說明** |
| **GB-39** 🆕 | **K-9-5 撤銷面積豁免後，街角第 1 宗必須接 `k98_virtual_measure_block`**——一般路徑之臨路停機閘以 `front_pt`（＝FRONT_LINE 端點）為參照，街角第 1 宗之分配線落在道路截角上時 ⇒ **假停機**；k98 路徑傳 `P_block`（在 `PQ` 上）由構造正確。**今日不可見**（K-4 豁免 ⇒ 街角宗不進量測）·**段六與 K-9-5 同批** |

### 6-3 正典重要更正（**引用前先看**）

- **K-9-5-1 ＝「正面路寬未填 ⇒ 停機」**；**K-9-5-2 ＝「街角範圍構成·面積夠⇒寬度合格」**。
  （曾短暫互換，已於 `c786c23` 還原。）
- **K-9-7-c 表之 `k`／`m` 係「擾動觀測值」、非解析偏導 ⇒ ⛔ 禁當解析靶對拍。**
  （分支① 之 `k=0` 是「輸出對深度不敏感」，而**正是解析 `k>0` 才使觀測為 0**。）
- **K-9-7-d 已更正為「10 格 FRONT 重疊段／2 格截角邊」**（原載「12 格全 FRONT」與實料不符）。
- **`P` 同名異物**：K-9-7 之 `P_front`（`ALLOC ∩ 前緣線`）vs K-9-8 之 `P_block`（`L_in ∩ BLOCK 邊界`）
  ⇒ **做 K-9-8 前必讀 K-9-7-a 衝突二**，實作時務必分開命名。

---

## 七、本波之可複用教訓（**踩過的坑**）

1. **判準的形狀錯了，量得再準也沒用。** K-9-4 初版把「穿過 BASELINE」寫成**裸跨號**
   ⇒ 對「配得最準的宗」誤報 33/68 格。凡**貼合／臨接**類判準**一律用容差界定「實質」**。
2. **多判準閘要先想「會不會同時成立、誰蓋掉誰」。** 跨過者其 gap 必 > 容差 ⇒ 必同時落入「未配到」；
   先報後者即**吞掉唯一補救**。⇒ 互斥處理＋併存時兩段都報。
3. **母體界定要正面分類、不可反面篩。** 以 `cut_coords is None` 反面篩會把**真幾何缺失**
   靜默吸收成「無地宗」⇒ 改以**推進側別**正面排除＋雙向自檢。
4. **「0」這種乾淨結果最該懷疑。** 段三(b′) 探針初版用錯 diag 欄名（實為 **`端`／`候選地號`**），
   16 格全讀 0，**首度輸出即自動得出「零翻盤」**——而該假結論與最終真結論**字面相同**、
   幾乎不可能從結果反查。**是交叉來源自相矛盾（攔截器顯示候選 2/11/12/15/14/5）才咬出來。**
5. **`git ls-files` 對中文檔名輸出八進位跳脫** ⇒ `| grep '\.md$'` 只中 ASCII 檔名者。
   一律用 `git ls-files -z … | tr '\0' '\n'`。
6. **報告裡逐字寫出「待查字串」會使自身成為命中** ⇒ 該類驗收永不可能通過。
   （段三(b′) §二-1 之撇號變體即此例·已改以文字描述。）
7. **閘要放哪，先問「它的主詞何時存在」。** K-9-4 之主詞是分配宗地，
   `cut_coords` 最早生於 Step G ⇒ 施工單所令之「開始配地之前」邏輯上不可滿足。

---

## 八、環境要點

- 工作分支 **`wip/s1-endpart`**；⛔ 不是 `main`。
- `python verify/run_all.py` 約 **15–20 分鐘** ⇒ **背景跑＋等通知**；
  ⚠️ `run_verification` **會寫 CSV** ⇒ 兩支長跑**不可併跑**。
- CAD：`data/V6.dxf`，`ezdxf.readfile(..., encoding="cp950")`。
- 現行驗收基線：**名目族 132／86 PASS／46 FAIL**；**夾具/golden 族 12／11 PASS／1 FAIL**
  （🔧 `3c6a184` 更新：夾具族由 `10/2` 變 **`11/1`**——`fixture_corner_range_k8` 已於段四(c) 轉綠；
   餘 1 紅 ＝ **E 系列實測快照閘**·CLAUDE.md 既載）。
- **最新靶 log ＝ `verify/out/K6A2_seg5c1_runall.log`**（`64b9bb8`·與 `K6A2_seg5a_runall.log`／
  `K6A2_seg4c_runall.log` **逐字節相同**——以 `diff` 為據）。
  🔴 **位元組數須聲明量測框**（**失敗考古 節 48**）：**工作樹** `wc -c` ⇒ `1688154`（CRLF）；
  **倉內 blob** `git cat-file -s $(git rev-parse HEAD:verify/out/K6A2_seg5c1_runall.log)` ⇒ `1681942`。
  二者差 `6212` ＝ 該檔之 CRLF 數（`core.autocrlf=true`，而 `.gitattributes` 之 `-text`
  **不涵蓋 `*.log`**）。⇒ **claude.ai 自倉內看到的是 `1681942`**；「逐字節相同」一律以 `diff` 為據。
  對拍工具 `verify/tools/nameset_diff.py`（**兩族分開比**·⛔ 禁以計數驗收·見 **GB-38**）。
- ⚠️ 守恆殘差 `F.3 結構永久閘0m` 現為 **`0.03`**（容差 `0.585`）——**KL 已裁定接受**；且依「池採量」之推論，**該殘差恆非零**、⛔ 不得再訂「回到精確零」之驗收線（正典 `grep -n "^#### 🔒 抵費地池之面積採" docs/rulings/K-6_*.md`）。
- 收官前置：`git rev-parse origin/wip/s1-endpart` 已含該 commit（**不綠不推、不推不報**）。

---

## 九、🛑 段五(a)+(b) 之現況與**唯一硬阻塞**（新 session 之主戰場）

> 三 commit：`c23df3b`（正典＋GB-39·零碼面）／`9fbce47`（引擎 opt-in·生產零呼叫點）／
> `0f8f35d`（探針＋log＋攤表）。
> 攤表：`docs/reports/W-G.6_K6-A2-段五ab_宗地帶_攤表.md`（**先讀 §〇**）；
> 實料：`verify/out/probe_K9_6_parcel_band.log`。

### 9-1 已做（(a)+(b)）

| 項 | 內容 |
|---|---|
| 正典 | **K-9-3-1 補正**（宗地層不再比附表深度；⛔ **但各宗最小深度仍須實量**）＋ `🌐 K-9-3 泛化待查`（BASELINE 折線 ⇒ 蘊含失效·**未查證是否已有直線性閘**·⛔ 不得寫成「無此閘」） |
| 引擎 | `parcel_min_width_n14` 新增末位 **`baseline_pts=None`**（None ⇒ **逐字沿用現行**）＋ 共用 helper **`_n14_band_hi`**（`grep -n "def _n14_band_hi" app.py`）→ `(t_lo, t_hi, D, g, bn_dot_n)` |
| 驗收 | **逐字節零行為**：`diff verify/out/K6A2_seg4c_runall.log verify/out/K6A2_seg5a_runall.log` ⇒ 空；**生產零呼叫點**：`grep -n -A2 "^    _w = parcel_min_width_n14" app.py` 三行內無 `baseline_pts` |

### 9-2 🗄️ ~~**硬阻塞：新帶與 N-14 量法之構造不一致**~~（**已推翻·原文存查勿刪**）

> 🔴 **2026-08-05 KL 圖示裁定：本節之診斷錯誤。**
> **量測對象不改**（量宗地多邊形之弦是對的）；錯的是 **K-9-6-a 表列第 1 列之受詞**
> ——法規原文之受詞為**後側境界線**，而該表列誤寫為**臨街段**
> ⇒ 帶底取錯（應取**後緣角點**、非前緣端點沿 BASELINE 法向之平移）。
> ⇒ 本節所謂「(c) 之範圍須擴大」**不成立**；(c) 之工作回到「改帶底 ＋ 切換」。
> 現行正解：`grep -n "^#### 🔒 K-9-6-a " docs/rulings/K-6_街角地分配程序與可分配判準.md` 之更正註
> ＋ 新則 `K-9-6-g`；教訓入**失敗考古 §42**。
> ⚠️ 下方原文（含 9-3／9-4）**保留存查**，惟其「(c) 範圍」與「不得據以訂門檻」之
> 推論鏈已由 (c)-1 取代。

### 🗄️ 原文（存查）

**現象**：新帶下緣抵／越過宗地之最深頂點，而 `parcel_min_width_n14` 量的是
**宗地多邊形之弦** ⇒ 該處弦長 0 ⇒ **34 / 118 列 loud raise**。

**機制（代數·已驗）**：帶下緣 ＝ 前緣沿 `b̂n` 平移 `D`（`t = t_lo + D·h`），該線**恰切 BASELINE**
（`dep = D − D(g²+h²) = 0`）。但宗地二側界係 **ALLOC 線、與 `b̂n` 不平行**
⇒ 自前緣端點沿 `b̂n` 直下**未必仍在宗內** ⇒ `max(t)` 可能 < `t_hi`。

**根因**：**K-9-6-b 之量測多邊形是「帶 ∩ 二側境界線（無限直線）」**
（`_build_corner_range_v3` 正是如此·「純量測工具·**不截斷實體**」），
而 `parcel_min_width_n14` 量「宗地多邊形之弦」
——**舊帶（14 m·遠在宗內）二者等價；新帶（抵 BASELINE）分家。**

⇒ 🛑 **(c) 不只是「傳入 `baseline_pts`」，尚須把量測對象一併改。此為量測之定義 ⇒ 需裁。**
⚠️ ⇒ **攤表之「寬度_新」欄係「現行量法 × 新帶」之產物、不是 K-9-6-b 之最終值**
⇒ ⛔ **不得據以訂門檻**。

### 9-3 攤表數字（⛔ 皆非門檻·僅供 KL 參考）

母體 **118 列**（`推進側別 ∈ {left,right}`·剔除抵費地 `0m 9`／`3.5m 11`）
＝ 街角 **12**（`N/A·豁免中`）＋ raise **34** ＋ 量得 **72**。
帶深 `14.00` → `33.13〜47.59`。翻轉 **8 列**（4 宗 × 2 情境·**全為合格→不合格**）。
🔬 **R2 全數 `Δ寬度 = 0.000000`**（其最小弦本就落在舊帶內）
⇒ **「新帶未必改變結果」之反例**，⛔ 不得由他街廓外推。

### 9-4 (c) 之工作清單（**待裁後方可動**）

1. 🛑 **先裁 9-2**（量測對象）——未裁前 ⛔ 不得動生產路徑。
2. 生產切換：`app.py` 之 `_w = parcel_min_width_n14(...)` 傳入 `baseline_pts`。
3. **同批拆 E-2′ 閘**（`grep -n "本宗深度 {max(_tv):.4f}m" app.py`）
   ——切換後變成「拿該宗**最大**深度比該宗**最小**深度」⇒ 由構造恆不成立（空閘）；
   拆除處**留註記指向 K-9-2** 街廓層檢查。
4. **夾具第二階段**：`verify/fixture_n14_min_width.py`（⛔ **段五(a) 未動之·刻意**——
   兩階段對拍之第一階段須用**未被同批修改**之夾具）。
5. `_min_depth_by_block` 之餵值鏈在 **`main()` 內** ⇒ 觸發 `CLAUDE.md:56`、**須另附合成案**。
6. **GB-39**／K-9-5：街角宗改走 `k98_virtual_measure_block`（**段六**·非本批）。

### 9-5 段五(a) 我對施工單所作之**兩處技術更正**（已載於碼內與 commit）

| # | 施工單原文 | 實測／正解 |
|---|---|---|
| 1 | `D = min over v of ((v − b0)·b̂n)` | `b̂n` 定號後**指向宗內（即指向 BASELINE）** ⇒ 前緣端點之 `(v−b0)·b̂n` **恆為負**（−33〜−48），且 `min` 會挑到**最深**而非最淺端點 ⇒ **正負與極值兩者皆反**。正解 **`D = min((b0 − v)·b̂n)`**（33〜48） |
| 2 | （未提 `n̂` 之號規） | `parcel_min_width_n14` 於 `(s,t)` 框內以「**形心之 `t` 為負則整環翻號**」定向（本案六街廓 **σ 皆為 −1**）⇒ helper 必須**逐字重現該翻號**取 `n̂_eff`，否則 `b̂n·n̂ < 0`、帶會朝宗外長 |

✅ `t_hi = t_lo + D·(b̂n·n̂)`（**乘**投影係數）**施工單是對的**——代數已驗；⛔ 不是 `D/h`、更不是 `D`。
✅ 獨立恆等式 `(b̂n·n̂) = sqrt(1−g²)`：逐宗最大差 **1.110e-16**（閘 `1e-12`）。

### 9-6 段五之可複用手法（**新 session 直接沿用**）

- **母體要以 `推進側別` 篩，⛔ 不以 `cut_coords`**——抵費地**有真幾何卻不量寬度**、以幾何為篩會稀釋分母。
- **helper 先算、寬度後算**：即使寬度 raise，`t_hi_新` 仍留得下 ⇒ log【D】能**自證**
  「帶之下緣是否越過該宗」。這一步把 34 個 raise 從「壞消息」變成**可讀之證據**。
- **零行為驗收看逐字節**（`diff` 空 ＋ 位元組數相同），不看名目集合。
- **兩階段對拍**（先只改引擎、夾具不動 → 再改夾具）是**唯一**能用獨立 oracle 驗證切換的機會；
  ⛔ 兩邊同批改則對拍恆綠、驗不到東西。
- **每次生產換邊，回頭反轉對照探針之「舊／新」欄**——漏反轉會使兩欄同源、`Δ` 恆 0（已犯過一次·GB-26 之舉證即因此失效過）。
