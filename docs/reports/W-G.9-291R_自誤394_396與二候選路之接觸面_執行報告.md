# `W-G.9-291R`　執行報告（**登記 ＋ 接觸面現查·零生產碼**）：`自誤 394`／`395`／`396` ／ 既出數之**語意更正** ／ 二候選路之**接觸面**

> **態**｜開工 ＝ `7574727bb649aad329c26218937a79436480cae4`（CC 自倉 `ls-remote` 實查·全 `40` 碼）
> **單** ＝ `docs/orders/W-G.9-291_施工單_自誤394_396與二候選路之接觸面.md`（工項零已原封入倉·`edbeb81`）
> **分級** ＝ **中**（登記 ＋ 純現查·**零生產碼**）
> 🛑 本批**⛔ 改生產碼一字、⛔ 開任何分支、⛔ 動用 KL 之放行**。

---

## `§零`　開工閘（九項·同格載產生指令·**全綠**）

| # | 受詞（指令逐字） | **實測** | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `7574727bb649aad329c26218937a79436480cae4` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`（`rc` **緊接該指令**取之·⛔ 經管線） | `rc = 1` ⇒ **非祖先**；判別力[必為祖先] `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d` ⇒ `rc = 0` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`·`P-5` `SELF_SHA256` ✅ **相符**（受詞 `15994` B）｜實算 `f7171131659d11cc…`／單載 `f7171131659d11cc…` | ✅ |
| — | ↳ 判別力二造（**須先自證 bytes 相異**） | 原 `16072` B vs 變造 `16073` B·`cmp rc = 1` ⇒ 二造成立；[必偽]變造本 ⇒ `rc = 1`·`P-5` **不符** `f03e005d4d36c134…` | ✅ |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**） | 器之表頭逐字印 `W-G.9-291_施工單_自誤394_396與二候選路之接觸面.md`；含 `W-G.9-291` ＝ **`True`**／含 `W-G.9-290` ＝ **`False`** | ✅ |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0`；**四造全成立**——靶① `W-G.9-190` 之 `P-3` **紅**／靶② 同單 `P-4` **提示**／靶③ `W-G.9-191 §六-3` 之 `P-6` **提示**／**反靶** `W-G.9-190R` 之 `P-3` **不紅** | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 `1` > 0／人造字樣 `0`） | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `383`／`393`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | ✅ 逐項相符 |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | 標籤 **`87`**／可解析 **`87`**（數字系 `26` ＋ 字母系 `61`）·🔴 命中 **`0`**·末標籤 **`bi`** ⇒ 下一標籤 **`bj`**；造甲 坑 `n` 定義處 `6` ≥ 1／造乙 人造坑 `0`／`0` ⇒ **器非紅** | ✅ |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**（`app.py` `1` ＋ `verify/` 頂層 `33`）；判別力[必不命中] pathspec 多咬之**子層** `.py` ＝ **`305`**（須 `> 0`·坑 `be`·`自誤 386`） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`git ls-files verify/baselines \| wc -l` ＝ **`298`**；併報 `-c core.quotePath=false` ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520` | ✅ |

**環境前置檢**：`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` ＝ **`0  0`**｜
`.claude/settings.json`（`282` B·`PreToolUse`/`Bash` 已掛 heredoc 硬閘）與 `verify/tools/wg9237_heredoc_guard.py`（`8341` B）**皆在**｜
`core.autocrlf` **三層分列** ＝ system `true`（`C:/Program Files/Git/etc/gitconfig`）／global **⛔ 設值**（`rc = 1`）／local `false` ⇒ **有效值 `false`**｜
`CLAUDE.md` 工作區 `236365` B ＝ blob `236365` B。

### `§零-2`　號占用之預查（**入倉前之號占用閘**）

🔒 **本單號**（宣告框 `①`–`⑧`·三軸 ＝ `blob@7574727b…`／全 `docs/` 倉側 blob **`758`** 檔／**檔框 ＋ 列框**）

| 號 | `D1` 檔 | `D2` 列 | `D3` 列（**嚴格式**·補款⑧） | 雙屬列 | **列框**（`\|D2 ∪ D3\|`） | **檔框** | 鬆框（檔／列·僅漏框偵察） |
|---|---|---|---|---|---|---|---|
| **`W-G.9-291`**（受詢） | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0`／`0` ⇒ **未占用** |
| `W-G.9-290`（造甲[必命中]） | `2` | `3` | `2` | `0` | `5` | `3` | `3`／`12` ✅ |
| 人造號（造乙[必為零]·**執行期組出**·字面⛔ 出艙） | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` ✅ |

🔒 **本批擬鑄之三號**（`B`⋀`C`⋀`D` **三形分列並取**·`GB-158` 體例化·三軸 ＝ 同上／**HEAD 全倉追蹤 `.md` `780` 檔**（框 ＝ `git ls-tree -r -z` 之**原始位元組**·坑 `bc`）／檔框 ＋ 列框）

| 號 | **B 形** ``自誤 `N` `` | **C 形** `` `自誤 N` `` | **D 形**（鬆框·錨定） | 判 |
|---|---|---|---|---|
| `自誤 394`（受詢） | `0`／`0` | `0`／`0` | `0`／`0` | **未占用** |
| `自誤 395`（受詢） | `0`／`0` | `0`／`0` | `0`／`0` | **未占用** |
| `自誤 396`（受詢） | `0`／`0` | `0`／`0` | `0`／`0` | **未占用** |
| `自誤 393`（造甲[必命中]·已鑄） | **`0`／`0`** 🔴 | `6`／`14` | `6`／`16` | — |
| `自誤 392`（造甲[必命中]·已鑄） | **`0`／`0`** 🔴 | `4`／`9` | `4`／`11` | — |
| 人造號（造乙[必為零]） | `0`／`0` | `0`／`0` | `0`／`0` | ✅ |

🔴 **`GB-158` 之案型再現（loud 具名·⛔ 逕判紅）**：二個已鑄之對照組於 **B 形皆得 `0`／`0`**
⇒ 依 `GB-158` 施行 `二`，**該形⛔ 單獨採信**；判由 **C 形**與 **D 形**承擔（二者於二對照組皆 `> 0`）⇒ **器非紅**。

---

## `§一`　裁之受領（`§一` 五款·**⛔ 重辦 `§零′`**）

| # | 裁 | CC 之處置 |
|---|---|---|
| `1` | 主答（winner 側之近側界**由鏈交遞**取得） | **受領·出艙即止**，本批⛔ 再量 |
| `2` | `forced` 與 `is_corner` **⛔ 一一對應** ⇒ `GB-170` 之受詞**收斂為「起算點」** | **受領**；已落於 `§四(1)` 之末端追加。🛑 本批**⛔ 解除、⛔ 收窄 `GB-170`** |
| `3` | 既出數之語意更正（`\|cos\|` `0.0473` ⇒ 真值 `0.9988785830613192`） | **受領**；已落於 `§四(2)`。🛑 **⛔ 追改 `286R`／`287R` 一字**（已機驗：本批之 `numstat` 刪除欄全 `0`·二報告未在異動清單內） |
| `4` | `R2/left@3.5m` 實為 `forced` ⇒ `自誤 395` | **受領**；已鑄 |
| `5` | 發單側自認（「已交付」而呈送未為）⇒ `自誤 396` | **受領**；已鑄 |

---

## `§二`　工項零：**本單原封入倉**　✅（`commit` `edbeb81`）

| 款 | **實測** |
|---|---|
| 所指之檔數 | 受詢（本單）＝ **`1`**（母體 ＝ 本 worktree `docs/orders/` 現存 **`157`** 檔·框 ＝ 檔名之**錨定框** `(?<![0-9\-])W-G.9-291(?![0-9])`）｜造甲 `W-G.9-290` 之單 ＝ **`1`**（須 `≥ 1`）✅｜造乙 人造名（**執行期組出**·字面⛔ 出艙）＝ **`0`** ✅ |
| `CR`（框 ＝ `bytes.count(b'\r')`·⛔ `od`） | 源 **`0`**／磁碟 **`0`**／blob **`0`**；`LF` 皆 **`185`**｜**判別力[必非零]** ＝ 倉內 blob `data/V6.dxf` ⇒ `CR` ＝ **`12308`** |
| 三方逐位 | `bytes` 皆 **`16072`**；blob `sha1` ＝ `dc9cabc4e906d836e3b0debd51a3e79aad6a459d`；內容 `sha256` **源 ＝ 磁碟 ＝ blob ＝ `0c052790bca7b07671dc9289b8a971f4d2d1677c4ec172b8ebd65f67487667f4`**｜判別力[必相異] 變造本 ⇒ `3b2a51e077608374…`·相異 `True` |
| `numstat` | `185`／**`0`**（刪除欄 `0`） |

---

## `§三`　工項一：三則 `自誤` 之鑄　✅

🔒 **payload 之來源**（`常規七 一`：塊內文字**由檔案管線直接產生**·⛔ 憑記憶重寫）——自**已入倉之單**逐列切片：

| 號 | 單之列（1-based·含兩端） | 列數 | bytes | `sha256`（前 16） |
|---|---|---|---|---|
| `自誤 394` | `69`–`77` | `9` | `1086` | `46ebb25c28483b6d…` |
| `自誤 395` | `79`–`84` | `6` | `633` | `969e01595c34a15f…` |
| `自誤 396` | `86`–`90` | `5` | `539` | `3e7c687efb36da47…` |

🔒 **`自誤 393` 判準之施行**：三 payload **⛔ 含 `###` 標題列**（機驗：其每一列皆以 `>` 起首·`0` 列以 `#` 起首）
⇒ **原樣保留 `> `**（⛔ 剝層）。**標題列另行機械組出**，範本 ＝ 簿內既有**最末一則**之形（`常規四（九）四`）：

> `### 🩸 \`自誤 393\`　**以引用裝置承載一份「須以其原形落地」之 payload，而該 payload 內含結構標記**`

解析 ⇒ 前綴符 `🩸`／分隔 ＝ **全形空白 `U+3000`**／末段 ＝ payload 首列 `：` 之後之**粗體首句**（`^> \*\*\`自誤 N\`\*\*：(\*\*.*?\*\*)`）。

🔒 **落地形之機驗**（框 ＝ 該簿之定義框逐字 `RE_ZW_DEF` ＝
`` ^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：] ``）

| 號 | 定義框命中 | 擷得之號 | 相等 |
|---|---|---|---|
| `394` | `True` | `394` | ✅ |
| `395` | `True` | `395` | ✅ |
| `396` | `True` | `396` | ✅ |
| **判別力[必不命中]** ＝ 未剝層之引文列 `> **\`自誤 394\`**：**…**` | **`False`** | — | ✅（證該框⛔ 恆命中） |

🔒 **落點之出艙**（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）

| 量 | 值 |
|---|---|
| 期初 | `819120` B／`9688` 列（`\n`）／`CR` `0`；末 `40` B ＝ `' 含標題</u>者原樣保留**。\n\n---\n'` |
| 追加段 | `2586` B／`35` 列／`CR` `0` |
| 期末 | **`821706`** B |
| `numstat` | `+35`／**`−0`** |
| 嚴格前綴檢 | 造甲[必真] `new.startswith(old) ∧ len(new) > len(old)` ＝ **`True`**／造乙[必偽] 於 `old` 末改一位元組後再追加 ＝ **`False`** |
| 簡繁機檢 | 受詞 ＝ 本批**自撰**之標題 `3` 列（⛔ 含單之 payload）⇒ 簡體命中 `{}`·**`rc` ＝ `0`**；判別力[必命中] 注入 `3` 個簡體字 ⇒ 命中 **`3`** |

🔒 **四簿期末**（同一器·`registry`）：自誤 相異 **`386`**／MAX **`396`**（＝ 期初 `383`／`393` ＋ **三**號·缺號 `[106, 355, 356, 357]` **未變**）；
`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動**。

---

## `§四`　工項二：二則末端追加　✅（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末）

🔒 **payload 之來源**（同 `常規七 一`）

| 則 | 單之列 | 列數 | bytes | `sha256`（前 16） |
|---|---|---|---|---|
| `(1)` `GB-170` 受詞之收斂 | `99`–`107` | `9` | `932` | `4f7226b5242163c0…` |
| `(2)` 既出數之語意更正 | `109`–`119` | `11` | `1130` | `8f3e9b02df209afd…` |

🔒 **payload 所載之碼面受詞之<u>當場現查</u>**（單自載「**當場現查·⛔ 採此行號**」⇒ CC 逐錨重量；
母體**正面列舉** ＝ `app.py` ＋ `verify/stepg_pipeline.py`·**列框**）

| 錨（`grep -F` 固定字串） | `app.py` | `verify/stepg_pipeline.py` | 當場現查之逐字 |
|---|---|---|---|
| `_first_corner_alloc_dir` | **`3`** | `0` | `:13929` `def _first_corner_alloc_dir(side_mid):`／`:13990` `allocation_dir = _first_corner_alloc_dir(side_mid)`／`:10194`（註解） |
| `if is_corner:` | **`1`** | `0` | **`app.py:13989`** 🛑 **與 payload 所載之 `:13990` 相異 `1`**——`:13990` 係其**被守護之賦值式**（照實回報·⛔ 追改 payload 一字） |
| `rot90` | **`33`** | `4` | 二形並取之寬字樣（⛔ 唯一 ⇒ ⛔ 作字樣錨用·此處僅作**存在性**之證） |
| `cut_coords` | **`76`** | `6` | 同上 |
| `if _fo_left:` | `0` | **`1`**（`:655`） | 見 `§五-3(3-1)`——**與 payload 所載之 `:657` 相異** |

🔒 **`GB-132` 之避讓（機械自檢·⛔ 目視）**：本批**自撰**之二標題列，以該簿之**三定義框**逐一擷取——

| 自撰之標題列（節略） | `RE_GB_F1`／`F2`／`F3` 擷得之號 | 判 |
|---|---|---|
| `## 🔧 \`GB-170\` 受詞之**收斂**：…` | **`[170]`**（**已鑄**） | ✅ ⛔ 造新號 |
| `## 🔧 既出數之**語意更正**：…` | **`∅`** | ✅ ⛔ 造新號 |

🔒 **落點之出艙**

| 量 | 值 |
|---|---|
| 期初 | `890962` B／`7845` 列／`CR` `0`；末 `40` B ＝ `'效條件三款⛔ 有一款成就。**\n'` |
| 追加段 | `3074` B／**`38`** 列（框 ＝ `add_text.count("\n") + 1`·**含末之空行**）／`CR` `0` |
| 期末 | **`894036`** B |
| `numstat` | **`+37`／`−0`**（框 ＝ `git diff --cached --numstat`）🛑 **與上列之 `38` 相異 `1`**——成因 ＝ **二框相異**（`git` 之「新增列」⛔ 計期初末尾既有之空行所併成之列）；⛔ 擇一出艙 |
| 嚴格前綴檢 | 造甲[必真] ＝ **`True`**／造乙[必偽] ＝ **`False`** |
| 簡繁機檢 | 受詞 ＝ 自撰之題 `2` 列 ＋ 前言 `2` 列 ⇒ 簡體命中 `{}`·**`rc` ＝ `0`**；判別力[必命中] ＝ **`3`** |

🔒 **機械之 append-only 稽核**（`verify/tools/wg942_append_audit.py edbeb81`·`PYTHONIOENCODING=utf-8`）：
受檢 **`77`** 筆／不適用 `0`／合計 `77`（＝ `TARGETS` `77`）⇒ 逐筆「**嚴格前綴 ＝ True·判別力自檢（行中插入）＝ False**」
⇒ 逐字 **「結論：✅ 全部成立」**·`rc = 0`。二落點皆在該 `TARGETS` 內。

---

## `§五`　工項三：二候選路之**接觸面現查**（🛑 **出艙即止**）

🔒 **器** ＝ `verify/probes/probe_WG9291_contact.py`（**唯讀**）·`rc = 0`·
**全量落檔** ＝ `verify/out/WG9291R_contact.log`（**`43034` B／`459` 列**·`CR` `0`·⛔ `tail -N`）。
🔒 **三軸**：`(1)` 來源 `commit` ＝ `edbeb81ffc4f2f220b56af13ef6004a54e09c8a9`／
`(2)` 母體 ＝ **生產碼 `34` 檔**（正面列舉·逐檔見落檔）**＋ `verify/` 子層 `*.py` `305` 檔** ⇒ 合計 **`339`** 檔／
`(3)` 粒度框 ＝ **檔框 ＋ 列框**。

### `§五-0`　款 `4`：判別力二造（🛑 先辦·任一不成立 ⇒ 判器紅、停）

| 造 | 實測 | 判 |
|---|---|---|
| [必命中] `_build_corner_range_v3` 之呼叫端（**二形並取**） | 形一（直呼 `…(`）檔 **`5`**／列 **`9`**｜形二 `ns['…']` 檔 **`2`**／列 **`2`**｜形二 `ns["…"]` 檔 **`28`**／列 **`50`** ⇒ 聯集列 **`61`** ≥ 1 | ✅ |
| [必為零] 一人造名（**執行期組出**·字面⛔ 出艙） | 檔 **`0`**／列 **`0`** | ✅ |

⇒ **器非紅**。

### `§五-1`　款 `1`　路甲：擴 `_build_corner_range_v3` 之回傳

**`(a)` 真呼叫端（AST `ast.Call` 實查·⛔ 文字層）＝ `23` 處（檔 `20`）**

| 所在 | 處數 | 備註 |
|---|---|---|
| **生產碼 `34` 檔內** | **`4`** | `app.py:13136`（直呼）／`app.py:20432`（直呼）／`verify/run_verification.py:246`（`ns` 字面）／`verify/stepg_pipeline.py:429`（`ns` 字面） |
| `verify/fixture_corner_range_k8.py` | `1` | （亦在 `34` 檔內·`:263`·`Return`） |
| `verify/probes/**` | `18` | 逐處具名見落檔 |

🛑 **文字框（`61` 列）與 AST 真呼叫端（`23` 處）之差 ＝ `38`**——其為 `def` 列、註解／docstring、
以及**替身之存取／還原**（`_orig = ns["…"]`／`ns["…"] = _patched`·⛔ 呼叫）。⇒ **文字框⛔ 得單獨作為呼叫端數**。

**`(b)` 逐處之回傳消費式（是否解包）** —— 全量逐字見落檔；**歸類如下**

| 敘述型別（AST） | 處數 |
|---|---|
| `Assign`（標的 ＝ `Name`） | **`14`** |
| `Assign`（標的 ＝ `Subscript`） | **`1`**（＝ `verify/stepg_pipeline.py:429` `ctx['corner_range_polys'][(_crp_lbl, _crp_wh)] = …`） |
| `Return` | **`8`** |
| **解包**（`Assign` 之標的為 `Tuple`） | **`0`** |

⇒ 🔒 **`23` 處<u>無一</u>解包，全部逕用其回傳之多邊形**。

**`(c)` 若改為回傳 `tuple`／`dict`，須同批改之呼叫端數 ＝ `23`**
（＝ 全部真呼叫端；其下游之用法逐處具名見落檔——如 `app.py` 之 `_rng_wb` 其後 `2` 列、`_rng_cr` 其後 `2` 列）。

### `§五-2`　款 `2`　路乙：自 `corner_range_polys[(blk,side)]` 取其遠側境界線

**`(a)` 於每一 `forced` 消費端之可達性（三態·框同 `W-G.9-289 §三`）**

| `forced` 消費端所在 scope | AST 區間 | scope 名數 | 頂層全域數 | `corner_range_polys` | `ctx` | **判** |
|---|---|---|---|---|---|---|
| `app.py` `main` | `14975`–`24579` | `2127` | `286` | scope `False`／全域 `False` | scope `False` | 🔴 `(iii)` ⛔ 可達 |
| `verify/stepg_pipeline.py` `_run_step_g_impl` | `277`–`1643` | `400` | `6` | scope `False`／全域 `False` | scope **`True`** | ✅ `(i)` **同一 scope 可達** |
| `verify/wf_f1.py` `compute` | `184`–`419` | `117` | `20` | scope `False`／全域 `False` | scope `False` | 🔴 `(iii)` ⛔ 可達 |
| `verify/wf_f4.py` `_reshape_block` | `1323`–`1549` | `115` | `54` | scope `False`／全域 `False` | scope `False` | 🔴 `(iii)` ⛔ 可達 |

🔑 **判別力二造**：[必可達] `ns` 於 `_run_step_g_impl` ＝ **`True`**／[必⛔ 可達] 一人造名（執行期組出·字面⛔ 出艙）＝ **`False`** ⇒ **器非紅**。

**`(b)` `corner_range_polys` 之現有消費端全部（🛑 本批<u>重量</u>·⛔ 轉引前批之數）**

🔒 框 ＝ 字樣 `corner_range_polys`（**無 `^` 錨 ⇒ 二數並報**）｜母體 ＝ 上開 `339` 檔 ⇒ **檔 `4` ／ 列 `23`**。

| 檔 | 列 | 角色（逐列具名見落檔） |
|---|---|---|
| `app.py` | `7` | 讀 `3`（`:12529`／`:21959`／`:23219`）·寫 `1`（`:13166` `setdefault('f3_corner_range_polys', …)`）·註解／字串 `3` |
| `verify/stepg_pipeline.py` | `8` | **寫 `1`**（`:429`）·讀 `1`（`:696`）·建構／轉手 `5`（`:413`／`:449`／`:452`／`:705`／`:1642`）·註解 `1` |
| `verify/probes/probe_WG9287_landeffect.py` | `7` | 讀 `1`（`:263`）·註解／字串 `6` |
| `verify/probes/probe_WG9290_neardir.py` | `1` | 字串 `1`（`:380`） |

🔒 **另計 `f3_corner_range_polys`（session 鍵·同族）⇒ 檔 `1` ／ 列 `3`**（皆 `app.py`）。

🔒 **與前批所載「唯一消費端 ＝ `_lot_gate` 之藍影閘」之對位**：於**生產路徑**上，
`ctx['corner_range_polys']` 之**唯一讀取式**為 `verify/stepg_pipeline.py:696`
（`_lg_crp_sg = {_w_sg: ctx['corner_range_polys'].get((blk_label, _w_sg)) …}`），其去處為 `:705` 之
`'corner_range_polys': _lg_crp_sg`（即 `_lot_gate` 之 `blk_ctx`）⇒ **與前批所載一致**；
餘之命中皆為**建構／轉手／註解**，或屬 `verify/probes/**`（⛔ 生產路徑）。

**`(c)` 自該多邊形取其遠側境界線所需之運算（逐項具名·🛑 ⛔ 判其是否構成「另算第二份」）**

🔒 **事實基礎**（`app.py` `_build_corner_range_v3` 末段·當場現查·**四錨皆 ✅ 唯一**）：
`:12881` `cut = _LS3([(_Tp[0] - sux * _ext, …` ／ `:12890` `rng = min(pieces, key=lambda g: g.distance(_anchor))` ／
`:12894` `rng = rng.difference(chamfer_tri)` ／ `:12899` `return rng`。

| # | 所需之運算 | 依據 |
|---|---|---|
| `①` | 取多邊形之**外環逐邊** | `rng.exterior.coords` 之相鄰點對；🛑 `difference(chamfer_tri)` 後可為 `MultiPolygon` ⇒ 須顯式處理**退化態**（`常規八 四`） |
| `②` | 取得側界之**線向** `(sux, suy)` 以辨識何邊為「平移後 SIDELINE」 | 碼面之 `sux`／`suy` 係該函式之**區域值**（同 `W-G.9-289R §二-2` 之判）⇒ 須自 `f3_cad_side_lines_by_side[blk][side]` 之端點**另行重算** |
| `③` | 取得「**遠近**」之基準 | 遠側界 ＝ 過 `_Tp` 之線（`_Tp = S1 + σ·S1_par·d̂`）；判「遠」須 `corner_pt` 與 `d_hat` 作 `s` 投影比較——二者於 `forced` 消費端**皆已在 scope 內** |
| `④` | 排除**截角後**之邊 | `rng` 已 `difference(chamfer_tri)` ⇒ 邊界含截角邊；`K-8 §五`「兩套側界·禁混用」⇒ 須以角度／長度門檻濾之（**門檻值⛔ 現存於碼面**） |
| `⑤` | 自該邊求 `cum_S`（即**起算點**） | 取該邊任一端點對 `corner_pt` 沿 `d_hat` 之投影 ⇒ 其值即 **`S1_par` 之重算** |
| `⑥` | 對每一 `forced` 側逐側為之 | `(blk, side)` 之鍵已存在於 `ctx['corner_range_polys']`（`verify/stepg_pipeline.py:429` 之寫入式）⇒ 讀之**⛔ 需新參數** |

🛑 **本批⛔ 判其是否構成「另算第二份」**——該判屬發單側（單 `§五 款 2 (c)` 逐字）。

### `§五-3`　款 `3`　二路共同

**`(3-1)` `verify/stepg_pipeline.py` 之 `if _fo_left:`（當場現查·⛔ 採單所載之行號）**

| 錨（**先驗唯一性**·坑 `bi`） | 命中 | 實際位置逐字 |
|---|---|---|
| `if _fo_left:` | **`1`** ✅ 唯一 | `verify/stepg_pipeline.py:655  if _fo_left:` |
| `if _fo_right:` | **`1`** ✅ 唯一 | `verify/stepg_pipeline.py:666  if _fo_right:` |

🔑 判別力自檢：寬字樣 `_fo_left` 於同檔 ＝ **`3`** 列 ⇒ 該檢**⛔ 恆為 `1`**。
🛑 **照實回報**：單 `§四(1)` 逐字所載之 `verify/stepg_pipeline.py:657` 與當場現查**相異**——實為 **`:655`**
（單自載「**當場現查·⛔ 採此行號**」⇒ ⛔ 屬單之瑕，惟仍逐字回報）。

**`(3-2)` `cum_S` 之二支賦值式逐字（`verify/stepg_pipeline.py`）**

| 錨 | 命中 | 逐字 |
|---|---|---|
| `left_cum_S = float(_left_buffer_S)` | **`1`** ✅ 唯一 | `:776  left_cum_S = float(_left_buffer_S)` |
| `right_cum_S = float(_right_buffer_S)` | **`1`** ✅ 唯一 | `:777  right_cum_S = float(_right_buffer_S)` |

🔑 判別力自檢：寬字樣 `cum_S` 於同檔 ＝ **`18`** 列 ⇒ ⛔ 恆為 `1`。

**`(3-3)` `app.py` 同形之處（🛑 ⛔ 本批改·逐處具名）**

| 錨 | 命中 | 逐字 | 落於 `def main`（AST `14975`–`24579`） |
|---|---|---|---|
| `left_cum_S = float(_left_buffer_S)` | **`1`** ✅ 唯一 | `app.py:22055` | **`True`** |
| `right_cum_S = float(_right_buffer_S)` | **`1`** ✅ 唯一 | `app.py:22056`（尾註 `# 同理右側`） | **`True`** |
| `_fo_left = bool(_fo_block.get('left_forced_offset', False))` | **`1`** ✅ 唯一 | `app.py:21886` | — |

🔑 判別力自檢：寬字樣 `cum_S` 於 `app.py` ＝ **`39`** 列 ⇒ ⛔ 恆為 `1`。

---

## `§六`　判別力二造之判（**逐閘彙總**）

| 閘 | [必命中]／[必真] | [必為零]／[必偽] | 判 |
|---|---|---|---|
| `§零` 閘 `3` | `dc373d01…` 之 `rc = 0` | `05a11bd6…` 之 `rc = 1` | ✅ |
| `§零` 閘 `4` | 原本 `rc = 0`·`P-5` 相符 | 變造本 `rc = 1`·`P-5` 不符（**先自證 bytes 相異**） | ✅ |
| `§零` 閘 `4'` | 含 `W-G.9-291` ＝ `True` | 含 `W-G.9-290` ＝ `False` | ✅ |
| `§零` 閘 `5` | 三靶皆觸 | **反靶**不紅 | ✅ |
| `§零` 閘 `6'` | 坑 `n` 定義處 `6` | 人造坑 `0`／`0` | ✅ |
| `§零` 閘 `7` | `34` 檔 | 子層 `305` > 0（**⛔ 定值**） | ✅ |
| `§零-2` 號占用（單號） | `W-G.9-290` 檔 `3`／列 `5` | 人造號 `0`／`0` | ✅ |
| `§零-2` 號占用（三新號） | `393`／`392` 之 C・D 形皆 `> 0` | 人造號三形皆 `0`；**B 形對照組得 `0` ⇒ loud 具名** | ✅（判由 C・D 形承擔） |
| `§三` 落地形 | 三標題列皆被定義框命中且號相等 | 未剝層之引文列 **不**命中 | ✅ |
| `§三`／`§四` 前綴 | `new.startswith(old)` ＝ `True` | 末改一位元組 ＝ `False` | ✅ |
| `§三`／`§四` 簡繁 | 注入 `3` 簡體字 ⇒ 命中 `3` | 自撰文 ⇒ `{}`·`rc = 0` | ✅ |
| `§四` `GB-132` 避讓 | 標題一擷得 `[170]`（已鑄） | 標題二擷得 `∅` | ✅ |
| `§五` 款 `4` | 呼叫端聯集列 `61` ≥ 1 | 人造名 `0`／`0` | ✅ |
| `§五-2(a)` | `ns` 於 `_run_step_g_impl` ＝ `True` | 人造名 ＝ `False` | ✅ |
| `§五-3` 唯一性（器之 `uniq()`·坑 `bi`） | 四錨皆命中 `1` | 寬字樣 `_fo_left` `3`／`cum_S` `18`／`39` ⇒ `uniq()` **⛔ 恆為 `1`** | ✅ |
| `§五-1` 詞界框 | — | 於 `run_verification.py` `242`–`250` 查名 `r`：子字串框 **`6`** 列／**詞界框 `2`** 列 ⇒ 相異 ⇒ 該修確有效 | ✅ |

---

## `§七`　一句話推導

> **路甲**之接觸面 ＝ `_build_corner_range_v3` 之 **`23` 個真呼叫端**（無一解包·其中 `4` 處落於生產碼 `34` 檔）
> ⇒ 改其回傳型別者須**同批改 `23` 處**；**路乙**之接觸面 ＝ `4` 個 `forced` 消費端中**僅 `1` 個**
> （`verify/stepg_pipeline.py` `_run_step_g_impl`）之 `ctx` 在 scope 內、其餘 `3` 個判 `(iii) ⛔ 可達`，
> 且自該多邊形反求遠側境界線須 `6` 項運算（其 `②` 須自 `f3_cad_side_lines_by_side` **另行重算**線向、
> 其 `⑤` 即 **`S1_par` 之重算**）⇒ **二路之接觸面已逐處具名；孰優之判屬發單側。**

---

## `§八`　🩸 本批之自捕（**出艙前攔下**·照實併記·⛔ 頂替·⛔ 自行鑄號）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | **旗標置於錯誤之 `argv` 位置而被靜默忽略** —— `§零` 閘 `5` 首跑寫成 `probe_order_preflight.py <單> --selftest`，而該器逐字為 `if len(sys.argv) > 1 and sys.argv[1] == "--selftest"` ⇒ 該旗標**從未生效** | 🔴 **輸出與不帶旗標之單檔檢查逐字相同、`rc` 亦為 `0`** ⇒ **看似已跑判別力造而實未跑**（`⛔ 以「未執行」充「已通過」`之形） | 凡旗標式之器，**先讀其 `argv` 解析式**再下指令；自證類之跑**須以其特徵輸出**（此處為四靶之判列）認定其已執行，⛔ 以 `rc` 認定 |
| `2` | **以單字元名作子字串比對** —— `§五-1(c)` 之「綁定標的其後之用法」首版以 `name in line` 為框；`verify/run_verification.py` 之綁定標的名為 **`r`** ⇒ 咬中 `verts`／`return`／`rng` 等 | 🔴 **命中數偏多而讀來像「更完整」**（`GB-35` 之同族）；實測同區間 子字串框 `6` 列 vs **詞界框 `2` 列** | 一律改**詞界錨定框** `(?<![A-Za-z0-9_])name(?![A-Za-z0-9_])`，並**同格出艙二框之數**以證該修有效 |
| `3` | **「其後之用法」實含賦值<u>之前</u>之列** —— 首版僅排除 `stmt.lineno` 一列 | 表中出現 `def rng(which):`（`:242`）等**早於**該敘述之列，而欄名自稱「其後」 | 起算列改為 `stmt.end_lineno + 1`，並**於欄名逐字載其起算列** |
| `4` | **`§五-1(c)` 之型別歸類不實** —— 首版逐字稱「其**全部**為單一標的之 `Assign`（⛔ 解包）」 | 實測 `23` 處中 `Return` **`8`** 處、`Assign(Subscript)` **`1`** 處 ⇒ 該概稱**為偽** | 改為**逐型別歸類之表**（`Assign(Name)` `14`／`Assign(Subscript)` `1`／`Return` `8`／解包 `0`），⛔ 概稱 |

🔒 **併記（量測器之非紅·⛔ 掩蓋）**：本器 `stderr` ＝ **`59`** B，其內容為
`<unknown>:13: SyntaxWarning: invalid escape sequence '\`'`。**已定位其來源檔** ＝ `verify/probes/probe_WG962_gate_power.py`
（**倉內既有**·⛔ 本批所產·⛔ 本批修）；該警告係 `ast.parse` 對該檔所發，**⛔ 影響任何計數**
（判別力：以同一母體逐檔重跑 `ast.parse` ⇒ 發警告者 **`1` 檔**、其餘 `338` 檔無）。

---

## `§九`　🛑 `⛔ 為之事`（`§七`）之**逐項自檢**

| # | `⛔ 為之事` | 自檢（機械） | 判 |
|---|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔一字 | 生產碼判法（`常規一` 補款）逐區間命中 **`0`** 行；對照組[必非零] `17fd981^..17fd981` ＝ **`2`** | ✅ |
| `2` | ⛔ 開任何分支 | `git ls-remote origin 'refs/heads/verify/*'` ⇒ `verify/W-G.9-289-gb170` 命中 **`0`**；判別力[必存在] `p3a` 命中 **`1`** | ✅ |
| `3` | ⛔ 動用 KL 之放行 | 由 `1` 機械蘊含（生產碼零異動） | ✅ |
| `4` | ⛔ 擬任何實作形、⛔ 判二路孰優 | `§五` 通篇為**列舉**；`(c)` 末逐字載「本批⛔ 判其是否構成『另算第二份』」 | ✅ |
| `5` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／任何函式簽章 | 同 `1` | ✅ |
| `6` | ⛔ 動 `ALPHA_LABELS` 一字 | `git diff --numstat` 對該檔輸出**空** | ✅ |
| `7` | ⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、⛔ 併線 `p3a` | `baselines` `298` 檔·框 `a898f4e1…` **期初＝期末**；`WV_BAKE` ＝ `None`；`p3a` `merge-base --is-ancestor` `rc = 1` | ✅ |
| `8` | ⛔ 引任何自 baseline 取得之數 | 本報告之數全部出自：`git`／`probe_WG9291_contact.py`／`probe_WG9267_issuer_measurers.py`／`probe_WG9269_pit_index.py`／`probe_order_preflight.py`／`wg942_append_audit.py`／`wg9223_acceptance_audit.py`——**無一取自 `verify/baselines`** | ✅ |
| `9` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號 | 四簿期末：`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動**；坑 `87`／`87`·末標籤 `bi` | ✅ |
| `10` | ⛔ 解除或收窄任何 `GB` | `§四(1)` payload 末逐字保留「**失效條件三款仍⛔ 有一款成就**」 | ✅ |
| `11` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | 同 `1` | ✅ |
| `12` | ⛔ 追改 `286R`／`287R` 或任何已入倉文件一字 | 逐 `commit` `git show --numstat` 之 **`deletions` 欄全 `0`**（框 ＝ `numstat` 第二欄）；`wg942_append_audit` `77`／`77` 全成立 | ✅ |
| `13` | ⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | 本報告⛔ 含任何修法主張；`§五(c)` 逐字保留「該判屬發單側」 | ✅ |
| `14` | ⛔ 刪任何 ref | 本批⛔ 執行任何 `git push --delete`／`git branch -d` | ✅ |
| `15` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | `§十` 之 `ls-remote` 實查於**推送後**以**末端追加**出艙（⛔ 預寫） | ✅ |

---

## `§十`　逐 `commit`

（收工閘之實測與逐 `commit` 之 `ls-remote` 實查，於報告入倉**後**以**嚴格末端追加**出艙。）

---

## `§十一`　收工閘（**報告入倉後量**·嚴格末端追加·⛔ 改上文一字）

| # | 受詞（指令逐字） | **實測** | 判 |
|---|---|---|---|
| `1` | `git show --numstat <各 commit>` 逐檔具名 | `edbeb81` ＝ `185`／**`0`**（`1` 檔）｜`49b56eb` ＝ `37`／`368`／`35`／`459`／`460`，**`deletions` 欄逐檔皆 `0`**（`5` 檔）⇒ **二 commit 共 `6` 檔·刪除欄全 `0`** | ✅ |
| `2` | 生產碼判法（母體**正面列舉 `34` 檔**·框 ＝ `git diff --name-only <區間> \| grep -xE 'app\.py\|verify/stepg_pipeline\.py\|verify/run_all\.py\|verify/run_verification\.py'`） | `7574727b..edbeb81` ＝ **`0`** 行｜`edbeb81..49b56eb` ＝ **`0`** 行｜`7574727b..49b56eb` ＝ **`0`** 行；**對照組[必非零]** `17fd981^..17fd981` ＝ **`2`** | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**·`git show HEAD:<path> \| tr -cd '\r' \| wc -c`） | 本批異動之 `6` 檔逐檔 ＝ **`0`**；**判別力[必非零]** ＝ 倉內 blob `data/V6.dxf` ⇒ **`12308`** | ✅ |
| `4` | 四簿期末（`probe_WG9267_issuer_measurers.py registry`） | 自誤 相異 **`386`**／MAX **`396`**（＝ 期初 `383`／`393` ＋ **三**號·缺號 `[106, 355, 356, 357]` **未變**）｜`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **一字未動** | ✅ |
| `5` | 坑之期末（`probe_WG9269_pit_index.py`·`rc = 0`） | 標籤 **`87`**／可解析 **`87`**（數字系 `26` ＋ 字母系 `61`）·🔴 命中 **`0`**·末標籤 **`bi`** ⇒ **下一標籤 `bj`**；母體 ＝ HEAD 追蹤 `.md` **`782`** 檔（期初 `780` ⇒ **`+2`** ＝ 本批新增之單與報告·**自洽**）；造甲 坑 `n` 定義處 `6` ≥ 1／造乙 人造坑 `0`／`0` ⇒ **器非紅**。**`ALPHA_LABELS`**：`git diff --numstat 7574727b..HEAD -- verify/probes/probe_WG9269_pit_index.py` **輸出空**，其 blob 期初 ＝ 期末 ＝ `7d174ac28e90b1a747d1354e4a245838e2e3c6cb` | ✅ |
| — | ↳ 併記（照實·⛔ 掩蓋） | 坑 `bh`／`bi` 之**引用處**由 `41`／`19` 增為 `42`／`21`——係本批報告**引述**該二坑之攔法所致；**定義處逐坑未變** ⇒ ⛔ 鑄坑（`GB-132` 之自指增長·已具名） | ✅ |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>`（受詞係 `index` ⇒ 復跑前先 `git add`） | 判定組（反引號 token）**`26`** 個／參考組（CJK 詞）`3` 個 ⇒ **命中 `0` 之字樣 ＝ `0`**·`rc = 0`。🩸 **首跑 `rc = 1`**·命中 `0` 者 **`6`** 個（`uniq`／`deletions`／`_first_corner_alloc_dir`／`rot90`／`cut_coords`／`first_corner_alloc_dir`）⇒ **補其受詞之實測**（見 `§四` 之「payload 所載碼面受詞之當場現查」表與 `§六`／`§九`）後始綠——⛔ 以「意思有到」充命中 | ✅ |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末**相異 `0`**（判別力[必非零]：同法對 `17fd981^` vs `17fd981` ⇒ 相異 **`2`**）｜`verify/baselines` **`298`** 檔·框 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**）｜`p3a` `git merge-base --is-ancestor 05a11bd6… HEAD` ⇒ **`rc = 1`**（非祖先）｜`WV_BAKE` ＝ **`None`**｜`K-9-29` 任何子項⛔ 落地、`GB-79`／`167`／`168`／`169`／`170` **⛔ 修** ⇒ 由「`34` 檔 blob 相異 `0`」**機械蘊含** | ✅ |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | 該 ref 族計 **`20`** 個；`verify/W-G.9-289-gb170` 命中 **`0`**（⇒ ⛔ 開·本批亦⛔ 開任何分支）；**判別力[必存在]** `verify/W-G.9-269-p3a` 命中 **`1`**（`05a11bd665f6c2790ff821c9e3a1606651e345d4`） | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（**`push` 後**） | 🛑 **於本 commit 推送後出艙於聊天**（⛔ 預寫·`§九` 第 `15` 項之受詞） | — |

---

## `§十二`　逐 `commit`

| # | `commit` | 標題 | `numstat`（逐檔·`added`／`deletions`） |
|---|---|---|---|
| `1` | `edbeb81ffc4f2f220b56af13ef6004a54e09c8a9` | `W-G.9-291 工項零：本單原封入倉（三方逐位對拍·受詞自證·同格載產生指令）⛔ 零生產碼` | 單 `185`／**`0`** |
| `2` | `49b56ebf2da3e4bc9f0e74394ada9e58e336ecfc` | `W-G.9-291 工項一／二／三：鑄 自誤 394〜396 ＋ 二則末端追加 ＋ 二候選路之接觸面現查 ⛔ 零生產碼` | `GB` 簿 `37`／`0`｜報告 `368`／`0`｜自誤簿 `35`／`0`｜落檔 `459`／`0`｜器 `460`／`0` |
| `3` | （本 commit） | `W-G.9-291 收工：收工閘 1〜7 之實測（報告入倉後量·嚴格末端追加）⛔ 零生產碼` | 報告之收工段（`deletions` ＝ `0`） |

🔒 **閘之量測與其所守護之動作為<u>分開之呼叫</u>**（`W-G.9-199` 常規補款 `一`）——
閘 `1`–`6'` 之量測與 `commit`／`push` **⛔ 置於同一命令塊**。
🛑 **KL 之放行仍<u>未被動用</u>**；本批**⛔ 改生產碼一字、⛔ 開任何分支、⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號**。

🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批）。
