# W-G.9-228R　`G2` 之框改標記法 ＋ 行號錨通則 ＋ 引擎硬限 `{0,3.5}` ＋ 前提更正（執行報告）

**批次性質** ＝ 零生產碼·append-only。**基座** ＝ `d2d3692edd44d3160f9df92e5f57a4a7ca36bd4a`（`left-right` `0 0`）。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
payload 一律 `Write` 落檔、⛔ heredoc（`GB-143` 補款二）。

---

## 工項零　✅ PASS

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **6425** | 92 | **0** | `c4dfdb616f89a23dda4238888183cff88856f1e08b1374eef92027014878ad4c` |
| 入倉（索引側 blob） | **6425** | 92 | **0** | `c4dfdb616f89a23dda4238888183cff88856f1e08b1374eef92027014878ad4c` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）。對照組：翻首位元組 ⧧ blob ＝ `True`；blob 自比 ＝ `True`。

---

## 工項一　`G2` 之框改**標記法** ✅

### a　現態命中集列之落地

落地處 ＝ `GB-144` 節（`docs/reports/W-G.4_泛用阻塞項登記表.md`）**節內末端**之補款三；
Δ（本檔全批）＝ **+12346** B（＝ 補款三 **3160** ＋ 分隔 `2` ＋ 分隔 `4` ＋ `GB-149`／`GB-150` **9177** ＋ `1`）。

其列逐字之要旨：`現態命中集@d2d3692　＝　app.py:10947／:12319／…／:23846`（**13** 個）。
🔒 其框（`VR-091` 三軸）：來源 `blob@d2d3692`／母體 `app.py` **1417936** B・**23942** 列／**列框**；
命中式 ＝ `f3L_setback_default`（**10**）∪ `禁靜默 3.5`（**4**）∪ 以 `3.5` 為 `.get` 預設（**3**）＝ **13**。

### b　新框之正字（落於 `GB-144` 節內·**既有正字處**）

🔒 **`grep` 定位之依據**：`G2` 之框**自述**三處皆在 `GB-144` 節內（該檔裸行號 `3820`／`3886`／`3934`），
⇒ **該節即既有正字處**（⛔ 新創檔）。

> **`G2` 之母體 ＝ `GB-144` 節內，標記為 `現態命中集@<commit>` 之列中，其 `<commit>` 為最新者；
> 取該列之 `app.py:NNN` 全部檔列。⛔ 依位置取值、⛔ 取全節聯集。**

🔒 **「最新」之判準 ＝ `git` 祖先序**（`merge-base --is-ancestor`），**⛔ 字典序／出現順序**；
多列同 `commit` ⇒ **停並回報**（⛔ 自擇）。
🔒 **⛔ 依位置之理由（發單側逐字）**：「後續若追加**部分**清單，位置法將取到部分集 ⇒ **假綠**」。

### c　新框重跑 ✅ **PASS**（雙向差集皆空）

| 端 | 元素數 | 集合 |
|---|---|---|
| `app.py` 現態 | **13** | `[10947, 12319, 12675, 12677, 12680, 12703, 14259, 19685, 19720, 19725, 22395, 22884, 23846]` |
| **新框**（標記法·`:3959`@`d2d3692`·全節僅 **1** 列） | **13** | 同上 |

🛑 **新框 ∖ 現態 ＝ `[]`／現態 ∖ 新框 ＝ `[]`**。
🔒 **判別力自證（二框須可分）**：**舊框**（全節聯集）＝ **18** 個 ⇒ 與現態**對稱差 5** 個
（`19684`／`19689`／`22359`／`22848`／`23810`·皆補款一之歷史值）
⇒ 新框相符 `True`／舊框相符 `False` ⇒ **二框判然可分**（⛔ 恆同）。
🔒 對照組：`f3L_setback_default` 命中 ＝ **10**（> 0）／一必不存在之同形鍵 ＝ **0**。

### d　維護義務（已入補款三 `④`）

> 每次 `app.py` 之相關檔列位移，須於 `GB-144` 節**新增**一列 `現態命中集@<新 commit>`（⛔ 修改舊列）；
> 未新增者 ＝ `G2` 之**已知失效**，須於該批出艙時**具名**（⛔ 靜默、⛔ 以舊列充現態）。

🔒 併載 `<commit>` 之語意 ＝ **該命中集所量之 `app.py` 所在之 commit**，⛔ 登記列之 commit。
🔒 併載歷史列之定性（裸行號 `3774`／`3868`／`3965`·**皆⛔ 標 `現態命中集@`**）⇒ **新框對舊列免疫**。

---

## 工項二　鑄 `GB-149`（行號錨之通則）

**號之推導**：`GB` 簿（**641406** B／**4470** 列）定義框相異 ＝ **64**／`MAX` ＝ **150**（本批鑄 `149`＋`150`）
⇒ 前次末號 **148**；次號 ＝ **GB-151**。

**受詞**　凡以 **append-only 簿內之行號**為錨之閘，其母體**隨簿之增長而單調擴張**；
行號一經位移，舊值⛔ 可撤而閘之母體含之 ⇒ 🔴 **永久轉紅且任何登記皆無法使其復綠**。

**已發生**　`G2`：位移前 現態 `12` ＝ 所登 `12` ✅；位移後 現態 **13** vs 所登 **18** 🔴。

**族之區辨（⛔ 併入 `GB-143`／`GB-147`）**

| | `GB-143` | `GB-147` | **`GB-149`** |
|---|---|---|---|
| 成因 | 轉義塌陷 | 哨兵自落地 | 🔴 **母體隨簿之增長而單調擴張** |
| 時態 | 一次性 | 一次性 | 🔴 **不可逆**（簿不可撤）|
| 可否「自扣本批增量」解 | 可 | 可 | 🔴 **⛔ 可**——舊值係**歷史正本**、非本批增量 |

**處置一**（已行·工項一）＝ 取「標記為現態者中之最新」。
**處置二**（🛑 列**通用化波**）＝ **錨改用符號名**（行號隨編輯位移，符號名不隨）。

### 回溯掃描（**窮舉變體**·母體 ＝ 索引側全部 `.py` **251** 檔·**列框**）

六變體之命中：① 正規式內之 `app\.py:` **86** 列／**37** 檔；② 形如 `:(\d…)` 之擷取式 **3** 列／**3** 檔；
③ 讀 append-only 簿之檔名字樣 **105** 列／**27** 檔；④ 讀 `CLAUDE.md` **139** 列／**71** 檔；
⑤ `findall`／`finditer` **48** 列／**26** 檔；⑥ 讀 `docs/` 之路徑字樣 **248** 列／**64** 檔。

🔒 **「行號錨待復驗之閘」清單 ＝ 2 支（⛔ 空集）**

| # | 檔·列 | 擷取式 | 母體 | 同 `GB-149` 之機制？ |
|---|---|---|---|---|
| `1` | `verify/probes/probe_order_preflight.py:134` | `re.finditer(r"app\.py:(\d{2,6})", body)` | **單之逐節本文** | 🟡 **部分**（⛔ 單調擴張，惟錨仍隨位移失準）⇒ **待復驗** |
| `2` | `verify/tools/wg99_anchor_audit.py:18` | `re.compile(r"CLAUDE\.md:(\d+)")` | 全倉之 `CLAUDE.md:<行號>` 引用 | 🟢 **否**——其**本身即失準偵測器**（用法逐字：「凡引用之行號 `>= P` 者，於插入後將失準」）⇒ 係**解**而非**病** |

🔒 其餘四變體之命中經抽樣核對**皆為註解／文件路徑之引用**、⛔ 閘之母體 ⇒ 排除。
🔒 **`G2` 自身⛔ 在此清單**——其係**單所定義之閘**、**⛔ 落地為 `.py`** ⇒ 掃描器取不到；
此點本身即一風險（**閘之定義只存於單**），**併期末批**。
🛑 **⛔ 逐處處置（併期末批）**。

---

## 工項三　鑄 `GB-150`（引擎硬限 `{0, 3.5}`）·🛑 停機款**⛔ 觸發**

### a　逐字復現（當場定位·⛔ 採單所抄之檔列）

`def _wf_tag_of(` 全檔命中 ＝ **1**（`app.py:14271`）：

| 列 | 逐字 |
|---|---|
| `app.py:14271` | `def _wf_tag_of(setback):` |
| `app.py:14272` | `    """退縮 float → 引擎情境 tag。硬對應 UC9898 雙情境；非 {0,3.5} 時 loud。"""` |
| `app.py:14273`–`:14276` | `if abs(float(setback) - 0.0) < 1e-9: return "0m"` ／ `if abs(float(setback) - 3.5) < 1e-9: return "3.5m"` |
| `app.py:14277` | `raise RuntimeError(f"🔴 七級調配：退縮 {setback} 非 UC9898 雙情境（0/3.5），引擎凍結錨不適用")` |

### b　呼叫鏈 ⇒ 🛑 **停機款⛔ 觸發**

| 項 | 實測 |
|---|---|
| 呼叫點 | **1** 處 ＝ `app.py:23846` |
| 其語境 | `with st.spinner("執行七級調配（f0→f4 全鏈；含 7-5 最佳化與終態整形）…")` 之**首行**；其下即 `_build_wf_ctx` → `wf_f0`〜`wf_f4` |
| 🔒 **是否在主分配路徑上** | 🟢 **否**——**限於七級調配**；Step G（G 值迭代·主分配）**⛔ 經此函式** |

🛑 ⇒ 單之停機款「工項三 `b` 顯示該函式在主分配路徑上」**⛔ 觸發** ⇒ **續行**。

### c　歸屬 ＝ **另鑄 `GB-150`**（⛔ 併入 `GB-144` 補款·理由如下）

| | `GB-144`（含第三層）| **`GB-150`** |
|---|---|---|
| 受詞 | 一參數之**取值路徑**三層皆非資料驅動 | 🔴 **下游消費端之值域限制 ＋ `raise`** |
| 第三層之位置 | **驗證器** `verify/run_verification.py` 之硬寫二元組 | 🔴 **`app.py`**（生產路徑）|
| 失效條件 | 改為**資料驅動** | 引擎解除**雙情境硬綁** |

🔒 **⇒ 逕併將使二者之失效條件混為一談** ⇒ **另鑄**。
🔴 **與 KL 裁示之衝突**（已入 `GB-150`）：KL 明示退縮**逐案由都市計畫書規定**
⇒ **他案若定 `2 m`，本路徑直接 `raise`**。
🔒 **土地後果** ＝ 🟡 **條件式**——本案（`UC9898`）⛔ 有（其二情境恰為 `{0, 3.5}`）；**他案有**。
🛑 **歸屬 ＝ 通用化波之阻塞項**；**⛔ 於本批改任何一行碼**。

### d　同形之情境硬對應（窮舉變體·`app.py`）

| # | 處（裸行號）| 形 | 是否 `raise` |
|---|---|---|---|
| `1` | `14271`–`14277` `_wf_tag_of` | `if 0 / if 3.5 / raise` | 🔴 **是** |
| `2` | `22396`–`22401` `_yd_tag`（Y 波診斷 JSON 下載）| `if 0 / elif 3.5 / else f'{v}m'` | 🟢 否 |
| `3` | `22885`–`22890` `_p7_scn`（Excel 匯出情境標記）| 同上 | 🟢 否 |
| `4` | `14175` `_build_wf_ctx` docstring | `tag∈{'0m','3.5m'}。缺鍵 loud。` | —（文件）|

🔒 **⇒ 硬限而 `raise` 者<u>唯一</u>**；`2`／`3` 有 `else` ⇒ 他案值可通行（僅標籤變）。
🔒 對照組：`'3.5m'` 字面 ＝ **4** 列／`'0m'` ＝ **4** 列／`abs(… − 3.5)` ＝ **2** 列／`UC9898` ＝ **26** 列。

---

## 工項四　二則之登記 ＝ **併記**（⛔ 鑄號）

🔒 **`grep` 之結果**（母體 ＝ 自誤簿·**列框**）：其一之同受詞號 ＝ **21** 則
（`115`／`116`／`193`／`197`／`204`／`214`／`215`／`220`／`222`／`225`／`230`／`235`／`237`／`238`／
`244`／`247`／`255`／`266`／`270`／`273`／`281`）；其二 ＝ `自誤 282`
⇒ **二者皆已有同受詞之號** ⇒ 依單之令**以併記行之**（自誤 `MAX` 仍 **285**·次號 **286**）。

### 其一（寄於 `自誤 266`）｜🔴 **當場復算之結果與單所述不符**

母體 ＝ `app.py` **1417936** B／**23942** 列·**列框**·`blob@d2d3692`：

| # | 框 | 實得 |
|---|---|---|
| `1` | 全檔列框（含 `3.5` 之全部列）| **44** |
| `2` | 排除註解列 | **30** |
| `3` | CC 所報（`-227R`「`3.5` 字面（**退縮相關**）」）| **11** |
| `4` | **單所指之發單側框**（含 `3.5` 且含 `f3L_setback_default` 或 `_SB_DEFAULT_M`）| 🔴 **5**（**⛔ `4`**）|
| `5` | **實得 `4` 之唯一框** ＝ `-227R` 表之**子分類**「🔴 取值用之字面」 | **4** |

🔒 **二重更正（承單之「⛔ 採用本單所載之任何數為錨——全數當場復算」）**
1. 🔴 **單所述之框得 `5`、⛔ `4`**——`12680` 之訊息字串同時含 `3.5` 與該鍵。
2. 🔴 **實得 `4` 者係 CC 自身之子分類框**，**⛔ 發單側自行 grep 之框**。

⇒ **違反之真機制**（依證據修正·⛔ 沿用單所擬）：發單側所出之 `4` **本身正確**，
惟其框係 CC 表之**子分類**「取值用之字面」，而出艙表述為「**`3.5` 字面**」（＝ 框 `1` 之名·實得 **44**）
⇒ **引用他人之子分類數而略去其限定語** ⇒ **跨二族**（形式違 `VR-091` 一 `(3)`；機制同 `自誤 282`）。

🔴 **加重情節**：該違發生於發單側**復驗 CC 出艙**之時 ⇒ **立閘者於執行閘時違其所立之閘**
——該閘之**唯一執行者**同時是**唯一違反者**，**無第三方可攔**。
🔒 與 `自誤 116`（同單內自相矛盾）之別：本則係**跨批·於執行他人之出艙復驗時**。

### 其二（寄於 `自誤 282`）｜`telescoping` 被轉述為 `GB-67` 族

| 情境 | 拋出處 | 閘 | 是否 `GB-67` |
|---|---|---|---|
| 退縮 `0` | `verify/stepg_pipeline.py:1003` → `app.py` 裸行號 `9524` | `②-宗 圍堵閘` | ✅ 是（**未結案**）|
| 退縮 `3.5` | `verify/stepg_pipeline.py:1261` | 結構閘 `telescoping` | 🔴 **否**（harness 專屬）|

🔒 `圍堵閘破` 字樣：`stepg_pipeline.py` 命中 **0**／`app.py` **1**。
🔒 倉內既有之區辨：`-172R:27`「**⛔ 非 `②-宗 圍堵閘`**」／`:59`「中止點 🔴 **相異**」。
🔒 **族之通則須擴充**：凡 CC 原文含**區辨**或**保留**者，轉述時皆須逐字帶入（本則係該族之**第二實例**）。
**責** ＝ 發單側（二則皆是）。

---

## 工項五　兩情境之待決閘依賴鏈（`CLAUDE.md` 檔尾·正本表 `序 13`／`序 14`）

Δ ＝ **+4274** B（＝ payload **4267** ＋ 分隔 **7**）。

| # | 鏈 | 閘 | 所在 | 登記 | 自解條件 | 土地後果 |
|---|---|---|---|---|---|---|
| `13` | 一｜`0 m` | `②-宗 圍堵閘` | **`app.py`**（生產碼）| **`GB-67`·未結案** | `②-宗` 三項落地介面（`【缺】` 5）＋ 破量歸零 | 🔴 **有**（`2` 宗／`4.7950 ㎡`）|
| `14` | 二｜`3.5 m` | 結構閘 `telescoping` | `verify/stepg_pipeline.py`（harness）| **既存紅·准紅碼**（`W-G.9-4` 起）| 鏈斷成因獲解（⛔ 放寬容差）| 🟡 間接（遮蔽 `R4`〜`R6`）|

🛑 **`序 12`（`R4` 逐值迴歸待補·`W-G.9-227` 所立）之依賴 ＝ `序 14`**（⛔ `序 13`——後者屬 `0 m` 情境）。
🛑 **本批⛔ 處置任一鏈、⛔ 放寬任何閘。**

---

## 閘 `P` ／ `numstat` ／ `left-right`

| 檔 | B | `sha256`（改前 ＝ 改後·逐位相符） |
|---|---|---|
| `app.py` | 1417936 | `091efa9b77c288082edea9cce809c0bb53c8b927f7cd0ea2e646a962c6409628` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

🛑 **閘 `P` ＝ PASS**（本批⛔ 動生產碼一位元組）。

| 檔 | 增 | **刪** |
|---|---|---|
| `CLAUDE.md` | 52 | **0** |
| `docs/orders/W-G.9-228_G2框改標記法與行號錨通則.md` | 92 | **0** |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 187 | **0** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 84 | **0** |
| **四檔小計（穩定值）** | **415** | **0** |
| `docs/reports/W-G.9-228R_…_執行報告.md`（**本報告**）| 🔒 ⛔ 於本報告內釘死（自指）| **0** |

🛑 **`deletions` ＝ `0`**（append-only 檔全數）。🔒 `left-right`（推前）＝ **`0 0`**。
追蹤 ＝ **1627** 檔／`.md` ＝ **555** 檔（`ls-files -z`）。
🔒 **`戒 40`（二端並報）**：`GB` 簿 Δ `+12346` ＝ `3160`＋`2`＋`4`＋`9177`＋`1`；
自誤簿 Δ `+5838` ＝ `5831`＋`7`；`CLAUDE.md` Δ `+4274` ＝ `4267`＋`7`——**逐檔差 `0`**
（**以索引側 blob 為錨**·承 `W-G.9-227` 之教訓）。

## 🟡 仍待

1. 🟡 **行號錨待復驗之 `2` 支閘**（`probe_order_preflight.py`／`wg99_anchor_audit.py`）——**併期末批**。
2. 🔴 **`GB-149` 處置二（錨改符號名）**——列**通用化波**。
3. 🔴 **`GB-150`**（引擎硬限 `{0,3.5}`）——**通用化波之阻塞項**；🔴 **須上呈 KL**（與其裁示衝突），交發單側依例上呈。
4. 🟡 **`G2` 之定義只存於單、⛔ 落地為 `.py`**——掃描器取不到，**併期末批**。
5. 🔴 **`:19720` 之 `3.5` 補正**（`W-G.9-227` 工項四之最小形）——含生產碼、須另單並經 KL 放行。
6. 🟡 `序 12`／`序 13`／`序 14` 三列之依賴鏈（見工項五）。
