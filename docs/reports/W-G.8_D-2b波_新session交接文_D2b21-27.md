# CC 新 session 交接文：**K-6-A2 D-2b 波**（`D-2b-21`〜`D-2b-27` 已落地）

**產出**：CC／2026-08-10　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`　**交接時 HEAD**：`2823ddb`（`origin` 實查同·工作樹追蹤檔異動 **0**）
**前一份交接文**：`docs/reports/W-G.8_D-2b波_新session交接文.md`（涵蓋 `D-2b-2`〜`D-2b-20`·**仍在**）

> # 🔴🔴 最先讀這一段
>
> **本波之 `D-2b-23`／`D-2b-24` 動了生產碼、有<u>土地後果</u>**（唯二兩筆）：
> `79bf2d7`（`K-9-5-4 ②` 第 2 宗近側界）與 `c89f1dc`（池帶校回幾何餘）。
> 其餘 **16 筆全為零生產碼**。
>
> **兩者皆<u>未收官</u>**：`D-2b-23` 之 §9-3 停機（5 格與事前預測不符）**仍在**；
> `D-2b-24` 已達成其**改後之靶**（類 B 歸零），但**類 A 未動**。
> ⇒ ⛔ **不得以「跑完了」當作已通過**——請先讀 §二（停機／待裁）。

> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前**重跑該指令**（行號／計數皆易腐）。
> 🔒 出處標注依 `grep -n "【倉】" CLAUDE.md`——`【倉】` 必附 `檔:行`；
> `【單】` ⛔ 不得被下游引用為事實。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD                                      # 應 ≥ 2823ddb
git ls-remote --heads origin | grep s1-endpart                  # ⛔ 須與 HEAD 同·收官硬前置
python -c "import shapely; print('shapely', shapely.__version__, '| GEOS', shapely.geos_version)"
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md       # 現值 57
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # ⛔ 節號以現查為準
grep -oE "^### 🔒 K-9-5-[0-9]+" docs/rulings/K-6_街角地分配程序與可分配判準.md | sort -u
python verify/fixtures/test_D2b15_tilegate.py                   # 應 rc=0
python verify/fixtures/test_D2b16_mutation.py                   # 應 rc=0
```

⛔ **禁 `git reset --hard`**。⛔ **禁對 `verify/out/` 用 `git add -A`／`.`**。
⛔ **禁覆寫任何已入庫 log**（探針已內建 `_resolve_out` 守衛：設 `WV_OUT_NAME=<新檔名>`·fail-fast）。
🔴 **grep 蒐證一律排除巢狀 worktree**：`| grep -v "^\./\.claude/worktrees/"`。

⚠️ **若你在 worktree**：主倉可能已檢出 `wip/s1-endpart` ⇒ 同分支不可二次 checkout。
改用 `git checkout -B <本地支> <sha>`（⛔ 非 `reset --hard`），推送用
`git push origin HEAD:wip/s1-endpart`。

---

## 一、現況

| 項 | 值 | 現查指令 |
|---|---|---|
| 分支／HEAD | `wip/s1-endpart` ／ **`2823ddb`**（`origin` 同·異動 0） | 見 §〇 |
| 環境 | `shapely 2.1.2 \| GEOS (3, 13, 1)` | 見 §〇 |
| 登記表 | **57 列**（本波新開 `GB-51`〜`GB-57`） | 見 §〇 |
| 失敗考古末節 | **59**（本波新增 **57／58／59**） | 見 §〇 |
| 正典 `K-9-5-*` | `K-9-5-1`〜**`K-9-5-9`** 已占用 ⇒ 新則取 `K-9-5-10` | 見 §〇 |
| 期望 FAIL 名單 | 仍為 `K6A2_期望FAIL名單_D2b1切換後.txt`（**22** 名目）·**本波一列未改** | `grep -c "^🔴 FAIL" <該檔>` |
| `run_all` | **本波跑過 3 次**（皆唯讀量測·名單零變更） | `ls verify/out/D2b2[34]*runall*.log` |
| 12 格全保真基準 | **未重烤**（仍為 `D-2b-16` 之值） | `verify/out/probe_D2b16_baseline.log` |

### 1-1 逐批（**全部已 push**）

| 批 | SHA | 生產碼 | 要旨 |
|---|---|---|---|
| D-2b-21 | `6b7ffa1` | ⛔ 零 | 🛑 T2 接線之靶與範圍**互斥**（停機上呈）＋ `GB-51` |
| D-2b-22 | `757951e`＋`ca8cf6c` | ⛔ 零 | KL 裁定落章 ＋ 池側殘差**逐界面全額歸因**（三問全答） |
| D-2b-23【甲】 | `acc3eac`／`ea9fdc4`（A/A′）→ **`79bf2d7`（B·🔴 有碼）** → `ecf35d6`（C） | ✅ **有** | `K-9-5-4 ②` 之另一半：第 2 宗近側界併為單一條線 ⇒ 🛑 §9-3 停機 |
| D-2b-23 補 | `3891d1e` | ⛔ 零 | `run_all` 唯讀量測（第一次）＋ KL 三點落地 |
| D-2b-24 | `0ca508c`／`4aebbfe`（A/A′）→ **`c89f1dc`（B·🔴 有碼）** → `78079fa`（C） | ✅ **有** | 池帶**校回幾何餘**（類 B）＋ `GB-52` 盲區 ＋ `run_all` 第二次 |
| D-2b-25 | `d2015d8`＋`aec849a` | ⛔ 零 | KL 八條裁示落地為 **`K-9-5-9`** ＋ `GB-53`〜`GB-57` ＋ 開波 **`W-G.9`** ＋ 考古節 59 |
| D-2b-26 | `cac03fb` | ⛔ 零 | `GB-54` **方向更正** ＋ `K-9-5-9` **原話並列** |
| D-2b-27 | `787e9d6`（A）→ `934074d`（C）→ `2823ddb`（補正） | ⛔ 零 | `D-3` 重烤**前置盤點**（P1 部分證偽） |

---

## 二、🛑 停機／待裁（**開工前必讀**）

### 2-1 🛑 `D-2b-23` 之 §9-3 停機**仍在**：類 A 未解

**事實**：`K-9-5-4 ②` 之實作（`79bf2d7`）令**街角第 1 宗與第 2 宗之界面只剩一條線**
——`0m R2` 之 `宗0×宗1` 由 `4.9205` **歸零**（✅ 規格達成）。
**但規格<u>必要而不充分</u>**：街角宗之傾斜量**大於第 2 宗之寬度**時仍切進第 3、4 宗。

| 格 | 甲後殘餘（`②-宗`） | 出處 |
|---|---|---|
| `0m R2` | **`45.9766`**（宗0×宗3 `26.1624` ＋ 宗0×宗2 `19.8142`） | 【倉】`verify/out/probe_D2b23C_attrib_after.log` |
| `0m R5` | **`56.3293`** | 同上 |

🔒 **KL 已裁：類 A <u>本批不動</u>、具名待決**；⛔ **不得動第 3 宗以後之任何界線方向**。
⚠️ **三個候選答法（寬度規則／逐宗傳遞／依序切除）後果不同，須 KL 另裁。**

### 2-2 🛑 `K-9-5-9`【裁】1 之**未裁事項**

正典逐字（`grep -n "^\*\*【裁】1\." docs/rulings/K-6_街角地分配程序與可分配判準.md`）：
街廓 R1 之「選槽依據與實際落地不符」（結構閘 理論＝實跑）列為**另案**；
⛔ **未裁事項**：**①本波是否因它停機　②`D-3` 重烤時點**
——⛔ **二者不得由「另案」推定，須另行請裁**。

⚠️ **R1 現為兩情境之停點**：`0m`／`3.5m` 之 `StepG` 皆停在 **Gate C**
（`結構閘 理論＝實跑`）——係**新抵達**（校回前早死於池閘、Gate C 從未執行），
⛔ **非新缺陷**。

### 2-3 🛑 `D-2b-25` §2 之**圖證待補**

KL 2026-08-10 之**三張示意圖**（第 1 種分配線方向兩版、第 2 種）**未入倉**
——成因：**該批訊息未附任何圖檔**；依 `CLAUDE.md`「施工單必須自足」⇒ 照實未辦。
🔒 **待補狀態已寫入正典本文**（`K-9-5-9` 節末之 🛑 段）。
⚠️ **`P0`／`Ps`／`S1` 之定義正典<u>已載</u>**於 `K-9-5-4` ⇒ 補送時**只補差額**，已載者不得重寫。

### 2-4 🛑 `K-9-5-9`【裁】2 之「容許量」門檻**地位未裁**

`D-2b-26` §2 之**原話對照**顯示：KL 原話為「**有**宗地互疊就不產出成果」（⛔ 未設門檻），
而形式化文字加了「**超出捨入容許量者**」之限定（claude.ai 依閘之設計所加）。
🔒 **衝突時以原話為準** ⇒ ⛔ **實作時不得逕以現行閘上界
（`(宗數−1)×0.005×深度`）當作「容許量」之正當性依據；該上界地位須另行請裁。**

### 2-5 🛑 `GB-53`〜`GB-56`：**已登記、未修**（其中三項標「需域裁」）

| 項 | 要旨 | 域裁？ |
|---|---|---|
| `GB-53` | `_end_band` 仍以 **⊥ALLOC 垂距**構帶 ＝ `K-8` 已作廢量法之同型；與 `K-9-5-9`【裁】4 相違 | 🛑 需（有土地後果：末端塊去留） |
| `GB-54` | `stepg` `raise` vs `app` `st.warning` ⇒ 驗證複刻件與生產偏離；**`GB-52` 盲區之真因** | 🛑 需（對齊時點與範圍） |
| `GB-55` | 三閘失敗一律冠「抵費地計算失敗」、宗-宗互疊不指認宗對 | ❌ 不需 |
| `GB-56` | 檢查破時「該擋的沒擋、不該少的少了」 | 🛑 需 |

🔴 **`GB-54` 之修法方向已於 `D-2b-26` 更正**——現行**兩種行為皆不合裁**；
合裁之目標為**第三種**：①逐街廓失敗→記錄後**繼續檢查其餘**；②全部檢查完→**一處未過即全不產出**。
⛔ **不得把 `stepg` 降為警告；⛔ 亦不得把 `app` 改為首個失敗即中止。**

> 🔒 **【`D-2b-29` §2-1 加註·⛔ 上文原句不刪】`GB-54` ⛔ <u>不是</u> `R1` 之卡點。**
>
> 🔴 **先記一項倉態事實（⛔ 施工單所稱之「現載」句，倉內查無）**：
> 施工單 §2-1 稱本檔現載「`GB-54`：R1 卡點。它同時是 `D-3` 之前置」——
> **現查 0 命中**：`grep -rn "卡點" . --exclude-dir=worktrees` ⇒ **5 命中**，
> 全部出自 `D-2b-28` 之報告與 `K-6` 之 `K-9-5-9` 加註 (二)；
> 且 `GB-54` 與「前置」於**本檔與登記表皆 0 同列**。
> ⇒ **本加註係「補記事實」，⛔ 非「更正既有錯句」**——無錯句可更正。
>
> **事實**（`D-2b-29` §1-2 #1 現查）：`GB-54` 之觸發點（`抵費地計算失敗`）
> 於 `verify/out/D2b24_runall.log` **命中 0**、**現行未發動**；
> `R1` 之實際中止因為 **`結構閘 理論＝實跑 破`**（同 log 六處）。
> ⇒ **單修 `GB-54` 不使 `StepG` 抵達 `R2`–`R6`。**
>
> 🔒 **正典依據**：`K-9-5-10`【裁】1 逐字「⛔ 不得解為 `GB-54`」
> （`grep -n "^### 🔒 K-9-5-10 " docs/rulings/K-6_街角地分配程序與可分配判準.md`）。
> ⚠️ 併看同節 `K-9-5-10-a` 第 1 點——`GB-54` 仍為**真阻塞項**、⛔ 未被撤銷，
> 只是**它不是 R1 停在 Gate C 的原因**；二者⛔ 不得互代。

---

## 三、🔴 已作廢·⛔ 不得引用之數字

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| `D-2b-23` 施工單 §3-3 | 「其餘六處落在 `0.92`–`1.02`」 | **claude.ai 已具名作廢**（重發更正本）；該封閉式**降為機制旁證**，⛔ 不得入閘／不得列驗收／不得作停機依據 |
| `D-2b-24` 原施工單 §1-4／§3-1 | 靶「三格歸零、其餘九格逐位不變」 | **作廢**——校回使 `①' 覆蓋閘` **構造恆真**；新靶＝**抵費地面積之逐格增加量**（`W-G.8_D-2b-24A2_…md` 補-2） |
| `D-2b-24` 原單 §1-2 | 「四項 × 兩情境 ＝ 8 個斷言」 | **5 項 × 2 ＝ 10**（AST 現查·`W-G.8_D-2b-24A_…md` §1-1） |
| `W-G.8_D-2b-23C_…md` §8-1 | `near_dir` 命中 **`25`** | **`24`**（`grep -c "near_dir" app.py`·已加具名更正·`GB-57`） |
| `W-G.8_D-2b-27C_…md` §6-1 | 凍存三子樹檔數 `69`／`70`／`64` | **`67`／`68`／`68`**（已加具名更正·合計 `218` 不受影響） |
| `D-2b-25` 施工單 §2 | 「三張示意圖已提供」 | **未附**（§2-3） |

---

## 四、生產碼現況（**本波唯二兩筆有碼變更**）

### 4-1 `79bf2d7`（`D-2b-23` commit B）：第 2 宗近側界

| 落點（自癒 grep） | 內容 |
|---|---|
| `grep -n "near_dir=None) -> dict" app.py` | `solve_G_binary` 新增 `near_dir`；`_cut_strip()` 為**唯一切帶入口** |
| `grep -n "界面單線鏈" app.py` | 推進迴圈（app 左/右）之 `near_dir` thread |
| `grep -n "_near_dir" verify/stepg_pipeline.py` | stepg 之鏡射（#20 同源） |

🔒 **`near_dir is None` ⇒ 原式一字未動**；第 3 宗以後 `near_dir == allocation_dir`
⇒ `_block_strip` **同向委派**落回單線 ⇒ **逐位不變**（R6 之 `SIDE ∥ ALLOC` 亦走此路）。
⛔ **未觸 `_n_alloc`**（W 量測軸）——`allocation_dir` 一槽兩用，`near_dir` 只改近側邊。

### 4-2 `c89f1dc`（`D-2b-24` commit B）：池帶校回幾何餘

`grep -n "D-2b-24：幾何餘" app.py` ⇒ `_pool_strips_for_block` 之**步驟 5b**（加性殘餘）。
🔒 **加性**：既有池帶逐位不動 ⇒ 無殘餘之街廓輸出逐位相同；
判準沿用 **T1 既有** `buffer(-1e-4).is_empty` ⇒ ⛔ **未新訂常數**。

### 4-3 🔴 **`①' 覆蓋閘`自此<u>構造恆真</u>**

`池 ⊇ 街廓 − ∪宗` ⇒ `union(宗+池) ≡ 街廓` ⇒ `cover_resid ≡ 0`。
⛔ **其 `0` 不得作為任何驗收憑據**（引用須併記「構造恆真·非證據」）；
⛔ **但不得刪除／停用／標作廢**。
⇒ **承重閘換位為守恆式** `|ΣG − Σ宗幾何|`（`grep -n "def _acct_geom_tol_block" app.py`）。

---

## 五、次步（**KL 未指定時之預設順位**）

| 序 | 事 | 前置 |
|---|---|---|
| 1 | **請 KL 裁 §2-2 之兩項**（本波是否停機／`D-3` 重烤時點） | 正典明令不得推定 |
| 2 | **類 A 之處置**（§2-1·三個候選答法） | 🛑 待裁 |
| 3 | **`D-3` 重烤**：`D-2b-27` 之盤點已備 ⇒ 見 §六之三項事實 | 🛑 待裁（授權僅限 `D-3`） |
| 4 | **`W-G.9`**（第 2 宗最小規定範圍） | 🛑 待圖證（§2-3）＋ §2-1 之判讀待實測坐實 |
| 5 | `GB-53`〜`GB-56` 之處置 | 🛑 三項需域裁 |

---

## 六、`D-3` 重烤之三項事實（`D-2b-27` 盤點·**⛔ 非建議**）

1. 🔴 **P1 只對 12 個 CSV 成立**——現用 **80** 檔中 **67** 檔（`v3/` 17 ＋ `wf/` 50）
   之 `PROVENANCE` **自載為 CC harness 實跑** ⇒「與 driver 非同源」性質對其**從未成立**。
2. 🔴 **現狀重烤之覆蓋與需求相反**：`StepG` 首個街廓失敗即中止 ⇒
   烤得到的是**不過期且非同源**之上游 12 檔；烤不到的是**已過期且本即同源**之下游 67 檔。
3. **`E系列實測快照` ×2 之產出者不可判定**——`PROVENANCE.md` 清單未列該二檔
   ⇒ 建議 `D-3` 動工前先補其 provenance 記載。

✅ **P2 成立**：自 `D-1`（`c56067f`）起，現用基準檔被修改數 ＝ **0**。

---

## 七、🔑 本波最貴的五條（**給下一手**）

1. **靶可能與範圍互斥**——`D-2b-21` 之停機即此：閘之母體在呼叫端，改被呼叫者動不到它。
   ⇒ **接到靶先問「這個量由誰產生」**。
2. **恆等式不是檢定**——`D-2b-22` 之「殘差 `±0.000000`」係構造恆真；
   `D-2b-24` 之 `①' 覆蓋閘` 亦然。⇒ **綠之前先問「它有沒有可能不綠」**。
3. **不變條件須指明<u>所守護之預測</u>與<u>該量於何處被讀</u>**（`CLAUDE.md` 新立·考古 58）
   ——只點變數名不足以界定射程；**同名不同位置，射程不同**。
4. **有輸出不等於量對**（考古 59 之同族）——本波三次量錯（側別鍵錯／參照選錯／off-by-one），
   三次外觀皆是「跑出數字了」；**救回來的都是與獨立量對不上**，不是讀碼讀出來的。
5. 🔴 **「檔數／列數」一律由指令產生**——我在寫「落筆前須現查」那條戒的同一份報告裡，
   把凍存檔數目測錯了（`D-2b-27` 補正）。⇒ **戒律不會自己套用到下一行。**

---

## 八、收官前置（不變）

`git rev-parse origin/wip/s1-endpart` 已含該 commit——**不綠不推、不推不報**。
⚠️ `run_all` 約 **15–20 分鐘** ⇒ 背景跑＋等通知；⚠️ **長跑期間 ⛔ 不得改 `app.py` 與 `verify/`**。
⚠️ **四份期望 FAIL 名單並存**——現行者為 `D2b1切換後`（**22** 名目），其餘刻意保留供考古、⛔ 勿誤用。
⚠️ **探針拒絕覆寫**：跑複驗須 `WV_OUT_NAME=<新檔名>`（`_resolve_out` 守衛·fail-fast）。
