# W-G.9-33：`W-G.9-32` 之交接查核與錨更正

**基座**：`wg933-anchor-fix` @ `0e44058`（＝開場時之 `origin/wip/s1-endpart`）
**批號現查**：`W-G.9-33` ⇒ **倉內檔名命中 0**·**全倉字樣 0** ⇒ 未占用
（判別力自檢：`W-G.9-32` ⇒ **4 檔** ⇒ 該式非恆 0）
**性質**：⛔ 零生產碼 · ⛔ 零 baseline 更新（`VR-012`）· ⛔ 未動 `app.py`／`verify/` 下任何檔
**受詞**：以施工單「∥SIDELINE 落地前之受詞校正現查」**原文**對 `W-G.9-32` 之入倉物做**獨立復現**
（⛔ 不採信其任何數字·CLAUDE.md🔒「自評 gate 綠不可替代獨立復現」）

**結論碼**：
1. ✅ **三項結論獨立復現成立**（`Q1`／`Q2`／`Q3` 全部重跑坐實）。
2. 🛑 **施工單 `§5⑤` 未辦**（本批補辦）。
3. 🔴 **兩處錨瑕疵**，已擴散進 `VR-013`（生效中）與 `GB-64`；**⛔ 皆不推翻結論**，以就地加註更正（KL 裁 2026-08-14）。

⚠️ **行號綁 commit**：本報告所載之一切生產碼行號，皆成立於 **`6e88094`**（＝`0e44058^`）；
因 `0e44058` **僅動 4 個 `docs/` 檔**（見下 §一），`app.py`／`verify/` 之 blob 逐位未變 ⇒ 於 `0e44058` **亦成立**。

---

## §一 開場現查（施工單 §0·原樣貼）

```
$ git rev-parse --short HEAD && git log -1 --format="%h %s"
6cb77b7
6cb77b7 skills(泛化波前置): 新增 expand-contract-refactor＋fixture-provenance（純文件·禁動碼）

$ git rev-parse --short origin/wip/s1-endpart
0e44058

$ python -c "import shapely, ezdxf, pandas, numpy; print('deps ok')"
deps ok
```

🔴 **開場即測得基座不符**：本 worktree（`handoff-verification-gb47-3ca5c2`）之 HEAD 為 `6cb77b7`，
與施工單所預設之基座**分岔 367 commits**，`docs/reports/` 內**無** `W-G.9-32` 之報告。
⇒ §二〜§四之現查全部改以**共用物件庫直讀**（`git show 6e88094:<path>`）辦理，⛔ 未動任何工作副本；
§五入倉前方另立分支 `wg933-anchor-fix` 自 `0e44058` 切出（`wg925-handoff` **已被 worktree
`frosty-agnesi-fa87af` 佔用** ⇒ ⛔ 不可於本 worktree checkout）。

**`0e44058` 之異動集**（裸·無過濾）：

```
$ git -c core.quotePath=false show --stat --format="" 0e44058
 docs/design/W-G.9-30_∥SIDELINE界線之接線設計.md |  20 ++++
 docs/reports/W-G.4_泛用阻塞項登記表.md          |   1 +
 docs/reports/W-G.9-32_受詞校正現查.md           | 148 ++++++++++++++++++++++++
 docs/驗證裁定登記表.md                          |  14 +++
 4 files changed, 183 insertions(+)
```

⇒ **4 檔·183 加·0 刪**·⛔ 無 `app.py`／⛔ 無 `verify/` ⇒ 施工單 `§1`「零生產碼」與 `§4 S-1` **成立**。

**必讀六份之存在性**（逐檔·於 `6e88094`）：六份**全部存在**
（`157`／`481`／`1984`／`2851`／`261`／`218` 行）。

---

## §二 三項現查之獨立復現（施工單 §2）

### 現查 A：破量之受詞 ⇒ ✅ **復現成立**

母體 `verify/probes/probe_D2b17_dual.py`（於 `6e88094`），逐字實得：

```
340:def measure_A(pieces, biz):
344:    for i in range(len(pieces)):
345:        for j in range(i + 1, len(pieces)):
351:    for pi, pg in enumerate(pieces):
352:        for bi, bg in enumerate(biz):
356:                detail.append((":9050", f"池{pi}×宗{bi}", v))
358:    for i in range(len(biz)):
359:        for j in range(i + 1, len(biz)):
363:                detail.append((":9062", f"宗{i}×宗{j}", v))
406:    _biz = [p for p in (biz_polys or []) if p is not None and not p.is_empty]
535:    def _spy(block_poly, d_hat, corner_pt, allocation_dir, biz_polys,
540:            pieces, biz = replicate_pieces(ns, block_poly, d_hat, corner_pt,
541:                                           allocation_dir, biz_polys)
614:        pieces, biz = c["pieces"], c["biz"]
618:        aP, aB, aZ, adet = measure_A(pieces, biz)
```

生產端（`app.py`·於 `6e88094`）：

```
20019:                                offset_geoms = _pool_strips_for_block(
20020:                                    blk_poly, d_hat, corner_pt, allocation_dir_block,
20021:                                    allocated_polys, _label=blk_label, _depth=avg_depth_default)
```

⇒ **決定枚舉之行 ＝ probe `:358-359`**；**所枚舉之容器 ＝ `biz` × `biz`**、⛔ 不含 `pieces`；
`biz` 之源 ＝ `_pool_strips_for_block` 第 5 位置參數 `biz_polys` ＝ **`allocated_polys`（業主宗集合）**。
**溯源鏈十處逐字全對**，⛔ 無一處與 `W-G.9-32` 所載不符。

### 現查 B：`_corner_buffer_S` 之回傳能否影響該受詞 ⇒ ✅ **復現成立**

**式一**（`_corner_buffer_S` 內部切帶）：

```
8954:        _g, _ar = _block_strip(block_poly, d_hat, bp, w, allocation_dir=allocation_dir)
```
⇒ ⛔ **未傳 `n_hat_far`**。其 docstring 逐字自陳（`:8898`）：
`⚠️ **D-2b-11【a-1】交叉引用**：\`_block_strip\` 已加雙線形式 \`n_hat_far\`；本函式**未傳**該參數`

**式二**（`_pool_strips_for_block` 內切池帶）：

```
9068:        g, _ = _block_strip(block_poly, d_hat, bp, b - a, allocation_dir=allocation_dir)
```
⇒ ⛔ **未傳 `n_hat_far`**。

**式三**（`buf` 之消費點）：見 §三「錨瑕疵之逐項更正」——**結論不變、量詞更正**。

🔒 **一句結論（復現後·母體已擴為全部 31 行）**：
> **`buf` 之全部消費點<u>無一</u>改變帶之【界線方向】**；其中 **12 個幾何消費點全部改變【起點位置】**、
> 另 **7 個非幾何消費點**（判 `>0`／UI 文案／入 dict）**不觸幾何**。

### 現查 C：近側界方向之表達力 ⇒ ✅ **復現成立·出艙碼 `不可`**

`_block_strip` docstring 逐字（`app.py`·於 `6e88094`）：

```
8419:    切割帶兩側方向 n_hat = rot90(allocation_dir)。allocation_dir 應為
8425:    `n_hat_far`：**遠側**切邊之方向。`None` ⇒ **沿用近側 `n_hat`** ＝ **現行行為·逐位不變**。
8426:    近側方向仍**只**由既有 `allocation_dir` 決定 ——⛔ **未新增近側參數**
8430:    造帶，兩條切邊**共用同一個 `n̂`** ⇒ **僅能表達平行四邊形**。
8434:    **幾何（雙線分支）**：近側線過 `bp`、方向 `n̂`；遠側線過 `bp + S·d̂`、方向 `n̂_far`。
```

**逐檔正面列舉 ＋ 判別力自檢**（⛔ 禁「全倉 grep 再排除」·指令原文）：

```
$ for f in app.py verify/stepg_pipeline.py verify/run_verification.py verify/run_all.py verify/selection_pipeline.py; do
    n=$(git show "6e88094:$f" | grep -c "n_hat_far")
    b=$(git show "6e88094:$f" | grep -c "_block_strip")
    printf '%-34s n_hat_far=%-4s _block_strip=%s\n' "$f" "$n" "$b"
  done
app.py                             n_hat_far=13   _block_strip=40
verify/stepg_pipeline.py           n_hat_far=0    _block_strip=1
verify/run_verification.py         n_hat_far=0    _block_strip=0
verify/run_all.py                  n_hat_far=0    _block_strip=0
verify/selection_pipeline.py       n_hat_far=0    _block_strip=0
```

⇒ 與 `W-G.9-32` 所載**一字不差**（13／40、0／1、0／0、0／0、0／0）。
🩸 **`S-2` 觸發 3 格**（後三檔對照組回 0）⇒ 依「先疑量測器」該三格量測**作廢**、具名為無鑑別力
（該三檔不提 `_block_strip` ⇒ 其 `n_hat_far` 為 0 屬必然）；本節結論⛔ 未依賴該三格。

**出艙碼 ＝ `不可`**（附禁止之逐字 `app.py:8426`）——`allocation_dir` 為**整條帶共用**之單一方向
（`:8419` `n_hat = rot90(allocation_dir)`）⇒ ⛔ 無法只令第 2 宗之近側界改向。
`n_hat_far` 之唯一雙線生產呼叫為 `app.py:9720`（`allocation_dir=_near_ad, n_hat_far=_far_nhat`）。

### `Q1`〜`Q3` 逐項判（施工單 §3）

| # | 預測 | 判 | 依據 |
|---|---|---|---|
| **Q1** | 現查 A 之枚舉容器 ＝ 業主宗集合 | ✅ **成立** | probe `:358-359`／`:406`／`:535` ← `app.py:20021` `allocated_polys` |
| **Q2** | 現查 B 三式之結論 ＝「位置」 | ✅ **成立** | 12 幾何消費點全為位置；式一／式二皆未傳 `n_hat_far` |
| **Q3** | 現查 C 出艙碼 ＝「不可」 | ✅ **成立** | `app.py:8426` 之禁止逐字 |

⇒ **`W-G.9-32` 之三項結論全部經獨立復現坐實**。

---

## §三 錨瑕疵之逐項更正

### 更正 ①｜`②-宗 圍堵閘` 之行號錨**指錯落點**

`W-G.9-32` 報告與 `VR-013` 皆作「`②-宗 圍堵閘`（**`:9062`**）」。實測 `app.py`（於 `6e88094`）：

| 行 | 逐字 | 是否該閘 |
|---|---|---|
| `:9047` | （空行） | ❌ |
| `:9050` | `cur = s_min` | ❌ |
| `:9062` | `pool_iv = [(a, b) for a, b in pool_iv if (b - a) > _S_EPS]` | ❌ |
| 🔒 **`:9144`** | `# ②-宗 圍堵閘：宗-宗重疊 ≤ (宗數−1)×0.005×分配深度` | ✅ **真落點** |
| 🔒 **`:9155`** | `f"🔴 ②-宗 圍堵閘破[{_label}]：宗-宗重疊 = {biz_x:.4f} > 上界 {bound:.4f}"` | ✅ **真落點** |

**成因**：`:9047`／`:9050`／`:9062` 三者係 **probe 檔自帶之過期註解標籤**
（見 §二 probe `:356`／`:363` 之 `detail.append((":9050", …))`／`((":9062", …))`），
被當成生產碼之**現行**落點引用。而 `W-G.9-32` 報告開頭立有
「⚠️ 行號綁 commit：本報告所載之一切行號，皆成立於 `6e88094`」**⇒ 該聲明就此三者未兌現**。

⛔ **不推翻結論**：現查 A 之溯源鏈**未經由該三個行號**，其依據為 probe `:358-359` 之枚舉式，已逐字復現。

### 更正 ②｜「八個消費點」之量詞與其母體不對齊

`W-G.9-32` 報告 §現查 B 式三之表題為「`buf` 之**全部**消費點（兩路徑×兩側·⛔ 正面列舉）」，
其結論句作「`buf` 之**八個**消費點<u>全部</u>改變帶之【起點位置】」。**裸列舉實得**（⛔ 無後續管線）：

```
$ grep -n "_left_buffer_S\|_right_buffer_S" app.py          # 於 6e88094
$ grep -n "_left_buffer_S\|_right_buffer_S" verify/stepg_pipeline.py
```

| 檔 | 總行 | 賦值 | **幾何消費** | **非幾何消費** |
|---|---:|---:|---:|---:|
| `app.py` | **21** | 6（`:19501`/`:19502`/`:19511`/`:19517`/`:19522`/`:19528`） | **8**（`:19624`/`:19625`/`:19627`/`:19628`/`:19656`/`:19669`/`:19902`/`:19904`） | **7**（`:19530`/`:19532`/`:19533`/`:19534`/`:19535`/`:19543`/`:19544`） |
| `verify/stepg_pipeline.py` | **10** | 6（`:515`/`:516`/`:524`/`:530`/`:535`/`:541`） | **4**（`:611`/`:612`/`:829`/`:830`） | **0** |
| **合計** | **31** | 12 | **12** | **7** |

**三者不對齊**：結論句稱 **8**（＝`app.py` 側幾何消費點之數）／報告自身表列 **12**（兩路徑×兩側）／
裸列舉母體 **31**（另有 7 行非幾何消費未列）。⇒ 表題之「**全部**」實為「全部**幾何**消費點」。

🔒 **正確表述**：
> **12 個幾何消費點全部改變【起點位置】；另 7 個非幾何消費點**（`:19530`/`:19532`/`:19534` 判 `>0`、
> `:19533`/`:19535` 入 `_msg_buf` UI 文案、`:19543`/`:19544` 入 dict）**亦不觸幾何**。

⛔ **不推翻結論**：該 7 行經逐行檢視，**無一**將 `buf` 餵入任何方向參數
（`allocation_dir`／`n_hat_far`／`n̂`）⇒ 「**⛔ 無一改變界線方向**」**仍成立，且母體擴為 31 行後更強**。

### 🔑 同型第三犯

| # | 實例 | 名目 | 實際 |
|---|---|---:|---:|
| 1 | `f3L_setback_default`（管線吃掉母體） | 4 | **9** |
| 2 | 期望 FAIL 名單（名目 vs `wc -l`·`GB-41` 批） | 46 | **57** |
| 3 | **本次**（量詞 vs 表列 vs 裸列舉母體） | 8 | **12** ／ **31** |

⇒ **「N 個」一出現，就要問 N 是哪一個母體的 N**。已請入失敗考古之戒律庫。

---

## §四 施工單逐條合規對照（對 `0e44058`）

| 條 | 要求 | 判 | 證 |
|---|---|---|---|
| §0 | 開場三式 | ⚠️ **無從查**（上批是否跑過不留倉內痕跡） | — |
| §0 | 必讀六份 | ✅ | 六份皆存在於 `6e88094` |
| §1 | 零生產碼 | ✅ | 4 檔皆 `docs/`·0 刪除 |
| §2-A | 兩式行號 ＋ 容器名 | ⚠️ **容器名對·行號錨錯** | 見 §三 更正 ① |
| §2-B | 三式逐字錨 ＋ 二選一結論 | ⚠️ **逐字全對·量詞不對齊** | 見 §三 更正 ② |
| §2-C | 逐檔正面列舉 ＋ 判別力自檢 | ✅ **一字不差復現** | §二 現查 C |
| §3 | `Q1`／`Q2`／`Q3` | ✅ 三項全成立 | §二 |
| §4 | `S-1`／`S-2`／`S-3` | ✅ `S-1`/`S-3` 未觸發·`S-2` 觸發 3 格已具名處置 | §一·§二 |
| §5① | 報告 | ✅ | `W-G.9-32_受詞校正現查.md`（148 行） |
| §5② | `VR-<下一未占用>` | ✅ **編號正確** | 施工前最大 `VR-012`（12 則）·新增 `VR-013` 於 `:267` |
| §5③ | `GB-<下一未占用>`（`Q3`＝不可 ⇒ 該辦） | ✅ **編號正確** | 施工前最大 **63**·`^\| \*\*GB-64\*\*` 實體命中 **0**·表 62→63 筆 |
| §5④ | 設計文件就地加註·⛔ 原句不刪 | ✅ | `+20 / −0` |
| §5⑤ | **交接文檔末加註** | 🛑 **未辦** | blob `1ce08e7…` 於 `6e88094`／`0e44058` **逐位相同**·異動集命中 **0** |
| §6 | 編號現查式 | ✅ 兩式自跑覆核成立 | 上二列 |
| §7 | push 後才回報 | ✅ | `origin/wip/s1-endpart` ＝ `0e44058` |

🛑 **`§5⑤` 未辦之性質**：`W-G.9-32` 報告自書標題為「**入倉四項**」、其 §未涵蓋之事**亦未列此項**
⇒ **⛔ 非「辦了漏記」，係整項漏辦且漏辦未被自檢察覺**。⇒ **該批不應計為完成**。

---

## §五 入倉四項

| # | 落點 | 前 → 後 | 性質 |
|---|---|---|---|
| ① | `docs/reports/W-G.9波_新session交接文_WG913-WG931.md` **檔末加註** | 157 → — | **補辦 `W-G.9-32` 之 `§5⑤`**·⛔ 原句一字未刪·⛔ 未改寫 `§10`·⛔ 未用行號錨 |
| ② | `docs/驗證裁定登記表.md` 之 **`VR-013` 就地加註** | 275 → — | 錨更正 ①②·⛔ 上表原句一字未刪·⛔ 未新增 VR 號（KL 裁「就地加註」） |
| ③ | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 309 → — | `GB-64` 列末**追加**指路 ＋ 檔末新增「四、錨更正加註」·⛔ 未刪任何字·⛔ 未改號 |
| ④ | 本報告（新建） | — | — |

⚠️ **`§5⑤` 之 `§11` 相容性**：交接文 `§11` 自證「⛔ 無行號錨」，其母體已自訂為
「扣除 `§11` 起至檔末」（`sed '/^## §11 /,$d'`）⇒ 本加註落在**扣除區內**；
且本加註**⛔ 未用任何行號錨** ⇒ **無論母體如何界定皆不污染該自證**。

---

## §六 ⛔ 本批未涵蓋之事

1. ⛔ **未重定設計 `§3`／`§5`**——本批僅查核與更正錨，重定屬另案（`VR-013` 已載其後果）。
2. ⛔ **未提出**「新增近側參數」或「另闢構造」之方案，⛔ 未估其**土地後果**——`GB-64` 仍**待 KL 域裁**。
3. ⛔ **未跑任何探針、未跑 `run_all`**——本批全為靜態讀取；⛔ 未實測「若真傳了 `n_hat_far` 會如何」。
4. ⛔ **未查** `_block_strip` 之**雙線分支本身**是否正確（其唯一生產呼叫端 `app.py:9720` 之行為未驗）。
5. ⛔ **未查** `W-G.9-32` 是否實際跑過施工單 `§0` 之開場三式——倉內不留痕，**無從查**、⛔ 不臆測。
6. ⛔ **未動** `verify/baselines/`、⛔ 未動任何凍存物與名單、⛔ 未動 `app.py`／`verify/` 下任何檔。
7. ⛔ **未推翻** `K-9-5-4` 釋示補之授權、⛔ 未推翻 `W-G.9-29` 之楔形歸因、
   ⛔ 未推翻 `W-G.9-32` 之三項結論——本批**坐實**之，僅更正其兩個錨。
8. ⛔ **未 pull 主目錄**——主目錄 `wip/s1-endpart` 停在 `17e3290`，經查**僅落後 41 commits、⛔ 非分岔**
   （`17e3290` 為 `0e44058` 之祖先）；是否 pull 屬 KL 之工作副本，本批不代為處置。
