# W-G.9-219R2R　退縮值常設顯示（執行報告·🛑 **候 KL 逐字放行**）

**批次性質** ＝ **含生產碼**（`app.py` `37` 增／`1` 刪）。**基座** ＝ `2fa1590da7ef680c616d85f8b1641fc5d106d20a`。
🛑 **本批⛔ 推入主線**——依單「完成【驗收】所列後停，候 KL 逐字放行方得推入主線」。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
payload 一律 `Write` 落檔、⛔ heredoc（`GB-143` 補款二）。
**前置**：`W-G.9-226` 已落地於 `2fa1590`（`G2` 當場重驗相符·見下）✅。

---

## 工項零　✅ PASS（二項）

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **6188** | 94 | **0** | `df842e0a52f5fb988a059a88572a16e5342ad07de6d8d27cc42e579e382a16ea` |
| 入倉（索引側 blob） | **6188** | 94 | **0** | `df842e0a52f5fb988a059a88572a16e5342ad07de6d8d27cc42e579e382a16ea` |

🔒 **逐位相同 ＝ `True`**（差 `+0`·`戒 40` 二端並報）。對照組：翻首位元組 ⧧ blob ＝ `True`；blob 自比 ＝ `True`。
🔒 **併：舊單 `-219R` 首行作廢標記**——基座 **6294** B → 入倉 **6412** B（Δ ＝ **`+118`** ＝ 標記本身 **118** B）；
**基座全文為嚴格後綴 ＝ `True`** ⇒ ⛔ 改其正文一位元組。
🛑 射程界：只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。

---

## 前置閘

### `G1`　態錨 ✅

`HEAD` ＝ `2fa1590da7ef680c616d85f8b1641fc5d106d20a`；`left-right` ＝ **`0 0`**；追蹤 **1620** 檔／`.md` **551** 檔。
生產碼五檔之**改前** `sha256` 見下「閘 `P`」表之左半。

### `G2`　✅ PASS（相符·當場重驗）

三軸：來源 `blob@2fa1590`／母體 `app.py` **1414673** B・**23906** 列／**列框**。解析器逐字沿用 `-219`。
現態 ＝ **12** ＝ 補登後 `GB-144` ＝ **12**；差集雙向皆 `[]`。
🔒 下游入口（讀取 ∪ 無兜底消費端·當場判）＝ `[12677, 14259, 23810]`。
🔒 對照組：`f3L_setback_default` 命中 **9**（> 0）／一必不存在之同形鍵 **0**。

### `G3`　✅ PASS（三項當場復現）

| 項 | 復現 |
|---|---|
| `a` | `app.py:19671` ＝ `with st.expander("⚙️ 街角地參數（共用設定 + 各街廓微調）", expanded=False):`；含 `expanded=False` ＝ `True`（該 expander **全檔命中 1**）|
| `b` | 其上最近之 `st.markdown("---")` ＝ `app.py:19658`（**ind 16**）；`:19671` **ind 16** ⇒ **相同** |
| `c` | `if not build_parcels:` ＝ `app.py:19650`（ind 12）；其後最近之 `else:` ＝ `app.py:19652`（ind 12）⇒ **成對** |

🔒 插入點 ＝ `:19670`（`""")`·`st.markdown("""…""")` 之收尾）之後、`:19671` 之前 ⇒ **敘述邊界**，敘述層 **16**。

### `G4'`　✅ **PASS——受詞成立（實測·⛔ 推理）**

**證據形** ＝ `verify/probes/probe_WG9219R2_widget_lifecycle.py`（`streamlit.testing.v1.AppTest`·`streamlit` **1.56.0**）。
最小重現與 `app.py` 步驟 L **同形**（`if not <flag>: … else: st.number_input(key='f3L_setback_default')`），
並於同一 script 內置一**非 widget** 之 session 鍵作對照。

| 步 | widget 渲染 | widget 鍵在 | 值 | **非 widget 鍵在（對照）** |
|---|---|---|---|---|
| ① 首跑（分支渲染） | True | True | `3.5` | True |
| ② 使用者輸入 `2` | True | True | `2.0` | True |
| ③ 分支**未渲染**（第 1 輪） | False | 🔴 **False** | `ABSENT` | **True** |
| ④ 分支**未渲染**（第 2 輪） | False | 🔴 **False** | `ABSENT` | **True** |
| ⑤ 分支**回復渲染** | True | True | 🔴 **`3.5`**（使用者之 `2.0` 已失） | True |

🔒 **判別力自證（兩造）**：widget 鍵**消失**、非 widget 鍵**存活** ⇒ 該機制**係 widget 專屬**，
**⛔ 「整個 `session_state` 被清」**；二者⛔ 同向 ⇒ 探針**有**判別力。
🔒 輸入 `3.5`（＝ 預設）之同一序列亦同（`③` 消失／`⑤` 回來）。
🔒 ⇒ **乙案之前提坐實**：持久旗標必活得比值久 ⇒ 錯報；「鍵在 ∧ 旗標」則自我修復。

---

## 改動摘要（何處、幾行、為何）

| # | 位置（改後行號） | 增／刪 | 內容 | 為何 |
|---|---|---|---|---|
| `P1` | `app.py:19671`–`:19694` | **+24／0** | **常設顯示**區塊（含射程之載明註解）| 工項二 `a`–`d`：甲案·置於 `st.expander` **之外·其上方**；**先調和、後顯示**；key 不在即丟棄過期旗標 |
| `P2` | `app.py:19708`–`:19719` | **+12／0** | `_f3L_setback_changed()`——退縮欄**專屬** `on_change`，先設旗標再委派 `_f3L_invalidate_g_cache()` | 工項一 `a`／`b`。🔴 **⛔ 逕於 `_f3L_invalidate_g_cache` 內設旗標**：該函式**亦被逐街廓深度覆寫欄共用**（`f3L_depth_ov_*` 之 `on_change`·改後 `:19767`），於彼處設旗標將使「**改深度**」被誤判為「**動過退縮**」（**偽陽**）|
| `P3` | `app.py:19726` | **+1／−1** | `on_change=_f3L_invalidate_g_cache,` → `on_change=_f3L_setback_changed,` | 將退縮欄改綁 `P2` 之專屬 callback；**⛔ 改被委派者一字**，失效行為逐字不變 |

🔒 **hunk 標頭（機驗）**：`@@ -19670,0 +19671,24 @@` ／ `@@ -19683,0 +19708,12 @@` ／ `@@ -19690 +19726 @@`
⇒ `24 + 12 + 1 = 37` 增、`1` 刪，與 `numstat` 相符。
🔒 `py_compile` 之 `rc` ＝ **0**。
🔒 **落地器之陷阱（自捕自正）**：首版以 `str.count` 取 `on_change=` 之 24 空格形，
得 **2**（`:19731` 之 **32** 空格列**內含**該 24 空格子字串）⇒ `assert` 攔下；
改為**列錨**（前綴換行）後得 **1**。對照組（去列錨）＝ **2**，證列錨有效。

### 🔴 工項一 `e`：**⛔ 於本批為之**，改列通用化波（具名回報）

受詞 ＝ `3.5` 之三處字面（改後 `:19720`／`:22395`／`:22884`）。**⛔ 收為具名常數**，理由逐項：

1. `:19720` 之 `3.5` 係**輸入欄首輪之種子**（`value=_setback_init`）⇒ 其值即**全下游計算之輸入之源**
   （`:12677`／`:14259`／`:23846` 三入口所收者皆本於此）⇒ **在計算路徑上**。
2. `:22395`／`:22884` 之 `3.5` **各出現二次**：一為 `.get` 之預設，一為 `abs(v − 3.5) < 1e-9` 之
   **情境判別**（選 `0m`／`3.5m`）。後者係**分支條件**，選定**凍結錨情境** ⇒ **⛔ 純顯示**。
3. `GB-144` 之**失效條件**逐字為「該參數改為自 `case_params`／都市計畫書**資料驅動**」
   ⇒ 正解係**資料驅動**、⛔ 字面改名；今改名將於通用化波**再被拆除**（對凍結檔之無謂 churn）。

🔒 **本批新增之第四處 `3.5`**（`app.py:19686` 之 `_SB_DEFAULT_M = 3.5`）：係工項二 `d`
（「首次渲染時 key 尚不存在，顯示須為 `3.5`」）之**必要**；已**具名**且**僅供顯示端**，
**⛔ 參與任何計算**。其一併列入通用化波之集中化清單。

---

## 工項三 `a`　✅ PASS（**七**情境·不符項 `0`）

**證據形** ＝ `verify/probes/probe_WG9219R2_source_display.py`。
🔒 **⛔ 鏡寫**——受測之三段碼**逐字自 `app.py` 抽出後執行**：
常設顯示區塊（`app.py:19671`–`:19694`）／`on_change` 區塊（`:19708`–`:19719`）／
下游 `_need`（`:14176`–`:14179`）與 `_wf_tag_of`（`:14271`–`:14277`）之**定義原文**。
🔒 **射程界（⛔ 誇稱）**：所量者係**三入口之讀取式**在該輪 session 上之實收；
**⛔ 為 app 之整輪實跑**（後者需 DXF／Step-G 全狀態，headless ⛔ 可得）。

| 情境 | 顯示之值 | 顯示之來源 | `E1` `:12677` 主路徑 | `E2` `:14259` 引擎接線 | `E3` `:23846` tag | 旗標 |
|---|---|---|---|---|---|---|
| ① 首次渲染（key 未存在） | `3.5` | **預設** | `3.5` | `3.5` | `3.5m` | （無）|
| ② 輸入 `0` | `0` | **使用者輸入** | `0.0` | `0.0` | `0m` | `True` |
| ③ 輸入 `3.5`（＝ 預設值） | `3.5` | **預設** | `3.5` | `3.5` | `3.5m` | （無）|
| ④ 輸入 `2` | `2` | **使用者輸入** | `2.0` | `2.0` | 🔴 loud raise | `True` |
| ⑤ 輸入 `2` → 未渲染 → 再回 | `3.5` | **預設** | `3.5` | `3.5` | `3.5m` | （無）|
| ⑥ 輸入 `3.5` → 未渲染 → 再回 | `3.5` | **預設** | `3.5` | `3.5` | `3.5m` | （無）|
| ⑦ **併呈**：`3.6` → 再改回 `3.5` | `3.5` | **使用者輸入** | `3.5` | `3.5` | `3.5m` | `True` |

🔒 **`②` 坐實工項一 `d`**：輸入 `0` 判為「**使用者輸入 `0` m**」，**⛔ 因 falsy 落回預設**。
🔒 **`⑥` 係乙案勝出之見證**：輸入值恰等預設，經未渲染往返後來源仍判「**預設**」
——**丙案（比對值相等）於此必誤報「使用者輸入」**。
🔒 **`④` 之 `E3` loud raise 係正解**——`_wf_tag_of` 逐字令「非 `UC9898` 雙情境（`0`／`3.5`）⇒ 引擎凍結錨不適用」；**⛔ 本批所生**。

### 🛑 上呈（**意思決定**·⛔ 由 CC 裁）——`③` 與 `⑦` 之語意

`③`（把 `3.5` 設為 `3.5`·**值未變**）得「**預設**」；`⑦`（`3.6` → 再改回 `3.5`·**值曾變**）得「**使用者輸入**」。
⇒ 🔒 **旗標所記者係「值曾被使用者<u>改變</u>」、⛔「欄曾被聚焦」**——此係 `on_change` 之**固有射程**
（Streamlit 僅於值改變時觸發），**⛔ 本實作之選擇**；且合於單之工項一 `b` 之 ⟺ 定義逐字。
🔒 二者**顯示之值皆 `3.5`（正確）**，相異者僅**來源標籤**；`③` 之情形下，在用之值**與預設不可分辨**。
🛑 **是否要求 `③` 亦顯示「使用者輸入」，屬 KL 之意思決定**——本批⛔ 自裁。

🔧 **本探針之二處自捕自正**（出艙前攔下）：
① `AppTest.session_state` 係 `SafeSessionState` 代理，**⛔ `.get()`／⛔ `dict()`** ⇒ 首版 `AttributeError`；
② `③` 之**期望值**首版誤填「使用者輸入」——係**探針作者之臆測**、⛔ 單之逐字；
依工項一 `a`＋`b` 之 ⟺ 逐字重導後為「**預設**」（`fixture-provenance`：期望⛔ 由輸出回填）。

---

## 工項三 `b`　✅ PASS（迴歸·**逐位相同**）

**證據形** ＝ `verify/probes/probe_WG9219R2_regression_R4.py`，於**拋棄式 clone**（`C:\w2r2`·自 `2fa1590` clone）
跑二次：改前 ＝ clone 之 `HEAD` `app.py`；改後 ＝ 覆蓋本批之 `app.py`。
鏈：`harvest()` → `rv.build_pipeline` → `build_ownership` → `build_build_parcels` →
`rv.build_param_table` → `run_corner_pk` → `run_step_g(setback)`；取 `g_rows` 之 `R4` 逐列。

| 量 | 改前 | 改後 | 判 |
|---|---|---|---|
| 結果物正規化序列化之 `sha256` | `2e0f54bd9578fd3091698fb6b97c89006bb4182c31dd261e411a6bd12fb7413b` | **同左** | ✅ |
| 其位元組 | **1380** | **1380** | ✅ |
| 全 `stdout`（去 `app.py`-sha 一列）`sha256` | `d32cb4c213ee16bd92b6c089cdf10e9d07874c3bce1dba7bc96ce0e1effeda85`（**5278** B）| **同左** | ✅ |
| `stderr` | **0** B | **0** B | ✅ |
| 🔒 **對照組（器須紅）** `app.py` 自身之 `sha256` | `0dd6b2575c598812fd2edb435a386feab20da763024d5d94fe7759f6a900154e` | `091efa9b77c288082edea9cce809c0bb53c8b927f7cd0ea2e646a962c6409628` | 🔴 **不同** |

🔒 **對照組之意義**：二輪之 `app.py` **確為不同物**（`sha256` 相異·`BEFORE.txt` vs `AFTER.txt` 亦不同），
而**結果逐位相同** ⇒ 「相同」**⛔ 因二輪跑了同一份碼**。

### 🔴 併呈：**改前之現態於 `run_step_g` 即 loud raise**（**⛔ 本批所生**）

「改前值自現態當場實算」之實得 ＝ **二情境皆 `RuntimeError`**（**⛔ R4 之逐列**）：

| 退縮 | 拋出處 | 訊息（截） |
|---|---|---|
| `3.5` | `stepg_pipeline.py:1261:run_step_g` | 🔴 結構閘 telescoping 破：街廓 `R2` left 側 `ΣRw_實跑=100.02` ≠ `R(W_final=34.39)−R(W₀=6.98)=44.41`（Δ=55.606 > 0.1）|
| `0` | `stepg_pipeline.py:1003:run_step_g` | 🔴 `②-宗 圍堵閘破[R2]`：宗-宗重疊 ＝ `45.9766` > 上界 `2.8906` |

🔒 **二者皆為<u>改前</u>即存在之紅**（`0m` 者即 `GB-67` 之受詞），**與本批無涉**；
本探針**照實序列化其失敗形（型別＋訊息＋拋出處）**、⛔ 吞 ⇒ **改前／改後之失敗形亦逐位相同**。
🛑 **⇒ 「`R4` 之逐列」於現態<u>不可得</u>**——`run_step_g` 於 `R2` 即停，未及 `R4`。
本批之迴歸因此係「**同一鏈之行為逐位相同**」，**⛔ 「R4 逐列相同」**——此射程差**照實載明**。
🔒 **結構性佐證**：本批改動全在 `main()` 之 Streamlit UI 區塊內，而 `harvest` **從不呼叫 `main()`**
（`CLAUDE.md` §7 逐字）⇒ 期望本即逐位相同，實測相符。

---

## 工項三 `c`　`numstat` 逐檔逐行 ＋ `deletions` 逐行具名

| 檔 | 增 | **刪** |
|---|---|---|
| `app.py` | **37** | 🔴 **1** |
| `docs/orders/W-G.9-219R2_退縮值常設顯示.md` | 94 | **0** |
| `docs/orders/W-G.9-219R_退縮值常設顯示.md` | 2 | **0** |
| `verify/probes/probe_WG9219R2_regression_R4.py` | 91 | **0** |
| `verify/probes/probe_WG9219R2_source_display.py` | 247 | **0** |
| `verify/probes/probe_WG9219R2_widget_lifecycle.py` | 110 | **0** |

**`deletions` 之逐行具名（`VR-069` 補款二之體例）**

| 檔·列 | 舊逐字 | 取代式 | 所修之受詞 |
|---|---|---|---|
| `app.py`（舊 `:19690`）| `                        on_change=_f3L_invalidate_g_cache,` | `                        on_change=_f3L_setback_changed,` | 退縮欄需**專屬** callback 方能設來源旗標；沿用共用者將使**逐街廓深度覆寫**（同函式之另一綁定）亦設旗標 ⇒ **偽陽**。新 callback **首行即委派** `_f3L_invalidate_g_cache()`，故**既有失效行為逐字不變** |

🔒 **⛔ 及 `verify/tools/`**；`app.py` 屬**生產碼五檔** ⇒ 依 `VR-069` 補款，其 `deletions` 本應為 `0`。
🛑 **本列係例外，其正當性繫於 KL 之放行**——⛔ 由 CC 自行認定合格。

---

## 閘 `P`（生產碼五檔·改前／改後逐檔 `sha256`）

| 檔 | 改前 B | 改前 `sha256` | 改後 B | 改後 `sha256` | 判 |
|---|---|---|---|---|---|
| `app.py` | 1414673 | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` | **1417936** | **`091efa9b77c288082edea9cce809c0bb53c8b927f7cd0ea2e646a962c6409628`** | 🔴 **已動（本單所令）** |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` | 35367 | 同左 | ✅ ⛔ 動 |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` | 96923 | 同左 | ✅ ⛔ 動 |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` | 96261 | 同左 | ✅ ⛔ 動 |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` | 24681 | 同左 | ✅ ⛔ 動 |

🔒 `left-right`（推前·對 `origin/wip/s1-endpart`）＝ **`0 0`**。

---

## 🔴 放行後之**必辦項**（阻塞下一批之 `G2`·⛔ 於本批落地）

本批之插入使 `app.py` **+36 列** ⇒ `GB-144` 所登之**五處檔列全數位移 `+36`**，另**新增一處**：

| `GB-144` 現登 | 本批後之實際 | 差 |
|---|---|---|
| `19684` | `19720` | +36 |
| `19689` | `19725` | +36 |
| `22359` | `22395` | +36 |
| `22848` | `22884` | +36 |
| `23810` | `23846` | +36 |
| —— | 🆕 **`19685`** ＝ `_SB_KEY = 'f3L_setback_default'` | 新增 |

⇒ **改後之命中集 ＝ 13**：`[10947, 12319, 12675, 12677, 12680, 12703, 14259, 19685, 19720, 19725, 22395, 22884, 23846]`。
🛑 **本批⛔ 落 `GB-144` 之補款**——理由：**行號於 KL 放行前非終局**（若 KL 令改碼即再位移），
而 `GB-144` 係 **append-only 正典檔**、補款**不可撤**。⇒ 其補款應與**放行批**同落。
🔒 **併記**：主線之 `app.py` 未動 ⇒ **主線之 `G2` 仍綠**；本位移只存於本側分支。
🔒 新增之旗標鍵 `f3L_setback_user_set` 之四處 ＝ `app.py:19679`（註）／`:19688`（丟棄）／
`:19692`（判來源）／`:19716`（設旗標）——**⛔ 落入 `G2` 之三群述詞**（其字樣⛔ 含 `f3L_setback_default`）。

---

## 🛑 停：候 KL 逐字放行

依單【回報後之處置】：**完成【驗收】所列後停**。本批**⛔ 推入主線**；
其 commit 已推至**側分支** `verify/W-G.9-219R2-c1`（⛔ `wip/s1-endpart`）供逐位復驗。
🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**（新窗入口之既載）。

**候裁二項**
1. 🛑 `③` 之語意（把 `3.5` 設為 `3.5`·值未變 ⇒ 顯示「預設」）——**KL 之意思決定**。
2. 🔴 `GB-144` 之補款（行號 `+36` ＋ 新增 `:19685`）——**與放行批同落**。
