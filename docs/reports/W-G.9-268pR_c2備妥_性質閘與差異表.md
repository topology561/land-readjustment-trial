# `W-G.9-268′` `c2` 備妥報告：性質閘 ＋ 逐宗差異表（🔴 **有土地後果**·**待 KL 放行**）

> 🛑 **本批⛔ 併線**。`c2` ＝ 🔴 生產碼·幾何變動·**有土地後果**，依施工單 `§五`／補令六 `§五`：
> **備妥（⛔ 推主分支）→ 側分支 `verify/W-G.9-268p-c2` → 發單側逐位元組復驗 → 呈 KL → KL 放行 → 方得併線。**
> 🔒 **主分支 `wip/s1-endpart` 於本報告出艙時<u>原地不動</u>**（其 tip ＝ `683b7fa`·零生產碼三批）。

---

## §零　三軸之總宣告（`VR-091`）

> `(1)` **來源 `commit`**：主分支 ＝ `683b7fa`；**`c2` 物件 ＝ `c1376afa2bc3ad0df3a34c412a9c06e08e188a5a`**。
> `(2)` **母體**：逐節具名。
> `(3)` **粒度框**：逐節具名（列框／面積 ㎡／長度 m／名目）。

---

## §一　`c2` 之受詞（**最小改動**·單檔單列單字元）

```
app.py:9269
-    if _os_a.environ.get('WG9268P_ANCHOR_GEOM', '0') != '1':
+    if _os_a.environ.get('WG9268P_ANCHOR_GEOM', '1') != '1':
```

| 項 | 值 |
|---|---|
| `numstat` | **`1` 增 `1` 刪**（單檔 `app.py`）|
| `py_compile` | OK |
| 旗標之存廢 | **仍在**（`c3` 方移除）⇒ 設 `WG9268P_ANCHOR_GEOM=0` 仍可關閉 |
| 判別力自檢 | 寬字樣 `WG9268P_ANCHOR_GEOM` 命中 **`2`**（docstring ＋ 碼）⇒ 「命中 `1`」**非恆真** |

🛑 **⛔ 動 docstring `app.py:9248` 之「預設 `off`」一語** —— 施工單 `§二` 明以 **`c3`**（`contract` 段）
承載「移除旗標；更新受該錨之註解」⇒ 動之即**逾核可射程**（`裁 三` 之先例：CC ⛔ 動該段之判斷正確）。
🔴 **⇒ 該語自 `c2` 起<u>過期</u>**（碼之預設已為 `on` 而註解仍載 `off`）——**具名為 `c3` 之受詞**，⛔ 靜默留置。

---

## §二　性質閘（施工單 `§三`）

**器** ＝ `verify/probes/probe_WG9268p_c2_gates.py`（新建·`argv[1]` 給定倉根·⛔ 硬編）。
`rc = 0`／`stderr` **`0`** B。**落檔** ＝ `verify/out/WG9268pR_c2_gates.log`。

### 二-0　母體基數（坑 `1`·⛔ 靜默綠）＋ **鍵之唯一性**（坑 `4`）

| 情境 | 態 | `g_rows` | 中止 | 分配宗 | 相異鍵 | 判 |
|---|---|---|---|---|---|---|
| `0m` | `off` | `21` | `R2` | `19` | `19` | ✅ |
| `0m` | `on` | `59` | 無 | `46` | `46` | ✅ |
| `0m` | **`dflt`** | `59` | 無 | `46` | `46` | ✅ |
| `3.5m` | `off` | `47` | `R5` | `36` | `36` | ✅ |
| `3.5m` | `on` | `60` | 無 | `46` | `46` | ✅ |
| `3.5m` | **`dflt`** | `60` | 無 | `46` | `46` | ✅ |

🔒 **鍵 ＝ `(情境, 暫編地號)`**（⛔ 幾何鍵·坑 `4`）；**相異鍵數 ＝ 宗數**逐態成立 ⇒ ⛔ 碰撞。

### 二-1　🔑 **`c2` 預設生效檢**（本批新增·⛔ 施工單所列，惟⛔ 之則 `c2` 本身未被驗）

`dflt` ＝ **環境變數不設**（⇒ 取碼內預設）。

| 情境 | `dflt` ＝ `on`？ | `dflt` ≠ `off`？ |
|---|---|---|
| `0m` | **是** ✅ | **是** ✅ |
| `3.5m` | **是** ✅ | **是** ✅ |

🔑 **判別力**：上二問若同為「是」則自相矛盾 ⇒ 本檢**⛔ 恆綠**；且 `off` 與 `on` 於本批確有實質相異（見 `§三`）。
🩸 **本檢之由**：三探針原以 `os.environ.pop(FLAG)` 取 `off`，而 `c2` 後 `pop` 所得者為 **`on`**
⇒ 三器之「`off` 態」**靜默變成 `on` 態**且⛔ 報錯（**自毀式**·CC 自捕）。
已於主分支 `683b7fa` 先行改為**顯式 `'0'`**，並以 **patch 前後輸出逐位相同**證其於 `c2` 前語意不變。

### 二-2　`P-8`　harness 可達性**只增不減**

| 情境 | `off` 可達 | `on` 可達 | **轉不可達** | 判 |
|---|---|---|---|---|
| `0m` | `R1, R2` | `R1, R2, R3, R4, R5, R6` | **無** | ✅ |
| `3.5m` | `R1, R2, R4, R5, R6` | `R1, R2, R3, R4, R5, R6` | **無** | ✅ |

⇒ 🔒 **本 `commit` 使管線通過原中止點**（`0m·R2`／`3.5m·R5`），六街廓**全部可達**——即施工單 `§二 c2` 所預期者。

### 二-3　🔴 `P-5`／`P-6`　街角地**本身**與其**歸屬**（本批之**核心不變量**）

🔑 **`P-5` 之比較端 ＝ `cut_coords` <u>全多邊形</u>逐位**（⛔ 自定義「遠側境界線」）
——⛔ 另寫第二份幾何定義（`GB-48` 族）；全多邊形逐位相同 ⇒ **遠側境界線 a fortiori 相同**。
🔒 **該法只會更嚴、⛔ 更寬。**

**判定集基數 ＝ `8`**（共有街角側）。**逐側全綠**：

| 情境 | 街角側 | 歸屬（前 → 後）| `G` | 街角範圍面積 | 全多邊形 |
|---|---|---|---|---|---|
| `0m` | `R1/left` | `628-37(1)` → 同 | 同 | 同 | 同 |
| `0m` | `R1/right` | `628(5)` → 同 | 同 | 同 | 同 |
| `0m` | `R2/left` | `628-41(1)` → 同 | 同 | 同 | 同 |
| `3.5m` | `R1/left` | `628-36(1)` → 同 | 同 | 同 | 同 |
| `3.5m` | `R1/right` | `628(5)` → 同 | 同 | 同 | 同 |
| `3.5m` | `R4/left` | `628(1)+` → 同 | 同 | 同 | 同 |
| `3.5m` | `R5/left` | `628-45(1)+` → 同 | 同 | 同 | 同 |
| `3.5m` | `R6/right` | `628-18(1)` → 同 | 同 | 同 | 同 |

🟡 **僅 `on` 有之街角側**（**新可達**·⛔ 判為歸屬變動）：
`0m` ＝ `R3/right`／`R4/left`／`R5/left`／`R6/right`；`3.5m` ＝ `R3/right`。

⇒ 🟢 **`P-5`／`P-6` 之停機款<u>未</u>觸發**——錨所移者為**其後各宗之起點**，⛔ 街角地本身。

### 二-4　🔴 `P-1`　覆蓋帳（**於 `c2` 態重得**·補令六 `§一-3` 所令）

**器** ＝ `verify/probes/probe_WG9268p_mem.py`（**已修版**·`off` 取顯式 `'0'`）。
**執行處** ＝ **拋棄式 clone**（`c2` 態·`app.py` 逐位驗明）。`rc = 0`／`stderr` **`0`** B。

🩸 **`_partial` 之取得（坑 `2`）**：`c2` 態之管線**跑完** ⇒ **⛔ 產 `_partial`**
（此即補令五 `§三-1` 之結構性缺口本身）。故先於同一 clone 內以
**`WG9268P_ANCHOR_GEOM=0`** 實跑 `run_verification`（`rc = 1`·中止 ⇒ 產檔），
得 `got_G值_退縮0m_partial.csv` **`15979`** B ／ `退縮3.5m_partial.csv` **`33670`** B。
🔒 **⛔ 比錯檔（坑 `5`）**：clone 為**全新**，該二檔係本次 `off` 實跑**新產**（⛔ 舊檔殘留）。

| 閘 | 結果 |
|---|---|
| 否證對照（幾何原語·人造） | ✅ |
| **閘一**（`off` 記憶體側 `cut_coords` ＝ `_partial` CSV·逐位）| 判定集 **`68`** 格／逐位相異 **`0`** ✅ |
| **閘二**（`off` 覆蓋帳 ＝ `S-6` 既有實測·4dp）| **`18/18`** 逐位相符 ✅ |
| 🔴 **【一】`on` 態覆蓋帳** | **母體基數 `34` 組**／**逐對總數 ＝ `0`** ✅ |

> 🟢 **`P-1` 之停機款（「`c2` 後仍有任一組之任一對交集面積 `> 0`」）<u>未</u>觸發**
> ——**全部 `34` 組之重疊對數 ⋀ 逐對面積皆為 `0`**。
> 🔒 對照：`off` 態尚有二組重疊（`0m·R2·left` `[19.8142, 26.1624]`／`3.5m·R5·left` `[56.8186]`）
> ⇒ **本檢有判別力、⛔ 恆綠**。

🔒 **與 `70844fa` 所入倉者<u>逐位相同</u>**：本次（`c2` 態）之輸出 `18901` B、
`sha256` 前 `20` ＝ `d39a16307b456cbb760c`；倉內 `verify/out/WG9268pR_mem.log` **同值**
⇒ **⛔ 另存重複檔**（其內容一位元不差）。
🔒 **其所以逐位相同**：本器**自行以顯式值切換旗標** ⇒ 其量測**⛔ 依賴倉內之預設**
——`c2` 所改者正是該預設 ⇒ 二態下本器所量者同。

### 二-5　`P-4`　守恆（**已於前置 `2` 出艙**）

判定集 **`17`**、全部殘差在其逐街廓具名容差內 ⇒ 停機款未觸發。
逐值見 `docs/reports/W-G.9-268pR_補令六_守恆必答.md` `§四-3`。

---

## §三　差異表（施工單 `§四`·**域語言**）

**逐宗全表** ＝ `verify/out/WG9268pR_c2_gates.log` 之【表一】（`0m` `46` 宗／`3.5m` `46` 宗）。
本節載**表三彙總**與**逐宗有變動者**。

### 三-1　【表三】彙總（**帶號與絕對值並列**·`CLAUDE.md` 鐵律）

| 量 | `0m` | `3.5m` |
|---|---|---|
| **街角地面積有變動之街廓** | **無**（預期 ＝ 無·`P-5`）| **無** |
| **街角地歸屬有變動之街廓** | **無**（`P-6`）| **無** |
| 位置外移之宗數 | `7` | `7` |
| 最大外移量 | **`3.39` m** | **`3.32` m** |
| 中位外移量 | `2.88` m | `3.04` m |
| 面積增減宗數 | `6` | `5` |
| **`ΣΔ`（帶號）** | **`+3.29 ㎡`** | **`+4.39 ㎡`** |
| **`Σ|Δ|`（絕對值）** | **`10.47 ㎡`** | **`6.37 ㎡`** |
| 所有權人受影響戶數 | `7` | `7` |
| 因本批而**首次可分配**之街廓 | `R3`／`R4`／`R5`／`R6`（宗 `27`）| `R3`（宗 `10`）|

🔒 **獨立復現**：`ΣΔ`／`Σ|Δ|` 與補令六 `§三-1` 之發單側實測（`+3.2900`／`10.4700`；`+4.3900`／`6.3700`）
**逐位相符** ✅；最大外移 `3.39` m 亦與補令六所載相符。
🔒 **交叉驗算**：表一逐宗 `ΔG` 之和 ＝ `−3.59+0.18+1.92+0.57+3.51+0.70 = +3.29`（`0m`）／
`−0.99+1.48+3.60+0.24+0.06 = +4.39`（`3.5m`）⇒ 與**獨立之守恆器**所得之 `ΔΣG` **逐位相同**。

### 三-2　🔴 **逐宗有變動者**（`0m`·`R2/left`）

| 配地次序 | 暫編地號 | 重劃前地號 | 所有權人 | 前 `G` | 後 `G` | **面積增減** | 前寬 | 後寬 | **寬度增減** | **位置變動** | 處置 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `1` | `628-41(1)` | `628-41` | `G023` | `175.95` | `175.95` | `+0.00` | `4.05` | `4.05` | `+0.00` | **`+3.39`** | 位置外移 |
| `2` | **`628-42(1)`** | `628-42` | **`G011`** | `3.82` | **`0.23`** | **`−3.59`** | `0.76` | **`0.18`** | **`−0.58`** | `+2.82` | 位置外移 |
| `3` | `628-27(1)` | `628-27` | `G030` | `29.08` | `29.26` | `+0.18` | `0.65` | `0.65` | `+0.00` | `+2.82` | 位置外移 |
| `4` | `628-40(1)+` | `628-40` | `G010` | `293.48` | `295.40` | `+1.92` | `6.52` | `6.56` | `+0.04` | `+2.87` | 位置外移 |
| `5` | `628-30(2)` | `628-30` | `G007` | `86.66` | `87.23` | `+0.57` | `1.93` | `1.95` | `+0.02` | `+2.88` | 位置外移 |
| `6` | `628-39(1)` | `628-39` | `G002` | `229.72` | `233.23` | `+3.51` | `5.11` | `5.20` | `+0.09` | `+2.97` | 位置外移 |
| `7` | `628-31(2)` | `628-31` | `G027` | `390.98` | `391.68` | `+0.70` | `8.72` | `8.74` | `+0.02` | `+2.99` | 位置外移 |

### 三-3　🔴 **逐宗有變動者**（`3.5m`·`R4/left` ＋ `R5/left`）

| 街廓/側 | 暫編地號 | 重劃前地號 | 所有權人 | 前 `G` | 後 `G` | **面積增減** | 前寬 | 後寬 | **寬度增減** | **位置變動** | 處置 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `R4/left` | `628(1)+` | `628` | `G009` | `2023.33` | `2023.33` | `+0.00` | `61.04` | `61.04` | `+0.00` | **`+1.58`** | 位置外移 |
| `R5/left` | `628-45(1)+` | `628-45` | `G007` | `346.18` | `346.18` | `+0.00` | `8.03` | `8.03` | `+0.00` | **`+3.32`** | 位置外移 |
| `R5/left` | **`628-53(2)`** | `628-53` | **`G025`** | `0.99` | **`0.00`** | **`−0.99`** | `0.39` | **`0.02`** | **`−0.37`** | `+2.96` | 位置外移 |
| `R5/left` | `628-18(2)` | `628-18` | `G005` | `213.06` | `214.54` | `+1.48` | `4.84` | `4.85` | `+0.01` | `+2.98` | 位置外移 |
| `R5/left` | `628-7(2)` | `628-7` | `G022` | `247.16` | `250.76` | `+3.60` | `5.58` | `5.64` | `+0.06` | `+3.04` | 位置外移 |
| `R5/left` | `628-31(1)` | `628-31` | `G027` | `10.46` | `10.70` | `+0.24` | `0.24` | `0.24` | `+0.00` | `+3.04` | 位置外移 |
| `R5/left` | `628-32(1)` | `628-32` | `G026` | `3.08` | `3.14` | `+0.06` | `0.07` | `0.07` | `+0.00` | `+3.04` | 位置外移 |

🔒 **`R4/left 628(1)+` 之 `+1.58`** ＝ 補令六 `§一-2` 所核可之 `殘-1`：**純位置量**，
其 `a 面積`／`G`／`幾何面積`／`宗地寬度`／`負擔比率` 逐欄 `Δ ＝ 0` ✅（本批復現）。

### 三-4　🔴 **與發單側所載量級之相異（照實·⛔ 頂替）**

補令六 `§五` 逐字載：「`G` 變動 `0m` `6` 宗／`3.5m` `5` 宗，**逐宗增減 `+0.06`〜`+1.48 ㎡`**」。

| 量 | 發單側所載 | **本批自得** | 判 |
|---|---|---|---|
| 變動宗數 | `0m` `6`／`3.5m` `5` | **`0m` `6`／`3.5m` `5`** | ✅ **相符** |
| `ΣΔ`／`Σ|Δ|` | `+3.29`／`10.47`；`+4.39`／`6.37` | **同** | ✅ **相符** |
| **逐宗增減之值域** | **`+0.06`〜`+1.48`**（全正）| **`0m` `−3.59`〜`+3.51`**／**`3.5m` `−0.99`〜`+3.60`** | 🔴 **相異** |

🔴 **該值域之陳述<u>不成立</u>**：實測含**負值**二筆（`−3.59`／`−0.99`），且正向最大為 `+3.60`（⛔ `+1.48`）。
🔒 **⛔ 影響 `ΣΔ`／宗數之相符**（二者逐位相同）⇒ 係**值域之陳述**單獨為誤。
🔒 **本項依補令六 `§五` 之明令**（「發單側實測·對拍用·**⛔ 採信**」）**自行重得**，照實出艙。

---

## §四　🔴 **呈 KL 之域界事項**（⛔ 由 CC 或發單側自裁）

🛑 下列**⛔ 施工單 `§八` 所列之停機款**（`P-1`／`P-5`／`P-6`／`P-8`／`P-9` 皆未觸發），
惟其**受詞為土地**，依「只在真正觸及土地法規之域界時才停機上呈」**呈 KL**：

### 四-1　二宗之分配面積被壓至**近乎零**

| 情境 | 街廓/側 | 暫編地號 | 所有權人 | `G`（前 → 後）| 宗地寬度（前 → 後）|
|---|---|---|---|---|---|
| `0m` | `R2/left` | **`628-42(1)`** | **`G011`** | `3.82` → **`0.23 ㎡`** | `0.76` → **`0.18 m`** |
| `3.5m` | `R5/left` | **`628-53(2)`** | **`G025`** | `0.99` → **`0.00 ㎡`** | `0.39` → **`0.02 m`** |

🔒 **性質之照實界定**：二宗於**改前即已遠低於法定最小寬**（`3.5 m`）
⇒ 係**既有違反之加劇**，**⛔ 本批所新生之違反類別**。
🔒 `628-53(2)` 另為**既有終局**之受詞（`CLAUDE.md` 載「【未配到屁股線】`1` 宗——`R5` 之 `628-53(2)`」）。
🛑 **`G` 被壓至 `0.00 ㎡` 是否可接受、該宗應否轉為現金補償或併入調配池，
係<u>域裁</u>（`裁示1(a)` governs ／ `Q3` 增配修正式之射程）⇒ 候 KL。**
⚠️ 併記：`628-42` 於 `on` 態另於 `R3/right` 獲 `628-42(2)` **`89.96 ㎡`**（新可達）；
`628-53` 亦於 `R6/right` 有 `628-53(1)` `0.26 ㎡`。**是否以歸戶合計論其後果，亦候 KL。**

### 四-2　新可達街廓使 `27`（`0m`）／`10`（`3.5m`）宗**首次獲配**

其分配面積自 `0.00 ㎡`（`0m·R5 628-53(2)`）至 `2017.54 ㎡`（`0m·R4 628(1)+`）。
🔒 此係施工單 `§二 c2` 所**預期**者（「本 `commit` 預期使管線通過原中止點」），⛔ 意外。

---

## §五　併線前必答（施工單 `§六`）之辦理狀態

| # | 項 | 狀態 |
|---|---|---|
| `1` | **覆蓋帳**（`P-1`）| ✅ **`§二-4`**——`on` 態 **`34`** 組／逐對總數 **`0`**（母體基數同格出艙·期初 `13` ⇒ 期末 **`34`** > `13` ✅）|
| `2` | **`V-1′` 五款 ＋ 三組對照** | 🔴 **結構上不可量·見 `§六`**（⛔ 判綠·⛔ 判不過）|
| `3` | **`run_all` 名目差集** | ✅ **`§七`**——類 `(iii)` 真迴歸 ＝ **`0`** ⇒ 停機款未觸發 |
| `4` | **差異表呈 KL** | ✅ `§三`／`§四` |
| `5` | **`P-5` 逐街角側出艙** | ✅ `§二-3`（`8` 側逐側·`G`／範圍面積／全多邊形三欄） |

🔒 **`R3` 組之併記**（必答 `1` 逐字要求）：`R3` 於期初**二情境皆不可達**、於 `c2` 態**始可達**，
其三組（`R3·left` `5` 宗／`R3·right` `5` 宗／`R3·抵費地` `1`）之**逐對總數皆 `0`** ✅
（見 `§二-4` 之 `34` 組母體）。依 `VR-095` **恆標「單源·歸納支持」**。

---

## §六　🔴 `V-1′`（必答 `2`）於 `c2` 態**結構上不可量**（**自毀式之第二例**）

🔒 **機械證據**：`verify/probes/probe_WG9266_V1prime_3groups.py` 之 docstring 逐字載其母體 ＝
「**二態之 harness 落檔 `verify/out/got_G值_退縮3.5m_partial.csv`**」。
而 `_partial` **只在管線<u>中止</u>時產生**（補令五 `§三-1` 所立·坑 `2` 逐字）。
🔴 **`c2` 之受詞正是「使管線⛔ 再中止」** ⇒ `3.5m` 於 `c2` 態**跑完**（本批實測：中止街廓 ＝ **無**）
⇒ **該 `_partial` 於 `c2` 態⛔ 存在** ⇒ `V-1′` 之器**結構上量不到 `c2` 態**。

🔒 **此即補令五 `§三-1` 所立之通則之<u>第二例</u>**（逐字）：
> **凡驗收條件所繫之量測管道，其存在本身若取決於「缺陷仍在」，該條件即為自毀式，⛔ 立。**

🛑 **CC ⛔ 自行處置**：其解為「將 `V-1′` 之器**改自記憶體側取幾何**」（＝ 補令五 `§三` 對覆蓋帳所為者），
**係新器之建置** ⇒ 依補令六 `§四`（⛔ 逾授權）與 `自解款`**黑名單**（須新訂準據）**候發單側**。
🔒 **⛔ 以「判定集為空」判綠**，亦⛔ 以 `rc` 代之（`序 11 之一般化` 款 `三`）。

---

## §七　`run_all` 名目差集（必答 `3` ＋ `P-9`）

**執行處** ＝ **拋棄式 clone** `C:/tmp/wg268c2`（`run_all` 改寫已追蹤 log）。
**受測物逐位驗**（起跑前）：`app.py` 工作區 `1458029` B／`CR 0`／`sha256` 前 `16` ＝ `9873b168278fbc08`
＝ **blob** `1458029` B／同 `sha256` ⇒ 受測物與 `c2` 倉態**逐位一致**方起跑。
🩸 **首次 clone 誤以<u>截斷</u>之 `commit` 取物 ⇒ `fetch` 失敗而 clone <u>靜默停在 `main`</u>**
（`app.py sha256 4860f108…` ≠ wip 之 `1e15d45a…`）——**CC 自捕**（`harness-worktree-crlf-trap` 同族），
已以**全 `40` 碼**重取並逐位驗明。

**比較端** ＝ `S-5` 之期初態 `verify/out/WG9268pR_runall_pre.log`（施工單 `§六 3` 逐字·⛔ 凍存名單之舊態）。
**器** ＝ `verify/probes/probe_WG9268p_c2_nameset.py`（新建），其三分類**一律呼叫倉內既有之
`verify/wv_reconcile.py` 之 `parse_log()` ＋ `classify()`**（⛔ 自擬框）。

### 七-1　實跑之量

| 量 | 期初（`S-5`·`off`）| **期末（`c2`·`on`）** |
|---|---|---|
| `rc` | `1` | **`1`** |
| `stdout`／`stderr` | `52904` B ／`0` B | **`193466`** B ／**`0`** B |
| `RESULT`（逐字）| `RESULT: FAIL` | **`RESULT: FAIL`** |
| **判定行** `✅ PASS`／`🔴 FAIL` | `10` ／ `24`（合 `34`）| **`24`** ／ **`40`**（合 **`64`**）|

🔒 **判定行合計由 `34` 增為 `64`**（＋`30`）⇒ **原中止之後之世代確已執行**，與 `P-8` 之可達性擴張一致。

### 七-2　三分類（器 ＝ 倉內既有 `wv_reconcile.classify`·⛔ 自擬框）

| 類 | 項數 | 內容 |
|---|---|---|
| **類 I**（正規化後相同·自動視為同）| **`16`**（其中 raw 即相同 `12`）| — |
| **類 II**（僅 traceback 行號不同）| **`0`**（**空**）| — |
| **類 III**（訊息實質不同）| **`2`** | 見下**逐項具名** |

🛑 **類 III 之逐項具名**（⛔ 靜默）：

| # | 名目 | 期初 → 期末之原因變動 | 判 |
|---|---|---|---|
| `1` | `W-F F.0` | `[F.0] stepg_ctx[0m] 缺（trunk A 未成功？）` → `[F.0] 🔴 GSA 錨檢破（0m）` | 🟡 **上游今已產出** ⇒ 該檢**首次真正執行**、其原因由「無從檢查」改為**實檢之結果**。⛔ 迴歸 |
| `2` | `W-G G.2 世代幾何曝出契約` | 上游未產出清單 `['trunkA(_ctx)', 'F.0', …]` → `['F.0', …]` | 🟢 **`trunkA(_ctx)` 已自清單消失** ＝ 其**轉綠** ⇒ 係**改善**、⛔ 惡化 |

### 七-3　🔴 `P-9` 之三類（**機械歸類**·器 ＝ `probe_WG9268p_c2_p9class.py`）

🔑 **判別式（⛔ 以街廓名為啟發式）**：`P-9` 類 `(iii)` 逐字 ＝「**原可達且原綠**而今紅」
⇒ 其**充要之機械判準** ＝ **該名目於期初 log 是否曾以 `✅ PASS` 出現**。
（期初⛔ 出現 ⇒ 該世代**未執行** ⇒ 類 `(ii)`。）
🔑 **判別力（兩造）**：取一**期初 PASS** 之名目（`-a診斷0m`）⇒ 判為類 `(iii)` ✅；
一必不存在之名目 ⇒ 判為類 `(ii)` ✅ ⇒ **本式非恆綠亦非恆紅**。

| 類 | 項數 | 內容 |
|---|---|---|
| **`(i)`　本批所修者<u>轉綠</u>** | **`6`** | `v3·StepG0m（結構閘/看守觸發）`／`v3·StepG3.5m（結構閘/看守觸發）`／`v3 財務接線閘`／`W-D.3 碎片`／`W-D.4`／`W-G G.1 接線層 ctx-builder 同源` |
| **`(ii)`　新可達之世代所新曝之紅** | **`21`** | `v3·G值{0m,3.5m}`／`v3·滑池槽{0m,3.5m}`／`v3·J表{0m,3.5m}`／`k* 六塊經驗錨{0m,3.5m}`／`W-D.3 碎片幾何`／`W-D.3 碎片逐邊CAD`／`W-D.4` 六項／`W-D.4 MinA_區`／`W-D.4 31旗標`／`F.0-pre 雙軌錨`／`F.0 釋池對象`／`W-D.4 遞補錨 R6` |
| 🟢 **`(iii)`　真迴歸**（原可達且原綠而今紅）| **`0`** | **空** |

🔒 **`v3·StepG0m`／`v3·StepG3.5m`（結構閘/看守觸發）之轉綠 ＝ 本批之標的本身**
——即 `GB-67` 病二所欲解者。

⚠️ **前綴命中 FAIL `1` 項（逐項具名·⛔ 靜默）**：
`W-G G.1 接線層 ctx-builder 同源（cb_by/cad+centerlines/gA/winners/β財務/blocks欄名/指紋/率鋪底）`
——其前綴鍵 `W-G G.1 接線層 ctx-builder 同源` 於期初**即為 FAIL**
⇒ **同一檢而名目字面加長**（因可比之項變多），**⛔ 新增之紅、⛔ 迴歸**。
🩸 其致該名目**同時**出現於「凍存有而現況無」與「現況有而凍存無」二清單（字面不同之故）。

### 七-4　判

> 🟢 **`P-9` 之停機款（施工單 `§八`：類 `(iii)` 非空 ⇒ 停機）<u>未</u>觸發**。
> 🛑 **紅之總數由 `24` 增為 `40`（＋`16`）本身⛔ 為判準**（施工單 `P-9` 逐字）
> ——其 `21` 項經機械判準歸為類 `(ii)`（期初**未執行**之世代），`0` 項為真迴歸。

---

## §八　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| `c1376af`（**`c2`**）| `683b7fa..c1376af` | **`app.py`**（命中 `1` 行）| 🔴 **生產碼 commit** ⇒ **⛔ 逕推主線**·側分支 ＋ KL 放行 |

---

## §九　續辦之界

🛑 **本報告⛔ 構成併線之請求以外之任何事**。
🔒 **KL 放行後**方得將主分支推進至 `c1376afa2bc3ad0df3a34c412a9c06e08e188a5a`。
🔒 **其後之 `c3`**（移除旗標 ＋ 更新註解·含本報告 `§一` 所具名之**過期 docstring**）
與 **`c4`**（差異表 ＋ 執行報告 ＋ `GB-67` 末端追加）**各為獨立 `commit`**。
