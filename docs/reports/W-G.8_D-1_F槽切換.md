# W-G.8／**D-1**：`F` 槽切換（路寬 → SIDE_LINE 全長）—— 🔴 **唯一之行為變更**

**錨**：commit A ＝ `2213f1e`（D-0c·零碼面）。本報告述 **commit B**。
**§B-7-1**：`git diff -U0 2213f1e -- app.py` ⇒ **恰 4 行**（2 刪 2 增）。

> **一句話**：切換正確、四項哨兵全過、對帳 41/41 全符、run_all 之變動**全部歸因至同一格**；
> 🛑 **但其正確結果撞到一條既有的未實作路徑**（`wf_f1.py:226` 右鷹架），**該實作不在 D-1 授權範圍內** ⇒ 停機上呈。

---

## §B-1 前置查核（兩項皆成立·**未停機**）

| # | 查核 | 現況 | 判 |
|---|---|---|---|
| 1 | `app.py:17986/:17987` 為 `左/右側路寬(m)`；`:19074/:19076` 為 `左/右側長度(m)` | 逐行貼出確認（見下 §B-2 之 diff 左半） | ✅ |
| 2 | `_SB_pc` 之側字典是否備有「長度」鍵 | **有** ＝ `期望長度_m`（`app.py:12036-12037` 由 `r["左側長度(m)"]` 建構） | ✅ **不停機** |

### 🔧 但「有」不等於「同源」——我第一次取錯，**由哨兵抓出**

我最初把 harness 改成取 `_SB_pc[_lbl]["左側"]["期望長度_m"]`。**哨兵 (a) 判為「切換前態」**，
逼我去量，結果：

| 街廓/側 | 快照 `期望長度_m` | `sb_row['側長度(m)']` | 差 |
|---|---:|---:|---:|
| R1 左 | 33.247000000 | 33.246768540 | **+0.000231460** |
| R1 右 | 33.270000000 | 33.270187112 | −0.000187112 |
| R4 左 | 33.268000000 | 33.267516388 | **+0.000483612** |
| R5 左 | 43.530000000 | 43.530484405 | −0.000484405 |
| R6 右 | 43.382000000 | 43.381506846 | **+0.000493154** |
| **R2 右** | **44.940000000** | **0.000000000** | **+44.94**（陳舊值·該側無 SIDE_LINE） |
| **R5 右** | **47.900000000** | **0.000000000** | **+47.90**（同上） |

⇒ **`期望長度_m` 是快照層之 3dp 手維值，`sb_row['側長度(m)']` 是現算全精度**——
**二者非同源**（且快照側對無 SIDE_LINE 之端仍留有陳舊值）。
⇒ 已改為 `_sbr_pc.get("側長度(m)")`——**與 app 路徑取同一個 dict**（`sb_rows_by_label`），
**bit-identical**。⚠️ 這正是 **GB-9 家族**（兩來源脫鉤、全倉無閘比對）之新實例。

🔑 **若哨兵寫成「與切換前不同即通過」，這個錯會直接溜過去**——
`期望長度_m` 也「不同於路寬」。**正對照（`==` 反向式）才擋得住。**

---

## §B-2 切換（可執行語句**僅此四處**）

```diff
--- a/app.py
+++ b/app.py
@@ -17986,2 +17986,2 @@
-                                        F_left=float(_sbr_ap.get('左側路寬(m)', 0) or 0),
-                                        F_right=float(_sbr_ap.get('右側路寬(m)', 0) or 0),
+                                        F_left=float(_sbr_ap.get('左側長度(m)', 0) or 0),
+                                        F_right=float(_sbr_ap.get('右側長度(m)', 0) or 0),
--- a/verify/selection_pipeline.py
+++ b/verify/selection_pipeline.py
@@ -436,2 +436,2 @@
-            F_left=float(_SB_pc[_lbl]["左側"]["路寬_m"] or 0),
-            F_right=float(_SB_pc[_lbl]["右側"]["路寬_m"] or 0),
+            F_left=float(_sbr_pc.get("左側長度(m)", 0) or 0),
+            F_right=float(_sbr_pc.get("右側長度(m)", 0) or 0),
```

**碼面異動檔恰 2 個**（`app.py` 4 行／`verify/selection_pipeline.py` 4 行）。
正典依據：`F ＝ 街角地第 1 筆面臨側面道路之長度 ＝ SIDE_LINE 全長`
（`.claude/skills/g-formula-rules/SKILL.md`）。登記項：**`GB-41`**。

---

## §B-3 切換後哨兵（**四項全過**）

`verify/out/probe_D1_F_slot.log`（切換後） vs `verify/out/probe_D1_F_slot_切換前凍存.log`（⛔ 一字未改）

| # | 受測 | 切換前凍存 | 切換後 | 判 |
|---|---|---|---|---|
| (a) | `F` 實參 16 格 | 皆 **`8.00`** | 各自側長度 **`33.08`〜`44.95`** | **16/16 翻** ✅ |
| (b) | `3.5m R1/right 628(5)` 真G | `240.57` | **`228.42`** | ✅ |
| (c) | `3.5m R1/right` 結局 | winner ＝ `628(5)` | **無人過門檻 ⇒ 強制抵費地** | ✅ |
| (d) | 全案 | winner `12`／強制 `4` | **`11`／`5`** | ✅ |

**總判：切換前態 False／切換後態 True ⇒ ✅ 具鑑別力（恰一態成立）。**

(a) 之逐格：`0m/3.5m` × `R1左 33.25`／`R1右 33.27`／`R2左 44.95`／`R3右 44.90`／
`R4左 33.27`／`R4右 33.08`／`R5左 43.53`／`R6右 43.38`（切換後 F 與側長度**逐格相等**）。

---

## §B-4 與凍存期望表對帳（**41/41 全符**）

`verify/out/probe_D1_reconcile.log`：解析框 ＝ 凍存檔 §二 四象限表（第 24〜69 行），
框內重複鍵 **0**，讀入 **41** 候選列，比對欄 ＝ **`F改正×舊門檻`**，三欄皆比（真G／門檻／達標含 ★）。

**⇒ 相符 41／不符 0 ⇒ 未觸發 §B-4 之停機條件。** ⛔ 凍存表全程唯讀、未改一字。

### 🔧 對帳器第一版報「10 格不符」——是我的解析 bug，不是資料

凍存檔之 §三 帳目表列首格式與 §二 四象限表**相同、鍵完全一樣**
（`情境 街廓/側 候選 …`）⇒ **同鍵覆蓋 10 列**，取到的是 §三 之 `F(m)` 欄（`33.25` 等）。
⚠️ **而「讀入 41 候選列」恰好等於預期的 41**，那行看起來完全正常。
⇒ 已加**解析框**（限 §二 表內·印出行號範圍）＋**框內重複鍵斷言**（須為 0，否則 `raise`）。

---

## §B-5 `run_all`（⚠️ 本批**預期有 diff**）

**靶**：`verify/out/D1_runall.log`（本批入庫）　vs　`verify/out/K6A2_seg6_gb40_runall.log`

| | 舊 | 新 |
|---|---:|---:|
| PASS | 86 | **74** |
| FAIL | 46 | **49** |
| 名目總數 | 132 | **123** |

### 🔑 逐項歸因——**全部指向同一格**

| 變動 | 內容 | 歸因 |
|---|---|---|
| **＋新增 FAIL 1** | `W-F F.1` | 3.5m R1 之 `right_forced_offset` 轉真 ⇒ 撞 `verify/wf_f1.py:226` 之既有 loud raise |
| **−消失 10** | `F.1 …` 十個細項名目 | F.1 世代中止 ⇒ 其細項不產出（同一歸因） |
| **＋轉紅 2** | `W-D.4 碎片遞補3.5m`（缺 `R1-抵費地-2`／多 `R1-抵費地-3`）<br>`k* 六塊經驗錨3.5m`（`R1` 由 `1` → `2`） | R1 右端**新增一個強制抵費地** ⇒ 抵費地編號位移／街廓分割數 +1 |
| **~改名 2** | `F.2 跨街廓守恆3.5m`（`0.0`→`0.02`）<br>`F.3 結構永久閘3.5m`（`0.01`→`0.03`） | 守恆殘差入名目字串（**GB-38**）；**二者皆仍 PASS**（≤ 容差 `0.585`） |
| **轉綠** | — | **0** |
| **重生之 tracked log 2** | `probe_ruling_K4_s_origin.log`：真G 全面下降（`628(5)` `240.57→228.42`）<br>`probe_ruling_N_e1_touch.log`：3.5m「街角 5／非街角 54」→「**街角 4／非街角 55**」、`628(5)` 由「是」→「否」 | 皆為 F 槽改正與 winner 失格之直接效應 |

🔑 **全部變動歸因至凍存期望表 `F改正×舊門檻` 之 `3.5m R1/right` 一格**
（winner ＝ `628(5)` → **無人過門檻 ⇒ 強制留設抵費地**）。
**⇒ 無法歸因之變動 ＝ 0 ⇒ 未觸發 §B-5 之停機條件。**

**期望 FAIL 名單已更新**：`verify/out/K6A2_期望FAIL名單_D1切換後.txt`
（**49 名目行**·計數指令 `grep -c "^🔴 FAIL"`·⛔ 非 `wc -l`〔含 29 註解行〕），
內含本批增減之逐項歸因。⛔ 舊名單 `K6A2_期望FAIL名單_902f5d1.txt` **未刪、未改**。

---

## §B-6 守恆入帳：**D-2 效應未提前出現**（正面舉證）

D-0 已立帳：`W` 口徑連動 `ΣΔG ＝ 0m +0.0423 ㎡／3.5m −0.0449 ㎡`——**該筆屬 D-2（幾何）**。

**碼面正面舉證（⛔ 非推論）**：

| 受檢 | 對 commit A 之 diff 行數 |
|---|---:|
| `_n_alloc`／`W = float`／`rw_increment`／`_ahat`（`git diff 2213f1e -- app.py \| grep -cE ...`） | **0** |
| `allocation_dir=allocation_dir_block`（`verify/stepg_pipeline.py`·4 處） | **未動** |
| 本批 diff 之全部受影響符號 | **4 `+` 與 4 `−`，全部是 `F_left`／`F_right`** |

⇒ **`W` 之量測軸仍為 ALLOC 法向、`Rw` 之算式未動 ⇒ D-2 效應未提前出現** ✅
⇒ **未觸發 §B-6 之停機條件**（兩效應**未糾纏**）。

⚠️ 併記：本批觀測到之守恆殘差變化為 **3.5m `+0.02`**（`0.01→0.03`），
與 D-2 帳之 `−0.0449` **異號且量級不同** ⇒ 數值面亦不支持「提前出現」之說；
惟**判定以上表之碼面舉證為準**（數值僅為輔證）。

---

## 🛑 §B-8 停機上呈（**本批唯一之停機項**）

**`W-F F.1` 之失敗係 D-1 之正確結果撞到一條既有的未實作路徑。**

`verify/wf_f1.py:226` 逐字：

> 🔴 [3.5m] F.1 R1：`right_forced_offset` 為真，然本檔推進結構為左向（且硬釘 R1）
> ——**右鷹架未實作**（W-7·#18④ 禁補未證死碼）。撞到即停機上呈·禁靜默走左向邏輯。

- **這不是 D-1 的 bug**——該 raise 是**既有的、明文的**設計，且明令「撞到即停機上呈」。
- **D-1 之結果是對的**：`3.5m R1/right` 依正典之 `F` 確實不過資格閘 ⇒ 強制留設抵費地
  ⇒ `right_forced_offset = True` 為**必然**。
- 🔴 **右鷹架之實作不在 D-1 授權範圍內**——D-0c §A-1「D 波之範圍界線」六項未授權此事，
  而它屬**新機制實作**、非 `F` 槽修正。

⇒ **上呈 KL 裁定其歸屬**：① 併入 D 波（須擴授權）／② 另開波次／③ 其他。
⛔ **CC 未自行實作、未以任何方式繞過**（⛔ 未加旗標、未靜默走左向）。

---

## §B-7 驗收

| # | 項 | 結果 |
|---|---|---|
| 1 | `git diff -U0 <commitA> -- app.py \| wc -l` ⇒ 恰 4 行 | ✅ **4**（逐行見 §B-2） |
| 2 | §B-3 四項哨兵全過；(a) 16/16 逐格列示 | ✅ |
| 3 | §B-4 對帳逐格；不符處已停機上呈 | ✅ **41/41 全符·不符 0** |
| 4 | §B-5 run_all 變動已逐項歸因；無法歸因者 ＝ 0 | ✅ **0** |
| 5 | §B-6 正面確認 D-2 效應未提前出現 | ✅ 碼面舉證 |
| 6 | push 後回報附 SHA，標明 commit A／B | ✅ 見聊天回報 |

## §B-9 ⛔ 本批**未**做者

1. **F.1 右鷹架之實作** —— 🛑 停機上呈（§B-8）。
2. **D-2**（`K-9-5-4` 幾何切換）—— 未動；`_build_corner_range_v3` 仍為舊構造。
3. **D-3 對帳** —— 待 D-2 之後。
4. **`E-1.7` 門檻之處置**（`GB-42`）—— 屬 KL。
5. **舊期望 FAIL 名單之刪除** —— ⛔ 未刪（保留供考古）。
