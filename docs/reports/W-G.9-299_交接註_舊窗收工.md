# `W-G.9-299`　交接註（舊窗收工·**態之移交**）

> 🛑 **本註⛔ 載任何判**（⛔ 判路丙、⛔ 判受領、⛔ 判「該處」）——判由**發單側**為之。
> 🛑 **本註內之數⛔ 冠「已交付」「已核可」之詞**——核可係發單側之行為（`自誤 403` 攔法 ①）。
> 🔒 **每項同格載其產生指令**（`自誤 392`）·**逐項自跑當場重得**·⛔ 轉引補令或 `W-G.9-299` 之數。
> 🔒 **態之判** ＝ **甲**（`W-G.9-299` 工項零／一／二 **未辦畢**）——其產生指令與 `commit` 列見第 `9` 項。

---

## `1`　主線（`$ git ls-remote origin refs/heads/wip/s1-endpart`）

```
80d59188c98a9e3f763173b3e4742f0e9fda6af7	refs/heads/wip/s1-endpart
```

（全 `40` 碼·⛔ 短前綴。🛑 本項係**本註入倉前**之值；其後尚有**本註之 commit** 與**收工閘之 commit**，
故新窗**⛔ 以本值為期**，一律**自行 `ls-remote` 實查**。）

## `2`　`$ git rev-parse HEAD:app.py`

`4379108a4856714078c410ff4e6291ecfdf6d2c1`（自 `W-G.9-281` 起一字未改）

## `3`　`$ git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`

`rc = 1` ⇒ **非祖先**（凍存 `p3a`·**⛔ 併線·⛔ 刪除**）

## `4`　四簿（`$ python verify/probes/probe_WG9267_issuer_measurers.py registry`）

| 簿 | 相異 | MAX | MIN | 缺號 |
|---|---|---|---|---|
| 自誤 | **`393`** | **`403`** | `7` | `[106, 355, 356, 357]` |
| `GB` | `168` | `170` | `1` | `[12, 87]` |
| `VR` | `80` | `95` | `14` | `[73, 75]` |
| `K-9` | `28` | `29` | `2` | `[]` |

🔒 **次號**（由上表之 MAX 推）＝ 自誤 `404`／`GB` `171`／`VR` `96`／`K-9` `30`。

## `5`　坑（`$ python verify/probes/probe_WG9269_pit_index.py`）

**`87` 標籤／`87` 可解析**（數字系 `26` ＋ 字母系 `61`）·末標籤 `bi` ⇒ **下一標籤 `bj`**；
母體 ＝ `HEAD` 之追蹤 `.md` **`798`** 檔。
器自證二造：造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ ⇒ **器非紅**。
🛑 本項係**本註入倉前**之值；本註入倉後母體 `.md` 檔數將 `+1`（其自洽檢見收工閘 `5`）。

## `6`　生產碼母體（**正面列舉**·⛔ 以 pathspec `verify/*.py` 界定·坑 `be`）

`$ git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＝ **`33`** ＋ `app.py` **`1`** ＝ **`34`** 檔。
判別力[必不命中] 子層（`$ git ls-files | grep -E '^verify/.+/.+\.py$'`）＝ **`313`**（須 `> 0`·**逐批漂移**·⛔ 定值·`自誤 386`）。

## `7`　`baselines`（`$ git ls-tree -r HEAD verify/baselines | sha256sum`·`core.quotePath` **預設**·⛔ `ls-files`）

`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`；
檔數（`$ git ls-files verify/baselines | wc -l`）＝ **`298`**。

## `8`　`$ git ls-remote origin 'refs/heads/verify/*'`

該族共 **`20`** 個；`verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**）；
判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`**。

## `9`　本批已辦工項之落點與逐 `commit`（`$ git log --format='%H %s' --reverse 9660b53cdc61690840029c52ec7126476d205d1a..HEAD`）

| # | `commit`（全 `40` 碼） | 題（逐字） |
|---|---|---|
| `1` | `f608d50e6380ceb095949b28ac12ca605a35c050` | `W-G.9-299 工項零：本單與補令一原封入倉（二單·三方逐位對拍·受詞自證）⛔ 零生產碼` |
| `2` | `d28660220a01ecc70c94110bd0eb6587d15a8b4a` | `W-G.9-299 工項一／二：自誤 400／401／402 之鑄 ＋ GB-170 之末端追加（哨兵界·⛔ 行號為錨）⛔ 零生產碼` |
| `3` | `80d59188c98a9e3f763173b3e4742f0e9fda6af7` | `W-G.9-299 補令一 §二：自誤 403 之鑄（分列 commit·哨兵界·⛔ 行號為錨）⛔ 零生產碼` |

**落點**：`docs/orders/W-G.9-299_施工單_鑄號形之更正與路丙之受領與修批.md`（新檔·`342`／`0`）／
`docs/orders/W-G.9-299_補令一_舊窗收工批.md`（新檔·`183`／`0`）／
`docs/reports/W-G.9波_claude.ai側自誤登記.md`（`69`／`0` ＋ `28`／`0`）／
`docs/reports/W-G.4_泛用阻塞項登記表.md`（`27`／`0`）。

🔒 **態甲之判**：`$ git log --format='%H %s' 9660b53cdc61690840029c52ec7126476d205d1a..HEAD`（**開工時**）
⇒ commit 數 ＝ **`0`**；其中題含 `W-G.9-299 工項` 者 ＝ **`0`** ⇒ **態甲**（工項零／一／二 未辦畢）。

## `10`　🔴 未辦工項之具名（供新窗接手·⛔ 略）

**未辦者 ＝ `W-G.9-299` 之工項三（`§五`）與工項四（`§六`）**——依補令 `§一` 態甲「續辦至工項二畢即止」
與 `§六-1`「一切態下⛔ 辦工項四」。其受詞逐字（自 `docs/orders/W-G.9-299_施工單_鑄號形之更正與路丙之受領與修批.md` 取）：

### `10-1`　工項三（`§五`）：**路丙之接觸面之現查**（主線·零生產碼·🛑 **含停機款**）

| 款 | 受詞（節略·**全文見該單 `§五`**） |
|---|---|
| `1` | `_corner_buffer_S` 之**全部呼叫端**：母體 ＝ 生產碼 **`34`** 檔（正面列舉）；框 ＝ **AST** 之 `Call` 節點（`func` 為 `Name('_corner_buffer_S')` **或** `Subscript(Name('ns'), Constant('_corner_buffer_S'))`·**二形並取**）；逐處出艙 `檔:函式:`**字樣錨**（⛔ 行號為錨）與其 `allocation_dir` 位之**實參逐字**（`ast.unparse`）。**檔／處二數並報**；併報**文字層**之數以資對照·**二數相異即 loud 具名** |
| `2` | 上開各處**執行期是否走到**：包裹 `ns["_corner_buffer_S"]` ＋ **呼叫鏈之 `co_name` 序**（⛔ `id(·)`）；二態（`SB` `0.0`／`3.5`）分列；**逐閘出艙執行次數**；⛔ 以「未執行」充「非接觸面」——未執行者 **loud 具名為「本案未走·射程未定」** |
| `3` | **`_first_corner_alloc_dir` 於各呼叫端之可達性**（三態：模組全域直呼／`ns` 注入／⛔ 可達）與 **`side_mid` 於各處之在域性**（`f_locals`／`t.body` 頂層·**鍵先自證存在**）；`自誤 399` 族：注入通道須**窮舉**（模組 `__globals__`／`ns`／參數／閉包）·⛔ 以「其一可達」充「皆可達」 |
| `4` | 🛑 **停機款**：`S` ＝ 款 `1` 之全部呼叫端、`F` ＝ 款 `2` 實測**生產期走到**者。判「**改 `F` 即足**」是否成立——`S∖F` 諸處**是否為 `forced` 之路徑**須逐處出艙其判與**其判之產生指令**。**`\|F\| = 0` ⇒ loud 拒測**；**`S∖F` 中有任一處經判為 `forced` 之路徑而未在 `F` 內 ⇒ 判器紅、停、上呈**（⛔ 續辦工項四）；**⛔ 由 CC 判「改幾處算只改該處」** |
| `5` | **判別力三造**：[必非零] `\|S\| ≥ 1`／[必相異] 文字層之數與 AST 之數須**各自出艙**／[必為零] 一**執行期組出**之人造函式名於款 `1` 之框下命中 ＝ `0` |

🛑 該單逐字：「**本工項⛔ 改碼一字、⛔ 開分支**。」

### `10-2`　工項四（`§六`）：**修批**（🔴 **凍存分支** `verify/W-G.9-299-gb170`·**⛔ 併線**·**⛔ push 生產碼至主線**）

🔑 其**明文授權**之三項手段（射程逐字·⛔ 逾之）：① **改生產碼**——射程 ＝ **僅款 `1` 所列之處**，且**僅該一位實參**
（`_corner_buffer_S` 之 `allocation_dir` 位）；② **開分支**——**僅** `verify/W-G.9-299-gb170` 一支；
③ **執行 harness**——改前／改後各一次全量。
🔴 **⛔ 在授權內**：⛔ 併線、⛔ push 生產碼至 `wip/s1-endpart`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、
⛔ 改任何**函式簽章**、⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／`k956_W_from_mp` **之函式體一字**。
🛑 **工項四受 `§五` 款 `4` 之停機款拘束**——該款紅則**⛔ 辦本工項**。

其六款之受詞（節略）：`1` 改之逐字（改動列數須 ＝ 處數）／`2` 其餘逐位不變之證（改前後各跑 `run_verification`·
判別力[必相異] ＝ 三 `forced` 格須確有相異）／`3` 🔑 **逐宗量分配面積差**（**第二次放行之受詞**·⛔ 只報總計·
⛔ 以 `2dp` 相同充「無差」）／`4` `A′`／`B′` 之**實測**（⛔ 設計驗算·與 `W-G.9-298R` 之設計驗算值**分表**）／
`5` 藍影（`K-9-23` 閘一）求值次數改前／改後（⛔ 判其應否求值）／`6` 分支之落地（該支命中 `1`·
`merge-base --is-ancestor <該支> origin/wip/s1-endpart` 須 `rc = 1`·主線 `app.py` 須仍 `4379108a…`）。

🔒 **KL 之放行**：**仍在·本窗仍⛔ 動用**——其動用移至新窗（`W-G.9-300`）。

## `11`　物證落檔（**先 `$ git cat-file -e <rev>:<path>` 驗存在**·**三態分列**）

| 物證 | 期初存在 | 期末存在 | blob | 判 |
|---|---|---|---|---|
| `verify/out/WG9295R_joint_v1_selfpolluted.log` | `True` | `True` | `1a35dc168655` → `1a35dc168655` | **逐位未動** |
| `verify/out/WG9296R_w0rec_v1_blknone.log` | `True` | `True` | `f93eb2fcb7e6` → `f93eb2fcb7e6` | **逐位未動** |
| `verify/out/WG9297R_w0caliber_v1_blkabsent.log` | `True` | `True` | `16742f3805e0` → `16742f3805e0` | **逐位未動** |
| `verify/out/WG9298R_gate3_v1_firstcall.log` | `True` | `True` | `5cef4c8dfe7a` → `5cef4c8dfe7a` | **逐位未動** |

🔒 本批**⛔ 新增**任何物證落檔（本批**⛔ 驅動長跑** ⇒ 無新落檔）。
判別力[必存在] ＝ `app.py` 於二態皆 `True`；對照組[必為零] ＝ 一**執行期組出**之人造路徑於期末存在 ＝ `False`。

## `12`　本窗自捕之全部（**逐則**·形／徵候／根因／攔法·**⛔ 略**）

本窗共 **`13`** 則，皆**於出艙前攔下**、**⛔ 頂替**。

### `W-G.9-295`（`3` 則）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `1` | 背景長跑之輸出以 `\| tail -25` 截斷，據**半成品**清點得「`67`／`67`·下一標籤 `ap`」 | 與首跑 `tail` 已見之 `bb`〜`bi` **互斥** | 母體殘缺而其數讀來合理 | **⛔ `\| tail -N` 取長跑輸出**；等待須**綁行程結束**（task 之 `rc` 行）·⛔ 以檔案現存大小判完成 |
| `2` | `CR` 之[必非零]對照報「未尋得」 | 其為 `0` 而該檢應恆非零 | 以 `text=True` 讀 `.dxf` blob ⇒ `UnicodeDecodeError` 被吞成空 | **讀 blob 一律取 bytes** |
| `3` | 自我驗證閘 `(iii)` 報「錄 `1` 筆·最小差 `0.0` ⇒ ✅」而同輸出之表頭載 `k956_W_from_mp` 執行 **`0`** 次 | **二數互斥** | 包裹仍在 `ns`，**器自身之呼叫被記入同一母體** ⇒ 拿自己的輸出當對照 | **量測器之母體⛔ 含其自身之輸出**——限 `drive()` 期間之**前綴**，並同格出艙「生產錄 `N`／自身 `M`」 |

### `W-G.9-296`（`3` 則）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `4` | 三格款 `1` 皆報「生產路徑⛔ 呼叫 `_mp_base_W0`」 | 同輸出之表頭**明載通道② 錄到 `1` 次** ⇒ 二數互斥 | `f_back.f_locals['blk']` 恆得 `None`——真名為 **`blk_label`** ⇒ 逐格比對必失配 | `f_locals` 變數名為鍵者須**自證其存在**（`in f_locals`）並**增第二鍵**·**二鍵並報** |
| `5` | [必為零]·受詞於三格皆「不可得」時判成 **✅** | 判定組為**空**而結論為綠 | `all(...)` 於**空集恆真** | **判定組為空一律 loud 拒測**，並出艙判定組**基數** |
| `6` | [必非零] 只印帶號值（皆負）而判 ✅ | 顯示與判準不同口徑 | — | **帶號與絕對值並列** |

### `W-G.9-297`（`4` 則）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `7` | 工項零之自證**逕跑前批（`-296`）之器** | 造甲欄印出「`W-G.9-295` 之單」而與本批無涉 | 受詞為他批之單 | **受詞自證**（坑 `bf`）：器須**先印其所量之檔名**並與本批單號對拍二造 |
| `8` | 三格皆報「無鏈頭之錄」 | **`blk_label` 存在率 `0`／`149`** | `solve_G_binary` **⛔ 由 `_run_step_g_impl` 直接呼叫**（實測鏈 `_solve_G_one` → `_corner_first_lot_G` → `_corner_block_true_G` → `run_corner_pk`）⇒ `f_back` 無該名 | **逐層上溯**（≤ `15` 層·出艙深度與呼叫鏈）＋**第二鍵 `side_mid`**·二鍵並報 |
| `9` | `rc = 1`（`TypeError` `int` vs `NoneType`）而**落檔 bytes 與前版相同** | 只看落檔會誤讀為「修法無效」 | 器拋出 ⇒ **未落檔** | 以 **`rc` ＋ stdout 之 traceback** 為判·⛔ 以落檔存在充「已跑完」 |
| `10` | 物證表報「`454d5e3dadb8` → `16742f3805e0` ⇒ 逐位未動 ＝ `False`」 | 其「期初」值 ＝ **commit 之 sha 前綴**、⛔ 一個 blob | `git rev-parse <rev>:<不存在之路徑>` **未加 `--`** 時**回退**為解析該 `rev` 並以 `rc = 0` 返回 ⇒ 判空從不觸發 | **先以 `git cat-file -e <rev>:<path>` 驗存在**·**三態分列**（`T/T` 比 blob／`F/T` **本批新入倉**／`*/F` 疑遭刪除） |

### `W-G.9-298`（`2` 則）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `11` | 款 `1` 同一性報 `[False, False, False]` | **與判別力四造全綠並存** ⇒ 二者互斥 | `settrace` 只取「**第一個** `call` 事件」，而 `J._w0` 先呼 `numpy` 之 `asarray`／`linalg.norm` ⇒ 攔到 `numpy` | 蒐集**全部** `call` 事件、以 `is` 判受詞之 `code` 是否在其中，並出艙呼叫鏈之 `co_name` 序 |
| `12` | 字樣錨之**判別力對照**取寬字樣 `自誤 400`，命中 ＝ `1`（須 `> 1`） | 「寬」字樣之命中**不大於**錨 | 該號**本批方鑄** | **本批方鑄之號⛔ 得作其自身之寬字樣對照**——改取真寬字樣（如 `自誤 `·含尾空格） |

### `W-G.9-299`（本批·`1` 則）

| # | 形 | 徵候 | 根因 | 攔法 |
|---|---|---|---|---|
| `13` | `三-1` 款 `2` 之「相異」我以 `RE_ZW_DEF` **單一框**算得 **`379`**，而期值為 `392` | 與同一受詞之器所報數**相異 `13`** | 簿之正典「相異」係**三框之聯集**（器碼逐字 `_summ(s \| e)`），⛔ 單一框 | 凡出艙簿之「相異／MAX／缺號」者，**一律取器本身之值**（⛔ 以自擬之單一框代之）；框與母體須同格載明 |

---

🔒 **本註⛔ 載任何判**；其受詞係**態之移交**。收工閘之數見 `docs/reports/` 之本批 `commit`（本註入倉後另行末端追加）。

---

## `§四`　收工閘（**交接註入倉後量**·嚴格末端追加）

### 閘 `1`　`$ git show --numstat <各 commit>` ⇒ **刪除欄全 `0`**

| `commit`（全 `40` 碼） | 逐檔 `numstat` |
|---|---|
| `f608d50e6380ceb095949b28ac12ca605a35c050` | `342`／`0`　`docs/orders/W-G.9-299_施工單_鑄號形之更正與路丙之受領與修批.md`；`183`／`0`　`docs/orders/W-G.9-299_補令一_舊窗收工批.md` |
| `d28660220a01ecc70c94110bd0eb6587d15a8b4a` | `27`／`0`　`docs/reports/W-G.4_泛用阻塞項登記表.md`；`69`／`0`　`docs/reports/W-G.9波_claude.ai側自誤登記.md` |
| `80d59188c98a9e3f763173b3e4742f0e9fda6af7` | `28`／`0`　`docs/reports/W-G.9波_claude.ai側自誤登記.md` |
| `e0215256c828b665f69497dca06f108761116da9` | `166`／`0`　`docs/reports/W-G.9-299_交接註_舊窗收工.md` |
| （本 `commit`） | 收工閘之末端追加（其 `sha` **⛔ 預寫**） |

### 閘 `2`　生產碼判法（母體**正面列舉 `34` 檔**）⇒ 逐區間 **`0` 行**

| 區間 | 命中行數 |
|---|---|
| `9660b53..f608d50` | **`0`** |
| `f608d50..d286602` | **`0`** |
| `d286602..80d5918` | **`0`** |
| `80d5918..e021525` | **`0`** |
| `9660b53..e021525`（全批） | **`0`** |
| **對照組[必非零]** `17fd981^..17fd981` | **`2`** |

### 閘 `3`　`.md` 之 `CR`（框 ＝ **倉內 blob**·**取 bytes**）

母體 ＝ 本批異動 **`5`** 檔：`W-G.9-299_施工單…md`（`36581` B）／`W-G.9-299_補令一…md`（`15254` B）／
`W-G.4_泛用阻塞項登記表.md`（`904710` B）／`W-G.9-299_交接註_舊窗收工.md`（`14292` B）／
`W-G.9波_claude.ai側自誤登記.md`（`836361` B）⇒ 逐檔 `CR` ＝ `0`·**合計 `0`**。
判別力[必非零] ＝ `data/V6.dxf` 之 `CR` ＝ **`12308`**（全長 `88341` B）。

### 閘 `4`　四簿期末（🔑 **三態同**之期值）

| 簿 | 期初（`§零` 閘 `5`·態甲） | **期末** | 期值 | 判 |
|---|---|---|---|---|
| 自誤 | 相異 `389`／MAX `399` | 相異 **`393`**／MAX **`403`** | 相異 `393`／MAX `403` | ✅ |
| `GB` | `168`／`170` | `168`／`170` | 一字未動 | ✅ |
| `VR` | `80`／`95` | `80`／`95` | 一字未動 | ✅ |
| `K-9` | `28`／`29` | `28`／`29` | 一字未動 | ✅ |

缺號（期末）：自誤 `[106, 355, 356, 357]`／`GB` `[12, 87]`／`VR` `[73, 75]`／`K-9` `[]`。

### 閘 `5`　坑之期末

**`87` 標籤／`87` 可解析**（數字系 `26` ＋ 字母系 `61`）·末標籤 `bi` ⇒ **下一標籤 `bj`**（⛔ 鑄坑）；
母體 ＝ `HEAD` 之追蹤 `.md` **`799`** 檔。
🔒 **自洽檢**：開工時（`HEAD` ＝ `9660b53`）＝ `796` ⇒ **`+3`**，而本批新增之 `.md` 檔數 ＝ **`3`**
（`W-G.9-299` 本單／補令一／交接註；二登記表係**既有檔之追加**）⇒ **相符**。
器自證二造：造甲 坑 `n` 定義處 `6 ≥ 1` ✅／造乙 人造坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ ⇒ **器非紅**。
`ALPHA_LABELS`：`$ git diff --numstat 9660b53..HEAD -- verify/probes/probe_WG9269_pit_index.py` ⇒ **輸出空**。

### 閘 `6`　自限復驗

| 受詞 | 實測 | 判 |
|---|---|---|
| 生產碼 `34` 檔 blob 期初＝期末 | **相異 `0`** 檔（**正面列舉**）；判別力[必相異] ＝ `docs/reports/W-G.9-299_交接註_舊窗收工.md` 期初存在 `False`／期末 `True` | ✅ |
| `app.py` 期末 blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | `$ git merge-base --is-ancestor 05a11bd6… HEAD` ⇒ `rc = 1`（**非祖先**·⛔ 併線·⛔ 刪） | ✅ |
| `WV_BAKE` | `None` | ✅ |
| `GB-79`／`167`／`168`／`169`／`170` **⛔ 修**；`K-9-5-*`／`K-9-12`／`K-9-29` 任何子項⛔ 落地 | 由生產碼 `34` 檔 blob **相異 `0`** 機械蘊含 | ✅ |

🔒 **四物證落檔⛔ 覆寫**（**先 `$ git cat-file -e <rev>:<path>` 驗存在**·**三態分列**）

| 物證 | 期初 | 期末 | blob | 判 |
|---|---|---|---|---|
| `verify/out/WG9295R_joint_v1_selfpolluted.log` | `True` | `True` | `1a35dc168655` → `1a35dc168655` | ✅ **逐位未動** |
| `verify/out/WG9296R_w0rec_v1_blknone.log` | `True` | `True` | `f93eb2fcb7e6` → `f93eb2fcb7e6` | ✅ **逐位未動** |
| `verify/out/WG9297R_w0caliber_v1_blkabsent.log` | `True` | `True` | `16742f3805e0` → `16742f3805e0` | ✅ **逐位未動** |
| `verify/out/WG9298R_gate3_v1_firstcall.log` | `True` | `True` | `5cef4c8dfe7a` → `5cef4c8dfe7a` | ✅ **逐位未動** |

判別力[必存在] ＝ `app.py` 於二態皆 `True`；對照組[必為零] ＝ 一**執行期組出**之人造路徑於期末存在 ＝ `False`。
🔒 本批**⛔ 新增**任何物證落檔（**⛔ 驅動長跑** ⇒ 無新落檔）。

### 閘 `6'`　`$ git ls-remote origin 'refs/heads/verify/*'`

該族共 **`20`** 個；`verify/W-G.9-299-gb170` 命中 **`0`**（**⛔ 開**·[必為零]）；
判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`**。

---

## `§五`　`⛔ 為之事` 之逐項自檢（補令 `§六`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 辦 `W-G.9-299` 工項四（及其所含之一切手段）；⛔ 開任何分支；⛔ 改生產碼 `34` 檔一字；⛔ 動用 KL 之放行 | ✅ 工項三／四**皆未辦**（交接註第 `10` 項已具名其受詞）；`verify/W-G.9-299-gb170` 命中 `0`；生產碼 blob 相異 `0`；放行未動用 |
| `2` | ⛔ 驅動任何長跑（`run_verification`／雙態或替身驅動／`drive()` 式之管線驅動） | ✅ 本批**⛔ 執行**任一者；所跑之器僅 `probe_order_preflight.py`／`probe_WG9267_issuer_measurers.py`／`probe_WG9269_pit_index.py`／`wg942_append_audit.py`（皆**唯讀掃描**·⛔ 驅動管線） |
| `3` | ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或任一物證落檔；⛔ `WV_BAKE`；⛔ 引任何自 `baselines` 取得之數 | ✅ 見閘 `6`；`refs/heads/verify/*` 之數 `20` 與期初同；本批之數無一取自 `baselines` |
| `4` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ `GB` 簿 `168`／`170` 一字未動；`四-1` 反向機驗證其**⛔ 鑄號之性質**；生產碼 blob 相異 `0` |
| `5` | ⛔ 判路丙成立與否、⛔ 判受領、⛔ 判「孰為射程 `(b)` 之該處」、⛔ 提修法主張、⛔ 判孰是、⛔ 判孰誤 | ✅ 本批只**出艙**與**登記**；交接註逐字自載「⛔ 載任何判」 |
| `6` | ⛔ 追改 `W-G.9-299` 或任何已入倉文件一字 | ✅ 二單原封入倉（三方逐位相同）；`append-only` 機檢三跑皆 `rc = 0`·受檢 `77` 筆·逐字「結論：✅ 全部成立」 |
| `7` | ⛔ 呈 KL 任何事 | ✅ 未呈 |
| `8` | ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述） | ✅ 各 `commit` 之「已推送」皆於 `push` **後**以 `ls-remote` 實查其值方稱之；閘 `7` **出艙於回報·⛔ 入倉** |

---

## `§六`　逐 `commit`（**全 `40` 碼**·**主線**）

| # | `commit` | 題 |
|---|---|---|
| `1` | `f608d50e6380ceb095949b28ac12ca605a35c050` | 工項零：本單與補令一原封入倉 |
| `2` | `d28660220a01ecc70c94110bd0eb6587d15a8b4a` | 工項一／二：`自誤 400`／`401`／`402` 之鑄 ＋ `GB-170` 之末端追加 |
| `3` | `80d59188c98a9e3f763173b3e4742f0e9fda6af7` | 補令一 `§二`：`自誤 403` 之鑄（**分列 `commit`**） |
| `4` | `e0215256c828b665f69497dca06f108761116da9` | 補令一 `§三`：交接註入倉 |
| `5` | （本 `commit`·收工閘之末端追加·其 `sha` **⛔ 預寫**） | 收工閘 |

🛑 **閘 `7`（`push` 後之 `ls-remote`）⛔ 寫入本檔**（補令 `§四` 閘 `7` 明令「**出艙於回報·⛔ 入倉**」）
——本 `commit` 之 `sha` 於其自身落地前尚不存在，事後補入**結構上遞迴**。

🛑 **辦畢 ⇒ 停 ⇒ 本窗封存。** 新施工窗之首則為另行交付之**開場文 `W-G.9-300`**。
