# `W-G.9-323` 交接註：舊窗收工（**⛔ 零生產碼**·⛔ 鑄任何號）

> 🛑 **本文之受詞 ＝ 本窗自身記憶中、<u>倉內未載</u>之事實**；倉內已載者**⛔ 重述**，一律引其 `檔:列` 或其字樣錨。
> 🛑 **本文由 `86%`→`68%` 之窗於換窗前所補**——其受詞為**該窗自身之記憶**，新窗結構上取不得
> （`恆常附款 k②` 之同族：由「新窗未見」⛔ 推「未發生」；**留白即永久遺失**）。
> 🔒 **體例**比照 `docs/reports/W-G.9-322R0_CC側新窗交接文.md` 之 `§六-1`。
> 🔒 **本節所載之四項，逐項⛔ 留白；無者具名「無」。**
> 🔒 **態** ＝ `d909fb5517524bedbed9de2e0e0524e8fcb91b93`（本文落地前之 `HEAD`）。
> 🔒 **「列」之口徑** ＝ `split('\n')` 之長度（`恆常附款 w②⑤`）。

---

## 項 `1`　`W-G.9-322R0 §六-1` 項 `6` 所載**七項**以後：本窗**新遇**或**已失效**之環境坑

| # | 對應 | 事實（**本窗實測**） | 其對新窗之意義 |
|---|---|---|---|
| `1` | 🔴 **項 `6`-`1` 之「首次必擋」於本窗<u>未再現</u>** | 本窗**三次** `push` **皆首推即成**（`rc 0`·`W-G.9-322`→`a97d9e1`／`W-G.9-320`→`97449f8`／`W-G.9-323`→`d909fb5`），**⛔ 被 auto-mode 權限分類器擋一次**。 | 🛑 **⛔ 得據此推論「分類器已不擋」**——本窗所用之形為 `git push origin HEAD:refs/heads/wip/s1-endpart`（**明示 refspec·自 `claude/…` 分支直推主線 ref**），而 `322R0` 所載之被擋者為**「主線快轉」**且理由逐字 `Merge Without Review` ⇒ **二者⛔ 同一操作**。新窗仍須**預期被擋**並依停機款上呈·⛔ 加力、⛔ 改目標值、⛔ 另闢途徑 |
| `2` | 🟢 項 `6`-`2` **仍活** | `verify/tools/wg9237_heredoc_guard.py` 於本窗**實擋 `1` 次**（`W-G.9-320` 批·`bash` 命令含 `<<'PYEOF'`），其提示逐字含 `GB-143 補款二`。 | 一切腳本一律 `Write` 落檔後以**路徑**呼叫；`git commit` 一律 `-F <檔>` |
| `3` | 🟢 項 `6`-`3` **本窗⛔ 觸發** | 本窗一切 `subprocess` 皆用 **list 形**（`subprocess.run([...])`）·**⛔ `shell=True`** ⇒ 未遇 `cmd.exe` 之 POSIX 具不可得。 | 其坑**仍在**，只是本窗繞開；新窗若用管線形仍會踩 |
| `4` | 🆕 **新坑** | **`python -c` 未設 `PYTHONIOENCODING=utf-8` 時，印<u>全形標點</u>（非 emoji）亦觸 `cp950` 之 `UnicodeEncodeError`**——本窗實遇之字元為 **`［`（`U+FF3B`）**，錯誤逐字 `'cp950' codec can't encode character '\uff3b' in position 0`。 | 🛑 `常規五` 之受詞逐字為「**含 emoji 之工具**」⇒ **其射程未及全形標點**。其為**量測器紅**·⛔ 受詞紅；**一切 `python`／`python -c` 一律前置 `PYTHONIOENCODING=utf-8`** |
| `5` | 🆕 **新坑** | **對全倉逐檔 `git cat-file blob` 之迴圈（母體 ~`1900`〜`2600` 檔）逾 `120` s**，Bash 工具自動轉背景（本窗於 `W-G.9-323` 之 `S-0` 實遇）。 | 改以 **`git grep -c -F <pat> HEAD`** 一次取全倉；🛑 **須逐次檢 `rc`**——`0` 有命中／`1` 無命中／**其餘 ＝ 器紅**（⛔ 靜默回 `0`·`恆常附款 n②`） |
| `6` | 🆕 **新坑** | **`git check-ignore -v <檔…>` 於「全部皆未被忽略」時 `rc = 1`**——易誤讀為失敗。 | 須併跑**判別力[必命中]**（本窗用 `verify/out/got_XXXX.log` ⇒ `.gitignore:30`·`rc 0`）方得判；⛔ 以 `rc` 單值出艙 |
| `7` | 🆕 **本窗自捕**（`GB-132`／`GB-147` 族） | 本收工令之器 `z1_crossbatch.py` 之**對照乙[必為零]**得 **`1`**——其唯一命中係**該器自身之宣告列**（哨兵之字面寫在 `print` 之格式字串內，而其掃描母體含該器自身）。逐筆歸類後 ＝ **`0`**。 | 🛑 **哨兵須於執行期組出、⛔ 使其字面落入受掃之母體**；對照組非零時**一律先判量測器紅**（`常規四（七）補款二 二`），⛔ 逕判受詞紅 |

---

## 項 `2`　scratchpad 之腳本：新窗**須重建**者、其用途、及「**期望值寫死他批**」之逐支具名

🔒 **母體（實測）**：`.py` **`30`** 支 ＋ 非 `.py` 落檔 **`42`** 支，**皆⛔ 在倉樹內**
（其逐支清單見 `docs/reports/W-G.9-323R_CC交接文.md` `§七-3` 與 `docs/reports/W-G.9-320R_CC交接文.md` `§七-3`·**⛔ 重述**）。
🔒 **判準（機械·⛔ 憑記憶）**：所屬批由檔名前綴定（`s*` ＝ `W-G.9-322`／`g*` ＝ `-320`／`h*` ＝ `-323`）；
「寫死他批」＝ **他批之號或他批之期望值落於<u>期望值之位</u>**（`期`／`單載`／`==` 之右側／`assert` 之受詞）。

### `2-a`　**須重建**者（逐批一份·其形跨批可複製）

| 形 | 本窗之三份 | 用途 | 「寫死他批」之判 |
|---|---|---|---|
| `<x>0_gate.py` | `s0_gate.py`／`g0_gate.py`／`h0_gate.py` | `S-0` 開工閘：態錨逐格自倉重跑 ＋ 取號（`h0` 為**三框**：檔框／列框／子字串框） | 🟢 **無**——其所含之他批號皆在**受詞位**（對照甲／對照乙之受詞），⛔ 期望值位 |
| `<x>1_ingest.py` | `s1_ingest.py`／`g1_ingest.py`／`h1_ingest.py` | 工項零：原封入倉之逐位驗 ＋ `P-5` 自驗（含判別力[必不符]） | 🟢 **無** |
| `<x>2_extract.py` | `s2_extract.py`／`g2_extract.py`／`h2_extract.py` | payload 之**機械抽取**（```` ``` ```` 圍欄之**逐列索引**·⛔ 硬編列號） | 🟢 **無** |
| `<x>3_append.py` | `s3_append.py`／`g3_append.py`／`h3_append.py` | 末端追加 ＋ 三值機驗（前綴／增長／尾段逐位）；`h3` 另含 **`GB-132` 避讓自證** | 🟢 **無** |
| `<x>4_recount.py` | `s7_recount.py`／`g4_recount.py`／`h4_recount.py` | `§五-1` 基數之當場重算 | 🟡 `g4`／`h4` 之「單載 `28`」等係**印在標籤內之他批對照**、⛔ 判定式；`g4` 曾因**判欄欄序硬編**而二造皆零（已具名於 `W-G.9-320R §四`） |
| `<x>5_append7.py` | `s8_append7.py`／`g5_append7.py`／`h5_append7.py` | `§七` 之嚴格末端追加 | 🟢 **無** |

### `2-b`　**跨批沿用**者（本窗**⛔ 重建**·🔴 **二支皆帶寫死之期望**）

| 支 | 用途 | 🔴 寫死之期望（**逐處具名·`檔:列`**） | 新窗之處置 |
|---|---|---|---|
| `s4_kuan.py` | 恆常附款**款數**之期初期末重算（款字母／款次／新增／受修訂） | **`:47`** 逐字 `"🟢" if mod == list("beiklnuw") else "🔴 具名相異"` ——`beiklnuw` 係 **`W-G.9-322` 批**之受修訂款字母 | 🛑 **本窗二度踩之**（`W-G.9-320` 得 `l m`／`W-G.9-323` 得 `k m w`·**皆印 `🔴` 而其值為正** ⇒ **標籤紅·⛔ 受詞紅**）。新窗**須先改其期望值為<u>由單當場解析</u>**、⛔ 沿用 |
| `s5_closegate.py` | 收工閘 `1`〜`7`（刪除欄／生產碼／`CR`／`append-only` 三值／自限／母體） | **`:11`** `BASE = sys.argv[1] if len(sys.argv) > 1 else "c08bd783…"`（**預設 ＝ `W-G.9-322` 批之基座**·**省引數即靜默量錯批**）／**`:12`** `ORDER_BASE = "4c5a143a…"`（**寫死 `W-G.9-322` 單所載之開工態**）／**`:97`**「期 `298`」／**`:98`**「期 `25`」「期 `20`」 | 🛑 **一律顯式傳 `BASE`**（本窗三批皆傳）；`ORDER_BASE` 與三個「期」須**逐批改**。`:56` 之 `OLD = "40375702…"` 係**判別力[必非零]之造**·⛔ 期望值 |

### `2-c`　一次性者（**新窗⛔ 須重建**·其受詞已隨該批結案）

`s0b_numbering.py`（宣告框 `D1`／`D2`／`D3`）／`s0c_k9frames.py`（`K-9` 節題框之三讀法）／
`s3b_frames_wt.py`・`g3b_frames.py`（`v②` 二框自驗）／`s6_baselines.py`（`baselines` 之口徑二造對拍）／
`g0b_pop.py`（取號母體之別）／`memline.py`・`memline2.py`・`memline3.py`（記憶檔之更新）／
`z0_inventory.py`・`z1_crossbatch.py`（**本收工令之器**）。
🟢 **其「寫死他批」之判**：皆為**受詞位**之他批號，⛔ 期望值位——惟 `z1_crossbatch.py` 有項 `1`-`7` 之自指瑕。

---

## 項 `3`　本窗之 worktree／分支／`left-right`

| 項 | 值（**當場實測**） |
|---|---|
| worktree | `.claude/worktrees/wg9-322-order-docs-47e0a6` |
| 分支 | `claude/wg9-322-order-docs-47e0a6` |
| `HEAD` | `d909fb5517524bedbed9de2e0e0524e8fcb91b93` |
| `left-right` vs `origin/wip/s1-endpart` | **`0 0`** |
| `core.autocrlf`（有效值） | `false` ⇒ 工作區 bytes ＝ blob bytes |
| `CLAUDE.md` 列(split) | **`2967`** |

🛑 **新窗若亦於 worktree，其路徑與分支必相異**——凡器之 `branch` 格因此相異者，係 **checkout 之別·⛔ 態之別**
（`322R0 §六-1` 項 `6`-`5` 同旨）。

---

## 項 `4`　其他**僅存於本窗記憶**之事項

| # | 事實 | 其對新窗之意義 |
|---|---|---|
| `1` | **本窗辦<u>三批</u>**：`W-G.9-322`（輕）／`W-G.9-320`（中）／`W-G.9-323`（中）。基座 ＝ `c08bd78`（＝舊窗最末一筆）→ `d909fb5`；**本窗 commit 總數 ＝ `22`**（`322` 批 `7`＝工項 `6`＋`§三` `1`／`320` 批 `7`／`323` 批 `8`）·**刪除欄非 `0` 之 `commit` ＝ `0`**。 | 倉內各批報告**分載**，惟「**三批同窗**」此一事實⛔ 載於倉。新窗之基座 ＝ `d909fb5` |
| `2` | 🔑 **單之遞交途徑**：KL 將單檔置於**主 checkout** 之 `C:\Users\admin\Desktop\land-readjustment-trial\docs\orders\<單名>.md`（**⛔ 在 worktree 內**）；CC 以**絕對路徑**自該處讀入並**二進位複製**入 worktree 之同名落點，再逐位對拍。 | 倉內⛔ 載此途徑。新窗**⛔ 於 worktree 內找單**——其於工作區為**未追蹤**且**僅存於主 checkout** |
| `3` | KL 告知本窗 context 達 **`68%`**（本收工令）；前窗之換窗點為 **`86%`**。 | 倉內⛔ 載。新窗之容量係全新；惟**換窗本身⛔ 使任何未入倉之物移轉**——項 `1`〜`4` 即其清單 |
| `4` | 🔑 **`probe_WG9323_gprime.py` 對 `probe_WG9320_stageb.py` 之依賴為<u>雙重</u>**：① `import` 其 `theo`／`real`／`build_vectors`／`ENV`／`CASES`／`NEED`；② **其自證閘 `2` 之對照來源為落檔 `verify/out/WG9320_stageb.md`**（逐字抽取）。 | ⇒ **動該器一字<u>或</u>重生該落檔**，將使 `probe_WG9323_gprime.py` 之自證閘 `2` **全格轉紅或轉「對照格缺」** ⇒ 其 `rc` 轉 **`2`**（loud 拒測）。倉內載「⛔ 動一字」之令與其 blob，惟**此一後果**僅本窗知 |
| `5` | 本窗**⛔ 遇**任何 harness 權限分類器之阻擋（見項 `1`-`1`）；**⛔ 遇**任何 `verify/baselines` 之寫入需求；**⛔ 跑**過 `verify/run_all.py`（三批皆⛔ 令之）。 | 新窗若首次遇 `run_all`，其耗時約 `15`–`20` 分（`CLAUDE.md` 已載）⇒ **背景跑＋等通知**·⛔ 與他長跑併跑 |
| `6` | **本窗⛔ 持有** `W-G.9-324` 或其後任何單之逐字；`W-G.9-309`（`GB-170` 修批）**仍保留·⛔ 取**。 | 次可取之號 ＝ **`W-G.9-324`**（`W-G.9-323R §五-3` 已載·此處⛔ 重述其算式） |
| `7` | **無**——除上列六項外，本窗⛔ 另有僅存於記憶而未入倉之事項。 | — |

---

🔒 **本節之四項逐項已填·⛔ 留白**（`恆常附款 d`：受詞及於倉外之物者，須同格載其不可實查之性質）。
🛑 **本文落地後，舊窗即得換窗。**
