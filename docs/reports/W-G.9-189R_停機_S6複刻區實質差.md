# 🛑 `W-G.9-189R`　**停機上呈**：`S-6` 複刻區同步閘觸發（`X-1`）

**出文窗**：CC　**施工單**：`W-G.9-189`（幾何本體批第二批·`K-9-17` 遞補）　**基座**：`761a4fa`
**性質**：🔒 **停機報告**·⛔ **零生產碼**·⛔ **未鑄任何號**·⛔ **未寫遞補一字**
**取號**：`常規四 三`（單號 ＋ 後綴 `R`）⇒ `W-G.9-189R`。
⚠️ **檔名**：收工閘 `9` 所令之 `W-G.9-189R_幾何本體批第二批.md` 係**完成批**之名，本批⛔ 未完成
⇒ 依 `常規三`（檔名·⛔ 無土地後果）取本倉停機報告之既有體例
（`W-G.9-145R_停機_常規編號撞號.md`）命名；**列 `§五-4` 呈裁**。

---

## `§零`　開工閘之辦理

| 閘 | 結果 |
|---|---|
| **`S-0`** 單之完整性 | ✅ 取檔內**最末**一個 `SELF_SHA256`（索引 `[10, 178]` ⇒ 取 `178`·**陷阱 6**）；全檔 `11644` B／`179` 列，扣除自該行起 ＝ `11566` B ⇒ 實算 `5df961612b26c040…` **逐位相符** |
| **`S-1`** 期初錨（⛔ 未照抄） | ✅ **九檔全符**（見 `§零-1`）；`app.py` 檔尾**無 `LF`**、末位元組 ＝ `)`、`wc -l` 得 `23411` —— 三者**皆與單載相符** |
| **`S-2`** 倉態 | ✅ `ls-remote` ＝ `761a4fad135941b2571386ece8487803eefdb09e`；`is-ancestor(761a4fa→HEAD)` `rc=0`；`left-right` ＝ `0 0` |
| **`S-3`** 拋棄式 clone | ✅ `C:\w189a`（clone）／`C:\w189x`（自檢器·**置於 clone 之外** ⇒ 母體自然自扣）；刪除之確認見 `§七` |
| **`S-4`** 禁改清單 | ✅ 已讀 `§五`；本批**⛔ 未動其中任一處**（`app.py` 全檔逐位未改） |
| **`S-5`** 基準量 | 🛑 **⛔ 未辦**——`S-6` 先行觸發停機，依 `§三 X-8`「⛔ 不先辦後呈」⇒ 停。**理由**：基準量繫於基座，若 `S-6` 之處置變動碼面，該基準須重取 ⇒ 先取反成廢量 |
| **🔑 `S-6`** 複刻區同步 | 🛑 **觸發**（實質差 **`20`** 處）⇒ **`X-1` 停機** |

### `§零-1`　`S-1` 九檔（框 ＝ blob 位元組／`splitlines()`／`sha256` 前 `16`·CRLF 皆 `0`）

`app.py` `1378708`／`23412`／`2be36e4c88b6e129` ✅　　`CLAUDE.md` `81200`／`903`／`3df01894ba38ccc5` ✅
`verify/stepg_pipeline.py` `96261`／`1454`／`0260273350021311` ✅　　自誤登記表 `402254`／`4347`／`82084b330ff89b4d` ✅
`W-G.4` 阻塞表 `529494`／`3238`／`499ce534e43f332e` ✅　　驗證裁定表 `333071`／`4258`／`c5b812d388040de3` ✅
`K-6` 正典 `258295`／`3632`／`6a5cada2901a10e8` ✅　　規格書 `42908`／`574`／`dfd46da5825c645e` ✅
`W-G.9-187R` 報告 `44424`／`610`／`f8937e374e79d6db` ✅

---

## `§一`　停機款之**逐字引錄**（`GB-121` 施行式·⛔ 非摘要）

> **`X-1`**　`S-6` 出現**實質差**。

> **`X-8`**　`deletions` 出現於**基座檔**（`app.py`／`CLAUDE.md`／`stepg_pipeline.py`／各登記表）而非同批新增之檔。

（`§三` 停機款前言逐字：「任一觸發 ⇒ 🛑 停機上呈，⛔ 不先辦後呈」。）

🛑 **`X-1` 觸發** ⇒ 依 `S-6 3.` 逐字「**若出現任一「實質差」⇒ 停機上呈，⛔ 不得先寫遞補**」——
本報告即該停機；**`N-1`／`N-2`／`N-3` 一字未辦**。

---

## `§二`　`S-6` 之框與**量測器自證**

### `§二-1`　框（逐字宣告·`S-6` 末段所令「出艙之數一律宣告框」）

**母體** ＝ 基座 `761a4fa` 之 `app.py`（`23412` 列）與 `verify/stepg_pipeline.py`（`1454` 列）之**四組對應區**。
🔒 **界定以 AST 之 `FunctionDef`／`For` 節點**，⛔ **非**自陳之舊行號「約 `14650-15450`」
（`stepg_pipeline.py:35`）——該錨係 `app.py` 尚為 `~15450` 列時所記，今已 `23412` 列（`行號衛生`）。

| 區 | app.py | stepg_pipeline.py |
|---|---|---|
| `_build_g_row` | `:20580`〜`:20621`（`42` 列） | `:346`〜`:385`（`40` 列） |
| `_solve_one` | `:20623`〜`:20639`（`17` 列） | `:387`〜`:399`（`13` 列） |
| `_advance_block_with_split` | `:20951`〜`:21231`（`281` 列） | `:593`〜`:798`（`206` 列） |
| 逐街廓迴圈 | `:20642`〜`:21521`（`880` 列） | `:402`〜`:1423`（`1022` 列） |

**正規化**（＝單所定義之「形式差」之機械實現）：①縮排（AST 天然不變）②註解／docstring（逐層剝除）
③取用形（`ns["X"]`→`X`；`st.session_state`→`ss`）。🔒 **其餘一切⛔ 不正規化**。

### `§二-2`　🩸 **量測器之首版缺口（CC 自捕）**——停機前先自證非紅（`W-G.9-14` 修法 ②）

首版於**敘述層**⛔ 未剝 docstring、⛔ 未正規化 `st.session_state`、對複合敘述⛔ 未下鑽
⇒ **偽陽**（把 docstring、取用形、及「僅內部有差之整個 `for`」全記為實質差）。
🔒 **修正後**方據以停機。**判別力對照**：注入 `_build_g_row` 之首個 `round(` → `int(`
⇒ 源碼相同 ＝ `False`、AST 仍判全等 ＝ `False` ⇒ **比對器非恆綠**，其「零分歧」之判具意義。

🔒 **本檔即 `verify/` 內該自動閘之<u>首次落地</u>**：`verify/probes/probe_WG9189_s6_replica.py`
（`rc=1` ＝ 有實質差）＋ `verify/out/probe_WG9189_S6_replica.log`。
——單 `§零 S-6` 逐字載「發單側已現查：`verify/` 內**⛔ 無任何比對此二區之自動閘**」，本批補之。

---

## `§三`　🔴 分歧清單（實質差 **`20`** 處·`st.*` 略去 **`0`** 處）

### `§三-1`　二區**零分歧**（正規化後逐位相同）

| 區 | dump | 判 |
|---|---|---|
| **`_build_g_row`** | `4353` B vs `4353` B | ✅ **零分歧** |
| **`_solve_one`** | `1293` B vs `1293` B | ✅ **零分歧** |

⇒ 🔒 單 `S-6 2.` 所令之第二項對拍（`:345` 起之二份）**完全同步**。

### `§三-2`　二區有分歧

| 區 | dump | 實質差 |
|---|---|---|
| `_advance_block_with_split` | `23821` B vs `20641` B | **`3`** |
| 逐街廓迴圈 | `74569` B vs `94370` B | **`17`** |

### `§三-3`　🔑 **`20` 處之分類**（CC 之判·⛔ 非單所定義之二分·列 `§五-1` 呈裁）

單之二分（形式差 ＝ 僅縮排／僅註解／僅 `ns` 取用形；其餘皆實質差）**過窄**，⛔ 無法表達實測所見。
CC 依成因**再分四類**（逐項可回查 log）：

**（甲）已登記之邏輯分歧 —— `1` 處 🔴 本批之真風險**

`_advance_block_with_split` 之 `_W_prev_*` **初值**：

- `app.py:20990-20991` 逐字：`_W_prev_left = (_left_buffer_S * _cos_dn) if _has_left_corner else 0.0`（＋ `_right`）
- `verify/stepg_pipeline.py:628-629` 逐字：`_W_prev_left = 0.0`（＋ `_right`），並於 `:837-838` 走 `_mp_base_W0`

🔒 **獨立倉內錨**：`CLAUDE.md:757` 逐字登記之**族②**——「`app.py` 之 `_W_prev_*` 初值（`:20500-20501`）
與 `_select_pool_slot` 之 `'b'`（`:20775/20777`）仍取**舊式** `buffer_S · cos_dn`，而
`verify/stepg_pipeline.py`（`:628-629`／`:838-839`）已改用 `_mp_base_W0`」。
⚠️ 其所載 `app.py:20500-20501` 係**本波 `W-G.9-187R` 之前**之行號；`-187R` 於 `:11737` 插入 `490` 列
⇒ **今為 `:20990-20991`**（`20500 + 490`·**位移量逐位吻合** ⇒ 此亦為該識別之佐證）。
🔒 **性質**（`CLAUDE.md` 逐字）：「⛔ **非待裁項、⛔ 非失效**，係**已裁而未遷移**」；受詞歸 **`W-G.9-5`**。
🔒 **已實測之後果**：`R2` 池帶 2 寬差 `0.1171`（app `32.9640` vs shim `33.0811`）。

**（乙）宿主介面差 —— app 側 UI／session vs harness 側純函式**

`st.error(...)＋st.stop()` vs `raise RuntimeError(...)`（**同一閘·出艙管道不同**·log 第 `17` 項）／
`ss['f3_wd2_pool_diag'][blk]=…` vs 區域 `pool_diag[blk]=…`（第 `20` 項）／
app 獨有之 `ss.setdefault('f3_forced_offset_diag',…)` 診斷寫回（第 `11` 項）／
`_corner_min_table = ss.get(…)` ＋ `next(…)` vs harness 預建索引 `rows_by_lbl.get(…)`（第 `10` 項）／
深度與 alloc_dir 取值之展開形 vs 一句 or-鏈（第 `4`／`6`／`7`／`8` 項）／
app 獨有之 `if _commit: st.info(…)`（右迴圈內·見 `§四`）。

**（丙）單側獨有之守備／階段 —— 另一側**結構上無其對應物**

harness 獨有：`_rw_stage1_only` ＋ `_IFACE_TOL`／`_PAR_TOL`（第 `18` 項）／telescoping 與界面閘之逐側迴圈
（第 `19` 項）／`_Wf_stage1`／`_stage2_ids`（第 `13` 項）／`if not _degenerate_order:`（第 `9` 項）／
`offset_geoms = []` 之前置（第 `16` 項）／`_proj_pop_assert_seq('stepg:v2_caller', …)`（第 `8` 項內）。
app 獨有：`allocation_dir_block = None` 之初始化（第 `5` 項）／`left_cum_S`／`right_cum_S` 之取出
＋ `if left_cum_S + right_cum_S > S_block_max + 0.5:` 之上限檢查（第 `14`／`15` 項）。

**（丁）呼叫序差 —— `1` 處**

第 `12` 項：`def _advance_block_with_split(…)` 與其前一 `for _side_nm_w2, …` 迴圈之**先後對調**
（app：`def` 在前／stepg：`for` 在前）。⚠️ `def` 係綁定、非執行 ⇒ **語意上無後果**，惟
**依單之字面確屬「呼叫序之差異」** ⇒ CC ⛔ 不自行豁免，照列。

---

## `§四`　🔑 **遞補所在之二迴圈**之下鑽（本批最具決定性之量）

`K-9-17` 遞補之落點 ＝ `for entry in left_group:`／`for entry in right_group:`。單獨下鑽：

| 迴圈 | app | stepg | 正規化後 body 句數 | 分歧 |
|---|---|---|---|---|
| `left_group` | `:21058`〜`:21120` | `:638`〜`:697` | **`28` / `28`** | **`1` 處**（敘述 `#14`） |
| `right_group` | `:21141`〜`:21224` | `:712`〜`:788` | **`29` / `29`** | **`2` 處**（敘述 `#14`／`#15`） |

🔒 **`#14`（左）／`#15`（右）之逐字**（正規化後 `unparse`）：

- **app**：`if _has_left_corner:` → `_W_prev_left = float(res.get('W_far', _W_prev_left))`
- **stepg**：`if _has_left_corner:` → `if not _W0_left_set:` → `_W0_left = float(res.get('W_near', 0.0))`／
  `_W0_left_set = True`／`_Wfirst_left = float(res.get('W', 0.0))` → **同一句** `_W_prev_left = …`

⇒ 🔒 **stepg 多出 `_W0_*`／`_Wfirst_*` 之捕獲**（供其獨有之 telescoping 閘用·＝**類（丙）**），
**app 之該句與 stepg 逐位相同** ⇒ 此處**⛔ 非邏輯歧異**。

- `#14`（右）之差 ＝ app 多 `if _commit: st.info(f'ℹ️ 街廓 … 右側起點數值微修 …')` **一句**
  ⇒ **`st.*` no-op**（`stepg_pipeline.py:401` 自陳「`st.*` 於 headless 為 fake no-op 故略」）＝**類（乙）**。

🔑 **⇒ 本批之關鍵結論**：**遞補所在之二迴圈，其 `28`／`29` 句中除上開者外<u>逐句相同</u>**；
真正會污染遞補基準者**唯有類（甲）之族②**（`_W_prev_*` 初值），且其在**迴圈之外**（`_advance_block_with_split` 頂層）。
⇒ 單 `S-6 3.` 所慮之「遞補整個落在此區，在已歪之基準上疊加即是假綠」**成立**，
惟其**歪之來源已收斂為單一具名項**，⛔ 非瀰漫性漂移。

---

## `§五`　🛑 擬辦之意見（`X-8` 令於停機回報中併陳）

### `§五-1`　建議：`S-6` 之判準由「二分」改為「**四分 ＋ 只擋類（甲）**」

🔒 **理由**：`app.py` 之區服務 Streamlit UI、`stepg_pipeline.py` 之區服務 headless harness
——二者**宿主不同**，類（乙）（丙）之差係**其存在之理由**，⛔ 非漂移。
以現行二分判之，「複刻區完全同步」是**結構上不可達之目標** ⇒ 該閘將**永遠紅**，
而永遠紅之閘與永遠綠之閘**同樣無判別力**（`GB-125`／`GB-126` 之同族）。

🛑 **建議之判準**：
> **實質差之受詞 ＝ 類（甲）**（**同一量於二側取不同定義／不同式**）⇒ 停機。
> 類（乙）（丙）（丁）⇒ **具名登記於清單，期末重驗其未增減**（＝單 `S-6 4.` 已有之機制）。

### `§五-2`　建議：本批之前置處置 ＝ **先辦族②之切換**（＝ `W-G.9-5`）

🔒 類（甲）僅 `1` 項，且**已裁而未遷移**（`CLAUDE.md:757` 逐字·`常規三` 之【查有】⇒ ⛔ 不上呈 KL）。
⇒ 建議次單令 CC **先將 `app.py:20990-20991` 之 `_W_prev_*` 初值切為新式**（併 `_select_pool_slot` 之 `'b'`），
其驗證含 `CLAUDE.md:757` 所載之**預測**「切換後 `R2` 之差（`0.1171`）是否消失」（**預測·⛔ 非結論**）。
🛑 ⚠️ **該切換<u>會改變配地結果</u>** ⇒ 須自成一批、自帶守恆式主閘，**⛔ 不宜與遞補同批**
（否則二因混一，`[T4-CONS]` 轉紅時無從歸因）。

### `§五-3`　`CLAUDE.md:757` 之行號已腐（⛔ 本批未改·僅具名）

其所載 `app.py:20500-20501` 因 `W-G.9-187R` 之 `+490` 插入而失準，今為 `:20990-20991`。
🔒 依 `行號衛生` 第 `5` 款「本規只能止血、不能回溯」⇒ **⛔ 不追改**；建議次單以**末端追加**補一可 grep 之字樣錨。

### `§五-4`　本報告之檔名（`常規三`·⛔ 無土地後果）

收工閘 `9` 令名為 `W-G.9-189R_幾何本體批第二批.md`，惟該名指**完成批**。CC 取
`W-G.9-189R_停機_S6複刻區實質差.md`（體例同 `W-G.9-145R_停機_常規編號撞號.md`）。**請核可或改名。**

---

## `§六`　⛔ 本批未辦（逐項具名）

⛔ **未寫遞補一字**（`N-1`）／⛔ 未接生產路徑呼叫點（`N-2`·`GB-127` **仍未解除**）／
⛔ 未產 `[T4-CONS]`（`N-3`）／⛔ 未辦 `N-4` 四項（`自誤 224`／`225` 之鑄、`-187R` 報告之框宣告補正、
`wf_f0.py:9` 偽述之更正）——**四項皆繫於本批之續行**，停機故未辦。
⛔ 未取 `S-5` 基準量（理由見 `§零`）。⛔ 未動 `app.py`／`CLAUDE.md`／`stepg_pipeline.py`／各登記表**一字**。
⛔ 未鑄任何號。⛔ 未跑 `run_all`。⛔ 未請 KL。

---

## `§七`　異動清單

| 檔 | `+` | `−` | 類 |
|---|---|---|---|
| `verify/probes/probe_WG9189_s6_replica.py` | 新檔 | — | `S-6` 之自動閘（`verify/` 內首次落地） |
| `verify/out/probe_WG9189_S6_replica.log` | 新檔 | — | `S-6` 之出艙證據 |
| `docs/reports/W-G.9-189R_停機_S6複刻區實質差.md` | 新檔 | — | 本報告 |

🔒 **基座檔 `deletions` ＝ `0`**（三者皆新檔·`X-8` 未觸）。
🔒 絕對路徑機檢：本批新增檔命中 **`0`**（探針之 `REPO` 自 `__file__` 推得）。
