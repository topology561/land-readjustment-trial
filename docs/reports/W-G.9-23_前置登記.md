# 【W-G.9-23 前置登記】僅修「非 `forced` 之 B 選槽」之影響估算（⛔ 只估·不修）

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`e82c90e`（§0 現查所得·⛔ 非施工單所引）
**字樣錨（倉規 A）**：`【W-G.9-23 前置登記】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行
　🔒 判別力自檢：改用寬字樣 `W-G.9-23` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> 🔒 **本檔為 commit A：⛔ 零生產碼、⛔ 零碼變更、⛔ 零 `J1`–`J6` 之量測。**
>
> # 🔴 本批之受詞（⛔ 窄·不得擴張）
>
> **只估一件事**：把 **B 選槽**之 `_b_L0`／`_b_R0` 改為目標值（非 `forced` ⇒ `0`），
> **其餘三處全部不動**，會怎樣。
> ⛔ 不估 `forced` 三格之修法（卡表示法）／⛔ 不估 A 之修法／⛔ 不估族①。
>
> 🔴 **`J2`／`J3` 破了比不破更有價值**——⛔ 不得為使其成立而調整任何參數。

---

## §0 登時已現查（⛔ 逐項附母體與判準）

| # | 指令／母體 | 判準 | 結果 | 對照組 |
|---|---|---|---|---|
| ① | `git rev-parse --short HEAD` | — | **`e82c90e`** | — |
| ② | `git grep -l "W-G.9-23"`（母體＝git index） | 檔數 | **0**（＝期望） | `W-G.9-22` ⇒ **8 檔** |
| ③ | `grep -n "_left_buffer_S = 0.0"`（母體＝該二檔） | 含該字樣之行 | **4 行**：`app.py` **2**／`stepg` **2** | — |
| ④ | `grep -n "_b_L0 = \|_b_R0 = "`（母體＝`stepg`） | 同上 | **2 行**（`_mp_base_W0(corner_pt, _left_buffer_S, …)`／`(_end_pt_o, _right_buffer_S, …)`） | — |
| ⑤ | `grep -n "left_cum_S = float"`（母體＝該二檔） | 同上 | **2 行**（`app.py` 1／`stepg` 1·皆 `float(_left_buffer_S)`） | — |

🔧 **③ 之就地更正**：施工單期望「兩檔**各有初值** `0.0`」；現查為**每檔各 2 行**
——**初值 1 行 ＋ `except` 退路 1 行**（`no-silent-fallback` 之 loud 退 0）。
⇒ ⛔ **不得說「各有一個初值」**；正確表述為「**每檔各 2 處賦 `0.0`：初值與 `except` 退路**」。

---

## §1 🔒 接縫之現查（`Q-2`／`Q-3` 之可量性前提·⛔ 由構造證）

| 端 | 來源（字樣錨） |
|---|---|
| **理論@k\*** | `_t_star.get('ΣRw_L')`，其中 `_t_star` 取自 `_slot_res['table']`，而 `_slot_res = _select_pool_slot(…)` |
| **實跑(階段1)** | `_rw_real_wd2('left')`（推進迴圈之產物·種子 `_W_prev_left = 0.0`） |

⇒ 🔒 **包裝 `ns["_select_pool_slot"]` 即可只動理論側**，⛔ **不動實跑側**
——正是本批所要之單變因。**`_b_L0` 為 `run_step_g` 之區域變數**（`_mp_base_W0` 亦為巢狀函式）
⇒ ⛔ **不可直接包裝**；**唯一合法接縫 ＝ `_select_pool_slot` 之入參 `left_side['b']`／`right_side['b']`**。

### 一-1 🔒 標籤與 `buf` 生產實值之取得（⛔ 不重建）

`_select_pool_slot` 之簽章**不含街廓標籤** ⇒ 以 **frame 內省**自呼叫端讀
`blk_label`／`_left_buffer_S`／`_right_buffer_S`（**純觀測·⛔ 不改任何值**）。
🔒 **自我驗證閘**：所讀之 `blk_label` 須落在**已知街廓標籤集**內，且**逐次呼叫可變**；
⛔ 閘不過即**不得據以下任何結論**（`CLAUDE.md` 常設）。

### 一-2 🔒 `Q-1` 之**構造證**（⛔ 非只靠量測）

`_left_buffer_S` 於 `stepg` 之**全部賦值點 ＝ 2**（§0-③）：
① `= 0.0`（初值·在 `if _fo_left:` **之外**）② `= _corner_buffer_S(…)`（**在 `if _fo_left:` 之內**）
＋ `except` 之 `= 0.0`。
⇒ **非 `forced` ⇒ 該變數<u>恆為初值 `0.0`</u>**（碼面結構所定，⛔ 非經驗）。
🔒 量測僅作**佐證**（射程限於實際跑到之街廓）。

---

## §2 `J1`–`J6` 可證偽預測（⛔ 於任何量測之前寫定）

| # | 預測 | 證偽 ⇒ 觸發 | **判別力自檢之作法** |
|---|---|---|---|
| **J1** | 非 `forced` 13 格之 `buf` 生產實值**恆 0** | 非 0 ⇒ 破 ⇒ `R-3` | 構造證（§一-2）＋量測佐證；另證 `forced` 格之 `buf` **非 0** ⇒ 該判法非恆得 0 |
| **J2** | 僅改 `_b_L0`／`_b_R0`，`R1` 之結構閘**轉為通過** | 仍破 ⇒ 破 ⇒ `R-2`（⚠️ **合法·重要**） | 併記**改前 Δ** 與**改後 Δ**；⛔ 只記「過/不過」不足 |
| **J3** | `k*` **不改變**（⇒ 配地位置不動） | 改變 ⇒ 破 ⇒ `R-1`（⚠️ **合法·重要**） | 同一次呼叫**同時**算改前與改後 `k*`（⛔ 兩次跑之比對不算） |
| **J4** | 改後 `run_step_g` **跑得比改前遠** | 不然 ⇒ 破，具名 | **逐街廓正面記錄「跑到哪裡、在哪裡停」**，⛔ 不只記有無跑完 |
| **J5** | 丁組之預期轉紅清單**可逐項現查判定** | 有判不出者 ⇒ **具名為無從判定**，⛔ 不猜 | ⛔ **不採信施工單所列舉之三項**，自行現查母體 |
| **J6** | 本批**零判定層變動** | 有差 ⇒ 破 ⇒ `R-5` | 同式對 `docs/reports/` 須**非空** ⇒ 證該式非恆空 |

🔴 **`J3` 之期望為「不改變」——期望值為<u>否定</u>者尤易假成立**（考古 76）
⇒ 須證「**若 `k*` 會變，該判法抓得到**」：以**必然改變**之替代值（如 `b := 1e6`）試，須判為**改變**。

---

## §3 停機條件

| 碼 | 條件 | 性質 |
|---|---|---|
| **R-1** | `Q-3` 顯示 `k*` 改變 ⇒ 🛑 上呈（**配地位置會動**） | 域邊界 |
| **R-2** | `Q-2` 顯示結構閘**仍不過** ⇒ 🛑 上呈（**該閘另有第二個成因**） | 域邊界 |
| **R-3** | `Q-1` 為偽 ⇒ 停機（**本批之拆分前提不成立**） | 紅線 |
| **R-4** | 只讀包裝**未還原**（`numstat` 非空）⇒ 停機 | 紅線 |
| **R-5** | `run_verification.results` 之 PASS/FAIL 層改變 ⇒ 停機 | 紅線 |

⚠️ **`R-1`／`R-2` 之閾為<u>布林</u>**（改變與否／過與否）⇒ ⛔ 無單位問題；
而**表中之量**分屬**公尺／百分點／平方公尺**三類 ⇒ 🔒 **三者分欄、⛔ 不比大小**（驗收 #3）。

---

## §4 ⛔ 明確未授權

1. ⛔ **零生產碼**——一律**只讀包裝**，**用畢還原並以獨立指令驗**（`numstat` 空輸出）。
2. ⛔ **不改 `forced` 三格之任何一處**、⛔ 不改 **A**、⛔ 不改**族①**、⛔ 不解表示法。
3. ⛔ 不改任何閘或容差、⛔ **不重凍任何凍存物**。
4. ⛔ 不動 `run_verification.py`／`wv_reconcile.py`／`baselines/`／既有夾具／三支複本／
   `docs/rulings/`／`CLAUDE.md`。⛔ 文件刪除欄一律 **0**。
5. ⛔ **不實施**丁組之任何一項（**只列**）。

---

## §5 🔒 本批自我加課（考古 60／65／67／68／69／71／76／77 逐條）

| 考古 | 本批如何套用 |
|---|---|
| **60** | 否定性宣稱**正面列舉受檢檔**，⛔ 不寫「倉內」 |
| **65** | 凡出艙一個計數，同時寫出**母體 ＋ 判準** |
| **67** | **公尺／百分點／平方公尺三者分欄**，⛔ 不混列、不比大小 |
| **68** | `buf` 用**生產實值**（frame 內省）＋**構造證**，⛔ 不用假設值 |
| **69** | 凡掛閘，**兩端至少一端來自受詞之外**（`forced` 集合以 `W-G.9-19` `D1` 為外部錨） |
| **71** | 「**我算不出**」與「**判定為偽**」⛔ 不共用出艙碼；**地板歧義**（`R(負)=0`）分開出艙 |
| **76** | ⛔ 不建套套邏輯之閘；`J3` 附「必然改變之替代值」自檢 |
| **77** | 🔴 **受詞取錯一層**——解析器**必設期望值閘**；引裁定為某數之依據須**指出哪一句指名了它** |

🔒 **本批新加**（承上批之教訓·驗收 #15）：**文件插入之錨須為<u>行尾或整行</u>**
——⛔ 不得以**行中片語**為錨（上批曾據此剖斷 `GB` 表之一列）；**每次寫入後以獨立指令驗落地**
（含**逐列淨管線數**）。

---

## §6 commit 分立

- **A** ＝ 本檔（⛔ 零碼、零量測）
- **B** ＝ 甲乙丙丁四組之量測（⛔ 零文件結論）
- **C** ＝ 報告 ＋ **丁組清單** ＋ `GB` 登記 ＋ **呈 KL 白話稿** ＋ 考古節（若有）

🔴 **呈 KL 之稿須寫明**：本批**只涵蓋 13 格（非 `forced`）**；
**`forced` 三格仍卡表示法、⛔ 不在此數內。**

⚠️ **依倉規（不可變文件不記可變值），本檔 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本檔之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本檔凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。
