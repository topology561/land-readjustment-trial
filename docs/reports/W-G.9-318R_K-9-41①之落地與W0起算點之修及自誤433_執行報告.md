# `W-G.9-318R`　執行報告：`K-9-41 ①` 之落地（起算點之二分）／`_mp_base_W0` 區塊之上移／`自誤 433`

> **態**｜開工 ＝ `40375702378af4cefd2d11515fdf3f98c0791580`（`ls-remote` 實查·全 `40` 碼·與單載相符）。
> **分級** ＝ **重**（含生產碼）。🛑 **本批⛔ `push`**——`c3`〜`c7` 五筆生產碼 `commit` 須 **KL 逐筆放行**，
> 放行前主分支 `wip/s1-endpart` **原地不動**。⛔ 開新分支（`refs/heads/verify/*` 維持 `20` 支）。
> 🛑 **`常規九`**：本報告所載之 `commit` **皆未 push** ⇒ ⛔ 得記為倉態；接手者之開工第一動作須驗
> `git ls-remote origin refs/heads/wip/s1-endpart`——若 ＝ `4037570…` 則本批**不在 `origin`**。

---

## `§零`　本批之 `commit` 串（**皆未 push**）

| # | `commit` | 受詞 | `numstat`（新增／刪除） |
|---|---|---|---|
| `c1` | `0ee3c4a2a95911ed9e38d2afbd48f9ccb5d3762b` | 工項零：本單原封入倉 | `416` ／ **`0`** |
| `c2` | `596ea3f3eb47739afb912fd413f6e32c79a21d33` | 工項一：態乙唯讀驅動（器 ＋ log） | `537` ／ **`0`** |
| **`c3`** | `7f43924c53f5c4f8b98bd9a48f5ffeff3506e0a0` | `app.py` `_mp_base_W0` 區塊上移 | `36` ／ **`36`**（單所授之位移） |
| **`c4`** | `7c346b19120744242d964c8c178d5e81ff6c663a` | `stepg` 同構上移 | `30` ／ **`30`**（單所授之位移） |
| **`c5`** | `7c07a360c9ee00bcc4b61982c287fa774c23785a` | `solve_G_binary` 新增 `w0_start` | `15` ／ **`0`** |
| **`c6`** | `6cc51e6183c534d4bc410f544082a2ae38a1ec99` | 薄殼直通 | `10` ／ **`0`** |
| **`c7`** | `b1141a3f95e6aff7f5646882e35f234e2f555198` | 六呼叫端傳之 | `6` ／ **`0`** |
| `c8` | `fd02962001a5be9fdc101a4185d4e0b12459511c` | 工項三：二態對拍（證據 log） | `4999` ／ **`0`** |
| `c9` | `f346f0c4e28b807dde91f82265d0582008033abf` | 工項四：`自誤 433` ＋ `GB` 簿末端追加 | `112` ／ **`0`** |

🛑 **粗體五筆（`c3`〜`c7`）＝ 生產碼 `commit`**，其 `push` 須 **KL 逐筆放行**。

---

## `§一`　開工閘（`14` 項·逐閘出艙）

| # | 閘 | 實得 | 判 |
|---|---|---|---|
| `1` | 版本 | `sys.version` ＝ `3.13.11 \| packaged by Anaconda, Inc. \| (main, Dec 10 2025, 21:21:58) [MSC v.1929 64 bit (AMD64)]`；`git version 2.53.0.windows.1`；`ezdxf 1.4.3`／`shapely 2.1.2` | ✅ |
| `2` | `ls-remote` | `40375702378af4cefd2d11515fdf3f98c0791580	refs/heads/wip/s1-endpart`；`merge-base --is-ancestor 4037570 HEAD` `rc` ＝ `0` | ✅ |
| `3` | `probe_order_preflight.py` | `P-5` ✅ 相符（實算 `e86dafbdc70c1ff6…`／單載 `e86dafbdc70c1ff6…`·受詞 `40696` B）；🔴 ＝ **`0`**；🟡 ＝ **`0`**（⛔ 有須逐項處置者）；`rc` ＝ `0`。併記 `P-3`：`app.py` 之 `def main` ＝ `14975`-`24579` | ✅ |
| `4` | 設定 | `core.autocrlf` ＝ `false`；`core.quotepath` **未設**（`rc` ＝ `1`） | ✅ |
| `5` | 生產碼 `34` 檔（**正面列舉**·逐檔列於 `verify/out/WG9318_stage5_recon.log` 之外另見本節下方） | blob 相異 ＝ **`0`**；`app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`；判別力[必不命中] 子層 `*.py` ＝ `333`（全 `verify/*.py` ＝ `366`） | ✅ |
| `6` | `baselines` | `git ls-tree -r HEAD verify/baselines \| sha256sum` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·**`298`** 列 | ✅ |
| `7` | 四簿 | 自誤 `422`／`432`（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `40`／`41`（缺 `[]`） | ✅ 逐位相符 |
| `8` | `refs/heads/verify/*` | **`20`** 列 | ✅ |
| `9` | 取號 `自誤 433` | 除本單自身之產物外 檔框 **`0`**／列框 **`0`**（錨定框 `(?<![0-9\-])<號>(?![0-9])`·B 形 ⋀ C 形並取·母體 ＝ HEAD 追蹤 `.md` **`849`** 檔）；本單自身之命中 `8` 列（排除項）。對照甲 `自誤 432` ＝ 檔 `3`／列 `18`（`> 0`）；對照乙 人造哨兵（執行期組出·字面⛔ 出艙）＝ `0` | ✅ 器非紅 |
| `10` | 五名之現行列號（AST） | `app.py`：`_b_L0` `22428`／`_b_R0` `22430`／`_mp_base_W0` `22403`／`_end_pt_o` `22418`／`_dhr_o` `22418`；`stepg`：`1048`／`1049`／`1023`／`1039`／`1039` | ✅ 全符 |
| `11` | `_advance_block_with_split` 呼叫節點 | `app.py` `[22388, 22391, 22450]`（`3`）／`stepg` `[1017, 1019, 1063]`（`3`）；判別力[必不命中] 人造函式名（執行期組出）＝ `0` | ✅ |
| `12` | `_solve_one` 呼叫節點 ＋ 包層鏈 | `app.py` `[22161, 22275, 22291]`（包層鏈皆 `main@14975 > _advance_block_with_split@22019`）／`stepg` `[821, 922, 937]`（皆 `_run_step_g_impl@277 > _advance_block_with_split@750`）⇒ **六者皆在該函式體內 ＝ True** | ✅ |
| `13` | `_solve_G_one` 呼叫節點 ＝ `6` | 母體 ＝ `app.py` ＋ `verify/` 全部 `*.py`（**`367`** 檔）⇒ **`6`**：`app.py:14117`／`app.py:21661`／`probe_K9_s62c_attribution.py:513`／`probe_WG914_gate_decomp.py:79`／`probe_WG916_isolate.py:57`／`verify/stepg_pipeline.py:516`；生產路徑三者逐位相符 | ✅（🩸 **首版量測器紅**·見 `§九`） |
| `14` | `data/V6_1.dxf` | blob `sha1` ＝ `c059aed10be5caeb811c3e044429f39e396d7971`·`88453` B；本批 `data/` 一字未動 | ✅ |

🔒 **生產碼 `34` 檔（正面列舉·⛔ 空 `pathspec`·⛔ 全倉排除式）**
`app.py`／`verify/app_harvest.py`／`b6_isomorphism.py`／`fixture_baseline_candidates.py`／`fixture_block_depth_n19p.py`／
`fixture_cad_binding_order.py`／`fixture_corner_range_k8.py`／`fixture_e2e_termination.py`／`fixture_end_fallback.py`／
`fixture_end_reserve.py`／`fixture_end_winner.py`／`fixture_g3_static_guards.py`／`fixture_klui_t2diag.py`／
`fixture_midlayer_6items.py`／`fixture_n14_feed_chain.py`／`fixture_n14_min_width.py`／`fixture_wf_ns_wiring.py`／
`fixture_yi_construction.py`／`run_all.py`／`run_verification.py`／`selection_pipeline.py`／`stepg_pipeline.py`／
`test_corner_first_lot_G.py`／`wd3_fragment_geom.py`／`wd4_tier_list.py`／`wf_f0.py`／`wf_f1.py`／`wf_f2.py`／
`wf_f3.py`／`wf_f4.py`／`wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py`／`wv_reconcile.py`

### `§一-補`　宣告框（CC 自跑·對拍 `§零-3`）

| 項 | 單載 | CC 自算 | 判 |
|---|---|---|---|
| 生產碼 | `34` 檔 | `34` 檔 | ✅ |
| 四簿期初 | 自誤 `422`／`432`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `40`／`41` | 同左 | ✅ |
| 母體 `docs/` | `826` | `826`（於 `4037570`） | ✅ |
| 母體 `verify/out/` | `928` | `928`（於 `4037570`） | ✅ |
| 母體 全倉追蹤 | `2447` | `2447`（於 `4037570`） | ✅ |
| 全倉 `.md` | `848` | `848`（於 `4037570`） | ✅ |

---

## `§二`　工項零（`c1`）

`SRC` ＝ `DST` **逐位相同**：`sha256` ＝ `a6ec0367b4eff42e750d0fa05f24a85e657c59db937f7e670eba80dcfa20ed47`（`40774` B·`CR` `0`·末換行有）；
入倉 blob 之 `sha256` **同值**。
判別力二造：**必綠** ＝ 原檔 `a6ec0367…`／**必紅** ＝ 改一位元組之副本 `01e07b3f155486345ba3c47c6be720b72a2e282f0ce7c06eac8de8c4bc46fa3f`
（該副本置 scratchpad·**⛔ 入倉**）⇒ 二造相異 ＝ `True`。

---

## `§三`　工項一（`c2`）：態乙之 `forced` 旗標與 `W₀`／`Wf`

**器** ＝ `verify/probes/probe_WG9318_stateB_forced.py`（唯讀·spy 逐位原樣回傳·⛔ 施替身·⛔ 寫 `baselines`·⛔ 設 `WV_BAKE`·⛔ 匯入即驅動）
**落檔** ＝ `verify/out/WG9318_stateB_forced.log`（`173` 列·`rc` `0`·`stderr` `0` B）
**驅動** ＝ 態乙 二情境各一次 ＋ **判別力造甲所需之態甲** 二情境各一次（單 `§三-2` 造甲逐字令「同受詞下態甲」⇒ 其驅動為該判別力之構成要件）

### 🔑 受詞之實得（未捨入）

| 量 | `0m` | `3.5m` |
|---|---|---|
| 態乙 `R4` `left_forced_offset` | `False` | `False` |
| 態乙 `R4` **`right_forced_offset`** | **`False`** | **`False`** |
| 態乙 `R4` `_fo_left`／`_fo_right`（`stepg:647`–`:648` 之區域名） | `False`／`False` | `False`／`False` |
| 態乙 `R4` `_b_L0`／`_b_R0` | `0.0000000000`／`0.0000000000` | `0.0000000000`／`0.0000000000` |
| 態乙 `R4` `W0_left`／`W0_right` | `-0.0000000000`／**`0.1800000000`** | `-0.0000000000`／**`0.1800000000`** |
| 態乙 `R4` `Wf_left`／`Wf_right` | `45.9400000000`／`14.2000000000` | 同左 |
| 態乙 `R4`【左】第 1 宗指配 | `G009（628）[628(1)]` | `G009（628）[628(1)]` |
| 態乙 `R4`【右】第 1 宗指配 | `G009（628-1）[628-1(1)]` | `G009（628-1）[628-1(1)]` |

🔒 **管線可達**：態乙 ＝ `['R1', 'R2', 'R4']`（中止閘 ＝ `結構閘 理論＝實跑 破 R4 right Δ=2.425`）；
態甲 ＝ `['R1','R2','R4','R6','R5','R3']`（跑完）。⛔ 可達之格一律標【**⛔ 可得**】並載其由。

### 🔒 判別力二造

· 造甲[必非零]：態甲 `R4` ⇒ `left_forced_offset` ＝ **`True`**／`right_forced_offset` ＝ **`False`**（二情境皆然）⇒ ✅
· 造乙[必為零]：人造街廓名（**執行期組出**·字面⛔ 出艙）之命中 ＝ **`0`** ⇒ ✅
⇒ **二造異色 ⇒ 器非紅**。

### 🛑 停機款之判

| 款 | 觸發條件 | 實得 | 判 |
|---|---|---|---|
| `停-1` | 態乙 `R4` `right_forced_offset = True` | **`False`**（二情境） | 🟢 **未觸發** |
| `停-2` | 態乙 `R4` 二旗標【⛔ 可得】 | 二旗標**皆可得** | 🟢 **未觸發** |

⇒ **續辦 `§四`**（⛔ 逕辦之違規）。

🔒 **旁證（⛔ 充實測·單 `§三-3` 逐字）**：`run_corner_pk` 之第三回傳 `_off` ——
態乙 `0m` `0` 列／態乙 `3.5m` `3` 列（`R5` 左 `299.73`／`R2` 左 `308.17`／`R3` 右 `307.79`）；
態甲 `0m` `1` 列（`R4` 左 `116.08`）／態甲 `3.5m` `2` 列（`R4` 左 `226.01`／`R2` 左 `308.17`）。
🛑 其與 `left/right_forced_offset` **⛔ 經證為同一物** ⇒ **本判係取 `_forced_offset_map` 之實測**，⛔ 以 `_off` 代之。

---

## `§四`　工項二（`c3`〜`c7`·生產碼）

### `c3`　`app.py:22396`–`:22431`（`36` 列）上移

插入於 `:22384`（`_slot_res = None`）之後、`:22385`（`if _degenerate_order or _N <= 1:`）之前；縮排 `28` → `24`。
· 首列逐字 ＝ `                            # 🆕 W 正典 W₀（補丁八 §一·脫鉤 S）：選槽理論之 forced 起始 W 改＝**首宗近側 KL W**`
· 末列逐字 ＝ `                                                 allocation_dir_block) if _fo_right else 0.0)`

| 閘 | 實得 | 判 |
|---|---|---|
| `c3-1`（AST·⛔ 文字層） | 移前／移後之 `ast.unparse` **逐字相同**；`sha256` 皆 ＝ `e29c66306a2542fc6bba4e85113c9a157390c6c8c5f34608473e005d0d2e1e83` | ✅ |
| `c3-2`（綁定·AST） | 十二名之首個綁定節點皆 `<` 新位置（`22385`）：`_left_buffer_S` `21893`／`_right_buffer_S` `21894`／`corner_pt` `7222`／`_side_mid_left` `21946`／`_side_mid_right` `21947`／`allocation_dir_block` `21716`／`_fo_left` `21886`／`_fo_right` `21887`／`_oblique_s_max` `9226`／`d_hat` `6516`／`blk_meta` `5669`／`k956_W_from_mp` `9094`（**皆 `≤ 21947`**·與單載相符）；`_end_pt_o`／`_dhr_o` 隨本區塊同移 | ✅ |
| `c3-3` | `numstat` 刪除 **`36`**／新增 **`36`**；`git diff -w` 之**可執行語句對稱差** ＝ **`0`**（移除 `11` 列／新增 `11` 列·其中可執行 `5`／`5`） | ✅ |
| `c3-4` | `py_compile` `rc` ＝ `0` | ✅ |

🛑 **語意變動之具名（⛔ 靜默承擔）**：原區塊在 `else:` 枝內，上移後**於退化枝亦執行**。其無害之三證 ——
`(1)` `_mp_base_W0` 對缺值一律 `return 0.0`；`(2)` `_end_pt_o`／`_dhr_o` 受
`if d_hat is not None and corner_pt is not None and blk_meta.get('vertices'):` 守衛；
`(3)` `_b_L0`／`_b_R0` 於 `_fo_* is False` 時為常數 `0.0`。
🔒 **其閘之實得**：見 `§五-4`（於 `c3`＋`c4`＋`c5` 之態，`got_*.csv` **18 檔 `sha256` 逐檔相同**）。

### `c4`　`verify/stepg_pipeline.py:1020`–`:1049`（`30` 列）同構上移

插入於 `:1014` 之後、`:1015` 之前；縮排 `12` → `8`。

| 閘 | 實得 | 判 |
|---|---|---|
| `c4-1` | `ast.unparse` 逐字相同；`sha256` 皆 ＝ `13d79c6a1a890984c4928501b1d231d8c116e21a68581f6a38a3c598c4a48359` | ✅ |
| `c4-2` | 十二名首個綁定節點：`_left_buffer_S` `650`／`_right_buffer_S` `651`／`corner_pt` `555`／`_side_mid_left` `682`／`_side_mid_right` `683`／`allocation_dir_block` `566`／`_fo_left` `647`／`_fo_right` `648`／`_oblique_s_max` `386`／`d_hat` `555`／`blk_meta` `530`／**`_k956_W_from_mp` `394`**（皆 `≤ 683`·**與單載逐位相符**） | ✅ |
| `c4-3` | `numstat` `30`／`30`；可執行語句對稱差 ＝ `0` | ✅ |
| `c4-4` | `py_compile` `rc` ＝ `0` | ✅ |
| `c4-5` | 二處移動後區塊之 `ast.unparse`（各 **`17`** 列）之 `unified_diff` 命中 **恰 `1` 處**，逐字 ＝ `-    return float(k956_W_from_mp(_bp0, _mp, _adir, _dv))` ／ `+    return float(_k956_W_from_mp(_bp0, _mp, _adir, _dv))` | ✅ |

🩸 **二值並報（`c4-5`）**：單載「其餘 **`7`** 列逐字相同」；實測 `ast.unparse` 為 **`17`** 列
⇒ **其餘 `16` 列逐字相同**。**相異列數 `1` 與單載一致**；`7` 係裝飾性之列數（`常規五之追加五 一-1` 白名單 `8`）。

### `c5`　`solve_G_binary` 新增關鍵字 `w0_start`（`+15` ／ `−0`）

🩸 **偏離之逐字回報（`常規二`：施工單內部條款互斥）**
單 `§四-c5（一）` 令「於 `near_dir=None,` 之後、`) -> dict:` 之前」增一列，
然倉內該二者**同在一列**——`app.py:10093` 逐字 ＝ `                   near_dir=None) -> dict:`
⇒ 依單之字面須**改**該列 ⇒ `deletions ≠ 0` ⇒ 與 **`§七` 閘 `1`**「刪除欄：僅 `c3`／`c4` 得非零，其餘全 `0`」**互斥**。
**取保守項**：改為於該列**之前純插入**（`deletions` ＝ `0`），語意相同。
🔒 **其安全性之機驗**：`solve_G_binary` 之**全倉呼叫節點 ＝ `3`**
（`app.py:14000`／`probe_WG92_W_origin_conformance.py:208`／`:209`），**位置實參皆 `0`**（全為關鍵字）
⇒ 參數次序之改**⛔ 影響任何呼叫端**。

🩸 **`numpy` 別名之現查（單明令「⛔ 逕採本單之 `_np_d`」）**：`solve_G_binary` 函式體內
`_np_d` 命中 **`0`**／`np` 命中 **`18`** ⇒ 採 **`np.isfinite`**。

| 閘 | 實得 | 判 |
|---|---|---|
| `c5-1`（⛔ 靜默） | 造① `'x'` ⇒ `ValueError`｜`🔴 K-9-41 ①：w0_start ⛔ 可解析為 float：'x'`；造② `[1, 2]` ⇒ `ValueError`｜`🔴 K-9-41 ①：w0_start ⛔ 可解析為 float：[1, 2]`；造③ `nan` ⇒ `ValueError`｜`🔴 K-9-41 ①：w0_start 非有限值：nan`。**判別力**（證⛔ 恆紅）：未傳與 `w0_start=None` 之回傳**逐位相同**；`w0_start=3.0` 使 `W_near` `0.0` → `3.0`（覆寫確生效）。落檔 `verify/out/WG9318_c5_loud.log` | ✅ |
| `c5-2`（預設不變） | 基座 ＝ **拋棄式 clone @ `4037570`**（`core.autocrlf=false`·`app.py` `CR` `0`·blob `4379108a…`）。名目 **`64`**（`24` PASS／`40` FAIL）之（判＋名目）序列**逐位相同**；`wv_reconcile`（名目＋原因）⇒ 類 I **`40`**／類 II **`0`**／類 III **`0`** ⇒ **對帳 PASS**（`rc` `0`）；`got_*.csv` **`18` 檔 `sha256` 逐檔相同（相異 `0`）** | ✅ **行為逐位不變** |
| `c5-3`（AST） | `_W_near` 之**讀取(Load)節點恰 `2`**（`:10225` `_W_near_out = _W_near`／`:10258` `_rw_start = …`），二者皆在插入點（`:10214` `if w0_start is not None:`）**之後** | ✅ |

### `c6`　薄殼直通（`app.py +8`／`stepg +2`·`deletions` `0`）

🩸 **同形之偏離（`常規二`）**：單令「簽章**末**各增」與「`return` 之實參**末**各增」，
然二檔之簽章末列與 `return` 末列**皆與 `)` 同列** ⇒ 依字面須改該列 ⇒ 與閘 `1` 互斥。
**取保守項**：於該列**之前**純插入。
🔒 **安全性之機驗**：`_solve_one` 之**六呼叫端位置實參皆為 `12`**（止於 `avg_depth_default`），其後全為關鍵字
（`_allocation_dir`／`_side_mid`／`_W_prev`／`_near_dir`／`_is_chain_head`）
⇒ 於第 `13` 位插入 `_w0_start=None`**⛔ 影響任何呼叫端**；`_solve_G_one` 為 `keyword-only`（`def _solve_G_one(*, …)`）⇒ 次序無語意。

· `iterate_G_S` fallback 之 **loud 閘**（`4` 列）已插於 `    _r = iterate_G_S(` 之前。
· **`(三)` `_corner_first_lot_G` ⛔ 傳**：該呼叫節點之 `ast.unparse` 改前（`4037570`）改後**逐字相同 ＝ `True`**，其含 `w0_start` ＝ `False` ✅
· `py_compile` `rc` ＝ `0`（二檔）

### `c7`　六呼叫端傳之（`app.py +3`／`stepg +3`·`deletions` `0`）

| 檔 | 所在群（AST 包層 `for.iter`） | 傳之值 |
|---|---|---|
| `app.py:22184` | `left_group` | `_b_L0` |
| `app.py:22299` | `right_group` | `_b_R0` |
| `app.py:22316` | `right_group`（極端防呆後援） | `_b_R0` |
| `stepg:823` | `left_group` | `_b_L0` |
| `stepg:925` | `right_group` | `_b_R0` |
| `stepg:941` | `right_group`（極端防呆後援） | `_b_R0` |

| 閘 | 實得 | 判 |
|---|---|---|
| `c7-1` | `_w0_start=_b_`（＝**呼叫端之傳值**·單載之 `6`）＝ **`6`**（`app` `3`／`stepg` `3`）。🩸 二數並報：`_w0_start=` 之**全命中 ＝ `8`**（另含 `c6` 所增之二簽章預設）⇒ 單載之 `6` 係**呼叫端**之框。判別力[必非零] 同框 `_is_chain_head=` ＝ **`8`** ⇒ 該框⛔ 恆為 `6` | ✅ |
| `c7-2` | 左群傳 `_b_L0`（`3`）／右群傳 `_b_R0`（`3`）·**⛔ 交叉**；`_W_prev` 之側別亦同向 | ✅ |

---

## `§五`　工項三（`c8`）：落地後之二態對拍

🔒 **驅動 ＝ 四格**（`0m`／`3.5m` × 態甲／態乙）。🩸 **框之具名**：`run_verification.py` **一次呼叫即含 `0m` ⋀ `3.5m`**
（`:520`／`:592` 之 `for setback, tag in ((0.0, "0m"), (3.5, "3.5m"))`）⇒ **二次呼叫涵蓋四格**（⛔ 逾四）。
基座二次於拋棄式 clone（`4037570`），改後二次於本工作樹（`b1141a3`）。
彙整落檔 ＝ `verify/out/WG9318_stage5_recon.log`。

### `§五-2`　二結構閘之**三值判**（次序 ＝ `telescoping` **先**、`理論＝實跑` **後**）

🔒 **判據**（⛔ 器之離線重建·一律取該次驅動之 log 事實）：已執行 ⟺ log 有
`[Δ_i·<blk>·<side>·總量]`（telescoping 迴圈內、閘之前之 `print`）／`[Δ_i·<blk>·<side>·階段1]`（`理論＝實跑` 迴圈內）；
破 ⟺ 中止訊息具名該 `(blk, side)`。

| 態·情境 | 格 | `telescoping`（先） | `理論＝實跑`（後） |
|---|---|---|---|
| 乙（基座） | `R4`·right | 🟢 已執行且通過 | 🔴 已執行且破·`Δ` ＝ **`2.425`** |
| 乙（改後） | `R4`·right | 🟢 已執行且通過 | 🔴 已執行且破·`Δ` ＝ **`0.676`** |
| 乙（基座 ⋀ 改後） | `R4`·left | 🟢 | 🟢 |
| 甲（基座 ⋀ 改後） | `R4`·left／right | 🟢 | 🛑 **未執行**（退化枝 ⇒ `_slot_res is None`） |

🔒 **破之逐字**
> 基座：`🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.83 ≠ 實跑(階段1)=85.41（Δ=2.425 >0.1）`
> 改後：`🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.72 ≠ 實跑(階段1)=88.40（Δ=0.676 >0.1）`

🔒 **十二格之逐格表**（四態各一張）見 `verify/out/WG9318_stage5_recon.log`。**態甲之十二格三值判改前改後逐格相同**。

🩸 **照實（⛔ 追改期值·⛔ 改法·單 `§五-2` 明令）**——單 `§零-2` 裁 `6` 之期值與實得之別 **二項**：

| # | 單之期值 | 實得 | 差 |
|---|---|---|---|
| `1` | `telescoping` **由 🔴／🛑 轉 🟢** | **基座即已 🟢**，改後仍 🟢 | 其**起點**與單所述不同（⛔ 由 🔴／🛑 轉） |
| `2` | `理論＝實跑` 之 `Δ` ∈ **`0.661141 ± 0.01`** | **`0.676`** | **落於帶外 `0.0049`**（帶 ＝ `0.651141`〜`0.671141`） |

🔒 `Δ` **未**落於 `2.42`〜`2.43` ⇒ **本修已生效** ⇒ 該停機款**未觸發**（⛔ 停機）。
🛑 **本報告⛔ 就其成因作任何主張**——`_宗地寬度` 與 `W` 孰為 `K-9-41 ②` 所指之寬度，其裁**候 KL**。

### `§五-3`　土地後果（恆常附款 `o`·三款俱載）

🩸 **受詞之更正（CC 自捕）**：態乙之管線中止 ⇒ `got_G值_退縮*.csv` **未被改寫**
（同目錄殘留前一趟態甲之檔）⇒ 態乙一律取 **`got_G值_退縮*_partial.csv`**。

#### 態乙（`0m` 與 `3.5m` **同值**）

| 項 | 值 |
|---|---|
| 母體 | 改前 `29`／`32` 宗·改後 `29`／`32` 宗·**只在一側者 `0`** ⇒ **可比** |
| 逐宗非零 | **`1`** 宗 ＝ `R4`·right·`628-1(1)` |
| `ΔRw` | **`+2.9900000000`** 個百分點（`85.41` → `88.40`） |
| `ΔG` | **`−0.9000000000`** ㎡（`458.47` → `457.57`） |
| `ΔS`／`Δ宗地寬度` | `−0.0300000000` ／ `−0.0300000000` m |
| 合計 | `ΣΔG` ＝ **`−0.9000000000`**（帶號）／`Σ\|ΔG\|` ＝ **`0.9000000000`**（絕對值） |
| `Δ抵費地面積` | `ΣΔ` ＝ **`0.0000000000`**／`Σ\|Δ\|` ＝ **`0.0000000000`**（三列皆未變） |
| 守恆 | `R4` `ΣG` `1979.5200` → `1978.6200`；街廓面積 `3207.87` **未變** ⇒ **調配池 `+0.9000` ㎡** |

🔒 **與 `W-G.9-316R` 替身 `α` 之對拍**：`α` 所得 `ΔRw +2.99` pp／`ΔG −0.90` ㎡／非零 `2` 宗（二情境各一）
⇒ **真落地與其逐值相符**。
⚠️ `α` 另載 `ΔΣ抵費地面積 +0.9017197385` ㎡——本批實測 **`got_抵費地`（PK 階段之強制抵費地）之 `ΣΔ` ＝ `0.0000`**；
該 `+0.90` 係**進入調配池** ⇒ 二者為**不同受詞**，⛔ 混同。

#### 態甲（🔴 **單所未預期之格·必呈 KL**）

| 情境 | 母體 | 非零宗 | 逐宗 |
|---|---|---|---|
| `0m` | 改前 `56`／改後 `56`·只在一側者 `0` ⇒ **可比** | **`1`** | `R4`·**left**·`628(1)+`：`ΔRw` **`+4.88`** pp／`ΔG` **`−1.48`** ㎡·調配池 `+1.48` ㎡ |
| `3.5m` | 改前 `59`／改後 `59`·只在一側者 `0` ⇒ **可比** | **`1`** | `R4`·**left**·`628(1)+`：`ΔRw` **`+4.12`** pp／`ΔG` **`−1.25`** ㎡·調配池 `+1.25` ㎡ |

🔑 **其成因（⛔ 意外·係 `K-9-41 ①` 後段所令）**：`c3`／`c4` 之上移使**退化枝亦算** `_b_L0`／`_b_R0`
（態甲 `R4` 走退化枝），`c7` 使 **forced 側**（態甲 `R4` 左 ＝ `⚠️ 強制抵費地`）之起算點
改取 `_mp_base_W0(…)`——即 `K-9-41 ①` **後段**「為強制抵費地之側 ⇒ 傳該抵費地之 `W`」。

### `§五-4`　其他必出之量

| 項 | 實得 |
|---|---|
| `iterate_G_S` fallback 之 loud 閘命中 | **`0`**（二 log 皆然·期值 `0`）。判別力 [必非零] 同框 `RESULT:` ＝ `1`／[必為零] 人造字樣（執行期組出）＝ `0` |
| 態甲退化枝街廓（`R4`·`_N == 1`）之 `g_rows` 改前改後逐位相同 | 🟢 —— 其證 ＝ 閘 `c5-2`（`c3`＋`c4`＋`c5` 之態，`got_*.csv` **18 檔 `sha256` 逐檔相同**）。🛑 `c7` 落地**之後**該街廓已變（見上）——二者⛔ 混同 |
| `GB-170` 三格 `range_area` | `116.08`／`308.17`／`226.01` —— **逐位未變** |
| `GB-170` 三對稱差 | `13.055884224441684`／`38.06818855324997`／`13.05518976183967` —— **逐位未變**（改前 ＝ `verify/out/WG9287R_landeffect.log`【倉】／改後 ＝ `verify/out/WG9318_gb170_post.log`·**同一支既有唯讀探針**·⛔ 新增第二支） |
| `F.0` `GSA` 六鍵之決策現況 | 改前改後**逐字相同**：`['G006','G007','G009','G010','G014','G017']` 皆「該情境無任何決策列」 |

🩸 **併記（只登記·🛑 ⛔ 解除、⛔ 收窄 `GB-170`）**：同二 log 之 `R(W_0) − R(S1_perp)` 由
`4.955250038496402` → `0.09425003849639779`（`0m`）／`4.265999999999998` → `0.10799999999999699`（`3.5m`）；
其 `W_0` 由 `4.48` → `3.71`／`7.79` → `7.02`。
🛑 **`GB-170` 之失效條件三項仍未成就**；**`GB-168`** 至多成就其 `1`／`2`，`3`／`4` **未成就**
（態乙可達仍為 `['R1','R2','R4']`·改前改後相同）⇒ 🔴 **未解除**；**`GB-169`** 本批未觸 ⇒ **未解除**。

---

## `§六`　工項四（`c9`）：登記

### `§六-1`　`自誤 433`

界列**當場以 `grep` 重取** ＝ 本單 `:286`／`:302`（與單載相符·⛔ 逕採）。
`payload` ＝ **`17`** 列／**`2961`** B／`sha256` **`b2cae18c97d27433ab5f05d28533e1264ace02de5f365afd501997654cb80954`**；
期初 **`919874`** B → 期末 **`922836`** B（＝ `919874` ＋ `2961` ＋ 分隔 NL `1`）；`CR` ＝ `0`；
`append-only` 嚴格前綴**逐位相符**；`payload` 尾段**逐位相符**；**界列⛔ 入簿**。

### `§六-2`　`GB` 簿末端追加（⛔ 鑄號·⛔ 解除·⛔ 收窄）

節題 ＝ `## 🔧 W₀ 二消費者之粒度不對稱（未捨入／2dp）＋ K-9-41 ① 落地後之二閘實測（W-G.9-318·⛔ 上文一字不刪·純末端追加）`
`payload` ＝ **`7409`** B／`sha256` **`97be927d822b863486e3d0a37324504ab9b545d14acb344bbde21166a48efe84`**；
期初 **`928354`** → 期末 **`935764`**；`CR` ＝ `0`；嚴格前綴逐位相符。
**`GB-132` 避讓之機驗**：`payload` 內以 `#` 起首且含 `GB-N` 之列 ＝ **`0`**
（判別力[必非零]：`#` 起首列總數 ＝ `5`）⇒ 立後 `GB` 之相異／`MAX` 與立前**逐位相同**（`168`／`170`）。

### `§六-3`　四簿期末

自誤 **`423`**／**`433`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜
`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `40`／`41`（缺 `[]`）——**與 `§零-3` 之期值逐位相符**。

---

## `§七`　收工閘

🔒 **本節於報告入倉後另以一 `commit` 追加**（嚴格末端追加）；閘 `9`（`push` 後之 `ls-remote`）
與閘 `10`（KL 放行之逐筆記錄）**須俟 KL 放行後方能出艙**。

---

## `§八`　⛔ 為之事（逐條答）

| # | 條 | 答 |
|---|---|---|
| `1` | ⛔ 改本單一字 | ✅ 未改（`SRC` ＝ `DST` `sha256` 逐位相同） |
| `2` | ⛔ 改生產碼 `34` 檔中除二檔以外之任一字 | ✅ 未改（收工閘 `2` 機驗：相異恰 `2`） |
| `3` | ⛔ 另寫第二份 `W`／起算點之推導 | ✅ 未寫——落地之法 ＝ 把**選槽側已算之** `_b_L0`／`_b_R0` **原封傳給實跑側** |
| `4` | ⛔ 統一 `k956_W_from_mp` 與 `_k956_W_from_mp` | ✅ 未統一（閘 `c4-5` 證其仍為唯一之別） |
| `5` | ⛔ 靜默退 `0` | ✅ 三造皆 loud `raise`（閘 `c5-1`） |
| `6` | ⛔ 以「可見」代「已綁定」 | ✅ 閘 `c3-2`／`c4-2` 皆以 **AST 綁定節點**為判 |
| `7` | ⛔ 以二值框出艙任一閘之判 | ✅ 二結構閘一律**三值框**並載其次序 |
| `8` | ⛔ 出艙母體相異之差為後果 | ✅ 四格之母體皆**可比**（只在一側者 `0`），故無此情形；判仍逐格載明 |
| `9` | ⛔ 解除或收窄 `GB-168`／`GB-169`／`GB-170` | ✅ 三則皆逐則載「未解除」；`GB` 簿只登記其量 |
| `10` | ⛔ 修理論側（`_宗地寬度`）一字 | ✅ 未動 |
| `11` | ⛔ 跳序；⛔ 於 `§三` 觸停機款後逕辦 `§四` | ✅ 序 ①〜⑦ 逐辦；`§三` 二停機款**皆未觸發** |
| `12` | ⛔ 合併 `push` 或先 `push` 後補呈 | ✅ **本批⛔ `push`**（見 `§十`） |
| `13` | ⛔ 開新分支 | ✅ `refs/heads/verify/*` 維持 **`20`** 支 |
| `14` | ⛔ 占用 `W-G.9-309`、⛔ 辦修批 | ✅ 未占、未辦 |

---

## `§九`　本批之**自解清單**（`常規五之追加五 一-5`）與**自捕**

### 🔧 自解清單（逐項載 ①疑義逐字 ②讀法 ③所取者 ④機械證據 ⑤所棄者之由）

| # | ①疑義逐字 | ②讀法 | ③所取 | ④機械證據 | ⑤所棄之由 |
|---|---|---|---|---|---|
| `1` | `§四-c5（一）`「於 `near_dir=None,` 之後、`) -> dict:` 之前，增一列」 | 甲：依字面改 `app.py:10093` 一列（`deletions` `1`）／乙：於該列之前純插入（`deletions` `0`） | **乙** | `solve_G_binary` 全倉呼叫節點 `3`·**位置實參皆 `0`** ⇒ 參數次序之改⛔ 影響任何呼叫端 | 甲與 `§七` 閘 `1`（刪除欄全 `0`）**互斥** ⇒ 依 `常規二` 取保守項 |
| `2` | `§四-c6（二）`「簽章**末**各增 `_w0_start=None`；其 `return` 之實參**末**各增 `w0_start=_w0_start`」 | 同上二讀法 | **乙** | `_solve_one` 六呼叫端**位置實參皆 `12`**，其後全為關鍵字；`_solve_G_one` 為 `keyword-only` | 同 `1` |
| `3` | `§一` 閘 `13`「`_solve_G_one` 之呼叫節點 ＝ `6`」（**母體未載**） | 甲：生產碼 `34` 檔（得 `3`）／乙：`app.py` ＋ `verify/` 全部 `*.py`（得 `6`） | **乙** | 乙 ＝ `W-G.9-317R §四-b` 逐字所載之母體（「`app.py` ＋ `verify/` 全部 `*.py` 之 AST `Call`」）⇒ ⛔ 新創 | 甲之得數與單載不符，且非單所本之母體 |
| `4` | 閘 `c4-5`「**其餘 `7` 列**逐字相同」 | 甲：以 `7` 為通過條件／乙：以「相異**恰 `1` 處**」為 operative、`7` 為裝飾性列數 | **乙** | 實測 `ast.unparse` 為 `17` 列·相異 `1` 列（逐字與單載相同） | 甲之 `7` 與實測 `16` 不符；單之 operative 判準明載「命中恰 `1` 處」（白名單 `8`） |
| `5` | 閘 `c7-1`「`_w0_start=` 之列框命中 ＝ `6`」 | 甲：全字樣 `_w0_start=`（得 `8`）／乙：呼叫端之傳值 `_w0_start=_b_`（得 `6`） | **乙**（**二數並報**） | `8` ＝ `6` 呼叫端 ＋ `2` 簽章預設（`c6` 所增） | 甲之 `8` 與單載不符，係框含簽章列之故（白名單 `7`） |
| `6` | `§三-2`「器與驅動（**一次**·⛔ 逾一）」 vs 同節「造甲：同受詞下**態甲**之 `R4`」 | 甲：只驅動態乙（則造甲無從成立·器紅）／乙：態乙 ⋀ 態甲 各二情境 | **乙** | 造甲為單自身所令之**判別力構成要件**；不驅動態甲則「二造異色」結構上不可能成立 | 甲使器恆紅 ⇒ ⛔ 出艙任何數 |
| `7` | `§五-1`「驅動（**四次**·⛔ 逾四）：`0m`／`3.5m` × 態甲／態乙，各一次全量」 | 甲：呼叫 `run_verification` 四次／乙：呼叫二次（一次即含二情境） | **乙** | `run_verification.py:520`／`:592` 逐字 `for setback, tag in ((0.0, "0m"), (3.5, "3.5m"))` ⇒ **一次呼叫涵蓋二情境** | 甲將使同一情境被跑二次（⛔ 逾四之實質） |
| `8` | `§五-4`「`GB-170` 三格與**三對稱差**之逐位對拍」（其產生器非本批受詞·而本批僅得新增**一支**探針·已用於 `§三`） | 甲：標【未量】／乙：**重跑既有之唯讀探針** `verify/probes/probe_WG9287_landeffect.py` | **乙** | 該器為**既有**（⛔ 新增）；其⛔ 寫任何檔（`grep` 證其只 `open(rv.V6DXF,'rb')`）⇒ 以 `stdout` 重導至新檔·**⛔ 覆寫已追蹤之 `WG9287R_landeffect.log`** | 甲將使單所令之必出之量缺漏 |

### 🩸 自捕（**皆於入倉前**·`4` 則）

| # | 形 | 攔法 |
|---|---|---|
| `1` | 閘 `13` 首版之 AST 偵測器**漏 `Subscript` 形呼叫**（`ns["_solve_G_one"](…)`）⇒ 得 `2`、判「不符 ⇒ 停機」 | 該判與單載之 `6` 相差過大 ⇒ 改以文字層 `grep` 覆查，發現 `stepg:516` 為 `ns[…]` 形 ⇒ 補 `Subscript` 分支後得 `6` ⇒ **量測器紅·⛔ 受詞紅** |
| `2` | 閘 `c5-1` 首版之合成幾何以 **`tuple`** 傳 `d_hat` ⇒ 三造**皆得同一無關之 `TypeError`**（偽綠：看似三造皆 `raise`） | **先跑「必綠之對照組」**（合法值須⛔ `raise`）——其**亦紅** ⇒ 立判器紅；改 `np.array` 後三造方得真訊息，且 `w0_start=3.0` 使 `W_near` `0.0` → `3.0` 證覆寫生效 |
| `3` | `§五-3` 首版比到**態乙之殘檔** `got_G值_退縮*.csv`（態乙管線中止 ⇒ 該檔未被改寫·同目錄殘留前一趟態甲之檔）⇒ 態乙之逐宗差**與態甲完全相同** | 四目錄之 `sha256` 交叉對拍發現 `clone_A` ＝ `clone_B`、`postA` ＝ `postB` ⇒ 察覺未改寫；改取 `_partial` 後得真值（`R4 right 628-1(1)`·與替身 `α` 逐值相符） |
| `4` | `§六-2` `payload` 之 `### 🔧 (d) \`GB-168\` 之成就度` 一列**以 `#` 起首且含 `GB-N` 形** ⇒ 觸 `GB-132` 自指污染 | **落地前之事前機驗**（掃 `payload` 之 `#` 起首列）攔下；改題為「既有三則之成就度」後重驗得 `0` 列，`GB` 之相異／`MAX` 立後與立前逐位相同 |

---

## `§十`　🛑 呈 KL（**放行格**·⛔ 用程式語）與待決事項

### `§十-1`　🔴 **單之附錄`甲`【對土地的影響】須補正**（CC 之照實回報）

單之附錄`甲`載「其餘 `21` 宗地主**分毫未變**」，其數係取自 `W-G.9-316R` 替身 `α`（**態乙**之母體）。
**實測之後**，**態甲**（＝ `WV_K6_STEP0` **未設**·現行預設之態）**另有一宗**受影響，且**不是同一宗**：

| 態 | 受影響之宗 | 負擔率 | 分配到的土地 | 抵費地（公家可標售） | 其餘地主 |
|---|---|---|---|---|---|
| **乙**（`WV_K6_STEP0=off`·`K-9-29 二` 所令之態） | `R4` 街廓**右**側 `628-1(1)` | **增加 `2.99` 個百分點** | **減少 `0.90` 平方公尺** | 強制抵費地 **未變**；該 `0.90` 平方公尺**進入調配池** | 其餘 `28`／`31` 宗**分毫未變** |
| **甲**（`WV_K6_STEP0` 未設·**現行預設**） | `R4` 街廓**左**側 `628(1)+` | **增加 `4.88`（`0` 公尺退縮）／`4.12`（`3.5` 公尺退縮）個百分點** | **減少 `1.48`／`1.25` 平方公尺** | 強制抵費地 **未變**；該面積**進入調配池** | 其餘 `55`／`58` 宗**分毫未變** |

🛑 **二態之受影響者⛔ 為同一宗**（乙 ＝ 右側 `628-1(1)`；甲 ＝ 左側 `628(1)+`），
**數字亦不同**。單之附錄`甲`只載乙態之數 ⇒ **請發單側據實測補正該格後再呈 KL**，
或由 KL 逕依本表裁決。**CC ⛔ 代擬其放行格之內容**（`序 11` 之一般化：閘之受詞⛔ 由受測方提供）。

### `§十-2`　待 KL 逐筆放行之五個 `commit`

`c3` `7f43924…`／`c4` `7c346b1…`／`c5` `7c07a36…`／`c6` `6cc51e6…`／`c7` `b1141a3…`
🛑 **未經逐筆放行，⛔ `push`**；主分支 `wip/s1-endpart` 現仍 ＝ `4037570…`。

### `§十-3`　候**發單側**之事項（⛔ 上呈 KL·`常規二`／`常規三`）

| # | 事項 |
|---|---|
| `1` | `§四-c5（一）`／`§四-c6（二）` 之**插入位置**與 `§七` 閘 `1`（刪除欄全 `0`）**互斥** ⇒ 依 `常規二` 由**發單側當場裁並具名為發單側自誤**；CC 已取保守項並逐字回報（`§九` 自解 `1`／`2`） |
| `2` | `§零-2` 裁 `6` 之期值二項與實得之別（`telescoping` 之**起點**／`Δ` **`0.676`** 落於帶外 `0.0049`）——CC **⛔ 追改期值、⛔ 改法**，照實出艙 |
| `3` | `§一` 閘 `13` 之「`6`」**未載母體**（`VR-091 一 (3)`）⇒ 建議後單補之 |
| `4` | 態甲亦有土地後果（`§十-1`）——是否須另呈、是否影響 `K-9-41 ①` 之射程判讀 |

---

🔑 **本報告之數皆為【倉】**（來源 `commit`、母體、粒度框逐處同格具名）；
凡僅見於施工單而倉內未復現者（如 `0.1775004071`、`0.661141`、`α` 之 `+0.9017197385`）一律標 **【單】**，
**⛔ 下游文件引用為事實**。

---

## `§七-實測`　收工閘（**報告入倉後量**·⛔ 上文一字不刪·嚴格末端追加）

🔒 **期初** ＝ `40375702378af4cefd2d11515fdf3f98c0791580`／**期末** ＝ `b5110caf53eee8bc5c7cce19c61746c31c53f3e8`（`c10`）。
🔒 **本節表內之 `→` 一律為「期初 → 期末」之<u>時序方向</u>**·⛔ 幾何方向，⛔ 涉任何座標系。
🔒 判準逐條**自本單條文重新錄入**（⛔ 沿用他批）；各數之來源 ＝ **當場以程式自倉重算**
（框與時點同格具名）。逐字落檔 ＝ `verify/out/WG9318_exit_gates.log`（`87` 列）。

| # | 閘 | 實得 | 判 |
|---|---|---|---|
| `1` | 逐 `commit` `numstat` 之**刪除欄** | `c1` `0`／`c2` `0`／**`c3` `36`**／**`c4` `30`**／`c5` `0`／`c6` `0`／`c7` `0`／`c8` `0`／`c9` `0`／`c10` `0` ⇒ 僅 `c3`／`c4` 之位移非零 | ✅ |
| `2` | 生產碼 `34` 檔（正面列舉·⛔ 空 `pathspec`） | 相異 **恰 `2`** ＝ `['app.py', 'verify/stepg_pipeline.py']`；其餘 `32` 檔 blob 逐位相同 ＝ `True`；`app.py` blob `4379108a4856714078c410ff4e6291ecfdf6d2c1` → `b2ebae51f29c4a5e…` | ✅ |
| `3` | 本批異動之 `.md` 之 `CR`（倉內 blob·取 bytes） | 受檢集 ＝ **`4`** 檔（`docs/orders/W-G.9-318_…md`／`docs/reports/W-G.4_泛用阻塞項登記表.md`／`docs/reports/W-G.9-318R_…md`／`docs/reports/W-G.9波_claude.ai側自誤登記.md`）·**CR 全 `0`**。🩸 已**先解跳脫再判**（`core.quotepath=false`）；判別力[⛔ 空集充綠]：受檢集基數 ＝ `4`（`> 0`） | ✅ |
| `4` | 四簿期末 | 自誤 **`423`**／**`433`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `40`／`41`（缺 `[]`） | ✅ 與期值逐位相符 |
| `5` | `append-only`（逐檔） | 自誤簿 `919874` B → `922836` B·嚴格前綴 `True`·判別力（改末位元組之竄改本）`False`（須 `False`）；`GB` 簿 `928354` B → `935764` B·同上。機檢器 `wg942_append_audit.py 4037570`：受檢 **`77`** 筆／不適用 `0`／結論 **✅ 全部成立**／`rc` ＝ `0` | ✅ |
| `6` | 自限復驗 | `baselines` `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·**`298`** 列；`refs/heads/verify/*` ＝ **`20`** 支；`verify/out/` `928` → `939`（新增 `11`／移除 `0`）·**既有 `928` 檔 blob 相異 ＝ `0`**（判別力[必非零]：本批異動之 `verify/out/` 全集 ＝ `11`）；`data/` `3` → `3`·相異 ＝ **`0`**（一字未動） | ✅ |
| `7` | 母體（框與時點同格具名） | 全 `docs/` **`826` → `828`**（＋`2`）／`verify/out/` **`928` → `939`**（＋`11`）／全倉追蹤 **`2447` → `2461`**（＋`14`）／全倉 `.md` **`848` → `850`**（＋`2`）——框皆 ＝ `git ls-tree -r <rev> <pfx>` 之**檔框** | ✅ |
| `8` | 標籤對照（**當場以程式自資料列計數**·⛔ 轉引單之自載數） | `probe_WG9269_pit_index.py`：資料列 ＝ **`94`**｜末欄「✅ 可解析」命中 ＝ **`94`**｜非之者 ＝ **`0`**｜`rc` ＝ `0` | ✅ |
| `9` | `ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | **【⛔ 可得】**——現態（`push` **前**）＝ `40375702378af4cefd2d11515fdf3f98c0791580`。其由 ＝ **本批⛔ `push`**（`c3`〜`c7` 須 KL 逐筆放行·單 `§四` 通則 ＋ `§八-12`） | 🛑 **未執行** |
| `10` | **KL 放行之逐筆記錄**（`c3`〜`c7` 五筆） | **【⛔ 可得】**——五列皆 **【未放行】**（`c3` `7f43924…`／`c4` `7c346b1…`／`c5` `7c07a36…`／`c6` `6cc51e6…`／`c7` `b1141a3…`） | 🛑 **未執行** |

🛑 **閘 `9`／`10` 之「未執行」⛔ 讀為「不過」**（`序 11` 之一般化 款 `三`：
「**無從回測**」⛔ 等同「回測不過」）——二者之受詞**結構上繫於 KL 之放行**，
而本批**依單之明令止於放行之前**。

### 🩸 本節之**自捕（第 `5` 則·入倉前）**

**形**：閘 `6` 首版之 `refs/heads/verify/*` 得 **`0`** 支（期 `20`）。
**攔法**：該數與**開工閘 `8`**（同一會期所量之 `20`）**互斥** ⇒ 先判**量測器紅**、⛔ 逕報受詞紅。
**成因（機械·三造並列）**——
`(i)` `bash`（單引號由 `bash` 解）⇒ **`20`**；
`(ii)` `python subprocess(shell=True)`（於 `cmd.exe` **單引號⛔ 被解為引號** ⇒ pattern 成**字面**）⇒ **`0`**（`rc` `0`·**靜默**）；
`(iii)` `python subprocess(shell=False)`（argv 直傳·⛔ 經 shell）⇒ **`20`**。
⇒ **量測器紅**，已改 `(iii)` 並於落檔內具名其框。
🔑 **通則**：`shell=True` 於本機之 shell 為 `cmd.exe`，其**⛔ 解單引號** ⇒ 凡含 glob 或引號之
`git` pattern 一律用 **argv 直傳**；其偽 `0` **不報錯、`rc` 仍為 `0`** ⇒ 唯一看得見它的是**與他處同量之互斥**。
