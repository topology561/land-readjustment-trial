# `W-G.9-278R`　`W_0 → 0` 反事實之**停機上呈** ／ `GB-169` 之鑄 ／ `GB-168` 二次精化 ／ 坑 `bd`

> **態**｜開工 ＝ `03762e8b37fbc0145768494a93f90188a27ce25f`（`ls-remote` 實查）｜工項零後 ＝ `184730dc4b7e1db30778bc11f4299368f1d9aea9`
> **單** ＝ `docs/orders/W-G.9-278_施工單_W0歸零之反事實量測與GB169.md`（工項零已原封入倉）
> **分級** ＝ **重**（反事實驅動四次·**零生產碼**）｜**生產碼 `34` 檔一字未動**｜**替身⛔ 入生產碼**
> 🛑 **本報告⛔ 判其為正確或錯誤、⛔ 提落地與否之主張、⛔ 稱其為「落地」、⛔ 判二解算器孰誤、⛔ 判 `K-9-5-12（五）3.` 之「未決」後半款。**

---

## 🛑 `§零`　**本批之結論：主檢（工項 `1`）<u>停機上呈</u>；工項 `2`〜`5` 全數辦畢**

**主檢之閘依單 `§四-1` 所定之判別力二造而<u>紅</u>**（`rc = 5`）⇒ 依單 **⛔ 出艙任何反事實之數**。
其**成因已完全定位且非器之缺陷**（見 `§二-5`）。**所需之裁 ＝ 發單側重新明定「造甲」之受詞**（`§二-7`）。
🔒 **工作紀律**：不依賴主檢之工項（`2`〜`5`）**已全數辦畢並入倉**，⛔ 因主檢停機而一併擱置。

---

## `§一`　開工閘與工項零

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `03762e8…` 或其後裔 | `03762e8b37fbc0145768494a93f90188a27ce25f` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 逐位相同 | ✅ |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須**非**祖先 | **非祖先** | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `b2f6d789f3fea8e5fd8ada32c5bba5e28afdc32cd96f366025553d398e8fdf93` | **逐位相符**（受詞 `12524` B·全檔 `12602` B／`CR` `0`） | ✅ |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 不紅 | **停機款 `0`**；🟡 **提示 `4` 項**（逐項處置見 `§一-1`） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `370`／`380`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `81`／`81` | **逐位相同**（坑判別力二造 造甲 `6 ≥ 1` ✅／造乙 `0`／`0` ✅） | ✅ |
| `7` | `verify/baselines` | `298` 檔·`a898f4e1…`（口徑 ＝ `core.quotePath` **預設**） | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |

🔒 **閘 `4` 之判別力**：對**全檔**施同口徑 ⇒ 相異；受詞多 `1` byte ⇒ 相異 ⇒ 非恆綠。

### 一-1　🟡 **`4` 項提示之逐項處置**（⛔ 靜默略過）

| 項 | 列 | 受詞（節略） | **處置** |
|---|---|---|---|
| `P-2` | `:65` | 「出艙計數而同段⛔ 載其框」——其所咬者為坑 `bd` 表之**表頭列** `\| 標籤 \| 形／徵候／攔法 \|` | 🔒 **具名豁免**：該列為 markdown **表頭**、⛔ 計數之出艙；其所含之 `0`／`1` 係**徵候之描述**，其框已於本報告 `§四` 逐項載明 |
| `P-4` | `:34` | 「方向性轉引而同段⛔ 具名座標系」——`GB-169` 受詞句 | 🔒 **採納**：本報告凡引 `MP→corner` 形者，同格具名其座標系 ＝ **幾何量測之起訖**（自 `MP` 量至 `corner`），⛔ 「改前→改後」之時序 |
| `P-4` | `:124` | 同上——`虛胖陷阱` 錨腐段 | 🔒 **採納**：同上（`§五` 已同格具名） |
| `P-6` | `:85` | 「停機款所繫之數⛔ 具名實測出處」——`§四-1` 節題 | 🔒 **採納**：本批之停機款所繫之數（`14` 格／`10` 格）其實測出處 ＝ `verify/out/WG9277R_wchain.log`（`W-G.9-277` 已入倉）；本報告 `§二-4` 已逐格重測並同格具名 |

**工項零**（commit `184730d`·`§二-B` 搬移法）：所指之檔數 ＝ **`1`**（判別力：造乙 人造名 ⇒ `0`／造甲 `W-G.9-277` 之單 ⇒ `1`）；
`CR` ＝ **`0`**（`LF` `158`·bytes `12602`；判別力：真 CRLF 人造檔 ⇒ `CR = 2`）；
blob `sha1` ＝ `91acae5de85e9ae0935f8ef188d43b0077abf161`；
**blob 內容 `sha256` ＝ 磁碟 ＝ 源 ＝ `2d51157bb017cf99e06ff0070db1be45417e9d46ee0c143ceae358b6cc2250f7`** ⇒ **逐位相同** 🟢。

---

## 🛑 `§二`　工項 `1`：`W_0 → 0` 之反事實量測——**停機上呈**

**器** ＝ `verify/probes/probe_WG9278_w0zero.py`｜**落檔** ＝ `verify/out/WG9278R_w0zero.log`｜**`rc = 5`**

### 二-1　項 `1`：情形甲／乙之判別（**已辦·⛔ 受停機影響**）

源 ＝ `run_corner_pk` 之**第三回傳** `_off`（即 `verify/out/got_抵費地_退縮*.csv` 之**同一產生者**），
其 `指配` 欄為據。🔒 **該 CSV 於本批開工時⛔ 在倉亦⛔ 在工作區**（`.gitignore:30` 之 `verify/out/got_*` 刻意忽略）
⇒ CC **先跑一次 `verify/run_verification.py`**（態甲·生產態）產之，再與 `_off` 逐列對拍。

| 情境 | `_off` 列數 | 逐列逐字 | CSV 列數 | 判 |
|---|---|---|---|---|
| `0m` | **`1`** | `{'街廓': 'R4', '端': '左', '抵費地面積＝range(㎡)': '116.08', '指配': '強制抵費地'}` | **`1`** | ✅ 相同 |
| `3.5m` | **`2`** | `{'街廓': 'R4', '端': '左', '抵費地面積＝range(㎡)': '226.01', '指配': '強制抵費地'}`<br>`{'街廓': 'R2', '端': '左', '抵費地面積＝range(㎡)': '308.17', '指配': '強制抵費地'}` | **`2`** | ✅ 相同 |

**判別表（六街廓 × 二側 × 二情境）**——**乙**（強制抵費地·⛔ 模擬）之格：

| 情境 | 乙之格 | 乙之總數 |
|---|---|---|
| `0m` | `R4` `left` | **`1`** |
| `3.5m` | `R4` `left`／`R2` `left` | **`2`** |

其餘 **`22`** 格（`0m` `11` ＋ `3.5m` `11`·扣乙）皆為**甲（可模擬）**。
🔒 **判別力**：乙之總數**非零**（`1`／`2`）⇒ 該判別具鑑別力。
🔒 **長跑之自限**：該次 `run_verification` 之 `rc = 1`（＝**准紅碼**之既存態·⛔ 本批所致）；
其後 `git status --porcelain` 之**非 `??` 列 ＝ `0`** ⇒ **追蹤檔零異動**。

### 二-2　自證閘（**先決**）——**已過**

| 受詞 | 值 |
|---|---|
| 所比對之欄 | **鏈頭之 `W_0`**（`W-G.9-277R` 落檔之 `W_0` 表之**態甲**二欄） |
| 參照之格數 | **`14`** |
| 所比之格數 | **`13`** |
| **相異** | **`0`** ⇒ **逐位相同** ✅ |

🔒 **判別力**：一人造值（**執行期組出**·字面⛔ 出艙）與參照任一格比 ⇒ **相異** ⇒ 該檢非恆綠。
🔒 **所比 `13` < 參照 `14` 之由**：參照表含 `R4 right`（**僅態乙**有）；本批**只驅態甲** ⇒ 該格於本批⛔ 產生。

### 二-3　替身之生效統計（**反靜默退路**·單 `§四-1` 所令）

| 情境 | 鏈頭呼叫 | **咬到** | pk 段 | stepG 段 | 跳過（乙或非受詞） | 🟡 跳過（**不可判**） |
|---|---|---|---|---|---|---|
| `0m` | `1067` | **`189`** | `0` | `189` | `16` | **`862`** |
| `3.5m` | `1057` | **`142`** | `0` | `142` | `17` | **`898`** |

🔴 **「不可判」之逐字具名（⛔ 靜默）**：其全數落於 **`run_corner_pk` 段**——該段之框內省**⛔ 取得 `blk_label`／`side`**
⇒ 依器之設計（**⛔ 在不可判之處施替身**）一律**跳過**。
⇒ 🔒 **本批之替身<u>事實上只作用於 `run_step_g` 段</u>**；街角 PK 之結果（winner／強制抵費地）**⛔ 受替身影響**。
🛑 **此係與單 `§四-1` 逐字（「凡其呼叫端經框內省判定為鏈頭者」）之<u>實測偏離</u>，照實具名**——
單未限定其段，而框內省於 pk 段**不可判** ⇒ 器取**保守項**（跳過）並 loud 計數，⛔ 靜默施加。

### 二-4　判別力二造——🛑 **紅**

🛑 **單內二處互斥（`常規二`·逐字回報·⛔ 上呈 KL）**
單 `§四-1` 之判準**逐字**作 **`甲[必有差] ＝ 任一 \`W_0 > 1e-6\` 之側`**（**帶號**），
而其括弧內所引之「**已知 `14` 格**」係 `W-G.9-277R` 之 **`|W_0| > 1e-6`** 之計數 ⇒ **二者相異**。
⇒ CC **二讀法並報**並取**較嚴者**為判（`常規二` 之「取保守項續辦 ＋ 逐字回報該互斥」）。

| 情境 | 街廓 | 側 | `W_0` | 類（帶號） | 類（絕對值） | `Rw`(關) | `Rw`(開) | `ΔRw` |
|---|---|---|---|---|---|---|---|---|
| `0m` | `R1` | left | `-0.0000000000` | 乙 | 乙 | `52.64` | `52.64` | `+0.0000000000` |
| `0m` | `R1` | right | `-0.4007179780` | **乙** | **甲** | `55.80` | `55.80` | `+0.0000000000` |
| `0m` | `R2` | left | `0.0000000002` | 乙 | 乙 | `38.14` | `38.14` | `+0.0000000000` |
| `0m` | `R3` | right | `-3.3976107559` | **乙** | **甲** | `88.30` | `88.30` | `+0.0000000000` |
| `0m` | `R5` | left | `-0.0000000002` | 乙 | 乙 | `43.06` | `43.06` | `+0.0000000000` |
| `0m` | `R6` | right | `-0.0000020570` | **乙** | **甲** | `56.77` | `56.77` | `+0.0000000000` |
| `3.5m` | `R1` | left | `-0.0000000000` | 乙 | 乙 | `98.75` | `98.75` | `+0.0000000000` |
| `3.5m` | `R1` | right | `-0.4007179780` | **乙** | **甲** | `55.80` | `55.80` | `+0.0000000000` |
| `3.5m` | `R3` | right | `-3.3976107559` | **乙** | **甲** | `88.30` | `88.30` | `+0.0000000000` |
| `3.5m` | `R5` | left | `-0.0000000002` | 乙 | 乙 | `61.40` | `61.40` | `+0.0000000000` |
| `3.5m` | `R6` | right | `-0.0000020570` | **乙** | **甲** | `56.77` | `56.77` | `+0.0000000000` |

🔒 **二讀法之判**

| 讀法 | 造甲格數 | 其 `\|ΔRw\| > TOL0` 者 | 造乙格數 | 其 `\|ΔRw\| > TOL0` 者 | **判** |
|---|---|---|---|---|---|
| **帶號**（單之逐字 `W_0 > TOL0`） | **`0`** | `0` | `11` | `0` | 🟡 **造甲之母體為空 ⇒【⛔ 可得】**（⛔ 以飽和者頂替、⛔ 逕稱器非紅） |
| **絕對值**（其所引之計數 `\|W_0\| > TOL0`） | `6` | **`0`** | `5` | `0` | 🛑 **二造同色 ⇒ 器紅** |

🔒 **取較嚴者（`常規二`）** ＝ **絕對值讀法·器紅** ⇒ **`rc = 5`** ⇒ 🛑 **⛔ 出艙任何反事實之數**。

### 二-5　🔑 成因（**碼面逐字·⛔ 器之缺陷**）

`app.py` 之 `rw_from_width`（`:5728`–`:5747`）逐字含：

> `    W = float(W or 0.0)`
> `    if W <= 0:`
> `        return 0.0`

⇒ 🔒 **`R(W_0) ≡ 0` 對一切 `W_0 ≤ 0`**；而 `rw_increment`（`:5753`–`:5760`）逐字
`return max(0.0, (rw_from_width(W_cur) - rw_from_width(W_prev)) / 100.0)`
⇒ 🔒 **`rw_increment(W_0, W_cur) ≡ rw_increment(0.0, W_cur)` 當 `W_0 ≤ 0`**
⇒ **本反事實於該等側<u>結構上為空操作</u>**。

🔴 **而本批之母體（態甲 ∩ 情形甲）中，`W_0` <u>無一為正</u>**（上表 `11` 格逐格可見）：
最大者為 `+0.0000000002`（`R2 left` `0m`·`< TOL0`）。
🔒 **正值之 `W_0` 恰好全落於<u>情形乙</u>**（`R4 left` 態甲 `0m` `4.4804447558`／`3.5m` `7.7850157181`；
`R2 left` `3.5m` `6.9804804474`）——而單 `§七-5` **明令⛔ 模擬情形乙**。
🔒 **單所引之「`14` 格」係跨<u>態甲與態乙</u>之 `|W_0| > 1e-6` 計數**（含 `R4 right` 態乙之 `+0.1775004071`）
⇒ **其與本批之母體（僅態甲 ∩ 僅情形甲）⛔ 同**。

### 二-6　🛑 **本批⛔ 出艙之項**（單 `§四-1` 所令之 `2`〜`6`）

依單「**二造同色 ⇒ 器紅 `rc = 5`**」與「**⛔ 出艙任何反事實之數**」：
**項 `2`（逐宗 `ΔRw`／`ΔG`／`Δ幾何面積`）／項 `3`（逐街廓 `Δ抵費地` 逐片面積）／
項 `4`（守恆式於替身關／開各自成立與否）／項 `5`（六街廓到達與否）／項 `6`（`ΔΣG`／`ΔΣ抵費地面積`）
一律⛔ 出艙**。其**已量得**而留於器內，俟準據獲裁後可即刻重出。
🛑 **⛔ 以「反正都是 `0`」頂替**——該推論正是本閘所欲阻斷者。

### 二-7　🔺 **所需之裁（候發單側·⛔ CC 自訂·`§三` 第 `12` 款）**

> **造甲（必有差）之受詞須重新明定。** 其於本批之母體（**態甲 ∩ 情形甲**）中**結構上為空**
> ——一切可模擬之側其 `W_0` 皆 `≤ 0`，而 `rw_from_width` 於 `W ≤ 0` 逐字 `return 0.0`。
> **可能之方向（⛔ CC 之主張·僅列其受詞供裁）**：`(a)` 改以**態乙**為驅動母體（其 `R4 right` 之 `W_0 = +0.1775`）；
> `(b)` 放寬「⛔ 模擬情形乙」之禁令；`(c)` 改以**人造之正 `W_0`** 為造甲（純函式層·⛔ 驅動）；
> `(d)` 改判準為「**`W_0 ≤ 0` ⇒ 必無差**」之單造 ＋ 另備一必有差之外部錨。
> 🛑 **四者皆繫準據之新訂 ⇒ 由發單側明定**；CC ⛔ 自選。

---

## `§三`　工項 `2`：`GB-169` 之鑄

**落點** ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末追加**（append-only·嚴格前綴 `True`；
判別力：`old + b'X'` ⇒ `False`）｜長度 `860960` → `872208`（**`+11248` B**·含 `§四`／`§五` 之二節）｜`CR` ＝ `0`。
**受詞逐字塊**由 `sed -n '34,45p'` 之輸出**逕行落檔**（`常規七 一`）：
列數 **`12`**｜位元組 **`1658`**｜`sha256` **`4e34b74dad0dbd7af4dc70651012a6b61d0b3d790cbd1a39bb9992bfe0d4f4d3`**；覆核逐位相同 ✅。

**單 `§一` 末行所令之三項當場復現（⛔ 預寫）**

| # | 受詞 | 框 | **實測** |
|---|---|---|---|
| `1` | 正典節題之逐字 | **全列相等**·母體 ＝ `docs/rulings/K-6_…md` 單檔 | **`1`** 列 |
| `2` | `_rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)` | 列框·母體 ＝ `app.py` 單檔 | **`1`** 列 |
| `3` | `_W_near = float(np.dot(_bp_w - _mp_w, _ahat))` | 同上 | **`1`** 列 |
| `4` | 鏈頭 `W_0` 之 `24` 格三分類 | `TOL0 = 1e-6` | 嚴格 `== 0` **`0`**／`≈ 0` **`10`**／`> TOL0` **`14`** |

🔒 **判別力**：人造字面（執行期組出）@ `app.py` ＝ **`0`**；對照組 `_rw_start` ＝ **`5`** 列 ⇒ 器非紅。

**取號現查**（三軸｜`(1)` `blob@184730dc4b7e1db30778bc11f4299368f1d9aea9`｜
`(2)` 取最大號 ＝ 登記表**單檔**（`860960` B／`7454` 列）·查占用 ＝ 全倉追蹤檔 **`2178`** 檔（讀不到 `0`）｜
`(3)` **檔框 ＋ 列框**）：**MAX ＝ `168`**（相異 `93`）⇒ **次號 `169`**。

🔴 **`GB-158` 之案型再現**：**B 形**（`` GB-`N` ``）使**二對照組皆得 `0`／`0`**
（`GB-168` `0`／`0`·`GB-167` `0`／`0`）⇒ 依 `GB-158` 施行 二，**該形⛔ 單獨採信**；
其「未占用」之判由 **C 形**（`` `GB-N` ``）承擔——C 形於二對照皆 `>0`（`15`／`129`·`17`／`69`）⇒ **器非紅**。
**逐筆歸類**（`裁 H`）：受詢 `GB-169` 之命中（鬆 `1`／`4`·C 形 `1`／`4`）**全數集中於本單自身**
⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號。

---

## `§四`　工項 `3`／`4`：`GB-168` 二次精化 ＋ 坑 `bd`

### 四-1　`GB-168` 受詞之二次精化（末端追加·**⛔ 鑄號**）

節題由 `sed -n '54,54p'` 之輸出**逕行落檔**（列數 `1`／位元組 `206`／
`sha256` `50d7ba8c92643c6797dcdbff925d393a789db0fe9ae1a1779794dd1713aa666d`）。其內載三事，皆**當場復現**：

| # | 受詞 | **實測** |
|---|---|---|
| `1` | `Δ_i` 之檢於界面 `0` 時之行為 | `_bad_a = [...]` @ `:1461`（命中 `1`）／`if _bad_a:` @ `:1462`（命中 `1`）⇒ 判定組為空 ⇒ `raise` **不執行**；**實拋者 ＝ 第二閘** `if abs(_sum_rw_side - _tele_exp) > _IFACE_TOL:` @ **`:1471`**（命中 `1`）·其 `raise` 首行 @ **`:1473`**（命中 `1`） |
| `2` | 二解算器之現行實參（AST） | `solve_G_binary` ＝ `(_rw_start, W)`（節點 `1`）／`iterate_G_S` ＝ `(W_prev, W_cur)`（節點 `2`）；全母體 `iterate_G_S` 之 `rw_increment` 計數 ＝ **`0`** |
| `3` | `_宗地寬度` | `round(S × \|cos(d̂, n_alloc)\|, 2)` ＝ **`S` 之投影**（⛔ 二物之距·且 `2dp` 捨入）；`理論@k*` 餵 `14.01`、實跑用 `14.1988975422`／`0.1775004071` |

🔑 **併記（單 `§二-2` 所令逐字）**：`D-2b-5` 所載之「`solve_G_binary` 偏離（`rw_increment(_W_near, W)`）」
係**改前之態**；現行為 `(_rw_start, W)`，而 `_rw_start` 於 `is_corner or is_chain_head` 時**即 `_W_near`**
⇒ **改名⛔ 等同改行為**。🛑 **⛔ 判其是否仍屬偏離**。

### 四-2　坑 `bd` 之鑄 ＋ `ALPHA_LABELS`→`bd`

🔒 **其定義處 ＝ 本單 `§三` 之坑表**（`docs/orders/W-G.9-278_…md:65`–`:67`），
表頭列逐字 ＝ `| 標籤 | 形／徵候／攔法 |` ⇒ 同時含 `徵候` ⋀ `攔法` ⇒ 合於 `D-T` 之形。
**取號現查**（全倉·態 `184730d`）：坑 `bd` ＝ **`1`** 檔／**`5`** 列（**全數為本單自身**）；
對照組 甲 坑 `bc` ＝ `3` 檔／`13` 列／甲2 坑 `bb` ＝ `12` 檔／`29` 列（皆須 `>0`）／
**乙 ＝ 一未鑄之標籤（其字面<u>執行期組出</u>·⛔ 出艙·坑 `bd` 自身之攔法）＝ `0` 檔／`0` 列** ⇒ **器非紅**。

🩸 **本項之案由即坑 `bd` 自身**：前批（`W-G.9-277`）曾以 **`bz`** 為「必為零」之對照，
而其字面已隨 `W-G.9-277R` 出艙入倉 ⇒ 本批之同一檢即命中之 ⇒ 該對照**已失其必為零之性質**。
本批遂改用**執行期組出**之標籤——**其字面⛔ 出艙**（`GB-147`）。

**`ALPHA_LABELS` 之擴**（受詞 ＝ `verify/probes/probe_WG9269_pit_index.py`·**⛔ 生產碼 `34` 檔之一**）：

| # | 處 | 改後 |
|---|---|---|
| `1` | `ALPHA_LABELS` 之末元 | `"bc",` ＋ 新列 `"bd"])  # 🆕 \`W-G.9-278\` §三 所准之一坑` |
| `2`／`3` | `run(...)`／`issuer_frame(...)` 之題 | `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bd\`` |

🔒 **判別力（先實測後填·⛔ `ast.literal_eval`：其只及字面 ⇒ 以 `compile` + `exec` 單一 `Assign` 取語法真值）**：
**成員數 `55` → `56`（`+1` ✅）**｜**尾元 ＝ `'bd'` ✅**｜前一元 `'bc'`｜**重複元 `0`** ✅；
**新形框字串命中 `2`／舊形（`bc`）殘留 `0`** ✅。
🔒 **總數之算式**：`NUM_LABELS` **`26`** ＋ `ALPHA_LABELS` **`56`** ＝ **`82`** ⇒ 與單 `§五-5` 之期值 **`82`／`82`** 一致。

---

## `§五`　工項 `5`：「虛胖陷阱」錨腐之登記（末端追加·**⛔ 鑄號·⛔ 修**）

節題由 `sed -n '121,121p'` 之輸出**逕行落檔**（列數 `1`／位元組 `193`／
`sha256` `7218f9df229c357f34429df4d0864a1f6244b0cbc258c1a3420f1dedb8478af5`）。

🔒 **`app.py` 全檔 `虛胖陷阱` 之命中 ＝ `2` 列·<u>皆為引用</u>·⛔ 定義段**（`:10191`／`:10222`）。
🔴 **錨腐之實測**：`app.py:10222` 與 `docs/rulings/K-6_…md:890` 二處所引之 `` `:9344` ``，
其**現行內容逐字** ＝ `故本契約靠**呼叫端正確**＋下列 assert 之粗篩，**不可倚賴函式自行糾正**。`
——屬 `_corner_buffer_S` 之**方位契約**段，**與「虛胖陷阱」⛔ 相關**。
🔒 其真正之定義文字在 `docs/配地計算總規格_v3.md:40`／`docs/specs/W-G.4_規格v3補丁六…:10`／
`docs/specs/W-G.4_規格_v3.md:294`·`:384`／`.claude/skills/cad-layer-semantics/SKILL.md:28`。
🔒 **判別力**：人造同形字樣（執行期組出）@ `app.py` ＝ `0`；對照組 `虛胖` @ `app.py` ＝ `3` 列 ⇒ 器非紅。
🔒 **`K-9-5-6` 之逐字規範本身⛔ 因錨腐而失效**——其係**正文**、⛔ 繫於該錨。
🛑 **本節只登記**；二處之錨屬 `app.py`（生產碼 `34` 檔）與 `K-6`（正典），**皆⛔ 在本批之授權內**。

---

## `§六`　本批之**自解清單**（`作業常規之追加五 §一` 款 `5`）

| # | 疑義逐字 | 二個以上之讀法 | **所取者** | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§四-1`「情形之判別以 `verify/out/got_抵費地_退縮*.csv` … 為據」——該檔**⛔ 在倉亦⛔ 在工作區** | `(a)` 停機請補；`(b)` **先跑 `run_verification.py` 產之**，再與其**同一產生者**（`run_corner_pk` 之 `_off`）逐列對拍 | **`(b)`** | `.gitignore:30` ＝ `verify/out/got_*`（刻意忽略·⛔ 缺失）；`verify/run_verification.py:604` ＝ `_dump_csv(off, …f"got_抵費地_退縮{tag}.csv")` ⇒ **`_off` 即其被 dump 之物**；二者列數逐情境相同（`1`／`1`·`2`／`2`） | `(a)` 過度——該檔係**可重生**之產物，其缺席⛔ 缺失（`CLAUDE.md` 之 `got_*` 族逐字） |
| `2` | 單 `§四-1`「凡其呼叫端經框內省判定為鏈頭者，以 `rw_increment(0.0, W_cur)` 代之」——未限定其**段**，而 `run_corner_pk` 段之框內省**不可判** | `(a)` 於不可判處**亦施**替身（賭其為甲）；`(b)` **跳過並 loud 計數** | **`(b)`** | 不可判之計數 ＝ `862`／`898`（**全數落於 pk 段**·`hit_pk = 0`）；已逐格出艙 | `(a)` 於**不可判**處施加替身 ⇒ 可能誤及**情形乙**（單 `§七-5` 明令⛔ 模擬）⇒ **⛔ 可逆之風險** |
| `3` | 單 `§四-1` 判別力之**內部互斥**（`W_0 > 1e-6` 帶號 vs 其所引「`14` 格」＝ `\|W_0\| > 1e-6`） | `(a)` 取帶號；`(b)` 取絕對值；`(c)` **二讀法並報並取較嚴者** | **`(c)`** | 帶號 ⇒ 造甲母體為空（【⛔ 可得】）／絕對值 ⇒ 二造同色（器紅）⇒ 較嚴者 ＝ **器紅** | `(a)`／`(b)` 皆為**單擇一**，而 `常規二` 逐字令「取保守項續辦 ＋ **逐字回報該互斥**」 |

🔒 **三項皆滿足自解五要件**：零土地後果（皆量測／登記·**替身⛔ 入生產碼**）／零生產碼（`34` 檔一字未動）／
可機驗（證據欄·皆附必紅必綠二組）／可逆（新建檔 ＋ append-only ＋ 一處**非生產碼**探針之就地更正）／出艙具名（本表）。
🔒 **黑名單之逐項自檢**：⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何（**本批之替身僅於器內·⛔ 入倉**）；
⛔ 動生產碼一字（含註解）；二讀法之後果**⛔ 在土地上相異**；**⛔ 新訂準據**（`§二-7` 之四方向係**列其受詞供裁**·CC ⛔ 自選）；
⛔ 廢止或放寬任何既有之閘（**本批之閘紅即照紅出艙**）。

## `§七`　🛑 本批⛔ 為之事之逐項自檢（單 `§七`）

⛔ 動生產碼 `34` 檔一字／⛔ 改五個 `*_EXPECT`／⛔ 修 `GB-167`·`168`·`169`；
**⛔ 落地 `K-9-5-12`**（替身係量測用之反事實·**⛔ 寫入倉之生產碼**·⛔ 稱其為落地）；
⛔ 落地 `K-9-29` 任何子項／⛔ 落地 `GB-164`·`165`·`166`；
`GB-162` 三禁令維持（⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`）；⛔ 引任何自 baseline 取得之數；
**⛔ 模擬情形乙**（其 `3` 格逐格具名於 `§二-1`·器逐次跳過）／**⛔ 判 `K-9-5-12（五）3.` 之「未決」後半款**；
⛔ 判二解算器孰誤／⛔ 判 `_宗地寬度` 是否落入虛胖陷阱／⛔ 提「結構閘應放寬」之主張；
⛔ 呈 KL／⛔ 併線 `p3a`／⛔ 刪任何 ref；⛔ 於任何入倉文件宣稱一件尚未發生之事。

🔒 **鑄號之授權（單 `§六`）逐項核對**：本批鑄 **`GB-169`**（`1` 號）＋ **坑 `bd`**（`1` 號）
＝ **恰如授權**；`自誤`／`VR`／`K-9` 之相異與 MAX **一字未動**（收工閘實測）。

---

*本報告受詞 ＝ `W-G.9-278`。⛔ 呈 KL。*
