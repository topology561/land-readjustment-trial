# W-G.7／K-6-A2 **S6-2-C**：§一 假說**證偽** ＋ 12.85㎡ 逐項歸因（⛔ 生產碼零觸）

**施工單所載錨**：`9a58bb5`　**倉內實況（動筆前現查）**：`73e4dfb`
⚠️ **錨已過期** ⇒ 依 CLAUDE.md「編號／錨衝突一律以倉內為準」以現況為錨，並在此載明差異。
`9a58bb5..73e4dfb` 之異動 **4 檔**（皆文件／探針·`git diff --name-only 9a58bb5 HEAD`）。
**§八-1 對兩個錨皆成立**：`git diff -U0 9a58bb5 -- app.py` ⇒ **0 行**；`… 527dbdd …` ⇒ **0 行**。

**實料**：`verify/out/probe_K9_s62c_attribution.log`（本批新探針·全表十六格）
🔴 `K-9-5-3` 仍暫停適用；二則互斥標記**未動**。⛔ 本文**不裁定**、未寫任何判定碼。

> **一句話**：§一 假說**不成立**（6/12 格殘差超標）；而 12.85㎡ 之成因**已窮舉**——
> **98.7% 來自一個 KL 五項未列的第六項：`F` 槽餵錯量（路寬 vs 長度）**，且**正典已明訂何者為是**。

---

## 〇、🛑 停機上呈（**本批唯一之停機項**·詳 §四-3／登記表 `GB-41`）

**`_corner_block_true_G` 之兩個呼叫端把「側街*路寬*」塞進 `F` 槽**，
而 `g-formula-rules` 正典逐字定義：

> **F ＝ 街角地第 1 筆面臨側面道路之長度 ＝ SIDE_LINE 全長**（畫到未截角理論角）

⇒ **實配側（`右側長度(m)` ＝ 33.27）合正典；資格閘之真G（`路寬_m` ＝ 8.00）違正典。**
⇒ 側街負擔 `Rw×F×l_side` 被短計 4 倍 ⇒ **真G 系統性高估**。
🔴 **土地後果**：靶格真G 若改正 ≈ **228.42 < 門檻 232.79** ⇒ **資格閘不過**
⇒ 該街角改走**強制留設抵費地**，且 §二-1 之**互斥案將自然消失**。
⛔ **本批未改任何一行**——改之即動全部街角之 winner 集合。**修正之時機與波次屬 KL。**

---

## 一、§三 判準：**走「假說不成立」分支**（⛔ 未更換深度口徑以救之）

| 分支 | 條件 | 本批 |
|---|---|---|
| 假說**成立** | §二-1 逐格 ✅ **且** §二-3 殘差 ≤ 0.1㎡ | **未觸發** |
| 假說**不成立** | 任一格 ❌ **或** 殘差超標 | ✅ **走此支** |

- §二-1：**12/12 ✅**（見 §二-1）
- §二-3：**12 格中 6 格 `|殘差| > 0.1`** ⇒ **超標** ⇒ **假說不成立**

🔒 深度口徑於探針 **docstring 先行宣告**、⛔ 未由結果反推，且**證偽後亦未更換**。

---

## 二、§二 直接測（⛔ 自生產取值）

### 2-1 winner 實配多邊形面積 vs 三欄——**12/12 ✅**

**實配多邊形之來源（正面指名）**：

| 層 | 符號 | `file:line`（綁 `73e4dfb`） |
|---|---|---|
| g_rows 欄 | `cut_coords` | `verify/stepg_pipeline.py:381` |
| ← 解算回傳 | `_res['cut_coords']` ← `solve_G_binary` 之 `cut` | `app.py:9340` |
| ← 幾何 | `_block_strip(...)` ＝ strip ∩ **街廓多邊形**（**截角後**） | `grep -n "def _block_strip" app.py` |
| ⑧ | `G(㎡)` ＝ `round(_res['G'],2)` ＝ **G_target** | `verify/stepg_pipeline.py:370` |
| ⑨ | `幾何面積(㎡)` ＝ `round(_res['area_geom'],2)` | `verify/stepg_pipeline.py:369` |

| 判別 | 結果 |
|---|---|
| 實配多邊形面積 == ⑨`幾何面積(㎡)` | **12/12 ✅** |
| 實配多邊形面積 == ⑧`g_rows.G(㎡)` | **12/12 ✅** |

（容差 `0.015` ＝ 收斂 `tol 0.01`〔`app.py:9302`〕＋ 2dp 捨入 `0.005`。）
⇒ 假說之推論「`g_rows.G(㎡)` 就是 winner 實配多邊形之面積」**成立**
——惟嚴格說 ⑧ 係 **G_target**、⑨ 才是幾何面積，收斂使二者差 ≤ `tol`。
**十六格全表見 log【B】**（4 格無 winner ⇒ 強制留設抵費地·亦列）。

### 2-2 `L_in` 與 `L(t*)` 之間距 ＋ 近／遠 SIDE 側

| 靶格 `3.5m R1/right` | 值 |
|---|---:|
| 沿 `d̂` 之間距（＝ k98 量寬口徑） | **0.152969** |
| 真垂距（× `\|sin∠(ALLOC,d̂)\|` ＝ 0.996741） | **0.152470** |
| `ld(L_in)` ／ `ld(L(t*))` | 6.847031 ／ 7.000000 |
| **`L_in` 位置** | **近 SIDE 側** |

✅ **KL 之方向預測命中**（「`L_in` 在近 SIDE 側」）。
⚠️ KL 預測之寬度差 `0.152966`，**實測 `0.152969`**（差 `3e-6`）——以實測為準。
📌 **十二格中僅此一格為「近 SIDE 側」**，其餘 11 格皆「遠 SIDE 側」（log【C】）。

### 2-3 恆等式逐格（深度口徑：**k98 量測帶之帶深 `t_hi − t_lo`**）

| 情境 | 街廓/側 | 左式(實配−範圍) | 右式(Δ寬×帶深) | **殘差** | 判 |
|---|---|---:|---:|---:|---|
| 3.5m | **R1/right** | −5.0678 | −5.0674 | **−0.0005** | ✅ |
| 0m | R1/right | 110.8795 | 110.8719 | 0.0076 | ✅ |
| 3.5m | R4/right | 204.9070 | 204.8773 | 0.0297 | ✅ |
| 0m | R4/right | 320.7579 | 320.7160 | 0.0419 | ✅ |
| 3.5m | R6/right | 9.6418 | 9.5634 | 0.0785 | ✅ |
| 0m | R2/left | 56.5108 | 56.4253 | 0.0854 | ✅ |
| 3.5m | R4/left | 1269.9343 | 1269.7653 | **0.1690** | ❌ |
| 0m | R4/left | 1385.8620 | 1385.6581 | **0.2040** | ❌ |
| 0m | R5/left | 15.5903 | 15.3222 | **0.2682** | ❌ |
| 0m | R6/right | 161.9029 | 160.9076 | **0.9953** | ❌ |
| 0m | R1/left | 94.0065 | 99.2899 | **−5.2834** | ❌（🔴 G1 未過·另計） |
| 3.5m | R1/left | 345.6355 | 350.9379 | **−5.3025** | ❌ |

⇒ **`|殘差| > 0.1` 者 6 格** ⇒ §三 第二分支。

**⛔ 本批未歸因此 6 格之成因**（⛔ 不臆測）。可見之線索，**僅陳述、不下結論**：
① 恆等式係**平行平移**之一階式，其成立所繫為「兩形之非-ALLOC 邊界相同」；
② 兩個最大殘差（−5.28／−5.30）**皆為 R1/left**（跨兩情境），
   而 `0m R1/left` 之 **G1 閘未過** ⇒ 該列本即不可據；`3.5m R1/left` G1 過而仍 −5.3025。
③ 殘差與 `Δ寬` **不單調**（`0m R4/right` Δ寬 9.69 ⇒ 殘差 0.042；`0m R5/left` Δ寬 0.35 ⇒ 0.268）。

---

## 三、🔒 自我驗證閘（四道·⛔ 未過者已逐格標記）

| 閘 | 內容 | 結果 |
|---|---|---|
| **G1** | `min(弦@帶二端) == k98["min_width"]` | **11/12 過**；未過：`0m R1/left`（差 `1.370e-05`·前批已知） |
| **G2** | `min(線距@帶二端, SIDE∞ vs L(t*)∞) == T` ← 自檢① 之保證 | **8/12 過**；未過 4 格：`0m R1/left`／`0m R2/left`／`0m R5/left`／`0m R6/right` |
| **G3** | 實配槽重建 → G 之 2dp == `g_rows.G(㎡)` | **✅ 過**（`227.72 == 227.72`） |
| **G4** | 全 toggle 至樂觀 → G 之 2dp == 真G | **✅ 過**（`240.57 == 240.57`） |

🔑 **靶格 `3.5m R1/right` 之 G1／G2 皆過**（G2 為 `7.000000 == 7.00`）⇒ §二／§四 之靶格結論可據。
⚠️ **G2 之 4 格未過皆在 `0m`**，其中 `0m R6/right` 回 `0.000000` ＝ **重建取到退化邊**
（⛔ 非「構造未達 T」）⇒ **該 4 格之 §二-2 欄已標「🔴 閘未過·不可據」**。
⛔ **未放寬閘門以掩蓋**。G2 容差取 `1e-3` 之由已寫進 log（`rng` 末段經 `difference(chamfer_tri)`
⇒ 邊界頂點受截角切割之數值影響；⛔ 非自檢① 之 `eps`）。

---

## 四、§四 12.85㎡ 之逐項歸因（**單變因**·考古節 32）

### 4-1 逐槽對照（正面列舉·附 `file:line`）

| 槽 | 實配 | 樂觀 | Δ |
|---|---|---|---|
| `baseline_pt` | `corner_pt + actual_max_proj·d̂`（`verify/stepg_pipeline.py:679-681`·`_oblique_s_max` ＝ **87.715481**） | `corner_pt + s_max_left·d̂`（`app.py:11585`·`‖FRONT‖` ＝ **87.313031**） | **−0.402450** |
| `S_max` | `max(0.1, actual_max_proj − left_cum_S − right_cum_S)`（`:712`）＝ **70.135481** | `max(0.1, s_max_right)`（`app.py:11589`）＝ **87.715481** | — |
| `W_prev` | `_W_prev_right` 首宗 ＝ **0.0**（`:616`） | **0.0**（`app.py:11591`） | 0 |
| `buf` | `right_cum_S = _right_buffer_S`（`:607`）·本格 `forced=False` ⇒ **0.0** | **0** | 0 |
| **`F`** | `sb_row['右側長度(m)']`（`:548`）＝ **33.27** | `snapshot[blk]['右側']['路寬_m']`（`verify/selection_pipeline.py:437`）＝ **8.00** | **−25.27** |
| `l₁ 側尺度` | `右側尺度` ＝ 1.00 | 同鍵 | **非分歧項** |

### 4-2 逐項 ΔG（全精度）

| 項 | toggle 後 G | **ΔG** | 備註 |
|---|---:|---:|---|
| (a) forced 兩端假設 → 皆不預設 forced | 227.720000 | **0.000000** | 本格 `forced=False` ⇒ 恆等 toggle |
| (b) buf → 0 | 227.720000 | **0.000000** | `_right_buffer_S` ＝ 0 ⇒ 恆等 toggle |
| (c) `W_prev` → 0.0 | 227.720000 | **0.000000** | 首宗即 0.0 ⇒ 恆等 toggle |
| (d) `baseline_pt` 錨 → `corner_pt + ‖FRONT‖·d̂`（K-4 第 5 條） | 228.420000 | **+0.700000** | Δ錨 −0.402450 m |
| (e) `S_max` 上界 → `s_max_right` | 227.720000 | **0.000000** | 70.1355 → 87.7155（**惰性**·與 `app.py:11531-11533` 之實測相符） |
| **(f) 🆕 `F` 口徑 → `路寬_m`** | **240.400000** | **+12.680000** | **KL 五項未列之第六項** |

- **Σ(單變因 ΔG) ＝ `13.380000`**　vs 實際落差 **`12.850000`**　⇒ **殘差 `+0.530000`**
- 全 toggle 一次到底 ＝ **`240.570000`**，`ΔG(全) ＝ +12.850000`

⚠️ **Σ ≠ 全 toggle 係預期**：`solve_G_binary` 之解對各槽**非線性**（`G_target` 與 `area_geom`
同時隨 `S` 變）⇒ 交互項不為 0。**`+0.53` 之殘差即 (d)×(f) 之交互項**，
⛔ **不得逕讀為「尚有未列之第七項」**——因 **G4 已閉合**（全 toggle 精確重現真G `240.57`）
⇒ **成因已窮舉**。

### 4-3 🛑 §四 停機條件之處理

施工單訂「殘差 > 0.1㎡ ⇒ 尚有未列之第六項成因 ⇒ 停機上呈」。
**本批確實找到第六項 ＝ `F` 口徑**（且它佔 12.85 之 **98.7%**）⇒ **依令停機上呈**（見 §〇）。
⛔ **未以「其餘為捨入」帶過**；⛔ 未改任何一行；已登記 **`GB-41`**（登記表 40 → **41** 列）。

---

## 五、§五 泛化檢查（十六格·⛔ 非只看爭議格）

| 類 | 格數 | 「樂觀 G − 實配 G」 |
|---|---:|---|
| **WINNER** | **12** | **+11.51 ～ +22.96** |
| 無 winner（強制抵費地） | 4 | — |

**同號情形（樂觀達標 ∧ 實配 < 街角規定範圍面積）＝ 僅 1 格：`3.5m R1/right 628(5)`。**

⇒ 依施工單之判別語：**僅此一格 ⇒ 可能與 R1 右之錨點敏感性有關**。
⚠️ **但本批之 §四 顯示錨點項僅 +0.70、`F` 項 +12.68** ⇒ 「錨點敏感性」**不足以解釋該格之落差**；
其所以**只有它同號**，是因為**只有它的 `實配 − 範圍` 為負**（−5.07）——
其餘 11 格之 `實配 − 範圍` 落在 `+9.64 ～ +1385.86`，**離門檻極遠** ⇒ 高估 12〜23㎡ 不足以翻邊。
⇒ **`F` 之錯是結構性（12/12 winner 全中），而「同號」之稀有性是邊界距離造成的。**
⛔ 二者**不可互推**；本節只陳述、不裁定。

---

## 六、§六 「撤銷」之爆炸半徑（⛔ 只查不改·**未實跑·未改動任何狀態**）

🔒 **前置碼面事實**：街角程序四段之關鍵詞於三個受檢檔（**正面列舉**·⛔ 非全倉排除式）
——「預掃」「全域排序」「逐一嘗試」「重排位次」「既不出也不進」「每候選只試一次」
「合併群」「上鎖」——**命中全為 0**（`app.py`／`verify/selection_pipeline.py`／`verify/stepg_pipeline.py`）。
⇒ **K-6-B（街角地分配程序四段本體）碼面零實作**（CLAUDE.md 進度表亦載「K-6-B ⬜ 未開工」）。

| 題 | 答 | 實據 |
|---|---|---|
| **6-1** `3.5m R1/right` 是否在段一即定案？ | ✅ **是**——且**必然**，因**段二／段三不存在於碼面**。現行分流只有二支：`app.py:11255` 閘過 ⇒ 三指數 PK 取 winner；無人達 ⇒ `app.py:11263-11266` 強制留設抵費地。**其合併機制從未啟動。** | log【G】關鍵詞表 |
| **6-2** 段一定案之「上鎖」在碼面何處？ | ❌ **碼面不存在**（「上鎖」「既不出也不進」皆 0 命中）⇒ **無「因此被上鎖之土地清單」可列**。 | 同上 |
| **6-3** 撤銷後多少候選進入段二全域排序？ | ❌ **無段二可進** ⇒ 該問於現行碼面上**不適用**。⛔ **未實跑、未改動任何狀態。** | 同上 |
| **6-4** 十六格之分流 | **段一定案 12 格／走到段三 0 格／強制抵費地 4 格**（段三恆 0）。逐格列示見 log【G】。 | log【G】 |

⚠️ **6-1 之 KL 前提須併記**：KL 更正之「全部候選失敗」係指「每一名候選都帶著合併群依段三集中規則
試過一輪後仍全部失敗」。**現行碼之「強制留設抵費地」不是那個意思**——它只是「無人過 G 門檻」
（`app.py:11263-11266`），**沒有任何合併嘗試**。⇒ 現行之 4 個強制抵費地格，
**依正典尚未走完段三，不得逕認為「已窮盡」**。
⛔ 本節**未提出任何處置建議**——撤銷與否屬 KL。

---

## 七、§七 併辦（純技術·已自行做完）

| # | 項 | 落點 |
|---|---|---|
| 1 | 失敗考古 **節 50**：節 40 之再現（`t = (P−F1)·b̂n`）＋**可執行檢查**（印 `g = d̂·b̂n` 並斷言 `<1e-12`，附 raise 片段） | `.claude/skills/failure-archaeology/SKILL.md`（節 49 → **50**） |
| 2 | `CLAUDE.md` 收 CC 自誌**逐字** | 自我驗證閘條之末（`grep -n "三次擋下之錯" CLAUDE.md`） |
| 3 | 前批 6 端（`d≈0` 近共線）之影響範圍 | log【H】 |

**§七-1 之次數考據（⛔ 未含糊帶過）**：倉內**可考者為二例**（節 40 原案 ＋ 本例）；
KL 稱「第三次」。⛔ 未作「僅二例」之否定性存在主張（行號衛生同族）——節 50 已載明此點。

**§七-3 之影響範圍**：該 6 端係前批 `probe_K9_s62_chord_vs_linedist.log` 之 **(b) 弦欄**。
**本批不引用該欄**——本批之弦一律由 `_chord_at`（`(s,t)` 解析）現算，並由 **G1** 逐格驗。
⇒ **影響範圍 ＝ 本批【B】【C】【D】【E】【F】之 0 格**；
⛔ **但該 6 端於前批報告仍不可據**（未因本批而恢復）。

---

## 八、§八 驗收

| # | 項 | 結果 |
|---|---|---|
| 1 | `git diff -U0 9a58bb5 -- app.py` | **0 行**（對 `527dbdd` 亦 0 行） |
| 2 | §二 四欄十六格全列；實配面積來源正面指名 ＋ `file:line` | ✅ §二-1 ＋ log【B】 |
| 3 | §二-3 深度口徑**先於**計算結果宣告 | ✅ 探針 docstring（⛔ 證偽後亦未更換） |
| 4 | §三 明標分支；未觸發者亦寫明 | ✅ §一（「假說成立」分支**未觸發**） |
| 5 | §四 五項 ΔG 逐項、Σ 與殘差全精度 | ✅ §四-2（**六項**·第六項為新發現） |
| 6 | §六 四題各有實據；6-3 確認未實跑未改狀態 | ✅ §六 |
| 7 | log 逐檔 `git add`（⛔ 禁 `-A`）；push 後附 SHA | ✅ 見聊天回報 |

## 九、⛔ 本批**未**做者（勿誤認已結案）

1. **`F` 之修正**——🛑 停機上呈中（§〇／`GB-41`）。
2. **§二-3 之 6 格超標成因**（⛔ 未臆測·§二-3 只列線索）。
3. **G2 未過之 4 格**（`0m`）之重建改良。
4. §二-1 之 🛑 兩則互斥、§二-3 之 S6-1(b) 去向：**仍待 KL**（本批未觸）。
