# `W-G.9-12`：裁定登記機制 ＋ 中間層前提實跑 ＋ `F.2`／`F.4` 靜態閘立閘

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg912-rulings`）
**基準**：`9c134f8`　**事前登記**：`docs/reports/W-G.9-12_前置登記.md`（commit `87d2ff8`）

> # ✅ **`P1`〜`P6` 全部成立；`Z-1`〜`Z-5` 皆未觸發。**
>
> 🔑 **三個核心結果**：
> ① **裁定有家了**——`docs/驗證裁定登記表.md`（錨 `【驗證裁定登記表】`）建立，
> 三則逐字登記；⛔ **未置於 `docs/rulings/`**（該處為權威序**第 1 級**之 KL 域裁定正典）。
> ② **`O-2` 實跑證成** ⇒ **【裁定 B】生效**，中間層 6 項改列可回收。
> ③ **`F.2`／`F.4 靜態閘` 已立閘**——⛔ **各自實跑證實**，⛔ 未引分類器、⛔ 未以 `F.3` 外推。
>
> 🔴 **一項現查推翻直覺**：三個靜態閘**並非同型同構**——`F.2` 之碼面多繞一個中介變數，
> 照 `F.3` 之形狀外推**會抽不到**（施工單 §8-1 所警告之陷阱，**確實存在**）。

---

## §1 `P1`〜`P6` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **P1** | ✅ **成立** | `F.2`／`F.3`／`F.4 靜態閘` **各自實跑**成功、違規皆**空**；⛔ 未引分類器（其已自宣「不得採信」） |
| **P2** | ✅ **成立** | CAD 管線**實跑跑完**（`harvest` → `build_pipeline` → `compute_total_burden_rate`），⛔ 未經 `run_step_g` |
| **P3** | ✅ **成立** | 三閘現況**皆綠**；竄改自檢**兩態異色**（合成含禁用名之原始碼 ⇒ 轉紅） |
| **P4** | ✅ **成立** | 判準**資料**複本 **0**（`sgC_rows` ⇒ 0）；`calc_a_prime` 之 2 命中經**逐一辨識皆為 docstring／print 文字** |
| **P5** | ✅ **成立** | 判定行 `diff` **rc=0**；`PASS 12／FAIL 22`；全檔**唯一非路徑差異** ＝ 改夾具之**描述文字**（`rc` 仍 `0`） |
| **P6** | ✅ **成立** | `VR-001`／`VR-002` 全倉**各唯一檔**命中 |

🔒 **依考古 69 修法 ④**，上列**每一項**皆附判別力自檢（見各節）——
⛔ 未只證其成立，**已證其會不成立**。

---

## §2 `O-1`：三個 `F.N 靜態閘` **各自實跑**（⛔ 不引分類器·⛔ 不以其一外推其二）

**產物**：`verify/out/probe_WG912_O1O2.log`。

| 閘 | 受測檔 | 禁用名 | 實跑違規 | 竄改自檢（合成含 `calc_a_prime(` 之原始碼） |
|---|---|---|---|---|
| `F.2 靜態閘` | `wf_f2.py` | 4 個 | **（空）** ✅ | ⇒ `['calc_a_prime(']` **✅ 具鑑別力** |
| `F.3 靜態閘` | `wf_f3.py` | 4 個 | **（空）** ✅ | ⇒ 同上 **✅** |
| `F.4 靜態閘` | `wf_f4.py` | 4 個 | **（空）** ✅ | ⇒ 同上 **✅** |

⇒ **`O-1` 成立**、**`P1` 成立**。

### 2-1 🔴 現查推翻「三者同型同構」之直覺（施工單 §8-1 所警告者）

`F.3`／`F.4` 之禁用名為**內嵌 tuple**：
`_fNhit = [f"{n}(" for n in (…四名…) if f"{n}(" in _wfN_src]`
而 **`F.2` 多繞一個中介變數**：
`_forbid = [f"{n}(" for n in (…四名…)]` ⇒ `_hit = [f for f in _forbid if f in _wf2_src]`

⇒ 若照 `W-G.9-10` 之 `_orig_f3_static_names()`（鎖 `_f3hit` 之 comprehension）外推，
**`F.2` 會抽不到** ⇒ 分類器之「同型同構」直覺**不成立**。
🔒 本批之抽取式改為**一份邏輯吃兩種形狀**（自 `results.append` 站點上溯其 `try`，
於 body 內找 `open(...)` 之受測檔 ＋ 唯一之字串 tuple ListComp），⛔ **未為每個閘各寫一份**。

---

## §3 `O-2`：CAD 管線於現況**實跑**跑得完（⛔ 非間接證據）

**取樣**（事前登記 §2-2 已寫死）：主樣本 ＝ **`v3 reverse-test③ 單位工程費 3500→3550 ⟹ 重劃總負擔率隨動`**。
**取樣理由**：其相依恰為 `ns`／`cb_by`／`snapshot`，**不含** `_ctx`／`stepg_ctx` ⇒ 對 `O-2` 最具鑑別力。

```
① harvest ⇒ ns 227 名
② build_pipeline ⇒ cb 11 塊、cad 鍵 14
③ compute_total_burden_rate（原快照）⇒ 率 ＝ 0.40712387
④ compute_total_burden_rate（單位工程費 3550）⇒ 率 ＝ 0.40781775
⇒ ✅ 管線跑完（⛔ 未經 run_step_g）⇒ O-2 成立
```

- 🔒 **判別力自檢**：餵壞輸入（`cb=None`）⇒ 實拋 `TypeError` ⇒ 「跑得完」之判定**非恆真**。
- ⚠️ **具名區分**（事前登記 §2-2）：該主判定之**斷言**（率隨動）亦成立
  ——⛔ **此項不構成 `O-2` 之判準**；判準只問「**管線跑不跑得完**」。
- ⚠️ **其餘 5 項未實跑**（⛔ 不以一項外推全部）。

⇒ **`O-2` 成立**、**`P2` 成立** ⇒ **【裁定 B】之生效條件達成**，中間層 6 項改列**可回收**。

---

## §4 裁定登記機制（甲組·本批首要交付）

**檔**：`docs/驗證裁定登記表.md`　**字樣錨**：`【驗證裁定登記表】`

| 項 | 內容 |
|---|---|
| **位置之由** | ⛔ **不置於 `docs/rulings/`**——該處為**權威序第 1 級**之 KL 域裁定正典；施工單方之裁定屬**驗證紀律層**，混入即為僭越 |
| **權威序聲明** | 檔頭就地寫明「**本檔不在權威序四級之內**；與四級任一級衝突時以權威序為準」 |
| **登記三則** | `VR-001`（`K5` 判準·**生效**）／`VR-002`（中間層判準·**條件生效 → 已生效**）／`VR-003`（`W-G.9-4b` 之 `S-2` 結案手段·**補登**） |
| **逐字** | 三則皆**逐字**登記（`VR-001`／`VR-002` 取自施工單 §2-1；`VR-003` 取自**倉內** `W-G.9-4b_前置登記.md` §4）⛔ 無改寫、⛔ 無摘要 |

### 4-1 掃描射程與其結果（⛔ 否定性宣稱·三項射程）

- **【目錄射程】** `docs/` 全樹 ＋ `CLAUDE.md` ＋ `.claude/skills/`（⛔ 不含 `verify/out/` 之凍存 log）。
- **【字樣射程】** 三式、⛔ 不預設批號：`裁定（claude.ai`／`claude.ai 裁`／`🔒 **裁定`。
- **【連接方式】** 各式獨立裸 grep（⛔ 無距離窗）。

**結果**：命中之裁定**多屬規格級**（`docs/specs/`·權威序第 3 級）或**域級**（`docs/rulings/`·第 1 級）
⇒ **不屬本檔範圍**；驗證紀律層而**無歸屬**者僅 `VR-003` 一則（已補登）。
另有三則**已有倉內歸屬**（皆在 `CLAUDE.md`）⇒ 於登記表以附表指路，⛔ **不重複登記**。
🔒 **判別力自檢**：以 `KL 裁` 重算 ⇒ **117 檔**命中 ⇒ 上開三式**非恆 0 命中**。

⚠️ **本次掃描⛔ 未涵蓋對話側**（依考古 61，對話側非倉內事實）；
若有裁定僅存於對話側，**本檔查不到、亦不得憑推想補寫**——其處置已寫入登記表末節。

### 4-2 `P6`：唯一定位

| 錨 | 全倉命中檔數 |
|---|---|
| `VR-001` | **1** |
| `VR-002` | **1** |
| `【驗證裁定登記表】` | **2**（登記表本身 ＋ 宣告該錨之本批事前登記·**屬預期**） |

🔒 **判別力自檢**：以 `裁定` 重算 ⇒ **310 檔**命中 ⇒ 「唯一命中」之檢查**非恆為 1**。

> 🔧 **加註（`W-G.9波` 交接文撰寫時·2026-08-12·⛔ 上表原句一字未刪）**：
> 上表之「**1**」係**本報告寫出<u>當時</u>**之值。此後 `VR-001`／`VR-002` 每被一份文件引用，
> 命中檔數即 **+1**（現查已為 **4** ＝ 登記表 ＋ 本批事前登記 ＋ 本報告 ＋ 交接文）。
> 🔒 **這不是 `P6` 破**——`P6` 之受詞為「**可由單一字樣錨定位**」，而**定義**該編號者
> 始終只有 `docs/驗證裁定登記表.md` 一處。⇒ **計數會長大、定位不會失效。**
> ⚠️ 此即 `W-G.9-11` 已記之「**母體隨自己寫的文件增長**」——⛔ 計數型宣稱須連**時點**一起走。
⇒ **`P6` 成立**（以 `VR-00N` 為每則之錨）。

---

## §5 丙組：`F.2`／`F.4` 立閘（併入既有夾具）

**落點**：`verify/fixture_g3_static_guards.py`（⛔ **未新建第二支**、⛔ **未新增第二份 AST 抽取邏輯**）。

### 5-1 四項聲明（⛔ 逐條·第 2、4 項不得留空）

| # | 答 |
|---|---|
| **1. 守哪個子集？** | `F.2`／`F.3`／`F.4 靜態閘` **之全部**——該三判定本身即純靜態（各讀其 `wf_fN.py` 原始碼、比對四個禁用名） |
| **2. 🔴 放棄哪個子集？後果？** | **本次擴充未放棄任何子集**（三個靜態閘之命題**完整**被守）。⚠️ 惟**本夾具整體**仍放棄：`G.2` 之 **(a) 曝出契約**（需上游世代）＋ `W-G.9-11` 全掃所列之**其餘未執行主判定**——**完全未守**，見該批之守備移交清單 |
| **3. 與 `except` 分支重疊？** | ⛔ **不重疊**。`except` 分支之受詞 ＝ **compute 失敗之訊息**（已由 `W-G.9-7` 凍存）；本閘之受詞 ＝ **命題本身之真偽** |
| **4. 另案修好之後會怎樣？** | **不變、仍綠**——三者受詞皆為**純檔案讀取**，與管線無關 ⇒ ⛔ **不會假紅**。⚠️ 屆時 `run_verification` 內之原判定亦將復活 ⇒ 同一命題兩處判定，**惟二者同源**（AST 抽取）⇒ **應併同檢討本夾具是否退場** |

### 5-2 `P4`：判準常數**零複本**

| 禁用名 | 於夾具內硬寫命中 | 辨識 |
|---|---|---|
| `calc_a_prime` | **2** | ⚠️ 逐一辨識：**皆為 docstring／print 之敘述文字**（`"""F.N 靜態閘：受測檔不得呼叫 calc_a_prime／三廢…"""`／`print("【G-1】…不呼叫 calc_a_prime／三廢")`）——**非判準複本** |
| `_allocate_three_tier_v1` | **0** | — |
| `_allocate_tier2_tier3_geometric` | **0** | — |
| `_inflate_a_for_orphan` | **0** | — |
| `sgC_rows`（`G-3` 之判準鍵） | **0** | ⇒ 證抽取式**確未持有複本** |

⇒ **判準<u>資料</u>複本 ＝ 0**、**`P4` 成立**。
🔒 **判別力自檢**：上式對 `calc_a_prime` 得 **2** ⇒ **非恆為 0**（若真有複本會被抓到）。

### 5-3 檔名與描述之處置

- **檔名保留** `fixture_g3_static_guards.py`（仍含 `g3`）：⛔ **未更名**——該檔名已被
  `W-G.9-10` 報告與 `run_all` 引用，更名即使既有引用失準（倉規「行號衛生」之精神：**止血不回溯**）。
  ⇒ **docstring 已就地說明檔名之歷史**。
- **docstring 射程已同步更正**（五處）：標題、涵蓋範圍、四項聲明表 1、放棄清單、小結。
- **`run_all` 之描述文字已同步更正**（`P5` 之唯一容許差）。

---

## §6 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未立中間層 6 項之閘**（縱使【裁定 B】已生效——立閘屬下一批）。
2. ⛔ **未查 `_w4` 來源**（`W-D.4 MinA_區 reverse-test` 維持**未定**）。
3. ⛔ **未解除明細截斷**、⛔ 未修任何既有 FAIL。
4. ⛔ **未重烤任何基準**、⛔ 未重凍任何凍存物、⛔ 未改期望 FAIL 名單
   （`git diff --numstat -- verify/baselines/` **空輸出**）。
5. ⛔ **未把任何裁定寫入 `docs/rulings/`**。
6. ⛔ **未動** `app.py`／`verify/run_verification.py`／`verify/wv_reconcile.py`／三支端到端複本
   （皆 `git diff --numstat` **空輸出**）。
7. ⛔ **未實跑中間層之其餘 5 項**（只跑主樣本·已正面聲明）。
8. ⛔ **未追改**歷史記錄之舊錨與舊雜湊。

---

## §7 後批**優先序**（⛔ 只排序·不實作）

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | **中間層 6 項之立閘** | 【裁定 B】已生效；其中 `v3 率分項錨`／`v3 率用 DXF 公設實值` **土地後果最近**（率直接決定 A/B/C）。⚠️ 立閘前**宜先實跑其餘 5 項**（本批只跑 1 項） |
| **2** | **`W-G G.1` 之上游 `_ctx["0m"]`／結構閘另案** | **一修即解 65＋ 項之阻塞**；且三支端到端複本會跑更遠 ⇒ 其夾具依設計轉紅 ⇒ 屆時一併更新凍存 |
| **3** | **查明 `_w4` 來源並歸類** `W-D.4 MinA_區 reverse-test` | `W-G.9-11` 未定之唯一項 |
| **4** | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」 |
| **5** | **解除明細截斷（12 列上限）** | 必使現行名單全面失效、須重凍 ⇒ 與序 2 之重烤時點協調 |
| **6** | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

⚠️ 序 2／5 之前置含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §8 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ python verify/probes/probe_WG912_O1O2.py | tail -3
【判定】`O-1` ＝ ✅ 成立｜`O-2` ＝ ✅ 成立

$ python verify/fixture_g3_static_guards.py | tail -3
✅ 夾具 PASS（rc=0）

$ grep -n "fixture_g3_static_guards" verify/out/WG912_runall.log
44:  ✅ 末端夾具 fixture_g3_static_guards.py（🆕 W-G.9-10/9-12 不依賴管線之主判定…）rc=0

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG911_runall.log)        <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG912_runall.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG912_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG912_runall.log ⇒ 22

$ grep -c "sgC_rows" verify/fixture_g3_static_guards.py            ⇒ 0
$ grep -c "calc_a_prime" verify/fixture_g3_static_guards.py        ⇒ 2（皆 docstring／print）
$ grep -rl "VR-001" . --exclude-dir=.git --exclude-dir=worktrees | wc -l ⇒ 1
$ grep -rl "VR-002" . --exclude-dir=.git --exclude-dir=worktrees | wc -l ⇒ 1

$ git diff --numstat -- app.py verify/run_verification.py verify/wv_reconcile.py       verify/baselines/ verify/wg_g*.py   ⇒ （空輸出）
```

⚠️ **依倉規（不可變文件不記可變值），本報告⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**

### 8-1 🔴 兩項自誌

| # | 症狀 | 若未察覺之後果 |
|---|---|---|
| 1 | **首版整塊文字替換之修補完全未套用**（三個錨中某一塊不符 ⇒ 第一個 `assert` 中止、寫檔前退出）；⛔ 我**先以 `grep -c` 獨立驗落地**才發現（`STATIC_GATES` ⇒ 0） | 會以為夾具已擴充而據以出艙——**考古 68「工具回報 vs 實際落地」之同族**（此次是**我自己的**修補腳本） |
| 2 | 分類器把 `F.2`／`F.4` 判為「只被順序擋住」**看似可直接沿用** | 沿用即違反施工單 §1-1；且**現查證實 `F.2` 之碼面形狀不同** ⇒ 照 `F.3` 外推之抽取式**會抽不到**，會得到一個**抽取失敗**或**空清單**之閘 |

🔒 **共通**：①由**寫後獨立驗落地**抓到；②由**各自實跑**抓到。
⇒ 二者皆為「**⛔ 不以看起來一樣代替驗過一樣**」。

---

## §9 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `87d2ff8` — 事前登記（⛔ 零碼、零量測）
- **B** `3d6f0ae` — `O-1`／`O-2` 實跑 ＋ `F.2`/`F.4` 併入 ＋ 掛入 ＋ 長跑 log（⛔ 零文件結論）
- **C** 本報告 ＋ 裁定登記表 ＋ `GB` 登記
