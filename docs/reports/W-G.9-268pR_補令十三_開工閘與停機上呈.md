# `W-G.9-268′` 補令十三　開工閘 ＋ 🛑 **停機上呈**（工項零不過）

> 🔒 **本報告⛔ 鑄任何號**（`自誤 348`／`GB` 次號／`VR` 次號**皆未鑄**）。
> 🔒 **零生產碼**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py` 一字未動）。
> 🔒 **純新增檔**（`deletions ＝ 0`）。

---

## §零　三軸

| 軸 | 值 |
|---|---|
| `(1)` 來源 `commit` | `7ce0ff8f3acd7475b76ec0f59b5cd0da56d44e2b`（`git ls-remote origin refs/heads/wip/s1-endpart` **實查**·⛔ 採信本地 ref）|
| `(2)` 母體 | 逐節分列（追蹤 `.md` **`678`** 檔／`verify/**/*.py` **`286`** 支／各該登記表單檔）|
| `(3)` 粒度框 | 逐節分列（**檔框**／**列框**／**bytes**）|

🔒 **態之相符**：`origin` ＝ `7ce0ff8…` ＝ 本地 `HEAD`；`left-right` ＝ **`0	0`**；`git status --porcelain` **空**。
🔒 `7ce0ff8` 係 `62d7f70` 之**零生產碼後裔**（其唯一異動 ＝ `docs/reports/…補令十二…md` 之交接註追加）⇒ 與開場文「承 `62d7f70` 之後裔」**相符**。

---

## §一　交接註之逐坑覆述與當場對照（開工閘之前置·單所令）

🔒 **已讀之二份**：`docs/reports/W-G.9-268pR_補令五_記憶體側量測器.md` `§十一`（坑 `1`〜`10`）／
`docs/reports/W-G.9-268pR_補令十二_序1_2_GB161與c1synth定位之修.md` `§六`（坑 `11`〜`19`）。

🛑 **依 `自誤 347` 之通則**（「⛔ 以『已讀過該交接註』代替『已執行其攔法』」），
下表之「本窗當場對照」欄係**實際執行**之結果，⛔ 覆述。

| 坑 | 徵候（節略） | 攔法 | 本窗當場對照 |
|---|---|---|---|
| `1` | `cut_coords` 記憶體側 `list`／落檔側 `str` ⇒ 只吃字串之 parser 落靜默退路、母體悄悄為 `0` | 二形皆吃之取值器 ＋ 出艙母體基數 | **未觸及**（本窗未跑記憶體側量測器）|
| `2` | `probe_WG9268p_mem.py` 之 `argv[1]` 改變其找 `_partial` 之位置；`rc=3` ＝ **無從驗**⛔ 驗不過 | 跑前確認 `off` 態二 `_partial` 在位 | **未觸及** |
| `3` | 間諜掛點須在 `run_step_g` 前；`base_pt` 左右不同源 | 出艙呼叫次數 ＋ ⛔ 器內重導 | **未觸及** |
| `4` | 幾何為鍵時首頂點碰撞 ⇒ 數字全對、標籤全錯 | 證 `相異鍵數 ＝ 物件數` | **未觸及** |
| `5` | 二態對拍比到同一舊檔 ⇒ 偽「逐位相同」 | 先清 `got_G值_退縮*.csv` | **未觸及** |
| `6` | 缺號集自擬框漏**範圍框** ⇒ 偽多 `13` 個 | 一律用倉內既有器 `wg9268_gate5_tally.py` | ✅ **已執行**——四簿量全取自該器，⛔ 自擬框（`§二` 表）|
| `7` | `sed` 替換端轉義／`bash` 單引號內 `` \` `` 偽 `0`／heredoc 被硬阻斷 | ⛔ heredoc；`Write` 落檔後以路徑呼叫；含反引號者以 `python` 復算 | 🩸 **本窗親踩二次**：`(a)` heredoc 於首次嘗試即被 `wg9237_heredoc_guard` **硬阻斷**（如坑所載）⇒ 改 `Write` ＋ 路徑呼叫；`(b)` 見 `§三` 之量測器紅 |
| `8` | `harvest()` 回 `tuple` | `ns, fake_st = harvest()` | **未觸及** |
| `9` | `rc ≠ 0` ⛔ 等同受詞紅 | 讀 `rc` 同時必讀閘紅逐項 | ✅ **已執行**（`§二` 各器 `rc` 直取並同讀其輸出）|
| `10`／`11` | 倉內探針硬編他窗 `worktree` ⇒ `rc=0`、輸出完好、**量到他樹** | 跑**任何**倉內探針前 `grep "worktrees\|REPO *="`，**且**逐支查其**取得方式** | ✅ **已執行·三支皆查**（`§二`）——`wg9268_gate5_tally.py` 命中 `0`；`probe_WG9268p_gb161_sweep.py` 之 `REPO ＝ argv[1] or dirname(dirname(HERE))` ⇒ 倉相對；`probe_WG9268p_c1_synth.py` 之 `2` 命中**逐字查為註解**（`:22`／`:59`），其 `:26` ＝ `REPO = os.path.dirname(os.path.dirname(HERE))` ⇒ **倉相對·未被回退** |
| `12` | 跨二源碼列之路徑逃過 `worktrees[/\\]` | 用**裸字樣** ＋ 併查 `os.chdir(` | ✅ **已執行**（`§二`／`§三` 之框皆用裸字樣 `worktrees`，併查 `os.chdir(`）|
| `13` | 對照組只備「必不命中」⇒ 證不出撈得到本類 | **三造**（甲 必非零／乙 人造哨兵／丙 **本類已知成員**）| ✅ **已執行**（`§二`／`§三` 逐表皆列三造）|
| `14` | 哨兵之號寫成字面隨檔入倉 ⇒ 次批其真零失效 | 執行期組出、字面⛔ 出艙、以代稱 `⟨NEG⟩` 指之 | ✅ **已執行**（本報告之哨兵一律以 **`⟨NEG⟩`** 出艙·其字面⛔ 入倉）|
| `15` | `c1_synth` 造 `a`：`(a1)` 綠／`(a2)` 紅，係定位修好之**結果**⛔ 新破損 | ⛔ 改該造判準以就結果；`c3` 後二式俱須重擬·**候發單側** | 🛑 **即本批序 `2` 之受詞** ⇒ 見 `§四`（**⛔ 自裁**）|
| `16` | 活體檢取合成幾何 ⇒ 二態皆拋例外、同紅、本檢失能 | 改以**分支之被觀測**為可觀測量 | **未觸及**（序 `3` 之受詞）|
| `17` | 「必紅」造可能**紅得不是地方** | 替身相依於**呼叫時**自 `ns` 取；逐造檢其紅之**理由** | **未觸及** |
| `18` | `a` emoji／`cp950`；`b` `rc` 經管線；`c` `argv` 契約；`d` 截斷 SHA；`e` 回傳契約；`f` `KeyError`；`g` heredoc；`h` `grep -P` | 逐項如該表 | ✅ **`a`**（全程 `PYTHONIOENCODING=utf-8`）／✅ **`b`**（`rc` **直取**·⛔ 經管線）／✅ **`c`**（用器前先讀其 `argv` 契約·`§二`）／✅ **`d`**（全 `40` 碼 `rev`）／**`g`** 見坑 `7` |
| `19` | `Σ池` 取「街廓面積 − `ΣG`」⇒ 殘差恆零、閘恆真 | 取獨立量測之池 | **未觸及** |

---

## §二　起手清單 `1`〜`5`（承坑 `11`〜`19` 之末節）—— **五項全 ✅**

| # | 受詞 | 期望（交接註所載） | 本窗實測 | 判 |
|---|---|---|---|---|
| `1` | 倉態 | `left-right ＝ 0	0`／`status` 空 | `0	0`／空 | ✅ |
| `2` | `app.py` blob bytes | `1458029` | `1458029` | ✅ |
| `2` | `app.py` blob `sha256` 前 `16` | `9873b168278fbc08` | `9873b168278fbc08` | ✅ |
| `2` | `app.py:9269` 逐字 | 須含 `'WG9268P_ANCHOR_GEOM', '1'` | `    if _os_a.environ.get('WG9268P_ANCHOR_GEOM', '1') != '1':` | ✅ |
| `3` | 自誤 相異／MAX／缺號 | `340`／`347`／`[106]` | `340`／`347`／`[106]` | ✅ |
| `3` | `GB` 相異／MAX／缺號 | `159`／`161`／`[12, 87]` | `159`／`161`／`[12, 87]` | ✅ |
| `3` | `VR` 相異／MAX／缺號 | `80`／`95`／`[73, 75]` | `80`／`95`／`[73, 75]` | ✅ |
| `3` | `K-9` 相異／MAX／缺號 | `28`／`29`／`[]` | `28`／`29`／`[]` | ✅ |
| `4` | `c1_synth` 之 `REPO` | 須為倉相對（`dirname(dirname(HERE))`）| `:26` 逐字 ＝ `REPO = os.path.dirname(os.path.dirname(HERE))` | ✅ **未被回退** |
| `5` | 七支 ＋ 共用模組之 `assert_live` | 各命中 `1` | 八檔**逐檔皆 `1`** | ✅ |

🔒 **`3` 之器** ＝ `verify/probes/wg9268_gate5_tally.py`（倉內既有·⛔ 自擬框·坑 `6`）；
`argv[1] ＝ 7ce0ff8f3acd7475b76ec0f59b5cd0da56d44e2b`（全 `40` 碼·坑 `18-d`）；`rc` **直取** ＝ `0`。
🔒 **`5` 之八檔**：`probe_WG9268p_c1_synth.py`／`_c2_gates.py`／`_c2_v1prime_mem.py`／`_c2_v1prime_pert.py`／
`_conserv.py`／`probe_WG9268p_mem.py`／`_offsrc.py` ＋ 共用模組 `wg9268p_selector_liveness.py`。

---

## §三　🛑 **工項零不過**：`補令十三` **⛔ 在倉**，其受詞之本體亦⛔ 在倉

### 三-1　量測（**三軸** ＝ `7ce0ff8…`／追蹤 `.md` **`678`** 檔〔讀不到 **`0`**〕／**檔框 ＋ 列框**）

🔒 **框之形**：純子字串 `s in line`（**⛔ shell `grep`**·坑 `7`）；
依 `CLAUDE.md`「常規四（七）補款二 之更正」（`GB-158`）**B 形 ⋀ C 形並取**，鬆框僅作漏框偵察。

| 受詞 | 角色 | B 形 檔/列 | C 形 檔/列 | 鬆框 檔/列 |
|---|---|---|---|---|
| `補令十三` | **受詞（單本體）** | **`0`／`0`** | — | — |
| `自誤 348` | **受詞（待鑄）** | **`0`／`0`** | **`0`／`0`** | **`0`／`0`** |
| `自誤 347` | `(甲)` 必非零 | `0`／`0` 🔴 | **`3`／`8`** ✅ | `4`／`10` |
| `自誤 346` | `(甲2)` 必非零 | `0`／`0` 🔴 | **`4`／`15`** ✅ | `4`／`15` |
| `GB-161` | `(丙)` **本類已知成員** | — | **`4`／`23`** ✅ | — |
| `⟨NEG⟩` | `(乙)` 人造哨兵 | `0`／`0` ✅ | `0`／`0` ✅ | `0`／`0` ✅ |

🩸 **本窗親踩之量測器紅（坑 `7` ＋ `GB-158`·⛔ 出艙前自捕）**
首版以 **B 形**（`` 自誤 `347` ``·反引號只包號）取甲造 ⇒ 得 **`0`** ⇒ 依**常規四（七）補款二 款二**
「對照組得數為 `0` 者，一律先判**量測器紅**」**先判器紅**、⛔ 逕以受詞紅出艙。
現查其真形為 **C 形** `` `自誤 347` ``（反引號包**全 token**·`:8147` 逐字
`### 🩸 \`自誤 347\`　**列定判定集而未執行倉內既有交接註所載之攔法**`）
⇒ **恰為 `GB-158` 所載之情狀**（「B 形於本倉對已知占用之對照組多得 `0`」）。
🔒 **改 B ⋀ C 並取後甲造 ＝ `3`／`8`、乙造 ＝ `0`** ⇒ 二造皆如預期、**器非紅**，上表方出艙。

### 三-2　判

🔴 **`補令十三` 於倉內⛔ 存在**（檔名命中 `0`；內容檔框／列框皆 `0`；兩造對照皆如預期）。
⇒ **`作業常規之追加四` 款 `一` 之工項零（原封入倉 `docs/orders/`）⛔ 可執行** ——
無原文可入倉，`sha256` 逐位驗**無受詞**。
🛑 依同節款 `二`：**「🔴 該工項不過即停，⛔ 續辦其餘工項。」**

---

## §四　三工項之可辦性（逐項·照實）

| 序 | 受詞 | 所需之本體 | 倉內現況 | 判 |
|---|---|---|---|---|
| `1` | 鑄 `自誤 348` | 其**形／通則／責**之逐字（體例 ＝ `補令十二 §四`）| **⛔ 在倉**（`§三-1`）| 🛑 **⛔ 可辦** |
| `1` | `GB-161` **母體更正** | 更正之**受詞與方向**之逐字 | **⛔ 在倉** | 🛑 **⛔ 可辦**（惟已備妥其**實測依據**·見 `§五`）|
| `2` | `c1_synth` 造 `a` **步一** | 「步一」之逐字界定 | **⛔ 在倉**（`造 \`a\` 步一` 命中 `0`）| 🛑 **⛔ 可辦** |
| `3` | `c3`（側支·生產碼）| 補令十一 `§三` ＋ 起手清單 `6` | ✅ **在倉** | 🛑 **⛔ 得先辦**——單令「序 `3` 之前⛔ 動 `app.py` 一字」，且坑 `15` 明載 `c3` 之**必辦項**含造 `a` 二式之重擬，其處置**候發單側**（＝ 序 `2`）⇒ **序 `3` 結構上下游於序 `2`** |

🔒 **⛔ 編造、⛔ 以倉內近似物代替**（`施工單必須自足` 之**判準（給 CC）**·失敗考古節 `52`）。
🔒 **⛔ 自行鑄號**（`補令十二 §五` 逐字：「若查得新阻塞項或新驗證裁定之需 ⇒ **停機上呈**，⛔ 自行鑄號」）。
🔒 **本窗⛔ 查前窗之對話**（單令）⇒ 開場文所載之通則**⛔ 得作為 `自誤 348` 之本體**——
其為**摘要**，而鑄號所需者為**逐字本文**（體例見 `docs/reports/W-G.4_泛用阻塞項登記表.md:6092` 逐字
「**受詞逐字轉錄自** `docs/orders/…補令十二…md` 之 `§二-2`（**⛔ 改寫**）」）。

---

## §五　🔑 **序 `1`「`GB-161` 母體更正」之實測依據**（⛔ 更正本體·只備料）

> 🛑 **本節⛔ 更動 `GB-161` 一字、⛔ 鑄號、⛔ 作成任何更正**；
> 其係**純量測**之出艙，供發單側據以撰其更正之逐字。

### 五-1　所發現者

🔴 **`GB-161` 所載之全族掃描四數，於其自身所宣告之態上⛔ 可復現。**

**`GB-161` 之三軸逐字**（`docs/reports/W-G.4_泛用阻塞項登記表.md:6111`）：
> **三軸**：工作區（態 `blob@e835ed5` ＋ **本批之修**）／母體 ＝ **`verify/**/*.py` `286` 支**（⛔ `__pycache__`）／檔框 ＋ 列框。

| 樣式 | `GB-161` 所載（＝ 落檔 `WG9268pR_gb161_sweep.log`）| **本窗於 `7ce0ff8` 實測** | Δ |
|---|---|---|---|
| `worktrees` | 檔 `14`／列 **`27`** | 檔 `14`／列 **`28`** | 列 **`+1`** |
| `os.chdir(` | 檔 `8`／列 **`10`** | 檔 `8`／列 **`9`** | 列 **`−1`** |
| `git -C` | 檔 `18`／列 `30` | 檔 `18`／列 `30` | **`0`** |
| 絕對路徑 `C:/` 或 `C:\\` | 檔 **`7`**／列 **`9`** | 檔 **`6`**／列 **`8`** | 檔 **`−1`**／列 **`−1`** |
| `(丙)` 必命中對照之列 | **`1`** | **`2`** | **`+1`** |

🔒 **母體 `286` 支⛔ 變**（二態皆 `286`）⇒ **Δ ⛔ 母體之別**。
🔒 **`verify/` 於 `62d7f70`→`7ce0ff8` 逐位未變**（`git diff --numstat 62d7f70 7ce0ff8 -- verify/` **空輸出**），
且工作區對 `HEAD` **乾淨**（`git status --porcelain -- verify/` 空）⇒ **Δ ⛔ 態漂移或髒工作區**。

### 五-2　成因之**直接量測**（⛔ 算術推論·常規八「`N/N` 自證只證一致、⛔ 證正確」）

**法**：於 scratchpad 重建一棵樹，**只**將 `verify/probes/probe_WG9268p_c1_synth.py` 換為
`e835ed5`（**改前**）之版本，其餘一字不動；以該器之 `argv[1]` 指向之重跑。
🔒 **相異檔數 ＝ `1`**（`diff -rq` 之輸出恰 `1` 列·坑 `10` 攔法之形）。

| 樣式 | **重建之改前樹** 實測 | `GB-161` 所載 | 判 |
|---|---|---|---|
| `worktrees` | 檔 `14`／列 **`27`** | 檔 `14`／列 `27` | ✅ **逐位相符** |
| `os.chdir(` | 檔 `8`／列 **`10`** | 檔 `8`／列 `10` | ✅ |
| `git -C` | 檔 `18`／列 `30` | 檔 `18`／列 `30` | ✅ |
| 絕對路徑 | 檔 **`7`**／列 **`9`** | 檔 `7`／列 `9` | ✅ |
| `(丙)` 必命中對照 | 列 **`1`** | 列 `1` | ✅ |

🔒 **判別力（⛔ 恆值之自證）**：**同一支器**、**同一母體基數 `286`**，於二樹得**相異**之四數
（`27`／`28`、`10`／`9`、`9`／`8`、`1`／`2`）⇒ 該量測**確能區辨此二態**、**⛔ 恆值**。
🔒 **逐支之 Δ 歸因**（`c1_synth` 單檔·框逐字取自該器自身之 `PATS`·⛔ 另寫第二份定義）：
`worktrees` `1 → 2`（`+1`）／`os.chdir(` `2 → 1`（`−1`）／`git -C` `0 → 0`／絕對路徑 `1 → 0`（`−1`）
⇒ **與全族之 Δ 逐項對齊、零餘量**。
其逐字：改前 `:21` ＝ `REPO = (r"C:/Users/admin/Desktop/land-readjustment-trial/.claude/worktrees"`；
改後之二命中 `:22`／`:59` **皆為註解**（記述舊路徑），`:26` ＝ `REPO = os.path.dirname(os.path.dirname(HERE))`。

### 五-3　🔒 結論（**只陳述事實·⛔ 作成更正**）

> **`GB-161` 之三軸所載「態 `blob@e835ed5` ＋ <u>本批之修</u>」，其<u>前半為真、後半為偽</u>**
> ——四數（及必命中對照之列數）**係<u>改前</u>之樹所產**，**⛔ 含序 `2` 之修**。
> 其落檔 `verify/out/WG9268pR_gb161_sweep.log` 亦同（該落檔與重建之改前樹**逐項相符**）。

🛑 **本窗⛔ 作成下列任一**：⛔ 改 `GB-161` 一字；⛔ 改該落檔一字
（`補令十二 §三-1` 逐字「⛔ 追改其既有落檔一字」）；⛔ 鑄號；⛔ 判其為何類之自誤。
🔒 **其處置（更正之受詞、方向、是否鑄號）候發單側**——此即序 `1`「`GB-161` 母體更正」之受詞。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

```
git diff --name-only 7ce0ff8f3acd7475b76ec0f59b5cd0da56d44e2b..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```
⇒ **輸出為空（命中 `0` 行）** ⇒ **零生產碼 commit** ⇒ 依 `常規一 🔧 一`（KL `2026-08-31`）**逕行 `push`**。

🔒 **`②` 之驗**（`常規一 補正③` 款 `3`·⛔ 用 `--cached`）：本批 `git diff --numstat 7ce0ff8..<本 commit>`
之**刪除欄逐列為 `0`**（純新增 `1` 檔）⇒ `deletions ＝ 0`。

---

## §七　🩸 本批之自捕清單（⛔ 出艙前皆已攔下）

| # | 形 | 徵候 | 攔法 ／ 處置 |
|---|---|---|---|
| `1` | **heredoc** 誤用於 `Bash` | 被 `wg9237_heredoc_guard` **硬阻斷**（坑 `7`／`18-g` 所載之形） | 改 `Write` 落檔 ＋ 以**路徑**呼叫 |
| `2` | 取甲造用 **B 形**（`` 自誤 `347` ``） | 甲造得 **`0`** ⇒ **量測器紅** | 依 `常規四（七）補款二 款二`**先判器紅**；改 **B ⋀ C 並取**後甲造 `3`／`8` ⇒ 器非紅（`§三-1`）|
| `3` | 初讀 `GB-161` 表**誤判為子集** | 表列 `1`–`6` ＋ 一列 `\`7\`–\`14\``（**摺疊列**）⇒ 首版誤讀為「`7` 列 vs 宣告 `14` 支」 | **當場覆查逐列逐字** ⇒ 該表**確涵蓋全 `14` 支** ⇒ **撤回該讀法**、⛔ 出艙 |
| `4` | 成因之**算術推論** | Δ 對得上⛔ 蘊含成因為真（常規八）| 以**重建之改前樹**直接重跑（相異檔數 ＝ `1`）⇒ 逐項相符 ＋ 判別力自證（`§五-2`）|

---

## §八　🛑 呈發單側（**待補**）

1. 🔴 **`補令十三` 之原文**（含 `§一`／`§五`）——單所令之工項零須**原封入倉** `docs/orders/`。
2. 🔴 **`自誤 348` 之逐字本體**（形／通則／責）——體例同 `補令十二 §四`。
3. 🔴 **`GB-161` 母體更正之逐字受詞與方向**——其**實測依據已備妥**於 `§五`。
4. 🔴 **`c1_synth` 造 `a`「步一」之逐字界定**——坑 `15` 明載其處置**候發單側**、本窗⛔ 自裁。

🔒 **已辦畢且⛔ 待補者**：開工閘（起手清單 `1`〜`5`·**五項全 ✅**）／交接註逐坑之**執行**（`§一`）／
序 `1` 之**實測備料**（`§五`）。
🔒 **本報告⛔ 鑄號**；`自誤 348` 經查為**未占用**（`§三-1`·三形皆 `0`）⇒ 其號**仍可取**。

---

## §九　併記（`GB-132` 族·自指增長）

🩸 本報告 payload 自身含 `補令十三`／`自誤 348`／`GB-161`／`⬜` 等字樣
⇒ **落地後其命中數必增**。上開 `§三-1` 之各數係**立節當時（`7ce0ff8`）**之現態，
依 `常規四（九）五` **⛔ 追改**；爾後引用須當場復算。
🔒 **哨兵之號**依坑 `14`／`GB-147` **執行期自倉掃出**，其字面**⛔ 入倉**，一律以 **`⟨NEG⟩`** 指之。
