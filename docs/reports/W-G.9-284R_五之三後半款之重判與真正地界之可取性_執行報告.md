# `W-G.9-284R`　執行報告：`K-9-5-12（五）3.` 後半款之**重判** ／ 「真正的地界」是否為可取之物 ／ `自誤 387` ＋ 坑 `bh`·`bi`

> **單** ＝ `docs/orders/W-G.9-284_施工單_五之三後半款之重判與真正地界之可取性.md`（工項零已原封入倉）
> **分級** ＝ **中**（出處判定 ＋ 逐字量測 ＋ 登記·**零生產碼**·**⛔ 替身**·**⛔ 反事實**）
> **開工態** ＝ `05f220cce97bcaa86387e47fde62b2d945605575`（自行 `ls-remote` 實查·⛔ 採單之數）
> 🛑 **本報告一律「出艙即止」**——⛔ 判該款是否已了、⛔ 判規格是否構成要求、⛔ 判碼面是否違反、
> ⛔ 解除／收窄 `GB-158`／`GB-168`／`GB-169`、⛔ 鑄 `GB`／`VR`／`K-9`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§零`　開工閘（九項·**皆自行重跑**·⛔ 採單之數）

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `05f220cce97bcaa86387e47fde62b2d945605575`（**全 `40` 碼**比對·⛔ 短前綴） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `p3a` `05a11bd665f6c2790ff821c9e3a1606651e345d4` | `git merge-base --is-ancestor … HEAD` ⇒ `rc = 1` ⇒ **非祖先**；判別力[必為祖先] ＝ `32afbe2c345cb6a33a4b7bf220b83de546e09bce` ⇒ `rc = 0` | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑·受詞 `17940` B） | 實算 ＝ 單載 ＝ `b3f0ac653ef2d999e718fb20c49ceae0cc9794d402d0683dccb25e3545e9e956`；判別力造乙（受詞區 `+2` B 之人造擾動·經**同一器**）⇒ `🔴 不符`（實算 `776139484d88a1f6…`·受詞 `17942` B） | ✅ |
| `4'` | 受詞自證（坑 `bf`） | 所量之檔逐字出艙 ＝ `…\docs\orders\W-G.9-284_施工單_五之三後半款之重判與真正地界之可取性.md`；[必真] basename 含 `W-G.9-284` ＝ `True`／[必偽] 含 `W-G.9-283` ＝ `False` | ✅ |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | 本單 `rc = 0`（🔴 機械停機款 **`0`** 項／🟡 提示 **`0`** 項／ℹ️ 記錄 `2` 項：`P-5` ✅ 相符、`P-3` `def main` ＝ `14975`-`24579`）；`--selftest` `rc = 0`（四造：靶① `P-3` 紅✅／靶② `P-4` 提示✅／靶③ `P-6` 提示✅／反靶 `P-3` 不紅✅） | ✅ |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 ＝ **`1`** > 0／人造字樣 ＝ **`0`**） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 **`376`**／**`386`**（缺 `[106, 355, 356, 357]`）｜`GB` `167`／`169`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `28`／`29`（缺 `[]`）；坑 **`85`／`85`** ✅ 可解析·下一標籤 `bh` | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（`app.py` ＋ `verify/` **頂層** `*.py` `33`·逐名見 `§七`）；判別力[必不命中] `verify/` **子層** `.py` ＝ **`302`**（**判準式「須 > 0」**·⛔ 定值·`自誤 386`） | ✅ |
| `8` | `verify/baselines` | **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**·`59560` B）；併報 `=false` ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`（`35935` B） | ✅ |

🔒 **坑之量測（閘 `6` 之後半·器 ＝ `verify/probes/probe_WG9269_pit_index.py HEAD`）**
母體 ＝ `HEAD` 之追蹤 `.md` **`766`** 檔；標籤母體 ＝ 數字系 `1`–`26` ＋ 字母系 `a`–`z`＋`aa`–`bg`（＝ **`85`**）；
**`✅ 可解析` ＝ `85` 列 ／ 標籤總數 `85`**；判別力二造：造甲[必有定義] 坑 `n` ⇒ 定義處 **`6`** ≥ 1 ✅／
造乙[必為零] 人造之坑（**執行期組出**·字面⛔ 出艙·`GB-147`） ⇒ 定義 `0`／引用 `0` ✅ ⇒ **器非紅**。

🔒 **環境前置檢**
`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` ＝ **`0  0`**（⛔ 自 `main` 開窗）；
`.claude/settings.json` 與 `verify/tools/wg9237_heredoc_guard.py` **皆在**（heredoc 硬閘·本窗一律 `Write` 落腳本）；
`core.autocrlf` 分層 ＝ system `true`／local（`…/.git/config`）`false` ⇒ **有效值 `false`**；
`CLAUDE.md` 工作區 `236365` B ＝ blob `236365` B（`CR` `0`／`LF` `2944`）。

🔒 **號占用之預查**（`開場文 §六-4 ②`·`B`⋀`C`⋀`D` **三形分列並取**·母體 ＝ `HEAD` 追蹤 `.md` **`766`** 檔·
框 ＝ `git ls-tree -r --name-only -z`（**原始位元組**·⛔ 受 `core.quotePath` 影響·坑 `bc`）·粒度 ＝ **檔框 ＋ 列框**）

| 角色 | `B` 形 檔／列 | `C` 形 檔／列 | `D`（鬆·錨定框）檔／列 |
|---|---|---|---|
| 受詢 `自誤 387` | `0`／`0` | `0`／`0` | `0`／`0` |
| 受詢 坑 `bh` | `0`／`0` | `0`／`0` | `0`／`0` |
| 受詢 坑 `bi` | `0`／`0` | `0`／`0` | `0`／`0` |
| 對照甲 `自誤 386`[須 > 0] | **`0`／`0`** | `3`／`11` | `3`／`11` |
| 對照甲2 `自誤 385`[須 > 0] | **`0`／`0`** | `3`／`5` | `3`／`5` |
| 對照甲3 坑 `bg`[須 > 0] | `6`／`8` | **`0`／`0`** | **`0`／`0`** |
| 對照乙 人造哨兵[須 ＝ 0] | `0`／`0` | `0`／`0` | `0`／`0` |

🔴 **`GB-158` 案型之第四度再現（loud 具名·⛔ 逕判紅）**：`自誤` 簿之 **`B` 形**使**二對照組皆得 `0`**
⇒ 依 `GB-158` 施行 `二`，該形**⛔ 單獨採信**，判由 **`C` 形**承擔；而 `坑` 簿**恰相反**
——其 `C`／`D` 形皆 `0`、判由 **`B` 形**承擔。⇒ **二簿之「非空形」相反**，故**三形分列並取**係必要而非冗餘。
**逐筆歸類**（`裁 H`）：三個受詢號之三形**皆 `0` 命中** ⇒ **無可歸類之筆**，判 ＝ **未占用**。
🔒 `D` 形之界定：`常規四（七）補款一` 之**錨定框**（前後皆不得為數字／連字號／英數）——
本單 `§一-6` 令「`B`⋀`C`⋀`D` 三形」而倉內**查無 `D` 形之定義**（`三形分列並取` 全倉命中 `0`），
⇒ 依**自解款白名單 `7`**（框慣例之別 ⇒ **取較嚴框**）取**鬆框**為 `D`；具名於 `§八` 之自解清單。

---

## `§一`　`§零′` 與 `§一` 之受領（**⛔ CC 重辦**·僅登記）

發單側於單 `§零′` 已獨立復驗 `W-G.9-283` 三 `commit` 並**全批核可**；其 `§一` 六裁逐條受領，
其中**與本批有落地受詞者** ＝ `1`（`自誤 387` 之鑄）／`2`（三處逐處出艙）／`3`（`§五-3 (2)` 之收窄）／
`5`（坑 `bh`／`bi` 之鑄·坑 `3` 重踩⛔ 鑄）／`6`（`§五-3 (3)` 之體例化）。**`4` 之受詞 ＝ 本報告 `§四`**（⛔ 鑄 `GB-170`）。

---

## `§二`　工項零：**本單原封入倉**

🔒 **搬移法**：python 內以**完整檔名逐字指定**（⛔ 全 ASCII glob、⛔ 差集法）。
**搬移前之自證**（母體 ＝ 源目錄 `docs/orders/` **`146`** 項）：

| 角色 | 所指之檔數 | 期 |
|---|---|---|
| 受詢（本單完整檔名） | **`1`** | `1` |
| 造甲[必為 `1`]　`W-G.9-283` 之單 | **`1`** | `1` |
| 造乙[必為 `0`]　人造名（**執行期組出**·字面⛔ 出艙·坑 `bd`） | **`0`** | `0` |

⇒ **器非紅**（造甲非零 ⋀ 造乙為零）。

🔒 **三方逐位相同**

| 側 | bytes | `CR` | `LF` | `sha256` |
|---|---|---|---|---|
| 源（主 checkout 之 `docs/orders/`） | `18018` | `0` | `195` | `60ffa12e2088741ad4b717a12906efc91579e5f76855c42d791ffe7b4609e53c` |
| 磁碟（本 worktree 之落點） | `18018` | `0` | `195` | 同上 |
| 倉內 blob（`git cat-file blob`） | `18018` | — | — | 同上 |

blob `sha1` ＝ `ba9cc879b774417fa90720e4ee0f03fec285ef3a`。
**判別力**（證「`CR` ＝ `0`」之檢**非恆真**）：同一內容之人造 CRLF 版 ⇒ `CR` ＝ **`195`** > 0。

---

## `§三`　工項一（**主檢**）：`K-9-5-12（五）3.` 後半款之**重判** —— 🛑 **出艙即止**

🔒 **器 ＝ `verify/tools/wg915_ruling_provenance.py` 之同一機械規則**（`import` 其 `is_quote`／
`build_blocks`／`classify`／`KL_MARKS`·⛔ 另寫第二份判準·**⛔ 改該器一字**）。
🔒 **母體 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` 單檔**：**`299931`** B ／
**`4125`** 列（框 ＝ `split("\n")`；`wc -l` ＝ `4124`·差 `1` ＝ 末尾換行之框差）／`CR` ＝ `0`。
全檔**引文行** ＝ `703`／**引文區塊** ＝ `141` 段。

### 三-1　款 `1`　`K-9-5-13（三）新裁之效力` 第 `2` 項

🛑 **錨非唯一 ⇒ loud 具名（坑 `bi` 之攔法·⛔ 逕取 `hs[0]`）**：錨 `^#### （三）`（regex）⇒ **`1` 檔／`2` 列**

- `:1971`　`#### （三）裁示句　**⚠️ 經對話側送達·⛔ 本批無法自倉內覆核其為 KL 逐字抑或施工單方之複述**`
- `:2035`　`#### （三）**新裁之效力**　🔒 **已定案**（⛔ 本段與（四）分開讀）`

⇒ 依**本單受詞之逐字**（`新裁之效力`）取 `:2035`；節之界 ＝ `:2035`〜`:2052`（迄於次一 `^#### `）；
節內 `^2. ` 之命中 ＝ **`1`** 列。

| 欄 | 值 |
|---|---|
| ① 行逐字（`:2048`） | `2. **起算一律用真正的地界**；「面積換算出來的替身線」**在側街負擔上不再有角色**（裁定二）。` |
| ② 行首 `repr` | `'2. **起'`　｜ 行首位元組 ＝ `b'2. **\xe8\xb5\xb7'` |
| ③ 所屬區塊 | **⛔ 無**（`is_quote` ＝ `False`） |
| ④ 引言逐字 | （⛔ 不在任何區塊） |
| ⑤ **判** | **助手側補充**　｜ 由 ＝ 不在任何引文區塊內 |

### 三-2　款 `2`　`K-9-5-12（七）` **指路加註**

錨 `指路加註`（固定字串·**無 `^` 錨 ⇒ 二數並報**）⇒ **`1` 檔／`1` 列**（`:2004`）。

| 欄 | 值 |
|---|---|
| ① 行逐字 | `> 🔗 **指路加註（`K-9-5-13`·2026-08-12·⛔ 上文原句一字未刪）**` |
| ② 行首 `repr` | `'> 🔗 **'`　｜ 行首位元組 ＝ `b'> \xf0\x9f\x94\x97 **'` |
| ③ 所屬區塊 | `#59`（`is_quote` ＝ `True`） |
| ④ 引言逐字 | `- ⛔ 未處置後半句之 `forced` 情形（列為未決）。` |
| ⑤ **判** | **無法判定**　｜ 由 ＝ 在區塊內但引言不含 `KL_MARKS` |

### 三-3　款 `3`　`失效加註` **三處**（逐處並列·⛔ 以其一概括三處）

錨 `失效加註`（固定字串）⇒ **`1` 檔／`3` 列**（`:252`／`:276`／`:910`·**當場現查**·⛔ 採單之號）。

| 處 | ① 行逐字（節略至 `**`） | ② 行首 `repr` | ③ 區塊 | ④ 引言逐字 | ⑤ **判** |
|---|---|---|---|---|---|
| `:252` | `> 🛑 **失效加註（`K-9-5-13`·KL 裁定六·2026-08-12·⛔ 上列原句一字未刪）**` | `'> 🛑 **'` | `#0`（`is_quote` `True`） | `（立法理由：法規第四條的最小寬度與最小深度互相依賴，此帶為 KL 定義以打斷該循環。）` | **無法判定** |
| `:276` | `> 🛑 **失效加註（`K-9-5-13`·KL 裁定四、五·2026-08-12·⛔ 上列原句一字未刪）**` | `'> 🛑 **'` | `#1`（`is_quote` `True`） | `🔒 量測用之側界取**截角前**（§4 末段：角地應截角時，其寬度及深度係指截角前之寬度及深度）。` | **無法判定** |
| `:910` | `  > 🛑 **失效加註（`K-9-5-13`·KL 裁定二·2026-08-12·⛔ 上文原句一字未刪）**` | `'  > 🛑 '` | **⛔ 無**（`is_quote` `False`） | （⛔ 不在任何區塊） | **助手側補充** |

🔑 **三處之判 ＝ 無法判定 ／ 無法判定 ／ 助手側補充 ⇒ ⛔ 一致**（此**不一致本身即受詞之事實**）。

🔑 **成因（逐處具名·`§一` 裁 `2` 所令）**

| 處 | 行首縮排（`repr`／位元組數） | `is_quote` | 引言是否含 `KL_MARKS` | 落入之分層 |
|---|---|---|---|---|
| `:252` | `''`／`0` B | `True` | **否** | 無法判定 |
| `:276` | `''`／`0` B | `True` | **否** | 無法判定 |
| `:910` | `'  '`／**`2`** B | **`False`** | （不適用·無區塊） | 助手側補充 |

⇒ **二種成因並存**：`:910` 之相異來自**行首縮排 `2` 位元組**（規則第 `1` 款以行首為準）；
`:252`／`:276` 之相異來自**引言不含 `KL 逐字`／`KL 裁`**（規則第 `5` 款）。
🛑 **⛔ 以「三處皆助手側補充」或「三處皆無法判定」概括之。**

### 三-4　款 `4`　KL 裁定二**逐字**之射程（🛑 **⛔ 由此判其射程及於何款**）

錨 `**裁定二**：`（固定字串）⇒ **`1` 檔／`1` 列**（`:2026`）。其判 ＝ **KL 逐字**
（區塊 `#62`·引言 ＝ `#### （二）所答　**✅ KL 逐字**`·含 `['KL 逐字']`）。

**逐字** ＝ `> **裁定二**：「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」`

**其字面之字樣命中**（母體 ＝ **該一列**·`0` 亦出艙）

| 字樣 | 命中 | 字樣 | 命中 |
|---|---|---|---|
| `forced` | **`0`** | `替身線` | `1` |
| `buffer` | **`0`** | `真正的地界` | `1` |
| `起算點` | **`0`** | `側街負擔` | `1` |
| `起算` | `1` | `W_0` | **`0`** |
| `buf` | **`0`** | `第 1 宗` | **`0`** |
| `抵費地` | **`0`** | `第 2 宗` | **`0`** |
| `街角` | **`0`** | — | — |

🛑 **出艙即止**——上表**⛔ 構成**「該原則及於／不及於本款」之判。

### 三-5　款 `5`　判別力二造（**同一器**·須得**相異**之判）

🛑 **二錨皆非唯一 ⇒ loud**：`^#### （二）所答` ⇒ **`1` 檔／`2` 列**（`:1967`／`:2024`）；
`- 🔒 **首宗起點**` ⇒ **`1` 檔／`2` 列**（`:892`／`:1986`）。
⇒ [必為 KL 逐字] 之受詞取**裁定二行本身**（`:2026`·款 `4` 所定位者）；
[必為助手側] 取 `K-9-5-6` 之**定義列** `:892`（`:1986` 係 `K-9-5-12` 節內之**引述**）。

| 造 | 受詞 | 判 | 期 |
|---|---|---|---|
| 甲 | `:2026`　`> **裁定二**：「是 ⇒ 起算一律用真正的地界…」` | **KL 逐字** | KL 逐字 |
| 乙 | `:892`　`- 🔒 **首宗起點**：`W_0` ＝ MP→corner（**照實可負·⛔ 禁 clamp**）；forced 時為其 buffer 起算點。` | **助手側補充** | 助手側補充 |

⇒ **二造相異且各如期 ⇒ 判器非紅** ✅（⛔ 同判 ⇒ 本可判器紅、停、上呈）。

---

## `§四`　工項二：「真正的地界」於街角 `forced` 側是否為**可取之物** —— 🛑 **出艙即止**

### 四-1　款 `1`　整表 ＋ 其下全部 bullet 之**逐字**

母體 ＝ `docs/specs/W-G.4_規格_v3.md` **單檔**：**`90937`** B ／ **`668`** 列（框 ＝ `split("\n")`·
倉側 blob；`wc -l` ＝ `667`）。表頭錨 `| 類 | 例 | 精度 |`（去首尾空白之**全列**比對）⇒ **`1` 檔／`1` 列**。
框 ＝ **表 `:201`〜`:205`**　｜　**其下 bullet `:206`〜`:223`**（迄於次一 `^- **N0-`）。**逐列出艙（⛔ 摘）**：

```
:201|   | 類 | 例 | 精度 |
:202|   |---|---|---|
:203|   | **法定成果（顯示值）** | 抵費地／宗地**面積**（㎡）、宗地**寬度**、**宗地 S**（深度向尺寸·出報表欄） | **2dp**（N0-18） |
:204|   | **地籍界線** | 頂點**座標** | 座標精度（非 2dp） |
:205|   | **內部幾何構造參數** | **`buffer_S′`**（街角 forced 之保留帶寬）、切線座標 `t`、bisect 中間量 | **全精度·不捨入** |
:206| 
:207|   - **判準＝「它出不出報表欄」**：`buffer_S′` **不出報表欄**（forced 抵費地列之 `S(m)` 現值即 **0.0**·`stepg:748`）
:208|     → 其**法定產物**為「**抵費地面積（2dp ㎡）＋地籍界線（座標）**」，**buffer_S′ 本身非法定成果長度**。
:209|   - **∴ §N1 之 `tol 0.01` 照立**（依據＝**幾何精度上限**·非成果慣例）。
:210|   - **🔴 (b)「buffer_S′ 受 N0-18 拘束 → tol 改 0.005×深度」駁回**——**結論不變、理由已更正**：
:211|     ~~理由＝N0-10 為規制量、無差額地價找補可吸收；對照表：宗地 S 有找補機制 / buffer_S′ 無~~
:212|     **🔴 上開對照表全部作廢（KL 2026-07-17·補丁四 §一-3）**——其前提（「宗地 S 之 0.005×深度 由差額地價找補」）
:213|     係 **claude.ai 誤推**（差額地價之法定範圍僅及「未達最小分配面積 1/2」者·**不適用於已達可分配之宗**）。
:214|     **更正後之理由（簡潔且更強）**：
:215|     > **內部計算本即全精度，<u>無任何</u>構造參數受 2dp 拘束**（N0-18b-2）。
:216|     > 2dp **僅為顯示**；`buffer_S′` 不受拘束**不是例外**，而是**通則**之一例。
:217|     → **N0-18a 由「buffer_S′ 之例外」升格為「內部全精度<u>通則</u>」**（本條標題義隨之擴張）。
:218|   - **同源紀律延伸（本條之推論·S1 硬約束）**：**`buffer_S′` 之推進與實切必同源（全精度一致）**——
:219|     禁重演第四源（N0-18 推論一：推進吃捨入值／實切用未捨入 → 錯開 ≤0.005m）。
:220|   - **S0c 不重開**：**宗地 S 屬法定成果長度**之裁定**不變**（其出報表欄）。
:221|   - 溯源：CC S1 偵察上呈（`a74fc28` §5·撞出 §N1 tol 0.01 與 N0-18 於 buffer_S′ 上不可兼得＝量子/維度張力第六次現形）
:222|     → KL 2026-07-17 落章「(a)＋N0-18a 正典化」。
:223| 
```

### 四-2　款 `2`　🛑 **⛔ 判**其係精度分類抑或產物要求；僅出艙二者之逐字

- **表頭逐字** ＝ `  | 類 | 例 | 精度 |`（三欄名 ＝ `類`／`例`／`精度`）
- **分隔列** ＝ `  |---|---|---|`
- **bullet 之判準逐字**（錨 `判準` ⇒ **`1` 檔／`1` 列**） ＝
  `  - **判準＝「它出不出報表欄」**：`buffer_S′` **不出報表欄**（forced 抵費地列之 `S(m)` 現值即 **0.0**·`stepg:748`）`

### 四-3　款 `3`　**正典之查**

🔒 **母體（正面列舉檔數）**：`docs/specs/` **`35`** 檔／`docs/rulings/` **`1`** 檔／`CLAUDE.md` **`1`** 檔。
（`docs/specs/` 之 `35` 檔逐名見落檔 `verify/out/` 外之本批 scratch；其檔數係以
`git ls-tree -r --name-only -z docs/specs/` 之**原始位元組**計·`core.quotePath=false` 併釘。）

| 字樣 | `docs/specs` 檔／列 | `docs/rulings` 檔／列 | `CLAUDE.md` 列 |
|---|---|---|---|
| `地籍界線` | `1`／`2` | `1`／`2` | `1` |
| `法定產物` | `1`／`1` | **`0`／`0`** | **`0`** |
| `強制抵費地` | `7`／`10` | `1`／`12` | `2` |
| `forced 抵費地` | `2`／`5` | **`0`／`0`** | **`0`** |
| `街角 forced` | `4`／`5` | **`0`／`0`** | **`0`** |
| `抵費地之地籍界線` | **`0`／`0`** | **`0`／`0`** | **`0`** |
| `頂點` | `5`／`18` | `1`／`12` | `1` |
| `座標` | `13`／`37` | `1`／`9` | `2` |
| `多邊形` | `10`／`29` | `1`／`26` | `7` |
| `fb_polys` | **`0`／`0`** | **`0`／`0`** | **`0`** |
| `buf 帶` | **`0`／`0`** | **`0`／`0`** | **`0`** |
| `buffer_S` | `18`／`66` | **`0`／`0`** | `5` |

**逐處之出處判**（器 ＝ `classify`·🛑 ⛔ 判其是否構成要求）

| 處 | 出處判 | 行（節略） |
|---|---|---|
| `docs/specs/W-G.4_規格_v3.md:204` | **助手側補充** | `| **地籍界線** | 頂點**座標** | 座標精度（非 2dp） |` |
| `docs/specs/W-G.4_規格_v3.md:208` | **助手側補充** | `→ 其**法定產物**為「**抵費地面積（2dp ㎡）＋地籍界線（座標）**」，**buffer_S′ 本身非法定成果長度**。` |
| `docs/rulings/K-6_…md:3701` | **助手側補充** | `### 🔒 K-9-25　地籍界線與街廓邊界間之無地號縫隙**⛔ 作密合處理**（KL 裁 `2026-09-02`·canonical·逐字）` |
| `docs/rulings/K-6_…md:3710` | **助手側補充** | `**一（規則）**　重劃前地籍界線與街廓邊界之間若存在**無地號之縫隙**，於街角地評選之各項量測` |
| `CLAUDE.md:1263` | **助手側補充** | `🔒 **KL 裁 `2026-09-02`（`K-9-25`）**：地籍界線與街廓邊界間之**無地號縫隙⛔ 作密合處理**，` |
| `抵費地之地籍界線`／`fb_polys`／`buf 帶` | （母體內命中 **`0`** ⇒ 無可判） | — |

🛑 **出艙即止**——⛔ 由上表判「碼面是否被任何正典**要求**產出街角 `forced` 抵費地之地籍界線」。

### 四-4　款 `4`　收窄後受詞之重量

🔒 **受詞** ＝ 「**街角 `forced` 之 `buf` 帶**⛔ 具名多邊形物件」。
🔒 **母體（正面列舉檔數）** ＝ `verify/` 全部 `.py`（**含子層**）**`335`** 檔 ＋ `docs/specs/` `.md` **`35`** 檔。

- **正控[必命中]**：`fb_polys` @ `verify/**.py` ⇒ **`1` 檔／`7` 列** ≥ 1 ⇒ **器非紅**
  （`verify/wf_f4.py:1428`／`:1444`／`:1452`／`:1458`／`:1469`／`:1472`／`:1477`；
  `:1428` 逐字 ＝ `fb_polys = {_abate_key: _rend_poly}`）
- **[必為零]**：哨兵（**執行期組出**·字面⛔ 出艙·坑 `bd`）⇒ **`0` 檔／`0` 列** ✅

**逐樣式命中**（`0` 亦出艙）

| 樣式 | `verify/**.py` 檔／列 | `docs/specs` 檔／列 |
|---|---|---|
| `_corner_buffer_S` | `15`／`52` | `8`／`18` |
| `buffer_S` | `23`／`117` | `18`／`66` |
| `buf_poly` | **`0`／`0`** | **`0`／`0`** |
| `forced_poly` | `1`／`1` | **`0`／`0`** |
| `range_area` | `15`／`59` | `1`／`4` |
| `強制抵費地` | `27`／`86` | `7`／`10` |
| `forced` | `159`／`673` | `19`／`135` |

🔑 **`forced_poly` 之唯一命中須逐筆歸類（`裁 H`：命中數⛔ 為判準）**：
其落點 ＝ `verify/probes/probe_WG9283_band.py:207`，該列係**前批探針自身之受檢字樣清單**
（`"_off_poly", "forced_poly", "range_poly", "forced_offset",`）⇒ **⛔ 一個生產側之符號**，
係**量測器自身之字面**。🛑 **出艙即止**——⛔ 由此判該收窄後之受詞成立與否。

---

## `§五`　工項三：鑄號與登記

### 五-1　`自誤 387`（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**）

🔒 **payload 逐字全由單供**（`常規七 一`：由抽取管線直接產生·⛔ 重寫）：**單之列 `105`–`111`**
（**`7`** 列／**`930`** B／`sha256` `983c11694b57260c…`·三數與塊內文字**出自同一次抽取**·`常規七 二`）。
🔒 **標題形**（`常規四（九）四`：取既有最末一則逐字為範本·⛔ 自創形）：範本錨命中 **`1`** 列 ＝
`### 🩸 \`自誤 386\`　**以一個隨倉態漂移之量書為開工閘之「期值」**——…`；
前綴逐字沿用、**僅換號**（殘留檢：前綴內 `386` ＝ **`0`**／`387` ＝ **`1`**·坑 `bh` 之攔法），
受詞句**逐字取自 payload 首列**（迄於 `——`）⇒ 落地之標題 ＝
`### 🩸 \`自誤 387\`　**以「倉內有記載」充「KL 已裁」**`。

| 量 | 值 |
|---|---|
| 期初／期末 bytes | `811384` → `812393`（**`+1009`**／**刪除 `0`**） |
| 嚴格前綴 造甲 `new.startswith(old) ⋀ len(new) > len(old)` | **`True`**（期 `True`） |
| 嚴格前綴 造乙 `new.startswith(old + b'X')` | **`False`**（期 `False`） |
| `CR`／`LF` | `0`／`9604` |

### 五-2　二坑之鑄（**定義處 ＝ 本節**·`ALPHA_LABELS` 已同批擴）

🔒 **payload 逐字全由單供**：坑 `bh` ＝ 單之列 `115`–`119`（**`5`** 列／**`640`** B／`sha256` `fd61de748f2c67b6…`）；
坑 `bi` ＝ 單之列 `121`–`124`（**`4`** 列／**`507`** B／`sha256` `afbe9a2c1c863f74…`）。

#### 坑 `bh`　批次替換之覆蓋不全

**形／徵候／攔法**（逐字由 `W-G.9-284 §五-2` 供·⛔ 改寫）：

> **坑 `bh`　批次替換之覆蓋不全**：以批次替換更新單／腳本之態錨時——`(1)` 替換**順序相依**，
> 後一替換把**前一替換剛插入之字樣**一併改掉（致對照組失效）；`(2)` `40` 碼 `commit` 之替換
> **未覆蓋判準式內之短前綴**（如 `startswith("8d1e50e8")`）⇒ 判準式仍比對**舊態** ⇒ **量測器紅而印 `❌`**。
> **徵候**：二值**逐位相同**而閘印 `❌`；或[必為 `1`]之對照組得 `0`。
> **攔法**：逐替換報「期／實」**之外**，再驗**替換後之組成**，並出艙**舊字樣之殘留數（含短前綴）**須為 `0`。

#### 坑 `bi`　字樣錨非唯一而逕取首命中

**形／徵候／攔法**（逐字由 `W-G.9-284 §五-2` 供·⛔ 改寫）：

> **坑 `bi`　字樣錨非唯一而逕取首命中**：以字樣為錨取行時，該字樣於母體內命中 `> 1`
> （如縮排之**引述**與**正本**各一），而程式逕取 `hs[0]` ⇒ **取到錯的一行**，其下游之判**全數為偽**。
> **徵候**：二造之判**相同**（本應相異）；或受詞行之內容與所期之節不符。
> **攔法**：一切字樣錨**先驗唯一性**（`uniq()`），非唯一即 **loud 拒測**並出艙其全部命中列；⛔ 逕取首命中。

### 五-2-1　`ALPHA_LABELS` 之擴（🛑 **單內二讀法互斥·`常規二` 逐字回報 ＋ 取保守項**）

🩸 **互斥之逐字**
- `§五-2` 末：「於其末端 `"bf", "bg"` 之後**續列** `"bh", "bi"`」——若讀為「插入該 list 字面之內」，
  則**必須改寫**現有之 `                   "bf", "bg"])       # 🆕 \`W-G.9-280\` §三 所准之二坑` 一列
  （其 `])` 收尾）⇒ **`deletions` ≥ 1**。
- `§六` 閘 `1`：「**全 `0`**（`ALPHA_LABELS` 之擴係**續列**·⛔ **就地更正既有列**）」。

⇒ 二者互斥。**取保守項 ＝ 後者**（其保全之不變量較多：`deletions` ＝ `0` ⋀ 既有列一字未動），
以**新敘述**續之：`ALPHA_LABELS += ["bh", "bi"]       # 🆕 \`W-G.9-284\` §五-2 所准之二坑`。

| 量 | 值 |
|---|---|
| 錨之唯一性（坑 `bi`） | `"bf", "bg"])` ⇒ **`1` 檔／`1` 列**（`:62`）；判別力（寬字樣 `ALPHA_LABELS`）＝ **`3`** 列 ⇒ 該檢⛔ 恆為 `1` |
| 期初／期末 bytes | `15048` → `15126`（**`+78`**） |
| `numstat` | **`1`　`0`**（插入 `1`／**刪除 `0`**） |
| 既有列一字未動之機驗 | 去掉新列後與期初**逐位相同** ⇒ **`True`** |
| 替換後之**組成**（坑 `bh` 之攔法） | `"bf"` `1`／`"bg"` `1`／`"bh"` **`1`**／`"bi"` **`1`**／`"bj"` **`0`**；舊字樣 `"bf", "bg"])` 殘留 ＝ `1`（**期 `1`·該列⛔ 動**） |
| `py_compile` | **OK** |

🔒 **該檔位於 `verify/probes/`（子層）⇒ ⛔ 生產碼 `34` 檔之一**——其證見 `§六` 閘 `2`／閘 `6`
（`34` 檔 blob 期初＝期末·相異 `0`）與 `§七` 之正面列舉清單（該清單**不含**任何 `verify/probes/` 之檔）。

### 五-3　三則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·⛔ 鑄號）

🔒 **payload 逐字全由單供**：`(1)` ＝ 單之列 `132`–`134`（`3` 列／`398` B／`sha256` `b1d1fa1e8268401f…`）；
`(2)` ＝ 列 `136`–`139`（`4` 列／`436` B／`sha256` `18c9f4ba4707dfd6…`）；
`(3)` ＝ 列 `141`–`144`（`4` 列／`459` B／`sha256` `6fe1aafd0e810f29…`）。

| 量 | 值 |
|---|---|
| 期初／期末 bytes | `880009` → `882492`（**`+2483`**／**刪除 `0`**） |
| 嚴格前綴 造甲 | **`True`**（期 `True`） |
| 嚴格前綴 造乙 `old + b'X'` | **`False`**（期 `False`） |
| `CR`／`LF` | `0`／`7729` |
| `GB-132` 之避讓 | 本節凡以 `#` 起首者，其 `GB-` 後緊接之數字皆為 `169`／`158`（**皆已鑄**）⇒ ⛔ 造新號；其驗 ＝ `§六` 閘 `4` |

🔒 **`常規七 三` 覆核**（重跑抽取並與**已落地**之塊逐位比對）：六塊**重抽 ＝ 首抽**皆 `True`；
`自誤387`／`GB(1)`／`GB(2)`／`GB(3)` **已落地 ＝ `True`**（期 `True`）；
`坑bh`／`坑bi` **已落地 ＝ `False`**（期 `False`——其落點係**本報告**，於本節落地）。

---

## `§六`　收工閘（**入倉後末端追加**）

🛑 本節於**報告入倉後**以末端追加填入（`§八` 之格所令）——⛔ 於此預寫任何尚未發生之數。

---

## `§七`　🛑 **⛔ 為之事**之逐項自檢

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字 | ✅ 未動——母體**正面列舉**：`app.py`／`verify/app_harvest.py`／`b6_isomorphism.py`／`fixture_baseline_candidates.py`／`fixture_block_depth_n19p.py`／`fixture_cad_binding_order.py`／`fixture_corner_range_k8.py`／`fixture_e2e_termination.py`／`fixture_end_fallback.py`／`fixture_end_reserve.py`／`fixture_end_winner.py`／`fixture_g3_static_guards.py`／`fixture_klui_t2diag.py`／`fixture_midlayer_6items.py`／`fixture_n14_feed_chain.py`／`fixture_n14_min_width.py`／`fixture_wf_ns_wiring.py`／`fixture_yi_construction.py`／`run_all.py`／`run_verification.py`／`selection_pipeline.py`／`stepg_pipeline.py`／`test_corner_first_lot_G.py`／`wd3_fragment_geom.py`／`wd4_tier_list.py`／`wf_f0.py`／`wf_f1.py`／`wf_f2.py`／`wf_f3.py`／`wf_f4.py`／`wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py`／`wv_reconcile.py`（其驗見 `§六` 閘 `2`／`6`） |
| `2` | ⛔ 改 `wg915_ruling_provenance.py` 一字 | ✅ 未動（僅 `import` 其 `is_quote`／`build_blocks`／`classify`／`KL_MARKS`） |
| `3` | ⛔ 另寫第二份出處判準 | ✅ 未寫（`§三`／`§四-3` 皆呼叫該器之符號） |
| `4` | ⛔ 修 `GB-167`／`168`／`169` | ✅ 未修（`§五-3 (1)` 係**撤回一則倉態載述**·其逐字自載「仍⛔ 解除、⛔ 收窄」） |
| `5` | ⛔ 解除／收窄／結清 `GB-158`／`168`／`169` | ✅ 未為 |
| `6` | ⛔ 落地 `K-9-5-12`／`K-9-5-13`／`K-9-29` 之任何子項 | ✅ 未為（本批**零生產碼**） |
| `7` | ⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 未動 |
| `8` | `GB-162` 三禁令維持 | ✅ `WV_BAKE` 未設／`verify/baselines` 未覆寫（`298` 檔·框 `sha256` 期初＝期末）／`p3a` 未併線 |
| `9` | ⛔ 引任何自 baseline 取得之數 | ✅ 本報告之數皆出自當場量測，**無一** 引自 `verify/baselines` |
| `10` | ⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑 | ✅ 未施；所跑者 ＝ 單所具名之 `probe_order_preflight`／`wg9223_acceptance_audit`／`probe_WG9267_issuer_measurers`／`probe_WG9269_pit_index` |
| `11` | ⛔ 鑄 `GB`／`VR`／`K-9` 之任何號 | ✅ 未鑄（其驗 ＝ `§六` 閘 `4`） |
| `12` | ⛔ 動 `ALPHA_LABELS` 除續列二字外之一字 | ✅ 僅新增一敘述列；既有列一字未動（機驗 `True`·見 `§五-2-1`） |
| `13` | ⛔ 追改任何已入倉文件一字 | ✅ 三落點皆**嚴格 append-only**（造甲 `True`／造乙 `False`）；`W-G.9-282R`／`283R` 未動 |
| `14` | ⛔ 判「替身線」之所指／該款是否已了／規格是否構成要求／碼面是否違反／提修法主張 | ✅ 未判（`§三`／`§四` 各節末皆明載「出艙即止」） |
| `15` | ⛔ 呈 KL | ✅ 未呈 |
| `16` | ⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ 未為（`p3a` 仍**非祖先**） |
| `17` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§六` 留白待入倉後填；`ls-remote` 實查於 `push` 後方出艙 |

---

## `§八`　**本批之自解清單** ＋ **自捕清單**（⛔ 頂替）

### 八-1　自解（`常規五`／自解款·逐項五欄）

| # | 疑義逐字 | 二讀法 | 所取 | 機械證據 | 所棄之由 |
|---|---|---|---|---|---|
| `1` | `§一-6`／開場文 `§四-f` 之「`B`⋀`C`⋀`D` **三形**」——倉內**查無 `D` 形之定義**（`三形分列並取` 全倉命中 `0`） | (i) `D` ＝ 鬆框（裸號·錨定）／(ii) `D` ＝ 其他未具名之形 | **(i)** | 鬆框係**較嚴**（其命中為 `B ∪ C` 之超集：對照甲 `D` ＝ `3`／`11` ＝ `C`；坑 `bg` 之 `D` ＝ `0` ＝ `C`）⇒ 取之只會**更容易**判為已占用 | 自解款**白名單 `7`**（框慣例之別 ⇒ 取較嚴框續辦·⛔ 據此作成明文） |
| `2` | `自誤 387` 之**標題形**單內未供（payload 首列以 `——` 續一未完之句） | (i) 全句照抄（尾為「…據倉內三處記載，裁」）／(ii) 截至 `——` 前之粗體句 | **(ii)** | 二者皆**逐字取自 payload 首列**；(ii) 為其**前綴**且與範本 `### 🩸 \`自誤 N\`　**…**` 同構；形之自證見 `§六` 閘 `4` | 自解款**白名單 `3`**（標題形之別·**⛔ 改任何數**） |

### 八-2　自捕（**皆於出艙前攔下**·形／徵候／攔法）

| # | 形 | 徵候 | 攔法（已施） |
|---|---|---|---|
| `1` | **以 `\uXXXX` 手寫 CJK 碼位而記錯字**——`§五-3` 之節題「一字不**刪**」首版誤成**簡體「删」**（`U+5220` ≠ `U+522A`） | 🔴 **位元組數對它全盲**：二者皆 `3` B ⇒ `+2483`／`deletions 0` 之檢**逐位相同**、嚴格前綴檢**照樣綠** | 還原二簿（`git checkout --`）、腳本改為**一律 UTF-8 字面**（⛔ `\uXXXX`），並加**簡繁誤字機檢**（受詞 ＝ 本批**自撰**之框架文字·⛔ 含單之 payload）：`18` 個簡體字樣命中 ＝ `{}`／[必命中]對照組正體 `刪` ＝ `1` > 0 ⇒ 器非紅；入倉後全檔 `一字不删` ＝ **`0`**、對照組 `一字不刪` ＝ **`93`** |
| `2` | **錨非唯一而逕取首命中**（＝ 本批所鑄之坑 `bi`·**立戒者在下一批最容易違反自己的戒**之反面：本批即以其攔法行之） | `^#### （三）` ⇒ `2` 列；`^#### （二）所答` ⇒ `2` 列；`- 🔒 **首宗起點**` ⇒ `2` 列 | 一切字樣錨**先驗 `uniq()`**，非唯一即 **loud 具名並出艙全部命中列**，再依**本單受詞之逐字**擇之（⛔ `hs[0]`）——見 `§三-1`／`§三-5` |
| `3` | **量測器之框在本 shell 下失配**——以 `grep -cE '^   \| .?\`?[0-9a-z]+\`? \|'` 數坑表列得 **`0`**（反引號於單引號內之逸出） | [必非零]之量得 `0` ⇒ 先判**器紅**（`常規五`·坑 `3` 之族） | 改以 `grep -c '✅ 可解析'` ＋ 標籤母體基數（`26 + 59 = 85`）二路併證 ⇒ `85`／`85` |

🔒 **坑 `3` 之遵行**：本批**一切含反引號之文字**一律以 `Write` 落腳本承載，**⛔ 以 `python -c "…"`**。

---

## `§九`　一句話推導

> `K-9-5-12（五）3.` 後半款「已了」之三個倉內載體，經**同一機械規則**分層後為
> **助手側補充**（`:910` 之失效加註·行首 `2` 位元組縮排）／**無法判定**（`:252`／`:276`·引言不含 `KL_MARKS`）／
> **助手側補充**（`K-9-5-13（三）` 第 `2` 項·不在任何引文區塊內），
> 而**唯一判為 KL 逐字**者係裁定二行本身，其字面之 `forced`／`buffer`／`起算點` 命中**皆 `0`**
> ——**以上為出艙之全部；該款是否已了，繫發單側／KL，本報告⛔ 判。**

---

## `§十`　逐 `commit` 與 `ls-remote` 實查

🛑 本節於 `push` **後**以末端追加填入（⛔ 預寫·`§七` 第 `17` 款）。

---

## `§六`　收工閘（**報告入倉後所量**·嚴格末端追加）

🔒 **態**：`commit 1` ＝ `ab68eb1440f0c33c0ab97396f95fc33f15f48690`／
`commit 2` ＝ `230b8ac4761dfbbaf8021b316751e7d8747695e4`／基座 ＝ `05f220cce97bcaa86387e47fde62b2d945605575`。

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `numstat` 刪除欄**逐檔具名** | `commit 1`：`+195 −0` 單；`commit 2`：`+33 −0` `GB` 登記表／`+446 −0` 本報告／`+12 −0` 自誤登記表／`+1 −0` `probe_WG9269_pit_index.py` ⇒ **刪除合計 ＝ `0`** | ✅ |
| `2` | 生產碼判法（母體**正面列舉 `34` 檔**·判法逐字 ＝ `git diff --name-only <區間> \| grep -xE 'app\.py\|verify/stepg_pipeline\.py\|verify/run_all\.py\|verify/run_verification\.py'`） | 逐區間皆 **`0`** 行（`基座..c1`／`c1..c2`／`基座..c2`）；對照組[必非零] ＝ `17fd981^..17fd981` ⇒ **`2`**（`app.py`／`verify/stepg_pipeline.py`） | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | 本批異動之 `5` 檔逐檔 `CR` ＝ **`0`**；判別力[必非零] ＝ `verify/out/KL_UI_3.5m_2e08a41_stdout.log` ⇒ `CR` ＝ **`2987`** | ✅ |
| `4` | 四簿期末 | 自誤 **相異 `377`／MAX `387`**（期初 `376`／`386` ⇒ **恰 `+1`**·缺號 `[106, 355, 356, 357]` **不變**）；`GB` `167`／`169`·`VR` `80`／`95`·`K-9` `28`／`29` **逐位未動** | ✅ |
| `5` | 坑之期末 | **`87`／`87`**·下一標籤 **`bj`**；判別力[必命中]：坑 `bh`／`bi` **各於器之輸出現身**（見下逐列） | ✅ |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | `rc = 0`；`【驗收】` 段 ＝ `:187`–`:192`；**判定組 `26` 個**（非空 ⇒ 該器於本單**非恆綠**）／報告命中 `0` 之字樣 ＝ **`0`**；參考組 `3` 個（其器逐字自載「**⛔ 作判**·係單之行文片段」） | ✅ |
| `6` | 自限復驗 | `34` 檔 blob 期初＝期末 **相異 `0`**（判別力[必非零] ＝ 同一 `34` 檔於 `17fd981^..17fd981` 相異 **`2`**）；`verify/baselines` **`298`** 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`quotePath` 預設）／`7f2a5fba0467…`（`=false`）；`p3a` **非祖先**（`rc = 1`）；`WV_BAKE` ＝ **`None`**；`K-9-29` 任何子項⛔ 落地 ／ `GB-167`／`168`／`169` ⛔ 修 —— 由「`34` 檔 blob 相異 `0`」**機械蘊含**（其受詞皆在該 `34` 檔內） | ✅ |
| `7` | `push` 後 `ls-remote` **實查** | 🛑 見 `§十`（⛔ 預寫） | ⏳ |

🔒 **閘 `5` 之逐列**（器 ＝ `verify/probes/probe_WG9269_pit_index.py HEAD`·母體 ＝ 追蹤 `.md` **768** 檔）

```
   | `bg` | `W-G.9-280_補令一_舊窗收工批.md:55`（D-T） | ✅✅✅ | `10` | ✅ 可解析 |
   | `bh` | `W-G.9-284R_五之三後半款之重判與真正地界之可取性_執行報告.md:1`（D-H）＋1 | ✅✅✅ | `13` | ✅ 可解析 |
   | `bi` | `W-G.9-284R_五之三後半款之重判與真正地界之可取性_執行報告.md:331`（D-H） | ✅✅✅ | `11` | ✅ 可解析 |
```

判別力二造：造甲[必有定義] 坑 `n` ⇒ 定義處 **6** ≥ 1 ✅／
造乙[必為零] 人造之坑（**執行期組出**·字面⛔ 出艙）⇒ 定義 `0`／引用 `0` ✅ ⇒ **器非紅**。

🩸 **照實併呈（⛔ 頂替）**：坑 `bh` 之**定義處為 `2` 處**——除 `§五-2` 之 `#### 坑 \`bh\`` 外，
**本報告之 H1 標題**（其含 `坑 \`bh\`·\`bi\``，而全文含 `形`／`徵候`／`攔法`）亦合於 `D-H` 之形。
⇒ **⛔ 影響其「可解析」之判**，惟其係**副作用而非設計**，具名之。

---

## `§六-補`　`§四-3` 母體之**正面列舉**（補足·⛔ 追改 `§四-3` 一字）

🩸 `§四-3` 原以「逐名見本批 scratch」指涉 `docs/specs/` 之 `35` 檔——**scratch ⛔ 在倉** ⇒ 該指涉
**不可復現**（`CLAUDE.md` 之「證據指令一律正面列舉」）。本節補其逐名（框 ＝
`git ls-tree -r --name-only -z HEAD docs/specs/` 之**原始位元組**·`core.quotePath` **已釘 `false`**）：

`docs/specs/W-G.4_CC交辦_v3.md`／`W-G.4_KL域裁L_達半佇列_算術不可行剔除.md`／`W-G.4_KL域裁M_原位次小往大_街角winner更正.md`／`W-G.4_KL域裁_兩階段落位_部分面積拆分_Rw側選.md`／`W-G.4_P2_兩階段落位_施工交接.md`／`W-G.4_S0b_plan.md`／`W-G.4_S1_plan.md`／`W-G.4_兩階段落位_plan_v3.md`／`W-G.4_施工plan.md`／`W-G.4_裁定M_plan_v1.md`〜`_v4.md`（`4` 檔）／`W-G.4_規格_v3.md`／`W-G.4_規格v3補丁_第四源裁定.md`／`補丁二`／`三`／`四`／`五`／`六`／`七`／`八`／`九`／`十`（`10` 檔）／`W-G.5_K4-3_來源母體_plan.md`／`W-G.5_裁定N_E系列_BLOCKED_plan.md`／`W-G.5_裁定N_乙案_plan_v1`〜`v3.md`（`3` 檔）／`W-G.5_裁定N_分配起算點.md`／`docs/specs/figures/PROVENANCE_宗地分配線邏輯.md`／`figures/PROVENANCE_最小深度示意圖.md`／`figures/README.md`／`終態幾何消解波_CC交辦.md`／`終態幾何消解波_規格.md`

⇒ 計 **`35`** 檔（含 `docs/specs/figures/` 之 `3` 檔）。`docs/rulings/` ＝ **`1`** 檔
（`K-6_街角地分配程序與可分配判準.md`）。

---

## `§十`　逐 `commit`（🛑 **`ls-remote` 實查於推送<u>後</u>出艙於聊天**·⛔ 預寫）

| # | `commit`（全 `40` 碼） | 內容 | 刪除 |
|---|---|---|---|
| `1` | `ab68eb1440f0c33c0ab97396f95fc33f15f48690` | 工項零：本單原封入倉 ＋ `§零` 九閘之實測 | **`0`** |
| `2` | `230b8ac4761dfbbaf8021b316751e7d8747695e4` | 工項一〜三：出處重判 ／ 真正地界之可取性 ／ 鑄 `自誤 387` ＋ 坑 `bh`·`bi` ＋ `ALPHA_LABELS` 續列 | **`0`** |
| `3` | （**本 `commit`**·其全 `40` 碼於推送後出艙於聊天） | `§六` 收工閘（報告入倉後所量）＋ `§六-補` 母體正面列舉 ＋ 本節 | **`0`** |

🩸 **照實併呈（⛔ 頂替·⛔ 逕修）**：坑索引器之**表頭字串**逐字仍為
「`字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bg\``」，而其 `ALPHA_LABELS` 已含 `bh`／`bi`（表列 **`87`** 列）。
⇒ **該字串已失準**，惟單 `§七` 明令「**⛔ 動 `ALPHA_LABELS` 除本單所令之續列二字外之一字**」
⇒ **本批⛔ 修**，僅具名。

🔒 **`push` 之依據**：`常規一` 之 `🔧 一`（KL `2026-08-31`）——**零生產碼 `commit` 逕行 `push`**；
其判法（`git diff --name-only <區間> | grep -xE '…'`）之輸出於 `§六` 閘 `2` 逐區間出艙，**皆 `0` 行**。

🛑 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批·⛔ 呈 KL）。
