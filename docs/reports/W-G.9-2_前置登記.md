# `W-G.9-2` 定式批：**事前登記**（⛔ 查之前 commit·**零量測**）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`
**基準**：`5056ae35e70570db17545b53716451cd10a78e99`（＝ `W-G.9-1` commit C）
**批號 `W-G.9-2` 現查未占用**：

```
$ grep -rn "W-G.9-2" . --exclude-dir=worktrees --exclude-dir=.git | wc -l
0
```

> 🔒 本檔須 commit 於**任何 Q1／Q2／Q3 判定與量測之前**；事後貼機械時序證明。
> **預測與結果不符 ⇒ 據實改答並具名，⛔ 不得回頭修改本檔一字。**

---

## §0 「已跑／未跑」聲明（⛔ 誠實界定，否則本登記無效）

**已跑（本登記之前·§0-4 之新戒：指令與輸出並列·皆於本次重跑）**：
施工單 §0 之批號占用檢查、`HEAD`／工作樹狀態，以及 §3-1〜§3-4 之**五道 grep**
——輸出與單所載**逐字相符**（含 `K-9-5-6` 標頭之「KL 追認 2026-08-08」、
`:793` 之「MP → 本宗**遠側**分配界線之垂距」、`:794` 之「⛔ 非以寬度累加堆出」、
`:796` 之「**首宗起點**：`W_0` ＝ MP→corner…forced 時為其 buffer 起算點」、
`app.py:9679`／`:9721` 之 `_rw_start`、`app.py:19297` 之缺分配線警示、
`app.py:19498` 之 `_side_mid_left`）。

🔴 **未跑（本登記之受詞）**：⛔ **尚未**
① 逐字通讀 `K-9-5-6` 全節以判 §3-0 之 KL 語是否含**正典所無之增量**（＝ P1 之判定）；
② 讀 `app.py` 之 `is_corner`／`W_prev` 之產生鏈；
③ 讀 `app.py:19297` 之後續控制流以驗「無 `raise`／`continue`／`return`」（＝ P5 之前提）；
④ 執行任何 Q1／Q2／Q3 之量測或合成案例。

⚠️ **併記（跨批·誠實揭露）**：`W-G.9-1` 批中我已讀過
`_cos_dn`／`_mp_base_W0`／`_strip_axis`／`_block_strip`／`_corner_buffer_S` 之本體，
且已量得 `_cos_dn` 十六格值與 `buf` 之度量識別。⇒ **P2／P4 之方向與該批結果一致**；
惟 `P1`〜`P5` 之**文字係施工單所給**，⛔ 非我事後所擬。

---

## §1 預測（⛔ 逐字採施工單 §4·各附證偽方式）

| # | 預測 | 證偽方式 |
|---|---|---|
| **P1** | §3-0 之 KL 語係 `K-9-5-6` 之**重申**、⛔ 非新裁 | 正典查無 §3-1 之逐字；**或** `K-9-5-7`〜`K-9-5-11` 中有任一條推翻之；**或** KL 語含 `K-9-5-6` 所無之增量（例如指定 `MP` 為 SIDELINE 中點而正典未指定）⇒ **P1 破** |
| **P2** | 族②③ 之 `buf · _cos_dn` **不合致** `K-9-5-6`——其非「MP → 某界線之垂距」，而係「寬度 × 投影」 | 能證 `buf·_cos_dn` 於本案任一 forced 格**恆等於**某「`dot(某點 − mp, â)`」之值（殘差 < 該處既有探針原式之閾）⇒ **P2 破** |
| **P3** | forced 情境下該側**首宗之 `is_corner` 為 `False`** ⇒ `_rw_start` 走 `float(W_prev)` 分支 ⇒ **種子確實被用**（⛔ 非被 `_W_near` 覆蓋） | forced 之三格中任一格其首宗 `is_corner=True`，或 `_rw_start` 實走 `_W_near` 分支 ⇒ **P3 破** |
| **P4** | 族④（`_mp_base_W0`）**合致** `K-9-5-6` 之原點（其式含 `− mp`） | 其式之原點非 `mp` ⇒ **P4 破** |
| **P5** | `GB-60` 之第三選項成立——缺 `alloc_dir` 時，警示自載「側街負擔以 0 計」而**實際 `Rw` ≠ 0** | 合成案例中 `alloc_dir=None` 時該側 `Rw` 實測為 `0`（殘差 < 既有閾）⇒ **P5 破**，`GB-60` 回復為二擇一 |

---

## §2 停機條件（⛔ 逐字採施工單 §6）

**真域邊界 ⇒ 停機上呈：**

- **S-1**：**P1 破**（KL 語非重申，係新裁或與正典衝突）⇒ **立即停機**，⛔ 不得自行入正典。
- **S-2**：**Q3 窮舉結果為「無任何構造同時滿足三條」** ⇒ 停機上呈（此時方為真取捨）。
- **S-3**：Q1 之 f 欄於任一格**沿遠側界不恆定**且其變幅超出該處既有探針原式之閾 ⇒ 停機
  （表示 `K-9-5-6` 之「垂距」在該格無單一值，屬定式問題）。

**純技術項 ⇒ 記錄後自行做完至 push：**

- P2／P3／P4／P5 任一破 ⇒ 於報告正面列出並據以調整登記敘述。
- `GB-60` 之「是否需域裁」欄更正（P5 成立 ⇒ 改標「**需前置偵察·三選項**」；
  P5 破 ⇒ 維持「需域裁」並把選項寫清）。
- `K-9-5-6` 節末之重申加註（P1 成立時）。
- 失敗考古**節 63** 之撰寫（§0-4 之案由·⛔ 具名記為 **claude.ai 犯·CC 現查抓出**）。
- 探針、log、報告之一切撰寫。

---

## §3 驗收（⛔ 逐字採施工單 §7·🔴 禁以計數或列數為證據）

| # | 閘 |
|---|---|
| 1 | 16 格皆**正面列出**判定，含不觸發格與退化格；⛔ 只列命中者即不過 |
| 2 | 每一「無／零命中／不存在」皆附**窮舉指令原文＋射程聲明**；⛔ 空集合不算通過 |
| 3 | 🆕 每一出艙之數，其指令皆**於落筆當次重跑**且指令與輸出並列（§0-4） |
| 4 | 凡「某符號有 N 處呼叫」皆以 **AST `ast.Call`** 為據 |
| 5 | Q1 之 f 欄附**至少兩個獨立取點**之恆定性自證 |
| 6 | Q3 附「是否窮盡」之書面自答；若為列舉須明寫「**列舉·非窮舉**」 |
| 7 | 探針**不得把自己量進母體**——須具名排除本批探針並列出排除前後之母體 |
| 8 | `git diff --numstat` 對 `app.py`／`verify/stepg_pipeline.py`／`verify/wf_f*.py` **為空輸出** |
| 9 | 正典之加註為**純附加**：`git diff --numstat` 對 `docs/rulings/` 之**刪除欄為 0** |

---

## §4 已預先承諾之判準（⛔ 事後不得放寬）

- **只量不判**：⛔ 不得於本批下「應改成什麼」之判——本批只判**現行是否合致正典**。
- ⛔ **不得為 §3-0 之 KL 語另立新 `K-9-5-12` 節**；判為重申者一律加註於 `K-9-5-6`。
- 判準之鍵一律為 **(情境 × 街廓 × 側別 × s 區間)**；⛔ 不得以宗序號為鍵。
- **射程宣告須逐個量測寫**，⛔ 不得於探針開頭寫一次即算數。
- 🆕 **§0-4**：出艙任何數前，須把產生該數之指令**原樣重跑一次**並與輸出並列；
  ⛔ 不得由「先前跑過」之記憶轉抄；指令與數若分兩次產生，**一律以重跑者為準**。
- 🔒 **Q3 之書面自答須寫在量測之前**：
  「是否存在一個構造使三者同時滿足？若存在，本批是否已列出它？若不存在，不相容之處恰在哪兩條之間？」
