# `W-G.9-270R5e`　補令七之執行（**舊窗收工批**：積欠之清 ／ 坑 `ay`・`az` ／ `Σa` 之復現 ／ 交接註）

> **態**｜開工 ＝ `5a14dc2f5019b6f478a8800fec9f3c3abc818ed1`（`ls-remote` 實查·`left-right ＝ 0　0`）｜
> 工項零後 ＝ `cc26132c108c78448a89c3dc6278da5ec6e646e5`｜序 `2` 後 ＝ `5964fff…`
> **級** ＝ **中**（純登記 ＋ 純診斷·**零生產碼收工批**）
> 🛑 **本批⛔ 辦**（補令七 `§六`）：⛔ 動 `34` 檔一字（**⛔ 改五個 `*_EXPECT` 任一字**）／
> ⛔ 落地 `K-9-29` 之任何子項・`GB-164`・`GB-166`／`GB-162` 三禁令維持／
> **⛔ 入倉開場文與交接文**／⛔ 對 `p11` 之清單提任何順序主張／⛔ 呈 KL 任何事。
>
> 🔒 **辦畢 ⇒ 停 ⇒ 本窗封存。**

---

## `§零`　工項零（補令七之原封入倉·commit `cc26132`）

| 量 | 值 |
|---|---|
| **條款一** `CR` | **`0`**（`LF` `95`·bytes `6307`）；判別力造甲（真 CRLF）得 `2` ✅ |
| **條款二** blob `sha1` | `5601694116a9bca7c9a7613cd368c73761560bdb` |
| **條款二** blob 內容 `sha256` | `1b42693d34fa9b73182f21c99a0b62cb4979cb6666b4c738b6b9f24319596544` ＝ **磁碟值**；逐位相同 `True` |
| **條款三** 來源逐字 | 主 checkout `docs/orders/` 之 untracked 檔·`6307` B·⛔ 由 KL 於本回合貼入 |

🔒 **`§二-B` 之新常規（本批<u>首次施行</u>）**：`(1)` ⛔ 以全 ASCII glob 定其唯一；`(2)` ⛔ 以差集定之；
`(3)` 於 python 內以**完整檔名逐字指定**（CJK ⛔ 穿過 shell 引號層），並於搬移**前**出艙
「**所指之檔數 ＝ `1`**」之自證 ⇒ 本件實得 `1` ✅。器 ＝ scratchpad 之 `move_order.py`（⛔ 入倉·一次性）。

### 零-1　🩸 本節之自捕二則（皆 loud·⛔ 出艙錯值）

| # | 形 | 症狀 | 處置 |
|---|---|---|---|
| `7` | **`os.path.join` 於 Windows 產生 `\` 分隔，而 git pathspec ⛔ 認之** | `git rev-parse :docs/orders\W-G…` ⇒ **`exit 128`** | 一律以 `/` 組 git pathspec |
| `8` | **`subprocess(text=True)` 於本機以 `cp950` 解碼** ⇒ CJK 路徑之 stderr 拋 `UnicodeDecodeError` | 🔴 **其在背景執行緒中** ⇒ **⛔ 使主流程失敗**、只印 traceback ⇒ **若主流程未因別故失敗，該錯將靜默** | `encoding="utf-8", errors="replace"` |

🔒 **二者皆⛔ 影響本件之結果**——`cp` 與逐位比對於修**前**即已成功（`逐位相同 = True`）。

---

## `§一`　序 `2`：積欠 ＝ **`3` 件**（⛔ `5` 件·受詞已更正）·commit `5964fff`

🔒 **發單側之受詞更正（CC 採納）**：`作業常規之追加四` 之受詞係**交予 CC 執行之單**
（`CLAUDE.md` 逐字「**CC 收單後第一工項即原封入倉 `docs/orders/`，⛔ 改一字、⛔ 改行尾**」）
⇒ **開場文**（其本文自載「**⛔ 為施工單**」）與**發單側交接文**（內部文件·⛔ 交 CC 執行）
**⛔ 在受詞內、⛔ 入倉**。
🛑 **⛔ 擴張該受詞**——以「其在 `docs/orders/` 目錄下」代「其為施工單」，即 **`自誤 367` 之形**
（禁令之受詞較其由為廣）。若日後認開場文／交接文應歸檔，須**另立受詞、另裁**。

🔒 **本 commit 之 staged 清單即其證**：恰 **`3`** 件；`新施工窗開場文` 之命中 ＝ **`0`**、
`發單側交接文` 之命中 ＝ **`0`** ✅

| # | 件 | bytes ／ `CR` | blob `sha1` | blob 內容 `sha256`（＝ 磁碟值） | 判 |
|---|---|---|---|---|---|
| `1` | `W-G.9-268prime_施工單_GB67病二_起算垂線錨點更正.md` | `20407` ／ `0` | `dfb9c8291ce4b093cd24cf90f1f05fe46528e012` | `037538f136b51a4ec7efdc4ae80471906f62575d9a3c8d860c20253500447ab9` | 🟢 往返逐位保存 |
| `2` | `W-G.9-269_補令四_安全烤法與P1改繫.md` | `10799` ／ `0` | `38279672900bb38c2372235dba763eb1aaae9f5b` | `e675e608057a4954fc3ba08bccb1ec76d5457faf9a25737c28e5e81c64e97979` | 🟢 |
| `3` | `W-G.9-269_補令五_驗乙之重定與二檔無從驗.md` | `9366` ／ `0` | `f0f02d50dc2f7561aeb6abaa039b30be48f0b493` | `7eb9a36720db11c1d0521370cc6781bc433ca6b83d1abce1e631cf3d9f3d711e` | 🟢 |

⇒ **三件之往返皆逐位保存**，⛔ 停機。逐件搬移**前**皆出艙「所指之檔數 ＝ `1`」之自證。

---

## `§二`　序 `3`：鑄坑 `ay`／`az` ＋ `ALPHA_LABELS`→`az` ＋ **三處**框字串齊一

### 二-1　🆕 本波新增之坑 `ay`〜`az`（補令七 `§二-A` **准鑄**·標籤續 `ax`·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `ay` | 🔴 **以碼位常數（`chr(N)`）組出字元而未驗其為所欲之字元** | 本批以 `chr(12295)` 組波浪號，而 `12295` ＝ **`〇`（U+3007）**、⛔ `〜`（**U+301C**·本批實查）⇒ 替換**靜默為空操作**；🔴 **其徵候是「什麼都沒發生」**——檔案未被改而流程正常結束 | 凡以碼位組字，須**同格印出該字元本身與期望之字元並比對**；且替換一律同格並報「**期 `N`／實 `M`**」——本次即由此**當場顯形**（得「期 `3` 實 `0`」） |
| `az` | 🔴 **診斷所標之索引與實際所取者不符** | 標籤稱取 `OLD[2]` 而受詞在 `OLD[3]` ⇒ 印出之碼位遂為**他字元**（`U+0060` ＝ 反引號）；🔴 其**⛔ 影響結果**（替換用完整字面），**惟使診斷⛔ 可信** | 診斷列須自**同一運算式**取值（如 `i = OLD.index(WAVE)` 後印 `OLD[i]`），**⛔ 另寫一份字面／索引常數** |

🔒 **二坑之攔法已於本批施行**：`fix_az.py` 之字面**直接寫出**（⛔ `chr(N)`）、
其診斷自 `OLD.index(WAVE)` 取值 ⇒ 實印 **「波浪號：字元 = `'〜'`　碼位 = `U+301C`　（期望之字元 = `'〜'`·相符 = `True`）」**。

### 二-2　`ALPHA_LABELS`→`az` ＋ **三處**框字串齊一

🔒 **逐字自證**：`aa`〜`az` 命中 **`3`**（期 `3`·逐處 `:40`／`:282`／`:284`）；`aa`〜`ax` 殘留 **`0`**；
`"ay", "az"` 命中 **`1`**；判別力（寬字樣 `字母系`）＝ **`4`** ⇒ 該檢⛔ 恆為 `3`。
`numstat` ＝ `5`／`4`；`py_compile` OK；`CR` ＝ `0`。

---

## `§三`　序 `4`：**`Σa 513.93` 之復現** ⇒ 🟢 **已復現·自此為【倉】**

**器** ＝ `verify/probes/probe_WG9270R5e_sigma_a.py`（**複製 `compute()` 之呼叫序**·⛔ 另寫第二份判準）｜
落檔 `verify/out/WG9270R5e_sigma_a.log`｜`rc = 0`。
🔒 **自我驗證閘**（重建之逐戶 `ΣG_戶` 與 `宗數` vs 同態 `compute()` 之 dump）：**二情境皆「不符之項 ＝ `0`」** ✅

| 情境 | 歸戶 | 暫編地號 | 原地號 | 街廓 | **`a 面積(㎡)`** | `幾何面積(㎡)` | `G(㎡)` |
|---|---|---|---|---|---|---|---|
| `0m` | `G010` | `628-40(1)+` | `628-40` | `R2` | **`513.93`** | `285.57` | `285.56` |
| `3.5m` | `G010` | `628-40(1)+` | `628-40` | `R2` | **`513.93`** | `299.50` | `299.51` |

🟢 **`Σa` ＝ `513.93`（二情境皆然·逐位相符）** ⇒ **發單側之現查已由 CC 復現 ⇒ 自此為【倉】**。
🔒 **成員（`a2c4b2b` commit message 逐字）** ＝ `628-40(1)` ＋ `628-43(1)` ⇒ 合併後 `628-40(1)+`
——其與本批所測之 `原地號 628-40`／`暫編 628-40(1)+` **逐字相符**。

### 三-1　🩸 一項當場自捕：**同名而異物之跨層比對**（⛔ 判孰誤）

🔴 本器另比 `Σ幾何面積`，得 `285.57`（`0m`）／`299.50`（`3.5m`），
而 `a2c4b2b` 之 commit message 載 `Σ幾何面積_m2 ＝ 513.79` ⇒ **相異 `−228.22`／`−214.29`**。
🔒 **其成因係<u>我之框錯</u>**（⛔ 二數之一為誤）：

| 名 | 所在層 | 受詞 |
|---|---|---|
| `Σ幾何面積_m2`（`a2c4b2b` 所載） | **`temp_parcels` 層** | **重劃前**之幾何面積之和 |
| `幾何面積(㎡)`（本器所取） | **`g_rows` 層** | **配地後**之幾何面積 |

⇒ 🔒 **同名而異物** ⇒ **⛔ 同一量**、**⛔ 可互比**。
🛑 **`Σ幾何面積_m2 513.79` 之復現須另取 `temp_parcels` 層之欄** ——**⛔ 本批之受詞**（序 `4` 逐字只令復現 `Σa`）
⇒ 照實具名，**⛔ 判孰誤、⛔ 追改任何記載**。
🔑 **與 `W-G.9-270R5d §二-1` 之判一致**：該節已判「`513.79` 之受詞 ＝ `Σ幾何面積_m2`、`513.93` 之受詞 ＝ `Σa`
⇒ 二者確⛔ 同一量」；本節**再加一層**——即使取「幾何面積」之名，**層不同亦為異物**。

---

## `§四`　交接註（補令七 `§四` 之七項）

### 四-1　結案 commit 與本批逐 commit

| # | commit | 受詞 |
|---|---|---|
| `1` | `cc26132c108c78448a89c3dc6278da5ec6e646e5` | 工項零（補令七原封入倉·依 `§二-B` 新常規） |
| `2` | `5964fff…` | 序 `2`：積欠 **`3`** 件之原封入倉 |
| `3` | **本報告之 commit** | 序 `3`（坑 `ay`／`az` ＋ `ALPHA_LABELS`→`az`）＋ 序 `4`（`Σa` 復現）＋ 序 `5`（交接註） |
| `4` | **末一 commit** | 收工：坑之定義處清單自證（`通則 I`·報告入倉**後**量） |

🛑 **`常規九 三`**：接手窗之開工閘**⛔ 以本文所載之 commit 為錨**，一律以
`git ls-remote origin refs/heads/wip/s1-endpart` **實查**。

### 四-2　四簿期末 ＋ 坑之期末數

| 簿 | 框之出處 | 相異 | MAX | 缺號集 |
|---|---|---|---|---|
| **自誤** | `VR-091` 補款二 `①` ＋ 範圍框 | `364` | **`374`** | `[106, 355, 356, 357]` |
| **`GB`** | `VR-093` 三框聯集（扣哨兵 `997`） | `164` | **`166`** | `[12, 87]` |
| **`VR`** | `VR-092` 就地加註所引（扣 `999`） | `80` | `95` | `[73, 75]` |
| **`K-9`** | `W-G.9-266` 工項四 `四-1` | `28` | `29` | `[]` |

**坑之期末數** ＝ 見**末一 commit** 之訊息（`通則 I`：本報告含 `D-T` 形之坑表 ⇒ 其為受詞之一員）。
**次號**：自誤 **`375`**／`GB` **`167`**／`VR` **`96`**／`K-9` **`30`**；**坑之下一標籤 ＝ `ba`**
（🛑 `az` 之後——`ALPHA_LABELS` 現行硬編止於 `az`，**新坑須同批擴之否則靜默漏**）。

### 四-3　自限復驗

| 受詞 | 實 | 判 |
|---|---|---|
| `app.py` blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ 期初 ＝ 期末 |
| 生產碼 `34` 檔聚合 | `c6320ce6def9a3c6b5a2c4281e30b84bf754c8ad44a3e603f42aa4c208c87458` | ✅ 期初 ＝ 期末 |
| 五個 `*_EXPECT` | `verify/wd4_tier_list.py` 非 `??` 列 ＝ `0` | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·聚合 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | `merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | 未設 | ✅ |
| `K-9-29` 之任何子項 | ⛔ 落地（`app.py` blob 未動即其證） | ✅ |
| 開場文／交接文 | **⛔ 入倉**（序 `2` 之 staged 清單恰 `3` 件·二者命中皆 `0`） | ✅ |

### 四-4　本波新立之通則（逐字）

> **`§二-B` 搬移法**：凡搬移一具 CJK 檔名之檔入倉——`(1)` ⛔ 以全 ASCII glob 定其唯一
> （同波多單下其唯一性**必然失效**）；`(2)` ⛔ 以「主 checkout 有而 worktree 無」之差集定之
> （含歷史積欠 ⇒ 亦不唯一）；`(3)` **一律於 python 內以完整檔名逐字指定**，
> 並於搬移**前**出艙「所指之檔數 ＝ `1`」之自證，⛔ 為 `1` 即 **loud 停**。

> **⛔ 在片段中預寫命中數**：片段內凡須載命中數者，一律**先實測後填**，⛔ 先填後驗。

> **遇求值失敗之態**（`補令五 §二-A`）：`(1)` 照實具名其 rev、失敗之逐字、可歸屬之既有號；
> `(2)` 略過之；`(3)` ⛔ 視為紅、⛔ 視為綠、⛔ 與「判定為偽」共用出艙碼——其出艙碼須為**第三種**；
> `(4)` **⛔ 以二分法跨越之**——二分之前提為受測序列**全序可判**。

> **錨只能是逐字**（`自誤 372`＋`374`）：**行號⛔ 為錨、他文之指涉亦⛔ 為錨**；
> 逐字命中而行號不符 ⇒ 以逐字為準並照實具名行號已腐。

> **`通則 I`**：凡一自證之受詞**包含承載該自證之文件本身**者，其「未變動」之結論
> **⛔ 得於該文件入倉前作成**（`GB-132` 族）。

### 四-5　🩸 本會期自捕之全表（形／徵候／攔法）

| 批 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| 補令一 | `git rev-parse :<glob>` 之 `:` 前綴使 bash ⛔ 展開 glob | `sha256sum` 印出 `e3b0c442…`（**空串之 sha256**）——形上與正常值⛔ 可辨 | 路徑以變數承接；當場印出空串值以坐實 |
| 補令一 | `cat -A \| grep '\^M'` 於 UTF-8 CJK 偽陽 | 得 `47` 而真值 `0` | ⇒ **坑 `as`** |
| 補令一 | `grep -c $'\r'` 對真 CRLF 檔偽陰 | 造甲得 `0`（期 `2`） | ⇒ **坑 `at`** |
| 補令一 | `io.open(p,'w')` 於 Windows 靜默轉 CRLF | 報告初落 `CR ＝ 305` | ⇒ **坑 `au`** |
| 補令一 | **以 `tail -N` 取長跑之結果** | 診斷輸出淹沒結果列；例外之 traceback 亦同遭截 | ⇒ **坑 `av`** |
| 補令一 | 標記含 `%` ⋀ `2>&1` 合流使 `grep` 命中源碼列 | `rc=1` 而「命中」之竟是**自己的源碼** | ⇒ **坑 `aw`** |
| 補令四〜七 | **片段內宣告之命中數未實測**（**四度**） | `10` vs `8`／`9` vs `57`／`3` vs `6`／`4` vs `7` | **附加前以全檔計數實測**（四度皆攔下）⇒ 已常規化 |
| 補令四 | heredoc 誤帶（`2` 次） | `wg9237_heredoc_guard` 硬閘 `rc ≠ 0` | 改 `Edit`／`Write`（⛔ 賴記憶·全賴該硬閘） |
| 補令四 | 未施行單所令之款 `1`（縮小候選集） | 直接對 `692` commit 全序列二分 ⇒ 步 `1` 即撞洞 | 改依單已令之法；**⛔ 新訂準據** |
| 補令四 | `python -c "…"` 之雙引號內含反引號 | bash 解為命令替換 ⇒ `rc=2`（loud） | 改 `Write` 落腳本；反引號以 `chr(96)` 執行期組出 |
| 補令五 | 單所稱之落點不存在 | `fixture_block_depth_n19p.py` 之 `.md` 命中 `0` | 依實測改落 `K-6`（取保守項·`常規二`）⇒ **`自誤 374`** |
| 補令五 | 以 `grep -E '…\|·'` 取結果 | `·` 咬中逐宗診斷 | ⇒ **坑 `ax`** |
| 補令六 | **全 ASCII glob 之唯一性於同波多單下失效** | `*p9*` 命中 `2`（差異只在 CJK） | ⇒ **`§二-B` 常規** |
| 補令六 | **「差集」法亦不唯一** | 實得 `6`（⛔ `1`）⇒ loud `rc=7` | 同上 |
| 補令六 | **`chr(N)` 碼位記錯** | `chr(12295)` ＝ `〇`（U+3007）⛔ `〜`（U+301C）⇒ 替換**靜默為空** | ⇒ **坑 `ay`**（由「期 `3` 實 `0`」當場顯形） |
| 補令六 | **診斷列之索引取錯** | 印 `U+0060` ＝ 反引號·⛔ 波浪號 | ⇒ **坑 `az`** |
| 補令六 | 判別力對照選了**本就唯一**之字樣 | `628-40(1)` @ commit message ＝ `1`（期 `>1`） | 改取真具判別力者（`628-` ＝ `32`） |
| 補令六 | **`grep -c` 係列框**，量單一長行內之出現次數 | 恆得 `1`／`0` ⇒ ⛔ 具判別力 | 改**字元框**（`grep -o \| wc -l`） |
| 補令七 | `os.path.join` 於 Windows 產生 `\` ⇒ git pathspec ⛔ 認 | `exit 128` | 一律以 `/` 組 git pathspec |
| 補令七 | `subprocess(text=True)` 以 `cp950` 解碼 CJK 路徑之 stderr | 拋 `UnicodeDecodeError`·**其在背景執行緒中** ⇒ **⛔ 使主流程失敗** ⇒ 可能**靜默** | `encoding="utf-8", errors="replace"` |
| 補令七 | **同名而異物之跨層比對** | `幾何面積` 於 `temp_parcels` 層（重劃前）與 `g_rows` 層（配地後）同名異物 ⇒ 比得 `−228.22` | **先對齊層與受詞再比**；⛔ 判孰誤 |

### 四-6　起手清單（予新窗·取**本批收工後**之倉態）

1. **開窗第一動作**：`git ls-remote origin refs/heads/wip/s1-endpart` 實查（**⛔ 本地追蹤 ref**）；
   驗 `left-right` ＝ `0　0`（⇒ 工作區為 **wip 態**·⛔ main 態）。
2. **凍存 `p3a`** ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（⛔ 併線·⛔ 刪除）。
3. **生產碼 `34` 檔** ＝ `app.py`（blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`）＋ `verify/*.py` **單層** `33` 檔；
   聚合（自擬框）＝ `c6320ce6def9a3c6b5a2c4281e30b84bf754c8ad44a3e603f42aa4c208c87458`。
4. **`verify/baselines`** ＝ `298` 檔·聚合 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`
   （框 ＝ `git ls-tree -r HEAD verify/baselines` 之輸出文字之 `sha256`）。
5. **四簿** ＝ 見 `§四-2`；**次號** 自誤 `375`／`GB` `167`／`VR` `96`／`K-9` `30`；**坑之下一標籤 `ba`**。
6. **器**：四簿 ＝ `verify/probes/probe_WG9267_issuer_measurers.py registry`；
   坑 ＝ `verify/probes/probe_WG9269_pit_index.py`（`PYTHONIOENCODING=utf-8`）。
7. **⛔ `python3`**（本機 `rc=49`）⇒ 用 `python`（`3.13.11`）；**⛔ heredoc**（硬閘攔）。
8. **本波新立之通則** 見 `§四-4`；**自捕全表** 見 `§四-5`。

### 四-7　🔴 未結項之逐條

| 項 | 狀態 |
|---|---|
| **`GB-159`** | 🔴 中間態配地表**⛔ 對外**；其缺口**繫於 `K-9-12`** |
| **`GB-160`** | 🔴 閘 `γ` 於 `3.5m·R5·left`／`R6·right` 不過（射程已收窄為二側） |
| **`GB-162`** | 🔴 `baselines` 與現況差·成因未歸因；**三禁令維持** |
| **`GB-164`** | ⬜ 未落地（不可判帶·須動 `wd4_tier_list.py` ⇒ 他批 ＋ KL 放行） |
| **`GB-165`** | ⬜ 未落地（近乎零之幾何剩餘片入「宗」之母體） |
| **`GB-166`** | 🔴 **⛔ 解除**——解除條件 `(3)` 已履行；`(1)`（逐錨分列入生產碼 ＋ KL 放行）與 `(2)`（判別力二造）未成就 |
| **`ok_t`／`ok_f`** | 🛑 **⛔ 改一字**——待 `K-9-29` **碼側落地後重量**再定（其紅係**已裁未落地**族） |
| **`p5`／`p6`** | 🟡 事實已量·**⛔ 判其為缺陷**·待 KL |
| **`K-9-12`** | 🟡 待 KL |
| **`p11` 之清單** | 🟡 已出艙（`W-G.9-270R5d §三`）·**⛔ 提任何順序主張**——落地單由新窗之發單側擬、KL 逐 commit 放行 |
| **`Σ幾何面積_m2 513.79`** | 🟡 **未復現**（其受詞在 `temp_parcels` 層·⛔ 本批之受詞） |

---

## `§五`　收工閘

🔒 **自限復驗** 見 `§四-3`（全綠）。
🔒 **四簿期末** 見 `§四-2`。
🔒 **坑之定義處清單自證**：其實測見**末一 commit** 之訊息（`通則 I`）。

---

## `§六`　🛑 **停 ⇒ 本窗封存**

辦畢。新施工窗之首則為另行交付之開場文。
