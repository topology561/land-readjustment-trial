# 【W-G.9-19 前置登記】「不丟棄遠側界」之影響估算（⛔ 只估·不改）

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`git rev-parse --short HEAD`（⛔ 本文不寫死·現查為準）
**字樣錨（倉規 A·唯一命中）**：`【W-G.9-19 前置登記】`

> 🔒 **本檔為 commit A：⛔ 零生產碼、⛔ 零碼變更、⛔ 零 `D1`–`D6` 之量測。**
>
> # 🔴 本批之最高原則（⛔ 違者本批作廢）
>
> **可估的那一半，⛔ 不得以任何形式呈現為「完整影響」。**
> 本批之產出**只是一個下界**，且**必須逐頁、逐表標明它是下界**。

---

## §0 登時已現查（⛔ 不採信施工單所引之三格）

| 錨 | 現查結果 |
|---|---|
| `git grep -l "W-G.9-19" \| wc -l` | `0` |
| **生產側啟動式**（字樣錨自行定位） | `_fo_left = bool(_fo_block.get('left_forced_offset', False))`<br>`if _fo_left:` → `_l_min = _row_for_buffer.get('【左】街角最小面積(㎡)')`<br>→ `if _l_min is not None and _l_min != float('inf'):` → `_corner_buffer_S(…)` |
| ⇒ **啟動條件** | **`left_forced_offset` ∧ `_l_min` 非 `None` ∧ `_l_min ≠ inf`**（右側同構） |
| `left_forced_offset` 之定義（`W-G.9-15` 已現查） | `bool(_l_forced and _l_has_side)`，而 `_l_forced ⟺ 無 winner` |

⚠️ **施工單 §0 所引之三格（`3.5m` R2 left／R3 right／R5 left）係其自既有 log 引用**
⇒ ⛔ **本批不採信**，母體一律由**上開啟動式**現查界定。

---

## §1 兩族之性質（⛔ 本批之分界）

| 族 | 受詞 | 可否估 |
|---|---|---|
| **②③ W 鏈種子／選槽 `b`** | **純量**（一個 `W`） | ✅ **可估** |
| **① 落位 `s` 累積** | 保留區之**表示法**（∥SIDELINE 界**非 `s` 區間**·`W-G.9-18` `C4` 已證） | 🔴 **不可估** |

🔒 **二者必須同時改**（否則第 1 宗之**位置**與其**負擔起算**互不一致
——正是本波一直在追的病）⇒ **完整之面積影響，在表示法問題解決前⛔ 估不出來。**

---

## §2 `D1`–`D6` 可證偽預測（⛔ 於任何量測之前寫定）

| # | 預測 | 證偽方式 ⇒ 觸發 |
|---|---|---|
| **D1** | 母體可由**生產側啟動式**現查界定，逐格具名 | 界定不出 ⇒ **D1 破**，具名，⛔ 不猜 |
| **D2** | 真正遠側界之 `W` **可由只讀包裝取得** | 取不到 ⇒ **D2 破 ⇒ `L-4`**（且前批 `B5′` 須重驗） |
| **D3** | 族②③ 之面積差**可逐宗算出** | 算不出 ⇒ **D3 破**，具名 |
| **D4** | 族① **不可估**（∥SIDELINE 界在 `s` 下**非單一值**） | 可估 ⇒ **D4 破**（**好消息**，須說明何以可估） |
| **D5** | `K-4` 成立（二者**必須同時改**） | 不成立 ⇒ **D5 破 ⇒ `L-1`**（本批產出之**性質改變**） |
| **D6** | 本批**零判定層變動** | 出現任何差 ⇒ **D6 破 ⇒ `L-5`** |

🔒 每項附**判別力自檢**。
🔴 **`D4` 之期望為「不可估」——期望值為<u>否定</u>者尤易假成立**
⇒ 須證「**若可估，該判法會給出不同結論**」（⛔ 否則它是恆真句·**考古 76**）。

---

## §3 停機條件

| 碼 | 條件 |
|---|---|
| **L-1** | `K-4` 為偽（可分開改）⇒ 🛑 上呈——本批產出由「下界」升格為「完整影響」，性質全然不同 |
| **L-2** | 乙組估出之面積差 **> 一個宗地之法定最小面積** ⇒ 🛑 上呈（已非誤差層級） |
| **L-3** | 估算過程中**改動任一生產函式** ⇒ 停機。⛔ 「只是暫時改回來」**不成立** |
| **L-4** | 真正遠側界之 `W` **取不到** ⇒ 停機（前批 `B5′` 須重驗） |
| **L-5** | `run_verification.results` 之 PASS/FAIL 層改變 ⇒ 停機 |

---

## §4 ⛔ 明確未授權

1. ⛔ **零生產碼**：不動 `app.py`、`verify/stepg_pipeline.py`——估算**一律以只讀包裝為之**。
2. ⛔ **不改任何一族消費者**、⛔ 不實作新表示法、⛔ 不解 `GB-58`。
3. ⛔ **不估族①**、⛔ **不以任何代表值近似之**（中點／平均皆不得取）。
4. ⛔ 不動 `run_verification.py`／`wv_reconcile.py`／`baselines/`／既有夾具／三支複本／`docs/rulings/`／`CLAUDE.md`。
5. ⛔ 不改期望 FAIL 名單、不重凍、不重烤、不修任何既有 FAIL。
6. ⛔ 不追那 57 條、不追 `X5` 之 6 條、⛔ 不掛 `wg915` 盤點器。
7. ⛔ 文件刪除欄一律 **0**。

---

## §5 🔒 本批自我加課

| # | 規矩 | 本批如何遵守 |
|---|---|---|
| 1 | 🔴 **每一頁、每一表**標明**下界** | ⛔ 不得任何一處呈現為完整影響——**標示不清比估錯更嚴重** |
| 2 | 真正遠側界之 `W` 由**該多邊形之遠側界**導出 | ⛔ **不由 `S1_par` 反推**（否則是拿輸入當輸出·套套邏輯） |
| 3 | **公尺**與**百分點**分欄 | ⛔ 不混列、⛔ 不比大小（`W-G.9-14` 之教訓·考古 72） |
| 4 | 面積差逐宗、鍵用**宗地字面** | ⛔ 不用 `side`（`W-G.9-14` 之取樣缺陷）、⛔ 不用宗序號 |
| 5 | ⛔ **不為族①取代表值** | **沒有數字是誠實的；取一個看似合理的中點是不誠實的** |
| 6 | 估算**一律只讀包裝** | ⛔ 「暫時改一下生產碼再改回來」不成立 |

---

## §6 commit 分立

- **A** ＝ 本檔（⛔ 零碼、零量測）
- **B** ＝ 只讀包裝 ＋ 逐格逐宗估算（⛔ 零文件結論）
- **C** ＝ 報告 ＋ `GB` 登記 ＋ **呈 KL 之白話稿** ＋ 考古節（若有）

⚠️ **依倉規（不可變文件不記可變值），本檔 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本檔之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
