# `W-G.9-261R`　鏈頭起點更正（無 WINNER 之側自抵費地遠側界起算）

> 🔴 **本批含生產碼且動配地面積** ⇒ 依單 `§六`：**⛔ 併主線**、出艙於側分支 **`verify/W-G.9-261-c1`**、
> **推側分支後停機候 KL 逐 commit 放行**。
> 🔒 **本批只改鏈頭起點一件事**：⛔ 動 `GB-67`、⛔ 動 `telescoping` 閘本身、⛔ 動五級（逐項驗於 `§六`）。
> 🔒 一切數字皆**當場實測**（⛔ 憑記憶·⛔ 採信單載）；量測法與三軸逐節載明。

---

## §零　工項零：收單原封入倉 ＋ 入倉前之號占用閘

### 零-1　新單之發現處（🔴 **第六度**）

🔴 本單於**主 checkout 工作區**（`C:/Users/admin/Desktop/land-readjustment-trial/docs/orders/`）發現，
**本 worktree 之倉內查無** ⇒ 係記憶所載「開窗第一動作 `④`」之形**第六度**再現。
主 checkout 之 `HEAD` ＝ `1e5a87c`（`2026-09-07`·`W-G.9-248R`），為 `eedfbc6` 之**祖先** ⇒ 其倉態落後，
新單以 **untracked** 之姿置於該處。

🔒 **併發現一未入倉之附件（具名·⛔ 逕入倉）**：
`docs/orders/R2左側街角第1宗_推進超跑_幾何示意.svg`（`18129` B·`sha256 3f18e101…`·mtime `2026-09-08 18:09`
——**早於**本單之 `2026-09-09 13:29`）。**本單全文⛔ 引之** ⇒ 其歸屬係**意思決定**，候發單側。

### 零-2　號占用閘（宣告框 `①`–`⑦`·**入倉前**）

**三軸**：來源 ＝ `blob@eedfbc69f43919c435c2e6c02773fb49901f57b7`／母體 ＝ **全 `docs/` `610` 檔**／
粒度框 ＝ **檔框 ＋ 列框**（`列框 ＝ |D2 ∪ D3|`）。
**讀不到 `19` 檔 ＝ `jpg 9` ＋ `png 10`**（單 `§一` 閘 `3` 所令之副檔名分布）；可讀 `591` 檔。
**器自檢**：含字面反斜線之路徑 ＝ `0`（`core.quotepath` 已關）／含 CJK 之路徑 ＝ `589`（`> 0` ⇒ 該檢有判別力）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | 列框 | 檔框 | 判 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-261`** | **0** | **0** | **0** | **0** | **0** | **0** | ✅ **未占用**（與單 `§一` 閘 `3` 之期望逐位相符）|
| `W-G.9-260` | 5 | 6 | 2 | 0 | 8 | 6 | 對照組甲（須 `≥ 1`）✅ |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱·執行期組出）| 0 | 0 | 0 | 0 | 0 | 0 | 對照組乙（須 `= 0`）✅ |

⇒ **器非紅**；單 `§一` 閘 `3` 之**自指款未觸發**（本單於量測時尚未入倉）。

### 零-3　原封入倉 ＋ `sha256` 三點逐位

| 項 | 值 |
|---|---|
| 落檔 | `docs/orders/W-G.9-261_鏈頭起點更正_無WINNER街廓自抵費地遠側界起算.md` |
| 位元組 | **`7647` B**／`LF 107`／**`CR 0`** |
| 來源 `sha256`（主 checkout）| `d2a2cf355c055d111cb915eb28179c75811ecad2169fca833a6a0d4ae190e948` |
| 落檔 `sha256`（本 worktree）| **同上** |
| 入倉 blob `sha256`（內容）| **同上**；blob size ＝ `7647` ⇒ 無 CRLF 正規化 |

🛑 **射程界限（承 `追加四` 之既載·照實）**：本驗所證者為「**來源檔 → 落檔 → 入倉**」之保真；
**⛔ 證「發單側送出之原文 → 主 checkout 之落檔」之保真**。

### 零-4　🔴 commit 之拆分（**偏離之逐字回報**）

初次 commit 曾把**工項零之單入倉（零生產碼）**與**生產碼**併為一個 ⇒ 前者遂**無法單獨快轉主線**。
依 `W-G.9-195 裁 四`（「凡單所令之 commit 順序，會使回報所繫之物落在**不可 push 之區間**者，
受單方應**先推**並逐字回報該偏離」）之同一理路，**拆為二**：

| commit | 生產碼判（`常規一` 補款之逐字指令）| 性質 |
|---|---|---|
| `70ce7d4` 工項零：施工單原封入倉 | 命中 **`0`** 行 | **零生產碼** |
| `023765c` c1：Rw 鏈頭起點更正 | 命中 `app.py`／`verify/stepg_pipeline.py` | **生產碼** |

🛑 **CC ⛔ 逕推主線之任一者**——單 `§六` 逐字「⛔ 併主線……直至 KL 放行」，
且 `W-G.9-206 §八` 令「未放行前主分支⛔ 前進一個 commit」。
🔒 **`70ce7d4` 係 `023765c` 之父** ⇒ 若發單側／KL 認為工項零應先入主線，其為**快轉可達**，
⛔ 需重做。**是否快轉 ＝ 意思決定 ⇒ 候發單側／KL。**

---

## §一　開工閘（五項·**逐條當場復現**）

| 閘 | 單所載期望 | 實測 | 判 |
|---|---|---|---|
| `1` 錨 | `origin/wip/s1-endpart` ＝ `eedfbc6` | `eedfbc69f43919c435c2e6c02773fb49901f57b7`（`ls-remote`）＝ 本地 `HEAD` | ✅ 逐位符 |
| `2` 受詞 | `app.py` `4860f10828fb8ae9`／`stepg` `c8d46d2e5fb79dc5` | **逐位相符**（併驗 `wf_f0` ＝ `b0722355c1978635`）| ✅ |
| `3` 號占用 | `D1 0`／`D2 0`／`D3 0` | **`0`／`0`／`0`**（見 `§零-2`）| ✅ |
| `4` 次號 | 本批**⛔ 鑄任何號**；三簿 `MAX` 前後不變 | 前值 ＝ 自誤 **`321`**／`GB` **`154`**／`K-9` **`29`**（框逐字見下）| 見 `§六` 之後值 |
| `5` `S-0b` | `rc=0`、`P-5` 相符 | **`rc=0`**；機械停機款 `0` 項／提示 `0` 項；`P-5` 實算 `b26932fc6848574f…` ＝ 單載 | ✅ |

🔒 **閘 `4` 之框逐字**（`常規四（八）三`）
- 自誤（母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔·`7688` 列）：
  定義框 ``^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`?`` ＋ 範圍框（`VR-091 四`）⇒ 相異 `305`／`MAX 321`
- `GB`（母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔·`5275` 列）：
  標題錨定框 ``^#{2,4} .?`?GB-([0-9]{1,4})`?`` ⇒ 相異 `68`／`MAX 154`
- `K-9`（母體 ＝ `docs/rulings/K-6_…md` 單檔·`4057` 列）：
  ``^#{2,4} \s*🔒?\s*K-9-([0-9]{1,3})(?![0-9-])`` ⇒ 相異 `24`／`MAX 29`
- **所扣之哨兵**：三簿皆 `[]`（未扣除數 ＝ 已扣除數）——`W-G.9-183` 補款所令之二數並報。

---

## §二　工項一：現況之確認（**全單之地基**·純量測·⛔ 動碼）

**量測法**：拋棄式 clone `C:/t26f`（`HEAD ＝ eedfbc6`），注入三事——
① `app.py` 之 `②-宗 圍堵閘` `raise → print`（錨 ＝ 訊息列之上一列·唯一）；
② `verify/stepg_pipeline.py` 之 **`11` 處**結構閘 `raise → print`
（列 `1198`／`1389`／`1397`／`1430`／`1439`／`1464`／`1473`／`1516`／`1543`／`1551`／`1598`·逐列驗其內容為 `raise RuntimeError(`）；
③ 於 `if not _sd_has:` **之前**注入唯讀探針（`79` 列·錨命中 `1`·判別力 寬字樣 `_sd_has` ＝ `4`）。
`diff --numstat` ＝ `app.py 1/1`／`stepg 90/11`；`py_compile` 過。
🛑 **降級係<u>量測手段</u>·⛔ 修法主張**；主倉三受詞一字未動（見 `§六 V-A`）。

### 二-1　`A-1`　`3.5m·R2 左側`確為無 WINNER 且強制抵費地充當第 1 宗　✅

| 受詞 | 來源（碼面·倉側 blob）| 實測 |
|---|---|---|
| forced 旗標 | `stepg:644` `_fo_left = bool(_fo_block.get('left_forced_offset', False))`（其源 ＝ `ss['f3L_forced_offset']` ＝ `forced_map`）| **`True`** |
| 有無私有街角勝者 | 鏈頭之 `第1筆街角` 欄（`stepg:487` `'第1筆街角': '是' if _is_first_corner else '否'`）與 `_chain` 節點之 `marker`（＝ `entry['is_first_corner_marker']`）| 鏈頭 `628-42(1)`：**否**／`marker ＝ False`／`街角地 ＝ 否` |
| 抵費地佔第 1 宗之位 | `stepg:647` `_left_buffer_S = _corner_buffer_S(…)`（bisect 解 `buf` 使**真實池帶面積 == 街角最小面積**）＋ `stepg:773` `left_cum_S = float(_left_buffer_S)` ⇒ 鏈自 `s = buf` 起 | `buf ＝ 8.696079893776`；**角落抵費地 ＝ `308.1700 ㎡` ＝ 街角最小面積 `308.1700 ㎡`**（逐位） |

### 二-2　`A-2`　🔴 **決定性**：`W₀_left ＝ 6.98` 是否確為抵費地之**遠側境界線**之 `W`　✅ **是**

🔒 **倉內既有之逐字定義**（⛔ CC 之推論）：`stepg:1014` 逐字
「⛔ **`forced` 側維持原式——其目標為<u>抵費地遠側界</u>**，卡『保留區之表示法』（`W-G.9-19 D3`）」；
其式 `stepg:1015` ＝ `_b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block)`，
而 `_mp_base_W0`（`:990`–`:1003`）純委派 `k956_W_from_mp`（`W ＝ (群起點 ＋ buf·d̂ − mp)·â`）。

| 量 | 值（`3.5m·R2 left`）|
|---|---|
| 抵費地**近側界**之 `W`（`buf = 0`·＝群起點自身）| **`-1.693923622653`** |
| 抵費地**遠側界**之 `W`（`_b_L0`·倉內式）| **`6.980480447374`** |
| 抵費地**面積**（`_corner_off_L`）| **`308.1700 ㎡`**（＝ 街角最小面積 `308.1700`·逐位）|
| 閘所捕獲之 `W₀_left`（`stepg:837` `res['W_near']`）| **`6.980000`** |
| **`|Δ|`** | **`0.000480`** ≤ `0.005`（`2dp` 捨入半量子）|

⇒ 🔒 **單之前提<u>成立</u>：`W₀ ＝ 6.98` 確為該強制抵費地之遠側境界線之 `W`，⛔ 巧合等值。**
🔒 **佐證二（獨立·非同一量測器）**：`t26e` log 之 `[T2-DIAG] 街廓 R2`（`3.5m`）逐字載
「池帶 `s` 寬 `[8.4283 33.0811]`｜池帶面積 `[308.1700 1471.2568]`｜`s` 域 `[0.2677,100.0274]`」
⇒ `0.2677 + 8.4283 = 8.6960` ＝ `buf`（逐位）；其帶面積 `308.1700` ＝ 上表之抵費地面積。

🩸 **本節<u>另捕</u>一陷阱（照實·⛔ 影響上判）**：`_b_L0`／`_b_R0` 僅於**非退化分支**賦值
⇒ 走退化分支之街廓**殘留前一街廓之值**。實測：`3.5m·R4 left` 之 `b0` 與 `R2` **逐位相同**
（`6.980480447374`）而其 `buf` 為 `7.818563518909` ⇒ **殘留坐實**。
🔒 **上判不受影響**：`R2` 之前一筆（`3.5m R1 left`）之 `b0 ＝ 0.0`（非 forced）
⇒ `R2` 之 `6.980480…` **必為當場重算**。`R4`／`0m R4` 之 `b0` **⛔ 採信**（已具名）。

### 二-3　`A-3`　全部鏈頭（**兩情境 × 全街廓 × 左右側 ＝ `24` 條·⛔ 取樣**）

| 情境 | 街廓 | 側 | `has` | forced | `W₀` | `W_f` | 鏈頭 | **第1筆街角** | `marker` | 頭 `W_near` | 頭 `Rw` | 該側 rows 全體／`F>0` |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `0m` | `R1` | left | ✅ | ✗ | `-0.0000` | `24.4900` | `628-37(1)` | **是** | True | `-0.0` | `52.64` | 2／2 |
| `0m` | `R1` | right | ✅ | ✗ | `-0.4000` | `27.5500` | `628(5)` | **是** | True | `-0.4` | `55.8` | 3／3 |
| `0m` | `R2` | left | ✅ | ✗ | `0.0000` | `26.0300` | `628-41(1)` | **是** | True | `0.0` | `38.14` | 7／7 |
| `0m` | `R2` | right | ✗ | ✗ | — | — | **（空鏈）** | — | — | — | — | **7／0** |
| `0m` | `R4` | left | ✅ | **✅** | `4.4800` | `65.1400` | `628(1)+` | **是** | True | `4.48` | `59.22` | 1／1 |
| `0m` | `R4` | right | ✅ | ✗ | — | — | （空鏈）| — | — | — | — | 0／0 |
| `0m` | `R6` | left | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 3／0 |
| `0m` | `R6` | right | ✅ | ✗ | `-0.0000` | `24.4200` | `628-18(1)` | **是** | True | `-0.0` | `56.77` | 5／5 |
| `0m` | `R5` | left | ✅ | ✗ | `-0.0000` | `17.4900` | `628-18(2)` | **是** | True | `-0.0` | `43.06` | 6／6 |
| `0m` | `R5` | right | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 2／0 |
| `0m` | `R3` | left | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 5／0 |
| `0m` | `R3` | right | ✅ | ✗ | `-3.4000` | `28.1300` | `628-30(3)+` | **是** | True | `-3.4` | `88.3` | 5／5 |
| `3.5m` | `R1` | left | ✅ | ✗ | `-0.0000` | `17.5200` | `628-36(1)` | **是** | True | `-0.0` | `98.75` | 1／1 |
| `3.5m` | `R1` | right | ✅ | ✗ | `-0.4000` | `34.5500` | `628(5)` | **是** | True | `-0.4` | `55.8` | 4／4 |
| **`3.5m`** | **`R2`** | **left** | ✅ | **✅** | **`6.9800`** | `34.3900` | **`628-42(1)`** | 🔴 **否** | **False** | `6.98` | `55.59` | 7／7 |
| `3.5m` | `R2` | right | ✗ | ✗ | — | — | **（空鏈）** | — | — | — | — | **7／0** |
| `3.5m` | `R4` | left | ✅ | **✅** | `7.7900` | `68.6200` | `628(1)+` | **是** | True | `7.79` | `40.06` | 1／1 |
| `3.5m` | `R4` | right | ✅ | ✗ | — | — | （空鏈）| — | — | — | — | 0／0 |
| `3.5m` | `R6` | left | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 3／0 |
| `3.5m` | `R6` | right | ✅ | ✗ | `-0.0000` | `24.4200` | `628-18(1)` | **是** | True | `-0.0` | `56.77` | 5／5 |
| `3.5m` | `R5` | left | ✅ | ✗ | `-0.0000` | `17.4800` | `628-45(1)+` | **是** | True | `-0.0` | `61.4` | 6／6 |
| `3.5m` | `R5` | right | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 2／0 |
| `3.5m` | `R3` | left | ✗ | ✗ | — | — | （空鏈）| — | — | — | — | 5／0 |
| `3.5m` | `R3` | right | ✅ | ✗ | `-3.4000` | `28.1300` | `628-30(3)+` | **是** | True | `-3.4` | `88.3` | 5／5 |

🔒 **判 `1`**：`24` 條中，鏈頭「第1筆街角 ＝ **否**」者**恰 `1` 條** ＝ **`3.5m·R2 left`** ⇒ **本改動之射程恰一鏈**。
🔒 **判 `2`**：forced 者 `3` 條（`0m R4 left`／`3.5m R2 left`／`3.5m R4 left`），
惟 `R4` 二者之鏈頭**仍為街角勝者**（`是`／`marker True`）⇒ 其 `is_corner` 已為真 ⇒ **本改動⛔ 及之**。
⇒ 🔒 **「forced」與「無 WINNER」⛔ 等價**——單 `§零` 之受詞為後者。

🔴 **判 `3`（與單 `§二 A-3` 所載之發單側基準<u>相異</u>·照實出艙）**
單載「`R2 right` 兩情境皆『否』且 `W ＝ 0.0`／`Rw ＝ 0.0`」。
**CC 實測**：`R2 right` 兩情境之 `_has_right_corner ＝ False`（`corner_range_keys` 逐字為
`['R1/left','R1/right','R2/left','R3/right','R4/left','R4/right','R5/left','R6/right']`——**⛔ 含 `R2/right`**），
該側 rows 全體 `7` 宗而 `F > 0` 者 **`0`** ⇒ **空鏈·<u>根本無鏈頭</u>**，
⛔ 「鏈頭之第1筆街角為否」。單所載「`R1` 四條鏈頭皆『是』」與「`3.5m R2 left` 為『否』」則**逐項復現**。

### 二-4　`A-4`　`R2 right` 兩情境之鏈頭 `W_near`　⇒ **不存在**（空鏈）

單 `A-4` 之停機款逐字為「**若非 `0`**，則本改動之射程不止一宗，須具名並停機候裁」。
**實測既非 `0` 亦非「非 `0`」——該側無鏈頭。**
🔒 **就該款之<u>受詞</u>（射程是否不止一宗）判：未觸發**，其證據為 `A-3` 之**全 `24` 條表**
（鏈頭「否」者恰 `1`），較 `A-4` 之代理量**更直接**。
併具名之機制理由：該側 `F ≤ 0` ⇒ `app.py:6445` `_apply_rw = (F > 0.0 and l_side > 0.0)` 為偽
⇒ `Rw` 恆 `0` ⇒ **`_rw_start` 之值於該側結構上無後果**。

### 二-5　🔒 判別力自證（供 `工項二` 之 `or` 形）

全 `24` 條之逐宗掃描：**非鏈頭而 `is_first_corner_marker` 為真之筆數 ＝ `0`**
⇒ `is_corner` **恆蘊含「鏈頭」** ⇒ 以 `(is_corner or is_chain_head)` 為式，
**⛔ 使任何非鏈頭之宗改變其起點**。

---

## §三　工項二：改碼

### 三-1　決定點之唯一性（**當場查·⛔ 逕信 `K-9-5-6` 之自述**）

| 檔 | `_rw_start` 命中 | 角色 |
|---|---|---|
| `app.py` | **`5`** 列（`:10097` 初值／**`:10139` 決定點**／`:10236`／`:10242` 輸出欄）| **唯一產生者** |
| `verify/stepg_pipeline.py` | `3` 列（`:1287`／`:1289` 註解·`:1291` `_n['res'].get('W_rw_start')`）| **消費端** |
| `verify/wf_f0.py`／`wf_f2.py`／`wf_f4.py`／`run_verification.py` | 各 **`0`** | — |

⇒ 🔒 **`K-9-5-6` 之「碼面唯一決定點」自述<u>經復驗為真</u>** ⇒ **⛔ 同源二份**、⛔ 二處同改。

### 三-2　所改之式（`app.py`·唯一行為變更）

```
改前： _rw_start = _W_near if is_corner else float(W_prev)
改後： _rw_start = _W_near if (is_corner or is_chain_head) else float(W_prev)
```

🔑 **一式同時滿足二裁**（單 `§三` 逐字）：鏈頭為街角勝者 ⇒ 其 `W_near ＝ 0`（`K-9-5-12（五）1`）；
鏈頭為無 WINNER 之第 2 宗 ⇒ 其 `W_near ＝ 抵費地遠側界`（`K-9-5-12（四）`）。**⛔ 有 `if forced` 之分支。**

### 三-3　接線（**純加性**·`is_chain_head` 預設 `False`）

| # | 檔:符號 | 內容 |
|---|---|---|
| `1` | `app.py` `solve_G_binary` | 簽章新增 `is_chain_head: bool = False` |
| `2` | `app.py` `_solve_G_one` | 簽章新增 ＋ 直通 `solve_G_binary` |
| `3` | `app.py` `_solve_one`（`main()` 內嵌薄殼）| 新增 `_is_chain_head=False` ＋ 直通 |
| `4` | `verify/stepg_pipeline.py` `_solve_one`（薄殼）| 同上 |
| `5`–`10` | 六處推進呼叫端（app 左/右/右重試 ＋ stepg 左/右/右重試）| 傳 `(_lg_idx_left == 0)`／`(_lg_idx_right == 0)` |

🔒 **⛔ 傳該旗標者**（其行為逐位不變）：`_corner_first_lot_G`（**資格路徑**）、
階段2 落位（`is_corner` 硬編 `False`）、`verify/wf_f0.py`。
🔒 **`is_chain_head` 之全倉命中**：`app.py` `10`／`stepg` `5`／`wf_f0` `0`／`run_verification` `0`／`run_all` `0`。

### 三-4　界（單 `§三` 之四禁·**逐項驗**）

| 禁 | 驗法 | 結果 |
|---|---|---|
| ⛔ 將 `_rw_start` 一律改為 `W_near` | 式中 `else float(W_prev)` 保留 | ✅ 未逾 |
| ⛔ 動 `telescoping` 閘（`stepg:1410`–`:1444`）| `git diff -U0` 之 hunk 全在 `:511`／`:520`／`:827`／`:914`／`:929` | ✅ 最近之 hunk 距 `:1410` 逾 `480` 列 |
| ⛔ 動 `②-宗` 閘（`app.py:9524`）| `app.py` hunk 於 `:10002` 起 | ✅ 距 `:9524` 逾 `478` 列 |
| ⛔ 動 `_select_pool_slot` 之 `_b_L0`／`_b_R0`（`stepg:1015`–`:1016`）| 同上 | ✅ 最近 hunk `:929` |

`git diff --numstat`（`eedfbc6..023765c` 之碼側）＝ **`app.py 29/4`**／**`verify/stepg_pipeline.py 6/2`**；
`py_compile` 二檔皆過。

---

## §四　工項三：土地後果（**逐宗·⛔ 取樣**）

**量測法**：二拋棄式 clone **皆原態**（⛔ 閘關·⛔ 探針）——`C:/pre1`＠`eedfbc6`（`app.py 4860f10828fb8ae9`）／
`C:/pst1`＠`023765c`（`app.py 1e15d45ae84b02ca`），各跑 `verify/run_verification.py`。
母體 ＝ `got_G值_退縮{0m,3.5m}_partial.csv`（**`53` 欄·欄集改前改後逐位相同**）；鍵 ＝ `(情境, 所屬街廓, 暫編地號)`（⛔ 列序）。

### 四-1　落檔範圍

| 情境 | 改前 | 改後 | 中止街廓 改前 → 改後 |
|---|---|---|---|
| `0m` | `21` 列 | `21` 列 | `R2` → `R2`（**不動**）|
| `3.5m` | `24` 列 | **`47` 列** | `R2` → **`R5`**（🔑 **新射程 `R3`／`R4`／`R6`**）|

**共同鍵**：`0m` `21`／`3.5m` `24`；**只在改前** 二情境皆 **`0`**；**只在改後** `0m` `0`／`3.5m` `23`。

### 四-2　🔴 `3.5m·R2` 之逐宗（改前 → 改後·**左鏈全 `7` 宗**）

| 宗 | `W(m)` | `Rw(%)` | `S(m)` | `宗地寬度(m)` | `G(㎡)` | `幾何面積(㎡)` | `負擔比率` |
|---|---|---|---|---|---|---|---|
| **`628-42(1)`**（鏈頭）| `6.98 → 7.09` | **`55.59 → 0.58`** | **`0.0 → 0.11`** | **`0.0 → 0.11`** | **`0.0 → 4.77`** | `0.01 → 4.77` | `0 → 0.4281` |
| `628-41(1)` | `11.05 → 11.16` | `20.34 → 20.20` | `4.08`（不變）| `4.07`（不變）| `183.17 → 183.23` | `183.17 → 183.22` | `0.4263 → 0.4262` |
| `628-27(1)` | `11.71 → 11.82` | `2.84 → 2.83` | `0.66` | `0.66` | `29.61`（不變）| `29.61 → 29.62` | `0.423`（不變）|
| `628-40(1)+` | `18.37 → 18.48` | `21.25 → 20.77` | `6.68` | `6.66` | `299.32 → 299.51` | `299.32 → 299.50` | `0.4176 → 0.4172` |
| `628-30(2)` | `20.38 → 20.49` | `0.0`（不變）| `2.01` | `2.00` | `89.97`（不變）| `89.98 → 89.97` | `0.4014`（不變）|
| `628-39(1)` | `25.65 → 25.76` | `0.0` | `5.29` | `5.28` | `236.54`（不變）| `236.54`（不變）| `0.4014` |
| `628-31(2)` | `34.39 → 34.50` | `0.0` | `8.77` | `8.75` | `391.66`（不變）| `391.66 → 391.67` | `0.4014` |
| **`ΣRw`（left）** | | **`100.02 → 44.38`** | | | | | |

🔑 **成因之單一歸因**：鏈頭 `628-42(1)` 之 `Rw` 由 `55.59` 降為 `0.58` ⇒ 其 `G` 由 `0.0` 升為 `4.77`
⇒ 其 `S` 由 **`0.0` 變為 `0.11 m`**（該宗**由「不佔地」變為「佔 `0.11 m`」**）
⇒ 同側其後六宗之推進起點沿 `d̂` **後移 `0.11 m`** ⇒ 其 `W` **齊一 `+0.11`** ⇒ `Rw`／`G` 隨之微調。
🔒 **判別力（證係「整體平移」而非「重新分配」）**：該側**除鏈頭外，`S` 與 `宗地寬度` 逐宗<u>全部不變</u>**。

### 四-3　調配池（`R2`）與守恆

| 量 | 改前 | 改後 | Δ |
|---|---|---|---|
| `R2-抵費地-1`（**中央池**）幾何面積 | `1471.26` | **`1466.26`** | **`−5.00`** |
| `R2-抵費地-2`（**街角強制抵費地**）幾何面積 | `308.17` | `308.17` | **`0.00`**（不變）|
| `Σ池`（`R2`）| `1779.43` | **`1774.43`** | **`−5.00`** |
| `ΣG`（`R2`·帶號）| `2589.17` | `2594.19` | **`+5.02`** |
| `Σ|G|`（`R2`·絕對值）| `2589.17` | `2594.19` | `+5.02` |
| `ΔΣG ＋ ΔΣ池` | | | **`+0.02`**（`2dp` 捨入）|

⇒ 🔒 **KL 之裁「抵費地那段免計 ⇒ 池減少」<u>實測坐實</u>**：`R2` 調配池**減少 `5.00 ㎡`**，
且 **`ΔΣ池 ＝ −ΔΣG`** 對帳成立。⛔ 以「應該如此」出艙。
🔒 **減少者為<u>中央池</u>**（`抵費地-1`）；**街角強制抵費地（`308.17`）本身一字未動** ——
與 `K-9-5-12（四）`「抵費地本身不計 `G`、不負擔」逐字相合。

**守恆 `ΣG ＋ 池 ＝ 街廓`（逐街廓·帶號與絕對值並列見上）**

| 情境 | 街廓 | 改前 殘差 | 改後 殘差 | 判 |
|---|---|---|---|---|
| `0m` | `R1` | `+0.00` | `+0.00` | ✅ |
| `0m` | `R2` | `−1800.01` | `−1800.01` | ⚠️ **改前＝改後**（`R2` 為中止街廓·抵費地未算出）⇒ **既有·⛔ 本批所致** |
| `3.5m` | `R1` | `−0.02` | `−0.02` | ✅ |
| `3.5m` | `R2` | `+0.00` | **`+0.02`** | ✅（`2dp` 捨入內）|
| `3.5m` | `R4` | （未落檔）| `−0.02` | ✅ 新射程 |
| `3.5m` | `R6` | （未落檔）| `−0.01` | ✅ 新射程 |
| `3.5m` | `R5` | （未落檔）| `−1765.31` | ⚠️ `R5` 為改後之**中止街廓**·抵費地未算出 ⇒ 同 `0m R2` 之形 |

---

## §五　【驗收】

| 閘 | 判準（單 `§五` 逐字要旨）| 實測 | 判 |
|---|---|---|---|
| **`V-1`** | 兩情境共同落檔宗中，除 `3.5m·R2 left·628-42(1)` 外，`Rw(%)`／`G(㎡)`／`宗地寬度(m)` 逐位不變 | `0m`：共同鍵 `21`·相異格 **`0`** ✅／`3.5m`：**`Rw(%)` 相異 `3` 宗、`G(㎡)` 相異 `2` 宗**（`宗地寬度(m)` 相異 **`0`** 宗）| 🔴 **未達**（見下） |
| **`V-2`** | `3.5m·R2 left` 之 telescoping **不再破**（容差 `0.1`）| `ΣRw ＝ 44.38` vs `R(34.50)−R(6.98) ＝ 44.414`·`Δ ＝ 0.034 ≤ 0.1`；全 log 之「結構閘 telescoping 破」命中 **`1` → `0`** | ✅ |
| **`V-3`** | `0m` 之中止點仍為 `②-宗 圍堵閘`＠`R2`、`biz_x ＝ 45.9766` | 改前／改後之 `中止閘` 欄**逐位相同**：`②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 2.8906`；`0m` 逐宗相異格 `0` | ✅ |
| **`V-4`** | `ΣG ＋ 池 ＝ 街廓` 於改後每一街廓成立 | 見 `§四-3`；已落檔且非中止之街廓殘差 `≤ 0.02` | ✅（中止街廓之 `−1800.01`／`−1765.31` 係抵費地未算出·**改前改後同形**）|
| **`V-5`** | 出艙改前／改後之紅項數**與其計數式**（🛑 單明令**⛔ 立「紅項須減少」為閘**）| 紅 **`24` → `24`**·**名稱差集雙向皆空**·共同項 `24` | ✅ |
| **`V-6`** | `R1` 四條鏈頭之 `Rw` 逐位不變 | `0m` `52.64`／`55.8`；`3.5m` `98.75`／`55.8` ——**改前＝改後**，且**與單所載四值逐位相符** | ✅ |

### 五-0　`V-5` 之三軸與計數式

**量測法**：同二 clone（`C:/pre1`＠`eedfbc6`／`C:/pst1`＠`023765c`·**皆原態**），各跑 `verify/run_all.py`。

| 量 | 改前（`pre_runall.log`）| 改後（`pst_runall.log`）|
|---|---|---|
| 三軸：母體 | `49162` B ／ `543` 列 | `52176` B ／ `561` 列 |
| **框 A**　`grep -c "🔴 FAIL"` | **`24`** | **`24`** |
| 框 C　`grep -c "^ *🔴 FAIL  "` | `24` | `24` |
| 對照組（器須綠·須 `> 0`）`grep -c "✅ PASS"` | `10` | `10` |
| 對照組乙（必不存在·代稱·執行期組出）| `0` | `0` |
| `RESULT` 末筆／`rc` | `RESULT: FAIL`／`1` | `RESULT: FAIL`／`1` |

🔒 **逐項具名（單所令）**：以 `🔴 FAIL  <名目>` 抽名後排序比對——
**只在改前 `0` 項／只在改後 `0` 項／共同 `24` 項** ⇒ **紅項<u>集合</u>逐項相同**。
⇒ 本改動於 `run_all` 名目層 **⛔ 新增任何紅項，亦⛔ 消解任何紅項**。

🛑 **⛔ 讀為「本改動無後果」——名目雖同，其<u>原因已變</u>**（當場查證·⛔ 推論）：
承載本案之名目為第 `11`／`12` 項 **`v3·StepG0m（結構閘/看守觸發）`**／**`v3·StepG3.5m（結構閘/看守觸發）`**
（其餘 `22` 項無 `telescoping`／`StepG` 字樣）。其 `3.5m` 一項之**觸發閘**：

| | 改前 | 改後 |
|---|---|---|
| 名目 | `v3·StepG3.5m（結構閘/看守觸發）` | **同**（逐位）|
| 中止街廓 | `R2` | **`R5`** |
| 中止閘（CSV `中止閘` 欄之逐字）| `🔴 結構閘 telescoping 破：街廓 R2 left 側 ΣRw_實跑=100.02 ≠ …=44.41（Δ=55.606 >0.1）` | `街廓 R5 抵費地計算失敗：🔴 ②-宗 圍堵閘破[R5]：宗-宗重疊 = 56.8186 > 上界 1.5999` |

🔴 ⇒ **此係 `W-G.9-7` 所立「同一名目下換一個全新的破損，對帳仍為綠」之<u>活體實例</u>**
（其時 `22` 名目中 `4` 項已發生）。**⛔ 以「名目數不變」宣稱零影響**；
本批之影響須讀 `V-1`／`V-2`／`§四` 之逐宗層，⛔ 讀名目層。
🔒 併記：`0m` 一項之中止閘**逐位未變**（`②-宗 圍堵閘破[R2]：45.9766`）⇒ 該項確為真不動。

### 五-1　🔴 `V-1` 未達之逐格與歸因（**照實·⛔ 掩飾**）

| 宗 | 欄 | 改前 → 改後 |
|---|---|---|
| `628-41(1)` | `Rw(%)` | `20.34 → 20.20` |
| `628-41(1)` | `G(㎡)` | `183.17 → 183.23` |
| `628-27(1)` | `Rw(%)` | `2.84 → 2.83` |
| `628-40(1)+` | `Rw(%)` | `21.25 → 20.77` |
| `628-40(1)+` | `G(㎡)` | `299.32 → 299.51` |

🔒 **`宗地寬度(m)` 之肢<u>成立</u>**（相異 `0` 宗）；不成立者為 `Rw(%)`（`3` 宗）與 `G(㎡)`（`2` 宗）。
🔒 **成因 ＝ 單一連鎖**（`§四-2`）：鏈頭由「不佔地」變為「佔 `0.11 m`」⇒ 同側其後各宗齊一後移 `0.11 m`。
🔴 **單 `§四` 之發單側預測「其餘一宗不動」<u>被推翻</u>**——單自載該預測「**⛔ 已證·CC 證之或推翻**」
⇒ 本批**推翻之**並具名成因。
🛑 **`V-1` 係<u>驗收閘</u>而非僅預測** ⇒ 其未達為**須上呈之事實**，**⛔ 由 CC 自行放寬或改判準**
（`W-G.9-199 裁 H` 之一般式：判準須綁性質，⛔ 綁計數代理；而本閘之性質「射程恰一宗」
於**直接受詞**（鏈頭）成立、於**連鎖後果**不成立）。

---

## §六　本批之界與出艙

🔒 **`V-A`（受詞不動）**：主倉之 `verify/wf_f0.py` blob ＝ `b0722355c1978635`（前後逐位相同）；
`app.py`／`verify/stepg_pipeline.py` **係本單所令之受詞**，其改動逐項載於 `§三`。

🔒 **三簿 `MAX` 前後**（單 `§一` 閘 `4`·**本批⛔ 鑄任何號**）——見 `§六-2`。

🔒 **`deletions`**：`70ce7d4` 為純新增（`deletions ＝ 0`）；`023765c` 之 `app.py 29/4`、`stepg 6/2`
之刪除欄**非 `0`** ⇒ **⛔ 滿足 `常規一 ②`**，惟其為**生產碼 commit** ⇒ 本即須請示，⛔ 逕推。

### 六-1　🛑 停機並呈 KL（單 `§六` 所令之**一句實務語言**）

> `R2` 在退縮 `3.5` 公尺下，`628-42(1)` 的側街負擔從 **`55.59%`** 變成 **`0.58%`**，
> 它的分配面積從 **`0` 平方公尺**變成 **`4.77` 平方公尺**，
> `R2` 的調配池從 **`1779.43` 平方公尺**變成 **`1774.43` 平方公尺**（減少 `5.00`）；
> 同側其餘六宗因為這一宗從「沒佔到地」變成「佔了 `0.11` 公尺」而整排往內挪了 `0.11` 公尺，
> 分配面積跟著微調（最大一筆 `+0.19` 平方公尺），**⛔ 是「完全不動」**。

🛑 **⛔ 併主線、⛔ 動 `CLAUDE.md` 之 `⬜` 項，直至 KL 放行。**
