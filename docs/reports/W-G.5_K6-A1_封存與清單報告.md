# W-G.5 — **K-6-A1** 報告（裁定檔＋【A】清單＋【E】M-5 封存）

> 起點 `e583075`。上游＝KL 裁 K-6（2026-07-30）＋ claude.ai 兩項複驗與排程調整。
> **行號一律當場 grep**。

---

## 〇、頭條：**F.0–F.3 之 62 個子檢，先前從未被執行過**

`run_all` 由 `56 PASS／14 FAIL`（70 項）→ `130 PASS／2 FAIL`（**132 項**）。

**⚠️ 這不是「12 個紅變綠」**（前版報告之敘述**已更正**）。真相是：

> **F.0／F.1／F.2／F.3 四個粗粒度 FAIL，一 raise 即整代收攤**——
> 其下之子檢**根本沒跑**。它們既不是綠也不是紅，而是**不存在**。
> 封存 M-5 後整代跑完，**73 個新名目首次執行且全綠**。

| | 數 | 內容 |
|---|---|---|
| 前 | 70 | — |
| **消失名目** | **11** | 6 個 M-5 閘 ＋ 粗粒度 `W-F F.0`／`F.1`／`F.2`／`F.3` ＋ `v3 A 逐宗全查（115 列）` |
| **新增名目** | **73** | F.0／F.1／F.2／F.3 之細項檢查（全綠）＋ `v3 A 逐宗全查（118 列）` |
| 後 | **132** | `70 − 11 + 73 = 132` ✅ |

**E 先做之理由據此改寫**：不是「它零影響」（已由實測否證·見 §一），
也不只是「解除 12 個長期紅」，而是——
**它讓 F.0–F.3 整代首次真正執行，把 62 個從未被檢查過的項目變成可觀測**。

**歸因**：v3 baseline 烤於 **M-5（KL 裁 2026-07-25）之前**；M-5 上線後從未重烤（**P-H 凍結**）
⇒ 3.5m 情境之八個 baseline 對拍恆紅、F.0 GSA 錨檢恆破 ⇒ F.1–F.3 連坐**整代不執行**。
K-6 §四作廢 M-5 後，系統回到 baseline 所描述之狀態。
⚠️ **這不表示「M-5 是 bug」**，而是「**M-5 從未被 baseline 承認過，而 K-6 現在把它作廢了**」。

### 🔴 連帶更正：我把 14 紅當「既有紅」擱置了整整數批
我在 K-4-3／K-4／E 系列每一批都以「**FAIL 名目 diff 為空**」作驗收——
該判準**只證明本批沒新增紅**，既看不見「12 紅同源」，更看不見「62 個子檢從未執行」
（它們不在任何名目裡，diff 當然是空的）。
⇒ **驗收判準已升級**（KL 裁·本批起生效·已寫入 `validation-runbook`）：
**測項名目集合 diff（雙向）＋ 逐項歸因**，且 `前 − 消失 + 新增 = 後` 須驗算成立。

---

## 一、複驗-2：**由實測否證·我不撤回**（附 file:line）

claude.ai 稱「`parcels₁` 僅活於 `run_verification.py` 約 556–813 之 M-5 自我守恆對帳段
⇒ 封存 M-5 對 F.0 應為零行為變動」。

**當場 grep（`e583075` 態）**：`_bp_by_tag` 之消費點含

| 行 | 用途 | 是否在 556–813 內 |
|---|---|---|
| `run_verification.py:596` | 物化後**趟1 重跑** `run_corner_pk` ⇒ 產 `winners_state`／`forced_map` | 是 |
| **`run_verification.py:946`** | `run_step_g(..., _bp_by_tag.get(tag, build_parcels), winners_state, forced_map, ...)` ⇒ `stepg_ctx[tag]["sg"]` | **否** |
| **`run_verification.py:1215`** | **F.0 ctx 之 `"build"` 鍵**；同段 `"gA"` 吃 `_c["sg"]["g_rows"]` | **否** |

⇒ M-5 有**兩條**路徑進 F.0。
**實測裁判**：F.0 段不但非「逐字 diff 為空」，而是**整段自 FAIL 清單消失**（F.0 轉綠）。
另佐證：封存前 F.0 之錯誤訊息**自己**寫著「確認係合法連動（**如 M-5 提前合併**）」，
且未被評估之錨鍵 `G007` **正是** M-5 唯一 award 之 gid。

**⇒ 複驗-2 之結論與其排程理由（「E 對 F.0 零影響、最便宜」）皆不成立。**
E 依然應該先做——但理由是**它會把 12 個長期紅一次解除**，而非「它零影響」。

## 二、複驗-1：**我接受·我原判高估**

`_bl_perp_depth`（`app.py` 內·巢狀於 `parse_cad_precision_layers`）確為 N-12′ 量測原語。
K-6-A2 要做的是**提升為 module 級 ＋ 加逐宗取 min 之外圈**，
照 `evaluate_parcel_width_n14` 檔頭所述之抽出理由與做法（headless 可測、+1 gate）。
我原寫「需新建幾何運算」**高估規模**，已更正。

---

## 三、【A】MinA 消費點窮盡清單

清單：`docs/reports/W-G.5_K6-A_MinA消費點清單.md`（**278 點·8 類·無未歸類**）。

| 角色 | 筆數 | K-6 處置 |
|---|---|---|
| A1 街角規定面積（判準3） | 36 | **不動** |
| A2 街角PK診斷欄（源＝判準3） | 17 | **不動** |
| **B 逐街廓達標判定** | 89 | 改寬深雙檢（**A2 批**） |
| **C 池三則** | 14 | 改寬深雙檢（**A2 批**） |
| D `region_min` 推導／7-0a | 16 | 算式保留·用途收斂＋立閘（**A2 批**） |
| **E 1/2 門檻** | 15 | 依 §零-4 定稿補件改吃 `region_min`（**A2 批**） |
| F 僅顯示／報表 | 13 | 隨 B/C 同步 |
| G 文字敘述（非消費點） | 78 | 隨對應碼點改寫 |

⇒ **停機條件甲不觸發**（無任何無法歸類之消費點）。

---

## 四、【E】K-4-3／M-5 封存（本批已完成）

| 項 | 處置 |
|---|---|
| 碼封存 | `verify/m_rescue.py`／`verify/probes/probe_ruling_K4_3_source.py` → **`verify/archive/`**，檔頭加「**K-6 段三取代，KL 裁 2026-07-30 作廢**」＋作廢範圍＋取代者＋「**禁再接線**」 |
| 呼叫鏈切除 | `run_verification.py` 之 M-5 整段（趟0→build_plan→物化→趟1＋三閘一帳）**整段刪**，代以封存註記；`_m5_corner_ctx`／`_m5_true_g_fn` 兩 helper **整支刪**（全倉零消費者） |
| 殘餘 fallback 清除 | `_bp_by_tag`／`_m5_by_tag` 宣告刪除；`_bp_by_tag.get(tag, build_parcels)` **兩處**直接改 `build_parcels`（不留 `.get(…, …)` 之舊邏輯） |
| `run_all` | K-4-3 來源母體閘移除，代以封存註記 |

### 4.1 F.4 具名錨之連動（**已歸因·非回歸破口**）

封存後 F.4 報 `E0 規則導出 ≠ 具名錨`，多出一對 **`628-30(2) → 628-45(2)`**。

**自證式歸因**：`E0_EXPECT["0m"]` **本來就有該對**——0m 無 forced 端 ⇒ **M-5 從不執行** ⇒
`628-30(2)` 未被攔截。3.5m 缺該對，正因 M-5 在 F.0 看見它之前先把它當來源消費掉
（其 target `628-45(2)` **即 M-5 唯一 award 之 winner**）。
⇒ 封存後該同歸戶集中**改於 E0（級0）發生**，兩情境回復同構——
**這正是 K-6 §三所要的**（「跨街廓同歸戶『小往較大集中』一律歸七級調配（F.0／F.2）」）。
⇒ 3.5m 錨補回該對，**歸因逐句寫入 `verify/wf_f4.py` 之 `E0_EXPECT` 上方**（非默默改綠）。

### 4.2 測項名目集合 diff·逐項歸因（KL 補帳要求）

實測：`verify/out/K43_after2.log` vs `verify/out/K6A_E_after2.log`
（`前 70 − 消失 11 + 新增 73 = 後 132` ✅）。

**消失之 11 個名目·逐一歸因**

| # | 名目 | 歸因 |
|---|---|---|
| 1–6 | `M-5 定點閘{0m,3.5m}`／`M-5 無新生閘{0m,3.5m}`／`M-5 結算閘{0m,3.5m}` | **機制本身封存** ⇒ 其專屬閘隨之消滅（K-6 §四） |
| 7–10 | `W-F F.0`／`F.1`／`F.2`／`F.3` | **粗粒度 FAIL 消失**——非「轉綠」，而是該代**改為執行其下細項**，細項各自成為新名目（見下） |
| 11 | `v3 A 逐宗全查（115 列＝…）` | 名目**含列數**⇒ 列數變動即名目變動；`115 → 118`（見下自證） |

**`115 → 118` 之自證**（非推測·附 log 引用）

- 封存前：`verify/out/K43_after2.log:201` 之 M-5 結算閘訊息載
  `母體對稱差 A0∖P1=['628-30(1)', '628-30(2)', '628-45(1)']（全額消費源）` ＝ **恰 3 宗**。
- M-5 封存 ⇒ 該 3 宗**不再被全額消費移出母體** ⇒ 回到 `v3 A 逐宗全查` 之母體 ⇒ **+3 列**。
- 封存後：`verify/out/K6A_E_after2.log:5558` ＝ `v3 A 逐宗全查（118 列＝…）`。
  （封存前同項：`verify/out/K43_after2.log:278` ＝ `115 列`。）

**新增之 73 個名目**：F.0／F.1／F.2／F.3 各代之細項檢查（例：`F.0 位次序不變{0m,3.5m}`、
`F.0 標的宗毗鄰不變{0m,3.5m}`、`F.0 六格 G(Σa) 錨`、`F.0 旗標 31→17（新生 0）`、
`F.0 k* f0 錨 trunk B`…）＋ `v3 A 逐宗全查（118 列）`。**全數綠**，且**先前從未被執行**。

---

## 四之三、🛑 **停機上呈**：裁定檔 §零-3「面積閘蘊含深度」在幾何上不成立

KL 補丁單第三項要求把 §零-3 之「蘊含街角第 1 宗**寬度**合格」改為「**寬度與深度**」，
並設停機條件：「若街角規定範圍**其深度向並非由法定最小深度導出** ⇒ 停機上呈，勿逕改」。

**查核（`de64885` fresh grep·逐點附錨）**：

| 事實 | 位置 |
|---|---|
| 範圍之建構原語 ＝ **單一條平行 ALLOC 之切線**，自 `side_mid` 沿 ALLOC 法向平移 `shift_distance`，取靠 `side_mid` 之片、再扣截角 | `app.py:8971 _shift_cut_block_range`（`cut_line = _LSv2([...])` 一條） |
| `shift_distance` 之實參 ＝ `_setback_default + _legal_w_disp`（**退縮 ＋ 畸零地最小寬**·**只有寬度項**） | `app.py:15933` |
| 範圍在**深度向**之界定者 ＝ **街廓多邊形自身**（FRONT 邊→BASELINE 邊），**法定最小深度未出現於任何一處** | 同上·全函式無 `min_depth` 參數 |

⇒ **「面積閘蘊含深度」不成立** ⇒ **§零-3 維持原文·不逕改**。

**併呈（供裁決用·非我自行改判）**：街角第 1 宗之深度**其實會合格**，但**走另一條路**——
`E-2′` 已鎖「街廓分配線深度不得小於畸零地規則最小深度」（手冊 二(一)3；
`grep -n "E-2′ 宗地深度" app.py`），那是**街廓級**約束、對該街廓**所有**宗地成立，
**與面積閘無關**。⇒ KL 所要之效果（winner 不會被深度踢出 §三）**達得到**，
但**理由須改寫**為「由街廓分配深度之下限保證」，而非把「深度」二字併入面積閘那句。

---

## 五、本批**未做**之項（誠實記載）

**【D】「同原地號合併」述詞置換**——**未動**。理由與待決點：

K-6 §一要求「同歸戶 ∧ **幾何連通**（共用線段·單點相接不算）」。
claude.ai 之驗收要求「容差優先沿用倉內既有相鄰述詞之同一依據（先 grep `wf_f2` 之 `adj`）」。
**當場 grep 之發現**：`wf_f2` 之 `adj` 係 **街廓級**鄰接（`frozenset((sblk, tgt_blk)) in adj`），
而 K-6 要的是**重劃前地籍之宗地級共線段連通**——**兩者層級不同、其容差依據不可直接沿用**。
⇒ 需另定依據（N0-17-a：地籍量測誤差源），且**禁由觀測殘差回推**。
本批**不動**，於 A2 批開工前先報 grep 結果與依據導出，再施工。

---

## 六、驗收

- `py_compile`：`run_verification.py`／`run_all.py`／`wf_f4.py` → OK
- `run_all` 最終：**130 PASS／2 FAIL**（`verify/out/K6A_E_after2.log`）
- golden 段全綠（K-4-3 閘已移除、其餘二探針 rc=0）

### 6.1 剩餘 2 紅之逐項歸因

| FAIL | 歸因 | 是否本批造成 |
|---|---|---|
| `W-F F.4` | `[3.5m] E2 7-5 批次全域最佳化不可行——停機上呈`（窮舉無可行指派·容量約束） | **否·既有** |
| `W-G G.2` | `上游世代未產出：['F.4']` ——**純連坐**，其自身未破 | **否·連坐** |

**F.4 之 3.5m E2 不可行係既有、且早已上呈待域裁之項**（W-G.4 628-37 P2 波記載：
「3.5m E2 結構性不可行·上界 < 需求·成因既有」）。
⚠️ **它先前被遮住**：F.0 之 GSA 錨破先 raise ⇒ F.1–F.4 全數跳過 ⇒ 這個更深的紅**看不到**。
K-6-A1 解除 F.0 後，鏈跑得更深，才把它**曝出來**。
⇒ **本批不是製造了新紅，而是掀開了一層舊紅之下的另一層舊紅。**
與 §〇 之教訓同族：**紅被別的紅遮住時，它連「既有」都不會被登記。**

---

## 七、🚩 上呈

| # | 題 | 性質 |
|---|---|---|
| **U-K6-1** | **12 個長期 FAIL 之真因已定位為 M-5 未重烤**。是否於 K-6 全案落地後統一重烤 v3 baseline（P-H 解凍）？ | 需 KL 授權 |
| **U-K6-2** | **【D】容差依據**：`wf_f2.adj` 為街廓級、不可沿用；宗地級共線段連通之容差須另定 | A2 批開工前報 |
| **U-K3／U-K4／U-K5** | **全部結案**，理由「**K-6 取代**」（K-4-3 整套作廢） | 結案 |
| **GB-7／GB-8** | 若上批已登記（M-5 未接入 app UI 等），**因作廢而消滅** | 結案 |

**既有 🚩（未動）**：`data/地籍資料來源_匿名版.xlsx` 未 commit 異動。


---

## 八、K-8 段一（KL 裁 2026-07-31·**追記於本報告**）

### 8.1 🔴 我差點取代掉一個正確的機制——claude.ai 當場撤銷

KL 施工單原要求「BASELINE↔街廓配對改結構判準（共線重疊長 ≈ 實體全長），**禁用最近距離**」。
我依此寫了量測探針，**六格對照靶全中**，正準備改 `app.py` 之配對碼——
**claude.ai 撤銷**：我要取代的是 **KL 2026-07-26 第二批之 C-1／C-2／C-3／C-5**
（失敗考古 #38），該機制**六個街廓全部正確**。

**我誤讀之處**：把「近平行＋屁股側＋深度域＋垂距收斂」整包看成「距離判準」。
實則前三者是**結構條件**，垂距只是其後之**收斂鍵**。
R6 之 `#0/#2` 於條件 (a) 即因 **Δθ=89.71°** 遭排除，**根本無歧義**。

**併記一個我自己就該察覺的訊號**：我的重疊比表裡，
`#0` 對 R5／R6 皆為 **0.50**、`#2` 對 R2／R3 皆為 **0.50**——
那正是「**一條 BASELINE 橫跨兩並排街廓**」之特徵。
我把它讀成「判準有鑑別力」，但它同時也是**該判準會產生假紅之處**：
若某案之一條 BASELINE 恰橫跨兩街廓且**兩邊皆需配對**，結構判準會**雙雙 0.50 ⇒ 兩塊皆零命中 ⇒ loud raise**。
⇒ 重疊比**保留為診斷欄、不得升為選取閘**。

### 8.2 交付（**無碼變更**）

`verify/probes/probe_ruling_K8_baseline_pairing.py` 入倉並掛 `run_all`：

| 檢 | 內容 | 結果 |
|---|---|---|
| 【1】配對輸出 | **集合**斷言（R1／R4 ∈ `{#0,#2}`·R2／R3→`#3`·R5／R6→`#1`）；`matched_count=6`、缺件 ∅ | ✅ |
| 【2】重疊比 | **診斷欄**（明書不得作閘·0.50 之意義） | — |
| 【3】N-19′ | 六塊平均深度對照靶（垂直 BASELINE·無限直線·解析式） | ✅ 逐格 |

⚠️ **實測揭示斷言必須是集合**：既有機制對 **R1 與 R4 都選了 `#0`**（C-5 無條件等價路徑），
而我先前之結構量測給 R1→`#2`。若寫死單一編號即製造假紅。

### 8.3 交接文 §六「BASELINE 六塊齊備」未驗項 → **已結案**

`matched_count = 6`、缺件集合 `∅`，且六塊之配對輸出已逐塊機器對拍（見上）。

### 8.4 段二 N-19′：**量測已對靶·碼未改**

六塊逐格相符（Δ ≤ 4e-4）；連帶 `region_min` **114.07 → 115.87**（R4 仍最淺）、
1/2 門檻精確值 **57.935**（`Decimal(str)` 慣例顯示 **57.94**·🚩 請 KL 確認 2dp 落點）。
**`_compute_block_depth_alloc` 之取值法置換屬下批**——本批不動碼，避免半套狀態。
