# `W-G.9-325` 交接註：舊窗收工（**⛔ 零生產碼**·⛔ 鑄任何號）

> 🛑 **本文之受詞 ＝ 本窗自身記憶中、<u>倉內未載</u>之事實**；倉內已載者**⛔ 重述**，一律引其 `檔:列` 或其字樣錨。
> 🛑 **本文由 `63%` 之窗於換窗前所補**——其受詞為**該窗自身之記憶**，新窗結構上取不得
> （`恆常附款 k②` 之同族：由「新窗未見」⛔ 推「未發生」；**留白即永久遺失**）。
> 🔒 **體例**比照 `docs/reports/W-G.9-323_交接註_舊窗收工.md`。
> 🔒 **本節所載之七項，逐項⛔ 留白；無者具名「無」。**
> 🔒 **態** ＝ `e7b80c588d28375f36de6e9ab473965089630c5c`（本文落地前之 `HEAD`）。
> 🔒 **「列」之口徑** ＝ `split('\n')` 之長度（`恆常附款 w②⑤`）。
> 🛑 **本窗⛔ 開辦 `W-G.9-309`**（該單將於新 CC 窗辦理）。

---

## 項 `1`　本窗所辦之批・基座 → 末筆・`commit` 總數

| 量 | 值（**當場自倉實測**） |
|---|---|
| 基座 | `df1f2625fa3a0f25897c9851a052301f72b911c5`（＝ `W-G.9-323` 交接註之 `commit`·**舊窗末筆**） |
| 末筆 | `e7b80c588d28375f36de6e9ab473965089630c5c` |
| **`commit` 總數** | **`15`**（`git rev-list --count df1f262..HEAD`） |
| 所辦之批 | **`2`** 批：`W-G.9-324`（**中**·`7` 筆·`8e2719f`〜`4dadbd2`）／`W-G.9-325`（**輕**·`8` 筆·`88a2747`〜`e7b80c5`） |
| `push` | **`2`** 次·**皆首推即成**（`rc 0`·形 ＝ `git push origin HEAD:refs/heads/wip/s1-endpart`） |

🔒 **刪除欄非 `0` 之 `commit`（逐筆具名）＝ `1` 筆**

| `commit` | 檔 | `numstat` | 其由 |
|---|---|---|---|
| `686d88e9e7422b5318bcb8f6791672ec3c1f4db1` | `verify/probes/probe_WG9321_issuer_anchor.py` | **`+21`／`−7`** | `W-G.9-325` 工項五（態錨器二項之去 shell 化）——**單 `§三` 閘 `1` 所令之<u>唯一具名豁免</u>** |

⇒ 其餘 **`14`** 筆之刪除欄**逐檔 `0`**；**`15`** 筆之生產碼判法命中**皆 `0` 行·輸出皆空**。

---

## 項 `2`　單之遞交途徑有無變動

🔒 **無變動。** 二批皆照 `W-G.9-323` 交接註 項 `4`-`2` 之途徑行之：
KL 置單於**主 checkout** 之 `C:\Users\admin\Desktop\land-readjustment-trial\docs\orders\<單名>.md`（**⛔ 在 worktree 內**）；
CC 以**絕對路徑**讀入、**二進位複製**入 worktree 之同名落點，再以 `bytes`／列／`CR`／`sha256` **四量逐位對拍**。

| 批 | bytes | 列(split) | `CR` | `sha256` 二側 | 停機款 |
|---|---|---|---|---|---|
| `W-G.9-324` | `47661` | `441` | `0` | **逐位相同** | 款 `8` ⛔ 成就 |
| `W-G.9-325` | `32384` | `411` | `0` | **逐位相同** | 款 `7` ⛔ 成就 |

🔒 二批之單入倉**前** `git ls-files` 命中皆 `0`（⛔ 覆蓋既有檔）；判別力[必命中]（同目錄之前一單）皆 `1`。

---

## 項 `3`　本窗之 context 百分比

**KL 告知本窗 `63%`**（本收工令）。
🔒 併記（供新窗定位換窗點）：前二窗之換窗點分別為 **`86%`**（`W-G.9-322` 補令一）與 **`68%`**（`W-G.9-323` 交接註 項 `4`-`3`）。
⚠️ **倉內⛔ 載本數**；新窗之容量係全新，惟**換窗本身⛔ 使任何未入倉之物移轉**——項 `1`〜`7` 即其清單。

---

## 項 `4`　本窗**新遇**之環境坑（`322R0 §六-1` 項 `6`／`323 交接註` 項 `1`／`324R §零-0-3` **皆未載**者）

### `4`-`1`　🔴 **`shell=True` 空輸出之<u>真因更正</u>**（既載之歸因與實測不符）

🩸 **既載之歸因**（⛔ 重述其全文·引其錨）：`verify/probes/probe_WG9321_cc_recheck.py:51`–`:52`
與 `docs/reports/W-G.9-323_交接註_舊窗收工.md` 項 `1`-`3`——其述真因為
「`shell=True` 走 `cmd.exe`，`sed`／`sort`／`uniq`／`tail` **⛔ 可得** ⇒ 管線**靜默回空**」。

🔒 **本窗之實測（可復現·`subprocess.run(cmd, shell=True)`）**

| 具 | `rc` | `stdout` 首段 | 判 |
|---|---|---|---|
| `sha256sum --version` | `0` | `sha256sum (GNU coreutils) 8.32` | 🟢 **可得** |
| `sed --version` | `0` | `sed (GNU sed) 4.9` | 🟢 **可得** |
| `uniq --version` | `0` | `uniq (GNU coreutils) 8.32` | 🟢 **可得** |
| `tail --version` | `0` | `tail (GNU coreutils) 8.32` | 🟢 **可得** |
| `grep --version` | `0` | `grep (GNU grep) 3.0` | 🟢 **可得** |
| `wc --version` | `0` | `wc (GNU coreutils) 8.32` | 🟢 **可得** |
| `sort --version` | `0` | `sort (GNU coreutils) 8.32` | 🟢 **可得** |

⇒ **七具皆可得，且皆為 GNU 版**（⛔ `cmd.exe` 之同名異義具）。

🔒 **逐段拆解該管線（同一 `shell=True`）**

| # | 形 | `rc` | `stdout` bytes | 判 |
|---|---|---|---|---|
| — | **原式**（全管線·**單引號**） | `0` | **`0`** | 🔴 空 |
| `①` | 只 `git grep`（**單引號**·⛔ 管線） | **`1`** | **`0`** | 🔴 **零命中** |
| `②` | 只 `git grep`（**雙引號**·⛔ 管線） | `0` | **`126095`** | 🟢 |
| `③` | **全管線**（**雙引號**） | `0` | **`144`** | 🟢 **得正確之 `12` 列** |
| `④` | 四具之管線（⛔ `git`·純對照） | `0` | `16` | 🟢 |

🔑 **真因 ＝ `cmd.exe` <u>⛔ 以單引號為引號字元</u>**——實測 `echo 'x'` ⇒ `stdout` ＝ `"'x'\r\n"`
⇒ 原式之 `'W-G\.9-[0-9]{1,4}'` 與 `'*.md'` **帶字面單引號**傳入 `git grep`，
致其 regex 與 pathspec 皆**零命中**（`①` 之 `rc 1`），**⛔ 四支 POSIX 具不可得**（`③` 之全管線得正確輸出）。

🛑 **⛔ 追改該二處**（`append-only`／歷史記載·`常規四（九）五`）；本項係其**更正之載體**。
🔒 **其處置⛔ 受影響**：`W-G.9-325` 工項五之去 shell 化與所立之 `恆常附款 n④`，
其禁令之受詞為「**用 shell 管線或平台限定之外部命令**」**本身**，**⛔ 繫於該歸因**
⇒ 該修**仍為正**；受影響者僅**歸因之文字**。
⚠️ **對新窗之意義**：⛔ 以「本平台無 `sed`／`sort`」為由設計任何量測——**該前提為偽**；
真正之禁忌係 **`cmd.exe` 之引號語意**（單引號⛔ 為引號字元）與 `n④` 之明文。

### `4`-`2`　**反引號⛔ 能經本工具之命令列傳入**（`恆常附款 h` 之射程擴大）

| 形 | 實得 |
|---|---|
| `grep -c "自誤 \`462\`" <自誤簿>`（**雙引號 ＋ 跳脫**） | **`0`**·`rc 1` 🔴 |
| `grep -c '自誤 \`462\`' <自誤簿>`（**單引號**） | **`0`**·`rc 1` 🔴 |
| `grep -c -F "自誤 462" <自誤簿>`（**對照·無反引號**） | **`1`**·`rc 0` 🟢 |

⇒ **二種引號形皆失**（受詞確在該檔·`自誤 462` 之標題於 `:10930`）。
🩸 本窗於 `W-G.9-324` 工項三之標題形自檢首次踩之，改以**檔案管線**（`u5_titleform.py`／`t5_titleform.py`）後方得。
⚠️ 既載之同族僅及「**單引號內之 `\``＝ 字面反斜線**」之形；**本項證其⛔ 限於該形**。
🔒 ⇒ **`恆常附款 h`（含反引號之文字一律以檔案管線）⛔ 例外**。

### `4`-`3`　**Bash 工具之 `timeout` 參數可免於 `120` s 自動轉背景**

`323 交接註` 項 `1`-`5` 載「逾 `120` s，Bash 工具自動轉背景」。
🔒 **本窗實測**：以 `timeout` ＝ `600000`（ms）跑 `verify/probes/probe_WG9324_dprime.py`
（**驅動全管線 `4` 次**·`rc 0`·`stderr` `0` B）⇒ **於前景完成·⛔ 轉背景**。
⚠️ **其確切耗時本窗⛔ 計**（⛔ 臆造）；只知 `< 600` s。

### `4`-`4`　`__pycache__` 於 `.gitignore:18`

⇒ `python -m py_compile` **⛔ 汙染工作區**（本窗二批皆用之·`git status` 未見其產物）。

---

## 項 `5`　新窗辦「**動生產碼·跑 harness·二態逐宗量測**」之批時，本窗所知而倉內未載之陷阱

### `5`-`1`　🔴🔴 **主 checkout 之 `app.py` 與倉態相異，而其 `git status` 對之<u>全盲</u>**

🔒 **本窗實測（可復現·⛔ 動主 checkout 一字）**

| 量 | 值 |
|---|---|
| 主 checkout 之 `HEAD` | `1e5a87c554a1334ef44cfa1f85a80fe4bf3ed866`（`2026-09-07 23:04`·`W-G.9-248R …`） |
| 其分支 | `wip/s1-endpart`（**本地**） |
| **落後 `origin/wip/s1-endpart`** | **`509`** 筆（`git rev-list --left-right --count` ⇒ `0 509`） |
| `HEAD:app.py` ＝ index 之 blob | `98cc9ef339ca84e027db6d8f54bef4153f3fb748`（**`1452247`** B） |
| **工作區 `git hash-object app.py`** | **`af295ecee7add9562a9c498ee78b0e0f5837ba01`（`1476683` B）** |
| 二者之差 | **`24436`** B；`cmp` 逐位 ⇒ **相異**（`rc 1`） |
| `git status --porcelain -- app.py` | **空**（`update-index --refresh` 後仍空） |
| `git diff --stat HEAD -- app.py` | **空**·`rc 0` |

🔑 **真因（機械·⛔ 臆測）**：`git ls-files --debug app.py` 顯示 index 之 **stat 快取**記
`size = 1476683`／`mtime = 1788794403`，**與工作區之實際 stat 逐格相同**（`os.stat` 實查同值），
而其 **blob OID 仍為 `98cc9ef3…`** ⇒ `status`／`diff` 之快速路徑以 **stat 比對**為之、
**⛔ 讀檔內容** ⇒ 對該 `24436` B 之差**全盲**。
🔒 **排除之因**：`git ls-files -v app.py` ＝ `H`（**無** `assume-unchanged`／`skip-worktree`；
全倉該二旗標之計數皆 `0`）；`core.fsmonitor`／`core.untrackedCache` **皆未設**；
`core.autocrlf` 有效值 `false`；`git check-attr -a app.py` **無屬性**。

🛑 **對新窗之意義（⛔ 判其處置）**
1. **KL 於 localhost 實跑之 `app.py` ＝ `af295ece…`（`1476683` B）**，
   **⛔ 為 `origin/wip/s1-endpart` 之 `8466f749…`**（本窗二批之錨）。
2. 凡令 KL 以 UI 驗收生產碼之改動者，**須先令其對拍 `git hash-object app.py`**
   ——**⛔ 以 `git status`／`git diff` 為據**（二者於此檔恆綠）。
3. 🔒 **本窗⛔ 動主 checkout 一字**（逾本令之射程）；其處置**候發單側／KL**。

### `5`-`2`　worktree 之數與其不可見性

`.git/info/exclude` 之內容逐字 ＝ `.claude/worktrees/` ⇒ **`git status` 永不顯示之**（已載之同族 ＝ `CLAUDE.md` 之 `GB-35`）。
🔒 **現有 worktree ＝ `17`**（主 `1` ＋ 次 `16`），其 `HEAD` 散落 `4037570`〜`e7b80c5`；
本窗者 ＝ `.claude/worktrees/w-g-9-324-order-40b012`（分支 `claude/w-g-9-324-order-40b012`·
`left-right` vs `origin/wip/s1-endpart` ＝ **`0 0`**·`core.autocrlf` 有效值 `false`）。
⚠️ **同一 `.git` 為全部 worktree 共用**（`git rev-parse --git-common-dir` ＝ 主 checkout 之 `.git`）
⇒ `git stash`／本地分支／`refs` 皆共用；**⛔ 用裸 `git stash`**。

### `5`-`3`　harness 權限分類器

🔒 **本窗二次 `push` 皆首推即成**（`rc 0`）——與 `323 交接註` 項 `1`-`1` 同。
🛑 **⛔ 得據此推論「分類器已不擋」**：本窗之形為 `git push origin HEAD:refs/heads/wip/s1-endpart`
（**明示 refspec·自 `claude/…` 直推主線 ref**），而 `322R0 §六-1` 項 `6`-`1` 所載之被擋者為
**主線快轉**（理由逐字 `Merge Without Review`）⇒ **二者⛔ 同一操作**。
🩸 **本窗⛔ 遇**任何被擋。
⚠️ **照實·⛔ 合計為一數**：前窗之「三次首推即成」係**其自載**（`323 交接註` 項 `1`-`1`）、
本窗之「**二次**首推即成」係**本窗實測** ⇒ 二者之母體相異，**⛔ 相加**為一「累計」數。

### `5`-`4`　`run_all` 與長跑

🔒 **本窗⛔ 跑過 `verify/run_all.py`**（二單皆明令⛔ 跑）。
其耗時與副作用**已載** ＝ `CLAUDE.md`（`15`–`20` 分·**背景跑＋等通知**·`run_verification` **會寫 CSV**·
**兩支長跑不可併跑**）；記憶檔另載「`run_all` 會改寫 `3` 個已追蹤 log ⇒ 須於拋棄式 clone 內跑」。
🔒 **本窗之唯一長跑** ＝ `probe_WG9324_dprime.py`（驅動全管線 **`4`** 次·`rc 0`·`stderr` `0` B·見 `4`-`3`）。
🔒 **本窗⛔ 遇**任何 `verify/baselines` 之寫入需求。

---

## 項 `6`　器之相依（`323 交接註` 項 `4`-`4` 之類）有無新增

### `6`-`1`　🔴 **`probe_WG9324_dprime.py` 之相依為<u>四重</u>**（`323` 所載之二重之擴大）

| # | 相依 | 錨（`檔:列`） |
|---|---|---|
| `①` | `probe_WG9320_stageb` 之**介面**（`theo`／`real`／`build_vectors`／`ENV`／`CASES`／`NEED`） | `verify/probes/probe_WG9324_dprime.py:56` |
| `②` | `probe_WG9323_gprime` 之**介面**（`drive2`） | 同檔 `:57` |
| `③` | 🔴 **該二器之<u>源碼文字</u>**——`inspect.getsource` 施於 `GP.drive2` 與 `S.drive` | 同檔 `:268`／`:300`（其函式 `line_diff` 於 `:168`–`:169`） |
| `④` | **落檔** `verify/out/WG9323_gprime.md`（自證閘 `2` 之**逐字抽取**） | 同檔 `:63` |

🛑 **`③` 係新增之性質**：`323 交接註` 項 `4`-`4` 所載者為「`import` ＋ 落檔逐字抽取」；
本器另以 `inspect.getsource` **比對源碼文字之逐行差**（停機款 `4`）
⇒ **改 `stageb.drive` 或 `gprime.drive2` 之<u>任一空白</u>即翻轉該閘 ⇒ `rc=5`**
——**⛔「可 import」即足**。
⇒ 🔒 **動該四檔（二器 ＋ 二落檔）之任一一字，皆使 `probe_WG9324_dprime.py` 之 `rc` 轉 `2` 或 `5`**。

### `6`-`2`　🔴 `probe_WG9321_cc_recheck.py:51`–`:52` 之具名**已過期**

其所具名之「項 `10` 器紅（環境）」**已於 `686d88e` 修畢**（`W-G.9-325` 工項五）。
🛑 **⛔ 追改該檔**（歷史記載·`常規四（九）五`）；其**歸因**另由本文 `4`-`1` 更正。
⚠️ 新窗讀該二列時，**⛔ 誤為現況**。

### `6`-`3`　🔒 `content_tail` 之可單獨驅動（`686d88e` 新立）

`verify/probes/probe_WG9321_issuer_anchor.py` 之 `content_tail(repo, pathspec, k)` 為 **module 級函式**
⇒ 後批得以 `ast` 取其定義、於獨立命名空間 `exec` 之而**⛔ 執行全器**
（本窗即以此法作 `W-G.9-325` 工項五 `四` 之必破對照）。

---

## 項 `7`　其他**僅存於本窗記憶**之事項

| # | 事實 | 其對新窗之意義 |
|---|---|---|
| `1` | **scratchpad 之腳本**：`.py` **`35`** 支（`t*` **`16`**＝`-324` 批／`u*` **`17`**＝`-325` 批／`v*` **`2`**＝本令）＋ 非 `.py` **`92`** 支（payload・`commit` 訊息・落檔）＝ 總 **`127`** 檔·**皆⛔ 在倉樹內** | 新窗**須重建**；其形跨批可複製（`<x>0_gate`／`1_ingest`／`2_extract`／`3_append`／`4_recount`／`5_closegate`） |
| `2` | 🔴 **跨批沿用者之寫死期望**：`t5_append.py`（末端追加之三值機驗器·**二批皆用**）之 **`--zw` 分支寫死 `463`／`464`**（`:83`）與 **`465`**（`:88`–`:89`） | **新窗若沿用該 `--zw` 分支必印錯**；`-325` 批已改以 `u8_zwframe.py`（新號自 `argv` 傳入）代之。⇒ 其**基本分支**（無 `--zw`）⛔ 帶期望，**可安全沿用** |
| `3` | 本窗**⛔ 沿用**前窗之 `s4_kuan.py`／`s5_closegate.py`（`323 交接註` 項 `2`-`b` 所具名之二支）；改以 `t6_kuan.py`／`u9_kuan.py`（**期望值當場自單 regex 解析**·解析失敗即 `assert` **器紅**·⛔ 靜默）與 `t9_closegate.py`／`ud_closegate.py`（`BASE` **顯式傳入**·`baselines` 之 `sha256` 改 Python `hashlib`） | 該二形**已驗於二批**；新窗宜沿此形，⛔ 回用寫死期望之支 |
| `4` | **本窗之自捕 ＝ `4` 則**（**皆出艙前**·⛔ 事後覆核）：`-324` 二則（器之標籤懸引 `m⑥`〔倉內無該款〕／收工閘硬編「**七筆**」〔前批之 `commit` 數〕）；`-325` 二則（判準**嚴於單之字面**／定位式**多剝一列**） | **其逐則全文已入倉** ＝ `W-G.9-324R §五-2`・`§七-5`／`W-G.9-325R §四-3`（⛔ 重述）。🔑 **四則之共通形 ＝ 器之判欄含「筆數／款號／空之表現形」之字面而未當場實查** |
| `5` | **本窗⛔ 收到** `W-G.9-309` 或其後任何單之逐字；`W-G.9-309`（`GB-170` 修批）**仍保留·⛔ 取** | 次可取之號 ＝ **`W-G.9-326`**（`W-G.9-325R §七-2` 之期末錨已載其據·此處⛔ 重述其算式） |
| `6` | **本窗⛔ 動**主 checkout、⛔ 動 `verify/baselines`、⛔ 動 `app.py` 與 `verify/` 頂層 `*.py` 任一字；本窗所動之**唯一** `verify/` 檔 ＝ `verify/probes/probe_WG9321_issuer_anchor.py`（`686d88e`） | 生產碼 `34` 檔之 blob 於本窗**期初期末逐檔相同**（`W-G.9-324R §七-1` 閘 `2`／`W-G.9-325R §七-1` 閘 `2`） |
| `7` | **無**——除上列六項外，本窗⛔ 另有僅存於記憶而未入倉之事項。 | — |

---

🔒 **本節之七項逐項已填·⛔ 留白**（`恆常附款 d`：受詞及於倉外之物者，須同格載其不可實查之性質）。
🛑 **本文落地後，舊窗即得換窗。**
