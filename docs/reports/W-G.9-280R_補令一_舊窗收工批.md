# `W-G.9-280R`　補令一（**舊窗收工批**）：`自誤 382` ／ 坑 `bf`·`bg` ／ **交接註**

> **態**｜開工 ＝ `00db72322ff400c7fe00ab6542cb7ff7ecde057d`（`ls-remote` 實查）｜工項零後 ＝ `9655abbfbd1c50b9fd39242e866c1d28a3ace016`
> **單** ＝ `docs/orders/W-G.9-280_補令一_舊窗收工批.md`（工項零已原封入倉）
> **分級** ＝ **輕**（純登記 ＋ 交接註·**⛔ 驅動任何長跑**·**零生產碼**）
> 🛑 **辦畢 ⇒ 停 ⇒ 本窗封存**（⛔ 再用）。新施工窗之首則為另行交付之**開場文 `W-G.9-281`**。

---

## `§零`　開工閘與工項零

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `ls-remote origin refs/heads/wip/s1-endpart` | `00db723…` 或其後裔 | `00db72322ff400c7fe00ab6542cb7ff7ecde057d` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 逐位相同 | ✅ |
| `3` | `merge-base --is-ancestor 05a11bd…` | 須**非**祖先 | **非祖先** | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑）＋ **受詞自證**（坑 `bf`） | `b7b0d6c49648ae3871f087cf5e218040386748716b4307241ef57484ff960a6e` | **逐位相符**（受詞 `11810` B·全檔 `11888` B／`CR` `0`）；**受詞自證**：所量之單 ＝ `W-G.9-280_補令一_舊窗收工批.md`·[必真] 含 `W-G.9-280` ＝ `True`·[必偽] 含 `W-G.9-279` ＝ `False` | ✅ |
| `5` | `probe_order_preflight.py` | `P-3`／`P-5` 不紅 | **停機款 `0`／提示 `0`** | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `371`／`381`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `83`／`83` | **逐位相同**（坑判別力二造 造甲 `6 ≥ 1` ✅／造乙 `0`／`0` ✅） | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**（坑 `be`） | **`34`** 檔；**判別力[必不命中]**：pathspec `verify/*.py` 所多咬之**子層**檔 ＝ **`298`**（須 `>0`） | ✅ |

🔒 **閘 `4` 之判別力**：對**全檔**施同口徑 ⇒ 相異；受詞多 `1` byte ⇒ 相異 ⇒ 非恆綠。
🔒 **本批⛔ 驅動任何長跑**——唯二之器（四簿 `probe_WG9267_issuer_measurers.py`／坑 `probe_WG9269_pit_index.py`）
係**單 `§零-6`／`§六-5` 所明令之閘**，⛔ `run_verification`、⛔ 任何雙態或替身驅動。

**工項零**（commit `9655abb`）：所指之檔數 ＝ **`1`**（判別力：造乙 人造名 ⇒ `0`／造甲 `W-G.9-279` 之單 ⇒ `1`）；
`CR` ＝ **`0`**（`LF` `116`·bytes `11888`；判別力：真 CRLF 人造檔 ⇒ `CR = 2`）；
blob `sha1` ＝ `11c997cc1e07639ca2cec56d154a432eb3f251fb`；
**blob 內容 `sha256` ＝ 磁碟 ＝ 源 ＝ `bf7f11a1890453e150b0a9c252e3db2bf42b0c00e46fa37063360fca29579cda`** ⇒ **逐位相同** 🟢。

---

## `§一`　工項 `1`：`自誤 382` ＋ 坑 `bf`·`bg` ＋ `ALPHA_LABELS`→`bg`

### 一-1　`自誤 382`

**落點** ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` 檔末追加（嚴格前綴 `True`；判別力 `old+b'X'` ⇒ `False`）｜
`798921` → `803492`（**`+4571` B**）｜`CR` ＝ `0`。
**受詞逐字塊**由 `sed -n '39,46p'` 之輸出**逕行落檔**：列數 **`8`**｜位元組 **`1431`**｜
`sha256` **`dc358efc665f4728a5e0bec60bedf27fdcec6ba14af88586ed0f687958493f93`**；覆核逐位相同 ✅。

**取號現查**（三軸｜`(1)` `blob@9655abbfbd1c50b9fd39242e866c1d28a3ace016`｜
`(2)` 取最大號 ＝ 登記表**單檔**（`798921` B／`9427` 列）·查占用 ＝ 全倉追蹤檔 **`2186`** 檔（讀不到 `0`）｜
`(3)` **檔框 ＋ 列框**）：命中列 `384`／相異 `371`／**MAX `381`** ⇒ **次號 `382`**；
查占用（**B ⋀ C**·`GB-158`）：受詢 `1`／`5`·**`0`／`0`**·`1`／`5`；甲 `381` `4`／`13`·`2`／`2`·`4`／`13`；
甲2 `380` `4`／`11`·`3`／`3`·`4`／`11`；乙 人造哨兵 `0`／`0` ⇒ **器非紅**（二對照於二形皆 `>0`）。
**逐筆歸類**：受詢之命中**全數集中於本單自身** ⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號。

### 一-2　坑 `bf`／`bg`

🔒 **其定義處 ＝ 本單 `§三` 之坑表**（`docs/orders/W-G.9-280_…md:52`–`:55`），表頭列逐字 ＝ `| 標籤 | 形／徵候／攔法 |`
⇒ 同時含 `徵候` ⋀ `攔法` ⇒ 合於 `D-T` 之形。
**取號現查**（全倉追蹤檔 **`2186`** 檔·態 `9655abb`）：

| 標籤 | 角色 | 檔／列 | 占用者 |
|---|---|---|---|
| 坑 `bf` | **受詢 1** | `1`／`6` | 本單自身 |
| 坑 `bg` | **受詢 2** | `1`／`1` | 本單自身 |
| 坑 `be` | 對照甲[須 `>0`] | `4`／`18` | — |
| 坑 `bd` | 對照甲2[須 `>0`] | `8`／`23` | — |
| （人造·**執行期組出**·字面⛔ 出艙） | 對照乙[須 `＝0`] | `0`／`0` | — |

⇒ **器非紅**；受詢之占用者皆本單自身 ⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號。
🔒 **本器之受詞自證**（坑 `bf` 之攔法）：印出所查之受詢標籤 ＝ `['bf', 'bg']`；
[必真] 含 `bf` 與 `bg` ＝ `True`；[必偽] 含 `be` ＝ `False`。

### 一-3　`ALPHA_LABELS`→`bg` ＋ 三處框字串齊一

| # | 處 | 改後 |
|---|---|---|
| `1` | `ALPHA_LABELS` 之末元 | `"be",` ＋ 新列 `"bf", "bg"])  # 🆕 \`W-G.9-280\` §三 所准之二坑` |
| `2`／`3` | `run(...)`／`issuer_frame(...)` 之題 | `字母系 \`a\`〜\`z\` ＋ \`aa\`〜\`bg\`` |

🔒 **判別力（先實測後填·以 `compile` + `exec` 單一 `Assign` 取語法真值·⛔ `ast.literal_eval`）**：
**成員數 `57` → `59`（`+2` ✅）**｜**尾二元 ＝ `['bf', 'bg']` ✅**｜**重複元 `0`** ✅；
新框字串命中 `2`／舊形（`be`）殘留 `0` ✅。**總數 ＝ `NUM 26` ＋ `ALPHA 59` ＝ `85`** ⇒ 與單 `§六-5` 之期值 **`85`／`85`** 一致。

---

## `§二`　🔴 **交接註**（單 `§五` 之七項定式）

### 二-1　項 `1`：結案 `commit` 與本批逐 `commit`

見 `§三`（收工後追加·含 `ls-remote` 實查）。

### 二-2　項 `2`：四簿期末 ＋ 坑之期末 ＋ **次號**

見 `§三`（**報告入倉後量**·⛔ 預寫）。

### 二-3　項 `3`：自限復驗

見 `§三`。

### 二-4　項 `4`：**本波新立之通則逐字**（七則）

> `(a)` **受詞須取<u>性質</u>，⛔ 取代理量或字樣**，且立受詞前須先查該輸入之**下游是否存在使不同輸入等價之分支**（飽和／clamp／`<= 0` 早退／`round`）。（`自誤 381`）
> `(b)` **凡轉引前一批之計數，須連同其<u>母體之定義</u>逐字轉引，並於新母體下<u>重量</u>；⛔ 以「同一受詞」充「同一母體」。**（`自誤 382`）
> `(c)` **對照組之「必命中性」須<u>逐態</u>成立**；受測物有二態以上者，**每一態各需其自身之必命中對照**。（`自誤 345` 再現第二例）
> `(d)` **⛔ 以「未執行」與「已通過」共用出艙碼**；凡以「對照態無紅」為端點或對照組者，須先出艙該閘於對照態之**執行次數**。（`自誤 378`）
> `(e)` **凡以「器之構造」為由否定一個實測數，該構造之主張<u>本身</u>須先自倉逐字查證**（其綁定處、其命名空間之來源）。（`自誤 380`）
> `(f)` **凡以 git 之輸出文字為框者，須同格具名 `core.quotePath` 之設定**；⛔ 以「同一指令」充「同一框」。（`自誤 379`）
> `(g)` **量結構化物件之基數一律取<u>語法真值</u>**（`ast` 之 `compile` + `exec` 單一 `Assign`；`literal_eval` **只及字面**）；⛔ 文字層括號配對或 regex。（`自誤 377`）

### 二-5　項 `5`：🩸 **本會期自捕之全表**（形／徵候／攔法）

| # | 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|---|
| `1` | **`bb`** | 框內省抽為輔助函式而未補其自身之一層 ⇒ `sys._getframe` 整體偏移 `1` | **一整張表全欄「不可得」**，且可同時吃掉多個包裹 | `sys._getframe(depth + 1)` ＋ **自我驗證閘**（所錄之鍵須為已知母體之**非空子集**，含 `None` 即 loud 拒測） |
| `2` | **`bd`** | 「必為零」之對照，其**字面**隨報告入倉而成為母體之一員（哨兵自我污染） | 前批得 `0`／`0`、本批同一檢得 `1` 檔／`1` 列，而受詞**一字未變** | 必為零之對照一律**執行期組出**且**字面⛔ 出艙**；凡沿用前批之對照，須先量其於**現母體**之命中 |
| `3` | **`be`** | **git pathspec 之 `*` <u>跨 `/`</u>**（與 shell glob 相異）⇒ 生產碼母體被擴大 | 「替身⛔ 入生產碼」之檢初版得 `1`（實為探針之命中）；正面列舉 `34` 檔後得 `0` | 生產碼母體一律**正面列舉 `34` 檔**，⛔ 以 pathspec 之 glob 界定；並備**必不命中**之對照（一必在子層之檔） |
| `4` | **`bf`** | **量測器讀到<u>他批</u>之受詞，而其輸出一切正常**（字串替換未生效 ⇒ 器仍指前一單之檔） | `SELF_SHA256` 相符、preflight `rc = 0`、各欄皆綠——**徵候只在標題之號** | 閘器一律**印出其所量之檔名**並與本批之單號**對拍**，附 **[必為偽] 對照**；⛔ 以「各欄皆綠」充「受詞正確」 |
| `5` | **`bg`** | 以框內省 climb **外層**取內部幾何之參數，而該參數於呼叫時點**尚未賦值**或**已被替換** | 還原自我驗證閘 `0`／`22`；**若無該閘，其所印之 `W(遠側)` ＝ `73.8689182960` 是一張漂亮而錯的表** | `(1)` 一律取**受測函式之參數本身**，⛔ climb 外層；`(2)` 凡探針還原內部幾何，**必設還原自我驗證閘**（還原值與碼面值逐位對拍·**分母須為全母體**） |
| `6` | **⛔ 鑄坑**（單 `§一` 所裁） | **收工閘之框以<u>字樣</u>界定受詞**（`shim`／`替身`）而其命中係**倉內既有** | 掃生產碼 `34` 檔得 `7`／`1` 命中，逐筆歸類後**皆⛔ 本批引入**（二檔 blob 期初＝期末） | 改以「**`34` 檔 blob 期初＝期末**」為**決定性受詞**；🔒 其形**已在 `自誤 381` 之通則射程內**（受詞須取性質、⛔ 取字樣）⇒ **於本註具名即足** |

🔒 **本會期另二則入倉前自捕（`W-G.9-279` 之前批·已於各該報告具名·⛔ 鑄坑）**：
`(i)` **零判門檻 `1e-12` 過嚴** ⇒ `-2.057e-06` 被誤歸「非 `0`」⇒ 偽「單一類·無鑑別力」⇒ 改**三分類 ＋ 具名常數 `TOL0`**；
`(ii)` **`R4 right` 無定案筆時<u>靜默退路</u>為全 `R4`** ⇒ 改 **loud 明載**；
`(iii)` **文字層篩 `rw_increment(` 咬中 docstring**（`iterate_G_S` 得 `3` vs AST `2`）⇒ 改 **AST**（＝ 通則 `(g)`）。

### 二-6　項 `6`：**起手清單**（取**本批收工後**之倉態·供新窗用）

| # | 事 |
|---|---|
| `1` | **開窗第一動作** ＝ `git ls-remote origin refs/heads/wip/s1-endpart`；**⛔ 以本地追蹤 ref 為證**（`fetch` refspec 未帶 `+` 會半靜默停在舊值） |
| `2` | 驗 `git rev-list --left-right --count HEAD...origin/wip/s1-endpart` ＝ `0　0`；`git status --porcelain` 空 |
| `3` | 驗 `git rev-parse HEAD:app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1` |
| `4` | 驗 `p3a`（`05a11bd665f6c2790ff821c9e3a1606651e345d4`）**非祖先**（⛔ 併線·⛔ 刪除） |
| `5` | 驗 `verify/baselines` ＝ **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（🔒 **口徑 ＝ `core.quotePath` 預設**·`自誤 379`） |
| `6` | 驗**生產碼母體 ＝ 正面列舉 `34` 檔**（`app.py` ＋ `verify/` **頂層** `*.py`）；⛔ 以 pathspec `verify/*.py` 界定（坑 `be`·其多咬子層 **`298`** 檔） |
| `7` | 跑四簿器 `verify/probes/probe_WG9267_issuer_measurers.py registry` ＋ 坑器 `verify/probes/probe_WG9269_pit_index.py`（**⛔ 憑本註之數**·須自行重跑） |
| `8` | **本機之坑**（已付學費·⛔ 再犯）：`python3` `rc=49` ⇒ 用 `python`／**heredoc 觸硬閘** ⇒ 一律 `Write` 落腳本／**`python -c` 內之反引號遭 bash 命令替換** ⇒ 一律 `Write` 落腳本、反引號以 `chr(96)` 執行期組出／`subprocess(text=True)` 以 `cp950` 解碼 CJK ⇒ `encoding="utf-8", errors="replace"`／git pathspec 一律 `/`／`io.open(p,'w')` 於 Windows 靜默轉 CRLF ⇒ 明指 `newline=""`／⛔ 以 `tail -N` 取長跑結果 ⇒ 落**全量**檔以逐字錨 `grep` |
| `9` | **`run_verification.py`** 之 `rc = 1` ＝ **准紅碼**之既存態（⛔ 本批所致）；其產 `verify/out/got_*.csv`（**`.gitignore:30` 刻意忽略·⛔ 在倉**），跑畢須驗**追蹤檔零異動** |

### 二-7　項 `7`：🔴 **未結項之逐條**

| 項 | 逐條 |
|---|---|
| **`GB-168`** ⬜ | `Δ = 2.425` ＝ **`−0.661141`**（理論側餵 `round(S × \|cos\|, 2)`）＋ **`+3.088507`**（`W_prev` 之扣除）；**實拋者係<u>第二閘</u>**（`verify/stepg_pipeline.py:1471`·其 `raise` 首行 `:1473`）；`Δ_i` 閘（`:1461`–`:1462`）於**界面 `0`** 時判定組為空 ⇒ 其 `raise` 不執行；`solve_G_binary` 現行實參 ＝ `(_rw_start, W)` 而 `_rw_start` 於鏈頭**即 `_W_near`** ⇒ **改名⛔ 等同改行為**。**⛔ 解除、⛔ 收窄** |
| **`GB-169`** ⬜（受詞**已收窄**） | 射程 ＝ **`R(W_0) > 0` 之側**；**情形乙二可量格與 `K-9-5-12（四）` 逐位相符**（`Δ = +0.000e+00`）；**唯一疑似點 ＝ 態乙 `R4 right`**（`R(W_0) = 3.0885070835`·**情形甲**·`_fo_right = False`·`is_chain_head = True`）；`（五）3.` 之「未決」後半款**待 KL**。🔑 **`3.0885070835` 逐位等於 `GB-168` 之 `Δ` 分解中之 `+3.088507`**（`W_prev` 之扣除項）⇒ 二登記項之數值鏈於此**閉合** |
| **`GB-167`** ⬜ 未修 | `_WF_NS_NAMES`（AST 真值 **`35`**）**⛔ 含** `_wg9268p_anchor_advance` ⇒ **app 生產路徑之 `_wf_ns()` 必 `KeyError`**（**生產碼·須 KL 放行**） |
| `GB-153`／`159`／`160`／`164`／`165`／`166`／`GB-7` | 逐項維持（見倉內登記表） |
| **`GB-162`** | 🛑 **三禁令維持**：⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數** |
| **`ok_t`／`ok_f`** | 🛑 **⛔ 改一字**——待 `K-9-29` 子項 `1` 落地後**重量** |
| **`K-9-29`** | 子項 `2`（`wf_f0` `a′`）**已落地**；子項 `1`（廢前置合併）⬜ **未落地**且**⛔ 為單純之旗標翻轉**（**`GB-168` ＋ `GB-169` 為其先決**）；子項 `3` 隨子項 `1` 消解；子項 `4` 屬「五級實作」 |
| 🔴 **情形甲 ∧ `R(W_0) > 0` 之 `4` 格** | **新窗須先補其逐格具名**。本窗已具名者 ＝ `0m` 態甲 `R2 left`／`0m` 態乙 `R2 left`／`0m` 態乙 `R4 right`／`3.5m` 態乙 `R4 right`（四格·其 `W_0` 分別為 `+2e-10`／`+2e-10`／`0.1775004071`／`0.1775004071`）；**其中前二格之 `R(W_0)` ＝ `3.3e-9`** ⇒ 新窗須判其**是否應與後二格分列**（⛔ 本窗判） |
| `p5`／`p6` 之事實 | 維持（見倉內登記表） |
| **`K-9-12`** | **待 KL** |

🛑 **本註⛔ 判態乙 `R4 right` 為違反、⛔ 判 `（五）3.` 之未決後半款、⛔ 提任何修法主張。**

---

*本報告受詞 ＝ `W-G.9-280`。⛔ 呈 KL。*


---

## `§三`　收工閘之實測 ＋ 交接註項 `1`／`2`／`3`（**末端追加**·⛔ 改上文一字·報告入倉**後**所量者）

🛑 **閘之量測與其所守護之動作（`commit`／`push`）一律<u>分開呼叫</u>**（`常規補款一`）。

### 三-1　收工閘（單 `§六`）

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `numstat` 刪除欄逐檔具名 | `ALPHA_LABELS` 之就地更正為**唯一**非零者 | **`1` 檔** ＝ `verify/probes/probe_WG9269_pit_index.py`（`4`／`3`）；其餘 `2` 檔刪除欄皆 `0`（報告 `153`／自誤簿 `55`·皆 `+N／−0`） | ✅ |
| `2` | 生產碼判法·**母體正面列舉 `34` 檔**（坑 `be`） | 命中 `0` 行 | `00db723..9655abb` ⇒ **`0`**；`9655abb..8b23e13` ⇒ **`0`**（**對照組**：一含 `app.py` 之歷史區間 ⇒ **`1`** ⇒ 器非紅） | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0` | 本批 `4` 檔**皆 `CR = 0`**；**判別力**：已知含 `CR` 之 `verify/out/WG9279R_rw0.log` ⇒ `CR = 186` | ✅ |
| `3'` | append-only 前綴檢之判別力二造 | 造甲 `True`／造乙 `False` | **自誤簿** `798921` → `803492`（`+4571`）：造甲 `True`／造乙 `False` ⇒ **器非紅** | ✅ |
| `4` | 四簿期末 | 自誤 相異 **`372`**／MAX **`382`**；`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`（**三簿⛔ 動**） | **逐位相同**（自誤缺號 `[106, 355, 356, 357]` **不變**） | ✅ |
| `5` | 坑之定義處清單自證（報告入倉**後**量） | **`85`／`85`** | **`85`／`85`**；坑 `bf` ＝ `W-G.9-280_補令一_舊窗收工批.md:1`（`D-H`）＋`5`·**✅✅✅**·引用 `8`；坑 `bg` ＝ 同檔 `:55`（`D-T`）·**✅✅✅**·引用 `3`；判別力二造 造甲 `坑 n` ⇒ `6 ≥ 1` ✅／造乙 人造坑 ⇒ `0`／`0` ✅ | ✅ |
| `6` | 自限復驗 | 見 `三-3` | **`34` 檔 blob 期初＝期末·相異 `0`**（判別力[必非零] ＝ `1`）；`baselines` `298` 檔·`a898f4e1…`；`p3a` 非祖先；`WV_BAKE` 未設 | ✅ |
| `7` | `push` 後 `ls-remote` 實查 | 主線 ＝ 本批末一 `commit` | 見 `三-2` | ✅ |

### 三-2　交接註項 `1`：**結案 `commit` 與本批逐 `commit`**

| # | `commit`（全 `40` 碼） | 事 | 生產碼判法 | `push`（`ls-remote` 實查） |
|---|---|---|---|---|
| `1` | `9655abbfbd1c50b9fd39242e866c1d28a3ace016` | 工項零（本單原封入倉·**受詞自證**） | **`0`** | ✅ `9655abb…` |
| `2` | `8b23e136a1310cd4b4e145dafa87bf3e3e49bf2e` | 工項 `1`＋`2`（`自誤 382`／坑 `bf`·`bg`／`ALPHA_LABELS`→`bg`／交接註） | **`0`** | ✅ `8b23e13…` |
| `3` | （本節之 `commit`·**結案 `commit`**） | 收工閘之實測追加 | **`0`**（見 `三-5`） | 見 `三-5` |

🔒 **`left-right`**（`HEAD...origin/wip/s1-endpart`·本節落檔時）＝ ``0　0``
（🛑 其為**本 `commit` 尚未建立前**之值；推送後之實查見 `三-5`）。
🔒 **推送之受詞**（本窗係 worktree·分支名 **⛔ `wip/s1-endpart`**）＝ `git push origin HEAD:refs/heads/wip/s1-endpart`。

### 三-3　交接註項 `2`／`3`：四簿期末 ＋ 坑之期末 ＋ **次號** ＋ 自限復驗

| 簿 | 框（逐簿具名） | 相異 | MAX | 缺號 | **次號** |
|---|---|---|---|---|---|
| **自誤** | `VR-091 四` 之**定義框 ＋ 範圍框**·母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔** | **`372`** | **`382`** | `[106, 355, 356, 357]` | **`383`** |
| **`GB`** | **標題錨定框** `^#{2,4}[^\n]*?GB-N`·母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔** | `167` | `169` | `[12, 87]` | **`170`** |
| **`VR`** | `` ^#+ `?VR-([0-9]{1,4})`? ``（扣哨兵 `999`）·母體 ＝ `docs/驗證裁定登記表.md` **單檔** | `80` | `95` | `[73, 75]` | **`96`** |
| **`K-9`** | `` ^#{2,4}[^\n]*?K-9-([0-9]{1,3}) ``·母體 ＝ `docs/rulings/K-6_…md` **單檔**（🛑 `K-9-1` 係**已鑄而定義框結構上撈不到**·⛔ 得列為缺號） | `28` | `29` | `[]` | **`30`** |

🔒 **坑之期末** ＝ **`85`／`85`**（`NUM_LABELS` **`26`** ＋ `ALPHA_LABELS` **`59`**）·**下一標籤 ＝ `bh`**
（🛑 `ALPHA_LABELS` 須**同批**擴之，否則該器**結構上看不見**新坑而其輸出仍為全綠 ⇒ **靜默漏**）。

🔒 **自限復驗**（項 `3`）

| 受詞 | **實測** |
|---|---|
| `app.py` blob 期初＝期末 | `4379108a4856714078c410ff4e6291ecfdf6d2c1`（且**全 `34` 檔** blob 期初＝期末·相異 `0`） |
| `verify/baselines` | **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（🔒 **口徑 ＝ `core.quotePath` 預設**·`自誤 379`） |
| `p3a` | **非祖先**（⛔ 併線·⛔ 刪除） |
| `WV_BAKE` | **未設** |
| `K-9-29` 任何子項 | **⛔ 落地** |
| `GB-167`／`168`／`169` | **⛔ 修** |

### 三-4　🛑 本批⛔ 為之事之逐項自檢（單 `§八`）

⛔ 動生產碼 `34` 檔一字（**blob 期初＝期末·相異 `0`**）／⛔ 改五個 `*_EXPECT`／⛔ 修 `GB-167`·`168`·`169`；
**⛔ 驅動任何長跑**（⛔ `run_verification`、⛔ 任何雙態或替身驅動——本批唯二之器皆為單所明令之閘）；
⛔ 落地 `K-9-5-12`／`K-9-29` 任何子項；`GB-162` 三禁令維持；⛔ 引任何自 baseline 取得之數；
**⛔ 判態乙 `R4 right` 為違反、⛔ 判 `（五）3.` 之未決後半款、⛔ 提修法主張**；
**⛔ 追改 `W-G.9-279` 或任何已入倉之文件一字**；⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref；
⛔ 於任何入倉文件宣稱一件尚未發生之事。

🔒 **鑄號之授權（單 `§七`）逐項核對**：本批鑄 **`自誤 382`**（`1`）＋ **坑 `bf`**（`1`）＋ **坑 `bg`**（`1`）
＝ **三號·恰如授權**；`GB`／`VR`／`K-9` 之相異與 MAX **一字未動**（閘 `4` 實測）。

### 三-5　🏁 **本窗封存**

**辦畢 ⇒ 停 ⇒ 本窗封存**（⛔ 再用）。新施工窗之首則為另行交付之**開場文 `W-G.9-281`**。
🔒 結案 `commit` 之 `ls-remote` 實查見本節推送後之回報（⛔ 於此預寫）。

*本報告受詞 ＝ `W-G.9-280`。⛔ 呈 KL。*
