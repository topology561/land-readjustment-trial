# `W-G.9-269R`　本體階　**新施工窗**（開工閘 ／ 逐坑覆述 ／ `c2` 三前置之備妥 ／ 🛑 二項上呈）

> **落點 ＝ `c1` 側支**（單之界：併線令未下達前⛔ 推主線；零生產碼之 commit 於側支待併期間亦落側支）。
> **基座** ＝ `7fb9c2bd2d7b6c73cff9ee49706261cf933df322`（`ls-remote` 實查）；
> 工項零之 commit ＝ `4dc35658f92f6a81191f8bcb423485728f9ac42e`（已推·主線⛔ 前進）。
> **單** ＝ `docs/orders/W-G.9-269_本體階新施工窗_開工令.md`（原封入倉·見 `§零`）。
>
> 🛒 **本段⛔ 載**：`c2` 之任何修法主張、任何併線動作、任何旗標預設之變更、
> 任何自 `verify/baselines` 取得之數（`GB-162`）、受影響三宗之配地圖或負擔表（`GB-159`）。

---

## §零　工項零 ＋ `S-0b`

### 零-1　原封入倉（`作業常規之追加四` 款 `一`）

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 落檔 | `475aa19d172dfd0bb55833d339bc546389dbb8be5122c598d64bc4b44c6a4393` | `4407` | `0` | `57` |
| **blob**（`git cat-file blob <commit>:<path> \| sha256sum`） | **同** | `4407` | — | — |

🛑 **射程（`追加四` 之界·照實·⛔ 頂替）**：本鏈所證者為「**落檔 → 入倉**」之保真；
來源係**聊天訊息、無獨立 `sha256` 可比** ⇒ **⛔ 證「發單側送出之原文 → 落檔」之保真**。

🔒 **檔名之取**（`常規三`·無土地後果·CC 提案 ＋ 具名其由·**候發單側核可**）
取 `W-G.9-269_本體階新施工窗_開工令.md`；候選二 `…_補令八_…` **⛔ 取**
——本單自身逐字引「補令八 `§四`」為**既有文件** ⇒ 該號係**意指占用**（`常規四（七）二`），
⛔ 得由本單占之；候選三 `…_開工令_本體階新施工窗.md` 僅語序之別。

### 零-2　`S-0b`（`probe_order_preflight`·`rc` **直取** ＝ `0`·`stderr` `0` B）

🔴 機械停機款 **`0`** 項 ✅；🟡 **`1`** 項**逐項處置**（⛔ 靜默略過）：

| 項 | 處置 |
|---|---|
| `P-5` 本單⛔ 載 `SELF_SHA256` | **具名豁免**（三端 `sha256` 鏈承擔·體例同補令六段／補令七段） |

ℹ️ `P-3` 記錄：`app.py` 之 `def main` 區間（AST 實查）＝ `14917`-`24497`
——本單之主閘受詞**⛔ 落於其內**。
🔒 **器之定位前置**（`GB-161`·`坑 10`／`11`）：`probe_order_preflight.py` 之 `REPO`
逐字 ＝ `os.path.dirname(VERIFY)`（自 `__file__` 推得·⛔ 硬編）；
三造 ＝ 丙（必命中之本類已知成員 `probe_WG9268p_c1_synth.py`）**`2`** ⋀
哨兵（本器內 `worktrees` 字樣）**`0`** ⇒ **器非紅**。

---

## §一　起手清單（`W-G.9-269R_本體階_補令六段.md` `§乙`·**逐項當場實查**·⛔ 引其表之值）

### 一-1　項 `1`：三 `ref`（`git ls-remote` 實查·⛔ 本地追蹤 ref·`坑 23`）

| `ref` | 期初實查值 | 與單所載之對拍 |
|---|---|---|
| `wip/s1-endpart` | `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2` | ✅ ＝ 單之 `0aa608e…` |
| `verify/W-G.9-269-c1` | `7fb9c2bd2d7b6c73cff9ee49706261cf933df322` | ✅ **係 `c8c1c9f` 之後裔**（`merge-base` 實查·單只載其為後裔） |
| `verify/W-G.9-269-p3a` | `05a11bd665f6c2790ff821c9e3a1606651e345d4` | ✅ ＝ 單之 `05a11bd…`（🛑 凍存·本窗⛔ 觸之） |

**期末**（工項零推畢）：`c1` ＝ `4dc35658f92f6a81191f8bcb423485728f9ac42e`；
主線 **`0aa608e…` 一字未動** ✅；`p3a` **`05a11bd…` 一字未動** ✅。

### 一-2　項 `2`：`left-right`

`git rev-list --left-right --count HEAD...0aa608ed…` ⇒ **`0	0`** ✅
（本窗之 worktree 恰在主線 `tip`·⛔ `main` 態——`harness-worktree-crlf-trap` 之六度重踩於本窗**未再現**）。
**併驗**：`core.autocrlf` 分層 ＝ system `true`／**local `false`**（有效值 **`false`**）；
`CLAUDE.md` 工作區 `236365` B ＝ blob `236365` B·`CR 0` ⇒ **工作區非 smudge 態** ✅。
**併驗**（`harness-worktree-crlf-trap` 更正二之末段）：`.claude/settings.json` **存在**（`267` B）
⇒ heredoc 硬閘於本 session **確已生效**——其**活體攔截**於本窗**實發一次**（見 `§四` 自解 `3`）。

### 一-3　項 `3`：四簿 MAX 及其框（倉內既有器 `verify/probes/wg9268_gate5_tally.py`·⛔ 自擬框）

🔒 **框** ＝ **標題形 ∪ 表列形**（`常規四（八）三`）；母體 ＝ **各該登記表單檔**；粒度框 ＝ **列框**。
🛑 **取號須二 `ref`（本窗取三 `ref`）並查**（`坑 s`）——單方 `rev` 之現查⛔ 足以判「未占用」。

| 簿 | 主線 `0aa608e` | `c1` `7fb9c2b` | 凍存 `05a11bd` | **三 `ref` 聯集之次號** |
|---|---|---|---|---|
| **自誤** | 相異 `348`／MAX **`358`**／缺 `[106, 355, 356, 357]` | 相異 `351`／MAX **`361`**／缺 `[106, 355, 356, 357]` | 相異 `350`／MAX **`357`**／缺 `[106]` | **`362`** |
| **`GB`** | 相異 `160`／MAX **`162`**／缺 `[12, 87]` | 同左 | 相異 `159`／MAX **`161`**／缺 `[12, 87]` | **`163`** |
| **`VR`** | 相異 `80`／MAX **`95`**／缺 `[73, 75]` | 同左 | 同左 | **`96`** |
| **`K-9`** | 相異 `28`／MAX **`29`**／缺 `[]` | 同左 | 同左 | **`30`** |

🔒 **交接註 `§丙` 之「⛔ 假定」已當場坐實**：主線自誤 MAX ＝ **`358`** vs `c1` ＝ **`361`**
⇒ **併線前主線與 `c1` 之簿態確相異**。
🔒 **本窗⛔ 鑄任何號**（單未授權鑄號）⇒ 上列次號係**現查之出艙**、⛔ 取號之行使。

### 一-4　項 `4`：生產碼 `34` 檔 blob（**正面列舉**·⛔ 全倉排除式）

框（逐字）＝ `^(app\.py|verify/[^/]+\.py)$`；主線 **`34`** 檔／`c1` **`34`** 檔·**檔名集合逐位相同**。
**blob 相異 ＝ 恰 `2`** ✅：

| 檔 | 主線 | `c1` |
|---|---|---|
| `app.py` | `1d7d7c7324719f13e7d61473a55001170ef44d8e` | `c5c28b826aa25f4f12a3bd5bdc1fcd0587ab021f` |
| `verify/stepg_pipeline.py` | `13e74a9f4c3faa9507fe7389c0fd86596960ecf0` | `ee5d82b2138cb2599809d96d5006db3788675794` |

### 一-5　項 `5`：`c1` 二檔之 blob **須仍為 `8f3dccc` 之內容**（單 `§五` 之主檢）

| 檔 | `@8f3dccc` | `@c1 tip 7fb9c2b` | 判 |
|---|---|---|---|
| `app.py` | `c5c28b826aa25f4f…` | `c5c28b826aa25f4f…` | ✅ **逐位相同** |
| `verify/stepg_pipeline.py` | `ee5d82b2138cb259…` | `ee5d82b2138cb259…` | ✅ **逐位相同** |

**工項零之 commit 後復驗**：`4dc3565` vs `7fb9c2b` 之生產碼 `34` 檔相異 ＝ **`0`** ✅
⇒ 本窗之入倉**⛔ 動 `c1` 之逐位驗收**。

### 一-6　項 `6`：`P-1` 基準器之 blob

`verify/probes/probe_WG9269_coverage_single.py` ＝ `07d8627e5e4c797f1278999e40f306acce8c01f6`
（`c1`）＝ **同** `05a11bd`（凍存側支）⇒ 逐位相同 ✅；**主線⛔ 存在該檔**（`fatal: path … does not exist`）
——合 `坑 x`（補令四／五及補令三〜五段報告僅存於凍存側支）之同族。

### 一-7　項 `7`：四 `GB` 之現況（`docs/reports/W-G.4_泛用阻塞項登記表.md`·標題形）

| `GB` | 錨（標題形之列） | 現況（本窗實讀·⛔ 憑記憶） |
|---|---|---|
| `GB-159` | `### \`GB-159\` 🆕　W-G.9-268′ 併線後之**中間態配地表**⛔ 對外` | **仍有效**；🔴 其受詞**今為三宗**（`628-42(1)`／`628-53(2)`／**`628-27(1)`**·`坑 v`）。解除條件三者皆備方解，其 `(1)` ＝ `W-G.9-269` 落地併線 |
| `GB-160` | `### \`GB-160\` 🆕　\`V-1′\` 之器於**某類側無從建框**（閘 `γ` 不過）` | **閘 `γ` 待重量·後置**（`3.5m·R5·left` `3/5`／`R6·right` `3/4`）；🛑 **⛔ 調框以使其過**；其重量俟退化之宗依 `K-9-11 三` 移出配地母體**後** |
| `GB-161` | `### \`GB-161\` 🆕　倉內探針／工具**硬編絕對路徑指向他窗 worktree**` | 母體逐字定稿；真硬編 `4` 支已修，餘**免修·具名**。**本窗之施行見 `§二` 坑 `10`／`11`** |
| **`GB-162`** | `### \`GB-162\` 🆕　\`verify/baselines\` 與現行輸出差 \`1137\` 格…` | 🔴 **生效**：⛔ 執行 `WV_BAKE=BASELINES`／⛔ 覆寫 `verify/baselines` 任一檔／⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數**。**本窗⛔ 觸之一位元組**（見 `§五` 自限復驗） |

### 一-8　項 `8`：🔴 旗標之預設**須仍為 `off`**

`c1` 態 `app.py:9279` 逐字 `K917_BACKFILL_ENV = "WG9269_K917_BACKFILL"`；
`:9285` `def k917_backfill_enabled()` 之回傳式逐字：

> `return str(_os_k917.environ.get(K917_BACKFILL_ENV, "0")).strip().lower() \`
> `    in ("1", "on", "true", "yes")`

⇒ 預設 **`"0"` ＝ `off`** ✅。其 docstring 逐字載「其**預設之翻轉**係 `c2`（🔴 有土地後果·須 KL 單獨放行）」。
🛑 **本窗⛔ 翻之**；本窗之二次實跑皆於 `WG9269_K917_BACKFILL` **未設**之環境下行之（已逐跑出艙該事實）。

---

## §二　逐坑覆述（單之明令：**⛔ 只讀坑之標題**·`自誤 347`：⛔ 以「已讀過」代替「已執行其攔法」）

> 🔒 母體 ＝ **坑 `1`〜`26`**（`W-G.9-268p_結案報告.md` `§五` 所索引之四份報告）
> **＋ 坑 `a`〜`i`**（`W-G.9-269R_前置階.md` 補令二段 `§丁`）
> **＋ 坑 `n`〜`z`**（`W-G.9-269R_本體階_補令六段.md` `§甲`）。
> 🔒 末欄「**本窗之施行**」逐坑載其**實際執行**者；標「（未觸）」者係本窗⛔ 有其受詞。

### 二-1　坑 `1`〜`10`（`W-G.9-268pR_補令五_記憶體側量測器.md` `§十一`）

| 坑 | 徵候 | 攔法 | 本窗之施行 |
|---|---|---|---|
| `1` | `cut_coords` 記憶體側為 `list`、落檔側為 `str` ⇒ 只吃字串之 parser 落入 `except: return None` 之靜默退路，母體悄悄變 `0` 組而器仍「跑完」 | 用**二形皆吃**之取值器；出艙**同格印母體基數**；母體為 `0` 者⛔ 得靜默綠 | 本窗之器**⛔ 觸 `cut_coords`**（受詞 ＝ 歸戶 ⋀ 三數量），惟仍**印判定集基數 `6`**，並設「判定集為空 ⇒ `rc = 3` loud 拒測」 |
| `2` | 傳了沒有 `_partial` 檔之目錄 ⇒ 閘一 loud 拒測 `rc = 3`；此為「**無從驗**」⛔「驗不過」 | 跑前先確認 `OUT` 下有 `off` 態之二 `_partial`；`on` 態**不會**產它 | （未觸）——本窗之器**⛔ 讀任何 `_partial`**、幾何與欄值**全自記憶體**（`rv.build_*` ＋ `run_step_g`） |
| `3` | 間諜未生效時 `calls` 為空；取錯基準點則「數字仍算得出來、仍很像對」，只是整條鏈平移一常數 | 出艙呼叫次數；⛔ 於器內重導 `d_hat`／`corner_pt`／`alloc`；`finally` 還原 `ns[...]` | （未觸）——本窗**⛔ 掛間諜**；惟其同族之戒**已施行**：歸戶查找**⛔ 另寫第二份定義**，一律用生產碼之 `_resolve_ownership`（自 `ns` 取） |
| `4` | 以 `cut_coords` **首頂點**為鍵 ⇒ 相鄰宗共用界線 ⇒ 碰撞（實測相異 `19`／宗 `21`）⇒ **數字全對、標籤全錯**且⛔ 報任何錯 | 鍵取**全部頂點**並當場證 `相異鍵數 ＝ 宗數`；`D-2`／`D-3` 之共同鍵 ＝ `(情境, 暫編地號)`⛔ 幾何 | **已施行**：本窗之鍵**逐字 ＝ `(暫編地號, 情境)`**（⛔ 幾何），恰為本坑所令之形 |
| `5` | `off` 中止只寫 `_partial`／`on` 跑完寫完整檔，**舊檔仍在原地** ⇒ 對拍得「逐位相同」係比到同一舊檔 | 快照前先清 `got_G值_退縮*.csv`；逐態各自快照並出艙**各態實際寫了哪個檔名** | **已施行（以更強之形）**：本窗之器**⛔ 寫任何 `got_*`**、⛔ 讀任何落檔 ⇒ 結構上免疫。二態之對拍係**同一器於二樹各跑一次**，其落檔**各自具名**（`…_mainstate.log`／`…_c1state.log`） |
| `6` | 自擬定義框**漏範圍框** ⇒ 缺號集偽多 `13` 個（`[106, 170–182]` vs 正典 `[106]`） | 一律用倉內既有器 `wg9268_gate5_tally.py`；⛔ 自擬框 | **已施行**：`§一-3` 之四簿三 `ref` 九組數**全部**出自該器；本窗**⛔ 自擬一個框** |
| `7` | `sed` 替換端反斜線被讀為轉義；`bash` 單引號內 `` \` `` 係字面反斜線 ⇒ 窄錨 `grep` 偽 `0`；`python -c "…"` 內反引號被命令替換吃掉；heredoc 被硬閘阻斷 | 一律 `python re.sub` 落檔；含反引號／反斜線／全形者以 `python`（`s in line`）復算；長字串一律 `Write` 落檔後以**路徑**呼叫；⛔ 用 heredoc | **已施行**：本窗**⛔ 用 `sed` 替換**；長字串（探針、報告、`commit` 訊息）**全部**以 `Write` 落檔後以路徑呼叫。heredoc 之一次誤用**由硬閘當場攔下**（`§四` 自解 `3`） |
| `8` | `harvest()` 回 `(ns, fake_st)` 之 `tuple`；逕 `ns["…"]` ⇒ `TypeError`（loud） | `ns, fake_st = harvest()` | **已施行**：本窗之器逐字 `ns, fake_st = harvest()` |
| `9` | `rc ≠ 0` ⛔ 等同「受詞紅」——判定集為空之 loud 拒測亦為 `rc ≠ 0` | 讀 `rc` 之同時必讀其閘紅逐項；「判定集為空」屬**無從驗**，⛔ 與「驗不過」共用出艙碼 | **已施行**：本窗之器之 `rc` 契約**逐碼分義**——`0` 全綠／`2` **量測器紅**／`3` **判定集為空（無從驗）**／`5` **受詞紅**，且其 docstring 逐字載「`rc ≠ 0` ⛔ 等同受詞紅」 |
| `10` | 倉內探針硬編他窗 worktree 之絕對路徑 ⇒ `rc = 0`、輸出完好、結論貌似全對（實測二窗 CSV 恰逐位相同 ⇒ **無後果，但那是運氣**） | 跑**任何**倉內探針前先 `grep -n "worktrees\|REPO *="`；指向他處者於 scratchpad 產**只改該一列**之副本 | **已施行**：本窗所跑之**二支**倉內探針（`wg9268_gate5_tally.py`／`probe_order_preflight.py`）**逐支**先掃——前者命中 `0`、後者 `REPO = os.path.dirname(VERIFY)`（自 `__file__`）⇒ **皆⛔ 硬編**。本窗**新建**之器亦⛔ 硬編（`REPO` 自 `__file__` 上溯·已自檢） |

### 二-2　坑 `11`〜`19`（`W-G.9-268pR_補令十二_…md` `§六`）

| 坑 | 徵候 | 攔法 | 本窗之施行 |
|---|---|---|---|
| `11` | `GB-161` 本體：所指之他樹**現仍存在** ⇒ 其「全綠」只因量到另一棵樹；**修其定位後同一支立刻轉紅** | 跑前 `grep`；**且逐支查其 `REPO`／`ROOT` 之取得方式**，⛔ 只看有無字樣 | **已施行**：二支之 `REPO` 取得方式**逐支讀其源碼列**（非只看字樣）——見坑 `10` 之末欄 |
| `12` | 路徑跨二源碼列之隱式串接 ⇒ `worktrees[/\\]` 樣式**靜默少抓**（`10` 檔／`17` 列 vs 裸字樣 `14` 檔／`27` 列），漏掉的正是起因那一支 | 路徑類樣式用**裸字樣**（⛔ 要求其後緊接分隔符）；併查 `os.chdir(`；見坑 `13` 之對照組 | **已施行**：本窗之定位掃描框逐字 ＝ `worktrees\|REPO *=\|ROOT *=\|os.chdir\|git -C`（**裸字樣** ⋀ **併查 `os.chdir` 與 `git -C`**） |
| `13` | 對照組只備「必不命中之哨兵」與「必非零之近義詞」⇒ **二者皆⛔ 證該框撈得到受詞那一類** ⇒ 對照組全綠而框漏抓本類 | **三造**：甲 必非零之近義詞／乙 人造哨兵／**丙 必命中之本類已知成員** | **已施行（二處）**：(i) 定位掃描之丙造 ＝ `probe_WG9268p_c1_synth.py`（得 `2`·必命中）⋀ 哨兵（得 `0`）；(ii) 歸戶器之**甲丙二造皆為「已知答案」造**（`628-42(1)` ⇒ `G011`／`628-53(2)` ⇒ `G025`）⋀ 乙造哨兵得空 |
| `14` | 哨兵之號寫成**字面**並隨探針入倉 ⇒ 次批其「真零」失效（由真零變 C 形 `1`／鬆框 `2`） | 哨兵**執行期自倉掃出**，其字面⛔ 出艙，一律以代稱指之 | **已施行**：本窗歸戶器之哨兵**於執行期組出**（`"".join(chr(c) for c in …)` ＋ 迴圈取首個不在 `own_map` 者），其**字面⛔ 落入 log**（log 只印「（空）」） |
| `15` | 造 `a` 之二式現況相異（`(a1)` 綠／`(a2)` 紅）；該紅在坑 `11` 修好**之前看不見** ⇒ `rc` 由 `0` 轉 `3` 係「定位修好」之**結果**、⛔ 新破損 | ⛔ 改該造之判準以就結果；`c3` 後二式**俱須重擬**（其處置候發單側） | （未觸）——本窗⛔ 跑 `c1_synth`、⛔ 動其任一字 |
| `16` | 活體檢取**合成幾何** ⇒ 二態**皆拋例外** ⇒ 皆「相同」⇒ 甲乙二造同紅、本檢失能；退路 `alloc ∥ d_hat` 亦堵死（失敗考古 `#7`：正交是特例、**斜交是本案常態**） | 改以**分支之被觀測**為可觀測量（`_oblique_s_max` 之被呼叫次數與回傳值）⇒ ⛔ 需任何幾何為真 | （未觸）——本窗之器**⛔ 裝選擇器活體檢**（其係單態器·同 `P-1` 基準器之款 `(2)`），亦⛔ 造任何合成幾何 |
| `17` | 兩造自證之「必紅」造可能**紅得不是地方**——替身於**定義時**捕獲真函式 ⇒ 其紅因例外而非因「二態同值」 | 替身內之相依**於呼叫時自 `ns` 取**；**逐造檢其紅之理由**，⛔ 只看紅不紅 | **已施行**：本窗三造**逐造檢其判之理由**——甲丙造係**與已知值逐位比對**（⛔ 只檢「非空」）；乙造係**必為空**且其受詞於執行期組出 |
| `18` | 其餘八則：emoji × `cp950` ⇒ 量測器紅；`rc` 經管線取到 `tail` 之 `rc`；`argv` 契約誤用；截斷 SHA ⇒ clone 靜默停在 `main`；`report()` 回 tuple；`KeyError` loud；heredoc；`grep -P` locale | 逐則之攔法見原文 | **已施行（四則）**：(a) 全程 `PYTHONIOENCODING=utf-8`；(b) `rc` **直取**（`echo "RC=$?"` 緊接受測物·⛔ 經管線）；(d) 拋棄式 clone 用**全 `40` 碼** `4dc35658f92f6a81191f8bcb423485728f9ac42e`，且 clone 後**逐位驗** `app.py` 工作區 `1464645` B ＝ blob `1464645` B ⋀ `hash-object` ＝ `rev-parse` ＝ `c5c28b82…`；(g) heredoc 之誤用由硬閘攔下 |
| `19` | 守恆帳之 `Σ池` 取「街廓面積 − `ΣG`」⇒ `街廓面積 − ΣG − Σ池 ≡ 0` **恆真**（`自誤 245` 恆真族），其「全綠」極具說服力 | 改取**獨立量測**之池（`推進側別 == '抵費地'` 列之 `幾何面積(㎡)`）⇒ 殘差⛔ 恆零 | （未觸）——本窗⛔ 記守恆帳。**其通則已施行**：本窗之外部錨取自**獨立來源**（補令七 `§四-1` 之表 ⋀ 前置階 `§戊` 項 `5`），⛔ 由本器自身之輸出重算（`常規八 二` ①） |

### 二-3　坑 `20`〜`26`（補令十四／十五・十六／十七／十八）

| 坑 | 徵候 | 攔法 | 本窗之施行 |
|---|---|---|---|
| `20` | 長跑之「無輸出」：`stdout` `0` B、無 `rc` 檔、無產物，而其**徵候看起來像「跑完且無輸出」**；`echo $?` 取到的是背景化本身之 `rc`（`0`）⇒ **假綠** | 須先出艙該跑確已執行之證：`(a)` `rc` 檔存在 ⋀ `(b)` 產物時戳晚於啟動 ⋀ `(c)` 產物檔數 `> 0`；**⛔ 於 `run_in_background` 之命令內再用 `&`** | **已施行**：本窗之二次實跑**皆前景執行**（⛔ `&`）、各落 `.log`／`.err`／`.rc` 三檔，且其 `stdout` 非空（`mainstate` 與 `c1state` 各 `60` 列）⇒ 三條件齊備 |
| `21` | `app.py` 之 hash **二式並存**——單載「`sha256` `1d7d7c73…`」實為 **git blob `sha1`** ⇒ 只比一式者必誤判而停機 | 一律**二式並報** ＋ blob bytes | **已施行**：本報告之 `app.py` 錨**逐處具名其式**——`1d7d7c73…`／`c5c28b82…` 皆標 **blob `sha1`**（`rev-parse`）；工作區之驗另以 `hash-object` ＋ **bytes** 並報 |
| `22` | 以「就地換檔」取二樹：`harvest()` 依**路徑**快取 ⇒ 不清快取者第二樹必取到第一樹之 `ns`，而 `rc = 0`、輸出完好、結論貌似全對 | 三項必備：`(1)` 每次取用前清快取 `(2)` 實查所取之樹之**自我驗證閘** `(3)` 還原以**換檔前之原始位元組**為基準置於 `finally` | **已施行（以更強之形）**：本窗**⛔ 就地換檔**——二態係**二個獨立行程於二棵樹**各跑一次（主 worktree ＋ **拋棄式 clone**）⇒ `harvest()` 之快取結構上不可能跨態污染；且**每跑各自印其樹之 `app.py` blob**（`1d7d7c73…` vs `c5c28b82…`）＝ 本坑款 `(2)` 之自我驗證閘 |
| `23` | `fetch` refspec 未帶 `+` ⇒ 非快轉之更新被拒、追蹤 ref **半靜默**停在舊值；據之者將判「對方之推送未落地」⇒ 不實指控 | 一律 `git ls-remote` 實查；本地追蹤 ref ⛔ 得作證；二者一致亦⛔ 免除實查 | **已施行**：本窗**全部四次** ref 之判（期初三 `ref`／`push` 前復查／`push` 後三 `ref`）**皆 `ls-remote`**；本窗之 `fetch` 之 refspec **逐字帶 `+`**（`+refs/heads/…:refs/remotes/…`） |
| `24` | 「拆器」只建新器而不清舊器 ⇒ 第二份定義並存，且舊器內該組落於前置閘之後而**結構上不可達**；二處日後相異時⛔ 可判何者為準 | 令拆器須逐項令**原處之處置**（移除／保留並具名其不可達／改為委派）三者擇一且須具名 | **已施行**：本窗**新建一器**（`probe_WG9269_c2_owner.py`），其 docstring 逐字具名「**⛔ 另寫第二份定義**」並載其所 `import` 之生產函式；本窗**⛔ 動任何既有器一字** ⇒ ⛔ 生第二份定義 |
| `25` | 移除一組造後器之**結語列**留下虛報覆蓋——`rc = 0`、全綠，而結語所列之造根本不存在 | 凡增刪任一造，同批檢其結語列；改結語後須**復跑一輪**證判定列逐位相同 | **已施行**：本窗於**發現「處置」欄未逐情境分列**後，改器並**復跑一輪**，且**逐位對拍**二態之判定段（見 `§三-1`）——其改**未動任何造**，三造之判定列改前改後逐位相同 |
| `26` | 呈 KL 之文件內**自行增列**單所未令之內容 ⇒ ⛔ 任何閘會紅，而增列之物⛔ 經「出艙一數須具名其框」之紀律，且讀者將以**最近之上下文**解讀之 | 增列前先問「這是單令我載的嗎」；增列須逐數具名其框；**⛔ 將不同錨態之數並列**。併記 `(1)` 來源受 `.gitignore` 忽略者自倉觀之即無推導紀錄 `(2)` 不同錨態之數⛔ 並列 | **已施行**：`§三` 之表**逐數具名其出處**（實跑落檔／補令七 `§四-1`／前置階 `§戊`），且其**證據落檔隨本報告入倉**（`verify/out/WG9269R_c2pre_*`·**⛔ 受 `.gitignore` 忽略**——該目錄只忽略 `got_*` 與 `W-D.4_*`）。🛑 **本報告⛔ 呈 KL**（其為 CC 側報告）；`c2` 呈 KL 之文件於**併線令下達後**另辦 |

### 二-4　坑 `a`〜`i`（`W-G.9-269R_前置階.md` 補令二段 `§丁`）

| 坑 | 徵候 | 攔法 | 本窗之施行 |
|---|---|---|---|
| `a` | 文字層讀寫致一檔 `LF`→`CRLF`；`numstat` 全檔「每列皆變」而 diff **可見內容完全相同**、`rc = 0`、⛔ 報錯（🔴 **成因未明**·隔離 round-trip ⛔ 復現） | `numstat` **逐檔須 `1 1`**（新檔則 `N 0`）＋ **`CR` 改前→改後**逐檔斷言；改以**二進位式**（`rb`／`replace(bytes)`／`wb`） | **已施行**：本窗所落之每一檔皆**當場量 `CR`／`LF`／bytes**（單 ＝ `CR 0`／`LF 57`；探針與報告同辦），且 `numstat` 逐檔為 `N	0`（純新增·`deletions 0`） |
| `b` | 主 checkout 之本地 `wip` 落後 `origin` **`90`** commit；本地追蹤 ref 看似正常 | 凡於主 checkout 動作前先 `ls-remote` ⋀ `left-right` 實查，⛔ 假定本地為最新 | **已施行**：`§一-1`／`§一-2`；本窗**⛔ 於主 checkout 動作**（全部動作落本 worktree 與拋棄式 clone） |
| `c` | 抽取器之錨**非唯一**（命中 `2` 列）⇒ 取首筆即取到**錯的段** | 抽取器內置 `assert len(idx) == 1`，當場 `AssertionError` 停，逐處讀過再窄化框；⛔ 取首筆 | **已施行**：本窗之「補令八」負存在查**⛔ 取首筆**，而係**逐 `ref` 列舉其全部 `docs/orders` 檔名** ＋ **逐列讀** `2` 筆同列命中之全文（見 `§五-2`） |
| `d` | 二方之數相異（補令 `3`／`16` vs 本窗 `4`／`40`）⇒ 看似其一有誤，實為**母體之別**（剔除子集 `21` 口徑 vs 全配地宗 `46` 口徑） | **先對齊母體，再判孰誤**；出艙相異之數時同格載各自之母體與其基數；母體不同而數相異者**二者可皆正確** | **已施行**：`§一-3` 之四簿表**逐 `ref` 分欄**（⛔ 把三 `ref` 之數混為一格）；`§三-1` 之表**逐情境分列**並載其母體（`g_rows` `59`／`60` 列·配地宗 `46`） |
| `e` | 情境相依之數未具名情境（「`K-9-12` 之 `19` 宗」「`B` 合格 `3`／不適用 `16`」皆為 `0m` 專屬） | **本案之一切量皆情境相依**；凡出艙一數須逐情境分列或同格具名其情境；**相減之二數須同屬一情境** | **已施行 ＋ 🩸 本窗自捕一次**：本器首版之「處置」欄**只載 `0m` 之分類而冠於二情境** ⇒ 於入倉**前**自捕並改為**逐 `(宗, 情境)`**（見 `§四` 自解 `1`） |
| `f` | `wg953-handoff-ready-d1e3c0` 已**不存在** ⇒ `wg960_z_selfcheck.py` 改前必 loud 失敗；**其既往落檔⛔ 復現、全部無效**，惟其**數**仍有效（`ROOT` 只決定輸出路徑） | 區辨「路徑之相依」與「值之相依」 | （未觸）——本窗⛔ 跑 `wg960_z_selfcheck.py` |
| `g` | ⛔ 鑄號之**形檢**⛔ 等同 **MAX 檢**——形檢報「payload 有 `2` 列被 `GB` 定義框命中」貌似鑄了號 | operative 判準 ＝ **MAX／相異／缺號集之不變** | **已施行**：本窗**⛔ 鑄任何號**，其證 ＝ `§五-1` 之期初／期末四簿**三數逐位不變**（⛔ 以形檢代之） |
| `h` | CSV 之**列數 ≠ 受詞之母體**（`got_G值` `59`／`60` 列 vs 閘之四態和 `46`）⇒ 逕以列數為母體則判別力閘紅，且會被誤讀為「閘壞了」 | 母體須以**受詞之射程**界定（須扣抵費地列）；「**四態和 ＝ 配地宗**」即其恆等式自檢 | **已施行**：`§三-1` 之表逐列**同格載** `g_rows` 列數（`59`／`60`）**與**配地宗（`46`），⛔ 以列數充母體 |
| `i` | `GB-158`：B 形（`` 自誤 \`N\` ``）⛔ 單獨採信——本波**三度**實測其對已占用者得 `0` | 意指占用查一律 **B 形 ⋀ C 形**並取，並同格出艙對照組於各形之命中；任一形使對照組全 `0` ⇒ 該形⛔ 單獨採信 | （未觸）——本窗⛔ 取號 ⇒ ⛔ 行意指占用查。🔒 **其通則已施行於他處**：`§五-2` 之負存在查**⛔ 單框**，而係「三 `ref` × 檔名列舉 ＋ 同列共現框 ＋ 二對照組」並行 |

### 二-5　坑 `n`〜`z`（`W-G.9-269R_本體階_補令六段.md` `§甲`）

| 坑 | 徵候 | 攔法 | 本窗之施行 |
|---|---|---|---|
| `n` | `WV_BAKE` **之三害**（⛔ 只知其一）：`(1)` **控制流改變**（`diff_rows` 一律回綠 ⇒ 下游守衛皆不中止 ⇒ 名目 `64 → 123`）`(2)` **覆寫集 `51` 檔**⛔ 九份 `(3)` 將真實資料覆寫為 **`0` B**（`F.0_合併決策_退縮{0m,3.5m}.csv` `947`／`964` B ⇒ `0` B） | `(a)` ⛔ 執行 `WV_BAKE=BASELINES`（`GB-162` 禁令·**永久**）`(b)` 側烤一律 `WV_BAKE=<拋棄式 side dir>` `(c)` 受詞集逐檔具名 `(d)` **逐檔驗「非空」**方得複製 | **已施行**：本窗**⛔ 設 `WV_BAKE` 任何值**、⛔ 跑任何烤；`verify/baselines` 期初期末**逐檔 blob 相同**（見 `§五-1`） |
| `o` | `baseline` ＝ **應然值**⛔ 會過期之快照；現行 `40` 紅之內容即其與現行輸出之 `1137` 格差。🔴 **「它過期了 ⇒ 重烤」讀起來完全合理**，而重烤 ＝ **一次抹去該 `1137` 格之缺陷證據**，且⛔ 任何閘會紅（重烤後全綠且**看起來像修好了**） | `GB-162` 三禁令 ＋ 立準：**凡令「更新／重產／覆寫」一既有之物，須先出艙該物於系統中之角色**（被比對之應然值 抑或 被產出之結果），前者之覆寫即**抹去判準** | **已施行**：本窗之一切數**皆自當次實跑取得**（`GB-162` 之「⛔ 引任何自 baseline 取得之數」）——`§三-1` 之六格三數係 `run_step_g` 之記憶體側 `g_rows`，**⛔ 一數取自 `verify/baselines`** |
| `p` | 跨器逐位對拍：`0／11` 全不過而其不成立**恆由寫檔器所致**（`_bake_csv` LF-only ＋ baseline 欄序 vs `_dump_csv` CRLF ＋ `rows[0].keys()` 之序）——**「全不過」讀起來像「全錯了」** | 立閘前**先出艙二者之序列化約定**並證其相同；相異者改**正規化後逐格**對拍，而正規化**只得**處理 行尾／BOM／欄依欄名排序——⛔ 轉浮點、⛔ 任何數值容差、⛔ trim／casefold；附硬判別力二造 | **已施行（以更強之形）**：本窗之逐位對拍係**同一器之二次輸出**（⛔ 跨器）⇒ 序列化約定**結構上同一**；其對拍**⛔ 任何正規化**（`diff` 直取）⇒ 免疫本坑 |
| `q` | 「某字樣命中 `0`」為閘者：本窗實測**五受詞於字樣框皆命中 `1` 而全在 docstring** ⇒ 以字樣命中為閘必誤判 | 以 **AST**（去 docstring／註解／字串常量）機械判；判別力須同格出艙 | **已施行（以規避之形）**：本窗**⛔ 立任何「字樣命中 `0`」之閘**。🔒 其相關之判（旗標預設仍 `off`）係**讀其回傳式之逐字**（`environ.get(K917_BACKFILL_ENV, "0")`）、⛔ 以字樣計數為之 |
| `r` | 態宣告：以 `git checkout <rev> -- <檔>` 切態者 `HEAD:<檔>` **恆不變** ⇒ 二次量測之態宣告相同而其實際執行之碼相異、**⛔ 任何錯誤訊息**（本窗實見 `1d7d7c73…` 實跑而宣告 `c5c28b82…`） | 態宣告**二值並報**——`rev-parse HEAD:<檔>`（committed）⋀ `hash-object <檔>`（工作區·**以後者為實**）；二值相異即告警，須同格具名 | **已施行**：本窗之器**內建**該二值並報，且**逐跑印其判**——二態皆得「**相同** ✅」（主 worktree `1d7d7c73…`／clone `c5c28b82…`）⇒ **二態確為不同態**之自證成立（且⛔ 用 `checkout -- <檔>` 之切態法） |
| `s` | 簿態**隨 `ref` 而異**：`自誤 355`／`356`／`357` 於主線 B／C 形皆 `0`／`0`，於側支 `05a11bd` 各 `3` 檔／`6`–`9` 列；簿態 主線 MAX `354` vs 側支 `357` ⇒ **單方 `rev` 之取號現查⛔ 足以判「未占用」** | `(1)` 取號現查**二 `ref` 並查**並出艙差集 `(2)` 取「於**二 `ref` 皆未占用**」之號 `(3)` 缺號永不重用且須於簿末逐號登記其歸屬 `(4)` 該登記節之形須避 `GB-132` | **已施行**：`§一-3` **三 `ref` 並查**並逐簿分欄；其差集（自誤 主線 `358` vs `c1` `361` vs 凍存 `357`）**逐值出艙**。🛑 本窗**⛔ 取號**（單未授權） |
| `t` | 於 `expand` 段（驗收 ＝ 逐位不變）內順手修無涉之既有瑕 ⇒ **修之即破壞該 `commit` 之驗收**，且使「本次改動之效」與「順手之效」不可分（實例：`_wg9268p_anchor_advance` 於 `_WF_NS_NAMES` 之既有漏列） | **具名而不修**；其修另立工項 | **已施行**：本窗**⛔ 動 `app.py`／`verify/stepg_pipeline.py` 一字**（`§五-1` 逐位驗），亦⛔ 動任何既有探針一字 |
| `u` | 對照組於**全部**受詞皆得 `0`——可能為受詞之實，亦可能為**量測器停在零**，**二者之徵候相同** | **先跑兩造自證**方得出艙該 `0`（本波 ＝ 注入單一字元得 `1` ⋀ `B vs` 現行 baseline 得 `264`） | **已施行**：本窗歸戶器之三造**先跑**且其判**先出艙**；`§五-2` 之負存在查亦備二對照（「補令七」`11`–`13` 檔 ⋀ 「補令八」字樣本身 `14` 檔 ⇒ **器能撈到該字樣**）⇒ 其 `0` 方得出艙 |
| `v` | **級聯剔除**：`K-9-17 五` 之必然——一宗被剔除後其後繼者**整體前移**、幾何隨之改變 ⇒ 閘一**重判**。🔴 旗標 `on` 後 `0m` 消失 **`3`** 宗，而 `off` 態閘一判不合格者僅 **`2`** 宗；`628-27(1)` 之原分配**大二個數量級** | `(1)` **⛔ 以「受詞二宗」呈 KL** `(2)` 差異表之「處置」欄須區分 `閘一直接剔除`／`級聯剔除`**⛔ 併為一類** `(3)` `表三` 之「改列調配池」須逐戶具名且**級聯剔除者單獨小計** `(4)` 終態須驗⛔ 再有閘一不合格者 | **已施行（款 `1`／`2`）**：`§三-1` 之表**逐宗三宗**（⛔ 二宗）⋀ 「處置」欄**二類分列且逐情境**。款 `3`／`4` 之受詞落於 `c2`／`c4`（**併線令下達後**） |
| `w` | 其餘八則自捕（`_dump_csv` 計數三值相異／注入錨於**外接框角**致交集恆 `0`／CJK `quotepath` 偽 `0`／違規列顯示截斷／讀 `rc` **於其寫入之際**／heredoc 三度／以「新欄 `0`」逕認驗丙過／判別力造選到一個恰為零之檔） | 逐則之攔法見原文 | **已施行（三則）**：`w-3` ＝ 本窗之檔名列舉**逐處用 `git ls-tree -r --name-only -z` ＋ `tr '\0' '\n'`**（⛔ 裸 `git ls-files`·避 CJK `quotepath` 偽 `0`）；`w-5` ＝ `rc` 與其 log **同格落檔並同格復讀**；`w-6` ＝ heredoc 由硬閘攔下（`§四` 自解 `3`）。`w-8` 之通則亦已施行：本窗⛔ 以「一個恰好為零之實測」充判別力，三造**皆為已知答案之造** |
| `x` | `docs/orders` 之**補令四／五僅存於凍存側支**——`c1` 側支之 `docs/orders/` 缺該二檔，補令三〜五段之**報告**亦僅在 `05a11bd` | 凡引用該二補令或其報告者，**須逐字附側支名 `verify/W-G.9-269-p3a` 與 `commit 05a11bd`**；⛔ 以「主線查無」推論其不存在 | **已施行**：`§一-6` 之 `P-1` 器「主線⛔ 存在」一判**逐字附其側支與 commit**；`§五-2` 之負存在查**三 `ref` 並查**（含凍存側支）⇒ ⛔ 以主線查無逕推 |

---

## §三　`c2` 之三前置（**本窗備妥**·`§戊` 所令·⛔ `c2` 本身）

> 🛑 **本節⛔ 翻旗標、⛔ 併線、⛔ 動生產碼一字**——三前置係**量測與紀律**，⛔ `c2` 之施作。

### 三-1　前置 `1`：級聯剔除**逐宗載明**（`0m` **`3` 宗**／`3.5m` **`1` 宗**）

🔒 **三軸**：`(1)` 來源 ＝ **二態各自實跑**（主 worktree `0aa608e`／拋棄式 clone `4dc3565`·**⛔ baseline**）；
`(2)` 母體 ＝ `run_step_g` 之 `g_rows`（`0m` **`59`** 列／`3.5m` **`60`** 列·其**配地宗 `46`**·⛔ 以列數充母體·`坑 h`）；
`(3)` 粒度框 ＝ **格**（宗 × 情境·判定集基數 **`6`**）。

🔒 **「處置」欄⛔ 本器所量**——其出處 ＝ `c1` 側支之實跑落檔 `verify/out/WG9269R_c1_on_drop.log`
（`0m` 消失 `3` 宗／`3.5m` 消失 `1` 宗·**二情境之終態皆⛔ 再有閘一不合格者** ⇒ 迴圈收斂）
＋ 補令七 `§四-1` 之二分類。**本器所量者** ＝ 歸戶 ⋀ `a 面積` ⋀ `G` ⋀ `宗地寬度`（皆 `off` 態·管線跑畢）。

| 宗地 | 情境 | **歸戶** | `a 面積(㎡)` | 原分配 `G(㎡)` | 宗地寬度(m) | **處置** |
|---|---|---|---|---|---|---|
| `628-42(1)` | `0m` | `G011` | `8.34` | `0.23` | `0.18` | **`閘一直接剔除`** |
| `628-42(1)` | `3.5m` | `G011` | `8.34` | `4.77` | `0.11` | ⛔ 剔除（**留於配地**） |
| `628-53(2)` | `0m` | `G025` | `2.78` | `0.00` | `0.02` | **`閘一直接剔除`** |
| `628-53(2)` | `3.5m` | `G025` | `2.78` | `0.00` | `0.02` | **`閘一直接剔除`** |
| **`628-27(1)`** | `0m` | **`G030`** | **`51.32`** | **`29.26`** | `0.65` | 🔴 **`級聯剔除`** |
| **`628-27(1)`** | `3.5m` | **`G030`** | `51.32` | `29.61` | `0.66` | ⛔ 剔除（**留於配地**） |

🛑 **`0m` 實為 `3` 宗**——**⛔ 以「受詞二宗」呈之**（補令七 `§四-2` 款 `4`）。
🛑 **二類⛔ 併為一類**：`0m` ＝ 直接 `2` ＋ **級聯 `1`**；`3.5m` ＝ 直接 `1` ＋ **級聯 `0`**。
🔒 **`3.5m` 之二列（`628-42(1)`／`628-27(1)`）係「留於配地」**——⛔ 以 `0m` 之處置冠於 `3.5m`（`坑 e`）。

**外部錨之判**（`常規八 二` ①·⛔ 以 `N/N` 自證）
- ✅ 三宗之 `a 面積`／`G`／`宗地寬度` 與**補令七 `§四-1` 之表**（`0m`）**逐值相符**。
- ✅ `628-42(1)` ⇒ `G011` ⋀ `628-53(2)` ⇒ `G025` 與 **`W-G.9-269R_前置階.md` `§戊` 項 `5`** 之已知值相符。

**量測器非紅之自證**（`坑 13` 三造·**先跑先判**·`坑 u`）

| 造 | 受詞 | 得 | 期 | 判 |
|---|---|---|---|---|
| 甲（必非零·**已知答案**） | `628-42(1)` → 歸戶 | 原地號 `628-42` ⇒ `G011` | `G011` | ✅ |
| 丙（必命中之本類已知成員） | `628-53(2)` → 歸戶 | 原地號 `628-53` ⇒ `G025` | `G025` | ✅ |
| 乙（必為空·哨兵**執行期組出**·字面⛔ 出艙·`GB-147`） | 一必不存在之暫編地號 | （空） | （空） | ✅ |

⇒ **器非紅**。併：`rv.build_ownership` 之**靶組核對**（`OWNERSHIP_TARGETS` 三環定理鏈 tripwire）＝ **✅ 全符**；
歸戶群組 **`33`** 組／`t8_ownership_map` 鍵 **`59`**／**對應失敗 `0`**。

### 三-2　前置 `2`：🔴 **`628-27(1)` 之歸戶 ＝ `G030`**（於**管線跑畢之態**取之）

🔒 **其取得之態**（補令七 `§四-2` 款 `2` 逐字：「發單側查其歸戶對映於 `harvest()` 時為空
⇒ 須於**管線跑畢之態**取之」）：本器於 **`rv.build_ownership` 已跑畢**之態取 `t8_ownership_map`，
**⛔ 於 `harvest()` 時取**；其查找用**生產碼之 `_resolve_ownership`**（`app.py` 四層查找·自 `ns` 取·
**⛔ 另寫第二份定義**·`GB-48` 族），正規化用生產碼之 `_normalize_landno_module`。

| 情境 | 歸戶 | `a 面積(㎡)` | 原分配 `G(㎡)` | 宗地寬度(m) |
|---|---|---|---|---|
| `0m` | **`G030`** | `51.32` | `29.26` | `0.65` |
| `3.5m` | **`G030`** | `51.32` | `29.61` | `0.66` |

🔴 **其量級**：`628-27(1)` 之原分配（`0m` `29.26 ㎡`）較 `628-42(1)`（`0.23`）與 `628-53(2)`（`0.00`）
**大二個數量級** ⇒ **`c2` 之土地後果大於補令三 `§零-1` 之受詞表**。
🛑 **`GB-159` 仍有效**：受影響之宗（**今為三宗**·歸戶 `G011`／`G025`／**`G030`**）之
**配地圖與負擔表 ⛔ 對外**。

### 三-3　前置 `3`：態宣告**二值並報**（`坑 r`）

| 跑 | `commit` | `rev-parse HEAD:app.py`（committed） | `hash-object app.py`（**工作區·實**） | 判 |
|---|---|---|---|---|
| 主 worktree | `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2` | `1d7d7c7324719f13e7d61473a55001170ef44d8e` | **同** | ✅ 同步 |
| 拋棄式 clone（`c1` 態） | `4dc35658f92f6a81191f8bcb423485728f9ac42e` | `c5c28b826aa25f4f12a3bd5bdc1fcd0587ab021f` | **同** | ✅ 同步 |

🔒 **二態確為不同態之自證**：二跑之 `app.py` 工作區 hash **相異**（`1d7d7c73…` ≠ `c5c28b82…`）
⇒ `坑 r` 所指之「二次量測之態宣告相同而實際執行之碼相異」**結構上不可能發生於本窗**。
🔒 **二態之判定段逐位對拍 ＝ 相同**（`diff` 直取·⛔ 任何正規化）
⇒ **旗標 `off` 下 `c1` 為純 `expand`·逐位不變**於本器之受詞上再度坐實。
🩸 **主 worktree 之 `git status --porcelain` 非空**（該跑之時點所量 ＝ **`5`** 列·逐字見其 log；
clone 側 ＝ `1` 列 ＝ 拷入之探針）——照實具名：其為本窗所落之
**未入倉新檔**（開工令·探針·首跑之 log／err／rc）；工項零之 commit 係**以 plumbing 落於側支**
（`hash-object -w --no-filters` ＋ `read-tree`／`update-index`／`write-tree`／`commit-tree`）
⇒ **本 worktree 之 index 未動** ⇒ 該檔於本 worktree 仍顯示為 `??`。**⛔ 讀為「入倉失敗」**。

---

## §四　本批之自解清單（`常規五` 自解款 款 `五`·逐項載 ①疑義 ②二個以上讀法 ③所取者 ④機械證據 ⑤所棄者之由）

### 自解 `1`　🩸 **本窗自捕**：「處置」欄未逐情境分列（`坑 e`）

① **疑義**：首版之器將補令七 `§四-1`（**`0m` 專屬**）之「處置」二分類**冠於二情境**，
致 `3.5m` 之 `628-42(1)`／`628-27(1)` 被標為「閘一直接剔除」「級聯剔除」，**而該二宗於 `3.5m` 並未被剔除**。
② **二讀法**：`(甲)` 該欄係「此宗於本波之處置」（情境無關）／`(乙)` 該欄係「此宗於**該情境**之處置」（情境相依）。
③ **所取** ＝ `(乙)`。
④ **機械證據**：`c1` 側支之實跑落檔 `verify/out/WG9269R_c1_on_drop.log` 逐字——
`[0m]` 消失之宗 ＝ `['628-27(1)', '628-42(1)', '628-53(2)']`；`[3.5m]` 消失之宗 ＝ `['628-53(2)']`
⇒ `(甲)` 與該落檔**直接矛盾**。改器後**復跑一輪**，三造之判定列改前改後**逐位相同**（`坑 25`）。
⑤ **所棄者之由**：`(甲)` 牴觸 `坑 e`（本案之一切量皆情境相依）且與倉內落檔矛盾。
🔒 **本自解之類** ＝ 白名單 `5`（**量測器紅**——CC 自身工具之缺陷 ⇒ 修工具、重跑、記為**自捕**）。
🔒 **零土地後果**：其為**出艙之標籤**，⛔ 改任何宗之 `G`／面積／歸屬（表內六格之四個量測欄改前改後逐位相同）。

### 自解 `2`　檔名之取（`常規三`）

① **疑義**：本開工令入倉之檔名，單未指定。
② **二讀法**：`(甲)` 依序號取 `補令八`／`(乙)` 取敘述性檔名。
③ **所取** ＝ `(乙)` `W-G.9-269_本體階新施工窗_開工令.md`。
④ **機械證據**：本單自身逐字引「補令八 `§四`」為**既有文件** ⇒ 該號係**意指占用**（`常規四（七）二`），
且 `W-G.9-269 補令八` 於三 `ref` 之 `docs/orders` **皆⛔ 存在**（見 `§五-2`）⇒ 由本單占之將**撞號**、嚴格劣。
⑤ **所棄者之由**：同上。🔒 **類** ＝ 白名單 `7`（框慣例之別 ⇒ 取較嚴者）／**無土地後果**（`常規三`）
⇒ **候發單側核可**（⛔ 逕作成明文）。

### 自解 `3`　heredoc 之一次誤用（**由硬閘當場攔下**·⛔ 自解，係**紀錄**）

`verify/tools/wg9237_heredoc_guard.py` 於本窗**硬阻斷一次**（`H1 heredoc/herestring 引入符`）
⇒ 改以 `Write` 落檔後以**路徑**呼叫。🔒 **其為「活體攔截實測」之證**（⛔ 只 `--selftest`·`坑 w-6`）；
併證 `§一-2` 之 `.claude/settings.json` 於本 session **確已生效**。

🛑 **本批⛔ 有落入黑名單之自解**——⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；
⛔ 動生產碼一字；⛔ 新訂任何準據；⛔ 放寬任何既有之閘。

---

## §五　自限復驗 ＋ 🛑 呈發單側

### 五-1　自限復驗

| 項 | 實測 |
|---|---|
| **主線⛔ 前進** | 期初 ＝ 期末 ＝ `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2`（`ls-remote` 實查·二次） |
| **凍存側支⛔ 動** | 期初 ＝ 期末 ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4` |
| **生產碼⛔ 動一字** | `4dc3565` vs `7fb9c2b` 之生產碼 `34` 檔 blob 相異 ＝ **`0`**；本報告之 commit 同辦（見其 `commit` 訊息） |
| **旗標預設⛔ 翻** | `k917_backfill_enabled` 之預設仍為 `"0"`；二次實跑皆於 `WG9269_K917_BACKFILL` **未設**之環境 |
| **`verify/baselines` ⛔ 觸一位元組**（`GB-162`） | `git rev-parse <rev>:verify/baselines` 期初 ＝ 期末 ＝ **`241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`**（樹物件·⇒ 其下**每一檔**逐位未動） |
| **⛔ 引任何自 baseline 取得之數** | `§三` 之全部數**皆自當次實跑取得**（二態各一次） |
| **⛔ 鑄任何號**（operative 判準 ＝ **三數之不變**·⛔ 形檢·`坑 g`） | 四簿於 `c1` 基座 `4dc3565` 與本批期末**逐簿三數相同**：自誤 `351`／`361`／`[106, 355, 356, 357]`；`GB` `160`／`162`／`[12, 87]`；`VR` `80`／`95`／`[73, 75]`；`K-9` `28`／`29`／`[]` |
| **`numstat`·`deletions ＝ 0`** | 逐 commit 出艙（純新增檔） |
| **`rc` 直取·`stderr` `0` B** | 二次實跑 `RC=0`／`stderr` `0` B；`S-0b` `rc = 0`／`stderr` `0` B |

### 五-2　🛑 呈發單側（**二項**·⛔ CC 可裁）

#### 項 `1`　🛑 **併線令未下達 ⇒ 本窗之工項鏈全部阻塞**

單之工項逐字 ＝ 「**待發單側之併線令** → 快轉併線 → `c2` → `c3` → `c4`」，
而單之界逐字 ＝ 「`c1` **候 KL 放行**；**併線令由發單側下達**——⛔ 自行併線、⛔ 自行判斷已放行」。
⇒ 🔒 **本窗⛔ 得起 `c2`／`c3`／`c4` 任一款**。本窗所辦者為**其前置與紀律**（`§零`〜`§四`）。
🔒 **`c1` 側支已備妥待併**：其生產碼二檔 blob **仍為 `8f3dccc` 之內容**（`§一-5`），
且本窗之二 commit 皆**零生產碼**（`§五-1`）⇒ **快轉併線之條件⛔ 因本窗而改變**。
🛑 **請發單側下達併線令**（或指示本窗於併線前另有可辦之工項）。

#### 項 `2`　🔴 **`W-G.9-269 補令八` 於三 `ref` 皆⛔ 存在**（照實回報·⛔ 編造·⛔ 以倉內近似物代替）

單逐字載「🔴 **`c2` 之三前置**（**補令八 `§四`**）」。**現查**：

| `ref` | `docs/orders` 內 `W-G.9-269` 之單（**正面列舉**·`git ls-tree -r --name-only -z`·⛔ CJK `quotepath`） |
|---|---|
| 主線 `0aa608e` | 施工單_病一_前置階／**補令一**／**補令二**／**補令三**／**補令六** |
| `c1` `7fb9c2b` | 上列 ＋ **補令七** |
| 凍存 `05a11bd` | 施工單_病一_前置階／補令一／補令二／補令三／**補令四**／**補令五** |

⇒ **`W-G.9-269 補令八` 於三 `ref` 之 `docs/orders` 皆⛔ 存在**；
且「`W-G.9-269` … `補令八`」之**同列共現**框於三 `ref` 各命中 **`2`** 列，
**逐列讀之**（⛔ 取首筆·`坑 c`）——二者**皆指 `W-G.9-268p 補令八`**
（`docs/orders/W-G.9-268p_補令十_序1p核可與併線授權.md:87`／
`docs/reports/W-G.9-268pR_補令十_序3併線與序4六號之鑄.md:97`·其受詞為 `GB-159`）。
**二對照組**：「補令七」＝ `11`／`13`／`11` 檔（**必非零** ⇒ 器非恆零）；
「補令八」之**裸字樣**（含他波）＝ `14`／`14`／`14` 檔（⇒ **器確能撈到該字樣**·`坑 13` 丙造）
⇒ **器非紅**，其 `0` 方得出艙。

🔒 **本窗之判（照實·⛔ 頂替）**：**其內容⛔ 滅失**——單所列之三前置，
款 `1`／`2` 與 **`docs/orders/W-G.9-269_補令七_c1核可與級聯剔除.md` `§四-2` 款 `1`／`2`／`4`**
（節題逐字 ＝ 「🔴 **級聯剔除：`c2` 之土地後果大於受詞表**」）**逐字對應**；
款 `3` ＝ `坑 r` ＋ 單之硬準則〔遠端與態〕。
⇒ 🔒 **本窗依單所<u>自載</u>之三前置逐項辦竣**（`§三`），**⛔ 據該引註推論任何未載之款**。
🛑 **是否為「`補令七` 之誤引」抑或「補令八確曾出單而未遞交」（`W-G.9-222` 工項四 `a` 之同形
——`219` 之「CC 側無痕跡係應然」），⛔ CC 可判**：
**請發單側確認**，若確有補令八則**請補**（`CLAUDE.md` 判準（給 CC）：單內查無 X 時⛔ 編造、照實回報並請補）。

### 五-3　🛒 本報告**⛔ 載**

`c2` 之任何修法主張；任何併線動作或其判斷；旗標預設之變更；
受影響三宗之**配地圖或負擔表**（`GB-159`·**⛔ 對外**）；
任何自 `verify/baselines` 取得之數（`GB-162`）；`GB-160` 閘 `γ` 之任何重量或調框。

---

# `W-G.9-269R`　**補令八段**（工項零 ／ `§三` 失效條款之復驗 ／ `§二-3` 立準 ／ 🩸 二項懸引）

> **落點 ＝ `c1` 側支**（補令八 `§六` 序 `1` 逐字：「工項零（本補令原封入倉）」→ `c1` 側支·主線待併期間⛔ 前進）。
> **基座** ＝ `a67f9d7679ba56df5c332a49e969778cb50aae03`（本窗前段之 commit·`ls-remote` 實查）。
> 🛒 **本段⛔ 載**：任何併線動作（`§六` 序 `2` 逐字「**KL 放行後** ⇒ 快轉併線」·KL 尚未放行）。

---

## `§零`　工項零 ＋ `S-0b`

### `零-1`　原封入倉（`作業常規之追加四` 款 `一`）

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付·主 checkout 之工作區·**該處⛔ 追蹤**） | `3ab23821ac764c4780002313f692b2f7abd161ed1e0efcc15280fb775597b526` | `6081` | `0` | `92` |
| 落檔（本 worktree·**二進位拷貝**·⛔ 文字層讀寫·`坑 a`） | **同** | `6081` | `0` | `92` |
| **blob** | **同** | `6081` | — | — |

🔒 **三端逐位相同** ✅（`a == b` 之直判 ＋ `sha256` 並報）。
🛑 **射程（`追加四` 之界·照實）**：本鏈所證者為「**落檔 → 入倉**」之保真；
本次之來源係 **KL 置於主 checkout 之實體檔**（⛔ 聊天文字之轉寫）⇒ 其保真**強於**本窗前段之開工令
（該件無獨立 `sha256` 可比）。**⛔ 以本件之強度回溯充該件。**

### `零-2`　`S-0b`（`probe_order_preflight`·`rc` **直取** ＝ `0`·`stderr` `0` B）

🔴 機械停機款 **`0`** 項 ✅；🟡 **`1`** 項逐項處置：`P-5` 本單⛔ 載 `SELF_SHA256` ⇒ **具名豁免**
（三端 `sha256` 鏈承擔·體例同補令六段／七段／本窗前段）。
ℹ️ `P-3` 記錄：`app.py` 之 `def main` ＝ `14917`-`24497`——本單之主閘受詞⛔ 落於其內。

---

## `§一`　補令八 `§一`／`§三` 之**態已推進**（照實具名·`常規六 四`·⛔ 頂替）

🩸 **補令八 `§一` 之表所載「側支 ＝ 主線 ＋ **`5`** commit」係其撰寫時之態**
（其 `§零` 逐字載側支 ＝ `c8c1c9ff9acc7429df4ef8ed2e1125dc9cdb4e09`）。
**現態（`ls-remote` 實查）＝ `a67f9d7679ba56df5c332a49e969778cb50aae03`·側支 ＝ 主線 ＋ **`8`** commit**
——其差之 `3` commit ＝ `7fb9c2b`（交接註·前窗收工令）／`4dc3565`（本窗工項零）／`a67f9d7`（本窗報告），
**三者皆零生產碼**。

🔒 **`§一` 之七項判於現態**逐項復算**皆仍成立**：

| 項 | 現態實測 |
|---|---|
| 主線 | `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2`·**未前進** ✅ |
| 快轉 | 主線為側支之祖先（`merge-base --is-ancestor` ⇒ 真）✅ |
| `app.py` blob | `c5c28b826aa25f4f12a3bd5bdc1fcd0587ab021f` ＝ **`8f3dccc` 之內容**·逐位相同 ✅ |
| `verify/stepg_pipeline.py` blob | `ee5d82b2138cb2599809d96d5006db3788675794` ＝ **`8f3dccc` 之內容**·逐位相同 ✅ |
| 生產碼 `34` 檔相異（vs 主線） | **恰 `2`**（即上二檔）✅ |
| 既有檔 `deletions` | 本窗二 commit 皆 `0`（純新增檔）✅ |
| 逐 commit 之生產碼判 | **`8` commit 中唯 `8f3dccc` 為 🔴 生產碼**（命中 `2`）；**餘 `7` 皆 `0`** ✅ |

### `一-1`　🟢 **補令八 `§三` 之失效條款——未觸發**

其逐字 ＝ 「併線前若 `app.py` 或 `verify/stepg_pipeline.py` 之 blob 與 `§一` 所載相異 ⇒ **停機上呈**，
KL 之放行**不及於**該情形。」
⇒ 二檔之 blob 於現態與 `§一` 所載**逐位相同**（見上表）⇒ **⛔ 觸發** ✅。
🛑 **本判須於 KL 放行、快轉併線之<u>當下</u>再行復算一次**（其為**併線前**之條件·⛔ 以本次之綠充之）。

---

## `§二`　`§二-3` 立準之落地（**本窗前段已施行**·⛔ 追溯宣稱）

**立準逐字（補令八 `§二-3`·⛔ 改寫）**

> **凡出艙一「態」之宣告，須<u>二值並報</u>：`git rev-parse HEAD:<檔>`（committed blob）⋀
> `git hash-object <檔>`（工作區），**並以後者為實**。**
> **二值相異即為「工作區與 `HEAD` 不同步」之告警 ⇒ 須<u>具名</u>其相異與其由，⛔ 靜默取其一。**

🔒 **本窗前段之施行**（⛔ 事後補記）：`probe_WG9269_c2_owner.py` 之 `_state()` **內建**該二值並報並逐跑印其判
（見本報告 `§三-3`）——主 worktree `1d7d7c73…`／拋棄式 clone（`c1` 態）`c5c28b82…`，**二態皆得「相同」**。
🔒 其與 `(乙)` 之關係：本窗之二態係**二棵樹之二個行程**（⛔ `git checkout -- <檔>` 之不 commit 切態）
⇒ 恰為 `§二-2` `(乙)` 所稱「二者本為不同 commit」之形；`(丙)` 之硬款亦已同時滿足。
🛑 **`c2` 之報告仍須依 `(丙)` 二值並報**——本窗之施行**⛔ 免除** `c2` 之該義務。

---

## `§三`　🩸 **二項懸引之現查**（照實回報·⛔ 編造·⛔ 以倉內近似物代替）

### `三-1`　`W-G.9-269 補令八` —— **已送達並入倉**（本窗前段之上呈項 `2` **就此結清**）

本窗前段（`a67f9d7`·`§五-2` 項 `2`）曾照實回報「`W-G.9-269 補令八` 於三 `ref` 皆⛔ 存在」。
**KL 已於其後交付該件**（見 `§零`）⇒ 🔒 **該上呈項<u>結清</u>**：其屬「**確曾出單而未遞交**」，
**⛔ 誤引**——與 `W-G.9-222` 工項四 `a` 之 `219` **同形**（「CC 側無痕跡係**應然**」）。
🔒 **本窗前段之量測本身⛔ 誤**（三 `ref` 之 `docs/orders` 正面列舉為真）；**誤在其若逕推「未出單」**
——本窗前段**已照實標為「⛔ CC 可判」並請確認**，⇒ 其處置**與 `W-G.9-222` 之通則一致**。
🔒 **併記**：本窗前段依單所**自載**之三前置辦竣（`§三`），與補令八 `§四` 之三項**逐項對應**
（見下 `§四`）⇒ **⛔ 因該件遲到而有任何款未辦**。

### `三-2`　🔴 **坑 `m`（補令八 `§二-3` 所承）於全倉⛔ 存在**

**三軸**：`(1)` 來源 ＝ `a67f9d7679ba56df5c332a49e969778cb50aae03` ⋀
`05a11bd665f6c2790ff821c9e3a1606651e345d4`（**二 `ref` 並查**）／
`(2)` 母體 ＝ 各該 `ref` 之**全追蹤 `.md`**（`704` 檔／`702` 檔·`git ls-tree -r --name-only -z` ＋ Python 解碼·
⛔ CJK `quotepath`）／`(3)` 粒度框 ＝ **字元框**（`str.count`）。
**框**：`坑 ␣ ` ＋ 反引號 ＋ 單一字母 ＋ 反引號（**於 Python 內以 `chr(96)` 組出**·⛔ 經 bash 之反引號層·`坑 7`）。

| 受詞／對照 | `a67f9d7` 檔／命中 | `05a11bd` 檔／命中 | 判 |
|---|---|---|---|
| 坑 `j` | `0`／`0` | `0`／`0` | 🔴 **⛔ 存在** |
| 坑 `k` | **`1`／`1`** | `0`／`0` | 🟡 **僅被引、⛔ 被定義**（其唯一命中即 `補令六段 §甲` 坑 `x` 之「**坑 `k` 之同族**」一語） |
| 坑 `l` | `0`／`0` | `0`／`0` | 🔴 **⛔ 存在** |
| **坑 `m`** | **`0`／`0`** | **`0`／`0`** | 🔴 **⛔ 存在** |
| 對照組[必非零] 坑 `n` | **`2`／`4`** | `0`／`0` | ✅ **器非紅**（於 `a67f9d7` 非零） |
| 對照組[必非零] 坑 `x` | **`1`／`1`** | `0`／`0` | ✅ **器非紅** |

🩸 **本表之對照組曾一度<u>全零</u>**（首版取「坑 `i`」為必非零之造）⇒ 依 `坑 u` **先判量測器紅**，
繼而查因：`前置階 §丁` 之坑係**表列形**（`| \`i\` | …`）、其**⛔ 冠「坑」字** ⇒ 該造**結構上必為零**
——**係造之選擇有誤、⛔ 器紅**。改取坑 `n`／`x`（**標題形 `### 坑 \`n\``**）為造後即非零。
🔒 **併記（母體之別·`坑 d`）**：凍存側支 `05a11bd` 之三造**皆零**——其**⛔ 器紅**，
而係該 `ref` **尚無** `補令六段`（坑 `n`〜`z` 之載體）⇒ **母體不同而數相異，二者皆正確**。

🔒 **其後果 ＝ 無**：補令八 `§二-3` 之立準**逐字寫於單內**（見 `§二`）⇒ **內容⛔ 滅失**，
本窗**⛔ 據該引註推論任何未載之款**。
🛑 **是否補鑄坑 `j`〜`m`（或更正該二處引註）＝ 意思決定 ⇒ 候發單側**；
**本段⛔ 就此作成任何條文、⛔ 鑄任何號**（補令八 `§五` 逐字：本補令⛔ 授任何鑄號）。

---

## `§四`　補令八 `§四` 三前置 × 本窗前段之辦竣（**逐項對應**）

| 補令八 `§四` | 本窗前段之落點 | 判 |
|---|---|---|
| `1` 級聯剔除逐宗載明（`0m` `3` 宗／`3.5m` `1` 宗·二類⛔ 併·⛔ 以「受詞二宗」呈） | 本報告 `§三-1` 之表（**逐 (宗, 情境)** `6` 格·二類分列） | ✅ **已辦竣** |
| `2` `628-27(1)` 之歸戶須於**管線跑畢之態**取之並具名 | 本報告 `§三-2`：**`G030`**（`rv.build_ownership` 跑畢後取 `t8_ownership_map` ＋ 生產碼 `_resolve_ownership`） | ✅ **已辦竣** |
| `3` 態宣告二值並報（`§二-3`） | 本報告 `§三-3` ＋ 本段 `§二` | ✅ **已辦竣** |

🔒 **`§四` 款 `2` 所載之二數**（`a 面積 51.32 ㎡`／原分配 `G 29.26 ㎡`）與本窗**當次實跑**之值
**逐值相符**（`0m`·見 `§三-1`）⇒ 其**⛔ 取自 baseline**（`GB-162`）。

---

## `§五`　自限復驗（補令八段）＋ 續辦

| 項 | 實測 |
|---|---|
| 主線⛔ 前進 | 期初 ＝ 期末 ＝ `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2`（`ls-remote` 實查） |
| 凍存側支⛔ 動 | `05a11bd665f6c2790ff821c9e3a1606651e345d4` |
| 生產碼⛔ 動一字 | 本段之 commit 之生產碼判 ＝ 命中 `0` 行；`34` 檔 blob 相對基座相異 ＝ `0` |
| ⛔ 鑄任何號（補令八 `§五`） | 四簿三數 期初 ＝ 期末（本段⛔ 觸四簿母體任一檔） |
| `verify/baselines` ⛔ 觸（`GB-162`） | 樹 ＝ `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`·期初 ＝ 期末 |
| ⛔ 併線 | 主線未前進；本段**⛔ 作成任何併線動作或其判斷** |
| `deletions` | 本段對既有檔之改動 ＝ **純末端追加**（本報告）·`deletions 0` |

🛑 **續辦（補令八 `§六`）**：序 `1` ✅ 本段辦竣 ⇒ **序 `2` ＝ 「KL 放行後 ⇒ 快轉併線」**
——🔒 **KL 尚未放行** ⇒ 本窗**⛔ 得併線、⛔ 得自行判斷已放行**。
🔒 **併線之當下須復算**：`ls-remote` 實查主線仍為 `0aa608e` ⋀ 二檔 blob 仍為 `8f3dccc` 之內容
（`§三` 失效條款）。
🔒 **序 `3`（`c2`）之三前置已備妥**（`§四`）⇒ 併線令一到即可起 `c2`。

---

# `W-G.9-269R`　**補令九段**（🟢 `c1` 已併線 ／ `§二-3` **索引修復** ／ 二框之誤各一）

> **落點 ＝ 主線**（補令九 `§五` 序 `3` 逐字·零生產碼·得逕推）。
> **基座** ＝ `2f1292748d1326f1590ae20d5d18dd8f5364af5b`（`ls-remote` 實查）。
> 🛒 **本段⛔ 載**：`c2` 之任何修法主張；旗標預設之變更；受影響三宗之配地圖或負擔表（`GB-159`）；
> 任何自 `verify/baselines` 取得之數（`GB-162`）。

---

## `§零`　工項零 ＋ `S-0b`

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付·主 checkout 之工作區·該處⛔ 追蹤） | `679ad9c60f171999d9933f6b093999d53b645dee759f1b1672c3898ce58a7dc9` | `7308` | `0` | `102` |
| 落檔（**二進位拷貝**·⛔ 文字層讀寫·`坑 a`） | **同** | `7308` | `0` | `102` |
| **blob** | **同** | `7308` | — | — |

`S-0b`（`rc` **直取** ＝ `0`·`stderr` `0` B）：🔴 機械停機款 **`0`** ✅；🟡 **`2`** 項逐項處置——

| 項 | 處置 |
|---|---|
| `P-5` 本單⛔ 載 `SELF_SHA256` | **具名豁免**（三端 `sha256` 鏈承擔） |
| `P-4` `:91`「\| 序 \| 事 \| 落點 \|」 | 🟡 **偽陽·具名豁免**——其為 `§五` **續辦表之表頭**，⛔ 方向性轉引之量 |

### 🛑 執行序之偏離（`常規二`·逐字回報·⛔ 追改單一字）

開場文令「**索引修復先於開工閘、先於任何工項**」，而補令九 `§五` 序 `1` ＝ 工項零、序 `3` ＝ 索引修復。
🔒 **二者⛔ 得同時<u>照字面</u>滿足**——`§二-3` 之落地若先於併線，將使側支 tip 由 `2f12927` 前進，
而 `§一` 逐字令「側支 tip **仍為** `2f12927…`；**任一相異 ⇒ 停機上呈**」⇒ **索引修復之落地會自行觸發併線之停機款**。
🔒 **所取（保守項）**：**索引修復之<u>執行</u>（量測與判）確為本窗第一件事**，其**落地**則置於工項零之後
——開場文之 operative 禁令逐字為「**在索引逐則可解析之前，⛔ 執行『逐坑覆述』**」，
🔒 **本段⛔ 有任何逐坑覆述**，該禁令**完全滿足**。

---

## `§一`　🟢 `c1` **已併線**——`§一` 之推進條件於本窗**已不成立**（照實·⛔ 逕併）

🔴 **實查（`ls-remote`·本窗第一動作）**：**主線 `wip/s1-endpart` ＝ `2f1292748d1326f1590ae20d5d18dd8f5364af5b`**
——**⛔ 補令九 `§一` 所載之 `0aa608e…`**。

🛑 依 `§一` 逐字「推進前須以 `git ls-remote` 實查主線仍為 `0aa608e…` ⋀ 側支 tip 仍為 `2f12927…`；
**任一相異 ⇒ 停機上呈，⛔ 逕併**」⇒ 🔒 **本窗⛔ 執行任何併線動作**，並照實上呈其相異。

🔒 **相異之成因已機械判明 ＝ 併線<u>已完成</u>**（⛔ 非預期外之推進）：

| 判 | 實測 |
|---|---|
| 主線 ＝ 側支 tip？ | 二者皆 `2f1292748d1326f1590ae20d5d18dd8f5364af5b` ⇒ **相同** |
| `0aa608e` → `2f12927` 為快轉？ | `merge-base --is-ancestor` ⇒ **真** |
| 新增 commit 數 | **`9`**（＝ `§一` 表所載之 `9`·逐值相符） |
| 主線 vs `0aa608e` 之生產碼 `34` 檔相異 | **恰 `2`** ＝ `app.py` ⋀ `verify/stepg_pipeline.py` |
| **失效條款**（二檔 blob 須為 `8f3dccc` 之內容） | `app.py` `c5c28b826aa25f4f…` ⋀ `verify/stepg_pipeline.py` `ee5d82b2138cb259…` ⇒ **逐位相同** ✅ |

⇒ 🔒 **主線之現態 ＝ `§一` 所授權之物件本身**，且**失效條款於該態⛔ 觸發**。
🛑 **本段⛔ 判「併線由誰執行」**（本窗⛔ 為之·倉內亦無其他佐證）——**照實登記其為<u>已完成</u>**，
其執行者之具名**候發單側**。
🔒 **本窗之 worktree `HEAD` 仍為 `0aa608e`**（`left-right` vs 併線前之主線 ＝ `0	0`）
⇒ **⛔ 以工作副本為態之來源**；本段之一切倉態判**一律 `git show <rev>:<path>`**（`harness-worktree-crlf-trap`）。

---

## `§二`　`§二-3` **索引修復**（款 `1`〜`5` 逐款）

### `二-1`　款 `1`：全倉之「坑」索引（**二系分列**·框具名·附判別力）

🔒 **器** ＝ `verify/probes/probe_WG9269_pit_index.py`（**本段新建**·零生產碼·`GB-161` 自檢：
⛔ 硬編他樹·其定位全自 `git` 之 `rev` 參數）。**落檔** ＝ `verify/out/WG9269R_pitindex_main.log`。
🔒 **三軸**：`(1)` 來源 ＝ `2f1292748d1326f1590ae20d5d18dd8f5364af5b`／
`(2)` 母體 ＝ 該 `rev` 之**全追蹤 `.md`** **`705`** 檔（`git ls-tree -r --name-only -z` ＋ Python 解碼·⛔ CJK `quotepath`）／
`(3)` 粒度框 ＝ **列框**。

🔒 **框（逐字·款 `1` 所令）**

| 類 | 逐字 |
|---|---|
| **定義處 `D-H`**（標題形） | 列首 `#{1,6}` … 含 `坑` … 標籤；🔴 **且其自該標題至<u>次一同級或更高級標題</u>之間，`形`／`徵候`／`攔法` 三字樣須齊備**——否則歸**引用** |
| **定義處 `D-T`**（坑表之資料列） | 一 markdown 表，其**表頭列**同時含 `徵候` ⋀ `攔法` ⇒ 其分隔列以下各資料列之**首格**（去反引號／星號／空白／前導 `坑` 字）即標籤；且該列之 `徵候` ⋀ `攔法` 格須**非空** |
| **引用處** | 文中 `坑` ＋ 標籤（**帶／不帶反引號二形並取**·`GB-158`），且**⛔ 落於任一定義列** |

🔑 **判別力二造（款 `1` 所令·先跑先判·`坑 u`）**

| 造 | 受詞 | 得 | 期 | 判 |
|---|---|---|---|---|
| 甲［必有定義］ | 坑 `n` | 定義處 **`4`** | `≥ 1` | ✅ |
| 乙［必為零］ | 一人造之坑（**執行期組出**·其字面⛔ 出艙·`GB-147`） | 定義 **`0`**／引用 **`0`** | `0`／`0` | ✅ |

⇒ **器非紅** ✅。

🔒 **索引之結果（全表逐則見落檔）**

| 系 | 標籤數 | **定義處 ＝ `0` 者** | 三項齊備者 | 判 |
|---|---|---|---|---|
| 數字系 `1`〜`26` | `26` | **`0` 個** | **`26`／`26`** | ✅ **全數可解析** |
| 字母系 `a`〜`x` | `24` | **`0` 個** | **`24`／`24`** | ✅ **全數可解析** |

🔒 **⇒ 款 `2`（定義處為 `0` 者逐則判三態）之受詞 ＝ <u>空集</u>**；款 `3` 之停機條件**⛔ 觸發**。

### `二-2`　款 `2`〜`3`：三態之判

**受詞為空** ⇒ ⛔ 得判甲／乙／丙；**亦⛔ 有「三態皆判不出」之情形** ⇒ **⛔ 停機**。
🛑 **本段⛔ 猜任何坑之內容、⛔ 略過任何坑、⛔ 以「大概是某某」填之**（款 `3`）。

### `二-3`　🔴 **二框之誤各一**（補令二 `§三`：**先對齊母體再判孰誤**·⛔ 逕判他方有誤）

**二框對照**（同一母體 `705` 檔·同一 `rev`·**列框**·逐值見落檔）

| 框（逐字） | 數字系 合計／得 `0` 者 | 字母系 合計／得 `0` 者 |
|---|---|---|
| **補令九 `§二-1` 所載**：`#### 坑 X` ∪ `\| **坑 X**` | `0`／**全部 `26` 個** | `1`／**`23` 個**（唯 `m` ＝ `1`） |
| **前窗（`§三-2`）所用**：字串 `坑 ` ＋ 標籤 | `129`／（無） | `38`／`c, e, f, g, h` |
| **本段所用**（`D-H` 三項齊備 ∪ `D-T`） | — ／**（無）** | — ／**（無）** |

#### `(甲)` 🩸 **前窗之框之誤（本窗自捕·責 ＝ CC）**

前窗 `§三-2` 出艙「坑 `m`／`j`／`l` 於全倉**⛔ 存在**；坑 `k` 僅被引而⛔ 被定義」。
🔴 **該四判<u>皆偽</u>**——四者**皆有定義處且三項齊備**：

| 坑 | 定義處（`檔:列`） | 形（首句節略） |
|---|---|---|
| `j` | `docs/reports/W-G.9-269R_本體階_補令六段.md:124`（`D-T`） | **號之簿態隨 `ref` 而異** |
| `k` | 同檔 `:125`（`D-T`） | **證據落於未併線之側支** |
| `l` | 同檔 `:126`（`D-T`） | **「重烤」之受詞係應然值** |
| **`m`** | 同檔 **`:467`**（`D-T`） | **量測器之態宣告取 `HEAD` 之 blob 而⛔ 工作區** |

🔒 **成因（機械·⛔ 臆造）**：該四則之定義皆為**坑表之資料列**，其**標籤格逐字為 `` `j` ``／`` `k` ``／
`` `l` ``／`` `m` ``——**⛔ 冠「坑」字**（「坑」字在**表頭**）⇒ **字串框 `坑 ` ＋ 標籤結構上必失配**。
🩸 **本誤之加重情狀**：前窗**於同一批內已診出該形**（其 `§三-2` 逐字載「`前置階 §丁` 之坑係**表列形**
（`| \`i\` |`·⛔ 冠「坑」字）⇒ 該造**結構上必為零**」），**卻未將同一診斷施於受詞 `j`／`k`／`l`／`m`**
——**只把它當成「對照組選錯」，而未回頭問「受詞是否也被同一個框漏掉」**。
⇒ 🔒 **恰為 `CLAUDE.md`「立戒者在下一批最容易違反自己的戒」之形**，且係**同一批內**、⛔ 次批。
🔒 **通則（承前窗所立而<u>擴其射程</u>）**：**凡發現某框對<u>對照組</u>結構上必失配者，
須<u>即刻</u>以同一判準重驗其<u>受詞</u>**——⛔ 僅換對照組。

#### `(乙)` 🩸 **補令九 `§二-1` 之框之誤（發單側·照實·⛔ 判其有誤前先對齊母體）**

以其**逐字所載之框**施於現態母體：
- **`a`〜`i` ＝ `0`** ⇒ 🔒 **與其表所載之「`a`〜`i` 定義 `0`」<u>可復現</u>**；
- **`j`〜`x` ＝ `1`〜`2`** ⇒ 🔴 **⛔ 可復現**：實得**唯 `m` ＝ `1`，餘 `23` 個皆 `0`**。
🔒 **且該唯一之 `1` 係<u>偽陽</u>**——其命中為
`docs/reports/W-G.9-269R_本體階_新窗開工與c2前置.md:491` 之一列，逐字 ＝
`| **坑 \`m\`** | **\`0\`／\`0\`** | **\`0\`／\`0\`** | 🔴 **⛔ 存在** |`
——**其為前窗<u>斷言該坑不存在</u>之表列**，⛔ 其定義。
⇒ 🔒 **`GB-132` 族之教科書例**：**一句「某坑不存在」之敘述，被框讀成「該坑之定義」**。
🛑 **本段⛔ 判發單側之數為誤**——依補令二 `§三`，二方之數相異時須先對齊母體與框；
**本段所報者為「以其<u>逐字所載之框</u>施於現態母體之實測」**，其與該表相異之部分
（`j`〜`x` 一列）**具名為⛔ 可復現**，其成因（框之別／母體之別／時點之別）**候發單側**。

### `二-4`　款 `4`〜`5`

🔒 款 `4`：**本段⛔ 追改任何既有報告之內文一字**——上開二誤之更正**一律以本末端追加之節承載**
（前窗 `§三-2` 之表**原文一字不動**；`W-G.9-269_補令八` 之 `§二-3` 標題亦**一字不動**）。
🔒 款 `5`：**索引已逐則可解析** ⇒ **新窗自本節起得依開場文「逐坑覆述」**；
判為 `(乙)`（⛔ 復現）者 ＝ **無** ⇒ ⛔ 須以「覆述其登記」代之。
🛑 **本段自身⛔ 行任何逐坑覆述**（其屬開場文之後續工項·⛔ 本段之受詞）。

### `二-5`　款 `1` 所令之「引用處逐筆」

🔒 **定義處為 `0` 而有引用者 ＝ 無** ⇒ 該逐筆清單之受詞為空（器於該情形會逐筆印出·本次未印）。
🔒 **全部 `50` 個標籤之引用處逐筆**，見落檔 `verify/out/WG9269R_pitindex_main.log` 之二表
（每則載其**定義處 `檔:列`**、**三項齊備之逐項判**、**引用處筆數**）。

---

## `§三`　補令九 `§二-4` 之立準（**入交接註**·逐字·⛔ 改寫）

> **交接註之「坑」係後窗唯一之攔法索引 ⇒ 其<u>每一個引用皆須可解析至一個定義處</u>。**
> **立一坑即須同時具名其 `形／徵候／攔法` 並落於倉內；⛔ 只在對話側稱「已入交接註」——該稱⛔ 為入倉之證。**
> **爾後每一次收工，交接註之末須附<u>本波所立之坑之定義處清單</u>，並自證其逐則可解析。**

🔒 **本波所立之坑之定義處清單（`W-G.9-269` 波·自證逐則可解析）**

| 坑 | 定義處（`檔:列`·`rev` ＝ `2f12927`） | 三項 |
|---|---|---|
| `a`〜`i`（九則） | `docs/reports/W-G.9-269R_前置階.md:865` 起之坑表（`D-T`） | ✅✅✅ |
| `j`／`k`／`l` | `docs/reports/W-G.9-269R_本體階_補令六段.md:124`／`:125`／`:126`（`D-T`） | ✅✅✅ |
| `m` | 同檔 `:467`（`D-T`） | ✅✅✅ |
| `n`〜`z`（`n` 起·`w` 含八則子目） | 同檔 `§甲` 之 `### 坑 \`X\``（`D-H`）＋ 本報告 `§二-5` 之覆述表（`D-T`） | ✅✅✅ |

🔒 **自證**：上表所列**全數**於 `二-1` 之索引中判為 **✅ 可解析**；**⛔ 一則為 `0`**。
🩸 **併記（`GB-132`·自指增長）**：本節列出定義處之行為**自身**會增加各該標籤之「引用處」計數
⇒ 凡後續以本索引之**引用數**為量者，須自扣本節之增量；**「定義處 ≥ 1」之判⛔ 受影響**。

---

## `§四`　自限復驗（補令九段）

| 項 | 實測 |
|---|---|
| ⛔ 併線 | **本窗⛔ 執行任何併線動作**；主線於本段落地前 ＝ `2f12927`（`ls-remote` 實查·`§一`） |
| ⛔ 動生產碼一字 | 逐 commit 之生產碼判 ＝ 命中 `0` 行；`34` 檔 blob 相對基座相異 ＝ `0` |
| ⛔ 鑄任何號（補令九 `§四`） | 四簿三數 期初 ＝ 期末（本段⛔ 觸四簿母體任一檔） |
| ⛔ 翻旗標之預設 | 本段⛔ 觸 `app.py`；`WG9269_K917_BACKFILL` 預設仍 `"0"` |
| `verify/baselines` ⛔ 觸（`GB-162`） | 樹 期初 ＝ 期末 |
| ⛔ 追改既有報告內文（款 `4`） | 本段對既有檔之改動 ＝ **純末端追加**·`deletions 0`·嚴格前綴 ✅ |
| ⛔ 逐坑覆述（開場文） | **本段⛔ 有任何逐坑覆述** |
| `rc` 直取·`stderr` `0` B | 索引器 `RC=0`／`stderr` `0` B；`S-0b` `rc = 0`／`stderr` `0` B |

---

## `§五`　🛑 呈發單側（**三項**·⛔ CC 可裁）

| # | 事 | 判 |
|---|---|---|
| `1` | **`c1` 已併線**（主線 ＝ `2f12927` ≠ `§一` 所載之 `0aa608e`）⇒ `§一` 之停機款依其字面**觸發** | 🔒 已照實上呈（`§一`）；成因判明為**併線已完成**且**失效條款⛔ 觸發**；**其執行者之具名候發單側**。🛑 本窗**⛔ 逕併、⛔ 判其為已授權之外** |
| `2` | 🩸 **前窗之框之誤**（坑 `j`／`k`／`l`／`m` 之「查無」為**偽陰**）——責 ＝ **CC** | 🔒 已於 `§二-3 (甲)` 具名並更正（末端追加·⛔ 追改原文）。**是否鑄號**（其為 CC 側之誤·⛔ `GB`）⇒ **候發單側**（補令九 `§四`：⛔ 授任何鑄號） |
| `3` | 🩸 **補令九 `§二-1` 之框，其 `j`〜`x` ＝ `1`〜`2` 一列⛔ 可復現**（實得唯 `m` ＝ `1` 且係**偽陽**·其命中為前窗之**否定斷言列**） | 🔒 已於 `§二-3 (乙)` 具名；**⛔ 判其為誤**（母體／時點未對齊之可能未排除）⇒ **候發單側** |

🔒 **續辦（補令九 `§五`）**：序 `1` ✅／序 `2` ＝ **已完成**（`§一`）／序 `3` ✅ 本段辦竣
⇒ **次為序 `4` `c2`**（🔴 旗標 `on`·**有土地後果**·**新側支**·**單獨呈 KL** ＋ 補令八 `§四` 三前置）。
🔒 **補令八 `§四` 三前置已備妥**（本報告 `§三`／`§四`·`628-27(1)` 之歸戶 ＝ `G030`）。
🛑 **`c2` 須 KL <u>單獨</u>放行**——補令九 `§一` 逐字：KL 之裁「**⛔ 及於** `c2`（旗標預設之翻轉）」。

---

# `W-G.9-269R`　**補令九段（續）**：逐坑覆述之**補遺** ＋ 起手清單於主線現態之重查

> **落點 ＝ 主線**（補令九 `§五` 序 `3` 之收尾·零生產碼·得逕推）。
> **基座** ＝ `8cf38b33114fac0cae531d60b8332f96675d09ba`（`ls-remote` 實查）。
> 🔒 **本節之受詞** ＝ 補令九 `§二-3` 款 `5` 所**解鎖**者：「**修畢後，新窗方得依開場文『逐坑覆述』**」。
> 🛒 **本節⛔ 載**：`c2` 之任何修法主張；旗標預設之變更；`GB-159`／`GB-162` 所禁之任何物。

---

## `§零`　態（**⛔ 以工作副本為態之來源**）

| 項 | 實測（`ls-remote` 實查） |
|---|---|
| 主線 `wip/s1-endpart` | **`8cf38b33114fac0cae531d60b8332f96675d09ba`** |
| `verify/W-G.9-269-c1` | `2f1292748d1326f1590ae20d5d18dd8f5364af5b`（**已併入主線**） |
| `verify/W-G.9-269-p3a` | `05a11bd665f6c2790ff821c9e3a1606651e345d4`（🛑 凍存） |
| **`left-right`**（本 worktree `HEAD` `...` 主線） | 🔴 **`0	11`**——本 worktree `HEAD` 仍為 `0aa608e`、**落後 `11` commit** |

🛑 **⇒ 本節之一切倉態判<u>一律</u>以 `git show <rev>:<path>` 為之**，**⛔ 讀工作副本**
（`harness-worktree-crlf-trap` 之「`left-right ≠ 0 0` ⇒ 工作區為錯態」）。
🔒 **本節所落之新檔⛔ 受此影響**（其為本窗新造之物、⛔ 自工作副本之舊態衍生）。
🔒 **補令九 `§一` 末款已生效**：併線完成 ⇒ 「主線⛔ 前進」之限制**解除**，至下一側支建立為止
⇒ 本節得**逕推主線**。

---

## `§一`　起手清單八項——**於主線現態之重查**（⛔ 沿用前段之值·補令九 `§三` 裁 `1` 之立準）

🔒 **立準（補令九 `§三` 裁 `1` 逐字）**：「凡單所載之『commit 數／檔數／列數』皆為**撰寫時之態**，
執行時一律**當下重算**，⛔ 沿用；重算相異即照實出艙其時點。」

| # | 項 | **主線現態 `8cf38b3` 之實測** | 與前段（`0aa608e` 時）之別 |
|---|---|---|---|
| `1` | 三 `ref` | 見 `§零` | 🔴 **主線已由 `0aa608e` 前進至 `8cf38b3`**（`c1` 併線 ＋ 本窗二 commit） |
| `2` | `left-right` | 🔴 **`0	11`**（⛔ `0	0`） | 前段為 `0	0` |
| `3` | 四簿（框 ＝ **標題形 ∪ 表列形**·母體 ＝ 各該單檔·列框） | 自誤 `351`／**`361`**／`[106, 355, 356, 357]`；`GB` `160`／**`162`**／`[12, 87]`；`VR` `80`／**`95`**／`[73, 75]`；`K-9` `28`／**`29`**／`[]` | **⛔ 變**（併線後主線 ＝ 原 `c1` 之簿態）⇒ 次號 自誤 **`362`**／`GB` **`163`**／`VR` **`96`**／`K-9` **`30`** |
| `4` | 生產碼 `34` 檔（**正面列舉**） | **`34`** 檔；`app.py` ＝ `c5c28b826aa25f4f…`／`verify/stepg_pipeline.py` ＝ `ee5d82b2138cb259…` | 🔴 **二檔已為 `c1` 之內容**（前段主線為 `1d7d7c73…`／`13e74a9f…`） |
| `5` | 二檔是否仍為 `8f3dccc` 之內容 | **二檔皆逐位相同** ✅ | ⛔ 變 |
| `6` | `P-1` 基準器 blob | `07d8627e5e4c797f1278999e40f306acce8c01f6` | 🔴 **已在主線**（前段主線⛔ 存在該檔） |
| `7` | 四 `GB` | `GB-159`／`160`／`161`／`162` 之標題形**逐則命中**（`W-G.4_…md:6042`／`:6069`／`:6090`／`:6408`） | ⛔ 變·**四則皆仍有效** |
| `8` | 旗標預設須仍為 `off` | 主線 `app.py:9292` 逐字 `return str(_os_k917.environ.get(K917_BACKFILL_ENV, "0")).strip().lower() \` ⇒ **`"0"` ＝ `off`** ✅ | 🔴 **其受詞已自側支移入主線**——`c2` 之翻轉自此**直接動主線之生產碼** |

---

## `§二`　🔑 **逐坑覆述之補遺：坑 `j`／`k`／`l`／`m`**（前段**漏覆述**之四則）

🩸 **何以為補遺**：前段（`§二-4`／`§二-5`）覆述了 `1`〜`26`、`a`〜`i`、`n`〜`x`，
**獨缺 `j`／`k`／`l`／`m`**——其因即本報告 `§二-3 (甲)` 所具名之**框之誤**
（前段誤判該四則「查無」而未覆述）。索引既已可解析（`§二-1`），**本節補之**。
🛑 **以下四則之 `形／徵候／攔法` 係<u>自其定義處逐字取得</u>**（`git show <rev>:<path>` ＋ 行號）
——**⛔ 憑記憶、⛔ 猜、⛔ 以「大概是某某」填之**（補令九 `§二-3` 款 `3`）。

### 坑 `j`　**號之簿態隨 `ref` 而異**
> **定義處** ＝ `docs/reports/W-G.9-269R_本體階_補令六段.md:124`（`D-T`·`rev` `8cf38b3`）

- **形**：**號之簿態隨 `ref` 而異**
- **徵候**：同一號於主線「未占用」而於側支「已鑄」——**單方 `rev` 之取號現查⛔ 足以判「未占用」**
- **攔法**：凡側支**⛔ 併線**而其上曾鑄號者，取號現查須**二 `rev` 並查**並出艙其**差集**；
  主線須**登記其缺口**，否則「缺號永不重用」之保護**⛔ 生效**（其繫於缺口被記錄）
- **本窗之施行**：前段之取號現查即**三 `ref` 並查**（主線／`c1`／凍存 `p3a`）並逐簿出艙其差集
  （自誤 `358` vs `361` vs `357`·缺號集 `[106, 355, 356, 357]` vs `[106]`）。
  🔒 **本節復於併線後重算**（`§一` 項 `3`）：主線已吸收 `c1` 之簿態 ⇒ 自誤 MAX `361`、缺口登記**仍在**。
  🛑 本窗**⛔ 取任何號**（補令九 `§四`：⛔ 授任何鑄號）。

### 坑 `k`　**證據落於未併線之側支**
> **定義處** ＝ 同檔 `:125`（`D-T`）

- **形**：**證據落於未併線之側支**
- **徵候**：`GB-162` 之機械依據（三端對拍 `1137` 格）其落檔**全在側支**
  ⇒ 自主線 `git show` **⛔ 可達**
- **攔法**：凡登記項之證據落於未併線之側支者，**須於該登記內逐字附側支名與 `commit`**，
  ⛔ 只寫落檔路徑
- **本窗之施行**：前段引 `P-1` 基準器之「主線⛔ 存在該檔」時，**逐字附**其側支名與 `commit`；
  本報告 `§二-3` 引凍存側支之量測時亦逐字附 `verify/W-G.9-269-p3a` ＝ `05a11bd…`。
  🔒 **本節新得之事實**：`c1` 併線後，`P-1` 器**已在主線**（`§一` 項 `6`）
  ⇒ **該支之「⛔ 可達」已解除**；惟 **`GB-162` 之證據仍在 `p3a`**（凍存·⛔ 併線）⇒ **本坑對其仍全效**。

### 坑 `l`　**「重烤」之受詞係<u>應然值</u>**
> **定義處** ＝ 同檔 `:126`（`D-T`）

- **形**：**「重烤」之受詞係應然值**
- **徵候**：`baseline` 為 `diff_rows` 之比較端；本分支之 `40` 紅其內容即該 `1137` 格
  ⇒ 重烤 ＝ **一次抹去缺陷證據**
- **攔法**：見補令六段 `二-1` 之立準；併 **`GB-162` 之三禁令**
- **本窗之施行**：本窗**⛔ 設 `WV_BAKE` 任何值**、**⛔ 觸 `verify/baselines` 一位元組**
  （其樹 `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`·**逐批期初 ＝ 期末**）；
  一切出艙之數**皆自當次實跑取得**（`628-27(1)` 之 `G030`／`51.32`／`29.26` 亦然）。

### 坑 `m`　**量測器之態宣告取 `HEAD` 之 blob 而⛔ 工作區**
> **定義處** ＝ 同檔 **`:467`**（`D-T`）　🔒 **即補令八 `§二-3`「承 CC 之坑 `m`」所指者**

- **形**：**量測器之態宣告取 `HEAD` 之 blob 而⛔ 工作區**
- **徵候**：以 `git checkout <rev> -- <檔>` 切態者，其 `git rev-parse HEAD:<檔>` **恆不變**
  ⇒ 二次量測之態宣告**相同**，而其實際執行之碼**相異**——**⛔ 任何錯誤訊息**
- **攔法**：態宣告須**二值並報**：`git rev-parse HEAD:<檔>`（**committed**）⋀
  `git hash-object <檔>`（**工作區·即實際執行者**）；**以後者為實**。
  凡「以證二者確為不同態」之自證，一律綁**後者**
- **本窗之施行**：`verify/probes/probe_WG9269_c2_owner.py` 之 `_state()` **內建**該二值並報並逐跑印其判；
  二態係**二棵樹之二個行程**（主 worktree ／ 拋棄式 clone·**⛔ `checkout -- <檔>` 之不 commit 切態**）
  ⇒ 其「二者確為不同態」之自證**綁工作區之 `hash-object`**（`1d7d7c73…` vs `c5c28b82…`）。
  🔒 **本節復以之為據**：`§零` 所報之 `left-right` ＝ `0	11` 即「工作區與主線不同步」之告警，
  故本節**一律 `git show <rev>:<path>`**。

---

## `§三`　覆述之**涵蓋自證**（款 `5` 所令之「依開場文逐坑覆述」之完成判）

| 系 | 標籤 | 前段已覆述 | 本節補遺 | 合計 | 索引之基數 |
|---|---|---|---|---|---|
| 數字系 | `1`〜`26` | `26`（`§二-1`／`§二-2`／`§二-3`） | `0` | **`26`** | `26` |
| 字母系 | `a`〜`x` | `20`（`a`〜`i` 於 `§二-4`；`n`〜`x` 於 `§二-5`） | **`4`**（`j`／`k`／`l`／`m`） | **`24`** | `24` |
| **合計** | — | `46` | `4` | **`50`** | **`50`** |

🔒 **⇒ 逐坑覆述之涵蓋 ＝ `50`／`50`·⛔ 一則遺漏。**
🔒 **`y`／`z` ⛔ 存在**（當場實查·母體 ＝ 主線全追蹤 `.md`·框 ＝ `D-H` ∪ 表列首格）：
坑 `y` **`0`** 筆／坑 `z` **`0`** 筆；**對照組**（必非零）坑 `x` **`2`** 筆／坑 `n` **`5`** 筆 ⇒ **器非紅**。
⇒ 前窗交接註之「坑 `n`〜`z`」係其**節題之稱**，**實際所立者止於 `x`**——⛔ 讀為「`y`／`z` 遺漏」。

---

## `§四`　🩸 **本節自捕：索引器之一處偏移**（照實·⛔ 隱去·其**⛔ 改本報告之任何判**）

🔴 **所見**：四則之定義所在之坑表，其**表頭為 `| 坑 | 徵候 | 攔法 |`（`3` 欄）而其資料列有 `4` 格**
（標籤／形／徵候／攔法）⇒ 表頭**缺「形」一欄之欄名**。
🔴 **其對器之後果**：`probe_WG9269_pit_index.py` 以**表頭之欄名**定位 `徵候`／`攔法` 之欄索引
（`ci_x = 1`／`ci_l = 2`）⇒ 於此類表，其**實際所檢者為「形」與「徵候」二格之非空**，
**「攔法」格（`index 3`）未被檢**。
🟢 **其⛔ 改本報告之任何判**：本節已**逐則人工讀過**該四列之**第 `4` 格**，
**四則之攔法欄皆非空且有實質內容**（逐字見 `§二`）⇒ 「三項齊備」之判**仍成立**。
🔒 **通則**：**以表頭之欄名定位欄索引者，須先證「表頭欄數 ＝ 資料列格數」**；
不相等即其索引偏移，而**其偏移⛔ 必然報錯**（本例即靜默偏移一格）。
🛑 **本節⛔ 改該器一字**（其判未受影響；改之須另立工項並重跑全表以證判不變·`坑 25`）。

---

## `§五`　自限復驗 ＋ 續辦

| 項 | 實測 |
|---|---|
| ⛔ 動生產碼一字 | 本節之 commit 之生產碼判 ＝ 命中 `0` 行；`34` 檔 blob 相對基座相異 ＝ `0` |
| ⛔ 鑄任何號（補令九 `§四`） | 四簿三數 期初 ＝ 期末（本節⛔ 觸四簿母體任一檔） |
| ⛔ 翻旗標之預設 | 本節⛔ 觸 `app.py`；主線 `:9292` 之預設仍 `"0"` |
| `verify/baselines` ⛔ 觸（`GB-162`） | 樹 期初 ＝ 期末 |
| ⛔ 追改既有報告內文（補令九 `§二-3` 款 `4`） | 本節對既有檔之改動 ＝ **純末端追加**·`deletions 0`·嚴格前綴 ✅ |
| ⛔ 併線／⛔ 動側支 | `c1` ＝ `2f12927`（已併入主線·本節⛔ 觸）；`p3a` ＝ `05a11bd`（凍存·本節⛔ 觸） |
| 覆述之涵蓋 | **`50`／`50`**（`§三`） |

🛑 **續辦**：補令九 `§五` 序 `1`〜`3` **全數辦竣**（序 `2` 之併線係**已完成**·見本報告 `§一`）
⇒ **次為序 `4` ＝ `c2`**。
🔴 **`c2` 之三前置已備妥**（`628-27(1)` 之歸戶 ＝ `G030`·級聯剔除 `0m` **`3` 宗**／`3.5m` **`1` 宗**·
態宣告二值並報）；🛑 **`c2` 須 KL <u>單獨</u>放行**——補令九 `§一` 逐字：
KL 對 `c1` 之裁「**⛔ 及於** `c2`（旗標預設之翻轉·須**單獨**呈 KL）、**⛔ 及於** `GB-159` 之解除」。
🛑 **且 `c2` 須落<u>新側支</u>**（補令九 `§五` 序 `4` 逐字）——⛔ 逕動主線之生產碼。

---

# `W-G.9-269R`　**補令十段（一）**：`自誤 362` 之鑄 ／ 二撤回之登記 ／ 二立準入交接註

> **落點 ＝ 主線**（補令十 `§六` 序 `1`·零生產碼·得逕推·**須先於側支建立**）。
> **基座** ＝ `4741d1298e6d3728bca1a04be1a651d8785bf55f`（`ls-remote` 實查）。
> 🛒 **本節⛔ 載**：`§三-2` 之三必答（其為序 `2`·另節）、`c2` 之任何實作（其為序 `3`·新側支）。

---

## `§零`　工項零 ＋ `S-0b`

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付·主 checkout·該處⛔ 追蹤） | `a0d5b956eeca2e37825c1a5baadc0967a07b1995ce6182e831ebff5e4e86b231` | `9186` | `0` | `118` |
| 落檔（**二進位拷貝**·`坑 a`） | **同** | `9186` | `0` | `118` |
| **blob** | **同** | `9186` | — | — |

`S-0b`（`rc` **直取** ＝ `0`·`stderr` `0` B）：🔴 機械停機款 **`0`** ✅；🟡 **`2`** 項逐項處置——

| 項 | 處置 |
|---|---|
| `P-5` 本單⛔ 載 `SELF_SHA256` | **具名豁免**（三端 `sha256` 鏈承擔） |
| `P-4` `:54`「`docs/W-F_細部規格.md` `§7`…**逐字**：」 | 🟡 **偽陽·具名豁免**——其為**逐字引文之引述句**，⛔ 方向性轉引之量（該引文本身⛔ 含箭頭以外之方向詞；其「→**不配地**」係**處置之指向**、⛔ 二態之座標） |

---

## `§一`　序 `1`：鑄 `自誤 362`

### `一-1`　取號現查（**⛔ 裸框**·`常規四（八）`：取最大號一律用**定義列式**）

🔒 **母體之分工**（`W-G.9-192-PRE-b 🔧 一`）：**取最大號 → 登記表單檔**；**查占用 → 全倉**。

| 量 | 框（逐字） | 母體 | 實測 |
|---|---|---|---|
| **MAX** | 定義框 `^#+[^0-9０-９（(\n]*?自誤[\s　\`]*\`?([0-9]{1,4})\`?` ＋ 範圍框（`VR-091 四`） | `docs/reports/W-G.9波_claude.ai側自誤登記.md` **單檔** | 相異 `351`／**MAX `361`** ⇒ **次號 `362`** |

🔒 **意指占用（`常規四（七）`·錨定框 ⋀ **二形並取**·`GB-158`）**——母體 ＝ 主線全追蹤 `.md` **`706`** 檔·列框：

| 角色 | B 形 `自誤 \`N\`` 檔／命中 | C 形 `` \`自誤 N\` `` 檔／命中 |
|---|---|---|
| **受詢 `362`** | **`0`／`0`** | **`0`／`0`** |
| 對照組甲［已占用·必非零］`361` | `0`／`0` | **`1`／`1`** |
| 對照組甲2［已占用·必非零］`358` | **`2`／`2`** | **`4`／`11`** |
| 對照組乙［人造哨兵·**執行期自倉掃出**·字面⛔ 出艙］ | `0`／`0` | `0`／`0` |

🔴 **`GB-158` 當場再度坐實**：對照組甲（`361`·**確已占用**）於 **B 形得 `0`**
⇒ **單採 B 形者將判其「未占用」** ⇒ **B 形⛔ 單獨採信**；其「未占用」由 **C 形**承擔。
⇒ 🔒 **`362` 於二形皆 `0` ⋀ 二造對照如預期** ⇒ **可取**。

### `一-2`　鑄之形（`常規四（九）四`：取既有**最末一則**之標題形為範本·⛔ 自創形）

**範本**（`docs/reports/W-G.9波_claude.ai側自誤登記.md:8444` 逐字）：
`### 🩸 \`自誤 361\`　**立「跨器之逐位對拍」為閘，而未先證二器之<u>序列化約定</u>相同**`
**所鑄**：`### 🩸 \`自誤 362\`　**出艙「一整類全無定義」之否定判，而⛔ 備「必命中之已知成員」對照組……**`
⇒ **同形**（`###` ＋ `🩸` ＋ 反引號包 token ＋ 全形空格 ＋ 粗體標題）。

🔒 **收工閘二項（`常規四（九）二`）**：① **形之自證**——該標題經定義列式命中 ✅；
② **MAX 之推進**——`361` → **`362`**，且 `362` ⛔ 在缺號集（缺號集 `[106, 355, 356, 357]` **未變**）✅。
🩸 **`GB-132` 之避讓**：本則之 payload **僅一列**為 `#` 起首（即其標題）；其餘（表列／引言／編號列）
**皆⛔ 被定義框或範圍框命中** ⇒ 其⛔ 令任何他號被誤計。

### `一-3`　`自誤 362` 之落點

`docs/reports/W-G.9波_claude.ai側自誤登記.md` **末端追加**（⛔ 追改既有一字·嚴格前綴 ✅）。
🩸 **本節所用之基座係<u>倉側 blob</u>**（`726353` B）而**⛔ 工作副本**（`717598` B·**二者相異**）
——本 worktree `HEAD` 落後主線 `12` commit（`left-right` ＝ `0	12`）⇒ 依 `坑 m`／
`harness-worktree-crlf-trap`，一律 `git show <rev>:<path>`。**若逕以工作副本為基座，將抹去 `8755` B 之既有內容。**

---

## `§二`　序 `1`：`§一-3` **二撤回之登記**

| # | 受詞（逐字） | 登記 |
|---|---|---|
| `1` | 補令九 `§二-1` 之診斷（「坑 `a`〜`i` 九則被引用而無定義處」） | 🗄️ **撤回**——其為**器之產物**（框 `#### 坑 X` ∪ `\| **坑 X**` 對 `D-T`／`D-H` 二形皆幾乎不命中）。`§二-2`「何以要緊」之推論**隨之失其前提** |
| `2` | 發單側交予舊窗之收工令中「🛑 ⛔ 寫交接註——其索引現有缺」一句 | 🗄️ **撤回**（基於同一錯誤讀數）⇒ **舊窗得依原收工令第 `2` 項撰其交接註** |

🔒 **⛔ 撤回者**（補令十 `§一-3` 逐字）：`§二-3` 之索引修復**令**雖前提為誤，其**產物為實資產**
（一支帶二造對照之索引器 ＋ 一份全倉逐則可解析之索引 ＋ 二框對照表）⇒ **保留**，
並列為**爾後每次收工之必辦**（`§二-4` 之立準**維持**）。
🔒 **本窗之對應**：`verify/probes/probe_WG9269_pit_index.py` ⋀ 其落檔
`verify/out/WG9269R_pitindex_main.log` **皆已入主線**（`8cf38b3`）⇒ 該必辦之器**已在倉**。

---

## `§三`　序 `1`：**二立準入交接註**（補令十 `§一-2` ⋀ `§二`·逐字·⛔ 改寫）

> **立準 `A`（否定判之對照組）**
> **凡出艙任何「查無／無定義／命中 `0`」之否定判，其器須先以「必命中之已知成員」自證其撈得到本類；
> 未自證者，該否定判⛔ 出艙。**

> **立準 `B`（以表頭欄名定位欄索引）**
> **以表頭欄名定位欄索引者，須先證「表頭欄數 ＝ 資料列格數」；不相等即<u>靜默偏移</u>，
> 其「已檢」之宣稱為偽。**

🔒 **二者之值域⛔ 相交**：`A` 管**否定判之可信性**（器是否撈得到本類）；
`B` 管**欄位之對位**（器所檢之格是否即其所宣稱之格）。
🔒 **本波之施行實例**（⛔ 臆造·皆當場實測）：
`A` ＝ 本節 `§一-1` 之**三造對照**（`361`／`358` 必非零 ⋀ 哨兵必為零）；
`B` ＝ 本報告前節 `§四` 之自捕（坑表**表頭 `3` 欄而資料列 `4` 格**）。

🔒 **本波所立之坑之定義處清單**（補令九 `§二-4` 末款所令·**爾後每次收工之必辦**）
——已載於本報告 `補令九段 §三`，其自證 ＝ 索引之 `50`／`50` 全數可解析。

---

# `W-G.9-269R`　**補令十段（二）**：`§三-2` 之**三必答**（🟢 判 `(甲)` 同一筆·⛔ 停機）

> **落點 ＝ 主線**（補令十 `§六` 序 `2`·零生產碼）。**基座** ＝ `659727b6b244271658a816904375566e80cf9d1c`。
> **器** ＝ `verify/probes/probe_WG9269_c2_pool.py`（新建）／`verify/probes/probe_WG9269_c2_blk.py`（新建）。
> **落檔** ＝ `verify/out/WG9269R_c2pool_main.log`／`verify/out/WG9269R_c2blk_main.log`（**隨本報告入倉**·`§七-2` 之準）。
> 🔒 **三軸**：`(1)` 來源 ＝ 上開 commit 之**拋棄式短路徑 clone**（`39` 字元·`core.autocrlf=false`·
> `app.py` 工作區 bytes ＝ blob bytes ＝ `1464645`·`hash-object` ＝ `rev-parse` ＝ `c5c28b82…`）／
> `(2)` 母體逐表自載／`(3)` 粒度框 ＝ **㎡（2dp）**。
> 🛑 **⛔ 引任何自 `verify/baselines` 取得之數**（`GB-162`）——本節之數**全自當次實跑**。

---

## `§一`　必答 `1`：**`G011` 之四梯分級**（⛔ 推定·自倉具名其出處）

🔒 **出處** ＝ `verify/wd4_tier_list.py` 之 `compute(fixture=False)` **當次實跑**（⛔ baseline）。
🔒 **母體**：二情境各 **`33`** 群；梯次分布 **`{'0': 11, '1': 9, '2': 3, '3': 2, '—': 8}`**（二情境同）。

| 歸戶 | 軌別 | 宗數 | 宗清單 | `ΣG_戶(㎡)` `0m` | `ΣG_戶(㎡)` `3.5m` | **梯次** | 對應v3級 | 阻卻 |
|---|---|---|---|---|---|---|---|---|
| **`G011`** | 建地軌 | `2` | `628-42(1);628-42(2)` | **`90.19`** | **`94.73`** | **`2`** | **`級1-4 增配(雙出口)`** | 否 |
| `G025` | 建地軌 | `2` | `628-53(1);628-53(2)` | `0.26` | `0.26` | `3` | `現金補償(兩式)` | 否 |
| `G030` | 建地軌 | `2` | `628-27(1);628-27(2)` | `55.80` | `56.15` | `3` | `現金補償(兩式)` | 否 |

🔒 **答**：**`G011` 屬<u>梯次 `2`</u>（`級1-4 增配(雙出口)`），⛔ 梯3** ⇒ **⛔ 既裁之「不配地・釋池」處置**。
⇒ 🔒 **補令十 `§三-1` 之表逐列坐實**：`G025`／`G030` 係梯3 二群之一；**`G011` ⛔ 在梯3 二群**。
🔴 **⇒ `c2` 三宗中，二宗（`G025`／`G030`）之歸戶早經 KL 裁定 (D) 不配地；唯 `G011` 之處置為新。**

🩸 **照實併記（⛔ 頂替）**：`docs/W-F_細部規格.md` `§7` 逐字所載之梯3 二群 `ΣG`
（`G025` **`1.84`**／`G030` **`55.64`**／Σ **`57.48`**）與**當次實跑**之值
（`0.26`／`55.80`／Σ **`56.06`**·`0m`）**相異**。
🔒 **本節⛔ 判孰誤**（`補令二 §三`：先對齊母體再判孰誤）——其**時點相異**（該 `§7` 之數為 `v3` 錨態），
且依 **`GB-162`** 本節**只得用當次實跑之數**。**其對帳屬波末重烤之受詞**，⛔ 本節之受詞。

**量測器非紅之自證**（`自誤 362` 之立準 `A`：否定判須備「必命中之已知成員」）

| 造 | 受詞 | 得 | 期 | 判 |
|---|---|---|---|---|
| 甲［**必命中之已知成員**］ | 梯次 ＝ `3` 之群 | `['G025', 'G030']` | 同 | ✅ |
| 乙［人造哨兵·**執行期組出**·字面⛔ 出艙］ | 一必不存在之歸戶鍵 | `0` | `0` | ✅ |

🩸 **本器首版之框為 `== "梯3"` 而該欄之值逐字為 `"3"`**（裸數字）⇒ 造甲得 `[]` ⋀ **當場轉紅**
⇒ **立準 `A` 於本節<u>當場生效</u>**：**一個否定判（「梯3 無群」）被對照組攔下**，⛔ 出艙。

---

## `§二`　必答 `2`：**釋池之會計**（🟢 判 `(甲)` **同一筆**）

### `二-1`　🔑 **機械依據（其一）：二階段之資料流——碼面**

🛑 **單 `§三-2` 款 `2` 之問句逐字為「`F.0` 釋池後該宗是否仍在 `c2` 之配地母體內」。
照實回報：<u>其時序相反</u>**——`c2` 之旗標讀於 **`run_step_g` 之內**（`app.py` `k917_backfill_enabled`），
而 `wf_f0` 係**消費 `run_step_g` 之輸出**並**再跑一次 `run_step_g`**（trunk B）
⇒ **`c2` 在前、`F.0` 在後**。**本節依其<u>實際時序</u>作答。**

🔒 **碼面三項**（`git show <rev>:verify/wf_f0.py`·⛔ 憑記憶）

| # | 事實 | 逐字錨 |
|---|---|---|
| `1` | `F.0` 之移除**作用於 `build_parcels`**（Step-G **之輸入**），**⛔ `g_rows`** | `_f0_transform` 之 `f0 = copy.deepcopy(build_parcels)` ⋀ `removed = set(TIER3_LOTS) \| {...}` ⋀ `f0 = [tp for tp in f0 if tp["暫編地號"] not in removed]` |
| `2` | 其受詞為**硬寫之四宗名單** | `TIER3_LOTS = ["628-53(1)", "628-53(2)", "628-27(1)", "628-27(2)"]` |
| `3` | 🔑 **池 ＝ <u>幾何剩餘</u>·每 trunk <u>各自重算</u>·⛔ 累加式** | `poolB = {lbl: round(float(v["池總=幾何剩餘(㎡)"]), 2) for lbl, v in sgB["pool_diag"].items()}`；`池差 ＝ poolB − poolA` |

🔒 **⇒ 結構上之結論**：「釋池」**⛔ 一筆記入貸方之帳目**，而是**二次獨立幾何重算之差**。
⇒ 同一宗地若已於 **trunk A**（`c2` 之 Step-G）被剔而其面積已計入 `poolA`，
則 trunk B 再移除之**⛔ 使 `poolB` 再增** ⇒ **`池差` 自然歸零** ⇒ **⛔ 二次釋池**。
🛑 **本項⛔ 以「守恆帳沒紅」為據**（款 `2` 明令）——其為**碼面之資料流**，⛔ 事後之帳。

### `二-2`　🔑 **機械依據（其二）：實測**（旗標二態各量一次）

🩸 **`F.0` 於<u>二態皆拋</u>同一既存例外** ⇒ **`F.0` 之 `池差` 欄⛔ 可得**（**⛔ 印 `0`**）：
逐字 ＝ `RuntimeError: 🔴 GSA 錨檢破（0m）…【未被評估】錨鍵 ['G006', 'G007', 'G009', 'G010', 'G014', 'G017']…`
🔒 **二態之訊息逐字相同** ⇒ **其為<u>既存紅</u>、⛔ `c2` 所致**（旗標 `off` ≡ `c1` 之逐位不變態）。
🛑 **本節⛔ 修之、⛔ 放寬之**；其屬**准紅碼**之對帳範圍。

⇒ 改以**梯3-only 之隔離量測**代之：`poolT3 − poolA`，其中 `poolT3` ＝ **只移除 `TIER3_LOTS`**
後重跑 Step-G 之池。🛑 **其與 `wf_f0` 之別（照實·⛔ 頂替）**：本重跑**⛔ 施加級0／0′ 之合併決策**、
**⛔ 跑 `GSA`／`k*`／旗標等錨檢** ⇒ 其為**梯3 一項之隔離效應**，**⛔ `F.0` 之全量**。
🔒 `TIER3_LOTS` **執行期自 `wf_f0` 取**（⛔ 本器重寫·`GB-48`）。

🔒 **六宗之定位**（`probe_WG9269_c2_blk.py`·`off` 態·判定集 **`12`** 格）

| 歸戶 | 宗地 | 街廓 | `G(㎡)` `0m` | `G(㎡)` `3.5m` |
|---|---|---|---|---|
| `G011` | `628-42(1)` | **`R2`** | `0.23` | `4.77` |
| `G011` | `628-42(2)` | `R3` | `89.96` | `89.96` |
| `G025` | `628-53(2)` | **`R5`** | `0.00` | `0.00` |
| `G025` | `628-53(1)` | `R6` | `0.26` | `0.26` |
| `G030` | `628-27(1)` | **`R2`** | `29.26` | `29.61` |
| `G030` | `628-27(2)` | `R3` | `26.54` | `26.54` |

#### 【情境 `0m`】

| 街廓 | `poolA` `off` | `poolA` `on` | **Δ(on−off)** | **梯3 釋池 `off`** | **梯3 釋池 `on`** | 判 |
|---|---|---|---|---|---|---|
| `R1` | `1175.50` | `1175.50` | `+0.00` | `+0.00` | `+0.00` | 二態皆 ~`0` |
| **`R2`** | `1796.70` | `1835.10` | **`+38.40`** | **`+30.76`** | **`+0.00`** | 🔑 **`on` 已歸零** |
| `R3` | `1795.13` | `1795.13` | `+0.00` | `+27.23` | `+27.23` | **⛔ 變**（`628-27(2)` **⛔ 被 `c2` 剔**）|
| `R4` | `1190.32` | `1190.32` | `+0.00` | `+0.00` | `+0.00` | 二態皆 ~`0` |
| **`R5`** | `1760.34` | `1766.97` | **`+6.63`** | **`+6.63`** | **`+0.00`** | 🔑 **`on` 已歸零** |
| `R6` | `1628.71` | `1628.71` | `+0.00` | `+0.26` | `+0.26` | **⛔ 變**（`628-53(1)` **⛔ 被 `c2` 剔**）|
| **Σ（帶號）** | — | — | **`+45.03`** | **`+64.88`** | **`+27.49`** | — |
| **Σ\|·\|（絕對值）** | — | — | **`45.03`** | **`64.88`** | **`27.49`** | — |

#### 【情境 `3.5m`】

| 街廓 | `poolA` `off` | `poolA` `on` | **Δ(on−off)** | **梯3 釋池 `off`** | **梯3 釋池 `on`** | 判 |
|---|---|---|---|---|---|---|
| `R1` | `1175.10` | `1175.10` | `+0.00` | `+0.00` | `+0.00` | 二態皆 ~`0` |
| `R2` | `1774.43` | `1774.43` | `+0.00` | `+30.71` | `+30.71` | **⛔ 變**（二宗於 `3.5m` 皆⛔ 被剔）|
| `R3` | `1795.13` | `1795.13` | `+0.00` | `+27.23` | `+27.23` | **⛔ 變** |
| `R4` | `1184.53` | `1184.53` | `+0.00` | `+0.00` | `+0.00` | 二態皆 ~`0` |
| **`R5`** | `1760.95` | `1766.97` | **`+6.02`** | **`+6.02`** | **`+0.00`** | 🔑 **`on` 已歸零** |
| `R6` | `1628.71` | `1628.71` | `+0.00` | `+0.26` | `+0.26` | **⛔ 變** |
| **Σ（帶號）** | — | — | **`+6.02`** | **`+64.22`** | **`+58.20`** | — |
| **Σ\|·\|（絕對值）** | — | — | **`6.02`** | **`64.22`** | **`58.20`** | — |

### `二-3`　🟢 **判 ＝ `(甲)` 同一筆**（⛔ 停機）

🔑 **判別力（⛔ 恆綠·其歸零與否**逐街廓**與 `c2` 之剔除**完全共位**）**

| 街廓（梯3 之宗所在） | 該宗是否為 `c2` 所剔 | 梯3 釋池 `off`→`on` | 判 |
|---|---|---|---|
| `R2`（`628-27(1)`·`0m`） | **是** | `+30.76` → **`+0.00`** | **歸零** |
| `R5`（`628-53(2)`·`0m`／`3.5m`） | **是** | `+6.63`／`+6.02` → **`+0.00`** | **歸零** |
| `R3`（`628-27(2)`） | **否** | `+27.23` → `+27.23` | **不變** |
| `R6`（`628-53(1)`） | **否** | `+0.26` → `+0.26` | **不變** |
| `R2`（`3.5m`·二宗皆未被剔） | **否** | `+30.71` → `+30.71` | **不變** |

⇒ 🔒 **`4`／`4` 之「被剔者歸零」⋀ `3`／`3` 之「未被剔者不變」·零例外**
⇒ **同一筆地⛔ 被釋池二次**。

🔒 **去重之機制（款 `2` `(甲)` 所令之具名）**：
> **池 ＝ `池總=幾何剩餘(㎡)`，係每一 trunk <u>各自自幾何重算</u>之量，⛔ 累加式之帳。
> 故「移除一宗」之效應為<u>冪等</u>——已在 `poolA` 內者，於 trunk B 再移除之⛔ 使 `poolB` 再增。**

🔴 **`c2` 之<u>新增</u>釋池（＝ 既有裁定所未涵蓋者）——逐情境具名**

| 情境 | `Δ(on−off)` 之 `Σ` | 其中屬**梯3 既裁之提前** | **🔴 新增（`G011`）** |
|---|---|---|---|
| `0m` | `+45.03` | `R2 +30.76` ＋ `R5 +6.63` ＝ `+37.39` | **`R2 +7.64`** |
| `3.5m` | `+6.02` | `R5 +6.02` | **`+0.00`** |

🔒 **`3.5m` 之 `R5` 二數逐值相同**（`+6.02` ＝ `+6.02`）⇒ **該情境之 `c2` 效應 ＝ <u>純梯3 提前</u>·⛔ 新增一分**。
🛑 **`0m` 之 `+7.64` ⛔ 等同 `628-42(1)` 之 `G`（`0.23`）**——其為**剔除 ＋ 遞補前移**之**淨幾何效應**
（`K-9-17 五`：後繼者整體前移 ⇒ 幾何改變 ⇒ `G` 重算）。**同理 `R5` 之 `+6.63` ⛔ 等同 `628-53(2)` 之 `G`（`0.00`）。**
🛑 **⇒ 差異表⛔ 以「ΣG_被剔」充「入池量」**——二者**⛔ 同一量**。

---

## `§三`　必答 `3`：**`3.5m` 之不對稱**（逐情境具名·⛔ 併為一事）

| 宗地 | 街廓 | 閘一 `驗_B藍影` `0m` | 閘一 `驗_B藍影` `3.5m` | `on` 態去留 `0m` | `on` 態去留 `3.5m` |
|---|---|---|---|---|---|
| `628-42(1)` | `R2` | **`不合格`** | **`不適用`** | **剔除**（閘一直接） | **留** |
| `628-53(2)` | `R5` | `不合格` | `不合格` | **剔除**（閘一直接） | **剔除**（閘一直接） |
| `628-27(1)` | `R2` | **`不適用`** | **`不適用`** | **剔除**（🔴 **級聯**） | **留** |

🔒 **其由 ＝ 二層·⛔ 一事**

1. **`628-42(1)` 之閘一判<u>隨情境而異</u>**：`0m` `不合格` ／ `3.5m` **`不適用`**
   ——其 `G` 由 `0.23`（`0m`）增為 `4.77`（`3.5m`）⇒ 退縮改變其幾何 ⇒ **藍影閘之判改變**
   ⇒ **`3.5m` **⛔ 有**閘一直接剔除之起點**。
2. **`628-27(1)` 之閘一判<u>二情境皆 `不適用`</u>** ⇒ 其於 `0m` 之消失**純屬級聯**
   （`K-9-17 五`：前序宗被剔 ⇒ 後繼者整體前移 ⇒ 幾何改變 ⇒ **重判**轉不合格）；
   🔒 **`3.5m` 因 `628-42(1)` 未被剔 ⇒ <u>級聯之起點不存在</u>** ⇒ 其**留於配地**。

🔒 **⇒ 二情境之剔除數**：`0m` **`3`** 宗（直接 `2` ＋ **級聯 `1`**）／`3.5m` **`1`** 宗（直接 `1` ＋ 級聯 `0`）
——與 `c1` 側支之實跑落檔 `verify/out/WG9269R_c1_on_drop.log` **逐項相符**。

---

## `§四`　🛑 續辦與界

🟢 **必答 `2` 判 `(甲)`** ⇒ **⛔ 觸發款 `2` `(乙)` 之停機**。
🔒 **三必答皆已出艙其機械依據**（碼面 ＋ 實測 ＋ 判別力）。
🔒 **`§三-3` 之差異表增列欄（「既有裁定」）之材料已備**：
`628-53(2)`／`628-27(1)` ⇒ `已由 KL 裁定 (D) 判不配地（梯3）`（出處 ＝ `docs/W-F_細部規格.md` `§7` 逐字）；
**`628-42(1)` ⇒ `⛔ 既有裁定·本批為新`**（其歸戶 `G011` ＝ **梯次 `2`**·`級1-4 增配(雙出口)`）。
🛑 **呈 KL 時須逐宗標明其是否已有既裁**——⛔ 令 KL 誤以為三戶皆為新生之損害。

🔒 **自限復驗**：本節**⛔ 動任何生產碼**（旗標之翻轉係**以環境變數**為之·⛔ 改其預設之字面）；
**⛔ 觸 `verify/baselines` 一位元組**；**⛔ 鑄任何號**（補令十 `§五`：得鑄者 ＝ `自誤 362` **一號**·已鑄）；
**⛔ 建任何側支**（`c2` 屬序 `3`）。
