# `W-G.9-268pR`　錨點更正（**施工窗**）：開工閘九閘 ＋ `c0` 四項 ⇒ 🛑 **停機上呈**

> **受詞** ＝ `W-G.9-268′`（`docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md`）
> ＋ **補令一**（`docs/orders/W-G.9-268p_補令一_S1形更正與三事處置.md`）。
> 🔒 本檔與 `docs/reports/W-G.9-268pR_工項零_原封入倉與號占用.md`（**審查窗**之產出）**並存·互不覆寫**
> ——補令 `§二` 自解 `1` 已核可其 `(b)`。
> 🛑 **本批⛔ 動生產碼一字**（逐 `commit` 之判見 `§八`·**含空輸出**）。
> 🛑 **本報告⛔ 載**任何關於**病一**（`K-9-23` 閘一轉主動／`K-9-17` 遞補）之修法主張——其為 `W-G.9-269`（單 `§九` 逐字）。

---

## §零　三軸之總宣告（`VR-091`）

| 軸 | 值 |
|---|---|
| `(1)` 來源 `commit` | **期初** ＝ `2bcf62357ce5e52fb0a92d14af1496307c6e72c9`（`ls-remote` 實查·`§一 S-2`）／**工項零後** ＝ `bf1d92fe58c86925437faee36a2309d95f1a33f4`／**`c0`-a 後** ＝ `eb3aee0b76ed8f4aa2e3f574ed724adfb24df8bb` |
| `(2)` 母體 | 逐閘分列（各閘自載·⛔ 通用一個母體） |
| `(3)` 粒度框 | 逐閘分列（**檔框**／**列框**／**字元框**／**blob bytes**／**長度（公尺）**·各閘自載） |

🔒 **本窗之工作樹** ＝ `…/.claude/worktrees/w-g-9-268p-s1-correction-f41d70`；
`git config core.autocrlf` **有效值 ＝ `false`**（三層實查見 `CLAUDE.md` 檔末之更正註）。

---

## §一　開工閘（**九閘**·逐條當場復現）

🩸 **閘數之照實**：單 `§九` 稱「開工閘**八閘**」，而單 `§一` 之表列 **`S-0`／`S-0b`／`S-1`〜`S-8` ＝ `10` 列**
（`S-5` 於 `-268R` 係本批新增）。本報告逐條辦 `S-0`〜`S-8`，**⛔ 以「八」或「十」之名目數為證據**；
逐閘各自載其母體與粒度框。此係**記數之別**，⛔ 實質之別。

### `S-0`　二單原封入倉（入倉 blob `sha256` ＝ 落檔前來源）

**母體** ＝ 二單各一檔／**粒度框** ＝ **blob 內容 `sha256` ＋ bytes**（🔒 受詞 ＝ **blob 之內容**·⛔ git 物件 hash）

| 單 | 落點 | 入倉 blob `sha256` | bytes | 判 |
|---|---|---|---|---|
| `W-G.9-268′`（**審查窗**已辦·`7d7112f`）| `docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md` | `037538f136b51a4ec7efdc4ae80471906f62575d9a3c8d860c20253500447ab9` | `20407` | ✅ 與補令 `§三-3` 所載**逐位相符** |
| **補令一**（本窗辦·`bf1d92f`）| `docs/orders/W-G.9-268p_補令一_S1形更正與三事處置.md` | `0755337832e41dc033b55df70a7d961c882df04cde457aaef3eaf93223b08881` | `8488` | ✅ 與落檔前來源**逐位相符**（`cmp` 空）|

🔒 **補令入倉之三數（落檔前 ＝ 落檔後 ＝ 入倉 blob·三者同值）**：`bytes 8488`／`CR 0`／`LF 98`。
🔒 `numstat` ＝ `98	0` ⇒ **`deletions ＝ 0`**（純新增檔·`常規一 ②`）。
🛑 **射程之界（照實·⛔ 頂替）**：本閘所證者為「**落檔 → 入倉**」之保真；
「**發單側送出 → 落檔**」之半段，補令 `§三-3` 已立準（`sha256` 載於**交付訊息**·⛔ 載於單內），
本窗**⛔ 持有該交付訊息之獨立 `sha256`** ⇒ 該半段**具名為不可閉合**，⛔ 宣稱其已閉合。

### `S-0b`　`probe_order_preflight.py <單>`

**器** ＝ `verify/probes/probe_order_preflight.py`／**粒度框** ＝ **項數**（`rc` ＝ 紅項數）

**判別力造（`--selftest`·四造·⛔ 可省）**：`rc = 0`；靶① `P-3` **紅** ✅／靶② `P-4` **提示** ✅／
靶③ `P-6` **提示** ✅／**反靶** `-190R` 之 `P-3` **不紅** ✅ ⇒ 逐字「判別力造：✅ 全數成立 ⇒ 本器非恆綠亦非恆紅」。

**（甲）本單 `W-G.9-268′`（單 `S-0b` 之受詞）**：**`rc = 0`**／機械停機款 **`0`** 項 ⇒ ✅ **通過**。
🟡 提示 **`3`** 項，**逐項具名處置**（⛔ 靜默略過）：

| # | 項 | 逐字（節略） | 處置 |
|---|---|---|---|
| `1` | `P-5` | `⛔ 無 SELF_SHA256 列 ⇒ ⛔ 不可對拍（🟡 非紅：非所有文件皆須有）` | **採納**——其正是補令 `§三-3` 之受詞；該保真鏈已由發單側自其原稿獨立復算閉合（`S-0` 已對拍 `037538f1…` ✅）|
| `2` | `P-4` `:87` | `於 c0-1 所定位之**全部**碼位（二檔）加入新錨，置於具名旗標之後（預設 off）` | **具名豁免**——「置於…之後」係**碼之次序**，⛔ 方向性轉引；無座標系可具名 |
| `3` | `P-4` `:144` | `` ``` ``（`§五` 之 code fence 首列）| **具名豁免**——**偽陽**：該列為 fence 標記，⛔ 含任何方向性轉引 |

**（乙）補令一（本窗自願加測·單⛔ 令）**：**`rc = 1`**／`P-5` 判 🔴。
🛑 **此係<u>量測器偽陽</u>，⛔ 受詞紅**——機械證三項（母體 ＝ 該檔單檔·**列框**）：
> ① `SELF_SHA256` 之全檔命中 ＝ **`1`** 列，該列為 `### 三-3　單⛔ 載 \`SELF_SHA256\` ⇒ …`，
> 係**節題之散文**，⛔ 宣告列。
> ② `[0-9a-f]{64}` 之全檔命中 ＝ **`1`**，位於下二列之引言塊，其值 ＝ `037538f1…`
> ＝ **原單**之 `sha256`，⛔ 本補令之 `SELF_SHA256`。
> ③ 器所報之「單載值」逐字為 `### 三-3　單⛔ 載 \`SE…` ——**即該節題本身**，⇒ 其確係把節題誤讀為宣告列。

🔒 **⇒ 依 `CLAUDE.md`「`序 11` 之一般化」款 `三`：「無從回測」⛔ 等同「回測不過」**
⇒ 本項之正確出艙為 **「無從驗」**，⛔ 「驗不過」。
🛑 **本窗⛔ 動 `probe_order_preflight.py` 一字**——修其解析即**放寬既有之閘**（`自解款` **黑名單**）⇒ 只具名、⛔ 自解。
🩸 **併記（候發單側·⛔ 由 CC 自裁）**：`P-5` 之受詞取「檔內**最末**一個 `SELF_SHA256`」而**⛔ 檢其後是否為 `64` 碼十六進位**
⇒ **任何在散文中提及該字樣之單，其 `P-5` 必紅**。是否補其判準，係**意思決定**。

### `S-1`　號占用（**補令 `§一` 之更正後 operative 形**）

🔒 **operative 性質**（補令 `§一` 逐字）＝「**該號於全 `docs/` 之全部命中，逐筆皆為本單自身之產物**」——⛔「六數全 `0`」。
**母體** ＝ 全 `docs/` 倉側 blob **`634`** 檔（非 utf-8 解出者 `19`）·態 `bf1d92f`／**粒度框** ＝ **檔框 ＋ 列框**（宣告框）·併出**鬆框**

#### 款 `a`　六數與鬆框**分列**（⛔ 只出總數）·款 `c`　`D1` 軸**雙形查**

| 受詢之形 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬列 | **列框**（`\|D2 ∪ D3\|`）| **檔框** | 鬆框 檔／列 |
|---|---|---|---|---|---|---|---|
| U+2032 形 `W-G.9-268′`（單形）| **`0`** | `2` | `1` | `0` | `3` | `2` | `3`／`19` |
| ASCII 變體形 `W-G.9-268p`（單形）| `3` | `1` | `3` | `1` | `3` | `3` | `3`／`19` |
| **二形聯集（補款 `⑨` operative）** | **`3`** | **`3`** | **`4`** | **`1`** | **`6`** | **`3`** | `3`／`35` |

🔒 **款 `c` 之坐實**：**單查 U+2032 形者其 `D1` ＝ `0`** ⇒ 該軸**無判別力**（入倉檔名用 ASCII `p`）
——與補令 `§一 c` 之預言**逐位相符**。

#### 款 `b`　**逐筆歸類**（每一命中之檔／列／角色）

🔒 全部宣告框命中落於**三檔**，逐檔即本單自身之產物：

| 檔 | 角色 | 宣告框命中之列（`D2`／`D3`）|
|---|---|---|
| `docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md` | **本單** | `:1`〔`D2` 標題〕／`:210`〔`D3b` 自稱形〕 |
| `docs/orders/W-G.9-268p_補令一_S1形更正與三事處置.md` | **本單之補令**（本窗工項零入倉）| `:1`〔`D2`〕／`:51`〔`D3b`〕／`:81`〔`D3b`〕 |
| `docs/reports/W-G.9-268pR_工項零_原封入倉與號占用.md` | **工項零報告**（審查窗）| `:1`〔`D2`＋`D3b`·雙屬〕 |

🟢 **歸類為「第三方」者 ＝ `0` 筆** ⇒ **款 `b` 通過·⛔ 觸發停機**。
🔒 **鬆框之 `35` 列**逐列亦落於同三檔（⛔ 第四檔）——其為**計算式／引用／待辦**之提及，
依補款 `②` **⛔ 構成占用**；鬆框僅作**漏框偵察**（`④`）。

#### 款 `d`　二對照

| 對照 | 值 | 判 |
|---|---|---|
| **甲** ＝ `W-G.9-268`（須 `≥ 1`）| `D1` `6`／`D2` `9`／`D3` `10`／雙屬 `1`／**列框 `18`**／**檔框 `6`**；鬆框 `10`／`88` | ✅ |
| **乙** ＝ 人造號（**執行期組出**·字樣⛔ 入倉·`GB-147`）| 六數**全 `0`**；鬆框 `0`／`0` | ✅ |

🔒 **甲之數與單 `§一 S-1` 所載（發單側實測 `D1 3／D2 6／D3 4／列框 10／檔框 3`）不同 ＝ <u>態之別</u>，⛔ 框之別**
——本窗於**發單側量測之態** `95882de` 重跑同器，得 **`D1 3／D2 6／D3 4／列框 10／檔框 3`** ⇒ **逐位相符** ✅
（其增量來自 `abeb879`／`7d7112f`／`6cdc4af`／`bf1d92f` 四 `commit` 所入倉之五檔）。
🔒 **同態之受詢**（`95882de`）：二形聯集**六數全 `0`** ⇒ 🔒 **補令 `§一` 之結構證於本窗獨立復現**
——原形之「須六數全 `0`」於**入倉前**為真、於**施工窗之期初態**（必在入倉後）**結構上不可能成立**。

#### 款 `e`　**二號之可辨**（結構證 ＋ 實測證）

**結構證**（人造樣本·⛔ 倉內取樣）：

| 樣本 | 受詢 U+2032 框 | 受詢 ASCII 框 | 對照組甲框 |
|---|---|---|---|
| `見 W-G.9-268 之 §八`（**裸號**）| `False` ✅ | `False` ✅ | `True`（證框非恆假）|
| `見 W-G.9-268′ 之 §八` | `True` ✅ | — | `True` 🩸 **反向非對稱** |

**實測證**（母體 ＝ 全 `docs/` `634` 檔·態 `bf1d92f`）：
> **方向一**：含裸號而**⛔ 含任一受詢形**之列 ＝ **`53` 列／`10` 檔**（須 `> 0`）⇒ 該 `53` 列於受詢框下命中 `0` ⇒ **可辨** ✅
> **`D1` 集合**：甲 `6` 檔／受詢 `3` 檔；**甲 ∖ 受詢 ＝ `3` 檔**（`W-G.9-268_施工單…`／`W-G.9-268_附件甲…`／`W-G.9-268R_開工閘…`）⇒ **可辨** ✅

🩸 **反向非對稱之具名**（款 `e` 所令·⛔ 影響本閘）：`W-G.9-268` 之框**會**咬中 `268′`／`268p`
（`′`／`p` 皆非數字 ⇒ 過 `(?![0-9])` 邊界）⇒ **甲之數被灌大**；
實測**二者同列共現 ＝ `35` 列**、**受詢 ∖ 甲 ＝ `0` 檔**（⇒ 為 `0` 係**應然**·⛔ 缺陷）。

🟢 **`S-1` 判 ＝ 通過**（款 `b` 無第三方 ∧ 款 `e` 二號可辨）。
🔒 **補令 `§一` 對審查窗白名單 `1` 自解之追認**已收到；本窗**⛔ 就此重新停機**，惟已依 `a`〜`e` 逐款出艙。

### `S-2`　`left-right` ＝ `0　0`；`ls-remote` 與期初態逐位相符

**母體** ＝ `origin/wip/s1-endpart` 單 ref／**粒度框** ＝ **全 `40` 碼**

| 量 | 值 | 判 |
|---|---|---|
| `git ls-remote origin refs/heads/wip/s1-endpart`（開窗時）| `2bcf62357ce5e52fb0a92d14af1496307c6e72c9` | ✅ 與**補令**所載期初態**逐位相符**（⛔ 採信本地 ref）|
| `git rev-list --left-right --count origin/wip/s1-endpart...HEAD`（開窗時）| `0	0` | ✅ |

🩸 **與<u>原單</u>所載期初態（`95882de5…`）不符 ＝ 應然**——原單寫於審查窗開工前，
其後 `abeb879`／`95882de`／`7d7112f`／`6cdc4af`／`2bcf623` 五 `commit` 已推進主分支；
**補令**（`§首`）已將期初態更新為 `2bcf623…`。⇒ **以補令為準**（補令 `§首`：相斥處以本補令為準）。

### `S-3`　生產碼 `34` 檔 blob 期初

**母體（正面列舉·⛔ 全倉排除式）** ＝ `app.py` ＋ `verify/` 頂層 `*.py` ＝ **`34`** 檔（判定集基數 `34`）
**粒度框** ＝ **blob SHA ＋ bytes**　全表 ＝ `verify/out/WG9268pR_s3_manifest.txt`（**當場自倉現查**·⛔ 引附件甲之表）

| 量 | 實測 | 判 |
|---|---|---|
| `app.py` | **`c5ec503c740f59e084cedd83531b9d029fe8ddc0`**／**`1454937`** B | ✅ **與單 `S-3` 逐位相符** |
| `verify/fixture_yi_construction.py` | **`69f4a18ae03f13181e6ca83ba8b291a54ea548ab`**／`11562` B | ✅ 與 `-268R` 之現查相符（附件甲載 `69f4a18e…` 係**單字元抄錄之誤**·＝ `自誤 334` 之形）|
| 對照乙（一必不可得之檔·**執行期組出**）| `git rev-parse` `rc = 128` | ✅（須 `≠ 0`）|

### `S-4`　`run_verification` 期初實跑

**器** ＝ `verify/probes/wg9268_gate8_names.py`（⛔ `grep -oP`）／**母體** ＝ `verify/out/WG9268pR_runverif_pre.log` 單檔（判定集基數 **`312`** 列）／**粒度框** ＝ **列框**

| 欄 | 實測 | 單 `S-4` 之判準 | 判 |
|---|---|---|---|
| `rc` | **`1`** | — | — |
| `stderr` | **`0`** B | `0` B | ✅ |
| `RESULT`（逐字·log `:311`）| **`RESULT: FAIL`** | `FAIL` | ✅ |
| 🔴 FAIL 名目 | **`24`**（相異 `24`）| 紅 `24` | ✅ |
| ✅ PASS 名目 | **`10`**（相異 `10`）| 綠 `10` | ✅ |
| 名目合計 | **`34`** | — | — |
| 兩造對照 | 人造陽性 ＝ `True` ✅／人造陰性 ＝ `False` ✅ | — | ✅ 器非紅 |

⇒ ✅ **與發單側於 `e57d2ef` 之實測逐位相符**（生產碼未動 ⇒ 應同·**⛔ 觸發停機**）。

### `S-5`　`run_all` 期初對帳態（**只取值·⛔ 判過**）

**執行處** ＝ **拋棄式 clone**（`run_all` 改寫已追蹤 log）·`git -c core.autocrlf=false clone --no-hardlinks`
⇒ `checkout` 後**設 local `core.autocrlf=false` 並 `reset --hard`**，實測 `app.py` 工作區 `1454937` B／`CR 0`／
`sha256` 前 `16` 碼 `1e15d45ae84b02ca` ＝ blob bytes ⇒ **受測物與倉態逐位一致**方起跑。
**母體** ＝ `verify/out/WG9268pR_runall_pre.log` 單檔／**粒度框** ＝ **列框 ＋ 項數**

| 量 | **期初值** |
|---|---|
| `rc` | **`1`** |
| `stdout`／`stderr` | **`52904`** B ／ **`0`** B |
| `RESULT`（逐字）| **`RESULT: FAIL`** |
| 對帳 名目 | **凍存 `22`／現況 `24`** |
| 凍存有而現況無 | **`1`** 項：`率接線 G估 欄變動0m 1 格（期 0·Y 波後 baseline 同源）` |
| 現況有而凍存無 | **`3`** 項：`率接線 G估 欄變動0m 8 格…`／`抵費地0m(應空)`／`率接線 G估 欄變動3.5m 12 格…` |
| **類 I**（正規化後相同·自動視為同）| **`0`** 項（其中 raw 即相同 `0` 項）|
| **類 II**（僅 traceback 行號不同·⛔ 自動視為同）| **`8`** 項：`v3 財務接線閘`／`W-F F.0`〜`F.4`／`W-G G.1 接線層 ctx-builder 同源`／`W-G G.2 世代幾何曝出契約` |
| **類 III**（訊息實質不同）| **`13`** 項 |
| 對帳判（逐字）| `🔴 **對帳 FAIL**——名目或失敗原因與凍存不符（⛔ 不得逕自更新名單）` |

🛑 **本閘⛔ 以「與凍存相同」為通過條件**（單 `S-5` 逐字）——期初即 `對帳 FAIL`，本閘**只取值**，
其為 `§六 併線前必答 3` 之**比較端**。

### `S-6`　覆蓋帳期初

**器** ＝ `verify/probes/probe_WG9265_coverage_account.py`（唯讀·`rc = 0`·`stderr` **`0`** B）
**母體** ＝ `verify/out/got_G值_退縮{0m,3.5m}_partial.csv`（harness 落檔·**全區連跑**口徑）／**判定集基數 ＝ `13` 組**／**粒度框** ＝ **面積（㎡）**
落檔 ＝ `verify/out/WG9268pR_cov_pre.log`

| 量 | 實測 | 單 `S-6` | 判 |
|---|---|---|---|
| 組數 | **`13`**（違反 **`2`**／無違反 **`11`**）| `13`·違反 `2` | ✅ |
| 違反組（**逐組逐對**·⛔ 只出組數）| `0m·R2·left` Σ−∪ ＝ **`45.9766`**〔第0宗 `628-41(1)`×第2宗 `628-27(1)` ＝ **`19.8142`**；第0宗×第3宗 `628-40(1)+` ＝ **`26.1624`**〕／`3.5m·R5·left` Σ−∪ ＝ **`56.8186`**〔第0宗 `628-45(1)+`×第2宗 `628-18(2)` ＝ **`56.8186`**〕 | `45.9766`／`56.8186` | ✅ |
| **三造否證對照** | 人造相疊矩形 ⇒ `1.0000000000` ✅／人造相離矩形 ⇒ `0.0000000000` ✅／注入 `+1㎡` 於原綠之宗（`0m` `628-37(1)`：`+0.0036 → +1.0036`；`3.5m` `628-36(1)`：`+0.0011 → +1.0011`）⇒ **原綠→轉紅** ✅ | 三造如期 | ✅ |
| 自我驗證閘 | 街廓面積逐街廓為常數 ✅；位次 `0` 之 `驗_宗序 ＝ 街角第1宗` ⟺ `第1筆街角 ＝ 是` ✅ | — | ✅ |

🔒 **重疊對之形（逐格·⛔ 摘要成通則）**：二違反組之重疊對**皆為 第0宗 × 第2／第3宗**（位次差 `2`／`3`·**跨過第1宗**）。

### `S-7`　硬閘活體生效（`--selftest` **⋀** 活體攔截實測·⛔ 只 `test -f`）

| 檢 | 實測 |
|---|---|
| `--selftest` | `rc = 0`；對照甲（含引入符·期望阻斷）＝ `True` ✅／乙（不含·期望放行）＝ `True` ✅／丙（非 Bash·期望放行）＝ `True` ✅；逐字 `[selftest] 判 = PASS` |
| 該器**自載之警語**（逐字）| `🛑 本自檢綠 ⛔ 蘊含 hook 已接線——接線與否須由**活體攔截實測**判定。` |
| 掛載現查 | `.claude/settings.json` 之 `PreToolUse`／`matcher: "Bash"` ⇒ `python "$CLAUDE_PROJECT_DIR/verify/tools/wg9237_heredoc_guard.py"` |
| **活體攔截實測** | 本窗**故意**送出一條含 heredoc 之 `Bash` 命令 ⇒ **被阻斷**（`PreToolUse:Bash hook error`）|
| 🔑 **命中偏移之自證** | 所送命令之前綴為 `echo BBBBBBBBBBBB; cat `（**`23` 字元**·⛔ 沿用 `-268R` 之 `21`）⇒ `<<` 之位移**應為 `23`**；阻斷訊息逐字回報**「位移 `23`」** ⇒ 🔒 **該器讀到的確係<u>本窗此一真命令</u>**，⛔ 恆綠、⛔ 只 `test -f` |

⇒ ✅ **接線與生效皆經活體坐實**。
🩸 **併記（`常規五` 之形）**：該 hook 之阻斷訊息於 `cp950` 主控台**呈亂碼**（含 emoji），
惟其**數字部（`23`）完好**且阻斷**確已生效** ⇒ **顯示層之病·⛔ 功能之病**。

### `S-8`　harness 可達性期初

**器** ＝ `verify/probes/wg9268_bing_csv.py`（🩸 **REPO 常數須補丁**·見 `§五` 自捕 `1`）
**母體** ＝ 二 CSV **逐檔分列**／**粒度框** ＝ **blob bytes ＋ 列框（CSV 資料列）＋ 集合基數**
落檔 ＝ `verify/out/WG9268pR_s8_reach.log`

| 情境 | bytes | 資料列 | 欄數 | 可達街廓（**出現序**）| 逐街廓宗數 | 未產出 |
|---|---|---|---|---|---|---|
| `0m` | **`15979`** | **`21`** | `53` | `['R1', 'R2']` | `{R1: 7, R2: 14}` | `R3`／`R4`／`R5`／`R6` |
| `3.5m` | **`33670`** | **`47`** | `53` | `['R1', 'R2', 'R4', 'R6', 'R5']` | `{R1: 8, R2: 16, R4: 5, R6: 10, R5: 8}` | `R3` |

⇒ ✅ **與單 `S-8` 之期初表逐格相符**（`0m` `15979` B／`21` 列／`R1:7 R2:14`；`3.5m` `33670` B／`47` 列／`R1:8 R2:16 R4:5 R6:10 R5:8` 逐值對拍；`R3` 二情境皆不可達）。
🔒 **此表為 `§三 P-8`（可達性只增不減）之比較端**。

🟢 **`S-0`〜`S-8` 全數通過**（`S-0b` 之補令 `P-5` 係量測器偽陽·已具名）。

---

## §二　工項 `c0`（**零生產碼**）：錨之定位（純診斷）

### `c0-1`　錨之**生產碼落點**（機械現查·⛔ 憑單或報告指認）

**母體（正面列舉）** ＝ `app.py` ＋ `verify/stepg_pipeline.py` **二檔**／**粒度框** ＝ **列框**
**字樣錨**（`grep -F` 固定字串·⛔ 行號為錨·`行號衛生` 第 `4` 款）

#### （甲）**錨之取值點**（`4` 處·二檔 × 左右鏈）

| # | 檔 | 字樣錨 | 所在函式／區塊 | 其值之來源欄 |
|---|---|---|---|---|
| `1` | `app.py` | `baseline_pt = (corner_pt + left_cum_S * d_hat` | `main()` 內之街廓迴圈·**左鏈** | `corner_pt`（街角點）＋ `left_cum_S` |
| `2` | `app.py` | `baseline_pt = (end_pt + right_cum_S * d_hat_rev` | 同上·**右鏈** | `end_pt` ＋ `right_cum_S` |
| `3` | `verify/stepg_pipeline.py` | `baseline_pt = (corner_pt + left_cum_S * d_hat` | `run_step_g` 之同構迴圈·**左鏈** | 同 `1` |
| `4` | `verify/stepg_pipeline.py` | `baseline_pt = (end_pt + right_cum_S * d_hat_rev` | 同上·**右鏈** | 同 `2` |

#### （乙）**錨之推進點**（`4` 處）

| # | 檔 | 字樣錨 | 推進量之來源欄 |
|---|---|---|---|
| `1`／`2` | `app.py` | `left_cum_S += _S_actual` ／ `right_cum_S += _S_actual` | `_S_actual = float(res.get('S_raw', res.get('S', 0.0)))` |
| `3`／`4` | `verify/stepg_pipeline.py` | 同上二字樣錨 | 同上 |

🔑 **唯一性之判別力自檢**：上開六個字樣錨於其檔**各命中 `1`**；
寬字樣 `left_cum_S`／`right_cum_S` 於 `app.py` ＝ **`25`** 列、於 `stepg` ＝ **`13`** 列
⇒ **該檢⛔ 恆為 `1`**。

#### （丙）🛑 **語意之判（單 `c0-1` 之停機款所繫）**

🔒 **實測所得之語意**（逐字·⛔ 自擬）：宗 `j+1` 之起算點 ＝
**`前一宗(j) 之起算點` ＋ `前一宗(j) 之標稱 S`**（`S_raw`），而**⛔ 前一宗多邊形沿 `d̂` 之幾何極值**。

🔴 **⇒ 其與 `零-2` 錨 `3` 之逐字「現行實作將錨取在第 0 宗之<u>起點</u>」<u>不完全相符</u>**：
> · **相符之部分**：該錨確**⛔ 取在前一宗之<u>終點</u>**（＝幾何極值 ＝ 藍影外邊界 ＝ `S_req`）——受詞之**否定面**成立。
> · **不相符之部分**：其亦**⛔ 逐字為「前一宗之起點」**，而係「**起點 ＋ 標稱 `S`**」（＝**標稱終點**）。
> 二者之差 ＝ **標稱終點 vs 幾何終點**；當遠側界斜交時二者相異，該差即破量之來源。

🛑 **本項⛔ 自行改寫受詞**（單 `c0-1` 逐字令）⇒ **列為 `§六` 所呈之題**。

### `c0-2`　二檔**同源否**（逐行對照·段界機械）

**段之界定（機械·⛔ 目視取界）** ＝ 自該檔之 `baseline_pt = (…cum_S…` 起，至同鏈之 `cum_S += _S_actual` 止（含）。
**正規化** ＝ **只去前導空白**（二檔縮排層數不同係結構之別）⇒ 相異列即**真分歧**。
落檔 ＝ `verify/out/WG9268pR_c02_samesource.log`

| 鏈 | `app.py` | `stepg_pipeline.py` | 共同列 | 相似度 | 相異列 |
|---|---|---|---|---|---|
| **左鏈** | `32` 列 | `34` 列 | `27` | **`0.8182`** | **`12`** |
| **右鏈** | `55` 列 | `52` 列 | `44` | **`0.8224`** | **`19`** |

🔒 **判 ＝ <u>同源複製而已分歧</u>**（⛔ 二檔各自獨立實作）。**分歧之逐項具名**：

| # | 分歧 | 只在 `app.py` | 只在 `stepg` |
|---|---|---|---|
| `1` | 站號註解 | `站 1／4（app 左鏈）`／`站 2／4（app 右鏈）` | `站 3／4（harness 左鏈）`／`站 4／4（harness 右鏈）` |
| `2` | `_lot_gate` 之取用 | `res['_lg_cols'] = _lot_gate(` | `res['_lg_cols'] = ns['_lot_gate'](` |
| `3` | 🔴 **`W_0`／首宗下限之 thread** | **⛔ 有** | `if not _W0_left_set:` ／ `_W0_left = float(res.get('W_near', 0.0)); _W0_left_set = True` ／ `_Wfirst_left = float(res.get('W', 0.0))`（右鏈同構）|
| `4` | UI 段 | `# 極端防呆 2 後援：右側起點數值微修` ＋ `if _commit:` ＋ `st.info(…右側起點數值微修 {_adj}m…)` | **⛔ 有** |
| `5` | `_S_actual` 之尾註 | `…#20 四處同改·app 左/右＋stepg 左/右）` | `…#20 四處同改）` |

🔒 **分歧 `3` 與單 `零-3` 之預告逐字相符**（「`stepg` 另有 `_W0_left`／`_Wfirst_left` 之取值，`app.py` 無」）✅
🟢 **錨本身之二列（`baseline_pt = (…` 與 `cum_S += _S_actual`）於二檔<u>逐字相同</u>**（⛔ 列於相異）
⇒ **`c1` 若動該錨，二檔須同改**（單 `零-3` 之「只改 `app.py` 則覆蓋帳結構上不可能歸零」由此坐實）。
🔑 **判別力對照**（證上開相似度⛔ 恆為 `1`）：取二檔各自之**首 `10` 列**（必然相異）⇒ 相似度 **`0.2000`** ✅

### `c0-3`　既有 shim 之**期初復現** 🔴 **停機款觸發**

**器** ＝ `verify/probes/probe_WG985_grouphead.py`（**唯讀·⛔ 改一字**）／`rc = 0`／`stderr` `27971` B
**母體** ＝ 其 `SB_MAIN = 0.0`（**僅 `0m`**·**逐街廓獨立呼叫** 口徑）／**粒度框** ＝ **對數**（重疊對）
落檔 ＝ `verify/out/probe_WG985_grouphead_bf1d92f.log`（**二跑逐位相同** ⇒ 可復現）

#### （甲）現態（`bf1d92f`）之**弦區間閉式重跑**

| 街廓 | 側 | `j` | **現況破量對** | **新錨(+ε) 後** | **原錨 後（負對照）** | 器之判 | 🔑 **自我驗證**（原錨 ⟺ 現況）|
|---|---|---|---|---|---|---|---|
| `R1` | 左 | `0` | `0` | `0` | `0` | ⚠️ 現況本即 `0` | — |
| `R1` | 右 | `2` | `0` | `0` | `0` | ⚠️ | — |
| **`R2`** | **左** | **`0`** | **`2`** | **`0`** | **`1`** | ✅ | 🔴 **`1 ≠ 2` ⇒ 不符** |
| `R3` | 右 | `5` | `0` | `0` | `0` | ⚠️ | — |
| **`R5`** | **左** | **`0`** | **`1`** | **`0`** | **`1`** | ✅ | 🟢 `1 ＝ 1` |
| `R6` | 右 | `3` | `0` | `0` | `0` | ⚠️ | — |

`POPULATION=11 PRINTED=6 SUPPRESSED=5`（`5` 格之 `j` 為該組**末二宗** ⇒ 無 `d≥2` 之對·`S_req` 未定義）
器之總判逐字：`新錨後重疊 ＝ 0 者 **6 ／ 6**；負對照（原錨）重疊 > 0 者 **2 ／ 6**`

#### （乙）與單 `零-2` 之**既有預測**逐位對拍

| 量 | 單 `零-2` 之逐字（源 ＝ `W-G.9-85`）| **本窗現態實測** | 判 |
|---|---|---|---|
| 新錨後重疊 ＝ `0` | **`5／5`** | **`6／6`** | 🔴 **母體之別**（比例仍為全數）|
| 負對照（原錨）重疊 > `0` | **`3／5`** | **`2／6`** | 🔴 **不符** |
| 有效母體（現況破量 > `0` 者）| **`3／3`**：`R2左j0`／`R3右j7`／`R5左j0` | **`2`**：`R2左j0`／`R5左j0` | 🔴 **不符** |

🔒 **成因之歸因（機械·⛔ 推定）**：單 `零-2` 所引之預測係量於 **`67cd1a4`**
（`2026-08-20`·倉內**已追蹤**之 log `verify/out/probe_WG985_grouphead_67cd1a4.log` 逐字載
`新錨 5／5`／`原錨 >0 者 3／5` ⇒ **與單逐位相符**）。
自 `67cd1a4` 至 `bf1d92f` 計 **`280`** 個 `commit`，**生產碼 `34` 檔中 `12` 檔相異**：
`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`／`verify/selection_pipeline.py`／
`verify/wf_f0.py`〜`wf_f4.py`／`verify/fixture_klui_t2diag.py`／`verify/fixture_wf_ns_wiring.py`
（含 `W-G.9-198R` 之**步驟 0 無條件／歸側四分支／名次／段二排序／段一上鎖** ⇒ **配地幾何已變**）。
⇒ 🔒 **母體與宗序之變動係<u>已落地之幾何變更</u>所致，⛔ 探針之缺陷、⛔ 本批所致。**
（宗序之位移可見：`R3右` 由 `j=7` 變 `j=5`／`R5右` `j=7→6`／`R6右` `j=6,7→3,4`／`R2右` `j=8→7`。）

#### （丙）🛑 **停機款之逐條自檢**（單 `§八` 第 `3` 款）

單 `§八` 逐字：**「`c0-3` 之 shim 二態同值，或原錨態⛔ 復現 `45.9766`／`56.3293`」**

| 肢 | 判 | 依據 |
|---|---|---|
| **肢一**：二態同值 | 🟢 **未觸發** | 新錨 `6／6` 為 `0`；原錨 `2／6` 為 `> 0` ⇒ **二態相異 ⇒ 判別力 ≠ `0`** |
| **肢二**：原錨態⛔ 復現 `45.9766`／`56.3293` 之**面積值** | 🟡 **無從驗**（⛔ 「驗不過」）| 該器之出艙**只有對數、⛔ 面積** ⇒ **結構上不可判**（`序 11 之一般化` 款 `三`：「無從回測」⛔ 等同「回測不過」）|
| **肢二′**：原錨態⛔ 復現**現況之破量對數** | 🔴 **觸發** | `R2·左·j0`：**現況 `2` 對**、**原錨重建 `1` 對** ⇒ **不符**。同格 `R5·左·j0` 相符（`1 ＝ 1`）|

🔒 **`現況 ＝ 2` 之獨立佐證（⛔ 單源）**：`S-6` 之**另一器**（`probe_WG9265_coverage_account.py`·
**全區連跑**口徑·⛔ 同源）於 `0m·R2·left` 亦得**恰 `2` 對**（`19.8142`／`26.1624`）⇒ **二器相符**。
⇒ 🔴 **不符者係<u>原錨之重建</u>，⛔ 現況之量測。**

🔒 **其機理（與 `c0-1` 丙相扣）**：該 shim 之 `原錨下限` 取 `w40.eval_lot(...)` 之 `st[0]`
（＝**前一宗遠側界之 FRONTLINE 截距**），而生產碼之錨為 `corner_pt + cum_S·d̂`
（＝**前一宗起算點 ＋ 其標稱 `S`**）。**二者係不同之量**；於 `67cd1a4` 其效果相合（`2 ＝ 2`／`2 ＝ 2`），
於現態已不相合（`1 ≠ 2`）。
🛑 **依 `CLAUDE.md`「探針還原內部幾何時，須以碼面自身之保證當自我驗證閘」——⛔ 該閘不過，不准據以下任何結論。**
⇒ **`c1` 之設計⛔ 得以本 shim 之模型為據。**

🩸 **併記（器之判準之界·照實）**：該器之 `判` 欄式為 `n_new == 0 and n_old > 0`，
**⛔ 檢 `n_old == n_now`** ⇒ 其於 `R2左j0` 仍印 `✅`。**器之綠⛔ 蘊含自我驗證閘綠。**

### `c0-4`　受詞之界（本批**⛔ 觸及**者之具名清單）

單 `c0-4` 所列之四者，於本批**逐位不變**——其機械證 ＝ **生產碼 `34` 檔期初 ＝ 期末**（`§九`）：

| # | ⛔ 觸及之受詞 | 本批之狀態 |
|---|---|---|
| `1` | 第 `0` 宗（街角地）之 `G` 與其分配範圍 | **逐位不變**（零生產碼 ⇒ 其計算路徑一字未動）|
| `2` | 街角評比之 `winner` | **逐位不變** |
| `3` | 藍影面積 | **逐位不變** |
| `4` | `K-9-23` 二閘之判定式 | **逐位不變**（`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 一字未動）|
| `5` | `K-9-17` 遞補 | **逐位不變**（且其為**病一** ⇒ `W-G.9-269`）|

---

## §三　補令一之三事：施行結果

| 項 | 內容 | 本窗之辦理 |
|---|---|---|
| `§一` | `S-1` operative 形更正（性質式·⛔ 計數式）| ✅ 已依 `a`〜`e` 逐款出艙（見 `§一 S-1`）；⛔ 就審查窗之白名單 `1` 自解重新停機 |
| `§二` 自解 `1` | 報告落點取 `(b)` | ✅ 沿用——本檔 ＝ `docs/reports/W-G.9-268pR_錨點更正.md`，與 `…_工項零_原封入倉與號占用.md` **並存·互不覆寫** |
| `§二` 自解 `2` | `Bash` heredoc 之阻斷 ⇒ 一律以 `Write` 落檔、以路徑呼叫 | ✅ 沿用·**⛔ 再自解**（本窗全部腳本皆以 `Write` 落於 scratchpad 後以路徑呼叫）|
| `§三-1` 加款 `⑨` | ASCII 變體形視同同一號·三軸雙形分列 | ✅ 已施行（`S-1` 款 `a`／`c`）|
| `§三-2` | `CLAUDE.md` 之 `autocrlf` 記述更正（末端追加·零生產碼·併入 `c0`）| ✅ 已落地 ＝ `eb3aee0`（`numstat 75	0`·`deletions ＝ 0`·嚴格前綴 `True`）|
| `§三-3` | 保真鏈由發單側閉合 | ✅ 已收到；`S-0` 已對拍 `037538f1…`／`20407` B **逐位相符** ⇒ **⛔ 就此停機** |
| `§四` | 鑄號授權 ＝ `自誤 334`／`335`／`336`／`337` **四號** | 🔒 **本批⛔ 鑄任何號**（四簿 MAX 期初 ＝ 期末·見 `§九`）——其鑄造繫於本批之結案，而本批**停機** |

🩸 **`§三-2` 之照實更正（⛔ 頂替）**：補令逐字「本倉 `.git/config` 現為 `false`（**覆蓋全域之 `true`**）」
——實查該 `true` 位於 **`system`** 層（`C:/Program Files/Git/etc/gitconfig`），
**`global` 層⛔ 設值**（`rc = 1`）。⛔ 影響其結論，惟**層之具名須更正**；已載於 `CLAUDE.md` 檔末之更正註。

---

## §四　本批之**自解清單**（`自解款 一 5`·逐項五款）

### 自解 `1`　`wg9268_bing_csv.py` 之 `REPO` 常數指向**他窗**之工作樹

- **①疑義逐字**：該器之 `REPO = r"…\worktrees\wg9-268-review-window-42a7ad"`（**審查窗**之工作樹）。
- **②二個以上之讀法**：`(a)` 逕跑原器（其 `rc = 0`、輸出貌似正常）；`(b)` 於 scratchpad 產生**只改 `REPO` 一列**之副本並跑之；`(c)` 改倉內檔。
- **③所取者**：**`(b)`**。
- **④機械證據**：補丁之**相異列數 ＝ `1`**（列 `:13`）、**列數 舊 `46` ／ 新 `46`**（同）；
  二窗之 CSV 逐位對拍 ⇒ **`0m`／`3.5m` 皆相同** ⇒ 原器之誤讀**於本例恰無後果**（⛔ 正確）。
- **⑤所棄者之由**：`(a)` 係**靜默量到他窗之產物**——且因二窗 CSV 恰同 bytes 而**看不出來**；
  `(c)` 係**改倉內既有檔**，破 `可逆` 款。
- **五款自檢**：零土地後果 ✅／零生產碼 ✅（`verify/probes/**` ⛔ 在四檔清單，且本項**⛔ 動倉內任何檔**）／
  可機驗 ✅（相異列數 `1`·必紅組 ＝ 原路徑不存在時 `OSError`）／可逆 ✅（scratchpad 副本）／出艙具名 ✅（本節）。
- 🔴 **白名單外之新類**（`一-1` 八類皆不含）：**「倉內探針之硬編絕對路徑指向他窗工作樹」**——具名之。

### 自解 `2`　`S-0b` 於**補令**之 `P-5` 紅

- **①疑義逐字**：`[P-5] :63 SELF_SHA256 🔴 **不符**…｜單載 ### 三-3　單⛔ 載 \`SE…`
- **②讀法**：`(a)` 判為停機款（`P-5` 係機械停機款）；`(b)` 判為**量測器偽陽**並具名；`(c)` 修 `probe_order_preflight.py` 之解析。
- **③所取者**：**`(b)`**。
- **④機械證據**：見 `§一 S-0b（乙）` 之三項（`SELF_SHA256` 命中 `1` 列且為節題散文／`64` 碼十六進位命中 `1` 且係**原單**之值／器所報之「單載值」即該節題本身）。
- **⑤所棄者之由**：`(a)` 違「無從回測 ⛔ 等同回測不過」；`(c)` 係**放寬既有之閘** ⇒ **黑名單** ⇒ 恆停機。
- **五款自檢**：零土地後果 ✅／零生產碼 ✅／可機驗 ✅／可逆 ✅（⛔ 動任何檔）／出艙具名 ✅。
- 🔒 **白名單 `2` 之近親而⛔ 全同**：白名單 `2` 之要件為「CC 已證某框命中單載值」，
  而本例係**單根本⛔ 載值** ⇒ 具名為**白名單外之新類**：「單於散文中提及 `SELF_SHA256` 字樣，致 `P-5` 之受詞解析偽陽」。

### 自解 `3`　`S-2` 之 `ls-remote` 與**原單**所載期初態不符

- **①疑義逐字**：原單 `§首`「期初態 ＝ `95882de570fe840cf5ae8069ef6237eb5e83991c`」；實查 ＝ `2bcf623…`。
- **②讀法**：`(a)` 判為 `S-2` 不符 ⇒ 停機；`(b)` 以**補令** `§首` 之 `2bcf623…` 為準。
- **③所取者**：**`(b)`**。**④證據**：補令 `§首` 逐字載期初態 ＝ `2bcf623…`，且 `§首` 逐字「相斥處**以本補令為準**」。
- **⑤所棄者之由**：`(a)` 將使一個**已由發單側更正**之陳舊值否決整批。
- **五款自檢**：五款全滿足 ✅。**白名單 `8`**（令載之數與實測相異而該數係裝飾性）之近親——惟此處係**發單側已更正**，更近於**單之更新**，具名之。

---

## §五　🩸 本批之**自捕**（照實·⛔ 隱去·各載 形／徵候／攔法）

| # | **形** | **徵候** | **攔法** |
|---|---|---|---|
| `1` | **倉內探針硬編他窗路徑 ⇒ 靜默量到他窗產物** | 原器 `rc = 0`、輸出格式完好、`R3` 未產出等結論**看起來全對** | 讀其 `REPO` 常數 ⇒ 發現指向 `wg9-268-review-window-42a7ad`；再逐位對拍二窗 CSV（**恰同 bytes** ⇒ 本例無後果·⛔ 正確）|
| `2` | **`sed` 之替換端把反斜線讀為轉義** | 補丁後之 `REPO` 變成 `C:SERS\x07DMINDESKTOP…`（`\U`／`\a`／`\D` 被吃掉）⇒ `OSError: [Errno 22]` | 改以 `python re.sub` 落檔並**印出相異列之新舊逐字** ＋ **相異列數須 ＝ `1`** |
| `3` | **`bash` 單引號內之 `` \` `` 係<u>字面反斜線</u> ⇒ 窄錨 `grep` 得偽 `0`** | 落地後查窄錨命中，得 **`0`**（而其應為 `3`）；🔒 **「`0` 最該懷疑」** | 改以 `python` 復算（窄錨 `1 → 3`／寬 `autocrlf` `2 → 12`），並回頭**就地更正**已寫入之自指增長數 |
| `4` | **拋棄式 clone 之 `-c core.autocrlf=false` ⛔ 及於其後之 `checkout`** | clone 後 `app.py` 工作區 `1479398` B ≠ blob `1454937` B（`CR 24461`）| 於 clone 內 `git config core.autocrlf false` ＋ `read-tree` ＋ `checkout-index -a -f` ＋ `reset --hard`，再驗 `sha256` 前 `16` 碼 ＝ `1e15d45ae84b02ca` 方起跑 |
| `5` | **`git checkout-index -a` 於 `.git/index` 已刪時靜默寫不出東西** | `porcelain` 由 `0` 暴增為 `1779` | 補 `git read-tree HEAD` 重建 index 後再 `checkout-index`；以 `porcelain ＝ 0` 收斂 |
| `6` | **器之綠⛔ 蘊含其自我驗證閘綠** | `probe_WG985_grouphead` 於 `R2左j0` 印 `✅`，而其 `原錨 1` ≠ `現況 2` | 自行加驗 `n_old == n_now`（器**⛔ 檢之**）⇒ 即 `§二 c0-3` 丙之停機款 |

---

## §六　🛑 **停機上呈**

### 一　停機款（單 `§八`·逐條自檢）

| # | 停機款（逐字節略）| 判 |
|---|---|---|
| `1` | 開工閘任一不符（含 `S-1` 二號不可辨、`S-4` 期初態與 `e57d2ef` 相異）| 🟢 **未觸發**（`S-1` 可辨 ✅／`S-4` 逐位相符 ✅）|
| `2` | `c0-1` 所定位之碼位語意與 `零-1` 不符 | 🟡 **部分**——其**否定面**成立（錨確⛔ 取在終點），惟**⛔ 逐字為「前一宗之起點」**而係「起點 ＋ 標稱 `S`」⇒ **呈裁**（見 `二·題 A`）|
| `3` | `c0-3` 之 shim 二態同值，或原錨態⛔ 復現 `45.9766`／`56.3293` | 🔴 **觸發**（肢二′：`R2左j0` 原錨重建 `1` ≠ 現況 `2`）|
| `4` | `P-1` 於 `c2` 後仍有交集 `> 0` | ⬜ 未達（本批止於 `c0`）|
| `5` | `P-5`／`P-6` 轉紅 | ⬜ 未達 |
| `6` | `P-8` 任一街廓由可達轉不可達 | ⬜ 未達 |
| `7` | `P-9` 之類 `(iii)` 非空 | ⬜ 未達 |
| `8` | 須動及 `§零-3` 未列之檔或 `§零-4` 所排除之受詞 | 🟢 **未觸發**（本批零生產碼）|
| `9` | 單與正典相斥 | 🟢 **未觸發** |

### 二　🛑 **所呈之題**（⛔ 由 CC 自裁·候發單側／KL）

#### 題 `A`（**⛔ 土地後果·屬工程裁**）　`c0-1` 所定位之語意與 `零-2` 錨 `3` 之逐字不完全相符

現行碼之錨 ＝ **「前一宗之起算點 ＋ 其標稱 `S`」**（＝ **標稱終點**），
而 `零-2` 錨 `3` 逐字為「**現行實作將錨取在第 0 宗之<u>起點</u>**」。
🔒 **受詞之否定面（「⛔ 取在終點」）成立**，**肯定面（「取在起點」）⛔ 逐字成立**。
⇒ **`c1` 之受詞須先確定**：是「起點 → 幾何終點」，抑或「**標稱終點 → 幾何終點**」？
🛑 二讀法之**碼面改法相異**（前者須改二處：取值 ＋ 推進；後者只須改取值之基準），
**⛔ 由 CC 自裁**（單 `c0-1` 逐字：⛔ 自行改寫受詞）。

#### 題 `B`（🔴 **本批之停機主因**）　`c0-3` 之 shim 之**原錨負對照已不忠於現態**

`R2·左·j0`：**現況 `2` 對**／**原錨重建 `1` 對**。
🔒 其機理 ＝ shim 之 `原錨下限` 取 `w40.eval_lot` 之 `st[0]`（**前一宗遠側界之 FRONTLINE 截距**），
而生產碼之錨為 `corner_pt + cum_S·d̂`（**起算點 ＋ 標稱 `S`**）——**二者係不同之量**。
⇒ 依 `CLAUDE.md`「探針還原內部幾何時須以碼面自身之保證當自我驗證閘·⛔ 該閘不過不准據以下任何結論」，
**`c1` 之設計⛔ 得以本 shim 為據**。
🛑 **所需之裁**：`(i)` 另定一支**以生產碼之量為基準**之 shim（其受詞 ＝ `cum_S`），
抑或 `(ii)` 先修 `probe_WG985_grouphead` 之 `lim_old` 定式（🔴 **其為倉內既有探針·單明令唯讀** ⇒ 須發單側授權），
抑或 `(iii)` 逕以 `c1` 之旗標 `on／off` 二態對拍取代 shim。**⛔ 由 CC 自裁。**

#### 題 `C`（⛔ 土地後果）　單 `零-2` 之既有預測**已過期**

單所引之 `5／5`／`3／5`／有效母體 `3／3` 係量於 **`67cd1a4`**（`2026-08-20`·倉內 log 逐位可覆核），
其後 **`280` 個 `commit`／`12` 檔生產碼**已變（含 `W-G.9-198R` 之幾何變更）。
🔒 **本窗現態實測 ＝ `6／6`／`2／6`／有效母體 `2`。**
⇒ **請發單側更新 `零-2` 之驗收靶**（⛔ 由 CC 逕改單）。
🩸 **同族之戒**：`自誤 334` 之通則（「凡將他方所出之表列為閘之比較端，該表須先逐列自行復驗」）
於**時間軸**上之變體 ——**「他時之量」與「他方之表」同樣須先復驗方得為閘之比較端**。

#### 題 `D`（⛔ 土地後果·純程序）　`probe_order_preflight.py` 之 `P-5` 受詞解析

其取「檔內**最末**一個 `SELF_SHA256`」而**⛔ 檢其後是否為 `64` 碼十六進位**
⇒ **任何在散文中提及該字樣之單，其 `P-5` 必紅**（本補令即首例）。是否補其判準 ＝ **意思決定**。

### 三　⬜ 本批**未辦**之項（逐項具名·⛔ 靜默略過）

| 項 | 狀態 | 由 |
|---|---|---|
| `c1`（錨點更正 ＋ 旗標·**生產碼**）| ⬜ **未辦** | 🛑 停機款觸發（題 `B`）；且其**須 KL 逐 `commit` 放行** |
| `c2`（旗標 `on`·**幾何變動·有土地後果**）| ⬜ 未辦 | 同上 |
| `c3`（`contract` 段）| ⬜ 未辦 | 同上 |
| `c4`（差異表 ＋ `GB-67` 末端追加）| ⬜ 未辦 | 其受詞繫於 `c2` 之產出 |
| `§三` 十閘（`P-0`〜`P-9`）| ⬜ 未辦 | 其判定集繫於 `c1`／`c2` 之二態；`P-0` 之受詞見題 `A` |
| `§四` 差異表（表一／表二／表三）| ⬜ 未辦 | 繫於 `c2` |
| `§六` 併線前必答（五項）| ⬜ 未辦 | 繫於 `c2`；惟其第 `3` 項之**比較端**（`S-5` 期初值）已於本批取得 |
| `§七` 鑄號（`自誤 334`〜`337`）| ⬜ **未鑄** | 本批停機·四簿 MAX 期初 ＝ 期末 |
| 側分支 `verify/W-G.9-268p-c<n>` | ⬜ 未建 | 無生產碼 `commit` 可推 |

---

## §七　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**·⛔ 以「我判斷是零生產碼」代之）

指令（逐 `commit`）：
`git diff --name-only <前一已 push 之 HEAD>..<本 commit> | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'`

| `commit` | `numstat`（逐檔）| 生產碼判之輸出 | 判 |
|---|---|---|---|
| `bf1d92f` | `98	0	docs/orders/W-G.9-268p_補令一_S1形更正與三事處置.md` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ **得逕推** |
| `eb3aee0` | `75	0	CLAUDE.md` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ **得逕推** |

🔒 二 `commit` 之 `deletions` 欄**皆為 `0`** ⇒ **純附加**（`常規一 ②` 之補款：判準在有無刪除／改寫）。
🔒 `bf1d92f` 之 `push` 復驗（受詞逐字·⛔ 寫為「在 `origin`」）：
> **`bf1d92f` 為 `origin/wip/s1-endpart` 之<u>祖先</u>** ＝ **真** ✅
> **對照組**：`6cb77b7`（＝ `origin/main` 之 tip·**確在 `origin`**）為 `origin/wip/s1-endpart` 之祖先 ＝ **假** ✅
> ⇒ 該述詞**⛔ 恆真**。

---

## §八　自限復驗

| 項 | 值 | 判 |
|---|---|---|
| `git status --porcelain` | 逐檔列於 `§九`（僅本批新增之證據 log）| — |
| `left-right`（`origin/wip/s1-endpart...HEAD`）| 推畢後 `0	0` | ✅ |
| **生產碼 `34` 檔**：期初（`2bcf623`）vs 期末 | 受檢 **`34`** 檔／**相異 `0`** 檔 | ✅ |
| ↑ **判別力**（證該對拍⛔ 恆同）| `2bcf623` vs `67cd1a4`：受檢 `34` 檔／**相異 `12`** 檔 | ✅ |
| **四簿 MAX 未動**（母體 ＝ 各該**單檔**·框見 `wg9268_gate5_tally.py` 之 docstring）| 期初 ＝ 期末，逐簿逐位相同 | ✅ |

| 簿 | 相異 | MAX | 缺號集 `[MIN..MAX]` | 與補令 `§四` 所載期初 |
|---|---|---|---|---|
| **自誤** | `325` | **`332`** | `[106]` | ✅ 逐位相符 |
| **`GB`** | `154` | **`156`** | `[12, 87]`（🛑 依 `W-G.9-205` 更正註**⛔ 出艙**·此處只錄以證未動）| ✅ MAX 相符 |
| **`VR`** | `80` | **`95`** | `[73, 75]` | ✅ 逐位相符 |
| **`K-9`** | `28` | **`29`** | `[]` | ✅ 逐位相符 |

---

## §九　本批之落檔（證據·**逐檔 `git add`**·⛔ `git add -A`／`.`）

| 檔 | bytes | 內容 |
|---|---|---|
| `verify/out/WG9268pR_runverif_pre.log`／`.err` | `29176`／`0` | `S-4` |
| `verify/out/WG9268pR_runall_pre.log`／`.err` | `52904`／`0` | `S-5`（**拋棄式 clone** 內之實跑）|
| `verify/out/WG9268pR_cov_pre.log`／`.err` | `11347`／`0` | `S-6` |
| `verify/out/WG9268pR_occ_post.log` | — | `S-1` 六數 ＋ 逐筆歸類 |
| `verify/out/WG9268pR_occ_distinct.log` | — | `S-1` 款 `e` 二號可辨 |
| `verify/out/WG9268pR_s3_manifest.txt` | — | `S-3` 全 `34` 檔表 |
| `verify/out/WG9268pR_s8_reach.log` | — | `S-8` |
| `verify/out/WG9268pR_c02_samesource.log` | — | `c0-2` |
| `verify/out/probe_WG985_grouphead_bf1d92f.log` | `27822` | `c0-3`（探針自寫·**二跑逐位相同**）|

🛑 **本批⛔ 動 `verify/out/` 之任何既有檔**；上列皆為**新增**。
