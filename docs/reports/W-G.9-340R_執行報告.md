# `W-G.9-340R`　執行報告：主線快轉至 `2ec1eb2` ＋ 合成案器入倉 ＋ 三塊末端追加

> **受單** ＝ CC（`2026-09-24`）。**單** ＝ `docs/orders/W-G.9-340_輕量單.md`。**級** ＝ 輕。
> 🔒 本批⛔ 新增生產碼；主線所納之生產碼 `commit` 僅 `5689a53`（KL 放行·單 `§二`）。

---

## ①　逐 `commit` 與工項之對應

| 工項 | `commit`（全 `40` 碼） | 說明 |
|---|---|---|
| 零′ | （無 `commit`） | 遠端 `refs/heads/wip/s1-endpart`：前 `205de68cf4458b6a1c8963fb63cfffb40f751710` → 後 `2ec1eb2bf43616ce662d95dc633b4c71f366eb15`（真快轉·`push` 無 `--force`·首推即成） |
| 零 | `68bfc202f1210022d866e4e116e8adbf5162017e` | 本單原封入倉（`330`／`0`） |
| 一 | `a79ae8ff2876f56ed53dbdb951802094a0d3998e` | 合成案器入倉（`193`／`0`） |
| 二 | `fa676c43881ed52835b8e7c9ae399b9b71b0222c` | 三塊末端追加（`CLAUDE.md` `16`／`0`·`GB` 簿 `22`／`0`·自誤簿 `10`／`0`） |
| 三 | （本報告之 `commit`·自指 ⇒ 於對話出艙） | 本檔 |

工項零〜二於同一次 `push` 推上（`2ec1eb2..fa676c4`）；逐 `commit` 之生產碼判法（`常規一` 補款 `🔧 二`·四檔）命中皆 `0` 行。

## ②　停機款三值

| # | 值 |
|---|---|
| `1` | 🟢 未觸發（主線 `205de68`／側支 `2ec1eb2`／施工樹追蹤檔變動 `0`） |
| `2` | 🟢 未觸發（`F1` `14169` B·`d4d26f38…12df2b` 相符；側支樹 `rc 0`；主線樹 `rc 12`） |
| `3` | 🟢 未觸發（`push` 首推即成·無 `--force`） |
| `4` | 🟢 未觸發（①〜④ 皆符·見 `③`） |
| `5` | 🟢 未觸發（三塊與 `F1` 皆符；入倉 blob 與來源逐位相同） |
| `6` | 🟢 未觸發（工項一入倉後合成案器於主線之端 `rc 0`） |
| `7` | 🟢 未觸發（本單／器以 `git check-ignore -v` 實查 `rc 1`；本報告同查見 `⑥`） |
| `8` | ⚪ 收工閘於本報告 `commit` 推後量·出艙於對話 |
| `9` | ⚪ 工項四於本報告之後辦·出艙於對話 |
| `10` | 🟢 未觸發（⛔ 作任何「孰為正典」之判；⛔ 改塊、`F1`、命令一字） |

## ③　工項零′ 之出艙

**前置之合成案**（暫存 worktree ＝ 倉外 `C:/Users/admin/wg340t`·detach 於 `2ec1eb2`·跑畢已 `git worktree remove`）：

| 樹 | `rc` | 甲部 ✅／🔴 | 乙部 ✅／🔴 | 丙部 |
|---|---|---|---|---|
| 側支 `2ec1eb2` | **`0`** | `15`／`0` | `20`／`0` | 四行皆 ✅（見下） |
| 主 checkout（`HEAD` ＝ `205de68`） | **`12`** | `0`／`1`（`甲0`） | `9`／`11` | `⚪ 丙部略（乙部已紅·突變無從證判別力）` |

丙部四行（側支·逐字）：
- `丙1 main() 一處 forced_right=_fo_right → _fo_left ⇒ 乙部紅項 ['乙2 引數形逐呼叫 …']`
- `丙2 main() 一處 _right_chain_origin_s → _oblique_s_max（退回舊式） ⇒ 乙部紅項 ['乙1 _right_chain_origin_s 呼叫點（main／stepg）']`
- `丙3 main() 刪去 _front_p2_blk 取 p2 之一行 ⇒ 乙部紅項 ['乙5′ app _front_p2_blk 之他賦值恰一·取 _p2_fl', '乙5″ app 同分支 corner_pt 取 _p1_fl']`
- `丙4 main() 首宗斷言去 \`and not is_first_corner_r\` ⇒ 乙部紅項 ['乙7 首宗斷言 main／stepg 各恰一且同文']`

主線樹之紅項（`12`）：`甲0`、`乙1`、`乙5`×2（app／stepg）、`乙5′`×2、`乙5″`×2、`乙6`、`乙7`、`乙8`、`乙9`。

**快轉**：`git merge-base --is-ancestor 205de68… 2ec1eb2…` ⇒ `rc 0`。
**快轉後**：
① `git ls-remote origin refs/heads/wip/s1-endpart` ＝ `2ec1eb2bf43616ce662d95dc633b4c71f366eb15` ✅
② 遠端 heads ＝ `28` ✅
③ 生產碼 `34` 檔：vs `2ec1eb2` 相異 `0`；vs `205de68` 相異 `2`（`app.py`、`verify/stepg_pipeline.py`） ✅
④ `git branch -r --contains 5689a53…` ＝ `origin/verify/W-G.9-338-origin`、`origin/wip/s1-endpart` ✅

## ④　塊與 `F1` 之實得

| 物 | 列（單內） | bytes | `sha256`（前 `16`） | 受檔 改前 → 改後 |
|---|---|---|---|---|
| `P1` | `:162`–`:183` | `2820` | `22f701de0cd64584` | `GB` 簿 `979274` → `982094` |
| `P2` | `:189`–`:198` | `1692` | `4be076b8a667e1b3` | 自誤簿 `1032410` → `1034102` |
| `P3` | `:204`–`:219` | `1747` | `0df77519ebeea955` | `CLAUDE.md` `246114` → `247861` |
| `F1` | — | `14169` | `d4d26f38560ead00…`（全碼相符） | 新檔 |
| 本單 | — | `30663` | `68a0597714f608d7`（入倉 blob ＝ 來源） | 新檔 |

各塊之前一列與後一列逐字為圍欄（`` ````markdown `` ／ `` ```` ``）。三受檔：改前 blob 為改後全檔之嚴格前綴（`cmp -n`），追加尾段之 `sha256` 與各塊相同，`CR` 皆 `0`。

## ⑤　`§五-1` 基數之重算

| # | 單載 | 重算 |
|---|---|---|
| `1` 工項數 | `6` | `6` |
| `2` `§六` 款數 | `28` | `28`（表列 `28` 列） |
| `3` 所鑄之號 | 自誤 `1`／其餘 `0`；解除 `1` | 同（收工閘 `517 516` 之自誤相異 `502 → 503`·`GB` `178` 不變） |
| `4` 停機款數 | `10` | `10` |
| `5` 塊 | 見上 | 全符 |
| `6` `F1` | `14169` B·`d4d26f38…` | 全符 |
| `7` 逐筆放行清單 | `2` 筆／動生產碼 `1` | `git rev-list --count 205de68..2ec1eb2` ＝ `2` |
| `8` pre-flight | 紅 `0`／🟡 `0`；`P-3` `15127`-`24794` | `rc 0`·紅 `0`／🟡 `0`；`P-5` 相符；`P-3` `15127`-`24794` |

## ⑥　CC 之自捕

- 無攔下之誤。
- 記錄：`§零-0` 三中，`§一` 項 `8`（`probe_WG9330_wfns_ast.py`）另行實跑以對格（`rc 3`·`38`／`38`·恰缺 `_wg9268p_anchor_advance`），其不在 `probe_WG9321_issuer_anchor.py` 之出艙內。
- 合成案之出艙與暫存 worktree 皆置倉外（`C:/Users/admin/`），⛔ 入倉。

## ⑦　下一 CC 窗須知

- 主線已含起點族之修（`app.py` blob `0269a8c1…`）；主 checkout 須同步（工項四）並重啟介面方見之。
- 側支 `verify/W-G.9-338-origin` 保留為歷史（`2ec1eb2`·為主線之祖）。
- `GB-178` 失效條件 `(3)` 未成就（`GB-182` 與 `R6` `85.71 ㎡`）；依賴序見 `CLAUDE.md` 檔末新節。
- 主 checkout 之根之二來源檔由 KL 自行移除。
