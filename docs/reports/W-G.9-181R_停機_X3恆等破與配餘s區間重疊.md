# 🛑 `W-G.9-181R`　**停機上呈**：`X-3` 之恆等對含 5b 配餘之街廓結構上不成立

**受單**：`W-G.9-181`（發單 claude.ai·`2026-08-30`）　**執行**：CC
**基座（開工閘實測）**：`c255ca44c3206a7c4488fa7334e887854b841af0`
**性質**：🛑 **停機**（`X-3` 觸發 ⇒ 依 `X-8` 一律先停機）·⛔ **零生產碼**·⛔ **未辦 `N-1`／`N-2`／`M-F-3`**

> 🛑 **依 `X-8` 逐字**：「**停機款觸發一律先停機**（`W-G.9-177 §六 一`）：⛔ 不得先辦後呈；於停機回報中**併陳擬辦之意見**。」
> ⇒ 本報告 ＝ **停機回報 ＋ 擬辦意見**。`N-1`（鑄 `自誤 218`）／`N-2`（`GB-123` 補述入倉）／`M-F-3`（探針）**⛔ 全未辦**。

---

## `§零`　開工閘（`S-1` 七項·**全綠**）

| 項 | 期望 | 實得 | 判 |
|---|---|---|---|
| `origin/wip/s1-endpart` | `c255ca44c3206a7c4488fa7334e887854b841af0` | 同 | ✅ |
| `app.py` blob | `68f7bfab610ec78688b9d7322ae4bc2aac74493e` | 同 | ✅ |
| `app.py` 三數 | `1352132`／`22922`／`7c290b6f55f83a24` | 同（`wc -l` `22921`） | ✅ |
| `CLAUDE.md` | `70877`／`761`／`7d6b231719805338` | 同 | ✅ |
| 自誤登記表 | `379494`／`4204`／`b1cba6530f3c9d92` | 同 | ✅ |
| `W-G.4` 泛用阻塞項登記表 | `502503`／`3098`／`82227466ce53f9e9` | 同 | ✅ |
| `docs/驗證裁定登記表.md` | `330984`／`4229`／`bc60869c80aa4858` | 同 | ✅ |
| `W-G.9-180R` 報告 | `21875`／`256`／`6ab34c98e9be4b1f` | 同 | ✅ |

⇒ **六檔三數 `6/6`** ＋ 二錨（框 ＝ `splitlines()`·CRLF 皆 `0`）。
🔒 **單身完整性**：`SELF_SHA256` ＝ `0892407c0c1ca2ee66fa1d45ad6b4ce93126896a5400761a9d4a61e32033227a` ✅ MATCH。

---

## `§一`　🛑 觸發之款（**逐字引錄**·`GB-121` 施行式）

🔒 **來源管線**：KL 於聊天視窗**貼入之 `.md` 檔**（`W-G.9-181_施工單_四裁落地與R1R4池寬面積脫鉤之判.md`·`18954` B／`202` 列·CRLF `0`）。**標 `【單】`**。

> **`X-3`（逐字）**：「`M-F-1 (a)` 之算術自檢（寬和 ＋ 宗帶總寬 ＝ `s` 域全寬）殘差 `> 0.001` ⇒ **停機上呈**——若連此恆等都不成立，則二態之數其一必被誤讀。」　停機？ 🛑 **是**

> **`X-8`（逐字）**：「🛑 **停機款觸發一律先停機**（`W-G.9-177 §六 一`）：⛔ 不得先辦後呈；於停機回報中併陳擬辦之意見。」　停機？ 🛑 **是**

🔒 **其餘各款之現況**（⛔ 未觸）：`X-1`（四檔皆未改）／`X-2`（**⛔ 未跑任何探針**·全批純檔案）／`X-4`〜`X-7`（⛔ 未辦到該階段）。

---

## `§二`　🛑 `X-3` 之觸發實測（`M-F-1 (a)`）

🔒 **資料源（逐字·⛔ 純檔案）**：【倉】`verify/out/KL_UI_3.5m_{a3c97aa,2e08a41}_stdout.log` 之 `[T2-DIAG]` 列。

| 街廓·態 | 宗數 | `s` 域全寬 | 池帶寬和 | **殘差（全寬 − 寬和）** | Σ池（印） | Σ面積（算） |
|---|---|---|---|---|---|---|
| `R1`·`a3c97aa` | `6` | `88.0403` | `35.9007` | `+52.1396` ✅ | `1175.0918` | `1175.0918` |
| `R1`·`2e08a41` | `5` | `88.0403` | `53.4658` | `+34.5745` ✅ | `1175.0963` | `1175.0963` |
| **`R4`·`a3c97aa`** | `3` | `98.5404` | **`130.1509`** | 🔴 **`−31.6105`** | `1228.3533` | `1228.3533` |
| **`R4`·`2e08a41`** | `2` | `98.5404` | **`130.1697`** | 🔴 **`−31.6293`** | `1228.3568` | `1228.3568` |

⇒ 🛑 **`R4` 二態之殘差 `≫ 0.001`** ⇒ **`X-3` 觸發**。

🔴 **併報一項<u>方法論</u>之發現（`S-2` 照實）**：`X-3` 所書之恆等「**寬和 ＋ 宗帶總寬 ＝ `s` 域全寬**」，其中「宗帶總寬」**⛔ 未由 `[T2-DIAG]` 印出** ⇒ 若由該恆等**反推**宗帶總寬，則殘差**恆為 `0`**、該式**⛔ 不可自我檢驗**。
⇒ CC 改以其**唯一可證偽之形式**判之：**池帶寬和 ≤ `s` 域全寬**（宗帶總寬 ≥ `0`）。`R4` 二態下宗帶總寬推得 **`−31.61`／`−31.63`（負）** ⇒ 恆等**破**。

---

## `§三`　🔑 成因已查明（**純檔案 ＋ 碼面·⛔ 未跑任何探針**）

### `§三-1`　機制：**5b 幾何餘之 `s` 區間可重疊 ⇒ `kept_iv` ⛔ 不互斥**

🔒 **碼面逐字**（`app.py`·`grep -n "kept_iv.append(_riv" app.py` ⇒ `:9476`）：

```
            _riv = _strip_s_range(_rg, d_hat, corner_pt, allocation_dir)
            pieces.append(_rg)
            kept_iv.append(_riv if _riv is not None else (s_min, s_max))
```

⇒ **步驟 5b（`K-9-5-7` 配餘·`D-2b-24`）** 對每一幾何餘 `_rg` 取其 `_strip_s_range` 並 **append 進 `kept_iv`**。
🔒 **步驟 4 之 `pool_iv` 係<u>補區間</u>（by construction 互斥）**，但 **5b 之追加⛔ 無互斥保證**——碼面註解逐字即載其來源為「凡落在 *某宗之 s 帶內、卻不屬該宗多邊形* 之土地」。
⇒ 🔑 **`kept_iv` 於 5b 之後⛔ 不再互斥** ⇒ **池帶寬和可 > `s` 域全寬**。

### `§三-2`　`R4` 之逐帶坐實（配餘列逐字·【倉】`stdout.log`）

| 態 | 配餘（逐字） | 其 `s` 寬 | 對應之 `[T2-DIAG]` 池帶 |
|---|---|---|---|
| `a3c97aa` | `幾何餘 26.0938㎡ … s∈[3.7114,47.7107]` | `43.9993` | 池帶 **3**（`43.9993`） |
| `a3c97aa` | `幾何餘 27.4390㎡ … s∈[83.0368,84.6759]` | `1.6391` | 池帶 **4**（`1.6392`·2dp） |
| `a3c97aa` | `幾何餘 4.9632㎡ … s∈[49.5299,98.5477]` | `49.0178` | 池帶 **5**（`49.0178`） |
| `2e08a41` | `幾何餘 27.4425㎡ … s∈[83.0368,84.6948]` | `1.6580` | 池帶 **4**（`1.6580`） |

⇒ **`R4` 之 `5` 池帶中 `3` 個係 5b 配餘**；`[3.7114,47.7107]` ∪ `[49.5299,98.5477]` 幾乎涵蓋整個 `s` 域 `[0.1759,98.7163]`，**且與池帶1 `35.3260` 及全部宗帶重疊** ⇒ 寬和 `130.15` > 全寬 `98.54` **⛔ 非誤讀，係構造使然**。

### `§三-3`　🔑 `R1` 之「寬增 `55×` 而面積逐位不變」**已完全解釋**

🔒 **配餘列逐字（二態·同一塊幾何）**：

| 態 | 配餘列（逐字） | `s` 區間 | 寬 | 面積 |
|---|---|---|---|---|
| `a3c97aa` | `🟢 [配餘] 街廓 R1：幾何餘 5.3835㎡ 入抵費地（\`K-9-5-7\` 配餘·s∈[17.2402,17.5649]）` | `[17.2402, 17.5649]` | `0.3247` | `5.3835` |
| `2e08a41` | `🟢 [配餘] 街廓 R1：幾何餘 5.3835㎡ 入抵費地（\`K-9-5-7\` 配餘·s∈[-0.3248,17.5649]）` | **`[-0.3248, 17.5649]`** | **`17.8897`** | **`5.3835`** |

⇒ 🔑 **同一塊配餘，面積 `5.3835` <u>逐位相同</u>，而其 `s` 區間之<u>下界</u>由 `17.2402` 變為 `-0.3248`**（＝ `s` 域之下界）⇒ 寬 `0.3247 → 17.8897`（`+17.5650`）。
⇒ 🔒 **單 `§一 N-3` 所列之三項異常，由此一事實同時解釋**：
- ① 寬 `0.3247→17.8897` 而面積 `5.3835→5.3835` 逐位相同 ⇒ **同一塊幾何、同一面積、不同 span**；
- ② Σ池僅 `+0.0045` ⇒ 因該帶**本已在池內**（`a3c97aa` 態即已是配餘），`2e08a41` 只是其 `span` 變大 ⇒ **⛔ 無新面積入池**；
- ③ `s` 域二態逐位相同、寬和之差 `17.5651` 與 ① 之增量互補 ⇒ **一致**，惟其「互補」係 `kept_iv` **不互斥**下之巧合，⛔ 非「宗帶轉池帶」。

### `§三-4`　🔴 量之定義：`_strip_s_range` 回傳**頂點 span**、⛔ 非實佔長

🔒 **倉內既有之具名**（`app.py` 之 `_pool_overlap_len_s` docstring 逐字·`grep -n "NOTE-1（reviewer 活抓）" app.py`）：

> ⚠️ **NOTE-1（reviewer 活抓）**：`_strip_s_range` 回傳的是**全體頂點 span**、非實占長——餵 MultiPolygon 會把片間空隙一併算進去（**雙片池虛胖**）。故本函式**先拆連通片、逐片取 s-區間長再相加**，禁直接對交集整體呼叫 `_strip_s_range`。

⇒ 🔑 **`[T2-DIAG]` 之「池帶 `s` 寬」對 5b 配餘而言，量的是<u>頂點 span</u>，⛔ 非該幾何之實佔長** ⇒ **「寬」與「面積」本即由不同量導出**，於分散／非凸幾何上必然脫鉤。
⚠️ 🛑 **`_pool_strips_for_block` 之 5b 恰恰「直接對整體呼叫 `_strip_s_range`」**（`app.py:9474`）——即該 NOTE-1 所**明文禁止**之用法，惟其受詞（`_pool_overlap_len_s`）與此處不同 ⇒ **⛔ 不逕判為違例**，具名待裁。

---

## `§四`　🛑 擬辦之意見（`X-8` 所令·⛔ 非結論）

1. **`X-3` 之恆等式須修正**。其現行書寫（「寬和 ＋ 宗帶總寬 ＝ `s` 域全寬」）**對含 5b 配餘之街廓結構上不成立**，且**⛔ 不可自我檢驗**（宗帶總寬未印 ⇒ 反推恆滿足）。擬改為下列之一：
   - **(i)** 限縮受詞為 **5b 之前**之 `pool_iv`（互斥）——惟該量**⛔ 未印於 `[T2-DIAG]`**，須另取；
   - **(ii)** 改判準為 **`union(kept_iv)` 之聯集寬 ≤ `s` 域全寬**（對重疊區間取聯集）；
   - **(iii)** 直接**撤除**該款——其所欲防之「誤讀」已由本節之機制解釋排除。
2. **`M-F-2` 之候選集⛔ 不窮盡**，須加 **(戊)**：
   > **(戊)**：「寬」取自 `_strip_s_range` 之**頂點 span**、「面積」取自幾何本身 ⇒ 二者**本即不同量**；於 5b 配餘（可為分散／非凸幾何）上必然脫鉤。**此係<u>量之定義</u>使然，⛔ 非缺陷、亦⛔ 非 (甲) 之 ghost 解釋。**
   ⇒ 🔒 **本批之純檔案證據已足以支持 (戊)**（`§三-3` 之同一塊配餘、同面積、異 span），且 **(甲) 已被排除**——消失之宗若係零面積 ghost，則其釋出之帶應成為**新**池帶或使既有池帶**面積**增加；實測面積 `5.3835` **逐位不變** ⇒ ⛔ 不合 (甲)。
3. **`M-F-3` 之閘 D／閘 E 之前提已變**：其設計係為判 (甲)（ghost 之帶寬 ≈ `17.5650` ∧ 面積 ≈ `0`）。而 `§三-3` 已證該 `17.5650` **⛔ 非某宗之帶寬**，係**同一塊配餘之 span 擴張**。⇒ **閘 D／E 縱使全中亦⛔ 不支持 (甲)**。擬請發單側重擬 `M-F-3` 之受詞。
4. **`覆蓋殘差` 自檢⛔ 無法區分諸支**（單已預見）：其為 `|union(宗+池) − 街廓|`，而 `union` 對重疊**免疫** ⇒ 二態皆 `0.0000` **符合預期**、⛔ 非漏檢。🔒 **且碼面已明載其自 `D-2b-24` 起為<u>構造恆真</u>**（`app.py` 之 5b 註解逐字：「本項使 `①' 覆蓋閘` 成為**構造恆真** … ⛔ **該閘之 `0` 自本批起不得作為任何驗收憑據**」）⇒ **本案⛔ 不構成新缺陷**。

---

## `§五`　🛑 射程外（`VR-082`·本停機回報**看不見**什麼）

1. ⛔ **未跑任何探針**（`M-F-3` 未辦）⇒ **⛔ 未取得 `R1` 之逐宗清單、`_GHOST` 之有無、其帶寬與面積**。(戊) 之支持係**純檔案推論**，⛔ 非探針實測。
2. ⛔ **`a3c97aa` 態⛔ 未量**（單 `§一 N-3` 已令⛔ 不辦跨態重跑）——二態之數皆取自**既有 log**，⛔ 非本批重跑。
3. ⛔ **未查** `R4` 之「宗數 `3→2`」所消失者為何宗（同 `1.`）。
4. ⛔ **未判** `app.py:9474` 直接對整體呼叫 `_strip_s_range` 是否違反其自身 NOTE-1 之禁令——二者受詞不同（`_pool_overlap_len_s` vs `_pool_strips_for_block`），**須發單側裁**。
5. ⛔ **未量** 其餘街廓（`R2`／`R3`／`R5`／`R6`）是否亦有 `池帶寬和 > s 域全寬` 之情形。

---

## `§六`　本批之異動（**⛔ 僅本報告一檔**）

| 檔 | 新增 | 刪除 | 性質 |
|---|---|---|---|
| `docs/reports/W-G.9-181R_停機_X3恆等破與配餘s區間重疊.md` | （見 commit） | **`0`** | `A`·新檔 |

🔒 **⛔ 未辦者逐項**：`N-1`（鑄 `自誤 218` ＋ 戒 `34`）／`N-2`（`GB-123` 補述入倉）／`M-F-3`（探針）／取號現查／`常規四（九）` 二項 —— **皆⛔ 未執行**，依 `X-8`「⛔ 不得先辦後呈」。
🔒 **⛔ 未動**：`app.py`（blob 期初 ＝ 期末 `68f7bfab…`）／`stepg_pipeline.py`／`run_all.py`／`run_verification.py`／`CLAUDE.md`／三登記表／`verify/**`（僅**只讀**）。
🔒 **報告命名（常規三）**：(a) `W-G.9-181R_停機_X3恆等破與配餘s區間重疊.md`（**採**——標題即載觸發款與其成因，日後由 `X-3` 或「配餘」回查皆命中）／(b) `W-G.9-181R_停機_R4池帶寬和逾s域全寬.md`／(c) `W-G.9-181R_停機_寬面積脫鉤之機制.md`。

---

## `§七`　🛑 呈發單側裁

1. 🛑 **`X-3` 之處置**（`§四 1`）：其恆等式對含 5b 配餘之街廓**結構上不成立**，且**⛔ 不可自我檢驗**。請裁採 (i)／(ii)／(iii) 之何者，或另擬。
2. 🔑 **`M-F-2` 之候選集⛔ 不窮盡**（`§四 2`）：請追認 **(戊)**（寬 ＝ 頂點 span／面積 ＝ 幾何 ⇒ 本即不同量）。🔒 **(甲) 已由純檔案排除**（同一塊配餘·面積逐位不變）。
3. 🛑 **`M-F-3` 之受詞已失效**（`§四 3`）：閘 D／E 縱全中亦⛔ 不支持 (甲)。請重擬——CC 建議改為「**逐帶列出 `kept_iv` 之 `s` 區間與其幾何面積，並標其係步驟 4 之補區間抑或 5b 之配餘**」，並加閘：**5b 配餘之 span 與其實佔長之比**。
4. 🟡 **`app.py:9474` 是否違反 NOTE-1**（`§三-4`）：該 NOTE 明文「禁直接對交集整體呼叫 `_strip_s_range`」，而 5b 正是如此；惟二者受詞不同。**請裁**是否立 `GB`。CC ⛔ 未自鑄（本批⛔ 未取號）。
5. 🔒 **本批⛔ 未鑄任何號**，`自誤 218`／戒 `34` **仍為未鑄**——若發單側裁「續辦」，該二號仍可用（CC 本批⛔ 未出艙任何人造受詞，故 `S-4` 清單維持 `8` 項）。
