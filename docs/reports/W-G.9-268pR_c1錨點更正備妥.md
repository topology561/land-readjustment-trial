# `W-G.9-268pR`　`c1` 備妥（**旗標預設 `off`·行為逐位不變**）＋ `自誤 333` 補鑄

> **受詞** ＝ `W-G.9-268′` **補令三**（`docs/orders/W-G.9-268p_補令三_新靶取代與累積形約束.md`）。
> 🔒 與 `W-G.9-268pR_錨點更正.md`／`…_c0-5新靶重得.md`／`…_工項零_原封入倉與號占用.md` **並存·互不覆寫**。
> 🛑 **本報告本身⛔ 生產碼**；`c1` 之生產碼 `commit` **只推側分支** `verify/W-G.9-268p-c1`，
> **主分支原地不動**（`W-G.9-206 §一`）。
> 🔒 **推序之偏離已依 `W-G.9-194 裁 四` 處置**：本報告**先推主分支**，`c1` 之 `commit` 置其後
> ——否則回報所繫之報告將落於**不可 push 之區間**。

---

## §零　三軸之總宣告（`VR-091`）

| 軸 | 值 |
|---|---|
| `(1)` 來源 `commit` | **期初** ＝ `5b05da0d345ef7a4def83ff28246c716428fc1e9`（`ls-remote` 實查·與補令三 `§首` **逐位相符**）／工項零 ＝ `9bf625f`／`自誤 333` ＝ `d079f5d`（**＝ `c1` 之基座**）|
| `(2)` 母體 | 逐節分列 |
| `(3)` 粒度框 | **檔框**／**列框**／**blob bytes**／**面積（㎡）**／**長度（公尺）** |

---

## §一　工項零：補令三原封入倉（`9bf625f`）

| 量 | 值 |
|---|---|
| 落點 | `docs/orders/W-G.9-268p_補令三_新靶取代與累積形約束.md` |
| 落檔前來源 `sha256` | `8b9f18298c84d48c44f20195e4786fd150b8705821407e1fdf71e193f2dab97b` |
| 入倉 blob `sha256` | **同上**（逐位相符）｜`bytes 5870`／`CR 0`／`LF 78`｜`cmp` 空 |
| `numstat` | `78	0` ⇒ **`deletions ＝ 0`** |

🔒 **`S-2`**：`ls-remote` ＝ `5b05da0…` ⇒ 與補令三 `§首` **逐位相符**；`left-right ＝ 0	0`；`porcelain ＝ 0`。
🔒 **`S-1`**（性質式）：受詢二形聯集 `D1 6／D2 10／D3 7／雙屬 1／列框 16／檔框 8`；
命中**全落於本單族**（原單／三補令／四報告）⇒ **第三方 `0` 筆**；
對照甲 `W-G.9-268` `檔框 11／列框 28`（須 `≥ 1`）✅；對照乙（人造·執行期組出）全 `0` ✅。
🔒 **側分支之占用現查**：`git ls-remote origin refs/heads/verify/W-G.9-268p-c1` ⇒ **空**（未占用）。

---

## §二　`自誤 333` 之補鑄（`d079f5d`·補令三 `§四`）

🔒 受詞**逐字轉錄**自 `docs/orders/W-G.9-268_施工單_GB67開修_…md` 之 `§七`（**⛔ 改寫**），
末加補令三所令之加註（「其鑄延至 `W-G.9-268′` 補令三；延宕之由 ＝ `W-G.9-268` 於 `c0` 停機、其授權隨之失效」）。

🔒 **`常規四（九）二` 之機檢**（器 ＝ `verify/probes/wg9268_gate5_tally.py`·**定義框 ＋ 範圍框**·母體 ＝ 登記表**單檔**）

| 檢 | 值 |
|---|---|
| ① 形之自證 | `333` 被定義框命中 ✅ |
| ② `MAX` | **`339`**（⛔ 推進——`333 < MAX` 係**補鑄**，⛔ 新號）／相異 `325` → **`332`** |
| ③ **缺號集** | **`[106]`** ⇒ 與補令三 `§四` 所令（「鑄畢後應為 `[106]`」）**逐位相符** ✅ |
| append-only | `678929` → `680626`（`+1697`·`numstat 18	0`）；`startswith` `True`／逆向 `False`；`CR 0` |

🩸 **本節之自捕 `1`**：首次以**自擬框**（定義框·**⛔ 含範圍框**）復算，得缺號集 `[106, 170–182]`
⇒ **量測器紅**（`170–182` 由**攢批範圍標題**承載，落於範圍框而⛔ 落於定義框）。
改以**倉內既有之器**復算方得 `[106]`。
🔒 **通則**：**缺號集之出艙一律用正典框（定義框 ＋ 範圍框·`VR-091 四`），⛔ 自擬框。**
🩸 **本節之自捕 `2`**：`d079f5d` 之 commit message 載 `numstat 22 0`，**實為 `18 0`**
——出艙前未當場復算（`數字錨引倉檔` 之違）。⛔ 改寫該 commit message（⛔ 事後修史）；於此具名。

---

## §三　`c1`：起算垂線之錨點更正（**expand 並存段**·旗標預設 `off`）

### 三-1　受詞與形

🔒 **受詞**（補令二 `§一` 逐字·補令三 `§一` 正式取代原單 `§零-2`）：
> 起算垂線之錨，由「前一宗之**標稱終點**（起算點 ＋ `S_raw`）」
> 改為「前一宗之**幾何終點**（其遠側界離開街廓之點 ＝ `S_req`·**帶軸框**·⛔ 框甲）」。

🔒 **形**（補令三 `§二`）：**累積形**——以**當下**之幾何極值為推進基準，
⛔ 逐格獨立套用落檔之 `累積S` 2dp 值。

### 三-2　異動（**純附加**·`deletions` 逐檔為 `0`）

| 檔 | `numstat` | 內容 |
|---|---|---|
| `app.py` | **`39	0`** | 新函式 `_wg9268p_anchor_advance`（緊接 `_oblique_s_max` 之後）＋ 左右鏈**二呼叫點** |
| `verify/stepg_pipeline.py` | **`10	0`** | `ns` 綁定一列（＋註解一列）＋ 左右鏈**二呼叫點** |

🔒 **`s` 之取法沿用倉內單一真相源** `_oblique_s_max`（`_strip_axis` 帶軸框·`#20` 四處同源）
——**⛔ 另寫第二份幾何定義**。
🔒 **取 `max`**：⛔ 令錨後退；與 `c0-5` 累積形之 `if d <= 0: continue` **同語意**。
🔒 **旗標** ＝ 環境變數 `WG9268P_ANCHOR_GEOM`（預設 `off`）；`off` 時**原值原樣回傳**、**⛔ 觸任何幾何**。
🔒 **loud**：帶軸退化時經 `_strip_axis` **raise**（`no-silent-fallback`）——本函式**⛔ 吞之**。

### 三-3　禁改清單之現查（**逐項零異動**）

| 符號 | `app.py` | `verify/stepg_pipeline.py` | 判 |
|---|---|---|---|
| `_K923_EPS_ZERO` | `3` → `3` | `0` → `0` | ✅ |
| `_blue_shadow_tri` | `7` → `7` | `0` → `0` | ✅ |
| `_k923_gate1` | `2` → `2` | `0` → `0` | ✅ |
| `_k923_gate2` | `2` → `2` | `0` → `0` | ✅ |
| `_lot_gate` | `7` → `7` | `3` → `3` | ✅ |
| **`solve_G_binary`（字樣）** | `17` → **`18`** | `4` → `4` | 🟡 **見下** |
| **`solve_G_binary`（<u>AST 呼叫數</u>）** | **`1` → `1`** | **`0` → `0`** | ✅ |

🩸 **字樣 `+1` 之逐字歸因**：新增之唯一命中為 helper **docstring** 之
`🔒 **只移錨點**：⛔ 改面積目標、⛔ 解 `S`（⛔ 呼叫 `solve_G_binary`）。`
⇒ **⛔ 呼叫**。**AST 實查之呼叫數 `1 → 1`** ⇒ 原單 `c1` 之「⛔ 解 `S`（⛔ 呼叫 `solve_G_binary`）」**成立**。
🔒 **此即「字樣框⛔ 足以判碼面事實」之實例**——判準取 **AST**、⛔ `grep`。

### 三-4　🛑 驗收（性質）：旗標 `off` 下輸出與父 `commit` **逐位相同**

**母體** ＝ `verify/run_verification.py` 之全 `stdout` ＋ 四 CSV／**粒度框** ＝ **bytes ＋ 列**
落檔 ＝ `verify/out/WG9268pR_c1_off.log`（`29176` B）／`.err`（**`0`** B）

| 受詞 | 結果 |
|---|---|
| `stdout` bytes | **`29176`** ＝ 父 `commit` 之 `WG9268pR_runverif_pre.log`（`29176`）|
| `stdout` `diff` | **恰 `1` 列相異**（`diff` 之 `<`／`>` 二側·落檔 `WG9268pR_c1_off_vs_parent.diff`·`282` B）|
| 該列逐字 | `…\verify\stepg_pipeline.py", line **1195**, in _run_step_g_impl` vs 父之 `line **1185**` |
| **歸因（閉式）** | `stepg` 之 `numstat` ＝ `10	0`，三個新增區間 `@@ +387,2`／`@@ +853,4`／`@@ +962,4` **皆 < 1185** ⇒ `1185 + 10 = **1195**` **逐位對上** |
| **分類** | **類 II**（僅 traceback 行號不同·`wv_reconcile` 三分類）——🛑 依 `CLAUDE.md` **⛔ 自動視為同**，於此**逐項具名確認** |
| 四 CSV（`got_G值_退縮{0m,3.5m}_partial.csv`）| **逐位相同** ✅（`15979` B／`33670` B）|
| `stderr` | **`0`** B |

🔒 **⇒ 行為逐位不變**（唯一相異係**新增列數所致之 traceback 行號位移**，其非行為）。

### 三-5　🔑 判別力：旗標 `on` **須相異**（證非死碼）

落檔 ＝ `verify/out/WG9268pR_c1_on.log`（**`168311`** B·⛔ `29176`）／`.err`（**`0`** B）

| 受詞 | `off` | `on` | 判 |
|---|---|---|---|
| `stdout` bytes ／ 列數 | `29176` ／ `311` | **`168311`** ／ **`1031`** | ✅ **相異** |
| `②-宗 圍堵閘破` 之命中 | `0m·R2`（`45.9766 > 2.8906`）／`3.5m·R5`（`56.8186 > 1.5999`）| **⛔ 命中**（**未中止**）| ✅ |
| 落檔之形 | 只產 **`_partial`**（中止街廓 `'R2'`／`'R5'`）| 產**完整** `got_G值_退縮{0m,3.5m}.csv` | ✅ |
| `0m` 可達街廓（出現序）| `['R1','R2']`（`21` 列）| **`['R1','R2','R4','R6','R5','R3']`**（`59` 列）| ✅ |
| `3.5m` 可達街廓（出現序）| `['R1','R2','R4','R6','R5']`（`47` 列）| **`['R1','R2','R4','R6','R5','R3']`**（`60` 列）| ✅ |

🔴 **⇒ 旗標 `on` 下 `②-宗 圍堵閘` 不再破，且 `R3` 於二情境<u>首次可達</u>**
——與原單 `c2` 之預告（「本 `commit` 預期使管線通過原中止點（`0m·R2`／`3.5m·R5`）⇒ 後續世代將**首次可達**」）**逐字相符**。
🛑 **本報告⛔ 就此作成任何 `c2` 之主張**——`c2` 之驗收（`§三` 十閘、`§四` 差異表、`§六` 併線前必答）
**⛔ 在本批**；補令三 `§五` 逐字「**`c2`⛔ 在此放行之內**——`c2` 須另呈」。

🩸 **本節之自捕 `3`**：首次以 `_partial.csv` 對拍 `on` vs `off`，得「逐位相同」⇒ **比錯檔**
（`off` 中止 ⇒ 只寫 `_partial`；`on` 未中止 ⇒ 寫**完整**檔，`_partial` 係 `off` 之**遺留**）。
🔒 **通則**：**凡以落檔對拍二態，須先確認二態<u>各自寫了哪一個檔</u>**——
中止與否會改變**落檔之檔名**，而**舊檔仍在原地**（其 `mtime` 為前一次）⇒ 對拍到的是**同一個舊檔**。

### 三-6　🛑 合成案（`main()` 內之敘述·`run_all` ⛔ 單獨作為驗收依據）

🔴 **必要性（AST 實查）**：`c1` 之**四**呼叫點中，**二處落於 `app.py` 之 `def main()` 內**
（`def main` 區間 ＝ `(14918, 24498)`；二呼叫點於 `:22136`／`:22258`）。
`harvest()` 以 AST 跳過 UI、harness 走 `run_step_g`
⇒ **該二處從不被 `run_all`／`run_verification` 執行**
⇒ 依 `CLAUDE.md`「🔒 `main()` 內之敘述」款，**必須另附合成案**。

**器** ＝ `verify/probes/probe_WG9268p_c1_synth.py`（`rc = 0`·`stderr` **`0`** B）
落檔 ＝ `verify/out/WG9268pR_c1_synth.log`
**期望值之來源（⛔ 現跑回填·`fixture-provenance`）** ＝ `_strip_s_range` docstring 逐字之**恆等式**
`s(corner_pt + s0·d_hat) ≡ s0` ⇒ 以 `corner_pt + s_i·d̂ + u_j·n̂` 造點者，`max strip` **閉式恆為 `max(s_i)`**。

| 造 | 受詞 | 得 | 期 | 判 |
|---|---|---|---|---|
| 閉式自證 | `_oblique_s_max`（斜交 `3.7°`·⛔ 正交特例）| `7.2500000000` | `7.25` | ✅ |
| `a` | 旗標 **off** ∧ `s_geo ＝ 7.25 > cum_S ＝ 1.0` | `1.0` | `1.0` | ✅ |
| `a′` | 旗標 **未設**（⛔ `'0'`）| `1.0` | `1.0` | ✅ |
| `b` | 旗標 **on** ∧ `s_geo > cum_S` | **`7.25`** | `7.25` | ✅ **必變** |
| `c` | 旗標 **on** ∧ `s_geo < cum_S ＝ 9.0` | `9.0` | `9.0` | ✅ **⛔ 後退** |
| `d` | 旗標 **on** ∧ `cut_coords` `[]`／`None`／`d_hat None`／`base_pt None` | 皆 `3.5` | `3.5` | ✅ **⛔ 靜默造值** |
| `e` | 旗標 **on** ∧ **帶軸退化**（切線 ∥ 推進向）| **`RuntimeError`**（訊息出自 `_strip_axis`）| `raise` | ✅ **loud** |

🔒 **四呼叫點之 AST 實查**：總數 **`4`** ✅；引數形（去第 1 引數）**恰二形**
——左鏈 `(res.get('cut_coords'), d_hat, corner_pt, allocation_dir_block)`／
右鏈 `(res.get('cut_coords'), d_hat_rev, end_pt, allocation_dir_block)` ✅；
**落於 `main()` 內者 ＝ `2` 處** ⇒ **本合成案即其唯一驗收路徑**。

🩸 **本節之自捕 `4`**：合成案首版取 `ns = harvest()`，而 `harvest()` 回 **`(ns, fake_st)` 之 tuple**
⇒ `TypeError`。**⛔ 靜默**（其為 loud 之 `TypeError`）⇒ 即刻改為 `ns, _fake_st = harvest()`。

---

## §四　補令三各條之施行對照

| 條 | 內容 | 本窗之辦理 |
|---|---|---|
| `§一` | 新靶之正式取代（取代原單 `§零-2` 整段）| ✅ **採納**；`c0-5` 之靶值即其期初（`102.7952 ㎡` ⇒ 新錨後全 `0`）|
| `§二` | `c1`／`c2` 之**累積形**約束 | ✅ **已落實於 helper**（取當下幾何極值·⛔ 落檔 2dp 值）；`c1` 驗收增列之「`\|δ\| > 0.01` 恰 `3` 列」款繫於 `c2` 之逐格出艙 ⇒ **候 `c2`** |
| `§三` | 差異表量級之填補 | ✅ 已載於 `-268pR c0-5` `§二-3`（`3／42`·`+3.3879`／`+3.3222`／`+0.3969`·`Σδ +7.1133`／`Σ\|δ\| 7.1878`）|
| `§四` | `自誤 333` 之補鑄授權（**一號·⛔ 逾此**）| ✅ **已鑄**（`§二`）；缺號集 `[106]` **逐位相符** |
| `§五` | 續辦：`333` 補鑄 → `c1`（側分支·KL 已放行）→ `c2`… | ✅ `333` 已推主分支；**`c1` 只推側分支**；**`c2`⛔ 辦** |

🔒 **`§一` 之 `🩸 ⛔ 主張者`（照實併記·⛔ 由 CC 判）**：補令三 `§一` 自載
「`3.311774572` 與 `3.5m·R5·left` 之 `δ ＝ +3.3222` 差 `1.04e-2`，**逾捨入量級** ⇒ **⛔ 認定二者同一**；
其受詞候查」——**本批⛔ 就此作成任何主張**，⛔ 阻塞。

---

## §五　🛑 `c1` 之推送：**側分支**，主分支原地不動

🔒 **流程**（`W-G.9-206 §一` 逐字）
```
備妥（⛔ 推主分支）→ 推至側分支 verify/W-G.9-268p-c1
→ 發單側自側分支逐位元組復驗 → 發單側請示 KL → KL 放行
→ CC 將主分支 wip/s1-endpart 推進至該物件
```
🛑 **本窗辦至「推至側分支」為止**。**主分支停於 `d079f5d`**（＝ `c1` 之基座）。
🔒 **由（`常規二` 取保守項）**：補令三 `§五` 載「**KL 已於 `2026-09-10` 放行 `c1`**」，
而上開流程之**「發單側自側分支逐位元組復驗」一步尚未發生**（側分支於本批之前**不存在**）
⇒ 本窗**⛔ 逕行前進主分支**，候發單側復驗後另示。
🔒 **`c1` 之 `commit` 內容**：`app.py` ＋ `verify/stepg_pipeline.py` **二檔**（`deletions` 皆 `0`）。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 生產碼判之輸出 | 判 |
|---|---|---|---|
| `9bf625f`（補令三入倉）| `5b05da0..9bf625f` | **空輸出**（`0` 行）| 🟢 零生產碼 ⇒ 逕推 |
| `d079f5d`（`自誤 333`）| `9bf625f..d079f5d` | **空輸出**（`0` 行）| 🟢 零生產碼 ⇒ 逕推 |
| **本報告之 `commit`** | `d079f5d..<本 commit>` | **空輸出**（`0` 行）| 🟢 零生產碼 ⇒ 逕推 |
| **`c1`** | `<本 commit>..<c1>` | `app.py`／`verify/stepg_pipeline.py` ⇒ **`2` 行** | 🔴 **生產碼 ⇒ ⛔ 逕推·側分支** |

🔒 **判別力**（證該判⛔ 恆 `0`）：`67cd1a4..HEAD` 之同一指令得 **`4`** 行。

---

## §七　自限復驗

| 項 | 值 | 判 |
|---|---|---|
| `left-right`（本報告推畢後）| `0	0` | ✅ |
| **生產碼 `34` 檔**：期初（`5b05da0`）vs **本報告之 commit** | 受檢 `34` 檔／**相異 `0`** 檔 | ✅ |
| ↑ **判別力** | `5b05da0` vs `67cd1a4`：相異 **`12`** 檔 | ✅ |
| 四簿 MAX | 自誤 `339`（**未動**·`333` 係**補鑄**）／`GB` `157`（未動）／`VR` `95`（未動）／`K-9` `29`（未動）| ✅ |
| 自誤 缺號集 | **`[106]`**（`333` 已補齊）| ✅ 與補令三 `§四` 逐位相符 |

---

## §八　本批之落檔（**逐檔 `git add`**·⛔ `git add -A`／`.`）

| 檔 | bytes | 內容 |
|---|---|---|
| `verify/probes/probe_WG9268p_c1_synth.py` | — | `c1` 之**合成案**（`main()` 內敘述之唯一驗收路徑）|
| `verify/out/WG9268pR_c1_synth.log` | `4369` | 合成案之全輸出（`rc = 0`）|
| `verify/out/WG9268pR_c1_off.log`／`.err` | `29176`／`0` | 旗標 `off` 之實跑 |
| `verify/out/WG9268pR_c1_on.log`／`.err` | `168311`／`0` | 旗標 `on` 之實跑（判別力）|
| `verify/out/WG9268pR_c1_off_vs_parent.diff` | `282` | `off` vs 父 `commit` 之 `diff`（**恰 `1` 列**·類 II）|

🛑 **`c1` 之二生產檔⛔ 在本 `commit` 內**——其於**次一 `commit`**，且**只推側分支**。
