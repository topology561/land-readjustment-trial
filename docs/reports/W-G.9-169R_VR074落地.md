# 🚧 `W-G.9-169R`　`VR-074` **落地**（生產碼批·🛑 **未 push·待 KL 核可**）

**受單**：`W-G.9-169`（`SELF_SHA256 = 42e61eb3e5c06e72fd17e0b58dc033071c47508e2fea5e8e710b790ebf23f0ea`·CC 覆算相符·四反引號界線 `6` 列 ＝ `3` 塊）
**基座**：`a3c97aa0ada17389c465550e04d90388c346f670`
**性質**：🛑 **生產碼 ＋ 有輸出後果**（`16` 格）·⛔ 未重產任何 baseline／凍存名單

## ✅ §零　結論（先講）

| # | 檢 | 值 |
|---|---|---|
| `1` | 🔴 `Z-2a`（**主判準**） | 期末 − 期初 ＝ **`16`** 格·期初 − 期末 ＝ **`0`（∅）**·逐閘總數 **`72`／`72`／`64`／`64`**·**逐格命中預測集合** |
| `2` | `Z-1`（通道層） | **變更列數 `6`**（只在期初 `6` ＋ 只在期末 `6` ＝ 合計相異 `12`）·**逐列與 `168R C-4` 表相符**·**集外 `0` 列** |
| `3` | 🔒 旁證閘 | 另 `4` 條截斷通知列（`14`／`14`／`17`／`17`）**逐字不變** |
| `4` | 🔴 `Z-4`（**土地後果之硬看守**） | 二態直接對拍：指配 rows **全欄逐格相異 `0`**·**winner 欄相異 `0`** ⇒ **winner 零改變**·`S-5` **未觸** |
| `5` | `Z-3` | `14` tag 逐一之執行次數**期初＝期末**（合計 `260`·未宣告 tag `[]`）·三類斷言**零觸發** |
| `6` | `Z-5` | `D-1`〜`D-6` **全部依期轉紅／轉綠** |
| `7` | `§四` 十一項 | 符 **`11`** ／ 不符 `0` ／ 不可判 `0` |
| `8` | 🛑 **push** | **⛔ 未 push**——本批生產碼、`deletions > 0`、blob 必變 ⇒ **常規一四項不齊**（`S-8`）⇒ **待 KL 核可** |

---

## §一　開工閘（`S-1`）

| 錨 | 期望 | 實測 | |
|---|---|---|---|
| `git ls-remote origin refs/heads/wip/s1-endpart` | `a3c97aa0ada17389c465550e04d90388c346f670` | 同 | ✅ |
| `app.py` **期初** blob | `f285b91c9e5f97f13dd74508e1a403833bc75cb3` | 同 | ✅ |
| `app.py` **期末** blob（**絕對值出艙**） | 🔴 必變 | **`60b550b7f4d651cca5418fdf9807a0cc3248278d`** | ✅ `Y-9` |
| 工作樹（開工時） | 淨 | `0` 列 | ✅ |

**clone**（皆拋棄式）：`/c/w169a`（**長跑**·`C:\w169a` ＝ **`8`** 字元·與期初 log 之 `C:\w168a` **等長**）／
`/c/w169b`（對照探針 ＋ 編輯 ＋ 入倉）／`/c/w169c`（**未打補丁**之期初碼態·供 `Z-4` 二態對拍）。

---

## §二　`L-4` 之編碼形式：**常規三提案與核可**（＋ CC 之一處偏離·逐字回報）

**提案（CC）**：甲 複合原子名／乙 序列（tuple）／丙 新原子名。**發單側核可 ＝ 甲**。

🔴 **CC 之偏離（一處·⛔ 未逕自決定後才回報，於此逐字呈報）**：
甲案之示例寫作 `"identity+no_ghost3"`，**CC 實作採原子名 `no_ghost`**（⇒ `"identity+no_ghost"` 等）。
**理由**：`L-3` 已令 `no_ghost` 之**述詞換源**為 `ghost3` 且逐字「**名可留**」；若另立 `no_ghost3`，
則 `no_ghost` 與 `no_ghost3` 成為**二個同義原子**，**正違甲案自身之理由「原子集不膨脹」**。
🛑 **若發單側要求改回 `no_ghost3`，一行可改**（`_proj_pop_filter` 之 `elif _atom == "no_ghost":` ＋ `_PROJ_POP_DECL` 六值）。

---

## §三　落地內容（`L-1`〜`L-8`·逐項碼面錨）

| 項 | 碼面錨（`檔:符號`·⛔ 不綁行號） | 內容 |
|---|---|---|
| `L-1` | `app.py`　`grep -n "def _proj_pop_ghost3" app.py` | ghost 述詞之**投影序層唯一定義處**；三判準**全寫**；⛔ 未綁 `_is_ghost_sliver`、⛔ 未綁名稱 |
| `L-2` | `grep -n "def _proj_pop_key_guard" app.py`／`_PROJ_POP_GROW_KEYS` | loud 守護斷言（`G(㎡)`／`a 面積(㎡)` 任一現身即 `raise`）；🛑 ⛔ 無環境變數出口 |
| `L-3` | `grep -n 'elif _atom == "no_ghost"' app.py` | `no_ghost` **述詞換源**（`_is_ghost_sliver` ⇒ `_proj_pop_ghost3`）·**名留** |
| `L-4` | `grep -n "_PROJ_POP_DECL = " app.py`／`def _proj_pop_filter` | 六點宣告改複合原子名；`_proj_pop_filter` 依 `+` 拆解逐段套用；`_proj_pop_assert_passthrough` 改比**首原子** |
| `L-4` 實參 | `grep -n "_all_in_blk = \[tp for tp in" app.py verify/selection_pipeline.py`／`grep -n "_stage1_parcels = \[tp for tp in" app.py verify/stepg_pipeline.py` | **四處**建構式加 `¬ghost3`（各覆蓋 `2`／`3` 個 tag） |
| `L-4` ns | `grep -n '"_proj_pop_ghost3",' app.py` | 🔒 **同批**補列 `_WF_NS_NAMES`（`W-G.9-163` 之戒**第四度施行**）——`verify/` 側二處以 `ns["_proj_pop_ghost3"]` 消費，漏列則 app 生產路徑必 `KeyError` |
| `L-5` | — | `RESHAPE` 三點 **⛔ 未變** |
| `L-6` | `grep -n "VR-074\` 破：ghost 落入合併群" app.py` | `app:k6step0/_ordered` **⛔ 未加濾**、改加 loud 斷言（保住 `_name` ＝ 合併宗暫編地號·**有土地後果**） |
| `L-7`／`L-8` | — | ⛔ 未改任何既有序對拍一字；⛔ 未重產任何 baseline／凍存名單 |

**`py_compile`**：`app.py`／`verify/selection_pipeline.py`／`verify/stepg_pipeline.py` **三檔全過**。
**生產碼 `numstat`（期末 vs 基座）**：`app.py` **`92`／`17`**；`verify/selection_pipeline.py` `3`／`1`；`verify/stepg_pipeline.py` `3`／`1`。

🔒 **`L-4` `passthrough` 之特性（單 `§二` 令逐字說明）**：`app:v2/pre_seq` 走
`_proj_pop_assert_passthrough`、**⛔ 不執行 `_proj_pop_filter`** ⇒ 其宣告由 `passthrough` 改為
`passthrough+no_ghost` 係**宣告層之誠實化**（其母體已由上游二呼叫端濾），
🛑 **⛔ 非於該點新增了執行期濾式**。`_proj_pop_filter` 內之 `passthrough` 原子亦定為**恆等**。

---

## §四　`Z-1`　全文對拍（通道層）

| 項 | 期初 | 期末 |
|---|---|---|
| 檔 | `verify/out/runall_a98a99f_full.log`（在倉） | `verify/out/runall_W-G.9-169R_post_full.log`（本批入倉） |
| clone | `C:\w168a`（`8`） | `C:\w169a`（`8`·**等長**） |
| 列數／位元組 | `506` ／ `46006` | `506` ／ **`46004`** |
| `sha256` | `69e1eee8a32eae35e4f9968231609c3f363d0da052d2e7a8465a2c26a35c5a21` | **`51b06cf615c4915651144fc49928be1daa9ef3be778a7e33f9b795526f210615`** |
| `stderr` ／ `rc` ／ 結論列 | — | **`0` B** ／ `EXIT=1` ／ `RESULT: FAIL`（**准紅碼**） |

| 框（逐字） | 值 |
|---|---|
| **未正規化**（LCS） | 只在期初 `19` ／ 只在期末 `19` ／ 合計 `38` |
| **正規化後·變更列數** | **`6`** |
| **正規化後·合計相異**（只在期初 ＋ 只在期末） | **`12`** |

🛑 **二數並報之由（`VR-058 一`）**：`168R C-4` 之「`6` 列」係**變更列數**框；`6` 之另一表述為
「`6` 只在期初 ＋ `6` 只在期末 ＝ 合計相異 `12`」。**二者⛔ 非相異之量測，係框之別。**

**逐列對拍（與 `168R C-4` 表·⛔ 未改預測）**：

| # | 期初逐字 | 期末逐字 | |
|---|---|---|---|
| `1` | `[v3·診斷0m] ('R6', '右', '628-18(1)') 欄「原位次(投影序)」: baseline='9' got=6` | `[v3·診斷0m] ('R4', '右', '628-1(1)') 欄「原位次(投影序)」: baseline='3' got=2` | ✅ |
| `2` | `[無串聯0m] …628-18(1)… baseline='9' got=6` | `[無串聯0m] …628-1(1)… baseline='3' got=2` | ✅ |
| `3` | `…（**另 56 列未顯示**·本閘違規總計 68 列）`（`v3·診斷0m`） | `…（**另 60 列未顯示**·本閘違規總計 72 列）` | ✅ |
| `4` | 同上（`無串聯0m`） | 同上 | ✅ |
| `5` | `…（**另 48 列未顯示**·本閘違規總計 60 列）`（`v3·診斷3.5m`） | `…（**另 52 列未顯示**·本閘違規總計 64 列）` | ✅ |
| `6` | 同上（`無串聯3.5m`） | 同上 | ✅ |

⇒ 🔒 **集外 `0` 列** ⇒ `S-2` **未觸發**（`Y-1` ✅）。
🔒 **旁證閘**（`Y-2`）：另 `4` 條截斷通知列（`參數0m` `14`／`參數3.5m` `14`／`指配0m` `17`／`指配3.5m` `17`）
**⛔ 未出現於 diff** ⇒ **逐字不變** ✅。
🔒 **判別力自檢**：① 未正規化之二 `sha256` **相異** ＝ `True`；② 人造一處**診斷文字**之差 ⇒ 合計相異 `12` → `14` ⇒ **轉紅** ✅。

---

## §五　🔴 `Z-2a`　逐格違規全集（受詞層·**主判準**）

🔒 **探針 ⛔ 未改一字**：`verify/probes/probe_WG9168_viol_full.py`·`sha256 = e920e02efe400b70ffe0206fab4b1d3f068870c2d73f667b853d1b9c7c926775`·
`git diff --name-only` 對該檔 ＝ **空** ⇒ **期初＝期末逐位相同**。

| 閘 | 期初總數 | **期末總數** | 期末 − 期初 | 期初 − 期末 |
|---|---|---|---|---|
| `v3·診斷0m` | `68` | **`72`** | **`4`** | **`0`** |
| `無串聯0m` | `68` | **`72`** | **`4`** | **`0`** |
| `v3·診斷3.5m` | `60` | **`64`** | **`4`** | **`0`** |
| `無串聯3.5m` | `60` | **`64`** | **`4`** | **`0`** |
| **合計** | — | — | **`16`** | **`0`（∅）** |

**逐格對 `WG9167_predicted_diff.json`**（相異之 `(列鍵, baseline, got)` 組·四閘共用同四格）：

| 列鍵 | baseline | got（期末） | 預測新值 | |
|---|---|---|---|---|
| `('R1', '左', '628-37(1)')` | `3` | `2` | `2` | ✅ |
| `('R1', '左', '628-36(1)')` | `4` | `3` | `3` | ✅ |
| `('R1', '右', '628(5)')` | `6` | `5` | `5` | ✅ |
| `('R4', '右', '628-1(1)')` | `3` | `2` | `2` | ✅ |

⇒ 🔒 **期末新增之每一格皆命中預測集合 ＝ `True`** ⇒ `S-3` **未觸發**（`Y-3` ✅）。

### 🩸 CC 自捕（一）：`對拍靶` 之**子字串框偽陽**

首次以 `'對拍靶' in c['角色']` 篩預測集合之對拍靶格數，得 **`40`**——
🔴 因凍存之角色字串為 `凍存·⛔ 非對拍靶`，**含該子字串** ⇒ 偽陽（`in` vs `==` 之族）。
🔒 **正確框**：`'非對拍靶' not in c['角色']` ⇒ **`16`** 格（`對拍靶·全欄逐格` `8` ＋ `對拍靶·無串聯` `8`；凍存 `24`）。
🔒 **結論⛔ 未受影響**（該集合去重後之相異格恆為同四格），惟計數**已更正並具名**。

---

## §六　🔴 `Z-4`　PK winner **零改變**（土地後果之硬看守）

🩸 **CC 自捕（二）**：首次以關鍵字（`選中`／`winner`／`地號`）篩 winner 欄，得「winner 欄之違規 ＝ `0`」——
🔴 **該框係猜測**：真正之欄名為 **`【左】第1宗指配`／`【右】第1宗指配`**，關鍵字**漏抓** ⇒ 該結論**作廢**。
🔒 **改以直接二態對拍**（⛔ 不以關鍵字推斷）：於 `/c/w169c`（**未打補丁**·`app.py` blob ＝ `f285b91c…`）
與 `/c/w169b`（**已落地**）各跑一次 `run_corner_pk`（同機器／同資料／僅碼態相異），dump 指配 rows：

| 情境 | 列數（期初／期末） | 鍵序相同 | **全欄逐格相異** | winner 欄（**自欄集現查**）相異 | 診斷列鍵集相同 |
|---|---|---|---|---|---|
| `0m` | `6` ／ `6` | `True` | **`0`** | **`0`** | `True` |
| `3.5m` | `6` ／ `6` | `True` | **`0`** | **`0`** | `True` |

⇒ 🔒 **winner 零改變** ⇒ `S-5` **未觸發**（`Y-5` ✅）。
🔒 **側證**：`指配0m`／`指配3.5m` 二閘之違規總計 **`17`／`17`** 期初＝期末；`抵費地{0m,3.5m}` 皆 `0`。
🔒 **外部錨**：`167R §五 M-2b`（二情境之指配列有差異之鍵 ＝ `0`）——本批以 **(A) 態**獨立復現。
**證據檔**：`verify/out/WG9169_z4_winner_a3c97aa.json`。

---

## §七　`Z-3`　三類斷言零觸發 ＋ 逐 tag 執行次數

| tag | 期初（`168R A-1`） | **期末** | |
|---|---|---|---|
| `app:v2/pre_seq` | `18` | `18` | ✅ |
| `app:main/v2_caller` | `0` | `0` | ✅ |
| `stepg:v2_caller` | `18` | `18` | ✅ |
| `app:main/_rank_by_tpid` | `0` | `0` | ✅ |
| `sp:_rank_by_tpid` | `102` | `102` | ✅ |
| `stepg:_proj_rank` | `18` | `18` | ✅ |
| `wf_f0`／`wf_f2`／`wf_f3`／`wf_f4:_proj_order` | `0`×4 | `0`×4 | ✅ |
| `wf_f1:_order` | `0` | `0` | ✅ |
| `wf_f4:_order_fb` | `2` | `2` | ✅ |
| `wf_f4:_order` | `22` | `22` | ✅ |
| 🔒 `app:k6step0/_ordered` | **`80`** | **`80`** | ✅ |

**合計記錄 ＝ `260`**；**未宣告而出現之 tag ＝ `[]`**。
🔒 **零觸發之證**：三類斷言（`POP_SYNC`／`ORDER_INVARIANCE`／`NAME_DERIVATION`）任一 `raise` 皆會使
該 fixture／trunk `rc≠0` 並改變 log；`Z-1` 之 diff **僅 `6` 列且全屬預期** ⇒ **零 `raise`**（`Y-4`／`Y-7` ✅）。
🛑 **次數 `0` 之 `7` 個 tag 之成因**（同 `168R A-1`·⛔ 非「有跑到」一語帶過）：
`app:main/*` `2` 個 ＝ `main()` 內、`run_all` **結構性不可達**；
`wf_f0`〜`f4`／`wf_f1` `5` 個 ＝ trunk A 之 `②-宗 圍堵閘破[R2]` 中止 ⇒ **連坐不可達**。

---

## §八　`Z-5`　判別力對照（`D-1`〜`D-6`·全部於**記憶體複本／函式局部**·⛔ 未改倉內任何檔）

| # | 對照 | 期望 | 實測 | |
|---|---|---|---|---|
| `D-1` | 改宣告而**不**改實參 | `raise` | 正對照（實參已同步）**未 raise**；負對照（實參未濾）**raise**，逐字含 `對稱差=['_GHOST_(R1)']` | ✅ |
| `D-2` | 注入 `G(㎡)` 鍵 | `L-2` `raise` | 正對照未 raise；負對照 **raise**，逐字含 `鍵='G(㎡)'／暫編地號='628-37(1)'` | ✅ |
| `D-3` | 人造第 `17` 格 | `Z-2a` 停機 | 正對照 `16` ⇒ 不停機；負對照（多一格）`17`、（少一格）`15` ⇒ **皆停機** | ✅ |
| `D-4` | `_mem` 注入 `ghost3` 成員 | `L-6` `raise` | 正對照命中 `0` ⇒ 不 raise；負對照（注入 `_GHOST_(R4)`）命中 `1` ⇒ **條件成立 ⇒ raise** | ✅ |
| `D-5` | `L-3` 換源之零變化 | 逐位相同 | `TEMP_LAYER` `114`⇒`106`／`106`·`BUILD_LAYER` `49`⇒`47`／`47` ⇒ **二層逐位相同**；**判別力**：同層 `identity` 出 `114`／`49`（證 `no_ghost` 確有作用） | ✅ |
| `D-6` | `L-1` 等價自檢 | `≥ 11` 樣本全同值 | **`14`** 樣本（真值表 `8` ＋ 三鍵缺鍵 `3` ＋ 三鍵全缺／空字串／`None` `3`）×**四式**（`L-1`／`:6289`／`:6301`／`:6319`）⇒ **逐樣本同值 ＝ True** | ✅ |

⇒ `S-6` **未觸發**（`Y-8` ✅）。`D-6` 即 `GB-116` 之**代償**（`L-1` 係第 `4` 份實作·⛔ 未重構既有三處）。
**證據檔**：`verify/out/probe_WG9169_ghost3_equiv_a3c97aa.log`／`WG9169_controls_a3c97aa.json`。

---

## §九　`§五`　取號現查（**自行複現**·逐格標框·自誤 `204` 之施行式）

🔒 母體 ＝ **全倉 `.md`** ⇒ **`446`** 檔；態 ＝ `blob@a3c97aa`。

| 待鑄 | 定義列式（逐字） | 相異號 | MAX | 錨定框：命中列數／出現次數／檔數 | 對照組（同三框） |
|---|---|---|---|---|---|
| `VR-080` | `` ^##\s*`VR-([0-9]{1,4})` `` | `65` | **`79`** | `0`／`0`／`0` | `VR-079` ＝ `6`／`7`／`4` |
| `GB-116` | 三框聯集（框①相異號 `110`／式A `3`／式B相異號 `26`） | `113` | **`115`**·缺號 `[12, 87]` | `0`／`0`／`0` | `GB-115` ＝ `9`／`11`／`4` |
| 自誤 `202`／`203`／`204` | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `166` | **`201`** | 三號之二形並取皆 `0`／`0`／`0` | `` 自誤 `201` `` ＝ `4`／`4`／`3` |

🔒 **`GB` 算術自檢（常規八）**：`115 − |{12, 87}|(2) == 113` ⇒ **True**；外部錨 ＝ `W-G.9-144:81` 之式A。
🔒 **與單 `§五` 表<u>逐格相符</u>**（本單已依施行式逐格標框 ⇒ 本批**⛔ 無框差**）。

**形之自證 ＋ MAX 推進**：`VR` `79`→**`80`**／`GB` `115`→**`116`**（`116` ⛔ 不在缺號集）／自誤 `201`→**`204`**（含 `202`／`203`／`204`）。

---

## §十　`§四` 十一項預測逐項對拍（⛔ 未改預測）

| # | 預測 | 實測 | 判 |
|---|---|---|---|
| `Y-1` | `Z-1` ＝ `6` 列·逐列與 `168R C-4` 相符 | 變更列數 `6`·逐列逐字相符·集外 `0` | ✅ **符** |
| `Y-2` | 旁證閘 `4` 條逐字不變 | ⛔ 未出現於 diff | ✅ **符** |
| `Y-3` | `Z-2a`：`16`／∅／`72`,`72`,`64`,`64` | `16`／`0`／`72`,`72`,`64`,`64` | ✅ **符** |
| `Y-4` | `Z-3` 零觸發；`app:k6step0/_ordered` ＝ `80` | 零觸發；`80` | ✅ **符** |
| `Y-5` | `Z-4` winner 零改變 | 二態全欄逐格相異 `0` | ✅ **符** |
| `Y-6` | `L-3` 換源零 diff | `D-5` 二層逐位相同·`Z-1` 無相關 diff 列 | ✅ **符** |
| `Y-7` | `L-6` 斷言零觸發 | `80` 次執行·零 `raise` | ✅ **符** |
| `Y-8` | `L-1` 等價自檢 `≥ 11` 樣本全同值 | `14` 樣本×四式全同值 | ✅ **符** |
| `Y-9` | `app.py` 期末 blob ≠ `f285b91c…` | `60b550b7f4d651cca5418fdf9807a0cc3248278d` | ✅ **符** |
| `Y-10` | 受檔 `deletions` > `0` | `app.py` `17`／`sp` `1`／`stepg` `1` | ✅ **符**（⇒ 常規一②不齊 ⇒ **須請示**） |
| `Y-11` | 三 payload `deletions` 逐列 ＝ `0` | `33`／`0`・`52`／`0`・`48`／`0` | ✅ **符** |

**計數對拍**：符 **`11`** ／ 不符 `0` ／ 不可判 `0` ⇒ 和 ＝ `11` ＝ 預測總數 ✅。

---

## §十一　入倉清單

| # | 檔 | 性質 | 增／刪 |
|---|---|---|---|
| `1` | `app.py` | 🛑 **生產碼**（`L-1`〜`L-4`／`L-6`／ns 白名單） | `92`／`17` |
| `2` | `verify/selection_pipeline.py` | 🛑 生產碼（`L-4` 實參） | `3`／`1` |
| `3` | `verify/stepg_pipeline.py` | 🛑 生產碼（`L-4` 實參） | `3`／`1` |
| `4` | `verify/probes/probe_WG9169_ghost3_equiv.py` | `D-1`／`D-2`／`D-4`／`D-5`／`D-6` 探針（**新檔**·⛔ 無絕對路徑） | — |
| `5` | `verify/out/probe_WG9169_ghost3_equiv_a3c97aa.log`／`WG9169_controls_a3c97aa.json` | 對照證據（**新檔**） | — |
| `6` | `verify/out/runall_W-G.9-169R_post_full.log` | `Z-1` 期末全量 log（**新檔**·`506`／`46004`／`51b06cf6…`） | — |
| `7` | `verify/out/WG9169_viol_full_post.json` | `Z-2a` 期末全集（**新檔**） | — |
| `8` | `verify/out/WG9169_z4_winner_a3c97aa.json` | `Z-4` 二態證據（**新檔**） | — |
| `9` | `docs/驗證裁定登記表.md` | `P-1`　**`VR-080`** | `48`／**`0`** |
| `10` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `P-2`　`GB-115` 就地加註 ＋ **`GB-116`** | `33`／**`0`** |
| `11` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `P-3`　**自誤 `202`／`203`／`204`** | `52`／**`0`** |
| `12` | 本報告 | `docs/reports/W-G.9-169R_VR074落地.md`（**新檔**） | — |

🔒 **報告命名（常規三）**：候選 ①`W-G.9-169R_VR074落地.md`（**採**）②`W-G.9-169R_ghost3投影序母體落地.md`
③`W-G.9-169R_生產碼_16格.md`。**理由**：倉內體例為「單號 `R` ＋ 主題」，本單之主題即其標題「`VR-074` 落地」。

🔒 **`§七` 不辦事項全數遵守**：⛔ 未重產任何 baseline／凍存名單；⛔ 未改 `probe_WG9168_viol_full.py` 一字
（`sha256` 期初＝期末）；⛔ 未改任何既有序對拍一字；⛔ 未重構 `app.py:6289`／`:6301`／`:6319` 之既有三判準式；
⛔ 未改 `_proj_pop_assert_subset` 一字；⛔ 未修 `GB-104`〜`115`；⛔ 未落地 `VR-075`／`K-9-*`／`K-6-A2` 剩餘／`K-6-B`；
⛔ 未動 `data/`；⛔ 未改任何既有 `docs/` 文字（三 payload 皆末端追加）；⛔ 未改 `.gitattributes`；⛔ 未追改任何既有已鑄之號。

---

## §十二　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | 開工 `ls-remote`；期初 blob；**期末 blob 絕對值** | ✅ `a3c97aa…`／`f285b91c…`／**`60b550b7f4d651cca5418fdf9807a0cc3248278d`** |
| `2` | `numstat`；三 payload 刪除欄 `0`；`wg942_append_audit` | ✅ `§十一`；`wg942` `rc=0`·`stderr` `0` B·受檢 `77` 筆·「結論：✅ 全部成立」 |
| `3` | `L-1`〜`L-8` 逐項碼面錨；`L-4` 編碼形式之提案與核可紀錄 | ✅ `§三`／`§二`（含 CC 之一處偏離逐字回報） |
| `4` | `Z-1` 全項 | ✅ `§四`（未正規化 `38`·變更列數 `6`·逐列對拍·旁證閘·判別力①②） |
| `5` | `Z-2a` 全項；探針 `sha256` 期初＝期末 | ✅ `§五`（`e920e02e…`·`git diff` 空） |
| `6` | `Z-3` `14` tag 逐一；零觸發；`80` 之對拍 | ✅ `§七` |
| `7` | `Z-4` winner；`指配`／`抵費地` 三閘總數 | ✅ `§六`（`17`／`17`／`0`／`0`） |
| `8` | `Z-5` `D-1`〜`D-6`；複本已刪除；原檔逐位未動 | ✅ `§八`（全部於**記憶體**為之·⛔ 未寫任何倉內檔；`D-3` 於 JSON **複本**上為之） |
| `9` | 每一命中數同格標**三數框 ＋ 母體 ＋ 態** | ✅ `§九` |
| `10` | 取號：錨定框 ＋ 二形並取 ＋ 形自證 ＋ MAX 推進 | ✅ `§九` |
| `11` | `§四` 十一項逐項對拍（和 ＝ `11`） | ✅ `§十`（符 `11`／不符 `0`／不可判 `0`） |
| `12` | 長跑於拋棄式 clone（`8` 字元）；工作倉入倉前淨；探針⛔ 無絕對路徑 | ✅ 長跑於 `/c/w169a`；入倉於 `/c/w169b`；新探針之絕對路徑樣式命中 **`0`**（對照組 `wg960_z_selfcheck.py` ＝ `1`） |
| `13` | 報告命名 | ✅ `§十一` |

---

## 🛑 §十三　push 之處置（`S-8`）

**常規一四項自評**：① 零生產碼變更 **✗**（本批係生產碼批）② `deletions` 逐列 `0` **✗**（`app.py` `17`／`sp` `1`／`stepg` `1`）
③ `app.py` 期末 blob ＝ 期初 **✗**（必變·`Y-9`）④ 開工／收工閘齊備 **✓**。
⇒ 🛑 **四項不齊 ⇒ ⛔ 不得逕行 push**（單 `§四 S-8` 逐字）。

🔒 **現狀**：本批已於 `/c/w169b` **commit**、**⛔ 未 push**。
🛑 **待 KL 核可後方 push**；核可前 `origin/wip/s1-endpart` 仍為 `a3c97aa`。

### 🛑 呈 KL 之三事（供裁）

1. 🔴 **本批有輸出後果**：`原位次(投影序)` 欄之 **`16` 格**（四份對拍靶 × `4` 格）——
   `R1` 三宗（`628-37(1)` `3→2`／`628-36(1)` `4→3`／`628(5)` `6→5`）＋ `R4` 一宗（`628-1(1)` `3→2`）。
   其成因 ＝ ghost（`_GHOST_(R1)`／`_GHOST_(R4)`）**⛔ 不再入投影序母體**（`VR-074`·`K-9-19 一`）。
   🔒 **winner 零改變**（`§六` 二態直接對拍）⇒ **⛔ 無配地結果之變動**。
2. 🔒 **⛔ 未重產任何 baseline**（`VR-079 三`）⇒ 四份對拍靶之違規總數由 `68/68/60/60` 增為 `72/72/64/64`，
   **屬已紅之閘增加違規列數**（准紅碼·`CLAUDE.md:67-72`），**⛔ 非由綠轉紅**。
   baseline 之更新屬 KL 已裁之**波末重烤**（與 `GB-111`／`GB-113` 同批）。
3. ⚠️ **`L-4` 之一處偏離**（`§二`）：原子名採 `no_ghost`（非甲案示例之 `no_ghost3`），理由已逐字載明；
   若不採，一行可改。

---

## 🔧 `W-G.9-169 補正①`　`app` 路徑之改動須證與 harness 路徑**同構**

**發單**：`W-G.9-169_補正①_app路徑同構證明.md`（`SELF_SHA256 = 1edeb52617d89e75ad8f5e93b899cfe6fef4fd76eb90cb2bd3dfaab7c2f7d347`
——**CC 現算逐位相符**·計法＝扣除最末一行·`107` 列／`8769` B）
**性質**：只讀證明 ＋ 純附加（`W-5` 之註解補記除外·`deletions` 逐檔 `0`）
**基座**：`6a4a0da`（🛑 **未 push**）　**量測時點**：`2026-08-29`
🔒 **行號之態**：本節**一切 `app.py`／`verify/**` 行號皆繫於 `6a4a0da`**。
⚠️ 本補正之 `W-5` 補記後，`app.py` 行號 **≥ `7785` 者一律 `+7`**（⛔ 勿以本節行號 grep 期末態）。

🛑 **總判**：`§三` 五項預測 ⇒ **符 `3`／不符 `2`／不可判 `0`**（和 ＝ **`5`** ✅）。
🔴 其中 **`W-2` 不符 ⇒ 依單之停機條件「🛑 停機回報」**（`N-2 ②`·逐字二側已附·⛔ **未改 app 側**、
⛔ **未加正規化規則**）。`W-5` 不符 ⇒ 依單「補之（⛔ 非停機）」，已補（見文末 `W-5` 節）。

---

### `N-1`　本批之**全部**碼面改動與其**執行覆蓋**

母體 ＝ `git diff --numstat 6a4a0da^ 6a4a0da` 之**生產碼三檔**（其餘 `10` 檔皆 `docs/**`／`verify/out/**`／
新增探針，⛔ 非碼面改動）。逐 hunk 由 `git diff -U0` 機械解析。

🔒 **算術自檢**：逐 hunk 合計 **`+98 / -19`** ＝ numstat 合計（`92+3+3` ／ `17+1+1`）⇒ **True**。

| # | `檔:行`（期初→期末） | 期初逐字 → 期末逐字 | **覆蓋** | 證 |
|---|---|---|---|---|
| `H1` | `app.py:7649→7649` | `…"filter": "passthrough"},` → `…"filter": "passthrough+no_ghost"},` | ✅ | tag `app:v2/pre_seq` 計數 **`18`**【倉】`docs/reports/W-G.9-169R_VR074落地.md` §七 `Z-3` 表 |
| 🔴 `H2` | `app.py:7651→7651` | `…"filter": "stage1"},` → `…"filter": "stage1+no_ghost"},` | 🔴 **⛔ 本批未執行** | tag `app:main/v2_caller` 計數 **`0`**（`main()` 內·結構性不可達）同上 |
| `H3` | `app.py:7653→7653` | `…"filter": "stage1"},` → `…"filter": "stage1+no_ghost"},` | ✅ | tag `stepg:v2_caller` 計數 **`18`** 同上 |
| 🔴 `H4` | `app.py:7655→7655` | `…"filter": "identity"},` → `…"filter": "identity+no_ghost"},` | 🔴 **⛔ 本批未執行** | tag `app:main/_rank_by_tpid` 計數 **`0`** 同上 |
| `H5` | `app.py:7657→7657` | `…"filter": "identity"},` → `…"filter": "identity+no_ghost"},` | ✅ | tag `sp:_rank_by_tpid` 計數 **`102`** 同上 |
| `H6` | `app.py:7659→7659` | `…"filter": "stage1"},` → `…"filter": "stage1+no_ghost"},` | ✅ | tag `stepg:_proj_rank` 計數 **`18`** 同上 |
| `H7` | `app.py:7729→7730`（`+37/-0`） | 期初**無**（純插入）→ 新增 `def _proj_pop_ghost3`／`_PROJ_POP_GROW_KEYS`／`def _proj_pop_key_guard`（⚠️ 全文長 `37` 列·逐字見 `git diff -U0 6a4a0da^ 6a4a0da -- app.py`） | ✅ | `_proj_pop_ghost3` 經 `_proj_pop_filter` 之 `no_ghost` 原子被 `stepg:v2_caller`＋`sp:_rank_by_tpid`＋`stepg:_proj_rank` 三 tag 共 **`138`** 次呼叫；另 `D-5`／`D-6` 正負對照【倉】`verify/out/probe_WG9169_ghost3_equiv_a3c97aa.log` |
| `H8` | `app.py:7731→7768`（`+22/-8`） | `_proj_pop_filter` 之 `if/elif` 三分支 → **原子迴圈**（`for _atom in str(fname).split("+")`） | ✅ | 同 `H7`（`138` 次）；`D-1` 正負對照同上 log |
| `H9` | `app.py:7746→7798`（`+1/-0`） | 期初**無** → `    _proj_pop_key_guard(tag, base, _d["source"])` | ✅ | 位於 `_proj_pop_assert_seq` 直線路徑（`138` 次）；`D-2` 負對照 `raise` 逐字同上 log |
| `H10` | `app.py:7761→7813`（`+2/-1`） | `if _d["kind"] != "POP_SYNC" or _d["filter"] != "passthrough":` → `…or str(_d["filter"]).split("+")[0] != "passthrough":` | ✅ | tag `app:v2/pre_seq` 計數 **`18`**（走 `_proj_pop_assert_passthrough`） |
| `H11` | `app.py:11608→11662`（`+13/-0`） | 期初**無** → `k6_step0_merge` 內之 `_proj_pop_key_guard(...)` ＋ `_gh3_mem` loud `raise` | ✅ | tag `app:k6step0/_ordered` 計數 **`80`**；其 `_proj_pop_note` 於 `_proj_pop_assert_subset`（`app.py:11659`）＝本 hunk **正上方同一直線路徑**、其間⛔ 無分支；`D-4` 正負對照同上 log |
| `H12` | `app.py:13213→13280`（`+5/-0`） | 期初**無** → `_WF_NS_NAMES` 補列 `"_proj_pop_ghost3"` | ✅ | `fixture_wf_ns_wiring.py rc=0`【倉】`verify/out/runall_W-G.9-169R_post_full.log:55`（靜態層 AST 讀該 list 字面 ＋ 動態層以 `_wf_ns()` 建 ns 實跑 `run_step_g`） |
| 🔴 `H13` | `app.py:19119→19190`（`+3/-1`） | `_all_in_blk = by_blk.get(_lbl, [])` → 見 `N-2` 逐字 | 🔴 **⛔ 本批未執行** | 位於 `main()`；`CLAUDE.md` 逐字「`app.py:main()` 內之敘述從不被 `run_all` 執行」；併證 tag `app:main/_rank_by_tpid` 計數 `0` |
| 🔴 `H14` | `app.py:20083→20156`（`+3/-1`） | `_stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp]` → 見 `N-2` 逐字 | 🔴 **⛔ 本批未執行** | 同上；併證 tag `app:main/v2_caller` 計數 `0` |
| `V1` | `verify/selection_pipeline.py:333→333`（`+3/-1`） | 同 `H13` 之對側（見 `N-2`） | ✅ | tag `sp:_rank_by_tpid` 計數 **`102`** |
| `V2` | `verify/stepg_pipeline.py:416→416`（`+3/-1`） | 同 `H14` 之對側（見 `N-2`） | ✅ | tag `stepg:v2_caller` **`18`** ＋ `stepg:_proj_rank` **`18`** |

🔴 **標 🔴 之 hunk 數 ＝ `4`**（`H2`／`H4`／`H13`／`H14`）。

---

### 🔴 `N-2`　`app` 側 ↔ harness 側之**同構證明**

#### ①　對應之具名

| 對 | app 側（🔴 未執行） | 對側（✅ 已執行） | 對應之依據 |
|---|---|---|---|
| `P1` | `app.py:7651`（tag `app:main/v2_caller` 之宣告） | `app.py:7653`（tag `stepg:v2_caller` 之宣告） | **同一函式語意**：二 tag 為 `_spatial_order_parcels_v2` 之**同一透傳呼叫端**於 app／harness 二側之孿生體（`_PROJ_POP_DECL` 係**單一宣告表**，故對側亦落於 `app.py`） |
| `P2` | `app.py:7655`（tag `app:main/_rank_by_tpid`） | `app.py:7657`（tag `sp:_rank_by_tpid`） | 同上（`_rank_by_tpid` 之 app／harness 孿生體） |
| `P3` | `app.py:19190`（`_all_in_blk`·`main()` 內） | `verify/selection_pipeline.py:333` | **同一註解錨**：二處新增之註解逐字皆為 `# 🆕 W-G.9-169 \`L-4\`：實參與宣告同步（\`identity+no_ghost\`）`；且二處為 `run_corner_pk` 候選池構造之孿生體 |
| `P4` | `app.py:20156`（`_stage1_parcels`·`main()` 內） | `verify/stepg_pipeline.py:416` | **同一註解錨**：`# 🆕 W-G.9-169 \`L-4\`：實參與宣告同步（\`stage1+no_ghost\`）`；且 `app.py:20155` 之既有註解逐字自陳「**必須**與 stepg 同構——否則 app 走舊單階段、harness 走新兩階段＝無聲分岔」 |

#### ②　同構之機械證　🛑 **`2` 對成立／`2` 對⛔ 不成立**

🔒 **正規化規則（逐字·`re.sub` 形·逐行套用後以單一空白接合非空列）**——⛔ 僅此四條，⛔ 未增刪：

```
R1  re.sub(r'\s*#.*$', '', line)        # 剝除行尾註解
R2  re.sub(r'^\s+|\s+$', '', line)      # 去縮排（含行尾空白）
R3  re.sub(r'\s+', ' ', line)           # 統一空白 run
R4  非空列以單一空白接合
```

| 對 | 正規化後逐位相同 |
|---|---|
| `P1` | ✅ **True** |
| `P2` | ✅ **True** |
| 🔴 `P3` | 🔴 **False**（首異位元 offset ＝ `56`） |
| 🔴 `P4` | 🔴 **False**（首異位元 offset ＝ `75`） |

🛑 **`P3` 之二側逐字**（正規化後）：

```
app 側    _all_in_blk = [tp for tp in by_blk.get(_lbl, []) if not _proj_pop_ghost3(tp)]
對側      _all_in_blk = [tp for tp in by_blk.get(_lbl, []) if not ns["_proj_pop_ghost3"](tp)]
```

🛑 **`P4` 之二側逐字**（正規化後）：

```
app 側    _stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp and not _proj_pop_ghost3(tp)]
對側      _stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp and not ns["_proj_pop_ghost3"](tp)]
```

🔒 **差異之定位**（🛑 ⛔ **非** 上述正規化規則之一·⛔ **不影響本判**）：另以一條**示範性**替換
`ns["_proj_pop_ghost3"]` → `_proj_pop_ghost3` 套於對側後，`P3`／`P4` 皆轉為**逐位相同 ＝ True`**
⇒ 二側之差異**恰只**在 `ns[…]` **間接層**、⛔ 無第二處差異。

🛑 **CC 之處置**（依單 `§三 W-2` 逐字「⛔ 不得自行改 app 側使其同構」＋ `§六 10` 逐字
「⛔ 不得為使預測轉符而改探針／濾式／容差／框／正規化規則」）：
**⛔ 未改 app 側一字**、**⛔ 未把該替換列入正規化規則**、**⛔ 未自行寬免** ⇒ **停機回報**，
`ns[…]` 間接層是否應計入「同構」之判**上呈發單側／KL**。
⚠️ **併陳一項倉內事實（⛔ 非 CC 之裁）**：`CLAUDE.md` 之「§7 引擎接線鐵律」逐字要求
**app 接線以 live ctx 注入呼叫同一引擎模組**、而 `verify/` 側之 app 真符號**一律經 `ns[...]` 取用**
（`_WF_NS_NAMES`／`_wf_ns()`）——即該間接層係**制度所令**、⛔ 非本批所引入；
惟「制度所令」**⛔ 不等於**單所定義之同構成立，故仍照實判為**不符**。

#### ③　判別力自檢（🛑 必辦·⛔ 僅於記憶體·⛔ 未改任何檔案）

人造把 app 側新增文字之首個 `ghost` 改為 `ghosT`（一字），再比較；改回後復原。

| 對 | 期初判 | 擾動後判 | 轉紅 | 復原 |
|---|---|---|---|---|
| `P1` | `True` | `False` | ✅ **是** | ✅ |
| `P2` | `True` | `False` | ✅ **是** | ✅ |
| `P3` | `False` | `False` | n/a（期初已紅） | ✅ |
| `P4` | `False` | `False` | n/a（期初已紅） | ✅ |

⇒ **於期初判為「相同」之 `2` 對，擾動皆轉紅** ⇒ 比較器**具判別力** ⇒ `②` 之 `P1`／`P2` 之
「逐位相同」**可採信**（`W-3` 符）。

#### ④　殘餘之具名（`app.py` 側有而 harness 側⛔ 無對應者）

| # | 項 | 有無對側 | 本批執行 | 風險 |
|---|---|---|---|---|
| `R-1` | `H7`／`H8`／`H9`／`H10`（`_proj_pop_*` 之**定義**） | ⛔ 無（依構造：定義只存 `app.py` 一份，harness 經 `ns[...]` 取用） | ✅ 已執行 | **無**——harness 之執行即**同一份**碼之執行 |
| `R-2` | `H11`（`k6_step0_merge` 之 loud 斷言） | ⛔ 無（`k6_step0_merge` 只存 `app.py`） | ✅ 已執行（`80` 次） | **無** |
| `R-3` | `H12`（`_WF_NS_NAMES`） | ⛔ 無（依構造為 app 專屬清單） | ✅ 已執行（`fixture_wf_ns_wiring` `rc=0`） | **無** |
| 🔴 `R-4` | `H2`／`H4`（`main()` 之二 tag 宣告值） | ✅ 有（`P1`／`P2`·**逐位相同**） | 🔴 ⛔ 未執行 | **經 `②` 移轉為已證**——其為**純字串常量**，與已執行之對側逐位相同 |
| 🔴 `R-5` | `H13`／`H14`（`main()` 之二處實參） | ✅ 有（`P3`／`P4`·**⛔ 不逐位相同**） | 🔴 ⛔ 未執行 | 🛑 **⛔ 未證**——差異在 `ns[…]` 間接層。**風險**：若 `_wf_ns()` 之 `ns` 與 `app.py` 之 module globals 於該二點**不同源**，則 app 與 harness 之濾式可分岔而無聲。⚠️ 倉內另有反向證據（`W-G.9-147`：`ns is __globals__` 實測 `True`），**惟該證⛔ 非本批所產、⛔ 不足以取代本單所令之機械同構** ⇒ 照實列為未證 |
| ⚠️ `R-6` | **併記**（⛔ 不擴大射程）：`_stage1_parcels` 之**第 5 個消費點** `verify/stepg_pipeline.py:501-507`（`stepg:_proj_rank` 結構閘）於 `app.py` **⛔ 無對應** | ⛔ 無（**反向**：harness 有、app 無） | ✅（harness 側 `18` 次） | **⛔ 非本批所生**——屬既登之「`19` 條 harness 專屬結構閘（app 命中 `0`）」（`app-vs-harness-stepg-divergence`） |

---

### `N-3`　`_stage1_parcels` 之**非 PO 用途**對 ghost 之敏感性

母體 ＝ `grep -n "_stage1_parcels" app.py verify/stepg_pipeline.py`（期末態·app `5` 列／stepg `7` 列）。

| 處（期末行號） | 可達性 | 對 ghost 敏感？ | 證 |
|---|---|---|---|
| `verify/stepg_pipeline.py:466-468` | `if _degenerate_order:` ⇒ **本案不可達** | — | 註解逐字「（UC9898 全塊皆有 FRONT_LINE·不觸）」【倉】`verify/stepg_pipeline.py:460`；`Z-3` 之 `stepg:v2_caller` `18` 次皆走 `else` 分支（否則 `stepg:483` 之斷言不會被記數） |
| `app.py:20182`（`first_corner_side` 之迴圈） | **可達**（app 側） | ✅ **⛔ 不敏感** | 見下「`W-4` 之現查」 |
| `app.py:20305`（`if _degenerate_order:` 內之 `ordered_v2` 退化構造） | `if _degenerate_order:` ⇒ **本案不可達** | — | 與 stepg 同構之孿生體（`app.py:20302`）；同上註解錨 |
| `parcels_in_block=_stage1_parcels`（`stepg:485`／`app:20340`） | **可達且有幾何後果** | 🔴 **是**（母體變） | harness 側已由 `Z-1`（`6` 列）／`Z-2a`（`16` 格·欄名集合 ＝ `{原位次(投影序)}` 單一值）／`Z-4`（指配 rows 逐格相異 `0`）三重覆蓋 ⇒ **⛔ 無幾何後果**；app 側繫於 `N-2` 之同構 ⇒ 🛑 **因 `P4` 不成立而⛔ 未證**（見 `R-5`） |

#### 🔒 `W-4` 之現查（⛔ 非推定）

**受詞**：`app.py:20182-20186` 之迴圈——其**唯一出口**係
`p = _params_for_g.get(tp['暫編地號'], {})` 之 `p.get('is_corner') and p.get('side', '無') in ('左側', '右側')`。
迴圈於首個滿足者 `break` ⇒ **移除「恆不滿足」之元素⛔ 不可能改變結果** ⇒
「敏感」⟺「**某 ghost 滿足該述詞**」。

| # | 現查 | 結果 |
|---|---|---|
| `a` | ghost 能否成為 PK 候選？`app.py:19215-19218` 逐字 `_parent = r.get('原地號', '')`／`_gid = _own_map.get(_parent, '')`／`if not _gid: continue`；ghost 之 `原地號 == '_GHOST'`（`app.py:4636`），而 `_own_map` 之鍵全由**地籍歸戶**之 `(地段, 地號)` 導出（`app.py:16348-16352`）、⛔ 無 `'_GHOST'` 之寫入點 | ⇒ **ghost 於 `_candidates` 被 `continue` 跳過** |
| `a′` | harness 側**同一守衛**逐字存在 | 【倉】`verify/selection_pipeline.py:347-351`（與 `a` 逐字同式） |
| `b` | **執行態現查**：`Z-4` 之 winner 表（`winner欄 = ['【左】第1宗指配', '【右】第1宗指配']`·`winner欄` 二欄 × `6` 街廓 × `2` 情境 ＝ **`24` 格**·母體**非空**：非佔位格 **`15`**、相異值 **`10`**（期初期末**同集**）） | `grep -c "_GHOST" verify/out/WG9169_z4_winner_a3c97aa.json` ⇒ **`0`** ⇒ **⛔ 無任一 winner 為 ghost** |
| `c` | 若 Step L 已跑：`app.py:20020-20026` 逐字對 `build_parcels` 中**該街廓全部**宗地寫 `is_corner=False`／`side='無'`，其後**僅** `p1_end`／`p2_end` 二 winner 被寫 `True` | ⇒ ghost 之 `_params_for_g` 條目必為 `is_corner=False`／`side='無'` ⇒ **⛔ 不滿足** |
| `d` | 若 Step L 未跑（`_step_l_winners` 空）：ghost 於 `_params_for_g` **⛔ 無條目** ⇒ `p = {}` ⇒ `p.get('is_corner')` ＝ `None` | ⇒ **⛔ 不滿足** |

🔒 **判：`W-4` 符——`app.py:20182` 對 ghost ⛔ 不敏感**（⇒ ⛔ **無土地後果**、⛔ 不觸該單之停機條件）。

⚠️ **殘餘之具名（⛔ 不自行寬免）**：`c`／`d` 未涵蓋之**唯一**組合 ＝
「Step L **未**跑 ∧ 使用者於 GIS **手動點選一個 ghost 多邊形**並標為街角地」（`app.py:17947`／`:17955`／`:17960` 之 UI 點選分支·其 session 寫回於 `:17967`）。
該路徑**⛔ 非 `run_all` 射程**、亦**⛔ 非本批所引入**（`H14` 只把 ghost 自 `_stage1_parcels` 移除，
於該組合下反而**消除**了風險）⇒ 列為**併記**，⛔ 不列為本批之未證項。

---

### `§三`　五項預測之逐項對拍

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `W-1` | `N-1` 中標 🔴 之 `app.py` hunk 數 **`> 0`** | **`4`**（`H2`／`H4`／`H13`／`H14`） | ✅ **符** |
| 🔴 `W-2` | `N-2 ②` 之同構**成立** | `4` 對中 **`2` 成立／`2` ⛔ 不成立**（`P3`／`P4`·差異恰在 `ns[…]` 間接層） | 🔴 **不符** ⇒ 🛑 **停機回報** |
| `W-3` | `N-2 ③` 之判別力對照**轉紅** | 期初判為相同之 `2` 對**皆轉紅**、`2` 對期初已紅（n/a） | ✅ **符** |
| `W-4` | `app.py:20107`（期末 `:20182`）對 ghost **⛔ 不敏感** | **⛔ 不敏感**（四項現查 `a`／`a′`／`b`／`c`／`d`） | ✅ **符** |
| 🔴 `W-5` | `no_ghost` 之唯一定義處已帶三項註解 | **缺**（見下） | 🔴 **不符** ⇒ 依單**補之**（⛔ 非停機） |

**和 ＝ `5`**（符 `3` ／ 不符 `2` ／ 不可判 `0`）✅

#### `W-5`　現查與補記

**現查**（態 ＝ `6a4a0da`）：`no_ghost` **原子**之定義處 ＝ `_proj_pop_filter` 之
`elif _atom == "no_ghost":`（`app.py:7785`），其 docstring（`app.py:7768-7778`）：

| 項 | 逐字要求 | 現查 |
|---|---|---|
| ① | 其述詞為 `K-9-19 一` 之**三判準合取** | 🔴 **缺**（該 docstring 內 `K-9-19`／「三判準」皆⛔ 未出現；⚠️ 該文字實存於 `_proj_pop_ghost3` 之 docstring `app.py:7734-7737`，**⛔ 非** `no_ghost` 之定義處） |
| ② | 🛑 ⛔ **不綁 `_is_ghost_sliver`** | ⚠️ **不逐字**（現有為「其述詞由 `_is_ghost_sliver` 換為 `_proj_pop_ghost3`（`VR-074` 拘束：⛔ 不綁旗標、⛔ 不綁名稱）」） |
| ③ | 其名之沿用係 `W-G.9-169 L-3`「**名可留**」所許 | ⚠️ **不逐字**（現有為「`no_ghost` 之名**留**」） |

⇒ **缺 ①、②③ 不逐字** ⇒ 依單「補之」。

**補記**（🔒 **純附加註解**·`app.py` `+7 / -0`·⛔ 未改既有一字·`py_compile` **OK**）：
於 `app.py:7785`（`elif _atom == "no_ghost":`）**正上方**插入 `7` 列註解，逐字：

```
        # 🆕 `W-G.9-169 補正①` `W-5`（發單側 `§零` 核可之**附帶條件**·⛔ 純附加註解）：
        #   ① `no_ghost` 之**述詞** ＝ `K-9-19 一` 之**三判準合取**——
        #      `原地號 == '_GHOST'` ∧ `G(㎡) == 0` ∧ `a 面積(㎡) == 0`（式見 `_proj_pop_ghost3`）。
        #   ② 🛑 ⛔ **不綁 `_is_ghost_sliver`**（`VR-074` 落地拘束·`K-6:3106`：其於 `g_row` 層恆假）、
        #      🛑 ⛔ 亦不綁**名稱**。
        #   ③ 其名之沿用（⛔ 未另立 `no_ghost3`）係 `W-G.9-169` `L-3`「**名可留**」所許；
        #      另立將使**二原子同義**、正犯同批所鑄 `GB-116`（同一述詞多份實作）之形。
```

🔒 **`app.py` 之態**（🛑 **本補正後已變**·⛔ 不得再以 `60b550b7…` 為工作區之錨）：

| 態 | blob | 列數（`splitlines`） | bytes | `sha16` |
|---|---|---|---|---|
| `6a4a0da`（＝ `W-G.9-169R` 期末） | `60b550b7f4d651cca5418fdf9807a0cc3248278d` | `22915` | `1351414` | `1b9483ba3a86f260` |
| **本補正期末** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`** | **`22922`** | **`1352132`** | **`7c290b6f55f83a24`** |

🔒 **純附加之機械自檢**：以 `split("\n")` 逐列比對，插入點前 `7784` 列與插入點後全部列**皆按序逐字保留**
⇒ `True`；檔尾末位元組期初期末皆 `b')'`（⛔ 無尾隨換行·未被改形）；`git diff --numstat -- app.py` ⇒ **`7  0`**。

---

### 🛑 收工聲明

1. 🛑 **`W-2` 不符 ⇒ 停機**：`P3`／`P4` 之同構於單所定義之四條正規化下**⛔ 不成立**；
   CC **⛔ 未改 app 側**、**⛔ 未增修正規化規則**、**⛔ 未自行寬免** ⇒ 上呈發單側／KL 裁。
2. 🔒 **`N-1` 標 🔴 之 `4` 個 hunk 中，`H2`／`H4` 已由 `②` 之 `P1`／`P2` 移轉為已證；
   `H13`／`H14` ⛔ 未證**（`R-5`）。
3. 🔒 **⛔ 未重跑 `run_all`**、**⛔ 未改任何探針**、**⛔ 未重產任何 baseline**（依單 `§三` 共通）。
4. 🔒 **push 仍須 KL 核可**（`W-G.9-169 S-8` 全效）——本補正**⛔ 不解除**該條件；
   且本補正**已觸 `app.py`** ⇒ 仍屬**生產碼批**、⛔ 不得逕行 push。
5. 🔒 `6a4a0da` **⛔ 不得**被記為倉態（常規九 一）；`/c/w169b` **保留不刪**。
6. 🔒 **`§四` 之擇一**（常規三·⛔ 無土地後果）：CC 擇 **「另起一 commit 疊於 `6a4a0da`／本地 tip 之上」**。
   **一句理由**：`6a4a0da` 之 `Z-1`〜`Z-5` 全綠**係以其自身之 `app.py` blob `60b550b7…` 為受詞**，
   若把本補正之 `W-5` 註解 amend 進該 commit，該 blob 即變而使**已入倉之 `Z` 系列錨全數失真**；
   另起 commit 則二態各自持有正確之錨。

---

## 🔧 `W-G.9-169 補正②`　`W-2` 之裁（`ns[…]` 計入同構）＋ `+7/-0` 之零行為機械證

**發單**：`W-G.9-169_補正②_ns間接層之裁與AST零行為證.md`
（`SELF_SHA256 = 85ef414b9a509dea7458af08ac4c3b91930ee1b42787a7234e0f88cda0c27492`
——**CC 現算逐位相符**·計法＝扣除最末一行·`118` 列／`10911` B）
**基座**：`926a0de`（🛑 **未 push**）　**量測時點**：`2026-08-29`
**性質**：只讀證明 ＋ 純附加　🔒 **⛔ 零碼面變更**（本節與 `P-1` 皆⛔ 不觸 `app.py`／`verify/**`）

🔒 **總判**：`§二` 七項預測 ⇒ **符 `7`／不符 `0`／不可判 `0`**（和 ＝ **`7`** ✅）。
🔒 **`W-2` 重判 ＝ `4`／`4` 成立**（`補正① §零` 之停機**解除**）；`V-2` AST 對拍**逐位相同**。

🔒 **行號之態**：本節 `app.py` 行號皆繫於 **`926a0de`**（＝ 現 tip）；
`N-1`／`N-2` 之 hunk 行號（`補正①`）繫於 `6a4a0da`，二者於 `≥ 7785` 處差 `+7`。
⚠️ **施工單 `§零 二` 所引之 `app.py:13161`／`:13178`／`:13218`／`:13221` 於本倉現查⛔ 不成立**
（實為 `:13234`／`:13251`／`:13296`／`:13299` @`926a0de`）——係發單側行號之時態落後、
⛔ 非受詞不存在；本節一律改引**現查值**並逐字複述其內容。

---

### 🔒 `④` 之四項條件（🛑 全備方得施用規則 ④）

#### `④-a`　`_proj_pop_ghost3` ∈ `app.py` 之 `_WF_NS_NAMES` 字面　✅ **成立**

`_WF_NS_NAMES = [` 起於 **`app.py:13234`**（@`926a0de`）；該名之逐字清單列 ＋ 其上之逐字註解：

```
    # 🆕 W-G.9-169 `L-4`（同批補列·`W-G.9-163` 之戒第四度施行）：`verify/` 側之實參同步
    #   須呼叫 `ns["_proj_pop_ghost3"]`（`selection_pipeline.py:333`／`stepg_pipeline.py:416`）
    #   ⇒ 漏列則 app 生產路徑（`_build_wf_ctx` → `_wf_ns()` → wf_f0.compute → run_step_g）
    #   **必 KeyError**。⛔ 禁改為 `ns.get(...)` 加自算 fallback。
    "_proj_pop_ghost3",
]
```

（`"_proj_pop_ghost3",` ＝ **`app.py:13291`**@`926a0de`＝`:13284`@`6a4a0da`。）
🔒 **⛔ 非以「雙向閘常綠」推得**——係**直接現查該 list 字面**（`grep -n '^    "_proj_pop_ghost3",' app.py` ⇒ 命中 `1` 列）。

#### `④-b`　`fixture_wf_ns_wiring.py` 期初期末皆 ✅ `rc=0`　✅ **成立**

| 端 | 來源（【倉】） | 逐字 |
|---|---|---|
| **期初** | `verify/out/runall_a98a99f_full.log:55` | `  ✅ 末端夾具 fixture_wf_ns_wiring.py（🆕 W-G.9-4b app 接線清單↔引擎 ns[] 消費（AST 靜態＋_wf_ns() 實跑＋竄改自檢））rc=0` |
| **期末** | `verify/out/runall_W-G.9-169R_post_full.log:55` | `  ✅ 末端夾具 fixture_wf_ns_wiring.py（🆕 W-G.9-4b app 接線清單↔引擎 ns[] 消費（AST 靜態＋_wf_ns() 實跑＋竄改自檢））rc=0` |

🔒 **二端逐位相同**。🔒 **⛔ 非由 `Z-1` 之「集外 `0`」推得**——**期末 log 本身即在倉內**；
另於拋棄式複本 `C:\w171a`（＝ `926a0de` 之 clone）**單跑該夾具**實得 **`rc=0`**（`stderr` `0` B）⇒ 三重。

#### `④-c`　`ns[<NAME>] is app.<NAME>`　✅ **成立（⛔ 非由 docstring 推定·當場自證）**

倉內逐字（`app.py:13295-13299`@`926a0de`）：

```
def _wf_ns():
    """引擎經 ns 呼叫之 app 真符號 dict（直取本模組 globals，非 harvest；清單＝`_WF_NS_NAMES`）。"""
    g = globals()
    ns = {}
    for n in _WF_NS_NAMES:
```

🛑 **⛔ 不以 docstring／命名推定**（`wg9125` 之戒）⇒ 於複本實測二條 ns 建構路徑：

| 路徑 | 量 | 實得 |
|---|---|---|
| （乙）`harvest()` 全 ns（`selection_pipeline.run_corner_pk` 於本夾具所收者·`fixture:276` 逐字） | `ns_full is f.__globals__` | **`True`**（`len(ns_full)` ＝ `len(f.__globals__)` ＝ `249`） |
| （甲）`_wf_ns()` 受限 ns（app 生產路徑·`run_step_g` 所收者） | `ns_app[NAME] is ns_full[NAME]` | **`True`**（`len(ns_app)` ＝ `29`） |
| 判別力（負） | `'_proj_pop_ghost3_X'` ∈ 二 ns | **`False`／`False`** |

⇒ `ns["_proj_pop_ghost3"](tp)` 與 `_proj_pop_ghost3(tp)` 於二路徑**皆解析至同一物件** ⇒
規則 ④ 之替換**⛔ 不改變語意**。

#### `④-d`　判別力對照　✅ **成立（雙例皆轉紅·復原逐位未動）**

於拋棄式複本 `C:\w171a` 內，把**對側**之 `ns["_proj_pop_ghost3"]` 改為**不在清單**之
`ns["_proj_pop_ghost3_X"]`（各檔命中恰 `1` 次），跑 `verify/fixture_wf_ns_wiring.py`：

| 例 | 受詞檔 | 竄改後 `rc` | 轉紅之**承載層**與逐字 | 復原 |
|---|---|---|---|---|
| 正對照 | （二檔皆未改） | **`0`** ✅ | — | — |
| `N1` | `verify/selection_pipeline.py` | **`1`** ✅ | 🔴 **動態層**：`KeyError: '_proj_pop_ghost3_X'` @ `selection_pipeline.py:335`（實跑 `run_corner_pk`） | `sha256` 逐位相同 `True`／復跑 `rc=0` |
| `N2` | `verify/stepg_pipeline.py` | **`1`** ✅ | **靜態層**逐字（見下方引文塊） | `sha256` 逐位相同 `True`／復跑 `rc=0` |

`N2` 靜態層之轉紅逐字：

```
🔴 引擎消費但 `_WF_NS_NAMES` 漏列（app 路徑必 KeyError）：['_proj_pop_ghost3_X']
```

🔴 **⛔ 不得以「皆轉紅」一語帶過——二例之承載層<u>不同</u>**（CC 現查·⛔ 非發單側所預期）：
`fixture_wf_ns_wiring.py` 之靜態層檔集 `HARD_FILES` **⛔ 不含 `selection_pipeline.py`**，其逐字理由
（`verify/fixture_wf_ns_wiring.py:52-53`）＝「其排除 `selection_pipeline.py` 之理由已記於該處：
**該檔僅走 harness 全 harvest、非 `_wf_ns()` 路徑**」。`N1` 之靜態層實印「✅ 差集為空」，
其轉紅**全由動態層承載**。
⇒ 🔒 **`P3`（對側 ＝ `selection_pipeline.py`）之規則 ④ 正當性所繫者係<u>動態層</u>**（實跑 `run_corner_pk`），
**`P4`（對側 ＝ `stepg_pipeline.py`）則靜態＋動態雙層**。二者皆為**現行常綠且具竄改自檢**之機械閘 ⇒
`④-d` **成立**；惟其**強度不等**，於此逐字具名、⛔ 不合併陳述。

---

### `§零 三`　`W-2` 之重判（規則 ①〜④）　✅ **`4`／`4` 成立**

🔒 **規則 ④ 之機械形**（逐字）：`re.sub(r'ns\["([A-Za-z_][A-Za-z0-9_]*)"\]', r'\1', s)`

| 對 | ①〜④ 正規化後（二側**共同**之結果·逐字） | 判 |
|---|---|---|
| `P1` | `"kind": "POP_SYNC", "source": "BUILD_LAYER", "filter": "stage1+no_ghost"},` | ✅ 逐位相同 |
| `P2` | `"kind": "POP_SYNC", "source": "BUILD_LAYER", "filter": "identity+no_ghost"},` | ✅ 逐位相同 |
| `P3` | `_all_in_blk = [tp for tp in by_blk.get(_lbl, []) if not _proj_pop_ghost3(tp)]` | ✅ 逐位相同 |
| `P4` | `_stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp and not _proj_pop_ghost3(tp)]` | ✅ 逐位相同 |

⇒ 🔒 **`補正① N-2 ②` 之停機解除**；`補正① N-2 ④` 之 `R-5`（`H13`／`H14` ⛔ 未證）
**改判為「經 `②` 移轉為已證」**——惟其移轉**繫於 `④` 之四項條件**，⛔ 非無條件。

### `W-3`　判別力重辦（四對·⛔ 僅於記憶體·⛔ 未改任何檔案）

🩸 **CC 自捕一則（量測器之設計誤·⛔ 非同構之瑕疵）**：首版擾動取**首個** `ghost`，
而 `P3`／`P4` 之首個 `ghost` 位於**註解列**（該列尾之 `identity+no_ghost` 字樣）⇒ **為 `R1` 所剝除** ⇒
擾動**⛔ 未達比較之內容**、二對不轉紅。⇒ 改取**末個** `ghost`（四對皆落於**可執行碼列**）重辦。
🔒 **首版之結果⛔ 不刪、併陳**——其正是 `R1` **確有作用**之正面證明。

| 對 | `W-3a`（擾動落**註解**） | `W-3b`（擾動落**碼**·正辦） |
|---|---|---|
| `P1` | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ |
| `P2` | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ |
| `P3` | 期初 `True` → 擾動 `True` ⇒ ⛔ 不轉紅（`R1` 剝除） | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ |
| `P4` | 期初 `True` → 擾動 `True` ⇒ ⛔ 不轉紅（`R1` 剝除） | 期初 `True` → 擾動 `False` ⇒ **轉紅** ✅ |

🔒 **`W-3b` 四對皆「期初相同 ∧ 擾動轉紅 ∧ 改回復原」＝ `True`** ⇒ 比較器於規則 ①〜④ 下**具判別力**。

---

### `§一 V-2`　`+7/-0` 之**零行為變更**（AST 對拍）　✅ **成立**

受詞取自 `git show <commit>:app.py`（⛔ 非工作樹）；
`ast.dump(ast.parse(src), annotate_fields=True, include_attributes=False)`。

| 端 | commit | `app.py` blob | bytes | **AST dump `sha256`** |
|---|---|---|---|---|
| 舊 | `6a4a0da` | `60b550b7f4d651cca5418fdf9807a0cc3248278d` | `1351414` | `59d550abf6eac68fb1a11748304eac492986e1d3ad15a79f3c482231f07caf74` |
| 新 | `926a0de` | `68f7bfab610ec78688b9d7322ae4bc2aac74493e` | `1352132` | `59d550abf6eac68fb1a11748304eac492986e1d3ad15a79f3c482231f07caf74` |

🔒 **逐位相同 ＝ `True`**（dump 長度二端皆 `2843204`）。

🔒 **雙向判別力（🛑 必辦·戒 `9`·⛔ 僅於記憶體）**：

| 向 | 擾動 | AST 對拍相同？ | 期望 | 判 |
|---|---|---|---|---|
| **正** | 以 AST 定位之**可執行**常數 `app.py@926a0de:175` 之 `0.0` → `0.1`（位元組級切換·`col_offset` 係位元組） | **`False`** | 須 `False` | ✅ |
| **負** | 於同處**僅插入一列註解** | **`True`** | 須 `True` | ✅ |

⇒ 🔒 **該 `+7` 列確為純註解、⛔ 無行為變更** ⇒ `Z-1`〜`Z-5` 之錨對 push 之物**仍有效**、
**⛔ 無須重跑 `run_all`**。
⚠️ **併記**：`py_compile OK`（`補正①` 所報）**只證語法**、⛔ 不證零行為變更——本節即其補證。

---

### `§二`　七項預測之逐項對拍

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `U-1` | `④-a`：`_proj_pop_ghost3` **在** `_WF_NS_NAMES` 內 | **在**（`app.py:13291`@`926a0de`·直接現查字面） | ✅ **符** |
| `U-2` | `④-b`：期末 `fixture_wf_ns_wiring.py` ＝ ✅ `rc=0` | 倉內期末 log `:55` 逐字 ✅ ＋ 複本單跑 `rc=0` | ✅ **符** |
| `U-3` | `④-d` 之對照**轉紅** | `N1`／`N2` 皆 `rc=1`；🔴 **承載層不同**（`N1` 動態層／`N2` 靜態＋動態） | ✅ **符**（附具名） |
| `U-4` | `W-2` 重判 ＝ **`4`／`4`** | **`4`／`4`** | ✅ **符** |
| `U-5` | `V-2` AST 對拍**逐位相同** | `sha256` 二端皆 `59d550ab…caf74` | ✅ **符** |
| `U-6` | `V-2` 雙向判別力齊備 | 正 `False`／負 `True` | ✅ **符** |
| `U-7` | 本補正全部受檔 `deletions` 逐列 ＝ `0` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` `17/0`；本檔 `208/0` | ✅ **符** |

**和 ＝ `7`**（符 `7` ／ 不符 `0` ／ 不可判 `0`）✅

---

### 🔒 取號現查（`§三 P-1` 之 `自誤 205`·常規四（八）＋ 三數框 ＋ 戒 `㉑`）

**母體 ＝ 全倉 `.md`（排除 `.git`）⇒ `448` 檔**（態 ＝ 工作樹 @`926a0de`）。
⚠️ **母體含未 push 之 `202`〜`204`**（繫於本批 push）⇒ **push 後須以 `origin` 為母體複現一次**並就地加註；二次不一致 ⇒ 停機。

| 框 | 式（逐字） | 實得 |
|---|---|---|
| **定義列式** | `` ^#+ 自誤 `?([0-9]{1,4})`? ``（母體 ＝ 自誤登記**單檔**） | 期初：相異號 **`169`**／MAX **`204`** ⇒ 次號 **`205`**；期末：相異號 **`170`**／MAX **`205`** |
| 缺號自檢 | — | 缺 `35` 個 ⇒ `204 − 35 == 169` **True** |
| **錨定框** | `(?<![0-9\-])205(?![0-9])`（全倉） | 命中列數 **`21`**／出現次數 **`22`**／檔數 **`20`** |
| 錨定框·占用之判 | 🛑 **逐列人工判**（常規四（七）：占用之受詞 ＝ **意指**） | **`21`／`21` 皆⛔ 非意指「自誤 205」**——面積值 `205.27`／`205.49`、commit `205f3e6`／`43a205b`、行號 `:205`、列數「增 `205` 列」、八進位跳脫 ⇒ **號 `205` 未被占用** ✅ |

🔒 **量測器自檢（戒 `㉑`·人造二造對照·跑在被測工具那一層）**：

| 對照檔 | 定義列式 | 錨定框 出現次數 | 期望 | 判 |
|---|---|---|---|---|
| `pos.md`（含 `### 自誤 \`205\`` ＋ 行內裸 `205`） | `['205']` | `2` | `(['205'], 2)` | ✅ |
| `neg.md`（`1205`／`2050`／`-205`／`12053`） | `['1205']` | `0` | `(['1205'], 0)` | ✅ |

🩸 **CC 自捕**：首版負對照置 `x205y`，實得 `1` ⇒ 該框只排除**相鄰數字與前導 `-`**、**⛔ 不排除字母**。
⇒ 係**對照檔設計之誤**、⛔ 非框之誤 ⇒ **⛔ 未改被測之框**，改對照檔。

---

### 🔒 收工聲明

1. 🔒 **`補正① §零` 之停機解除**：`W-2` 重判 `4`／`4` 成立（繫於 `④` 之四項條件全備）。
2. 🔒 **`V-2` 成立** ⇒ `926a0de` 之 `+7` 列對 `Z-1`〜`Z-5` 之錨**⛔ 無影響**、⛔ 無須重跑。
3. 🔒 **本補正⛔ 零碼面變更**（`app.py` blob 仍 `68f7bfab…`／`verify/**` 逐檔未動）；
   ⛔ 未重跑 `run_all`、⛔ 未改任何探針、⛔ 未重產任何 baseline、⛔ 未入倉任何新探針。
   （`④-c`／`④-d` 之實跑一律於**拋棄式複本** `C:\w171a`，跑畢整份刪除。）
4. 🔒 **push 仍須 KL 核可**（`W-G.9-169 S-8` 全效）；`926a0de`／`b9cb3cf`／`6a4a0da` 及本 commit
   **⛔ 不得被記為倉態**（常規九 一）；`/c/w169b` **保留不刪**。
5. 🔒 **push 後之待辦一項**：取號以 `origin` 為母體**複現**並就地加註（`§三 P-1` 之特別處置）。

---

## 🔧 就地加註（`W-G.9-174`·`2026-08-30`）：`VR-074` 落地之 **app 路徑土地後果**——本報告當時⛔ 未預測、⛔ 未驗收

🩸 **本節係對本報告之補正，⛔ 非否定。** `Z-1`〜`Z-5` 之結論於其**所宣告之射程內**全部成立；缺的是**射程本身⛔ 未涵蓋 app 路徑之抵費地池面積**（`GB-118`）。

### 一、實測（KL 三趟 UI 實跑·`3.5m`·參數經 `verify/case_params_UC9898.json` 比對確認相同）

| 街廓 | 量 | `a3c97aa`（落地前） | `2e08a41`（落地後） | Δ |
|---|---|---|---|---|
| `R1` | 宗數 | `6` | `5` | `−1` |
| `R1` | Σ池（未捨入） | `1175.0918` | `1175.0963` | **`+0.0045`** |
| `R4` | 宗數 | `3` | `2` | `−1` |
| `R4` | Σ池（未捨入） | `1228.3533` | `1228.3568` | **`+0.0035`** |
| `R2`／`R6`／`R3` | 宗數・Σ池 | — | — | **逐位相同** |

🔒 顯示層各 `+0.01`（`1175.09→1175.10`／`1228.35→1228.36`）係**捨入邊界**所放大；底下之實變動為 `+0.0045`／`+0.0035`。
🔒 宗-宗疊上界隨宗數收緊（`0.8287→0.6630`／`0.3311→0.1656`），算術與訊息所載之式 `(宗數−1)×0.005×深度` 相符。

### 二、機制（現查·⛔ 非推測）

二態之 `R1` 皆逐字含 `🟢 [配餘] 街廓 R1：幾何餘 5.3835㎡ 入抵費地（K-9-5-7 配餘）`，**面積逐位相同**。
⇒ 🔒 ghost 所佔之 `5.28 ㎡`（`_GHOST_(R1)` 之 `_ghost_area_m2`·見 `verify/out/probe_WG9148_ghost_rank_c9bce13.log:26`）**於舊態即已流入抵費地**；`L-4` 排除 ghost 後，該空間僅**由「配餘」改歸「池帶」**（第二池帶 `s` 寬 `0.3247 → 17.8897`），面積幾乎不動 ⇒ 殘餘 `+0.0045`。
⇒ 🔒 **`L-4` 之效果係「ghost ⛔ 不再被計為<u>宗地</u>」**，⛔ 非「其面積被改判歸屬」。**方向係修正**（app 之 Σ池自此與 harness 之 `1175.096297168` **逐位相同**）。

### 三、`16` 格預測集合於 app 路徑**亦成立**

發單側就 KL 二趟之四份匯出 CSV 逐列 `diff`：`W-D.1.3-d 驗收_抵費地情形` `0` 列相異／`W-B 驗收_街角規定範圍面積` `0` 列／**`第 1 宗街角地指配結果` `0` 列**（🔒 **配地結果零變動**）／`W-D.1.2 診斷_逐候選評點明細` `8` 列（＝ `4` 行 × 前後），其**唯一**相異欄為**原位次**：`R4/右/628-1(1)` `3→2`、`R1/左/628-36(1)` `4→3`、`R1/左/628-37(1)` `3→2`、`R1/右/628(5)` `6→5`
⇒ 🔒 **與 `verify/out/WG9167_predicted_diff.json` 逐格相同**，且**除該欄外全表零變動**。
🔑 **更強之證**：前三份匯出檔之 `sha256` **二態逐位相同**（`8458e075…`／`d0adeee1…`／`f5447f5f…`）⇒ ⛔ 非「內容等價」而係**位元組全等**；唯一相異之診斷明細其**位元組數二態同為 `2783`**，與「四處單字元數字替換」相符、⛔ 無其他增刪。
🔒 **輸入資料已入倉**（`W-G.9-174 N-0`）：八份匯出檔 ＋ 二趟終端機逐字，路徑見本報告 `N-0-c`；本節之全部數字**皆可自倉內複現**。

### 四、⛔ 本加註不變更之事

⛔ 不變更 `VR-074`／`VR-079` 一字（`VR-079 三` 之「落地批⛔ 不重產 baseline」全效）；⛔ 不變更本報告 `Z-1`〜`Z-5` 之任何結論或數字；⛔ 不追改 `W-G.9-167R`／`-168R` 一字；⛔ 不改凍存名單。
🔒 **`Δ +0.0045`／`+0.0035` 之處置**：其屬 baseline 與現況之差，歸**波末重烤**（`GB-111`·KL 已裁時點：俟二閘落地後）；本批⛔ 不重產。
