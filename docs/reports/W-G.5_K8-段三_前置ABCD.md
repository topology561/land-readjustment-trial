# W-G.5 K-8 段三【前置 A–D】施工報告

> 基準 `HEAD == origin/wip/s1-endpart == 45b943e`（施工前）。
> 施工單：K-8 段三（錨定 `45b943e`），依其授權「**容量不足時先交前置、本體留下批**」。
> **【本體】§三 街角規定範圍新構造：本批未開工**（另見 §五 之相依項）。

---

## 零、一分鐘定位

四項前置中，**兩項的施工單前提經實測不成立**：

| 前置 | 狀態 | 摘要 |
|---|---|---|
| **A** 脫鉤偵測閘 | ✅ 完成 | `run_all` 每趟印出「引擎↔快照 脫鉤清單」六塊逐塊；`_ok_der` 已具名登記為波末遷移項 |
| **B** SIDE_LINE 配對驗證 | ✅ 完成（**前提更正**） | 現行**不是**貪婪 1:1——C-6 早已換成結構判準；探針已固化並掛 `run_all` |
| **C** 清除 `14.0` 硬編兜底 | 🔴 **停機上呈 U-K8-5** | `14.0` **不是兜底、是每趟必走的實際路徑**；逕改 raise ＝ PK 路徑每次必炸 |
| **D** 正典更正 | ✅ 完成 | 裁定檔補 §二-2／§二-3；失敗考古 #36 |

---

## 一、前置 A：脫鉤偵測閘

**做法**：`fixture_block_depth_n19p.py` 之 **T10 段**（app-live N-19′ 2dp vs 快照深度·六塊逐塊）
於 `run_all [1/3]` 中**無條件回顯**，不判紅（脫鉤係 KL 裁 U-K8-1 ＝ 乙之授權現況）。

**併記（波末遷移項）**：`run_verification.py` 之 `_ok_der`
（`grep -n "_ok_der = " verify/run_verification.py`）為**寫死值且綁舊快照**，
換快照前**恆綠**。已於該處加註：

> 綠燈**不代表段二正確**——它證明的是「舊輸入下什麼都沒變」。

**本批不改其值**（改了就是假紅）；K-8 全案換快照時**同批**更新為 `115.85 / 57.93`，
連同 `wd4_tier_list.MINA_QU_EXPECT`／`HALF_EXPECT` 與該測項**名目字串本身**。

⚠️ 施工實作曾出一次 bug：擷取 T10 段之迴圈在標題後**第一條分隔線**即 break，
清單只印出標題一行。已修（須跨過首條分隔線、待收到表身後才收段）。

---

## 二、前置 B：SIDE_LINE 配對驗證 —— **施工單前提更正**

### 2.1 前提更正

施工單稱 `app.py:1281-1283` 現行為「**貪婪式 1:1 配對 ＋ 街角地校正**」、與失敗考古 #38 同族。
**實測：該敘述來自 Phase 11 時期之陳舊註解，機制早已換掉。**

現行＝ **C-6**（KL 裁 2026-07-26）之**結構判準**：
`_best_block()` ＝ `_line_block_overlap()`（共線＋投影重疊長）取最大者、
同分按塊名之**決定性排序**、勝差 ≤ `_BIND_MARGIN_M` 入 `_ambig`（禁靜默選）；
側別由既鎖之 `FRONT_LINE p1=左／p2=右` 端點距離導出，逾 `_TOL_SL` 亦入 `_ambig`。
⇒ **與 BASELINE 之 C-1~C-5 同族**。

⇒ 施工單所列「**登記泛化項：貪婪 1:1 須比照 C-1~C-5 改為逐街廓獨立解**」
**已由 C-6 完成、無待辦**。
該陳舊註解**正是本次誤讀之來源**，已改寫並註明考古（見 `app.py` `_side_line_buffer` 上方）。

### 2.2 量測結果（新探針 `verify/probes/probe_ruling_K8_sideline_pairing.py`·rc=0）

**8 實體、8 街角側、無共用**——施工單對照靶逐格重現：

| 實體 | 認領 | 長度 | 重疊率 | 靶 | Δ | 截角缺口(m) |
|---|---|---|---|---|---|---|
| #0 | R2 | 44.955 | 0.9212 | 0.92 | +0.0012 | 3.544 |
| #1 | R3 | 44.899 | 0.9225 | 0.93 | −0.0075 | 3.479 |
| #2 | R5 | 43.530 | 0.9185 | 0.92 | −0.0015 | 3.548 |
| #3 | R4 | 33.084 | 0.8933 | 0.90 | −0.0067 | 3.529 |
| #4 | R4 | 33.268 | 0.8884 | 0.89 | −0.0016 | 3.712 |
| #5 | R1 | 33.270 | 0.8970 | 0.90 | −0.0030 | 3.427 |
| #6 | R1 | 33.247 | 0.8897 | 0.89 | −0.0003 | 3.668 |
| #7 | R6 | 43.382 | 0.9216 | 0.92 | +0.0016 | 3.401 |

容差 `0.01` ＝ 靶所述之 **2dp 粒度**（非由殘差回推之閘寬）。

**側別對拍**：`cad['side_lines_by_side'][blk][side]['mid']` 與該實體中點**差 0.00e+00m**（八側全中）
——R1 左=#6／右=#5、R2 左=#0、R3 右=#1、R4 左=#4／右=#3、R5 左=#2、R6 右=#7。

**歸屬決定性**：勝差（首名−次名，公尺）＝ **29.56〜41.42m**，遠大於 `_BIND_MARGIN_M = 1.0`；
`diagnostics.binding_ambiguity` 之 SIDE_LINE 條目 **0 筆**。
⇒ **停機條件 1 不觸發。**

**截角缺口 3.40〜3.71m**（皆非零）⇒ 佐證施工單之
**「SIDE_LINE 實體 ＝ 截角前側界；街廓多邊形邊界 ＝ 截角後側界」**（本體 §2 之實證基礎）。

### 2.3 🔑 施工中自犯之兩次同型錯誤（均已改用倉內機制）

1. **自訂容差**：初次以 `10×q`（µm 級）自寫「共線重疊」，得「`#6` 對所有街廓皆 0.0000」之假象，
   險些誤報停機條件 1。倉內原語 `_line_block_overlap` 用 `ang_tol_deg=2.0／perp_tol_m=1.0`
   ——**正是為「SIDE 線畫到未截角尖角」而設**。
2. **自創更嚴之判準**：把「無共用」寫成「重疊率 > 0 者恰一塊」，
   而 `#0/#2/#3/#4` 對鄰塊有**次毫米級**重疊 ⇒ **4 筆假紅**。
   歸屬之正典是「重疊最大 ＋ 勝差 > 1.0m」，不是「唯一非零」。

⇒ 兩者同一病根：**用自己的判準去驗別人的機制**。已寫入探針 docstring。

---

## 三、前置 C：🔴 **停機上呈 U-K8-5**（前提不成立）

施工單令「缺值 ⇒ loud raise、刪 `14.0`」，前提為「`14.0` 係休眠安全網」。**實測兩處皆為實際路徑**：

| 位置 | 讀的鍵 | 實況 |
|---|---|---|
| `_legal_depth_B4`（`select_corner_lots_both_sides_v12` 內·街角 PK） | `f3L_sb_rows_by_label` | 該 session 鍵**全倉無人寫入**（唯一提及＝`selection_pipeline.py:293` 之註解）⇒ 首源恆 0；次源 `_candidates` 之 **15 鍵集不含**`平均深度(m)`／`街廓分配深度(m)` ⇒ 亦恆 0 ⇒ **每趟每塊皆落 `14.0`** |
| `_min_area_by_block`（`main()`） | `f3_sb_rows`（臨街負擔列） | 該表**無**`街廓分配深度(m)` 欄（該欄在**另一張表** `f3L_corner_min_table`）⇒ **恆走 `else: _info['min_area']`** |

**自證**：`python -c "import sys;sys.path.insert(0,'verify');import run_all;print(sorted(run_all._candidates_append_keys('app.py')))"`
⇒ 15 鍵、無任何深度欄。

**後果**：
- 逕改 raise ⇒ app 與 harness 之 PK 路徑**每次必炸**（不可推）。
- 改讀真來源（`f3_alloc_depth_by_label`／`f3L_corner_min_table`）⇒ corner_polygon 深度
  由 `14.0` 變為 **33.15〜45.71**，**直接改動跨占街角面積 → 三指數 → PK 勝負**
  ——該 delta 即**本體 §三**（街角規定範圍新構造）之範圍，不得在前置批夾帶。

⇒ **本批不改行為、僅具名登記**（`app.py` 兩處註解·`+21/−2` 全為註解；
唯一非註解 diff 為同一敘述之行尾註解文字）。修法與 delta 量測**隨本體同批**。

---

## 四、前置 D：正典更正

- 裁定檔補 **K-8 §二-2**（U-K8-2）：段二施工單 §三「單一咽喉」之表述更正為
  **兩條路徑、兩種鍵拼法**（附產／耗對照表）。
- 裁定檔補 **K-8 §二-3**（U-K8-5）：前置 C 之停機記錄。
- **U-K8-4**：錨 `run_verification.py:1122` 指錯行（實為 `:711-713`），已改引符號名·維持。
- 失敗考古 **#36**：跨 commit 引錨未重 grep ＋ **陳舊註解變成下一個人的錯誤前提**
  （三個獨立實例：`:1122` 指錯行／「單一快照鍵」／「貪婪 1:1」）。
  通則含「**改機制時把舊機制的名字從註解裡一併改掉**——否則舊名會活得比舊碼久」。

---

## 五、驗收

**對照基準** `verify/out/K8_seg2.log`（`45b943e`）→ **本波** `verify/out/K8_seg3_pre.log`。

### 5.1 測項名目集合雙向 diff
`132 → 132`｜消失 **0**｜新增 **0**｜驗算 `132 − 0 + 0 = 132` ✅。

### 5.2 PASS/FAIL **狀態**逐項比對
共同名目 132｜**狀態翻轉 0**｜PASS 130｜FAIL 集合逐字未變＝
`W-F F.4`（3.5m E2 不可行·既有）／`W-G G.2`（F.4 純連坐）。

> 本批 `app.py` **零行為變更**（唯一非註解 diff 為同一敘述之行尾註解文字），
> 故零翻轉屬 **(甲) 真的純加性**——與段二之 **(乙)** 不同（失敗考古 #34）。

### 5.3 golden 段清點（施工單 §八·名目/狀態 diff 皆看不到此段）
**14 → 15 支**；新增 **1**（`K-8 SIDE_LINE 配對驗證閘`）、移除 **0**；**全 15 支 ✅**。

### 5.4 前置 A 驗收：脫鉤清單每趟顯示·六塊逐塊

```
【T10】app-live 深度 vs 快照深度：制度看守
  街廓      app N-19′(2dp)        快照         Δ
  R1               33.15     32.97     +0.18
  R2               44.47     43.90     +0.57
  R3               44.34     43.64     +0.70
  R4               33.10     32.59     +0.51
  R5               45.71     45.04     +0.67
  R6               45.51     44.59     +0.92
  ⇒ **制度甲：已知分岔態**（K-8 段二施工後·快照未換）　✅
```

### 5.5 前置 C 驗收（**部分不適用**）
施工單原訂「`grep -n "14\.0" app.py` 於該區段零命中 ＋ 反例夾具 rc=0」。
因前提不成立（§三）而**未施作**——`14.0` 仍在（為實際路徑），
該區段之命中僅為**說明其已登記**之註解與訊息文字。反例夾具亦未建。

### 5.6 其他
`py_compile` 綠（`app.py`／`run_all.py`／`run_verification.py`／新探針）；
`REAL_EXIT=1` ＝既有 2 紅所致、與前波同值。

---

## 六、🚩 上呈 KL

| # | 題 | 現況 |
|---|---|---|
| **U-K8-5** | 前置 C 之 `14.0`／`_info['min_area']` 係**實際路徑**非兜底；修之即改 PK 勝負 ⇒ 應與**本體 §三 同批**。請確認併批 | **停機待裁** |
| **U-K8-1** | 換快照＋重烤（KL 已裁＝乙：留到 K-8 全案完成後一次換）。本批已依裁定加脫鉤清單 | 已裁·執行中 |
| 前置 B 泛化項 | 施工單所列「貪婪 1:1 改逐街廓獨立解」**已由 C-6 完成** ⇒ 建議自 backlog 移除 | 知會 |

## 七、本體（§三）開工前之相依項

1. **U-K8-5 併批**（見上）——本體改街角範圍構造時，`_legal_depth_B4` 之來源必須一併修正，
   否則新構造仍吃 `14.0`。
2. 前置 B 已坐實 **SIDE_LINE ＝ 截角前側界／街廓邊界 ＝ 截角後側界**，本體 §2 可直接引用。
3. 本體之 delta 母體須用**包住 `diff_rows` 之全母體落檔**（見段二報告 §六），
   **禁**照 `viol[:12]` 之打印輸出寫（K-8 段二曾因此差點漏掉 G033）。

---

# 追加：【前置 E／F】（施工單 `262e5a0` 批·**取代原前置 C**）

## 八、前置 E：刪死碼 —— **U-K8-5 上半撤銷**

### 8.1 我上一批錯在哪

前一批據「`14.0` 恆被走到」上呈 U-K8-5（「改之即動 PK 勝負」）。**錯了。**
**追到「這個值恆被走到」就停了，沒再追一步「這個值有沒有人用」。**

獨立復驗（`262e5a0` 當場 grep）：

| 端 | 實況 |
|---|---|
| **寫入端** | `f3L_sb_rows_by_label` **全倉無人寫入**（`grep -rn` 後只剩考古註解） |
| **讀取端** | `_legal_depth_B4` 共 **5 次**出現（`:9290/9291/9292/9297/9298`），**全為賦值與條件、零讀取**；`grep -n "_legal_depth_B4[,)]"` **0 命中** ⇒ 從未當引數傳出 |

跨占實際用的是 `_build_corner_range_v2` 之輸出
（`_corner_range_left`（`:9324`）→ `_corner_poly_p1_B4`（`:9365`））
——**W-B §5 搬過去時即已架空該段，只是沒刪**。
⇒ 整段連同 `14.0` 為**死碼**，刪除**不影響任何結果、不改 PK 勝負**。

### 8.2 已刪

`_sb_rows_B4` → `_blk_param_B4` → `_legal_depth_B4` 之封閉死鏈，
連同其上方**已過期之 Patch E-1.8 註解**（該註解描述的是一個**從未生效的 patch**）。

**驗收**：`grep -n "_legal_depth_B4\|_blk_param_B4\|_sb_rows_B4" app.py` ⇒ **非註解 0 命中**。
`f3L_sb_rows_by_label` 於 `app.py` 尚有 **3 處命中、全為註解**（考古）——
施工單原訂「零命中」，此處保留考古文字，**無任何可執行引用**（已逐行確認）。

## 九、前置 F：登記，本批不動

`_min_area_by_block` 恆走 `else` ⇒ `get_min_lot_size` 之 `min_area`
（**衍生值**＝`round(min_width × min_depth, 2)`，非畸零地附表欄位；
住宅區×路寬 7–15m ⇒ `3.50 × 14.00 = 49.00㎡`）。
**唯一活消費端** ＝ `merge_subparcels_by_parent(g_rows, min_area_by_block)`
——即 **K-6 §五待置換之「同原地號合併」**；`f3_min_area_by_block` 無下游。
⇒ 已下具名註解，**由 K-6 §五 一併處理，K-8 段三不動**。

## 十、波末遷移項登記（只登記不改）

已入裁定檔末「K-8 波末遷移項登記」節：`wf_f4` 三處 `114.07`／`run_verification._ok_der`
＋其測項名目字串／`wd4_tier_list.MINA_QU_EXPECT`・`HALF_EXPECT`／`WIDTH_VERDICT_CORNER_K4`。
四者須與快照 ＋ v3 baseline **同批**更新。

## 十一、🔒 本體開工前之既有事實（免下一手重複懷疑）

1. **門檻與跨占同源**：`_corner_poly_p1_B4 = _corner_range_left`，而
   `_corner_range_left = _build_corner_range_v2(...)` ⇒ **改一處即兩處同步**，
   不會產生兩套互相矛盾的街角形狀。（施工單 §8 要求明記者，已坐實。）
2. **兩套側界資料層分得開**：重疊率 0.8884–0.9225、缺口 3.40–3.71m（≒截角股長）。
3. Patch E-1.8 **已刪**（死碼）；Patch E-1 §6 **未生效·已登記**。

## 十二、驗收（前置 E／F）

`verify/out/K8_seg3_pre.log` → `verify/out/K8_seg3_EF.log`：
**名目 132 → 132**（雙向 diff 空）｜**狀態翻轉 0**｜PASS 130｜FAIL 集合逐字未變
（`W-F F.4`／`W-G G.2`）｜**golden 段 15 → 15 支全綠**｜脫鉤清單仍每趟顯示。
⇒ 符合施工單「純刪死碼 ⇒ 名目雙向 diff 為 0」之驗收；**且狀態亦零翻轉**，
即該段確為死碼之獨立佐證。

## 十三、失敗考古 #37

> **一個從未生效的 patch，其註解會取得與生效者相同的可信度。**

通則含：**凡 patch 註解宣稱「改用 X」，必須同時指出 X 的寫入點與該值的讀取點**；
**影響力分析的最小單位是讀取點、不是執行點**；
**搬遷式重構最容易留下這種殘骸**（舊碼還在跑、還在算，只是沒人要它的結果）。

## 十四、【本體】§三：**本批未開工**

全新幾何（最小寬度帶 ＋ ALLOC_LINE 解析定位 ＋ 範圍多邊形）＋ 八街角 delta ＋
PK/F.0–F.4 連帶量測，需兩趟長跑；以本批剩餘容量開工會留半套，
依施工單「容量不足時先交前置、本體留下批」之授權留下批。
開工前之相依項已備齊（§十一）。
