# CC 新 session 交接文：**K-6-A2 D-2b 波**（D-2b-2 〜 D-2b-20 已落地·**次步＝T2 接線**）

**產出**：CC／2026-08-09　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`
**交接時 HEAD**：`63bc889`（`D-2b-20`·已 push·`origin` 實查同·工作樹追蹤檔異動 **0**）
　（沿革：初版 `0f9e0f8`／`D-2b-17` `dca0a85`／`D-2b-18` `8b88c13`／`D-2b-19` `63bc889` 前之 `9344da2`）

> # 🔴🔴 新 session 最先讀這一段：**12 格 sha256 會全變，那是預期的**
>
> `T2` 接線（**`D-2b-21【b-2】`**）會改 `_pool_strips_for_block`、令每條界線各自帶方向
> ⇒ **12 格全保真基準之 sha256 <u>必然全變</u>**。
> ⚠️ 該基準自 `D-2b-10` 起**連續十批逐格未變**——這個「未變」本身已養成一種預期，
> **必須主動拆掉**：⛔ **見全變即判為壞，是本文最想擋下的第一個反應。**
> ✅ 正確反應 ＝ **逐格歸因**（12 格逐一說明「為何變」「變成什麼」「是否為 T2 之預期後果」）。
> ⛔ **無歸因之全綠與無歸因之全紅，同樣不可採信。** 詳 §八。

> 🛑 **第二件事：讀 §二（停機／待裁五項）**——⚠️ 其中 **2-1 已由 `D-2b-17` 大幅縮小**（見該節 🔧 段）。
> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前**重跑該指令**（行號／計數皆易腐）。
> 🔒 **出處標注**：本文新寫入之數字依 `CLAUDE.md` 之 `【倉】`／`【單】` 規範
> （`grep -n "【倉】" CLAUDE.md`）——`【倉】` 必附 `檔:行`；`【單】` **⛔ 不得被下游引用為事實**。
> 🔴 **§三 列有一批「已作廢·不得引用」之數字**——開工前必讀。
> 🔒 **前一份交接文** `docs/reports/W-G.8_D波_新session交接文.md` **仍在**（涵蓋 D-0〜D-2b-1），
> 其 §四 #1 之 `app.py:17986-17987` **已腐**（見 §三）。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD                                     # 應 ≥ 63bc889（D-2b-20）
git ls-remote --heads origin | grep s1-endpart                 # ⛔ 須與 HEAD 同·收官硬前置
python -c "import shapely; print('shapely', shapely.__version__, '| GEOS', shapely.geos_version)"
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md      # 現值 50
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # ⛔ 節號以現查為準·本文不寫死
grep -n "^### 🔒 K-9-5-[5678] " docs/rulings/K-6_街角地分配程序與可分配判準.md
python verify/fixtures/test_D2b15_tilegate.py                  # 應 rc=0
python verify/fixtures/test_D2b16_mutation.py                  # 應 rc=0
```

⛔ **禁 `git reset --hard`**。⛔ **禁對 `verify/out/` 用 `git add -A`／`.`**。
⛔ **禁覆寫任何已入庫 log**（基準探針已內建 `_assert_not_exists`，須設 `WV_BASELINE_LOG=<新檔名>`）。

🔴 **grep 蒐證一律排除巢狀 worktree**：`| grep -v "^\./\.claude/worktrees/"`
（`.claude/worktrees/` 內有停在舊 commit 之 `app.py`，會回錯行號；由 `.git/info/exclude` 排除
⇒ **不污染 commit、只污染 grep**）。

---

## 一、現況

| 項 | 值 | 現查指令 |
|---|---|---|
| 分支／HEAD | `wip/s1-endpart` ／ **`63bc889`**（`origin` 實查同·追蹤檔異動 0）| `git rev-parse --short HEAD` ＋ `git ls-remote --heads origin` |
| 環境 | `shapely 2.1.2 \| GEOS (3, 13, 1)` | 見 §〇 |
| 登記表 | **50 列**（本波新開 `GB-47`〜`GB-50`） | 見 §〇 |
| 失敗考古末節 | **⛔ 以現查為準·不寫死**（撰文時 53；`D-2b-18【g-1】` 後 55；`D-2b-19【h-1】` 後 **56**） | 見 §〇 |
| 🔒 **`【倉】`／`【單】` 出處標注** 🆕 | **常設規範·已入 `CLAUDE.md`**（`D-2b-19【h-2】` 立）：`【倉】`＝現查可得、**必附 `檔:行`**；`【單】`＝僅見於施工單／對話／報告、**倉內查無** ⇒ 🔴 **下游不得引用為事實**，須引用則**先入庫**。**來源不明一律預設 `【單】`**。🔒 另立「**兩個近似值相符 ≠ 相互驗證**」（具名實例：`72.2002` 距真值 `72.1964` 為 `0.0038`、距編造值 `72.2032` 為 `0.0030`，**一樣近** ⇒ 鑑別力為零）。⚠️ **下一手撰寫任何文件（施工單／報告／交接文／考古節次）一律適用，⛔ 不限本波。** | `grep -n "【倉】" CLAUDE.md` |
| 正典新則 | `K-9-5-5`@667／`K-9-5-6`@745／`K-9-5-7`@793／`K-9-5-8`@818 | 見 §〇 |
| **T2 接線** | **尚未接線**——`_block_partition` 生產路徑呼叫 **0**、未入 `_WF_NS_NAMES` | §四 |
| **`run_all`** | 🔴 **本波從未跑過**（最後一份 ＝ `verify/out/D2b1_runall.log`） | `ls verify/out/D2b*_runall.log` |
| 期望 FAIL 名單 | 現行仍為 `K6A2_期望FAIL名單_D2b1切換後.txt`（**22** 名目·`grep -c "^🔴 FAIL"`·⛔ 非 `wc -l`） | 同上 |

### 1-1 逐批 SHA（全部已 push）

| 批 | SHA | 性質 |
|---|---|---|
| D-2b-2 | `c0e0706` | ⛔ 生產碼零觸·暴露量測（方法前提不成立·上呈） |
| **D-2b-3** | `dc4b441` | 🔴 `K-9-5-5` 入正典 ＋ 三閘合圍一般化（`Δ_i`） |
| **D-2b-4** | `664e4f5` | 🔴 `K-9-5-6` W 鏈定式落地 ＋ `K-9-5-5` 半條撤回 |
| D-2b-5 | `e82af35` | `100.02` 歸因結案 ＋ `K-9-5-6` 改寫為追認 ＋ `GB-47` |
| D-2b-6 | `2f44ab3` | ⛔ 生產碼零觸·分跑保值性實證 ＋ **首次取得全六街廓** |
| D-2b-7 | `08df6bd` | ⛔ 零生產碼·R6 成因查明（`SIDE ∥ ALLOC`） |
| **D-2b-8** | `2957a3b` ＋ `f8674de` | 🔴 守衛判準修正 ＋ `K-9-5-8` 入正典／R6 全鏈量測 |
| **D-2b-9** | `d57e316` ＋ `426023c` | 🔴 退化分支**改斷言**（原為豁免）／閘族清單自生成 |
| D-2b-10 | `00a8a3a` ＋ `edbc784` | ⛔ 探針側·去萬用桶＋雙向對帳／全保真基準（sha256） |
| **D-2b-11** | `2ffde31` ＋ `2aebea5` | 🔴 `_block_strip` 加**雙線形式** `n_hat_far`（呼叫端零改） |
| **D-2b-12** | `1d6c153` ＋ `4e20c7b` | 🔴 同向即**委派回單線**＋`_STRIP_PARALLEL_TOL`／`GB-48` |
| **D-2b-13** | `3339a2c` ＋ `265d79a` | 🔴 新增 **`_block_partition`**（依序削餘＋鋪滿閘）／`GB-49` |
| D-2b-14 | `cd611b9` ＋ `bf91181` | ⛔ 零碼面·區帶邊界實測（🛑 掃不到·未定門檻） |
| **D-2b-15** | `2d2fd01` ＋ `556ecf4` | 🔴 鋪滿閘② **改面積鏈恆等式**／回歸案例入庫 |
| D-2b-16 | `2ce620c` ＋ `0f9e0f8` | ⛔ 零碼面·對拍補欄＋突變測試／`GB-50`（🛑 上呈） |
| **D-2b-17** | `dca0a85` | ⛔ 零生產碼·**重疊量兩法對打 ＋ 五處運算元判定** ⇒ 🔑 **重疊為真、非假陽性**（詳 §二-1 🔧／§五 🔄） |
| D-2b-18 | `e97664c`（g-1）＋ `8b88c13`（g-2） | ⛔ 零碼面·失敗考古節 **54／55** 補記／交接文考古節數改現查 |
| D-2b-19 | `9344da2`（h-1）＋ `63bc889`（h-2） | ⛔ 零碼面·考古節 **56**／`CLAUDE.md` 立 `【倉】`／`【單】` 規範 |
| **D-2b-20** | *（本次）* | ⛔ 零碼面·**本交接文更新至 `T2` 接線可開工狀態** |

---

## 二、🛑 停機／待裁（**開工前必讀**）

### 2-1 🛑 **`GB-50`：生產碼有 3 處 sliver 互切**（D-2b-16·**擋 T2 接線**）

`.intersection()` 在 **sliver × sliver** 時會給出**與自身 `contains` 矛盾**之答案
（實證：`verify/fixtures/case_D2b15_tilegate_intersection.py`——
`pieces[2].intersection(pieces[4]).area = 924.201867`＝**片4 全部面積**，而
`片2.contains(取樣點) = False`；分割本身經**帶號**檢驗為正確）。

**AST 窮舉**（⛔ 非字面 grep）：`app.py` 集合運算 **72** 處、`.intersection()` **26** 處（**含鏈式 4**）。
**sliver 互切 3 處，全在 `_pool_strips_for_block`**：

| 行 | 運算元 | 所屬閘 |
|---|---|---|
| `:9047` | 池片 × 池片 | ②-池 |
| `:9050` | 池片 × 業主宗片 | ②-池 |
| `:9062` | 業主宗片 × 業主宗片 | ②-宗 圍堵 |

⇒ **與 `_block_partition` 舊閘② 同型** ⇒ 若同病，該二閘可能**誤報**。
⚠️ **R1–R6 每街廓每次 Step-G 皆執行該三處。**
⚠️ 另 **5 處未判定**（`:4525`／`:9120`／`:11379`／`:11391`／`:16238`）——⛔ **未判定 ≠ 安全**。
**待裁**：是否比照改面積鏈恆等式／處置範圍與時機。實料 `verify/out/probe_D2b16_tail.log`【§e-3】。

🔧 **具名更新（`D-2b-17`·⛔ 上文一字未刪）——本節之待裁範圍已大幅縮小**：

1. 🔑 **「該三閘是否<u>誤報</u>」⇒ 已答：否。** 三處所報之重疊量係**真實幾何**、⛔ 非假陽性。
   甲法（`.intersection()`）於**全部 20 對**皆正確：大值 R2 甲 `76.2616`／乙 `77.7522`、
   R5 甲 `72.1964`／乙 `74.1497`【倉】`verify/out/probe_D2b17_dual.log`【f-1】；
   甲≈0 之 14 對經**實寬**判準（與面積無關、與 `.intersection()` 無關）裁決，
   `max_d` **全部 `0.000000`**（<5e-7 m）⇒ 乙之貼邊偏壓、甲為真
   【倉】`verify/out/probe_D2b17_adjacent.log`。
   ⇒ 🔑 **`T2` 驗收靶「破量欄全部歸零」⛔ 不必換量測器。**
2. 🔑 **5 處未判定 ⇒ 已全數判定，<u>無一為 sliver × sliver</u>**（實跑攔截·⛔ 非讀碼推定）
   【倉】`verify/out/probe_D2b17_operands.log`【f-3】。⚠️ 惟 `:4525` 須盯，見 §六。
3. 🛑 **殘留待裁者僅一**：本列之**觸發特徵仍未查明**（§2-2 未動）
   ⇒ 「**他處**是否比照改判準」仍屬域邊界。
   ⛔ **不得**因 1./2. 而推論 `.intersection()` 於本倉他處必然可靠。
4. ⇒ 本節標題之「**擋 T2 接線**」⚠️ **應讀為：擋的是「是否要一併改判準」之決策，
   ⛔ 不再擋「T2 接線本身」**——接線之驗收靶已確認可用。

### 2-2 🛑 **觸發特徵未查明**（`GB-50` 之另一半）

**已排除**：① 版本差異（兩側環境一致）／② 單純淺角（D-2b-14 掃 `0.01°–90°` **630 例** 0 不一致）／
③ 界線未排序（**80 例** 0）／④ 組內散布本身（D-2b-14 `θ=45°` 檔散布 ±6.75°、`θ=90°` 檔 ±13.5°，皆 ≥5° 卻 0）。
**未查明**：**確切之街廓幾何特徵**——D-2b-15 §d-2 顯示，於**該 fixture 之街廓**上散布 ≥5° 即觸發、
且平均 θ 含 **45°**（⛔ 非淺角），而 D-2b-14 之隨機街廓母體**全域不觸發**
⇒ **觸發條件繫於街廓幾何本身**，⛔ 非單由界線角度組態決定。
🔒 **未證之觀察**（明標：**未證·claude.ai 提出**）：可疑特徵為「兩個 sliver 互切」。

### 2-3 🛑 **Gate C `結構閘 理論＝實跑`**（D-2b-3 起·**循環相依**）

成因已定位：`_select_pool_slot(widths, …)`（`grep -n "def _select_pool_slot" app.py`）之
理論母體是**宗地寬度**、**不是 `W`** ⇒ 即「甲-1 選位採同一定義」。
而歷批施工單皆令「選位只做對照表、**禁落地**」⇒ **Gate C 擋住覆蓋、甲-1 落地又是 Gate C 之解**。
⚠️ 3.5m R2／R5／R3 之 `Δ ≈ 55.6`（`100 − 44.41 = 55.59 ≈ R(6.98)`）疑為 `GB-47` 種子（**假說·未證**）。

### 2-4 🛑 **`GB-49`：同向委派誤差之累積無人看管**

`_block_strip` **無狀態**，每次呼叫獨立委派；單次上界 `½·L²·tol`
（D-2b-13 改**實際弦長**後：R1 `3.842593e-03`／R2 `4.950885e-03`／R3 `4.645035e-03`／
R4 `4.824801e-03`／R5 `4.243765e-03`／R6 `3.975492e-03`）——**單次即達覆蓋閘 `0.01` 之 38–50%**
⇒ **3 次同號累加即撞閘**。⚠️ 目前僅 R6/right 觸發委派 ⇒ 尚不累加。**歸屬 ＝ T2 接線批之帳層。**

### 2-5 🛑 **`GB-47`：`W_prev` 種子 app-vs-harness 分歧**

`app.py:19474`／`:19475` 仍為**已廢止式** `_left_buffer_S * _cos_dn`（另 `:19678`／`:19680` 之 `b` 參數，
**共 4 處**）；`verify/stepg_pipeline.py:615` 為 `0.0`。而 **stepg 早已遷至 `_mp_base_W0`**
⚠️ **行號已較 `GB-47` 原載（`:19217`／D-2b-5 時）位移**——本波對 `app.py` 之新增所致；
現查指令：`grep -n "_left_buffer_S \* _cos_dn\|_right_buffer_S \* _cos_dn" app.py`。
⇒ 性質係「**app 側單向未遷移**」，⛔ 非「兩版分歧」。`664e4f5` 起 `W_prev` **進入計算** ⇒ 由潛在轉實效。

---

## 三、🔴 **已作廢·⛔ 不得引用之數字**

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| 前交接文 `W-G.8_D波_新session交接文.md` §四 #1 | `app.py:17986-17987`（F 槽） | **`:18030-18031`**（D-1 時代行號·已腐） |
| `W-G.8_D-2b-2_鋪滿機制一般化.md` §1-5 | 「以前根本走不到」 | D-2 **抵達且過**；破係 D-2b-1 引入（已加更正橫幅） |
| 同檔 §1-6 | 「剔除 `cb` 之街廓 ⇒ 非保值 ⇒ 排除」 | **錯誤**——負擔率母體＝`cb`、迴圈母體＝`parcels_by_block`（已加更正橫幅） |
| `probe_D2b8_guard.log`【B】 | 七道閘之**行號與標籤**（`:1276`／`:1323` 皆非 raise） | `verify/out/probe_D2b10_family.log`（AST 自生成·**閘族 15**·非 14） |
| 同檔【驗收 1】 | 「全部 12 格基準已截斷」 | 實為 **9 格完整、3 格截斷**（`[3.5m]` R2／R5／R3） |
| `K-9-5-5`（正典 `:667`） | 「允許收不滿」「差額吸收於抵費地」「第二種豁免」全段 | **KL 已撤回**（帳務漏項·非誠實短收）·僅保留「臨側街寬度隨新界線量測·不回退」 |
| `_block_partition` 舊閘② | 「所有片兩兩交集 ≤ 1e-6」 | **面積鏈恆等式**（D-2b-15 §d-3·舊判準已整條移除） |
| D-2b-11【a-3】第 1 項 | 「差 0.0 ⇒ 雙線與單線等價」 | **軸對齊整數矩形之假象·鑑別力為零**；難幾何下修前 **36/48 例不同** |
| 🆕 `D-2b-17` 施工單 §f-1／§f-4 | R5 之 ②-宗 重疊 **`72.2032`**（**編造精度**·全倉 0 命中）| **`72.1964`**【倉】`verify/out/probe_D2b8_guard.log:12`、`verify/out/probe_D2b10_baseline.log:18`。⚠️ **陷阱**：丙法獨立估計 `72.2002` 距真值 `0.0038`、距編造值 `0.0030`，**一樣近** ⇒ ⛔ **不得以其「確認」任一方**（失敗考古節 54-1／`CLAUDE.md` `【倉】`/`【單】` 第 4 項）|
| 🆕 `D-2b-17` 施工單 §f-4 | 「交接文 **§二** 之全案暴露表」 | 該表在 **§五**（§二 為停機／待裁·無該表）——舊交接文之節號（考古節 56-1）|
| 🆕 `D-2b-18` 施工單 §g-3 | 「交接文考古節數三處與現況不符」 | **三處前提皆不成立**·交接文於 `dca0a85` 時**零錯誤**（考古節 56）⇒ ⛔ 該三項「更正」不得被視為曾發生之缺陷 |

---

## 四、生產碼現況（本波已改動處）

### 4-1 `app.py`

| 落點 | 內容 | 批 |
|---|---|---|
| `:8384` `_STRIP_PARALLEL_TOL = 1e-6` | 同向委派門檻（**述詞同** `stepg` 之 `_PAR_TOL`·`GB-48` 記其重複） | D-2b-12 |
| `:8387` `_strip_halfplane` | 自 `_block_strip` 內提為 **module 級**（與 `_block_partition` 共用） | D-2b-13 |
| `:8414` `_block_strip` | 加 **`n_hat_far`**（雙線·預設 `None` 語意不變）＋ **同向即委派回單線** | D-2b-11／-12 |
| `:8527` `_PARTITION_TILE_TOL = 1e-6` ／ `:8530` `_block_partition` | 🆕 **依序削餘原語**＋內建鋪滿閘三項 | D-2b-13／-15 |
| `:9616` `K-9-5-6 W 鏈之唯一決定點` | `_rw_start = _W_near if is_corner else W_prev` | D-2b-4 |
| `:11786` `K-9-5-4 ② 之唯一決定點` | `allocation_dir = _first_corner_alloc_dir(side_mid)`（`:11736`） | D-2b-1 |

### 4-2 `verify/stepg_pipeline.py`

| 落點 | 內容 | 批 |
|---|---|---|
| `:1031` `_PAR_TOL = 1e-6` | 方向平行判定 | D-2b-3 |
| `:1067` `_iface_deltas` | **`Δ_i` 唯一產生器**（三閘共用） | D-2b-3 |
| `:1112` `_side_alloc_degenerate` | `K-9-5-8` 退化判定（幾何算出·容差同源） | D-2b-8 |
| `:1138` `_guard_auth_scope` ／ `:1181` | 範圍守衛；退化側**仍斷言**（⛔ 非豁免） | D-2b-8／-9 |

🔒 **未刪未停用**：`k97_solve_alloc_t`／`k98_virtual_measure_block`／`K-9-6/7/8`／`GB-36`／`GB-40`。
🔒 `_pool_strips_for_block`／`_strip_s_range`／`_strip_axis` 之**碼**全波未動（僅 docstring）。
🔒 `_block_partition` **未接線**：生產路徑呼叫 **0**、`_WF_NS_NAMES` 命中 **0**。

---

## 五、六街廓現況（solo 分跑·`probe_D2b8_guard.log`【A-3】）

| 情境 | R1 | R2 | R4 | R6 | R5 | R3 |
|---|---|---|---|---|---|---|
| **0m** | 覆蓋 `12.0568` | ②-宗 `76.2616` | 覆蓋 `58.4792` | ✅ **跑完全鏈** | ②-宗 `72.1964` | 覆蓋 `76.0262` |
| **3.5m** | 覆蓋 `12.0553` | telescoping `Δ55.606` | 覆蓋 `58.4792` | ✅ **跑完全鏈** | telescoping `Δ55.576` | telescoping `Δ55.589` |

🔄 **本表兩類數字分屬<u>兩個不同量測器</u>**（`D-2b-18【g-2】` 補註·⛔ 上表數字一字未改）：
**缺口**（`覆蓋 …`）出自 **`①' 覆蓋閘`**（`app.py:9036`·`union` 型）；
**重疊**（`②-宗 …`）出自 **`②-宗 圍堵閘`**（`app.py:9062`·`.intersection()` 型）。
⇒ ⚠️ 「六街廓、兩種外觀、一個根因」之推論，其「兩種外觀」**跨了兩個量測器** ⇒ 須各自驗。
🔑 **兩者皆已於 `D-2b-17` 經獨立方法確認**：
- **重疊側**：兩法對打 ＋ 實寬裁決 ⇒ **真實幾何**（甲全對正確）。
- **缺口側**：量法丙（⛔ 未用 `union`）重算 ⇒ `12.0568→12.0566`／`76.0262→76.0399`／
  `58.4792→58.4828`（差 ≤0.014 ㎡·在格解析度內）⇒ **`union` 未失效**。
- 🆕 **順帶產出之方向判定**：**缺口 ＝ 全額、溢出 `0.0000`** ⇒ 係**真缺口**（街廓內有地未被覆蓋），
  ⛔ **非**「片溢出街廓外」。⚠️ 原 `cover_resid` 取**絕對值** ⇒ 本無方向，此為**首次**判定。
- 🔑 丙法之**重疊多重度**（全街廓格·與 §f-1 不同取樣域）另給 R2 `76.2629`／R5 `72.2002`
  ⇒ 重疊量已由**三個互相獨立之量測器**確認。

⛔ **R6 之「跑完全鏈」不得作為任何結論之通過憑據**——其 `W` 鏈為常數 `7.2000`
（`W_far＝W_near＝W_start`·`Δ_i=+0.0000`），三道鏈閘於該塊為 **`0 == 0` 之空過**。

🔒 **12 格全保真基準**（T2 對照靶·`verify/out/probe_D2b16_baseline.log`【3】）：
`0m` 長度 `109/117/109/5/117/109`、`3.5m` `109/763/109/5/669/671`，sha256 全 64 hex 在該檔。
⚠️ 自 D-2b-10 起**每批複驗、12/12 逐格未變**——T2 接線後**必變**，屆時須逐格歸因。

---

## 六、本波新增之登記項與考古

| 項 | 要旨 |
|---|---|
| `GB-47` | `W_prev` 種子 → 更正為「**app 側已廢止式殘存**」（4 處） |
| `GB-48` | 同一述詞之平行容差**兩處各自定義**（`app` vs `stepg`·暫以探針斷言把關） |
| `GB-49` | 同向委派誤差**逐次發生**、`_block_strip` 無狀態 ⇒ 累積無人看管 |
| `GB-50` | `.intersection()` sliver 互切之矛盾（**觸發特徵未查明**·生產碼 3 處） |
| **考古節 53** | 「以**固定 regex** 代窮舉」＋「**比錯基準向量**」——claude.ai 與 CC 各犯一次（涵蓋 D-2b-6／-7）|
| **考古節 54** 🆕 | **claude.ai 側六類**（D-2b-8〜-17·`D-2b-18【g-1】` 補記）：54-1 **編造精度**（最危險·詳下）／54-2 引用已失效節號／54-3 行號腐化**在自己明文警告後重犯**／54-4 四假說皆被實測打掉（**自洽性≠證據強度**）／54-5 把自身量測失效寫成施工單前提／54-6 收緊容差 vs 收緊上界算法 |
| **考古節 55** 🆕 | **CC 側四類**（同上）：55-1 **空真假綠 ×3**／55-2 固定 pattern 代窮舉第三次（**可鏈式者一律 AST**）／55-3 以 R6 為通過憑據之過度宣稱／55-4 判準寫錯 vs 因結果不利放寬容差（🔒 記為**應仿效之處置**）|
| **考古節 56** 🆕 | **以壞掉之量測器判定他人資料有誤、並據以下令更正正確之物**（`D-2b-18`·`D-2b-19【h-1】` 補記·⛔ 不併入 54）。🔗 **與 55-1 互為鏡像**：55-1 母體空而印 **✅ 綠**（放過自己）／56 母體空而印 **🔴 紅**（**究責他人**）⇒ 空母體之危害不止於假綠，**印紅會主動製造不存在之缺陷並驅動真實修改**，且錯誤理由會寫進 commit ⇒ **史料污染**。🔒 **56-6 記執行者之正確處置為<u>應仿效</u>**：前提不成立即停止不照辦／改依真實理由並於 commit 言明理由已置換／只動真正過時者其餘一字不改／主動掃全文回答「到底錯了幾處」|
| 🔴 **`app.py:4525` 待盯** 🆕 | `overlay_polygons_to_blocks` 之 `residue.intersection(pp_poly).intersection(blk_poly)`（**鏈式**）。實測（`D-2b-17` §f-3）：R1–R6 × 兩情境**執行 848 次**、左運算元緊緻度中位 **`0.0020`**（**全倉最薄**·較 `GB-50` 參照類 `:9062` 之中位 `0.1785` 薄兩個數量級）、🔴 **結果流入面積帳**（§2-1 歸屬修復）。⚠️ 右運算元為**大而簡單**之多邊形（緊緻度中位 `0.5652`）⇒ 型態 ＝ **sliver × 簡單**、⛔ 非 sliver × sliver ⇒ **現判定不觸發停機**；惟為**最接近風險邊界者**——若日後 `GB-50` 之觸發特徵查明為「**單側** sliver 即可」，此處為**第一順位**受影響點。實料：`verify/out/probe_D2b17_operands.log`【f-3】|

---

## 七、🔑 本波最貴的四條（**給下一手**）

1. **「0」有<u>三</u>義**：沒到／到了且過／敘述文字命中。本波三者都踩過
   （`telescoping 破` 命中 0 被誤讀為「走不到」；`0 不一致`；`grep -c` 抓到自己寫的敘述）。
   ⇒ 判「0」之前先問**母體是多少**。
2. **母體非空 ≠ 涵蓋**（`GB-50` 之對拍）：240 例、四格全 0，但**舊判準破者為 0**
   ⇒ 該組**根本沒發生受測現象** ⇒ 「0 差異」不構成等價性證據。**承認 n=1 比湊出完整的表誠實。**
3. **要比「是否等於生產的 X」，X 必須<u>取自生產同一函式</u>**——D-2b-7 只差一個 `rot90`，
   影響面就從「4 格」翻成「無」。抓到它的唯一機制是**同一份報告對同一件事寫兩個獨立算法**。
4. **閘不得建在會自我矛盾的量測上**。舊閘② 用 `.intersection()` 判重疊，而該運算即失效者
   ⇒ 會在**正確之分割**上誤報。改**面積鏈恆等式**（純算術）後，突變測試證其三類注入皆叫得出來。

---

## 八、次步 ＝ **`D-2b-21【b-2】` T2 接線**

> # 🔴 開工前必讀：**12 格 sha256 會全變 —— 那是<u>預期</u>，不是壞了**
>
> **為什麼必變**：`T2` 接線要改 `_pool_strips_for_block`、令**每條界線各自帶方向**
> （現行為單一 `d_hat` ＋ 純量 `s`）⇒ 切出之池片幾何改變 ⇒ 12 格逐格輸出**必然**改變。
> **為什麼會被誤判**：該基準自 `D-2b-10` 起**連續十批、12/12 逐格未變**
> （`0m` 長度 `109/117/109/5/117/109`／`3.5m` `109/763/109/5/669/671`
> 【倉】`verify/out/probe_D2b16_baseline.log`【1】）
> ⇒ 「未變」已養成預期，**見全變之第一反應必然是「壞了」**。⛔ **那個反應要先拆掉。**
>
> **✅ 正確做法 ＝ 逐格歸因**（12 格逐一，缺一格即不算過）：
> 1. 該格**變成什麼**（新長度／新 sha256）；
> 2. **為何變**——須指到**具體機制**（哪條界線之方向改變、影響哪些池帶之 s 區間）；
> 3. 該變化**是否為 `T2` 之預期後果**——⛔ 不得以「接線本來就會變」一句帶過全部 12 格。
>
> ⛔ **無歸因之全綠，與無歸因之全紅，同樣不可採信**（失敗考古節 55-1「空真假綠」／
> 節 56「空母體印紅」——兩者互為鏡像，都是**沒有母體卻下結論**）。
> 🔒 舊基準**不得覆寫**：另立新檔名（`WV_BASELINE_LOG=<新檔名>`），
> 使**新舊並存、可逐格對拍**（`GB-45` 同族：留下可比對之母體）。

**`T2` 接線之驗收靶**（現查確認可用·⛔ 不必換量測器）：

| 靶 | 判準 | 現況 |
|---|---|---|
| **破量欄全部歸零** | `①' 覆蓋閘` 殘差 ／ `②-池` ／ `②-宗` 三者皆 ≤ 各自閘寬 | 🔑 **量測器已於 `D-2b-17` 確認可信**（三個獨立量測器一致）⇒ **靶可直接用** |
| **鋪滿閘三項** | `_block_partition` 內建①總量②逐步恆等③半平面互補 | 閘寬 `_PARTITION_TILE_TOL = 1e-6`（`app.py:8527`·⛔ 遠嚴於下游 `0.01`，不得以其代掃）|
| **`GB-49` 帳層累積閘** | `Σ_i ½·L_i²·\|cross_i\|` < `0.01` | 🔴 **接線時須一併帶入**——`_block_strip` 無狀態，累積須由**帳**承擔 |
| **12 格逐格歸因** | 見上方紅框 | ⛔ 必變·須逐格 |

> 🔄 **本表已因 `D-2b-17`／`D-2b-18`／`D-2b-19` 更新**（此三批於原「次步＝T2 接線」之前**插入**）。
> **`D-2b-17` 之結論**（`dca0a85`）：`②-宗`／`②-池` 所報之重疊量係**真實幾何**，
> ⛔ **非 `.intersection()` 假陽性**——甲法於**全部 20 對**皆正確
> （大值：R2 甲 `76.2616`／乙 `77.7522`、R5 甲 `72.1964`／乙 `74.1497`；
> 甲≈0 之 14 對經**實寬**判準裁決 `max_d` 全為 `0.000000` ⇒ 乙之貼邊偏壓）。
> ⇒ 🔑 **`T2` 驗收靶「破量欄全部歸零」⛔ 不必換量測器**——該靶原有兩格建立在 `②-宗` 上，
> 現已由**三個互相獨立之量測器**確認。
> 實料：`verify/out/probe_D2b17_dual.log`、`verify/out/probe_D2b17_adjacent.log`、
> `verify/out/probe_D2b17_operands.log`；報告 `docs/reports/W-G.8_D-2b-17_重疊量兩法對打與運算元判定.md`。
> **`D-2b-18` 之結論**（`e97664c`）：本波考古十批補記完畢（節 54／55）。

| 序 | 事 | 前置 |
|---|---|---|
| 1 | **`GB-50` 之處置範圍**（`:9047`／`:9050`／`:9062` 是否比照改面積鏈） | 🔄 **裁題已縮小**（`D-2b-17`）：「該三閘是否**誤報**」⇒ **於本案已答：否** ⇒ ⛔ 不必為「誤報」而改判準。**殘留待裁者僅**：本列**觸發特徵**仍未查明 ⇒ 「他處是否比照」仍屬域邊界 🛑 |
| **2** | 🔴 **`D-2b-21【b-2】` T2 接線**：`_pool_strips_for_block` 改用 `_block_partition` ＋ 切線帳 | ✅ **驗收靶已備**（見上表）；須帶 `GB-49` 之帳層累積閘；⛔ 12 格必變·須逐格歸因。⚠️ 與序 1 **可解耦**：序 1 之殘留裁題係「**他處**是否比照」，⛔ 不擋接線本身 |
| 3 | **甲-1 選位落地** ⇒ 解 Gate C | 🛑 待裁（歷批禁落地） |
| 4 | `GB-47` app 側 4 處遷至 `_mp_base_W0`（`main()` 內 ⇒ 須合成案） | 🛑 待裁 |
| 5 | **`run_all` 重跑 ＋ 新期望 FAIL 名單** | 本波從未跑；接線後必須 |

---

## 九、收官前置（不變）

`git rev-parse origin/wip/s1-endpart` 已含該 commit——**不綠不推、不推不報**。
⚠️ `run_all` 約 **15–20 分鐘** ⇒ 背景跑＋等通知；⚠️ **長跑期間 ⛔ 不得改 `app.py` 與 `verify/`**。
⚠️ **四份期望 FAIL 名單並存**（`902f5d1`／`D1切換後`／`D2切換後`／`D2b1切換後`）
——**現行者為 `D2b1切換後`**（22 名目），其餘三份**刻意保留供考古**、⛔ 勿誤用。
⚠️ 基準探針**拒絕覆寫**：跑複驗須 `WV_BASELINE_LOG=<新檔名> python verify/probes/probe_D2b10_baseline.py`。
