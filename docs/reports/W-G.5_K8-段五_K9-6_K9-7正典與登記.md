# W-G.5 · K-8 段五 —— **K-9-6／K-9-7 最小深度與最小寬度定義更正**（正典＋登記＋探針改寫·零行為）

**分支**：`wip/s1-endpart`　**起點 HEAD**：`b061b00`
**批性質**：正典 ＋ 登記 ＋ 一支**只量不判**之探針改寫。
**⛔ 本批不上任何閘、不改任何算式、不動任何判準。未跑 `run_all`（交辦文明令不需）。**

> 本報告為本批之**唯一本體**；聊天僅 ping。數字一律錨引倉檔。

---

## 〇、一句話結論

碼面兩處把**畸零地附表之可建築門檻 `14.00`** 當成**量測帶深**（正解為逐宗／逐範圍
之**自身最小深度** 33〜46m）；本批將法規定義入正典（**K-9-6**）、把 ALLOC_LINE 之
分段線性解析定位入正典（**K-9-7**）、登記 **GB-13／14／15**、
並**整支改寫探針**——**段四之八格寬度數字全部作廢**。

---

## 一、交付格式證明

基準 commit `b061b00`。

| 項 | 結果 |
|---|---|
| `git diff --stat b061b00 -- '*.py'` | **僅 1 檔**：`verify/probes/probe_ruling_K9_corner_width.py`（481+／191−） |
| 既有 `.py`（**扣除改寫之探針**）之異動 | **0 檔** |
| ⇒ 本批**未動任何既有 `.py` 之 AST** | **✅ 成立** |
| `py_compile` | 探針通過 |

依交付格式 §四：「新增／改寫之探針無舊版可比 ⇒ 改證『本批未動任何既有 `.py` 之 AST』」。
本批該集合為**空集**（`app.py` 等一字未動）⇒ 該證明**平凡成立且已逐檔查核**。

---

## 二、正典（3.1／3.2）

落點：`docs/rulings/K-6_街角地分配程序與可分配判準.md`，插於 `### K-9 §三-1` 之後、
`### K-9 §四、落地狀態` 之前（**在 K-9-5 之後、`## 🔒 K-8 §二 補記` 之前**·合交辦文位置要求）。

- **`### K-9-6 最小深度與最小寬度之法定定義`**
  含 (a) §4 原文**逐字**（含「但道路境界線為曲線者…」該句）／
  (b) **K-9-6-a 兩個「最小深度」同名不同量·嚴禁互代**（比照 WARNING-C 體例·量測值 vs 門檻值）／
  (c) **K-9-6-b 最小深度範圍之構造**（KL 定式逐字·三種二側境界線）／
  (d) **K-9-6-c 與 K-9-2 之分工**（附表深度合規歸 K-9-2·**K-9-2 不受本次更正影響**）／
  (e) **K-9-6-d 落地狀態**（已裁定·實作屬 K-6-A2·碼面行為未變）。
- **`### K-9-7 街角規定範圍之 ALLOC_LINE 解析定位`**
  含【深度】【寬度】【最小寬度】四分支表、**一次除法**、
  **禁二分／取樣／迭代**、**須補分支判斷**（最多 2×2＝4 支）、
  **斜率正解 `(c − k·m)`**、**退化 loud raise**、
  **K-9-7-a 記號衝突警語**、**K-9-7-b 與 K-8 §三〜§五 之關係**、
  **K-9-7-c 本案實測**。

⛔ **K-8 §三〜§五 原文一字未改**；僅於 K-9-7-b 加「其帶深來源已由 K-9-6-b／K-9-7 取代、
其餘構造仍有效」之指向（合交辦文要求）。

另更新 `### K-9 §四、落地狀態` 之落地表（補 K-9-6／K-9-7 兩列），
並標注 K-9-2 不受影響。

---

## 三、🔴 記號衝突（**本批主動攔下·交辦文未提**）

交辦文 K-9-7 之記號：`k` ＝ `∂w/∂d`、`c` ＝ `∂w/∂t`。
而 `app._build_corner_range_v3` 之**區域變數** `k = sigma / den_a`
（`grep -n "k = sigma / den_a" app.py`）**係交辦文之 `c`**、**非**其 `k`。

⇒ 若照抄記號去對碼，會把「寬度對 `t` 之斜率」讀成「寬度對深度之斜率」，
**正是 GB-14 ② 之錯誤本身**。
處置：正典 **K-9-7-a** 立警語、探針檔頭立警語、**GB-14 內文標明**。
**正典仍用交辦文記號（canonical 逐字），只加警語、不改記號。**

---

## 四、探針改寫（3.4）

`verify/probes/probe_ruling_K9_corner_width.py` **整支改寫**。
檔頭與 log 首行皆明寫「**前版數字已全部作廢（帶深定義錯誤·K-9-6）**」；
`verify/out/` 下同名 `.log`／`.csv` **已由本版覆寫**（無殘留舊值）。

**紀律**：未改 `app.py` 一行／不掛 `run_all` 判定鏈／rc 恆 0／
**不代裁 U-K9-1／U-K9-2**（前提已換·兩題暫緩）。

**方法**：斜率一律以**擾動 `_build_corner_range_v3` 之輸入、觀測其輸出**取得
——該函式為純函式且 `W` 對 `t`／`d` 皆仿射 ⇒ 差分**精確**，且**以 app 為 oracle**，
非另寫一份算式。線性性另以**兩組擾動量互證**。
容差只用倉內既鎖之兩個法定粒度：`EPS_TOUCH = 0.01`（長度 2dp·同姊妹探針）、
`EPS_AREA = 0.01`（面積·同 E-8a 凸性閘寬）——**無自訂容差**。

### 4-a (B)(C) 斜率與 K-9-7 分支

| 街廓/側 | `c` | `c` 線性 | `k` | `m` | `m` 線性 | 分支 |
|---|---|---|---|---|---|---|
| R1/left | −1.003270 | ✅ | 0.000000 | （見 4-b） | 🔴 | ① |
| R1/right | +1.003270 | ✅ | 0.000000 | −0.000000 | ✅ | ① |
| R2/left | +1.002499 | ✅ | −0.075547 | +0.009750 | ✅ | ③ |
| R3/right | +1.004284 | ✅ | 0.000000 | （見 4-b） | 🔴 | ① |
| R4/left | +1.001034 | ✅ | −0.047599 | +0.000266 | ✅ | ③ |
| R4/right | −1.001034 | ✅ | −0.050087 | 0.000000 | ✅ | ② |
| R5/left | +1.004284 | ✅ | −0.075610 | −0.000000 | ✅ | ② |
| R6/right | −1.003245 | ✅ | 0.000000 | 0.000000 | ✅ | ① |

⇒ **三支皆有實例**（① 4／② 2／③ 2）。`k ≥ 0` 者 4 個 ⇒ **半數街角之深度定義
對其寬度完全無影響**。真正受 GB-14 ② 咬者為分支 ③ 之 **R2 左／R4 左**。

### 4-b 🔴 `D(t)` 之分段線性已由實測坐實

`D(t)` 三點量：**R1 左** `29.5051 → 33.1623 → 33.1621`、
**R3 右** `41.4195 → 44.8499 → 44.8439`
——**首段跳 3.66m／3.43m、次段方回穩**（次段斜率 `−0.000413`／`+0.012109`）。

⇒ K-9-7 之「**須補分支判斷**」係**必要、非保險**。

> **本項為過程更正**：初版探針以連鎖律 `m = (dD/ds)·(ds/dt)` 取 `m`，
> 得 `|ds/dt| ≈ 6.8／7.0`，而由構造 `|ds/dt| = |1/den_a| = |c| ≈ 1.00`
> ⇒ **自相矛盾**。改為**直接三點量 `D(t)`** 並加線性自檢後，矛盾消失、
> 且**正是該自檢把拗點抓出來**。連鎖律路徑於「臨街段鏈跳段」時失效。

### 4-c (A)(D) 自身最小深度 與 據之重量之最小寬度

**附表深度恆為 `14.00`；實測自身最小深度 33〜46m。** 截角後寬度（0m 情境·摘）：

| 街廓/側 | 宗 | 範圍深 | 宗深 | 寬度·**新帶深** | 寬度·舊帶深 14 |
|---|---|---|---|---|---|
| R1/right | `628(5)` | 33.1294 | 33.1294 | **1.4165** | 3.4643 |
| R2/left | `628-40(1)` | 44.8876 | 44.8750 | **4.6051** | 4.6051 |
| R4/left | `628(1)` | 33.1240 | 33.1129 | **26.9476** | 43.2119 |
| R5/left | `628-7(2)` | 43.6744 | 43.6744 | **0.6706** | 3.6833 |
| R6/right | `628-18(1)` | 43.5317 | 43.5317 | **1.3999** | 3.8197 |

⇒ **新帶深下寬度普遍大幅縮小**（量測帶加深 2〜3 倍 ⇒ 取 min 之母體變大）。
**本批只量不判**——不與任何門檻比較、不出合格與否。逐格全表見 log／CSV。

### 4-d (E) 包含關係 `範圍 ⊆ 宗`

**12 格中 9 格成立、3 格不成立**（閾＝面積法定粒度 `0.01㎡`；成立者之殘差 `~1e-10㎡`）：

| 情境 | 街廓/側 | 宗 | 溢出 |
|---|---|---|---|
| 0m | R1/left | `628-37(1)` | **5.325538㎡** |
| 3.5m | R1/left | `628-36(1)` | **5.325538㎡** |
| 3.5m | R1/right | `628(5)` | **5.067912㎡** |

⇒ **KL 之推論（包含關係 ⇒ 寬度必過）不恆成立**。此為 **U-K9-3** 之底料。

> **過程更正**：初版以 `over.area <= 0` 判包含，浮點噪訊（`~1e-10㎡`）使 5 格
> 誤報「🔴 不成立·溢出 0.000000㎡」——**判準與顯示自相矛盾**。改用法定面積粒度後修正。

### 4-e (F) 截角接合實距 —— **假說否證**

| 街廓/側 | 交集型態 | 頂點最小距·絕對框 | 頂點最小距·**局部框** |
|---|---|---|---|
| R1/left | Point | **0.000e+00** | **0.000e+00** |
| R4/right | Point | **0.000e+00** | **0.000e+00** |
| 其餘 6 格 | LineString | 0.000e+00 | 0.000e+00 |

⇒ 交辦文假說「**R1 左係次微米級座標不合導致之假點接合**」**否證**：
兩者之頂點**完全重合**（距離為 `0`，且局部框重測**逐位相同** ⇒ 非絕對大地座標之數值退化）。
**R1 左與 R4 右皆為真點接合。**

接觸組態（局部框 `(s,t)`）：R1 左之接觸點 `(3.6682, −1.27e-05)` 恰為截角三角形之
**前緣側頂點**，宗之最靠前緣兩頂點為 `(3.6682, −0.0)`、`(6.4943, −0.0)`
⇒ **宗自截角之末端起沿前緣線往內排**，故僅共頂點、不共邊。
R4 右之接觸點 `(95.3499, +0.014826)` 即 **GB-15／U-K9-2 之同一頂點**。

### 4-f (G) `−1.4826e-2m` 溯源 —— **CAD 圖面既有·已定讞**

1. 街廓 `R4` 多邊形（W-A.3 方案B **直讀** DXF BLOCK）之 `t` 最大頂點
   ＝ `(310582.658400, 2651871.934270)`，`t = +0.014826m`
2. 宗 `628-1(1)` 之 `cut_coords` 之 `t` 最大頂點 ＝ **同一座標**，距 **`0.000e+00 m`**
3. 原始 `data/V6.dxf` **BLOCK 圖層**（舊式 `POLYLINE`）距該頂點最近之點 ＝ **同一座標**，
   距 **`0.000e+00 m`**，**entity handle `178`**

⇒ **非程式產生**。BLOCK 圖層之該頂點本身即位於 FRONT_LINE 外側 `+0.014826m`；
配地引擎只是原樣帶下來。**修圖 vs 改判準係 KL 裁量。**

> 過程註記：BLOCK 圖層實為舊式 `POLYLINE`（非 `LWPOLYLINE`），
> 初版以 `get_points()` 取點失敗且**靜默跳過** ⇒ ④ 整段未印。已改為兩式皆試 ＋
> 取不到時 loud 記錄（no-silent-fallback）。

---

## 五、登記（3.3）

`docs/reports/W-G.4_泛用阻塞項登記表.md`，照既有七欄體例（原 12 列 → **15 列**）：

- **GB-13**：`parcel_min_width_n14` 帶深誤用附表門檻 `14.00`（應為逐宗量測最小深度）·**K-6-A2 修**。
- **GB-14**：`_build_corner_range_v3` §三 帶深用 `D_avg`（應為範圍自身最小深度且**隨 `t` 變動**）
  ＋**斜率應為 `(c − k·m)`**·**K-6-A2 修**·內含記號衝突警語。
- **GB-15** 🆕（**交辦文未列·本批實測發現**）：**街角規定範圍之前緣側邊界實為街廓多邊形邊界、
  非 FRONT_LINE**——實測偏移 R1 左 `−0.166151m`、R3 右 `−0.059879m`（**且該邊不平行 FRONT**）、
  R4 右 `+0.014826m`。⇒ 依 K-9-6 量深度時**起算邊差多少、深度就差多少**。

**GB-13／GB-14 互相指認為同源**（同一「附表門檻誤作量測帶深」之根因），
兩列「真資料覆蓋」皆標**未走過**、「需否域裁」皆 ❌（法規原文已明訂）。
**GB-15 之「需否域裁」標 ⚠️ 可能需**（構造定義問題＋涉 U-K9-2 之修圖裁量）。

---

## 六、作廢傳播（**防誤引**）

前版數字散落三處，皆已同批標作廢（**原文保留·僅加作廢框·失敗考古**）：

| 檔 | 處置 |
|---|---|
| `verify/out/probe_ruling_K9_corner_width.{log,csv}` | **已由改寫版覆寫**（無殘留） |
| `docs/reports/W-G.5_K8-段四_K9正典與登記.md` §五 | 加**整節作廢框**；並註明該報告**其餘各節不受影響** |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` §一-B | 「土地後果預告」加作廢框 ＋ 補段五三項結論 |
| `docs/rulings/K-6_…md` K-9 §四 | 「土地後果預告」加作廢框 ＋ 新增 K-9-6-e／K-9-6-f 實測段 |

---

## 七、⛔ 上呈 KL（**本批不代裁**）

| # | 狀態 |
|---|---|
| **U-K9-1**（點接合 ⇒ 截角前幾何不良定義） | **暫緩**（前提已換）。段五新增事實：R1 左／R4 右之**頂點完全重合**、局部框重測同值 ⇒ 「假點接合」假說**否證**、**兩者皆真** |
| **U-K9-2**（`628-1(1)` 之 `−0.014826m`） | **暫緩**（前提已換）。段五**已溯源定讞＝CAD 圖面既有**（DXF handle `178`）⇒ 待 KL 裁**修圖** vs **改判準** |
| **U-K9-3**（§零-3「蘊含」語） | 段五補得底料：**包含關係非恆真**（3 格溢出 5.07〜5.33㎡）⇒ 「`G ≥ 範圍面積` 蘊含寬度合格」**不可由包含關係逕推** |
| **U-K9-4** 🆕 | **GB-15**：範圍之前緣側邊界究係 **FRONT_LINE** 抑或**街廓多邊形邊界**？二者於本案差至 `0.166m`，且 R3 右該邊**不平行 FRONT**（曲線道路境界線之 §4 但書是否適用？）**本批未問、未裁** |

---

## 八、本批未做（**明列·免下一手誤認**）

- **未上任何閘**、未改任何算式、未動任何判準 ⇒ **未跑 `run_all`**。
- **K-9-6／K-9-7 之實作** ⇒ 屬 **K-6-A2**（GB-13／14 於彼時修）。
- `app.py`／`probe_ruling_N_e1_touch.py` ⇒ **一字未動**。
- **不代裁 U-K9-1／U-K9-2**（交辦文明令）。

---

## 九、段五之後

1. **K-6-A2**（K-9 全七則之實作·**有土地後果**）——前置：本報告 §4 表經 KL 過目
   ＋ U-K9-1〜4 裁定。
2. **波末批**：換快照＋重烤 v3／wf／E 系列＋兌現全部遷移項（含 GB-11 之兩個同名異體同批改）。
3. **K-6-B**：街角地分配程序四段本體。
4. **泛化波**：去錨、恆真閘家族統一體例（含 GB-10）、騎樓表分支。
