# `W-G.9-268′` 補令十三　序 `1`／`2`／`3` —— 執行報告

> 🔒 **本報告⛔ 鑄任何號**（本批恰鑄 `自誤 348` **一**號，其於 `655d86a`·單 `§四` 授權之上限）。
> 🔒 **本報告本身 ＝ 零生產碼·純新增檔**（`deletions ＝ 0`）。
> 🛑 **序 `3`（`c3`）⛔ 在既有放行之內**——已推**側支**，**主線原地不動**；
> 須由發單側逐位元組復驗 → 呈 KL → 放行後方得併線。

---

## §零　三軸 ＋ 倉態

| 軸 | 值 |
|---|---|
| `(1)` 來源 | 主線 `d49d9db707c7c6ff77195699ecd81082ba5f2074`；側支 `10d82025c3883ed75a17027d14d5c5f2b7289d19` |
| `(2)` 母體 | 逐節分列（追蹤 `.md` `678` 檔／全倉追蹤檔 `1857`／全 `docs/` `657`／`verify/**/*.py` `286` 支／生產碼 `34` 檔）|
| `(3)` 粒度框 | 逐節分列（檔框／列框／**bytes**）|

🔒 **二 ref 之實查（`git ls-remote`·⛔ 採信本地 ref）**
`refs/heads/wip/s1-endpart` ＝ `d49d9db707c7c6ff77195699ecd81082ba5f2074`
`refs/heads/verify/W-G.9-268p-c3` ＝ `10d82025c3883ed75a17027d14d5c5f2b7289d19`

🩸 **期初態之對帳（照實）**：單 `§零` 載主線期初 ＝ `62d7f70`，而收單時 `origin` 實為 `b34e42e`。
其間二 commit（`7ce0ff8` 交接註／`b34e42e` 本窗之停機上呈）**逐 commit 生產碼判皆空輸出**，
且 **生產碼 `34` 檔相異 ＝ `0／34`** ⇒ 單 `§一` 之核可（「生產碼相異 `0／34`」）**仍成立**。

---

## §一　工項零：本補令原封入倉（`ad4bb81`）

| 量 | 值 |
|---|---|
| `sha256`（落檔前來源） | `1ca1f94ff52f68f3158ca326e546693331f3e405dc81e624fc0b60d07006c477` |
| `sha256`（落檔後） | **同** |
| `sha256`（入倉 blob） | **同** |
| bytes ／ `CR` | `7530` ／ `0` |

⇒ **逐位相同**、⛔ 改一字、⛔ 改行尾。落點 ＝ `docs/orders/W-G.9-268p_補令十三_c1synth造a之二步處置.md`。

🔒 **入倉前之號占用查**（`作業常規之追加四` 補款 款一·**宣告框**·母體 ＝ 全 `docs/` **`657`** 檔〔讀不到 `0`〕）

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | 列框 `\|D2∪D3\|` | 檔框 |
|---|---|---|---|---|---|---|
| **`補令十三`**（受詢） | `1` | `2` | `0` | `0` | **`2`** | **`1`** |
| `補令十二`（甲·必命中） | `2` | `6` | `0` | `0` | `6` | `3` |
| `⟨NEG⟩`（乙·人造哨兵·執行期組出·字面⛔ 出艙） | `0` | `0` | `0` | `0` | `0` | `0` |

⇒ 二造皆如預期 ⇒ **器非紅**。

🔒 **自解（`作業常規之追加五` `一-1` 白名單 `1`：占用者全為本單自身之產物）**
唯一占用檔 ＝ `docs/reports/W-G.9-268pR_補令十三_開工閘與停機上呈.md`（本窗前一輪之停機 R 報告）。
**否證對照當批實跑**：
> **必綠組** ＝ `補令十三`——占用者全為本單產物 ⇒ 判準**成立**。
> **必紅組** ＝ `補令十二`——其占用檔含 `docs/reports/W-G.4_泛用阻塞項登記表.md`（**非**本單產物）⇒ 判準**不成立**。

⇒ 二造皆如預期 ⇒ 判準**具判別力、非恆成立** ⇒ 自解續辦，**⛔ 自行改號**。
🩸 **併記**：此即 `GB-132` 族「**同號重發之單過不了自己的閘**」之形——該占用**由本窗自身之停機報告所生**。

---

## §二　序 `1`：`自誤 348` 之鑄 ＋ `GB-161` 母體更正（`655d86a`）

### 二-1　取號現查

**定義框**（倉內既有器 `verify/probes/wg9268_gate5_tally.py`·⛔ 自擬框·坑 `6`）·母體 ＝ 登記表**單檔**：
期初 相異 **`340`**／MAX **`347`**／缺號 **`[106]`** ⇒ `348` 為次號且**⛔ 在缺號集**。

**意指占用**（母體 ＝ 全倉追蹤檔 **`1857`**〔讀不到 `22` ＝ 二進位〕·**錨定框**·**B 形 ⋀ C 形並取**）

| 號 | 角色 | B 形 檔/列 | C 形 檔/列 | 鬆框 檔/列 |
|---|---|---|---|---|
| `自誤 348` | 受詢 | `0`／`0` | **`2`／`11`** | `2`／`11` |
| `自誤 347` | `(甲)` 必非零 | `0`／`0` 🔴 | **`5`／`12`** ✅ | `6`／`15` |
| `自誤 343` | `(甲2)` 必非零 | `0`／`0` 🔴 | **`8`／`45`** ✅ | `9`／`47` |
| `⟨NEG⟩` | `(乙)` 人造哨兵 | `0`／`0` ✅ | `0`／`0` ✅ | `0`／`0` ✅ |

🔒 **逐筆歸類（`裁 H`：命中數⛔ 為判準、係待歸類之材料）**：`11` 列**全為本單自身之產物**
——單本體 `4` 列（`docs/orders/…補令十三…md` 之 `:1`／`:64`／`:78`／`:89`·**其委派本批鑄之**）
＋ 本窗停機報告 `7` 列。⇒ 白名單 `1` 自解（同 `§一`）。

🩸 **本窗親踩之量測器紅（⛔ 出艙前自捕）**：首版以 **B 形**（`` 自誤 `347` ``）取甲造 ⇒ 得 **`0`**
⇒ 依 **`常規四（七）補款二` 款二**「對照組得數為 `0` 者，一律先判**量測器紅**」**先判器紅**、
⛔ 逕以受詞紅出艙。現查其真形為 **C 形** `` `自誤 347` ``（反引號包**全 token**）
——`docs/reports/W-G.9波_claude.ai側自誤登記.md:8147` 逐字
`### 🩸 \`自誤 347\`　**列定判定集而未執行倉內既有交接註所載之攔法**`
⇒ **恰為 `GB-158` 所載之情狀**。改 **B ⋀ C 並取**後甲造 ＝ `5`／`12` ⇒ 器非紅，上表方出艙。

### 二-2　`自誤 348` 之鑄

**受詞逐字轉錄自**單 `§三`（**⛔ 改寫**）；**責 ＝ 發單側**。
🔒 **CC 之現查（倉內為準）**：`docs/orders/W-G.9-268p_補令十二_GB161硬編他樹與c3前置.md` 之
`§二-1` 標題逐字 ＝ `### 二-1　所診（發單側自倉全掃·母體 ＝ \`verify/**/*.py\` **\`285\`** 支）`，
其下之表**確為 `8` 列** ⇒ **單所述之案由屬實**。

🩸 **照實併呈（⛔ 頂替·⛔ 自裁）**
1. **`285` vs `286`**：單載母體 `285` 支，CC 當場實測 **`286`** 支（框同）。差 `1` ＝
   **`verify/probes/probe_WG9268p_gb161_sweep.py` 自身**（該器於補令十二 序 `1` 入倉，晚於單之撰寫）
   ⇒ **母體含其自身**（`GB-132` 族之自指）。**⛔ 影響本則之受詞。**
2. **「`14` ＋ `8`」之集合語意**：二族**⛔ 互斥**——**交 `3` 支** ⇒ **聯集 ＝ `19` 支**，⛔ `22`。
   🛑 **⛔ 更動單之「`14` ＋ `8`」一字**（其係二族之各自基數）；只載聯集以防下游誤加。

### 二-3　`GB-161` 之末端追加（母體更正）

**三軸**：態 `blob@ad4bb81`（`verify/` 對 `HEAD` 乾淨）／母體 `verify/**/*.py` **`286`** 支／檔框 ＋ 列框。
🔒 **框逐字自該器源碼之 `PATS` 抽出**（⛔ 另寫第二份定義·`GB-48` 族）。
**族甲 `worktrees` ＝ `14` 支**（Σ 列 `28`）／**族乙 `os.chdir(` ＝ `8` 支**（Σ 列 `9`）——**逐支具名已入倉**。
**交 `3`／甲∖乙 `11`／乙∖甲 `5`／聯集 `19`**。
**篩選規則**（逐字）＝「該檔之全文含該樣式之命中 `≥ 1`」；**被濾除之筆數** ＝ `272`（甲）／`278`（乙）
——即 `自誤 348` 之通則施於本表自身。
**對照組三造**：`import os` `265` 檔 ✅／人造哨兵 `0` ✅／**必命中之本類已知成員** `c1_synth` `2` 列 ✅。

### 二-4　鑄號之收工閘（`常規四（九）二`）

| 閘 | 結果 |
|---|---|
| ① **形之自證**（定義框掃 payload） | 命中恰 `['348']`·**⛔ 誤鑄他號** ✅ |
| ② **MAX 之推進** | 自誤 相異 `340 → 341`／MAX **`347 → 348`**／缺號 `[106]` **不變** ✅ |
| ② **他簿⛔ 污染** | `GB` `159`／`161`／`[12, 87]`・`VR` `80`／`95`／`[73, 75]`・`K-9` `28`／`29`／`[]` **逐簿不變** ✅ |

🔒 **append-only 自證**：二簿皆「改前全檔為改後之**嚴格前綴**」∧ 長度增加 ∧ `CR = 0`
（自誤簿 `698312 → 701344` B·Δ `3032`／`GB` 簿 `766556 → 772948` B·Δ `6392`）；`numstat` 刪除欄逐列為 `0`。

---

## §三　序 `2`（步一）：`c1_synth` 造 `a` 之 `(a2)` 期望更正（`d49d9db`）

🔒 **所改者 ＝ 期望值**（`1.0` → `S_GEO` ＝ `7.25`），**於同列具名其由**：
**`c2`（`c1376af`）已將預設改為 `on`**；逐字錨 ＝ `app.py` 之
`_os_a.environ.get('WG9268P_ANCHOR_GEOM', '1') != '1'`。
🛑 **此⛔ 為「調判準以就結果」**：該期望值所編碼者為「**預設為何**」，
而預設已由一個**既經 KL 放行之裁**（`c2`）**合法改變**。

**改法**（坑 `7` 攔法：⛔ 用 `sed`）：`OLD` 命中數 `1`（須 `1`）／被移除之列數 `1`（須 `1`）／
列數 `192 → 200`（Δ `+8` ＝ `8` 列註解）；相異列之新舊逐字已出艙；`py_compile` ✅。
🛑 **容差沿用** `chk` 之預設 `1e-9`·⛔ 放寬；**⛔ 動造 `a` 之節標題**（其重擬屬**步二**）。

🔒 **判別力之保全（單 `§二-2` 硬款）**
`(a1)`（顯式 `'0'`·期望**維持 `1.0`**）實測 **得 `1.0000000000` 期 `1.0000000000` ✅ 仍通過**
⇒ 🔒 **`(a1)` ⛔ 轉紅 ⇒ 旗標分支仍活 ⇒ ⛔ 觸「若 `(a1)` 亦轉紅 ⇒ 停機上呈」之停機款**。
`(a1)` 期 `1.0` vs `(a2)` 期 `7.25` ⇒ **二式期望相異** ＝ 該器仍能偵得旗標分支之證。

**驗收**：改後 `rc = 0`（**直取**·⛔ 經管線）·✅ 合成案全綠。

🔒 **逐項差異（承 `§三-1` 之形）**——**二組比較皆恰 `1` 項**
`(A)` 改前本窗實跑（`rc=3`） vs 改後（`rc=0`）／
`(B)` 改前**既有落檔** `verify/out/WG9268pR_c1_synth_gb161fix.log`（補令十二·已入倉） vs 改後。
差異項 ＝ ① `(a2)` 判定列 🔴 → ✅　② 結論列「紅 ＝ `1` 項」→「全綠」。
🔒 **其得值 `7.2500000000` 二態逐位未變——只有期望變** ⇒ 坐實此係**期望之更正**、**⛔ 行為之改變**。
其餘各造（閉式自證／`(a1)`／`b`／`c`／`d`／`e`／AST 四呼叫點）**逐項不變**。
🛑 **⛔ 追改既有落檔一字**（補令十二 `§三-1`）；新落檔另立二檔（`before`／`after`）。

---

## §四　序 `3`：`c3`（contract）＋ 步二（`10d8202`·**側支**）

### 四-1　`c3` 之改

**AST 定位**（⛔ 硬編行號·`行號衛生`）＋ **python 落檔**（⛔ 用 `sed`·坑 `7`）。
**移除 `5` 列**（`3` 可執行：`import os as _os_a`／旗標 `if`／其 `return`；`2` docstring）＋ **新增 `4` 列**。
`py_compile` ✅。🛑 **旗標之字面⛔ 再現於 `app.py`（含註解）**——否則 `Q-2` 必不過。

🔒 **⛔ 動之六符號：逐位不變之自證**（父 `d49d9db` vs `c3`）

| 符號 | 父 | `c3` | 逐位相同 |
|---|---|---|---|
| `_oblique_s_max` | `1081` B | `1081` B | ✅ |
| `_K923_EPS_ZERO` | `59` B | `59` B | ✅ |
| `_blue_shadow_tri` | `3570` B | `3570` B | ✅ |
| `_k923_gate1` | `1219` B | `1219` B | ✅ |
| `_k923_gate2` | `398` B | `398` B | ✅ |
| `_lot_gate` | `7522` B | `7522` B | ✅ |
| 🔑 **判別力造** `_wg9268p_anchor_advance` | `1477` B | `1446` B | **`False`** ✅ |

⇒ 判別力造為 `False` ⇒ **該檢非恆真**。
🔒 **⛔ 改錨之語意**：`c3` 只拿掉旗標 ⇒ 行為**等同於 `c2` 後之預設（`on`）**。

### 四-2　`Q-1`（輸出與父 commit 逐位相同）

**主檢（`c3` vs 父態 `d49d9db`）＝ ✅ `20`／`20` 逐位相同**

| 類 | 結果 |
|---|---|
| `got_*` CSV | **`18`** 檔逐位相同（`sha256`）|
| `stdout` | `168276` B ／ `168276` B ·逐位相同 |
| `stderr` | `0` B ／ `0` B |
| `rc` | `1` ／ `1` |

🩸 **照實二項**
1. 單載「**八** `got_*` CSV」，**實際寫出為 `18` 檔**（逐檔具名於
   `verify/out/WG9268pR_c13_c3_Q1_c3_stdout.log` 同批之快照）。
2. `rc = 1` 係**本分支之准紅碼常態**（驗收 ＝ 與凍存之期望 FAIL 名單相同·⛔ 全綠）
   ⇒ **⛔ 讀為 `c3` 致紅**；其父態同為 `rc = 1`。

**判別力對照（`c3` vs `683b7fa`·`c2` 前·`off` 預設）＝ 🔴 相異 `15`／相同 `7`** ✅
⇒ **該比法非恆綠**。`683b7fa` 提早中止 ⇒ 產 `_partial`、`stdout` 僅 `29106` B（vs `168276` B）。
🔒 `683b7fa` vs 父態之**生產碼相異 ＝ `1／34`**（唯 `app.py`）⇒ **換 `app.py` 一檔即忠實重建該態**。
🔒 **坑 `5` 攔法**：每態跑前**先清** `got_*`、**逐態各自快照**、**出艙各態實際寫出之檔名**。

### 四-3　`Q-2`（旗標字樣於生產碼 `34` 檔命中 `0`）

**Σ 命中 ＝ `0`／`34` 檔**（**正面列舉**·⛔ 全倉排除式）✅
🔑 **對照（父態 `app.py`）＝ `2`** ✅ ⇒ **該檢非恆為 `0`**。
**錨計算無條件執行之 AST 實查**：函式內 `environ` 存取 **`0`** 處 ✅／`_oblique_s_max` 呼叫 **`1`** 處。

### 四-4　`Q-3`（七支選擇器活體檢）

| 探針 | `rc` | 活體檢 | 判 |
|---|---|---|---|
| `probe_WG9268p_c1_synth.py` | `4` | 🛑 loud 拒測 | ✅ 合 `Q-3` |
| `probe_WG9268p_c2_gates.py` | `4` | 🛑 loud 拒測 | ✅ |
| `probe_WG9268p_c2_v1prime_mem.py` | `4` | 🛑 loud 拒測 | ✅ |
| `probe_WG9268p_c2_v1prime_pert.py` | `4` | 🛑 loud 拒測 | ✅ |
| `probe_WG9268p_conserv.py` | `4` | 🛑 loud 拒測 | ✅ |
| `probe_WG9268p_mem.py` | `4` | 🛑 loud 拒測 | ✅ |
| `probe_WG9268p_offsrc.py` | `4` | 🛑 loud 拒測 | ✅ |

🔒 **⛔ 任一支以「二態逐位相同」判綠** ⇒ **合 `Q-3`**（其允「逐支 loud 拒測**或**依其新設之取態法通過」）。
🔒 其轉紅係**預期**（起手清單 `6`／坑 `16`：`c3` 後二態皆呼叫 ⇒ 該量逐位相同）、**⛔ 新破損**。
🔒 **loud 拒測 ＝「無從驗」，⛔ 等同「驗不過」**（坑 `9`／`序 11 之一般化` 款三）。

### 四-5　步二（`§二-3`）：造 `a` 重擬為三造

| 造 | 受詞 | 期望 |
|---|---|---|
| `a′` 錨⛔ 後退 | `cum_S ＝ 10.0` 而 `s_geo ＝ 7.25` | `10.0`（`max()` 之語意）|
| `a′`（併保留） | `cum_S ＝ 9.0` 而 `s_geo ＝ 7.25` | `9.0`（**自原造 `c` 移入**·同一不變量·保留以⛔ 減損既有覆蓋）|
| `b` 生效 | `cum_S ＝ 1.0` | `7.25`（**原造 `b`·⛔ 動**）|
| `c` 旗標已無效 | 設 `'0'` ⋀ 未設 | 二者皆 `7.25`（＝ 造 `b`）＝ **正面斷言⛔ 殘留分支** |

🛑 **容差沿用** `chk` 之既有 `1e-9`·⛔ 放寬。

🔑 **判別力（坑 `16`／`17`：須驗其<u>紅得其所</u>）**
以**父態 `app.py`** 跑**重擬後**之 `c1_synth` ⇒ 造 `c` 之「設 `'0'`」式**轉紅**
（**得 `1.0000000000` 期 `7.2500000000`**）⇒ 其紅係「**偵得殘留分支**」、**⛔ 因例外**
⇒ **紅得其所** ✅。落檔 ＝ `verify/out/WG9268pR_c13_c3_c1synth_discriminating.log`。
🩸 **照實併呈**：造 `c` 之「**未設**」式於父態**亦綠**（父態預設即 `on`）
⇒ **該式無判別力**；本造之判別力**全由「設 `'0'`」式承擔**。

---

## §五　🛑 呈發單側之二事（**⛔ 自裁**）

### 五-1　🔴 **`補令十一 §二-2` 之活體閘 與 `補令十三 §二-3` 之步二，於 `c3` 態互斥**

**形**：活體閘置於**全部判定之前**，於 `c3` 態 **loud 拒測並 `rc = 4` 中止**
⇒ **步二所立之造 `a′`／`b`／`c` 於 `c3` 態<u>從未被執行</u>**。
⇒ 造 `c` 所欲提供之「**行為**之證」（與 `Q-2` 之「**字樣**之證」互為兩證·單 `§二-3` 逐字）
**於 `c3` 態實際上取不到**；現僅存 `Q-2` 之字樣證。

🔒 **本窗之處置 ＝ ⛔ 動該閘一字**，理由如下（**⛔ 自解**）：
> `作業常規之追加五` **`一-2` 黑名單**逐字列「須**新訂**一條準據，或須**廢止／放寬任何既有之閘**」
> ⇒ **恆停機**、**⛔ 自解**。修改 `wg9268p_selector_liveness.py` 係**放寬既有之閘**，
> 且該模組為**七支共用** ⇒ 其改動將同時變更 `Q-3` 對全七支之判。

🔒 **`Q-3` 本身⛔ 因此不過**——其受詞為「逐支 loud 拒測**或**依其新設之取態法通過」，
loud 拒測**即合格**。本項所呈者係**步二之可觀測性**，⛔ `Q-3` 之合格與否。
🛑 **候發單側裁**：`(i)` 認 `c3` 態下步二之造為「已立而不可觀測」，俟後續批另設取態法；
抑或 `(ii)` 為 `c1_synth` 另設一「新設之取態法」使其得以執行。**本窗⛔ 選其一。**

### 五-2　🟡 `GB-161` 四數之來源態（承本窗 `b34e42e` 之停機報告 `§五`）

已依單 `§三` 之授權於 `GB-161` 之**末端追加**內以**併記**載其事實
（其三軸逐字「態 `blob@e835ed5` ＋ **本批之修**」之**後半為偽**——四數係**改前**之樹所產），
並逐字聲明 **⛔ 更動該段一字、⛔ 改其落檔、⛔ 鑄號、⛔ 判其為何類之自誤**。
🔒 **⛔ 影響母體更正之數**（受詞係**檔框**，而 `14`／`8` 於改前改後二態**皆同**）。
🛑 其處置**候發單側**。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 判 | 落點 |
|---|---|---|
| `ad4bb81` 工項零 | **（空）零生產碼** | 主線·已推 |
| `655d86a` 序 `1` | **（空）零生產碼** | 主線·已推 |
| `d49d9db` 序 `2` | **（空）零生產碼** | 主線·已推 |
| **`10d8202` 序 `3`（`c3`）** | 🔴 **`app.py`** ⇒ **生產碼** | **側支 `verify/W-G.9-268p-c3`**·⛔ 主線 |

🔒 **主線之 `app.py` 自單所載期初態起逐位未變**：`62d7f70` ＝ `d49d9db` ＝ `9873b168278fbc08`
⇒ 單 `§五`「序 `3` 之前⛔ 動 `app.py` 一字」**成立**。

---

## §七　🩸 本批之自捕清單（⛔ 出艙前皆已攔下）

| # | 形 | 徵候 | 攔法 ／ 處置 |
|---|---|---|---|
| `1` | **heredoc** 誤用於 `Bash`（**二次**）| 被 `wg9237_heredoc_guard` **硬阻斷** | 改 `Write` 落檔 ＋ 以**路徑**呼叫（坑 `7`／`18-g`）|
| `2` | 取甲造用 **B 形** | 甲造得 `0` ⇒ **量測器紅** | 依 `常規四（七）補款二` 款二**先判器紅**；改 **B ⋀ C 並取**（`GB-158`）|
| `3` | 初讀 `GB-161` 表**誤判為子集** | 誤讀為「`7` 列 vs 宣告 `14` 支」 | 覆查逐列 ⇒ 第 `7` 列為**摺疊列 `\`7\`–\`14\``** ⇒ **撤回該讀法**、⛔ 出艙 |
| `4` | 成因之**算術推論** | Δ 對得上⛔ 蘊含成因為真（`常規八`）| 以**重建之改前樹**（相異檔數 `1`）直接重跑 ⇒ 逐項相符 |
| `5` | 🆕 **背景長跑以 `&` 再背景化** | 包裹程序返回即**殺掉子殼** ⇒ `stdout` `0` B、無 `rc.txt`、無 `got_*`——**看起來像「跑完且無輸出」** | 讀其 task 輸出證其**未執行**；改**⛔ 用 `&`**（工具本身已背景化）。🛑 **⛔ 讀為「二態無差異」** |
| `6` | 造 `c` 之「未設」式 | 於父態**亦綠** ⇒ 該式**無判別力** | 照實具名；判別力**全由「設 `'0'`」式承擔**（坑 `17`：逐造檢其紅之**理由**）|

---

## §八　併記（`GB-132` 族·自指增長）

🩸 本報告 payload 自身含 `補令十三`／`自誤 348`／`GB-161` 等字樣 ⇒ **落地後其命中數必增**。
上開各節之數係**立節當時**之現態（`§一`／`§二` 之占用數為 `ad4bb81` 前之態），
依 `常規四（九）五` **⛔ 追改**；爾後引用須**當場復算**。
🔒 **哨兵之號**依坑 `14`／`GB-147` **執行期自倉掃出**，其字面**⛔ 入倉**，一律以 **`⟨NEG⟩`** 指之。
