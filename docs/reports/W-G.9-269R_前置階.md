# `W-G.9-269R`　前置階　執行報告

> **態**：期初 ＝ `3eb65f6e1e3306af2090809ca45ade7876a9ed13`（`ls-remote` 實查）／期末 ＝ 見 `§七`
> **受詞** ＝ CC。**全批零生產碼**（生產碼 `34` 檔逐檔 blob 期初 ＝ 期末·`Q-1`）。
> **結論**：`p1` ✅ 出艙／`p2` ✅ 出艙／**`p3` 🛑 停機上呈**（二獨立理由·`§五`）。

---

## §零　🔴 單之送達與其記載之二處失準（照實·⛔ 頂替）

### 零-1　本單於期初態**⛔ 在倉**

單之導引逐字令「正本全在倉內，逐字自倉讀：`docs/orders/W-G.9-269_…`（本單）」。
**現查（態 `3eb65f6`·母體 ＝ 追蹤檔全體）**：檔名含 `269` 者 **`0`** 檔；`docs/orders/` 下 `0` 檔；
工作區 `find` 亦 `0`。
**對照組**：檔名含 `268` 者 ＝ **`144`** 檔（須非零）✅／哨兵（執行期組出·`GB-147`）＝ **`0`** ✅
⇒ 量測器非紅。

⇒ 🔒 **該「已在倉」之記載為偽**；單係由 KL 於對話側交付。
⛔ 以倉內近似物代替（`施工單必須自足` 之判準）——本窗**照實回報並俟 KL 交付後始辦工項零**。

### 零-2　單所載之**期初態**已過期

單 `§零` 逐字載期初態 ＝ `2a3fe88bfccdd87890ba7d12b27cd6277275890f`。
**`git ls-remote origin refs/heads/wip/s1-endpart` 實查** ＝ **`3eb65f6e1e3306af2090809ca45ade7876a9ed13`**。
`3eb65f6` 係 `2a3fe88` 之**後裔**（`3eb65f6` ＝「本窗收工：交接註增補（三項）⛔ 零生產碼」）。
⇒ 依 `S-2` 逐字「⛔ 採信本地追蹤 ref、一律 `ls-remote` 實查」，**以實查值 `3eb65f6` 為期初態**。
🔒 **⛔ 影響 `S-3`**：`app.py` 三值與單所載**逐位相同**（見 `§一 S-3`）。

🩸 **併記（坑 `23` 之現形）**：主 checkout 之本地 `wip/s1-endpart` 現停於 `1e5a87c`，
較 `origin` **落後 `90` 個 commit**（`left-right` ＝ `0 90` ⇒ **⛔ 分岔**）。
⇒ 若以本地追蹤 ref 判態，將得一個落後 `90` 個 commit 之「期初態」。**本窗未採信之。**

---

## §一　開工閘（逐條當場復現·各載母體與粒度框）

| # | 閘 | 實測 | 判 |
|---|---|---|---|
| `S-0` | 本單原封入倉 | 三 `sha256` 逐位相同 | ✅ |
| `S-0b` | `probe_order_preflight.py` | 停機款 **`0`** 項；🟡 `3` 項（逐項處置見下） | ✅ |
| `S-1` | 號占用 | 宣告框 檔 `3`／列 `3`·**逐筆歸類皆白名單類 `1`** | ✅ |
| `S-2` | `ls-remote` 實查 ＋ `left-right` | `3eb65f6`；`left-right` ＝ `0 0` | ✅ |
| `S-3` | 生產碼 `34` 檔 blob 全表 | `34` 檔；`app.py` 三值全符 | ✅ |
| `S-4` | `run_verification` 期初實跑 | `24` PASS／`40` FAIL／`RESULT: FAIL` | ✅ |
| `S-5` | 四簿取號量 | 四簿**全數相符** | ✅ |
| `S-6` | 硬閘活體生效 | `--selftest` 四造全成立 ⋀ **活體攔截實測** | ✅ |
| `S-7` | 探針定位普查 | 見 `§三`（先於 `p1`／`p3` 之任何實跑） | ✅ |

### `S-0`　工項零之三 `sha256`

**受詞** ＝ `docs/orders/W-G.9-269_施工單_病一_前置階.md`

| 側 | `file sha256` | bytes |
|---|---|---|
| 來源（KL 交付檔） | `e040c1d4143f656e286d45c269e95d2519737335379e13cf6201b3987a0053b0` | `11487` |
| 落檔 | 同上 | `11487` |
| 倉內 blob | 同上 | `11487` |

**blob `sha1`** ＝ `ed07dc6ae98f561305af74cbf8fe15516423d5ee`（🔒 二式並報·`雜湊一律具名其式`）。
**行尾**：`CR 0`／`LF 151`；`core.autocrlf` **有效值 ＝ `false`**
（三層：system `true`／global **未設**（`rc=1`）／local `false` ⇒ local 覆蓋）⇒ **無正規化** ⇒ 三側必然相等。

### `S-0b`　🟡 `3` 項之逐項處置（⛔ 靜默略過）

| 項 | 受詞 | 處置 |
|---|---|---|
| `P-5` | 單內⛔ 無 `SELF_SHA256` 列 ⇒ ⛔ 可對拍 | **具名豁免**。其所欲保證者（傳輸保真）已由 `S-0` 之**三側逐位相同**承擔，且**來源檔在手**⇒ 證據較 `SELF_SHA256` 自證**更強**。🛑 依「`序 11` 之一般化」款 `三`，本項係「**無從回測**」⛔ 非「回測不過」。 |
| `P-1` `:85` | 「先定『九份』之所指」被判為全稱否定而未具名母體 | **偽陽**。該列係**指示**（令 CC 去抽），⛔ 係斷言；且其母體（`CLAUDE.md`）同段已具名。**具名豁免**。 |
| `P-1` `:109` | 「開工閘任一不符」 | **偽陽**。係**停機款之列舉**，⛔ 係否定性存在主張。**具名豁免**。 |

### `S-1`　號占用（六數分列 ＋ 逐筆歸類 ＋ 二對照組）

**三軸**：`(1)` 來源 ＝ `blob@3eb65f6`／`(2)` 母體 ＝ **全 `docs/` `672` 檔**（讀不到 `19`）／
`(3)` 粒度框 ＝ 檔框 ＋ 列框（宣告框；`D3` 取**嚴格式**·補款 `⑧`）。

| 號 | `D1` | `D2` | `D3` | 雙屬 | **列框**（`|D2 ∪ D3|`） | **檔框** | 鬆檔／鬆列 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-269`** | `0` | `3` | `0` | `0` | **`3`** | **`3`** | `20`／`52` | 受詢 |
| `W-G.9-268` | `44` | `57` | `15` | `1` | `71` | `46` | `49`／`330` | 對照甲（須 `≥1`）✅ |
| `W-G.9-267` | `4` | `13` | `5` | `0` | `18` | `6` | `13`／`60` | 對照甲2 ✅ |
| 哨兵（執行期組出·字面⛔ 出艙） | `0` | `0` | `0` | `0` | `0` | `0` | `3`／`4` | 對照乙（須 `=0`）✅ |

🔒 **逐筆歸類（`3` 筆·全體·⛔ 節略）**——依 `裁 H`「命中數本身⛔ 非判準，係**待歸類之材料**」：

| # | 錨 | 逐字（節略） | 歸類 |
|---|---|---|---|
| `1` | `docs/orders/W-G.9-268p_施工單_GB67病二_起算垂線錨點更正.md:217` | `## §十一　\`W-G.9-269\` 之預告（⛔ 本批辦）` | **發單側對本批之前瞻引用** ⇒ 白名單類 `1` |
| `2` | `docs/orders/W-G.9-268p_補令十二_GB161硬編他樹與c3前置.md:62` | `### 三-2　…（登記·排入 \`W-G.9-269\` **之前置**·本批⛔ 辦）` | 同上 |
| `3` | `docs/reports/W-G.4_泛用阻塞項登記表.md:6042` | `### \`GB-159\` 🆕　…（俟 \`W-G.9-269\` 解除）` | 同上 |

⇒ 🔒 **三筆<u>皆</u>為發單側歷次補令／登記中對本批之前瞻引用**，**⛔ 第三方**
⇒ 依宣告框補款 `②`（引用⛔ 構成占用）**⋀** 自解款白名單類 `1` ⇒ **未占用·可用**。
🛑 **`§四` 停機款「`S-1` 之第三方占用」⛔ 觸發。**

### `S-3`　生產碼 `34` 檔（正面列舉·⛔ 全倉排除式）

**`app.py` 期初三值 vs 單所載**

| 量 | 單所載 | 實測 | 判 |
|---|---|---|---|
| bytes | `1458081` | `1458081` | ✅ |
| blob `sha1` | `1d7d7c73…` | `1d7d7c7324719f13…` | ✅ |
| file `sha256` | `b74fd07c…` | `b74fd07c3c86a914…` | ✅ |

🔒 全 `34` 檔之逐檔 bytes／`sha256`／`sha1` 見 `verify/out/WG9269R_S4_runverif_818a95b.log` 同批之出艙
（本報告 `§七` 另載其期初 ＝ 期末之判）。檔數 ＝ **`34`** ✅。

### `S-4`　`run_verification` 期初實跑

**落檔** ＝ `verify/out/WG9269R_S4_runverif_818a95b.log`（`168276` B·`sha256` `934c32cd4cd1d05d…`·**已入倉**）
**`rc`** ＝ `1`／**`stderr`** ＝ `0` B／**`RESULT: FAIL`**／耗時 ≈ `2m52s`

**紅綠名目集合**（⛔ 只記數）：**`24` PASS／`40` FAIL／總 `64` 名目**。
`40` 紅之逐項名目見落檔（`§六` 之 `run_all` 對帳因 `p3` 停機而未辦·見 `§五`）。

🔴 **本閘之一項實測，係 `p1` 之獨立外部錨**（詳 `§二`）：
- `✅ PASS  結構不變量永久閘0m（守恆/⊥/位次=投影序/J(k*)≥J(naive)/理論=實跑/telescoping）`
- `✅ PASS  結構不變量永久閘3.5m（…同上…）`
- `圍堵閘` 之字樣命中 ＝ **`0`**；可達街廓 ＝ **`R1`–`R6` 全六塊**。

### `S-5`　四簿取號量（各載母體與框·⛔ 混框）

| 簿 | 母體（單檔） | 框（逐字） | 相異 | MAX | 次號 | 缺號集 | 對單 |
|---|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`709398` B／`8280` 列） | 定義框 `` ^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`? `` ＋ 範圍框 | `345` | `352` | `353` | `[106]` | ✅ |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md`（`778303` B／`6342` 列） | **標題形 ∪ 表列形**（見下） | `159` | `161` | `162` | `[12, 87]` | ✅ |
| `VR` | `docs/驗證裁定登記表.md`（`394392` B／`5124` 列） | `` ^#+ *`?VR-([0-9]{1,4})`? `` | `80` | `95` | `96` | `[73, 75]` | ✅ |
| `K-9` | `docs/rulings/K-6_街角地分配程序與可分配判準.md`（`295061` B／`4057` 列） | `` ^#{2,4}[^\n]*?K-9-([0-9]{1,3}) `` | `28` | `29` | `30` | `[]` | ✅ |

🔴 **`GB` 之框須為<u>聯集</u>（本窗當場自捕）**：單框試三式皆**不符**單所載
（緊反引號式 相異 `75`／寬式 `85`／H2 式 `3`）。
其正解 ＝ **標題形 `` ^#{2,4}[^\n]*?`?GB-([0-9]{1,4})`? ``（相異 `85`）∪ 表列形 `` ^\| *\*{0,2}`?GB-([0-9]{1,4})`? ``（相異 `134`）**
⇒ 聯集 **`159`**／MAX `161`／缺號 `[12, 87]` ⇒ **三數全符**。
🔒 **成因**：`GB` 簿與自誤簿同病——**二形並存**（表列式如 `| **\`GB-103\`** 🆕 |`）。
🛑 依 `W-G.9-205` 更正註「`GB` 之取號須同格載明所用之框」，**本報告所用之框即上開聯集框**。

**所扣之靜態清單**（`常規四（八）` 補款）：`VR` 扣哨兵 `999`（`1` 個·實測所扣 ＝ `[999]`）；
`VR-1`–`13`／`K-9-1`／自誤 `1`–`6` 之靜態清單**於各該框下本即⛔ 命中**（結構上撈不到）
⇒ 其「未扣除」與「已扣除」二數**相同**，此與三節靜態清單之記載**一致**。

### `S-6`　硬閘活體生效（`--selftest` **⋀** 活體攔截·⛔ 只 `test -f`）

- **`--selftest`** `rc=0`；**判別力造四項全數成立**：靶① `P-3` 紅 ✅／靶② `P-4` 提示 ✅／
  靶③ `P-6` 提示 ✅／**反靶 `P-3` 不紅** ✅ ⇒ 逐字「本器非恆綠亦非恆紅」。
- **活體攔截實測**：同一跑次中 `P-5` **實際轉紅**於一真實檔
  （`:441 SELF_SHA256 🔴 不符｜實算 53ac7331d04e2442… ／ 單載 28fad809f2063941…`）
  ⇒ 該停機款**確能於活體攔下**，⛔ 僅存在。

---

## §二　`p1`　`A` 母體以**新錨態**重量

### 二-1　器（⛔ 另寫第二份定義）

**判準器** ＝ `verify/probes/probe_WG988_nocross.py`（`K-9-15` 之「地界線不交叉」·`s_star_of`／`margin_of`）。
**驅動器** ＝ `verify/probes/probe_WG991_yishim.py`（**原樣 import** 上者之判準）。
🔒 二者皆**⛔ 在 `GB-161` 族之 `23` 支內** ⇒ `S-7` 對其放行。
🔒 **⛔ 另寫第二份定義**——本窗**未改該二器一字**。

**情境之界（照實·⛔ 頂替）**：`probe_WG991_yishim.py:76` 逐字
`SB = 0.0                    # 🔒 情境母體 ＝ 僅 0m（同 \`-88\`／\`-90\`·⛔ 不擴·具名）`
⇒ 🔴 **既有器之情境母體<u>僅 `0m`</u>，且其原文明令「⛔ 不擴」**。
⇒ 單 `§二 p1` 之「二情境分列」**於既有器⛔ 可得**；擴之即係**改其受量欄位**（`p2` 款 `1` 所禁）
或**另寫第二份定義**（`p1` 器款所禁）。🛑 **本窗依 `常規二` 取保守項：⛔ 擴，逐字具名於此。**
🔒 **併記**：`GB-67` **病一**之受詞本即**退縮 `0` 公尺情境**（`CLAUDE.md`「鏈一｜退縮 `0 m` 情境 → `GB-67`」）
⇒ `0m` 即其正受詞；`3.5m` 屬**鏈二**（telescoping·harness 專屬）。

### 二-2　逐宗表（新錨態 vs 舊錨態·全列·⛔ 節略）

**新錨態**（態 `818a95b`·落檔 `verify/out/probe_WG991_yishim_818a95b.log`·`70375` B·`sha256` `1c2466b7f1a6576b…`·**已入倉**）

```
**A-1-1 違反宗之確認**：母體 ＝ **36 列**；違反 ＝ **0 宗**
POPULATION=36 PRINTED=0 SUPPRESSED=36  # A-1-1 違反宗（全列）
   面積合計（帶號）＝ **0.0000 ㎡** ／（絕對值）＝ **0.0000 ㎡**
```

**舊錨態**（態 `2cb0fe9`·落檔 `verify/out/probe_WG991_yishim_2cb0fe9.log`·**倉內既有**）

```
**A-1-1 違反宗之確認**：母體 ＝ **47 列**；違反 ＝ **2 宗**
鍵                  i      前                  s*            面積(㎡) 情形
R2|左|1|0           1      0             10.0407       3.80822376 在內
R5|左|1|0           1      0              5.1067       0.98672881 在內
POPULATION=47 PRINTED=2 SUPPRESSED=45
   面積合計（帶號）＝ **4.7950 ㎡** ／（絕對值）＝ **4.7950 ㎡**
```

🔒 **`2cb0fe9` 確為舊錨態**：`git merge-base --is-ancestor c1376af 2cb0fe9` ⇒ **偽**
（`c1376af` ＝ `2026-09-10`「`c2`：旗標 `WG9268P_ANCHOR_GEOM` 之預設由 `off` 改為 `on`」；
`2cb0fe9` ＝ `2026-08-21`）。

### 二-3　三分對拍

| 分 | 宗數 | 逐宗 |
|---|---|---|
| **新增** | **`0`** | — |
| **消失** | **`2`** | `R2|左|1|0`（`3.80822376 ㎡`）／`R5|左|1|0`（`0.98672881 ㎡`） |
| **留存** | **`0`** | — |

🔒 **母體基數同格出艙**：舊 `47` 列 ／ 新 **`36`** 列；「全部宗」舊 `59` ／ 新 **`46`**。

🔴 **⇒ `p1` 之答：新錨態之 `A` ＝ <u>空集</u>（`0` 宗／`0.0000 ㎡`）。**
🛑 ⇒ 單 `§零` 所預期之「違反宗集**可能已非** `628-42(1)`／`628-53(2)`」——
**實測更強：其<u>已為空</u>。**

### 二-4　🔴 判別力與**量測器非紅之自證**（本節為 `p1` 之要點）

單之判別力款（「若逐宗**相同**須另證」）**⛔ 觸發**（二者相異）。惟本窗**另設三項**，
因「`0` 宗」恰是**最該被懷疑之數**（「未量」與「量得為零」⛔ 得共用出艙碼）：

**(a) 🔴 該器之<u>自身負對照失敗</u>——⛔ 隱去**

新錨態之落檔逐字：
```
🔒 **負對照（判別力·常設 8）＝ 移除集 ＝ ∅** ⇒ 須得倉內錨 `R2 45.9766`／`R5 56.3293`：
   R2   倉內錨    45.9766 ／ 負對照實得 🔴 未命中 ⇒ 🔴 **不符 ⇒ 本 shim 之構造存疑·停機**
   R5   倉內錨    56.3293 ／ 負對照實得 🔴 未命中 ⇒ 🔴 **不符 ⇒ 本 shim 之構造存疑·停機**
⇒ 負對照判 ＝ **🔴 **未通過****
```
（舊錨態之同一對照 ＝ `45.9765888243`／`56.3292619405` ⇒ ✅ 通過。）

🔒 **「未命中」之受詞已查明**：新錨態之 `A-1-3` 破閘格 ＝ `POPULATION=0 PRINTED=0 SUPPRESSED=0`
⇒ **破量表為空** ⇒ 以 `R2`／`R5` 為鍵之查找自然「未命中」。
⇒ 該負對照所寫死之 `45.9766`／`56.3293` 係**舊錨態之期望值**，於新錨態下**其受詞已不存在**。

🛑 **⇒ 二讀法並存，⛔ 得由該器自身分辨**：
`(甲)` 受詞真歸零（錨點更正消滅了破量）／`(乙)` 量測器壞、母體塌陷 ⇒ 什麼都沒量到。
**⇒ 依「量測器非紅之自證」款，該器之輸出在此**⛔ 得單獨出艙**；須另備<u>外部錨</u>。**

**(b) ✅ 外部錨（`常規八` 款 `二` `①` 與獨立來源對拍）＝ `S-4` 之 `run_verification`**

`run_verification` 係**生產 harness**、**⛔ 該 shim**，其為完全獨立之第二量測器。
其於**同一態 `818a95b`** 之實測（落檔 `verify/out/WG9269R_S4_runverif_818a95b.log`）：

| 受詞 | 舊態（`CLAUDE.md` 所載） | **新態實測** |
|---|---|---|
| `0m` 中止閘 | `②-宗 圍堵閘破[R2]`·`45.9766` | 🟢 **⛔ 中止**——`結構不變量永久閘0m` ＝ **`✅ PASS`** |
| `3.5m` 中止閘 | `②-宗 圍堵閘破[R5]`·`56.8186` | 🟢 **⛔ 中止**——`結構不變量永久閘3.5m` ＝ **`✅ PASS`** |
| `圍堵閘` 字樣命中 | （既有紅） | **`0`** |
| 可達街廓 | `0m` 止於 `R2`／`3.5m` 止於 `R5` | **`R1`–`R6` 全六塊** |

⇒ 🔒 **獨立坐實**：`②-宗 圍堵閘`**已不再破**，六街廓**全跑通**。
⇒ 讀法 `(乙)`（母體塌陷／什麼都沒量到）**被否證**——若真塌陷，`run_verification` 不可能全六塊可達且該閘轉綠。
⇒ 🔒 **`(甲)` 成立：`A` 之歸零係<u>真</u>，而該 shim 之負對照失敗係其<u>寫死之舊錨態期望值過期</u>（量測器之期望值紅，⛔ 其構造紅）。**

**(c) ✅ 「確係於新錨態所量」之正面證（單 `Q-2`）**

1. `git merge-base --is-ancestor c1376af HEAD` ⇒ **真** ⇒ 本態已含錨點更正。
2. 🔒 **更強之證（⛔ 依賴 commit 之身分·而量其**輸出**）**：二態之**逐宗幾何值相異**——
   `R1 i=0` 之 `s_lo` 舊 `0.260267915` → 新 **`0.260279154`**；
   `s_hi` 舊 `8.838507535` → 新 **`8.838609579`**。
   ⇒ **幾何確已改變** ⇒ 該跑**⛔ 是舊錨態之重播**。

🛑 **本窗⛔ 改該 shim 之寫死錨一字**（`p1` 器款「⛔ 另寫第二份定義」／`p2` 款 `1`「⛔ 改其判準」）。
🔒 **其負對照之期望值已過期一事，係<u>登記</u>，其處置候發單側**（⛔ 由 CC 自裁）。

---

## §三　`p2`　`GB-161` 全族之修與**效力判**

### 三-1　`S-7` 探針定位普查 ＝ `p2` 之母體重掃

**三軸**：`(1)` `blob@01177f9`／`(2)` 母體逐框分列（見下）／`(3)` 檔框 ＋ 列框。
🔒 母體以 `git ls-tree` 取得（`常規補款二`：⛔ 以 `grep -r … .`——該形恆含 `.claude/worktrees/` 之巢狀倉副本·`GB-35`）。

| 框 | 母體 | `worktrees` | `os.chdir(` | 交 | **聯集** |
|---|---|---|---|---|---|
| **甲 `GB-161` 正典框**（`verify/**`·`os.chdir(`） | `288` 檔 | `14` 檔／`28` 列 | `10` 檔／`11` 列 | `3` | **`21`** |
| **乙 本窗擴框**（全倉 `*.py`·`os.chdir`） | `293` 檔 | `14` 檔／`28` 列 | `12` 檔／`14` 列 | `3` | **`23`** |

🔴 **單 `§二 p2` 所載 ＝ `19`（`14` ＋ `8`·交 `3`）；實測 ⇒ 二數皆 `≠ 19`。**
依單逐字「若與 `19` 相異，**照實出艙並以實測為準**」⋀「⛔ 以任何較小之集為限」⇒ **母體取 `23`**。

🔒 **差之逐項歸因（⛔ 臆造）**

| 段 | 量 | 成因（逐支具名） |
|---|---|---|
| `19` → `21` | ＋`2` | `verify/probes/probe_WG9268p_c3_noresidual.py`／`verify/probes/probe_WG9268p_c3_unit.py` —— 二支於 `GB-161` 掃描**之後**方入倉（`c3` 批之產物）。**族隨倉長大**。 |
| `21` → `23` | ＋`2` | `docs/reports/probes/probe_§4_R_end.py`／`docs/reports/probes/probe_§4_s0_pin.py` —— **母體軸**之差（⛔ 在 `verify/` 下）。 |

**對拍**：`GB-161` 所列之 `os.chdir` 八支 vs 本窗實測十支 ⇒ **新增 `2`**（即上開 `c3` 二支）／**消失 `0`**。
**對照組**：含 `import` 之 `*.py` ＝ `292` 檔（須 `>0`）✅／哨兵（執行期組出）＝ `0` ✅ ⇒ 量測器非紅。

### 三-2　逐支之分流（`23` 支·**全體**·⛔ 節略·`自誤 348`）

🔒 **判準**：`worktrees`／`os.chdir` 之字樣是否**參與執行期之路徑決定**（＝定位），
抑或僅為註解／排除邏輯／合成靶。**本窗⛔ 自動判定**，係逐支讀其 `REPO`／`ROOT` 之**定義列**後人工判。

**🔴 須修者 ＝ `4` 支（硬編絕對路徑指向他窗 worktree）**

| # | 檔 | 變數 | 改前所指之樹 |
|---|---|---|---|
| `1` | `verify/probes/wg9268_bing_csv.py` | `REPO` | `wg9-268-review-window-42a7ad` |
| `2` | `verify/probes/wg9268_ingest_verify.py` | `REPO` | `wg9-268-review-window-42a7ad` |
| `3` | `verify/probes/wg9268_nameset_diff.py` | `REPO` | `wg9-268-review-window-42a7ad` |
| `4` | `verify/tools/wg960_z_selfcheck.py` | `ROOT` | `wg953-handoff-ready-d1e3c0` |

🔴 **其一為<u>活體</u>危害**：`wg9-268-review-window-42a7ad` **現仍存在**（`git worktree list` 實查·其 `HEAD` ＝ `abeb879`）
⇒ `#1`–`#3` 改前所讀者為**一棵活的他樹**——即 `GB-161` 所預言之假綠，**於本窗仍在force**。
🔒 `wg953-handoff-ready-d1e3c0` **已不存在** ⇒ `#4` 改前係**loud 失敗**（⛔ 靜默錯）。

**🟢 免修·具名者 ＝ `19` 支（⛔ 靜默略過·逐支載其由）**

| 由 | 支數 | 逐支 |
|---|---|---|
| **已為 `__file__` 相對**（`dirname^n(abspath(__file__))`） | `9` | `docs/reports/probes/probe_§4_R_end.py`／`docs/reports/probes/probe_§4_s0_pin.py`／`verify/fixture_end_fallback.py`／`verify/fixture_end_reserve.py`／`verify/fixture_end_winner.py`／`verify/fixture_wf_ns_wiring.py`／`verify/probes/probe_WG9218R_setback_sensitivity.py`／`verify/probes/probe_WG9268p_c3_noresidual.py`／`verify/probes/probe_WG9268p_c3_unit.py` |
| **`GB-35` 排除邏輯**（字樣係**排除** `.claude/worktrees/` 之判準本身·改之即改判準） | `6` | `verify/probes/probe_D2b29_wg9_q1q3.py`／`probe_WG91_static_checks.py`／`probe_WG93_mp_definition_scope.py`／`probe_WG996_yi_invariants.py`／`probe_WG997_reassign_order.py`／`verify/tools/wg99_anchor_audit.py` |
| **僅見於註解**（`#1` 已修·其殘留字樣係沿革記載） | `1` | `verify/probes/probe_WG9268p_c1_synth.py` |
| **本器自身之 `PATS` 字面**（自指命中·`GB-132` 族·⛔ 真實受詞） | `1` | `verify/probes/probe_WG9268p_gb161_sweep.py` |
| **合成靶**（`TPL` 內之字面路徑係**受量對象**·⛔ 定位·改之即改判準） | `1` | `verify/probes/probe_WG971_wrap.py`（`:203`·其造係「同一 traceback 只換 worktree 名 ⇒ `[-300:]` 之切點不同」） |
| **`rev-parse --show-toplevel` 自定位**（且附 loud `assert` 驗倉根） | `1` | `verify/tools/wg911_baseline_trace.py` |

🔒 `9 + 6 + 1 + 1 + 1 + 1 = 19`；`19 + 4 = 23` ⇒ **與母體基數自洽** ✅

### 三-3　四支之**三項出艙**（單 `§二 p2` 款 `2`）

🔒 **所改者僅定位之取得方式**：`<var> = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))`
**⛔ 改其判準、容差、受量欄位、出艙形一字**。
**機檢**：`git diff --numstat` 四支**皆 `1 1`**；`CR` 改前→改後 **`0`→`0`**；`py_compile` `rc=0`；咬到 `4`／退回 `0`。

| # | `(i)` 改前所指之樹 | `(ii)` **改後**實算所指之樹 | `(iii)` 改後重跑 vs 改前既有落檔 |
|---|---|---|---|
| `1` `wg9268_bing_csv.py` | `…/worktrees/wg9-268-review-window-42a7ad` | **本倉** ✅ | **`rc` `0` → `1`**：`FileNotFoundError: …/jolly-brahmagupta-655b14/verify/out/got_G值_退縮0m.csv`。其輸入係 `verify/out/got_*`（**`.gitignore` 刻意忽略之族**）⇒ 本樹無之。**⛔ 有既有落檔**（其輸出僅至 stdout）。 |
| `2` `wg9268_ingest_verify.py` | 同上 | **本倉** ✅ | **`rc` `0` → `1`**：先正確印出 `受詞檔數 ＝ 11` 與第 `1` 筆 `✅ 逐位相同`（`16df5696`），繼而 `FileNotFoundError: …/verify/probes/W-G.9-268_附件甲_審查窗報告與草稿.md`——該來源係 `-268` 窗之 session 產物。**⛔ 有既有落檔**。 |
| `3` `wg9268_nameset_diff.py` | 同上 | **本倉** ✅ | **`rc` `0` → `1`**：`FileNotFoundError: …/verify/probes/wg9268_runverif_pre.out`（`-268` 窗之暫存）。🔒 其**凍存側**（`git -C REPO show <40 碼 REV>:<FROZEN>`）**仍成功**——`REV` 係完整 `SHA` ⇒ **樹無關**。既有落檔 ＝ `verify/out/WG9268pR_c2_nameset.log`（**未被覆寫**）。 |
| `4` `wg960_z_selfcheck.py` | `…/worktrees/wg953-handoff-ready-d1e3c0`（**已不存在**） | **本倉** ✅ | **`rc` ＝ `0`**（改前必 loud 失敗）。既有落檔 ＝ `verify/out/_z60_selfcheck.txt`。**差異 ＝ 純行尾**：正規化 `\r\n`→`\n` 後**逐位相同 ＝ `True`**；`Δbytes` ＝ `28` ＝ `CR` 增量 ＝ `28` ＝ 列數。**其七塊之判、bytes、`sha256` 逐格不變**（皆 `🔴 不符`·係該檔 docstring 自載之既有 loud 紅）。 |

🔒 **`(iii)` 之落檔已還原**：`#4` 之重跑覆寫了**已追蹤**之 `verify/out/_z60_selfcheck.txt`，
本窗已 `git checkout --` **還原**（⛔ 追改任何既有落檔一字·單 `§二 p2`）。差異僅記於本報告。
🩸 **併呈**：該器寫檔式為 `io.open(OUT, "w", encoding="utf-8")`（**`newline=None`**）
⇒ 於 Windows 將 `\n` 譯為 `\r\n`，而倉內 blob 為 `LF` ⇒ **每次重跑必產生 `28` 個純行尾差**。
**此係<u>登記</u>，⛔ 本批修**（改之即改其出艙形）。

### 三-4　**效力判**（逐支·單 `§二 p2` 款 `3`）

| # | 效力判 | 理由 |
|---|---|---|
| `1` `bing_csv` | 🔴 **其既往落檔之數<u>全部</u>⛔ 為本倉之證據** | `REPO` 決定其**輸入**（他樹之 `got_*.csv`）⇒ 所量者為**他樹之配地結果**。 |
| `2` `ingest_verify` | 🔴 **全部⛔ 為本倉之證據** | `REPO` 決定其比對之 **blob 側**（`git -C`／路徑皆他樹）。 |
| `3` `nameset_diff` | 🔶 **部分無效**：**凍存側有效**（`REV` 係完整 `SHA` ⇒ 樹無關·`GB-161` 已具名）；**實測側⛔ 為本倉之證據**（其讀他樹之 `runverif_pre.out`）。 | 二側之樹相依性**相異**。 |
| `4` `z_selfcheck` | 🟢 **其既往落檔之<u>數</u>有效** | `ROOT` **只決定輸出路徑**，⛔ 參與任何計算——七塊之內容係該檔內之**字面常數**，`TARGET` 亦然 ⇒ **樹無關**。本窗之重跑**正面坐實**：正規化行尾後與既有落檔**逐位相同**。 |

🔒 **本節之通則**：`GB-161` 之危害**⛔ 一律等重**——須逐支問「該硬編路徑決定的是**輸入**、**輸出**、抑或**比對端**」。
只有決定**輸入／比對端**者其數方失效；只決定**輸出**者，其數仍有效（惟其寫入了他樹）。

---

## §四　`§三` 驗收閘（逐閘·各載判定集基數與兩造對照）

| # | 所保證之性質 | 判定集基數 | 實測 | 判 |
|---|---|---|---|---|
| `Q-1` | 生產碼 `34` 檔逐檔 blob 期初 ＝ 期末 | `34` 檔 | 相異 **`0`**／`34` | ✅ |
| `Q-2` | `p1` 之 `A` 係於**新錨態**所量 | `2` 造 | `c1376af` 係 HEAD 之祖先 ⋀ **逐宗幾何值相異**（`s_lo` `0.260267915`→`0.260279154`） | ✅ |
| `Q-3` | `p2` 之母體為全族 ⋀ 逐支皆有三項出艙與效力判 | `23` 支 | 母體 `23`（實測·具名）；`4` 支三項全出 ＋ 效力判；`19` 支「免修·具名」逐支載由 | ✅ |
| `Q-4` | `p3` 之「九份」係自倉內既有記載機械抽出 | — | 🛑 **見 `§五`** | 🛑 **停機** |
| `Q-5` | `p3` 重烤後逐檔差異皆歸因於錨點更正 | — | 🛑 **未辦**（前置之 `Q-4` 停機） | 🛑 |
| `Q-6` | `run_all` 對帳之真迴歸為空 | — | 🛑 **未辦**（`p3` 停機·`run_all` 屬重烤後之驗收） | 🛑 |

🔒 **`Q-1` 之判別力（證本比法⛔ 恆綠）**：取二必動之檔 ⇒
`W-G.9-269_施工單…md`（`(缺)` → `ed07dc6ae98f`）／`wg9268_bing_csv.py`（`6a59b7f5ad94` → `a4a54a8e6851`）
⇒ **皆相異** ✅ ⇒ 該比法非恆綠。

🛑 **`Q-5`／`Q-6` ⛔ 得記為「通過」亦⛔ 得記為「不過」**——其判定集為**空**
⇒ 依「`序 11` 之一般化」款 `二`／`三`：**loud 拒測**，且「**無從回測**」⛔ 等同「回測不過」。

---

## §五　🛑 `p3` 停機上呈（**二獨立理由**·任一即足）

### 五-1　理由甲：**辦 `p3` 必須動生產碼一字** ⇒ 觸 `§四` 停機款

`CLAUDE.md` 逐字（其節「🔒 `W-G.9-206` 補款　…`_DECLARED_EXTRAS` 之移除時點」）：

> 🛑 **九份 baseline 重產（波末）時，須將 `指數名次` 自 `_DECLARED_EXTRAS` 移除**，
> 使該欄回歸守衛之射程；⛔ 得任其留存。

**現查（正面列舉·四檔）**：`_DECLARED_EXTRAS` 之命中 ＝
`app.py` **`0`**／`verify/stepg_pipeline.py` **`0`**／`verify/run_all.py` **`0`**／**`verify/run_verification.py` `4`**。
其定義體（`verify/run_verification.py:390`–`400`）含 `指數名次` **二處**
（`W-D.1.2 診斷_退縮0m.csv` 與 `…3.5m.csv` 各一）。

🔴 `verify/run_verification.py` **正是 `常規一 🔧 二` 生產碼四檔清單之一**
⇒ 依其逐字判法，動之即為**生產碼 commit**。
⇒ 而單 `§零` 載前置階生產碼 ＝ **零**，`§四` 停機款逐字「**須動生產碼一字（本階為零生產碼）**」
⇒ 🛑 **停機。**

🔒 **⛔ 以「先重烤、`_DECLARED_EXTRAS` 留待後批」規避**——該款逐字為「**重產時，須**……移除」，
且其立法理由載明「使該欄**回歸守衛之射程**」：若先重烤而不移除，
`指數名次` 將被**烤進 baseline**，該守衛（`U-K2`）對其**永久失明** ⇒ 反使該款**永不可能履行**。

### 五-2　理由乙：**「九份」之所指<u>歧義</u>**，擇一即係 `⛔ 自擬` ⇒ 觸 `§四` 停機款

**單 `p3` 款 `1` 逐字**令「自 **`CLAUDE.md`** 之既有記載**機械抽出**該九份之檔名清單」。

**現查（態 `818a95b`·母體 ＝ 追蹤之 `.md` `695` 檔·讀不到 `0`·列框）**
- `九份` ＝ `39` 檔／`72` 列；其中 **`CLAUDE.md` 佔 `8` 列**（`:575`／`:596`／`:1103`／`:1314`／`:1463`／`:1606`／`:2659`／`:2662`）。
- **該 `8` 列<u>逐列皆為<u>裸標籤</u>**（「九份 baseline 重產／重烤」），**⛔ 任一列枚舉檔名**。
- **變體之窮舉（先窮舉再斷言）**：`九支` `0`／`九個` `0`／`9 份` `0`／`9份` `0`／`九張` `0`／`九檔` `20` 檔（皆他義）。
- **對照組**：含 `baseline` 之 `.md` ＝ `395` 檔（須非零）✅／哨兵（執行期組出）＝ `0` ✅ ⇒ 量測器非紅。

⇒ 🔴 **單款 `1` 之逐字指示（自 `CLAUDE.md` 抽出）<u>結構上不可履行</u>**——該檔⛔ 含任何清單。

**惟「倉內」確有二份候選清單，且二者<u>相異</u>**：

| 候選 | 出處 | 內容 |
|---|---|---|
| **甲** | `docs/reports/W-G.9-122_CC交接文.md:87`–`:90`（`G-6` KL 行動清單·逐字「該重產之 `9` 份·皆在 `verify/baselines/`」） | `退縮0m參數.csv`／`退縮3.5m參數.csv`／`第 1 宗街角地指配結果_退縮0m.csv`／`第 1 宗街角地指配結果_退縮3.5m.csv`／`W-D.1.2 診斷_退縮0m.csv`／`W-D.1.2 診斷_退縮3.5m.csv`／**`v3/W-D.1.2 診斷_退縮0m.csv`／`v3/W-D.1.2 診斷_退縮3.5m.csv`**／`W-D.1.3-d 驗收_退縮3.5m.csv` |
| **乙** | `verify/run_verification.py` 之 `diff_rows` 呼叫點中**根於 `BASELINES`** 者（AST 實查·`32` 呼叫點中之 `5` 處·展開 `tag∈{0m,3.5m}`） | 同甲，惟以 **`W-D.1.3-a 診斷_退縮0m.csv`／`W-D.1.3-a 診斷_退縮3.5m.csv`** 取代甲之 `v3/W-D.1.2 診斷` 二檔 |

🔒 **二者皆恰為 `9`**，且**差集恰為 `2`**：
甲∖乙 ＝ `{v3/W-D.1.2 診斷_退縮0m.csv, v3/W-D.1.2 診斷_退縮3.5m.csv}`；
乙∖甲 ＝ `{W-D.1.3-a 診斷_退縮0m.csv, W-D.1.3-a 診斷_退縮3.5m.csv}`。

🛑 ⇒ **擇甲或擇乙皆係 `⛔ 自擬`** ⇒ 觸 `§四` 停機款逐字「`p3` 之『九份』查無明確清單（⛔ 自擬）」。

🔒 **併呈（供發單側裁之材料·⛔ 由 CC 擇定）**
- `verify/baselines/` 追蹤檔 ＝ `298`；**頂層 ＝ `13`**；`verify/baselines/v3/` ＝ **`17`**（與單所載 `17` **相符** ✅）。
- `diff_rows` 呼叫點**全體 ＝ `32` 處**，展開後 **`60` 檔**（含 `V3RUN`／`F0DIR`–`F4DIR`）
  ⇒ 🔴 **`WV_BAKE` 之重烤機制所覆蓋者為此 `60`，⛔ 僅九份**。
- **重烤程序確已在倉**（⛔ 須另寫）：`verify/run_verification.py:417`–`424` 逐字
  「`🆕 重烤機制（env WV_BAKE=<dir>；未設＝inert，零行為變更）` … `正式烤 WV_BAKE=BASELINES 本身（原地覆寫）`」。
  ⇒ 🔴 **但其粒度為「全部 `diff_rows` 之 baseline」，⛔ 可只烤九份** ⇒ 「九份」與「重烤機制之射程」**⛔ 對齊**。

🩸 **併記（一則已過期之記載·⛔ 據以停機·僅登記）**：
`docs/reports/W-G.9-122_CC交接文.md:151`–`:155` 之 `§七`「🛑 待 KL 之三件事」表，其欄名逐字為
「**為何非 CC 可代**」，第 `2` 列 ＝ 「**九份 baseline 之重產** ｜ 重產係 **KL 於 UI 之動作**；且須先定圖資（`§五`）」。
🔒 **其二前提今已各自消滅**：`(i)` 圖資已由 `K-9-20`（KL 裁 `2026-08-24`）定為 `V6_1.dxf` 唯一；
`(ii)` `WV_BAKE` 機制已入倉 ⇒ 重產**⛔ 再必然是 UI 動作**。
⇒ 本窗**⛔ 以該列為停機理由**；其停機專據理由甲與理由乙。

### 五-3　🛑 本窗**⛔ 辦**之事（逐項具名）

⛔ 動 `verify/run_verification.py` 一字；⛔ 跑 `WV_BAKE`；⛔ 覆寫任何 `verify/baselines/**`；
⛔ 自擬「九份」之所指；⛔ 就「甲抑或乙」作成任何裁；⛔ 辦 `Q-5`／`Q-6`；
⛔ 就 `GB-67` 是否結案作成任何主張（其屬本體階）。

---

## §六　🩸 本批之自捕（照實·⛔ 隱去·各載 形／徵候／攔法）

### 自捕 `1`　首版 patch 以文字層讀寫 ⇒ 一檔 `LF`→`CRLF`（🔴 **成因未解**）

- **形**：`p2` 首版以 `io.open(..., encoding="utf-8", newline="")` 讀寫四支；三支保 `LF`，
  **`verify/tools/wg960_z_selfcheck.py` 獨獨全檔轉 `CRLF`**（`numstat` ＝ **`251 251`**·`Δbytes` `+308` ＝ `251` CR ＋ 行長差）。
- **徵候**：`git diff --numstat` 之「每列皆變」——**⛔ 報錯**，`rc` 為 `0`，diff 之**可見內容完全相同**。
- **攔法（已生效）**：`numstat` **逐檔須為 `1 1`** 之閘 ＋ **`CR` 改前→改後**之逐檔斷言。
  已 `git checkout --` 還原，改以**二進位式**（`open(p,"rb")`／`replace` bytes／`open(p,"wb")`）重做
  ⇒ 四支皆 `1 1`、`CR 0→0`、回讀逐位相符。
- 🔴 **成因未解·⛔ 臆造**：該版**逐字**為 `newline=""`（讀寫皆是·已現查其源碼列），
  且**隔離之 round-trip 測試（同一讀寫式、同四檔）⛔ 復現**（四檔皆「🟢 逐位相同」，
  人造 `CRLF` 檔亦保留 ⇒ 該測非恆綠）。**⇒ 照實登記為「已攔下、成因未明」。**
- 🔒 **同族之<u>已知</u>機制（⛔ 混同）**：`wg960_z_selfcheck.py:250` 自身之寫檔式
  `io.open(OUT, "w", encoding="utf-8")` 係 **`newline=None`** ⇒ 其**每次重跑必產生 `CRLF`**。
  此為**該器之性質**，與上開自捕**⛔ 同一事**（本窗之 patch 未經該器）。

### 自捕 `2`　`GB` 取號之單框**三式皆偽**

- **形**：`S-5` 首以 `GB` 之「標題錨定框」單式取號，得相異 `75`（另試寬式 `85`、H2 式 `3`）——**皆 ≠ 單所載 `159`**。
- **徵候**：`MAX` **`161` 相符**而**相異／缺號集不符** ⇒ 若只報 `MAX`（取號所需），**此偽永不現形**。
- **攔法**：`W-G.9-205` 更正註之「`GB` 取號須同格載明所用之框」＋ **單所載三數之對拍**。
  正解 ＝ **標題形 ∪ 表列形**（`GB` 簿與自誤簿同病：**二形並存**）⇒ `159`／`161`／`[12, 87]` **三數全符**。

### 自捕 `3`　抽「九份」時之**錨非唯一**

- **形**：以「含 `G-6` ⋀ `行動清單`」為錨抽 `W-G.9-122` 之清單，**命中 `2` 列**（`:87`／`:146`）。
- **徵候**：若未設唯一性斷言，將取到**錯的段**（`:146` 屬 `§七` 之另一段）。
- **攔法**：抽取器內置 `assert len(idx) == 1`，**當場 `AssertionError` 停**，繼而人工判讀二列。
  （此即 `行號衛生` 第 `4` 款「唯一性須附判別力自檢」於**抽取器**上之施行。）

### 自捕 `4`　`p1` 之「`0` 宗」**險些以單一器出艙**

- **形**：新錨態之 `A` ＝ `0` 宗，而**該器自身之負對照同時為紅**。
- **徵候**：`0` 之二讀法（受詞歸零／量測器塌陷）**⛔ 得由該器自身分辨**；
  且「`0` 宗」恰與「錨點更正應改善配地」之**期待一致** ⇒ 依 `A-0` 之理路，**最該被懷疑**。
- **攔法**：「量測器非紅之自證」款 ⇒ **⛔ 出艙**，改以**獨立第二器**（`run_verification`）取外部錨
  ⇒ 六街廓全可達 ⋀ 二結構閘 `✅ PASS` ⋀ `圍堵閘` 命中 `0` ⇒ `(乙)` 被否證、`(甲)` 成立。

### 自捕 `5`　證據落檔**未入倉**

- **形**：`p1` 所據之 `verify/out/probe_WG991_yishim_818a95b.log` 產出後為 **untracked**
  （`.gitignore` **⛔ 涵蓋**之——其只忽略 `verify/out/got_*` 與 `verify/out/W-D.4_*`）。
- **徵候**：`git status --porcelain` **⛔ 空**；若逕報「porcelain 乾淨」即為偽。
- **攔法**：自限復驗之 `porcelain` 須空 ⇒ 當場現形。已依「凡引一數，其來源須為**已入倉**之物」
  **逐檔** `git add`（⛔ `git add -A`／`.`·`verify/out/` 之禁款）並入倉，`S-4` 之落檔同辦。

---

## §七　本批之自解清單（`常規五` 自解款 款 `五`）

| # | 疑義（逐字） | 二個以上之讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§零` 期初態 `2a3fe88` vs `ls-remote` 實得 `3eb65f6` | `(a)` 以單所載為準 `(b)` 以實查為準 | **`(b)`** | `3eb65f6` 係 `2a3fe88` 之後裔且為**零生產碼** commit；`S-3` 之 `app.py` 三值與單所載**逐位相同** | `(a)` 與 `S-2` 之明文（⛔ 採信本地追蹤 ref、一律 `ls-remote`）相斥 |
| `2` | `p1` 之「二情境分列」vs 既有器 `SB = 0.0`（逐字「⛔ 不擴」） | `(a)` 擴該器至 `3.5m` `(b)` 守 `0m` 並具名 | **`(b)`**（保守項·`常規二`） | 器之 `:76` 逐字；`GB-67` 病一之受詞本即 `0m`（`CLAUDE.md` 鏈一） | `(a)` 即「改其受量欄位」／「另寫第二份定義」，為 `p1` 器款與 `p2` 款 `1` 所禁 |
| `3` | `p2` 母體：單載 `19` vs 實測 `21`（正典框）／`23`（擴框） | `(a)` `19` `(b)` `21` `(c)` `23` | **`(c)`** | 單逐字「以實測為準」⋀「⛔ 以任何較小之集為限」；差之逐項歸因已具名（`§三-1`） | `(a)` 與實測相斥；`(b)` 較 `(c)` 為小 |
| `4` | `S-5` `GB` 之框 | `(a)` 標題形 `(b)` 表列形 `(c)` 聯集 | **`(c)`** | 僅 `(c)` 使三數（`159`／`161`／`[12,87]`）**全符**單所載 | `(a)`／`(b)` 單用皆與單所載相異 |

🔒 **四項皆合自解款一〜五**：零土地後果（`1`/`3`/`4` 係框與態之別；`2` 係**縮**射程而非擴）／
零生產碼／可機驗且已跑否證對照／可逆（append-only 或新建檔）／已具名出艙。
🛑 **`p3` ⛔ 自解**——其落入黑名單（須動生產碼一字 ⋀ 須新訂一條準據）⇒ **恆停機**。

---

## §八　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | **空** ✅（`verify/out/got_*` 係 `.gitignore` 刻意忽略之族·⛔ 列為缺失） |
| `left-right`（期末 vs 期初 `3eb65f6`） | `3 0` ⇒ 本窗新增 `3` 個 commit·**⛔ 分岔** |
| 生產碼 `34` 檔 blob 期初 ＝ 期末 | **相異 `0`／`34`** ✅（判別力見 `§四 Q-1`） |
| 四簿 MAX 期初 ＝ 期末 | 自誤 `352`／`GB` `161`／`VR` `95`／`K-9` `29` —— **本批⛔ 鑄任何號**（單 `§五`：本階得鑄者 ＝ 無） |
| `docs/orders/W-G.9-269_…md` blob | `sha256` `e040c1d4…`／`sha1` `ed07dc6a…` —— 與來源**逐位相同** ✅ |

---

## §九　🛒 本報告**⛔ 載**

病一之任何修法主張（`K-9-23` 閘一轉主動／`K-9-17` 遞補迴圈）——其屬**本體階**，俟發單側之補令。

---

## §十　呈發單側（`p3` 之停機 ＋ 二待裁）

1. 🛑 **`p3` 停機**（`§五`）——請裁：`(i)` 「九份」取**甲**（`W-G.9-122` `G-6` 清單）抑或**乙**（`BASELINES` 根之 `diff_rows` 呼叫點），
   抑或**其三**（`WV_BAKE` 之全射程 `60` 檔）；`(ii)` `_DECLARED_EXTRAS` 之 `指數名次` 移除係生產碼變更
   ⇒ 須另開**生產碼批**並經 KL 逐 `commit` 放行。
2. 🔴 **`p1` 之結果對本體階之影響**（⛔ 由 CC 判）：新錨態之 `A` ＝ **空集**
   ⇒ 本體階（`K-9-23` 閘一轉主動 ＋ `K-9-17` 遞補迴圈）之**受詞於現態下為空**。
   其規格繫於此，請發單側於補令中明示。
3. 📋 **登記（⛔ 本批處置）**：`probe_WG991_yishim.py` 之負對照寫死 `R2 45.9766`／`R5 56.3293`
   係**舊錨態期望值**，於新錨態下**恆紅** ⇒ 該器自此**每跑必自判「構造存疑·停機」**。
   本窗**⛔ 改其一字**；其處置候發單側。
