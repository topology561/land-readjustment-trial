# W-G.4 裁定 M · **P-G — 容量拆解重跑**

> 基準 `a7a9aa7`。log：`verify/out/M_0725_PG_capdecomp.log`（`WV_CAPDECOMP=1`）。
> BEFORE：`verify/out/M_before_capdecomp.log`（plan v4 §〇.5 之 `2a7ea7d`）。
> **措辭鐵律（plan v4 〇.1）**：forced 街角解鎖 → **面積回歸中央調配池**（抵費地總量不變·位置由街角移回中間）。

---

## 〇、一句話

**「9 戶差 2 戶」消失**：3.5m 可容筆數上界 **7 → 9**（**完全符合預測閘 d**），
且**不再只是上界**——本次 `_feas` **真窮舉為「可行」並輸出建構指派**，即 W-7 所要求之可行性證明。
R3 右端 forced 鎖定 **308.93 → 0.00**、R2 左／R5 左**維持 forced**，與驗收靶逐項相符。

---

## 一、3.5m 逐塊拆解（BEFORE → AFTER）

| 塊 | pool | 碎片Σ | **forced鎖Σ** | reachable | 容量 | **可容上界** |
|---|---|---|---|---|---|---|
| R1 | 125.00 → **387.01** | 5.33 → 5.33 | 0 → 0 | 119.67 → **381.68** | 4.78 → **266.79** | **0 → 2** |
| R2 | 908.74 → 914.27 | 0 | **309.05 → 309.05** | 599.69 → 605.22 | 446.54 → 452.07 | 2 → 2 |
| R3 | 743.06 → 447.53 | 0 | **308.93 → 0.00** | 434.13 → **447.53** | 281.89 → **295.29** | 1 → 1 |
| R4 | 651.27 → 651.27 | 0 | 0 | 651.27 | 537.70 | 4 → 4 |
| R5 | 463.15 → 463.13 | 0 | **300.52 → 300.52** | 162.63 → 162.61 | 5.49 → 5.47 | 0 → 0 |
| R6 | 267.42 → 267.42 | 85.71 | 0 | 181.71 | 26.15 | 0 → 0 |
| **和** | | | **918.50 → 609.57** | | | **7 → 9** |

`forced 角落鎖定片明細` AFTER **只剩兩片**（R5 左 300.52／R2 左 309.05）——**R3 右不在列**。

---

## 二、驗收靶逐項對拍（plan v4 §八 P-G·reviewer 更正版）

| 靶 | 預期 | 實測 | |
|---|---|---|---|
| 實測可行域 **≤ R3 右一端** | 僅 R3 右可解鎖 | R3 右 forced 鎖 `308.93 → 0.00`；R2左/R5左 不動 | ✅ |
| 街角補足 **308.93 ＋ 餘額** | 門檻 308.93 恰達＋餘額回街角 | `got_M5合併帳_退縮3.5m.csv`：門檻 **308.93**·①補足 a′ **179.32**·③餘額 **90.68**（合 **270.00**） | ✅ |
| R2 左／R5 左 **維持 forced（池∅）** | 二端池 ∅ ⇒ 不可救 | 二片 forced 明細原值不動 | ✅ |
| **面積回歸中央調配池後之可及容量** | R3 上升 | R3 `reachable 434.13 → 447.53`（+13.40）、`容量 281.89 → 295.29`（+13.40）；**全區上界和 7→9**（增量實出於 **R1 `0→2`**·非 R3） | ⚠️ 數值符合方向，**成因不可單一歸因**（見下框） |
| 「9 戶差 2 戶」是否消失 | 消失 | 上界 **7 → 9**；需求 9 → **6** ⇒ 由「短 2」轉為「**餘 3**」 | ✅ |

> **⚠️ R3 pool 下降（743.06 → 447.53）而 reachable 上升（+13.40）——不作機制宣稱**
>
> 帳面恆等式：`reachable = pool − 碎片 − forced鎖`。
> BEFORE `743.06 − 0 − 308.93 = 434.13`；AFTER `447.53 − 0 − 0 = 447.53`。
> ⇒ forced 鎖之 **308.93 自 `unreach` 歸零**，同時 **pool 亦減少 308.93**（該面積改由地主宗
> `628-45(2)` 以其分配 G 承接）⇒ **二者於 `reachable` 上相抵**；
> 實得之 **+13.40 係其餘池變動所致、非解鎖本身**。
>
> **不宣稱**「解鎖直接使 R3 可及容量上升 13.40」——本探針之 `pool` 取自 **trunk E**（E1/E2/E3 全部
> 落位後），非 trunk A；逐塊 pool 差（如 R1 `125.00→387.01`、R2 `+5.53`、R5 `−0.02`）係
> **S1 幾何 ＋ M-5 ＋ E1/E2 重新指派**之合成結果，**無法以簡單算術拆到單一成因**
> （同 BK-4 之不可分離性）。本節只據實載讀數。
>
> **可確認者**：①R3 forced 鎖 `308.93 → 0.00`（解鎖成立）；②上界和 `7 → 9`；
> ③抵費地總量之全區守恆由 `F.4 結構永久閘`／守恆閘負責，**不在本探針射程內**。

---

## 三、預測閘 d（§七）之對拍——**部分符合·據實載**

| 預測（canonical §七.3·**不改**） | 實測 | 判 |
|---|---|---|
| 上界 **7→9** | **7→9** | ✅ **逐字符合** |
| （vs **9**）＝需求群 9 | 需求群 **6** | ⚠️ **不符** |
| 必標「上界·未窮舉·非可行性證明」 | 本次**已可真窮舉**（見 §四）⇒ 該但書之前提消失 | 條件轉變 |
| 可行性另以建構指派／真窮舉證 | **已達成**（§四） | ✅ |

### 3.1 「需求群 9→6」之查因（S-9 要求·**不改預測遷就實測**）

- BEFORE 之 9 係於 **`2a7ea7d`** 取得，而該態 **`run_all` 42 PASS／22 FAIL、F.0 六格錨破**
  （plan v4 §〇.5 自載）⇒ **BEFORE 讀數本身係在 trunk A 錨破之態下產生**。
- 消失之三員＝**G003／G016／G025**。三者於現行 baked 輸出中**皆落在 `E1·7-4`**
  （`74·G003@R1/@R2/@R5`／`74·G016@R1/@R2/@R5`／`74·G025@R1/@R3`）⇒ **由 E1 完成規配、未進 E2**。
- 且**倉內 baseline（舊烤）之 `F.4_公設調配_退縮3.5m` 亦已將該三員列於 `E1·7-4`**
  ⇒ 三者進 E2 並非常態，**BEFORE 之 9 才是異常態之產物**。

**⇒ 結論**：「9→6」**不歸因於 M-5**，而是 **BEFORE 讀數取自 F.0 錨破態**之偽基準。
**預測未改**；此差異記為「**基準不可比**」，非機制變化。
（**不宣稱** M-5 使 3 戶消失——該說法無據。）

---

## 四、W-7 誠實註：本次**已非「僅上界」**

| 項 | BEFORE | AFTER |
|---|---|---|
| `_feas` 窮舉判決 | `space 559872>300000·**未窮舉**` | **可行** |
| 建構指派 | 無 | `[('G014','R1'), ('G033','R1'), ('G020','R3'), ('G018','R4'), ('G004','R4'), ('G015','R2')]` |

- BEFORE 之「上界 7」**不是**不可行性證明，AFTER 之「上界 9」**也不是**可行性證明——
  真正的可行性證明是上表右欄之**建構指派**（`_feas` 之 `itertools.product` 窮舉命中）。
- **為何本次可窮舉**：`_feas` 之上限 `cap_space=300000`；搜尋空間 ∝ 各戶候選塊數之積，
  需求群由 9 降為 6 使空間降至上限內 ⇒ **窮舉得以真正執行**。
  ⚠️ 故「可窮舉」本身亦繫於 §3.1 之基準差異，**非**「演算法變強」。

### 4.1 需求群成員差（並列·plan v4 §八 要求）

| | 成員 |
|---|---|
| BEFORE（9） | `G003, G004, G014, G015, G016, G018, G020, G025, G033` |
| AFTER（6） | `G004, G014, G015, G018, G020, G033` |
| 差（3） | `G003, G016, G025` ⇒ 皆改由 **E1·7-4** 規配（§3.1） |

---

## 五、`W-F F.4` 殘紅之誠實定位

| 問 | 答 |
|---|---|
| 「E2 上界 7 < 需求 9」之死因是否已解？ | **已解**。本次 capdecomp（`WV_BAKE` 使 F.0 不 raise）下 `_e2_optimal` **跑完並輸出 dump**，`_feas` 判**可行**，F.4 六表 CSV 全數產出 |
| 那為何 `run_all` 之 `W-F F.4` 仍紅？ | **成因已換**：現行紅係 **F.0 raise（GSA 錨 R-2/R-3 待 P-H）之存在性守衛級聯**——`F.3 未成功，F.4 跳過`。**非** E2 不可行 |
| ⇒ 定位 | `W-F F.0~F.4` 五列紅＝**單一上游成因（F.0 錨待重錨）**，不再含「E2 結構性不可行」。該項自 `wg4-628-37-3.5m-pinpoint` 起掛之「待域裁」**可撤**（實測已可行） |

---

## 六、措辭鐵律自檢（plan v4 〇.1）

**活碼**：`rg -n '釋回|減少抵費地' --glob '*.py' .` ⇒ **僅 1 命中**，且為 `m_rescue.py` 之**禁令條文本身**
（`**禁「釋回／減少抵費地」語。**`）⇒ **實際用例 0**。

本次一併改掉之**活碼輸出**（原會真的印出禁語）：

| 檔 | 原 | 改 |
|---|---|---|
| `probe_corner_trueG.py`（2 處 f-string 輸出＋2 處 docstring） | `｜**釋回={release:.2f}㎡**`／`｜釋回 {rel:.2f}㎡` | `｜**回歸中央調配池={…}㎡**`／`｜回歸中央調配池 {…}㎡` |
| `wf_f2.py` docstring | 「前移釋回源塊中央池」 | 「前移·面積回歸源塊中央調配池」 |

**文件**：全倉 `.md` 尚有 14 命中——拆解為 **3 條規則條文本身**（交接文／F1-F5 報告／重錨登記之「禁…語」）
＋ **11 處歷史紀錄**（`plan_v1_reviewer裁決`／`T1_真G探針結果`／`停機上呈_救援池實測`／`盤點表`）。
後者 **plan v4 §〇.1 已明文宣告「全作廢」**，且屬**已發生之紀錄**——
**不改寫歷史**（改寫即偽造紀錄）。

**本報告自身之自檢（誠實載·非宣稱 0）**：`rg -n '釋回|減少抵費地'` 於本檔 **4 命中**，
**全部落在本節 §六**——即「**引述禁語以說明其已被移除**」（2 處為禁令條文引用、2 處為
改前字串之對照）。**用例 0**。
> 若要求字面 0 命中，則無法在報告內展示「改了什麼」；本檔採「**用例 0 ＋ 引述可見**」，
> 與 `重錨登記`／`CC交接文` 之處理一致（該二檔亦各含 1 處禁令條文引用）。

---

## 七、🔴 探針自身之 bug（本次實測撞到·已修）

`probe_capacity_decomp.py` 於跑完還原 `verify/wf_f4.py` 時拋
`[Errno 22] Invalid argument`，**卻仍 `exit 0`** ⇒ 引擎檔留在**被注入狀態**而退出碼報成功
（實測：跑後 `git status` 顯示 `verify/wf_f4.py` +142 行）。**最危險之組合**：下一次跑會吃到變異引擎。

已修三點：① 還原**重試 3 次**；② 寫回後**逐 byte 複驗**（禁「寫了就算」）；③ 失敗即 **`return 3`**（禁 exit 0）。
本次殘留已以 `git checkout -- verify/wf_f4.py` 復原並複驗：**0 探針標記·py_compile 綠·與 HEAD 一致**。

---

## 八、0m 對照（無 forced 端·作陰性對照）

需求群 6（BEFORE/AFTER 同）；`forced鎖Σ` 全塊 **0.00**（§3 對數 `0.00㎡`）；
上界和 13 vs 需求 6·**窮舉可行**。⇒ 0m 無 forced 端 ⇒ M-5 不觸發，**與 BK-4 裁定 (1) 一致**。

---

## 九、次步

P-G 完。次為 **P-H**——惟依重錨登記 §五鐵則，**執行重烤前**須先有
`W-G.4_裁定M_PH前置_f4逐格歸因表.md` §三之 **R3 0m 域旗標**之 KL 裁示。
