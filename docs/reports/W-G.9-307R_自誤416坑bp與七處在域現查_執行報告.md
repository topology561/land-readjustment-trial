# `W-G.9-307R`　施工單（**中**）執行報告：`自誤 416`・坑 `bp` 之鑄 ／ `GB-170` 末端追加 ／ **七處之在域現查** ／ **改前之釘紅與逐宗基線**

> **態**｜開工 ＝ `da9ca62eb2d007d13c08eaa9a066031228932e8a`（`ls-remote` 實查·全 `40` 碼）。
> **單** ＝ `docs/orders/W-G.9-307_施工單_自誤416坑bp與七處在域現查及改前釘紅.md`（`§二` 工項零已原封入倉）。
> **分級** ＝ **中**（登記 ＋ 靜態現查 ＋ `run_verification` 全量一次 ＋ `probe_WG9287_landeffect.py` 一次·**零生產碼**·⛔ 開分支·⛔ 修批）。
> 🛑 **本批鑄號 ＝ `自誤 416` ＋ 坑 `bp`**（**二號**）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔴 **KL 之放行於本批仍未動用**；⛔ 改生產碼 `34` 檔一字。
> 🔒 凡出艙之數，同格載其**框、母體與產生指令**（`VR-091` 三軸）。

---

## `§二`　工項零：**本單原封入倉**（主線·🔑 先於開工閘）

| 受詞（產生指令逐字） | 實得 | 期／判準 | 判 |
|---|---|---|---|
| 落點 | `docs/orders/W-G.9-307_施工單_自誤416坑bp與七處在域現查及改前釘紅.md` | 同左 | ✅ |
| `P-5` `SELF_SHA256`（受詞 ＝ 自檔首至**最末**一個 `SELF_SHA256` 列之前一個 `\n`（含）） | 受詞 **`40645`** B；實算 ＝ `eddd84f7db52caced32103bce1bd9a3fe707eea65dbf2ada00a4dfe6ad955acb` | 與單載**逐位相符** | ✅ |
| [必綠造] 原檔 | 相符 ＝ `True` | `True` | ✅ |
| [必紅造] 改一位元組之副本（**該副本⛔ 入倉**·落於 scratchpad） | 相符 ＝ `False`（實算 `31968000c97eadd2…`） | `False` | ✅ |
| `git rev-parse HEAD:<單>`（框 ＝ blob `sha1`） | `df8abac91d0c70401bad88151de7402b1d1d2430` | —— | ✅ |
| **二框並報**：blob 內容 `sha256` ＝ 磁碟 ＝ 源 | 三者皆 `7edfa9cc7f866e51d48051f3ce7139afb5a090ebc54a23361597ddc51bc0d75b`（各 `40723` B）⇒ `blob == disk == src` ＝ `True` | 逐位相同 | ✅ |
| `CR`（**讀 blob 取 bytes**） | **`0`** | `0` | ✅ |
| [必非零造] `data/V6.dxf` 之 blob `CR` | **`12308`** | `> 0` | ✅ |
| `git show --numstat` ⇒ `deletions` | **`0`**（`367` 增／`0` 刪） | `0` | ✅ |
| 檔數 ＝ `1` 之自證（`git ls-tree -r --name-only -z HEAD docs/orders/`·取 bytes·母體 `180` 檔） | 前綴 `W-G.9-307_` 命中 **`1`** | `1` | ✅ |
| [造甲·必 ≥ 1] 前綴 `W-G.9-306_` | **`1`** | `≥ 1` | ✅ |
| [造乙·必為 0] **執行期組出**之人造前綴（字面⛔ 出艙） | **`0`** | `0` | ✅ |
| 生產碼判法（`git diff --name-only da9ca62e..HEAD` ⋂ 四檔清單） | **`0`** 行 | `0` | ✅ |
| 推前驗快轉 `git merge-base --is-ancestor origin/wip/s1-endpart HEAD` | `rc = 0` | `0` | ✅ |

🔒 **落地後即 `push`**（`常規一 🔧 一`：零生產碼 commit 逕行 `push`）⇒ `da9ca62..247b1a1`，其後方跑 `§一`。

---

## `§一`　開工閘（**十四項**·每列同格載其產生指令·跑於 `§二` 之後）

🔒 **逐閘執行次數 ＝ `1`**（閘 `6'` 見其註）。

| # | 受詞（產生指令逐字） | 實得 | 期／判準 | 判 |
|---|---|---|---|---|
| `0` | `python -c "import sys; print(sys.version)"` | `3.13.11 \| packaged by Anaconda, Inc. \| (main, Dec 10 2025, 21:21:58) [MSC v.1929 64 bit (AMD64)]`；`rc = 0` | `3.13.11` 起首 | ✅ |
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `247b1a1d22b80655edf6b9376d7833d4ecef0b9b`；`rc = 0` | `da9ca62e…` **或其後裔** | ✅ |
| `1`證 | `git merge-base --is-ancestor da9ca62e… HEAD` | `rc = 0` ⇒ 確為後裔 | `rc = 0` | ✅ |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1`；`rc = 0` | 同左 | ✅ |
| `3`存 | `git cat-file -t 05a11bd665f6c2790ff821c9e3a1606651e345d4` | `commit`；`rc = 0` | 存在 | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd6… HEAD` | `rc = 1` | **非**祖先 | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | 🔴 機械停機款 **`0`**；`P-5` ✅ 相符（受詞 `40645` B）；`P-3` **不紅**（`def main` 區間 ＝ `14975`-`24579`）；🟡 **`2`** 項；`rc = 0` | `P-5` 相符·`P-3` 不紅 | ✅ |
| `4'` | 受詞自證（坑 `bf`·以器所印之檔名列） | `【order pre-flight】W-G.9-307_…md（40723 B／367 列）` ⇒ 含 `W-G.9-307` ＝ `True`[必真]／含 `W-G.9-306` ＝ `False`[必偽] | 同左 | ✅ |
| `5` | `… --selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`／`rc = 0` | 皆 `0` | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（🔒 **器之輸出為正典**） | 自誤 **`405`**／**`415`**（缺 `[106, 355, 356, 357]`）｜`GB` `168`／`170`（缺 `[12, 87]`）｜`VR` `80`／`95`（缺 `[73, 75]`）｜`K-9` `29`／`30`（缺 `[]`）；`rc = 0` | 同左 | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py 247b1a1…`（🔒 `rev` **釘死**·**全量落檔** `open(p,'wb')`·⛔ `\| tail -N`） | 數字系 **`26`** ＋ 字母系 **`67`** ⇒ **`93`／`93`**·末 **`bo`** ⇒ 下一 **`bp`**；母體 ＝ `247b1a1` 之追蹤 `.md` **`824`** 檔；`rc = 0` | `93`／`93`·末 `bo` | ✅ |
| `6'`造 | 器自印判別力二造 | 造甲[必有定義] 坑 `n` ⇒ 定義處 **`6`**（≥ `1`）✅／造乙[必為零] 人造之坑（**執行期組出**·字面⛔ 出艙）⇒ 定義 `0`／引用 `0` ✅ ⇒ **器非紅** | 二造如預期 | ✅ |
| `7` | `git ls-tree HEAD --name-only -z verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**） | 頂層條目 `42` ⇒ ⋂ `*.py` ＝ `33` ＋ `app.py` ⇒ **`34`** | `34` | ✅ |
| `7`造 | 判別力[必不命中]：子層 `*.py` 檔數 | **`325`** > `0`（🔒 單載「發單側於 `da9ca62e` 得 `325`」·**逐批漂移**·⛔ 定值） | `> 0` | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`；`… --name-only \| wc -l` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 **`298`** | 同左 | ✅ |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | 該族 **`20`** 支；`verify/W-G.9-299-gb170` 命中 **`0`**（`rc = 1`·**⛔ 開**）；判別力[必存在] `verify/W-G.9-269-p3a` 命中 **`1`**（`rc = 0`） | 同左 | ✅ |
| `10` | `git ls-tree -r --name-only -z HEAD docs/orders/` ⋂ 五前綴（**取 bytes**·坑 `bk`） | `303_` **`7`**／`304_` **`1`**／`305_` **`1`**／`306_` **`1`**／`307_` **`1`** ⇒ **`11`**·**三態分列：態1 `11`／態2 `0`／態3 `0`** | `11` 皆態1 | ✅ |
| `10`造 | 判別力[必為零]：**執行期組出**之人造路徑（字面⛔ 出艙） | `git cat-file -e` ⇒ `rc = 128`（≠ `0`） | `rc ≠ 0` | ✅ |
| `11` | 集合甲（交接註節 `10` 之相異 `.log` 名·**以程式自 blob 抽取**·⛔ 轉錄）∪ 集合乙（`verify/out/` ⋂ 四前綴 ⋂ `*.log`） | 甲 **`13`**／乙 **`9`**／甲之裸名解析不到者 **`0`** ⇒ **聯集 `22`**；**三態分列：態1 `22`／態2 `0`／態3 `0`**；🆕 對照物 `verify/out/WG9287R_landeffect.log` ⇒ 態1·期初 blob `sha1` `d306c91d88558de01033e60baf4acc1100f1145c` | `22`·逐位未動 | ✅ |

🔒 **閘 `11` 之期初 blob `sha1` 已逐檔存錄（`23` 筆·含對照物）**，供 `§七` 閘 `6` 之「逐位未動」比對。

### `§一-1`　`P-2`／`P-4` 之逐項處置（**⛔ 靜默略過**·**⛔ 目視**·先查該器之觸發式再定位其真受詞）

🛑 **🟡 提示⛔ 計入 `rc`**（器自印：`rc` ＝ 紅項數，僅 `P-3`／`P-5` 之機械款）。

**`[P-2] :35`　⇒ 受<u>具名豁免</u>（🔴 **其真受詞⛔ 單所預期者**·照實回報）**
- **器之觸發式逐字**（自 `verify/probes/probe_order_preflight.py` 當場取）：
  `if re.search(r"(命中|得數|得|計|共)\s*`?\d+`?\s*(處|列|次|筆|個|檔)", p): has_frame = ("框" in p) or ("grep" in p) or ("^" in p)`
- **真受詞** ＝ 「**段內含〈計數詞＋數＋量詞〉，而⛔ 含 `框`／`grep`／`^` 之字面**」。
- 🔴 **單 `§一` 項 `4` 預期其觸於 `裁 2` 之「生產碼 `34` 檔（正面列舉）」；<u>實測⛔ 然</u>**——
  該段（`§零-2` 之裁表·`:35`–`:46`）之**唯一**命中片段為 **`得 \`4\` 處`**，
  出自 **`裁 9`**（發單側本窗之自捕）之一列：
  「擬本批前以**文字層** `_corner_buffer_S\(` 檢呼叫端，**初得 `4` 處**；與 `W-G.9-300R` 之 AST 二形並取 `7` 處對照後攔下」。
  **照實回報，⛔ 頂替。**
- **豁免之由**：該格**同格已具名其框**——「**文字層** `_corner_buffer_S\(`」即其框本身（以**展示該樣式**具名），
  其對照者為「**AST 二形並取**」；且該 `4` 係**已被攔下之誤值**、⛔ 出艙之宣稱。
  器之 `has_frame` 只認 `框`／`grep`／`^` 三**字面** ⇒ 對「以展示樣式具名其框」者**必失配**。
- ⇒ **受具名豁免**；**⛔ 靜默略過**、⛔ 改單一字。

**`[P-4] :72`　⇒ 受<u>具名豁免</u>（類 (甲)）**
- **器之觸發式逐字**：`if ("→" in p) and re.search(r"逐字|轉引|引", p): if not re.search(r"座標系|方向|注入方向|來源.*[:：]|自.*抽出", p)`
- **命中段** ＝ `§一-1` 恆常附款之清單（`:72`–`:98`）；其**唯一** `→` 出自
  `機械順序：⛔ 預書 之款一律「佔位符 → 跑 → 填」` ⇒ **流程節序之串接**
  ⇒ 正合 `W-G.9-305 §一-1` 三類之 **(甲)**（逐字：「`→`／`⇒` 係**賦值鏈／消費鏈／查表鏈／流程節序**之串接（含**引述**他處之此類串接者）」）
  ⇒ **⛔ 幾何向量之方向性轉引 ⇒ 無座標系可具名** ⇒ **受豁免**。

---

## `§三`　工項一：**`自誤 416` ＋ 坑 `bp` 之鑄 ＋ `ALPHA_LABELS` 之擴**（**三段分列 `commit`**）

🔒 **「payload」＝ 界內之列去其首尾之空列**；追加之式 ＝ `old + b'\n' + payload + b'\n'`；**落檔 `open(p,'wb')`**。

| 段 | 界 | 唯一性（**整列逐位相等**·母體 ＝ 本單單檔） | **二數並報**（子字串框） | payload | 增量 |
|---|---|---|---|---|---|
| 段一 | `<!-- ZW416-BEGIN/END -->` | BEGIN **`1`**／END **`1`** | BEGIN `2`／END `2` | **`17`** 列／**`1466`** B／`sha256` `28444fb3c5a1a26e…` | `876560` → `878028`（**`1468`** ＝ `1+1466+1`） |
| 段二 | `<!-- PITBP-BEGIN/END -->` | BEGIN **`1`**／END **`1`** | BEGIN `2`／END `2` | **`15`** 列／**`1421`** B／`sha256` `8557fbbdafc02cc1…` | `878028` → `879451`（**`1423`** ＝ `1+1421+1`） |
| 段三 | —（`ALPHA_LABELS`） | — | — | **新增一列** | `301` → `302` 列 |

**段一／段二之 append-only 二造**：[造甲·必真] 嚴格前綴 ＝ `True`／[造乙·必偽] 反向 ＝ `False`；
`wg942_append_audit.py` ⇒ 受檢 **`77`** 筆／結論 **✅ 全部成立**／`rc = 0`（二段各跑一次）。
**`deletions`** ＝ `0`（段一 `18`／`0`；段二 `16`／`0`；段三 `1`／`0`）；新檔 `CR` ＝ `0`。

### 三-1　段三　`ALPHA_LABELS` 之擴（射程逐字·⛔ 逾之）

🔒 **射程** ＝ **僅** `verify/probes/probe_WG9269_pit_index.py` 內**新增一列**；**⛔ 改該檔任何既有列一字**。
🔒 **體例之取得**（⛔ 自創形）：以該檔**既有之單標籤列**為範本——
範本逐字 ＝ `ALPHA_LABELS += ["bm"]             # 🆕 \`W-G.9-304\` §四 所准之一坑`；其 `#` 之欄位（0-indexed）＝ **`35`**。
**新列逐字** ＝ `ALPHA_LABELS += ["bp"]             # 🆕 \`W-G.9-307\` §三 所准之一坑`（`#` 欄位 ＝ `35`·機驗相符）。

| 受詞 | 實得 | 判 |
|---|---|---|
| 既有 `+=` 列數 | **`5`** 條（其末條 ＝ `ALPHA_LABELS += ["bn", "bo"]       # 🆕 \`W-G.9-305\` §四 所准之二坑`） | 🔒 |
| **既有列逐列逐位未動**（機驗：`舊[:i+1] == 新[:i+1]` ⋀ `舊[i+1:] == 新[i+2:]`） | **`True`** | ✅ |
| `git show --numstat` | **`+1`／`-0`** | ✅（單 `§三` 明文期值） |
| `python -m py_compile` | `rc = 0` | ✅ |

### 三-2　落地後之機驗（🔒 **簿之數一律取器之輸出**）

| 受詞 | 實得 | 期 | 判 |
|---|---|---|---|
| `registry` ⇒ 自誤 | 相異 **`406`**／MAX **`416`**（缺 `[106, 355, 356, 357]`） | `406`／`416` | ✅ |
| 同上 ⇒ `GB`／`VR`／`K-9` | `168`／`170`·`80`／`95`·`29`／`30` | **一字未動** | ✅ |
| 坑（器之輸出·`rev` **釘死** `cdbee6c`·**全量落檔**） | 數字系 **`26`** ＋ 字母系 **`68`** ⇒ **`94`／`94`**·末 **`bp`**；母體 `.md` **`824`** 檔；`rc = 0`；器自印判別力二造 ✅（造甲 `6`／造乙 `0`／`0`）⇒ **器非紅** | `94`／`94`·末 `bp` | ✅ |
| 哨兵 `ZW416` 入簿命中 | **`0`**（[對照組·必非零] 本單內 **`5`**） | `0` | ✅ |
| 哨兵 `PITBP` 入簿命中 | **`0`**（[對照組·必非零] 本單內 **`5`**） | `0` | ✅ |

### 三-3　`416` 之命中（🔒 **`GB-158` 三形分列**·母體 ＝ 簿單檔 `10357` 列·**列框**）

| 形 | 逐字 | 命中 |
|---|---|---|
| **C 形**（🔒 自誤之標題形） | `` `自誤 416` `` | **`1`** ✅ |
| B 形 | `` 自誤 `416` `` | **`0`** |
| 鬆框 | `自誤 416` | **`1`** |
| 定義框（器之 `RE_ZW_DEF`·**自器碼當場取**）＝ `` ^#{1,6} [^\w`]{0,6}`?自誤 `?(\d+)`?`?\s*[｜（　:：] `` | — | **`1`** ✅ |

### 三-4　🔒 **三框交叉誤中**（**分列並報**·框自器碼當場取）

| payload | 自誤框 | 坑框（標題形 ∪ 表列形） | `K-9` 框 |
|---|---|---|---|
| `ZW416`（`17` 列） | **`1`**（＝ 本則自身之定義列） | **`0`** | **`0`** |
| `PITBP`（`15` 列） | **`0`** | **`1`**（＝ 本則自身之定義列） | **`0`** |
| [對照組·必非零] 全簿 | **`393`** | **`8`** | — |

### 三-5　🔒 坑 `bp` 之**定義處多於一處**（單 `§三` 之**預先受領**·⛔ 判紅·⛔ 逕修）

器之定義框所出艙者（逐字）：
`| \`bp\` | \`W-G.9-307_施工單_自誤416坑bp與七處在域現查及改前釘紅.md:1\`（D-H）＋3 | ✅✅✅ | \`10\` | ✅ 可解析 |`
⇒ **定義處 `≥ 2`**（本單自身含其定義標題，係單所預先受領者）；**形／徵候／攔法** 三者齊備（`✅✅✅`）；引用處 `10`。
🛑 **照實具名·⛔ 判紅、⛔ 逕修**（單 `§三` 明文）。

---

## `§四`　工項二：`GB-170` 之末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末追加）

🔒 本 payload **⛔ 鑄任何號**（`戒 36`）⇒ 其形**逐字保留 `> `**；🛑 **`§三` 入倉後方辦**（其 `commit` 序在段三之後）。

| 受詞 | 實得 | 期 | 判 |
|---|---|---|---|
| 唯一性（整列逐位相等·母體 ＝ 本單單檔） | BEGIN **`1`**／END **`1`**；**二數並報**：子字串框 `2`／`2` | 各 `1` | ✅ |
| payload | **`19`** 列／**`2282`** B／`sha256` `c94cdb36d9284315…` | — | 🔒 |
| 追加之式 | `916610` → **`918894`** B（增量 **`2284`** ＝ `1+2282+1`）；逐位驗 ＝ `True` | 相符 | ✅ |
| [append-only 二造] | 嚴格前綴 `True`／反向 `False` | 同左 | ✅ |
| `wg942_append_audit.py` | 受檢 `77` 筆／✅ 全部成立；`rc = 0` | `rc = 0` | ✅ |
| `deletions`／`CR` | **`0`**（`20` 增／`0` 刪）／**`0`** | `0` | ✅ |

### 四-1　🔒 **反向機驗**（**三形分列並報**·框**自器碼當場取**·母體 ＝ `GB` 簿單檔 **`8133`** 列·**列框**）

| 形 | 逐字 | 命中相異 | MAX |
|---|---|---|---|
| `RE_GB_F1` | `` ^#{2,4} [^\w`]{0,8}`?GB-(\d{1,3})`?(?![0-9]) `` | `93` | `170` |
| `RE_GB_F2` | `` ^\| *\*\*`?GB-(\d{1,3})`?\*\* `` | `131` | `137` |
| `RE_GB_F3` | `` ^#{1,6} .*?`?GB-(\d{1,3})`?(?![0-9]) `` | `95` | `170` |
| 🔒 **三形之聯集**（扣哨兵 `997`） | — | **`168`** | **`170`** |

🔒 **聯集之缺號 ＝ `[12, 87]`** ⇒ 相異 `168`／MAX `170`／缺號 `[12, 87]`——**三者皆一字未動** ✅。

| 判別力二造（**三形分列**） | `RE_GB_F1` | `RE_GB_F2` | `RE_GB_F3` | 判 |
|---|---|---|---|---|
| [必命中] `GB-170` 之既有定義列（**至少一形**須 `True`） | `True` | `False` | `True` | ✅ |
| [必為零] **執行期組出**之人造號（三形**皆**須 `False`） | `False` | `False` | `False` | ✅ |

### 四-2　`GB-132` 之避讓

| 受詞 | 實得 | 期 | 判 |
|---|---|---|---|
| payload 內 `^#` 起首**且**含 `GB-` 之列 | **`0`** | `0` | ✅ |
| [判別力·必非零] 同一偵測器掃 `GB` 簿全檔 | **`211`** | `> 0` ⇒ 器非紅 | ✅ |

---

## `§五`　工項三：**七處之在域現查**（**零生產碼**·**靜態**·⛔ 跑 harness）

🔒 量測器 ＝ `verify/probes/probe_WG9307R_sites.py`（新增）；落檔 ＝ `verify/out/WG9307R_sites.log`
（**`19794`** 列·`CR` `0`）；`rev` **釘死** ＝ `c4a8a95f31203884fa64d82433c06ca4ab883d14`；牆鐘 **`22.4`** 秒。
🔒 母體 ＝ **生產碼 `34` 檔**（**正面列舉**·外部錨 ＝ `§一` 閘 `7`）。

🔒 **`seg` 之自我驗證閘**（重建須自證 ＝ 現況·⛔ 以器綠代之）：
本器以自建之 `seg` 取代 `ast.get_source_segment`（效能故），其結果與後者**逐位對拍**（抽樣 `1/97`）
⇒ **相同 `837`／相異 `0`** ⇒ ✅。

### 五-1　款 `1`　呼叫端集 `S`（框之語法形**窮舉**）

🔒 **框**：`ast.Call` 之 `func` ＝ ①`Name(id=_corner_buffer_S)`／②`Subscript(slice=Constant('_corner_buffer_S'))`／
③`Attribute(attr=_corner_buffer_S)`／④`Name` 而其 `id` 由同模組／同函式之 `Assign` 綁至 ①②③ 之形（**別名一層**）。

**`S` 之基數 ＝ `7`**（期 `7`）；與 `W-G.9-300R` 之 `S1`〜`S7` **逐一對照全量出艙**（行號 ⛔ 作錨）：

| # | 檔 | 所在函式 qualname | 形 | `allocation_dir` 位逐字 | `side` 逐字 | `_label` 逐字 | 對位之 `W-G.9-300R` |
|---|---|---|---|---|---|---|---|
| `1` | `app.py` | `main` | ①Name | `allocation_dir_block` | `'left'` | `blk_label` | `S1` |
| `2` | `app.py` | `main` | ①Name | `allocation_dir_block` | `'right'` | `blk_label` | `S2` |
| `3` | `verify/stepg_pipeline.py` | `_run_step_g_impl` | ①Name | `allocation_dir_block` | `'left'` | `blk_label` | `S3` |
| `4` | `verify/stepg_pipeline.py` | `_run_step_g_impl` | ①Name | `allocation_dir_block` | `'right'` | `blk_label` | `S4` |
| `5` | `verify/wf_f1.py` | `compute` | ②Subscript | `alloc_dir` | `'left'` | `f"{lbl}·F.1"` | `S5` |
| `6` | `verify/wf_f4.py` | `_reshape_block` | ②Subscript | `alloc_dir` | `'left'` | `f"{blk}·E3"` | `S6` |
| `7` | `verify/wf_f4.py` | `_reshape_block` | ②Subscript | `alloc_dir` | `'right'` | `f"{blk}·E3"` | `S7` |

🔒 **逐一對照相符**（檔／所在函式／形／`side` 四欄逐格與 `W-G.9-300R` 三-1 之表相同；
`S6`／`S7` 之區辨依該報告所令**併載其 `side` 實參**）。

**🔒 殘餘之全量**（含字樣 `_corner_buffer_S` 而**⛔ 屬上列 `func`** 者·⛔ 靜默略過）＝ **`7`** 節點：

| 檔 | 節點型別 | 列（⛔ 作錨·僅出艙） | qualname |
|---|---|---|---|
| `app.py` | `FunctionDef` | `9332` | `_corner_buffer_S`（**其定義列**） |
| `app.py` | `Constant` | `14463` | `<module>` |
| `verify/stepg_pipeline.py` | `Name` | `389` | `_run_step_g_impl`（`_corner_buffer_S = ns[…]` 之**取用**） |
| `verify/stepg_pipeline.py` | `Constant` | `389` | `_run_step_g_impl` |
| `verify/wf_f1.py` | `Constant` | `216` | `compute` |
| `verify/wf_f4.py` | `Constant` | `1351` | `_reshape_block` |
| `verify/wf_f4.py` | `Constant` | `1362` | `_reshape_block` |

⚠️ **照實具名**：其中 `wf_f1:216`／`wf_f4:1351`／`wf_f4:1362` 之 `Constant` 係**②形之 `func`（`Subscript`）之 `slice` 子節點**
——其本身**⛔ 該 `func` 節點**，故依框之字面落入殘餘。**⛔ 判為漏**；列之以符「⛔ 靜默略過」。

### 五-2　款 `2`　逐處之在域事實（**全量 `7` 處**·每處一節·全文見落檔）

| # | `(a)` 外圍守衛（由內而外·逐字） | `(c)` 守衛名 ⇒ 字面鍵 | `(d1)` | `(d2)` | `(e1)` | `(e2)` 未綁定 |
|---|---|---|---|---|---|---|
| `1` | `_l_min is not None and _l_min != float('inf')` → `try`(`TypeError, ValueError`) → `_fo_left` → `_row_for_buffer and avg_depth_default > 0` → `_btn_clicked or _auto_recalc` | `_l_min` ⇒ `['【左】街角最小面積(㎡)']`；`_fo_left` ⇒ `['left_forced_offset']` | ✅ `_side_mid` ／ `_side_mid_left if _has_left_corner else None` | **後** | ✅ `_is_corner` | 🛑 `_is_corner` |
| `2` | 同 `1`，惟 `_r_min` ／ `_fo_right` | `_r_min` ⇒ `['【右】街角最小面積(㎡)']`；`_fo_right` ⇒ `['right_forced_offset']` | ✅ 同上 | **後** | ✅ `_is_corner` | 🛑 `_is_corner` |
| `3` | `_l_min is not None and _l_min != float('inf')` → `try` → `_fo_left` → `_row_for_buffer and avg_depth_default > 0` | `_l_min` ⇒ `['【左】街角最小面積(㎡)']`；`_fo_left` ⇒ `['left_forced_offset']` | ✅ 同上 | **後** | ✅ `_is_corner` | 🛑 `_is_corner` |
| `4` | 同 `3`，惟 `_r_min` ／ `_fo_right` | `_r_min` ⇒ `['【右】街角最小面積(㎡)']`；`_fo_right` ⇒ `['right_forced_offset']` | ✅ 同上 | **後** | ✅ `_is_corner` | 🛑 `_is_corner` |
| `5` | `fo.get("left_forced_offset")` | `fo` ⇒ `['forced']` | 🛑 **空** | （無受詞） | 🛑 **空** | （無） |
| `6` | `fo.get("left_forced_offset")` → `side == "left"` | `side` ⇒ `['s', 's']` | 🛑 **空** | （無受詞） | 🛑 **空** | （無） |
| `7` | `fo.get("right_forced_offset")` → `side == "left"` | `side` ⇒ `['s', 's']` | 🛑 **空** | （無受詞） | 🛑 **空** | （無） |

🔑 **`(d1)` 之鍵鏈（處 `1`〜`4`·逐字·止於「以字面鍵取自登記表之式」）**
> `_side_mid` ⇒ `_side_mid_left if _has_left_corner else None`
> ⇒ `_side_mid_left = _sl_left.get('mid')`（字面鍵 `mid`）
> ⇒ `_sl_left = _side_lines_blk.get('left') or {}`（字面鍵 `left`）
> ⇒ `_side_lines_blk = (ss.get('f3_cad_side_lines_by_side', {}) or {}).get(blk_label, {}) or {}`
>   （**字面鍵 `f3_cad_side_lines_by_side`**·基名 `ss` ＝ **形參** ⇒ **界外** ⇒ **止**）

🔒 **`(d2)` ＝ 「後」（處 `1`〜`4` 逐處）**——**以語句路徑判·⛔ 以行號判**：
`_side_mid_left` 之綁定語句路徑 **晚於** 強制呼叫之語句路徑；二者之間**重綁該名之語句 ＝ 空集**（loud 具名其為空）。

🛑 **`(d3)`／`(e3)` 之處置（處 `5`〜`7`）**：`(d1)`／`(e1)` **為空**，已 loud 具名；
其 `(e3)`（該函式內無 `_solve_G_one` 之呼叫 ⇒ 出艙讀取同一欄位名之處）**命中為空** ⇒ **loud 具名「⛔ 在域」**。
🔒 **依單 `§五-4` 明文：款 `2` 之任一「⛔ 在域」⛔ 停**——**逐處照實具名，裁屬發單側**。

🛑 **`(f)`**：**⛔ 重量**；逕引 `W-G.9-300R` 之 `F`（其 `三-2`（甲）之靜態遞移 `import` 閉包·基數 **`11`**；
本器⛔ 複算·其逐處具名見該報告）。

### 五-3　款 `3`　判別力造

| 造 | 實得 | 判 |
|---|---|---|
| [必為 `7`] 外部錨 ＝ `W-G.9-300R` 之「AST 之處」 | `S` 基數 **`7`** | ✅ |
| [必命中甲] `stepg_pipeline.py` 左支之 `(d1)` | 得名集含 `_side_mid_left` ＝ `True`；鍵鏈含字面 `f3_cad_side_lines_by_side` ＝ `True` | ✅ |
| [必命中乙] 同上之 `(d2)` | ＝ **`後`**（期 `後`） | ✅ |
| 🔑 **鍵鏈之判別力對照**（⛔ 令 [必命中甲] 恆真） | 該字面於逐處之命中 ＝ 處 `1`✅／`2`✅／`3`✅／`4`✅／`5`✗／`6`✗／`7`✗ ⇒ **`4` ／ `7`** | ✅ **非恆真** |
| [必為零] 以**執行期組出**之人造函式名重跑款 `1` 之框 | `S` 基數 **`0`** | ✅ |

🔒 **[必命中甲]／[必命中乙] 係發單側之預查**——二者**皆中** ⇒ ⛔ 判器紅、准續工項四。

### 五-4　停機款

> **停十一**（`S` 基數 ≠ `7`，或⛔ 與 `S1`〜`S7` 逐一對照）⇒ ✅ **不成就**。

🛑 **loud 之全量**：`逆溯深度逾 12 ⇒ 截斷` **×`7256`**。
🔒 **照實具名其性質**：單令「逆溯其名之**每一**綁定」⇒ 本器之逆溯係**廣度完備**；
於 `app.py :: main`／`_run_step_g_impl` 之規模下，多數分支於抵「登記表之式」前即觸**深度上限 `12`**
⇒ 該 `7256` 次截斷**係本量測之已知界限**，**⛔ 讀為「鏈不存在」**；
🔒 **受詞（處 `1`〜`4` 之 `(d1)` 鍵鏈）⛔ 受其影響**——其鏈**已抵**登記表之式（見 `五-2` 之逐字鏈）。

---

## `§六`　工項四：**改前之釘紅與逐宗基線**（**零生產碼**）

🔒 **授權三手段之實施**（射程逐字·⛔ 逾之）：
① 執行既有量測器 `verify/probes/probe_WG9287_landeffect.py` **一次**（**⛔ 動該器一字**·等待綁行程結束·全量落檔）；
② 新增 `verify/probes/probe_WG9307R_prepin.py`（**以 `probe_WG9306R_reach.py` 之包裝骨架為底本另存新檔**·
**⛔ 動該既有探針一字**），驅動 `run_verification` 全量**一次**（`rc` ＝ **`1`**·🔒 **既存之准紅碼**·⛔ 本批所致；牆鐘 **`202.3`** 秒）；
③ 行程內包裝 `ns` 之三鍵 ＋ `verify/stepg_pipeline.py` 之 `_run_step_g_impl`。

| 受詞 | 觸發數 | 判 |
|---|---|---|
| `ns['_corner_buffer_S']` | **`21`** | ✅ |
| `ns['_solve_G_one']` | **`2065`** | ✅ |
| `ns['_first_corner_alloc_dir']` | **`807`** | ✅ |
| `verify/stepg_pipeline.py` 之 `_run_step_g_impl`（**僅供計其進入序號**） | **`15`** | ✅ |

### 六-1　款 `1`　🔑 **釘紅**（KL 射程 `(b)` 三段辦法之 ①）

🔒 落檔 ＝ `verify/out/WG9307R_landeffect_pre.log`（`85712` B／`692` 列／`CR` `0`）；該器 `rc` ＝ **`0`**。
⚠️ **`CRLF`→`LF` 之正規化（照實具名）**：該既有器**⛔ 設** `reconfigure(newline="\n")`（坑 `bn`），
其 `stdout` 於本機經 Windows 之換行轉換 ⇒ 原捕獲 `86404` B／`CR` **`692`**；
本批**僅**施 `b"\r\n" → b"\n"`（差 `692` ＝ `CR` 數·**⛔ 改任何字元**·文字內容等同之機驗 ＝ `True`）
⇒ 落檔 `85712` B／`CR` `0`（符坑 `bn`）。🛑 **⛔ 動該器一字**。

| 項 | 實得 | 期／判準 | 判 |
|---|---|---|---|
| **格集**（抽取式逐字 ＝ `\[必非零\]\s*` + 反引號組 + `\s*之對稱差面積\s*＝\s*([0-9.eE+-]+)`·**同一式施於二檔**·⛔ 轉錄） | 參照 ＝ 本次 ＝ `['R2/left@3.5', 'R4/left@0.0', 'R4/left@3.5']` | 格集相同 | ✅ |
| `R4/left@0.0` | 參照 `13.055884224441684` ／ 本次 `13.055884224441684` | **字串逐字相同** | ✅ |
| `R2/left@3.5` | 參照 `38.06818855324997` ／ 本次 `38.06818855324997` | 同上 | ✅ |
| `R4/left@3.5` | 參照 `13.05518976183967` ／ 本次 `13.05518976183967` | 同上 | ✅ |
| **第二抽取式**（款 `4` 之 `對稱差` 列·**交叉對**） | 參照 ＝ 本次 ＝ `['13.055884224441684', '38.06818855324997', '13.05518976183967']` | 相同 | ✅ |
| 二式所得之值集相同 | `True` | — | ✅ |
| 該器之**自我驗證閘三重** | `(i)` `\|S1_par·sinθ − S1_perp\|` ＝ `0.0`（須 ≤ `1e-9`）✅／`(ii)` `is` 碼面之 `corner_range_polys[side]` ⇒ `True` ✅／`(iii)` 逐格**預測 ＝ 實際**（`22` 格全綠）✅ | 三重皆綠 | ✅ |
| 本次落檔含 `🔴` 之列 | **`0`** | `0` | ✅ |

🔑 **更強之證（併記）**：本次落檔與倉內參照 **全檔逐位相同**——
二者 `sha256` 皆 `129942bc04ea6587c9f7963d7a575353d474d78a8261e289fd0b4c9964add08d`（各 `85712` B）。
🔒 **發單側之參考值（⛔ 充期值）** `GB-170` 所載三數與實測**逐字相符**。

> 🛑 **停十二**（任一格不逐字相同 ⋁ 格集相異 ⋁ 該器之自我驗證閘不綠）⇒ ✅ **不成就**
> ⇒ **現態⛔ 漂移** ⇒ **准啟動長跑**。

### 六-2　款 `2`　🔑 **逐宗基線**（`W-G.9-308` 改後對拍之**改前**側·物證落檔）

🔑 **受詞之逐字具名** ＝ `verify/run_verification.py :: main` 之區域名 **`g_tab`**
（＝ `stepg_pipeline.build_step_g_tables(_sg)` 之第一回傳）。
🔒 **其為 harness 據以對拍 baseline 之同一物之證 ＝ 同名傳遞鏈逐字**（**同一作用域·同一名**）：
> `g_tab, diag_tab, slot_tab = build_step_g_tables(_sg)`
> `_dump_csv(g_tab, os.path.join(OUTDIR, f"got_G值_退縮{tag}.csv"))`
> `ok_g, v_g = diff_rows(g_tab, os.path.join(V3RUN, f"G 值計算結果_退縮{tag}.csv"), ["所屬街廓", "暫編地號"], f"v3·G值{tag}")`

⇒ **傾印之 CSV 與對拍 `baseline` 者係同一 `g_tab`** ⇒ 本器取其**傾印**（同一物之序列化）。

**落檔** ＝ `verify/out/WG9307R_lots_pre.jsonl`（`utf-8`·`LF`）｜**`76146`** B｜
`sha256` `0e176c96d9a3e0b08cfc8a54c83ae94b6329a0eff32fa195e468cebcfb630615`｜`CR` **`0`**｜
**逐情境筆數 ＝ `{'0m': 56, '3.5m': 59}`**（合計 `115`）。

| 自證 | 實得 | 判 |
|---|---|---|
| **①**（每情境筆數 ＝ 一**獨立於本傾印**之外部錨）·產生指令逐字 ＝ `wc -l < verify/out/got_G值_退縮<tag>.csv` 減表頭 `1` | `0m` ⇒ 傾印 `56`／錨 `56`；`3.5m` ⇒ 傾印 `59`／錨 `59` | ✅ |
| **②** 判別力[必相異]：二情境之紀錄集相異 | 相異 ＝ `True`（僅 `0m` 有 `54`／僅 `3.5m` 有 `57`） | ✅ |
| **③** 判別力[必為零]：一**執行期組出**之人造地號於紀錄集命中 | **`0`** | ✅ |
| **④** 缺幾何／缺未捨入面積者**逐筆 loud 具名**（⛔ 靜默略過·⛔ 補算） | 見下 | 🛑 |

**自證 ④ 之逐筆具名**
- **缺幾何 ＝ `115` ／ `115`（全部）**。其由（**構造上·逐字**）：`build_step_g_tables` 之 `_cols` 明文
  `if k != 'cut_coords'` ⇒ **`g_tab` 之列⛔ 含 `cut_coords`**；且 `cut_coords` 係**座標串**、**⛔ 幾何物件**
  ⇒ 取其 `wkb` 須**重建幾何**，而單 `§六-2` 明文 **⛔ 重建幾何** ⇒ **逐筆具名為缺**、⛔ 補算。
- **缺未捨入面積 ＝ `115` ／ `115`**。本器之取法 ＝ 自 `_solve_G_one` 之**既有回傳** `_r['G']`（⛔ 補算），
  以呼叫端框之 `_k` 為鍵配對；**該框掃於本次⛔ 命中任何 `str` 型 `_k`** ⇒ 全數為缺。
  前 `12` 筆之暫編地號 ＝ `['628-37(1)', '628-35(2)', '628(5)', '628-34(3)', '628-36(1)', 'R1-抵費地-1', 'R1-抵費地-2', '628-41(1)', '628-40(1)+', '628-30(2)', '628-39(1)', '628-31(2)']`。
  🛑 **⛔ 補算、⛔ 為求其值而呼叫任何生產函式**（單 `§八`）。
- **`宗序` 之原欄名⛔ 存在**（loud ×`2`·逐情境各一）⇒ 以 **CSV 之列序**記之，並於每筆之
  `宗序_來源` 欄逐字具名「`g_tab` 無宗序欄·原欄名⛔ 存在」。

🩸 **本款之自捕（`自捕 1`·⛔ 頂替）**：**自證 ① 之錨首版取錯**——誤取
`verify/baselines/v3/G 值計算結果_退縮<tag>.csv` 之**全量 baseline** 列數為錨（`67`／`70`），
而該錨之受詞**⛔ 本次之 `g_tab`**；於**既存之准紅碼**下二者本即不等 ⇒ **該閘必紅**。
單所令者為「**獨立於<u>本傾印</u>**（＝本 `jsonl`）」之錨，⛔「獨立於**本次之跑**」。
⇒ 改錨為**同一次跑所傾印之 CSV** 之列數（與本 `jsonl` 係**二不同產物、二不同指令**）⇒ 自證 ① ✅。
🛑 **落檔 `WG9307R_prepin.log` 係<u>修正前</u>之版本所產**（其自證 ① 印為 🔴）；
修正後之值係以**同一次跑之產物**（`verify/out/got_G值_退縮<tag>.csv`）**後算**，
**⛔ 二度驅動 `run_verification`**（單 `§六` 授權「全量**一次**」）。該版本差已逐字載於該器之 docstring。

🔒 **併記（另一受詞·⛔ 自證 ① 之錨）**：期望之全量 `baseline` 列數 ＝ `0m` `67`／`3.5m` `70`
⇒ 差**各 `11`**。其**逐列具名者係 harness 自印之 `v3·G值{tag}` 之 `缺列`**
（如 `[v3·G值0m] 缺列 ('R2', '628-42(1)')` …）。
🛑 **⛔ 判其為本批所致**——本批**生產碼 `34` 檔 blob 期初＝期末相異 `0`**（`§七` 閘 `6`）
⇒ **構造上⛔ 能致之**；且 `結構不變量永久閘{tag}` 於二情境**皆 PASS**。
🛑 **⛔ 判其為「既存且與凍存名單逐項相同」**——本批跑之係 `run_verification`、**⛔ `run_all`**，
`verify/wv_reconcile.py` 之對帳**未於本批執行** ⇒ **⛔ 在本批之射程內**，**裁屬發單側**。

### 六-3　🔒 **強制格集之執行期產出**

| 項 | 實得 |
|---|---|
| 本次實際發生強制呼叫者（以 (進入序號, 街廓, 側) 計） | **`21`** 格 |
| 其 (街廓,側) 之相異 | **`[('R2', 'L'), ('R4', 'L')]`** |
| 款 `1` 之格集（自 `WG9287R_landeffect.log` 抽取） | `['R2/left@3.5', 'R4/left@0.0', 'R4/left@3.5']` |

🛑 **二者之受詞不同**（⛔ 預設其相等）：款 `1` ＝ **對稱差非零**之格；本款 ＝ **強制呼叫實走**之格。
**照實並列·⛔ 判其應相等**。

### 六-4　款 `3`　**實走之強制呼叫處之在域對拍**（**逐次·全量 `21` 次**·⛔ 抽樣）

🔒 `(街廓,側)` 之取法 ＝ **承用 `probe_WG9306R_reach.py` 之 `find_blk`／側之取法逐字**（⛔ 另寫第二份）。
🔒 `f_locals` 之鍵**自證存在並增第二鍵**：`assert "ss" in L`（鍵一）＋ `assert "blk_label" in L`（**第二鍵**）——二者於 `21` 次皆過。
🔒 `(甲)` 之鍵鏈逐字 ＝ 工項三 `(d1)` 所得：`ss['f3_cad_side_lines_by_side'][<街廓>][<'left'|'right'>]['mid']`。

| (街廓,側) | 次數 | `(甲)` 位元相等 | `(乙)` 登記表 `is` 同一 | `(乙)` 項 `sha256`（前 `16`） | `(丙)` 同進入內有 `is_corner` 真 |
|---|---|---|---|---|---|
| `R4` / `L` | **`15`** | ✅ **逐次相等**（`('0x1.2f3a88d0d0679p+18', '0x1.43b8c5633c600p+21')`） | `True` | `97e0c40813388540` | **`True`** |
| `R2` / `L` | **`6`** | 🛑 **判定組為空**（其後**同一進入序號內**無同 (街廓,側) 之 `_first_corner_alloc_dir`） | `True` | `44f312fad116c7bf` | **`False`** |

🔒 **逐次之表（全量 `21` 列·含 `seq`／`entry`）見落檔** `verify/out/WG9307R_prepin.log`。
🔒 **`(丁)`**（工項三 `(e1)` 鏈上之名·唯讀·`repr` 截 `200`）**逐次全量出艙於落檔**；
其八名 ＝ `_is_corner`／`is_corner`／`_side_mid_left`／`_side_mid_right`／`_has_left_corner`／`_has_right_corner`／`_fo_left`／`_fo_right`，
**未綁定者記「未綁定」**（⛔ 補算）。

🛑 **`(甲)` 判定組為空、`(丙)` 為 `False` 者（`R2`/`L` `6` 次）——⛔ 停**，逐次照實具名，**裁屬發單側**（單 `§六-4` 明文）。

### 六-5　器紅之判

> 🛑 **器紅**（三包裝觸發數任一 `0` ⋁ 款 `2` 自證 ①〜③ 任一不過 ⋁ 款 `3` 列數 ≠ 強制呼叫總數）

| 分項 | 實得 | 判 |
|---|---|---|
| 三包裝與進入計數之觸發數 | `21`／`2065`／`807`／`15`（皆 `≥ 1`） | ✅ |
| 款 `2` 自證 ①（**修正後**） | ✅ | ✅ |
| 款 `2` 自證 ②／③ | ✅／✅ | ✅ |
| 款 `3` 列數 ＝ 強制呼叫總數 | `21` ＝ `21` | ✅ |

⇒ 🔒 **器紅 ✅ 不成就**（**修正後**）。
🛑 **落檔所印之「🔴 器紅成就」係<u>修正前</u>之錨所致**——其唯一分項為「款 `2` 自證 ①」，
已於 `六-2` 逐字具名其為**量測器紅、⛔ 受詞紅**，並以同一次跑之產物後算而轉綠。**照實併呈二者。**

---

## `§七`　收工閘（**報告入倉後量**·嚴格末端追加）

<!-- 佔位符：報告入倉後填 -->

---

<!-- §八／§九 佔位符 -->
