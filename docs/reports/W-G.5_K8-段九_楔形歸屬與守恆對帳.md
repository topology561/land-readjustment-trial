# W-G.5 · K-8 段九 —— **楔形歸屬 ＋ 面積守恆對帳 ＋ 宗邊#0 產生點**（只量不判）

**分支**：`wip/s1-endpart`　**起點 HEAD**：`88a9162`
**⛔ 不提任何新假說、不下結論、不判定是否為缺陷、不改任何一行判準碼。**
**⛔ 只以 `data/V6_1.dxf` 跑；`V6` 那輪輸出保留未動。未跑 `run_all`。**

> 本報告為本批之**唯一本體**。實料：`verify/out/probe_ruling_K9_corner_width_V6_1.log`。

---

## 〇、三項量測結果（一句話各一）

1. **楔形歸誰**：**殘量 ＝ 0.000000㎡（0.00%）**——三片溢出各 **100%** 落在**單一**對象上。
2. **守恆對帳**：六街廓殘差 `−0.0437㎡ ～ +0.0251㎡`；**R1 之殘差非全區最大**。
3. **`宗邊#0` 產生點**：`app.solve_G_binary` → `_block_strip` 之 `block_poly.intersection(strip)`。

---

## 一、交付格式證明

基準 commit `88a9162`。

| 檔 | `byteΔ` | `A1` | 說明 |
|---|---|---|---|
| `verify/probes/probe_ruling_K9_corner_width.py` | True | **False** | 本項為真變更（新增段九量測） |
| **其餘既有 `.py`** | **False（0 檔異動）** | **⇒ `A1` 全為 `True`** | 見下 |

```
$ git diff --stat 88a9162 -- '*.py'
 verify/probes/probe_ruling_K9_corner_width.py | 69 ++++++   （1 檔·純新增）

$ git diff 88a9162 --stat -- app.py verify/run_verification.py
（空 ⇒ 生產路徑零異動）

$ git status --porcelain verify/out/probe_ruling_K9_corner_width.log
（空 ⇒ V6 那輪輸出保留未動）
```

`py_compile` 通過。**`app.py` 一行未動。**

---

## 二、(1) 那塊楔形歸誰（**只出數字·不下結論**）

母體 ＝ 同街廓內**每一宗** `g_row` 之 `cut_coords`（逐宗列·非只鄰宗）＋ 抵費地各片 ＋ 街廓多邊形。

### 2-a `0m R1/left` 片0（溢出 `5.325538㎡`）

| 對象 | 交集(㎡) | 佔溢出 |
|---|---|---|
| `628-37(1)` [left] | 0.000000 | 0.00% |
| `628-35(2)` [left] | 0.000000 | 0.00% |
| `628(5)` [right] | 0.000000 | 0.00% |
| `628-34(3)` [right] | 0.000000 | 0.00% |
| `628-36(1)` [right] | 0.000000 | 0.00% |
| `R1-抵費地-1` [抵費地] | 0.000000 | 0.00% |
| **`R1-抵費地-2` [抵費地]** | **5.325538** | **100.00%** |
| 街廓多邊形（**不計入加總**·母體重疊） | 5.325538 | 100.00% |
| **── 各宗＋抵費地 加總** | **5.325538** | **100.00%** |
| **── 殘量（溢出 − 加總）** | **0.000000** | **0.00%** |

### 2-b `3.5m R1/left` 片0（溢出 `5.325538㎡`）

同上：**`R1-抵費地-2` 交集 `5.325538`（100.00%）**，其餘各宗皆 `0.000000`。
**殘量 `−0.000000`（−0.00%）。**

### 2-c `3.5m R1/right` 片0（溢出 `5.067860㎡`）

| 對象 | 交集(㎡) | 佔溢出 |
|---|---|---|
| `628-36(1)` [left] | 0.000000 | 0.00% |
| `628(5)` [right] | 0.000000 | 0.00% |
| **`628-34(3)` [right]** | **5.067860** | **100.00%** |
| `628-37(1)` [right] | 0.000000 | 0.00% |
| `628-35(2)` [right] | 0.000000 | 0.00% |
| `R1-抵費地-1`／`R1-抵費地-2` | 0.000000 | 0.00% |
| **── 各宗＋抵費地 加總** | **5.067860** | **100.00%** |
| **── 殘量（溢出 − 加總）** | **−0.000000** | **−0.00%** |

### 2-d 三片之對照

| 情境·街廓側 | 溢出(㎡) | 100% 落於 | 殘量(㎡) |
|---|---|---|---|
| 0m R1/left | 5.325538 | **`R1-抵費地-2`**（抵費地片） | 0.000000 |
| 3.5m R1/left | 5.325538 | **`R1-抵費地-2`**（抵費地片） | −0.000000 |
| 3.5m R1/right | 5.067860 | **`628-34(3)`**（另一宗地·`right`） | −0.000000 |

⚠️ **本節不下「是碎屑／不是碎屑」之結論**（交辦文明令）。

---

## 三、(2) 面積守恆對帳（**只出數字·不判定是否為缺陷**）

⚠️ **資料精度註記（事實·非判定）**：`G(㎡)` 與 `幾何面積(㎡)` 於 `g_rows` 中**已為 2dp**
（`round(..., 2)`·見 `grep -n "'G(㎡)': round" verify/stepg_pipeline.py`），
街廓面積則為全精度 ⇒ **殘差含 2dp 記載之捨入量**。

### 3-a 退縮 0m

| 街廓 | ΣG(業主宗) | Σ抵費地G | Σ抵費地幾何 | 街廓面積 | **殘差（ΣG＋抵費地幾何−街廓）** |
|---|---|---|---|---|---|
| **R1** | 1718.9200 | 0.0000 | 1174.6400 | 2893.5531 | **+0.006913** |
| R2 | 2569.2300 | 0.0000 | 1799.3900 | 4368.6033 | +0.016653 |
| R3 | 2432.4600 | 0.0000 | 1774.3600 | 4206.7996 | +0.020376 |
| R4 | 1978.8000 | 0.0000 | 1229.0800 | 3207.8665 | +0.013501 |
| R5 | 2384.0900 | 0.0000 | 1766.1000 | 4150.2149 | −0.024877 |
| R6 | 2347.5700 | 0.0000 | 1628.6900 | 3976.3037 | **−0.043657** |

### 3-b 退縮 3.5m

| 街廓 | ΣG(業主宗) | Σ抵費地G | Σ抵費地幾何 | 街廓面積 | **殘差** |
|---|---|---|---|---|---|
| **R1** | 1719.1700 | 0.0000 | 1174.4000 | 2893.5531 | **+0.016913** |
| R2 | 2597.6300 | 0.0000 | 1770.9900 | 4368.6033 | +0.016653 |
| R3 | 2440.0500 | 0.0000 | 1766.7200 | 4206.7996 | −0.029624 |
| R4 | 1978.8000 | 0.0000 | 1229.0800 | 3207.8665 | +0.013501 |
| R5 | 2412.4600 | 0.0000 | 1737.7800 | 4150.2149 | +0.025123 |
| R6 | 2347.5700 | 0.0000 | 1628.6900 | 3976.3037 | **−0.043657** |

**對照組（溢出 ＝ 0 者）**：R2〜R6。
**數字事實**：兩情境下 **R1 之殘差絕對值皆非全區最大**（最大者為 R6 `0.043657`）。

⚠️ **本節不判定是否為缺陷。**

---

## 四、(3) `宗邊#0` 之產生點（**只指出·不判斷對錯·不改任何一行**）

`宗邊#0` ＝ 該宗 `cut_coords` 之第 0 條邊 ⇒ 追 `cut_coords` 之寫入點。

### 4-a 窮盡 grep（原始輸出）

```
$ grep -rn "'cut_coords'\]\s*=\|\"cut_coords\"\]\s*=\|cut_coords=\|'cut_coords':\|\"cut_coords\":" --include="*.py" .
./app.py:8867:        'cut_coords': cut_coords,  # 任務七：最終配地之多邊形頂點（list[[x,y]]）
./app.py:10150:    _r['cut_coords'] = []
./app.py:17367:                            'cut_coords': _res.get('cut_coords', []) or [],
./app.py:18105:                                        'cut_coords': _coords_list,
./app.py:18240:                                'cut_coords': None,
./app.py:18275:                                'cut_coords': None,
./app.py:18325:                                            _largest['cut_coords'] = list(
./app.py:18343:                                            _largest['cut_coords'] = list(
./verify/fixture_end_fallback.py:71 / :73        （夾具·非生產）
./verify/fixture_end_reserve.py:94              （夾具·非生產）
./verify/fixture_end_winner.py:104 / :106       （夾具·非生產）
./verify/stepg_pipeline.py:381:            'cut_coords': _res.get('cut_coords', []) or [],
./verify/stepg_pipeline.py:969:                        'cut_coords': _coords_list,
```

### 4-b 逐段候選與判定（**本探針所用之 `g_rows` 走 harness 路徑**）

| # | 候選（`file:line`） | 段別 | 是否為本案 `宗邊#0` 之來源 |
|---|---|---|---|
| 1 | `verify/stepg_pipeline.py:381` | 落位（業主宗列組裝） | ✅ **是**——但僅**轉抄** `_res['cut_coords']` |
| 2 | `verify/stepg_pipeline.py:969` | 落位（**抵費地**列組裝） | ✅ 是（**抵費地片**之 `cut_coords`·非業主宗） |
| 3 | `app.py:8867` | **切割**（`solve_G_binary` 之回值） | ✅ **是——真正之產生點** |
| 4 | `app.py:10150` | 幾何修復／fallback | ❌ 否（`iterate_G_S` fallback·**設為 `[]`**） |
| 5 | `app.py:17367`／`:18105` | 落位（**app UI 路徑**之孿生） | ❌ 否（本探針走 harness·**非** app 路徑） |
| 6 | `app.py:18240`／`:18275` | 落位（設為 `None`） | ❌ 否 |
| 7 | `app.py:18325`／`:18343` | 碎片歸併（`_largest['cut_coords']`） | ❌ 否（app 路徑之碎片歸併） |
| 8 | `verify/fixture_*.py` | 夾具 | ❌ 否（非生產） |

### 4-c 產生鏈（逐段·附 `file:line`）

```
verify/stepg_pipeline.py:381   'cut_coords': _res.get('cut_coords', []) or []
        ↑ _res ← _solve_one(...)                      verify/stepg_pipeline.py:384
        ↑ ns["_solve_G_one"](...)                     verify/stepg_pipeline.py:390
app.py:10111  def _solve_G_one(...)
app.py:10125       _r = solve_G_binary(...)
app.py:8695   def solve_G_binary(...)
app.py:8832        final_cut, _final_area = _block_strip(block_poly, d_hat,
                                                        baseline_pt, _S_cut,
                                                        allocation_dir=allocation_dir)
app.py:8846–8854   cut_coords = [[float(p[0]), float(p[1])]
                                 for p in list(final_cut.exterior.coords)]
                   （MultiPolygon ⇒ 取**面積最大者** `_biggest`·app.py:8847–8850）
app.py:8867        return { … 'cut_coords': cut_coords, … }
```

**`_block_strip`（`app.py:7783`）之本體**：

```
app.py:7809–7816   n_hat = rot90(allocation_dir)      （缺值/零向量 → 退 rot90(d_hat)）
app.py:7822–7828   strip = Polygon([bp − big·n_hat, bp + S·d_hat − big·n_hat,
                                    bp + S·d_hat + big·n_hat, bp + big·n_hat])
app.py:7829+       cut = block_poly.intersection(strip)
```

⇒ **段別歸屬：`宗邊#0` 屬「切割」段**——
`block_poly ∩ 平行四邊形切帶`，切帶之兩側邊方向為 `n_hat = rot90(allocation_dir)`。
**非**落位、**非**碎片歸併、**非**幾何修復所產生。

⚠️ **本節只指出產生點，不判斷對錯、未改任何一行。**

---

## 五、已否證／已排除之項（**登記·勿再重做·勿再引用**）

claude.ai 已提出並自行否證四個假說，**本批未重做**（交辦文明示）：

| # | 假說 | 否證數字 |
|---|---|---|
| ① | 溢出 ＝ 截角還原角 | 交集 **0.00%** |
| ② | SIDE_LINE 與街廓側邊不共線 | 最大 **1.2mm** |
| ③ | 後界 BASELINE 折點 | 楔形 **0.032㎡** |
| ④ | 宗地直弦 vs 街廓折邊 | 折角 **17 角秒**、僅 **1.16㎡** |

⚠️ 段七所附之 **79.4%／87.8% 比例係數字巧合、勿再引用**（KL 2026-08-03 明示）。

---

## 六、本批未做

- **未提任何新假說、未下結論、未判定是否為缺陷**（交辦文明令三處）。
- 未改判準、未設門檻、未調參、未掛 `run_all`。
- **未動 `app.py` 一行**；`verify/run_verification.py` 零異動。
- **未重跑 `V6` 那輪**（其輸出保留·`git status` 空）
  ⇒ ⚠️ 該 log 係**段七版探針**產物，不含段八／段九區段。
- 四個已否證假說**未重做複驗**。

---

## 七、段九之後

1. **U-K9-3 之 KL 裁定**（本批只交事實）。
2. **K-6-A2**：K-9 全八則之實作（GB-13／14／18／19）。
3. **波末批**：換圖＋換快照＋重烤——同一次烤完（GB-16）。
4. **泛化波**：GB-17 全倉資產校驗碼掃描、恆真閘家族（GB-10）、去錨。
