# `W-G.9-91`　「乙」之 shim ＋ 🛑 藍影定式對圖**不符（停機）** ＋ 遞補同構實證 ＋ 容差閘普查

**基座**：`2cb0fe9`（`origin/wip/s1-endpart`·`W-G.9-90`）——🔒 **與施工單所載一致**（本批無基座落差）。
**受詞**：「乙」＝ **施加「不交叉」判準 ⇒ 違反者不配地 ⇒ 遞補**（`VR-050` 四）之土地後果
**性質**：🔒 **只量不修**·⛔ 零 `app.py` 變更·🔴 ⛔ **未移任何線**·⛔ 未建任何介面
**探針**：`verify/probes/probe_WG991_yishim.py`（新增）　**log**：`verify/out/probe_WG991_yishim_2cb0fe9.log`

---

## §0　三句結論

1. 🛑 **停機④ 觸發**：claude.ai 之**藍影定式與 KL 圖說不符**——其下限線通過 **`B1`**，
   而 KL 圖之逐字為「第 2 宗**至少要從 `B2` 開始**／**`B1~B2` 範圍內之 ALLOCLINE 都不能**為其遠側界」；
   本批以圖之**拓樸權威源**（`.cnt`／`.lin`）機械證 **`B1` ≠ `B2`**（`.lin` 有一段 BASELINE **直連**二點）
   ⇒ **`A-2-2` ⛔ 不辦**（⛔ 不得以自定式續算·⛔ 不得另立第二定式）。
2. 🔴 **「乙」第一輪之土地後果**：不配地 **2 宗**／**`4.7950 ㎡`** 入調配池；
   而 **`②-宗` 破量<u>逐位完全不變</u>**（`R2 45.9766`／`R5 56.3293` ⇒ 同值）
   ——因破量對為 **`(j=0,k=2)`／`(j=0,k=3)`**，**根本不含被移除之 `i=1`**。
   ⇒ `P2`（仍 > 0）**成立**，且結論**比預測更強**：**「乙」之第一輪對破量之影響 ＝ 0**。
3. 🔴 **遞補介面：全倉 `verify/**.py` ＋ `app.py` 之 `遞補` 命中 71 行，<u>可執行層 ＝ 0</u>**
   （全在註解／字串／f-string 內）⇒ **`W-G.9-89` 之 `【已有】`（`run_verification.py`×11）被否證**
   ——那 11 行**皆為非註解、卻全是字串字面**（測試名／CSV 檔名／print 訊息）。

---

## §A　五組擴搜 ＋ 反向檢五錨 ＋ 四款停機判定

### A-1　⑥ 指令與範圍**逐字出艙**

母體 ＝ `git ls-files -z` 之**追蹤檔**（⇒ ⛔ 未追蹤檔與**巢狀 worktree** 一律不在母體）；
字樣以 `re.escape()` **直入 Python**、⛔ 不經 shell。**適用動作 ＝ 讀 ＋ 寫。**

```
files = [p.decode("utf-8") for p in
         subprocess.run(["git","-C",REPO,"ls-files","-z"],capture_output=True).stdout.split(b"\x00") if p]
rx = re.compile(re.escape(term));  n = len(rx.findall(io.open(f,encoding="utf-8",newline="").read()))
```

⚠️ **自身污染之排除（⛔ 逐字·含「排除 0 筆」）**：`probe_WG991_yishim.py` 與
`W-G.9-91_乙之shim與藍影對G.md` 自各組母體排除——**其於各組母體內者 ＝ 0 筆**（搜尋時二檔尚未建）。

### A-2　五組之逐字樣命中

| 組 | 【出處分類】 | 母體 | 字樣 → 命中檔／命中總數 |
|---|---|---:|---|
| ⓪ | **【正典款號】**@`docs/rulings/` | 1 | `K-9-9` 1/29・`K-9-11` 1/8・`K-9-13` 1/11・`K-9-15` 1/4・`K-9-12` 1/34・`K-9-5-14` 1/16 |
| ⓪′ | **【土地行話】**@`docs/rulings/` | 1 | `遞補` 1/22・`調配池` 1/15・`合併` 1/28・`不配地` 1/6・`超配` 1/7・`投影序號` 1/7・`應分配` 1/1 |
| ⓪″ | **【對方原詞】**@`rulings`+`figures` | 19 | **`藍色影線` 0/0**・`第 0 宗` 1/7・`第 1 宗` 3/79・`不交叉` 1/2・**`遞補試算` 0/0** |
| ① | 【我方·可】@`verify/**.py`+`app.py`+`reports` | 487 | `弦區間` 20/85・`s*` 92/432・`λ_a` 13/93・`λ_b` 14/92・`S_req` 16/190・`容差閘` 1/1・**`藍影面積` 0/0** |
| ② | 【碼面】@同上 | 487 | `遞補標的宗` 2/2・`遞補錨` 8/30・`遞補整形` 12/26・`_PAR_TOL` 17/94・`1e-12` 58/136・`1e-9` … |

🔒 **回 0 之三字樣逐一具名（⛔ 不得一概而論·⛔ 不得據其 0 推論任何事）**：

- **`藍色影線` 0**／**`遞補試算` 0** ＝ **KL 對話側之口語、倉內從未寫下**（承 `-85`〜`-88` 之既有具名）。
- **`藍影面積` 0** ＝ 🔴 **本批之新造詞**（施工單 `A-2` 首用）——⛔ 非「查無 ⇒ 不存在」。
  🛑 **且本批之結論即：該詞所指之物<u>尚未有一個與 KL 圖說相符之定義</u>**（§D）。

🔒 **判別力自檢**：寬字樣皆 > 1（`s*` 432・`S_req` 190・`1e-12` 136・`λ_b` 92・`第 1 宗` 79）
⇒ 該搜非恆 0；⓪⓪′ 兩組**無一為 0** ⇒ 三組皆非全 0。

### A-3　⑦ 命中清單之**已答／未答** ＋ 修法 110

**`n ≥ 3` 之命中檔 ＝ 89 檔 ⇒ 逐檔開啟**（全文 `read()`）。

| 層 | 定義 | **分母** | 判 |
|---|---|---:|---|
| **A** | 含受詞語彙 **且** 含面積級量測（`\d+\.\d{2,} ㎡`） | **27** | 26 未答 ＋ **1 須人工裁** |
| **B** | 含受詞語彙、⛔ 無面積級量測 | **30** | **未答** |
| **C** | ⛔ 不含任何受詞語彙 | **32** | **未答** |

🔒 **合計自校**：27 ＋ 30 ＋ 32 ＝ **89** ／ `n≥3` 之檔 **89** ⇒ ✅
🔒 **量詞三對齊**：結論句 N ＝ **89** ／ 表列數 ＝ **89** ／ 裸列舉母體 ＝ **89** ⇒ ✅

🔴 **唯一須人工裁者 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md:2290`**：

> 「而我算之 **`S_req` ＝ `7.4487`** ⇒ 🔑 **`S_req` 就是那條藍影下限線。**」

⇒ **判：未答**——該句係對「**下限線之身分**」之宣稱，⛔ 非「**藍影面積**」之量測；
本單受詞（`藍影面積` vs `G₁`）**倉內查無任何實測**。
🛑 **惟該句直接關聯 §D**：其把下限線之通過點認定為 `B1`，而 KL 圖說之下限為 `B2`。
🔒 **本批⛔ 不斷言 `S_req` 應等於何者**——那即是 §七-6 所禁之**第二定式**。

### A-4　⑨ 反向檢五錨之逐字覆核（本單宣告「⛔ 無待 KL 裁之題」，故正面舉證）

| 錨 | 逐字要旨（本批現查） | 效力 | 判 |
|---|---|---|:--:|
| 🛑 `K-9-11` 三 | `docs/rulings/` 命中 **8**；含「調配池、合併調配、超配三事皆已裁，⛔ 不得再上呈 KL」 | ⇒ 🛑 本單⛔ 不得就該三事出裁定題 | ✅ 遵 |
| `K-9-9 四` | `K-9-9` 命中 **29**；`不配地` **6**／`超配` **7**／`投影序號` **7** | ⇒ 「乙」之處置已定 | ✅ |
| `K-9-15` 一／三-2 | `K-9-15` 命中 **4** | ⇒ 判準與位置法則已定 | ✅ |
| `K-9-13` | 命中 **11** | ⇒ 「甲」之處置（本單⛔ 不辦） | ✅ 未辦 |
| `VR-050` 四 | 「移線至 `S_req`」＝ 超配 ⇒ 明禁 | ⇒ ⛔ 本單不得移任何線 | ✅ **零移線** |

### A-5　🛑 四款停機之判定

| 款 | 判準 | 本批現查 | 判 |
|---|---|---|:--:|
| ① | 上開任一錨已被後裁取代 | 五錨於 `docs/rulings/` 皆現查可得、⛔ 無後裁字樣 | ⛔ **不觸發** |
| ② | 「不得超配」已被後裁放寬 | `超配` 於 `docs/rulings/` 命中 **7**，逐處皆為**禁止**語境 | ⛔ **不觸發** |
| ③ | 已有探針出艙過「以不交叉判準決定不配地並量其土地後果」 | `probe_WG983_k99prep` 之 A-2-4 **確有**「移除宗後重測破量」，惟其移除集出自**舊判準**：對 `WG988`／`K-9-15` 命中皆 **0**（`K-9-15` 裁於 2026-08-20·**晚於** `-83`）；移除集 `R2 (甲)=[2,9]`／`R5 (甲)=[1,8]`，而不交叉之違反 ＝ `{R2 i=1, R5 i=1}` ⇒ **`R2` 之集⊉`{1}`、`R5` 之集≠`{1}`** ⇒ **集合不同**（⛔ 非只看包含） | ⛔ **不觸發** |
| ④ | 🆕 藍影定式與 KL 圖說不符 | 🛑 **見 §D** | 🛑 **觸發** |

---

## §B　§Z 二塊之自檢

### B-1　前置三道

| 塊 | 目標檔 | bytes | LF | sha256 前 16 | `CR` | 起頭 | 收尾 |
|---|---|---:|---:|---|---:|:--:|:--:|
| `Z-1`（`VR-050`） | `docs/驗證裁定登記表.md` | **7419** ✅ | **86** ✅ | `ca2dbdd65812f55e` ✅ | **0** ✅ | ✅ | ✅ |
| `Z-2`（`自誤 94`） | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **2324** ✅ | **35** ✅ | `d56b0bc1d6902dc8` ✅ | **0** ✅ | ✅ | ✅ |

🔒 **CRLF 之戒（`-90` §B）已內化**：本批**自始即對 `git cat-file -s` 之 blob 量**，⛔ 未於工作區量後誤報 MISMATCH。

| 塊 | **blob** 現況 | ＋N | ＝ | 期望 | 判 | （工作區 bytes·⛔ 不以之驗） |
|---|---:|---:|---:|---:|:--:|---:|
| `Z-1` | 194673 | 7419 | **202092** | **202092** | ✅ | 197145 |
| `Z-2` | 163066 | 2324 | **165390** | **165390** | ✅ | 165530 |

### B-2　入倉後自檢四項

| 自檢 | 實得 | 判 |
|---|---|:--:|
| ① 末 N bytes 逐位 == 片段（**工作區 ＋ blob 各驗**） | 二塊**皆逐位相同**（兩側） | ✅ |
| ② **blob** 檔長增量 == N | `202092`／`165390` ＝ 期望**二檔全中**；增量 `7419`／`2324` == 片段 | ✅ |
| ③ `numstat` 刪除欄全 0（**二法** ＋ 陽性對照） | 法一 `+35/-0`・`+86/-0`・`+9/-0`；法二 `+35/-0`・`+86/-0` ⇒ **非 0 者 ＝ 0**<br>🔒 陽性對照 `b3aaf7b..1ad98d4` ⇒ **7 筆會咬** | ✅ |
| ④ 二個新編號之定義節標題各 1 | `^## \`VR-050\`` ＝ **1**／`^## 自誤 94` ＝ **1**<br>🔒 判別力：`^## \`VR-049\`` ＝ 1、`^## \`VR-999\`` ＝ **0** ⇒ 非恆 1 | ✅ |

🔒 入倉後 blob `CR` ＝ **0**（二檔）；**對照組** `data/V6.dxf` blob `CR` ＝ **12308** ⇒ 該檢**會咬**。

---

## §C　A-1　「乙」之 shim（⛔ 不移線·判準 ＝ 不交叉·原樣 import `w88`）

### C-1　違反宗之確認（A-1-1）

母體 ＝ **47 列**；違反 ＝ **2 宗**。

| 鍵 | i | 前 | `s*` | 面積（㎡） | 情形 |
|---|---:|---:|---:|---:|---|
| `R2\|左\|1\|0` | 1 | 0 | `10.0407` | **`3.80822376`** | 在內 |
| `R5\|左\|1\|0` | 1 | 0 | `5.1067` | **`0.98672881`** | 在內 |

`POPULATION=47 PRINTED=2 SUPPRESSED=45`；面積合計（帶號）＝ **`4.7950 ㎡`**／（絕對值）＝ **`4.7950 ㎡`**。

🔒 **與 `-88`／`-90` 之逐位對拍**：二宗之面積於 **8dp** 逐位相同；**名單對稱差 ＝ 0**。

### C-2　第一輪處置（A-1-2·`K-9-9 四`·⛔ 無遞補·⛔ 不移線）

- 不配地之宗 ＝ `{'R2': [1], 'R5': [1]}`；其面積**入調配池** ＝ **`4.7950 ㎡`**（＝ C-1 之合計·⛔ 非另算）。
- **其餘各宗 ＝ 57 宗**（全部 **59** − 不配地 **2**），其 `s_lo`／`s_hi`／面積**已逐宗全列**於 log
  （`POPULATION=57 PRINTED=57 SUPPRESSED=0`）。
- 🔒 **「逐位不變」之性質為<u>構造保證</u>、⛔ 非量測**：移除 ＝ 自**破量對之母體**剔除該宗（同 `w83` A-2-4），
  **⛔ 未觸及任何宗之幾何** ⇒ 其餘各宗**構造上不可能變**。
  ⇒ 🔒 **⛔ 不以構造代替量測**：真正之判別器 ＝ 下方**負對照**——若構造有誤（隱含移線），負對照必不符。

### C-3　第一輪後之 `②-宗` 破量（A-1-3）＋ 負對照

🔒 **負對照（判別力·常設 8）＝ 移除集 ＝ ∅**：

| 格 | 倉內錨 | 負對照實得 | 判 |
|---|---:|---:|:--:|
| `R2` | `45.9766` | **`45.9765888243`** | ✅ 相符 |
| `R5` | `56.3293` | **`56.3292619405`** | ✅ 相符 |

⇒ 負對照**通過**（同源可比）⇒ 🔒 該 shim **未隱含移線**。

| 街廓 | 移除之宗 | 移除前 | **移除後** | 判 |
|---|---|---|---|:--:|
| `R2` | `[1]` | 2 對 / `45.9766 ㎡` | **2 對 / `45.9766 ㎡`** | 🔴 **仍 > 0** |
| `R5` | `[1]` | 2 對 / `56.3293 ㎡` | **2 對 / `56.3293 ㎡`** | 🔴 **仍 > 0** |

**殘餘破量對逐對**：`R2 (j=0,k=2)` `19.814184 ㎡`／`R2 (j=0,k=3)` `26.162405 ㎡`／
`R5 (j=0,k=2)` `36.133656 ㎡`／`R5 (j=0,k=3)` `20.195606 ㎡`
⇒ **第一輪後之殘餘合計 ＝ 4 對 ／ `102.3059 ㎡`**（帶號）／`102.3059 ㎡`（絕對值）。

🔴 **本批之關鍵發現（比 `P2` 更強）**：移除前後**逐位完全相同**
——**因四個破量對之 `(j,k)` 皆為 `(0,2)`／`(0,3)`，<u>根本不含被移除之 `i=1`</u>**。
⇒ 🔒 **「乙」之第一輪處置對 `②-宗` 破量之影響 ＝ `0`（逐位）**，⛔ 非「減少了一些但仍 > 0」。
⇒ 與 `W-G.9-83` 之既有結論（「移除而不移線 ⇒ 重疊⛔ 不消除」）**同向**，
   且本批以**新受詞**（不交叉·2 宗）給出**更尖銳**之形式。

⛔ **本數係<u>第一輪</u>**（⛔ 無遞補）——⛔ 不得充作「乙」之最終土地後果。

---

## §D　🛑 A-2　藍影面積 vs `G₁`——**定式對圖不符 ⇒ 停機**

### D-1　對驗之源與其權威射程

- **拓樸權威源** ＝ `docs/specs/figures/宗地分配線邏輯.{cnt,lin}`（**29 點／28 線** ⇒ 與 `PROVENANCE §三` 相符）。
- 🔒 **`PROVENANCE §四` 明訂**：本圖為**拓樸**之權威，⛔ **非度量之權威** ⇒ ⛔ 不得作為數值對拍之 fixture。
  ⇒ 本節**只用其拓樸**（何點與何點相異、何段連何段），⛔ 不取其度量。

**群方向之分類**（方向**原樣引自** `PROVENANCE §三`·⛔ 非本批新算）：

| 群 | 實得 | §三 | 判 |
|---|---:|---:|:--:|
| 正街(FRONTLINE) | 7 | 7 | ✅ |
| 最小深度垂線 | 5 | 5 | ✅ |
| 後側境界線(BASELINE) | **6** | 5 | 🔴 **具名**（見下） |
| ALLOCLINE | 5 | 5 | ✅ |
| SIDELINE | 3 | 3 | ✅ |
| 截角斜邊 | 1 | 1 | ✅ |
| (未歸類) | **1** | — | 🔴 **具名**（見下） |

⚠️ **二處落差之具名（⛔ 不因不影響結論而不報·皆⛔ 不影響下判）**：

1. **BASELINE 6 vs §三 之 5**：多出者為段 `2–24`（`(120.773,255.861) → (123.419,268.435)`·長 `12.8494`）
   ——BASELINE 之**延伸段**。
2. **未歸類 1 段** ＝ `3–4`（`(183.089,255.861) → (123.419,268.435)`·單位 `(-0.978510, 0.206197)`）
   ——其向與 BASELINE **正交**（`(0.2059,0.9786)` 旋 90° ＝ `(-0.9786,0.2059)`）⇒ **街廓之右緣**。

### D-2　KL `.jpg` 之逐字（⛔ 原樣引用·⛔ 不改寫不濃縮）

> **條件 2**：「交於FRONTLINE及BASELINE的點要在前一宗土地的遠側境界線的交點後（例如：要在P1及B1後）」
> **條件 3**：「後側境界線交於BASELINE的點一定要在前一宗土地的遠側境界線於FRONTLINE上的交點
> （例如P1）**沿FRONTLINE法向往BASELINE方向交於的點開始往後分配（例如B2後）**」
> **藍字**：「此種方向的宗地分配線／**第2宗土地至少要從B2開始**／
> **B1~B2範圍內的ALLOCLINE（藍色虛線）都不能為第2宗土地的遠側境界線**」

⚠️ **併記（名之落差·⛔ 不掩蓋）**：施工單稱「**藍色影線**」（hatching），
而 KL 圖上之標註為「**藍色虛線**」（dashed lines·＝ `B1~B2` 範圍內之**禁區 ALLOCLINE**）。

### D-3　機械證：`B1` 與 `B2` **是相異之點**

| 步 | 依據 | 實得 |
|---|---|---|
| ① 街角第 1 宗之**遠側界** | ∥SIDELINE 且自 FRONTLINE 連至 BASELINE 之段（`K-9-14`）·**候選 ＝ 1 段**（須 1）✅ | 段 `19–20` ⇒ **`P1` ＝ pt19 `(183.089, 72.662)`**／**`B1` ＝ pt20 `(78.486, 54.913)`** |
| ② 依**條件 3** 求 `B2` | `P1` 沿 FRONTLINE **法向**（＝ 水平·因正街為鉛直）交 BASELINE | BASELINE 上與 `P1` 同 `y`（`72.662`）者 **唯一** ⇒ **`B2` ＝ pt18 `(82.221, 72.662)`** |
| ②′ 佐證 | `最小深度垂線` 群確有段 `15–29`＋`29–16` 自 `(183.089, 72.662)` **水平**連至 `(82.221, 72.662)` | ✅ |
| ③ 🛑 **判** | `‖B1−B2‖` ＝ **`18.137729`**（圖面單位·🔒 **僅用於判「是否同點」**·⛔ 非度量） | 🔴 **`B1` ≠ `B2`** |
| ③′ 佐證（**拓樸**） | `.lin` 中有一段 BASELINE **直接連接** `B1` 與 `B2` ＝ `[(20, 18)]` | ⇒ 二者為 BASELINE 上之**相鄰<u>相異</u>點** |

### D-4　🛑 停機條款④ 之判定

| | 下限線之**通過點** |
|---|---|
| claude.ai 之定式（施工單 A-2-1 逐字） | 「通過 **`B1`** 之 ∥ALLOCLINE 之內側」 |
| KL 圖之逐字 | 第 2 宗**至少要從 `B2` 開始**；**`B1~B2` 範圍內之 ALLOCLINE 都不能**為其遠側界 |

⇒ 🔴 **不符** ⇒ 🛑 **停機上呈**（施工單 §一 停機④／§七-6）。

🔒 **方向之具名（⛔ 尤不得續算之理由）**：`B1` 較 `B2` **在前**
⇒ 以 `B1` 為界之藍影**較小** ⇒ **其瑕恰落在 A-0 所禁之方向**（使後果看起來更小）。

🔒 **本節之唯一正確處置 ＝ 不算**：
⛔ 不得以 `B2` 版自行改式——那即是 §七-6 所禁之「**藍影之第二定式**」。
⇒ **`A-2-2` ⛔ 未辦**；**`P4` ⛔ 無法判**（其受詞 ＝ 藍影面積·而藍影**未定義**）
⇒ 具名為**未答**、🔒 **⛔ 非否證**。

---

## §E　A-3　遞補介面之同構實證（⛔ 只證不建）

### E-1　`run_verification.py` 之 `遞補` 全部命中（**17 行·全列**）

🔒 產生指令（節 105／修法 113）：
`len([i for i,l in enumerate(open('verify/run_verification.py',encoding='utf-8'),1) if '遞補' in l])`
⇒ 母體 ＝ **1463 行**；命中 ＝ **17 行**（`POPULATION=17 PRINTED=17 SUPPRESSED=0`）。

**分層（`tokenize`·🩸 含 f-string·見 §I-1）**：可執行 **0**／註解 **6**／字串（含 f-string）**11**。

🔴 **與 `W-G.9-89` 之「`run_verification.py`×11」對帳（同名不同量·⛔ 不得互代）**：

| 數 | 受詞 | 值 |
|---|---|---:|
| `-89` 之 **11** | **非註解**之行（17 − 6 註解） | **11** ✅ 本批複現 |
| 本批之 **0** | **可執行層**（另扣 11 個「字樣落在字串／f-string 內」之行） | **0** |

⇒ 🔒 **二數皆對、母體不同**；而**判「介面是否存在」之受詞應為可執行層**
（`-89` ⑯ 之自戒：⛔ 只在註解／字串裡的字⛔ 不得判為 `【已有】`）。
🛑 ⇒ **`W-G.9-89` 之 `【已有】` 判定<u>被否證</u>**。

### E-2　「位次遞補」之判定與**二側**判別力

🔒 判準（**正面列舉**·⛔ 非啟發法）：含 `投影序號`／`位次`／`下一位`／`次一位` 任一者
——即 `K-9-9 四` 之逐字「**投影序號中下一位之土地遞補該位次**」之語意。

⇒ **判為「位次遞補」者 ＝ 0 ／ 17**。

| 判別力 | 受詞 | 實得 | 須 |
|---|---|---|---|
| **陽性對照**（🆕 本批自補·⛔ 不可省） | 合成句 `results.append(("投影序號中下一位之土地遞補該位次", ok, v))` | **✅ 是** | 是 |
| **陰性對照**（施工單所令） | `F.1 遞補整形` 之 **7 處**逐處 | **⛔ 否**（7/7） | 否 |

🔒 **陽性對照之必要性**：判準對全母體**皆判「否」** ⇒ **⛔ 無從與「判準恆否」區辨**；
補之後方知「**0 ／ 17**」係**真 0**、⛔ 非量測器壞掉。

### E-3　🔴 母體之擴掃（**CC 自補**·⛔ 施工單未令）

由：「`run_verification.py` 可執行層 ＝ 0」⛔ **不蘊含**「全倉無遞補介面」——**母體選錯**是「0」之第一嫌疑。

⇒ 母體 ＝ `verify/**.py` ＋ `app.py` ＝ **189 檔**；`遞補` 命中 ＝ **71 行**；
🔴 **可執行層 ＝ 0 行**（`POPULATION=0 PRINTED=0 SUPPRESSED=0`）。

⇒ 🛑 **全倉<u>沒有任何可執行碼</u>提到「遞補」。**

⚠️ **本結論之射程（⛔ 具名·⛔ 不得誇大）**：上開係**字樣法**之結論
——它證「**`遞補` 二字未出現於任何可執行行**」，⛔ **不證**「無**異名同義**之機制」。
（`-89` §E-3 具名之是**同名異義**；此處之未涵蓋者為其**逆**：**異名同義**。）

### E-4　與 `K-9-9 四` 之逐項對照（A-3-2／A-3-3）

`W-D.4 遞補錨` 之逐字（`遞補錨` 全列 **4 行**）核心 ＝
`run_verification.py:905`「`W-D.4 遞補錨 R6 85.66→628-4(1)`（**跳過** `628(2)`;`628-1(2)`;`628-23(1)`）」。

| 項 | `K-9-9 四`（正典） | `W-D.4 遞補錨`（倉內實例） | 判 |
|---|---|---|:--:|
| **觸發** | 依該宗 `G` 值算出之**遠側界位置**在起算垂線之前 ⇒ 不配地 | `W-D.4` 之**碎片**（面積過小·`R6 85.66`） | ⛔ **不同**（幾何位置 vs 面積門檻） |
| 受詞 | **投影序號中下一位之土地** | `628-4(1)`（跳過 3 筆） | ⚠️ 形式相近 |
| 結果 | 該宗**遞補該位次** | 該地號成為遞補標的 | ⚠️ 形式相近 |
| 🔴 **跳過之依據** | 正典**未規定跳過** | 跳過 3 筆·**其規則於該檔內查無** | 🔴 **不同構之關鍵** |

🔴 **必答（A-3-3）**：`W-D.4 遞補錨` 是否即 `K-9-9 四` 所指之機制？
⇒ 🛑 **未答·具名**——**觸發條件不同**（面積門檻 vs 遠側界位置），
且 `W-D.4` 之**跳過規則**於該檔內**查無其依據** ⇒ ⛔ 不得判為同構。
🔒 ⛔ **本判⛔ 非裁定題**（`K-9-11` 三：遞補之機制⛔ 不另訂）——係**實證之結果**。

---

## §F　A-4　容差級分類閘之全倉普查（⛔ 只列不改·⛔ 不重驗）

🔒 **起因已坐實**（`W-G.9-90`）：`|sin(源甲,源乙)| ≤ 1e-12` 之宗於換圖後 **8 ⇒ 9**（新入列 ＝ `R1|1`）。

**母體** ＝ `git ls-files` 之 `verify/**.py` ∪ `app.py`，**扣除本探針自身**（其於母體內者 ＝ **0 筆**）
⇒ **189 檔**。🔒 產生指令（修法 113）：`git ls-files -z` → 篩 `verify/**.py` ∪ `app.py` → 扣 SELF。

| 項 | 值 |
|---|---:|
| 樣式「`<op> <tol>`／`<tol> <op>`」之命中 | **530 處** |
| 其中**可執行層** | **342** |
| 註解／字串（含 f-string） | 16 ／ 172 |
| 🔒 分層失敗之檔 | **0**（⛔ 不得靜默；若有，其行一律落入「可執行」⇒ 偏向多報·符 A-0） |
| 🔴 **入判定鏈者（＝ 標記「波末換圖後須重驗」）** | **279 ／ 342** |

🔒 「入判定鏈」之判準（**正面列舉**·⛔ 非啟發法）：該行含
`raise`／`assert`／`return`／`if `／`elif `／`while `／`continue`／`break`／`not `／` and `／` or ` 任一者。
⚠️ **A-0（偏向多報）**：不確定者一律**列入**並具名。

**容差值之分布（可執行層·全列·`POPULATION=17 PRINTED=17`）**：
`1e-9` **140**／`1e-6` **82**／`1e-12` **64**／`1e-15` 26／`1e-3` 15／`5e-4` 8／`1e-8` 4／
`1e-5` 3／`1e-18` 2／`1e-4` 2／`5e-5` 2／`5e-9` 2／`1e-09` 1／`1e-10` 1／`1e-20` 1／`1e-7` 1／`2e-9` 1。

🔒 **279 支已逐處全列**於 log（`POPULATION=279 PRINTED=279 SUPPRESSED=0`）。

### F-1　🔴 判別力與**射程之具名**（依施工單「⛔ 未命中即普查器有誤·具名」）

🔒 **三個數之標籤（⛔ 不得互代·同名不同量）**：

| # | 受詞 | 值 |
|---|---|---:|
| ① | `_PAR_TOL\s*=` 之**字樣命中**（含註解／docstring 之引述） | **7 處** |
| ② | 其中**真定義行**（行首即 `_PAR_TOL =`） | **1 處**（`verify/stepg_pipeline.py:1054  _PAR_TOL = 1e-6`） |
| ③ | **主樣式**（比較運算元 ＋ 容差**字面**）對含 `_PAR_TOL` 之行之命中 | **1 處** |

🔴 **具名之限制**：主樣式為「比較運算元 ＋ 容差**字面**」
⇒ **⛔ 抓不到「先定義為具名常數、再於他處比較」之閘**——`_PAR_TOL` 正是此形
⇒ 🔴 **本普查器之射程為<u>下界</u>**（⛔ 非全集）·⛔ 不得以其數宣稱「全倉只有這些」。

🔒 **補掃（具名常數之<u>比較</u>·樣式 ＝ 比較運算元 ＋ 全大寫識別字 ∧ 含 `TOL`）** ⇒ **69 處**（可執行層）。
⇒ **`_PAR_TOL` 之消費點**（＝ 真正之分類閘）由補掃命中 **2 處**：

```
verify/stepg_pipeline.py:1065  return abs(float(_np_d.cross(_u / _nu, _v / _nv))) <= _PAR_TOL
verify/stepg_pipeline.py:1159  return (_c <= _PAR_TOL), _c, float(_np_d.degrees(_np_d.arcsin(min(1.0, _c))))
```

⇒ ✅ **施工單所令之判別力對照<u>成立</u>**（惟係由**補掃**、⛔ 非主樣式）。

---

## §G　A-5　「乙」之土地後果彙整（⛔ 不含建議）

| 項 | 狀態 |
|---|---|
| 第一輪不配地之宗數／面積（A-1-2） | **2 宗 ／ `4.7950 ㎡`**（🔒 **第一輪**·⛔ 無遞補） |
| 第一輪後 `②-宗` 破量之殘餘（A-1-3） | **4 對 ／ `102.3059 ㎡`**（🔒 **第一輪**·且**與移除前逐位相同**） |
| **`G₁` vs 藍影之逐宗差**（A-2-2） | 🛑 **⛔ 未量**（A-2-1 不符 ⇒ 停機④） |
| **遞補後之宗數／面積／破量** | 🔴 **⛔ 未量**（遞補介面**全倉可執行層 ＝ 0** ⇒ A-3 判為**未答**） |
| 「乙」之射程宗數 | **26** ✅ **本批複驗相符**（修法 114：施工單標 `【未複驗】`） |

🔒 **射程之複驗**（定義 ＝ `W-G.9-89` A-4 逐字「有第 0 宗之組中，第 1 宗以後」）：
全部宗 **59**；街角宗（第 0 宗）**8**；**有街角宗之組中扣除該組第 0 宗後之宗數合計 ＝ 26** ⇒ 與 **26** 相符。

⛔ **本節之全部數皆為<u>第一輪</u>**——⛔ 不得充作「乙」之最終土地後果。

---

## §H　`P1`〜`P8` 逐項判 ＋ 十一閘 ＋ 施工單項數對帳

### H-1　預測

| # | 預測（逐字要旨） | 實得 | 判 |
|---|---|---|:--:|
| `P1` | §Z 二塊 bytes／LF／sha256 逐塊相符 ⇒ **2／2**；定義節標題合計 **2**；`docs/rulings/` 變更 **0 檔** | 二塊逐位全中；定義節標題 **2**（各 1）；`docs/rulings/` 變更 **0** | ✅ **成立** |
| `P2` | **強·判別力·可證偽**：第一輪之 `②-宗` 破量**仍 > 0**；🔒 負對照須得 `R2 45.9766`／`R5 56.3293` | 殘餘 **4 對 / `102.3059 ㎡` > 0**；負對照 **`45.9765888243`／`56.3292619405`** 相符 | ✅ **成立**（且**更強**：影響 ＝ 0） |
| `P3` | **強·可證偽**：2 宗不配地之面積合計 ＝ **`4.7950 ㎡`**；其餘各宗之位置與面積**逐位不變** | `4.7950 ㎡` ✅；其餘 57 宗已逐宗全列<br>🔒 「逐位不變」係**構造保證**（未觸幾何）·其**判別器 ＝ 負對照**（已過） | ✅ **成立** |
| `P4` | **強·判別力·可證偽**：違反之 2 宗其 `G₁ <` 藍影面積 | 🛑 **⛔ 無法判**——其受詞（藍影面積）**未定義**（A-2-1 不符 ⇒ 停機④） | ⚠️ **未答**（🔒 **⛔ 非否證**） |
| `P5` | **強·可證偽**：claude.ai 之藍影定式**與 KL 圖說相符**；⛔ 不符 ⇒ 停機上呈 | 🔴 **不符**（`B1` vs `B2`·`‖B1−B2‖ ＝ 18.137729 > 0`·`.lin` 直連段 `(20,18)`） | 🛑 **否證 ⇒ 停機上呈** |
| `P6` | **強·判別力·可證偽**：`run_verification.py` 之遞補命中中 **≥ 1 處⛔ 非位次遞補**；🔒 `F.1 遞補整形` 須被判為「⛔ 非」 | **17／17 皆⛔ 非位次遞補**（≥1 ✅）；`F.1 遞補整形` 7 處**皆判否** ✅；🔒 陽性對照 ✅ | ✅ **成立** |
| `P7` | **強·可證偽**：全倉之容差級分類閘 **≥ 2 支**；判別力對照（`_PAR_TOL = 1e-6`）須被命中 | 入判定鏈 **279 支**（≥2 ✅）；`_PAR_TOL` 消費點由**補掃**命中 **2 處** ✅（射程限制已具名） | ✅ **成立** |
| `P8` | `app.py` blob 仍 `a9e5671d…`；`data/`／`docs/rulings/`／`verify/baselines/` 各 **0**；`verify/**.py` 變更 **2**；`run_all` ＝ **15**（第十七法） | 逐項相符（見 H-2 閘 5／7） | ✅ **成立** |

⚠️ `P2`〜`P7` 係 claude.ai 之預測，其否證⛔ 不記 CC 之錯。
🔒 **`P5` 之否證 ＝ 停機事由** ⇒ 本批已**停機於 A-2-2**、⛔ 未逕自續判。

### H-2　十一閘

| # | 閘 | 期望 | 實得 | 判 |
|---|---|---|---|:--:|
| 1 | 純追加稽核（入倉前·基座 `2cb0fe9`） | `rc=0` | **`rc=0`** | ✅ |
| 4 | `TARGETS` 追加（先現查·只補未在列者） | 只追加 | `73 ⇒ 74`；**移除 ＝ []**；新增 ＝ `['docs/reports/W-G.9-91_乙之shim與藍影對G.md']`；既有 73 項逐元素全在新表 | ✅ |
| 2 | 變異測試（**先證在母體內**·舊文**行中**插字） | `rc=1`·具名 | 受詞 `W-G.9-89_…md`（在 `TARGETS` 中 ＝ True·變異前 status 空）；byte **475** 插 1 byte `X` ⇒ **`rc=1`**·具名 | ✅ |
| 2′ | 變異復原 | `git diff` 0 行·`rc=0` | `git diff` **0 行**·bytes 復原 `39072`·**`rc=0`** | ✅ |
| 1′ | 純追加稽核（入倉後） | `rc=0` | **`rc=0`** | ✅ |
| 3 | `numstat` 刪除欄全 0（二法 ＋ 陽性對照） | 全 0 | 二法**皆 0**；陽性對照 **7 筆會咬** | ✅ |
| 5 | `app.py` blob ＋ `verify/**.py` ＋ `data/` ＋ `docs/rulings/` ＋ `verify/baselines/` | `a9e5671…`／**2**／**0**／**0**／**0** | `a9e5671d64d254907a0396f898f046d9d85e8283`（HEAD ＝ 工作區 hash·`diff` 空）／**2**／**0**／**0**／**0**；`V6DXF` **逐字未變** | ✅ |
| 6 | `py_compile`（二檔） | 全過 | 二檔 `rc=0` | ✅ |
| 6′ | 位元組層複查·附對照組 | 0 ＋ 對照命中 | 二檔 blob `CR ＝ 0`；對照組 `data/V6.dxf` blob `CR ＝ 12308` ⇒ **會咬** | ✅ |
| 7 | 🔒 `run_all` ⛔ 不跑（逸出條款逐項現查） | 未跑 | 四項逸出條款**全部不觸發**；`run_all.py` `diff` **空** | ✅ 未跑 |
| 8 | `git status --porcelain` 全樹（前後各一次） | 無非預期 | 前：**空**；後：**5 項**（2 `M` 受詞檔 ＋ 1 `M` `wg942_append_audit.py` ＋ 2 `??`：新探針、log）⇒ 皆預期 | ✅ |

🔒 **第十七法 ＝ 檔案系統交叉法**（⛔ 與既用十六法皆不同族）：

> 既用之法**皆自 `run_all.py` 之<u>語法結構</u>出發**（① 全檔文本／④⑥ 常數池／⑫ 運算元／
> ⑬ `co_lines()` 行號區間／⑭ AST 四元位置×切片／⑮ `ast.unparse` 往返／⑯ `For.iter.elts` 子節點計數）。
> 🔴 **本法反向**：母體**自檔案系統（`git ls-files verify/`）出發**，判準 ＝ 其 `verify/` 相對路徑
> 是否以**加引號之字面**出現於 `run_all.py` ⇒ ⛔ **不解析 `run_all.py` 之任何語法結構**。

- 母體 ＝ **188 檔**；命中 ＝ **16**。
- 🔒 **受詞之精確化（⛔ 不調參湊數·二數皆對·受詞不同）**：本法之 **16** ＝「任何被引用之 `verify/*.py`」；
  `P8` 之 **15** ＝「**受測夾具**」（＝ `for _fx, _what in (…)` 之成員·由 `:181`
  `subprocess.run([sys.executable, os.path.join(HERE, _fx)])` 起）。
- 剔除判準（**正面列舉**）：該檔名之**全部**出現行**皆**含 `_candidates_append_keys`
  ⇒ 剔除 **1**：`selection_pipeline.py:36`（`_sel = _candidates_append_keys(os.path.join(HERE, "selection_pipeline.py"))`）。
- 🔒 判別力：剔除器**非全剔**（未剔者 ＝ **15**）；本法**非恆命中**（188 檔中未命中 **172**·三具名 ＋ 一假檔名皆 False）。
- ⇒ **受測夾具 ＝ 15**（`P8` 期望 **15**）✅

### H-3　施工單項數對帳（🔒 逐項打勾·量詞三對齊自校）

| 節 | 項數 | 逐項 | 辦畢 |
|---|---:|---|---:|
| §零 | **2** | ① `TARGETS` 追加 ✅　② 新增 `probe_WG991_yishim.py` ✅ | **2** |
| §一 | **9** | ⓪ ✅　⓪′ ✅　⓪″ ✅　① ✅　② ✅　⑥ 指令逐字 ✅　⑦ 已答／未答（分層·每層分母）✅　⑧ 編號現查 2 列＋判別力 ✅　⑨ 反向檢五錨＋**四款**停機 ✅ | **9** |
| §二 | **7** | A-0 ✅　A-1 ✅　A-2 🛑 **停機**（A-2-1 辦畢·A-2-2 依令不辦）　A-3 ✅　A-4 ✅　A-5 ✅　A-6 ✅（九款逐款未違） | **7** |
| §三 | **5** | 前置三道 ✅✅✅ ＋ `Z-1`／`Z-2` 入倉 ✅✅ | **5** |
| §四 | **8** | `P1`〜`P8` 逐項判 ✅ | **8** |
| §五 | **11** | 閘 1／4／2／2′／1′／3／5／6／6′／7／8 ✅ | **11** |
| §六 | **13** | `§0`／`§A`／`§B`／`§C`／`§D`／`§E`／`§F`／`§G`／`§H`／`§I`／`§J`／`§K`／`§L` | **13** |
| §七 | **15** | 1–7 逐條未違；8–14 常設（同 `-90` §七）；15 節 110＋`自誤 92`／`93`／`94` 之六款 ✅ | **15** |
| **合計** | **70** | | **70** |

🔒 **量詞三對齊自校**：結論句 N ＝ **70** ／「項數」欄合計 ＝ `2+9+7+5+8+11+13+15` ＝ **70** ／
「辦畢」欄合計 ＝ **70** ⇒ ✅

🔒 **§七 之逐條未違（抽四條具名）**：
第 2 條「⛔ 不建任何介面、⛔ 不移任何線」⇒ §C-3 之負對照已證**未隱含移線**；`verify/**.py` 變更 2 檔皆為量測物。
第 6 條「⛔ 不得就遞補／調配池／合併調配／超配出裁定題」⇒ §E-4 之**未答**係**實證結果**、⛔ 非出題；
第 6 條（新）「⛔ 不得另立藍影之第二定式」⇒ §D-4 已明言**不算**、⛔ 未以 `B2` 版改式；
第 7 條「凡出艙『0 例外』須同批出艙最接近翻面之反例及其餘裕」⇒ §E-2 之**陽性對照**即該具名。

---

## §I　CC 本批之自誤（🔒 **四則**·皆本批自察並更正）

1. 🩸 **f-string 之分層漏判**：首版分層器只認 `tokenize.STRING`，
   而 **Python 3.12 起** f-string 拆為 `FSTRING_START`／`FSTRING_MIDDLE`／`FSTRING_END`
   ⇒ **f-string 內之字樣被誤判為「可執行」**（`run_verification.py:823`／`:825` 即此）。
   🔒 實測（本機 **3.13.11**）：`x = f"abc遞補{y}def"` ⇒ `FSTRING_MIDDLE 'abc遞補'`（⛔ 非 `STRING`）。
   ⇒ 修為**正面列舉** `STRING` ＋ `FSTRING_*` 三者（`layers_of()`）。
   🔴 **後果之量**：`A-3` 之可執行層由 **4** 改為 **0**；`A-4` 之可執行層由 **355** 改為 **342**。
   🔒 **若未捕**，`A-3` 會報「可執行 4」⇒ 讀者會以為**確有 4 處可執行之遞補引用**。

2. 🩸 **A-1-2 之 57 列漏印**：首版只算 `nrest` 而**未印任何一列**，
   而施工單 A-1-2 逐字為「🔴 **逐宗出艙**其餘各宗之位置與面積」。⇒ 補為全列 ＋ `POPULATION/PRINTED`。

3. 🩸 **第十七法之子篩選器壞了（`-1`）**：首版以「該行是否自含 `subprocess`」剔除非夾具，
   得 **受測夾具 ＝ −1**——**⛔ 不可能之數** ⇒ ⛔ 不採信自己的數、當場追因：
   夾具名列於 `:105-178` 之 tuple，而 `subprocess.run` 在 `:181` 用**變數** `_fx`
   ⇒ **無一行自含 `subprocess`** ⇒ 全部被誤剔。
   ⇒ 改以**正面列舉該消費函式之名**（`_candidates_append_keys`·由 `:33-39` 讀得）⇒ 剔除 **1** ⇒ **15**。
   🔒 **抓到它的唯一理由 ＝ 那個數是負的**——`0` 尚可能被誤收，`−1` 不能。

4. 🩸 **`≥3dp` 機檢之陽性對照<u>失效</u>（自我污染）**：本批首跑機檢，其**陽性對照**
   （人造一個「必查無」之數）竟得 **`True`**——即**該檢不會咬**。
   🔴 **追因**：`W-G.9-90` 之哨兵字面被 `VR-050` **記功五逐字收錄**入
   `docs/驗證裁定登記表.md`（本批 `Z-1` 剛入倉之塊），⇒ **上一批之哨兵於本批已成倉內文字**。
   ⇒ 改以**執行期組出**之哨兵（⛔ 其字面不落入本報告、log、或任何倉內檔）⇒ 對照回復 `False` ✅。
   🔒 此即 memory 之既有戒（**母體含本檔自身 ⇒ 自我污染；哨兵字樣須執行期組出**）之**再次坐實**
   ——🔴 **而本次之污染源⛔ 不是自己的 log，是<u>對方把我的哨兵寫進了裁定</u>**（新形狀）。

---

## §J　未涵蓋之事（⛔ 逐項具名·⛔ 不以「未涵蓋」吸收漏辦）

1. 🛑 **`A-2-2`（藍影面積 vs `G₁`）⛔ 未辦**——停機④。`P4` **未答**（⛔ 非否證）。
2. 🔴 **「乙」之最終土地後果⛔ 未量**——其**必須含遞補**（`VR-050` 四），而遞補介面
   **全倉可執行層 ＝ 0** ⇒ ⛔ 無從 shim。本批之數**全為第一輪**。
3. **情境母體 ＝ 僅 `0m`**（同 `-88`／`-90`·⛔ 不擴）——`3.5m` 之「乙」後果**未量**。
4. **`A-3` 之結論係<u>字樣法</u>**：證「`遞補` 二字未出現於任何可執行行」，
   ⛔ **不證**「無**異名同義**之機制」（`-89` §E-3 之**逆**）。
5. **`A-4` 之射程為<u>下界</u>**：主樣式抓不到「具名常數 ＋ 他處比較」之閘；
   補掃（含 `TOL` 之全大寫識別字）得 **69 處**，惟**未含不以 `TOL` 命名之容差常數**。
6. **`A-4` 之「入判定鏈」判準為<u>行內字樣</u>**（`raise`／`if ` 等）
   ⇒ ⛔ **未做資料流分析** ⇒ 其 **279** 為**上界**（符 A-0 之偏向多報）。
7. ⛔ **未出艙「應改領現金之宗」**（`W-G.9-40` §C-4 之禁令仍有效）。
8. ⛔ **未提任何落地或重烤之排程建議**（KL 已裁）；⛔ 未就遞補／調配池／合併調配／超配出任何裁定題（`K-9-11` 三）。

---

## §K　波末佇列現況（🔒 含 KL 2026-08-20 之排程裁示）

**KL 逐字**（`GB-67` 加註·`W-G.9-90` §K）：

> **先落乙，把最基本的幾何分配位置及方式確定，才做後續甲矩形容納**
> **落地前，先以 shim 量出各自的土地後果**

| 項 | 狀態 |
|---|---|
| `W-G.9-90` | ✅ 已完成（換圖不翻動任何判定；重烤射程 **72 檔**） |
| `W-G.9-91`（本批） | ⚠️ **部分完成**：A-1／A-3／A-4／A-5 辦畢；🛑 **A-2 停機**（藍影定式不符） |
| **乙**（射程 **26 宗**·本批複驗） | 🛑 **⛔ 無法完成 shim**——① 藍影未定義 ② 遞補介面全倉可執行層 ＝ 0 |
| **甲**（`K-9-12` 矩形容納·射程 **51 宗**） | 🛑 乙之後 |
| **波末批**（換圖＋換快照＋重烤·射程 **72 檔**） | 🔒 三者鎖成一組（`K-6:1651`／`:1753`／`:237`）·時點 ＝ `K-8` 全案完成後 |
| 高水位 | `VR-050`／自誤 **94**／考古節 **110**／正典 `K-9-15` |

---

## §L　交下一窗之三件

1. 🛑 **停機上呈（第一項）：藍影之定式須由<u>發單側或 KL</u>給出，⛔ 不由 CC 定。**
   本批已備**機械證據**：`B1 ＝ pt20 (78.486, 54.913)`／`B2 ＝ pt18 (82.221, 72.662)`／
   `‖B1−B2‖ ＝ 18.137729`／`.lin` 直連段 `(20,18)`／KL 圖三條件與藍字之逐字。
   ⛔ **本批⛔ 未提出任何替代定式**（§七-6）。

2. 🔴 **「乙」之落地前提<u>尚缺一項</u>**：`VR-050` 四明訂「乙」之正確落地 ＝ 判準 ⇒ 不配地 ⇒ **遞補**，
   而本批實證 **遞補介面於全倉可執行層 ＝ 0**（71 行命中全在註解／字串）。
   ⇒ 🔒 **`W-G.9-89` 之 `【已有】` 應更正為 `【缺】`**——⛔ 惟該更正屬**登記表之受詞**、⛔ 非 CC 自改。
   ⚠️ 併記本結論之射程：字樣法⛔ 不證「無異名同義之機制」（§J-4）。

3. 🔴 **容差級分類閘之普查已備**：入判定鏈 **279 支**（`1e-9` 140／`1e-6` 82／`1e-12` 64 為前三），
   已逐處全列於 log。🔒 **其為上界（判準為行內字樣）且射程為下界（主樣式漏具名常數）**
   ——二者方向相反、**已分別具名**。⛔ 本批**只列不改·不重驗**。

---

---

## 🔒 附　本報告之機檢（⛔ 憑記憶出艙者一律不寫）

**受詞**：本報告內每一個 **`≥ 3dp`** 之數，須能於**倉內或本批 log** 查到。
**乾草堆**：`probe_WG991_yishim_2cb0fe9.log`／`宗地分配線邏輯.{cnt,lin}`／其 `PROVENANCE`／
`probe_WG988_nocross_3c88ece.log`／`probe_WG990_v61delta_3c88ece.log`／`probe_WG983_k99prep_bee1abf.log`／
`驗證裁定登記表.md`／`自誤登記.md`／`K-6`／`run_verification.py`／`run_all.py`／`stepg_pipeline.py`／
`probe_WG991_yishim.py`／`.gitattributes`。

| 項 | 值 |
|---|---|
| 相異之 `≥3dp` 數 | **30** |
| **查無** | **0** ⇒ ✅ **全部逐一查得**（⛔ 無豁免項） |
| 🔒 判別力（陽性對照·**執行期組出**之哨兵） | 於乾草堆 ＝ **`False`** ⇒ 該檢**會咬** ✅ |

🔒 **刪除欄全 0**（閘 3·二法＋陽性對照 7 筆會咬）；
**`app.py`／`data/`／`docs/rulings/`／`verify/baselines/` 皆 0 檔**（閘 5）。

---

🔒 **一句話**：那 2 宗拿掉之後，破量**一格都沒動**（破量對根本不含它們）；
而要往下走的兩件事——**藍影怎麼定義**、**遞補在哪裡**——
一個**與 KL 的圖不符**（停機），一個**在倉裡連一行可執行碼都沒有**。
