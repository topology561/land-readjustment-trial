# W-G.9-232R　`v3` §7-4 排序款之更正標記 ＋ `wf_f2` 容量過濾補查（執行報告）

**批次性質** ＝ **零生產碼**（生產碼五檔一位元組未動）。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 取 `subprocess.run(...).returncode`（`GB-145`）；
⛔ heredoc（`GB-143`）；對照組字樣⛔ 落入本文（`GB-147`·以代稱行之）。
🔒 **單所載之一切檔列（`v3:427`／`wf_f4:689`／`wf_f2:161–165`）皆<u>當場復現</u>**，⛔ 採單所載為錨。
🛑 本報告含**單側不符 `2` 處**與 **CC 側器紅自捕 `2` 處**；**⛔ 鑄任何 `自誤`／`GB`／`VR`／`戒`／`K-9` 號**。

---

## 工項零　✅ **PASS**（本單原封入倉）

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **4405** | 66 | **0** | `5aceb836884ab510390be64dfa5110e93f720266e45489e3846326a05c60e253` |
| 入倉（索引側 blob `4f6b1638…`） | **4405** | — | — | `5aceb836884ab510390be64dfa5110e93f720266e45489e3846326a05c60e253` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）。入倉 commit ＝ `c5e29d0`；`deletions` ＝ **0**。
🛑 **射程界限（`作業常規之追加四` 自載）**：該 `sha256` 只證「**落檔 → 入倉**」，**⛔ 證「聊天原文 → 落檔」**。

### 🛑 號衝突之處置（**逐字回報**·⛔ 由 CC 自裁其餘）

**受詞**　本單自載之號 ＝ `W-G.9-231`，而該號於倉內**已被占用**。

🔒 **占用之現查**（母體 ＝ 追蹤 `.md` **562** 檔·倉側 blob `4fe2b44`·**檔框 ＋ 列框**並報）

| 號 | 檔框 | 列框 | 占用者 |
|---|---|---|---|
| `W-G.9-231` | **2** | **7** | `docs/reports/W-G.9-231_CC交接文.md`／`docs/reports/W-G.9-231R_開窗態錨復驗與次號重算_執行報告.md` |
| **`W-G.9-232`** | **0** | **0** | —— |
| `W-G.9-233` | 0 | 0 | —— |

🔒 **對照組（`常規五`·兩造）**：器須綠 `W-G.9-229` ＝ **6** 檔／`W-G.9-230` ＝ **3** 檔；
器須紅（一必不存在之同形號·代稱）＝ **0** 檔 ✅。

**依據**　`CLAUDE.md`「工作紀律」（字樣錨 `編號衝突一律以倉內為準`·KL 立 `2026-08-03`）逐字：

> **施工單所載編號與倉內衝突時，一律以倉內既有編號為準；新則取下一個未占用號。
> ⛔ 禁為配合施工單而改編既有正典編號。**……正確處置是**改新則之號並回報**，**不是改舊則**。

**處置**　本單以 **`W-G.9-232`** 入倉、本報告命名為 **`W-G.9-232R`**；
🔒 **單之內容一字未改**（含其標題與工項零內自載之 `W-G.9-231` 字樣）——`⛔ 改一字` 之受詞係**內容**。
🛑 **其餘處置（是否另有安排、是否鑄號、由何側鑄）＝ 意思決定 ⇒ 候發單側／KL**，⛔ 由 CC 自裁。

### 🩸 CC 側器紅自捕（其一·**⛔ 出艙錯值**）

本批首次所用之零對照 `W-G.9-999` **於倉內實有**（**檔框 2** ＝ `CLAUDE.md` ＋ `W-G.9-221R` 報告·
係既有哨兵）⇒ **⛔ 得為零對照**；該次量測依 `W-G.9-14` 修法 `1` **⛔ 出艙**，已換一必不存在之同形號重跑。

---

## 工項一　`v3` §7-4 排序款之更正標記

### `a`　三處逐字之當場復現 ✅

🔒 **受詞之三軸（`VR-091 一`）**：來源 ＝ `blob@4fe2b44`（索引側）／母體逐檔載於下表／粒度框 ＝ **列框**。

| # | 源檔（母體） | 字樣錨（**⛔ 以行號為錨**·`行號衛生 1`）| 逐字 |
|---|---|---|---|
| `1` | `docs/配地計算總規格_v3.md`（`blob 921a7f4a…`·**40446** B／**643** 列）| `重劃前總地價高者先調配`（命中 **1**·於 `4fe2b44` 為 `:427`）| `**排序**：多個歸戶搶同一最近街廓時，重劃前總地價高者先調配。` |
| `2` | `docs/specs/W-G.4_KL域裁_兩階段落位_部分面積拆分_Rw側選.md` | `裁定K＝B-4 結案`（命中 **1**·於 `4fe2b44` 為 `:65`）| `**【裁定K＝B-4 結案·道路用地優先分配之本質】**（KL 2026-07-23·claude.ai 倉內坐實）` |
| `3` | `verify/wf_f4.py` | `三處消費`（命中 **1**·於 `4fe2b44` 為 `:438`）| ``#   ⇒ `value`（Σa×重劃前區段單價）三處消費**全數退場**、不再計算（原行號已朽·勿引）。`` |
| `4` | `verify/wf_f4.py` | `裁定K：距離優先`（命中 **1**·於 `4fe2b44` 為 `:689`）| `gs = sorted(requests[blk], key=lambda g: (dists[(g, blk)], g))   # 裁定K：距離優先` |

🔒 **裁定K 之 operative 段逐字**（同檔·緊接 `2` 之下）：

> **泛用推論（恆真·非本案特例）**：能進**第四順位調配佇列**之道路用地，依定義**已通過「兩側皆無」之篩**
> ⇒ 該佇列**恆為單一類別** ⇒ **裁定D 之「類別優先」在此永不觸發**。
> ⇒ P5 實作＝**單一類別佇列、純距離優先、每筆可部分面積**。

### 🔴 **單側不符（其一）**：`pre_price` vs `value`

**單 工項一 `b` ②** 逐字令標記載「**`pre_price` 三處消費全數退場之事實**」。
🔒 **當場復現否證之**：碼側自陳（上表 `3`）所名之退場者 ＝ **`value`**（其定義 ＝ Σa×重劃前區段單價），
**⛔ `pre_price`**；`pre_price` 於 `verify/wf_f4.py` 之**列框**命中 ＝ **10**，仍為 a′ 折算（模式一／模式二）
之**現用輸入**、⛔ 退場。
⇒ 依「**數字錨引倉檔**……與倉態衝突**以倉為準**」，標記所載者取**碼側之 `value`**，
並於標記內**同格具名二者之別**（⛔ 混同）。🛑 **⛔ 改單一字**；本節即其逐字回報。

### `c`　既例之確認（**先於 `b` 辦**）

**單所令之檢查** ＝ 「先 grep 確認 `v3` 有無『⛔ 上文一字不刪』型宣告」。

#### 🩸 CC 側器紅自捕（其二·**否定性宣稱之母體取窄**）

首跑以**九變體**窮舉（`上文一字不刪`／`上文原句一字不刪`／`一字不刪`／`純末端追加`／`純附加`／
`一字未刪`／`append-only`／`⛔ 改本文`／`不追改`）得**全部 `0`**（對照組 `調配` ＝ **82** 列／
一必不存在之字樣 ＝ **0** 列 ⇒ 器**表面**非紅），據以擬斷「`v3` 無 append-only 型宣告」。
🔴 **該斷言為偽·當場自捕**——`v3` **確有**同義宣告，其措辭為本檔自身之語：

> `v3` 檔頭（`K-8 §五-2`·KL 裁 `2026-07-31`）末句逐字：**以下為歷史文件·內文不改**。

**機制**　變體集係自 `CLAUDE.md`／登記表之措辭外推，**未涵蓋受詞檔自身之措辭** ⇒
**否定性宣稱之母體取窄**（同族 ＝ 本倉量測器族之母題）。
**修法（自加之戒·⛔ 鑄號）**：凡對**某一檔**下否定性存在宣稱者，變體集須**先自該檔之既有措辭取樣**
（如掃其標題／檔頭之全部祈使語），⛔ 僅以他檔之措辭外推。**⛔ 出艙錯值**（該斷言未入任何倉檔）。

#### 判　**既例<u>容</u>於 `v3` 之檔內體例 ⇒ ⛔ 停，續辦 `b`**

🔒 **既例之逐字**（`CLAUDE.md`·字樣錨 `另置之標記行／末端追加之新節`）：

> 「⛔ 動舊處一字，於**另置之標記行／末端追加之新節**內宣告其不再適用」

🔒 **相容之三證**
1. `v3` **自身已有同形先例**——檔頭 `K-8 §五-2` 之退場標記（宣告某值「已退場／作廢／**不再作為任何驗收對拍基準**」）＋「**內文不改**」。
2. `v3` 之結構本即**末端追加**：`附 A`／`附 B`／`附 C`（「已知缺口與死碼掃描清單（記載待修；**KL 2026-07-05 裁示新設**）」·其 `C-1`〜`C-5` 逐次追加）／`附 D`（`2026-07-08` 追加）。
3. 既例之二形（**標記行** ／ **末端追加之新節**）**皆**為倉內既例所承認 ⇒ 本批取後者。

🔒 **節名之取法（⛔ 觸 `常規三` 之提案義務）**：⛔ 取 `附 E`（將構成新**節號**之意思決定），
改採 `戒 36` 所定之**非鑄號形**（`🔧` 起首）⇒ 標題 ＝
`## 🔧 §7-4「排序」一款之**更正標記**（\`W-G.9-232\`·⛔ 上文一字不刪·純末端追加）`。

### `b`　標記之落地 ✅ **三項齊備**

| 項 | 單所令 | 落地處（標記內之節） | 判 |
|---|---|---|---|
| `①` | 該款已由裁定K（KL `2026-07-23`）取代為**純距離優先** | `### ① 該款已由**裁定K** 取代為**純距離優先**`（含裁定K 逐字） | ✅ |
| `②` | 碼側現況（排序 key）＋ 三處消費全數退場之事實 | `### ② 碼側現況（**事實·⛔ 評價**）`（四列字樣錨表 ＋ **`value` vs `pre_price` 之精確界**） | ✅ |
| `③` | 本標記為**部分**更正，⛔ 得讀為 `§七` 全節失效 | `### ③ 🛑 本標記為**部分**更正`（逐列具名未受影響之五款 ＋ 「合併重劃前總地價」式之**存廢未經裁**併記） | ✅ |

#### 落地之機驗（**三閘全綠**）

| 閘 | 內容 | 實測 |
|---|---|---|
| ① **嚴格前綴** | 改後全檔須以基座（`blob@4fe2b44` 之 `v3`）全檔為**嚴格前綴** | 基座 **40446** B／改後 **44494** B／Δ **+4048** ＝ payload **4048** B（同格並報二端·`戒 40`）；`startswith` ＝ **True**；`CR` ＝ **0** ✅ |
| ② **逐字保真**（`常規七 三`） | 標記所引之**四**處逐字，與其源檔**逐位** `in` 比對（⛔ 僅比三數） | **4／4 相符** ✅ |
| ③ **`GB-132` 零污染** | 新節標題⛔ 落入任何號類之定義列式 | 自誤定義框／自誤範圍框／`GB` 標題錨定框／`VR` 定義列式／`K-9` 定義列式 **命中皆 `0`**；對照組（一真定義形）＝ **1** ✅ |

🔒 **`:427` 一字未動**：`diff <(基座 :427) <(改後 :427)` ⇒ **空**（`rc` ＝ 0）。
🔒 **`v3` 之 `LF` 列數 `643 → 711`**（Δ **+68**）；**⛔ 動既有 `643` 列任一位元組**（由 ① 之嚴格前綴機驗）。

🛑 **器之射程（照實·⛔ 頂替）**：`常規一 ②` 補款所指名之機檢器 `verify/tools/wg942_append_audit.py`
其 `TARGETS` **⛔ 含 `docs/配地計算總規格_v3.md`**（字樣 `配地計算總規格_v3` 於該器命中 **0**）
⇒ **該器對本批無受詞**；本批以**自寫之嚴格前綴機驗**代之（受詞 ＝ `blob@4fe2b44` 之 `v3`，
逐位 `bytes.startswith` ＋ payload 長度相等，見上表 ①）。⛔ 讀為「已過 `wg942`」。

---

## 工項二　`wf_f2` 之容量過濾補查

🔒 **母體**：`verify/wf_f2.py`（`blob 9cdff738…`·**16220** B／**292** 列·**列框**）；⛔ 評價、唯讀出艙。

### `a`　選定目標街廓時之容量／池量／超配過濾 ⇒ 🔴 **【查無】**

**選定之唯一實作** ＝ `_decide`（字樣錨 `grep -n "def _decide" verify/wf_f2.py`·於 `4fe2b44` 為 `:76`），逐字：

```
    blks = byg[gid]
    qual = {}
    for blk, rows in blks.items():
        if any(float(r["G(㎡)"]) >= mina[blk] and not r["畸零地旗標"].strip() for r in rows):
            qual[blk] = sum(float(r["G(㎡)"]) for r in rows)
    if not qual:
        return None
    tgt_blk = max(sorted(qual), key=qual.get)               # 並列取字典序（決定性）
```

🔒 **判**：`qual` 之入選條件 ＝「該塊**已有**一宗 `G ≥ mina[blk]` 且非畸零地」——係**達標**判準
（源自 `7-1` 級 0「已達標集中」之受詞），**⛔ 容量判準**；`tgt_blk` 取 `ΣG` **最大**者，
**⛔ 涉池量**。

🔒 **字面窮舉（列框·先窮舉再斷言）**

| 字樣 | `wf_f2.py` 命中 |
|---|---|
| `budget` | **0** |
| `cap`／`capacity` | **0**／**0** |
| `超配` | **0** |
| `餘裕`／`remain`／`容量` | **0**／**0**／**0** |
| `不足` | **0** |
| （對照組·器須綠）`池` | **14** |
| （對照組·器須綠）`pool` | **22** |
| （對照組·器須綠）`mina`／`MinA` | **8**／**8** |

🔒 **`poolB` 之射程**：其於 `:130` 讀入（`池總=幾何剩餘(㎡)`），惟 **`_decide` ⛔ 取用**，
其**全部**消費點皆在**灌入之後**（`:196` 起之「池流向＋三則」／`:213` 起之跨街廓守恆）
⇒ **池量⛔ 進入選塊決策**。

🔒 **級別之時序（🔴 與 `7-4` 之關鍵相異）**：`級1相鄰`／`級2鄰近`／`級3非鄰近` 之指派
在 `_decide` **之後**（單所載 `wf_f2:161–165` 之當場復現）：

```
            if frozenset((sblk, tgt_blk)) in adj:
                lvl = "級1相鄰"
            elif cd <= med:
                lvl = "級2鄰近"
            else:
                lvl = "級3非鄰近"
```

⇒ 🔒 **級別係<u>標記</u>、⛔ 選擇準據**——目標塊已由 `_decide` 定，級別僅事後貼標。

### `b`　與 `7-4` 之逐項對照

🔒 `7-4` 側之受詞 ＝ `verify/wf_f4.py`（字樣錨見下·⛔ 採單所載之 `:691–:698`）。

| 維度 | **`7-1` 級 1/2/3（`wf_f2`）** | **`7-4` 3 級調配（`wf_f4`）** |
|---|---|---|
| 選塊準據 | `_decide`：**達標塊中 `ΣG` 最大者**（並列取字典序） | 逐輪自 `border[gid]` 依序取**首個 `_usable` 為真**之塊（`R4` 政策跳過） |
| 級別之角色 | 🔴 **事後標記**（`_decide` 之後貼） | **選擇準據**（距離 → 級2/3 門檻 ＝ 距離中位數 `med`） |
| 排序鍵 | **⛔ 有**（`F2_GROUPS` 之遍歷序 ＋ `_decide` 內之 `max(sorted(qual))`） | `sorted(..., key=(dists, g))`（**裁定K：距離優先**·字樣錨 `裁定K：距離優先`） |
| **可灌量（budget）** | 🔴 **【查無】** | `_budget(blk)`（字樣錨 `可灌量＝可達池`）逐字：`return _reach(blk) - (mina[blk] + E1_MARGIN)`；`_reach` ＝ `eng.pool(blk) - unreach.get(blk, 0.0)` |
| **事前可用性閘** | 🔴 **【查無】** | `_usable(g, blk)` 逐字：`if _budget(blk) < mina[blk] - 0.5: return False, demG`（＋小片走 `_trial` 實測） |
| **比例分攤** | 🔴 **【查無】**（`inject[…] += m1` 無條件累加） | `shares = ({g: demG[g] …} if tot <= budget + TOL else {g: budget * demG[g] / tot for g in gs})` |
| **`share < MinA` 退出** | 🔴 **【查無】** | `small = [g for g in gs if shares[g] < mina[blk] - 0.01 and shares[g] < demG[g] - 0.01]` ⇒ `gs.remove(max(small, key=lambda g: (dists[(g, blk)], g)))`（W-7 順解：剔佇列末位） |
| **剩餘感知（縮灌）** | 🔴 **【查無】** | `rem_after`／`need` 之縮灌（字樣錨 `剩餘感知`）＋「無次塊：不強塞」 |
| 事後守恆／三則 | ✅ **有**（見 `c`） | ✅ 有（`cons_resid`／`verdict_all_green`·`wf_f4` 側同構） |

### `c`　「該街廓裝不下」之現行行為 ⇒ **無事前過濾；先無條件灌入，再由<u>事後</u>閘捕捉**

🔒 **灌入之逐字**（無條件）：`inject[tgt_row["暫編地號"]] += m1`
🔒 **⛔ 跳過**：`_decide` 回 `None`（無達標塊）之分支係 **`raise`**、⛔ skip——逐字：
`raise RuntimeError(f"🔴 [{tag}] {gid} 無達標塊，不應在 F.2 名單（應 F.4）")`
🔒 **⛔ 靜默灌入**：灌後有**五重**事後閘（前四者 `raise`、第五者為 harness 之 PASS/FAIL 層）

| # | 位置（字樣錨） | 逐字／內容 | 形 |
|---|---|---|---|
| `1` | `run_step_g(...)` 之內（`wf_f2` 於灌後重跑 Step G） | 其自身之結構閘（`telescoping`／`②-宗 圍堵閘` 等） | `raise` |
| `2` | `停機②：池落 (0,MinA)` | `raise RuntimeError(f"🔴 [{tag}] 停機②：池落 (0,MinA) 開區間：{mid_state}")` | `raise` |
| `3` | `純源塊`／`純目標塊` | `純目標塊 {l} 池未減（Δ=…）：吸收 a′ 應減池`（`Δ > 0.5` 時） | `raise` |
| `4` | `灌後 G=` | `raise RuntimeError(f"🔴 [{tag}] 目標宗 {tid} 灌後 G={r['G(㎡)']}<MinA_{r['所屬街廓']}")` | `raise` |
| `5` | `cons_resid`／`verdict_all_green` | 匯出為 anchors，由 `verify/run_verification.py`（字樣錨 `F.2 結構永久閘`）判 **FAIL** | **⛔ `raise`**·harness 判定層 |

🔴 **併記（事實·⛔ 評價）**：`tot_B`（`grep -n "tot_B" verify/wf_f2.py` ⇒ 命中 **1**）
**計算後⛔ 被任何處消費**。

### `d`　`K-9-9 四`（⛔ 超配）於 `wf_f2` 側之對應實作 ⇒ 🔴 **【查無】**（惟有**間接**之守恆捕捉）

🔒 **`K-9-9 四` 逐字**（`docs/rulings/K-6_街角地分配程序與可分配判準.md`·字樣錨 `四、不合格之處置`）：

> **四、不合格之處置**：依該宗 G 值算出之遠側界位置若在起算垂線**之前**
> ⇒ **該宗不配地**，走**合併／調配機制**；重劃前投影順序中**下一位**之土地
> **遞補該位次**；騰出之地**入調配池**。
> 🔴 ⛔ **不得強制配到下限**（⛔ 不得超配）。

🔒 **受詞之界（照實·⛔ 頂替）**：`K-9-9` 之節題 ＝ 「**宗地形成之起算垂線約束**」（KL 裁 `2026-08-16`），
其受詞係**原位次宗地形成端**（沿 FRONTLINE 推進·遠側界 vs 起算垂線），**⛔ 跨街廓調配**；
同節 `六` 逐字並載「依 G 值公式原理，**必然產生配餘地**（＝調配池），⛔ **不會發生「塞不下」**」。
⇒ 🔒 「`wf_f2` 側之**對應**實作」係**類推**之問；本節據實分報「字面」與「效果」二層。

| 層 | 判 | 證 |
|---|---|---|
| **字面** | 🔴 **【查無】** | `超配` 於 `verify/wf_f2.py` 命中 **0**（對照組見 `a` 表） |
| **效果**（超配 ⇒ 池 < 0 ⇒ 違守恆） | 🟡 **間接有**——由**事後全塊級守恆閘**捕捉，**⛔ 事前逐宗之 ⛔超配 過濾** | 見下 |

🔒 **效果層之機制（當場坐實·⛔ 推測）**：`池總` 係**幾何量**——
`app.py`（字樣錨 `_pool_total_blk = float(sum(`）逐字 `_pool_total_blk = float(sum(_g.area for _g in offset_geoms))`
⇒ **shapely 面積恆 ≥ 0** ⇒ 「負池」**於資料上不可表示**；超配之顯形為
「池總 → 0 而 `ΣG` 超出街廓面積」⇒ 守恆殘差（`ΣG + 池總 − 街廓面積`·字樣錨 `_resid_wd2`）超容差
⇒ `判定` ＝ `'🔴 守恆破'` ⇒ `verdict_all_green` 為 `False` ⇒ `run_verification` 之
「F.2 結構永久閘（trunk C 守恆全綠）」**FAIL**。

#### 🩸 CC 側之假說被自證否證（**⛔ 出艙**·記其為方法）
本批中途曾擬「守恆閘對負池**結構性全盲**（因池總 ＝ 街廓面積 − ΣG 之導出量）」；
**當場查 `_pool_total_blk` 之賦值二處**（`app.py` 字樣錨 `_pool_total_blk = None`／`= float(sum(`）
⇒ 其為 `offset_geoms` 之**幾何面積和**、**⛔ 導出量** ⇒ **該假說為偽**，已於出艙前撤除。

---

## 工項三　`K-9-1` 之現況勘查（⛔ 實作·⛔ 提設計）

### `a`　`K-9-1` 之正典全文與入倉 commit

🔴 **首要事實**：`K-9-1` **⛔ 有獨立之正典節**——`^#+.*K-9-1([^0-9]|$)` 於
`docs/rulings/K-6_街角地分配程序與可分配判準.md` 命中 **0**。
其**全文即下列<u>表列二處</u>**（同檔·**列框**·`blob@4fe2b44`）：

| 處 | 所在節 | 逐字 | 入倉 commit |
|---|---|---|---|
| 一 | `## K-9 街廓層深度檢查與街角寬度實量（KL 裁 2026-08-01·canonical·逐字·**K-7 撤銷**）` 之 `### K-9 §一、五則裁定` 表 | `\| **K-9-1** \| 街角第 1 宗**不豁免** §零-2「街廓最小建築面積」 \| U-K7-1 定案（原為未裁） \|` | **`b061b00`**（`2026-08-01`·`文件(K-8 段四): K-9 五則入正典＋K-7 整條撤銷＋GB-10/11 登記＋只量不判 probe（零 AST 異動）`）|
| 二 | 同檔 `### K-9 §四、落地狀態` 表 | `\| K-9-1 ⬜ **未實作**（段六） \| §零-2 街廓最小建築面積之 UI 欄與判準（街角宗不豁免） \|` | **`c91550d`**（`2026-08-05`·`K-6-A2 段四**收官** ＋ 段五**開工偵察**（⛔ 零行為變更·app.py 與 verify/ 一行未改）`）|

🔒 **同表之上位宣告逐字**：`K-9 全七則**已裁定；實作屬 K-6-A2**。`／
`**段四／段五僅完成正典與登記，現行碼行為未變。**`

### `b`　`-224` 待落地正本表之對應列 ＋ 受詞同一性之判

🔒 **`CLAUDE.md`（字樣錨 `§零-2 街廓最小建築面積之 UI 欄與判準`·命中 **1**）之逐字**——
其位於「🔧 `K-6-A2` 之**部分廢止標記**（`W-G.9-224` 工項一）」之 `② 存續之四項` 表：

> `\| \`1\` \| **\`K-9-1\`** \| \`:1602\` \`\| K-9-1 ⬜ **未實作**（段六） \| §零-2 街廓最小建築面積之 UI 欄與判準（街角宗不豁免） \|\` \|`

🔒 **受詞同一性之判 ＝ 🔴 <u>不同</u>（後者⊋前者）·具名其別**

| | `a` 之受詞（`K-9 §一` 表·**裁定本身**）| `b` 之受詞（`-224` 引 `K-6:1602`·**落地物**）|
|---|---|---|
| 內容 | 「街角第 1 宗**不豁免** §零-2『街廓最小建築面積』」 | 「§零-2 街廓最小建築面積之 **UI 欄** ＋ **判準**（街角宗不豁免）」 |
| 性質 | **判準之內容**（豁免與否）| **落地之二物**（UI 欄 ＋ 判準）|
| 別 | ⊂ | 🔴 **多出「UI 欄」一物**——其⛔ 見於 `a` 之逐字 |

⇒ 🔒 **`b` 之受詞 ＝ `a` ∪ {UI 欄}**；二者**⛔ 同一**。⛔ 以其一代其二。
（併證：`W-G.7_K6-A2-段六_偵察.md` 字樣錨 `真正待補之 UI 欄是另一個` 逐字載
「**§零-2「街廓最小建築面積」**（per 街廓·`K-9-1` 所需）——`grep -n "街廓最小建築面積" app.py` ⇒
**1 命中且為註解** ⇒ **UI 欄確不存在**」·⚠️ 該數係彼時之態，本批⛔ 復算其值。）

### `c`　現行「臨正面道路寬度」UI 欄之完整結構（⛔ 提設計·⛔ 改一行碼）

🔒 母體 ＝ `app.py`（`blob 02cc6d73…`·**1417946** B／**23942** 列·**列框**）；行號繫 `commit 4fe2b44`。

| 項 | 值 |
|---|---|
| **UI 元件** | `st.number_input`（字樣錨 `"正面路寬 (m)", min_value=0.0,`·命中 **1**·該行於 `4fe2b44` 為 `:19355`；其呼叫塊 ＝ `:19354`–`:19358`）|
| **逐字** | `fw = ca1.number_input(` ／ `"正面路寬 (m)", min_value=0.0,` ／ `value=float(r.get('front_width', 8.0)), step=0.5,` ／ `key=f"f3_fw_{bid}"` |
| **widget key** | `f"f3_fw_{bid}"`（**逐街廓**·`bid = b['id']`·字樣錨 `bid = b['id']`）|
| **型別** | `float`（`st.number_input` ＋ `min_value=0.0`／`step=0.5`）|
| **逐街廓與否** | 🟢 **是**——key 綁 `bid`，且其容器為 `for b in build_blocks` 之迴圈 |
| **預設值** | `float(r.get('front_width', 8.0))`；初始化處另有 `'front_width': 8.0`（字樣錨 `'front_width': 8.0,`）|
| **session_state 之鍵** | `SS_ROAD = 'f3_block_road'`（字樣錨 `SS_ROAD = 'f3_block_road'`·命中 **1**）；結構 ＝ `st.session_state['f3_block_road'][bid] = {…, 'front_width': fw, …}` |
| **寫入時點** | `st.form_submit_button("✅ 儲存路寬資料")` 之後：`st.session_state[SS_ROAD].update(new_roads)`（字樣錨 `儲存路寬資料`）⇒ 🔒 **表單提交式**（⛔ 即時 `on_change`）|
| **打包出口** | `road_data.append({... 'front_width': r.get('front_width', 0.0), ...})`（字樣錨 `road_data.append(`）|
| **下游消費（正面列舉·`front_width` 全命中 5 列）** | `:4827` 負擔計算（`fw = float(b.get('front_width', 0.0) or 0.0)`）／`:17698` 初始化預設／`:19356` UI 欄本體／`:19434` 寫回 `new_roads`／`:19520` 打包 `road_data` |
| **另一路（引擎側鍵名）** | `front_road_width_by_label`（`{label: 正面路寬(m)}`·字樣錨 `front_road_width_by_label`·`app.py` 命中 **3**）⇒ 為 `k92_block_depth_check`／`get_min_lot_size(category, front_road_width_m)` 之輸入 |

🛑 **⛔ 提出任何新增欄位之設計**（單之禁項）；上表**僅為結構之出艙**。

### `d`　內接矩形檢查之實作處與其現行射程

🔒 **實作處（正面列舉·`app.py`·行號繫 `4fe2b44`）**

| 符號（字樣錨 ＝ `def <符號>`）| 行 | 角色 |
|---|---|---|
| `_rect_fit_edge_angles_deg` | `:11781` | 候選角度集 |
| `_rect_fit_sweep` | `:11806` | Minkowski 掃掠 |
| `_rect_fit_erode_axis` | `:11834` | 軸向侵蝕（凸路 ＝ **同一恆等式之快速形·⛔ 近似**·其 docstring 自載）|
| **`_rect_fits_free_pose`** | `:11867` | **核心**（可旋轉可平移·掃描域 `[0, 180)`·⛔ `[0, 90)`）|
| **`_k923_gate2`** | `:12163` | **`K-9-12` 閘二（可建築）之包裝** ⇒ 逕呼 `_rect_fits_free_pose` |

🔒 **射程之逐字（`_k923_gate2` 之 docstring）**：

> 🔒 `W`／`D` **一律由呼叫端自 `get_min_lot_size` 查表**（`K-9-12 二`·⛔ 不得硬編）。
> 🔒 **街角地本身⛔ 不走閘二**（`K-9-12-e`／`K-9-13` 四段）——**射程之界定在呼叫端**。

🔴 **現行射程之實測 ＝ <u>呼叫端不存在</u>**（⇒ 該排除於碼面**無受詞**）

| 受詞 | 母體 | 命中 | 逐處 |
|---|---|---|---|
| `_k923_gate2` | 全倉 `*.py` | **2 檔** | `app.py:12163`（**`def` 本身**）／`verify/probes/probe_WG9187_t3gate.py`（**3** 列·`ns["_k923_gate2"]` 之**探針**消費）|
| `_rect_fits_free_pose(`（呼叫形）| `app.py` | **2 列** | `:11867`（`def`）／`:12171`（**`_k923_gate2` 內**）|
| `_rect_fits_free_pose` | `verify/**` | **2 列** | 皆於 `verify/fixtures/fixture_wg9187_rect_fit.py`（fixture）|

🔒 **對照組（`常規五`）**：器須綠 `_k923_touch_len` ＝ **4** 命中（`> 0`）✅；
器須紅（一必不存在之同形符號·代稱）＝ **0** ✅。

⇒ 🔒 **判**：`_k923_gate2`（＝ `K-9-12` 閘二）於**生產路徑之呼叫點 ＝ `0`** ⇒ **建置未接線**，
僅由**探針／fixture** 消費。**⇒ 「是否已排除街角第 1 宗」於現行碼<u>無受詞可判</u>**
——⛔ 讀為「已排除」，亦⛔ 讀為「未排除」。

🔒 **正典側之要求逐字**（`K-9-5-15 二`·字樣錨 `⛔ 不另設寬度閘`）：

> **二、構造保證（第 1 點）**：街角最小規定範圍由**平移規則單獨決定**、**與 `G` 值無關**；
> 該構造已保證範圍之最小寬度 ≥ 畸零地最小寬
> ⇒ 🔒 **「面積夠 ⇒ 街角第 1 宗寬度合格」為真**
> ⇒ 🔴 **街角第 1 宗⛔ 不另設寬度閘、⛔ 不另跑矩形容納測試。**

🔒 **同節 `四` 之落地狀態逐字**（⛔ 憑記憶）：

> ⚠️ **落地狀態**：`K-9-12-b` 逐字「已裁定、⛔ 生產碼未實作」；現行仍走 `parcel_min_width_n14`

⇒ 與本批之實測（呼叫點 `0`）**一致**。🛑 ⛔ 評價、⛔ 提名處置。

---

## 驗收（逐項·缺一即不過）

| 項 | 值 | 判 |
|---|---|---|
| 工項零之入倉 `sha256` 逐位相同 | 落檔 ＝ 入倉 ＝ `5aceb836884ab510390be64dfa5110e93f720266e45489e3846326a05c60e253`（**4405** B 兩端同）| ✅ |
| 工項一 `a` 之三處逐字 | **四**處（單令三·本批另補裁定K 標題列）·`4/4` 逐位相符 | ✅ |
| 工項一 `b` 之標記三項齊備 | `①`／`②`／`③` 皆落地（見上表）＋ 三閘（嚴格前綴／逐字保真／`GB-132` 零污染）全綠 | ✅ |
| 工項一 `c` 之既例確認 | **既例容**（三證）⇒ ⛔ 停；🩸 併報首跑之器紅（否定宣稱母體取窄）| ✅ |
| 工項二 `a`–`d` | `a` 🔴【查無】／`b` 九維對照表／`c` 五重事後閘（`raise` ×4 ＋ harness FAIL ×1）／`d` 字面🔴【查無】·效果🟡間接 | ✅ |
| 工項三 `a`–`d` | `a` 🔴 無獨立節·表列二處 ＋ 入倉 `b061b00`／`c91550d`／`b` 受詞**不同**（多「UI 欄」）／`c` 十二項結構表／`d` 呼叫點 **0** | ✅ |
| **閘 `P`** 前後逐檔 `sha256` | `app.py` `e3e464ea…`／`selection_pipeline` `6c678342…`／`run_verification` `e48f24c8…`／`stepg_pipeline` `02602733…`／`run_all` `4ba89fef…` ⇒ **五檔前後逐位相同** | ✅ |
| **`left-right`** | **`0 0`** | ✅ |
| **`deletions`** | 本批二 commit 之 `deletions` 皆 **0**（`c5e29d0` ＝ `66/0`；本 commit 見其 `--numstat`）| ✅ |

### 🔒 一句話**推導式**

> 單令「於 `v3` §7-4 排序款掛更正標記 ＋ 補查 `wf_f2` 之容量過濾 ＋ 勘查 `K-9-1`」；
> 既例經三證確認**容**於 `v3` 體例（其自身檔頭即同形先例）⇒ 以 `🔧` 非鑄號形末端追加標記，
> 三閘（嚴格前綴 `+4048` ＝ payload／逐字 `4/4`／號類污染 `0`）全綠且 `:427` 逐位未動；
> `wf_f2` 之選塊準據經窮舉證為**達標＋ΣG 最大**、**容量／池量／超配過濾【查無】**，
> 級別係**事後標記**而非選擇準據，裝不下者由**五重事後閘**捕捉；
> `K-9-1` 無獨立正典節、其落地物較裁定本身**多一「UI 欄」**，而內接矩形閘**呼叫點為 `0`**
> ⇒ 生產碼五檔一位元組未動、`deletions` ＝ `0` ⇒ 依 `常規一` 逕行 `push`。

---

## 附　🛑 單側不符與 CC 側器紅之彙總（**逐字回報·⛔ 鑄號·⛔ 由 CC 自裁**）

| # | 側 | 受詞 | 處置 |
|---|---|---|---|
| `1` | **單** | 自載號 `W-G.9-231` **已被倉內占用**（檔框 2） | 依 KL `2026-08-03` 裁改新則之號為 `W-G.9-232`；**單內容一字未改** |
| `2` | **單** | 工項一 `b` ② 稱「`pre_price` 三處消費全數退場」；碼側自陳所名者為 **`value`**，`pre_price` 實有 **10** 列且為現用輸入 | 標記依**倉**載 `value` 並具名二者之別；⛔ 改單一字 |
| `3` | **CC** | 零對照誤取 `W-G.9-999`（**倉內實有**·既有哨兵）⇒ 器未過兩造 | 該次量測**⛔ 出艙**，換一必不存在之同形號重跑 |
| `4` | **CC** | 「`v3` 無 append-only 型宣告」**為偽**（九變體母體取窄，漏該檔自身措辭「**內文不改**」）| 斷言撤回·**⛔ 出艙錯值**；自加之戒 ＝ 否定宣稱之變體集須先自受詞檔取樣 |
| `5` | **CC** | 中途假說「守恆閘對負池結構性全盲」**經查為偽**（`_pool_total_blk` 係幾何量）| 出艙前撤除；記其為方法（假說須先查賦值處） |

🛑 `1`／`2` 之**是否鑄號、由何側鑄** ＝ **意思決定** ⇒ 候發單側／KL；本報告**只登記事實**。

---

## 附二　本批之產物

| 檔 | 性質 | `deletions` |
|---|---|---|
| `docs/orders/W-G.9-232_v3-427更正標記與wf_f2容量過濾補查.md` | 🆕 純新增（工項零·commit `c5e29d0`）| **0** |
| `docs/配地計算總規格_v3.md` | 🔧 **純末端追加**（`+4048` B·嚴格前綴機驗 `True`）| **0** |
| `docs/reports/W-G.9-232R_v3-427更正標記與wf_f2容量過濾補查_執行報告.md` | 🆕 純新增（本檔）| **0** |

🔒 **生產碼五檔⛔ 動一位元組**；`verify/out/` **⛔ `git add -A`**（逐檔 add·`CLAUDE.md` 既例）。
