# W-G.8 D-2b-21【b-2】T2 接線 —— **開工前偵察：驗收靶與施工範圍互斥（🛑 停機上呈）**

**產出**：CC／2026-08-09　**倉**：`github.com/topology561/land-readjustment-trial`　**分支**：`wip/s1-endpart`
**基準 HEAD**：`fa7f5aa`（D-2b-20）　**⛔ 生產碼零觸**（本批未改 `app.py`／`verify/` 之任一可執行行）

> # 🛑 一句話結論
>
> **`②-宗` 一欄之破量，在「只改 `_pool_strips_for_block`」之範圍下 <u>數學上不可達零</u>。**
> 該閘之母體 ＝ **呼叫端傳入之 `biz_polys`**（`app.py:8975`），其值來自上游 `cut_coords`
> （`app.py:19768`）——`_pool_strips_for_block` 改任何內部實作都**碰不到該母體**。
> 而破量之 **100%** 係「**宗0 × 宗1／宗2／宗3**」（§三 逐項對帳，殘差 `0.0001`／`0.0000`）。
> ⇒ 交接文 §八 驗收靶表第 1 列與其自身之施工範圍**互斥**；**未經裁定前不施工**
> （失敗考古 56-6-1「前提不成立 ⇒ 停止施作，不照單辦」）。

> ⚠️ **本文所有數字皆為 `fa7f5aa` 當下現查**，每筆附 `檔:行` ＋ 可自癒 grep（`CLAUDE.md:85-100`／`:101-107`）。
> 🔒 標 `【單】` 者**倉內查無**，⛔ 下游不得引用為事實。

---

## 〇、開工清單結果（交接文 §〇·8/8 通過）

| # | 項 | 現查值 | 判 |
|---|---|---|---|
| 1 | HEAD | `fa7f5aa` | ✅ ≥ `63bc889` |
| 2 | `origin/wip/s1-endpart` | `fa7f5aa` | ✅ 與 HEAD 同 |
| 3 | 環境 | `shapely 2.1.2 \| GEOS (3, 13, 1)` | ✅ 與 `app.py:8577` 所載一致 |
| 4 | 登記表列數 | **50** | ✅ |
| 5 | 失敗考古末節 | **56**（`.claude/skills/failure-archaeology/SKILL.md:1367`） | ✅ |
| 6 | 正典 `K-9-5-5`〜`-8` | `:667`／`:745`／`:793`／`:818` | ✅ 四行全中 |
| 7 | `test_D2b15_tilegate.py` | 片數 6／總量 `0.000e+00`／逐步 `1.819e-12`／互補 `5.972e-13` | ✅ `rc=0` |
| 8 | `test_D2b16_mutation.py` | 三類注入皆 raise、殘差正負號符合 | ✅ `rc=0` |

### 0-1 ⚠️ 一項**偏離交接文之操作**（具名記載）

交接文 §〇 首行為 `git checkout wip/s1-endpart`。本 session 之工作目錄係 **git worktree**
（`.claude/worktrees/k6a2-d2b21-wiring-6ef500`），而 `wip/s1-endpart` 已由**主倉**檢出
（`git worktree list` 首列）⇒ 同一分支**不可二次檢出**。

**實際採法**：`git checkout -B claude/k6a2-d2b21-wiring-6ef500 fa7f5aa`
（⛔ **非** `git reset --hard`）。原指向之 `6cb77b7` 係 `main`／`origin/main` 之 tip
（`git branch --contains 6cb77b7 -a` 實查含 `main`／`remotes/origin/main`）⇒ **零損失**。
工作樹追蹤檔異動 **0**、未追蹤／被忽略檔 **0**（`git status --porcelain --ignored | wc -l`）。

⚠️ **收官時之推送式須為 `git push origin HEAD:wip/s1-endpart`**（非 `git push`）——本分支名不同。

---

## 一、🛑 停機主體：靶與範圍互斥

### 1-1 `②-宗` 閘之母體 ＝ **呼叫端傳入之幾何**（⛔ 非本函式所產）

```bash
grep -n "_biz = \[p for p in (biz_polys" app.py          # ⇒ :8975
grep -n "biz_x += float(_biz\[i\].intersection" app.py   # ⇒ :9062
grep -n "allocated_polys.append(_p)" app.py              # ⇒ :19783
grep -n "_coords = _res.get('cut_coords')" app.py        # ⇒ :19768
```

- `app.py:8975`　`_biz = [p for p in (biz_polys or []) …]`　← **參數**，本函式全程未改寫
- `app.py:9060-9062`　`biz_x += _biz[i].intersection(_biz[j]).area`　← 閘之被量物 ＝ `_biz`
- `app.py:19768→19783`　`_p ← Polygon(_res['cut_coords'])` → `allocated_polys` → 傳入本函式

⇒ **`_pool_strips_for_block` 之任何內部改寫（含改用 `_block_partition`）皆不改變 `biz_x` 之值。**
這是**算術事實**，⛔ 非估計、⛔ 不需實測佐證。

### 1-2 破量之來源（**逐項對帳·殘差已算**）

【倉】`verify/out/probe_D2b17_dual.log:67-70`（R2）／`:76-79`（R5）——甲法（`.intersection()`）逐對值：

| 街廓 | 對 | 甲法值 | 出處 |
|---|---|---|---|
| **R2** [0m] | 宗0×宗3 | `46.0539` | `probe_D2b17_dual.log:68` |
| | 宗0×宗2 | `25.2873` | `:69` |
| | 宗0×宗1 | `4.9205` | `:70` |
| | **Σ** | **`76.2617`** | vs 閘破量 **`76.2616`**【倉】`verify/out/probe_D2b8_guard.log:9`　⇒ **殘差 `0.0001`**（＝三項各 4dp 之捨入） |
| **R5** [0m] | 宗0×宗2 | `41.5297` | `probe_D2b17_dual.log:77` |
| | 宗0×宗3 | `29.0522` | `:78` |
| | 宗0×宗1 | `1.6145` | `:79` |
| | **Σ** | **`72.1964`** | vs 閘破量 **`72.1964`**【倉】`verify/out/probe_D2b8_guard.log:12`　⇒ **殘差 `0.0000`** |

🔑 **兩塊之破量皆 100% 由「宗0 × 其他」構成**；`probe_D2b17_dual.log` 該段之**其他所有對**
甲法皆為 `0.0000`（該段逐對明細已列全部非零對·⛔ 非抽樣）。

⇒ 令 `②-宗` 歸零 ⟺ **改變宗0 或宗1/2/3 之幾何**。二者**皆在 `_pool_strips_for_block` 之外**。

---

## 二、機制：`K-9-5-4 ②` **只實作了一半**（🔑 本波之真正落點）

### 2-1 正典逐字（**單一條線**）

【倉】`docs/rulings/K-6_街角地分配程序與可分配判準.md:622`
（`grep -n "^### 🔒 K-9-5-4 " docs/rulings/K-6_街角地分配程序與可分配判準.md`）：

> 街角**第 1 宗與第 2 宗之境界線** ＝ **SIDELINE 平行移動**（⛔ **不是** ALLOCLINE 方向）

「第 1 宗**與**第 2 宗之境界線」＝ **一條**界線、**一個**方向。

### 2-2 現碼只做了近側那一半

| 半 | 碼面 | 狀態 |
|---|---|---|
| 第 1 宗之**遠側**界 ∥SIDELINE | `app.py:11791-11792`　`if is_corner: allocation_dir = _first_corner_alloc_dir(side_mid)`（`grep -n "K-9-5-4 ② 之唯一決定點" app.py`） | ✅ **已落地**（D-2b-1） |
| 第 2 宗之**近側**界 ∥SIDELINE | 需 `_block_strip` 之雙線形式 `n_hat_far`（`app.py:8415`） | 🔴 **未落地** |

**`n_hat_far` 生產端實際傳入之呼叫 ＝ 0**（窮舉指令原文與輸出）：

```bash
grep -rn "n_hat_far=" --include=*.py . | grep -v "^\./\.claude/worktrees/" | grep -v "^\./verify/probes/"
# ⇒ 3 命中，全為 app.py 之**定義列與 docstring**：:8395（docstring）／:8415（def 之預設值）／:8751（docstring）
# ⇒ 實際傳參之呼叫 0。（⛔ 本識別字不可鏈式 ⇒ 字面 grep 之計數於此成立·考古 55-2 之 AST 要求不適用）
```

⇒ **同一個界面被兩條方向不同的線各自定義**：宗0 之遠側界 ∥SIDELINE，宗1 之近側界 ∥ALLOC。
⇒ 宗0 之帶為**斜置平行四邊形**，斜跨進宗1／宗2／宗3 ⇒ §1-2 之三項重疊。

### 2-3 方向差之實測（**與破/不破 6/6 對位**）

【倉】`verify/out/probe_D2b7_dirscope.log:5-20`【A】——`SIDE` vs `ALLOC_cad` 之 `|cross|`／夾角：

| 街廓·側 | 夾角(deg) | 判 | 該塊 Step-G 結果【倉】`probe_D2b8_guard.log:8-16` |
|---|---|---|---|
| R2/left | `4.321972`（`:12`） | 不平行 | 🔴 ②-宗 `76.2616` |
| R5/left | `4.321968`（`:18`） | 不平行 | 🔴 ②-宗 `72.1964` |
| R3/right | `4.321958` | 不平行 | 🔴 覆蓋 `76.0262` |
| R4/left·right | `2.713695`／`2.867975` | 不平行 | 🔴 覆蓋 `58.4792` |
| R1/left·right | `0.557951`／`0.690832` | 不平行 | 🔴 覆蓋 `12.0568` |
| **R6/right** | **`0.000003`**（`:17`） | **∥** | ✅ **跑完全鏈** |

⚠️ **⛔ 本表不得讀成「R6 過 ⇒ 機制得證」**——失敗考古 55-3 明令**退化案例不得作為通過憑據**
（R6 之 `W` 鏈為常數 `7.2000`、三道鏈閘於該塊係 `0 == 0` 空過）。本表之證據力**僅在破的那五格**：
五塊皆方向不同、且 R2／R5 之破量已於 §1-2 **逐項歸因至宗0**。

### 2-4 🔴 **未證項（明標·⛔ 不得當結論）**

`①' 覆蓋閘` 之三格缺口（R1 `12.0568`／R4 `58.4792`／R3 `76.0262`）**是否亦全額源自同一界面**
——**本批未量**。已知者僅：該三塊之甲法宗-宗重疊為 `0.0000`
（【倉】`probe_D2b17_dual.log:73`／`:74` 等段：R1 僅 `池0×宗4` 甲 `0.0000`、R3 僅 `宗9×宗10` 甲 `0.0000`）
⇒ 三塊之症狀為**缺口非重疊**；且缺口方向已於 D-2b-17 判為**真缺口、溢出 `0.0000`**
（【倉】交接文 §五 之「順帶產出之方向判定」）。
**缺口之逐界面歸因 ＝ 施工前必做之量測**（§四-3 列為前置）。

---

## 三、🛑 上呈 KL：**域裁題（範圍）**

### 3-1 裁題

> **D-2b-21 是否授權改動「第 2 宗之近側界方向」（即 `K-9-5-4 ②` 之另一半）？**

**為何非 CC 可自裁**：該改動有**土地後果**——第 2 宗由平行四邊形變**梯形**
（`app.py:8429-8432` docstring 逐字：「`K-9-5-4 ②` … 使其遠側界留 ∥ALLOC ⇒ **第 2 宗必為梯形**，
現行原語無法表達」）⇒ 同一 `S` 下面積改變 ⇒ `solve_G_binary` 收斂至不同 `S`
⇒ **宗地寬度、G、乃至 PK 資格皆可能動**。屬「意思決定」，依 `CLAUDE.md` 不自動裁。

### 3-2 ⚠️ 開場文字之「不授權 #3」——**現查其出處，與本裁題不同一事**

開場文字列「不授權 3. 第 2 宗 5 格為另案（界線 #3）」。現查：

```bash
grep -n "第 2 宗\|界線 #3\|不授權" docs/reports/W-G.8_D-2b波_新session交接文.md   # ⇒ rc=1，0 命中
grep -rn "界線 #3" --include=*.md . | grep -v "^\./\.claude/worktrees/"
```

- 該條**不在**現行交接文內（0 命中）。
- 「界線 #3」之唯一倉內出處為**舊**交接文【倉】`docs/reports/W-G.8_D波_新session交接文.md:186`：
  逐字為「**第 2 宗 5 格低於最小寬** ⛔ **D 未解決·須另案**（界線 #3）」
  ——其受詞是**寬度不足之處置**，⛔ **不是**「第 2 宗之界線方向」。
  同義佐證【倉】`verify/probes/probe_D2b1_sentinel.py:358`：「⛔ **只量不處置**（D 波界線 #3）」。

⇒ **兩者共用「第 2 宗」三字而受詞不同**（同 `CLAUDE.md` 兩個「最小分配面積」之同名不同量族）。
🛑 **CC 不自行認定 #3 是否涵蓋本裁題**——⛔ 這正是「以自己的解讀替代裁定」。**請 KL 明示。**

### 3-3 三個可能之處置（**只陳列·⛔ 不推薦、不預設**）

| 案 | 內容 | 後果 |
|---|---|---|
| **甲** | 本批**一併**實作 `K-9-5-4 ②` 之另一半（第 2 宗近側界 ∥SIDELINE·經 `n_hat_far`） | 驗收靶三欄**可能**全達；但**有土地後果**、且須重跑 `run_all` ＋ 換期望 FAIL 名單 |
| **乙** | 本批**只做池側**（`_pool_strips_for_block` → `_block_partition` ＋ 切線帳） | ⚠️ **驗收靶第 1 列須先改寫**（`②-宗` 不可能歸零）；且切線帳於宗0/宗1 界面會**收到兩個方向**，`_block_partition` 之 `lines` 需要**每界面恰一條線** ⇒ 帳之建構本身即撞同一缺口 |
| **丙** | 本批先做**缺口逐界面歸因量測**（零生產碼），把 §2-4 之未證項補成實測，再依結果定甲/乙 | 不動土地；代價＝多一批 |

⚠️ **乙案之隱患須明載**：若把 `②-宗` 之母體改成「由 partition 導出之宗片」，該閘將因**構造性互斥**
而**恆為 0** ⇒ 變成 `0 == 0` 之**空過**（失敗考古 55-1／55-3 同族）。
⛔ **不得以「閘綠了」宣稱問題已解**；真正該被問的仍是 `cut_coords`（進 G 帳者）是否重疊。

---

## 四、本批已完成／未完成（**誠實清單**）

**已完成**：§〇 開工清單 8/8；§一〜§三 之偵察與對帳；本報告入倉。
**未完成（⛔ 未動工）**：`_pool_strips_for_block` 之接線、切線帳、`GB-49` 帳層累積閘、12 格重烤與逐格歸因。
**未量（⛔ 標為未證·§2-4）**：`①' 覆蓋閘` 三格缺口之逐界面歸因。

**⛔ 12 格全保真基準本批<u>未重烤</u>**——故其 sha256 仍為【倉】`verify/out/probe_D2b16_baseline.log`
所載之值，**與交接文一致、未變**。⛔ 本批不得被讀為「12 格已驗」。

---

## 五、新登記項

**`GB-51`**（本報告新開）：`K-9-5-4 ②` **半實作**——街角第 1 宗之遠側界已 ∥SIDELINE，
第 2 宗之近側界仍 ∥ALLOC ⇒ **同一界面雙定義** ⇒ 宗-宗重疊（本案 R2 `76.2616`／R5 `72.1964`，
100% 歸因宗0）／覆蓋缺口（R1／R3／R4·**歸因未量**）。詳見登記表該列。

---

## 六、異動清單（**逐檔**）

| 檔 | 異動 | 性質 |
|---|---|---|
| `docs/reports/W-G.8_D-2b-21_T2接線前偵察_靶與範圍互斥.md` | 新增（本檔） | 文件 |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 新增 `GB-51` 一列 | 文件 |

⛔ `app.py`／`verify/` **零觸**（`git diff --stat` 實查為上表二檔）。
