# `W-G.9-258R`　`c1` 之 KL 放行與併線 ＋ 自誤 `320`

> 🔴 **`c1` 已併入主線**（`15f27b9`）。KL 放行日 `2026-09-08`。
> 🔒 **併法 ＝ `cherry-pick`**（單 `§三 a` 二擇一·具名；⛔ `rebase` 主線既有 commit）。
> 🔒 側分支 `origin/verify/W-G.9-255-c1` ＝ `3242e96` **保留⛔ 刪**（既例）。

## §零　KL 所放行之句（`-258 §零` 逐字·⛔ 該段程式碼）

> 「`c1` 這一批，在本案不動任何一宗地的面積。同一區段內的合併，折算係數恰為 `1`，面積逐位還原、一個位元都不差（窮舉全部宗地 × 全部區段，反例 `0`；並用錯誤寫法當反靶，證這個檢查真的抓得到東西）。跨區段的折算路徑已經打開、由三組人造測資證明算得對——**但那條路在本案的真實資料裡還沒被走到過**。」

## §一　開工閘（五項全過）

錨 ＝ `a5b4edd`／側分支 `3242e96` ✅；`W-G.9-258` 號占用全 `0`（三軸 ＝ `blob@a5b4edd`／全 `docs/` `601` 檔／檔框＋列框），對照甲 `-256` 檔框 **`3`** 與單載相符、對照乙 人造號全 `0`；次號 自誤 `MAX 319`／缺號 `[106]`／恆等式 `True` ⇒ `320`；`S-0b` `rc=0`、`P-5` 相符（`8d5cc44db0bceb71…`）。
🟡 二提示（`P-4`／`P-6` ＠ `:60`）＝ **表頭列** `| 閘 | 判準 | 停機款 |` ⇒ **具名豁免（偽陽）**。

## §二　工項零〜一

入倉 `b362b694216bf4d9`（`6914` B·逐位同）；自誤 `320` 已鑄（責 ＝ 發單側·第五度）。
**CC 之口徑具名**（單 `§二` 併記所令）：`60` ＝「`(暫編地號, a)` **序對**」之去重數／二情境皆讀／含 `a=0.0`（判準 `if v:` 只跳空字串）／去重前 `111` ＝ `54`＋`57`（**與發單側覆核之列數逐位相符**）。
🔒 三數並列（口徑不同、皆非誤）：CC `60`（序對去重）／發單側 `47`（僅 `a` 值相異數）／單原載 `57`（僅 `3.5m` 列數）。**`V-G1` 之判⛔ 因口徑而動。**

## §三　工項二：併線

| 檔 | 併線前 `a5b4edd` | 併線後 `15f27b9` | 單載期望 | 判 |
|---|---|---|---|---|
| `verify/wf_f0.py` | `6758ea766b001b95` | **`b0722355c1978635`** | `b0722355c1978635` | ✅ **逐位相符** |
| `app.py` | `4860f10828fb8ae9` | `4860f10828fb8ae9` | 不變 | ✅ |
| 其餘九檔＋夾具 | — | 全部不變 | 全部不變 | ✅ |

### 🩸 一處偏離之具名（⛔ 追改已推之 commit）

單 `§三 c` 令 commit message 須載「`SELF_SHA256` 之單號 `-258`、KL 放行日 `2026-09-08`、及 `§零` 所引之放行句首句」。
🔴 **`cherry-pick` 沿用側分支之原 message**（其首行為「…🔴 生產碼·側分支·候 KL 放行」），
**⛔ 載上開三項** ⇒ **未依 `§三 c`**。
🔒 **處置**：`15f27b9` 已推 ⇒ ⛔ 追改（`常規四（九）五`·⛔ 事後改史）；
本節即其**唯一**補記載體——**單號 `W-G.9-258`／KL 放行日 `2026-09-08`／放行句首句
「`c1` 這一批，在本案不動任何一宗地的面積。」**
🔑 **通則（CC 側自立）**：`cherry-pick` **保留原 commit message** ⇒ 凡單對 message 有要求者，
須以 `-e`／`--no-commit` 或事後 `amend`（**推之前**）為之；⛔ 逕行 `cherry-pick` 後才想起。

## §四　工項三：併線後之復閘（**⛔ 只跑一次·全在併線後之主線態**）

| 閘 | 實測 | 判 |
|---|---|---|
| `V-G1` | 括號式反例 **`0`**；左結合反靶反例 **`9`**（含 `947.97/b` `Δ=-1.1368683772161603e-13`、`245.0/a` `Δ=-2.842170943040401e-14`）·口徑同 `§二` | ✅ |
| `V-G2` | `a_m * (p_of(m) / p_t)` 命中 **`1`**；左結合二形命中 **`0`** | ✅ |
| `V-G3` | `停機#2` `0`／`停機#3` `1`／`zones` `1`／`_key` `1`／`py_compile` rc `0` | ✅ |
| `V-G′` | 三組判別力 ＋ 反靶（**於併線後之主線態重跑**·⛔ 引用側分支舊結果）| ✅ |
| `V-A′` | 十一檔中**僅** `verify/wf_f0.py` 相異；`app.py` ＝ `4860f10828fb8ae9` | ✅ |
| `V-B` | 自誤簿 deletions ＝ `0` | ✅ |
| `V-C` | 自誤 `MAX 319→320`、缺號仍 `[106]`、恆等式 `True`（`313+1 == 314`）·次號 `321` | ✅ |

🩸 **`V-C` 首跑得 `MAX 311`（誤）**——未設 `GIT_INDEX_FILE` 致讀到**主 checkout 之 index**；
改以 `HEAD` 之明確 rev 重跑得 `320`。**量測器紅、⛔ 受詞紅。**

## §五　待辦

| 項 | 狀態 |
|---|---|
| **`-257`（`GB-67` 定位波·`Q-1`〜`Q-7`）** | ⬜ **KL 令開跑**；單 `§六` 載其**結果呈新窗**。環境須裝 `ezdxf`／`shapely`／`plotly`／`streamlit`／`pandas`／`openpyxl` |
| `c2`（廢步驟 0） | 🔴 **⛔ 開工**（`-256 §六`·`-258 §六` 重申）|
| `-253` 段乙（`-251` `c1`／`c2`·純 UI） | ⬜ 原令全效·可並行·`V-K` 候 KL 目檢 |
