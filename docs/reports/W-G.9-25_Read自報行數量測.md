# W-G.9-25：`Read` 自報行數之量測

**批號**：`W-G.9-25`（占用檢查：`grep -rl "W-G\.9-25" docs/ .claude/ CLAUDE.md | wc -l` ⇒ **0**；
判別力自檢：同式改 `W-G.9-24` ⇒ **6** ⇒ 該式非恆 0）⇒ **未占用·⛔ 未改任何既有號**。
**基座**：`wg925-handoff` @ `4dbc642`（＝`origin/wip/s1-endpart`）
**性質**：⛔ 零生產碼 · 本批**僅新增本報告一檔** · ⛔ **未插入任何既有文件**（§0）
**結論**：🛑 **停機**——§6 判別力自檢**未過**、§7 **R-1 觸發** ⇒ ⛔ **不交付 H-A〜H-D 之擇一判**。

---

## §1 受詞與讀檔途徑（⛔ 逐一具名·⛔ 不以「工具」概稱）

**受詞 ＝ `Read` 工具之<u>自報總行數</u>**（⛔ 非檔案內容是否正確、⛔ 非 `sed`／`cat` 之輸出）。

| # | 途徑 | 可用性 | 本批角色 |
|---|---|---|---|
| **A** | **`Read`（內建）** | ✅ 可用 | **受詞本體** |
| **B** | `mcp__official_docs__read_text_file` | 🔴 **不可用** | ⛔ 非本 session 之讀檔途徑 |
| **B′** | `mcp__official_docs__list_allowed_directories` | 🔴 **不可用** | 同上 |
| **C** | Bash `wc -l`／`sed`／`tail`／`od` | ✅ 可用 | **外部錨**（紀律第 7 條：閘之一端在受詞之外） |

**B／B′ 之不可用·逐字原文**（兩者同一錯誤）：

> Error: Tool 'read_text_file' has an invalid outputSchema: JSON Schema declares an unsupported dialect ("$schema": "http://json-schema.org/draft-07/schema#"). The default validator supports JSON Schema 2020-12 only; pass a pre-configured Ajv instance to AjvJs

⇒ **本 session 之可用讀檔途徑僅 `Read` 一種**；§1 之「一種以上 ⇒ 各測」**前提不成立**（已具名、非略過）。

---

## §2 外部錨（⛔ 先於任何 `Read`·紀律第 7 條）

| # | 檔 | `wc -l` | `wc -c`(工作樹) | git blob `wc -l` | **末字元** | **真行數** |
|---|---|---:|---:|---:|---|---:|
| 1 | `docs/驗證裁定登記表.md` | 206 | 14,251 | 206 | 換行 | **206** |
| 2 | `verify/stepg_pipeline.py` | 1,443 | 96,672 | 1,443 | 換行 | **1,443** |
| 3 | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | 1,963 | 132,474 | 1,963 | 換行 | **1,963** |
| 4 | `.claude/skills/failure-archaeology/SKILL.md` | 2,693 | 213,872 | 2,693 | 換行 | **2,693** |
| 5 | `app.py` | 22,048 | 1,320,095 | 22,048 | 🔴 **非換行（`)`）** | **22,049** |

🔒 **五檔之 `wc -l` 與施工單 §3 之「預測 `wc -l`」欄逐檔相符**（206／1443／1963／2693／22048）⇒ 無差異可載。

**H-B 推算依據之自算（⛔ 未採信單內數）**：
`git show HEAD:#4 | head -2524 | wc -c` ⇒ **199,169 B**（與單之 199,169 **逐位相符**）；
`app.py`(LF) 累計位元組首次 ≥ 199,169 之行 ＝ **4112**（單預測 ~4111；差 1 係「落在該行」與「累計≥之首行」之界定差·⛔ 不影響 H-B 與其他假說之可分性）。

---

## §3 Q-1：逐檔比對（受檢順序 `#5→#1→#2→#3→#4→#5`·⛔ 逐次呼叫·⛔ 未併發）

| 序 | # | 檔 | **`Read` 自報** | `wc -l` | Δ(自報−`wc -l`) | 真行數 | Δ(自報−真行數) | **出艙碼**（判準＝單 §3：自報 ＝ `wc -l`） |
|---|---|---|---:|---:|---:|---:|---:|---|
| 1 | 5 | `app.py`（第一次） | **22,049** | 22,048 | **+1** | 22,049 | **0** | `DIFF` |
| 2 | 1 | `驗證裁定登記表.md` | **207** | 206 | **+1** | 206 | +1 | `DIFF` ← **陰性對照** |
| 3 | 2 | `stepg_pipeline.py` | **1,444** | 1,443 | **+1** | 1,443 | +1 | `DIFF` |
| 4 | 3 | `K-6_…判準.md` | **1,964** | 1,963 | **+1** | 1,963 | +1 | `DIFF` |
| 5 | 4 | `failure-archaeology/SKILL.md` | **2,694** | 2,693 | **+1** | 2,693 | +1 | `DIFF` ← **陽性對照** |
| 6 | 5 | `app.py`（第二次） | **22,049** | 22,048 | **+1** | 22,049 | 0 | `DIFF`·**與第一次同值** ⇒ ⛔ **非 `UNSTABLE`** |

⇒ **六列全數 `DIFF`，Δ 對 `wc -l` 一律 `+1`**。⛔ 無 `NO_SELF_REPORT`／`ERROR`／`MISSING`。

### 🔑 `+1` 之歸屬（外部錨判定·⛔ 非推測）

`Read` 之計數慣例 ＝ **`\n` 數 ＋ 1**（＝ `split("\n")` 之段數）。逐檔驗：

- `#1`〜`#4` **末字元為換行** ⇒ 末段為空 ⇒ `Read` **多算一個空段**（自報 ＝ 真行數 +1）。
- `#5` `app.py` **末字元為 `)`、無尾隨換行**（`tail -c 1 app.py | od -c` ⇒ `)`）
  ⇒ 真行數確為 **22,049** ⇒ 🔴 **此格是 `Read` 對、`wc -l` 少算一行**。

⚠️ 即 `CLAUDE.md`「🔒 位元組數／校驗碼須聲明量測框」之同族——**兩邊在量不同側**；
戒 (d)「指控他方數字有誤之前，先問我們是不是在量同一側」於本批**正面成立**。

---

## §4 Q-2：分頁能否越過自報值（受詞＝`app.py`·自報值 22,049）

| 呼叫 | `offset` | 結果 | 原文／對拍 |
|---|---:|---|---|
| (a) | **22,050**（＝自報值＋1） | **被拒** | 逐字：`Warning: the file exists but is shorter than the provided offset (22050). The file has 22049 lines.` |
| (b) | **20,000** | 🔴 **回傳內容** | 首行原文：`20000\t                                            _p = None` |

**對拍**：`sed -n '20000p' app.py` ⇒ `                                            _p = None`（`cat -A` 驗行尾）
⇒ **與 `Read` 回傳之首行逐字相符** ✅。

⇒ **§7 R-3 之「兩次皆被拒」不成立**（(b) 回傳內容）⇒ **R-3 未觸發** ⇒ ⛔ **不凍結 `app.py` 之 `Edit`**。

---

## §5 Q-3：自報字串逐字（🔴 出艙之數與本報告之數為<u>同一次執行</u>·考古 63）

> `Warning: the file exists but is shorter than the provided offset (999999). The file has 22049 lines.`
> `Warning: the file exists but is shorter than the provided offset (999999). The file has 207 lines.`
> `Warning: the file exists but is shorter than the provided offset (999999). The file has 1444 lines.`
> `Warning: the file exists but is shorter than the provided offset (999999). The file has 1964 lines.`
> `Warning: the file exists but is shorter than the provided offset (999999). The file has 2694 lines.`
> `Warning: the file exists but is shorter than the provided offset (999999). The file has 22049 lines.`
> `Warning: the file exists but is shorter than the provided offset (22050). The file has 22049 lines.`

⚠️ **elicitor 之具名**：本批一律以 `offset=999999, limit=1`（越界探針）誘出自報值，**六列同式**。
⛔ 未混用「分頁註腳（`of N total`）」通道——該通道之值另見 §7-2。

---

## §6 判別力自檢（🔴 下放一層·紀律第 4 條）＝ **未過** 🛑

| 對照 | 檔 | 單之預期 | **實得** |
|---|---|---|---|
| **陰性** | `#1`（206 行） | `SAME` | 🔴 **`DIFF`**（207 vs 206） |
| **陽性** | `#4`（2,693 行） | `DIFF`（自報 < 2693） | **`DIFF`**——惟為 **2694 > 2693**，⛔ **非**「自報 < 實際」 |

🛑 **兩者同碼（皆 `DIFF`）⇒ 依 §6 判定「量測器無鑑別力」⇒ 停機，⛔ 不得據以判 `app.py`。**

🔴 **且其不可過係<u>構造性</u>的**：單 §3 定 `SAME` ＝「自報 ＝ `wc -l`」，
而 `Read` 對**任何末字元為換行之檔**必 `+1` ⇒ **陰性對照之預期碼 `SAME` 在本工具下不可達**。
⇒ 此為**失敗考古節 76 之鏡像**：節 76 是「閘不可能紅」，本例是「**對照組不可能出現其預期碼**」
——二者皆使該檢查**什麼也證不了**。**⛔ 本批不自行改判準**（域外·屬施工單方）。

> ## 🔧 **判準更正（`W-G.9-26`·2026-08-13·⛔ 上文原句一字未刪）**
>
> 🔴 **上開「量測器無鑑別力」之判，其成因為<u>判準錯</u>，⛔ 非量測器錯。**
> 施工單 `W-G.9-25` §3 定 `SAME` ＝「自報 ＝ `wc -l`」，而 `Read` 之計數慣例
> ＝ `split("\n")` 之段數 ⇒ 對任何末字元為換行之檔**恆 `+1`** ⇒ 陰性對照之預期碼**不可達**。
> 🔒 **該錯由施工單方犯**（已立**考古節 79**）。
>
> **判準已由 `VR-009` 改為方向式三碼**：`COVERS/EXACT`／`COVERS/OVER`／`SHORT`
> （`SHORT` ＝ 自報 **<** 真行數 ⇒ 🔴 會漏讀；真行數 ＝ `wc -l` ＋（末字元非換行 ? 1 : 0））。
>
> **依新判準重讀本批**：`#1`〜`#5` **五檔全為 `COVERS`** ⇒ ⛔ **無漏讀**。
> ⇒ 🔒 **本批之停機正確、其所指之疑慮為偽**——⛔ **二者分開讀**。
>
> ⚠️ `VR-009` 之射程為**判準形式**、⛔ **不追溯**；本加註 ⛔ 不改 §3 之任何已出艙碼。
> ⚠️ `docs/驗證裁定登記表.md` **⛔ 不在權威序四級之內**。

---

## §7 停機條件之逐條判

| 碼 | 條件 | 判 | 說明 |
|---|---|---|---|
| **R-1** | `#1` 得 `DIFF` | 🛑 **觸發** | 惟其 stated 理由「問題比預想廣」**⛔ 不成立**——Δ 為 uniform `+1` 之**計數慣例**、⛔ 非截斷 |
| **R-2** | `#4` 得 `SAME` | ⛔ **未觸發** | `#4` 得 `DIFF`。**惟其實質條件「現象不可復現」成立**，見 §7-2 |
| **R-3** | `app.py` `DIFF`／`UNSTABLE` **且** §4 兩次皆被拒 | ⛔ **未觸發** | §4(b) 回傳內容 ⇒ ⛔ 不凍結 `Edit` |
| **R-4** | 寫入落到禁動清單 | ⛔ **未觸發** | 見 §8 |

### 🔴 §7-2 前批之更正：`2524` **不可復現**（⛔ 原句不刪·具名）

**本 session 於本批<u>之前</u>之觀測**（⚠️ **⛔ 非本批之執行**·⛔ 倉內無從覆核·考古 61）：
`Read(#4, offset=2202, limit=492)` 僅回傳至 **2524**（請求 492 行·實得 323 行），
續呼 `Read(#4, offset=2525, limit=169)` 得逐字
`Warning: the file exists but is shorter than the provided offset (2525). The file has 2524 lines.`
——CC 據以於聊天回報「`Read` 視域截斷·節 76／77／78 讀不到」。

**本批原樣重跑同一呼叫**（`offset=2525, limit=169`）⇒ 🔴 **成功回傳 2525–2693 全 169 行**
（節 76／77／78 俱在，末行 `2693` 為節 78 之末句）⇒ **⛔ 該宣稱不可復現。**

| 受詞 | 前批 | 本批 | 判 |
|---|---:|---:|---|
| `#4` 之自報總行數 | **2,524** | **2,694** | 🔴 **同檔·同 session·兩次不同值** |
| `offset=2525` 之可達性 | **被拒** | **回傳 169 行** | 🔴 **反轉** |

⚠️ **⛔ 依考古 74 不得記為「H-D 成立」**：H-D 之名稱含**兩段**——
「同檔兩次量得不同值」（**現象** ✅ 已量得）與「**受檢順序影響結果**」（**機制** ⛔ **本批未做隔離實驗**）。
⇒ 正確之記法為「**現象成立·機制未隔離**」。
⛔ 且 §6 自檢未過 ⇒ **本批一律不作擇一判**（見 §9）。

⚠️ **另一通道之值（⛔ 非本批 elicitor·僅併記）**：本 session 前批對 `#3` 之分頁註腳曾載
`of 1964 total`，與本批越界探針所得之 **1964** 同值 ⇒ 二通道於該檔一致。⛔ 未對其餘檔測此通道。

---

## §8 還原自證（考古 68：「已完成」≠「已落地」）

| 式 | 時點 | 輸出 | 判 |
|---|---|---|---|
| `git status --porcelain` | 本批寫入**前** | **空** | ✅ 基線乾淨 |
| `git diff --numstat HEAD -- app.py verify/ docs/rulings/ CLAUDE.md` | 本批寫入**前** | **空** | ✅ |
| 同上 | 本批寫入**後** | **空** | ✅ **禁動清單零觸** |
| **判別力自檢**：`git status --porcelain -- docs/reports/W-G.9-25_Read自報行數量測.md` | 寫入後 | **非空**（`?? …`） | ✅ **證該式非恆空** |

⇒ **R-4 未觸發**。本批**唯一異動 ＝ 新增本報告**；⛔ **未插入任何既有文件**。

---

## §9 H-A〜H-D 之判 ＝ ⛔ **不交付**（依 §6 停機）

🛑 §6 明訂「兩者同碼 ⇒ 量測器無鑑別力 ⇒ 停機，**⛔ 不得據以判 `app.py`**」。
⇒ **本批 ⛔ 不作擇一判**，亦 ⛔ 不寫「大概是」。

**下列為原始觀測·⛔ 非判、⛔ 不得下游引用為結論**：

| 假說 | 對 `app.py` 自報值之預測 | 實得 22,049 |
|---|---|---|
| H-A（行數上限 ≈2524） | ≈2,524 | 不符 |
| H-B（位元組上限） | ≈4,112（自算） | 不符 |
| H-B′（token 上限） | ≫4,112 | 不符 |
| H-C（無上限） | ＝22,048 | 差 `+1`（＝計數慣例·見 §3） |
| H-D（會話累積） | 同檔兩次不同值 | `app.py` 兩次**同值**；`#4` 跨批**不同值**（§7-2·機制未隔離） |

🛑 **上呈 KL／施工單方之必答題（⛔ CC 不自裁）**

1. **`SAME` 之判準**：現定為「自報 ＝ `wc -l`」，而 `Read` 之計數慣例使其對有尾隨換行之檔**恆不可達**
   ⇒ 是否改為「自報 − 真行數 ∈ {0}」（真行數＝依末字元校正）或「Δ ∈ {0,+1} 視為 `SAME`」？
2. **`DIFF` 是否須分碼**：本批之 `+1`（計數慣例）與前批之 `−169`（疑似截斷）**性質不同**，
   現行單一 `DIFF` 碼**把二者混為一談**——正是紀律第 3 條（無從判定 ≠ 判定為偽·分碼）之同一形狀。
3. **是否授權隔離實驗**：復刻前批之**完整順序**（`offset=2202,limit=492` → `offset=2525`）
   以隔離 H-D 之「受檢順序影響」段。⛔ 本批未做。

---

## §10 本波之戒·逐條自檢（⚠️ 立戒者在下一批最容易違反自己的戒）

| # | 戒 | **已套用** |
|---|---|---|
| 1 | 紀律第 1 條（先現查生產路徑實際吃的是哪一個） | ✅ §1 逐一具名三途徑（含**不可用**者之原文），⛔ 未以「工具」概稱 |
| 2 | 紀律第 3 條（無從判定 ≠ 判定為偽·分碼） | ✅ §3 六碼全備；本批未用 `NO_SELF_REPORT`／`ERROR`／`MISSING` 而正面記其為 0 |
| 3 | 紀律第 4 條（自檢下放一層） | ✅ §6 先判量測器，**未過即停**、⛔ 未續判 `app.py` |
| 4 | 紀律第 7 條（閘之一端來自受詞之外） | ✅ `wc -l`／`tail -c 1`／`od`／`sed` 皆在 `Read` 之外；§2 先於任何 `Read` 執行 |
| 5 | 考古 63（同一次執行） | ✅ §5 之七條字串為本批當次輸出原樣貼出，⛔ 無憑記憶轉寫 |
| 6 | 考古 78（陽性對照非飽和區） | ⚠️ **本批坐實其為飽和**——`#4` 之預期 `DIFF` 與陰性 `#1` **同碼** ⇒ 對照組**無鑑別力**（§6）。⛔ 未以「取個極端值」自我豁免 |
| 7 | 考古 74（現象 ≠ 機制） | ✅ §7-2 記為「現象成立·機制未隔離」，⛔ 未記 H-D 成立 |
| 8 | 紀律第 8 條（錨須為行尾或整行） | ⛔ **本批不適用**——⛔ **未插入任何既有文件**（§0·§8 已正面坐實） |

---

## §11 ⛔ 本批未做之事

1. ⛔ **未作 H-A〜H-D 之擇一判**（§6 停機）。
2. ⛔ **未做 H-D 之順序隔離實驗**（復刻前批完整呼叫序列）——待授權。
3. ⛔ **未改任何判準／出艙碼定義**（域外·上呈）。
4. ⛔ **未動** `app.py`／`verify/`／`docs/rulings/`／`CLAUDE.md`（§8 坐實）。
5. ⛔ **未對「分頁註腳（`of N total`）」通道逐檔量測**——僅併記 `#3` 之單一同值觀測。
6. ⛔ **未跑** `run_all`；⛔ 未觸任何 baseline／凍存物。
7. ⛔ **未更正**上一輪聊天回報之外之任何既有文件——該更正見 §7-2，**本報告即其落點**。
