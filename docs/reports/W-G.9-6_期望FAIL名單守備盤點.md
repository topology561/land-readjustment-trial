# `W-G.9-6`：期望 FAIL 名單之**守備盤點**（純量測·⛔ 零生產碼·⛔ 未立新閘）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg96-audit`）
**基準**：`82d44bd`　**事前登記**：`docs/reports/W-G.9-6_前置登記.md`（commit `0b103e6`）

> # 🛑 **最高優先序：`T-3` 已觸發**
>
> **22 名目中 4 項之當前失敗原因 ≠ 入單時之原因**，而對帳（只比名目）仍為**綠**
> ⇒ **洞 B 不是理論風險，已實際發生**。
> 且該名單**自陳之失效條件已滿足**：其檔頭寫「⚠️ 本名單於**覆蓋閘修復後即失效**」，
> 而 `覆蓋閘破` 之出現次數已由入單時之 **7** 降為現況之 **0**。
> ⛔ 依 §3 未授權清單，CC **未**更新名單、**未**修任何 FAIL。**處置待 KL 裁。**

> # 🔴 **次高：守備規模**
>
> `verify/run_verification.py` 之 `results.append` 站點共 **107** 處，
> 一次完整 `run_all` 中 **80 處從未執行**——即 **80 道閘連「登記一個結果」都沒發生**。
> 可執行敘述行 **807** 行中 **384 行（47.6%）未執行**。

---

## §1 🔴 射程與其上界（⛔ 置於顯眼處·非附註·施工單 §8-2）

**本批核心工具（行覆蓋）之射程<u>嚴格小於</u>命題（「有沒有在守」）。**

| 項 | 內容 |
|---|---|
| **工具** | stdlib `sys.monitoring` 之 `LINE` 事件（Python **3.13.11**）。`coverage.py` 於本機**不可得**，⛔ 本批未安裝任何套件 |
| **足夠性** | `G1` 之命題為「某行執行次數為 0」；`LINE` 事件由直譯器**於每一行執行時**直接觸發 ⇒ 為該命題之**直接觀測量**。`coverage.py` 7.x 於 Python ≥3.12 亦用同一機制 ⇒ 對**本命題**等價（⛔ 不主張分支覆蓋等其他命題等價） |
| **🔴 上界 1** | **行覆蓋只能證「未執行」，⛔ 不能證「執行了就有在守」**——執行了但**恆真**之閘同樣不守任何事。**本批⛔ 未量恆真性**；已執行之 **27** 處是否恆真，**未涵蓋** |
| **🔴 上界 2** | **`run_all` 之 subprocess 段不在覆蓋射程內**（10 支末端夾具等）。射程僅及**同進程執行之 `run_verification.py`**（`run_all.py:261` `import` ／ `:268` `v.main()`） |
| **🔴 上界 3** | **母體限 `run_verification.py`**；`app.py`／`wf_f*.py`／`stepg_pipeline.py` 之內部斷言**不在母體內** |
| **計數上限** | 每行達 `CAP=1000` 即 `DISABLE`，其後記為 `>=1000`。⛔ 已於產物正面標示，非靜默截斷 |

---

## §2 `G1`〜`G5` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **G1** | ✅ **成立** | 可執行敘述行 807 中 **384 未執行**；`results.append` 站點 107 中 **80 從未執行**。**種子實例**（考古 65 之 W-8 反向閘）由覆蓋直證：見 §2-1 |
| **G2** | ✅ **成立** | 改**訊息**不改名目 ⇒ 對帳**未察覺**；反向自檢（改名目）⇒ **已察覺** ⇒ 探針具鑑別力（`T-2` 未觸發） |
| **G3** | ✅ **成立** | **3 項**之原守命題現**無**任何仍活之閘覆蓋：`W-D.3 碎片`／`W-F F.3`／`W-G G.2 世代幾何曝出契約`（三項射程之否定性宣稱見 §4-2） |
| **G4** | ✅ **成立** | 覆蓋跑與 `WG94b_runall.log` 之**判定行** `diff` **rc=0**；`PASS 12／FAIL 22` 同值（⛔ 非以整檔雜湊或行數為據） |
| **G5** | ✅ **成立** | `G4` 之比對相同 ⇒ 覆蓋量測**未改變被測物** ⇒ `T-1` 未觸發，⛔ 無須另跑無覆蓋之對照 |

### 2-1 `G1` 之種子實例（覆蓋直證·⛔ 非讀 `try` 結構推得）

```
:1300  執行=✅ 有       _nat = _ctx["0m"]
:1326  執行=🔴 **0 次**  _ns_missing = sorted(set(ns["_WF_NS_NAMES"]) - set(_bctx["ns"]))
:1337  執行=🔴 **0 次**  import re as _re_ns
:1344  執行=🔴 **0 次**  _ns_used |= set(_re_ns.findall(…))
:1346  執行=🔴 **0 次**  _ns_unlisted = sorted(_ns_used - set(ns["_WF_NS_NAMES"]))
:1347  執行=🔴 **0 次**  if _ns_unlisted:
:1348  執行=🔴 **0 次**  _vg1.append(f"引擎消費但 _WF_NS_NAMES 漏列（app 路徑必 KeyError）：{_ns_unlisted}")
:1379  執行=🔴 **0 次**  results.append(("W-G G.1 接線層 ctx-builder 同源（…）", …))
:1381  執行=✅ 有       except Exception as _e_g1:
:1383  執行=✅ 有       results.append(("W-G G.1 接線層 ctx-builder 同源", False, …))
```

⇒ 考古 65 第二層所述之機制（**閘在、但寄生於崩掉的前置**）**由覆蓋資料證實**。

---

## §3 🛑 `T-3`：洞 B 已實際發生（4／22 項）

**準則之出處**（⛔ 非 CC 自撰）：**正典·權威序第 2 級**，`CLAUDE.md:71-72` 逐字——
「與凍存之期望 FAIL 名單**逐項相同**」、「**逐項名目**·⛔ 非計數」。

### 3-1 逐項判定（⛔ 已正規化·未正規化之數不得出艙）

⚠️ **正規化之必要**：入單時 log（`D2b1_runall.log`）與現況 log 產於**不同 worktree**
⇒ 明細行內 traceback 含絕對路徑。**未正規化直接比對得 12 項「已變」，
去絕對路徑與 traceback 行號後實為 4 項**——⛔ 前者不得出艙（`W-G.9-4b` `E4` 同型教訓）。
🔒 已實測：其餘 8 項**僅差絕對路徑**，traceback **行號本即相同** ⇒ 該正規化未遮蔽真差異。

| 名目 | 入單時之失敗原因 | 現況之失敗原因 |
|---|---|---|
| `v3·StepG0m（結構閘/看守觸發）` | `①' 覆蓋閘破[R1]：\|union(宗+池) − 街廓\| = 12.0568 > 0.01` | `結構閘 理論＝實跑 破：R1 left 理論@k*=97.18 ≠ 實跑=100.02（Δ=2.837）` |
| `v3·StepG3.5m（結構閘/看守觸發）` | `①' 覆蓋閘破[R1]：… = 12.0553 > 0.01` | `結構閘 理論＝實跑 破：R1 left 理論@k*=96.30 ≠ 實跑=98.75（Δ=2.446）` |
| `W-D.3 碎片` | `compute 失敗：… ①' 覆蓋閘破[R1] … 12.0568` | `compute 失敗：結構閘 理論＝實跑 破 …` |
| `W-D.4` | `compute 失敗：… ①' 覆蓋閘破[R1] … 12.0568` | `compute 失敗：結構閘 理論＝實跑 破 …` |

⇒ 四者**同源**：根因由 `①' 覆蓋閘破` **遷移**至 `結構閘 理論＝實跑 破`。
**而對帳結果（只比名目）＝ 符合。**

### 3-2 名單**自陳之失效條件已滿足**

名單檔頭（`K6A2_期望FAIL名單_D2b1切換後.txt`）逐字：

> 🔑 **根因單一**：`①' 覆蓋閘破`（app.py:8789）… 本 log 中 `覆蓋閘破` 出現 **7** 次…
> ⚠️ 本名單於**覆蓋閘修復後即失效**——本批之紅係**單一阻塞之連鎖**、非各自獨立之問題。

```
$ grep -c "覆蓋閘破" verify/out/D2b1_runall.log        （入單時）
7
$ grep -c "覆蓋閘破" verify/out/WG96_runall_cov.log    （現況）
0
```

⇒ **該名單依其自身條款已失效**，而仍作為驗收基準使用中。
🔴 **⛔ CC 未更新名單**（§3 未授權）——**處置待 KL 裁**。

### 3-3 另一項具名不一致（⛔ 本批不代裁）

| 項 | 正典（`CLAUDE.md:71-72`·權威序第 2 級） | 現行實務（施工單 §0／兩份交接文） |
|---|---|---|
| 凍存名單檔 | `K6A2_期望FAIL名單_902f5d1.txt`（**46** 名目） | `K6A2_期望FAIL名單_D2b1切換後.txt`（**22** 名目） |

現況 log 對 `902f5d1` 對帳 ⇒ **不符**；對 `D2b1切換後` ⇒ **符合**。
⇒ **正典所指之基準與實務所用之基準不是同一份。**

---

## §4 `Q3`：22 名目逐項三欄（⛔ 含 ②③ 為空者亦正面列出）

**② 之判定一律來自覆蓋資料**（`verify/out/WG96_coverage.txt`），⛔ 非由 `try` 結構之閱讀推得。

| # | 名目 | ① 站點（`run_verification.py`） | ② 失聲之判定式 | ③ 現由誰守 |
|---|---|---|---|---|
| 1 | `參數0m` | `:513` | **無**（不在 `try` 內） | **自身仍活**（本輪有執行·產出 FAIL 明細） |
| 2 | `參數3.5m` | `:513` | **無** | **自身仍活** |
| 3 | `v3·診斷0m（全欄逐格·無豁免）` | `:599` | **無** | **自身仍活** |
| 4 | `率接線無串聯0m（…）` | `:608` | **無** | **自身仍活** |
| 5 | `率接線 G估 欄變動0m 1 格（…）` | `:629` | **無** | **自身仍活** |
| 6 | `指配0m` | `:640` | **無** | **自身仍活** |
| 7 | `v3·StepG0m（結構閘/看守觸發）` | `:685` | `try(:655)` 27 行→**24 行未執行**；死站點 **5**：`結構不變量永久閘`／`v3·G值`／`v3·滑池槽`／`v3·J表`／`k* 六塊經驗錨` | 候選 4（皆為**財務錨**類·⚠️ 僅共享「錨」字、**不守同一命題**）⇒ **實質無人接手** |
| 8 | `v3·診斷3.5m（…）` | `:599` | **無** | **自身仍活** |
| 9 | `率接線無串聯3.5m（…）` | `:608` | **無** | **自身仍活** |
| 10 | `指配3.5m` | `:640` | **無** | **自身仍活** |
| 11 | `抵費地3.5m` | `:645` | **無** | **自身仍活** |
| 12 | `v3·StepG3.5m（…）` | `:685` | 同 #7（**24 行**／死站點 **5**） | 同 #7 ⇒ **實質無人接手** |
| 13 | `v3 財務接線閘` | `:786` | `try(:692)` 76 行→**50 行未執行**；死站點 **6**（`reverse-test①②③`／`重劃總負擔率錨`／`率分項錨`／`率用 DXF 公設實值`） | 候選 4（`v3 財務錨 B`／`C`／`中繼錨`／`A 逐宗全查`·**皆仍活**）⇒ **部分接手**：靜態錨仍守，**reverse-test（隨動性）無人接手** |
| 14 | `W-D.3 碎片` | `:805` | `try(:793)` 8 行→**6 行未執行**；死站點 **2**（`碎片幾何/三分類（v3）`／`碎片逐邊CAD（v3）`） | 🔴 **無人接手**（§4-2 之否定性宣稱） |
| 15 | `W-D.4` | `:921` | `try(:810)` 76 行→**74 行未執行**；死站點 **14** | 候選 5（含 `W-D.3 碎片`——**其本身亦為 FAIL 之殼**）⇒ **實質無人接手** |
| 16 | `W-F F.0` | `:1010` | `try(:933)` 65 行→**58 行未執行**；死站點 **13** | 候選 4（財務錨類）⇒ **實質無人接手** |
| 17 | `W-F F.1` | `:1061` | `try(:1015)` 33 行→**31 行未執行**；死站點 **5** | 候選 5（同上）⇒ **實質無人接手** |
| 18 | `W-F F.2` | `:1127` | `try(:1066)` 49 行→**47 行未執行**；死站點 **11** | 候選 4（同上）⇒ **實質無人接手** |
| 19 | `W-F F.3` | `:1191` | `try(:1132)` 48 行→**46 行未執行**；死站點 **12** | 🔴 **無人接手**（§4-2） |
| 20 | `W-F F.4` | `:1292` | `try(:1199)` 70 行→**68 行未執行**；死站點 **10** | 候選 4（財務錨類）⇒ **實質無人接手** |
| 21 | `W-G G.1 接線層 ctx-builder 同源` | `:1383`（except 分支） | `try(:1299)` 70 行→**69 行未執行**；死站點 **1**（`:1379` 主判定·**內含 W-8 反向閘**） | ⚠️ **部分接手**：`_WF_NS_NAMES ↔ ns[]` 子命題已由 `verify/fixture_wf_ns_wiring.py`（`W-G.9-4b` 新增·**在 `run_all` 內**·本輪 `rc=0`）接手；**其餘子命題**（`cb_by`／`cad+centerlines`／`gA`／`winners`／β財務／`blocks` 欄名／指紋／率鋪底）**無人接手** |
| 22 | `W-G G.2 世代幾何曝出契約` | `:1437`（except 分支） | `try(:1392)` 38 行→**32 行未執行**；死站點 **1**（`:1433` 主判定） | 🔴 **無人接手**（§4-2） |

**小結**：22 項中 **12 項**之 `try` body 有未執行行；落於其中之**死站點共 80 處**
（＝全檔死站點總數，⛔ 無遺漏於 22 項之外者）。

### 4-1 ⚠️ 「候選」之解讀（⛔ 不得讀成「有人接手」）

③ 欄之「候選」係機械求得：**曾執行之站點 ∩ 命題關鍵詞**（⛔ 排除該項自身之站點）。
**候選存在 ≠ 接手**——多數候選僅共享「錨」「接線」等字，**所守命題不同**。
🔒 故上表對候選非空者多判為「**實質無人接手**」，其判斷依據為**逐一讀該候選之名目字面**。

### 4-2 三項「無人接手」之否定性宣稱（⛔ 三項射程齊備）

- **【目錄射程】** `run_all` 之**自動流程執行單元**：`verify/run_verification.py`（同進程）
  ＋ **10 支末端夾具**（subprocess）＋ `verify/run_all.py` 之 golden 段。
  ⚠️ **⛔ 不含** `verify/probes/**`、`verify/wg_g*_smoke.py`、`verify/wg_g3.py`、`verify/tools/**`
  ——現查**皆不在任何自動流程內**（`grep -c "wg_g1_smoke\|wg_g2_smoke\|wg_g3" verify/run_all.py` ⇒ `0`）。
- **【字樣射程】** 逐項多式、⛔ 不預設符號名（**領域語意詞**）：
  `W-D.3` ＝ `碎片`／`三分類`／`逐邊`；
  `W-F F.3` ＝ `F.3`／`公設調配`／`轉7-4`／`trunk D`／`F.3·`／`零遺漏`；
  `W-G G.2` ＝ `曝出`／`只寫不讀`／`cut_coords`。
- **【連接方式】** **各式獨立裸 grep、逐式記命中檔**；⛔ 無管線過濾、⛔ 無固定距離窗。

**結果**：

| 項 | 夾具／`run_all` 命中 | 判 |
|---|---|---|
| `W-D.3 碎片` | `碎片`／`三分類` 於 `fixture_end_fallback.py`／`fixture_end_winner.py` 各 1 命中——**現查其為資料欄值** `"三分類": "碎片"`（合成宗之欄位），**非閘** | 🔴 **無人接手** |
| `W-F F.3` | 六式**全部 0 命中** | 🔴 **無人接手** |
| `W-G G.2` | `只寫不讀`／`曝出` 僅命中 `run_verification.py`（即其死站點自身） | 🔴 **無人接手** |

---

## §5 `Q2` 探針之兩態（施工單 §6 閘 4）

| 試驗 | 內容 | 結果 |
|---|---|---|
| **A** | 🔴 **真實資料**：入單時 log 與現況 log **皆**對帳 | 二者**皆符合**（⇒ 對帳看不出四項原因已變） |
| **B** | 合成：只改**訊息**、名目不動 | **✅ 符合（＝未察覺）** ⇒ `G2` 成立 |
| **C** | **反向自檢**：只改**名目**（`參數0m` → `參數0m·合成改名`） | **✅ 不符（已察覺）** ⇒ 具鑑別力，`T-2` 未觸發 |
| **D** | 22 名目逐項訊息比對 | raw 異 12 → **正規化後真正原因已變 4** |

⛔ 合成僅存於記憶體；**未改動 `verify/out/` 之任何真實檔**、未改名單、未改 `run_verification.py`。

---

## §6 §2-4：`wg_g1_smoke.py` 之盤點（⛔ 只盤不掛）

🔴 **具名更正（CC 自誤·`W-G.9-4b` §9-3）**：該報告稱 `wg_g1_smoke.py` 為
「真 app 路徑之**唯一**端到端複本」——**現查為誤，實有三支**：

| 檔 | `_build_wf_ctx` 命中 | 跑 `wf_f4.compute` | 在 `run_all` 內 |
|---|---|---|---|
| `verify/wg_g1_smoke.py` | 6 | ✅ | 🔴 **否** |
| `verify/wg_g2_smoke.py` | 3 | ✅ | 🔴 **否** |
| `verify/wg_g3.py`（**W-G 收官王牌**·`CLAUDE.md` 收官判準 ① 所指之 G.3 終驗） | 6 | ✅ | 🔴 **否** |

⇒ **真 app 路徑（`_build_wf_ctx` → f0→f4）之三支端到端複本，全部不在任何自動流程內。**
其中 `wg_g3.py` 係 `CLAUDE.md` 明訂之 **W-G 收官判準 ①** 之驗證工具。

🔴 **本批 ⛔ 不得掛入**（掛入會改變 `run_all` 判定層、與本批「零觸」抵觸）。
**結論交下一批**；優先序見 §8。

---

## §7 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未立任何新閘**。
2. ⛔ **未修任何既有 FAIL**（含 `W-G G.1` 之 `KeyError: '0m'`、結構閘 `理論＝實跑`、`①'` 相關）。
3. ⛔ **未更新任何一份期望 FAIL 名單**（四份皆未動），縱使 `T-3` 已觸發、且名單依自身條款已失效。
4. ⛔ **未掛** `wg_g1_smoke.py`／`wg_g2_smoke.py`／`wg_g3.py` 或任何檔入 `run_all`。
5. ⛔ **未量任何閘之恆真性**——已執行之 **27** 處是否恆真，**完全未涵蓋**。
6. ⛔ **未涵蓋 subprocess 段**（10 支末端夾具之內部行覆蓋不在射程內）。
7. ⛔ **未涵蓋** `app.py`／`wf_f*.py`／`stepg_pipeline.py` 之內部斷言。
8. ⛔ **未動生產碼**（`git diff --numstat -- app.py` ⇒ 空輸出）。
9. ⛔ **未安裝任何套件**（`coverage.py` 仍不可得，改用 stdlib）。
10. ⛔ **未代裁** §3-3 之「正典指名 `902f5d1` vs 實務用 `D2b1切換後`」。

---

## §8 下一批**立閘優先序**（⛔ 只排序·不實作）

判準：**土地後果之近距離 × 現況無人接手之確定性 × 立閘成本**。

| 序 | 標的 | 理由 |
|---|---|---|
| **1** | **對帳準則本身：名目 → 名目 ＋ 失敗原因指紋** | `T-3` 已實證洞 B 發生。**此為根治項**——不修它，以下每一項都會重演。成本最低（名單加一欄「原因指紋」即可） |
| **2** | **`W-G G.1` 之上游 `_ctx["0m"]`** | 一崩即使 **69 行**判定式（含 W-8 反向閘）全數失聲。修好上游 ⇒ #21 之「其餘子命題」自動復活，**一項換回一整片** |
| **3** | **`W-F F.3`（公設地調配 trunk D）** | 六式全 0 命中、**確定無人接手**；且其死站點含**守恆／位次序**類命題 ⇒ 土地後果直接 |
| **4** | **`W-D.3 碎片`** | 確定無人接手；碎片三分類直接影響 W-D.4 四梯與遞補 |
| **5** | **`W-G G.2 世代幾何曝出契約`** | 確定無人接手；惟其為**呈現層契約**，土地後果較間接 |
| **6** | **`v3·StepG` 之 5 個死站點**（`結構不變量永久閘`／`v3·G值`／`滑池槽`／`J表`／`k* 六塊經驗錨`） | 均為土地後果最近之命題，但其復活**取決於結構閘另案** ⇒ 不宜先於該另案 |
| **7** | **三支端到端 smoke／G.3 終驗掛入自動流程** | 覆蓋率洞同族；`wg_g3.py` 為收官判準 ① 之工具。⚠️ 掛入必改判定層 ⇒ 須與重烤時點協調 |

⚠️ **序 6／7 之前置**均含**本批不得處置之另案**，⛔ 不得逕自開工。

---

## §9 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**·雜湊一律 `git hash-object`）

```
$ git rev-parse --short HEAD
（見 §10 commit 鏈）

$ git hash-object verify/out/WG96_coverage.txt verify/out/WG96_runall_cov.log \
                  verify/out/probe_WG96_reconcile.log verify/out/probe_WG96_inventory.log
d0119528c354f04a7d97c0af46232014d1994e0f   WG96_coverage.txt
3e5bd86d657001fed25d58842da1b57add6bedf5   WG96_runall_cov.log
09af442d07ac8ee1045226df7c42b7fe44fd6c4c   probe_WG96_reconcile.log
e1ccaf79472e571960c7e1b527ef991a73c0c23a   probe_WG96_inventory.log

$ python -V
Python 3.13.11
$ python -c "import coverage"
ModuleNotFoundError: No module named 'coverage'

$ grep -c "results\.append" verify/run_verification.py
107
$ grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_D2b1切換後.txt
22
$ grep -c "覆蓋閘破" verify/out/D2b1_runall.log      ⇒ 7
$ grep -c "覆蓋閘破" verify/out/WG96_runall_cov.log  ⇒ 0

$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG94b_runall.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG96_runall_cov.log); echo rc=$?
rc=0
$ grep -c '✅ PASS' verify/out/WG96_runall_cov.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG96_runall_cov.log ⇒ 22

$ grep -c "wg_g1_smoke\|wg_g2_smoke\|wg_g3" verify/run_all.py
0
$ find verify -name "*.py" | wc -l
115

$ git diff --numstat -- app.py            ⇒ （空輸出）
```

⚠️ **一項自誌**：§4 之「③ 候選為空」清單，CC **首版**以
`grep -B12 "候選為空" … | grep "^▸"` 抽取 ⇒ 得 `{W-D.3, W-G G.1, W-G G.2}`，**錯**。
成因：`-B12` 之**窗口截斷**（管線過濾在後）。改以探針**原文逐項**讀取後，
正解為 `{W-D.3, W-F F.3, W-G G.2}`。
🔒 ⇒ 本倉已記之「窮舉 grep 受過濾管線限制」再現一次，**且是在寫『守備盤點』的當下**。

---

## §10 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `0b103e6` — 事前登記（⛔ 零量測·零碼變更）
- **B** `20f3446` — 量測：覆蓋跑、`Q2` 探針、`Q3` 盤點與 log（⛔ 零文件結論）
- **C** 本報告 ＋ `GB` 登記 ＋ 考古節
