# `W-G.9-223`（兩項修訂）執行報告：能力斷言之**實測否證** ＋ 「單筆列命中」之框

> 🔒 **本檔＝本修訂之唯一本體**；⛔ 回改 `W-G.9-223R` 一字（其已於 `8b4c3d4` 推送）。
> 🔒 **態**：落地前 `HEAD ＝ 8b4c3d4bd78be457a829eafbc12d9779c6081a05`。
> 🔒 **⛔ 採用單所載之任何數為錨**——全數當場復算；與單所載不符者於本報告具名。
> 🔒 **零生產碼**：閘 P 五檔 `sha256` 落地前後逐位相符。
> 🔒 **本批鑄號 ＝ `自誤 280`／`自誤 281`**；加註三處。

---

## §一　工項六（新·取代原工項六）：發單側能力斷言之實測否證 ⇒ 鑄 **`自誤 280`**

### 實測（可重跑·`rc` 直取·⛔ 經管線·⛔ 用本機憑證）

於一空目錄內，**停用一切憑證輔助**：

```
GIT_TERMINAL_PROMPT=0 GIT_ASKPASS=/bin/true \
git -c credential.helper= -c credential.useHttpPath=false \
    ls-remote https://github.com/topology561/land-readjustment-trial refs/heads/wip/s1-endpart
```

| 量 | 值 |
|---|---|
| `rc` | **`0`** |
| `stdout` | `8b4c3d4bd78be457a829eafbc12d9779c6081a05	refs/heads/wip/s1-endpart` |
| `stderr` | （空） |

⇒ 🔴 **該倉<u>公開</u>，無憑證即可讀**；其 `HEAD` **與 CC 所報逐位相符**。
🔒 **對照組（證器非紅）**：同法查一必不存在之倉 ⇒ `rc` ＝ **`128`**、
`stderr` 逐字 `remote: Repository not found.` ⇒ **量測器分得開**。
🔒 **閘 V 已於 commit 前<u>重跑</u>該二測**（見 `§四`），二值一致。

### 🛑 攔阻**未及**——其所欲阻止者**已落地並推送**

單載「⛔ 落地（本則即其攔阻）」。**實況**：該擬案已於 `W-G.9-223` 落為
`CLAUDE.md` 之 `## 🔧 \`三方分工鐵律\` 之補正：**發單側⛔ 有倉之存取權**`，
並隨 commit **`8b4c3d4`** 推送至 `origin/wip/s1-endpart`（`2026-09-04 01:30 +0800` 量測時刻）。
⇒ **令到達時該節已在遠端** ⇒ 🔒 **⛔ 回改該節一字**，以**末端追加之更正註**廢其結論。

### 更正註之落地（`CLAUDE.md`·⛔ 回改原節一字）

`## 🔧 \`三方分工鐵律\` 之補正之**更正**：**發單側<u>有</u>倉之存取權**`——其四款：

| 款 | 內容 |
|---|---|
| `一` | `CLAUDE.md:30` 之「**讀已 push 之 commit、由構造驗證**」**為真**——發單側**可**讀該倉 |
| `二` | 🔴 前節之「獨立驗證於發單側這一環**結構上不成立**」**作廢**（其前提經實測為偽） |
| `三` | 「獨立復現」**兼**由 CC 之閘與**發單側之覆核**承擔；⛔ 以「只能由 CC 承擔」免除其覆核義務 |
| `四` | 「凡倉側事實之斷言標為**待 CC 復現**」一款**⛔ 作廢**——其為良性保守做法，惟**理由**改為「出單時未必已跑該量測」，⛔「發單側不能跑」 |

### `自誤 280` 之推導式

> 發單側斷言「從頭到尾查不到該倉」；無憑證 `ls-remote` `rc ＝ 0` 且回傳之 `HEAD`
> 與 CC 所報逐位相符（對照組 `rc ＝ 128`）⇒ **該斷言為偽**。
> **族** ＝ 與「憑記憶給錨」同源（以推想代實測），**受詞為發單側<u>自身之能力</u>**。
> **通則**：凡對「能為／不能為」之斷言須先實測；**射程明含發單側對自身能力之陳述**。
> **責** ＝ 發單側。

### `自誤 279` 之就地加註（方向翻轉·⛔ 回改原節一字）

`279` 之受詞（**陳述與實際權能不符**）**仍成立**，惟其**方向翻轉**：

| | `279` 原節所判 | 本註所正（實測） |
|---|---|---|
| 何者為誤 | `CLAUDE.md:30` 之正字 | 🔴 **發單側之自陳** |
| 實際權能 | 推定「⛔ 有存取權」 | ✅ **有**（`rc ＝ 0`·`HEAD` 相符） |
| `:30` 之真偽 | 判為與自陳不符 | 🔒 **其為真** |

⇒ `279` 之「**責 ＝ 發單側**」不變；其**證據基礎**由「自陳 vs 正字」改為「**自陳 vs 實測**」。

---

## §二　工項七：CC 側出艙框未載 ⇒ 鑄 **`自誤 281`**

### 實測（當場復現·索引側 blob·**列框**）

母體 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **`528697`** B／**`5825`** 列。
**範圍框之命中 ＝ `8` 列**，其中**同時被定義框咬中者 ＝ `3` 列**：

| 列 | 逐字 |
|---|---|
| `:3338` | `## 自誤 143〜156 之總說（**十四則·一批入倉**·claude.ai \`-103\` 窗自犯·\`W-G.9-128\`）` |
| `:3373` | `## 自誤 157〜164（**八則·一批入倉**·claude.ai \`-103\` 窗自犯·\`W-G.9-132\`）` |
| `:3403` | `## 自誤 165〜169（**五則·一批入倉**·claude.ai \`-103\` 窗自犯·\`W-G.9-134\`）` |

🔒 **餘 `5` 列⛔ 重咬**（`:3434`／`:3453`／`:3468`／`:3532`／`:3588`）——其形為
`## 🩸 \`W-G.9-NNN\` 攢批登記：自誤 \`170\`〜\`179\`（…）`，**前綴含數字**
⇒ 定義框之 `[^0-9０-９\n]*?` 前綴不允 ⇒ **二框⛔ 同時命中**。

| 框 | 定義框列命中 | 重咬 | **純單筆** |
|---|---|---|---|
| 舊框（基準） | **`269`** | **`3`** | **`266`** |
| 正字框 `A` | **`268`** | **`3`** | **`265`** |

⇒ 🔴 **所報之數 ＝ 純單筆 ＋ 重咬 `3`**，而其標籤僅載「單筆」⇒ **框未載**，違 `VR-091` 一 `(3)`。
🔒 **對照組（器非紅）**：範圍框對一必不匹配之人造列 ⇒ **`0`**（須 `0`）／對一必匹配者 ⇒ **`1`**（須 `1`）。

### 🛑 單所指之數之來源更正（⛔ 採單所載·當場復現）

單載「`-222R` 所報『單筆列命中 `267`』…扣後純單筆 `264`」。**實測**：
`267` 係 **`W-G.9-223R` `§一 a`** 表之「舊（基準）」欄（落地前態），**⛔ `-222R`**；
`-222R` `§一 b` 表所報為 **`265`**（其純單筆為 **`262`**）。
🔒 **二者皆同一形之誤**，故該號之受詞不因來源更正而變。

### `VR-091` 補款二之補充（已落地·⛔ 改補款二既有四項一字）

> **一** 「**定義框列命中**」＝ 定義框之**全部**列命中（⛔ 扣除任何列）。
> **二** 「**重咬列**」＝ 同時被定義框與範圍框咬中之列。
> **三** 「**純單筆列命中**」＝ **一** `−` **二**。
> **四** 🔴 出艙時**三數須同格併載**；**⛔ 以「單筆列命中」一詞單獨出艙任一數**。

🔒 **⛔ 追改任何既出之數**（`常規四（九）五`）——`-222R` 之 `265`／`-223R` 之 `267` 一字不動。
🔒 **相異／`MAX`／缺號⛔ 受影響**（範圍框之展開本即併入相異集）。

---

## §三　閘 `7`：反向框重算 ＋ **三數併載**（本補充之首度自我適用）

| 框 | 落地前 相異｜列命中／重咬／純單筆 | payload 內 | 落地後 相異／`MAX`｜列命中／重咬／純單筆 | 缺號 | 期望 | 判 |
|---|---|---|---|---|---|---|
| 舊框（基準） | `272`｜`269`／`3`／**`266`** | 相異 `3`（`[279, 280, 281]`）／列命中 `3`／重咬 `0` | **`274`／`281`**｜`272`／`3`／**`269`** | **`[106]`** | `274`／`272` | ✅ |
| **正字框 `A`** | `272`｜`268`／`3`／**`265`** | 同上 | **`274`／`281`**｜`271`／`3`／**`268`** | **`[106]`** | `274`／`271` | ✅ |

🛑 `VR-091` 補款五：缺號 ＝ `[106]`（二框皆然）⇒ ✅。🔒 **次號 ＝ `282`**（二框一致）。

---

## §四　閘

### 閘 `1`〜`6`：append-only 位元組級自證

| 檔 | `HEAD` B | 索引 B | `Δ` | payload B | `Δ−payload` | 前綴 | 檔尾 | `CR` | 哨兵欄 |
|---|---|---|---|---|---|---|---|---|---|
| `W-G.9波_claude.ai側自誤登記.md` | `528697` | `534763` | `+6066` | `6066` | **`0`** | ✅ | ✅ | `0` | ✅ `1` |
| `CLAUDE.md` | `149884` | `152232` | `+2348` | `2348` | **`0`** | ✅ | ✅ | `0` | ✅ `1` |
| `docs/驗證裁定登記表.md` | `359287` | `361311` | `+2024` | `2024` | **`0`** | ✅ | ✅ | `0` | ✅ `1` |

### 閘 `8`：`GB-147` 處置一 ⇒ 三 payload 之哨兵字樣命中合計 ＝ **`0`** ✅

### 閘 `V`：工項六之實測**重跑**（commit 前）

無憑證 `ls-remote` ⇒ `rc` ＝ **`0`**／`stdout` ＝ `8b4c3d4bd78be457a829eafbc12d9779c6081a05	refs/heads/wip/s1-endpart`；
對照組（必不存在之倉）⇒ `rc` ＝ **`128`** ⇒ ✅ **該倉公開·器非紅**。

### 閘 P ／ `left-right` ／ `deletions`

五檔 `sha256` 落地前後逐位相符（`app.py` `1fc5de29…`／`selection_pipeline` `6c678342…`／
`run_verification` `e48f24c8…`／`stepg_pipeline` `02602733…`／`run_all` `4ba89fef…`）。
`numstat`：`39 0 CLAUDE.md`／`121 0 …自誤登記…`／`35 0 …驗證裁定登記表…` ⇒ **`deletions ＝ 0`** ✅。
🔒 **`left-right` ＝ `0 0`**。

---

## §五　🛑 候裁／未辦

1. 🔴 **`W-G.9-219`（含生產碼）仍待執行**——原文已入倉；其【回報後之處置】全效：
   完成自驗後停、⛔ 未經 KL 逐字放行即推入主線、**⛔ 與零生產碼批混入同一 commit**。
2. 🛑 **`K-6-A2` 廢止標記：停**（承 `-223R §三`）——義務僅**部分**被 `K-9-5-15 二` 取消。
3. 🟡 **`自誤 279` 之責歸屬未變（發單側）**，惟其證據基礎已由本批之加註更正；
   ⛔ 追改 `279` 原節一字。
4. 🟡 **歷批以「單筆列命中」出艙之數一字不動**（`常規四（九）五`）；爾後一律三數併載。
