# `W-G.9-294R`　執行報告（**窄批·現查·零生產碼**）：`range_area` 之出處與範圍多邊形實算面積之逐位差

> **態**｜開工 ＝ `a48b3d8acc10850f430d83402cfbbc6755a79c7b`（CC 自倉 `ls-remote` 實查·全 `40` 碼）
> **單** ＝ `docs/orders/W-G.9-294_施工單_range_area之出處與逐位差.md`（工項零已原封入倉·`0d23706`）
> **分級** ＝ **窄**（一項量測 ＋ 一則末端追加·**零生產碼**·**⛔ 鑄任何號**）
> 🛑 本批**⛔ 改碼一字、⛔ 開分支、⛔ 鑄任何號、⛔ 含設計驗算、⛔ 動用 KL 之放行**。
> 🛑 **款 `4` 因<u>單內互斥</u>依 `常規二` 取保守項 ⇒ 其比值 loud 具名為「不可得」**（見 `§五`）。

---

## `§零`　開工閘（九項·同格載產生指令·**全綠**）

| # | 受詞（指令逐字） | **實測** | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `a48b3d8acc10850f430d83402cfbbc6755a79c7b` | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`（`rc` **緊接該指令**） | `rc = 1` ⇒ **非祖先**；判別力[必為祖先] `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d` ⇒ `rc = 0` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`·`P-5` `SELF_SHA256` ✅ **相符**（受詞 `13211` B）｜實算 `31d533faceb4c061…`／單載 `31d533faceb4c061…` | ✅ |
| — | ↳ 二造（**須先自證 bytes 相異**） | 原 `13289` B vs 變造 `13290` B·`cmp rc = 1` ⇒ 二造成立；[必偽]變造本 ⇒ `rc = 1`·**不符** `b90c7426303cde62…` | ✅ |
| `4'` | 受詞自證（坑 `bf`） | 器之表頭逐字印 `W-G.9-294_施工單_range_area之出處與逐位差.md`；含 `W-G.9-294` ＝ **`True`**／含 `W-G.9-293` ＝ **`False`** | ✅ |
| `5` | 同 `4` ＋ `--selftest`（先自證其 `argv` 位置被讀入） | **先自證**：`verify/probes/probe_order_preflight.py:247` 逐字 `if len(sys.argv) > 1 and sys.argv[1] == "--selftest":`／`:252` `r = check(sys.argv[1])` ⇒ 置於 `argv[1]`。**四靶逐項**：靶① `P-3` **紅**／靶② `P-4` **提示**／靶③ `P-6` **提示**／**反靶** `P-3` **不紅**·`rc = 0` | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 `1` > 0／人造字樣 `0`） | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `389`／`399`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | 標籤 **`87`**／可解析 **`87`**（數字系 `26` ＋ 字母系 `61`）·🔴 `0`·末標籤 **`bi`** ⇒ 下一標籤 **`bj`**；母體 ＝ HEAD 追蹤 `.md` **`786`** 檔；造甲 坑 `n` 定義處 `6` ≥ 1／造乙 人造坑 `0`／`0` ⇒ **器非紅** | ✅ |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py` | **正面列舉 `34` 檔**；判別力[必不命中] 子層 `.py` ＝ **`308`**（須 `> 0`·坑 `be`） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`git ls-files` ⇒ **`298`** 檔 | ✅ |

**環境前置檢**：`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` ＝ **`0  0`**｜
`core.autocrlf` 有效值 ＝ **`false`**｜`CLAUDE.md` 工作區 `236365` B ＝ blob `236365` B。

### `§零-2`　號占用之預查

| 號 | `D1` 檔 | `D2` 列 | `D3` 列（嚴格式） | 雙屬列 | **列框** | **檔框** | 鬆框（檔／列） |
|---|---|---|---|---|---|---|---|
| **`W-G.9-294`**（受詢） | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0`／`0` ⇒ **未占用** |
| `W-G.9-293`（造甲[必命中]） | `2` | `2` | `3` | `0` | `5` | `2` | `2`／`12` ✅ |
| 人造號（造乙[必為零]·執行期組出·字面⛔ 出艙） | `0` | `0` | `0` | `0` | `0` | `0` | `0`／`0` ✅ |

（母體 ＝ 全 `docs/` 倉側 blob **`764`** 檔·**檔框 ＋ 列框**·宣告框 `①`–`⑧`）
🔒 **本批⛔ 鑄任何號** ⇒ 無新號之三形查；其驗改由**收工閘 `4`（四簿逐位未動）**承擔。

---

## `§一`　裁之受領（`§一` 四款）

| # | 裁 | CC 之處置 |
|---|---|---|
| `1` | 款 `9` [必相異] 造**撤回**——不成立者係**本單所預設之機制**為誤，**器非紅**；責 ＝ 發單側，⛔ 鑄新號，以末端追加登記 | **受領**；`§四` 已落地（⛔ 鑄任何號） |
| `2` | 殘差之成因**仍未明** ⇒ ⛔ 訂任何取代之準據界、⛔ 判路丙成立與否；本批查另一候選 | **受領**；`§三` 已出艙，**⛔ 作任何該判** |
| `3` | `自誤 399` 之受詞**坐實**；`wf_f1`／`wf_f4`／`app main` 三端之 **loud 具名維持** | **受領**（本批⛔ 再量該項） |
| `4` | `ns` 之身分受領 ⇒ `自誤 380` 所載之舊假說**自本批起⛔ 得再引** | **受領**；本報告⛔ 引該假說 |

---

## `§二`　工項零：**本單原封入倉**　✅（`commit` `0d23706`）

| 款 | **實測** |
|---|---|
| 所指之檔數 | 受詢（本單）＝ **`1`**（母體 ＝ 本 worktree `docs/orders/` 現存 **`160`** 檔·框 ＝ 檔名之**錨定框** `(?<![0-9\-])W-G.9-294(?![0-9])`）｜造甲 `W-G.9-293` 之單 ＝ **`1`** ✅｜造乙 人造名（執行期組出·字面⛔ 出艙）＝ **`0`** ✅ |
| `CR`（框 ＝ `bytes.count(b'\r')`） | 源 **`0`**／磁碟 **`0`**／blob **`0`**；`LF` 皆 **`162`**｜**判別力[必非零]** ＝ 倉內 blob `data/V6.dxf` ⇒ **`12308`** |
| 三方逐位 | `bytes` 皆 **`13289`**；blob `sha1` ＝ `7b2dc9f556726cbdc5d392e10577c1d7bd07a117`；內容 `sha256` **源 ＝ 磁碟 ＝ blob ＝ `805b5b09fc53b8ac7375c4c01e7dcf98232fe43b801d20129241a982debed0e6`**｜判別力[必相異] 變造本 ⇒ `d4100784ef525f11…`·相異 `True` |
| `numstat` | `162`／**`0`** |

---

## `§三`　工項一（**主檢·窄**）：`range_area` 之出處與**逐位差**

🔒 **器** ＝ `verify/probes/probe_WG9294_rangearea.py`（**唯讀**·包裹**純委派**·⛔ 替身）·`rc = 0`·`stderr` **`0`** B·
**全量落檔** ＝ `verify/out/WG9294R_rangearea.log`（`CR` `0`·⛔ `tail -N`）。
🔒 **通道** ＝ harness（`run_step_g`）·**態甲**·`SB ∈ {0.0, 3.5}`·終局二態皆 **（未拋）**；
**底本** ＝ `verify/probes/probe_WG9293_tol.py` 之 `drive`（⛔ 另寫第四份驅動）。
🛑 **本器⛔ 以任何<u>假設之輸入</u>複算**——其「帶之重建」一律用**當場之** `allocation_dir` 與**當場之** `buf`
（⇒ **重建**·**⛔ 設計驗算**·合於單 `§六`「本批⛔ 含設計驗算」）。

### `§三-1`　款 `1`：`range_area` 實參之**賦值鏈逐字**（錨取字樣·先驗唯一性·⛔ 行號為錨）

🔒 **母體正面列舉** ＝ `app.py`／`verify/stepg_pipeline.py`／`verify/run_verification.py`／`verify/selection_pipeline.py`（**四檔**）

| # | 錨（逐字·`grep -F` 固定字串） | 檔 | 命中 | 判 | 該處位置 |
|---|---|---|---|---|---|
| `1` | `_l_min = _row_for_buffer.get('【左】街角最小面積(㎡)')` | `verify/stepg_pipeline.py` | **`1`** | ✅ 唯一 | `:656` |
| `2` | `float(_l_min), 'left', _label=blk_label)` | `verify/stepg_pipeline.py` | **`1`** | ✅ 唯一 | `:661` |
| `3` | `"【左】街角最小面積(㎡)": (rng("left") if has_l else None),` | `verify/run_verification.py` | **`1`** | ✅ 唯一 | `:261` |
| `4` | `return round(float(r.area), 2) if r is not None else None` | `verify/run_verification.py` | **`1`** | ✅ 唯一 | `:250` |
| `5` | `_left_min = round(float(_rng_cr.area), 2)` | `app.py` | **`1`** | ✅ 唯一 | `:20439` |
| `6` | `'【左】街角最小面積(㎡)': _left_min,` | `app.py` | **`1`** | ✅ 唯一 | `:20467` |

🔑 **判別力自檢**（寬字樣·須 `> 1` ⇒ 該檢**⛔ 恆為 `1`**）：`街角最小面積` @ `verify/run_verification.py` ＝ **`3`** 列／
@ `app.py` ＝ **`10`** 列；`round(` @ `verify/run_verification.py` ＝ **`32`** 列。
🔒 **上表之唯一性先驗即 `uniq`-型檢**（坑 `bi`：字樣錨**先驗唯一性**·非唯一即 loud 拒測·**⛔ 逕取首命中**）
——六錨**各命中 `1`**，且每一錨皆附其**寬字樣**之對照（`> 1`）以證該 `uniq` 檢**⛔ 恆為 `1`**。

🔒 **賦值鏈（逐處具名 `檔:函式`）**

| # | 環 | 逐字／出處 |
|---|---|---|
| `1` | **產生處（harness 側）** | `verify/run_verification.py` 之 `def rng(which)`（`:242`）內 ⇒ `r = ns["_build_corner_range_v3"](…)`；`return round(float(r.area), 2) if r is not None else None`（`:250`） |
| `2` | **入參數表欄** | `verify/run_verification.py:261`／`:262` ⇒ `"【左/右】街角最小面積(㎡)": (rng("left"/"right") if has_… else None)` |
| `3` | **取出** | `verify/stepg_pipeline.py` 之 `_run_step_g_impl`（`:656`／`:667`）⇒ `_l_min = _row_for_buffer.get('【左】街角最小面積(㎡)')` |
| `4` | **入 `_corner_buffer_S` 之第 `5` 實參** | `verify/stepg_pipeline.py:661`／`:672` ⇒ `float(_l_min), 'left'/'right', _label=blk_label)` |
| — | **（app 側之同源產生處）** | `app.py` 之 `main`：`_left_min = round(float(_rng_cr.area), 2)`（`:20439`）／`_right_min = …`（`:20441`）⇒ `'【左/右】街角最小面積(㎡)': _left_min/_right_min`（`:20467`／`:20468`） |

🔒 **當場值**（`repr` 全位）與其**直接來源欄**之對拍

| 格 | `range_area` **實參**（當場） | 參數表欄 `【左】街角最小面積(㎡)`（當場列） | 相等 |
|---|---|---|---|
| `R4/left@0.0` | `116.08` | `116.08` | **`True`** |
| `R2/left@3.5` | `308.17` | `308.17` | **`True`** |
| `R4/left@3.5` | `226.01` | `226.01` | **`True`** |

### `§三-2`　款 `2`：`corner_range_polys[(blk, side)]` 之**實算面積**

🔒 **閘 `(i)`（同一性·`is` 比對·⛔ `id`）**：三格皆 `corner_range_polys[(blk, side)]` **`is`** `_build_corner_range_v3` 之回傳 ⇒ **`True`**（執行 `3` 次）。

| 格 | `corner_range_polys[(blk, side)].area`（`repr` 全位） |
|---|---|
| `R4/left@0.0` | `116.07949125015402` |
| `R2/left@3.5` | `308.1653997358061` |
| `R4/left@3.5` | `226.00643589362406` |

### `§三-3`　🔑 款 `3`：**逐位差**（`range_area − area(poly)`·含正負號·`repr` 全位）

| `SB` | 格 | 款 `1` `range_area`（實參） | 款 `2` `area(poly)`（實算） | 🔑 **款 `3` 逐位差** |
|---|---|---|---|---|
| `0.0` | `R4/left` | `116.08` | `116.07949125015402` | **`0.0005087498459772632`** |
| `3.5` | `R2/left` | `308.17` | `308.1653997358061` | **`0.004600264193925341`** |
| `3.5` | `R4/left` | `226.01` | `226.00643589362406` | **`0.0035641063759328517`** |

🛑 **三格之差皆為<u>正</u>**（`range_area > area(poly)`）。**⛔ 判其是否即殘差之成因**——出艙即止。

🔒 **款 `3` 之量與碼面之 `_resid` <u>⛔ 同物</u>（受詞之別·須併記以免混同）**

| 量 | 碼面之式（錨·命中） | 其比較之二端 | 本批三格之值 |
|---|---|---|---|
| 碼面之 **`_resid`** | `_resid = abs(_band_area(buf) - range_area)`（`app.py`·命中 **`1`**）；其消費式 `if _resid > float(tol):`（命中 **`1`**·寬字樣 `_resid` @ `app.py` ＝ **`34`** 列 ⇒ 該 `uniq` 檢⛔ 恆為 `1`） | **帶之面積** vs **`range_area`** | 本器之閘 `(ii)` 係其**外部複算**（同式·以當場之實參重建）⇒ `4.508891038312868e-09`／`3.2185312193178106e-09`／`9.360974217997864e-10` |
| **款 `3`** 之逐位差 | 本報告之量（⛔ 碼面既有之名） | **`range_area`** vs **`area(corner_range_polys[(blk, side)])`** | `5.087498459772632e-4`／`4.600264193925341e-3`／`3.5641063759328517e-3` |

🛑 ⇒ **二者之被比較端相異**（前者比**帶**、後者比**規定範圍多邊形**）⇒ **⛔ 互代**；
其量級相差約 `5`–`6` 個數量級**係實測之並列**，**⛔ 任何因果之判**。

### `§三-4`　款 `5`：款 `1` 之值**是否經捨入**

🔒 **判 ＝ <u>有</u>捨入**（**⛔ 書「查無」**）。其**位數逐字 ＝ `2`**，二產生處之式皆為 `round(float(<poly>.area), 2)`：

> `verify/run_verification.py:250`　`return round(float(r.area), 2) if r is not None else None`
> `app.py:20439`　`_left_min = round(float(_rng_cr.area), 2)`
> `app.py:20441`　`_right_min = round(float(_rng_cr.area), 2)`

🔒 **所查之母體與字樣**（逐項列舉·⛔ 全倉排除式）：母體 ＝ 上開**四檔**；
字樣 ＝ `§三-1` 表之**六個逐字錨**（各命中 `1`·已附判別力自檢）。

### `§三-5`　款 `6`：判別力二造

| 造 | 判準 | **實測** | 判 |
|---|---|---|---|
| **[必非零]** | **現行** `buf` 所生之帶（**重建**·**當場之實參**·⛔ 設計驗算）與規定範圍之對稱差須 `> 0` | `13.055879754480518`（`甲∖乙` `6.5281919472685885`／`乙∖甲` `6.527687807211929`）／`38.06818855324997`（`19.036394407279545`／`19.031794145970423`）／`13.05518976183967`（`6.529376933853159`／`6.525812827986511`） | ✅ |
| **[必為零]** | **同一多邊形**之面積與其自身之差 ＝ `0`（**先自證可滿足**） | `0.0`／`0.0`／`0.0` | ✅ |

⇒ **二造皆成立 ⇒ 器非紅**（⛔ 停機上呈之器紅款未觸發）。

🔒 **自我驗證閘之逐閘<u>執行次數</u>**（⛔ 以 `rc = 0` 充其已跑）

| 閘 | 受詞 | **執行次數** | 逐次之判 |
|---|---|---|---|
| `(i)` | `corner_range_polys[(blk, side)]` **`is`** `_build_corner_range_v3` 之回傳 | **`3`** | 皆 `True` ⇒ ✅ |
| `(ii)` | 帶之**重建**（當場之 `allocation_dir`／`buf`）面積與 `range_area` 之 `\|Δ\| ≤ tol` | **`3`** | `4.508891038312868e-09`／`3.2185312193178106e-09`／`9.360974217997864e-10`（`tol` 皆 `0.01`）⇒ 皆 ✅ |
| `(iii)` | 同一多邊形之面積與其自身之差 ＝ `0`（＝ 款 `6` [必為零]） | **`3`** | `0.0`×3 ⇒ 皆 ✅ |

---

## `§四`　工項二：一則末端追加　✅（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末）

🔒 **payload 之來源**（`常規七 一`：塊內文字**由檔案管線直接產生**·⛔ 憑記憶重寫）——自**已入倉之單**逐列切片：

| 受詞 | 單之列（1-based·含兩端） | 列數 | bytes | `sha256`（前 16） |
|---|---|---|---|---|
| `§四` 末端追加 | `96`–`109` | `14` | `1424` | `1bf639359ca8a58d…` |

🔒 **`自誤 393` 判準之施行**：payload **⛔ 含 `###` 標題列**（機驗：逐列皆以 `>` 起首 ＝ `True`／以 `#` 起首之列數 ＝ **`0`**）
⇒ **原樣保留 `> `**（⛔ 剝層）。

🔒 **`GB-132` 之避讓（機械自檢·⛔ 目視）**：本批**自撰**之標題列，以該簿**三定義框**逐一擷取 ⇒ 得 **`[170]`**（**已鑄**）⇒ **⛔ 造新號**。其逐字：

> `## 🔧 \`GB-170\` 之修：\`tol\` ⛔ 控精度 ＋ \`自誤 398\` 之**第二後果**（\`W-G.9-294\` \`§四\`·⛔ 上文一字不刪·純末端追加）`

| 落點 | 期初 bytes | 追加段 | 期末 bytes | `numstat` | 嚴格前綴檢（造甲／造乙） | 簡繁機檢 |
|---|---|---|---|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `895489`（`7900` 列·`CR` `0`） | `1927` B／**`23`** 列·`CR` `0` | **`897416`** | **`+22`／`−0`**（**當批實測** `git diff --cached --numstat`·⛔ 轉引前批） | `True`／`False` | 簡體命中 `{}`·`rc = 0`（判別力[必命中] 注入 `3` 字 ⇒ `3`） |

🛑 **二框之別（當批實測·⛔ 擇一出艙）**：器所報之追加段列數（框 ＝ `add_text.count("\n") + 1`·**含末之空行**）＝ **`23`**；
`git diff --cached --numstat` 之新增列 ＝ **`22`** ⇒ **差 `1`**。
其成因 ＝ 期初末尾為 `'…成就。**\n'`（**以單一 `\n` 結尾而⛔ 以空行結尾**）⇒ 追加時之分隔 `\n` 與該尾併成**一列**，
`git` 之「新增列」⛔ 計之。⚠️ **該差⛔ 為常態**——`W-G.9-293R` 之自誤簿本批為 `27` vs `+27`（**無差**）
⇒ **一律當批實測**（`W-G.9-292R §七` 自捕 `3` 之戒）。

🔒 **機械之 append-only 稽核**（`wg942_append_audit.py 0d23706`）：受檢 **`77`** 筆／不適用 `0`
⇒ 逐筆「嚴格前綴 `True`·判別力自檢（行中插入）`False`」⇒ 逐字 **「結論：✅ 全部成立」**·`rc = 0`。

🔒 **四簿期末（坐實⛔ 鑄號）**：自誤 `389`／`399`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29`
⇒ **與期初逐位相同**（`§零` 閘 `6` 之值）。

---

## `§五`　🛑 款 `4`：**單內互斥 ⇒ 依 `常規二` 取保守項**

🔒 **互斥之二端（逐字）**

| 端 | 逐字 | 出處 |
|---|---|---|
| 甲 | 「該差與**路丙之對稱差**（`5.0259e-4`／`4.6003e-3`／`3.5641e-3`·**當場重得**·⛔ 轉引）之**逐格比值**」 | 單 `§三-2` 款 `4` |
| 乙 | 「⛔ 驅動生產路徑之替身（本批**⛔ 含設計驗算**）」 | 單 `§六` |

🔒 **何以互斥**：重得**路丙**之帶，須以**假設之輸入**（`_first_corner_alloc_dir(side_mid)` 取代當場之 `allocation_dir`）
複算——依 **`W-G.9-292 §三-2`** 之界定（「設計驗算 ＝ 於探針內以純幾何複算一個**假設之輸入**所生之結果」）
**即設計驗算**；而乙端明令本批**⛔ 含**之。

🔒 **處置（`常規二`）**：**取保守項**（遵 `§六` 之禁）⇒ 本批**⛔ 跑設計驗算**，
**款 `4` 之逐格比值 ⇒ 🟡 不可得（loud 具名）**，其互斥已**逐字回報**於上。
🛑 **「不可得」⛔ 等同「比值為零」，亦⛔ 等同「款 `4` 已辦」**（承「`序 11` 之一般化」款 `三`：
「**無從回測**」⛔ 等同「回測不過」）。
🛑 本報告**⛔ 以轉引之數代算該比值**（單明令「當場重得·⛔ 轉引」），亦**⛔ 就款 `3` 與路丙之對稱差作任何比較或判**。

---

## `§六`　一句話推導

> `range_area` 之**唯一產生式**（二處·錨各命中 `1`）為 **`round(float(<poly>.area), 2)`**（**位數 `2`**），
> 其經參數表欄 `【左/右】街角最小面積(㎡)` 入 `_corner_buffer_S` 之第 `5` 實參（三格之當場值與來源欄**逐位相等**）；
> 而 `corner_range_polys[(blk, side)]`（**`is`** `_build_corner_range_v3` 之回傳·閘 `(i)` `3`／`3`）之**實算面積**
> 為 `116.07949125015402`／`308.1653997358061`／`226.00643589362406`
> ⇒ 🔑 **逐位差 `range_area − area(poly)` ＝ `5.087498459772632e-4`／`4.600264193925341e-3`／`3.5641063759328517e-3`（三格皆正）**；
> 款 `4` 之比值因**單內互斥**依 `常規二` 取保守項而**不可得**，
> **⛔ 判其是否即殘差之成因、⛔ 訂任何取代之準據界、⛔ 判路丙成立與否**。

---

## `§七`　🩸 本批之自捕

🔒 **本批⛔ 有自捕**——`§三` 之器一次跑成（`rc = 0`·`stderr` `0` B），其三重自我驗證閘逐閘執行 `3` 次且逐次皆 ✅；
`§四` 之落地於 `--dry` 與實落之數**逐位相同**。
🛑 **「⛔ 有自捕」⛔ 等同「⛔ 有瑕」**——其僅謂**本批未於出艙前攔下任何瑕**；
本報告之一切數皆附其**框、母體與產生指令**，並附判別力二造（見 `§三-5`）。

🔒 **併記（照實·⛔ 掩蓋）**：`§五` 之**單內互斥**係**單之瑕**（發單側），**⛔ 本批之自捕**，
依 `常規二` 由**發單側當場裁並具名為發單側自誤**；本批之處置為**取保守項續辦 ＋ 逐字回報**，
**⛔ 鑄任何號**（單 `§六` 明令）。

---

## `§八`　🛑 `⛔ 為之事`（`§六`）之**逐項自檢**

| # | `⛔ 為之事` | 自檢（機械） | 判 |
|---|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔一字 | 生產碼判法逐區間命中 **`0`** 行；對照組[必非零] `17fd981^..17fd981` ＝ **`2`**；`34` 檔 blob 期初＝期末**相異 `0`** | ✅ |
| `2` | ⛔ 開任何分支 | `git ls-remote origin 'refs/heads/verify/*'` ⇒ `verify/W-G.9-289-gb170` 命中 **`0`**；[必存在] `p3a` ＝ **`1`** | ✅ |
| `3` | ⛔ 動用 KL 之放行 | 由 `1` 機械蘊含 | ✅ |
| `4` | ⛔ 擬路丁或任何第四形 | 本報告通篇為**現查之出艙**；`§五`／`§六` 逐字保留其禁 | ✅ |
| `5` | ⛔ 訂任何取代之準據界 | `§三` 只出艙**逐位差**之值，**⛔ 以之為界**；`§六` 逐字載其禁 | ✅ |
| `6` | ⛔ 判殘差之成因 | `§三-3`／`§六` 逐字載「**⛔ 判其是否即殘差之成因**——出艙即止」 | ✅ |
| `7` | ⛔ 事後放寬任何門檻 | 本批⛔ 觸及任何門檻（準據 `A`／`tol` 皆一字未動） | ✅ |
| `8` | ⛔ 驅動生產路徑之替身（本批**⛔ 含設計驗算**） | 本器之包裹**一律純委派**（`out = _o_*(*a, **kw)` 後原樣 `return out`）；其「帶之重建」用**當場之實參**（⛔ 假設之輸入）；`§五` 之款 `4` 已因此**不辦** | ✅ |
| `9` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／任何函式簽章 | 同 `1` | ✅ |
| `10` | ⛔ 動 `ALPHA_LABELS` 一字 | `git diff --numstat -- verify/probes/probe_WG9269_pit_index.py` 輸出**空** | ✅ |
| `11` | ⛔ 覆寫 `verify/baselines`、⛔ `WV_BAKE`、⛔ 併線 `p3a`、⛔ 引任何自 baseline 取得之數 | `baselines` `298` 檔·框 `a898f4e1…` 期初＝期末；`WV_BAKE` ＝ `None`；`p3a` `rc = 1`；本報告之數全部出自 `git`／本批之器 | ✅ |
| `12` | **⛔ 鑄任何號** | 四簿期末 自誤 `389`／`399`·`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` **⇒ 與期初逐位相同**；坑 `87`／`87`·末標籤 `bi` | ✅ |
| `13` | ⛔ 解除或收窄任何 `GB` | `§四` payload 末逐字保留「**失效條件三款仍⛔ 有一款成就**」 | ✅ |
| `14` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | 同 `1` | ✅ |
| `15` | ⛔ 追改任何已入倉文件一字 | 逐 `commit` `git show --numstat` 之 **`deletions` 欄全 `0`**；`wg942_append_audit` `77`／`77` 全成立 | ✅ |
| `16` | ⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | `§五`／`§七` 逐字保留「屬發單側」；本報告⛔ 含任何修法主張 | ✅ |
| `17` | ⛔ 刪任何 ref | 本批⛔ 執行任何 `git push --delete`／`git branch -d` | ✅ |
| `18` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | `§九` 之 `ls-remote` 實查於**推送後**以**末端追加**出艙（⛔ 預寫） | ✅ |

---

## `§九`　逐 `commit`

（收工閘之實測與逐 `commit` 之 `ls-remote` 實查，於報告入倉**後**以**嚴格末端追加**出艙。）
