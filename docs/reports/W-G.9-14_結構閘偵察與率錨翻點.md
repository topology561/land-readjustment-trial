# 【W-G.9-14 全單報告】結構閘另案之純偵察 ＋ 考古 71 修法升格 ＋ 率錨翻點定位

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`6f066a7`　**事前登記**：`docs/reports/W-G.9-14_前置登記.md`（commit `f02bb47`）
**字樣錨（倉規 A·唯一命中）**：`【W-G.9-14 全單報告】`

> # 🔴 **歸因已明**——且**修它會改變分配面積** ⇒ 依施工單 §4 **上呈 KL**（見 §7）。
>
> **`Δ` 之完整分解**（兩道自我驗證閘皆過·⛔ 非推想）：
>
> | 情境 | **起點項** | **終點項** | 合計 | 引擎自報 `Δ` |
> |---|---|---|---|---|
> | `0m` | **+2.8167** | +0.0000 | **2.8167** | `2.837`（差 `0.0203`） |
> | `3.5m` | **+2.8167** | +0.3689 | **2.4478** | `2.446`（差 `0.0018`） |
>
> 🔑 **一句話**：**理論側自「街角點之 `W`」起算（`0.161877` m），實跑側自「硬 `0`」起算**
> ——`R(0.161877) = 2.8167 %`，這 `2.82` 個百分點就是 `0m` 之全部落差。
>
> 🔴 **施工單 §2-2 之警告「量級差一個數量級（2.8 vs 0.16）」<u>理由不成立</u>**：
> `0.16` 是**公尺**、`2.8` 是**百分點**，二者隔著 `Rw` 累積表。⇒ **已入考古節 72。**
> ⚠️ 惟其**要求**（須以構造驗證、⛔ 不得由既有結論推定）**完全正確**，本批照辦。
>
> 🔒 **⛔ 本批零修復**：`app.py`／`verify/stepg_pipeline.py`／`run_verification.py`／
> `baselines/`／各夾具／三支複本，`git diff --numstat 6f066a7` 皆**空輸出**。

---

## §1 `U1`〜`U7` 逐項結果

| # | 判 | 依據 |
|---|---|---|
| **U1** | ✅ **成立** | 現況重跑之三數與凍存名單**逐字相同**（`0m` `97.18`／`100.02`／`2.837`；`3.5m` `96.30`／`98.75`／`2.446`），`ΣΔ_i` 皆 `+0.000` |
| **U2** | 🔴 **字面破·結論成立**（**事前登記 §1 已預先寫定**） | 碼註之 `0m` 數字（`ΣΔ_i=+0.912`／差 `1.92`）與現況不符；⛔ 但其**結論**（落差與鏈斷量無關）**成立且更強** |
| **U3** | ✅ **成立** | `_th`／`_re` 之組成逐項列出，且分解**恰好解釋** `Δ`（差 `0.0203`／`0.0018` ≤ `0.05`） |
| **U4** | 🔴 **破（3.5m）·字面成立（0m）** | 通過之候選：`0m` ＝ **1 項**（③）／`3.5m` ＝ **2 項**（③⑥）；且二者**非互斥**（同一恆等式之兩個加項）⇒ 候選清單須改為**分解**、⛔ 非單選 |
| **U5** | ✅ **成立** | `stepg_ctx` 之鍵**恆為空**，成因已由 **AST 構造**定位（三段傳遞） |
| **U6** | ✅ **成立** | 三率錨之**最後 `✅ PASS`** 與**最早完全不出現**皆已定位並附 commit |
| **U7** | ✅ **成立** | `run_all` 判定行 `diff` **rc=0**、**零容許差**；`PASS 12／FAIL 22`／對帳 PASS |

🔒 依**考古 69 修法 ④**，`U1`／`U2`／`U7` 之「期望相同／期望為空」皆附判別力自檢（§6）。

---

## §2 甲組：`理論@k*` 與 `實跑(階段1)` 之逐項分解（`U3`）

**產物**：`verify/out/probe_WG914_gate_decomp.log`。

### 2-1 🔴 母體之判定：**二者<u>不是</u>同一母體，但也<u>不是</u>「差幾宗」**

碼註自載「理論側之 widths 結構上只含階段1宗」。**現查該敘述為真**，
⛔ 但**它不是落差之來源**——真正的差異在**兩側之計算形式**：

| 側 | 產生式（⛔ 現查所得·非採信碼註） |
|---|---|
| **理論** | `_select_pool_slot(_adv_base['widths'], …)` ⇒ `ΣRw_L = R(b_L + Σw[:k*]) − R(b_L)`，其中 `_adv_base = _advance_block_with_split(**k_naive**, False)` |
| **實跑** | `_rw_stage1_only('left')` ＝ Σ 逐宗 `Rw(%)`，其 rows 出自 `_adv_final = _advance_block_with_split(**k\***, True)`；由 telescoping 兩閘（皆過）得 `_re = R(W_階段1末) − R(W₀)` |

⇒ **二者皆為 `R(終) − R(起)`** ⇒ 落差只可能來自**起點**或**推進量**，⛔ 無第三處。

### 2-2 逐項數值（鍵＝側別·⛔ 皆自探針 log）

| 量 | `0m` | `3.5m` |
|---|---|---|
| `widths`（`k_naive` 趟） | `[6.46, 17.87, 18.21, 2.38, 7.43]` | `[17.5, 18.29, 6.74, 2.38, 7.43]` |
| `k*` ／ 左群 | `2`／`[6.46, 17.87]` | `1`／`[17.5]` |
| **理論起點 `b_L`** | `0.161877` m ⇒ `R = 2.8167` | `0.161877` m ⇒ `R = 2.8167` |
| **理論終點** `b_L+Σw` | `24.491877` m ⇒ `R = 100.0000` | `17.661877` m ⇒ `R = 99.1209` |
| ⇒ **理論 `ΣRw_L`** | **`97.1833`** | **`96.3042`** |
| **實跑起點 `W₀`** | `-0.000000` m ⇒ `R = 0.0000` | `-0.000000` m ⇒ `R = 0.0000` |
| **實跑終點 `W_階段1末`** | `24.490000` m ⇒ `R = 100.0000` | `17.520000` m ⇒ `R = 98.7520` |
| ⇒ **實跑 `ΣRw`（逐宗和）** | **`100.02`** | **`98.75`** |
| **推進量**（理論 vs 實跑） | `24.3300` vs `24.4900`（差 **`+0.16` m**） | `17.5000` vs `17.5200`（差 **`+0.02` m**） |

**左側階段 1 宗數**（`side ∈ ['左側']`·⛔ `side` 之實有值為**中文** `['右側','左側','無']`）：
`k_naive` 趟 **1 宗**／`k*` 趟 **1 宗**；`0m` 該宗 `a=350.76`、`W_near=-0.0`、`W_far=6.46`、`Rw=52.64`；
`3.5m` 該宗 `a=947.97`、`W_near=-0.0`、`W_far=17.52`、`Rw=98.75`。

### 2-3 🔒 兩道自我驗證閘（⛔ 不過即不採·`CLAUDE.md` 探針自我驗證閘條款）

| 閘 | `0m` | `3.5m` |
|---|---|---|
| **①重建閘**：本探針由所記 `widths`／`b` 重建之理論值 vs 引擎自算 `_t_star['ΣRw_L']` | `97.1833` vs `97.1833`（Δ `0.000000`）✅ | `96.3042` vs `96.3042`（Δ `0.000000`）✅ |
| **②分解閘**：起點項−終點項 vs 引擎自報 `Δ` | `2.8167` vs `2.837`（差 `0.0203`）✅ | `2.4478` vs `2.446`（差 `0.0018`）✅ |

⚠️ 閘②之殘差來源已具名：`_re` 為**逐宗 `Rw(%)` 之 2dp 捨入值之和**，
而 telescoping 端點式**未捨入** ⇒ 殘差即該捨入累積，⛔ 非未解釋之量。

---

## §3 乙組：候選歸因之**窮舉**（`U4`）

| # | 候選 | 判別實驗（可證偽） | 判 |
|---|---|---|---|
| ① | 理論側母體**多含**階段 2 宗 | 階段 2 段 telescoping 之端點 `W_final == W_階段1末` ⇒ **本側無階段 2 推進**（`+0.000000` m） | ✅ **不通過** |
| ② | 實跑側母體**多含**非階段 1 之推進 | `_rw_stage1_only` 已排除 `_stage2_ids`；且同上 | ✅ **不通過** |
| ③ | 兩側 `Rw` 累積**起點不同** | 實測 `b_L=0.161877` m（`R=2.8167`）vs `W₀=-0.000000` m（`R=0`）⇒ 起點項 **`+2.8167` pp** | 🔴 **通過** |
| ④ | `k*` 選定後**又被改動** | `_select_pool_slot` 全程呼叫 **1 次**；以其回傳之 `k*` 重建理論值與引擎**逐位相同**（重建閘 Δ=0） | ✅ **不通過** |
| ⑤ | 容差 `0.1` 過嚴 | ⛔ 須先排除 ①〜④；③ 已通過 ⇒ **不成立**（且本批⛔ 不得據此提修法） | ✅ **不通過** |
| ⑥ | 🆕 **本批補列**：理論之 `widths` 取自 `k_naive` 趟、實跑為 `k*` 趟 ⇒ **同一宗之寬度在兩趟不同** | 推進量差 `+0.16` m（`0m`）／`+0.02` m（`3.5m`）⇒ 終點項 `+0.0000`／`+0.3689` pp | 🔴 **通過（`3.5m`）**<br>⚠️ `0m` 差**存在**但貢獻 `0`——兩端 `W≥18` ⇒ `R` **飽和吸收** |

### 3-1 🔒 書面自答：「是否窮盡？有無第三選項使兩邊同時滿足？」

**已窮盡。** 本分解為**恆等式**：

```
Δ = [R(理論起) − R(實跑起)] − [R(理論終) − R(實跑終)]
    且  理論終 = 理論起 + Σw[:k*]  ；  實跑終 = 實跑起 + 推進量
```

⇒ **任何來源都必落入「起點」或「推進量」二者之一**，⛔ 無第三處容身。
二者皆已具名量出，且**合計恰好等於引擎自報之 `Δ`**（差 `0.0203`／`0.0018`）。

### 3-2 ⚠️ 對施工單 §2-2「量級不符」之**具名更正**

施工單警告：「`Δ` 之量級（2.8／2.4）與 `W_0`（0.16）**差一個數量級**，草率連結即為附會」。
🔴 **該理由不成立**：`0.16` 為**公尺**、`2.8` 為**百分點**，二者隔著 `Rw` 累積表；
實測 **`R(0.161877 m) = 2.8167 %`** ⇒ **套上換算後量級完全相符**。
⛔ 若採信該理由而排除 ③，**就會排除掉真正的來源**。
✅ 惟施工單之**要求**（須以構造驗證、⛔ 不得由既有結論推定）**完全正確**，本批照辦
——③ 之成立**不是**由 `W-G.9-2` 之 `W_0` 表推定，而是由**實跑取值 ＋ 兩道自我驗證閘**證成。
⇒ **已入失敗考古節 72。**

### 3-3 🔗 與既有 `GB-47` 之關聯（⛔ 現查·非推定）

`GB-47` 已登記 `_corner_buffer_S` 之回傳純量有**五族土地後果消費者**，其中
**② W 鏈種子**（`_W_prev_left = (_left_buffer_S * _cos_dn)`·`app.py`）與
**④ W₀ 基準**（`_mp_base_W0(corner_pt, _left_buffer_S, …)`）**正是本批分解出的兩個起點**。
⚠️ **本批所測之落差⛔ 不等同於 `GB-47` 之 app-vs-harness 分歧**——
本批兩側**同在 harness 內**（`stepg_pipeline` 之 `_W_prev_left = 0.0` vs `_mp_base_W0` 之 `0.161877`）；
且 `0m` 之 `_left_buffer_S` 縱為 `0`，`_mp_base_W0` 仍得 `0.161877`
（其量的是**街角點相對側街中點**之 `W`，⛔ 非 buffer）。
⇒ 二者屬**同族**（`GB-46`「共用界線須有單一產生者」），⛔ 非同一項。

---

## §4 丙組：`_ctx["0m"]` 之 `KeyError`（`U5`）

**產物**：`verify/out/probe_WG914_ctx_and_flip.log`（AST 構造 ＋ 倉內長跑 log 佐證）。

| 步 | 現查 |
|---|---|
| ① | `stepg_ctx` 初始為 `{}`；其**唯一下標寫入** `stepg_ctx[tag] = {...}` 位於 `try` body 內、**排在 `run_step_g(...)` 之後** ⇒ 兩情境皆 raise ⇒ **該寫入從未執行** ⇒ **恆為空** |
| ② | F.0 段先 `_ctx = {}`，其填充迴圈以 `raise RuntimeError("stepg_ctx[tag] 缺（trunk A 未成功？）")` 中止 ⇒ `_ctx` **停在空 dict**（⛔ 非 `None`——`None` 會是 `TypeError`） |
| ③ | G.1 段讀 `_ctx["0m"]` ⇒ **`KeyError: '0m'`**（`WG914_runall.log` 之 `[G.1] '0m'`） |

⇒ 🔒 **G.1 之 `KeyError` 是三段傳遞之後果，⛔ 非 G.1 自身缺陷**
——與**考古 70**（寫入點排在 `raise` 下游、同一 `try`）**同族**。
⇒ **修好結構閘即自動解除**，⛔ 不需獨立修法。

---

## §5 戊組：三率錨之**翻點定位**（`U6`·⛔ 只登記·不裁）

**母體**：`verify/out/*.log` **236** 檔（已扣除本探針自身輸出）；
其中**全跑 log**（正面判準 ＝ 檔內含收尾行 `W-V run_all:`）**88** 檔，**未入倉 0 檔**。

| 錨 | 全跑 log 判 `PASS` | 判 `FAIL` | **最後一次 `✅ PASS`** | **最早一次完全不出現** |
|---|---|---|---|---|
| `v3 重劃總負擔率錨` | **74** 檔 | **0** 檔 | `f8b42fc`（2026-08-08）`D2_runall.log` | `021bca3`（2026-08-08）`D2b1_runall.log` |
| `v3 率分項錨` | **74** 檔 | **0** 檔 | 同上 | 同上 |
| `v3 率用 DXF 公設實值` | **74** 檔 | **0** 檔 | 同上 | 同上 |

🔴 **三錨在倉內<u>從未出現 `FAIL` 判定</u>** ⇒ 其「翻」⛔ **不是 `PASS→FAIL`，而是 `PASS→不再被評估`**。
⇒ **翻點被夾在 `f8b42fc` 與 `021bca3` 之間**（皆 2026-08-08）。

🔒 **登記為**：解阻後判「**基準過期 vs 算錯**」之**起算點 ＝ `f8b42fc`**；
其後對產生路徑之觸及史即為候選成因清單。⛔ **本批只登記、不裁**（該判定須 KL 裁）。

---

## §6 丁組：`CLAUDE.md` 加註（考古 71 修法 ③ 升格）

🔒 **先現查有無同義條款** ⇒ 有：`探針還原內部幾何時，須以「碼面自身之保證」當自我驗證閘`。
⇒ 依施工單 §2-4 **併入該條款升格**，⛔ **未另立**（免同一述詞兩處定義·`W-G.9-9` 之先例）。
新增者為其**第二觸發點**（`grep -n "升格·第二觸發點" CLAUDE.md` ⇒ 1）：
⛔ 停機上呈之前必先自證「量測器非紅」；併「無從判定」與「判定為偽」**不得共用出艙碼**；
併第 4 款「量測器之母體若含其自身輸出，自檢會自我污染」。

**錨稽核**（`verify/tools/wg99_anchor_audit.py 198 20`·判準＝`VR-001` 之**差量式**）：

```
掃描檔數 991｜引用總數 62｜以 P=198、N=20 判：失準 33 ＝ 活文件 5 ＋ 歷史記錄 28
🔒 判別力自檢：門檻降為 P=1 ⇒ 活文件 6 處 ⇒ 上式非恆為 0
```

🔒 **活文件之 5 處<u>加註前即已失準</u>**（⇒ 本批**新增 0** ⇒ `VR-001` 判準成立）：

| 引用 | 加註前之 `CLAUDE.md` 該行實際內容 | 引用方自述 |
|---|---|---|
| `SKILL.md` → `CLAUDE.md:207`（3 處） | 「任何 `Σ` 之出艙，一律同時給 `ΣX`（帶號）與 `Σ\|X\|`…」 | 該檔**自己已註明**「本則之舉例本身已再度漂移…現查已不成立」 |
| `W-G.9波_新session交接文_WG91-94.md` → `CLAUDE.md:328`（2 處） | 「（實測：八街角中七角所保證之寬度低於法定最小寬…）」 | 該檔**自己已註明**「`CLAUDE.md:328` **已失準**」 |

⚠️ 歷史記錄之 28 處 ⛔ **不追改**。

---

## §7 🛑 上呈 KL（**真域邊界**·施工單 §4 之格式）

> **【現況】**
> 決定「調配池要插在第幾宗之後」的那個計算，和土地**實際配出來**的結果，**起算點不一樣**。
> 選位那一側，是從**街角那個點**開始起算側街負擔（量得 `0.16` 公尺）；
> 實際配地那一側，是從**第一宗的近側地界**開始起算（量得 `0` 公尺）。
> 這 `0.16` 公尺的差，查側街負擔累積表之後，等於 **`2.82` 個百分點**的側街負擔。
> 發生在 **R1 街廓左側**，兩種退縮情形（0 公尺、3.5 公尺）**都一樣**。
>
> **【要改成】**（二擇一·⛔ 本批未替您選）
> 甲、**以街角點為準**：實際配地時，側街負擔也從街角點起算。
> 乙、**以第一宗近側地界為準**：選位計算時，也從第一宗的近側地界起算。
>
> **【對土地的影響】**
> 側街負擔比率**直接進入配地面積公式**，所以兩種改法**都會改變分配面積**：
> 甲案會**直接**改變每一宗的負擔與 G 值；乙案會改變選位分數，**可能改變池槽位置**，
> 進而改變整個街廓的排列。
> 🔴 **受影響之宗數與面積合計，本批⛔ 未估算**——估算須以修正後之起算點**實際重跑引擎**，
> 而本批**不授權動生產碼**（施工單 §1、§3）。**此為下一批之前置**（見 §8）。
>
> **【要你判斷】**（一句是非題）
> **側街負擔之累積，應自「街角點」起算——是或否？**
> （答「是」＝甲案；答「否」＝乙案。）
>
> 標記：**真需 KL 域專業判斷**。

⚠️ **另有一項不需域裁**：候選 ⑥（理論之 `widths` 取自 `k_naive` 趟、實跑為 `k*` 趟）
係**技術面之同源問題**——選槽以 `k_naive` 之寬度為所有 `k` 評分，而落地為 `k*` 之寬度。
⛔ 本批不提修法；列為下一批之前置（§8）。

---

## §8 🔴 下一批（**修結構閘·動生產碼**）之**前置條件清單**（⛔ 只列前置·不設計修法）

| # | 前置 | 為何是前置 |
|---|---|---|
| **1** | 🛑 **§7 之一句是非題已獲 KL 裁定** | 甲／乙兩案**改的是不同側**，⛔ 未裁即無從施工 |
| **2** | **候選 ⑥ 之處置方向已定** | 與 #1 **不可分開改**——二者同時決定終點；只改其一會使 `Δ` 由「已解釋」變回「部分未明」 |
| **3** | **影響面之量化已授權**（受影響宗數／面積／其中角地與抵費地各幾宗） | §7【對土地的影響】欄現為**未估算**；估算本身即需動碼重跑 ⇒ 須明文授權 |
| **4** | **重烤與凍存之時點已協調** | 修好後 `run_step_g` 將**跑得更遠** ⇒ ① 三支端到端複本依**設計**轉紅（⛔ 非 regression）② `W-G.9-11` 之 16 檔基準差異將大量出現 |
| **5** | 🛑 **「基準過期 vs 算錯」之判定已獲 KL 裁**（⛔ CC 不自行判定是否重烤） | 本批已備妥其**起算點**（§5：`f8b42fc`）與**候選成因清單之取得方式** |
| **6** | **`_ctx["0m"]` ⛔ 不需獨立修法之認定已被接受** | §4 已證其為三段傳遞之後果 ⇒ 修好結構閘即自動解除；若另行修補即為**重複守備** |
| **7** | **本批所立之閘（無）與既有閘之關係已確認** | 本批**零新閘**；修碼後 `理論＝實跑` 閘本身即為驗收器 ⇒ ⛔ 不得為求綠而放寬其容差 |

---

## §9 出艙之數（⛔ 指令與輸出並列·**皆本報告落筆當次重跑**）

```
$ python verify/probes/probe_WG914_gate_decomp.py     ⇒ rc=0｜完成分解之情境 ＝ ['0m','3.5m']
$ python verify/probes/probe_WG914_ctx_and_flip.py    ⇒ rc=0｜U5 ✅ 已由構造定位
$ diff <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG913_runall.log) \
       <(grep -E "✅ PASS|🔴 FAIL" verify/out/WG914_runall.log)      ⇒ rc=0（零差）
$ diff <(grep "末端夾具" …WG913…) <(grep "末端夾具" …WG914…)          ⇒ rc=0（零差）
$ grep -c '✅ PASS' verify/out/WG914_runall.log ⇒ 12
$ grep -c '🔴 FAIL' verify/out/WG914_runall.log ⇒ 22
$ grep -c '對帳 PASS' verify/out/WG914_runall.log ⇒ 1

$ git diff --numstat 6f066a7 -- app.py verify/stepg_pipeline.py verify/run_verification.py \
      verify/wv_reconcile.py verify/baselines/ verify/fixture_g3_static_guards.py \
      verify/fixture_e2e_termination.py verify/fixture_wf_ns_wiring.py \
      verify/fixture_midlayer_6items.py verify/wg_g1_smoke.py verify/wg_g2_smoke.py \
      verify/wg_g3.py verify/run_all.py verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt
  ⇒ （空輸出）
🔒 判別力自檢：同式對 `CLAUDE.md`／`SKILL.md` ⇒ `20 0`／`61 0`（非空 ⇒ 上式非恆空）
```

### 9-1 判別力自檢（⛔ 逐項）

| 檢查 | 自檢 | 結果 |
|---|---|---|
| `U7` 零差 | 把 `WG914` 首行 `🔴 FAIL` 改為 `✅ PASS` 再 `diff` | `rc=1` 且逐行指出 ⇒ **具鑑別力** |
| 甲組量測器 | 包裝前後 `R(12.34)` 逐位相同；`R(18.0)==100.0`（已知為真）；`R(0.0)!=100`（已知為偽） | 三者皆 ✅ |
| 丙組量測器 | `stepg_ctx` 寫入 `2` 處（已知存在·須 >0）／`ZZZ_不存在之變數` `0` 處（已知不存在·須 ==0） | ✅ |
| 戊組量測器 | `✅ PASS` 命中 `96` 檔（已知為真）／〈哨兵·執行期組出〉命中 `0` 檔（已知為偽） | ✅ |
| 錨稽核 | 門檻降為 `P=1` ⇒ 活文件 `6` 處 | ⇒ 非恆 0 |

### 9-2 字樣錨之唯一性自檢（倉規 A）

```
$ grep -rl "【W-G.9-14 全單報告】" . --exclude-dir=.git --exclude-dir=__pycache__ | wc -l ⇒ 1
$ grep -rl "【W-G.9-14 前置登記】" . --exclude-dir=.git --exclude-dir=__pycache__ | wc -l ⇒ 2
🔒 判別力自檢（必然命中）：`全單報告` ⇒ 3 檔 ⇒ 上式**非恆 1**
🔒 判別力自檢（必不命中）：以一個**執行期組出**之哨兵字樣重算 ⇒ 0 檔 ⇒ 上式**非恆 ≥1**
```

🔴 **就地更正（⛔ 本節之首版兩處數字寫錯·原因逐字記下）**：
首版寫「`前置登記` ⇒ **1**」與「哨兵 ⇒ **0**」，**皆錯**（實為 **2** 與 **2**）。
**成因 ＝ 本文件把自己的 `grep` 指令<u>逐字</u>寫了進去** ⇒ **被搜尋的字樣自己進入了母體**：
① `【W-G.9-14 前置登記】` 因本節之指令行而多命中本檔 ⇒ `2`；
② 哨兵字樣同理，且 `W-G.9-13` 報告亦寫過同一個哨兵 ⇒ `2`。
⇒ 🔒 **哨兵一律於執行期組出、⛔ 不使其字面落入任何檔**
（此即本批寫入 `CLAUDE.md` 自我驗證閘條款**第 4 款**者——**我在同一批內又踩了一次**）。

⚠️ 上列之「1」「2」係本報告寫出**當時**之值；此後每被引用即 `+1`，⛔ 但**定位不會失效**
——**定義**該錨者始終只有各該檔一處。⇒ **計數會長大、定位不會失效。**

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告**未以「正典」二字作為任何斷言之依據**；凡涉權威層級處已就地標明
（`docs/rulings/` ＝ 權威序**第 1 級**；`docs/驗證裁定登記表.md` **不在**權威序四級之內）。

---

## §10 🔴 **本批未做之事**（⛔ 逐項）

1. ⛔ **未修任何碼**——`app.py`／`verify/stepg_pipeline.py`（含 `_IFACE_TOL`、兩處 `raise`、
   `_rw_stage1_only`、`_select_pool_slot`）／`run_verification.py`／`wv_reconcile.py`／
   `baselines/`／各夾具／三支複本 **皆零觸**。
2. ⛔ **未提交任何修法之實作**——縱使 §7 之歸因已明。**⛔ 看見了也不修**（施工單 §8-1）。
3. ⛔ **未修碼註**——§1 已指出其 `0m` 數字過期，⛔ 但 §3 不動 `stepg_pipeline.py`。
4. ⛔ **未動任何閘或容差**、⛔ 未新增任何閘（本批**零新閘**）。
5. ⛔ **未修任何既有 FAIL**、⛔ 未解除明細截斷。
6. ⛔ **未重烤任何基準**、⛔ 未重凍任何凍存物、⛔ 未改期望 FAIL 名單。
7. ⛔ **未估算 §7 之受影響宗數與面積**（須動碼重跑·列為下一批前置 #3）。
8. ⛔ **未裁**「基準過期 vs 算錯」（只登記起算點）。
9. ⛔ 未把任何裁定寫入 `docs/rulings/`；⛔ 未追改歷史記錄之舊錨與舊雜湊；**文件刪除欄 ＝ 0**。

---

## §11 commit 鏈（`git merge-base --is-ancestor` 逐對證時序）

- **A** `f02bb47` — 事前登記（⛔ 零碼、零量測）
- **B** `838836c` — 偵察探針 ＋ 逐項分解 ＋ `_ctx` 定位 ＋ 翻點 ＋ 長跑 log（⛔ 零文件結論）
- **C** — 倉規加註 ＋ 本報告 ＋ `GB` 進度列 ＋ 失敗考古節 **72**
