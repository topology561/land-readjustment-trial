# `W-G.9-268′` 補令十：序 `3` 併線 ＋ 序 `4` 六號之鑄 —— ✅ **辦畢**

> 🔒 **序 `3` ＝ `c2` 已併線**（主線快轉至補令十 `§六` 所授權之**確切物件**）。
> 🔒 **序 `4` ＝ 六號已鑄 ＋ `GB-158` 波及面掃描 ＋ `CLAUDE.md` 末端追加**（零生產碼·逕推）。
> 🛑 **序 `5`（`c3` → `c4`）未辦**——其動生產碼，須側分支 ＋ 發單側復驗 ＋ **呈 KL 放行**。

---

## §零　三軸之總宣告（`VR-091`）

> `(1)` **來源 `commit`**：主線期初 ＝ `683b7fa49e4e20f53593c363b7d7050c91218d20`；
> **併線後 ＝ `95a530c187ca8f6b15581a1250e2a72dcfe7eb6d`**；本節各量之態逐處具名。
> `(2)` **母體**：逐節具名。　`(3)` **粒度框**：逐節具名（檔框／列框／bytes）。

🔒 **工項零**：補令十原封入倉（`1c3c4fa`）——`113` 列／**`8608`** B／`CR 0`；
來源與落檔 `sha256` 皆 `ac51a89f0a3ea5d9a4f061d2b90d07a5bc92d99089ab4efc3031c00997a2b3eb`／`cmp rc = 0`。

---

## §一　序 `3`：快轉併線（**已辦**）

**推進前之實查**（⛔ 採信本地 ref）

| 項 | 實查 | 須 | 判 |
|---|---|---|---|
| 主線 | `683b7fa49e…` | 同（原地不動） | ✅ |
| 側支 | `95a530c187…` | 同 | ✅ |
| 快轉 | 主線**為**側支之祖先 | 是（⛔ 需 rebase） | ✅ |

**推進**：`683b7fa..95a530c` → `refs/heads/wip/s1-endpart`。

**併線後之機驗**

| 項 | 實測 |
|---|---|
| 主線 | **`95a530c187ca8f6b15581a1250e2a72dcfe7eb6d`** |
| `c2`（`c1376af`）是否已為主線祖先 | ✅ **是** ⇒ 併線成立 |
| `app.py:9269` 逐字 | `if _os_a.environ.get('WG9268P_ANCHOR_GEOM', '1') != '1':` ⇒ **預設 `on` 已落地** |
| **`app.py` blob `sha256` 前 `16`** | 主線 ＝ `9873b168278fbc08`／`c1376af` ＝ **同** ⇒ **逐位相同** |

🔒 ⇒ **落地者正是 KL `2026-09-10` 所核准、發單側於序 `2` 逐位元組復驗通過之內容**。
🔒 **⛔ 須 KL 再放行**（補令十 `§六`）：序 `1′`／工項零皆零生產碼，生產碼逐位未變。

---

## §二　序 `4`：六號之鑄

### 二-1　取號現查（倉內既有器 `wg9268_gate5_tally.py`·**⛔ 自擬框**·坑 `6`）

**態 ＝ `blob@1c3c4fa`**（鑄前）／母體 ＝ 各該**單檔**／**列框**。

| 簿 | 期初 相異／MAX／缺號集 | 次號 | 與補令十 `§七` 之授權 |
|---|---|---|---|
| 自誤 | `335`／**`342`**／`[106]` | **`343`** | ✅ 相符 |
| `GB` | `155`／**`157`**／`[12, 87]` | **`158`** | ✅ 相符 |

### 二-2　意指占用查（補令九 `§一-2`：**二形並取** ＋ 對照組**逐形**出艙）

**器** ＝ `verify/probes/probe_WG9268p_occ6.py`（新建）／**落檔** ＝ `verify/out/WG9268pR_occ6.log`。
**三軸**：`blob@1c3c4fa`／**全倉追蹤檔 `1839` 檔**（讀不到 `22` ＝ 二進位）／檔框 ＋ 列框（錨定框·三形分列）。

| 受詢 | B 形 檔／列 | C 形 檔／列 | 鬆框 檔／列 |
|---|---|---|---|
| `自誤 343` | `0`／`0` | `5`／`10` | `5`／`10` |
| `自誤 344` | `0`／`0` | `2`／`4` | `2`／`5` |
| `自誤 345` | `0`／`0` | `1`／`4` | `1`／`4` |
| `GB-158` | `0`／`0` | `5`／`11` | `5`／`11` |
| `GB-159` | `0`／`0` | `5`／`11` | `5`／`11` |
| `GB-160` | `0`／`0` | `3`／`6` | `3`／`6` |

🛑 **逐筆歸類（`裁 H`：命中係<u>待歸類之材料</u>·⛔ 判準）**：六號之 C 形命中**全數**落於
`docs/orders/W-G.9-268p_補令八｜九｜十` 與 `docs/reports/W-G.9-268pR_補令八｜九` ——
即**本單自身之補令與報告**（授權宣告、續辦表、`⛔ 鑄` 之聲明）
⇒ **`自解款` 白名單類 `1`**（占用者全為本單自身之產物）⇒ **未占用·可鑄**。

**對照組（逐形出艙）**

| 組 | B 形 | C 形 | 判 |
|---|---|---|---|
| 甲 `自誤 341` | `1` | `14` | ✅ `≥1` |
| 甲 `自誤 340` | **`0`** | `16` | ✅ `≥1`（**惟 B 形為 `0`**）|
| 甲 `GB-157` | **`0`** | `21` | ✅（同上）|
| 甲 `GB-156` | **`0`** | `20` | ✅（同上）|
| 乙（人造·⛔ 入倉·代稱）| `0` | `0` | ✅ `=0` |

🔴 **⇒ `GB-158` 之受詞於本次取號中<u>當場復現</u>**：四個已知占用之對照組中 **`3` 個於 B 形得 `0`**
⇒ **B 形⛔ 單獨採信**；本次之「未占用」由 **C 形**承擔。

### 二-3　六號之受詞（逐字轉錄·⛔ 改寫）

| 號 | 受詞（節略） | 逐字之出處 | 落點 |
|---|---|---|---|
| `自誤 343` | 以列印樣本之極值充作全母體之值域 | 補令九 `§一-3` | 自誤簿 |
| `自誤 344` | 沿用他變更之判別力校準組，只重量基數而未驗敏感度 | 補令九 `§五` | 自誤簿 |
| `自誤 345` | 立人造對照時未驗其落於器之可測域、亦未驗其能驅動受量欄位（**二肢**）| 補令十 `§三-1` | 自誤簿 |
| `GB-158` | `常規四（七）補款二` B 形為量測器紅 | 補令九 `§一-4` | `GB` 簿 |
| `GB-159` | 中間態配地表⛔ 對外（俟 `W-G.9-269` 解除）| 補令八 `§三` | `GB` 簿 |
| `GB-160` | `V-1′` 之器於某類側無從建框（閘 `γ` 不過）| 補令九 `§四` | `GB` 簿 |

🔴 **`自誤 345` `(ii)` 肢已逐字併記為 `自誤 338` 之<u>二犯</u>**（補令十 `§七` 明令·⛔ 以新號掩之）。
🔴 **`GB-159` 之生效時態已載明 ＝ 本次併線之 `commit` `95a530c187ca8f6b15581a1250e2a72dcfe7eb6d`**（補令十 `§六` 明令）。

### 二-4　鑄號收工閘（`常規四（九）二`·**⛔ 入倉前先過**）

框**逐字自倉內器 `wg9268_gate5_tally.py` 取出並 `exec`**（⛔ 自擬）：
`F_ZIWU_DEF`／`F_ZIWU_RNG`／`F_GB_TBL`／`F_GB_A`／`F_GB_B`。

| 簿 | 相異（前→後） | MAX（前→後） | 缺號集（前→後） | ① 形之自證 | ② MAX 之推進 | ②′ ⛔ 在缺號集 | 判別力 |
|---|---|---|---|---|---|---|---|
| 自誤 | `335 → 338` | **`342 → 345`** | `[106] → [106]` | ✅ | ✅ | ✅ | ✅ |
| `GB` | `155 → 158` | **`157 → 160`** | `[12, 87] → [12, 87]` | ✅ | ✅ | ✅ | ✅ |

🔒 **二簿之缺號集皆⛔ 變動**；判別力（一必不存在之號須未命中）二簿皆 ✅ ⇒ **本檢非恆綠**。

---

## §三　`GB-158` 波及面掃描（補令十 `§八`·**逐處具名·含命中 `0`**）

**器** ＝ `verify/probes/probe_WG9268p_gb158_sweep.py`（新建）／**落檔** ＝
`verify/out/WG9268pR_gb158_sweep.log`（**`96170`** B·**逐列具名 `檔:列` ＋ 該列逐字**）。
**三軸**：`blob@1c3c4fa`／全倉追蹤 `.md` **`469`** 檔（讀不到 `0`）／檔框 ＋ 列框。
**框為正面列舉**（⛔ 全倉排除式·`CLAUDE.md` 明令）。

| 字樣 | 檔 | 列 |
|---|---|---|
| `意指占用` | **`61`** | **`150`** |
| `二形並取` | **`44`** | **`92`** |
| `常規四（七）補款二` | **`15`** | **`19`** |
| `補款二` | **`83`** | **`164`** |

🛑 **四個字樣<u>皆⛔ 命中 `0`</u>** ——「含命中 `0`」之款於本次掃描**無受詞**，**此即其具名**（⛔ 靜默）。
🔑 **對照組**：`常規四` ＝ **`460`** 列（須 `> 0`）✅／一必不存在之字樣（**執行期組出**·⛔ 入倉·`GB-147`）＝ **`0`** 列 ✅
⇒ **掃描器非紅**。
🔒 **凡該落檔所列之既往鑄號查，一律須以二形重查**；**⛔ 追改任何既往之取號結論**（`常規四（九）五`）。

---

## §四　`CLAUDE.md` 末端追加（`常規四（七）補款二` 之更正）

🔒 **⛔ 追改該補款原文一字**（補令九 `§一-4` 明令「更正以末端追加承載」）——其三款字面**一字不動**。
🔒 **落點** ＝ `CLAUDE.md` **檔末**；`bytes` `232841 → 236365`（增 **`3524`**）／列 `2893 → 2944`／`CR 0`。
🔒 **受詞以<u>字樣錨</u>指之**（`行號衛生` 第 `4` 款·⛔ 行號），並附**唯一性之判別力自檢**
（該錨鑄前命中 `1`；寬字樣 `補款二` ＝ `2` ⇒ 該檢⛔ 恆為 `1`；人造對照 ＝ `0` ⇒ 器能歸零）
＋ **自指增長之具名**（`GB-132` 族）。

**其施行（節略·逐字見該節）**：意指占用之搜尋一律 **B 形 ⋀ C 形並取**、**對照組於各形之命中同格出艙**；
**任一形使對照組全 `0` ⇒ 該形⛔ 單獨採信**；鬆框**僅作漏框偵察**、⛔ 為判準。
🔒 **⛔ 改** `常規四（八）`／`補款一`／`W-G.9-192-PRE-b 🔧 一` **任一字**。

---

## §五　append-only 之機驗

| 檔 | `bytes`（前→後） | 嚴格前綴 | `deletions` | `CR` |
|---|---|---|---|---|
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `687571 → 694207` | **`True`** | `0` | `0` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `750897 → 758971` | **`True`** | `0` | `0` |
| `CLAUDE.md` | `232841 → 236365` | **`True`** | `0` | `0` |

🔒 **`verify/tools/wg942_append_audit.py`**（`嚴格前綴 ＝ True`·`PYTHONIOENCODING=utf-8`）：
**受檢 `77` 筆／不適用 `0`／合計 `77`（＝ `TARGETS` `77`）⇒ 結論「✅ 全部成立」**
——**機械證明既有檔未被改寫**；其**判別力自檢（行中插入）＝ `False`（須 `False`）**逐筆出艙。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| 序 `3`（快轉） | `683b7fa..95a530c` | `app.py`（命中 `1` 行）| 🔴 **生產碼**——惟其係**KL 已放行之 `c2`**，經序 `2` 逐位元組復驗、補令十 `§六` 授權快轉 |
| `1c3c4fa`（工項零） | `95a530c..1c3c4fa` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 逕推 |
| 本批（序 `4`） | `1c3c4fa..HEAD` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 逕推 |

🔒 **序 `4` ⛔ 動 `app.py` 一字**——`git diff --numstat 95a530c..HEAD -- app.py` **空輸出**。

---

## §七　續辦之界

🛑 **本批⛔ 辦者**：⛔ 辦 `c3`／`c4`（其動生產碼·須側支 ＋ 復驗 ＋ **呈 KL 放行**）、
⛔ 鑄授權六號以外之任何號、⛔ 追改任何既有正典文字、⛔ 追改任何既往之取號結論。

🔒 **餘項（補令十 `§八`）**
> 序 `5`：**`c3`**（`contract` 段·移除旗標 ＋ 更新受該錨之註解，**其受詞含 `app.py:9248` 之過期 docstring**——
> 該處逐字仍載「預設 `off`」而碼面自 `c2` 起為 `on`）→ **`c4`**（差異表 ＋ 執行報告 ＋ `GB-67` 末端追加）。
> **各為獨立側支·逐 `commit` 呈 KL。**
🔒 **`W-G.9-269`（病一）之前置**：`A` 母體（`K-9-9 二` 不交叉之違反）係於**原錨態**下量得，
錨更正後其違反宗集**可能改變** ⇒ **⛔ 沿用 `2` 宗／`4.7950 ㎡`，須重量**；於 `c4` 併線後另單。
