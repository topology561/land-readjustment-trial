# W-G.9-34：W-G.9 收束——錯誤更正入倉 ＋ 深度量法修正掛號

**基座**：`wg933-anchor-fix` @ `b093630`（開場時 ＝ `origin/wip/s1-endpart`）
**批號現查**：`W-G.9-34` ⇒ 倉內檔名 **0**·全倉引用 **0** ⇒ 未占用
（判別力自檢：`W-G.9-33` ⇒ 檔名 **1**·引用 **4**）
**性質**：⛔ 零生產碼 · ⛔ 零 baseline 更新（`VR-012`）· ⛔ 未動 `app.py`／`verify/` 下任何 `.py`

⚠️ **行號綁 commit**：本報告所載之一切生產碼行號皆成立於 **`b093630`**
（本批未動 `app.py`／`verify/`，故亦成立於本批之 commit）。

---

## §0 開場（原樣貼）

```
$ git rev-parse --short HEAD && git log -1 --format="%h %s"
b093630
b093630 W-G.9-33：W-G.9-32 之交接查核與錨更正 ⇒ ✅ 三項結論復現成立·🛑 §5⑤ 未辦·🔴 兩處錨更正（⛔ 零生產碼）

$ git rev-parse --short origin/wip/s1-endpart
b093630

$ git rev-parse --abbrev-ref HEAD
wg933-anchor-fix
```

**🔴 自證分支**：`wg933-anchor-fix`；`origin/wip/s1-endpart` 為其祖先（含同值）⇒ **係工作分支之衍生**；
**⛔ 非 `main`**（本地 `main` ＝ `6cb77b7`）。

### 🩸 對前批表述之照實更正（施工單 §0 明令）

`W-G.9-33` 開場所在之 worktree（`handoff-verification-gb47-3ca5c2`）HEAD ＝ `6cb77b7`，
而本地 **`main` ＝ `6cb77b7`（同值）** ⇒ 該處**開在 `main` 的 tip 上**（分支名為
`claude/object-correction-investigation-8b210d`、與 `main` 指向同一 commit）。
⇒ 前批表述之「基座較舊 367 個 commit」**⛔ 不準確**，照實應為**開在 `main` 上**。

### 🩸 量測框之照實更正（施工單 §0 🔒 明令）

`W-G.9-33` 出艙之「原本那列 **1284 個字**，加註後 **1509 個字**」——該二數係 bash `${#var}`
之**位元組**數，而出艙時以「**字**」統稱、⛔ 未聲明量測框。
**現查**：該列以 python `len()` 量得 **799 字元**。
⇒ 正確表述應為「**1284 → 1509 位元組**（＝ 799 字元）」。**結論（純插入·原文一字未刪）不受影響。**

---

## §一 丙組現查（C-1〜C-4）

### C-1 「截角前」之現況射程

**逐檔正面列舉**（⛔ 未用「全倉 grep 再排除」）：

| 檔 | `截角前` | 判別力對照 `截角` | 該格是否可採 |
|---|---:|---:|---|
| `app.py` | **12** | **169** | ✅ 可採 |
| `verify/stepg_pipeline.py` | 0 | **0** | ⚠️ **作廢**（`S-2`） |
| `verify/run_verification.py` | 0 | **11** | ✅ 可採（對照非 0 ⇒ 證該式非恆 0） |

🩸 **`S-2` 觸發 1 格·已處置**：`stepg_pipeline.py` 之對照組回 **0** ⇒ 該格量測**作廢**、
具名為無鑑別力（該檔根本不提「截角」⇒ 其 0 屬必然、⛔ 非資訊）。

**(a) 宗地層寬度量測函式所收之宗地座標** ⇒ 🔒 **出艙碼 ＝ `截角後`**
- 入口：`app.py:7012` `_w = parcel_min_width_n14(row.get('cut_coords') or [], …)`
- 逐字錨：`app.py:6968`「街角宗之 `cut_coords` 確為**截角後**幾何（實測：R3 `628-45(2)`
  最近頂點距 FRONT **0.7655m**、R4 `628-1(1)` 反向 **−1.4826e-2m**）」
- 另一入口 `_mark_zaling`（`app.py:19579-19582`）之寬度 ＝ `S · cos` **由 `S` 導出**、
  ⛔ **根本不收宗地座標** ⇒ 該入口對本問**不適用**（非「截角前」亦非「截角後」）。

**(b) 倉內是否已有「現行碼尚未實作實量」之逐字** ⇒ ✅ **有**
- `app.py:6925`「⚠️ 現行碼**尚未實作實量**（實作屬 **K-6-A2**）⇒ **上段描述之現況為過渡態**。」
- `app.py:2005`「（**截角前**幾何），門檻＝退縮寬＋畸零地最小寬。現行碼**尚未實作實量**，」

### C-2 宗地層深度之現況量測軸 ⇒ 🔒 **正街（前緣線）法向**

- `app.py:6447`「**新式**：帶底 ＝ 通過**後緣連續段之最淺端點**、**平行前緣線**之直線」
- `app.py:6448`「⇒ `t_hi = min(後緣角點之 t)`——**直接讀環上該頂點之 `t` 分量**。」
- `app.py:6566`「🔴 ⛔ **禁乘、禁除任何投影係數**」
- 法規原文之轉錄見 `app.py:6443`（「臨接之道路境界線至該基地**後側境界線**垂直距離之最小值」）

⇒ 與 `K-9-6-h ①` 之「沿垂直於正街道路境界線之方向量」**軸別一致**；
⛔ 惟①之**垂線容納條件**（整條落在宗內、交後側**實體**線段）**未實作**——
現行僅取後緣角點之 `t` 最小值，⛔ 不檢查垂線是否落在宗內。

### C-3 「臨後側境界線」之現行閘 ⇒ 🔒 **出艙碼 ＝ `有無交點`**

`app.py:6257` 表列逐字：

| 情形 | 判準 | 意義 |
|---|---|---|
| **未配到** | `not touch`（`gap > _EPS_TOUCH_FRONT`） | 宗地後緣**構不到**屁股線 |
| **跨過** | `crosses` | 宗地**穿過**屁股線 |

⇒ ⛔ **不含**`K-9-6-h ②` 之「兩點所夾線段範圍內至少一條合格垂線」之容納條件。

### C-4 以 B-0 座標檔自行重建（⛔ 未採信施工單任何數字）

**(a) 單位方向向量與分組**（號已正規化·六位小數）：

| 群 | 單位向量 | 段數 | 語意 |
|---|---|---:|---|
| 正街 | `(0.000000, 1.000000)` | 5 | `x = 183.089` |
| 最小深度垂線 | `(1.000000, −0.000000)` | 2 | `y = 72.695` |
| 後側境界線 | `≈(0.206, 0.979)` | 3 | 同一直線之三段 |
| SIDELINE／第 1 宗遠側界 | `≈(0.986, 0.167)` | 3 | 二條互相平行之線 |
| ALLOCLINE（虛·實） | `≈(0.871, 0.492)` | 3 | 二條互相平行之線 |
| 截角斜邊 | `(0.577334, 0.816508)` | 1 | `10 → 9` |
| 街廓端邊 | `(0.999979, 0.006410)` | 1 | `12 → 11` |

**(b) 內積** —— 🔴 **施工單措辭與 `Q4` 互斥，具名如下**：

| 式 | 量值 |
|---|---:|
| `dot(u_正街方向, u_垂線)` | **`0`** |
| `dot(n_正街法向, u_垂線)` | **`−1`** |
| 夾角 | **`90.000000000000°`** |

C-4(b) 之措辭為「正街**法向**與該垂線之內積」，其值恆為 `±1`（法向 ∥ 垂線），**結構上不可能為 0**；
而 `Q4` 預期「＝ 0」。得 0 者為「正街**方向**·垂線」。
🔒 **`S-6` ⛔ 未觸發**：其受詞係「**該圖之垂直性不成立**」，而垂直性**成立**（90.000000000000°）
⇒ 停機條件之實質未達，⛔ 不因措辭誤而停機。**此為施工單自身之措辭錯誤**（同族：考古 79
「判準把對照組之預期碼訂在該工具結構上不可達的地方」）。

**(c) 第 2 宗之投影區間與重疊**（沿正街方向·近側界 ＝ 綠線 `Ps-B1`、遠側界 ＝ 過 `B2` 之實線 ALLOCLINE）：

| 段 | 區間 | 長 |
|---|---|---:|
| 臨正街段 `[Ps .. ALLOC實∩正街]` | `[72.662000, 129.642000]` | `56.980000` |
| 臨後側段 `[B1 .. B2]` | `[54.913000, 72.695000]` | `17.782000` |
| **重疊** | `[72.662000, 72.695000]` | **`0.033000`** |

⇒ **`Q5` 成立**（`> 0` 且 `< 0.1`）。該值恰 ＝ (d2) 之 Δy ⇒ **係圖面不精確之顯現**，
理想圖上二段應恰首尾相接（重疊 `0`）。

**(d) 圖面不精確之逐筆實測**：

| # | 受詞 | 應然 | 實然 | 差 |
|---|---|---|---|---:|
| d1 | `Pw`（`id=3`） | 落在正街 `x = 183.089` | `x = 183.044` | **0.045000** |
| d2 | `Ps`(`id=7`) 與 `Pw`(`id=3`) 之距 | — | — | **0.055803** |
| d3 | 後側境界線三段之共線性 | 偏移差 `0` | `−65.497580`／`−65.495748`／`−65.495667` | **0.001913** |
| d4 | SIDELINE 二段之共線性 | 偏移差 `0` | `13.014358`／`13.009095` | **0.005262** |
| d5 | ALLOCLINE（虛）二段之共線性 | 偏移差 `0` | `9.230413`／`9.228500` | **0.001912** |

⇒ **最大不精確 ＝ `0.055803` > `1e-3`** ⇒ **B-4 之射程限制成立**。

---

## §二 `Q1`〜`Q5` 逐項判

| # | 預測 | 判 | 依據 |
|---|---|---|---|
| **Q1** | `near_dir` 交遞點於**兩路徑皆存在** | ✅ **成立** | `app.py` 24 命中（初始化 `:19691-19692`／傳入 `:19731`・`:19813`・`:19828`／交遞 `:19736`・`:19840`）；`verify/stepg_pipeline.py` 9 命中（`:626-627`／`:662`・`:736`・`:750`／`:670`・`:761`） |
| **Q2** | C-1(a) ＝ `截角後` | ✅ **成立** | `app.py:6968` 逐字 ＋ 入口 `:7012` |
| **Q3** | C-3 ＝ `有無交點` | ✅ **成立** | `app.py:6257` 表列 |
| **Q4** | C-4(b) 內積 ＝ 0 | ⚠️ **措辭互斥·垂直性成立** | `dot(u_F,u_P)=0`／`dot(n_F,u_P)=−1`／夾角 `90.000000000000°` |
| **Q5** | C-4(c) 重疊 `>0` 且 `<0.1` | ✅ **成立** | **`0.033000`** |

⇒ `Q1` 成立 ⇒ **A-1 之撤銷理由與現查實得相符**（`S-3` **⛔ 未觸發**）。

---

## §三 甲組辦理結果

### A-1 撤銷 `GB-64`（近側界方向無表達力）

**辦法**：就地加註（⛔ 原文一字不刪），「🛑 需 KL」欄**追加**改記「否——已撤銷」。
**撤銷理由**（⛔ 照抄施工單）：該則係對 `_block_strip` docstring 之誤讀；原句
「近側方向仍只由既有 `allocation_dir` 決定 ——⛔ 未新增近側參數」之意為
「既有槽已足、不必另加參數」，⛔ 非「不可設定近側方向」。**現況近側界方向不但可設，且已在用。**
**犯者：claude.ai**（⛔ 非 CC）。
**CC 現查佐證**：見 `Q1`；宿主 ＝ `solve_G_binary`（`app.py:9637` 簽章）、
字樣錨 `:9649`「D-2b-23【甲】`near_dir`：一個界面只有一條線（`K-9-5-4 ②` 之另一半）」、
`:9662`「`near_dir` **只改①之近側邊**」；落地 commit **`79bf2d7`**（2026-08-09）。

### A-2 三處實作狀態註記

| # | 落點 | 過期之陳述 | 現查實得 |
|---|---|---|---|
| ① | `docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-51` | 「第 2 宗之**近側**界仍 ∥ALLOC」 | **已實作**（`near_dir`·`79bf2d7`） |
| ② | 同上 | 「`n_hat_far` 之**生產端傳參呼叫 ＝ 0**」 | **＝ 1**（字樣錨 `allocation_dir=_near_ad, n_hat_far=_far_nhat`） |
| ③ | `docs/rulings/K-6_…md`（字樣錨 `② 之「街角第 1、2 宗之實配境界線」尚未實作`） | 「尚未實作」＋「見登記表 `GB-44`」 | 已實作；且 `GB-44` **早已自標關閉**（其欄逐字「✅ **已關閉（D-2b-1·2026-08-08）**」） |

⇒ ①② 於 `GB-51` 列末就地加註；③ 於 `K-6` 該段後就地加註。**皆⛔ 原文一字不刪。**
⚠️ ③ 之加註**明載⛔ 不主張 `D-②` 已全部完成**——僅更正「尚未實作」四字之現時真偽。

### A-3 破量成因之受詞更正

**已記之事實**：舊歸因「成因 ＝ 同一界面雙定義」**撤回**；⛔ **數值不撤回**
（`W-G.9-29` 之 `J1` 殘差 `0.0000` 仍成立）；**新受詞**（登記為待查·⛔ 本批不查）＝
**不相鄰之宗之間為何會重疊** ⇒ 立 **`GB-67`**。

🩸 **⛔ 施工單所載之撤回理由未照抄·具名如下**：單稱「相鄰之宗0×宗1 本就不應有楔形——
**實測亦無**」。**現查與之不符**：

| 街廓 | 宗0×宗1（甲） | 宗0×宗1（乙） |
|---|---:|---:|
| R2 | **4.9205** | **4.9480** |
| R5 | **1.6145** | **1.6796** |

（【倉】`verify/out/probe_D2b17_dual.log`）⇒ **非零**。
且該 log 之入倉 commit `dca0a85`（2026-08-09 **13:14**）**早於** `near_dir` 落地
`79bf2d7`（同日 **18:37**）⇒ 為**落地前舊態**；落地**後未重跑入倉**
（`git log 79bf2d7..HEAD -- verify/out/probe_D2b17_dual.log` ⇒ **空**）。
⇒ 依 §0 🔒「凡出艙一個現查值，該現查須實際執行過；若沒跑，就寫**未查**」，
「實測亦無」記為 **未查**、⛔ 未出艙。
🔒 **撤回本身仍成立**，惟其**理由已改為**：該歸因所據之 log 為 `near_dir` 落地前之舊態、
落地後未重測 ⇒ **事實基礎已失效**。

### A-4 claude.ai 側自誤入倉

- 於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **追加二則**（⛔ 不刪既有文字）：
  **自誤 7**（交付法上不可能之取捨·發現者 **KL**·正典逐字反證已現查坐實於 `K-6` 之
  `K-9 §一-1`：`宗地寬度 ≥ 畸零地最小寬` **且** `G ≥ 街廓最小建築面積`）；
  **自誤 8**（以射程小於命題之工具作否定性結構宣稱·發現者 **claude.ai 自查**）。
- 失敗考古 **節 83** 入倉（節號現查：最大為 82 ⇒ 83 未占用）：
  「一次無瑕的徹底列舉，證出了一個假的『做不到』——因為母體選錯了，而復現只會把同一個母體再走一遍」。

---

## §四 乙組辦理結果

### B-0 座標檔入倉

⚠️ **檔名與施工單不符·已具名**：單載半形 `最小深度示意圖_座標.*`，實體為**全形底線 `＿`**
（U+FF3F·UTF-8 `EF BC BF`）⇒ 依「⛔ 不採信本單」以**實體檔名為準**、⛔ 未改名。
**S-4 ⛔ 未觸發**（二檔存在）。

- **置入路徑**：`docs/specs/figures/`（現查倉內既有圖資慣例目錄——該目錄已有 6 個 `.jpg`；
  ⛔ 未新造目錄）。
- **`.gitattributes`**：依既有 `*.dxf -text`／`*.dwg -text`（案由 `GB-17`）之**同型**條款，
  新增 `*.cnt -text`／`*.lin -text`，並載明同型案由與「選 `-text` 而非 `binary`」之沿用理由。
- **PROVENANCE**：`docs/specs/figures/PROVENANCE_最小深度示意圖.md`
  （來源 ＝ **KL**／送達日 ＝ **2026-08-15**／送達途徑 ＝ **對話側**）。

**構造證**（量測框 ＝ **byte**）：

| 檔 | 來源 | working tree | **blob** | 判 |
|---|---:|---:|---:|---|
| `.cnt` | 703 | 703 | **703** | ✅ 三者相同 |
| `.lin` | 378 | 378 | **378** | ✅ 三者相同 |

🔒 **判別力自檢**（⛔ 證非恆真）：以 `git hash-object -w --path=<X>` 對**同一來源檔**套用不同副檔名：
`probe.cnt`／`probe.lin`／`probe.dxf` ⇒ **703**；`probe.tmpx`／`probe.txt`／`probe.md` ⇒ **684**
（恰 ＝ `703 − 19` CRLF）⇒ 條款確實生效。

🔒 **量測器自檢**（⛔ 先自檢後量測·人造檔兩造皆可見）：人造已知檔
`a|CRLF|b|CRLF|c|CRLF|d|LF|e|LF|f|CR|g`，期望值**逐字元手推** ＝ 16 bytes／CRLF 3／孤立 CR 1／孤立 LF 2，
實測全符。判別力：純 LF 檔量得 CRLF ＝ 0（非恆定）。
🩸 **兩次自檢曾壞·⛔ 未掩蓋**：(1) 首次期望位元組誤推為 17（實為 16）——**錯在期望值、非量測器**；
(2) 判別力自檢首版漏 `-w` 致 blob 未寫入、`git cat-file -s` 回空值而被讀成「已正規化」——
**空值有多義，先疑量測器**。二者皆已更正後重跑，⛔ 未沿用壞版結論。

**二檔之行尾實測**：`.cnt` 703 B／CRLF **19**／孤立 CR 0／孤立 LF 0；
`.lin` 378 B／CRLF **18**／孤立 CR 0／孤立 LF 0 ⇒ **均為純 CRLF**，單之陳述**坐實**。

### B-1 `K-9-6-h` 入倉

**編號現查**：`K-9-6-*` 已占用 `0`／`a`〜`g` ⇒ **`h`** 為下一未占用。
**落點**：`K-6` 之 `K-9-6-d 落地狀態` 之後、`K-9-7` 之前（⛔ 原文不刪）。
**五款逐字照抄**（⛔ 未改寫、⛔ 未補充）；併附「與現行碼之落差」對照表（依 §一 之實得）。

🔒 **`S-5` ⛔ 未觸發**：五款自洽、與 B-0 示意圖相符；④ 之「向量分量」所指未定一節，
施工單已自行指派為 `B-2` 之待決項 ⇒ ⛔ 非「與 KL 原意有疑」。

### B-2 `GB-65`（平行判準兩讀法）

兩讀法**⛔ 未擇一**照錄；併記讀法甲之**非等向性**（軸向較嚴、45° 較寬，約 `√2` 倍）。
**🛑 需 KL ＝ 否**（先以資料判別）。**⛔ 本批未試算**。

🔒 **`B-2 之否證構造證`**（⛔ 已否證·不得再列入之讀法 ＝ 以**原始 Δ 分量**比至 `1e-3`）：
B-0 座標檔中後側境界線為**同一直線之三段**，其原始 Δ 分別為

| 段 | 原始 Δ |
|---|---|
| `13→5` | `(6.197000, 29.452000)` |
| `5→2` | `(3.742000, 17.782000)` |
| `2→14` | `(38.461000, 182.766000)` |

⇒ **互異** ⇒ 該讀法會判**同一直線之三段互不平行**。**構造證成立。**
（對照：三段之**單位**向量四捨五入至 `1e-3` 後皆為 `(0.206, 0.979)` ⇒ 讀法甲判為平行。）

### B-3 `GB-66`（分岔閘之兩側連續性未證）

已登記；⛔ 本批不證。併記「若證得不連續，其處置須 KL 裁」。

### B-4 示意圖之權威射程

已於 PROVENANCE §四 **逐字照抄**，並附 d1〜d5 之**自量**實測（⛔ 未採信單之數字）。

---

## §五 停機條件之逐條判

| 碼 | 判 | 依據 |
|---|---|---|
| **S-1**（`.py` 出現 diff） | ⛔ **未觸發** | `git status --porcelain \| grep -c '\.py$'` ⇒ **0** |
| **S-2**（對照組回 0） | 🛑 **觸發 1 格·已處置** | C-1 之 `stepg_pipeline.py`（`截角` 對照 ⇒ 0）⇒ 該格作廢、⛔ 未用於結論 |
| **S-3**（A-1 撤銷理由與實得不符） | ⛔ **未觸發** | `Q1` 成立 ⇒ 相符 |
| **S-4**（B-0 二檔不存在） | ⛔ **未觸發** | 二檔存在（檔名全形·已具名） |
| **S-5**（B-1 逐字與 KL 原意有疑） | ⛔ **未觸發** | 五款自洽·④ 之歧義已由 `B-2` 承接 |
| **S-6**（`Q4` 不成立） | ⛔ **未觸發** | 其受詞為「垂直性不成立」，而垂直性**成立**（`90.000000000000°`）；措辭誤已具名 |

---

## §六 入倉清單

| # | 落點 | 性質 |
|---|---|---|
| ① | `docs/reports/W-G.9-34_更正入倉與深度量法掛號.md` | 新建（本報告） |
| ② | `.gitattributes` | 純追加（`*.cnt`／`*.lin -text` ＋ 同型案由） |
| ③ | `docs/specs/figures/最小深度示意圖＿座標.cnt`／`.lin` | 新增（KL 送達之原檔·逐 byte 凍結） |
| ④ | `docs/specs/figures/PROVENANCE_最小深度示意圖.md` | 新建（含 B-4 射程） |
| ⑤ | `docs/rulings/K-6_…md` ＋ **`K-9-6-h`** | 純插入（B-1 五款逐字） |
| ⑥ | `docs/rulings/K-6_…md` 之實作狀態段 | 就地加註（A-2 ③·⛔ 原文不刪） |
| ⑦ | `docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-64` | 就地加註（A-1 撤銷·⛔ 原文不刪） |
| ⑧ | 同上之 `GB-51` | 就地加註（A-2 ①②·⛔ 原文不刪） |
| ⑨ | 同上 ＋ **`GB-65`／`GB-66`／`GB-67`** | 新列三則（B-2／B-3／A-3） |
| ⑩ | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 純追加二則（自誤 7／8） |
| ⑪ | `.claude/skills/failure-archaeology/SKILL.md` ＋ **節 83** | 純追加 |
| ⑫ | `docs/reports/W-G.9波_新session交接文_WG913-WG931.md` | 檔末加註（§4③·⛔ 無行號錨） |
| ⑬ | `docs/design/W-G.9-30_∥SIDELINE界線之接線設計.md` | 檔末加註（§4④·⛔ 原句不改） |

**施工單 §4 開四項（①②③④）·本批辦四項**（②「一切加註純追加」為**橫向要求**、非獨立落點；
③④ 為 ⑫⑬）。

---

## §七 ⛔ 本批未涵蓋之事

1. ⛔ **未實作 `K-9-6-h` 之任何一款**（施工單明訂「本批不實作」）。
2. ⛔ **未試算 `GB-65` 之兩讀法對六街廓之分類**，⛔ 未量任何街廓之夾角。
3. ⛔ **未證 `GB-66` 之兩側連續性**，⛔ 未估 `d_A`／`d_B` 之差。
4. ⛔ **未查 `GB-67` 之新受詞**（不相鄰之宗之間為何會重疊）；⛔ **未重跑** `probe_D2b17_dual`
   ——故 `near_dir` 落地**後**之破量落點**倉內仍無**。
5. ⛔ **未重定** `W-G.9-30` 設計文件之 `§3`／`§5`——本批僅於檔末加註其受詞已由 A-1／A-3 更正。
6. ⛔ **未跑 `run_all`**、⛔ 未跑任何探針、⛔ 未動 `verify/baselines/`（`VR-012`）。
7. ⛔ **未查** `W-G.9-32` 是否實際跑過其施工單 §0 之開場三式——倉內不留痕，**無從查**、⛔ 不臆測。
8. ⛔ **未認定 `D-②` 之整體完成度**——A-2 ③ 之加註僅更正「尚未實作」四字之現時真偽。
9. ⛔ **未 pull 主目錄**（`wip/s1-endpart` 停在 `17e3290`·僅落後·非分岔）——屬 KL 之工作副本。
10. ⛔ **未撤回** `VR-013` 之其餘部分——`VR-013` 之受詞為「§3 與 §5 是否同一界面」，
    其**界面歸屬**之結論不因 `GB-64` 撤銷而變；惟其所據之「無表達力」已由本批證偽
    ⇒ **該則之後續處理待 `§3`／`§5` 重定時一併辦**（⛔ 本批不動 `VR-013`）。
