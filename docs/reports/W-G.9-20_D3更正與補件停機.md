# 【W-G.9-20 補件停機】`D3` 更正已入倉；§2 因**施工單第 1 版未達 CC** 而停機

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`126b72b`　**量測**：`W-G.9-20` commit B₁
**證據**：`verify/out/probe_WG920_pp_lowerbound.log`
**字樣錨（倉規 A）**：`【W-G.9-20 補件停機】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行（＝既有報告之慣例·⛔ 無第三處）
　🔒 **判別力自檢**：改用**寬字樣** `W-G.9-20` ⇒ **5 檔**命中 ⇒ 上式**非恆為 2**

> 🔴 **本批之處置**
>
> · **§1（`D3` 更正）**：✅ **已辦、已入倉**——⛔ 不因後面有新工作而晚一步。
> · **§2（族① 上限估算）**：🛑 **停機**——施工單第 1 版**從未進入 CC 之可見範圍**。
>
> ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 未觸；估算全程**只讀包裝、用畢還原**。

---

## §一 🛑 停機事由：施工單**不自足**

第 2 版 §1 曰：「**§0 現查、§1 之 `D3` 更正三處、§8 考古節 —— 全部照第 1 版辦，⛔ 一字不改**」，
§4 曰「**追加於第 1 版之<u>十五條</u>**」。

🔴 **第 1 版未達 CC。** 現查（⛔ 正面列舉母體與判準）：

| 母體 | 判準 | 結果 | 對照組（證判法非壞掉） |
|---|---|---|---|
| 本 session 之 transcript（`…/a0c15b41-….jsonl`） | 含字串 `W-G.9-20` 且 `role == user` | **1 筆**（＝**第 2 版**本身） | `W-G.9-19` ⇒ **36** 命中 |
| git index 全部追蹤檔 | `git grep -c "W-G.9-20"` | **0 檔** | `W-G.9-19` ⇒ **5 檔** |

⇒ 依 `CLAUDE.md`「🔒 **施工單必須自足**」之**判準（給 CC）**：
> 收到「將本訊息之 X 入倉」而單內查無 X 時，⛔ **禁編造、禁以倉內近似物代替**；
> **照實回報「單內無 X」並請補**（失敗考古節 52）。

### 一-1 請補之逐字（⛔ 逐項·⛔ CC 不代擬）

| # | 缺件 | 為何非補不可 |
|---|---|---|
| **1** | **§1 之「`D3` 更正<u>三處</u>」逐字** | 🔴 我自行現查所得之更正落點為 **6 處**（卷首／`D3` 表列／§3／§5／§9／§10），**非 3 處** ⇒ ⛔ 我無從知道第 1 版指的是哪三處 |
| **2** | **§0「現查」之項目清單** | ⛔ 不知其受檢對象與判準，無從「照辦」 |
| **3** | **§8「考古節」之逐字** | 考古節須逐字入 `.claude/skills/failure-archaeology/SKILL.md`，⛔ 不得改寫 |
| **4** | **第 1 版之「十五條」驗收** | §4 之「新 A–E」係**追加**；缺基底十五條則驗收不完整 |
| **5** | **§4 新 E 所載之「8.6〜8.9 個百分點」之出處** | 見 §三-3——該數為 **【單】**，倉內查無 |

### 一-2 ⛔ 我在缺件下**做了什麼／沒做什麼**

| | |
|---|---|
| ✅ **做了** | `D3` 更正之**實質**——因其為偽與否，**我可自行現查判定**（§二），⛔ 不需第 1 版 |
| ⛔ **沒做** | 更正之**逐字**——本批之加註係 **CC 自撰**，⛔ **未冒充第 1 版之文字**，且已於加註內就地聲明 |
| ⛔ **沒做** | §2（族① 上限估算）——見 §四 |

🔒 **理由**：施工單自陳「⛔ **不得因為後面有新工作就晚一步更正**」。
⇒ **等補件再更正 ＝ 晚一步**；**編造第 1 版之文字 ＝ 造假**。
⇒ 取**第三條路**：以自行現查立更正、就地聲明其出處、待補件後**逐字對讀**（⛔ 原句仍不刪）。

---

## §二 ✅ §1 `D3` 更正——**已現查、已入倉**

### 二-1 更正之落點（⛔ 加註·原句一字未刪·刪除欄 0）

於 `docs/reports/W-G.9-19_下界估算未達與同源前置.md` 加註 **3 處**
（字樣錨 `【W-G.9-20 D3 更正】`）：卷首之後（涵蓋卷首＋`D3` 表列＋`D5`）、§3 末、§10 末。

🔒 **計數指令與母體**：`grep -rn "【W-G.9-20 D3 更正】" docs verify | wc -l` ⇒ **4**
＝ 加註 **3** ＋ 本報告之引述 **1**。⛔ 該錨**非唯一命中型**（它是**加註標記**、非報告標題）。
🔒 **刪除欄自證**：`git diff --numstat` 對該檔 ⇒ **`85  0`** ⇒ **原句一字未刪**（機械驗）。

### 二-2 更正之內容（🔴 一句話）

> **`D3` 之理由為偽：我把<u>族①</u> 之槽當成了<u>族②③</u> 之槽。**

`W-G.9-19` 讀到 `left_cum_S = buf`（**沿 `s`**）便判「現行值沿 `s`、新值 ⊥SIDELINE ⇒ **不同軸·不得相減**」。
但那是**族①（落位）** 之槽。**族②③（起算）** 之槽是另一個。

**現查（⛔ 正面列舉受檢檔·判準＝含該字樣）**：

| # | 檔 | 字樣 | 意義 |
|---|---|---|---|
| ① | `app.py` | `_select_pool_slot` docstring：`b＝forced 起始 W`／`w_i（⊥ALLOC、W 軸單位）` | 該槽之受詞**自其定義即為垂距** |
| ② | `verify/stepg_pipeline.py` | `'b': _b_L0` ← `_mp_base_W0(…)` ← `_k956_W_from_mp(…)` | 🔴 **引擎已餵正典 `K-9-5-6` 直量** |
| ③ | `app.py`（`main()` 內） | `'b': _left_buffer_S * _cos_dn`　#『**入 W 軸**』 | 舊式，但**換算係數早在碼面** |
| ④ | `app.py`（`main()` 內） | `_W_prev_left = (_left_buffer_S * _cos_dn) if _has_left_corner else 0.0` | 同③ |

### 二-3 🔒 實測錨（⛔ 非推論·附判別力自檢）

引擎於 `R1` 餵入 `_select_pool_slot` 之值：

| 槽 | 值 | 是否 ∈ `k956_W_from_mp` 之輸出（`1e-12` 內） |
|---|---|---|
| `b_L` | `0.1618772119011016` | ✅ |
| `b_R` | `-0.2005692029254842` | ✅ |

🔒 **判別力自檢**：以**必不命中**之值（`b_L + 1.0`）試 ⇒ **不命中** ⇒ 上式**非恆真**。

⇒ 🔴 **引擎之 `b` <u>即</u>正典 `K-9-5-6`「MP → 遠側界之垂距」** ⇒
與 `W-G.9-19` 所取之新值（`6.9990`／`6.9999`／`6.9990` m）**同軸·可相減** ⇒
**`D3` 之「不同軸」證偽。**

### 二-4 ✅ 未受影響／🔴 連帶為偽

| | 項 | 判 |
|---|---|---|
| ✅ | **面積差仍未估** | 換成面積仍須族①（落位）一併定案 ⇒ ⛔ 本批未估任何面積 |
| ✅ | **`D4`（族① 不可估）** | 其判準與本更正無涉 ⇒ 不受影響 |
| 🔴 | **`D5` 之「且更強」** | **為偽** ⇒ **`K-4` 回到原述強度**（「兩族必須同時改」），⛔ 不再更強 |

---

## §三 🔴 下界之**值**仍未量得——**成因與上批所述完全不同**

### 三-1 成因具名

`run_step_g` 於**第一個街廓 `R1`** 即撞結構閘 `理論＝實跑` 而拋：

| 情境 | 閘之訊息（逐字要點） |
|---|---|
| `0m` | 街廓 `R1` left 側　理論@k\*=`97.18` ≠ 實跑(階段1)=`100.02`（Δ=`2.837` > `0.1`） |
| `3.5m` | 街廓 `R1` left 側　理論@k\*=`96.30` ≠ 實跑(階段1)=`98.75`（Δ=`2.446` > `0.1`） |

⇒ `_select_pool_slot` **全程只被呼叫 1 次**（母體＝只讀包裝之記錄·判準＝呼叫計數）
⇒ 目標三格（`R5/left`／`R2/left`／`R3/right`·`W-G.9-19` `D1` 之母體）**從未被跑到**。

### 三-2 🔴 「未量得」≠「量得為 0」

| 上批所述 | **實際** |
|---|---|
| **不可估**（結構上不可能）⇒ 須先解表示法問題 | **沒跑到**（閘於 `R1` 攔截）⇒ **可修**——該閘即 `W-G.9-14` 所分解者 |

🩸 **本批之自誌**：探針**首版**在識別失敗時**印出「下界 ＝ 0.0000 百分點」**
——那正是我自己在 `CLAUDE.md` 立過的戒（「**無從判定**」與「**判定為偽**」⛔ 不得共用出艙碼）。
已改為兩個分開之出艙碼（`無從判定` ／ `判準下為 0`）。
🔒 **教訓**：**立戒者在下一批最容易違反自己的戒**——本例之違反距立戒僅數批。

### 三-3 ⚠️ 施工單所載之「8.6〜8.9 個百分點」

**標 `【單】`**——母體＝git index 全部追蹤檔，判準＝`git grep`，**倉內查無**。
🔴 依 `CLAUDE.md`「`【倉】`／`【單】` 出處標注」第 2 款：
**⛔ 下游文件不得引用為事實**；須引用則**先入庫使其成為 `【倉】`**。
🔴 **本批⛔ 未能將其轉為 `【倉】`**（§三-1 之故）。
⚠️ 併看第 4 款：**兩個近似值相符 ≠ 相互驗證** ⇒ 即使他日量得之值與其相近，
亦⛔ 不得據以認定該數為真。

---

## §四 🛑 §2（族① 上限估算）之停機

⛔ **本批未起手。** 事由：

1. **§5 之次序為紅線**：「§1（更正）**先辦並先入 commit**，⛔ **不得與 §2 併批**」
   ⇒ §1 已入 commit（本批），§2 應為**下一批**。
2. **§4 之驗收不完整**：「追加於第 1 版之**十五條**」，而該十五條未達 CC（§一）。
3. **§2-2 之舉證標準第 ② 項（母體涵蓋）現況不可能滿足**：
   🔴 上限須「證明所考慮之情形**窮盡**」，而**目前連現有三格都跑不到**（§三-1）
   ⇒ 任何「上限」都只會是**已觀測最大值**，且**觀測數為 0** ⇒ ⛔ 連該退路都不成立。

⚠️ **⛔ 我未對 §2-3（幾何約束之值域）作任何現查**——它屬 §2，而 §2 停機。
⚠️ **⛔ 我未對「上限是否依賴表示法」作任何論斷**（§3 之 `F3`）。

### 四-1 §2 之解凍前置（⛔ 只列·不設計）

| # | 前置 |
|---|---|
| **1** | 🛑 **施工單第 1 版之逐字補齊**（§一-1 之五項） |
| **2** | 🛑 **結構閘於 `R1` 之攔截已解除**——否則母體恆為 0，§2-2 ② 不可能滿足 |
| **3** | 其修法（校回起點）之授權——已呈 KL，⛔ 本批未動 |

---

## §五 🔴 併發現：`GB-62` 之**碼面實例**（⛔ 非本批受詞·但不得不記）

**同一個 `'b'` 槽，兩條路徑不同源**：

| 路徑 | `'b'` 之值 | 性質 |
|---|---|---|
| **引擎**（`verify/stepg_pipeline.py`·harness 走此） | `k956_W_from_mp(…)` | **正典 `K-9-5-6` 直量** |
| **`main()`**（`app.py`·**KL 實際操作之介面**走此） | `_left_buffer_S * _cos_dn` | **舊式** |

🔴 **無任何閘可擋**：依 `CLAUDE.md`，`main()` 內之敘述**從不被 `run_all` 執行**
⇒ 二者可長期分歧而不被發現。**本次係碰巧撞到才發現。**

🔒 **旁證**：本批探針讀 `f3_forced_offset_diag`（`W-G.9-4` 之併行診斷欄）得**空 dict**
——因該診斷欄之寫入敘述亦在 `main()` 內。

⇒ 登記為 **`GB-62`** 之具名實例（⛔ 不另立新號·⛔ 不改既有編號）。

---

## §六 ⛔ 本批未做之事（⛔ 逐項）

1. ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 零觸；只讀包裝**用畢還原**。
2. ⛔ **未估任何面積**、⛔ 未取任何代表值、⛔ 未為族① 取任何值。
3. ⛔ **未量得下界之值**（§三-1 具名成因）、⛔ **未把 `8.6〜8.9` 當成事實引用**。
4. ⛔ **未起手 §2**、⛔ 未現查 §2-3 之幾何約束、⛔ 未論斷 `F3`。
5. ⛔ 未動 `docs/rulings/`／`CLAUDE.md`／`run_verification.py`／`baselines/`／既有夾具。
6. ⛔ 未改期望 FAIL 名單、未重凍、未重烤、未修任何既有 FAIL。
7. ⛔ **未跑長跑**——本批未觸及 `run_verification` 之任何輸入
   （`git diff --numstat 126b72b -- app.py verify/stepg_pipeline.py verify/run_verification.py
   verify/wv_reconcile.py verify/baselines/ docs/rulings/ CLAUDE.md` ⇒ **空輸出**；
   🔒 判別力自檢：同式對 `docs/reports/` ⇒ **非空** ⇒ 上式非恆空）
   ⇒ 判定層在構造上不可能變動。
8. ⛔ 未追那 57 條、未追 `X5` 之 6 條、⛔ 未掛 `wg915` 盤點器。

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。

---

## §白話稿 呈 KL（依 `重劃白話` 之呈送段）

### 【現況】

**上一次我給您的說法錯了。我說「算不出來」——其實是「<u>沒算到</u>」。**

這兩個差很多：

| | 意思 | 能不能修 |
|---|---|---|
| 我上次說的「**算不出來**」 | 就算給再多時間也沒用，得先解一個技術問題 | ❌ |
| **實際上是「沒算到」** | 程式跑到**第一個街廓就停住**了，要看的那三個街廓根本沒跑到 | ✅ **可以修** |

**我錯在哪：這裡有<u>兩個不同的量</u>，我把其中一個當成另一個。**

```
   ┌─────────────────────── 前緣線 ───────────────────────┐
   │                                                      │
   │   MP ●━━━━━━━━━━━━━━━━━━ 側街境界線（SIDELINE）        │
   │      ┃╲                                              │
   │      ┃ ╲ ← 新的界線（平行側街境界線）                  │
   │      ┃  ╲                                            │
   │      ┃                                               │
   │   ①「**地要排在哪裡**」用的量 → 沿**前緣線**方向量      │
   │   ②「**負擔從哪裡開始算**」用的量 → 從 MP **垂直**量    │
   │                                                      │
   │   ⚠️ 上次我拿 ① 去跟新界線比 ⇒ 方向對不上 ⇒ 我說不能比  │
   │   ✅ 但該拿去比的是 ② ——它**本來就是垂直量的**          │
   │      跟新界線**同一個方向**，本來就能比                 │
   └──────────────────────────────────────────────────────┘
   單位：①② 皆為公尺｜⚠️ 三者不同物：MP＝一個點；界線＝一條線；①②＝一段距離
```

**還有一件更意外的**：程式裡**負責計算的那一支（引擎）<u>已經改用新的量了</u>**，
只有**畫面那一支（您實際操作的介面）還在用舊的**。**兩邊不一樣，而且沒有任何檢查在管這件事。**

### 【對土地的影響】

1. **這次更正<u>本身不改變任何一筆配地面積</u>**——它只是把上次的說法改對。
2. **「側街負擔會差多少」這個數字，這次還是沒有量到**——但原因不同：程式在第一個街廓就停了。
3. ⛔ **我不會給您一個猜的數字。** 施工單上有一個數（8.6〜8.9 個百分點），
   但**倉裡查不到它的來源**，所以我把它標成「未經查證」，**⛔ 不當成事實用**。

### 【要你判斷】

**這一題不需要您裁。標記：只是知會。**

（但下面第 1 點會需要您決定**順序**。）

### 【裁完之後會接著冒出來的問題】

1. 🔴 **要量那個數字，得先讓程式跑得過第一個街廓。** 那個攔截點的修法我早先已呈給您，
   還沒動手。⇒ **您要不要讓我先修它？**
2. 🔴 **引擎與畫面兩邊不同步，沒有任何檢查在管。** 這次是碰巧撞到才發現的
   ——⇒ 同樣的分歧**可能還有別處**，本批⛔ 未查。
3. ⚠️ **上次那份稿子請不要再用**——裡面有三句是錯的，已在倉裡標明作廢（原文保留供考古）。
4. ⚠️ 「排位置那一半的上限」（您上次點頭要我去估的那個）**本批未起手**
   ——因為**目前一格都跑不到**，估不出「上限」，只會估出一個假的。
