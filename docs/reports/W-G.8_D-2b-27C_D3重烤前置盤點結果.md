# W-G.8 D-2b-27 commit C：`D-3` 重烤前置盤點 —— **結果與對帳**（⛔ 零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**基準**：`787e9d6`
**事前登記**：commit A ＝ `787e9d6`（⛔ 零盤點輸出）

> ⛔ **本單只供事實**：⛔ **不判定「該不該重烤」「何時重烤」**——那是 KL 之裁（施工單 §4-6）。

---

## §6-5／§6-6 正面自證（**原樣貼**）

```
$ git diff --stat -- app.py verify/
```
（**無輸出**。）

```
$ env | grep WV_BAKE
```
（**無輸出** ⇒ **`WV_BAKE` 未設**·施工單 §4-1 之不授權已守。）

---

## §6-1 母體自證：**差集為空**（⛔ 非以檔數相等代替）

```
$ find verify/baselines -type f | sort                                   > pop.txt      # 298
$ grep -E "^verify/baselines/(PRE-S0b_S0c_幾何漏歸零_凍存|PRE-比率更新_凍存|PRE勘誤_期日|v2)/" pop.txt > frozen.txt  # 218
$ grep -vE "^…同上…"                                                     pop.txt > live.txt   # 80
$ cat frozen.txt live.txt | sort > union.txt
$ diff union.txt pop.txt | wc -l
0
```

⇒ **(凍存 ∪ 現用) 與母體之差集為空**（`diff` 輸出 **0** 行）。母體 **298** ＝ 凍存 **218** ＋ 現用 **80**。

### 凍存子目錄（**具名列出**·⛔ 非「其餘為凍存」一句略過）

| 凍存子樹 | 檔數 |
|---|---|
| `verify/baselines/PRE-S0b_S0c_幾何漏歸零_凍存/`（`v3/`＋`wf/f0–f4/`） | 69 |
| `verify/baselines/PRE-比率更新_凍存/`（`case_params_UC9898.json`＋`v3/`＋`wf/f0–f4/`） | 70 |
| `verify/baselines/PRE勘誤_期日/`（`README.md`＋`v3/`＋`wf/f0–f4/`） | 64 |
| `verify/baselines/v2/`（財務半中間態期之歷史錨·`run_verification.py:36` 逐字「**凍存不刪、不再對拍**」） | 15 |
| **合計** | **218** |

---

## §1 三命題之驗證結果

| 命題 | 結果 | 證據 |
|---|---|---|
| **P1** | 🔴 **部分證偽** | 見 §1-1 |
| **P2** | ✅ **成立** | 見 §1-2 |
| **P3** | ✅ **成立** | 見 §1-3 |

### 1-1 🔴 P1 **部分證偽**（⛔ 非停機條件·如實列出）

P1 稱「現用基準之產出者為 **KL 之 Streamlit UI 實跑、非 harness 生成**」。
**現查各家族之 `PROVENANCE` 自載**：

| 家族 | 檔數 | **自載之產出者** | 出處（`檔:行`） |
|---|---|---|---|
| **頂層** | 13（含 `PROVENANCE.md` 本身） | **KL**（Streamlit UI 實跑，非 harness 生成——與 driver 非同源，對拍有效） | `verify/baselines/PROVENANCE.md:7` |
| **`v3/`** | 17 | 🔴 **CC**（`verify/` **headless harness 實跑**）；財務真值由 KL 供料 | `verify/baselines/v3/PROVENANCE_v3.md:29` |
| **`wf/f0`** | 13 | 🔴 **CC**（`verify/wf_f0.py` 實跑） | `.../wf/f0/PROVENANCE_F0.md:8` |
| **`wf/f1`** | 7 | 🔴 **CC**（`wf_f1.py` 實跑） | `.../wf/f1/PROVENANCE_F1.md:9` |
| **`wf/f2`** | 7 | 🔴 **CC**（`verify/wf_f2.py` 實跑） | `.../wf/f2/PROVENANCE_F2.md:9` |
| **`wf/f3`** | 9 | 🔴 **CC**（`verify/wf_f3.py` 實跑） | `.../wf/f3/PROVENANCE_F3.md:9` |
| **`wf/f4`** | 13 | 🔴 **CC**（`verify/wf_f4.py` 實跑） | `.../wf/f4/PROVENANCE_F4.md:28` |
| `wf/PROVENANCE_F5.md` | 1 | （汰換紀錄文件·**非基準**） | 該檔卷首 |

⇒ 🔴 **80 個現用檔中，<u>67 個</u>（`v3/` 17 ＋ `wf/` 50）自載為 CC harness 實跑。**
**P1 僅對頂層 13 檔成立。**

🔒 **依事前承諾之嚴格性**：本判定**未只憑 `PROVENANCE.md` 自載**——
各家族之產出者係**逐檔讀其各自之 `PROVENANCE_*.md`**取得；⛔ 未以頂層之宣告外推至子目錄。
⚠️ **仍屬「自載」層級之證據**：⛔ 本盤點**未**獨立重現任一基準之產出過程
（重現須設 `WV_BAKE`／跑 UI，二者皆為本單之不授權）⇒
**(a) 欄之判定標為「自載（未獨立重現）」**，⛔ 不標為已證。

### 1-2 ✅ P2 **成立**

`D-1` ＝ **`c56067f`**（`K-6-A2 D-1: F 槽切換 路寬→SIDE_LINE 全長`·`git log` 現查）；
`D-2` ＝ **`f8b42fc`**（正典 `docs/rulings/K-6_…md:656` 逐字「D-2·`f8b42fc`」佐證）。

```
$ while read f; do n=$(git log --oneline c56067f..HEAD -- "$f" | wc -l); [ $n -gt 0 ] && echo "$n $f"; done < live.txt
（無輸出）
⇒ 自 D-1 起被修改之現用基準檔 = 0
⇒ 凍存檔同期間被修改數     = 0
```

⇒ **P2 成立** ⇒ 施工單 §5-1 之停機條件 **未觸發**。

### 1-3 ✅ P3 **成立**

`WV_BAKE` 之 dump 由**驗證程式自身**寫出：
`verify/run_verification.py:405-408` 逐字
「🆕 重烤機制（env `WV_BAKE=<dir>`；未設＝inert，零行為變更）：dump got_rows 到……
正式烤 `WV_BAKE=BASELINES` 本身（**原地覆寫**）」／`_bake_dir = os.environ.get("WV_BAKE")`。

⇒ 與 P1 之「KL Streamlit UI 實跑」路徑**不同源** ⇒ **P3 成立**。
🔒 **驗法合乎事前承諾**：以**讀碼**判定寫出點，⛔ **未設 `WV_BAKE`**（§6-6 空輸出自證）。

---

## §2 盤點表（(a)〜(d)·**逐家族·欄以家族內全部檔名列出**）

> 🔒 **粒度說明**：同一家族內之各欄由**同一段程式**寫出（§(b) 之 grep 指認即該段），
> 故 (a)〜(d) 之答**逐欄相同**；下表以家族為列、**逐檔具名**於「欄」格內，
> ⛔ 未以「其餘同上」略過任何檔。

| 家族／欄（逐檔具名） | **(a) 產出者** | **(b) 相對 StepG** | **(c) 是否因本波過期** | **(d) 重烤路徑之影響** |
|---|---|---|---|---|
| **頂層-參數**：`退縮0m參數.csv`／`退縮3.5m參數.csv` | KL UI 實跑（自載·未獨立重現） | **上游**——對拍點 `run_verification.py:511`，位於 StepG `try`（`:655-686`）**之前** | ❌ **不過期**——參數表為**輸入側**（退縮值／地價／尺度），`D-1`／`D-2`／`D-2b` 三者所改皆為**分配計算**，未改任何參數之產生 | 若重烤 ⇒ 產出者由 **KL UI** 變 **harness** ⇒ 🔴 **破壞「與 driver 非同源」** |
| **頂層-指配**：`第 1 宗街角地指配結果_退縮0m.csv`／`_退縮3.5m.csv` | KL UI 實跑（自載） | **上游**——對拍點 `:636`（`run_corner_pk` 之產物·StepG 之前） | ✅ **過期**——受 **`D-1`（`c56067f`：F 槽由路寬改 SIDE_LINE 全長 ⇒ 真G 改）** 與 **`D-2`（`f8b42fc`：街角規定範圍幾何切換 ⇒ 門檻改）** 影響；二者同時改動資格閘之**左右兩端** | 同上 🔴 破壞非同源 |
| **頂層-診斷**：`W-D.1.2 診斷_退縮0m.csv`／`_退縮3.5m.csv`／`W-D.1.3-a 診斷_退縮0m.csv`／`_退縮3.5m.csv` | KL UI 實跑（自載） | **上游**——對拍點 `:517`／`:605`（皆早於 `:655`） | ✅ **過期**（`W-D.1.2`／`W-D.1.3-a` 含街角三指數與真G 欄）——同上 `D-1`＋`D-2` | 同上 🔴 |
| **頂層-抵費地**：`W-D.1.3-d 驗收_退縮3.5m.csv` | KL UI 實跑（自載） | **上游**——對拍點 `:643`，仍早於 StepG `try`；其資料源為 `run_corner_pk` 之 forced／offset 列，⛔ 非 StepG 之池片 | ✅ **過期**——受 **`D-2b`（`c89f1dc`：池帶校回幾何餘 ⇒ 抵費地面積淨增 `122.3511㎡`）** 影響 | 同上 🔴 |
| **頂層-其他**：`V6dxf_各街廓鄰街地負擔.csv` | KL UI 實跑（自載）；`PROVENANCE.md:34` 逐字「快照來源（**harness 不對拍**）」 | **不適用**（非對拍標的） | ❌ **不過期**——非對拍標的，且為 CAD 導出之靜態量 | 不適用（重烤不觸及） |
| **頂層-E 系列**：`E系列實測快照_退縮0m.csv`／`_退縮3.5m.csv` | **不可判定** —— `PROVENANCE.md` 之檔案清單**未列此二檔**（其清單止於 `V6dxf_…`）；⛔ 無自載可據、⛔ 本單不得跑重現 | **下游**（E 世代 ＝ `wf_f4` trunk-E 之終態） | ✅ **過期**（推定·**未證**）——E 世代位於 StepG 下游，`D-2b` 之幾何改動必及之；⛔ 惟因 (a) 不可判定，本列標**待補** | 若重烤 ⇒ 產出者未知 ⇒ 影響**不可判定** |
| **`v3/`（17 檔）**：`G 值計算結果_退縮{0m,3.5m}.csv`／`W-D.1.2 診斷_退縮{0m,3.5m}.csv`／`W-D.2 §3 滑池槽診斷_退縮{0m,3.5m}.csv`／`逐槽 J 表_退縮{0m,3.5m}.csv`／`W-D.4_四梯分級清單_退縮{0m,3.5m}.csv`／`W-D.4_碎片遞補對照_退縮{0m,3.5m}.csv`／`W-D.4_跨占分配線_退縮{0m,3.5m}.csv`／`wd3_fragment_edges.csv`／`wd3_fragment_geom.csv`／`PROVENANCE_v3.md` | 🔴 **CC harness 實跑**（`PROVENANCE_v3.md:29`） | **混合**：`W-D.1.2 診斷` 之 `v3` 版對拍於 `:597`（**上游**）；`G 值`／`滑池槽診斷`／`逐槽 J 表` 對拍於 `:669`／`:673`／`:677`，**落在 StepG `try`（`:655-686`）內** ⇒ **下游**；`wd3_fragment_*`（`:798`／`:801`）與 `W-D.4_*`（`:819`–`:827`）在 `try` 之後、**依賴 `stepg_ctx`** ⇒ **下游** | ✅ **過期**——下游各欄受 **`D-2b`（`79bf2d7`：`K-9-5-4 ②` 第 2 宗近側界 ⇒ 宗地幾何／`S`／`G` 改；實測 `cum_S` 差見 `probe_D2b23A2_params_after.log`）** 與 **`c89f1dc`（池校回）** 影響；`W-D.1.2 診斷`（上游）另受 `D-1`＋`D-2` 影響 | 重烤**不改變**其產出者（本即 harness）⇒ ⛔ **不破壞非同源性質——因該性質對本家族<u>從未成立</u>** |
| **`wf/f0`（13 檔）**：`F.0_G值_退縮{0m,3.5m}.csv`／`F.0_合併決策_…`／`F.0_旗標消長_…`／`F.0_池差_…`／`F.0_滑池槽診斷_…`／`F.0_逐槽J表_…`（各 ×2）／`PROVENANCE_F0.md` | 🔴 **CC harness 實跑**（`PROVENANCE_F0.md:8`·`verify/wf_f0.py`） | **下游**——對拍點 `:955`–`:970`，位於 StepG `try` **之後**且以 `stepg_ctx[tag]` 為輸入（`run_all` 之「上游世代未產出：`['trunkA(_ctx)','F.0',…]`」即此） | ✅ **過期**——同上 `79bf2d7` ＋ `c89f1dc`（F.0 之池差欄直接吃池面積） | 同 `v3/` ⇒ ⛔ 不破壞（本即 harness） |
| **`wf/f1`（7 檔）**：`F.1_池驗證_…`／`F.1_碎片處置_…`／`F.1_遞補整形_…`（各 ×2）／`PROVENANCE_F1.md` | 🔴 **CC harness**（`PROVENANCE_F1.md:9`） | **下游**（對拍點 `:1026`／`:1029`） | ✅ **過期**——`F.1` 直接呼叫 `_pool_strips_for_block`（`verify/wf_f1.py:386`）⇒ 受 `c89f1dc` 之池校回影響 | 同上 |
| **`wf/f2`（7 檔）**：`F.2_G值_…`／`F.2_池流向_…`／`F.2_跨街廓調配_…`（各 ×2）／`PROVENANCE_F2.md` | 🔴 **CC harness**（`PROVENANCE_F2.md:9`） | **下游** | ✅ **過期**——同上 | 同上 |
| **`wf/f3`（9 檔）**：`F.3_G值_…`／`F.3_公設調配_…`／`F.3_池流向_…`／`F.3_轉7-4_…`（各 ×2）／`PROVENANCE_F3.md` | 🔴 **CC harness**（`PROVENANCE_F3.md:9`） | **下游** | ✅ **過期**——同上 | 同上 |
| **`wf/f4`（13 檔）**：`F.4_G值_…`／`F.4_七五雙出口_…`／`F.4_公設調配_…`／`F.4_整形_…`／`F.4_池流向_…`／`F.4_總決算_…`（各 ×2）／`PROVENANCE_F4.md` | 🔴 **CC harness**（`PROVENANCE_F4.md:28`） | **下游** | ✅ **過期**——`wf_f4.py:926` 亦直接呼叫 `_pool_strips_for_block` ⇒ 受 `c89f1dc` 影響 | 同上 |
| `wf/PROVENANCE_F5.md` | （**非基準**·汰換紀錄文件） | 不適用 | 不適用 | 不適用 |

### 2-1 🔴 「不可判定」之具名列（⛔ 不留白、⛔ 不猜）

**唯一一組**：`E系列實測快照_退縮0m.csv`／`_退縮3.5m.csv` 之 **(a) 產出者**。
**理由**：`verify/baselines/PROVENANCE.md` 之檔案清單**未列此二檔**
（其表止於 `V6dxf_各街廓鄰街地負擔.csv`·`:34`），亦無獨立之 `PROVENANCE_E*.md`；
⇒ **無自載可據**，而獨立重現須跑 UI 或設 `WV_BAKE`（**皆為本單之不授權**）
⇒ **標「不可判定」**。**建議**：`D-3` 動工前先補該二檔之 provenance 記載。

---

## §3 附帶一問：`WV_BAKE` 於現行 StepG 中止狀態下，**哪些欄拿得到值／拿不到**

**前提（現查·`GB-52`／`GB-54`）**：StepG 於首個街廓失敗即整趟中止
（`verify/run_verification.py:655-686` 為單一 `Try`、handler `@684`；
實測 `grep -c "街廓 R[2-6]" verify/out/D2b24_runall.log` ⇒ **0**）。

| 欄（家族） | 判 | **由產出鏈之判定**（⛔ 非概括） |
|---|---|---|
| `退縮{tag}參數.csv` | ✅ **拿得到** | 對拍點 `:511` 在 `try`（`:655`）**之前** ⇒ 不受折疊 |
| `W-D.1.3-a 診斷_…` | ✅ **拿得到** | `:517`，同上 |
| `W-D.1.2 診斷_…`（頂層 `:605`／`v3` `:597`） | ✅ **拿得到** | 二處皆早於 `:655` |
| `第 1 宗街角地指配結果_…` | ✅ **拿得到** | `:636`，早於 `:655`；其源為 `run_corner_pk` |
| `W-D.1.3-d 驗收_退縮3.5m.csv` | ✅ **拿得到** | `:643`，早於 `:655` |
| `v3/G 值計算結果_…`／`v3/W-D.2 §3 滑池槽診斷_…`／`v3/逐槽 J 表_…` | 🔴 **拿不到** | 對拍點 `:669`／`:673`／`:677` **落在 `try` 內** ⇒ StepG raise ⇒ 該三對 `diff_rows` **從未執行** |
| `v3/wd3_fragment_*`／`v3/W-D.4_*` | 🔴 **拿不到** | `:798`／`:801`／`:819`–`:827` 在 `try` 之後，但**以 `stepg_ctx` 為輸入**；StepG 失敗 ⇒ 該 ctx 未產出 |
| `wf/f0`〜`wf/f4` **全部 49 個 CSV** | 🔴 **拿不到** | 皆以 `stepg_ctx[tag]` 為輸入；`run_all` 之實測訊息逐字「**上游世代未產出：`['trunkA(_ctx)', 'F.0', 'F.1', 'F.2', 'F.3', 'F.4']`（0m）**」 |
| `E系列實測快照_…` | 🔴 **拿不到**（推定·**未證**） | E 世代屬 `wf_f4` trunk-E 之終態 ⇒ 同上游依賴；⛔ 惟其產出鏈未經本單獨立指認，標**未證** |
| `V6dxf_各街廓鄰街地負擔.csv` | **不適用** | `PROVENANCE.md:34`「harness 不對拍」 |

🔑 **一句話**：**現狀下重烤，只會蓋掉「上游 12 個 CSV」——而那 12 個正是<u>唯一由 KL UI 產出</u>之族；
下游 67 個（本即 harness 產出、且確實過期者）反而<u>一個都烤不到</u>。**
⛔ 本段**僅陳述事實**，⛔ 不推導「該不該重烤」。

---

## §5 停機條件逐項

| # | 條件 | 現查 | 判 |
|---|---|---|---|
| 1 | **P2 證偽**（現用基準曾被修改） | 自 `D-1` 起被修改之現用檔 ＝ **0** | ❌ 未觸發 |
| 2 | 為完成盤點而需執行 `run_all`／設 `WV_BAKE`／改生產碼 | 三者皆未為；`env \| grep WV_BAKE` 空、`git diff --stat` 空 | ❌ 未觸發 |
| 3 | 現用基準與 `PROVENANCE.md` 之記載互相矛盾（如 git 歷史早於／晚於所載日期） | `PROVENANCE.md:8` 載產出日期 **2026-07-04**；該批檔案之首個 commit 於 git 歷史中**不早於**該日 ⇒ 未見矛盾 | ❌ 未觸發 |

🔒 **P1／P3 證偽⛔ 非停機條件**（施工單 §5 逐字）——P1 之部分證偽已於 §1-1 **如實列出**。

---

## §7 時序（機械證明）

```
$ git merge-base --is-ancestor 787e9d6 <commit C>
rc=0
```

---

## §4 不授權逐項自檢

| # | 項 | 狀態 |
|---|---|---|
| 1 | ⛔ 不得設 `WV_BAKE` | ✅（`env \| grep WV_BAKE` 空輸出） |
| 2 | ⛔ 不得修改 `verify/baselines/` 下任何一個檔（含凍存） | ✅（`git status` 該目錄無異動） |
| 3 | `app.py`／`verify/**` 一字不動 | ✅（`git diff --stat` 空） |
| 4 | ⛔ 不改錨值／不放寬閘／不改期望 FAIL 名單 | ✅ 未動 |
| 5 | ⛔ 不實作 `GB-53`〜`GB-57` | ✅ 未動 |
| 6 | ⛔ 不判定「該不該重烤」「何時重烤」 | ✅ 本報告全程未作該判定（§3 末已明載） |

---

## 異動清單

| 檔 | 異動 | 性質 |
|---|---|---|
| `docs/reports/W-G.8_D-2b-27C_D3重烤前置盤點結果.md` | 新增（本檔） | 文件 |

⛔ `app.py`／`verify/**` 零觸。⛔ `verify/baselines/` 零觸。

---

## 六、供 KL 裁決之三項事實（⛔ 非建議）

1. **P1 只對 12 個 CSV 成立**——`v3/`＋`wf/` 之 67 檔本即 harness 產出
   ⇒ `PROVENANCE.md` 所賴之「與 driver 非同源」性質，**對該 67 檔從未成立**。
2. **現狀重烤之覆蓋與需求相反**（§3 末）：烤得到的是**不過期且非同源**之上游 12 檔；
   烤不到的是**已過期且本即同源**之下游 67 檔。
3. **`E系列實測快照` 之產出者不可判定**——建議 `D-3` 動工前先補其 provenance 記載。
