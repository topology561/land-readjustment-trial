# 🧾 `W-G.9-170`　**CC 交接文**（新 session 接手用·`W-G.9-169R` 收束時出）

**出文窗**：CC（本 session context 逼近上限）
**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ 零登記

---

## 🛑 §零　**最要緊之一件事**（⛔ 讀完再動任何東西·**常規九**）

🔴 **`W-G.9-169R` 係<u>生產碼批</u>，已 commit 而<u>尚未 push</u>**——其 push 依 `W-G.9-169 §四 S-8`
逐字「⛔ **不得逕行 push·須請示 KL**」（常規一四項不齊：非零生產碼／`deletions > 0`／`app.py` blob 必變）。
CC 已請示、**待覆**。

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart` 出文時之 tip | **`a3c97aa0ada17389c465550e04d90388c346f670`**（＝ `W-G.9-168R`） |
| 本地已 commit 而**未 push** | **`6a4a0da9dd90d84deda17282e13bb4ab1ea149a9`**（＝ `W-G.9-169R`）＋ 本檔（`-170`） |
| 🔴 **該二 commit 之唯一存放處** | **本機 `/c/w169b`**（🛑 **本窗刻意<u>保留未刪</u>**·⛔ 非一般拋棄式 clone 之處置） |

🛑 **接手者之第一判斷**：`git ls-remote origin refs/heads/wip/s1-endpart` 之值——

- 若 ＝ **`6a4a0da` 或其後裔** ⇒ KL 已核可且已 push ⇒ 照 `§一` 正常開工。
- 若 ＝ **`a3c97aa`** ⇒ 🔴 **`-169R` 之成果<u>不在 origin</u>**。處置**依序**：
  1. `ls -d /c/w169b` ⇒ **在** ⇒ 於該 clone 內 `git log -1` 覆核為 `6a4a0da`（或其後裔），
     **俟 KL 核可後自該 clone `push`**；⛔ 在核可前不得 push、⛔ 不得刪該 clone。
  2. ⇒ **不在** ⇒ 🛑 **須向 KL 確認是否重做 `W-G.9-169`**；⛔ **不得假設其已落地**、
     ⛔ 不得據本檔 `§三` 之「已落地」逕行下一批。
- 其餘值（既非 `a3c97aa` 亦非 `6a4a0da` 之後裔）⇒ 🛑 **停機**上呈。

🔒 **本則之由**：`CLAUDE.md` 最高紀律「未 push 不得報收官」之對偶（**常規九 一**）——
**交接文亦不得把未 push 之物記為倉態**。
🔒 **常規九 三**：接手窗之開工閘 **⛔ 不得以本檔所載之 commit 為錨**，一律以 `ls-remote` 之實查值為準。

⚠️ **本檔入倉後本地 tip 將前進一格**（`6a4a0da` → 本檔之 commit）；上表之「未 push」二者仍成立。

> 🆕 **`W-G.9-169 收官` 之就地加註（`E-4`·`2026-08-29`·⛔ 未刪上文一字）**
> 🔒 **上文「未 push」之態已<u>失效</u>**——KL 逐字裁「`可push`」，四 commit 已於 `2026-08-29` 推送。
> `origin/wip/s1-endpart` ＝ **`acd50ebcdbc9de30f18fefb0216582d301d2cd47`**
> （`a3c97aa` → `6a4a0da` → `b9cb3cf` → `926a0de` → **`acd50eb`**·`left-right` ＝ `0 0`）。
> ⇒ 上表 `/c/w169b` 之「唯一存放處·🛑 ⛔ 不得刪除」之戒 **已解除**（`E-5`），該 clone 已刪除。
> 🛑 **惟 `常規九 三` 仍<u>全效</u>**：接手窗之開工閘 ⛔ 不得以本檔所載之 commit 為錨，
> 一律以 `git ls-remote` 之實查值為準。

---

## §一　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（態 ＝ `6a4a0da`：**`751`** 列／**`69552`** B／`sha16 5c013ba334464def`）。
   🔴 **末端已增**：`常規六`／`常規七`／`常規八`／`常規九`／`常規四（七）(八)(九)` ＋ `（七）補款一`／`補款二`。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （**`3632`** 列／**`258295`** B／`sha16 6a5cada2901a10e8`·`grep -c "^#"` ⇒ **`196`**）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**：`git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNN` ⇒
   `cd` ⇒ `git config core.autocrlf false` ＋ `user.name diag` ／ `user.email diag@local`
   ⇒ **對拍 `HEAD` 全 `40` 碼與 `app.py` blob**。
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、⛔ 不得放 scratchpad；跑畢刪除（🛑 **惟 `/c/w169b` ⛔ 不刪**·見 `§零`）。
   🔴 **若本批將做全文對拍或長跑**，clone 路徑須為 **`8` 字元**（`C:\wNNNa` 之形）——
   與既有期初 log 之產生路徑（`C:\w168a`／`C:\w169a`）**等長**（`VR-080 一`／`自誤 203` 之戒 `18`）。

---

## §二　倉態（態 ＝ 本地 `6a4a0da`·⛔ 未 push·CC 現查·量測時點 `2026-08-29`）

| 項 | 值 |
|---|---|
| 本地 tip | `6a4a0da9dd90d84deda17282e13bb4ab1ea149a9` |
| `origin` tip | `a3c97aa0ada17389c465550e04d90388c346f670` |
| 🔴 **`app.py` blob** | **`60b550b7f4d651cca5418fdf9807a0cc3248278d`**（`22915` 列／`1351414` B／`sha16 1b9483ba3a86f260`） |
| `CLAUDE.md` | `751`／`69552`／`5c013ba334464def` |
| `K-6` 正典 | `3632`／`258295`／`6a5cada2901a10e8` |

🔴 **`app.py` 於本波已<u>三度</u>變更**：`b4cc3083…` →（`-161`）→ `8daca97f…` →（`-163`）→
`f285b91c…` →（**`-169`**）→ **`60b550b7…`**。
🛑 **`-169` 係本波首個<u>有輸出後果</u>之生產碼批**（`16` 格）。

> 🆕 **`W-G.9-169 收官` 之就地加註（`E-4`·`2026-08-29`·⛔ 未刪上文一字）**
> ① 🔒 **上表 `app.py` blob `60b550b7…`／`22915` 列／`1351414` B 之適用態，<u>限於 `6a4a0da`</u>。**
> ② 🔒 **推送後之實值（`origin` tip `acd50eb…`）＝ blob
>    `68f7bfab610ec78688b9d7322ae4bc2aac74493e`／`22922` 列／`1352132` B／`sha16 7c290b6f55f83a24`。**
> ③ 該差 ＝ **`W-5` 之 `+7 / -0` 純註解**（於 `_proj_pop_filter` 之 `no_ghost` 原子定義處），
>    入倉於 **`926a0de`（`補正①`）**，並由 **`acd50eb`（`補正②` `V-2`）之 AST 對拍**證**零行為變更**：
>    二端 `ast.dump(ast.parse(src), annotate_fields=True, include_attributes=False)` 之 `sha256` 皆
>    **`59d550abf6eac68fb1a11748304eac492986e1d3ad15a79f3c482231f07caf74`**（長度皆 `2843204`）；
>    雙向判別力齊備（改可執行常數 ⇒ 轉紅／僅插一列註解 ⇒ 不轉紅）。
>    ⚠️ **收官單 `E-4 ③` 稱「`補正② W-5`」係筆誤**——`W-5` 係 `補正①` 之預測項、其補記入 `926a0de`；
>    `補正②` 所辦者為該 `+7` 列之 **AST 零行為證**。逐字照實更正、⛔ 不改收官單一字。
> ⚠️ ⇒ **`app.py` 於本波已<u>四</u>度變更**（承上文「三度」）：… → `60b550b7…` →（`W-5`）→ **`68f7bfab…`**。
> 🔒 **`Z-1`〜`Z-5` 之錨（繫於 `60b550b7…`）對推送之物<u>仍有效</u>**——該 `+7` 列為純註解、零行為變更
> ⇒ **⛔ 無須重跑 `run_all`**。

### 🔒 取號現查（**常規四（八）** 定義列式 ＋ **逐格三數框**·自誤 `204` 之施行式）

母體 ＝ **全倉 `.md`**（`rglob("*.md")`·排除 `.git`）⇒ **`447`** 檔；態 ＝ 本地 `6a4a0da`。

| 類 | 定義列式（逐字） | 相異號 | **MAX** | 錨定框：列數／次數／檔數 |
|---|---|---|---|---|
| `docs/reports` 之 `W-G.9-NNN` | `^docs/reports/W-G\.9-(\d{1,4})` | `120` | **`169`** | `W-G.9-170` ＝ `0`／`0`／`0`（對照 `W-G.9-169` ＝ `16`／`17`／`5`） |
| `VR` | `` ^##\s*`VR-([0-9]{1,4})` `` | `66` | **`80`** | — |
| `GB` | 三框聯集（母體 ＝ `W-G.4` 登記表**單檔**） | `114` | **`116`**（缺號 `[12, 87]`·算術自檢 `116−2==114` True） | — |
| 自誤 | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `169` | **`204`** | — |
| 常規 | `^\*\*常規(.)`（**寬框**） | 命中 `12` 列 | **九** | 🔴 窄框 `[一-十]` **⛔ 不含「四」**（`U+56DB > 十 U+5341`）⇒ 靜默漏抓 |

🛑 **取號之三戒**（⛔ 缺一不可）：
1. **定義列式**取 MAX（常規四（八））；🛑 裸框⛔ 不得用於取號。
2. **錨定框** `(?<![0-9\-])<完整號>(?![0-9])` 查意指占用（補款一）；⛔ 不得用號之尾段。
3. **二形並取**（補款二）＋ **三數框**（命中列數／出現次數／檔數·自誤 `204` 施行式）；
   🔴 **對照組得 `0` 者一律先判<u>量測器紅</u>**。

---

## §三　本波之位置（`-164` 〜 `-169`）

| 單 | commit | 要旨 | 狀態 |
|---|---|---|---|
| `-164` | `0e4b508` | CC 交接文 | ✅ 在倉 |
| `-165R` | `001f60a` | 🛑 **停機**：第四類正規化入倉，惟 `M-1` 第二列 ＝ `2` ≠ `0`；`GB-112` | ✅ 在倉 |
| `-166R` | `6b77b4a` | `VR-078`（具名例外五判準）＋ `GB-113` ＋ 🔓 **`GB-105` 解除** ＋ 自誤 `199` | ✅ 在倉 |
| `-167R` | `a98a99f` | `VR-074` **前置量測** ＋ **預測 diff 集合**（`16` 格）＋ `GB-114` ＋ 自誤 `200` | ✅ 在倉 |
| `-168R` | `a3c97aa` | `k6step0` 補量（`A-5` 二項齊備）＋ **期初錨** log ＋ `VR-079`／`GB-115`／自誤 `201` | ✅ 在倉（＝ **`origin` tip**） |
| 🔴 `-169R` | **`6a4a0da`** | 🛑 **`VR-074` 落地**（生產碼·`16` 格）＋ `VR-080`／`GB-116`／自誤 `202`-`204` | 🔴 **見 `§零`（未 push）** |

### 🔑 `-167`〜`-169` 之結論鏈（**下一單之全部前提**）

1. 🔒 **`VR-074` 已落地（`-169R`）**：ghost（`K-9-19 一` **三判準合取**）**⛔ 不入投影序母體**。
   碼面 ＝ `_proj_pop_ghost3`（**唯一定義處**）／`_proj_pop_key_guard`（loud 守護）／
   `_proj_pop_filter` 之**複合原子名**（`identity+no_ghost` 等·`+` 拆解逐段套用）／
   四處實參同步／`_WF_NS_NAMES` 補列 `_proj_pop_ghost3`。
   🛑 `app:k6step0/_ordered` **⛔ 未加濾**、改加 **loud 斷言**（保住 `_name` ＝ 合併宗暫編地號·**有土地後果**）。
2. 🔴 **輸出後果 ＝ `16` 格**（四份對拍靶 × `4` 格·皆 `原位次(投影序)`）：
   `R1` `628-37(1)` `3→2`／`628-36(1)` `4→3`／`628(5)` `6→5`；`R4` `628-1(1)` `3→2`。
   🔒 **winner 零改變**（二態直接對拍·指配 rows 全欄逐格相異 `0`）⇒ **⛔ 無配地結果之變動**。
3. 🔒 **四對拍靶之違規總數 `68/68/60/60` → `72/72/64/64`**——係**已紅之閘增列**（准紅碼），
   **⛔ 非由綠轉紅**。⛔ **未重產任何 baseline**（`VR-079 三`：屬 KL 已裁之**波末重烤**，與 `GB-111`／`GB-113` 同批）。
4. 🔒 **`VR-080`**（`-169` 所立）：`B-3` 之判準 ＝「`B` 類（無法歸因）＝ `0`」＋「四類正規化後 ＝ `0`」；
   `C-3` 之判準 ＝ 綁**值層**（剝 `baseline=`／`got=` 之外層單引號後比較）、⛔ 不綁**呈現層**。
5. 🔒 **`Z` 骨架**（`VR-080 三`·凡有輸出後果之生產碼批）：**受詞層**（不截斷之逐格重算·期末−期初恰為
   預先登記之集合、期初−期末 ＝ ∅）＋ **通道層**（全文對拍之每一列須落入出單前已宣告之封閉集）**二層並列**。

---

## §四　🛑 下一單之受詞（由發單側落筆）

1. 🔴 **`W-G.9-169R` 之 push**（俟 KL 核可）——**⛔ CC 不自辦**。核可後自 `/c/w169b` push 並三重驗證。
2. ⚠️ **`L-4` 之一處偏離待裁**（`-169R §二`）：原子名採 `no_ghost`（甲案示例作 `no_ghost3`）；
   理由 ＝ `L-3` 已令 `no_ghost` 換源且「名可留」，另立將使**二原子同義**、違甲案之理由。若不採，**一行可改**。
3. 🔒 **波末重烤**（baseline 九份 ＋ 凍存名單 ＋ `GB-111`／`GB-113`）——**須 KL 決時點**。
4. 🔒 **待落地清單**（`CLAUDE.md` 之「待落地清單之更正二」）：`K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17`
   重排／遞補（**幾何影響批·須另開新窗**）→ `K-9-12` 矩形容納（**同批**）→ `K-6-A2` 剩餘六項 →
   `K-9-22` 落地 → `K-6-B` 段一〜段四 → `K-6 §四` 合併述詞置換 → 波末批 → 泛化波。

---

## §五　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104` 之 loud 斷言（`_block_strip` 雙線⛔ 無交叉閘） | ⬜ 未辦 |
| `2` | `GB-107`（`wg960_z_selfcheck.py:12` 寫死絕對路徑·該 worktree 已不存在） | ⬜ 未修 |
| `3` | `GB-108`（`K-9-23-a` 八格表對「`σ` 由形心導出」⛔ 無判別力） | ⬜ 未辦 |
| `4` | `GB-110`（八格表對閘一之**關係詞** `>` vs `>=` ⛔ 無判別力） | ⬜ 未解除 |
| `5` | `GB-111`（凍存期望 FAIL 名單已失效·**須 KL 決重產時點**） | ⬜ |
| `6` | `GB-112`（`_WF_NS_NAMES` 反向閘寄生於既已 FAIL 之 `try`·**死碼**） | ⬜ 未修 |
| `7` | `GB-113`（`traceback.format_exc()[-700:]` 定長字元尾切致偽差） | ⬜ 未修 |
| `8` | `GB-114`（`無串聯` 閘之 `skip_cols` 指向**不存在**之欄名 ⇒ 豁免失效） | ⬜ 未修 |
| `9` | `GB-115`（驗收通道之 `12` 列**顯示上限**·驗收力阻塞） | ⬜ 未修 |
| `10` | 🆕 `GB-116`（同一正典述詞於倉內 **`≥ 4`** 處各自實作·⛔ 無單一定義處） | ⬜ 只登記 ＋ 已加等價自檢 |
| `11` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `12` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `13` | `K-9-22` 落地／`VR-074` 之他層／`VR-075` 落地 | ⬜ |

---

## §六　🔑 本波所立之家法與陷阱（🛑 接手者必讀）

1. 🛑 **`Z` 骨架**（`VR-080 三`）：有輸出後果之批，**受詞層 ＋ 通道層二層並列**，⛔ 不得只取其一。
2. 🛑 **判準⛔ 不得綁「代理」**（`VR-080 二`／自誤 `202`）：欲證「序與內容相同」而寫「渲染後逐位相同」，
   於二端型別不同源時**必假紅**。判準須綁**值層**，並同格具名該正規化之**唯一**規則與其**有作用之證**。
3. 🛑 **長跑之環境相依項須出單前具名**（自誤 `203` 之**戒 `18``）：工作區路徑長度／行號／時間戳／編碼／行尾。
   🔴 **未正規化之跨 clone `diff` ＝ `0` 於構造上不可達**（二 log 各含 `12` 列 clone 路徑）。
4. 🛑 **顯示上限**（`GB-115`）：`run_verification.py` 每閘只印 **`12`** 列違規；
   凡受詞為「**哪幾格**變了」者，該通道**恆無判別力** ⇒ 須以**不截斷之逐格重算**承載（`probe_WG9168_viol_full.py`）。
5. 🛑 **豁免清單須現查其元素之命中數**（`GB-114`／自誤 `200`）：命中 `0` 者 ⇒ **該豁免無效**，⛔ 不得據以推論。
6. 🛑 **取號之三數框**（自誤 `204` 施行式）：命中列數／出現次數／檔數 **三數同格**；缺一即視為⛔ 未標框。
   🔴 **同形已四度**（`GB-112` `7`／`8`；`VR-078` `15`／`16`；`GB` 式B `25`／`26`；`對拍靶` `16`／`40`）。
7. 🛑 **`in` vs `==` 之子字串偽陽**：`'對拍靶' in 角色` 會咬中 `凍存·⛔ 非對拍靶` ⇒ 得 `40`（真值 `16`）。
8. 🛑 **欄名⛔ 不得以關鍵字猜測**：`Z-4` 之 winner 欄真名為 **`【左/右】第1宗指配`**——
   以 `選中`／`winner`／`地號` 為框**漏抓**，該結論當場作廢，改以**直接二態對拍**證。
9. 🛑 **凡於 `verify/**` 新增 `ns["X"]` 之消費，必須<u>同批</u>把 `X` 加入 `app.py` 之 `_WF_NS_NAMES`**
   ——否則 app 生產路徑必 `KeyError`。🔴 **同族已第四度**（`W-8`／`-4b`／`-161`／**`-169`**）。
10. 🛑 **⛔ 不得為使預測轉符而改探針／濾式／容差／框／正規化規則**——**其不過本身即該批之產出**。
11. 🛑 **層疊之「濾掉 `0` 列」⛔ 不等於層疊失效**：須先判**受詞是否在射程內**，再以**正／負對照**自證生效
    （`-168R M-A` 之自捕）。
12. 🛑 **`shell` 之反引號與 `\\` 會吃字**：`python -c "…"` 內含反引號 ⇒ **命令替換**吞字；
    引號式 heredoc 亦曾把 `\\` 吃成 `\`。⇒ 交付塊／正規式一律**由檔案管線產生**或用 Write 工具落檔。

---

## §七　已知 harness 限制

- 🔴 **`run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止** ⇒ `wf_f0`〜`f4` **主路徑從不執行**。
- 🔴 **`app.py:main()` 內之敘述從不被 `run_all` 執行**（`CLAUDE.md` 逐字）。
- 🔒 **`run_all` 於基座即 `FAIL`**（**准紅碼**）：`RESULT: FAIL`／`EXIT=1`；`-169R` 期末亦然。
- 🔒 **`_PROJ_POP_DECL` `14` tag 之執行次數**（`WV_PROJ_POP_COUNT`·`-168R`／`-169R` 二度實測相同）：
  `18`／`0`／`18`／`0`／`102`／`18`／`0`／`0`／`0`／`0`／`0`／`2`／`22`／**`80`**；合計 `260`。
  **`7` 個為 `0`** ＝ `main()` 內 `2`（結構性不可達）＋ trunk A 連坐 `5`。
- ⚠️ `run_all` 會改寫 `verify/out/**` 之**已追蹤** log（`GB-94`·實測 `3` 檔）⇒ **須於拋棄式 clone 內跑**，
  **入倉自另一未跑長跑之 clone 為之**。

---

## §八　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**：`push-before-shipping-claim`（🔒 **未 push 不得報收官**·本檔 `§零` 之上位）／
`grep-multibyte-charclass-trap`（量測器靜默給錯數之諸面·含 heredoc 吃 `\\`、`find` 命中他處毀檔）／
`run-all-side-effects`／`harness-worktree-crlf-trap`／`wg9161-projpop-landed`（本波主線）／
`wg9143-handoff-entry`／`app-vs-harness-stepg-divergence`。

---

## §九　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit
  （**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值。
- 🔒 **拋棄式 clone 跑畢整份刪除**並於回報中確認——🛑 **惟 `/c/w169b` ⛔ 不刪**（`§零`）。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push（① 零生產碼 ② `deletions = 0` 或既有檔純附加
  ③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠）。
  🛑 **惟施工單另令「須請示」者，以單為準**；🛑 **生產碼批⛔ 不得逕行**。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`
  （期望 `rc=0`·`stderr` `0` B·逐字「結論：✅ 全部成立」·受檢 **`77`** 筆）。
- 🔒 **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
