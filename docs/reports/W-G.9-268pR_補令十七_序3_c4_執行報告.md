# `W-G.9-268′` 補令十七　序 `3`（`c4`）—— 執行報告

> 🔒 **零生產碼**·**零號鑄造**。交付物 ＝
> `docs/reports/W-G.9-268pR_c4_起算垂線錨點更正_逐宗差異表.md`（**域語言**·呈 KL·⛔ 求放行）。

---

## §零　三軸

| 軸 | 值 |
|---|---|
| `(1)` 來源 | 主線 `a6d974738d354cc5582831a0b3a5e29055e54293`；二態實跑之受測物 ＝ `7eaa485:app.py`（**pre-`c3`·旗標尚存**）|
| `(2)` 母體 | 逐節分列（`got_G值_退縮{0m,3.5m}` 之宗；生產碼 `34` 檔）|
| `(3)` 粒度框 | **宗**（逐宗）／**街廓**（守恆）／**bytes** |

---

## §一　差異表之**取數法**（⛔ 以 `c3` vs 其父態為之）

🔴 **`c3` 之 `Q-1` 已證其輸出與父態<u>逐位相同</u>**（`20／20`）
⇒ **`c3` 本身⛔ 生任何土地後果**——其為 `contract`（移除旗標），錨之語意於 `c2` 即已生效。
⇒ 🔒 **土地後果之受詞係<u>錨點更正本身</u>**（`c1` 所引入者），
其唯一可量之法係以**尚存旗標**之 `app.py` 跑**二態**：

| 態 | 取法 | 結果 |
|---|---|---|
| **改前**（原錨點·名目終點） | `7eaa485:app.py` ＋ 旗標 `0` | `rc = 1`·**提早中止**·只產 `_partial`·`stdout` `29106` B |
| **改後**（更正後·幾何終點） | `7eaa485:app.py` ＋ 旗標 `1` | `rc = 1`·**跑完**·`18` 檔·`stdout` `168276` B |

🔒 **二態之受測物為<u>同一份 `app.py`</u>**（`9873b168278fbc08`）⇒ **差異純由錨點之取法而生**。
🔒 **坑 `5` 攔法**：每態跑前**先清** `got_*`、逐態各自快照、**出艙各態實際寫出之檔名**。
🔒 **坑 `22` 款 `3`**：`app.py` 之還原以**換檔前之原始位元組**為基準
——還原後 `sha256` ＝ `b74fd07c3c86a914…`（＝ `c3`）·`git status` 對 `app.py` **乾淨**。

### 一-1　🔴 **可比母體之限制**（照實·⛔ 掩去）

**改前之態跑不完** ⇒ 逐宗對照之母體 ＝ **二態共同之宗**：

| 情境 | 改前 | 改後 | **共同鍵** | 改後獨有（⛔ 計入差異表）|
|---|---|---|---|---|
| `0m` | `21` 宗（`R1`,`R2`）| `59` 宗 | **`21`** | `38` |
| `3.5m` | `47` 宗（`R1`,`R2`,`R4`,`R6`,`R5`）| `60` 宗 | **`47`** | `13` |

🛑 「改後獨有」之宗**非「改動所致之變化」**，而係**改前根本算不出來** ⇒ **⛔ 得計入差異表**。

---

## §二　必載三項之核對（補令十六 `§三`）

### 二-1　必載 `1`：`表三` 之 `ΣΔ` 與值域（`min`／`max` **各自具名其宗與歸戶**）

| 情境 | `ΣΔ`（帶號） | `Σ|Δ|`（絕對值） | `min` | `max` |
|---|---|---|---|---|
| `0m` | **`+3.2900`** | `10.4700` | **`−3.5900`** ⇐ `628-42(1)`／**`G011`**（`R2`）| **`+3.5100`** ⇐ `628-39(1)`／**`G002`**（`R2`）|
| `3.5m` | **`+4.3900`** | `6.3700` | **`−0.9900`** ⇐ `628-53(2)`／**`G025`**（`R5`）| **`+3.6000`** ⇐ `628-7(2)`／**`G022`**（`R5`）|

🔑 **與單 `§三` 所載之【單】值逐位相符** ⇒ **該四值自本批起為【倉】**：
單載 `0m` `−3.5900`〜`+3.5100`（`min ＝ 628-42(1)`／`G011`；`max ＝ 628-39(1)`）；
`3.5m` `−0.9900`〜`+3.6000`（`min ＝ 628-53(2)`／`G025`；`max ＝ 628-7(2)`）。
🩸 **CC 所補之二歸戶**（單未載）：`628-39(1)` ⇒ **`G002`**；`628-7(2)` ⇒ **`G022`**
——`自誤 343` 之通則令 `min`／`max` **各自**具名其宗**與歸戶**，故補齊。
🔒 **帶號與絕對值合計並列**（`CLAUDE.md` 之令）：`ΣΔ` 抓符號錯與抵銷，`Σ|Δ|` 抓漏項與值錯。

### 二-2　必載 `2`：二宗近乎零之配地（**逐宗單列**）

| 宗／歸戶 | 情境 | 配地面積 | 宗地寬度 | 畸零地註記（改前／改後）|
|---|---|---|---|---|
| `628-42(1)`／`G011`（`R2`）| **`0m`** | `3.82` → **`0.23`** | `0.76` → **`0.18`** | **皆**「移出／第 1 調配順位」|
| 同上 | `3.5m` | `4.77` → `4.77`（未變）| `0.11` → `0.11`（未變）| **皆**同上 |
| `628-53(2)`／`G025`（`R5`）| `0m` | 🛑 **改前無此宗** ⇒ **不可比** | — | — |
| 同上 | **`3.5m`** | `0.99` → **`0.00`** | `0.39` → **`0.02`** | **皆**同上 |

🔑 **與單所載逐位相符** ⇒ 轉為【倉】。
🩸 **CC 之照實併呈（單未載·⛔ 頂替）**
`(a)` **單所列之二宗，其值各出自<u>不同情境</u>**——`628-42(1)` 之 `3.82 → 0.23`／寬 `0.76 → 0.18` 係
**退縮 `0` 公尺**；`628-53(2)` 之 `0.99 → 0.00`／寬 `0.39 → 0.02` 係**退縮 `3.5` 公尺**。
單併列而未分情境，**易被讀為二者於兩情境皆然**；交付表已**逐情境分列**。
`(b)` **`628-42(1)` 於 `3.5m` 之配地亦近乎零**（`4.77 ㎡`·寬 **`0.11 m`**）而**⛔ 因本次更正而變**
——其為**既有**之近零配地，**⛔ 本次更正所致**；已於交付表列出以免漏。
`(c)` **`628-53(2)` 於 `0m` 不可比**（改前中止）——**具名為不可比**，⛔ 略去。

### 二-3　必載 `3`：`表二` 守恆（`Σ池` ⇐ **獨立量測**）

🔒 **取數端 ＝ `推進側別 == '抵費地'` 之 `幾何面積(㎡)`**，
**⛔ `街廓面積 − ΣG`**（後者使殘差**恆為零**·`自誤 245` 族·坑 `19`）。
🔑 **其獨立性之證 ＝ 殘差<u>非零</u>**：`0m` `+0.0000`〜`+0.0300`；`3.5m` `−0.0300`〜`+0.0200`
——若以倒算式取之，**六街廓之殘差必皆恰為 `0`**。**⇒ 本帳非恆真。**
🔒 **殘差全部落在 `±0.03 ㎡` 內**，係各宗面積 2dp 捨入之累積，**非帳不平**。
🛑 **改前之態⛔ 列守恆帳**（其跑不完、配地未全部產生 ⇒ 該帳無意義）。

### 二-4　域語言之自檢（補令十六 `§三` 末：表內⛔ 出現程式術語、變數名、行號、旗標名）

**受檢** ＝ 交付表全文。**命中須皆 `0`**：
旗標名 `0`／`_wg9268p` `0`／`cum_S` `0`／`s_geo` `0`／`run_verification` `0`／
`app.py` `0`／`harvest` `0`／`推進側別` `0`／`rc =` `0`／`probe_` `0` ✅
🔑 **對照組（必命中·證器非紅）**：`配地面積` **`11`**／`抵費地` **`4`**／`歸戶` **`10`** ✅

---

## §三　`GB-67` 之末端追加

已落於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（**⛔ 追改其原文與歷次加註一字**）：
載**併線之 `commit`**（`363602b…`·並為 `GB-159` 現行有效期間之錨）、`app.py` 三值、
`c1`→`c2`→`c3` 三段之落地形、`c3` 三閘之結果、**土地後果之指向與其母體限制**，
並明文**⛔ 解除 `GB-159`**、**⛔ 解除病一**、**⛔ 變更任何依賴序**。

---

## §四　收工閘

| 閘 | 結果 |
|---|---|
| 二態實跑 | 改前 `rc=1`·中止·`8` 檔（含 `_partial`）／改後 `rc=1`·跑完·`18` 檔 |
| 受測物同一 | 二態皆 `7eaa485:app.py`（`9873b168278fbc08`）⇒ 差異純由錨點取法而生 ✅ |
| `app.py` 還原 | `b74fd07c3c86a914`（＝ `c3`）·`git status` 乾淨 ✅ |
| 必載 `1`／`2` 之值 | 與單所載**逐位相符** ⇒ 【單】轉【倉】✅；二歸戶（`G002`／`G022`）由 CC 補齊 |
| 必載 `3` 之獨立性 | 殘差**非零** ⇒ 該帳**非恆真** ✅ |
| 域語言 | 程式術語逐項 `0`；對照組非零 ⇒ 器非紅 ✅ |
| `append-only` | `GB` 簿 `775093 → 778303` B·**嚴格前綴** ∧ `CR = 0` ✅ |
| 生產碼判 | 空輸出 ⇒ **零生產碼** ✅ |

🛑 **序 `4`（差異表呈 KL）＝ 交付·⛔ 求放行**；其處置屬 KL。
🔒 **`c4` 辦畢 ⇒ `W-G.9-268′`（`GB-67` 病二）全批結案**（單 `§六` 逐字）。
