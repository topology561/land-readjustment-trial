# W-G.9-27：非 `forced` 之 B 選槽起算點校回 `0`

**批號**：`W-G.9-27`（現查 `git grep -c "W-G.9-27" | wc -l` ⇒ **0**；判別力自檢：同式改 `W-G.9-26` ⇒ **2** ⇒ 該式非恆 0）⇒ 未占用·⛔ 未改任何既有號。
**基座**：`wg925-handoff` @ `609348a`（＝`origin/wip/s1-endpart`）
**性質**：🔴 **本波首次動生產碼**——射程 ＝ `verify/stepg_pipeline.py` **兩行**。
**結論**：**`J0`〜`J4` 全部成立**；停機條件 **`R-1`〜`R-7` 全部未觸發**。
⛔ **本批未更新任何凍存物／baseline**（§0 鐵律·待獨立批次）。

---

## §1 甲組：改前基線（🔴 全部跑完於動碼**之前**）

### 甲-1 `verify/fixture_e2e_termination.py`

`rc=0`·**夾具 PASS**。名目：凍存 3／現況 3·逐項相同；類 I ＝ 3（raw 即相同 3）／類 II ＝ 0／類 III ＝ 0；竄改自檢**具鑑別力**（竄改態🔴 ∧ 還原態✅）。

**終止點原文**（三支逐字相同·出處 `verify/out/WG99_端到端複本_終止點凍存_投影.txt`）：

```
File "verify/stepg_pipeline.py", line 1362, in run_step_g
RuntimeError: 🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）
```

### 甲-2 `run_all` ＋ 對帳器

`rc=1`（**准紅碼**·判準＝「與凍存名單逐項相同」、⛔ 非全綠）。

| 量 | 值 |
|---|---|
| `[3/3]` 名目 | **12 `✅ PASS`／22 `🔴 FAIL`**（判準＝含該標記之行·⚠️ **非行首**，以行首計得偽 0） |
| 對帳器 | 名目：凍存 **22**／現況 **22**·**逐項相同**；類 I ＝ **22**（raw 即相同 14）／類 II ＝ **0**／類 III ＝ **0** ⇒ ✅ **對帳 PASS** |
| `[1/3]` 非零項（🔴 全集·⛔ 非窗抽） | 禁寫死絕對路徑閘 **1 命中**／`fixture_corner_range_k8.py` `rc=1`／**`E 系列實測快照閘` `rc=1`**（其 `RuntimeError` 即 `R1` 結構閘 `97.18≠100.02`） |

🔒 兩情境之結構閘原文（自本次 log 現算）：`0m` `理論@k*=97.18 ≠ 實跑=100.02`／`3.5m` `理論@k*=96.30 ≠ 實跑=98.75` ——與施工單 `J2` 之錨**逐字相符**。

### 甲-3 `W-G G.1 接線層 ctx-builder 同源`（`_ctx["0m"]`）

**現為紅·且為死閘**——正面坐實（⛔ 非由構造推定）：

- 該名目以**短名**（`except` 路徑之名）列於凍存名單，原因全文 ＝
  `[G.1] '0m'` ＋ `KeyError: '0m'` @ `run_verification.py:1300`。
- 碼面：`try:` 之**首敘述**即 `_nat = _ctx["0m"]`；W-8 反向閘之差集檢查在**其後**
  （字樣錨：`引擎消費但 _WF_NS_NAMES 漏列`）⇒ 首行一拋，整組陪葬（考古 65／70 之形狀）。

### 甲-4 `verify/baselines/`（19 項·🔴 本批**已讀逐檔內容**）

`W-G.9-23` 具名「無從判定」係因**未讀**；本批補讀。母體 ＝ 該目錄之 `os.listdir` ⇒ **19 項**（13 檔 ＋ 6 目錄；目錄內另 44 項亦已列）。

**關鍵字掃描**（母體＝該目錄全樹·判準＝內容含該字樣·**判別力自檢**：必然命中之 `街廓` ⇒ **245 檔**）：

| 字樣 | 命中 |
|---|---|
| `終止點`／`到達`／`街廓序`／`b 值`／`選槽` | **0 檔**（各） |
| **`k*`** | 🔴 **28 檔**——其中**現用**（非 `PRE-*` 凍存）**9 檔** |

**受詞含 `k*` 且為本批所改 `b` 之直接下游**（欄名 `ΣRw_L理論@k*(%)`）：

| 檔 | `R1` 之（`k*`／理論@k\*／實跑） |
|---|---|
| `verify/baselines/v3/W-D.2 §3 滑池槽診斷_退縮0m.csv` | `2`／**97.18**／97.18 |
| `verify/baselines/v3/W-D.2 §3 滑池槽診斷_退縮3.5m.csv` | `1`／**96.36**／96.38 |
| `verify/baselines/wf/f0/F.0_滑池槽診斷_退縮0m.csv` | `2`／**97.18**／97.18 |
| `verify/baselines/wf/f0/F.0_滑池槽診斷_退縮3.5m.csv` | `1`／**96.36**／96.38 |

⚠️ **一項受詞落差（⛔ 不混談）**：baseline 之 `ΣRw_L實跑` ＝ `97.18`／`96.38`，而甲-1 凍存與 `run_all` 現況所載改前實跑 ＝ `100.02`／`98.75` ⇒ **baseline 為凍存值、現碼已漂**（即准紅碼狀態），**二者非同一時點**。

---

## §2 乙組：動碼（逐字 diff）

**錨唯一性**（判準＝**整行**·⚠️ 該檔工作樹為 **CRLF**（`wc -c` − blob `wc -c` ＝ `1443` ＝ 行數）⇒ 比對**先正規化行尾**）：錨 A ⇒ **1**／錨 B ⇒ **1**。
**判別力自檢**：改用片語 `_mp_base_W0` ⇒ **4 行**／`_b_L0` ⇒ **2 行** ⇒ 證「整行」≠「片語」、該檢查非恆為 1。

**前置現查（⛔ 禁假設）**：`_fo_left`／`_fo_right` 定於**同一函式 `_solve_one`** 之**函式體層**（縮排 8·⛔ 非條件內），目標行在其後之縮排 12 ⇒ **必先繫結**。`_mp_base_W0` 回傳 `float`，且其退化分支本即回 `0.0` ⇒ **型別相容、與既有退化值同形**。

```diff
@@ -823,8 +823,11 @@ def run_step_g(ns, fake_st, cb, cad, snapshot, param_rows, build_parcels,
                  if _smax_o is not None:
                      _end_pt_o = corner_pt + _smax_o * _np_d.asarray(d_hat, dtype=float)
                      _dhr_o = -_np_d.asarray(d_hat, dtype=float)
-             _b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block)
-             _b_R0 = _mp_base_W0(_end_pt_o, _right_buffer_S, _dhr_o, _side_mid_right, allocation_dir_block)
+             # 🔒 K-9-5-12（五）-1「首宗起點一律為 0」＋ K-9-5-13 裁定二「起算一律用真正的地界」
+             #    ⇒ 非 forced 之選槽起算點校回 0（施工單 W-G.9-27·影響估算見 W-G.9-23）。
+             #    ⛔ forced 側維持原式——其目標為抵費地遠側界，卡「保留區之表示法」（W-G.9-19 D3）。
+             _b_L0 = _mp_base_W0(corner_pt, _left_buffer_S, d_hat, _side_mid_left, allocation_dir_block) if _fo_left else 0.0
+             _b_R0 = _mp_base_W0(_end_pt_o, _right_buffer_S, _dhr_o, _side_mid_right, allocation_dir_block) if _fo_right else 0.0
              _slot_res = _select_pool_slot(
```

**numstat** ＝ `5　2`（＝事前登記）⇒ **`R-7` 未觸發**。`py_compile` **rc=0**。行數 1443 → 1446；行尾 `\r\n` 保留。

---

## §3 丙組：`J0`〜`J4` 逐項判（🔴 `J0` 先判·紀律第 4 條）

### `J0` ✅ 成立——**自檢下放一層**

量測器 ＝ `verify/probes/probe_WG923_bslot_only.py`（frame 內省讀 `_select_pool_slot` 之**入參**）⇒ 其「改前」欄即**生產現值**。

| 情境 | 街廓 | `forced`(L/R) | `buf_L`(生產) | **`b_L`（本批生產實測）** | `W-G.9-23` 所載（改前） |
|---|---|---|---:|---:|---:|
| `0m` | `R1` | False/False | 0.0000 | **0.0000** | 0.1619 |
| `0m` | `R2` | False/False | 0.0000 | **0.0000** | −1.6939 |
| `3.5m` | `R1` | False/False | 0.0000 | **0.0000** | 0.1619 |
| `3.5m` | `R2` | **True**/False | 8.6959 | **6.9803** | 6.9803 |

🔒 **同一張表即帶判別力**：非 `forced` 三格全歸 `0` **且** `forced` 那格停在 `6.9803` ⇒ **射程正確、⛔ 非全域歸零**。

### `J1` ✅ 成立 ⇒ `R-3` 未觸發
`3.5m R2/left`（`forced`）之 `b` ＝ `6.9803`，與錨**逐位相符**、**未變**。

### `J2` ✅ 成立 ⇒ `R-2` 未觸發

🔴 **⛔ 不採探針之判**——具名對照：

| 判準器 | 判 | 其受詞 |
|---|---|---|
| 探針 `Q-2` | `🛑 R-2 觸發` | 🔴 **`None`**·**其自身同段印「⚠️ 無從判定——停於非該閘之處」** |
| **甲-1 夾具**（改前 log 封存於動碼**之前**·改後為真生產跑） | ✅ **`R1` 結構閘不再拋** | 三支端到端複本之 `raise` 幀 ＋ 訊息 |

**探針失效之成因（⛔ 非推測）**：其「改前」欄**取自生產現值**，而生產已由本批改掉 ⇒ 「改前」＝「改後」⇒ `Q-2` 之受詞（「改前拋於哪個街廓」）為 `None`。
⚠️ 該探針把**無從判定**升級為**判定為偽**（`R-2 觸發`），係**紀律第 3 條**所禁之共用出艙碼；⛔ 本批未改該探針（射程外）、僅具名。

**權威量測（甲-1 改後·類 III 三項）**：

| | 凍存（改前） | 現況（改後） |
|---|---|---|
| `raise` 幀 | `stepg_pipeline.py:1362` | `stepg_pipeline.py:995` |
| 訊息 | 結構閘 理論＝實跑 破：街廓 **R1** left 理論@k\*=97.18 ≠ 實跑=100.02 | 街廓 **R2** 抵費地計算失敗：②-宗 圍堵閘破 45.9766 > 3.1129 |

🔒 **`:995` 係行號位移、⛔ 非新 raise**——逐字對拍：新 `:995` ＝ 舊 `:992`（本批加 3 行註解之 **+3**）；結構閘本體仍在新 `:1365` ＝ 舊 `:1362`，**只是不再觸發**。

### `J3` ✅ 成立 ⇒ `R-1` 未觸發
`k*`：`0m` `R1`=2·`R2`=8／`3.5m` `R1`=1·`R2`=8 ——**四格全與錨相符、零改變**。
**自檢下放一層**：`ΣRw_L@k*` 隨 `b` 得 **9 個相異值**（2／2 街廓）⇒ **替換確實抵達計算**（⛔ 非探針沒接上）。

### `J4` ✅ 成立 ⇒ `R-4` 未觸發
到達街廓序 ＝ **`['R1','R2']`**；停點**逐字符合兩錨**：
`0m` ⇒ `②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 3.1129`；
`3.5m` ⇒ `結構閘 telescoping 破：街廓 R2 left 側 ΣRw_實跑=100.02 ≠ 44.41（Δ=55.606）`。

⚠️ **射程**：`R3`〜`R6` **從未進入** ⇒ ⛔ 對其**不作任何宣稱**；具名為**無從判定**、⛔ **非**判定為偽。

---

## §5 丁組：轉紅之逐項處置（⛔ 三碼不共用·⛔ 本批不更新）

| # | 受詞 | 改前 | 改後 | **碼** |
|---|---|---|---|---|
| 甲-1 | 三支端到端複本之終止點 | PASS·類 III ＝ 0 | **FAIL**·類 III ＝ **3** | **`EXPECTED_RED`** |
| 甲-2 | 期望 FAIL 名單之名目＋原因 | 對帳 PASS·類 III ＝ 0 | **對帳 FAIL**·名目 **22／22 逐項相同**·類 I ＝ 18／類 II ＝ 0／類 III ＝ **4** | **`EXPECTED_RED`** |
| 甲-3 | `W-G G.1`（`_ctx["0m"]`） | 紅·`KeyError: '0m'` | 紅·`KeyError: '0m'`（**逐字未變**） | **未變**·⛔ **非 `NEWLY_LIVE`** |
| 甲-4 | `verify/baselines/` | 19 項 | **未動**（`git diff --numstat` 空） | **未變**（§0：本批不更新） |
| `[1/3]` | `E 系列實測快照閘` | 🔴·因 **`R1` 結構閘** | 🔴·因 **`②-宗 圍堵閘破[R2]`** | **`EXPECTED_RED`**（原因移） |
| `[1/3]` | 禁寫死絕對路徑閘 | 1 命中 | 1 命中 | 未變 |
| `[1/3]` | `fixture_corner_range_k8.py` | `rc=1` | `rc=1` | 未變 |

🔒 **`UNEXPECTED_RED` ＝ 0**；**`NEWLY_LIVE` ＝ 0**。
🔒 **名目集合零變動**：`PASS` 12→12、`FAIL` 22→22，兩集合之差**雙向皆空**。

**甲-2 之四項類 III 皆同一根因**（`R1` 結構閘 → `R2` 之既有閘）：
`v3·StepG0m`／`v3·StepG3.5m`／`W-D.3 碎片`／`W-D.4`。

### 🔴 一項與事前預期不同者（⛔ 具名·不掩蓋）

交接文與施工單皆預期 甲-3 之 `W-G G.1` **可能由紅轉綠／`NEWLY_LIVE`**。**實測未發生**——其原因全文**逐字未變**（`KeyError: '0m'`）。
**成因**：`_ctx["0m"]` 缺鍵之上游是 `run_step_g` 之 `raise`；本批只把該 `raise` 由 `R1` 移到 `R2`，**並未使其消失** ⇒ 上游仍崩 ⇒ 該閘仍死。

### 🔴 一項伴生現象（⛔ 非本批弄壞）

甲-1 改後印「竄改態🔴 ∧ 還原態🔴 ⇒ **竄改自檢無鑑別力** ⇒ 本夾具不得計入交付」。
🔒 此係**失敗考古節 65 第五層**已載之設計限制：判準寫成「竄改態紅 ∧ 還原態綠」，**當閘本身正因真變動而紅時必誤報無鑑別力**。
⇒ 故本報告**⛔ 不採甲-1 之 verdict**，只採其**原始逐字輸出**（類 III 之凍存／現況兩欄），並另以 `run_all` 對帳器、探針、碼面行號對拍**三路獨立佐證**。

### 🔒 量測器非紅之自證（考古 71·呈送前置）

同一支對帳器於**同一次執行**同時產出 **類 I ＝ 18（判「相同」）** 與 **類 III ＝ 4（判「不同」）**
⇒ **兩種出艙碼皆出現** ⇒ ⛔ 非恆紅、⛔ 非恆綠 ⇒ **具鑑別力**。
探針側：`ΣRw_L@k*` 得 9 個相異值（判「有變」）而 `k*` 單一值（判「無變」）⇒ 同理。

---

## §6 落地自證（考古 68：「已完成」≠「已落地」）

| 式 | 輸出 | 判 |
|---|---|---|
| `git diff --numstat HEAD -- verify/stepg_pipeline.py` | `5　2` | ✅ ＝事前登記（`R-7` 未觸發） |
| `git diff --numstat HEAD`（全樹） | 僅該一檔 | ✅ `R-6` 未觸發 |
| `git diff --numstat HEAD -- verify/baselines/ verify/out/` | **空** | ✅ `R-5` 未觸發 |
| `git status --porcelain \| grep "^??"` | **空** | ✅ 無未追蹤新檔 |
| `python -m py_compile verify/stepg_pipeline.py` | `rc=0` | ✅ |

🔒 **一項須具名之處置**：`verify/out/probe_WG923_bslot_only.log` 現查為**追蹤檔**（`git ls-files` ⇒ 1·未被 `.gitignore` 忽略），而該探針**硬編**輸出至該路徑、無覆寫參數 ⇒ 逕跑會觸 `R-6`。
處置 ＝ **備份 → 跑 → 輸出另存 → `git checkout` 還原 → 驗還原**（該路徑之 `git diff` 為空）。輸出零損失。

---

## §7 本波之戒·逐條自檢（⚠️ 立戒者在下一批最容易違反自己的戒）

| # | 戒 | **已套用** |
|---|---|---|
| 1 | 紀律第 4 條（自檢下放一層） | `J0` 先判且其受詞為**中間量 `b`**；`J3` 另附「`ΣRw_L@k*` 是否隨 `b` 變」之下放層 |
| 2 | 紀律第 7 條（閘之一端來自受詞之外） | `J0`〜`J4` 之錨**全部來自 `W-G.9-23` 之前批執行**；甲組基線**全部跑完於動碼之前** |
| 3 | 紀律第 3 條（無從判定 ≠ 判定為偽） | `R3`〜`R6` 具名為**無從判定**；§5 三碼未共用；**具名指出探針把無從判定升級為 R-2** |
| 4 | 紀律第 8 條 ＋ `VR-011` | §2 兩錨為**整行**，比對**先正規化行尾**（該檔為 CRLF） |
| 5 | 考古 63（同一次執行） | 本報告之數**全部為本批當次輸出**；甲組基線與改後各自現跑 |
| 6 | 考古 68（已完成 ≠ 已落地） | §6 五式獨立指令 ＋ `py_compile` |
| 7 | 考古 78（對照組須有鑑別力） | `J0` 受詞為中間量；量測器非紅自證（類 I 18 ∧ 類 III 4） |
| 8 | `W-G.9-23` §七（⛔ 不得逕自重凍） | §5 `EXPECTED_RED` 之正辦 ＝ 具名列值、**本批不更新** |
| 9 | 考古 66／67（⛔ 不以窗抽清單） | 🩸 **本批曾犯一次**：`[1/3]` 之非零項首版以 `awk 'NR<=3'` 截取，誤得「`E 系列實測快照閘` 消失」；改讀**該段全集**後更正為「仍紅、原因移」。⇒ 已於本報告記載，⛔ 未讓該誤判出艙 |

---

## §8 ⛔ 本批未做之事

1. ⛔ **未更新任何凍存物／baseline**（§0 鐵律·另立 `VR-012` 押後）——含甲-1 之終止點凍存、期望 FAIL 名單、`verify/baselines/` 之四檔滑池槽診斷。
2. ⛔ **未改 `forced` 側**（三格仍卡「保留區之表示法」）、⛔ 未改 A 路徑（`app.py`）、⛔ 未改族①、⛔ 未解表示法。
3. ⛔ **未對 `R3`〜`R6` 作任何宣稱**——該四街廓**從未進入**（探針到達序 ＝ `['R1','R2']`）。
4. ⛔ **未量任何宗地之 `G`／分配面積之變動量**——`k*` 四格未變、`指配0m`／`指配3.5m` 之原因全文落**類 I**（逐字未變）係**間接**證據；⛔ **不據此宣稱「面積零變動」**。
5. ⛔ **未改該探針**（其 `Q-2` 之受詞失效，射程外·具名待處置）。
6. ⛔ **未做全倉轉紅盤點**——§5 之母體 ＝ 甲-1〜甲-4 ＋ `[1/3]` 全集（正面列舉），⛔ 不宣稱「全倉只有這些會轉紅」。
