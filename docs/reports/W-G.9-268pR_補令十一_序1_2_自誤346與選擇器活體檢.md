# `W-G.9-268′` 補令十一 序 `1`／`2`：`自誤 346` 之鑄 ＋ 七支**選擇器活體檢** —— ✅ **辦畢**

> 🔒 序 `1` ＝ `自誤 346` 已鑄（收工閘全過）。
> 🔒 序 `2` ＝ 七支**皆已增設**選擇器活體檢，並於**旗標仍活**之態**逐支出艙其判**：**`7`／`7` 通過**。
> 🛑 序 `3`（`c3`·🔴 生產碼）**未辦**——依 `§六` 之序，其落**側支** `verify/W-G.9-268p-c3`、⛔ 主線。
> 🔴 **一項新阻塞項候選已具名上呈**（`§五`）——⛔ 自行鑄號、⛔ 自行修。

---

## §零　三軸之總宣告（`VR-091`）

> `(1)` **來源 `commit`**：主線期初 ＝ `79a9144dc1f89f34f272d75ba598c4e9066cfe80`（`ls-remote` 實查·相符）。
> `(2)` **母體**：逐節具名。　`(3)` **粒度框**：逐節具名。

🔒 **工項零**：補令十一原封入倉——`98` 列／**`8474`** B／`CR 0`；
來源與落檔 `sha256` 皆 `52be19fbed91038c6ba39acea3c89b2ba989a9a56e4c81b894944a9254544733`／`cmp rc = 0`。

---

## §一　序 `1`：`自誤 346`

**取號現查**（倉內既有器 `wg9268_gate5_tally.py`·⛔ 自擬框·態 `blob@79a9144`·母體 ＝ 登記表單檔·列框）：
自誤 相異 `338`／**MAX `345`**／缺號集 `[106]` ⇒ **次號 `346`**，與補令十一 `§五` 所載期初值**逐位相符**。

**意指占用**（補令九 `§一-2` 二形並取·全倉 `1839` 檔·落檔 `verify/out/WG9268pR_occ346.log`）

| 受詢／對照 | B 形 | C 形 | 鬆框 | 判 |
|---|---|---|---|---|
| **`自誤 346`** | `0` | **`0`** | `0` | ✅ **未占用** |
| 甲 `自誤 341` | `2` | `17` | `19` | ✅ `≥1` |
| 甲 `自誤 340` | **`0`** | `19` | `21` | ✅（B 形為 `0`）|
| 甲 `GB-157` | **`0`** | `24` | `27` | ✅（同上）|
| 甲 `GB-156` | **`0`** | `24` | `28` | ✅（同上）|
| 乙 `⟨NEG⟩`（**執行期選出**·字面⛔ 出艙）| `0` | `0` | `0` | ✅ `=0` |

🔴 **`GB-158` 於本次取號中再度復現**：四個已知占用之對照組中 **`3` 個於 B 形得 `0`**
⇒ **B 形⛔ 單獨採信**；本次之「未占用」由 **C 形**承擔。

**鑄號收工閘（`常規四（九）二`·框逐字自倉內器取出並 `exec`）**

| 量 | 前 → 後 |
|---|---|
| 相異 | `338 → 339` |
| **MAX** | **`345 → 346`** |
| 缺號集 | `[106] → [106]`（**不變**）|
| ① 形之自證／② MAX 之推進／②′ ⛔ 在缺號集／判別力 | ✅／✅／✅／✅ |

🔒 **append-only**：嚴格前綴 `True`／增量 `2146` B／`CR 0`。

### 一-1　🩸 自捕：**哨兵之字面隨探針入倉**（`GB-147` 之形）

🩸 上批（序 `4`）之 `probe_WG9268p_occ6.py` 將乙組哨兵之號**寫死為字面**並隨該檔入倉
⇒ 本批之查中，該號由**真零**變為 **C 形 `1`／鬆框 `2`**（其命中即該探針與其落檔自身）。
🔒 **其為 `CLAUDE.md` 明令之反面**：「哨兵字樣宜**於執行期組出**、⛔ 使其字面落入任何 log」（`GB-147`）。
🔒 **處置**：改為**執行期自倉掃出**一個三形皆 `0` 之號，其字面**⛔ 出艙**，一律以代稱 `⟨NEG⟩` 指之
⇒ 本批之乙組三形皆 `0` ✅。**⛔ 追改上批既出之數**（`常規四（九）五`）。

---

## §二　序 `2`：七支之**選擇器活體檢**（補令十一 `§二-2`）

**模組** ＝ `verify/probes/wg9268p_selector_liveness.py`（新建·**⛔ 改任何既有判準與容差**）。

### 二-1　🔑 **所取之可觀測量 ＝ <u>分支之被觀測</u>**（⛔ 合成幾何）

> `(‵_oblique_s_max‵ 之被呼叫次數, 回傳值)`
> —— 旗標 `off` 之分支**逕回 `cum_S` 而⛔ 呼叫** `_oblique_s_max`；`on` 則必呼叫之。
> `c3` 移除旗標後二態皆呼叫 ⇒ 該量逐位相同 ⇒ **本檢轉紅**。

🔒 **其適用於<u>以「相同」為期望結果</u>之探針**（如 `offsrc`：`run_corner_pk` 結構上⛔ 讀該旗標）
⇒ **⛔ 需另備「已知須相異」之管線輪**（補令十一 `§二-2` 末款所慮者）。

🩸 **首版之自捕（⛔ 以合成幾何為受詞）**
首版取「單位正方形 ＋ `d̂ = (1,0)` ＋ `allocation_dir = (0,1)`」，而 `_strip_axis` 之 `m̂ ∥ allocation_dir`
⇒ `denom = d̂·m̂ = 0` ⇒ **二態皆拋 `RuntimeError`（loud）**、皆「相同」⇒ **甲乙二造同紅**、本檢失能。
🔒 **由其<u>兩造自證</u>當場擋下**（甲須通過、乙須轉紅——當時二者同紅 ⇒ 判「本檢失能」）。
🛑 **且⛔ 得改用 `alloc ∥ d_hat`**——`_strip_axis` docstring 逐字載其為**失敗考古 `#7`**：
「首版合成測用 `alloc ∥ d_hat`（正交）全綠＝**只測了自己的想像**；**斜交是本案常態**」。
⇒ 改以**分支之被觀測**為受詞，**⛔ 需任何幾何為真**（`_oblique_s_max` 以替身取代）。

### 二-2　本檢之**兩造自證**（落檔 `verify/out/WG9268pR_liveness_selftest.log`·`rc = 0`）

| 造 | 態 | `off` | `on` | 判 |
|---|---|---|---|---|
| **甲**（旗標**仍活**·現主線） | 真函式 | `(0, 0.0)` | `(1, 1.0)` | ✅ **須通過** ⇒ 通過 |
| **乙**（**模擬 `c3`**·無條件執行之替身） | 替身 | `(1, 1.0)` | `(1, 1.0)` | ✅ **須轉紅** ⇒ 轉紅（**二態同值**）|

🔑 **判別力 ＝ 甲通過 ⋀ 乙轉紅 ⇒ 本檢非恆綠亦非恆紅** ✅

🩸 **乙造首版「紅得不是地方」之自捕**：其替身於**定義時**捕獲真 `_oblique_s_max`
⇒ 活體檢所裝之替身進不來 ⇒ 該對照**因例外而紅**、**⛔ 因「二態同值」而紅**
⇒ **證不出本檢能偵得 `c3` 之情狀**。改為**呼叫時**自 `ns` 取，方得上表之正確紅。

### 二-3　七支之增設（**⛔ 改既有判準**·`deletions` 逐支 `0`）

| 探針 | 增列 | `assert_live` 命中 | `py_compile` |
|---|---|---|---|
| `probe_WG9268p_c1_synth.py` | `15` | `1` | OK |
| `probe_WG9268p_c2_gates.py` | `6` | `1` | OK |
| `probe_WG9268p_c2_v1prime_mem.py` | `5` | `1` | OK |
| `probe_WG9268p_c2_v1prime_pert.py` | `5` | `1` | OK |
| `probe_WG9268p_conserv.py` | `6` | `1` | OK |
| `probe_WG9268p_mem.py` | `6` | `1` | OK |
| `probe_WG9268p_offsrc.py` | `6` | `1` | OK |

🔒 **置於其全部判定<u>之前</u>**（緊接 `harvest()`）；不過即 `return 4`／`sys.exit(4)` 並印
「🔴 選擇器活體檢不過 ⇒ 本次輸出⛔ 出艙（⛔ 判綠·⛔ 靜默續跑）」。

### 二-4　**旗標仍活**之態之逐支出艙（`§六` 序 `2` 所令）

**落檔** ＝ `verify/out/WG9268pR_liveness_all.log`（`rc = 0`·`stderr` `0` B）。
逐支之 `off` ＝ `(0, 0.0)`／`on` ＝ `(1, 1.0)` ⇒ **二態確有相異**。

| 探針 | 活體檢 | 該支之 `rc` |
|---|---|---|
| `c1_synth` | ✅ **仍活** | `0` |
| `c2_gates` | ✅ **仍活** | `0` |
| `c2_v1prime_mem` | ✅ **仍活** | `9`（＝ 已知之**三組同色**停機·補令九 `§二`）|
| `c2_v1prime_pert` | ✅ **仍活** | `0` |
| `conserv` | ✅ **仍活** | `0` |
| `mem` | ✅ **仍活** | `3`（＝ 本樹**無 `_partial`** 之 loud 拒測·坑 `2`）|
| `offsrc` | ✅ **仍活** | `0` |

> 🟢 **`7`／`7` 通過** ⇒ **各支之活體檢於旗標仍活時確可用**（`§六` 序 `2` 之受詞成立）。
🛑 **⛔ 讀為「七支之全部判定皆綠」**——本節之受詞**僅為活體檢**；二支之 `rc ≠ 0` 係**既有之已具名紅**，⛔ 本節所生。
🔒 **七支皆⛔ 逾時**（逾時設 `240 s`）⇒ ⛔ 有截斷之虞。

---

## §三　立準之落地（補令十一 `§二-3`）

🔒 **立閘之三問（累積）**——本模組 docstring 已逐字載之：
> `(1)` 此閘所欲驗之事成立時，此閘還量得到嗎？（補令五 `§三-1`·自毀式第一例）
> `(2)` 此對照落於器之可測域內、且能驅動所令之受量欄位嗎？（`自誤 345`·第二例）
> `(3)` **此二態之選擇機制，於受測物變更後仍活嗎？**（本項·第三例）

---

## §四　`自誤 346` 之受詞（逐字轉錄·⛔ 改寫）

> **形**：**於重級批之工項表中，為一個<u>動生產碼</u>之工項漏立驗收條款。**
> `c0`／`c1`／`c2` 各載其驗收與判別力，而 **`c3` 僅載受詞與一則 ⛔ 追改款，⛔ 任何驗收條款**
> ⇒ 若照原單辦，`c3` 之生產碼變更將**無閘通過**。
> 🔒 **與 `338`／`345` 之別**：彼二者係閘**立錯**；本則係閘**未立**。

🔒 其處置 ＝ 補令十一 `§三` 之補訂（`Q-1`／`Q-2`／`Q-3` ＋ `c3` 專屬停機款）。

---

## §五　🔴 **新阻塞項候選**（⛔ 自行鑄號·⛔ 自行修·依 `§五` 上呈）

### `probe_WG9268p_c1_synth.py` 之 `REPO` **硬編指向他窗 worktree**（坑 `10`）

**現查（逐字）**：
```
REPO = (r"C:/Users/admin/Desktop/land-readjustment-trial/.claude/worktrees"
        r"/w-g-9-268p-s1-correction-f41d70")
```
並隨即 `os.chdir(REPO)` ⇒ `harvest()` 所讀者為**該樹之 `app.py`**、**⛔ 本倉者**。

**實測（本批·正面列舉二樹）**

| 樹 | `app.py:9269` 之逐字 |
|---|---|
| 他窗 `w-g-9-268p-s1-correction-f41d70`（**現仍存在**）| `…environ.get('WG9268P_ANCHOR_GEOM', '0')…` ＝ **pre-`c2`** |
| **本倉**（主線 `79a9144`）| `…environ.get('WG9268P_ANCHOR_GEOM', '1')…` ＝ **`c2` 後** |

🔴 **其後果直接打在補令十一 `§二` 之受詞上**：
`c3` 於**本倉**移除旗標後，**只要他窗之樹仍存旗標，該支之活體檢<u>仍會通過</u>** ⇒ **假綠**
——**恰為 `§二` 所欲防之情狀**。
🛑 ⇒ **該支之活體檢之「✅ 仍活」（`§二-4`）⛔ 為<u>本倉</u>之證據**；本批已於該支**就地具名**此事
並印出其 `REPO`（⛔ 動其 `REPO` 一字——**逾補令十一之授權**）。

🔒 **⛔ 自行鑄號**（補令十一 `§五`：本補令得鑄者 ＝ `自誤 346` **一號**；查得新阻塞項 ⇒ **停機上呈**）。
🔒 **候發單側**：是否鑄為 `GB`／其修法（`REPO` 改由 `argv` 或自 `__file__` 上溯）／
其既往落檔之效力是否受影響。

---

## §六　逐 `commit` 之生產碼判（`常規一 🔧 二`·**含空輸出**）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| 本批 | `79a9144..HEAD` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 逕推 |

🔒 **⛔ 動 `app.py` 一字**（`git diff --numstat 79a9144..HEAD -- app.py` 空輸出）
——`c3` 之前之明令（補令十一 `§六`）。

---

## §七　續辦之界

🛑 **本批⛔ 辦者**：⛔ 辦 `c3`（序 `3`·其動生產碼·須**側支** `verify/W-G.9-268p-c3`）、
⛔ 鑄 `自誤 346` 以外之任何號、⛔ 修 `§五` 之硬編路徑、⛔ 改任何既有探針之判準與容差。

🔒 **序 `3` 之受詞（補令十一 `§三`）**：移除旗標 ＋ 更新 `_wg9268p_anchor_advance` docstring 之
「預設 `off`」（自 `c2` 起已過期）；驗收 `Q-1`／`Q-2`／`Q-3`；
⛔ 動 `_oblique_s_max`／`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` 一字；
⛔ 改錨之語意。
