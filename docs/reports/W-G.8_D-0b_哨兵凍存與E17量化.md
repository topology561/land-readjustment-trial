# W-G.8／D-0b：哨兵凍存 ＋ `E-1.7` 出局量化 ＋ 帳目更正（⛔ 零碼面·**未切換**）

**錨**：`wip/s1-endpart` @ `027a06d`（現查符合）。**§五-1**：`git diff -U0 027a06d -- app.py` ⇒ **0 行**。
⛔ 只跑凍存、不切換、不接 `run_all`。

---

## 🛑 §一 **仍未完成**——「六項不授權逐字」不在訊息裡

D-0b §一 令「將**本訊息**『六項不授權逐字』整段，接於 `K-9-5-4` 之後入正典，⛔ 一字不改」。

**但該訊息（D-0b）之全文（§一〜§五）並不含任何六項清單。** 逐段查：
§一 ＝ 指示本身；§二 ＝ 哨兵規格；§三 ＝ E-1.7 量化；§四 ＝ 帳目更正；§五 ＝ 驗收。
**無一段為「六項不授權事項」之逐字。**

倉內亦查無：`grep -rn "不授權" docs/ .claude/ CLAUDE.md` ⇒ **0 命中**（D-0 已查過一次，本批複查同結果）。

⇒ ⛔ **未自行編造六項**、⛔ **未作「該六項不存在」之否定性存在主張**。
**請 KL 直接貼出該六項逐字**（純文字即可），CC 收到後單獨一批補入 `K-9-5-4` 之後，
標題沿用「**🔒 D 波之範圍界線**」。**這是 D-0b 唯一未完成項。**

⚠️ **同一漏件已連續兩批**（D-0 §一-3、D-0b §一）——⇒ 研判為**貼文時該段被截掉**，非 KL 未寫。

---

## §二 哨兵規格定案 ＋ 切換前凍存（**已跑**）

### 2-0 🔒 切換順序已裁：**D-1（F）先、D-2（幾何）後**

**理由（KL 裁·入報告）**：D-2 先行會使 D-1 於 `3.5m R1/right` 之**結局不變**
（新門檻 `226.13` < 改正後真G `228.42` ⇒ 該格仍達標）⇒ 哨兵之 (c)(d) 兩項**失去鑑別力**，
只剩 (a)(b) 之數值可辨 ⇒ **哨兵降級**。
⇒ 故 **D-1 必須先行**，方能以「winner → 強制抵費地」之**結局翻轉**作為證據。

### 2-1 D-1 哨兵（`verify/out/probe_D1_F_slot_切換前凍存.log`）

| # | 受測 | 切換前應 | 切換後應 | **凍存實測** |
|---|---|---|---|---|
| (a) | `_corner_block_true_G` 之 `F` **實參**（16 格） | ＝ 路寬 | ＝ 側長度 | **切換前態 16/16** ✅（切換後態 0/16） |
| (b) | `3.5m R1/right 628(5)` 真G | `240.57` | `228.42` | **240.57** ✅ 切換前態 |
| (c) | `3.5m R1/right` 結局 | winner ＝ `628(5)` | 無人過門檻 ⇒ 強制抵費地 | **`628(5)`** ✅ 切換前態 |
| (d) | 全案 | winner `12`／強制 `4` | winner `11`／強制 `5` | **12／4** ✅ 切換前態 |

**總判：切換前態 ✅ 全數成立／切換後態 🔴 未全成立 ⇒ ✅ 具鑑別力（恰一態成立）。**

🔑 **(a) 之逐格實測揭露一件事**：**16/16 格之 `F` 實參皆為 `8.00`**（側街路寬全案一致），
而正典所要之側長度為 **`33.08`〜`44.95`** ⇒ 差 **`25.08`〜`36.95` m**
⇒ 側街負擔被短計 **4〜5.6 倍**，且**不是個案、是全案一致**。

### 2-2 D-2 哨兵（`verify/out/probe_D2_corner_range_切換前凍存.log`）

| 項 | 值 |
|---|---|
| 判準容差（先行宣告） | `1e-4` |
| **有效鑑別格** | **14/16** |
| 🔴 **無鑑別力·非通過** | `0m R6/right`（舊新間距 `0.000100`）／`3.5m R6/right`（間距 `0.000000`） |
| 命中**舊值** | **14/14** ✅ |
| 命中**新值** | **0/14** |

**總判：切換前態 ✅／切換後態 🔴 ⇒ ✅ 具鑑別力。**

⛔ **禁記為 16/16**——那 2 格之 `==` 對舊新兩值**皆真**，**不能證明任何事**。
（本探針已將其自分母剔除，並在 log 逐格標「🔴 **無鑑別力·非通過**」。）

### 2-3 🔒 兩支哨兵之共同設計原理

**零 diff 有兩種相反解釋**——「改對了而該處本就不受影響」與「根本沒改到」。
⇒ 哨兵一律為**正對照**（`==` **反向式**）：**切換前 FAIL、切換後 PASS**。
🔒 **合格條件 ＝ 恰有一態成立**；⛔ 兩態同時成立 ⇒ 哨兵無鑑別力、須重設計
（此即 D-2 那 2 格被剔除之理由）。

---

## §三 `E-1.7` 出局之量化（⛔ 只出數字與出處·**未提任何建議**）

**實料**：`verify/out/probe_E17_intersection.log`
🔒 **自我驗證閘**：範圍面積 vs 凍存之 GEO-1 §三 ⇒ **E2（舊）16/16 過・E1（新）16/16 過**。
🔒 真交集之算式**逐字沿用生產**：`cp.intersection(_corner_poly).area`（`app.py:11102`）；
候選多邊形取 `cand['polygon_coords']`（`app.py:11089`）。⛔ 未另寫第二套。

### (1)(2)(3) 三格「—」之量化

| 情境 | 街廓/側 | 候選 | **(1) 舊真交集** | **(2) 新真交集** | **(3) 對 `1.0㎡` 之差（新）** | 舊 | 新 |
|---|---|---|---:|---:|---:|---|---|
| 0m | R2/left | 628-40(1) | **8.928003** | **0.000000** | **−1.000000** | ✅ 在池 | ❌ 出局 |
| 0m | R3/right | 628-28(1) | **13.786544** | **0.001113** | **−0.998887** | ✅ 在池 | ❌ 出局 |
| 0m | R5/left | 628-7(2) | **1.070652** | **0.000000** | **−1.000000** | ✅ 在池 | ❌ 出局 |

🔑 **三格皆非「略低於門檻」，而是幾乎歸零**（`0.000000`／`0.001113`／`0.000000`）
⇒ 這三宗在新範圍下**幾乎完全不與街角範圍相交**。
⇒ ⛔ **不可讀成「差一點點就過」**——即使門檻為 `0.01`，前二者仍出局。

🔴 **`0m R5/left 628-7(2)` 另有一事**：其**舊**真交集 ＝ **`1.070652`**，
僅高於 `E-1.7` 之 `1.0` 門檻 **`0.070652`** ⇒ **它在現行構造下就已經是勉強在池**，
**而它正是該格現行之 winner**。

### (4) `E-1.7` 之 `1.0` 出處與身分（**正面指名**）

```
app.py:11145   _has_p1 = _inter_p1_area > 1.0   # 🆕 W-D.1.3-b E-1.7 門檻 0.01→1.0㎡（KL 絕對地板；剔 sliver 如 628-40 之 0.33）
app.py:11146   _has_p2 = _inter_p2_area > 1.0
```

**身分判定（依上引註解之逐字）**：

| 候選身分 | 判定 | 據 |
|---|---|---|
| 法規數字 | ❌ **否** | 註解未引任何法源；`1.0㎡` 不見於畸零地使用規則或 104 手冊之引文 |
| 純程式參數 | ❌ **否** | 註解明載「**KL 絕對地板**」，並記錄沿革（`0.01 → 1.0㎡`）與具體案由 |
| **KL 裁定之工程門檻** | ✅ **是** | `W-D.1.3-b`·由個案 sliver（`628-40` 之 `0.33㎡`）觸發而立 |

🔴 **併記（正典側查核·正面列舉）**：

```
grep -c "E-1.7"   docs/rulings/K-6_街角地分配程序與可分配判準.md ⇒ 0
grep -c "絕對地板" docs/rulings/K-6_街角地分配程序與可分配判準.md ⇒ 0
grep -c "E-1.7"   CLAUDE.md                                      ⇒ 0
grep -c "絕對地板" CLAUDE.md                                      ⇒ 0
```

⇒ **該 KL 裁定之唯一逐字出處為碼內註解**——**未入裁定正典、未入 `CLAUDE.md`**。
⛔ 只陳述此事實，**不提出任何處置建議**。

### (5) 全 16 格逐候選（**132 候選列**·全表見 log 之【C】）

| 類 | 列數 |
|---|---:|
| 兩態皆**出局**（該宗本就不臨該街角·`真交集 = 0`） | **94** |
| 兩態皆**在池** | **35** |
| 🔴 **舊在池 → 新出局** | **3**（即上表三格） |
| 反向（**舊出局 → 新在池**） | **0** |

⚠️ **新構造下逼近門檻者（`1.0 < 真交集 ≤ 2.0`·標示線 `2.0` 先行宣告）＝ 1 列**：
`3.5m R3/right 628-29(1)`（新 **`1.5419`**）⇒ **換案時最可能翻邊之格**。

⛔ **本節未提出任何調整建議**——門檻是否調整**屬 KL**（施工單 §三 明令）。

---

## §四 帳目更正（確認）

`3.5m ΣΔG = **−0.0449**`（claude.ai 原記 `−0.0549` ＝ **抄成隔壁 `Δ抵費地` 欄**）。
**帳已於 D-0（`027a06d`）以 `−0.0449` 立**（`docs/reports/W-G.8_D_期望影響表_凍存.md` §三-1）。

**常設條已入 `CLAUDE.md`**：

> 🔒 **凡列合計，帶號合計與絕對值合計必須並列（KL 立 2026-08-08·D-0b §四）**

- **理由**：二者所偵測之錯**不相交**——`Σ|X|` 抓「漏項／多項／值錯」，
  `ΣX` 抓「**符號錯／抄錯欄**」。
- **本案即證**：`Σ|ΔG|` 兩邊完全相符（`0.5243`／`0.3275`）⇒ **對抄錯欄全盲**；
  **只有帶號合計抓得到**（`−0.0449` vs `−0.0549`）。

錨：`grep -n "凡列合計，帶號合計與絕對值合計" CLAUDE.md`

---

## §五 驗收

| # | 項 | 結果 |
|---|---|---|
| 1 | `git diff -U0 027a06d -- app.py` | **0 行** |
| 2 | §一 六項逐字入正典 | 🛑 **未完成**——六項不在訊息內（見 §一） |
| 3 | 兩支凍存 log 已產出並入版控；D-2 之 2 格無鑑別力已標明 | ✅ |
| 4 | §三 五欄齊備；(4) 正面指名；⛔ 未提建議 | ✅ |
| 5 | §四 常設條已入 `CLAUDE.md` | ✅ |
| 6 | push 後回報附 SHA | ✅ 見聊天回報 |

## §六 ⛔ 本批**未**做者

1. **切換**——⛔ 明令不切換；`app.py` 零 diff。
2. **§一 六項入正典**——🛑 待 KL 補件。
3. **E-1.7 門檻之處置**——屬 KL（§三 明令只出數字）。
4. `K-9-5-4` 之實作（D-1／D-2）——本批僅備妥哨兵。
