# `W-G.9-208R`　`commit 2` 執行報告——步驟 0 回歸無條件 ＋ `K-9-24 二` 歸側四分支

> 施工單：`docs/orders/W-G.9-208_施工單_207入倉與自誤261與GB140與補款二與commit2授權.md` `§三`
> 母單：`docs/orders/W-G.9-198R_施工單_步驟0無條件與歸側四分支與名次與段二排序與段一上鎖.md`（`commit 2`）
> 🛑 **本報告⛔ 綁於 `commit 2`、⛔ 置於其後**——`commit 2` 走側分支候放行，報告走主線。

---

## §零　落地態（全 `40` 碼·量測時刻 `2026-09-02 16:47:22 +0800`）

| 受詞 | 值 |
|---|---|
| `commit 2` | `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b` |
| 其 `parent` | `c746d2dd2ce9d52714d3673c7cece9441259967a` |
| 其 `tree` | `d4ef8fc706eacaaa7455bbd3b89423f1184434d7` |
| **側分支** `origin/verify/W-G.9-198R-c2` | `0d88e1a5bbe93f8a98c029dd9d45e1c216b41d6b` |
| 側分支 `origin/verify/W-G.9-198R-c1` | `b772f8aa09820f01312081588fd8c8d718c8ac86`（`⛔ 刪`·單 `§三-4`） |
| **主線** `origin/wip/s1-endpart` | `c746d2dd2ce9d52714d3673c7cece9441259967a` |

**主線⛔ 因 `commit 2` 前進之證**（三路獨立）：

1. `commit 2` 之 `parent` ＝ `c746d2dd…` **恰等於**現主線頭 ⇒ 主線未含 `commit 2`。
2. `git merge-base --is-ancestor 0d88e1a origin/wip/s1-endpart` ⇒ **非祖先**（合規）。
3. 主線之二生產檔 blob **仍係 `commit 1` 之態**：`app.py` ＝ `6f0317e47ef92c7e06d6d904ecf2d129656bd331`；
   `verify/selection_pipeline.py` ＝ `10f90c509ae13c77c83e3e5c76fe3c09ca675b14`。

🔴 **量測器自捕（⛔ 受詞紅）**：初次復驗時我將期望常數寫死為 `b772f8aa…`（＝ `commit 1` 之頭）而得
`🔴 主線已動`。實則主線係由 **`W-G.9-208 §一` 之二 commit** 合法前進（`1712aed` 二單入倉＋鑄號／
`c746d2d` §一 報告入倉，**皆零生產碼**、已於 `§一` 回報），`b772f8aa` 為 `c746d2dd` 之祖先。
期望常數過期 ⇒ **量測器紅**。另反引號於雙引號內觸發指令替換（`W-G.9-207: command not found`），
**僅損顯示文字、⛔ 觸任何量測**。二者皆依 `常規五` 先證器紅、再以正確錨證受詞綠。

---

## §一　異動總表

| 檔 | `+`／`−` | 舊 blob | **新 blob** |
|---|---|---|---|
| `app.py` | `+219`／`−11` | `6f0317e47ef92c7e06d6d904ecf2d129656bd331` | **`144349c834c2289434e1b0edbcfc8d0a27d88435`** |
| `verify/selection_pipeline.py` | `+9`／`−0` | `10f90c509ae13c77c83e3e5c76fe3c09ca675b14` | **`f878276b70ff5244b71f0d688ce7ffe5bb8fee2c`** |

- `numstat` 逐檔：`219  11  app.py` ／ `9  0  verify/selection_pipeline.py`（commit 計 `228` 增／`11` 減）。
- 🔒 **`verify/run_verification.py` 未觸**——`commit 2` **未增任何診斷欄** ⇒ `U-K2` 未觸發
  ⇒ ⛔ 需 `_DECLARED_EXTRAS` 之申報（`commit 1` 所加之 `指數名次` 一則不動）。
- 🔒 **`補二-4` 之界**：`staged` 檔 `2`，**逾所准者 `0`**；二 blob 之 `CR` 皆 `0`
  （`app.py` `1406177` B／`selection_pipeline.py` `34568` B）。
- 工作樹於 commit 後**乾淨**（`git status --porcelain` 去未追蹤後為空）。

### 新 `AST` 區間（🛑 ⛔ 沿用 `S-0c` 之值）

| 函式 | **新** | （`S-0c` 舊值） |
|---|---|---|
| `k6_step0_block_locked` | **`11567-11589`** | `11567-11583` |
| `k6_step0_merge` | **`11592-11752`** | `11586-11743` |
| `select_corner_lots_both_sides_v12` | **`12603-12972`** | `12594-12963` |
| `_pk_one_side_v12` | **`12975-13085`** | `12966-13076` |
| `k6b_stage2_global_order` | **`13088-13156`** | `13079-13143` |
| `k6b_resolve_dual_crossing_sides` | **`13159-13322`** | （本 commit 新增） |
| `main` | **`14505-23777`** | `14326-23569` |

`app.py`（`LF` 正規化）＝ **`1406177`** B ／ `LF` **`23779`** ／ `CR` **`0`**（`S-0c` 舊值 `1391897` B／`23571`）。

### 被刪之 `11` 列（逐列具名·⛔ 只報「刪 11 列」）

| 來源 | 列數 | 內容 |
|---|---|---|
| `R2-1` 所令之呼叫點移除（`k6_step0_merge` 內） | `4` | 該處註解 ＋ `if k6_step0_block_locked(...)` ＋ `_diag["blocks_locked_k921"].append(_lbl)` ＋ `continue` |
| `R2-9`／`補二-5` 所令之 docstring 改述 | `7` | `k6b_stage2_global_order` docstring 內「`CC` 之技術判讀」一段 |

自檢計數：`139 → 143`（未減）／`3 → 3`（未減）。補丁自證：`7＋3` 處替換**逐處 `count==1`**；
寫入後行尾仍均質（`CR == LF`）。

---

## §二　`D2-a` 〜 `D2-i` 逐項

> 基準 ＝ **`commit 1` 落地後之態 `b772f8a`**（依 `補二-3` 重做改前基線，⛔ 沿用更早之 `6a3f5fe`）。
> 二態 `run_all` 皆於**拋棄式 clone** 內跑（`run_all` 會改寫 `3` 個已追蹤 log）。

### `D2-a`　共有鍵之逐格對拍

**共有鍵之逐格有變 ＝ `0` 格**（土地欄 ＋ 四指數欄 ＋ `指數名次`）。差異**全為列集之變**：

| 方向 | 鍵（`街廓`,`端`,`候選地號`） |
|---|---|
| 僅改前有 | `('R4','右','628-1(1)')` ／ `('R4','左','628(1)')` |
| 僅改後有 | `('R4','右','628(1)+')` |

**逐格歸因**：`628(1)` ＋ `628-1(1)`（同歸戶 `G009`·共用界線）於步驟 0（**無條件**）合併為 `628(1)+`
⇒ 依 `K-9-24 二·②` 歸右（指數 `0.992472 > 0.991558`）⇒ **只出現於右側**；左側扣除 `M` 後候選集空 ⇒ **無列**。

### `D2-b`　步驟 0 之解除確有咬到

| 受詞 | 改前（`b772f8a`） | **改後** |
|---|---|---|
| `blocks_locked_k921` | `['R1','R4']` | **`[]`** |
| `groups_merged` | `8` | **`9`** |
| `parcels_absorbed` | `12` | **`13`** |
| `parcels_out` | `114` | **`113`** |

新增之群**逐筆具名**：街廓 `R4`／命名 `628(1)+`／成員 `['628(1)','628-1(1)']`／`gid` `G009`／
`Σ幾何面積` `3199.78` ㎡／聯集 `Polygon`。

### `D2-c`　歸側之判別力

歸側出艙 **`1`** 宗。左右指數 `0.991558`（`p1_end`）vs `0.992472`（`p2_end`）⇒ **相異**；
`依據款次 ＝ ②`，`歸側結果 ＝ p2_end` ⇒ **與指數大者一致** ✅。

| 端 | 真 G（㎡） | 門檻（㎡） | 達標 | 指數 | winner | 拿得下 |
|---|---|---|---|---|---|---|
| `p1_end` | `2005.21` | `226.01` | ✅ | `0.991558` | ✅ | ✅ |
| `p2_end` | `2005.36` | `225.42` | ✅ | `0.992472` | ✅ | ✅ |

`M ＝ 628(1)+`（街廓 `R4`·跨占 左,右）⇒ **依據款次 `②` ／ 歸側結果 `p2_end` ／
另一側 ⚠️ 強制抵費地 `226.01` ㎡**。

### `D2-d`　強制抵費地面積之**嚴格相等**

`另一側.強制抵費地範圍面積` ＝ **`226.01`** ㎡ ／ `f3_corner_range_areas` 之 `R4`·`left` ＝ **`226.01`** ㎡
⇒ **二數相等 ＝ `True`**。併證 `_offset_diag_rows` 之 `R4` 列 ＝
街廓 `R4`／端 `左`／`抵費地面積＝range(㎡)` `226.01`／指配 `強制抵費地`。

### `D2-e`　`R2-3` 之反例證（「拿得下」⛔ 得以「第 1 名」代之）

本案**存在 `4` 個**「該側第 1 名但**未達標**」之側 ⇒ 其側之 `winner` 皆⛔ 因它而定：

| 街廓／端 | 第 1 名（未達標） |
|---|---|
| `R1`／左 | `628-37(1)` |
| `R2`／左 | `628-41(1)` |
| `R5`／左 | `628-18(2)` |
| `R6`／右 | `628-38(1)` |

⇒ `_holds()` 之**二條合取**（屬 `qualified` ∧ 等於 `winner`）**生效**、非空言。

### `D2-f`　反靜默退路（母體 ＝ `k6b_resolve_dual_crossing_sides` 之函式區間 `13159-13322`）

| 樣式 | 命中 |
|---|---|
| `raise RuntimeError` | **`6`**（原 `4` ＋ `D2-f` 自捕所補 `2`） |
| `except` | `0` |
| `return None` | `0` |
| `return False` | `0` |
| `pass` | `0` |
| `else:` | `2` |

二個 `else:` 皆為**正典明文之分支**、⛔ 靜默預設：`:13263` ＝ **款④**（兩側皆拿不下 ⇒ 辦原位次）；
`:13276` ＝ **款⑤**（無 `winner` ⇒ 強制留設抵費地）。

🔴 **自捕並補之缺口**：原式於取不到 `_corner_range_area` 時**靜默退為 `0`**，而該值即
**強制抵費地之面積**（**有土地後果**）；`門檻(㎡)` 出艙欄同病。補 loud 守衛
`_rngarea_of`／`_thr_of`——取不到值一律 `raise`。
🔒 **其加入經逐位對拍證「零行為變更」**：四 `CSV`（`got_診斷_退縮3.5m`／`got_診斷_退縮0m`／
`got_指配_退縮3.5m`／`got_抵費地_退縮3.5m`）**逐位相同**、`run_all` stdout `diff` **`0` 列**。

### `D2-g`　四閘違規全集之差（逐格歸因·⛔ 只報「有 N 格變」）

| 閘 | 改前 | 改後 |
|---|---|---|
| 四閘之一 | `96` | `92` |
| 四閘之二 | `104` | `100` |
| 四閘之三 | `96` | `92` |
| 四閘之四 | `104` | `100` |

差集 **`40` 格**，**逐格可溯至同一受詞**：僅改前者 `7×4` ＝ `628(1)`／`628-1(1)` 二列之既有違規消失；
僅改後者 `3×4` ＝ 多列 `628(1)+`、缺列二筆。

### `D2-h`　`--selftest`

`--selftest` 於生產碼四檔**皆 `0`**（倉內查無；對照組 `6`）⇒ 該旗標於本倉**不存在**。
改以**模組級自檢 ＋ `34` 閘名目**代之（見 `D2-i`）。

### `D2-i`　既有終局⛔ 移位　🔴 **一項紅·已逐項歸因**

`34` 閘之 `PASS`／`FAIL` 集出現 **`3` 列相異**（`diff` 恰 `3` 列·其餘 `31` 列逐字相同）：

| # | 閘 | 改前 → 改後 | 歸因 |
|---|---|---|---|
| ① | `抵費地0m(應空)` | **✅ `PASS` → 🔴 `FAIL`** | `[抵費地0m] 預期空、得 1 列`：街廓 `R4`／端 `左`／`抵費地面積＝range(㎡)` `116.08`／指配 `強制抵費地` ⇒ **即 `K-9-24 二·⑤` 所令之結果**（`0m` 情境之 `R4` 左街角範圍 `116.08` ㎡） |
| ② | `率接線 G估 欄變動0m` | `5` → `8` 格 | **值異動仍 `0`**；＋新候選 `('R4','右','628(1)+')`、＋消失候選 `('R4','右','628-1(1)')`／`('R4','左','628(1)')` ＝ `+3` |
| ③ | `率接線 G估 欄變動3.5m` | `9` → `12` 格 | 同理 `+3` |

🔒 三者**皆溯至同一受詞**（`R4` 之合併與歸側）；②③ 之「**值異動 `0`**」與 `D2-a` 之
「共有鍵 `0` 格」**互證**。

🛑 **①之處置＝上呈，⛔ 自行更新凍存名單**——該閘之期望「`0m` 之抵費地應空」係**舊正典下之期望**，
已被 `K-9-24 二·⑤` 取代；但「更新凍存期望」屬**意思決定**，非 `CC` 之值域。

**`D2-i` 所名之終局受詞**（`k94_assert_baseline_touch`／`628-53(2)`／`38.803`）於二態 harness
**皆 `0` 命中** ⇒ 係 **app 路徑之終局**、⛔ 於 harness 可觀測（繫 `GB-123`）。
`②-宗 圍堵閘破[R2]` 之破量二態皆 **`45.9766`**（上界 `2.8906`／凍存 `3.1129`）**逐字不變**。

---

## §三　`§六` 附帶量測（⛔ 修、⛔ 鑄號）

### `M-1`　`②-宗 圍堵閘破[R5]` 之破量 `56.8186` vs `56.8187`

**問一（是否同一母體）＝ ✅ 是**。三錨逐字比對（對照組 `宗數9` 二側皆 `0` ⇒ 器非紅）：

| 錨 | 【倉】`verify/out/probe_WG9187_baseline_76378d09.log:14` | 【單】KL `7fab020` 實跑（載於 `CLAUDE.md:1205`） |
|---|---|---|
| `宗數8` | ✅ | ✅ |
| `深度45.71` | ✅ | ✅ |
| `上界 1.5999` | ✅ | ✅ |

**問二（差 `0.0001` 是否僅為 `4dp` 顯示末位）＝ 🔴 於本通道⛔ 可確證**——本 harness 通道於
**`R2` 先破**（`45.9766 > 2.8906`）而**未及 `R5`**：二態之 `56.81` 命中**皆 `0`**
（對照組 `上界` 二態皆 `17` 列 ⇒ 器非紅）。照實回報，**⛔ 修、⛔ 鑄號，繫 `GB-67`**。

**本批⛔ 使該值改變**：二態之 `圍堵閘破` **`12` 列去行號後逐位全等**；倉內既有 log 未動。

### `M-2`　`R3` 之 `Phase 8` `MultiPolygon` 警示

`MultiPolygon`／`Phase 8` 於**二態皆 `0` 命中**（同因：`R2` 先破）⇒ 該警示於本 harness 通道
**⛔ 出現**；**二態逐字相同（皆 `0`）**、⛔ 修。

### `M-3`　🆕 `R4` 內之無地號面

🔴 **該面於生產路徑並非幾何殘餘，而是具名宗地 `_GHOST_(R4)`**（`temp_p` 內一宗·
原地號 `_GHOST`／`cut_type` `cut_整筆`／`幾何面積_m2` 欄 `0.0000`）。
其與 `628(1)+` 之聯集**恰等於** `R4` BLOCK 面積（`3207.866499` ㎡）⇒ **殘餘 `0.000000` ㎡、交 `0`**。
`polygon_coords` 為 `5` 邊形，邊長（降序）＝ `91.638453`／`71.934709`／`19.833573`／`0.099980`／`0.088502`。

| 項 | 【倉】生產路徑實算 | 【單】`K-9-25 五` 重建 | 差 |
|---|---|---|---|
| 面積（㎡） | **`8.085648`** | `7.9615` | `+0.124148`（`+1.5594%`） |
| 最長邊（m） | **`91.638453`** | `91.638453` | **`0.000000`**（逐位相同 ✅） |
| 平均寬 `A/Lmax`（m） | **`0.088234`** | `0.0869` | `+0.001334`（`+1.5354%`） |
| **臨左截角**（m） | **`0.099980`** | `0.0958` | `+0.004180`（`+4.3628%`） |
| **臨右截角**（m） | **`0.088502`** | `0.0855` | `+0.003002`（`+3.5112%`） |
| 臨左 − 臨右（m） | **`0.011477`** | `0.0103` | `+0.001177` |

臨截角長之**認定式**：ghost 上**距該截角邊最近**（距 `0.0000000000` m ⇒ 貼合）之邊，其長
**與該邊於截角線上之投影跨度逐位相符**（`left` 邊 idx=4 `0.099980`；`right` 邊 idx=1 `0.088502`）。
左右之定名取自生產碼 `geom_restore` 之 `theoretical_corners` 之 `side` 欄
（`cutoff_idx=2` → `left`／`cutoff_idx=4` → `right`）。

🔑【單】`K-9-25 五` 稱「`0.0958 − 0.0855 = 0.0103` ＝ 左右臨截角之差 ⇒ **勝負之唯一來源**」——
生產路徑實算之差 ＝ **`0.011477` m**，**同號**（左 > 右）⇒ **方向一致**；惟**量值⛔ 相符**（`+11.4%`）。
⛔ 修、⛔ 鑄號，繫 `VR-087`。

**二處量測器自捕**（皆於出艙前）：

1. 以 `1e-9` 緩衝取「貼邊長」再折半 ⇒ 得 `0.049990`／`0.044251`，係**雙側緩衝環之假影**
   （真值恰為其 `2` 倍）。改直讀 ghost 之短邊 ＋ 投影跨度交叉驗。
2. 「平均寬」我初用 `2A/Lmax` 得 `0.176468`；【單】之 `0.0869` ＝ `A/Lmax`（`7.9615/91.638453 = 0.086880`）
   ⇒ **公式不同源**，改以 `A/Lmax` 並列方可比。

---

## §四　🛑 停機事項（候發單側逐位復驗 ＋ KL 裁）

1. 🔴 **`D2-i` ① 之凍存期望**：`抵費地0m(應空)` 已被 `K-9-24 二·⑤` 取代 ⇒ 待裁是否更新該閘期望。
   **⛔ 由 `CC` 逕改。**
2. 🔴 **土地後果⛔ 確認**：`R4` 左街角由「候選 `628(1)`」變為「**強制抵費地 `226.01` ㎡**（`3.5m`）／
   `116.08` ㎡（`0m`）」——**待 KL localhost 實跑 UI 核**（`GB-123` ⛔ 解除）。
3. 🟡 **`M-3` 之量值歧異**：`_GHOST_(R4)` 之面積／臨截角長與【單】重建值差 `1.6%`〜`4.4%`；
   結論方向一致但錨值⛔ 相符 ⇒ 繫 `VR-087`，待發單側判係重建法之異抑或圖資版本之異。
4. 🟡 **`GB-140` ⛔ 於本批收斂**：`priority_index` 段一 tie-break 用 `4dp`、段二鍵③ 用 `6dp`
   （`R2-10`／`補二-6`：docstring 只陳述現況、⛔ 動 `round` 位數）。
5. 🛑 **`commit 3`（段一上鎖）⛔ 開工**，候 `commit 2` 放行。

---

## §五　下一批之次號

自誤 `262`／`GB 141`／`VR 090`／戒 `41`／`K-9 27`（`W-G.9-208 §一` 落地後未再鑄）。
