# `W-G.9-4` expand 階段：單一 `W` 產生者（🔴 **生產行為零變化**）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`　**基準**：`6ab9e86`
**批號現查未占用**：`grep -rn "W-G.9-4" . --exclude-dir=worktrees --exclude-dir=.git | wc -l` ⇒ `0`

> # 🔴 **本批是否使任何生產輸出改變？ ⇒ 否。**
>
> **`E3`／`E4` 皆成立**，且 `E4` 之證據**強於施工單所要求之「逐項相同」**：
> `run_all` log 兩版**皆 319 行**，**非路徑差異之行數 ＝ 0**；
> 正規化 worktree 路徑與 `stepg` traceback 行號後 **全檔 `diff rc=0`、殘差 0 行**
> ⇒ **整份 log 逐字相同**。
>
> 🔒 這是 expand-contract 之 **expand** 側：**加路、不換路**。切換屬 `W-G.9-5`。

---

## §1 `E1`〜`E5` 逐條判定

| # | 判 | 依據 |
|---|---|---|
| **E1** | ✅ **成立** | **兩個口徑**之「新式減原式」絕對值極大 ＝ **`0.000e+00`**（16 格） |
| **E2** | ✅ **成立** | `W_0`(實測) ≡ 期望表 **`0.000000`**；恆等式殘差極大 ＝ **`1.629e-10`** |
| **E3** | ✅ **成立** | 碼面 ＋ 輸出雙證（見 §3） |
| **E4** | ✅ **成立** | 全檔 log 逐字相同（見 §4） |
| **E5** | ✅ **成立** | `_select_pool_slot` 之 `b` 語意**確為 W** ⇒ **`S-1` 未觸發** |

⇒ **`S-1`／`S-2` 皆未觸發**。

### 1-1 `E5` 之判定（⛔ 未由參數名推定·追至消費端）

`_select_pool_slot`（`grep -n "def _select_pool_slot" app.py`）之 docstring **逐字**：

> `b`＝forced 起始 W（無則 0）
> ΣRw_側 = R(b＋Σw_群) − R(b)（telescoping；R=Rw 累積表、W≥18 飽和 100%）

**消費端**（AST 定位·函式體內僅二處取 `b`）：`b_L = float(_L.get('b', 0.0) or 0.0)`／同右，
其後進 `R(b + Σw) − R(b)`，`R` ＝ `rw_from_width`（**Rw 查表·輸入為垂距**）
⇒ **`b` 確在 `W` 軸上** ⇒ `E5` 成立。

---

## §2 `E1`／`E2`：合成測試（⛔ `run_all` 不得充作 `main()` 內敘述之證據）

**為何須合成測試**：`app.py` 之族②③ 落點**巢狀於 `main()`**，`run_all` **永不執行之**
（`W-G.9-1` §3-8 已證）。

### 2-1 🔒 `E1` 之參照**非**現行 `stepg._mp_base_W0`（否則恆真）

該函式已於本批改為**委派**至新產生者 ⇒ 拿它對拍即**構造恆真**（考古 60 族之形狀）。
⇒ 參照改為**變更前 `6ab9e86` 之原式**，逐字轉錄為 `_mp_base_W0_ORIG`，
並以**閘 T** 自證轉錄無誤：對 `git show 6ab9e86:verify/stepg_pipeline.py` 之**實文**
比對 **6 條關鍵行**，**缺 0 條**。

### 2-2 `E2` 之 16 格（⛔ 全列·含不觸發格與退化格）

逐格表見 `verify/out/probe_WG94_expand_conformance.log`。要點：

- `max|(新 − 舊) − W_0(實測)| = **1.629e-10**` ⇒ **恆等式 `舊式 ＝ 新式 − W_0` 成立**。
- `max|W_0(實測) − W_0(期望·事前寫死)| = **0.000000**` ⇒ 與 commit A 之表**逐格相符**。
- 🔒 `R6/right` 為**退化格**（`W_0 ≈ +1e-6`）：**已正面列出**，⛔ 不作任何結論之通過憑據。

### 2-3 🔴 事前登記**未涵蓋**之發現（CC 現查·具名）

`stepg:822` 右組傳 `_dhr_o = −d_hat` ⇒ `â_定向` **翻號**
⇒ **生產口徑下右側 `W_0` 為登記表值之負數**（左側同號同值·實測「甲＋乙」右側四格皆 `0.000e+00`）。

🔒 **二口徑之 `E1` 皆逐位相等** ⇒ **不影響本批之「行為不變」**；
⛔ 惟 **`W-G.9-5` 切換時必須指明口徑**，否則右側符號會翻。

---

## §3 `E3`：生產輸出零變化（碼面證據）

**對拍方法**：以 AST 取兩版（`6ab9e86` vs `HEAD`）之**函式體原文**、比其 `md5`。

| 函式 | 前 | 後 | 判 |
|---|---|---|---|
| `solve_G_binary` | `c9f7c4893339` | `c9f7c4893339` | ✅ **逐字未變** |
| `iterate_G_S` | `7c78b3fb7fc4` | `7c78b3fb7fc4` | ✅ |
| `_corner_buffer_S` | `94f1f2d7b374` | `94f1f2d7b374` | ✅ |
| `_build_corner_range_v3` | `14511a42ea01` | `14511a42ea01` | ✅ |
| `_block_strip` | `9cf9b31e3b56` | `9cf9b31e3b56` | ✅ |
| `_strip_axis` | `33ee4bbc3487` | `33ee4bbc3487` | ✅ |

**`app.py` 之 diff 恰 2 個 hunk，皆為純插入**：

```
@@ -8731,0 +8732,48 @@ _S_EPS = 2.0e-4          ← 新增單一產生者
@@ -19572,0 +19621,58 @@ def main():        ← 新增併行診斷
```

⇒ `-N,0` 表示**零刪除** ⇒ `git diff --numstat -- app.py` 之刪除欄為 **0**。

**族②③ 之四個生產消費點逐字未變**（於 diff 之 `±` 行**零命中**）：
`_W_prev_left`／`_W_prev_right`／`'b': _left_buffer_S * _cos_dn`／`'b': _right_buffer_S * _cos_dn`。

**診斷落點之風險界定**：新值只寫 `f3_forced_offset_diag[blk_label]` 與 `print`。
該 dict 之**三處讀取端**（`app.py` 之 `:20478`／`:20548`／`:20573`·AST 字串常數命中共 4 處，
1 寫 3 讀）**皆取特定內層鍵**（`v.get('left_forced')` 等）、**僅迭代外層街廓鍵**
⇒ **加內層鍵零風險**。

---

## §4 `E4`：`run_all` 逐項對照

**基準** ＝ `verify/out/D2b24_runall.log`
——其有效性由「生產碼自 `78079fa` 起未動」保證：
`git log --format=%h 78079fa..HEAD -- app.py verify/stepg_pipeline.py | wc -l` ⇒ **`0`**（於 commit A 時現查）。

**本次** ＝ `verify/out/WG94_runall.log`。

```
$ wc -l verify/out/D2b24_runall.log verify/out/WG94_runall.log
  319 verify/out/D2b24_runall.log
  319 verify/out/WG94_runall.log
$ diff 兩檔 ⇒ 差異 44 行
$ 差異中「非 worktree 路徑」之行數 ⇒ 0
$ 正規化（worktree 路徑 ＋ stepg traceback 行號）後 diff ⇒ rc=0、殘差 0 行
```

🔒 **兩類差異之成因皆已具名**：
① **worktree 路徑**——基準產於 `k6a2-d2b21-wiring-6ef500`、本次產於本 worktree；
② **`stepg` traceback 行號** `1357 → 1362`——**恰為本批於 `stepg` 所加之 5 行**（`+3` ns 取用 ＋ `+7−6+1` 委派）。

⇒ **PASS 12／FAIL 22**（兩版同值），且**逐項名目 diff 為 0 行**——
⛔ 本節**非只報總數**：上開為**全檔逐行**比對，強於逐項。

---

## §5 §7 驗收逐閘

| # | 閘 | 依據 |
|---|---|---|
| 1 | 16 格全列（含不觸發與退化） | ✅ `probe_WG94_expand_conformance.log` 之 `E2` 表 16 列 |
| 2 | `E1`／`E2` 來自合成測試 | ✅ 直呼 `k956_W_from_mp`／`_mp_base_W0_ORIG`；⛔ 未以 `run_all` 充證 |
| 3 | `E3` 之對拍方法與殘差 | ✅ §3（函式體 md5 ＋ hunk 型態 ＋ 消費點零命中） |
| 4 | `E4` 逐項對照 | ✅ §4（**全檔逐行**·非只總數） |
| 5 | 產生者為 module 級 | ✅ AST：頂層 `True`／被巢狀 **0** 次 |
| 6 | 未增加第三份 W 定義 | ✅ 見下 5-1（附三項射程） |
| 7 | 每數之指令當次重跑並列 | ✅ 全文之數皆附指令與輸出 |
| 8 | 否定性宣稱附三項射程 | ✅ 5-1 ／ §3 之「零命中」 |
| 9 | `wf_f*` numstat 空；`app.py` 刪除欄 0 | ✅ `wf_f*` 空輸出；`app.py` `106  0` |
| 10 | 文件更正一律加註·刪除欄 0 | ✅ `GB-47` 加註列（錨 `grep -c "^| \*\*GB-"` 仍 **60**） |

### 5-1 閘 6 之否定性宣稱（⛔ 三項射程齊備）

- **【目錄射程】** 全倉 `*.py`，⛔ 排除 `.claude/worktrees/`；母體 **114 檔**。
- **【字樣射程】** 語意合取（⛔ 不預設符號名）：
  `(np.dot|_np_d.dot|內積)` ∧ `(mp|side_mid|中點)` ∧ `(adir|allocation_dir|n_alloc|â)`。
- **【連接方式】** **同一 `ast.FunctionDef` 之函式體內**，⛔ **非**固定距離窗 `.{0,N}`。

**結果**：`verify/stepg_pipeline.py` 之 `_mp_base_W0` **已無自寫算式**（改委派）；
`app.py` 新增之 `k956_W_from_mp` **即取代之**；`solve_G_binary` 之內算**逐字未變**（§3 md5）。
⇒ **未因本批增加第三份**。

⚠️ **一項自誌**：我首版以「委派 vs 自定義」之啟發式分類器算出「4 → 2」，
但該分類器對 `main()` 之判定**不可靠**（其 `_np_d.dot` 被字串替換洗掉）
⇒ **已棄用該數**，改以上開**可逐一驗證之窄命題**（函式體 md5 ＋ 委派之 6 行原文）為據。

---

## §6 `W-G.9-5` 待辦（contract 側）

1. 🔴 **切換族②③ 之生產消費**：`_W_prev_*` 與 `_select_pool_slot` 之 `b`
   由 `buf · _cos_dn` 改取 `k956_W_from_mp(...)`。
   ⚠️ **土地後果非零**：二式之差 ＝ `W_0`，實測範圍 `−1.693924` 〜 `+1.691802`，
   **`|W_0| < 1e-9` 之格 ＝ 0／16** ⇒ **每一格都會變**。
2. 🔴 **必須指明口徑**（§2-3）：右側之生產口徑 `W_0` 為登記表值之**負數**。
3. ⚠️ 切換必使 `run_all` 之基準過期 ⇒ 須與**重烤時點**協調
   （`D-3` 仍受 `K-9-5-10`【裁】1／2 拘束：等 `R1` 結構閘另案修好之後才做）。
4. ⚠️ `GB-58`／`GB-59`（∥SIDELINE 多邊形與族②③④ 之耦合）之處置**併入 `W-G.9`**。

---

## 🔧 加註（`W-G.9-4b`·CC／2026-08-11·⛔ 上文原句一字未刪）

> 🔴 **本報告 §1 之 `E3` 判定「✅ 成立」<u>撤回</u>。**
> 本批**確實使生產行為改變**：`verify/stepg_pipeline.py` 新增 `ns["k956_W_from_mp"]`，
> 而 `app.py` 之 `_WF_NS_NAMES` **未同步登錄該名** ⇒ app 生產路徑
> （「執行七級調配」→ `_build_wf_ctx` → `_wf_ns()` → `wf_f0.compute` → `run_step_g`）
> 之 ns 取不到該名 ⇒ **`KeyError` 於 `stepg_pipeline.py:334`**。
> ⇒ 本批事前登記 §3 之 **`S-2` 於當批即已觸發，而當批未偵測到**。

### 加-1 `E3` 撤回之實證（⛔ 非推定）

| 證據 | 出處 |
|---|---|
| AST：`ns["k956_W_from_mp"]` 落於 `run_step_g` 函式體、祖先鏈**無** `Try`／`If` ⇒ **無條件執行** | `verify/out/probe_WG94b_wiring_PRE.log`【A 層】 |
| 實呼：以 `_wf_ns()` 建出之受限 ns 跑 `run_step_g` ⇒ **實拋** `KeyError: 'k956_W_from_mp'` @ `:334` | 同上【B 層·B-2】 |
| 缺陷態全檢：於 `1041874` 跑守護夾具 ⇒ 靜態差集 `['k956_W_from_mp']`、動態 `KeyError @ :334`、`rc=1` | `verify/out/fixture_wf_ns_wiring_缺陷態1041874.log` |

**§3 之碼面證據為何未攔下**：§3 所證者為「`app.py` 之**生產函式體**逐字未變」
（函式體 md5 ＋ hunk 為純插入 ＋ 族②③ 消費點零命中）——**皆為真**。
🔒 **斷點在於：本缺陷不在任何函式體之內，而在 `_WF_NS_NAMES` 這個<u>清單</u>該不該多一個名。**
⇒ 「函式體未變」與「生產路徑仍可走」**是兩個命題**；§3 只證了前者。

### 加-2 `E4` ——**判定維持、措辭更正、證據力撤回**（⛔ 逐項分述）

⚠️ **本節與施工單 `W-G.9-4b` §2-4 之文字有出入**：該單指示「加註撤回 `E3`／`E4` 之『✅ 成立』」。
CC 現查後**只撤回 `E3`**，`E4` 改為三分處置——理由：`E4` 之**登記原文**
（「`run_all` 之 PASS/FAIL 層與 `6ab9e86` **逐項相同**」）**經現查為真**，
⛔ 把一個為真之陳述整條撤回，會在倉內留下新的不準確。**偏離已於此具名。**

| 項 | 處置 | 依據（指令與輸出並列·本次重跑） |
|---|---|---|
| `E4` 之**登記原文**（逐項相同） | ✅ **維持** | `diff` 之 44 行差異**全部**含 `worktrees`；非 worktree 路徑行 ＝ **0** |
| §4／紅框之**措辭**「整份 log 逐字相同」 | 🔧 **更正為「正規化後逐字相同」** | `md5sum` **兩檔不同**：`8762557103cac46a7fd5bf13f280b514`（`D2b24_runall.log`）／`1aaad8d7ed1d689c3bcd4c2bc7a057b8`（`WG94_runall.log`） |
| `E4` 作為**「生產行為零變化」之證據力** | 🔴 **撤回** | `run_all` 全程走 `app_harvest.harvest()` 之**全 globals**，**從不**建 `_wf_ns()` 之受限 ns ⇒ 對本缺陷**結構性零覆蓋**——`E4` 不論綠紅都不可能反映之 |

```
$ md5sum verify/out/D2b24_runall.log verify/out/WG94_runall.log
8762557103cac46a7fd5bf13f280b514 *verify/out/D2b24_runall.log
1aaad8d7ed1d689c3bcd4c2bc7a057b8 *verify/out/WG94_runall.log
$ diff verify/out/D2b24_runall.log verify/out/WG94_runall.log | wc -l
44
$ diff verify/out/D2b24_runall.log verify/out/WG94_runall.log | grep "^[<>]" | grep -v worktrees | wc -l
0
```

### 加-3 §5 閘 6 之否定性宣稱 —— **為真，但與本缺陷正交**

⚠️ **本節亦與施工單文字有出入**：該單指示加註「閘 6 之否定性宣稱**射程未涵蓋 `_wf_ns()` 接線層**」。
現查：閘 6 之**目錄射程確已涵蓋** `app.py`（§5-1 逐字：「全倉 `*.py`…母體 **114 檔**」）。
⇒ 洞**不在目錄射程**，而在**字樣射程之受詞**：

- 閘 6 之字樣為 `(np.dot|_np_d.dot|內積) ∧ (mp|side_mid|中點) ∧ (adir|allocation_dir|n_alloc|â)`
  ——所找者為 **W 之算式**。
- 本缺陷之受詞為 **`_WF_NS_NAMES` ↔ `ns[<字面字串>]` 之接線登錄**，**內無任何上述字樣**。

⇒ 閘 6 之宣稱（「未增加第三份 W 定義」）**成立且未被推翻**；
🔒 真正之缺口是：**`W-G.9-4` 全批<u>無任何一道閘</u>檢查「新增之 `ns[...]` 消費是否已登錄於 app 清單」**
——⛔ 不是某道閘沒掃到，是**該命題從未被立為閘**。

### 加-4 處置與現況

- **`S-2` 之結案手段**：經 claude.ai 裁定（施工單 `W-G.9-4b` §2-1）改採**前推修復**（roll-forward），
  **不回退** `2e6afc6`；判準未放寬。逐條理由見 `docs/reports/W-G.9-4b_前置登記.md` §4。
- **修復**：`"k956_W_from_mp"` 已補入 `_WF_NS_NAMES`（25 → **26** 名目）。
- **新守護**：`verify/fixture_wf_ns_wiring.py` 已掛入 `verify/run_all.py` 之末端夾具清單。
- 全批處置與殘留見 `docs/reports/W-G.9-4b_接線修復報告.md`。
