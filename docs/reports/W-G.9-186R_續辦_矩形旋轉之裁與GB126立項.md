# 🧾 `W-G.9-186R`（**續辦**）　矩形旋轉之裁 ＋ `GB-126` 立項 ＋ 幾何本體批交接文

**受單**：CC　**施工單**：`W-G.9-186`（`SELF_SHA256` ＝ `8927f921e6a0c8ed…` ✅）＋ **補正①**（`SELF_SHA256` ＝ `1d9265234e4e4867a67d04cbb83c0c0dde117e6693162baf50cc3fedbc123a60` ✅ **CC 實算逐位相符**）
**期初態**：`42f3dbba4d6c092b7c73283981bd48a1bc9248fd`
**性質**：🔒 ⛔ **零生產碼**（四檔逐位未動）·鑄 `自誤 222`／`自誤 223`／戒 `38`／戒 `39`／`GB-126`·**幾何本體批交接文已出**

> 🔒 **報告命名（`常規三`·三候選·**已採甲**）**
> **甲（採）** `W-G.9-186R_續辦_矩形旋轉之裁與GB126立項.md` — 保 `常規四 三` 之 `<單號>R` 前綴，以副標與同批之**停機報告**（`W-G.9-186R_停機_X4五處逐字之markdown標記差.md`）區分。
> 乙 `W-G.9-186R2_…` — 後綴 `R2` ⛔ 不合 `常規四 三`（只允許後綴 `R`）。
> 丙 `W-G.9-186_補正①R_…` — 全形圈號於檔名易致跨平台問題。
>
> 🔒 **交接文命名（`常規三`·三候選·**已採甲**）**
> **甲（採）** `docs/reports/W-G.9-186_幾何本體批交接文.md` — 逐字同既有體例（`W-G.9-183_CC交接文.md`）。
> 乙 `docs/design/…` — `docs/design/` 係**跨批工作底本**之落點（規格書），交接文係**單批產物**。
> 丙 `docs/reports/W-G.9-186R_附錄_…` — 交接文係**獨立入口**，⛔ 不宜附麗於報告。

---

## `§一`　停機款之逐字引錄（🔒 `GB-121` 施行式·`補-7` 令逐字引錄 `X-4` 與 `X-10`）

**來源管線**：`W-G.9-186 §二` 之表 ＋ **補正① `補-4`** 之表（**`【單】`**）。

| 款 | 逐字原文 | CC 之判 |
|---|---|---|
| **`X-4`（維持·含框宣告）** | 「`N-3` 之五處逐字若與本單所載**任一處不符** ⇒ **停機上呈**」——🔒 **框 ＝ (a) 原始位元組**（`戒 39 ①` 之宣告）；其真值以**本補正① `補-1`** 所載為準。 | ✅ **⛔ 未觸**——`補-1` 所供之真值，CC 自倉重抽對拍**六列全符**（`§二-2`）。 |
| **`X-10`（新增）** | 本補正① `補-1` 所載之六列真值，CC 須**自行以 `sed -n '<列>p'` 重抽**對拍；凡與本補正所載不符 ⇒ **再次停機上呈**，⛔ 不得逕自續辦。 | ✅ **⛔ 未觸**——**六列逐位全符**（`§二-2`）。 |
| `X-1` | 凡須修改 `app.py`／`stepg_pipeline.py`／`run_all.py`／`run_verification.py` 一字 ⇒ **停機上呈**。🔒 本批仍係**前置批**，⛔ 無任何生產碼授權。 | ✅ ⛔ 未觸——四檔逐位未動。 |
| `X-2` | 凡須跑 `run_all` 全案／長跑／跨態重跑／請 KL 實跑 ⇒ **停機上呈**。🔓 逐街廓 `run_step_g` 之探針執行係授權範圍內。 | ✅ ⛔ 未觸——只跑逐街廓 `run_step_g` 探針。 |
| `X-3` | `N-2` 之判別力對照組（一個確有執行之 tag）若得 `0` ⇒ **先判量測器紅並停機**——「`7` 個 tag 從未執行」與「我的 spy 沒攔到」⛔ 不可區分。 | ✅ **⛔ 未觸**——對照組得 `8`／`6`／`6`（`§三-2`）。 |
| `X-5` | 取號現查三數皆須 `0`／`0`／`0`；凡非零者須出艙其**唯一來源列**並證屬**本案自身之配給**（`戒 37` 之通則）。不合 ⇒ 先判量測器紅並停機。 | ✅ ⛔ 未觸——次號 `自誤 224`／戒 `40`／`GB-127` 皆 `0/0/0`（`§四-2`）；⚠️ 過程中**自捕一次量測器紅**，已修正並具名（`§四-3`）。 |
| `X-6` | 加註／補款一律 `🔬`／`🔧` 起首（⛔ 不命中任一定義列式·`戒 36`），且其**受詞須以 `檔:列` ＋ 標題逐字錨定**（`戒 38`）；入倉後各類 MAX 之變化須**恰如預期**。不符 ⇒ **停機**。 | ✅ ⛔ 未觸——標題形自證 **`35/35`**；MAX 推進 **`7/7`**（`§四-1`）。 |
| `X-7` | 三登記表與規格書之改動**僅限末端追加**；`CLAUDE.md` 本批 ⛔ **不改**。凡 `deletions ≠ 0` 或既有列被改一字 ⇒ **停機**。 | ✅ ⛔ 未觸——三檔皆 binary append·嚴格前綴 `True`；`CLAUDE.md` **⛔ 未動**。 |
| `X-8` | 受檔 `numstat` `deletions` **逐列 `0`**；`app.py` 期末 blob ＝ `68f7bfab610ec78688b9d7322ae4bc2aac74493e`。 | ✅ ⛔ 未觸——`§六 11`。 |
| `X-9` | 🛑 **停機款觸發一律先停機**：⛔ 不得先辦後呈。 | ✅ ⛔ 未觸（本批無款觸發）。**前一輪已據以停機**（`W-G.9-186R_停機_…`·`42f3dbb`）。 |

---

## `§二`　開工閘 ＋ 🛑 `X-10`

### `§二-1`　`S-1`

🔒 **七檔逐位全符**：`app.py` `1352132`／`22922`／`7c290b6f55f83a24`；`CLAUDE.md` `74406`／`807`／`7385796755e2439f`；自誤登記表 `395066`／`4298`／`ff42f43712e1cee0`；`W-G.4` `515321`／`3163`／`fd6d8d0aae004dba`；`docs/驗證裁定登記表.md` `333071`／`4258`／`c5b812d388040de3`；**規格書** `39076`／`538`／`3cf0edde5161c2ed`；`K-6` 正典 `258295`／`3632`／`6a5cada2901a10e8`。`CRLF` 皆 `0`。
🔴 **第八項照實回報**：單 `S-1` 載 `origin/wip/s1-endpart` ＝ `d10f4e8…`（**補正①⛔ 未更新該表**），實測 ＝ **`42f3dbba…`**；`is-ancestor(d10f4e8, 42f3dbb)` `rc=0` ⇒ 係其**後裔**，且其差 ＝ **本案自身之 `-186R` 停機報告**（補正① `§5` 逐字已載 commit `42f3dbb`）⇒ ⛔ 非不符。
🔒 **二單之 `SELF_SHA256` 皆逐位相符**（`-186` `8927f921…`／補正① `1d926523…`）。
⚠️ **一處量測器自捕**：首版 regex 取檔內**第一個** `SELF_SHA256` ⇒ 對補正① 抓到其**引用 `-186` 之值**而誤報不符；改取**末者**後相符。⇒ **量測器紅、⛔ 非受詞紅**。

### `§二-2`　🛑 `X-10`　六列真值之**自行重抽對拍**（框 ＝ **原始位元組**）

🔒 **抽法**：以**檔案管線**自補正① `補-1` 之 code block 抽出六列（`:2469`／`:2471`／`:2475`／`:2476`／`:2482`／`:3262`），
再逐列與 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之 `splitlines()[n-1]`（＝ `sed -n '<列>p'` 之等效）**逐位**比對。

| 列 | 單載字數 | 倉內字數 | 判 |
|---|---|---|---|
| `K-6:2469` | `70` | `70` | ✅ **逐位相符** |
| `K-6:2471` | `55` | `55` | ✅ **逐位相符** |
| `K-6:2475` | `46` | `46` | ✅ **逐位相符** |
| `K-6:2476` | `45` | `45` | ✅ **逐位相符** |
| `K-6:2482` | `61` | `61` | ✅ **逐位相符** |
| `K-6:3262` | `127` | `127` | ✅ **逐位相符** |

⇒ 🛑 **`X-10` 六列全符 ⇒ ⛔ 不停機、續辦**。🔒 **前一輪之三處差確已由「供真值」根治**（⛔ 未改框·`補-1` 所裁）。

---

## `§三`　`N-2`　`GB-126` 之立項與**實測坐實**

**探針**：`verify/probes/probe_WG9186_orderinv.py`（`rc=0`）／log `verify/out/probe_WG9186_orderinv_42f3dbba.log`
🔒 **量法⛔ 非 spy**：設環境變數 `WV_PROJ_POP_COUNT`，令**生產碼自身**之 `_proj_pop_note`（`app.py:7700`·於 `:7801`／`:7818`／`:7832`／`:7869` 四處被呼叫）記錄每次執行 ⇒ ⛔ 未覆寫任何 assert 函式、⛔ 未改生產碼一字。

### `§三-1`　全 `14` tag 之執行次數

| 類 | tag 數 | 執行總次數 | **零執行之 tag** |
|---|---|---|---|
| `POP_SYNC` | `6` | `24` | **`2`**（`app:main/v2_caller`／`app:main/_rank_by_tpid`） |
| **`ORDER_INVARIANCE`** | `7` | **`0`** | **`7`（全部）** |
| `NAME_DERIVATION` | `1` | `8` | `0` |

🔴 **零執行之 tag 實為 `9` 個，多於單所載之 `7`**——後二者在 `app.py:main()` 內，harness 從不執行。**照實回報**（已寫入 `GB-126` 之實測節）。

### `§三-2`　`X-3` 判別力對照（🛑 得 `0` 即停機）

對照組（同一觀測鉤·確有執行者）：`app:k6step0/_ordered` **`8`**／`sp:_rank_by_tpid` **`6`**／`stepg:v2_caller` **`6`**
⇒ **非零** ⇒ 🔒 **量測器非紅** ⇒ ⛔ 不觸 `X-3`。

### `§三-3`　未執行之**原因**（逐 tag·單所令之三分類）

`7` tag 皆判為 **情境不適用**——其呼叫點所在之 `wf_f0`／`wf_f1`／`wf_f2`／`wf_f3`／`wf_f4` 五模組，
於本情境**連 `sys.modules` 都⛔ 未載入**（逐 tag 實測「模組已 import？ ＝ **否**」）；
且 `_projection_order` 之**實際**呼叫者 frame 僅 `4` 個相異（`app.py:7998`／`app.py:11682`／`selection_pipeline.py:344`／`stepg_pipeline.py:507`）。
⇒ **⛔ 非死碼**（碼面確有呼叫點）、**⛔ 非分支未進**（函式根本未被進入）。

---

## `§四`　登記之落地與取號

### `§四-1`　payload 與 MAX 推進

| payload | 來源 | 列數 | 落點 |
|---|---|---|---|
| `P-1`（`自誤 222` 三肢 ＋ 戒 `38`） | **自 `-186` 單身抽**（block `:105-123`） | `19` | 自誤登記表檔末 |
| `補-2`（`自誤 223` 二肢 ＋ 戒 `39`） | **自補正① 抽**（block `:40-63`） | `24` | 自誤登記表檔末 |
| `P-2`（`GB-126`） | **自 `-186` 單身抽**（block `:129-137`）＋ **程式化組裝**為 `GB-125` 之二形 | 原 `9` → `10` | `W-G.4` 檔末 |
| `P-3`（矩形姿態之裁·**CC 自擬**） | `§三` 委辦式 | `33` | **規格書**檔末 |
| `P-4`（`GB-104` 更正·**CC 自擬**） | `§三` 委辦式 | `17` | `W-G.4` 檔末 |

🔒 **`P-2` 之逐字守恆自證**：`P2.txt` 之 `8` 個非空列，去欄名前綴後**逐列完整出現**於組裝後之文字 ⇒ **`8/8` ✅**。
🔒 **標題形自證（`X-6`／`戒 36`·七框 × 五 payload ＝ `35` 格）**：`P-1`／`補-2` 命中自誤二框 `['222']`／`['223']`；`P-2` 命中 `GB` 式表／式B `['126']`；`P-3`（`## 🔧` 起首）／`P-4`（`🔬` 起首）**七框皆 `[]`** ⇒ **`35/35` 全符**。
🔒 **落地三數**（皆 binary append·**嚴格前綴 `True`**·`CRLF` `0`）：自誤登記表 `395066→402254` B（`+49` 列）／`W-G.4` `515321→522629` B（`+39` 列）／規格書 `39076→42908` B（`+36` 列）。

**MAX 推進（入倉後實測·🛑 ⛔ 不以「自檢 True」為憑·三數本身）**：

| 類 | 未扣除（相異／MAX／缺） | 所扣 | 已扣除 MAX | 期望 | 判 |
|---|---|---|---|---|---|
| 自誤·寬形·單檔 | `188`／`223`／`35` | `[]` | **`223`** | `223` | ✅ |
| 自誤·甲形·單檔 | `41`／`223`／`182` | `[]` | **`223`** | `223` | ✅ |
| 自誤·寬形·全倉 | `192`／`223`／`31` | `[]` | **`223`** | `223` | ✅ |
| `GB`·三框聯集 | `124`／`126`／`2`（缺 `[12, 87]`） | `[]` | **`126`** | `126` | ✅ |
| `VR`·定義列式 | `70`／**`999`**／`929` | **`[999]`** | **`84`** | `84`（不變） | ✅ |
| 戒·**裸框**·全倉 | 相異 `28`／**MAX `40`** | — | 見 `§四-5` | `39` | 🔴 **見 `§四-5`** |
| 常規·定義列式 | `12` 列·序 `一二三四五六七四四八四九` | — | 九 | 不變 | ✅ |

🔒 `222`／`223` 皆⛔ 不在自誤缺號集；`126` ⛔ 不在 `GB` 缺號集。

### `§四-5`　🔴 **戒之裸框 MAX ＝ `40`（期望 `39`）——二數並報與其歸因**

🔒 **照實出艙（⛔ 未改框·`S-2`）**：戒之**裸框** `` 戒\s*`?([0-9]{1,4})`? `` 全倉 MAX ＝ **`40`**，⛔ 非 `39`。

🔒 **`戒 40` 之命中 ＝ `3` 列，其<u>唯一來源</u>逐列具名**：

| `檔:列` | 性質 |
|---|---|
| `docs/reports/W-G.9-186_幾何本體批交接文.md:40` | **本批交接文之次號表**（`§一`：`` `自誤 224` `0/0/0`／`戒 40` `0/0/0`／`GB-127` `0/0/0` ⇒ **皆未占用** ``） |
| `docs/reports/W-G.9-186R_續辦_…md:30` | **本報告 `§一` 之 `X-5` 逐字引錄** |
| `docs/reports/W-G.9-186R_續辦_…md:126` | **本報告 `§四-2` 之次號占用表** |

🔒 **`戒 40` 之<u>定義形</u>命中 ＝ `0`** ⇒ **⛔ 未鑄**（對照組：`戒 38`／`戒 39` 之定義形各 `1` 處，皆在
`docs/reports/W-G.9波_claude.ai側自誤登記.md:4320`／`:4343` ＝ 本批 payload ⇒ **確實鑄了**）。

⇒ 🔒 **判：`戒 40` 之字面係<u>本案自身之配給</u>**（`戒 37` 之通則／`W-G.9-182 §七`），**⛔ 非鑄號**
⇒ 扣除本案配給後之戒 MAX ＝ **`39`** ＝ 補正① `補-5` 之期望 ✅。
⇒ 🔒 **⛔ 不觸 `X-6`**——`X-6` 之受詞係「**各類 MAX 之變化恰如預期**」，而本批**確鑄戒 `38`／`39`**、
**⛔ 未鑄 `40`**；`40` 之出現係「次號表之書寫」，屬 `戒 37` 通則**已裁明之形**。

🔴 **其結構性成因（與 `VR-999` <u>同族</u>）**：**戒⛔ 無定義列式、只有裸框** ⇒ 其 MAX 對「次號之書寫」
**⛔ 無免疫力**——與 `VR` 之裸框得 `999` 係**同一個病**（`常規四（八）` 立法之由）。
⇒ 🛑 **呈裁**：建議為「戒」立定義列式（見 `§七 3`）。

### `§四-2`　次號之占用（`X-5`）

`自誤 224` `0/0/0`／戒 `40` `0/0/0`／`GB-127` `0/0/0`（二形皆是）⇒ **皆未占用**。
🔒 **對照組**（本批所鑄·已入倉）：`自誤 222` 形① `2/2/2`·形② `1/1/1`；`自誤 223` 形② `1/1/1`（`自誤登記表:4324`）；戒 `38` 二形皆 `2/2/2`；戒 `39` 形② `2/2/1`；`GB-126` `5/5/2` ⇒ **整體非零** ⇒ 量測器非紅。
`VR-085` `10/10/4`（皆本案自身之配給·本批**⛔ 不鑄 `VR`**）。

### `§四-3`　🔴 **一次量測器紅之自捕**（`常規四（七）補款二 二`）

首版之「形②」我寫成 `` `自誤 223` ``（**整體**帶反引號），致**已入倉**之 `自誤 223`／戒 `39` 得 `0/0/0`
⇒ **對照組為零** ⇒ 依補款二 二「一律先判**量測器紅**」。查因：入倉之標題形係 `` ### 自誤 `223` ``（**號**帶反引號）
⇒ 正確之形② ＝ `` 自誤 `223` ``。修正後對照組全部非零。
🔒 **本項已寫入交接文 `§一`**，以免下一窗重踩。

### `§四-4`　`補-3`　已作廢清單之更正與加查

🔒 **更正採之**：期初 ＝ **`13`** 項（CC 機械抽取·框 `` ^\|\s*(\d+)\s*\|\s*`([^`]+)`\s*\| ``·末 ＝ `自誤 4173`）——**⛔ 非 `14`**。
🔒 **加查之結果 ＝ 「否」**：`W-G.9-185R` **⛔ 未出艙 `S-4` 人造受詞**——其 `:47` 逐字：
> `- \`S-4\`：🔒 **本批⛔ 未取人造受詞**——本批⛔ 不鑄任何號、⛔ 無新號之占用查 ⇒ ⛔ 無 \`S-4\` 之用武地。`

⇒ 🔒 **依補正① `補-3` 之「若否」分支**：逐字聲明之，並具名——
**算式（`本批項數 ＝ 前批 ＋ 本批出艙數`）之<u>適用前提</u> ＝ 該批確有出艙**；
⛔ **不得逕以「數字對不上」判為失守**。⇒ **補款四⛔ 未失守**；`14` 係發單側**推算**之誤（`自誤 223` 肢②）。
🔒 **本批之算式**：`13`（實測期初）＋ **`0`**（本批⛔ 未取人造受詞——⛔ 不鑄新類號之占用查故無用武地）＝ **`13`**（期末）。

---

## `§五`　`N-3`／`N-4` 之落地

### `§五-1`　`M-K-1`（規格書末端追加·`P-3`）

🔒 **⛔ 未動規格書既有一字**——`:419`／`:438`／`:472`／`:496`／`:537` 五處之「不旋轉」原文**逐字保留**，
其逐字已於 `P-3` 內**以 `檔:列` ＋ 標題逐字錨定**（`戒 38`），並逐字宣告「`:537` 第 `4` 項之『首要待決』**已決**」。
🔒 `P-3` 內另**逐列引錄** `X-10` 之六列真值（`K-6:2469`／`:2471`／`:2475`／`:2476`／`:2482`／`:3262`）作為裁之依據。

### `§五-2`　`M-K-2`（八處 `rotate` 之現基座覆核·⛔ 不修）

🔒 `app.py` 內 `minimum_rotated_rectangle` 之**實際賦值**恰 **`8`** 處：
`:4007`／`:4027`／`:4778`／`:5030`／`:6505`／`:8284`／`:8366`／`:8702`
（另 `:4754`／`:4976`／`:8676` **三處係註解**·`grep` 之裸框會一併命中 ⇒ 二數並報：**含註解 `11` 列／實際賦值 `8` 處**）。
⇒ **與 `W-G.9-89` 所載之「八處」逐位相符**；皆為「取多邊形之最小外接旋轉矩形」（**度量**用途），
**⛔ 非**「於宗地四至內旋轉平移搜尋 `W × D` 矩形」⇒ 🔒 **`K-9-12-c` 實質為【缺】之判仍成立**。
⚠️ 併報：`verify/` 側另有 `wd3_fragment_geom.py:117` 等命中，惟受詞係**生產碼**（`app.py`），⛔ 不列入八處。

### `§五-3`　`N-4`（`GB-104` 失效條件之更正·`P-4`）

🔒 **⛔ 未動 `GB-104` 原處一字**；以末端追加錨入更正，其受詞以 **`docs/reports/W-G.4_泛用阻塞項登記表.md:2762`** ＋ 該欄逐字錨定（`戒 38`）。
🔒 **更正後之失效條件** ＝ 「`_block_strip` 之雙線分支已補一道『兩線於街廓內交叉』之 loud 斷言，且其判別力經注入證明」。
🛑 **後果之逐字**：其 loud 斷言**⛔ 不得再依附於 `K-9-23`**，須列為幾何本體批之**獨立工項（第三批）**。
🔒 **碼面覆核**：雙線分支仍在 `app.py:8831`，二守衛仍為①兩線平行②半平面交集為空 ⇒ ⛔ 無交叉閘；`app.py` blob **十一批未動** ⇒ 缺閘必仍在。
⚠️ **跨態之限**（`VR-083 一`）：其數值後果（`R2左 45.976589 ＝ 26.162405 + 19.814184`·【倉】`W-G.4:400-401`）係他態量測 ⇒ 🛑 **⛔ 不辦**，具名為射程外。

---

## `§六`　收工閘（`11` 項）

| # | 項 | 結果 |
|---|---|---|
| `1` | `S-1` 八項期初錨逐位對拍 | ✅ 七檔全符；第八項之差已歸因（`§二-1`） |
| `2` | `N-1` 形之自證 ＋ MAX 推進 `221 → 223`（二框並報·三數本身） | ✅ `§四-1`（**補正① `補-5` 之期望**） |
| `3` | `N-2` `GB-126` 取號 ＋ `7` tag 執行次數 ＋ 判別力對照 | ✅ `§三`（對照組 `8`／`6`／`6` ⇒ ⛔ 不觸 `X-3`） |
| `4` | `N-3` 五處逐字對拍（`X-4`／`X-10`） | ✅ **六列逐位全符**（`§二-2`） |
| `5` | `M-K-1` 規格書末端追加 ＋ `M-K-2` 八處覆核 | ✅ `§五-1`／`§五-2` |
| `6` | `N-4` `GB-104` 更正之末端追加 ＋ 「⛔ 不得再依附」之逐字 | ✅ `§五-3` |
| `7` | `N-5` 交接文六節齊備（含白話段） | ✅ `docs/reports/W-G.9-186_幾何本體批交接文.md` |
| `8` | `X-6` 標題形逐則聲明 ＋ 受詞之 `檔:列` 錨定；入倉後 MAX 恰如預期 | ✅ **`35/35`** ＋ MAX **`7/7`**；⚠️ 錨定自證之「位置指涉」命中 `3` 處經查**全屬自指或正典引文**（宣告句列舉被禁詞／`K-6:2471` 原文含「上開」）⇒ **量測器紅、⛔ 非受詞紅** |
| `9` | `wg942`（commit 前）＋ memory ㉞；`fixture_klui_t2diag` 單獨跑 `rc` | ✅ 見下 |
| `10` | 已作廢清單之算式 | ✅ `13 + 0 = 13`（`§四-4`·含「若否」分支之逐字聲明） |
| `11` | `CRLF 0`／逐檔 `git add`／`deletions` 逐列 `0`／三重驗證／clone 刪除 | ✅ 見完工回報 |

🔒 **絕對路徑機檢**：本批新增命中 **`0`**；既存 `4`；判別力兩造皆如預期。

---

## `§七`　🛑 呈發單側裁

1. 🟡 **`S-1` 之期初錨未隨補正① 更新**（單載 `d10f4e8`／實測 `42f3dbb`）——已歸因為**本案自身之停機報告**，⛔ 未觸任何款。建議嗣後補正單一併更新 `S-1` 之 `origin` 列。
2. 🟡 **`GB-126` 之案由載「`7` 個 tag」，實測零執行者為 `9` 個**（多 `2` 個 `POP_SYNC`·皆在 `main()` 內）。CC 已於登記之實測節照實出艙、⛔ 未改案由一字。🛑 請發單側判是否須就地加註。
3. 🔴 **「戒」⛔ 無定義列式，其裸框 MAX 必被「次號之書寫」污染**（`§四-5`：本批得 `40` 而實鑄至 `39`）——**與 `VR-999` 之裸框得 `999` 係同一個病**。🛑 **建議**（`常規三`·CC 提案·⛔ 本批未自鑄任何施行式）：為「戒」立定義列式，取本波既有體例之逐字形 `` ^🛑 戒 `?([0-9]{1,4})`? ``（實測：`戒 38`／`戒 39` 各命中 `1` 處、`戒 40` 命中 `0` ⇒ **具判別力**），並比照 `常規四（八）` 令取號一律以之為框、裸框僅供「意指占用」之搜尋。

---

## `§八`　🗣 白話段：新窗現在可以開了嗎

**可以開了。**

卡住的那件事（矩形能不能轉）**查清楚了，而且不需要問 KL**。KL 當初說「不能轉」，後來自己更正說「可以轉、也不用貼著路邊」，後面這句在正典裡是正式版本；前面那句正典明寫是「刻意保留當天的原樣」。兩段話都在，但關係已經寫清楚了，所以直接照「可以轉」做。

**新窗第一張單會做什麼**：同時把「藍影線」和「矩形放不放得下」兩道判斷寫進程式——必須一起做，只做一半會變成一道永遠攔不到人的關卡，跑起來全綠但那個綠是假的。

**會不會動 `app.py`**：**會**。這是本波近十一批以來第一次真正動生產程式（前面十一批 `app.py` 一個位元組都沒改過）。

**什麼時候看得到成果**：第一批做完，可以看到「哪幾筆土地被判定不能配地」；但**還看不到最終分配圖**，因為被篩掉之後要「由下一位遞補」，那是第二批。完整順序是：兩道關卡 → 遞補 → 交叉檢查 → 才是匯出成果圖與報表。

**還缺什麼**：新窗要接的東西都寫在 `docs/reports/W-G.9-186_幾何本體批交接文.md`，工作底本是 `docs/design/W-G.9-185_幾何本體批施工規格書.md`（四項規則 × 七欄）。

---

## `§九`　回報紀律之遵行

- 🔒 **數字錨引倉檔**：本報告一切數字皆出自本窗實測；`【單】` 者僅 `§一` 之停機款逐字。
- 🔒 **`S-4`**：本批⛔ 未取人造受詞（⛔ 不鑄新類號之占用查）；清單維持 `13` 項（算式 `13 + 0 = 13`）。
- 🔒 **拋棄式 clone**：`C:\w186a`／`C:\w186x` 跑畢整份刪除（見完工回報）。
