# `W-G.9-289R`　執行報告：🛑 **`§三` 硬停機款<u>觸發</u> ⇒ `§四`／`§五` ⛔ 辦** ／ 鑄 `自誤 393`

> **單** ＝ `docs/orders/W-G.9-289_施工單_GB170之修_凍存分支.md`（工項零已原封入倉·主線）
> **基座** ＝ `d01749c9072775437b913ffce48280d6ed863839`（`ls-remote` 實查·全 `40` 碼）
> **分級** ＝ 單載 **重**（含生產碼之改）；🔴 **本批實為<u>零生產碼</u>**——`§三` 之硬停機款觸發 ⇒ **⛔ 改碼一字**。
> 🛑 **出艙即止**：本報告**⛔ 判**本修是否已了 `GB-170`、**⛔ 解除／收窄** `GB-170`、**⛔ 提**修法主張、**⛔ 呈** KL。

---

## `§零`　開工閘（九項·逐項當場重跑·⛔ 採單所載之數·**同格載其產生指令**）

| # | 受詞（**產生指令逐字**） | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `d01749c9072775437b913ffce48280d6ed863839`（全 `40` 碼） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` ⇒ **非祖先**；判別力[必為祖先] ＝ `7402ebfd5300cd7b00fd9fc3f4e3a4eb299a8615` ⇒ `rc = 0` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `SELF_SHA256` 實算 ＝ 單載 ＝ `92659eabd154b2ea38aee3aa02f959ceedd65abc9aa1ef4261313b5421a9b64b`（`P-5` 受詞 `14473` B）；判別力二造（**先自證 bytes 相異**再比對）：造甲 全檔 `14551` B（`Δ +78`）`5c03acb1…`／造乙 payload 去末尾 `\n` `14472` B（`Δ −1`）`4465dc75…` ⇒ **皆相異** | ✅ |
| `4'` | 受詞自證（坑 `bf`·受詞 ＝ **所量之檔名**·⛔ 本文正文） | 檔名 ＝ `W-G.9-289_施工單_GB170之修_凍存分支.md`；[必真] 含 `W-G.9-289` ＝ `True`／[必偽] 含 `W-G.9-288` ＝ `False`（併報本文命中 `12`／`4`） | ✅ |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0`（🔴 停機款 `0`／🟡 提示 `0`）·`P-3` `def main` 區間 ＝ `14975`-`24579`；`--selftest` `rc = 0`（四造：靶① `P-3` 紅／靶② `P-4` 提示／靶③ `P-6` 提示／**反靶不紅**） | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 `1` ＞ `0`／人造字樣 `0`） | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `382`／`392`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`**·🔴 命中 `0`·末標籤 `bi` ⇒ **下一標籤 `bj`**；造甲 坑 `n` 定義處 `6` ≥ `1`／造乙 人造坑 `0`／`0` ⇒ **器非紅**（母體 ＝ `HEAD` 之追蹤 `.md` **`776`** 檔·落檔 `verify/out/WG9289R_pitindex_pre.log`·`stderr` `0` B） | ✅ |
| `7` | `git ls-files \| grep -E '^verify/[^/]+\.py$'` ＋ `app.py` | `33` ＋ `1` ＝ **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec `verify/*.py` 多咬之子層檔數 ＝ **`304`**（判準「須 `> 0`」·⛔ 定值） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **預設**·⛔ `ls-files`） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 **`298`** | ✅ |

### `§零-2`　號占用之預查

**三軸**：`(1)` 來源 ＝ `blob@d01749c9072775437b913ffce48280d6ed863839`／`(2)` 母體 ＝ 全 `docs/` **`754`** 檔（讀不到 `0`）／
`(3)` 粒度框 ＝ **檔框 ＋ 列框**（宣告框·`D3` 取嚴格式·`列框 = |D2 ∪ D3|`）。
**產生指令** ＝ `git -c core.quotePath=false ls-tree -r -z --name-only <rev> docs/` ＋ `git show <rev>:<path>`。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | **列框** | **檔框** | 鬆框 | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-289`** | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0`／`0` | 受詢 ⇒ **未占用·可用** ✅ |
| `W-G.9-288` | `2` | `3` | `2` | `0` | `5` | `3` | `13` 列／`3` 檔 | 對照組 甲[必占用] ✅ |
| （執行期組出之人造號·字面⛔ 出艙·`GB-147`） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 對照組 乙[必為零] ✅ |

---

## `§一`　工項零：本單原封入倉（主線）

| 受詞（**產生指令**） | 實測 |
|---|---|
| 所指之檔數（`git -c core.quotePath=false ls-files -z -- docs/orders/` ＋ 錨定框施於**檔名**） | **`1`**（母體 ＝ 追蹤之 `docs/orders/` **`155`** 檔）；造甲 `W-G.9-288` 之單 ＝ `1` 檔（須 ≥ `1`）✅／造乙 執行期組出之人造名 ＝ `0` 檔 ✅ |
| 行尾（`git cat-file blob <sha1> \| tr -cd '\r' \| wc -c`·**⛔ `od -c \| grep -c`**） | `CR` ＝ **`0`** ✅／`LF` ＝ `185`／bytes ＝ `14551`；判別力[必非零] ＝ `verify/out/KL_UI_3.5m_2e08a41_stdout.log` 之 blob `CR` ＝ **`2987`** |
| 三方逐位相同 | blob `sha1` ＝ `499ebcc926d08dff592849909a5bbeaa23db5e2a`；**blob 內容 `sha256` ＝ 磁碟 ＝ 源** ＝ `5c03acb12c23d85150d3f86c84afb8f81972cc097106a8b3f88bd541043d3ee7`（blob `14551` B ＝ 磁碟 `14551` B） |
| `numstat` | `185	0`（**`deletions` ＝ `0`**） |

---

## `§二`　工項一：**改前之現查**（🛑 ⛔ 改碼一字·唯讀）

### `§二-1`　款 `1`：`_corner_buffer_S` 之呼叫端（**二形並取**·⛔ 單形）

🩸 **本批自捕 `1`（出艙前攔下）**：首版之框取 `_corner_buffer_S(`，**漏抓** `ns["_corner_buffer_S"](` 之形
⇒ `verify/wf_f1.py`／`verify/wf_f4.py` **二族靜默不見**（得 檔 `2`／列 `4`·真值 檔 `4`／列 `7`）。
⇒ 改**二形並取**（`_corner_buffer_S(` ∪ `"_corner_buffer_S"](`·⛔ 含 `def` 列）。
**母體（正面列舉）** ＝ 生產碼 **`34`** 檔（`app.py` ＋ `verify/` 頂層 `*.py` `33`）；框無 `^` 錨 ⇒ **二數並報**：**檔 `4` ／ 列 `7`**。
對照組[必為零] ＝ 一必不存在之同形字樣 ⇒ `0`。

| # | `檔:行` | **函式**（AST 定） | 指派標的 | 屬 `forced` 分支？ |
|---|---|---|---|---|
| `1` | `app.py:21903` | `main` | `_left_buffer_S` | ✅（其守衛 ＝ `_fo_left`） |
| `2` | `app.py:21914` | `main` | `_right_buffer_S` | ✅（`_fo_right`） |
| `3` | `verify/stepg_pipeline.py:659` | `_run_step_g_impl` | `_left_buffer_S` | ✅（`_fo_left`·**本波之通道**） |
| `4` | `verify/stepg_pipeline.py:670` | `_run_step_g_impl` | `_right_buffer_S` | ✅（`_fo_right`·**本波之通道**） |
| `5` | `verify/wf_f1.py:216` | `compute` | `left_buffer_S` | ✅ |
| `6` | `verify/wf_f4.py:1351` | `_reshape_block` | `buf` | ✅ |
| `7` | `verify/wf_f4.py:1362` | `_reshape_block` | `buf` | ✅ |

**其回傳之消費式逐字**（本波通道·`verify/stepg_pipeline.py`）：

```
:659-661   _left_buffer_S = _corner_buffer_S(
               blk_poly, d_hat, corner_pt, allocation_dir_block,
               float(_l_min), 'left', _label=blk_label)
:670-672   _right_buffer_S = _corner_buffer_S(
               blk_poly, d_hat, corner_pt, allocation_dir_block,
               float(_r_min), 'right', _label=blk_label)
:776       left_cum_S  = float(_left_buffer_S)     ← 🔑 **`forced` 消費端**（`cum_S` 之取值）
:777       right_cum_S = float(_right_buffer_S)    ← 🔑 **`forced` 消費端**
```

（其下游：`:818` `S_remain = max(0.1, S_block_max - left_cum_S - right_cum_S)`／
`:819` `baseline_pt = (corner_pt + left_cum_S * d_hat …)`／`:1048`–`:1049` `_mp_base_W0(…, _left_buffer_S, …)`。）

### `§二-2`　款 `2`：`_build_corner_range_v3` 之回傳結構（母體 ＝ `app.py` 單檔·**AST 區間**·⛔ 文字層估界）

**AST 區間 ＝ `12676`–`12899`**。`return` 敘述 ＝ **`4`** 處（AST 實查）：

| `檔:行` | 逐字 | 所屬 |
|---|---|---|
| **`app.py:12899`** | `return rng` | 🔑 **外層唯一之 `return`**（節點型別 `Name`·shapely 多邊形） |
| `app.py:12675` | `return ax / n, ay / n` | 巢狀輔助函式 |
| `app.py:12724` | `return ((S1[0] - F1[0]) * snx + … ) / den_s` | 巢狀輔助函式 |
| `app.py:12769` | `return abs((p[0] - F1[0]) * -dy + (p[1] - F1[1]) * dx)` | 巢狀輔助函式 |

**受詢之量於該函式內之身分**（AST·⛔ 文字層）：

| 名 | 身分 |
|---|---|
| `S1_perp` | **區域值**（賦值於 `app.py:12814` ＝ `max(_seg_P0Ps * _sin_t, T)`） |
| `S1_par` | **區域值**（賦值於 `app.py:12815` ＝ `S1_perp / _sin_t`） |
| `_sin_t` | **區域值**（`app.py:12798`） |
| `_Tp` | **區域值**（`app.py:12847`·遠側界之錨點） |
| `F1`／`S1` | **區域值**（`app.py:12692`／`:12701`） |

🔒 ⇒ **回傳⛔ 含** `S1_par`／`S1_perp`／`sinθ`／遠側界之二端點——其**全部為區域值**，
外層唯一之回傳係**多邊形** `rng`。

### `§二-3`　款 `3`：`S1_par`（或等值量）於**每一** `forced` 消費端之可達性

🩸 **本批自捕 `2`（出艙前攔下）**：首版之「模組全域」蒐集器用 `ast.walk(tree)`
⇒ **把巢狀函式內之區域名也算成全域**，令 `app.py` 之 `S1_par` **偽陽為「可達」**。
⇒ 改取 **`tree.body` 頂層敘述**（`FunctionDef`／`ClassDef`／`Assign`／`Import` 之頂層標的）。
**判別力二造**：[必可達] `_corner_buffer_S` 於 `app.py` 頂層全域 ＝ **`True`**／
[必⛔ 可達] 一必不存在之名 ＝ **`False`** ⇒ 器非紅。

| `forced` 消費端所在 scope | `S1_par` | `S1_perp` | `_sin_t` | **判** |
|---|---|---|---|---|
| `app.py` `main`（AST `14975`–`24579`·scope 名 `1753`／頂層全域 `286`） | scope `False`／全域 `False` | 同 | 同 | 🔴 **`(iii) ⛔ 可達`** |
| `verify/stepg_pipeline.py` `_run_step_g_impl`（AST `277`–`1643`·scope 名 `276`／頂層全域 `6`） | scope `False`／全域 `False` | 同 | 同 | 🔴 **`(iii) ⛔ 可達`** |
| `verify/wf_f1.py` `compute`（AST `184`–`419`·scope 名 `72`／頂層全域 `20`） | scope `False`／全域 `False` | 同 | 同 | 🔴 **`(iii) ⛔ 可達`** |
| `verify/wf_f4.py` `_reshape_block`（AST `1323`–`1549`·scope 名 `85`／頂層全域 `54`） | scope `False`／全域 `False` | 同 | 同 | 🔴 **`(iii) ⛔ 可達`** |

**全倉窮舉之佐證**（母體 ＝ 追蹤之 `*.py` 全部·框無 `^` 錨 ⇒ **二數並報**）：

| 框 | 檔 | 列 | 其中**生產碼**之分布 |
|---|---|---|---|
| `S1_par` | `15` | `90` | 🔑 **生產碼僅 `app.py` 一檔**（`15` 列·**全部落於 `_build_corner_range_v3` 之 AST 區間內**）；餘 `14` 檔皆 `verify/probes/` 或 `verify/fixture_yi_construction.py`（**字樣比對之靶**·非取值） |
| `S1_perp` | `8` | `64` | 同上；`verify/run_all.py:164` 與 `verify/fixture_yi_construction.py` 之命中皆為**字串常數**（回退變異之靶） |

🔒 ⇒ **`S1_par` 之「等值量」於任一 `forced` 消費端<u>亦不可得</u>**——其唯一之既有來源即該函式之區域值。

### `§二-4`　款 `4`：`corner_range_polys[_side]` 之可達性

| `forced` 消費端所在 scope | 判 | 依據 |
|---|---|---|
| `verify/stepg_pipeline.py` `_run_step_g_impl` | ✅ **`(i)` 同一 scope 可達** | `ctx` 於 scope 內（`:413` `ctx = {'corner_range_polys': {}, …}`·`:429` `ctx['corner_range_polys'][(_crp_lbl, _crp_wh)] = ns['_build_corner_range_v3'](…)`），取法 ＝ `ctx['corner_range_polys'][(blk_label, _side)]`（`:696` 之既有取法） |
| `app.py` `main` | 🔴 **`(iii) ⛔ 可達** | scope 與頂層全域皆無 `ctx`／`corner_range_polys` |
| `verify/wf_f1.py` `compute` | 🔴 **`(iii) ⛔ 可達** | 同上 |
| `verify/wf_f4.py` `_reshape_block` | 🔴 **`(iii) ⛔ 可達** | 同上 |

### `§二-5`　款 `5`：`S1_par · sinθ` 與 `S1_perp` 之逐位對拍（**當場重得**·⛔ 轉引前批之數）

**器** ＝ `verify/probes/probe_WG9287_landeffect.py`（**倉內既有**·⛔ 另寫第二份）·`rc = 0`·`stderr` **`0`** B·
落檔 `verify/out/WG9289R_k5_closedform.log`。`forced` 側**當場重量** ＝ `0m` `1` 格 ＋ `3.5m` `2` 格 ＝ **`3`** 格。

| 格 | `\|S1_par·sinθ − S1_perp\|` | 期 ≤ `1e-9` |
|---|---|---|
| `R4/left@0.0m` | **`0.0`** | ✅ |
| `R2/left@3.5m` | **`0.0`** | ✅ |
| `R4/left@3.5m` | **`0.0`** | ✅ |

⇒ 🔒 **款 `5` <u>未</u>觸發停機**。

---

## `§三`　🛑 **硬停機款之判：<u>觸發</u>**

單 `§三` 之逐字：

> 🛑 **硬停機款（⛔ 繞過）**：若款 `3` 於**任一** `forced` 消費端判為 `(iii) ⛔ 可達`，
> 或款 `5` 之差逾 `1e-9` ⇒ **停機、上呈、⛔ 改碼一字**；
> **⛔ 自行加參數、⛔ 改任何函式之簽章、⛔ 改 `_corner_buffer_S` 或 `_build_corner_range_v3` 一字**。

🔴 **款 `3` 於<u>四處</u>（⛔ 只一處）皆判 `(iii) ⛔ 可達`** ⇒ **硬停機款觸發**。
🔒 **⇒ `§四`（改·凍存分支）與 `§五`（驗收準據 `A`–`E`）<u>⛔ 辦</u>；分支 `verify/W-G.9-289-gb170` <u>⛔ 開</u>；
生產碼 `34` 檔<u>一字未動</u>。**

### `§三-1`　🔑 與單 `§四-1` 之明文相印證（⛔ 我之推論）

單 `§四-1` 令「改取 `S1_par`（其值自 `§三` 款 `2`／`3` 所現查之**既有**來源取回·**⛔ 另算第二份**）」
——而 `§二-2`／`§二-3` 之現查坐實：**該「既有來源」不存在**。
單同節之末款並已預置其處置：

> 🛑 **若最小實作之形與上開相異** … ⇒ **停機上呈**、**⛔ 自擬第二形**、⛔ 為湊任一準據而改第二處。

⇒ 🔒 **本批之停機係單自身所預置之路徑**，⛔ 我之自裁。

### `§三-2`　🛑 **⛔ 為之事（本節逐項自檢）**

**⛔ 自行加參數**（`_build_corner_range_v3` 之簽章一字未動）／**⛔ 改 `_corner_buffer_S` 一字**／
**⛔ 改 `_build_corner_range_v3` 一字**／**⛔ 自擬第二形**／**⛔ 另算第二份**（⛔ 由 `corner_range_polys[_side]`
之多邊形反推 `S1_par`——該法即「另算第二份」，單明文所禁）。

### `§三-3`　併記：`準據 A` 之**改前**值（⛔ `§五` 之施行·僅供發單側判斷）

🛑 **`§五` ⛔ 辦** ⇒ 準據 `A`–`E` **皆未量**。惟 `§二-5` 之同一次落檔內**既有**之改前值，照實併呈
（**⛔ 改後值**·⛔ 反事實）：

| 格 | 改前 `W_0`（`solve_G_binary` 鏈頭筆之 `W_near`） | `S1_perp` | 差 |
|---|---|---|---|
| `R4/left@0.0m` | `4.48` | `3.6954999940774775` | **`0.7845000059225225`** |
| `R2/left@3.5m` | **`None`**（鏈頭筆 ＝ `0`·倉內既有現象） | `7.0` | **不可得** |
| `R4/left@3.5m` | `7.79` | `7.0` | **`0.79`** |

⇒ 🔑 二格之差**皆 `> 1e-9`** ⇒ 單 `§五` 之判別力造 `[必相異]`（「改前之 `W_0` 與 `S1_perp` 之差須 `> 1e-9`
⇒ 證準據 `A` **非恆真**」）**可滿足**；🛑 **此係其<u>可滿足性</u>之先證，⛔ 準據 `A` 之施行**。

---

## `§四`　工項四：`自誤 393`（主線·自誤簿檔末·嚴格 append-only）

| 受詞 | 實測 |
|---|---|
| payload（`常規七 一`·由檔案管線直接抽出·⛔ 手寫） | 單之列 `126`–`134`（**`9`** 列／**`1011`** B／`sha256` `5aa39aaac6157c22755c34802c81344a1fd9b3bedceda4f7850d13a9136ab0dd`） |
| **落地形之判準**（🔑 **單 `§六` 攔法之逐字自套**） | payload 內含 `#` 標題列 ＝ **`False`** ⇒ **原樣保留 `> `**（⛔ 剝層·與 `W-G.9-288` 之 `GB-170` **相異**） |
| 標題範本之錨（`常規四（九）四`） | `### 🩸 \`自誤 392\`` ⇒ 命中 **`1`** 列（`:9663`）⇒ **唯一**（`uniq`） |
| 落地之標題 | `### 🩸 \`自誤 393\`　**以引用裝置承載一份「須以其原形落地」之 payload，而該 payload 內含結構標記**` |
| 坑 `bh` 殘留檢 | 題內 `自誤 392` ＝ **`0`**（期 `0`）／`自誤 393` ＝ **`1`**（期 `1`） |
| 🔒 **落地形之機驗**（單 `§六` 攔法逐字） | 首列可被該簿之定義框（`VR-091 四`）命中 ＝ **`True`**；擷得號 ＝ **`393`** ＝ 所鑄之號 ⇒ **`True`** |
| 期初／期末 bytes | `817971` → `819120`（**`+1149`**／**刪除 `0`**）·追加段 `CR` ＝ `0`／`LF` ＝ `14` |
| 嚴格前綴之二造 | 造甲[必真] `new.startswith(old)` ＝ **`True`**／造乙[必偽] `(old + b'X').startswith(new)` ＝ **`False`** |
| 簡繁機檢 | 受詞 ＝ 本批**自撰**之標題（⛔ 含單之 payload）⇒ 簡體命中 `{}`·**`rc` ＝ `0`**；[必命中]對照 `自誤` ＝ `1` |
| `常規七 三` 覆核 | 重抽 == 首抽 ⇒ **`True`**；簿內含該 payload ⇒ **`True`** |
| `wg942_append_audit.py` | `rc = 0`·受檢 **`77`** 筆·逐字「結論：✅ 全部成立」 |

---

## `§五`　驗收準據 `A`–`E` 之判

| 準據 | 判 | 由 |
|---|---|---|
| `A` 三 `forced` 格之 `W_0` 與 `S1_perp` 之差 ≤ `1e-9` | 🛑 **⛔ 量**（`§五` 未施行） | 硬停機款觸發 ⇒ ⛔ 改碼 ⇒ 無「改後」可量 |
| `B` `forced` 帶之落地片與 `corner_range_polys[_side]` 之對稱差 ＝ `0` | 🛑 **⛔ 量** | 同上 |
| `C` 其餘 `19` 格之 `W_0`／`Rw`／`G` 逐位不變 | 🛑 **⛔ 量** | 同上 |
| `D` 三 `forced` 格之 `G` 與分配面積之改前→改後差 | 🛑 **⛔ 量** | 同上 |
| `E` `結構不變量永久閘` 二情境之判 | 🛑 **⛔ 量** | 同上 |

🛑 **「⛔ 量」⛔ 等同「量而不過」**（`序 11 之一般化` 款 `三`：「**無從回測**」⛔ 等同「回測不過」）；
亦**⛔ 以「未觸」充綠**（單【驗收】逐字）。

**判別力三造**（單 `§五`）：`[必命中]` `forced` 格數 ＝ **`3`** ≥ `1` ✅（`§二-5` 當場重得）／
`[必相異]` 改前之 `W_0` 與 `S1_perp` 之差 `> 1e-9` ⇒ **`0.7845…`／`0.79`** ✅（其**可滿足性**已證·見 `§三-3`）／
`[必為零]` 改前／改前之自身對拍 ⇒ 🛑 **⛔ 施**（其受詞係 `§五` 之二跑對拍·本批未跑第二跑）。

---

## `§六`　一句話推導

> 單 `§四-1` 令「改取 `S1_par`，其值自**既有**來源取回·⛔ 另算第二份」——
> 而 `_build_corner_range_v3` 之**外層唯一回傳**為多邊形 `rng`，`S1_par`／`S1_perp`／`_sin_t` **皆為其區域值**，
> 且於**四個** `forced` 消費端之 scope 與模組頂層全域**皆不可達**（全倉窮舉：`S1_par` 之生產碼命中僅
> `app.py` 一檔且全落於該函式內）⇒ **既有來源不存在** ⇒ 單 `§三` 之**硬停機款觸發**
> ⇒ **⛔ 改碼一字、⛔ 開分支、`§四`／`§五` ⛔ 辦**；款 `5` 之閉式回代三格皆 `0.0` ⇒ **⛔ 觸發第二停機款**。

---

## `§七`　本批之自捕（**`2` 則**·皆於出艙前攔下·⛔ 頂替）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | **框漏一形** ＝ 我取 `_corner_buffer_S(` 為框，而 `verify/wf_f1.py`／`verify/wf_f4.py` 之呼叫形為 `ns["_corner_buffer_S"](` ⇒ **二族靜默不見**（檔 `2`／列 `4` vs 真值 檔 `4`／列 `7`） | **⛔ 報錯**；且「檔 `2`／列 `4`」讀起來完全合理（app ＋ stepg 各二） | **二形並取** ＋ 出艙對照組[必為零]；🔑 **本則之後果**：若未攔下，款 `3` 之受詞母體將漏二處 ⇒ 硬停機款之「**任一**」判準被削弱 |
| `2` | **scope 分析取錯層** ＝ 「模組頂層全域」以 `ast.walk(tree)` 蒐集 ⇒ **把巢狀函式之區域名算成全域** | 🔴 令 `app.py` 之 `S1_par` **偽陽為「可達」** ⇒ **硬停機款會被誤判為<u>未</u>觸發** | 改取 **`tree.body` 頂層**敘述；並附判別力二造（[必可達] `_corner_buffer_S` ＝ `True`／[必⛔ 可達] 一必不存在之名 ＝ `False`） |

🔒 **二則之共性**：**皆會把「停機」誤判成「可續」**——即**朝<u>放寬</u>之方向**失準。

---

## `§八`　`⛔ 為之事`之逐項自檢（單 `§八`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ **併入 `verify/W-G.9-289-gb170` 於主線** | ✅ **該分支⛔ 開**（硬停機款觸發）⇒ 無可併之物；見 `§九` 閘 `6'` 之 loud 具名 |
| `2` | ⛔ 於主線動生產碼 `34` 檔一字／⛔ 改 `app.py` 一字 | ✅ 見 `§九` 閘 `2`／閘 `6`（blob 期初＝期末·相異 `0`） |
| `3` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／任何函式簽章／任何 `*_EXPECT`／任何判準、閘、框、輸出格式 | ✅ 同 `2`（**零生產碼**·本批⛔ 新增任何器、⛔ 改任何既有器一字） |
| `4` | ⛔ 為湊任一驗收準據而改第二處／⛔ 自擬最小實作之第二形 | ✅ 準據 `A`–`E` **皆未量**；本報告**⛔ 提**任何第二形 |
| `5` | ⛔ 動 `ALPHA_LABELS` 一字 | ✅ `git diff --numstat d01749c9..HEAD -- verify/probes/probe_WG9269_pit_index.py` 輸出**空** |
| `6` | ⛔ 覆寫 `verify/baselines` 任一檔／⛔ `WV_BAKE`／⛔ 併線 `p3a`／⛔ 引任何自 baseline 取得之數 | ✅ `baselines` `298` 檔·框 `sha256` 未變；`WV_BAKE` ＝ `None`；`p3a` `rc = 1`；本報告之數皆出自**本批當場**之量測或**本單所供之 payload** |
| `7` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號 | ✅ 四簿期末 `GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29` 逐位未動；坑 `87`／`87` 未動 |
| `8` | ⛔ 解除或收窄 `GB-170` | ✅ 本報告**未載**任何解除／收窄；其失效條件三項**仍皆未成就** |
| `9` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項／⛔ 改藍影一字／⛔ 判該側是否應有第 2 宗 | ✅ 由生產碼 blob 相異 `0` 機械蘊含；本報告⛔ 作成該類判斷 |
| `10` | ⛔ 追改任何已入倉文件一字 | ✅ 見 `§九` 閘 `1`（`numstat` 刪除欄全 `0`）＋ `wg942_append_audit.py` `rc = 0` |
| `11` | ⛔ 判本修是否已了 `GB-170`、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | ✅ 本報告只述**現查**與**停機之事實**；孰誤之判**未作**（`§三` 已具名其係單自身所預置之路徑） |
| `12` | ⛔ 刪任何 ref | ✅ `p3a` 與主線 ref 皆在（`§九` 閘 `7`） |
| `13` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§九`／`§十` 為**入倉後**之末端追加；`ls-remote` 之實查於**推送後**出艙於聊天 |

---

【驗收】本批之驗收固定項已逐 token 出艙：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`merge-base`／`is-ancestor`／`verify/W-G.9-289-gb170`／`GB-170`／`_corner_buffer_S`／`_build_corner_range_v3`／
`S1_par`／`S1_perp`／`corner_range_polys`／`cum_S`／`W_0`／`numstat`／`ALPHA_LABELS`／`baselines`／`sha256`／`ls-tree`。
