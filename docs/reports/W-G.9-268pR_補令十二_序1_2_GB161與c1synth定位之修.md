# `W-G.9-268′` 補令十二 序 `1`／`2`：`GB-161`／`自誤 347` 之鑄 ＋ `c1_synth` 定位之修 —— ✅ **辦畢**

> 🔒 序 `1` ＝ `GB-161`／`自誤 347` 已鑄 ＋ **全族掃描清單**（只登記·⛔ 修）。
> 🔒 序 `2` ＝ `c1_synth` 之 `REPO` 改倉相對定位 ＋ **三項出艙** ＋ `Q-3` 判定集回復為 `7`。
> 🛑 序 `3`（`c3`·🔴 生產碼）**未辦**——落**側支** `verify/W-G.9-268p-c3`、⛔ 主線；⛔ 動 `app.py` 一字。
> 🔴 **一項真陽性之紅已現形**（`§三-3`）——其處置**候發單側**。

---

## §零　三軸 ＋ 工項零

`(1)` 主線期初 ＝ `e835ed5db7412d793f3c72efd61063f09d01d93b`（`ls-remote` 實查·相符）。
`(2)`／`(3)` 逐節具名。
🔒 **工項零**：補令十二原封入倉——`103` 列／**`9049`** B／`CR 0`；
來源與落檔 `sha256` 皆 `d09b60f16055112e94acfaf7ee12852c682c368b7c0620f278623941fa12d218`／`cmp rc = 0`。

---

## §一　序 `1`：二號之鑄

**取號現查**（倉內既有器·⛔ 自擬框·態 `blob@e835ed5`）：
自誤 相異 `339`／MAX `346`／缺號 `[106]` ⇒ 次號 **`347`**；`GB` 相異 `158`／MAX `160`／缺號 `[12, 87]` ⇒ 次號 **`161`**
——與補令十二 `§五` 所載期初值**逐位相符**。

**鑄號收工閘**（`常規四（九）二`·框逐字自倉內器取出並 `exec`）

| 簿 | 相異 | MAX | 缺號集 | ①／②／②′／判別力 |
|---|---|---|---|---|
| 自誤 | `339 → 340` | **`346 → 347`** | `[106] → [106]` | ✅／✅／✅／✅ |
| `GB` | `158 → 159` | **`160 → 161`** | `[12, 87] → [12, 87]` | ✅／✅／✅／✅ |

🔒 **append-only**：`GB` 簿 ＋`7585` B／自誤簿 ＋`1959` B；嚴格前綴皆 `True`／`CR 0`。

---

## §二　`GB-161` 之全族掃描（補令十二 `§三-2`·**只登記·⛔ 修 `#2`〜**）

**器** ＝ `verify/probes/probe_WG9268p_gb161_sweep.py`／**落檔** ＝ `verify/out/WG9268pR_gb161_sweep.log`。
**母體 ＝ `verify/**/*.py` `286` 支**（⛔ `__pycache__`）。

| 樣式 | 檔 | 列 |
|---|---|---|
| `worktrees` | **`14`** | **`27`** |
| `os.chdir(` | **`8`** | **`10`** |
| `git -C` | **`18`** | **`30`** |
| `絕對路徑 C:/Users` | **`7`** | **`9`** |

🛑 **四樣式皆⛔ 命中 `0`**——「含命中 `0`」之款本次**無受詞**，此即其具名。
🔑 **對照組三造**：`import os` ＝ `265` 檔（須 `>0`）✅／人造字樣（執行期組出）＝ `0` ✅／
🔴 **必命中之對照**（本類已知成員 `c1_synth`）＝ `1` 列 ✅。

🔴 **母體較單所載為大**：補令十二 `§二-1` 表列 `8` 項；本掃描得 **`14`**（`worktrees`）＋ **`8`**（`os.chdir`）。
**照實出艙，⛔ 以單之表為限。** 逐支之樹相依判見 `GB-161` 本文與落檔。

### 二-1　🩸 本器首版之自捕（⛔ 靜默）

首版樣式作 `worktrees[/\\]`（要求其後**緊接分隔符**），而 `c1_synth` 之路徑**跨二源碼列**
（`…worktrees"` ／ `r"/w-g-…"`）⇒ 該列**不命中** ⇒ **漏抓了 `GB-161` 之起因那一支**
（`14` 檔誤為 `10`／`27` 列誤為 `17`）。
🔒 **成因 ＝ 對照組只放「必不命中」之乙組**——其只證器**能歸零**，**⛔ 證其<u>撈得到本類</u>**。
**今增設「必命中之對照」（本類之已知成員）**，該漏當場現形。
🔒 **通則（已併記於 `自誤 347`）**：攔法之**執行**與其**框之涵蓋性**係二事。

---

## §三　序 `2`：`c1_synth` 定位之修 ＋ **三項出艙**（補令十二 `§三-1`）

🔒 **所改者僅 `REPO` 之取得方式** ＝ `os.path.dirname(os.path.dirname(HERE))`
（自 `__file__` 上溯二層）——**⛔ 改其判準、容差、受量欄位、出艙形一字**。

### 三-1　`(i)`／`(ii)`

| 項 | 值 |
|---|---|
| `(i)` **改前**所指之樹 | `…/.claude/worktrees/w-g-9-268p-s1-correction-f41d70`（**現仍存在**·其 `app.py:9269` ＝ `'0'` ＝ pre-`c2`）|
| `(ii)` **改後**所指之樹 | `…/.claude/worktrees/w-g-251r-handoff-traps-b6f8a9` ＝ **本倉** ✅ |

### 三-2　`(iii)` **逐項差異**（改前既有落檔 vs 改後重跑）

| 態 | 結果 | `rc` |
|---|---|---|
| **改前**（`verify/out/WG9268pR_c1_synth.log`·在倉） | `✅ 合成案全綠` | `0` |
| **改後**（`verify/out/WG9268pR_c1_synth_gb161fix.log`） | **`🔴 合成案紅 ＝ 1 項`** | `3` |

**差異 ＝ 恰 `1` 項**，其逐字 ＝ **`· off（環境變數**未設**）·同上`**。
其餘各造（閉式自證／造 `a` 之顯式 `'0'`／造 `b`／`c`／`d`／`e`／四呼叫點 AST 實查）**逐項不變**。

### 三-3　🔴 該紅 ＝ **真陽性**·**⛔ 新缺陷**（候發單側）

該造（`c1_synth:96`）逐字為
`chk("off（環境變數**未設**）·同上", float(adv(1.0, CC, d_hat, corner, alloc)), 1.0)`
——其斷言「**環境變數未設 ⇒ 原值原樣**」，所編碼者為 **pre-`c2` 之預設**。
而 **`c2`（KL `2026-09-10` 放行·已於序 `3` 併線）已將預設改為 `on`**
⇒ 環境變數未設即取 `on`（回 `7.25`）⇒ 該斷言於**本倉**不成立。

🔴 **其既往之全綠<u>純因所量者為 pre-`c2` 之他樹</u>** ——**即 `GB-161` 所預言之假綠，於本批當場現形**。
🛑 **⛔ 改該造之判準以就結果**（補令十二 `§三-1` 明令）。
🛑 **其處置候發單側**：該造應否隨 `c3` 之 `contract` 段一併更新（`c3` 移除旗標後，
「環境變數未設」與「設 `'0'`」將**皆**取無條件執行 ⇒ 造 `a` 二式**俱須重擬**）。
🔒 **⛔ 追改其既有落檔一字**；**效力判 ＝ 該落檔之「全綠」⛔ 為本倉之證據**。

### 三-4　`Q-3` 之判定集

`#1` 既修，其活體檢之判**已為本倉之證據** ⇒ **判定集回復為 `7` 支**；
補令十一 `§二-4` 之「`7／7`」**自本修起**方為本倉之證據
（其在本修之前須重讀為「`6／7` ＋ `1` 具名排除」·`GB-161` 本文已載）。

---

## §四　逐 `commit` 之生產碼判（`常規一 🔧 二`·含空輸出）

| `commit` | 受檢區間 | 輸出 | 判 |
|---|---|---|---|
| 本批 | `e835ed5..HEAD` | **空輸出**（命中 `0` 行）| 🟢 零生產碼 ⇒ 逕推 |

🔒 **⛔ 動 `app.py` 一字**（`git diff --numstat e835ed5..HEAD -- app.py` 空輸出）。

---

## §五　續辦之界

🛑 **本批⛔ 辦者**：⛔ 辦 `c3`（序 `3`）、⛔ 修 `GB-161` 之 `#2`〜`#14`（逾射程·已排入 `W-G.9-269` 前置）、
⛔ 改 `c1_synth` 造 `a` 之判準、⛔ 鑄授權二號以外之任何號。

🔒 **`W-G.9-269` 之前置（累計·補令十二 `§六`）**
`(i)` `A` 母體須以**新錨態重量**·⛔ 沿用 `2` 宗／`4.7950 ㎡`；
`(ii)` `GB-160` 之閘 `γ` 重量；
`(iii)` **`GB-161` 全族之修與效力判**（本批已修 `#1`·餘 `#2`〜`#14`）。

---

## §六　🔑 **交接註**（本窗收工令 `2.`·⛔ 上文一字不刪·純末端追加）

> 🛑 **本節之受詞 ＝ 下一窗**（序 `3`／`c3` 之施工窗）。逐坑載 **形／徵候／攔法**，**⛔ 只寫結論**。
> 🔒 所列**皆為本窗親踩**（或親自事前攔下）者，**⛔ 臆測之風險清單**。
> 🔒 **承補令五 `§十一` 之形**；該節之**坑 `1`〜`10` 仍全效**，本節**只寫本窗新得之坑**（`11` 起續號）。

### 坑 `11`　🔴 **硬編絕對路徑指向他窗 `worktree`**（＝ `GB-161`·**一族·⛔ 一支**）

- **形**：倉內探針／工具以**字面絕對路徑**指向**他窗之 worktree**，且數支為**活體使用**
  （`os.chdir` 後 `harvest()`／讀寫其 `verify/out/`／`git -C` 其樹）⇒ **其所量者為他樹**。
- **徵候**：🔴 **`rc = 0`、輸出格式完好、結論貌似全對**——本窗實測 `probe_WG9268p_c1_synth.py`
  所指之樹**現仍存在**，其 `app.py:9269` 為 `'0'`（pre-`c2`）而本倉為 `'1'`
  ⇒ 其「全綠」**只因量到另一棵樹**。**修其定位後同一支立刻轉紅 `1` 項**（見坑 `15`）。
- 🔴 **其對閘之直接後果**：`c3` 移除旗標後，只要他樹仍存旗標，該支之**選擇器活體檢仍會通過**
  ⇒ **假綠**——恰為補令十一 `§二` 所欲防者。
- **攔法**：跑**任何**倉內探針前先 `grep -n "worktrees\|REPO *=" <probe>`（＝ 補令五 `§十一` 坑 `10`
  之攔法·**本窗執行之而撞見**）；**且**須**逐支查其 `REPO`／`ROOT` 之**取得方式**，
  ⛔ 只看有無 `worktrees` 字樣（見坑 `12`）。

**🔒 全族之現況（本窗掃描·母體 ＝ `verify/**/*.py` `286` 支·落檔 `verify/out/WG9268pR_gb161_sweep.log`）**

| 樹相依 | 支 | 用法 |
|---|---|---|
| **是**（活體使用） | `probes/probe_WG9268p_c1_synth.py` | `os.chdir` ＋ `harvest()` ⇒ 讀**他樹工作區**之 `app.py`　🔒 **本窗已修**（坑 `15`）|
| **是** | `probes/wg9268_bing_csv.py` | 讀他樹之 `verify/out/*.csv` |
| **是** | `tools/wg960_z_selfcheck.py` | **寫入**他樹之 `verify/out/` |
| **是** | `tools/wg99_anchor_audit.py` | 裸 `os.chdir(REPO)` |
| **待判（`git -C`）** | `probes/wg9268_ingest_verify.py` | `git -C REPO cat-file blob <spec>` |
| **待判（`git -C`）** | `probes/wg9268_nameset_diff.py` | `git -C REPO show "<REV>:<FROZEN>"`·其 `REV ＝ e57d2ef8…`（**完整 SHA** ⇒ **樹無關**）；`FROZEN ＝ verify/out/…`（**待人工判**）|
| **待判** | `fixture_wf_ns_wiring.py`／`probes/probe_D2b29_wg9_q1q3.py`／`probes/probe_WG91_static_checks.py`／`probes/probe_WG93_mp_definition_scope.py`／`probes/probe_WG971_wrap.py`／`probes/probe_WG996_yi_invariants.py`／`probes/probe_WG997_reassign_order.py`／`probes/probe_WG9268p_gb161_sweep.py` | 字樣見於註解／字串·⛔ 抽得樹名（末者 ＝ **本掃描器自身**·`GB-132` 族之自指命中）|

🔑 **`git -C` 之樹相依判準（機械·⛔ 推定）**：同一倉各 worktree **共用物件庫**
⇒ `git -C <任一樹> cat-file blob <完整 SHA>` **與樹無關**；惟 `<rev>:<path>` 之 `rev` 若為
**分支名或 `HEAD`**，其解析為**該樹之 `HEAD`** ⇒ **樹相依**。**須逐支現查其 `spec`／`REV` 之實際形。**
🛑 **本窗只修 `#1`**（補令十二 `§三-1`）；`#2`〜`#14` **只登記**，已排入 `W-G.9-269` 之前置。

### 坑 `12`　🔴 **跨二源碼列之路徑，逃過 `worktrees[/\\]` 樣式**

- **形**：`c1_synth` 之路徑以**隱式字串串接**跨二列——
  `REPO = (r"…/.claude/worktrees"` ／ `        r"/w-g-9-268p-s1-correction-f41d70")`
  ⇒ 含 `worktrees` 之那一列，其後**緊接者為 `"`、⛔ `/`**。
- **徵候**：🔴 **靜默少抓，且「少」得看不出來**——本窗掃描器首版用 `worktrees[/\\]`，
  得 **`10` 檔／`17` 列**；改為裸字樣 `worktrees` 後得 **`14` 檔／`27` 列**。
  **漏掉的正是 `GB-161` 之起因那一支。** ⛔ 任何錯誤訊息。
- **攔法**：`1.` 路徑類樣式**⛔ 要求其後緊接分隔符**——用**裸字樣**（`worktrees`）；
  `2.` **併查 `os.chdir(`**（本窗實測 `8` 支·其中 `probe_WG9218R_setback_sensitivity.py`／
  `tools/wg911_baseline_trace.py` **⛔ 在 `worktrees` 之 `14` 支內**）——**二族⛔ 互相涵蓋**；
  `3.` 見坑 `13` 之對照組。

### 坑 `13`　🔴 **掃描器之對照組只備「必不命中」者 ⇒ 證不出其撈得到本類**

- **形**：對照組慣例只放**人造哨兵**（須 `= 0`）與一**必非零之近義詞**（須 `> 0`）；
  二者皆**⛔ 證該框撈得到<u>受詞那一類</u>之成員**。
- **徵候**：🔴 **對照組全綠、而框漏抓本類** ——本窗坑 `12` 即此：
  `import os` ＝ `265` 檔 ✅、人造哨兵 ＝ `0` ✅，**而 `c1_synth` 漏抓**、⛔ 任何紅。
- **攔法**：對照組須**三造**——
  `(甲)` **必非零之近義詞**（證器非恆零）／`(乙)` **人造哨兵**（證器能歸零）／
  🔴 `(丙)` **必命中之<u>本類已知成員</u>**（證器**撈得到本類**）。
  **只備 `(甲)(乙)` 者，其「命中 `N`」⛔ 得讀為完整。**
  🔒 本窗增設 `(丙)` 後（`CTL_MUST ＝ verify/probes/probe_WG9268p_c1_synth.py`）該漏當場現形。

### 坑 `14`　🔴 **哨兵之號寫成字面並隨探針入倉 ⇒ 次批其「真零」失效**（`GB-147` 之形）

- **形**：`probe_WG9268p_occ6.py` 首版將乙組哨兵之號**寫死為字面**（`CONTROL_LO` 清單）並隨該檔入倉。
- **徵候**：**次批之占用查中，該號由<u>真零</u>變為 C 形 `1`／鬆框 `2`**
  ——其命中即**該探針與其落檔自身**。⇒ 乙組**⛔ 再證器能歸零**。
- **攔法**：哨兵之號一律**執行期自倉掃出**（本窗作法：於 `9000`–`9999` 內取**首個三形皆 `0`** 者），
  **其字面⛔ 出艙**，一律以**代稱 `⟨NEG⟩`** 指之。
  🔒 `CLAUDE.md` 逐字既載：「哨兵字樣宜**於執行期組出**、⛔ 使其字面落入任何 log」（`GB-147`）。
  🩸 **併記**：本窗**只修 `occ6`**；其**既出之數⛔ 追改**（`常規四（九）五`）。

### 坑 `15`　🔴 **`c1_synth` 造 `a` 之二式現況：`(a1)` 綠／`(a2)` 紅**（⛔ 讀為新缺陷）

- **形**：造 `a`（旗標 `off` ⇒ 原值原樣）有**二式**，其**現況相異**：

| 式 | 逐字 | 得 | 期 | 判 |
|---|---|---|---|---|
| `(a1)` | `` off·`cum_S ＝ 1.0` 而 `s_geo ＝ 7.25`（須仍 1.0） `` | `1.0000000000` | `1.0000000000` | ✅ |
| **`(a2)`** | `` off（環境變數**未設**）·同上 `` | **`7.2500000000`** | `1.0000000000` | 🔴 |

- **成因（機械·已定位）**：`(a2)` 所編碼者為 **pre-`c2` 之預設**；而 **`c2`（KL `2026-09-10` 放行·
  已於序 `3` 併線）已將預設改為 `on`** ⇒ 「環境變數未設」即取 `on`（回 `7.25`）。
  🔒 **`(a1)` 顯式設 `'0'` ⇒ 仍為 `off` ⇒ 仍綠。**
- **徵候**：🔴 **該紅在坑 `11` 修好<u>之前</u>看不見**——修前該支量的是 pre-`c2` 之他樹，`rc = 0` 全綠。
  ⇒ **`rc` 由 `0` 轉 `3` 係「定位修好」之<u>結果</u>，⛔ 新破損。**
- **攔法**：🛑 **⛔ 改該造之判準以就結果**（補令十二 `§三-1` 明令）。
  🔴 **`c3` 移除旗標後，`(a1)` 與 `(a2)` 將<u>皆</u>取無條件執行 ⇒ 二式<u>俱須重擬</u>**
  ——此係 `c3` 之**必辦項**，**其處置候發單側**（本窗⛔ 自裁）。

### 坑 `16`　🔴 **活體檢之可觀測量⛔ 取合成幾何**

- **形**：欲證「二態之選擇機制仍活」，直覺作法是**合成一組幾何**餵生產函式、比其回傳值。
- **徵候**：🔴 **二態<u>皆拋例外</u> ⇒ 皆「相同」⇒ 甲乙二造同紅、本檢失能**。
  本窗首版取「單位正方形 ＋ `d̂ = (1,0)` ＋ `allocation_dir = (0,1)`」，
  而 `_strip_axis` 之 `m̂ ∥ allocation_dir` ⇒ `denom = d̂·m̂ = 0` ⇒ `RuntimeError`（loud）。
- 🛑 **退路亦堵死**：⛔ 改用 `alloc ∥ d_hat`——`_strip_axis` 之 docstring **逐字**載其為
  **失敗考古 `#7`**：
  > 「首版合成測用 `alloc ∥ d_hat`（正交）全綠＝**只測了自己的想像**；
  > **斜交是本案常態（`CLAUDE.md §8`），正交才是特例。**」
- **攔法**：改以**分支之被觀測**為可觀測量——
  `(‵_oblique_s_max‵ 之被呼叫次數, 回傳值)`：旗標 `off` 之分支**逕回 `cum_S` 而⛔ 呼叫**該函式，
  `on` 則必呼叫之 ⇒ `off ⇒ (0, 0.0)`／`on ⇒ (1, 1.0)`。
  🔒 **其⛔ 需任何幾何為真**（`_oblique_s_max` 以替身取代）⇒ 免疫本坑；
  且對**以「相同」為期望結果**之探針（如 `offsrc`）**一體適用**。
  🔒 **`c3` 後二態皆呼叫 ⇒ 該量逐位相同 ⇒ 本檢如期轉紅。**

### 坑 `17`　🔴 **兩造自證之「必紅」造，可能<u>紅得不是地方</u>**

- **形**：本窗之乙造（模擬 `c3`）以替身取代生產函式，而該替身於**定義時**捕獲真 `_oblique_s_max`。
- **徵候**：🔴 **乙造確實轉紅 ⇒ 判別力表面成立**，然其紅**因例外**（真幾何退化）、
  **⛔ 因「二態同值」** ⇒ **證不出本檢能偵得 `c3` 之情狀**。
- **攔法**：替身內之相依一律**於呼叫時自 `ns` 取**（⛔ 定義時捕獲）；
  並**逐造檢其紅之<u>理由</u>**，⛔ 只看紅不紅。修正後乙造得 `off ＝ (1, 1.0)`／`on ＝ (1, 1.0)`
  ⇒ **紅在正確之處**。

### 坑 `18`　🟡 **本窗其餘自捕（形／徵候／攔法·從簡）**

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `a` | 含 emoji 之器於 `cp950` 主控台 | `rc = 1`／`UnicodeEncodeError` ⇒ **量測器紅、⛔ 稽核紅** | 一律先設 `PYTHONIOENCODING=utf-8`（`常規五`）|
| `b` | `rc` 經**管線**取得（`… \| tail`） | 取到 `tail` 之 `rc`、**⛔ 受測物之 `rc`** | `rc` **直取**、⛔ 經管線 |
| `c` | `wg9268_gate6_occupancy.py` 之 `argv[3:]` **全為甲組**（乙組其自行組出） | 誤置人造號於該位 ⇒ 被判「甲組未達 `≥ 1`」 | 用器前先讀其 `argv` 契約；⛔ 器之瑕 |
| `d` | 拋棄式 clone 以**截斷之 SHA** `fetch` | `fetch` 失敗 ⇒ clone **靜默停在 `main`**（`app.py sha256 4860f108…` ≠ wip 之 `1e15d45a…`）| 一律用**全 `40` 碼**；clone 後**逐位驗** `app.py` 工作區 bytes ＝ blob bytes |
| `e` | `wv_reconcile.report()` 回 **`(rc, 行清單)` 之 tuple** | 逕 `print` 之 ⇒ 整個 tuple 印成一列 | 讀器之回傳契約後再用：`_rc, _lines = wv.report(...)` |
| `f` | 探針將二態存入區域 `ST` 而**未填** `ROWS["OFF"]` | `KeyError: 'OFF'`（**loud**·⛔ 靜默） | loud 即其攔法；修後併**明文化**「比較端 `OFF` ＝ `3.5m`／`off`·⛔ 混情境」 |
| `g` | 於 `bash` 命令內誤帶 **heredoc** | 被 `wg9237_heredoc_guard` **硬阻斷**（本窗一次） | ⛔ 用 heredoc；落檔一律 `Write`，再以**路徑**呼叫 |
| `h` | `grep -P` 於本機 locale | `grep: -P supports only unibyte and UTF-8 locales` | 含 lookaround 之框一律改 `python re` 復算 |

### 坑 `19`　🔑 **守恆帳之 `Σ池` ⛔ 取「街廓面積 − `ΣG`」**（本窗事前攔下·⛔ 事後補救）

- **形**：補令五 `§六-2` 以 **`抵費地 ＝ 街廓面積 − ΣG`** 記帳。
- **徵候**：🔴 **若逕以之為 `Σ池`，則 `街廓面積 − ΣG − Σ池 ≡ 0` <u>恆真</u>**（`自誤 245` 恆真族）
  ⇒ 該閘**證不出任何事**，而其表面「全綠」**極具說服力**。
- **攔法**：改取**獨立量測**之池 ＝ `run_step_g` 之 `g_rows` 中 `推進側別 == '抵費地'` 列之
  **`幾何面積(㎡)`**（`解法 ＝ '幾何剩餘'`·`G(㎡) ≡ 0`）⇒ 殘差**⛔ 恆零**、係真檢
  （本窗實測殘差 `−0.03`〜`+0.03`·容差 `(列數＋1)×0.005`）。

---

### 🔒 下一窗之**起手清單**（承坑 `11`〜`19`）

1. **倉態**：`git ls-remote origin refs/heads/wip/s1-endpart` ⇒ 須 ＝ **`62d7f703792d4209004fc2bfc606509bb8c11b96`**；
   `left-right` 須 `0	0`；`git status --porcelain` 須**空**。
2. **`app.py` blob**：`git cat-file -s HEAD:app.py` ⇒ **`1458029`** B；
   `sha256` 前 `16` ⇒ **`9873b168278fbc08`**（＝ KL 所核准之 `c1376af` 之內容·`c2` 已併線）。
   🔒 其 `:9269` 逐字須含 `'WG9268P_ANCHOR_GEOM', '1'`（⇒ 預設 `on`）。
3. **四簿 MAX**（倉內既有器 `verify/probes/wg9268_gate5_tally.py`·⛔ 自擬框）：
   自誤 相異 `340`／**MAX `347`**／缺號 `[106]`；`GB` 相異 `159`／**MAX `161`**／缺號 `[12, 87]`；
   `VR` 相異 `80`／**MAX `95`**／缺號 `[73, 75]`；`K-9` 相異 `28`／**MAX `29`**／缺號 `[]`。
4. 🔴 **`c1_synth` 之 `REPO` 現值**：`REPO = os.path.dirname(os.path.dirname(HERE))`
   （＝ 倉相對定位·`GB-161` 之修）。**若其復為字面絕對路徑 ⇒ 該修被回退，停機。**
5. **`Q-3` 判定集之七支**（皆已裝選擇器活體檢·`assert_live` 各命中 `1`）：
   `probe_WG9268p_c1_synth.py`／`_c2_gates.py`／`_c2_v1prime_mem.py`／`_c2_v1prime_pert.py`／
   `_conserv.py`／`probe_WG9268p_mem.py`／`_offsrc.py`。
   🔒 其共用模組 ＝ `verify/probes/wg9268p_selector_liveness.py`。
   🛑 **`c3` 之後，該七支之活體檢<u>應當轉紅</u>**（旗標消失 ⇒ 二態同值）——
   **其轉紅係預期，⛔ 新破損**；`Q-3` 之受詞為「**⛔ 任一支以『二態逐位相同』判綠**」。
6. 🛑 **序 `3`（`c3`）之界**（補令十一 `§三`）：移除旗標 ＋ 更新 `_wg9268p_anchor_advance` docstring
   之「預設 `off`」（自 `c2` 起已過期）；驗收 `Q-1`／`Q-2`／`Q-3`；
   **⛔ 動** `_oblique_s_max`／`_K923_EPS_ZERO`／`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate` **一字**；
   **⛔ 改錨之語意**（`c3` 為 `contract`、⛔ 第二次語意變更）。
   🔴 **側支 `verify/W-G.9-268p-c3`·⛔ 主線**；發單側逐位元組復驗 → **呈 KL** → 放行 → 快轉併線。
7. 🔴 **候發單側之未決三項**（本窗⛔ 自裁）：
   `(i)` 坑 `15` 之 `c1_synth` 造 `a` 二式於 `c3` 後之重擬；
   `(ii)` `GB-161` 之 `#2`〜`#14` 之修與其既往落檔之效力判（已排 `W-G.9-269` 前置）；
   `(iii)` `GB-160` 之閘 `γ` 重量（`R5·left` `3/5`／`R6·right` `3/4`·`W-G.9-269` 前置）。
