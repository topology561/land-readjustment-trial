# `W-G.9-11`：22 名目全拆解 ＋ 基準溯源與判準預登（**事前登記**·⛔ 零量測·零碼變更）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg911-dissect`）
**基準**：`cd9569f`

> 🔴 **本批之最大陷阱（施工單 §8-1）**：拆解器泛化後，**可能對已拆過之 `G3` 三項給出不同答案**
> ——而人會傾向相信新工具。⇒ **「逐字重現」是唯一防線**（考古 69 修法 ③）。
> 🔒 **本批⛔ 零新閘、⛔ 不掛任何東西入 `run_all`**；立閘屬下一批。

---

## §0 現查（⛔ 指令與輸出並列·**皆本登記落筆當次重跑**·⛔ 僅倉內事實·⛔ 不以行號出艙）

```
$ git rev-parse --short HEAD
cd9569f
$ grep -rn "W-G.9-11" . --exclude-dir=worktrees --exclude-dir=.git | wc -l
0
$ grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt
22
$ ls verify/baselines/v3/*.csv | wc -l
16
$ git log --format="%h %ad" --date=short -1 -- verify/out/WG96_coverage.txt
20f3446 2026-08-11
$ git diff --numstat 20f3446 cd9569f -- verify/run_verification.py
（空）
$ git diff --numstat 20f3446 cd9569f -- verify/run_all.py        ← 判別力自檢
57  2  verify/run_all.py
$ grep -n "TARGETS" verify/tools/wg910_g3_dissect.py
31:TARGETS = ("W-D.3 碎片", "W-F F.3", "W-G G.2 世代幾何曝出契約")
```

⇒ **`N-1`／`M1` 已現查證實**：`WG96_coverage.txt` 產於 `20f3446`，其母體
`verify/run_verification.py` 自彼時起**零變動** ⇒ **覆蓋資料對本批仍有效**。
🔒 **附判別力自檢**：同一式對 `verify/run_all.py` 得 `57 2` ⇒ **該式非恆為空**（考古 69 修法 ④）。

### 0-1 母體為「全倉 `*.py`」之閘——**全部只有一道**（⛔ 否定性宣稱·三項射程）

- **【目錄射程】** `run_all` 自動流程之執行單元：`verify/run_all.py`／`verify/run_verification.py`／
  全部 `verify/fixture_*.py`（母體 **14 檔**）。
- **【字樣射程】** 六式、語意窮舉、⛔ 不預設符號名：
  `os\.walk`／`glob\.`／`rglob`／`iglob`／`listdir`／`\.glob(`。
- **【連接方式】** 各式**獨立裸 grep**（⛔ 無距離窗）。

**結果**：唯一命中 ＝ `verify/run_all.py` 之 `os.walk(REPO)`（即**禁寫死絕對路徑閘**）；
其餘五式**皆 0 命中**。
🔒 **判別力自檢**：以必然命中之字樣 `def ` 重算 ⇒ **14 檔** ⇒ 上式**非恆 0 命中**。

⇒ 本批新增之 `.py` 只需驗**該一道閘**；驗畢再跑長跑確認（`M6`）。

🔴 **未跑**：⛔ 尚未泛化拆解器；⛔ 尚未拆任何 `G3` 以外之名目；⛔ 尚未建任何溯源表；
⛔ 尚未跑本批之 `run_all`。

---

## §1 待證命題

- **`N-1`**：覆蓋資料仍有效 ⇒ **§0 已證實**。
- **`N-2`**：22 名目中，**`G3` 三項以外**尚存在「只被順序擋住」之主判定。
  ⚠️ **為偽是合法且有價值之結果**（表示 `G3` 是特例、回收已完成）⛔ **不算失敗**，照實報。

---

## §2 分類判準（⛔ 事前寫死·考古 70 修法 ②）

對每一個**未執行之主判定**，逐項列出其**輸入**：

| 判 | 條件 |
|---|---|
| **只被順序擋住**（可回收） | 其輸入**只有**下列之一或多：① 原始碼檔案 ② 常數表／字面 ③ `snapshot`（`load_snapshot()` 之產物） |
| **真需管線** | 其輸入含任何**須經 `run_step_g` 或 `wf_f*.compute` 產生**之物（`_ctx`／`_f0`〜`_f4`／`g_rows`／`anchors` 等） |

- ⛔ **不得以「它在那個 `try` 裡」推定它需要管線**（考古 70 修法 ①）。
- ⛔ **不得以 `G3` 之 3/15 外推**——`G3` 是**被挑出來之樣本，非隨機樣本**（施工單 §8-2）。
- ⛔ **不得由名目字面推定**（兩個方向皆禁·承 `W-G.9-10` §1-1）。
- 🛑 若某項之「可回收」其實**依賴既有另案之特定失敗態**（另案一修好即假紅）
  ⇒ **停機上呈該項**（`Y-3`·考古 65 第四層）。

---

## §3 可證偽預測（⛔ 事後不得回頭修改）

| # | 預測 | 證偽方式 |
|---|---|---|
| **M1** | 覆蓋資料仍有效 | numstat 非空 ⇒ **M1 破 ⇒ `Y-1`**。⇒ **§0 已證實成立** |
| **M2** | 泛化後之工具**逐字重現** `W-G.9-10` 對 `G3` 三項之結論（15 個主判定／全未執行／三名目皆出自 `except`） | 不重現 ⇒ **M2 破 ⇒ `Y-2`**（⛔ 不得擇一採用） |
| **M3** | 22 名目中，`G3` 以外**尚存在**可回收之主判定 | 為 0 ⇒ **M3 破**（⚠️ **合法結果**·⛔ 不算失敗，照實報並收批） |
| **M4** | 16 個基準**皆可**由 `git log` 定其烘焙 commit 與日期 | 任一檔不可定 ⇒ **M4 破**（該檔另立處置） |
| **M5** | **至少一個**基準，其烘焙以來生產側有變動觸及其產生路徑 ⇒「過期」為**活假說** | 全部皆無 ⇒ **M5 破**（**好消息**：未來不符必為**算錯**，須正面寫入報告） |
| **M6** | 本批**零判定層變動**：`run_all` 判定行與 `WG910_runall.log` 相同，且**無任何容許之差** | 出現任何差 ⇒ **M6 破 ⇒ `Y-4`** |

🔒 **依考古 69 修法 ④**，下列「期望為空／順利」之檢查**一律附判別力自檢**：
`M1`（已附·§0）、`M2`（對拍不符時須能轉紅）、`M6`（⛔ 尤其——「無任何容許之差」）、
以及 §0-1 之「其餘五式皆 0 命中」（已附）。

---

## §4 停機條件（⛔ 逐字採施工單 §4）

**🛑 真域邊界 ⇒ 停機上呈 KL**：本批**無**預期之域邊界。若觸及，以
【現況】→【要改成】→【對土地的影響】→【要你判斷】格式上呈，
⛔ 一句是非題只綁一個命題、⛔ 禁程式術語。

**🛑 立即停機（紅線）**：`Y-1` 覆蓋過期／`Y-2` 泛化後未逐字重現／
`Y-3` 某「可回收」項依賴另案之特定失敗態／`Y-4` PASS/FAIL 層改變。

---

## §5 乙組：溯源表與判準預登（⛔ 事前定式）

### 5-1 溯源表五欄

| 欄 | 取得方式 |
|---|---|
| ① 基準檔名 | `ls verify/baselines/v3/*.csv` |
| ② **哪些主判定拿它對比** | **由 `diff_rows` 之引數現查**（AST）⛔ 非由檔名推想 |
| ③ 烘焙 commit ／ 日期 | `git log -1 --format="%h %ad" -- <檔>` |
| ④ **自烘焙以來，生產側是否觸及其產生路徑** | 🔴 **附三項射程**；⛔ 不做遞移閉包則**明寫「⛔ 未做」** |
| ⑤ ⇒「基準過期」是否為**活假說** | 由 ④ 導出 |

⚠️ ④ 若某檔之產生路徑**無法由構造判定** ⇒ **具名列為射程外**，⛔ **不得猜**、⛔ 不得留白。

### 5-2 「基準過期 vs 算錯」之判準預登（⛔ 本組之價值全在時效）

🔒 **凡標「現在就能取得」之證據，本批<u>就取</u>**——⛔ 不得只登記取得方式而不取。
**理由（施工單 §8-3）**：溯源證據**現在取得幾乎免費、解阻當天取得幾乎不可能**。

---

## §6 驗收（⛔ 逐字採施工單 §6）

| # | 閘 |
|---|---|
| 1 | 22 名目**逐一**拆解；`G3` 三項須以泛化後之工具**逐字重現**既有結論 |
| 2 | 分類**逐項附現查證據**，⛔ 不得由所在 `try` 推定 |
| 3 | 「只被順序擋住」者**逐一列出其輸入清單** |
| 4 | 立閘優先序附**排序準則**；⛔ 只排序不實作 |
| 5 | 16 檔溯源表五欄齊備；④ 附**三項射程**；不可判定者**具名列為射程外** |
| 6 | 「過期 vs 算錯」證據類型逐項標記；🔴 標「現在就能取得」者**本批已取**並附產物 |
| 7 | 掃描器**母體自證＋印母體規模**；泛化後**與泛化前輸出對拍** |
| 8 | 凡期望值為 0／空／順利之檢查**一律附判別力自檢** |
| 9 | 每次寫入後以**獨立指令**驗其落地 |
| 10 | 引用用**可 grep 之唯一字樣**並附唯一性判別力自檢；雜湊記於**該檔檔頭之本體雜湊**、⛔ 不寫進報告 |
| 11 | `git diff --numstat` 對 `app.py`／`run_verification.py`／`wv_reconcile.py`／兩支既有夾具／三支複本／`verify/baselines/` 皆**空輸出**；文件刪除欄 **0** |
| 12 | 凡書寫「正典」二字，就地標明**權威序層級**（本倉共 **4 級**） |

---

## §7 已預先承諾之判準（⛔ 事後不得放寬）

- ⛔ **零生產碼**；⛔ **零新閘、不掛任何東西入 `run_all`**。
- ⛔ 不動 `verify/run_verification.py`／`wv_reconcile.py`／既有兩支夾具／三支端到端複本。
- ⛔ 不改期望 FAIL 名單、⛔ 不重凍任何凍存物、⛔ **不重烤任何基準**。
- ⛔ 不修任何既有 FAIL、⛔ 不解除明細截斷。
- ⛔ 不追改歷史記錄之舊行號錨與舊雜湊。
- ⛔ 文件與凍存物之**刪除欄一律 0**；⛔ 長跑期間不動 `app.py` 與 `verify/`。
- 🔴 **`rc=1` 不構成「命題為偽」**（考古 64）；🔴 清單⛔ 不得由 `grep -A/-B/-C` 抽取（考古 67 修法 ③）。

🔒 **自誌（⛔ 逐條自檢·施工單 §8）**

1. **最大陷阱已定式**：`M2`（逐字重現）為唯一防線——⛔ 泛化後之工具若與舊工具不符，
   **停機**，⛔ 不得因「新工具比較完整」而採信新答案。
2. ⛔ **不以 `G3` 之 3/15 外推**——§2 已寫死「每一項都要自己判」。
3. **乙組之價值在時效**：§5-2 已寫死「標『現在就能取得』者本批就取」。
4. **考古 67／68／69／70 逐條套用**：母體自證＋母體規模（§0-1 已示範）；
   每一「0 命中」附判別力自檢；⛔ 不以 grep 上下文窗抽清單；寫入後獨立驗落地。
5. 🔴 **對本單之保留**：該單自陳施工單方已兩次「前提未現查即寫死預測」。
   本登記對其 `N-1` **已自證**（§0）；`M3` 兩面皆合法，⛔ 不替本單圓場。
6. ⚠️ **本批新增 `.py` 只需驗一道全倉閘**（§0-1 之否定性宣稱）——
   ⛔ 該宣稱本身附三項射程與判別力自檢，非「應該只有一道」之推定。
