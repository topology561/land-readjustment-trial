# 🧾 `W-G.9-164`　**CC 交接文**（新 session 接手用·`W-G.9-163R` 收束時出）

**出文窗**：CC（本 session context 逼近上限）
**倉態基座**：`1616160a827856ccce48d561f1c1d1be07589bd8`（`W-G.9-163R`）
**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ 零登記

---

## 🛑 §零　**最要緊之一件事**（⛔ 讀完再動任何東西）

🔴 **`W-G.9-163R`（`1616160`）於出文時<u>尚未 push</u>**——其 push 依 `W-G.9-163 §七-2` 逐字
「**push 前須請示**（⛔ 不繫於 `deletions` 之值）」，CC 已請示、**待覆**。

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart` 出文時之 tip | `2b7ade94f4ff0e1896919c58314e72e5dd81e49a`（＝ `W-G.9-162R`） |
| 本地已 commit 而**未 push** | `1616160`（`-163R`）＋ 本檔（`-164`） |

🛑 **接手者之第一判斷**：`git ls-remote origin refs/heads/wip/s1-endpart` 之值——

- 若 ＝ **`1616160` 或其後裔** ⇒ 已 push，照 `§一` 正常開工。
- 若 ＝ **`2b7ade9`** ⇒ 🔴 **`-163R` 之成果<u>不在 origin</u>**。其唯一存放處為前窗之拋棄式 clone
  （**該 clone 於出文後即刪**）⇒ 🛑 **須向 KL 確認是否重做 `W-G.9-163`**；
  ⛔ **不得假設其已落地**、⛔ 不得據本檔 `§三` 之「已入倉」逕行下一批。

🔒 **本則之由**：`CLAUDE.md` 最高紀律「**未 push 不得報收官**」之對偶——
**交接文亦不得把未 push 之物記為倉態**。本節即該防線。

---

## §一　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（態 ＝ `1616160`：`736` 列／`67940` B／`sha16 2d44a84937c5a35e`）。
   🔴 **末端已增**：`常規六`／`常規七`／`常規四（七）（八）（九）`／**`常規四（七）補款一（錨定框）`**／
   **`補款二（號之二形並取）`**。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （`3632` 列／`258295` B／`sha16 6a5cada2901a10e8`·`grep -c "^#"` ⇒ **`196`**）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**：`git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNN` ⇒
   `cd` ⇒ `git config core.autocrlf false` ⇒ **對拍 `HEAD` 全 `40` 碼與 `app.py` blob**。
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、**⛔ 不得放 scratchpad**；跑畢**整份刪除**。
   🔴 **若本批將做全文對拍**，clone 路徑須與**期初 log 之產生路徑<u>等長</u>**（見 `§六-1`）。

---

## §二　倉態（態 ＝ `1616160`·CC 現查·量測時點 `2026-08-28`）

| 項 | 值 |
|---|---|
| 本地 tip | `1616160a827856ccce48d561f1c1d1be07589bd8` |
| **`app.py` blob** | **`f285b91c9e5f97f13dd74508e1a403833bc75cb3`**（`22840` 列／`1346395` B／`sha16 17cfe3e70a299a02`） |
| `CLAUDE.md` | `736`／`67940`／`2d44a84937c5a35e` |
| `K-6` 正典 | `3632`／`258295`／`6a5cada2901a10e8` |

🔴 **`app.py` 於本波已<u>二度</u>變更**（`W-G.9-136` 以來首見）：
`b4cc3083…` →（`-161`）→ `8daca97f…` →（`-163`）→ **`f285b91c…`**。

### 🔒 取號現查（**常規四（八）** 定義列式·母體 ＝ `docs/**/*.md` ＋ `CLAUDE.md` ⇒ `392` 檔）

| 類 | 定義列式（逐字） | 相異 | **MAX** |
|---|---|---|---|
| `docs/reports` 之 `W-G.9-NNN` | `^docs/reports/W-G\.9-(\d{1,4})` | — | **`164`**（＝本檔） |
| 自誤 | `^#+ 自誤 \`?([0-9]{1,4})\`?` | `166` | **`197`** |
| `VR` | `^##\s*\`VR-([0-9]{1,4})\`` | `63` | **`77`** |
| `GB` | 三框聯集（母體 ＝ `W-G.4` 登記表**單檔**） | `109` | **`111`**（缺號 `[12, 87]`） |
| 常規 | `^\*\*常規[一-十]` | — | **七**（另有常規四之補款 `七`／`八`／`九` ＋ `（七）補款一`／`補款二`） |

🛑 **取號之三戒**（本波所立·⛔ 缺一不可）：
1. **定義列式**取 MAX（常規四（八））；🛑 裸框⛔ 不得用於取號。
2. **錨定框** `(?<![0-9\-])<完整號>(?![0-9])` 查意指占用（補款一）；⛔ 不得用號之尾段。
3. **二形並取**（補款二）：`自誤 N` 與 `` 自誤 `N` `` **二形皆須量並二數並報**；
   🔴 **對照組得 `0` 者一律先判<u>量測器紅</u>**。

---

## §三　本波之位置（`-155` 〜 `-163`）

| 單 | commit | 要旨 | 狀態 |
|---|---|---|---|
| `-156R` | `b8ac8f2` | 八格驗收預測表之可重算輸入（`GB-109` 素材·只讀探針） | ✅ 在倉 |
| `-157R` | `12d84d3` | `VR-075`（`R3右` 遞補宗 → `628-31(3)`）＋ `GB-109` 解除 ＋ **常規八** | ✅ 在倉 |
| `-158R` | `2d72de3` | `GB-110` 索引補正（二錨）＋ 自誤 `193` ＋ **常規四（九）** | ✅ 在倉 |
| `-159R` | `de3d63f` | 🛑 **停機**：`L-3 ④` 之 `⊆` 恆假／五類不足／`BASE` 歧義 | ✅ 在倉 |
| `-160R` | `77ebc49` | **`VR-076`** 三受詞類 × 三母體層 ＋ `GB-105` 前提否證 ＋ **錨定框補款** | ✅ 在倉 |
| `-161R` | `3e8a8ab` | 🔴 **首張生產碼批**：`_PROJ_POP_DECL` ＋ 三類不變式落地 | ✅ 在倉（**惟其「零行為變更」宣稱已被 `-162` 證偽**） |
| `-162R` | `2b7ade9` | 🛑 **停機**：全文對拍揭出 `-161` 之**真實破壞** ＋ **`VR-077`**／`GB-111`／自誤 `196` | ✅ 在倉 |
| `-163R` | `1616160` | `_WF_NS_NAMES` 補列（**修該破壞**·`Z-1″ rc=0`）＋ `VR-077 二` 更正 ＋ 自誤 `197` | 🔴 **見 `§零`** |

### 🔑 `-159` 〜 `-163` 之結論鏈（**下一單之全部前提**）

1. 🔒 **`VR-076`**：`_projection_order` 十二呼叫點分**三受詞類 × 三母體層**——
   `POP_SYNC` `4` 點（產 `pre_position`／`_rank_by_tpid`）／`ORDER_INVARIANCE` `7` 點（前後二次呼叫之序對拍）／
   `NAME_DERIVATION` `1` 點（導出合併後之名）；層 ＝ `TEMP_LAYER`（步驟 0 合併**前**）／
   `BUILD_LAYER`（合併**後**）／`RESHAPE`（合成 dict）。🔴 **前二層之 id 空間<u>不相交</u>**。
2. 🔒 **已落地（`-161`＋`-163`）**：`app.py` 之 `_PROJ_POP_DECL`（**`14` 列 ＝ `12` 呼叫點 ＋ `2` 透傳呼叫端**）
   ＋ 四支斷言器 ＋ `_proj_pop_filter`（濾式唯一定義處）＋ `_proj_pop_note`（`L-6′` 計數）；
   `filter` 相異值 **`6`** 類。**未宣告之 `tag` ⇒ `raise`** ⇒ 新增呼叫點必被逼入表。
3. 🔒 **`VR-077`**（准紅碼分支上「零行為變更」之判準）：
   **一** 判準 ＝ **「期初 ≡ 期末」**，⛔ 非「全綠」（`CLAUDE.md:71` KL 裁：本分支為**准紅碼**）。
   **二** 對拍**粒度須為<u>全文</u>**；僅得正規化三類並附判別力自檢：
   ① 絕對路徑／臨時目錄名 ② 時間戳 ③ **任一 `<檔名>.py:<行號>`**（`-163` 已將 ③ 自 `app.py` 擴為任一檔）。
4. 🔴 **`GB-105` 尚未解除**——其解除依 `W-G.9-162 §五` 繫於全文對拍 `= 0`；
   `-162` 得 `22`、`-163` 得 **`2`** ⇒ **仍未達**（`§四-2` 為其唯一殘項）。

---

## §四　🛑 下一單之受詞（**二選一·由發單側落筆**）

### `1`　`Z-2″` 之殘餘 `2` 列（`-163R` 之停機點）

🔒 **逐字**（正規化後·`-163R §四`）：

```text
── 只在期初 ──   497│現況:                        params, build, winners, forced, setback)
── 只在期末 ──   497│現況:                       params, build, winners, forced, setback)
```

差 ＝ `現況:` 之後**一個空白**（`24` vs `23`），其後文字逐字相同。
🔒 **機制已隔離**：被截斷之內嵌 traceback；上游 `stepg_pipeline.py, line 995`（期初）／`line 1001`（期末）
**後者長 `1` 字元** ⇒ 固定 offset 之截斷點位移 `1`。
規則 ③ 未吸收係因**該片段中 `line NNN` 之文字本身已被截掉**；`strip()` 未吸收係因該空白位於**行內**。
🔴 **來源批 ＝ `W-G.9-161`**（其於 `verify/stepg_pipeline.py` `+6` 列），`-163` ⛔ 未動該檔一字。
🛑 **CC ⛔ 未擴充正規化規則使其歸零**（`-163 §七` 末逐字所禁）。
⇒ **待裁**：是否補入第四類正規化（「**因行號位數變動而生之行內截斷位移**」），抑或另立處置。

### `2`　`GB-105` 之解除（繫於上項）

`-162 §五` 之 `GB-105` 解除 payload **⛔ 未入倉**（其入倉繫於全文對拍 `= 0`）。
🔒 其內容（十四 tag 之具名）已由 `-162` 單載明，可逕行沿用。

### `3`　🔴 **`§零之零` 之診斷更正**（`-163R §一`·待發單側落筆）

`W-G.9-163 §零之零` 載「二閘皆存在…且**確實紅了**」。
🔒 **CC 現查 ＝ 只有一支發聲**：`fixture_wf_ns_wiring.py` 確紅；而反向閘
`run_verification.py:1355-1357` 之字樣 `漏列` 於**二份全文 log 命中皆 `0`**。
**根因（AST 現查）**：`:1309` 與 `:1355-1357` 位於**同一 `try`**（`lineno 1308 – end_lineno 1393`），
`:1309` 之 `KeyError: '0m'`（trunk A 中止之連坐）使 `:1355` **從未執行**
⇒ 🔴 **`GB-61` 洞 A「寄生失聲」之又一實例**，且其受詞恰為本波所需之守門閘。

---

## §五　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104` 之 loud 斷言（`_block_strip` 雙線⛔ 無交叉閘） | ⬜ 未辦 |
| `2` | `GB-107`（`wg960_z_selfcheck.py:12` 寫死絕對路徑·該 worktree 已不存在） | ⬜ 未修 |
| `3` | `GB-108`（`K-9-23-a` 八格表對「`σ` 由形心導出」⛔ 無判別力） | ⬜ 未辦 |
| `4` | `GB-110`（八格表對閘一之**關係詞** `>` vs `>=` ⛔ 無判別力） | ⬜ 未解除 |
| `5` | `GB-111`（凍存期望 FAIL 名單已失效且**結構上無原因欄**·`46` 項僅 `18` 附片語） | ⬜ **須 KL 決重產時點** |
| `6` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `7` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `8` | `K-9-22` 落地（原地號保留 ＋ 區段回退）／`VR-074`／`VR-075` 落地 | ⬜ |
| `9` | `K-6-A2` 剩餘六項／`K-6-B` 段一〜段四 | ⬜ |

---

## §六　🔑 本波所立之家法與陷阱（🛑 接手者必讀）

1. 🛑 **全文對拍之作法**（`VR-077 二`·`-162`／`-163` 已二度施行）：
   ① 二支長跑於**獨立 clone**（**一 clone 一支**）且**路徑<u>等長</u>**
      ——本專案已知「**工作目錄名長度差 `1` byte 即令輸出差 `1` 字元**」（固定 offset 截斷）。
   ② 用 **LCS**（`difflib.SequenceMatcher`）取真差集，⛔ 非逐位平移比較
      （`-162` 實測：平移法得 `459`、LCS 得 `22`）。
   ③ **期初 log 已入倉**：`verify/out/runall_77ebc49_full.log`（`46005` B）／
      `runall_3e8a8ab_full.log`（`46754` B）／`runall_f285b91_full.log`（`46006` B）
      ⇒ 🔒 **後續生產碼批得逕與之對拍、⛔ 不必再重跑期初**。
   ④ **判別力自檢**：未正規化之二 `sha256` 須**相異**；人造一處**診斷文字**之差須使 `diff` **轉紅**。
2. 🛑 **凡於 `verify/**` 新增 `ns["X"]` 之消費，必須<u>同批</u>把 `X` 加入 `app.py` 之 `_WF_NS_NAMES`**
   ——否則 app 生產路徑（`_build_wf_ctx` → `_wf_ns()` → `wf_f0.compute` → `run_step_g`）**必 `KeyError`**。
   🔴 **同族已第三度**（`W-8 補漏`／`W-G.9-4b`／`W-G.9-161`）；清單自身之註解早有明文。
3. 🛑 **名目框看不見 `run_all` 之 `[1/3] golden` 段**——該段輸出形如 `  ✅ 末端夾具 <名>（…）rc=0`，
   **⛔ 不含 `✅ PASS`／`🔴 FAIL` 字樣**；`run_all.py` 自身註解亦載其「**不進** `run_verification.results`」。
   ⇒ 🔴 `-161R` 之「`34` 閘 `diff rc=0`」為真而**結論不成立**（母體未涵蓋）。
4. 🛑 **凡作「逐項歸類且宣稱無殘餘」者，須同格列各類之數與其和並與總數對拍**
   （CC 自立·`-163` 自捕：`-162R` 之（甲）少計 `1` 列，三類和 `21` ≠ `22` 而我寫「無殘餘」）。
5. 🛑 **對照組得 `0` ⇒ 先判量測器紅**（常規四（七）補款二 二）——
   `-161` 實例：`自誤 194` 無反引號得 `0`／帶反引號得 `3`；`-163` 實例：`自誤 196` 得 `0`／`2`。
6. 🛑 **判別力對照之粒度須與受詞同級**（`-158` 自捕）——受詞是「某一列之形」則對照亦須**逐列**；
   整塊掃會被塊內他錨餵飽而恆綠。
7. 🛑 **`redirect_stdout(StringIO)` 撞 `sys.stdout.reconfigure`** ⇒ `AttributeError` ⇒ 記錄 `0` 筆。
   🔴 **`0` 筆若逕行出艙即成「該點從未被呼叫」之假結論**。⇒ 收長跑輸出一律用 shell 之 `>`。
8. 🛑 **前批之報告入倉本身即改變母體**（`-160` 之 `Q-7` 即此）——
   凡預測繫於「某字樣命中數」者，其**態一律取本批之基座**，⛔ 不得沿用前批之數。
9. 🛑 **`git` 身分未繼承**：clone 後須 `git config user.name diag` ／ `user.email diag@local`
   （**⛔ 非 gmail**·與既有 commit 之 author 一致）。`/c/` 根**不可寫** ⇒ 腳本落 scratchpad、clone 落 `/c/wNNN`。
10. 🛑 **⛔ 不得為使驗收轉綠而修改濾式、放寬容差、關閉斷言、或擴充正規化規則**
    ——**其不過本身即該批之產出**（`-159`／`-162`／`-163` 三度施行）。

---

## §七　已知 harness 限制

- 🔴 **`run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止** ⇒ `wf_f0`〜`f4` **主路徑從未執行**；
  `stepg_ctx["0m"]` 缺 ⇒ 其後諸閘連坐（含 `run_verification.py:1355` 之反向閘·見 `§四-3`）。
- 🔴 **`app.py:main()` 內之敘述從不被 `run_all` 執行**（`CLAUDE.md` 逐字）。
- 🔒 **`run_all` 於基座即 `FAIL`**（**准紅碼**）：`RESULT: FAIL`／`EXIT=1`／`11 PASS`／`23 FAIL`／
  對帳器 `類 III` `13` 項 ⇒ 🛑 **凡預測寫「`run_all` 全綠」者，其前提於基座即不成立**。
- 🔒 **`Z-4′②` 之逐點計數**（`WV_PROJ_POP_COUNT`）：`run_all` 一趟中 `14` tag 有 **`7` 個為 `0`**
  （`main()` 內 `2` ／ trunk A 中止致 `wf_*` 主路徑 `5`）；
  🔒 惟 `wf_f4:_order_fb`＝`2`／`wf_f4:_order`＝`22` 由 **fixture subprocess** 驅動 ⇒ **斷言於子行程確實執行**。
- ⚠️ `run_all`／`run_verification` 須於**拋棄式 clone** 內跑（會改寫 `verify/out/**` 之已追蹤 log·`GB-94`）。

---

## §八　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**：`wg9161-projpop-landed`（**本波主線·含 `-161`〜`-163` 之全部結論**）／
`wg9159-projorder-base-refuted`／`wg9156-blue-shadow-reproduced`／
`push-before-shipping-claim`（🔒 **未 push 不得報收官**）／
`grep-multibyte-charclass-trap`（量測器靜默給錯數之諸面）／`run-all-side-effects`／
`harness-worktree-crlf-trap`。

---

## §九　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit
  （**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值。
- 🔒 **拋棄式 clone 跑畢整份刪除**，並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push——① 零生產碼變更 ② 純新增檔（或既有檔之**純附加**·
  `deletions = 0`）③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠。
  🛑 **惟施工單另令「須請示」者，以單為準**（`-161`／`-163` 皆然）。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`
  （期望 `rc=0`·`stderr` `0` B·逐字「結論：✅ 全部成立」·受檢 `77` 筆）。
