# `W-G.9-271R`　`s` 框之同源性 ／ `GB-153` 前提之實測 ／ `R6` 起點端 ／ 步驟 D 分母 ／ `自誤 375`

> **態**｜開工 ＝ `c168e993e73cb6775ebf72b46e4337ca812967ca`（`ls-remote` 實查）｜
> 工項零後 ＝ `f20047f86df7dfda7d499978e5e77053ecc92a49`
> **級** ＝ **中**（純登記 ＋ 純診斷·含一次實跑·**零生產碼**）
> 🛑 **本批⛔ 辦**（單 `§五`）：⛔ 動生產碼 `34` 檔一字（**⛔ 改五個 `*_EXPECT` 任一字**）／
> ⛔ 落地 `K-9-29` 之任何子項・`GB-164`・`GB-166`／`GB-162` 三禁令維持／
> **⛔ 判 `GB-153` 之存廢、⛔ 判 `p6` 之結論作廢、⛔ 判 `R6` 之片為缺陷、⛔ 判 `p6` 二數孰誤**／
> ⛔ 呈 KL 任何事。
>
> 🔒 **辦畢 ⇒ 出報告 ⇒ 停**（⛔ 自續下一批）。

---

## `§零`　開工閘與工項零

### 零-1　`§零` 開工閘（五項全綠）

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `ls-remote` 主線 | `c168e993e73cb6775ebf72b46e4337ca812967ca` | 🟢 |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1`（＝ 期值） | 🟢 |
| `3` | `p3a` `05a11bd…` 非祖先 | `merge-base --is-ancestor` ⇒ **非祖先** | 🟢 |
| `4` | `probe_order_preflight.py` | `P-3`／`P-5` 機械停機款 **`0`** 項；二 🟡 提示**逐項具名處置**（見下） | 🟢 |
| `5` | 環境自證 | `python 3.13.11`／`ezdxf 1.4.3`／`shapely 2.1.2`／`streamlit 1.56.0`／`pandas 2.3.3`／`numpy 2.4.4`／`pyproj 3.7.2`（**⛔ `try/except` 靜默吞**·皆在） | 🟢 |

**閘 `4` 之二 🟡 提示（⛔ 靜默略過·恆綠即器紅）**

| 提示 | 受詞 | 處置 |
|---|---|---|
| `P-5` | 本單⛔ 含 `SELF_SHA256` 列 | **具名豁免**——依「`序 11` 之一般化」款 `三`，此為「**無從回測**」，**⛔ 等同「回測不過」**。🛑 **⛔ 由 CC 代擬**其自證值（受詞由受測方提供 ⇒ 判別力歸零·同款 `一`）；欲使該閘及於全鏈，**須發單側於單內同格載其送出時之 `sha256`**——**候發單側決** |
| `P-4` `:72` | 「方向性轉引而同段⛔ 具名座標系」 | **具名豁免·偽陽**——該箭頭係 `二-1` 款 `2` 之 `` `R1`→`R6` ``，其受詞為**街廓之列舉序**，⛔ 一個量之**變化方向** ⇒ 無座標系可言 |

### 零-2　工項零（本單原封入倉·commit `f20047f`）

| 量 | 值 |
|---|---|
| **搬移前自證**（`§二-B` 款 `3`） | 母體 ＝ 主 checkout `docs/orders` 之 `listdir`（**`129`** 項）；完整檔名逐字比對 ⇒ **所指之檔數 ＝ `1`** ✅（判別力：人造名 `0` ✅／`CLAUDE.md` `1` ✅） |
| **條款一** `CR` | **`0`**（`LF` `188`·bytes `14344`）；判別力造甲（真 CRLF·**執行期組出**）⇒ `CR = 2` ✅ |
| **條款一** `core.autocrlf` 三層 | `system` **`true`**／`global` **未設**（`rc=1`）／`local` **`false`** ⇒ **有效值 `false`**（⛔ 以 `.gitattributes` 之記載代之） |
| **條款二** blob `sha1` | `0837e77d8037369075c5839f6ecb7ece42a06fac` |
| **條款二** blob 內容 `sha256` | `fb1c18f8390c04f06f6c890b3dfcdd4987ab845ed9eca4ef780ae3278537f331` ＝ **磁碟值**；**逐位相同 `True`** ⇒ 🟢 往返逐位保存 |
| **條款三** 來源逐字 | 主 checkout `docs/orders/` 之 untracked 檔·`14344` B·由 KL 於本回合貼入。全長 `sha256` 係 **CC 實測值**，⛔ 經發單側復驗 |

---

## `§一`　工項 `1`：`GB-153` 前提之實測（🛑 **出艙即止**·⛔ 判其存廢）

**器** ＝ `verify/run_verification.py` 一次全量實跑｜落檔 `verify/out/WG9271_fullrun.log`
（**`160055` B／`1030` 列**·`rc = 1`·**stderr `0` B**·歷時 `3` 分 `17` 秒）｜彙整落檔 `verify/out/WG9271_gb153.log`。
🔒 **三軸**｜`(1)` 來源 ＝ 活體實跑＠`f20047f…`｜`(2)` 母體 ＝ 上開 log 單檔｜`(3)` 粒度框 ＝ **列框**。

### 一-1　`RESULT` 與紅項數

| 量 | 值 |
|---|---|
| `RESULT` 逐字 | `RESULT: FAIL` |
| 名目 | `✅ PASS` **`24`** ／ `🔴` **`55`**（框 ＝ 列首之符·`str.startswith`） |
| 對照組[必為零] | 列首之一人造符（**執行期組出**）＝ **`0`** ✅ |

### 一-2　🔴 **逐街廓 `R1`→`R6` 之到達 ＝ 六街廓<u>全部到達</u>**

🔒 **逐字錨**（⛔ 行號）＝ `` [T2-DIAG] 街廓 R `` ＋ 號。

| 量 | 值 |
|---|---|
| `[T2-DIAG]` 之列命中 | **`90`** |
| 出現序（逐字） | `R1, R2, R4, R6, R5, R3` × **`15`** 輪 |
| 相異街廓 | `['R1', 'R2', 'R3', 'R4', 'R5', 'R6']` ⇒ **六街廓全到** |
| 對照組[必非零] | `[T2-DIAG]` 之列框 ＝ `90`（`>0`）✅ |
| 對照組[必為零] | 一人造標記（**執行期組出**）＝ `0` ✅ |

🔒 **情境閘之逐字（二列·皆 `✅ PASS`）**

> `✅ PASS  結構不變量永久閘0m（守恆/⊥/位次=投影序/J(k*)≥J(naive)/理論=實跑/telescoping）`
> `✅ PASS  結構不變量永久閘3.5m（守恆/⊥/位次=投影序/J(k*)≥J(naive)/理論=實跑/telescoping）`

⇒ 🔴 **`GB-153` 逐字前提之<u>第一肢</u>（「`StepG` 於 `R2` 中止」）於現態<u>⛔ 成立</u>。**

### 一-3　`F.0`–`F.4` 六族 CSV 之可得性

🔒 **落檔真名之逐字根據**（`run_verification.py` 之 `_dump_csv` 呼叫·正面列舉）：
`got_F.0_{nm}_退縮{tag}.csv`／`got_F.1_…`／`got_F.2_…`／`got_F.3_…`／`got_F.4_…`。

| 族 | `verify/out` 內之檔數 |
|---|---|
| `got_F.0_*` | **`0`** |
| `got_F.1_*` | **`0`** |
| `got_F.2_*` | **`0`** |
| `got_F.3_*` | **`0`** |
| `got_F.4_*` | **`0`** |
| 判別力[必非零] `got_G值_*` | **`2`** ✅ |
| 判別力[必為零] 一人造前綴（**執行期組出**） | **`0`** ✅ |

（母體 ＝ `verify/out` 之 `listdir` **`773`** 項。）

⇒ 🔴 **`GB-153` 前提之<u>第二肢</u>（「`F.0`–`F.4` 六族 CSV 全不可得」）於現態<u>仍成立</u>**，
**惟其<u>成因已改</u>**（逐字·⛔ 濃縮）：

| 名目 | 逐字理由 |
|---|---|
| `W-F F.0` | `[F.0] 🔴 GSA 錨檢破（0m）`——錨鍵 `['G006','G007','G009','G010','G014','G017']` **未被任何一次比對評估**（**已評估鍵 ＝ `[]`**）⇒「**沒人檢查≠相符**」 |
| `W-F F.1` | `RuntimeError: F.0 未成功，F.1 跳過（W5 守衛）` |
| `W-F F.2` | `RuntimeError: F.0 未成功，F.2 跳過（存在性守衛）` |
| `W-F F.3` | `RuntimeError: F.2 未成功，F.3 跳過（存在性守衛）` |
| `W-F F.4` | `RuntimeError: F.3 未成功，F.4 跳過（存在性守衛）` |
| `W-G G.2` | `[G.2] 上游世代未產出：['F.0', 'F.1', 'F.2', 'F.3', 'F.4']（0m）——…G.2 **無從檢查**、非 G.2 自身破。` |

🔒 **`GB-153` 案由所載之二檔·照實並列（⛔ 判孰誤）**

| 檔 | `GB-153` 案由所載 | 本次實跑 |
|---|---|---|
| `got_G值_退縮0m_partial.csv` | `21` 列 | **⛔ 在**（本次未產出該名） |
| `got_G值_退縮3.5m_partial.csv` | `24` 列 | **⛔ 在**（本次未產出該名） |
| `got_G值_退縮0m.csv`（**無 `_partial`**） | —— | **在**·**`57`** 列／`16718` B |
| `got_G值_退縮3.5m.csv` | —— | **在**·**`60`** 列／`17565` B |

（`got_*` 係 `.gitignore` **刻意忽略**之族 ⇒ ⛔ 得列為缺失·`CLAUDE.md` 明載。）

### 一-4　`R4` 左街角之量（`c2` 之 `V-H` 之前提）⇒ 🟢 **可得**

| 情境 | 落檔逐字 |
|---|---|
| `0m` | `R4,左,116.08,強制抵費地` |
| `3.5m` | `R4,左,226.01,強制抵費地`；`R2,左,308.17,強制抵費地` |

⇒ `GB-153` 前提所稱「`R4` 在 `R2` 之後 ⇒ `V-H` 亦不可得」之**推理鏈已斷**（`R4` 確已到達且其量已落檔）。

🛑 **本節⛔ 判 `GB-153` 已失效／應解除／應收窄**——**出艙即止**，其裁由發單側。

### 一-5　🔴 一項**本節新捕之紅**（照實回報·⛔ 自裁·⛔ 修）

> `🔴 FAIL  W-G G.1 接線層 ctx-builder 同源（…）`
> `      引擎消費但 _WF_NS_NAMES 漏列（app 路徑必 KeyError）：['_wg9268p_anchor_advance']`

🛑 其受詞屬**生產碼**（`verify/run_verification.py` 之 `_WF_NS_NAMES`）⇒ **本批⛔ 動一字**；
照實具名，其處置**候發單側**。

---

## `§二`　工項 `2`：`s` 框之同源性（**本批之主檢**·🛑 出艙即止）

**器** ＝ `verify/probes/probe_WG9271_s_frame.py`（**以 `probe_WG9270R4_p6_interleave.py` 之 `drive_spy` 為底本**·
⛔ 另寫第二份判準、⛔ 器內重導切線座標）｜落檔 `verify/out/WG9271_s_frame.log`（`rc = 0`·**stderr `0` B**）。
🔒 **容差 ＝ `_S_EPS`（自 `ns` 取值·⛔ 器內另定常數）＝ `0.0002`**；重疊長 ＝ `max(0, min(b, y) − max(a, x))`。

### 二-0　判別力二造（**先於**任何實測）

| 造 | 受詞 | 實測 | 判 |
|---|---|---|---|
| 甲[必重疊] | 一宗之區間與**其自身**對拍 | 重疊長 ＝ `25.000000`（其長 ＝ `25.000000`） | ✅ |
| 乙[必不重疊] | 一**人造區間**（**執行期組出**·字面⛔ 出艙）置於 `s` 域外 | 重疊長 ＝ `0.000000` | ✅ |

⇒ **器非紅**（二造異色）。

### 二-1　呼叫次數（單 `§二-2` 末款）

| 情境 | 逐街廓之呼叫次數 | 判 |
|---|---|---|
| `0m` | `R1`–`R6` **各 `1` 次**（回傳片數 `2／2／1／4／2／2`） | **無一街廓被呼叫 `>1` 次** |
| `3.5m` | `R1`–`R6` **各 `1` 次**（回傳片數 `3／2／1／4／2／2`） | 同上 |

⇒ 🔒 **⛔ 有須「逐次分列 `corner_pt`／`d_hat`／`allocation_dir`」之情形。**

### 二-2　🔴 **組甲之準據（逐片重疊長須全為 `0`）⛔ 成立**

| 情境 | 街廓 | 組甲 非零／總 | 組乙 非零／總 |
|---|---|---|---|
| `0m` | `R1` | **`1`**／`2` | `1`／`2` |
| `0m` | `R2` | **`1`**／`2` | `1`／`2` |
| `0m` | `R3` | **`0`**／`1` | `0`／`1` |
| `0m` | `R4` | **`2`**／`4` | `2`／`4` |
| `0m` | `R5` | **`1`**／`2` | `1`／`2` |
| `0m` | `R6` | **`0`**／`2` | `0`／`2` |
| `3.5m` | `R1` | **`2`**／`3` | `2`／`3` |
| `3.5m` | `R2` | **`0`**／`2` | `0`／`2` |
| `3.5m` | `R3` | **`0`**／`1` | `0`／`1` |
| `3.5m` | `R4` | **`2`**／`4` | `2`／`4` |
| `3.5m` | `R5` | **`1`**／`2` | `1`／`2` |
| `3.5m` | `R6` | **`0`**／`2` | `0`／`2` |

🔒 **組甲之非零片總數 ＝ `10`／組乙 ＝ `10`（<u>逐格相同</u>）。**

**逐片具名**（🛑 **⛔ 判其孰誤**·單 `§二-2` 明令）：

| 情境 | 街廓 | 池片 | `s` 起 | `s` 迄 | 面積(㎡) | `s` 長 | Σ重疊長 | 重疊／本長 | 重疊之宗 |
|---|---|---|---|---|---|---|---|---|---|
| `0m` | `R1` | `R1-池#2` | `-0.324813` | `-0.035835` | `0.0046` | `0.288978` | **`0.288978`** | **`100.0%`** | `R1-biz#1` |
| `0m` | `R2` | `R2-池#2` | `4.055372` | `10.839544` | `152.3551` | `6.784172` | **`6.784172`** | **`100.0%`** | `R2-biz#1: 3.393333`; `R2-biz#2: 3.390839` |
| `0m` | `R4` | `R4-池#3` | `1.576666` | `49.529873` | `26.1116` | `47.953207` | **`45.030121`** | **`93.9%`** | `R4-biz#1` |
| `0m` | `R4` | `R4-池#4` | `49.529873` | `97.236015` | `26.0858` | `47.706142` | **`17.470632`** | **`36.6%`** | `R4-biz#1` |
| `0m` | `R5` | `R5-池#2` | `4.849830` | `11.485592` | `145.1703` | `6.635762` | **`6.635763`** | **`100.0%`** | `R5-biz#1: 3.311945`; `R5-biz#2: 3.323818` |
| `3.5m` | `R1` | `R1-池#2` | `17.240188` | `17.564931` | `5.3835` | `0.324743` | **`0.324742`** | **`100.0%`** | `R1-biz#1` |
| `3.5m` | `R1` | `R1-池#3` | `-0.324813` | `-0.035837` | `0.0046` | `0.288976` | **`0.288976`** | **`100.0%`** | `R1-biz#1` |
| `3.5m` | `R4` | `R4-池#3` | `7.818563` | `9.395131` | `26.1102` | `1.576568` | **`1.576567`** | **`100.0%`** | `R4-biz#1` |
| `3.5m` | `R4` | `R4-池#4` | `68.920124` | `70.495919` | `26.0843` | `1.575795` | **`1.575795`** | **`100.0%`** | `R4-biz#1` |
| `3.5m` | `R5` | `R5-池#2` | `8.059214` | `14.718028` | `146.1806` | `6.658814` | **`6.658815`** | **`100.0%`** | `R5-biz#1: 3.323450`; `R5-biz#2: 3.335364` |

🔒 **`10` 片中 `8` 片之重疊長 ＝ 本片 `s` 長之 `100.0%`**；另 `2` 片（`0m·R4`）為 `93.9%`／`36.6%`。
🛑 **⛔ 判其孰誤、⛔ 判其為缺陷**——照實具名，其裁候發單側。

### 二-3　🟢 組丙：`biz_polys` 與 `cut_coords` 之 `s` 區間 **逐宗一一對應·逐位相同**

| 量 | 值 |
|---|---|
| 一一對應之〔情境 × 街廓〕格 | **`12`**／`12`（**有殘餘者 `0`**） |
| 全部配對之 **max\|Δ\|**（`s` 起／`s` 迄／面積 三欄） | **`0.000000000000`** |
| 配對法（**具名**·⛔ 隱含） | 對每一 `cut_coords` 之宗，取其與各 `biz_polys` 之 `s` 區間**重疊長最大**者；重疊長皆 `≤ _S_EPS` 者具名為**無配對**（⛔ 靜默丟） |

⇒ 🔒 **二形（`biz_polys` vs `cut_coords`）於 `s` 框上<u>同源</u>**；併證組甲與組乙**逐格相同**
（非零片數逐格一致·總數皆 `10`）⇒ **「`p6` 之取法」與「同一次呼叫所錄之 `biz_polys`」於本檢上⛔ 可區辨**。

🛑 **本節⛔ 判 `p6` 之結論作廢、⛔ 判「交錯」之有無**——出艙即止。

---

## `§三`　工項 `3`：`R6` 起點端之成因（🛑 出艙即止）

**器** ＝ `verify/probes/probe_WG9271_r6_start.py`（承 `§二` 之 `drive_spy`·實參逐位同源）｜
落檔 `verify/out/WG9271_r6_start.log`（`rc = 0`·**stderr `0` B**）。

**判別力二造**：甲[必非零] `s_max − s_min` ＝ **`89.461740602`**（`>0`）✅；
乙[必為零] 同一 `block_poly` **自比**之 `s_min` 差 ＝ **`0.000000000000`** ✅ ⇒ **器非紅**。

| # | 受詞 | `0m` | `3.5m` |
|---|---|---|---|
| `1` | `s` 域 `(s_min, s_max)`（**未捨入**） | `(-3.606791806, 85.854948797)` | **逐位相同** |
| `2` | 鏈頭第一宗（`s` 起最小之配地宗） | `628(2)+`·`s` 起 ＝ **`-0.000000000`**·面積 `153.1791 ㎡` | **逐位相同** |
| `3` | `2` − `1` 之差（**未捨入**） | **`3.606791806` m**（`2dp` 顯示 ＝ `3.61`） | **逐位相同** |
| `4` | `R6-池#2` 之 `s` 區間與面積 | `s ∈ [-3.606791806, -0.000000000]`·`s` 長 `3.606791805`·**面積 `85.7064 ㎡`** | **逐位相同** |

（併載 `R6-池#1` ＝ `s ∈ [27.302533319, 61.354566236]`·`s` 長 `34.052032917`·面積 `1543.0063 ㎡`。）

🔒 **機械事實**：`block_poly` 之 `s` 域起於 `-3.606792`，而**鏈頭第一宗之 `s` 起 ＝ `0`**
⇒ 區間 `[s_min, 0]`（長 `3.606792`）**未被任何配地宗佔用**，其即 `R6-池#2`（`85.7064 ㎡`）。
**單所稱之 `85.71 ㎡` ＝ 其 `2dp` 顯示值**（未捨入值 ＝ `85.7064`）。

🔒 **照實並列已登記之錨移二值**（🛑 **⛔ 判其為同一量**）

| 出處之情境 | 街廓 | 側 | 錨移值 | 本量 |
|---|---|---|---|---|
| `0m` | `R2` | `left` | `+3.39 m` | `R6` 之「鏈頭 `s` 起 − `s_min`」＝ `3.606792 m` |
| `3.5m` | `R5` | `left` | `+3.32 m` | 同上 |

🛑 二者**街廓相異**（`R6` vs `R2`／`R5`）、**側之受詞相異**（本量⛔ 分側）
⇒ **⛔ 得逕判為同一量**；先對齊母體／受詞再判，其裁候發單側。
🛑 **⛔ 判其為缺陷、⛔ 提任何處置主張。**

---

## `§四`　工項 `4`：步驟 D 分母之二態量（⛔ 動任何碼）

**器** ＝ `verify/probes/probe_WG9271_stepD_denom.py`｜落檔 `verify/out/WG9271_stepD_denom.log`（`rc = 0`·**stderr `0` B**）。
🔒 **受詞之逐字錨**（⛔ 行號）＝ `all_original_parcels = sorted({tp['原地號'] for tp in temp_parcels`
（判別力：寬字樣 `all_original_parcels` ＠ `app.py` ＝ **`6`** ⇒ 該檢⛔ 恆為 `1`）。
🔒 **共用性之逐字根據** ＝ `app.py` 註解「…`verify/selection_pipeline.build_build_parcels` **共用之最後一道 app 側轉換**」。

| 量 | 態甲（`WV_K6_STEP0` **未設** ＝ 預設 `on`·**現行生產態**） | 態乙（`WV_K6_STEP0=off`） |
|---|---|---|
| `temp_parcels` 筆數 | **`113`** | **`126`** |
| **分母**（去 ghost sliver 之原地號相異） | **`51`** | **`53`** |

| 項 | 值 |
|---|---|
| 乙∖甲（`off` 有而預設無） | **`2`** 筆：`['628-28', '628-48']` |
| 甲∖乙 | **`0`** 筆 |
| 第三方對照 `snap["財務接線_v3"]["原地號_區段"]` | **`53`** 筆（型 `dict`）⇒ 與**態乙差 `+0`**、與**態甲差 `+2`** |

**步驟 `0` 之 `_diag` 四欄**

| 欄 | 態甲 | 態乙 |
|---|---|---|
| `parcels_in` | `126` | **不可得**（`off` ⇒ 步驟 `0` 未執行·具名·⛔ 以 `0` 代之） |
| `parcels_absorbed` | `13` | 同上 |
| `parcels_out` | `113` | 同上 |
| `groups_merged` | `9` | 同上 |

🛑 **準據：二態之大小須相異** ⇒ 態甲 `51` vs 態乙 `53` ⇒ 🟢 **相異（如期）**，⛔ loud 停。
🛑 **本項僅以環境變數驅動既有出口**——**⛔ 落地 `K-9-29` 任何子項、⛔ 動 `k6_step0_enabled` 之預設一字**；
執行後已將 `WV_K6_STEP0` 自環境**還原**（⛔ 遺留於後續量測）。

---

## `§五`　工項 `5`：`wf_f0` `a′` 已落地之登記

落於 `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（⛔ 節末）·題逐字依單。
其**證**（CC 當場復現）：`add += a_m * (p_of(m) / p_t)` 命中 **`1`**；`先除後乘` **`1`**；`停機#2` **`1`**；
`p ≤ 0 ⇒ raise` 之逐字俱在 ⇒ 🟢 **子項 `2` 已落地**。
**射程**：子項 `1`（`temp_parcels, _diag_k6 = k6_step0_merge(` ＝ **`1`**）🔴 **仍在**；子項 `3`／`4` **本節⛔ 及之**。
🩸 **併呈**：該對位表節（`74` 列）內 `K-9-29`／`wf_f0`／`a′`／`Σa`／`步驟 0`／`入池閘` **命中皆 `0`**
⇒ **本更正之受詞⛔ 見於該表之表內**；照實具名，**⛔ 自行改題、⛔ 自行改受詞**，其處置候發單側。

---

## `§六`　工項 `6`：`自誤 375` 之鑄

落於 `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末**·節題形取 `自誤 374` 為範本（`常規四（九）四`）。
**取號現查**（三軸｜`blob@f20047f…`｜母體 ＝ 追蹤 `.md` **`741`** 檔｜檔框 ＋ 列框·**二形並取**）：
受詢 `375` B 形 `2`／`2`・C 形 `1`／`4`・鬆框（容反引號）`2`／`7` ⇒ **聯集 `7` 列／`2` 檔**；
**逐筆歸類**（`裁 H`）：`6` 列為**本單自身之授權條款**，餘 `1` 列為 `W-G.9-270R5e…md` 之「**次號**」一列
（依補款 `②`：**計算式⛔ 構成占用**）⇒ **⛔ 一列為定義列式** ⇒ **⛔ 撞號**。
**對照組**：甲 `374` C 形 `5`／`6` ✅／甲2 `373` C 形 `4`／`11` ✅／乙 人造哨兵三形皆 `0` ✅ ⇒ **器非紅**。
🔴 **`GB-158` 之更正獲坐實**：**B 形對二已知占用之對照組皆得 `0`** ⇒ 該形⛔ 單獨採信。

---

## `§七`　本批之自解清單 ＋ 自捕表

### 七-1　本批之自解清單（`作業常規之追加五` `§一` 五項齊備）

| # | 疑義逐字 | 二讀法 | 所取者 | 機械證據 | 所棄者之由 |
|---|---|---|---|---|---|
| `1` | 單 `§二` 序：「工項 `1` **背景啟動**後，**同時**辦 `2`〜`6`」 vs `CLAUDE.md` 逐字「`run_verification` **會寫 CSV** ⇒ 兩支長跑**不可併跑**」 | (a) 依單同時跑 `2`〜`4` 之管線探針；(b) **序列化**——長跑期間只辦純 `git`／`grep` 之工項（`5`／`6` 之現查），俟其畢再跑管線探針 | **(b)** | 當場實測：`run_verification.py` 以 `_dump_csv` **寫** `verify/out/got_*.csv`（`:602`–`:604`／`:678`–`:680` 等），而 `二-2` 之底本 `probe_WG9270R4_p6_interleave.py:104-108` **讀** `verify/out/got_抵費地_退縮*.csv` ⇒ **讀寫同一組檔** | (a) 之風險 ＝ 探針讀到半寫之 CSV；且**⛔ 會報錯**（坑之典型） |

🔒 **本項係<u>白名單外之新類</u>**（單 vs 倉規之衝突·⛔ 單內自相矛盾）——**具名如上**。
其五項自檢：**零土地後果** ✅（僅排程）／**零生產碼** ✅／**可機驗** ✅（讀寫面已實測）／
**可逆** ✅（僅次序）／**出艙具名** ✅（本表）。
🔒 **⛔ 落入黑名單任一項。**

### 七-2　🩸 本批之自捕（四則·皆 loud 或當場顯形·⛔ 出艙錯值）

| # | 形 | 徵候 | 攔法（本批已施行） |
|---|---|---|---|
| `1` | **判別力對照與受詞同值** ⇒ 該檢恆綠 | 以框 `to_csv` 判「探針是否寫檔」，而**對照組 `run_verification` 亦得 `0`** | 對照組全零 ⇒ **先判量測器紅**（`常規五`）；改框為「落檔動詞之正面列舉」後得**真相反之事實** |
| `2` | **所命名之「最寬框」實非最寬** | `B 形` 得 `2` 檔／`2` 列而「鬆框」僅 `1`／`5` ⇒ **`B 形 ⊄ 鬆框`** | 鬆框之分隔類須**含反引號**；並以「**各單形之命中須為聯集之子集**」為當場自檢 |
| `3` | **坑 `az` 之再現**：診斷標籤與所取之運算式不符 | 標籤寫「嚴格**後綴**(`new.endswith(old)`)」而實取 `new.startswith(old)`；**⛔ 影響結果**（所算者正是 append-only 之判準），**惟使診斷不可信** | 標籤與運算式**自同一處取**；本批已正名重量 |
| `4` | **`python -c "…"` 內之反引號遭 bash 命令替換** | `rc ≠ 0` ＋ `command not found` ＋ `%d format` 之 `TypeError`（**loud**） | 改以 `Write` 落腳本（本機坑 `2` 之攔法） |

🛑 **坑：本批⛔ 預先授權** ⇒ 上四則**⛔ 鑄為坑**；其**形／徵候／攔法**已逐則出艙如上，**候發單側准**。
（若准，標籤 ＝ **`ba`**，且須**同批擴** `verify/probes/probe_WG9269_pit_index.py` 之 `ALPHA_LABELS` 至 `ba`。）

---

## `§八`　收工閘（`§三`·**commit 前·與被守護之動作分開呼叫**）

### 八-1　閘 `1`：`numstat` 刪除欄（逐檔具名）

**刪除欄逐檔皆 `0`**（**非零者 ＝ `0` 檔** ⇒ 無須具名其由）：

| 檔 | `+` | `−` |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `71` | **`0`** |
| `docs/reports/W-G.9-271R_s框之同源性與GB153前提之實測.md` | `343`（🩸 **本數為自指**·見下） | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `46` | **`0`** |
| `verify/out/WG9271_fullrun.log` | `1029` | **`0`** |
| `verify/out/WG9271_gb153.log` | `71` | **`0`** |
| `verify/out/WG9271_pit_index.log` | `107` | **`0`** |
| `verify/out/WG9271_preflight.log` | `14` | **`0`** |
| `verify/out/WG9271_r6_start.log` | `66` | **`0`** |
| `verify/out/WG9271_registry.log` | `5` | **`0`** |
| `verify/out/WG9271_s_frame.log` | `351` | **`0`** |
| `verify/out/WG9271_stepD_denom.log` | `31` | **`0`** |
| `verify/probes/probe_WG9271_r6_start.py` | `144` | **`0`** |
| `verify/probes/probe_WG9271_s_frame.py` | `289` | **`0`** |
| `verify/probes/probe_WG9271_stepD_denom.py` | `139` | **`0`** |

🩸 **自指之量⛔ 填數**：本報告自身之 `+` 欄係**自指**（其受詞含承載它的這份文件）
⇒ 上列 `343` 係**本節落地前**之值，落地後必增；⛔ 追改（`常規四（九）五`）。
🔒 **`verify/probes/probe_WG9271_s_frame.py` 於本批內曾再修一次**（加錄 `block_poly` 供工項 `3` 用）
——依 `常規一` 補款③ `(1)`：**同批內新增之檔於同批內再修改，⛔ 使本批喪失 `②` 之資格**；
其於 `commit` 之 `numstat` 為 `289`／`0`（**純新增檔**·基座無此檔）。

### 八-2　閘 `2`：生產碼判法（四檔）

```
git diff --cached --name-only | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

⇒ **輸出為空（命中 `0` 行）** ⇒ **零生產碼 commit** ⇒ **逕行 `push`**（`常規一` 補款 `🔧 一`）。
🔒 `verify/probes/**` 係 **第二層**，⛔ 在生產碼 `34` 檔（`app.py` ＋ `verify/*.py` **單層** `33`）之內。

### 八-3　閘 `3`：`CR` ＝ `0` ＋ 嚴格前綴

| 檔 | `CR` | `LF` | bytes |
|---|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` | **`0`** | `7149` | `841339` |
| `W-G.9-271R_…md` | **`0`** | `343` | `21671` |
| `W-G.9波_claude.ai側自誤登記.md` | **`0`** | `9005` | `765791` |
| `probe_WG9271_r6_start.py` | **`0`** | `144` | `7412` |
| `probe_WG9271_s_frame.py` | **`0`** | `289` | `13518` |
| `probe_WG9271_stepD_denom.py` | **`0`** | `139` | `6184` |

（`.log` 含 `CR` 係**既有慣例**·⛔ 判紅。）

**既有檔之嚴格前綴**（基座 ＝ `f20047f`·受檢之既有檔數 ＝ **`2`**）：

| 檔 | 基座 | 現 | `new.startswith(old)` |
|---|---|---|---|
| `W-G.4_泛用阻塞項登記表.md` | `836234` B | `841339` B | **`True`** |
| `W-G.9波_claude.ai側自誤登記.md` | `761744` B | `765791` B | **`True`** |

**判別力二造**：必真 `b'abcd'.startswith(b'abc')` ＝ `True` ✅／必偽 `b'abXd'.startswith(b'abc')` ＝ `False` ✅。

### 八-4　閘 `4`：四簿期末（逐簿具名其框·**期值⛔ 為現值**）

**器** ＝ `verify/probes/probe_WG9267_issuer_measurers.py registry`（`rc = 0`·**stderr `0` B**）｜
落檔 `verify/out/WG9271_registry.log`（**期初態**）。**期末**之實測：

| 簿 | 框之出處 | 相異（期／實） | MAX（期／實） | 缺號集 | 判 |
|---|---|---|---|---|---|
| **自誤** | `VR-091` 補款二 `①` ＋ 範圍框·母體 ＝ 登記表**單檔** | `365` ／ **`365`** | `375` ／ **`375`** | `[106, 355, 356, 357]` | 🟢 |
| **`GB`** | `VR-093` 三框聯集（扣哨兵 `997`）·母體 ＝ `W-G.4` 登記表單檔 | `164` ／ **`164`** | `166` ／ **`166`** | `[12, 87]` | 🟢 **⛔ 動** |
| **`VR`** | `VR-092` 就地加註所引（扣 `999`）·母體 ＝ `docs/驗證裁定登記表.md` 單檔 | `80` ／ **`80`** | `95` ／ **`95`** | `[73, 75]` | 🟢 **⛔ 動** |
| **`K-9`** | `W-G.9-266` 工項四 `四-1`·母體 ＝ `K-6` 單檔 | `28` ／ **`28`** | `29` ／ **`29`** | `[]` | 🟢 **⛔ 動** |

🔒 **`常規四（九）二` 閘 `②`（MAX 之推進）**：自誤 MAX **由 `374` 轉為 `375`**，且 `375` ⛔ 在缺號集 ✅。
🔒 **`常規四（九）二` 閘 `①`（形之自證）**：以自誤之**定義框**掃 payload ⇒ 命中新號 `375` ✅
（本報告 `§六` 之取號表已載其三形之數）。
🔒 **`GB-132` 之避讓獲驗**：`§五` 之節落於 `W-G.4` 登記表，而 `GB` 之相異／`MAX` **立前立後逐位相同**
（`164`／`166`）⇒ 本節⛔ 觸 `GB-132`；`K-9` 亦同（`28`／`29`）——縱使該節題含 `K-9-29` 字樣，
其母體係 `K-6` 單檔 ⇒ **結構上不受本次追加影響**。

### 八-5　閘 `6`：自限復驗

| 受詞 | 實測 | 判 |
|---|---|---|
| `app.py` blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ 期初 ＝ 期末 |
| 生產碼 `34` 檔 | `5a14dc2`↔`c168e99` 相異 `0`；本批 `numstat` 之四檔命中 `0` 行 | ✅ ⛔ 動一字 |
| 五個 `*_EXPECT` | `verify/wd4_tier_list.py` **⛔ 在本批 staged 之中** | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | `merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | `None` | ✅ 未設 |
| `WV_K6_STEP0` | `None`（工項 `4` 後**已還原**） | ✅ 未設 |
| `K-9-29` 之任何子項 | 子項 `1` 之逐字錨＠`app.py` 仍 **`1`** ⇒ **⛔ 落地** | ✅ |
| `GB-164`／`GB-166` | **⛔ 落地**（`GB` 簿之相異／`MAX` 逐位未動即其證） | ✅ |

### 八-6　閘 `5`：坑之定義處清單自證（`通則 I`）

🔒 **`通則 I`**：本自證之受詞（**追蹤 `.md` 之全集**）**含承載其結論之本報告** ⇒
其「全數可解析」之結論 **⛔ 得於本報告入倉前作成** ⇒ **於報告 commit <u>之後</u>量**，
其 rev 與實測值**逐字載於本批之末一 commit 訊息**。
🔒 **期初態之現查**（rev ＝ `c168e99`·本批開工時）＝ **`78`／`78`**
（造甲 坑 `n` 定義處 `6` ≥ 1 ✅／造乙 人造之坑 `0`／`0` ✅ ⇒ 器非紅）·落檔 `verify/out/WG9271_pit_index.log`。
🛑 **坑：本批⛔ 預先授權** ⇒ `§七-2` 之四則自捕**⛔ 鑄為坑**，坑之期末數**應仍為 `78`／`78`**
（**期值⛔ 為現值**·實測見末一 commit）。


---

## `§九`　🛑 停

**辦畢 ⇒ 出報告 ⇒ 停**（單 `§五` 末·⛔ 自續下一批）。
本報告送回發單側；發單側自倉、自跑復驗其主檢後，方核可或指其誤。
