# `W-G.9-265R`　自解款與開工閘三級之立 ＋ 五則未決之裁 ＋ 覆蓋帳與逐款對位（**中級批**·執行報告）

> 🛑 **零生產碼**：`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`
> **一字未動（含註解）**；`verify/` 之唯一異動為**三支新增之唯讀探針**。
> 🛑 **零土地後果**：本批⛔ 動任何一宗地之 `G`／面積／街角歸屬／抵費地／配地幾何。
> 🛑 **⛔ 含修法主張**；**⛔ 就 `GB-67` 是否開修作成任何決定**。

**本批之級 ＝ 中級**（純登記 ＋ 純診斷·唯讀探針讀落檔）——依單 `§二` 所立之開工閘三級。
`verify/run_verification.py` 之**受詞層落檔**係**全批一次**所產（工項四〜七共用）；
另依 `V-l` 之受詞（「**前後**不變」）於全部落地**後**再跑一次——**其非「逐工項各跑」**，
理由見「本批之自解清單」第 `2` 項。

---

## §零　三軸與來源層之總宣告（`VR-091` 一·⛔ 逐處重述）

| 軸 | 值 |
|---|---|
| **`(1)` 來源 commit** | **`blob@8b812983577261246cec1a3d17db3a0fbae33cd4`**（開工態·＝ `origin/wip/s1-endpart`）|
| **`(2)` 母體** | 逐節載明（三簿為**單檔**；號占用為**全 `docs/`**；覆蓋帳為 **harness 落檔**）|
| **`(3)` 粒度框** | 逐節載明（**列框**／**檔框**／**bytes**／**長度（公尺）**）|

**來源層之別（單 §十二 c）**：本報告凡引一數，皆標其層——
**倉 blob**／**工作副本**／**harness 落檔**／**harness log**／**本探針之計算**。
🔒 **行號衛生**：本報告**⛔ 以行號（含 log 列號）為錨**，一律用**內容錨**或 **hunk 範圍**。

---

## §一　開工閘（六條逐條當場復現·⛔ 採信開工令與單所載之任一值）

### 閘 `1`　`ls-remote` ＝ 本地 `HEAD`

```
git ls-remote origin refs/heads/wip/s1-endpart
  ⇒ 8b812983577261246cec1a3d17db3a0fbae33cd4	refs/heads/wip/s1-endpart
git rev-parse HEAD
  ⇒ 8b812983577261246cec1a3d17db3a0fbae33cd4
```
⇒ **逐位相同** ✅（來源層 ＝ **倉**）

### 閘 `2`　生產碼二檔之 blob `sha256[:16]`（**當場重算**）

| 檔 | blob 位元組 | `sha256[:16]` |
|---|---|---|
| `app.py` | `1454937` B | **`1e15d45ae84b02ca`** |
| `verify/stepg_pipeline.py` | `109245` B | **`cb0bbaaa0f1eb8bc`** |

⇒ 與開工時（`W-G.9-261` 併線後之值）**逐位相同** ✅（來源層 ＝ **倉 blob**）
🔒 **收工時之復算**見 `§十 V-j`——二值**一字未變**。

### 閘 `3`　三簿 ＋ `K-9`（框逐字 ＋ 母體 ＋ 三軸）

🔒 **三軸 `(1)`** ＝ `blob@8b81298…`；**`(3)`** ＝ **列框**（逐列 `re.match`）。
🛑 **缺號集一律取 `[MIN..MAX]`**（單 `§三 三-1` 之裁 ⇒ 已鑄 `VR-092`）。
🛑 **⛔ 以恆等式 `True` 為框正確之證據**（`自誤 325`）——本節⛔ 引用該式為憑。

| 簿 | 母體（單檔·三軸 `(2)`） | 框（逐字） | 相異 | `MAX` | `MIN` | 缺號集 |
|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`／`661325` B／`7770` 列 | 定義框 `` ^#+[^0-9０-９\n]*?自誤[\s　`]*`?([0-9]{1,4})`? `` ∪ 範圍框 `` ^#+[^\n]*?自誤[\s　`]*`?([0-9]{1,4})`?[\s　`]*[〜～\-–~]+[\s　`]*`?([0-9]{1,4})`? ``（**展開**）| **`319`** | **`326`** | **`7`** | **`[106]`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md`／`707399` B／`5421` 列 | **`GB-116` 三框聯集**（式表 `` ^\| *\*\*`?GB-N`?\*\* `` ∪ 式A `` ^## `GB-N` `` ∪ 式B `` ^### `GB-N` ``·扣哨兵 `997`）| **`153`** | **`155`** | **`1`** | **`[12, 87]`** |
| `VR` | `docs/驗證裁定登記表.md`／`364971` B／`4696` 列 | `` ^#+ `?VR-N`? ``（扣哨兵 `999`）| **`76`** | **`91`** | `14` | — |
| `K-9` | `docs/rulings/K-6_街角地分配程序與可分配判準.md`／`295061` B／`4057` 列 | `` ^#{2,4}[^\n]*?K-9-([0-9]{1,3}) `` | `28` | **`29`** | — | — |

🔒 **計數式（單 §十二 b）**：自誤 ＝ `|定義框之號集 ∪ 範圍框展開之號集|`（定義框命中 `324` 列／範圍框命中 `8` 列·展開 `46` 號）；
`GB` ＝ `|式表 ∪ 式A ∪ 式B| − |{997}|`（式表 `131` 列／`131` 相異·式A `3`／`3`·式B `69` 列／`66` 相異）。
🔒 **第二態之同式結果**見 `§十 V-c`（落地後重算）。

⇒ **四行與單 `§一 3-a` 表逐位相符** ✅

### 閘 `4`　號占用（**宣告框六數分列** ＋ 二對照）

🔒 **三軸**：`(1)` `blob@8b81298…`／`(2)` **全 `docs/` `620` 檔**（**讀不到 `19`** ＝ `jpg 9` ＋ `png 10`·可讀 `601`）／
`(3)` **檔框 ＋ 列框**。
🔒 **器自檢**：含字面反斜線之路徑 ＝ **`0`**（須 `0`）✅／含 CJK 之路徑 ＝ **`599`**（須 `> 0`）✅
——即 `git -c core.quotepath=false` ＋ POSIX `rsplit("/")` 之防呆（`GB` 之 CJK 檔名陷阱）。

| 號 | `D1` 檔數 | `D2` 列數 | `D3` 列數 | 雙屬列數 | 列框 `\|D2 ∪ D3\|` | 檔框 | 角色 |
|---|---|---|---|---|---|---|---|
| **`W-G.9-265`** | `2` | `2` | `1` | `0` | **`3`** | **`2`** | 受詢 ⇒ 🔴 **非零** |
| `W-G.9-264` | `3` | `6` | `1` | `0` | `7` | `5` | 對照甲（須 `≥ 1`）✅ |
| 一人造號（字樣**⛔ 入倉**·`GB-147`·代稱·執行期組出）| `0` | `0` | `0` | `0` | `0` | `0` | 對照乙（須 `= 0`）✅ |

🔒 對照甲之六數與單 `§一 4-a` 所載（`D1 3`／`D2 6`／`D3 1`／雙屬 `0`／列框 `7`／檔框 `5`）**逐位相符**。

🩸 **`W-G.9-265` 之非零 ⇒ 依單 `§一` 自指款改判「占用者是否<u>全為</u>本單自身之產物」**：
占用者 ＝ **`2` 檔**，逐檔具名——
> `docs/orders/W-G.9-265_自解款與開工閘三級_五則未決之裁_覆蓋帳與逐款對位.md`〔`D1+D2+D3`〕＝ **本單自身**
> `docs/orders/W-G.9-265_附件甲_審查窗草稿.md`〔`D1+D2`〕＝ **本單 `§零 b` 所令入倉之附件甲**

⇒ **二者皆本單自身之產物（工項零之落檔）** ⇒ 🔒 **過閘**（單 `§二 一-1` 白名單 `1`）。**⛔ 改號。**
記入「本批之自解清單」第 `1` 項。

### 閘 `5`　本單原封入倉，入倉 blob `sha256` ＝ 落檔前 `sha256`

🩸 **本窗之受詞已改**（開工令 三 逐字）：工項零已由審查窗辦畢（`8b81298`），
本窗之受詞為「**驗其入倉 blob 與開工令 二 之三數逐位相符**」，**⛔ 重寫該檔**。

**當場復算**（來源層 ＝ **倉 blob**·`git cat-file blob HEAD:<path>`）：

| 量 | 實算 | 開工令 二 所載 | 判 |
|---|---|---|---|
| blob 位元組 | **`29887`** B | `29887` B | ✅ |
| `LF` | **`314`** | `314` | ✅ |
| `CR` | **`0`** | `0` | ✅ |
| `sha256` | **`67025cff74e3722aad63cf6ee7747906737a8a9f8b8b5660ee553ba19a078268`** | 同 | ✅ 逐位 |

⇒ **三者全符** ✅。`§一 6-a` 之自指款**未觸發**（`P-5` 未不符）。

### 閘 `6`　`probe_order_preflight.py <本單>` 之 `P-3`／`P-5`

```
rc = 0 ／ stderr = 0 B
🔴 機械·停機款：0 項
🟡 提示·須逐項處置：0 項
ℹ️ 記錄：2 項
  [P-5] SELF_SHA256 ✅ 相符（取檔內**最末**一個·受詞 29689 B）
        實算 e29d846e6aa96029… ／ 單載 e29d846e6aa96029…
  [P-3] `app.py` 之 `def main` 區間（AST 實查）＝ 14887-24459
```
⇒ `P-3`／`P-5` **皆非紅** ✅（來源層 ＝ **harness log**）

### 🔑 中級之減免

`verify/run_verification.py` 於**受詞層跑一次**（開工態·**全批共用**），⛔ 逐工項各跑。
其 `rc` ／ 逐字結論列 ／ `stderr` 位元組 見 `§十 V-l`。

🛑 **閘之量測與其所守護之動作為<u>分開之呼叫</u>**（`W-G.9-199` 常規補款 `一`）——
本節六閘之量測**先出艙、經判讀**，`commit`／`push` 於其後另行呼叫。

---

## §二　工項一：作業常規之追加五（自解款 ＋ 開工閘三級）

**落點** ＝ `CLAUDE.md` **檔末追加**（append-only）。**⛔ 鑄號**（其為常規之追加·`戒 36`）。

🔒 **產生方式（`常規七`·⛔ 憑記憶重寫）**：payload 由**檔案管線**自本單 **倉 blob** 抽取——
以**內容錨**定起迄（起錨命中 `1`／迄錨命中 `1`·⛔ 行號），段內 `50` 列，
**非引文列 `0`**（自檢），逐列去 `"> "` 前綴。

| 量 | 值 |
|---|---|
| payload 列數 | `50` |
| payload 位元組（**去前綴後**）| **`4008`** B |
| payload `sha256` | `5fe0332c2c4c5710ae24fc027bad404b9e7aced2e39ed192819e0ece73d75443` |

**`CLAUDE.md` 之前後（來源層 ＝ 工作副本 ⇒ 收工後即倉 blob）**

| | 位元組 | `LF` | `CR` | `sha256[:16]` |
|---|---|---|---|---|
| 前 | `196541` | `2399` | `0` | `7c25e981e5004fbe` |
| 後 | `203544` | `2479` | `0` | `f05d07328e227fd5` |

🔒 **嚴格前綴** `new.startswith(old)` ＝ **`True`**；增 `7003` B（含工項三 `b` 之靜態清單）。

**驗收（`V-b`）**見 `§十`。

---

## §三　工項二：五則未決之裁（發單側裁·皆零土地後果）

🔒 **鑄號之授權**：本單授權鑄 **`VR-092`／`VR-093`／`VR-094`** 三號，**⛔ 逾此**。
本批**實鑄三號**，⛔ 多⛔ 少；`K-9`／`GB`／戒／常規 **⛔ 鑄**。

| 裁 | 內容 | 落地 |
|---|---|---|
| **三-1** | 缺號集之區間慣例 ⇒ **`[MIN..MAX]`** | `docs/驗證裁定登記表.md` 檔末 ⇒ **`VR-092`** |
| **三-2** | `V-1′` 之落點 ⇒ **落 `docs/驗證裁定登記表.md`** | ⇒ **`VR-094`**（內容見 `§九`）|
| **三-3** | `自誤 1`–`6` ⇒ **已鑄·另立靜態清單** | `CLAUDE.md` 檔末（**⛔ 鑄號**）|
| **三-4** | `GB` 之定義列式 ⇒ **`GB-116` 三框聯集** | ⇒ **`VR-093`** |
| **三-5** | `R3` 之射程 ⇒ **甲（後於幾何批）** | **⛔ 鑄號**·具名於工項六之射程表（`§七`）|

🔒 **`VR-092` 內已依單具名**：「`GB-125` 之式於 `[MIN..MAX]` 慣例下**不適用**，**⛔ 據以判器紅**」——
其於本裁慣例下實測 `326 − 1 = 325 ≠ 319` ⇒ **不成立係框之別、⛔ 器之紅**。**`GB-125` 原文一字不刪。**
🔒 **`VR-093` 內已依單具名**：本裁係 **`GB-138` 處置③之落地**；**⛔ 動 `GB-138` 一字**。

**驗收（`V-c`）**見 `§十`。

---

## §四　工項三：`自誤 327` 之鑄 ＋ `自誤 1`–`6` 靜態清單

**a**　`自誤 327` ⇒ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**。
payload 由**檔案管線**自本單 blob 抽取（起錨／迄錨各命中 `1`·段內 `13` 列·**非引文列 `0`**），
去前綴後 **`2191` B**／`sha256 e452ef4707fd3cc8e75854c4da1d992ef36ff834caac49c8ad3216f420e415a8`。

🔒 **標題形（`常規四（九）四`：取既有最末一則為範本·⛔ 自創形）**：
既有最末一則之形 ＝ `` ### 🩸 `自誤 326`　**…** ``；本單所給之 `327` 形 ＝ `` ### 🩸 `自誤 327`　**…** ``
⇒ **同形**，**本批⛔ 偏離**（⚠️ 與 `W-G.9-264` 之 `GB-155` 不同——該次單所給之形不可被定義框命中）。

**登記表之前後**：`661325` B／`7769` LF ⇒ `663518` B／`7783` LF（增 `2193` B）；**嚴格前綴 `True`**。

**b**　`自誤 1`–`6` 靜態清單 ⇒ `CLAUDE.md` 檔末（與工項一同一次追加）。**⛔ 鑄號。**

🛑 **`GB-132` 之避讓（單 §三 三-3 明令·**須出艙立後之重算**）**——本節各列**⛔ 以 `#` 起首**
（一律為表列式 `| \`N\` | … |`），且節題內之「自誤」後**⛔ 緊接分隔符再接數字**
（節題逐字 ＝ `## 🔧 自誤簿首六則之靜態清單：**已鑄而定義框結構上撈不到**（…）`）。

**機械驗（母體 ＝ `CLAUDE.md`·框 ＝ 定義框 ∪ 範圍框·列框）**

| | 命中號集 |
|---|---|
| 立**前**（倉 blob） | `[227, 245, 267, 268, 282]` |
| 立**後**（工作副本） | `[227, 245, 267, 268, 282]` |
| **差集（後 ∖ 前）** | **`[]`** ✅ |

⇒ **本節 payload 未使任何號進入該框** ⇒ **`GB-132` 未觸發**。
🔒 併：**自誤之取號母體係登記表<u>單檔</u>**（`W-G.9-192-PRE-b 🔧 一`）⇒ 本清單於取號側**結構上無影響**；
上表係**額外**之寬母體佐證（⛔ 取代該款）。

**驗收（`V-d`）**見 `§十`。

---

## §五　工項四：覆蓋帳

**器** ＝ `verify/probes/probe_WG9265_coverage_account.py`（**新增·唯讀**·`py_compile` 過）。

| 欄 | 值 |
|---|---|
| `rc` | **`0`** |
| 逐字結論列 | `違反 2 組／無違反 11 組／合計 13 組` |
| `stderr` 位元組 | **`0`** B |

**全 `13` 組之逐組值、逐宗對之宗序與相鄰性、二人造對照、二支自我驗證閘**
——**全文已落倉**於 `docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末之
「🔧 `GB-67` 補款之再一次末端追加：射程表」節 `四`（⛔ 於此重述其表）。

🔑 **本節只列其**判**與**外部錨之對拍**：

- **違反 `2` 組**（`0m·R2·left` `Σ−∪ ＝ 45.9766`／`3.5m·R5·left` `56.8186`）／**無違反 `11` 組**。
- 逐宗對：`0m·R2·left` ＝ 第 `0` 宗 `628-41(1)`〔街角地〕× 第 `2` 宗 `628-27(1)` 疊 `19.8142`
  ＋ × 第 `3` 宗 `628-40(1)+` 疊 `26.1624`（**`19.8142 + 26.1624 = 45.9766`**·**皆非相鄰**）；
  `3.5m·R5·left` ＝ 第 `0` 宗 `628-45(1)+`〔街角地〕× 第 `2` 宗 `628-18(2)` 疊 `56.8186`（**非相鄰**）。
- **人造對照**：相疊矩形 ⇒ `1.0000000000` ✅／相離矩形 ⇒ `0.0000000000` ✅／`11` 組必綠 ✅。

🩸 **與單 `§五 (d)` 之外部錨之對拍（**事後**·⛔ 以其為調框之停止條件）**：
`11` 組 `0.0000`／`45.9766`（`19.8142 + 26.1624`）／`56.8186` ⇒ **逐位相符**。

🔒 **落檔⛔ 覆寫任何 tracked 檔**：實跑後 `git status --porcelain` 之 tracked 修改 ＝ **`4` 檔**，
**逐檔具名皆為本批工項一〜三之 append**（`CLAUDE.md`／自誤登記／`VR` 登記／`GB` 登記），
**⛔ 有一檔係 `run_verification.py` 所寫**——`verify/out/got_*` 係
`.gitignore:30` 之 `verify/out/got_*` 所**刻意忽略**之族（`git check-ignore -v` 實查）⇒ **本款成立**。

**驗收（`V-e`）**見 `§十`。

---

## §六　工項五：`area(cut_coords)` 對 `G` 欄之逐宗對帳

**器** ＝ 同上（同一支探針·同一次跑）。**全文已落倉**於同節 `五`。

🛑 **自我驗證閘（⛔ 省）**：`街廓面積(㎡)` 欄**逐街廓為常數** ⇒ 二情境共 `7` 街廓、
**相異值皆 `1`** ✅（`R1 2893.55`／`R2 4368.6`／`R4 3207.87`／`R5 4150.21`／`R6 3976.3`）。
**不過即⛔ 據此表下任何結論**（單 §十一 2）——**本批過**。

🩸 **二母體分列（⛔ 併為一數）**——單 `§六 (a)` 之受詞逐字**含 `推進側別 ＝ 抵費地` 之列**，
而 `(b)` 之判準為 `|area − G| ≤ 0.02`；**該母體之 `G(㎡)` 欄逐列 ＝ `0.0`**
（`解法` 欄逐字 ＝ `幾何剩餘`）⇒ 該判準於其**結構上不可能成立**。
⇒ 依 `§二 一-1` **白名單 `6`**（單內二處自相矛盾·二讀法皆不動土地 ⇒ **取較嚴者**），
**二母體分列出艙**、抵費地之對應量取 `幾何面積(㎡)`。記入自解清單第 `3` 項。

| 情境 | 母體 | 宗／列數 | `Δ` 範圍（6dp） | 逾容差 |
|---|---|---|---|---|
| `0m` | 甲 ＝ 分配宗·對 `G(㎡)` | `19` | `[−0.011957, +0.005983]` | `0` ✅ |
| `0m` | 乙 ＝ 抵費地·對 `幾何面積(㎡)` | `2` | `[+0.003745, +0.004575]` | `0` ✅ |
| `3.5m` | 甲 ＝ 分配宗·對 `G(㎡)` | `36` | `[−0.014064, +0.009266]` | `0` ✅ |
| `3.5m` | 乙 ＝ 抵費地·對 `幾何面積(㎡)` | `11` | `[−0.003694, +0.004680]` | `0` ✅ |

🔒 **容差之推導（單 §六 (b) 令載）**：`G` 欄為 `2dp` ⇒ 其捨入上界 ＝ `0.005 ㎡`；
判準取其**四倍裕度** ＝ `0.02 ㎡`。⇒ 實測最大 `|Δ| ＝ 0.014064` **落於裕度內**、
且**大於捨入上界 `0.005`** ⇒ 該裕度**⛔ 冗餘**（若取 `0.005` 則 `628-45(1)+` 將轉紅）。

🩸 **與單 `§六 (d)` 之外部錨之對拍（**事後**·⛔ 調框以就其數）**
單載審查窗實走之範圍為 `[−0.0141, +0.0092]`；CC 實測（全精度）＝ **`[−0.014064, +0.009266]`**。
> · **下界逐位相符**（`round(−0.014064, 4) = −0.0141`）；且其宗 ＝ `628-45(1)+`，與單載相符。
> · 🔴 **上界差 `0.0001`**：`round(+0.009266, 4) = +0.0093`，而單載 `+0.0092` ＝ **截斷**
>   （`int(0.009266 × 1e4) / 1e4 = 0.0092`）⇒ **成因 ＝ 4dp 之<u>捨入 vs 截斷</u>，⛔ 值之別**。
> · 🔒 CC **⛔ 調整量測框以就單載之數**；出艙**全精度值 `+0.009266`** ＋ 二種 `4dp` 呈現。
>   記入自解清單第 `6` 項（白名單 `8` 之同類：**呈現層之捨入慣例**）。
> · 單載另舉「二重疊源頭 `628-41(1) −0.0037`」——CC 實測 `0m` 之 `628-41(1)` 落於
>   `0m` 甲之範圍 `[−0.011957, +0.005983]` 內（**該宗於 `0m` 為第 `0` 宗**）。

🔒 **必紅對照**：將任一宗 `cut_coords` 自其質心外推使面積 `+1 ㎡` ⇒
`0m` 之 `628-37(1)` `Δ +0.0036 → +1.0036` ✅ 轉紅；`3.5m` 之 `628-36(1)` `Δ +0.0011 → +1.0011` ✅ 轉紅。

🔑 **本項之意義（單 §六 逐字）**：重疊**⛔ 因某宗多邊形被畫大**——全 `55` 分配宗
＋ `13` 抵費地列**無一逾容差** ⇒ **每宗面積皆等於其對應面積**；**重疊即同一塊地被計入兩宗**。

**驗收（`V-f`）**見 `§十`。

---

## §七　工項六：射程表

**落點** ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 之 `GB-67` 補款**再一次末端追加**
（**⛔ 動其原文一字**·**⛔ 鑄 `GB`**）。**全文已落倉**（節 `一`〜`三`）。

| 情境 | `所屬街廓` distinct（出現序·**harness 落檔**） | 中止街廓 | `R3` ∈ 可達？ |
|---|---|---|---|
| `0m` | `['R1', 'R2']` | `R2` | **否** |
| `3.5m` | `['R1', 'R2', 'R4', 'R6', 'R5']` | `R5` | **否** |

🔒 **中止閘逐字**（**落檔欄之原文複製**·⛔ 改寫·⛔ 濃縮）已全文落倉；其**上界之獨立驗算**：
`(14−1) × 0.005 × 44.47 = 2.89055` ⇒ 顯示 `2.8906` ✅；`(8−1) × 0.005 × 45.71 = 1.599850` ⇒ 顯示 `1.5999` ✅。
**判別力**：二式互換 ⇒ `2.97115` ／ `1.556450` ⇒ **皆不符** ⇒ 該驗算**⛔ 恆真**。

🔒 **`§三 三-5` 之裁已具名於該節 `三`**：`R3` 之驗收**後於**幾何批，**⛔ 另闢只算 `R3` 之臨時途徑**；
其機械依據 ＝ `3.5m` 之處理序 `R1 → R2 → R4 → R6 → R5`，`R3` 排於 `R5` **之後**，
而 `R5` 之中止即 `②-宗 圍堵閘破` ⇒ **不消除即結構上到不了 `R3`**。
🔒 **閘關態⛔ 得為原態之代理**（其 `cut_coords` 與原態相異·`W-G.9-264R` 已具名）。

**驗收（`V-g`）**見 `§十`。

---

## §八　工項七：已裁未實作之逐款對位表

**器** ＝ `verify/probes/probe_WG9265_clause_mapping.py`（**新增·唯讀**·`py_compile` 過）。

| 欄 | 值 |
|---|---|
| `rc` | **`0`** |
| 逐字結論列 | `款數 14｜無受詞 4｜含「未量」4｜含 🔴 之判 6` |
| `stderr` 位元組 | **`0`** B |

**`14` 款之全表**（受詞逐字／受詞內之宗〔宗地號 ＋ 宗序〕／現態之判／機械證據）
＋ **`6` 個未量子項之逐款具名與阻斷點**——**全文已落倉**於
`docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末之「🔧 已裁未實作之逐款對位表」節。

🔒 **`W × D` 之來源 ＝ 生產函式**（**⛔ 硬編 `3.5`／`14`**）：
`get_min_lot_size('住宅區', 10.0)` ⇒ `min_width 3.50`／`min_depth 14.00` ⇒ `W×D = 49.00 ㎡`。
**判別力**：`get_min_lot_size('住宅區', 5.0)` ⇒ `{'min_width': 3.0, 'min_depth': 12.0, …}` ⇒ **⛔ 為常數** ✅。

🔒 **否證對照**：必綠 ＝ `K-9-9 三` 判「無受詞」而**⛔ 觸紅** ✅；
必紅 ＝ 對一款人為填入「有受詞」而其受詞集為空 ⇒ **轉紅** ✅。

🩸 **與單 `§八 (d)` 之外部錨之對拍（事後）**：單載審查窗之數為「`7` 款有受詞／`2` 款無受詞／
`3` 款判『未量』／`3` 款判『違反』」；CC 得 `14` 款／無受詞 `4`／未量 `4`／🔴 `6`。
🔒 **相異之唯一成因 ＝ 款之切分粒度**——單 `§八 (a)` 之受詞為
「`K-9-9`（**七款**）／`K-9-12`／`K-9-14`／`K-9-15`／`K-9-17`／`K-9-19`」＝ **`12`** 個款組，
CC 另將 `K-9-15` 拆為 `二`／`三-4`、`K-9-17` 拆為 `一`／`二` ⇒ **`14`**。
**CC 之表涵蓋單所列之全部 `12` 款組**；🔒 **⛔ 就此調整切分以就其數**（照實回報）。

🛑 **未量之子項（逐款具名·⛔ 靜默略過）**：`d-1`（`K-9-9 一` ∥ALLOCLINE）／
`d-2`（`K-9-9 七`＋`K-9-10 二` 有效垂線母體）／`d-3`（`K-9-12` 之充分側）／
`d-4`（因果鏈之直證）／`d-5`（逐街廓正面路寬）／`d-6`（`K-9-19` 之適用）。
🔒 **`d-1`／`d-2`／`d-3` 之阻斷點皆為「自我驗證閘⛔ 可得」**——碼面對該三量**⛔ 有既有斷言**
可資作外部重建之驗證閘 ⇒ 依單 `§十一 3`，**該三項⛔ 得下結論**，具名為不可得。
🔒 **⛔ 以「未查」了事**（單 §十二 e）——三者之阻斷點已逐項具名於落倉之表。

**驗收（`V-h`）**見 `§十`。

---

## §九　工項八：`V-1′` 之立（落 `VR-094`）

**器** ＝ `verify/probes/probe_WG9265_V1prime.py`（**新增·唯讀**·`py_compile` 過）。

| 欄 | 值 |
|---|---|
| `rc` | **`0`** |
| 逐字結論列 | `🔑 **判別力**：陽性 (5) 出艙 2 宗、陰性 0 宗 ⇒ ✅ ⛔ 皆零亦⛔ 皆非零 ⇒ 閘**有判別力**` |
| `stderr` 位元組 | **`0`** B |

🔒 **二態**（來源層 ＝ **倉**·各於拋棄式 clone 內原態實跑一次 `run_verification.py`）：
態A ＝ `70ce7d46b27a1c9f5fd057f697ba3b7f382af7ad`（`app.py` blob `sha256[:16] = 4860f10828fb8ae9`）／
態B ＝ `e9d1b9c3e6e46580da28392e1ed7495d5b34638f`（`= 1e15d45ae84b02ca`）。

🛑 **本工項已<u>先實走</u>該閘（含二對照）方落地**（單 `§九 (d)` 明令）——
其閘於 `W-G.9-264R` 之 `V-d` 逐字記為「不適用·未跑」，**本批為其首次實走**。

**五款之判、二對照之逐宗值、三支自我驗證閘**——**全文已落倉**於 `VR-094`。本節只列其判：

| 款 | 陽性 `R2·left` | 陰性 `R2·right` |
|---|---|---|
| `(1a)` 落檔 `宗地寬度(m)` 欄（正典 `N-14` 之量）| ✅ 六宗逐字相同 | ✅ 六宗逐字相同 |
| `(1b)` `s` 區間長度（全精度）| 🔴 `Δ` ∈ `[−0.000069, +0.004250]` | 🔴 `Δ` ∈ `[−0.000287, +0.000053]` |
| `(2)` 鏈序 | ✅ | ✅ |
| `(3)` `δ` 逐宗同一常數 | 🔴 極差 `0.005683` | 🔴 極差 `0.000389` |
| `(4)` 守恆 | ✅ 殘差 `+0.0000`／`−0.0200` | ✅ 同（街廓層之量）|
| `(5)` 出艙 | 🔑 **`2` 宗** | ✅ **`0` 宗** |

🔑 **`(5)` 之逐宗出艙（陽性）**：
`628-41(1)` `ΔG ＝ +0.0600 ㎡`·`δ ＝ +0.106088212`；
`628-40(1)+` `ΔG ＝ +0.1900 ㎡`·`δ ＝ +0.107449423`。
🩸 **與單 `§九 (c)` 之外部錨之對拍（事後）**：單載須出艙 `628-40(1)+ +0.19㎡`／`628-41(1) +0.06㎡`
⇒ **宗與值逐位相符**；陰性須出艙 `0` 宗 ⇒ **相符**。
⇒ 🔒 **閘⛔ 失能**（單 §九 (c) 之停機條件**未觸發**）。

🛑 **精度款已遵行**：`(3)` **⛔ 以 `verify/out/*.csv` 之欄評估**；`s` 位置由**共用邊 × `FRONT_LINE`**
之交點求得，`d̂` 取自**生產解析器**（`run_verification.build_pipeline` → `cad['front_lines']`）
⇒ **⛔ 自寫第二份 DXF 解析**。

🔒 **三支自我驗證閘（⛔ 省）**：閘 `γ`（共用邊恰 `2` 頂點）`24/24` ✅；
閘 `α`（`b_i − b_{i−1}` vs 落檔 `S(m)`·`±0.01`）`20/20` ✅（最大 `|Δ| = 0.003818`）；
閘 `β`（`area` vs `G`·`±0.02`）`28/28` ✅。

🩸 **本器首版為紅、由閘 `α` 當場擋下（自捕）**——見自解清單第 `4` 項。

🔴 **`(1)`／`(3)` 之紅：照實回報，⛔ 自行放寬**
1. `(1)` 之款文含**二義**（「宗地寬度」係正典 `N-14` 之量；括號內「`s` 區間長度」係另一量）
   ⇒ 依白名單 `6` **二讀法並列出艙**（自解清單第 `5` 項）；`(1a)` 綠、`(1b)` 紅。
2. `(1b)`／`(3)` 於**陰性**對照**亦紅** ⇒ 該二款於 `1e-6` 之判準下**⛔ 區辨「有變」與「無變」**
   ——陰性側之 `G` 二態逐位相同，其 `δ` 極差仍達 `3.89e-4`。
   ⇒ 🔒 **本器所量之 `s` 位置有一 `~4e-4 m` 之底噪**（來源 ＝ `G` 於 tol 下之非確定性·`GB-101`）。
3. 🛑 **本批⛔ 變更 `(1)`／`(3)` 之判準一字**——判準之修訂繫發單側／KL。
   本報告**只照實登記實測與底噪**（單 §十二 d：**自擬之框⛔ 以待驗值為停止條件**）。
4. 🔒 **可據以續辦者 ＝ `(2)`／`(4)`／`(5)` 三款**：於二對照皆如預期，且 `(5)` **具判別力**。

🔒 **`V-1`（`W-G.9-261`）之「未達」判⛔ 回改一字**（KL `2026-09-09` 明令·⛔ 事後放寬）。

**驗收（`V-i`）**見 `§十`。

---

## §十　驗收（**性質閘**）

| 閘 | 判 | 證據 |
|---|---|---|
| `V-a` | ✅ | `§一` 六閘逐條復現並出艙；本批之級**自載為中級**；`run_verification.py` 之**受詞層跑一次**（工項四〜七共用），另為 `V-l` 之「前後」跑第二次（自解清單第 `2` 項）|
| `V-b` | ✅ | payload 去引文前綴後（`50` 列／`4008` B）**逐字落於 `CLAUDE.md`** ＝ `True`；**必紅** ＝ 人為刪第 `910` 個字元（`'單'`）後比對 ⇒ **轉紅** ✅；**必綠** ＝ 原文 ⇒ 綠 ✅ |
| `V-c` | ✅ | 落地後以本裁所釘之框重算（**同格載框逐字、母體、三軸**·見下）：自誤 `327`／`GB 155`／`VR 94`／`K-9 29`；缺號集 自誤 **`[106]`**／`GB` **`[12, 87]`**；**必紅** ＝ 以 `[1..MAX]` 重算，自誤缺號集得 `[1,2,3,4,5,6,106]` ⇒ **判為⛔ 採** ✅ |
| `V-d` | ✅ | `自誤 327` 被**定義框**命中 ＝ `True`；未鑄之 `328` 命中 **`0`**；靜態清單落地後**相異 ＝ `320`**（`= 319 + 327`）、**缺號集仍 `[106]`**；`GB-132` 差集（後∖前）＝ `[]` |
| `V-e` | ✅ | `13` 組全數出艙（**逐宗對具名宗序與相鄰性**）＋ 二人造對照 `1.0`／`0.0` ＋ `11` 組必綠 |
| `V-f` | ✅ | 自我驗證閘 PASS（`7` 街廓相異值皆 `1`）＋ 全宗 `\|Δ\| ≤ 0.02`（逾容差 `0`）＋ 注入 `+1㎡` 之必紅 ✅（二情境各一）|
| `V-g` | ✅ | 可達集合與落檔 distinct **逐位相同**（`['R1','R2']`／`['R1','R2','R4','R6','R5']`）＋ **人造 `R3` 之必紅**（見下）＋ 中止閘逐字已落倉 |
| `V-h` | ✅ | 逐款判表 `14` 格**每格皆有對應之量**；`K-9-9 三` 判「無受詞」而**不觸紅** ✅；**未量之款逐款具名**（`6` 子項 ＋ 表內 `4` 款）|
| `V-i` | ✅ | `V-1′` 閘 ＋ 陽性／陰性二對照**皆已跑**；`(5)` 之判別力如預期（`2` vs `0`）；`(1b)`／`(3)` 之紅**已具名**（⛔ 靜默略）|
| `V-j` | ✅ | 見下「零生產碼之逐 commit 出艙」|
| `V-k` | ✅ | `git diff --numstat <基座 8b81298> -- <四檔>` ⇒ **刪除欄逐列為 `0`**（`80/0` `CLAUDE.md`／`205/0` `GB` 登記／`14/0` 自誤登記／`169/0` `VR` 登記）·**⛔ 用 `--cached`** |
| `V-l` | ✅ | 見下 |
| `V-m` | ✅ | 「本批之自解清單」＝ `§十一`（**`6` 項·非空**），每項五款俱全 |

### `V-c` 之落地後重算（**同格載框逐字、母體、三軸**）

🔒 三軸 `(1)` ＝ **本批落地後之工作副本**（收工後即 `commit` 之倉 blob）／`(3)` ＝ **列框**。

| 簿 | 母體（單檔） | 框（逐字） | 相異 | `MAX` | `MIN` | 缺號集 `[MIN..MAX]` |
|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 定義框 ∪ 範圍框（同 `§一 閘 3`·逐字不再重述·**⛔ 二形之數混列**）| **`320`** | **`327`** | `7` | **`[106]`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `GB-116` 三框聯集（扣 `997`）| `153` | **`155`** | `1` | **`[12, 87]`** |
| `VR` | `docs/驗證裁定登記表.md` | `` ^#+ `?VR-N`? ``（扣 `999`）| **`79`** | **`94`** | `14` | — |
| `K-9` | `docs/rulings/K-6_…md` | `` ^#{2,4}[^\n]*?K-9-N `` | `28` | **`29`** | — | — |

🔒 **判別力**：未鑄之 `自誤 328` 命中 `0`／未鑄之 `GB-156` 命中 `0`／未鑄之 `VR-095` 命中 `0` ⇒ **三簿之器皆非恆真**。
🔒 **`GB` 之相異／`MAX`／缺號集於本批<u>三數皆未變</u>**（`153`／`155`／`[12,87]`）
⇒ 本批於 `GB` 簿**⛔ 鑄號**（工項六／七皆為補款與澄清·`戒 36`）。

### `V-g` 之必紅對照

於射程表之可達集合文字中**人為多列一個 `R3`** ⇒ `['R1','R2','R3']` vs 落檔 distinct `['R1','R2']`
⇒ **比對轉紅** ✅；原文（`['R1','R2']`）⇒ **綠** ✅。

### `V-j` 零生產碼（**逐 commit** 出艙該指令輸出·**含空輸出**）

```
git diff --name-only 8b812983577261246cec1a3d17db3a0fbae33cd4..<本 commit> \
  | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
  ⇒ （空輸出·命中 0 行）
```
🔒 **併驗（絕對值出艙）**：`app.py` blob `sha256[:16]` 期末 ＝ **`1e15d45ae84b02ca`**（＝ 期初）；
`verify/stepg_pipeline.py` ＝ **`cb0bbaaa0f1eb8bc`**（＝ 期初）⇒ **二檔一位元組未動**。
🔒 `verify/` 之**唯一異動** ＝ **三支新增之唯讀探針**（`probe_WG9265_coverage_account.py`／
`probe_WG9265_clause_mapping.py`／`probe_WG9265_V1prime.py`）——皆**新檔**，⛔ 生產路徑四檔之一。

### `V-l` `run_verification.py` 前後

| 量 | 前（開工態·全批共用之落檔來源） | 後（全部落地後） | 判 |
|---|---|---|---|
| `rc` | **`1`** | **`1`** | ✅ 相同 |
| `stderr` 位元組 | **`0`** B | **`0`** B | ✅ 相同 |
| `RESULT` 逐字 | `RESULT: FAIL` | `RESULT: FAIL` | ✅ 逐字相同 |
| 紅項**名目集合** | **`24`** 個 | **`24`** 個 | ✅ |
| 差集 前∖後 | — | — | **`[]`** ✅ |
| 差集 後∖前 | — | — | **`[]`** ✅ |

🔒 **`rc` 之機械依據（⛔ 憑觀察）**：`verify/run_verification.py` 之 `main()` 末句逐字
`return 0 if allok else 1`，其上一句即印 `RESULT:` ⇒ **`RESULT: FAIL` ⟺ `rc = 1`**（雙向）。
🔒 **判別力（⛔ 恆綠之自證）**：自後集合**人為移除一名目**後比對 ⇒ **轉紅** ✅。
🔒 **`RESULT: FAIL` ＝ 本分支之<u>准紅碼</u>既有終局**（`CLAUDE.md` 分支狀態宣告·`W-G.9-4` 起），
**⛔ 本批所致、⛔ 新紅**；`V-l` 之受詞為**前後不變**，⛔ 全綠。
🔒 **紅項 `24` 名目**已逐項出艙於本批之工具輸出（`§十二` 之落檔清單）。

---

## §十一　🔑 本批之自解清單（單 `V-m` 所令·**非空**·每項五款俱全）

### 自解 `1`　開工閘 `4` 之號占用非零

> **① 疑義逐字**：單 `§一` 表 `4` 令「`W-G.9-265` 六數分列**皆 `0`**」，而實測列框 `3`／檔框 `2`。
> **② 二讀法**：(甲) 依字面判**不過**、停機上呈；(乙) 依同節自指款逐字
> 「若本單（或附件甲）已入倉致 `265 ≠ 0`，**⛔ 改號**；改判『占用者是否全為本單自身之產物』，是則過閘」。
> **③ 所取**：**乙**。
> **④ 機械證據**：占用者恰 `2` 檔，皆 `docs/orders/W-G.9-265_*`，即工項零之落檔（本單自身 ＋ 其附件甲）
> ⇒ **全為本單自身之產物**；對照甲 `W-G.9-264` `≥ 1` ✅、對照乙人造號 `= 0` ✅（器非紅）。
> **⑤ 所棄之由**：甲之停機將使**同一張單永遠過不了自己的閘**（`W-G.9-237` 已具名之同族缺陷），
> 且該讀法與同節自指款**逐字相斥**。
> **類** ＝ 白名單 `1`。**零土地後果**／**零生產碼**／**可機驗**（二對照皆跑）／**可逆**（未落地任何物）／**已出艙**。

### 自解 `2`　`V-l` 之「前後」與中級「全批只跑一次」

> **① 疑義逐字**：單 `§一` 表末令「`run_verification.py` **全批只跑一次**……⛔ 逐工項各跑」，
> 而 `V-l` 令「`run_verification.py` **前後**：`rc`／`stderr`／`RESULT`／紅項名目集合**前後不變**」。
> **② 二讀法**：(甲) 只跑一次，`V-l` 之「前後」讀為「與前批之記載對拍」；
> (乙) 跑二次（開工態一次為工項四〜七之受詞層落檔；全部落地後一次為 `V-l` 之「後」），
> 「⛔ 逐工項各跑」之受詞係**逐工項**、⛔ 及於 `V-l` 之收工閘。
> **③ 所取**：**乙（較嚴）**。
> **④ 機械證據**：二跑之 `RESULT` 逐字相同、`stderr` 皆 `0` B、紅項名目集合**二向差集皆 `[]`**；
> 判別力對照（人為移除一名目）**轉紅** ⇒ 該對比⛔ 恆綠。
> **⑤ 所棄之由**：甲之「與前批記載對拍」係**憑記憶／憑轉引**，違 `數字錨引倉檔`；
> 且 `V-l` 之受詞明文為「**前後**」，須有二個當場之態方成立。
> **類** ＝ 白名單 `6`。五款俱全（⛔ 動土地／⛔ 動生產碼／二對照已跑／可逆／已出艙）。

### 自解 `3`　工項五之受詞含抵費地，而其 `G` 欄恆 `0`

> **① 疑義逐字**：單 `§六 (a)` 受詞逐字「二情境全部有 `cut_coords` 之宗（**含 `推進側別 ＝ 抵費地` 之列**）」，
> 而 `(b)` 判準逐字「`|area − G| ≤ 0.02 ㎡`」。
> **② 二讀法**：(甲) 依字面對抵費地列亦比 `G` ⇒ 該母體**全數紅**（`0m` `2` 列／`3.5m` `11` 列）；
> (乙) 二母體分列，抵費地之對應量取 `幾何面積(㎡)`。
> **③ 所取**：**乙**，且**二母體之數皆出艙**（⛔ 併為一數·⛔ 靜默捨棄甲）。
> **④ 機械證據**：抵費地列之 `G(㎡)` 欄**逐列 ＝ `0.0`**、`解法` 欄逐字 ＝ `幾何剩餘`、
> 其 `幾何面積(㎡)` 欄與本探針所算之 `area` 相符至 `[−0.003694, +0.004680]`（`13` 列全綠）
> ⇒ 判準於甲讀法下**結構上不可能成立**（＝ 恆假之閘·`自誤 251` 同族）。
> **⑤ 所棄之由**：甲讀法產生 `13` 個**與性質無關**之紅（該性質為「分配多邊形面積等於其**分配面積**」，
> 而抵費地**⛔ 分配宗**）⇒ 其紅係**框之誤**、⛔ 受詞之紅。
> **類** ＝ 白名單 `6`。五款俱全。

### 自解 `4`　`V-1′` 探針**首版為紅**（量測器紅·**自捕**）

> **① 疑義逐字**：無單載之疑義；係 CC 自身工具之缺陷。
> **② 二讀法**：(甲) 放寬閘 `α` 之容差使其過；(乙) 判**量測器紅**、修工具、重跑。
> **③ 所取**：**乙**。
> **④ 機械證據**：首版之 `s` 軸取「第 `0` 宗質心 → 末宗質心」之單位向量，
> 致投影區間長**一律較落檔 `S(m)` 欄大約 `+3.39 m`**（`28/28` 格全紅·二側二態）；
> 成因 ＝ 斜交街廓下宗地之**深度方向分量**投影入該軸。
> 改取**生產解析器之 `FRONT_LINE` 方向**、並改以**共用邊 × FRONTLINE 之交點**為界後，
> 三支閘（`γ` `24/24`／`α` `20/20`／`β` `28/28`）**全過**。
> **⑤ 所棄之由**：甲係**以待驗值為調框之停止條件**（`自誤 325` 之受詞·單 §十二 d 明禁）；
> 且首版之輸出「**逐宗單調遞增、數字漂亮**」——**其紅⛔ 可見於外觀**，唯自我驗證閘看得見。
> **類** ＝ 白名單 `5`。五款俱全；**該紅⛔ 出艙**（首版之任何數皆未進入本報告之結論）。

### 自解 `5`　`V-1′` 之 `(1)` 款文含二義

> **① 疑義逐字**：單 `§九 (b) (1)` ＝ 「宗地寬度（`s` 區間長度）逐宗不變」。
> **② 二讀法**：(甲) 「宗地寬度」＝ 正典 `N-14` 之量（`CLAUDE.md` 三量：**沿 FRONTLINE 量之兩側界最小距**）
> ⇒ 讀落檔 `宗地寬度(m)` 欄；(乙) 「`s` 區間長度」＝ 全精度重建之區間長。
> **③ 所取**：**二讀法並列出艙**（`(1a)` ／ `(1b)` 分列·⛔ 靜默擇一）。
> **④ 機械證據**：`(1a)` 二對照皆**六宗逐字相同** ✅；`(1b)` 陽性 `Δ` 至 `+0.004250`、陰性至 `−0.000287`
> ⇒ **二讀法之判相異**（甲綠乙紅）。
> **⑤ 所棄之由**：⛔ 靜默擇一——二量係**不同之幾何量**（`CLAUDE.md` 三量之戒「三量分清·勿混用」），
> 擇一即隱去另一之結果。
> **類** ＝ 白名單 `6`。五款俱全。

### 自解 `6`　`area − G` 上界之 `4dp` 呈現差 `0.0001`

> **① 疑義逐字**：單 `§六 (d)` 載「全宗 `area − G` 落於 `[−0.0141, +0.0092]`」，
> 而 CC 全精度實測上界 ＝ `+0.009266`。
> **② 二讀法**：(甲) 判為值之相異 ⇒ 停機上呈；(乙) 判為 `4dp` 之**捨入 vs 截斷**之別。
> **③ 所取**：**乙**，且**出艙二值**（全精度 `+0.009266` ＋ 二種 `4dp` 呈現）。
> **④ 機械證據**：`round(0.009266, 4) = 0.0093`；`int(0.009266 × 1e4) / 1e4 = 0.0092`
> ⇒ 單載之 `+0.0092` **恰為截斷值**。併：**下界 `round(−0.014064, 4) = −0.0141` 逐位相符**，
> 且其宗（`628-45(1)+`）與單載相符 ⇒ **二端非同時偏移** ⇒ ⛔ 係量測框之別。
> **⑤ 所棄之由**：甲將使一個**純呈現層**之差觸發停機；且該差**⛔ 逾任何判準**
> （判準為 `|Δ| ≤ 0.02`，二值皆遠在其內）。
> 🛑 **CC ⛔ 調整量測框以就單載之數**——本項係**事後對拍**之照實回報，
> 本探針之停止條件始終為其**自我驗證閘**（`街廓面積` 逐街廓常數）。
> **類** ＝ 白名單 `8`（呈現層之捨入慣例）。五款俱全。

🔒 **黑名單之自檢**：上 `6` 項**無一**落入 `§二 一-2` 黑名單任一項——
⛔ 動任何宗地之 `G`／面積／街角歸屬／抵費地／配地幾何；⛔ 動生產碼一字（含註解）；
二讀法之後果**在土地上皆相同**（本批零土地後果）；**⛔ 新訂任何準據**、
**⛔ 廢止或放寬任何既有之閘**；**⛔ 涉及 KL 已裁之款而二讀法相斥**。

---

## §十二　落檔清單與逐工具三欄

| 工具 | `rc` | 逐字結論列 | `stderr` (B) |
|---|---|---|---|
| `verify/probes/probe_order_preflight.py <本單>` | `0` | `🔴 機械·停機款：0 項` ／ `🟡 提示：0 項` | `0` |
| `verify/run_verification.py`（前·開工態）| `1` | `RESULT: FAIL` | `0` |
| `verify/run_verification.py`（後·落地後）| `1` | `RESULT: FAIL` | `0` |
| `verify/probes/probe_WG9265_coverage_account.py` | `0` | `違反 2 組／無違反 11 組／合計 13 組` | `0` |
| `verify/probes/probe_WG9265_clause_mapping.py` | `0` | `款數 14｜無受詞 4｜含「未量」4｜含 🔴 之判 6` | `0` |
| `verify/probes/probe_WG9265_V1prime.py` | `0` | `陽性 (5) 出艙 2 宗、陰性 0 宗 ⇒ 閘有判別力` | `0` |
| `python -m py_compile <三支探針>` | `0` | （無輸出）| `0` |

🔒 **本報告⛔ 含任何 `app.py` diff、⛔ 含任何修法主張**（單 §十二 g）。
🔒 **本報告⛔ 以行號（含 log 列號——其係 clone 路徑長度之函數）為錨**；
一律用**內容錨**（`W-G.9-264R` 之 `V-f` 已實證：clone root 長度 `5`／`24`／`62` 使同一 `RESULT` 落於列 `312`／`312`／`311`）。

🗑️ **拋棄式 clone**（`常規六 三`）：本批用 `3` 份（受詞態 ＋ 態A ＋ 態B），**收工後全數刪除**。

---

## §十三　🛑 本批⛔ 辦之事（逐項具名）

1. **⛔ 決定 `GB-67` 是否開修**——其由丙案（只定位）改甲案（開修）**繫 KL 之裁**。
   本批之工項六／七**只登記與澄清受詞**，**⛔ 更動 `GB-67`／`GB-154` 之現況欄、⛔ 判其解除**。
2. **⛔ 變更 `V-1′` 之 `(1)`／`(3)` 判準一字**——其紅已照實登記於 `VR-094`，判準之修訂繫發單側／KL。
3. **⛔ 回改 `V-1`（`W-G.9-261`）之「未達」判一字**（KL `2026-09-09` 明令）。
4. **⛔ 辦 `d-1`／`d-2`／`d-3`**——三者之**自我驗證閘⛔ 可得**，依單 `§十一 3` 具名為不可得。
5. **⛔ 另闢只算 `R3` 之臨時途徑**（單 `§三 三-5` 之裁）。
6. **⛔ 動配地幾何、⛔ 動生產碼四檔一字（含註解）。**

🔒 **單 `§十一` 停機款之逐條自檢**

| # | 停機款 | 是否觸發 |
|---|---|---|
| `1` | 工項四之人造對照未如預期 | ❌ **未觸發**（`1.0`／`0.0` 皆如預期）|
| `2` | 工項五之自我驗證閘不過 | ❌ **未觸發**（`7` 街廓相異值皆 `1`）|
| `3` | 工項七之 `d-1`／`d-2`／`d-3` 之自我驗證閘不過 | ✅ **已依該款處置**——**⛔ 得下結論**、**停機具名**（三項逐項具名其阻斷點·⛔ 靜默略過）|
| `4` | 任一工項須改生產碼四檔一字方能取得之量 | ❌ **未觸發**（四檔命中 `0` 行·期末 blob 逐位＝期初）|
| `5` | 量得**新的**重疊組（`13` 組以外，或既有組之重疊值改變）| ❌ **未觸發**——`13` 組全數量到，違反恰 `2` 組，其值 `45.9766`／`56.8186` 與既載**逐位相同** |
| `6` | `V-l` 之紅項名目集合出現差集（任一方向）| ❌ **未觸發**（二向差集皆 `[]`）|
| `7` | 疑義落入黑名單任一項 | ❌ **未觸發**（`§十一` 末之黑名單自檢）|
| `8` | CC 停機並回報恆為正確程序 | 🔒 **本批無須停機**；`#3` 之三子項係**款內具名之不可得**、⛔ 全批停機 |
