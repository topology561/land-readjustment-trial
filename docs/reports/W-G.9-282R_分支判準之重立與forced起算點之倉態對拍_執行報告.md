# `W-G.9-282R`　執行報告：**分支判準之重立** ／ `forced` 起算點與 `K-9-5-13` 之倉態對拍

> **單** ＝ `docs/orders/W-G.9-282_施工單_分支判準之重立與forced起算點之倉態對拍.md`（工項零已原封入倉）
> **分級** ＝ **中**（量測 ＋ 登記·**零生產碼**·⛔ 替身·⛔ 反事實）
> **開工態** ＝ `8d1e50e8245b35eaa1fe1e3b735ee557bad0a2aa`（`ls-remote` 實查·⛔ 本地追蹤 ref）
> 🛑 **本批鑄號 ＝ `自誤 383`／`384`／`385` 三號**（其逐字全由單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**。
> 🛑 **出艙即止**：⛔ 判碼面是否違反 `K-9-5-12`／`K-9-5-13`、⛔ 判碼註為誤、⛔ 判「替身線」之所指、
> ⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§零`　開工閘（九項·**全綠**）

三軸：`(1)` 來源 ＝ `blob@8d1e50e8245b35eaa1fe1e3b735ee557bad0a2aa`（工項零後 ＝ `be69045e…`）／
`(2)` 母體逐項具名／`(3)` 粒度框逐項具名。

| # | 受詞 | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `8d1e50e8…` 或其後裔 | `8d1e50e8245b35eaa1fe1e3b735ee557bad0a2aa`（**實查**·本窗 `HEAD` 同值·⛔ 以本地追蹤 ref 為證） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | **逐位相同** | ✅ |
| `3` | `p3a` 非祖先 | 須**非**祖先 | `rc = 1`（非祖先）｜**判別力[必為祖先]** `85c58147…` ⇒ `rc = 0` | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `b7156847cc073e378f89bfa004369b84de0a11c28ec0536c3ed18e8cf9457333` | 磁碟 `16846` B·`CR` `0`·`LF` `203`；受詞 `16768` B ⇒ **逐位相符**｜**判別力二造**：全檔同口徑 `302b281b…` **相異**／受詞多 `1` byte `daf8fcef…` **相異** ⇒ 該閘**非恆綠** | ✅ |
| `4'` | **閘器之受詞自證**（坑 `bf`） | [必真] `W-G.9-282` ＝ `True`；[必偽] `W-G.9-281` ＝ `False` | 所量之檔名逐字出艙；`True`／`False` | ✅ |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` | `rc = 0`·`stderr` `0` B｜🔴 停機款 **`0`**／🟡 提示 **`0`**／ℹ️ 記錄 `2`（`P-5` ✅；`P-3` `def main` ＝ `14975`-`24579`）｜`--selftest` `rc = 0`·**三靶皆觸·反靶不觸** ⇒ 非恆綠亦非恆紅 | ✅ |
| `5'` | `wg9223_acceptance_audit.py --selftest`（🆕 由 ＝ `自誤 383`） | `rc = 0` | **`rc = 0`**｜自證：必命中字樣 **`1`**（須 `> 0`）／人造字樣 **`0`**（須 `0`） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `372`／`382`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `85`／`85`·下一 `bh` | **逐位相符**；坑 `rc = 0`·`stderr` `0` B·母體 ＝ 追蹤 `.md` **`762`** 檔·可解析 **`85`／`85`**（不可解析 `0`）·末二標籤 `bf`／`bg` ⇒ 下一 ＝ `bh`｜判別力二造：造甲 坑 `n` 定義處 `6` ✅／造乙 人造之坑（執行期組出·字面⛔ 出艙）`0`／`0` ✅ | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] `> 0` | `app.py` `1` ＋ `verify/` 頂層 `.py` **`33`** ＝ **`34`**｜判別力 ＝ pathspec `verify/*.py` 多咬之**子層** **`299`**（期 `> 0`·坑 `be`） | ✅ |
| `8` | `verify/baselines` | `298` 檔·`a898f4e1…7ec9`（`quotePath` 預設） | `298` 檔｜預設 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`｜`=false` `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520` | ✅ |

🩸 **一項須照實具名之差（⛔ 頂替）**：閘 `7` 之判別力[必不命中]，**單載 `298`**、**本窗實測 `299`**。
差 ＝ **`1`** ＝ 前一批（`W-G.9-281` `commit 2`）新增之 `verify/probes/probe_WG9281_w0chain.py`
（落於 `verify/probes/` ⇒ 為 pathspec `verify/*.py` 所多咬之子層檔）。
🔒 該款之判準為「須 `> 0`」⇒ **仍 ✅**；所差之 `1` 係**發單側之期值量於本批基座<u>之前</u>**，⛔ 本批之異常。

🔒 **本窗環境之前置檢**（`harness-worktree-crlf-trap` 之開窗第一動作）：
`left-right` vs `origin/wip/s1-endpart` ＝ **`0  0`**（worktree `HEAD` 即 `wip` tip·**⛔ 自 `main` 開**）；
`.claude/settings.json` 與 `verify/tools/wg9237_heredoc_guard.py` **皆在**（heredoc 硬閘生效）；
`core.autocrlf` 有效值 **`false`**（local 覆蓋 system）。

---

## `§一`　`§零′` 之受領 ＋ 本批鑄號

🔒 **`§零′`**（前批之核可）係**發單側已辦**·⛔ CC 重辦——本報告僅**受領**其記載，⛔ 復驗、⛔ 引為本批之證。

**本批鑄號**（落點 ＝ `docs/reports/W-G.9波_claude.ai側自誤登記.md` **檔末追加**·嚴格 append-only）

| 簿 | 本批 | 期末 |
|---|---|---|
| `自誤` | **鑄 `383`／`384`／`385`**（逐字全由單供·⛔ 改寫） | 相異 `372` → **`375`**；MAX `382` → **`385`** |
| `GB` | **⛔ 鑄** | `167`／`169` **一字未動** |
| `VR` | **⛔ 鑄** | `80`／`95` **一字未動** |
| `K-9` | **⛔ 鑄** | `28`／`29` **一字未動** |
| 坑 | **⛔ 鑄** | `85`／`85`·下一 `bh`；`ALPHA_LABELS` **一字未動** |

**取號現查**（三軸｜`(1)` `blob@be69045e…`｜`(2)` 取最大號 ＝ 登記表**單檔**（`803492` B／`9482` 列）·
查占用 ＝ **全倉追蹤檔 `2199` 檔**（`-z` 原始位元組·可讀 `2177`）｜`(3)` **檔框 ＋ 列框**）
定義框列命中 `379`／範圍框列命中 `9` ⇒ 相異 `372`／**MAX `382`** ⇒ **次號 `383`**。
占用（**B 形 ⋀ C 形並取**·`GB-158`）：受詢 `383`／`384`／`385` 之 C 形 ＝ `1`／`5`・`1`／`4`・`1`／`2`；
對照組甲 `382` ＝ `3`／`13`（B 形 `1`／`1`）、甲2 `381` ＝ `6`／`19`（B 形 `2`／`2`）⇒ **二形皆 `>0`**；
對照組乙（人造哨兵·**執行期組出**·字面⛔ 出艙）三形皆 `0` ⇒ **器非紅**。
**逐筆歸類**（`裁 H`）：受詢三號之命中**全數集中於單一檔** ＝ 本單自身（本批工項零已入倉）
⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號、⛔ 順延。

---

## `§二`　工項零：本單原封入倉　✅

| 款 | **實測** |
|---|---|
| 所指之檔數 | 受詢（本單）＝ **`1`**｜造甲 `W-G.9-281` 之單 ＝ **`1`**（須 `1`）✅｜造乙 人造名（**執行期組出**·字面⛔ 出艙）＝ **`0`**（須 `0`）✅ |
| `CR` | 源／磁碟／blob 皆 **`0`**；`bytes` 皆 **`16846`**；`LF` ＝ **`203`**｜**判別力** 人造真 CRLF（記憶體內·⛔ 落檔）`CR` ＝ `2`（期 `> 0`）✅ |
| 三方逐位 | blob `sha1` ＝ `02bc1abd5479435589300e86fa5bd3cf0efc6106`（與源之**預算**相符 ⇒ ⛔ 抄錄）｜內容 `sha256` 源 ＝ 磁碟 ＝ blob ＝ **`302b281bcf2f190131a606e55112f984ad412f2de335dd9cf74512e3fb7c8ad9`** ✅ |

`git status --porcelain` 列數 ＝ **`1`**（逐檔 `git add`·⛔ `-A`／⛔ `.`）。

---

## `§三`　工項一（**主檢**）：分支判準之**重立**與逐格歸類

**器** ＝ `verify/probes/probe_WG9282_branch.py`（新建·`rc = 0`·`stderr` **`0`** B）
**落檔** ＝ `verify/out/WG9282R_branch.log`（**全量出艙**·⛔ `tail -N` 取結果）

### 三-0　器之結構與自我驗證閘

承 `probe_WG9281_w0chain.py` 之 `drive`：**包裹 `ns["solve_G_binary"]` 本身**取其**參數本身**
（`baseline_pt`／`side_mid`／`allocation_dir`／`d_hat`／`is_corner`），**⛔ climb 外層**（坑 `bg`）；
非參數之符號（`corner_pt`／`end_pt`／`*_cum_S`／`blk_label`）自呼叫端框內省取回。
另**包裹 `ns["_corner_buffer_S"]`**（**先原樣呼叫**·⛔ 改回傳一位元）以取 `buf`。

| 態 | `solve_G_binary` 呼叫 | `_corner_buffer_S` 呼叫 | 鏈頭所錄之 `blk` | 包裹器錯誤 | 判 |
|---|---|---|---|---|---|
| `0m` 態乙 | `110` | `0` | `['R1','R2','R4']` | `0` | ✅ |
| `0m` 態甲 | `149` | `1` | `['R1','R2','R3','R4','R5','R6']` | `0` | ✅ |
| `3.5m` 態乙 | `110` | `1` | `['R1','R4']` | `0` | ✅ |
| `3.5m` 態甲 | `149` | `2` | `['R1','R3','R4','R5','R6']` | `0` | ✅ |

判別力[必非零]：四態合計 `solve_G_binary` 呼叫 ＝ **`518`**（期 `> 0`）✅ ⇒ **器非紅**。
⚠️ **照實**：`_corner_buffer_S` 之呼叫次數（`0`／`1`／`1`／`2`）**少於**乙格數（`0`／`1`／`3`／`2`）
——因 `run_step_g` 於 `R4 right` **提早中止**（見「終止態」）⇒ 其後之街廓未被進入。**⛔ 判其成因。**

### 三-1　情形甲／乙（源 ＝ `run_corner_pk` 之第三回傳 `_off` 之 `指配` 欄）

| 態 | `_off` 列數 | **乙之格** |
|---|---|---|
| `0m` 態乙 | `0` | （無） |
| `0m` 態甲 | `1` | `[('R4','left')]`（`116.08`） |
| `3.5m` 態乙 | `3` | `[('R2','left'), ('R3','right'), ('R5','left')]`（`308.17`／`307.79`／`299.73`） |
| `3.5m` 態甲 | `2` | `[('R2','left'), ('R4','left')]`（`308.17`／`226.01`） |

判別力[必命中]：四態之乙格合計 **`6`**｜`_off` 列合計 **`6`** ⇒ ✅（**⛔ 以空集充綠**）。

### 三-2　新判準之逐格歸類（母體 ＝ 同一有值鏈頭格·**本批於現母體重量**）

**母體之列數 ＝ `48`**｜**有值格 ＝ `22`**（⛔ 轉引前批）。
二布林皆 **`float ==` 之逐位**相等（**⛔ 容差、⛔ 門檻**）：

```
錨(該鏈自身) = 該呼叫端之 baseline_pt 賦值式中之基點（左鏈 corner_pt／右鏈 end_pt·逐格自碼面取回）
b1 = 「錨 逐位等於 該側 SIDE_LINE 之 p1 或 p2」（來源 ＝ side_mid 之同一結構 f3_cad_side_lines_by_side）
b2 = 「該鏈之 cum_S 逐位等於 0.0」（左鏈 left_cum_S／右鏈 right_cum_S）
```

**支之分布（全母體 `22` 格）**

| 支 | 定義 | 格數 |
|---|---|---|
| **`A1`** | `b1` 真 ∧ `b2` 真 | **`10`** |
| **`A2`** | `b1` 真 ∧ `b2` 偽 | **`2`** |
| **`B1`** | `b1` 偽 ∧ `b2` 真 | **`10`** |
| **`B2`** | `b1` 偽 ∧ `b2` 偽 | **`0`** |

**不可得之格 ＝ `0`**（⛔ 以偽代之之情形未發生）。逐格全欄（含 `錨之符號名`／`錨`／`p1`／`p2`／
`錨 == p1`／`錨 == p2`／`b1`／`cum_S 之符號名`／`cum_S`／`b2`／`支`／`W_0`／`R(W_0)`／`is_corner`／`_fo`／
`â·(p2 − p1)`／`â·(錨 − p1)`／外積）見落檔。

### 三-3　`§三-4` 判別力三造

| 造 | 受詞 | 期 | **實測** | 判 |
|---|---|---|---|---|
| 甲[必命中] | `b1` 為**真**之格數 | `≥ 1` | **`12`**（`A1` `10` ＋ `A2` `2`·逐格具名見落檔） | ✅ |
| 乙[必命中] | `b1` 為**偽**之格數 | `≥ 1` | **`10`**（`B1`） | ✅ |
| 丙[必偽] | 人造點（**執行期組出**·字面⛔ 出艙）vs `p1`／`p2` | `False`／`False` | `False`／`False` | ✅ |

🔒 **新判準之判別力經實測成立**——四支中 **`3` 支非空**；⛔ 再現 `自誤 385` 之「結構上恆為單一支」。

### 三-4　`§三-5`　情形甲 ∧ `R(W_0) > 0` 之格：依**新判準**逐格歸類

**當場重得之成員 ＝ `4` 格**（⛔ 轉引前批之 `4`）——與單所具名者逐格相同。

| # | 格 | 暫編地號 | `(b1, b2)` | **支** | 錨（符號名 ＋ 值） | `p1` | `cum_S` |
|---|---|---|---|---|---|---|---|
| `1` | `0m` 態甲 `R2/left` | `628-41(1)` | `(True, True)` | **`A1`** | `corner_pt` ＝ `[310361.51526, 2651912.18005]` | `[310361.51526, 2651912.18005]`（**逐位相同**） | `left_cum_S` ＝ `0.0` |
| `2` | `0m` 態乙 `R2/left` | `628-41(1)` | `(True, True)` | **`A1`** | 同上 | 同上 | `0.0` |
| `3` | `0m` 態乙 `R4/right` | `628-1(1)` | `(False, True)` | **`B1`** | `end_pt` ＝ `[310584.97277032473, 2651869.489542534]` | `[310585.0948, 2651869.36064]`（**相異**） | `right_cum_S` ＝ `0.0` |
| `4` | `3.5m` 態乙 `R4/right` | `628-1(1)` | `(False, True)` | **`B1`** | 同上 | 同上 | `0.0` |

`W_0`／`R(W_0)`：`1`／`2` 格 ＝ `1.9195844913610927e-10`／`3.340077014968301e-09`；
`3`／`4` 格 ＝ `0.17750040709932335`／`3.088507083528226`。四格之 `is_corner` 皆 `True`、`_fo` 皆 `False`。

**末三欄（🛑 出艙即止·⛔ 據以判「錨是否在側線上」、⛔ 以任何門檻判之）**

| 格 | `â·(p2 − p1)` | `â·(錨 − p1)` | `(錨 − p1) × (p2 − p1)` |
|---|---|---|---|
| `R2/left`（`1`／`2`） | `0.0` | `0.0` | `-0.0` |
| `R4/right`（`3`／`4`） | `3.552713678800501e-15` | `0.17750040690957658` | `5.8751683808028785` |

🔒 **⇒ 該 `4` 格落於 `2` 支（`A1`／`B1`）⇒ 依單之「歸類之效力 ＝ 分屬不同支者即分列」，
「應分列」即為<u>機械事實</u>**（⛔ 由 CC 另判其法律效果）。

---

## `§四`　工項二：`forced` 側起算點與 `K-9-5-13` 之**倉態對拍**（🛑 **出艙即止**）

**落檔** ＝ `verify/out/WG9282R_j4_static.log`（靜態款·`rc = 0`·`stderr` `0` B）
＋ `verify/out/WG9282R_branch.log`（執行期款）
三軸：`(1)` 來源 ＝ `blob@be69045e…`／`(2)` 母體 ＝ `K-6` 單檔（`299931` B／`4125` 列）、
`verify/stepg_pipeline.py` 單檔（`111526` B／`1671` 列）、`app.py` 單檔（`1464014` B／`24582` 列）／
`(3)` **列框**。

### 款 `1`　`K-9-5-13（二）所答` 之裁定二逐字 ＋ `K-9-5-6` 失效加註全段逐字

框甲（**有 `^` 錨**）＝ `^### 🔒 K-9-5-13 ` ⇒ 命中列 **`1`**；
框乙（**無 `^` 錨·二數並報**）＝ `失效加註` ⇒ **命中檔數 `1`／命中列數 `3`**。

```
[K-6:2026] > **裁定二**：「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
[K-6:2048] 2. **起算一律用真正的地界**；「面積換算出來的替身線」**在側街負擔上不再有角色**（裁定二）。
```

`K-9-5-6` 節內之**失效加註**全段（所屬之節 ＝
`### 🔒 K-9-5-6 W 鏈定式：每條界線只量一次·Rw 逐宗累積（KL **追認** 2026-08-08·D-2b-4·canonical·**逐字**）`）：

```
[K-6:910]   > 🛑 **失效加註（`K-9-5-13`·KL 裁定二·2026-08-12·⛔ 上文原句一字未刪）**
[K-6:911]   >
[K-6:912]   > 上開「後半句」**自本批起失效**。KL 裁定二逐字：
[K-6:913]   > 「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
[K-6:914]   > ⇒ `forced` 時之起算**不再取「其 buffer 起算點」**（該點即「面積換算出來的替身線」）。
[K-6:915]   > 依據見 `grep -n "^### 🔒 K-9-5-13 " 本檔`。
```

### 款 `2`　`left_cum_S`／`right_cum_S` 之**賦值式逐字** ＋ 其當場值

框（**有 `^` 錨**）＝ `^\s*left_cum_S` ⇒ **命中檔數 `1`／命中列數 `4`**；
`^\s*right_cum_S` ⇒ **命中檔數 `1`／命中列數 `4`**（母體 ＝ `verify/stepg_pipeline.py` **單檔**）。

```
[stepg:776]             left_cum_S = float(_left_buffer_S)
[stepg:777]             right_cum_S = float(_right_buffer_S)
[stepg:862]                 left_cum_S += _S_actual
[stepg:865]                 left_cum_S = _wg9268p_anchor_advance(
                                left_cum_S, res.get('cut_coords'), d_hat, corner_pt, allocation_dir_block)
[stepg:980]                 right_cum_S += _S_actual
[stepg:983]                 right_cum_S = _wg9268p_anchor_advance(
                                right_cum_S, res.get('cut_coords'), d_hat_rev, end_pt, allocation_dir_block)
```

**其於 `forced` 側鏈頭格之當場值**

| 情境 | 格 | `left_cum_S` | `b2` | `b1` | 支 | `W_0` | `R(W_0)` |
|---|---|---|---|---|---|---|---|
| `0m` | 態甲 `R4/left` | **`4.49975224003872`** | `False` | `True` | **`A2`** | `4.4804447557822895` | `40.77875748585019` |
| `3.5m` | 態甲 `R4/left` | **`7.8185635189090075`** | `False` | `True` | **`A2`** | `7.785015718090257` | `59.93908487768739` |

（二格之錨 ＝ `corner_pt` ＝ `[310517.10709, 2651941.1775]`，**逐位等於** `p1`；`p2` ＝ `[310495.1684, 2651916.16941]`。）

### 款 `3`　`_corner_buffer_S` 之**產生式逐字**：其解算之**目標量**

框（**無 `^` 錨·二數並報**）＝ `_corner_buffer_S` ⇒ **命中檔數 `2`／命中列數 `19`**；
`^def _corner_buffer_S`（有 `^` 錨）⇒ 命中列 **`1`**。

```
[app.py:9335]     §3 街角 forced band（plan v3 §3·補丁九）：**bisect 解 `buf` 使「真實池帶面積 == range_area」**
[app.py:9336]     （|Δ| ≤ tol）。**全精度回傳**（N0-18a·不 round）。取代舊矩形近似 `range_area ÷ avg_depth`
[app.py:9348]                         ∵ 消費端 `left_cum_S = buf` ⇒ 首宗自 `s=buf` 起 ⇒ 保留區恆為 `[s_min, buf]`
[app.py:9366]     if range_area is None or float(range_area) <= 0:
[app.py:9367]         return 0.0                      # 無 forced（該側無強制抵費地）→ 不進 bisect
```

🔒 **其解算之目標量 ＝ <u>面積</u>**；**目標符號名 ＝ `range_area`**（其為 `_corner_buffer_S` 之第 `5` 個位置參數）。

**其回傳 `buf` 之逐次出艙 ＋ 與 `cum_S` 之逐位對拍**

| 態 | `blk` / `side` | `buf` | `range_area` | `front_p1` | 對應之 `cum_S` | **逐位** |
|---|---|---|---|---|---|---|
| `0m` 態甲 | `R4` / `left` | `4.49975224003872` | `116.08` | `[310517.10709, 2651941.1775]` | `left_cum_S` ＝ `4.49975224003872` | **相同** |
| `3.5m` 態甲 | `R4` / `left` | `7.8185635189090075` | `226.01` | 同上 | `left_cum_S` ＝ `7.8185635189090075` | **相同** |
| `3.5m` 態甲 | `R2` / `left` | `8.696079893776137` | `308.17` | `[310361.51526, 2651912.18005]` | （該格未成鏈頭·見落檔） | — |
| `3.5m` 態乙 | `R2` / `left` | `8.696079893776137` | `308.17` | 同上 | （同上） | — |

### 款 `4`　該側 `_off` 之原列（逐列出艙）

```
{'街廓': 'R4', '端': '左', '抵費地面積＝range(㎡)': '116.08', '指配': '強制抵費地'}      ← 0m 態甲
{'街廓': 'R4', '端': '左', '抵費地面積＝range(㎡)': '226.01', '指配': '強制抵費地'}      ← 3.5m 態甲
{'街廓': 'R2', '端': '左', '抵費地面積＝range(㎡)': '308.17', '指配': '強制抵費地'}      ← 3.5m 態甲／態乙
{'街廓': 'R3', '端': '右', '抵費地面積＝range(㎡)': '307.79', '指配': '強制抵費地'}      ← 3.5m 態乙
{'街廓': 'R5', '端': '左', '抵費地面積＝range(㎡)': '299.73', '指配': '強制抵費地'}      ← 3.5m 態乙
```

### 款 `5`　碼面**是否產生**「抵費地之實際配地多邊形」

🛑 **判 ＝ ⛔ 產生**（loud·母體與字樣**逐項正面列舉**·⛔ 只書「查無」）。

**母體（正面列舉·三檔）**：`app.py`（`24582` 列）／`verify/stepg_pipeline.py`（`1671` 列）／
`verify/selection_pipeline.py`（`629` 列）。

**所查之字樣（逐項具名）與其命中（檔數／列數）**

| 字樣 | 檔／列 | | 字樣 | 檔／列 |
|---|---|---|---|---|
| `抵費地` | `3`／`192` | | `range_poly` | `2`／`16` |
| `強制抵費地` | `2`／`27` | | `forced_poly` | **`0`／`0`** |
| `抵費地面積` | `2`／`15` | | `抵費地.*Polygon` | `1`／`1` |
| `offset_poly` | **`0`／`0`** | | `指配` | `2`／`30` |
| `_off_poly` | **`0`／`0`** | | `f3_forced_offset_diag` | `1`／`5` |
| `抵費地多邊形` | **`0`／`0`** | | `left_buffer_S`／`right_buffer_S` | `2`／`16` 各 |
| `抵費地_poly` | **`0`／`0`** | | `forced_offset` | `3`／`53` |
| `抵費地.*cut_coords` | **`0`／`0`** | | `抵費地.*vertices` | **`0`／`0`** |

`抵費地` ∩ 幾何字樣（`Polygon`／`poly`／`shapely`／`buffer`／`intersection`）之列 ＝ **`10`** 列，
**全數落於 `def main` 之 AST 區間（`14975`-`24579`）內之 UI／診斷段**——其一逐字 ＝
`該 buffer 區（街角最小面積÷D_avg）不參與分配、應顯示為橘色抵費地；`；
且 `_block_strip(` 之命中為 `app.py` **`9`**／`verify/stepg_pipeline.py` **`0`**／`verify/selection_pipeline.py` **`0`**。
唯一之池幾何 ＝ `app.py:9869` 之 `_pool_poly, _ = _block_strip(blk_poly, d_hat, _cp + _pool_lo * _dh, _pool_S, …)`，
其 `_pool_lo = _cum_left` ⇒ **buffer 區僅以 `s` 區間之<u>下界</u>被隱含排除，⛔ 具名之多邊形物件**。

對照組：[必命中] `def ` ⇒ 檔 `3`／列 `366`；[必為零] 人造字樣（**執行期組出**·字面⛔ 出艙）⇒ `0`／`0`
⇒ **器非紅**。

🔒 **⇒ 單所令之「其遠側界線與 `corner_pt + buf·d̂` 之逐位對拍」<u>⛔ 可行</u>**（受詞不存在）；
本報告以**款 `2`／`3` 之 `cum_S` 與 `buf` 之逐位對拍**（**二格皆相同**）代之，並**具名該替代**。

🛑 **出艙即止**：⛔ 判碼面是否違反 `K-9-5-13`／`K-9-5-12（四）`、⛔ 判「替身線」之所指、
⛔ 鑄 `GB`、⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張。**歸屬由發單側於復驗後定之。**

---

## `§五`　工項三：三則 `自誤` 之登記 ＋ 二則末端追加

### 五-1　`自誤 383`／`384`／`385`（落點 ＝ 自誤登記表**檔末追加**）

🔒 三則之**逐字全由單供**，其塊內文字由**抽取管線直接產生**（`常規七 一`·`常規七 二`：
三數與塊內文字出自**同一次**抽取）：`383` ＝ 單之列 `133`–`136`（`4` 列／`476` B）／
`384` ＝ 列 `138`–`143`（`6` 列／`713` B）／`385` ＝ 列 `145`–`149`（`5` 列／`531` B）。
標題形取**既有最末一則**（`自誤 382`）為**逐字範本**（`常規四（九）四`）＝ ``### 🩸 `自誤 N`　**…**``。

| 檔 | 期初 bytes | 期末 bytes | `+N` | `CR` | 嚴格前綴·造甲[必真] | 造乙[必偽] |
|---|---|---|---|---|---|---|
| 自誤登記表 | `803492` | `808375` | **`+4883`** | `0` | `True` | `False` |

### 五-2　二則末端追加（落點 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**·同一節內二列）

🔒 二則之逐字亦由抽取管線產生（單之列 `155`／`156` 之「」內·`356` B／`343` B）。

| 檔 | 期初 bytes | 期末 bytes | `+N` | `CR` | 嚴格前綴·造甲[必真] | 造乙[必偽] |
|---|---|---|---|---|---|---|
| `GB` 登記表 | `876204` | `877570` | **`+1366`** | `0` | `True` | `False` |

🔒 **`GB-132` 之避讓經實測成立**：立後四簿 ＝ 自誤 相異 **`375`**／MAX **`385`**（**恰 `+3`**）；
`GB` `167`／`169`、`VR` `80`／`95`、`K-9` `28`／`29` **逐位未動**。

---

## `§六`　一句話推導（驗收固定項）

> 情形甲 ∧ `R(W_0) > 0` 之 `4` 格，依**新判準**（`b1` ＝ 錨逐位等於該側 `SIDE_LINE` 之 `p1`／`p2`；
> `b2` ＝ 該鏈 `cum_S` 逐位為 `0.0`）落於**二支**——`R2/left` 二格之錨（`corner_pt`）
> **逐位等於** `p1` ⇒ `A1`，而 `R4/right` 二格之錨（`end_pt`）與 `p1` **相異** ⇒ `B1`
> ⇒ **「應分列」為機械事實**；而 `forced` 側（`R4/left`）之鏈頭起算 `left_cum_S`
> **逐位等於** `_corner_buffer_S` 之回傳 `buf`（其目標量 ＝ **面積** `range_area`）⇒ 支 `A2`。

---

## `§七`　🩸 本批之**自捕**（⛔ 頂替·單 `§八` 所令）

### 自捕 `1`｜取號現查之**量測器紅**（二因併發·於出艙前攔下）

| 項 | 內容 |
|---|---|
| **形** | `(i)` **坑 `bc`**：母體以 `git ls-tree -r --name-only`（**`core.quotePath` 預設**）取得 ⇒ 含 CJK 之路徑被**引號＋八進位跳脫**包起，`p.endswith('.md')` **恆偽**；`(ii)` **坑 `bd`**：所取之人造哨兵**其字面已入倉** |
| **徵候** | `.md` 母體僅 **`32`** 檔（真值 **`763`**）、**自誤登記表自身被排除** ⇒ 對照組甲 `382`／`381`（**已鑄·須 `>0`**）**三形皆得 `0`**；而**必為零**之哨兵反得 `1`／`1` |
| **後果（若未攔下）** | 「受詢三號未占用」之結論**⛔ 帶資訊**（器對一切號皆判未占用），且哨兵之「必為零」性質已失 ⇒ 二造對照**同時失效** |
| **攔法（已施）** | ① 母體改用 `-z`（**原始位元組**·⛔ 跳脫）並**併報二口徑之數**；② 哨兵**執行期組出**並**先驗其於現母體三形命中皆為 `0`**，其**字面⛔ 出艙** |

### 自捕 `2`｜落檔之 `CRLF`（坑 `6` 之變體）

`python <probe> > <log>` 之重導向以**文字模式**寫出 ⇒ `log` 帶 `CRLF`；以**二進位**就地歸正
（**內容一字未改**）。實測見 `§八` 收工閘 `3`。

---

## `§八`　收工閘之實測（**入倉後末端追加**）

> 🛑 本節於本報告入倉**之後**追加——⛔ 預寫。

---

## `§九`　🛑 `⛔ 為之事`之逐項自檢

| # | 受詞 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字 | ✅ 未動（收工閘 `6` 之 blob 期初＝期末） |
| `2` | ⛔ 改任何 `*_EXPECT` | ✅ 未改 |
| `3` | ⛔ 修 `GB-167`／`168`／`169` | ✅ 未修（`GB` 登記表之異動係**檔末追加**·⛔ 改 `GB-169` 原節一字） |
| `4` | ⛔ 解除、收窄或結清 `GB-168`／`GB-169` | ✅ 未為 |
| `5` | ⛔ 落地 `K-9-5-12`／`K-9-5-13`／`K-9-29` 之任何子項 | ✅ 未為 |
| `6` | ⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 未改 |
| `7` | `GB-162` 三禁令（⛔ `WV_BAKE`／⛔ 覆寫 `verify/baselines`／⛔ 併線 `p3a`） | ✅ 皆守；**⛔ 引任何自 baseline 取得之數** |
| `8` | ⛔ 施替身、⛔ 反事實、⛔ 未經具名之長跑 | ✅ 未為（二包裹器**皆先原樣呼叫**再記錄·⛔ 改回傳一位元） |
| `9` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號、⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未為（四簿之 `GB`／`VR`／`K-9` 期末未動；`ALPHA_LABELS` 之檔本批變更檔數 `0`） |
| `10` | ⛔ 追改任何已入倉文件一字（含 `W-G.9-281R`） | ✅ 未為——二登記表之異動**皆嚴格 append-only**（前綴檢二造：`True`／`False`），`deletions` 全 `0` |
| `11` | ⛔ 判違反／判碼註為誤／判「替身線」之所指／判孰誤／提修法主張 | ✅ 未為（`§三`／`§四` 皆**出艙即止**） |
| `12` | ⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ 未為 |
| `13` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§八`／`§十` 皆**入倉後／推送後**追加 |

---

## `§十`　逐 `commit` 與 `push` 之 `ls-remote` 實查

> 🛑 本節於推送**之後**追加——⛔ 預寫。

---

## `§八`（本體）　收工閘之實測

> 🔒 上二節（`§八`／`§十`）之標頭係 `commit 2` 所立之**佔位**（其時該數尚未量得）；本節與次節為其**本體**，
> 於 `commit 2`（`4070b492…`）**推送並 `ls-remote` 實查後**方量得，以**嚴格末端追加**寫入（⛔ 改既有一字）。
> 三軸：`(1)` 來源 ＝ `blob@4070b492856d97c5a4984bd5f745521a024750f9`／`(2)` 母體逐項具名／`(3)` 粒度框逐項具名。

| # | 受詞 | 期 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `numstat` **刪除欄逐檔具名** | 全 `0` | `commit 1`：`+203／−0`（單）｜`commit 2` **十檔**：`+18／−0`（`GB` 登記表）・`+382／−0`（本報告）・`+69／−0`（自誤登記表）・`+0／−0`（`branch .err`）・`+289／−0`（`branch .log`）・`+1／−0`（`branch .rc`）・`+0／−0`（`j4_static .err`）・`+228／−0`（`j4_static .log`）・`+30／−0`（`numbering .log`）・`+475／−0`（探針）⇒ **刪除欄全 `0`** | ✅ |
| `2` | 生產碼判法·母體**正面列舉 `34` 檔** | 逐區間 `0`；對照組 `≥ 1` | `commit 1` 變更檔 `1`・∩ `34` 檔 **`0`**・∩ `常規一` 之 `4` 檔 **`0`**｜`commit 2` 變更檔 `10`・∩ `34` 檔 **`0`**・∩ `4` 檔 **`0`**｜**判別力[必非零]** 歷史區間 `17fd9818` ⇒ ∩ `34` 檔 ＝ **`2`** | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ **倉內 blob**） | `0`；判別力 `> 0` | 本批所觸 `.md`／`.py` ＝ **`1`** 檔（`probe_WG9282_branch.py`）·`CR` ＝ **`0`**｜**判別力[必非零]** `verify/out/KL_UI_3.5m_2e08a41_stdout.log` ⇒ `CR` ＝ **`2987`** | ✅ |
| `4` | 四簿期末 | 自誤 相異 **`375`**／MAX **`385`**；`GB`／`VR`／`K-9` 一字未動 | 自誤 相異 **`375`**／MAX **`385`**／缺號 `[106, 355, 356, 357]`（**恰 `+3`**·⛔ 其他增減）｜`GB` `167`／`169`・缺號 `[12, 87]`｜`VR` `80`／`95`・缺號 `[73, 75]`｜`K-9` `28`／`29`・缺號 `[]` | ✅ |
| `5` | 坑之期末 | **`85`／`85`**·下一 `bh`；`ALPHA_LABELS` 一字未動 | `rc = 0`·`stderr` **`0`** B·母體 ＝ `4070b49` 之追蹤 `.md` **`764`** 檔（`8d1e50e8` 態 `762` ＋ 本批新增之 `2` 份 `.md` ⇒ **自洽**）｜可解析 **`85`／`85`**（不可解析 `0`）·末標籤 `bg` ⇒ **下一 ＝ `bh`**｜判別力二造：造甲 坑 `n` 定義處 `6` ✅／造乙 人造之坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅｜`probe_WG9269_pit_index.py` 於 `8d1e50e8..4070b492` 之變更檔數 ＝ **`0`** ⇒ `ALPHA_LABELS` **一字未動** | ✅ |
| `5'` | `wg9223_acceptance_audit.py <本單> <本報告>` | 命中 `0` 之字樣 ＝ `0`；併報判定組個數 | 【驗收】段 ＝ 單之 `:196`–`:200`｜**判定組（反引號 token）`29` 個**／參考組（CJK 詞）`3` 個（**⛔ 作判**）⇒ **判定組之報告命中 `0` ＝ `0` 個**（🔒 判定組**非空** ⇒ 該器於本單**非恆綠**·`序 11` 之一般化款 `二`） | ✅ |
| `6` | 自限復驗 | 相異 `0` 等六款 | `(a)` **`34` 檔 blob 期初（`8d1e50e8`）＝期末（`4070b492`）·相異 ＝ `0`**；**判別力[必非零]** 同法於 `17fd9818` ⇒ 相異 ＝ **`2`**｜`(b)` `verify/baselines` **`298`** 檔·框 `sha256` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（口徑 ＝ `core.quotePath` **預設**）／`7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520`（`=false`）｜`(c)` `p3a` **非祖先**（`rc = 1`；**判別力[必為祖先]** `be69045e` ⇒ `rc = 0`）｜`(d)` `WV_BAKE` ＝ `None`｜`(e)` `K-9-29` 任何子項**⛔ 落地**、`GB-167`／`168`／`169` **⛔ 修**——二者皆由 `(a)` 之「`34` 檔 blob 相異 `0`」**機械蘊含** | ✅ |
| `7` | `push` 後 `ls-remote` **實查** | 主線 ＝ 本批末一 `commit` | 見 `§十`（本體） | ✅（`commit 1`／`2`） |

🔒 **收工閘 `1`〜`6` ＋ `5'` 全綠**；`7` 見次節。

---

## `§十`（本體）　逐 `commit` 與 `push` 之 `ls-remote` 實查

| # | `commit` | 內容 | `push` 後 `ls-remote origin refs/heads/wip/s1-endpart`（**實查·⛔ 本地追蹤 ref**） |
|---|---|---|---|
| 基座 | `8d1e50e8245b35eaa1fe1e3b735ee557bad0a2aa` | `W-G.9-281` 收工（前批） | 開工閘 `1` 實查 ＝ 同值 |
| `1` | `be69045e09dcc255f95b2393ababfe9426427f83` | `§二` 工項零：本單原封入倉 | **`be69045e09dcc255f95b2393ababfe9426427f83`** ✅ |
| `2` | `4070b492856d97c5a4984bd5f745521a024750f9` | `§三`〜`§五` 工項一〜三 ＋ 本報告 `§零`〜`§九` | **`4070b492856d97c5a4984bd5f745521a024750f9`** ✅ |
| `3` | （本節所在之 `commit`） | `§八`／`§十` 本體之末端追加 ＋ 收工閘 `5` 之落檔 | 🛑 **其 `ls-remote` 實查於推送<u>之後</u>出艙於聊天**——⛔ 預寫、⛔ 於入倉文件宣稱一件尚未發生之事（單 `§七` 末款） |

🔒 **偏離之逐字回報**（承 `W-G.9-194` 裁四之體例）：`commit 3` 之受詞（收工閘）**結構上必在其自身之 `push` 之前量得**，
故其自身之 `ls-remote` **⛔ 能寫入該 `commit`**；本表以「候聊天出艙」載之——⛔ 留空、⛔ 預寫。

---

## `§十一`　交付清單

| 檔 | 性質 |
|---|---|
| `docs/orders/W-G.9-282_施工單_分支判準之重立與forced起算點之倉態對拍.md` | 本單原封入倉（`commit 1`） |
| `verify/probes/probe_WG9282_branch.py` | 主檢之器（新建·`475` 列） |
| `verify/out/WG9282R_branch.log`／`.err`／`.rc` | 主檢之**全量**落檔（`32694` B／`0` B／`rc = 0`） |
| `verify/out/WG9282R_j4_static.log`／`.err` | `§四` 靜態款之逐字出艙（`18010` B／`0` B） |
| `verify/out/WG9282R_numbering.log` | 取號現查（修正版·`2015` B） |
| `verify/out/WG9282R_pitindex_post.log`／`.err`／`.rc` | 收工閘 `5` 之落檔 |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 三則自誤之登記（**末端追加** `+69` 列） |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 二則末端追加（`+18` 列） |
| `docs/reports/W-G.9-282R_…_執行報告.md` | 本報告 |

---

## `§十二`　🔺 **候發單側裁之項**（⛔ 由 CC 自裁·⛔ 呈 KL）

| # | 項 | 現態 |
|---|---|---|
| `1` | **`§三-5` 之 `4` 格落於二支（`A1`／`B1`）** | 依單之明定「分屬不同支者即分列」⇒ **「應分列」為機械事實**；其**法律效果**候裁 |
| `2` | **`§四` 款 `5` 之受詞不存在** | 碼面⛔ 產生「抵費地之實際配地多邊形」⇒ 單所令之「其遠側界線與 `corner_pt + buf·d̂` 之逐位對拍」**⛔ 可行**；本報告以 `cum_S` ↔ `buf` 之逐位對拍**具名替代**，其是否受領候裁 |
| `3` | **本批自捕之量測器紅（坑 `bc` ＋ 坑 `bd` 併發）** | 已於出艙前攔下並補攔法；🛑 本批**⛔ 鑄坑**（單 `§一`）⇒ **其是否須鑄新坑候裁**（發單側於 `W-G.9-282 §一` 裁 `3` 已就坑 `9` 之新徵候採「⛔ 鑄坑·末端追加」之體例） |
| `4` | **開工閘 `7` 之判別力期值 `298` vs 實測 `299`** | 差 ＝ `1` ＝ 前批新增之 `probe_WG9281_w0chain.py`；判準為「須 `> 0`」⇒ 仍 ✅。**⛔ 追改單一字**；其登記與否候裁 |
| `5` | **`_corner_buffer_S` 呼叫次數少於乙格數** | `0`／`1`／`1`／`2` vs 乙格 `0`／`1`／`3`／`2`——因 `run_step_g` 於 `R4 right` 提早中止 ⇒ 其後之街廓未被進入。**⛔ 判其成因**；候裁 |
