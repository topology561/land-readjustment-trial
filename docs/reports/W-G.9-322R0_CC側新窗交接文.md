# CC 側新窗交接文（`W-G.9-322` 補令一 工項五·發單側所備）

> 🛑 **本文係發單側自倉可驗之部分**；舊窗 CC 只有自身知悉之事項，另以本檔 `§六` 之末端追加承載（本補令 工項六）。
> 🛑 **本文任何數⛔ 採信**——新窗之第一動即逐格自倉重跑。

## `§一`　三造工作法（⛔ 逾越）

| 造 | 角色 | 界限 |
|---|---|---|
| **KL** | 域裁（域權威）＋ UI 操作者 | 只作域裁與放行；**⛔ 程式設計者** |
| **CC**（你） | **唯一實作者** | **只在 KL 貼入一張單時動作**；⛔ 自主開工、⛔ 自裁、⛔ 追改單一字、⛔ 自改取法 |
| **claude.ai** | 獨立驗證者（發單側） | 出單、驗收、登記自誤；**⛔ 採信 CC 之自述**，一律自倉 clone 並 grep |

🛑 **停機即上呈**：凡單之停機款成就，一律停機回報·**⛔ 逕行入倉**·**⛔ 自行改取法**。

## `§二`　開工態錨（🛑 **⛔ 採信·須逐格自倉重跑**）

**主線** ＝ `wip/s1-endpart` @ `4c5a143a2d55d7d9a251a255f46e216086babd43`

| # | 項 | 值 |
|---|---|---|
| `1` | 生產碼 | `verify/` 頂層 `*.py` **`33`** ＋ `app.py` ＝ **`34`** 檔·**⛔ 動**；`app.py` blob ＝ `8466f7493082bf84aa4f02d6c1c6e55ff746fc4a` |
| `2` | 子層 `*.py`（判別力[必不命中]） | **`370`**（`probes 342`／`tools 22`／`fixtures 4`／`archive 2`）。🛑 **量測時點須在<u>自身最末一筆 commit 落地之後</u>**（`自誤 450` 之族·前窗於此連踩二側） |
| `3` | 四簿（**二框並報**） | 正典框 自誤 `439`／`449`；自擬框 `435`／`449`｜`GB` `169`／`171`｜`VR` `80`／`95`｜`K-9` 正典框 `40`／`41`·節題框 `36`／`41` |
| `4` | 四簿之 blob·bytes·列 | 自誤簿 `76b8410b…`·`951718` B·`10807` 列｜恆常附款簿 `a01264ed…`·`45887` B·`419` 列｜`GB` 簿 `64e91c72…`·`944120` B·`8396` 列｜`K-6` 典 `3bc628df…`·`360462` B·`4498` 列｜`VR` 簿 `7cff9a37…`·`394392` B·`5124` 列 |
| `5` | 母體 | 全倉追蹤 `2593`｜`.md` `881`｜`docs/` `847`｜`verify/out/` `1016` |
| `6` | `baselines` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`298` 列（⛔ 動） |
| `7` | refs | 遠端 `25`（`verify/*` `20`／`wip/*` `3`／`main` `1`／`claude/*` `1`） |
| `8` | 保全分支 | `origin/wip/W-G.9-318-preserve` ＝ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`·**已落後主線**·⛔ 再是同步鏡·⛔ 刪 |

## `§三`　倉之紀律（**逐條·⛔ 例外**）

1. **`append-only`**：自誤簿／恆常附款簿／`GB` 簿／`K-6` 典／`VR` 簿／`CLAUDE.md` 一律**純末端追加**，**⛔ 改既有一字**；更正以末端追加承載。其判準**三值化**（🟢 前綴且增長／🔵 前綴且未改／🔴 前綴被破），⛔ 二值合取。
2. **零生產碼**：`app.py` 與 `verify/` 頂層 `*.py` 共 `34` 檔，**非經 KL 逐 `commit` 放行⛔ 動一字**。零生產碼之批次得依 `常規一` 補款 `🔧 一` **逕行 `push`**·⛔ 請示；**⛔ 及於繞過 harness 權限分類器**。
3. **`git`**：clone 一律 `git config core.autocrlf false`；`.gitattributes` 防 DXF 行尾正規化；用畢刪除。`commit` 刪除欄須**逐檔 `0`**（純追加批）。
4. **含反引號之文字一律以檔案管線處理**（`恆常附款 h`）——`commit` 訊息一律 `git commit -F <file>`；**⛔ `-m`**。前例：`W-G.9-319` 之 `f54c2bf` 訊息遭 shell 命令替換吞去受詞。
5. **器之落檔**入 `verify/out/`；量測器本身入 `verify/probes/`（皆⛔ 生產碼）。
6. **新立器前**須以其受詞之**每一量測項**逐項為名查既有器（`恆常附款 k②`）；判為「復用」者一律**呼叫**既有器·**⛔ 重寫**；另置之實作一律標「**對拍造·自擬·⛔ 正典**」。
7. **器對倉外之物有依賴者**（外部工具／環境／網路），須逐項檢其可得性並於不可得時出艙 🔴；**⛔ 靜默回空**、⛔ 吞 `stderr`、⛔ 不檢 `rc`（`恆常附款 n②`·本波新立）。
8. **凡閘一律三值**：🟢 已執行且通過／🛑 未執行／🔴 已執行且破。⛔ 二值框、⛔ 空白格。
9. **⛔ 追改單一字**；單之數與實測不符者，**以實測為準並具名相異**，⛔ 改單、⛔ 改器（`恆常附款 f`／`v②`）。

## `§四`　本次之待辦

**唯一工項 ＝ 執行 `W-G.9-322` 輕量單**（其全文另行貼付）。其要旨：

| 工項 | 受詞 | 受詞檔 |
|---|---|---|
| 零 | 本單原封入倉 | `docs/orders/W-G.9-322_輕量單.md` |
| 一 | `K-9-42` 之鑄（法源二分） | `K-6` 典（末端追加） |
| 二 | `K-9-5-15` 之就地加註（⛔ 鑄號） | `K-6` 典（末端追加） |
| 三 | `自誤 450`〜`458` 之鑄（九則） | 自誤簿（末端追加） |
| 四 | 恆常附款八款射程之增補（新立 `0`·修訂 `8`） | 恆常附款簿（末端追加） |

🔒 **原單之自簽**：`SELF_SHA256 ba065db22d2f0e3735c516b8500a7da427a80dc44e75cae3ae68cfe033160ada`（受詞 ＝ 自簽列以前之全部位元組 `44402` B）；全檔 `44480` B·`395` 列·`CR` `0`·`sha256 0418350347c44a5da9dff796c7b40bce67fdc1cd9f8d74cf3811d0bb08e88a23`。**收單第一動 ＝ 以該受詞重算並逐位對拍**。
🔒 **次動 ＝ `verify/probes/probe_order_preflight.py`**；其 `P-5` 認之形為 `SELF_SHA256:` ＋ 半形空格 ＋ 裸 hash 置於檔末最末一列，原單已採之 ⇒ 應 ✅ 相符。🛑 **`P-5` ⛔ 動一字**（`恆常附款 v②`）。
🔒 **收工閘** ＝ `python verify/probes/probe_WG9270_closegate.py 458 449 .` ⇒ 須 `rc 0`。

## `§五`　CC 側之已知戒（**族·⛔ 重踩**）

| 族 | 形 | 出處 |
|---|---|---|
| **量於自身產物落地之前** | 報一個「落地後之數」時，量測時點早於自己最末一筆 `commit` | `自誤 450`（前窗二側同踩：發單側 `367`／受單側 `369`／實測 `370`） |
| **破→空→靜默跳過→印綠** | 器之依賴不可得而回空，該格既非 🟢 亦非 🛑 亦非 🔴 | `自誤 451`；`GB-88`／`91`／`92`／`96` |
| **反引號遭 shell 吞去** | `commit -m` 傳含反引號之文字 | `W-G.9-319R §七` 自捕三 |
| **間諜錄得 `0` 筆而不自知** | 回傳型別假設錯誤（`tuple` vs `dict`）致 `isinstance` 恆偽 | `W-G.9-319R §七` 自捕一 |
| **表列切片含分隔列／漏末行** | 以 `:N`–`:M` 取 payload 時把 `\|---\|` 或末款切掉 | `W-G.9-319 補令一 §五` 自捕一 |
| **沿用前批之標籤** | 器之受詞表沿用前批，括註與實判不符 | `W-G.9-319R` 閘 `5` 之具名 |

🔑 **凡自捕，一律於報告內具名·⛔ 靜默修正**；已入 `commit` 訊息者**⛔ amend、⛔ 改寫歷史**，其完整逐字以報告承載（倉內報告 ＝ 唯一本體）。

## `§六`　舊窗 CC 之末端追加（**本節由舊窗 CC 於換窗前補**·見本補令 工項六）

（此節於落檔時留為空節題；由舊窗 CC 以嚴格末端追加填入。）

## `§七`　阻塞與已裁未落地（⛔ 採信·須自倉現查）

**未解除之阻塞**：`GB-168`（結構閘之破·其 `(b)` 式之形相異一項之裁**候發單側**）／`GB-169`（鏈頭 `W_0` 分判）／`GB-170`（強制抵費地裡側界線取錯線·號 `W-G.9-309` 保留）／`GB-171`（量測軸之別·二軸孰為正典**尚未問 KL**）／`GB-138` 處置 ③。
**已裁未落地**：`K-9-41 ②`〜`⑥`／`K-9-30`〜`K-9-37`／`K-6-A2`（街角首宗寬深雙檢）／`K-6-B`（街角四階段分配）／七級調配丙類六軸／泛用化波／`K-9-12-b`（第 2 宗與宗地層之最小矩形受測·現行仍走 `parcel_min_width_n14`）。
**保留之號**：`W-G.9-320`（階段 `b` 三造）／`W-G.9-309`（`GB-170` 修批）——**⛔ 取**。次可取之號 ＝ `W-G.9-323`。

🛑 **本次⛔ 及於**上開任一項之解除、收窄或修法主張。

### `§六-1`　舊窗 CC 之末端追加（`W-G.9-322` 補令一 工項六·⛔ 改上文一字）

🛑 **本節由 `86%` 之舊窗於換窗前所補**——其受詞為**該窗自身之記憶**，新窗結構上取不得
（`恆常附款 k②` 之同族：由「新窗未見」⛔ 推「未發生」；**留白即永久遺失**）。
🔒 **本窗之身分**：worktree `.claude/worktrees/land-readjustment-trial-docs-196235`；
分支 `claude/land-readjustment-trial-docs-fb1eca`；`left-right` vs `origin/wip/s1-endpart` ＝ `0 0`。
🔒 **本節所載之六項，逐項⛔ 留白；無者具名「無」。**

---

#### 項 `1`　本窗**尚未入倉**之觀察或量測（含已跑而未落檔者）

| # | 器／法 | 值 | 未入倉之由 |
|---|---|---|---|
| `1` | `verify/probes/probe_WG9319_widths.py --keys`（探索跑） | `g_rows` 之**鍵全表 `51` 個**——含 `W(m)`／`宗地寬度(m)`／`S(m)`／`F(m)`／`Rw(%)`／`G(㎡)`／`a 面積(㎡)`／`幾何面積(㎡)`／`累積S(m)`／`平均深度(m)`／`正面長度(m)`／`街廓面積(㎡)`／`A 地價比`／`l₁ 側面尺度`／`l₂ 正面尺度`／`負擔比率`／`迭代次數`／`是否收斂`／`解法`／`cut_coords`／`暫編地號`／`原地號`／`所屬街廓`／`重劃前區段`／`推進側別`／`街角地`／`街角側別`／`第1筆街角`／`畸零地旗標`／`驗_宗序`／`驗_總判`／`驗_鏈`／`驗_rejected`／`驗_不可判輸入`／`驗_A_*` 五／`驗_B_*` 六／`驗_C_*` 六 | 係**探索用**（為定 `W` 與 `_宗地寬度` 之取值點），其輸出未落檔；**其結論已入** `W-G.9-319R §三-1` |
| `2` | scratchpad 之 `spytest.py`（診斷器·**⛔ 入倉**） | `ns["_solve_G_one"]` 於**一次 `0m 態甲` 全跑**中被呼叫 **`149`** 次（`run_corner_pk` 後已 **`52`** 次·`run_step_g` 後累計 `149`）；同跑之 `g_rows` ＝ **`56`**；`ns["_solve_G_one"] is spy` 於 `build_pipeline`／`build_param_table`／`run_corner_pk`／`run_step_g` **四點後皆 `True`** | 其唯一用途 ＝ 查 `自誤` 級之量測器紅（間諜錄得 `0` 筆之真因）；**該診斷之結論已入** `W-G.9-319R §七` 自捕一 |
| `3` | `git ls-tree -r <rev> \| grep -cE '^verify/.+/.+\.py$'` 之逐態值 | `be54799` ＝ **`356`**／`c286a79` ＝ **`357`**／`d9df12b` ＝ **`366`**／`4c5a143` ＝ **`369`**／**本補令二 `commit` 落地後 ＝ `370`**（發單側 `§二` 項 `2` 所載者） | 逐態值僅部分入報告（`357`／`369` 已入）；**全序列僅本窗知悉** |
| `4` | `.gitignore` 之忽略族逐列 ＋ `git check-ignore -v` 之逐檔輸出 | 本窗所有落檔（`WG9318p4_*`／`WG9318p5_*`／`WG9318p6_*`／`WG9319_*`／`WG9319b_*`／`WG9321_*`）**皆⛔ 在忽略族**（空輸出）；判別力[必命中] `verify/out/got_XXXX.log` ⇒ `.gitignore:30` | 逐批之結論已入各報告；**逐檔之原始輸出未落檔** |

---

#### 項 `2`　本窗之**自捕**（**`13` 則**·已修正者亦列）

🔒 **其逐則全文已入倉** ＝ `docs/reports/W-G.9-321R_CC交接文.md` `§三`（表列 `13` 列·含批次／形／攔點）。
🛑 **本項⛔ 重述**（`恆常附款 ab②`：可由索引取得者⛔ 以文字重寫）；此處只補**該表所未載之三事**：

| # | 補 |
|---|---|
| `1` | **第 `10` 則（違 `恆常附款 h`）已入 `commit f54c2bf` 之訊息**——其受詞 token `` `0m 態乙 R4 628-1(1)` `` 遭 shell 命令替換**吞去**，該列現讀為「`[必相符] 不成立——`」。🛑 **⛔ amend、⛔ 改寫歷史**；其完整逐字在 `W-G.9-319R §三-4`。**此為倉內 `commit` 訊息之永久缺損**，新窗讀該 `commit` 時⛔ 誤為「受詞未載」。 |
| `2` | **攔點之分佈**：`13` 則中，由**器之自證閘**擋下者 `2` 則（對齊自證閘）／由**對照組紅**擋下者 `3` 則／由 **shell 或 guard 之錯誤提示**擋下者 `2` 則／由**落檔後目視抽查**擋下者 `2` 則／由**逐格對拍**擋下者 `1` 則／其餘 `3` 則由當批之機驗。⇒ 🔑 **無一則由「事後覆核」擋下**——皆在出艙前。 |
| `3` | **本窗（`W-G.9-322` 補令一）之自捕 ＝ 無**（至本節落地時）。 |

---

#### 項 `3`　本窗對**發單側單載之數**所提之相異（**二值並列·以實測為準**·⛔ 追改單）

| # | 批 | 受詞 | 單載 | **CC 實測** | 已報？ |
|---|---|---|---|---|---|
| `1` | 補令四 | `GB` 相異 | `169` | **六框皆⛔ 復現**（`96`／`134`／`174`／`175`／`176` 等）；`MAX` `171` 相符 | 已報（`R5 §一-1`）·後由補令五 `裁 2` 指出倉內已有該框之器 |
| `2` | 補令四 | 恆常附款款數 | `23` → `25` | **`25` → `27`** | 已報（`R5 §二-1`） |
| `3` | 補令五 | 恆常附款款數 | `23` → `25`／`§三` 載 `25` | **`25` → `27`** | 已報（`R6 §二-1`） |
| `4` | 補令五 | 書寫形之種數 | **`2`** 種（並令「二形並取」） | **`4`** 形（二形並取將漏 `12` 款） | 已報（`R6 §二-2`）·後由補令六 `裁 2` **全部撤回**並裁「⛔ 釘定其數」 |
| `5` | `319` | 子層 `*.py`（判別力造） | `356` | **`357`**（`356` 係 `be54799` 之值） | 已報（`319R §二-4`）·其形即 `自誤 445`／`446` |
| `6` | `319` | `P-5` 之 `SELF_SHA256` | 🔴 不符 | **框之別**·傳輸保真未破（原口徑重算逐位相符） | 已報（`319R §二-2`）·後由補令一 `裁 4` 裁「改單之形·⛔ 擴器之框」 |
| `7` | `319` 補令一 | 收工閘 `6` 之主線期值 | `4037570…` | **`51a641d…`**（單載係上批快轉**前**之態） | 已報（`319R` 補令一收工閘節） |
| `8` | `321` | 子層 `*.py` | `367` | **`369`**（列舉本批所增 `3` 支器） | 已報（`321R §二-1`／`§七` 三） |
| `9` | **本補令** | 恆常附款 受掃／🟢／⚪ | `§五` 表載 **`28`／`16`／`12`** | **`28`／`18`／`10`** | 🔑 **本節即其報**——單**自身之 `§四` 末已具名此更正**（「本節首版載 `16`／`12`……改為 `18`／`10`」），惟 `§五` 之驗收表**未同步**。⛔ 追改單一字；受單側依 `恆常附款 f` **以列舉為準**：🟢 ＝ `a d e f g h k l n p r t u v w x aa ab`（`18`）／⚪ ＝ `b c i j m o q s y z`（`10`）·`18 + 10 = 28`·重疊 `[]`。 |

🔑 **九項之共通形**：**單載之數繫他態或未經本批自算** ⇒ 其族即 `自誤 441`／`445`／`446`／`447`。
🛑 **無一項經 CC 追改單**；皆以**具名相異 ＋ 以實測為準**承載。

---

#### 項 `4`　本窗**未辦或部分辦**之單內工項

| # | 單 | 工項 | 狀態 | 由 |
|---|---|---|---|---|
| `1` | `W-G.9-319` 原單 | `§二-2` **階段 `b`（替身）** | 🛑 **⛔ 辦** | **停一 成立**（`[必相符]` 造不成立）⇒ 依單 `§二-1` 明文停機上呈·⛔ 追改·⛔ 改取法。後由補令一 `裁 3` **重設為三造**（`α`／`β`／`γ`），其執行**另單 `W-G.9-320`·須新窗** |
| `2` | `W-G.9-318` 補令六 | **工項零′（主線快轉）** | 🟢 **首次未辦·重試辦畢** | 首次執行遭 CC 側 auto-mode 分類器擋（`Merge Without Review`）⇒ 依 `§五-1′` 停機上呈·⛔ 加力·⛔ 改法；經人工核可後**原封重試**（指令一字不差）而成。二次快轉之登記見 `W-G.9-318R7 §九` |
| `3` | `W-G.9-322` **原單** | 工項零〜四之**全部** | 🛑 **⛔ 於本窗辦** | 本補令 `§零` 序 `4` 明令其於**新窗**；本窗**未收到原單全文**（其 `44480` B·`395` 列之逐字**未到達本窗**） |

🛑 **項 `3` 之要**：本窗**⛔ 持有** `W-G.9-322` 原單之任何逐字——其 `SELF_SHA256`／bytes／列數皆
**僅見於本補令 `§一` payload 之轉述**。新窗收單時之第一動（以該受詞重算並逐位對拍）**⛔ 由本窗代辦**。

---

#### 項 `5`　本窗所立而**未入倉**之器（**逐支具名路徑與 `sha256`**·⛔ 逕行入倉·候單）

🔒 **位置** ＝ 本窗之 scratchpad（`%TEMP%\claude\…\709d79e8-…\scratchpad`）·**⛔ 在倉內**·
**⛔ 在 `.gitignore` 之忽略族**（其根本不在倉樹內）。

**（甲）可執行之器（`.py`·`7` 支）**

| # | 檔 | bytes | `sha256`（前 `32`） | 受詞 |
|---|---|---|---|---|
| `1` | `s0_ingest.py` | `1171` | `abc6d350471509a7a84afe11e891b17e` | 補令四之原封入倉與逐位驗 |
| `2` | `s0_ingest5.py` | `1072` | `0be4500ea341fb8e801e8298c7a3e0cc` | 補令五之同 |
| `3` | `s0_ingest6.py` | `1066` | `1ff85de46a5c098800e7071cb628ac06` | 補令六之同 |
| `4` | `s0_ingest319.py` | `1058` | `4b13a907bae84202dbfc693be4ca8a07` | `W-G.9-319` 之同 |
| `5` | `s0_ingest319b.py` | `1061` | `b2078618cf9dfed5896de31a15a5429c` | `319` 補令一之同 |
| `6` | `s0_ingest321.py` | `1617` | `87fe5e473587e277436e875c159d2297` | `W-G.9-321` 之同（含器之 `sha256` 對拍） |
| `7` | **`spytest.py`** | `1984` | `c780d3e487a715c92990120aab05530b` | 🔑 **診斷器**：查 `ns["_solve_G_one"]` 之間諜為何未觸發（其結論 ＝ 自捕一） |

🔑 **其中 `1`〜`6` 係同一形之六份**（原封入倉之落檔與逐位驗）⇒ **候發單側裁是否併為一支入倉**；
`7` 係一次性診斷器，**其結論已入倉**（`319R §七` 自捕一）。

**（乙）payload 與 `commit` 訊息之落檔（`.md`／`.txt`·`29` 支）**
其內容**皆已入倉**（為各批之末端追加 payload 與 `commit` 訊息之來源）；
逐支之 `sha256` **未入倉**。🔒 **其中 payload 類者，其入倉後之逐位相符已由各批之四造機驗坐實**
（前綴逐位／payload 逐位／bytes 算式／期末 `sha256`）⇒ **⛔ 再入倉之必要**。

---

#### 項 `6`　本窗所知而**倉內未載**之事實（⛔ 以「發單側應已知」為由略之）

| # | 事實 | 其對新窗之意義 |
|---|---|---|
| `1` | 🔑 **CC 側之 auto-mode 權限分類器會擋「推主線」**——其理由逐字 `Merge Without Review`。**首次必擋**；經**人工核可**後以**原封重試**（指令一字不差）方成。**⛔ 加力（`--force`）、⛔ 改目標值、⛔ 改分支名、⛔ 另闢傳輸途徑**。 | 新窗若須推主線，**須預期首次被擋**並依 `§五-1′` 型之停機款上呈·⛔ 自行繞過 |
| `2` | 🔑 **`verify/tools/wg9237_heredoc_guard.py` 於本窗實擋 `4` 次**（`GB-143` 補款二）——凡 `bash` 命令含 heredoc／herestring 即 `rc 1`。⇒ **一切腳本一律以 `Write` 落檔後以<u>路徑</u>呼叫**；`git commit` 一律 `-F <檔>`。 | 新窗之第一支腳本即會踩；**⛔ 以「只是小腳本」為由用 heredoc** |
| `3` | 🔑 **Python `subprocess.run(..., shell=True)` 於 Windows 走 `cmd.exe`** ⇒ `sed`／`sort`／`uniq`／`tail`／`sha256sum` 等 POSIX 具**不可得**，管線**靜默回空**（`capture_output` 吞其 `stderr`）。本窗於 `probe_WG9321_issuer_anchor.py` 之 `項10` 實遇（`自誤 451` 之族·`恆常附款 n②` 之受詞）。 | 新窗跑**任何含管線之既有器**皆須先檢其空輸出係**器紅**抑或**受詞紅** |
| `4` | 🔑 **KL 於 `2026-09-20` 告知本窗施工窗已達 `86%`** ⇒ 本補令之由。**倉內⛔ 載此事實**。 | 新窗之容量係全新；惟**換窗本身⛔ 使任何未入倉之物移轉**——項 `1`〜`5` 即其清單 |
| `5` | **本窗之 worktree 路徑** ＝ `.claude/worktrees/land-readjustment-trial-docs-196235`（分支 `claude/land-readjustment-trial-docs-fb1eca`·`left-right` ＝ `0 0`）。發單側之器 `probe_WG9321_issuer_anchor.py` 之 `branch` 格因此與單載相異（**checkout 之別·⛔ 態之別**）。 | 新窗若亦於 worktree，**該格必再相異**；⛔ 判為態之別 |
| `6` | **`core.autocrlf` 於本倉 local ＝ `false`**（system 層為 `true`·local 覆蓋）⇒ 工作區 bytes ＝ blob bytes。**拋棄式 clone 若⛔ 設 local，將繼承 system 之 `true`** 而使 `app.py` 工作區 bytes 由 `1454937` 變 `1479398`。 | 新窗若 clone，**第一動即 `git config core.autocrlf false`**（交接文 `§三` 款 `3` 已載其令·此處補其**實測差值**） |
| `7` | **本窗所辦之六批**（`W-G.9-318` 補令四〜六／`W-G.9-319` 原單＋補令一／`W-G.9-321`）**皆已 push 且皆零生產碼**；主線由 `4037570` 推進至 `4c5a143`（**含 KL 放行之 `6` 筆生產碼 `commit`**）。 | 新窗之開工態錨**⛔ 沿用本文 `§二`**——須自倉重跑（`§六` 開場序列序 `1`／`3`） |

---

🔒 **本節之六項逐項已填·⛔ 留白**（`恆常附款 d`：受詞及於倉外之物者，須同格載其不可實查之性質）。
🛑 **本節落地後，舊窗即得換窗**（本補令 `§五` 款 `3`）。

### `§六-2`　🔴 舊窗 CC 之**自捕**：`§六-1` 項 `1` 第 `3` 列之二格為誤——**重踩 `自誤 450`**（⛔ 改上文一字）

🛑 **⛔ 追改 `§六-1` 一字**（`常規四（九）五`）——本節即其更正之載體。
🔒 **攔點 ＝ 發單側**：本文 `§二` 項 `2` 載 `370`，CC 於收工閘實測期末得 `371`，
逐態追查後方知**錯在 CC 自己**、⛔ 在發單側。

#### 一　被更正之對象（逐字）

`§六-1` 項 `1` 第 `3` 列之「值」欄，其逐字含：

> `be54799` ＝ **`356`**／`c286a79` ＝ **`357`**／`d9df12b` ＝ **`366`**／`4c5a143` ＝ **`369`**／**本補令二 `commit` 落地後 ＝ `370`**（發單側 `§二` 項 `2` 所載者）

#### 二　實測之全序列（當場逐態重跑·⛔ 沿用）

| 態 | 子層 `*.py` | 判 |
|---|---|---|
| `d9df12b` | **`366`** | 🟢 上文所載為正 |
| `3879812`（`321` 工項零＋二） | **`368`** | （上文未載） |
| `a3a4c2c`（`321` 工項一） | **`369`** | （上文未載） |
| `8a3150e`（`321` 工項三） | **`369`** | （上文未載） |
| **`4c5a143`**（`321` `§七` 收工閘） | **`370`** | 🔴 **上文載 `369`·誤** |
| `91a0d10`（本補令 工項五） | **`371`** | — |
| `c4139f0`（本補令 工項六） | **`371`** | 🔴 **上文載「本補令二 `commit` 落地後 ＝ `370`」·誤** |

🔒 **差之逐檔**：`8a3150e → 4c5a143` 新增 `verify/probes/probe_WG9321_closing.py`（`+1` ⇒ `370`）；
`4c5a143 → 91a0d10` 新增 `verify/probes/probe_WG9322b_payload.py`（`+1` ⇒ `371`）。

#### 三　根因 ＝ **重踩 `自誤 450`**（量於自身產物落地之前）

🔴 `W-G.9-321R` `§七` 三所出艙之「CC 實測 **`369`**」，其**量測時點 ＝ `8a3150e`**
——**早於該批自身之最末一筆 `commit` `4c5a143`**，而該 `commit` 正**新增了第 `4` 支器**
（`probe_WG9321_closing.py`·即承載該 `§七` 之 `commit`）。
⇒ 該批之**真期末 ＝ `370`**，其所列舉之「本批所增 `3` 支」應為 **`4` 支**。
🛑 **本文 `§二` 項 `2` 之警語逐字**：「**量測時點須在<u>自身最末一筆 commit 落地之後</u>**
（`自誤 450` 之族·前窗於此連踩二側）」——**CC 於讀該警語<u>之後</u>，於 `§六-1` 仍再踩一次**。

🔒 **其⛔ 影響任何 operative 判準**：該數為**判別力[必不命中]之造**，`369`／`370`／`371` 皆 `> 0`
且皆⛔ 落入生產碼 `34` 檔 ⇒ 其判別力之結論⛔ 變。**惟其為錨表之格，須實**（`恆常附款 w②①`）。

🛑 **⛔ 追改 `W-G.9-321R` 一字**（`常規四（九）五`）——該報告之 `369` 連同其所宣告之量測時點
（`8a3150e`）**一字不動**；本節即其更正。

#### 四　🔑 攔法之候（**⛔ 自立款·候發單側**）

本形已有 `自誤 450` 與本文 `§二` 項 `2` 之警語，而**警語⛔ 足以攔之**——CC 讀之而仍踩。
⇒ 其缺者為**機械閘**：凡出艙「本批所增之 N」或任何**期末**之數者，
其量測**須以該批之最末一筆 `commit` 為 `rev`**，且該 `rev` **須於出艙時同格載明並與 `HEAD` 對拍**；
`rev ≠ HEAD` 即🔴。**是否立為款，繫發單側。**

🔒 **本節之數皆繫 `c4139f009a16defdbc0651e32a68fb5150086d65`**；
**本節自身之 `commit` 落地後，子層 `*.py` 將再增 `1`**（`verify/probes/probe_WG9322b_closing.py`）
**⇒ `372`**（`GB-132` 族之自指增長·**已預先具名**·⛔ 追改本表）。
