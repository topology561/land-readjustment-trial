# CC 新 session 交接文：**`W-G.9` 波**（`W-G.9-1`〜`W-G.9-4` 已落地）

**產出**：CC／2026-08-11　**倉**：`github.com/topology561/land-readjustment-trial`
**分支**：`wip/s1-endpart`　**交接時 HEAD**：`edb8324`（`origin` 實查同·工作樹異動 **0**）
**前一份交接文**：`docs/reports/W-G.8_D-2b波_新session交接文_D2b21-27.md`（**仍在**）

> # 🔴🔴 最先讀這一段
>
> **`W-G.9-4` 是本波<u>第一個動生產碼</u>之批，但生產行為<u>零變化</u>。**
> 現況為 **expand-contract 之 expand 側**：**路已加、尚未換**。
>
> - ✅ **已加**：`app.py` module 級 `k956_W_from_mp`（`W` 之**單一產生者**）；
>   `verify/stepg_pipeline.py` 之 `_mp_base_W0` **已改純委派**。
> - 🔴 **尚未換**：`app.py` 之**族②③**（`_W_prev_*`、`_select_pool_slot` 之 `b`）
>   **仍取舊式** `buf · _cos_dn`；新值**只寫診斷**。
> - 🔴 **切換有土地後果**：二式之差 ＝ `W_0`，實測 `−1.693924`〜`+1.691802`，
>   **`|W_0| < 1e-9` 之格 ＝ `0`／`16`** ⇒ **每一格都會變**。
>
> ⇒ ⛔ **不得以「跑完了／全綠」當作可切換**——切換屬 `W-G.9-5`，且須先解 §二之待裁。

> ⚠️ **本文所有數字皆為撰寫當下現查**；引用前**重跑該指令**。
> 🔒 **本波新立三戒**（`W-G.9-3`／`W-G.9-4`），⛔ 下一批須逐條自檢並正面列出「已套用」：
> ① 否定性宣稱須同附【目錄射程】【字樣射程】【連接方式】，字樣須語意窮舉且**⛔ 不預設符號名**；
> ② 凡書「正典」須就地標**權威序層級**（`sed -n '4p' CLAUDE.md` ⇒ 共 **4 級**）；
> ③ 出艙任何數之前，把產生它的指令**於落筆當次重跑**、指令與輸出並列。

---

## 〇、開工前必做

```bash
git fetch origin wip/s1-endpart && git checkout wip/s1-endpart && git pull
git rev-parse --short HEAD                                       # 應 ≥ edb8324
git ls-remote --heads origin | grep s1-endpart                   # ⛔ 須與 HEAD 同
python -c "import shapely; print('shapely', shapely.__version__, '| GEOS', shapely.geos_version)"
sed -n '4p' CLAUDE.md                                            # 權威序四級·⛔ 先讀
grep -c "^| \*\*GB-" docs/reports/W-G.4_泛用阻塞項登記表.md        # 現值 60
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -1   # 節號以現查為準
grep -oE "^### 🔒 K-9-5-[0-9]+" docs/rulings/K-6_街角地分配程序與可分配判準.md | sort -u
grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_D2b1切換後.txt      # 現值 22
```

⛔ **禁 `git reset --hard`**；⛔ **禁對 `verify/out/` 用 `git add -A`／`.`**；
⛔ **禁覆寫已入庫 log**。🔴 **grep 蒐證排除巢狀 worktree**：`| grep -v "^\./\.claude/worktrees/"`。
⚠️ **若你在 worktree**：主倉可能已檢出同分支 ⇒ 用 `git checkout -B <本地支> <sha>`
（⛔ 非 `reset --hard`），推送用 `git push origin HEAD:wip/s1-endpart`。
🔴 **`run_all` 約 15–20 分鐘 ⇒ 背景跑；長跑期間 ⛔ 不得改 `app.py` 與 `verify/`。**

---

## 一、現況（撰寫當下現查）

| 項 | 值 | 現查指令 |
|---|---|---|
| 分支／HEAD | `wip/s1-endpart` ／ **`edb8324`**（`origin` 同·異動 0） | 見 §〇 |
| 環境 | `shapely 2.1.2 \| GEOS (3, 13, 1)` | 見 §〇 |
| 登記表 | **60 列**（本波新開 `GB-59`／`GB-60`） | 見 §〇 |
| 失敗考古末節 | **64**（本波新增 **63**／**64**） | 見 §〇 |
| 正典 `K-9-5-*` | `K-9-5-1`〜**`K-9-5-11`** 已占用 ⇒ 新則取 `K-9-5-12` | 見 §〇 |
| 期望 FAIL 名單 | **22** 名目·**本波一列未改** | 見 §〇 |
| `run_all` | **PASS 12／FAIL 22**（`verify/out/WG94_runall.log`·319 行） | `diff` 見 §四 |

### 1-1 逐批（**全部已 push**）

| 批 | commit | 生產碼 | 要旨 |
|---|---|---|---|
| `W-G.9-1` | `2e573e9`／`b64fa28`／`5056ae3` | ⛔ 零 | `buf` 度量識別（`P1`〜`P5` 全成立）＋ `GB-59`／`GB-60` 登記 ＋ `GB-53` 補引據 |
| `W-G.9-2` | `d87728f`／`c1a9ba3`／`185342f` | ⛔ 零 | 族②③④ 對 `K-9-5-6` 之合致性判定；🔴 **其 `P1` 判定<u>錯誤</u>**（見 §三） |
| `W-G.9-3` | `c474b9c`／`13c5f24`／`6ab9e86` | ⛔ 零 | `P1` 誤判撤回 ＋ `MP` 定義指向入正典 ＋ `GB-60` 改三選項 ＋ 考古節 64 |
| **`W-G.9-4`** | `7a3f9d3`／**`2e6afc6`（🔴 有碼）**／`edb8324` | ✅ **有** | **expand**：單一 `W` 產生者 ＋ `stepg` 委派 ＋ `app` 併行診斷（**行為零變化**） |

---

## 二、🛑 待裁／停機（**開工前必讀**）

### 2-1 🛑 `W-G.9-5`（contract 側）之切換——**有土地後果·尚未提請域裁**

**要切換者**：`app.py` 之族②③ ——
`_W_prev_left/right`（`grep -n "_W_prev_left = (_left_buffer_S \* _cos_dn)" app.py`）與
`_select_pool_slot` 之 `b`（`grep -n "'b': _left_buffer_S \* _cos_dn" app.py`），
由 `buf · _cos_dn` 改取 `k956_W_from_mp(...)`。

🔴 **土地後果非零**：`W-G.9-2` 已解析定式並經 `W-G.9-4` 實測坐實——
**`舊式 ＝ 新式 − W_0`**（恆等式殘差極大 `1.629e-10`），
而 `W_0` 實測 `−1.693924`〜`+1.691802`、**`|W_0| < 1e-9` 之格 ＝ `0`／`16`**
⇒ **十六格全數會變**。

🔴 **切換時<u>必須指明口徑</u>**（`W-G.9-4` §2-3·事前登記未涵蓋之發現）：
`stepg:822` 右組傳 `_dhr_o = −d_hat` ⇒ `â_定向` **翻號**
⇒ **生產口徑下右側 `W_0` 為登記表值之負數**（左側同號同值）。
⛔ 未指明口徑即切換 ⇒ **右側符號會翻**。

⚠️ 切換必使 `run_all` 基準過期 ⇒ 須與**重烤時點**協調（見 2-4）。

### 2-2 🛑 `GB-60`：**三選項·需前置偵察**（`W-G.9-3` 已改標）

缺 `alloc_dir` 時：**①** 停機／**②** 維持退 `1.0` 但把未宣告後果補入警示／
**③** 限縮 `_cos_dn` 之射程，使警示「側街負擔以 0 計」成為**完整**真實（不停機亦不靜默）。
🔴 **列舉·非窮舉**。
**背景**：`P5` 已證警示所載為真（`allocation_dir=None` ⇒ `W=0`、`Rw_pct=0`），
但同一路徑另令 `_cos_dn = 1.0`，而其**第二消費者**
（`grep -n "_pw = float(_res.get('S', 0.0)) \* _cos_dn" app.py` ⇒ 1 命中）為**宗地寬度**·供畸零地判去留
⇒ 警示**未提之** ⇒ 該後果**靜默**；量級**高估 `0.10%`〜`0.43%`**。

### 2-3 🛑 `GB-58`／`GB-59`：併入 `W-G.9`（KL 裁 2026-08-10）

- `GB-58`：強制抵費地帶未隨 `D-2` 切換至 ∥SIDELINE ⇒ **同一塊地兩種畫法**。
- `GB-59`：`GB-58` 之修正**必經** `GB-47` 之四處殘存（族②③），二者觸發條件同一（分歧 0／16）。
  ⚠️ **具名覆蓋缺口**：「`forced` ∧ `SIDE ∥ ALLOC`」於本案母體**零覆蓋** ⇒ 該子條件**未被檢定**。
- `GB-53`：`_end_band` 仍以 ⊥ALLOC 垂距構帶（與 `K-9-5-9`【裁】4 相違）·**需域裁**。

### 2-4 🛑 `D-3` 重烤：**仍受 `K-9-5-10`【裁】1／2 拘束**

**【裁】1**：`D-3` 重烤**等 `R1` 之卡點修好之後才做**——該卡點 ＝ 結構閘「理論＝實跑」之**另案本身**，
⛔ **不得解為 `GB-54`**（`GB-54` 之觸發點 `抵費地計算失敗` 現行**未發動**·log 命中 0）。
**【裁】2**：已過期之頂層 **7 份**採**丙案：由 KL 以 UI 重跑**（⛔ 非甲、⛔ 非乙），時點同【裁】1。

### 2-5 🛑 `W-G.9 §2-1` 之判讀：**地基不成立、結論未定·禁令未解**

三張示意圖雖已入倉（`docs/specs/figures/第2宗示意圖_*.jpg`），
🔒 **入倉不等於重測**——⛔ 圖到位**不解除**引用禁令。

---

## 三、🔴 已作廢·⛔ 不得引用之陳述

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| `W-G.9-2_定式批報告.md` 紅框／`:38`／`:134` | 「`P1` 破·`MP` 於正典從未定義」 | 🔴 **誤判**——`MP` 定義**存在**於權威序 ②③③′（`CLAUDE.md:328`／v3 `:189` 之 `M`／`docs/specs/` 之 `mp`）。成因＝**目錄與字樣雙重射程不足**。已於 `W-G.9-3` 具名更正（**原句一字未刪**） |
| `W-G.9-2` 之 `GB-60`「二擇一」 | 選項數 | **三選項**（`W-G.9-3` §4） |
| `D-2b-30` 報告之 **D 欄**（面積差·三格合計 `224.97 ㎡`） | 土地後果 | ⛔ **非土地後果**（`D-2b-31` §2 已具名作廢）；`A`／`B`／`C`／`E` 欄不受影響 |
| `W-G.9-1` 施工單 §3-1 之 `33` | 消費者窮舉之總數 | **31**（二選項式）；`33` 係四選項式之輸出（考古節 63） |

---

## 四、生產碼現況（本波唯一有碼之批 ＝ `2e6afc6`）

| 落點（自癒 grep） | 現況 |
|---|---|
| `grep -n "^def k956_W_from_mp" app.py` ⇒ **1** | **`W` 之單一產生者**（module 級·`K-9-5-6` intrinsic 直量·權威序第 1 級） |
| `grep -n "_k956_W_from_mp(_bp0" verify/stepg_pipeline.py` ⇒ **1** | `_mp_base_W0` **已純委派**（語意不變·簽章不變） |
| `grep -c "_W_prev_left = (_left_buffer_S \* _cos_dn)" app.py` ⇒ **1** | 🔴 **族② 仍取舊式**（⛔ 未切換） |
| `grep -c "'b': _left_buffer_S \* _cos_dn" app.py` ⇒ **1** | 🔴 **族③ 仍取舊式**（⛔ 未切換） |

🔒 **`app.py` 之 diff 恰 2 個 hunk，皆純插入**（`-8731,0` 與 `-19572,0`）⇒ **刪除欄 0**。
🔒 **六個關鍵生產函式函式體 md5 前後相同**：`solve_G_binary`／`iterate_G_S`／
`_corner_buffer_S`／`_build_corner_range_v3`／`_block_strip`／`_strip_axis`。
🔒 **`E4` 之對拍**：`WG94_runall.log` 與基準 `D2b24_runall.log` **皆 319 行**，
非路徑差異 **0 行**；正規化 worktree 路徑與 `stepg` traceback 行號後 **`diff rc=0`·殘差 0**。

---

## 五、次步（**KL 未指定時之預設順位**）

| 序 | 事 | 前置 |
|---|---|---|
| 1 | **`W-G.9-5`（contract）：切換族②③** | 🛑 待域裁（土地後果 16/16 全變·且須指明口徑） |
| 2 | **`GB-60` 三選項之前置偵察** | 🛑 待裁 |
| 3 | **`GB-58`／`GB-59` 之實作**（∥SIDELINE 多邊形 ＋ 族②③④ 同步） | 🛑 待裁·⚠️ 覆蓋缺口未檢定 |
| 4 | `W-G.9 §2-1` 之**重測** | 🛑 禁令未解 |
| 5 | `D-3` 重烤 | 🛑 等 `R1` 結構閘另案修好 |

---

## 六、🔑 本波最貴的六條（**給下一手**）

1. **`rc=1` 不構成「命題為偽」**——它只構成「**該式**沒找到」。
   否定性宣稱須同附【目錄射程】【字樣射程】【連接方式】，且字樣**⛔ 不預設符號名**
   （`MP`／`mp`／`M` 是同一個概念的三種寫法·考古 **64**）。
2. **「正典」是四級之總稱**——只查 `docs/rulings/` 就寫「正典無此規定」＝射程不足。
3. **出艙之數與寫進文件之指令，可能是兩次不同的執行**——唯一能抓到它的動作是**重跑**
   （`33` vs `31`·考古 **63**）。
4. **委派之後，拿被委派者當參照即恆真**——`E1` 之參照必須是**變更前**之原式
   （本波以「閘 T：對 `git show` 實文比對 6 條關鍵行」自證轉錄無誤）。
5. **缺鍵之 `.get(k, 預設)` 會讓預設值冒充量測結果**——先印**鍵集**自證，再取值
   （`Rw` 根本不在回傳字典裡，我卻拿 `1.0` 判了 `P5`）。
6. 🔴 **「立戒者在下一批最容易違反自己的戒」在本波出現<u>同批內</u>實例**——
   `W-G.9-3` 之更正批，**首版探針**仍用我剛判定為錯的字樣（只找 `mp`、漏 `M`）。
   ⇒ ⛔ 不得以「已知此戒」為由略過自檢。

---

## 七、收官前置（不變）

`git ls-remote --heads origin | grep s1-endpart` 實查含該 commit——**不綠不推、不推不報**。
⚠️ **四份期望 FAIL 名單並存**——現行者為 `D2b1切換後`（**22** 名目），其餘刻意保留供考古、⛔ 勿誤用。
⚠️ **探針拒絕覆寫**：跑複驗須 `WV_OUT_NAME=<新檔名>`（`_resolve_out` 守衛·fail-fast）。
⚠️ **`verify/out/` ⛔ 禁 `git add -A`／`.`**——逐檔 `git add`，commit 前跑 `git show --name-status` 對照。
