# 📐 `W-G.9-168R`　`VR-074` 落地之**前置量測 ＋ 期初錨 ＋ 登記**

**受單**：`W-G.9-168`（`SELF_SHA256 = 9147263d3e70a47087d70aa2e29aad7e58d52e6c31b0aa0c224344796d13a5f1`·CC 覆算相符·四反引號界線 `6` 列 ＝ `3` 塊）
**基座**：`a98a99f5bad8c415e19042c9dd68f30d681c2d8f`
**性質**：只讀量測 ＋ 純新增／純附加（⛔ 零生產碼·⛔ 未落地 `VR-074`·⛔ 未改 `app.py`／`verify/*.py` 既有一字）

## 📊 §零　結論（先講）

| # | 事 | 值 |
|---|---|---|
| `1` | 🔴 **`M-A` 判準 `A-5` 二項齊備** | ①`ghost3` 命中逐次 **`0`**（`8`／`8` 次）②`_name` 二態逐次**相同** ⇒ **(A) ≡ (B) 於該點** ⇒ 🔒 **`167R` 之 `16` 格預測集合<u>可轉用於 `169`</u>**（`S-2` **未觸**） |
| `2` | `M-B` 期初全量 log | `verify/out/runall_a98a99f_full.log`（`506` 列／`46006` B／`sha256 69e1eee8…5c5a21`·`stderr` `0` B·`EXIT=1`·`RESULT: FAIL`） |
| `3` | 🔴 `B-3`（`X-5`） | 未正規化相異 **`24`** 列（⛔ 非 `0`）——**`24`／`24` 全數歸因於 clone 路徑字串**（`w163b` vs `w168a`），**B 類（無法歸因）＝ `0`** ⇒ **`S-4` 未觸** |
| `4` | `M-C` `C-2` 外部錨 | 四閘全集 **`68`／`68`／`60`／`60`** ⇒ 與基座 log「本閘違規總計」**逐閘相符** |
| `5` | 🔴 `C-3`（`X-8`） | 前 `12` 列**逐位**不符；**內容與序 `48`／`48` 全同**（差之唯一來源 ＝ `got` 之 `!r` 呈現·見 `§四` `C-3`）⇒ 單之 `C-1` 與 `C-3` **互斥**（常規二） |
| `6` | ✅ `C-4`（`X-9`） | `169` 之 log 應變更列 ＝ **`6`**（顯示列 `2` ＋ 截斷通知列 `4`）**與發單側預測逐字相符** |
| `7` | `§五` 十一項 | 符 **`8`** ／ 不符 **`2`**（`X-5`／`X-8`）／不可判 **`1`**（`X-10`）⇒ 和 ＝ `11` |

---

## §一　開工閘（`S-1`）

| 錨 | 期望 | 實測 | |
|---|---|---|---|
| `git ls-remote origin refs/heads/wip/s1-endpart` | `a98a99f5bad8c415e19042c9dd68f30d681c2d8f` | 同 | ✅ |
| `HEAD` 全 `40` 碼 | 同上 | 同 | ✅ |
| `app.py` blob（期初＝期末） | `f285b91c9e5f97f13dd74508e1a403833bc75cb3` | 同 | ✅ |
| 工作樹 | 淨 | `git status --porcelain` ＝ `0` 列 | ✅ |

**clone**（皆拋棄式）：`/c/w168a`（**長跑**·`C:\w168a` ＝ `8` 字元·與期初 log 之產生路徑 `C:\w163b` **等長**）／`/c/w168b`（`M-A` ＋ 編輯 ＋ 入倉）。**一 clone 一支長跑** ✅。

### 🩸 CC 自捕（一）：首次長跑之 clone 路徑**不等長**（已作廢重跑）

首跑用 `/c/w168`（`C:\w168` ＝ **`7`** 字元）⇒ 違本倉家法「路徑須與期初 log 之產生路徑**等長**」。
實測其 log ＝ `45996` B（期初 `46006` B·差 `10`），未正規化 LCS 相異 **`28`** 列。
⇒ **整份作廢**，改於 `/c/w168a`（`8` 字元）重跑；重跑之 log ＝ **`46006` B**（**與期初逐位同長**）、相異降為 `24`（＝純 clone 名字串）。
🔒 **⛔ 未沿用首跑之任何數字**；`/c/w168` 已刪除。

---

## §二　`M-A`　`app:k6step0/_ordered` 之補量

### `A-1`　逐 tag 執行次數（`WV_PROJ_POP_COUNT` ＝ scratchpad 之檔·⛔ 不落倉內）

一趟 `run_all` 共記 **`260`** 筆／相異 tag **`7`**；未宣告而出現之 tag ＝ **`[]`**。

| tag | kind | source | filter | **執行次數** |
|---|---|---|---|---|
| `app:v2/pre_seq` | POP_SYNC | BUILD_LAYER | `passthrough` | **`18`** |
| `app:main/v2_caller` | POP_SYNC | BUILD_LAYER | `stage1` | **`0`** |
| `stepg:v2_caller` | POP_SYNC | BUILD_LAYER | `stage1` | **`18`** |
| `app:main/_rank_by_tpid` | POP_SYNC | BUILD_LAYER | `identity` | **`0`** |
| `sp:_rank_by_tpid` | POP_SYNC | BUILD_LAYER | `identity` | **`102`** |
| `stepg:_proj_rank` | POP_SYNC | BUILD_LAYER | `stage1` | **`18`** |
| `wf_f0:_proj_order` | ORDER_INVARIANCE | BUILD_LAYER | `no_ghost` | **`0`** |
| `wf_f2:_proj_order` | ORDER_INVARIANCE | BUILD_LAYER | `no_ghost` | **`0`** |
| `wf_f3:_proj_order` | ORDER_INVARIANCE | BUILD_LAYER | `no_ghost` | **`0`** |
| `wf_f4:_proj_order` | ORDER_INVARIANCE | BUILD_LAYER | `no_ghost` | **`0`** |
| `wf_f1:_order` | ORDER_INVARIANCE | RESHAPE | `pseudo_of` | **`0`** |
| `wf_f4:_order_fb` | ORDER_INVARIANCE | RESHAPE | `pseudo_of` | **`2`** |
| `wf_f4:_order` | ORDER_INVARIANCE | RESHAPE | `pseudo_of` | **`22`** |
| 🔴 `app:k6step0/_ordered` | NAME_DERIVATION | TEMP_LAYER | `group_members` | **`80`** |

🛑 **次數為 `0` 者 `7` 個之逐點成因**（⛔ 非「有跑到」一語帶過）：

| tag | 成因（具名） |
|---|---|
| `app:main/v2_caller`／`app:main/_rank_by_tpid` | 二者皆落於 `app.py` 之 `main()` 內；`CLAUDE.md` 逐字「**`main()` 內之敘述從不被 `run_all` 執行**」（harvest 以 AST 跳過 UI、harness 走 `verify/stepg_pipeline.run_step_g`）⇒ **結構性不可達** |
| `wf_f0:_proj_order`／`wf_f2`／`wf_f3`／`wf_f4:_proj_order`／`wf_f1:_order` | `run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止 ⇒ `wf_f0`〜`f4` **主路徑從未執行**（`W-G.9-164 §七` 已載之 harness 限制）⇒ **連坐不可達** |

🔒 **與既有錨相符**：`W-G.9-164 §七` 逐字「`14` tag 有 **`7`** 個為 `0`（`main()` 內 `2` ／ trunk A 中止致 `wf_*` 主路徑 `5`）」⇒ **本批逐 tag 復現**（`2 + 5 = 7`）。
🔒 `wf_f4:_order_fb` ＝ `2`／`wf_f4:_order` ＝ `22` 係由 **fixture subprocess** 驅動（同上出處）。

### `A-2`／`A-3`　逐次記錄（現況態·**`8` 次**）

| 序 | blk | `len(_mem)` | **`ghost3` 命中** | `_mem` 之暫編地號 | `base ＝ TEMP_LAYER(blk)` 之 len／其 `ghost3` | `_ordered[0]` ⇒ **`_name`** |
|---|---|---|---|---|---|---|
| `1` | `R2` | `2` | **`0`** | `628-40(1)`／`628-43(1)` | `15` ／ `0` | `628-40(1)` ⇒ `628-40(1)+` |
| `2` | `R3` | `2` | **`0`** | `628-30(3)`／`628-45(2)` | `14` ／ `0` | `628-30(3)` ⇒ `628-30(3)+` |
| `3` | `R3` | `3` | **`0`** | `628-47(1)`／`628-48(1)`／`628-49(2)` | `14` ／ `0` | `628-47(1)` ⇒ `628-47(1)+` |
| `4` | `R3` | `2` | **`0`** | `628-28(1)`／`628-29(1)` | `14` ／ `0` | `628-29(1)` ⇒ `628-29(1)+` |
| `5` | `R5` | `3` | **`0`** | `628-21(2)`／`628-22(2)`／`628-23(2)` | `12` ／ `0` | `628-21(2)` ⇒ `628-21(2)+` |
| `6` | `R5` | `3` | **`0`** | `628-20(2)`／`628-30(1)`／`628-45(1)` | `12` ／ `0` | `628-45(1)` ⇒ `628-45(1)+` |
| `7` | `R6` | `2` | **`0`** | `628(2)`／`628-1(2)` | `11` ／ `0` | `628(2)` ⇒ `628(2)+` |
| `8` | `R6` | `3` | **`0`** | `628-21(1)`／`628-22(1)`／`628-23(1)` | `11` ／ `0` | `628-23(1)` ⇒ `628-23(1)+` |

🔒 **判準之 `ghost3` 逐字**：`str(x.get("原地號","")) == "_GHOST"` ∧ `float(x.get("G(㎡)", 0) or 0) == 0` ∧ `float(x.get("a 面積(㎡)", 0) or 0) == 0`
⇒ **⛔ 未綁 `_is_ghost_sliver`、⛔ 未綁名稱**（`VR-074` 落地拘束逐字）。
🔴 **併記**：`base`（該街廓之 `TEMP_LAYER`）之 `ghost3` 命中亦皆 `0`——因八個 ghost 全落在 `G1`／`R1`／`R4`／`RD1`／`RD2`／`RD3`，而本 `8` 次之 blk 為 `R2`／`R3`／`R5`／`R6`。

### `A-4`　二態對拍

| 序 | blk | 現況 `_name` | `VR-074` `_name` | |
|---|---|---|---|---|
| `1`〜`8` | `R2`／`R3`×3／`R5`×2／`R6`×2 | 如上表 | **逐次相同** | ✅ |

🔒 **自證**：`ns is _projection_order.__globals__` ＝ **`True`** ⇒ 層疊亦及於 `app.py` 內部直呼。

### 🔒 層疊生效之**正／負對照**（CC 自加·⛔ 不以「射程內濾掉 `0` 列」逕判失效）

| 檢 | 現況態 | `VR-074` 態 |
|---|---|---|
| `R1` 之 `ghost3` 數（受詞規模） | `1` | `1` |
| **正對照**（餵含 ghost 之 `R1` 全體）之濾掉數 | `0` | **`1`**（入`6`／出`5`） |
| **負對照**（餵已去 ghost 者）之濾掉數 | `0` | **`0`**（入`5`／出`5`） |

⇒ 🔒 **層疊生效之自證 ＝ `True`**（正對照非零且＝受詞數・負對照零・現況態零）。
🩸 **CC 自捕（二）**：首版探針之反靜默計數器於射程內得「濾掉 `0` 列」而印出「🛑 層疊未咬到」——
🔴 **該警示係<u>誤報</u>**：本探針射程 ＝ `build_build_parcels`，其 PO 呼叫**皆**來自 `k6_step0_merge` 之 `_mem`（合併群成員），依構造⛔ 無 ghost ⇒ 「濾掉 `0`」係**受詞不在射程內**、⛔ 非層疊失效。
⇒ 補上二造人造對照後方得出艙（**⛔ 未以「數字好看」略過該警示**）。

### `A-6`　對照組（外部錨 ＝ `167R §三 M-1` 表）

| 量 | 期望（外部錨） | 實測 |
|---|---|---|
| `TEMP_LAYER` 全體 `ghost3` | `8` | **`8`** ✅ |
| `BUILD_LAYER` 全體 `ghost3` | `2` | **`2`** ✅ |
| （總列數） | — | `temp` `114` ／ `build` `49` |

### `A-5`　判準

| 項 | 結果 |
|---|---|
| ① `ghost3` 命中數逐次皆 `0` | **True**（現況 `8` 次 ＋ `VR-074` `8` 次） |
| ② `_name` 二態逐次相同 | **True** |
| ⇒ 二項齊備 | ✅ **True**（層疊自證亦 `True`） |

⇒ 🔒 **(A) 與 (B) 於該點等價** ⇒ **`167R` 之 `16` 格預測集合可轉用於 `169`**；`S-2` **未觸發**。

---

## §三　`M-B`　期初全量 log

### `B-1`／`B-2`

| 項 | 值 |
|---|---|
| clone | `/c/w168a`（拋棄式·`8` 字元·等長） |
| 指令 | `WV_PROJ_POP_COUNT=<scratchpad> PYTHONIOENCODING=utf-8 python verify/run_all.py > <log> 2> <err>; echo "EXIT=$?" >> <log>` |
| 入倉之檔 | `verify/out/runall_a98a99f_full.log`（**以基座 commit 命名**·⛔ 非 blob） |
| 列數／位元組 | **`506`**（`splitlines()`）／**`46006`** B |
| `sha256` | `69e1eee8a32eae35e4f9968231609c3f363d0da052d2e7a8465a2c26a35c5a21` |

🔒 **`GB-94` 之實證**：長跑後 `/c/w168a` 之 `git status --porcelain` 有 **`3` 個已追蹤 log 被改寫**
（`probe_ruling_K4_s_origin.log`／`probe_ruling_K8_baseline_pairing.log`／`probe_ruling_K8_sideline_pairing.log`）
⇒ **入倉係自 `/c/w168b`（未跑 `run_all`·工作樹淨）為之**，符單 `B-1` 之禁令。

### `B-3`　與 `runall_f285b91_full.log` 之逐位 `diff`（⛔ 未正規化）

| 量 | 值 |
|---|---|
| 列數 | 期初 `506` ／ 期末 `506` |
| **未正規化 LCS** | 只在期初 **`12`** ／ 只在期末 **`12`** ／ **合計 `24`** |

**逐列歸因**（⛔ 無殘餘·三類之和 ＝ `24` ＝ 總數）：

| 類 | 列數 | 說明 |
|---|---|---|
| **A 之一：clone 路徑字串** | **`24`** | 期初含 `w163b` 之列 `12` ／ 期末含 `w168a` 之列 `12`——二者**同長度**（皆 `8` 字元）⇒ ⛔ 無截斷位移 |
| A 之二：traceback 行號 | **`0`** | `verify/*.py` **既有檔零改** ⇒ 行號未動 |
| **B：其他（須逐字歸因）** | **`0`** | — |

⇒ 🔴 **`X-5`（相異列數 ＝ `0`）不符**；🔒 **然 `B` 類 ＝ `0` ⇒「無法歸因」之列為零 ⇒ `S-4` 未觸發**。
🩸 **不符之結構性成因**：二 log **各自含 `12` 列 clone 路徑**（`File "C:\w163b\…"` vs `File "C:\w168a\…"`）
⇒ **未正規化之 `diff` ＝ `0` 於構造上不可達**，除非二 clone 同名。
🔒 單 `X-5` 之依據欄只述「`.py` 變動僅二新檔」，**⛔ 未計入 log 內之 clone 路徑** ⇒ 交發單側落筆（`§七-1`）。
🔒 **補證（⛔ 非判準·僅供對照）**：依 `VR-077 二` 之四類正規化後，該 `24` 列**全數吸收**（規則 ① 絕對路徑）。

### `B-4`　三欄交叉檢核

| 欄 | 值 |
|---|---|
| `rc`（log 末列） | **`EXIT=1`** |
| 逐字結論列 | **`RESULT: FAIL`** |
| `stderr` 位元組數 | **`0`** B |

⇒ `X-6` ✅ **符**（准紅碼·`CLAUDE.md:67-72`）。

---

## §四　`M-C`　逐格違規**全集**（繞開 `12` 列顯示上限）

**探針** ＝ `verify/probes/probe_WG9168_viol_full.py`（新檔·⛔ 無絕對路徑）。
🔒 **比較式⛔ 未自寫**——`import run_verification` 並呼叫其 **`diff_rows`**（`verify/run_verification.py:406`）
⇒ `_norm`／`key_cols = ["街廓","端","候選地號"]`／三階段順序（缺列→多列→逐列逐欄）／列序欄序**同源**；
其回傳 `viol` **未截斷**（截斷發生於 `:1454` 之列印端）。`WV_BAKE` 未設 ＝ `True`（設之則 `diff_rows` 早退回綠）。

### `C-1`／`C-2`　期初全集 ＋ 外部錨

| 閘（逐字） | `skip_cols` | 全集 | 外部錨（基座 log「本閘違規總計」） | |
|---|---|---|---|---|
| `v3·診斷0m` | ∅ | **`68`** | `68` | ✅ |
| `無串聯0m` | `{"原位次(距角序·暫行)", "G估(㎡)"}` | **`68`** | `68` | ✅ |
| `v3·診斷3.5m` | ∅ | **`60`** | `60` | ✅ |
| `無串聯3.5m` | 同上 | **`60`** | `60` | ✅ |

⇒ `X-7` ✅ **符**（外部錨 ＝ 基座 log 之截斷通知列·⛔ 非 `N/N` 自證）。

### 🔴 `C-3`　序之證明

| 檢 | 結果 |
|---|---|
| 前 `12` 列與 log 顯示列**逐位**相同 | 🔴 **False**（四閘皆然） |
| 只正規化 `got` 之 `!r` 引號後 | ✅ **True**（`48`／`48` 列·四閘皆然） |

🔒 **差之唯一來源（已隔離）**：`run_verification` 比對之 **got 側係記憶體 `diag`**（數值型），
而單 `C-1` 逐字令本探針「**讀該趟所產之 `verify/out/got_診斷_退縮{0m,3.5m}.csv`**」（字串型）
⇒ f-string 之 `{g[col]!r}` 呈現不同（`got=116.08` vs `got='116.08'`）；
`diff_rows` 內之 `_norm` 於**比對**時已吸收該型別差 ⇒ **違規集合與序相同**（`C-2` 之 `68/68/60/60` 即其獨立佐證）。

🛑 **依常規二逐字回報單之內部互斥**：`C-1`（got 側須讀 **CSV**）與 `C-3`（須與 log 顯示列**逐位**相同）
**⛔ 不可同時成立**——只要 got 側改讀 CSV，`!r` 之呈現即必異。
🔒 **CC 之處置（取保守項）**：**⛔ 未改探針之 got 源以求逐位相同**（那會違 `C-1`），
改為**二數並報** ＋ 具名其唯一來源，**判由發單側落筆**（`§七-2`）。

### `C-4`　🔴 `169` 之期末預算（**本批主產出之一**）

| 閘 | 套用之預測格數 | 期初總數 | **期末總數** | 前 `12` 列是否變動 |
|---|---|---|---|---|
| `v3·診斷0m` | `4` | `68` | **`72`** | **是**（第 `12` 顯示列） |
| `無串聯0m` | `4` | `68` | **`72`** | **是**（第 `12` 顯示列） |
| `v3·診斷3.5m` | `4` | `60` | **`64`** | 否 |
| `無串聯3.5m` | `4` | `60` | **`64`** | 否 |

**③ log 應變更列清單**（逐列「期初逐字 → 期末逐字」）：

| # | 期初逐字 | 期末逐字 |
|---|---|---|
| `1` | `[v3·診斷0m] ('R6', '右', '628-18(1)') 欄「原位次(投影序)」: baseline='9' got='6'` | `[v3·診斷0m] ('R4', '右', '628-1(1)') 欄「原位次(投影序)」: baseline='3' got='2'` |
| `2` | `[無串聯0m] ('R6', '右', '628-18(1)') 欄「原位次(投影序)」: baseline='9' got='6'` | `[無串聯0m] ('R4', '右', '628-1(1)') 欄「原位次(投影序)」: baseline='3' got='2'` |
| `3` | `…（**另 56 列未顯示**·本閘違規總計 68 列）` | `…（**另 60 列未顯示**·本閘違規總計 72 列）` |
| `4` | 同上（`無串聯0m`） | 同上 |
| `5` | `…（**另 48 列未顯示**·本閘違規總計 60 列）` | `…（**另 52 列未顯示**·本閘違規總計 64 列）` |
| `6` | 同上（`無串聯3.5m`） | 同上 |

⇒ **合計 `6` 列**（顯示列 `2` ＋ 截斷通知列 `4`）——**與發單側預測逐字相符** ⇒ `X-9` ✅ **符**。
⚠️ 上表 `1`／`2` 之**期初逐字**係 `diff_rows` 之字串形（`got='6'`）；於 log 內呈現為 `got=6`（見 `C-3` 之型別差）。

### `C-5`　判別力自檢（於**複本**上人造改一格）

| 檢 | 值 |
|---|---|
| 受測閘 | `v3·診斷0m` |
| 人造改動 | `真交集(㎡)` 之首列 `'109.15'` → `'999.99'` |
| 總數 | `68` → **`69`**（Δ `+1`·須 `+1`）✅ |
| 該格是否逐字出現於集合內 | ✅ **True**：`[v3·診斷0m] ('R4', '左', '628(1)') 欄「真交集(㎡)」: baseline='999.99' got='116.08'` |

🔒 **「本探針⛔ 不會抹平什麼」**：其比較式 ＝ `diff_rows` **本體**，⛔ 無任何 `skip`／容差／取樣／去重；
上開**人造反例**（一格）即令總數 `+1` 且逐字現形 ⇒ 證其**⛔ 非恆綠**。複本已刪除、原 baseline ⛔ 未動。

### `C-6`　產物

`verify/out/WG9168_viol_full_a98a99f.json`（機器可讀·窮舉·含逐閘總數／逐列違規／`C-4` 之期末預算）。

---

## §五　`M-D`　顯示上限之現查（`GB-115` 之被檢物）

**① `sed -n '1454p;1460p' verify/run_verification.py`（自基座 blob 重抽·常規七）**

```text
        for x in viol[:12]:
            print(f"        …（**另 {len(viol) - 12} 列未顯示**·本閘違規總計 {len(viol)} 列）")
```

**② 自 `runall_a98a99f_full.log` 逐字抄出之截斷通知列（全 `8` 條·本批四閘者以 🔴 標）**

```text
        …（**另 2 列未顯示**·本閘違規總計 14 列）
        …（**另 2 列未顯示**·本閘違規總計 14 列）
        …（**另 56 列未顯示**·本閘違規總計 68 列）
        …（**另 56 列未顯示**·本閘違規總計 68 列）
        …（**另 5 列未顯示**·本閘違規總計 17 列）
        …（**另 48 列未顯示**·本閘違規總計 60 列）
        …（**另 48 列未顯示**·本閘違規總計 60 列）
        …（**另 5 列未顯示**·本閘違規總計 17 列）
```

🔒 **本批四閘於上 `8` 條中之對應**（註記置於圍欄**之外**·俾塊內保持逐字）：
第 `3` 條 ＝ `v3·診斷0m`（`68`）／第 `4` 條 ＝ `無串聯0m`（`68`）／第 `6` 條 ＝ `v3·診斷3.5m`（`60`）／第 `7` 條 ＝ `無串聯3.5m`（`60`）；第 `1`／`2`／`5`／`8` 條（`14`／`14`／`17`／`17`）屬**他閘**。

✅ **已覆核·態 ＝ `a98a99f`**（二者皆由檔案管線直接產生；覆核法 ＝ 重跑抽取並逐位 `diff`）。
⚠️ **併記**：全量 log 之截斷通知列實為 **`8`** 條（⛔ 非 `4`）——另 `4` 條屬他閘（`14`／`14`／`17`／`17`）；
單 `M-D` 只令抄「四閘之截斷通知列」，本報告**全 `8` 條併列並標明何者屬本批四閘**，以免讀者誤取。

---

## §六　`M-E`　登記 ＋ 取號自行複現（常規六四）

🔒 **母體 ＝ `docs/**/*.md` ＋ `CLAUDE.md` ⇒ `396` 檔**；態 ＝ `blob@a98a99f`。

| 待鑄 | 定義列式（逐字） | 相異／MAX | 錨定框命中 | 對照組 |
|---|---|---|---|---|
| `VR-079` | `` ^##\s*`VR-([0-9]{1,4})` `` | `64`／**`78`** | `0`（`0` 檔） | `VR-078` ＝ **`15` 列／`5` 檔** |
| `GB-115` | 三框聯集：`` ^\| *\*\*`?GB-N`?\*\* `` `109`／式A `` ^## `GB-N` `` `3`／式B `` ^### `GB-N` `` `25` | `112`／**`114`**·缺號 `[12, 87]` | `0`（`0` 檔） | `GB-114` ＝ **`8` 列／`3` 檔** |
| 自誤 `201` | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `165`／**`200`** | `0`／`0`（二形並取） | `自誤 200` ＝ `1`／`` 自誤 `200` `` ＝ `6` |

🔒 **`GB` 算術自檢（常規八）**：`114 − |{12, 87}|(2) == 112` ⇒ **True**；外部錨 ＝ `W-G.9-144:81` 之式A。

### 🔒 與單 `§三 M-E` 表之**三處差·皆為框／母體之別**（⛔ 非值錯）

| 項 | 單載 | CC 實測 | 已隔離之成因 |
|---|---|---|---|
| 母體 | `.md` **`445`** 檔 | **`396`** 檔 | 單之母體 ＝ **全倉 `.md`**（實測 `445` ＝ `docs/**` `395` ＋ 其餘 `50`）；CC 依單 `§零` 之慣用母體取 `docs/**/*.md` ＋ `CLAUDE.md` ⇒ **母體定義之別** |
| `VR-078` 對照組 | **`16`** | **`15`** | **二框**：命中**列**數 ＝ `15`／**出現次數**（`str.count`）＝ **`16`**（一列含二次）⇒ 單之 `16` 係出現次數框。🔴 **同形第三度**（`W-G.9-166R` 之 `GB-112` `7`／`8` 為第二度） |
| `GB` 式B | **`26`** | **`25`** | **二框**：命中**列**數 ＝ `26`／**相異號**數 ＝ `25` ⇒ 單之 `26` 係列數框 |

🔒 **三者之聯集／MAX／缺號皆相同**（`112`／`114`／`[12, 87]`）⇒ **取號結論一致**、⛔ 未順延。

### 形之自證 ＋ MAX 推進（常規四（九））

| 類 | payload 之標題形被該類定義列式命中 | MAX 推進 | 新號在缺號集？ |
|---|---|---|---|
| `VR` | ✅（`## \`VR-079\`　…`） | `78` → **`79`** | — |
| `GB` | ✅（`### \`GB-115\` 🆕` ＋ 表列 `\| **\`GB-115\`** 🆕 \|`） | `114` → **`115`** | ⛔ 否（缺號仍 `[12, 87]`） |
| 自誤 | ✅（`### 自誤 \`201\`（發單側·落地窗）：…`） | `200` → **`201`** | — |

---

## §七　`§五` 十一項預測逐項對拍（⛔ 未改預測）

| # | 預測 | 實測 | 判 |
|---|---|---|---|
| `X-1` | `app:k6step0/_ordered` 執行次數 `> 0` | **`80`** | ✅ **符** |
| `X-2` | `A-2` 之 `ghost3` 命中逐次皆 `0` | `8` 次皆 `0`（二態） | ✅ **符** |
| `X-3` | `A-3` 之 `_name` 二態逐次相同 | `8`／`8` 相同 | ✅ **符** |
| `X-4` | 對照組 `TEMP` ＝ `8`／`BUILD` ＝ `2` | `8` ／ `2` | ✅ **符** |
| `X-5` | `B-3` 相異列數 ＝ `0` | **`24`**（**全數歸因** clone 路徑字串·`B` 類 `0`） | 🔴 **不符**（`S-4` 未觸） |
| `X-6` | `rc` ＝ `1`、結論列 ＝ `RESULT: FAIL` | `EXIT=1`／`RESULT: FAIL`／`stderr` `0` B | ✅ **符** |
| `X-7` | `C-2` 四閘總數 ＝ `68`／`68`／`60`／`60` | 逐閘相符 | ✅ **符** |
| `X-8` | `C-3` 前 `12` 列逐位相同 | 逐位 **False**；內容／序 `48`／`48` **全同** | 🔴 **不符**（單 `C-1`／`C-3` 互斥·常規二） |
| `X-9` | `C-4` ＝ `6` 列（`0m` 二閘各 `1` 顯示列 ＋ `4` 條截斷通知列） | **`6`**（`2` ＋ `4`）·逐字相符 | ✅ **符** |
| `X-10` | `wv_reconcile` 對帳區塊於 `169` 期末逐字不變 | 單自載「⛔ 非本批之受詞」 | ⚪ **不可判** |
| `X-11` | 全部受檔 `deletions` 逐列 ＝ `0` | `numstat` 刪除欄逐列 `0` | ✅ **符** |

**計數對拍**：符 **`8`** ／ 不符 **`2`** ／ 不可判 **`1`** ⇒ 和 ＝ **`11`** ＝ 預測總數 ✅。

---

## §八　入倉清單（⛔ 零生產碼·純新增／純附加）

| # | 檔 | 內容 | 刪除欄 |
|---|---|---|---|
| `1` | `verify/probes/probe_WG9168_k6step0.py` | `M-A` 探針（**新檔**·⛔ 無絕對路徑） | — |
| `2` | `verify/probes/probe_WG9168_viol_full.py` | `M-C` 探針（**新檔**·⛔ 無絕對路徑） | — |
| `3` | `verify/out/runall_a98a99f_full.log` | 🔴 **期初全量 log**（`506`／`46006` B·**新檔**） | — |
| `4` | `verify/out/probe_WG9168_k6step0_a98a99f.log`／`WG9168_k6step0_a98a99f.json` | `M-A` 產物（**新檔**） | — |
| `5` | `verify/out/probe_WG9168_viol_full_a98a99f.log`／`WG9168_viol_full_a98a99f.json` | 🔴 `M-C` 產物（**新檔**·含 `C-4` 期末預算） | — |
| `6` | `docs/驗證裁定登記表.md` | `P-1`　**`VR-079`**（四項裁 ＋ 量測通道之更正） | `0` |
| `7` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `P-2`　**`GB-115`** | `0` |
| `8` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `P-3`　**自誤 `201`**（含**戒 `16`**） | `0` |
| `9` | 本報告 | `docs/reports/W-G.9-168R_前置量測與期初錨.md`（**新檔**） | — |

🔒 **報告命名（常規三：CC 提案 ＋ 發單側核可）**：候選 ①`W-G.9-168R_前置量測與期初錨.md`（**採**）
②`W-G.9-168R_VR074前置補量與違規全集.md` ③`W-G.9-168R_k6step0補量與不截斷通道.md`。
**理由**：倉內既有體例（`165R_停機_…`／`166R_VR078…`／`167R_VR074前置量測…`）皆為「單號 `R` ＋ 主題」，
而本單之主題即其標題**「前置量測與期初錨」** ⇒ 與單標題一致最利檢索。

🔒 **`§七` 不辦事項全數遵守**：⛔ 未落地 `VR-074`；⛔ 未改 `_proj_pop_filter`／`_PROJ_POP_DECL`／任何濾式一字；
⛔ 未改 `app.py`／`verify/*.py` 既有一字；⛔ 未重產任何 baseline／凍存名單；
⛔ 未修 `GB-104`／`106`／`107`／`108`／`110`〜`114`；⛔ 未落地 `VR-075`／`VR-078`／任何 `K-9-*`；
⛔ 未動 `data/`；⛔ 未改任何既有 `docs/` 文字（三 payload 皆末端追加）；⛔ 未改 `.gitattributes`；⛔ 未追改任何既有已鑄之號。

---

## §九　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | `HEAD` 全 `40` 碼；`app.py` blob 逐位；`app.py`／`verify/*.py` 既有檔零改 | ✅ `app.py` ＝ `f285b91c9e5f97f13dd74508e1a403833bc75cb3`（**絕對值**）；`numstat` 中既有 `.py` 零命中 |
| `2` | `numstat` 刪除欄逐列 `0`；`wg942_append_audit` | ✅ 見 `§七 X-11` 與檔尾 |
| `3` | `A-1`〜`A-6` 逐項有數；`A-4` 自證 `ns is …__globals__` ＝ `True`；`A-6` 對照組非零 | ✅ `§二`（另加層疊之正／負對照） |
| `4` | `B-1`〜`B-4` 逐項有數；新 log 入倉；`B-3` 相異列數 ＋ 逐列歸因 | ✅ `§三`（`24` 列·三類和 ＝ `24` ＝ 總數·⛔ 無殘餘） |
| `5` | `C-1`〜`C-6` 逐項有數；`C-2` 外部錨具名；`C-3` 之證；`C-5` 判別力反例逐字 | ✅ `§四` |
| `6` | `M-D` 二列逐字（管線產生·已覆核） | ✅ `§五`（標「✅ 已覆核·態 ＝ `a98a99f`」） |
| `7` | `M-E` 三 payload 入倉；取號三列自行複現；形之自證 ＋ MAX 推進 | ✅ `§六`（`VR` `78→79`／`GB` `114→115`／自誤 `200→201`；`115` ⛔ 不在缺號集） |
| `8` | 每一命中數同格標框（逐字）＋ 母體 ＋ 母體之態 ＋ 對照組 | ✅ 全報告；態 ＝ `blob@a98a99f` |
| `9` | `§五` 十一項逐項對拍（計數和 ＝ `11`） | ✅ `§七`（符 `8`／不符 `2`／不可判 `1`） |
| `10` | 探針⛔ 無絕對路徑；`run_all` 只在拋棄式 clone 內跑；工作倉入倉前淨 | ✅ 二探針之絕對路徑樣式命中 **`0`**（**對照組** `verify/tools/wg960_z_selfcheck.py` ＝ `1`）；長跑於 `/c/w168a`、入倉於 `/c/w168b`（`GB-94` 實證見 `§三`） |
| `11` | 報告命名 | ✅ `§八`（三候選 ＋ 理由） |

🔒 **常規一 四項自評**：① 零生產碼變更 ✅ ② `deletions` 逐列 `0` 且既有檔零命中 ✅
③ `app.py` 期末 blob 逐位 ＝ 期望值 ✅ ④ 開工／收工閘齊備 ✅ ⇒ **四項齊備 ⇒ 逕行 `push`**。
🛑 **`W-G.9-169`（生產碼批）⛔ 不得逕行·須請示 KL**（單 `§六` 逐字·本報告併記以防遺忘）。

---

## §十　🛑 交發單側落筆（⛔ CC 不自裁）

1. 🔴 **`X-5` 之不符係<u>構造性</u>**：二 log 各含 `12` 列 clone 路徑 ⇒ **未正規化之 `diff` ＝ `0` 不可達**，
   除非二 clone 同名。⇒ 建議將 `B-3` 之判準改為「**依 `VR-077 二` 四類正規化後 ＝ `0`**」，
   或「**未正規化之 `B` 類（無法歸因）＝ `0`**」。🛑 CC ⛔ 未自改判準。
2. 🔴 **單 `C-1` 與 `C-3` 互斥**（常規二）：`C-1` 令 got 側讀 **CSV**、`C-3` 令與 log 顯示列**逐位**相同，
   而 log 之 got 側係**記憶體 `diag`**（數值型）⇒ `!r` 呈現必異。
   ⇒ 建議 `C-3` 之判準改為「**去 `got` 之 `!r` 引號後逐位相同**」，或 `C-1` 改令 got 側取記憶體 rows。
   🔒 **CC 已取保守項**（二數並報·⛔ 未改探針）。
3. ⚠️ **`M-D` 之量詞**：全量 log 之截斷通知列實為 **`8`** 條、本批四閘僅佔 `4` ⇒ 引用時須具名何者。
4. 🔴 **`VR-078` 對照組之 `15`／`16`** 係**命中列數 vs 出現次數**二框（**同形第三度**）
   ⇒ 建議於 `VR-058 一` 之下加一款：**取號之對照組一律標明所用之框**。
