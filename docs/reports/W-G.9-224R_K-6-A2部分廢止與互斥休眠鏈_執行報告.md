# `W-G.9-224` 執行報告：`K-6-A2` 之部分廢止標記 ＋ 互斥休眠鏈之登記 ＋ 發單側轉述失真之登記

> 🔒 **本檔＝本波唯一本體**；全以 `git cat-file blob` 復驗。
> 🔒 **態**：落地前 `HEAD ＝ e1d3d20`（全 40 碼見 `git ls-remote`）。
> 🔒 **⛔ 採用單所載之任何數為錨**——`274`／`281`／`106`／`6`／`2`／`4` 等**全數當場復算**。
> 🔒 **零生產碼**：閘 P 五檔 `sha256` 落地前後逐位相符（`§六`）。
> 🔒 **本批鑄號 ＝ `自誤 282` 一則**。
> 🛑 **`W-G.9-219`（含生產碼）本批⛔ 執行**（單之絕對禁項）。

---

## §零　工項零：本單原封入倉 ⇒ ✅ **過**

| 量 | 值 |
|---|---|
| 路徑 | `docs/orders/W-G.9-224_K-6-A2部分廢止與互斥休眠鏈.md` |
| 來源（落檔） | **`5239`** B ／ `CR` **`0`** ／ `LF` **`69`** ／ `sha256` `6f7f0aa86b56faf229db3c7b6b1c875e3ab92aa270fed57b6b44cfc16e88ba26` |
| 入倉（索引側） | **`5239`** B ／ `CR` **`0`** ／ `sha256` 同上 |
| 判 | **逐位相同 ＝ `True`**／`Δ` ＝ **`+0`** B |

---

## §一　工項一：`K-6-A2` 之部分廢止標記（裁：甲）

### a　四列之**當場復現**（⛔ 採單所抄）

| 列 | 逐字 |
|---|---|
| `CLAUDE.md:313` | `\| K-6-A2（寬深雙檢實作 ＝ **K-9 實作·有土地後果**） \| ⬜ 未開工 \|` |
| `CLAUDE.md:592` | `\| \`3\` \| \`K-6-A2\` 剩餘（\`K-9-1\`／\`K-9-3\`／\`K-9-5\`／\`K-9-6\` 宗地層 \`min_depth\`／\`K-9-8\` 之 \`GB-18\`／\`K-9-5-2\` ①⑤） \| ⬜ 未開工 \| — \|` |
| `CLAUDE.md:1100` | （與 `:592` **逐字相同**） |
| `CLAUDE.md:1277` | `- ⬜ \`K-6-A2\` 寬深雙檢` |

🔒 **與 `-223R §三 a` 之對拍 ⇒ 逐字相符** ✅（⇒ 停機款「四列逐字與 `-223R` 不符」**未觸發**）。

### b　標記之三項（**齊備**·缺一即為落地不全）

已落 `CLAUDE.md` 之 `## 🔧 \`K-6-A2\` 之**部分廢止標記**`（⛔ 改四列本文一字）：

| 項 | 內容 | 落地 |
|---|---|---|
| `①` | **已廢之二項**逐項具名（`K-9-5`／`K-9-5-2` ①⑤）＋ 其依據**逐字**（`K-6:2670`–`:2673`） | ✅ |
| `②` | 🔴 **存續之四項**逐項具名（`K-9-1`／`K-9-3`／`K-9-6` 宗地層 `min_depth`／`K-9-8` 之 `GB-18`）＋ 各自依據逐字（`K-6:1602`／`:1604`／`:1607` 及 `K-9-8` 列）＋「有土地後果」之逐字出處 | ✅ |
| `③` | 明載本標記為**部分廢止**、**⛔ 得讀為全案結清**；令爾後提及 `K-6-A2` 者須同格載「已廢二項／存續四項」之別 | ✅ |

🔒 **依據逐字（`K-9-5-15 二`·KL 裁 `2026-08-18`）**：
`K-6:2673` ＝ `⇒ 🔴 **街角第 1 宗⛔ 不另設寬度閘、⛔ 不另跑矩形容納測試。**`

🔒 **存續四項之依據逐字（`K-6` 落地表）**：
`:1602` `\| K-9-1 ⬜ **未實作**（段六） \| §零-2 街廓最小建築面積之 UI 欄與判準（街角宗不豁免） \|`／
`:1604` `\| K-9-3 ⬜ **未實作**（**段五**·與 K-9-6 同批） \| 拆除宗地層深度量測… \|`／
`:1607` `\| **K-9-6** 🔶 **部分**（…**宗地層之 \`min_depth\`（GB-13）⬜ 未實作·段五**） \| …`／
`K-9-8` 列逐字 `（**GB-18**·🔴 **仍未修·已重標段六**）`。

🔒 **「有土地後果」之逐字出處（當場窮舉）**：`CLAUDE.md` **`5`** 列（`:213`／**`:313`**／`:512`／`:549`／`:1256`）；
`K-6` **`3`** 列（`:3746`／`:3764`／`:3766`）。**與 `K-6-A2` 直接相關者 ＝ `CLAUDE.md:313`**。

### c　正本表之**拆列**（已落地·⛔ 保留原合一之列）

| # | 項 | 判 |
|---|---|---|
| `3-a` | 🗄️ `K-6-A2` 之**已廢部分**（`K-9-5`／`K-9-5-2` ①⑤） | ✅ **結案** |
| `3-b` | 🔴 `K-6-A2` 之**存續部分**（四項） | ⬜ **未落地**·🔴 **有土地後果** |

---

## §二　工項二：互斥休眠鏈之登記（裁：⛔ 上呈 KL）

### a　正字處之定位 ＋ 復活鏈

🔒 **落點 ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` 末端追加**（⛔ 新創檔）
——**既例**：同檔末端已有二節（`:3874` `## 🔧 \`K-9-3-1-a\` 之現況指標…`／
`:3906` `## 🔧 \`6.847031\` 之作廢指標…`·皆 `W-G.9-221` 所落）。

**復活鏈（逐環具名·已落地）**：

> **互斥復活** ⟸ **`K-9-5`（街角第 1 宗寬度實量）落地** ⟸ **`K-9-5-15 二` 被推翻**

| 環 | 內容 | 現況 |
|---|---|---|
| `1` | `K-9-5-15 二`（`K-6:2670`–`:2673`）令「街角第 1 宗⛔ 不另設寬度閘、⛔ 不另跑矩形容納測試」 | **有效** |
| `2` | `K-9-5`（`K-6:1606` 逐字「⬜ **未實作**（段六）」）**已由環 `1` 取消** | **已廢** |
| `3` | `K-9-5-2-a`-3 之受詞係「**`K-9-5` 實量**若出現面積過、寬不足」（`K-6:571`–`:573`）⇒ **無適用之情形** | **休眠** |
| `4` | `K-9-5-3` 之效力限**街角第 2 宗與宗地層** ⇒ 與環 `3` **射程⛔ 重疊** | **無交集** |

🔒 **雙重休眠**：環 `2` 之 `K-9-5` **本身即工項一之已廢二項之一** ⇒ 復活須**先推翻環 `1`**、
再使環 `2` 落地 ⇒ **二層條件**。

### b　復活後之性質

🔴 **直接影響街角第 1 宗之合格判定** ⇒ 🛑 **真域邊界**；屆時須以 KL 格式上呈
（【現況】【要改成】【對土地的影響】【要你判斷】），**⛔ 由 CC 或發單側自裁**。

### c　`GB-84` 結案之「⛔ 蘊含」**仍全效**（逐字已載入該節）

「⛔ `K-9-5-3` **未**失效」「⛔ 二則之逐字互斥**未**解決」
⇒ 🔒 **本登記係「射程分析」而非「互斥之解決」**，**⛔ 讀為後者**。

### d　正本表之**增列**（`序 10`·已落地）

| # | 項 | 判 | 觸發條件 |
|---|---|---|---|
| `10` | `K-9-5-2-a`-3 與 `K-9-5-3` 之互斥（現為**休眠**） | 🟡 **條件式待決** | 上開 `a` 之鏈（二層） |

---

## §三　工項三：發單側轉述失真 ⇒ 鑄 **`自誤 282`**

**受詞**：發單側轉述 CC 之結論予 KL 時，**抹除其保留語**——有保留之結論被讀為**全稱**結論。

🔒 **`-220R` 原文之逐字（當場復現）**

| 列 | 逐字 |
|---|---|
| `:211` | `3. 餘之未落地（第 2 宗與宗地層改採最小矩形）＝ **既具名待落地項**（更正五正本表 \`序 2\`），` |
| `:212` | `且 \`K-9-5-15 四\` 逐字自載「本裁⛔ 未使其落地」⇒ ⛔ 新缺陷。` |
| `:207` | `### 🛑 \`d\` 之停機條件 ⇒ 🟢 **⛔ 觸發**（三條·⛔ 缺一）` |

⇒ 🔴 其結論係「停機條件⛔ 觸發」＋「**餘之未落地**係既具名待落地項」，**⛔「已結案」**。

**推導式**：轉述句之**量詞**（「已結案」）與原文之**限定詞**（「餘之」「既具名待落地」）**互斥**
⇒ 該轉述抹除了保留。**族** ＝ 與「向 KL 之例行出艙不可信」同族，**受詞不同**
（該族係**沿用前批文字**；本則係**摘要時抹除保留**）。
**通則**：凡 CC 原文含保留語者，該保留須**逐字帶入**；⛔ 以摘要抹除。**責** ＝ 發單側。

### 併：回掃 ⇒ 🛑 **母體不可得**（⛔ 空集）

🔒 母體 ＝ `docs/reports/` 內合 `W-G.9-2\d\dR` 之報告 **`21`** 份·**列框**·
字樣集 `餘者`／`餘之`／`部分`／`既具名`／`惟`／`⛔ 蘊含`。逐份含保留語之列數：
`-200R` `1`／`-201R` `3`／`-203R` `1`／`-205R` `1`／`-206R` `0`／`-208R`(207入倉) `0`／
`-208R`(commit2) `1`／`-209R` `1`／`-210R-c3` `0`／`-210R` `1`／`-211R` `1`／`-214R` `1`／
`-215R` `2`／`-216R` `2`／`-217R` **`13`**／`-218R` **`7`**／`-220R` **`9`**／`-221R` `2`／
`-222R` `4`／`-223R2` `4`／`-223R` **`11`**。

🔴 **不符者清單⛔ 可構造**——**發單側向 KL 之轉述<u>不在倉</u>**（存於對話側）⇒ CC **無可比之受詞**。
⇒ 🔒 **該清單⛔ 為「空集」，而係「<u>母體不可得</u>」**；本則之一例係**發單側於本單自陳**者。
🔒 **⇒ 欲使該回掃可行，發單側須將其向 KL 之結論性轉述<u>入倉</u>**（候發單側裁）。

---

## §四　工項四：機檢器之回測擴充

### 🛑 二處自捕自正（`常規五`）

1. **可攜性界限**：`_repo()` 以 `git rev-parse --show-toplevel` 於 **cwd** 定倉根
   ⇒ 於倉外執行即 `rc=128` 而 `assert` 崩。**本批⛔ 改其碼**；照實具名：**本器須於倉內執行**。
2. 🔴 **真缺陷（已修）**：`parse_acceptance` 之**早退路徑**（無 `【驗收】` 段時）回 **`2` 元組**，
   而呼叫端期待 **`3`** ⇒ `ValueError: not enough values to unpack`。
   **成因**：`W-G.9-223` 之二次收斂改了正常回傳之元數而**漏改早退路徑**。
   **修正**：`return [], [], (None, None)`（＋ docstring 補正）。

### a　擴回測（母體 ＝ 本波全部 `W-G.9-2\d\dR?_` 報告 **`21`** 份）

| # | 報告 | 單在倉 | 判定組 | 參考組 | 判定組缺項 | 缺項字樣 |
|---|---|---|---|---|---|---|
| `200` | `-200R_K-9-24入倉` | ✅ | **`0`** | `0` | `0` | — |
| `201` | `-201R_歸側實算` | ✅ | **`0`** | `0` | `0` | — |
| `203` | `-203R_K-9-25入倉` | ✅ | **`0`** | `0` | `0` | — |
| `205` | `-205R_二檔入倉…` | ✅ | **`0`** | `0` | `0` | — |
| `206` | `-206R_側分支復驗…` | ✅ | **`0`** | `0` | `0` | — |
| `208` | `-208R_207入倉…` | ✅ | **`0`** | `0` | `0` | — |
| `208` | `-208R_commit2…` | ✅ | **`0`** | `0` | `0` | — |
| `209` | `-209R_commit2放行…` | ✅ | **`0`** | `0` | `0` | — |
| `210` | `-210R-c3_段一上鎖` | ✅ | **`0`** | `0` | `0` | — |
| `210` | `-210R_自誤263與264…` | ✅ | **`0`** | `0` | `0` | — |
| `211` | `-211R_commit3放行…` | ✅ | **`0`** | `0` | `0` | — |
| `214` | `-214R_號框法典化…` | 🔴 **否** | — | — | — | **不可回測** |
| `215` | `-215R_分類差坐實…` | 🔴 **否** | — | — | — | **不可回測** |
| `216` | `-216R_106登記…` | 🔴 **否** | — | — | — | **不可回測** |
| `217` | `-217R_授權查證…` | ✅ | `6` | `11` | **`3`** | `git`／`cat-file`／**`left-right`** |
| `218` | `-218R_定義框與清單正本…` | ✅ | `6` | `18` | **`2`** | `cat-file`／**`left-right`** |
| `220` | `-220R_框候選出艙…` | 🔴 **否** | — | — | — | **不可回測** |
| `221` | `-221R_本單入倉正字化…` | ✅ | `6` | `18` | **`1`** | `git` |
| `222` | `-222R_三候選框補正…` | ✅ | `6` | `21` | **`1`** | `cat-file` |
| `223` | `-223R2_修訂…` | ✅ | `7` | `17` | **`2`** | `cat-file`／`import` |
| `223` | `-223R_框之落地…` | ✅ | `7` | `17` | **`0`** | — |

🔑 **`217` 與 `218` 皆缺 `left-right`** ⇒ **獨立復現 `自誤 276` 之族**（其受詞為 `-220R`）。

### b　🛑 判定組為 `0` 之報告（該閘於其上**恆綠·判別力為零**）

**`11`** 份：`200`／`201`／`203`／`205`／`206`／`208`×2／`209`／`210`×2／`211`。
🔒 **成因（逐份相同·當場復現）**：其**單之 `【驗收】` 段<u>不存在</u>**
（段內反引號 token ＝ `0`／ASCII 詞 ＝ `0`）——非「段存在而缺 token」。
🔴 **不可回測** ＝ **`4`** 份（`214`／`215`／`216`／`220`）——其**單⛔ 在倉**。
⇒ 🔒 **有效判定者 ＝ `6` 份**（`21` − `11` − `4`）。

🔒 **對照組（器非紅）**：`218` 判定組缺項 ＝ `2`（已知應 > `0`）／`223R` ＝ `0`（已知應 ＝ `0`）。
🔒 **自證**：`--selftest` `rc ＝ 0`（必命中 `1`／必不命中 `0`）。

### c　⛔ 於本批接阻擋 `commit` ✅（僅擴回測與出艙）

---

## §五　所鑄各號之推導式

| 號 | 受詞 | 推導式（一句） | 責 |
|---|---|---|---|
| `自誤 282` | 轉述時抹除保留語 | `-220R:211` 逐字「**餘之未落地**…**既具名待落地項**」而轉述作「已結案」⇒ 量詞與限定詞互斥 | **發單側** |

---

## §六　閘

### 閘 `1`〜`6`：append-only 位元組級自證

`CLAUDE.md`（payload **`5621`** B）／`K-6` 正典（**`2651`** B）／自誤登記（**`3273`** B）
——三者 `Δ−payload` 全 **`0`**、嚴格前綴 ✅、檔尾 `≡` payload ✅、`CR` ＝ **`0`**、哨兵欄各 ✅ `1`。
新檔 `-224` 單：`Δ ＝ 索引 B ＝ 工作樹 B ＝ 5239`、`CR ＝ 0`。

### 閘 `7`：反向框重算 ＋ 三數併載

| 框 | 落地前 相異｜列命中／重咬／純單筆 | payload 內 | 落地後 相異／`MAX`｜列命中／重咬／純單筆 | 缺號 | 期望 | 判 |
|---|---|---|---|---|---|---|
| 舊框（基準） | `274`｜`272`／`3`／**`269`** | 相異 `1`（`[282]`）／列命中 `1` | **`275`／`282`**｜`273`／`3`／**`270`** | **`[106]`** | `275`／`273` | ✅ |
| **正字框 `A`** | `274`｜`271`／`3`／**`268`** | 同上 | **`275`／`282`**｜`272`／`3`／**`269`** | **`[106]`** | `275`／`272` | ✅ |

🔒 **次號 ＝ `283`**（二框一致）。

### 閘 `8`：`GB-147` 處置一 ⇒ 三 payload 之哨兵字樣命中合計 ＝ **`0`** ✅

### 閘 P ／ `left-right` ／ `deletions`

五檔 `sha256` 落地前後**逐位相符**（`app.py` `1fc5de29…`／`selection_pipeline` `6c678342…`／
`run_verification` `e48f24c8…`／`stepg_pipeline` `02602733…`／`run_all` `4ba89fef…`）。
🔒 **`left-right` ＝ `0 0`**。

🛑 **`deletions` 之逐檔（驗收所令之 `= 0` 於一檔<u>不符</u>·照實）**

| 檔 | `+` | `−` |
|---|---|---|
| `CLAUDE.md` | `84` | **`0`** |
| `docs/orders/W-G.9-224_…md` | `69` | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `59` | **`0`** |
| `docs/rulings/K-6_…md` | `43` | **`0`** |
| 🔴 `verify/tools/wg9223_acceptance_audit.py` | `4` | **`2`** |

🔒 **該 `2` 行 deletions 之<u>逐行理由</u>**（`W-G.9-219 c` 之體例）：

| 被刪之行（逐字） | 理由 |
|---|---|
| `    """自單之 \`【驗收】\` 段取出字樣（資料驅動）。回傳 (字樣 list, 段之列範圍)."""` | docstring 之回傳說明**與實際元數不符**（實為 `3`）⇒ 以 `3` 行正確之 docstring 取代 |
| `        return [], (None, None)` | 🔴 **早退路徑回 `2` 元組而呼叫端期待 `3`** ⇒ 無 `【驗收】` 段之單即 `ValueError` 崩 ⇒ 以 `return [], [], (None, None)` 取代 |

🔒 **`verify/tools/` ⛔ 生產碼五檔** ⇒ **閘 P 不受影響**；修正後 `--selftest` `rc ＝ 0`。

---

## §七　🛑 候裁／未辦

1. 🔴 **`W-G.9-219`（含生產碼）仍待執行**——另立批次；完成自驗後**停**，⛔ 未經 KL 逐字放行即推主線。
2. 🔴 **回掃之母體不可得**——發單側向 KL 之轉述不在倉 ⇒ 欲使 `自誤 282` 之通則可機檢，
   **發單側須將其結論性轉述入倉**（候發單側裁）。
3. 🟡 **判定組為 `0` 之 `11` 份**：其單之 `【驗收】` 段不存在 ⇒ 機檢器於其上恆綠；
   欲補之須回溯補寫該段（⛔ 追改既落之單）或於正字令爾後之單必含該段（候裁）。
4. 🟡 **不可回測之 `4` 份**（`214`／`215`／`216`／`220`）——其單⛔ 在倉（`-222R §四 b` 判為類別三）。
5. 🟡 **機檢器之可攜性界限**：須於倉內執行（`_repo()` 依 cwd）；本批⛔ 改其碼。
6. 🟡 **已失效哨兵 `3` 族**／正典 `29` 列「尚未／未實作」型陳述 ⛔ 復核——併期末批。
