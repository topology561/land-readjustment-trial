# 🧾 `W-G.9-183`　**CC 交接文**（新 session 接手用·`W-G.9-182R` 收束時出）

**出文窗**：CC（KL 令換窗）
**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ 不鑄任何自誤／`GB`／`VR`／戒號

---

## ✅ `§零`　**最要緊之一件事**（⛔ 讀完再動任何東西·**常規九**）

🔒 **本窗之全部成果<u>皆已 push</u>，⛔ 無任何未 push 之 commit。**

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart` | **`d034e5944cfb23943660b25fa4e9e8a3fd7885c0`** |
| 本地未 push 之 commit | **無**（`left-right` ＝ `0 0`·工作 clone 已刪） |
| **`app.py` blob** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`**（`1352132` B／`22922` 列／`sha16 7c290b6f55f83a24`） |

🛑 **接手者之第一判斷**（**常規九 三**：開工閘 ⛔ 不得以本檔所載之 commit 為錨）：
`git ls-remote origin refs/heads/wip/s1-endpart` ⇒
- ＝ **`d034e594…` 或其後裔** ⇒ 照 `§一` 正常開工。
- **其他值** ⇒ 🛑 **停機**上呈（本窗收束時⛔ 無未 push 之物，故任何落後皆非本窗所留）。

🔒 **`app.py` 自 `W-G.9-169 補正①`（`926a0de`）起<u>七批未動</u>**——本窗 `-176`〜`-182` **全批零生產碼**。

---

## `§一`　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（態 ＝ `d034e594`：**`70877`** B／**`761`** 列／`sha16 7d6b231719805338`）。
   🔴 **末端已增**：`常規一`〜`常規九` ＋ `常規四（七)(八)(九)` ＋ 二補款 ＋ **`W-G.9-179` 之「待落地清單之補列」**（族②③ 歸 `W-G.9-5`）。
   🔒 `CLAUDE.md` **在 `wg942` 之 `TARGETS` 內** ⇒ 一律**末端追加**、⛔ 不得就地改字。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （**`3632`** 列／**`258295`** B／`sha16 6a5cada2901a10e8`·`grep -c "^#"` ⇒ **`196`**）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**：`git -c core.autocrlf=false clone --branch wip/s1-endpart <public url> C:\wNNNa` ⇒
   `cd` ⇒ `git config core.autocrlf false` ＋ `user.name diag` ／ `user.email diag@local`
   ⇒ **對拍 `HEAD` 全 `40` 碼與 `app.py` blob**。
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、⛔ 不得放 scratchpad；自檢器與 payload 置於**另一**短路徑（本窗用 `C:\wNNNx`）⇒ **母體 `rglob` 自然自扣**（`S-4`）。跑畢二者皆刪。
4. 🛑 **長跑（`run_all`）一律於<u>另一個</u>拋棄式 clone**（`GB-94`：`run_all` 會改寫 `verify/out/**` 之**已追蹤** log）。
5. 🔴 **harness worktree 之陷阱**（本窗開工時實測）：Claude Code 之 worktree **自 `main` 開分支** ⇒ 其工作區**落後 `515` commit**，自動載入之 `CLAUDE.md` 係 **`main` 態之 `190` 列**（⛔ 非工作態之 `761` 列）。
   ⇒ 🔒 **⛔ 不得直接 `cat` worktree 之任何檔**；一律 `git show <工作分支 rev>:<path>`，或於 clone 內讀。

---

## `§二`　倉態與取號現查（態 ＝ `d034e594…`·CC 現查·量測時點 `2026-08-30`）

### `§二-1`　三數（框 ＝ `splitlines()`·CRLF 皆 `0`）

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1352132` | `22922` | `7c290b6f55f83a24` |
| `CLAUDE.md` | `70877` | `761` | `7d6b231719805338` |
| `docs/rulings/K-6_…md` | `258295` | `3632` | `6a5cada2901a10e8` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `386213` | `4243` | `52ca7315cf63dbe7` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `509346` | `3139` | `89305e453d8aab41` |
| `docs/驗證裁定登記表.md` | `330984` | `4229` | `bc60869c80aa4858` |
| `docs/reports/W-G.9-182R_X3解除與R1逐帶重建.md` | `24014` | `276` | `618a1953c2048d49` |
| `verify/probes/probe_WG9182_R1_bands.py` | `16702` | `345` | `3ddbc270efebc52e` |
| `verify/out/probe_WG9182_R1_bands_9fe070a2.log` | `9166` | `99` | `0509466cfc59912d` |

⚠️ `app.py` 檔尾**⛔ 無 `LF`** ⇒ `wc -l` 得 `22921`（差恰 `1`·`VR-057`）。其餘八檔二框同值。

### `§二-2`　取號（母體 ＝ 全倉 `.md` **`460`** 檔·**已扣量測器自身**：含 `183` 之檔名 ＝ `[]`）

| 類 | 定義列式（逐字） | 相異 | **MAX** | 次號 | 缺號 |
|---|---|---|---|---|---|
| `docs/reports` 之 `W-G.9-NNN` | 檔名 `^W-G\.9-(\d{1,4})` | `133` | **`182`** | **`183`**（本檔） | `49` 個 |
| 自誤·框①**單檔** | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `184` | **`219`** | **`220`** | `35`（自檢 `219−35==184` True） |
| 自誤·框②**全倉** | 同 | `188` | **`219`** | — | `31`（自檢 True） |
| `GB`（**三框聯集**·母體 ＝ `W-G.4` **單檔**） | 式表 `` ^\| *\*\*`?GB-N`?\*\* `` `119`／式A `` ^## `GB-N` `` `3`／式B `` ^### `GB-N` `` `36`(相異 `35`) | `122` | **`124`** | **`125`** | `[12, 87]`（自檢 `124−2==122` True） |
| **`VR`**（定義列式·母體 ＝ `docs/驗證裁定登記表.md` **單檔**） | `` ^## `VR-([0-9]{1,4})` `` | **見下 🔴** | **見下 🔴** | **見下 🔴** | 見下 |
| 戒（**裸框** `戒 \`?N\`?`） | — | — | **`35`** | **`36`** | — |
| 常規（`^\*\*常規(.)`·母體 ＝ `CLAUDE.md` 單檔） | — | — | **九** | — | — |

**次號之占用現查**（錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·二形分報·三數 ＝ 命中列數／出現次數／檔數）：
`W-G.9-183` ＝ `0/0/0`（對照組 `W-G.9-182` ＝ `11/13/3` ⇒ 量測器非紅）；`自誤 220` 二形皆 `0/0/0`；`GB-125` ＝ `0/0/0`；`VR-085` ＝ `0/0/0`；`戒 36` 二形皆 `0/0/0`。
🆕 **`S-4` 人造受詞**：`自誤 3182` ⇒ `0/0/0`（**已作廢 `10` 項之外**）🛑 **本檔出艙後即作廢** ⇒ **清單增為 `11` 項**。

### `§二-3`　🔴 **`VR` 取號之污染（本窗所生·<u>須下一單處置</u>）**

🛑 **`VR` 之定義列式現得 MAX ＝ `999`**（相異 `70`／缺號數 `929`／自檢 `999−929==70` True）。

🔒 **成因逐字**：`docs/驗證裁定登記表.md:4202` ＝
> `## \`VR-999\` 之性質澄清：係**哨兵**、⛔ 非實鑄之裁定（claude.ai 裁 \`2026-08-30\`·\`W-G.9-180\`）`

其入倉之 commit ＝ **`c255ca4`（`W-G.9-180R`）**。該標題形**恰符** `VR` 之定義列式 `` ^## `VR-NNN` `` ⇒ **被計入**。

🔒 **扣除 `999` 後之真值**：相異 **`69`**／MAX **`84`**／缺號數 `15`（自檢 `84−15==69` True）；缺號集 ＝ `[1..13, 73, 75]`。
⇒ 🔒 **`VR` 之真實次號 ＝ `VR-085`**（⛔ 非 `VR-1000`）。

🔴 **其諷刺與嚴重性**：`W-G.9-180 §三 P-2` 逐字警告「若日後有窗以**裸框**取號，將取到 `VR-1000`」——而該澄清**自身之標題形**使**定義列式**亦得 `999`。⇒ **風險由裸框搬到了定義列式**，而定義列式正是 `常規四（八）` 所令之**唯一**正確框 ⇒ **反而更危險**。
🩸 **歸責**：發單側供形（`P-2`）＋ CC ⛔ 未察覺而照抄入倉。🔒 **CC ⛔ 未自鑄任何自誤號**（本檔⛔ 不鑄號）⇒ 列為下一單之受詞（`§四 1`）。
🛑 **接手者之防線**：**凡取 `VR` 號，一律先扣除 `999`**，或改以「排除已知哨兵」之框；⛔ 不得逕用定義列式之 MAX。

🔒 **新形之教訓（建議立戒·⛔ 本檔不立）**：**「非鑄號之澄清／加註」⛔ 不得使用「鑄號之定義列式形」**——`常規四（九）四` 令鑄號取既有範本，其**反面**同樣成立：非鑄號者⛔ 不得取該形。

---

## `§三`　本波之位置（`-176` 〜 `-182`·**全數已 push**·全批**零生產碼**）

| 單 | commit | 要旨 |
|---|---|---|
| `-176R` | `a44eb82` | 二處就地更正 ＋ `自誤 212` ＋ `R2` `0.1171` 之**逐宗定位** |
| `-177R` | `666e0bd` | `自誤 213`／`214` ＋ `GB-121` ＋ 🔑 **`R2` `0.1171` 歸因完成** |
| `-178R` | `4b20b2d` | `自誤 215`／`216` ＋ `GB-122` ＋ 🔑 **`G.3` 判 ＝ 第四支 (丁)** |
| `-179R` | `b850af5` | 🔓 **`G.3` 停機解除（`VR-084`）** ＋ 🔑 **`GB-123`** ＋ `MEMORY.md` 聚合 `−81%` |
| `-180R` | `c255ca4` | `自誤 217`＋戒 `33` ＋ 🔑 **`GB-123` 嚴重度界定** ＋ `VR-999` 澄清（🔴 見 `§二-3`）＋ 已作廢清單落地 |
| `-181R` | `9fe070a` | 🛑 **停機上呈**（`X-3` 之恆等對含 5b 配餘之街廓結構上不成立） |
| `-182R` | `d034e59` | `自誤 218`／`219` ＋ 戒 `34`／`35` ＋ **`GB-124`** ＋ 🔑 **`R1`／`R4` 二問分別了結** |

### 🔑 本波之**六條結論鏈**（下一單之全部前提）

**① `R2` 之 `0.1171` ＝ 族②（`_W_prev_*` 初值）**（`-177R`）
app 用**舊式** `buffer_S × cos_dn ＝ 8.674404`（`app.py:20500-1`），stepg 用 `0.0`（`stepg:628-9`）。碼面自陳見 **`app.py:9117`**：「app 之族②③（`_W_prev_*`／`_select_pool_slot` 之 `'b'`）**仍取舊式**」。
🔒 **單一變因令三量同時收斂**：池帶2 寬 `33.0811→32.9640`（`|Δ|=0.0000441`）／退化帶 `2→3`／`merged` `3→4` 段。落點 ＝ **甲**（池帶2 **下界**）。
🔒 **族③**（`_select_pool_slot['b']`）於 `R2` **零後果**（`k*` 二式皆 `7`）⇒ 降候選。
⇒ **切換歸 `W-G.9-5`（contract 側）**·**已裁而未遷移**·⛔ 非待裁項（`CLAUDE.md` 末端已補列）。

**② `G.3` 停機已解除 ＝ 第四支 (丁)**（`-178R`／`VR-084`）
`G.3`（`verify/wg_g3.py`）之射程**含** Step-G 之池（`wf_f0` 之 `池_v3(㎡)` ← `app.py:13422` `"poolA": _need("f3_wd2_pool_diag")`），**但其二端皆 harness 側**（`got` 之種子 ＝ `run_step_g`；`base` ＝ `run_verification.py:420` 之 `_bake_csv`）⇒ **app-vs-shim 之差結構上⛔ 不可能被抓到**。
併「執行於 `R2` 前即中止」（凍存終止點 `stepg:995`）與「本不在任何自動流程內」（`W-G.9-6 §6`）三層獨立成立 ⇒ **逐 commit 二分 ⛔ 不必要**。

**③ `GB-123`：生產路徑之池 ⛔ 無任何自動守備**（`-179R`／`-180R`）
母體逐字：`verify/**/*.py` ＝ `224` 檔／`run_all` 實掛 `21` 項／含 `baselines` 者 `74` 檔 ⇒ 其 `got` 端經 `app.py:main()` 產生者 ＝ **`0`**。機制錨 ＝ `CLAUDE.md:58`（「`main()` 內之敘述**從不被 `run_all` 執行**」）／`:298`。
🔒 **嚴重度已界定（`-180R`）**：**降但⛔ 不降至可接受**——池之**唯一人工錨** ＝ `stdout.log` 之 `[T2-DIAG]`（**終端機輸出**）；**四份匯出 CSV ⛔ 無一含池**（`W-D.1.3-d驗收_抵費地情形.csv` 僅 `1` 列且其 `308.17` 係**街角規定範圍面積**·⛔ 非池）；覆蓋 ＝ **`1` 次**（`W-G.9-174R`）／⛔ 無排程／態⛔ 不可自證（`GB-119`）。
🔒 **解法三候選**（`-180R §四-4`·⛔ 未實作）：**CC 推薦 (a) KL 產物之凍存對拍**（新增 `fixture_klui_t2diag.py`·⛔ 不動生產碼·⛔ 不需 KL 實跑）⚠️ **惟須於 `run_all` 掛載清單新增一列 ⇒ 其實作須另單明示授權**（`-182 §六 三` 仍⛔ 不授權）。

**④ `R1`／`R4`「宗數減而 Σ池 不動」已完全了結**（`-182R`·**二問分別作答**）
- **問甲（寬增 `55×`）** ⇒ ⛔ **非 span 虛胖**（實測 **`span/實佔 ≡ 1.0000`·`8/8` 格**；各帶皆單一 `Polygon`·**投影保持連通** ⇒ s-span 恆等實佔長）。真因 ＝ **`kept_iv` 之區間可與宗帶重疊**：`R1` 配餘帶2 `s∈[-0.3248, 17.5649]` **與宗 `628-36(1)` 之 s 區間<u>完全相同</u>**。
- **問乙（面積幾乎不變）** ⇒ 🔑 **`(甲)` 成立**：自 **`build_p`**（**未經 `VR-074` 過濾**）直查得 `_GHOST_(R1)`／`_GHOST_(R4)`，其 **`幾何面積_m2` ＝ `0`** ⇒ 消失之宗確為**零面積 ghost**。
🔴 **閘 `F` 之二讀**（受詞須分辨）：`biz_polys`（**已過 `VR-074`**）得 `0` ⇒ ⛔ 不支持 (甲)；**`build_p`（未過濾）得 `1` ⇒ 支持** ⇒ **閘 F 之受詞應為 `build_p`**。

**⑤ `GB-124`：診斷欄 `池帶 s 寬` 之語意陷阱**（`-182R`）
其取自 `_strip_s_range`（**頂點 span**），且 5b 之後 **`kept_iv` ⛔ 無互斥保證** ⇒ 該欄之**和⛔ 無守恆意義**。
🔒 **CC 已限縮其案由**：實測 `span/實佔 ≡ 1.0000`（`8/8`）⇒ ①（凹形 span 虛胖）**⛔ 未出現**，成立者**僅 ②（帶間重疊）**。

**⑥ 覆蓋自檢替代式之驗證**（`-182R M-G-3`·🛑 **⛔ 尚未立法**）

| 式 | `R1` 殘差 | `R4` 殘差 | 判 |
|---|---|---|---|
| (i) 步驟4補區間 ＋ merged宗帶 ＋ 退化帶 ＝ `s` 域全寬 | `0.0` | `0.0` | ✅ 成立（惟其受詞⛔ 未印於 `[T2-DIAG]`） |
| **(ii) `measure(∪kept_iv ∪ ∪biz_iv)` ＝ `s` 域全寬** | `0.0` | `0.0` | ✅ **最宜**（對重疊免疫） |
| (iii) Σ實佔長 ＝ `s` 域全寬 | `−18.58` | `−94.68` | 🔴 ⛔ 不成立（對重疊重複計數） |
| 舊 `X-3`（Σspan ≤ `s` 域全寬） | ✅ | 🔴 超出 `31.6294` | 🔴 已**撤除而不替代** |

🛑 依 **`戒 35`**（新閘須先以既有資料驗其成立方得立為停機款）：式(ii) **僅於 `R1`／`R4` 驗過** ⇒ **推廣為閘前須先擴大母體**（`R2`／`R3`／`R5`／`R6`）。

---

## 🛑 `§四`　下一單之受詞（由發單側落筆·**依優先序**）

1. 🔴 **`VR` 取號之污染**（`§二-3`）——`VR-999` 之澄清標題形使定義列式 MAX 成 `999`。須裁其處置（末端追加一則「取號時須扣除」之施行式？改標題形？）＋ 是否鑄號。**⛔ 不得放著**——下一個取 `VR` 號之窗若機械現查將取到 `VR-1000`。
2. 🔒 **式(ii) 之擴大驗證與立法**（結論鏈 ⑥）：於 `R2`／`R3`／`R5`／`R6` 補驗，殘差全 `0` 方得依 `戒 35` 立為閘。
3. 🔒 **`GB-123` 之解法**：候選 (a) 之實作**須另單明示授權**（涉 `run_all` 掛載清單）。
4. 🟡 **`+0.0045`／`+0.0035` 之殘量⛔ 未精確歸因**——已知其⛔ 非 ghost 之面積（＝`0`），係 ghost 排除後**配餘形狀微變**所致；**精確歸因須跨態重跑**（迄今⛔ 未授權）。
5. 🔒 **波末重烤**（baseline 九份 ＋ 凍存名單 ＋ `GB-111`／`GB-113`）——**須 KL 決時點**（已裁「俟二閘落地後」）。
6. 🔒 **待落地清單**（`CLAUDE.md` 之「待落地清單之更正二」＋ `W-G.9-179` 之補列）：`K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17` 重排／遞補（**幾何影響批·須另開新窗**）→ `K-9-12` 矩形容納（**同批**）→ 族②③ 切換（歸 `W-G.9-5`）→ 步驟 0 `裁 A`／`裁 B` → `K-6-A2` 剩餘六項 → `K-9-22` → `K-6-B` 段一〜段四 → `K-6 §四` → 波末批 → 泛化波。

---

## `§五`　積欠（🛑 下一登記批一次入倉）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104`（`_block_strip` 雙線⛔ 無交叉閘之 loud 斷言） | ⬜ 未辦 |
| `2` | `GB-107`（`wg960_z_selfcheck.py:12` 寫死絕對路徑·**本窗四度實測仍在**） | ⬜ 未修 |
| `3` | `GB-108`／`GB-110`（八格表⛔ 無判別力） | ⬜ 未辦 |
| `4` | `GB-111`（凍存期望 FAIL 名單已失效·**須 KL 決重產時點**） | ⬜ |
| `5` | `GB-112`〜`GB-120` | ⬜ 只登記 |
| `6` | **`GB-121`**（施工單之逐字⛔ 不入倉 ⇒ 停機款字面事後⛔ 不可現查）·⬜ 未解：**施工單全文入倉之機制須 KL 裁** |
| `7` | **`GB-122`**（`st.error` vs `RuntimeError` 之控制流分歧）／**`GB-123`**（app 池無自動守備）／**`GB-124`**（`池帶 s 寬` 語意） | ⬜ 只登記 |
| `8` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `9` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `10` | `K-9-22` 落地／`VR-074` 之他層／`VR-075` 落地 | ⬜ |
| `11` | 🆕 **`VR` 取號污染之處置**（`§二-3`） | ⬜ **本窗新生** |

---

## `§六`　🔑 本波所立之家法與陷阱（🛑 接手者必讀）

1. 🔴 **停機款觸發<u>一律先停機</u>**（`W-G.9-177 §六 一`）：⛔ 不得先辦後呈；於停機回報中**併陳擬辦之意見**。本窗於 `-181R` 施用（`X-3` 觸發 ⇒ 整批停機、⛔ 未辦 `N-1`／`N-2`／`M-F-3`），發單側判其**完全正確**。
2. 🔴 **`X` 款「對照組」之預設判準**（`W-G.9-181 §零`）：**整體**（二形至少一形非 `0`）＋ **兩造自證**（人造受詞得 `0/0/0` ＋ 一已知應非零者得非 `0`）⇒ 量測器非紅、⛔ 不觸停機。**自該單起⛔ 不再構成歧義。**
3. 🔴 **`戒 35`：新閘須先以既有資料驗其成立，方得立為停機款。** 凡欲立「恆等式／守恆式／不變量」型之閘，須附 ① 驗證母體 ② 結構性理由；二者之一闕如 ⇒ **只得立為「受檢之假說」**。
   🛑 **配套**：停機款之措辭⛔ 不得排除「本款自身不成立」之可能（⛔ 不得寫「若不成立則**必**係 X」）。
4. 🔴 **`S-4` 人造受詞<u>一經出艙即作廢</u>·權威在<u>清單</u>⛔ 不在量測**（補款三／四）。
   🛑 **現有 `11` 項**（含本檔之 `自誤 3182`）：`自誤 9997`／`GB-997`／`自誤 8811`／`GB-883`／`VR-999`／`VR-751`／`GB-746`／`自誤 6205`／`自誤 4907`／`GB-357`／`自誤 3182`。清單在 `docs/驗證裁定登記表.md` 之「已作廢之人造受詞清單」節。
   ⚠️ **`自誤 8811` 曾三數為 `0` 而意指已出艙** ⇒ ⛔ 不得僅憑三數為 `0` 即用。
5. 🔴 **取號之「本案自身之配給」vs「他案之占用」**（`W-G.9-182 §七`）：`自誤 218`／戒 `34` 之意指占用**唯一來源** ＝ `-181R` 之呈裁文（**本案自身**）⇒ **⛔ 不順延**。反面先例 ＝ `CLAUDE.md:664` 之 `VR-073`（**他案**停機 payload ⇒ 順延正確）。
6. 🔴 **`grep` 之負存在斷言 ⛔ 不得接 `head`／`tail`**——本窗曾因 `| head -12` 被前一檔之 `10` 個命中占滿而誤判「`_cos_dn` 只在 `app.py`」（實則 `stepg:564` 同式）。
7. 🔴 **參數值之斷言須 spy <u>實收</u>**——本窗曾由 stepg 之 `_side=` 推定 `_solve_G_one` 之 `side` 為 `'left'`，實測其相異值 ＝ **`['無']`** ⇒ 注入命中 `0` 而印出漂亮之「零變化」。
   🛑 **判準**：**計數為 `0` 而結論為「無變化」者，一律先當作 patch 沒咬到。**
8. 🔴 **探針之共用鍵會被後一街廓覆寫**——本窗 `occ_len` 誤用 `CAP["_dh"]`，`R4` 攔截時覆寫之 ⇒ 印 `R1` 時吃到 `R4` 之軸。
   🛑 **判準**：**單一 `Polygon` 之 `span/實佔比 ≠ 1` 即量測器紅之徵**（投影保持連通 ⇒ 二者必等）。
9. 🔴 **`bytes` 相同 ⛔ 不等於內容相同**——`KL_UI_3.5m_*_W-D.1.2診斷_逐候選評點明細.csv` 二態 bytes 皆 `2783` 而 `sha256` 相異。**須驗 `sha256`。**
10. 🔴 **一張表只准一個框**（`戒 29`）：`wg942` 所報之三數係**字元數**（`len(str)`），⛔ 非位元組。二框皆證嚴格前綴，⛔ 不得混列。
11. 🔴 **`wg942` 對 `HEAD` 比 ⇒ 必於 `commit` <u>前</u>跑**（入倉後 `HEAD` 前進、比較端變本批自身而恆綠·考古節 `123`）。
12. 🔴 **`覆蓋殘差` 之 `0` 係<u>構造恆真</u>**（`app.py` 之 5b 註解逐字：「本項使 `①' 覆蓋閘` 成為**構造恆真**……⛔ **該閘之 `0` 自本批起不得作為任何驗收憑據**；⛔ **但不得刪除／停用／標作廢**」）⇒ **⛔ 不得讀為「自檢未察覺」**。
13. 🩸 **heredoc 吃反斜線**（`SyntaxWarning: invalid escape sequence`）——凡含 `\` 之樣式一律**自檔案管線抽出並 `eval` 其字面**；markdown 之反引號用**雙反引號**寫法，⛔ 不用 `\``。
14. 🩸 **全形括號 `）` 於 shell 會致解析錯**；`&&` 鏈遇 `grep` 命中 `0`（`rc=1`）即中斷 ⇒ 負存在查詢**⛔ 不以 `&&` 串接**。
15. 🩸 **Git Bash 之 `/tmp` 與 Windows Python 之 `/tmp` ⛔ 非同一目錄** ⇒ 跨二者傳檔一律用 `/c/xxx` ＝ `C:/xxx`。

---

## `§七`　已知 harness 限制

- 🔴 **`run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止** ⇒ `wf_f0`〜`f4` **主路徑從不執行**。
- 🔴 **`app.py:main()` 內之敘述從不被 `run_all` 執行**（`CLAUDE.md:58`／`:298`）⇒ **app 路徑之池呼叫端 `app.py:20892` 在 `main()` 內** ⇒ **shim 結構上到不了**。
- 🔴 **`run_step_g` 於 `R2` 之結構閘 telescoping 破終止**（`GB-118`）——**惟<u>逐街廓</u>呼叫可繞過**：`run_step_g(ns, …, [tp for tp in build_p if tp.get("所屬街廓")==<blk>], …)`（範本 ＝ `probe_D2b24C_pool.py`）⇒ 本窗 `-176`〜`-182` 之探針皆賴此法取得 `R1`／`R2`／`R4`。
- 🔴 **`wg_g3.py` 本不在任何自動流程內**；`run_all` 所掛者係 `fixture_e2e_termination.py`，其守**終止點**（`raise` 點＋型別＋訊息）而**⛔ 非其綠否**。凍存 ＝ `verify/out/WG99_端到端複本_終止點凍存_投影.txt`。
- 🔒 **`run_all` 於基座即 `FAIL`**（**准紅碼**）；且會改寫 `verify/out/**` 之**已追蹤** log（`GB-94`·實測 `3` 檔）。
- 🔒 **KL 之 UI 係目前唯一可達完整終態之儀器**；其 `[T2-DIAG]` 印出**未捨入**之全部池量（`verify/out/KL_UI_3.5m_*`·`10` 檔 ＝ `2` 態 × `5` 產物）。

---

## `§八`　memory 之指引（新 session 自動載入 `MEMORY.md`）

🔒 **`MEMORY.md` 已於 `W-G.9-179R` 聚合**：`44899 → 8741` B（`−81%`）／`73 → 16` 行；**只改索引行·`73` 個 memory 檔全在⛔ 未刪** ⇒ 零丟失、可逆（聚合表存於 `W-G.9-179R §五-1`）。

🛑 **施行式（`W-G.9-182 §一 N-2 2` 所令·逐字）**：
> `MEMORY.md` 之主題行係**併看入口**；查具體主題時**須循主檔之併看清單再讀該檔**。

🔒 **可檢索性已量**（`-180R §三-1`）：`56` 個非主檔之**檔名字面涵蓋率 ＝ `56/56`**（未涵蓋 `[]`）⇒ `grep <檔名>` 於 `MEMORY.md` 仍命中。
🔒 **保留不聚合之 `4` 條**（每窗必讀）：`push-before-shipping-claim`／`grep-multibyte-charclass-trap`／`wg9143-handoff-entry`（**主線入口**）／`harness-worktree-crlf-trap`。

---

## `§九`　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值、⛔ 不得改框／母體／容差使其相符。
- 🔒 **拋棄式 clone 跑畢整份刪除**並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push（① 零生產碼 ② `deletions = 0` 或既有檔純附加 ③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠）。🛑 惟施工單另令「須請示」者以單為準；🛑 **生產碼批⛔ 不得逕行**。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`（期望 `rc=0`·`stderr` `0` B·逐字「結論：✅ 全部成立」·受檢 **`77`** 筆·`嚴格前綴＝True` ×`77`）。🛑 **於 `commit` 前跑**，並依 memory ㉞ 驗本批所改之檔確在受檢母體內（查「無此檔」之命中須 `0`）。
- 🔒 **新探針入倉前必過** `verify/run_all.py` 之「禁寫死本機絕對路徑」全倉機檢（**本批新探針**命中須 `0`·附判別力對照）。⚠️ 全倉現有 **`4`** 個**既存**命中（`probe_WG971_wrap.py:203`／`:247`／`tools/nameset_diff.py:19`／`tools/wg960_z_selfcheck.py:12`）⇒ 「命中須 `0`」之受詞係**本批新增者**、⛔ 非全倉。
- 🔒 **依 `GB-121` 之施行式**：施工單之**停機款**須於報告 `§一` **逐字引錄其原文**（⛔ 非摘要），並標 `【單】` ＋ 其來源管線。
- 🔒 **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
