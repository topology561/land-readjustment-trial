# `W-G.9-275R`　補令一之執行（**舊窗收工批**：坑 `bb` 之鑄 ／ 交接註 ／ 收工 ⇒ **停 ⇒ 本窗封存**）

> **態**｜開工 ＝ `36f50f8d82228a8d8ed3f7f6f63b758b5a24546d`（`ls-remote` 實查）｜工項零後 ＝ `8ad54b2`
> **級** ＝ **輕**（純登記 ＋ 交接註·**⛔ 長跑**·**零生產碼**）
> 🛑 **本批⛔ 辦**（單 `§八`）：⛔ 動生產碼 `34` 檔一字／⛔ 修 `GB-167`·`GB-168`／
> **⛔ 驅動任何長跑**（含 `run_verification`、任何雙態驅動）／⛔ 落地 `K-9-29` 任何子項／
> `GB-162` 三禁令維持／**⛔ 補測 `k956` 之呼叫次數**（留予新窗）／**⛔ 判孰為正確之 `W`**／
> ⛔ 呈 KL 任何事／⛔ 併線 `p3a`。
>
> 🔒 **辦畢 ⇒ 停 ⇒ 本窗封存。**

---

## `§零`　開工閘與工項零

| # | 受詞 | 實測 | 判 |
|---|---|---|---|
| `1` | `ls-remote` 主線 | `36f50f8d82228a8d8ed3f7f6f63b758b5a24546d` | 🟢 |
| `2` | `HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 🟢 |
| `3` | `p3a` 非祖先 | `merge-base --is-ancestor` ⇒ **非祖先** | 🟢 |
| `4` | **`SELF_SHA256`**（`P-5` 內建口徑·受詞 **`8988`** B） | 實算 ＝ 單載 `6a5cefb6adb2ebbaf8c6b5ce203bdd8197ad82c6d413f1370e50fe5d53c0b948` | 🟢 |
| `5` | `probe_order_preflight.py` | **`rc = 0`**；機械停機款 **`0`** 項；🟡 提示 **`0`** 項 | 🟢 |
| `6` | 四簿 ＋ 坑 | 自誤 `368`／`378`｜`GB` `166`／`168`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `79`／`79` | 🟢 |

🔒 **閘 `4` 之判別力三造**：全檔 `3b366834…`（**不符** ✅）／⛔ 含該 `\n` 之舊口徑 `c8f556b2…`（**不符** ✅）／自比（**相符** ✅）。
🔒 **閘 `6` 之坑數係<u>引用</u>**：`79`／`79` 已於 `36f50f8` 之 commit 實測（**rev `dc7a68c`**·落檔
`verify/out/WG9274_pit_index_post.log`）⇒ **⛔ 重跑**（單 `§八` 款 `2`：本窗已飽和·⛔ 驅動任何長跑）。

**工項零**（commit `8ad54b2`）：所指之檔數 ＝ **`1`** ✅；`CR` ＝ **`0`**（`LF` `114`·bytes `9066`；
造甲真 CRLF ⇒ `CR = 2` ✅）；blob `sha1` ＝ `dd84adba2702e3699f94201173610b237deef90a`；
blob 內容 `sha256` ＝ 磁碟 `sha256` ＝ `3b366834b65f868dcc325f03c26ba10ff178cb2667bb135efaa8a8e4b5c02d67` ⇒ **逐位相同** 🟢。

---

## `§一`　工項 `1`：坑 `bb` 之鑄 ＋ `ALPHA_LABELS`→`bb` ＋ 三處框字串齊一

### 一-1　🆕 本波新增之坑 `bb`（補令一 `§四-1` **准鑄**·標籤續 `ba`·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `bb` | 🔴 **抽出「框內省」為輔助函式，而未補其自身之一層** ⇒ `sys._getframe(depth)` **整體偏移 `1`** | 🔴 **一整張表全欄「⛔ 可得」**——讀起來像「**管線未達該街廓**」，而實為**框差一層**；且**同一偏移可同時命中多個包裹**（本案 `_spy_ord` 與 `_spy_slot` 二者），致其餘列**靜默消失**；🛑 其⛔ 由 `rc` 顯形（`rc = 0`） | `(1)` 輔助函式內一律 `sys._getframe(depth + 1)`；`(2)` 附**自我驗證閘**——所錄之鍵須為**已知母體之非空子集**，含 `None` 即 **loud 拒測**（**⛔ 以「不可得」出艙**） |

🔒 **二款攔法已於 `W-G.9-274` 施行**：修後之四格自我驗證閘全綠
（所錄之 `blk` 皆為 `BLKS` 之成員且非空），三態表方得完整。

### 一-2　`ALPHA_LABELS`→`bb` ＋ **三處**框字串齊一（逐字自證）

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| `aa`〜`ba` 之替換（**字元框**） | `3` | **`3`** | ✅ |
| `aa`〜`bb` 改後命中 | `3` | **`3`** | ✅ |
| `aa`〜`ba` 改後殘留 | `0` | **`0`** | ✅ |
| `"bb"])` 改後命中 | `1` | **`1`** | ✅ |
| `"ba",` 改後命中（⛔ `0`） | `1` | **`1`** | ✅ |
| **`ALPHA_LABELS` 成員數**（**語法真值**） | `+1` | **`53` → `54`** | ✅ |
| **尾元** | `bb` | `'ba'` → **`'bb'`** | ✅ |

🔒 **波浪號之自證**（承坑 `ay`／`az`）：字元 `'〜'`·碼位 **`U+301C`**·與期望之字元**相符 `True`**
（其診斷自 `OLD.index(WAVE)` 取值·⛔ 另寫索引常數）。
🔒 **判別力**（寬字樣 `字母系`）＝ **`4`** ⇒ 該檢**⛔ 恆為 `3`**。
🔒 `py_compile` OK；`CR` ＝ `0`；`numstat` ＝ **`5`／`4`**（**就地更正**·⛔ 前綴式）。

### 一-3　🩸 本節之自捕（loud 攔下·**⛔ 寫回**·⛔ 出艙錯值）

🔴 **`ast.literal_eval` ⛔ 適用於含<u>運算式</u>之賦值**：`ALPHA_LABELS` 之賦值式為
**list comprehension ＋ `+`** ⇒ 首版得 `ValueError: malformed node or string … ListComp`（`rc = 1`）
⇒ **loud、⛔ 寫回**。
🔑 **`自誤 377` 之通則須再細一層**：其逐字為「一律以**語法真值**取之（`ast.literal_eval`／`ast.parse`）」
——實測顯示 **`literal_eval` 只及<u>字面</u>**；含運算式者須 **`compile` ＋ `exec` 該<u>單一</u> `Assign` 節點**。
🛑 **⛔ import 整個模組代之**——`verify/probes/probe_WG9269_pit_index.py` 之
`sys.exit(main())` **未被 `if __name__ == "__main__":` 守衛** ⇒ **import 即長跑**（違單 `§八` 款 `2`）。
🔒 **本則⛔ 鑄號**（單 `§七`：坑 `bb` **僅此一號**）——於本節具名即足。

---

## `§二`　🔴 一項**⛔ 採信之數**（發單側之裁·CC 採納）

> **`k956_W_from_mp` 於 `0m` 全流程「呼叫次數 ＝ `0`」——此數本批⛔ 採信。**

🔒 **發單側之由（逐字轉錄·⛔ 改寫）**：`_mp_base_W0` 係 `app.py` `main()` 內之**巢狀函式**，
其末行為 `return float(k956_W_from_mp(_bp0, _mp, _adir, _dv))` ——**以模組全域名解析**，⛔ 經 `ns[...]`。
而 CC 之間諜係**改寫 `ns["k956_W_from_mp"]`** ⇒ **該路徑之呼叫，間諜於構造上即看不見**
⇒ 其 `0` **可能為器之構造所致**，⛔ 為受詞之事實。

🛑 **其為「出艙一否定判而未備必命中之對照」** —— 對照組全零 ⇒ **先判量測器紅**。
🔒 **責亦在發單側**（`W-G.9-274 §四-1` 項 `4` 令了對位而未同時令該計數之判別力對照）。

🩸 **CC 之照實併呈（⛔ 補測·⛔ 自裁·單 `§八` 款 `4`）**
本批**⛔ 量測**，僅照實記一項**與該由相關之事實**：CC 之驅動器所走者為 **harness 路徑**
（`verify/stepg_pipeline.run_step_g`），**⛔ `app.py` 之 `main()`**；而 `stepg_pipeline` 內之
`_mp_base_W0` 其末行逐字為 `return float(_k956_W_from_mp(_bp0, _mp, _adir, _dv))`，
其 `_k956_W_from_mp` 係自 `ns` 取得。
⇒ 🛑 **發單側所舉之機制（`app.py main()` 之巢狀函式）於 CC 之驅動路徑上未必為 operative**；
**惟其<u>結論不受影響</u>**——該 `0` 因**判別力對照未備**而 ⛔ 採信，此點**⛔ 繫於路徑之別**。
🔒 **⛔ 就此作成任何量測或主張**；列為新窗之**首要量測**（見 `§三-7`）。

---

## `§三`　交接註（補令一 `§五` 之七項）

### 三-1　結案 commit 與本批逐 commit

| # | commit | 受詞 |
|---|---|---|
| `1` | `8ad54b2` | 工項零（本單原封入倉） |
| `2` | **本報告之 commit** | 工項 `1`（坑 `bb` ＋ `ALPHA_LABELS`→`bb`）＋ 工項 `2`（交接註） |
| `3` | **末一 commit** | 收工：坑之定義處清單自證（`通則 I`·報告入倉**後**量） |

🛑 **`常規九 三`**：接手窗之開工閘**⛔ 以本文所載之 commit 為錨**，一律以
`git ls-remote origin refs/heads/wip/s1-endpart` **實查**。

### 三-2　四簿期末 ＋ 坑之期末數 ＋ 次號

| 簿 | 框之出處 | 相異 | MAX | 缺號集 |
|---|---|---|---|---|
| **自誤** | `VR-091` 補款二 `①` ＋ 範圍框·母體 ＝ 登記表**單檔** | `368` | **`378`** | `[106, 355, 356, 357]` |
| **`GB`** | `VR-093` 三框聯集（扣哨兵 `997`）·母體 ＝ `W-G.4` 登記表單檔 | `166` | **`168`** | `[12, 87]` |
| **`VR`** | `VR-092` 就地加註所引（扣 `999`）·母體 ＝ `docs/驗證裁定登記表.md` 單檔 | `80` | `95` | `[73, 75]` |
| **`K-9`** | `W-G.9-266` 工項四 `四-1`·母體 ＝ `K-6` 單檔 | `28` | `29` | `[]` |

**坑之期末數** ＝ 見**末一 commit** 之訊息（`通則 I`：本報告含 `D-T` 形之坑表 ⇒ 其為受詞之一員）。
**次號**：自誤 **`379`**／`GB` **`169`**／`VR` **`96`**／`K-9` **`30`**；
**坑之下一標籤 ＝ `bc`**（🛑 `ALPHA_LABELS` 現行硬編**止於 `bb`**，新坑須**同批擴之**否則**靜默漏**）。

### 三-3　自限復驗

| 受詞 | 實 | 判 |
|---|---|---|
| `app.py` blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ 期初 ＝ 期末 |
| 生產碼判法四檔 | `numstat` 命中 `0` 行 | ✅ ⛔ 動一字 |
| 五個 `*_EXPECT` | `verify/wd4_tier_list.py` **⛔ 在本批 staged 之中** | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | **未併線** | ✅ |
| `WV_BAKE` | 未設 | ✅ |
| `K-9-29` 之任何子項 | 子項 `1` 之逐字錨＠`app.py` 仍 **`1`** ⇒ **⛔ 落地** | ✅ |
| `GB-167`／`GB-168` | **⛔ 修**（二則之原節一字未動） | ✅ |

### 三-4　本波新立之通則（**逐字**）

> **`(a)`　⛔ 以「未執行」與「已通過」共用出艙碼**（`自誤 378`）
> 凡以「對照態無紅」為端點或對照組者，須先出艙該閘（或該式）於對照態之**執行次數**；
> 次數為 `0` ⇒ 其為「未觸發」，**⛔ 得充「通過」、⛔ 得為端點**。
> 🔑 此係既有戒之**對偶**：既有戒禁「⛔ 以『無從判定』與『判定為偽』共用出艙碼」。

> **`(b)`　立對照前須先驗其落於量測式之<u>非飽和域</u>且能驅動受量欄位**（`自誤 345` 之再現）
> 承 `CLAUDE.md`：「**兩個近似值相符 ≠ 相互驗證**……須先確認該估計**對候選之間具鑑別力**」。

> **`(c)`　量結構化物件之基數一律取<u>語法真值</u>**（`自誤 377`）
> `⛔` 以文字層括號配對或 regex 代之。
> 🔑 **本批之補**：`ast.literal_eval` **只及<u>字面</u>**；含**運算式**（list comprehension／`+`）者，
> 須 `compile` ＋ `exec` **該單一 `Assign` 節點**；**⛔ import 整個模組代之**
> （未被 `__main__` 守衛之模組，import 即長跑）。

> **`(d)`　框內省抽為輔助函式須 `depth + 1` ＋ 自我驗證閘**（坑 `bb`）
> 其自我驗證閘 ＝ 所錄之鍵須為**已知母體之非空子集**，含 `None` 即 **loud 拒測**。

> **`(e)`　片段內須載之量，先留<u>佔位</u>、俟實測後填**
> ⛔ 先填後驗——縱使事後相符，其程序仍為「預寫」。

### 三-5　🩸 本會期自捕之全表（形／徵候／攔法）

| # | 批 | 形 | 徵候 | 攔法 |
|---|---|---|---|---|
| `1` | `-271` | **判別力對照與受詞同值** ⇒ 該檢恆綠 | 以框 `to_csv` 判「探針是否寫檔」，對照組亦得 `0` | 對照組全零 ⇒ **先判量測器紅** |
| `2` | `-271` | **所命名之「最寬框」實非最寬** | `B 形` 得 `2`／`2` 而「鬆框」僅 `1`／`5` ⇒ `B 形 ⊄ 鬆框` | 鬆框之分隔類須**含反引號**；各單形之命中須為聯集之**子集** |
| `3` | `-271` | **坑 `az` 之再現**：診斷標籤與所取之運算式不符 | 標籤寫 `endswith` 而實取 `startswith` | 標籤與運算式**自同一處取** |
| `4` | `-271` | **`python -c` 內之反引號遭 bash 命令替換**（**第一次**） | `rc ≠ 0` ＋ `command not found` | 改以 `Write` 落腳本 |
| `5` | `-272` | **量 `list` 之成員數而用文字層括號配對** | 得 `40`，而 AST 真值 `35`；🔴 `40` 比真值**更像對的** | 一律以 AST 取值（⇒ `自誤 377`·坑 `ba`） |
| `6` | `-273` | **器之二義共用同一出艙碼** | `theo()` 對「未錄到」與「鍵缺」皆回 `None` | 改為**三態分類**（可得／第三種出艙碼／器紅） |
| `7` | `-273` | **對照組落於飽和區** | `W = 46.08 ≥ 18` ⇒ `R = 100` ⇒ 二式必同 | 照實併呈；自我驗證閘改繫於**碼面之 raise 所斷言之二值** |
| `8` | `-273` | **替換之錨用逃逸單引號而原文為雙引號** ⇒ **空操作**，而同批其餘替換**卻生效** | `TypeError` loud 攔下 | **逐替換同格報「期 `N`／實 `M`」** |
| `9` | `-274` | **命令串內誤留 heredoc** | `wg9237_heredoc_guard` **硬閘** `rc ≠ 0` | 一律以 `Write` 落腳本 |
| `10` | `-274` | 🔴 **框內省抽為輔助函式而未補其自身之一層** ⇒ `sys._getframe` **整體偏移 `1`** | **一整張表 `24` 格全欄「⛔ 可得」**；**同一偏移同時命中二個包裹** | `depth + 1` ＋ **自我驗證閘**（⇒ **坑 `bb`**） |
| `11` | `-274` | **`python -c` 內之反引號**（**第三次**·第二次於記憶索引） | 吃掉 docstring 內二 token；**功能改動完好** ⇒ ⛔ 由 `rc` 顯形 | 一律以 `Write`／`Edit` 落腳本 |
| `12` | `-274` | 🩸 **紀律之自陳**：片段內**預寫** `_N` 之值 | 實測相符、落地前已改寫 ⇒ **⛔ 出艙錯值** | 先留**佔位**、俟實測後填（⇒ 通則 `(e)`） |
| `13` | `-275` | **`ast.literal_eval` ⛔ 適用於含運算式之賦值** | `ValueError: malformed node … ListComp`（**loud**·⛔ 寫回） | `compile` ＋ `exec` 該**單一 `Assign` 節點**（⇒ 通則 `(c)` 之補） |

🔒 **`4`／`11` 同形三度**（另一次於記憶索引之更新）⇒ **其攔法已立而仍犯** ——
「**立戒者在下一批最容易違反自己的戒**」之實例。

### 三-6　起手清單（**取本批收工後之倉態**·供新窗用）

1. **開窗第一動作** ＝ `git ls-remote origin refs/heads/wip/s1-endpart`（⛔ 以本文所載之 commit 為錨）。
2. 驗 `git rev-parse HEAD:app.py` ＝ `4379108a4856714078c410ff4e6291ecfdf6d2c1`。
3. 驗 `p3a`（`05a11bd665f6c2790ff821c9e3a1606651e345d4`）**非祖先**。
4. 四簿現查（器 ＝ `verify/probes/probe_WG9267_issuer_measurers.py registry`）。
5. 坑之清單自證（器 ＝ `verify/probes/probe_WG9269_pit_index.py <rev>`·**長跑 ~4 分**）。
6. 收單 ⇒ **工項零：原封入倉**（`§二-B` 搬移法：python 內**完整檔名逐字指定**·搬移**前**出艙
   「所指之檔數 ＝ `1`」之自證·⛔ 為 `1` 即 loud 停；⛔ 全 ASCII glob、⛔ 差集法）。
7. **本機坑**（逐條已付過學費·⛔ 再犯）：⛔ `python3`（用 `python`）／⛔ heredoc（用 `Write`／`Edit`）／
   **⛔ 於 `python -c "…"` 內置反引號**（本會期**三度**）／`subprocess` 一律
   `encoding="utf-8", errors="replace"`／git pathspec 一律 `/`／⛔ `tail -N` 取長跑結果／
   `io.open(p,'w')` 明指 `newline=""`／⛔ 以文字層量結構化物件之基數／
   **凡替換一律逐替換同格報「期 `N`／實 `M`」**／**框內省抽為輔助函式須 `depth + 1`**。

### 三-7　🔴 未結項（逐條）

| 項 | 狀態 |
|---|---|
| **`GB-168`** | 🔴 **未解除**。`Δ=2.425` 之二式相異有**二處**（`(a)` 所餵之 `W`：理論 `_宗地寬度` vs 實跑 `W_cur = W_prev + S`；`(b)` 式之形：**絕對值** vs **差**）。**⛔ 解除、⛔ 收窄**；**⛔ 判孰為正確之 `W`** |
| 🔴 **`k956_W_from_mp` 之「呼叫次數 `0`」** | **⛔ 為【倉】**——其**判別力對照未備**（`§二`）。**新窗之首要量測**：以「**同法包裹一必被呼叫之函式須得 `>0`**」之對照補證後方為【倉】 |
| **`GB-167`** | ⬜ **未修**（`_WF_NS_NAMES` 漏列 `_wg9268p_anchor_advance`·**生產碼·須 KL 放行**） |
| **`GB-153`** | 🔴 **未結**（第一肢已塌、第二肢之成因改為 `GSA 錨檢破`·**⛔ 結清、⛔ 收窄**） |
| **`GB-159`／`160`／`164`／`165`／`166`／`GB-7`** | 🔴 **未結**（`160` 射程已收窄為二側·⛔ 結清；`164`／`165`／`166` ⛔ 落地） |
| **`GB-162`** | 🛑 **三禁令維持**：⛔ `WV_BAKE`、⛔ 覆寫 `verify/baselines` 任一檔、⛔ 併線 `p3a`；**⛔ 引任何自 baseline 取得之數** |
| **`ok_t`（`TIER_EXPECT`）／`ok_f`（`FLAGGED_EXPECT`）** | 🛑 **⛔ 改一字**——其紅係「**已裁未落地**」族，待 `K-9-29` 碼側落地後**重量**再定 |
| **`K-9-29` 子項 `1`** | ⬜ **未落地**——其落地**⛔ 為旗標翻轉**（`GB-168` 為其**先決**：旗標一翻，`R4` 之 `_N` 由 `1` 變 `2`，該結構閘由「從未執行」轉為「執行且破」）。**子項 `2`（`wf_f0` `a′`）已落地** |
| **`p5`（中央池連通性）** | 已量（真相源 ＝ `_pool_strips_for_block`）·**⛔ 判其為缺陷**——KL 之應然 vs 正典 `N0-19` 之關係**候裁** |
| **`p6`（沿 `s` 軸之交錯）** | 已量·**⛔ 判其結論作廢**；其 `10` 片非零重疊**恰為「⛔ 步驟 `5`」之 `10` 片**（`自誤 376` 已坐實） |
| **`K-9-12`** | 🛑 **待 KL** |
| **`58.4792㎡`** | 受詞已對齊（＝「**缺口總量**」）·**⛔ 等於**「⛔ 步驟 `5` 之片之面積合計」；**⛔ 判孰誤、⛔ 追改該註解一字** |

---

## `§四`　收工閘（`§六`·**commit 前·與被守護之動作分開呼叫**）

### 四-1　閘 `1`：`numstat` 刪除欄（逐檔具名）

| 檔 | `+` | `−` | 非零之由 |
|---|---|---|---|
| `docs/reports/W-G.9-275R_補令一_舊窗收工批.md` | `223`（🩸 **自指**·見下） | **`0`** | — |
| `verify/out/WG9275_preflight.log` | `13` | **`0`** | — |
| `verify/probes/probe_WG9269_pit_index.py` | `5` | **`4`** | 🔒 **就地更正**（`ALPHA_LABELS`→`bb` ＋ 三處框字串齊一）——**單 `§六` 款 `1` 已預載其為唯一非零者·⛔ 前綴式**；依 `常規一` 補款 `🔧 一`，**零生產碼側⛔ 受 `常規一` `②③④` 之拘束** |

🩸 **自指之量⛔ 填數**：本報告自身之 `+` 欄係**自指** ⇒ 上列 `223` 係**本節落地前**之值，落地後必增；⛔ 追改。

### 四-2　閘 `2`：生產碼判法（四檔）

```
git diff --cached --name-only | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'
```

⇒ **輸出為空（命中 `0` 行）** ⇒ **零生產碼 commit** ⇒ **逕行 `push`**。

### 四-3　閘 `3`：`CR` ＝ `0` ＋ 前綴檢

| 檔 | `CR` | `LF` | bytes |
|---|---|---|---|
| `W-G.9-275R_補令一_舊窗收工批.md` | **`0`** | `223` | `17334` |
| `probe_WG9269_pit_index.py` | **`0`** | `291` | `14732` |

**既有檔之前綴檢**（基座 ＝ `8ad54b2f8168128ef7bf3672e5f32f70c2c35ce0`·受檢之既有檔數 ＝ **`1`**）：

| 檔 | 基座 | 現 | `new.startswith(old)` | 判 |
|---|---|---|---|---|
| `probe_WG9269_pit_index.py` | `14644` B | `14732` B | **`False`** | 🔒 **就地更正**（見 `四-1` 之由·⛔ 前綴式·**單已預載**） |

**判別力二造**：必真 `b'abcd'.startswith(b'abc')` ＝ `True` ✅／必偽 `b'abXd'.startswith(b'abc')` ＝ `False` ✅。

### 四-4　閘 `4`：四簿期末（**期值⛔ 為現值**）

| 簿 | 相異（期／實） | MAX（期／實） | 缺號集 | 判 |
|---|---|---|---|---|
| **自誤** | `368` ／ **`368`** | `378` ／ **`378`** | `[106, 355, 356, 357]` | 🟢 **⛔ 動** |
| **`GB`** | `166` ／ **`166`** | `168` ／ **`168`** | `[12, 87]` | 🟢 **⛔ 動** |
| **`VR`** | `80` ／ **`80`** | `95` ／ **`95`** | `[73, 75]` | 🟢 **⛔ 動** |
| **`K-9`** | `28` ／ **`28`** | `29` ／ **`29`** | `[]` | 🟢 **⛔ 動** |

🔒 **四簿皆⛔ 動**（＝ 單 `§六` 款 `4` 之期值）——本批之唯一鑄號係**坑 `bb`**，
其**⛔ 屬四簿之任一**（坑之母體 ＝ 全倉 `.md` 之 `D-H`／`D-T` 形·⛔ 四簿之定義列式）。
🔒 **`GB-132` 之避讓獲驗**：本報告之節題**皆⛔ 含可被四簿之定義框讀為新號之形**
⇒ 四簿之相異／`MAX` **逐位未動**。

### 四-5　閘 `6`：自限復驗

| 受詞 | 實測 | 判 |
|---|---|---|
| `app.py` blob | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ 期初 ＝ 期末 |
| 生產碼判法四檔 | `numstat` 命中 `0` 行 | ✅ ⛔ 動一字 |
| 五個 `*_EXPECT` | `verify/wd4_tier_list.py` **⛔ 在本批 staged 之中** | ✅ ⛔ 改一字 |
| `verify/baselines` | `298` 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | **未併線** | ✅ |
| `WV_BAKE` | `None` | ✅ 未設 |
| `K-9-29` 之任何子項 | 子項 `1` 之逐字錨＠`app.py` 仍 **`1`** ⇒ **⛔ 落地** | ✅ |
| **`GB-167`／`GB-168` ⛔ 修** | `app.py` blob 期初＝期末即其證；二則之**原節一字未動** | ✅ |
| **⛔ 驅動任何長跑**（單 `§八` 款 `2`） | 本批**⛔ 跑** `run_verification`／任何雙態驅動；坑數係**引用**`36f50f8` 之實測 | ✅ |
| **⛔ 補測 `k956` 之呼叫次數**（單 `§八` 款 `4`） | 本批**⛔ 量測**之，僅照實記其路徑之別（`§二`） | ✅ |

### 四-6　閘 `5`：坑之定義處清單自證（`通則 I`）

🔒 **`通則 I`**：本自證之受詞（**追蹤 `.md` 之全集**）**含承載其結論之本報告**
（其 `§一-1` 係 `D-T` 形之坑表 ⇒ 為受詞之一員）
⇒ **於報告 commit <u>之後</u>量**，其 rev 與實測值**逐字載於本批之末一 commit 訊息**。
🛑 **期值 ＝ `80`／`80`**（單 `§六` 款 `5`·新增 `bb`）。🔴 **期值⛔ 為現值**。


---

## `§五`　🛑 停 ⇒ **本窗封存**

**辦畢 ⇒ 停 ⇒ 本窗封存**（單 `§八` 末）。新施工窗之首則為另行交付之**開場文**。
