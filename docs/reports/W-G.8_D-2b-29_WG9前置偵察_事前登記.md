# W-G.8／`D-2b-29` §4-1：`W-G.9` 前置偵察之**事前登記**（⛔ 查之前 commit）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**施工單**：`D-2b-29` §4-1

> # 🔴 本檔之效力條件
>
> 本檔須 commit 於**任何 Q1〜Q3 查詢指令之前**，事後以
> `git merge-base --is-ancestor <本commit> <結果commit>` 貼出機械時序證明。
> 🔒 **預測與結果不符 ⇒ 停機上呈，⛔ 不得靜默改預測。**

## §0 登記時之「已跑／未跑」聲明（⛔ 誠實界定，否則本登記無效）

**已跑（本登記之前）**：僅 `D-2b-29` §0 與 §1-2 之現查——
編號盤點、`HEAD`、`結構閘 理論＝實跑 破` 計數、`D2b24_runall.log` 之中止因、
`git log 78079fa..HEAD`、`grep -rn "卡點" docs/rulings/`、
以及 §1-2 #3 之遞移閉包探針（`verify/probes/probe_D2b29_reach_closure.py`）。

🔴 **未跑（本登記之受詞）**：⛔ **尚未執行任何**針對
`w_lo`／`_W`／`min(_W(...), _W(...))`／`k97_solve_alloc_t`／`app.py:11031`
之 grep、AST 或其他查詢。

⚠️ **順序調整之具名記帳**：施工單之隱含順序為 A → B → 登記 → C，
本批改為**登記 → A → B → C**。
**理由**：§2-3 之更正須現查 `app.py:11031` 之 `w_lo`，而該行**正是 Q1 之受詞**；
若先做 §2-3 再登記，本登記即成事後追認、失其全部效力。
⇒ 調整方向為**收緊**而非放寬，⛔ 未減任何一項要求。

## §1 預測（⛔ 可證偽·逐題附「什麼結果會推翻它」）

### Q1　`w_lo = min(_W(0,0), _W(D,0))` 於倉內是否存在為**活碼**

**預測**：倉內**有文字命中**（≥1），但逐函式定位後，該式於 `_build_corner_range_v3` 內
**不是活碼**（係註解）。⇒ Q1 之答為 **「否——於 `_build_corner_range_v3` 內無活碼」**。

**推翻條件**：AST 於**任一** FunctionDef 內找到該式為活之 `ast.Assign`／`ast.Expr`
（非 `ast.Constant` 字串、非 `#` 註解）⇒ 預測錯 ⇒ **停機上呈**。

**本預測之來源**：施工單 §3 例 3 之自糾陳述（`【單】`）＋ §2-3 之更正文字。
⛔ **本登記不採信其為事實**，須由本批 AST 獨立坐實。

### Q2　活之「min 取二端」結構位於哪些函式，其二端幾何各是什麼線

**預測**：
1. 活之 `min(_W(...), _W(...))` 結構**存在**，且其**主要落點為 `k97_solve_alloc_t`**
   （施工單 §3 例 3 稱 `_W` 之「真身」在該函式）。
2. 另可能存在**同名異義**之 `w_lo`（施工單稱「他函式」），其與 `_W` **無關**。
3. 🔴 其二端之**幾何指認**（各是哪一條線）**預測為「不可判定」**——
   依 §4-4-2，三張示意圖未入倉；若碼註未自載幾何語意，即無從坐實。

**推翻條件**：活結構出現於 `k97_solve_alloc_t` **以外**之函式（第 1 點錯）；
或碼註自載足以定二端幾何之語意（第 3 點錯·此為**好的**推翻，須據實改答）。

### Q3　`k97_solve_alloc_t` 之**生產呼叫數**

**預測**：**0**（施工單 §3 例 3 逐字「生產呼叫 0 之 `k97_solve_alloc_t`」）。
射程須同時涵蓋 `func.id`／`func.attr`／`ns["…"](…)` 三形式，並附**差集自證**。

**推翻條件**：三形式窮舉後得 **> 0** ⇒ 預測錯 ⇒ **停機上呈**
（因其將同時推翻 §3 考古例 3 之「真身在生產呼叫 0 之 `k97_solve_alloc_t`」）。

## §2 已預先承諾之判準（⛔ 事後不得放寬）

- ⛔ **不以計數或列數為憑**；結論一律以**函式名 ＋ 該處實參之幾何指認**為鍵。
- ⛔ **不用行號**（易腐）、⛔ **不用宗序號**（涉逐街廓資料時改用 `side ＋ s 區間`）。
- **R6（`SIDE ∥ ALLOC`·夾角 `0.000003°`）為退化情形**：須記錄，
  ⛔ **不得作任何結論之通過憑據**。
- 凡宣稱窮舉必附**差集自證**；⛔ 總數相符不算；**空集合不算通過**。
- **「不存在／未被使用」之否定性宣稱**：須先明寫工具射程並確認 ⊇ 命題射程
  （即本批 §3 所立之新考古節之機械修法，⛔ 立戒者次批即須自檢）。
