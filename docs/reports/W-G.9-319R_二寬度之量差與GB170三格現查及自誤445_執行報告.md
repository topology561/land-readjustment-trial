# `W-G.9-319R`　執行報告：二寬度之量差（🛑 **停一 成立**）／`GB-170` 三格現查／`自誤 445` 與 `恆常附款 w②` 之增補／`preserve` 性質變更

> **級** ＝ **輕**（唯讀·零生產碼）。**開工態** ＝ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`。
> 🛑 **⛔ 以 shell 命令列傳遞含反引號之文字**（`恆常附款 h`）——🩸 **本批於 `commit` 訊息違之一次**，見 `§七` 自捕三。
> 🔑 **檔名（`常規三`）**：本檔取 `W-G.9-319R`，承既有體例（單號 ＋ `R`）。**候發單側核可。**
> 🔴 **本批之結論一：`§二` 之停一 成立 ⇒ 工項一階段 `b`（替身）⛔ 辦**（`§三-4`）。

---

## `§一`　工項零：本單原封入倉（`追加四` 款一）

| 量 | 值 |
|---|---|
| 落檔 | `docs/orders/W-G.9-319_施工單_二寬度之量差與GB170三格現查及自誤445.md` |
| 來源／入倉 bytes | `28872` B／**同** |
| 來源／入倉 `sha256` | `29897d2c76e1bdad9344bcde1acef4fdd82758328782a19f63dd09a6890c17d5`／**同** |
| 逐位相符／`CR`／`LF` | 🟢 **True**／`0`／`282` |
| `commit` | `b80c6fc` |

---

## `§二`　輕級開工閘（五項最小集）＋ `§零-3` 態錨之復現

| 閘 | 實得 | 判 |
|---|---|---|
| `①` 開工態 | `git rev-parse HEAD` ＝ **`c286a79cc6a699c57c7763306c4beae9ef7b60a3`**；`ls-remote` 主線同值 | 🟢 |
| `②` 號占用 | 見 `§二-1` | 🟢 |
| `③` 生產碼 | `verify/` 頂層 `*.py` ＝ **`33`** ＋ `app.py`；`app.py` blob ＝ **`8466f7493082bf84aa4f02d6c1c6e55ff746fc4a`**（絕對值出艙） | 🟢 |
| `④` 四簿期初 | **二框並報**：正典框 自誤 **`434`／`444`**（缺 `[106,355,356,357]`）；自擬框 自誤 **`430`／`444`**・`GB` **`169`／`171`**（缺 `[12,87]`）・`VR` `80`／`95`・`K-9` `40`／`41` | 🟢 |
| `⑤` `append-only` 期初 | 五受詞之工作區 ＝ 開工態 blob 皆 `True`（自誤簿 `940833`／恆常附款簿 `40233`／換手文 `32398`／`GB` 簿 `944120`／`K-6` 典 `360462` B） | 🟢 |

### `§二-1`　閘 `②`（`恆常附款 z`：**先列舉定義域**·⛔ 以「執行期組出」充證）

母體 ＝ 追蹤 `.md` **`868`** 檔（讀不到 `0`）。
🔒 **定義域之列舉** ＝ 全倉 `.md` 內出現過之 `W-G.9-N` 之 `N` ＝ **`315`** 個相異號（`MIN` `1`／`MAX` `999`）。

| 造 | 受詞 | B 形 | C 形 | 並取 | 除本單自身之產物外 | 判 |
|---|---|---|---|---|---|---|
| 受詢 | `W-G.9-319` | `0` | `0` | `0` | **`0`** | 🟢 |
| 對照甲［必命中·**同形族**］ | `W-G.9-318` | `238` | `24` | `238` | `238` | 🟢 |
| 對照甲2［同形族］ | `W-G.9-309` | `111` | `109` | `111` | `111` | 🟢 |
| 對照乙［必為零］ | 哨兵（字面⛔ 出艙·**先證 ∉ 定義域**） | `0` | `0` | `0` | `0` | 🟢 |

🔒 **`aa②` 二框之相異維度** ＝ **鄰接形**（裸 vs 反引號括全 token），⛔ 僅差標點或動詞。

### `§二-2`　🔴 `P-5` 之紅係**框之別**（自解款白名單 `2`·**⛔ 動 `P-5` 一字**）

`probe_order_preflight.py` 報 `P-5` 🔴 **不符**。**機械確證其為框之別**（器 ＝ `probe_WG9319_selfsha.py`）：

| 框 | 命中 |
|---|---|
| 舊形 `SELF_SHA256: <hash>`（該器所認者） | **`0`** |
| 本單所採之形 `` `SELF_SHA256` ＝ `<hash>` ``（帶反引號·全形等號） | **`1`** ⇒ `f30226221be80d39…` |

`P-5` **原口徑**之重算（受詞 `28739` B）＝ `f30226221be80d3926314de492136a0df6cc52059ea5df382dc414ef193e1c94`
＝ 單載值 **逐位相符** 🟢 ⇒ **傳輸保真未破**。
🔒 **判別力[必不符]**：對**全檔**施同一口徑 ⇒ `29897d2c76e1bdad…` ≠ 單載值 ⇒ **`False`** 🟢（該檢⛔ 恆綠）。
🛑 依自解款**白名單 `2`** 續辦：**⛔ 動 `P-5` 一字**，只在本報告具名。

### `§二-3`　🟡 `3` 項之**逐項具名處置**（⛔ 靜默略過）

| 項 | 所在 | 處置 |
|---|---|---|
| `P-1`（全稱否定未具名母體） | 單 `:152`「⛔ 判本工項對 `GB-170` 失效條件之影響…出艙即止」 | **偽陽**——該列係**禁令**，⛔ 全稱否定之斷言；其「⛔」為祈使、非存在量詞。**⛔ 採納** |
| `P-4`（方向性轉引未具名座標系） | 單 `:196`「`preserve` 現值 ＝ `c286a79…`」 | **偽陽**——該列⛔ 含任何箭頭或方向性轉引；其「變為」係**時序**、⛔ 幾何方向。**⛔ 採納** |
| `P-6`（停機款所繫之數未具名實測出處） | 單 `:196` 同列 | **採納**——該值之實測出處由 CC 當場補：`git ls-remote origin refs/heads/wip/W-G.9-318-preserve` ⇒ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`（`§二` 閘 `①` 同批量得） |

### `§二-4`　🩸 `§零-3` 態錨之復現：`11` 項中 **`10` 項 🟢**、**`1` 項相異**

| 項 | 單載 | 實測 | 判 |
|---|---|---|---|
| 項 `3` `verify/` 頂層 `*.py` | `33` | **`33`** | 🟢 |
| **項 `3` 判別力「子層 `*.py`」** | **`356`** | **`357`** | 🩸 **相異** |
| 項 `7` 自誤簿列數 | `10758` | **`10758`** | 🟢 |
| 項 `7` 恆常附款簿列數 | `342` | **`342`** | 🟢 |
| 項 `8` 全倉追蹤／`.md`／`docs/`／`verify/out/` | `2548`／`868`／`842`／`989` | **同** | 🟢 |
| `§五` 受詞檔列數 | `423` | **`423`** | 🟢 |
| `GB` 簿 blob／`K-6` 典 blob | `64e91c72…`／`3bc628df…` | **同** | 🟢 |

🔒 **相異之成因已機械坐實（⛔ 推定）**：`356` 係 **`be54799`** 之值；
`diff` 之唯一項 ＝ `verify/probes/probe_WG9318p6_ffverify.py`（`c286a79` 所加）。
⇒ **該錨表格之值繫 `be54799`，而其所載之態為項 `1`（`c286a79`）**。
🔑 **其形即本批所鑄 `自誤 445` 之受詞**（錨表之格未繫其所載之態）。
🛑 **⛔ 追改單一字**；其**⛔ 影響任何 operative 判準**——該值僅為[必不命中]之造，`356` 與 `357` 皆足以證子層 `*.py` ⛔ 在生產碼 `34` 檔內。

### `§二-5`　`§八` 三數之當場重算（`恆常附款 f` 後段）

| 量 | 單載 | 實測 | 判 |
|---|---|---|---|
| 受掃之款 | `28` | **`28`** | 🟢 |
| 🟢 施行 | `21` | **`21`** | 🟢 |
| ⚪ 構造上不生 | `7` | **`7`** | 🟢 |

`21 + 7 = 28`；**二列舉之聯集 ＝ 簿內 `28` 款**（差集〔簿∖表〕`[]`／〔表∖簿〕`[]`；`DONE ∩ NA` ＝ `[]`）🟢。
索引（`ab②` 之受詞）：具索引形 `出處` 者 **`28`**／缺 **`0`** 🟢。

---

## `§三`　工項一 階段 `a`：二寬度之量差（**觀測·忠實·⛔ 替身**）

### `§三-1`　取法之具名（`§二-1` 令二者擇一）

🔒 **所擇之 `W`** ＝ **碼面 `_res['W']` 之未捨入值**，取值點 ＝ `ns["_solve_G_one"]` 之**回傳**
（即 `verify/stepg_pipeline.py:492` 所 `round` 之同一物）；**⛔ 與 `k956_W_from_mp` 混用**。
`R(·)` 一律 `ns["rw_from_width"]`（⛔ 器內另算第二份）。

🔴 **`_宗地寬度` 之「未捨入」於碼面<u>結構上不存在</u>**——其**賦值處**即
`_res['_宗地寬度'] = round(_pw, 2)`（`app.py:22036`／`verify/stepg_pipeline.py:737`）
⇒ 本表該欄之值即**碼面之值**（`2dp`）。**⛔ 以任何外算之 `S × |cos|` 充其未捨入值**（`§零-5`）。

### `§三-2`　對齊自證閘（探針還原內部量之必設閘）

判準 ＝ `round(W,2)` **⋀** `round(G,2)` 皆相符；未命中者**逐列具名·⛔ 採信**。

| 態·情境 | `g_rows` | 對齊命中 | 出艙宗 | `Σ(R(W)−R(寬))`（**帶號**） | `Σ|·|`（**絕對值**） |
|---|---|---|---|---|---|
| 態甲·`0.0m` | `56` | `43` | **`42`** | **`+127.656000`** | `1775.692000` |
| 態乙·`0.0m` | `29` | `20` | **`19`** | **`+43.554000`** | `586.780000` |
| 態甲·`3.5m` | `59` | `45` | **`43`** | **`+384.096000`** | `2027.704000` |
| 態乙·`3.5m` | `32` | `22` | **`20`** | **`+286.483000`** | `825.281000` |

🔒 **未命中者皆為抵費地列**（`R1-抵費地-1` 等·其⛔ 經 `_solve_G_one`）⇒ 逐列具名「⛔ 出艙其未捨入 `W`」。
🔒 **帶號與絕對值並列**（`CLAUDE.md` 之並列鐵律：二者所偵測之錯不相交）。
🔒 **逐宗六量之全表**見 `verify/out/WG9319_widths.log`（`255` 列）。

### `§三-3`　母體之可比性（`恆常附款 o`）

| 情境 | 態甲 | 態乙 | 判 |
|---|---|---|---|
| `0.0m` | `56` 列 | `29` 列 | 🛑 **母體相異 ⇒ ⛔ 可比**（其差⛔ 出艙為後果·僅得為**附帶量**） |
| `3.5m` | `59` 列 | `32` 列 | 🛑 **同上** |

🩸 **態乙二情境皆於 `R4 right` 之結構閘中止**（逐字）：
`🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.72 ≠ 實跑(階段1)=88.40（Δ=0.676 >0.1）`。

### `§三-4`　🛑 **停一 成立** ⇒ 階段 `b`（替身）**⛔ 辦**

**[必相符]** `0m` 態乙 `R4` 之 **`628-1(1)`**（`GB` 簿 `:7359`–`:7360` 逐字）：

| 欄 | 單載（`GB` 簿） | 本批實得 | 差 |
|---|---|---|---|
| `宗地寬度(m)` | `14.01` ⇒ `R` `87.835000` | **`13.98`** ⇒ `R` **`87.724000`** | `−0.03` |
| `W(m)` | `14.20` ⇒ `R` `88.500000` | **`14.17`** ⇒ `R` **`88.395000`** | `−0.03` |

⇒ **逐位相符：`False`** 🔴 ⇒ 依 `§二-1` 之明文 **停機上呈**，**⛔ 追改、⛔ 改取法**
⇒ 🛑 **工項一階段 `b`（替身）本批 ⛔ 辦**。
**[必為零]** 任一宗之 `R(W) − R(W)` ＝ **`0.0000000000`** 🟢。

🔒 **倉內事實（⛔ 據以斷言成因·⛔ 與實得同格）**：
`GB` 該節所載之三軸來源 **`f46ce92`**（`2026-09-13 23:30:14 +0800`）**係開工態之祖先**（`is-ancestor` `rc` `0`），
且 `f46ce92..c286a79` 之生產碼 `commit` **恰為 `W-G.9-318` 之六筆**
（`7f43924`／`7c346b1`／`7c07a36`／`6cc51e6`／`b1141a3`／`8e056b4`·即 `K-9-41 ①` 之落地）。
🛑 **以上係倉內事實之列舉；其與本差之因果關係⛔ 由 CC 判**（`§零-1` 款 `7`／`8`）⇒ **上呈**（`§八-1`）。

---

## `§四`　工項二：`GB-170` 三釘紅格於**現態**之現查（唯讀·出艙即止）

🔒 `forced_map` 之形**當場出艙**（⛔ 推定）＝ `{blk: {'<side>_forced_offset': bool, '<side>_corner_min_area': float, '<side>_has_side': bool}}`。

| 格（`GB` 簿 `:7773`–`:7775`） | 態甲 | 態乙 |
|---|---|---|
| **`R4/left@0.0m`**（`116.08`） | `left_forced_offset` **`True`**·`area` **`116.08`** ⇒ 🔴 **仍為強制抵費地** | **`False`**·`0.0` ⇒ 🟢 **已⛔ 為** |
| **`R2/left@3.5m`**（`308.17`） | **`True`**·**`308.17`** ⇒ 🔴 **仍為** | **`True`**·**`308.17`** ⇒ 🔴 **仍為** |
| **`R4/left@3.5m`**（`226.01`） | **`True`**·**`226.01`** ⇒ 🔴 **仍為** | **`False`**·`0.0` ⇒ 🟢 **已⛔ 為** |

**逐格併出**（全文見 `verify/out/WG9319_gb170.log`）：

| 格·態 | 該側 `_lot_gate` 筆數 | 街角第 `1` 宗／`驗_宗序`／`G(㎡)` | 該街廓抵費地列 |
|---|---|---|---|
| `R4/left@0m`·甲 | `1` | `628(1)+`／`街角第1宗`／`2017.54` | `4` 筆 |
| `R4/left@0m`·乙 | `1` | `628(1)`／`街角第1宗`／`1521.05` | `5` 筆 |
| `R2/left@3.5m`·甲 | `0` | 【該側⛔ 有 `第1筆街角` ＝ `是` 之列】 | `2` 筆 |
| `R2/left@3.5m`·乙 | `0` | 同上 | `2` 筆 |
| `R4/left@3.5m`·甲 | `1` | `628(1)+`／`街角第1宗`／`2023.33` | `4` 筆 |
| `R4/left@3.5m`·乙 | `1` | `628(1)`／`街角第1宗`／`1521.05` | `5` 筆 |

🟢 **停四 ⛔ 觸發**——三格於二態**皆可得**（管線皆達該街廓）。

**判別力二造**
- **[必非零]** 態甲 `R4 left` ＝ **`True`**（`GB` 簿 `:8293` 逐字「態甲 `R4` 左 ＝ ⚠️ 強制抵費地」）🟢
- **[必為零]** **選法之具名**：候選 ＝ 態甲 `0m` 之全部 `(街廓, 側)` 對 **`12`** 個；
  濾去該側有 `街角地 ＝ 是` 之列者 ⇒ **非街角側 `5`** 個
  （`[('R2','right'),('R4','right'),('R6','left'),('R5','right'),('R3','left')]`）；
  **取候選之首**（⛔ 以「看起來不是街角」充之）＝ `R2/right` ⇒ **`False`** 🟢

🛑 **⛔ 判本工項對 `GB-170` 失效條件之影響、⛔ 解除、⛔ 收窄、⛔ 提修法主張**——**出艙即止**。

---

## `§五`　工項三：`自誤 445` 之鑄 ＋ `恆常附款 w②` 射程之增補

**`commit`** ＝ `782ec52`（與工項四同批·其訊息以 `git commit -F` 之**檔案管線**產生）。

| 受詞 | 期初 B | payload B／`sha256` | 期末 B／`sha256` | 四造 |
|---|---|---|---|---|
| 自誤簿 | `940833` | `1714`／`d3a6333100afb33c…` | **`942547`**／`3b8e63e3fd9ad30c…` | 全 `True` |
| 恆常附款簿 | `40233` | `1677`／`1c3dd3cb7f6808fa…` | **`41910`**／`6831ff5e592bf25f…` | 全 `True` |

🔒 **`v①`**：`自誤 445` 之標題形與簿內**最近一則**（`自誤 444`）前綴同族 ⇒ `True`。
🔒 **`v②` 二框自驗**：自擬框 `RE_ZW_DEF` **命中**（捕獲 `445`）／正典框 **命中**（捕獲 `445`）
⇒ **二框皆命中·⛔ 落入缺號集**；判別力[必不命中]：一非標題列 ⇒ 二框皆 `False` 🟢。
🔒 **`w②` 之增補係<u>修訂</u>·⛔ 新立款**（`§零-2` 裁 `3`）⇒ 款數 期初 **`28`** ＝ 期末 **`28`**、**修訂 `1`**。
🔒 **其逐字出處** ＝ `docs/orders/W-G.9-319_施工單_二寬度之量差與GB170三格現查及自誤445.md:171`
（**本單入倉後當場現查之列號**·⛔ 憑單載）；**除本補令自身之產物外 ＝ `0`**（`恆常附款 e`）。
🔒 **`w` 之索引⛔ 變**（其 `出處` 節題一字不動）；⛔ 另立索引項。

---

## `§六`　工項四：`preserve` 性質變更之登記

**受詞** ＝ `docs/reports/W-G.9-318_CC側換手_一.md`·期初 `32398` B ＋ payload `1342` B
＝ 期末 **`33740`** B·`sha256` `84c89c003894aff72739263b899a33179afa5dd445955b7302d5628a3ca791d7`·四造全 `True`。
三項逐字自單 `:196`–`:198` 抽（**檔案管線**·⛔ 改一字）。

---

## `§七`　本批之**自捕**（**`3`** 則·皆於出艙／入倉前，惟第 `3` 則已入 `commit` 訊息）

| # | 自捕 | 其形 | 攔點 |
|---|---|---|---|
| `1` | `ns["_solve_G_one"]` 之間諜錄得 **`0`** 筆 | 其回傳係 **`(res, solver_label)` 之 tuple**（`stepg:823` 逐字），⛔ `dict` ⇒ `isinstance(res, dict)` 恆偽 | **對齊自證閘**報 `0`／`56` 而非靜默出錯 ⇒ **該閘生效一次** |
| `2` | 逐宗 `寬` 恆為 `None` | `_宗地寬度` 係於 `_solve_G_one` **回傳之後**才寫入**同一 dict**（`stepg:737`）⇒ 當下取值恆 `None`；改**存參照**、於出艙時方讀 | 同上（表內逐列 🩸 二量非數） |
| `3` | 🩸 **違 `恆常附款 h`** | `commit` `f54c2bf` 之訊息以 `-m` 傳遞含反引號之文字 ⇒ 其中之受詞 token **遭 shell 命令替換吞去**（該列現讀為「`[必相符] 不成立——`」） | **bash 之 `syntax error` 提示**；其後之 `commit` 一律 `git commit -F` |

🔒 **自捕 `3` 之處置**：**⛔ amend、⛔ 改寫歷史**；其完整逐字在**本報告 `§三-4`**（**倉內報告＝唯一本體**）。

---

## `§八`　上呈（**意思決定·⛔ CC 自裁**）

1. 🔴 **停一 之處置**（`§三-4`）——`[必相符]` 之期值繫 `GB` 簿三軸所載之 `f46ce92`，
   而開工態 `c286a79` 與其之間**恰隔 `W-G.9-318` 之六筆生產碼**。
   **其因果⛔ 由 CC 判**（`§零-1` 款 `7`／`8` 明禁）⇒ **繫發單側／KL**：
   期值應否改繫現態？階段 `b` 應否於期值更新後另單辦？
2. 🩸 **`§零-3` 項 `3` 判別力值 `356` 繫 `be54799` 而其所載之態為 `c286a79`**（`§二-4`）
   ——**其形即本批所鑄 `自誤 445` 之受詞**；是否另登記，**繫發單側**。⛔ 追改單一字。
3. 🔑 **`P-5` 之框**（`§二-2`）：本單採 `` `SELF_SHA256` ＝ `<hash>` `` 之新形，而
   `probe_order_preflight.py` 只認舊形 ⇒ **此後之單將持續報 `P-5` 🔴**。
   **是否擴 `P-5` 之框（＝動生產碼路徑外之器）繫發單側**；本批**⛔ 動 `P-5` 一字**。
4. 🔑 **檔名之核可**（`常規三`）：本報告取 `W-G.9-319R`。
5. 🛑 **本批⛔ 及於**：`GB-168`／`169`／`170`／`171` 之解除或收窄、`GB-170` 取錯線之修、
   `GB-138` 處置 ③、`K-9-41 ②`〜`⑥` 之落地、`K-9-30`〜`K-9-37`、`K-6-A2`／`K-6-B`／
   七級調配丙類六軸／泛用化波。

---

## `§九`　收工閘（**輕·報告入倉後量**）

（本節於報告入倉後以**嚴格末端追加**補。）

---

## `§九`（**末端追加**）　收工閘 `1`〜`8` 之實測

🔒 **量測時點** ＝ **報告入倉後**（`ff7bc9f9a74a97947f66ce427fff897b573d81e4`）。
🔒 **器** ＝ `verify/probes/probe_WG9319_closing.py`·落檔 `verify/out/WG9319_closing.log`。
🔒 **開工態** ＝ `c286a79cc6a699c57c7763306c4beae9ef7b60a3`（`恆常附款 w②`·⛔ 沿用他批）。
🛑 **凡閘一律三值**（🟢 已執行且通過／🛑 未執行／🔴 已執行且破·`恆常附款 n①`），
且**同格載其基線態**（`恆常附款 r`）。
🛑 **本節之數係 `§九` 追加<u>前</u>之態**（`GB-132` 族之自指增長·⛔ 追改）。

### 閘 `1`　逐 `commit` `numstat`（刪除欄須全 `0`）🟢 **已執行且通過**

本批 `commit` ＝ **`6`** 筆；刪除欄逐筆皆 **`0`** ⇒ 非零之 `commit` ＝ **`0`**。

| # | 雜湊 | 檔 | 刪除欄 | 題 |
|---|---|---|---|---|
| `1` | `b80c6fc` | `1` | **`0`** | 工項零：本單原封入倉 |
| `2` | `589f9a2` | `4` | **`0`** | 輕級開工閘之量測器與落檔 |
| `3` | `f54c2bf` | `2` | **`0`** | 工項一階段 `a`（🛑 停一 成立） |
| `4` | `0361405` | `2` | **`0`** | 工項二：`GB-170` 三格現查 |
| `5` | `782ec52` | `9` | **`0`** | 工項三＋四 |
| `6` | `ff7bc9f` | `1` | **`0`** | `W-G.9-319R` 報告入倉 |

### 閘 `2`　生產碼 `34` 檔之相異 🟢

相對開工態之相異 ＝ **`0`**（清單 `[]`）。
🔑 **判別力［必非零］**：`app.py` 之開工態 vs **`40375702378af4cefd2d11515fdf3f98c0791580`**
⇒ 相異 ＝ **`True`** ⇒ 本閘 **⛔ 恆綠**。

### 閘 `3`　本批 `.md` 之 `CR` 🟢

受檢集 ＝ **`8`** 檔（**`0` ⇒ 器紅**）；`CR` 逐檔皆 `0`，合計 **`0`**。

### 閘 `4`　四簿（**二框並報**）🟢

| 框 | 簿 | 開工 相異／MAX | 期末 相異／MAX | 缺號（期末） |
|---|---|---|---|---|
| CC 正典框 | 自誤 | `434`／`444` | **`435`／`445`** | `[106, 355, 356, 357]` |
| 標題錨定框 | `VR` | `80`／`95` | **`80`／`95`** | — |
| 標題錨定框 | `K-9` | `40`／`41` | **`40`／`41`** | — |
| 發單側自擬框 | 自誤 | — | **`431`／`445`** | `[106,355,356,357,434,435,436,437]` |
| 發單側自擬框 | **`GB`** | — | **`169`／`171`** | `[12, 87]` |

🔒 **算式（`恆常附款 w①`）**：自誤 相異 ＝ 開工 **`434`** ＋ 本批新鑄 **`1`**
（**逐號列舉** ＝ `自誤 445`）＝ **`435`**；MAX ＝ **`445`**。
🟢 **與單 `§四-3` 所載之正典框 `435`／`445`、自擬框 `431`／`445` 逐位相符**。
🛑 **`GB`** 依補令五 `裁 2`：其**定義列式⛔ 經正典釘定**（候 `GB-138` 處置 ③），
**本數僅為該一框之值**；本批**⛔ 鑄任何 `GB` 號** ⇒ `169`／`171` **⛔ 變** 🟢。

### 閘 `5`　`append-only`（**三值化**·`恆常附款 x`）🟢

| 檔 | 判 | 期初 B／`sha256` | 期末 B／`sha256` |
|---|---|---|---|
| 恆常附款簿 | 🟢 **前綴且增長** `+1677` | `40233`／`59d86cc50d345ce2…` | `41910`／`6831ff5e592bf25f…` |
| 自誤簿 | 🟢 **前綴且增長** `+1714` | `940833`／`2d419207277b6052…` | `942547`／`3b8e63e3fd9ad30c…` |
| CC 側換手文 | 🟢 **前綴且增長** `+1342` | `32398`／`87ab2188d52af67d…` | `33740`／`84c89c003894aff7…` |
| `K-6` 典 | 🔵 **前綴且未改**（本批⛔ 涉） | `360462`／`bf195413d2ab88d3…` | **同左** |
| `CLAUDE.md` | 🔵 **前綴且未改** | `238340`／`e2670c472a1823e4…` | **同左** |
| **`GB` 簿** | 🔵 **前綴且未改**（本批⛔ 涉·`§零-1` 款 `4`／`6`） | `944120`／`0a0f1ee56bcfbc8c…` | **同左** |
| `VR` 簿 | 🔵 **前綴且未改** | `394392`／`3be11bb3b312ccad…` | **同左** |

🟢 增長 `3` 檔／🔵 未改 `4` 檔／🔴 `0` 檔。

### 閘 `6`　自限 🟢（**含一項具名相異**）

`baselines` ＝ `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·**`298`** 列 🟢；
遠端總數 **`25`**／`verify/*` **`20`** 🟢。

🩸 **主線之期值相異（`恆常附款 f`·以實測為準並具名·⛔ 追改單）**

| 量 | 值 |
|---|---|
| 單 `§六` 閘 `6` 所載之期值 | `40375702378af4cefd2d11515fdf3f98c0791580` |
| **實測之主線** | **`c286a79cc6a699c57c7763306c4beae9ef7b60a3`** ＝ **本批開工態·未被他動** 🟢 |
| `is-ancestor 4037570 → 主線` | `rc` **`0`**（證 `4037570` 仍在祖先鏈內·歷史一位未失） |

🔒 **成因**：單所載之 `4037570` 係**上一批（補令六）主線快轉<u>前</u>之態**；
該快轉已於 `2026-09-20` 依 KL 放行完成（`W-G.9-318R7 §九`）⇒ 該期值於本批開工時**即已過期**。
🛑 **⛔ 追改單一字**；其⛔ 影響任何 operative 判準（本批**⛔ 觸主線以外之分支**、⛔ 動生產碼）。

### 閘 `7`　母體三數（`VR-091 一`）

`(1)` **來源 `commit`** ＝ `ff7bc9f9a74a97947f66ce427fff897b573d81e4`
`(2)` **母體** ＝ 全倉追蹤 `.md` **`873`** 檔／全 `docs/` **`844`** 檔／`verify/out/` **`998`** 檔
（期初分別為 `868`／`842`／`989`·**自指增長逐項具名**：`.md` `+5`／`docs/` `+2`／`verify/out/` `+9`）
`(3)` **粒度框** ＝ **列框**（計數）／**bytes**（blob 比對）

🔒 `verify/out/` 開工態既有 **`989`** 檔，其 blob 相異 ＝ **`0`** 🟢；本批新增 **`9`** 檔。
🔒 **`恆常附款 g` 之復驗**：`git check-ignore -v` 對三類**實際檔名**逐檔 ⇒ **空輸出**（皆⛔ 在忽略族）；
判別力［必命中］：`verify/out/got_XXXX.log` ⇒ `.gitignore:30:verify/out/got_*`·`rc` `0` 🟢。

### 閘 `8`　`ls-remote`（全 `40` 碼）

🛑 **於 `push` <u>前</u>量**（`常規補款 一`）：主線 ＝ `c286a79…`；`preserve` ＝ `c286a79…`；
本地 `HEAD` ＝ `ff7bc9f9a74a97947f66ce427fff897b573d81e4`；本批 **`6`** 筆（本節落地後為 **`7`**）。
🔒 **`push` 之受詞 ＝ 主線 `wip/s1-endpart`**（單 `§七`·全數零生產碼·`常規一` 補款 `🔧 一`·⛔ 請示）；
其後之全 `40` 碼另以**聊天回報**出艙。

## 🔧 停一 成因之坐實與 [必相符] 造之撤回重設（W-G.9-319 補令一·⛔ 上文一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**；**⛔ 追改本報告上文一字**（`常規四（九）五`）
——`§三-4` 所載之「停一 成立」係**其立節當時之現態**，其**沿革**於此保留。
🔒 **態** ＝ `51a641d334847d285f323257909748acdf7052e1`（本補令之開工態）。

### 一　發單側之五裁（逐字轉錄·⛔ 改寫）

> **裁 `1`　`停一` 之判維持·CC 之處置受領為正·⛔ 追改**
> `§二-1` `[必相符]` 造不成立為**事實**，CC 依單明文停機、⛔ 追改、⛔ 改取法——**處置正確**。惟其**成因⛔ 在受單側，亦⛔ 在碼面**，而在**發單側之期值繫他態**（`自誤 447`）。
>
> **裁 `2`　`[必相符]` 造之撤回與重設**
> 原造（`GB` 簿 `:7359`–`:7360` 之 `14.01`／`14.20` ⇒ `87.835000`／`88.500000`）**全部撤回**——其三軸之來源 `f46ce92`（`2026-09-13`）係開工態之**祖先**，且 `f46ce92..c286a79` **恰含 `W-G.9-318` 之六筆生產碼**（`K-9-41 ①` 之落地）⇒ **跨批·⛔ 可比**（`恆常附款 w②`）。
> **重設之造**（繫開工態·同簿之**末端追加**節 `:8254` 逐字）：
> > `🔴 結構閘 理論＝實跑 破：街廓 R4 right 側 理論@k*=87.72 ≠ 實跑(階段1)=88.40（Δ=0.676 >0.1）`
>
> 🔒 **CC 之實得對此造之判（發單側當場覆核）**：`R(13.98)` ＝ `87.724000` ⇒ `2dp` **`87.72`** ⇒ 與 `理論@k*` **逐位相符** 🟢 ⇒ **其器非紅、其量為正**。
> 🩸 **併記（只登記·⛔ 判其成因·⛔ 據以改任何判）**：`R(14.17)` ＝ `88.395000` ⇒ `2dp` **`88.39`**，而閘之 `實跑(階段1)` ＝ **`88.40`** ⇒ **⛔ 逐位相符**，具名其差 `0.01`（`2dp` 顯示層）。其可能之由（**⛔ 判**）＝ 實跑側所餵之 `W` 為**未捨入**而本器之 `W(m)` 欄為 `2dp`——即 `GB` 簿 `W₀ 二消費者之粒度不對稱` 一節所登記之同族現象。
>
> **裁 `3`　階段 `b` 之**重設計**（⛔ 依原單之二造逕辦）
> 原設計（以**未捨入** `W` 代 `widths`）於本批坐實**同時更動二事**：
> `(一)` **軸**（`_宗地寬度` 含一 `|cos|` 投影／`W` 不含）；`(二)` **粒度**（`_宗地寬度` 之未捨入**結構上不存在**·其賦值處即 `round(_pw, 2)`·`app.py:22036`／`verify/stepg_pipeline.py:737`；而 `W` 為未捨入）。
> ⇒ 其產物**⛔ 可歸因** ⇒ **重設為三造**：
>
> | 造 | `widths` 所餵 | 分離之因 |
> |---|---|---|
> | `α` | `_宗地寬度`（現況·`2dp`） | 基座 |
> | `β` | `round(W, 2)` | **只換軸**·粒度同 `α` |
> | `γ` | `W`（未捨入） | 換軸 ＋ 換粒度 |
>
> 🛑 **三造之執行⛔ 於本補令辦**——其受詞於此釘死，另單（`W-G.9-320`）辦，且須於**新窗**擬（本波之窗已近收口）。
>
> **裁 `4`　`P-5` ⛔ 擴框·⛔ 動一字——改單之形**
> `P-5` 之解析逐字 ＝ `ls[i].decode(...).split(":")[-1].strip().strip("`")`；原單採 `` `SELF_SHA256` ＝ `<hash>` `` 之形，其**無半形冒號** ⇒ 整列入 `split` ⇒ **結構上必不符**。
> ⇒ 🔒 **發單側此後一律採 P-5 既認之形**：`SELF_SHA256:` ＋ 半形空格 ＋ **裸 hash**（⛔ 反引號·⛔ 全形等號），且置於**檔末最末一列**。
> 🔒 **其據** ＝ `恆常附款 v②` 逐字「失配即改 payload、**⛔ 改器**」；本補令將其射程增補（`§三`）。
> 🔒 CC 本批依**自解款白名單 `2`** 機械確證傳輸保真未破 ⇒ **處置正確·受領**。
>
> **裁 `5`　`W-G.9-319` `§零-3` 項 `3` 與 `§八` 款 `w` 之相異·⛔ 追改**
> 原單錨表項 `3` 之判別力值載 `356`，實測 `357`（差 ＝ `c286a79` 所加之 `verify/probes/probe_WG9318p6_ffverify.py`·發單側當場重得）；原單 `§八` 款 `w` 判 🟢「`§零-3` 項 `1`〜`8` 皆繫 `c286a79` 當場重取」與結語「`12`／`12` 格皆量得」**對該格為偽陳述**。
> 🛑 **⛔ 追改原單一字**（`常規四（九）五`）——本補令即其具名；其登記 ＝ `自誤 446`。

### 二　重設之造與 CC 實得之逐位對拍

| 量 | 值 | 判 |
|---|---|---|
| 重設之造（同簿末端追加 `:8254` 逐字）`理論@k*` | **`87.72`** | — |
| CC 實得 `R(13.98)` | `87.724000` ⇒ `2dp` **`87.72`** | 🟢 **逐位相符** ⇒ **其器非紅、其量為正** |

### 三　🩸 具名之相異（**只登記·⛔ 判其成因·⛔ 據以改任何判**）

| 量 | 值 |
|---|---|
| CC 實得 `R(14.17)` | `88.395000` ⇒ `2dp` **`88.39`** |
| 閘之 `實跑(階段1)` | **`88.40`** |
| 差（`2dp` 顯示層） | **`0.01`** ⇒ **⛔ 逐位相符** |

🛑 其可能之由（**⛔ 判**·單 `§零-1` 裁 `2` 逐字）＝ 實跑側所餵之 `W` 為**未捨入**而本器之 `W(m)` 欄為 `2dp`——即 `GB` 簿 `W₀ 二消費者之粒度不對稱` 一節所登記之同族現象。

### 四　階段 `b` 三造之受詞（**釘死於此**·其執行**另單 `W-G.9-320`·新窗**）

| 造 | `widths` 所餵 | 分離之因 |
|---|---|---|
| `α` | `_宗地寬度`（現況·`2dp`） | 基座 |
| `β` | `round(W, 2)` | **只換軸**·粒度同 `α` |
| `γ` | `W`（未捨入） | 換軸 ＋ 換粒度 |

🔒 **其分離之由**（單 `裁 3` 逐字）：原設計同時更動**軸**與**粒度** ⇒ 其產物**⛔ 可歸因**。
🛑 **三造之執行⛔ 於本補令辦。**
