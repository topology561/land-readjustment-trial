# W-G.9-30：設計批報告

**基座**：`wg925-handoff` @ `ed598a2`（＝`origin/wip/s1-endpart`）
**性質**：⛔ 零生產碼 · ⛔ 零凍存物更新（`VR-012`）
**交付**：`docs/design/W-G.9-30_∥SIDELINE界線之接線設計.md`（九節齊備）＋ 入倉兩處 ＋ 本報告
**結論**：`R-1`〜`R-6` 全部未觸發（`R-3` 曾觸發、已修並重驗）。

---

## §0 編號現查（🔴 對施工單之一項更正）

施工單稱 `W-G.9-30` **0 命中**；**現查為 1**（`git grep -c "W-G.9-30" | wc -l` ⇒ **1**，判別力自檢 `W-G.9-29` ⇒ **4 檔**）。

**該 1 命中之出處**：`docs/reports/W-G.9-29_R2破量歸因與授權入倉.md` 之兩行，皆為**前批之前瞻引用**（該報告預告「`W-G.9-30` 之設計範圍」與「屬 `W-G.9-30` 之範圍」）。
**判**：`docs/reports/` 無 `W-G.9-30` 之報告檔（`ls | grep -c` ⇒ **0**）、`docs/design/` **不存在**
⇒ **非已占用之批** ⇒ **可用該號**、⛔ 未改號。
🔒 **此即「查無 ≠ 不存在」之對偶**：**查到 ≠ 已占用**——須看**命中之性質**。

---

## §1 甲組：現查結果

### 甲-1 五族 × 兩路徑（🔴 祖先鏈以 AST 求得）

**工具**：scratchpad 之 AST 腳本，對每一命中行求其**完整函式/類祖先鏈**（走訪全樹並比對 `lineno`／`end_lineno` 之行域），⛔ **非**「最近之前 `def`」。
🔒 **判別力自檢**：哨兵字樣 ⇒ **0 命中**（兩檔皆）。
**母體**：`app.py` **22** 行、`verify/stepg_pipeline.py` **10** 行（含 `_left_buffer_S` 或 `_right_buffer_S` 之全部行）。

**祖先鏈之實測**：A 路徑 22 行全部落在 `def main`，其中族①②④ 之 6 行落在 `def main → def _advance_block_with_split`；
B 路徑 10 行全部落在 `def run_step_g`，其中族① 之 2 行落在 `def run_step_g → def _advance_block_with_split`。

**逐族逐路徑之整行原文**見設計文件 §2-1（字樣錨 `五族消費者 × 兩條生產路徑`）。

### 甲-1 之問題 2：「0 命中」之正面回答

| 族／路徑 | 施工單所載 | **現查所得** |
|---|---|---|
| 族②／B | 「該字樣 0 命中——現查其實際寫法」 | 🔴 **該路徑沒有這一族**——`_W_prev_left = 0.0`／`_W_prev_right = 0.0` **硬寫**；該檔含 `_left_buffer_S` 之 10 行中**無一**流入 `_W_prev_*` |
| 族④／A | 「該字樣 0 命中」 | **是寫法不同**——A **不用** `_mp_base_W0` 之名，而直接呼叫 `k956_W_from_mp` 並以 `± buf·_du_diag` 平移起點（字樣錨 `k956_W_from_mp(_gs_l + float(_left_buffer_S)`） |
| 右側之 `left_cum_S = float(_right_buffer_S)` | 「0 命中」 | **正確且應為 0**——右側之名為 `right_cum_S`（兩路徑各 1 命中）⇒ ⛔ 不是缺件 |

🔒 **右側之語意不對稱已由碼面逐字坐實**（⛔ 非推定）：
`兩側語意**刻意不對稱**（左 `buf`＝絕對 s 上界／右 `buf`＝自 `s_max` 起之寬），**與消費端逐位對映**`

### 甲-1 之 `R-1` 判：⛔ **未觸發**

族② 於兩路徑**確實取自不同的量**（`forced` 側：A ＝ `buf·_cos_dn`、B ＝ `0.0`）。
惟 `R-1` 之受詞為「**新的**三值分歧」，而**此分歧已登記**：

- `docs/reports/W-G.9-21_b槽路徑分歧.md`（字樣錨 `B 之推進種子`／`② 推進種子`）逐字載
  「A ＝ `buf · _cos_dn`／B ＝ **硬 `0.0`**／僅 `buf ≠ 0`（即 `forced`）⇒ **3 格**」，
  並指名其成因 commit（字樣錨 `W 脫鉤打樣`）。
- `GB-62` 方向① 亦已載「`首宗起點` 之碼面兩路徑實測不一致」。

⇒ **⛔ 非新** ⇒ `R-1` 未觸發。設計文件 §2-3 已**具名承接**、⛔ 未當作已解決。

### 甲-2 `n_hat_far` 之契約

逐字六則見設計文件 §2-4（字樣錨 `n_hat_far` 之契約）。
**生產端傳參 ＝ 1 處**（字樣錨 `allocation_dir=_near_ad, n_hat_far=_far_nhat`）。
現查式：`grep -rn "n_hat_far" --include="*.py" . | grep -v "verify/probes/"` ⇒ 扣除 `def` 預設值、docstring、函式內部使用（雙線分支之實作）與三處交叉引用註解後，實際傳參者即該一處。
⇒ **雙線分支已具備且已有呼叫端在用** ⇒ 本設計**不需新造幾何原語**。

### 甲-3 新介面 ＋ 靜默改值之防護

`_corner_buffer_S` 現行**回傳純量**（字樣錨 `return float(buf)`），另有三個早退 `return 0.0`。

🔴 **本設計之選擇 ＝ ⛔ 不改回傳型別**，只**新增輸入參數** `n_hat_far=None`
⇒ 族①②④⑤ **吃得下、無須改寫**；唯一須改者為**傳參端**（設計 §3 之 `C-3`／`C-4`）。

🔴 **「哪一道現有的閘會擋住靜默改值」之正面回答 ＝ 沒有**：
唯一候選為 `verify/run_verification.py` 之 `結構不變量永久閘{tag}`（字樣錨
`結構不變量永久閘{tag}（守恆/⊥/位次=投影序/J(k*)≥J(naive)/理論=實跑/telescoping）`），
但它與 `GB-63` 之四個滑池槽名目**同族**——落在同一個 `try` 內，而該 `try` 之首敘述
（字樣錨 `_sg = run_step_g(ns, fake_st, list(cb_by.values()), cad, snapshot,`）現況即拋
⇒ **它現況不執行**。
⇒ 依 `R-2`，設計**已提出三道新閘** `G-α`／`G-β`／`G-γ`（設計 §4-2），⛔ 未留空 ⇒ **`R-2` 未觸發**。

### 甲-4 釋示④ 之連動義務

5 格之現值（逐字·出處 `docs/rulings/K-6…`·字樣錨 `第 2 宗最小寬低於畸零地最小寬之 5 格`）：
`0m R3/right −1.088138`／`0m R5/left −0.877844`／`3.5m R1/right −1.057060`／`3.5m R3/right −0.994436`／`3.5m R6/right −3.272963`。

該節之加註逐字：`⚠️ **量測基礎將因 `K-9-5-4 ②` 落地而作廢**——本列五格中之**四格須重量**；`3.5m R6/right` 不受影響。`
⇒ **須重量者 ＝ 前四格**；**用什麼量** ＝ 改後之右界（∥SIDELINE），⛔ 非現況右界。

### 甲-5 `R5` 之射程

`R5` 之同楔形樣態（`36.1337 ＋ 20.1956 ＝ 56.3293`）出處為 `docs/reports/W-G.9-29_R2破量歸因與授權入倉.md`。
⚠️ 該值係**量測工具單獨跑得**、⛔ **非生產路徑之現況後果** ⇒ 設計 §5 之 `P6` 已標明
**須待程式到達該街廓後才可驗**、在此之前為**無從判定**（⛔ 非為偽）。

---

## §2 佔位符之填值與其現查式（🔴 本窗新戒之落點）

| 佔位符 | **所填之值** | 現查式與結果 |
|---|---|---|
| `<下一未占用節號>` | **`81`** | `grep -o "^## [0-9]*\. " <考古檔> \| grep -o "[0-9]*" \| sort -n \| tail -1` ⇒ **80**；`grep -c "^## 81\. "` ⇒ **0**；判別力自檢 `grep -c "^## 80\. "` ⇒ **1** |
| `<GB表頭整行>` | `\| # \| 阻塞項 \| 觸發條件（換案） \| 現況處置 \| 倉內出處 \| 真資料覆蓋 \| 需 KL 域裁？ \|` | 取該檔首個含 `GB-62` 之列，其上方第二行即表頭（⛔ 未憑行號指定，係由該錨相對定位）；插入器內建「錨命中須為 1」之斷言，**通過** |

---

## §3 落地自證（考古 68）

| 檔 | 前 → 後 | 新內容命中 | 刪除行 |
|---|---|---|---|
| `.claude/skills/failure-archaeology/SKILL.md` | 2788 → **2821**（**+33 純插入**） | `^## 81\. ` ⇒ **1** | **0** |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 305 → **307**（**+2 純插入**） | `本表 ⛔ 非單調序` ⇒ **1** | **0** |

⚠️ **表格列數不變（`^\| ` 116 → 116）之說明**：本批之警語為 **`>` 引用行**、**⛔ 非表列**
⇒ 表格列數**本應不變**；`GB` 列仍為 **63**。（⛔ 與 `W-G.9-29` 之「＋2 表列」不同受詞。）

**禁動清單** `git diff --numstat HEAD -- app.py verify/**/*.py verify/*.py docs/rulings/ CLAUDE.md verify/baselines/ verify/out/` ⇒ **空** ⇒ **`R-4`／`R-5` 未觸發**。
**全樹異動** ＝ 上表二檔 ＋ 未追蹤之 `docs/design/`（本批新建）。

### §9 自足性自檢之實測

| 式 | 母體 | 結果 | 判 |
|---|---|---|---|
| 行號錨樣態 | 設計文件（**扣除 `§9` 起至檔末**） | **0** | ✅ |
| 對話側引用樣態 | 同上 | **0** | ✅ |
| 判別力自檢·行號錨 | `docs/reports/W-G.9-29_…md`（同扣除） | **9** | ✅ 非 0 |
| 判別力自檢·對話側 | `docs/reports/W-G.9-25_…md`（同扣除） | **1** | ✅ 非 0 |

🩸 **`R-3` 曾觸發、已修並重驗**（⛔ 不掩蓋）：首版之對話側樣態得 **3**，逐項查明為**兩種病**——

1. **真違規 1 處**：我自己寫的「逐字見**上**「釋示補」錨」⇒ 已改為以 §1 表列之字樣錨取得。
2. **自我污染 2 處**：`§9` 之**自檢式本身**載有該樣態之字面，而母體含 `§9` ⇒ 量測器讀到自己。
   ⇒ 此即 `CLAUDE.md` 已載之形狀（**量測器之母體若含其自身輸出，自檢會自我污染**）
   ⇒ 依其修法將**母體扣除自身**（`sed '/^## §9 /,$d'`），並於文件內就地寫明此扣除與其理由。

---

## §4 ⛔ 本批未做之事

1. ⛔ **未寫任何碼**——本批之產出為**設計文件**；設計 §3 之「改後逐字」⛔ 不得逕行貼上（施工批須先現查其字樣錨仍為 1）。
2. ⛔ **未動** `app.py`／`verify/` 下任何檔／`docs/rulings/`／`CLAUDE.md`／`verify/baselines/`／`verify/out/`。
3. ⛔ **未跑** `run_all`、⛔ 未跑任何探針（本批之現查全為靜態）。
4. ⛔ **未處置** 族② 之兩路徑分歧（既有·設計 §8 已具名）。
5. ⛔ **未查** `3.5m` 之 `telescoping` 破（`Δ 55.606`）之成因（設計 §8 已具名）。
6. ⛔ **未量** `R3`／`R4`／`R6` 之楔形有無 ⇒ **無從判定**、⛔ 非為偽。
7. ⛔ **未指定** 設計 `C-3` 之「側街方向單位向量」之來源變數名——⛔ 未經現查者不得寫成 `【倉】`。
8. ⛔ **未更新** `GB-51` 之原列一字（其裁題已裁之事實由 `W-G.9-29` 之 `↳` 列承載）。

---

## §5 逐條自檢

| # | 戒 | **已套用** |
|---|---|---|
| 1 | **本窗新戒**（逐字區塊 ⛔ 不含須現查之值） | §2 兩佔位符之填值與現查式已具名；且本批入倉之考古 **81** 即該戒之立節 |
| 2 | 紀律第 1 條（先現查生產路徑吃哪一個） | 甲-1 兩路徑逐族·母體正面列舉（22／10） |
| 3 | 紀律第 2 條（「查無」≠「不存在」） | 甲-1 問題 2 逐格正面回答；§0 另記其**對偶**（「查到 ≠ 已占用」） |
| 4 | 紀律第 3 條（無從判定 ≠ 為偽） | `P6` 之 `R5` 標明**須待到達該街廓才可驗** |
| 5 | 紀律第 7 條（錨之一端來自受詞之外） | 設計 §5 六項錨值**全部**指向既有報告或正典，⛔ 無本設計自產之值 |
| 6 | 考古 68（已完成 ≠ 已落地） | §3 之獨立指令驗；插入器內建「純插入」斷言 |
| 7 | 考古 80 ＋ 其增量 | §9 之自我污染已依「母體扣除自身」修法處置 |
| 8 | `VR-011` | 插入器比對前正規化行尾 |
