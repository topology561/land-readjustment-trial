# `W-G.9-219` 執行報告：**停機於前置閘 `G2` 與 `G3`**（⛔ 改一行生產碼）

> 🔒 **本檔＝本批唯一本體**；全以 `git cat-file blob` 復驗（索引側·`GB-139 ②`）。
> 🔒 **態**：`HEAD ＝ d81e509fbcbd74d07d11505371e4a7bc55c64ac7`。
> 🔒 **⛔ 採用單所抄之檔列**——全數當場 `grep`。
> 🛑 **本批⛔ 改生產碼一位元組**——`G2`／`G3` 二閘皆觸發「不過即停」⇒ ⛔ 進入工項一／二／三。
> 🔒 **本 commit 零生產碼**（僅本報告一檔）⇒ ⛔ 與 `-219` 之生產碼混同（該碼尚未寫）。

---

## §一　`G1`　態錨（改前值）

| 量 | 值 |
|---|---|
| `HEAD` | `d81e509fbcbd74d07d11505371e4a7bc55c64ac7` |
| `left-right`（`origin/wip/s1-endpart...HEAD`） | **`0 0`** |
| 追蹤檔數 ／ `.md` 檔數 | **`1612`** ／ **`543`** |

🔒 **生產碼五檔之內容 `sha256`（改前值·閘 P 之基準）**

| 檔 | B | `sha256` |
|---|---|---|
| `app.py` | `1414673` | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` |
| `verify/selection_pipeline.py` | `35367` | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | `96923` | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | `96261` | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | `24681` | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

🔒 **本批未改任何生產碼** ⇒ 上表即改前＝改後值（本 commit 之 `numstat` 見 `§五`）。

---

## §二　🛑 `G2`　**命中集與 `GB-144` 不符 ⇒ 停**

🔒 母體 ＝ `app.py` 索引側 blob **`1414673`** B／**`23906`** 列·粒度框 ＝ **列框**。

### a　現行取值路徑之**全部命中處**（逐處判其性質）

| 檔列 | 性質 | 逐字 |
|---|---|---|
| `app.py:10947` | 🔒 硬停（他義·`T ≤ 0`） | `_stop(f"退縮寬＋畸零地最小寬 ＝ {T} ≤ 0（min_width 查表失敗？·禁靜默 3.5）")` |
| `app.py:12319` | 🔒 硬停（同上） | （與 `:10947` 逐字相同） |
| `app.py:12675` | 註解 | `# 側街退縮（補丁六 §四／補丁七 §五）：源＝既有輸入欄 f3L_setback_default（保留）；` |
| `app.py:12677` | **讀取（無兜底）** | `_setback_raw = _ss_B4.get('f3L_setback_default')` |
| `app.py:12680` | 🔒 硬停 | `f"🔴 f3L_setback_default 未設（街廓 {_this_blk_B4}）·補丁六 §四 no-silent-fallback：禁靜默 3.5 兜底")` |
| `app.py:12703` | 註解 | `#   no-silent-fallback：缺值 loud raise，禁靜默 3.5（UC9898 住宅區×8/12m 恰同檔 3.50·巧合非常數·case_params:26）。` |
| 🔴 `app.py:14259` | **消費端** | `"setback": float(_need("f3L_setback_default")),` |
| 🔴 `app.py:19684` | **兜底**（`.get(…, 3.5)`） | `_setback_init = float(st.session_state.get('f3L_setback_default', 3.5))` |
| `app.py:19689` | UI 欄之宣告 | `key='f3L_setback_default',` |
| 🔴 `app.py:22359` | **兜底** | `_yd_setback = float(st.session_state.get('f3L_setback_default', 3.5))` |
| 🔴 `app.py:22848` | **兜底** | `_p7_sb = float(st.session_state.get('f3L_setback_default', 3.5))` |
| 🔴 `app.py:23810` | **消費端** | `_tag = _wf_tag_of(_wg_ss['f3L_setback_default'])` |

⇒ **現態 ＝ `12` 處**。

### b　與 `GB-144`（`W-G.9-218R` 工項四）所登記者之比對

🔒 `GB-144` 節 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md:3762`–`:3815`（**`54`** 列）；
其所登記之 `app.py` 檔列 ＝ **`9`** 個 ＝
`[10947, 12319, 12677, 12680, 12703, 19684, 19689, 22359, 22848]`。

| 比對 | 結果 |
|---|---|
| `GB-144` `∖` 現態 | **`[]`**（其所登皆仍在 ⇒ 量測器非紅） |
| 🔴 **現態 `∖` `GB-144`** | **`[12675, 14259, 23810]`** |
| **命中集相符** | 🛑 **`False`** |

🔴 **差之三處逐處具名**

| 檔列 | 性質 | 何以重要 |
|---|---|---|
| `app.py:12675` | 註解 | `GB-144` 登了 `:12703` 之註解而漏此 ⇒ 其註解清單不完整 |
| 🔴 `app.py:14259` | **消費端**（`_build_wf_ctx` 之 ctx 建構） | `float(_need("f3L_setback_default"))`——**§7 引擎接線**之退縮入口；**⛔ 兜底**（`_need` 缺值即報錯）。單之工項二令「顯示之值須與下游計算實際使用之值**同源**」⇒ **此即下游之一** |
| 🔴 `app.py:23810` | **消費端** | `_wf_tag_of(_wg_ss['f3L_setback_default'])`——直接下標；**⛔ 兜底**（缺鍵即 `KeyError`）；其下游 `_wf_tag_of`（`:14271`–`:14277`）於退縮 `∉ {0, 3.5}` 時 `raise` |

🛑 **依 `G2` 之令「命中集與 `W-G.9-218R` 工項四所登記者不符 ⇒ 停並回報差異」⇒ 停。**
🔒 **對照組（器非紅）**：`f3L_setback_default` 於 `app.py` ＝ **`9`** 列（須 > `0`）／
一必不存在之鍵 ＝ **`0`** 列（須 `0`）。
🛑 **量測器之自陷（`常規五`·當場更正）**：首版對拍器僅擷取 `app.py:NNN` 形，
漏 `GB-144:3774` 之 `` `app.py:10947`／`:12319` `` **續寫形** ⇒ 誤報 `:12319` 為差異。
補正後之差異為上表 `3` 處（⛔ `4` 處）。

---

## §三　🛑 `G3`　**版面取捨存在 ⇒ 停並回報候選方案，候 KL 定**

### a　退縮欄所在區塊之版面結構（當場讀·⛔ 憑空指定位置）

| 檔列 | 逐字（節錄） |
|---|---|
| `app.py:19670` | `""")`（前一段 `st.markdown` 之結束） |
| 🔴 `app.py:19671` | `with st.expander("⚙️ 街角地參數（共用設定 + 各街廓微調）", expanded=False):` |
| `app.py:19673`–`:19683` | `def _f3L_invalidate_g_cache():`（`on_change` callback） |
| `app.py:19684` | `_setback_init = float(st.session_state.get('f3L_setback_default', 3.5))` |
| `app.py:19685`–`:19691` | `_setback_default = st.number_input("街角地退縮距離（m，全區共用預設）", min_value=0.0, max_value=10.0, value=_setback_init, step=0.1, key='f3L_setback_default', on_change=_f3L_invalidate_g_cache, help='…')` |
| `app.py:19692`–`:19696` | `if st.session_state.get('f3_g_needs_rerun'): st.info("ℹ️ **參數已修改 → …**")`（**條件式**顯示之既例） |

🔒 **鄰近之既有顯示元件樣式**（同區塊·`:19640`–`:19720` 內）：
`st.markdown("---")`／`st.markdown("#### 📍 步驟 L：街角地參數設定 + 第 1 宗街角地優先權 PK（Step G 前置）")`／
`st.info(...)`／`with st.expander(...)`。

### b　🔴 **版面取捨（本閘之停機由**

單之工項二令：**「永遠顯示，⛔ 條件式出現」**。
🔴 **退縮欄現處於 `expanded=False` 之 `st.expander` 內**（`:19671`）
⇒ **摺疊時其內一切元件不可見** ⇒ 若將常設顯示置於同一 expander 內，
**⛔ 滿足「永遠顯示」**——其為**條件式（依使用者是否展開）**。

🛑 ⇒ **「可放之處有版面取捨」** ⇒ 依 `G3` 之令**停並回報候選方案，候 KL 定**。

### c　候選方案（⛔ 由 CC 擇一）

| 案 | 位置 | 得 | 失 |
|---|---|---|---|
| **甲** | 移至 `st.expander` **之外·其上方**（`:19670` 之後、`:19671` 之前） | ✅ 永遠顯示；與「步驟 L」標題相鄰、語意相符 | 於「步驟 L」標題與參數摺疊器之間插入一行，**壓縮該區塊之視覺密度** |
| **乙** | 置於 `#### 📍 步驟 L` 標題**同列之後**（併入標題行） | ✅ 永遠顯示；⛔ 增行 | 標題行變長；退縮值之語意被標題吸收，**⛔ 醒目** |
| **丙** | 置於**頁面側欄**（`st.sidebar`） | ✅ 永遠顯示且不佔主欄 | **⛔ 與退縮欄相鄰** ⇒ 使用者改欄時須跨區確認，違「顯示與計算同源」之直觀性 |
| **丁** | 於 expander 之**標題字串**內動態嵌入（如 `⚙️ 街角地參數（退縮 X m·預設）`） | ✅ 永遠顯示（expander 標題恆可見）；⛔ 增行、⛔ 壓縮版面 | 標題字串變長；**其為 expander label，⛔ 標準之資訊元件** |

🔒 **CC 之判（⛔ 裁）**：**甲**於「永遠顯示」「與欄相鄰」「用標準元件」三者皆成立，
其代價僅為一行之視覺密度；**丁**次之（不增行）惟以 label 承載資訊非標準。
🛑 **⛔ 由 CC 擇一** ⇒ 候 KL 定。

---

## §四　🔒 併呈：工項一之要害（`0` 之判別）**於現行碼中之實況**

🛑 **本節係唯讀勘查，⛔ 工項一之執行**（`G2`／`G3` 已停）。

| 處 | 判空之寫法 | `0` 之判別 |
|---|---|---|
| **主路徑** `app.py:12677`–`:12681` | `_setback_raw = …get('f3L_setback_default')` ⇒ `if _setback_raw is None: raise` ⇒ `float(_setback_raw)` | ✅ **正確**——`is None`（⛔ falsy） |
| `app.py:12676`（其註解逐字） | `#   no-silent-fallback：缺值 loud raise，禁靜默編造 3.5（W-B \`or 3.5\` 陷阱）；**0.0 合法（0m 情境·不竄改）**。` | ✅ **碼面已明載 `0.0` 合法** |
| `app.py:19684`／`:22359`／`:22848` | `st.session_state.get('f3L_setback_default', 3.5)` | ✅ **⛔ falsy**——`.get(k, d)` 之 `d` 僅於**鍵不存在**時生效；鍵存在且值為 `0.0` 時回 `0.0` |
| `app.py:14259`／`:23810` | `_need(...)`／`_wg_ss[...]` | ✅ **無兜底**（缺值即報錯） |
| `app.py:10945`／`:12317` | `T = float(setback or 0.0) + float(min_width or 0.0)` | 🟡 **唯一 falsy 形**——惟 `0.0 or 0.0` ＝ `0.0` ⇒ **數值無害**（風格問題） |

🔴 **⇒ 「使用者輸入 `0` 因 falsy 而落回預設」於現行碼中<u>未見實例</u>**。
🛑 **該判尚須以工項三 `a` 之四情境實測坐實**（本批已停·未跑）
⇒ 🔒 **⛔ 據本節斷言工項一無事可做**——其為**唯讀勘查**，非實測。

🔒 **併記（`GB-144` 之登記不完整）**：`:14259`／`:23810` 二消費端未入 `GB-144`
⇒ 其「三層」之第二層（兜底）清單**遺漏二處無兜底之消費端**。
🛑 **本批⛔ 補登**（`-219` 之射程不含改 `GB-144`）⇒ **候發單側裁**。

---

## §五　閘

### 閘 P：生產碼五檔之 `sha256`（本批**未改一位元組**）

五檔之 `sha256` 與 `§一` 之改前值**逐檔相同**（本 commit 之 `numstat` 不含任一生產碼檔）。

### `deletions` ／ `left-right`

本 commit 之 `numstat`：`docs/reports/W-G.9-219R_停機_前置閘G2與G3.md` 之 `+N 0`（**新檔**）
⇒ **`deletions ＝ 0`**。`left-right` ＝ **`0 0`**（推後復驗見聊天回報）。

---

## §六　🛑 候裁（`-219` 之續行條件）

1. 🛑 **`G2`**：命中集差 `3` 處（`:12675` 註解／🔴 `:14259`、`:23810` **二消費端**）。
   續行前須：（甲）由發單側更新 `GB-144` 之登記使命中集相符；或（乙）於單內明載該三處之處置。
   ⛔ 由 CC 自行補登或自行放行。
2. 🛑 **`G3`**：退縮欄在 `expanded=False` 之 expander 內 ⇒ **⛔ 滿足「永遠顯示」**。
   四候選方案（甲／乙／丙／丁）已出艙，**候 KL 定**。⛔ 由 CC 擇一。
3. 🟡 **工項一之要害於現行碼未見實例**（唯讀勘查）——續行時仍須以工項三 `a` 之四情境**實測**坐實，
   ⛔ 以本節之勘查代之。
4. 🔒 **本批⛔ 改生產碼一位元組**；`-219` 之【回報後之處置】未進入（其前提為 `a`–`c` 完成）。
