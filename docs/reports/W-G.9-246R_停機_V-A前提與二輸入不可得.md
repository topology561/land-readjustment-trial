# `W-G.9-246R`　🛑 **停機上呈**：`V-A` 之前提不成立 ＋ 二項輸入於掛載點結構上不可得

> 🛑 本文 **⛔ 鑄任何號**（自誤／`GB`／`VR`／戒／`K-9` 一律不鑄）——鑄號屬**意思決定**，候發單側。
> 🔒 本文一切數字**現查於倉**；三軸逐處載明（`VR-091 一`）。
> 🔒 **工項零已辦畢並推進主線**；**工項一–三 ⛔ 動一行生產碼**（停於偵察）。

---

## §零　態錨（本文全部量測之來源）

| 項 | 值 |
|---|---|
| 主線 `origin/wip/s1-endpart` | **`34230bb3e9d22cb629446f59e96c48456907b5f7`**（`ls-remote` 現查） |
| 本文全部量測之受詞 | **倉側 blob@`34230bb`**（`git cat-file blob`·`GB-139`） |
| `app.py` `sha256[:16]` | `ad7a91dba08119f5` |
| `verify/stepg_pipeline.py` `sha256[:16]` | `0260273350021311` |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/**.md` | `1667` ／ `592` ／ `570` ／ `542` 檔 |

---

## §一　工項零：**已辦畢**（`34230bb`·已推主線）

`docs/orders/W-G.9-246_逐宗驗證站_lot_gate觀測模式.md` 原封入倉（`6779` B／LF `104` 列）。
**落檔 `sha256` ＝ 入倉 blob 內容 `sha256` ＝ `5e5b7386…d658d8a5`**（逐位）。

### 開工閘（`§零`）逐項結果

| 閘 | 結果 |
|---|---|
| `S-0` `SELF_SHA256` | ✅ **逐位相符**（受詞 `6701` B·出現 `2` 次取最末）；判別力：對全檔施同一口徑得 `5e5b7386…` ≠ 單載 ⇒ **非恆綠** |
| `S-1` 宣告框三數分列 | ✅ `246` `D1 0`／`D2 0`／`D3 0` ⇒ 檔框 `0`·列框 `0`（鬆框 檔 `1`／列 `1`）⇒ **未占用**；對照甲 `245` `1`／`1`／`0`（須 `≥1`）✅；對照乙 `247` `0`／`0`／`0`（須 `=0`）✅ |
| `S-3` 生產路徑十檔 | ✅ **10/10 逐檔相符** |
| `S-0b` `probe_order_preflight` | ✅ `--selftest` `rc=0`·**四造全數成立**；本單 `rc=0`·🔴 `0` 項／🟡 `0` 項·`stderr` `0` B |

🔒 `S-1` 之三軸：來源 blob@`a9e84c2`（單所載之態）／母體 全 `docs/` **`569`** 檔（其中 **`19`** 為二進位·不可解碼）／粒度框 `D1` 檔框 ＋ `D2` 列框 ＋ `D3` 列框。
🔒 **`246` 之鬆框唯一落點逐筆歸類**（`裁 H`：命中須逐筆歸類·⛔ 只報數）：
`docs/reports/W-G.9-245_CC交接文.md:60` ＝ `§二` 次號表之**計算式** ⇒ 依補款 `②` **⛔ 構成占用**。
🔒 與單 `§零` `S-1` 之發單側表**逐格相符**。

---

## §二　🛑 停機事由（三項·逐項附正面列舉 ＋ 兩造對照組）

### 🔴 發現一（**停機款**）　`V-A` 之受詞於現態**結構上不可得**——`自誤 300` 同族·**第二度**

單 `§四` 逐字：

> **`V-A` 迴歸（🔴 本批為真·非構造性）**　拋棄式 clone 改前／改後各實跑 UC9898 二情境至自然中止；
> Step G 之 `g_rows`／`conv_rows` **去除 `驗_*` 新欄後逐位 diff 空**。

🔒 **碼面實證（⛔ 推理·逐列逐字·blob@`34230bb`）**

`verify/run_verification.py`：

```
 667|        try:
 668|            _sg = run_step_g(ns, fake_st, list(cb_by.values()), cad, snapshot,
 671|            g_tab, diag_tab, slot_tab = build_step_g_tables(_sg)
 672|            _dump_csv(g_tab, os.path.join(OUTDIR, f"got_G值_退縮{tag}.csv"))
 673|            _dump_csv(diag_tab, …)
 674|            _dump_csv(slot_tab, …)
 696|        except RuntimeError as _e_sg:
 697|            results.append((f"v3·StepG{tag}（結構閘/看守觸發）", False,
 698|                            [f"[StepG{tag}] {_e_sg}"]))
```

⇒ 🔴 **`run_step_g` 一旦 `raise`，`:671`–`:674` 一列不執行** ⇒ `g_tab` 不產生、`got_G值_退縮*.csv` **不落檔**。

`verify/stepg_pipeline.py` 之二中止點（**皆在逐街廓迴圈內·皆在 `run_step_g` 之 `return` 前**）：

| 情境 | 中止點 | 逐字 |
|---|---|---|
| 退縮 `0 m` | `:1003` | `raise RuntimeError(f"街廓 {blk_label} 抵費地計算失敗：{_eOff}")` |
| 退縮 `3.5 m` | `:1261` | `raise RuntimeError(f"🔴 結構閘 telescoping 破：街廓 {blk_label} …")` |

🔒 **二情境皆於街廓 `R2` 中止**（【倉】`docs/reports/W-G.9-172R_池面積歸因與步驟0前置量測.md:27`；
破量 【倉】`docs/reports/W-G.9-69_重烤與乙式耦合斷言.md:273`）。
🔒 **本窗交接文 `§五` 已逐字預告**：`F.0`〜`F.4` 逐級「跳過（存在性守衛）」⇒ `got_F.2_*`／`got_F.4_*` **一檔未生成**
⇒ **`conv_rows` 亦不可得**（其源 ＝ `F.2`／`F.3` 之 `d2["conv_rows"]`／`d3["conv_rows"]`）。

🔴 **併壞其判別力款**：單自附「🔒 併證 `V-A` 之判別力：改後之 `驗_*` 欄**非全空**（否則量測器未接）」
——該自證亦**經同一管線**（`g_tab` → `_dump_csv`）⇒ **一併不可得**。
⇒ 🛑 **`V-A` 於現態既非「可執行但無鑑別力」，而是<u>不可執行</u>。**

🩸 **同族第二度**：`自誤 300` 鑄於**上一個 commit**（`a9e84c2`），其標題逐字 ＝
「**驗收條款之前提未查：以「某輸出可得」立驗收，而該輸出之產生路徑⛔ 涵蓋本批所改之碼**」。
`W-G.9-244R3` commit message 亦已逐字載「單所稱『Step G 輸出可得』於現態不成立（候裁）」。

---

### 🔴 發現二　`V-S`（**本批之要義**）之母體結構性殘缺——harness 側只剩 **`A` 一項 × `R1` 一街廓**

單 `§四` 逐字：「**`V-S` 觀測統計（🔴 次一單之地基·⛔ 略）**　每情境 × 每街廓 × 每鏈：`A`／`B`／`C` 各四態之計數」。

| 判 | app 二站 | harness 二站 | 依據 |
|---|---|---|---|
| **A** 幾何 | 🟢 可判 | 🟢 可判 | `res['cut_coords']` ＋ `get_min_lot_size` 查表 |
| **B** 藍影 | 🟢 可判 | 🔴 **恆 `不可判`** | 見下之正面列舉 |
| **C** 面積 | 🟢 可判 | 🔴 **恆 `不適用`** | **單自載**：「harness 因 `eff ＝ {}` ⇒ C 恆 `不適用`」 |

🔒 **`B` 不可得之正面列舉**（母體 ＝ 逐檔·粒度框 **列框**·blob@`34230bb`）

| 檔 | `f3_corner_range_polys` | `f3_corner_range_areas` |
|---|---|---|
| `verify/stepg_pipeline.py` | **0** | **0** |
| `verify/run_verification.py` | **0** | **0** |
| `verify/selection_pipeline.py` | **0** | **0** |
| `verify/run_all.py` | **0** | **0** |

**兩造對照組**：`f3_cad_side_lines_by_side` @ `stepg_pipeline.py` ＝ **`1`**（器須綠 ✅）／
一必不存在之符號 `_NOSUCH_SYMBOL_ZZ` @ `app.py`／`stepg` ＝ **`0`／`0`**（器須紅 ✅）⇒ **量測器非紅**。

🔒 **加強證**：`app.py:12824`–`:12845` 之寫入處包在 `try: import streamlit as _st_cr6 … except Exception: pass`
⇒ 其寫入之受詞係**真 streamlit 之 session_state**，**⛔ harness 之 `fake_st`**
⇒ 縱使該段於 harness 內執行，`stepg` 之 `ss` 亦**看不到**。
🔒 `stepg` 於掛載點可得者僅 `_row_for_buffer.get('【左/右】街角最小面積(㎡)')`（**一個數**），
**⛔ 多邊形** ⇒ `_blue_shadow_tri` 之必要入參 `corner_poly` 取不到。

🛑 **⇒ harness 側之 `V-S` ＝「`A` 一項 × `R1` 一街廓」**（`R2` 中止·`R4`／`R5`／`R6` 一筆不產生）。
🛑 **app 側之 `V-S` 於自動驗收下 ＝ `0`**——`app_harvest.harvest()` **⛔ 呼叫 `main()`**
（倉內二 fixture 逐字自證：`verify/fixture_klui_t2diag.py:10`–`11`／`verify/fixture_n14_feed_chain.py:7`–`9`），
而四站中之 app 二站**全在 `main()` 內**（`def main` 區間 AST 實查 ＝ `14629`–`24044`·由 `S-0b` 之 `P-3` 記錄欄現查）
⇒ app 側之 `驗_*` **只有 KL 於 UI 實跑方見得到**。

⇒ 🛑 **單 `§五` 所令之上呈（「若接閘，UC9898 二情境各有幾宗、哪幾宗、因哪一項會被剔」）
於現態<u>不可得</u>**；且單自設之防呆「若不合格宗數異常（`0` 或近全數）⇒ 先疑判定式」
**恰會被本結構觸發**（`B`／`C` 於 harness 恆非「不合格」）。

---

### 🟡 發現三　單漏令補列 `_WF_NS_NAMES`——**無編號之戒第五度**（⛔ 停機·CC 已備妥處置）

單 `§二 a` 逐字：「harness 經 **`ns["_lot_gate"]`** 取用」。

🔒 **單內命中（正面列舉·母體 ＝ 該單單檔·列框）**：
`_WF_NS_NAMES` **`0`**／`_wf_ns` **`0`**／`接線清單` **`0`**；對照組 `_lot_gate` ＝ **`6`** ⇒ **器非紅**。

🔒 **倉內既有之戒逐字**（【倉】`docs/reports/W-G.9波_claude.ai側自誤登記.md:3717`）：

> **戒（新增）**：🛑 凡令受單方於 `verify/**` 新增 **`ns[<字面>]` 之消費**者，
> 單內須同時令其現查 `app.py` 之接線清單（`_WF_NS_NAMES`）並補列；⛔ 不得只令新增消費端。

🔒 **不補列之後果（碼面實證）**：`app.py` `_wf_ns()`（`:14180`–`:14188`）逐字
`raise RuntimeError(f"🔴 七級調配接線：app 缺真符號 {n}…")`——**其迴圈之母體即 `_WF_NS_NAMES`**；
而 `verify/fixture_wf_ns_wiring.py` 係為此**專設**之雙向守護夾具（其 docstring 逐字自載 `W-G.9-4` 之同型事故）。
⇒ 漏列 ⇒ **app 生產路徑必 `KeyError`／`RuntimeError`**，且該夾具轉紅。

🟢 **CC 之處置（⛔ 待裁·受詞在單之射程內）**：`_WF_NS_NAMES` 位於 `app.py`，屬單所許之二檔
⇒ **逕行補列 `"_lot_gate"` 並逐字回報**（`常規二` 取保守項之體例）。**本項⛔ 構成停機事由。**

---

## §三　對單之逐條影響

| 單之條款 | 影響 |
|---|---|
| `§二` 工項一（`_lot_gate` 純函式） | 🟢 **不受影響**——其為 module 級純函式，`V-D`（合成輸入之單元對拍）**可執行**且係真鑑別力所在 |
| `§三` 工項二（四站掛載） | 🟡 **可實作**，惟其**觀測產出於自動驗收下幾不可見**（見發現二） |
| `§四` `V-A` | 🔴 **不可執行**（發現一） |
| `§四` `V-S` | 🔴 **母體殘缺**（發現二）⇒ ⛔ 作為次一單之地基 |
| `§四` `V-B`／`V-C`／`V-D` | 🟢 **可執行** |
| `§五` 上呈 | 🔴 **其受詞不可得**（發現二） |

---

## §四　候發單側裁（🛑 ⛔ 由 CC 自裁）

| # | 待裁 |
|---|---|
| `1` | **`V-A` 之替代**：是否改採 `W-G.9-244R3` 之既例——「**行為逐位相同（含同點 `raise`）**：結果物正規化序列化之 `sha256` ＋ 全 `stdout` 於改前／改後逐位相同，對照組 ＝ 二輪 `app.py` `sha256` 相異」？（該既例已於倉內施行並經 KL 之窗接受） |
| `2` | **`V-S` 之射程**：接受「harness ＝ `A` × `R1`」之殘缺母體並照實出艙，抑或**擴大單之射程**把 `corner_poly` 經 ctx 送進 harness（⇒ 須動 `_build_wf_ctx`／`run_verification.py` ⇒ **逾單所許之二檔**）？ |
| `3` | **app 側觀測之取得法**：`main()` 內之四站產出只有 KL 於 UI 可見 ⇒ 是否須另立 KL 目檢工項（同 `W-G.9-197` 之 UI 實跑既例）？ |
| `4` | **是否鑄號**：發現一是否鑄 `自誤`（同族第二度·責 ＝ 發單側）；發現三是否鑄（無編號之戒第五度）。**⛔ 由 CC 自裁**（`常規四`）。 |

---

## §五　⛔ 受本停機影響、可即辦者（候放行後續辦）

1. **工項一 `_lot_gate`** 之實作 ＋ `_WF_NS_NAMES` 補列（生產碼 ⇒ 側分支 `verify/W-G.9-246-c1`·⛔ 推主線）。
2. **`V-D`** 之合成輸入對拍（四項 × 四態各至少一例；含邊界 `G ＝ area(藍影)` 須 `不合格`、`G ＝ e` 須 `合格`）。
3. **`V-B`**（二檔 diff 逐行人閱）／**`V-C`**（十檔雜湊）。

🔒 **偵察已備妥之碼面事實**（供次一單直接引用·皆 blob@`34230bb`·列框）

| 受詞 | 現況 |
|---|---|
| 四站掛載點 | `app.py:21694`／`:21798`；`stepg:676`／`:767` —— **與單所載逐列相符**；右鏈二次 solve `stepg:737`／`:751`（`app:21766`／`:21781`）⇒ 掛於**定案之 `res`** 後 |
| `res` 之可用鍵 | `G`／`area_geom`／`S`／`S_raw`／`W`／`W_far`／`W_near`／`cut_coords`／`_宗地寬度`／`_alloc_dir_used`／`trace` |
| `cut_coords` 之空值路徑 | `_solve_G_one:13695` 之代數迭代 fallback 逐字 `_r['cut_coords'] = []` ⇒ **`A` 於該路徑須 `不可判`**（⛔ 當不合格） |
| 既有四態基建 | `_T3_NA:12215`／`_t3_fmt_gate:12218`／`_t3_gate_line:12232`／`_t3_gate_emit:12271` —— **已在倉·⛔ 需新造** |
| 既有閘 | `_k923_gate1:12174`／`_k923_gate2:12203`／`_blue_shadow_tri:12094`／`_rect_fits_free_pose:11907` |
| 🔴 上開六者之**生產呼叫點** | **皆 `0`**（正面列舉·`app.py` 單檔）⇒ 建置未接線·本批為其**首個消費端** |
| 列之寫入處 | `_build_g_row` 於**二檔各為 `main()`／`run_step_g` 內之閉包**（`app.py:21172`／`stepg` 同構）；`_rows_local.append(_build_g_row(…))`（`app:21711`／`:21812`·`stepg:691`／`:782`） |
| `W`／`D` 之來源 | `get_min_lot_size(category, front_road_width)`（⛔ 硬編·`K-9-12 二`）；`stepg` 於 `:277`–`:280` 已有同式 |

---

🛑 **本文之界限（照實·⛔ 頂替）**：發現一／二係**碼面結構之推證**（`raise` → `except` → `_dump_csv` 不執行；
`grep` 命中 `0` ＋ 兩造對照），**⛔ 實跑復現**——實跑須 `15`–`20` 分鐘之拋棄式 clone 長跑，
且其結論已由**倉內二份既有報告**逐字載明。若發單側要求實跑復現，CC 可即辦。
