# `W-G.9-251R`　CC 新 session 交接文（`2026-09-08`）

> 🔑 **開窗前四件事見 `§八`**。本文一切數字**現查於倉**；⛔ 採本文之數——**每一節皆須重跑**。
> 🛑 本文 **⛔ 鑄任何號**（自誤／`GB`／`VR`／戒／`K-9` 一律不鑄）。
> 🔒 **取號之由**：依 `常規四（三）`（CC 對某單之報告、產物一律「該單號 ＋ 後綴 `R`」·⛔ 另取新號）
> ⇒ 本文取 **`W-G.9-251R`**（本窗最後一單 ＝ `W-G.9-251`；同號多產物之既例 ＝ `-246R2` 之二份）。
> ⛔ **取 `W-G.9-252`／`-253`**（未來單號·意指未定）；⛔ **取 `-249`**（`K-6` 段三·發單側出單中）
> 與 **`-250`**（接閘單·KL `2026-09-07` 改號）。

---

## §一　態錨（**開窗第一動作 ＝ 重跑本節**）

| 項 | 值（現查 `2026-09-08`）|
|---|---|
| 主線 `origin/wip/s1-endpart` | **`c524cb8ee09b3c2003ebe732f254b45844db3401`** |
| 🟢 **未併之側分支** | **無**——`-248-c1` 已於 `2026-09-07` 經 KL 放行併線 |
| 已併之側分支（⛔ 刪·復驗痕跡）| `verify/W-G.9-198R-c1`／`-c2`／`-c3`／`-219R2-c1`／`-229-c1`／`-243-c1`／`-244-c1`／`-244-c2`／`-246-c1`／`-247-c1`／**`-248-c1` ＝ `2709504b2aad9f4924d4182ef37e3d47d861767e`** |
| 舊分支（⛔ 工作分支）| `main` ＝ `6cb77b7`／`claude/wg9-95z-v3-resume-hgpzfs`／`wip/W-G.4-S0b-S0c` |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/**.md` ／ `docs/orders/` | **1684** ／ **607** ／ **585** ／ **557** ／ **62** 檔 |
| `CLAUDE.md` | **193298** B／**2354** LF／`sha16 663e334b78b45548` |

**生產路徑十檔 ＋ 夾具之 `sha256[:16]`（`blob@c524cb8`）**

| 檔 | 前 16 位 | 檔 | 前 16 位 |
|---|---|---|---|
| `app.py` | **`4860f10828fb8ae9`** | `verify/wf_f0.py` | `6758ea766b001b95` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/run_verification.py` | `d0d531f7dc051b65` | `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/stepg_pipeline.py` | **`c8d46d2e5fb79dc5`** | `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/run_all.py` | `4ba89fef90979491` | `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| 夾具 `verify/fixture_wf_ns_wiring.py` | `b17826a6f7b98bf5` | | |

🛑 **十檔 ＝ 閘 `P` 五檔 ＋ `wf_f*` 五檔**；🔴 **夾具⛔ 屬十檔**（`自誤 307` 之受詞）。

**本地保全 ref（`keep/*`·⛔ 遠端）**：`-198R-c2`／`-c2-preRebase`／`-198R-c3`／`-c3-preRebase`／
`-219R2-c1`／`-229-c1`／`-246-c1` ＝ `29ebc14`／`-246-c1-preRebase` ＝ `200dc53`／`-247-c1`／
**`-248-c1` ＝ `2709504`**／**`-248-c1-preRebase` ＝ `89a9a7a`**。

**本窗之 commit 序（自 `e0c3971` 起·`11` 個）**

```
c524cb8 W-G.9-251R 段乙停機上呈：工項一之意圖與 I-2 相斥 ⛔ 零生產碼 ⛔ 零號鑄造
d8b80e9 W-G.9-251R 段甲：工項零（本單原封入倉）＋ 工項四（鑄 自誤 312）⛔ 零生產碼
1e5a87c W-G.9-248R 段乙併線之出艙報告 ⛔ 零生產碼 ⛔ 零號鑄造
2709504 W-G.9-248 c2：工項一（K-9-1 唯讀顯示·丙案）🔴 生產碼·⛔ 推主線
d8774e3 W-G.9-248 c1：工項二（正面道路名稱欄）＋ 工項三（混型欄顯示層修）🔴 生產碼·⛔ 推主線
e4b35cf W-G.9-248R 段乙報告：丙案落地與末態驗收 ＋ 鑄 自誤 311 ⛔ 零生產碼
6010a80 W-G.9-248R 段乙停機上呈：工項一之 I-2（st.form 致延遲）⛔ 零生產碼 ⛔ 零號鑄造
dde8ba8 W-G.9-248R 段甲：工項零（本單原封入倉）＋ 工項四（鑄 自誤 310）⛔ 零生產碼
f73ce5e W-G.9-246R3 §七：收工閘之實測補記 ⛔ 零生產碼 ⛔ 零號鑄造
cc9cbff W-G.9-246R3：KL 二裁落地 ＋ -246-c1 併線 ⛔ 零生產碼
29ebc14 W-G.9-246R2 工項一〜三：逐宗驗證站 _lot_gate（觀測模式）🔴 生產碼·⛔ 推主線
```

---

## §二　次號（**現查·⛔ 採本文之數·開工前重算**）

| 簿 | 現況（`blob@c524cb8`）| 次號 |
|---|---|---|
| 自誤（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）| **630066** B／**7397** LF；框 ＝ `VR-091 四` **定義框 ＋ 範圍框**；列命中 **310**／範圍框 **8**／相異 **305**／`MIN 7`／**`MAX 312`**／缺號 **`[106]`**／恆等式 `True` | **`313`** |
| `GB`（`docs/reports/W-G.4_泛用阻塞項登記表.md`）| **681606** B／**5036** LF；標題錨定框 `MAX` **151** | **`GB-152`** |
| `VR`（`docs/驗證裁定登記表.md`）| **364971** B／**4695** LF；標題形相異 **76**（哨兵 `999` 已扣）·`MAX` **91** | **`VR-092`**（三位補零）|
| `K-9`（`docs/rulings/K-6_…md`）| **285233** B／**3972** LF；`MAX` **`K-9-28`** | **`K-9-29`** |
| 戒（母體 追蹤 `.md` **607** 檔·命中 **4** 檔·`W-G.9-188 裁 B` 新式）| 相異 **23**／`MAX` **40** | **戒 `41`** |
| 單號 | `249` 宣告框 `0/0/0`（鬆 `2`／`2`）**惟已由發單側指為 `K-6` 段三**；`250` `0/0/0`（鬆 `3`／`3`）**惟已指為接閘單**；`252` `0/0/0`（鬆 `1`／`1`）／`253` `0/0/0`（鬆 `0`／`0`）| 🛑 `249`／`250` **保留**；CC 自起事者須先向發單側取得其最後單號（`常規四（四）`）|

🛑 **`GB` 之「相異／缺號集」⛔ 出艙**（`CLAUDE.md` 字樣錨 `在其釘定前，⛔ 得出艙`）。
🔒 **自誤之缺號集必用<u>二框</u>**（定義框 **＋ 範圍框**）——**只用定義框**得 `[106, 170–182]`，
`170`–`182` 由**攢批範圍標題**涵蓋（`戒 29`：一張表只准一個框）。
🔒 **鑄號前須取該類<u>既有最末一則</u>之標題形逐字為範本**（`常規四（九）四`）。

---

## §三　工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——前置 ＝ `git ls-remote origin wip/s1-endpart` 已含該 commit（**⛔ `origin/main`**）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**；**`--selftest` 綠亦⛔ 蘊含閘已生效**（須**活體實測**）。
3. 🔒 **倉內報告 ＝ 唯一本體**；聊天 ＝ ping。push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
4. 🔒 **數字錨一律引倉檔**，⛔ 憑記憶出艙；與倉態衝突以倉為準。
5. 🔒 **工項零優先於一切**——收單後 **① 查宣告框占用（三數分列）→ ② 原封入倉 `docs/orders/`**（⛔ 顛倒）。
   驗「落檔 `sha256` ＝ 入倉 blob」。🛑 該閘只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。
6. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（經發單側）。
7. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 `deletions ＝ 0`。
8. 🔒 **常規補款一**：閘之量測與其所守護之動作（`commit`／`push`）**⛔ 置於同一命令塊**。
9. 🔒 **常規五**：每一量測須有**兩造對照組**（器須綠 ＋ 器須紅）。
10. 🔒 **零生產碼 commit 逕推**；**生產碼 commit 走側分支**。生產碼檔 ＝
    `app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`
    （判法逐 commit：`git diff --name-only <前一已 push>..<本 commit> | grep -xE …`·**空輸出亦須出艙**）。

### 🔴 生產碼 commit 之流程（**⛔ 逕推主線**·本窗已二度施行）

```
報告先行入倉主線（零生產碼·逕推）——W-G.9-194 裁四
  → 生產碼本地 commit（可多個·保留單所令之 commit 結構）→ ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c1 ＋ 本地保全 ref keep/<單號>-c1
  → 本地分支回退主線（git update-ref ＋ git read-tree --reset -u）
  → 發單側逐位復驗 → 請示 KL → KL 逐字放行
  → 主線若於其間前進 ⇒ 依 W-G.9-206 §八 rebase（純 plumbing）＋ --force-with-lease 重推 ＋ 重驗
  → 純快轉主線（git push origin <sha>:refs/heads/wip/s1-endpart）·⛔ merge commit
  → 併後報：主線 SHA／十檔變不變／夾具／全部遠端 heads；側分支 ⛔ 刪
```

🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。
🔑 **rebase 之最強同一性閘**（本窗二度用之）：
**`sha256(git diff <舊基座> <舊側>)` == `sha256(git diff <新基座> <新側>)`**
——`-246-c1` 得 `b711d9ac9c86f49152a71fc7`；`-248-c1` 得 `54ae67285a15c4612c553144`。
其餘：`is-ancestor rc=0`／`--merges 0`／父數逐一 `1`／rebased tree vs 舊側之差 ＝ **恰為主線那幾檔**。

---

## §四　現行之閘與框（**開工前逐一重跑**）

| 閘 | 母體／框 | 現況 |
|---|---|---|
| **閘 `P`** | **五檔**之內容 `sha256`（基準見 `§一`）| 零生產碼批須前後逐位相同 |
| **號占用閘（宣告框·三數分列）** | 母體 ＝ **全 `docs/`**；`D1` 檔名 ∪ `D2` 列首 `#+` ∪ `D3` 自稱形 | 收單後、入倉**前**；**已占即停·⛔ 自行改號** |
| **`S-0b`** | `python verify/probes/probe_order_preflight.py <單>`（**須於倉內**·`GB-148`）| `P-3`／`P-5` 機械停機款；`P-1`／`P-2`／`P-4`／`P-6` 提示**須逐項具名處置**·⛔ 靜默略過 |
| **`V-0`** | `run_all` 全名目 `rc` 前後相同 | 🔴 **比對框須用<u>放寬框</u>** ⇒ 見下 `1` |
| **`PreToolUse` heredoc 硬閘** | `.claude/settings.json` ＋ `verify/tools/wg9237_heredoc_guard.py` | 🟢 **本窗實測生效並攔下一次** |
| **`wg942_append_audit`** | `TARGETS` **77** 筆／相異路徑 **62** | 🔴 **⛔ 涵蓋 `docs/配地計算總規格_v3.md`** ⇒ 動 `v3` 須另驗 |

### 🆕 本窗新捕之陷阱（**逐條皆有實例·付過學費**）

| # | 受詞 |
|---|---|
| `1` | 🔴 **`V-0` 之比對框須用放寬框**（`^  (✅｜🔴｜🟡) `）——本窗實測 **窄框 `34` 列／放寬框 `71` 列 ⇒ 多抓 `37`**；判分佈 `✅28`／`🔴35`／`🟡8`。窄框漏抓「末端夾具」形 ⇒ **偽綠** |
| `2` | 🔴 **CRLF vs LF 之量測框**——工作區（`autocrlf=true`）為 CRLF 而 `git cat-file blob` 為 LF ⇒ 比對「函式源碼逐位相同」時**差恰 ＝ 該函式行數**、**必偽紅**。**讀工作區一律 `.replace(b"\r\n", b"\n")`**。判法：`git diff` 若顯示該處僅為 **context line** ⇒ 未改 |
| `3` | 🔴 **靶自檢（⛔ 可省）**——合成靶之**每一受詞欄**須**實際**具備所測性質。本窗 `V-4` 之 `【左】達資格候選` 二列皆 `int`、無哨兵 ⇒ 「還原其混型須失敗」**偽綠**、「哨兵可辨」**偽紅**。⇒ 靶自檢須列為 `⓪` |
| `4` | 🔴 **⛔ 假設原始碼之空白為單一空格**——`'RD': '道路'`（單空格）命中 `0`，真碼為 `'RD':     '道路',`（**多空格對齊**）⇒ 驗碼面須**先取該行原文**，或用**去空白正規化**之比對 |
| `5` | 🔴 **⛔ 重用前批寫死期望值之驗證腳本**——`s4_check.py` 之「`MAX` 須仍 `311`」於次批必偽紅（`W-G.9-214 🔧 四`「閘之通過條件⛔ 內嵌未於同會期當場復現之數」之反例）。**判準須由二態當場復算** |
| `6` | 🩸 **shell 內嵌 `python -c` 本窗已四度出錯**——① raw-string 使 `\u` 未被解釋 ⇒ `FileNotFoundError`；② `glob` 過寬（`7` 命中）；③ **`\r\n` 被 shell 展開為真換行而毀檔**；④ `\s` 逃脫警告。⇒ **含反斜線之字面一律 `Write` 落檔**（`GB-143`）|
| `7` | 🔑 **步驟區塊之邊界 ＝ 相鄰二 `st.markdown("####`**（⛔ 目視）。現態：D `:19354`／**E `:19595`**／F `:19800`／H `:19856`／G `:19928`／**L `:20010`** ⇒ **步驟 E ＝ `[19595, 19800)`** |
| `8` | 🔑 **`檔框 ≠ D1`**——檔框 ＝ `\|D1 ∪ D2 之檔 ∪ D3 之檔\|`。本窗**二度**（CC 一次、發單側一次）|
| `9` | 🔑 **Streamlit form 語意**：form 內 widget 改值**⛔ 觸發 rerun**，其回傳值與 `session_state[key]` **須提交方更新**；form 內設 `on_change` **必 `raise`**（`streamlit/elements/lib/policies.py:43-54` `check_callback_rules`）|
| `10` | 🔑 **Browser pane 驅動 Streamlit**：`number_input` 之合成 `Enter` **無效**（顯示「Press Enter to apply」而 rerun 不動）⇒ 須點 **`+`／`−` stepper**；form 內用 **Tab 失焦**⛔ Enter；pane 初開 **viewport `0x0`** ⇒ 須先 `resize_window` |
| `11` | 🔑 **`run_all` 二 clone 可併跑**——該戒之受詞係 CSV 互相覆寫，二獨立 clone 之 `verify/out/` 隔離（實測產物檔數皆 `521`、`got_*` `8` 檔逐位同）。**目錄名須同長度**（記憶 ㊲）|
| `12` | 🔑 **`with` 非函式作用域**——`with st.expander(...)` 內所賦之名，於該區塊**之後仍可及**（`W-G.9-226` `G4'` 已活體實測）。此為 `-251` 丁案之基礎 |
| `13` | 🔒 承前波：`GB-139`（`autocrlf`）／`GB-143`（heredoc 硬閘）／`GB-145`（`rc` ⛔ 取自管線之後）／`GB-147`（哨兵自落地）／`GB-148`（機檢器倉根繫 `cwd`）／`GB-132`（自指增長）|

🩸 **本窗之「量測器紅而非受詞紅」達<u>七次</u>**（壞錨含右括號／檔框≠D1／CRLF／靶未涵蓋受詞／單空格假設／期望值過期／`PANEL` 抓到註解）。
🔑 **通則**：**出艙任何「不符」之前，先問「我和它是不是在量同一側」。**

---

## §五　已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 鏈 | 受詞 | 現況 |
|---|---|---|
| `GB-67` | 退縮 `0` 之 `②-宗 圍堵閘`（**`app.py`**·生產碼）| **未解**：`R2` 宗-宗重疊 `45.9766 > 上界 2.8906` |
| telescoping | 退縮 `3.5` 之 `ΣRw`（**`verify/stepg_pipeline.py`**·**harness 專屬**）| **未解**：`R2 left ΣRw_實跑 100.02 ≠ 44.41` |

🔒 **二者係二事**（中止點相異）；**皆使 Step G 於 `R2` 中止** ⇒ `R4`／`R5`／`R6` **一筆不產生**。
🟢 `W-G.9-247` 已使中止時之逐宗表可得（`got_G值_退縮{tag}_partial.csv`）。

---

## §六　待辦／待裁

### 🔴 **最優先：`W-G.9-251` 工項一之四案裁**（停機中）

**受詞**：`K-9-1` 面板（`🏛️ 街廓最小建築面積` `expander` `[20143,20184]`）之遷移。
**相斥之證**（`app.py@c524cb8`·AST／逐符號賦值列實測）：

- **步驟 E ＝ `[19595, 19800)`**；**面板 `:20143` ⛔ 在其內**——其在**步驟 L**（`:20010`）內。
- 面板之**全部前置**（`_build_blocks :20092`／`_depth_use_by_blk :20094`／`_min_alloc_area_by_blk :20126`／
  `_k91_cats :20138`／`_k91_by_cat :20140`／`_k91_by_lbl :20141`）**與消費端**
  （`_k91_contrib :20198`／`_region_min :20200`／`f3_70a :20205`）
  **皆在 `⚙️ 街角地參數` `[20046, 20393]` 內**。
- ⇒ 移至步驟 E（`<19800`）⇒ **`NameError`**；移至 `>20393` ⇒ **晚於消費端** ⇒ **二路皆違 `I-2`**。

| 案 | 判 |
|---|---|
| **甲**（單之字面·移至步驟 E）| 🔴 **⛔ 可行**（須把全部前置前移約 `500` 列跨四步驟 ⇒ 違 `I-2` 明文）|
| **乙**（把 `[20046,20185]` 移出 expander）| 🔴 **障礙 `9` 列**——其間夾雜「街角地退縮距離」（`:20071`–`:20076`）與「街廓分配深度覆寫」（`:20107`／`:20116`–`:20117`）之**真受詞** |
| **丙**（面板 `expander` 改直接渲染）| 🟡 **部分解**——巢狀 `2→1`、`I-1`〜`I-4` 全保，惟**外層 `⚙️ 街角地參數` 仍在** |
| **丁**（CC 依 `常規三` 提案）| 🟢 把 `[20138, 20221]`（面板＋寫回＋消費端）**整組**移至 `⚙️ 街角地參數` **之後**、仍在步驟 L。**`with` 非函式作用域** ⇒ 前置⛔ 須移、消費端隨面板同移 ⇒ `I-2` 保全 |

🛑 **丁案之界限（照實）**：① `:20361`–`:20375` 之 `_corner_rows_init`／`_df_corner`／
`f3_burden_range_18m` 是否依賴 `f3_70a` 或 `_k91_eff`，**本窗未查**；② ⛔ 單之字面 ⇒ 須核可。

### 🟢 `W-G.9-251` 工項二／三（**⛔ 受該相斥影響·可辦**）

| 工項 | 受詞之位置 |
|---|---|
| **二**（路名管理）| `:19601` DXF 按鈕**下方**、`:19617` `f3_road_form` **上方** ⇒ **步驟 E 內、form 外**（即時生效）|
| **三**（下拉 ＋ `RD` 自動偵測）| 街廓卡片內（`-248` 之 `text_input` 現址 `:19670`）|

🛑 **惟單 `§六` 令段乙為一整體** ⇒ **是否另成 commit 先行實作，候發單側裁**（⛔ CC 逕為）。
🔒 **工項三之資料源已現查**：`f3_cad_front_lines`（`17` 列）／`f3_manual_road_centerlines`（`13` 列）／
`_is_road_like_category`（**定義在 `:7303`**·判準 ＝ `('道' in cat) or ('路' in cat) or ('溝' in cat) or ('渠' in cat)`·
首個使用 `:1359`）／`_aliases` 之 `'RD':     '道路',` ＠ `:2508`。
🔒 **門檻 `8.0 m` 係 KL 逐字**（重劃新配道路寬度實務約 `8 m`）·⛔ 改、⛔ 另立第二門檻。

### 🛑 待裁清單（**⛔ 由 CC 自裁**）

| # | 受詞 |
|---|---|
| `1` | 上開 `-251` 工項一之四案（**最優先**）|
| `2` | 工項二／三是否另成 commit 先行實作 |
| `3` | `-251` 單之**工項編號自相斥**（`§前言`「段甲（工項零／**五**）」vs `§五` 標題「工項**四**」）——CC 已依 `常規二` 取保守項（以節標題為準）並續辦 |
| `4` | 承前波未決：`GB-67` 與 telescoping 之排程；`min_width` 三源（`GB-151`）之處置時點 |

### ⬜ 次一單

`-249` ＝ **`K-6` 段三**（發單側出單中）；`-250` ＝ **接閘單**（`K-9-23` 二閘之接線）。

---

## §七　本窗（`-246R2` 交接 → `-251R`）之摘要

| 批 | 要旨 | 落點 |
|---|---|---|
| `-246R3` | KL 二裁落地（`K-9-23` 閘一之受詞 ＝ **受檢宗自身**之 `G`·`K-6:3261` 正確／`v3:893` 誤 ⇒ `v3` 檔尾落更正標記）＋ 鑄 `自誤 308`／`309` ＋ **`-246-c1` 併線**（rebase → `29ebc14`）| `cc9cbff`／`f73ce5e` |
| `-248` 段甲 | 工項零入倉 ＋ 鑄 `自誤 310`（`V-B` 未就 UI 落點對拍單之意圖）| `dde8ba8` |
| `-248` 段乙 | 🛑 **工項一停機**（`I-2`·`st.form` 致延遲·**活體三步實驗**坐實）→ KL 裁**丙案** → 落地 `c1`（工項二＋三）／`c2`（工項一唯讀顯示）→ **末態驗收全綠** → 鑄 `自誤 311` → **KL 放行併線**（rebase → `2709504`）| `6010a80`／`e4b35cf`／`d8774e3`／`2709504`／`1e5a87c` |
| `-251` 段甲 | 工項零入倉 ＋ 鑄 `自誤 312`（`V-K` 之驗收條目未涵蓋**可達性**）| `d8b80e9` |
| `-251` 段乙 | 🛑 **工項一停機**（意圖與 `I-2` 相斥·四案候裁）| `c524cb8` |

### 🔑 碼面要害（**下一波必知**）

- **`-248` 落地之三物**：① `SS_FRONT_ROAD_NAME = 'f3_front_road_name_by_bid'`（module 級·
  正面道路名稱之新鍵·**⛔ 消費端**）② `wg9248_stringify_mixed_cols(df, cols)`（module 級·
  混型欄之顯示層修·**呼叫點 `2`**·受詞欄不存在 ⇒ loud `KeyError`）③ `K-9-1` **唯讀顯示**
  （介面甲每街廓 `expander` 之正面道路區塊下方·`:19713` 一帶·**當場現算** `k91_effective_min_build_area`）。
- **混型欄之修法**：DF-1／DF-2 採 `.astype(str)`（保 `'—'` 之語意）；
  **DF-3 採 `None` ＋ 既有 `.style.format(..., na_rep="-")`**（語意由該機制保留）。
- **`k91_effective_min_build_area` 為<u>四參</u>**（`label, category, by_label, by_category`）。
- **`K-9-1` 入參之二層取得**：優先 **widget key**（`f3L_mba_ov_{label}`／`f3L_mba_cat_{cat}`·
  form **外** ⇒ 本輪值·無延遲）→ 回落**持久 dict** → `0.0`（無規定）。
- **`_lot_gate`（`app.py` module 級）** ＝ 逐宗驗證站之判定式單一真相源（**觀測模式**·
  `-246R2` 落地·函式體現於 `:12330`–`:12511` 一帶）。

---

## §八　開窗前四件事（**依序·⛔ 跳**）

1. **驗 `left-right`**：`git ls-remote origin wip/s1-endpart` ＋
   `git rev-list --left-right --count HEAD...origin/wip/s1-endpart`。
   **非 `0 0` 即工作區為舊態** ⇒ ⛔ 直接 `cat` 工作區之 `CLAUDE.md`／`app.py`；一律 `git show <rev>:<path>`。
   **harness 逐窗自 `main` 開 worktree ＝ 已多度重踩。**
2. **驗硬閘是否生效**：`test -f .claude/settings.json` ＋ `test -f verify/tools/wg9237_heredoc_guard.py`。
   🟢 **本窗二檔皆在且實測攔下一次**——惟「閘已落地 ⇏ 閘於本 session 生效」，
   **須<u>活體實測</u>**（送一個含 `<<` 之無害命令，看是否被 deny）。
3. **讀本文 `§一`／`§二` 並<u>重跑</u>其每一數**（⛔ 採本文之值）。
4. **查 `-251` 工項一之裁是否已下**：讀 `docs/orders/` 是否有新單；
   `git ls-remote --heads origin 'refs/heads/verify/*'` 確認**無**未併之側分支。

🔒 **收單後首二步之順序**：① 查宣告框占用（三數分列）→ ② 原封入倉。**⛔ 顛倒。**
🔑 **單之所在**：本窗**二度**發現單在**主 checkout 之工作區**（`C:\Users\admin\Desktop\land-readjustment-trial\docs\orders\`）
而**⛔ 在倉** ⇒ **尋單之母體須含該處**。
🔒 **長跑一律於拋棄式 clone**（自 origin·`core.autocrlf=false`·**短路徑**·**目錄名同長度**）
——`run_all` 會改寫已追蹤 log；跑畢整份刪除（`GB-94`）。
