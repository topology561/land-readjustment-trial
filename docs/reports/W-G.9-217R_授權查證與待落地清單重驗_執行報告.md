# `W-G.9-217R`　執行報告——`Commit 2` 授權查證 ＋ 待落地清單整份重驗 ＋ 過期前提之登記

> 施工單 `W-G.9-217R`（**聊天交付·⛔ 入倉為檔**；`-213`〜`-217` 亦同）。
> 承接：`W-G.9-217` **全單作廢**（其前提「`commit 1` 未落地／`commit 2` 待授權」已由 CC 機械否證）；
> 其 `工項二`（預登記）**永久取消**。
> 基座 `wip/s1-endpart` ＝ `bc2f8bbbb159f0c43545a8826340077f39cd3cf6`。**零生產碼。**

🛑 **本批之處置**
`工項一` ✅ 結案（授權紀錄存在）／`工項三`＋`工項四` ✅ 已辦並落地／
🔴 **`工項二` 停於停機款 ⑤**，以 KL 格式上呈（見 `§三`），⛔ 自行判斷。

🔒 **三軸**（`VR-091 一`）`(1)` `blob@bc2f8bbbb159f0c43545a8826340077f39cd3cf6`（落地前）／工作區（落地後）
`(2)` 母體逐表載明 `(3)` 粒度框**逐數**載明

---

## §一　工項一　`Commit 2` 授權紀錄之查證 ⇒ **存在**

### `a`　既有登記體例（母體 ＝ `docs/` 倉側全檔 **`505`** 檔·**列框**）

判準「同列含 `KL` ∧ 含（放行∨授權∨准推∨核可∨已放行∨`准 `）」⇒ 命中 **`186`**【列】／涉 **`96`**【檔】。
其**最強形**（同列另含「逐字」）⇒ 命中 **`25`**【列】，其中三則形式完全一致：

| commit | 檔列 | 逐字 |
|---|---|---|
| `1` | `docs/orders/W-G.9-207_施工單…:7`／`:9` | `🔒 **KL 已放行（\`2026-09-02\`·逐字）**` ／ `> 「是 放行 \`commit 1\` 進主線」` |
| **`2`** | **`docs/orders/W-G.9-209_施工單…:8`／`:10`** | `🔒 **KL 已放行（\`2026-09-02\`·逐字）**` ／ **`> 「是 放行 \`commit 2\` 進主線」`** |
| `3` | `docs/orders/W-G.9-211_施工單…:8`／`:10` | 同形 ／ `> 「是 放行 \`commit 3\` 進主線」` |

⇒ **體例存在且已成定式**；其正字亦見 `docs/orders/W-G.9-212_發單側交接文.md:114`
逐字「**KL 之逐字須原封抄入單與正典**，⛔ 轉述。「continue」「好」之類⛔ 得記作放行。」

### `b`　`commit 2`（`a1107a17`）之授權紀錄 ⇒ **存在**

判準「同列含（`a1107a17` ∨ `commit 2`）∧（放行∨授權∨准推∨`KL`）」⇒ 命中 **`28`**【列】；
其**決定性者** ＝ `docs/orders/W-G.9-209_施工單_commit2放行與GB140更正註與GB141與自誤262.md:8`／`:10`（上表）。

`a1107a17` 之物件屬性：`author=diag <diag@local>`／`author-date=2026-09-02 16:42:59 +0800`／
`committer-date=2026-09-02 17:25:26 +0800`／`parent=be1b326a9526c704a37a495474b04aa0694cb1e0`。

### `c`　`W-G.9-198R` 原單之條件字面 ＋ 其解除

原單逐字（`docs/orders/W-G.9-198R_施工單…:226`）：
`🔒 **放行之粒度**：發單側將**逐 commit** 請示 KL，⛔ 一次放行三個。\`commit 1\` 放行後方進 \`commit 2\`。`

解除鏈之命中（判準「同列含該 commit 之名或物件 ∧（放行∨准推）」·**列框**）：
`commit 1` **`24`**【列】／`commit 2` **`18`**【列】／`commit 3` **`12`**【列】——三者皆完整。

⇒ 依單之分支：**本工項結案**；「發單側交接態過期」之登記入 `工項三`。

---

## §二　工項三　過期前提之登記（鑄 `自誤 272`）

**鑄號推導式**（三軸：`(1)` `blob@bc2f8bb`／`(2)` 自誤簿單檔 `495625` B／`5307` 列／`(3)` 逐數載框）
⇒ 相異號數 **`264`**【號】／單筆列命中 **`257`**【列】／範圍 **`8`**【條】／**MAX `271`**
⇒ **`N ＝ MAX ＋ 1 ＝ 272`**；`272`／`273` 之標題形命中皆 **`0`**。
🔒 `VR-091` 補款 `五` 之恆定期望值：**缺號清單 ＝ `[106]`** ✅

`自誤 272` 之要旨（全文見自誤簿）：

- **受詞**：`W-G.9-217` 以未經 `grep` 之**交接態**為單之**前提**。
- **實測否證**：三 commit 皆在主線；六符號於生產碼之命中逐一列表（`指數名次` `app.py` **`10`** 等）；
  對照組 `k6b_stage9_nosuch` ＝ **`0`**；`commit 2` 之 KL 授權紀錄檔列具名。
- **後果分二層**：`(1)` 對 CC ＝ 一單作廢、一輪空跑（**⛔ 落地污染**——閘擋住）；
  `(2)` 對 KL ＝ **同一過期宣稱經連續五批傳播**，耗其注意於已了結之事。
  🔴 **`(2)` 之危害重於 `(1)`**——`(1)` 可由閘攔下，`(2)` **⛔ 有任何閘可攔**（KL 所見者為敘述、非倉態）。
- **族之具名**（依 `自誤 257`）：**與 `自誤 267` 同族之<u>升級形</u>**——受詞由「閘之一個數」擴及
  「單之整組前提」，傳播對象由 CC 擴及 KL；四維度對照表載於該則。⛔ 為 `267` 之重複。
- **通則三款**：① 否定宣稱須同單當場以 `grep` ＋ `merge-base --is-ancestor` 復現；
  ② **向 KL 之待落地清單每次出艙前須整份重驗**，⛔ 沿用前批；部分落地者須具名已落與未落之部分；
  ③ `自誤 267` 之通則射程擴及「單之前提」。

---

## §三　🔴 工項二　停於停機款 ⑤ —— 以 KL 格式上呈

### 停機之依據（機械·逐字）

單之停機款 ⑤ 逐字：「`e` 之二值成因**非資料驅動**（即由**硬寫常數**或**臨場參數**決定）」。

| 側 | 逐字 | 判 |
|---|---|---|
| harness | `verify/run_verification.py:520`／`:592`　`for setback, tag in ((0.0, "0m"), (3.5, "3.5m")):` | **硬寫二元組** |
| harness（檔頭） | `:3`　`W-V 驗證熔爐 — headless 重現 app.py 街角選位管線，**雙情境（退縮 0m/3.5m）**，` | 同上 |
| `app.py` | `:12677`　`_setback_raw = _ss_B4.get('f3L_setback_default')` | **臨場參數**（UI 輸入欄·`:19689` `key='f3L_setback_default'`） |

⇒ **二者皆非自 `data/V6_1.dxf` 或任何資料表導出** ⇒ 停機款 ⑤ **按字面成立** ⇒ **停·⛔ 自行判斷**。

🔒 **併記（同一參數之內部張力·CC 實測）**
`app.py:12680` 逐字硬停：`f"🔴 f3L_setback_default 未設（街廓 {_this_blk_B4}）·補丁六 §四 no-silent-fallback：**禁靜默 3.5 兜底**"`
——而 `:19684`／`:22359`／`:22848` **三處**逐字以 `float(st.session_state.get('f3L_setback_default', **3.5**))` 取值。
`CLAUDE.md:427` 逐字：「**退縮 / 法定最小寬是案件/街廓級參數，不是系統常數**——fallback 不可用案件值（如 3.5m）兜底」。
🛑 **此項本身即一獨立之待裁項**，惟其非本單之停機款，**⛔ 由 CC 鑄號**、⛔ 逕改。

---

### 📮 上呈 KL（🔵 **知會即可** ／ 惟需一句回答）

**【現況】**
系統在驗算街角地時，會把「臨路面要不要往內退一段（退縮）」這個數字**同時用兩個值各算一遍**：
一個是 **不退縮（`0` 公尺）**，一個是 **退縮 `3.5` 公尺**。
這兩個值不是從地籍圖或任何資料算出來的，而是**寫死在驗算程式裡的一組對照情境**。
畫面（UI）那邊則是**由使用者在欄位裡自己填**。

**【要改成】**
本案（花蓮這個重劃區）的退縮值只會有**一個**。請指定它是多少公尺，
系統即以該值為**本案之正式值**，另一個值降為**參考情境**。

**【對土地的影響（帶數字）】**
`R4` 街廓左側街角，因 `628(1)` 與 `628-1(1)` 合併後歸到右側，左側成為**強制抵費地**。
其面積**隨退縮值而不同**（🛑 以下二數係**倉內既載**·`docs/orders/W-G.9-209_施工單…:70`／
`docs/orders/W-G.9-212_發單側交接文.md:62`／`docs/reports/W-G.9-209R…:115`，本批**⛔ 當場實算**）：

| 退縮 | 該筆強制抵費地面積 |
|---|---|
| `3.5` 公尺 | **`226.01` ㎡** |
| `0` 公尺 | **`116.08` ㎡** |

**差 `109.93` ㎡**——這是同一塊地在兩個情境下的差，⛔ 是誤差。

**【要你判斷（單一問題）】**
**本案的退縮值是幾公尺？**

---

## §四　工項四　待落地清單之**整份重驗**

🔒 **全表已隨 `自誤 272` 落於自誤簿**（`§二`），本節僅摘其核心發現。
三軸：`(1)` `blob@bc2f8bb`／`(2)` 生產碼五檔倉側 blob／`(3)` **字元框**（另註列框者從其註）。

| # | 項 | 判 |
|---|---|---|
| `A1`–`A3` | `W-G.9-198R` `commit 1`／`2`／`3` | **已落地**（三者皆在主線·六符號逐一具名） |
| `1` | `K-9-23` 二閘 ＋ `K-9-16` 藍影 ＋ `K-9-17` 遞補 | 🔶 **部分落地** |
| `2` | `K-9-12` 矩形容納 | 🔶 **部分落地** |
| `3` | `K-6-A2` 剩餘 | 🔶 **部分落地** |
| `4` | `K-9-22` | **未落地**（清單相符） |
| `5` | `K-6-B` 段一〜段四 | 🔶 **部分落地** |
| `6` | `K-6 §四` → 波末批 → 泛化波 | **未落地**（清單相符） |
| `併` | 九份 baseline 重產 | **未落地**（須 KL 決時點） |
| 🆕 `7` | `K-9-27`／`K-9-28`（碼面） | **未落地**（四符號命中皆 `0`） |
| 🆕 `8` | 通用化波（去 `UC9898`） | **未落地**（`UC9898` `app.py` `26` 等） |

### 🔴 核心發現

**主鏈六項中，`1`／`2`／`3`／`5` <u>四項</u>係「部分落地」而 `CLAUDE.md` 清單載為「⬜ 未開工」。**

- `1`：`_blue_shadow_tri`（`app.py:12054`）＋ `K-9-23` 骨架（`:11948`–`:12228`）**已在碼中**，
  惟 `:11963` 逐字「本區塊之一切輸出**只寫入診斷**」／`:12234` 逐字「**只印**，⛔ 無回傳值被消費」
  ⇒ **觀測模式**；`K-9-17` 遞補則 `:11966` 逐字「**本批⛔ 不辦**」。
- `2`：`K-9-12` `14` 次／`rect_fit` `16` 次，`:11755` 逐字「⛔ 純函式·**觀測模式**」⇒ 未接生產。
- `3`：🔴 **清單自身與其更正節互斥**——`CLAUDE.md:564` 已逐字裁「`K-6-A2` ＝ 🔶 **部分已落地**，
  ⛔ 非『未開工』」，而 `:592`／`:1100` 仍載「⬜ 未開工」。
- `5`：步驟 0／段一／段二 **已落**；**段三**（`VR-086`）與**段四**（`重排位次`／`k6b_stage4`／`k6_stage4`
  三符號命中皆 `0`；`段四` 之 `20` 列**全為 `K-6-A2 段四` 之他義**）**未落**。

⇒ **清單之粒度不足以承載「觀測模式」與「部分接線」之區別**，遂使向 KL 之陳述系統性偏誤。

### 「五則『典入而碼未動』警示」之查證 ⇒ **六框並查、無一得 `5`**

| 框 | 命中（**列框**） |
|---|---|
| `碼面未依` ／ `已入正典`∧`碼` ／ `正典`∧(`未實作`∨`未落地`∨`碼未`) ／ `碼與裁定`∨`碼與正典` | `0`／`0`／`0`／`0` |
| **`裁定`∧(`未落地`∨`未實作`)** | **`4`**（`CLAUDE.md:420`／`:432`／`:439`／`:459`） |
| `⛔ 適用`∨`經取代` | `4`（`:1245`／`:1246`／`:1416`／`:1426`） |

⇒ 最接近者為「已裁定、`K-6-A2` 尚未實作」型之 **`4`** 列，⛔ `5`。依 `VR-091 一 (3)` 須同格載框。

### 🔒 併記：`⬜` 之**自指增長**

`CLAUDE.md` 之 `⬜` **列命中** `blob@06a054a` **`37`** → 現態 **`38`**（字元框 `39` → `40`）。
其增列**唯一** ＝ `CLAUDE.md:1321`（`W-G.9-214` 補款之案由行，逐字引用「全檔 `⬜` 命中 `37` 處」）
⇒ **引用即自增**，族同 `自誤 264`。
🛑 凡以「某字樣之全檔命中數」為待落地清單之量者，**須自扣本批引用所增之量**。

---

## §五　落地與驗收

### `T-1`　append-only 之位元組級自證（受詞取**倉側** blob·`戒 40` 二端並報）

| 檔 | 工作樹 before → after（Δ） | **倉側 blob** 基座 → 現（Δ） | 行尾分流 | 嚴格前綴 | 哨兵 | `CR` |
|---|---|---|---|---|---|---|
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `500604` → `510905`（`+10301`） | `495625` → `505926`（`+10301`＝`10300`＋`1`） | 非均質（`CR 4979`／`LF 5307`）⇒ 原樣 LF | ✅ | `1` | `0` |

payload `sha256` ＝ `4111f3c356dfef439b901e2f13377f0a0ec2fc13084ee9a42a3e7efbff0ec347`（`10300` B）；
落地後 blob ＝ **`78e3217ec263bb0c53b95d4587e3dfe13839c10e`**（基座 `075fc217e96b93b370d67509afd53ebd10351c01`）。
🔒 落地器 `g217r_land.py` 依 `GB-143 處置一` 以**檔案落地**產生、⛔ 經 heredoc。

### `T-2`　取號自證（**二框並報**）

自誤：相異 `264` → **`265`**／單筆列命中 `257` → **`258`**／MAX `271` → **`272`**；新增之號恰 `{272}`。
範圍條數 `8` → `8`；🔴 **`VR-091` 補款 `五`：缺號清單恆為 `[106]`**。
`GB`／`VR`／`K-9`／`戒` 本批**⛔ 鑄**（其登記表於本 commit 命中 `0`）。

### `T-3`　🔒 閘 P（生產碼五檔之內容 `sha256`·作業**前後**逐檔相同）

| 檔 | blob | bytes | `sha256`（前 ＝ 後） |
|---|---|---|---|
| `app.py` | `a6a6ffaf…` | `1414673` | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` |
| `verify/selection_pipeline.py` | `167a7beb…` | `35367` | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | `d07955c5…` | `96923` | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | `fef5865a…` | `96261` | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | `e5cf9a22…` | `24681` | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

### `T-4`　母體

🛑 依 `自誤 251` 之戒，實測值繫於提交後之具名量測態，載於推後復驗（聊天回報），⛔ 於本檔內自宣。
本批**新增恰一檔**（本報告）、另修既有一檔（自誤簿）；`deletions` 總計 ＝ `0`。
🔒 `CLAUDE.md`／`docs/驗證裁定登記表.md`／`docs/reports/W-G.4_泛用阻塞項登記表.md` 本批**⛔ 觸**。

---

## §六　🛑 上呈與其後

1. 🔵 **`§三` 之 KL 上呈**（退縮值）——**知會即可·惟需一句回答**。
2. 🟡 **`app.py` 之退縮預設值內部張力**（`:12680` 硬停禁 `3.5` 兜底 vs `:19684`／`:22359`／`:22848`
   以 `3.5` 為 `.get` 預設）——⛔ 由 CC 鑄號、⛔ 逕改；宜於下一單具名處置。
3. 🟡 **`CLAUDE.md` 主鏈清單之粒度**——四項「部分落地」載為「⬜ 未開工」（`§四`）；
   其更正須發單側之單（本批已將全表登記於 `自誤 272`，⛔ 改 `CLAUDE.md` 一字）。
4. 🔵 `工項二` 之 `a`–`d`／`f` 於 ⑤ 解決後**仍可執行且有價值**（落地後之全精度實算復驗）；
   屆時須改稱**落地後復驗**、⛔ 稱前置證據（`W-G.9-217` 之作廢事由）。

---

## §七　次號（本批落地後）

自誤 **`273`**／`GB` **`144`**／`VR` **`092`**（三位補零）／戒 **`41`**／`K-9` **`29`**。
