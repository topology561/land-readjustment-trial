# 🔧 `W-G.9-149R`　`K-9-19` 全文交付 ＋ 四份 `_proj_order` 對拍 ＋ 號之更正

**基座** ＝ `f71c1474722758006ce7a14bcb3e188cbabba41b`　**`app.py` blob** ＝ `b4cc30835feaad03dfc37f394a669bbb45988b8b`（期末逐位相同·⛔ 零觸）
**性質**：🔒 只讀交付 ＋ 純附加登記·⛔ 零生產碼·⛔ 未改任何既有文字
**量測時點**：`2026-08-27`·情境 `0m`／`WV_K6_STEP0=on`·**harness 態**
🔒 **本報告之四反引號區塊恰 `4` 個**（塊 `J`／`K1`／`K2`／`L`）。塊 `M` 係標題行清單、⛔ 未用圍籬。

> 🔴 **三句話結論**
> ① **`R-7` 得<u>第三態</u>**：正典 `K-6:3294` 之 `R1右 → 628-36(1)` 於**含／去 ghost 二態下皆重現** ⇒ 該格對本命題 **⛔ 無鑑別力** ⇒ 八格表 **⛔ 未編碼讀法甲**，亦 **⛔ 不與 `148R` 互斥**（停機閘所防之危害**已由實測排除**）。單之三分支**未涵蓋此態**。
> ② **`R-5` 不符**：`wf_f0._proj_order` 與其餘三份**實質分歧**（傳完整 `tp` vs 只傳二鍵）；**惟 `N-2` 實測五份 `6/6` 逐位相同**（`_projection_order` 只讀 `polygon_coords`）⇒ **分歧存在但⛔ 不影響排序**。
> ③ **`R-3` 之受詞**：`K-9-19` 之 **KL 逐字段（blockquote）⛔ 全文未提 `K-9-4`**；`K-9-4` 之限縮**僅出現於 `一`／`三`**，而該二節之性質（KL 逐字 vs claude.ai 整理）**倉內未具名** ⇒ **CC ⛔ 不自裁**。

---

## §零　`V-0`　取號現查（三事齊）

| 形（框逐字） | 命中 |
|---|---|
| `^###.*W-G\.9-149`（標題式） | **`0`** |
| `` `W-G.9-149` ``（反引號式） | 🔴 **`2`** |
| `W-G_9-149`（檔名式·內容） | **`0`** |
| 檔名含 `149`（`git ls-tree -r --name-only HEAD`） | **`0`** |
| 裸 `W-G\.9-149` | 🔴 **`2`** |
| **對照組** `W-G\.9-148` | **`7` 檔** ⇒ 量測器非紅 |

🔒 **二命中之落點逐一具名**：
- `docs/reports/W-G.9-148R_ghost母體量測.md:104`
- `docs/reports/W-G.9-148R_ghost母體量測.md:214`（逐字「依單 `§一 M-5` 逐字：`W-G.9-149` **須先建 `BASE`**」）

⇒ 🔒 **二者皆在 CC 前批之報告內 ＝ 前瞻引用**（`W-G.9-148 §零 V-0` 已立此判別：「前瞻引用·⛔ 非實體占用」）；**實體**（標題式／檔名式／檔名）**皆 `0`** ⇒ 取號安全、續辦。
⚠️ **`R-1` 之字面**（「實體命中 ＝ `0`」）**符**；惟**裸框非 `0`** ⇒ 逐字回報。

---

## §一　`K-9-19` 全文之交付（`D-1`／`D-2`）

**出處檔 blob（全 `40` 碼）** ＝ `cfb55ad6a738d58d260e1c2a805779228c6e4492`
（`docs/rulings/K-6_街角地分配程序與可分配判準.md` @ `f71c147`）

### 塊 `M`　起迄之界定（證 `J` 未截斷、未溢出）

| 行號 | `^###` 標題逐字 |
|---|---|
| `3067` | `### 🔒 K-9-20　**圖資之權威版本 ＝ KL 最後一次依裁定修改後所交付者；生產與驗證須讀同一份**（KL 裁 \`2026-08-24\`·\`W-G.9-123\`）` |
| **`3097`** | `### 🔒 K-9-19　**無地主之未覆蓋殘餘⛔ 非「宗」；有地主而無法配地者走現金補償**（KL 裁 \`2026-08-24\`·\`W-G.9-128\` 補登）` |
| `3123` | `### 🔧 \`K-9-20\` 之補記（\`W-G.9-128\`·⛔ 上文原句一字不刪）` |

⇒ 🔒 **塊 `J` ＝ `K-6:3097-3122`**（自其 `^###` 起至下一 `^###` 前一行止）。

### 塊 `J`　`K-9-19` 全文

**出處** `K-6:3097-3122`　**三數**：列數 **`26`**／位元組 **`2498`**／`sha256` ＝ `2ce30afdc93df3561ac6001826adfcbdadfa1110e69562630d57b2fa5c54b29d`

````
### 🔒 K-9-19　**無地主之未覆蓋殘餘⛔ 非「宗」；有地主而無法配地者走現金補償**（KL 裁 `2026-08-24`·`W-G.9-128` 補登）

> **KL 逐字**：「`R4` 街廓內的碎片是重劃範圍邊界的細碎塊? **如果是就不算一宗土地**。但如果是有地主的地，那就是重劃前土地面積很小，重劃後扣除負擔面積更小(`G=0`)，但是**若這筆碎塊土地是有地主的，無法分配土地就是要給補償**」

🔒 **一（判準綁性質·⛔ 不綁名稱）**：`K-9-4`（每宗須配到 `BASELINE`）之「**宗**」，**⛔ 不含「無地主之未覆蓋殘餘」**。
其判準 ＝ **三個「該筆自身之屬性」之合取**，三者須**同時**成立：
`①` `原地號 == '_GHOST'`（依構造係 `residue.difference(全部宗地)` 之殘餘 ⇒ ⛔ 無地主）
`②` `G(㎡) == 0`（其上⛔ 未分配任何土地）
`③` `a 面積(㎡) == 0`（**重劃前⛔ 無面積 ⇒ ⛔ 無地主**）
🛑 **任一不成立 ⇒ 照舊嚴查**。⛔ **不得**綁 `_is_ghost_sliver`（該旗標於 `app.py:19367-19401` 新建 `g_row` 時**未傳遞**·綁之即恆假）、⛔ **不得**綁 `重劃前區段`（係 `_build_g_row` 之**參數 `_zone`**·由上游填入之**街廓層級**值·`自誤 149`）、⛔ **不得**以 `cut_coords is None` 反面篩（`app.py:6229` 之禁令）。

🔒 **二（有地主者之出路·⛔ 不落空）**：有地主而無法配地者，其 `推進側別` 應為 `'💰 現金補償'`，
而該側別**已在 `K94_NO_LAND_SIDES` 內** ⇒ **本就⛔ 不受 `K-9-4` 拘束** ⇒ ⛔ 不走本裁之排除。

🔒 **三（立法目的之對齊）**：`K-9-4` 之目的逐字為「其上**分配之土地**無法成為合法建築基地」——
而 `G == 0` 者其上**⛔ 無分配之土地** ⇒ **該目的於其⛔ 不適用**。

🔒 **四（本案之實測·⛔ 非本裁之一部·僅供溯源）**：全部 ghost **`8` 筆**，`_ghost_reason` **全為「邊界帶」**、
`原地號` 全為 `'_GHOST'`、`重劃前地價區段` 全為空、**距重劃範圍邊界皆 `0.000000 m`**；
面積分別 `8.09`／`5.28`／`10.78`／`1.34`／`1.33`／`1.07`／`0.75`／`0.45 ㎡`。
`_GHOST_(R4)` 之幾何 ＝ 三點退化多邊形（兩兩距離 `0.3890`／`0.0195`／`0.3894 m`·**面積 `0.003790 ㎡`**·**等效寬度 `0.019477 m`**）。

⛔ **本裁不辦**：⛔ 不授權刪除任何殘餘、⛔ 不變更 `K94_NO_LAND_SIDES`、⛔ 不放寬 `_EPS_TOUCH_FRONT`。

---

````

### 🛑 塊 `K` 之受詞**不成立**——`K-9-4` **⛔ 無自己的 `^###`**

單 `D-2` 令「自 `K-9-4` 之 `^###` 起至下一則前止」。**現查**：`grep -n "K-9-4" K-6` 之全部命中中，**⛔ 無任何 `^###.*K-9-4`（不含子號）**；`K-9-4` 之**本體** ＝ `K-6:375` 之**一列表列**，隸屬 `### K-9 §一、五則裁定`（`:368`）。惟有 **`K-9-4-1`** 具 `^###`（`:493`）。
⇒ 🔒 **CC 依常規二取保守項**：交付 **`K1`（`K-9 §一、五則裁定` 全節·含 `K-9-4` 本體）** ＋ **`K2`（`K-9-4-1` 全節）**，並逐字回報該受詞不成立。⛔ 未自行調整、⛔ 未截斷。

### 塊 `K1`　`K-9 §一、五則裁定`（含 `K-9-4` 本體 @ `:375`）

**出處** `K-6:368-384`　**三數**：列數 **`17`**／位元組 **`1598`**／`sha256` ＝ `b594ccd81630367d35eff6794645b091601a76d80b7c9bd4890db8dd142e41bd`

````
### K-9 §一、五則裁定

> 1. 街角地作為街廓內第一宗分配地，不能豁免街廓內最小建築面積之限制。
> 2. 深度部分是要街廓層次的檢查，不是宗地層。
> 3. 街廓層次深度檢查通過，宗地就不用再檢查深度。
> 4. 每一宗土地都必須分配到底（BASELINE），不支援同一街廓前後配兩排。
> 5. 街角地第一宗的寬度要實量（截角前），門檻＝退縮寬＋畸零地最小寬；其餘各宗＝畸零地最小寬。

| 則 | 內容 |
|---|---|
| **K-9-1** | **街角地第 1 宗⛔ 不豁免**街廓最小建築面積 |
| **K-9-2** | 深度改**街廓層**檢查（前緣線上每點沿 BASELINE 法向至 BASELINE 之垂距，**任一點 ≤ 畸零地最小深度即警示**·**等於也警示**） |
| **K-9-3** | 街廓層過 ⇒ **宗地層一律不量深度**（⚠️ 射程已由 **K-9-3-1** 更正為「量、但不比附表深度」） |
| **K-9-4** | **每宗必須配到 BASELINE**；未達即**停機報錯**。**不支援同一街廓前後配兩排**（補充四則見 **K-9-4-1**：`grep -n "^### 🔒 K-9-4-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`） |
| **K-9-5** | 街角第 1 宗**寬度要實量**（**截角前**幾何），門檻＝**退縮寬＋畸零地最小寬**；其餘各宗＝**畸零地最小寬** |

````

### 塊 `K2`　`K-9-4-1`

**出處** `K-6:493-507`　**三數**：列數 **`15`**／位元組 **`874`**／`sha256` ＝ `886ab83052d47674aca32f0d7af19befdeca428d60e3d1b1b11fd5d2fc1b4933`

````
### 🔒 K-9-4-1 BASELINE 之來源、兩排之作法、與共用（KL 裁 2026-08-03·canonical·**逐字**）

> ① BASELINE 都是**人工繪製**，並非自動生成。
> ② 「不支援同一街廓前後配兩排」係指**同一街廓內不得有前後兩排宗地**；若圖面確需兩排，
>    應**在中間切一條 BASELINE**（＝把街廓切成兩個街廓），⛔ 不是在同一街廓內配兩排。
> ③ 若某宗**跨過** BASELINE，那是**圖畫錯了**——應改圖，⛔ 不是放寬容差。
> ④ **一條 BASELINE 可以同時服務兩個街廓**（屁股對屁股），此係常態、⛔ 非例外。

🔒 **本則之效力**：K-9-4 之「配到底」＝**配到該街廓自身之 BASELINE**（1:N 配對之正典見
`grep -n "配對機制：\*\*不動\*\*" docs/rulings/K-6_街角地分配程序與可分配判準.md`）。

````

### 塊 `L`　`K-9-17` KL 逐字段二（遞補與配餘地）

**出處** `K-6:2932-2938`　**三數**：列數 **`7`**／位元組 **`617`**／`sha256` ＝ `33257c2394e3fd3662dcf37219fb49d7f29586785df6c729532ff89aec47e689`

````
**二（遞補與配餘地）**

> 此時若1地號(第1宗土地，假設街角第為第0宗)若無法內接矩形成立而併入合併/調配機制，那麼該空位就由3遞補(空位位置係由第0宗遠側境界線開始依據遞補宗的G再決定第1宗遠側境界線，並注意宗與宗之間境界線不要交叉)，而1地號空出之面積當然依據街廓面積守恆式，就是列為配餘地(調配池)

🩸 **併記（發單側逐字）**：原文作「2地號空出之面積」，KL 於同日逐字更正
「**是筆誤，應為被踢除的1地號。**」⇒ 上段已採更正後之文字。

````

⇒ 🔒 **`R-4` 符**：遞補之受詞逐字為「**由 `3` 遞補**」＝**一個地號**；該段**⛔ 無任何允許非宗地物遞補之文字**。

### 🔴 `R-3` 之判：**CC ⛔ 不自裁·交發單側**

| 觀察 | 現查 |
|---|---|
| **KL 逐字段（blockquote）** | **⛔ 全文未提 `K-9-4`**——其語為無限定之「**如果是就不算一宗土地**」 |
| `K-9-4` 之限縮 | **僅出現於 `一`（「`K-9-4` … 之『宗』」）與 `三`（「`K-9-4` 之目的……**該目的於其⛔ 不適用**」）** |
| 該二節之性質 | 🔴 **倉內未具名**——`K-9-19` 之 `一`／`三` **⛔ 未標「非 KL 逐字·係 claude.ai 之整理」**（對比 `K-9-12`／`K-9-13`／`K-9-5-14` 皆有該標記），亦 **⛔ 未標「KL 逐字」** |

🛑 **⇒ `R-3` 不可由倉內逐字判定**：
- 若 `一`／`三` 係 **KL 逐字** ⇒ `K-9-4` 係**述詞之定義域** ⇒ **讀法甲⛔ 不成立**，本題須另裁（`R-3` **不成立**）。
- 若係 **claude.ai 整理** ⇒ KL 之裁為**通則**，`K-9-4` 僅係**適用場合** ⇒ **讀法甲成立**（`R-3` **成立**）。
🔒 **CC ⛔ 不選**（單 `§六` 逐字「⛔ 不就 ghost 一題作成任何處置（**裁由發單側落筆**）」）。

---

## §二　`N-1`／`N-2`　四份 `_proj_order` 之對拍

### `N-1`　逐字與逐位對拍 ⇒ 🔴 **`R-5` 不符**

| 份 | `檔:起-迄` | 列數 | 位元組 | `sha256`（前 32） |
|---|---|---|---|---|
| `1` | `verify/wf_f0.py:251-256` | `6` | `360` | `71e7ba6f3a6d1aa4d0e5b927a6ba41ed` |
| `2` | `verify/wf_f2.py:91-95` | `5` | `378` | `a2b28fb930dcc56103ef6e3c86caf695` |
| `3` | `verify/wf_f3.py:74-78` | `5` | `378` | `a2b28fb930dcc56103ef6e3c86caf695` |
| `4` | `verify/wf_f4.py:1039-1043` | `5` | `378` | `a2b28fb930dcc56103ef6e3c86caf695` |

**去前導縮排後**之 `sha256` **與上表逐份相同**（四份皆已在頂層縮排 ⇒ `dedent` 無作用）。

| 對 | 判 |
|---|---|
| `wf_f0` vs `wf_f2`／`wf_f3`／`wf_f4` | 🔴 **實質分歧** |
| `wf_f2` vs `wf_f3` vs `wf_f4` | ✅ **逐位相同** |

**`c` 逐行差異（`diff -u` `wf_f0` vs `wf_f2`）**：
```
 def _proj_order(ns, cad, parcels, blk):
-    """該街廓之投影序（暫編地號 list）。"""
     fl = (cad.get("front_lines") or {}).get(blk) or {}
-    pib = [tp for tp in parcels if tp["所屬街廓"] == blk
-           and not tp.get("_is_ghost_sliver")]
-    return [tp["暫編地號"] for tp in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
+    pib = [{"暫編地號": tp["暫編地號"], "polygon_coords": tp.get("polygon_coords")}
+           for tp in parcels if tp["所屬街廓"] == blk and not tp.get("_is_ghost_sliver")]
+    return [x["暫編地號"] for x in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
```

🔒 **是否影響排序結果 ⇒ ⛔ 否**——`d` 之現查：`app.py:7593-7619` 之 `_projection_order`，其排序鍵 `_proj_of(tp)` 逐字**只讀 `tp.get('polygon_coords')`**：
```
    def _proj_of(tp):
        cs = tp.get('polygon_coords') or []
        if len(cs) < 3 or line is None:
            return 0.0
        ...
    return sorted(parcels, key=_proj_of)
```
⇒ 二版**皆傳 `polygon_coords`**，且 `sorted` 為**穩定排序**（tie 依輸入序，二版輸入序相同）⇒ **排序等價**。
🔒 **`d`（第 `5` 份對拍）**：`app.py:7593` 之 `_projection_order` **係被呼叫者本身**、⛔ 非第 `5` 份複製——四份 `_proj_order` 皆為其 **wrapper**。

### `N-2`　🔴 排序判準之等價性**實測**（⛔ 未以「逐字相同」代替）

探針 `verify/probes/probe_WG9149_projorder_equiv.py`（`rc=0`／`stderr` `0` B）：

| 街廓 | `n` | 五份之輸出 |
|---|---|---|
| `R1` | `5` | **逐位相同** |
| `R2` | `14` | **逐位相同** |
| `R3` | `10` | **逐位相同** |
| `R4` | `2` | **逐位相同** |
| `R5` | `8` | **逐位相同** |
| `R6` | `8` | **逐位相同** |

🔒 **`6/6` 街廓 五份逐位相同** ⇒ **`R-6` 符**。
🔒 **判別力對照（戒 `12`）**：注入反轉比較子後 **轉紅之街廓 ＝ `6/6`**（須 `≥ 1`）⇒ 本對拍**有判別力**、⛔ 非恆綠。

⚠️ **粒度之具名**：單令「六街廓**左右各側**」；然四份 `_proj_order` 之簽章為 `(ns, cad, parcels, blk)`、**⛔ 無側別參數**（`_projection_order` 之呼叫粒度係**街廓層**·`147R` 已載）⇒ 本表以街廓層出艙、`6/6`（⛔ 非 `10/10`），⛔ 非規避。

---

## §三　`P-1`／`P-2`／`R-7`

### `P-1`　🛑 **量不到**——其受詞 ⛔ 不在倉

| 框 | 命中 |
|---|---|
| `git ls-tree -r --name-only HEAD \| grep -i "9-142\|WG9142"` | **`0`** |
| **對照組** 同指令 `grep -c "9-148"` | **`1`** ⇒ 量測器非紅 |

⇒ 🔒 **`W-G.9-142` 之報告與探針⛔ 不在倉**（該批係**純量測·零 commit**·`W-G.9-143 §三` 已逐字載「`W-G.9-138`〜`-142` 皆係純量測·零 commit ⇒ 編號不連續係設計如此」）
⇒ **其所用之母體⛔ 無法自倉內現查**。⛔ 未臆造。
🔒 **倉內僅存其<u>結果</u>**：`K-6:3294` 逐字「🔬 遞補鏈模擬（**只算不改**）：`R1右→628-36(1)`／`R2左→628-40(1)+`／`R3右→628-29(1)+`／`R5左→628-7(2)`／`R6右→628-7(1)`」。

### `P-2` ／ `R-7`　🔴 **改以重導** ⇒ 得**第三態：該格 ⛔ 無鑑別力**

🔒 **鑑別之設計**：`build` 層之 ghost 僅存於 `R1`／`R4`（`148R` 已證）；而遞補鏈五格中**唯 `R1右` 落於 ghost 街廓** ⇒ **該格即唯一之鑑別點**。取 `K-9-23-a` 所載 `R1右` 之第 1 宗 `628-34(3)`，求其於二態下之鄰位。

**實測**（探針 `verify/probes/probe_WG9149_stage.py`·`rc=0`／`stderr` `0` B）：

| 態 | 投影序 | `628-34(3)` 索引 | 順序下一位 | 逆序下一位 |
|---|---|---|---|---|
| **含 ghost**（`n=6`） | `['628-35(2)', '_GHOST_(R1)', '628-37(1)', '628-36(1)', '628-34(3)', '628(5)']` | `4` | `628(5)` ❌ | **`628-36(1)` ✅** |
| **去 ghost**（`n=5`） | `['628-35(2)', '628-37(1)', '628-36(1)', '628-34(3)', '628(5)']` | `3` | `628(5)` ❌ | **`628-36(1)` ✅** |

🔒 **二態之<u>同一方向</u>（逆投影序）皆重現正典所載之 `628-36(1)`**
⇒ 🔴 **該格對「母體含不含 ghost」⛔ 無鑑別力**（成因：ghost 於 `R1` 之排名 `r = 2`，距 `628-34(3)`（`r = 5`）尚有二位 ⇒ 進不了其鄰位）。

🛑 **⇒ `R-7` 之判：單之三分支<u>未涵蓋此態</u>**

| 單所設之分支 | 實測 |
|---|---|
| `R-7` **成立**（`142` 用排除 ghost 母體 ⇒ 八格表已編碼讀法甲） | ❌ **證不出**（二態皆重現 ⇒ 無鑑別力） |
| `R-7` **不成立**（`142` 用含 ghost 母體 ⇒ 八格表與 `148R` **互斥** ⇒ 停機） | ❌ **亦不成立**——**⛔ 無互斥**（二態皆重現） |
| 🆕 **第三態** | ✅ **該格對本命題⛔ 無鑑別力** ⇒ 八格表 **⛔ 未編碼讀法甲**、亦 **⛔ 不與 `148R` 互斥** |

🔒 **⇒ `§七-163` 之停機閘所防之危害（「八格表與 `148R` 互斥而仍出主批」）<u>已由實測正面排除</u>**（⛔ 非「未量到而放行」）⇒ CC 依常規二取保守項**續辦 `§四` 之登記**（三則皆**純附加**·⛔ 無土地後果），並逐字回報本第三態。

### 🩸 CC 自捕：`_spatial_order_parcels_v2` 之**位置參數推定錯**（家法 `6`）

首版 `R-7` 探針以 `SOP2(pib, p1, p2, pk)` 呼叫，得 `ordered` ＝ `['628-37(1)', '628-36(1)', '628-35(2)', '628(5)', '628-34(3)', '_GHOST_(R1)']`（ghost **末位**·`r = 6`），與 `148R` `M-3` 之 `r = 2` **相矛盾**。
🔒 **⛔ 未臆測，逐階段實測歸因**：`run_corner_pk` **前後** `polygon_coords` 改變之宗 ＝ **`0/6`**（`R1`）／**`0/3`**（`R4`）、`front_line` `p1`／`p2` 皆 `A==B` ⇒ **序 A == 序 B** ⇒ **⛔ 非 stage-dependent**、`148R` `M-3` **正確**。
🔴 **真因**：`_spatial_order_parcels_v2` 之簽章第 `2` 位係 **`d_hat`**（`app.py:7689-7694`），⛔ 非 `front_line_p1`——CC 由 `:7731` 之**呼叫名**推定了**位置**，遂把 `p1`→`d_hat`、`p2`→`front_line_p1`、`pk`→`front_line_p2` ⇒ `_projection_order` 收到一條**錯的線**。
🔒 **戒（家法 `6` 之再現）**：**參數值之斷言須 spy 實收**——**位置參數之對應亦然**，⛔ 不得由他處呼叫之**關鍵字名**推定**位置**。正確呼叫式見 `verify/stepg_pipeline.py:479-486`（**全 keyword**）。
🔒 **⇒ 該首版輸出已作廢·⛔ 未用於任何結論**；`R-7` 之判係改以**不依賴 `SOP2`** 之直接鄰位法取得。

---

## §四　登記（`W-G.9波_claude.ai側自誤登記.md` **純附加 `39/0`**）

### 取號現查（四事齊）

| 受詞 | 所用之框 | 既有最大號 | 新號之命中 | 對照組 |
|---|---|---|---|---|
| 自誤 | **四框並列**（表列式反引號 `13`/max `182`；表列式裸 `5`/max `3`；`^#{2,6}.*?自誤 N` `170`/max **`184`**；`^###\s*自誤 N` `50`/max **`184`**）⇒ UNION 相異 `180` | **`184`** | `自誤 185`／`186` ⇒ **`0`** | `自誤 184` ＝ `2` 檔 |

### 三則之內容

| 則 | 號 | 內容 |
|---|---|---|
| `4-1` | — | **號之更正**：`183`／`184` 之確定意指表 ＋ 三項仍積欠者之錨 |
| `4-2` | **`185`** | 以「結論對」掩蓋「理由錯」 |
| `4-3` | **`186`** | 引用一個自己已知未入倉之號 |

🔒 **`4-1` 之三項積欠者 ⛔ 刻意不預先取號**——依 `4-3`（＝ `自誤 186`）所立之**新戒逐字**：「**未入倉者⛔ 不得以號作錨，須改以逐字描述 ＋ 其出處文件作錨**」。其錨 ＝ 意指描述 ＋ 出處 `W-G.9-146-PRE §二` 積欠定稿表第 `1`〜`3` 項。
🔒 **`148R §三-3` 之自指**已於 `4-1` 具名更正：該句應讀為「**累犯：與上表第 `3` 列（發出窗·對照組門檻未現查）同族·第四度**」。⛔ 原句一字不刪。

**期初／期末三數**：`3489`／`292194`／`6f6930bb1e0d7ebe` → **`3528`／`296714`／`1851ff4ba03a8c2b`**（列增 `39`·位元組增 `4520`）。

---

## §五　`§五` 七項預測之逐項對拍（⛔ 未改預測）

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `R-1` | `149` 實體命中 `0`；對照組 `148` ≥ `1` | 實體 `0`（標題式／檔名式／檔名皆 `0`）；**裸框 `2`**（皆 CC 前批之前瞻引用）；對照組 `7` 檔 | ✅ **符**（附註裸框非 `0`） |
| `R-2` | 塊 `J` 列數 > `20` | **`26`** | ✅ **符** |
| `R-3` | 塊 `K`：`K-9-4` 係**適用場合**、⛔ 非述詞之定義域 | 🛑 **不可由倉內逐字判定**——KL 逐字段⛔ 未提 `K-9-4`；`一`／`三` 之性質**倉內未具名** | ⚠️ **不可判·CC ⛔ 不自裁** |
| `R-4` | 塊 `L`：遞補之受詞為**地號**，⛔ 無允許非宗地物遞補之文字 | 逐字「該空位就由 **`3`** 遞補」；該段⛔ 無此類文字 | ✅ **符** |
| `R-5` | 四份 `_proj_order` **逐位相同或僅縮排異** | 🔴 `wf_f0` 與其餘三份**實質分歧**（完整 `tp` vs 二鍵投影） | ❌ **不符** |
| `R-6` | 五份於同一輸入 `10/10` 逐位相同；注入擾動後至少一格轉紅 | **`6/6`** 逐位相同（粒度係街廓層·⛔ 無側別）；擾動後 **`6/6`** 轉紅 | ✅ **符**（量詞為 `6/6`·⛔ 非 `10/10`） |
| `R-7` | `142` 之母體為**排除 ghost** ⇒ 八格表已編碼讀法甲 | 🆕 **第三態**：該格 **⛔ 無鑑別力**（二態皆重現 `628-36(1)`） | ❌ **不符**（惟**⛔ 無互斥** ⇒ 停機閘之危害已排除） |

### 🛑 依 `§五` 分水嶺之判讀

- **`R-3` 不可判** ⇒ 🔒 **讀法甲之錨<u>未確立</u>**，亦**未被推翻** ⇒ 本題**⛔ 不由 CC 判**；發單側須先定 `K-9-19` `一`／`三` 之性質（KL 逐字 抑或 claude.ai 整理），方能落 `VR`（讀法甲）或改走上呈。
- **`R-7` 得第三態** ⇒ **⛔ 無互斥** ⇒ **⛔ 不停機**；八格表**⛔ 未編碼讀法甲**（⇒ 亦⛔ 不得引之為讀法甲之佐證）。

---

## §六　收工閘

| # | 實得 |
|---|---|
| `1` | `HEAD`（期末）見 commit；`app.py` blob ＝ **`b4cc30835feaad03dfc37f394a669bbb45988b8b`**（⛔ 零觸·絕對值出艙） |
| `2` | `git diff --numstat f71c147` ＝ 恰 `1` 列（`39 0`）·**刪除欄為 `0`**；`wg942_append_audit.py` `rc=0`（逐字結論列見 commit message） |
| `3` | `D-1`／`D-2` 四塊皆附「出處 ＋ blob 全 `40` 碼 ＋ 三數」；**四反引號區塊恰 `4` 個** |
| `4` | `N-1`／`N-2`／`P-1`／`P-2` 逐項有數、附框與逐字 |
| `5` | `§五` 七項預測逐項標明 符／不符 |
| `6` | 凡「命中 `0`」之斷言皆同格附框／命中數／對照組 |
| `7` | 量測時點 ＝ `2026-08-27`·情境 `0m`／`STEP0=on`·**harness 態** |
| `8` | `§四` 三則之取號四事齊 |
| `9` | 二支探針對 `run_all.py:73` 之樣式命中 **`0`**（自證通過） |

**本批異動**：`W-G.9波_claude.ai側自誤登記.md` **純附加 `39/0`** ＋ 新增本報告 ＋ 二探針 ＋ 二 log。

🛑 **回報即停**——`K-9-19` 之射程（`R-3`）與 ghost 一題之處置**由發單側落筆**，CC ⛔ 不自裁。
