# 🔬 `W-G.9-158R`　`GB-110` 索引之補正 ＋ 自誤 `193` ＋ 常規四（九）

**單**：`W-G.9-158`（`SELF_SHA256 = 8859a4ada7376b4f1d7f1da64ec631cd65a33ed0a0491c848d444793bcee7676`·CC 覆核**逐位相符**）
**基座**：`12d84d3385e900a309a65f1a666801857d00dadd`　**`app.py` blob**：`b4cc30835feaad03dfc37f394a669bbb45988b8b`（期初＝期末·**⛔ 一位元未動**）
**性質**：🔒 **純附加登記**·⛔ 零生產碼·⛔ 未改任何既有文字（`deletions` 逐列 ＝ `0`）
**量測時點**：`2026-08-28`　🔒 **母體之態**（`VR-058 三`）＝ `blob@12d84d3` 之工作區
🔒 **母體**（`§一`〜`§三` 之一切命中數）＝ **`docs/**/*.md` ＋ `CLAUDE.md` ⇒ `386` 檔**
（`S-7` 另量 `blob@b8ac8f2`·母體逐項具名於 `§四`）

| 受檔（皆在 `wg942` `TARGETS` ⇒ append-only） | 增 | 刪 |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `11` | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `17` | **`0`** |
| `CLAUDE.md` | `14` | **`0`** |

🔒 三塊逐字皆自**單之檔案管線**抽取（剝一層 blockquote 前綴）、⛔ 未手寫一字（**常規七**）。
🔒 三受檔之 `CR` 數皆 **`0`**（⛔ 無 CRLF 改形）。

---

## §一　取號現查（🛑 CC 自跑·⛔ 未採單表·**二量詞框並報**·`VR-058 一`）

| 待鑄 | 定義列式（**逐字**） | 該式 MAX（期初） | 新號 **命中列數**（`grep -c` 框） | 新號 **出現次數**（`str.count()` 框） | 對照組 | 判 |
|---|---|---|---|---|---|---|
| `W-G.9-158` | `^docs/reports/W-G\.9-(\d{1,4})` | `157` | `0` | `0` | `W-G.9-157` ⇒ `8`／`8` | ✅ 未占用 |
| 自誤 `193` | `^#+ 自誤 \`?([0-9]{1,4})\`?` | **`192`**（相異 `161`） | `0` | `0` | `自誤 192` ⇒ `5`／`5` | ✅ 未占用 |
| 常規四（九） | `^\*\*常規四（([一二三四五六七八九十]+)）` | **（八）**（命中 `2` 列：`CLAUDE.md:663`／`:670`） | `0` | `0` | `常規四（八）` ⇒ `12`／`12` | ✅ 未占用 |

🔒 **常規四（九）四之施用**——鑄號前先取該類**既有最末一則**之標題形逐字為範本：

```text
docs/reports/W-G.9波_claude.ai側自誤登記.md:3612｜### 自誤 `192`（發單側·落地窗）：**有倉可查而未查**——誤載基座 ＋ 引用未執行之單所立之款
```

⇒ 本批所鑄 `### 自誤 \`193\`（發單側·落地窗）：…` **與之同形**。

---

## §二　`§六-3`　常規四（九）之二項機檢（**本批新立·即刻施用**）

### ① 形之自證（**⛔ 於附加前**·以三框掃 payload 文字）

| payload 之列 | 命中之框 |
|---|---|
| `### \`GB-110\` 🆕` | **式B** ✅ |
| `| **\`GB-110\`** 🆕 | 🟡 八格… |` | **定義列** ✅ |
| 其餘 `4` 列（散文／表頭／分隔列） | 無（**預期**） |

⇒ 🔒 **payload 命中 `110`**（**二錨並補**·合單 `§一` 之「採提案甲並加乙」）。

🔒 **判別力對照（⛔ 逐列測·⛔ 非整塊掃）**——首版之對照組**失效**並經 CC 當場更正：
整塊掃時，縱令標題形改回 emoji-在前，**表列錨仍使該塊命中 `110`** ⇒ 該對照組**無鑑別力**。
改逐列測後：

| 形 | 三框之判 |
|---|---|
| `### \`GB-110\` 🆕`（本批·emoji 在號**後**） | **式B** ✅ |
| `### 🟡 \`GB-110\` 🆕　…`（既有 `:2879`·emoji 在號**前**） | 🔴 **三框皆不命中**（＝已知為偽者須紅） |
| `| **\`GB-110\`** 🆕 | … |` | **定義列** ✅ |
| `### \`GB-109\` 🆕`（既有體例） | **式B** ✅ |

⇒ 🔒 二造（已知為真／已知為偽）**皆如預期** ⇒ 該自檢**非恆綠**。

### ② MAX 之推進（入倉後全倉重掃）

| 時點 | 母體 | GB 三框聯集 MAX | 相異 | `110` 在聯集? | 缺號集 |
|---|---|---|---|---|---|
| **期初** | `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔 | **`109`** | `107` | 🔴 否 | `[12, 87]` |
| **期末** | 同上 | **`110`** | `108` | ✅ **是** | `[12, 87]`（**`110` ⛔ 不在其中**） |

🔒 `110` 之命中出處 ＝ **式B @ `:2894`** ＋ **定義列 @ `:2901`**（二錨）。
🔒 **量詞框**：上表之「MAX／相異／缺號」皆基於**定義列式之命中**（⛔ 非裸框、⛔ 非出現次數）。

🔒 **併報之期末推進**（同一定義列式框）：

| 類 | 期初 MAX | 期末 MAX | 新號之出處 |
|---|---|---|---|
| 自誤 | `192` | **`193`** | `docs/reports/W-G.9波_claude.ai側自誤登記.md:3639` |
| 常規四（N） | （八） | **（九）** | `CLAUDE.md:705`（命中列 `663`／`670`／`705`） |

---

## §三　`§六-4`　常規七 逐字覆抽（自**基座 blob** 重抽·與單所載比對）

```text
docs/reports/W-G.4_泛用阻塞項登記表.md:2849｜### `GB-109` 🆕

docs/reports/W-G.4_泛用阻塞項登記表.md:2853｜| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |

docs/reports/W-G.4_泛用阻塞項登記表.md:2879｜### 🟡 `GB-110` 🆕　八格驗收預測表對閘一之**關係詞**（`>` vs `>=`）**⛔ 無判別力**
```

🔒 **三處與單所引逐位相符**（`:2849` emoji 在號**後**／`:2853` 為**七欄**表頭／`:2879` emoji 在號**前**）⇒ ⛔ 不停機。

---

## §四　`S-7`　`GB-110` 於 `blob@b8ac8f2` 之二框（🔒 母體逐項具名·`VR-058 三`）

| 母體 | 檔數 | **命中列數** | **出現次數** |
|---|---|---|---|
| 全倉 `*.md`（＝ `W-G.9-157R` 所用者） | `434` | **`1`** | **`2`** |
| `docs/**/*.md` ＋ `CLAUDE.md`（＝ 本單 `§零` 之母體） | `385` | **`1`** | **`2`** |

🔒 **二母體同數** ⇒ 該唯一命中列（`docs/reports/W-G.9-156R_八格表可重算輸入.md:21`）落於二母體之交集。
⚠️ 依 `VR-058 四`，⛔ **不得**據「二框於此同為 `1`／`2`」推論「框無所謂」——本例正因二框**相異**（`1` vs `2`）而生 `自誤 193` 肢②。

---

## §五　`§四` 八項驗收預測之逐項對拍（🛑 **⛔ 未改任何預測**）

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `S-1` | 三者定義列式命中皆 `0`（二框並報） | `W-G.9-158` `0`／`0`；自誤 `193` `0`／`0`；常規四（九） `0`／`0` | ✅ **符** |
| `S-2` | 期初 GB 三框 MAX ＝ `109`，`110` ⛔ 不在聯集 | MAX `109`／相異 `107`／`110` 不在聯集 | ✅ **符** |
| `S-3` | 期末 GB 三框 MAX ＝ `110`，`110` ⛔ 不在缺號集 | MAX `110`／相異 `108`／缺號 `[12, 87]` | ✅ **符** |
| `S-4` | `W-G.4:2879` 逐字為 `### 🟡 \`GB-110\` 🆕　…` | 逐位相符（`§三`） | ✅ **符** |
| `S-5` | `:2849` 為 `### \`GB-109\` 🆕`；`:2853` 表頭為七欄 | 逐位相符（`§三`·`:2853` 計得 **`7`** 欄） | ✅ **符** |
| `S-6` | 自誤定義列式 MAX 期初 ＝ `192`；`VR` ＝ `75` | 自誤 `192`（相異 `161`）；`VR` `75`（相異 `61`·`K-6:3588`） | ✅ **符** |
| `S-7` | `GB-110` 於 `blob@b8ac8f2`：命中列數 `1`／出現次數 `2` | `1`／`2`（**二母體皆然**·見 `§四`） | ✅ **符** |
| `S-8` | 受檔 `deletions` 逐列 ＝ `0` | `11/0`、`17/0`、`14/0` | ✅ **符** |

⇒ **符 `8`／不符 `0`／不可判 `0`**。

---

## §六　`§五` 不辦事項之逐項確認

⛔ 未改 `W-G.4:2879` 原節一字（**append-only**·以索引錨補之）；⛔ 未追改 `GB-109` 及其以前任何號之索引；
⛔ 未解除 `GB-110`；⛔ 未新立 `VR`；⛔ 未落地 `VR-074`／`VR-075`；⛔ 未建 `BASE`；⛔ 未寫不變式；
⛔ 未改 `app.py`／`verify/`／`data/` 任一位元組；⛔ 未改任何既有文字；⛔ 未補 `GB-104` 之 loud 斷言；⛔ 未重產 baseline。

🔒 **`verify/` 零觸之證**：`git diff --numstat 12d84d3 -- .` 之輸出**僅三列**（見檔頭表），⛔ 無任何 `verify/` 或 `app.py` 之列。

## §七　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | `HEAD` 全 `40` 碼；`app.py` blob 逐位 | 基座 `12d84d3385e900a309a65f1a666801857d00dadd`；`app.py` ＝ `b4cc30835feaad03dfc37f394a669bbb45988b8b` ✅ |
| `2` | `numstat` 刪除欄逐列 `0`；`wg942_append_audit` | `11/0`、`17/0`、`14/0`；`rc=0`·`stderr` `0` B·受檢 `77` 筆·逐字「結論：✅ 全部成立」 ✅ |
| `3` | 常規四（九）① 形自證 ＋ ② MAX 推進（二數同格·具名量詞框） | ✅ `§二`（① 二錨命中·對照組已知為偽者紅；② `109` → `110`·`110` ⛔ 不在缺號集） |
| `4` | 常規七：`:2879`／`:2849`／`:2853` 重抽比對 | ✅ `§三`（三處逐位相符·⛔ 未停機） |
| `5` | `VR-058 一`：每一命中數皆標框；`三`：母體之態具名 | ✅ 檔頭 ＋ `§一`／`§二`／`§四` 逐表標框 |
| `6` | 常規八：`N/N` 型自證須同格具名外部錨 | ✅ 見下 |
| `7` | `§四` 八項預測逐項對拍·⛔ 不改預測 | ✅ `§五`（**符 `8`**／不符 `0`） |
| `8` | 常規四（七）報二事、（八）逐字列定義列式 | ✅ `§一` |
| `9` | 報告命名 ＝ `W-G.9-158R` | ✅ |

🔒 **常規八之施用**：本報告之 `N/N` 型自證為 **`§五` 之「符 `8`／`8`」**。其**外部錨**同格具名——
① **與獨立來源對拍** ＝ 單 `§四` 之八項預測係**發單側**於 `blob@12d84d3` 之實查，本批逐項重跑；
② **判別力對照** ＝ `§二①` 之逐列三形測（已知為偽之 `:2879` 形**須紅且確紅**）；
③ **界限檢查** ＝ `§二②` 之 MAX **由 `109` 轉 `110`**（若 payload 之形無效，此數**必不動**）。
🔒 **⛔ 不以該 `8/8` 單獨作為「本批正確」之證據**（常規八 三）。

---

## §八　🔒 併記：本批之二處 CC 自捕（⛔ 未出艙）

1. **`§二①` 之對照組首版失效**——以「整塊掃 payload」為之時，縱令標題形改回 emoji-在前，
   **表列錨仍使該塊命中 `110`** ⇒ 對照組**恆綠、無鑑別力**。改**逐列測**後二造皆如預期。
   🔒 同族於 `CLAUDE.md`〈探針之自我驗證閘〉之「宜再餵一個**已知為偽**者須紅」。
2. **`§四` 之母體須逐項具名**——`S-7` 之 `1`／`2` 於**二個不同母體**下同數，
   惟依 `VR-058 四` ⛔ 不得據此推論「框無所謂」；二框在此**確實相異**（`1` vs `2`），
   該相異正是 `自誤 193` 肢②之受詞。
