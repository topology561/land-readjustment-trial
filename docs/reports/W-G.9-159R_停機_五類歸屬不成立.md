# 🛑 `W-G.9-159R`　**停機上呈**：`§二` 五類之歸屬**經實測不成立**（`L-3` ④ 之 `⊆` 恆假）

**單**：`W-G.9-159`（`SELF_SHA256 = ee7712b5c86809c35152baf67907d3eeca7fc4e6843d7c8f2584e385bf45c761`·CC 覆核**逐位相符**）
**基座**：`2d72de349edc658616be8096c3b2d88318517d1a`
**`app.py` blob 期末**：`b4cc30835feaad03dfc37f394a669bbb45988b8b`
🔴 **＝ 期初值**——**⛔ 未寫任何生產碼**（停機於 `§三` 落地之<u>前</u>·理由見 `§三`）。
**性質**：🔒 **只讀探針 ＋ 純新增三檔**·⛔ 零生產碼·⛔ 未改任何既有文字（`deletions` 逐列 `0`）
**量測時點**：`2026-08-28`

| 出艙物 | 位元組 | `sha256` |
|---|---|---|
| `verify/probes/probe_WG9159_projorder_pop.py` | `9446` | `1379c6fe899b15178e1021cf96bce941fbe3d375a14197df6134b482688093df` |
| `verify/out/probe_WG9159_projorder_pop_2d72de3.log` | `7531` | `b30af1773201ffd748cf2bc67dcd8165f6b0ebb390c990bdbf302898583194e7` |
| `verify/out/probe_WG9159_projorder_pop_2d72de3.json` | `13496` | `98d7f21f3ce48b850a6caa5f949d32bca118b8e083ed19027a16c6da2565b950` |
| `docs/reports/W-G.9-159R_停機_五類歸屬不成立.md` | （本檔） | — |

---

## 🛑 §零之零　結論先行（**三項停機 ＋ 一項限制**）

| # | 事 | 判 |
|---|---|---|
| `S-A` | 🔴 **`L-3` 之 ④ 例外「`_mem ⊆ BASE(該街廓)`」<u>恆假</u>** —— 實測 **`8／8` 相異呼叫全數違反**，且二者之 id 空間**完全不相交** | 🛑 **停機** |
| `S-B` | 🔴 **`§二` 之五類<u>不足</u>** —— `wf_f1:376`／`wf_f4:1433`／`:1503` 之實參係**合成 dict**（僅二鍵·幾何為**重整後**），⛔ 非 `BASE` 之任何濾式 ⇒ 須**第六類** | 🛑 **停機** |
| `S-C` | 🔴 **`L-1` 之 `BASE` 定義<u>自身歧義</u>** —— 單逐字「該街廓之全體 `temp_parcels`·**現行 `by_blk[blk]` 之等價物**」，而實測二者**⛔ 非同物**（前者為步驟 0 合併**前**、後者為合併**後**） | 🛑 **停機** |
| `S-D` | ⚠️ **`Z-4` 之「全情境」實測上不可達** —— `run_verification` 於 trunk A 之 `②-宗 圍堵閘破[R2]` 即中止 ⇒ `wf_f0`〜`wf_f4` **從未執行**；`app.py:18913` 在 `main()` 內 ⇒ 十二點中**僅 `4` 點**可被觀測 | ⚠️ **限制**（⛔ 非停機·但使 `Z-4` 之射程須重界定） |

🔒 **⛔ 未自行改寫 `L-3` 以求可實作**——`④` 應改斷言於**何母體**係<u>意思決定</u>，屬發單側。
🔒 依單 `§八` 逐字「**⛔ 不得為使 `Z-1`〜`Z-4` 轉綠而修改濾式、放寬容差、或關閉斷言——任一不過即停機，其不過<u>本身</u>即本批之產出**」⇒ 本報告即該產出。

---

## §一　取號現查（🛑 CC 自跑·二量詞框並報·`VR-058 一`）

🔒 **母體** ＝ `docs/**/*.md` ＋ `CLAUDE.md` ⇒ **`387` 檔**（態 ＝ `blob@2d72de3`·`VR-058 三`）

| 待鑄 | 定義列式（**逐字**） | 該式 MAX | 命中列數 | 出現次數 | 對照組 | 判 |
|---|---|---|---|---|---|---|
| `W-G.9-159` | `^docs/reports/W-G\.9-(\d{1,4})` | `158` | `0` | `0` | `W-G.9-158` ⇒ `6`／`6` | ✅ 未占用 |
| `VR-076` | `^##\s*\`VR-([0-9]{1,4})\`` | `75`（相異 `61`） | `0` | `0` | `VR-075` ⇒ `7`／`8` | ✅ 未占用 |
| `GB-111` | 三框聯集（母體 ＝ `W-G.4` 登記表**單檔**） | `110`（相異 `108`·缺號 `[12, 87]`） | `0` | `0` | `GB-110` ⇒ `42`／`48` | ✅ 未占用 |

🔒 **本批⛔ 未鑄 `VR-076`／`GB-111`**（單逐字：「僅於 `§二` 之具名須成裁時」／「僅於出現新阻塞時」）
——`S-A`〜`S-C` 係**單之規格與倉態不符**，其處置屬發單側；CC ⛔ 不代鑄。

### 🔴 併報：**裸號框之子字串偽陽性**（新面·`常規四（七）二` 所令之框有此洞）

`常規四（七）二` 令意指占用之框「須含 `docs/reports/**` 之**裸號**」。CC 依之以裸框 `9-159` 量得：

| 框 | 命中列數 | 出現次數 | 唯一命中 |
|---|---|---|---|
| 裸號 `9-159` | **`1`** | **`1`** | `docs/specs/W-G.4_P2_兩階段落位_施工交接.md:119` |
| 完整前綴 `W-G.9-159` | **`0`** | **`0`** | — |

該唯一命中之逐字（自基座 blob 重抽）：

```text
docs/specs/W-G.4_P2_兩階段落位_施工交接.md:119｜| §N3-0 逐宗主閘 | `:818-832` | `app.py:15959-15966` |
```

⇒ 🔒 **`9-159` 係 `15959-15966` 之<u>子字串</u>**（`1595` ＋ `9-159` ＋ `66`），**⛔ 非意指占用**。
🔒 **對照組**（證量測器非紅）：裸框 `9-158` ⇒ `6` 列／`W-G.9-158` ⇒ `6` 列（**二框同數**）；
裸框 `9-157` ⇒ `15` 列／`16` 次 vs `W-G.9-157` ⇒ `15` 列／`15` 次（**次數框相異 `1`**·同族）。
🛑 **提案（常規三·二候選）**：

| 候選 | 作法 | 理由 |
|---|---|---|
| **甲（建議）** | 意指占用之框改用**完整前綴** `W-G.9-NNN`，並於 `常規四（七）二` 就地加註 | 消除數字子字串偽陽性；⛔ 不減其涵蓋（凡真占用必帶前綴） |
| 乙 | 保留裸號框，惟**須逐筆目視其上下文**再判 | 與現行逐字一致，惟每次取號多一道人工 |

---

## §二　`§八-6`　常規七 逐字覆抽（`§二` 五類之構造處·自**基座 blob** 重抽）

```text
app.py:18897｜                            _all_in_blk = by_blk.get(_lbl, [])
verify/selection_pipeline.py:333｜        _all_in_blk = by_blk.get(_lbl, [])

verify/stepg_pipeline.py:416｜        _stage1_parcels = [tp for tp in parcels_in_blk if '配地階段' not in tp]

verify/wf_f0.py:254｜    pib = [tp for tp in parcels if tp["所屬街廓"] == blk
verify/wf_f0.py:255｜           and not tp.get("_is_ghost_sliver")]

app.py:11382｜            _mem = [_pk[_j] for _j in _g]

app.py:20037｜                                parcels_in_block=_stage1_parcels,   # P2-a：僅階段1宗
```

🔒 **五處皆與單所載逐位相符** ⇒ `P-2` **符**。
⚠️ 單將 `wf_f0:254-255` 以**單列**形載之（`pib = [tp for tp in parcels if tp["所屬街廓"] == blk and not tp.get("_is_ghost_sliver")]`），
倉內實為**二列**（續行）——**內容相同、⛔ 非不符**；具名之以免後人誤判。

---

## §三　🛑 `S-A`　`L-3` ④ 之 `⊆` **恆假**（本批之主產出）

### 一、實測

```text
── 🔴 類 ④ `_mem`（`app.py:11396`） vs `BASE(該街廓)`（單 `L-3` 令斷言 `⊆`）──
  R2   n=2  ids=['628-43(1)', '628-40(1)']                     ⊆BASE ＝ False  只在 `_mem` ＝ ['628-43(1)', '628-40(1)']
  R3   n=2  ids=['628-45(2)', '628-30(3)']                     ⊆BASE ＝ False  只在 `_mem` ＝ ['628-45(2)', '628-30(3)']
  R3   n=3  ids=['628-47(1)', '628-48(1)', '628-49(2)']        ⊆BASE ＝ False  只在 `_mem` ＝ ['628-47(1)', '628-48(1)', '628-49(2)']
  R3   n=2  ids=['628-28(1)', '628-29(1)']                     ⊆BASE ＝ False  只在 `_mem` ＝ ['628-28(1)', '628-29(1)']
  R5   n=3  ids=['628-23(2)', '628-22(2)', '628-21(2)']        ⊆BASE ＝ False  只在 `_mem` ＝ ['628-23(2)', '628-22(2)', '628-21(2)']
  R5   n=3  ids=['628-20(2)', '628-45(1)', '628-30(1)']        ⊆BASE ＝ False  只在 `_mem` ＝ ['628-20(2)', '628-45(1)', '628-30(1)']
  R6   n=2  ids=['628(2)', '628-1(2)']                         ⊆BASE ＝ False  只在 `_mem` ＝ ['628(2)', '628-1(2)']
  R6   n=3  ids=['628-23(1)', '628-22(1)', '628-21(1)']        ⊆BASE ＝ False  只在 `_mem` ＝ ['628-23(1)', '628-22(1)', '628-21(1)']
  ⇒ 🔴 **違反 `⊆` 之相異呼叫 ＝ 8／8（全數）**
```

### 二、根因（**⛔ 非偶然·係構造上不可能**）

`_mem` 之來源鏈（自基座 blob 逐字）：

```text
app.py:11368｜
app.py:11369｜        _pk = []
app.py:11370｜        for _i in _idxs:
app.py:11371｜            _tp = temp_parcels[_i]
app.py:11372｜            _cs = _tp.get("polygon_coords") or []
app.py:11373｜            _d = dict(_tp)
app.py:11374｜            _d["polygon"] = _Poly_k6(_cs) if len(_cs) >= 3 else None
app.py:11375｜            _d["_k6_src_index"] = _i
```

⇒ `_pk` 建自 **`temp_parcels`**（**步驟 0 合併<u>前</u>之暫編層**），而 `BASE ＝ by_blk[blk]` 係
**合併<u>後</u>之建物層**。二者之 id 空間**不相交**：實測 `BASE(R2)` 含 `628-40(1)+`（**帶 `+`**），
而 `_mem` 含 `628-40(1)`（**不帶 `+`**）。

🔑 **決定性之一句**：`_projection_order(_mem, …)` 之**用途本身**即「**導出合併後之名**」——

```text
app.py:11394｜            # 🔒 命名與位次：位次由**聯集幾何**依 `_projection_order` 導出（⛔ 不另訂）；
app.py:11395｜            #    命名取「投影序在前者之暫編地號」＋`+`。
app.py:11396｜            _ordered = _projection_order(_mem, _p1, _p2)
app.py:11397｜            _name = str(_ordered[0].get("暫編地號", "")) + "+"
```

⇒ 🔒 **`_mem` 之元素於呼叫當下<u>尚未</u>成為 `BASE` 之元素**（`BASE` 之 `…+` 名正由本呼叫產生）
⇒ **`_mem ⊆ BASE` 於因果上不可能成立**，⛔ 非「本案恰好不成立」。

### 三、若照單實作之後果（**⛔ 未實作·此為推論**）

`L-4` 令斷言 **loud `raise`**、⛔ 不得以環境變數關閉。實測 `WV_K6_STEP0` 預設 `on` 時
該點於 `run_verification` 一趟內觸發 **`8`** 個相異合併群 ⇒ **首個步驟 0 合併群即 `raise`**
⇒ 🔴 **全案立即停機**（`K-6 §二` 步驟 0 為 `W-G.9-136` 已落地之**有土地後果**碼路徑）。

### 四、🛑 待裁（**CC ⛔ 不自裁**）

`④` 之不變式應繫於**何母體**？至少三個候選，其**土地後果與診斷力互異**：

| 候選 | 斷言 | 評 |
|---|---|---|
| 甲 | `_mem ⊆ temp_parcels(該街廓)`（**暫編層**·⛔ 非 `BASE`） | 可捕跨街廓污染；須先立**第二個** `BASE`（暫編層），與 `L-1` 之「單一構造」相衝 |
| 乙 | `_mem` 之 `_k6_src_index` 皆為 `temp_parcels` 之合法索引 ∧ 其 `所屬街廓` 單一 | ⛔ 不需第二 `BASE`；診斷力較弱（不涵蓋 id 層） |
| 丙 | `④` **⛔ 不設不變式**，於 `L-2` 宣告表標 `source ＝ TEMP_LAYER`／`filter ＝ group_members` 並具名其**⛔ 不可由 `BASE` 導出** | 最保守；`GB-105` 之失效條件「十二點皆納入」仍可滿足（納入 ＝ **宣告**，⛔ 非**斷言**） |

---

## §四　🛑 `S-B`　`§二` 五類不足 ⇒ 須**第六類**

```text
§D　十二點之**靜態**分類（實參符號·逐行自檔案抽取）
========================================================================================================
  app.py                        :7731  pre_seq = _projection_order(parcels_in_block, front_line_p1, front_line_p2)
  app.py                        :11396 _ordered = _projection_order(_mem, _p1, _p2)
  app.py                        :18913 _projection_order(_all_in_blk, _fl_p1_lstep, _fl_p2_lstep))
  verify/selection_pipeline.py  :339   ns["_projection_order"](_all_in_blk, _fl_p1_lstep, _fl_p2_lstep))
  verify/stepg_pipeline.py      :499   ns["_projection_order"](_stage1_parcels, _cad_fl_blk.get('p1'),
  verify/wf_f0.py               :256   return [tp["暫編地號"] for tp in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
  verify/wf_f1.py               :376   return [tp["暫編地號"] for tp in ns["_projection_order"](pseudo, fl["p1"], fl["p2"])]
  verify/wf_f2.py               :95    return [x["暫編地號"] for x in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
  verify/wf_f3.py               :78    return [x["暫編地號"] for x in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
  verify/wf_f4.py               :1043  return [x["暫編地號"] for x in ns["_projection_order"](pib, fl.get("p1"), fl.get("p2"))]
  verify/wf_f4.py               :1433  return [t["暫編地號"] for t in ns["_projection_order"](pseudo, fl["p1"], fl["p2"])]
  verify/wf_f4.py               :1503  return [t["暫編地號"] for t in ns["_projection_order"](pseudo, fl["p1"], fl["p2"])]

  🔒 `pseudo` 三處之構造（`wf_f1:374-375`／`wf_f4:1431-1432`／`:1501-1502`）皆為
     `[{"暫編地號": k, "polygon_coords": list(v.exterior.coords)} for k, v in <polys>.items()]`
     ⇒ **合成 dict·僅二鍵·其 `polygon_coords` 係<u>重整後</u>之幾何**
     ⇒ 🔴 **⛔ 非 `BASE` 之任何濾式**（`BASE` 之元素有 14〜16 鍵·且幾何為重整<u>前</u>）。

```

🔒 **`P-10` 證偽**：單預測（⛔ 純推測）`wf_f1:376`／`wf_f4:1433`／`:1503` 之 `filter` 亦為 `no_ghost`；
實為 **`pseudo`** ——`[{"暫編地號": k, "polygon_coords": …} for k, v in <polys>.items()]`。

🔒 **其受詞為「重整<u>前後</u>之位次序不變」**（三處之上下文逐字皆為 `# 位次序（投影序）不變` 一類），
⇒ 其母體**刻意**係**重整後之新幾何**；若令其 ≡ `filter(BASE)`，**該三處之受詞即被取消**。

🛑 **待裁**：第六類（暫名 `RESHAPE_PSEUDO`）之 `source`／`filter` 如何宣告？
CC 之提案（**常規三**·⛔ 未自辦）：`source ＝ RESHAPE`／`filter ＝ pseudo_of(<呼叫端 tag>)`，
其不變式改為**弱式**：`實參之暫編地號集合 ≡ 同一呼叫端<u>另一次</u>呼叫之集合`（即重整前後**同集合**）
——此即該三處**碼面已在做**之事（`oB` vs `oN` 之比較），故為**導出關係式**而非新規。

---

## §五　🛑 `S-C`　`L-1` 之 `BASE` 定義自身歧義

單 `L-1` 逐字：「立一處 `BASE(blk)`（＝該街廓之全體 **`temp_parcels`**·現行 **`by_blk[blk]`** 之等價物）」。
🔴 **二者⛔ 非同物**（`§三-二` 已實測）：`temp_parcels` 係步驟 0 合併**前**、`by_blk[blk]` 係合併**後**。
⇒ 依前者則 ①②⑤ 之斷言不成立；依後者則 ④ 之斷言不成立。**⛔ 無單一選擇可使五類皆成立。**
🛑 **待裁**：`BASE` 取何層？抑或立**二層**（`TEMP_LAYER` ／ `BUILD_LAYER`）？
⚠️ 若立二層，`L-2` 之 `source` 欄須擴為三值（`TEMP_LAYER`／`BUILD_LAYER`／`RESHAPE`），
而 `L-2` 所欲之「次批一處修改」性質仍可保（`VR-074` 之 ghost 判準只落在 `BUILD_LAYER` 之 `no_ghost`）。

---

## §六　⚠️ `S-D`　`Z-4` 之射程限制

```text
§A　觀測到之呼叫點與次數（🔒 母體 ＝ 本次 `run_verification` 之全部呼叫）
========================================================================================================
  <app.py:harvest>:11396                     呼叫 32 次
  <app.py:harvest>:7731                      呼叫 8 次
  selection_pipeline.py:339                  呼叫 24 次
  stepg_pipeline.py:499                      呼叫 8 次
  合計 72 次／相異呼叫點 4 個

  🔴 **十二點中僅 4 點被觀測**——`run_verification` 於 trunk A 之
     `②-宗 圍堵閘破[R2]` 即中止 ⇒ `wf_f0`〜`wf_f4` **從未執行**；
     `app.py:18913` 在 `main()` 內（`CLAUDE.md` 逐字：**從不被 `run_all` 執行**）。
     ⇒ 其餘各點之歸類只能**靜態**認定（見 `§D`），⛔ 非「不存在」。
```

⇒ 🔒 **`Z-4` 逐字「十二點之不變式於<u>全情境</u>皆⛔ 未觸發」在本 harness 下⛔ 不可證**——
`4／12` 點可被 `run_verification` 觀測；`app.py:18913` 依 `CLAUDE.md` 逐字**從不被 `run_all` 執行**；
`wf_*` 七點須由 `run_all` 之 **fixture subprocess**（`fixture_end_fallback`／`fixture_end_winner`／
`fixture_midlayer_6items`）驅動，⛔ 不在同一 process ⇒ 本探針之 in-process wrapper 攔不到。
🛑 **待裁**：`Z-4` 之判準改為「**凡被執行到之點皆未觸發** ＋ **未被執行到之點逐點具名**」？
（此即 `CLAUDE.md` 之「`main()` 內之敘述須另附合成案」之同族處置。）

---

## §七　`§六` 十項驗收預測之逐項對拍（🛑 **⛔ 未改任何預測**）

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `P-1` | 三號之二量詞框皆 `0` | `W-G.9-159` `0`／`0`（**完整前綴框**）；`VR-076` `0`／`0`；`GB-111` `0`／`0` | ✅ **符**（併報裸號框之偽陽性·`§一`） |
| `P-2` | `§二` 五類構造處逐字逐位相符 | 五處皆相符（`wf_f0:254-255` 之單列／二列係**呈現差**·內容同） | ✅ **符** |
| `P-3` | `Z-1` 九份 baseline 全綠 | ⛔ **未實作** ⇒ 無受詞 | ⬜ **不可判** |
| `P-4` | `Z-2` 期初／期末逐閘相同 | ⛔ 未實作（期初已跑：`run_verification` `11 PASS`／`23 FAIL`／`RESULT: FAIL`·**准紅碼**） | ⬜ **不可判** |
| `P-5` | `Z-3` 八格逐欄不變 | ⛔ 未實作 | ⬜ **不可判** |
| `P-6` | `Z-4` 十二點斷言全綠 | ⛔ 未實作；且 `S-D` 已證其**射程不可達** | ⬜ **不可判** |
| `P-7` | `D-1`／`D-2`／`D-3` 各至少一次 raise | ⛔ 未實作 | ⬜ **不可判** |
| `P-8` | `L-2` 之 `filter` 相異值恰 `4` 類 ＋ `passthrough` | 🔴 實測需 **`6`** 類（`identity`／`stage1`／`no_ghost`／`group_members`／`passthrough`／**`pseudo`**），且 `group_members` 之 `source` ⛔ 非 `BASE` | 🔴 **不符** |
| `P-9` | `app.py` blob 期末 ≠ `b4cc3083…` | 期末 ＝ **`b4cc3083…`**（⛔ 未動一位元·停機） | 🔴 **不符** |
| `P-10` | ⛔ 純推測：`wf_f1:376`／`wf_f4:1433`／`:1503` 之 `filter` 亦為 `no_ghost` | 🔴 三者皆為 **`pseudo`**（合成 dict·重整後幾何） | 🔴 **不符**（單已標⛔ 非停機） |

⇒ **符 `2`／不符 `3`／不可判 `5`**。🔒 `P-3`〜`P-7` 之「不可判」係**停機之後果**、⛔ 非量測失敗。

---

## §八　收工閘

| # | 項 | 結果 |
|---|---|---|
| `1` | `HEAD` 全 `40` 碼；`app.py` blob 期末全 `40` 碼 | 基座 `2d72de349edc658616be8096c3b2d88318517d1a`；`app.py` ＝ **`b4cc30835feaad03dfc37f394a669bbb45988b8b`**（＝期初·停機） |
| `2` | 常規一 `②` | 🔒 **本批 `deletions` 逐列 ＝ `0`**（⛔ 零生產碼·純新增三檔）⇒ **常規一四項齊備** ⇒ 逕行 `push`。⚠️ 單 `§八-2` 之「須請示」係**繫於 `deletions > 0`**，本批因停機而未達該態 |
| `3` | `§四` `Z-1`〜`Z-4` | ⬜ **未辦**（停機於落地前·理由 `§零之零`） |
| `4` | `§五` `D-1`〜`D-3` | ⬜ **未辦**（同上） |
| `5` | `§六` 十項預測逐項對拍 | ✅ `§七`（符 `2`／不符 `3`／不可判 `5`·⛔ 未改預測） |
| `6` | 常規七：五處構造處重抽比對 | ✅ `§二`（五處逐位相符） |
| `7` | `VR-058 一`／`三`：命中數標框 ＋ 母體之態具名 | ✅ 檔頭 ＋ `§一` |
| `8` | 常規八：`N/N` 型自證同格具名外部錨 | ✅ 見下 |
| `9` | 常規四（七）（八）（九） | ✅ `§一`（**本批未鑄任何號** ⇒ （九）之二機檢**無受詞**·已具名） |
| `10` | `data/` 零觸；`docs/` 既有檔零改 | ✅ `git diff --numstat 2d72de3 -- .` 僅四列新增（見檔頭） |
| `11` | 報告命名 ＝ `W-G.9-159R` | ✅ |

🔒 **常規八之施用**：本報告之 `N/N` 型自證為 `§三` 之「**`8／8` 相異呼叫違反 `⊆`**」。外部錨三項——
① **與獨立來源對拍** ＝ `W-G.9-147R` 之 `§D`（`R2` 逐街廓對拍逐字載「只在前者：`['628-40(1)', '628-43(1)']`」）**與本批同數同名**；
② **判別力對照** ＝ 同一支量測器於**類 ②** 得 `6／6` **成立**、於**類 ⑤** 得**逐位相同** ⇒ 證其⛔ 非恆判 False；
③ **界限檢查** ＝ `BASE(blk)` 係取自**類 ① 之實參本身**（⛔ 非另行重建）⇒ ⛔ 無「重建錯了」之退路。

🔒 **量測器紅之自捕（常規五·⛔ 未出艙）**：首版以 `contextlib.redirect_stdout(StringIO)` 收 `rv.main()` 輸出，
而該函式呼叫 `sys.stdout.reconfigure(...)`（`StringIO` 無該法）⇒ `AttributeError` ⇒ `REC` **`0` 筆**。
🔴 **`0` 筆若逕行出艙，即成「十二點皆未被呼叫」之假結論**。改由 shell 之 `>` 收其輸出後得 **`72`** 筆。

---

## §九　🛑 交予發單側之四項

1. **`S-A`／`S-B`／`S-C` 三項停機**——`L-1`／`L-3` 之規格與倉態不符，須重定；三項各附 CC 之候選（⛔ 未自辦）。
2. **`S-D`**——`Z-4` 之「全情境」須改射程；CC 提案「凡被執行到之點皆未觸發 ＋ 未被執行到者逐點具名」。
3. **裸號框之子字串偽陽性**（`§一`）——`常規四（七）二` 之框有此洞，附二候選。
4. **`P-8`／`P-10` 不符**（單已標⛔ 非停機）——`filter` 實需 `6` 類，逐點具名於 `§四`。
