# 【W-G.9-22 全單報告】三值對目標之偏差——**沒有一條是全對的**，且 `8.9` 已定位

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`ccd1e3d`　**事前登記**：`docs/reports/W-G.9-22_前置登記.md`（commit `f75af82`）
**證據**：`verify/out/probe_WG922_three_vs_target.log`／`verify/out/probe_WG922_scope_and_guard.log`（commit `dc21ad0`）
**字樣錨（倉規 A）**：`【W-G.9-22 全單報告】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行
　🔒 判別力自檢：改用寬字樣 `W-G.9-22` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> 🔴 **本報告之一切數字為<u>下界</u>**——只含**族②③（起算）**，
> ⛔ **未含族①（落位）之面積效果**（`H5` 成立·§四）。
> ⛔ **不得與面積並列、⛔ 不得讀成區間。**
>
> ⛔ **零生產碼**：`app.py`／`verify/stepg_pipeline.py` 未觸；全程**只讀**。
> ⛔ **未估上限**（施工單 §1 已裁押後）、⛔ 未取任何代表值。

---

## §一 `H1` 目標定式之自行覆核 ⇒ **導得出**，但**依據就地更正**

| 情形 | 目標起算 | 施工單所標依據 | 🔧 **現查之正確依據** |
|---|---|---|---|
| 非 `forced` | **`0`** | `K-9-5-12` | ✅ **`K-9-5-12`（五）-1**：「**首宗起點一律為 `0`**（即自 **MP** 起量）」 |
| `forced` | **抵費地遠側界之 `W`** | `K-9-5-13` **裁定二** | 🔧 **`K-9-5-12`（四）**：「…**第 2 宗**之起算為**該抵費地之遠側境界線**；**抵費地本身不計 G、不負擔**」 |

🔴 **依據標錯了**：`K-9-5-13` 裁定二之逐字為「起算一律用**真正的地界**，那條**面積換算出來的替身線**
在側街負擔上**不再有角色**」——它**證成「三個值都要換掉」**，但**⛔ 不指名換成什麼**。
⇒ 已加註 **考古 77**（第四個實例·**裁定引用層**·⛔ 未另立新節）。

🔒 **受詞補明一層**（⛔ 不得以「顯然」帶過）：`K-9-5-12`（四）之受詞為「**第 2 宗**」，
而本批之受詞為「**該側之起算種子**」。**對應成立之由**：forced 時抵費地佔第 1 宗位置而
**不計 G、不負擔** ⇒ **第一個有負擔的宗即第 2 宗**。

🔒 **目標之受詞 ＝ 垂距**（⛔ 不得以 `S1_par` 代之——後者為**沿 FRONTLINE·路平行**，
`app.py` 逐字：「⛔ **非垂距**；`W`（供 Rw 查表）為垂距，**二者同名不同量·禁互代**」）。
⚠️ 二者**數值相近**（`7.000000` vs `6.9990`〜`6.9999`）⇒ 🔴 **正因相近才危險**。

⇒ **`P-1` 未觸發。**

---

## §二 甲組：三值 × 16 格（🔴 **三條路徑⛔ 不得合併為「現行」**）

### 二-1 `H2` `forced` 之生產實值現查（🔒 **外部錨閘**）

| 情境 | 現查所得 | 外部錨（`W-G.9-19` `D1`·⛔ 非本批自產） | 判 |
|---|---|---|---|
| `0m` | **0 格** | `0 格` | ✅ 相同 |
| `3.5m` | **3 格**：`R2/left`／`R3/right`／`R5/left` | 同左 | ✅ 相同 |

🔒 判別力自檢：以**必不同**之集合（錨 ∪ `{('R9','left')}`）比 ⇒ **判為不同** ⇒ 該閘**非恆過**。
🔴 **生產實值**：非 `forced` 格之 `buf` ＝ **0** ⇒ ⛔ **未沿用 `W-G.9-1` 表之假設 `buf`**。

### 二-2 公尺欄（⛔ **與百分點分欄**·⛔ 不比大小·考古 67）

**`forced` 三格**（`3.5m`）：

| 鍵 | 目標（垂距） | `A − 目標` | `B 選槽 − 目標` | `B 推進 − 目標` |
|---|---|---|---|---|
| `R2/left` | `6.999919` | **`+1.674263`** | `−0.019661` | **`−6.999919`** |
| `R3/right` | `6.998992` | **`+1.666543`** | **`+3.358344`** | **`−6.998992`** |
| `R5/left` | `6.998992` | **`+1.622485`** | `−0.017767` | **`−6.998992`** |

**非 `forced` 十三格**（目標 ＝ `0`）：`A − 目標` ＝ **`0.000000`**（全部）／
`B 推進 − 目標` ＝ **`0.000000`**（全部）／
`B 選槽 − 目標` ＝ **`W_0`**：`+0.161877`／`+0.200569`／`−1.693924`／`+1.691802`／
`−0.787529`／`+0.651126`／`−1.640251`／`+0.000001`。

### 二-3 百分點欄（⛔ 分欄）

| 路徑 | `forced` 三格 | 帶號合計 | 絕對值合計 |
|---|---|---|---|
| **A** | **`+8.9062`／`+8.8665`／`+8.6374`** | `+26.4102` | `26.4102` |
| **B 選槽** | `−0.1121`／**`+17.0495`**／`−0.1013` | `+74.3511` | `74.7778` |
| **B 推進** | **`−55.6995`／`−55.6943`／`−55.6943`** | `−167.0880` | `167.0880` |

🔒 倉規：帶號與絕對值**並列**（二者所偵測之錯**不相交**）。

### 二-4 🔴 `H4` 地板歧義之**兩義分碼**（驗收 #3）

**地板歧義之格 ＝ 4**（判準：差 `< 1e-9` **且** 該值或目標 `< 0`）⇒ 出艙碼 **🟡**。
⇒ 🟡 之 `0.0000` ⛔ **不得讀作「無偏差」**——`R(負)` 被夾為 `0`，
**偏差仍在**，只是**在起算項這一欄看不見**（會經**終點項**顯現）。

🔒 **判別力自檢**：`R(+0.1619) − R(0)` ＝ `2.8171`（出艙碼**空**）
vs `R(−1.0) − R(0)` ＝ `0.0000`（出艙碼 **🟡**）⇒ **兩義確實分得開** ⇒ `H4` ✅。

### 二-5 🔴 判：**沒有一條是全對的**

| 路徑 | 非 `forced`（13 格） | `forced`（3 格） |
|---|---|---|
| **A** | ✅ **全對**（`0` ＝ 目標） | 🔴 偏 `+1.62`〜`+1.67` m（`+8.6`〜`+8.9` pp） |
| **B 選槽** | 🔴 偏 `W_0`（**15／16 格**；`R6/right` 之 `W_0 ≈ 0`） | 🔶 `R2`／`R5` **近乎正確**（`0.02` m）；🔴 `R3/right` 偏 `+3.36` m |
| **B 推進** | ✅ **全對**（`0` ＝ 目標） | 🔴 **整整差掉一個目標**（`−7.00` m／`−55.7` pp·**最大偏差**） |

🔒 **離目標最近者 ＝ A**（絕對值合計 `26.41` pp）；**最遠者 ＝ B 推進**（`167.09` pp）。
⚠️ ⛔ **本判為「離目標遠近」，非「何者為對」**——後者屬**域裁**（§七）。

---

## §三 🔴 `8.6〜8.9 個百分點` **已定位**（承 `W-G.9-21` 乙組之未決）

`W-G.9-21` 判「`8.9` 對不上任何一格」⇒ 維持**未證**。**本批已定位**：

> 它是 **`A`（`main()` 排地路徑）之起算<u>對目標</u>之偏差**：
> `+8.9062`（`R2/left`）／`+8.8665`（`R3/right`）／`+8.6374`（`R5/left`）
> ⇒ 範圍 **`8.6374`〜`8.9062`** ⇒ 取 1dp ＝ **`8.6`〜`8.9`** ✅

🔒 **鑑別力自檢**（倉規：**兩個近似值相符 ≠ 相互驗證** ⇒ 須證該候選**具鑑別力**）：
`W-G.9-21` 之候選（`A` 與 `B 選槽` **之間**的差）為 `−9.0183`／`+8.1830`／`−8.7387`
⇒ 範圍 `8.18`〜`9.02` ⇒ **1dp 兩端皆對不上**。
⇒ 🔒 **兩組候選給出不同結論** ⇒ 本次之對應**具鑑別力**，⛔ 非「湊得近」。

⚠️ **⇒ `8.9` 之受詞為 `A` 對目標之偏差**，⛔ **不是**「A 與 B 之間的差」，
亦⛔ **不是**任何面積。**該數自本批起可標 `【倉】`**（出處 ＝ 本報告 §二-3）。

---

## §四 乙組：`H5` 成立——**族① 仍不可估**

碼面逐字 `left_cum_S = float(_left_buffer_S)` ⇒ 族① 取 **`buf` 本身**（**沿 `s` 之量**）。
目標為**真正之地界**（∥SIDELINE）⇒ 該界線在 `s` 下**非單一值** ⇒ **仍需表示法**。

🔒 **判別力自檢**（⛔ 期望值為**否定** ⇒ 尤須自檢·考古 76）：
**若**遠側界 ∥ `d̂`，它在 `s` 下即為單一值 ⇒ **可估** ⇒ 該判法**會給出不同結論** ⇒ ⛔ 非恆真句。

⇒ 🔴 **上列各數為<u>下界</u>**（只含族②③ 起算），⛔ **非完整影響**。

---

## §五 丙組：影響面（⛔ 只列·不評估難易·不建議作法·不排序）

| 餵入點 | 下游 |
|---|---|
| **①③ 選槽側** | `_select_pool_slot` → `k*` → **左右群之分割** → 各群之 `widths`／`ΣRw` → **落位順序與池槽位置** |
| **②④ 推進種子側** | `_solve_G_one(_W_prev=…)` → 該宗之 `W_far`／`Rw_pct` → **該宗之側街負擔** → `G` → **分配面積**；且 `W_far` **thread 到下一宗** |

🔴 ⇒ **一格改動會沿該側之整條鏈傳遞**，⛔ **非只影響第 1 宗**。
🔒 判別力自檢：以**必不命中**之下游字樣試 ⇒ **0** 行 ⇒ 上式非恆命中。

---

## §六 丁組：`GB-62` 方向③ ⇒ 🔴 **無人守**

🔒 **候選母體**（⛔ 正面列舉）：`verify/fixture_*.py` ＝ **13 檔**
（母體＝`git ls-tree -r <commit> verify/`·判準＝檔名符 `fixture_*.py`）；
其中**同時提及** `app.py` 與 `stepg` 者 ＝ **2**。

| 候選 | 受詞（🔴 關鍵：受詞是什麼，不是它叫什麼） | 判 |
|---|---|---|
| `verify/fixture_wf_ns_wiring.py` | **名單是否登錄**（`ns[<字面>]` 與清單取差集） | 🔴 **抓不到**——該改動**未增減任何 `ns[...]` 名**，只換了**餵進 dict 字面之運算式** |
| `run_verification.py` 之 `W-G G.1 接線層 ctx-builder 同源` | **ctx 之鍵值同源** | 🔴 **抓不到**——其受詞為**餵給引擎之 ctx**；A 之 `'b'` 生於 `main()` 自己的 Step-G 碼，**不經 `_build_wf_ctx`**。⚠️ 併：該閘現為**死閘** |
| `verify/fixture_n14_feed_chain.py` | **（本批未讀其斷言）** | ⚠️ **無從判定**（⛔ **非**判為抓不到） |

🔴 **結構性理由**（⛔ 非「剛好沒人寫」）：依 `CLAUDE.md`，**`main()` 內之敘述從不被 `run_all` 執行**
⇒ A 之 `'b'` **在執行期根本觀測不到** ⇒ 任何**動態**閘都不可能抓到
⇒ 唯一可能之守備是**靜態**（比對兩處運算式），而**現無此類夾具**。
🔒 判別力自檢：兩式字樣（`_left_buffer_S * _cos_dn`／`_b_L0`）於該 13 檔 ⇒ **各 0 檔命中**
⇒ ✅ 該否定判**有據**（⛔ 非憑印象）。

🔒 **射程聲明**：本判之受詞為「**有無機制其斷言涵蓋「A 與 B 對同一受詞是否餵同值」**」；
⛔ **不宣稱**「全倉無任何機制」。

---

## §七 `H1`–`H6` 逐項判 ＋ 停機

| # | 判 | 依據 |
|---|---|---|
| **H1** | ✅ **不破**（⛔ 但依據就地更正） | §一 |
| **H2** | ✅ | §二-1（**外部錨閘**過·附判別力自檢） |
| **H3** | ✅ | §二-2／二-3 |
| **H4** | ✅ | §二-4（兩義分碼·附判別力自檢） |
| **H5** | ✅ | §四（附判別力自檢） |
| **H6** | ✅ | §八-7 |

🛑 **`P-1` 未觸發**（定式導得出）。
🔴 **`P-2` ⛔ 不成立**——其閾為**面積**，而本批之差為**公尺／百分點**
⇒ **受詞不同、不可比大小**（考古 72）。⛔ **本批未取任何換算係數**，故該比較**不做**。
⛔ `P-3`／`P-4` 均未觸發。

---

## §八 ⛔ 本批未做之事（⛔ 逐項）

1. ⛔ **零生產碼**；⛔ 未改任何餵入點、⛔ 未改任何閘或容差。
2. ⛔ **未估族①**、⛔ 未取任何代表值、⛔ **未估上限**（已裁押後）。
3. ⛔ **未判「哪一條是對的」**——屬**域裁**（§白話稿）。
4. ⛔ **未把差換算為面積**（`P-2` 之比較因此不做）。
5. ⛔ **未讀** `verify/fixture_n14_feed_chain.py` **之斷言** ⇒ 已具名為**無從判定**。
6. ⛔ 未動 `docs/rulings/`／`CLAUDE.md`／`run_verification.py`／`wv_reconcile.py`／
   `baselines/`／既有夾具／三支複本；⛔ 未改期望 FAIL 名單、未重凍、未重烤。
7. ⛔ **未跑長跑**——`git diff --numstat f75af82 -- app.py verify/stepg_pipeline.py
   verify/run_verification.py verify/wv_reconcile.py verify/baselines/ docs/rulings/ CLAUDE.md`
   ⇒ **空輸出**；🔒 判別力自檢：同式對 `docs/reports/` ⇒ **非空** ⇒ 上式非恆空。
8. ⛔ **未重跑** `verify/out/probe_WG91_buf_metric_and_candidates.log` 之產生工具（驗收 #12）。
9. ⛔ 未追那 57 條、未追 `X5` 之 6 條、⛔ 未設計任何新機制（丁組**只列**）。

### §八-自誌 🩸 我把 `GB` 登記表的一列從中間切斷了——**被驗收 #13 當場攔下**

寫 `GB` 進度列時，我以「該列**尾段**之一句」為錨插入新列
⇒ 實際效果是**把 `W-G.9-21` 那一列剖成兩半**，其尾段被接到我兩列**之後**。

🔒 **攔下它的是「文件刪除欄 0」這條驗收**：`git diff --cached --numstat` 顯示 **`3 1`**
——**多出來的那個 `1`** 即被切斷之原列。已 `git checkout` 還原、改以**該列<u>行尾</u>之唯一字樣**
為錨重做 ⇒ 現為 **`2 0`**，diff hunk 為 `@@ -52,0 +53,2 @@`（**純插入**）。

🔒 **併加之獨立驗**（驗收 #12「每次寫入後以獨立指令驗其落地」）：
逐列**淨管線數**（總數扣除 `\|` 跳脫）——`↳` 列 **23** 列中，同表之列皆為 **8**
（`52`／`53`／`54` 皆 8）；`71`／`117` 為 **6** 係**另一張表**之列（⛔ 未觸及）。

⇒ 🔒 **教訓**：**以「一句話」為錨插入 Markdown 表格列時，該句若不在<u>行尾</u>，
就會把原列剖開**——而 Markdown 不會報錯，**表格照樣渲染得出來**。
⛔ **不得只看渲染結果**；⇒ 判準為 **`numstat` 之刪除欄** ＋ **逐列管線數**。

---

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。

---

## §白話稿 呈 KL（依 `重劃白話` 之呈送段）

> 🔴 **本稿全部數字為<u>下界</u>**——只算「負擔從哪裡起算」這一半，
> **另一半（地要排在哪裡）還算不出來**。⛔ 這**不是**一個「介於 X 到 Y 之間」的區間。

### 【現況】

**上次報告說「三個答案」。這次量出來：<u>三個都不完全對</u>，而且差很多。**

先講**正確答案是什麼**（這是規定書裡已經有的，不是我定的）：

| 情形 | 負擔應該從哪裡起算 | 出處（規定書·最高層級） |
|---|---|---|
| 街角有配到地 | **從側街道路中點（也就是 0）** | `K-9-5-12`（五） |
| 街角被強制列為抵費地 | **從那塊抵費地的遠側地界** | `K-9-5-12`（四） |

**現在程式裡三個答案，各差多少**（🔴 **下界**·單位分開列）：

```
   ┌─────────────┬────────────────────────┬────────────────────────┐
   │             │ 13 個「街角有配到地」  │ 3 個「強制抵費地」     │
   ├─────────────┼────────────────────────┼────────────────────────┤
   │ ① 排地時    │ ✅ 全對                │ 🔴 多算 1.62〜1.67 公尺 │
   │             │                        │    ＝ 8.6〜8.9 個百分點 │
   ├─────────────┼────────────────────────┼────────────────────────┤
   │ ② 選池位置  │ 🔴 15/16 格都偏        │ 🔶 兩格幾乎對（差 2 公分）│
   │             │    偏 0.16〜1.69 公尺  │ 🔴 一格多算 3.36 公尺   │
   ├─────────────┼────────────────────────┼────────────────────────┤
   │ ③ 實際算負擔│ ✅ 全對                │ 🔴 整整少算一整個起算點 │
   │             │                        │    ＝ 7.00 公尺／55.7 pp│
   └─────────────┴────────────────────────┴────────────────────────┘
   ⚠️ 公尺與百分點是兩種東西，⛔ 不能互相比大小。
   ⚠️ 「起算點」是一個位置（公尺）；「百分點」是負擔比率。二者不同類。
```

**離正確答案最遠的是③（實際算負擔那一步）**——在那三個街廓，它**根本沒有起算點**（用 0），
而正確答案是 **7 公尺**。

**還有一件**：上次施工單上寫的那個「`8.9` 個百分點」，**這次對上了**
——它是**①（排地時）**多算的量。⚠️ 我另外驗過：上次的候選算法給的是 `8.18〜9.02`，
**對不上**；這次的給 `8.64〜8.91`，**對得上**。**兩組會給出不同答案，所以這不是湊近的。**

### 【對土地的影響】

1. **這三個街廓的負擔會算錯**——③ 少算一整個起算點，那三筆的負擔會**偏低**。
2. **影響不只第 1 宗**——負擔是一筆接一筆傳下去的，**改一格會沿著那一側整排傳**。
3. ⛔ **「面積實際差多少」還是沒量到**——那要跑完整個流程，而它卡在第一個街廓。
   **⛔ 我不會給您一個猜的數字。**

### 【要你判斷】

> **三個答案要統一成哪一個？**

規定書上已經寫了正確答案（上表），所以**技術上三個都該改成那個**。
但**「改」會改變每一筆的配地面積** ⇒ 依既定順序，**要您點頭才動**。

**標記：真需地政專業判斷。**

> ## 🔧 **本題<u>撤回·⛔ 不呈 KL</u>（`W-G.9-24`·2026-08-13·⛔ 上文原句一字未刪）**
>
> 🔴 **上面這一題<u>不該問 KL</u>——目標<u>已經有答案了</u>。**
>
> `K-9-5-12`（五）-1 定「首宗起點**一律**為 `0`」、（四）定 `forced` 時**第 2 宗**
> 自抵費地遠側界起算——**二則皆在 `docs/rulings/`（權威序<u>第 1 級</u>）**。
> ⇒ 「**應該是什麼**」已定；剩下的只是**碼面三處與該答案不一致**
> ⇒ **屬實作偏差（技術問題）**，⛔ **非待裁之域問題**。
>
> 🔒 依 `VR-005`（`docs/驗證裁定登記表.md`·⚠️ **該檔⛔ 不在權威序四級之內**）。
> ⚠️ **本節上文之「【要你判斷】」一段自本加註起<u>失效</u>**，⛔ 原句不刪。
> **仍須 KL 點頭者 ＝ 「<u>要不要現在動手改</u>」**（因改動會變更配地面積），
> ⛔ **不是**「改成哪一個」。

### 【裁完之後會接著冒出來的問題】

1. 🔴 **改了之後配地面積會變，變多少現在還算不出來**——要先讓程式跑得過第一個街廓。
2. 🔴 **「只改一邊沒人看得到」這件事，這次確認了：<u>真的沒有任何檢查在管</u>。**
   原因是結構性的——您在畫面上操作的那一半程式，**驗證流程從來不會執行到**。
   ⚠️ 同類分歧**可能還有別處**，本批⛔ 未查。
3. ⚠️ 「地要排在哪裡」那一半**仍然算不出來**（要先解斜線的表示法），
   ⇒ 上面所有數字都是**下界**。
