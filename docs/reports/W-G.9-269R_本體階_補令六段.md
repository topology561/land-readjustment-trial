# `W-G.9-269R`　本體階　**補令六段**（`p3` 整項撤出 ／ `GB-162` ／ `自誤 358` ／ 病一開工）

> **落點 ＝ 主線**（補令六 `§七` 序 `1`·零生產碼·得逕推）。
> **基座** ＝ `8677e9617816ba03c9c9e18e45e32bbda9508279`（`ls-remote` 實查·與補令六 `§零` 相符·`left-right` ＝ `0	0`）。
> **單** ＝ `docs/orders/W-G.9-269_補令六_p3撤出與GB162與病一開工.md`（原封入倉·三端 `sha256` 逐位相同
> ＝ `d6cb7ae5696b00dab732e312367f3abb72b9333284360ddf96681c8902ee4199`·`9169` B·`CR 0`／`LF 109`）。
>
> 🔴 **本報告係主線之<u>第一份</u> `W-G.9-269` 本體階報告**——補令三〜五段之報告落於**側支**
> `verify/W-G.9-269-p3a` ＝ `05a11bd`（依補令六 `§二-2` 款 `2` **⛔ 併線**）⇒ **自主線觀之⛔ 可達**。
> 🛒 **本段⛔ 載**：病一之任何修法主張（其屬 `c1`〜`c3`）。

---

## §零　工項零 ＋ `S-0b`

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付） | `d6cb7ae5696b00dab732e312367f3abb72b9333284360ddf96681c8902ee4199` | `9169` | `0` | `109` |
| 落檔 | **同** | `9169` | `0` | `109` |
| **blob** | **同** | `9169` | — | — |

`S-0b`（`rc` **直取** ＝ `0`）：🔴 停機款 **`0`** ✅；🟡 `2` 項逐項處置——
`P-5` 本單⛔ 載 `SELF_SHA256` ⇒ **具名豁免**（三端 `sha256` 鏈承擔）；
`P-4` `:11` 之「`(a)(b)` ✅／`(c)` 🔴」係**判別力二造之逐款判**、⛔ 方向性轉引之量 ⇒ **偽陽·具名豁免**。

---

## §一　序 `1`：鑄號 —— 🔴 **一項簿態衝突已現並處置**

### 一-1　`GB-162`（**無衝突**）

**取號現查**（框 ＝ **標題形 ∪ 表列形**·母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔）

| `rev` | 相異 | MAX | 缺號 |
|---|---|---|---|
| 主線 `8677e96` | `159` | **`161`** | `[12, 87]` |
| 側支 `05a11bd` | `159` | **`161`** | `[12, 87]` |

🔒 **二 `rev` 之 `GB` 簿逐數相同**（側支∖主線 ＝ `∅`／主線∖側支 ＝ `∅`）⇒ **次號 `162` 於二 `rev` 皆成立**。

**意指占用**（母體 ＝ 各 `rev` 之全倉追蹤檔·**錨定框**·**二形並取**·`GB-158`）

| 號 | 主線 B／C 形 | 側支 B／C 形 | 角色 |
|---|---|---|---|
| **`GB-162`** | `0` 檔／`0` 列·`0`／`0` | `0`／`0`·`0`／`0` | 受詢 ⇒ **未占用** ✅ |
| `GB-161` | `37` 檔／`126` 列·`33`／`113` | `39`／`130`·`35`／`117` | 對照甲（須 `≥ 1`）✅ |
| ⟨NEG⟩（**執行期自倉掃出**·字面⛔ 出艙） | `0`／`0` | `0`／`0` | 對照乙 ✅ |

**鑄之機檢**：範本取既有最末一則之標題形（`常規四（九）四`）＝ `` ### `GB-161` 🆕　… ``；
形之自證 ⇒ 定義框掃 payload 命中 **`[162]`** ✅；**MAX `161 → 162`**／相異 `159 → 160`／
**缺號 `[12, 87]` 未變** ✅；`numstat` **`29	0`**／嚴格前綴 `True`／`CR 0`。

### 一-2　🔴 `自誤 358`：**主線簿態與單所載之號相異**（照實·⛔ 頂替）

**簿態之現查**

| `rev` | 相異 | MAX | **次號** | 缺號 |
|---|---|---|---|---|
| **主線 `8677e96`** | `347` | **`354`** | **`355`** | `[106]` |
| 側支 `05a11bd` | `350` | **`357`** | `358` | `[106]` |

🔒 **側支∖主線 ＝ `[355, 356, 357]`**／主線∖側支 ＝ `∅`。
⇒ 🔴 **單 `§六` 所授權之 `自誤 358` 係依<u>側支</u>之簿態算得；而本序之落點為<u>主線</u>，其次號為 `355`。**

**意指占用**（二 `rev` 並查）

| 號 | 主線 B／C 形 | 側支 B／C 形 | 判 |
|---|---|---|---|
| **`358`** | `0`／`0`·`0`／`0` | `0`／`0`·`0`／`0` | 受詢 ⇒ **二 `rev` 皆未占用** |
| `357` | **`0`／`0`** | `3` 檔／`8` 列·`3`／`8` | **已鑄於側支** |
| `356` | **`0`／`0`** | `3`／`6`·`3`／`6` | **已鑄於側支** |
| `355` | **`0`／`0`** | `3`／`9`·`3`／`9` | **已鑄於側支** |
| `354` | `3` 檔／`14` 列·`3`／`11` | `4`／`15`·`4`／`12` | 對照甲 ✅ |
| ⟨NEG⟩ | `0`／`0` | `0`／`0` | 對照乙 ✅ |

### 一-3　🔒 **所採之處置與其理由**（⛔ 自訂準據·僅適用既有二規之合取）

**二選項之後果（逐項出艙·⛔ 只出所採者）**

| 選項 | 後果 |
|---|---|
| **甲**：依主線簿態取 `355` | 🔴 **與側支已鑄之 `自誤 355` <u>撞號</u>**——同號二則、內容相異。**嚴格劣於乙。** |
| **乙**：依單取 `358` | 主線之缺號集增 `[355, 356, 357]`；該三號**已鑄於側支**，依 **`缺號永不重用`** ⇒ **⛔ 得重鑄** |

🔒 **⇒ 採乙**。其依據為**既有二規之合取**，⛔ 新訂準據：
`(1)` `CLAUDE.md`「**編號衝突一律以倉內為準**」——側支**亦在倉**（且補令六 `§二-2` 款 `2` 明令其**保留·⛔ 刪除**）
⇒ 該三號於倉內**確已占用**；
`(2)` 「**缺號永不重用**」——主線之缺號集記其為缺，即已阻斷重鑄。

🛑 **惟 `(2)` 之生效繫於「該缺口<u>被記錄</u>」**——若主線之簿對該三號**一字未提**，
後窗將無從分辨「缺號」與「記帳之誤」。⇒ **本批於自誤簿末端追加一節，逐號登記其
「已鑄於側支 `05a11bd`／⛔ 得於主線重鑄／其內容僅可自該側支取得」**（**事實之登記·⛔ 鑄號**）。

🔒 **`GB-132` 避讓之當場復算**：該登記節**⛔ 使 `355`／`356`／`357` 被任一框命中**
——立後之三號**仍在缺號集** ＝ `True` ✅（其節內之號一律以**中文數字**書之、各列⛔ 以 `#` 起首）。

**鑄之機檢**：形之自證 ⇒ 定義框掃 payload 命中 **`[358]`** ✅（器內置 `assert`·不符即停機⛔ 寫檔）；
**MAX `354 → 358`**／相異 `347 → 348`／**新增恰 `[358]`**／
**缺號 `[106] → [106, 355, 356, 357]`**（其增量即上開三號·已逐號登記）；
`numstat` **`48	0`**／嚴格前綴 `True`／`CR 0`。

🛑 **本節係<u>照實回報</u>**（`常規二`：取保守項續辦並逐字回報）——
**若發單側認主線應改取 `355` 或另有處置，本批之二 `commit` 皆 `append-only` 且可獨立回退。**

---

## §二　`§二`／`§四` 之立準（入交接註）

### 二-1　🔒 **立準（補令六 `§四` 逐字·⛔ 改寫）**

> **凡令「更新／重產／覆寫」一既有之物，須先出艙<u>該物於系統中之角色</u>：
> 其為「被比對之應然值」抑或「被產出之結果」？前者之覆寫即<u>抹去判準</u>，
> 須另備其不可逆性之處置（凍存／逐格歸因／解除條件），⛔ 以「它過期了」為由逕令重產。**

### 二-2　🔒 **立準（補令五 `§一-1` 逐字·本窗前段所承）**

> **凡以「某字樣於某檔命中 `0`」為閘，須以 <u>AST</u>（去 docstring／註解／字串常量）機械判，
> ⛔ 以字樣命中代之。** 判別力須同格出艙。

### 二-3　🔑 **交接註（本段新得·承側支各段之坑）**

| 坑 | 徵候 | 攔法 |
|---|---|---|
| `j` | **號之簿態隨 `ref` 而異** | 同一號於主線「未占用」而於側支「已鑄」——**單方 `rev` 之取號現查⛔ 足以判「未占用」** | 凡側支**⛔ 併線**而其上曾鑄號者，取號現查須**二 `rev` 並查**並出艙其**差集**；主線須**登記其缺口**，否則「缺號永不重用」之保護**⛔ 生效**（其繫於缺口被記錄） |
| `k` | **證據落於未併線之側支** | `GB-162` 之機械依據（三端對拍 `1137` 格）其落檔**全在側支** ⇒ 自主線 `git show` **⛔ 可達** | 凡登記項之證據落於未併線之側支者，**須於該登記內逐字附側支名與 `commit`**，⛔ 只寫落檔路徑 |
| `l` | **「重烤」之受詞係<u>應然值</u>** | `baseline` 為 `diff_rows` 之比較端；本分支之 `40` 紅其內容即該 `1137` 格 ⇒ 重烤 ＝ **一次抹去缺陷證據** | 見 `二-1` 之立準；併 `GB-162` 之三禁令 |

---

## §三　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | 空 |
| 生產碼 `34` 檔 blob（基座 → 本段期末） | **相異 `0`／`34`**（本段全為 `docs/**`） |
| 四簿 MAX | 自誤 **`354 → 358`**／`GB` **`161 → 162`**／`VR` `95` 未動／`K-9` `29` 未動——**皆經補令六 `§六` 逐號授權** |
| 自誤缺號 | `[106]` → **`[106, 355, 356, 357]`**（增量已逐號登記於簿末·`一-3`） |
| `GB` 缺號 | `[12, 87]` **未變** |
| `deletions` | 本段各 `commit` 皆 `0` |
| `verify/baselines` | **逐位未動**（`GB-162` 禁令 `(2)`） |
| `p3a` 側支 | `05a11bd` **保留·⛔ 併線·⛔ 刪除**；主線**⛔ 含 `p3a` 之任何物件** |

---

## §四　續辦

| 序 | 事 | 狀態 |
|---|---|---|
| `1` | 工項零 ＋ 鑄 `GB-162`／`自誤 358` ＋ 立準入交接註 | ✅ **辦畢**（`一-2`／`一-3` 之簿態衝突已照實回報） |
| `2` | **`c1`**（🔴 生產碼·遞補迴圈骨架 ＋ 旗標預設 `off` ⇒ 行為逐位不變） | ⬜ **次辦**·落**新側支** `verify/W-G.9-269-c1` |
| `3` | 發單側復驗 → 呈 KL → 放行 → 快轉併線 | ⬜ |
| `4` | `c2`（旗標 `on`·**有土地後果**·單獨呈 KL）→ `c3` → `c4` | ⬜ |

🔒 **`c1`〜`c4` 之規格悉依補令三 `§二`／`§三`／`§四`**，惟：
**`P-1` 之基準** ＝ 補令四 `§三-2` 之**單態器**所得（父態 `f7c833e`／**`34` 組·逐對總數 `0`**）
——🛑 **該器與其實測落於側支 `05a11bd`**（`verify/probes/probe_WG9269_coverage_single.py`）
⇒ **`c1` 之側支須自主線起，該器⛔ 在主線** ⇒ **其處置見 `§五`**；
**`S-10` 撤銷**；**⛔ 引任何自 baseline 取得之數**（`GB-162` 生效），差異表之數一律自**當次實跑**取得。

---

## §五　🛑 呈發單側（**二項**·⛔ CC 可裁）

| # | 項 | 受詞 |
|---|---|---|
| `1` | **`自誤 358` 之簿態衝突** | 已依 `一-3` 採**乙**（取 `358` ＋ 登記缺口）並照實回報。**若發單側認應另處置，二 `commit` 皆可獨立回退。** |
| `2` | 🔴 **`P-1` 之基準器落於⛔ 併線之側支** | 補令六 `§七` 令 `P-1` 之基準 ＝ 單態器所得，而 **`verify/probes/probe_WG9269_coverage_single.py` 僅存於側支 `05a11bd`**；`c1` 之側支若自**主線**起，該器**⛔ 在其樹內** ⇒ `c2` 屆時**無從施行 `P-1`**。**候發單側裁**：`(甲)` 將該器單獨 cherry-pick 至主線（**零生產碼·純新增檔**）／`(乙)` `c1` 側支自 `05a11bd` 起（🔴 將含 `p3a` ⇒ 與 `§二-2` 款 `2` 相斥）／`(丙)` 另指基準。 |

🛑 **`2` 之未決⛔ 阻 `c1`**——`c1` 之驗收為「旗標 `off` 下輸出逐位不變」，⛔ 用 `P-1`；
`P-1` 於 **`c2`** 方為受詞。⇒ **本窗即續辦 `c1`**，並俟裁再辦 `c2`。

🔒 **`GB-159` 仍有效**：二宗之配地圖與負擔表**⛔ 對外**。

*本段受詞 ＝ CC。⛔ 動 `app.py` 一字（本段）、⛔ 改 `verify/baselines/**` 一字、⛔ 併線 `p3a` 側支。*

---

# `W-G.9-269R`　**`c1` 段**（`K-9-23` 閘一之消費 ＋ `K-9-17` 遞補迴圈·`expand`·旗標預設 `off`）

> **本段⛔ 追改上文一字**（`append-only`）。
> **落點 ＝ 新側支 `verify/W-G.9-269-c1`**（自主線 `0aa608e` 起·`§七` 序 `2`·**⛔ 推主線**）。
> **`c1` commit ＝ `8f3dccc`**（🔴 生產碼·候發單側復驗 → 呈 KL → 放行 → 快轉併線）。
> **結論**：**驗收 ✅ 全部輸出逐位相同**（旗標 `off`）／**判別力 ✅ 輸出相異**（旗標 `on`·`13` 項）。
> 🔴 **併呈一項有土地後果之發現**（`§三-3`：`0m` 之**級聯剔除**）——其屬 `c2` 之受詞，**⛔ 本段判**。

---

## §一　受詞與其界（補令三 `§零-3`）

| 檔 | 受詞 | `numstat` |
|---|---|---|
| `app.py` | 模組層三名 ＋ `_WF_NS_NAMES` 補列 ＋ **站 `1／4`**（左鏈）**站 `2／4`**（右鏈） | **`94	0`** |
| `verify/stepg_pipeline.py` | **站 `3／4`**（左鏈）**站 `4／4`**（右鏈） | **`21	0`** |

🔒 **`deletions` 逐檔 ＝ `0`** ⇒ **`expand`（並存）段·⛔ 刪任何既有碼路徑** ✅
🔒 **⛔ 動 `§零-3` 之七符號內部一字**：`_oblique_s_max`／`_wg9268p_anchor_advance`／`_K923_EPS_ZERO`／
`_blue_shadow_tri`／`_k923_gate1`／`_k923_gate2`／`_lot_gate`——本批只改**其呼叫端之消費**。

| 量 | 改前 | 改後 |
|---|---|---|
| `app.py` bytes／`sha256` | `1458081`／`b74fd07c3c86a914…` | **`1464645`／`14c25f7a8cee4b75…`** |
| `verify/stepg_pipeline.py` bytes／`sha256` | `110206`／`1ada2cf2696ceb4a…` | **`111722`／`b8c4d748d7264038…`** |
| `CR` | `0` | **`0`**（二進位式·坑 `a`） |
| `py_compile` | — | `rc = 0`（**直取**） |

**反靜默退路計數器**：六站**皆咬到 `1` 處**（期 `1`）。
🩸 **照實**：模組層與 `_WF_NS_NAMES` 二處之「**退回 `1`**」係**插入式之構造必然**
（其 replacement 以 anchor 起首）⇒ ⛔ 靜默退路；**四站之「退回 `0`」方為替換式之判**。

---

## §二　設計（逐款繫其正典）

| 款 | 設計 | 正典 |
|---|---|---|
| 受詞 | **欄 `驗_B藍影`**（`_k923_gate1` 之判）——**⛔ `驗_rejected`**（其 ＝ `any(A,B,C)`·含射程外之 `K-9-12` ⇒ 以之為受詞將逾射程 `19` 宗） | 補令一 `§二-2`／補令三 `§零-2` |
| 四態 | **⛔ 併三**：只有逐字 `不合格` 者剔除；`合格`／`不適用`／**`無從判定`** 一律**留** | `_lg_fmt`；「**無從判定**」⛔ 與「判定為偽」共用出艙碼 |
| 遞補 | `cum_S` ⋀ `_lg_idx` **皆⛔ 推進** ⇒ 下一 `entry` **遞補其位**，其後各宗整體前移、`G` **全部重算** | `K-9-17` 二·四·**五**；🛑 **⛔ 依 `K-9-9 四` 字面之「重劃前投影順序」**（`K-9-17` 晚於 `K-9-9`） |
| 入池 | **⛔ 另設「入池」之碼**——本管線之池 ＝ 街廓幾何餘（`ΣG` 以外者）；該宗既不進 `g_rows`，其幾何**即歸池** ⇒ 構造之必然 | `K-9-11 三`（不配地 ＋ 騰出之地入調配池 ＋ **⛔ 超配**） |
| loud `1` | **街角第 `1` 宗遭剔除 ⇒ `raise`** | `GB-104`（「⛔ 得以『上游擋住了』為由不設」）——其為 loud 斷言，**⛔ 為其判準** |
| loud `2` | **末位遭剔除而⛔ 後繼可遞補 ⇒ `raise`**（候選耗盡） | `K-9-9 六` 逐字「必然產生配餘地……⛔ 會發生『塞不下』」；**⛔ 靜默兜底·⛔ 回 `None`** |

### 二-1　`ns` 接線（**其必要性**）

`verify/stepg_pipeline.py` 經 `ns[<字面>]` 消費該三名 ⇒ **須同列入 `app.py` 之 `_WF_NS_NAMES`**
（app 生產路徑自該清單建 `ns`·**漏列即 `KeyError`**）。

| 量 | 改前 | 改後 |
|---|---|---|
| 引擎消費 `ns[<字面>]` | `28` | **`31`** |
| `_WF_NS_NAMES` 宣告 | `33` | **`36`** |
| **引擎消費而清單漏列** | `['_wg9268p_anchor_advance']` | **`['_wg9268p_anchor_advance']`（未動）** |
| 本批三名**皆在清單** ⋀ **皆被引擎消費** | — | **`True`** ✅ |

🔒 **既有之漏列⛔ 本批受詞**——其為本窗**首份報告**已具名之現行紅（`W-G G.1`）；
**⛔ 順手修之**：修之即令 `G.1` 由紅轉綠 ⇒ **破壞本 `commit`「旗標 `off` 下逐位不變」之驗收**。
⇒ **維持原狀**，其 `G.1` 訊息**逐位不變** ✅。

---

## §三　驗收 ＋ 判別力（**三跑·同一工作樹路徑**·⛔ 路徑長度混淆）

🔒 **三跑皆於本 worktree 依序執行**（⛔ 用二 clone ⇒ ⛔ 坑 ㊲ 之「工作目錄名長度差 `1` byte 即令輸出差 `1` 字元」）。
態之切換以 `git checkout <rev> -- app.py verify/stepg_pipeline.py`，切後**逐跑前**驗 `app.py` 之 `sha256`。

### 三-1　🔒 **驗收（旗標 `off`）＝ 全部輸出與父 `commit` <u>逐位相同</u>**

| 受詞 | 父 `0aa608e` | 子 `8f3dccc`（`off`） | 判 |
|---|---|---|---|
| `stdout` | `168296` B／`34c942c3bd4975fb…` | `168296` B／`34c942c3bd4975fb…` | ✅ **逐位相同** |
| `stderr` | `0` B／`e3b0c44298fc1c14…` | `0` B／同 | ✅ |
| `rc` | `1` | `1` | ✅ |
| `got_*` 檔名集合 | `18` 檔 | `18` 檔 | ✅ 相同 |
| `got_*` **逐檔逐位** | — | — | ✅ **`18／18` 相同** |

🔒 **⛔ 出現 traceback 框行號之位移** ⇒ **⛔ 須援用** `W-G.9-268′` 補令四 `§一-2` 之自解款。

### 三-2　🔒 **判別力（旗標 `on`·同一 `commit`）＝ 輸出<u>相異</u>（證非死碼）**

| 受詞 | `off` | `on` | 判 |
|---|---|---|---|
| `stdout` | `168296` B／`34c942c3…` | **`161084` B／`7326891f…`** | 🔴 **相異** ✅ |
| `stderr` | `0` B | `0` B | 相同（⛔ 判準·其恆空） |
| `got_*` 相異檔數 | — | **`12／18`** | 🔴 **相異** ✅ |

**相異之 `got_*` 逐檔**：`got_G值_退縮{0m,3.5m}.csv`／`got_W-D.4_清單_{0m,3.5m}`／
`got_W-D.4_跨占_{0m,3.5m}`／`got_W-D.4_遞補_{0m,3.5m}`／`got_滑池槽診斷_{0m,3.5m}`／`got_逐槽J表_{0m,3.5m}`。
🔒 **合計相異 `13` 項 ⇒ 判別力成立**（`on` 之碼路徑確被執行·⛔ 死碼）。

### 三-3　🔴 **併呈（`c2` 之受詞·有土地後果·⛔ 本段判）：`0m` 之<u>級聯剔除</u>**

| 情境 | CSV 列 | **配地宗** | **消失之宗** | `off` 態閘一判不合格者 | 二集合相同 |
|---|---|---|---|---|---|
| `0m` | `59 → 56` | **`46 → 43`** | **`628-27(1)`／`628-42(1)`／`628-53(2)`（`3` 宗）** | `628-42(1)`／`628-53(2)`（`2` 宗） | 🔴 **否** |
| `3.5m` | `60 → 59` | **`46 → 45`** | `628-53(2)`（`1` 宗） | `628-53(2)`（`1` 宗） | ✅ 是 |

🔴 **`0m` 之 `628-27(1)` 於 `off` 態判「<u>合格</u>」**——其係**遞補後幾何前移**方轉為「不合格」而被剔除
⇒ **級聯剔除**（`cascade`）。
🔒 **迴圈<u>收斂</u>**：二情境之 `on` 態**皆⛔ 再有閘一判「不合格」者**（`∅`）。
🔒 **新增之宗 ＝ （無）**——本迴圈之「遞補」係**後繼者前移**，⛔ 外部候選進入；
其騰出之地依 `K-9-11 三` **入調配池**（構造之必然·見 `§二`）。

🛑 **⇒ `c2` 之土地後果<u>大於</u>補令三 `§零-1` 之受詞表（`0m` 二宗／`3.5m` 一宗）**
——`0m` 實為 **`3` 宗**。**本段⛔ 就此作任何修法主張**；其為 `c2` 之差異表所須逐宗載明者。

---

## §四　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | 空 |
| 生產碼 `34` 檔 blob（父 → `c1`） | **相異 `2`／`34`** ＝ `app.py`／`verify/stepg_pipeline.py`（**即本批之受詞**） |
| `deletions` | `app.py` `0`／`stepg_pipeline.py` `0`（**`expand`**） |
| 四簿 MAX | 自誤 `358`／`GB` `162`／`VR` `95`／`K-9` `29` —— **本段一號未鑄** ✅ |
| `verify/baselines` | **逐位未動**（`GB-162` 禁令 `(2)`） |
| 主線 | **⛔ 前進**（仍 `0aa608e`） |
| `p3a` 側支 | `05a11bd` 保留·⛔ 併線·⛔ 刪除 |

---

## §五　🛑 續辦

| 序 | 事 | 狀態 |
|---|---|---|
| `2` | **`c1`** | ✅ **備妥於側支 `verify/W-G.9-269-c1`**·驗收 ⋀ 判別力皆成立 |
| `3` | 發單側逐位元組復驗 → **呈 KL** → 放行 → 快轉併線 | 🛑 **候** |
| `4` | `c2`（旗標 `on`·🔴 **有土地後果**·**單獨**呈 KL·附差異表）→ `c3` → `c4` | ⬜ |

🛑 **`c2` 之二前置（本段具名·⛔ 本段辦）**
1. 🔴 **`§三-3` 之級聯剔除**：`0m` 之土地後果為 **`3` 宗**（⛔ 二宗）——差異表須逐宗載明，
   且 `628-27(1)` 之剔除**係遞補之後果**、⛔ 其自身於原位之判。
2. 🔴 **`P-1` 之基準器僅存於側支 `05a11bd`**（`verify/probes/probe_WG9269_coverage_single.py`）
   ——本 `c1` 側支自**主線**起 ⇒ **該器⛔ 在其樹內** ⇒ `c2` 屆時**無從施行 `P-1`**。
   **候發單側裁**（承補令六段 `§五` 項 `2`）：`(甲)` 將該器單獨 cherry-pick 至主線（**零生產碼·純新增檔**）／
   `(乙)` 另指基準。

🔒 **`GB-159` 仍有效**：二宗之配地圖與負擔表**⛔ 對外**。
🔒 **`GB-162` 生效**：⛔ 引任何自 baseline 取得之數；差異表之數一律自**當次實跑**取得。

*本段受詞 ＝ CC。⛔ 推主線、⛔ 翻旗標之預設（其為 `c2`·須 KL 單獨放行）、⛔ 改 `verify/baselines/**` 一字。*

---

# `W-G.9-269R`　**補令七段**（`355`–`357` 之內容補鑄 ／ `P-1` 基準器之取入 ／ 🔴 一項量測器缺陷）

> **本段⛔ 追改上文一字**（`append-only`）。
> **落點 ＝ `c1` 側支 `verify/W-G.9-269-c1`**（補令七 `§五`：主線待併期間⛔ 前進·補令十五 `§四-2`）。
> **基座** ＝ `5bd0868aeddb2d36f01a6cfba3937f999fe754dc`（`ls-remote` 實查·與補令七 `§零` 相符）。
> **單** ＝ `docs/orders/W-G.9-269_補令七_c1核可與級聯剔除.md`（原封入倉·三端 `sha256` 逐位相同
> ＝ `e7fbdd54406764867c09e848487dce63213a22f803cb0e6d8c2b7bf300efd8e4`·`8869` B·`CR 0`／`LF 117`）。
> **結論**：序 `1` **全項辦畢**；🔴 **併呈一項<u>量測器缺陷</u>**（`§三-3`）——其**⛔ 影響本批之任何數**，
> 惟**defeats `P-1` 之明文目的** ⇒ **停機上呈其處置**（⛔ 本批修之·其修將破壞 `§三` 之逐位取入要求）。

---

## §零　工項零 ＋ `S-0b`

三端 `sha256` **逐位相同** ＝ `e7fbdd5440676486…`（`8869` B·`CR 0`／`LF 117`）。
`S-0b`（`rc` **直取** ＝ `0`）：🔴 停機款 **`0`** ✅；🟡 `3` 項**逐項處置**——

| 項 | 處置 |
|---|---|
| `P-5` 本單⛔ 載 `SELF_SHA256` | **具名豁免**（三端 `sha256` 鏈承擔） |
| `P-4` `:66`「`46 → 43`」 | 🔒 **採納**——其**座標系 ＝ 旗標 `off` → `on`**（本窗 `c1` 段 `§三-3` 已逐處具名） |
| `P-6` `:55` `P-1` 器之「純新增檔·零生產碼」 | 🔒 **採納**——其**實測出處 ＝ 本窗 `c1` 段 `§五` 項 `2`**（其為 CC 所呈之候裁項） |

---

## §一　`§二`：`355`–`357` 之**內容補鑄**為 `359`–`361`

**取號現查**（框 ＝ **標題形 ∪ 表列形**·母體 ＝ 自誤簿單檔）：期初 主線 MAX **`358`**·缺號 `[106, 355, 356, 357]`
⇒ 次三號 ＝ **`359`／`360`／`361`**。

| 新號 | 承載之凍存原號 | 受詞（首句） |
|---|---|---|
| **`359`** | `自誤 355` | 以機制之「存在」充其「適用」之證 |
| **`360`** | `自誤 356` | 指定一其第二態已被本線前批消滅之二態器為閘之基準 |
| **`361`** | `自誤 357` | 立「跨器之逐位對拍」為閘，而未先證二器之序列化約定相同 |

🔒 **內容<u>逐字轉錄</u>**自凍存側支 `05a11bd`（⛔ 改寫·⛔ 摘要）；
**標題形取自原則**（`常規四（九）四`）、**僅換其號**；每則**首列逐字載其對應之凍存原號**。
🛑 **⛔ cherry-pick 凍存側支之任何 commit**——其係 `GB-162` 之證據，物件須保持原狀（單 `§二-2` 款 `4`）。

**鑄之機檢（二項自證·器內置 `assert`·不符即停機⛔ 寫檔）**

| 自證 | 結果 |
|---|---|
| **形之自證**：定義框掃 payload | 命中 **`[359, 360, 361]`** ✅ |
| **逐字轉錄之自證**：三則之 `body` 須為**凍存原文之逐位子字串** | **✅ 三則皆是** |
| MAX 之推進 | `358 → 361`／相異 `348 → 351`／**新增恰 `{359, 360, 361}`** ✅ |
| 🔒 **缺號登記未被污染** | `355`／`356`／`357` **仍在缺號集** ＝ **`True`** ✅（⇒ ⛔ 追改主線既有之缺號登記一字·其為真） |
| `numstat` | **`103	0`**／嚴格前綴 `True`／`CR 0` |

---

## §二　`§三`：`P-1` 基準器之**取入**

**辦法** ＝ `git checkout 05a11bd… -- verify/probes/probe_WG9269_coverage_single.py`
（**⛔ cherry-pick 該側支之 commit**·理由同 `§二-2` 款 `4`）。

| 端 | blob `sha1` | file `sha256` | bytes |
|---|---|---|---|
| 來源（凍存側支 `05a11bd`） | `07d8627e5e4c797f1278999e40f306acce8c01f6` | `73aaa3b1a49ac1d668e76b133623b7a5360cf2b274249c1a591c29e19e265a7e` | `12000` |
| **取入之檔** | **同** | **同** | `12000` |

🔒 **二端逐位相同** ✅（`CR 0`）；`numstat` **`243	0`**（純新增檔）。
🔒 **來源之具名**（單 `§三` 所令）：側支 **`verify/W-G.9-269-p3a`** ＝ **`05a11bd665f6c2790ff821c9e3a1606651e345d4`**。

### 二-1　取入後之重跑（單 `§三` 之三條件）

| 跑 | 工作區 `app.py`（**啟動前實查**） | 三造 | 母體 | 逐對總數 | `rc`／`stderr` |
|---|---|---|---|---|---|
| `c1` 側支態 | `c5c28b826aa25f4f…` | ✅ 全成立 | **`34`** 組 | **`0`** | `0`／`0` B |
| **主線態**（單所令） | **`1d7d7c7324719f13…`**（＝ 主線 `0aa608e` 之值·切換後當場 `git hash-object` 實查） | ✅ 全成立 | **`34`** 組 | **`0`** | `0`／`0` B |

🔒 **⇒ 單 `§三` 之三條件<u>皆成立</u>**：人造三造全成立 ⋀ `34` 組 ⋀ 逐對總數 `0`
（＝ 補令四 `§三-2` 之**父態基準** `f7c833e`／`34` 組／`0` 對）✅
🔒 **切態之法**：`git checkout <rev> -- app.py verify/stepg_pipeline.py`，跑畢**還原**
（還原後 `git hash-object app.py` ＝ `c5c28b826aa25f4f…` 當場實查）。

---

## §三　🔴 **併呈：一項量測器缺陷**（`⛔ 本批修`·停機上呈其處置）

### 三-1　所見

**主線態**之跑，其工作區 `app.py` **啟動前實查為 `1d7d7c7324719f13…`**（主線之值），
**而該器之態宣告逐字印出**：

> `app.py` blob `sha1` ＝ `c5c28b826aa25f4f12a3bd5bdc1fcd0587ab021f`

——**即 `c1` 之 committed blob**，⛔ 其實際執行之工作區檔。

### 三-2　成因（**碼面·機械·⛔ 推定**）

`verify/probes/probe_WG9269_coverage_single.py` 之 `_state()` 逐字：

```
        r = subprocess.run(["git", "-C", REPO] + list(a), …)
    return g("rev-parse", "HEAD"), g("rev-parse", "HEAD:app.py"), g("status", "--porcelain")
```

🔒 **`git rev-parse HEAD:app.py` 取之為 `HEAD` 之<u>已 commit</u> blob**，
**⛔ 工作區之檔**；而 `harvest()` 所讀、所執行者**恰為工作區之檔**。
⇒ 🔴 **該器之態宣告對「工作區之改動」<u>結構上失明</u>**。

**判別力（當場）**：於 `c1` 態二者相同（`c5c28b82…`）；於主線態二者**相異**
（`HEAD:app.py` `c5c28b82…` vs 工作區 `1d7d7c73…`）⇒ **該檢非恆同**。

### 三-3　🛑 **其後果之界（照實·⛔ 擴大亦⛔ 縮小）**

| 受詞 | 判 |
|---|---|
| 本批之**覆蓋帳數**（`34` 組／`0` 對） | 🟢 **⛔ 受影響**——其計算全經 `harvest()` 讀**工作區**，與 `_state()` 無涉 |
| 本批之**三造判別力** | 🟢 **⛔ 受影響**（其為人造幾何·與態無涉） |
| 單 `§三` 之三條件 | 🟢 **皆成立**（見 `§二-1`） |
| 🔴 **`P-1` 之<u>明文目的</u>** | 🔴 **被 defeat**——補令四 `§三-2` 逐字：「二次量測須出艙其各自之 `commit` 與 `app.py` 之 blob `sha1`，**以證二者確為不同態**」；而該器所出之 `sha1` **⛔ 隨工作區而變** ⇒ **證不出「二者確為不同態」** |

🛑 **⇒ 於 `c2` 屆時**（`P-1` ＝ 以同一單態器於 `c2` 之**父 `commit`** 與 `c2` **各量一次**），
若二次量測以 `git checkout -- <檔>` 切態而**⛔ 各自 commit**，**該器將二次皆報同一 `sha1`**
⇒ **`P-1` 之「二者確為不同態」之自證失效**。

### 三-4　🛑 **本批⛔ 修之**（⛔ 自解·具名其由）

🔒 **單 `§三` 明令**：「**取入之檔與凍存側支之同名檔 blob `sha1` ⋀ file `sha256` 逐位相同**」
⇒ **編修該器即破壞該要求** ⇒ **⛔ 本批修之**。
🔒 併：`常規五` 自解款款 `4`（可逆）逐字「**⛔ 編修任何既有檔之既有一字**」。
🛑 **⇒ 停機上呈其處置**（見 `§五`）。

---

## §四　`§一-4` 之立準（入交接註）

### 四-1　🔒 **立準（補令七 `§一-4` 逐字·⛔ 改寫）**

> **凡於一「行為須逐位不變」之 `commit` 內發現<u>與本受詞無涉</u>之既有瑕，一律<u>具名而不修</u>**
> ——修之即破壞該 `commit` 之驗收，且使「本次改動之效」與「順手之效」不可分。其修另立工項。

🔒 **本窗之實例（`c1` 段 `§二-1`）**：`_wg9268p_anchor_advance` 於 `_WF_NS_NAMES` 之既有漏列
**具名而未修**——修之即令 `W-G G.1` 由紅轉綠，**破壞 `c1`「旗標 `off` 下輸出逐位不變」之驗收**。

### 四-2　🔑 **交接註（本段新得）**

| 坑 | 徵候 | 攔法 |
|---|---|---|
| `m` | **量測器之態宣告取 `HEAD` 之 blob 而⛔ 工作區** | 以 `git checkout <rev> -- <檔>` 切態者，其 `git rev-parse HEAD:<檔>` **恆不變** ⇒ 二次量測之態宣告**相同**，而其實際執行之碼**相異**——**⛔ 任何錯誤訊息** | 態宣告須**二值並報**：`git rev-parse HEAD:<檔>`（**committed**）⋀ `git hash-object <檔>`（**工作區·即實際執行者**）；**以後者為實**。凡「以證二者確為不同態」之自證，一律綁**後者** |

---

## §五　自限復驗 ＋ 呈發單側

### 五-1　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | 空（本段落檔皆逐檔 `git add`） |
| 生產碼 `34` 檔 blob（`5bd0868` → 本段期末） | **相異 `0`／`34`** ⇒ **`c1` 之二檔 blob 仍為 `8f3dccc` 之內容** ✅（單 `§五` 之主檢） |
| 四簿 MAX | 自誤 **`358 → 361`**（本段**唯三**之鑄·經 `§六` 逐號授權）／`GB` `162`・`VR` `95`・`K-9` `29` **皆未動** |
| 自誤缺號 | `[106, 355, 356, 357]` **未變** ✅ |
| `deletions` | 本段各 `commit` 皆 `0` |
| `verify/baselines` | **逐位未動**（`GB-162` 禁令 `(2)`） |
| 主線 | **⛔ 前進**（仍 `0aa608e`） |
| 凍存側支 | `05a11bd` **未動**（本段只 `git checkout <其物件> -- <一檔>`·⛔ cherry-pick 其 commit） |

### 五-2　🛑 呈發單側（**一項**·⛔ CC 可裁）

> 🔴 **`P-1` 基準器之態宣告缺陷**（`§三`）——其 `_state()` 取 `HEAD` 之 committed blob，
> ⛔ 工作區 ⇒ **`P-1` 之「以證二者確為不同態」之自證於 `c2` 將失效**。
> 🛑 **本批⛔ 修之**：單 `§三` 令「取入之檔與凍存側支之同名檔逐位相同」，編修即破壞之。

**候發單側裁**（本窗**⛔ 作建議之外之任何主張**）：
`(甲)` 授權於**另一 commit** 修該器（**零生產碼**·其後該檔即⛔ 再與凍存側支逐位相同——須明文豁免 `§三` 之該款）；
`(乙)` `c2` 之 `P-1` 二次量測**各自 `commit`**（則 `HEAD:app.py` 隨之而異 ⇒ 現行 `_state()` 即足）；
`(丙)` 於 `c2` 之報告內**另行出艙** `git hash-object app.py` 二值，以補其自證（**⛔ 動該器一字**）。

### 五-3　續辦

| 序 | 事 | 狀態 |
|---|---|---|
| `1` | 工項零 ＋ `§二` 補鑄 ＋ `§三` 取入並重跑 ＋ `§一-4` 立準 | ✅ **全項辦畢** |
| `2` | 發單側**重驗** → **KL 放行** → 快轉併線 | 🛑 **候** |
| `3` | `c2`（🔴 旗標 `on`·**有土地後果**·**單獨呈 KL** ＋ `§四-2` 之差異表） | ⬜ |
| `4` | `c3`（contract）→ `c4` | ⬜ |

🛑 **`c2` 之三前置（本段彙整·⛔ 本段辦）**
1. 🔴 **級聯剔除**：`0m` 實為 **`3` 宗**（含 `628-27(1)`·其原分配 `G ＝ 29.26 ㎡`·較前二宗**大二個數量級**）
   ——差異表之「處置」欄須區分 **`閘一直接剔除`** 與 **`級聯剔除`** 二類（單 `§四-2`）。
2. 🔴 **`P-1` 之態宣告缺陷**（`§五-2`）。
3. `628-27(1)` 之**歸戶須於管線跑畢之態取之**（單 `§四-2` 款 `2`：`harvest()` 時為空）。

🔒 **`GB-162` 生效**：⛔ 引任何自 baseline 取得之數——差異表之數一律自**當次實跑**取得。
🔒 **`GB-159` 仍有效**：**三宗**之配地圖與負擔表**⛔ 對外**。

*本段受詞 ＝ CC。⛔ 推主線、⛔ 翻旗標之預設、⛔ 改 `verify/baselines/**` 一字、⛔ 動取入之器一字。*

---

# 🔑 `W-G.9-269` 本波　**交接註 ＋ 起手清單**（本窗收工令 項 `2`·⛔ 上文一字不刪·純末端追加）

> 🛑 **本節之受詞 ＝ 下一窗**（`c2` 之施工窗）。逐坑載 **形／徵候／攔法**，**⛔ 只寫結論**。
> 🔒 所列**皆為本窗親踩**（或親自事前攔下）者，**⛔ 臆測之風險清單**。
> 🔒 **只寫本波新得之坑**；`W-G.9-268′` 之**坑 `1`〜`26` 仍全效**
> （其索引見 `docs/reports/W-G.9-268p_結案報告.md` `§五`）。
> 🔒 本波前段之坑 `a`〜`i` 見 `docs/reports/W-G.9-269R_前置階.md` 補令二段 `§丁`。

---

## §甲　交接註（本波新得·坑 `n`〜`z`）

### 坑 `n`　🔴 `WV_BAKE` **之三害**（⛔ 只知其一）

- **形**：`WV_BAKE=<dir>` 掛於 `diff_rows` **之入口**（`verify/run_verification.py:417-421`），
  其分支逐字 `return True, []`。
- **徵候（三者·⛔ 只記其一）**
  `(1)` 🔴 **控制流改變**——`diff_rows` **一律回綠** ⇒ 凡以其結果為前提之下游守衛**皆不再中止**
  　⇒ 管線走得**遠於正常跑**：名目 **`64 → 123`**（綠 `24 → 105`／紅 `40 → 18`）
  　⇒ **所烤者含該 `+59` 段所產之列**，**⛔ 正常跑之真值**。
  `(2)` 🔴 **覆寫集為 `51` 檔**，**⛔ 九份**——其 ＝ 該次執行**所實際到達之每一個 `diff_rows` 呼叫點**之 baseline。
  `(3)` 🔴 **將真實資料覆寫為 `0` B**——`wf/f0/F.0_合併決策_退縮{0m,3.5m}.csv`
  　（`947`／`964` B ⇒ **`0` B**·其現行內容為 `G006 R3 級0'` 之合併決策列）；
  　成因 ＝ 其呼叫點**被到達**（故入覆寫集）而 `got_rows` **為空**（`F.0` 於現態失敗）。
- **攔法**：`(a)` **⛔ 執行 `WV_BAKE=BASELINES`**（`GB-162` 禁令 `(1)`·**永久**）；
  `(b)` 側烤一律 `WV_BAKE=<拋棄式 side dir>`（**⛔ `verify/baselines`**）；
  `(c)` 受詞集**逐檔具名**並出艙「其餘 N 檔⛔ 觸一位元組」；
  `(d)` **逐檔驗「非空」**（bytes `> 0` ⋀ 資料列 `> 0`）方得複製。

### 坑 `o`　🔴 **`baseline` ＝ <u>應然值</u>，⛔ 會過期之快照**

- **形**：`run_verification` 以 `diff_rows(got_rows, baseline_path, …)` 取 baseline 為**比較端**；
  本分支現行之 **`40` 個紅，其內容即 `verify/baselines` 與現行輸出之 `1137` 格差**。
- **徵候**：🔴 **「它過期了 ⇒ 重烤」讀起來完全合理**——而重烤 ＝ **一次抹去該 `1137` 格之缺陷證據**。
  其**⛔ 任何閘會紅**：重烤後全綠，且**看起來像修好了**。
- **攔法**：`(1)` **`GB-162` 之三禁令**；
  `(2)` **立準（補令六 `§四`·逐字）**：
  > **凡令「更新／重產／覆寫」一既有之物，須先出艙<u>該物於系統中之角色</u>：
  > 其為「被比對之應然值」抑或「被產出之結果」？前者之覆寫即<u>抹去判準</u>，
  > 須另備其不可逆性之處置（凍存／逐格歸因／解除條件），⛔ 以「它過期了」為由逕令重產。**

### 坑 `p`　🔴 **跨器之逐位對拍，須先證二器之<u>序列化約定</u>相同**

- **形**：立「側烤之檔 ＝ 正常跑之同名 `got_*` **逐位相同**」為閘，而二者**分由二器所寫**。
- **徵候**：🔴 **`0／11` 全不過**，而其不成立**恆由寫檔器所致**、⛔ 所烤之值有異
  ——**「全不過」讀起來像「全錯了」**。

| 器 | 行尾 | 欄序 |
|---|---|---|
| `_bake_csv`（`:333`／`:348-349`） | `lineterminator="\n"` ⇒ **LF-only** | `base_col_order`（**baseline 欄序**） |
| `_dump_csv`（`:320`／`:327`） | `open(newline="")` ＋ `csv` **預設** ⇒ **CRLF** | `rows[0].keys()` 之序 |

（**欄集相同**·差集雙向皆空·如 `診斷0m` `24` 欄）
- **攔法**：`(1)` 立「二產物逐位對拍」之閘前，**先出艙二者之序列化約定**（行尾／編碼／BOM／欄序／引號／浮點格式）並證其相同；
  `(2)` 相異者改以**正規化後逐格**對拍，而**正規化<u>只得</u>處理**：行尾轉 `\n`／去 BOM／**欄依欄名排序**
  ——🛑 **⛔ 轉浮點、⛔ 任何數值容差、⛔ 任何 trim／casefold**（逾此即為「處理值」＝ 放寬）；
  `(3)` 附**硬判別力二造**：一格注入**單一字元**須轉紅／同檔未注入須綠。**二造同色 ⇒ 閘失能**。

### 坑 `q`　🔴 **「某字樣命中 `0`」為閘者，須以 <u>AST 去 docstring</u> 判**

- **形**：以字樣框（`grep`／`str.count`）判「碼面⛔ 觸某物」。
- **徵候**：🔴 **本窗實測五受詞於字樣框<u>皆命中 `1`</u>，而<u>全在 docstring</u>**
  （`os.environ`／旗標 `WG9268P_ANCHOR_GEOM`／`assert_live`／`_partial`／`csv`）
  ⇒ **以字樣命中為閘，必將該器誤判為違反其四款**。
- **攔法**：以 **AST**（去 docstring／註解／字串常量）機械判；**判別力須同格出艙**
  （本窗 ＝ 可執行敘述內 `Call` 節點 **`114`**／`pairs_of` 之 `Name` **`4`**·皆須 `> 0`）。
- 🔒 **本波二度用之**：`_dump_csv` 之計數——**字樣框 `16` ＝ AST 呼叫點 `15` ＋ 定義 `1`**。

### 坑 `r`　🔴 **態宣告須<u>二值並報</u>**（`committed` ⋀ **工作區**）

- **形**：量測器以 `git rev-parse HEAD:<檔>` 宣告其所量之態。
- **徵候**：🔴 **以 `git checkout <rev> -- <檔>` 切態者，`HEAD:<檔>` <u>恆不變</u>**
  ⇒ **二次量測之態宣告相同，而其實際執行之碼相異**——**⛔ 任何錯誤訊息**。
  本窗實見：主線態之跑其工作區 `app.py` 實為 `1d7d7c7324719f13…`，
  而該器宣告 `c5c28b826aa25f4f…`（`c1` 之 committed blob）。
- **攔法**：態宣告**二值並報**——`git rev-parse HEAD:<檔>`（**committed**）
  ⋀ `git hash-object <檔>`（**工作區·即實際執行者**），**以後者為實**；
  **二值相異即「工作區與 `HEAD` 不同步」之告警**，須同格具名。
- 🔒 **其直接後果**：`P-1`（補令四 `§三-2`）之「**以證二者確為不同態**」之自證，
  於「二次量測⛔ 各自 `commit`」之作法下**失效**。其處置**候發單側**（補令七段 `§五-2`）。

### 坑 `s`　🔴 **簿態隨 `ref` 而異 ⇒ 取號須二 `ref` 並查**

- **形**：某側支**⛔ 併線**而其上曾鑄號 ⇒ 該號於主線「未占用」、於側支「已鑄」。
- **徵候**：本窗實測——`自誤 355`／`356`／`357` 於**主線** B／C 形**皆 `0`／`0`**，
  於**側支 `05a11bd`** 各 **`3` 檔／`6`–`9` 列**；簿態 主線 MAX `354` vs 側支 `357`。
  ⇒ **單方 `rev` 之取號現查⛔ 足以判「未占用」**。
- **攔法**：`(1)` 取號現查**二 `ref` 並查**並出艙其**差集**；
  `(2)` 取「下一個於**二 `ref` 皆未占用**」之號（本窗 ＝ `358`；取 `355` 將**撞號**、嚴格劣）；
  `(3)` **缺號永不重用**——惟其保護**繫於缺口被記錄** ⇒ **須於簿末逐號登記其歸屬**
  　（本窗已登記 `355`–`357` 之「已鑄於側支 `05a11bd`／⛔ 得於主線重鑄／其內容僅可自該側支取得」）；
  `(4)` 該登記節之形須**避 `GB-132`**（各列⛔ 以 `#` 起首·號以**中文數字**書之），
  　並**當場復算**其⛔ 使該三號被任一框命中。

### 坑 `t`　🔒 **「行為須逐位不變」之 `commit` 內發現<u>無涉</u>之既有瑕 ⇒ 具名而不修**

- **形**：於 `expand` 段（驗收 ＝ 逐位不變）內順手修一個與本受詞無涉之既有缺陷。
- **徵候**：🔴 **修之即破壞該 `commit` 之驗收**，且使「本次改動之效」與「順手之效」**不可分**。
  本窗實例：`_wg9268p_anchor_advance` 於 `_WF_NS_NAMES` 之**既有漏列**
  ——修之即令 `W-G G.1` 由紅轉綠 ⇒ `c1` 之「旗標 `off` 下輸出逐位不變」**必不過**。
- **攔法**：**具名而不修**；其修**另立工項**。

### 坑 `u`　🔴 **對照組全零 ⇒ 先判量測器紅**

- **形**：某量之對照組於**全部**受詞皆得 `0`。
- **徵候**：本窗實見「**錨之效**（`B vs C`）於**全 `11` 檔皆 `0`」——
  其可能為 `(a)` 受詞之實，亦可能為 `(b)` **量測器停在零**；**二者之徵候相同**。
- **攔法**：**先跑兩造自證**方得出艙該 `0`——
  本窗 ＝ 造乙（於 `B` 之一格**注入單一字元**）得 **`1`** ⋀ 造丙（`B vs` **現行 baseline**）得 **`264`**
  ⇒ **器非紅** ⇒ 該 `0` 方得出艙。

### 坑 `v`　🔴 **級聯剔除**：`K-9-17` 之遞補**使原本合格之宗轉為不合格**

- **形**：`K-9-17 五`（「後面的宗也會整體往前分配，所以也要重算 `G`」）之**必然**——
  一宗被剔除後，其**後繼者整體前移**，其幾何隨之改變 ⇒ 閘一**重判**。
- **徵候**：🔴 **旗標 `on` 後 `0m` 消失 `3` 宗，而 `off` 態閘一判不合格者僅 `2` 宗**。

| 宗地 | `a 面積(㎡)` | 原分配 `G(㎡)` | 宗地寬度(m) | 閘一（`off` 態） | 何以消失 |
|---|---|---|---|---|---|
| `628-42(1)` | `8.34` | `0.23` | `0.18` | **不合格** | 直接剔除 |
| `628-53(2)` | `2.78` | `0.00` | `0.02` | **不合格** | 直接剔除 |
| **`628-27(1)`** | **`51.32`** | **`29.26`** | `0.65` | **不適用** | 🔴 **級聯剔除** |

🔴 **其量級**：`628-27(1)` 之原分配較前二宗**大二個數量級**。
- **攔法**：`(1)` **⛔ 以「受詞二宗」呈 KL**——`0m` 實為 **`3` 宗**；
  `(2)` 差異表之「處置」欄須區分 **`閘一直接剔除`** 與 **`級聯剔除`** 二類，**⛔ 併為一類**；
  `(3)` `表三` 之「改列調配池」須**逐戶具名**且**級聯剔除者單獨小計**；
  `(4)` 終態須驗**⛔ 再有閘一不合格者**（＝ 迴圈收斂·本窗實測二情境皆 `∅`）。

### 坑 `w`　🟡 **本波其餘自捕**（逐則 形／徵候／攔法·從簡）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `w-1` | **`_dump_csv` 呼叫點之計數三值相異** | 單載 `16`／本窗前段載 `14` ⇒ 二數相異而**皆非呼叫點之真值** | **AST 實查**：呼叫點 `15` ＋ 定義 `1` ＝ 字樣框 `16` ⇒ 先對齊母體再判孰誤（坑 `d`），⛔ 逕判他方有誤 |
| `w-2` | **判別力造之注入矩形錨於<u>外接框角</u>** | `tgt.bounds` 之左下角**⛔ 必在多邊形內**（宗地多為斜交四邊形）⇒ 交集恆 `0.000000` ⇒ 造三必不成立，而其紅係**器之紅**、⛔ 受詞之紅 | 以 `buffer(-0.75)` 取**確在內部**之區，於其 `representative_point()` 為心造邊長 `1.0` 之方（心到角 `√0.5 ≈ 0.707 < 0.75`）⇒ 重疊**恰 `1.0㎡`**（**構造保證**·⛔ 試誤）；並取組內**面積最大**者（⛔ 取首筆——`G ≈ 0` 之退化宗容不下該方） |
| `w-3` | **`git ls-files` 之 CJK `quotepath`** | `git ls-files <dir> \| grep 四梯` 得**偽 `0`**——CJK 檔名被轉義為八進位 | 一律 `git ls-files -z` ＋ Python 解碼；重查得 **`10`** 檔。對照組：`v3/` 下 `68` 檔（須非零） |
| `w-4` | **違規列之顯示截斷** | 「…（**另 `81` 列未顯示**·本閘違規總計 `93` 列）」⇒ `U-K2` 之逐字 violation **不在可見區** | **照實具名**「所據者為<u>總計數</u>、⛔ 其逐字」，並改以**碼面機械證**承擔歸因（`declared_extras_for` 之消費點恰 `1` ⋀ `U-K2` 每次呼叫至多 `+1` ⋀ 二鍵 × 二呼叫點 ＝ `4`） |
| `w-5` | **讀長跑之 `rc` 檔<u>於其寫入之際</u>** | 同一檔二讀二值（首讀 `RC=2`／`.log` `0` B；再讀 `RC=0`／`4023` B） | `rc` 與其 log **同格復讀並比其時戳**；`0` B 之 `stdout` **⛔ 逕讀為「跑了而無輸出」**（坑 `20`） |
| `w-6` | **heredoc 三度誤用** | `wg9237_heredoc_guard` **硬阻斷**（本波 `3` 次·⛔ 靜默） | ⛔ 用 heredoc；長字串一律 `Write` 落檔 ＋ 以**路徑**呼叫；多行訊息用 `printf '%s\n'`。🔒 **該三次即「活體攔截實測」之證**（⛔ 只 `--selftest`） |
| `w-7` | **以「新欄 ＝ `0`」逕認 `驗丙` 過** | `驗丙` 之三維（新欄／缺列多列／值異動）只辦其一，而**其一之綠讀起來像全過** | 凡多維之閘，**逐維出艙其判**；未辦之維**自陳「⛔ 作成」**，⛔ 令其沉默 |
| `w-8` | **判別力造選到一個恰為零之檔** | 取 `第 1 宗街角地指配結果_退縮0m.csv` 為「錨之效非零」之對照，實測得 `0` ⇒ **該檔⛔ 具判別力** | 改以**注入**造（必紅）＋**跨端**造（`B vs A`·必紅）為對照；⛔ 以一個恰好為零之實測充判別力 |

### 坑 `x`　🔴 **`docs/orders` 之補令四／五<u>僅存於凍存側支</u>**

- **形**：補令六 `§二-2` 令 `p3a` 側支**保留·⛔ 併線·⛔ 刪除** ⇒ 其上之 commit 永不入主線。
- **徵候（本窗當場實查）**：`c1` 側支之 `docs/orders/` **缺 `2` 檔**——
  `W-G.9-269_補令四_安全烤法與P1改繫.md`／`W-G.9-269_補令五_驗乙之重定與二檔無從驗.md`
  （其**僅存於** `05a11bd`）；同理，補令三〜五段之**報告**亦僅在該側支。
- **攔法**：凡引用該二補令或其報告者，**須逐字附側支名 `verify/W-G.9-269-p3a` 與 `commit 05a11bd`**
  ——⛔ 以「主線查無」推論其不存在（坑 `k` 之同族）。

---

## §乙　🔑 **起手清單**（新窗第一件事須現查者·⛔ 採信本文之值）

> 🛑 **本清單所列之值係<u>本窗收工時之態</u>**（`常規六 四`：態之陳述須同格附該 `commit`）。
> **新窗須逐項<u>當場實查</u>**，⛔ 逕引下表。

| # | 須現查者 | 法 | 本窗收工時之值 |
|---|---|---|---|
| `1` | **三 `ref`** | `git ls-remote origin refs/heads/wip/s1-endpart refs/heads/verify/W-G.9-269-c1 refs/heads/verify/W-G.9-269-p3a` | 主線 `0aa608ed6cedb209fb8de64b0cc3fa195872cdc2`／`c1` 見 `§丙`／**凍存 `05a11bd665f6c2790ff821c9e3a1606651e345d4`** |
| `2` | **`left-right`** | `git rev-list --left-right --count HEAD...<ls-remote 之值>` | 須 `0	0`；🛑 **⛔ 採信本地追蹤 ref**（坑 `23`） |
| `3` | **四簿 MAX 及其框** | 倉內既有器 `verify/probes/wg9268_gate5_tally.py <rev>`（⛔ 自擬框） | 見 `§丙` |
| `4` | **生產碼 `34` 檔 blob** | **正面列舉** `app.py` ＋ `verify/[^/]+\.py`（⛔ 全倉排除式） | `34` 檔；**主線 vs `c1` 相異恰 `2`** ＝ `app.py`／`verify/stepg_pipeline.py` |
| `5` | **`c1` 二檔之 blob** | `git rev-parse <rev>:<檔>` | `app.py` ＝ `c5c28b826aa25f4f12a3bd5bdc1fcd0587ab021f`；`verify/stepg_pipeline.py` ＝ `ee5d82b2138cb2599809d96d5006db3788675794` ⇒ 🛑 **須仍為 `8f3dccc` 之內容**（單 `§五` 之主檢） |
| `6` | **`P-1` 基準器之 blob** | 同上 | `verify/probes/probe_WG9269_coverage_single.py` ＝ `07d8627e5e4c797f1278999e40f306acce8c01f6`（**＝ 凍存側支之同名檔·逐位相同**） |
| `7` | **`GB-159`／`160`／`161`／`162` 之現況** | `docs/reports/W-G.4_泛用阻塞項登記表.md`（標題形） | 見 `§丁` |
| `8` | 🔴 **旗標之預設須仍為 `off`** | `k917_backfill_enabled` 之字面 | `WG9269_K917_BACKFILL` 預設 `"0"` ⇒ **⛔ 翻之**（其為 `c2`·須 KL **單獨**放行） |

### §丙　本窗收工時之 `c1` 側支態 ＋ 四簿

| 項 | 值 |
|---|---|
| `verify/W-G.9-269-c1` | 見 `§戊` 之收工出艙 |
| **自誤** | 相異 `351`／**MAX `361`**／缺號 **`[106, 355, 356, 357]`** |
| **`GB`** | 相異 `160`／**MAX `162`**／缺號 `[12, 87]` |
| **`VR`** | 相異 `80`／**MAX `95`**／缺號 `[73, 75]` |
| **`K-9`** | 相異 `28`／**MAX `29`**／缺號 `[]` |

🔒 **四簿之框**（`常規四（八）三`·逐字）：**標題形 ∪ 表列形**；母體 ＝ **各該登記表單檔**。
🛑 **取號另須二 `ref` 並查**（坑 `s`）——主線之自誤 MAX 亦為 `361`？**⛔ 假定**：本窗之補鑄落 `c1` 側支，
**主線 `0aa608e` 之自誤 MAX 仍為 `358`** ⇒ **併線前主線與 `c1` 之簿態相異**，須當場實查。

### §丁　四 `GB` 之現況

| `GB` | 現況 |
|---|---|
| `GB-159` | **仍有效**——中間態配地表⛔ 對外。🔴 **其受詞今為<u>三宗</u>**（`628-42(1)`／`628-53(2)`／**`628-27(1)`**·坑 `v`） |
| `GB-160` | 閘 `γ` **待重量·後置**（其解除繫於「退化之宗依 `K-9-11 三` 移出配地母體**後**」⇒ 俟 `c2` 併線） |
| `GB-161` | 母體逐字定稿 `14`＋`8`·交 `3`·聯集 `19`；**真硬編 `4` 支已修**（`W-G.9-269` `p2`），餘**免修·具名** |
| **`GB-162`** | 🆕 **生效**——⛔ 執行 `WV_BAKE=BASELINES`／⛔ 覆寫 `verify/baselines` 任一檔／⛔ 單行 `p3a`；**⛔ 引任何自 baseline 取得之數** |

### §戊　🛑 下一窗（`c2`）之三前置（本窗彙整·⛔ 本窗辦）

1. 🔴 **級聯剔除**（坑 `v`）：`0m` 實為 **`3` 宗**；差異表須區分二類；**⛔ 以「受詞二宗」呈 KL**。
2. 🔴 **`P-1` 之態宣告缺陷**（坑 `r`）：候發單側裁三案（補令七段 `§五-2`）。
3. `628-27(1)` 之**歸戶須於管線跑畢之態取之**（`harvest()` 時為空·補令七 `§四-2` 款 `2`）。

🛑 **`c1` 候 KL 放行**；**併線令由發單側下達**，⛔ 本窗或下一窗自行併線。
🛑 **`c2` 之前：⛔ 推主線、⛔ 翻旗標之預設、⛔ 動 `app.py`／`verify/stepg_pipeline.py` 一字。**
