# `W-G.9-269R`　本體階　**補令六段**（`p3` 整項撤出 ／ `GB-162` ／ `自誤 358` ／ 病一開工）

> **落點 ＝ 主線**（補令六 `§七` 序 `1`·零生產碼·得逕推）。
> **基座** ＝ `8677e9617816ba03c9c9e18e45e32bbda9508279`（`ls-remote` 實查·與補令六 `§零` 相符·`left-right` ＝ `0	0`）。
> **單** ＝ `docs/orders/W-G.9-269_補令六_p3撤出與GB162與病一開工.md`（原封入倉·三端 `sha256` 逐位相同
> ＝ `d6cb7ae5696b00dab732e312367f3abb72b9333284360ddf96681c8902ee4199`·`9169` B·`CR 0`／`LF 109`）。
>
> 🔴 **本報告係主線之<u>第一份</u> `W-G.9-269` 本體階報告**——補令三〜五段之報告落於**側支**
> `verify/W-G.9-269-p3a` ＝ `05a11bd`（依補令六 `§二-2` 款 `2` **⛔ 併線**）⇒ **自主線觀之⛔ 可達**。
> 🛒 **本段⛔ 載**：病一之任何修法主張（其屬 `c1`〜`c3`）。

---

## §零　工項零 ＋ `S-0b`

| 端 | `file sha256` | bytes | `CR` | `LF` |
|---|---|---|---|---|
| 來源（KL 交付） | `d6cb7ae5696b00dab732e312367f3abb72b9333284360ddf96681c8902ee4199` | `9169` | `0` | `109` |
| 落檔 | **同** | `9169` | `0` | `109` |
| **blob** | **同** | `9169` | — | — |

`S-0b`（`rc` **直取** ＝ `0`）：🔴 停機款 **`0`** ✅；🟡 `2` 項逐項處置——
`P-5` 本單⛔ 載 `SELF_SHA256` ⇒ **具名豁免**（三端 `sha256` 鏈承擔）；
`P-4` `:11` 之「`(a)(b)` ✅／`(c)` 🔴」係**判別力二造之逐款判**、⛔ 方向性轉引之量 ⇒ **偽陽·具名豁免**。

---

## §一　序 `1`：鑄號 —— 🔴 **一項簿態衝突已現並處置**

### 一-1　`GB-162`（**無衝突**）

**取號現查**（框 ＝ **標題形 ∪ 表列形**·母體 ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔）

| `rev` | 相異 | MAX | 缺號 |
|---|---|---|---|
| 主線 `8677e96` | `159` | **`161`** | `[12, 87]` |
| 側支 `05a11bd` | `159` | **`161`** | `[12, 87]` |

🔒 **二 `rev` 之 `GB` 簿逐數相同**（側支∖主線 ＝ `∅`／主線∖側支 ＝ `∅`）⇒ **次號 `162` 於二 `rev` 皆成立**。

**意指占用**（母體 ＝ 各 `rev` 之全倉追蹤檔·**錨定框**·**二形並取**·`GB-158`）

| 號 | 主線 B／C 形 | 側支 B／C 形 | 角色 |
|---|---|---|---|
| **`GB-162`** | `0` 檔／`0` 列·`0`／`0` | `0`／`0`·`0`／`0` | 受詢 ⇒ **未占用** ✅ |
| `GB-161` | `37` 檔／`126` 列·`33`／`113` | `39`／`130`·`35`／`117` | 對照甲（須 `≥ 1`）✅ |
| ⟨NEG⟩（**執行期自倉掃出**·字面⛔ 出艙） | `0`／`0` | `0`／`0` | 對照乙 ✅ |

**鑄之機檢**：範本取既有最末一則之標題形（`常規四（九）四`）＝ `` ### `GB-161` 🆕　… ``；
形之自證 ⇒ 定義框掃 payload 命中 **`[162]`** ✅；**MAX `161 → 162`**／相異 `159 → 160`／
**缺號 `[12, 87]` 未變** ✅；`numstat` **`29	0`**／嚴格前綴 `True`／`CR 0`。

### 一-2　🔴 `自誤 358`：**主線簿態與單所載之號相異**（照實·⛔ 頂替）

**簿態之現查**

| `rev` | 相異 | MAX | **次號** | 缺號 |
|---|---|---|---|---|
| **主線 `8677e96`** | `347` | **`354`** | **`355`** | `[106]` |
| 側支 `05a11bd` | `350` | **`357`** | `358` | `[106]` |

🔒 **側支∖主線 ＝ `[355, 356, 357]`**／主線∖側支 ＝ `∅`。
⇒ 🔴 **單 `§六` 所授權之 `自誤 358` 係依<u>側支</u>之簿態算得；而本序之落點為<u>主線</u>，其次號為 `355`。**

**意指占用**（二 `rev` 並查）

| 號 | 主線 B／C 形 | 側支 B／C 形 | 判 |
|---|---|---|---|
| **`358`** | `0`／`0`·`0`／`0` | `0`／`0`·`0`／`0` | 受詢 ⇒ **二 `rev` 皆未占用** |
| `357` | **`0`／`0`** | `3` 檔／`8` 列·`3`／`8` | **已鑄於側支** |
| `356` | **`0`／`0`** | `3`／`6`·`3`／`6` | **已鑄於側支** |
| `355` | **`0`／`0`** | `3`／`9`·`3`／`9` | **已鑄於側支** |
| `354` | `3` 檔／`14` 列·`3`／`11` | `4`／`15`·`4`／`12` | 對照甲 ✅ |
| ⟨NEG⟩ | `0`／`0` | `0`／`0` | 對照乙 ✅ |

### 一-3　🔒 **所採之處置與其理由**（⛔ 自訂準據·僅適用既有二規之合取）

**二選項之後果（逐項出艙·⛔ 只出所採者）**

| 選項 | 後果 |
|---|---|
| **甲**：依主線簿態取 `355` | 🔴 **與側支已鑄之 `自誤 355` <u>撞號</u>**——同號二則、內容相異。**嚴格劣於乙。** |
| **乙**：依單取 `358` | 主線之缺號集增 `[355, 356, 357]`；該三號**已鑄於側支**，依 **`缺號永不重用`** ⇒ **⛔ 得重鑄** |

🔒 **⇒ 採乙**。其依據為**既有二規之合取**，⛔ 新訂準據：
`(1)` `CLAUDE.md`「**編號衝突一律以倉內為準**」——側支**亦在倉**（且補令六 `§二-2` 款 `2` 明令其**保留·⛔ 刪除**）
⇒ 該三號於倉內**確已占用**；
`(2)` 「**缺號永不重用**」——主線之缺號集記其為缺，即已阻斷重鑄。

🛑 **惟 `(2)` 之生效繫於「該缺口<u>被記錄</u>」**——若主線之簿對該三號**一字未提**，
後窗將無從分辨「缺號」與「記帳之誤」。⇒ **本批於自誤簿末端追加一節，逐號登記其
「已鑄於側支 `05a11bd`／⛔ 得於主線重鑄／其內容僅可自該側支取得」**（**事實之登記·⛔ 鑄號**）。

🔒 **`GB-132` 避讓之當場復算**：該登記節**⛔ 使 `355`／`356`／`357` 被任一框命中**
——立後之三號**仍在缺號集** ＝ `True` ✅（其節內之號一律以**中文數字**書之、各列⛔ 以 `#` 起首）。

**鑄之機檢**：形之自證 ⇒ 定義框掃 payload 命中 **`[358]`** ✅（器內置 `assert`·不符即停機⛔ 寫檔）；
**MAX `354 → 358`**／相異 `347 → 348`／**新增恰 `[358]`**／
**缺號 `[106] → [106, 355, 356, 357]`**（其增量即上開三號·已逐號登記）；
`numstat` **`48	0`**／嚴格前綴 `True`／`CR 0`。

🛑 **本節係<u>照實回報</u>**（`常規二`：取保守項續辦並逐字回報）——
**若發單側認主線應改取 `355` 或另有處置，本批之二 `commit` 皆 `append-only` 且可獨立回退。**

---

## §二　`§二`／`§四` 之立準（入交接註）

### 二-1　🔒 **立準（補令六 `§四` 逐字·⛔ 改寫）**

> **凡令「更新／重產／覆寫」一既有之物，須先出艙<u>該物於系統中之角色</u>：
> 其為「被比對之應然值」抑或「被產出之結果」？前者之覆寫即<u>抹去判準</u>，
> 須另備其不可逆性之處置（凍存／逐格歸因／解除條件），⛔ 以「它過期了」為由逕令重產。**

### 二-2　🔒 **立準（補令五 `§一-1` 逐字·本窗前段所承）**

> **凡以「某字樣於某檔命中 `0`」為閘，須以 <u>AST</u>（去 docstring／註解／字串常量）機械判，
> ⛔ 以字樣命中代之。** 判別力須同格出艙。

### 二-3　🔑 **交接註（本段新得·承側支各段之坑）**

| 坑 | 徵候 | 攔法 |
|---|---|---|
| `j` | **號之簿態隨 `ref` 而異** | 同一號於主線「未占用」而於側支「已鑄」——**單方 `rev` 之取號現查⛔ 足以判「未占用」** | 凡側支**⛔ 併線**而其上曾鑄號者，取號現查須**二 `rev` 並查**並出艙其**差集**；主線須**登記其缺口**，否則「缺號永不重用」之保護**⛔ 生效**（其繫於缺口被記錄） |
| `k` | **證據落於未併線之側支** | `GB-162` 之機械依據（三端對拍 `1137` 格）其落檔**全在側支** ⇒ 自主線 `git show` **⛔ 可達** | 凡登記項之證據落於未併線之側支者，**須於該登記內逐字附側支名與 `commit`**，⛔ 只寫落檔路徑 |
| `l` | **「重烤」之受詞係<u>應然值</u>** | `baseline` 為 `diff_rows` 之比較端；本分支之 `40` 紅其內容即該 `1137` 格 ⇒ 重烤 ＝ **一次抹去缺陷證據** | 見 `二-1` 之立準；併 `GB-162` 之三禁令 |

---

## §三　自限復驗

| 項 | 實測 |
|---|---|
| `git status --porcelain` | 空 |
| 生產碼 `34` 檔 blob（基座 → 本段期末） | **相異 `0`／`34`**（本段全為 `docs/**`） |
| 四簿 MAX | 自誤 **`354 → 358`**／`GB` **`161 → 162`**／`VR` `95` 未動／`K-9` `29` 未動——**皆經補令六 `§六` 逐號授權** |
| 自誤缺號 | `[106]` → **`[106, 355, 356, 357]`**（增量已逐號登記於簿末·`一-3`） |
| `GB` 缺號 | `[12, 87]` **未變** |
| `deletions` | 本段各 `commit` 皆 `0` |
| `verify/baselines` | **逐位未動**（`GB-162` 禁令 `(2)`） |
| `p3a` 側支 | `05a11bd` **保留·⛔ 併線·⛔ 刪除**；主線**⛔ 含 `p3a` 之任何物件** |

---

## §四　續辦

| 序 | 事 | 狀態 |
|---|---|---|
| `1` | 工項零 ＋ 鑄 `GB-162`／`自誤 358` ＋ 立準入交接註 | ✅ **辦畢**（`一-2`／`一-3` 之簿態衝突已照實回報） |
| `2` | **`c1`**（🔴 生產碼·遞補迴圈骨架 ＋ 旗標預設 `off` ⇒ 行為逐位不變） | ⬜ **次辦**·落**新側支** `verify/W-G.9-269-c1` |
| `3` | 發單側復驗 → 呈 KL → 放行 → 快轉併線 | ⬜ |
| `4` | `c2`（旗標 `on`·**有土地後果**·單獨呈 KL）→ `c3` → `c4` | ⬜ |

🔒 **`c1`〜`c4` 之規格悉依補令三 `§二`／`§三`／`§四`**，惟：
**`P-1` 之基準** ＝ 補令四 `§三-2` 之**單態器**所得（父態 `f7c833e`／**`34` 組·逐對總數 `0`**）
——🛑 **該器與其實測落於側支 `05a11bd`**（`verify/probes/probe_WG9269_coverage_single.py`）
⇒ **`c1` 之側支須自主線起，該器⛔ 在主線** ⇒ **其處置見 `§五`**；
**`S-10` 撤銷**；**⛔ 引任何自 baseline 取得之數**（`GB-162` 生效），差異表之數一律自**當次實跑**取得。

---

## §五　🛑 呈發單側（**二項**·⛔ CC 可裁）

| # | 項 | 受詞 |
|---|---|---|
| `1` | **`自誤 358` 之簿態衝突** | 已依 `一-3` 採**乙**（取 `358` ＋ 登記缺口）並照實回報。**若發單側認應另處置，二 `commit` 皆可獨立回退。** |
| `2` | 🔴 **`P-1` 之基準器落於⛔ 併線之側支** | 補令六 `§七` 令 `P-1` 之基準 ＝ 單態器所得，而 **`verify/probes/probe_WG9269_coverage_single.py` 僅存於側支 `05a11bd`**；`c1` 之側支若自**主線**起，該器**⛔ 在其樹內** ⇒ `c2` 屆時**無從施行 `P-1`**。**候發單側裁**：`(甲)` 將該器單獨 cherry-pick 至主線（**零生產碼·純新增檔**）／`(乙)` `c1` 側支自 `05a11bd` 起（🔴 將含 `p3a` ⇒ 與 `§二-2` 款 `2` 相斥）／`(丙)` 另指基準。 |

🛑 **`2` 之未決⛔ 阻 `c1`**——`c1` 之驗收為「旗標 `off` 下輸出逐位不變」，⛔ 用 `P-1`；
`P-1` 於 **`c2`** 方為受詞。⇒ **本窗即續辦 `c1`**，並俟裁再辦 `c2`。

🔒 **`GB-159` 仍有效**：二宗之配地圖與負擔表**⛔ 對外**。

*本段受詞 ＝ CC。⛔ 動 `app.py` 一字（本段）、⛔ 改 `verify/baselines/**` 一字、⛔ 併線 `p3a` 側支。*
