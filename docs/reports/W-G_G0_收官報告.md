# W-G G.0 微波收官報告（顯示層＋紀律，零引擎）

- 波次：W-G（app 接線波）子波 G.0
- 日期：2026-07-12
- 基準：HEAD 前 = `65218c4`（W-F F.5 收官）
- 性質：**零引擎**。僅 app.py 使用者可見文案 ＋ CLAUDE.md 紀律／對照表。
- 機器證明：`run_all` 全綠且 **wf/f0~f4 baseline 逐格零 diff**（引擎、閘檔、baseline 全未觸）。

---

## ④ 閘數釘死（開工首務；154／168 分歧結案）

**真實閘數 ＝ 153 `✅ PASS`**（headless 對拍閘），三方交叉印證：

1. 本機實跑 `python verify/run_all.py`（2026-07-12，G.0 前後各一次）→ `grep -c "✅ PASS"` = **153**；`W-V run_all: ALL GREEN`。
2. F.5 commit `65218c4` 訊息**逐字**：「run_all **153** PASS ALL GREEN」。
3. memory `wf-recon`：「run_all **153** PASS 綠」。

另有 **10** 條非 `✅ PASS` 綠斷言 → 全 `✅` 計 **163**：
- `[1/3]` golden ×2（手冊圖8：1地號 0.6685／2地號 0.3315；滑池槽 8/8）
- `[2/3]` diff 引擎自檢 ×2（竄改 3/3、`_classify_gxxx` 分流）
- `[3/3]` ownership 靶 ×6（628-18→G005、628-45→G007、628→G009、628-37→G019、628-41→G023、628-36→G029）

**153 淨分割**（各行首 token，Σ=153）：

| 族 | 數 | 族 | 數 |
|---|---|---|---|
| F.0 | 27 | v3（含財務錨）| 19 |
| F.1 | 10 | W-D.4 | 12 |
| F.2 | 19 | 率接線 | 4 |
| F.3 | 21 | W-D.3 | 3 |
| F.4 | 25 | 結構不變量 | 2 |
| **F 系小計** | **102** | 指配／抵費地／參數／k*／-a診斷 | 各 2 |
| | | ownership | 1 |

**154／168 結案**：`git grep` 全倉，`154` 與 `168` **皆不作為閘數出現於任何倉檔**。
- 反推自洽：`docs/W-F_F4_上呈_2026-07-11.md:85`「run_all **128**＋F.4 閘全綠」＝ 128（F.4 前）＋ 25（F.4 自身閘，見上表）＝ **153**。
- **機制證明**：F.5（`65218c4`）僅改 4 檔 {CLAUDE.md, app.py −864, `配地計算總規格_v3.md`, `verify/baselines/wf/PROVENANCE_F5.md`}，
  **未觸任何閘檔**（`run_verification.py` / `run_all.py` / `tests/` 全未動）→ 後 F.5 閘數 **≡** 後 F.4 閘數，皆 **153**。
  前波聊天回報「168」為出艙數字、無倉錨，作廢。

---

## ① Tab 編號校正（根治漂移）

**權威序**：app.py `main()` 之 `TAB_NAMES`（sidebar radio）。使用者文案一律用**分頁名**、汰除寫死 Tab 數字。

**成果**：`grep -cE "Tab [0-9]" app.py` 由 **141 → 60**；**81 條使用者文案**之 Tab 數字改為分頁名。
殘留 60 條**全為程式碼註解／docstring**（範圍外）；`st.*`／`col.*` 顯示呼叫殘留數 = **0**（機器驗證）。

**施工法**：count-asserted 腳本（78 映射，每筆帶 expected count，數不符即中止不靜默）替換 85 處 ＋ 4 條手改（一鍵匯入區 12751/12752/12754/12757）。

**映射規則**（語意指涉，非位置對照——app 舊「Tab N」為舊版面編號，土地歸戶前移致漂移）：

| 舊「Tab N」文案語意 | 現行分頁名 |
|---|---|
| Tab 1（歸戶／登記面積／白名單）| 土地歸戶 |
| Tab 3（街廓分類／推送／回送 C 值）| 街廓互動分析 |
| Tab 4 **重劃後均價**來源 | **土地開發分析法**（見下 KL 裁定）|
| Tab 4／Tab 5 面積・**重劃前均價**來源 | 預期開發分析法 |
| 推送至 Tab 6 | 地價區段分析 |
| Tab 7（C 值／工程費／總負擔比率）| 財務分析 |

**KL 裁定（2026-07-12，本報告鎖入）**：交辦例將「一鍵匯入」按鈕之 `Tab 4/5` 寫為「街廓互動分析／預期開發分析法」，
惟**重劃後均價**（`weighted_price_sqm`）程式**只在土地開發分析法頁寫入**（`app.py:8493/8497`，caption 8490「重劃後土地之特定價格」），
程式原註解（`app.py:12747`）亦寫「土地開發分析法」。KL 裁：**依碼真值＝土地開發分析法**（修正交辦例）。
影響 5 條文案：`1802/10694/12751/12754/12757`。重劃前均價→預期開發分析法（`pre_land_price_sqm` 只寫於 `8799`）與交辦例一致。

---

## ② 步驟 J 殘留提示軟化（指向已刪步驟 K）

F.5 拆除步驟 K UI（§7 引擎僅在 `verify/`、尚未接 app）。4 條使用者提示改指法定機制、不再承諾已刪 UI：

| 位置 | 原（→已刪步驟 K）| 軟化後 |
|---|---|---|
| 15085 | → 待 Step K 跨街廓調配 | → 待跨街廓調配（§31 機制）|
| 15092 | 將於 **Step K 跨街廓指配 / 現金補償** 完成後… | 將於**跨街廓指配 / 現金補償**（§31 機制）完成後… |
| 15639 | 需**跨街廓調配**（進入步驟 K）| 需**跨街廓調配**（§31 機制）|
| 15760 | → 請執行**步驟 K**進行跨街廓調配 | → 需進行**跨街廓調配**（§31 機制）|

驗證：無任何 `st.*` 顯示呼叫殘留「步驟 K／Step K」（僅註解 6857/10529 留存，範圍外）。

---

## ③ 回報紀律新制 ＋ 對照表入 CLAUDE.md

- CLAUDE.md 新增「工作紀律（回報鐵律，W-G 起強制）」節：報告入倉、聊天只准貼 `git log -1` 逐字＋報告路徑、數字錨引倉檔禁出艙。
- CLAUDE.md 新增「分頁權威序與歷史鍵名對照（W-G G.0 校正）」表，含**紅線清單**（快照鍵名 `財務接線_…_Tab4/Tab5`、docs 歷史 TabN 稱呼、session_state 小寫 `t4/t5` 鍵——一字不動）。
- 建 `docs/reports/` 目錄（本報告落地即建）。

---

## 驗證總表

| 項 | 結果 |
|---|---|
| py_compile app.py | ✅ OK |
| run_all | ✅ ALL GREEN，153 `✅ PASS`（引擎/baseline 零 diff）|
| app.py diff | 85 ins / 85 del（**1:1 平衡＝零結構變動**，純字串內容替換）|
| 顯示呼叫殘留 Tab 數字 | 0 |
| 顯示呼叫殘留「步驟 K」 | 0 |
| 紅線（快照鍵名／spec／session key）| 未觸（edit 面全在 app.py 使用者文案）|

---

## 上呈 KL／claude.ai 收波判讀事項

1. **KL 裁定已鎖入**：重劃後均價來源＝土地開發分析法（修正交辦例「街廓互動分析」）。
2. **非 1:1 改寫（判讀確認）**：
   - `12576`「Step E/F/G、Tab 5/6/7/8」→「Step E/F/G 與下游分析頁」（Tab 5/6/7/8 為含混列舉，改概稱）。
   - `16304` 步驟 I 標頭「下游 Tab（Tab 5 / Tab 6 / Tab 7）」→「下游分頁（預期開發分析法 / 地價區段分析 / 財務分析）」。
   - `12754` 一鍵匯入按鈕採**最小改**（僅數字→名），保留「計算之重劃前後平均地價」（交辦例簡化為「計算值」，未採以免失資訊）。
3. **② 軟化措辭**「（§31 機制）」為 CC 擬定（交辦僅曰「軟化」）；如需改口徑請示。
4. **範圍界定**：僅改 app.py 使用者可見文案；程式碼註解／docstring（60 條殘留 Tab 數字，如 `12747`）維持原狀——非文案、且部分為 KL 裁定佐證。
5. 真 app Tab3 之 §7 七級調配 UI 仍未接（G.1 主體），G.0 未涉。
