# 🧾 `W-G.9-172R`　app 路徑池面積 `+0.01 ㎡` 之歸因 ＋ 步驟 0 二裁之前置量測

**施工單**：`W-G.9-172_池面積歸因與步驟0前置量測.md`
（`SELF_SHA256 = 5d5d0ba6e888eb6dcf1fe7713eae738cbc667177c1e63a4e04c8a294e6103aa2`
——**CC 現算逐位相符**·計法 ＝ 扣除最末一行·`224` 列／`24496` B；
四反引號界線實查 ＝ `[131, 156, 160, 186, 190, 209]` ⇒ **`3` 塊**·與單載相符）
**基座（`S-1` 實查·⛔ 非推定）**：`fcad7fdd4986ad4b2e80fb3033ed72f824ec91ea`
**`app.py` blob（期初 ＝ 期末）**：`68f7bfab610ec78688b9d7322ae4bc2aac74493e`
**性質**：只讀量測 ＋ 純附加登記　🛑 ⛔ 零生產碼·⛔ 未重產 baseline·⛔ 未改任何既有一字

🛑 **總判**：`§三` 九項預測 ⇒ **符 `5`／不符 `3`／不可判 `1`**（和 ＝ **`9`** ✅）。
🔴 **`+0.01 ㎡` ⛔ 未能歸因**——`M-A-2` 之判準已觸「**先判量測器紅**」（`V-2`）：
shim 路徑於 `R2` 與 KL 之外部錨**不符**、`R4`／`R6` **結構上不可達** ⇒ 依單逐字**⛔ 不得逕報受詞**。

---

## 🔴 `§一`　`M-A`　app 路徑抵費地池面積之歸因

### `M-A-1`　可行性現查 ＋ shim 與 `main()` 之**逐項具名差異**

| # | 現查 | 答（⛔ 非推測） |
|---|---|---|
| `1` | `_pool_strips_for_block` 是否 harvestable？ | ✅ **是**——`app.py:9345` 係**模組級**函式，`harvest()` 之 `ns` 內可取（`harvest stats = {'kept': 244, 'skipped': 8}`） |
| `2` | **app 路徑之呼叫端**是否 harvestable？ | 🔴 **否**——`app.py:20892` 位於 `main()` 內；`app_harvest._filter_module` 濾除 `main()` ⇒ shim **結構上到不了該呼叫端** |
| `3` | 二呼叫端之**實參式**是否同構？ | ✅ 逐字相同（`app.py:20892-20894` vs `verify/stepg_pipeline.py:959-961`），惟其**上游組裝碼分屬二份** |
| `4` | 可執行之驅動器 | 只有 `verify/stepg_pipeline.run_step_g`（＝ harness 呼叫端）⇒ 本探針所量者係 **harness 側之值**·⛔ 非 app `main()` 之值 |
| `5` | 可達之街廓 | 🔴 **僅 `R1`／`R2`**——`run_step_g` 於 `R2` 之 **`結構閘 telescoping 破`** 拋出而終止（⛔ 非 `②-宗 圍堵閘`）⇒ `R4`／`R5`／`R6` **一筆不產生** |

🛑 **本探針之二處具名偏離**（`M-A-1` 逐字所令）：
> **偏離①**　呼叫端係 harness（見上 `2`／`4`）——⛔ 非「逕以 harness 版代替」，而係**唯一可執行之 shim**；其與 `main()` 之差異已於上表逐項具名（`GB-117`／`GB-118` 同族）。
> **偏離②**　spy 於 `②-宗 圍堵閘` 拋出時**記錄例外並回傳 `[]`**，使迴圈得續行；🛑 該吞例外**僅於 spy 內**、⛔ 未改 `app.py` 一字。⚠️ 實測顯示**此舉無濟於事**——`run_step_g` 於 `R2` 另有 `telescoping` 閘拋出而終止。

**碼面錨**：`grep -n "_pool_strips_for_block" app.py verify/stepg_pipeline.py`
⇒ 定義 `app.py:9345`；呼叫端 `app.py:20892`（`main()` 內）／`verify/stepg_pipeline.py:959`。

### `M-A-2`　二態（實為**五**態）之未捨入池面積

🔒 **母體 ＝ 五個 `app.py` blob 之態**，各以**該態自身之 `verify/`**（`git worktree`）驅動同一探針
（⛔ 不以新 harness 跑舊 `app.py`——`-169R` 後 `verify/` 亦改）。

| 態 | commit | `app.py` blob | `R1` Σ池 `0m`（未捨入） | `R1` Σ池 `3.5m` | `R2` Σ池 `3.5m` | `R4`／`R5`／`R6` |
|---|---|---|---|---|---|---|
| `S0`（KL 首跑之態） | `e9ad898` | `b4cc3083…` | `1175.498319859` | `1175.096297168` | `1779.426820031` | 🔴 **不可達** |
| `S1`（`-161R`） | `3e8a8ab` | `8daca97f…` | `1175.498319859` | `1175.096297168` | `1779.426820031` | 🔴 **不可達** |
| `S2`（`-163R`） | `1616160` | `f285b91c…` | `1175.498319859` | `1175.096297168` | `1779.426820031` | 🔴 **不可達** |
| `S3`（`-169R`） | `6a4a0da` | `60b550b7…` | `1175.498319859` | `1175.096297168` | `1779.426820031` | 🔴 **不可達** |
| `S4`（現態） | `fcad7fd` | `68f7bfab…` | `1175.498319859` | `1175.096297168` | `1779.426820031` | 🔴 **不可達** |

🔒 **五態逐位相同**（`R1` 之逐池片亦同：`0m` ＝ `1175.493745 / 0.004575`；
`3.5m` ＝ `1169.708187 / 5.383510 / 0.004600`）。

🛑 **與 KL 外部錨之對拍 ⇒ 量測器紅**：

| 量 | KL 實跑 | shim | 判 |
|---|---|---|---|
| `R1` 池 | `1175.10`（次跑） | `1175.096297168` ⇒ 2dp **`1175.10`** | ✅ **相符** |
| `R2` 池 | `1774.20` | `1779.426820031` ⇒ 2dp `1779.43` | 🔴 **相異 `5.23`** |
| `R6` 池 | `1628.71` | — | 🔴 **不可達** |
| 中止點 | `圍堵閘破[R5]` | `telescoping 破[R2]` | 🔴 **相異** |

⇒ 依 `M-A-2` 逐字「若 `R2`／`R6` 亦相異 ⇒ **先判量測器紅**（shim 路徑非 KL 之路徑），
⛔ 不得逕報受詞」——**本報告⛔ 不報 `+0.01` 之歸因**。
⚠️ **併陳一項讀法之別**：若只讀「**二態**逐位相同」之字面，`R2` 於五態**確係逐位相同**（`V-2` 之字面成立）；
惟其與**外部錨**不符 ⇒ 仍落入「shim ≠ KL 路徑」之判。二讀法並陳、⛔ 不擇一隱去。

**證據檔**：`verify/out/probe_WG9172_pool_{b4cc3083,8daca97f,f285b91c,60b550b7,68f7bfab}.log`
＋ 同名 `WG9172_pool_*.json`（五態各一份）。

### `M-A-3`　機制之現查（⛔ 非推測）

| ？ | 受詞 | **現查之答** | 證 |
|---|---|---|---|
| ① | ghost 之 `polygon_coords` 是否進入池之扣除？ | 🔒 **否** | `[T2-DIAG] 街廓 R1｜宗數 **5**` 於**五態皆同**（含 `-169R` **之前**）⇒ ghost **從未**在 `biz_polys` 內；⇒ `L-4` 之濾**於池之扣除上⛔ 無作用** |
| ② | 位次（`pre_position`）是否進入池之**幾何**？ | 🔒 **否** | `_pool_strips_for_block` 之簽章 ＝ `(block_poly, d_hat, corner_pt, allocation_dir, biz_polys, _label, _depth, _verbose)` ⇒ **⛔ 無位次參數**；位次之唯一載體 ＝ `biz_polys` 之**順序** |
| ③ | 是否存在依序累加之浮點運算，其加總順序隨位次改變？ | 🔒 **否**（實測） | 以**同一組 `biz_polys`** 之三種順序（原序／反序／依面積升序）呼叫：`R1` Σ池 三者皆 `1175.096297167773`（Δ ＝ `+0.000e+00`）、`R2` 皆 `1779.426820030561`；**逐池片面積亦逐位相同** |

🔒 **⇒ 於 shim 可達之範圍內，三批（`-161R`／`-163R`／`-169R`）之碼改動⛔ 不可能改變池面積**
——與「五態逐位相同」互相印證。🛑 **惟此⛔ 不足以解釋 KL 之 `+0.01`**（見 `M-A-2` 之量測器紅）。

### `M-A-4`　三批之拆分　🔴 **⛔ 無法拆分·逐字回報所缺**

四態（`S0`〜`S3`）＋ 現態之 `R1` 池**逐位相同**、`R4` **五態皆不可達**
⇒ **⛔ 無任何一批可被指認為 `+0.01` 之產生者**。**所缺**逐項：

1. `R4`／`R6` 之池值——`run_step_g` 於 `R2` 中止（`GB-118`）；
2. app `main()` 之呼叫端——`harvest` 濾除 `main()`（`M-A-1` `2`）；
3. KL 之 UI 態下之中間量——app 於輸出中⛔ 不顯示自身 commit（`GB-119`），亦⛔ 無逐街廓之池 log。

🛑 ⇒ `V-5`（「`+0.01` 產於 `-169R`」）判為 **⛔ 不可判**；**⛔ 未以三批合計代替**。

---

## `§二`　步驟 0 二裁之前置量測

### `M-B-1`　八群逐成員之 `幾何面積_m2`（實值·未捨入）

🔒 **取法**：於 `harvest()` 之 `ns` 攔 `_projection_order`，取 `k6_step0_merge` 傳入之 `_mem`
（成員 ≥ 2 者恰 **`8`** 群；`k6_merge_groups` 呼叫 `4` 次、其回傳之元素型別 ＝ `['int']`（**索引**·⛔ 非 dict），
非單元素群之群大小 ＝ `[2, 2, 3, 2, 3, 3, 2, 3]`）。
🔒 **app 側與 harness 側「各一份」之答**：二呼叫端（`app.py:main()` 與
`verify/selection_pipeline.build_build_parcels`）**皆呼叫同一份 app 函式**
`_annotate_temp_parcel_cut_type` → `k6_step0_merge`（`app.py:4292`／`:11571`）
⇒ **二側之值⛔ 不可能相異**；本表即該唯一之份。

| # | 街廓 | 現行命名 | 逐成員（暫編地號 ＝ `幾何面積_m2` 全精度） | Σ | `-136` 錨 | 投影序首員 | **面積最大** | 判 |
|---|---|---|---|---|---|---|---|---|
| `1` | `R2` | `628-40(1)+` | `628-40(1)`=`513.340000000`／`628-43(1)`=`0.450000000` | `513.79` | `513.79` ✅ | `628-40(1)` | `628-40(1)` | 不變 |
| `2` | `R3` | `628-30(3)+` | `628-30(3)`=`783.060000000`／`628-45(2)`=`362.200000000` | `1145.26` | `1145.26` ✅ | `628-30(3)` | `628-30(3)` | 不變 |
| `3` | `R3` | `628-47(1)+` | `628-47(1)`=`246.500000000`／`628-48(1)`=`246.480000000`／`628-49(2)`=`135.940000000` | `628.92` | `628.92` ✅ | `628-47(1)` | **`628-47(1)`** | 🔒 **不變**（見下） |
| `4` | `R3` | `628-29(1)+` | `628-29(1)`=`114.650000000`／`628-28(1)`=`114.300000000` | `228.95` | `228.95` ✅ | `628-29(1)` | `628-29(1)` | 不變 |
| `5` | `R5` | `628-21(2)+` | `628-22(2)`=`891.370000000`／`628-21(2)`=`809.720000000`／`628-23(2)`=`727.220000000` | `2428.31` | `2428.31` ✅ | `628-21(2)` | `628-22(2)` | 🔴 **改名** |
| `6` | `R5` | `628-45(1)+` | `628-20(2)`=`516.460000000`／`628-45(1)`=`92.660000000`／`628-30(1)`=`26.980000000` | `636.10` | `636.10` ✅ | `628-45(1)` | `628-20(2)` | 🔴 **改名** |
| `7` | `R6` | `628(2)+` | `628-1(2)`=`228.690000000`／`628(2)`=`11.530000000` | `240.22` | `240.22` ✅ | `628(2)` | `628-1(2)` | 🔴 **改名** |
| `8` | `R6` | `628-23(1)+` | `628-21(1)`=`352.410000000`／`628-22(1)`=`254.180000000`／`628-23(1)`=`118.860000000` | `725.45` | `725.45` ✅ | `628-23(1)` | `628-21(1)` | 🔴 **改名** |

🔒 **外部錨 `8`／`8` 全中**（`W-G.9-136` commit 訊息之八個 Σ）⇒ `V-6` **符**。
🔒 **改名者 ＝ `4` 群（`R5`×2／`R6`×2）**、不變 ＝ `4` 群。

🛑 **`R3` 第三群（`628-47(1)+`）之二實值逐字出艙**：
`628-47(1)` ＝ **`246.500000000`** ／ `628-48(1)` ＝ **`246.480000000`** ⇒ **差 ＝ `0.020000000`**
⇒ 🔒 **判得出：面積最大者 ＝ `628-47(1)` ＝ 現行命名 ⇒ ⛔ 不改名。**
⚠️ 發單側之 DXF 重建預期其「⛔ 判不出（`0.01 ㎡` 之差）」——**實值之差為 `0.02`、⛔ 非 `0.01`**，故**可判**。

🔒 **`裁 B` 之脆弱性清冊（top-2 面積差 `< 1 ㎡` 之群·⛔ 不設容差 ⇒ 其平手分支之風險清單）**：

| 街廓 | 群 | top1 | top2 | **差** |
|---|---|---|---|---|
| `R3` | `628-47(1)+` | `628-47(1)` ＝ `246.500000000` | `628-48(1)` ＝ `246.480000000` | **`0.020000000`** |
| `R3` | `628-29(1)+` | `628-29(1)` ＝ `114.650000000` | `628-28(1)` ＝ `114.300000000` | **`0.350000000`** |

⇒ **`2` 群**落於 `< 1 ㎡`；🛑 二者皆⛔ 未平手，惟 `幾何面積_m2` 係 **2dp 之量**（`0.02` ＝ **二個最小刻度**）
⇒ 凡使幾何微動之改（深度／切線／DXF）皆可能翻轉 `628-47(1)+` 之命名。

⚠️ **併記（⛔ 非判準）**：發單側之 DXF 重建值與實值之差最大者 ＝ `628-22`（重建 `891.10` vs 實 `891.37`·差 `0.27`）
⇒ **超出其自陳之 `0.15 ㎡` 精度上限**；其餘各項多在 `0.15` 內。逐項照實併陳、⛔ 不改實值遷就。

**證據檔**：`verify/out/probe_WG9172_step0.log`／`verify/out/WG9172_step0_68f7bfab.json`。

### `M-B-2`　`temp_parcels` 之鍵流向 ⇒ `裁 A` 新增欄應否 `_` 前綴

母體 ＝ `temp_parcels` 之 **`20`** 鍵 vs `verify/baselines/**` 之 **`266`** CSV／**`245`** 相異欄名。

| 類 | 鍵 |
|---|---|
| 🔴 **流入**被對拍輸出（`3`） | `原地號`／`所屬街廓`／`暫編地號` |
| ⛔ **未流入**（`17`） | `_ghost_area_m2`／`_ghost_reason`／`_integrity_incomplete`／`_is_ghost_sliver`／`centroid_x`／`centroid_y`／`cut_type`／`polygon_coords`／`分攤登記面積_m2`／`合併依據`／`合併組成`／`幾何面積_m2`／`登記面積_m2`／`街廓分類`／`重劃前地價區段`／`面積_m2`／`面積_坪` |
| `_` 前綴之既有鍵（`4`） | `_ghost_area_m2`／`_ghost_reason`／`_integrity_incomplete`／`_is_ghost_sliver` |

🔒 **`_` 前綴者流入 baseline 欄集之數 ＝ `0`**（先例之判別力：該前綴與「不入對拍輸出」**完全共現**）
⇒ 🔒 **建議：`裁 A` 所新增之成員幾何欄採 `_` 前綴**（承 `_k6_src_index`／`_is_ghost_sliver` 之先例）。
⚠️ **併記**：`合併組成`／`合併依據`（`W-G.9-136` 所加·⛔ 無 `_` 前綴）亦**未流入** ⇒
「不入對拍輸出」⛔ 不以 `_` 前綴為**必要**條件；`_` 前綴係**充分之慣例**、⛔ 非機制。

### `M-B-3`　`_rank_by_tpid` 之靜默退路現查

碼面逐字（`app.py:19262`／`verify/selection_pipeline.py:379`）：
`'_pre_position_rank': _rank_by_tpid.get(r.get('暫編地號', ''), float('inf')),`

| 情境 | 母體 | **落 `inf` 之宗** |
|---|---|---|
| `0m` | `6` 街廓 | **`0`** |
| `3.5m` | `6` 街廓 | **`0`** |
| 合計 | — | **`0`** |

🔒 **判別力對照**：`{}.get("__nonexistent__", float("inf"))` ⇒ `inf`（該退路確會被走到，若鍵不存在）。
🔒 **結構性之由（⛔ 非僅實測）**：`_candidates` 之母體 ＝ `_candidates_pool` ＝ `_all_in_blk`，
而 `_rank_by_tpid` 之鍵集亦由**同一個 `_all_in_blk`** 導出（`_projection_order(_all_in_blk, …)`）
⇒ 每一候選之 `暫編地號` **必**在 rank 內 ⇒ `inf` **結構上不可達**（除非同街廓內有重複 `暫編地號`）。
⇒ `V-8` **符**；🔒 **落地時仍宜加 loud 斷言**——其現況不可達係**繫於二母體同源**，
換案／重構若使二者分家即靜默錯配（`GB-117` 同族之「守衛繫於構造」）。

### `M-B-4`　改名之波及面（母體 ＝ **全倉** `.md`／`.py`／`.csv`／`.log`／`.json`／`.txt`）

🔒 **母體 ＝ `1402` 檔**（🛑 **已自母體扣除本探針自身之 `3` 檔**——其原始碼／log／json 皆含八名之字面·
`VR-081`／`自誤 207` 同族之「量測器須自母體扣除自身」）。**態 ＝ `fcad7fdd…`**。
🔒 **量測器自檢**：人造不存在之名 `628-99(9)+` ⇒ 出現次數 **`0`**；已知非 `0` 之 `628-30(3)`（⛔ 不含 `+`）⇒ **`403`**。

| 現行名 | 命中列數 | 出現次數 | 檔數 | 落 `裁 B` 後 |
|---|---|---|---|---|
| `628-40(1)+` | `84` | `114` | `29` | 不變 |
| `628-30(3)+` | `95` | `103` | `23` | 不變（🔒 `VR-075` 一系⛔ 不受影響·與單載相符） |
| `628-47(1)+` | `18` | `22` | `11` | 🔒 **不變**（見 `M-B-1`） |
| `628-29(1)+` | `87` | `93` | `28` | 不變 |
| 🔴 `628-21(2)+` | `20` | `24` | `12` | **改為 `628-22(2)+`** |
| 🔴 `628-45(1)+` | `101` | `109` | `24` | **改為 `628-20(2)+`** |
| 🔴 `628(2)+` | `21` | `26` | `12` | **改為 `628-1(2)+`** |
| 🔴 `628-23(1)+` | `20` | `24` | `12` | **改為 `628-21(1)+`** |

🔒 **落地批之就地加註清單（受改名影響之四名·逐處具名）**——依檔類：

| 類 | 檔（節錄·全集見證據檔） |
|---|---|
| **正典** | `docs/rulings/K-6_街角地分配程序與可分配判準.md`（四名皆命中） |
| **登記表** | `docs/驗證裁定登記表.md`（`628-40(1)+` 命中·四改名者於該檔之命中見證據檔） |
| **報告** | `W-G.9-134`／`-143`／`-149R`／`-150pR`／`-156R`／`-157R`／`-159R`／`-168R` 等 CC 交接文與報告 |
| **baseline／log** | `verify/out/runall_{0e4b508,3e8a8ab,77ebc49,a98a99f,f285b91,W-G.9-169R_post}_full.log`（`628-45(1)+` 各 `7` 列）／`WG9168_viol_full_a98a99f.json`／`WG9169_viol_full_post.json`（各 `16` 列）／`WG9169_z4_winner_a3c97aa.json`（`2` 列） |
| **探針 log** | `probe_WG9147_projorder_e9ad898.log`／`probe_WG9150_endblock_8bdb3e3.log`／`probe_WG9159_projorder_pop_2d72de3.{log,json}`／`probe_WG9167_vr074_pre_6b77b4a.log`／`probe_WG9168_{k6step0,viol_full}_a98a99f.log`／`WG9168_k6step0_a98a99f.json` |

🔒 **已知之土地後果錨（單載·CC 現查坐實）**：`W-G.9-136` 之「`3.5m R5左` winner ＝ `628-45(1)+`」
與 `verify/out/runall_a98a99f_full.log` 之 `[628-45(1)+]` **皆在上表之 `628-45(1)+` 命中內**
⇒ 該名改為 `628-20(2)+` 後，**四份對拍靶與六份全量 log 皆須重產**（與 `GB-111`／`GB-113`／baseline 重烤同批）。

**證據檔**：`verify/out/probe_WG9172_names.log`／`verify/out/WG9172_names.json`（含逐檔之列／次全集）。

### `M-B-5`　期初錨

| 項 | 值 |
|---|---|
| 全量 log | `verify/out/runall_fcad7fd_full.log`（**`505`** 列／`45997` B／`sha16 8bf72ce3e31bc430`） |
| 產生處 | **等長路徑之拋棄式 clone `C:\w174b`**（`8` 字元·`S-3`／`GB-94`）；入倉自**另一未跑長跑之 clone** `C:\w174a` |
| 結果 | `RESULT: FAIL`（`:386`）／`W-V run_all: FAIL`（`:505`）——**准紅碼**（基座即 FAIL） |
| 逐格違規全集 | `verify/out/WG9168_viol_full_fcad7fd.json`（`62795` B／`sha16 1d0c18100a83d4e7`）＋ `probe_WG9168_viol_full_fcad7fd.log`（`8153` B／`sha16 8230ff415b4a6115`） |

🛑 **一處具名之處置**：`probe_WG9168_viol_full.py` **⛔ 未改一字**（`§六` 所令），
惟其 `BASE_REF = "a98a99f"` 係**硬寫**，其輸出檔名恆為 `*_a98a99f.*`。
本批於**拋棄式 clone 內**跑之，再將產物**改名**為 `*_fcad7fd.*` 後複製入倉
——🔒 **改名係必要而⛔ 非便宜**：新舊產物**逐位不同**（json `61187 B` → `62795 B`；log `8621 B` → `8153 B`），
若沿用 `a98a99f` 之名即**覆寫既有錨並使其成為假錨**。⛔ 未觸倉內既有之 `*_a98a99f.*` 二檔。

🔒 **`GB-118` 之案由於本基座獨立重現**：`grep -o "T2-DIAG\] 街廓 R[0-9]" runall_fcad7fd_full.log | sort | uniq -c`
⇒ **`R1`×`4`／`R2`×`1`**、**`R4`／`R5`／`R6` 一筆皆無**；中止逐字（`:72`）＝
`RuntimeError: 🔴 ②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766 > 上界 2.8906（(宗數14−1)×0.005×深度44.47）……停`。

---

## `§四`　取號現查之**自行複現**（`§五-5` 之三數 ＋ 母體 ＋ 態 ＋ 二形字面）

母體 ＝ **全倉 `.md`** ⇒ **`449`** 檔（🔒 已扣量測器自身）；**態 ＝ `fcad7fdd4986ad4b2e80fb3033ed72f824ec91ea`**（期初 tree·`VR-081 一`）。

| 待鑄 | 定義列式（逐字） | 相異號 | MAX | 錨定框：命中列數／出現次數／檔數 | 對照組（同框） |
|---|---|---|---|---|---|
| `VR-082` | `` ^##\s*`VR-([0-9]{1,4})` `` | `66`（單檔）／`67`（全倉） | **`81`** | `0`／`0`／`0` | `VR-081` ＝ `16`／`17`／`4` |
| `GB-118`／`GB-119` | 三框聯集（式表 `112`／式A `3`／式B `29`） | `115` | **`117`**·缺號 `[12, 87]` | 皆 `0`／`0`／`0` | `GB-117` ＝ `8`／`9`／`2` |
| 自誤 `208` | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `172` | **`207`**·缺號數 `35` | 形① `自誤 208` ＝ `0`／`0`／`0`；形② `` 自誤 `208` `` ＝ `0`／`0`／`0` | 形① `自誤 207` ＝ `1`／`1`／`1`；形② `` 自誤 `207` `` ＝ `2`／`2`／`1` |

🔒 **二形之字面**（`§零` 之體例更正·逐列附之）：**形① `X N`**／**形② `` X `N` ``**。
🔒 **算術自檢**：`117 − 2 == 115` True；`207 − 35 == 172` True；`VR`（單檔）`81 − 15 == 66` True。
🔒 **與單載逐項相符**（`GB` 三數與對照組、自誤三數與二形對照組、母體 `449` 檔皆同）。
⚠️ `VR` 之相異號係**母體之別**（單檔 `66`／全倉 `67`·差集 `{VR-075}` 落於 `K-6`）——承 `W-G.9-171R §五-3 A`，**MAX 皆 `81`** ⇒ ⛔ 不影響取號。

### `§五-6`　形之自證 ＋ MAX 推進

| 類 | 期初@`fcad7fd` 相異／MAX | 期末 相異／MAX | **新號** | 新號 ∈ 缺號集？ | 算術自檢 |
|---|---|---|---|---|---|
| `VR` | `66`／`81` | `67`／**`82`** | **`[82]`** | `False` ✅ | `82−15==67` True |
| `GB` | `115`／`117` | `117`／**`119`** | **`[118, 119]`** | `False` ✅ | `119−2==117` True |
| 自誤 | `172`／`207` | `173`／**`208`** | **`[208]`** | `False` ✅ | `208−35==173` True |

⇒ **MAX 推進 ＝ `VR` `81→82`／`GB` `117→119`／自誤 `207→208`**（與 `§五-6` 所令逐項相符）。

---

## `§三`　九項預測之逐項對拍

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `V-1` | shim 可驅動 app 路徑至 `R1`／`R4` 池 | `R1` ✅ 可達；🔴 **`R4` 不可達**（`run_step_g` 於 `R2` `telescoping` 中止）；且 app `main()` 之呼叫端**結構上** harvest 不到 | 🔴 **不符** ⇒ 停機回報所缺（`M-A-1`／`M-A-4`）·⛔ 未改用 harness 版「代替」而係逐項具名其為唯一可執行之 shim |
| `V-2` | `R2`／`R6` 二態逐位相同 | `R2` 五態**逐位相同**（字面成立），🔴 惟其值 `1779.4268` 與外部錨 `1774.20` **相異 `5.23`**；`R6` **不可達** | 🔴 **不符** ⇒ 依停機條件**先判量測器紅**（shim ≠ KL 路徑） |
| `V-3` | `R1`／`R4` 二態之差 ∈ `[0.005, 0.015]` | `R1` 五態之差 ＝ **`0.000000000`**（落區外）；`R4` 不可測 | 🔴 **不符** ⇒ 逐字回報·⛔ **未調整容差** |
| `V-4` | ghost 之幾何**⛔ 未**進入池之扣除 | ✅ **未進入**——`R1` 宗數 ＝ `5` 於**五態皆同**（含 `-169R` 前） | ✅ **符**（現查坐實·⛔ 未以發單側之否證代替） |
| `V-5` | `+0.01` 產於 `-169R` | 四態＋現態逐位相同 ⇒ **⛔ 無法拆分** | ⚪ **不可判** ⇒ 所缺三項已逐字列出·⛔ 未以三批合計代替 |
| `V-6` | 八群 Σ 對上 `W-G.9-136` 之八數 | **`8`／`8` 全中** | ✅ **符** |
| `V-7` | 改名者 `4` 群（R5×2／R6×2），R3 第三群⛔ 不可判 | 改名者 **`4`** 群 ✅（R5×2／R6×2）；🔴 R3 第三群 **可判**（`246.50` vs `246.48`·差 `0.02`）⇒ **不改名** | 🟡 **部分符**——群數相符；「不可判」一句**不符**，依停機條件「以實值為準·⛔ 不得改框」照實出艙 |
| `V-8` | 現況**無**任一宗落在 `inf` | **`0`** 宗（二情境）＋結構性之由 | ✅ **符** |
| `V-9` | 受檔 `deletions` 逐列 `0`；`app.py` blob ＝ `68f7bfab…` | `25/0`／`26/0`／`19/0` ＋ 新增檔；`app.py` ⛔ 不在 `numstat` 內 | ✅ **符** |

**和 ＝ `9`**（符 `5` ／ 不符 `3` ／ 不可判 `1`）✅
🔒 **`S-2` 遵守**：⛔ 未為使預測相符而改探針／框／容差／母體——三處不符與一處不可判**逐項具名**。

---

## `§五-1`／`§五-2`　倉態 ＋ `numstat` ＋ `wg942`

| 項 | 值 |
|---|---|
| 開工閘 `ls-remote` | `fcad7fdd4986ad4b2e80fb3033ed72f824ec91ea`（＝ `S-1`） |
| 拋棄式 clone `HEAD`（全 `40` 碼） | `fcad7fdd4986ad4b2e80fb3033ed72f824ec91ea` |
| **`app.py` blob（期初 ＝ 期末）** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`** |

| 受檔 | 新增 | **刪除** |
|---|---|---|
| `docs/驗證裁定登記表.md`（`P-1`） | `25` | **`0`** |
| `docs/reports/W-G.4_泛用阻塞項登記表.md`（`P-2`） | `26` | **`0`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`P-3`） | `19` | **`0`** |
| 新增檔（本報告 ＋ `3` 探針 ＋ `verify/out/**` 之證據） | — | **`0`** |

🔒 **三 payload 皆末端追加**（`new.startswith(old)` ＝ `True`·`crlf` ＝ `0`）：

| 檔 | 期初 列／B／`sha16` | 期末 列／B／`sha16` |
|---|---|---|
| `docs/驗證裁定登記表.md` | `4109`／`319414`／`c4a037b292b81c5a` | `4134`／`322017`／`ea70bc8d13c9c3db` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `3019`／`482211`／`d758504c073c2aa1` | `3045`／`487910`／`44fa1bae0a7c9f7f` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `3951`／`348196`／`b55d34d9295b127d` | `3970`／`350566`／`6e2aeaa0b38e2cd6` |

🔒 **payload 之取法（常規七）**：`sed -n 'A,Bp' <施工單> > <檔>` **自單身直取**；
三檔 `sha16` ＝ `b4dce46e9335ecfe`（`P-1` `24` 列）／`321a2780686d4e5e`（`P-2` `25` 列）／`bf4fa63cb90e546b`（`P-3` `18` 列）。

🔒 **`run_all.py` 之「禁寫死本機絕對路徑」全倉機檢**：三新探針之命中皆 **`0`**；
判別力對照（一個已知含 `C:\` 之拋棄式腳本）＝ **`1`** ⇒ 量測器非紅。拋棄式腳本已刪、⛔ 未入倉。

🔒 **`wg942_append_audit.py`**：見文末收工聲明。

---

## `§五-8`　報告檔名之擇一（常規三·⛔ 無土地後果）

| 候選 | 檔名 |
|---|---|
| **甲（採）** | `docs/reports/W-G.9-172R_池面積歸因與步驟0前置量測.md` |
| 乙 | `docs/reports/W-G.9-172R_VR082_GB118-119_自誤208.md` |
| 丙 | `docs/reports/W-G.9-172R_shim量測器紅與八群面積.md` |

**一句理由**：採**甲**——與施工單 `W-G.9-172_池面積歸因與步驟0前置量測.md` **同名尾**，
承 `W-G.9-171R` 已立之體例，單與報告以**單一框**即可成對取得。

---

## 🔒 收工聲明

1. 🔴 **`+0.01 ㎡` ⛔ 未歸因**——`V-2` 已觸「先判量測器紅」；`M-A-4` 之所缺三項已逐字列出。
   🛑 **下一步之受詞**（發單側裁）：或修 shim 使其達 `R4`／`R6`（須先解 `GB-118` 之中止），
   或令 KL 於 UI 實跑時輸出逐街廓之池中間量（涉 `GB-119` 之處置·生產碼批）。
2. 🔒 **`裁 A`／`裁 B` 之前置量測全數完成**：八群實值、`R3` 第三群可判、脆弱性清冊 `2` 群、
   `_` 前綴之先例判別力 `0`／`4`、`inf` `0` 宗、改名波及面之三數與逐檔清單。⛔ **未落地**任一裁。
3. 🔒 **⛔ 零生產碼**：`app.py` blob 期初 ＝ 期末；⛔ 未改 `probe_WG9168_viol_full.py`／`HARD_FILES`／
   `fixture_wf_ns_wiring.py` 一字；⛔ 未重產任何 baseline 或凍存名單；⛔ 未追改
   `-161R`／`-163R`／`-169R` 之報告一字；⛔ 未追改任何既有已鑄之號；⛔ 未動 `data/`。
4. 🔒 **長跑於等長路徑（`8` 字元）之拋棄式 clone `C:\w174b`**，入倉自另一未跑長跑之 clone `C:\w174a`；
   五態量測用之 `git worktree`（`/c/s0`〜`/c/s3`）與二 clone 跑畢**整份刪除**。
5. 🔒 **常規一四項自評齊備**（① 零生產碼 ② `deletions` 逐列 `0` ③ `app.py` blob 逐位 ＝ 期望值
   ④ 開工／收工閘全綠）⇒ 依 `§三` 共通逐字**逕行 push**、⛔ 未請示。
