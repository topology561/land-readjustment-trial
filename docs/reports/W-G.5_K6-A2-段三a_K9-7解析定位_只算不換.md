# W-G.5 — **K-6-A2 段三(a)：K-9-7 解析定位·只算不換**

> **基準**：`9d6ae21`　**分支**：`wip/s1-endpart`
> **性質**：**並行實作·未接生產路徑**。生產仍走 `_build_corner_range_v3` 之舊式。
> **🛑 本批結束於施工單 §三(b) 之強制停機點——(c) 切換須待 KL 放行。**
> ⚠️ 所引行號**全部當場重 grep**；差異見 §七。

---

## 一、實作

### 1-A 新增 `k97_solve_alloc_t`（module 級·**零呼叫點**）

`grep -n "def k97_solve_alloc_t" app.py`；
生產未接之確證：`grep -rn "k97_solve_alloc_t" --include="*.py" .`
⇒ 僅命中**定義**與**探針**，生產路徑零呼叫。

**四分支·每支一次除法·各檢查有效區間**（正典 K-9-7 表）：

| 分支 | 條件 | `W(t)` | 有效區間 |
|---|---|---|---|
| ① | `k ≥ 0` | `w₀ + c·t` | 全 `t` |
| ② | `k < 0`、`D` 常數 | `(w₀ + k·D₀) + c·t` | `m·t ≤ 0` |
| ③ | `k < 0`、`D(t)=D₀−m·t` | `(w₀ + k·D₀) + (c − k·m)·t` | `m·t > 0` |
| ④ | **切換點** | ②↔③ 之界 ＝ `m·t = 0` ⇒ `m ≠ 0` 時**恆為 `t = 0`** | — |

⛔ **全函式無迴圈求解**：無二分、無取樣、無迭代兜底。
⛔ **退化一律 loud raise**：`c` 為零／分母為零／`(c − k·m)` 為零／四分支皆無解。

**係數之閉式**（皆非量測）：
`c = σ/den_a`；`k = σ·(−(bn·an)/den_a + (bn·sn)/den_s)`；
`m = g/den_a`（`g = d̂·bn`）；`D₀ = (B0−F1)·bn − t_q·g` ＝ `Q0` 至 BASELINE 之垂距。

### 1-B 後置斷言（＝ **K-9-5-1 ③** 之程式化）

解出 `t*` 後硬驗：所構造範圍之**最小寬 ≥ 退縮＋畸零地最小寬**、**最小深度 > 0**；
任一不成立 ⇒ **loud raise（構造被破壞）**，⛔ 不容忍、不調參。
**本案 16 格全部通過**（見 §三 表之「斷言」欄）。

### 1-C `_build_corner_range_v3` 之 `_t_override`（**純加性·只供對照**）

為使新舊面積走**同一條多邊形構造路徑**（避免另寫幾何），
於 v3 加 `_t_override`（`grep -n "_t_override" app.py`）：
傳入時以該 `t` 建範圍並**跳過自檢①**（該自檢係「回量最小寬 ＝ T」之**定義性回代**，
而覆寫之 `t` 依定義不是該方程之解 ⇒ 硬跑必假紅）；**其餘幾何與自檢一律照舊**。
⛔ **生產路徑一律不傳**（預設 `None` ⇒ 行為不變）。

---

## 二、§六-2 係數對拍：新求解器 vs **正典 K-9-7-c 表**

實料：`verify/out/probe_ruling_K9_7_alloc_t.log`【A】段

| 街廓/側 | `c`(新) | `c`(典) | `k`(新) | `k`(典) | `m`(新) | `m`(典) | 支(新) | 支(典) |
|---|---|---|---|---|---|---|---|---|
| R1/left | −1.003270 | −1.003270 | **+0.009794** | 0.000000 | −0.000410 | （非線性） | ① | ① |
| R1/right | +1.003270 | +1.003270 | **+0.012149** | 0.000000 | −0.000410 | −0.000000 | ① | ① |
| R2/left | +1.002499 | +1.002499 | −0.075547 | −0.075547 | +0.009750 | +0.009750 | ③ | ③ |
| R3/right | +1.004284 | +1.004284 | **+0.075689** | 0.000000 | +0.012109 | （非線性） | ① | ① |
| R4/left | +1.001034 | +1.001034 | −0.047599 | −0.047599 | **+0.000424** | +0.000266 | ③ | ③ |
| R4/right | −1.001034 | −1.001034 | −0.050087 | −0.050087 | **+0.000424** | 0.000000 | ② | ② |
| R5/left | +1.004284 | +1.004284 | −0.075610 | −0.075610 | **−0.047548** | −0.000000 | ② | ② |
| R6/right | −1.003245 | −1.003245 | 0.000000 | 0.000000 | **+0.050197** | 0.000000 | ① | ① |

### 2-1 ✅ 逐格相符者

- **`c`：8/8 完全相符**（六位小數）。
- **分支歸屬：8/8 完全相符**（①①③①③②②①）——**此即決定套哪一條公式者**。
- **`k`：4/4 相符**於正典標為**負值**之四角（R2/left、R4/left、R4/right、R5/left）。

### 2-2 🔬 不符之四項及其解釋（**已查明·非推測**）

**(甲) `k`：正典之 `0.000000` 係「觀測到的不敏感」、非解析之 `∂w/∂d`。**

正典該表明載其取得方式：「斜率係以**擾動 `_build_corner_range_v3` 之輸入、觀測其輸出**取得
（app 為 oracle）」。而分支 ① 之定義即「`k ≥ 0` ⇒ min 恆在 `d=0` ⇒ **深度完全不影響**」
⇒ 擾動深度時**輸出不動** ⇒ 觀測值為 `0.000000`。
⇒ 正典之 `0.000000` 與本批之 `+0.0098／+0.0121／+0.0757` **並不矛盾**：
前者是「輸出對深度之敏感度」，後者是「`w` 對 `d` 之偏導」，
**而正是後者為正，才使前者為零**。⇒ **二者互相印證，非互相牴觸。**

**(乙) `m`：正典標「（非線性）」之兩角，本批之解析值與其自載之「次段斜率」相符。**

正典 K-9-7-c 末段載：R1/left 與 R3/right 之 `D(t)` 有拗點，
「**次段斜率 `−0.000413`／`+0.012109`**」（`grep -n "次段斜率" docs/rulings/K-6_…md`）。

| 角 | 本批解析 `m` | 正典所載次段斜率 | 差 |
|---|---|---|---|
| R1/left | **−0.000410** | −0.000413 | `3e-6` |
| R3/right | **+0.012109** | **+0.012109** | **0（逐位相同）** |

⇒ 本批之 `m` **重現了正典自己量到的次段斜率**；表中「（非線性）」係因**擾動跨過拗點**
所致之量測假象，非本批算錯。

**(丙) 其餘 `m` 之差（R4/left `+0.000424` vs `+0.000266`、R4/right、R5/left、R6/right）
本批＝解析閉式，正典＝跨拗點之擾動觀測。** 同 (乙) 之形狀。
⚠️ **本批不宣稱正典表有誤**——該表已誠實標明其取得方式為擾動觀測；
本批只指出**二者量的不是同一個東西**。

**(丁) 分支歸屬 8/8 相符 ⇒ 公式選擇無分歧。**
⇒ **§六-2 之「不符即停機上呈」：本批據此上呈，並附上述歸因。**
🔴 **請 KL 裁**：正典 K-9-7-c 表之 `k`／`m` 欄是否改標「擾動觀測值（非解析偏導）」，
以免日後被當成解析靶對拍。**本批不擅改正典之實測表。**

---

## 三、🛑 §三(b) 位移表（**停機點·攤給 KL**）

實料：`verify/out/probe_ruling_K9_7_alloc_t.log`【B】段
（面積走**同一條** `_build_corner_range_v3` 構造路徑，只換 `t`）

| 情境 | 街廓/側 | 支 | `t*`(舊) | `t*`(新) | **Δ`t*`** | 面積舊(㎡) | 面積新(㎡) | **Δ面積(㎡)** |
|---|---|---|---|---|---|---|---|---|
| 0m | R1/left | ① | −3.48859 | −3.48859 | **0** | 114.761 | 114.761 | **0** |
| 0m | R1/right | ① | 3.48859 | 3.48859 | **0** | 116.841 | 116.841 | **0** |
| 0m | **R2/left** | ③ | 6.84247 | 6.87391 | **+3.145 cm** | 225.689 | 227.105 | **+1.416** |
| 0m | R3/right | ① | 3.48507 | 3.48507 | **0** | 226.911 | 226.911 | **0** |
| 0m | R4/left | ③ | 5.07029 | 5.07140 | +0.111 cm | 135.150 | 135.187 | +0.037 |
| 0m | R4/right | ② | −5.15255 | −5.15173 | +0.082 cm | 137.031 | 137.004 | −0.027 |
| 0m | **R5/left** | ② | 6.92645 | 6.76085 | **−16.560 cm** | 225.237 | 217.970 | **−7.267** |
| 0m | R6/right | ① | −3.48868 | −3.48868 | **0** | 145.883 | 145.883 | **0** |
| 3.5m | R1/left | ① | −6.97719 | −6.97719 | **0** | 230.810 | 230.810 | **0** |
| 3.5m | R1/right | ① | 6.97719 | 6.97719 | **0** | 232.793 | 232.793 | **0** |
| 3.5m | **R2/left** | ③ | 10.33374 | 10.36262 | **+2.888 cm** | 382.851 | 384.151 | **+1.300** |
| 3.5m | R3/right | ① | 6.97014 | 6.97014 | **0** | 383.667 | 383.667 | **0** |
| 3.5m | R4/left | ③ | 8.56667 | 8.56771 | +0.104 cm | 251.080 | 251.115 | +0.035 |
| 3.5m | R4/right | ② | −8.64893 | −8.64812 | +0.082 cm | 252.882 | 252.855 | −0.027 |
| 3.5m | **R5/left** | ② | 10.41152 | 10.24592 | **−16.560 cm** | 378.465 | 371.171 | **−7.294** |
| 3.5m | R6/right | ① | −6.97736 | −6.97736 | **0** | 298.145 | 298.145 | **0** |

**⇒ 16 格中 8 格有位移**（＝分支 ②③ 之四角 × 兩情境）；
**分支 ① 之四角（R1 左右、R3 右、R6 右）位移恰為零**
——與理論一致（`k ≥ 0` ⇒ 深度不影響 ⇒ 換帶深定義不改結果）。

### 3-1 用土地語言講

| 街角 | 街角規定範圍怎麼移 | 面積怎麼變 |
|---|---|---|
| **R5 左** | **往街角方向縮回 16.6 公分**（範圍變小） | **少 7.27／7.29 ㎡** |
| **R2 左** | 往街廓內多推 2.9〜3.1 公分（範圍變大） | 多 1.30／1.42 ㎡ |
| R4 左 | 往內多推 0.1 公分 | 多 0.04 ㎡ |
| R4 右 | 移 0.08 公分 | 少 0.03 ㎡ |
| R1 左右／R3 右／R6 右 | **不動** | **不變** |

🔴 **R5 左之 −7.27㎡ 係本批最大者**——街角規定面積是**街角第 1 宗 PK 資格**之門檻
（`G ≥ 街角規定面積`），門檻下修 7.27㎡ **可能使原本不夠格者變夠格**
⇒ **有翻盤之虞**。
⚠️ **本批未評估 PK 是否真翻盤**（需跑 PK 全鏈），**不擅自斷言「差很小可忽略」**
——依施工單 §三(b)：**位移不為零 ⇒ 停機上呈**。

### 3-2 位移之機制（**已實測坐實·非推測**）

新舊之唯一差別＝**帶深**：舊式用**街廓平均深度** `D_avg`，
新式用**範圍自身之最小深度** `D₀`（＝ `Q0` 至 BASELINE 之垂距）。
於分支 **②**（`k<0`、`D` 常數），二式之差為閉式：

```
Δt* = −k·(D₀ − D_avg) / c
```

**逐角實測**（`0m`）：

| 角 | `D₀`（範圍自身） | `D_avg`（街廓平均） | `D₀ − D_avg` | `k` | Δ`t*` 理論(cm) | Δ`t*` 實測(cm) |
|---|---|---|---|---|---|---|
| R1/left | 33.1639 | 33.1500 | +0.0139 | **+0.0098** | 0（①） | **0** |
| R1/right | 33.1283 | 33.1500 | −0.0217 | **+0.0121** | 0（①） | **0** |
| R2/left | 44.9543 | 44.4700 | +0.4843 | −0.0755 | +3.650※ | +3.145 |
| R3/right | 44.8981 | 44.3400 | +0.5581 | **+0.0757** | 0（①） | **0** |
| R4/left | 33.1256 | 33.1000 | +0.0256 | −0.0476 | +0.122※ | +0.111 |
| R4/right | 33.0837 | 33.1000 | −0.0163 | −0.0501 | **+0.082** | **+0.082** |
| **R5/left** | **43.5104** | **45.7100** | **−2.1996** | −0.0756 | **−16.560** | **−16.560** |
| R6/right | 43.3615 | 45.5100 | **−2.1485** | **0.000000** | 0（①） | **0** |

※ 分支 **③** 之斜率為 `(c − k·m)` 非 `c` ⇒ 上式（②之形）不完全適用，故理論值略異於實測——
**此差本身即 `(c − k·m)` 修正之證據**。分支 ② 之兩角（R4/right、**R5/left**）
**理論與實測逐位相同**。

**⇒ R5 左位移最大之由**：其 `D₀ − D_avg = −2.1996 m`，**為八角之最**。
R5 之前緣線兩端垂距為 `43.510 / 47.904`（**相差 4.39m·全區最斜**）
⇒ 街廓平均 `45.71` 距左角實際深度 `43.51` 甚遠。

**⚠️ 對照組（自我否證用）**：**R6/right 之 `D₀ − D_avg = −2.1485`（幾乎同樣大）
卻位移為零**——因其 `k = 0.000000`（分支 ①）。
⇒ **深度差大不足以致位移，須 `k < 0` 方生效**；二者並列即排除「純粹深度差驅動」之誤讀。

### 3-3 🔬 `D₀` 之獨立交叉驗證（**非本批自產之期望值**）

本批之 `D₀` 係由 `k97_solve_alloc_t` 之閉式算出。
其八個值**與 K-9-2 探針所量之「前緣線兩端垂距」逐位相符**
（`verify/out/probe_ruling_K9_block_depth.log`【A】段·**段一即已入庫**）：

| 角 | 本批 `D₀` | K-9-2 兩端垂距（對應端） |
|---|---|---|
| R1/left · right | 33.1639 · 33.1283 | 33.164 · 33.128 |
| R2/left | 44.9543 | 44.954 |
| R3/right | 44.8981 | 44.898 |
| R4/left · right | 33.1256 · 33.0837 | 33.126 · 33.084 |
| R5/left | 43.5104 | 43.510 |
| R6/right | 43.3615 | 43.361 |

⇒ **`D₀` 之正確性由一條完全獨立、且早於本批入庫之量測鏈背書**
（該鏈本身又對拍正典 K-8 §二 兩端垂距表）。

---

## 四、§五 分支覆蓋之合成案

⚠️ **本批未做。** 施工單 §五 令「①×截角、③×截角、切換點支（④）本案無實例，須以合成案補」。

**未做之由（技術阻斷·非省略）**：`P_front(t*)` 落截角與否，於本案 **16 格全為否**
（`verify/out/probe_ruling_K9_7_alloc_t.log`【D】段）。
而 **K-9-7-d 之第二層分支（`P_block` 落截角邊 vs FRONT 重疊段）
需 K-9-8 之 `PQ` 延伸線段**方能定義（正典 K-9-6-b-1：量測用之「道路境界線」
＝ K-9-8 (3) 之延伸線段），**K-9-8 屬段四、尚未實作**
⇒ 現在造「×截角」之合成案，等於**先臆造 K-9-8 之構造**，違「禁把未驗證的機制寫進碼」。

⇒ **建議**：①×截角、③×截角、④ 切換點之合成案**併入段四（K-9-8）**一併做。
**請 KL 裁**是否同意此順序調整。

（本批已實作 ④ 之**明列**：②↔③ 切換點 ＝ `m·t = 0` ⇒ `m ≠ 0` 時恆為 `t = 0`，
見【B】表「切換點」欄。）

---

## 五、`run_all`

實料：`verify/out/K6A2_seg3a_runall.log`（`EXIT=1`·一般模式·未用 `WV_BAKE`）
⇒ `132 名目／86 PASS／46 FAIL`。

### 5-1 名目集合 diff 之實際輸出行

```
$ diff <(K8_seg3_C 靶 FAIL 名目 sort) <(段三a 輪 FAIL 名目 sort)
  (無輸出)
$ diff <(K8_seg3_C 靶 PASS 名目 sort) <(段三a 輪 PASS 名目 sort)
  (無輸出)
$ diff <(F-2 輪 FAIL 名目 sort) <(段三a 輪 FAIL 名目 sort)
  (無輸出)
$ diff <(F-2 輪 PASS 名目 sort) <(段三a 輪 PASS 名目 sort)
  (無輸出)
```

⇒ **四組皆空** ⇒ **進／出之名目各為 0，無成因可列。**

### 5-2 ⚠️ 「允許變動」之條款於本批不適用

施工單 §六-1 令「`run_all` 名目集合**允許變動**，但須逐格列出進／出並附成因」。
**該條款之前提是生產路徑已切換**（＝**段三(c)**）。
本批係 **(a) 只算不換**：`k97_solve_alloc_t` **零生產呼叫點**、
`_t_override` **預設 `None` 且生產不傳** ⇒ **生產行為本就不應變**
⇒ **零進零出才是本批之正確結果**；若有變動反而是接線洩漏。

**零接線之確證**：

```
$ grep -rn "k97_solve_alloc_t" --include="*.py" .        ⇒ 僅命中定義（app.py）與探針
$ ast 掃描 main() 內之 k97_solve_alloc_t 呼叫             ⇒ 無 ✅
$ grep -rn "_t_override=" --include="*.py" . | grep -v None ⇒ 僅命中探針 1 處
```

### 5-3 直接覆蓋被改檔之夾具

```
末端夾具 fixture_corner_range_k8.py（K-8 §三〜§五 街角規定範圍新構造·構造/單調性自檢＋判別力反例）rc=0
```

⇒ `_build_corner_range_v3` 加 `_t_override` 後**仍 PASS** ⇒ 該參數之純加性成立。

### 5-4 🔴 覆蓋誠實聲明

`k97_solve_alloc_t` **未被 `run_all` 之任何名目執行**（零生產呼叫點）
⇒ 上表**不證明新求解器正確**，只證明「**未波及生產**」。
新求解器之正確性由 **§二 係數對拍**（vs 正典 K-9-7-c 表）
＋ **§3-3 `D₀` 交叉驗證**（vs 段一即已入庫之 K-9-2 量測鏈）承擔。

---

## 六、🔴 K-9-7-d 第二層分支之依賴循環（**上呈·非本批可解**）

施工單 §順序更正 已載：「K-9-8 之 `L_in` 位置由 K-9-7 之 `t*` 決定（正典 `:683`）」。
而本批發現**反向依賴亦成立**：

- **K-9-7-d 之第二層分支**（`P` 落截角邊 vs FRONT 重疊段）**需 K-9-8 之 `PQ`**
  （K-9-6-b-1：量測用之道路境界線 ＝ K-9-8 (3) 之延伸線段）。

⇒ **K-9-7 之完整 8 支 ⟷ K-9-8 之 `PQ`：互為前提。**

**本批之處置**：只實作**K-9-7 單獨即可完全決定**之四分支（①②③④），
第二層分支**只偵測、不臆造**（回傳 `P_front_on_chamfer` 旗標，本案 16 格全 False）。
🔴 **請 KL 裁**該循環之切法（例：段四先以「退化支」假設實作 K-9-8，再回頭補 K-9-7 之第二層）。

---

## 七、施工單與倉內實況之差異（§八-3）

| 施工單所載 | 倉內實況（`9d6ae21` 當場 grep） | 處置 |
|---|---|---|
| `_build_corner_range_v3` `app.py:9568` | 段三-0 後移至 `:9769`（正典段新增所致） | 當場 grep·未沿用 |
| `k = sigma / den_a` `app.py:9691` | 移至 `:9892` | 同上 |
| 正典 `:589-636`／`:623-632`／`:634`／`:636`／`:657-675` | 段三-0 後行號整體位移 | 以**標題 grep** 定位 |
| §六-2 「不符即停機上呈」 | **7/8 格 `k` 或 `m` 不符** | **已上呈＋附歸因**（§二-2）；**`c` 與分支歸屬 8/8 相符** |
| §五 合成案（①×截角／③×截角／④） | **需 K-9-8 之 `PQ`（段四）** | **本批未做·已述阻斷理由**（§四），建議併段四 |
| §三(a) 「最多 8 支」 | 第二層分支與 K-9-8 **互為前提** | 實作 4 支＋偵測旗標；循環上呈（§六） |
