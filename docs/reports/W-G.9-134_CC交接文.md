# 🧾 `W-G.9-134`　**CC 交接文**（新 session 接手用·`W-G.9-133′` 收束時出）

**出文窗**：CC（本 session context 達 90%）　**倉態基座**：`0145d3b0ef8b9ce4da56e0adc5a5a176e88ba287`
**性質**：🔒 **純追加**·⛔ 零生產碼·⛔ 零管線

---

## §一　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md`**（`543`＋`32` 列·末端有 `W-G.9-132` 之進度表更正）。
2. 🛑 **讀本檔全文**，再讀 `docs/rulings/K-6_街角地分配程序與可分配判準.md` 之**全部節標題**
   （`3130` 列·🔴 **`自誤 161` 之防線**：設計任何量測前須先列該正典之全部節標題並逐節判「是否為本受詞之規格」，⛔ 不得只搜關鍵詞）。
3. 🛑 **開工閘**（每張施工單皆同）：
   `git -c core.autocrlf=false clone --branch wip/s1-endpart <url> /c/wNNN` ⇒ `cd` ⇒ `git config core.autocrlf false`
   ⚠️ **clone 路徑須短**（`MAX_PATH`）；**⛔ 不得放 scratchpad**。
   ⚠️ 若須二 clone 逐位對拍 log，**二者路徑須<u>同長度</u>**（`W-G.9-123` 之教訓：截斷點落在固定 byte offset）。

---

## §二　倉態（態 ＝ `blob@0145d3b`·CC 現查）

| 項 | 值 |
|---|---|
| `origin/wip/s1-endpart` | `0145d3b0ef8b9ce4da56e0adc5a5a176e88ba287` |
| `app.py` blob | `411c4ff7d924b502f27081144130f2e8bf0bc47b`（`22421` 列） |
| 取號 | 自誤 `max 164`／`GB` `max 103`／`K-9` `max 20` |
| `K-6` 正典 | `3130` 列 |

🔴 **本波先前各單所載之 `app.py` 行號<u>全部失效</u>**——`W-G.9-131` 已使其增 `205` 列。**一律 `grep` 現查**。

---

## §三　本波之位置（🛑 KL 決 `2026-08-24`：**丙 → 乙 → 甲**）

| 案 | 受詞 | 狀態 |
|---|---|---|
| **丙** | `K-6 §一` 幾何連通判定之新建 | ✅ **已落地**（`W-G.9-131`·`f4c5385`·⛔ 零接線·⛔ 零行為變更） |
| **乙** | `K-6 §二` 步驟 0 ＋ 段一〜段四 | 🔶 **步驟 0 已量測完**（`W-G.9-133′`）·**落地係下一單 `W-G.9-134`** |
| **甲** | `K-6-A2` 之剩餘六項 | ⬜ 未開工 |

### 丙案已備之地基（`app.py` 模組層·⛔ 零接線）

| 符號 | 現查行號 | 要義 |
|---|---|---|
| `K6_SHARE_MIN_LEN = 0.01` | `:11070` | 共用線段之**長度門檻**·⛔ **非座標容差** |
| `k6_shares_segment(poly_a, poly_b, min_len=None) -> (bool, float)` | `:11076`〜`:11113` | 二片**邊界**交集之**線性**部分總長；`Point` ⇒ `0` ⇒ ⛔ 不連通 |
| `k6_merge_groups(parcels, own_map, min_len=None) -> list[list[int]]` | `:11116`〜`:11167` | 同 `gid` 建圖取**連通分量**·**決定性** |
| `k6_merge_selftest()` | `:11170`〜`:11257` | 七造·⛔ **不於 import 時自動執行** |

🔒 **零接線之驗法**：二函式於 `app.py` 之命中（扣 `def`）須**全部落在 `:11076`〜`:11257`**；`verify/` 命中須 `0`。

---

## §四　下一單（`W-G.9-134`）之受詞與**三個前置**

**受詞** ＝ `K-6 §二 步驟 0`（同街廓相鄰合併）之**落地**。正典逐字（`K-6:57-60`）：

> 同街廓 ∧ 同歸戶 ∧ 相鄰 ⇒ 視為同一筆：**面積相加、幾何取聯集、佔單一原位次**
> （位次由聯集幾何依 `_projection_order` 導出，**禁另訂**）。命名 `{投影序在前者之暫編地號}+`；
> 清冊增「**合併組成**」「**合併依據**」兩欄。

### 🛑 前置一：受詞係**暫編層**（`temp_parcels`），⛔ 非原地號層

`W-G.9-133` 曾令以 `parcel_polygons`（原地號層）⇒ **錯一層**（發單側自誤 `167`）。
🔒 **實證（`W-G.9-133′ A-6`）**：二法之群劃分**相異**——原地號層片 `53`／片數≥2 之群 `9`；
**暫編層（逐街廓）片 `126`／片數≥2 之群 `17`**。僅暫編層有者（原地號集合）＝
`['628','628-1']`／`['628-12','628-13','628-16']`／`['628-22','628-23']`／`['628-30','628-45']`／`['628-40','628-43']`。

### 🛑 前置二：面積欄須用 `幾何面積_m2`，⛔ **不得用 `面積_m2`**

`temp_parcels` 之 **`面積_m2` 於該階段恆為 `0`**（非零 `0/126`）——四欄面積係 `build_parcels` **之後**才填。
`幾何面積_m2` 非零 `118/126`·Σ `34920.83`（與 `shapely` 實算逐格相符）。

### 🛑 前置三：`K-6:59` 之命名規則在無 `FRONT_LINE` 之街廓⛔ **不可施用**（**須 KL 裁**）

十七群中 **`8` 群**（`G1` 四群、`RD1` 一群、`RD2` 二群、`RD4` 一群）**投影序不可定義** ⇒ `{投影序在前者之暫編地號}+` 無從導出。
可定義者九群之命名：群5 `628-40(1)+`／群6 `628-30(3)+`／群7 `628-47(1)+`／**群8 `628-29(1)+`**／群9 `628(1)+`／群10 `628-21(2)+`／群11 `628-45(1)+`／群12 `628(2)+`／群13 `628-23(1)+`。

### 其餘已備之事實（`W-G.9-133′`）

- **落地點之建議**（⚠️ 係建議·⛔ 非決定）：插在 `overlay_polygons_to_blocks`（`:4314` def／`:16604` 呼叫）**之後**，即 `temp_parcels` 層——該層已同時具 `原地號`／`所屬街廓`／`polygon_coords` 三鍵。下游數：`overlay` `4`／`temp_parcels` `144`／`build_parcels` `251`／`by_blk` `9`。
- **`unary_union`**：十七群**皆單一 `Polygon`** ⇒ ⛔ 無 `MultiPolygon` 之處置問題。
- **清冊二欄**：`合併組成`／`合併依據` 於 `app.py`／`verify/` 命中**皆 `0`** ⇒ 係新增受詞。
- **街廓分配線兩側天然⛔ 不併**：`overlay` 係「× 每個街廓」⇒ `628` ⇒ `628(1)@R4`／`628(2)@R6` ⇒ ⛔ 不需另作處理。
- **土地後果之射程**：三側強制抵費地中，僅 **`R3` 右**有同群對（群8 `628-28(1)`／`628-29(1)`·`Σ幾何面積 228.9500 ㎡`／門檻 `307.79`·比 `0.744`）
  🛑 **該比⛔ 非 `G`、⛔ 不可據以判達標**（`G(Σa) ≠ ΣG(a)`·自誤 `159`）。
- **KL 圖說之「被他人土地隔開」確有實例**：`G1` 街廓內 `gid = G003` 之片被 `G016` 之 `718(1)`／`719(1)` 隔開（共用長皆 `0.000000000`）。

---

## §五　積欠登記（🛑 下一登記批一次入倉）

| # | 項 |
|---|---|
| `1` | 自誤 `165`（`W-G.9-132 §二 2-1` 之縮排陳述不自洽·**第三度**）＋ **`sha256` 反推之家法**（CC 立·發單側採認） |
| `2` | 自誤 `166`（`W-G.9-133 A-5` 之門檻自檢⛔ 無鑑別力——`min_len=0` 使判準退化為恆真） |
| `3` | 自誤 `167`（`W-G.9-133` 之受詞錯一層） |
| `4` | 🆕 **自誤 `168`（CC 立）**：`W-G.9-133′` 首版取 `面積_m2` 而該欄恆 `0` ⇒ `Σ面積` 全成 `0.0000`（依戒「`0` 最該懷疑」當場自糾） |
| `5` | 🆕 **待裁項**：`K-6:59` 之命名規則於無 `FRONT_LINE` 之街廓不可施用（`8` 群） |
| `6` | `W-G.9-130` 偵察報告入倉（**已准**·須另定路徑與格式） |
| `7` | 九份 baseline 重產（須 KL 決時點） |

---

## §六　本波所立之家法與陷阱（🛑 接手者必讀）

| # | 家法／陷阱 | 出處 |
|---|---|---|
| `1` | 🛑 **凡對某機制設計量測前，須先列出其正典檔之<u>全部節標題</u>並逐節判「是否為本受詞之規格」**；⛔ 不得只搜關鍵詞 | 自誤 `161`（最重） |
| `2` | 🛑 **凡多個布林維度並存者，驗收須跑其<u>笛卡兒積</u>**，⛔ 不得各維各測一造 | 自誤 `151`／`GB-100` |
| `3` | 🛑 **凡以「某邊 ∥ 某族」為判準者，須先證該族與對照族之方向集合⛔ 不重疊** | 自誤 `156` |
| `4` | 🛑 **參數值之斷言須 spy 其<u>實收值</u>**，⛔ 不得由 docstring／命名推定 | `W-G.9-127` |
| `5` | 🛑 **凡以 `G` 或 `幾何面積` 跨環境對拍者，容差一律 `±0.05 ㎡`** | `GB-101` |
| `6` | 🛑 **跨圖資／跨環境對拍法定記載值者須併報全精度值** | `GB-102` |
| `7` | 🛑 **改任一布林判準：改前以舊式裸樣式全檔 `grep` 得 `N`，改後新式須得同一 `N`、舊式須得 `0`** | `GB-100` |
| `8` | 🔧 **施工單縮排有歧義時，以其<u>期末 `sha256` 反推</u>**（窮舉組合取唯一命中） | `W-G.9-132` |
| `9` | 🛑 **`CLAUDE.md` 在 `wg942_append_audit` 之 `TARGETS`（`77` 筆）內 ⇒ ⛔ 不得就地改字，須末端追加**；`verify/run_verification.py` **不在**其內故得就地 | `W-G.9-132` |
| `10` | 🛑 **「`0` 最該懷疑」**：任何量測得 `0` 或「與前次逐格相同」時，先疑欄名錯／沒讀到新資料 | 本波三度自糾 |
| `11` | 🛑 **命中二框**（`grep -c` 命中列數 vs `str.count` 出現次數）⛔ 不得同表混用 | `VR-058` |
| `12` | 🛑 **`tokenize` 排除 `STRING` 會把 dict 鍵名一併排除** ⇒ 判「程式碼命中」時須只排 `COMMENT` | `W-G.9-130` |
| `13` | 🛑 **`run_all`／`run_verification` 須於<u>拋棄式 clone</u> 內跑**（會改寫 `verify/out/**` 之已追蹤 log） | `GB-94` |
| `14` | 🛑 **`git add` 具名逐檔**，⛔ 不得用 `-A`；暫存檔（commit 訊息／腳本／log）**一律寫在 clone 之外** | `a47e758` 之覆轍 |

---

## §七　已知之 harness 限制（量測時必先知）

- 🔴 **`verify/run_verification.py` 跑不完整**：`RESULT: FAIL`／`PASS 12`／`FAIL 22`；於 `R2`／`R4` 撞閘而中止。
  三個擋路 raise 中**二個係 harness 專屬**（`結構閘 telescoping 破`／`結構閘 理論＝實跑 破`·`app.py` 命中 `0`）。
- 🔧 **取 `g_rows` 之四路徑**（`W-G.9-125` 立）：①全跑 ⇒ R2 raise ②縮 `cb` ⇒ 撞 `C 兩形分岔`（**`C` 閘在 `stepg:172`·街廓迴圈 `:402` <u>之前</u>** ⇒ 財務係全區量·子集必炸）③app 路徑無法 headless
  **④成功**：`cb` 完整 ＋ **重排 `build`** 使目標街廓先跑 ＋ **自 `exc.__traceback__` 之 `run_step_g` frame 讀 `f_locals['g_rows']`**。重排無影響之證 ＝ `R1` 之列逐格全等。
- 🔧 **`app.py` 之真符號取得**：`sys.path.insert(0, '<clone>/verify')` ⇒ `from app_harvest import harvest` ⇒ `ns, fake_st = harvest()`；
  `ns['<符號名>']` 即真函式（⛔ 不得 `import app`——會跑 Streamlit UI）。
- ⚠️ **`solve_G_binary` 之回傳已 `round(_,2)`**（`'G'`／`'S'`／`'W'`）⇒ 全精度須自 `_block_strip` 之**第二回傳值**取。
- ⚠️ **`負擔比率` 欄係 `round(_,4)`** ⇒ ⛔ 不可用以驗算 `G`。

---

## §八　memory 之指引（新 session 自動載入 `MEMORY.md`）

接手後**至少須讀**下列記憶檔（`MEMORY.md` 之索引列已含其鉤子）：

`wg9133p-step0-temp-layer`（**本波正解**）／`wg9131-k6-connectivity`（丙案）／`wg9130-k6b-recon`（`K-6-A2`／`K-6-B` 偵察）／
`wg9129-corner-pk-per-lot`（街角 PK 係逐宗）／`wg9128-registry-batch`／`push-before-shipping-claim`（🔒 **未 push 不得報收官**）／
`grep-multibyte-charclass-trap`（量測器靜默給錯數之三十七面）／`app-vs-harness-stepg-divergence`／`dxf-source-divergence`。

---

## §九　回報紀律（CLAUDE.md 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：收官前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值。
- 🔒 **拋棄式 clone 跑畢整份刪除**，並於回報中確認。
