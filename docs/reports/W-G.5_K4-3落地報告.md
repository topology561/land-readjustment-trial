# W-G.5 — **K-4-3 來源母體**落地報告（＋E-10 歸因三度更正／U-K2 extras 帳／s_max_left 斷言）

> 起點 `8a697c2`。上游＝KL 裁 K-4-3（2026-07-27·解除任務 2 停機）＋ BLOCKED-A ＋ U-K2 ＋ 四項附帶。
> plan＝`docs/specs/W-G.5_K4-3_來源母體_plan.md`（reviewer 判 **BLOCKED×3 ＋ WARNING×9**·全數處置見 §六）。
> **行號一律當場 grep**。

---

## 〇、一句話

K-4-3 已落地，**UC9898 零行為變動**（六變體 award／winner／allocs／registry 逐格全等）；
真正的收穫是 reviewer 打掉的三個錯——其中兩個是**我自己剛寫下的正典文字**。

---

## 一、K-4-3 施工（`verify/m_rescue.py`）

| 項 | 處置 |
|---|---|
| **A1** 刪同塊排除 | 來源篩選迴圈內之 `if sr.get("所屬街廓") == A.get(pid,{}).get("所屬街廓"): continue` **整段刪** |
| **A1′** 檔頭修正 | 「M-2『其他可建築街廓』要件**不變**」→「於 **①（街角救援）經 K-4-3 排除適用**」＋裁定日 |
| **A2** 窮盡搜尋 | 見 §二（reviewer 獨立跑四道·CONFIRMED） |
| **A3** 加 (c) | `_source_ok` 之 (c)：`G_A₀(spid) < gtrue`；基準**釘死合併前 `gtrue`**（禁隨合併進度放寬·否則成回饋迴路）；`G_A₀` 取 **趟0 trunk A₀**（與 U₀ 同源）；缺值 **loud raise** |
| **A4** 結算層硬閘 | `ConsumedRegistry.set_u0()` ＋ `claim` 內斷言 `spid ∈ U0`，破則 loud、訊息引 K-4-3 題二；**未注入即呼 `claim` 亦 loud**（禁預設放行） |
| **A5** 述詞抽 module 級 | `_source_ok`（**不含 `tgt_blk`**·見 §六 WARNING-5） |
| **NOTE-4** | `U0` 建構改走同一 `source_g_a0` ⇒ (b)/(c) **同一口徑、同時 loud**（舊式缺值靜默作 0，兩條相反） |

### 🔒 A4 之身分（**不得被引用為「已驗之閘」**）
reviewer 實證：`claim` 之全部呼叫點（①補足／③餘額）之來源皆已由 `_source_ok` 的 (b) 濾過，
② 完全不呼叫 `claim` ⇒ **本閘現為恆真**。
其價值在**凍結未來新增之 `claim` 呼叫點**，**不是** (b) 的獨立佐證
（那是 `rot90(rot90(x))∥x` 同族）。已寫入 docstring。

---

## 二、A2 窮盡搜尋（否定性存在主張·N0-17-c）

reviewer 獨立跑四道，命中如下：

| 指令 | 結果 |
|---|---|
| `grep -rn "所屬街廓.*==.*所屬街廓" --include=*.py .` | **1 命中**＝標的 |
| `grep -rn "所屬街廓" --include=*.py . \| grep -E "==\|!="` | 17 命中，逐條判：app UI 篩選 5／標的 1／探針取組 1／`wd4_tier_list`＋`wf_f0~f4` 之「取該塊之列」投影 10 ⇒ **皆非來源排除** |
| `grep -rnE "\b(sblk\|tgt_blk\|src_blk\|_blk[0-9]?\|blk_of\|blkA\|blkB)\b\s*(==\|!=)"` | **0 命中** |
| `grep -rn "其他可建築街廓\|同塊\|跨街廓\|同街廓"` | `verify/` 內僅 `m_rescue.py` 檔頭與該註解 |

⇒ **「只此一處」在 M-5 來源述詞範圍內成立**。
**適用界（已寫入檔頭）**：`wf_f2` 之級1/2/3 塊級鄰接（`frozenset((sblk, tgt_blk)) in adj`）
**屬另一機制·不受本條影響**——否則日後會被讀成「全倉再無塊級來源限制」。

---

## 三、六變體量測（`verify/probes/probe_ruling_K4_3_source.py`·已掛 run_all）

**0m 情境無 forced 端 ⇒ M-5 根本不執行**（探針已於表頭明寫，**該空表不得讀為「K-4-3 無衝擊」之證據**）。
3.5m forced 端＝`R2left／R3right／R5left`。

| 變體 | 同塊排除 | (c) | award/winner/allocs/registry | 同塊源**實際納入候選池** |
|---|---|---|---|---|
| **A** 現況 | 有 | 無 | 基準 | ∅ |
| **A′** 載具自檢 | 有 | 無 | **逐格全等** ✅ | ∅ |
| **B** 只刪同塊排除 | 無 | 無 | 逐格全等 | `628-28(1)`,`628-29(1)`,`628-30(1)`,`628-43(1)` |
| **C** 只加 (c) | 有 | gtrue | 逐格全等 | ∅ |
| **D ＝K-4-3** | 無 | gtrue | 逐格全等 | 同 B（**4 筆**） |
| **D′** (c) 改 A₀ 口徑 | 無 | A₀ | 逐格全等 | **3 筆**（`628-29(1)` 掉出）|

- **②`_t2` 六變體皆未觸發**（reviewer WARNING-2 之對治欄）。
- **同塊源雖合格但未被消費**（唯一 award 之 registry 源＝`628-30(1)`/`628-30(2)`/`628-45(1)`，
  對其 target `628-45(2)`@R3 而言**全為他塊**）⇒ **WARNING-3 之位次序風險為潛伏、本案未觸發**。
- 唯一 award：`R3right winner=628-45(2) need_p=172.63 g_at=308.93 stage3=97.37`。

📌 **措辭**（M-5(1) 概念更正）：forced 解鎖 ＝ **抵費地位置由街角移回中間**（總量不變），
**禁「釋回／減少抵費地」語**。本波無 forced 解鎖發生。

---

## 四、🚩 U-K3：(c) 之右式口徑（**已量出差異·未裁**）

K-4 第 3 條原文：「判**該試算 G** 是否為同歸戶中**應分配面積**最小者」
⇒ 左式＝來源宗之**原位次**應分配面積、右式＝候選之**街角第 1 槽試算 G**，**跨口徑係原文如此**。

**副作用（reviewer 活抓·我已逐格復現）**：`_corner_first_lot_G` **只吃 `a`**
⇒ 同 `a` 之同歸戶宗 `gtrue` **相同** ⇒ 二者**互為合格來源**、(c) 於該對**喪失反對稱性**：

| 候選 | `gtrue` | 來源 | 來源 `G_A₀` | (c)@gtrue | (c)@A₀ |
|---|---|---|---|---|---|
| `628-28(1)` | 65.61 | `628-29(1)` | 64.78 | ✅ | ❌ |
| `628-29(1)` | 65.61 | `628-28(1)` | 63.90 | ✅ | ✅ |

（二者 `a` 皆 114.00、同屬 R3、同 gid `G014`。）

⇒ **`gtrue` 讀法下「小往較大集中」退化為「兩邊都行」**，誰吃誰由端序與排序偶然決定。
**本案兩讀法結果相同**（見 §三：六變體逐格全等）⇒ **不擋施工**，
但**泛用化前須裁**：K-4-3 (c) 之「該街角地候選之應分配面積」＝原位次 A₀ G，抑或街角槽試算 G？
併請裁 **tie 規則**（2dp 下 `G` 相等時 `<` 判去或判留）。

---

## 五、E-10 歸因**三度更正**（BLOCKED-A）＋ 一處假數字勘誤

### 5.1 決定性實驗（同宗 R3·`628(4)`·**同絕對座標**·只換 T）

| T | GEOS@絕對座標 | 解析 | 差 |
|---|---|---|---|
| **1.230729e-08**（＝**E-9 量測點**·前緣邊之 t） | 1.01575637 | 1.02653549 | **−1.0779e-02** |
| 1e-5／1e-3／**1e-2**／0.1／1／7／14 | 1.02653549 | 1.02653549 | **~1e-10**（1e-2 處 **+2.562e-10**） |
| **同一 T 但改於 (s,t) 局部框** | 1.02653549 | 1.02653549 | **+2.22e-16** |

### 5.2 三個版本的歸因

| 版 | 歸因 | 隱含施工單 | 判決 |
|---|---|---|---|
| ① | 資料性質（ALLOC 非平行／碎宗臨街邊未 canonical 化） | 放寬 E-6 規格 | **錯**（#31） |
| ② | 「**TWD97 量級**下 GEOS 少算」 | 換函式庫／全域平移 | **錯**——暗示誤差到處都有，實測其餘深度全 ~1e-10 |
| ③ | **量測線與前緣邊近乎共線** | 改弦長算法，且**綁定產生該量測點的規格** | ✅ |

⇒ **硬前置 ＝「E-9 不得在絕對大地座標下取弦」**。
**⚠️ 第四次過頭（reviewer 當場擋下）**：我原本收尾寫「**解析弦**是 E-9 的硬前置」——
表格最後一行就是反例：**只換局部框、同一 T、同樣用 GEOS**，誤差即 2.22e-16。
局部框與解析式**擇一即消滅** 1.08e-2；解析式之**獨立**價值是消滅退避法與相切分支
（＝E-3′/E-8b 之可靠性），**那不是「E-9 的硬前置」**。已改為 reviewer 之措辭。

### 5.3 🔴 舊碼特徵值勘誤（WARNING-6）

`app.py` docstring 前版寫「**±1e5 超長切線**」與「**1.015735**」——**皆非舊碼實況**。
舊碼之輔助線半長為 `_big = max(bounds寬, bounds高) × 4 + 100`（R3 `628(4)` 實測 **≈230.6**），
其誤差為 **−1.077912e-02**（→ **1.01575637**）。
`1.015735`／`±1e5` 係**我 recon 腳本之組態、從未上線** ⇒ 曾被誤寫為舊碼特徵值。已更正。

失敗考古 **#32** 已改寫為三版歸因對照，並補記「**修正歸因的那一段最容易犯下一個歸因錯**」。

---

## 六、U-K2「已宣告 extras 帳」

閘：`got 欄集 == baseline 欄集 ∪ declared_extras`，**未申報即紅**
（`grep -n "_DECLARED_EXTRAS" verify/run_verification.py`）。
現宣告：`W-D.1.2 診斷_退縮{0m,3.5m}.csv` → `真G(㎡)`（理由＋加入日入 `verify/README.md`）。

- **不重烤 v3 baseline、不解凍 P-H**。
- `_bake_csv` 會把新欄追加於後 ⇒ **單一次重烤週期到來時 extras 自然歸零**
  ⇒ 已寫入 README **重烤 checklist**（含「清空該 baseline 之宣告，勿留過期條目」）。
- **⚠️ 母體不完整（WARNING-7）**：`wf/f0~f4` 之 gate 因 **F.0 raise** 而**從未執行**
  ⇒ extras 現**不可觀測**、故未宣告。F.0 恢復後須補跑枚舉並補宣告（checklist 已列）。
- **WARNING-8**：另一 `diff_rows` 消費者 `verify/wg_g3.py` 走同一閘，已列入 checklist。

---

## 七、`s_max_left` 斷言（E·**REFUTED 後重做**）

- plan 原寫「兩呼叫端」→ reviewer 實跑 `grep -rn "s_max_left=" --include=*.py .` ＝ **7 處**。
- 更要緊：**放在呼叫端是恆真式**——三個生產呼叫端之 `s_max_left` 皆在前一兩行由
  `norm(p2−p1)` 現算，拿值與其自身定義式比、位元恆等、不可能失敗。
- ⇒ **改於消費端** `_corner_first_lot_G` 驗，並要求呼叫端**另行獨立讀取** `front_p2`
  （來自 `cad['front_lines'][blk]['p2']`）。**鑑別力實測**：塞 `_oblique_s_max`(60) ⇒ loud；
  `front_p2=None` ⇒ loud。
- 容差 `1e-9` 之導出：TWD97 ~3.1e5 × 2⁻⁵² ≈ 7e-11／座標，`norm` 累積數 ulp ⇒ ~1e-10，取 10 倍。
  **⚠️ 與 `_EPS_ZERO_CHORD` 之 1e-9 數值同、依據不同**（後者係 (s,t) 局部框 O(1e2) 之噪訊 ~1e-14）
  ——已於註解明寫**勿互相引用為依據**（N-17）。
- 連帶更新：`_corner_block_true_G` 透傳、app／`selection_pipeline`／`run_verification`／
  `probe_corner_trueG`／`test_corner_first_lot_G` 各補 `front_p2`。

**副產物（閘立刻兌現）**：`front_p2` 成必填後，`probe_ruling_K4_s_origin` 之 patch 變體
簽名不符 ⇒ **K-4 S 起算點閘當場轉紅**。已修（變體簽名同步，且**變體內照樣驗該前提、不豁免**）。

---

## 八、F／G 兩條紀律

- **F 就地改檔**：倉內自查 **0 命中**（三種樣式）。案由＝本 session 我用
  `io.open(p,"w").write(io.open(p).read()...)` 把報告檔寫成 0 bytes（`"w"` 先截斷再求值內層 read）。
  ⚠️ reviewer NOTE-2：plan 給的自查式過窄，較寬式抓到 `verify/tools/scan_embedded_numeric_names.py`
  之未關檔寫入（`txt` 已在記憶體、**無害**），已知悉。
- **G**：「引擎自報診斷（`T2-DIAG`／`S0d`）覆核時不得過濾」寫入 `validation-runbook`。
  ⚠️ reviewer NOTE-3 稱「該條已在倉裡」——**那是它讀到我本輪剛加的**，非既有；
  其附帶指出之 `.agents/` 副本與 `.claude/` 不同步且未被 git 追蹤，成立（`.agents/` 本在既有 untracked 清單內·未動）。

---

## 九、驗收

- `py_compile`：app.py／m_rescue.py／run_verification.py／selection_pipeline.py／三探針 → OK
- `verify/test_corner_first_lot_G.py` → PASS（2/2）
- `verify/fixture_n14_min_width.py` → 23 格 rc=0
- 三探針（`probe_ruling_N_e1_touch`／`probe_ruling_K4_s_origin`／`probe_ruling_K4_3_source`）rc=0
- `python verify/run_all.py` → **56 PASS／14 FAIL**，FAIL 名目對 `K4_after2.log` **diff 為空**

**⚠️ 驗收判準之補強（WARNING-9）**：F.0 之失敗**名目**恆為 `W-F F.0`，
其訊息內含 `G014 G(Σa)=…`——而 **G014 正是 A1 新准入之同塊互源對**（`628-28(1)`/`628-29(1)`）、
**G007 正是唯一 award 之 gid**。⇒ 名目 diff 為空**不足以**證明無影響，
須另比 **F.0 之 GSA 錯誤訊息逐字**。本波已比：**逐字全等**。

---

## 十、🚩 上呈

| # | 題 | 性質 |
|---|---|---|
| **U-K3**（新） | K-4-3 (c) 右式＝原位次 A₀ G 抑或街角槽試算 G；併請裁 2dp tie 規則 | **域邊界**（法規本意）·已量出差異·不擋施工 |
| **U-K4**（新·reviewer 提） | **③ 之來源集 ≡ ① 之來源集** ⇒ (a)(b)(c) 自動及於 ③——是否為 KL 原意？③ 是否應有獨立母體 | 域邊界 |
| **U-K5**（新·reviewer WARNING-3） | A1 後**同塊源可被移除**⇒ 該塊推進序重排，而 M-5 三閘**無位次序項**（F.0 有專屬閘）。本案潛伏未觸發 | 是否比照 F.0 立閘 |
| **U-K2** | extras 帳已建；`wf/f0~f4` 母體待 F.0 恢復後補 | 記帳·已列 checklist |
| **U-E3** | 對照清冊 12→13 欄（`寬度判定`） | 交付物格式異動·待知會 |

**既有 🚩（未動）**：`data/地籍資料來源_匿名版.xlsx` 未 commit 異動。
