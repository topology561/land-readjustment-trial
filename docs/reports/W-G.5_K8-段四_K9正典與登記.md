# W-G.5 · K-8 段四 —— **K-9 五則裁定落地（正典＋登記）·K-7 撤銷**

**分支**：`wip/s1-endpart`　**起點 HEAD**：`1e632a1`
**批性質**：**正典＋註解＋登記＋一支只量不改之 probe**。
**⛔ 本批不上任何閘、不改任何判準、不動任何算式。行為零變動、未跑 `run_all`。**

> 本報告為**本批之唯一本體**（CLAUDE.md 回報鐵律）；聊天僅 ping、不承載內容。
> 一切數字錨引倉檔路徑。

---

## 〇、一句話結論

K-7（街角面積門檻之寬深豁免）**整條撤銷**，K-9 五則入正典；碼面僅補**過渡態註解**、
**零 AST 異動**；新增之只量不判 probe 產出八街角寬度實測，
**發現 1 格上閘後會翻盤 ＋ 3 項須 KL 域裁之硬前置**（U-K9-1〜3）。

---

## 一、交付格式證明（AST 層）

基準 commit `1e632a1`。工具：`ast.parse` → 剝除所有 docstring → `ast.dump` 逐字比對。

| 檔 | `byteΔ` | **`A1`**（去 docstring 後 AST 逐字相同） | **`PC`**（AST 層竄改第一個數值字面後比） | `co_code 相異`（參考） |
|---|---|---|---|---|
| `app.py` | True | **True** | **False** | 0 |
| `verify/probes/probe_ruling_N_e1_touch.py` | True | **True** | **False** | 0 |

- `A1=True` ⇒ **本批未動任何既有 `.py` 之 AST**（＝未動任何字串值、常數、算式、控制流）。
- `PC=False` ⇒ A1 **非恆真閘**（正向對照在 **AST 層**施加·非文字替換）。
- 新增檔 `verify/probes/probe_ruling_K9_corner_width.py` 無舊版可比 ⇒ 改由上表之
  「每個既有 `.py` 各 `A1=True`」扛證明。
- `py_compile`：`app.py`／兩支 probe 皆通過。

---

## 二、正典（4.1）

落點：`docs/rulings/K-6_街角地分配程序與可分配判準.md`，
新增 `## K-9 街廓層深度檢查與街角寬度實量` 節（`:329`·**引用時請重 grep**），位於 `## K-8 §五-2` 之後、
`## 🔒 K-8 §二 補記` 之前（`grep -n "^## K-9 " docs/rulings/K-6_街角地分配程序與可分配判準.md`）。

含：**K-9 §一** 五則表／**§一-1** 合併後判準／**§二** 封閉性證明三段／
**§二-1** 本案實測（六街廓零警示）／**§三** K-7 與 §零 之既存牴觸／
**§三-1** §零-3 殘留「蘊含」語（上呈）／**§四** 落地狀態＋前置未決項／
**§五** K-7 原文考古段（刪節線標作廢·**未刪**）。

同檔另更正兩處**既有陳述**（皆為文件、非判準）：

1. **節號體系對照表**：`段四（K-7 落地）` → `段四＝K-9 正典與登記`，並加「段四內容已更換」警語。
2. **K-8 波末遷移項登記表**：更正其對 baseline 綁定之**誤載**（見 §四）。

---

## 三、`CLAUDE.md`／`AGENTS.md`（4.2）

`grep -n "K-7" CLAUDE.md` 於 `1e632a1` 實測 **4 處**，逐一處置：

| 處 | 原內容 | 處置 |
|---|---|---|
| `:10` 正典指標 | 「K-6／K-7／K-8 之正典主場」 | 改為 **K-6／K-7／K-8／K-9**（K-7 雖撤銷仍在該檔留考古），並註明撤銷日 |
| `:115` 進度表 | 「K-7（…深度豁免）✅ 已裁·碼與文件未落地＝段四」 | 刪節線＋「🗄️ 已撤銷（2026-08-01）·由 K-9 取代」＋自癒 grep；**另新增** 「K-9 五則」一列 |
| `:117` 進度表 | 「段四 K-7 落地」 | 改「段四 K-9 正典與登記（行為零變動·**不含實作**）」 |
| `:231` 規則段 | K-7 全文 | **刪節線標作廢·原文未刪**；其下**新增 K-9 五則摘要**＋「實作屬 K-6-A2·現行碼行為未變」 |

進度表另將「K-6-A2（寬深雙檢實作）」一列補記為 **「＝ K-9 實作·有土地後果」**。

`AGENTS.md:11` 之正典指標同步（K-9 入列＋K-7 撤銷警語）。

---

## 四、碼註解（4.3）——**只動註解與 docstring·零字串值異動**

### 4.3-a 窮盡搜尋（**詞表已擴充**·原始輸出見下）

起手三條之外，另跑：`豁免`／`逕認`／`不走 N-14`／`不重檢`／`不量測`／`免量`／`N-14 不適用`。
（血訓①：**同義不同名會讓符號名索引之窮盡搜尋失明**。）

### 4.3-b 逐項判定

| `file:line`（段四後） | 種類 | 判定 | 理由 |
|---|---|---|---|
| `app.py:2002` | `#` 註解 | **改** | 補 K-9-5 撤銷＋過渡態＋自癒 grep |
| `app.py:6344`／`:6346` | `#` 註解 | **改**（於其下補 `:6347–6354` 之 K-9 段） | 同上；原文保留供追溯 |
| **`app.py:6355`** | **常數字串值** | **不改** 🔒 | 紅線 #1；`_K4` 正名＋措辭屬**波末重烤批**（GB-11） |
| `app.py:6376–6379` | docstring | **改**（補 `:6381–6391`） | 補 K-9-1/3/5＋probe 指路 |
| `app.py:6442` | docstring | **改** | 補「四態現況為過渡態」 |
| `app.py:8996–8999` | `#` 註解 | **改** | 同上 |
| `app.py:9011` | **f-string（`raise` 訊息）** | **不改** | 見 4.3-c |
| `app.py:19147` | `#` 註解 | **改** | 同上 |
| `app.py:19192`／`:19196`／`:19198` | **f-string（畫面訊息）** | **不改** | 見 4.3-c |
| `app.py:6407`／`:6408`／`:6452`／`:6453`／`:19176` | 碼（常數引用） | **不改** | 非措辭、屬判準碼路徑 |
| `probe_ruling_N_e1_touch.py:81` | docstring | **改** | 補 K-9-5＋baseline 綁定警語 |
| `probe_ruling_N_e1_touch.py:200` | `#` 註解 | **改** | 同上 |
| **`probe_ruling_N_e1_touch.py:208`** | **字串字面 → 落 baseline `判定` 欄** | **不改** 🔒 | **綁 13 列**（見 §四-1） |
| `probe_ruling_N_e1_touch.py:112` | 碼（讀 `ns[...]` 比較） | **不改** | 讀常數、不含字面 |
| `verify/tools/y_dump_diff.py:97` | 碼（欄名集合） | **不改** | 欄名、非措辭 |
| **`verify/wf_f0.py:361`** | `#` 註解 | **不改（相符）** | 「gate#9 數學上蘊含毗鄰」——**與街角寬深無關**，係位相不變之敘述 |
| `app.py:9180`／`:9184`／`:9231`／`:9007`／`:19187` | 註解／訊息 | **不改（相符）** | 述「街角地最小分配面積門檻」本身或 `PENDING` 閘，非寬深豁免歸因 |
| `run_verification.py:791`／`wd3_fragment_geom.py:183` | `#` 註解 | **不改（相符）** | 碎片判準之「面積門檻全廢」，與 K-4/K-7 無涉 |

### 4.3-c 兩類**不改**之理由（**兩條各自獨立成立**）

1. **交付格式硬約束**：字串字面**是 AST 節點** ⇒ 改之即 `A1=False`。
   本批之交付要求為 `A1=True` ⇒ **f-string 與常數值一律不得動**。
2. **baseline 流向實測**（交辦文要求自驗·**原始輸出**）：

```
$ grep -rn "面積門檻已於 winner 選拔時檢過" verify/      → rc=1（0 命中）
$ grep -rn "筆街角第 1 宗以「面積門檻蘊含」判定寬度合格" verify/ → rc=1（0 命中）
$ grep -rn "蘊含寬度與深度皆合格" verify/                → rc=1（0 命中）
$ grep -rn "K-4 後街角第 1 宗恆為" verify/               → rc=1（0 命中）
$ grep -rn "上游殘留舊框架" verify/                      → rc=1（0 命中）
```

⇒ 交辦文所稱「`app.py:8977`／`:19152` 之 0 命中」**複驗相符**
（惟行號於 `1e632a1` 實為 `:9011`／`:19192`——🔒 行號衛生）。
**該兩處不流進 baseline，但仍因理由 1 不動。**

### 4.3-d 窮盡 grep 原始輸出（段四後工作樹）

```
$ grep -rn "蘊含" --include="*.py" .
./app.py:2002:    #   🆕 **K-4**：街角第 1 宗為 `合格（K-4 面積門檻蘊含）`（舊 `待判（截角）` 框架已作廢）；
./app.py:6346:#   ⇒ 街角第 1 宗**不走 N-14 寬度量測**，其判定為 **`合格`**，依據為 K-4 面積門檻之蘊含。
./app.py:6351:#   ⇒ 上段所述之「面積門檻蘊含寬度與深度」**已非現行判準**。
./app.py:6355:WIDTH_VERDICT_CORNER_K4 = '合格（K-4 面積門檻蘊含）'
./app.py:6376:    ── 🔒 K-4（KL 裁 2026-07-27）：街角第 1 宗**不走 N-14**，判定 ＝ 面積門檻之蘊含 ────
./app.py:6377:    (Ⅰ) 街角規定範圍之**寬度即法規最小寬** ⇒ 「G ≥ 街角最小分配面積」**蘊含**
./app.py:6379:    直接回 `合格（K-4 面積門檻蘊含）`，並帶依據數字（`_width_k4_G`／`_width_k4_threshold`）。
./app.py:6387:    ⇒ 上段「蘊含寬度、深度皆合格」**已非現行判準**。
./app.py:6442:    🆕 **K-4 後**：街角第 1 宗之態為 `合格（K-4 面積門檻蘊含）`；`PENDING`
./app.py:8996:            # ── 🆕 K-4（KL 裁 2026-07-27）：街角第 1 宗 ＝ `合格（K-4 面積門檻蘊含）` ──
./app.py:8998:            #   **(Ⅰ) 街角規定範圍之寬度即法規最小寬**⇒「G ≥ 街角最小分配面積」蘊含寬深合格，
./app.py:19147:                #     （`f3_corner_range_areas`·即 winner 選拔所用者），判定由該門檻蘊含。
./app.py:19196:                        f"📐 **K-4：{len(_k4_rows)} 筆街角第 1 宗以「面積門檻蘊含」判定寬度合格**"
./app.py:19198:                        f"蘊含寬度與深度皆合格；該門檻已於 winner 選拔時檢過，**不重檢、不量測**。"
./verify/probes/probe_ruling_N_e1_touch.py:81:    `G ≥ 街角最小分配面積`（＝ (Ⅰ) 街角規定範圍面積）之**蘊含**。
./verify/probes/probe_ruling_N_e1_touch.py:200:            #    之寬度即法規最小寬 ⇒ `G ≥ 街角最小分配面積` 蘊含寬深合格（已於 winner
./verify/probes/probe_ruling_N_e1_touch.py:208:            w_s, verdict, e6 = "—", "街角·K-4 面積門檻蘊含（N-14 不適用）", "—"
./verify/wf_f0.py:361:        #   gate#9（oB==約簡序）為序不變之強式，數學上蘊含毗鄰；gate#10 為 KL「標的宗左右毗鄰不變」之具名複述。
```

（`WIDTH_VERDICT_CORNER_K4|_width_by_area_gate` 與 `面積門檻|面積閘 ∩ 寬|深|街角`
兩條之原始輸出已逐行納入 4.3-b 之判定表，此處不重貼。）

---

## 四-1、🔴 **交辦文與既有登記之誤載更正**（實測）

**交辦文紅線 #1 與正典遷移項表皆載**：
`WIDTH_VERDICT_CORNER_K4` 之字串值 `'合格（K-4 面積門檻蘊含）'` 綁
`E系列實測快照_退縮0m.csv` 8 列／`退縮3.5m.csv` 5 列。

**實測不符**（原始輸出）：

```
$ grep -rn "合格（K-4 面積門檻蘊含）" verify/baselines/     → 0 命中
$ grep -rln "寬度判定" verify/baselines/                    → 0 命中（無任何 baseline 有該欄）
$ grep -c "街角·K-4 面積門檻蘊含（N-14 不適用）" verify/baselines/E系列實測快照_退縮0m.csv   → 8
$ grep -c "街角·K-4 面積門檻蘊含（N-14 不適用）" verify/baselines/E系列實測快照_退縮3.5m.csv → 5
```

⇒ **8/5 之計數正確，但綁定之符號指錯**。真正落 baseline `判定` 欄者係
`verify/probes/probe_ruling_N_e1_touch.py:208` 之**硬編字面**——
該探針**比較**時讀 `ns["WIDTH_VERDICT_CORNER_K4"]`（`:112`），
**寫入 CSV 時卻用自己的字面**（`:208`）⇒ **同名異體、改一個不動另一個**。

**後果（波末重烤批必讀）**：
- 只改 app 常數 ⇒ **baseline 不會紅**，而探針輸出與 app 語意**靜默脫鉤**。
- 只改探針字面 ⇒ **必翻 13 列**。
- ⇒ **兩者須同批改**。本批依紅線**兩者皆不動**，僅更正登記（正典遷移項表＋GB-11）。

⚠️ 附帶：上開 8/5 係 **baseline 檔內容**之計數；**現行實跑值不同**
（0m 實跑街角 winner **7** 個·見 §五），因 **E 系列快照閘現為紅**（既有 46 紅之一）。
**兩者皆真、勿互代。**

---

## 五、probe（4.4）：`verify/probes/probe_ruling_K9_corner_width.py`

> # 🔴 本節（§五 全節）之寬度數字已於**段五作廢**（2026-08-02）
>
> 本節之量測以**畸零地附表之最小深度 `14.00`** 為 `parcel_min_width_n14` 之量測帶深。
> **該帶深定義錯誤**——依 **K-9-6**（KL 裁 2026-08-01·法規原文），量測帶深應為
> **該宗／該範圍自身之最小深度**（本案 33〜46m），`14.00` 係**可建築門檻**、非量測量。
>
> ⇒ **§五-a 表之全部寬度數字、以及「1 格翻盤／1 格貼線」之結論，一律作廢、不得引用。**
> 本報告**其餘各節（正典、`CLAUDE.md`、註解、§四-1 之 baseline 綁定更正、GB-10/11）不受影響**。
>
> 改寫後之實測 ⇒ `verify/out/probe_ruling_K9_corner_width.log`、
> 正典 **K-9-7-c**、報告 `docs/reports/W-G.5_K8-段五_K9-6_K9-7正典與登記.md`。
> **本節逐字保留供追溯（失敗考古），勿刪。**

**只量不判·rc 恆 0·不掛 `run_all` 判定鏈**（`grep -n "probe" verify/run_all.py` ⇒ 本檔不在其中）。
**未改 `app.py` 任何一行**；量測全走倉內既有原語與其自帶容差
（`parcel_min_width_n14`／`_make_chamfer_tri_wb`／`get_min_lot_size`），
**未自寫幾何算法、未自訂容差**。

輸出：`verify/out/probe_ruling_K9_corner_width.log`／`.csv`（**只寫 `out/`·不進 `baselines/`**）。

### 5-a 八街角 × 兩情境（16 格·全列）

| 情境 | 街廓/側 | 退縮 | 門檻 | 街角第 1 宗 | 接合 | **截角前寬** | 截角後寬 | 差 | 若上閘會如何 |
|---|---|---|---|---|---|---|---|---|---|
| 0m | R1/left | 0.00 | 3.50 | `628-37(1)` | **點接合** | — | 2.8261 | — | ⏸ 待 KL 裁 |
| 0m | R1/right | 0.00 | 3.50 | `628(5)` | 邊接合 | **6.8469** | 3.4643 | +3.3469 | 合格 |
| 0m | R2/left | 0.00 | 3.50 | `628-40(1)` | 邊接合 | **7.0916** | 4.6051 | +3.5916 | 合格 |
| 0m | R3/right | 0.00 | 3.50 | —（強制留設抵費地） | — | — | — | — | — |
| 0m | R4/left | 0.00 | 3.50 | `628(1)` | 邊接合 | **46.2567** | 43.2119 | +42.7567 | 合格 |
| 0m | R4/right | 0.00 | 3.50 | `628-1(1)` | **點接合** | — | —（raise） | — | ⏸ 待 KL 裁 |
| 0m | R5/left | 0.00 | 3.50 | `628-7(2)` | 邊接合 | **6.0869** | 3.6833 | +2.5869 | 合格 |
| 0m | R6/right | 0.00 | 3.50 | `628-18(1)` | 邊接合 | **7.2212** | 3.8197 | +3.7212 | 合格 |
| 3.5m | R1/left | 3.50 | 7.00 | `628-36(1)` | **點接合** | — | 13.9164 | — | ⏸ 待 KL 裁 |
| **3.5m** | **R1/right** | 3.50 | **7.00** | **`628(5)`** | 邊接合 | **6.8470** | 3.4644 | **−0.1530** | 🔴 **不合格·翻盤** |
| 3.5m | R2/left | 3.50 | 7.00 | —（強制留設抵費地） | — | — | — | — | — |
| 3.5m | R3/right | 3.50 | 7.00 | —（強制留設抵費地） | — | — | — | — | — |
| 3.5m | R4/left | 3.50 | 7.00 | `628(1)` | 邊接合 | **46.2567** | 43.2119 | +39.2567 | 合格 |
| 3.5m | R4/right | 3.50 | 7.00 | `628-1(1)` | **點接合** | — | —（raise） | — | ⏸ 待 KL 裁 |
| 3.5m | R5/left | 3.50 | 7.00 | —（強制留設抵費地） | — | — | — | — | — |
| 3.5m | R6/right | 3.50 | 7.00 | `628-18(1)` | 邊接合 | **7.2212** | 3.8197 | **+0.2212** | 合格（**餘裕僅 22cm**） |

**彙總**：可量（邊接合）**8 格**／點接合**4 格**（上呈）／無街角第 1 宗 **4 格**／
**上閘後翻盤 1 格**。

### 5-b 方法要點

- **截角前幾何** ＝ `Polygon(cut_coords) ∪ _make_chamfer_tri_wb(blk, side)`
  （`cut_coords` 係截角後·直接餵會量到偏小之寬度、造成**假不合格**）。
- **接合型態逐格記錄**：`宗 ∩ 截角` 為 `LineString`（~5.0m）⇒ **邊接合**、聯集為單一
  Polygon ⇒ 可量；為 `Point` ⇒ **點接合**、聯集為 `MultiPolygon` ⇒ **不可量**。
- ⛔ **未以 `buffer(±ε)` 合攏點接合**——實測**非單調**
  （R4 右：`±1e-6`／`±1e-5` 合成 Polygon，`±1e-4`／`±1e-3` 又散回 MultiPolygon）
  ⇒ 該手法係**換了一把尺**、非更嚴謹。
- **免費一致性看守**：`get_min_lot_size(...)['min_width']` 與參數表 `法定最小寬(m)`
  逐塊比對，分歧即 loud raise（實測八街角皆 **3.5**、零分歧）。
- `parcel_min_width_n14` 之三道 loud raise **不吞**，逐格記入「量測例外」區。

---

## 六、登記（4.5）

落點：`docs/reports/W-G.4_泛用阻塞項登記表.md`（照既有七欄體例）。

- **GB-10**：`parcel_min_width_n14` 之 **E-2′ 深度閘自 K-9-4 起成為恆真閘**
  ——保留不刪（防上游改動），**須誠實標注「不再具獨立鑑別力」**，標注時機為 **K-6-A2**。
  ⚠️ 恆真閘家族**已第四例**（前三：`A4 registry`／`K-4-4 s_max_left·front_p2`／
  `A-4 assert_depth_same_source`）⇒ 併入泛化波統一體例。
- **GB-11**：「K-4 面積門檻蘊含」字串族之**語意已錯**且係**兩個同名異體**
  （app 常數 ＋ 探針硬編字面）；**只有後者綁 baseline**（見 §四-1）⇒ **兩者須同批改**。
- **編號沿革註記**：已加於登記表頂端——舊 GB-7／GB-8 已於 K-6-A1 結案消滅，
  現行 GB-7／GB-7b／GB-8／GB-9 係 2026-08-01 重新啟用之**新項、與舊同名項無關**；
  **兩份歷史報告凍結不動**；引用時**須連日期一起引**。
- **新增 §一-B「K-6-A2 硬前置·待 KL 域裁」**（U-K9-1〜3·見下節）。
  ⚠️ 該節**與上表判準不同**（本案即觸發、非換案才觸發），已於節首明寫以免誤讀。

---

## 七、⛔ 上呈 KL（**本批不代裁**）

| # | 問題 | 為何非 CC 可決 |
|---|---|---|
| **U-K9-1** | 街角第 1 宗僅以**一點**接觸截角三角形時（16 格中 4 格），其「**截角前**寬度」之量測幾何為何？<br>(甲) 該宗不適用截角條款、截角前＝截角後；(乙) 另定重建構造 | **土地後果不同**；且現行構造下該幾何**不良定義**，`buffer` 合攏已證非單調 ⇒ 任何選擇都是**裁定**、非技術 |
| **U-K9-2** | `R4/right 628-1(1)` 連**截角後**寬度都量不出（臨接 FRONT 閘 raise：−0.014826m > 容差 0.01m）。K-9-5 上閘後**必停機** | 屬「上游分配線不合規 vs 容差過嚴」之判斷。該 −1.4826e-2m 係倉內既載事實，惟因 K-4 豁免使街角宗**根本不走 N-14**，故**過去從未浮現** |
| **U-K9-3** | 正典 **§零-3 末句**之「『G ≥ 街角規定面積』**蘊含**街角第 1 宗寬度合格」與 **K-9-5「實量」**不相容。應 (甲) 隨 K-7 一併作廢，抑或 (乙) 保留為獨立幾何命題？ | **改判準之正典表述逾 CC 之權**。八街角 ∠(SIDE_LINE, ALLOC) ＝ 0.000°〜4.322° ⇒ **非退化**，蘊含關係不可由退化性逕推 |

**另請 KL 過目（K-6-A2 之前置）**：§五-a 之八街角寬度表，特別是
🔴 **3.5m `R1/right 628(5)` 差 −0.1530m（翻盤）** 與
⚠️ **3.5m `R6/right 628-18(1)` 餘裕僅 +0.2212m（貼線）**。

---

## 八、本批未做（**明列·免下一手誤認**）

- **未上任何閘**、未改任何判準、未動任何算式 ⇒ **未跑 `run_all`**（交辦文明令不需）。
- **K-9-2／K-9-4／K-9-5 之實作** ⇒ 屬 **K-6-A2**。
- `WIDTH_VERDICT_CORNER_K4` 之**常數名與字串值**、探針 `:208` 之字面 ⇒ **波末重烤批**（GB-11）。
- `_min_width_by_block`／`_min_depth_by_block` ⇒ **一律未動**（現行判準之輸入）。
- E-2′ 閘之「恆真」標注 ⇒ **K-6-A2**（現況仍具鑑別力·GB-10）。

---

## 九、段四之後

1. **K-6-A2**（K-9 實作批·**有土地後果**）——前置：§五-a 表經 KL 過目 ＋ U-K9-1〜3 裁定。
2. **波末批**：換快照＋重烤 v3／wf／E 系列＋兌現全部遷移項（含 **GB-11 之兩個同名異體同批改**）
   ＋GB-7／7b／8／9。
3. **K-6-B**：街角地分配程序四段本體。
4. **泛化波**：去錨、**恆真閘家族統一體例（含 GB-10）**、騎樓表分支。
