# `W-G.9-283R`　執行報告：「替身線」等同之**出處判定** ／ `forced` band 之界係**定義**抑或**近似** ／ `GB-169` 之成因指名

> **單** ＝ `docs/orders/W-G.9-283_施工單_替身線等同之出處判定與forced帶之界.md`（工項零已原封入倉）
> **分級** ＝ **中**（出處判定 ＋ 逐字量測 ＋ 登記·**零生產碼**·⛔ 替身·⛔ 反事實）
> **開工態** ＝ `3fb2f250a7a665f46ca87cd6e96f10e377efc13e`（`ls-remote` 實查·⛔ 本地追蹤 ref）
> 🛑 **本批鑄號 ＝ `自誤 386` 一號**（逐字由單供）；`GB`／`VR`／`K-9`／坑 **⛔ 鑄**。
> 🛑 **出艙即止**：⛔ 判該等同成立與否、⛔ 判碼面是否違反 `K-9-5-13`／`K-9-5-12（四）`、
> ⛔ 判「替身線」之所指、⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§零`　開工閘（九項·**全綠**）

三軸：`(1)` 來源 ＝ `blob@3fb2f250a7a665f46ca87cd6e96f10e377efc13e`（工項零後 ＝ `f5e9ca08…`）／
`(2)` 母體逐項具名／`(3)` 粒度框逐項具名。
🛑 **本批一切以 `git` 輸出文字為框者，一律同格具名 `core.quotePath`** 或改取 `-z` 之原始位元組（坑 `bc`）。

| # | 受詞 | 期值／判準 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `3fb2f250…` 或其後裔 | `3fb2f250a7a665f46ca87cd6e96f10e377efc13e`（**實查**·本窗 `HEAD` 同值·⛔ 以本地追蹤 ref 為證） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | **逐位相同** | ✅ |
| `3` | `p3a` 非祖先 | 須**非**祖先；判別力[必為祖先] | `merge-base --is-ancestor 05a11bd6… HEAD` ⇒ `rc = 1`｜**判別力[必為祖先]** `4070b492` ⇒ `rc = 0` | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑） | `42a4074dbb4f45f2cfcd3c4e52f1d1e752d1b2fdbcfad07b8325b8b2d399a03f` | 磁碟 `16475` B·`CR` `0`·`LF` `194`；受詞 `16397` B ⇒ **逐位相符**｜判別力二造：全檔同口徑 `5571c9c8…` **相異**／受詞多 `1` byte `70e9f651…` **相異** | ✅ |
| `4'` | 受詞自證（坑 `bf`） | [必真] `W-G.9-283` ＝ `True`；[必偽] `W-G.9-282` ＝ `False` | 所量之檔名逐字出艙；`True`／`False` | ✅ |
| `5` | `probe_order_preflight.py <本單>` ＋ `--selftest` | `P-3`／`P-5` 不紅；`--selftest` `rc = 0` | `rc = 0`·`stderr` `0` B｜🔴 停機款 **`0`**／🟡 提示 **`0`**／ℹ️ 記錄 `2`（`P-5` ✅·受詞 `16397` B；`P-3` `def main` ＝ `14975`-`24579`）｜`--selftest` `rc = 0`·**三靶皆觸·反靶不觸** ⇒ 非恆綠亦非恆紅 | ✅ |
| `5'` | `wg9223_acceptance_audit.py --selftest` | `rc = 0` | **`rc = 0`**｜自證：必命中字樣 `1`（須 `> 0`）／人造字樣 `0`（須 `0`） | ✅ |
| `6` | 四簿 ＋ 坑 | 自誤 `375`／`385`｜`GB` `167`／`169`｜`VR` `80`／`95`｜`K-9` `28`／`29`｜坑 `85`／`85`·下一 `bh` | **逐位相符**；坑 `rc = 0`·`stderr` `0` B·母體 ＝ 追蹤 `.md` **`764`** 檔·可解析 **`85`／`85`**（不可解析 `0`）·末標籤 `bg` ⇒ 下一 ＝ `bh`｜判別力二造：造甲 坑 `n` 定義處 `6` ✅／造乙 人造之坑（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ | ✅ |
| `7` | 生產碼母體 | **正面列舉 `34` 檔**；判別力[必不命中] **須 `> 0`**（🆕 **判準式·⛔ 定值**） | `app.py` `1` ＋ `verify/` 頂層 `.py` **`33`** ＝ **`34`**｜判別力 ＝ pathspec `verify/*.py` 多咬之**子層** ＝ **`300`**（於 `3fb2f250`·單載發單側實跑得 `299`·**其值逐批漂移·⛔ 據以判異常**·由 ＝ `自誤 386`） | ✅ |
| `8` | `verify/baselines` | `298` 檔·`a898f4e1…7ec9`（`core.quotePath` **預設**） | `298` 檔｜預設 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`｜`=false` `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520` | ✅ |

🔒 **本窗環境之前置檢**：`left-right` vs `origin/wip/s1-endpart` ＝ **`0  0`**；
`.claude/settings.json` 與 `verify/tools/wg9237_heredoc_guard.py` **皆在**；`core.autocrlf` 有效值 **`false`**。

---

## `§一`　`§零′` 之受領 ＋ `§一` 五裁之受領 ＋ 本批鑄號

🔒 **`§零′`**（前批之核可）與 **`§一` 之五裁**皆係**發單側已辦**·⛔ CC 重辦——本報告僅**受領**，⛔ 復驗。

| 簿 | 本批 | 期末 |
|---|---|---|
| `自誤` | **鑄 `386`**（逐字由單供·⛔ 改寫） | 相異 `375` → **`376`**；MAX `385` → **`386`**（**恰 `+1`**） |
| `GB`／`VR`／`K-9`／坑 | **⛔ 鑄** | `167`／`169`｜`80`／`95`｜`28`／`29`｜`85`／`85`·下一 `bh`（`ALPHA_LABELS` **一字未動**） |

**取號現查**（三軸｜`(1)` `blob@f5e9ca08…`｜`(2)` 取最大號 ＝ 登記表**單檔**（`808375` B／`9551` 列）·
查占用 ＝ **全倉追蹤檔 `2211` 檔**（`git ls-tree -z` 之**原始位元組**·可讀 `2189`）｜`(3)` **檔框 ＋ 列框**）
定義框列命中 `382`／範圍框列命中 `9` ⇒ 相異 `375`／**MAX `385`** ⇒ **次號 `386`**。
🔒 **口徑對照（坑 `bc`）**：`-z` ⇒ `.md` **`765`** 檔；`core.quotePath` **預設** ⇒ `p.endswith('.md')` 僅得 **`32`**。

🔴 **`GB-158` 之案型再現**：**B 形**（`` 自誤 `N` ``）使**二對照組**（已鑄之 `385`／`384`·須 `> 0`）
**皆得 `0`／`0`** ⇒ 依 `GB-158` 施行 `二`，**該形⛔ 單獨採信**；判由 **C 形**（`` `自誤 N` ``）承擔
——C 形得 `3`／`5` 與 `2`／`6` ⇒ **器非紅**；哨兵（**執行期組出**·字面⛔ 出艙）三形皆 `0`。
**逐筆歸類**（`裁 H`）：受詢 `386` 之 C 形命中 `1` 檔／`5` 列，**全數集中於本單自身**
⇒ **自解款白名單 `1`** ⇒ ⛔ 撞號、⛔ 順延。

---

## `§二`　工項零：本單原封入倉　✅

| 款 | **實測** |
|---|---|
| 所指之檔數 | 受詢（本單）＝ **`1`**｜造甲 `W-G.9-282` 之單 ＝ **`1`**（須 `1`）✅｜造乙 人造名（**執行期組出**·字面⛔ 出艙）＝ **`0`** ✅ |
| `CR` | 源／磁碟／blob 皆 **`0`**；`bytes` 皆 **`16475`**；`LF` ＝ **`194`**｜**判別力** 人造真 CRLF（記憶體內·⛔ 落檔）`CR` ＝ `2`（期 `> 0`）✅ |
| 三方逐位 | blob `sha1` ＝ `def362cd7e1d16f16c7028203024575533feb4a1`（與源之**預算**相符 ⇒ ⛔ 抄錄）｜內容 `sha256` 源 ＝ 磁碟 ＝ blob ＝ **`5571c9c8220685f183992cfec1067999d2cc794e271f0ed5ed3dc6a36a6f8477`** ✅ |

`git status --porcelain` 列數 ＝ **`1`**（逐檔 `git add`·⛔ `-A`／⛔ `.`）。

---

## `§三`　工項一（**主檢**）：「該點即『面積換算出來的替身線』」之**出處判定**

**器** ＝ `verify/probes/probe_WG9283_provenance.py`（新建·`rc = 0`·`stderr` **`0`** B）
**落檔** ＝ `verify/out/WG9283R_provenance.log`（**全量出艙**·⛔ `tail -N` 取結果）

### 三-0　準據之沿用與**其受詞不合之處**（loud）

🔒 本器**直接 `import`** `verify/tools/wg915_ruling_provenance.py` 之
`build_blocks`／`classify`／`is_quote`／`KL_MARKS`——**⛔ 另寫第二份判準**。
其機械規則（自該器 docstring **逐字**抽取）：引文行 ＝ `^>`；引文區塊 ＝ 極大連續段；
引言 ＝ 區塊起點往上最近之非空白非引文行；三類 ＝ KL 逐字（引言含 `KL_MARKS` ＝ `('KL 逐字', 'KL 裁')`）／
助手側補充（不在任何引文區塊內）／無法判定（在區塊內但引言不含上開字樣）。

🛑 **不合之處（loud·⛔ 逕自改寫）**：該器之**條目母體** ＝ 「含 `🔒` 之行」
（其 `main()` 逐字 ＝ `pop = [i for i, x in enumerate(lines) if "🔒" in x]`），
而本項之受詞**不含 `🔒`** ⇒ **⛔ 落於該器之條目母體內**。
🔒 **處置**：**只沿用其機械規則之四符號**，**⛔ 沿用其條目母體**；受詞由單 `§三-3` 款 `1` 之字樣錨界定。
⇒ **⛔ 改該器一字**（其 blob 於本批期初＝期末·見收工閘 `6`）。

母體：`docs/rulings/K-6_街角地分配程序與可分配判準.md` **單檔**（`299931` B／`4125` 列·
`core.quotePath=false`——該器之 `git` 包裝所釘）；引文行 **`703`**；引文區塊 **`141`** 段。

### 三-1　款 `1`：受詞行逐字 ＋ 區塊起迄 ＋ 引言

框（**無 `^` 錨·二數並報**）＝ `失效加註` ⇒ **命中檔數 `1`／命中列數 `3`**。
受詞之字樣錨（`grep -F` **固定字串**·⛔ regex）＝ `（該點即「面積換算出來的替身線」）` ⇒ **命中列數 `1`**。

```
[914]   > ⇒ `forced` 時之起算**不再取「其 buffer 起算點」**（該點即「面積換算出來的替身線」）。
```

🔴 **`is_quote`（規則第 `1` 款「行首符合 `^>`」）＝ `False`** ⇒ **受詞行⛔ 落於任何引文區塊內**（`BID` ＝ `None`）。
其由係**機械規則以行首為準**，而本行**以二空白起首**——逐位證據：

```
前 8 個字元之 repr        ＝ '  > ⇒ `f'
LINES[I].startswith('>')          ＝ False
LINES[I].lstrip().startswith('>') ＝ True
```

🔒 **⛔ 逕自改寫該規則**（單 `§三-2` 明令）。其**往上最近之六個非空白行**已逐字出艙於落檔。

### 三-2　款 `2`：受詞行與前一行之分界 ＋ 是否落於「」引號對內

```
前一行 [913]   > 「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」
受詞行 [914]   > ⇒ `forced` 時之起算**不再取「其 buffer 起算點」**（該點即「面積換算出來的替身線」）。
```

框 ＝ 該列內 `「[^」]*」` 之比對（**逐列**）：
前一行之引號對 ＝ `['「是 ⇒ 起算一律用真正的地界，那條面積換算出來的替身線在側街負擔上不再有角色。」']`；
受詞行之引號對 ＝ `['「其 buffer 起算點」', '「面積換算出來的替身線」']`。
🔒 **受詞（括號子句之全文）落於任一引號對之內？ `False`**——其所含之引號對係**子句之內部**，
⛔ 使該子句落於某引號對之內。

### 三-3　款 `3`：引入 `commit` 與日期（`git log -S` 之**字面**·⛔ regex）

| 受詞 | `git log -S <字面> -- <K-6>` |
|---|---|
| 受詞（括號子句） | 命中 `1` ⇒ **`34b01aa724012692218b51e4613b5340b43f3483`**｜`2026-08-13 01:58:45 +0800` |
| `K-9-5-13` 節之節題 | 命中 `1` ⇒ **`34b01aa724012692218b51e4613b5340b43f3483`**｜`2026-08-13 01:58:45 +0800` |

🔒 **二者係<u>同一</u> `commit`** ⇒ `merge-base --is-ancestor` 之正、反向**必同得 `rc = 0`**
——此係**受詞之事實**（同批引入），**⛔ 該檢之缺陷**。
其判別力另由下式證：**[必非祖先]** `p3a` ⇒ `rc = 1`（期 `≠ 0`）✅。

### 三-4　款 `4`：器之判

🔒 **受詞行之判 ＝ 「助手側補充」**；其由（器之逐字輸出）＝ **「不在任何引文區塊內」**。
🛑 **⛔ 以「在 `>` 內」充「KL 逐字」**——本判係依 `三-2` 之**引言**規則所得。

### 三-5　款 `5`：判別力二造

| 造 | 錨（`grep -F`·**須唯一**） | 命中列數 | 判 | 期 | 判 |
|---|---|---|---|---|---|
| [必為 KL 逐字] | `**裁定二**：「是 ⇒ 起算一律用真正的地界` | **`1`**（列 `2026`） | **KL 逐字**（引言含 `['KL 逐字']`｜引言 ＝ `#### （二）所答　**✅ KL 逐字**`） | KL 逐字 | ✅ |
| [必為助手側] | `；forced 時為其 buffer 起算點。` | **`1`**（列 `892`） | **助手側補充**（不在任何引文區塊內） | 助手側補充 | ✅ |

🔒 **二造之判相異**（`['KL 逐字', '助手側補充']`）⇒ **器非紅** ✅。

### 三-6　併查（照實·出艙即止）：`失效加註` 三處之逐行判

| 列 | `is_quote` | 判 |
|---|---|---|
| `252`（`§三 最小寬度帶`） | `True` | **無法判定** |
| `276`（`§四 ALLOC_LINE 定位`） | `True` | **無法判定** |
| `910`（`K-9-5-6` 節內） | **`False`** | **助手側補充** |

🛑 **出艙即止**：⛔ 判該等同成立與否、⛔ 判碼面是否違反 `K-9-5-13`／`K-9-5-12（四）`、⛔ 鑄 `GB`、
⛔ 解除或收窄 `GB-168`／`GB-169`、⛔ 提修法主張、⛔ 呈 KL。

---

## `§四`　工項二：`forced` band 之界 ＋ 否定性宣稱之射程補足

**器** ＝ `verify/probes/probe_WG9283_band.py`（新建·`rc = 0`·`stderr` **`0`** B）
**落檔** ＝ `verify/out/WG9283R_band.log`（**全量出艙**）

### 四-1 款 `1`　`_corner_buffer_S` 之二段逐字（**本批重量**）

錨（**無 `^` 錨·二數並報**）＝ `_corner_buffer_S`｜母體 ＝ `app.py`（`1464014` B／`24582` 列）
＋ `verify/stepg_pipeline.py`（`111526` B／`1671` 列）**二檔** ⇒ **命中檔數 `2`／命中列數 `19`**。
🔒 **AST 區間**（⛔ 文字層估界）：`def` 於列 **`9332`**，`end_lineno` ＝ **`9435`**（共 `104` 列）。

```
[9335]     §3 街角 forced band（plan v3 §3·補丁九）：**bisect 解 `buf` 使「真實池帶面積 == range_area」**
[9336]     （|Δ| ≤ tol）。**全精度回傳**（N0-18a·不 round）。取代舊矩形近似 `range_area ÷ avg_depth`
[9347]       `side='left'`  → 帶 `s ∈ [max(s_min, 0), buf]`   ← **上界＝`buf`**（**非** `lo+buf`·BLOCKED-1）
[9348]                         ∵ 消費端 `left_cum_S = buf` ⇒ 首宗自 `s=buf` 起 ⇒ 保留區恆為 `[s_min, buf]`
[9349]       `side='right'` → 帶 `s ∈ [s_max − buf, s_max]`   ← step 0 後 `amp == s_max`（`_oblique_s_max`）
```

### 四-1 款 `2`　bisect 之**目標函式**：面積由**實際多邊形**抑或**近似**算得

母體 ＝ `_corner_buffer_S` 之**函式體**（**AST 區間 `9332`–`9435`**·⛔ 文字層估界）·粒度框 ＝ **列框**。

| 字樣 | 列數 | | 字樣 | 列數 |
|---|---|---|---|---|
| `_block_strip(` | **`1`** | | `range_area` | `10` |
| `_strip_s_range(` | `1` | | `area` | `12` |
| `avg_depth` | `1` | | `bisect` | `4` |
| `矩形` | `1` | | `rect` | **`0`** |
| `近似` | `3` | | | |

**[必命中]** `_block_strip(` 於該函式體內 ＝ **`1`**（須 `≥ 1`）✅
**[必為零]** 人造字樣（**執行期組出**·字面⛔ 出艙）＝ **`0`** ✅ ⇒ **器非紅**。

```
[9409]         _g, _ar = _block_strip(block_poly, d_hat, bp, w, allocation_dir=allocation_dir)
```

含 `avg_depth`／`矩形`／`近似` 之三列逐字（供發單側判讀）：
`[9336]` 之「**取代舊矩形近似 `range_area ÷ avg_depth`**」／
`[9357]` 之「`allocation_dir` 缺值/零向量時 … 會**退 `rot90(d_hat)` 正交近似**」／
`[9377]` 之 `raise` 訊息（其**禁**該正交退路）。

🛑 **出艙即止**：⛔ 判該界為「真正的地界」抑或「替身線」——該判繫於 `§三` 之出處判定。

### 四-1 款 `3`　`range_area` 之**來源**

母體 ＝ 呼叫端**二檔**（逐檔具名）：`app.py` 之 `_corner_buffer_S(` 命中 **`3`** 列
（含 `def` 一列 ＋ `21903`／`21914` 二呼叫）；`verify/stepg_pipeline.py` 命中 **`3`** 列
（`389` 之 `ns[...]` 取得 ＋ `659`／`670` 二呼叫）。其逐字（含續行）已全量出艙於落檔。
上游錨 ＝ `_build_corner_range_v3`（**無 `^` 錨·二數並報**）⇒ 檔數 `2`／列數 `18`。

### 四-1 款 `4`　帶之**上界**是否即 `buf` ＋ 消費端之逐位對映（**本批重量其框**）

框（**有 `^` 錨**）＝ `^\s*left_cum_S = float`｜母體 ＝ `verify/stepg_pipeline.py` **單檔**
⇒ **命中檔數 `1`／命中列數 `1`**：

```
[776]             left_cum_S = float(_left_buffer_S)
```

🔒 前批（`W-G.9-282R §四` 款 `2`／`3`）之逐位對拍（**轉引·其母體 ＝ `verify/out/WG9282R_branch.log`**）：
`0m` 態甲 `R4/left` `left_cum_S` ＝ `4.49975224003872` ＝ `buf`（`range_area` ＝ `116.08`）；
`3.5m` 態甲 `R4/left` ＝ `7.8185635189090075` ＝ `buf`（`range_area` ＝ `226.01`）。
🛑 本款**只重量其框**，**⛔ 於本批重跑該長跑**（單 `§七`：⛔ 未經本單具名之長跑）。

### 四-2　否定性存在宣稱之**射程補足**（🔴 **前批之三檔母體確有遺漏**）

**母體（正面列舉·`git ls-tree -z` 之原始位元組·坑 `bc`；⛔ 以 pathspec glob 界定·坑 `be`）**：
`verify/` 之**全部** `.py`（**含子層**）＝ **`333`** 檔 ＋ `docs/specs/` 之全部 `.md` ＝ **`35`** 檔
⇒ 合計 **`368`** 檔（可讀 `368`·讀不到 `0`）。

**所查之字樣（逐項具名·⛔ 只書「查無」）與其命中（檔數／列數）**

| 字樣 | 檔／列 | | 字樣 | 檔／列 |
|---|---|---|---|---|
| `抵費地` | `107`／`432` | | `_corner_buffer_S` | `22`／`61` |
| `強制抵費地` | `33`／`95` | | `_block_strip(` | `16`／`23` |
| `抵費地多邊形` | **`0`／`0`** | | `橘色抵費地` | **`0`／`0`** |
| `抵費地_poly` | **`0`／`0`** | | `指配` | `26`／`66` |
| `offset_poly` | **`0`／`0`** | | `Polygon` | `85`／`266` |
| `_off_poly` | **`0`／`0`** | | `shapely` | `115`／`285` |
| `forced_poly` | **`0`／`0`** | | `left_buffer_S` | `20`／`61` |
| `range_poly` | `1`／`8` | | `right_buffer_S` | `12`／`31` |
| `forced_offset` | `47`／`121` | | | |

判別力：[必命中] `def ` ⇒ 檔 `368` 之子集·列數 `> 0`；[必為零] 人造字樣（**執行期組出**·字面⛔ 出艙）⇒ `0`／`0` ⇒ **器非紅**。

🔴 **射程補足之所得（⛔ 判·照實）**：`抵費地` ∩ 幾何字樣之交集 ＝ **`23`** 列，其中**前批三檔母體所未含**者包括——

```
[verify/fixture_end_fallback.py:72] frag_row = {"街廓": BLK, "暫編": f"{BLK}-抵費地-2", "poly": frag, …}
[verify/fixture_end_winner.py:105]  frag_row = {"街廓": BLK, "暫編": f"{BLK}-抵費地-2", "poly": frag, …}
[verify/wf_f4.py:1458] for _k, _p in fb_polys.items():   # 非疊複驗：抵費地末 vs 內移宗（KL/reviewer②）
[verify/wf_f4.py:1464] def _order_fb(polys):             # 位次序不變（排除抵費地末·新增於末端位）
```

🔒 **逐處追查（正面列舉·框 ＝ 列框）**：`verify/wf_f4.py` 之 `fb_polys` 命中 **`7`** 列，
其賦值式三處（`1428`／`1444`／`1452`）；其首處逐字 ＝

```
[1422]             _, _rend_poly, _rend_area = ns["_end_region_R"](
[1426]             if _rend_poly.geom_type != "Polygon":
[1427]             _abate_key = f"{blk}-抵費地末"
[1428]             fb_polys = {_abate_key: _rend_poly}
```

⇒ 🔒 **碼面<u>確有</u>一「抵費地」之多邊形物件——其為 `{blk}-抵費地末`（`_end_region_R` 之回傳），
即「無勝者 fallback」之<u>末端</u>抵費地**；
而**街角 `forced` 之 `buf` 帶**於生產碼八檔（`app.py`／`wf_f0`〜`wf_f4`／`stepg_pipeline`／`selection_pipeline`）內——
`抵費地街角` 命中 **`3`** 列（`app.py:21587` 之 UI 警語 ＋ `wf_f4:924`／`1421` 之**守恆式註解**）、
`街角抵費地` 命中 **`4`** 列（皆於 `app.py` 之 `def main` 區間內之 UI／診斷）、
`抵費地末` 命中 **`9`** 列——**⛔ 具名之多邊形物件**。

🛑 **⇒ 前批之否定性宣稱須<u>收窄其受詞</u>**：其為真者係「**街角 `forced` 之 `buf` 帶⛔ 有具名之配地多邊形**」，
**⛔ 「碼面⛔ 產生任何抵費地多邊形」**。🛑 **本批只出艙此一射程差·⛔ 判其效力。**

🔒 **`docs/specs/` 內之相關規格（loud 具名·⛔ 判其效力）**：
`docs/specs/W-G.4_規格_v3.md:207`–`:208` 逐字載
「判準＝『它出不出報表欄』：`buffer_S′` **不出報表欄**（forced 抵費地列之 `S(m)` 現值即 **0.0**·`stepg:748`）」
／「→ 其**法定產物**為「**抵費地面積（2dp ㎡）＋地籍界線（座標）**」，**buffer_S′ 本身非法定成果長度**」。
🛑 **⛔ 判其是否已實作、⛔ 判其效力。**

---

## `§五`　工項三：登記（逐字由單供·⛔ 改寫）

🔒 三則之塊內文字**全由抽取管線產生**（`常規七 一`／`二`：三數與塊內文字出自**同一次**抽取）：
`§五-1` ＝ 單之列 `119`–`128`（`10` 列／`1049` B）；`§五-2` ＝ 列 `132`–`137`（`6` 列／`714` B）；
`§五-3` ＝ 列 `141`–`145`（`5` 列／`553` B）。

| 落點 | 期初 bytes | 期末 bytes | `+N` | `deletions` | `CR` | 嚴格前綴·造甲[必真] | 造乙[必偽] |
|---|---|---|---|---|---|---|---|
| `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`§五-2`） | `808375` | `811384` | **`+3009`** | **`0`** | `0` | `True` | `False` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md`（`§五-1` ＋ `§五-3`） | `877570` | `880009` | **`+2439`** | **`0`** | `0` | `True` | `False` |

🔒 **`GB-132` 之避讓經實測成立**：立後四簿 ＝ 自誤 相異 **`376`**／MAX **`386`**（**恰 `+1`**）；
`GB` `167`／`169`、`VR` `80`／`95`、`K-9` `28`／`29` **逐位未動**。

🔒 **`§五-1`（`GB-169` 之成因指名）所登記之受詞（節略·其全文逐字已入 `GB` 登記表檔末）**：
其 `A1` 二格之錨 ＝ **`corner_pt`**（逐位等於該側 `SIDE_LINE` 之 `p1`）；
其 `B1` 二格之錨 ＝ **`end_pt`**（＝ `corner_pt ＋ actual_max_proj·d̂`·`actual_max_proj` 源 ＝ **`_oblique_s_max`**），
**⛔ 逐位等於** `p1`。🛑 該節**⛔ 解除、⛔ 收窄 `GB-169`**（射程仍為 `R(W_0) > 0` 之側·四格全數在內）。

---

## `§六`　一句話推導（驗收固定項）

> 以 `W-G.9-15` 甲組之**同一**機械規則判之，「該點即『面積換算出來的替身線』」一語所在之行
> **以二空白起首** ⇒ `is_quote` ＝ `False` ⇒ **不在任何引文區塊內** ⇒ 判 ＝ **助手側補充**
> （判別力二造：`K-9-5-13（二）` 之裁定二行判 **KL 逐字**／`K-9-5-6` 之「首宗起點」行判 **助手側補充**·**相異**）；
> 而 `_corner_buffer_S` 之 bisect 目標面積係由 `_block_strip(` 之**實際多邊形**算得（函式體內命中 `1`·
> 其 docstring 逐字自載「**取代舊矩形近似 `range_area ÷ avg_depth`**」）⇒ **⛔ 矩形／平均深度之近似**。

---

## `§七`　🩸 本批之**自捕**（⛔ 頂替·單 `§八` 所令）

| # | 形 | 徵候 | 攔法（已施） |
|---|---|---|---|
| `1` | **批次替換之順序相依**：寬替換 `W-G.9-282` → `W-G.9-283` 把**剛插入之造甲檔名**一併改掉 | 造甲[**必為 `1`**] 得 **`0`** ⇒ 對照組失效 | 逐替換報「期／實」**之外**，須再驗**替換後之組成**；改逐字指定並復驗 ⇒ 造甲 `1` ✅ |
| `2` | **`40` 碼 `commit` 之替換未覆蓋判準式內之<u>短前綴</u>**（`startswith("8d1e50e8")`） | 閘 `1` 印 `❌` 而二值**逐位相同** ⇒ **量測器紅**（⛔ 受詞紅） | 併替短前綴並**出艙殘留數**（舊二短前綴皆 `0`）⇒ 閘 `1` ✅ |
| `3` | **坑 `3` 重踩**：`python -c "…"` 內之**反引號遭 bash 命令替換** | 修補後之文字**靜默缺字**（`（ ＝ None）`／`行首符合 」`／`（單  明令）`） | 依 `§四` 坑表之攔法——**一律 `Write` 落腳本**；反引號以 `chr(96)` **執行期組出** |
| `4` | **字樣錨非唯一**：造甲之錨命中 `2`（縮排之引述 ＋ 正本）而程式取 `hs[0]` | `rc = 5`·二造同判「助手側補充」⇒ **量測器紅**（⛔ 受詞紅） | 錨改取**唯一**之字樣，並於器內加 **`uniq()` 之 loud 拒測**（命中 `≠ 1` 即逐筆列出並 `rc = 5`） |

🔒 四則**皆於出艙前攔下**；其鑄號**候發單側**（單 `§一` 裁 `3` 已就同族採「**⛔ 鑄坑·末端追加**」之體例）。

---

## `§八`　收工閘之實測（**入倉後末端追加**）

> 🛑 本節於本報告入倉**之後**追加——⛔ 預寫。

---

## `§九`　🛑 `⛔ 為之事`之逐項自檢

| # | 受詞 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字 | ✅ 未動（收工閘 `6` 之 blob 期初＝期末） |
| `2` | ⛔ 改任何 `*_EXPECT` | ✅ 未改 |
| `3` | ⛔ 修 `GB-167`／`168`／`169` | ✅ 未修（`GB` 登記表之異動係**檔末追加**·⛔ 改 `GB-169` 原節一字） |
| `4` | ⛔ 解除、收窄或結清 `GB-168`／`GB-169` | ✅ 未為 |
| `5` | ⛔ 落地 `K-9-5-12`／`K-9-5-13`／`K-9-29` 之任何子項 | ✅ 未為 |
| `6` | ⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 未改 |
| `7` | `GB-162` 三禁令；**⛔ 引任何自 baseline 取得之數** | ✅ 皆守 |
| `8` | ⛔ 施替身、⛔ 反事實、⛔ 未經本單具名之長跑 | ✅ 未為（本批**⛔ 驅動** `run_step_g`／`run_verification`；`§四` 款 `4` 之逐位值係**轉引前批落檔**並具名之） |
| `9` | ⛔ 鑄 `GB`／`VR`／`K-9`／坑之任何號、⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未為 |
| `10` | ⛔ 追改任何已入倉文件一字 | ✅ 未為——二登記表之異動**皆嚴格 append-only**（前綴檢二造 `True`／`False`），`deletions` 全 `0` |
| `11` | ⛔ 判「替身線」之所指、⛔ 判碼面是否違反任何裁、⛔ 判任何側為違反、⛔ 判孰誤、⛔ 提修法主張 | ✅ 未為（`§三`／`§四` 皆**出艙即止**） |
| `12` | ⛔ 呈 KL；⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ 未為 |
| `13` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§八`／`§十` 皆**入倉後／推送後**追加 |

---

## `§十`　逐 `commit` 與 `push` 之 `ls-remote` 實查

> 🛑 本節於推送**之後**追加——⛔ 預寫。
