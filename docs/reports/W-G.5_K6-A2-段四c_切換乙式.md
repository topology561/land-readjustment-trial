# W-G.5／K-6-A2 段四(c) — **切換至乙式**（生產行為首次真變）

> 基準 `af23188`。KL 裁 2026-08-05：**(c) 放行**。
> ⛔ **前四批之「只算不換·生產零呼叫點」紅線自本批解除。**
> 正典：`grep -n "^#### 🔒 K-9-6-b-2 " docs/rulings/K-6_街角地分配程序與可分配判準.md`（含放行留痕）。

---

## 〇、交付狀態

| 施工單 | 內容 | 狀態 |
|---|---|---|
| §一 | 切換（`_build_corner_range_v3` 多傳 `block_vertices`） | ✅ |
| §四-1 | 呼叫點斷言**反轉**（0 → 1） | ✅ |
| **§四-2** | **夾具 FAIL → PASS（本系列唯一一次真正的獨立驗收）** | ✅ **成立** |
| §四-3 | 基準線逐格對帳（⛔ 不重烤） | ✅ **零新增／零消失·29 列改值全可歸因** |
| §四-4 | PK 十六格 | ✅ **決定性欄位零翻盤**·唯一容許之指數變動如預告 |
| §四-5 | 守恆 | ⚠️ **一處偏離施工單條款·見 §五**（`trunk D 0m` 殘差 `0.01 → 0.03`） |
| §四-6 | 兩族對拍 | ✅ 名目族零翻轉／夾具族 **1 項翻轉**（見 §六 之數目更正） |
| §二 | GB-37 收尾 | ✅ 偵測器整段移除·驗收 grep **零命中** |
| §三 | 失敗考古 **§40** 入倉 | ✅ |
| §五 | 落地矩陣與登記表 | ✅ |

---

## 一、切換本體 ＋ **一處必要之連帶更動**

### 1-1 切換（施工單 §一·一個引數）

`_build_corner_range_v3` 內之 `k97_solve_alloc_t` 呼叫加 **`block_vertices=block_vertices`**。

- ⛔ **`k97_solve_alloc_t` 函式體未動**（第二層已於 `af23188` 落地並經階段 A／B 驗證）。
- ⛔ **兩處生產呼叫點逐字未動**——以 `git show HEAD:app.py` 抽出 `_rng_wb`／`_rng_cr`
  各 12 行與現行**逐行比對** ⇒ **皆 True**。

### 1-2 🔴 連帶：**自檢①／①′ 之量測線必須同步改為乙式帶**（純技術·施工單未列）

施工單 §一 只列切換一個引數。惟 `_build_corner_range_v3` 之**自檢①**係
「回量帶內最小寬 ＝ `T`」之**定義性回代**，其量測線原為甲式之 `(d=0, d=D_at)`。
切換後 `t*` 已是乙式解 ⇒ 若沿用 `(0, D_at)`，**量到的是另一個帶**，
其與 `T` 之差恰為**階段 A 之殘差 `−k·δ_P`**（最劣 `4.867e-03`），
而 `eps` 之硬上限 `_CR_EPS_LEGAL_CEIL = 0.005`
⇒ **僅以 2.7% 之餘裕僥倖不紅，且所驗者非本構造之定義**。

⇒ 已改為量**乙式帶之二端** `(_band_lo, _band_hi) = (δ_P, δ_P + D_new)`
（取自 `k97_solve_alloc_t` 之 `band_d_top`／`band_d_bot`，`af23188` 已備）。
🔒 **未動 `k97_solve_alloc_t` 函式體、未動兩處生產呼叫點** ⇒ 施工單 §一之紅線未破。
⚠️ `_t_override` 之診斷重播路徑仍取第一層口徑 `[0, D_at]`（自檢① 於該路徑本即跳過），
並於碼內註明「⛔ 勿據覆寫輪之帶推論乙式之幾何」。

### 1-3 陳舊敘述之清除（純技術·必要）

切換使 `app.py` 內多處敘述**當場過期**，已同批更正（⛔ 留著即成下一個人的錯誤前提·失敗考古 §36）：

| 位置 | 過期之敘述 | 處置 |
|---|---|---|
| `k97_solve_alloc_t` docstring 之 `block_vertices` 節 | 「**(c)-2(a) 為只算不換 ⇒ 生產零呼叫點**」＋自癒查法「四行內**不得出現** `block_vertices`」 | 改為「**生產已傳**」＋查法反轉為「**應出現**」 |
| 同 docstring 開頭 | 「段三(a)：並行實作·只算不換·**本函式未接生產路徑**」 | 標**已過期·存查**（自 `0429772` 起即生產之 §四 定位器） |
| 第二層區塊之抬頭註 | 「⛔ 只算不換」「生產行為零變化」 | 改為「**段四(c) 起即生產路徑**」 |
| `probe_K9_7d_stageA.py` | 「只算不換·第二層維持零生產呼叫點」 | 標明其注入已成**同值 no-op**、價值改為保留 A／B 可重跑對照 |

### 1-4 探針之「舊／新」語意**再反轉**（純技術·必要）

切換後生產**已是第二層** ⇒ `probe_K9_7d_layer2` 與 `probe_K9_7d_pk_impact`
若沿用切換前之寫法（一層＝生產、二層＝注入 `t_star`），**兩欄將同源、Δ 恆 0**
——即上批以 §五-2 登記並修好之**同型缺陷**。
⇒ 二檔已改為：**「二層」＝生產原樣；「一層」＝以 `_t_override` 注入 `t_star_layer1`**。
**驗證**：反轉後二檔之數字與 `af23188` 逐格相同（見 §四），⇒ 非退化。

---

## 二、§四-1／4-2 立即驗

### 2-1 呼叫點斷言**反轉**

```
grep -n -A4 "_k97 = k97_solve_alloc_t" app.py | grep -c "block_vertices"
```
⇒ **1**（前四批為 **0**）。

### 2-2 🔴 §四-2 —— 夾具 **FAIL → PASS**

⛔ **本批未動 `verify/fixture_corner_range_k8.py` 一個字元**
（`git diff --quiet -- verify/fixture_corner_range_k8.py` ⇒ 未動）。

該夾具於 `af23188` 已改為乙式，而生產當時仍為甲式 ⇒ 其紅落在
`R1/left 0m`（`+1.63e-03`）與 `R3/right 0m`（`+4.53e-03`）兩格。**切換後**：

```
python verify/fixture_corner_range_k8.py   ⇒ rc = 0（PASS）
```

| 情境 | 街廓側 | 靶 T | 夾具獨立量測 | Δ | 判 |
|---|---|---:|---:|---:|:--:|
| 0m | R1 left | 3.50 | 3.500000 | +1.30e-10 | ✅ |
| 0m | R3 right | 3.50 | 3.500000 | +9.48e-11 | ✅ |
| — | 其餘 14 格 | — | — | \|Δ\| ≤ 3.14e-10 | ✅ |

實料：`verify/out/K6A2_seg4c_fixture_after.log`【T1】。

🔒 **這是整個 K-9-7／K-9-8 系列中，夾具第一次以「未被同批修改之獨立實作」驗收一次生產切換**
——夾具寫在 `af23188`、切換做在本批，**兩者不同批**。

---

## 三、§四-3 基準線逐格對帳（⛔ 未重烤任何 CSV）

**手法**：把**切換前**（`verify/out/K6A2_seg4c2a2_runall.log`）與**切換後**
（`verify/out/K6A2_seg4c_runall.log`）之**全部 `baseline=… got=…` 不符列**取差集。
⇒ 這才問得出「**哪些列是因本次切換而動**」；直接列全部不符列會混入
`verify/baselines/` 之既有陳舊（N-19′ 深度、K-8 §三 新構造等·屬 P-H 波末重烤同批）。

```
切換前 285 列 ／ 切換後 285 列
── 切換後**消失**之不符列：0
── 切換後**新增**之不符列：0
── 兩邊都不符但 got 改值：29
```

### 3-1 🔑 根因列（直接由街角規定範圍變動而來）——**恰為攤表中位移最大之二格**

| 閘 | 鍵 | 欄 | got 切換前 → 後 | 對應攤表列 |
|---|---|---|---|---|
| 參數0m | R1 | 【左】街角最小面積(㎡) | **114.76 → 114.71** | `0m R1/left` 114.761386→114.707167 ✅ |
| 參數0m | R3 | 【右】街角最小面積(㎡) | **226.91 → 226.69** | `0m R3/right` 226.910765→226.692974 ✅ |
| 指配0m | R3 | 【右】最小面積(㎡) | 226.91 → 226.69 | 同上 |
| 碎片幾何 | 0m R1-抵費地-1／-2 | 參考_街角範圍 | 114.76 → 114.71 | 同上（同值傳遞） |

⇒ **有差異之格 ⊆ 攤表中 `Δt* ≠ 0` 之格** ✅（施工單 §四-3 判準一）。
其餘 14 格之 Δ 面積 ≤ `1.04e-04 ㎡`，**低於報表 2dp 之列印精度** ⇒ 不產生差異列（一致）。

### 3-2 下游列（全部集中於 `R3 0m` ＋ 一筆 R1 之戶別加總）

`F.0·池差0m`／`F.0·決策0m`／`F.0·滑池槽0m`／`F.2·池流向0m`／`F.3·池流向0m`／
`v3·J表0m`／`v3·滑池槽0m`／`W-D.4清單0m` 共 25 列，**全部為 `R3`**（唯一例外
`W-D.4清單0m G007 ΣG_戶` 為含 R1 宗之戶別加總）。量級：

| 代表列 | got 切換前 → 後 | Δ |
|---|---|---:|
| `F.0·滑池槽0m R3 ΣG(㎡)` | 2405.24 → 2405.22 | −0.02 |
| `F.0·滑池槽0m R3 Σ配地幾何(㎡)` | 2405.23 → 2405.21 | −0.02 |
| `F.0·池差0m R3 池_F.0(㎡)` | 1801.57 → 1801.59 | +0.02 |
| `F.0·滑池槽0m R3 J(k*)` | 24.6668 → 24.6800 | +0.0132 |
| `F.0·滑池槽0m R3 ΣRw_R實跑(%)` | 54.94 → 54.97 | +0.03 |

**機制**：`R3/右` 之街角規定面積 `226.91 → 226.69`（−0.22㎡）⇒ 該端之
`min_area_to_apply` 門檻下移 ⇒ **`R3/右` 本即 `winner ＝ —`／`forced_offset ＝ True`**
（PK 十六格未變·§四）⇒ 影響落在**強制留設抵費地之落位**與其後之池／ΣG 分帳。
⇒ **無「攤表以外之格」** ⇒ 施工單 §四-3 之停機條件**未觸發**。

### 3-3 連帶：**E 系列實測快照閘**（既有之 golden 1 紅）之違例數 `331 → 335`

`verify/out/probe_ruling_N_e1_touch.log`（由 `run_all` 重生）之
`RESULT: FAIL（331 違例）` → **`（335 違例）`**（＋4）。

- 🔒 **該閘切換前即為紅**（CLAUDE.md 既載之「golden 16 支（1 紅＝E 系列實測快照閘）」）
  ⇒ **判定未翻轉**、不進兩族對拍之翻轉計數。
- **異動之明細列全部落在 `R3` 且全部為 `0m`**：`git diff` 之列級統計 ⇒ `+14 R3／−14 R3`、
  **其他街廓零列**；量級為 `1e-4`（如寬度 `1.0266 → 1.0267`），判語欄
  （`臨街(法定粒度內)`）**逐列未變**。
- 另一處連帶：`T7③[0m]` **鑑別力**測試之壓值 `113.76 → 113.71`
  ——即 `R1/左` 街角最小面積 `114.76 → 114.71` **減 1**（該測試以「門檻下 1㎡」構造反例）
  ⇒ **完全可歸因**，且其判定仍 ✅。

⇒ 與 §三-1／3-2 同一footprint（`R1/左 0m` ＋ `R3/右 0m`），**未溢出攤表之格**。

---

## 四、§四-4 PK 十六格

實料：`verify/out/probe_K9_7d_pk_impact.log`【A】【A2】【B】。

- ✅ **決定性欄位零翻盤**：候選集合／合格集合／winner／`forced_offset` **十六格逐格相同**。
- ✅ **唯一之指數變動，與施工單預告逐字相同**：
  `0m R1/left` 之 `628-35(2)` **0.1829 → 0.183**；
  **名次第三未易位**（指數前二 舊／新皆為 `[('628-37(1)', 0.5747), ('628-36(1)', 0.2423)]`，
  與第二名間距 `0.0592` ＝ 變動量之 **592 倍**）。
- ⛔ **無任何其他指數變動、無任一名次易位** ⇒ 停機條件未觸發。
- 三條路徑確有被走到（【A2】·7 列）：`0m R1/左` 三筆 ＋ `0m R3/右` 四筆。
  其中 `R3/右 628-28(1)` 之真交集 `13.83 → 13.79`——即 KL 放行依據所引之
  **最小餘裕格**（距資格線 `1.0㎡` 餘 `12.79㎡`、最大擾動 `0.09㎡` ⇒ 142×）。

---

## 五、⚠️ §四-5 守恆 —— **一處偏離施工單條款，如實上報**

### 5-1 逐閘實測

| 閘 | 切換前 | 切換後 | 容差 | 判 |
|---|---:|---:|---:|:--:|
| `F.0 結構永久閘0m／3.5m`（trunk B） | 全綠 | 全綠 | — | ✅ |
| `F.3 結構永久閘3.5m`（trunk D） | `0.01` | `0.01` | 0.585 | ✅ **未變** |
| **`F.3 結構永久閘0m`（trunk D）** | **`0.01`** | **`0.03`** | 0.585 | ✅ 仍綠·⚠️ **變差** |
| `F.4 結構永久閘`（trunk E） | 未印 | 未印 | — | —（F.4 於 3.5m 先行 FAIL·既有紅） |

### 5-2 🔴 偏離之陳述（⛔ 不粉飾）

施工單 §四-5 載「**守恆殘差不得較切換前變差**」。
`trunk D 0m` 之 `cons_resid` 由 **`0.01` 變為 `0.03`** ⇒ **該條款字面未達成**。

**量之身分**（`grep -n "cons_resid" verify/wf_f3.py`）：
`cons_resid = |ΣG(全區) + Σ抵費地池 − 22803.33|`，即**守恆恆等式之殘差**。

- **相對量**：`0.03 / 22803.33 ＝ 1.3 ppm`；**佔容差 `0.585` 之 5.1%**。
- **歸因**：與 §三-2 同源——`R3 0m` 之 `ΣG −0.02`／`池 +0.02`〜`+0.03`
  （皆 2dp 列印）之重新分帳；**`3.5m` 情境完全未動**（`0.01 → 0.01`）
  ⇒ 變動**僅限 `0m` 且僅限 `R3`**，與位移最大之格一致。
- **判**：**未破閘**（`0.03 ≤ 0.585`）、**未破守恆恆等式之結構**；
  惟 ⛔ **本批不自行認定「0.01→0.03 屬容許」**——該條款是 KL 於施工單所定之驗收線，
  **謹以本節上報，由 KL 判是否接受**。

---

## 六、§四-6 兩族對拍

```
python verify/tools/nameset_diff.py verify/out/K6A2_seg4c2a2_runall.log \
                                    verify/out/K6A2_seg4c_runall.log
```

```
【名目族】 靶 132 項 86/46 ／ 本次 132 項 86/46
           出 1 ／ 進 1 ／ **判定翻轉 0**
   - [PASS] F.3 結構永久閘0m（… |Σ(G−幾何)| 0.01 ≤ 0.585）
   + [PASS] F.3 結構永久閘0m（… |Σ(G−幾何)| 0.03 ≤ 0.585）
【夾具族】 靶 12 項 10/2 ／ 本次 12 項 **11/1**
           出 0 ／ 進 0 ／ **判定翻轉 1**
   ! fixture_corner_range_k8.py: FAIL → PASS
```

**名目族之「出 1／進 1」須具名說明**（施工單 §四-6）：
該工具以**整行**為名目，而 `F.3 結構永久閘0m` 之行內含殘差數值
⇒ `0.01 → 0.03` 使**同一個閘**被讀成兩個名目。**其判定仍為 PASS ⇒ 翻轉 0。**
⛔ 這**不是**新增或消失之名目；實質即 §五 所報之守恆殘差變動。

### 🔧 對施工單 §四-2 「**2 項** FAIL → PASS」之數目更正

**實測為 1 項。** `af23188` 之夾具族為 `10 PASS / 2 FAIL`，其中
**`fixture_corner_range_k8.py` 是唯一與本切換相關者**；另一 FAIL 為
**E 系列實測快照閘**（CLAUDE.md 既載之 golden 1 紅），與本批無關、切換後仍紅。
⇒ 施工單所稱之「2」應係指**該夾具內之兩格**（`R1/left 0m` 與 `R3/right 0m`），
而非兩支夾具。**二格確已同時轉綠**（§二-2）。

---

## 七、§二 GB-37 收尾（假陰性偵測器）

**移除**：`k97_solve_alloc_t` 內以 **`P_front(t*)`** 測截角之偵測器**整段刪**，
其回傳鍵一併移除；docstring 同步。
**遷移消費端**：`verify/probes/probe_ruling_K9_7_alloc_t.py` 之【D】段**整段刪**，
留指路行 → `verify/out/probe_K9_7d_layer2.log`【C】段（`P_block` 落點表）。
⛔ **未改為讀 `P_block_on_chamfer`**——該探針**不傳** `block_vertices`、取到必為 `None`，
那只會生一個新的假陰性（施工單 §二-2 明令）。

**驗收**：

```
grep -rn --exclude-dir=.claude "<已刪之回傳鍵>\|<已刪之區域變數>" --include="*.py" .
```
⇒ **零命中**（`grep` rc=1）。
🔒 為使該 grep 為**真零命中**，碼內之墓碑註**刻意不寫出已刪符號之字面名**
（改以「舊之 `P_front` 截角旗標」稱之），考古說明完整保留。
🔒 `_on_cham98`（`k98_virtual_measure_block` 之另一符號·`\b` 不匹配）**未動**、仍 4 命中。

---

## 八、§三 失敗考古 **§40** 入倉

`.claude/skills/failure-archaeology/SKILL.md` 新增
**§40「於非正交基底上以內積取係數——方向沒選錯，錯在誤以為內積能取出分量」**。
含：症狀／16 格實測（最劣錯 `4.120598 m`）／機制（`(P−F1)·b̂n = δ_P + α·g`）／
正解（以 `n̂` 消 `α`）／**⛔ 與 `34e3027` 相鄰但非同族**之逐條辨異／三條旁證／戒三則
（其中 (c) 給出最便宜的檢核法：找 `α` 大而真值近零之格，兩式差四個數量級）。

⛔ **未沿用 claude.ai 前一則所述之「`34e3027` 同族復發」**（KL 2026-08-05 已更正該歸類）。

---

## 九、§五 落地矩陣與登記表

- 正典 `K-9-8` 標題改 **✅ 已實作**（段四(c)）；`K-9-8-a` 落地狀態更新
  （**GB-19 ✅ 已落地**／**GB-18 仍未修·已重標段六**·原文存查）。
- K-9 矩陣之 `K-9-8` 列同步。
- `K-9-6-b-2` 末加註 **KL 之 (c) 放行留痕**（最大位移 `0.004846 m`／餘裕比 `142×`／
  PK 決定性欄位零翻盤·含最小餘裕格之實料指引）。
- 登記表：**GB-19 ✅ 已落地**／**GB-36 ✅ (c) 已切換**／**GB-37 ✅ 已收**。

---

## 十、次步 ＋ 本批未做

1. ⚠️ **上呈 KL**：§五 之 `trunk D 0m` 守恆殘差 `0.01 → 0.03`（偏離 §四-5 條款字面）。
2. **下一站 ＝ 段五（K-9-6 ＋ K-9-3·GB-13）**——依交接文之依賴順序
   `K-9-7 → K-9-8 → K-9-6 → K-9-5`，**K-9-8 已完成**。
3. **已知可能連帶（⛔ 本批未修·施工單 §六）**：
   - **GB-32**：`T4[0m]`／`T1[0m]` 之期望式與錨陳舊——本批**未觀察到其轉紅**
     （名目族零翻轉）；維持登記。
   - **GB-26**：依 KL 明令**維持開啟**；其舉證數字於本批未再變動
     （`probe_ruling_K9_7_alloc_t` 之 `R4/left 0m` 仍為 `+0.111cm`／`+0.037㎡`）。
4. ⛔ **本批未做**：`data/r3.dxf` 之去留（KL 未決項）／`verify/baselines/` 之任何重烤
   （P-H 仍凍結）／GB-18／段五、段六。
