# `W-G.9-270R5c`　補令五之執行（坑 `av`／`aw` ／ `自誤 372` ／ 遷移項更正 ／ `p9′` 值空間法）

> **態**｜開工 ＝ `a43a6472f1cb4398856fe44815310e38d3f3711f`（`ls-remote` 實查·`left-right ＝ 0　0`）｜
> 工項零後 ＝ `121804a36e745b362e10264a1211445386aef60e`
> **級**（作業常規追加五 `§二`）＝ **中**（純登記 ＋ 純診斷·**零生產碼**）
> 🛑 **本批⛔ 辦**（補令五 `§七`）：⛔ 動 `34` 檔一字（含 `wd4_tier_list.py`／`fixture_block_depth_n19p.py`／
> 五個 `*_EXPECT` 之任一）／⛔ 落地 `GB-164`・`GB-166`／`GB-162` 三禁令維持／
> **⛔ 以 commit 空間二分跨越洞**（`§二-A` 款 `4`）／⛔ 判 `p6` 之交錯為缺陷／⛔ 呈 KL 任何事／
> ⛔ 因 `p9′` 款 `1` 查出新紅錨而自行歸因或提處置。

---

## `§零`　開工閘 ＋ 工項零

| 受詞 | 實測 | 判 |
|---|---|---|
| `ls-remote` 主線 | `a43a6472f1cb4398856fe44815310e38d3f3711f` | ✅ ＝ 補令五 `§零` 所載 |
| `ls-remote` `p3a` | `05a11bd665f6c2790ff821c9e3a1606651e345d4` | ✅ 未動 |
| `left-right` | `0　0` | ✅ |
| **條款一** `CR` | **`0`**（`LF` `153`）——位元組層二法並取；判別力造甲（真 CRLF）得 `2` | ✅ |
| **條款一** `core.autocrlf` | 有效值 **`false`** | ✅ |
| **條款二** blob `sha1` | `afcc78d2f31961d3db9f81b86a73bf56235b2e60`（`index` 時 ＝ `HEAD:` 時） | ✅ |
| **條款二** blob 內容 `sha256` | `56d42db906bbe2c476868b921dc7c463a659e6056e1f60703bdb0fee1377518a` ＝ **磁碟值**；`12879` B；`diff` ＝ **DIFF-EMPTY** | 🟢 **往返逐位保存** |
| **條款三** 來源逐字 | 主 checkout `docs/orders/` 之 untracked 檔·`12879` B·⛔ 由 KL 於本回合貼入 | ✅ |

🔒 **對照組（證此量測非恆真）**：補令四 blob 內容 `sha256` ＝ `6fe2c71ae05d4023…` ⇒ **相異且逐位相符**。
🔒 **搬移**：`cp` ＋ 全 ASCII glob `W-G.9-270_*p9*.md`（唯一性 `1`·判別力 寬 glob `7`／必不命中 `0`）。

---

## `§一`　序 `1`：鑄坑 `av`／`aw` ＋ 鑄 `自誤 372` ＋ `ALPHA_LABELS`→`aw` ＋ **三處**框字串齊一

### 一-1　🆕 本波新增之坑 `av`〜`aw`（補令五 `§一-A` **准鑄**·標籤續 `au` 之後·⛔ 跳號·⛔ 四簿之號）

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `av` | 🔴 **以 `tail -N`／`head -N` 取長跑之結果**：被測物之診斷輸出（逐宗／逐格）**淹沒**結果列；若其拋例外，`traceback` 亦同遭截 ⇒ **受詞靜默失去，而輸出讀起來完全正常** | 本批取 `a2c4b2b` 與其父之 `tiers` 時以 `tail -6` 取，所得**全係 `[Δ_i·R1·…]` 之診斷列**——滿屏皆是合理之數字，而我所印之結果列**一列未見**；🔴 其時 `compute()` 實已拋例外，而 `traceback` 亦同遭截 ⇒ **二重失明** | 結果加**唯一標記**並以 `grep -F` 取；`grep` 落空時 **loud 具名「⛔ 取得標記列 ⇒ 量測器紅」**，⛔ 靜默當作無結果 |
| `aw` | 🔴 **搜尋之標記其字面存在於<u>搜尋者自身之源碼</u>中**：合流（`2>&1`）或 `traceback` 使該源碼列進入被搜文本 ⇒ `grep` **偽陽**，命中的是自己 | 本批改用標記 `%%%RES%%%` 後 `rc=1`，而 `grep` 之「命中」竟是 `print('%%%RES%%% FAIL %s: %s' % …)` **本身**——🔴 **讀起來像「器有輸出」**，實則管線早已斷（`'%%%'` 於 `%` 格式化下解為 `%` ＋ `%R` ⇒ 無效格式字元而拋） | 標記**執行期組出**（例 `"RES" + "MARK" + str(7*13)`·⛔ 字面）＋ **分離二流**（`capture_output=True`·**只搜 stdout**） |

🔒 **`av` 與坑 `al` 非同族**（發單側 `§一-A` 之判·⛔ 再議）：`al` 之受詞為**母體為 `0`**，
`av` 之受詞為**輸出之取法**——二者**可同時發生而互不掩蔽**。
🔒 **發單側之拆分已採納**：我原把「標記含 `%`」與「合流使 `grep` 命中源碼列」**併為一則**；
發單側裁「前者係**一次性之寫法錯**（其徵候 loud、其攔法即改寫法）、**⛔ 為坑**」⇒ 本表只列後者為 `aw`。

### 一-2　`ALPHA_LABELS` 之擴充（`au` → `aw`）＋ **三處**框字串齊一（`§一-1` 准改第三處）

| 受詞（逐字） | 改前 | 改後 |
|---|---|---|
| `ALPHA_LABELS` | 止於 `"au"` | 續加 `"av", "aw"` |
| `# 字母系：\`a\`〜\`z\` ＋ 二字母形 \`aa\`〜\`ak\``（**第三處·註解**） | `aa`〜`ak` | `aa`〜`aw` |
| `run(rev, ALPHA_LABELS, "字母系 …")` | `aa`〜`au` | `aa`〜`aw` |
| `issuer_frame(rev, ALPHA_LABELS, "字母系 …")` | `aa`〜`au` | `aa`〜`aw` |

🔒 **三處齊一之逐字自證**：`aa`〜`aw` 命中 **`3`**（期 `3`·逐處 `:40`／`:280`／`:282`）；
`aa`〜`au` 殘留 **`0`**；`aa`〜`ak` 殘留 **`0`**；判別力（寬字樣 `字母系`）＝ **`4`** ⇒ 該檢⛔ 恆為 `3`。
`numstat` ＝ `5`／`4`（該檔⛔ 在禁改清單 ⇒ 得就地改）；`py_compile` OK；`CR` ＝ `0`。

### 一-3　`自誤 372` 之鑄（責 ＝ **發單側**）

**受詞**（`§零` 逐字）＝ **以行號為指涉之錨，而未同格具名其逐字**；
**通則** ＝ **凡指涉倉內一行，須同格具名其逐字**；行號僅為**導引**，⛔ 為錨——
**逐字命中而行號不符 ⇒ 以逐字為準並照實具名行號已腐**。

🔒 **取號現查**（態 `blob@121804a`·母體 追蹤 `.md` **`732`** 檔·二形並取·錨定框）：
`自誤 372` B 形 `1` 檔／`4` 列・C 形 `1`／`4`；**逐筆歸類**（`裁 H`）——`4` 列**全數**為
**補令五自身之授權條款** ⇒ **⛔ 一列為定義列式** ⇒ **⛔ 撞號**。
**對照組**：甲 `自誤 371` `4`／`6`・`4`／`6` ✅；甲2 `自誤 368` `11`／`28`・`11`／`27` ✅；
乙 人造哨兵（**執行期組出**·字面⛔ 出艙）`0`／`0` ✅ ⇒ **器非紅**。

🔒 **閘 `①②`**（`常規四（九）二`·本次為**次號**⇒ 閘 `②` **適用**）：
自誤 相異 `361 → 362`（`+1`）／**MAX `371 → 372`** ✅／缺號集 `[106,355,356,357]` **⛔ 動** ✅；
其餘三簿（`GB` `164`／`166`・`VR` `80`／`95`・`K-9` `28`／`29`）**⛔ 動** ✅。

---

## `§二`　序 `2`：`GB-7` 之補追加 ＋ 波末遷移項之更正

| 落點 | 受詞 | 增量 |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md`（檔尾） | **`GB-7` 之補追加**（`§四`·其前提之破裂**歸因於圖資切換**） | `+58`／`−0` 列·`825106 → 828856` B |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md`（檔尾） | **波末遷移項之更正**（`§三`·遷移目標已陳舊） | `+68`／`−0` 列·`295061 → 299931` B |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md`（檔尾） | **`自誤 372`** | `+37`／`−0` 列·`753894 → 757037` B |

🔒 **`append-only` 之機驗**（基座 `121804a`·⛔ `--cached`）：三檔皆 `new.startswith(old)` ⋀ `len(new) > len(old)` ＝ **OK**；
判別力［必 `FAIL`］`old.startswith(new)` ＝ `False`（期 `False`）；`numstat` 刪除欄逐列為 `0`；三檔 `CR` 皆 `0`。
🔒 **`docs/rulings/**` 屬禁改清單**，惟本節為 `deletions ＝ 0` 之**純末端追加** ⇒ 合 `常規一 §二 補款③ (2)`。
🔑 **`GB-132` 之避讓驗**：K-6 追加後其 `K-9` 之**相異仍為 `28`**（⛔ 變）⇒ 本節**⛔ 鑄任何 `K-9` 號** ✅

### 二-1　🔑 `GB-7` 補追加之要旨

- 其「本案零後果」之前提於 **`V6.dxf` 態<u>確實成立</u>**（`R4` 兩形同值·`Δ ＝ 0`·**當場實測**）
  ⇒ **該敘述⛔ 為誤**，係**被圖資切換所破**、⛔ 自始即錯。
- **破裂之受詞** ＝ `4214b3a`（`V6.dxf → V6_1.dxf`），其 commit message 逐字載
  **KL `2026-08-24` 裁「是，以 `V6_1.dxf` 為生產與驗證之唯一圖資」**（`K-9-20`）。
- 🔑 **新測之照實併記**：二形之 ½ 顯示**同為 `57.94`** ⇒ **`Δ ＝ −0.005` 於 ½ 顯示層<u>不可見</u>**
  （判別力：`114.07`／`115.85`／`115.88`／`115.885` 四值之 ½ 相異個數 ＝ **`3`**·⛔ `4`；
  對照：`round(114.07/2, 2)` ＝ `57.03` vs 生產側 `_half_display` ＝ `57.04` ⇒ 坐實 `Decimal` 之必要）。

### 二-2　🩸 照實併呈：**單所稱之落點不存在**（⛔ 追改補令五一字）

補令五 `§三` 令落點「取 `verify/fixture_block_depth_n19p.py:317` **所引之 `.md` 正典**」，
而 CC 當場實測 **該檔內 `.md` 路徑之命中 ＝ `0`**（判別力：同檔 `.py` 之命中 ＝ `4` ⇒ 器非紅）。
🔒 **遷移項之實際登記處係由<u>他處</u>指認**（二錨·各命中 `1`）：
`app.py` 之逐字 `#      （遷移項登記見上開正典之「K-8 波末遷移項登記」節）。` ⋀
`CLAUDE.md` 之逐字 `含 K-6 §零〜§五、K-8 §一〜§五-2、波末遷移項登記。`
⇒ 二者皆指向 `docs/rulings/K-6_街角地分配程序與可分配判準.md`。
⇒ **本批依該實測落於 K-6**（取保守項並逐字回報·`常規二`）。
🔒 **`:317` 之逐字確為單所指者**：
`"\`wd4_tier_list.MINA_QU_EXPECT\` 114.07→115.85 ＋ \`HALF_EXPECT\` 57.04→57.93 ＋ "`。

---

## `§三`　序 `3`：**`p9′` 款 `1` —— 五錨逐錨分列**（＝ `GB-166` 之現態出艙·其解除條件 `(3)`）

🛑 **本節⛔ 歸因、⛔ 提處置**——照實出艙即止（補令五 `§七` 款 `7`）。
🛑 **⛔ 動 `verify/wd4_tier_list.py` 一字**——本器於**自身**內 `import` 各常數與 `compute()` 比對
（補令五 `§二-B` 款 `1` 明令）；`git status` 對該檔之非 `??` 列 ＝ `0`。

**器** ＝ `verify/probes/probe_WG9270R5c_p9p_anchors.py`｜落檔 `verify/out/WG9270R5_p9p_anchors.log`｜
`rc = 0`｜dump ＝ `verify/out/WG9270R5_p9p_groups_cur.json`（`19821` B）｜
**三軸**：`(1)` `blob@2fb3b7c3c870df552796d5c029dc8cd2624e24fe` 之**活體實跑**／
`(2)` 母體 ＝ 群組 `33`（建地軌 `25` ／ 公設軌 `8`）× 二情境／`(3)` 粒度框 ＝ **錨**。

### 三-1　🔴 **`3` 紅 `2` 綠**（二情境**逐錨相同**）

| 錨 | 判定式（逐字·複製自 `main()`） | `0m` | `3.5m` | 實測值 | 期望值 |
|---|---|---|---|---|---|
| `ok_m` | `(d["mina_qu"] == MINA_QU_EXPECT) and (d["half_disp"] == HALF_EXPECT)` | 🔴 | 🔴 | `115.88` ／ `57.94` | `114.07` ／ `57.04` |
| **`ok_f`** | `(d["flagged_ct"] == FLAGGED_EXPECT) and (consumed == FLAGGED_EXPECT)` | 🔴 | 🔴 | **`18`／`18`**（`0m`）·**`20`／`20`**（`3.5m`） | **`31`**（二者皆須等之） |
| `ok_t` | `(d["tracks"] == TRACK_EXPECT) and (d["tiers"] == TIER_EXPECT)` | 🔴 | 🔴 | `tracks` **逐格相符**；`tiers {"0":11,"1":9,"—":8,"2":3,"3":2}` | `tiers {"—":8,"0":13,"1":7,"2":3,"3":2}` |
| `ok_p` | `(_pub == PUB_TRACK_GROUPS)` | 🟢 | 🟢 | `["G003","G012","G013","G015","G016","G024","G028","G031"]` | 同左 |
| `ok_m2` | `(_n_pub_parcels == PUB_PARCELS_EXPECT and _n_pub_holders == PUB_HOLDER_GROUPS_EXPECT)` | 🟢 | 🟢 | `59` ／ `28` | `59` ／ `28` |

⇒ 聚合 `allok` ＝ **`False`**（`RESULT: W-D.4 FAIL`）·二情境同。
🔑 **紅錨 ＝ `['ok_m', 'ok_f', 'ok_t']`；綠錨 ＝ `['ok_p', 'ok_m2']`**。

### 三-2　🔴 **`GB-166` 之預言坐實**（照實·⛔ 驚訝·⛔ 追改任何既有結論）

🔒 **`ok_f` 係本批之<u>新見</u>**——迄今之出艙皆為單一聚合碼 `RESULT: W-D.4 FAIL`，
而該碼因 `ok_m`（**已登記之恆紅**）而**恆 `False`** ⇒ `ok_f` 之紅**結構上不可觀測**。
🔒 **此即 `GB-166` 受詞逐字所稱**：「一個**已歸因、待消化之登記紅**若與其他錨共用單一聚合出艙碼
（`allok`／`RESULT`），則該聚合對其餘各錨**永久失明**——其餘任一錨翻紅時，出艙逐字不變。」
⇒ 🔒 **本節即其解除條件 `(3)`（現態五錨之逐錨態出艙）之履行**；
其 `(1)`（逐錨分列入生產碼 ＋ KL 放行）與 `(2)`（判別力二造）**仍未成就** ⇒ **`GB-166` ⛔ 解除**。

🛑 **本節⛔ 就 `ok_f` 作任何歸因**（補令五 `§七` 款 `7`）——其歸屬歸發單側。
🔑 **惟須併記一項<u>單已預先指出</u>之交叉**：補令五 `§二-B` 款 `3` 逐字
「**`(c)` 若成立，則 `ok_f`（旗標數錨）亦應為紅** ⇒ 款 `1` 之逐錨分列即其交叉驗證。**二者相斥即先判量測器紅**。」
⇒ 🔒 **`ok_f` 確為紅** ⇒ **路 `(c)`（畸零地旗標消失）⛔ 被排除**，款 `3` 須逐一驗之（⛔ 逕取其一）。
⇒ 🟢 **二者⛔ 相斥** ⇒ ⛔ 判量測器紅。

### 三-3　自我驗證閘（⛔ 可省·證「逐錨分列」**⛔ 恆紅亦⛔ 恆綠**）

| 受詞 | `0m` | `3.5m` |
|---|---|---|
| `allok == all(五錨)`（本器之聚合語意 ＝ `main()` 之） | `True` | `True` |
| 紅錨 `≥ 1`（`ok_m` 係已登記之恆紅 ⇒ 必有） | `True` | `True` |
| **綠錨 `≥ 1`**（否則⛔ 能區辨「逐錨分列」與「全紅」） | `True` | `True` |

⇒ **器非紅** ✅

### 三-4　🩸 一項**既有之 loud 記錄**（照實併記·⛔ 本批所致·⛔ 判其為缺陷）

本次實跑之 `stderr` 併印四列（二情境 × 二退縮值）：

> `🔴 首宗下限未達（補丁六 §二）：街廓 R4 right 側 首宗 KL W=0.00 < 退縮(0.00)＋畸零寬(3.50)=3.50——loud 記錄·禁靜默/禁 clamp（重烤後應 ≥ 下限）`
> `🔴 首宗下限未達（補丁六 §二）：街廓 R4 right 側 首宗 KL W=0.00 < 退縮(3.50)＋畸零寬(3.50)=7.00——loud 記錄·禁靜默/禁 clamp（重烤後應 ≥ 下限）`

🔒 其自身逐字已載「**loud 記錄·禁靜默/禁 clamp（重烤後應 ≥ 下限）**」⇒ **係既有之設計行為**，
**⛔ 本批所致**（本批零生產碼）。🛑 **本節⛔ 判其為缺陷、⛔ 提處置**——照實併記一次。

---

## `§四`　序 `4`：**`p9′` 款 `2`〜`5` —— 值空間定位法**

🛑 **本節⛔ 以 commit 空間二分跨越洞**（`§二-A` 款 `4`）；⛔ 改 `TIER_EXPECT` 一字、⛔ 判其為缺陷、⛔ 提遷移主張。

### 四-1　款 `2`：翻梯之戶之具名 ⇒ 🟢 **判準全綠**

**器** ＝ `verify/probes/probe_WG9270R5c_p9p_flip.py`｜落檔 `verify/out/WG9270R5_p9p_flip.log`｜`rc = 0`。
**二端** ＝ `7fa957e`（`tiers` ＝ `TIER_EXPECT`）與現態（`blob@2fb3b7c`），**皆可求值**
⇒ 受詞在**值**上定位（⛔ 在 commit 上）。
**母體對齊之自我驗證閘**：二態之歸戶鍵集合**逐格相同**（各 `33` 戶·僅舊／僅現皆「（無）」）⇒ 器非紅。

| 情境 | 舊 `tiers` | 現 `tiers` | 翻梯之戶 | 方向 | 判準（戶數 `2` ⋀ 方向 `0→1`） |
|---|---|---|---|---|---|
| `0m` | `{"0":13,"1":7,"—":8,"2":3,"3":2}` | `{"0":11,"1":9,"—":8,"2":3,"3":2}` | **`2`**（`G010`／`G017`） | `0 → 1` | 🟢 **全綠** |
| `3.5m` | 同上 | 同上 | **`2`**（同二戶） | `0 → 1` | 🟢 **全綠** |

**逐戶**（`0m`／`3.5m` 二情境之翻梯戶**相同**）

| 歸戶 | 舊梯→現梯 | 宗數 | `ΣG_戶`（`0m`） | `ΣG_戶`（`3.5m`） | 旗標 | 宗清單 |
|---|---|---|---|---|---|---|
| `G010` | `0`→`1` | `2`→**`1`** | `294.14`→`285.56`（**降**） | `299.54`→`299.51`（**降**） | **`1`→`0`** | `628-40(1);628-43(1)` → **`628-40(1)+`** |
| `G017` | `0`→`1` | `6`→**`2`** | `1848.23`→`1848.93`（**升**） | `1848.37`→`1848.93`（**升**） | **`2`→`0`** | 六宗 → **`628-21(2)+;628-23(1)+`** |

**併記（款 `3` 路 `(c)` 之材料·⛔ 歸因）**：`flagged_ct` 舊 `31` → 現 **`18`**（`0m`）／**`20`**（`3.5m`）；
旗標數相異之戶 ＝ **`9`**（`0m`）／**`7`**（`3.5m`）：`G006 4→1`／`G007 3→1`／`G009 4→3`／`G010 1→0`／
`G011 2→1`（僅 `0m`）／`G014 2→1`／`G017 2→0`／`G025 2→1`／`G030 2→1`（僅 `0m`）。

### 四-2　款 `3`：**三路逐一判** ⇒ 🔴 **三路皆⛔ 單獨成立**

**器** ＝ `verify/probes/probe_WG9270R5c_p9p_three.py`｜落檔 `verify/out/WG9270R5_p9p_three.log`｜`rc = 0`。
**材料** ＝ 二態各重建之 `g_rows` 與 `mina`（器 ＝ 一次性之 `rebuild_rows.py`·**複製 `compute()` 之呼叫序**·
⛔ 另寫第二份判準），落檔 `verify/out/WG9270R5_p9p_rows_{7fa957e,cur}.json`。
🔒 **自我驗證閘（`CLAUDE.md` 之戒·⛔ 可省）**：重建所導出之 `mina_qu`／`flagged_ct`／逐戶 `ΣG_戶`／
逐戶 `宗數` 須與**同態之 `compute()` dump** 逐格相同 ⇒ **二態皆 `selftest.ok = True`·`bad = []`** ✅

**路 `(b)`　`mina[街廓]` 之二態並列（全街廓）**

| 街廓 | 舊態 | 現態 | 判 |
|---|---|---|---|
| `R1` | `116.02` | `116.02` | 同 |
| `R2` | `155.64` | `155.64` | 同 |
| `R3` | `155.19` | `155.19` | 同 |
| **`R4`** | **`115.85`** | **`115.88`** | 🔴 **升** |
| `R5` | `159.99` | `159.99` | 同 |
| `R6` | `159.28` | `159.28` | 同 |

⇒ 🔒 **路 `(b)` ⛔ 成立**——**無一街廓之 `mina` 降**；唯一相異者 `R4` 反**升**，
且翻梯二戶之宗**皆⛔ 在 `R4`**（`G010` 在 `R2`；`G017` 在 `R5`／`R6`）。

**路 `(a)`／`(c)`　逐宗四欄（`0m`·`3.5m` 同形）**

`G010`：舊態 `2` 宗 ⇒ `628-40(1)`（`R2`·`G 293.88` ≥ `155.64`·無旗標·✅）
／**`628-43(1)`（`R2`·`G 0.26` < `155.64`·旗標 `⚠️移出/第1調配順位`·🔴）** ⇒ `all_qual = False`
現態 `1` 宗 ⇒ `628-40(1)+`（`R2`·`G 285.56` ≥ `155.64`·無旗標·✅）⇒ `all_qual = True`

`G017`：舊態 `6` 宗 ⇒ 其中 **`628-22(1)`（`R6`·`G 152.21` < `159.28`·旗標·🔴）**
與 **`628-23(1)`（`R6`·`G 71.03` < `159.28`·旗標·🔴）** ⇒ `all_qual = False`
現態 `2` 宗 ⇒ `628-21(2)+`（`R5`·`G 1415.25` ≥ `159.99`·✅）／`628-23(1)+`（`R6`·`G 433.68` ≥ `159.28`·✅）
⇒ `all_qual = True`

🔴 **三宗⛔ `_qual` 之由<u>皆為二者合取</u>**：`G < mina` **⋀** 旗標 `⚠️移出/第1調配順位`
⇒ **單獨修正任一欄皆不足以使其 `qual`** ⇒ 🔒 **路 `(a)`（`G` 升）與路 `(c)`（旗標消失）
縱令發生亦⛔ 單獨致翻梯**。
🔴 **且該三宗於現態<u>⛔ 存在</u>**——其⛔ 是「`G` 升」亦⛔ 是「旗標消失」，而是**該宗本身不再存在**。

### 四-3　🔴 **實測出<u>第四路</u>**（照實·⛔ 逕取其一·⛔ 歸因）

`_qual`／`all_qual` 之逐字 ＝
`all_qual = bool(lots) and all(_qual(r) for r in lots)`
⇒ 🔒 **其受詞係 `lots` 之<u>集合</u>**——單 `§二-B` 款 `3` 所列之三路皆為「**某宗之某欄變**」，
**⛔ 涵蓋「`lots` 之組成變」**。

🔒 **實測之因 ＝ `lots` 之組成變（舊態⛔ qual 之三宗被<u>合併吸收</u>）**：
`628-43(1)` → `628-40(1)+`；`628-22(1)`／`628-23(1)` → `628-21(2)+`／`628-23(1)+`
——其繼受之合併宗 `G ≥ mina` ⋀ 無旗標 ⇒ `all_qual` 翻 `True`。
🔒 **旁證（⛔ 歸因·照實）**：二戶之新宗清單**皆含 `+`**，而 `a2c4b2b`（`W-G.9-136` 步驟 0 落地）之
commit message **逐字**載其八群，含 `R2 628-40(1)+628-43(1)→628-40(1)+`／
`R5 628-21(2)+628-22(2)+628-23(2)→628-21(2)+`／`R6 628-21(1)+628-22(1)+628-23(1)→628-23(1)+`
⇒ **與實測之三宗<u>逐宗對上</u>**。

🛑 **⇒ 單所列之三路<u>不窮盡</u>**——照實回報，⛔ 逕取其一、⛔ 補訂第四路為準據（其屬發單側）。

### 四-4　款 `4`：以受詞縮小候選（⛔ 全序列·⛔ 二分）

**框** ＝ `git log -S` 於款 `3` 所得之具體受詞（**宗之合併**）·**正面列舉**：
`k6_step0_merge` @ `app.py` ⇒ `6e67722`／`a1107a1`／`a2c4b2b`；@ `verify/stepg_pipeline.py` ⇒ **`0`**；
`k6_merge_groups` @ `app.py` ⇒ `a2c4b2b`／`f4c5385`。
**對照組**：`run_corner_pk` @ `verify/wd4_tier_list.py` ⇒ `1` ✅／一必不存在之符號 ⇒ `0` ✅ ⇒ 器非紅。

**於候選<u>及其父</u>上實跑**（依 `§二-A` 款 `1`–`3`：照實具名·略過·**出艙碼為第三種**）：

| rev | 身分 | `R2` 宗數／上界／重疊 | 出艙碼 |
|---|---|---|---|
| `2b49340` | `a2c4b2b^` | `15` ／ `3.1129` ／ `45.9766` | **`⛔ 可求值`** |
| `a2c4b2b` | 步驟 0 落地 | `14` ／ `2.8906` ／ `45.9766` | **`⛔ 可求值`** |
| `a1107a1` | 步驟 0 回歸無條件 | `14` ／ `2.8906` ／ `45.9766` | **`⛔ 可求值`** |
| `6e67722` | 段一上鎖 | `14` ／ `2.8906` ／ `45.9766` | **`⛔ 可求值`** |
| `3d5b8e0` | `6e67722^` | `14` ／ `2.8906` ／ `45.9766` | **`⛔ 可求值`** |
| `f4c5385` | `K-6 §一` 連通判定（零接線） | `15` ／ `3.1129` ／ `45.9766` | **`⛔ 可求值`** |
| `c890859` | `f4c5385^` | `15` ／ `3.1129` ／ `45.9766` | **`⛔ 可求值`** |

🔒 **其可歸屬之既有號 ＝ `GB-67`**（逐字 ＝ `②-宗 圍堵閘破[R2]：宗-宗重疊 = 45.9766`）。
🛑 **⛔ 視為紅、⛔ 視為綠、⛔ 與「判定為偽」共用出艙碼**（`§二-A` 款 `3`）。
🔒 **併記（發單側 `§二-A` 之判·本批具名一次·⛔ 另鑄號）**：
**`GB-67` 未解之前，`7fa957e..HEAD` 之中段對一切歷史實跑而言皆為<u>盲域</u>。**

### 四-5　款 `5`：收束 ⇒ 🛑 **候選全數落於洞內 ⇒ 停機**（惟**受詞已具體**）

🔒 **依款 `5` 之第二分支**：「候選全數落於洞內 ⇒ **停機**，惟此時**受詞已具體**（款 `3` 之答）
⇒ 歸屬即可由發單側判，⛔ 再須 commit。」
⇒ **本節即其履行**——`p9′` 之**受詞已具體**：**`lots` 之組成變（宗被合併吸收）**。

🛑 **歸屬 ⇒ 停機上呈**（⛔ CC 自判）：`§二-B` 之**歸屬判準**（發單側先行明定）逐字為
「若款 `3` 之因為 `(a)` **價格相依之 `G`** ⇒ 歸屬為**合法變動·錨未同步**，⛔ 為破損。
若為 `(b)` 或 `(c)` ⇒ **停機上呈**，⛔ 自判。」
⇒ 🔴 **實測之因⛔ 落於該三分支之任一**（其為**第四路**）⇒ **該判準⛔ 涵蓋之**
⇒ 🔒 **取保守項：停機上呈**，⛔ 自判其歸屬。

🔒 **款 `3` 之併判（同源否·以 sha 逐位對）**：
`p8` 之答 ＝ `4214b3a6a36b1efc3b877863289c9569cc9a4f73`；
`p9′` 之答（commit 層）＝ **未得**（候選全數落洞）。
🔒 **惟其<u>受詞</u>已可判為⛔ 同源**——`p8` 之受詞 ＝ **圖資切換**（`V6.dxf → V6_1.dxf`），
而本批 `§四-2` 已機械證明 **`mina` 之變⛔ 改 `tiers`**（唯一相異之 `R4` 且該二戶之宗⛔ 在 `R4`）；
`p9′` 之受詞 ＝ **`lots` 之組成變**。**二受詞相異** ⇒ 🔒 **⛔ 同源**（以**受詞**證之·⛔ 以 sha）。

---

## `§五`　收工閘

### 五-1　自限復驗

| 受詞 | 期 | 實 | 判 |
|---|---|---|---|
| 生產碼 `34` 檔 | 期初 ＝ 期末 | `app.py` blob `4379108a4856714078c410ff4e6291ecfdf6d2c1`；聚合 `c6320ce6def9a3c6b5a2c4281e30b84bf754c8ad44a3e603f42aa4c208c87458` | ✅ |
| `verify/wd4_tier_list.py` | ⛔ 動一字（`§二-B` 款 `1` 明令） | `git status` 之非 `??` 列 ＝ `0` | ✅ |
| `verify/fixture_block_depth_n19p.py` | ⛔ 動一字 | 同上 ＝ `0` | ✅ |
| `verify/baselines` | ⛔ 覆寫 | `298` 檔·聚合 `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` 側支 | ⛔ 併線·⛔ 刪除 | `merge-base --is-ancestor` ⇒ **未併線** | ✅ |
| `WV_BAKE` | ⛔ 設任何值 | 未設 | ✅ |
| **拋棄式 worktree** | 跑畢整份刪除（`常規六 三`） | `p9b` 已 `git worktree remove --force`；`git worktree list` 之 `p9b` 命中 ＝ `0` | ✅ |
| **commit 空間二分** | ⛔ 以之跨越洞（`§二-A` 款 `4`） | 本批**⛔ 跑二分器**；候選係由 `git log -S` 縮小而得 | ✅ |

### 五-2　四簿期末（逐簿具名其**各自**之框）

| 簿 | 框之出處 | 相異（期初 → 期末） | MAX | 缺號集 |
|---|---|---|---|---|
| **自誤** | `VR-091` 補款二 `①` ＋ 範圍框 | **`361 → 362`** | **`371 → 372`** | `[106,355,356,357]`（⛔ 動） |
| **`GB`** | `VR-093` 三框聯集（扣 `997`） | `164`（⛔ 動） | `166` | `[12,87]`（⛔ 動） |
| **`VR`** | `VR-092` 就地加註所引（扣 `999`） | `80`（⛔ 動） | `95` | `[73,75]`（⛔ 動） |
| **`K-9`** | `W-G.9-266` 工項四 `四-1` | `28`（⛔ 動） | `29` | `[]`（⛔ 動） |

🔑 **`GB-132` 之避讓驗**：K-6 檔尾之追加後，其 `K-9` 之**相異仍為 `28`** ⇒ 該追加**⛔ 鑄任何 `K-9` 號** ✅

### 五-3　本批之**自捕**（皆當場攔下·⛔ 出艙錯值）

| # | 形 | 症狀 | 處置 |
|---|---|---|---|
| `1` | **片段內宣告之命中數未實測**（**同形第三度**） | 宣告 `115.85 @ K-6 ＝ 4`，實測 **`7`**（子字串框） | 附加前以全檔計數實測；⇒ 通則：**⛔ 在片段中預寫任何命中數** |
| `2` | **單所稱之落點不存在** | 補令五 `§三` 令取 `fixture_block_depth_n19p.py:317` **所引之 `.md` 正典**，而該檔內 `.md` 命中 ＝ **`0`** | 依實測改落 `K-6`（其由 `app.py` 與 `CLAUDE.md` 二逐字錨指認·各命中 `1`）；取保守項並逐字回報（`常規二`） |
| `3` | **以 `grep -E '…\|·'` 取結果**（坑 `av` 之變體） | `·` 咬中 `compute()` 之逐宗診斷 ⇒ 輸出被淹 | 改以**落檔 ＋ 具名字樣**取；本批之 `rebuild_rows.py` 已改為**⛔ 經 stdout**（直接寫 JSON） |

🔒 **本批⛔ 再犯坑 `av`／`aw`**——`rebuild_rows.py` 與 `dump_groups.py` 皆
**⛔ 經 stdout 傳結果**（直接寫 JSON 檔）⋀ **`newline=''`**（避坑 `au`）⋀ **失敗一律 loud**。

### 五-4　🛑 **上呈項**（⛔ CC 自裁）

1. 🔴 **`p9′` 之歸屬 ⇒ 停機上呈**——實測之因係**第四路**（`lots` 之組成變），
   **⛔ 落於 `§二-B` 所明定之三分支任一** ⇒ 該歸屬判準**⛔ 涵蓋之**（`§四-5`）。
2. 🔴 **單所列之三路<u>不窮盡</u>**（`§四-3`）——`all_qual` 之受詞係 `lots` 之**集合**。
3. 🔴 **`ok_f` 係新見之紅錨**（`§三-2`）——`flagged_ct` `31` → `18`／`20`；
   **⛔ 歸因**（`§七` 款 `7`）⇒ 歸屬歸發單側。
4. 🟡 **候選新坑一則**（`§五-3` 之 `3`：以 `grep -E` 之寬框取結果·坑 `av` 之變體）——⛔ 鑄（授權僅 `av`／`aw`）。
5. 🟡 **`GB-166` ⛔ 解除**——其解除條件 `(3)` 已由本批履行，`(1)`（逐錨分列入生產碼 ＋ KL 放行）
   與 `(2)`（判別力二造）**仍未成就**。
6. **承前批而仍待者**：`p5`／`p6` 之事實；`MinA_區` 之擇一（`§三` 已裁**⛔ 呈 KL**·本批已改以登記代之）。

### 五-5　坑之**定義處清單自證** ＋ `通則 I`

🔒 **其實測見本批之<u>末一 commit</u> 之訊息**——本報告含 `D-T` 形之坑表（`§一-1`）
⇒ 其為**受詞之一員**（`GB-132` 族·`通則 I`：凡一自證之受詞包含承載該自證之文件本身者，
其「未變動」之結論**⛔ 得於該文件入倉前作成**）。

---

## `§六`　🛑 **停**

候發單側之令，⛔ 自行續辦。
