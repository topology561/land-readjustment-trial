# 【W-G.9-23 全單報告】✅ 這一半**可以動手修**——`k*` 不動、`R1` 之結構閘轉綠

**產出**：CC／2026-08-13　**分支**：`wip/s1-endpart`（本地支 `wg913-midlayer`）
**基準**：`e82c90e`　**事前登記**：`docs/reports/W-G.9-23_前置登記.md`（commit `865a1d8`）
**證據**：`verify/out/probe_WG923_bslot_only.log`（commit `0b99d66`）
**字樣錨（倉規 A）**：`【W-G.9-23 全單報告】`
　母體＝`docs/` ＋ `verify/`；命中 **2** ＝ 標題行 ＋ 本宣告行
　🔒 判別力自檢：改用寬字樣 `W-G.9-23` ⇒ 命中數**不同** ⇒ 上式非恆為 2

> 🔴 **射程（⛔ 逐頁標明）**：本批**只涵蓋 13 格（非 `forced`）**；
> **`forced` 三格仍卡表示法、⛔ 不在此數內。**
>
> ⛔ **零生產碼**：全程**只讀包裝**、**三趟皆已還原**（`R-4` 未觸發·§六-1）。
> ⛔ **本批仍不修，只估。**
>
> ## 🔧 **射程更正（`W-G.9-24`·2026-08-13·⛔ 上文原句一字未刪）**
>
> 🔴 **上句之「本批只涵蓋 13 格」<u>把受詞範圍寫成了量測涵蓋</u>。**
> `13` 係「**非 `forced` 之格數**」（＝**受詞範圍**），而**實測涵蓋者遠少於此**。
>
> **現查**（母體＝`verify/out/probe_WG923_bslot_only.log`·判準＝含「到達之街廓序」之行
> ⇒ **6 行命中**）：**實際到達之街廓 ＝ `R1`／`R2` <u>兩個</u>；
> `R3`／`R4`／`R5`／`R6` <u>從未進入</u>**（改前趟僅 `['R1']`）。
>
> 🔒 **結論⛔ 不撤，只縮射程**：**`k*` 未變**之判，其射程限於**實際到達之街廓**；
> ⛔ **不宣稱** `R3`〜`R6` 之 `k*` 亦不變——該四街廓為 **⚠️ 無從判定**（⛔ **非**判定為偽·考古 **71**）。
>
> ⚠️ **`J2`／`J4` 不受影響**：二者之受詞本即為「**改前拋出之該街廓（`R1`）**」與
> 「**到達之街廓數**」，已逐處具名，⛔ 未溢出。

---

## §一 `J1`–`J6` 逐項判 ＋ 停機

| # | 預測 | 判 | 依據 |
|---|---|---|---|
| **J1** | 非 `forced` 之 `buf` 恆 0 | ✅ | §二（**構造證**·⛔ 非只靠量測） |
| **J2** | `R1` 之結構閘**轉為通過** | ✅ | §三 |
| **J3** | `k*` **不改變** | ✅ | §四（**自檢已下放一層**） |
| **J4** | 改後跑得**比改前遠** | ✅ | §五 |
| **J5** | 丁組清單可逐項判定 | 🔶 **3／4** | §七（1 項**具名為無從判定**·⛔ 未猜） |
| **J6** | 本批零判定層變動 | ✅ | §六-1 |

🛑 **`R-1` 未觸發**（`k*` 未變 ⇒ **配地位置不動**）。
🛑 **`R-2` 未觸發**（`R1` 之結構閘**轉綠**）。
⛔ `R-3`／`R-4`／`R-5` 均未觸發。

---

## §二 `Q-1`／`J1`：**構造證**（⛔ 非經驗）

`_left_buffer_S`／`_right_buffer_S` 於 `verify/stepg_pipeline.py` 之**全部賦值點 ＝ 6**
（母體＝該檔之 AST·判準＝`Assign` 且 target 為該二名）：

| 守衛鏈 | 個數 | 右值 |
|---|---|---|
| **無**（無條件·即初值） | **2** | `0.0` |
| 在 `if _fo_left:`／`if _fo_right:` **之內** | **4** | `_corner_buffer_S(…)` ×2、`except` 退路 `0.0` ×2 |

⇒ 🔴 **非 `forced`（`_fo_* == False`）時，該變數<u>恆為初值 `0.0`</u>**——**碼面結構所定**。
🔒 **判別力自檢**：守衛內之 4 個賦值其右值含 `_corner_buffer_S`
⇒ ✅ **該判法非恆得 0**（`forced` 時非 0·實測 `R2/left@3.5m` `buf ＝ 8.6959`）。

⇒ **`R-3` 未觸發**：本批之拆分前提**成立**（族① 於這 13 格**無涉**）。

---

## §三 `Q-2`／`J2`：`R1` 之結構閘**轉為通過**

單位：**百分點**（容差 **0.1**）。

| 情境 | 改前（`R1 left`） | 改後 |
|---|---|---|
| `0m` | 理論@k\* ＝ `97.18`　實跑(階段1) ＝ `100.02`　**Δ ＝ `−2.84`** ⇒ 🔴 不過 | ✅ **不再拋於 `R1`** |
| `3.5m` | 理論@k\* ＝ `96.30`　實跑(階段1) ＝ `98.75`　**Δ ＝ `−2.45`** ⇒ 🔴 不過 | ✅ **不再拋於 `R1`** |

🔒 **接縫之單變因性（由構造證·事前登記 §1）**：
**理論@k\*** ← `_slot_res['table']`（＝ `_select_pool_slot` 之輸出）⇒ **本包裝只動理論側**；
**實跑(階段1)** ← 推進迴圈（種子 `_W_prev = 0.0`）⇒ ⛔ **未動**。

⚠️ **受詞**：本判之受詞為「**改前拋出之該街廓（`R1`）**」，
⛔ **不宣稱**「全部街廓之結構閘皆過」——改後停於 `R2` 之**別的閘**（§五）。

---

## §四 `Q-3`／`J3`：`k*` **不改變**（🔴 自檢已下放一層）

| 情境 | 街廓 | `forced`(L/R) | `buf_L`（生產） | `b_L` 改前 | `b_L` 改後 | `k*` 改前 | `k*` 改後 | 判 |
|---|---|---|---|---|---|---|---|---|
| `0m` | `R1` | False/False | `0.0000` | `0.1619` | `0.0000` | `2` | `2` | ✅ 未變 |
| `0m` | `R2` | False/False | `0.0000` | `−1.6939` | `0.0000` | `8` | `8` | ✅ 未變 |
| `3.5m` | `R1` | False/False | `0.0000` | `0.1619` | `0.0000` | `1` | `1` | ✅ 未變 |
| `3.5m` | `R2` | **True**/False | `8.6959` | `6.9803` | `6.9803`（⛔ **未改**） | `8` | `8` | ✅ 未變 |

🔒 **同一次呼叫同時算改前與改後**（⛔ 非兩次跑之比對）。

### 四-1 🔴 判別力自檢**下放一層**（⛔ 本節為本批之要點）

| # | 問 | 結果 |
|---|---|---|
| ① | **替換是否<u>抵達計算</u>** | 掃 `b ∈ {0,2,…,16}` ⇒ **`ΣRw_L@k*` 得 9 個相異值**（`5.6`／`12.2`／`20.0`／`28.9`／`38.9`／`50.0`…）⇒ ✅ **確實抵達** |
| ② | **`k*` 是否隨之改變** | `k*` 全程**單一值** ⇒ ✅ **`k*` 對 `b` 不敏感<u>係真結論</u>** |

⚠️ **為何非如此不可**：只看 `k*` 時，「**探針沒接上**」與「**`k*` 真的不敏感**」
**⛔ 給出同一個輸出**（考古 **71** 之形狀，發生在**自檢自身**上）。
🩸 **首版之對照值 `b := 1e6` 更糟**——`rw_from_width` **W≥18 飽和 100**
⇒ `R(b+Σw) − R(b) = 0` 對每個 `k` 皆同 ⇒ **構造上不可能改變 `k*`**
⇒ 該自檢**恆判未變**、⛔ **無鑑別力**。⇒ 已立**考古節 78**。

⇒ 🛑 **`R-1` 未觸發：配地位置<u>不動</u>。**

---

## §五 `J4`：跑到哪裡、在哪裡停（⛔ 逐街廓正面記錄）

| 情境 | 趟 | 到達之街廓序 | 停於 |
|---|---|---|---|
| `0m` | **改前** | `['R1']` | 街廓 `R1` `left` 側（**結構閘**） |
| `0m` | **改後** | `['R1','R2']` | `R2`「**②-宗 圍堵閘破**：宗-宗重疊 `45.9766` > 上界 `3.1129`」 |
| `0m` | **只改 `R1`** | `['R1','R2']` | **同上·數字完全相同** |
| `3.5m` | **改前** | `['R1']` | 街廓 `R1` `left` 側（**結構閘**） |
| `3.5m` | **改後** | `['R1','R2']` | `R2`「**結構閘 telescoping 破** Δ＝`55.606`」 |
| `3.5m` | **只改 `R1`** | `['R1','R2']` | **同上·數字完全相同** |

🔒 **第三趟（只改 `R1`）之作用 ＝ 分辨「新曝」與「新破」**：
兩趟停於**完全相同之落點與數字** ⇒ 🔴 **`R2` 之二失敗<u>非本替換所致</u>**
——它們**本來就在那裡，只是從來沒跑到**。
⚠️ ⛔ **不得記為「修了之後壞了兩個地方」**。

⚠️ **`3.5m` `R2` 之 telescoping 破，其 `W₀＝6.98` 屬 `forced` 左側**
——而本批**⛔ 未改 `forced` 側** ⇒ 與上判一致。

---

## §六 丙組：連鎖傳遞之範圍（⛔ 只述）

🔒 **由構造**：`_select_pool_slot` 之入參（`widths`／`left_side`／`right_side`）
**逐街廓建置**（於 `run_step_g` 之 `for blk_label, parcels_in_blk in parcels_by_block.items():` 內）
⇒ **`k*` 之改變止於該街廓側，⛔ 不跨街廓**。

⚠️ **但**：推進種子以 `W_far` **thread 到下一宗**（`W-G.9-22` 丙組已查）
⇒ **同一街廓側之整排宗**皆受影響。⛔ **跨街廓之量本批未估**（施工單：只述）。

### 六-1 `J6`／`R-4`：只讀包裝之還原（**獨立指令驗**）

`git diff --numstat 865a1d8 -- app.py verify/stepg_pipeline.py verify/run_verification.py
verify/wv_reconcile.py verify/baselines/ docs/rulings/ CLAUDE.md verify/fixture_e2e_termination.py
verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt` ⇒ **空輸出**。
🔒 判別力自檢：同式對 `verify/probes/` ⇒ **非空**（`411 0`）⇒ 上式**非恆空**。
🔒 探針內另設 `restored = (ns["_select_pool_slot"] is _orig)` ⇒ **三趟皆為真**。

---

## §七 丁組：預期轉紅清單（🔴 **⛔ 不實施**·⛔ 不採信施工單之列舉）

| 機制（字樣錨） | 會不會轉紅 | 預期／非預期 | 屆時之正辦 |
|---|---|---|---|
| `verify/fixture_e2e_termination.py`（**三支端到端複本之終止點凍存**） | ✅ **會** | 🟢 **預期內** | 其受詞即「**在哪裡停**」，而本修法之目的**正是讓它停得更遠** ⇒ 轉紅為**成功之訊號**。**逐項確認新終止點與預期相符後**才更新，⛔ **不得逕自重凍** |
| `verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt`（對帳器 `verify/wv_reconcile.py`） | ✅ **會** | 🟢 **預期內** | 對帳準則為「**名目 ＋ 正規化後原因全文**」；本修法改變 `v3·StepG*` 之**原因全文** ⇒ **類 III（訊質不同）＝紅**。**逐項具名確認**後更新，⛔ **不得逕自重凍** |
| `run_verification.py` 之 `W-G G.1 接線層 ctx-builder 同源`（`_ctx["0m"]`） | 🔶 **可能由紅轉綠** | 🟢 **預期內** | 該閘**現為死閘**（`_ctx["0m"]` 缺鍵先崩·`W-G.9-4b` 已具名），其缺鍵之根因即 `run_step_g` 之 raise ⇒ 修法後**它可能首度真正執行** ⇒ ⛔ **不得把它的首次紅誤認為本修法之破壞** |
| `verify/baselines/`（**重烤基準**·19 檔） | ⚠️ **無從判定** | — | 🔴 **本批⛔ 未讀其逐檔內容** ⇒ ⛔ **不猜**（考古 **71**：「無從判定」≠「判定為偽」） |

🔒 **射程聲明**：本清單之母體 ＝ 上列 **4 項**（⛔ 正面列舉）；
⛔ **不宣稱**「全倉只有這些會轉紅」——⛔ 本批未做全倉盤點。

---

## §八 ⛔ 本批未做之事（⛔ 逐項）

1. ⛔ **零生產碼**、⛔ **未實施任何修法**——本批**只估**。
2. ⛔ **未改 `forced` 三格之任何一處**、⛔ 未改 **A**、⛔ 未改**族①**、⛔ 未解表示法。
3. ⛔ 未改任何閘或容差、⛔ **未重凍任何凍存物**、⛔ 未實施丁組任何一項。
4. ⛔ **未估** `Q-4`（配地面積差）——**因 `k*` 未變而不需要**；⚠️ ⛔ **非**「估不出來」。
5. ⛔ **未讀** `verify/baselines/` 之逐檔內容 ⇒ 丁組該項**具名為無從判定**。
6. ⛔ 未做全倉之同類分歧盤點、⛔ 未追那 57 條。
7. ⛔ **未跑長跑**——§六-1 之 `numstat` 空輸出 ⇒ 判定層在構造上不可能變動。

⚠️ **依倉規（不可變文件不記可變值），本報告 ⛔ 不記任何可變檔之雜湊。**
⚠️ **依倉規（行號衛生第 4 款），本報告之倉內引用一律用<u>名目字面與檔名</u>，⛔ 不以行號為錨。**
⚠️ 本報告凡涉權威層級處已就地標明（`docs/rulings/` ＝ 權威序**第 1 級**）。

---

## §白話稿 呈 KL（依 `重劃白話` 之呈送段）

> 🔴 **這一批只看 13 個「街角有配到地」的情形。**
> **另外 3 個「街角被強制列為抵費地」的，還卡在斜線怎麼表達，⛔ 不在下面任何數字裡。**
>
> ## 🔧 **更正（`W-G.9-24`·2026-08-13·⛔ 上面兩句一字未刪）**
>
> 🔴 **上句寫得太寬了。** 程式**實際只跑到 2 個街廓**（`R1`、`R2`）就停住
> ——**其他 4 個（`R3`、`R4`、`R5`、`R6`）根本沒跑到**。
>
> ⇒ 下面「**配到的地不會挪位置**」這個結論，**只對那 2 個街廓成立**。
> 其他 4 個**我不知道**——⛔ **不是「也不會挪」，是「還沒量到」**。
>
> ⚠️ 另外兩個結論（**卡住的檢查會過**、**程式跑得比較遠**）**不受影響**，
> 因為它們講的本來就是那 2 個街廓的事。

### 【現況】

**上一批查出：同一個「負擔從哪裡起算」，程式裡有三個答案。**
**這一批問的是：如果<u>只改其中一個</u>（②選池子位置那個），會怎樣。**

答案是三件好消息：

```
   ┌────────────────────────────────┬──────────────────────────────┐
   │ ① 配到的地會不會挪位置？        │ ✅ 不會（k* 完全沒變）        │
   ├────────────────────────────────┼──────────────────────────────┤
   │ ② 那個卡住程式的檢查會不會過？  │ ✅ 會（R1 兩種情境都轉綠）    │
   ├────────────────────────────────┼──────────────────────────────┤
   │ ③ 程式會不會跑得比較遠？        │ ✅ 會（從第 1 個街廓 → 第 2 個）│
   └────────────────────────────────┴──────────────────────────────┘
   ⚠️「k*」是程式決定「調配池要擺在第幾個位置」的編號，⛔ 不是面積、不是長度。
```

**這是十幾批以來，第一次出現「可以動手修、而且不會動到配地位置」的東西。**

⚠️ **但程式跑到第 2 個街廓（R2）又停了**，兩種情境各停在**不同的檢查**上。

🔒 **我另外做了一趟對照**：**只改第 1 個街廓、其他都不動** ⇒ 結果**停在完全一樣的地方、數字一模一樣**。
⇒ **那兩個問題本來就在那裡，只是以前從來沒跑到。**
⛔ **不是「修了之後又壞兩個地方」。**

### 【對土地的影響】

1. **配地位置不會動**——這是最要緊的一點，已逐格確認（`k*` 改前改後相同）。
2. **負擔數字會變**——那正是修法的目的（把起算點改成規定書寫的那個）。
   ⛔ **變多少，這一批沒算**（要跑得完才知道）。
3. **3 個強制抵費地的街廓完全不在這批裡**——它們還卡著。

### 【要你判斷】

> **這一個（②選池子位置）要不要先修？**

**修它不會挪動任何一筆地的位置**，而且會讓程式往前多走一段。
⛔ **本批不修**——依既定順序，要您點頭才動。

**標記：一般作業判斷**（本題不涉法規裁量；⚠️ 但「三個答案統一成哪一個」那題**仍是地政判斷**，且**尚未裁**）。

### 【裁完之後會接著冒出來的問題】

1. 🔴 **修法落地那天，本波建的幾個守護機制會同時轉紅**——**這是預期內的**，
   因為它們凍存的正是「現在停在哪裡」。⛔ **屆時不得逕自重凍了事**，
   要**逐項確認新的結果對不對**才更新。**那正是它們存在的理由。**
2. 🔴 **R2 那兩個問題會浮上來變成下一個關卡**（一個是宗地重疊、一個是負擔累加對不上）。
3. ⚠️ 有一個守護機制（重烤基準 19 檔）**我這批沒讀**，
   ⇒ 它會不會轉紅，我**照實標成「不知道」**，⛔ 沒有猜。
