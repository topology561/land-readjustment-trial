# `W-G.9-296R`　執行報告：`W_0` 重建式之**對生產態驗證**（窄批）

> **單** ＝ `docs/orders/W-G.9-296_施工單_W0重建式之對生產態驗證.md`（工項零已原封入倉）
> **分級** ＝ **窄**（一項量測 ＋ 一則末端追加·**零生產碼**）
> **開工態** ＝ `f45f946bd2623fdf6bf9b9e136c6f54f1e531928`（`ls-remote` 自查·全 `40` 碼）
> 🛑 **本批⛔ 鑄任何號**；`ALPHA_LABELS` **⛔ 動一字**。
> 🔑 **設計驗算之出艙一律冠** `【設計驗算·⛔ 非實測】`。
> 🔒 **KL 之放行仍在·仍⛔ 動用**；生產碼 `34` 檔一字未動。

---

## `§零`　開工閘（九項·**逐項自跑**·⛔ 採單之數）

| # | 產生指令（逐字） | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `f45f946bd2623fdf6bf9b9e136c6f54f1e531928`（＝ `git rev-parse HEAD`） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD`（`rc` **緊接該指令**·⛔ 經管線） | `rc = 1` ⇒ **非祖先** | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`·🔴 紅 `0` 項·`SELF_SHA256` **✅ 相符**（受詞 `14647` B·實算 `51b14c76622b46c5…` ＝ 單載 `51b14c76622b46c5520e6c262efff9194037b1f1794e59f244ad67b6840d4fa9`） | ✅ |
| `4'` | 受詞自證（坑 `bf`）＝ 器所印之檔名列 | 含 `W-G.9-296` ＝ `True`／含 `W-G.9-295` ＝ `False` | ✅ |
| `5` | 同 `4` ＋ `--selftest` | `rc = 0`·**四靶逐項成立**；逐字「判別力造：✅ 全數成立 ⇒ 本器非恆綠亦非恆紅」 | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`；逐字「必命中字樣 ⇒ **1**（須 > 0）／必不命中之人造字樣 ⇒ **0**（須 0）⇒ ✅」 | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `389`／`399`（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` `28`／`29`（缺 `[]`） | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87` 標籤／`87` 可解析**（數字系 `26` ＋ 字母系 `61`）·末標籤 `bi` ⇒ **下一標籤 `bj`**；母體 ＝ `HEAD` 之追蹤 `.md` **`790`** 檔；器自證 造甲 `6 ≥ 1`／造乙 `0`／`0` ⇒ **器非紅**。🔒 依新附款 `(a)` **全量落檔後清點**（⛔ `\| tail -N`）、**等待綁行程結束**（task 之 `rc` 行） | ✅ |
| `7` | `git ls-files` ⋂ `^verify/[^/]+\.py$` ＋ `app.py`（**正面列舉**） | `33 + 1 = ` **`34`** 檔；判別力[必不命中] 子層 `^verify/.+/.+\.py$` ＝ **`310`**（須 `> 0`） | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`（`core.quotePath` **未設 ⇒ 預設**） | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·`git ls-files verify/baselines \| wc -l` ＝ `298` | ✅ |

🔒 **`argv` 位置之自證**（閘 `5` 所令）：碼面 `verify/probes/probe_order_preflight.py:247` 逐字
`if len(sys.argv) > 1 and sys.argv[1] == "--selftest":` ⇒ 置於 `argv[2]` 時**⛔ 觸 selftest**、改走檔檢（其所印之檔名列即證）。
⚠️ **閘 `7` 之子層數 `310`（前批 `309`·`+1`）** ＝ 前批新增之 `probe_WG9295_joint.py`；其判準係「須 `> 0`」
⇒ **⛔ 受影響**（`自誤 386` 已明定其逐批漂移·⛔ 定值）。

---

## `§一`　工項零：本單原封入倉

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/orders/W-G.9-296_施工單_W0重建式之對生產態驗證.md` |
| 所指之檔數 ＝ `1`（框 ＝ 磁碟 `os.path.isfile`·母體 ＝ 工作區） | 受詢 `True`／造甲[必≥1] `W-G.9-295` 之單 `True`／造乙[必為0] 人造名（**執行期組出**·字面⛔ 出艙）`False` |
| `CR`（框 ＝ `bytes.count(b'\r')`·**⛔ text 解碼**·新附款 `(b)`） | **`0`**；判別力[必非零] ＝ 倉內 blob `data/V6.dxf` 之 `CR` ＝ **`12308`**（全長 `88341` B） |
| **三方逐位** | 源（主 checkout）`14725` B `sha256` `8ad7708e1689281b78be17db4913110fcf02102d7bb5cc07dc467cb868fe1804` ＝ 磁碟（本 worktree）同值 ＝ **blob 內容** `sha256` 同值；blob `sha1` ＝ `4cfc0e058bbfa0c1bd3b90d67a5bcaa0dbd4ef43` |
| `numstat` | `172`／`0`（**`deletions` 欄 `0`**） |

---

## `§二`　工項一（**主檢·窄**）：`W_0` 重建式之**對生產態驗證**

**器** ＝ `verify/probes/probe_WG9296_w0rec.py`（新建）·**落檔** ＝ `verify/out/WG9296R_w0rec.log`（`11504` B）·`rc = 0`。
🔒 **重建式 ＝ 同一份**（⛔ 另寫第二份·單 `§六`）：`probe_WG9295_joint._w0`，由本器 `import` 而來；
其為 `verify/stepg_pipeline.py:1023`-`1036` `_mp_base_W0` 之**純委派式**複刻。

### 二-1　生產態 `W_0` 之**二通道**與逐閘執行次數

| 通道 | 取法 | 是否倚賴 `ns` 之包裹 |
|---|---|---|
| **②（主）** | `sys.settrace` 攔 **`_mp_base_W0`** 之 `return`（`co_name` 比對），取其 `f_locals`（`_gs`／`_buf`／`_dv`／`_mp`／`_adir`）與回傳值；自 `f_back`（＝ `_run_step_g_impl`）取 **`blk_label`** 定街廓，並以 **`_buf` 逐位相等**為**第二鍵** | **⛔ 倚賴** |
| **①（佐）** | `probe_WG9292_pathC.drive()` 所置之 `k956_W_from_mp` 包裹之**生產期前綴** | 倚賴 |

🔒 **併查**：`verify/stepg_pipeline.py:394` 之 `_k956_W_from_mp = ns["k956_W_from_mp"]` 位於
`_run_step_g_impl`（`277`-`1643`·AST 實查）⇒ 係**呼叫期**綁定 ⇒ 包裹確能攔到（通道② 已覆核之）。

| `SB` | `_mp_base_W0` 生產呼叫（通道②） | `k956` 生產錄（通道①） | `_build_corner_range_v3` | （段內） | `_corner_buffer_S` | （段內） | `run_step_g` 終局 | `forced` 格 |
|---|---|---|---|---|---|---|---|---|
| `0.0` | **`0`** | **`0`** | `24` | `8` | `1` | `1` | （未拋） | `1`（`R4/left`） |
| `3.5` | **`1`** | **`1`** | `24` | `8` | `2` | `2` | （未拋） | `2`（`R2/left`、`R4/left`） |

`SB = 3.5` 之通道② 錄 `0` 逐字：`blk` ＝ `'R2'`／側 ＝ `'left'`／呼叫者 ＝ `'_run_step_g_impl'`／
`_buf` ＝ `8.696079893776137`／**回傳 ＝ `6.98048044737404`**。

### 二-2　款 `1`〜`5` 之逐格全量（**同一次執行**·`repr` 全位）

| `SB` | 格 | 款 `5` 通道②（鍵甲／鍵乙·一致） | 款 `5` 通道①（生產錄／自身追加） | 款 `1` `W_0^prod` | 款 `2` `W_0^rec` | **款 `3` 逐位差** |
|---|---|---|---|---|---|---|
| `0.0` | `R4/left` | `0`（`0`／`0`·`True`） | `0`／`0` | 🟡 **不可得（loud）** | `3.7076106246376597` | 🟡 **不可得** |
| `3.5` | `R2/left` | **`1`**（`1`／`1`·`True`） | `1`／`0` | **`6.98048044737404`** | `6.98048044737404` | **`0.0`**（ULP 距離 `0`） |
| `3.5` | `R4/left` | `0`（`0`／`0`·`True`） | `1`（係 `R2` 之錄）／`2` | 🟡 **不可得（loud）** | `7.023016189779138` | 🟡 **不可得** |

🔒 **`R2/left@3.5` 之交叉驗證**
- 通道① 之對拍：命中 `1` 筆·最小差 ＝ `0.0` ⇒ **二通道逐位相同**。
- **實參之逐位對拍（重建 vs 生產）**：`buf` `True`／`_gs`（＝ `front_p1`）`True`／`_dv`（＝ `d_hat`）`True`。
  其值：`_gs` ＝ `(310361.51526, 2651912.18005)`／`_buf` ＝ `8.696079893776137`／`_dv` ＝ `(0.6756855318673246, 0.7371899768887057)`。
- 生產態之式（逐字）＝ `_bp0 = _gs + _buf·_du`，`W_0 = k956_W_from_mp(_bp0, _mp, _adir, _dv)`。

🛑 **「不可得」之性質（⛔ 讀為「未量到」）**：`_mp_base_W0` 之**定義與呼叫皆在 `else` 分支內**
（`not _degenerate_order and _N > 1` 方進）⇒ 該二格於生產路徑**⛔ 被呼叫** ⇒ **生產態之 `W_0` 結構上不存在**。
依「**無從回測 ⛔ 等同 回測不過**」，該二格之款 `3` **⛔ 判其為 `0`、亦⛔ 判其為非 `0`**。

### 二-3　款 `4`（**【設計驗算·⛔ 非實測】**·⛔ 與實測同表混列）

| `SB` | 格 | 現行 `buf` | `buf′` | `W_0′` | `S1_perp` | `\|W_0′ − S1_perp\|` | 跨距（`L_su`／`L_edge`·取較大） | `A′` 界 | 比值 | 判 |
|---|---|---|---|---|---|---|---|---|---|---|
| `0.0` | `R4/left` | `4.49975224003872` | `3.7114402919876444` | `3.6955152872177086` | `3.6954999940774775` | `1.5293140231076308e-05` | `33.26627503242344`／`33.26627503226274` ⇒ `33.26627503242344` | `0.00015030237064795143` | `0.10174916180728083` | ✅ |
| `3.5` | `R2/left` | `8.696079893776137` | `7.000183533577529` | `7.000102486246391` | `7.0` | `0.00010248624639075388` | `44.93777987966314`／`44.88675333792245` ⇒ `44.93777987966314` | `0.00011126495375137079` | `0.9211008762001237` | ✅ |
| `3.5` | `R4/left` | `7.8185635189090075` | `7.030272552440337` | `7.000107141448771` | `7.0` | `0.00010714144877077558` | `33.572497547604144`／`33.265390173978915` ⇒ `33.572497547604144` | `0.00014893142796154046` | `0.7194012052207236` | ✅ |

### 二-4　款 `6`　判別力三造之判

| 造 | 判準 | 實測 | 判 |
|---|---|---|---|
| （母體） | `forced` 格數 `≥ 1` | **`3`** | ✅ |
| **[必為零]·自證可滿足** | 同一 `buf` 餵二次之差 ＝ `0.0` | `0.0`／`0.0`／`0.0` | ✅ |
| **[必為零]·受詞** | 款 `3` ＝ `0.0`（`None` ＝ 不可得·**⛔ 計入判定組**） | 不可得／`0.0`／不可得 | ✅（**判定組 `1`／`3` 格**） |
| **[必相異]** | 以 `buf′` 餵之所得**異於**款 `2`（證重建式非恆值） | `3.6955152872177086` vs `3.7076106246376597`／`7.000102486246391` vs `6.98048044737404`／`7.000107141448771` vs `7.023016189779138` | ✅ |
| **[必非零]** | `\|buf′ − buf\| > 0`（**帶號／絕對值並列**） | 帶號 `-0.7883119480510761`／`-1.6958963601986081`／`-0.7882909664686704`；絕對值 `0.7883119480510761`／`1.6958963601986081`／`0.7882909664686704` | ✅ |

### 二-5　🩸 入倉前自捕**三則**（皆**量測器紅**·⛔ 頂替）

| # | 形 | 徵候 | 根因 | 攔法 | 後果 |
|---|---|---|---|---|---|
| `1` | **v1 三格之款 `1` 皆報「生產路徑⛔ 呼叫 `_mp_base_W0`」** | 同一輸出之表頭**明載通道② 錄到 `1` 次**（`_buf` ＝ `8.696079893776137`·回傳 `6.98048044737404`）⇒ **二數互斥** | `_run_step_g_impl` 之街廓變數名為 **`blk_label`**（⛔ `blk`）；以 `blk` 取之**恆得 `None`** ⇒ 逐格比對必失配 | 取 `blk_label`，並增**第二鍵**（`_buf` 逐位相等）**二鍵並報**、不一致即 loud | 🔴 **把「量測器比對失配」誤記成「生產路徑未呼叫」**；修後 `R2/left` 之款 `1` 可得、款 `3` ＝ `0.0` |
| `2` | v1 之 **[必為零]·受詞** 於三格皆「不可得」時判成 **✅** | 判定組為**空**而結論為綠 | `all(x == 0.0 for x in k3s if x is not None)` 於**空集恆真** | 判定組為空 ⇒ **loud「不可判」**（`W-G.9-226` 通則 `二`：⛔ 靜默綠），並出艙判定組**基數** | 修後出艙「判定組 `1`／`3` 格」 |
| `3` | **[必非零]** 只印帶號值（皆負）而判 ✅ | 顯示與判準不同口徑，讀者無從分辨 | — | **帶號與絕對值並列**（`CLAUDE.md`：二者所偵之錯不相交） | — |

🔒 **物證保全**：v1 落檔另存 `verify/out/WG9296R_w0rec_v1_blknone.log`（`10791` B·**⛔ 覆寫**）；
體例承本單 `§零′` 逐字所立（「其**保全污染版落檔為物證**之處置，立為體例」）。

### 二-6　🔴 二項**照實上呈**（⛔ 判孰誤·⛔ 頂替·**候發單側**）

**（一）單 `§三-1` 稱該對拍「於三格皆可施」——實測僅 `1`／`3` 格可施。**
成因係**結構性**：`_mp_base_W0` 之定義與呼叫皆在 `else` 分支內 ⇒ 另二格**⛔ 被呼叫**
（**二通道一致**：通道② `0` 次 ⋀ 通道① 該格無錄）⇒ **⛔ 讀為「未量到」**。

**（二）單 `§三-1` 所載之生產態 `W_0` 二值與本批實測之唯一生產值⛔ 相符。**
單載 `4.4804447557822895`／`7.785015718090257`；本批實測（`3.5 R2/left`）＝ `6.98048044737404`。
倉內現查（框 ＝ `git -c core.quotePath=false grep -F`·母體 ＝ `HEAD` 之 `docs/**` ＋ `verify/**`·**檔數／列數並報**）：

| 值 | 檔數 | 列數 | 倉內之同列脈絡（節略·逐字見該檔） |
|---|---|---|---|
| `4.4804447557822895` | `7` | `12` | `W-G.9-282R` 之表：`\| 0m \| 態甲 R4/left \| **4.49975224003872** \| … \| A2 \| 4.4804…` |
| `7.785015718090257` | `7` | `12` | 同表：`\| 3.5m \| 態甲 R4/left \| **7.8185635189090075** \| … \| A2 \| 7.78…` |
| `6.98048044737404` | `16` | `28` | `docs/orders/交接文_W-G.9-262.md` 逐字：「`3.5m`·`R4 left` 之 `_b_L0` 與 `R2` **逐位相同**（`6.98048044737404`）而其 `buf` 相異」 |
| **對照組[必為零]** ＝ 一人造數 | `0` | `0` | ⇒ 該量測器**非恆非零** |

🛑 **並列出艙而⛔ 判**：倉內既載「`3.5m R4 left` 之 `_b_L0` ＝ `6.98048044737404`」，
而本批實測「`3.5m R4/left` 之 `_mp_base_W0` 呼叫 **`0`** 次」。
**二者孰是、其成因為何，⛔ 由 CC 判**（單 `§六`：⛔ 判孰誤）⇒ **候發單側**。

### 二-7　`range_area`／`corner_range_polys` 於本批之角色與**當場現查**

🛑 二者為本批**主檢之輸入**（⛔ 以「未觸」充綠·**⛔ 轉引前批之數**——下列皆本批當場所量）。

| 受詞 | 於本批之角色 | 當場現查（框／母體／粒度） |
|---|---|---|
| **`range_area`** | 款 `4` 之 `buf′` 係以 `ns["_corner_buffer_S"](…, ra, side, tol=tol, …)` **解得**，其中 `ra = float(br["range_area"])` ＝ 當場自 `_corner_buffer_S` 之包裹所錄之**實參** | 其**二產生處**（框 ＝ `grep -F` 固定字串·母體 ＝ `app.py` 單檔·**列框**）：`cand_p1['_corner_range_area'] = (round(float(_corner_poly_p1_B4.area), 2)` ＝ **`1`**；`cand_p2['…'] = (round(float(_corner_poly_p2_B4.area), 2)` ＝ **`1`**。判別力：寬字樣 `range_area` ＠`app.py` ＝ **`31`** 列（`> 1` ⇒ 上二錨**非恆為 `1`**）；對照組[必為零] ＝ 一人造字樣 ＝ **`0`** |
| **`corner_range_polys`** | 款 `4` 之**跨距**（`L_su`／`L_edge`）係量 `cp = crp.get((blk, side))` ＝ `run_step_g` 回傳之 `corner_range_polys[(街廓, 側)]` | 碼面命中（**正面列舉二檔**·`grep -F`·列框）：`app.py` ＝ **`7`**／`verify/stepg_pipeline.py` ＝ **`8`** |

🩸 **照實併記**：本批之落檔 `verify/out/WG9296R_w0rec.log` 內，`range_area` 與 `corner_range_polys`
之字樣命中**皆 `0`**（該器只印其**導出量**而未印其名）——上表係**本節為補其出艙而當場另量**，
**⛔ 由該落檔取得**。其逐格**數值**之全量出艙見前批 `docs/reports/W-G.9-295R_三量之同次併量與導出界之判_執行報告.md`
之款 `1`（【倉】·⛔ 本批之實測）。

### 二-8　一句話推導

> 在**唯一**存在生產態 `W_0` 之格（`3.5 R2/left`），本器之重建式吃**同一組實參**（`front_p1`／`buf`／`d_hat` 逐位皆同）
> 而得**逐位相同**之值（ULP 距離 `0`）⇒ **重建式可信**；其餘二格之生產態 `W_0` 因 `_mp_base_W0` **⛔ 被呼叫**
> 而**結構上不存在**，故其 `W_0′` 之可信度**仍未由本項建立**。

🛑 **出艙即止**：⛔ 判路丙成立與否、⛔ 判餘量之成因、⛔ 擬實作、⛔ 提修法主張。
🛑 **⛔ 以本項之通過充「路丙成立」**（單 `§三-1` 逐字）。

---

## `§三`　工項二：一則末端追加（`docs/reports/W-G.4_泛用阻塞項登記表.md` 檔末）

| 受詞 | 實測 |
|---|---|
| payload 之產生（`常規七`·**由檔案管線直接產生**·⛔ 憑記憶重寫） | 本單第 `104`–`117` 列 ＝ `14` 列／`1532` B／`CR` `0`；**全列皆以 `> ` 起首** ＝ `True` |
| 落地形之機驗（`自誤 393`） | payload 內 `^###` 起首之列 ＝ **`0`**（單自載⛔ 含 ⇒ **⛔ 剝層·原樣保留**）；對照組[必非零] ＝ 本單全檔 `^##` 起首列 ＝ **`11`** |
| 期初／期末 bytes | `898838` → `900377`（**`+1539`**·`numstat` ＝ `17`／`0` ⇒ **`deletions` 欄 `0`**） |
| 嚴格前綴檢**二造** | `new.startswith(old)` ＝ `True`[必真]／`old.startswith(new)` ＝ `False`[必偽] |
| payload 逐位在末端 | `new.endswith(payload + b'\n')` ＝ `True` |
| **字樣錨之唯一性（`uniq`·坑 `bi`）** | 錨 ＝ `> **\`GB-170\` 之修：殘差之成因指名` ⇒ 命中 **`1`**（列框·母體 ＝ 該檔單檔）；判別力 ＝ 寬字樣 `GB-170` 命中 **`19`**（`> 1` ⇒ **非恆為 `1`**）；對照組[必為零] ＝ 人造錨 **`0`** |
| `append-only` 機檢 | `python verify/tools/wg942_append_audit.py` ⇒ `rc = 0`（先設 `PYTHONIOENCODING=utf-8`·`常規五`）·受檢 **`77`** 筆·逐字「結論：✅ 全部成立」 |

🔒 **命名之提案（`常規三`·無土地後果·候發單側核可）**：v1 物證取 `WG9296R_w0rec_v1_blknone.log`；
候選二 `WG9296R_w0rec_v1_mismatch.log`／候選三 `WG9296R_w0rec_pre.log`。
**理由** ＝ 承前批之 `WG9295R_joint_v1_selfpolluted.log` 之形，且使**其缺陷為何**於檔名即可讀。

---

## `§四`　`⛔ 為之事` 之逐項自檢（單 `§六`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔一字 | ✅ 逐 `commit` 之生產碼判法皆 **`0` 行**（見 `§五`）；對照組[必非零] `17fd981^..17fd981` ＝ `2` |
| `2` | ⛔ 開任何分支 | ✅ 未開；`verify/W-G.9-289-gb170` 命中 `0` |
| `3` | ⛔ 動用 KL 之放行 | ✅ 未動用 |
| `4` | ⛔ 擬任何實作形 | ✅ 未擬 |
| `5` | ⛔ 調任何界、⛔ 事後放寬任何門檻 | ✅ `A′` 逐格由單所令之算式當場導出，未調 |
| `6` | ⛔ 自寫第二套幾何或**第二份重建式** | ✅ 重建式 ＝ `probe_WG9295_joint._w0`（`import` 而來·本器內**⛔ 另定義** `_w0`） |
| `7` | ⛔ 入倉任何設計驗算之數為生產態之數 | ✅ 款 `4` 皆冠 `【設計驗算·⛔ 非實測】`，與款 `1`／`2`／`3` 之實測**分表** |
| `8` | ⛔ 改 `_corner_buffer_S`／`_build_corner_range_v3`／`_first_corner_alloc_dir`／任何函式簽章 | ✅ 未改（`app.py` blob 期初＝期末） |
| `9` | ⛔ 動 `ALPHA_LABELS` 一字 | ✅ 未動（`git diff --numstat` 對 `probe_WG9269_pit_index.py` 輸出空） |
| `10` | ⛔ 覆寫 `verify/baselines` 任一檔、⛔ `WV_BAKE`、⛔ 併線 `p3a`、⛔ 引任何自 `baselines` 取得之數 | ✅ `baselines` `298` 檔·框 `sha256` 未變；`WV_BAKE` 未設；`p3a` 仍非祖先；本報告之數無一取自 `baselines` |
| `11` | **⛔ 覆寫 `WG9295R_joint_v1_selfpolluted.log`（物證）** | ✅ 未覆寫（其 blob 期初＝期末·見 `§五` 閘 `6`） |
| `12` | ⛔ 鑄任何號 | ✅ 四簿期末與期初逐位相同（見 `§五` 閘 `4`） |
| `13` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`） | ✅ 未解除、未收窄 |
| `14` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ 由生產碼 blob 相異 `0` 機械蘊含 |
| `15` | ⛔ 追改任何已入倉文件一字 | ✅ `append-only` 機檢 `77` 筆全成立 |
| `16` | ⛔ 判路丙成立與否、⛔ 判餘量之成因、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | ✅ 本報告只出艙；`二-6` 之二項**並列而⛔ 判**、明載候發單側 |
| `17` | ⛔ 刪任何 ref | ✅ 未刪 |
| `18` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ 收工閘之數係**報告入倉後**以末端追加補入；閘 `7` **出艙於回報·⛔ 入倉** |
