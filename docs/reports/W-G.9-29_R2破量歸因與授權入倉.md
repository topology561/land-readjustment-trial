# W-G.9-29：`R2` 破量之逐界面歸因 ＋ KL 授權入正典

**批號**：`W-G.9-29`（現查 ⇒ **0**；判別力自檢：`W-G.9-28` ⇒ **5 檔** ⇒ 該式非恆 0）
**基座**：`wg925-handoff` @ `89694f3`（＝`origin/wip/s1-endpart`）
**性質**：⛔ 零生產碼 · 🔴 **本批破例動 `docs/rulings/`**（**純插入**·刪除行 **0**）
**結論**：`J0` 過 ⇒ `J1` **成立**（殘差 `0.0000`）·`J2` 不成立 ⇒ **楔形假說成立**；`R-1`〜`R-6` 全部未觸發。

---

## §1 甲組：`R2` 破量之逐界面歸因

### 1-1 量測器與其射程（⛔ 不新寫探針）

沿用 `verify/probes/probe_D2b17_dual.py`。
🔒 **先證其走已改之路徑**：該檔 `:41` `from stepg_pipeline import run_step_g` ⇒ **受詞為 `W-G.9-27` 所改之路徑**，⛔ 非 A 路徑之靜態複本。
🔒 **其 log 為追蹤檔**（`git ls-files verify/out/probe_D2b17_dual.log` ⇒ **1**·未被忽略）
⇒ 依 §1-2：**備份 → 跑 → 輸出另存 → `git checkout` 還原 → 驗還原**（該路徑 `git diff` 為**空**·全樹亦空）。

### 1-2 🔴 `J0`（自檢下放一層·⛔ 先過才判 `J1`／`J2`）

| 值 | 命中 | 判 |
|---|---:|---|
| **`45.9766`**（現況·`W-G.9-27` 後） | **1** | ✅ |
| `76.2616`（改碼**前**之值） | **0** | ✅ ⛔ 非讀舊資料 |

⇒ **`J0` 過**、**`R-1` 未觸發**。
🩸 **一項自誤（已就地更正）**：我首次之判別力對照選了「宗-宗」⇒ **0 命中** ⇒ **該次自檢作廢**（考古 80 修法 ③：對照組回 0 時先疑對照組），重選 log 內必然存在之字樣（`R2` ⇒ 5／`情境` ⇒ 3／`D-2b-17` ⇒ 1）＋偽值 `ZZZ9999` ⇒ 0，方成立。

### 1-3 `0m` 之逐界面全集（⛔ 正面列舉·`甲` 欄 ＝ 生產閘 `:9062` 之算式）

母體 ＝ 探針 `【f-1b】[0m] R2 #0` 之**全部 4 對**：

| 界面 | `甲`（生產） | `乙`（獨立·純 numpy） | 屬 |
|---|---:|---:|---|
| 宗0×宗3 | **26.1624** | 26.1619 | **宗 0** |
| 宗0×宗2 | **19.8142** | 19.8131 | **宗 0** |
| 宗5×宗6 | 0.0000 | 0.3015 | 非宗 0 |
| 宗1×宗2 | 0.0000 | 0.0236 | 非宗 0 |

| 量 | 值 |
|---|---:|
| **宗 0 項合計** | **45.9766** |
| 非宗 0 項合計 | **0.0000** |
| `ΣX`（帶號） | **45.9766** |
| `Σ\|X\|`（絕對值） | **45.9766** |
| **破量**（`②-宗 圍堵閘破[R2]`） | **45.9766** |
| **殘差** `\|宗0合計 − 破量\|` | **0.0000** |

### 1-4 `3.5m` ⇒ **不適用**（⛔ 非「歸因為 0」）

`3.5m` 之破為 **`結構閘 telescoping`**（`ΣRw_實跑=100.02 ≠ R(W_final)−R(W₀)=44.41`·Δ `55.606`），
⛔ **非**重疊閘。而本探針所量者為**重疊量**（`:9047`／`:9050`／`:9062`）
——其 `【f-1】3.5m R2` 之 `甲:9062` ＝ `0.0000`、`乙:9062` ＝ `0.0000`。

🔒 **⇒ 具名為「該閘型不適用逐界面重疊歸因」**，⛔ **非**「宗 0 之歸因為 0」（紀律第 3 條）。
⇒ `3.5m` 之破量歸因**本批未得**，須另用適用於 telescoping 之分解器。

### 1-5 `J0`〜`J3` 逐項判

| # | 預測 | 判 | 依據 |
|---|---|---|---|
| **J0** | 破量 ＝ `45.9766` | ✅ | §1-2 |
| **J1** | 宗 0 合計 ＝ 破量（殘差 ≤ `0.0001`） | ✅ **成立** | 殘差 **`0.0000`** ⇒ **楔形假說成立** ⇒ `W-G.9-30` 之設計範圍 ＝ **界線方向** |
| **J2** | 宗 0 合計 ＜ 破量 | ⛔ **不成立** | ⇒ **`R-2` 未觸發**·⛔ 無第二來源 |
| **J3** | 逐界面全集正面加總·宗0 ＋ 非宗0 ＝ 破量 | ✅ | `45.9766 ＋ 0.0000 ＝ 45.9766` |

⚠️ **射程（⛔ 逐項具名）**

1. **`3.5m` 為不適用**（見 §1-4），⇒ `J1` 之成立**僅涵蓋 `0m`**。
2. 探針之母體含 `R1`〜`R6`，但**生產管線停於 `R2`** ⇒ `R3`〜`R6` 之值係**探針獨立跑得**、
   ⛔ **非生產路徑之現況後果**。**併記（⛔ 不作宣稱）**：`0m R5` 亦呈同一楔形樣態
   （宗0×宗2 `36.1337` ＋ 宗0×宗3 `20.1956` ＝ `56.3293` ＝ 該格破量），
   ⇒ 該樣態**疑非 `R2` 獨有**，惟 `R5` 從未進入生產路徑 ⇒ **無從判定其現況後果**。
3. 探針另報 **`乙` 與 `甲` 不一致之 7 對**（`🔴 矛盾`），係 `D-2b-17` 已載之既有現象
   ⇒ ⛔ 本批未處理、⛔ 不影響 `甲` 欄之加總（`甲` 欄即生產閘自身之算式）。

---

## §2 乙組：入倉三處

| # | 落點 | 手法 | 結果 |
|---|---|---|---|
| §2-1 | `docs/rulings/K-6_街角地分配程序與可分配判準.md`（🔴 **權威序第 1 級**） | 插於整行錨之後（`:702`）、`4. **連動義務**` 之前 | 1963 → **1984**（**+21 純插入**） |
| §2-2 | `docs/reports/W-G.4_泛用阻塞項登記表.md` | 列一插於 `GB-51` 列後；列二插於末 GB 列後 | 303 → **305**；`^\| ` **114 → 116（+2）** |
| §2-3 | `.claude/skills/failure-archaeology/SKILL.md` | 插於節 80 之末（＝檔末·該節至檔末） | 2777 → **2788**（**+11 純插入**） |

🔒 **錨唯一性**（判準＝**整行**·先正規化行尾·`VR-011`）：三錨各 **1**。
🔒 **判別力自檢**：`K-9-5-` 片語於 K-6 ⇒ **76**（⛔ 非 1）⇒ 該式非恆為 1。
　⚠️ 單所警告之 `落地時點`／`D-2b-22` 經現查**確實皆為 1** ⇒ ⛔ 不可作對照，本批未用。

### 🔴 §2-2 之兩項具名（⛔ 不採信單、以倉內為準）

**① 編號：`GB-60` 已占用 ⇒ 改用 `GB-63`。**
現查 GB 已占用號為 `GB-1`〜`GB-62`（另有 `GB-7b`）；`GB-63`／`GB-64` 皆 **0 命中**。
⚠️ `GB-12` 為**已載之缺號**（6 檔明載「沿革未載·本批未查得其是否曾用於他項」）⇒ ⛔ **不回填**。
⇒ 依 `CLAUDE.md`「新則取下一個未占用號·⛔ 禁改既有號」，取 **`GB-63`** 並於此回報。

**② 表之排序慣例本身不一致 ⇒ 我依單之字面落位，具名待裁。**
現查：`GB-1`〜`GB-21` 遞增（`:56`〜`:83`）／`GB-39`〜`GB-22` 遞減（`:85`〜`:105`）／
`GB-41`〜`GB-43`、`GB-59`、`GB-60` 在中段（`:106`〜`:110`）／`GB-58`〜`GB-44` 遞減（`:113`〜`:131`）／
🔴 **最近兩則 `GB-61`／`GB-62` 置於<u>表首</u>**（`:37`〜`:38`·緊接分隔列）。
⇒ 「表末列之後」與「最近兩則置頂」**指向不同位置**。本批**依施工單字面**插於末 GB 列之後；
⛔ 未自行改置，具名於此供施工單方裁定是否移至表首。

### §2-3 之受詞

該加註**縮小**了節 80 標題之射程：標題寫「跳脫寫錯」，而 CC 側那一則實為**同一工具在不同執行環境下行為不同**
⇒ 真受詞為「量測器行為受**執行環境**影響且靜默」，「跳脫寫錯」僅其子情形。

---

## §3 落地自證（考古 68·考古 63：數＝當次輸出）

**1. `git show --numstat`**（逐檔）

```
11	0	.claude/skills/failure-archaeology/SKILL.md
2	0	docs/reports/W-G.4_泛用阻塞項登記表.md
21	0	docs/rulings/K-6_街角地分配程序與可分配判準.md
```

🔴 **`docs/rulings/K-6` 之刪除欄 ＝ `0`** ✅

**2. `K-6` 純插入之構造證**：`git diff -- docs/rulings/` 之刪除行 ⇒ **0**；hunk 之非 `+`／非 context 行 ⇒ **0**；
　`^### 🔒 K-9-5-` 改前 **13** → 改後 **13** ✅

**3. 禁動清單 `git diff --numstat`**（`app.py`／`verify/**/*.py`／`CLAUDE.md`／`verify/baselines/`／兩份凍存物／`WG98`）⇒ **空**
　🔒 判別力自檢：同式對本批三檔 ⇒ **非空**（即上表三列）⇒ 證該式非恆空

**4. 三處新內容各 1 命中**：`釋示補（KL 裁 2026-08-14` ⇒ **1**／`^| **GB-63**` ⇒ **1**／`射程更正（\`W-G.9-29\`` ⇒ **1**

**5. `W-G.4` 表格列數**：`^| ` **114 → 116**（**＋2**·⛔ 非 +1、⛔ 非 +3）⇒ **`R-6` 未觸發**

**6. 考古檔行數**：**2777 → 2788**；該檔 `git diff` 刪除行 ⇒ **0**；`^## <N>. ` 節數 ⇒ **80**

**未追蹤新增**：**無**。

---

## §4 claude.ai 側之自誤（本窗·四則·⛔ 不摘要成通則）

🔒 **登記之用在阻斷、⛔ 非追責**：施工單方之前提須**獨立驗證**，⛔ 不得採信。

| # | 自誤 | 既有形狀 | 出處與狀態 |
|---|---|---|---|
| **1** | **判準之預期碼結構上不可達**——`W-G.9-25` §3 定 `SAME` ＝「`Read` 自報 ＝ `wc -l`」，而 `Read` 之計數慣例對**任何末字元為換行之檔恆 `+1`** ⇒ 陰性對照之預期碼**不可能出現** ⇒ 兩對照同碼 ⇒ 該自檢什麼也證不了 | **節 76 之鏡像**（節 76：閘不可能紅；本例：**對照組不可能出現其預期碼**） | 【倉】已立**考古 79**（`grep -n "^## 79\. " .claude/skills/failure-archaeology/SKILL.md`）·判準已由 `VR-009` 改為方向式三碼 |
| **2** | **量測不可見字元之量測器連壞兩次**——覆核他方「該檔為 CRLF」之陳述時，兩次量測皆回 `0`（`$'\r'` 未展開／heredoc 內跳脫被加倍）⇒ 幾將「受詞不同、兩者皆真」寫成「受驗者之陳述為偽」 | **節 69 之專例**（壞掉的檢查器吐出正好是你期望的值） | 【倉】已立**考古 80**·本批另加**射程更正**（真受詞為**執行環境差異**、跳脫寫錯僅其子情形） |
| **3** | **為受驗者之未驗宣稱加碼**——`W-G.9-25` 之前，將「射程寫小了」逕行升格 | **節 75 之形狀**（拿一個沒查過的前提換到一個域裁·免責標記無用） | 【單】**經對話側送達·⛔ 倉內無從覆核** ⇒ ⛔ 本批不入正典、⛔ 未立新節 |
| **4** | **令 CC 引用當時只在對話側之 `VR-011`**——`W-G.9-27` 施工單 §7 之戒表列 `VR-011`，而該則之**定義式當時倉內 0 命中** ⇒ `W-G.9-27` 報告 `:206` 成為**懸引** | **自足鐵律**之違反（同形狀曾由 `W-G.9-20`／`W-G.9-24` 犯·見 `W-G.9波_新session交接文_WG913-WG924.md` §七 第 1 則） | 【倉】可覆核：`W-G.9-28` §0 現查 `VR-011` 定義式 ⇒ **0**、懸引在 `W-G.9-27` 報告 `:206`；已於 `W-G.9-28` 補登 ⇒ ⛔ 未立新節 |

⚠️ **第 3 則之處置**：其**唯一**來源為施工單之敘述，倉內**無從覆核** ⇒ 依 `【單】` 標注規範，
⛔ **下游文件不得引用為事實**；若須引用，**先入庫使其成為 `【倉】`**。

---

## §5 停機條件之逐條判

| 碼 | 條件 | 判 | 依據 |
|---|---|---|---|
| **R-1** | `J0` 得 `76.2616` | ⛔ 未觸發 | `76.2616` ⇒ 0 命中 |
| **R-2** | `J2` 成立 | ⛔ 未觸發 | 殘差 `0.0000` ⇒ `J1` 成立 |
| **R-3** 🔴 | `docs/rulings/` 有刪除行，或 `K-9-5-*` ≠ 13 | ⛔ 未觸發 | 刪除行 **0**；`K-9-5-*` **13**（＝改前） |
| **R-4** | 寫入落到禁動清單 | ⛔ 未觸發 | §3-3 空 |
| **R-5** | 探針覆寫追蹤檔而未還原 | ⛔ 未觸發 | `probe_D2b17_dual.log` 已 `git checkout` 還原·該路徑 diff 空 |
| **R-6** | `W-G.4` 表格列數差 ≠ ＋2 | ⛔ 未觸發 | `^| ` 114 → 116 |

---

## §6 逐條自檢

| # | 戒 | **已套用** |
|---|---|---|
| 1 | 紀律第 4 條（自檢下放一層） | `J0` **先於** `J1`／`J2`；且 `J0` 之判別力對照**自誤一次後重做** |
| 2 | 紀律第 3 條（無從判定 ≠ 判定為偽） | `3.5m` 具名為**不適用**、⛔ 非 0；`R3`〜`R6` 具名為**無從判定** |
| 3 | 紀律第 8 條 ＋ `VR-011` | 三錨為整行·比對先正規化行尾·判別力用 `K-9-5-` ⇒ **76** |
| 4 | 考古 63（同一次執行） | §1／§3 之數皆為本批當次輸出 |
| 5 | 考古 68（已完成 ≠ 已落地） | §3 六項；每次插入後即以獨立指令驗（純插入斷言內建於插入器） |
| 6 | 考古 80 ＋ 其增量 | §1-2 之對照組自誤已依修法 ③ 作廢重做 |
| 7 | `VR-012`（碼改與基準改分批） | 本批 ⛔ 零生產碼、⛔ 零基準更新 |
| 8 | 考古 66（站點從未執行 ≠ 通過） | §2-2 列二（`GB-63`） |
| 9 | `CLAUDE.md`（編號以倉內為準） | `GB-60` 已占用 ⇒ 改 **`GB-63`** 並回報·⛔ 未改既有號·⛔ 未回填 `GB-12` 缺號 |

---

## §7 ⛔ 本批未做之事

1. ⛔ **未得 `3.5m` 之破量歸因**——其閘型（telescoping）不適用本探針之重疊分解（§1-4）。
2. ⛔ **未對 `R3`〜`R6` 作任何現況宣稱**——`R5` 之同樣態僅**併記**、⛔ 非結論。
3. ⛔ **未處理探針所報 `甲`／`乙` 不一致之 7 對**（`D-2b-17` 已載之既有現象）。
4. ⛔ **未動任何 `.py`**、⛔ 未動 `app.py`／`CLAUDE.md`／`verify/baselines/`／任何凍存物與名單。
5. ⛔ **未動 `docs/rulings/` 之其他任何處**——本批於該檔之異動**僅 §2-1 之一段純插入**。
6. ⛔ **未實作** `K-6` 該釋示補所授權之界線方向變更（屬 `W-G.9-30` 之範圍）。
7. ⛔ **未將第 3 則自誤入正典**（其為 `【單】`·倉內無從覆核）。
8. ⛔ **未自行調整 `W-G.4` 之列序**（排序慣例不一致已具名待裁）。
