# `W-G.9-304R`　施工單（中）執行報告：`K-9-30` 之落地 ／ `自誤 411`・`412` ＋ 坑 `bm` ／ 已算之向於強制處之可得性

> **受詞** ＝ `docs/orders/W-G.9-304_施工單_K-9-30之落地與已算之向之可得性.md`（自倉讀單）。
> **分級** ＝ **中**；**零生產碼**（`34` 檔 blob 期初＝期末相異 `0`）；⛔ 開分支；⛔ 修批；KL 之放行**未動用**。
> **本批之鑄** ＝ `K-9-30` ＋ `自誤 411`・`412` ＋ 坑 `bm`（**四號**）；`GB`／`VR` **⛔ 鑄**。
> **⛔ 估算或出艙本窗 context 之百分比**；本批未遭遇任何資源硬限之可觀測事件。

---

## `§二`　工項零：本單原封入倉（**先於開工閘**）

🔒 **整全性之判準 ＝ `SELF_SHA256`（`P-5`）**·**⛔ 另設帶外釘死**（`自誤 412` 攔法 ①）。

| 造 | 受詞 | 實得 | 期 | 判 |
|---|---|---|---|---|
| **[必綠]** | 原封交付檔 | `P-5` ✅ 相符（受詞 **`38266`** B·實算 `d5dfcc90ce9ee3f9…`／單載 `d5dfcc90ce9ee3f9…`） | 相符 | ✅ |
| **[必紅]** | 受詞區間內改 `1` 位元組之**副本**（**⛔ 入倉**·落於倉外 scratchpad） | 🔴 不符（實算 `28295ccdd840c34b…`） | 不符 | ✅ |

| 項 | 產生指令 | 值 | 判 |
|---|---|---|---|
| 源 bytes／`CR` | `wc -c` ／ `b.count(b'\r')` | `38344` ／ `0` | —— |
| 源內容 `sha256` | `sha256sum <源>` | `3b0389e5f23b625377da12132c5ce655e3a6e4fa980892abb3dc0949124db5f8` | —— |
| **blob `sha1`** | `git rev-parse HEAD:<路徑>` | `9c0fd4de5b52b3c31eccca7f372090179f79233f` | 🔒 **二框並報** |
| **blob 內容 `sha256`** | `git cat-file blob HEAD:<路徑>` ⇒ `sha256` | `3b0389e5f23b6253…`（`38344` B）＝ 磁碟 ＝ 源 | ✅ |
| `CR`（**讀 blob 取 bytes**） | 同上 | **`0`** | ✅ |
| 判別力[必非零] | `git cat-file blob HEAD:data/V6.dxf` | `CR` ＝ **`12308`**（全長 `88341` B） | ✅ |
| `deletions` | `git diff --numstat -z <base> <head>` | `395 / 0` ⇒ **`0`** | ✅ |
| 受詞自證（坑 `bf`） | 落點字串 | 含 `W-G.9-304` ＝ `True`[必真]／含 `補令六` ＝ `False`[必偽] | ✅ |
| 檔數 ＝ `1` | `git diff --name-only -z <base> <head>` | **`1`** | ✅ |
| 造甲[必≥1] | `git cat-file -e HEAD:docs/orders/W-G.9-303_補令六_新窗首則與起手單.md` | `rc = 0` | ✅ |
| 造乙[必為0] | `git cat-file -e HEAD:<執行期組出之人造名>`（字面⛔ 出艙） | `rc = 128` | ✅ |
| 生產碼判法 | `git diff --name-only <base>..<head> \| grep -xE '<四檔>'` | **`0`** 行（`rc = 1`）；對照組[必非零] 掃 `git ls-tree -r --name-only HEAD` ＝ **`4`** 行 | ✅ |

⇒ **零生產碼 commit** ⇒ `常規一` 補款：**逕行 `push`** ⇒ `c9cc385..e89bb56`（`rc = 0`）。

---

## `§一`　開工閘（十二項·**跑於 `§二` 之後**）

| # | 產生指令（逐字） | 實得 | 期 | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `e89bb56973f0a6b4ebb4778d9261e8786fddbc08` | `c9cc3854…` **或其後裔** | ✅ |
| `1`附 | `git merge-base --is-ancestor c9cc3854ca0a2ed9b59e4820ef929c5a38b4a50e HEAD` | `rc = 0` | 後裔 | ✅ |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 同左 | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` | 非祖先 | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本單>` | 🔴 機械款 **`0`**；`P-5` ✅ 相符（受詞 `38266` B）；`P-3` 不紅；🟡 **`6`** 項 | 同左 | ✅ |
| `4'` | 受詞自證（坑 `bf`）：器所印之檔名 ＝ `W-G.9-304_施工單_K-9-30之落地與已算之向之可得性.md` | 含 `W-G.9-304` ＝ `True`[必真]／含 `補令六` ＝ `False`[必偽] | 同左 | ✅ |
| `5` | `… --selftest`；`python verify/tools/wg9223_acceptance_audit.py --selftest` | 二器 `rc = 0`；preflight **四靶逐項成立** | 同左 | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 **`400`**／**`410`**（缺 `[106,355,356,357]`）｜`GB` `168`／`170`（缺 `[12,87]`）｜`VR` `80`／`95`（缺 `[73,75]`）｜`K-9` **`28`**／**`29`**（缺 `[]`） | 同左 | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`90`／`90`**·末標籤 **`bl`** ⇒ 下一 **`bm`**；器自印判別力二造 ✅ | 同左 | ✅ |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py` | **`34`**（`33 + 1`）；判別力[必不命中] 子層 `*.py` ＝ **`320`** > `0`（🔒 `W-G.9-303R6` 時為 `316` ⇒ **逐批漂移**·⛔ 定值） | `34` | ✅ |
| `8` | `git ls-tree -r HEAD verify/baselines \| sha256sum`；`… --name-only \| wc -l` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`·檔數 **`298`** | 同左 | ✅ |
| `9` | `git ls-remote origin 'refs/heads/verify/*'` | 共 `20` 支；`verify/W-G.9-299-gb170` 命中 **`0`**；`verify/W-G.9-269-p3a` 命中 **`1`** | 同左 | ✅ |
| `10` | 本波七單 ＋ 本單：逐單 `git cat-file -e HEAD:<p>` ⇒ `git rev-parse` | **八單皆態1**（態1 `8`／態3 `0`）；blob 見下 | 皆態1 | ✅ |
| `10`造 | 一**執行期組出**之人造路徑（字面⛔ 出艙） | `rc = 128`（≠ `0`） | [必為零] | ✅ |
| `11` | 物證落檔（清單**自 `交接註_舊窗收工二` 項 `10` 機械抽出**·⛔ 轉錄 ＋ `W-G.9-303R6` 之四份·自 `commit 094b8413…` 之 `--name-only` 取） | 受詞 **`17`** 檔；**三態分列：態1 `17` ／ 態2 `0` ／ 態3 `0`** ⇒ 逐位未動 | 逐位未動 | ✅ |

🔒 **閘 `10` 之逐單 blob**：施工單 `8c85fc1ccdf781ce`／補令一 `87cb2813c3cf2040`／補令二 `3d91f357bb734c33`／
補令三 `d03552c4bcfcef05`／補令四 `d709a39879e52d0e`／補令五 `db74056172eca9a6`／補令六 `44a0ddaff4fd8c92`／
**本單 `9c0fd4de5b52b3c3`**。
🔒 **閘 `11` 之 `W-G.9-303R6` 四份 `.log` 之 `CR`**（已具名之自捕 `5`·**⛔ 修**·⛔ 判紅）：
`WG9303R6_dynamic.log` `1086`／`dynamic2.log` `1091`／`static.log` `236`／`static2.log` `124`。

### `§一-1`　`P-4` 之逐項處置（**⛔ 靜默略過**·**⛔ 目視**）

🔒 **先查該器之觸發式**（`verify/probes/probe_order_preflight.py` 逐字）：
`if ("→" in p) and re.search(r"逐字|轉引|引", p):` ⇒ 若 `not re.search(r"座標系|方向|注入方向|來源.*[:：]|自.*抽出", p)` 則提示。
🔒 **歸類之機械依據 ＝ 逐段印出其每一個 `→` 之上下文**（±26 字）·⛔ 目視。

| 起列 | `→` 數 | `逐字\|轉引\|引` 之命中 | **真受詞**（自箭頭上下文機械定位） | 歸類 | 處置 |
|---|---|---|---|---|---|
| `:3` | `2` | `['逐字','逐字']` | `① §二 工項零 → ② §一 開工閘 → ③ 其餘工項` | **(甲)** 流程節序 | 具名豁免 |
| `:24` | `1` | `['引']` | `（\`:27\` 之 \`→\`×3 中三者全來自被引之觸發式字面）` ⇒ **`→` 符號之字面被引述** | **(甲)**（引述他處之串接） | 具名豁免 |
| `:72` | `2` | `['逐字','逐字']` | `佔位符 → 跑 → 填` | **(甲)** 流程節序 | 具名豁免 |
| `:159` | `1` | `['逐字']` | `「幾何 → 街廓識別」映射 \`_best_block\`` ⇒ **映射之定義域→值域** | **(甲)** 查表鏈／映射之串接 | 具名豁免 |
| `:330` | `2` | `['逐字']` | `佔位符 → 跑 → 填`（收工閘 `5'` 列） | **(甲)** 流程節序 | 具名豁免 |
| `:370` | `10` | `['逐字','逐字']` | 報告之節序（`§二 → §一 → §三 → §四 → §五 → §六 → 收工閘 → 自檢 → 逐 commit`）＋ `佔位符 → 跑 → 填` | **(甲)** 流程節序 | 具名豁免 |

🛑 **六項皆⛔ 幾何向量之方向性轉引** ⇒ **無座標系可具名**；⛔ 有任一項落於三類之外 ⇒ ⛔ 停。
🛑 本批凡出艙**幾何向量**者（`§六` 款 `3` 之 `allocation_dir` 實值）**⛔ 出艙其夾角** ⇒
「釘槽並以實邊機驗」之附款**無受詞**——此係**判定組為空**之照實具名，⛔ 以「未觸」充綠。

---

## `§三`　工項一：`K-9-30` 之鑄

**落點** ＝ `docs/rulings/K-6_街角地分配程序與可分配判準.md` **檔末追加**（嚴格 append-only）。

### 三-1　界之唯一性（**二數並報**·母體 ＝ 本單單檔 `38344` B／`396` 列）

| 界 | **整列逐位相等**框 | 子字串框 |
|---|---|---|
| `<!-- K930-BEGIN -->` | **`1`**（列 `109`） | `2` |
| `<!-- K930-END -->` | **`1`**（列 `136`） | `2` |

payload ＝ **`24`** 列／**`1882`** B；首列 ＝ `### 🔒 \`K-9-30\`　**無街角第 \`1\` 宗之街廓：…**`；
末列 ＝ `⛔ 推強制抵費地之任何量。`。**⛔ 剝層、⛔ 加 `> `、⛔ 改一字**；追加式 ＝ `old + b'\n' + payload + b'\n'`；**落檔 `open(p,'wb')`**。

### 三-2　append-only 之三造 ＋ 機驗

| 項 | 實得 | 期 | 判 |
|---|---|---|---|
| 舊／新 bytes | `299931` → `301815`（`CR` 皆 `0`） | —— | —— |
| 造①[必真] `new.startswith(old)` | `True` | `True` | ✅ |
| 造②[必真] `new.endswith(payload + b'\n')` | `True` | `True` | ✅ |
| 造③[必偽] `old.startswith(new)` | `False` | `False` | ✅ |
| `wg942_append_audit.py`（**`append_audit`**） | 受檢 `77` 筆·**結論：✅ 全部成立**·`rc = 0` | `rc = 0` | ✅ |
| 哨兵 `K930` 入該檔命中 | **`0`** | `0` | ✅ |

### 三-3　鑄號框之**當場現查**（框**自器碼逐字取**·⛔ 轉引本單）

`RE_K9_H` ＝ `'^#{2,4} '`；`RE_K9` ＝ `` '`?K-9-(\d{1,2})`?(?![0-9])' ``。

| 項 | 實得 | 期 | 判 |
|---|---|---|---|
| `K-9` 相異／MAX（母體 ＝ `K-6` 單檔） | **`29`** ／ **`30`** | `29`（`28+1`）／`30` | ✅ |
| `30` 之命中 | **`1`** | `1`（⛔ `> 1`） | ✅ |
| 判別力[必命中] `29` 之號 | `True` | `True` | ✅ |
| 判別力[必為零] 人造號（執行期組出） | `False` | `False` | ✅ |
| **器本身**（`registry`） | `K-9 : {'相異': 29, 'MAX': 30, 'MIN': 2, '缺號': []}` | 同左 | ✅ |

### 三-4　🔒 **三框交叉誤中**（分列並報·payload `24` 列）

| 框 | 命中 | 期 |
|---|---|---|
| ① `K-9`（`RE_K9_H` ⋀ `RE_K9`）**本 payload 之正框** | **`1`** | `1` |
| ② 自誤（`RE_ZW_DEF` ／ `RE_ZW_RNG` ／ `RE_ZW_BAT`） | **`0` ／ `0` ／ `0`** | 皆 `0` |
| ③ 坑（`hrx` D-H 標題形·自 `probe_WG9269_pit_index.py` 取） | **`0`** | `0` |

🔒 **判別力[必非零]** ＝ 框① 之命中 `1` ⇒ 三框之量測器**⛔ 恆為 `0`**。

---

## `§四`　工項二：`自誤 411`・`412` ＋ 坑 `bm` ＋ `ALPHA_LABELS`（**三段分列 `commit`**）

### 四-1　段一 `ZW411`（`自誤 411`・`412`）

| 項 | 實得 |
|---|---|
| 界之唯一性（**二數並報**） | `<!-- ZW411-BEGIN -->` 整列框 **`1`**（列 `155`）／子字串框 `2`；`<!-- ZW411-END -->` 整列框 **`1`**（列 `215`）／子字串框 `2` |
| payload | **`57`** 列／**`4937`** B |
| 目標檔 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`：`857958` → `862897` B（`CR` 皆 `0`） |
| append-only 三造 | 造①`True`／造②`True`／造③`False` ✅ |
| 哨兵 `ZW411` 入簿命中 | **`0`** ✅ |
| 定義列命中（`RE_ZW_DEF`） | `411` ⇒ **`1`**／`412` ⇒ **`1`** ✅ |
| 🔒 標題形（**C 形**·⛔ B 形） | `` `自誤 411` `` ⇒ **`1`**／B 形 `` 自誤 `411` `` ⇒ **`0`** ✅ |

### 四-2　段二 `PITBM`（坑 `bm`）

| 項 | 實得 |
|---|---|
| 界之唯一性 | `<!-- PITBM-BEGIN -->` 整列框 **`1`**（列 `217`）／子字串框 `2`；`<!-- PITBM-END -->` 整列框 **`1`**（列 `240`）／子字串框 `2` |
| payload | **`20`** 列／**`1646`** B |
| 目標檔 | 同上：`862897` → `864545` B（`CR` 皆 `0`） |
| append-only 三造 | 造①`True`／造②`True`／造③`False` ✅ |
| 哨兵 `PITBM` 入簿命中 | **`0`** ✅ |

### 四-3　段三 `ALPHA_LABELS` 之擴至 `bm`（**僅新增一列**·⛔ 改既有列一字）

| 項 | 產生指令 | 實得 | 期 | 判 |
|---|---|---|---|---|
| `numstat` | `git diff --numstat -- verify/probes/probe_WG9269_pit_index.py` | **`1` 增 ／ `0` 刪** | `1 / 0` | ✅ |
| `CR` | `open(p,'rb').read().count(b'\r')` | **`0`**（`15322` → `15398` B） | `0` | ✅ |
| `ALPHA_LABELS` 出現次數 | 全檔子字串 | `8` → **`9`** | `+1` | ✅ |
| 既有列⛔ 改之機驗 | 舊全檔逐列於新全檔**按序全數存在** | `True`（未對上之舊列 **`0`**·列數 `299` → `300`） | `True` | ✅ |
| 判別力[必偽] | 舊全檔為新全檔之**嚴格前綴** | `False`（⇒ 係**插入**、非追加·該檢非恆真） | `False` | ✅ |

新增之列逐字 ＝ ``ALPHA_LABELS += ["bm"]             # 🆕 `W-G.9-304` §四 所准之一坑``（形同其上之 `+= ["bl"]` 列）。

### 四-4　落地後之機驗（**器本身**）

| 項 | 實得 | 期 | 判 |
|---|---|---|---|
| 自誤 相異／MAX | **`402`** ／ **`412`** | `402`（`400+2`）／`412` | ✅ |
| `K-9` 相異／MAX | `29` ／ `30` | 同 `§三` | ✅ |
| `GB`／`VR` | `168`／`170`·`80`／`95` | **一字未動** | ✅ |
| 坑 | **`91`／`91`**·末標籤 **`bm`** ⇒ 下一 **`bn`**·`bm` 可解析 `True` | `91/91`·`bm` | ✅ |
| 坑之判別力二造（器自印） | 造甲[必有定義] 坑 `n` ⇒ 定義處 `6`（≥1）✅／造乙[必為零] 人造之坑 ⇒ `0`／`0` ✅ | —— | ✅ |
| `append_audit` | 受檢 `77` 筆·**✅ 全部成立**·`rc = 0` | `rc = 0` | ✅ |

🔒 **坑 `bm` 之定義處<u>多於一處</u>（預先受領·⛔ 判紅、⛔ 逕修）**：器之輸出逐字 ＝
`` | `bm` | `W-G.9-304_施工單_K-9-30之落地與已算之向之可得性.md:1`（D-H）＋3 | ✅✅✅ | `12` | ✅ 可解析 | ``
⇒ **定義處 ＝ `1 + 3`**。**照實具名**：其成因係**本單已入倉且其 payload 含該標題形**（單內 `§四` 之界內一處 ＋ 其**已落地於簿**之一處 ＋ 單內之他處）。
**同族為本倉常態**：`bl` ＝ `＋9`／`bk` ＝ `＋1`。

---

## `§五`　工項三：`GB-170` 之末端追加

| 項 | 實得 |
|---|---|
| 界之唯一性（**二數並報**） | `<!-- GB304-BEGIN -->` 整列框 **`1`**（列 `255`）／子字串框 `2`；`<!-- GB304-END -->` 整列框 **`1`**（列 `282`）／子字串框 `2` |
| payload | **`24`** 列／**`2311`** B（🔒 其形**逐字保留 `> `**·⛔ 鑄任何號） |
| 目標檔 | `docs/reports/W-G.4_泛用阻塞項登記表.md`：`910274` → `912587` B（`CR` 皆 `0`） |
| append-only 三造 | 造①`True`／造②`True`／造③`False` ✅ |
| `append_audit` | 受檢 `77` 筆·**✅ 全部成立**·`rc = 0` ✅ |

### 五-1　🔒 **反向機驗**（`自誤 402` 攔法 ④·**三形分列**·框**自器碼當場取**）

`RE_GB_F1` ＝ `` '^#{2,4} [^\w`]{0,8}`?GB-(\d{1,3})`?(?![0-9])' ``；
`RE_GB_F2` ＝ `` '^\| *\*\*`?GB-(\d{1,3})`?\*\*' ``；
`RE_GB_F3` ＝ `` '^#{1,6} .*?`?GB-(\d{1,3})`?(?![0-9])' ``。

| 形 | 相異 | MAX |
|---|---|---|
| `RE_GB_F1` | `93` | `170` |
| `RE_GB_F2` | `131` | `137` |
| `RE_GB_F3` | `95` | `170` |
| **聯集** | **`168`**（期 `168`） | **`170`**（期 `170`） |

缺號（`[MIN..MAX]` ＝ `[1..170]`）＝ **`[12, 87]`**（期 `[12, 87]`）⇒ **三數皆未變** ✅。
🔒 **二形以上皆 `0` 者 ＝ 無**（三形皆非零 ⇒ ⛔ 須 loud）。

| 造 | 三形分列 | 判 |
|---|---|---|
| [必命中] `GB-170` | `{F1: True, F2: False, F3: True}` ⇒ 至少一形 `True` | ✅ |
| [必為零] 人造號（執行期組出·字面⛔ 出艙） | `{F1: False, F2: False, F3: False}` ⇒ 皆 `False` | ✅ |

### 五-2　`GB-132` 避讓

| 項 | 實得 | 期 | 判 |
|---|---|---|---|
| payload 內 `^#` 起首之列 | **`1`**（逐字 ＝ `## 🔧 路丙落點之**三度受阻** ＋ 域裁之落地（…）`） | —— | —— |
| 其中含 `GB-` 者 | **`0`** | `0` | ✅ |
| 判別力[必非零]（**同一偵測器**掃 `GB` 簿全檔） | **`211`** 命中（樣本 ＝ ``## 十、`GB-69`（…）``） | `> 0` | ✅ |
| 判別力[必為零]（人造列·執行期組出） | `False` | `False` | ✅ |

---

## `§六`　工項四：已算之向於強制處之可得性現查

**落檔（全量·⛔ `| tail -N`）** ＝ `verify/out/WG9304R_static.log`（`216` 列·`CR = 0`）
＋ `verify/out/WG9304R_dynamic.log`（`1157` 列·`CR = 0`）。
**母體之產生指令** ＝ `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py` ⇒ **`34`** 檔（外部錨 ＝ 閘 `7`）。

### 六-1　款 `1`　該值於 `_solve_G_one` 體內之全部去向

**定義處（函式名二形並取）**：形① `def _solve_G_one` ⇒ **`1`** 處（`app.py`）；
形② 字面 `'_solve_G_one'`（`ns[<字面>]` 族）⇒ **`4`** 處（`app.py`／`verify/stepg_pipeline.py`／`verify/test_corner_first_lot_G.py`）。
其形參 ＝ `0` 個位置形參 ＋ **`20`** 個 kwonly。
🔑 **非頂層之基數**（`自誤 411` 攔法 ②·本框⛔ 及於該層）：`_solve_G_one` 體內之 `FunctionDef`／`Lambda` ＝ **`0`**。

**值之流**（自 `allocation_dir` 之 `Store` 起·遞移追派生名）⇒ 追蹤之名 ＝ `['_alloc_dir_used', '_r', 'allocation_dir']`：

```
_alloc_dir_used ← _alloc_dir_used = (None if allocation_dir is None
_r              ← _r['_alloc_dir_used'] = _alloc_dir_used          # D-2b-3 §二-3（純加性·fallback 路徑同記）
```

🔑 **窮舉之產生方式（其產生方式須出艙）**：⛔ 以單所列之「十三形」為上限。本器改採
**依<u>實際父節點型別</u>分類**——對受詞之**每一個** AST 出現，取 `(父節點型別, 欄位名, ctx)` 三元組為其形；
此分類**構造上窮舉**（任一出現必有唯一之父節點），⛔ 依賴任何人所能想到之清單。

受詞之出現總數 ＝ **`17`**；相異之 `(父節點型別, 欄位, ctx)` ＝ **`9`**：

| 父節點型別 | 欄位 | `ctx` | 次數 | 名 | 逐字（首例） |
|---|---|---|---|---|---|
| `Assign` | `targets` | `Store` | `4` | `['_alloc_dir_used','_r','allocation_dir']` | `_alloc_dir_used = (None if allocation_dir is None` |
| `Subscript` | `value` | `Load` | `4` | `['_r']` | `_r['area_geom'] = round(_r.get('S', 0) * avg_depth, 2)` |
| `Assign` | `value` | `Load` | `2` | `['_alloc_dir_used']` | `_r['_alloc_dir_used'] = _alloc_dir_used` |
| `Tuple` | `elts` | `Load` | `2` | `['_r']` | `return _r, '代數迭代(fallback)'` |
| `Attribute` | `value` | `Load` | `1` | `['_r']` | `_r['area_geom'] = round(_r.get('S', 0) * avg_depth, 2)` |
| `Compare` | `left` | `Load` | `1` | `['allocation_dir']` | `_alloc_dir_used = (None if allocation_dir is None` |
| `arguments` | `kwonlyargs` | `-` | `1` | `['allocation_dir']` | `allocation_dir=None, side_mid=None, W_prev=0.0, near_dir=None,` |
| `comprehension` | `iter` | `Load` | `1` | `['allocation_dir']` | `else tuple(float(_c) for _c in allocation_dir))` |
| `keyword` | `value` | `Load` | `1` | `['allocation_dir']` | `allocation_dir=allocation_dir,` |

🔴 **十三形之外者（＝ 該清單⛔ 窮舉之<u>直證</u>）**：
`('Assign','value','Load')`／`('Tuple','elts','Load')`／`('Compare','left','Load')`／
`('comprehension','iter','Load')`／`('Attribute','value','Load')` —— **`5`** 個。
🛑 **十三形中本案零命中者（逐項 loud·⛔ 判為「無此受詞」）**：
`.get`／`.setdefault`／`.update`／`Attribute-Store`／`Call 之實參`／`Dict 鍵字面`／`DictComp`／
`For target`／`Return`／`Subscript-Store`／`解包 Store` —— **其於本案非真形**。
⚠️ 其中 `Return` 之零命中係**框之口徑**所致：受詞之名⛔ 直接為 `Return.value`，而係
`Tuple.elts`（`return _r, '幾何二分法'`）⇒ **實質上該值確經 `Return` 出艙**。

判別力：[必為零] 人造名（執行期組出·字面⛔ 出艙）之出現數 ＝ **`0`** ✅；[必非零] 出現總數 ＝ **`17`** ✅。

### 六-2　款 `2`　跨呼叫可見之容器

`_solve_G_one` 之區域名（`Store` ∪ 形參）基數 ＝ **`23`**；`Return` 處數 ＝ **`2`**；`Return` 所含之名 ＝ `['_r']`。
受詞被寫入之 `Subscript` 容器 ⇒ **`3`** 處（其中 `'_alloc_dir_used'` **二**處、`'area_geom'` **一**處）：

| receiver 逐字 | 鍵逐字 | 鍵之 AST | 賦值逐字 | 區域名？ | **回傳值？** | 跨呼叫可見 |
|---|---|---|---|---|---|---|
| `_r` | `'_alloc_dir_used'` | **字串常數** | `_r['_alloc_dir_used'] = _alloc_dir_used      # D-2b-3 §二-3（純加性）` | `True` | **`True`** | **`True`** |
| `_r` | `'_alloc_dir_used'` | **字串常數** | `_r['_alloc_dir_used'] = _alloc_dir_used          # …fallback 路徑同記` | `True` | **`True`** | **`True`** |
| `_r` | `'area_geom'` | 字串常數 | `_r['area_geom'] = round(_r.get('S', 0) * avg_depth, 2)` | `True` | `True` | `True` |

🔑 **明確回答一問：其鍵<u>⛔ 含街廓識別</u>**——鍵為**字串常數**，逐字 `'_alloc_dir_used'`
（⛔ 含 `blk`／`label`／`lbl`／`block_id`）⇒ **⛔ 受 `§零-3` 裁 `2` 之同限**。

**該鍵於生產碼 `34` 檔之消費端（正面列舉）**：`app.py` **`8`** 列／`verify/stepg_pipeline.py` **`6`** 列。
其**讀**之逐字（⛔ 註解列）：

```
app.py                     : _near_dir_left  = res.get('_alloc_dir_used')
app.py                     : _near_dir_right = res.get('_alloc_dir_used')
verify/stepg_pipeline.py   : _near_dir_left  = res.get('_alloc_dir_used')
verify/stepg_pipeline.py   : _near_dir_right = res.get('_alloc_dir_used')
verify/stepg_pipeline.py   : _prev_dir       = _n['res'].get('_alloc_dir_used')
```

### 六-3　款 `3`　強制呼叫端之在域名與**在域實值**

`_corner_buffer_S` 之呼叫點（三形並取）＝ **`7`** 處（**本批獨立重得**·與 `W-G.9-303R6` 相符）。
**強制呼叫端** ＝ `verify/stepg_pipeline.py :: _run_step_g_impl`，其**第 `4` 位序實參（`allocation_dir`）逐字 ＝ `allocation_dir_block`**。

**動態之在域實值**（`f_locals`·**⛔ 以「在域」充「可取」**·坑 `bg`）——生產錄 **`21`** 次，逐次之框皆 `_run_step_g_impl`：

| (街廓, 側) | `f_locals` 之街廓鍵（**自證存在**） | 同框其他命中鍵（**第二鍵**） | 第 `4` 實參之**當場值** | 該框之 WATCH 實值 |
|---|---|---|---|---|
| `('R4','L')`（seq `91` 等 `15` 次） | `blk_label` ✅ | **`[]`**（🛑 `_lbl`／`blk`／`lbl`／`label`／`_blk` **皆⛔ 在域**） | `array([ 0.71966705, -0.69431933])` | `{'allocation_dir_block': array([ 0.71966705, -0.69431933])}` |
| `('R2','L')`（seq `213` 等 `6` 次） | `blk_label` ✅ | **`[]`** | `array([0.72601884, 0.68767481])` | `{'allocation_dir_block': array([0.72601884, 0.68767481])}` |

🔴 **WATCH 之其餘四名（`_near_dir_left`／`_near_dir_right`／`res`／`_alloc_dir_used`）
於該框之 `f_locals` 中<u>⛔ 出現</u>**（`21` 次逐次皆然）。
🛑 **本報告⛔ 由此判其可施或不可施**——只出艙該事實。

⚠️ **靜態器之同名欄⛔ 採信（自捕 `3`）**：靜態器印「款 `2` 之容器鍵是否**在該函式內出現**」得 `True`，
惟其實作為 `k in SRC[f]`（**全檔**包含檢）⇒ **標籤與量測不符**；其正解由上表之 `f_locals` 實值承擔。

### 六-4　款 `4`　時序（動態·授權之 `run_verification` 全量**一次**）

`run_verification.main()` 之 `rc` ＝ **`1`**（🔒 **既存之准紅碼**·⛔ 本批所致）。

**量測器之自證**（🛑 母體⛔ 含其自身輸出）：

| 受詞 | 生產錄 | **器自身（須 `0`）** | 判 |
|---|---|---|---|
| `_corner_buffer_S` | **`21`** | **`0`** | ✅ |
| `_solve_G_one` | **`2065`** | **`0`** | ✅ |

🔑 **判別力[必非零]（⛔ 以恆為 `0` 之欄充造）＝ 本器內之<u>合成對照</u>**：刻意**雙**包裝一 dummy
⇒ 器自身之命中 **`2`／`2`**（≥ `1`）✅ ⇒ 該欄**⛔ 恆為 `0`**。
冪等自證：`RV.harvest` **`2`** 次／`wrap()` **`2`** 次／**因已包裝而跳過 `1` 次**。
呼叫端一律以 `(co_filename, co_name, co_firstlineno)` 序辨（🛑 **`id(·)` ⛔ 作錨**）。

**`side` 之原值集合（原值與正規值並報）**：`_corner_buffer_S` ⇒ `['left']`；
`_solve_G_one` ⇒ `['右側', '左側', '無']` ⇒ **詞彙不同**，故正規化為 `L`／`R`／`無` 後比較。

| (街廓, 側正規) | `_corner_buffer_S` **強制**之首次呼叫序號 | `_solve_G_one`（`is_corner` 真）之首次呼叫序號 | **停八成就？** |
|---|---|---|---|
| `('R2', 'L')` | **`213`** | **`19`** | ✅ **不成就**（`19 ≤ 213`） |
| `('R4', 'L')` | **`91`** | **`1`** | ✅ **不成就**（`1 ≤ 91`） |

**`_alloc_dir_used` 之實測**：`_solve_G_one` 生產錄 `2065` 次，其回傳之 `_alloc_dir_used`
**非 `None` 者 ＝ `2065` 次**（逐次皆有值）——**跨呼叫可見之直證**。
**不可得之分項成因**：**無記錄**（🛑 ⛔ 讀為「無不可得之分項」——其僅表本器所設之二探點未觸發該分支）。

### 六-5　🛑 **二停機款之判**（逐 (街廓,側) 分列·⛔ 以「多數格不成就」充不成就）

| 停機款 | 受詞 | 逐格之判 | **判** |
|---|---|---|---|
| **停七**（可得性·繫於性質） | 款 `1` 之**全形窮舉**下，該值⛔ 寫入任何跨呼叫可見之容器 | 該值**確已寫入** `_r['_alloc_dir_used']`（**二**處·`_r` ＝ **回傳值**·鍵為字串常數）；其消費端於生產碼 `34` 檔中實測 **`5`** 處 `read` | ✅ **不成就** |
| **停八**（時序·繫於性質） | 於**任一**強制之 (街廓,側)，`_solve_G_one`（`is_corner` 真）之首次序號**晚於** `_corner_buffer_S` 強制之首次序號 | `('R2','L')` `19 ≤ 213`／`('R4','L')` `1 ≤ 91` ⇒ **成就之格 ＝ `[]`** | ✅ **不成就** |

🛑 **⇒ 二停機款皆不成就 ⇒ ⛔ 停機、⛔ 上呈。**
🛑 **本工項⛔ 判其可施或不可施、⛔ 擬任何改法或 diff、⛔ 改生產碼一字、⛔ 開分支。**

### 六-6　款 `5`　判別力四造

| 造 | 實得 | 判 |
|---|---|---|
| [必為 `34`] 外部錨（坑 `bj`） | `34` | ✅ |
| [必非零] 款 `1` 之去向處數 | `17`（≥ `1`） | ✅ |
| [必為零] 人造名（執行期組出）於款 `1` 之框下 | `0` | ✅ |
| [必真] `_first_corner_alloc_dir` 在 `ns` 之鍵 | `True` | ✅ |

---

## 🩸 本批之**自捕**（⛔ 頂替·**⛔ 鑄號**·鑄號候發單側）

🛑 **⛔ 讀為「本批無誤」** ——本批自捕 **`4`** 則，**全為 CC 自身量測器／落檔之缺陷**，皆於出艙前攔下。

| # | 形 | 徵候 | 成因 | 處置 |
|---|---|---|---|---|
| `1` | 🔴 **`grep` 框經 shell 引號失真 ⇒ 偽 `0`** | 開工閘 `6'` 首次以 `grep -cE` 取坑之資料列得 **`0`**，而**同一輸出**之造甲為「定義處 `6`」⇒ **自相矛盾** | 反引號於 `"$( … )"` 內之雙層引號解析中失真（memory 陷阱 ㊽ 之同族） | 改以 **Python 直讀落檔**（反引號**執行期組出**·⛔ 使其字面經 shell）重算 ⇒ `90/90`·末 `bl` |
| `2` | 🔴 **落檔 `CR = 216`** | `verify/out/WG9304R_static.log` 首版全檔 CRLF | **⛔ 我之寫檔層**（已 `open(p,'wb')`）——係**子行程 `sys.stdout` 之 Windows 文字層**將 `\n` 譯為 `\r\n`；`reconfigure(encoding=…)` **⛔ 改 `newline`** | 二器改 `reconfigure(encoding="utf-8", newline="\n")` 並重跑 ⇒ `CR = 0`（`17363 − 17147 = 216` ＝ `CR` 數·**自洽**）。首版**保全⛔ 覆寫**（改名 `_v1_crlf`）並**移出倉外**——🛑 **⛔ 入倉**，否則破收工閘 `3'` |
| `3` | 🔴 **標籤與量測不符** | 靜態器印「款 `2` 之容器鍵是否**在該函式內出現**」⇒ `True` | 其實作為 `k in SRC[f]`（**全檔**包含檢），⛔ 函式範圍 | 該欄**⛔ 採信**；款 `3` 之正解改由動態器之 `f_locals` **實值**承擔（見 `六-3`） |
| `4` | 🟡 **判別力造選得不具鑑別力** | `§五` `GB-132` 避讓之[必非零]造（payload 內含 `GB-` 之非 `#` 起首列）得 **`0`** | payload **全檔無 `GB-`** ⇒ 該造**結構上不可能非零** | 改以**同一偵測器**掃 `GB` 簿全檔 ⇒ **`211`** 命中（樣本已具名）⇒ 偵測器非惰性獲證 |

---

## 一句話推導

該已算之向（`_first_corner_alloc_dir(side_mid)` 之結果）**確經 `_r['_alloc_dir_used']`（回傳值·鍵為字串常數·⛔ 含街廓識別）跨呼叫出艙**，且於二個強制格其 `is_corner` 真之首次呼叫**皆早於**強制之首次呼叫（`19 ≤ 213`／`1 ≤ 91`）⇒ **二停機款皆不成就**；惟強制呼叫端之 `f_locals` 中**⛔ 有** `res`／`_near_dir_*`／`_alloc_dir_used` 任一名，其當場唯一在域之向係 `allocation_dir_block`——**此為現查之出艙，⛔ 任何可施與否之判。**

---

## 逐 `commit`（**全 `40` 碼**）

| # | `commit` | 內容 | `deletions` |
|---|---|---|---|
| `1` | `e89bb56973f0a6b4ebb4778d9261e8786fddbc08` | `§二` 工項零：本單原封入倉 | `0` |
| `2` | `1e7e21f00c47d06f6d574a249813bf191ae97b0f` | `§三` 工項一：`K-9-30` 之鑄 | `0` |
| `3` | `a99af6072b26718961b861eac6f9fa03062a5d17` | `§四` 段一：`自誤 411`・`412` | `0` |
| `4` | `118247a0d2ff8399baf9385b920c66789eccf72a` | `§四` 段二：坑 `bm` | `0` |
| `5` | `4192d5228d805673faa7366ce55a11a097af57f4` | `§四` 段三：`ALPHA_LABELS` 擴至 `bm` | `0` |
| `6` | `fb7f8f83ac697bb5ede059bcf32bcbf0d746e0e1` | `§五` 工項三：`GB-170` 末端追加 | `0` |
| `7` | `ceaa747bd750ca91a09aa92fd6de53e22ae16311` | `§六` 工項四：二量測器與二落檔 | `0` |
| `8` | （本報告之 `commit`·見末端追加段） | 執行報告入倉 | `0` |

**基座** ＝ `c9cc3854ca0a2ed9b59e4820ef929c5a38b4a50e`。

---

## `§七`　收工閘（**報告入倉後量**·嚴格末端追加·🛑 **落檔 `open(p,'wb')`**）

🛑 本段之一切數**皆於報告 `commit`（`bcff8f7387b171a7219a87b5797f4bf5de4ec369`）之後實跑**；
🔒 **先跑再填**（坑 `bm` 攔法 ①：佔位符 → 跑 → 填）——本段落筆前無任何佔位以外之數。

### 七-1　逐閘

| # | 產生指令 | 實得 | 期 | 判 |
|---|---|---|---|---|
| `1` | `git show --numstat --format= -z <各 commit>` | 九 `commit` 逐檔具名（見 `七-2`）；**deletions 合計 ＝ `0`** | 全 `0` | ✅ |
| `2` | `git diff --name-only <前>..<後> \| grep -xE '<四檔>'`（**逐區間**） | 九區間**逐區間 `0` 行**（`rc = 1`） | 逐區間 `0` | ✅ |
| `2`造 | 同 pattern 於 `17fd981^..17fd981` | **`2`** 行 ＝ `['app.py', 'verify/stepg_pipeline.py']` | [必非零] | ✅ |
| `3` | `.md`／`.py` 之 `CR`（框 ＝ 倉內 blob·`git cat-file blob` **取 bytes**） | **`8`** 檔逐檔 `CR` ＝ `0`（`CR` 最大值 **`0`**） | `0` | ✅ |
| `3'` | 🆕 **本批新增之 `.log` 之 `CR`** | `WG9304R_dynamic.log` `0`／`WG9304R_static.log` `0`（最大值 **`0`**） | `0` | ✅ |
| `3`造 | `git cat-file blob HEAD:data/V6.dxf` | `CR` ＝ **`12308`**（全長 `88341` B） | [必非零] | ✅ |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 相異 **`402`**／MAX **`412`**（缺 `[106,355,356,357]`）；`K-9` 相異 **`29`**／MAX **`30`**（缺 `[]`）；`GB` `168`／`170`（缺 `[12,87]`）；`VR` `80`／`95`（缺 `[73,75]`）·`rc = 0` | ＋二／＋一·`GB`／`VR` 一字未動 | ✅ |
| `5` | `python verify/probes/probe_WG9269_pit_index.py` | 資料列 **`91`**／可解析 **`91`**·末標籤 **`bm`** ⇒ 下一 **`bn`**·`rc = 0` | `91/91`·`bm` | ✅ |
| `5`自洽 | 期初 `git ls-tree -r --name-only -z <基座>` ⋂ `*.md`（**⛔ `--with-tree`**·坑 `bl`）；新增以 `git diff --diff-filter=A` | 期初 **`817`**（**＝ `W-G.9-303R6` 收工閘 `5` 之期末·外部錨**）／期末 **`819`** ⇒ 增量 **`2`** ＝ **純新增** `.md` **`2`** | 自洽 | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py <本單> <本報告>`（受詞係 `index` ⇒ 復跑前先 `git add`） | 見 `七-4`（🛑 **首跑紅** ⇒ 修 ⇒ 復跑） | 命中 `0` ＝ `0` | 見 `七-4` |
| `6` | 自限復驗（見 `七-3`） | 全數如期 | —— | ✅ |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**[必為零]；`verify/W-G.9-269-p3a` 命中 **`1`**[必存在] | 同左 | ✅ |
| `6''` | 本波**八單**之在倉性（三態分列） | 計 **`8`**·**皆態1**；判別力[必為零] 人造路徑 ⇒ `rc = 128` | 皆態1 | ✅ |
| `6'''` | 🆕 **逐款回掃之出艙**（坑 `bm` 攔法 ②） | 見 `七-5`（**逐項**·⛔ 只報「已回掃」） | 逐項 | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** | —— | —— |

### 七-2　閘 `1` 之逐檔具名（刪除欄全 `0`）

| `commit` | 檔 | `+` | `-` |
|---|---|---|---|
| `e89bb569…` | `docs/orders/W-G.9-304_施工單_K-9-30之落地與已算之向之可得性.md` | `395` | `0` |
| `1e7e21f0…` | `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `25` | `0` |
| `a99af607…` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `58` | `0` |
| `118247a0…` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `21` | `0` |
| `4192d522…` | `verify/probes/probe_WG9269_pit_index.py` | `1` | `0` |
| `fb7f8f83…` | `docs/reports/W-G.4_泛用阻塞項登記表.md` | `25` | `0` |
| `ceaa747b…` | `verify/out/WG9304R_dynamic.log` ／ `WG9304R_static.log` ／ `verify/probes/probe_WG9304R_dynamic.py` ／ `probe_WG9304R_static.py` | `1157`／`216`／`225`／`304` | `0`（四檔皆） |
| `bcff8f73…` | `docs/reports/W-G.9-304R_K-9-30與已算之向之可得性_執行報告.md` | `404` | `0` |
| （本段之 `commit`） | 同上（末端追加） | 見 `七-6` | `0` |

### 七-3　閘 `6` 自限復驗（逐項）

| 受詞 | 產生指令 | 實得 | 期 | 判 |
|---|---|---|---|---|
| `34` 檔 blob 期初＝期末 | 逐檔 `git rev-parse <基座>:<f>` vs `HEAD:<f>` | **相異 `0`** ⇒ `[]` | `0` | ✅ |
| 判別力[必相異] | 本批確有異動之檔 | **`10`** 個 | ≥ `1` | ✅ |
| `app.py` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | 同左 | ✅ |
| `baselines` | `git ls-tree -r HEAD verify/baselines \| sha256sum` | `a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | 同左 | ✅ |
| `baselines` 檔數 | `git ls-tree -r --name-only -z HEAD verify/baselines` | **`298`** | `298` | ✅ |
| `p3a` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` | `1` | ✅ |
| **`WV_BAKE`** | `os.environ.get("WV_BAKE")` | **`None`** | `None` | ✅ |
| `GB-79`／`167`／`168`／`169`／`170` | `GB` 簿之器輸出（`168`／`170`·缺 `[12,87]`） | **⛔ 修** | ⛔ 修 | ✅ |
| `K-9-5-*`／`K-9-12`／`K-9-29` 之子項 | 生產碼 `34` 檔相異 `0` | **⛔ 落地** | ⛔ 落地 | ✅ |
| 🔒 **`K-9-30` 亦⬜ 未實作** | 同上（生產碼一字未改即其證） | ⬜ **未實作** | ⬜ | ✅ |
| 物證落檔逐位未動 | 先 `git cat-file -e` ⇒ `git rev-parse`（框 ＝ blob `sha1`） | **`17`** 檔·三態分列：態1 **`17`**／態2 `0`／態3 `0` | 逐位未動 | ✅ |

### 七-4　閘 `5'`　`acceptance_audit`（🛑 **首跑紅** ⇒ 修 ⇒ 復跑·⛔ 頂替）

**首跑**（報告 `commit` 後·**收工閘段尚未追加**）：
`【驗收】` 段 ＝ `:386`–`:392`｜判定組（反引號 token）**`33`** 個／參考組（CJK 詞）**`3`** 個
⇒ 🔴 **判定組之報告命中 `0` ＝ `1` 個**：``WV_BAKE``（報告 **`0`** ／ 單本身 **`3`**）·`rc = 1`。
🔒 **成因**：`WV_BAKE` 之實測值（`None`）係**收工閘 `6`** 之受詞，而該段係**入倉後**方追加
⇒ 首跑時其字樣尚未進入報告。**⛔ 器之缺陷、⛔ 受詞紅。**
**處置**：以本段（`七-3`）出艙 `WV_BAKE` ＝ `None` 後**復跑**（先 `git add`）。
**復跑之實得**：見 `七-6` 之末列（🛑 **先跑再填**）。

### 七-5　閘 `6'''`　**逐款回掃**（坑 `bm` 攔法 ②·⛔ 只報「已回掃」）

🔒 **本報告內所出現之「某框／某法有 X 缺陷」之警告**，逐項及其對**後續各款**之回掃結果：

| # | 警告（逐字要旨·出處） | 回掃之受詞 | **回掃結果** |
|---|---|---|---|
| `W1` | **`grep` 框經 shell 引號失真 ⇒ 偽 `0`**（自捕 `1`） | 本報告**全部**計數之產生方式 | ✅ 後續計數**一律**改以 **Python 直讀**（反引號執行期組出）或 **`argv` 直呼**，⛔ 經 shell；閘 `5` 之坑計數、`§三`／`§四`／`§五` 之框計數皆然 |
| `W2` | **子行程 `stdout` 之 Windows 文字層 ⇒ CRLF**（自捕 `2`） | 本批**全部**落檔 | ✅ 閘 `3`（`8` 檔 `.md`／`.py`）與 `3'`（`2` 檔 `.log`）**逐檔** `CR = 0`；二器皆已 `reconfigure(…, newline="\n")` |
| `W3` | **標籤與量測不符**（宣稱「函式內」而實測「全檔」·自捕 `3`） | 本報告**全部**「是否在 X 內／是否為 X」型之欄 | 🔴 **回掃捕獲 `1` 例** ⇒ **自捕 `5`**（閘 `5` 之「本批**新增**之 `.md`」實以 `git diff --name-only`＝**異動**量得 `5`）；已改 `--diff-filter=A` 重算 ⇒ `2` ⇒ 自洽 `True` |
| `W4` | **判別力造不具鑑別力**（其造結構上不可能非零·自捕 `4`） | 本報告**全部**[必非零]造 | ✅ 逐項實得皆非零：`§三` 框① `1`／`§五` 偵測器 `211`／`§六` 合成對照 `2`／`3`造 `data/V6.dxf` `12308`／`2`造 `2` 行／`6` 造 `10` 檔 |
| `W5` | **單所列之「十三形」⛔ 窮舉**（`§六-1`·其直證 ＝ 十三形外者 `5` 個） | `§六` 之後續款（`2`／`3`／`4`）之框 | ✅ 款 `2` 以 `Subscript`-Store **全量** ＋ `Return` 所含之名判、款 `3` 以 **AST 位序實參** ＋ `f_locals` **多鍵並取**、款 `4` 以**執行序號**——**皆⛔ 有限形清單** |
| `W6` | **`Return` 形零命中係框之口徑所致**（`§六-1` 之 ⚠️） | 該值是否確經 `Return` 出艙 | ✅ 已具名：其為 `Tuple.elts`（`return _r, '幾何二分法'`）⇒ **實質上確經 `Return`**；款 `2` 之「回傳值」判即據此 |

### 七-6　🩸 **自捕 `5`**（本段新增·⛔ 追改上文之「自捕 `4` 則」一字 ⇒ **本批自捕實為 `5` 則**）

| 項 | 內容 |
|---|---|
| 形 | 🔴 **框之標籤與量測不符（同 `W3` 之族·第二例）** |
| 徵候 | 收工閘 `5` 之自洽檢：`.md` 增量 ＝ **`2`**，而「本批新增之 `.md`」量得 **`5`** ⇒ 自洽 `False` |
| 成因 | 我以 `git diff --name-only`（＝**異動**·含 `M` 修改）量「**新增**」；其 `5` ＝ 純新增 `2` ＋ 修改 `3`（`W-G.4` 登記表／自誤簿／`K-6`） |
| 處置 | 改以 `git diff --name-only --diff-filter=A` 重算 ⇒ 純新增 `.md` ＝ **`2`** ⇒ **自洽 `True`**；併報 `--diff-filter=M` ＝ **`4`** 檔（含 `probe_WG9269_pit_index.py`），且 `A ∪ M` ＝ **`10`** ＝ `--name-only` 之數（**自洽驗算**） |
| 攔下它的 | **閘 `5` 之自洽檢本身** ＋ **`W3` 之逐款回掃**（坑 `bm` 攔法 ②·其首次施行即有斬獲） |

🛑 **⛔ 讀為「本批無誤」**——本批自捕合計 **`6`** 則（第 `6` 則見 `七-7`），
**全為 CC 自身量測器／落檔／撰寫順序之缺陷**，皆於出艙前攔下。

**閘 `5'` 之復跑（🛑 本段追加並 `git add` 後實跑·**先跑再填**）**：
判定組（反引號 token）**`33`** 個／參考組 **`3`** 個
⇒ 🔒 **判定組之報告命中 `0` ＝ `0` 個**（逐字「✅ 判定組全數命中」）·`rc = 0` ✅
（參考組命中 `0` ＝ `1` 個·**⛔ 作判**·係單之行文片段）。
🔒 其受詞之報告 bytes ＝ **`45006`**（＝ `git add` 當時之 index 態）；`【驗收】` 段 ＝ `:386`–`:392`。

### 七-7　🩸 **自捕 `6`**（坑 `bm` 之**第三例**·由 CC 親犯並於 `commit` 前自捕）

| 項 | 內容 |
|---|---|
| 形 | 🔴 **禁令之文字與它所禁之行為，出現在同一個撰寫動作裡**——即**本批所鑄之坑 `bm`** |
| 徵候 | 本段（`七-6` 末）之初稿**直接寫出**閘 `5'` 復跑之三數（判定組／命中 `0` 者／`rc`），而該指令**當時尚未執行**；而**同段之上一行**正是「🛑 本段追加並 `git add` 後實跑·**先跑再填**」 |
| 成因 | 與 `自誤 411` ② 同根：**寫戒之當下最有信心**。坑 `bm` 之根因逐字：「立禁之時，人正處於**對該禁最有信心**之狀態」 |
| 處置 | 於 `commit` **前**自捕 ⇒ 依坑 `bm` 攔法 ①**改行機械順序**：① 先落**佔位符**（`〈待填-A〉`〜`〈待填-E〉`·其字面**⛔ 為任何數**）② `git add` ③ 實跑 ④ 以其輸出填之 |
| 🔑 併記 | **坑 `bm` 之首次施行即捕獲其自身之第三例** ——`W-G.9-303 補令五` 為①、`W-G.9-303` 施工單 `§三`→`§四` 為②、**本例為③**；三者皆**同一撰寫動作**內 |

---

## `§八`　🛑 `⛔ 為之事` 之逐項自檢

| # | 受詞 | 判 | 證 |
|---|---|---|---|
| `1` | ⛔ 改生產碼 `34` 檔**一字** | ✅ **未為** | 逐區間生產碼判法皆 `0` 行（對照組 `2`）；`34` 檔 blob 期初＝期末**相異 `0`** |
| `2` | ⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 辦修批；⛔ 自續 `W-G.9-305` | ✅ **未為** | `refs/heads/verify/*` 之集合未變（`20` 支·`gb170` `0`）；本報告止於 `W-G.9-304R` |
| `3` | ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**任一物證落檔**；⛔ `WV_BAKE`；⛔ 引任何自 `baseline` 取得之數 | ✅ **未為** | `p3a` `rc = 1`；`baselines` `298`·`a898f4e1…`；物證 `17` 檔態1·逐位未動；`WV_BAKE` ＝ `None`（二器皆以 `assert` 自證）；本報告未引任何 baseline 之數 |
| `4` | ⛔ 改任何函式簽章／新增形參／新增 `import` 於生產碼／改任何 `*_EXPECT` | ✅ **未為** | 生產碼零異動（同 `1`） |
| `5` | ⛔ 自寫第二套幾何或第二份 `W`／`side_mid`／側界線／街廓識別之定義（`GB-48` 族） | ✅ **未為** | 靜態器只讀 AST；動態器只**包裝**既有函式、⛔ 重算任何幾何（`side_mid` 之值未經本批任何重算） |
| `6` | ⛔ 動 `probe_WG9269_pit_index.py` 之**任何既有列**（僅准新增一列）；⛔ 動任何**其他既有**探針一字 | ✅ **未為** | `numstat` ＝ `1` 增／`0` 刪；舊列於新檔**按序全數存在**（未對上 `0`）；其餘既有探針之 `git diff` 輸出空 |
| `7` | ⛔ 鑄 `GB`／`VR` 之任何號 | ✅ **未為** | `GB` `168`／`170`·`VR` `80`／`95` **與開工閘逐格相同** |
| `8` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-30`／`K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ **未為** | `GB` 三形聯集之相異／MAX／缺號**皆未變**；生產碼零異動 |
| `9` | ⛔ 修 `W-G.9-303R6` 之四份 `.log` | ✅ **未為** | 其 blob 於閘 `11`／`6` 之三態分列中**逐位未動**（態1） |
| `10` | ⛔ 追改本波八單／二交接註／`W-G.9-303R`／`-303R4`／`-303R6` 或任何已入倉文件一字 | ✅ **未為** | 九 `commit` 之 `deletions` 全 `0`；三處末端追加皆經 `append_audit`（`77` 筆·✅ 全部成立）＋ 三造 |
| `11` | ⛔ 擴 KL 放行之射程；⛔ 呈 KL；⛔ 擬第二次放行之呈文 | ✅ **未為** | 本報告**⛔ 呈 KL** |
| `12` | ⛔ 判 `GB-170` 可否解除／路丙落點可施與否／`停二`·`停四` 為誤／擬 fallback 之實作形／判孰是孰誤／提修法主張 | ✅ **未為** | `§六-5` 僅出艙二停機款之**成就與否**；`§六-3` 末明載「⛔ 由此判其可施或不可施」 |
| `13` | **⛔ 估算或出艙本窗 context 之百分比** | ✅ **未為** | 本報告無任何百分比；亦未遭遇資源硬限之可觀測事件 |
| `14` | ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述） | ✅ **未為** | 收工閘之值**皆於報告入倉後量**，以**末端追加**落地；閘 `7`（`push` 後）**⛔ 入倉**、只出艙於回報 |

---

## `§九`　逐 `commit`（**全 `40` 碼**·本段補入第 `9` 列）

| # | `commit` | 內容 | `deletions` |
|---|---|---|---|
| `1` | `e89bb56973f0a6b4ebb4778d9261e8786fddbc08` | `§二` 工項零：本單原封入倉 | `0` |
| `2` | `1e7e21f00c47d06f6d574a249813bf191ae97b0f` | `§三` 工項一：`K-9-30` 之鑄 | `0` |
| `3` | `a99af6072b26718961b861eac6f9fa03062a5d17` | `§四` 段一：`自誤 411`・`412` | `0` |
| `4` | `118247a0d2ff8399baf9385b920c66789eccf72a` | `§四` 段二：坑 `bm` | `0` |
| `5` | `4192d5228d805673faa7366ce55a11a097af57f4` | `§四` 段三：`ALPHA_LABELS` 擴至 `bm` | `0` |
| `6` | `fb7f8f83ac697bb5ede059bcf32bcbf0d746e0e1` | `§五` 工項三：`GB-170` 末端追加 | `0` |
| `7` | `ceaa747bd750ca91a09aa92fd6de53e22ae16311` | `§六` 工項四：二量測器與二落檔 | `0` |
| `8` | `bcff8f7387b171a7219a87b5797f4bf5de4ec369` | 執行報告入倉 | `0` |
| `9` | （本段之 `commit`·收工閘末端追加） | 收工閘之實測 ＋ 自捕 `5` | `0` |

🛑 **辦畢 ⇒ 出報告 ⇒ 停。⛔ 呈 KL。**
