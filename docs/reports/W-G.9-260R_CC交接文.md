# `W-G.9-260R`　CC 新 session 交接文（`2026-09-09`）

> 🔑 **開窗前四件事見 `§八`**。本文一切數字**現查於倉**；⛔ 採本文之數——**每一節皆須重跑**。
> 🛑 本文 **⛔ 鑄任何號**。取號依 `常規四（三）` ⇒ `W-G.9-260R`（同號多產物之既例 ＝ `-246R2` 之二份；
> 另一產物 ＝ `W-G.9-260R_telescoping定位.md`）。⛔ 取 `-261` 以後（未來單號）。
> 🔒 **`常規九`（倉態宣告）**：撰本文時**⛔ 有未 push 之 commit**；工作區 `git status --porcelain` ＝ `0` 列。

---

## §一　態錨（**開窗第一動作 ＝ 重跑本節**）

| 項 | 值（現查 `2026-09-09`）|
|---|---|
| 主線 `origin/wip/s1-endpart` | **`cd3e809d9a1c…`**（`ls-remote` 現查·全 40 碼須自驗）|
| 🟡 **遠端 `verify/*` 中<u>非祖先</u>者 `2` 支** | `verify/W-G.9-244-c1`（`6b5fe71`）／`verify/W-G.9-255-c1`（`3242e96`）——**二者之<u>內容</u>皆已併線**（皆走 `cherry-pick` ⇒ commit 物件非祖先）。🔑 **「已併」之受詞係<u>內容</u>，⛔ `--is-ancestor`** |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/orders/` | **1706** ／ **629** ／ **607** ／ **72** 檔 |
| `CLAUDE.md` | **`7c25e981e5004fbe`**／**196541** B／**2399** LF |
| 自誤簿 ／ `GB` 簿 LF | **7687** ／ **5274** |
| 本地 `keep/*` | **12** 支（含 `keep/W-G.9-255-c1` ＝ `3242e96`）|

**生產路徑十檔 ＋ 夾具之 `sha256[:16]`**

| 檔 | 前 16 位 | 檔 | 前 16 位 |
|---|---|---|---|
| `app.py` | **`4860f10828fb8ae9`** | `verify/wf_f0.py` | 🆕 **`b0722355c1978635`** |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/run_verification.py` | `d0d531f7dc051b65` | `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/stepg_pipeline.py` | **`c8d46d2e5fb79dc5`** | `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/run_all.py` | `4ba89fef90979491` | `verify/wf_f4.py` | `b36aeca6c4f6b364` |
| 夾具 `verify/fixture_wf_ns_wiring.py` | `b17826a6f7b98bf5` | | |

🔑 **`wf_f0.py` 已變**（`c1` 併線·`K-9-29 三`／`十` 之 `a′` 折算 ＋ 廢 `停機#2`）。

---

## §二　次號（**現查·⛔ 採本文之數·開工前重算**）

| 簿 | 框／母體 | `MAX` | 次號 |
|---|---|---|---|
| 自誤 | `VR-091 四` 定義框＋範圍框·登記表**單檔** | **`321`** | **`322`** |
| `GB` | 標題錨定框 `^#{2,4} .?GB-N`·`W-G.4` **單檔** | **`154`** | **`GB-155`** |
| `VR` | `^## .?VR-N`·`驗證裁定登記表` 單檔·🛑 **須扣哨兵 `999`** | **`091`** | **`VR-092`** |
| `K-9` | `^### 🔒 K-9-N`·`K-6` 單檔 | **`29`** | **`K-9-30`** |
| 戒 | `W-G.9-188 裁 B` 新式·全倉 `.md` | **`40`** | **戒 `41`** |

🩸 **本窗踩過之取號陷阱**：① `VR` 之裸 `MAX` 得 **`999`**（哨兵）⇒ **必扣**（`W-G.9-183` 補款）；
② `grep -oE` 之單引號內寫 `` \` `` ＝ **字面反斜線＋反引號** ⇒ ERE 失效、三框同時得**空**（`GB`／`VR`／戒）；
③ `.` 於 `grep -E` 之 byte 模式**⛔ 匹配多位元組 emoji** ⇒ `^#{2,4} .?GB-N` 對 `## 🆕 \`GB-N\`` **失配**。

---

## §三　工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——前置 ＝ `git ls-remote origin wip/s1-endpart` 已含該 commit（⛔ `origin/main`）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**；`--selftest` 綠亦⛔ 蘊含閘已生效（須**活體實測**）。
3. 🔒 **倉內報告 ＝ 唯一本體**；push 後**單獨跑** `git log -1 --format="%h %s"`、原樣貼 ＋ 倉內路徑。
4. 🔒 **工項零優先於一切**——收單後 ① 查宣告框占用（三數分列 ＋ **雙屬**）→ ② 原封入倉（⛔ 顛倒）。
5. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（經發單側）。
6. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 `deletions ＝ 0`。
7. 🔒 **常規補款一**：閘之量測與其所守護之動作（`commit`／`push`）**⛔ 置於同一命令塊**。
8. 🔒 **零生產碼 commit 逕推**；**生產碼 commit 走側分支**（`app.py`／`stepg_pipeline`／`run_all`／`run_verification`；
   `wf_f0.py` 於 `-255 c1` 已循此例）。
9. 🔒 **`常規四（九）二`**：新號之標題形須**可被該類定義列式命中**；鑄號前取**既有最末一則**逐字為範本。
   🩸 本窗因此攔下一次（`GB-154` 採單所給之形而失配）。
10. 🔒 **長跑一律於拋棄式 clone**（自 GitHub `fetch`·**目錄名同長度**·跑畢**整份刪除**·`GB-94`）。

### 🔴 生產碼 commit 之流程（本窗已一度施行·`c1`）

```
報告先行入倉主線（零生產碼·逕推）→ 生產碼本地 commit → ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c<n> ＋ 本地保全 keep/<單號>-c<n>
  → 本地回退主線 → 發單側逐位復驗 → 呈 KL 一句實務語言 → KL 放行
  → 併線（merge --no-ff 或 cherry-pick·**擇一並具名**·⛔ rebase 主線既有 commit）→ 併線後復閘
```
🩸 **本窗之偏離**：`cherry-pick` **沿用側分支原 message** ⇒ 未依單所令載單號／放行日／放行句。
**已推者⛔ 追改** ⇒ 補記於 `W-G.9-258R.md §三`。**通則**：對 message 有要求者，須以 `-e`／`--no-commit` 或**推之前** `amend`。

---

## §四　現行之閘與框 ＋ **本窗新捕之陷阱**

| 閘 | 母體／框 | 現況 |
|---|---|---|
| 閘 `P` | 十檔＋夾具之 `sha256` | 零生產碼批須前後逐位相同 |
| 號占用（**宣告框**）| 全 `docs/`；`D1` 檔名 ∪ `D2` 列首 `#+` ∪ `D3` 自稱形；**`⑦` `列框 ＝ |D2 ∪ D3|`** | 三數分列 ＋ **雙屬列數** ＋ 讀不到之副檔名分布 |
| `S-0b` | `probe_order_preflight.py <單>` | `P-3`／`P-5` 停機款；🟡 提示**須逐項具名處置** |
| `PreToolUse` heredoc 硬閘 | `.claude/settings.json` ＋ `wg9237_heredoc_guard.py` | 🔴 **其生效繫於 worktree 之態**（`GB-152`）|

### 🆕 本窗新捕（**逐條付過學費**）

| # | 受詞 |
|---|---|
| `1` | 🔴 **硬閘之生效係<u>條件式</u>**（`GB-152`）——worktree 開在 `main` 時閘之二檔**結構上不存在**（活體實測「未生效」**當時為真**）；`git switch` 至 wip 後**即生效**。⛔ 以開窗時之實測外推全程 |
| `2` | 🔴 **`--is-ancestor` ⛔ 是「已併」之框**——`cherry-pick` 併線者 commit 物件非祖先而**內容已併**。給證 ＝ `sha256(git diff <父> <commit>)` 二端相等 |
| `3` | 🔴 **自指增長之<u>遞迴</u>**（`GB-132` 族·本窗**三度**）——① 自檢句列舉受檢字樣致自己命中（`V-P6`）；② 修正自指陳述時**又製造新的自指**（增量誤判為 `1` 而實為 `2`）。🔒 **通則**：出艙「落地後之命中數」前須**先對 payload 自身逐錨計數**；凡以「某字樣命中 ＝ `0`」為閘者，**該字樣⛔ 逐字出現於受檢文件，含自檢句自身** |
| `4` | 🔴 **CRLF vs LF**——工作區 `wc -c`／`sha256sum` 之值 ⛔ 倉側 blob（`app.py` 工作區 `05df406b…`／blob `4860f108…`）。**量倉檔一律 `git cat-file blob`** |
| `5` | 🔴 **Git Bash 路徑 `/c/…` 於 `python -c` 之<u>引號內</u>⛔ 被 MSYS 轉換**（作為命令列參數時則會轉）⇒ `FileNotFoundError`。**Python 內一律用 `C:/…`** |
| `6` | 🔴 **`ziwu_num.py` 等器讀 index 須設 `GIT_INDEX_FILE`**——否則於 worktree 中讀到**主 checkout 之 index**（本窗得 `MAX 311` 而真值 `320`）。**改以明確 rev 為錨最穩** |
| `7` | 🔴 **`push origin HEAD:refs/…` ⛔ 更新本地分支 ref** ⇒ 自本地 `clone` 會得**舊態**。clone 後須自 GitHub `fetch` 並 `checkout FETCH_HEAD` |
| `8` | 🔴 **靶自檢**——`V-G′` 首版把標的宗之 `a` 放進 `分攤登記面積_m2`，而碼係 `面積_m2 += add`（前者**唯讀·⛔ 入累加基底**）⇒ 初判「器無鑑別力」，**實為靶錯** |
| `9` | 🔴 **相減二數須同容器**——`-257R` 之「相異 `8`」係 CC 之閘關實測減**他窗**之閘開實測 ⇒ `proxy` 代 `property`（已由 `GB-153` 更正為 `0`）|
| `10` | 🔑 **母體之組數須具名<u>去重鍵</u>**——`60`（序對去重）／`47`（僅 `a` 值）／`57`（單檔列數）三者皆自同一批 CSV |
| `11` | 🔑 **欲證某量「不可得」，框須為<u>受詞之可達性</u>**，⛔ 某一**變數名**之字面命中（`pre_price_by_zone` vs 快照鍵 `重劃前區段_面積單價`）|
| `12` | 🔑 **行號一律 `grep -n` 機械取**，⛔ 自 `sed` 輸出目測推算（本窗因此誤報「單載行號差 `2` 列」）|
| `13` | 🔑 **受令執行某閘時，排程之前先問「此閘之產生路徑今日跑得動嗎」**（`自誤 319`／`321` 之族）|

---

## §五　已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 項 | 現況 |
|---|---|
| `GB-67`（`②-宗 圍堵閘`·`app.py`）| **未解**·`R2` 宗-宗重疊 `45.9766 > 2.8906`。🆕 **原受詞「不相鄰之宗」係誤框**（四宗累積S `4.06`／`4.81`／`5.46`／`12.0` ⇒ **推進序上相鄰**）；重疊 **`100%` 集中於 `628-41(1)` 之二對**；**破非恆定**（`3.5m` 得 `0.0000`）|
| **`GB-154`**（`telescoping`·`stepg_pipeline`）| 🆕 **本窗鑄**·**獨立且充分之阻斷點**。🆕 **二情境二機制皆已定位**（見下）|
| `GB-153` | `F.0`–`F.4` 全世代之逐值迴歸不可得。🆕 歸因更正：**`②-宗` 停用之相異 ＝ `0`（恆等）**；`telescoping` 獨立充分 ⇒ **只解 `GB-67` ⛔ 能使 `R3`–`R6` 可達** |

### 🆕 `telescoping` 之成因（`W-G.9-260R_telescoping定位.md`·**⛔ 修**）

| 情境 | `Δ` | 機制 | 源頭 |
|---|---|---|---|
| `3.5m` | `55.606` | **首宗之 `Rw` 自 `W=0` 起算，而閘期望自 `W₀=6.98` 起算** ⇒ 差 ＝ `R(W₀)`（`55.586`·殘差 `0.020` 為 `2dp` 捨入）| 鏈首宗 `628-42(1)`；`W₀` 之捕獲式 ＝ `res['W_near']`（`stepg:837`）|
| `0m` | `6.160` | **`W` 序列非單調**（`4.06 → 3.11`）⇒ 累積差額制之前提破 ⇒ `R(3.11)→R(3.75)` 區段被計二次 | **`628-41(1)`**（＝ `②-宗` 之重疊源頭）|

🔒 **`3.5m` 之「鏈逐界面全未斷而總和仍差」之詭已解**——其差**不在界面之間**，而在**鏈首宗自身之起算點**。

---

## §六　待辦／待裁

| # | 項 | 狀態 |
|---|---|---|
| `1` | 🔴 **`GB-67` 之修** | 候 KL 裁（`-257R` 判 ③ **尚不能判**·尚差三項證據：`宗0` 之 `s` 上界式與其輸入實值／`kept_iv` 只落 `k=0` 之成因／`②-宗` 逐次 `biz_x` 之情境標籤）|
| `2` | 🔴 **`telescoping` 之修** | 成因**已定位**（`§五`）·候 KL 裁。⛔ 於診斷單內開修 |
| `3` | 🔴 **`c2`（廢步驟 0·`app.py:11633` `k6_step0_enabled()` `on→off`）** | **⛔ 開工**（`-256 §六`／`-258 §六` 明令）——其 `V-H` 由 `GB-153` 判為不可得，**放行證據形須發單側另議並候 KL** |
| `4` | ⬜ **`-253` 段乙（`-251` `c1`／`c2`·純 UI）** | 原令全效·可並行·`V-K` 候 KL 目檢 |
| `5` | ⬜ **`-249` 段三觀測（`V-S` 表）** | 依 `-254 §三` 之裁**延後**至 `K-9-29` 碼側 `c2` 併線後以落地後母體重跑 |
| `6` | ⬜ **`K-9-29` 碼側落地之餘項** | `c1` 已併（`a′`＋廢 `停機#2`）；**`c2`（廢步驟 0）／UI 步驟 D 逐筆／`K-9-29 六` 入池閘之五級層**未辦·🔴 **有土地後果**（`R4` `226.01㎡`）|
| `7` | ⬜ `a′` 模式二（`p_average`）／五級（`r1`–`r8`）／通用化波 | 未起 |

---

## §七　本窗摘要（`-251R` 交接 → `-260R`）

| 批 | 要旨 | 落點 |
|---|---|---|
| `-249R` | 段甲（鑄 `自誤 313`）／段乙 plan → reviewer **六項 `BLOCKED`** → CC 逐項獨立復現全成立 → **停機** | `c608b10`／`bb9b429`／`bf5980a` |
| `-251R` | KL 裁**丁案**落於被裁報告檔尾 | `6ce7017` |
| `-253R` | `K-9-29` 入典（**廢步驟 0**·一切合併恆走 `a′`）＋ 宣告框 `⑦` ＋ `自誤 314`／`315` | `4e6f00f` |
| `-254R` | `自誤 316` ＋ `GB-152` ＋ `-249` 延後之裁；**`c1` 停機上呈**（三環）| `92e4244`／`70a676d` |
| `-255R` | 停機**撤回**（二環係 CC 之框錯·已具名撤回）＋ `K-9-29` 補款 `十`（跨區段合併准予·廢 `停機#2`）＋ `自誤 317`／`318` | `8a52dbd` |
| `-256R` | `V-G` 改令（逐值迴歸 → **代數恆等**）＋ `自誤 319` ＋ `GB-153` | `a5b4edd` |
| `-258R` | **`c1` 併線**（KL 放行·`cherry-pick`）＋ `自誤 320`；併線後七閘全綠 | `6b3999a`／`15f27b9`／`ff79584` |
| `-257R` | **`GB-67` 定位**（丙案·純診斷）——`Q-1`〜`Q-7`；原受詞係誤框 | `1531916` |
| `-260R` | **`telescoping` 定位**＋`自誤 321`＋`GB-153` 歸因更正（相異 `0`）＋**`GB-154`** | `8621958`／`cd3e809` |

🩸 **本窗「量測器紅而非受詞紅」逾十次**（逐條見 `§四`）；**受詞紅**僅二次（交接文之「列命中 `310`」係舊框／`GB-154` 標題形失配）。
🔑 **通則**：**出艙任何「不符」之前，先問「我和它是不是在量同一側」。**

---

## §八　開窗前四件事（**依序·⛔ 跳**）

1. **驗 `left-right`**：`git rev-list --left-right --count HEAD...origin/wip/s1-endpart`。
   **非 `0 0` 即工作區為舊態** ⇒ ⛔ 直接 `cat` 工作區之 `CLAUDE.md`／`app.py`；
   **harness 逐窗自 `main` 開 worktree ＝ 已多度重踩**。處置 ＝ `git switch --detach <wip rev>`。
2. **驗硬閘**：`git switch` **之後**再驗（`GB-152`）——`test -f .claude/settings.json` ＋ **活體實測**（送一含 `<<` 之無害命令）。
3. **讀本文 `§一`／`§二` 並<u>重跑</u>其每一數**（⛔ 採本文之值）。
4. **查新單**：🔑 **母體須含<u>主 checkout 之工作區</u>**（`C:\Users\admin\Desktop\land-readjustment-trial\docs\orders\`）
   ——本窗**五度**於該處發現單而倉內查無。併驗 `git ls-remote --heads origin 'refs/heads/verify/*'`（**內容判**·⛔ `--is-ancestor` 單獨判）。

🔒 **收單後首二步之順序**：① 查宣告框占用（三數分列 ＋ 雙屬）→ ② 原封入倉。**⛔ 顛倒。**
