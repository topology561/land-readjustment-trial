# W-G.9-234R　號占用閘之條款補正 ＋ 發單側二誤登記 ＋ 區外道路清單之現況勘查（執行報告）

**批次性質** ＝ **零生產碼**（生產碼五檔一位元組未動）。
一切數字錨取索引側 `git cat-file blob`（`GB-139 ②`）；`rc` 直取 `subprocess.run(...).returncode`（`GB-145`）；
⛔ heredoc（`GB-143`）；對照組字樣⛔ 落入本文（`GB-147`·以代稱行之）。
🔒 **單所載之一切數（`231`／`232`／`233`／`234`／`438`／`10`／`21`／`8.2`／`7.4`）皆<u>當場復算</u>**，⛔ 採單所載為錨。
🛑 本報告含**單側不符 `1` 處**（新增）與 **CC 側自陳 `2` 處**（器紅 `1`／違規 `1`）；本批**鑄二號**（`自誤 286`／`287`）。

---

## 工項零　✅ **PASS**

### `a`　號占用查（**本款之首次施行**）

🔒 **三軸（`VR-091 一`）**：來源 ＝ `blob@d36256185f0eb05fe83cc89cd78ee1f4c5412e8a`（索引側）／
母體 ＝ 追蹤 `.md` **564** 檔（**讀不到 `0`**）／粒度框 ＝ **檔框 ∪ 列框**。

| 號 | 檔框 | 列框 | 角色 |
|---|---|---|---|
| **`W-G.9-234`** | **0** | **0** | 受詢 ⇒ **未占用·可用** ✅ |
| `W-G.9-232` | 2 | 8 | 對照組 甲（已知占用·須 `≥ 1`）✅ |
| `W-G.9-231` | 4 | 13 | 對照組 甲2 ✅ |
| `W-G.9-233` | **1** | **1** | 旁證（見下）|
| `W-G.9-235` | 0 | 0 | 旁證（次一號）|
| 一人造號（字樣**⛔ 入倉**·`GB-147`·以代稱行之）| **0** | **0** | 對照組 乙（須 `= 0`）✅ |

🔴 **`W-G.9-233` 之 `1`／`1` 之來源**：其**唯一**命中處 ＝
`docs/reports/W-G.9-232R_v3-427更正標記與wf_f2容量過濾補查_執行報告.md` 之**占用查表**
（該表當時列其為 `0`／`0`）⇒ 🔒 **出艙「某號未占用」之行為本身，即使該號之占用數由 `0` 變為 `≥ 1`**
（自指增長·同 `GB-132` 族）。**⛔ 據此放寬本款之判準**；其處置已於工項二之補款內**標旗候發單側／KL**。

### `b`　本單原封入倉 ✅

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **6345** | 91 | **0** | `df29c14be2a0321ff6cafa82c148867933983680cfedea5c86168ff8b55b9b0d` |
| 入倉（blob `1ff4e64e…`）| **6345** | — | — | `df29c14be2a0321ff6cafa82c148867933983680cfedea5c86168ff8b55b9b0d` |

🔒 **逐位相同 ＝ `True`**（差 `+0`）；入倉 commit ＝ `ce19acc`；`deletions` ＝ **0**。
🛑 **射程界限**：只證「落檔 → 入倉」，**⛔ 證「聊天原文 → 落檔」**。

---

## 工項一　`W-G.9-232` 入倉單之號衝突標記 ✅

🔒 **既例之判（⛔ 假設·當場窮舉 `docs/orders/` 全檔首行）**：本倉之**單**採**首行標記**體例——
**掛註前**現存 **5** 則（`W-G.9-190`／`-213`／`-217`／`-219`／`-219R`；**掛註後 6** 則·含本批之 `-232`），其形為
`🗄️ 【作廢】…（…掛註·⛔ 改以下正文一字）` ＋ **空列** ＋ 原正文；
其上位依據 ＝ `CLAUDE.md`「`作業常規之追加四`」款 `三`（**作廢單於檔內首行標**·⛔ 改其正文一字）。
⇒ 🔒 **驗算方向為「掛前全檔 ＝ 掛後之<u>嚴格後綴</u>」**（⛔ 前綴——單已預留此分支）。

**所掛之標記行（逐字·`N` 已當場復算）**

> 🔀 【號衝突】本單原發時自載 W-G.9-231；該號於倉內已被占用（檔框 2／列框 7·當場復算·三軸 ＝ `blob@4fe2b448a26402463316710f0af3d1c470e830c6`／母體 追蹤 `.md` 562 檔／粒度框 檔框 ∪ 列框），依 KL 2026-08-03 裁改以 W-G.9-232 入倉，正文一字未改。（`W-G.9-234` 工項一掛註·⛔ 改以下正文一字）

🔒 **`N` ＝ 7 之復算**（三軸同上·**發單時**態 ＝ `blob@4fe2b44`·母體 追蹤 `.md` **562** 檔·讀不到 `0`）：
`W-G.9-231` ＝ 檔框 **2**／列框 **7**；占用者逐檔 ＝ `docs/reports/W-G.9-231_CC交接文.md`／
`docs/reports/W-G.9-231R_開窗態錨復驗與次號重算_執行報告.md`。
🔒 **對照組（同器同母體）**：器須綠 `W-G.9-230` ＝ **3** 檔 ✅；器須紅（人造號·代稱）＝ **0** 檔 ✅。
🔒 **單所載之「檔框 2」經復算<u>相符</u>**（⛔ 因其相符而免復算——本批係先算後對）。

### 驗算 ✅

| 項 | 值 |
|---|---|
| 掛前（倉側 `blob@ce19acc`）| **4405** B·`sha256` `5aceb836884ab510390be64dfa5110e93f720266e45489e3846326a05c60e253` |
| 掛後（工作樹）| **4778** B·`LF` **68**·`CR` **0** |
| payload（標記行 ＋ 空列）| **373** B ／ `Δbytes` ＝ **+373** ⇒ **二端同格並報·相等**（`戒 40`）✅ |
| **掛前全檔為掛後之嚴格後綴** | **`True`** ✅ |
| **正文區段 `sha256`** | `5aceb836884ab510390be64dfa5110e93f720266e45489e3846326a05c60e253` ⇒ **＝ 掛前全檔·逐位不變** ✅ |

---

## 工項二　`作業常規之追加四` 之補款：**入倉前之號占用閘** ✅

🔒 **落地處**（⛔ 新創檔）＝ `CLAUDE.md` **末端追加之新節**，其標題逐字：

> `## 🔧 \`作業常規之追加四\` 之補款：**入倉前之號占用閘**（\`W-G.9-234\` 工項二·⛔ 上文一字不刪·純末端追加）`

🔒 **受詞之定位**（`行號衛生` 第 `4` 款：`CLAUDE.md` 係**活文件**·⛔ 以行號為錨）
＝ 字樣錨 `作業常規之追加四：`（母體 `blob@ce19acc` 之 `CLAUDE.md`·**2006** 列·**列框**·命中 **1**）；
**判別力自檢**：寬字樣 `作業常規` 命中 **7** ⇒ 該檢**非恆為 `1`** ✅。

### 三款之逐款出艙（**逐字**）

> **一**　CC 收單後第一工項為**入倉前**，須先查該單號之**倉內占用**（**檔框 ∪ 列框**）；
> **已占即停並回報**，**⛔ 自行改號後續辦**（改號屬**意思決定**，歸發單側／KL）。
> **二**　該查核**須附對照組**（查一**已知占用**之號須 `≥ 1`），**⛔ 恆綠**。
> **三**　**發單側於出單前亦須自查倉內下一未占用號**；
> **⛔ 憑自身之序號記憶取號**（本則之根因）。

🔒 **併記於該節者**：① 其序**⛔ 母款 `一`〜`四` 之續編**（⛔ 讀為 `五`／`六`／`七`）；
② 與 `常規四（七）` 之關係 ＝ **純增補、⛔ 取代**（前者管**取號端**之「意指占用」、本款管**入倉端**之阻斷閘）；
③ 首次施行之實測表（＝ 工項零 `a`）；④ **自指增長**之事實登記 ＋ 其**標旗**（⛔ 由 CC 自裁）。

### 落地之機驗 ✅

| 項 | 值 |
|---|---|
| 基座（`blob@ce19acc` 之 `CLAUDE.md`）| **169005** B·`LF` **2006**·`sha256` `5ae6537a…` |
| 改後 | **172318** B·`LF` **2052**·`CR` **0**·`sha256` `a2d07eab…` |
| payload ／ `Δbytes` | **3313** B ／ **+3313** ⇒ 相等（`戒 40`）✅ |
| **嚴格前綴**（基座段逐位未動）| **`True`** ✅ ⇒ **⛔ 回改 `追加四` 既有四款一字** |

---

## 工項三　發單側二誤之登記（**分鑄**·⛔ 併）✅

🔒 **末號之獨立復算**（框 ＝ `VR-091` 補款二 ① 之**正字定義框 `A`** ＋ **範圍框**〔僅既有 `8` 條〕；
三軸 ＝ `blob@828f9734c56836e29384e14eeccf050e01cd95ac`／母體 該簿**單檔** **557438** B・**6294** 列／
粒度框 **列框**）：

| 量 | 改前 | 改後 |
|---|---|---|
| 相異〔集合基數〕 | **278** | **280** |
| `MAX` | **285** | **287** |
| **缺號清單** | **`[106]`** | **`[106]`** ✅ 符 `VR-091` 補款五之恆定期望值 |
| 範圍條數〔列框〕 | **8** | **8**（不變）|
| 三數同格併載（改後）| —— | 定義框列命中 **280** ／ 重咬 **3** ／ 純單筆 **277** |

🔒 **對照組（`常規五`）**：甲（真定義形）＝ **1** ✅；乙（括號引用形）＝ **0** ✅。
⇒ **`N1` ＝ `自誤 286`**、**`N2` ＝ `自誤 287`**。

### 查重之結果（`W-G.9-226` 工項三所令）

| 字樣（母體 ＝ 自誤簿單檔·列框）| 命中 | 判 |
|---|---|---|
| `號占用` | **2** | ⛔ 同受詞——二列皆為**他受詞**（`GB-125`／`VR-085` 之次號占用表射程論；`GB-135` 可取之零對照結論）|
| `未查號` ／ `出單而未查` | **0** ／ **0** | ⛔ |
| `pre_price` ／ `三處消費` | **0** ／ **0** | ⛔ |
| （對照組·器須綠）`發單側` | **395** | —— |

⇒ 🔒 **二受詞皆無既有號 ⇒ 依單之令分鑄二號**。

### `自誤 286`｜出單而未查號占用

**受詞**　`W-G.9-232`（單自載 `W-G.9-231`）於發單時該號已被倉內二檔占用（檔框 **2**／列框 **7**·三軸見工項一）。
**結構成因（當場復現）**　`作業常規之追加四` 四款之受詞**僅及原封入倉之保真**——**⛔ 有號占用閘**；
而該檢查**曾**存於單側：`docs/orders/W-G.9-217_201實算探針與Commit1期望值預登記.md` 之 `G0` 逐字
（於 `blob@ce19acc` 為 `:20`）＝ ` G0 grep docs/orders/，確認 217 未被占號；已占即停、回報。`
⇒ **條款化（`W-G.9-221` 工項三）時漏收**。

🔴 **本批新捕之併記（照實·⛔ 頂替）**：**縱使 `G0` 未被漏收，照其框亦攔不下本案**
——`G0` 之框逐字為 **`grep docs/orders/`**（**僅及 `docs/orders/`**），而本案二占用者**皆在 `docs/reports/`**。
⇒ 本批所補之款取「**倉內占用（檔框 ∪ 列框）**」而**⛔ 限於 `docs/orders/`**，係**強於** `G0`；
⛔ 讀為「回復 `G0`」。

**加重情節**　發單側自 `W-G.9-223` 起即**有**倉之存取權（`CLAUDE.md` 之
`🔧 三方分工鐵律 之補正之更正：發單側<u>有</u>倉之存取權` 節·其載該能力**經實測否證**其先前自陳）。
**推導式**（載於該則）　四款無閘 ∧ 檢查曾存於 `G0` ∧ 發單側有存取權 ∧ 發單時檔框 `2`／列框 `7`
⇒ **出單前之號占用查為可行且應為而未為** ⇒ 責 ＝ 發單側。

### `自誤 287`｜誤述碼側符號

**受詞**　單載「`pre_price` 三處消費全數退場」；碼側自陳所名者為 **`value`**。
🔒 **當場復算**（三軸 ＝ `blob@b7b059c3d2199f099b34156f3b3b4872426bd978`／
母體 `verify/wf_f4.py` **單檔** **100373** B・**1614** 列／粒度框 **列框**）：

| 符號 | **子字串框** | **詞邊界框** | 判 |
|---|---|---|---|
| `value` | **21** | **1** | 碼側自陳所名之退場者；作為**獨立符號**僅該一列（即自陳自身）|
| `pre_price` | **10** | —— | 🔴 **⛔ 退場**（現用輸入）|

🔒 二框須同格併載（`VR-091 一 (3)`）——`21` 係**子字串**命中（含 `values()`／`value=` 等），
**⛔ 讀為「該符號有 21 處」**。**單所載之 `21`／`10` 經復算相符**（其框 ＝ 子字串框）。
🔒 **對照組**：器須綠 `def ` ＝ **50** ✅；器須紅（必不存在之符號·代稱）＝ **0** ✅。
**族之具名**　與 `自誤 283` **同族**（引用他人之出艙而略去限定語），🔴 **受詞由「數」擴及「符號名」**。
**推導式**（載於該則）　碼側自陳逐字所名者為 `value` ∧ `pre_price` 為現用輸入 ∧ 二受詞於本簿既有命中皆 `0`
⇒ 單所載之符號名與倉態相異 ⇒ 依「與倉態衝突**以倉為準**」⇒ 責 ＝ 發單側。

### 鑄號之收工閘（`常規四（九）二`）✅ **二項皆過**

| 項 | 實測 |
|---|---|
| ① **形之自證**（定義框掃 payload）| 命中 ＝ `[286, 287]` ✅（標題形逐字取既有最末一則 `## 自誤 \`285\`　…` 為範本·`常規四（九）四`）|
| ② **`MAX` 之推進** | `285 → 287` ✅；`286`／`287` **皆⛔ 在缺號集** ✅；缺號恆 `[106]` ✅ |

### 併：CC 側自捕之器紅之**查重** ⇒ **⛔ 重複鑄**

| 受詞（字樣）| 於 `W-G.9-232R` 之命中（列框）| 判 |
|---|---|---|
| `append-only 型宣告`（`v3` 偽否定宣稱）| **2** | ✅ **已登記** ⇒ ⛔ 重複鑄 |
| `內文不改`（`v3:6` 逐字）| **3** | ✅ 已登記 |
| `W-G.9-999`（零對照誤取）| **2** | ✅ 已登記 |
| `結構性全盲`（守恆閘假說）| **2** | ✅ 已登記 |

🔒 **未登記者之補鑄** ＝ **無**——本批新捕之 CC 側器紅（見「附」節）係**本批**之事、
其登記處為本報告，**⛔ 入自誤簿**（該簿之受詞為**發單側**之錯·其檔首逐字「⛔ 非 CC 之錯」）。

---

## 工項四　區外道路清單之現況勘查（⛔ 設計·⛔ 實作）

🔒 母體 ＝ `app.py`（`blob@02cc6d7313789149ba852f997d2d8051a6cefaa0`·**1417946** B／**23942** 列·**列框**）；
行號繫 `commit ce19acc`。

### `a`　「逐街廓輸入」型欄位之完整體例（`front_width` 為範本）

| 環節 | 逐字／值 |
|---|---|
| **宣告** | `fw = ca1.number_input(` ／ `"正面路寬 (m)", min_value=0.0,` ／ `value=float(r.get('front_width', 8.0)), step=0.5,` ／ `key=f"f3_fw_{bid}"`（字樣錨 `"正面路寬 (m)", min_value=0.0,`·命中 **1**·於 `ce19acc` 為 `:19355`；呼叫塊 `:19354`–`:19358`）|
| **鍵名規則** | widget key ＝ `f"f3_fw_{bid}"`；`bid = b['id']`（字樣錨 `bid = b['id']`·命中 **1**）⇒ **逐街廓** |
| **容器** | `for b in build_blocks` 之迴圈內、且置於一 `st.form` 中 |
| **寫入 `session_state` 之形** | `SS_ROAD = 'f3_block_road'`（字樣錨·命中 **1**）；`new_roads[bid] = {'front_width': fw, …}` → `st.session_state[SS_ROAD].update(new_roads)`（字樣錨 `儲存路寬資料`）⇒ 🔒 **表單提交式**（⛔ 即時 `on_change`）|
| **打包出口** | `road_data.append({... 'front_width': r.get('front_width', 0.0), ...})`（字樣錨 `road_data.append(`·命中 **1**）|
| **初始化預設** | `'front_width': 8.0,`（字樣錨·命中 **1**）|

🔒 **`front_width` 之全部命中 ＝ 5 列（正面列舉·列框）**：
`:4827`（負擔計算之消費）／`:17698`（初始化預設）／`:19356`（UI 欄之 `value=`）／
`:19434`（寫回 `new_roads`）／`:19520`（打包 `road_data`）。

🛑 **關於「新增一個逐街廓欄位需動之最小處集」**：上開 **5** 列係 `front_width` **現存**之觸點集
（**事實**）。🔴 **「新欄位是否須觸及全部 5 處」屬<u>設計</u>** ⇒ **⛔ 由 CC 判**（單之禁項）；
本節僅出艙**既有體例之觸點**，⛔ 推之為新欄位之最小處集。

### `b`　「使用者建立之清單／字典」型 UI 之既例 ⇒ 🔴 **【查無】**

🔒 **窮舉（列框·先窮舉再斷言）**：`st.data_editor` **4**／`num_rows=` **3**／
`st.experimental_data_editor` **0**／`dynamic` **0**／`新增一列` **0**／`新增項` **0**／`➕` **0**。
🔒 **四處 `data_editor` 逐處查其 `num_rows`**：`:17285`／`:17383`／`:17443` **皆 `num_rows="fixed"`**；
`:20912` **未給 `num_rows`**（Streamlit 預設即 `"fixed"`）且其註逐字「**data_editor 純為「顯示鏡像」**」；
四者之欄多為 `disabled=True`。
⇒ 🔒 **倉內⛔ 有「可新增項、逐街廓指派」型 UI 之既例**。
🔒 **對照組**：器須綠 `st.number_input` ＝ **7** ✅；器須紅（人造 widget 名·代稱）＝ **0** ✅。
🛑 **「區外道路清單應沿既例抑或新創體例」＝ 意思決定 ⇒ 候發單側／KL**；本節⛔ 提名。

### `c`　`FRONT_LINE` 之幾何推導現況

🔒 **`result['front_lines'][label]` 之完整欄位集（逐字·於 `ce19acc` 為 `:1464`–`:1469`）**

```
        result['front_lines'][_w] = {
            'p1': (_fr['pts'][0][0], _fr['pts'][0][1]),
            'p2': (_fr['pts'][-1][0], _fr['pts'][-1][1]),
            'angle_deg': float(_ang_fl),
            'length': _fr['length'],
        }
```

⇒ **四欄**：`p1`／`p2`／`angle_deg`／`length`。🔴 **⛔ 含 DXF entity `handle`**（承 `W-G.9-230R`）。
另 `result['front_lengths'][_w] = _fr['length']`；`result['front_lines_matched_count'] = len(result['front_lines'])`。

🔒 **`_ambig` 之產生條件（逐字·`:1444`–`:1446`）**

```
        if len(_rk) > 1 and (_rk[0]['overlap_m'] - _rk[1]['overlap_m']) <= _BIND_MARGIN_M:
            _ambig.append({'layer': _layer_nm, 'handle': _handle,
                           'candidates': _rk[:4], 'chosen': None})
```

其中 `_BIND_MARGIN_M = 1.0`（字樣錨 `_BIND_MARGIN_M = `·命中 **1**）；
`_rk` 由 `_best_block` 依 `_line_block_overlap` 排序（`key=lambda x: (-x['overlap_m'], x['block'])`·**同分按塊名**）。
另二來源：`:1510`（`'SIDE_LINE·側別'`·逾 `_TOL_SL`）／`:1730`（BASELINE·勝差 ≤ `_BL_PERP_TOL_M`）。
🔒 **`_ambig` 之全部命中 ＝ 11 列**（列框·當場復算）：`:1284`／`:1285`（註）／`:1432`（宣告 `_ambig = []`）／
`:1445`／`:1510`／`:1730`（**三處 `append`**）／`:1693`／`:1695`（註）／`:1803`／`:1804`／`:1805`（去向）。
🔒 `_BIND_MARGIN_M` 之全部命中 ＝ **4** 列；其中**賦值列**（字樣錨 `_BIND_MARGIN_M = `）＝ **1**。

🔒 **去向 ⇒ 🟢 <u>已</u>出艙至 UI**（逐字·`:1803`–`:1807`）

```
    result['diagnostics']['binding_ambiguity'] = list(_ambig)
    if _ambig:
        _exc = CadBindingAmbiguity(_ambig)
        _exc.partial = result      # C-11：已解出之部分交給 UI 預填（非歧義列免重問）
        raise _exc
```

UI 端：`class CadBindingAmbiguity(RuntimeError)`（`:5311`）／`def render_cad_binding_confirm(...)`（`:5377`）／
攔截點 `except CadBindingAmbiguity as _e_amb:`（`:18117`）→ `render_cad_binding_confirm(`（`:18118`）。
🔒 註之逐字（`:1800`–`:1802`）：「UI 層以 `render_cad_binding_confirm` 攔截並渲染「圖資配對確認」頁；
headless（`run_all`／harness）無 UI ⇒ 例外即停機＝正確之 loud 行為。」

### `d`　各可建築街廓之「沿線道路街廓覆蓋率」（**逐街廓實算**）

🔒 **算法源 ＝ 生產碼之 `_line_block_overlap`**（自 `harvest` 取·⛔ 於探針另寫第二份判準）；
受詞由 `run_verification._build_cb_cad(ns)` 產出（**⛔ 於探針重建街廓建構碼**）。

🔒 **三軸**：來源 ＝ `data/V6_1.dxf`（**88453** B·現行 `V6DXF` 之解析結果）／
母體 ＝ 街廓 **11** 塊（可建築 **6** ＝ `R1`…`R6`；道路類 **4** ＝ `RD1`…`RD4`·其 `category` 皆為 `道路`）／
粒度框 ＝ **長度（公尺·1-D 投影重疊長）**。
🔒 **容差 ＝ `_line_block_overlap` 之預設 `ang_tol_deg=2.0`／`perp_tol_m=1.0`**（⛔ 本探針另設）。

🔒 **自我驗證閘**（`CLAUDE.md`「探針還原內部幾何時…須以碼面自身之保證當自我驗證閘」）：
以 `|p1−p2| ≈ length` 驗「該 `FRONT_LINE` 係**單一直段** ⇒ `p1`／`p2` 即其全部幾何」——
**6／6 全過**（`Δ` 皆 `< 0.0001` m）⇒ 以 `[p1, p2]` 為線之輸入**無近似**。

| 街廓 | `FRONT_LINE` 線長 (m) | Σ重疊 (m) | **覆蓋率 (%)** | 逐道路塊之重疊 (m) |
|---|---|---|---|---|
| **`R1`** | 87.3131 | 7.0508 | **8.08** | `RD1` 3.6682／`RD2` 3.3826 |
| `R2` | 100.0274 | 96.4832 | **96.46** | `RD1` 96.4832 |
| `R3` | 93.4051 | 89.8392 | **96.18** | `RD2` 89.8392 |
| **`R4`** | 98.8938 | 7.2554 | **7.34** | `RD2` 3.7114／`RD3` 3.5440 |
| `R5` | 92.7995 | 89.3363 | **96.27** | `RD2` 89.3363 |
| `R6` | 85.8549 | 82.4534 | **96.04** | `RD3` 82.4534 |

🔒 **對照組（`常規五`·同器同母體）**：
器須綠 ＝ `FRONT_LINE` × **其自身街廓**之重疊，六塊皆 `> 0`
（`R1` 80.2623／`R2` 96.4832／`R3` 89.8392／`R4` 91.6385／`R5` 89.3363／`R6` 82.4534）✅；
器須紅 ＝ 一**人造遠方線段**（其座標⛔ 入倉·以代稱行之）× 全部街廓之**最大**重疊 ＝ **0.0** ✅。

#### 🔴 **單側不符（新增）**：實算與單所報之三數皆相異

| 受詞 | 單所報 | **本批實算** | 差 |
|---|---|---|---|
| `R1` 覆蓋率 | `8.2` | **8.08** | `−0.12` |
| `R4` 覆蓋率 | `7.4` | **7.34** | `−0.06` |
| 其餘四塊之覆蓋率 | `100.0` | **96.04**〜**96.46** | 🔴 **`−3.5`〜`−4.0`** |

🛑 **⛔ 評價其成因**（單之禁項）；本節**只出艙事實**。
🔒 **可能之框差已具名（⛔ 判定）**：本批之分母 ＝ `FRONT_LINE` 之**全長**（含其**畫至未截角尖角**之延伸段·
見 `_line_block_overlap` docstring 逐字「CAD 之 FRONT/SIDE 線畫到**未截角尖角**、比 BLOCK 邊長約一截角腿」）；
若他方之分母或分子取**截角後**之量，二者本即不可直接相比。**其孰為所欲之定義 ＝ 意思決定 ⇒ 候發單側／KL。**

🔒 **併記（事實·⛔ 評價）**：`R1`／`R4` 之覆蓋率**顯著低於**其餘四塊（`8.08`／`7.34` vs `96.04`〜`96.46`），
且二者之重疊**皆分散於二個道路塊**而每塊僅約 `3.5`–`3.7` m。

### `e`　「重劃範圍邊界」之可取得性 ⇒ 🟢 **有（二路）**

| 路 | 構造（逐字）| 位置（字樣錨）| 去向 |
|---|---|---|---|
| **一**　`outer_boundary` | `union_geom = unary_union([p['shapely'] for p in leaf_polys])` → `outer_boundary_pts = list(union_geom.exterior.coords)`（`MultiPolygon` 時取**最大**者之外環）| `# 重劃範圍外框：所有葉節點街廓的幾何聯集`（命中 **1**·於 `ce19acc` 為 `:808`）| `parse_ua_cadastral_dxf` 之回傳鍵 `'outer_boundary'`（其註逐字「最大多邊形視為重劃範圍外框」）＋ `outer_area_m2`；UI 顯示於 `**重劃範圍（外框）面積：**`（`:17673`）與繪圖 `name='重劃範圍外框'`（`:17794`）|
| **二**　`_reform_boundary` | `# 建重劃區邊界（所有非 outer BLOCK 聯集的 boundary）` → `_reform_boundary = _reform_union.boundary` | 字樣錨 `建重劃區邊界`（命中 **1**·`:4461`–`:4467`）| 消費於 ghost 判定：`if res_sub.distance(_reform_boundary) < BOUNDARY_BAND_M:`（`:4619`），`BOUNDARY_BAND_M = 2.0`（`:4457`）|

🔒 **⛔ 另有專用之「重劃範圍」DXF 圖層**——`_CAD_BOUNDARY_LAYERS`（`:946`–`:947`）之成員為
`ROAD`／`CADASTRAL_BOUND`／`CADASTRAL_BOUNDARY`／`BOUNDARY`／`PARCEL_BOUND`／`PARCEL_BOUNDARY`／`地籍線`
——其受詞係**地籍界址線**，**⛔ 重劃範圍外框**；`BLOCK_BOUNDARY`（`:1832`／`:1853`）則係**輸出** DXF 之圖層名。
🔒 **對照組**：器須綠 `BLOCK` ＝ **89** ✅；器須紅（人造圖層名·代稱）＝ **0** ✅。

---

## 驗收（逐項·缺一即不過）

| 項 | 值 | 判 |
|---|---|---|
| 工項零 `a` 之占用查與對照組 | `W-G.9-234` ＝ **0**／**0**；對照組 甲 `W-G.9-232` ＝ 2／8（`≥1`）、乙（人造號）＝ 0／0 | ✅ |
| 工項零 `b` 之入倉 `sha256` 逐位相同 | 落檔 ＝ 入倉 ＝ `df29c14be2a0321ff6cafa82c148867933983680cfedea5c86168ff8b55b9b0d`（**6345** B 兩端同）| ✅ |
| 工項一之標記逐字／前綴驗算／正文區段 `sha256` 不變 | 標記逐字見上；**嚴格後綴** `True`（既例 ＝ 首行標記·`docs/orders/` 現存 5 則）；正文 `sha256` ＝ `5aceb836…` **不變** | ✅ |
| 工項二之落地處檔列與三款逐款出艙 | 落地 ＝ `CLAUDE.md` 末端新節（字樣錨 `入倉前之號占用閘`）；三款逐字見上；嚴格前綴 `True`·`Δ +3313` ＝ payload | ✅ |
| 工項三 `N1`／`N2` 之號與推導式、既登者之查重 | `自誤 286`／`287`；二推導式逐字載於各則；查重 ＝ 二受詞既有命中皆 `0`；CC 側四受詞**皆已於 `-232R` 登記** ⇒ ⛔ 重複鑄 | ✅ |
| 工項四 `a`–`e` 之逐字出艙（`d` 之逐街廓實算為必要項）| `a` 六環節表 ＋ 5 列觸點／`b` 🔴【查無】（四處 `data_editor` 皆 `fixed`）／`c` 四欄位 ＋ `_ambig` 產生與去向（🟢 已至 UI）／**`d` 六街廓逐塊實算**（自我驗證閘 6/6·兩造對照過）／`e` 🟢 二路 | ✅ |
| **閘 `P`** 前後逐檔 `sha256` | `app.py` `e3e464ea…`／`selection_pipeline` `6c678342…`／`run_verification` `e48f24c8…`／`stepg_pipeline` `02602733…`／`run_all` `4ba89fef…` ⇒ **五檔前後逐位相同** | ✅ |
| **`left-right`** | **`0 0`** | ✅ |
| **`deletions`** | 本批二 commit 皆 **0**（`ce19acc` ＝ `91/0`；本 commit 見其 `--numstat`）| ✅ |

### 🔒 一句話**推導式**

> 本單新增之「入倉前查號占用」款於**其自身之入倉**首次施行：`W-G.9-234` 檔框 `0`／列框 `0` 且兩造對照皆過 ⇒ 可用；
> 據此原封入倉（`sha256` 逐位相同）後，於 `W-G.9-232` 單首行掛號衝突標記（**嚴格後綴** `True`·正文 `sha256` 不變）、
> 於 `CLAUDE.md` 末端補三款（**嚴格前綴** `True`·`Δ` ＝ payload）、於自誤簿分鑄 `286`／`287`
> （形之自證 `[286,287]`·`MAX 285→287`·缺號恆 `[106]`）；
> 勘查四項以生產碼自身之符號實算（`d` 之自我驗證閘 6/6）
> ⇒ 生產碼五檔一位元組未動、`deletions` ＝ `0` ⇒ 依 `常規一` 逕行 `push`。

---

## 附　🛑 單側不符與 CC 側器紅（**逐字回報**）

| # | 側 | 受詞 | 處置 |
|---|---|---|---|
| `1` | **單** | 覆蓋率之三數（`8.2`／`7.4`／`100.0`）與本批實算（`8.08`／`7.34`／`96.04`〜`96.46`）相異 | **只出艙事實**；框差之可能來源已具名（截角前／後之分母）；🛑 其孰為所欲之定義 ＝ 意思決定 ⇒ **候發單側／KL**，⛔ 由 CC 自裁·⛔ 鑄號（單⛔ 令就此鑄）|
| `2` | **CC** | 首跑之占用查以 `git ls-files --with-tree <rev>` 取母體 ⇒ **併入 index** ⇒ `4fe2b44` 態之母體混入現態檔名 ⇒ **讀不到 3** ⇒ **母體有洞** | 依 `W-G.9-14` 修法 `1` 該輸出**⛔ 出艙**；改 `git ls-tree -r -z --name-only <rev>` 重跑（讀不到 `0`）；**自加之戒 ＝ 凡取「某 `rev` 之母體」一律用 `ls-tree`，⛔ `ls-files --with-tree`** |
| `3` | **CC** | 🩸 **第三次**誤送 heredoc（`python - <<'PY' … PY`）⇒ 違 `GB-143` 補款二之硬閘「**凡經 heredoc 者一律⛔ 執行**」 | 該命令**於 `stdin` 懸掛**、逾時轉背景，已即刻 `TaskStop` 終止；**產物後果 ＝ 零**（其為 `noop`·未動任何檔·工作樹 `M` 三檔逐一具名於附二）。🔒 **累犯之登記**：前二次 ＝ `docs/reports/W-G.9-225R_…:263` 與 `docs/reports/W-G.9-231R_…` 工項五 其二。🛑 **「後果為零」⛔ 得作為放寬該閘之理由**；**自加之戒（第二度重申）＝ `Bash` 之 `command` 內⛔ 出現 `<<` 之任何形**，一次性 Python 一律 `Write` 落檔後以路徑呼叫 |

🔒 **`1` 之單所載其餘數皆經復算<u>相符</u>**：`W-G.9-231` 檔框 `2`（發單時）／`wf_f4` 之 `value` 子字串框 `21`／
`pre_price` 子字串框 `10`／碼側自陳之行（於 `ce19acc` 為 `verify/wf_f4.py:438`）。
🛑 **號衝突（`231`）與 `pre_price` 二事已依單之令鑄為 `自誤 286`／`287`**，⛔ 於本表重複計列。

---

## 附二　本批之產物

| 檔 | 性質 | `deletions` |
|---|---|---|
| `docs/orders/W-G.9-234_號占用閘與發單側二誤與區外道路勘查.md` | 🆕 純新增（工項零·commit `ce19acc`）| **0** |
| `docs/orders/W-G.9-232_v3-427更正標記與wf_f2容量過濾補查.md` | 🔀 **首行標記**（`+373` B·嚴格**後綴** `True`·正文 `sha256` 不變）| **0** |
| `CLAUDE.md` | 🔧 **純末端追加**（`+3313` B·嚴格前綴 `True`）| **0** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 🔧 **純末端追加**（`+6090` B·嚴格前綴 `True`·鑄 `286`／`287`）| **0** |
| `docs/reports/W-G.9-234R_號占用閘與發單側二誤與區外道路勘查_執行報告.md` | 🆕 純新增（本檔）| **0** |

🔒 **生產碼五檔⛔ 動一位元組**；`harvest` 之副作用經 `git status --untracked-files=no` 驗**為零**
（僅本批之三檔為 `M`）；`verify/out/` **⛔ `git add -A`**（逐檔 add）。
