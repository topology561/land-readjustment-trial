# 🧾 `W-G.9-194R` 執行報告：`GB-133` 解除 ＋ 攢批登記

**出文窗**：CC　**施工單**：`docs/orders/W-G.9-194_施工單_GB133解除與攢批登記.md`（`24502` B／`274` 列·`CRLF 0`）
**基座**：`daa618d0952d5d4c7e4d7e273edc5bcc32c6d1fe`　**工作副本**：主 checkout（`core.autocrlf` ＝ `true`）

## 🛑 結論摘要（⛔ 不以「全綠」一語帶過）

- **`commit 1`（零生產碼·`N-1` ＋ `N-2` ＋ `A-1`）已 push** ＝ `f44e0008f7cf06f4c8b920ba0593f1a5fbeabe9a`。
- 🔴 **`app.py` 之改動（`N-3`）已備妥、⛔ 未 push**——依 `§四-2（三）`：`§四-2（二）` 第 `5` 款**文義上成立**，KL 之預先核可於該情形**不生效** ⇒ **回報發單側**（⛔ 未直接問 KL）。
- 🔒 **`D-1`／`D-2`／`D-3` 三閘全過**；`§四-2（二）` 第 `1`〜`4` 款**皆未觸發**。
- 🔒 第 `5` 款之**所載理由為偽**：對帳段二態**逐位相同**（見 `§五`）——**類 III 係基座既有、⛔ 非本批所致**。

🛑 **本報告先行入倉之理由（`常規二`·逐字回報之偏離）**：單 `§零` 令 `A-2` 為 `commit 3`（在 `commit 2` 之後）。
惟 `commit 2` 依 `§四-2（三）` **⛔ 不得 push**，而 `push` 之判單位 ＝ **ref 之推進區間**（`W-G.9-190R` 補款 `§二-6`）
⇒ 置於其後之報告**結構上亦不可 push**，發單側**無從自倉讀取**。
⇒ **取保守項**：本報告 commit 於 `f44e000` 之上（零生產碼·可 push），`app.py` 之 commit **置於其後、備妥不推**。
⛔ 未改單一字；本偏離逐字載於此。

---

## `§一`　開工閘與收工閘之**三欄**（`rc` ／ 逐字結論列 ／ `stderr` B）

| 閘 | `rc` | 逐字結論列 | `stderr` B |
|---|---|---|---|
| `S-0a` | — | `ls-remote` ＝ **`daa618d0952d5d4c7e4d7e273edc5bcc32c6d1fe`**（＝ 單載·本地 `HEAD` 同） | `0` |
| `S-0b` | **`0`** | `🔴 機械·停機款：0 項` ／ `🟡 提示：6 項` ／ `[P-5] SELF_SHA256 ✅ 相符（受詞 24424 B）` ／ `[P-3] app.py 之 def main ＝ 14242-23460` | `0` |
| `S-7` | — | `declared` ＝ `recomputed` ＝ `ef75f3bdabbe24b698fa18c028863b4cf540bbac7264b375406c8968181bab76` | `0` |
| `S-1` | — | `36\t0\tdocs/reports/W-G.4_泛用阻塞項登記表.md` ／ `13\t0\tdocs/reports/W-G.9波_claude.ai側自誤登記.md` ⇒ **刪除欄逐列 `0`** | — |
| `S-2` | **`0`** | `受檢 ＝ 77 筆／不適用 0／合計 77` ／ `結論：✅ 全部成立` | `0` |
| `S-3` | **`0`**／**`0`** | `py_compile` **無輸出**（無例外）／`ast.parse OK（無例外）` | `0`／`0` |
| `S-4` | — | `6\t6\tapp.py`（🛑 搬移 ⇒ `deletions > 0` 係預期） | — |
| `S-5` | — | 自誤 `244` 命中定義列式 ＝ `True`；`GB-134` 命中三形聯集 ＝ `True`；**判別力對照**（未鑄之 自誤 `245`／`GB-135`）皆 `False` | — |
| `S-6` | — | 自誤 MAX `243`→**`244`**；`GB` 已扣除 MAX `133`→**`134`**；`244`／`134` **皆⛔ 不在缺號集** | — |
| `S-8` | — | `D-1` ✅ 轉／`D-2` ✅ ⛔ 非全 `0.0`／`D-3` ✅ 逐格不變（見 `§四`） | — |
| `S-9` | `run_all rc=1`（二態同） | 類 I `0`／類 II `8`／**類 III `13`**（**二態逐位相同**·見 `§五`） | — |

**逐 `commit` 之生產碼判法輸出（含空輸出）**

| `commit` | 區間 | 輸出（原樣） | `grep_rc` | 命中行數 |
|---|---|---|---|---|
| `1`（`f44e000`） | `daa618d..f44e000` | **（空）** | `1` | **`0`** |
| 本報告 commit | `f44e000..<本>` | **（空）** | `1` | **`0`** |
| `app.py` commit（**備妥未推**） | `<本>..<其>` | `app.py` | `0` | **`1`** 🔴 |

🔒 **對照組**：以字串 `app.py` 餵同一 `grep -xE` ⇒ 命中 `1` ⇒ **框非紅**。

---

## `§二`　`S-0b` 之 🟡 逐項具名（⛔ 未靜默略過）

CC 實跑得 **🟡 `6` 項、🔴 `0` 項**——**恰為單 `§零-b` 所預告之集合**，⛔ 無表外項。
🔒 單所列之 `P-1`（`` 🩸 **案由（活體）** ``）**於本次實跑⛔ 未觸發**——與單載「✅ **已採納**——同段已補具名母體與所查範圍」一致。

| 檢 | 觸發點（字樣錨） | 處置 |
|---|---|---|
| `P-2` | `` \| 檢 \| 觸發點（字樣錨） \| 發單側之處置 \| ``（`§零-b` 豁免表自身之表頭） | **具名豁免（承發單側）**——該表⛔ 不出艙任何新計數，其所引之數（`5`／`71`／`442`／`0`）皆為他節之引述 |
| `P-2` | `` 🔑 **判別力造（四造** ``（`§二`） | **具名豁免（承發單側）**——該段之數係被引述之樣本逐字，⛔ 非本單之出艙計數 |
| `P-4` | `` 1. 分類迴圈 ``（`§三-1`） | **具名豁免（承發單側）**——碼面控制流之列舉；`§三-2` 之座標系宣告已涵蓋全 `§三` |
| `P-4` | `` - ⛔ 不改 `priority_index` 之算式 ``（`§三-7`） | **具名豁免（承發單側）**——禁改清單之列舉，⛔ 不承載方向性量 |
| `P-4` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§五` 表頭） | **具名豁免（承發單側）** |
| `P-6` | `` \| 閘 \| 受詞 \| 判準 \| ``（`§五` 表頭） | **具名豁免（承發單側）**——`§五` 為閘之索引表，各閘之實測出處逐列具名於 `§三-4`／`§三-5` |

---

## `§三`　取號現查（🛑 二數並報 ＋ 所扣之值逐字 ＋ 意指占用二形並取 ＋ 對照組）

🛑 **框之宣告（`自誤 244` 之一般式·⛔ 不倚賴 `P-2`）**：下列「相異／MAX／缺號」之框皆為 **`列`**（`^` 錨之定義列式，逐檔逐列一次）；「意指占用」之框皆為 **`次`**（`re.findall` 之出現次數）。

| 類 | 母體 | 定義列式（逐字） | 未扣除 | 已扣除 | 次號 |
|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md` 單檔 | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | 相異 `208`【列】／MAX `243`／缺 `35` | **同左** | **`244`** |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔 | 三形聯集（式A `3`／式B `44`／式表 `128`【列】） | 相異 `131`／MAX `133`／缺 `[12, 87]` | **同左** | **`134`** |

🔒 **所扣之值逐字**：自誤 ＝ 七則人造值 `自誤 9997`／`8811`／`6205`／`4907`／`3182`／`5741`／`4173`；
`GB` ＝ `{997, 883, 746, 357}`。**二類之所扣值於其定義列式皆不命中** ⇒ **未扣除與已扣除二數相同**。

**意指占用**（母體 ＝ **全倉 `.md`** `496` 檔·開不到 `0`·錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·**二形並取**·扣本單·框 ＝ **`次`**）

| 號 | 無反引號 | 帶反引號 | 判 |
|---|---|---|---|
| `自誤 244` | **`0`** | **`0`** | ⛔ 未占用 |
| `GB-134` | — | **`0`** | ⛔ 未占用 |
| **對照組** `自誤 243` | **`3`** | **`3`** | ⇒ 量測器⛔ 非恆零 |
| **對照組** `GB-133` | — | **`27`** | ⇒ 量測器⛔ 非恆零 |

🔒 payload 皆自單之 fence **逐字抽出**（⛔ 非手抄），`unified_diff` **二者皆 `0` 列**。

---

## `§四`　`N-3` 之落地與 `D` 閘

### `§四-1`　`§三-3 之 2` 之**現查坐實**（🛑 ⛔ 未採信單述）

| 字樣錨（`grep -F` 固定字串） | 檔 | 命中【**列**】 |
|---|---|---|
| `cand_p1['_corner_cut_len'] = round(_corner_cut_len_p1.get(id(cand), 0.0), 3)` | `app.py:12914` | **`1`** |
| `cand_p2['_corner_cut_len'] = round(_corner_cut_len_p2.get(id(cand), 0.0), 3)` | `app.py:12932` | **`1`** |

🔑 **判別力對照（證該檢⛔ 非恆為 `1`）**：寬字樣之**列**數 —— `_corner_cut_len` **`12`**／`_corner_cut_den` **`4`**／`_side_line_len` **`7`**／`_side_line_den` **`4`**／`priority_index` **`6`**，**皆 `> 1`** ✅。

### `§四-2`　落地之逐列 `diff`（`S-4` ＝ `6`／`6`）

**二事，⛔ 無第三事**：① `for cand in qualified:` → `for cand in group:`（`1` 列）
② 早退區塊（`if not qualified:` ＋ 其 `return` 三列 ＋ 空行 ＝ `5` 列）**移至指數迴圈之後**。
🔒 迴圈**本體逐字未動**；`app.py` blob `da805fc2…` → `b9a50a2…`。
🔒 **禁改清單十一項之機檢**：三 `score_*` 算式／權重 `0.4`／`0.2`／`0.4`／`round` 位數／四欄欄名／`qualified.sort` 二鍵／早退 `note` 逐字——**逐項次數 `1→1` 不變** ✅。
🔒 `verify/**` 之異動檔數 ＝ **`0`**。

🛑 **⛔ 未加任何註解**——單 `§三-2` 逐字「**只改二事**」，加註即第三事、逾 KL 核可之射程。
🔬 **併記一項供發單側裁**：`# 🆕 V13 修正 #3 + #7：**第二關**優先權指數…` 一段現位於早退**之前**，其「第二關」之名對**落選**候選已略不精確（決策仍只用 `qualified`）。⛔ 本批未動之；建議於**次一零生產碼 commit** 加註。

### `§四-3`　🔴 `D` 閘（🛑 通道 ＝ **harness**·⛔ 非土地後果·見 `§六`）

量測器置於**倉外**（⛔ 未動 `verify/**` 一字）：spy 包 `ns['_pk_one_side_v12']`，走 `verify/selection_pipeline.run_corner_pk`，`SB = 3.5 m`。
**改前／改後各一趟，`_pk_one_side_v12` 呼叫次數皆 `12`。**

**`D-1`**（段一全滅之街角，其候選之 `priority_index` 須由「無值」轉為數值）—— 受詞側數 ＝ **`1`**（`#6`·`group_size = 4`）

| 候選 | 改前 | 改後 |
|---|---|---|
| `628-42(1)` | `None` | **`0.051927`** |
| `628-41(1)` | `None` | **`0.89727`** |
| `628-40(1)+` | `None` | **`0.018626`** |
| `628-27(1)` | `None` | **`0.032177`** |

⇒ ✅ **`D-1` 過**。

**`D-2`**（⛔ 不得全為 `0.0`）：四值**皆非 `0.0`**（非零者 `4` 筆）⇒ ✅ **過** ⇒ `§三-3` 命題 `2` **未破**。

**`D-3`**（段一有得標之街角，winner 與其 `priority_index` 須逐格不變）

| 側 | winner（前→後） | `priority_index`（前→後） | `qualified` 逐格同 |
|---|---|---|---|
| `#0` | `628(1)` → `628(1)` | `0.991558` → `0.991558` | `True` |
| `#1` | `628-1(1)` → 同 | `0.992472` → 同 | `True` |
| `#3` | `628-18(1)` → 同 | `0.131073` → 同 | `True` |
| `#4` | `628-45(1)+` → 同 | `0.43065` → 同 | `True` |
| `#9` | `628-30(3)+` → 同 | `0.609538` → 同 | `True` |
| `#10` | `628-36(1)` → 同 | `0.237704` → 同 | `True` |
| `#11` | `628(5)` → 同 | `0.991533` → 同 | `True` |

⇒ ✅ **`D-3` 過**；併驗全 `12` 側之 `note`／`group_size`／`winner`／`winner_pi`／`qualified` **逐格相異數 ＝ `0`**。
🔑 **判別力對照**（把改後 `#0` 之 winner 竄改後須判不同）⇒ `True` ⇒ 本對拍 ⛔ 非恆綠。

---

## `§五`　🔴 `S-9` 對帳與**其二態對拍**

二趟 `run_all` 於**拋棄式 clone** 內跑（`CLAUDE.md`：`run_all` 會改寫已追蹤 log）：
`C:\w194a` ＝ 基座 `app.py` blob `da805fc2…`／`C:\w194b` ＝ 本批態。**二 clone `HEAD` 皆 `f44e000`·`core.autocrlf=false`**。
二態框之坐實：`for cand in qualified:` `1`→`0`；`for cand in group:` `1`→`2`。

| 態 | `rc` | 類 I | 類 II | **類 III** | 逐字結論列 |
|---|---|---|---|---|---|
| **基座** | `1` | `0` | `8` | **`13`** | `🔴 **對帳 FAIL**——名目或失敗原因與凍存不符（⛔ 不得逕自更新名單）` |
| **本批** | `1` | `0` | `8` | **`13`** | 同左 |

🔒 **機械證（⛔ 非目視）**：對帳段（`L410`〜檔尾·**`117` 列**）於二態**逐位相同 ＝ `True`**，相異列 ＝ **無**；
**判別力對照**（於本批側注入一字元）⇒ **`False`** ⇒ 本對拍 ⛔ 非恆綠。
⇒ 🔒 **類 III 之 `13` 項係<u>基座既有</u>、⛔ 非本批所致**；其成因係**凍存名單過期**（`GB-111`／`GB-97` 族·單 `§八` 已列波末佇列）。

### `§五-1`　全 log 之二態逐位對拍——**`4` 列相異**，其射程已逐格究明

`run_all` 全 log 各 `526` 列，正規化 clone 路徑後**相異 `4` 列**，皆為四閘之「本閘違規總計」：

| 閘 | 基座 | 本批 | Δ |
|---|---|---|---|
| `v3·診斷0m` | `72` | `96` | **`+24`** |
| `無串聯0m` | `72` | `96` | **`+24`** |
| `v3·診斷3.5m` | `64` | `104` | **`+40`** |
| `無串聯3.5m` | `64` | `104` | **`+40`** |

🛑 log 之顯示上限為 `12` 列（`GB-115`）⇒ **新增之違規於 log 上不可見** ⇒ 改由**不截斷之通道**取全集
（`verify/probes/probe_WG9168_viol_full.py`·其比較式 ＝ `import run_verification.diff_rows`·⛔ 未自寫）：

| 閘 | 基座全集 | 本批全集 | **新增** | **消失** | 新增之**欄**分佈 |
|---|---|---|---|---|---|
| `v3·診斷0m` | `72` | `96` | `24` | **`0`** | `正街角分(0.4)` `6`／`側街分(0.2)` `6`／`跨占分(0.4)` `6`／`總分` `6` |
| `無串聯0m` | `72` | `96` | `24` | **`0`** | 同上 |
| `v3·診斷3.5m` | `64` | `104` | `40` | **`0`** | 四欄各 `10` |
| `無串聯3.5m` | `64` | `104` | `40` | **`0`** | 同上 |

🔒 **新增之違規逐格皆為同一形**：`baseline='—' got=<數值>`，且**僅出現於<u>落選（未達標）</u>候選列**
（`0m`：`R2左` `2` 宗／`R3右` `2`／`R5左` `1`／`R6右` `1` ＝ `6` 列 × `4` 欄 ＝ `24`；
`3.5m`：`R1左` `1`／`R2左` `3`／`R3右` `2`／`R5左` `3`／`R6右` `1` ＝ `10` 列 × `4` 欄 ＝ `40`）。
🔒 **`消失 = 0`** ⇒ **既有之違規一格未動**；**⛔ 無任何 winner／達標候選列之欄值改變**。

🔑 **⇒ 此即 `GB-133` 登記本文所<u>預告</u>之行為變更**（其逐字：「🔴 **該移動會改變既有出艙**（`未達標` 列之四欄由 `—` 變為數值）⇒ 屬**行為變更**，須以判別力閘看守，⛔ 不得逕改」）。

---

## `§六`　`§三-5`　二通道之**分列**（🛑 ⛔ 不得混列）

| 通道 | 本批之數 | 可證 | ⛔ 不可證 |
|---|---|---|---|
| **harness**（`verify/selection_pipeline.py:310` 之 `v12 = ns["select_corner_lots_both_sides_v12"]`） | `D-1`／`D-2`／`D-3` 全過；全 `12` 側逐格對拍相異數 `0` | **碼面行為** | 🔴 **土地後果** |
| **KL UI 實跑** | 🛑 **待 KL 實跑**——本批⛔ 未取得 | **土地後果** | — |

🔴 **二者⛔ 非等價**（`GB-123`：`main()` 無自動守備）⇒ **本報告⛔ 未以 harness 之綠宣稱土地後果**。
倉內現有之 KL UI 代 ＝ `2e08a41`／`a3c97aa`／`7a6b7f3` 三代，**皆為改前態**；本批態之 UI 實跑列為**後續觀測項**。

---

## `§七`　🔴 `§四-2（二）` **五款逐款判**（⛔ 不得以「全綠」一語帶過）

| # | 情形 | 判 | 逐字情狀 |
|---|---|---|---|
| `1` | `D-2` 觸發（落選候選之 `priority_index` 全為 `0.0`） | ✅ **未觸發** | 四值 ＝ `0.051927`／`0.89727`／`0.018626`／`0.032177`，**非零者 `4` 筆** |
| `2` | `D-3` 觸發（任一街角之 winner 或其 `priority_index` 有變） | ✅ **未觸發** | `7` 個有得標街角逐格不變；全 `12` 側逐格相異數 `0`；違規全集之**消失 `0`**、新增**僅落選列之四指數欄** |
| `3` | `app.py` 以外之生產碼檔有異動 | ✅ **未觸發** | `git diff --name-only daa618d \| grep -xE '…'` 之輸出**空**；`verify/**` 異動 `0` 檔 |
| `4` | `§三-7` 禁改清單之任一項被觸及 | ✅ **未觸發** | 十一項字樣逐項次數 `1→1` 不變 |
| `5` | **`S-9` 之對帳器出現類 III（訊息實質不同）** | 🔴 **文義上成立** | **類 III ＝ `13` 項**（`≠ 0`）。🛑 惟該款所載之理由「**既有紅之組成已變**」**為偽**——對帳段 `117` 列於**基座／本批二態逐位相同**（判別力對照 `False`）⇒ **組成⛔ 未變**、`13` 項係**基座既有**（凍存名單過期·`GB-111`／`GB-97`） |

### 🛑 處置（依 `§四-2（三）` 逐字）

> 上表任一成立 ⇒ CC 將 `commit 2` **備妥但⛔ 不 push**……**回報發單側**（⛔ 不直接問 KL）。

⇒ 🔴 **`app.py` 之 commit 已備妥、⛔ 未 push**。⛔ **未**以「KL 已說準」為由略過本款。

🔒 **呈發單側之二問（⛔ 不上呈 KL）**
1. 第 `5` 款之**情形**（出現類 III）成立，而其**理由**（組成已變）經二態逐位對拍為偽。
   ⇒ 該款究係「**出現類 III 即停**」抑或「**類 III 之組成相對基座有變即停**」？
   前者於本倉**結構上不可能通過**（基座即 `13`，且單 `§八` 自載凍存名單過期屬波末佇列）。
2. `§五-1` 之四閘違規 `+24`／`+24`／`+40`／`+40`，經不截斷全集判定**全部**為
   「落選候選之四指數欄由 `—` 變為數值」，**消失 `0`**——此即 `GB-133` 本文所預告者。
   ⇒ 其是否屬 `§四-2（二）2` 之「已生土地後果」？CC 判**否**（winner／達標候選逐格未動），惟其**確為既有出艙之變更**，且**九份 baseline 之重產時點**因此更形迫切。

---

## `§八`　本批⛔ 未辦（承單 `§三-7`／`§八`）

⛔ 未改 `priority_index` 之算式／權重／`round` 位數／四欄欄名；⛔ 未動 `qualified.sort` 之鍵序；
⛔ 未改早退分支之 `note` 逐字（**本批⛔ 不解除強制抵費地**）；⛔ 未辦段一「上鎖」／段二全域排序／段三逐一嘗試／段四重排位次；
⛔ 未動 `verify/**` 任何一字；⛔ 未重產任何 baseline、⛔ 未重記期望 `FAIL` 名單。
