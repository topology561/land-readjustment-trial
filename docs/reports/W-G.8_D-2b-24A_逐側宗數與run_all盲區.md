# W-G.8 D-2b-24 commit A：逐側宗數（類 C）＋ `run_all` 盲區登記 ＋ 事前預測表

**產出**：CC／2026-08-09　**分支**：`wip/s1-endpart`　**⛔ 生產碼零觸**
**實料**：`verify/out/probe_D2b24A_sidecount*.log`（v1〜v5·**五版全部入庫**·⛔ 未覆寫）

> # 🔑 結論
> - **類 C 已完成分類**：`3.5m R1` 左側 ＝ **1 宗** ⇒ **無第 2 宗** ⇒ **歸入類 B**。
>   ⇒ 施工單 §6-1 之停機條件（「量出該側有第 2 宗 ⇒ 新類別」）**未觸發**。
> - **`run_all` 盲區已具名登記** ＝ **`GB-52`**。
>   🔴 **具名更正施工單 §1-2 之計數**：被折疊者為 **5 項 × 2 情境 ＝ 10** 個斷言，**非 4 項 ＝ 8**。
> - **事前預測表 12 格**已立（§四）·⛔ 先於 commit B。
> - ⚠️ **本檔記載我在本批的三次量測失誤**（§三-4）——三者外觀皆為「跑出數字了」。

---

## 一、§1-2 現查（**原樣複跑**·⛔ 未照抄）

| KL 之主張 | 現查輸出 | 判 |
|---|---|---|
| `grep -n "except RuntimeError as _e_sg" verify/run_verification.py` | **`684:        except RuntimeError as _e_sg:`**（`rc=0`） | ✅ |
| `grep -c "街廓 R1" verify/out/D2b23B_runall.log` | **9** | ✅ |
| `grep -c "街廓 R[2-6]" verify/out/D2b23B_runall.log` | **0** | ✅ |
| 四關鍵詞於期望名單之命中 | `G值`／`滑池槽`／`J表`／`六塊經驗錨` **全 0** | ✅ |
| 名單卷首自載失效條件 | `:11` 逐字「⚠️ **本名單於覆蓋閘修復後即失效**——本批之紅係**單一阻塞之連鎖**」 | ✅ |

### 1-1 🔴 **具名更正：折疊者為 5 項（＝10 個斷言），非 4 項（8 個）**

AST 求 `Try` 範圍 ⇒ **`verify/run_verification.py:655-686`**，handler 唯一（`@684`）；
try 主體**第一句**即 `run_step_g(...)`（`:656`）⇒ 其 raise 折疊**其後全部** `results.append`：

| 行 | 斷言名 | 期望名單命中 | `run_all` log 命中 |
|---|---|---|---|
| `:666` | **`結構不變量永久閘{tag}`**（🆕 施工單未列） | **0** | 2 ⚠️ |
| `:671` | `v3·G值{tag}` | 0 | 0 |
| `:675` | `v3·滑池槽{tag}` | 0 | 0 |
| `:679` | `v3·J表{tag}` | 0 | 0 |
| `:682` | `k* 六塊經驗錨{tag}` | 0 | 0 |
| `:685` | `v3·StepG{tag}（結構閘/看守觸發）` | **在名單內** | ← handler 內·**唯一會 append 者** |

⚠️ **`結構不變量永久閘` 之 log 命中 2 係<u>章節標題</u>、非結果列**
（`:75`／`:82` 逐字「… Step G headless（0m；v3 真值財務＋**結構不變量永久閘**）」），
且 pre-B 亦為 2 ⇒ **該斷言確實從未 append**。
🔒 **我是看了原文才沒誤判**——子字串命中敘述文字，是同一族陷阱（考古 55-2）。

⇒ **5 項 × 2 情境 ＝ 10 個斷言，既不在 FAIL、亦不在 PASS、不在結果集內。**

---

## 二、`GB-52`（新登記項·編號現查最大 `51` ＋1）

要旨見登記表該列；核心：**期望 FAIL 名單之「零新增零消失」，其母體結構性排除該 10 項**
⇒ ⛔ 不得以名單無變化宣稱本批對 R2–R6 無影響。

### 2-1 🔴 事前登記：**下一次大規模「新抵達」**（⛔ 事後不得補寫）

> R1 之閘全數轉過後，`StepG` 將**首次推進至 R2–R6**，並使上開 **10** 個斷言首次執行。
> 屆時新增之 FAIL，其**預設歸因為「新抵達」**（此前從未執行），
> ⛔ **不得逕判為該批之退步**；**每一列須先查其在 `preB` 是否曾執行**：
> **未曾執行者列為新抵達；曾執行且由過轉破者才是退步。**
> 🔒 證據取法：`verify/out/D2b23B_runall_preB.log` 與當時之 log 逐項比對該列**是否出現過**。

---

## 三、類 C：逐側宗數實測

### 3-1 涵蓋（KL §4「逐側」·**24 列全部列出**）

見【倉】`verify/out/probe_D2b24A_sidecount_v5.log`——6 街廓 × 2 側 × 2 情境 ＝ **24** 列，
⛔ 非只列有 `SIDE_LINE` 者、⛔ 非只列有街角者；並標注該側有無 `SIDE_LINE`。
🔒 **母體守恆自證**：已印出之呼叫 ＝ **248**／總呼叫 **248**，未印出之鍵 ＝ **無**。

### 3-2 兩個獨立量測器

| 量測器 | 原理 | 結果（`3.5m R1`） |
|---|---|---|
| **甲：`t`-遞增後綴** | spy `_solve_G_one` 記 `t = baseline_pt·d̂`，同趟內單調遞增 ⇒ 末趟 ＝ 極大遞增後綴 | 左 **1**／右 4 |
| **乙：生產順序切分** | `biz_polys` 之順序即 `left_results + right_results`（【倉】`verify/stepg_pipeline.py:916`／`app.py:19837` 逐字），左組 s 遞增、右組 s 遞減 ⇒ 分界 ＝ 首個嚴格下降；**左組 ＝ k−1** | 左 **1**／右 4 |

**兩法一致**。且乙法於 **R6** 修正了甲法之失效（甲 6+3=9 ≠ biz 11；乙 6+5=**11** ✅）。

### 3-3 逐格（乙法·⛔ 含不可判者）

| 情境·街廓 | biz | 左組 | 右組 | 判 |
|---|---|---|---|---|
| 0m R1 | 5 | **2** | 3 | 左側**有**第 2 宗 ⇒ 楔形應被吸收（與甲後缺口 `0.0046` 一致 ✅） |
| **3.5m R1** | 5 | **1** | 4 | 🔑 **左側無第 2 宗 ⇒ 類 C 歸入類 B** |
| 0m／3.5m R4 | 2 | **不可判** | 不可判 | ⚠️ 序列 `0.18 83.04` **無嚴格下降** ⇒ 分界不可判。**惟**現碼實測 `is_corner = 4`（2 趟 × **2 側**）⇒ 兩側皆有街角、皆有宗；biz 共 2 ⇒ **各 1 宗** ⇒ 同屬類 B |
| 0m／3.5m R2 | 15 | 8 | 7 | 兩側皆有第 2 宗 |
| 0m／3.5m R5 | 12 | 7 | 5 | 同上 |
| 0m／3.5m R3 | 14 | 7 | 7 | 同上 |
| 0m／3.5m R6 | 11 | 6 | 5 | ⛔ **退化案例·不計入任何憑據**；⚠️ 序列含相等值 `78.63 78.63` ⇒ 分界±1 不確定（和為 11 不受影響） |

⇒ **施工單 §6-1 之停機條件未觸發**（未量出「該側有第 2 宗」之情形）。

### 3-4 ⚠️ **本批之三次量測失誤（⛔ 併記·不隱藏）**

| # | 症狀 | 根因 | 對策（已落地） |
|---|---|---|---|
| 1 | 逐側宗數全為 **1**（而 R2 有 15 宗） | 以 `_solve_G_one` 之 **`side` 實參**當推進側。現查 `side = entry.get('side', '無')`（`verify/stepg_pipeline.py:635`／`:709`·`app.py:19550`／`:19633`）＝**該宗自身之街角側屬性**，多數為 `'無'` ⇒ 248 次呼叫只有約 22 次落在兩個標籤，其餘**靜默漏失** | 改以 **`d_hat` 與 FRONT `p1→p2` 之內積正負**分左右；並加**母體守恆自證**（已印/總數＋未印鍵，不符即 raise） |
| 2 | 效度檢查「失敗」於 6 格 | **參照選錯**：以 `build_parcels` 之宗數為真相源，然其含**未進推進迴圈**之宗（R1 6 vs biz 5、R4 3 vs 2） ⇒ 失敗係我的參照錯、非啟發式錯（考古 §七-3「X 必須取自生產同一函式」） | 改以 **`biz_polys` 數**為參照（＝生產路徑實際餵入者） |
| 3 | 乙法左組**多算 1** | 右組之**首宗即全域最大 s** ⇒ 「停止遞增之處 `k`」已把它算進左組 | **左組 ＝ k−1**；無嚴格下降者標 **`不可判`**、⛔ 不猜 |

🔒 **共同機制**：三者外觀皆是「**跑出數字了**」。⛔ **有輸出不等於量對**——
救回來的都是**與獨立量對不上**（15 宗卻報 1、和不等於 biz 數），⛔ 不是靠讀碼讀出來的。

---

## 四、事前預測表（12 格·🔒 **先於 commit B**·⛔ 事後不得修改）

**判定規則**（施工單 §1-4·⛔ 不得寫入生產碼）：
① 有街角楔形且**該側無下一宗** ⇒ 缺口**歸零**（楔形入池）
② 有街角楔形且該側**有**下一宗 ⇒ 類 A 殘餘**不變**（本批不觸）
③ 無街角宗、或該側 `SIDE ∥ ALLOC` ⇒ **逐位不變**

| # | 情境·街廓 | 甲後現況【倉】`probe_D2b23C_attrib_after.log` | 適用規則 | **預測** |
|---|---|---|---|---|
| 1 | 0m R1 | 缺口 `0.0046` | ②（左右皆有下一宗） | **逐位不變**（`0.0046`） |
| 2 | 0m R2 | 重疊 `45.9766` | ②（類 A） | **不變** |
| 3 | **0m R4** | 缺口 `58.4792` | **①**（兩側各 1 宗·無下一宗） | 🔑 **歸零** |
| 4 | 0m R6 | `0`／`0` | ③（`SIDE ∥ ALLOC`） | **逐位不變** |
| 5 | 0m R5 | 重疊 `56.3293` | ②（類 A） | **不變** |
| 6 | 0m R3 | 缺口 `0.0001` | ②／退化帶 | **逐位不變** |
| 7 | **3.5m R1** | 缺口 `5.3881` | **①**（左側 1 宗·無下一宗） | 🔑 **歸零** |
| 8 | 3.5m R2 | `0`／`0` | ③（無街角宗） | **逐位不變** |
| 9 | **3.5m R4** | 缺口 `58.4792` | **①** | 🔑 **歸零** |
| 10 | 3.5m R6 | `0`／`0` | ③ | **逐位不變** |
| 11 | 3.5m R5 | `0`／`0` | ③ | **逐位不變** |
| 12 | 3.5m R3 | 缺口 `0.0001` | ③／退化帶 | **逐位不變** |

🔒 **本批之靶**（KL 裁）＝ **類 B 歸零（#3／#7／#9）＋ 類 C 完成分類（已完成）**。
⛔ 「破量全歸零」**不列為本批之靶**——類 A（#2／#5）不在射程內。
🔒 驗收鍵 ＝ **側別 ＋ s 區間**，⛔ 非宗序號。

---

## 五、異動清單

| 檔 | 異動 | 性質 |
|---|---|---|
| `verify/probes/probe_D2b24A_sidecount.py` | 新增（探針·含三次修正之逐條註記） | ⛔ 非生產碼 |
| `verify/out/probe_D2b24A_sidecount.log`〜`_v5.log` | 新增 **5 份**（⛔ 全部保留·失誤版亦入庫供考古） | 實料 |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | 新增 `GB-52` | 文件 |
| `docs/reports/W-G.8_D-2b-24A_逐側宗數與run_all盲區.md` | 新增（本檔） | 文件 |

⛔ `app.py`／`verify/stepg_pipeline.py` **零觸**。⛔ 期望 FAIL 名單**零變更**。⛔ 未重烤任何 baseline。
