# W-G.9-219R　🛑 停機於前置閘 `G4`（⛔ 改一行生產碼·本 commit 零生產碼）

**基座** ＝ `b7873bc00388dbee8ab76be72a36f0a785c557cc`（`left-right` `0 0`）。
**一切數字錨取索引側 `git cat-file blob`**（`GB-139 ②`）；**一切 `rc` 取 `subprocess.run(...).returncode`**（`GB-145`）；
**payload 一律 `Write` 落檔、⛔ heredoc**（`GB-143` 補款二）。
🛑 **本批⛔ 動生產碼一位元組**——`G4` 依單之令「不會 ⇒ **停並回報**」而停。

---

## 工項零　✅ PASS（二項）

### 一、新單原封入倉

| 端 | 位元組 | `LF` | `CR` | `sha256` |
|---|---|---|---|---|
| 落檔（工作樹） | **6294** | 92 | **0** | `24c382c3f661c52e9af85afe6c557aa8411182179e2d6ff9fb2d0d16036e1d1f` |
| 入倉（索引側 blob） | **6294** | 92 | **0** | `24c382c3f661c52e9af85afe6c557aa8411182179e2d6ff9fb2d0d16036e1d1f` |

🔒 **逐位相同 ＝ `True`**（位元組數差 ＝ `+0`·`戒 40` 二端並報）。`cat-file blob` 之 `rc` ＝ **0**。
🔒 對照組（器須紅）：翻首位元組 ⧧ blob ＝ `True`；對照組（器須綠）：blob 自比 ＝ `True`。
🛑 射程界：只證「**落檔→入倉**」，**⛔ 證「聊天原文→落檔」**。

### 二、舊單首行之作廢標記

檔 ＝ `docs/orders/W-G.9-219_退縮值常設顯示.md`：基座 **3562** B → 入倉 **3678** B，**Δ ＝ `+116`**
（＝ 標記本身 **116** B·`戒 40` 二端並報）。
🔒 **基座全文為入倉之嚴格後綴 ＝ `True`** ⇒ **⛔ 改其正文一位元組**；`CR` ＝ **0**。
🔒 標記逐字 ＝ `🗄️ 【作廢】本單已由 W-G.9-219R 取代（`W-G.9-219R` 工項零掛註·⛔ 改以下正文一字）。`
🔒 體例之出處（逐字比對，⛔ 自創）＝ `docs/orders/W-G.9-213_出艙數粒度框之補正.md:1`／
`docs/orders/W-G.9-217_201實算探針與Commit1期望值預登記.md:1`。

---

## `G1`　態錨（改前值）✅

`HEAD` ＝ `b7873bc00388dbee8ab76be72a36f0a785c557cc`；`left-right` ＝ **`0 0`**；
追蹤 ＝ **1616** 檔／`.md` ＝ **547** 檔（`ls-files -z`·已含本批二單）。

| 生產碼檔 | B | **改前** `sha256` |
|---|---|---|
| `app.py` | 1414673 | `1fc5de29721d45afa5eee88c657f5cafd3f1b663b9aa98b5e426d7d6f05bc28f` |
| `verify/selection_pipeline.py` | 35367 | `6c6783420dbc635c4c6b6eee1f1cab7054cc281a9733fd10e35dae41c8f25cbc` |
| `verify/run_verification.py` | 96923 | `e48f24c84079f3652606bdf8fb7333964bca8cceee976533afd511964dffbebd` |
| `verify/stepg_pipeline.py` | 96261 | `0260273350021311c7ba81821fc4bc66aace6ba84d31166f3645ceb4ec6601a5` |
| `verify/run_all.py` | 24681 | `4ba89fef909794910e5705af3880e0e9ca87208f663ad4c56f16e558936ebfcf` |

---

## `G2`　✅ **PASS（相符）**——`W-G.9-225` 之補登已生效

三軸：來源 `blob@b7873bc`／母體 `app.py` **1414673** B・**23906** 列／**列框**。
解析器**逐字沿用 `-219`**（⛔ 為使閘綠而調整任一方之清單）。

- 現態命中集 ＝ **12**：`[10947, 12319, 12675, 12677, 12680, 12703, 14259, 19684, 19689, 22359, 22848, 23810]`
- 補登後之 `GB-144` 所登集 ＝ **12**：同上
- 差集：`GB-144` ∖ 現態 ＝ `[]`／現態 ∖ `GB-144` ＝ `[]`

🔧 **本批量測器之當場更正**：性質分類器之啟發式「前二列含 `raise`」把 `app.py:12677` 誤判為「硬停」——
其前二列 `:12676` 係**註解**，內含「loud raise」字樣 ⇒ **假命中**。
**`:12677` 實為「讀取（無兜底·單引數 `.get`）」**。**⛔ 影響集合成員**（只影響性質欄）。

🔒 **下游入口（讀取 ∪ 無兜底消費端·更正後）＝ `[12677, 14259, 23810]`** —— 與單所指之三入口相符。

🔒 **併呈（全倉窮舉·⛔ 只查 `app.py`）**：`f3L_setback_default` 於索引側全部 `.py` **248** 檔中命中 **20** 列
（`app.py` **9** ＋ `verify/` **11**：`selection_pipeline.py:289`／`stepg_pipeline.py:272`／
`run_verification.py:1324`／`wg_g1_smoke.py:70`／`wg_g2_smoke.py:45`／`wg_g3.py:93`／
`probes/probe_WG9218R_setback_sensitivity.py` 五列）。

---

## `G3`　✅ 三項當場復現，皆與單所述相符

| 項 | 單所述 | 當場復現 | 判 |
|---|---|---|---|
| `a` | `:19671` 之 `expanded=False` | `app.py:19671` ＝ `with st.expander("⚙️ 街角地參數（共用設定 + 各街廓微調）", expanded=False):`（該 `st.expander("⚙️ 街角地參數…` 全檔命中 ＝ **1**）| ✅ |
| `b` | 插入點縮排層與 `:19658` 同 | `:19658` ＝ `st.markdown("---")` **ind ＝ 16**；`:19671` **ind ＝ 16** ⇒ 插入點（`:19670` 之後、`:19671` 之前）之敘述層 ＝ **16** | ✅ |
| `c` | `:19650 if not build_parcels` ／ `:19652 else` | `:19650` ＝ `if not build_parcels:`（ind 12）／`:19651` ＝ `st.info(…)`（ind 16）／`:19652` ＝ `else:`（ind 12） ⇒ **成對** | ✅ |

🔒 **併記（插入點之語法脈絡）**：`:19660`–`:19670` 係一個 `st.markdown("""…""")` 三引號字串
（`:19661`–`:19670` 之 ind ＝ 0 係**字串內容**、⛔ 敘述縮排）；`:19670` ＝ `""")` 為該呼叫之收尾
⇒ 「`:19670` 之後、`:19671` 之前」確為**敘述邊界**，插入合法。

---

## 🛑 `G4`　**停**——旗標**不會**落入該清除路徑之範圍

### 一、`app.py:16135`–`:16142` 之逐字（單所指之「整批 session_state 清除碼」）

```
:16134  ind=16   # 清除超出年期的殘留 key（避免 Streamlit widget 狀態污染）
:16135  ind=16   for _k in list(st.session_state.keys()):
:16136  ind=20       if _k.startswith('const_ratio_') or _k.startswith('replot_ratio_'):
:16137  ind=24           try:
:16138  ind=28               _idx = int(_k.split('_')[-1])
:16139  ind=28               if _idx >= _cur_ty:
:16140  ind=32                   del st.session_state[_k]
:16141  ind=24           except ValueError:
:16142  ind=28               pass
```

其所屬語境（`:16120`）逐字 ＝ `# ---------- 偵測開發年期變動：自動重置各年度比例 ----------`。

### 二、清除範圍之**判準**（單問「全清？依前綴？依白名單？」）

🔒 **答 ＝ 依前綴，且再加一個索引條件**：
`_k.startswith('const_ratio_')` **∪** `_k.startswith('replot_ratio_')`，**且** `int(_k.split('_')[-1]) >= _cur_ty`。

⇒ 🔴 **⛔ 全清**（僅二前綴）；🔴 **⛔ 白名單**（無名單可登錄）；
⇒ 🔴 **其二前綴與 `f3L` 無任何交集** ⇒ **`f3L*` 前綴之任何鍵⛔ 落入其清除範圍**。

### 三、判：**不會** ⇒ 依單之令**停並回報**

工項一 `a` 令「旗標鍵名須與退縮欄同前綴（`f3L…`），俾**落入 `G4` 之清除範圍**」——
該前提**不成立**：`f3L` 前綴之鍵**永遠**不被 `:16135` 之迴圈觸及。
單所留之逃生口「若判準為**白名單**則同時登錄」亦**不適用**（判準非白名單）。
唯一能使其落入者係**修改 `:16136` 之前綴條件**，而單逐字明令
「**⛔ 自行修改該清除路徑（其射程不在本單）**」。
🛑 ⇒ **`G4` 停。⛔ 進入工項一／二／三。⛔ 改一行生產碼。**

### 四、「另有其他清除／換案路徑」之**窮舉**（母體 ＝ 索引側全部 `.py` **248** 檔·**列框**）

| 形 | 命中列 | 逐處 | 是否及於 `f3L*` |
|---|---|---|---|
| `del …session_state[…]` | **1** | `app.py:16140`（即上開）| 🔴 **否** |
| `session_state.pop(` | **9** | `app.py:15252`（`_f3_push_consume`）／`:19677`–`:19680`（`_f3L_invalidate_g_cache`）／`:20926`／`:20957`／`:20958`／`:22218` | 皆為**具名鍵**、⛔ 前綴掃；其中 `:19680` 之 `f3L_corner_winners` 係**逐字具名**，⛔ 及新鍵 |
| `session_state.clear(` | **0** | — | — |
| `session_state.update(` | **0** | — | — |
| `list(session_state[.keys()])` | **1** | `app.py:16135`（即上開）| 🔴 **否** |
| `for k in session_state` | **2** | `app.py:22772`／`:23020`（皆**讀** `f3_G_values`）| 否（非清除）|
| `cache_*.clear(` | **0** | — | — |
| `session_state = {` | **1** | `verify/probes/probe_f2_stepg_invalidate.py:81`（**探針之假 st**·非 app 路徑）| 否 |
| 「換案／重置／清空…」× `session_state` | **0** | — | — |

🔒 **`f3L_setback_default` 本身是否曾被 `del`／`pop`（全倉）＝ 命中 `0`** ⇒ **該鍵在碼中從未被主動清除**。
🔒 **對照組（器須綠）**：全倉 `f3L_setback_default` 命中 ＝ **20** 列（> 0）。
🔒 **對照組（器須紅→0）**：一必不存在之同形鍵 ＝ **0** 列。

---

## 🔴 單之前提失真（**出艙候鑄·⛔ 由 CC 逕鑄號**）

單於 `G4` 逐字稱 `app.py:16135`–`:16140` 為「**整批** session_state 清除碼」，
並據以令工項一 `a`「旗標鍵名須與退縮欄同前綴，**俾落入 `G4` 之清除範圍**」。
🔴 **實測：該區係<u>財務分析頁</u>之「開發年期變動 ⇒ 重置各年度比例」之<u>局部</u>殘留鍵清理**
（其前綴 ＝ `const_ratio_`／`replot_ratio_`，且限於索引 `>= _cur_ty`），
**⛔ 「整批」、⛔ 及 `f3L*`**。

🔒 **族**：與 `自誤 272`（**以未經 `grep` 之交接態為單之<u>前提</u>**）同一度；
亦與同批所鑄之 `自誤 283`（**單所擬之修改對象與碼不符**）相鄰。
🔒 **推導已備（⛔ 逕鑄）**：自誤簿現態 `MAX` ＝ **283** ⇒ 次號 ＝ **284**。
🛑 **⛔ 由 CC 鑄之**——本單未授權鑄號；候發單側裁其是否成號、抑或以 `自誤 283` 之併記行之。

---

## 🔑 併呈：**真正的失效機制⛔ 在 app.py 之清除路徑**（唯讀分析·⛔ 工項一之執行）

KL 裁示「**被沖掉而回落預設者，來源即為『預設』**」所指之「沖掉」，其**實際成因**經本批勘查
**⛔ 為 app.py 內之任何清除碼**（上已窮舉為 `0`），而係 **Streamlit 自身之 widget 狀態生命週期**：

- 退縮欄 `st.number_input(..., key='f3L_setback_default', ...)` 在 `:19685`，
  位於 `:19652` 之 `else` 分支內 ⇒ **`build_parcels` 為空時該 widget ⛔ 被渲染**。
- Streamlit 對「本次 rerun 未被渲染之 widget」其 key 之狀態**可能被丟棄** ⇒
  下一次 `:19684` 之 `.get('f3L_setback_default', 3.5)` 即**回落 `3.5`**。
- 🔴 **此時，一個「持久布林旗標」會存活下來** ⇒ 顯示將**錯報**「使用者輸入」，
  而實際在用之值已是預設 **`3.5`** ⇒ **恰違 KL 裁示**。

🛑 ⇒ **單所設計之「持久旗標 ＋ 依賴清除路徑同步」此一路徑，即使 `G4` 為真亦不足**——
`:16135` 只在**財務頁之年期變動**時跑，而上開丟棄發生在**街角地區塊未渲染**時，二者**時點不同**。

### 候議之最小改動（**出艙·🛑 ⛔ 由 CC 擇一·候發單側／KL 裁**）

| 案 | 內容 | 是否動清除路徑 | 備註 |
|---|---|---|---|
| `甲` | 於 `:16136` 之前綴條件加入 `f3L_setback_` | 🔴 **會** | **單明令 ⛔** ⇒ 出艙備列、⛔ 採 |
| `乙` | 旗標改為**衍生**：來源 ＝ 「使用者輸入」⟺ `'f3L_setback_default' in st.session_state` **且** 旗標為真；且鍵不存在時就地丟棄過期旗標 | 🟢 **否**（新碼在顯示區）| **自我修復**；符 KL 裁示之二態 |
| `丙` | 旗標存**值**而非布林（`f3L_setback_user_value`），來源 ＝ 「使用者輸入」⟺ 該鍵存在且與現值相等 | 🟢 **否** | 同為自我修復；多一個鍵 |
| `丁` | 於 `_f3L_invalidate_g_cache`（`:19673`）設旗標，另於**未渲染分支**（`:19651`）就地清旗標 | 🟢 **否**（惟動到 `:19651` 之分支）| 需在 `if not build_parcels:` 內加一行 |

🔒 **CC ⛔ 表態於取捨**——上表僅陳「是否動清除路徑」與「是否自我修復」二可機驗之性質。

---

## 閘 `P`（生產碼五檔）✅ PASS ＝ 改前值

五檔逐檔 `sha256` 與上 `G1` 表**逐位相符**（本批**⛔ 動一位元組**）：
`app.py` `1fc5de29…`／`selection_pipeline.py` `6c678342…`／`run_verification.py` `e48f24c8…`／
`stepg_pipeline.py` `02602733…`／`run_all.py` `4ba89fef…`。

## `numstat`／`left-right`

| 檔 | 增 | **刪** |
|---|---|---|
| `docs/orders/W-G.9-219R_退縮值常設顯示.md`（新） | 92 | **0** |
| `docs/orders/W-G.9-219_退縮值常設顯示.md`（首行標記） | 2 | **0** |
| `docs/reports/W-G.9-219R_停機_前置閘G4.md`（**本報告**） | 🔒 **⛔ 於本報告內釘死**（自指）| **0** |

🛑 **`deletions` ＝ `0`（全檔）**——本批**⛔ 動任何工具檔、⛔ 動生產碼**。
🔒 `left-right`（推前）＝ **`0 0`**。

## 🔶 驗收出艙閘之現況：**本單無【驗收】段** ⇒ 不可回測（`序 11` 之<u>活體</u>首例）

`verify/tools/wg9223_acceptance_audit.py` 於本批之 `rc` ＝ **1**，其輸出逐字 ＝
「🔴 該單⛔ 有 `【驗收】` 段 ⇒ **不可回測**」——`W-G.9-219R` 之末段為【回報後之處置】，**⛔ 【驗收】**。

🔒 **此係 `W-G.9-225` 工項三 `c` 所落之待落地正本表 `序 11`（`CLAUDE.md` 檔尾）之<u>活體</u>首例**：
該閘之判定組於無【驗收】段之單上為**空**，故其設計已改為**loud 拒測**（`rc` ＝ `1`），
**⛔ 恆綠**——與 `-224R:254` 所記之 `11` 份（判定組為 `0` 而**靜默綠**）相比，本閘現行版本**不再假綠**。
🛑 **⛔ 由 CC 代擬本單之【驗收】段**（代擬即令閘之受詞與其守護者同源 ⇒ 判別力歸零）；
**⛔ 以此 `rc=1` 為本批之失敗**——其義為「**無從回測**」、⛔ 「回測不過」。

## 🟡 仍待

1. 🛑 **`G4` 之處置**——`f3L*` 旗標**不落**任何清除範圍；四候選（甲／乙／丙／丁）已出艙，**⛔ 由 CC 擇一**。
2. 🔴 **單之前提失真**（「整批 session_state 清除碼」）之鑄號與否——**候發單側裁**（次號 **284** 已備）。
3. 🔑 **真機制係 Streamlit widget 生命週期**（⛔ app.py 之清除碼）——此改變工項一之設計前提，須先裁。
4. ✅ `G1`／`G2`／`G3` 皆綠；`G2` 已由 `W-G.9-225` 之 `GB-144` 補登解鎖。
