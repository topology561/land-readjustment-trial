# 🧾 `W-G.9-205R` 執行報告：二檔原封入倉 ＋ `VR-088` ＋ `自誤 255` ＋ `CLAUDE.md` 更正註

> **執行窗**：CC　**開工態**：`origin/wip/s1-endpart` ＝ `829bdc98132792807067cb7e201fc615827ed72b`
> **性質**：🟢 **零生產碼**（`app.py` 與**既有** `verify/**.py` 逐檔 `0` 變更）
> **工作副本**：主 checkout `C:\Users\admin\Desktop\land-readjustment-trial`；一切 `git` 以 `git -C` 執行
> 🔴 **本批 ＝ 同批<u>二</u> commit**（`§二-b` 之互斥所致）：`2528884`（`§零-2` 所令之 `6` 檔）＋ 本報告之 commit

🔒 本檔一切計數皆於同段具名 ① 母體 ② 框 ③ **產生它的命令逐字**（`§六-2`）。
🛑 **本報告⛔ 鑄任何號**——`VR-088`／`自誤 255` 係**依單鑄於工項 `2`／`3`**、⛔ 本報告所鑄；`GB`／`K-9`／`戒` 皆未動。

---

## `§一`　`§六-1`　實際 `HEAD` 全 `40` 碼（**`push` 後之 `git ls-remote`**·非本機）＋ 量測時刻

| 受詞 | 命令逐字 | 值 |
|---|---|---|
| 量測時刻 | `date -u +"%Y-%m-%dT%H:%M:%SZ"` | **`2026-09-02T03:39:06Z`** |
| `push` 後遠端 `HEAD` | `git ls-remote https://github.com/topology561/land-readjustment-trial.git refs/heads/wip/s1-endpart` | **`2528884e76469ee6f80b20168eb6744631d059fc`** |
| `left-right` | `git -C <M> rev-list --left-right --count origin/wip/s1-endpart...HEAD` | **`0	0`** |
| `S-0a` `push` 後複驗 | `git -C <M> rev-parse HEAD:app.py` | **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** ＝ 單載 |

🛑 **本報告自身之 commit 於封版時尚未存在** ⇒ 其 `push` 結果**結構上⛔ 可能載於本報告**（同 `W-G.9-203R §七-1` 之形）；其落點 ＝ 完工 `ping` 與下一單之開工現查。**應成立之不變式**：`push` 後 `left-right` ＝ `0	0` 且 `ls-remote` ＝ 該 commit 之 SHA。

---

## `§二`　🔴 二項單內互斥（依 `常規二`：取保守項續辦 ＋ 逐字回報·⛔ 上呈 KL）

### `§二-a`　`§零-3`「全程 `-c core.autocrlf=false`」 vs append-only

**受詞** ＝ 單 `§零-3` 逐字：「🛑 全程 `git -c core.autocrlf=false -c core.quotepath=false`」。
**衝突**：追加之三檔（`docs/驗證裁定登記表.md`／`docs/reports/W-G.9波_claude.ai側自誤登記.md`／`CLAUDE.md`）之**工作區皆為混合 `CRLF`**（本機 `core.autocrlf=true` 之系統級設定所致）。

**唯讀實測**（⛔ 假設·命令逐字 `git -C <M> -c core.autocrlf=<X> hash-object --path <p> <p>`·**無 `-w`** ⇒ ⛔ 寫入 object DB）：

| 檔 | `false` 之 blob | `true` 之 blob | 二設定同 | 現行 `HEAD` blob |
|---|---|---|---|---|
| `W-G.9-198_…md`（新增） | `8132fede…` | `8132fede…` | **`True`** | `<HEAD 無此檔>` |
| `W-G.9-204_…md`（新增） | `700ac32f…` | `700ac32f…` | **`True`** | `<HEAD 無此檔>` |
| `W-G.9-205_…md`（新增） | `9ffdb661…` | `9ffdb661…` | **`True`** | `<HEAD 無此檔>` |
| `驗證裁定登記表.md`（追加） | `03bfcdd1…` | `605c2abd…` | **`False`** | **`605c2abd…`** |
| `W-G.9波_…登記.md`（追加） | `eee25922…` | `96de36d3…` | **`False`** | **`96de36d3…`** |
| `CLAUDE.md`（追加） | `1f2e4711…` | `20b6542e…` | **`False`** | **`20b6542e…`** |

🔒 **判讀**：新增三檔為**純 `LF`**（`CR` ＝ `0`）⇒ 二設定**同果**、`false` 零風險。
🔴 追加三檔若用 `false`，將把 **`4258`／`4528`／`1041`** 個 `CR` 寫入 blob ⇒ **全檔偽異動**、`deletions` 爆表、破 append-only 與 `S-8` 之形。

🛑 **保守項（CC 之處置）** ＝ **新增三檔用 `-c core.autocrlf=false`（依單明文·且實測同果）；追加三檔走常規 `add`（＝ `autocrlf=true`）**。
**實測結果**：三追加檔之 staged blob `CR` 皆 ＝ **`0`**（`git cat-file blob $(git ls-files -s -- <p> | awk '{print $2}')` ｜ `tr -cd '\r' | wc -c`）；`deletions` 逐檔 ＝ `0`。

### `§二-b`　`§零-2`「恰 `6` 檔·⛔ 得多」 vs `§六`「報告同批入倉」

**衝突**：`§零-2` 令變更檔清單**恰為 `6` 檔**、`⛔ 得多`（`S-3` 之 `1574`／`507` 亦逐字載「＝ 新增三檔：`198`／`204`／`205`」，**未計本報告**）；而 `§六` 令執行報告 `docs/reports/W-G.9-205R_…md` **同批入倉**。二者⛔ 並存於**同一 commit**。
🛑 **保守項** ＝ **拆為<u>同批二 commit</u>**：`2528884` 恰含該 `6` 檔（使 `§零-2` 與 `S-3` **逐字成立**），本報告以**另一零生產碼 commit** 入倉、同批同推。
🔑 **依據**：`§六` 逐字為「**同批**入倉」而**⛔ 是「併入同一 commit」**（`W-G.9-200`／`-203` 於需同一 commit 時皆逐字寫「併入同一 commit」）⇒ 二 commit 之解**⛔ 違反任一條文**。

---

## `§三`　`§六-2`　`S-0a`〜`S-9` 逐閘

| 閘 | 判準 | 實測 | 命令逐字 | 判 |
|---|---|---|---|---|
| `S-0a`① | `HEAD:app.py` ＝ `b9a50a2e…` | **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`**（開工時與 `push` 後**各一次**皆同） | `git rev-parse HEAD:app.py` | ✅ |
| `S-0a`② | 變更檔清單**恰 `6` 檔** | **`6`**【檔】：`A` `198`／`A` `204`／`A` `205`／`M` `驗證裁定登記表.md`／`M` `W-G.9波_…登記.md`／`M` `CLAUDE.md` | `git -c core.quotepath=false diff --cached --name-only -z \| tr '\0' '\n' \| sed '/^$/d' \| wc -l` | ✅ |
| `S-1` | 三新檔四值**入倉前 ＝ 入倉後** | 見 `§四` ⇒ **逐檔四值全同 ＝ `True`** | `open(p,'rb')` 四值 vs `git cat-file blob HEAD:<p>` 四值 | ✅ |
| `S-2` | `ls-tree` 對 `198`／`204` 各命中 `1` 檔；三列既有引用所指之檔現已存在 | 各 **`1`**【檔】；`git cat-file -e HEAD:<198>` `rc=0`／`<204>` `rc=0` ⇒ **存在**。🔒 **零對照**：`HEAD:docs/orders/NOSUCH.md` `rc=128` ⇒ 量測器⛔ 恆綠 | `git ls-tree -r --name-only -z HEAD` ／ `git cat-file -e HEAD:<p>` | ✅ |
| `S-3` | 追蹤 `1571`→**`1574`**；`.md` `504`→**`507`** | 落地前 **`1571`／`504`**；`2528884` 後 **`1574`／`507`** ⇒ 逐值相符 | `git ls-tree -r --name-only -z HEAD \| tr '\0' '\n' \| sed '/^$/d' \| wc -l` ／ `\| grep -c '\.md$'` | ✅ |
| `S-4` | `VR` 相異 `72`→**`73`**、MAX `87`→**`88`** | 落地前 **`72`／`87`**；後 **`73`／`88`** | 框 `` ^#+ `?VR-([0-9]{1,4})`? ``·母體 `docs/驗證裁定登記表.md` 單檔·扣 `{999, 751}`（python `re.match` 逐列） | ✅ |
| `S-5` | 自誤 相異 `219`→**`220`**、MAX `254`→**`255`** | 落地前 **`219`／`254`**；後 **`220`／`255`** | 框 `` ^#+ 自誤 `?([0-9]{1,4})`? ``·母體 該單檔 | ✅ |
| `S-6` | `GB` ⛔ 得變：MAX 仍 ＝ `138` | 相異 **`52`**／MAX **`138`** | 框 `` ^#{2,4} `?GB-([0-9]{1,4})`? ``·母體 `docs/reports/W-G.4_泛用阻塞項登記表.md` 單檔·扣 `{997}` | ✅ |
| `S-7` | 🔴 **append-only 之判別力自證** | 見 `§五`（🔴 **首測係量測器紅**·受詞以二獨立法證綠） | 見 `§五` | ✅ |
| `S-8` | `K-6` bytes／列 ⛔ 得變（`269196` B／`3738` 列） | **`269196`** B／**`3738`** `\n` ⇒ 逐值不變 | `git cat-file blob HEAD:docs/rulings/K-6_…md` ｜ `len()` ／ `count(b'\n')` | ✅ |
| `S-9` | pre-flight 逐項**以字樣錨具名**（⛔ 用行號）；紅項逐筆歸類 | 見 `§六` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-205_施工單_二檔入倉與VR088與自誤255.md` | ✅ |

🔒 **`V-2` 覆驗**（`git -c core.quotepath=false diff --numstat 829bdc9..HEAD`·`2528884` 態）：`deletions` **逐檔 ＝ `0`** —— `CLAUDE.md` `+8/-0`／`W-G.9-198_…` `+175/-0`／`W-G.9-204_…` `+227/-0`／`W-G.9-205_…` `+168/-0`／`W-G.9波_…登記.md` `+16/-0`／`驗證裁定登記表.md` `+13/-0`。
🔒 **生產碼判法**（`CLAUDE.md 🔧 二`·逐 commit·**空輸出亦出艙**）：`git diff --cached --name-only -z | tr '\0' '\n' | grep -xE 'app\.py|verify/stepg_pipeline\.py|verify/run_all\.py|verify/run_verification\.py'` ⇒ **輸出為空**、命中 **`0`**【列】；**判別力對照** `.md` ＝ **`6`**【列】；**零對照** `*.py` ＝ **`0`**【列】。

---

## `§四`　`§六-3`／`S-1`　三新檔之四值與自簽自驗（宣告值與重算值**二者並列**）

**四值**（`bytes` ／ `\n`【次】 ／ `CR`【次】 ／ `sha256`）·命令逐字：
`python -c "import hashlib,sys;b=open(sys.argv[1],'rb').read();print(len(b),b.count(b'\n'),b.count(b'\r'),hashlib.sha256(b).hexdigest())" <檔>`（入倉前）／同式對 `git cat-file blob HEAD:<檔>`（入倉後）

| 檔 | 時點 | bytes | `\n` | `CR` | `sha256` |
|---|---|---|---|---|---|
| `docs/orders/W-G.9-198_…md` | 入倉前 | `18865` | `175` | **`0`** | `5ded02aa16fc4a87fa411a98253e8cd1ba9e1842a4b55c286049711ac78bd200` |
| 同上 | 入倉後 | `18865` | `175` | **`0`** | 同上 ⇒ **四值全同 ＝ `True`** |
| `docs/orders/W-G.9-204_…md` | 入倉前 | `20189` | `227` | **`0`** | `1ec023f750c2abb82703ad3459a2495a4021b1146e3fe52d1cf232d4ae20b70f` |
| 同上 | 入倉後 | `20189` | `227` | **`0`** | 同上 ⇒ **四值全同 ＝ `True`** |
| `docs/orders/W-G.9-205_…md` | 入倉前 | `10963` | `168` | **`0`** | `2d82e7b4c6e25260da35048ded56a5cda4ea7471094e995a377c17890ba974e1` |
| 同上 | 入倉後 | `10963` | `168` | **`0`** | 同上 ⇒ **四值全同 ＝ `True`** |

### `§四-1`　`SELF_SHA256` 自簽自驗（🛑 **自算**·⛔ 照抄單載之前綴）

| 檔 | 自簽列（`grep -n '^SELF_SHA256'`） | **宣告值** | **重算值**（`probe_order_preflight` `P-5`·受詞 ＝ 自檔首至該列前一個 `\n` 含之） | 判 |
|---|---|---|---|---|
| `W-G.9-204_發單側交接文.md` | `:227` | `3ecd03ecfcec0fdca6ed0dad0202a103a7eb9d009a91e4064061f958ab1e4329` | `3ecd03ecfcec0fdca6ed0dad0202a103a7eb9d009a91e4064061f958ab1e4329`（受詞 `20111` B） | ✅ **相符** |
| `W-G.9-198_施工單_…md` | `:175` | `f16bdb9e5c52b4040fd7ebb73dcb9776c9c19b8f98908f94be8caea1e88695ec` | `f16bdb9e5c52b4040fd7ebb73dcb9776c9c19b8f98908f94be8caea1e88695ec`（受詞 `18787` B） | ✅ **相符** |

🔒 `§一-5` 之「若無該列，據實報『無自簽列』」**未被觸發**——`W-G.9-198` **有**自簽列（`:175`）。
🔒 單 `§一-4` 所載之前綴 `3ecd03ec…` 與 CC 自算之前綴**一致**，惟本值係 CC **自算**所得、⛔ 照抄。

### `§四-2`　三 payload 之落地（🔴 `§六-5` 之新戒·首次適用：**`Δ` 與 payload 二端之位元組數同格並報**）

| 工項 | 落地檔 | payload bytes | payload `sha256` | before B | after B | **Δ B** | 所補之位元組（逐檔具名） |
|---|---|---|---|---|---|---|---|
| `2` `VR-088` | `docs/驗證裁定登記表.md` | **`1060`** | `9eba9e36fc0e423cc1cdeee83051d94d0f726b8c97a511cbec0fb6ed330137fb` | **`345303`** | **`346365`** | **`1062`** | 前導 `b'\n'`（`1` B）＋ 收尾 `b'\n'`（`1` B）⇒ `Δ ＝ payload(1060) + 補(2)` |
| `3` `自誤 255` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | **`1407`** | `f6692fa3f29470c5fcf23c8661fb3ee42176dbc29e83ece0001020acd1c5647d` | **`469579`** | **`470988`** | **`1409`** | 前導 `b'\n'`（`1` B）＋ 收尾 `b'\n'`（`1` B）⇒ `Δ ＝ payload(1407) + 補(2)` |
| `4` 更正註 | `CLAUDE.md` | **`784`** | `563a1e924ce9be41ee0607ae4460f2e09ff8f20bd8e0e44b7b3261a0fe6475fc` | **`115203`** | **`115988`** | **`785`** | 前導 `b''`（`0` B·**payload 自帶前導 `\n`**）＋ 收尾 `b'\n'`（`1` B）⇒ `Δ ＝ payload(784) + 補(1)` |

🛑 **⛔ 宣稱「`Δ` 恰 ＝ payload」**——三檔皆有補位元組，其量與內容已逐檔具名（此即 `§六-5` 所立之戒；其案由 ＝ `W-G.9-203R` 之不精確）。
🔒 **fence 邊界自證**：單內行首 `` ``` `` 共 **`8`**【列】，其中 `` ```markdown `` **`3`**【列】（`:62`／`:95`／`:119`）⇒ 三對邊界⛔ 有歧義。
🔒 **逐檔位元組級自證**（落地當下）：`嚴格前綴 ＝ True`／`檔尾（扣收尾補位）逐位 ＝ payload ＝ True`／`仍以 \n 收尾 ＝ True`／`哨兵命中 ＝ 1`（三檔皆同）。

---

## `§五`　`S-7`　🔴 **首測係量測器紅**（`常規五`）——受詞以二獨立法證綠

**單 `S-7` 所令之字樣錨（逐字）** ＝ `四框皆` 須命中 **`1`** 列 ∧ `W-G.9-205 更正註` 須命中 **`1`** 列。
**首測**（命令逐字 `git cat-file blob HEAD:CLAUDE.md` ｜ python `sum(1 for ln in ... if <錨> in ln)`）：`四框皆` ＝ **`2`**／`W-G.9-205 更正註` ＝ **`0`** ⇒ **二者並存 ＝ `False`**。

🔒 **依 `CLAUDE.md`「⛔ 停機上呈之前，必先自證『量測器非紅』」先查量測器**：

| 錨 | 命中 |
|---|---|
| `W-G.9-205 更正註`（單所令·**無反引號**） | **`0`**【列】 |
| `` `W-G.9-205` 更正註 ``（**落地之實際字樣·帶反引號**） | **`1`**【列】 |
| `更正註`（最寬） | **`1`**【列】 |

⇒ 🔴 **係「錨與落地字樣不符」之量測器紅**（payload 之標題逐字為 `` ### 🔒 `W-G.9-205` 更正註　`GB` 取號之框與 `MAX` ``），**⛔ 受詞紅**。

**受詞之真狀**（逐列具名）：`HEAD:CLAUDE.md` 之 `四框皆` 命中 **`2`**【列】——
① **既有偽句**（`W-G.9-203` 所落）：`🔒 **\`GB\` 之取號**（承 \`GB-138\`）：\`MAX\` ⛔ 受框差影響（四框皆 \`137\`）…`
② **更正註內之引用**：`本檔前文載有逐字「**\`MAX\` ⛔ 受框差影響（四框皆 \`137\`）**」——該句**已於 \`2026-09-02\` 由 \`自誤 255\` 更正**：`
⇒ 單載「須 `1`」**未計及其自身 payload 對該句之引用** ⇒ 該計數為**單載之誤**，⛔ CC 之施工誤。

🔒 **`S-7` 之 operative 判準（append-only 而非就地改）以二獨立法證綠**：

| # | 法 | 命令逐字 | 結果 |
|---|---|---|---|
| ① | **既有偽句逐字仍在** | 取 `git cat-file blob 829bdc9:CLAUDE.md` 之含 `四框皆` 之列（**`1`**【列】），驗其**逐字**存在於 `HEAD:CLAUDE.md` 之列集合 | **`True`** |
| ② | **位元組級嚴格前綴** | `HEAD:CLAUDE.md` blob 之前 **`114162`** B ≡ `829bdc9:CLAUDE.md` blob **全文** | **`True`**；其後 **`785`** B ＝ `§四-2` 之 `Δ` |
| ③ | 新增節之標題列 | `sum(1 for ln in cur if ln.startswith("### 🔒 \`W-G.9-205\` 更正註"))` | **`1`**【列】 |

🔒 **零對照**（人造字樣·應 `0`）：`四框皆XYZ` 命中 ＝ **`0`**【列】 ⇒ 量測器⛔ 恆非零。
🛑 **⛔ 就地修那句偽述**（單 `§五` 末之明令）——其仍在 `CLAUDE.md`，由本批之 `自誤 255` 與 `§四` 之更正註於**檔尾**更正之。

---

## `§六`　`S-9`　pre-flight 逐項（🛑 **以字樣錨具名·⛔ 用行號**）

**命令逐字**：`PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-205_施工單_二檔入倉與VR088與自誤255.md`

| 款 | 實測 | 逐項字樣錨（`grep -F` 固定字串） | 歸類（裁 `H`） |
|---|---|---|---|
| 🔴 機械·停機款（`P-3`／`P-5`） | **`0`**【項】 | — | ⛔ 紅項 ⇒ ⛔ 須歸類 |
| 🟡 提示 | **`1`**【項】·`[P-1]` | `` 🔑 **入倉之附帶效果（S-2 會驗）**：下列 `3` 列既有引用將由「查無此檔」轉為可驗證—— `` | 🔒 **具名處置**：該列之「全稱否定」＝ 引號內之**「查無此檔」**，其母體已由同段逐字列出之**三個檔路徑**界定（`W-G.9-197R`／`W-G.9-199R_CC交接文`／`W-G.9-199R_四自誤…執行報告`），⛔ 對倉之全稱否定；且 `S-2` 已機械驗之 |
| ℹ️ 記錄 | **`2`**【項】 | `P-5` 自簽相符（實算 ＝ 單載 ＝ `e4951e249e758c39…`·受詞 `10885` B）；`P-3` `def main` ＝ `14242`-`23460` | — |

🔑 **判別力自證**（同器·CC 本窗實跑）：對 `W-G.9-201` ＝ **`1`**【項】／`W-G.9-203` ＝ **`0`**【項】 ⇒ 該器**⛔ 恆綠亦⛔ 恆紅**。
🛑 CC 實跑**⛔ 出現表外之 🟡**。

---

## `§七`　`§六-4`　`§三` 四框之**自行復算**（🛑 ⛔ 照抄單表）

**母體** ＝ `docs/reports/W-G.4_泛用阻塞項登記表.md` **單檔**·**扣哨兵 `GB-997`**·態 ＝ `829bdc9`（＝ 單所載之態）
**命令逐字** ＝ `git cat-file blob 829bdc9:docs/reports/W-G.4_泛用阻塞項登記表.md` ｜ python 逐列 `re.match`（末列 `GB-([0-9]{1,4})` 用 `re.finditer` 全文掃描）

| 框 | CC 復算 相異 | 單載 相異 | CC 復算 MAX | 單載 MAX | 判 |
|---|---|---|---|---|---|
| `` ^## `GB-([0-9]{1,4})` `` | **`3`** | `3` | **`83`** | `83` | ✅ |
| `` ^#{2,4} `?GB-([0-9]{1,4})`? `` | **`52`** | `52` | **`138`** | `138` | ✅ |
| `` ^#+ .*?GB-([0-9]{1,4}) `` | **`61`** | `61` | **`138`** | `138` | ✅ |
| `GB-([0-9]{1,4})`（全文） | **`141`** | `141` | **`883`** | `883` | ✅ |

⇒ 🔒 **四行逐值與單載相符** ⇒ `自誤 255` 之受詞（「`MAX` 在**所有標題錨定框**下恆 ＝ `138`」為偽，因 `` ^## `GB-N` `` 之 MAX ＝ `83`）**坐實**。
🛑 **⛔ 出艙「`GB` 之相異／缺號集」作為結論**——本表係 `自誤 255` 之**證據**，四行**皆同格載明其框**（`GB-138` 之現況處置①）。

---

## `§八`　⛔ 本報告不辦

⛔ 不動 `app.py`／既有 `verify/**.py` 一字。
⛔ 不就地改 `CLAUDE.md` 內之既有偽句「四框皆 `137`」（單 `§四` 明令 append-only；`S-7` 即其閘）。
⛔ 不改 `W-G.9-198`／`W-G.9-204` 二檔任何一字（原封入倉）。
⛔ 不辦 `GB` 定義列式之釘框正典化（`GB-138` 處置③ 攢批中）。
⛔ 不鑄 `GB`／`K-9`／`戒` 號；⛔ 追改任何既有已出艙之數（`常規四（九）五`）。
⛔ 不辦 `K-9-24`／`K-9-25` 之碼面落地（由重發之 `W-G.9-198R` 辦）。
