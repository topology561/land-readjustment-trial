# W-G.8 D-2b-26：`GB-54` 方向更正 ＋ `K-9-5-9` 原話並列（⛔ 全程零生產碼）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`　**基準**：`aec849a`
**編號現查**：`grep -rn "D-2b-26" .`（排除巢狀 worktree）⇒ **0 命中** ⇒ 可用。

---

## §5-3 生產碼零觸自證（**原樣貼**）

```
$ git diff --stat -- app.py verify/
```
（**無輸出** ⇒ 對 `app.py`／`verify/` 之變更為空。）

---

## §1 `GB-54` 修法方向更正

### 1-1 何以原方向不合裁

`GB-54` 原載「**應改 `app` 向 `stepg` 對齊（改為停機）**」。
該方向與**同 commit 落地**之正典 `K-9-5-9`【裁】3 **相衝突**：

**【裁】3 逐字**（🔒 現查自
`grep -n "^\*\*【裁】3\." docs/rulings/K-6_街角地分配程序與可分配判準.md` **複製**·⛔ 非轉抄）：

> **【裁】3.** 檢查**一律跑完全部街廓**、把問題一次列齊；但只要有任何一處未過，
> **分配成果一概不產出**。

**衝突之機制**：`stepg` 之 `raise` 使**首個街廓失敗即整趟中止**
⇒ **R2–R6 永不受檢** ⇒ 直接違反【裁】3 **前半**「一律跑完全部街廓」。
（該中止之實測見 `GB-52`：`grep -c "街廓 R[2-6]" verify/out/D2b23B_runall.log` ⇒ **0**。）

### 1-2 已寫入之更正（逐字·落於 `GB-54` 之列內·⛔ 原句不刪）

> 🔴 **【方向更正·D-2b-26】現行兩種行為（app 警告後續跑／stepg raise 中止）皆不合裁。**
> 合裁之目標行為為**第三種**：
> ①逐街廓失敗 → 記錄後**繼續檢查其餘街廓**（【裁】3 前半）；
> ②全部檢查完 → 只要有任何一處未過，**分配成果一概不產出**（【裁】2 ＋【裁】3 後半）。
> ⛔ **不得把 `stepg` 降為警告；⛔ 亦不得把 `app` 改為首個失敗即中止。**

並於同列附【裁】2／【裁】3 之**現查逐字**，及
「⛔ **原句保留供對照，⛔ 不得據以施工**」之明文。

### 1-3 交叉指認 `GB-56`（⛔ 使同表兩條不再互相打架）

`GB-56` 之判讀逐字為「現行為『**部分產出**』，與該二裁**方向相反**」
——與本更正**指向同一目標行為**（跑完全部 ＋ 一處未過即全不產出）。
⇒ 已於 `GB-54` 列內加 🔗 交叉指認。

### 1-4 §5-1 驗收：**一次 grep 同時命中**

| 標的 | 指令 | 行號 |
|---|---|---|
| 原句 | `grep -n "應改 \`app\` 向 \`stepg\` 對齊" <登記表>` | **92** |
| 更正註 | `grep -n "方向更正·D-2b-26" <登記表>` | **92** |

⇒ **同一行** ⇒ 任一命中該列之 grep 皆同時見到原句與更正註。✅

⚠️ **`git diff --numstat` 於登記表顯示 `1` 增 `1` 刪**——係**單行表格列**之必然
（整列為一行，改列即計一刪一增）；**內容為純加性**：原句 grep 仍命中（§1-4）。

---

## §2 `K-9-5-9` 原話並列（⛔ 既有八條一字不動）

**落點**：`K-9-5-9` 節末新增小節「🔒 **原話對照（KL 2026-08-10·D-2b-26 補入**）」。
**地位（已逐字寫入）**：上方八條係 **claude.ai 之形式化文字**；本小節為 **KL 原話**；
**二者衝突時，以<u>原話</u>為準。**

### 2-1 (a)【裁】2 之限定語

**KL 原話**（逐字）：

> 「有宗地互疊就不產出成果⋯這個不就是程式錯誤不能用了，這配地程式不應該有這樣的情況」

**差額**：原話為「**有**宗地互疊」（⛔ 未設量之門檻）；形式化文字加了
「**超出捨入容許量者**」之限定 —— 該限定係 claude.ai **依閘之設計**所加。
⇒ **以原話為準時射程較寬**。已逐字寫入之戒：
⛔ **實作時不得逕以現行閘之上界（`(宗數−1)×0.005×深度`）當作「容許量」之正當性依據；
該上界之地位須另行請裁。**

### 2-2 (b)【裁】4 之漏列理由句

**KL 原話**（逐字·八條轉錄時**漏列**·於此補回）：

> 「現在末端塊土地因為深度呈現線性遞增或遞減」

⇒ 該句為【裁】4 之**理由**，與【裁】6 之「FRONTLINE 與 BASELINE 不平行、但二者皆為直線
⇒ 距離沿深度呈線性遞增／遞減」**同一機制**。
🔒 已明載「⛔ **前次轉錄漏列，非 KL 未說**」。

### 2-3 §5-2 驗收：八條逐字未動

```
$ git diff -U0 -- docs/rulings/K-6_街角地分配程序與可分配判準.md | grep -c "^-[^-]"
0
$ git diff --numstat -- docs/rulings/K-6_街角地分配程序與可分配判準.md
24      0      docs/rulings/K-6_街角地分配程序與可分配判準.md
```

⇒ **刪除行數 0**、該檔**僅見新增** ⇒ 八條之文字與更正前版本**逐字相同**。✅

---

## §4 停機條件逐項

| # | 條件 | 現查 | 判 |
|---|---|---|---|
| 1 | 倉內查得**更晚之裁示**與 §1 之更正相反 | `grep -rn "KL 裁 2026-08-1[1-9]" docs/rulings/ \| wc -l` ⇒ **0** | ❌ 未觸發 |
| 2 | 為完成本單而需修改任何生產碼 | §5-3 之輸出為空 | ❌ 未觸發 |

---

## §3 不授權逐項自檢

| # | 項 | 狀態 |
|---|---|---|
| 1 | `app.py`／`verify/**` 一字不動 | ✅（§5-3） |
| 2 | 不刪 `K-9-5-9` 既有八條任何一字、不刪 `GB-54` 原句 | ✅（§2-3 刪除 0／§1-4 原句仍命中） |
| 3 | 不實作 `GB-53`〜`GB-57` 之任何修法 | ✅ 未動碼 |
| 4 | 不接線 `k97_solve_alloc_t`／`_block_partition` | ✅ 未動 |

---

## 五、異動清單

| 檔 | 異動 | 純加性？ |
|---|---|---|
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `GB-54` 列內加方向更正 ＋ 交叉指認 `GB-56` | ✅ 內容純加性（`1`增`1`刪係單行表列之必然） |
| `docs/rulings/K-6_街角地分配程序與可分配判準.md` | `K-9-5-9` 節末加「原話對照」小節 | ✅ `24` 增 `0` 刪 |
| `docs/reports/W-G.8_D-2b-26_GB54方向更正與原話並列.md` | 新增（本檔） | 新檔 |

---

## 六、未完成／待決

1. 🛑 **【裁】2 之「容許量」門檻地位未裁**——原話未設門檻，形式化文字設了；
   §2-1 已載「該上界之地位須另行請裁」。
2. `GB-53`〜`GB-57` 皆**已登記未修**（本單零生產碼）。
3. `D-2b-25` §2 之**圖證仍待補**。
4. `K-9-5-9`【裁】1 之未裁事項（本波是否停機、`D-3` 重烤時點）**仍待請裁**。
