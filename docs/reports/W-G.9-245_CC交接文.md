# `W-G.9-245`　CC 新 session 交接文（`2026-09-06`）

> 🔑 **開窗前四件事見 `§八`**。本文一切數字**現查於倉**；**⛔ 採本文之數**——每一節皆須重跑。
> 🛑 本文**⛔ 鑄任何號**（自誤／`GB`／`VR`／戒／`K-9` 一律不鑄）。

---

## §一　態錨（**開窗第一動作 ＝ 重跑本節**）

| 項 | 值（現查於 `2026-09-06`）|
|---|---|
| 主線 `origin/wip/s1-endpart` | **`ffb05446dff09bb92233d5809bd74f18da87b49a`** |
| 🔴 **未併之側分支** | **`verify/W-G.9-244-c1` ＝ `6b5fe718a0d53d07b86a460d520037affa555a1b`**（`K-9-1` 資料層·生產碼·**候發單側復驗 → KL 放行**；本地保全 ref ＝ `keep/W-G.9-244-c1`）|
| 已併之側分支（⛔ 刪·復驗痕跡）| `verify/W-G.9-198R-c1`／`-c2`／`-c3`／`-219R2-c1`／`-229-c1`／**`-243-c1`** |
| 舊分支（⛔ 工作分支）| `main` ＝ `6cb77b7`／`claude/wg9-95z-v3-resume-hgpzfs`／`wip/W-G.4-S0b-S0c` |
| 追蹤 ／ `.md` ／ `docs/` ／ `docs/**.md` | **1664** ／ **589** ／ **567** ／ **539** 檔（`git ls-tree -r -z`）|
| `CLAUDE.md` | **188031** B／**2285** 列／`sha16 bd8f13e7a4209695` |

**生產路徑十檔之 `sha256` 前 16 位（`blob@ffb0544`·閘 `P` 五檔 ＋ `wf_f*` 五檔）**

| 檔 | 前 16 位 | 檔 | 前 16 位 |
|---|---|---|---|
| `app.py` | `e3e464ea2493d046` | `verify/wf_f0.py` | `6758ea766b001b95` |
| `verify/selection_pipeline.py` | `6c6783420dbc635c` | `verify/wf_f1.py` | `30e19048dfd781bc` |
| `verify/run_verification.py` | `e48f24c84079f365` | `verify/wf_f2.py` | `f974724d7e694ad7` |
| `verify/stepg_pipeline.py` | `0260273350021311` | `verify/wf_f3.py` | `226c7c5fa00464f3` |
| `verify/run_all.py` | `4ba89fef90979491` | `verify/wf_f4.py` | **`b36aeca6c4f6b364`**（`-243` seam 抽出後）|

🛑 **閘 `P` 之五檔<u>不含</u> `wf_f*.py`**——該五檔**無雜湊閘**，已登記為待落地缺口（`GB` 簿·`W-G.9-243 §六 f`）；
現行以逐批 `V-3`／`S-3` 併報代之。**引用十檔時須分清二組。**

🔴 **工作副本 ＝ 主 checkout** `C:\Users\admin\Desktop\land-readjustment-trial`（**⛔ worktree**·KL `2026-09-01` 裁）。
🔴 **本波實際施工於短路徑拋棄式 clone** `C:/Users/admin/AppData/Local/Temp/w240`（`core.autocrlf=false`）
——長跑（`run_verification`）**須**於此，⛔ 主 checkout（`run_all` 會改寫已追蹤 log）。

**近 8 commit**

```
ffb0544 W-G.9-244R2 段甲′：改版之差之落地（舊版作廢標記 ＋ v3.md ⑧⑪ 更正 ＋ GB 簿第二則）⛔ 零生產碼
799da5e W-G.9-244R 段甲：K-9-1 域裁入典（六款）＋ 自誤 297／298 ＋ GB-143 範圍限定 ⛔ 零生產碼
845d08d W-G.9-243R 段乙：排序準據 seam 抽出（射程乙·四站）🔴 生產碼
90157ee W-G.9-243R 段甲：宣告框三數分列 ＋ 級之影響面清單 ＋ 具名 key 解析表 ＋ 自誤 295／296 ⛔ 零生產碼
135316e W-G.9-242R：宣告框 D3 邊界補款 ＋ 排序準據母體改 AST ＋ 五級依賴序入典 ＋ 自誤 293／294 ⛔ 零生產碼
5f5cdcb W-G.9-242 工項零：宣告框查號 ＋ S-1′ 逐筆落點（恰一列·⛔ 觸停機）＋ 本單原封入倉 ⛔ 零生產碼
cb4b82f W-G.9-241R：排序準據 seam 偵察 ＋ 交接文 §七 更正 ＋ 自誤 292 ⛔ 零生產碼·⛔ 抽 seam
b4ca330 W-G.9-241 工項零：宣告框查號（0／0·未占用）＋ 本單原封入倉 ⛔ 零生產碼
```

---

## §二　次號（**現查·⛔ 採本文之數，開工前重算**）

| 簿 | 現況（`blob@ffb0544`）| 次號 |
|---|---|---|
| 自誤（`docs/reports/W-G.9波_claude.ai側自誤登記.md`）| blob `1ee6616ce498`·**582314** B／**6729** 列；定義框列命中 **292**／重咬 **3**／純單筆 **289**；範圍框 **8**；相異 **291**／`MIN` **7**／`MAX` **298**／缺號 **`[106]`**；恆等式 `True`；盲區 **0** | **`299`** |
| `GB`（`docs/reports/W-G.4_泛用阻塞項登記表.md`）| **標題錨定框** `MAX` **150**（窄框命中 67／寬框 150）| **`GB-151`** |
| `VR`（`docs/驗證裁定登記表.md`）| 標題形 `MAX` **091**（`999` ＝ 哨兵·已扣）| **`VR-092`**（三位補零）|
| `K-9`（`docs/rulings/K-6_…md`）| 標題形 `MAX` **`K-9-28`** | **`K-9-29`** |
| 戒 | 🔴 **本文未以正典定義列式復算** | **⛔ 採用任何數·開工時須以 `CLAUDE.md` 之「`戒` 之定義列式與其三類受詞」節之框現查** |
| 單號 | `W-G.9-245` 宣告框 `D1 0`／`D2 0`／`D3 0` ⇒ **`0`／`0`**（鬆框 `3`／`3` ＝ `-244` 諸檔之前瞻委派·依 `②`／`⑤` ⛔ 占用）| **`W-G.9-246`**（`246` 現查 `0/0`）|

🛑 **`GB` 之「相異／缺號集」⛔ 出艙**（`CLAUDE.md` 字樣錨 `在其釘定前，⛔ 得出艙`）。
🔒 **自誤之定義框（正字 ＝ 候選 `A`·`VR-091` 補款二）**：
`` ^#+[^0-9０-９（(\n]*?自誤[\s　`]*`?([0-9]{1,4})`? ``；**範圍框須<u>展開</u>後併入相異集**；
**缺號清單恆為 `[106]`**；**缺號母體 ＝ `[min(命中集), MAX]`**（⛔ `[1, MAX]`）。
🔒 **恆等式** `MAX − MIN + 1 − 缺號數 == 相異` **為現行閘**（`-242` 起）。
🔒 **鑄號前須取該類<u>既有最末一則</u>之標題形逐字為範本**（`常規四（九）四`·⛔ 自創形）
——自誤簿之現形 ＝ `## 自誤 \`NNN\`　**…**`。

---

## §三　工作紀律（最高優先·違反即停機）

1. 🔒 **未 push 不得報收官**——前置 ＝ `git ls-remote origin wip/s1-endpart` 已含該 commit（**⛔ `origin/main`**）。
2. 🔒 **自評 gate 綠⛔ 替代獨立復現**；**`--selftest` 綠亦⛔ 蘊含閘已生效**。
3. 🔒 **倉內報告 ＝ 唯一本體**；聊天 ＝ ping。push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑，**別無他物**。
4. 🔒 **數字錨一律引倉檔**，**⛔ 憑記憶出艙**；與倉態衝突以倉為準。
5. 🔒 **工項零優先於一切**——單一律原封入倉 `docs/orders/`，驗「落檔 `sha256` ＝ 入倉 blob」。
   其**前**另有一步：**入倉<u>前</u>先查該單號之占用（宣告框·三數分列）**——見 `§四`。
   🛑 該 `sha256` 閘只證「落檔→入倉」，**⛔ 證「聊天原文→落檔」**。
6. 🔒 **意思決定⛔ 自裁**——標旗呈 KL（**經發單側**）。
7. 🔒 **⛔ 追改任何既出之數／既有標題形**（`常規四（九）五`）；append-only 檔 `deletions ＝ 0`。
8. 🔒 **常規補款一**：閘之量測與其所守護之動作（commit／push）**⛔ 置於同一命令塊**。
9. 🔒 **常規五**：每一量測須有**兩造對照組**（器須綠 ＋ 器須紅）。
10. 🔒 **凡宣稱某閘「已落地」者，其<u>活體實測</u>為必要條件**；且**「閘已落地」⇏「閘於本 session 生效」**（見 `§四` 末）。

### 🔴 生產碼 commit 之流程（**⛔ 逕推主線**）

```
備妥（本地 commit·獨立於零生產碼批）→ ⛔ 推主線
  → 推側分支 origin/verify/<單號>-c<n> ＋ 本地保全 ref keep/<單號>-c<n>
  → 本地分支 git reset --hard origin/wip/s1-endpart（⇒ 下一批⛔ 可能誤推）
  → 發單側逐位復驗 → 請示 KL → KL 逐字放行
  → 合併：**純快轉**（`git push origin <sha>:refs/heads/wip/s1-endpart`）·**⛔ merge commit**
  → 併後報：主線 SHA／十檔雜湊／全部遠端 heads 逐一列示；**⛔ 刪側分支**（復驗痕跡）
```
🔒 **放行之受詞 ＝ 已復驗之<u>內容</u>，⛔ 某一 SHA**。
🔒 **`-243` 之既例**：KL 逐字放行後以 `845d08d:refs/heads/wip/s1-endpart` 直推行快轉，
併後以 `rev-list --merges` ＝ `0` ＋ 父數 ＝ `1` ＋ 祖先鏈給證「⛔ merge commit·⛔ 改寫」。

---

## §四　現行之閘與其框（**開工前逐一重跑**）

| 閘 | 母體／框 | 現況 |
|---|---|---|
| **閘 `P`** | **五檔**（`app.py`／`selection_pipeline`／`run_verification`／`stepg_pipeline`／`run_all`）之內容 `sha256` | 零生產碼批須前後逐位相同（基準見 `§一`）|
| 🆕 **`S-3` 生產路徑錨** | `verify/wf_f*.py` **五檔** | 閘 `P` **⛔ 涵蓋之**（已登記缺口）⇒ 逐批以 `S-3`／`V-3` 併報 |
| **號占用閘（宣告框·三數分列）** | 母體 ＝ **全 `docs/`**（倉側 blob）；`D1` 檔名 ∪ `D2` 列首 `#+` 之列 ∪ `D3` 自稱形；皆帶 `(?!\d)` | 收單後、入倉**前**執行；**已占即停·⛔ 自行改號** |
| **驗收出艙閘** | `verify/tools/wg9223_acceptance_audit.py <單> <報告>`（須於倉內執行·`GB-148`）| `rc=1` 且訊息為「該單⛔ 有【驗收】段」⇒ **無從回測**、**⛔ 回測不過** |
| **`G2`**（`GB-144` 取值路徑對拍）| **標記法框**：`GB-144` 節內 `現態命中集@<commit>` 之列中 `commit` **最新**者 | 🔴 **現態已知失效**——見下 |
| **`PreToolUse` heredoc 硬閘** | `.claude/settings.json`（`matcher` ＝ `Bash`）＋ `verify/tools/wg9237_heredoc_guard.py` | 🔴 **射程受限**——見下 |

### 🆕 宣告框之**三數分列**（補款 `⑥`·`W-G.9-243` 立）

> 凡出艙宣告框之數，須**同格分列**三數：**`D1` 檔數／`D2` 列數／`D3` 列數**，並明載 **`列框 ＝ D2 ＋ D3`**。
> 🔒 **`D1` 係<u>檔框</u>之款、⛔ 產生任何列命中**；`檔框 ＝ |D1 ∪ D2 之檔 ∪ D3 之檔|`。
> 🔒 **`D3` 之「本單」款依補款 `⑤`**：僅於該號 ＝ **該檔<u>檔名</u>所載之單號**時成立。
> 🛑 **輔檢 `宣告框列 ≤ 鬆框列` ⛔ 足以攔下 `-242` 之瑕**（該案 `10 ≤ 10` 恰成立）⇒ **主檢須為三數分列**。

### 🔴🔴 `PreToolUse` 硬閘之**範圍限定**（`W-G.9-244` 入 `GB` 簿）

> 掛點逐字 ＝ `python "$CLAUDE_PROJECT_DIR/verify/tools/wg9237_heredoc_guard.py"`
> ⇒ 閘之成立**同時**繫於 `$CLAUDE_PROJECT_DIR` 內之**二檔**存在。
> **凡 session 之 project dir 為自 `main` 開之 worktree，該二檔皆不存在 ⇒ 閘<u>結構性不存在</u>，且⛔ 有任何徵兆。**
> 🔒 **通則：「閘已落地（倉內有其碼與設定）」⇏「閘於本 session 生效」**；
> **最廉價現查 ＝ `test -f .claude/settings.json`**（不存在即閘不存在）。

🩸 **本波實況（誠實登記）**：CC 於 `-239`〜`-245` 之窗中**四度**誤送含引入符之 `Bash` 命令，
**四度皆未被攔**（該 session 之 project dir 為自 `main` 開之 worktree）。
四次皆為**未完成即滯留**之命令、皆以 `TaskStop` 終止、**未寫任何檔、未動倉態**
（各批 `git status`／`numstat` 逐項對得上）⇒ **土地後果 ＝ 零、倉態後果 ＝ 零**。
🛑 **`GB-143` 之發生次數計數器現為 `3`**——**本波之四度是否計入 ＝ 意思決定**，⛔ 由 CC 自改。

### 🔴 `GB-144` `G2` 之現態命中集**已知失效**（`W-G.9-244` 段乙所致）

段乙（側分支 `verify/W-G.9-244-c1`）之 `app.py` 改動使該節 `現態命中集@d2d3692` 之
**13 個 `app.py:NNN` 錨全部位移**（實測：`< 19774` 者 `+46`、其後者 `+104`；
逐點內容比對確認舊行號現皆為無關文字）。
🛑 **依 `-228` 之維護義務「每次位移須<u>新增</u>一列（⛔ 改舊列），未新增 ＝ 已知失效須具名」**
——本波**具名為已知失效**：新行號須待**段乙併入主線後**方穩定，故**併線批必辦**：
於 `GB-144` 節末新增一列 `現態命中集@<併線後之 commit>`（⛔ 改舊列）。

### 🔴 陷阱清單（**已付學費·逐條皆有實例**）

| 號 | 受詞 |
|---|---|
| `GB-139` | `core.autocrlf=true` ⇒ 閘之受詞一律取**倉側** `git cat-file blob` |
| `GB-143`（＋三補款）| 含反斜線之字面一律 `Write` 落檔；**凡經 heredoc 者一律⛔ 執行**；其硬閘**射程受限**（見上）|
| `GB-145` | 閘之 `rc` **⛔ 取自管線之後**；一律 `subprocess.run(...).returncode` 直取 |
| `GB-147` | **哨兵自落地**——對照組字樣⛔ 寫入任何入倉文字 |
| `GB-148` | 機檢器倉根繫 `cwd`；**倉外 `rc=1`**；`--selftest` ⛔ 繞過受測路徑 |
| `GB-149` | append-only 簿內**行號為錨** ⇒ 母體單調擴張·永久轉紅；處置 ＝ 錨改符號名 |
| `GB-132` | **自指增長**——清單／占用表之敘述句自身污染其所量之集合 |
| 🆕 **量測器族（本波新捕）** | ① `grep -c` 之**跳脫形**（`$'\r'` 等）在此 harness **塌陷為空樣式** ⇒ 命中每一列；**判法 ＝ 其輸出恰等於母體列數即先判器紅**。② `2>/dev/null` 會吞掉 `UnicodeEncodeError` ⇒ 列印在 `cp950` 上**中途夭折**而看似「短但成功」；**輸出一律落 UTF-8 檔再讀**。③ 以「名 → 定義」解析者**須先報該名之定義處數**；`> 1` 者須綁**呼叫點之前最近**（實例：`_dep` 於 `app.py` 有 **4** 個定義）。④ 以「檔:列」為鍵之判定表，**須先查同列多站數**（AST 粒度下常態存在）。|
| 🆕 **Streamlit widget 生命週期** | **分支未渲染之輪 ⇒ Streamlit 丟棄 widget key，而非 widget 之持久鍵存活**。凡「持久 dict ＋ widget」並用者，`value=` 之種**須取自持久 dict**，**⛔ 取自 widget key**——否則回渲染時以 `0`／預設值**靜默覆寫**使用者輸入。已付學費三次：`W-G.9-219R`／`-226`（`G4`）、`W-G.9-244` 段乙（本波·由反駁式審查捕得）。|
| 🆕 **`main()` 從不被 harness 執行** | `app_harvest.harvest()` 只 `exec` **過濾後之頂層定義**（`_filter_module` 之 `_KEEP_TOP`），**⛔ 呼叫 `main()`**；倉內二 fixture 逐字自證（`verify/fixture_klui_t2diag.py:10`–`11`／`verify/fixture_n14_feed_chain.py:7`–`9`）。⇒ **凡改動落在 `main()` 內者，`run_verification` 之輸出 diff <u>必空且零鑑別力</u>**——⛔ 以其為「無迴歸」之證據。|

---

## §五　已登記之待決閘（**⛔ 放寬、⛔ 逕修**）

| 鏈 | 受詞 | 現況 |
|---|---|---|
| `GB-67` | 退縮 `0` 之 `②-宗 圍堵閘` | **未解**：實跑破量 `R2` 宗-宗重疊 `45.9766 > 上界 2.8906` |
| telescoping 結構閘 | 退縮 `3.5` 之 `ΣRw` | **未解**：`R2 left ΣRw_實跑 100.02 ≠ 44.41`（`Δ=55.606`）|

🔒 **二者係<u>二事</u>**（中止點相異·`W-G.9-172R:59` 逐字「相異」），**且皆為 harness 專屬**
（`app.py` 命中 `0`）⇒ **⛔ 影響 app 之配地**；其所阻塞者為 **`R4`／`R5`／`R6` 逐值迴歸之驗收能力**。
🔴 **其後果（`-241R` 實測）**：二情境皆於街廓 `R2` 中止 ⇒ `trunk A` 未成功 ⇒
`F.0`／`F.1`／`F.2`／`F.3`／`F.4` **逐級「跳過（存在性守衛）」** ⇒ `verify/out/got_F.2_*`／`got_F.4_*` **一檔未生成**。
⇒ 🛑 **「UC9898 輸出逐值不變」此一驗收⛔ <u>可執行</u>**（非「可執行但無鑑別力」）；
且**級之命中筆數係「不可測」⛔「＝ 0」**（「量不到」與「量到而為零」⛔ 同義）。
🔒 **KL 已裁（`-242` 入典）**：**五級實作得以替代證據放行**，⛔ 俟二閘解除；
其代價 ＝ 驗收**⛔ 得用 `R4`／`R5`／`R6` 逐值迴歸**，只得用 `R2` 以前之替代證據 ＋ 單元級證據。

---

## §六　待辦／待裁

### 🔴 最優先：`W-G.9-244` 段乙之放行鏈（**未完**）

- 側分支 **`origin/verify/W-G.9-244-c1` ＝ `6b5fe718a0d53d07b86a460d520037affa555a1b`** 已推；
  `wip/s1-endpart` **一字未動**（現查仍 `ffb0544`）；本地保全 ref ＝ `keep/W-G.9-244-c1`。
- 其報告 ＝ `docs/reports/W-G.9-244R3_段乙_K-9-1資料層_執行報告.md`（在該側分支上，**⛔ 在主線**）。
- **`app.py` 改後 `sha256` 前 16 位 ＝ `ad7a91dba08119f5`**（主線現態仍 `e3e464ea2493d046`）。
- **下一步 ＝ 發單側逐位復驗 → 請示 KL → 逐字放行 → 純快轉併線**。
- **併線批必辦**：`GB-144` 節末新增 `現態命中集@<新 commit>` 一列（見 `§四`）。
- 🩸 **段乙曾由反駁式審查捕得四則缺陷並已修**（含一則 serious ＝ **Streamlit widget 生命週期靜默清零**，
  係本倉 `W-G.9-219R`／`-226` `G4` 之**同一機制**、CC 明知而重踩）——詳見該報告 `§四`。

### 🔒 五級實作之依賴序（KL 裁 `2026-09-06`·已入 `GB` 簿）

```
1 K-9-1 最小建築面積 UI 欄（**本波段乙已做<u>資料層</u>**·⛔ 接閘、⛔ 實作 r2）
2 區外道路清單 ＋ 正面道路識別符暴露   （r3 之前提·有土地後果）
3 排序準據 seam 抽出                  （**已完成**·`-243` 併線）
4 五級實作                            （r1–r8 齊備後）
5 GB-67 ／ telescoping                （驗收能力·得與 1–3 並行·得整批後推）
6 事後協調合併重算                    （須在 4 之後）
```

### 🛑 待裁清單（**⛔ 由 CC 自裁**）

| # | 受詞 |
|---|---|
| 1 | `-244` 改版單 `§一` 工項零**仍寫舊檔名**（`…資料層UI.md`）；實交付者為 `…改版.md` |
| 2 | 正典 `v3.md` 現**同時載二份 ⑧⑪**（舊版節 ＋ 末端更正節）；是否於上文節加指標（須改該節一字·與 `常規四（九）五` 衝突）|
| 3 | 作廢標記後之單檔 `sha256` 已異於原交付檔（**嚴格後綴**已機驗）；是否於 `追加四` 補一款 |
| 4 | `GB-143` 計數器（現 `3`）是否計入本波之**四度**誤送 |
| 5 | 宣告框是否增設「**作廢單⛔ 計入**」一款（同號重發之單永遠過不了自己的閘）|
| 6 | **自我記錄是否構成已占**（本波 `244` 於現態得 `6/11`，占用者逐筆皆本批自身足跡）|
| 7 | `.py` 自稱形是否構成占用；號占用母體是否及於 `*.py` |
| 8 | `-244` 段乙之 UI 掛載點**偏離單之逐字**（單云「正面路寬所在處」，實掛於深度覆寫所在之逐街廓介面）——理由已具名（前者在 `st.form` 內且以 `bid` 為鍵）|
| 9 | AST 母體是否擴及「具名 `key` 函式之定義展開」；`列框 ∖ AST` 之「級字面」是否自對照組定義剔除 |
| 10 | `verify/wf_f4.py:798`（`"級別": LV[_level(…)]`）**二框皆不取** ⇒ 是否另設「級之消費點」框 |

---

## §七　本波（`-240` → `-244R2`）之新增摘要

| 批 | 要旨 |
|---|---|
| `-240` | 五級（八鍵詞典序）入典；`GB-143` 補款 `③` 更正（**只走 `stderr` ＋ `exit 2`**·⛔ `stdout` deny JSON）；事後協調合併重算之射程（全區 `B`／`C`／`mina_qu` **恆不變**·街廓層傳遞閉包·宗地層自 `p*` 向後）|
| `-241` | seam 偵察：排序準據站點 **165** 相異列（列框）；🔴 **判別力前置量測 ⇒ 級之命中「不可測」**；母體三洞 |
| `-242` | 宣告框 `D3` 邊界款 `⑤`；**排序準據母體改 AST**（**367** 站·`A 19`／`B 66`／`C 85`／`D 197`）；五級依賴序入典 |
| `-243` | **seam 抽出**（四站·`_key_anchor`／`_key_border`／`_key_queue`／`_key_share` ＋ registry `_SORT_KEYS`）；宣告框**三數分列**補款 `⑥`；**級之影響面清單**（母體 34 檔·落點僅 **14** 列·`_level` 呼叫點**恰 2 處**）|
| `-244` | `K-9-1` 域裁六款入典（⑧⑨⑩⑪·**改版後 ⑧⑪ 另有更正節**）；`GB-143` 範圍限定；調配側缺驗登記；**段乙 ＝ `K-9-1` 資料層**（側分支候放行）|

### 🔑 碼面要害（**下一波必知**）

- **seam 之唯一決策路徑**：`verify/wf_f4.py` 之 `_SORT_KEYS["border"]` → `border[gid]` → 首個 `_usable` 者即 `pick`。
  `_level` 定義於 `wf_f4.py`；其**呼叫點恰 2 處**——決策鍵 ＋ 輸出欄（後者⛔ 受 seam 影響）。
- **`wf_f2` 之級係<u>事後標記</u>、⛔ 選擇準據**（`_decide` 先選定，`lvl` 後算且唯一去處為報表欄）。
- **`app.py` 側之級落點 ＝ `0`**；`app.py` `import wf_f4 as _wf4` ⇒ **`wf_f*` 在 app 之生產路徑上、⛔ 僅 harness 複刻**。
- **`K-9-23` 藍影（含二閘）與 `K-9-12` 矩形判皆只在 `app.py`**；`verify/wf_f0`–`wf_f4` **零命中**
  ⇒ 五級調配置宗時引擎側現**無**幾何／藍影／面積之驗（已登記為五級實作批之依賴）。
- **`K-9-1` 資料層之鍵**（段乙）：`f3_min_build_area_by_category`／**`f3_min_build_area_by_label`**（倉內自有提案名·
  `docs/specs/W-G.5_裁定N_乙案_plan_v1.md:204`）／`f3_min_build_area_effective_by_label`；
  helper ＝ `k91_effective_min_build_area`／`k91_min_alloc_contrib_by_blk`（**module 級 ⇒ `harvest` 可取、可單元對拍**）。

---

## §八　開窗前四件事（**依序·⛔ 跳**）

1. **驗 `left-right`**：`git ls-remote origin wip/s1-endpart` ＋ `git rev-list --left-right --count HEAD...<tip>`。
   **非 `0 0` 即工作區為舊態** ⇒ ⛔ 直接 `cat` 工作區之 `CLAUDE.md`／`app.py`／`docs/**`；
   一律 `git show <rev>:<path>`。**harness 逐窗自 `main` 開 worktree ＝ 已第六度重踩。**
2. **驗硬閘是否生效**：`test -f .claude/settings.json` ＋ `test -f verify/tools/wg9237_heredoc_guard.py`。
   **二者缺一即閘不存在** ⇒ 該 session 內 `Bash` **⛔ 出現 `<<` 任何形**（腳本一律 `Write` 落檔後以路徑呼叫）。
3. **讀本文 `§一`／`§二` 並<u>重跑</u>其每一數**（⛔ 採本文之值）。
4. **查 `W-G.9-244` 段乙之放行進度**：`git ls-remote --heads origin 'refs/heads/verify/*'`；
   若已放行 ⇒ 依 `§三` 之流程**純快轉**併線，並辦 `GB-144` 之新列（`§四`）。

🔒 **收單後首二步之順序**：① 查宣告框占用（三數分列）→ ② 原封入倉。**⛔ 顛倒。**
