# W-G.5 — **K-6-A2 段三(c)：K-9-7 接生產（KL 放行·有土地後果）**

> **基準**：`5e263f4`　**分支**：`wip/s1-endpart`
> **KL 裁 2026-08-04**：**放行 (c) 切換**。
> **性質**：**有土地後果**——街角規定範圍之定位改用 K-9-7 解析解，
> 8 格位移（最大 R5 左 −16.56cm ⇒ −7.27㎡）；**PK 零翻盤**。
> ⚠️ 所引行號**全部當場重 grep**；差異見 §七。

---

## 〇、執行環境（施工單 §〇-0 四項）

| 項 | 結果 |
|---|---|
| `pwd` | `/c/Users/admin/Desktop/land-readjustment-trial` |
| `git rev-parse --short HEAD` | **`5e263f4`** ✅ 與施工單基準相符 |
| `git status --porcelain -- data/` | `M data/地籍資料來源_匿名版.xlsx`（**異動仍在**）＋`?? data/r3.dxf` |
| `ls verify/probes/probe_ruling_K9_7_pk_impact.py` | **在** |
| 機器 | **與家用機同一台**（`C:/Users/admin/Desktop` 存在；三支臨時腳本亦仍在 scratchpad） |

⇒ **環境無差異**，交接文 §五 所列之「不會跟到辦公室」三項**本批皆仍在手**。

---

## 一、🔴 施工單 §一 之一項前提不成立（**以倉內為準·先報**）

施工單 §一 載：

> 🔒 `k97_solve_alloc_t` 之後置斷言（K-9-5-2 ③）必須在生產路徑實際執行
> ⇒ **`min_depth_legal` 必須有給**（該斷言在未給時不執行·見 `app.py:9599` 起之 docstring）。
> ⛔ 不得讓生產走進「未給 ⇒ 不斷言」之路。

**查證**：`grep -n "min_depth_legal\|assert_ok\|assert_note" app.py`
⇒ **僅命中 docstring 兩行，參數與回傳鍵自始不存在**。

⇒ **該前提源自段三(a) 我自己寫的 docstring 之過度宣稱**（我在 (a) 描述了一個從未實作的
參數與兩個從未存在的回傳鍵）。施工單依該 docstring 下指示，**指示本身無法照辦**。

### 1-1 實況：K-9-5-2 ③ 之**二半各有歸屬**

| ③ 之半 | 由誰保證 | 實況 |
|---|---|---|
| **最小寬 ≥ 退縮＋畸零地寬** | **`k97_solve_alloc_t`** | ✅ **自始即無條件硬斷言**（`_W_at >= T`），**從無「未給⇒不斷言」之路** |
| **最小深度 ≥ 畸零地深** | **K-9-2 街廓層剖面警示** | ✅ 已上線（段一）；本函式僅作**退化守衛**（`D(t*) > 0`） |

### 1-2 為何**不**在本函式補比附表深（**否則違正典**）

**K-9-6-c 明令「不重複管」**（`grep -n "^#### K-9-6-c " docs/rulings/K-6_街角地分配程序與可分配判準.md`）：

> 「附表深度之合規（宗地最小深度 ≥ 附表深度）由 **K-9-2 街廓層剖面警示**管；**K-9-6 不重複管**。」

**且係幾何必然**：`D(t)` ＝ 範圍臨街段 `Q0→P_front(t)` 之最小垂距，
該段為**前緣線之子段** ⇒ `D(t) ≥ 前緣線全段之最小垂距 ＝ K-9-2 之剖面最小深度`。

**🔬 實測坐實（16 格·全精度·非 3dp 表值）**：

| | `D(t*) − K-9-2 全精度剖面 min` |
|---|---|
| 最小值 | **`+0.000000000`**（R1右／R4右／R5左／R6右 恰等於——**該街角 `Q0` 正是較淺之端**）|
| 最大值 | `+1.084007723`（0m R3/right）|
| **負值** | **無一格** |

⇒ 全 16 格 `D(t*) ≥ 14.00`（最小者 `33.0837`·餘裕 `+19.08`）。
⇒ **在本函式再比一次附表深，係 K-9-6-c 明禁之「重複管」** ⇒ **不補。**

> ⚠️ **首度量測時曾誤判**：以正典表之 **3dp 值** `33.084` 為靶，得 R4/right「🔴 不成立」；
> 實為 `D(t*) = 33.083668` vs 真值 `33.083668`——**是我的比較常數被四捨五入**，非違例。
> 改用全精度後 16 格全過。**（3dp 表值不可當解析靶——同 K-9-7-c 之戒。）**

### 1-3 本批之處置

**改 docstring、不改行為**：已把該段改寫為上表之「二半各有歸屬」，
並就地標明前版之誤（`grep -n "本 docstring 前版之誤" app.py`）。
⛔ **未新增 `min_depth_legal` 參數**（新增即違 K-9-6-c）。

---

## 二、切換本體（§一）

`grep -n "_k97 = k97_solve_alloc_t" app.py`

- `_build_corner_range_v3` 之 §四 定位**改呼叫 `k97_solve_alloc_t`**取得 `t*`。
- **舊式已移除**：`k = sigma / den_a; w_lo = min(_W(0,0), _W(D,0)); t_star = (T-w_lo)/k`
  ⇒ `grep -n "^\s*k = sigma / den_a\s*$" app.py` ⇒ **零命中**（僅存註解引用）。
  ⇒ **GB-14 ①②（帶深用 `D_avg`／斜率漏 `−k·m`）同批兌現。**
- **帶深自此改用 `D_at = D(t*)`**（範圍自身最小深度）：
  自檢① 與自檢①′ 之量測深度**一併改基準**，否則以舊 `D` 回代必假紅。
- **生產只有一條路徑**：無切換開關、無新舊並存、無靜默降級
  （`grep -n "k97_solve_alloc_t(" app.py` 扣除定義 ⇒ **1 處**）。
- **兩個消費端自動同步**（v12 內部＋步驟 L 參數表）——即 (b′) 探針以 patch 模擬之路徑。

### 2-1 `_t_override` 之語意反轉（已更新註解）

| | 切換前（段三 a／b′） | 切換後（本批） |
|---|---|---|
| 生產 | 舊式 | **新式** |
| 探針以 `_t_override` 注入 | **新** `t*` | **舊** `t_star_legacy` |

⇒ 探針 `probe_ruling_K9_7_pk_impact.py` 已同批反轉；
`grep -rn "_t_override=" --include="*.py" .` ⇒ **仍只命中探針**（生產零傳）。

---

## 三、🔒 §二 十六格對拍（**本批最強驗收**）

實料：`verify/out/K6A2_seg3c_16cell.log`

| 情境 | R1左 | R1右 | R2左 | R3右 | R4左 | R4右 | R5左 | R6右 |
|---|---|---|---|---|---|---|---|---|
| **0m 生產實算** | 114.76 | 116.84 | **227.11** | 226.91 | **135.19** | **137.0** | **217.97** | 145.88 |
| **0m (b′)新欄靶** | 114.76 | 116.84 | 227.11 | 226.91 | 135.19 | 137.0 | 217.97 | 145.88 |
| **3.5m 生產實算** | 230.81 | 232.79 | **384.15** | 383.67 | **251.12** | **252.86** | **371.17** | 298.14 |
| **3.5m (b′)新欄靶** | 230.81 | 232.79 | 384.15 | 383.67 | 251.12 | 252.86 | 371.17 | 298.14 |

⇒ **十六格逐格相符，差皆 `+0.0000`。**
⇒ 證明「切換後生產實走之路徑」＝「(b′) 以 patch 模擬之新路徑」
⇒ **(b′) 之 PK 零翻盤結論可直接承接**。

### 3-1 PK 重跑（探針已反轉·舊欄改注入 `t_star_legacy`）

實料：`verify/out/probe_ruling_K9_7_pk_impact.log`（已更新入庫）

- patch 命中 **32 次**（**舊**輪）；**新輪＝生產原樣、零 patch**。
- **候選／合格／winner／`forced_offset` 十六格逐格不變**（14 格全同 ＋ 2 格僅指數微變）。
- 指數微變二格與 (b′) **逐位相同**：
  `0m R2/left 628-40(1) 0.0153→0.0157`、`0m R5/left 628-7(2) 0.0028→0.0020`；
  **次位者皆未達標 ⇒ 無名次可翻**。

---

## 四、🔴 `run_all`：**級聯紅·未收官·停機上呈**

實料：`verify/out/K6A2_seg3c_runall.log`（`EXIT=1`）
⇒ **`64` 名目／`33` PASS／`31` FAIL**（前為 `132`／`86`／`46`）。

⚠️ **名目自 132 掉到 64 ＝ 68 個名目「根本沒跑」**，非「跑了變紅」。
**本批據此停機上呈**——即使 §四-2 允許名目集合變動，
「**suite 提早中止**」與「**名目換紅綠**」是兩回事，**不得以後者之條款結案**。

### 4-1 逐格進／出與成因（§四-2）

| 方向 | 名目 | 成因（**已逐項查證·非推測**） |
|---|---|---|
| **出**（68） | F.0／F.1／F.2／F.3 之**全部子閘**（`F.0·G值*`／`F.1 錨*`／`F.2 跨街廓守恆*`／`F.3 零遺漏*`…） | **級聯**：F.0 raise ⇒ 後續階段之存在性守衛跳過。log 原文：`[F.1] F.0 未成功，F.1 跳過（W5 守衛）`／`[F.2] F.0 未成功，F.2 跳過（存在性守衛）`／`[F.3] F.2 未成功，F.3 跳過（存在性守衛）` |
| **進**（4） | `W-F F.0`／`W-F F.1`／`W-F F.2`／`W-F F.3` | 上述四階段之**彙總 FAIL 列**（子閘未跑，改以階段列報紅） |
| **翻紅**（1） | `末端夾具 fixture_corner_range_k8.py` `rc=0 → rc=1` | 見 §4-3 |

### 4-2 🔴 根因一：**`GSA_EXPECT` 凍結硬錨破**（1 gid·1 情境）

```
[F.0] 🔴 GSA 錨檢破（3.5m）——【值不符 1 項】G010 G(Σa)=301.17 ≠ 錨 301.12
```

- **僅 `3.5m`**；**`0m` 六 gid 全數通過**。
- **僅 `G010` 一格**，Δ ＝ **`+0.05㎡`**。
- 錨源：`grep -n "GSA_EXPECT = " verify/wf_f0.py`（UC9898 凍結值）。

**性質判定**：此係**改街角規定範圍之預期後果**，非缺陷——
`3.5m` 之街角規定面積本批確有變動（R2左 `+1.30`／R4左 `+0.035`／R4右 `−0.027`／R5左 `−7.29`㎡），
而街角第 1 宗之 G 由該範圍導出 ⇒ 下游 `G(Σa)` 隨之微移。

🔴 **但「重錨」不是 CC 之權**——`CLAUDE.md` §7 明訂：

> **重錨**＝更新硬錨值使被 raise 截斷之名目重新產生（**claude.ai 層之技術動作**，
> 須附**逐 gid 因果歸因表**，前例 P-0c 2026-07-25／K-8 §五-1 2026-07-31）

⇒ **本批不動 `GSA_EXPECT`**（§〇-4 禁重烤·且逾權），**上呈 claude.ai 執行重錨**。
⚠️ 本批**未能**完成 gid→街廓之對應（試查 `歸戶` 鍵未中）⇒ **逐 gid 因果歸因表留給 claude.ai**，
本報告只提供已查證之事實（上列三點）。

### 4-3 🔴 根因二：**`fixture_corner_range_k8` T1 以【已被取代之帶深】量測**

T1 之量測呼叫（`grep -n "_band_min_width(cad, cb_by, lbl, which," verify/fixture_corner_range_k8.py`）
傳入之帶深為 **`snap["blocks"][lbl]["街廓分配深度_m"]`＝街廓平均深度**，
而 **K-9-6-b 明訂**帶深為「**該範圍自身之最小深度**」＝ `D(t*)`。

**⇒ 該夾具量的是舊定義之帶，非新定義之帶。** 逐格坐實：

| 街角 | T1 量得 | 靶 `T` | Δ | 是否位移組 |
|---|---|---|---|---|
| R5 left | **3.3337** | 3.50 | **−0.166** | ✅（`D₀−D_avg = −2.1996`）|
| R2 left | 3.5315 | 3.50 | +0.0315 | ✅ |
| R4 left | 3.5011 | 3.50 | +0.0011 | ✅ |
| R4 right | 3.4992 | 3.50 | −0.0008 | ✅ |
| R1 左右／R3 右／R6 右 | 3.500000 | 3.50 | `~1e-10` | ❌（零位移組·**全過**）|

**🔑 決定性算術**：R5 left 之 `Δ = −0.166` **恰等於** `k × (D_avg − D₀) = −0.0756 × 2.1996 = −0.1663`
⇒ **T1 之紅完全由「多量了 `D_avg − D₀` 這一段深度」解釋**，
且**紅的正好是四個位移角、零位移之四角全過** ⇒ **非隨機、非本批算錯**。

**⇒ 修法應為「把 T1 之帶深由 `街廓分配深度_m` 改為 `D(t*)`」**（隨 K-9-6-b）。
🔴 **本批未改**——理由：
1. 我**剛造成這個紅**，隨即改測試使其變綠，正是本倉一再警告之「**把綠當過**」形狀；
2. 該改動係**判準量測基準之變更**，宜與 §4-2 之重錨**同批**由 claude.ai 定奪；
3. §〇-4 之「該紅照紅」精神。

### 4-4 ⚠️ 本批之驗收力因級聯而受限（**誠實聲明**）

68 個名目未跑 ⇒ **`run_all` 本輪無法用來證明切換正確**。
**切換之正確性全部由 §三 承擔**（十六格對拍逐格相符 ＋ PK 十六格零翻盤），
二者皆**不經 `run_all`**、且對拍靶來自切換前之獨立量測。

---

## 五、§三 正典與登記

| 項 | 落地 |
|---|---|
| **K-9-7 標「已實作」** | `grep -n "^### K-9-7 " docs/rulings/K-6_街角地分配程序與可分配判準.md`·體例比照 K-9-4·附落地錨與對拍靶 |
| **GB-14 結案** | ①②同批兌現·結案欄註明對拍靶＝十六格（`verify/out/K6A2_seg3c_16cell.log`）·「真資料覆蓋」欄由「無看守」改為「已有看守」 |
| **GB-26 維持開啟** | 🔴 已標「**本項已生效但仍未獨立確認·不得因切換完成而順手結案**」（KL 明令） |

### 5-1 §三-4 待裁 B／C／D 之裁示（claude.ai 已裁·**入案存查**）

- **B（K-9-7-d ⟷ K-9-8 循環）**：**切在分支層次**——第一層（①②③④）屬 K-9-7、**已完成**；
  第二層（`P_block` 落截角邊 vs FRONT 重疊段）屬 K-9-8、**歸段四**。
  維持現行「只偵測 `P_front`、不臆造 `P_block`」之作法。（已載於正典 K-9-7 之落地錨段。）
- **C（合成案）**：**併入段四**。①×截角、③×截角之分支條件需 K-9-8 之 `PQ`，現在造即臆造。
- **D（R5 左餘裕 `0.07㎡`）**：**只記錄、不處置**。

### 5-2 D 之記錄（**報告與波末重烤清單各一條**）

1. `0m R5/left` winner `628-7(2)` 之真交集 **`1.07㎡`**（候選資格門檻 `1.0㎡`·餘裕 `0.07㎡`）
   ——**現況合格、不處置**。
2. ⚠️ **`R4` 是唯一同時受「K-9-7 切換」與「波末換 `V6_1`」兩次擾動之街角**
   ——波末重烤**須單獨拉出對照**。
   （claude.ai 已比對兩份 DXF：`BLOCK`／`BASELINE` 兩層逐字相同，變動僅
   `FRONT_LINE` 2 條、`SIDE_LINE` 3 條、端點各移約 1.5cm；兩變動端點至 **R5** 為
   `58.19m／33.27m`、至 **R4** 僅 `2.50m／2.74m` ⇒ **換 `V6_1` 不會動到 R5 之餘裕**。）

---

## 六、土地後果（承 (b′)·此處為**已生效**之值）

| 街角 | 街角規定範圍怎麼移 | 面積（0m／3.5m） |
|---|---|---|
| **R5 左** | 往街角縮回 **16.6 公分** | **−7.27／−7.29 ㎡** |
| R2 左 | 往內推 2.9〜3.1 公分 | +1.42／+1.30 ㎡ |
| R4 左／右 | 約 0.1 公分 | +0.04／−0.03 ㎡ |
| R1 左右／R3 右／R6 右 | **不動** | **0** |

**PK 結果零翻盤**：無人換手、無抵費地狀態改變。

---

## 七、施工單與倉內實況之差異（§六）

| 施工單所載 | 倉內實況（`5e263f4` 當場 grep） | 處置 |
|---|---|---|
| `_build_corner_range_v3` `app.py:9769` | 屬實 | 已改 |
| `k97_solve_alloc_t` `app.py:9568` | 屬實 | 已接 |
| v12 內部 `:10198`／步驟 L `:16881` | 屬實 | 自動同步·未逐一改 |
| `_t_override` `:9772`／`:9902` | 屬實 | 註解已更新（語意反轉） |
| **§一 `min_depth_legal` 必須有給** | 🔴 **該參數與 `assert_ok`／`assert_note` 自始不存在**（源自段三(a) docstring 之過度宣稱） | **改 docstring、不新增參數**；深度半依 **K-9-6-c** 歸 K-9-2 管、**不重複管**（§一） |
| §一 「該斷言在未給時不執行」 | 🔴 **不成立**——寬度斷言**自始無條件執行** | 已於 docstring 就地更正 |
