# 🧾 `W-G.9-186R`　**CC 交接文**（新 session 接手用·KL 令換窗 `2026-08-31`）

**出文窗**：CC　**性質**：🔒 **純追加一新檔**·⛔ 零生產碼·⛔ 零管線·⛔ **不鑄任何號**
**取號之依據**：`常規四 三`（CC 之產物一律以「**該單號 ＋ 後綴 `R`**」命名，⛔ 不另取新號）
⇒ 本檔用 `W-G.9-186R`，**⛔ 未取 `W-G.9-187`**（該序屬發單側·`常規四 四`）。

> 🔑 **本檔與 `docs/reports/W-G.9-186_幾何本體批交接文.md` 之分工**（二者互補·**須併讀**）：
> - **本檔** ＝ **session** 之交接（倉態／取號／本波全鏈／積欠／家法／harness／memory／回報紀律）
> - **該檔** ＝ **工項** 之交接（四項幾何裁定之分批、相依序、矩形姿態之裁、規格書七欄讀法）

---

## ✅ `§零`　最要緊之一件事（⛔ 讀完再動任何東西·`常規九`）

🔒 **本窗之全部成果<u>皆已 push</u>，⛔ 無任何未 push 之 commit。**

| 態 | 值 |
|---|---|
| `origin/wip/s1-endpart` | **`f2ea2e4dd46e5ffcc34ffa5c17cc4e13249eedb1`** |
| 本地未 push 之 commit | **無**（`left-right` ＝ `0 0`·工作 clone 已刪） |
| **`app.py` blob** | **`68f7bfab610ec78688b9d7322ae4bc2aac74493e`**（`1352132` B／`22922` 列／`sha16 7c290b6f55f83a24`） |

🛑 **接手者之第一判斷**（`常規九 三`：開工閘 ⛔ **不得以本檔所載之 commit 為錨**）：
`git ls-remote origin refs/heads/wip/s1-endpart` ⇒
- ＝ **`f2ea2e4…` 或其後裔** ⇒ 照 `§一` 正常開工。
- **其他值** ⇒ 🛑 **停機**上呈（本窗收束時⛔ 無未 push 之物，故任何落後皆非本窗所留）。

🔒 **`app.py` 自 `W-G.9-169 補正①`（`926a0de`）起<u>十二批未動</u>**——`-176`〜`-186` **全批零生產碼**。
🔴 **⇒ 幾何本體批之第一張單，將是本波首次真正動 `app.py`。**

---

## `§一`　接手第一件事（🛑 依序辦·⛔ 不得跳）

1. 🛑 **讀 `CLAUDE.md` 全文**（態 ＝ `f2ea2e4`：**`74406`** B／**`807`** 列／`sha16 7385796755e2439f`）。
2. 🛑 **讀本檔全文**，續讀 **`docs/reports/W-G.9-186_幾何本體批交接文.md`**（`14732` B／`183` 列／`cdd3b64fb0671fa0`）
   ＋ **工作底本 `docs/design/W-G.9-185_幾何本體批施工規格書.md`**（`42908` B／`574` 列／`dfd46da5825c645e`）。
   🔴 **`自誤 161` 之防線**：設計任何量測前須先列 `K-6` 正典之全部節標題（`grep -c "^#"` ⇒ **`196`**）並逐節判，⛔ 不得只搜關鍵詞。
3. 🛑 **開工閘**：`git -c core.autocrlf=false clone --branch wip/s1-endpart <public url> C:\wNNNa` ⇒
   `git config core.autocrlf false` ＋ `user.name diag`／`user.email diag@local`
   ⇒ **對拍 `HEAD` 全 `40` 碼與 `app.py` blob**。
   ⚠️ clone 路徑須**短**（`MAX_PATH`）、⛔ 不得放 scratchpad；自檢器與 payload 置於**另一**短路徑（`C:\wNNNx`）⇒ **母體 `rglob` 自然自扣**（`S-4`）。跑畢二者皆刪。
4. 🛑 **長跑（`run_all`）一律於<u>另一個</u>拋棄式 clone**（`GB-94`：`run_all` 會改寫 `verify/out/**` 之**已追蹤** log）。
5. 🔴 **harness worktree 之陷阱（每窗必撞·本波三度實測）**：Claude Code 之 worktree **自 `main` 開分支**
   ⇒ 工作區落後數百 commit，**自動載入之 `CLAUDE.md` 係 `main` 態之 `190` 列**（工作態 `807` 列）
   ⇒ 🔒 **⛔ 不得直接 `cat` worktree 之任何檔**；一律 `git show <工作分支 rev>:<path>`，或於 clone 內讀。
   **判準**：`git rev-list --left-right --count HEAD...origin/wip/s1-endpart` **非 `0 0` 即工作區為錯態**。
   ⚠️ 其**具體數字逐窗變動**（`515`／`523`／…），⛔ 不得照抄——恆真者只有判準本身。

---

## `§二`　倉態與取號現查（態 ＝ `f2ea2e4`·CC 現查·量測時點 `2026-08-31`）

### `§二-1`　三數（框 ＝ `splitlines()`·`CRLF` 皆 `0`）

| 檔 | bytes | 列 | `sha16` |
|---|---|---|---|
| `app.py` | `1352132` | `22922` | `7c290b6f55f83a24` |
| `CLAUDE.md` | `74406` | `807` | `7385796755e2439f` |
| `docs/rulings/K-6_…md` | `258295` | `3632` | `6a5cada2901a10e8` |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `402254` | `4347` | `82084b330ff89b4d` |
| `docs/reports/W-G.4_泛用阻塞項登記表.md` | `522629` | `3202` | `1d1d678c309f0288` |
| `docs/驗證裁定登記表.md` | `333071` | `4258` | `c5b812d388040de3` |
| **`docs/design/W-G.9-185_幾何本體批施工規格書.md`** | `42908` | `574` | `dfd46da5825c645e` |
| **`docs/reports/W-G.9-186_幾何本體批交接文.md`** | `14732` | `183` | `cdd3b64fb0671fa0` |
| `docs/reports/W-G.9-186R_續辦_矩形旋轉之裁與GB126立項.md` | `21447` | `249` | `358e657f596620e4` |
| `verify/run_all.py` | `24681` | `377` | `4ba89fef90979491` |

⚠️ `app.py` 檔尾**⛔ 無 `LF`** ⇒ `wc -l` 得 `22921`（差恰 `1`·`VR-057`）。其餘九檔二框同值。

### `§二-2`　取號（母體 ＝ 全倉 `.md` **`468`** 檔·🛑 **⛔ 不以「算術自檢 True」為憑**·`GB-125`）

| 類 | 定義列式（逐字） | 未扣除（相異／MAX／缺） | 所扣 | **已扣除 MAX** | **次號** |
|---|---|---|---|---|---|
| 自誤（單檔） | `` ^#+ 自誤 `?([0-9]{1,4})`? `` | `188`／`223`／`35` | `[]` | **`223`** | **`224`** |
| 自誤（全倉） | 同 | `192`／`223`／`31` | `[]` | **`223`** | — |
| `GB`（三框聯集·`W-G.4` 單檔） | 式表／式A `` ^## `GB-N` ``／式B `` ^### `GB-N` `` | `124`／`126`／`2`（缺 `[12, 87]`） | `[]` | **`126`** | **`127`** |
| `VR`（`docs/驗證裁定登記表.md` 單檔） | `` ^##\s*`VR-([0-9]{1,4})` `` | `70`／**`999`**／`929` | **`[999]`** | **`84`**（缺 `[1..13, 73, 75]`） | **`085`** |
| 戒 | **裸框** `` 戒\s*`?N`? `` ／ **CC 提案之定義列式** `` ^🛑 戒 `?N`? `` | 裸框 相異 `28`／MAX **`40`** ｜ 定義列式 相異 `12`／MAX **`39`** | — | **`39`**（以定義列式） | **`40`** |
| 常規（`CLAUDE.md` 單檔） | `^\*\*常規(.)` | `12` 列·序 `一二三四五六七四四八四九` | — | **九** | — |
| `docs/reports` 之 `W-G.9-NNN` | 檔名 `^W-G\.9-(\d{1,4})` | 相異 `137`／MAX `186` | — | **`186`** | **`187`**（**發單側之序**） |

🔴 **「戒」之二數必須並報**：其**⛔ 無定義列式**（歷來只有裸框）⇒ 裸框 MAX 對「**次號之書寫**」⛔ 無免疫力
——本窗實鑄至 `39`，而裸框得 `40`（其 `3` 列命中全在本窗二新檔之次號表·**定義形命中 `0`**）。
**與 `VR-999` 之裸框得 `999` 係同一個病。** CC 已提案定義列式 `` ^🛑 戒 `?N`? ``（實測 MAX `39`·`40` ⛔ 不在其中 ⇒ **具判別力**），🛑 **待發單側裁**。

**次號之占用現查**（錨定框 `(?<![0-9\-])<完整號>(?![0-9])`·**二形分報**·三數 ＝ 命中列數／出現次數／檔數）：
`自誤 224` 形① `4/4/2`·形② `0/0/0`；`戒 40` 形① `6/6/2`·形② `2/2/1`；`GB-127` `4/4/2`；`VR-085` `11/11/5`
——🔒 **其命中之唯一來源皆係本窗之次號表**（`W-G.9-186_幾何本體批交接文.md:40`／`W-G.9-186R_續辦_…:30`／`:128`）
⇒ **本案自身之配給**（`戒 37` 通則／`W-G.9-182 §七`）⇒ **⛔ 不順延**。
`W-G.9-187` ＝ `0/0/0`。**對照組**（必非零）：`GB-126` `14/14/4`／`VR-084` `11/11/5`／`W-G.9-186` `41/43/6` ⇒ 量測器非紅。

🔴 **號之「形②」寫法（本窗自捕之量測器紅·下窗必踩）**：形② ＝ `` 自誤 `223` ``（**號**帶反引號）
——⛔ **不是** `` `自誤 223` ``（整體帶）。誤用後者會使**已入倉之號**得 `0/0/0` ⇒ 對照組為零 ⇒ 依 `常規四（七）補款二 二` 須先判量測器紅。

🔒 **已作廢之人造受詞清單 ＝ `13` 項**（末 ＝ `自誤 4173`·`W-G.9-184R`）。
**算式**（`W-G.9-184` 立）：`本批項數 ＝ 前批 ＋ 本批出艙數`；🔒 **其<u>適用前提</u> ＝ 該批確有出艙**
——`W-G.9-185`／`-186` 皆⛔ 未取人造受詞 ⇒ `13 + 0 + 0 = 13`，**補款四⛔ 未失守**（`W-G.9-186R §四-4`）。

---

## `§三`　本波之位置（`-176`〜`-186`·**全數已 push**·**全批零生產碼**）

| 單 | commit | 要旨 |
|---|---|---|
| `-176R` | `a44eb82` | 二處就地更正 ＋ `自誤 212` ＋ `R2` `0.1171` 之逐宗定位 |
| `-177R` | `666e0bd` | `自誤 213`／`214` ＋ `GB-121` ＋ 🔑 **`R2` `0.1171` 歸因完成（族②）** |
| `-178R` | `4b20b2d` | `自誤 215`／`216` ＋ `GB-122` ＋ 🔑 `G.3` 判 ＝ 第四支 (丁) |
| `-179R` | `b850af5` | 🔓 `G.3` 停機解除（`VR-084`）＋ 🔑 `GB-123` ＋ `MEMORY.md` 聚合 `−81%` |
| `-180R` | `c255ca4` | `自誤 217`＋戒 `33` ＋ `GB-123` 嚴重度界定 ＋ `VR-999` 澄清 |
| `-181R` | `9fe070a` | 🛑 **停機上呈**（`X-3` 之恆等對含 5b 配餘之街廓結構上不成立） |
| `-182R` | `d034e59` | `自誤 218`／`219` ＋ 戒 `34`／`35` ＋ `GB-124` ＋ 🔑 `R1`／`R4` 二問分別了結 |
| `-183R` | `5a5e150` | `VR` 取號污染處置（`常規四（八）` 補款）＋ 🔑 **式(ii) 判 (B)：構造恆真 ⇒ ⛔ 不立閘** |
| `-184R` | `e9e3d1e` | 🔓 **`GB-123` 守備上線**（`fixture_klui_t2diag` 掛入 `run_all` `15`→`16`）＋ 🔑 **`GB-125`** |
| `-185R` | `d10f4e8` | 🔑 **四項幾何裁定之施工規格書**（`docs/design/`·四項×七欄）＋ `_projection_order` 母體同步實測 |
| `-186R` | `42f3dbb` | 🛑 **停機上呈**（`X-4` 五處逐字·三處因 markdown 標記不符） |
| `-186R`（續辦） | **`f2ea2e4`** | 🔓 **矩形可旋轉可平移之裁** ＋ `GB-126` ＋ `自誤 222`／`223` ＋ 戒 `38`／`39` ＋ **幾何本體批交接文** |

### 🔑 本波之**七條結論鏈**（下一單之全部前提）

**① `R2` 之 `0.1171` ＝ 族②（`_W_prev_*` 初值）**（`-177R`）——app 用舊式 `buffer_S × cos_dn`，stepg 用 `_mp_base_W0`。
**切換屬 `W-G.9-5`（contract 側）·已裁而未遷移**。

**② `G.3` 停機已解除 ＝ 第四支 (丁)**（`-178R`／`VR-084`）——其二端皆 harness 側 ⇒ app-vs-shim 之差**結構上⛔ 不可能被抓到**。

**③ `GB-123`：生產路徑之池⛔ 無自動守備 ⇒ `-184R` 已建守備**（`fixture_klui_t2diag`）——
🔴 **惟 `GB-123` ⛔ 未消滅**：射程外五項（只守**已入倉之那一趟**·KL 下次實跑須**人工入倉**／態⛔ 不可自證／守**變動**⛔ 非守**正確**／⛔ 不涵蓋 `R5`／二端**皆 app 側**）。

**④ 🔑 式(ii) 判 (B)：⛔ 不立為閘**（`-183R`）——其殘差 **≡ Σ退化帶寬**（六街廓實測·差 ≤ `4e-15`），
上界 ＝ 退化帶數 × `_S_EPS`（`2.0e-4`·`app.py:9092`）⇒ **構造恆真、判別力為零**。
⇒ 逐字具名「**本波所尋之覆蓋自檢實不存在**」；承重閘之正解已在倉內 ＝ 守恆式 `|ΣG − Σ宗幾何|`（`app.py:9465-9467`）。

**⑤ 🔑 三個「恆真／永綠」同族**（**本波最重要之通則**）：
① `覆蓋殘差 ≡ 0`（構造恆真·`app.py:9462-9467`）／② 式(ii)（同上）／③ **`GB-125`**（`MAX − 缺號數 == 相異` 之算術自檢**代數恆真**）／④ **`GB-126`**（`ORDER_INVARIANCE` `7` tag **執行 `0`** ⇒ 永綠）。
🔒 **通則**：**「殘差恆為 `0`／自檢恆為 `True`」係壞閘之<u>徵候</u>，⛔ 非好閘之證據。**

**⑥ 🔑 「識別符命中 `0`」⛔ 不等於「未實作」**（`自誤 222` 肢②）——
`K-9-17` 之**重排已落地**（`app.py:7960` `Patch D-2 Minimal-Disruption`），真正缺者係**遞補**。
⇒ **⛔ 不得以裁定編號之字面命中數推斷實作狀態**；碼面⛔ 無須以裁定編號命名其實作。

**⑦ 🔑 `GB-104` 之交叉，二閘落地後<u>仍可能發生</u>**（`-185R` 判·`-186R` `P-4` 落地）——
其 loud 斷言**⛔ 不得再依附 `K-9-23`**，列為幾何本體批之**獨立第三批**。

---

## 🛑 `§四`　下一單之受詞（**幾何本體批·第一批**）

🔑 **工項之細節一律讀 `docs/reports/W-G.9-186_幾何本體批交接文.md` ＋ 規格書**，本節只給骨架：

| 批 | 工項 | 要點 |
|---|---|---|
| **第一批** | **`K-9-23` 藍影骨架（含 `K-9-16` 外界 ＝ `B1`）＋ `K-9-12` 矩形容納** | 🔴 **必須同批**（只落其一 ⇒ 閘二恆真**假綠** 或 **死碼**）。🔓 **矩形可旋轉、可平移、⛔ 不須貼齊 FRONTLINE**（`K-6:2482` 判準逐字）⇒ **開工阻塞已解除**。 |
| 第二批 | `K-9-17` 之**遞補**迴圈 | 含被踢除宗之面積**入調配池** ＋ 遞補宗之 **`G` 重算**；須後於二閘。 |
| 第三批 | `GB-104` 之交叉 loud 斷言 | **獨立工項**·⛔ 不得依附 `K-9-23`（結論鏈 ⑦）。 |

🛑 **第一批將動 `app.py`** ⇒ 依 `常規一`，**生產碼批⛔ 不得逕行 push**，須請示。
🛑 **落地時須同步更新 `_PROJ_POP_DECL`**（`app.py:7646`）——遞補會改變投影序母體，新增之呼叫點若未宣告，`_proj_pop_decl_of`（`app.py:7717`）**loud raise**。

---

## `§五`　積欠（🛑 下一登記批一次入倉·承 `W-G.9-183` 交接文 `§五` 並更新）

| # | 項 | 狀態 |
|---|---|---|
| `1` | `GB-104` 之 loud 斷言 | ⬜ 未辦 —— 🔒 **失效條件已於 `-186R` 更正**（⛔ 不得依附 `K-9-23`·列第三批） |
| `2` | `GB-107`（`wg960_z_selfcheck.py:12` 寫死絕對路徑·**本波六度實測仍在**） | ⬜ 未修 |
| `3` | `GB-108`／`GB-110`（八格表⛔ 無判別力） | ⬜ 未辦 |
| `4` | `GB-111`（凍存期望 FAIL 名單已失效·**須 KL 決重產時點**） | ⬜ |
| `5` | `GB-112`〜`GB-120` | ⬜ 只登記 |
| `6` | `GB-121`（施工單逐字⛔ 不入倉 ⇒ 停機款字面事後⛔ 不可現查）·**機制須 KL 裁** | ⬜ 未解 |
| `7` | `GB-122`／**`GB-123`（🔶 部分：守備已上線·射程外五項使其⛔ 未消滅）**／`GB-124` | ⬜ 只登記 |
| `8` | `GB-18`（`K-9-8` 之） | 🔴 仍未修 |
| `9` | 九份 baseline 重產（**須 KL 決時點**） | ⬜ |
| `10` | `K-9-22` 落地／`VR-074` 之他層／`VR-075` 落地 | ⬜ |
| `11` | 🆕 **`GB-125`**（算術自檢恆真）／🆕 **`GB-126`**（`ORDER_INVARIANCE` `7` tag 空轉） | ⬜ 只登記 |
| `12` | 🆕 **「戒」之定義列式未定**（裸框 MAX 被次號書寫污染·CC 已提案 `` ^🛑 戒 `?N`? ``） | ⬜ **待發單側裁** |
| `13` | 🆕 **`GB-126` 案由載「`7` 個 tag」而實測零執行者為 `9` 個** | ⬜ **待發單側判是否加註** |

🔒 **已辦結而自清單撤除者**：`VR` 取號污染（`-183R`）／已作廢清單之補列（`-184R`）／自誤定義列式之正典框（`-184R`·採**寬形**）／矩形姿態之待決（`-186R`）。

---

## `§六`　🔑 本波所立之家法（🛑 接手者必讀·**只列本波新立者**·完整清單見 `CLAUDE.md` 末端）

1. **`戒 35`**：新閘須先以既有資料驗其成立，方得立為停機款（須附①驗證母體②結構性理由；闕一 ⇒ 只得立為**受檢之假說**）。**配套**：停機款之措辭⛔ 不得排除「本款自身不成立」之可能。
2. **`戒 36`**：**非鑄號**之澄清／加註／補正，⛔ 不得採該類號之**定義列式形**（本倉體例 ＝ `🔬`／`🔧` 起首）；供形者與受單方**各驗一次**。
3. **`戒 37`**：閘款內援引已立之通則，須**逕引其名與出處**，⛔ 不得以**列舉**代之（列舉必不窮盡）。
4. **`戒 38`**：**末端追加之加註，其受詞須以「`檔:列` ＋ 標題逐字」錨定**；⛔ 不得用「本則」「上開」等**位置指涉**——末端追加之物與其受詞之間必隔著其後各批之新增。
5. **`戒 39`**：**「逐字」之框須逐則宣告**（(a) 原始位元組／(b) 去 markdown 標記後／(c) 語意等同）；**未宣告者一律以 (a)**；⛔ **`grep` 之輸出⛔ 不得作為引文之來源**（經 `cut`／`head`／終端渲染者已非原始位元組）；發單側⛔ 無法親自抽出時改用委辦式。
6. **`常規四（八）` 補款**（`-183`）：**取號框 ＝ 定義列式命中集 ∖ 已作廢之人造受詞清單** ⇒ 每次現查**未扣除／已扣除二數並報**。
7. **`GB-125` 之施行式**（`-184`）：取號現查**⛔ 不再以「算術自檢 True」為憑**，改出艙 `相異`／`MAX`／`缺號數` **三數本身** ＋ **二形分報之差集**。
8. **`W-G.9-185 §六 一` 之判準**：**已入倉之偽述 ⇒ 鑄號；入倉前經受單方攔下之偽述 ⇒ 就地加註。**（為抑制登記膨脹而立·⛔ 非寬免）

### `§六-1`　🩸 陷阱（本波實測·⛔ 不重踩）

1. **heredoc 吃反斜線／全形括號 `）` 於 shell 致解析錯** ⇒ 含 `\` 或全形括號之內容**改用 `Write` 直寫檔**。
2. **`grep` 之負存在斷言⛔ 不得接 `head`／`tail`**。
3. **`bytes` 相同 ⛔ 不等於內容相同**（`KL_UI_*_逐候選評點明細.csv` 二態 bytes 皆 `2783` 而 `sha256` 相異）。
4. **`wg942` 對 `HEAD` 比 ⇒ 必於 `commit` <u>前</u>跑**（入倉後恆綠）。
5. **Git Bash 之 `/tmp` 與 Windows Python 之 `/tmp` ⛔ 非同一目錄** ⇒ 跨二者一律用 `/c/xxx` ＝ `C:/xxx`。
6. **`SELF_SHA256` 之抽取須取檔內<u>最後</u>一個**——補正單常引用前一單之值，取第一個會誤報不符（本窗自捕）。
7. **量測器之「位置指涉」自檢會咬到自己**——宣告句「⛔ 不用『本則』『上開』」本身含該二詞（本窗自捕·⇒ 須看上下文）。
8. **Python tuple 之清單新增一項，若插於<u>末項之後</u>必須改末項之 `")):`** ⇒ 觸「⛔ 不得改既有列」；**解法 ＝ 插於末項<u>之前</u>**（`-184R` 之實例）。

---

## `§七`　已知 harness 限制

- 🔴 `run_verification` 於 `②-宗 圍堵閘破[R2]` **中止** ⇒ **`wf_f0`〜`wf_f4` 主路徑從不執行**
  （`-186R` 實測：五模組**連 `sys.modules` 都⛔ 未載入**）。
- 🔴 `app.py:main()` 內之敘述**從不被 `run_all` 執行**（`CLAUDE.md`）⇒ app 路徑之池呼叫端 `app.py:20892` 在 `main()` 內。
- 🔧 **逐街廓 `run_step_g` 可繞過 `GB-118`**：`run_step_g(ns, …, [tp for tp in build_p if tp.get("所屬街廓")==<blk>], …)`
  ⇒ **六街廓之池皆可攔到**（`R2`／`R4`／`R5` 雖各拋 `RuntimeError`，**攔取在前**）。範本 ＝ `verify/probes/probe_WG9183_pool_cover.py`。
- 🔧 **`_proj_pop` 之觀測⛔ 不需 spy**：設環境變數 **`WV_PROJ_POP_COUNT`** ⇒ **生產碼自身**之 `_proj_pop_note` 記錄每次執行（`-186R` 之量法）。
- 🔒 `run_all` 於基座即 `FAIL`（**准紅碼**）；驗收改為與凍存名單「**名稱＋原因**」一併比對；且會改寫 `verify/out/**` 之**已追蹤** log（`GB-94`）。
- 🔒 **KL 之 UI 係目前唯一可達完整終態之儀器**；其產物 ＝ `verify/out/KL_UI_3.5m_*`（`10` 檔）；
  🔴 **`R5` 於該趟在 `K-9-4` BASELINE 臨接閘拋 `RuntimeError` 而終止**（`stdout.log:2985`）⇒ `[T2-DIAG]` 僅 `5` 列、**`R5` 缺席**（已凍存為**期望事實**·`fixture_klui_t2diag`）。
  ⚠️ `[T2-DIAG]` 之數係 **4dp 顯示值** ⇒ **⛔ 不可「逐位」對拍**。

---

## `§八`　memory 之指引（新 session 自動載入 `MEMORY.md`）

🛑 **施行式**：`MEMORY.md` 之主題行係**併看入口**；查具體主題時**須循主檔之併看清單再讀該檔**。
🔒 **保留不聚合之 `4` 條（每窗必讀）**：`push-before-shipping-claim`／`grep-multibyte-charclass-trap`／
**`wg9143-handoff-entry`（主線入口·已同步為本窗）**／`harness-worktree-crlf-trap`。
⚠️ **memory 所載之具體數字皆易腐**（逐批前進即變）——⛔ 不得照抄，一律當場現查。

---

## `§九`　回報紀律（`CLAUDE.md` 常設·⛔ 不得省）

- 🔒 **未 push 不得報收官**：前置 ＝ `git rev-parse origin/wip/s1-endpart` 已含該 commit（**三重驗證**：`ls-remote` 直查／`is-ancestor` `rc=0`／`left-right count` ＝ `0 0`）。
- 🔒 **自評 gate 綠⛔ 不可替代獨立復現**。
- 🔒 **數字錨引倉檔**，⛔ 禁憑記憶出艙；`【倉】`／`【單】` 之標記法須施用。
- 🔒 **任一不符 ⇒ 照實回報**，⛔ 不得修補、⛔ 不得改期望值、⛔ 不得改框／母體／容差使其相符。
- 🔒 **停機款觸發一律先停機**（⛔ 不得先辦後呈），於停機回報中**併陳擬辦之意見**。
- 🔒 **`GB-121` 施行式**：施工單之**停機款**須於報告 `§一` **逐字引錄**其原文（⛔ 非摘要）。
- 🔒 **收工閘必跑** `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py`
  （期望 `rc=0`·`stderr` `0` B·受檢 **`77`** 筆·`嚴格前綴＝True` ×`77`）。🛑 **於 `commit` 前跑**。
- 🔒 **新探針入倉前必過**「禁寫死本機絕對路徑」全倉機檢（**本批新增**命中須 `0`·附判別力對照）。
  ⚠️ 全倉現有 **`4`** 個**既存**命中（`probe_WG971_wrap.py:203`／`:247`／`tools/nameset_diff.py:19`／`tools/wg960_z_selfcheck.py:12`）。
- 🔒 **`fixture_klui_t2diag.py` 自 `-184R` 起已掛入 `run_all`** ⇒ 每批**單獨跑**其 `rc` 並回報。
- 🔒 **拋棄式 clone 跑畢整份刪除**並於回報中確認。
- 🔒 **常規一（push 之自辦）**：四項齊備者逕行 push（① 零生產碼 ② `deletions = 0` 或既有檔純附加 ③ `app.py` 期末 blob 逐位＝期望值 ④ 開工／收工閘全綠）。
  🛑 **生產碼批⛔ 不得逕行** ⇒ **幾何本體批第一張單起須請示**。
- 🔒 **完工聊天回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、**原樣貼**其輸出 ＋ 倉內報告路徑。
