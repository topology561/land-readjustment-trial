# `W-G.9-269R`　`c4` —— 執行報告（**零生產碼**·補令十六 `§三`／`§五` 序 `3`）

> **受詞** ＝ 補令十六 `§三` `(a)`〜`(c)`：差異表定稿／`GB-160` 閘 `γ` 重量／`GB-159` 三條件逐條結清判。
> **交付物** ＝ `docs/reports/W-G.9-269R_c4_差異表.md`（呈 KL·域語言）。

---

## `§零`　開工閘（**逐條當場復現**）

### `零-1`　遠端二 `ref`（`git ls-remote` 實查·⛔ 本地追蹤 ref）

| `ref` | 實查值 |
|---|---|
| `wip/s1-endpart`（主線） | `8f4281c8933a18c33ae40c0b4b975304ed3c6402` |
| `verify/W-G.9-269-p3a`（凍存） | `05a11bd665f6c2790ff821c9e3a1606651e345d4` |
| `verify/W-G.9-269-c3`（已併線） | `ce06622f2caa8e0f3bac03c347c7e041e84b73b4` |
| `verify/W-G.9-269-c2`（已併線） | `dc373d018fa04a2a2538d04b6b194a1dfb4dcd3d` |

🔒 **`c3` 之併線 `commit`（補令十六 `§二` 令載於 `c4` 報告）＝ `ce06622f2caa8e0f3bac03c347c7e041e84b73b4`**
——主線由其**快轉**推進；本窗開工時主線已在其後裔 `8f4281c`。

### `零-2`　`left-right` ＋ 態宣告（**二值並報·以 `hash-object` 為實**）

| 項 | 值 |
|---|---|
| `git rev-list --left-right --count origin/wip…HEAD` | **`0`　`0`** ✅（⛔ main 態·⛔ CRLF 陷阱） |
| `CLAUDE.md`　`rev-parse HEAD:` ／ `hash-object` | `579d8df329d3e22861d08062b15b284cabcb834a` ／ **同** ✅（`2944` 列） |
| `app.py`　`rev-parse HEAD:` ／ `hash-object` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` ／ **同** ✅ |
| `app.py`　file `sha256`（**具名其式**·坑 `21`） | `8da160742e230550a72d259744527a3394f311ef35af0271cd10926adfa61339` |
| `git status --porcelain`（期初） | **`0`** 列 |

### `零-3`　併線之失效條款（補令十六 `§二`·當下復算）

| 項 | 實測 |
|---|---|
| `dc373d0`（`c2` 併線錨）∈ 主線 | ✅ |
| `ce06622`（`c3` 併線錨）∈ 主線 | ✅ |
| `05a11bd`（`p3a` 凍存）∈ 主線 | ❌ **⛔ 在**（如期·**對照組**：證該檢⛔ 恆真） |

---

## `§一`　開工閘之前：**逐坑覆述**（`63` 坑·補令十六所令）

🔒 **索引器** ＝ `verify/probes/probe_WG9269_pit_index.py`。**器之自檢先跑**：
`worktrees`／`REPO *=` 之命中 **`0`**（⛔ 硬編他樹）；對照組 `import` ＝ **`10`**（必非零）⇒ **器非紅**。
🔒 **三軸**：`(1)` 來源 ＝ `blob@8f4281c…`／`(2)` 母體 ＝ 該 `rev` 之全追蹤 `.md` **`714`** 檔／`(3)` 粒度框 ＝ **列框**。
🔒 **索引器之判別力二造**：造甲［必有定義］坑 `n` ⇒ 定義處 **`5`**（期 `≥ 1`）✅；
造乙［必為零］人造之坑（**執行期組出**·字面⛔ 出艙·`GB-147`）⇒ 定義 `0`／引用 `0` ✅ ⇒ **器非紅**。
🔒 **結果**：數字系 **`26`／`26`** ⋀ 字母系 **`37`／`37`** ＝ **`63`／`63` 皆可解析**，
**形／徵候／攔法三項逐坑齊備**，**⛔ 一則為 `0`** ⇒ ⛔ 觸「查不到定義 ⇒ 停機」之款。

🛑 **本節之覆述逐則<u>自索引器所指之定義處</u>取得**（`sed`／`git show` 逐字讀過）——**⛔ 憑記憶、⛔ 猜**。
🔒 **`自誤 347` 之通則**：⛔ 以「已讀過該交接註」代替「已執行其攔法」⇒ 各坑之**施行**逐則列於末欄。

### `一-1`　數字系 `1`〜`26`

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `1` | Bash 迴圈以 `git ls-tree`（未 `-z`）＋ `\|\| true` 掃 ⇒ 得 `0` | 改 `-z` ＋ Python | **已施行**：`34` 檔與母體之列舉皆用 `git ls-tree -r --name-only -z`；⛔ 用 `\|\| true` |
| `2` | 憑記憶寫標記字面 ⇒ 得 `0` | 「`0` 最該懷疑」⇒ 讀碼面之 `print` 字面 | **已施行**：`🔴 FAIL` 之字面**當場自 log 取**，得 `40`（`24 + 40 = 64`） |
| `3` | AST 取 `run_step_g` 找街廓迴圈 ⇒ `AssertionError`（其為薄殼） | 實作在 `_run_step_g_impl` ⇒ loud | **未觸及**（本批⛔ 取 AST 於該符號） |
| `4` | 扁平 AST walk 判跨迭代相依 ⇒ comprehension 變數誤判 | 逐處直讀源碼否證 | **未觸及** |
| `5` | 寬字樣命中數**憑記憶**寫 | 當場實測並於落地前更正 | **已施行**：本報告一切命中數**當場復算**並載三軸 |
| `6` | `sed` 替換端把反斜線讀為轉義 ⇒ 路徑變形 | 一律 `python re.sub`／`Edit` | **已施行**：本批之改檔全走 `Write`／`Edit`；⛔ 用 `sed` 置換 |
| `7` | 命令中殘留 heredoc ⇒ `wg9237_heredoc_guard` 硬阻斷 | 改以 `Write` 落檔、以**路徑**呼叫 | **已施行·本批親踩二次** ⇒ **活體攔截實測**（見 `§四` 自捕 `1`） |
| `8` | `harvest()` 回 `tuple` ⇒ `TypeError`（loud） | `ns, fake_st = harvest()` | **已施行**（二支新器皆如此） |
| `9` | `rc ≠ 0` ⛔ 等同受詞紅 | 讀 `rc` 同時必讀其閘紅逐項 | **已施行·本批正面撞見一例**：`V-1′` 器 `rc = 4` 判為**loud 拒測**、⛔ 受詞紅（見 `§三-(b)`） |
| `10`／`11` | 倉內探針硬編他窗 `worktree` ⇒ `rc = 0`、輸出完好、**量到他樹** | 跑任何探針前 `grep "worktrees\|REPO *="` | **已施行·三支皆查**：`pit_index`（`0`）／`v1prime_mem`（`REPO` 由 `HERE` 導出·⛔ 硬編）／二支新器（`0`） |
| `12` | 跨二源碼列之路徑逃過 `worktrees[/\\]` | 用**裸字樣** ＋ 併查 `os.chdir(` | **已施行**：框為裸字樣，併查 `os.chdir(` ⇒ 皆 `0` |
| `13` | 對照組只備「必不命中」⇒ 證不出撈得到本類 | **三造**（必非零／人造哨兵／本類已知成員） | **已施行**：`GB-159` 器之三造（`628-31(1)` 必命中／人造 `0`／受詞二宗） |
| `14` | 哨兵之號寫成字面隨檔入倉 | **執行期組出**、字面⛔ 出艙 | **已施行**：本報告與所有入倉檔**⛔ 含任何哨兵字面** |
| `15` | `c1_synth` 之二式係定位修好之**結果**⛔ 新破損 | ⛔ 改判準以就結果 | **未觸及**（受詞為 `c4`） |
| `16` | 活體檢取合成幾何 ⇒ 二態皆拋、同紅、本檢失能 | 改以**分支之被觀測**為可觀測量 | **未觸及**（本批之新器係**單態**·⛔ 需活體檢） |
| `17` | 「必紅」造可能**紅得不是地方** | 逐造檢其紅之**理由** | **已施行**：γ 器之造乙（注入）其紅之理由逐對出艙（`2/4`·具名被注入之宗） |
| `18` | `a` `cp950`／`b` `rc` 經管線／`c` `argv` 契約／`d` 截斷 SHA／`g` heredoc… | 逐項如該表 | **已施行**：`a`（全程 `PYTHONIOENCODING=utf-8`）／`b`（`rc` 一律**直取**落 `.rc` 檔）／`c`（用器前先讀其 `argv` 契約）／`d`（全 `40` 碼 `rev`）／`g`（見坑 `7`） |
| `19` | `Σ池` 取「街廓面積 − `ΣG`」⇒ 殘差恆零、閘恆真 | 取**獨立量測**之池 | **已施行**：復驗器之 `Σ池` 取「抵費地」列之**幾何面積**合計·⛔ 倒算 |
| `20` | 長跑之「無輸出」⛔ 得讀為「無差異」 | ⛔ 於命令內再 `&`；以 `.rc` 為完成之訊號 | **已施行**：長跑走 `run_in_background`·⛔ 用 `&`；**啟動前先 `rm -f <rc>`**；完成後以 log `161049` B ＋ `18` 檔 `got_*` 之 `mtime` **正面證其確已執行** |
| `21` | `app.py` 之 hash **二式並存** | 雜湊**具名其式** | **已施行**：`§零-2` 逐處載 `git blob sha1` 與 `file sha256`，⛔ 混稱 |
| `22` | 「就地換檔」取二樹之器，三項必備 | 如該則 | **未觸及**（本批⛔ 以就地換檔取二樹·⛔ 切態） |
| `23` | `git fetch` refspec 未帶 `+` ⇒ 追蹤 ref 半靜默停在舊值 | 態之唯一來源 ＝ `ls-remote` | **已施行**（`§零-1`） |
| `24` | 「拆器」只建新器而不清舊器 | 令建新器時同時令舊器之處置 | **已施行**：新建二器**⛔ 取代**任何既有器；既有 `V-1′` 三器**原地保留·⛔ 改一字**（其處置於 `§三-(b)` 逐支具名） |
| `25` | 移除一組造後，器之**結語列**虛報覆蓋 | 移造即更新結語 | **已施行**：二新器之結語**逐造列印其名與判** |
| `26` | 呈 KL 之文件內單所未令之**增列**亦受同一紀律所轄 | 增列者同受三軸／對照組之拘束 | **已施行**：差異表之增列（`三-2` 之**三**時點表·單只令二組）載其出處逐列 |

### `一-2`　字母系 `a`〜`i`

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `a` | 含 emoji 之器於 `cp950` ⇒ `UnicodeEncodeError` | **量測器紅、⛔ 稽核紅**；先設 `PYTHONIOENCODING=utf-8` | **已施行**：本批**全程**設之·⛔ 一次遺漏 |
| `b` | `rc` 經**管線** ⇒ 取到 `tail` 之 `rc` | `rc` **直取** | **已施行**：一切長跑之 `rc` 皆 `echo $? > *.rc` **直取**後再讀 |
| `c` | 器之 `argv` 契約被誤用 | 用器前先讀其契約 | **已施行**：`pit_index`（`<rev>…`）／`v1prime_mem`（`[倉根] [blk:side…]`）皆**先讀其 docstring 之逐字用法**方呼叫 |
| `d` | 以**截斷之 SHA** `fetch` ⇒ 靜默停在 `main` | 一律全 `40` 碼 | **已施行**：本批一切 `rev` 皆全 `40` 碼 |
| `e` | `wv_reconcile.report()` 回 tuple ⇒ 逕 `print` | 讀其回傳契約後再用 | **未觸及** |
| `f` | 探針未填某態之鍵 ⇒ `KeyError`（loud） | loud 即其攔法；併明文化其比較端 | **未觸及**（本批之器係**單態**·⛔ 有二態之鍵） |
| `g` | 誤帶 heredoc ⇒ 硬阻斷 | ⛔ 用 heredoc | **已施行·親踩二次**（見坑 `7`） |
| `h` | `grep -P` 於本機 locale 失敗 | 含 lookaround 者改 `python re` | **已施行**：含 lookaround 之錨定框（`(?<![0-9\-])`）一律走 `python re`·⛔ 用 `grep -P` |
| `i` | `GB-158`：B 形⛔ 單獨採信 | 二形並取並出艙對照組於各形之命中 | **已施行**：`§二` 之號占用查**二形並取**（完整號 ⋀ 裸標籤），並逐筆歸類 |

### `一-3`　字母系 `j`〜`m`

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `j` | 號之簿態隨 `ref` 而異 | 取號現查須二 `rev` 並查並出艙差集 | **已施行（以規避之形）**：本批**⛔ 鑄任何號**（補令十六 `§四`）；四簿之數係**唯讀**且單一 `rev`、已具名其 `rev` |
| `k` | 證據落於**未併線之側支** ⇒ 自主線 `git show` ⛔ 可達 | 須**逐字附側支名與 `commit`** | **已施行**：`GB-162` 之證據仍在 **`verify/W-G.9-269-p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`**（凍存·⛔ 併線），本報告引之逐字附之 |
| `l` | 「重烤」之受詞係**應然值** | `GB-162` 三禁令 | **已施行**：本批**⛔ 設 `WV_BAKE` 任何值**、**⛔ 觸 `verify/baselines` 一位元組**（其樹期初 ＝ 期末 ＝ `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`）；一切數**全自當次實跑** |
| `m` | 量測器之態宣告取 `HEAD` 而⛔ 工作區 | 二值並報·**以後者為實** | **已施行**：`§零-2` 之 `CLAUDE.md`／`app.py` 皆二值並報（本批二者相同·因工作樹乾淨） |

### `一-4`　字母系 `n`〜`x`

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `n` | `WV_BAKE` **之三害**（控制流改變·名目 `64 → 123`／覆寫集 `51` 檔／真實資料覆寫為 `0` B） | ⛔ 執行 `WV_BAKE=BASELINES`（`GB-162`·**永久**） | **已施行**：本批⛔ 設之；`verify/baselines` 期初 ＝ 期末 |
| `o` | `baseline` ＝ **應然值**；「它過期了 ⇒ 重烤」讀起來完全合理而重烤 ＝ **抹去缺陷證據** | `GB-162` 三禁令 ＋ 覆寫前須出艙該物之**角色** | **已施行**：同 `l` |
| `p` | 跨器逐位對拍 `0／11` 全不過，其不成立**恆由寫檔器所致** | 立閘前先出艙二者之**序列化約定** | **已施行（以更強之形）**：本批之 γ 量測**⛔ 跨器**——`side_rows`／`shared_edge` 係**同一模組之同一函式**，序列化約定**結構上同一** |
| `q` | 「某字樣命中 `0`」為閘者：五受詞於字樣框皆命中 `1` 而**全在 docstring** | 以 **AST** 去 docstring 機械判 | **未觸及**——本批**⛔ 以「某字樣命中 `0`」為任何閘**（唯一之 `0` 類宣稱為號占用，其框為**宣告框**且已逐筆歸類） |
| `r` | `git checkout <rev> -- <檔>` 切態者 `HEAD:<檔>` **恆不變** | 二值並報·**以工作區為實** | **已施行（以規避之形）**：本批**⛔ 切態**（單態量測）⇒ 結構上免疫；`§零-2` 仍二值並報 |
| `s` | 簿態隨 `ref` 而異 ⇒ 單方 `rev` ⛔ 足以判「未占用」 | 二 `ref` 並查 | **已施行**：號占用查之母體 `rev` 已具名；且本批**⛔ 鑄號** ⇒ 其結論⛔ 承擔任何取號 |
| `t` | 於「逐位不變」之 `commit` 內順手修**無涉**之既有瑕 | **具名而不修** | **已施行·本批用之二次**：`probe_WG9268p_c2_v1prime_mem.py` 之活體檢已失效（**具名而不修**·另立新器）⋀ `probe_WG9266_V1prime_3groups.py` 之自毀式資料源（**具名而不修**） |
| `u` | **對照組全零 ⇒ 先判量測器紅** | 先跑兩造自證方得出艙該 `0` | **已施行·且當場攔下二則**：γ 首版二造**全零** ⇒ 判**量測器紅**、⛔ 出艙（見 `§四` 自捕 `2`）；四簿之二對照組得 `0` ⇒ 判**量測器紅**、更正後方出艙（自捕 `3`） |
| `v` | **級聯剔除**：`on` 後 `0m` 消失 `3` 宗而閘一判不合格者僅 `2` 宗 | ⛔ 以「受詞二宗」呈 KL；「處置」欄須二類分列；`表三` 逐戶具名且級聯者**單獨小計** | **已施行**：差異表之「處置」欄**二類分列**（第一關直接剔除／🔴 級聯剔除）、`表三` **逐戶具名**且級聯者**單獨小計** |
| `w` | 其餘八則自捕 | 逐則之攔法見原文 | **已施行（三則）**：`w-3`（檔名列舉用 `ls-tree`）／`w-4`（引違規列時具名其為**總計數**）／`w-6`（heredoc 由硬閘攔下·**本批親踩二次**） |
| `x` | 補令四／五**僅存於凍存側支** | 引用者須逐字附側支名與 `commit` | **已施行**：本報告凡引凍存側支之物，逐字附 `verify/W-G.9-269-p3a` ＝ `05a11bd…` |

### `一-5`　字母系 `y`〜`ac`（前波末五坑）

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `y` | **列舉值之字面形與人所預期者相異**——`== "梯3"` 得 `[]`，而該 `[]` 讀起來正像「梯3 無群」之實 | ⛔ 憑欄名之語意猜其值之形——**先印該欄之值分布再定框**；否定判須備對照組 | **已施行·且本批親踩二次**：`推進側別` 之值分布**先印再定框**（得 `left`/`right`/`抵費地`）；四簿之對照組字面**憑重建**而得 `0` ⇒ 改**自母體逐字取**（見 `§四` 自捕 `3`） |
| `z` | **一格之值⛔ 可得時印 `0` 使整表之結論反轉** | 一律印 **`⛔ 可得`**，⛔ 印 `0`；`Σ` 亦然 | **已施行·且本批正是其受益者**：`GB-159` 器對「不在配地母體」者印 **`⛔ 在母體`** 而⛔ 印 `0` ⇒ `628-42(1)`＠`3.5m` 之 `4.77` **⛔ 被偽裝為 `0`（＝已剔除）** |
| `aa` | **表頭欄數 ≠ 資料列格數 ⇒ 欄索引靜默偏移**，而器仍印「三項齊備」 | 以欄名定位者須先證「表頭欄數 ＝ 資料列格數」 | **已施行**：`got_G值` 二檔逐檔證之——表頭 **`50`** 欄 ＝ 資料列 **`50`** 格（`0m` 全 `56` 列／`3.5m` 全 `59` 列）✅ |
| `ab` | **下游階段之閘先拋 ⇒ 該階段之量⛔ 可得，而其紅非本批所致** | 先比二態之例外訊息；改以**隔離量測**代之 | **已施行（以規避之形）**：本批之量測止於配地階段之落檔與 `run_step_g` 之 `g_rows`（**跑完·中止街廓「無」**）⇒ 結構上免疫 |
| `ac` | 🔴 **執行方之運行態⛔ 得為判準之受詞** | 出單側須指明其取得方式；未指明者該款⛔ 可評估 | **未觸及**——本單⛔ 以任何運行態為判準 |

### `一-6`　字母系 `ad`〜`ak`（前批末八坑）

| 坑 | 徵候 | 攔法 | 本批之施行 |
|---|---|---|---|
| `ad` | **`environ.pop(FLAG)` 取「某一具名之態」**——其所取實為**該旗標之預設** | **⛔ 以 `pop` 取態——須<u>顯式設值</u>**；凡改旗標預設須同批盤點全部 `pop` 取態之消費點 | **已施行（以更強之形）**：本批之新器**⛔ 觸任何旗標**——`WG9268P_ANCHOR_GEOM` 與 `WG9269_K917_BACKFILL` **皆已移除** ⇒ 單態量測、⛔ 有「取態」之動作 |
| `ae` | **沿用他批之活體檢**——其所繫者為**他旗標**，該旗標移除後其二態恆同 ⇒ 假綠 | 沿用他批之活體檢須先證其所繫之**選擇器即本批之選擇器** | **已施行·且本批正面撞見一例**：`probe_WG9268p_c2_v1prime_mem.py` 之活體檢繫於**已被 `W-G.9-268′ c3` 移除**之旗標 ⇒ 其 `rc = 4` **loud 拒測** ⇒ **⛔ 判綠、⛔ 沿用**，改**另立單態新器**（見 `§三-(b)`） |
| `af` | **單以「計數 ＝ `0`」為態之證**——`0` 有二讀法（分支未執行／管線未行至該處） | 活體檢須**二款並取** | **未觸及**（本批之器係**單態**·⛔ 以任何計數為態之證） |
| `ag` | **批次變換器之寫檔時點**——寫檔散置各變換之間 ⇒ 錨之 `assert` 中途停時成**半改之態** | 寫檔一律置於**全部變換之後** | **未觸及**（本批⛔ 動任何生產碼·⛔ 有批次變換） |
| `ah` | **註解錨於多站逐字相同** ⇒ `assert n == 1` 必炸 | ⛔ 取首筆；改 `expect=N` 明載期望命中數 | **未觸及**（本批⛔ 改任何碼面之註解） |
| `ai` | **形近 CJK 字致 `dict.get` 靜默回 `None`**（`守恒 U+6052` vs `守恆 U+6046`） | ⛔ 憑字形猜鍵——一律**自倉內之鍵逐字取其碼點** | **已施行**：`GB-159` 器**逐鍵印其碼點**（`暫編地號 0x66ab 0x7de8 0x5730 0x865f`／`G(㎡) 0x47 0x28 0x33a1 0x29`／`宗地寬度(m)`／`畸零地旗標`）⇒ 四鍵皆自表頭逐字取 |
| `aj` | **以「目錄內全部 `X*`」充「某次執行之產物」**——其中數檔之 `mtime` 早於本次二日 | 須先證**每一檔確為該次所寫**：`mtime` 晚於起跑，**或**執行前先清空該目錄 | **已施行（取更強之款）**：長跑**啟動前 `rm -f verify/out/got_*`**（清空實測 `0` 檔），跑畢得 **`18`** 檔，其 `mtime` **逐檔** ≥ `08:50:48` ＞ 起跑 `08:50:23` ✅ |
| `ak` | **本波其餘自捕**（`ak-1`〜`ak-6`） | 逐則之攔法見原文 | **已施行**：`ak-1`（`FAIL` 字面自碼面取）／`ak-3`（`rc` 直取）／`ak-4`（長跑前先 `rm -f <rc>`·**本批照辦**）／`ak-5`（判準綁**性質**⛔ 計數代理·見 `§三-(b)` 之 γ 判準綁「共用頂點恰 `2`」而⛔ 綁對數）／`ak-6`（含反引號之長字串一律以 `Write` 落檔·**本批全程照辦**） |

---

## `§二`　工項零：補令十七**原封入倉**

🔒 **入倉前之號占用查**（`作業常規之追加四` 補款 `一`·**宣告框**·三軸）
`(1)` 來源 ＝ `blob@8f4281c…`／`(2)` 母體 ＝ `docs/` 全檔 **`692`**（其中 `.md` **`664`**·讀不到 **`0`**）／`(3)` 粒度框 ＝ **檔框 ＋ 列框**。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | **列框(聯集)** | **檔框** | 鬆框列 | 判 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-269 補令十七`** | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0` | 🟢 **未占用** |
| `補令十七`（裸標籤） | `3` | `5` | `0` | `0` | `5` | `3` | `16` | 🟡 **逐筆歸類 ⇒ 皆他單號** |
| `補令十六`（對照甲-1·須 `≥1`） | `2` | `6` | `0` | `0` | `6` | `5` | `36` | ✅ |
| `補令十五`（對照甲-2·須 `≥1`） | `3` | `11` | `1` | `0` | `12` | `5` | `47` | ✅ |
| 人造（對照乙·**執行期組出**·字面⛔ 入倉） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | ✅ |

🔒 **裸標籤之 `8` 筆命中逐筆歸類**（`裁 H`：命中非 `0` 而未逐筆具名其歸類 ⇒ 停機）：
`docs/orders/W-G.9-268p_補令十七_併線授權與c1synth退役化.md`／
`docs/reports/W-G.9-268pR_補令十七_序1_2_執行報告.md`（`3` 列）／
`docs/reports/W-G.9-268pR_補令十七_序3_c4_執行報告.md`
⇒ **`8` 筆<u>全數</u>屬 `W-G.9-268′`（他單號）** ⇒ **⛔ 構成本單號之意指占用**。
🔒 **此即 `常規四（七）補款一` 之所以令用<u>完整號</u>而⛔ 用號之尾段**——其於本批**活體坐實**。

🔒 **入倉**：`docs/orders/W-G.9-269_補令十七_c4專窗開工令.md`
`(1)` 落檔 `sha256` ＝ `5db90297da6198dc8ce4c593d5bb5d1a51fa472d5836a2a95ae5c7df0ace7400`（`4680` B）
`(2)` 入倉後之 blob 內容 `sha256` ＝ **見 `§六` 收工復算**（逐位相符）。
🛑 **射程界限（照實·⛔ 頂替）**：該 `sha256` 所證者為「**落檔 → 入倉**」之保真，
**⛔ 證「發單側送出之原文 → 落檔」之保真**——來源係對話訊息、無獨立 `sha256` 可比。

---

## `§三`　`c4` 之三受詞

### `三-(a)`　差異表定稿

🔒 **交付物** ＝ `docs/reports/W-G.9-269R_c4_差異表.md`。硬款之逐條對照：

| 硬款 | 落點 | 判 |
|---|---|---|
| 三表 ＋ 「既有裁定」欄逐宗標明 | 差異表 `§一`／`§二`／`§三` | ✅ |
| `G025`／`G030` ＝ 已由 KL 裁定 (D) 判不配地（梯3）；`G011` ＝ ⛔ 既有裁定·本批為新 | `§一` 各表末欄 ＋ `§三-1` | ✅ |
| `ΣG_被剔` ⋀ `Δ池（實測）` **分欄並列**並具名其非同一量 | `§三` 表三（`29.49` vs `+45.03`·差二個數量級） | ✅ |
| 規格 §7 與當次實跑**二組並列並具名其時點** | `§三-2`（**三**時點·見下） | ✅ |
| 值域之 min／max 各自具名其宗與歸戶；⛔ 以區間掩去負值之個體 | `§三` 表三 ＋ `§一` 逐宗列出 | ✅ |
| `GB-159` 之「⛔ 對外」與其三解除條件載於**表首** | 差異表表首 | ✅ |
| 域語言·⛔ 程式術語／變數名／行號／旗標名 | 全文 | ✅（「閘一」改述為「**可分配判準第一關**」；⛔ 出現任何欄名／旗標名／行號） |

🔴 **單所令之二組數，本批實測為<u>三</u>個時點**（照實·⛔ 頂替·坑 `26`：增列同受三軸拘束）：

| 時點 | `G025` | `G030` | Σ 釋池 | 出處 |
|---|---|---|---|---|
| 規格 §7·`v3` 錨態 | `1.84` | `55.64` | **`57.48 ㎡`** | `docs/W-F_細部規格.md`（`grep -n "57.48" docs/W-F_細部規格.md`） |
| **改前**·現錨態 | `0.26` | `55.80` | **`56.06 ㎡`** | `docs/reports/W-G.9-269R_c2.md` `四-4` |
| **改後**·**當次實跑** | `0.26` | `26.54` | **`26.80 ㎡`** | 本批 `verify/out/WG9269Rc4_runverif.log`（`釋池對象` 之唯一命中列） |

🛑 **單之括號所載之 `0.26`／`55.80`／Σ`56.06` 係「<u>改前</u>」之值，⛔ 本窗之「當次實跑」**
——本窗之當次實跑係**病一落地併線後**之態，其值為 `26.80`。**二者相差 `29.26 ㎡` ＝ `628-27(1)` 之原分配面積**（級聯剔除之宗·**逐位相符**）。⇒ 差異表**三組並列並各具名其時點**，⛔ 以其一頂替。

🔒 **獨立復驗（本批之增值·⛔ 單所令）**：以**當次實跑**復算差異表 `表二` 之「**後**」欄
——`ΣG`／`Σ池`／街廓面積 **逐街廓逐格相符**（二情境 × `6` 街廓 ＝ **`12`** 格·相異 **`0`**）。
**判別力**：以 `0m` 之實測比 `3.5m` 之期望 ⇒ 相異街廓 **`3`／`6`** ⇒ **該比對非恆綠** ✅。
🛑 **`Δ` 欄⛔ 復驗**——其「前」態繫於**已移除之旗標**，於現態**結構上⛔ 可得**（⛔ 印 `0`·坑 `z`）；
`Δ` 之來源逐字為 `docs/reports/W-G.9-269R_c2.md`（**倉內**·其自身之當次實跑·⛔ baseline）。

### `三-(b)`　`GB-160` 閘 `γ` 之重量

🩸 **既有二器<u>皆⛔ 可用</u>（逐支具名·⛔ 修之·坑 `t`／`24`）**

| 器 | 何以⛔ 可用（**機械**·⛔ 推定） | 本批之處置 |
|---|---|---|
| `probe_WG9268p_c2_v1prime_mem.py` | 其二態由旗標 `WG9268P_ANCHOR_GEOM` 驅動，而該旗標已由 `W-G.9-268′` `c3` **移除** ⇒ 選擇器活體檢 **loud 拒測**。**本批當場實測**：`rc` **直取** ＝ **`4`**·`stderr` **`0`** B·落檔逐字載二種可能成因 `(a)` 受測之變更為 no-op／`(b)` 態之選擇機制已失效（`坑 ae` 之活體實例） | **具名而不修** |
| `probe_WG9266_V1prime_3groups.py` | 其資料源 ＝ `got_G值_退縮3.5m_partial.csv`，而 `_partial` **只在管線中止時產生**；病一落地後管線**已跑完** ⇒ 該檔⛔ 存在（**自毀式**）。**且完整落檔⛔ 帶 `cut_coords` 欄**——本批實測 **`18` 支 `got_*` 逐檔皆⛔ 含該欄** ⇒ 自落檔量 `γ` 於**結構上不可能** | **具名而不修** |

🔒 **⇒ 另立新器**（**零生產碼**·`verify/probes/probe_WG9269_c4_gamma.py`）：
閘 `γ`（共用邊恰 `2` 頂點）係**單態**之自我驗證閘、**⛔ 需二態**
⇒ 以**記憶體側**驅動生產管線**一次**（現態·旗標已不存在），取其 `cut_coords`，
再以**倉內既有 `V1` 之 `side_rows`／`shared_edge` 逐字**判 `γ`
⇒ **⛔ 另寫第二份 `γ` 之定義**、**⛔ 改 `V1` 一字**、**⛔ 改生產碼一字**、**⛔ 觸任何旗標**。

🔒 **量測器之二造（⛔ 恆綠亦⛔ 恆紅）**：造甲 `R3·left` ⇒ γ **`4/4`** ✅ 能綠；
造乙［注入］`R5·left` 之 `628-18(2)` 平移 `+1000 m`（改 **`1`** 列）⇒ γ **`2/4`** ✅ 能紅
⇒ **器非紅**。母體 ＝ `g_rows` **`59`** 列·⛔ 帶 `cut_coords` 之列 **`0`**·中止街廓 **無·跑完**。

**結果（情境 `3.5m`）**

| 側 | `GB-160` 舊值 | **本批重量** | 判 |
|---|---|---|---|
| `R5·left` | `3/5` | **`3/4`** | 🔴 **仍不過** |
| `R6·right` | `3/4` | **`3/4`** | 🔴 **仍不過** |

🛑 **⇒ 補令十六 `§三-(b)` 之「仍不過 ⇒ 成因⛔ 在退化幾何，須另診並具名」條款<u>觸發</u>。**

#### `三-(b)-1`　另診：成因之鑑別（全 `12` 側·⛔ 只看受詞二側）

🔒 **器** ＝ `verify/probes/probe_WG9269_c4_gamma_diag.py`（**零生產碼**·判準逐字沿用 `V1`）。
🔒 **全 `12` 側之 `γ`**：全過 **`7`** 側（`R1·right` `3/3`／`R2·left` `6/6`／`R2·right` `6/6`／
`R3·left` `4/4`／`R3·right` `4/4`／`R5·right` `1/1`／`R6·left` `2/2`）／
不過 **`2`** 側／**母體 `0`** **`3`** 側（`R1·left` ⋀ `R4·left` 各 `1` 宗、`R4·right` `0` 宗 ⇒ 無相鄰對·**標「⛔ 靜默綠」**）。
🔒 **判別力**：**⛔ 恆紅**（`7` 側全過）亦**⛔ 恆綠**（`2` 側不過）。

| 側 | 失敗對 | 共用頂點 | 宗 A（街角／分配面積／寬） | 宗 B | **假說** |
|---|---|---|---|---|---|
| `R5·left` | `628-45(1)+` ─ `628-18(2)` | **`0`** | **是**／`346.18`／`8.03` | 否／`206.72`／`6.32` | **`H2` 街角界面** |
| `R6·right` | `628-53(1)` ─ `628-38(1)` | **`1`** | 否／**`0.26`**／**`0.01`** | 否／`117.73`／`2.69` | **`H1` 退化幾何** |

🔒 **反面對照（⛔ 只看失敗對·否則假說⛔ 可否證）**：
含**街角地**而 `γ` **通過**之對 ＝ **`3`** ⇒ 🔴 `H2` 之「街角**必**敗」**被否證**；
含**近乎零之宗**而 `γ` **通過**之對 ＝ **`3`** ⇒ 🔴 `H1` 之「退化**必**敗」**被否證**。

🔒 **⇒ 具名之成因（照實·⛔ 擇一而不言其二）**

1. 🔴 **二失敗對之成因<u>相異</u>，⛔ 同一成因**：
   `R6·right` 之失敗對**含一退化宗**（`628-53(1)`·分配面積 `0.26 ㎡`·臨路寬 **`0.01 m`**）
   ⇒ **與 `GB-160` 原之推定<u>一致</u>**；
   而 `R5·left` 之失敗對**二宗皆健全**（`346.18 ㎡` 與 `206.72 ㎡`），其界面為
   **街角地（第 `0` 宗）↔ 其鄰宗**，共用頂點 **`0`**（＝**完全不相接**）
   ⇒ **與退化幾何<u>無關</u>** ⇒ **補令十六所預設之「成因⛔ 在退化幾何」於此側成立、於彼側不成立**。
2. 🔴 **`628-53(1)` <u>⛔ 為病一所剔除</u>**——病一所剔者為 `628-53(**2**)`（`R5` 左）、
   `628-42(1)`、`628-27(1)`；`628-53(**1**)` 在 `R6` 右，其可分配判準第一關判為「**合格**」
   ⇒ **留存** ⇒ 其退化幾何**仍在** ⇒ `R6·right` 之 `γ` **本就不應因病一而改變**（實測 `3/4` → `3/4`·**未變**）。
3. 🟢 **`R5·left` 確有改善**：由 `3/5` 變為 `3/4`——**對數減一而通過數不變**
   ⇒ **被消去者係一個<u>失敗</u>對**（`628-53(2)` 之退出所致）；其**殘餘之失敗對為街角界面**，⛔ 退化。

🛑 **本批⛔ 就此修任何碼、⛔ 調任何框**（`VR-094 二`：⛔ 調框以使其過）。
🛑 **`GB-160` ⛔ 解除**——其解除須俟該二失敗對之成因獲處置。

### `三-(c)`　`GB-159` 三條件之逐條結清判

🔒 **器** ＝ 本批之量測（落檔 `verify/out/WG9269Rc4_gb159_cond3.log`）。
🔒 **三軸**：來源 ＝ **當次實跑**之 `got_G值_退縮{0m,3.5m}.csv`（`mtime` `08:50:50`／`08:50:53`·晚於起跑 `08:50:23`）／
母體 ＝ `0m` 全 `56` 列（配地宗 `43` ＋ 抵費地 `13`）·`3.5m` 全 `59` 列（`45` ＋ `14`）／粒度框 ＝ **列**。
🔒 **二造**：造甲［必命中之已知成員］`628-31(1)` ⇒ **`1`** 列 ✅；
造乙［人造·執行期組出·字面⛔ 入倉］⇒ **`0`** 列 ✅ ⇒ **器非紅**。

| # | 條件（逐字） | **判** | 依據 |
|---|---|---|---|
| `(1)` | 病一落地併線 | 🟢 **成立** | `dc373d0`（`c2` 併線錨·補令十六 `§三-(c)` 所定）⋀ `ce06622`（`c3` 併線）**皆為主線 `8f4281c` 之祖先**（實查·對照組 `05a11bd` **⛔ 在** ⇒ 該檢⛔ 恆真） |
| `(2)` | 二宗依 `K-9-11 三` 處置完畢（不配地 ＋ 合併／調配 ＋ 入調配池 ＋ ⛔ 超配） | 🔴 **未成立** | `0m`：二宗**皆**不在配地母體 ⇒ 成立；**`3.5m`：`628-42(1)` 仍在配地母體**（`R2`·左）**分配面積 `4.77 ㎡`** ⇒ **未處置** |
| `(3)` | 二宗於配地表不再以近乎零之配地出現（或不在配地母體、或其 `G` 達法定最小建築基地面積） | 🔴 **未成立** | 同上——`4.77 ㎡` **＜** `115.85 ㎡`（法定最小分配面積【倉】`CLAUDE.md`），**臨路寬 `0.11 m`**，畸零地旗標 `⚠️移出/第1調配順位` |

🔒 **成因（機械·⛔ 推定）**：`628-42(1)`＠`3.5m` 之可分配判準**第一關**欄位值為 **`不適用`**
（⛔ `不合格`）——其值分布**先印再定框**（坑 `y`）：`3.5m` 全 `59` 列 ＝
`{'不適用': 40, '合格': 5, '': 14}`，**`'不合格'` 之鍵⛔ 出現**。
⇒ 病一之剔除迴圈**結構上⛔ 取之**（其受詞係「第一關**判為不合格**者」）。
🛑 **⇒ 此⛔ 病一之瑕**；該宗於該情境**未被第一關評估**。**其應否剔除係<u>域裁之題</u>。**

🔴 **併記（⛔ 誤讀·本批特為之具名）**：該宗之 `驗_總判` 欄為 **`剔除`**、`驗_rejected` 為 **`True`**。
🛑 **此⛔ 得讀為「已判剔除而仍配地」之瑕**——該欄係**另一條驗證鏈**（矩形容納·`K-9-12`），
其於 `3.5m` 對 **`20`** 宗皆標 `剔除`（含 `147.18 ㎡`／`153.19 ㎡` 等**健全**之宗）
⇒ 其**⛔ 生產之剔除依據**，且 `K-9-12` 於 `CLAUDE.md` 待落地表逐字載為
**「⛔ 純函式·觀測模式」⇒ 未接生產**。**⇒ 二欄分屬二鏈，⛔ 混同。**

🛑 **⇒ 三條件未齊備 ⇒ `GB-159` ⛔ 解除**（`⛔ CC 自裁`）。
**本判呈發單側，由發單側呈 KL**；其「⛔ 對外」**繼續有效**。

---

## `§四`　🩸 本批之**自捕**（照實·⛔ 隱去）

| # | 形 | 徵候 | 攔法／處置 |
|---|---|---|---|
| `1` | **heredoc 被硬閘阻斷**（坑 `7`／`g`／`w-6`）·**本批二次** | `wg9237_heredoc_guard` **硬阻斷**（`2` 次：號占用器、四簿器之更正） | 改以 `Write` 落檔、以**路徑**呼叫 ⇒ **該硬閘之活體攔截實測二次**。🔒 二次皆係**反射性**動作 ⇒ 坑 `g` 之「⛔ 用 heredoc」須列為**開工即設之硬習慣**，⛔ 事後補救 |
| `2` | 🔴 **γ 器首版二造<u>全零</u>**（坑 `u` 之教科書式） | 首版以**落檔**為資料源，受詞二側得 `0/0`、造甲 `0/0`、造乙 `0/0` ⇒ **四數皆零** | 依坑 `u` **先判量測器紅**、**⛔ 出艙該表**。追因得：`cut_coords` **⛔ 為完整落檔之欄**（`18` 支 `got_*` 逐檔皆無）⇒ 資料源結構上不可用 ⇒ 改**記憶體側**。🔒 **若當初只看受詞二側**（皆 `0/0`），其徵候與「`γ` 全破」**完全相同**，將產出一份「二側全滅」之假表 |
| `3` | **四簿對照組之字面<u>憑重建</u>**（坑 `y`／`ai`） | 對照組取 `自誤363`／`VR-95` ⇒ 皆 **`0`** 列（須 `>0`）⇒ 二造紅 | 依常規五**先判量測器紅**；追因得真形為 `` `自誤 363` ``（**帶反引號 ＋ 空格**）與 **`VR-095`**（**補零至三位**）⇒ 改為**自母體逐字取 `MAX` 之實際標題列**作對照 ⇒ 四簿八造**全數如期** |
| `4` | **單所載之「當次實跑」係<u>改前</u>之值** | 單之括號載 `0.26`／`55.80`／Σ`56.06`；本窗當次實跑得 Σ**`26.80`** | ⛔ 逕採單之數、⛔ 逕採本批之數頂替 ⇒ **三時點並列並各具名**，並將其差 `29.26 ㎡` **逐位歸因**於 `628-27(1)`（見 `§三-(a)`） |

🔒 **併記**：上列 `2`／`3` 二則**皆為「對照組為零」之形**——`2` 若未攔，將產出一份
**「`GB-160` 二側全滅」**之假表；`3` 若未攔，將使四簿之 `MAX` **看似無據**。
**二者之攔法同一：對照組全零 ⇒ 先判量測器紅，⛔ 先判受詞紅。**

---

## `§五`　四阻塞項之現況與解除條件（補令十六 `§五` 所令）

| `GB` | 現況 | 解除條件 | 本批之關係 |
|---|---|---|---|
| **`GB-159`** | 🔴 **仍有效** | 三條件皆備（見 `§三-(c)`） | **`(1)` 🟢 成立**／**`(2)` 🔴 未成立**／**`(3)` 🔴 未成立** ⇒ **⛔ 解除**。缺口 ＝ `628-42(1)`＠`退縮 3.5 公尺` 仍配地 `4.77 ㎡`（臨路寬 `0.11 m`）。**呈發單側 → 呈 KL** |
| **`GB-160`** | 🔴 **仍有效** | 退化之宗移出配地母體後**重量**該二側之閘 `γ`；仍不過 ⇒ 成因另診並具名 | **已重量**：`R5·left` `3/5`→**`3/4`**·`R6·right` `3/4`→**`3/4`** ⇒ **二側仍不過** ⇒ **已另診並具名**（`R6·right` ＝ 退化幾何〔`628-53(1)`·⛔ 為病一所剔〕／`R5·left` ＝ **街角界面**·⛔ 退化）。⛔ 調框 |
| **`GB-162`** | 🔴 **生效** | `(a)` `1137` 格逐項歸因 `(b)` 紅項降至已裁之准紅 `(c)` 重烤受詞集與不可逆性之處置逐項具名 | 本批**⛔ 設 `WV_BAKE` 任何值**、**⛔ 覆寫 `verify/baselines` 任一檔**（其樹期初 ＝ 期末 ＝ `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`·**對照組**：全樹異動 `7` 列 ⇒ 該檢⛔ 恆綠）、**⛔ 併線 `p3a`**、**⛔ 引任何自 baseline 取得之數**、**⛔ 出艙三戶之最終補償數**。證據仍在 `verify/W-G.9-269-p3a` ＝ `05a11bd665f6c2790ff821c9e3a1606651e345d4`（坑 `k`） |
| **`GB-163`** | 🔴 **仍有效** | 為**第一關未評估之宗**另備臨接長之量測，使判定集達於全部留存宗 | 本批**⛔ 引 `P-3` 為「每一留存宗皆臨二線」之證**（其判定集僅 `6/56`／`5/59`）——差異表與本報告**全文⛔ 出現該全稱** |

---

## `§六`　收工出艙

| 項 | 實測 |
|---|---|
| 生產碼 `34` 檔 | **⛔ 動一字**——`app.py` blob `4379108a…` 期初 ＝ 期末；`git diff --name-only` 對該 `34` 檔命中 **`0`** 行（見收工復算） |
| 本批之新增檔 | `docs/orders/W-G.9-269_補令十七_c4專窗開工令.md`／`docs/reports/W-G.9-269R_c4_差異表.md`／`docs/reports/W-G.9-269R_c4_執行報告.md`／`verify/probes/probe_WG9269_c4_gamma.py`／`verify/probes/probe_WG9269_c4_gamma_diag.py` |
| 鑄號 | **⛔ 鑄任何號**（補令十六 `§四`）；四簿之數期初 ＝ 期末 |
| `verify/baselines` | 樹 `241b2a2f98881148a8ae2b5ba8b45b5ae7f8f7d9`（期初 ＝ 期末） |
| `p3a` | `05a11bd665f6c2790ff821c9e3a1606651e345d4`（**未動**·⛔ 併線·⛔ 刪除） |

🔒 **四簿之數**（**每簿具名其框** ＝ 標題形 ∪ 表列形·三軸：`blob@8f4281c…`／各簿**單檔**／**列框**）

| 簿 | 母體（單檔） | 框 | 相異 | **MAX** | **次號** | 對照組 |
|---|---|---|---|---|---|---|
| 自誤 | `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`733797` B／`8579` 列） | 定義框（`VR-091 四`）∪ 表列形 ∪ 範圍框（`9` 條） | `353` | **`363`** | **`364`** | 必命中 `1` ✅／人造 `0` ✅ |
| `GB` | `docs/reports/W-G.4_泛用阻塞項登記表.md`（`787800` B／`6462` 列） | 標題錨定框（`W-G.9-205` 更正註）∪ 表列形 | `161` | **`163`** | **`164`** | 必命中 `1` ✅／人造 `0` ✅ |
| `VR` | `docs/驗證裁定登記表.md`（`394392` B／`5124` 列） | 定義框（`VR-092` 就地加註）∪ 表列形·扣哨兵 `999` | `93` | **`95`** | **`96`** | 必命中 `1` ✅／人造 `0` ✅ |
| `K-9` | `docs/rulings/K-6_街角地分配程序與可分配判準.md`（`295061` B／`4057` 列） | 標題形 ∪ 表列形 | `29` | **`29`** | **`30`** | 必命中 `1` ✅／人造 `0` ✅ |

🛑 **對照組之字面<u>自母體逐字取</u>**（⛔ 重建·見 `§四` 自捕 `3`）：
自誤 ＝ `` ### 🩸 `自誤 363`… ``／`GB` ＝ `` ### `GB-163` 🆕… ``／
`VR` ＝ `` ## `VR-095`… ``／`K-9` ＝ `### 🔒 K-9-29　…`。

---

## `§七`　🔑 **收工交接註**（補令十六 `§五` 序 `5`·本波新得之坑 `al`〜`ao`）

🔒 **標籤之取（`常規三`·無土地後果·CC 提案 ＋ 具名其由·<u>候發單側核可</u>）**：
既有字母系 ＝ `a`〜`z` ＋ `aa`〜`ak`（索引器實查·MAX ＝ `ak`）⇒ 續取 **`al`**〜**`ao`**
（⛔ 跳號·承「缺號永不重用」之精神）。
🛑 **坑之標籤⛔ 四簿之號**——補令十六 `§四` 之「⛔ 授任何鑄號」其受詞為**自誤／`GB`／`VR`／`K-9`** 四簿
（四簿之數本批期初 ＝ 期末·見 `§六`）。
🛑 **本表之表頭 `4` 欄 ＝ 其資料列 `4` 格**（施行**立準 `B`**〔坑 `aa`〕）。

| 坑 | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `al` | 🔴 **自毀式資料源之第二形：<u>欄之缺席</u>** | 既知之自毀式係「**檔**只在缺陷仍在時產生」（`_partial`）。本批遇其**第二形**——檔在、列在、**欄不在**：缺陷修畢後管線跑完，完整落檔**⛔ 帶 `cut_coords` 欄**（`18` 支 `got_*` 逐檔皆無）⇒ 以該欄為受詞之器**母體恆 `0`**。🔴 **其徵候與「受詞全破」完全相同**——首版四數皆 `0`，讀起來正像「二側之閘全滅」 | **⛔ 以「檔在」推「欄在」**——凡以某欄為受詞者，須**先印該欄之在否與非空列數**，母體為 `0` 即 **loud 拒測**（⛔ 靜默綠、⛔ 逕判受詞紅）。🔒 **其之所以現形，全賴坑 `u`**（二造全零 ⇒ 先判量測器紅）。🔒 **通則**：自毀式之受詞⛔ 限於「檔之存在」，**「欄之存在」「列之非空」同受其害** |
| `am` | 🔴 **器之退役，其成因須<u>逐支</u>診** | 本批之既有二器**皆⛔ 可用，而其不可用之成因<u>完全不同</u>**：`v1prime_mem` 係**活體檢繫於已被他批移除之旗標**（坑 `ae`）；`V1prime_3groups` 係**自毀式資料源**（坑 `al`）。🔴 **若只診其一而以「既有器皆不可用」一語帶過，另一支之成因即永不現形**——而該成因（欄之缺席）正是**下一支器之設計前提** | **凡宣稱「既有器⛔ 可用」者，須<u>逐支</u>具名其成因並各附機械證據**；⛔ 以「皆不可用」一語涵蓋。🔒 **併**：二支皆採**具名而不修**（坑 `t`）——其與本批受詞無涉，修之即動已凍之基準器。🔒 **新器之立須自證其⛔ 承接任一舊成因**（本批之新器：單態 ⇒ ⛔ 需活體檢；記憶體側 ⇒ ⛔ 依賴落檔之欄） |
| `an` | 🔴 **單所引之數，其<u>時點</u>與受單方之「當次實跑」相異** | 單之硬款逐字令「規格 §7 與**當次實跑**（`0.26`／`55.80`／Σ`56.06`）二組並列」。而本窗之**當次實跑**得 Σ **`26.80`**——單所引者實為「**改前**」之值（其出自前一批之報告）。🔴 **若逕採單之數填入「當次實跑」欄，將出艙一個<u>與本窗實測不符</u>之數，且其看起來完全正常**（二數皆真、只是時點錯置） | **⛔ 逕採單所引之數充本窗之「當次實跑」**——凡單以括號給定某數者，須**當場復算**並**二數並報**；相異則**逐時點分列並各具名其出處**，⛔ 以其一頂替。🔒 本批之處置：**三時點並列**（規格 `57.48`／改前 `56.06`／改後 `26.80`），並將其差 **`29.26 ㎡`** 逐位歸因於 `628-27(1)`。🔒 **⛔ 讀為單之誤**——單之數於其自身之時點為真 |
| `ao` | 🟡 **同名而分屬二鏈之判定欄** | `628-42(1)`＠`退縮 3.5 公尺` 之 `驗_總判` ＝ `剔除`／`驗_rejected` ＝ `True`，而其**第一關**欄 ＝ `不適用`。🔴 **逕讀前二者將得「已判剔除而仍配地」之重大瑕**——實則該欄屬**另一條驗證鏈**（矩形容納·`K-9-12`），其於同情境對 **`20`** 宗皆標 `剔除`（**含 `147.18 ㎡`／`153.19 ㎡` 等健全之宗**），且該鏈於 `CLAUDE.md` 待落地表逐字載為「⛔ 純函式·**觀測模式**」⇒ **未接生產** | **凡以某「判定欄」推「生產之處置」者，須先證<u>該欄即驅動該處置之欄</u>**——法有二：① 印該欄之**全體值分布**（若其對多數健全之宗亦標同值 ⇒ 其⛔ 為生產判準）；② 查該鏈於正典之**落地狀態**（觀測模式者⛔ 為據）。🔒 **本批二法並用**方未誤報一個不存在之重大瑕 |

### `七-1`　**本波所立之坑之定義處清單自證**（補令九 `§二-4`）

🔒 **索引器之 `ALPHA_LABELS` 已擴至 `al`〜`ao`**（`verify/probes/probe_WG9269_pit_index.py`·**⛔ 生產碼 `34` 檔**）。
🔒 **自證**：擴框後重跑索引器，`al`〜`ao` 之**定義處須 `≥ 1`** 且**形／徵候／攔法三項齊備**。

🔒 **前後二態並報（⛔ 恆綠之自證）**——⛔ 只報「入倉後為 `≥ 1`」，其單獨**⛔ 帶資訊**：

| 態 | `al` | `am` | `an` | `ao` | 落檔 |
|---|---|---|---|---|---|
| **入倉前**（`blob@8f4281c…`·**前對照**） | `0` ⚪ | `0` ⚪ | `0` ⚪ | `0` ⚪ | `verify/out/WG9269Rc4_pitidx_post.log` |
| **入倉後**（`blob@35d004f`） | **`1`** ✅✅✅ | **`1`** ✅✅✅ | **`1`** ✅✅✅ | **`1`** ✅✅✅ | `verify/out/WG9269Rc4_pitidx_self.log` |

🔒 **⇒ 四坑皆由 `0` **轉為** `≥ 1`** ⇒ 該自證**⛔ 恆綠**。
🔒 **入倉後之總數 ＝ `67`／`67` 皆可解析**（數字系 `26` ＋ 字母系 `41`）·**⛔ 一則為 `0`**；
`rc` **直取** ＝ `0`·`stderr` `0` B。
🔒 **判別力二造**：造甲［必有定義］坑 `n` ⇒ 定義處 **`6`**（期 `≥ 1`）✅；
造乙［必為零］人造之坑（**執行期組出**·字面⛔ 出艙）⇒ `0`／`0` ✅ ⇒ **器非紅**。

### `七-2`　**起手清單**（次窗開工之第一動作·⛔ 憑記憶）

1. **`git ls-remote`** 實查三 `ref`：`wip/s1-endpart`（主線）／`verify/W-G.9-269-p3a` ＝
   **`05a11bd665f6c2790ff821c9e3a1606651e345d4`**（🛑 凍存·⛔ 併線·⛔ 刪除）／
   `verify/W-G.9-269-c3` ＝ `ce06622…`（已併線）。**⛔ 用本地追蹤 ref。**
2. **`left-right`** 須為 **`0　0`**；非此則自動載入之 `CLAUDE.md` 係 main 態（缺數百列規則）⇒ **停機**。
3. **態宣告二值並報**（`rev-parse HEAD:<檔>` ⋀ `hash-object <檔>`·**以後者為實**）。
4. **跑坑索引器**（`PYTHONIOENCODING=utf-8`·約 `2`–`3` 分）⇒ **逐坑覆述徵候與攔法**；
   查不到定義者⛔ 猜、⛔ 略過 ⇒ **停機上呈**。現態應得 **`26` ＋ `41` ＝ `67`／`67`**。
5. **四阻塞項全數未結**：`GB-159`（俟三條件·**其解除⛔ CC 自裁**）／`GB-160`（二側 `γ` 仍不過·**已另診**）／
   `GB-162`（⛔ `WV_BAKE`·⛔ 覆寫 baselines·⛔ 併線 `p3a`·⛔ 引其數）／`GB-163`（⛔ 引 `P-3` 為全稱）。
6. **四簿之次號**（本批期初 ＝ 期末）：自誤 **`364`**／`GB` **`164`**／`VR` **`96`**／`K-9` **`30`**。
   🛑 取號一律用**定義列式**（⛔ 裸框）；意指占用查**二形並取**（**完整號**·⛔ 號之尾段）。
7. 🔴 **呈 KL 而未獲裁之二點**（見結案報告 `§三`）：`G011` 之不配地係本批新生；
   **同一宗於二情境之處置相異**（`退縮 3.5 公尺` 仍配地 `4.77 ㎡`／臨路寬 `0.11 m`）。
   **在其獲裁之前，`GB-159` 之「⛔ 對外」繼續有效。**
