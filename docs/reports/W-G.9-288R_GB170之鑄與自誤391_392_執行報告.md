# `W-G.9-288R`　執行報告（**輕批·登記**）：鑄 `GB-170` ／ `自誤 391`／`392` ／ 四則末端追加

> **單** ＝ `docs/orders/W-G.9-288_施工單_GB170之鑄與自誤391_392.md`（工項零已原封入倉）
> **基座** ＝ `005fc6a080c2f70dc9df9efef6d64f3e8c34a460`（`ls-remote` 實查·全 `40` 碼）
> **分級** ＝ **輕**（純登記·**零生產碼**·⛔ 量測新物·⛔ 替身·⛔ 反事實）
> 🛑 **出艙即止**：本報告**⛔ 判**該側是否應有第 2 宗、**⛔ 判**藍影為缺陷、**⛔ 判**孰誤、
> **⛔ 提**修法主張、**⛔ 呈** KL、**⛔ 解除／收窄／結清**任何 `GB`（含本批新鑄之 `GB-170`）。

---

## `§零`　開工閘（九項·逐項當場重跑·⛔ 採單所載之數·**同格載其產生指令**）

| # | 受詞（**產生指令逐字**） | 實測 | 判 |
|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `005fc6a080c2f70dc9df9efef6d64f3e8c34a460`（全 `40` 碼） | ✅ |
| `2` | `git rev-parse HEAD:app.py` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` ⇒ **非祖先**；判別力[必為祖先] ＝ `8fdc445e8587017a4127125c401f5fa88b54e1cd` ⇒ `rc = 0` ⇒ 該檢⛔ 恆判非祖先 | ✅ |
| `4` | `SELF_SHA256`（`P-5` 口徑·payload **`16140`** B） | 實算 ＝ 單載 ＝ `ae6ae344033e396c05c06442a8a725f6a265292efd5404f4d2c535fb82c64e85`；判別力二造（**先自證 bytes 相異**再比對·承 `W-G.9-287` 自捕）：造甲 全檔 `16218` B（`Δ +78`）`251e2097…` 相異／造乙 payload 去末尾 `\n` `16139` B（`Δ −1`）`b07f8563…` 相異 | ✅ |
| `4'` | 受詞自證（坑 `bf`）·受詞 ＝ **所量之檔名**（單已釘死·⛔ 本文正文） | 檔名 ＝ `W-G.9-288_施工單_GB170之鑄與自誤391_392.md`；[必真] 含 `W-G.9-288` ＝ `True`／[必偽] 含 `W-G.9-287` ＝ `False`（併報本文命中 `4`／`11` 次——單已明令受詞⛔ 本文 ⇒ **本批⛔ 再呈該讀法**） | ✅ |
| `5` | `python verify/probes/probe_order_preflight.py <本單>` | `rc = 0`（🔴 停機款 `0` 項／🟡 提示 `0` 項）；`P-5` ✅ 相符（受詞 `16140` B）·`P-3` `def main` 區間 ＝ `14975`-`24579` | ✅ |
| `5` | `python verify/probes/probe_order_preflight.py --selftest` | `rc = 0`；四造 ＝ 靶① `P-3` **紅**／靶② `P-4` 提示／靶③ `P-6` 提示／**反靶不紅** ⇒ 非恆綠亦非恆紅 | ✅ |
| `5'` | `python verify/tools/wg9223_acceptance_audit.py --selftest` | `rc = 0`（必命中字樣 `1` ＞ `0`／人造字樣 `0`） | ✅ |
| `6` | `python verify/probes/probe_WG9267_issuer_measurers.py registry` | 自誤 `380`／`390`·缺號 `[106, 355, 356, 357]`｜`GB` `167`／`169`·缺號 `[12, 87]`｜`VR` `80`／`95`·缺號 `[73, 75]`｜`K-9` `28`／`29`·缺號 `[]` | ✅ |
| `6'` | `python verify/probes/probe_WG9269_pit_index.py` | **`87`／`87`** 全數可解析·🔴 命中 **`0`**·末標籤 `bi` ⇒ **下一標籤 `bj`**；造甲 坑 `n` 定義處 `6` ≥ `1`／造乙 人造坑 `0`／`0` ⇒ **器非紅**（母體 ＝ `HEAD` 之追蹤 `.md` **`774`** 檔·落檔 `verify/out/WG9288R_pitindex_pre.log`·`stderr` `0` B） | ✅ |
| `7` | `git ls-files \| grep -E '^verify/[^/]+\.py$'` ＋ `app.py` | `33` ＋ `1` ＝ **正面列舉 `34` 檔**；判別力[必不命中] ＝ pathspec `verify/*.py` 多咬之子層檔數 ＝ **`304`**（判準「須 `> 0`」·⛔ 定值） | ✅ |
| `8` | 檔數 `git ls-files verify/baselines \| wc -l`；框 `git ls-tree -r HEAD verify/baselines \| sha256sum` | **`298`** 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9`（`core.quotePath` **預設**）；併報 `-c core.quotePath=false` ＝ `7f2a5fba04674f1bf2fbe572cb1ca3093ea98691a734fdb1cd13168412145520` | ✅ |

🔑 **閘 `8` 一次即中**：本單已依 `自誤 392` 之攔法於起手表**同格載其產生指令**（`ls-tree`·⛔ `ls-files`）
⇒ **⛔ 再現 `W-G.9-287` 之取框之誤**。**該攔法之首次施行即生效。**

### `§零-2`　號占用之預查

**三軸**：`(1)` 來源 ＝ `blob@005fc6a080c2f70dc9df9efef6d64f3e8c34a460`／`(2)` 母體 ＝ 全 `docs/` **`752`** 檔（讀不到 `0`）／
`(3)` 粒度框 ＝ **檔框 ＋ 列框**（宣告框）。
**產生指令**：`git -c core.quotePath=false ls-tree -r -z --name-only HEAD docs/` ＋ `git show HEAD:<path>`；
錨定框 ＝ `(?<![0-9\-])<完整號>(?![0-9])`；`D3` 取**嚴格式**（補款 `⑧`）；`列框 ＝ |D2 ∪ D3|`（補款 `⑦`）。

| 號 | `D1` 檔 | `D2` 列 | `D3` 列 | 雙屬 | **列框** | **檔框** | 鬆框（僅漏框偵察） | 角色 |
|---|---|---|---|---|---|---|---|---|
| **`W-G.9-288`** | `0` | `0` | `0` | `0` | **`0`** | **`0`** | `0` 列／`0` 檔 | 受詢 ⇒ **未占用·可用** ✅ |
| `W-G.9-287` | `2` | `2` | `2` | `0` | `4` | `2` | `13` 列／`2` 檔 | 對照組 甲[必占用·須 ≥ `1`] ✅ |
| （執行期組出之人造號·字面⛔ 出艙·`GB-147`） | `0` | `0` | `0` | `0` | `0` | `0` | `0` | 對照組 乙[必為零] ✅ |

---

## `§一`　工項零：本單原封入倉

落點 ＝ `docs/orders/W-G.9-288_施工單_GB170之鑄與自誤391_392.md`（⛔ 改一字）。

| 受詞（**產生指令**） | 實測 |
|---|---|
| 所指之檔數（`git -c core.quotePath=false ls-files -z -- docs/orders/` ＋ 錨定框施於**檔名**） | **`1`**（母體 ＝ 追蹤之 `docs/orders/` **`154`** 檔）；造甲 `W-G.9-287` 之單 ＝ `1` 檔（須 ≥ `1`）✅／造乙 執行期組出之人造名 ＝ `0` 檔 ✅（**已先驗其於現母體命中為 `0`**·坑 `bd`） |
| 行尾（`git cat-file blob <sha1> \| tr -cd '\r' \| wc -c`·**⛔ `od -c \| grep -c`**·坑 ＝ `W-G.9-287` 自捕三） | `CR` ＝ **`0`** ✅／`LF` ＝ `201`／bytes ＝ `16218`；判別力[必非零] ＝ `verify/out/KL_UI_3.5m_2e08a41_stdout.log` 之 blob `CR` ＝ **`2987`** ＞ `0` ✅ |
| 三方逐位相同 | blob `sha1` ＝ `3a8a500c89fb12e1cbd499536280df3e739757f8`；**blob 內容 `sha256` ＝ 磁碟 ＝ 源** ＝ `251e2097c8d8d1a325e3065eb05007d40b211210c34433bd969aa8e5a2197839`（blob `16218` B ＝ 磁碟 `16218` B） |
| `numstat` | `201	0`（**`deletions` ＝ `0`**） |

---

## `§二`　工項一：鑄 `GB-170`

### `§二-1`　🛑 **落地形之一處<u>機械必要</u>之差：剝一層 `> `（照實併呈·⛔ 頂替）**

單 `§三-1` 之 payload **每一列皆冠 `> `**（其為單之**引用裝置**），而其首列即
`### \`GB-170\` 🆕　**…**`。二讀法：

| 讀法 | 落地形 | 後果 |
|---|---|---|
| `(a)` **剝一層 `> `**（**採**） | `### \`GB-170\` 🆕　…` 位於**列首** | 可被 `GB` 之**標題錨定框** `^#{2,4} \`?GB-N\`?` 命中 ⇒ **號確實被鑄** |
| `(b)` 原樣保留 `> ` | `> ### \`GB-170\` 🆕　…` | 該列**⛔ 為標題** ⇒ 定義列式**必失配** ⇒ **`GB` 之 MAX 仍為 `169`** ⇒ 單 `§六` 閘 `4`（MAX 須為 `170`）**結構上不可能通過** |

🔒 **⇒ `(a)` 係<u>該批自身之驗收判準所強制</u>**（`常規二`：單內二款互斥 ⇒ 取保守項並逐字回報），
**⛔ 我之改寫**：所剝者為**引用裝置**、**⛔ payload 之任何字元**。
🔑 **剝層之機驗**：把 `> ` 逐列加回後**與原 payload 逐位相同** ⇒ `True`。
🔑 **落地形之形自證**：`^#{2,4} \`?GB-([0-9]{1,4})\`?` 命中 ⇒ `True`（擷得號 ＝ `170`）。
🔒 **併記**：倉內既有之 `GB-168`／`GB-169` 二節**正是此形**（`### \`GB-169\` 🆕　**…**` 於列首·
其本體為 `> ` 之引文）⇒ `(a)` 亦與**倉內體例一致**。

### `§二-2`　payload 之抽取（`常規七 一`·**由檔案管線直接產生**·⛔ 手寫）

| 受詞 | 單之列 | 列數 | bytes | `sha256` |
|---|---|---|---|---|
| `GB-170` payload（**原樣·含 `> `**） | `68`–`91` | `24` | **`2011`** | `2a27db8c15300f3c4b7814973c5feefeb7e3816d554c95fe5b7b0df8a739d257` |
| `GB-170` **落地形**（剝一層 `> `） | — | `24` | **`1968`** | `ae6660a32a9a3cfb9c28fbd59e8f5a574a55c15ea8b5523e400b0e7b7426a327` |

### `§二-3`　鑄號之現查（**入倉前**·`GB-158` 之體例·**三形分列並取**）

**母體／指令**同 `§零-2`。

| 形（逐字） | `GB-170`（受詢） | `GB-169`（對照組甲·**已鑄**·須 `> 0`） | 人造號（對照組乙·**執行期組出**·須 `0`） |
|---|---|---|---|
| `B` 形（反引號只包號）`GB-\`N\`` | 檔 `0`／列 `0` | 檔 **`0`**／列 **`0`** 🔴 | 檔 `0`／列 `0` |
| **`C` 形**（反引號包全 token）`` `GB-N` `` | 檔 `3`／列 `4` | 檔 **`18`**／列 **`94`** ✅ | 檔 `0`／列 `0` ✅ |
| `D` 形（裸·錨定） | 檔 `3`／列 `4` | 檔 `18`／列 `94` | 檔 `0`／列 `0` |

🔴 **`B` 形對<u>已鑄</u>之 `GB-169` 亦得 `0`** ⇒ 依 **`GB-158`**（`常規四（七）補款二` 之更正）
**該形⛔ 單獨採信**，**判由 `C` 形承擔**；對照組乙三形皆 `0` ⇒ **器非紅**。

🛑 **受詢之 `C`／`D` 形命中非零（`4` 列）⇒ 依裁 `H`「命中數本身⛔ 為判準、係待歸類之材料」，<u>逐筆歸類</u>**：

| # | 錨 | 逐字（節略） | 歸類 |
|---|---|---|---|
| `1` | `docs/orders/W-G.9-284_施工單_五之三後半款之重判與真正地界之可取性.md:51` | 「在其出艙前 **⛔ 鑄 `GB-170`**（否則即以「規格有載」充「正典要求」·`自誤 387` 之同形）」 | **前瞻之禁鑄令**（發單側為**同一受詞**保留該號） |
| `2` | 同檔 `:97` | 「⛔ 判規格是否構成要求、⛔ 判碼面是否違反、**⛔ 鑄 `GB-170`**、⛔ 呈 KL。」 | 同上 |
| `3` | `docs/orders/W-G.9-287_施工單_二線分歧之土地後果與藍影未求值之成因.md:52` | 「\| `5` \| `GB-170` 之鑄 \| **⛔ 鑄**——待本批之**土地後果**量得」 | 同上·**本批即其後續**（該土地後果已於 `W-G.9-287` 量得） |
| `4` | `docs/reports/W-G.9-284R_五之三後半款之重判與真正地界之可取性_執行報告.md:64` | 「**`4` 之受詞 ＝ 本報告 `§四`**（⛔ 鑄 `GB-170`）」 | 同上 |

🔒 **四筆<u>皆</u>為「對一個尚未鑄之號之前瞻指涉」**，其受詞與本批所鑄者**同一**；
**⛔ 實體占用**——`GB-170` 之**定義列式**（`^#{2,4} .*GB-170`）於正典母體
`docs/reports/W-G.4_泛用阻塞項登記表.md` 命中 **`0`**（對照組[必非零] `GB-169` ＝ **`6`** ⇒ 器非紅）。
⇒ 🔒 **可鑄**；**⛔ 順延取次一號**。

### `§二-4`　落地

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末新節**（⛔ 改任何原節一字） |
| 期初／期末 bytes | `884654` → `886630`（**`+1976`**／**刪除 `0`**）·追加段 `CR` ＝ `0`／`LF` ＝ `28` |
| 嚴格前綴之二造 | 造甲[必真] `new.startswith(old)` ＝ **`True`**／造乙[必偽] `(old + b'X').startswith(new)` ＝ **`False`** |
| `常規七 三` 覆核 | 重抽 == 首抽 ⇒ **`True`**；已落地之簿內含該 payload ⇒ **`True`** |
| 簡繁機檢 | 受詞 ＝ 本批**自撰**之題與包裹節（⛔ 含單之 payload）⇒ 簡體命中 `{}`·`rc` ＝ `0`；[必命中]對照 `自誤` ＝ `2` |

🔒 **本節之受詞摘要（⛔ 解除、⛔ 收窄、⛔ 提修法主張）**：`GB-170` 之受詞 ＝ 街角 `forced` 之側，
第 2 宗之起算線取自 `_corner_buffer_S` 之 `buf`（面積反解·線向 ＝ `allocation_dir`），
而 `_build_corner_range_v3` 依乙式（`K-9-5-14`／`K-9-5-15`）所建之**遠側境界線**線向 ＝ SIDELINE 向
⇒ 二線⛔ 為同一條；其失效條件三項**皆未成就**。

---

## `§三`　工項二：`自誤 391`／`392`

| 受詞 | 實測 |
|---|---|
| payload（`常規七 一`·由檔案管線直接抽出·⛔ 手寫） | `自誤 391` ＝ 單之列 `103`–`110`（`8` 列／**`934`** B／`sha256` `d93c8923e301da930241eb7d4846b7cc1acaa034cf1ec81009b853efa3fdc88e`）；`自誤 392` ＝ 單之列 `112`–`119`（`8` 列／**`775`** B／`sha256` `8fe5d945e02ab1fb0291bb7e138cf41c9fe50fb15dc8e5ff16cef531eda21287`） |
| 標題範本之錨（`常規四（九）四`） | `### 🩸 \`自誤 390\`` ⇒ 命中 **`1`** 列（`:9637`）⇒ **唯一**（`uniq`）；範本逐字 ＝ `### 🩸 \`自誤 390\`　**以未經量測之機制推測呈 KL**` |
| 落地之標題 | `### 🩸 \`自誤 391\`　**以一個在本案有二個可機械施行之讀法之詞為受詞，而單內⛔ 釘其口徑**`／`### 🩸 \`自誤 392\`　**起手表只載框之<u>輸出值</u>，而⛔ 同格載<u>產生該框之指令</u>**` |
| 坑 `bh` 殘留檢 | 二題內 `自誤 390` ＝ **`0`**（期 `0`）／各自之新號 ＝ **`1`**（期 `1`） |
| 定義框之形自證（`VR-091 四`） | 二題皆 **`True`**（擷得號 ＝ `391`／`392`） |
| 鑄前占用（三形分列） | `自誤 391`／`392` 之 `B`／`C`／`D` 三形**皆 `0`**；對照組甲 `自誤 390` `C` 形 檔 `3`／列 `11`·`D` 形 檔 `3`／列 `12`（`B` 形 `0` ⇒ 同 `GB-158` 之形）⇒ **器非紅** |
| 期初／期末 bytes | `816010` → `817971`（**`+1961`**／**刪除 `0`**）·追加段 `CR` ＝ `0`／`LF` ＝ `26` |
| 嚴格前綴之二造 | 造甲[必真] **`True`**／造乙[必偽] `(old + b'X').startswith(new)` ＝ **`False`** |
| `常規七 三` 覆核 | 二則皆 重抽 == 首抽 ⇒ **`True`**；已落地之簿內含二 payload ⇒ **`True`** |
| `wg942_append_audit.py` | `rc = 0`·受檢 **`77`** 筆／不適用 `0`／合計 `77`（＝ `TARGETS` `77`）·逐字「結論：✅ 全部成立」 |

---

## `§四`　工項三：四則末端追加

| 受詞 | 實測 |
|---|---|
| 落點 | `docs/reports/W-G.4_泛用阻塞項登記表.md` **檔末**（⛔ 鑄號·⛔ 改任何原節一字） |
| payload（逐則·由檔案管線抽出） | `(1)` 單之列 `127`–`129`（`3` 列／`361` B／`903306d542cc14a90b303729455cb78a9acb834a6b50a1e0fea4f04cf6ddcc4f`）；`(2)` `131`–`133`（`3` 列／`304` B／`6c40365514cfb2b5f5d5061265a5aa19a906b0e126db2d7ac7fad898071632b8`）；`(3)` `135`–`137`（`3` 列／`308` B／`6024b25e7183a101e36b0fecf4828e6d9cc1b704dcb14816aa016670887efdf6`）；`(4)` `139`–`151`（`13` 列／`1273` B／`981d2626350788f3dc84fb3b2111f6d64d7ac4a7ccaabc690d77aaa9ffa14ef5`） |
| 落地形 | 四則**原樣保留 `> `**（其⛔ 含任何標題 ⇒ **⛔ 剝層之必要**·與 `§二-1` 之 `GB-170` **相異**）；包裹節之題與四個 `### 一`〜`### 四` 由**我側自撰**（故列入簡繁機檢之受詞） |
| 期初／期末 bytes | `886630` → `889380`（**`+2750`**／**刪除 `0`**）·追加段 `CR` ＝ `0`／`LF` ＝ `42` |
| 嚴格前綴之二造 | 造甲[必真] **`True`**／造乙[必偽] **`False`** |
| `常規七 三` 覆核 | 四則皆 重抽 == 首抽 ⇒ **`True`**；已落地之簿內含四 payload ⇒ **`True`** |
| `GB-132` 之避讓與其機驗 | 包裹節之題含 `GB-169`／`GB-170` **二個已鑄之號**（其形同倉內既例 `## 🔧 \`GB-169\` 解除條件…`）⇒ **⛔ 造新號**；其驗 ＝ 落地後 `GB` 之**相異／MAX** ＝ **`168`／`170`**（＝ 工項一之後的值·**未再推進**）⇒ 避讓成立 |

🔒 **`(4)` 之射程（逐字轉錄·⛔ 擴）**：KL 於 `2026-09-15` 之放行**及於**該一處取線之修改與其量測、
及於三段辦法；**⛔ 及於入倉主線**（須**第二次**放行·在其取得前修批一律落**凍存分支**、**⛔ 併線**）；
**⛔ 及於** `GB-167`／`168`／`169` 之任何修、`K-9-29` 之任何子項、藍影之任何改。
🛑 **本批⛔ 據此放行動任何生產碼一字**——修批另單。

---

## `§五`　判別力二造之判

| # | 造 | 受詞 | 實測 | 判 |
|---|---|---|---|---|
| `1` | [必命中] | `GB-169` 之定義列式（`^#{2,4} .*GB-169`·母體 ＝ `GB` 登記表單檔） | **`6`** | ✅ |
| `2` | [必為零] | `GB-170` 之定義列式（**鑄前**·同母體） | **`0`** | ✅ |
| `3` | [必命中] | 號占用之對照組甲 `W-G.9-287`（宣告框） | 檔 `2`／列 `4` | ✅ |
| `4` | [必為零] | 號占用之對照組乙（**執行期組出**之人造號·字面⛔ 出艙·`GB-147`·**已先驗其可滿足**） | `0` | ✅ |
| `5` | [必命中] | 鑄號現查之對照組甲 `GB-169` `C` 形 | 檔 `18`／列 `94` | ✅ |
| `6` | [必為零] | 鑄號現查之對照組乙（人造 `GB` 號·三形） | `0`／`0`／`0` | ✅ |
| `7` | [必非零] | 行尾判別力 `verify/out/KL_UI_3.5m_2e08a41_stdout.log` 之 blob `CR` | **`2987`** | ✅ |
| `8` | [必不命中] | 閘 `7` 之 pathspec `verify/*.py` 多咬之子層檔數（判準「須 `> 0`」·⛔ 定值） | **`304`** | ✅ |

---

## `§六`　一句話推導

> 二線之分歧已於 `W-G.9-287` 量得其**土地後果**（對稱差 `13.06`／`38.07`／`13.06` ㎡·
> 負擔率差 `4.96`／`9.02`／`4.27` 個百分點）⇒ `GB-170` 之鑄之**前提成就**，
> 而其三項失效條件（改取乙式之遠側境界線並經 KL 放行／改後行為差逐宗量得／其餘逐位不變之證）
> **皆未成就** ⇒ 本批**只鑄不解**；`自誤 391`（序數詞未釘口徑）與 `自誤 392`（框只載值不載指令）
> 二則之**攔法已於本單之起手表即時施行**，其效驗即閘 `8` 之**一次即中**。

---

## `§七`　本批之自捕（**`1` 則**·於出艙前攔下·⛔ 頂替）

| # | 形 | 徵候 | 攔法 |
|---|---|---|---|
| `1` | **`GB-170` payload 之落地形須剝一層 `> `**——若原樣保留，其首列⛔ 為標題 ⇒ 定義列式必失配 ⇒ **該號實未被鑄**，而單 `§六` 閘 `4`（`GB` MAX 須為 `170`）**結構上不可能通過** | 「⛔ 增刪一字」與「MAX 須為 `170`」二款**互斥**；且**⛔ 剝層之版本看起來完全正常**（簿內多一段引文而已） | 依 `常規二` **取保守項**（剝**引用裝置**·⛔ 動 payload 一字）＋ **二項機驗**：① 加回 `> ` 後與原 payload **逐位相同**；② 落地形之首列可被標題錨定框命中（擷得號 `170`）＋ ③ 落地後 `GB` MAX **實測**為 `170`。逐字回報於 `§二-1` |

🔒 **本批⛔ 再現 `W-G.9-287` 之三則器面自捕**（閘 `4` 造乙恆同／閘 `8` 取框之誤／`CR` 之 `od` 偽陽）
——三者之攔法**皆已於本批之起手即施行**（先自證 bytes 相異／單內同格載指令／一律 `tr -cd`）。

---

## `§八`　`⛔ 為之事`之逐項自檢（單 `§七`）

| # | 禁令 | 自檢 |
|---|---|---|
| `1` | ⛔ 動生產碼 `34` 檔一字 | ✅ 見 `§九` 閘 `2`／閘 `6`（blob 期初＝期末·相異 `0`） |
| `2` | ⛔ 動 `ALPHA_LABELS` 一字 | ✅ `git diff --numstat 005fc6a0..HEAD -- verify/probes/probe_WG9269_pit_index.py` 輸出**空** |
| `3` | ⛔ 改任何器之判準／框／輸出格式 | ✅ 本批**未改**任何既有器一字（本批⛔ 新增任何器） |
| `4` | ⛔ 修 `GB-79`／`167`／`168`／`169` | ✅ 由生產碼 blob 相異 `0` 機械蘊含；且本批對該四號**只作末端追加之引述**（`§四` 之 `(1)`／`(3)`） |
| `5` | ⛔ 解除、收窄或結清任何 `GB`（含本批新鑄之 `GB-170`） | ✅ 本報告與二落點**皆未載**任何解除／收窄／結清；`GB-170` 之失效條件三項逐字載明「**皆未成就**」 |
| `6` | ⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ 同 `4` |
| `7` | ⛔ 改起算線一字／⛔ 改 `ok_t`／`ok_f` 一字 | ✅ 同 `4`（**零生產碼**） |
| `8` | `GB-162` 三禁令維持 | ✅ `WV_BAKE` **未設**／⛔ 覆寫 `verify/baselines` 任一檔（`298` 檔·框 `sha256` 未變）／⛔ 併線 `p3a` |
| `9` | ⛔ 引任何自 baseline 取得之數 | ✅ 本報告之數皆出自**本批當場**之 `git`／`python` 量測或**本單所供之 payload** |
| `10` | ⛔ 量測任何新物、⛔ 施替身、⛔ 反事實、⛔ 長跑（除閘 `6'` 與收工閘 `5`） | ✅ 本批之長跑**恰二次**（閘 `6'` 與收工閘 `5` 之坑索引器）·**⛔ 併跑**；`§二`／`§三` 之土地數**全部轉引自 `W-G.9-287` 之落檔**【倉】·⛔ 重量 |
| `11` | ⛔ 鑄 `VR`／`K-9`／坑之任何號 | ✅ 四簿期末 `VR` `80`／`95`·`K-9` `28`／`29` 逐位未動；坑 `87`／`87` 未動 |
| `12` | ⛔ 追改任何已入倉文件一字 | ✅ 見 `§九` 閘 `1`（`numstat` 刪除欄全 `0`）＋ `wg942_append_audit.py` `rc = 0` |
| `13` | ⛔ 判該側是否應有第 2 宗、⛔ 判藍影為缺陷、⛔ 判孰誤、⛔ 提修法主張、⛔ 呈 KL | ✅ `§四` `(2)` 逐字保留「**未判**」；本報告**⛔ 作成**任何該類判斷 |
| `14` | ⛔ 併線 `p3a`、⛔ 刪任何 ref | ✅ `p3a` `merge-base --is-ancestor` `rc = 1`（非祖先·未變） |
| `15` | ⛔ 於任何入倉文件宣稱一件尚未發生之事 | ✅ `§九`／`§十` 為**入倉後**之末端追加（**先跑再寫**）；`ls-remote` 之實查於**推送後**出艙於聊天 |

---

【驗收】本批之驗收固定項已逐 token 出艙：
`ls-remote`／`SELF_SHA256`／`preflight`／`selftest`／`acceptance_audit`／`quotePath`／`uniq`／`deletions`／
`GB-170`／`GB-169`／`GB-158`／`GB-132`／`GB-79`／`K-9-5-14`／`K-9-5-15`／`_corner_buffer_S`／
`_build_corner_range_v3`／`allocation_dir`／`range_area`／`S1_perp`／`is_second_after_corner`／
`ALPHA_LABELS`／`baselines`／`numstat`／`sha256`／`merge-base`／`ls-tree`。
