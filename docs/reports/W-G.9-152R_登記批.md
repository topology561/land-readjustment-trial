# 🔧 `W-G.9-152R`　登記批（常規六／七 ＋ `K-9-19` 性質具名 ＋ 末端塊正典依據 ＋ `VR-074` ＋ 自誤 `189`）

**基座** ＝ `75ab7238d02bfc8b7dd9b83912602451072d84dd`　**`app.py` blob** ＝ `b4cc30835feaad03dfc37f394a669bbb45988b8b`（期末逐位相同·⛔ 零觸）
**性質**：🔒 **純附加登記**·⛔ 零生產碼·⛔ 未改任何既有文字（`deletions` 逐列 ＝ `0`）
**量測時點**：`2026-08-27`·態 ＝ `blob@75ab7238`
🔒 **本報告之四反引號區塊恰 `0` 個**。

> 🔴 **二句話結論**
> ① **`A-5` 不符**：單 `§四` 所載之 `K-6:1021-1022` 逐字 **⛔ 非逐位**——**省略**了「（`D-①` F 槽修正 ＋ `D-②` `K-9-5-4`）」（該括號**限定了「本裁定」之所指**），另有換行合併與粗體差。⇒ 已依**常規七**改以 `sed` 自倉切片入倉，⛔ 未依單文字。
> ② **`VR-073` ⛔ 未取·改取 `VR-074`**：`VR-073` 之定義列命中 `0`（⛔ 無實體），惟全倉 `5` 處前瞻引用，且 `W-G.9-111_CC交接文.md:91` 逐字載其 payload「⛔ 仍未入倉」⇒ **意指已被 `W-G.9-109` 之停機 payload 占用**。

---

## §零　取號（三類·各四事齊）

| 類 | 所用之框 | 既有最大號 | 新號之命中 | 對照組 |
|---|---|---|---|---|
| **常規** | 粗體列 `^\*\*常規X（`（命中 `5` 列：`:500` 一／`:504` 二／`:507` 三／`:607` 四／`:616` 五）；併呈標題式 `^#+.*常規` ＝ `4` | **五** | `常規六`／`常規七` ⇒ **`0`** | `常規五` ＝ `3` 檔 |
| **自誤** | 四框聯集（表列反引號 `15`/max `184`；表列裸 `3`/`3`；`^#{2,6}.*?自誤 N` `170`/**`188`**；`^###\s*自誤 N` `54`/**`188`**）⇒ UNION 相異 **`184`** | **`188`** | `自誤 189` ⇒ **`0`** | `自誤 188` ＝ `2` 檔 |
| **`VR`** | 多形（定義列 `^## \`VR-NNN\`` 相異 `59`/max `72`；裸 `VR-(\d+)` 相異 `72`/max `72`；表列式 `0`）⇒ UNION 相異 **`72`** | **`72`** | 見下 | `VR-072` ＝ `3` 檔 |

⇒ **`A-2` 符**（常規 max ＝ 五）／**`A-3` 符**（自誤 max ＝ `188`）。

### 🔴 `VR` 之取號：**⛔ 未取 `VR-073`·改取 `VR-074`**

| 受詞 | 框 | 命中 |
|---|---|---|
| `VR-073` **實體** | `^## \`VR-073\`` | **`0`** |
| `VR-073` **全倉** | 裸 `VR-073` | 🔴 **`5`** |
| `VR-074` 全倉 | 裸 `VR-074` | **`0`** |
| **對照組** | `VR-072` | `3` 檔 |

**`5` 處之落點逐一具名**：
- `docs/reports/W-G.9-111_CC交接文.md:84`／`:91`／`:109`／`:123`
- `docs/reports/W-G.9波_claude.ai側自誤登記.md:3274`

🔒 **決定性逐字**（`W-G.9-111_CC交接文.md:91`）：「🛑 **然其 payload（`GB-61` 就地加註／`VR-073`／自誤 `137`）⛔ 仍未入倉。**」
同檔 `:123` 之表列：「`GB-96`／`K-9-18`／`VR-073` ｜ 全倉 `0` ｜ — ｜ ⛔ 未鑄（在停機之 payload 內）」
⇒ 🔒 **`VR-073` 之<u>意指</u>已被 `W-G.9-109` 之停機 payload 占用** ⇒ 依 `CLAUDE.md`「🔒 編號衝突一律以倉內為準……**新則取下一個未占用號**」，取 **`VR-074`**。
⚠️ **依單 `§零` 之字面**（「任一新號**實體**命中 `≠ 0` ⇒ 停機」），`VR-073` 之**實體**命中為 `0` ⇒ **⛔ 不觸停機**；本處置係**取保守項**（常規二）並逐字回報，⛔ 非停機。
🩸 **同形之第三度**：`自誤 183`／`184`（`W-G.9-148`）與本次 `VR-073` **皆為「交接文所述之號 vs 倉內實體」之衝突**。

---

## §一　`CLAUDE.md` 末端追加（`19/0`）

**常規六**（倉為公開·後續窗自行現查）／**常規七**（逐字交付塊之產生方式）——逐字依單、⛔ 未改一字，另於節首加**編號現查**一行（框／命中／對照組）。

---

## §二　`K-9-19` 之就地加註（`K-6` 末端·併於 `46/0` 內）

🔒 **`A-4` 符**——`一`（`K-6:3101-3106`）之三錨 **CC 逐項現查·各命中 `1`**：

| 錨 | 命中 |
|---|---|
| `app.py:19367-19401` | **`1`** |
| `app.py:6229` | **`1`** |
| `自誤 149` | **`1`** |

⇒ 已具名 `一`／`三` 為「**claude.ai 之整理**」，⛔ 非 KL 之語。
⚠️ 加註內併記 `Z-2` 之反例，並明載**本具名⛔ 不依賴「非 bq ⇒ 非 KL 逐字」之推論**（該推論已由 `W-G.9-150′R` 證偽），而依上開三錨。

---

## §三　🔴 `A-5`　末端塊正典依據之逐字對拍 ⇒ **不符**（已依常規七處置）

### 單 `§四` 所載（逐字）

> 本裁定**未授權**下列六項。⛔ 於 D 波（D-0～D-3）內一律不得觸及；**欲動須另案取得 KL 裁定**。

> **末端塊／`N0-20`**：其範圍定義（`R_end = (p1 之外未臨正街土地) ∪ (p1→pz 帶)`）、勝者規則、`strip_area(S) = G − wedge_area` 常數項 —— **一律不動**（KL 2026-08-07 明示「末端塊仍沿用之前裁示的作法」）。

### 倉內真值（`sed -n '1021,1026p'`·CC 現查）

```
本裁定（`D-①` F 槽修正 ＋ `D-②` `K-9-5-4`）**未授權**下列六項。
⛔ 於 D 波（D-0～D-3）內一律不得觸及；欲動須另案取得 KL 裁定。

1. **末端塊／`N0-20`**：其範圍定義（`R_end = (p1 之外未臨正街土地) ∪ (p1→pz 帶)`）、
   勝者規則、`strip_area(S) = G − wedge_area` 常數項 —— **一律不動**
   （KL 2026-08-07 明示「末端塊仍沿用之前裁示的作法」）。
```

### 逐項之差

| # | 差 | 判 |
|---|---|---|
| `1` | 🔴 單**省略**「（`D-①` F 槽修正 ＋ `D-②` `K-9-5-4`）」 | **實質**——該括號**限定了「本裁定」之所指**；省略後讀者無從知其射程 |
| `2` | 單**省略**列首之 `1. ` 序號 | 形式 |
| `3` | 單將**三行合併為一行**（去換行與續行縮排） | 形式（惟違「逐位」） |
| `4` | 單於「欲動須另案取得 KL 裁定」加**粗體**，倉內**無**粗體 | 形式（惟係**加重語氣**之改寫） |

⇒ 🔒 **`A-5` 不符**。**處置（依常規七·本單所立）**：⛔ **未依單文字入倉**，改以 **`sed` 自倉切片**產生塊內逐字後入 `K-6`；並以機檢自證「塊內文字 ＝ 重抽之逐位」（`K-6:1021-1026` 與 `K-6:1889-1894` 二塊皆 **`True`**）。

🔒 **`K-6:1891` 之 KL 逐字對拍 ＝ 相符**（「現在末端塊土地因為深度呈現線性遞增或遞減」·命中 `1`）。
⚠️ 單載其上下文為「`K-6:1893-1894`」，現查該二行確為「⇒ 該句為【裁】4 之**理由**……**同一機制**。⛔ 前次轉錄漏列，非 KL 未說。」⇒ **相符**。

### 🛑 本節之實質結論（已入 `K-6`）

🔴 **末端塊之「二側界不平行」在正典中<u>已有 KL 逐字</u>**（`K-6:1891`），其處置（`R_end` 之範圍定義、勝者規則、`strip_area(S) = G − wedge_area` 常數項）**已由 KL 於 `2026-08-07` 明示「末端塊仍沿用之前裁示的作法」並列為「一律不動」**。
🛑 ⇒ **KL 於 `2026-08-27` 交辦之藍影式末端塊門檻，係<u>變更既有裁定</u>之題**，⛔ 非補一個【查無】之款；其上呈之正當性源自 `K-6:1022` 之「欲動須另案取得 KL 裁定」。
🔒 併記碼面現行實作與 KL 交辦之**二處相異**（關係詞 `>=` vs `>`；受詞 `area(R_end)` vs 藍影面積·⛔ 非同量）——**⛔ 本節不裁**。

---

## §四　`VR-074` ＋ 自誤 `189`

- **`VR-074`** 入 `docs/驗證裁定登記表.md`（`23/0`）——逐字依單，另加**編號現查**段（含 `VR-073` 未取之由）與 **CC 之併記**（具名本 `VR` 之據二**⛔ 不依賴**已被證偽之「非 bq ⇒ 非 KL 逐字」推論，而依 `一` 之三錨 ⇒ **該三錨即本 `VR` 之可證偽面**）。
- **自誤 `189`** 入 `docs/reports/W-G.9波_claude.ai側自誤登記.md`（`13/0`）——逐字依單、⛔ 未改一字。

---

## §五　期初／期末三數

| 檔 | 期初 列／位元組／`sha16` | 期末 列／位元組／`sha16` | 列增 |
|---|---|---|---|
| `CLAUDE.md` | `636`／`56963`／`2fc88bf960ccbbd3` | `655`／`59736`／`1f636de9c1504990` | **`19`** |
| `docs/rulings/K-6_…md` | `3456`／`244441`／`1a4fe95cc97ec223` | `3502`／`248513`／`82c80f35a8a7e8e0` | **`46`** |
| `docs/驗證裁定登記表.md` | `3792`／`288362`／`528f95e56a0e8456` | `3815`／`291352`／`4922cd5ac68d05ab` | **`23`** |
| `docs/reports/W-G.9波_claude.ai側自誤登記.md` | `3554`／`300997`／`4f05b7deb790741f` | `3567`／`302547`／`c31fab7385393fbf` | **`13`** |

---

## §六　`§六` 六項預測之逐項對拍（⛔ 未改預測）

| # | 預測 | 實得 | 判 |
|---|---|---|---|
| `A-1` | `152` 實體命中 `0`；對照組 `150` ≥ `1` | 五形皆 `0`；`7` 檔 | ✅ **符** |
| `A-2` | 常規既有最大 ＝ **五** | 粗體列框命中 `5` 列·max ＝ 五 | ✅ **符** |
| `A-3` | 自誤既有最大 ＝ **`188`** | 四框聯集 MAX ＝ `188` | ✅ **符** |
| `A-4` | `一` 含 `app.py:19367-19401`／`app.py:6229`／`自誤 149` **三錨俱在** | 各命中 `1` | ✅ **符** |
| `A-5` | `K-6:1024-1026` 之逐字與單 `§四` 所引**逐位相同** | 🔴 **不符**——單**省略** `（D-① F 槽修正 ＋ D-② K-9-5-4）`（**實質**）＋ 三處形式差 | ❌ **不符** |
| `A-6` | 四受檔 `deletions` 逐列 ＝ `0` | `19/0`／`46/0`／`23/0`／`13/0` | ✅ **符** |

---

## §七　收工閘

| # | 實得 |
|---|---|
| `1` | `HEAD`（期末）見 commit；`app.py` blob ＝ **`b4cc30835feaad03dfc37f394a669bbb45988b8b`**（⛔ 零觸·絕對值出艙） |
| `2` | `git diff --numstat 75ab7238` ＝ 恰 `4` 列·**刪除欄逐列 `0`**；`wg942_append_audit.py` `rc=0`（逐字結論列見 commit message） |
| `3` | `§零` 三類取號各四事齊 |
| `4` | 🔴 **依常規七**：`§二`／`§四` 所引之倉內逐字已以 `sed` 重抽並對拍——`K-6:3101-3106` 三錨各 `1`；`K-6:1891` 相符；**`K-6:1021-1026` 不符**（見 `§三`）⇒ 已改以重抽之逐位入倉、⛔ 未依單文字 |
| `5` | `§六` 六項預測逐項標明 符／不符 |
| `6` | 量測時點 ＝ `2026-08-27`·態 ＝ `blob@75ab7238` |
| `7` | 報告命名 ＝ `W-G.9-152R` |

🛑 **回報即停**——末端塊之 (甲)/(乙) **須上呈 KL**（其正當性錨已於本批立於 `K-6:1022`）；`wedge_area` 之受詞查明後另單。CC ⛔ 不自裁。
