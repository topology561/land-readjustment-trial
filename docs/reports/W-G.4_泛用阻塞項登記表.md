# W-G.4 — **已掛名之泛用阻塞項 登記表**

> 立於 KL 交辦（2026-07-25·丁）。**與普通 backlog 分離**：本表所列係
> **「本案（UC9898）不觸發、但換一個案子會硬停或算錯」之已知結構性阻塞**，
> 每項須有**倉內出處**與**觸發條件**；解除多半需**泛化波**，部分尚須 **KL 域裁**。
>
> 判準（入表 vs 入 backlog）：
> - **入本表**＝換案即**硬停／錯值**（非「可以再優化」）。
> - **入 backlog**＝品質改善、容差調校、報告措辭等。
>
> ⚠️ 本表**不得**寫成「已驗過」——凡未被真資料走過之分支，一律於「真資料覆蓋」欄標明。

> 🔢 **編號沿革註記（K-8 段四·2026-08-01 補）**：舊 **GB-7／GB-8** 曾用於 M-5 相關項，
> 並已於 **K-6-A1 結案消滅**（`docs/reports/W-G.5_K6K8_新session交接文.md:133`、
> `docs/reports/W-G.5_K6-A1_封存與清單報告.md:204`）。
> 現行 **GB-7／GB-7b／GB-8／GB-9** 係 **2026-08-01 重新啟用之新項、與舊同名項無關**。
> **兩份歷史報告凍結不動**（不回改其舊編號，以免破壞當時之交叉指認）。
> ⇒ 引用 GB-7／GB-8 時**須連日期一起引**，否則指涉不明。

> 🔢 **排列與完整性（K-6-A2 段三(c)-3 補·2026-08-04）**：
> 本表**新項採前插**（`GB-1…GB-21` 為升序，其後 `GB-28→GB-22` 為**降序**）
> ——⚠️ **排序無語意**，勿據以推論先後或關聯。
> **`GB-12` 為缺號**（沿革未載·本批未查得其是否曾用於他項）。
> 🔴 **⛔ 禁以「列數」作為驗收條款**——列數會**巧合通過**（本表列數曾正確而順序與缺號皆異常）。
> 應以**內容檢查**代之（重複 ID／缺號／最大號），例：
> ```
> ids = re.findall(r'^\| \*\*(GB-\d+b?)\*\*', open(表).read(), re.M)
> ⇒ 檢查 重複、缺號、最大號；⛔ 不檢查 len(ids)
> ```

---

## 一、登記

| # | 阻塞項 | 觸發條件（換案） | 現況處置 | 倉內出處 | 真資料覆蓋 | 需 KL 域裁？ |
|---|---|---|---|---|---|---|
| **GB-1** 🆕 | **E3「往後找」分支必觸楔形相鄰守衛** ——末端 gate 開時以「G ≥ area(R_end)」往後找標的，但 `_reshape_block` 要求標的與楔形相鄰；跨過被跳過之宗後距離必 ≥0.05 ⇒ raise「楔形與標的不相鄰」 | 末端 gate 開（無 SIDELINE ∧ 未臨正街半平面>ε）**∧** 首個合格宗 `G < area(R_end)` | **硬停**（loud raise） | 實測：`verify/fixture_end_winner.py` 驗2 之 A 欄（`M_0725_F2_end_winner.log`）——**左右兩側皆然**；碼：`grep -n "楔形與標的" verify/wf_f4.py`／`grep -n "往後找" verify/wf_f4.py` | ❌ 本案未觸及（F-5 證 fallback latent、R1/R6 皆有 winner 且 G≫area(R_end)） | ✅ **可能需**——「往後找之標的**是否必須**與楔形相鄰」係分配原則問題（位相不變 vs 末端保留孰先），非純技術。**本批未問** |
| **GB-2** | **M-5 階段② 未實作**（餘額他併至 MinA） | ①補足後源仍有餘 **∧** 同 gid 尚有其他「較大但未達 MinA」之宗 | **loud 停機**（禁靜默轉③） | `grep -n "②" verify/m_rescue.py`；`W-G.4_裁定M_CC交接文.md` §六 | ❌ 本案母體為空 | — |
| **GB-3** | **`wf_f1` 右支架未建**（該檔硬釘 `lbl="R1"`＋結構左向） | R1 右側成為 forced 端 | **loud raise**（碼內留 KL 裁語 TODO） | `W-G.4_S1_band_N0-20_recon＋side參數化approach.md` §18① | ❌ R1 兩情境皆非 forced | — |
| **GB-4** | **全倉無「部分面積拆分併入」機制**（裁定A 之拆分僅列 plan、未實作） | 需以**部分面積**併入（`wf_f0._transform` 為整筆、`wf_f2` 為全搬單一 `tgt_blk`） | **缺口**（無守衛） | `W-G.4_裁定M_盤點表.md` §「部分拆分」列 | ❌ | 待泛化波盤 |
| **GB-5** | **引擎 UC9898 硬錨**（`GSA_EXPECT`／`E0_EXPECT`／`E2_NAMED`／`COMP_EXPECT`／`F1_TARGET`／`F1_REVERIFY` 等＋67 raise 斷言） | 任何非 UC9898 案 | `_is_uc9898` 圍欄·非本案不硬跑 | `CLAUDE.md` §7 引擎接線鐵律；`W-G_收官總報告.md` §二 | — | — |
| **GB-6** | **`wf_f0.ROUTE_OUT` 決策常數未刪**（驅動控制流·且 E-1 導出式吃其產出 ⇒ 循環相依） | 換案（去錨） | 本波不刪·`F2_GROUPS` 已改導出式 | `docs/specs/W-G.4_裁定M_plan_v4.md` §六 E-2／§九 | — | — |
| **GB-7** 🆕 | **per-block MinA 之 round 位置與正典不符**——**三份實作**（`wf_f0._mina_by_block`／`wd4_tier_list._mina_by_block`／**`app.py` 之 `_min_alloc_area_by_blk`**·見 GB-8）皆實作 `round(D×w, 2)`（先乘後捨），而正典 **K-8 §二-1** 明訂 `round(D,2)×w` 且「**`round()` 只在輸出層**」⇒ **輸出層以外多捨一次** | 任何使 `min(MinA_i)` **落在乘積非 2dp 之街廓**的案子（＝`round(D,2)` 之第二位小數為奇數 × `min_width=3.5` 時必生第三位）。本案 min 落在 **R4**（`33.10×3.5=115.85` 恰 2dp）故兩形同值 | **不改**（本波只登記）。今日 0.005㎡ 之差落在下游 `−0.01`／`−0.5` 容差內、**零翻盤** | 實測（w=3.5·現行注入深度）：R1 `116.025`/`116.02`、R2 `155.645`/`155.64`、R5 `159.985`/`159.99`、R6 `159.285`/`159.28` **四塊真分歧各 0.005㎡**；R3/R4 等值。碼：`grep -rn "_mina_by_block" --include="*.py" .`；正典：`grep -n "K-8 §二-1" docs/rulings/K-6_街角地分配程序與可分配判準.md` | ⚠️ **本案零後果係「min 恰落在 R4」之巧合**、非結構保證 ⇒ 視同**未被真資料走過** | ❌ 不需——正典已明訂（K-8 §二-1），屬**碼面未依裁定**之技術修正。**K-6-A2 拆除 per-block MinA 達標角色時一併收** |
| **GB-7b** 🆕 | **GB-7 之第二觸發條件：生產側之「等值前提」根本不成立**——GB-7 今日零後果之前提為「深度已 2dp 且乘積恰 2dp」，但**生產側深度可非 2dp**：逐街廓覆寫格 `f3L_depth_ov_*`（`grep -n "深度覆寫（m）" app.py`）為 `st.number_input`，**未限小數位、未 `format=`、取值後亦未 `round`**（`grep -n "_depth_use_by_blk\[_lbl\] = float(_ov)" app.py`）⇒ KL 手填任意小數即繞過 2dp 鏈 | **KL 於畫面填入非 2dp 覆寫值**。🔴 **此非邊緣個案、而是結構性**：`round(D,2)×3.5` 與 `round(D×3.5,2)` 之差，**Δ 之量級為 0.005～0.02**，落在容差內與否**取決於 D 之第三位小數**，**無任何機制保證其落在容差內**。實測三例：`33.157` ⇒ `116.06`/`116.05`（Δ=−0.01）、`44.473` ⇒ `155.645`/`155.66`（Δ=+0.015）、**`45.715` ⇒ `160.02`/`160.00`（Δ=−0.02·實測最劣）**。**全域掃描**（D 自 30.000 至 49.999·步長 0.001·w=3.5·20,000 點）之 abs(Δ) 分布：`0.000`×3,000（15%）／`0.005`×5,000／`0.010`×6,000／`0.015`×4,960／`0.020`×1,040 ⇒ **85% 之 D 產生非零 Δ、60% 之 D 使 abs(Δ) ≥ 0.01（即達或逾下游容差）**。⚠️ 與 **GB-9 同結構**：生產側有 **UI 輸入**繞過 harness 所賴之前提（此處為深度覆寫格、GB-9 為正面路寬欄）——**波末批同批處理** | **只登記·不動 widget、不動算式**（動之即非零行為，屬**波末批**） | widget：`grep -n "深度覆寫（m）" app.py`；取值：`grep -n "float(_ov) if _ov > 0" app.py`；容差錨：`grep -n "mina\[blk\] - 0.01" verify/wf_f4.py`（5 命中） | ❌ **未被真資料走過**（今日六街廓皆用自動量測值·`_dinfo['D_avg']` 已 2dp） | ❌ 不需——正典已明訂 round 位置（K-8 §二-1） |
| **GB-8** 🆕 | **per-block MinA 有【三份】實作、無一致性看守**——① `wf_f0._mina_by_block`（3 參數→`dict`·**引擎土地後果路徑**：`wf_f0`/`wf_f2`/`wf_f3`/`wf_f4` 與 harness 皆用之）② `wd4_tier_list._mina_by_block`（4 參數→`(dict, min)`·**清單／回歸路徑**）③ **`app.py` 之 `_min_alloc_area_by_blk`**（`grep -n "_min_alloc_area_by_blk\[" app.py`·**生產 UI 路徑**·寫入 `f3_min_alloc_area_by_label`）。三者皆 `round(D×w, 2)` 先乘後捨，**全倉無任何閘比對之** | 任一份被改（去錨／K-6-A2 拆除達標角色／換 round 位置）而其餘未同步 ⇒ **靜默分歧**。🔴 **第三份在生產 UI 側**——與引擎側分歧時**不只影響清單與回歸斷言，是 KL 螢幕上看到的數字**（`f3_min_alloc_area_by_label` 供畫面顯示與下游消費） | **不改**（本波只登記）。三份簽章／宿主皆不同**不可互代**，非「刪兩份」即可了事 | ①②`grep -rn "_mina_by_block" --include="*.py" .`；③`grep -n "_min_alloc_area_by_blk\[" app.py`＋`grep -rn "f3_min_alloc_area_by_label" --include="*.py" .`；一致性看守之不存在：前者 grep 之輸出再接 `grep -i`（BRE 交替 pattern：`assert`／`==`／`一致`／`同源`）⇒ **僅命中註解、無斷言** | ❌ **從未被看守**（今日相同係三者碼面巧合一致，非機器保證） | ❌ 不需——屬 #20「避第三份抄寫」之未竟部分（**第三份即在 app**）。**K-6-A2 拆除 per-block MinA 達標角色時一併收** |
| **GB-9** 🆕 | **`f3_pk_legal_min_width` 之「單一真相源·app==engine」宣稱逾實證**——註解見 `grep -n "單一真相源·app==engine" app.py`。**前半屬實**（兩路皆走 `get_min_lot_size` 查表、已廢 v12 內硬編 3.5）；**後半逾實證**：兩寫入點之**輸入取自不同來源**——engine `verify/selection_pipeline.py` 之 `正面路寬(m)` 溯源至**快照** `blocks[*].正面.路寬_m`（`grep -n 'fw = float(blk\["正面"\]' verify/run_verification.py`），app 溯源至 **live `sb['rows']`**（`grep -n "_sb_row = sb_rows_by_label.get" app.py` → `grep -n "sb_rows_by_label = " app.py`）；且**全倉無閘比對二者** | 兩來源脫鉤 ⇒ `min_width` 分歧 ⇒ **PK 前置篩選與街角規定範圍門檻同時偏移**。🔴 **脫鉤不必等「換案」**：app 側 `front_width` 之最終來源是 **UI 表單欄位**、**非量測值**——`fw = ca1.number_input("正面路寬 (m)", …, key=f"f3_fw_{bid}")`（`grep -n '"正面路寬 (m)"' app.py`）→ `'front_width': fw`（`grep -n "'front_width': fw" app.py`）→ `'正面路寬(m)': fw`（`grep -n "'正面路寬(m)':  fw" app.py`）→ `sb_rows_by_label` → `get_min_lot_size`。⇒ **KL 今天在畫面改一個正面路寬即刻脫鉤**，而快照側不動。⚠️ 與 **GB-7b 同結構**：生產側有 UI 輸入繞過 harness 所賴之前提（此處為正面路寬欄、GB-7b 為深度覆寫格）——**波末批同批處理** | **不改**（本波只登記）；註解已同批降級為「兩路同式、**惟輸入來源不同且無看守**」 | 註解：`grep -n "單一真相源·app==engine" app.py`；寫入點：`grep -n "f3_pk_legal_min_width'\] = " app.py verify/selection_pipeline.py` | ❌ **從未被看守**（今日相符係二來源同案） | ❌ 不需——與 GB-8 同形態（無看守之等值宣稱）。**K-6-A2／G.3 雙路同源批一併收** |

| **GB-10** 🆕 | **`parcel_min_width_n14` 之 E-2′ 宗地深度閘自 K-9-4 起成為【恆真閘】**——該閘為 `max(_tv) < _md ⇒ raise`（`grep -n "E-2′ 宗地深度 < 法定最小深度" app.py`）。**K-9-4**（每宗必須配到 BASELINE·未達即停機）＋**K-9-2**（街廓層剖面 ≥ 畸零地最小深度）上線後：前端已有臨接 FRONT 閘（`grep -n "_EPS_TOUCH_FRONT = " app.py`）、後端將有臨接 BASELINE 閘、街廓層已保證剖面 ≥ 最小深 ⇒ **「整宗都比最小深淺」幾何上不可能發生** | K-9-4 上線（＝ **K-6-A2**）。⚠️ **本項與本表多數條目相反：非「換案才觸發」，而是「本案上線後即失去鑑別力」** | **保留不刪**（防上游改動），但**須誠實標注「自 K-9-4 起為蘊含閘、不再具獨立鑑別力」** ⇒ 標注屬 **K-6-A2 同批** | 閘：`grep -n "E-2′ 宗地深度 < 法定最小深度" app.py`；裁定：`grep -n "^### K-9 §一、五則裁定" docs/rulings/K-6_街角地分配程序與可分配判準.md`；封閉性證明：同檔 `grep -n "^### K-9 §二、"` | ✅ **現況仍具鑑別力**（K-9-4 未上線）⇒ 標注時機為 K-6-A2、**非現在** | ❌ 不需——K-9 已裁定，屬碼面隨裁定調整 |
| **GB-11** 🆕 | **「K-4 面積門檻蘊含」字串族之語意已因 K-7 撤銷而錯，且係【兩個同名異體】**——① `app.WIDTH_VERDICT_CORNER_K4 = '合格（K-4 面積門檻蘊含）'`（`grep -n "WIDTH_VERDICT_CORNER_K4 = " app.py`）；② `verify/probes/probe_ruling_N_e1_touch.py` 之**硬編字面** `'街角·K-4 面積門檻蘊含（N-14 不適用）'`（`grep -n "街角·K-4 面積門檻蘊含" verify/probes/probe_ruling_N_e1_touch.py`）。K-7 撤銷後**不再有「蘊含」**（K-9-5 改實量）⇒ 二者措辭俱錯；且**常數名之 `_K4` 亦須正名** | 波末重烤批。🔴 **關鍵**：**只有 ② 綁 baseline**——`grep -rn "合格（K-4 面積門檻蘊含）" verify/baselines/` ⇒ **0 命中**；`grep -rln "寬度判定" verify/baselines/` ⇒ **0 命中**（**無任何 baseline 有該欄**）。② 落 `判定` 欄，`E系列實測快照_退縮0m.csv` **8 列**／`退縮3.5m.csv` **5 列**（`grep -c` 實測·**係 baseline 檔內容**，非現行實跑值——E 系列閘現為紅） ⇒ **只改 ① baseline 不會紅、而探針輸出與 app 語意靜默脫鉤；只改 ② 必翻 13 列。二者須同批改。** | **本批兩者皆不動**（紅線）·僅更正登記＋補過渡態註解 | 正典遷移項表（已同批更正誤載）：`grep -n "K-8 波末遷移項登記" docs/rulings/K-6_街角地分配程序與可分配判準.md`；探針比較點（讀常數·不受 ② 影響）：`grep -n 'ns\["WIDTH_VERDICT_CORNER_K4"\]' verify/probes/probe_ruling_N_e1_touch.py` | ⚠️ **原登記誤指 ① 綁 baseline**（正典該列與段四交接文皆然）⇒ 視同**未被查證過** | ❌ 不需——K-9 已裁定，屬措辭與命名之技術修正 |

| **GB-13** 🆕 | **`parcel_min_width_n14` 之量測帶深誤用「畸零地附表門檻」**——其 docstring 寫「深度至**法定最小深度**為止」（`grep -n "深度至\*\*法定最小深度\*\*為止" app.py`），而生產呼叫端餵入 `get_min_lot_size(...)['min_depth']` ＝ **附表值 `14.00`**（`grep -n "_min_depth_by_block\[_lbl_w\] = " app.py`；表：`grep -n "HUALIEN_MIN_LOT_TABLE = " app.py` 住宅區·路寬 8/12m →`(3.50, 14.00)`）。依 **K-9-6** 應為「**該宗自身之最小深度**」（本案 **33〜46m**）⇒ **把可建築門檻當成量測帶深** | **本案即觸發**（非換案才觸發）。量級：帶深 `14.00` vs 實測 `33〜46` ⇒ 量測帶**短了 2〜3 倍**，寬度取 min 之母體因而過小 ⇒ **系統性高估寬度**。實測（段五 probe·截角後·0m）：R1 右 `3.4643`→**`1.4165`**、R5 左 `3.6833`→**`0.6706`**、R6 右 `3.8197`→**`1.3999`** | ✅ **已落地（K-6-A2 段五(c)-2·KL 放行 2026-08-06）**——① 帶深來源已由**附表值**改為**逐宗實量**；② 其定義經 KL 圖示裁定更正為**後緣連續段最淺端點**（(c)-1）；③ **生產已切換**（(c)-2·`grep -n "_r_jw.update(evaluate_parcel_width_n14(" app.py` 已傳 `baseline_pts`）。⚠️ **E-2′ 閘未刪**、改為 `baseline_pts is None` 分支之守衛（夾具 ⑧b 仍走該分支）。土地後果：判定翻轉 **0**、`\|Δ寬度\| > 0.01m` 之列 **0**（`verify/out/probe_K9_6_parcel_band.log`【H】） | 定義：`grep -n "^### K-9-6 " docs/rulings/K-6_街角地分配程序與可分配判準.md`；實料：`verify/out/probe_ruling_K9_corner_width.log`【A】【D】段 | ❌ **從未被走過**——K-4 豁免使街角宗根本不走 N-14（`grep -n "_width_by_area_gate" app.py`）⇒ 該帶深之錯誤在街角宗上**從未浮現** | ❌ 不需——**法規原文已明訂**（畸零地使用規則 §4·見 K-9-6 逐字引文） |
| **GB-14** 🆕 | **`_build_corner_range_v3` §三 之帶深用 `round(D_avg, 2)`（街廓平均深度）＋斜率漏項**——① 帶深應為「**該街角規定範圍自身之最小深度**」且**隨 ALLOC_LINE 位置 `t` 變動**（K-9-6-b），現行取街廓平均深度之常數（`grep -n "block_depth" app.py`）；② 解析斜率取 `k = sigma / den_a`（＝ K-9-7 記號之 **`c`**），**正解為 `(c − k·m)`**（K-9-7 分支③）⇒ 「寬度往內變窄且深度往內遞減」時 **ALLOC_LINE 定錯位置** | **本案即觸發**。量級（依正典 K-8 §二 兩端垂距表·**勿自行重算**）：R1 差 `0.036m`／R4 差 `0.042m`（幾乎無差）、R2 `0.973m`／R3 `1.126m`、**R5 `4.394m`／R6 `4.296m`** ⇒ **斜街廓才是真正咬人處**。段五實測：分支 ③ 者 **2 個**（R2 左 `k=−0.0755·m=+0.0098`／R4 左 `k=−0.0476·m=+0.0003`）⇒ 該二角之 `(c−k·m)` 與 `c` 有實差 | ✅ **已結案（K-6-A2 段三(c)·2026-08-04）**——① 帶深改「範圍自身最小深度 `D(t)`」、② 斜率改 `(c − k·m)`，**二者同批兌現**（`grep -n "_k97 = k97_solve_alloc_t" app.py`·舊式已移除）。**對拍靶＝段三(b′) 探針「新」欄十六格**（`verify/out/K6A2_seg3c_16cell.log`·逐格相符）。土地後果：8 格位移、最大 R5 左 −7.27㎡；**PK 零翻盤** | 規格：`grep -n "^### K-9-7 " docs/rulings/K-6_街角地分配程序與可分配判準.md`；碼：`grep -n "k = sigma / den_a" app.py` | ✅ **已有看守**——自檢①①′ 之帶深已改用 `D(t*)`（與 §四 定位同基準），前提錯之情形消滅；另有十六格對拍靶 | ❌ 不需——法規原文已明訂 |
| **GB-15** 🔄**已於段六第二批大幅更正** | **街角規定範圍之「前緣側邊界」實為街廓多邊形邊界、非 FRONT_LINE**——正典 K-8 §五 稱範圍由「**前緣線**…所圍」，惟實作取「**街廓多邊形 ∩ L(t*) 半平面**」（`grep -n "以 L(t\*) 切街廓" app.py`）。🔴 **原登記就 R1／R3 之歸因有誤，茲更正**：實測**全六街廓之圖面落差**（各街廓多邊形本身頂點對 FRONT_LINE 之最大垂距·工具【5b】）＝`R1 −0.000013`／`R2 0.000000`／`R3 0.000000`／**`R4 +0.014826`**／`R5 0.000000`／`R6 0.000000` ⇒ **只有 R4 曾有圖面落差；R1／R3 從來就沒有** | **本案即觸發**。原登記所引之「R1 左 `−0.166151m`／R3 右 `−0.059879m`」係**街角規定範圍多邊形自身於退縮 0m 之前緣側極值**——該範圍**整個落在截角區內**，極值遂落在**截角邊**上，**與圖面無關**。兩項實證：(i) 與「不扣截角」之值**逐格相同**；(ii) 退縮 **3.5m** 時回到 `−0.000013`／`−0.000001`。⇒ **R4（圖面）與 R1／R3（0m 範圍落在截角內）非同源、非同類問題** | R4 一項**已由 `data/V6_1.dxf` 修正**（`+0.014826` → **`+0.000006`**·全區 ≤ `7.6e-6m`）·**待波末批換圖生效**（GB-16）。R1／R3 **無圖面可修**；其為「0m 範圍落在截角內時臨街段如何定義」之問題 ⇒ 併入 **U-K9-4**（重述後） | 工具：`verify/tools/verify_dxf_v6_1.py`【5b】；實料：`verify/out/verify_dxf_v6_1.log`、`verify/out/probe_ruling_K9_corner_width{,_V6_1}.log`；探針【前緣偏移】表已同批改寫抬頭以防再誤讀 | ❌ **【5b】為段六第二批新增之量**——此前從未逐街廓量過「圖面落差」，故原歸因未經查證 | ⚠️ **可能需**——「0m 情境下街角範圍整個落在截角區內時，其臨街段／最小深度如何定義」屬構造定義問題（U-K9-4） |

| **GB-16** 🆕 | **換圖＝重烤觸發**——`data/V6_1.dxf`（KL 修圖 2026-08-02）之 `FRONT_LINE` **長度幾乎不變（`+0.0516mm`）但方位角改變 `32.61 角秒`**（`1.5809e-4 rad`·**本批自算**）。`FRONT_LINE` 係一切寬深量測之**座標軸**（`s` 沿前緣線／`t` 為其法向）⇒ 方位角一動，**全部幾何衍生數字隨之毫米級變動** | **換圖之時**。量級（自算）：於深度 `14m` 處橫移 `2.21mm`／`33m` 處 `5.22mm`／**`45m` 處 `7.11mm`**。第二條 `FRONT_LINE` 另變 `−0.63 角秒`（`45m` 處 `0.14mm`） | **本批不換圖**（`data/V6_1.dxf` 僅入版控·生產路徑仍讀 `V6.dxf`）。🔴 **處置：換圖與換快照（`verify/case_params_UC9898.json`）併入波末批同一次重烤，禁分兩次烤**——分兩次烤會使中間態之 baseline 既非舊圖亦非新圖、無可溯源基準 | 驗收：`verify/baselines/PROVENANCE.md` 附錄 A（§A.5）；實料：`verify/out/verify_dxf_v6_1.log`；工具：`verify/tools/verify_dxf_v6_1.py`。**交叉指認**：正典之「K-8 波末遷移項登記」節（`grep -n "K-8 波末遷移項登記" docs/rulings/K-6_街角地分配程序與可分配判準.md`）＋ U-K8-1（＝乙·快照留到 K-8 全案完成後一次換） | ❌ **尚未走過**——`V6_1.dxf` **未進生產路徑**，全部凍存 baseline 仍以 `V6.dxf` 產出 | ❌ 不需——**技術事實**（方位角變動之量級可自算複驗） |
| **GB-17** 🆕 | **`.gitattributes` 缺失 ⇒ 文字型二進位資產入庫遭行尾正規化 ⇒ 來源檔校驗碼 ≠ 入庫產物校驗碼**——本倉 `core.autocrlf=true` 且原無 `.gitattributes`，git 以**啟發法**判定 `.dxf` 為文字檔。實測 `data/V6_1.dxf`：來源 88,405 bytes（CR 12,324）→ **入庫 blob 76,081 bytes（CR 0）**、md5 `dfaf3daf…` → `7f56ef4e…`。⚠️ 同倉 `data/V6.dxf` **未**被正規化（blob 仍含 CR 12,308）⇒ **兩檔待遇不一致**——**啟發法非契約**，此不一致本身即隱患 | 任何**文字型二進位資產**入庫（DXF／DWG／SVG／CSV／JSON），且其校驗碼被記載於 PROVENANCE 或用於對拍。🔴 **本案已實際發生**：段六首版 `28cbf51` 之 PROVENANCE md5 與入庫 blob 不符 | **已修**（段六第二批）：新增 `.gitattributes`＝`*.dxf -text`（**選 `-text` 非 `binary`**：前者僅關行尾正規化·恰好對症；後者＝`-diff -merge -text`，另關文字 diff 與 merge，係逾越所需之副作用）。PROVENANCE 之 md5 表**改對 blob 計算**並附計算指令 | `.gitattributes`；`verify/baselines/PROVENANCE.md` §A.1／§A.1-1；覆驗指令：`git cat-file -p HEAD:<path>` 之輸出再接 `md5sum` | ❌ **其餘資產尚未掃描**——`verify/baselines/*.csv`（20 檔）／`*.json`／`docs/**/*.svg` 是否亦受影響、其記載之校驗碼是否失準，**本批未查** | ❌ 不需——技術事實。**泛化波須全倉掃描** |
| **GB-18** 🆕 | **`parcel_min_width_n14` 之量測多邊形為 `cut_coords`（＝截角後之實際宗地），與 K-9-8 之虛擬還原塊不符**——K-9-8 明訂量測用多邊形係「延伸線段＋該宗二側境界線＋BASELINE 所圍」之**虛擬還原塊**，其在街角處**伸出街廓之外**（含被截角切掉之部分）；現行碼逕以 `cut_coords` 量 ⇒ **街角宗與其鄰宗之寬度量得偏小** | **本案即觸發**。段七實測（截角後·0m·二代 → 三代）：**R1 右 `0.7855` → `4.1725`**／**R5 左 `0.6706` → `3.8531`**／**R6 右 `1.3999` → `4.8108`**／R4 右 `9.8102` → `13.1894` ⇒ 差距達**數倍** | 🔴 **落地時點 ＝ 段六（與 K-9-5 同批）**（K-6-A2 段四(c)-1 更正·2026-08-04）——原標「K-6-A2 修」過寬。**理由係結構事實、非排程偏好**：① `parcel_min_width_n14` **全倉僅二處呼叫**（`grep -rn "parcel_min_width_n14(" --include="*.py" .`）：`app.py:6750`（**非街角宗**·生產）與 `app.py:9704`（k98 builder·**僅探針消費**）；② 街角宗於 `app.py:6744` 之 `'實際寬度(m)': None` **恆為 None**（K-4 提前 return·註明「非『未決』」）；③ k98 之 `min_width` **唯一消費端**為 `verify/probes/probe_k98_virtual_block.py:232`；④ `_build_corner_range_v3` 之 `min_width` 係**法定門檻查表值**（`app.py:9995` 逐字「退縮寬／畸零地最小寬（`get_min_lot_size` 查表·禁自建表）」）、**非量測值**。⇒ **GB-18 現無生產消費端**；該消費端由 **K-9-5 撤銷 K-4 豁免時才建立**（`evaluate_parcel_width_n14` docstring 逐字：「**K-9-5 已撤銷該豁免**…街角第 1 宗改**寬度實量**（截角前）…**本函式尚未改**（實作屬 K-6-A2）⇒ 上段所述之四態現況為**過渡態**」）。⛔ **段四不得改 `app.py:6750`**——它服務**非街角宗**，與 **GB-33** 直接衝突（改之即動全案 N-14）。<br>🔒 **本項維持獨立·⛔ 不得併吞入 GB-13／GB-33**（K-6-A2 段五(c)-1 覆核）：前一輪曾主張「量測對象應改為帶 ∩ 二側無限直線」並據以併吞本項；**KL 2026-08-05 圖示裁定量測對象不改** ⇒ 該併吞主張**隨之作廢**。本項之症狀（量測**多邊形**應為虛擬還原塊、而非截角後之 `cut_coords`）與帶底、帶軸**皆為不同軸之問題**，落地時點仍為**段六**。 | 裁定：`grep -n "^### K-9-8 " docs/rulings/K-6_街角地分配程序與可分配判準.md`；碼：`grep -n "def parcel_min_width_n14" app.py`；實料：`verify/out/probe_ruling_K9_corner_width_V6_1.log`【K-9-8-D】。<br>🔴 **「死路徑」之限定詞**：GB-18 後果為「街角宗**與其鄰宗**」，而「真資料覆蓋 ❌ 從未被走過」**只對街角宗成立**——**鄰宗（非街角）每次 `run_all` 都在走** `app.py:6750`（實測：0m 68 筆中 61 筆、3.5m 70 筆中 65 筆走到該行）⇒ 該缺陷**有一半是活的**。詳見 `docs/reports/W-G.5_K6-A2-段四c1_停機_GB18前提倒置.md`。 | ❌ **從未被走過**——K-4 豁免使街角宗根本不走 N-14 | ❌ 不需——K-9-8 已裁定 |
| **GB-19** 🆕 | **`_build_corner_range_v3` §三 之量測起算線為 FRONT_LINE，未實作 K-9-8 之「通過 `P` 之平行線」**——K-9-8 (3)(5) 明訂起算線係**自 `P` 作平行 FRONT_LINE 之延伸線段**、深度自該線段起算；現行 §三 之帶一律自 FRONT_LINE 平移 ⇒ **街角範圍之 ALLOC_LINE 定位偏差** | **`P` 落在截角邊時**觸發（`P` 落在 FRONT 重疊段時 K-9-8 (6) 退化、與現行同）。🔬 段七實測（**以 `V6_1` 跑**）12 格全部落在退化支 ⇒ 於 `V6_1` 零偏差。<br>🔴 **更正（K-6-A2 段四 §一·2026-08-04）：該「零偏差」僅對 `V6_1` 成立，對【現行生產圖 `V6.dxf`】不成立**——於 `V6` 為 **10 格 FRONT 重疊段／2 格截角邊**（`R4/right` 之 `0m` 與 `3.5m`·`t_P=+0.013039m`·`verify/out/probe_ruling_K9_corner_width.log:72`／`:78`；該表表頭同檔 `:65` 逐字載明 `P ＝ L_in ∩ BLOCK 邊界` ⇒ 確為 K-9-8 (2) 之 `P_block`）。⇒ **兩處敘述之差別在【量測所用 DXF】**、非其一有誤；二方實料引用**皆保留**。<br>⇒ **(a) 段四 ⛔ 不得以「本案零實例」為由略過截角邊支**（現行生產圖上它是**活的**）；**(b) 波末換 `V6_1`（GB-16）後該支將失去唯一真資料覆蓋** ⇒ 合成案為其日後**唯一覆蓋**。⚠️ 該實例出現於 `R4`、與 GB-15 之 `+0.014826` **同階**，**⛔ 不主張因果**——同階只是同階，**成因待段四查** | ✅ **已落地**（K-6-A2 段四(c)·2026-08-05·**生產已切換為乙式**）。獨立驗收：`verify/fixture_corner_range_k8.py`（**寫在前批·本批一字未動**）**FAIL → PASS**（16 格 T1 殘差 `~1e-10`）。以下為 (a) 之只算不換實測——第二層求解器已入 `k97_solve_alloc_t`（**opt-in `block_vertices`**·**生產零呼叫點**），16 格實算：`t*` 位移 8 格、最大 **0.0729 cm**、最大 Δ面積 **+0.024157㎡**、**PK 十六格零翻盤**；🛑 **待 KL 放行方切換**。併看 🆕 **GB-36**（帶之**上緣**該取 FRONT 抑或 `PQ`·字面 vs 推導） | 裁定：同上 K-9-8；碼：`grep -n "def _build_corner_range_v3" app.py`；實作：`grep -n "K-9-7-d \*\*第二層分支\*\*" app.py`；攤表：`docs/reports/W-G.5_K6-A2-段四c2a_實作與攤表.md` §二。**與 GB-14（帶深）互相指認為同源**——同屬 **§三 量測帶之定義問題**（GB-14＝帶「多深」、GB-19＝帶「從哪裡起算」） | ❌ **未被走過**（本案全走退化支） | ❌ 不需——K-9-8 已裁定 |

| **GB-20** 🆕 | **`get_min_lot_size` 於【正面路寬 ≤ 0】時靜默取附表最窄列**——其查表迴圈為 `for upper, mw, md in table: if w <= upper: return`（`grep -n "def get_min_lot_size" app.py`），`w = 0.0` **恆滿足第一列**（住宅區 `upper=7.0`）⇒ 住宅區回 `(3.00, 12.00)`，即「**路寬未填**」被靜默當成「**路寬 ≤7m**」。**無任何 raise、無警示** | **正面路寬欄未填即觸發**（非換案才觸發）。⚠️ 該欄係 **UI 輸入**（`grep -n '"正面路寬 (m)"' app.py`）⇒ **KL 清空／未填該欄即刻落此支**。現行消費端**皆未防**：`grep -n "_mw_d = float(get_min_lot_size" app.py`（→ `f3_min_alloc_area_by_label`／`f3_min_width_by_label`）、`grep -n "_size_info = get_min_lot_size" app.py`（→ 街角參數表「法定最小寬/深」欄）⇒ **MinA 與法定最小寬同時靜默偏移**（住宅區：`3.50→3.00`、深 `14.00→12.00`） | **只登記·不改既有消費端**（改之即動 MinA 之生產路徑·屬波末批）。🔹 **惟本批新增之 `k92_block_depth_check` 已自行 loud raise**（`grep -n "正面路寬\*\*缺值" app.py`）——新碼**不承襲**該靜默 | 表：`grep -n "HUALIEN_MIN_LOT_TABLE = " app.py`；函式：`grep -n "def get_min_lot_size" app.py`；實測：`docs/reports/W-G.5_K6-A2-段一_K9-2街廓層深度警示.md` §四 E2c | ❌ **從未被走過**（本案六街廓路寬皆 8／12m 已填） | ❌ 不需——屬 no-silent-fallback 之技術修正（CLAUDE.md：「缺值應**警示或 loud raise**，不得靜默編造」） |
| | ↳ **與 GB-9／GB-13 之分野（勿混）**：**GB-9** ＝ app 與 engine **兩個來源**可能脫鉤（無看守）；**GB-13** ＝ 附表值被誤用為**量測帶深**；**GB-20** ＝ **單一來源之缺值**被靜默代換為最窄列。**三者互不涵蓋** | | | | | |
| | ↳ 🔒 **KL 已裁（2026-08-03·段二-0 一-6）**：**正面路寬未填 ⇒ 停機要求補填，不得降為警示**。土地後果＝`region_min` `115.85 → 99.30`、½ 門檻 `57.93 → 49.65` ⇒ **G 介於 49.65〜57.93㎡ 之地主在「現金補償」與「得申請增配」之間換邊**（權利歸屬之改變·非顯示瑕疵）。正典：`grep -n "^### 🔒 K-9-5-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`。⇒ **新碼分支（`k92_block_depth_check` 之 loud raise）已經 KL 裁定確認、不動**；**既有消費端仍待波末批** | | | | | |
| **GB-21** 🆕 | **`get_min_lot_size` 之區域變數 `non_buildable` 命名與內容相反**——該 tuple 裝的是**住宅區／商業區／甲乙丙丁種建築用地／風景區／工業區**，即**可建築**分區（`grep -n "non_buildable = " app.py`）。條件式 `if cat not in non_buildable and cat not in HUALIEN_MIN_LOT_TABLE: return 0/0/0`（`grep -n "if cat not in non_buildable" app.py`）**寫法正確、行為無誤**（雙重否定恰好抵銷），惟**名稱反轉** | **任何人照字面讀該變數**即誤解——讀成「非可建築清單」會得出「住宅區屬非可建築」之相反結論。⚠️ 此正是 **GB-13／K-9-6-a「同名不同量」誤讀**之溫床：本倉已有兩次因名實不符而誤用之前例（附表深度誤為帶深、`D_min` vs `D_k92_min`） | **只登記·不改**（本批不動）·**波末批改名** | 碼：`grep -n "non_buildable = " app.py`；條件：`grep -n "if cat not in non_buildable" app.py` | ⚠️ **行為從未出錯**（條件式正確）⇒ 本項係**可讀性／誤讀風險**，非算錯 | ❌ 不需——純命名之技術修正 |
| | ↳ **為何本批不順手改名**：本批（段二-0）之驗收格式為「**未動之既有 `.py` 逐檔 `A1=True`**」＋「`run_all` 名目集合逐字零進零出」。順手改名會使 `app.py` 之 diff 混入**與本批議題無關**之改動，破壞「一項一 commit、各自可獨立回退」（施工單 §二 紅線 1）⇒ **改名須自成一次可獨立回退之 commit**，列波末批 | | | | | |
| **GB-39** 🆕 | **K-9-5 撤銷面積豁免後，街角第 1 宗必須接到 `k98_virtual_measure_block`，⛔ 不得接到一般路徑**（與 **GB-18** 同批·**同解法·不同症狀**）。<br>**症狀**：一般路徑之**臨路停機閘**（`grep -n "本宗未臨接正面路街線" app.py`）以入參 `front_pt` 為參照，而生產傳的是 **FRONT_LINE 端點**（`grep -n -A1 "^    _w = parcel_min_width_n14" app.py`）；當街角第 1 宗之分配線落在**道路截角**上、該宗**完全不臨 FRONT_LINE** 時 ⇒ **假停機**。<br>🔒 **k98 路徑由構造正確**：其傳入之 `front_pt` 為 **`P_block`**（在 `PQ` 上）——`grep -n "front_pt ＝ .P_block." app.py` | **K-9-5 落地（段六）之當下即觸發**。**今日不可見之由**：街角宗因 **K-4 面積門檻豁免**於 `evaluate_parcel_width_n14` **提前 return**（`grep -n "街角地" app.py` 之該分支）⇒ **根本不進量測** ⇒ 該假停機無從顯現 | **本批只登記·不改**（段五(a) 為只算不換）。🔴 **段六與 K-9-5 同批處理**：街角第 1 宗之寬度改由 `k98_virtual_measure_block` 供給，⛔ 不得沿用一般路徑 | 正典：`grep -n "^### 🔒 K-9-5-2 " docs/rulings/K-6_街角地分配程序與可分配判準.md`；碼：上開三處 grep；GB-18（同批同解法）：`grep -n "^. \*\*GB-18\*\*" docs/reports/W-G.4_泛用阻塞項登記表.md` | ❌ **未被走過**——街角宗現不進量測（見觸發條件欄） | ❌ 不需——K-9-5／K-9-8 已裁定，本項為其落地之必然推論 |
| **GB-40** 🆕 | **k98 路徑曾走舊帶分支**——`app.py` 之 `k98_virtual_measure_block` 呼叫 `parcel_min_width_n14` 時**未傳 `baseline_pts`**，帶底沿用 `depth_from_PQ`（受詞 ＝ **臨街段**·失敗考古**節 42** 之殘留），與正典「通過後緣連續段之最淺端點、平行 `PQ` 之直線」（`grep -n "通過後緣連續段" docs/rulings/K-6_*.md`）不符。切換前全 `app.py` 之 `parcel_min_width_n14` 呼叫僅二處（生產已傳／k98 未傳）⇒ k98 曾為**最後一個舊帶消費者**。<br>🔴 **病態性（非僅數值差）**：舊帶底**越過該宗最淺後緣角點**、落入**末端薄片**（多邊形朝最深角點收斂為一點之區段）⇒ `min` 取在**崩解中的尾巴**。放大倍率 ＝ `w_old ÷ (max(_tv) − 舊帶底)`，實測 **`20.04`〜`2359.09`**；未越過之格則為主線段弦斜率 **`0.045`〜`0.076`**。⛔ 禁以任一比值當係數外推。 | **段六 K-9-5 撤銷面積豁免、街角第 1 宗接入量測之當下**（切換時 k98 生產呼叫點仍為 0 ⇒ **切換本身零生產行為變更**）。⚠️ **換案風險**：`max(_tv) − 舊帶底`（＝ **E-2′ 餘裕**）實測最小僅 **`0.001692`** ⇒「E-2′ 從未觸發」係**擦邊而過**，⛔ 換案不得據此假設不會響。 | ✅ **已切換**（K-6-A2 段六前置收官批·**KL 放行 2026-08-06**「照新量法切換」）。放行前依 KL 裁「**寬度影響先算完再切**」產出逐格攤表；土地後果：`3.5m R6/right 628-18(1)` **翻盤**（強制留設抵費地 → 可分配）、`max\|Δ寬\| = 2.674315 m`。 | 碼：`grep -n "min_width = float(parcel_min_width_n14" app.py`／`grep -n "def _n14_band_hi" app.py`；正典：`grep -n "通過後緣連續段" docs/rulings/K-6_*.md` ＋ `grep -n "^##### 🔒 K-9-6-g-2-a " 同檔` 之裁定段；量（**凍存**）：`verify/out/probe_K9_seg6_w_delta_切換前凍存.log:25`–`:55`（攤表與結語三數）、`:57`–`:81`（比值與 E-2′ 餘裕） | ❌ **仍未被真資料走過**——k98 生產呼叫點為 0（`grep -rn --exclude-dir=.claude "k98_virtual_measure_block" --include="*.py" .` ⇒ 僅定義與三支探針）。🔴 **段六接線後即被走過**，屆時本欄須更新 | ✅ **已裁**——KL 2026-08-06 放行，土地後果已量出並載於裁定段。⚠️ 段六接線（GB-18／GB-39）另有其自身之待裁面，⛔ 不得以本項已裁推定該批亦已裁 |
| **GB-38** 🆕 | **`verify/tools/nameset_diff.py` 以「整行」為名目，而多支閘之行文內嵌實測數值**⇒ 該數值一動即被讀成「**出 1／進 1**」之換名，名目集合之**增減計數不可信**。🔬 本波實例：`F.3 結構永久閘0m（trunk D 守恆綠·絕對值 Σ(G−幾何) 0.01 ≤ 0.585）` 之 `0.01 → 0.03` ⇒ 工具報「出 1／進 1」，而**判定仍為 PASS、翻轉 0**。<br>🔒 **判定翻轉之計數不受影響**（該欄比的是 PASS/FAIL、非行文）⇒ 本項**不影響既有結論**，只影響「出／進」二欄之可讀性 | **已在觸發**（非換案才觸發）：凡行文內嵌數值之閘，其數值一動即命中。受影響之閘族至少含 `F.0／F.2／F.3／F.4 結構永久閘`（行文含 `絕對值 Σ(G−幾何)` 之實測值）與其他把實測值寫進名目字串者 | **只登記·本波不改工具**（改工具屬對拍器本身之變更、須另批並重驗其對歷史 log 之相容性）。🔒 **使用紀律**：以該工具驗收時，「出／進」非零**一律須逐條具名說明**，⛔ 不得以「出 1／進 1、翻轉 0」一語帶過 | 工具：`grep -n "def main" verify/tools/nameset_diff.py`；本波實例：`python verify/tools/nameset_diff.py verify/out/K6A2_seg4c2a2_runall.log verify/out/K6A2_seg4c_runall.log` 之【名目族】段；具名說明之範例：`docs/reports/W-G.5_K6-A2-段四c_切換乙式.md` §六 | ✅ **已被走過**——K-6-A2 段四(c) 當場命中 | ❌ 不需——屬對拍工具之可讀性，非域裁 |
| **GB-37** 🆕 | **生產路徑上掛著一個「假陰性」偵測器，且無任何呼叫端在讀它**——`k97_solve_alloc_t` 之 `_on_cham`／`P_front_on_chamfer` 測的是 **`P_front`（`ALLOC ∩ 前緣線`）**、**不是** K-9-8 (2) 之 **`P_block`（`L_in ∩ BLOCK 邊界`）**（二者同名異物·K-9-7-a 衝突二）。🔬 實測：`probe_ruling_K9_7_alloc_t`【D】段 ＝ **（無）＝ 0/16**；同一批 `probe_K9_7d_layer2`【C】段量得 `P_block` **4/16 離開 FRONT**（落截角內 2 ＋ 二者皆非 2）。且 `_build_corner_range_v3` **從不讀** `_k97['notes']` 與 `_k97['P_front_on_chamfer']`（只取 `t_star`／`depth_at_t_star`／`D0`／`m`），而 k97 自身之註解逐字寫「由**呼叫端**決定是否停機」⇒ **無人讀·且讀了也會被誤導** | **已在觸發**（非換案才觸發）：任何 `P_block` 離開 FRONT 而 `P_front` 未離開之街角，本案即 4/16。⚠️ 其危害不是算錯值（該旗標不驅動任何分支），而是**讀者據其 0/16 誤判「GB-19 於本案零實例」**——**已在段四之敘述中發生過一次** | ✅ **已收**（K-6-A2 段四(c)·2026-08-05）·偵測器**整段移除**（含其回傳鍵），判定改由 **`P_block_on_chamfer`** 承擔（測 K-9-8 (2) 之 `P_block`）。其唯一消費端（`probe_ruling_K9_7_alloc_t`【D】段）**整段刪**並留指路行 → `verify/out/probe_K9_7d_layer2.log`【C】；⛔ **未改為讀 `P_block_on_chamfer`**——該探針不傳 `block_vertices`、取到必為 `None`，那只會生一個新的假陰性。驗收：`grep -rn --exclude-dir=.claude "舊鍵名\|舊區域變數名" --include="*.py" .` ⇒ **零命中**（墓碑註**刻意不寫出已刪符號之字面名**，使該 grep 為真零命中；`_on_cham98` 係 k98 之另一符號，不在此列） | 碼：`grep -n "_on_cham = bool(chamfer_tri" app.py`（其上方 `_sp = _s_Pfront(t_star)` 即證所測為 `P_front`）；消費端正面列舉（**重錨 `902f5d1`**）：`grep -n "_k97\[" app.py` ⇒ **4 行·6 鍵**——`:10680 t_star`／`:10683 depth_at_t_star`／`:10689 band_d_top,band_d_bot`（**段四(c)-2 乙式新增·GB-36**）／`:10701 D0,m`〔🗄️ 原載「只 `t_star`／`depth_at_t_star`／`D0`／`m`」＝**4 鍵·修前數**，屬跨 commit 引錨未重 grep·**失敗考古節 36**〕；`grep -c "P_front_on_chamfer" app.py` ⇒ **0**（**重錨 `902f5d1`**·符號已於段四(c) 整段移除、與本列處置欄一致）〔🗄️ 原載「⇒ 2」係**修前**數·同節 36〕；實料：`verify/out/probe_ruling_K9_7_alloc_t.log`【D】／`verify/out/probe_K9_7d_layer2.log`【C】 | ✅ **已被走過**——4/16 之落點差異係**現行生產圖 `V6` 上之實測**，非構造推論 | ❌ 不需——屬碼面正確性與診斷可信度，非域裁 |
| **GB-36** 🆕 | **量測帶之「起算線」——正典字面 vs 推導／夾具，二式不同**：K-9-6-b 之帶為「**道路境界線**、與其沿 `b̂n` 平移 `min(垂距)` 之平行線所夾」，而 K-9-6-b-1 已把「道路境界線」更正為 **`PQ`** ⇒ **字面之帶 ＝ `[PQ, PQ+D_new]`**；惟 **段四(c)-2(a) 推導 §三／§四** 與 **夾具 `fixture_corner_range_k8._band_min_width`** （自 `F1` 起量·(c)-1 未改此處）皆採 **`[FRONT, FRONT+D_new]`**（只把**帶深**改自 `PQ` 量）。二式差在**帶之上緣**。<br>⚠️ **推導 §四 之分支 1／2 斜率為 `c`** ——若採字面式，`k ≥ 0` 之支斜率將為 `c + k·μ`、與該表不符 ⇒ **二式不可同時為真** | **任何 `P_block` 離 FRONT 有內縮（`δ_P > 0`）之街角**（`k` 正負皆然）——`W` 之差 ＝ **`k · δ_P`**（帶之二端一齊平移 `δ_P`）。🔴 此 `k` 須取**解析** `∂w/∂d`，⛔ **不得取正典 K-9-7-c 表之 `k` 欄**（該欄係擾動觀測 `∂W/∂D`、於 `k ≥ 0` 時**恆觀測為 0**）。本案 16 格最劣為 **`R3/right 0m`：`絕對值 k·δ_P` ＝ 4.53e-03 m ／ 3.50 m**（次之 `R1/left 0m` 1.63e-03），**低於法定粒度 `0.01 m`**，惟**約為 (c)-2 自身最大影響（7.30e-04 m）之 6 倍**。⚠️ **受影響之集合亦不同**：(c)-2 動的是 `k < 0` 四格；GB-36 最劣二格為 `k ≥ 0` 之 `R3/right`／`R1/left`——該二格於 (c)-2 之 `Δt* ＝ 0`，但字面式下會**開始動** | 🔒 **KL 已裁：採乙式**（2026-08-05·K-9-6-b-2）＋ ✅ **(c) 已切換**（K-6-A2 段四(c)·KL 放行 2026-08-05·**生產行為首次真變**）。落地：引擎 `grep -n "K-9-7-d \*\*第二層分支\*\*" app.py`（**opt-in·生產零呼叫點**）＋夾具 `grep -n "def _band_min_width" verify/fixture_corner_range_k8.py`；階段 A（甲式夾具 × 乙式引擎）16 格殘差逐格對上閉式 `−k·δ_P`、分支 ① 之二格由 0 躍為最大／次大（`verify/out/probe_K9_7d_stageA_asrun.log`）。⚠️ 施工單 §二-2 之 `δ_P = (P_block − F1) @ bn` 為**筆誤**（實測最劣錯 4.12 m），已依乙式推導 §二-3 改為 `((P_block−F1)·n̂)/(b̂n·n̂)` | 正典：`grep -n "^#### K-9-6-b " docs/rulings/K-6_街角地分配程序與可分配判準.md`（含 `K-9-6-b-1`）；推導：`docs/reports/W-G.5_K6-A2-段四c2a_第二層分支推導.md` §三／§四；夾具：`grep -n "def _band_min_width" verify/fixture_corner_range_k8.py`；量級實料：`docs/reports/W-G.5_K6-A2-段四c2a_實作與攤表.md` §五 | ⚠️ **本案有活實例但影響低於法定粒度**——`R4/right` 兩情境 `δ_P` ≈ 0.0145／0.0140 且 `k < 0`；⛔ 惟該實例**換 `V6_1` 後消失**（GB-15／GB-16） | ✅ **已裁**（KL 2026-08-05·採乙式） |
| **GB-34** 🆕 | **`grep` 之多 `-e` 呼叫曾於本機偽造「0 命中」**——背景任務實測：GNU grep 3.0（MSYS）以兩個以上 `-e` pattern 呼叫時 **abort（rc=134）**，只印 `Broken pipe`／`Aborted`、**零輸出**，且與**真正的 0 命中無法區分**。已差點污染三條證據指令（GB-8／GB-30／GB-22）。<br>🔴 **⚠️ CC 於 `e91c0b1` 當場嘗試復現：六種組合全部未復現**（兩 `-e` ASCII／兩 `-e` 中文／三 `-e` 中文／無 `--include`／`--include=*.md`／接管線送 `head`）——rc 皆 0（接管線者 rc=141＝**SIGPIPE**，非 SIGABRT）。⇒ **觸發條件未收斂**。🔴 **這使它更危險、不是更安全**：間歇性偽 0 比穩定失效更難防，且**恰好落在本倉最戒備之「乾淨的 0」形狀**上（`no-silent-fallback`／失敗考古之反覆案由）。⛔ **禁**因「我這次跑沒事」而視為已解除。 | **不明**（未收斂）。已知環境：GNU grep 3.0 · MSYS · Windows。🔴 **此為 GB-31（執行環境未鎖）之具體實例，且把該登記之影響面推廣了**——GB-31 原記之風險為**浮點末位**（診斷層），本條顯示**工具版本差異可直接竄改證據**（舉證層）。 | **本批只登記**。**緩解已落地且無條件**：證據指令一律改 **BRE 交替記法**（以**反斜線跳脫之豎線**把數個 pattern 併為**單一** pattern，如 `A` 與 `B` 合寫成一個 pattern·**不觸多 `-e` 路徑**）。🔒 三處已複驗可重現其原載結果（CC 自跑·`e91c0b1`）：GB-8 系 `grep -rn "_mina_by_block" --include="*.py" .` ⇒ 命中含 `verify/wd4_tier_list.py:428`；GB-30 系（探針 tee 真 stdout）⇒ **0**；GB-22 系（`assert`／`==`／`一致`／`同源` 過濾）⇒ **僅 `verify/wd4_tier_list.py:428` 一行且為註解**（＝該列原載之「僅命中註解、無斷言」）。 | 症狀來源：背景任務實測（2026-08-04）＋ CC memory `grep-e-abort-trap`；非復現實測：見本列上段六組合。⚠️ **該發現原僅存於 memory、不在倉內** ⇒ 依 `verify/run_all.py:157` 之案由入倉。 | ⚠️ **已被真資料走過**（曾差點污染三條證據指令）；惟**觸發條件未復現** ⇒ 影響面未掃。 | ❌ 不需——工具環境之技術項。 |
| **GB-35** 🆕 | **倉內巢狀 worktree 使「全倉 grep」靜默雙計**——`.claude/worktrees/` 之 worktree 係**完整倉庫副本**且由 `.git/info/exclude:7` 忽略 ⇒ **`git status` 永遠看不到它**，但 **`grep -rn … .` 看得到**。實測（`e91c0b1`·當場）：`grep -rln "_mina_by_block" --include="*.py" .` ⇒ **含 worktree 12 檔／排除後 6 檔**——**整整一倍**。⚠️ 本表多條之證據指令即為 `grep -rn … .` 形式（GB-8／GB-22／GB-24 等）⇒ **當下正被污染**。 | **任一 worktree 存在於倉內時**（背景任務、`isolation: worktree` 之 agent、手動 `git worktree add` 至倉內路徑）。⚠️ **非「換案才觸發」**——今日即為真：`git worktree list` 顯示 `…/.claude/worktrees/frosty-agnesi-fa87af` 為 active。🔴 **與 GB-34 同族但更陰險**：GB-34 使命中數變 **0**（偽陰性·本倉最戒備者），本條使命中數**加倍**（偽陽性）——而「命中變多」讀起來像**更完整**，不像出錯。 | **本批只登記**。⛔ 不自決是否刪該 worktree（屬背景任務之產物·非本波所有）。**緩解＝ §0-3 所立之紀律：證據指令一律正面列舉受檢檔**，不以 `grep -rn … .`（其範圍隨倉庫狀態漂移）界定母體。 | 現場：`git worktree list`；`ls -la .claude/worktrees/`；忽略來源 `git check-ignore -v .claude/worktrees` ⇒ `.git/info/exclude:7`。雙計實測見上。 | ⚠️ **已被真資料走過**——本條即於 §0-1 複驗 GB-8／GB-22 之證據指令時**當場撞見**。 | ❌ 不需——證據衛生之技術項。 |
| **GB-33** 🆕 | **`parcel_min_width_n14` 之量測帶沿 FRONT 法向平移，與正典 K-9-6-b「沿 **BASELINE 法向**平移」不符**——碼面逐字：`app.py:6411`「令 `t` ＝ 沿 FRONT 法向（指向宗地內）之深度」（`grep -n "沿 FRONT 法向（指向宗地內）之深度" app.py`）；法向構造於 `app.py:6464`（`_n = np.array([-_d[1], _d[0]])`·`_d` ＝ FRONT 單位向量）。正典則為 `grep -n "^#### K-9-6-b " docs/rulings/K-6_街角地分配程序與可分配判準.md`「前緣線、與『前緣線沿 **BASELINE 法向**平移 `min(垂距)`』之平行線，二線所夾之**帶**」，且 **K-9-6-a 表列第 1 種**（`grep -n "^#### 🔒 K-9-6-a " 同檔`）明定量測帶深度＝「臨街段每點至 **BASELINE** 垂距之 min」。**KL 2026-08-04 重申**。<br>🔴 **混用點（同一函式內二軸並存）**：`k98_virtual_measure_block` 以 `_dep()`（**BASELINE 法向**·`grep -n "def _dep" app.py` ⇒ `app.py:9638`）算 `depth_from_PQ`，再把該值當帶深**餵進 FRONT 法向之帶**（`grep -n "min_width = float(parcel_min_width_n14" app.py` ⇒ `app.py:9704`）。 | **FRONT 與該街廓 BASELINE 之夾角 θ 夠大時**偏差進入法定粒度。🔬 量級（**出處：claude.ai 2026-08-04 自 `data/V6.dxf` 量·⛔ 非 CC 自驗**·同 GB-31 §一-2 之出處紀律）：θ ＝ `0.023°`（133° 群 R1/R4）／`0.691°`（48.7° 群 R2/R5）／`0.557°`（R6）／`2.868°〜4.891°`（43.2° 群 R3）；帶深偏差 `D(1−cosθ) ≤ 0.146 m`，乘 `abs(k) ≤ 0.0756` ⇒ **寬度偏差 ≤ 約 3 mm ＜ 法定粒度 `0.01 m`** ⇒ **本案零土地後果**。⚠️ 換案（θ 更大／`abs(k)` 更大之街廓）即可能翻過粒度。 | 🔧 **定義已更正·待切換**（K-6-A2 段五(c)-1·2026-08-05）——本項所指之「帶沿 FRONT 法向平移」**已隨帶底更正而消滅**：新式帶底**直接由後緣連續段最淺端點之 `t` 定位**，⛔ **不再有任何平移／投影係數**（`grep -n "def _n14_band_geom" app.py`）。<br>✅ **已落地（K-6-A2 段五(c)-2·KL 放行 2026-08-06）·生產已切換**。⚠️ 🔴 **本項不得因此結案**：其**另一半**（`grep -n "屁股側：垂距沿 FRONT 法向" app.py`／`grep -n "FRONT 法向·取號指向街廓內" app.py` 之屁股側判號同軸用法）**本批未觸**，須另立專批。 | 碼：上列三組 grep；正典：K-9-6-b ＋ K-9-6-a 表列第 1 種；量級出處：claude.ai 實跑（同 GB-31 §一-2）。<br>⚠️ **併記待該專批一併判**：`app.py:1559`（註「屁股側：垂距沿 FRONT 法向（指向街廓內）為**正**」）與 `app.py:1651`（`_nb = (-_db[1], _db[0])`·註「FRONT 法向·取號指向街廓內」）另有同軸用法（屁股側判號）——**⛔ 本批不主張其對錯**，僅登記其存在，避免專批漏掃。 | ⚠️ **已被真資料走過**（全案非街角宗之 N-14 每次 `run_all` 皆走），惟**其誤差在本案恆低於法定粒度** ⇒ 從未顯形。 | ✅ **可能需**——「量測帶該沿哪一軸」已由 K-9-6-b 定式（**BASELINE 法向**），碼面照改即可，**惟改後全案 N-14 數字會動**⇒ 其**重烤時機**與**是否併入某波**須 KL 定。 |
| **GB-32** 🆕 | **`probe_ruling_N_e1_touch` 之 `T4[0m]` 期望式與 `T1[0m]` 錨，陳舊於 K-8 §三 commit B**——① **`T4`**：`_exp_n = {"0m": _n_side, "3.5m": _n_side - _n_forced}`（`grep -n "_exp_n = " verify/probes/probe_ruling_N_e1_touch.py`）⇒ **`0m` 之期望式整項省略 `− 強制抵費地`**（非「寫死 8」——`_n_side` 係自快照現算）；且 `_n_forced = len(_read_csv(".../W-D.1.3-d 驗收_退縮3.5m.csv"))` **只讀 3.5m 之 baseline CSV、無 0m 對應來源**。設計出處逐字：`docs/reports/W-G.5_裁定N_E系列_BLOCKED_修正報告.md:140`「`case_params` 之 `有SIDE_LINE`(8) − baseline 強制抵費地(3)」⇒ **兩情境本應同式**。② **`T1[0m]`** 之錨 `R3·628-45(2) min t = 0.7655`（`grep -n "0.7655" 同檔`）係**以該宗為「街角」身分**所取 | **已觸發**（非換案）：K-8 §三 commit B（街角規定範圍新構造）使 **0m 之 forced 由 0 變 1**（正典逐字：`grep -n "強制留設抵費地" docs/rulings/K-6_街角地分配程序與可分配判準.md`——「**R3 右**：`G007（628-45）` → **強制留設抵費地**（該側合格候選 1 → 0）」）⇒ 0m 街角數 `8 → 7`、且 `628-45(2)` **早已非街角** ⇒ 二錨同時失準。**真值 `7 = 8 − 1` 完全正確** ⇒ **係陳舊斷言／陳舊錨、非土地錯誤** | **本批只登記**·⛔ **不得逕改常數使其變綠**·**與 P-H 波末重烤同批更新**（正解：`_n_forced` 改為**逐情境**取得、`0m` 亦套 `_n_side − _n_forced[tag]`；`T1[0m]` 之錨隨該宗身分變更而重取）| 探針：上列二 grep；設計出處：`…BLOCKED_修正報告.md:140`；commit B 之土地後果：`docs/reports/W-G.5_K8-段三_commitB_街角規定範圍新構造.md`（記其「改變三筆街角地之歸屬」）| 🔴 **毒性二層**：(1) **混在 46 FAIL 裡** ⇒ 日後真正的新紅無法被辨識（同 GB-27 之「潛伏紅」形狀）；(2) **從未在 `run_all` 輸出中現形**——失敗時只印最後 14 行（`grep -n "splitlines()\[-14:\]" verify/run_all.py` ⇒ **4 處**），而 T 斷言全在那 14 行**之前**（實核 `verify/out/K6A2_seg3c3_runall.log` 該段 14 行**全是快照 diff、零條 T 斷言**）| ❌ 不需——屬期望值／錨之陳舊，技術修正 |
| **GB-31** 🆕 | **執行環境未鎖**——`requirements.txt` **11 條相依全部只寫 `>=`**（`awk` 實數：非註解行 11、含 `==` 或上界者 **0**）、**無 lockfile**（`ls poetry.lock Pipfile.lock uv.lock requirements.lock` ⇒ 皆不存在）、**無 GEOS 版本紀錄**（`grep -in geos requirements.txt` ⇒ 0 命中）。而幾何鏈實際經 **Shapely/GEOS**（`grep -n "_S_cut = S_conv" app.py` 之 bisect）| **不同機器**即可能得不同全精度結果。🔬 **已坐實**：同一支探針於他機與 KL 機，`S0d` 診斷之全精度浮點為 `0.1953660243826076` vs `0.19536602438260764`（**兩個不同 float64**）。⚠️ **泛化波之使用者是不同機器上之公務員與廠商** ⇒ 屆時「同一份圖跑出不同數字」將難以歸因 | **本批只登記**。⛔ **禁現在改 `requirements.txt`**——動版本會**牽動全部 baseline**，屬**波末批** | 證據：上列三組 grep／`ls`／`awk`；跨機浮點實例與其反向（策展 `.log` 跨機逐位元全等）見 **GB-30**。<br>🆕 **環境解析快照（2026-08-04·`5ef7de3` fresh clone）**：`docs/reports/W-G.5_環境基準_換機確認.md` §一——Python **3.13.11**／**shapely 2.1.2**／**GEOS 3.13.1**／numpy 2.4.4／pandas 2.3.3／ezdxf 1.4.3／openpyxl 3.1.5（Windows 11 10.0.26200·`os.sep='\'`）。⚠️ 施工單原指「寫入 GB-31 之**本機環境**欄」，惟**本表無此欄**（7 欄制）⇒ 依「以倉內為準並載明差異」**不新增欄**（會動全表每列），改立該檔為常駐落點、自本欄指去。🔴 **該快照是紀錄、不是鎖**——GB-31 之成立條件（全 `>=`／無 lockfile／無 GEOS 紀錄）**未變**。<br>🆕 **跨主機實測（出處：claude.ai 2026-08-04 於其主機實跑·⛔ 非 CC 自驗）**：見同檔 **§一-2**。**(甲) 判定層跨主機穩定**——Win/Py3.13/pandas2.3.3 vs Linux/Py3.12/pandas3.0.2，對同靶正規化對拍 **判定翻轉 0**、嚴格計數同為 86／46；唯一出／進即 `S0d` 全精度浮點末位（`…60764` vs `…6076`）⇒ **分歧只到診斷層、未進判定層**。**(乙) 🔴 鎖版本必要但不充分**——兩機 shapely/GEOS **同版**（`2.1.2`/`3.13.1`）而全精度值仍不同 ⇒ 補 lockfile 也**消不掉**此分歧；剩餘嫌犯（**未收斂·⛔ 禁擇一寫成結論**）：Py3.12 vs 3.13／Linux vs Windows libm／`ezdxf 1.4.3 vs 1.4.4`。⚠️ **(甲) 係量化風險邊界、非解除本登記。** | ⚠️ **已被真資料走過**（跨機浮點差異已實測）；惟**影響面未掃**——僅知診斷行不穩定、實質量在報告精度上穩定 | ❌ 不需——屬環境治理之技術項 |
| **GB-30** 🆕 | **探針之存證 log 由構造排除引擎自報診斷**——`verify/probes/` **22 支**全數自寫 `OUT`／`L` 緩衝再落 `LOG`，**無一支 tee 真 stdout**（對 `verify/probes/*.py` 之 `grep -n`·BRE 交替 pattern：`Tee`／`redirect_stdout`／`dup2`／`sys\.stdout =` ⇒ **0 命中**）⇒ 引擎於**探針行程內**自報之 `🔴 S0d 異常…（no-silent-fallback）`（`grep -n "S0d 異常" app.py`）與 `[T2-DIAG]`（`grep -n "T2-DIAG" app.py`）**由構造不會進入存證檔** | **新的引擎 🔴 於探針實跑中出現時**：存證檔看不到，而該檔正是 **GB-24** 等處被引為證據者 ⇒ 讀者以為看的是實跑逐字稿，實為**策展報告**。⚠️ **非「換案才觸發」**——結構性、恆存。<br>🔴 **併記（反向）**：`.full.log` **⛔ 不得作為跨機逐位元對拍之回歸靶**——同一支探針於他機與 KL 機，`S0d` 診斷之**全精度浮點不同**（`0.1953660243826076` vs `0.19536602438260764`·**兩個不同 float64**）；`_S_cut = S_conv`（`grep -n "_S_cut = S_conv" app.py`）之 bisect 經 Shapely/GEOS，而 `requirements.txt` **全部只寫 `>=`、無上界、無 lockfile、無 GEOS 版本紀錄**（見 **GB-31**）。<br>⚠️ **反向亦須記**：**策展 `.log` 跨機逐位元全等** ⇒ **實質量在報告精度上是機器穩定的**——不穩定者僅診斷行之全精度浮點 | **本批只登記＋補三支全文存證**（`verify/out/*.full.log`·以 `> … 2>&1` 於探針外捕獲·**⛔ 未改探針碼**）。⛔ **禁順手改 22 支**——族群性改法屬**波末批** | 構造證據：上列反向 grep；全文 vs 策展之逐行對拍：`verify/out/F2_stepg_invalidate.full.log`／`verify/out/K9_4_gate_discrimination.full.log`（剝除之行**皆為**引擎自報診斷、剝除後逐行全等）。<br>🆕 **閘措辭補允許清單（段四 §0-2）**：除引擎自報診斷外，**探針自身之寫檔宣告行**（`→ verify/out/*.log`）及其前之空行**亦不算非診斷差異**——該行由 `print` 落在 `LOG` 寫入之後、**由構造**進不了策展檔。初版清單漏列致必然恆停機；⛔ **不得為此改探針**（規格太緊，非探針有錯）。實例：`verify/out/probe_k98_virtual_block.full.log` 與其 `.log` 之差 16 行＝ `[T2-DIAG]`×12 ＋ `🔴 S0d`×2 ＋ 空行×1 ＋ 寫檔宣告×1| ⚠️ **本案零後果**——該二診斷係**既有且普遍**之診斷（幾乎每份 `verify/out/*runall*.log` 皆有；專節見 `docs/reports/W-G.5_K8-段二_N19′施工與連帶量測.md`），非新紅 | ❌ 不需——屬存證保真之技術缺口 |
| **GB-29** 🆕 | **`verify/probes/` 之多支未接 `run_all`**（🔧 **K-6-A2 段五(a) 補記**：本波再新增 `probe_K9_6_parcel_band`／`probe_K9_7d_layer2`／`probe_K9_7d_pk_impact`／`probe_K9_7d_stageA` 四支，**同屬未接**——⛔ 其中 `probe_K9_6_parcel_band` 係**依施工單明令不接**（本批無門檻可判），**與既有未接者同批處理**）——原登記：22 支中 18 支未接 `run_all`——`run_all.py` 僅以 `subprocess` 起 **4 支**（`grep -n 'os.path.join(HERE, "probes"' verify/run_all.py` ⇒ `probe_ruling_N_e1_touch`／`probe_ruling_K4_s_origin`／`probe_ruling_K8_sideline_pairing`／`probe_ruling_K8_baseline_pairing`）| **未接之 18 支所消費之介面漂移時**：無人執行 ⇒ **靜默腐爛**。本波新升格三支（`probe_k94_gate_discrimination`／`probe_f2_stepg_invalidate`／`probe_k92_edge_cases`）消費 `harvest()`／`run_corner_pk`／`run_step_g`／`k92_block_depth_check`——**皆為會漂移之介面**。⚠️ 本波三支之施工單**原就未要求接** ⇒ **非本批缺失**，惟形狀與 **GB-24** 同族 | **本批只登記**。⛔ **禁現在接線**——18 支全接會使 `run_all` 工時失控，**取捨屬波末批之範圍決定** | 已接之 4 支：上列 grep；`run_all` 之同族案由：`grep -n "先前\*\*只存在於聊天與註解\*\*" verify/run_all.py`（BLOCKED-3：「倉內不可重現」）；同族：GB-24（`run_all` 對 app UI 分支覆蓋＝零）、F-2／BLOCKED-3 之「**沒人檢查 ≠ 相符**」| ⚠️ **18 支從未被 `run_all` 執行過** | ❌ 不需——屬覆蓋範圍之取捨，波末批決定 |
| **GB-28** 🆕 | **`fixture_corner_range_k8` T1 之量測起算線為 FRONT_LINE，未實作 K-9-8 之 `PQ` 延伸線段**——K-9-6-b-1 明訂量測用之「道路境界線」＝ **K-9-8 (3) 之延伸線段**（平行 FRONT_LINE、通過 `P_block`），**深度自該線段起算**；T1 之 `_range_min_depth`（`grep -n "def _range_min_depth" verify/fixture_corner_range_k8.py`）現行自 **FRONT_LINE** 起算 | **`P_block` 落在截角邊時**觸發（落 FRONT 重疊段時 K-9-8 (6) 退化、與現行同）。🔬 本案 12 格中 **10 格落 FRONT 重疊段、2 格落截角邊**（`R4/right` 兩情境·正典 K-9-7-d 已更正）⇒ **現行退化可行，惟非全域可行** | **本批只登記**·**K-9-8 落地（段四）時 T1 之起算線須一併改** | 夾具：`grep -n "段四須複查" verify/fixture_corner_range_k8.py`（已就地留註）；裁定：`grep -n "^#### 🔴 K-9-6-b-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md` | ⚠️ **本案多數格走退化支**⇒ 截角邊支之起算線**未被真資料充分走過** | ❌ 不需——K-9-8 已裁定，屬碼面隨裁定調整 |
| **GB-27** 🆕 | **`GSA_EXPECT` 錨檢容差 `abs(G − exp) > 0.01` 對「一個法定面積粒度」為盲**——被比欄 `G(㎡)` **於來源即 `round(..., 2)`**（`grep -n "'G(㎡)': round" verify/stepg_pipeline.py`），而錨檢為**嚴格**不等式（`grep -n "abs(G - exp) > 0.01" verify/wf_f0.py`）⇒ **`Δ` 恰為 `0.01` 時 `>0.01` 為 False、不觸發** ⇒ **有效解析度 ＝ `0.02㎡`**，而 **`0.01㎡` 正是本案之法定面積粒度**（市地重劃實施辦法 §3 記至二位小數） | **任何使某 gid 之 `G(Σa)` 恰移動 1 個粒度單位之改動**。⚠️ **非「換案才觸發」——本案已坐實一例**：K-6-A2 段三(c) 之 `G007@3.5m` `370.02→370.01`（Δ `−0.01`）**未被錨檢咬到**，係人工逐格對拍方才發現。🔴 **真正的害＝潛伏紅**：該格若留舊錨，後波再一個 `0.01` 微擾即累積為 `0.02` 而爆，**屆時歸因會指向後波、真成因卻在前波** ⇒ 正是 F-7-3「禁打地鼠」所防之形狀 | **本批只登記不改**——改容差會**牽動其他錨**（現行全部錨值皆在 `0.01` 容差下定讞），屬**波末批**。🔹 本批已就 `G007` **主動重錨**以消該例之潛伏紅（claude.ai 裁 2026-08-04），但**機制缺口未修** | 容差：`grep -n "abs(G - exp) > 0.01" verify/wf_f0.py`；粒度源：`grep -n "'G(㎡)': round" verify/stepg_pipeline.py`；實例與量測：`verify/out/probe_k97_r5_attribution.log`【1·3.5m】段；歸因表：`grep -n "K-6-A2 段三(c) 重錨" verify/wf_f0.py` | ⚠️ **已被真資料走過且未被咬到**（G007@3.5m）——此即本項之直接證據 | ❌ 不需——屬閘之數值設計缺口，純技術 |
| **GB-26** 🆕 | **`R4/left` 係唯一「`m` 真正進入斜率（分支 ③）、且解析與正典觀測相差約 60%」之格·段四須單獨確認**——解析 `m = 0.000424` vs 正典 K-9-7-c 表 `+0.000266`（`grep -n "^#### K-9-7-c " docs/rulings/K-6_街角地分配程序與可分配判準.md`）。⚠️ **`R2/left` 同為分支 ③ 但兩者一致**（`0.009750`）⇒ **不能與 R2 一併結案**。分支 ①② 之 `m` 不進斜率（①深度不影響、②`D` 釘在 `Q0`）⇒ 其差異已由「觀測 vs 解析」解釋完畢；**唯 ③ 之 `m` 真正乘進 `(c − k·m)`** ⇒ **R4/left 是唯一「`m` 之差異真的影響結果」之格** | **段三(c) 切換後即生效**。位移雖僅 `0.111cm`／面積 `+0.037㎡`（八格中次小），惟**其正確性未經獨立確認**——`R2/left` 之一致**不能外推**至 `R4/left` | 🔴 **維持開啟**（K-6-A2 段三(c) 已切換·**本項已生效但仍未獨立確認**）——⛔ **不得因切換完成而順手結案**（KL 明令 2026-08-04）。**本批只登記·不查因·段四單獨確認** | 解析：`verify/out/probe_ruling_K9_7_alloc_t.log`【A】段；🔧 **該探針之 `a_old` 於 K-6-A2 段四(c)-2(a′)（`af23188`）修**（前為與 `a_new` 同源、`Δ面積` 恆 `0.000`）——修後**本列之「位移 `+0.111cm`／面積 `+0.037㎡`」已可由其自身所引之證據源復現**（**有效期 commit ＝ `3c6a184`**·於該 commit 之 `verify/out/probe_ruling_K9_7_alloc_t.log`【B】段 `0m R4/left` 列當場可讀）；正典表：上列 grep；求解器：`grep -n "def k97_solve_alloc_t" app.py` | ⚠️ **未經獨立確認·且自段三(c) 起已進入生產**——`R4/left` 之位移（+0.111cm／+0.037㎡）即由該 `m` 決定；`R2/left`（同為 ③、`m` 兩路一致）**不能外推**至本格 | ❌ 不需——屬數值正確性之技術確認 |
| **GB-25** 🆕 | **Step G 之產物鍵無「進入重算即失效」之機制**——`f3_G_values`／`f3_G_trace` 寫在 Step G **最末**（`grep -n "st.session_state\['f3_G_values'\] = " app.py`）⇒ 中途 raise 時二者**不被覆寫、也不被清除**；而成果區與下載鈕（`grep -n "if st.session_state.get('f3_G_values'):" app.py`）照樣渲染**舊值**。既有失效路徑 `_f3L_invalidate_g_cache`（`grep -n "def _f3L_invalidate_g_cache" app.py`）**只綁街角退縮／分配深度二 widget 之 `on_change`**⇒ **改 DXF／改屁股線指派均不觸發** | **成功跑過一次 → 改屁股線指派 → 重跑被 K-9-4 閘擋下** ⇒ 使用者仍可下載**與現行屁股線設定不符**之分配成果表。🔴 **土地後果**：交付錯誤之分配成果——**而這正是 K-9-4 最想防之情境**。⚠️ 非 K-9-4 引入：Step G 內另有既有 raise（`grep -n "raise RuntimeError" app.py` 之 Step G 段·已確證不在任何 `try` 內）同樣暴露此風險 | ✅ **本單修**（K-6-A2 補正 F-2）：於重算分支起點清 `f3_G_values`／`f3_G_trace` ＋ 立 `f3_g_needs_rerun`＝**家族修**，一次覆蓋 Step G 內現有與日後所有 raise | 修法：`grep -n "K-6-A2 補正 F-2" app.py`；驗證：`verify/out/F2_stepg_invalidate.log`（三項·自 `app.py` 抽真敘述執行） | ✅ **已修並驗**（成功路徑不變／失敗路徑清乾淨／上游輸入未誤清） | ❌ 不需——純狀態機缺陷 |
| | ↳ 🔒 **併記（實作紅線）**：`f3_corner_winners`／`f3L_corner_winners` 屬 **Step G 之上游輸入**（步驟 L 寫於 `grep -n "st.session_state\['f3_corner_winners'\] = " app.py`，Step G 讀於 `grep -n "_step_l_winners = " app.py`）⇒ **不得納入該失效集合**；於重算起點 pop 之即**當場毀掉 Step G 自己的輸入**。⛔ 故**禁照抄** `_f3L_invalidate_g_cache` 之鍵名清單。另 `f3_g_needs_rerun`（橫幅旗標）與 `f3_g_needs_recalc`（自動重算觸發）**是兩把不同的鍵**——碰後者會造成「失敗後自動重跑、再失敗」之迴圈 | | | | | |
| | ↳ ⬜ **待查（登記不改）**：其餘步驟（A〜F、H〜L）是否有同型之「產物鍵不隨重算失效」，**本單未掃**，留**泛化波** | | | | | |
| **GB-24** 🆕 | **K-9-4 停機閘只在 app 路徑生效，`run_all` 永遠咬不到 K-9-4 違例**——上閘 `k94_assert_baseline_touch` 之**唯一呼叫點**在 `main()` 之 Step G 分支（`grep -n "k94_assert_baseline_touch(_k94_touch)" app.py` ⇒ **1 命中**）；harness 走 `verify/stepg_pipeline.run_step_g`（另一套 Step G），**不呼叫該閘**（`grep -c "k94_assert_baseline_touch" verify/run_all.py` ⇒ **0**、`grep -c "k94_assert_baseline_touch" verify/stepg_pipeline.py` ⇒ **0**）<br>🔴 **證據指令已改「正面列舉」（段四(c)-2 §0-3）**：原式為「全倉 grep 之輸出再接 `grep -v` 排除 `app.py`」，該式**隨倉庫長大自動過期**——今回已 **3 命中**（`verify/probes/probe_f2_stepg_invalidate.py:8,135`、`verify/probes/probe_k94_gate_discrimination.py:92`）。⚠️ **已查明非實質變動**：二探針於 `d6b7c7f` 入庫，**晚於**本列登記之 `dd02b5b`；且 `probe_k94_gate_discrimination.py` 之存在目的即「**證明該閘之鑑別力**」⇒ 其命中係**本列已記之緩解措施**、非反證。⇒ 改為**正面列舉受檢檔**（`run_all.py`／`stepg_pipeline.py` 各 0），探針之命中**係刻意覆蓋**。參見 **GB-35**（倉內巢狀 worktree 使全倉 grep 雙計）——同屬「範圍界定隨倉態漂移」之族。。探針 `probe_ruling_K9_4_baseline_touch.py` 亦**只呼叫量測函式、不呼叫閘**（設計如此·段二-1(a) 為「只量不判」） | **任何 K-9-4 真違例之案**：`run_all` 全綠、但 KL 於 UI 實跑時停機。⚠️ **方向與多數 GB 相反**——不是「換案才壞」，是「**換案會壞、而 run_all 不會事先告訴你**」 | **本批只登記**。⛔ **禁**逕把閘塞進 `verify/stepg_pipeline`——app 與 harness 之 Step G 係**兩套實作**（既有二元性），貿然接線屬引擎接線本體、逾本批之界 | 呼叫點：`grep -n "k94_assert_baseline_touch(_k94_touch)" app.py`；harness 缺席：上列反向 grep；閘之鑑別力**另由合成案證明**（`verify/out/K9_4_gate_discrimination.log`·三案皆咬） | ⚠️ **`run_all` 對本閘之覆蓋＝零**。本案零違例故無差別，但**「run_all 綠」不得被引為「K-9-4 已驗」之證據**。<br>⚠️ **併記（K-6-A2 段三 F-1 補）**：K-9-4 之**母體界定** `k94_partition_land_rows`（`grep -n "def k94_partition_land_rows" app.py`）於本案**零觸發**（無地宗 **0 宗**）⇒ 其**排除邏輯**與 **`_bad_no` 自檢分支從未被執行** ⇒ **不得記為「已驗」**，與閘本身同屬**換案防呆** | ❌ 不需——屬 app/harness 雙路覆蓋之技術缺口，同 GB-9／GB-8 之家族 |
| **GB-23** 🆕 | **`cad_layers['baselines']` 為每街廓單值之字典、全倉無「以 BASELINE 切分街廓」之機制**——結構為 `{block_label: {'point','angle_deg','enabled','source'}}`（`grep -n "result\['baselines'\]\[_lbl\] = " app.py`；契約見 `grep -n "'baselines': {block_label" app.py`）⇒ **一街廓恆只掛一條屁股線**。而 K-9-4-1 ③ 之「街廓內再畫一條 BASELINE 使大街廓成為兩個小街廓」**須由使用者在 CAD 端先把街廓切開**（BLOCK 圖層），程式端**無**「依 BASELINE 自動切分街廓」之機制 | 使用者在**未切開 BLOCK** 之情形下於街廓內多畫一條 BASELINE ⇒ 該線可能參與配對、或被忽略；無論何者，**同街廓兩排之意圖不會被程式理解**。⚠️ 惟 **K-9-4 上閘已使此情形轉為「跨過屁股線」而停機**（訊息明示「街廓需先切成兩個小街廓」）⇒ **不會靜默配錯** | **KL 裁 2026-08-03**：圖層一律由使用者於 CAD 繪妥後匯入，程式不需要亦不應設計畫設 BASELINE 之功能 ⇒ **本項為說明性登記、非缺件**，**不排入泛化波待修項**。併記：BASELINE 得由二街廓共用，且得兼任鄰街廓末端塊第 1 筆宗地之起始境界線（非 SIDELINE）。 | 結構：`grep -n "result\['baselines'\]\[_lbl\] = " app.py`；裁定：`grep -n "^### 🔒 K-9-4-1 " docs/rulings/K-6_街角地分配程序與可分配判準.md`；本案 4 條服務 6 街廓之實測：同檔 `grep -n "^#### 🔬 K-9-4-1-b "` | ✅ **本案即走「多對一共用」**（4 條服務 6 街廓·背對背／共線／兼任末端境界線三種情形皆有實例） | ❌ **已裁**（KL 2026-08-03·說明性登記） |
| **GB-22** 🆕 | **BASELINE 法向有【兩份】實作、號規不同、無一致性看守**——① **`_baseline_normal_axis`**（K-6-A2 段二-1(a) 抽出·`grep -n "def _baseline_normal_axis" app.py`）：`bn = (−bdy, bdx)/abs(b)`、**無號規**（呼叫端自取絕對值），現為 **N-19′（`_compute_block_depth_alloc`）與 K-9-4（`k94_baseline_touch_by_parcel`）共用**；② **`_build_corner_range_v3` 內聯者**（`grep -n "定號：指向 BASELINE" app.py`）：`bnx, bny = -buy, bux` 後**再依 `(B0−F1)·bn < 0` 翻號**、**定號指向 BASELINE**。⇒ **二者號規不同、不可互代** | 任一份被改而另一份未同步 ⇒ **靜默分歧**。⚠️ 本項與 GB-8（per-block MinA 三份）**同形**：多份實作、**全倉無任何閘比對之**。🔬 本批已把「原 `_compute_block_depth_alloc` 內聯之第三份」併入①（**零行為**·算式逐字相同）⇒ **份數自 3 降為 2**；**②未動** | **不改②**（本批只統一①）·**波末批評估是否可併** | ①`grep -n "def _baseline_normal_axis" app.py`；②`grep -n "定號：指向 BASELINE" app.py`；一致性看守之不存在：`grep -rn "_baseline_normal_axis" --include="*.py" .` 之輸出再接 `grep -i`（BRE 交替 pattern：`assert`／`==`／`同源`）⇒ **0 命中** | ⚠️ **①已由 K-9-4 探針【D】段實測與 N-19′ 逐格對拍（六街廓差 0.00e+00）**；**②從未與①對拍過** | ❌ 不需——純技術之實作統一。⛔ **②之號規係街角範圍構造所需**（範圍須朝 BASELINE 方向鋪），逕行統一會動街角幾何本體 ⇒ **禁順手併** |
| **GB-41** 🆕 | **`_corner_block_true_G` 之兩個呼叫端把「側街*路寬*」塞進 `F` 槽——而正典定義 `F ＝ 街角地第 1 筆面臨側面道路之*長度* ＝ SIDE_LINE 全長`**（逐字：`grep -n "^- \*\*F ＝" .claude/skills/g-formula-rules/SKILL.md`）。誤用之二處：`grep -n "F_left=float(_sbr_ap.get" app.py`（app 路徑·取 `左側路寬(m)`）與 `grep -n 'F_left=float(_SB_pc' verify/selection_pipeline.py`（harness 路徑·取 `路寬_m`）。**實配側則正確**：`grep -n "左側長度(m)" verify/stepg_pipeline.py app.py`（取 `左側長度(m)`／`右側長度(m)`）⇒ **同一 `F` 槽、兩路餵不同量**，而二者之 docstring 明寫「與實配第 1 宗**共用同一 solve 路徑**（Q-M4『禁另寫平行式』）」（`grep -n "共用同一 solve 路徑" app.py`） | **本案即觸發**（⛔ 非換案才觸發）。`Rw×F×l_side` 之側街負擔因 `F` 短 4 倍而**被系統性低估** ⇒ 資格閘所用之**真G 系統性高估**。實測 `3.5m R1/right 628(5)`：`F` **33.27 vs 8.00**、單變因 **ΔG ＝ +12.680000**，佔該格總落差 `+12.85` 之 **98.7%**；另 5 項（forced／buf／W_prev／S_max）ΔG **皆 0.000000**，錨點項 +0.70。全 toggle 閉合閘 G4 重現真G `240.57` ⇒ **成因已窮舉**。🔴 **土地後果**：該格真G 若依正典改正 ≈ **228.42**（＝單變因 (d) 之值）**< 門檻 232.79** ⇒ **資格閘不過** ⇒ 該街角改走**強制留設抵費地**；且 §二-1 之 `K-9-5-3` vs `K-9-5-2-a`-3 **互斥案將自然消失**（該格不再是「面積過、寬不足」） | ✅ **KL 裁 2026-08-08 放行修正·待 D-1 切換**（本批**仍未改任何一行**）。原記「🛑 停機上呈 KL」**已解除**。切換前之哨兵規格見 `docs/reports/W-G.8_D_期望影響表_凍存.md` §四（D-1 哨兵：資格閘之 `F` 實參／靶格真G `240.57 → 228.42`）。⚠️ 改之即動全部街角之 PK 資格與 winner 集合（**土地後果**）⇒ 切換須附**正對照哨兵**（`==` 反向式·零 diff 有兩種相反解釋） | 正典：`.claude/skills/g-formula-rules/SKILL.md`（`F` 之定義列）；實料：`verify/out/probe_K9_s62c_attribution.log`【E】；報告：`docs/reports/W-G.7_K6-A2-S6-2C_成因歸因與泛化.md` | ✅ **本案全部 12 個 winner 格皆走過**——真G − 實配G 恆正 `+11.51〜+22.96`，而 29 個非-winner 格落 `−5.48〜+6.95`（**零重疊**·空隙 4.56㎡） | ✅ **需**——改正之**土地後果**（winner 集合、強制抵費地側別）屬 KL；⛔ 惟「`F` 之定義」本身**正典已明訂、非待裁**（待裁者係*修正之時機與波次*） |
| **GB-42** 🆕 | **`E-1.7` 真交集地板 `1.0㎡` 之裁定失所**——該門檻係 **KL 裁定**，但其**唯一逐字出處為碼內註解**。碼面逐字（`grep -n "_inter_p1_area > 1.0" app.py` ⇒ `:11145`）：『🆕 W-D.1.3-b E-1.7 門檻 0.01→1.0㎡（KL 絕對地板；剔 sliver 如 628-40 之 0.33）』（`:11146` 為 p2 之同式）。**身分判定**：⛔ **非法規數字**（註解未引任何法源，`1.0㎡` 不見於畸零地使用規則或 104 手冊之引文）；⛔ **非純程式參數**（註解明載「**KL 絕對地板**」並記錄沿革 `0.01 → 1.0㎡` 與具體案由）⇒ **＝ KL 裁定之工程門檻（W-D.1.3-b）**。**失所之實據（正面列舉·⛔ 非全倉排除式）**：`grep -c "E-1.7" docs/rulings/K-6_街角地分配程序與可分配判準.md` ⇒ **0**；`grep -c "絕對地板" 同檔` ⇒ **0**；`grep -c "E-1.7" CLAUDE.md` ⇒ **0**；`grep -c "絕對地板" CLAUDE.md` ⇒ **0** | **本案即觸發**（⛔ 非換案才觸發）。該門檻**逐格決定候選是否進入 PK 池**（`_has_p1 = _inter_p1_area > 1.0`·**嚴格大於**）⇒ 「≤ 1.0」者**排除出候選池**，⛔ 與「未達 G 門檻」是**兩種不同的落敗方式**。🔴 **現況既有脆弱性（本批實測·`verify/out/probe_E17_intersection.log`【C】）**：`0m R5/left 628-7(2)` 之現行真交集 ＝ **`1.070652`㎡**，對地板之餘裕僅 **`0.070652`㎡**，**而它正是該格之 winner** ⇒ 該格之 winner 資格繫於一個 **7 公分見方**量級的餘裕。併記：D-② 新構造下**新增 3 宗出局**（`0m R2/left 628-40(1)` 8.928003→**0.000000**／`0m R3/right 628-28(1)` 13.786544→**0.001113**／`0m R5/left 628-7(2)` 1.070652→**0.000000**）——皆**幾乎歸零**、⛔ 非「略低於門檻」 | 🔴 **待 KL 追認**——**本批僅轉錄**，⛔ **未變更任何數值**、⛔ **未提任何調整建議**、⛔ **未改動 `app.py:11145` 之任何字元**。門檻之去留與位階（是否升格入正典）**屬 KL** | 碼面：`grep -n "_inter_p1_area > 1.0" app.py`；實料：`verify/out/probe_E17_intersection.log`（【B】三格量化／【C】132 候選列／【D】出處與身分）；報告：`docs/reports/W-G.8_D-0b_哨兵凍存與E17量化.md` §三 | ✅ **本案全 132 候選列皆走過**（兩態出局 94／兩態在池 35／舊在池→新出局 3／反向 0）；新構造下逼近門檻（`1.0 < x ≤ 2.0`）者 **1**：`3.5m R3/right 628-29(1)`（`1.5419`） | ✅ **需**——① 追認該門檻之位階（碼內註解 ⇒ 是否升格入 `docs/rulings/`）；② 其數值之去留。⛔ 二者**皆非技術題**，CC 不代裁 |

> **關於 KL 所引之「E-3 批次迭代未實作」**：交辦文以之為同級參照，惟**倉內以該名稱查無條目**
> （`rg -n '批次迭代|E-3.*未實作' .` ⇒ **扣除本檔自身後 0 命中**·於 `e45bbb2` 工作樹實跑）。
> 最接近之既有條目為 **GB-2／GB-4**。
> **不臆測、不代填**——出處待補；若係他處用語，請指出對應條目後併入。

---

## 一-B、**K-6-A2 硬前置·待 KL 域裁**（K-8 段四 probe 實測·2026-08-01）

> ⚠️ **本節與上表判準不同**：上表係「本案不觸發、換案才硬停」；
> 本節三項**本案即觸發**，係 **K-9-5／K-9-2 上閘（K-6-A2）之硬前置**。
> 實料：`verify/out/probe_ruling_K9_corner_width.log`
> （`python verify/probes/probe_ruling_K9_corner_width.py`·**只量不判·rc 恆 0**）。
> **本批只量不判、不代裁。**

| # | 未決項 | 實測 | 為何 CC 不能自行決定 |
|---|---|---|---|
| ~~**U-K9-1**~~ 🗄️ **已撤回（段七·2026-08-02）** | ~~街角第 1 宗與截角三角形之「點接合」⇒ 截角前幾何不良定義~~ | **撤回理由**：**K-9-8 之量測不需將截角三角形聯集回宗地**——虛擬還原塊係由「延伸線段＋二側境界線＋BASELINE」**直接圍成**，與「宗地 ∪ 截角三角形」無關 ⇒ 「宗地與截角三角形係點接合抑或邊接合」**對量測無影響**，該題**自始不成立** | 🔴 **記為施工單缺陷·非資料問題**：原交辦文令 CC 以「宗地 ∪ 三角形」重建截角前幾何，**係方法錯誤**。前兩批圍繞該點接合所做之全部量測（頂點距、局部框重測、假說否證）**結論仍為真，但與判準無關** | 裁定：`grep -n "^### K-9-8 " docs/rulings/K-6_街角地分配程序與可分配判準.md` | — | — |
| **U-K9-2** ✅✅ **已消滅**（KL 裁修圖 2026-08-02·已交付 `data/V6_1.dxf`·**待波末批換圖生效**·GB-16）。段六實測：`+0.014826m` → **`+0.000006m`**，且該角之截角接合由**點接合**轉為**邊接合**。〔以下原文存查〕 **`R4/right 628-1(1)` 連【截角後】寬度都量不出**——`parcel_min_width_n14` 之**臨接 FRONT 閘** loud raise：「距 FRONT 最近之頂點 **−0.014826m** ·\|偏移\| > 容差 0.01m」 | 兩情境皆然（該 −1.4826e-2m 係**倉內既載事實**：`grep -n "1.4826e-2" app.py`），惟**過去從未浮現**——K-4 豁免使街角宗**根本不走 N-14** | K-9-5 要求街角第 1 宗**實量** ⇒ 該宗上閘後**必停機**。屬「上游分配線不合規 vs 容差過嚴」之判斷，**碼面無從自決** |
| **U-K9-3** | **§零-3 末句之「蘊含」語與 K-9-5 不相容**——§零-3：「故『G ≥ 街角規定面積』**蘊含**街角第 1 宗寬度合格」；K-9-5：「街角第 1 宗**寬度要實量**」。若確為蘊含，實量恆屬多餘 | 八街角實測 ∠(SIDE_LINE, ALLOC) ＝ **0.000°〜4.322°**（正典 §五 實作附記）⇒ **非退化** ⇒ 蘊含關係**不可由退化性逕推** | **正典條文之效力問題**：§零-3 末句應 (甲) 隨 K-7 一併作廢，抑或 (乙) 保留為獨立幾何命題。**改正典判準表述逾 CC 之權** |

> 🔴 **本節原載之「上閘後之土地後果預告」已作廢（段五·2026-08-02）**
> ~~16 格中 8 格可量、1 格會翻盤——3.5m R1/right 628(5) 截角前寬 6.8470m < 門檻 7.00m。~~
> 該組數字以**畸零地附表之 `14.00`** 為量測帶深，**帶深定義錯誤**（K-9-6·見 GB-13）
> ⇒ **全部作廢、不得引用**。改寫後之實測見
> `verify/out/probe_ruling_K9_corner_width.log` 與正典 **K-9-7-c**。

**段五之補測結論（三項·皆只量不判）**：

- **U-K9-1 之假說已否證**：R1 左與 R4 右之「宗↔截角三角形」頂點距
  **絕對框／局部框皆 `0.000e+00`**（＝**頂點完全重合**）
  ⇒ **兩者皆為真點接合**，非「次微米級座標不合」。
- **U-K9-2 已溯源定讞**：`−1.4826e-2m` ＝ **CAD 圖面既有**
  （DXF BLOCK 圖層 entity handle **178** 之頂點本身即在 FRONT_LINE 外側），
  **非程式產生**。修圖 vs 改判準係 **KL 裁量**。
- **U-K9-3 之底料**：包含關係 `範圍 ⊆ 宗` **非恆真**——12 格中 **3 格不成立**
  （0m/3.5m R1 左各溢出 **5.325538㎡**、3.5m R1 右溢出 **5.067912㎡**）
  ⇒ 「`G ≥ 範圍面積` 蘊含寬度合格」**不可由包含關係逕推**。

⚠️ **U-K9-1／U-K9-2 之前提已隨帶深更正而改變 ⇒ 兩題暫緩、段五不代裁。**

---

### 🔚 U-K9 系列之現況（**段七·2026-08-02 總結**）

| # | 現況 |
|---|---|
| **U-K9-1** | 🗄️ **已撤回**——K-9-8 之量測不經「宗地 ∪ 截角三角形」⇒ 點接合與否與判準無關。**記為施工單方法缺陷** |
| **U-K9-2** | ✅✅ **已消滅**——KL 裁修圖、`V6_1.dxf` 已交付（待波末批換圖生效·GB-16） |
| **U-K9-3** | 🔴 **未撤回·停機上呈**（見下） |
| **U-K9-4** | 🗄️ **已撤回**——GB-15 歸因已更正（段六-2）＋K-9-8 已定義起算線 |

### 🔴 U-K9-3：**停機上呈**（段七·**探針不代解釋**）

段七交辦文令：「驗證該溢出多邊形是否確實落在截角區內。**若否 ⇒ 停機上呈，不要自行解釋。**」

**實測（`data/V6_1.dxf`·`verify/out/probe_ruling_K9_corner_width_V6_1.log`）：否。**

| 情境 | 街廓側 | 溢出(㎡) | 其中在**截角三角形內** | 落截角內比例 | 在街廓**內** | 在街廓**外** |
|---|---|---|---|---|---|---|
| 0m | R1/left | `5.325538` | `0.000000` | **0.00%** | `5.325538` | `0.000000` |
| 3.5m | R1/left | `5.325538` | `0.000000` | **0.00%** | `5.325538` | `0.000000` |
| 3.5m | R1/right | `5.067860` | `0.000000` | **0.00%** | `5.067860` | `0.000000` |

**位置（局部框 `(s,t)`·純量測）**：

| 情境·街廓側 | 溢出之 `s` 範圍 | 截角△之 `s` 範圍 | 溢出形心 `(s,t)` |
|---|---|---|---|
| 0m R1/left | `[0.260, 2.684]` | `[0.000, 3.668]` | `(1.699, −22.342)` |
| 3.5m R1/left | `[0.260, 9.684]` | `[0.000, 3.668]` | `(1.699, −22.342)` |
| 3.5m R1/right | `[80.313, 90.397]` | `[83.930, 87.631]` | `(81.730, −16.565)` |

> 🔄 **段八更新（2026-08-03）**
> ① 上表之 `s`／`t` 範圍**係前版跨片取 min/max 之產物、已作廢**——成因為
> `difference()` 之**零面積碎片**與**零寬尖刺**（`3.5m R1/left` 之片1 面積
> `0.000000000㎡`，卻把 s 上界拉到 `9.6838`）。逐片讀數見報告 §二／log【段八-1】。
> ② **「溢出＝截角還原角」假說已否證**（交集 `0.00%`）；其所附之
> **79.4%／87.8% 比例係數字巧合、勿再引用**（KL 2026-08-03 明示）。
> ③ **逐邊來源歸屬已完成**（51〜55 條候選線·逐條不合併）：**「來源不明」＝ 0 條**。
> 三個有面積之片之兩長邊分別歸屬於 **`宗邊#0`** 與
> **`街角範圍邊#0`／`街廓邊#2`**（δ ≤ `4.4e-11`）。
> 詳見 `docs/reports/W-G.5_K8-段八_溢出多邊形逐邊歸屬.md` §三。
> ④ claude.ai 另已排除二候選（**本批未重做**）：SIDE_LINE 與街廓側邊不共線
> （全區最大 `1.2mm`）／BASELINE 折點（楔形僅 `0.032㎡`·差 166 倍）。
> **U-K9-3 仍未決·仍不代裁。**

> 🔄 **段九更新（2026-08-03）·只出數字不下結論**
> ⑤ **楔形歸屬**：以溢出多邊形對同街廓**每一宗**＋抵費地各片求交集，
> **殘量 ＝ `0.000000㎡`（0.00%）**——三片各 **100%** 落在**單一**對象：
> `0m`／`3.5m R1/left` → **`R1-抵費地-2`**（抵費地片）；
> `3.5m R1/right` → **`628-34(3)`**（另一宗地）。
> ⑥ **守恆對帳**：六街廓殘差 `−0.043657` ～ `+0.025123㎡`（`G` 已 2dp ⇒ 含捨入量）；
> **R1 之殘差絕對值兩情境皆非全區最大**（最大為 R6 `0.043657`）。
> ⑦ **`宗邊#0` 產生點**：`stepg_pipeline.py:381`（轉抄）← `app.solve_G_binary`
> （`app.py:8867`）← `_block_strip`（`app.py:7783`）之
> `block_poly.intersection(strip)`，切帶側邊方向 `n_hat = rot90(allocation_dir)`
> ⇒ **屬「切割」段**，非落位／碎片歸併／幾何修復。
> 詳見 `docs/reports/W-G.5_K8-段九_楔形歸屬與守恆對帳.md`。

> ✅ **段十結案（KL 裁示 2026-08-03）：`R1/left` 已解釋·結案**
> **R1 左之溢出 ＝ 既載 S=0 碎片 `R1-抵費地-2`**。證據：`verify/out/wd3_fragment_geom.csv`
> 記 面積 `5.3`／寬⊥ALLOC `0.32`／深∥ALLOC `29.64`／臨正街長 `0.0`／`(a)S=0=是`／
> `(b)寬<最小=是`／三分類＝`碎片`——與段八實測 `5.325538㎡`／`0.3248m`／`29.6361m`
> **三項全中**（本批複驗）。
> 依 **KL 2026-07-06 碎片判準** ＋ **W-F F.1「R1 楔形等 G 吞納＋後續宗前移」**
> （`grep -n "R1 楔形等 G 吞納" verify/run_verification.py`）
> ⇒ **`範圍 ⊄ 宗` 為預期行為、非缺陷**。**登記後不再追。**
> 🗄️ **claude.ai 前批之甲／乙／丙 上呈題，對 `R1/left` 不成立、已撤回**（正典早有裁定）。
>
> 🔴 **唯一未結項：`3.5m R1/right`**（與左側**非同源**——R1 碎片表僅兩片、無第二塊 S=0 碎片；
> 且 `0m` 之 R1/right 溢出為 0、**僅 3.5m 出現**）。段十四項量測見
> `docs/reports/W-G.5_K8-段十_R1右未結項四項量測.md`：
> ① 右側落位序 0m vs 3.5m **不同**（`628-37(1)`／`628-35(2)` 由 left 轉 right、`628-36(1)` 反之）；
> ② `街角規定範圍(右)` 與 `628(5)` **六頂點中四個逐位相同**，差異僅 `v0`／`v1`、
> 兩端沿 `s` **同差 `0.1529m`**；`範圍−宗 = 5.067860㎡`、`宗−範圍 = 0`；
> ③ `628-34(3)` 係**業主宗**（`G=86.01`）、寬⊥ALLOC `2.5874m` < `3.50`
> ⇒ **命中 wd3 (b)**（(a)(c) 否）；**不在 `wd3_fragment_geom.csv` 母體內**（該檔只收抵費地片）；
> ④ 可改變落位序之碼段已逐條列出（退縮消費點／F.1 前移／forced 角落鎖定／S=0 遞補）
> ——**只列 `file:line`，不判斷成因**。

> ✅ **段十一結案（KL 裁示 2026-08-03）：U-K9-3 ＝「判準誤訂·非缺陷」**
> **claude.ai 之「包含關係」判準訂錯**——比的是**多邊形**，而程式閘比的是
> **`G` vs `226.88`**（純量門檻）。對稱差實測顯示形狀無異常
> （六頂點中四個逐位相同、僅一邊沿 `s` 平移 `0.1529m` 且兩端同量），
> 溢出係**面積差之算術後果**、**與 `628-34(3)` 無關**。
> 🔴 **記為 claude.ai 之施工單缺陷**（與 U-K9-1 同型：**方法錯了，量得再準也沒用**）。
>
> 🔎 **`226.88` vs `232.7931` 之來源已查明：非 live 之同名兩值，係「凍結 baseline vs live」。**
> ① live 鏈自始至終只有一個來源（`_build_corner_range_v3(...).area`
> → `run_verification.py:252/253` → `selection_pipeline.py:379/385/510/543`），
> 鏈上無第二算式；② **`③ live PK` − `② live 參數表` ＝ `+0.0000`，十六格全中**；
> ③ `226.88` 係凍結 baseline CSV 之內容，該檔最後異動於 `1b290e4`（2026-07-05），
> **早於 K-8 §三〜§五 落地（`56b345a`·2026-07-31）26 日**（`git merge-base --is-ancestor` 已驗）；
> ④ **`② − ①` 十六格全部非零**（`−0.99` ～ `+78.31`），**R1/right 之 `+5.91` 屬最小之列**。
> ⑤ **四個 `_build_corner_range_v3` 呼叫點皆傳入 `_make_chamfer_tri_wb(...)`、
> 無一走 `chamfer_tri=None` 預設**（`app.py:9239` 之 `None` 僅簽名預設值）。
> ⇒ **交辦文所設想之「兩路傳入不同」不成立 ⇒ 未開 GB 條目**；
> `① vs ②` 之落差屬**凍結 baseline 未重烤**，倉內已登記於 `CLAUDE.md`「46 紅」
> 與 **GB-16**（波末批同一次重烤），**不另立新條目**。
> 詳見 `docs/reports/W-G.5_K8-段十一_最小面積三路對照.md`。

⇒ 溢出**全部在街廓內**、形心深達 `t ≈ −22m`／`−16m`，
而截角三角形僅及 `t ∈ [0, −3.7]` 附近。**⛔ 本批不解釋、不代裁。**

⚠️ 附記（**僅為對帳·非解釋**）：交辦文所給之還原三角形面積為 `6.709961`／`5.771140㎡`；
本批自 `V6_1.dxf` 自算為 **`6.710438`／`5.771148㎡`**（差 `4.8e-4`／`8e-6`）。
溢出佔截角△之比為 **79.4%／87.8%**（與交辦文相符），
**惟該比值係面積巧合——交集面積為 `0`。**

> ✅ **U-K9-2 已於 2026-08-02 由 KL 裁定＝修圖**（`data/V6_1.dxf`）。
> 段六平行對照實測：該角之 `+0.014826m` → **`+0.000006m`**、截角接合由**點接合**轉**邊接合**。**待波末批換圖方生效**（GB-16）。
> 🔴 **U-K9-1 未隨之解決**：`R1/left` 之點接合於新圖**原樣存在**（前緣偏移 `−0.166151m` → `−0.166141m`）⇒ **R1 左與 R4 右非同源**。

---

## 二、真資料覆蓋之誠實登記（**禁寫成「已驗過」**）

| 機制 | 紅向（會不會咬） | 正向（真資料是否走過） |
|---|---|---|
| **F-10-3**（M-5 三列可證偽） | ✅ 由 `WV_BITE=1` 注入偽 forced 端證其會紅（`M_0725_F10_bite.log`·3 列全紅） | ❌ **從未被真資料走過**——0m 無 forced 端（＝**空真綠**）、3.5m 走 award 分支。正向僅由 claude.ai grep `m_rescue` 之「維持強制抵費地」log 格式**坐實格式相符**，**非**實跑覆蓋 |
| **F-10-1 (A′)** | ✅ 由 `WV_BITE=1` 注入 `_BITE_A0ONLY_` 證其會紅 | ❌ 本案無「原 a==0 且自 parcels₁ 消失」之宗 |
| **§4 無勝者 fallback** | ✅ `fixture_end_fallback.py` 左右各 5 項 | ❌ latent（69 個 bake 檔全掃「抵費地末」命中 0） |
| **末端保留窗** | ✅ `fixture_end_reserve.py` 12/12 | ✅ R6 左·21/21 觸發（惟係 **BAKE 致能之條件式讀數**·待 P-H 後非 BAKE 重現） |

---

## 三、維護

- 新增阻塞項時：**先給倉內出處與觸發條件**，再入表；無出處者不得入表。
- 解除時：於該列註記解除 commit，**不刪列**（保留歷史）。
- 本表由各波報告引用，**不重述數字**——數字一律引 log 檔名。
