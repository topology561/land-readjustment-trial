# `W-G.9-8`：引用錨去行號化 ＋ 三支端到端複本掛入（**事前登記**·⛔ 零量測·零碼變更）

**產出**：CC／2026-08-11　**分支**：`wip/s1-endpart`（本地支 `wg98-anchors`）
**基準**：`dc619a4`

> 🔴 **本批最大之風險（施工單 §8-1）**：三支若以「**跑不跑得通**」為判準掛入，
> 就是**三個新的永紅名目** ＝ 把本波剛修好的病**再造一次**。
> ⇒ 判準逐字採**考古 65 第四層**：**⛔ 不可寫成「該功能是否成功」，須寫成受詞本身之命題**。

---

## §0 現查（⛔ 指令與輸出並列·**皆本登記落筆當次重跑**·⛔ 僅倉內事實）

```
$ git rev-parse --short HEAD
dc619a4

$ grep -rn "W-G.9-8" . --exclude-dir=worktrees --exclude-dir=.git | wc -l
0

$ git diff -U0 ad56351 dc619a4 -- CLAUDE.md | grep "^@@"
@@ -75,0 +76,11 @@ …

$ grep -rnoE 'CLAUDE\.md:[0-9]+' --include=*.md --include=*.py . --exclude-dir=.git | wc -l
32

$ for f in wg_g1_smoke wg_g2_smoke wg_g3; …
  wg_g1_smoke: run_all=0 run_verification=0
  wg_g2_smoke: run_all=0 run_verification=0
  wg_g3:       run_all=0 run_verification=0

$ grep -c "未顯示" verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt
6

$ grep -rn "未顯示" --include=*.py verify/
verify/run_verification.py:1451:            print(f"        …（**另 {len(viol) - 12} 列未顯示**·本閘違規總計 {len(viol)} 列）")
```

⇒ 施工單 §0 之七項**經本機重跑皆相符**；⛔ 未發現任何無法於倉內查證之項（§8-4 未觸發）。

### 0-1 三支之呼叫結構（供 §2 之預測）

```
$ grep -n "run_step_g(" verify/wg_g1_smoke.py verify/wg_g2_smoke.py verify/wg_g3.py
verify/wg_g1_smoke.py:58    verify/wg_g2_smoke.py:37    verify/wg_g3.py:83
```

⇒ **三支皆於其 `main()` 早段呼叫 `run_step_g`**（且皆餵 harvest 全 ns）。

---

## §1 `P′-1`：受行號位移影響之**逐項清單**（⛔ 由程式原文逐項讀出·非 grep 上下文窗）

**【目錄射程】** 全倉（⛔ 排除 `.git`／`worktrees`／`__pycache__`）·**⛔ 不限副檔名**
（施工單 §0 之式限 `*.md`／`*.py`，本登記**放寬**以求完整）。
**【字樣射程】** 正則 `CLAUDE\.md:(\d+)`。
**【連接方式】** 逐行 `finditer`（⛔ 無上下文窗·考古 67 修法 ③）。

```
（母體＝本登記檔**寫出之前**之倉態）
引用總數 = 39｜N<=75（未受影響）= 24｜**N>=76（已失準）= 15**
未受影響者之分布：CLAUDE.md:4（3 處）／:56（9 處）／:71（11 處）／:75（1 處）
```

⇒ **`P′-1` 前半成立**（受影響者恰為 `N ≥ 76`；`N ≤ 75` 者未受影響）。

🔒 **「母體隨自身文件增長」之具名**（⛔ 出艙前現查所得）：本登記檔**自身**列出上開錨，
故其寫出後同一式得 **52／27**。多出之 13 處**全部**位於 `docs/reports/W-G.9-8_前置登記.md`
——係**對錨之提及**（清單），**非指向 `CLAUDE.md` 之活指標**。
⇒ 本節之數一律以**本登記檔寫出前**之倉態為準，⛔ 二數皆須連射程與時點一起走。

🔴 **掃描器 v1 之缺陷（自誌·⛔ 不掩蓋）**：首版以 `except Exception: continue`
**靜默跳過** decode 失敗之檔 ⇒ 母體被悄悄縮小（**考古 67 同族·且是我自己剛立的節**）。
v2 已改為**逐檔具名並改以 `latin-1` 納入**。
**實測其對本案之影響 ＝ 0**：被特殊處理之 **19 檔**（`.dxf`／`.xlsx`／`.png`／`.jpg`）
**無一含該字樣**。⇒ 缺陷為真、後果為零，**二者分開記**。

### 1-1 已失準之 15 處逐項（分類）

| 類 | 檔:行 | → 錨 | 處置 |
|---|---|---|---|
| 🔴 **活文件·交接文** | `docs/reports/W-G.9波_新session交接文_WG91-94.md:129` | `CLAUDE.md:328` | 🔧 **加註字樣錨**（施工單 §2-1 之優先處置） |
| **活文件·skill** | `.claude/skills/failure-archaeology/SKILL.md:1072` | `CLAUDE.md:207` | 🔧 **加註**（見 1-2） |
| **活文件·skill** | `.claude/skills/failure-archaeology/SKILL.md:1075` | `CLAUDE.md:207` | 🔧 **加註**（見 1-2） |
| ⛔ 批次報告 | `docs/reports/W-G.7_K6-A2-GEO2_W1口徑更正.md:143`／`:146` | `CLAUDE.md:207` ×2 | ⛔ **不動** |
| ⛔ 批次報告 | `docs/reports/W-G.8_D-2b-21_T2接線前偵察_靶與範圍互斥.md:15` | `CLAUDE.md:85` | ⛔ **不動** |
| ⛔ 批次報告 | `docs/reports/W-G.8_D-2b-22_池側殘差逐界面全額歸因.md:29` | `CLAUDE.md:85` | ⛔ **不動** |
| ⛔ 批次報告 | `docs/reports/W-G.9-2_定式批報告.md:49` | `CLAUDE.md:328` | ⛔ **不動** |
| ⛔ 批次報告 | `docs/reports/W-G.9-7_對帳準則升級.md:164` | `CLAUDE.md:207` | ⛔ **不動**（且其為**提及**該錨之分布、非使用該錨） |
| ⛔ 凍存 log | `verify/out/probe_WG93_mp_definition_scope.log:29/31/34/35/36/37` | `:291/:328/:290/:292/:327/:329` | ⛔ **不動**（6 處） |

⇒ **需加註者 ＝ 3 處**（1 交接文 ＋ 2 skill·後二者屬同一課節區塊）。
⇒ **⛔ 不動者 ＝ 12 處**（批次報告 6 ＋ 凍存 log 6）——改歷史記錄等於**事後修史**。

### 1-2 🔴 三處活文件之**現查判定**（⛔ 不憑施工單斷言）

| # | 現查 | 判 |
|---|---|---|
| ① 交接文`:129` | 其指 `CLAUDE.md:328`＝「`MP` ＝ SIDE 線之中點」之**權威序第 2 級**出處。該內容現落於 **`CLAUDE.md:339`**（＝328＋11） | 🔴 **確已失準**·且為**活指標**（下一 session 之入口）⇒ **須加註** |
| ② skill`:1072` | 「補丁一（KL 稱其漏 grep `CLAUDE.md:207`）… ⚠️ **行號指不到**」——**提及**一個歷史錨 | 屬課節敘述；⚠️ 惟其**舉例本身**已再度漂移（見 ③）⇒ 併 ③ 一併加註 |
| ③ skill`:1075-1078` | 課節逐字稱「該行**現為 CAD 圖層表之一列**（`\| BASELINE \| 每街廓必畫 \| …`）」——**現查該敘述已不成立**：`CLAUDE.md:207` 現為「**判準（給施工單作者）**：交付物與指示**同在一個 block**；」 | 🔴 **課節自身之舉例已再度漂移** ⇒ **須加註**（此加註本身即該課之再次示範） |

### 1-3 字樣錨之候選與其唯一性（⛔ 附判別力自檢·見 §5 `J5`）

```
$ grep -c "SIDE_LINE 中點到該筆靠街角側地界線的垂距" CLAUDE.md   ⇒ 1
$ grep -c "^| BASELINE |" CLAUDE.md                              ⇒ 1
```

---

## §2 `P′-2` 之**預測**（⛔ 標明為預測·凍存值由 commit B 之實測產生）

🔒 **本節係預測、非量測**：`P′-2` 之驗證須實跑三支，屬**量測** ⇒ 依施工單 §5 須**在本登記之後**。
故此處寫死**預測**，以供事後對照；⛔ 事後不得回頭修改本節。

**預測之依據（倉內事實）**：三支皆於 `main()` 早段呼叫 `run_step_g` 且餵 harvest 全 ns
（§0-1）；而 `run_step_g` 於**兩情境皆** raise（`W-G.9-6`／`W-G.9-7` 之
`v3·StepG0m`／`3.5m`「結構閘/看守觸發」為既有 FAIL）。

| 項 | 預測 |
|---|---|
| 三支之終止型別 | `RuntimeError` |
| 終止點 | `stepg_pipeline.py` 之**結構閘** raise（`理論＝實跑 破`） |
| 終止訊息 | `🔴 結構閘 理論＝實跑 破：街廓 R1 left 側 理論@k*=97.18 ≠ 實跑(階段1)=100.02（Δ=2.837 >0.1）…`（`0m`） |
| 三支是否同一處 | **是** |

⚠️ **若實測與本預測不符**：只要**三支彼此相同**，`J1` 仍成立；預測之誤差**照實寫入報告**，
⛔ 不得回頭改本節。**唯有三支彼此不同**（或任一支 `rc=0`）才觸 `V-1`。

---

## §3 新夾具之**判準定式**（🔒 ⛔ 事後不得放寬）

> **受詞 ＝「三支之終止點 ＋ 終止原因，與凍存者相符」**，⛔ **非**「三支是否跑通」。

- **凍存內容**：三支之**終止記錄全文**（正規化後），⛔ 非雜湊、⛔ 非 `rc`。
- **正規化與分類**：**沿用** `verify/wv_reconcile.py` 之 `R1`／`R2` 與**三分類**
  （`normalize` ／ `classify` ／ `report`）——⛔ **不另寫第二套**（`#20` 族）。
  儲存格式亦沿用其 `render_list`／`parse_list`，⛔ 不新增任何解析碼。
  🔒 若其介面不敷用 ⇒ **停機上呈**（`V-4`），⛔ 不得就地複製一份改。
- **設計意圖（須寫入碼註）**：另案修好後三支會**跑得更遠** ⇒ 終止點改變 ⇒ 夾具**轉紅**
  ⇒ **逼人來看並更新凍存**。**這是特性，不是缺陷。**
- **`J2` 之竄改受詞**：**終止記錄本身**——現查其**必被執行**（三支皆終止於此，
  該記錄即夾具所擷取之物）⇒ ⛔ 非「不保證執行到之點」（考古 65 第五層）。

---

## §4 可證偽預測（⛔ 事後不得回頭修改）

| # | 預測 | 證偽方式 |
|---|---|---|
| **J1** | 三支複本**終止於同一處**，其終止原因逐字相同（正規化後） | 任一支不同 ⇒ **J1 破 ⇒ `V-1`** |
| **J2** | 新夾具**具鑑別力**：人造「終止點提前」之情境 ⇒ 必轉紅；還原 ⇒ 轉綠 | 竄改後仍綠 ⇒ **J2 破**，該夾具不得計入交付 |
| **J3** | 掛入後 `run_all` 判定層與 `WG97_runall.log` 相同，**唯一容許之差 ＝ 新夾具之列** | 出現其他判定改變 ⇒ **J3 破 ⇒ `V-3`** |
| **J4** | `W-G.9-7` 之對帳於本批仍 **PASS**（類 II／III 皆空） | 非空 ⇒ **J4 破**（表示本批動到了不該動的） |
| **J5** | 每一新設之字樣錨，其 grep 命中**唯一** | 任一錨多命中或零命中 ⇒ **J5 破 ⇒ `V-2`** |

---

## §5 停機條件（⛔ 逐字採施工單 §4）

**🛑 真域邊界 ⇒ 停機上呈 KL**：本批**無**預期之域邊界。若觸及，以
【現況】→【要改成】→【對土地的影響】→【要你判斷】格式上呈，
⛔ 一句是非題只綁一個命題、⛔ 禁程式術語、⛔ 一律正體中文土地用語。

**🛑 立即停機（紅線）**：

- **`V-1`**：`P′-2` 為偽（任一支 `rc=0`，或三支終止點不同）⇒ 停機，設計前提錯誤。
- **`V-2`**：任一活文件之引用**做不到唯一命中之字樣錨** ⇒ 停機上呈，⛔ 不得退回用行號。
- **`V-3`**：掛入後 `run_verification.results` 之 PASS/FAIL 層改變 ⇒ 停機。
- **`V-4`**：`wv_reconcile` 之介面不敷用而需另寫正規化 ⇒ 停機上呈。

---

## §6 驗收（⛔ 逐字採施工單 §6）

| # | 閘 |
|---|---|
| 1 | 三支之凍存含**終止原因全文**（沿用 `R1`／`R2`），⛔ 非雜湊、⛔ 非 `rc` |
| 2 | `J2` 之竄改自檢**兩態皆印**；受詞以**現查證明必被執行** |
| 3 | 每一新字樣錨附**其 grep 指令與輸出**，且附唯一性之**判別力自檢**（示範一個會多命中之字樣） |
| 4 | 受位移之逐項清單**由程式原文逐項讀出**，⛔ 非 `grep -A/-B/-C` 之窗 |
| 5 | 截斷盲點之聲明含**現算之量**與**緩解**，並就地點明與考古 67 之關聯 |
| 6 | 「已掛入」之證據 ＝ **本批 log 內之該列本身**，⛔ 非敘述 |
| 7 | 凍存 log／凍存名單／批次報告之行號錨**確未被動**（附 `git diff --numstat` 為證） |
| 8 | 每一出艙之數，其指令於**落筆當次重跑**且指令與輸出並列 |
| 9 | 雜湊一律 `git hash-object`；`git diff --numstat` 對 `app.py`／`verify/run_verification.py` 皆**空輸出**；文件刪除欄 **0** |
| 10 | 凡書寫「正典」二字，就地標明**權威序層級**（本倉共 **4 級**） |

---

## §7 已預先承諾之判準（⛔ 事後不得放寬）

- ⛔ **零生產碼**：不動 `app.py`。
- ⛔ 不修結構閘另案（`理論＝實跑`）、不修 `W-G G.1` 之 `KeyError: '0m'`。
- ⛔ **不改 `verify/run_verification.py`**（含 `:1451` 之 12 列上限）。
- ⛔ 不改三支複本原檔之判準或命題（其「全鏈跑通」之原始命題**保留**，
  作為結構閘另案修好後之終驗）。
- ⛔ 不動任何凍存 log／凍存名單／批次報告內之行號錨。
- ⛔ 不另寫第二套正規化規則。
- ⛔ 不立 `G3` 三項之閘。
- ⛔ 文件與凍存物之**刪除欄一律 0**。
- ⛔ 長跑期間不動 `app.py` 與 `verify/`。
- 否定性宣稱須同附【目錄射程】【字樣射程】【連接方式】。
- 🔴 **`rc=1` 不構成「命題為偽」**（考古 64）。

🔒 **自誌（⛔ 逐條自檢·施工單 §8）**

1. **考古 65 第四層之首次實戰**：本批判準已定式為**受詞本身之命題**
   （「終止點＋終止原因與凍存相符」），⛔ **非**「三支是否跑通」——見 §3。
   ⇒ 另案未修時夾具**綠**；另案修好後三支跑更遠 ⇒ 終止點變 ⇒ **轉紅逼人更新**。
2. **考古 67 之機械修法逐條套用**：
   ① **比較前先斷言兩端存在且非空**——`scan_anchors` 已含 `assert rows`；
      新夾具亦須含「凍存不存在／解析出 0 筆 ⇒ 判紅」。
   ② **每一個「0 命中」附判別力自檢**——§1-3 之唯一性檢查將附「示範一個會多命中之字樣」。
   ③ **⛔ 不以 grep 上下文窗抽清單**——§1 之 15 處清單以 `finditer` 逐行讀出。
   ④ **建檔後立刻驗其存在**——凍存檔產生後即 `ls` ＋ 反讀驗。
3. **計數型宣稱皆附判別力自檢**（§8-3）：§1 之「15 處」、§1-3 之「唯一命中」、
   §2-3 之截斷量，⛔ 皆不得逕以數字出艙。
4. ⚠️ **「14 處」與「15 處」之差**（`W-G.9-7` 報告 §8 vs 本登記）——
   🔴 **成因已現查、⛔ 非射程不同**：第 15 處 ＝ `docs/reports/W-G.9-7_對帳準則升級.md:164`
   ——**該報告自己**在 §8 列出受影響之錨分布，而我算「14」時該報告**尚未寫出**。
   ⇒ **母體隨自己寫的文件增長**；⛔ 兩個數皆須連**時點**一起走，⛔ 不得只報數。
   ⚠️ 我**首版曾把成因誤寫為「射程不同」**（限 `*.md`／`*.py` vs 不限副檔名），
   經現查推翻——**⛔ 不憑「聽起來合理」出艙**。
