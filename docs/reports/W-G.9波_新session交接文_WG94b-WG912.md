# W-G.9 波 新 session 交接文（`W-G.9-4b` 〜 `W-G.9-12`）

**產出**：CC／2026-08-12　**分支**：`wip/s1-endpart`
⚠️ **HEAD ⛔ 以現查為準**：`git rev-parse --short HEAD`（本文撰寫當時為 `735d93a`）

> # 🔴 一句話：**這 9 批動了 8 行生產碼，其餘全部在修「驗證系統自己看不見東西」這件事。**
>
> `git diff --numstat 1041874 HEAD -- app.py` ⇒ `8 0`
> ——唯一那次是 `W-G.9-4b` 之接線修復（把一個名字補進 `_WF_NS_NAMES`）。
> 其餘八批**零生產碼**。
>
> ⛔ **不要**把本波當成「做了很多驗證工具」——本波之產出是**一串已證實的盲點**，
> 工具只是把盲點釘住的釘子。**盲點清單在 §二，那才是交接的主體。**

---

## §〇 開工清單（⛔ 照順序·任一道對不上就先查，不要施工）

```bash
git fetch origin wip/s1-endpart && git checkout -B <本地支> origin/wip/s1-endpart
git rev-parse --short HEAD
```

⚠️ 若主倉已檢出 `wip/s1-endpart` ⇒ 用 `git checkout -B <本地支> <sha>`（⛔ 非 `reset --hard`），
推送用 `git push origin HEAD:wip/s1-endpart`。

**必讀（照順序）**：

| # | 讀什麼 | 為何 |
|---|---|---|
| 1 | 本文 **§二（待辦與待裁）→ §三（已作廢陳述）→ §七（你一定會踩的那一個）** | 主體 |
| 2 | `CLAUDE.md` 之**權威序四級**（`grep -n "權威序（上位優先）" CLAUDE.md`） | 凡書「正典」須標層級 |
| 3 | `docs/驗證裁定登記表.md`（`grep -rn "【驗證裁定登記表】" docs/`） | 施工單方之裁定**現在有家了**；⛔ 未入倉者視同未裁 |
| 4 | 失敗考古**末六節**（`grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md \| tail -6`） | 本波新增之戒全在那裡 |

**現況自檢（⛔ 數字皆現查·本文不寫死）**：

```bash
grep -c '✅ PASS' verify/out/WG912_runall.log      # 最近一次長跑之 PASS
grep -c '🔴 FAIL' verify/out/WG912_runall.log      # 同上之 FAIL
grep -c "^🔴 FAIL" verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt   # 期望 FAIL 名目數
grep -c "對帳 PASS" verify/out/WG912_runall.log    # 對帳是否綠
grep -oE '\("fixture_[a-z0-9_]+\.py"' verify/run_all.py | wc -l       # 末端夾具支數
```

---

## §一 本波做了什麼（一覽）

| 批 | 交付 | 報告（字樣錨即檔名） |
|---|---|---|
| `W-G.9-4b` | 接線修復（`_WF_NS_NAMES` 補名）＋ 接線守護夾具 | `W-G.9-4b_接線修復報告.md` |
| `W-G.9-6` | **期望 FAIL 名單之守備盤點**（行覆蓋量測） | `W-G.9-6_期望FAIL名單守備盤點.md` |
| `W-G.9-7` | **對帳準則升級**為「名目 ＋ 失敗原因」＋ 自動對帳 | `W-G.9-7_對帳準則升級.md` |
| `W-G.9-8` | 引用錨去行號化 ＋ 三支端到端複本掛入 | `W-G.9-8_錨去行號化與端到端複本掛入.md` |
| `W-G.9-9` | 凍存受詞**去呈現層**（投影）＋ 倉規二則升格 | `W-G.9-9_錨去行號化之倉規升格.md` |
| `W-G.9-10` | `G3` 三項守備拆解 ＋ 三閘 | `W-G.9-10_G3守備拆解與立閘.md` |
| `W-G.9-11` | **22 名目全拆解** ＋ 16 檔基準溯源 | `W-G.9-11_22名目全拆解與基準溯源.md` |
| `W-G.9-12` | **裁定登記機制** ＋ 中間層實跑 ＋ `F.2`/`F.4` 立閘 | `W-G.9-12_裁定登記與F2F4立閘.md` |

⚠️ 每批皆有 `*_前置登記.md`（事前登記·commit A）——**看結論前先看它的預測**，
因為報告之價值取決於「預測是不是事後才寫的」。

---

## §二 🔴 待辦與待裁（**本文之主體**）

### 2-1 最大阻塞（⛔ 非本波授權範圍·一修即解一大片）

| 項 | 現況（現查指令） | 影響 |
|---|---|---|
| **結構閘另案**「理論＝實跑 破」 | `grep -o "\[StepG0m\][^｜]*" verify/out/WG912_runall.log \| head -1` | `run_step_g` 於**兩情境皆** raise ⇒ **七級調配全鏈不可完成**；22 名目中 **65+ 個主判定**因此永不執行 |
| `W-G G.1` 之 `KeyError: '0m'` | `grep -o "\[G.1\][^ ]*" verify/out/WG912_runall.log` | 與上同源（`_ctx` 取不到 `"0m"`）；使 **W-8 反向閘**寄生失效 |

🔒 **修好之後會發生什麼（⛔ 屆時不要當成 regression）**：
① 三支端到端複本會**跑得更遠** ⇒ `fixture_e2e_termination` 依**設計**轉紅 ⇒ 逼人更新凍存；
② `W-G.9-11` 之 16 檔基準**全部**是「過期為活假說」⇒ 差異會大量出現，
**須先判「基準過期」還是「算錯」**——判準已預登於 `W-G.9-11` 報告，
⛔ **CC 不自行判定是否重烤，須 KL 裁**。

### 2-2 待裁（⛔ 非 CC 可自裁）

| # | 待裁項 | 出處字樣錨 |
|---|---|---|
| 1 | **解阻後「基準過期 vs 算錯」之判定**（含是否重烤） | `grep -rn "過期 vs 算錯" docs/reports/` |
| 2 | **中間層 6 項是否立閘、何時立** | `grep -rn "VR-002" docs/` |
| 3 | `W-G` **收官判準三條件**之②③（GIS 點選 bug／KL 實跑） | `grep -n "W-G 收官判準" CLAUDE.md` |

### 2-3 待辦（純技術·CC 可做）

| 序 | 項 | 備註 |
|---|---|---|
| 1 | **中間層 6 項之立閘** | 【裁定 B】已生效。⚠️ **本波只實跑了 1 項**（主樣本），**其餘 5 項未實跑** ⇒ 立閘前宜先補跑 |
| 2 | 查明 `_w4` 來源，歸類 `W-D.4 MinA_區 reverse-test` | `W-G.9-11` 之**唯一未定項** |
| 3 | **洞 A 之通則處置** | 逐項登記「原守何命題／現由誰守」；無人接手者另立**不受上游影響**之閘 |
| 4 | **解除明細截斷（12 列上限）** | ⚠️ **必使現行名單全面失效、須重凍** ⇒ 與重烤時點協調 |
| 5 | **恆真性之量測** | 行覆蓋射程外；需變異注入類工具 |

---

## §三 🔴 已作廢／已更正之陳述（⛔ 不得引用）

| 出處 | 作廢範圍 | 現行代替 |
|---|---|---|
| `W-G.9-4_expand報告.md` 之 `E3`「✅ 成立」 | 「本批生產行為零變化」 | **撤回**——app 路徑當時必 `KeyError`（該報告已加註） |
| 同上之「**整份 log 逐字相同**」 | 措辭 | 更正為「**正規化後**逐字相同」 |
| `W-G.9-4b` §9-3 之「**唯一**複本」 | 端到端複本之數 | **三支**（`wg_g1_smoke`／`wg_g2_smoke`／`wg_g3`） |
| `W-G.9-9` `K5`「字面未達成 ⇒ 停機上呈」 | 判準形式 | **已裁**：改差量式「未新增任何失準」⇒ `K5` 成立（`grep -rn "VR-001" docs/`） |
| `W-G.9-11` 之「中間層待裁」 | 判準擴充 | **已裁且已生效**（`grep -rn "VR-002" docs/`） |
| **舊四份期望 FAIL 名單** | 驗收基準 | 現行 ＝ `verify/out/K6A2_期望FAIL名單_WG97_名目加原因.txt`（**名目 ＋ 原因**）。⛔ 舊四份**一份未刪**、僅不再為基準 |

---

## §四 生產碼與驗證基礎建設現況

### 4-1 生產碼

```bash
git diff --numstat 1041874 HEAD -- app.py        # ⇒ 8 0（唯一一次·W-G.9-4b 接線修復）
```

⚠️ **`app.py` 之七級調配仍不可完成**——接線修好了，但撞 §2-1 之結構閘另案。

### 4-2 驗證基礎建設（本波新增·⛔ 皆非生產碼）

| 類 | 檔 | 用途 |
|---|---|---|
| **對帳器** | `verify/wv_reconcile.py` | 名目 ＋ 失敗原因、三分類（I/II/III）、`R1`/`R2` 正規化。**於每次 `run_all` 自動執行** |
| **夾具** | `verify/fixture_wf_ns_wiring.py` | app 接線清單 ↔ 引擎 `ns[]` 消費 |
| **夾具** | `verify/fixture_e2e_termination.py` | 三支端到端複本之**終止點**（受詞已投影去呈現層） |
| **夾具** | `verify/fixture_g3_static_guards.py` | `F.2`/`F.3`/`F.4 靜態閘` ＋ `F.3 跨區段 fixture` ＋ `G.2 只寫不讀` |
| **工具** | `verify/tools/wg911_all_dissect.py` | 22 名目全拆解＋分類（⚠️ **自宣「不得採信」·僅作候選**） |
| **工具** | `verify/tools/wg911_baseline_trace.py` | 16 檔基準溯源 |
| **工具** | `verify/tools/wg99_anchor_audit.py` | `CLAUDE.md` 引用錨稽核 |
| **工具** | `verify/tools/wg96_cov_runall.py` | 行覆蓋（stdlib `sys.monitoring`） |
| **登記** | `docs/驗證裁定登記表.md` | 驗證紀律層裁定（`VR-001`〜`VR-003`） |

---

## §五 本波新立之戒（⛔ 讀這幾條，勝過讀八份報告）

### 5-1 失敗考古（末六節·⛔ 節號以現查為準）

```bash
grep -n "^## [0-9]" .claude/skills/failure-archaeology/SKILL.md | tail -6
```

| 節 | 一句話 |
|---|---|
| 65 | 接了供給端不等於接完了——「我只給 X 用」是**用途推定**，不是呼叫端現查 |
| 66 | 「有閘」與「在守」之落差是**可量的** |
| 67 | 比較器之一端**不存在／為空**時，它仍會給你一個看起來有意義的答案 |
| 68 | **工具回報「成功」而檔案未變**——「已完成」與「已落地」是兩件事 |
| 69 | **壞掉的檢查器，吐出的正好是你期望的那個值** |
| 70 | 一個 `try` 把「要管線的」與「不要管線的」判定綁在一起——上游一崩，連不需要它的也陪葬 |

### 5-2 倉規（已入 `CLAUDE.md`）

- **行號衛生第 4／5 款**：活文件之引用 ⛔ 不得以行號為錨；判準為「**未新增失準**」⛔ 非「歸零」。
  （`grep -n "本規只能止血、不能回溯" CLAUDE.md`）
- **⛔ 不可變文件不得記載可變檔之雜湊**；雜湊記於**該檔檔頭之本體雜湊**。
  （`grep -n "⛔ 不可變文件不得記載可變檔之雜湊" CLAUDE.md`）

### 5-3 🔒 三條機械動作（本波每一批都靠它們抓到自己的錯）

1. **凡期望值為 0／空／順利之檢查，一律附判別力自檢**——⛔ 不只證它現在成立，**證它會不成立**。
2. **每次寫入後，以獨立指令驗其落地**——⛔ 不以工具回傳訊息代替。
3. **任何掃描器須母體自證＋印出母體規模**；工具換位置／擴充後，**與舊輸出逐字對拍**。

---

## §六 🔴 你一定會踩的那一個

> **這個倉裡，「看起來一樣」和「驗過一樣」之間，本波已經翻船五次。**

具體型態（皆真實發生於本波）：

| 型 | 實例 |
|---|---|
| **看起來同型 ⇒ 照抄** | `F.2`/`F.3`/`F.4 靜態閘` 看似同構，**`F.2` 之碼面多繞一個中介變數** ⇒ 照 `F.3` 外推之抽取式**抽不到** |
| **工具說成功 ⇒ 相信它** | `Edit` 回報 updated successfully 而**檔案一個位元組沒變**；我自己的修補腳本也發生過**整段未套用** |
| **母體壞掉 ⇒ 剛好給出期望值** | 稽核器少取一層 `dirname` ⇒ 母體縮成子樹 ⇒ 輸出「**失準 0**」＝ 正好是期望值 |
| **比較器一端缺席 ⇒ 假警報／假綠** | 對不存在的備份檔 `diff` ⇒ 「全部新增」；空樣式集 ⇒ 「0 命中」 |
| **數字未驗即出艙** | 報告草稿寫下一個沒跑過的 `grep -c` 值；分類器一度把**實需管線**之項判為「可回收」（**危險方向**） |

✅ **正確反應**：任何結論落筆前，問一句
**「如果我錯了，這裡會不會變紅？」**——答不出具體構造，就當作**尚未檢查**。

---

## §七 不授權（承本波各單之未授權清單·⛔ 逐項）

1. ⛔ **不動 `app.py`**（除非新施工單明文授權）。
2. ⛔ **不修**結構閘另案（`理論＝實跑`）與 `W-G G.1` 之 `KeyError: '0m'`——屬另案。
3. ⛔ **不重烤任何基準**、⛔ 不重凍任何凍存物、⛔ 不改期望 FAIL 名單。
4. ⛔ **不解除**明細截斷（12 列上限）——會使現行名單全面失效。
5. ⛔ **不把任何裁定寫入 `docs/rulings/`**（該處為權威序**第 1 級**之 KL 域裁定正典）。
6. ⛔ **不追改**歷史記錄（批次報告／事前登記／凍存 log）之舊行號錨與舊雜湊。
7. ⛔ **文件與凍存物之刪除欄一律 0**（更正一律加註、原句不刪）。
8. ⛔ **長跑期間不動 `app.py` 與 `verify/`**（`run_all` 會走訪全倉 `*.py`）。

---

## §八 紀律（⛔ 不重述內容·去讀本體）

- **未 push 不得報收官**：`git ls-remote --heads origin` 實查含該 commit 才算。
- **報告本體寫倉內**；**聊天＝ping**（⛔ 不承載內容、不重述數字）。
- **完工回報**：push 後**單獨跑** `git log -1 --format="%h %s"`、原樣貼 ＋ 倉內報告路徑。
- **每波三 commit 分立**：A 事前登記（零量測）／B 量測與實作（零文件結論）／C 報告與登記。
- **⛔ 施工單之敘述不得採信**——一律現查；查不到即**停機上呈**，⛔ 不憑猜補齊。
- **⛔ `rc=1` 不構成「命題為偽」**；否定性宣稱須附【目錄射程】【字樣射程】【連接方式】。
