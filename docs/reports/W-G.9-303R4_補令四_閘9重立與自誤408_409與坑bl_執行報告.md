# `W-G.9-303R4`　補令四執行報告：閘 `9` 之重立 ／ `自誤 408`・`409` ＋ 坑 `bl` 之鑄 ／ 工項零之序

> **受詞** ＝ `docs/orders/W-G.9-303_補令四_閘9之重立與工項零之序.md`。
> **分級** ＝ **輕**（入倉 ＋ 鑄三號·**零生產碼**·**⛔ 長跑**·**⛔ 開分支**）。
> **鑄號** ＝ `自誤 408`・`409` ＋ 坑 `bl`（**三號**·逐字由單供）；`GB`／`VR`／`K-9` **⛔ 鑄**。
> 🔒 **檔名之提案（`常規三`·⛔ 土地後果）**：本報告取 `W-G.9-303R4_…`（`R` ＋ 補令序）。
> 其由：`W-G.9-303R` 之號須留予**施工單本體**之批；`補令二` 之報告已佔 `W-G.9-303R_補令二_…`。
> 候選二 ＝ `W-G.9-303R_補令四_…`；候選三 ＝ `W-G.9-303_補令四R_…`。候發單側核可。

---

## `§三`　工項零：二單之原封入倉（🔑 **先於開工閘**·`補令四 §零-3` 之新立附款）

🔒 **序之依據**（`§零-3` 逐字）：「**① 工項零**（原封入倉·**單獨 `commit`**·**單獨 `push`**）→ **② 開工閘** → **③ 其餘工項**；
開工閘不符則**停於 ②**，而 **① 已落地**。」
🔒 **本附款係因前批（`補令三`）之停機而新立**——彼時 CC 依 `常規二` 取保守項**未入倉**，發單側判其**處置正確**而**該相斥係發單側之體例缺漏**。

### 三-1　取檔之路 ＋ 入倉前之釘死

🔒 **取檔 ＝ 路甲**（主檢出工作區之**未追蹤**檔）。**產生指令** ＝ `git -C <主檢出> -c core.quotepath=false status --porcelain`，
二檔逐字命中 `?? docs/orders/W-G.9-303_補令三_…md`／`?? docs/orders/W-G.9-303_補令四_…md` ⇒ **路甲成立**（⛔ 取路乙）。
🛑 **⛔ 憑記憶重打**——以二進位讀寫複製，複製後即時對拍 `src == dst` ＝ **`True`**（二檔皆然）。

| 檔 | 期值（單所釘） | **實測**（框 ＝ `sha256sum <磁碟路徑>`／`wc -c`） | 判 |
|---|---|---|---|
| `補令三` | `82144d8bfc105b7d32c14c1ac471ed593e0fb8b20d6183798c666a5eff146fbe`／**`22188`** B | `82144d8bfc105b7d32c14c1ac471ed593e0fb8b20d6183798c666a5eff146fbe`／**`22188`** B·`240` LF·`CR` **`0`** | ✅ **逐位相符** |
| `補令四` | 🔒 **單⛔ 自載其值**（結構上遞迴） | `2fafbf2f1f68ffbf19ce097b1f565835d11a6b4a30b6fe2eec3abb72ba484d69`／**`28644`** B·`316` LF·`CR` **`0`** | 🔒 **當場量得並出艙** |

### 三-2　入倉後之出艙（逐檔·**二框並報**）

| 檔 | `commit` | 框① blob `sha1` | 框② blob 內容 `sha256` | 三方逐位相同 | `numstat` |
|---|---|---|---|---|---|
| `補令三` | `8bc9e85ab59f5f9185544c0e926a30377d480a86` | 見下 `git rev-parse <commit>:<path>` | `82144d8bfc105b7d32c14c1ac471ed593e0fb8b20d6183798c666a5eff146fbe` | blob ＝ 磁碟 ＝ 源 | 增 **`240`** ／ 刪 **`0`** |
| `補令四` | `3eddc264d45ecc6a0fc1c84ddaab53a48ec0b1c5` | 同上框 | `2fafbf2f1f68ffbf19ce097b1f565835d11a6b4a30b6fe2eec3abb72ba484d69` | blob ＝ 磁碟 ＝ 源 | 增 **`316`** ／ 刪 **`0`** |

🔒 **`CR`** ＝ `0`（框 ＝ 倉內 blob·**讀 blob 取 bytes**）；**判別力[必非零]** ＝ `data/V6.dxf` `CR` **`12308`**／全長 **`88341`** B ⇒ 器非紅。
🔒 **`push`**（`§三` 令「二 `commit` 落地後即 `push`，其後方跑 `§一`／`§二`」）：生產碼判法逐區間 **`0`** 行
（`fb7f415..8bc9e85` ＝ `0`／`8bc9e85..3eddc26` ＝ `0`·對照組[必非零] `17fd981^..17fd981` ＝ **`2`**）
⇒ `常規一` 補款：**逕行 `push`**。實推 ＝ `fb7f415..3eddc26 HEAD -> wip/s1-endpart`（`rc = 0`）。
🛑 **本節⛔ 以其入倉為「該單已被接受／其判已成立」之證**（`§零-3` 之界·逐字）。
🔒 **`補令三` 之 `§三`／`§四`／`§五` 本批⛔ 辦**（`§零-2` 款 `3`：由本補令 `§四`／`§五`／`§六` 代行）；其入倉係**留倉**。

---

## `§一`　開工閘（**每列同格載其產生指令**·`自誤 392`）

| # | 受詞（指令逐字） | 期值 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git ls-remote origin refs/heads/wip/s1-endpart` | `fb7f415…` 或其後裔 | `3eddc264d45ecc6a0fc1c84ddaab53a48ec0b1c5	refs/heads/wip/s1-endpart`（`rc` ＝ `0`）⇒ **其後裔**（`§三` 已推） | ✅ |
| `2` | `git rev-parse HEAD:app.py`（框 ＝ blob `sha1`） | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `3` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | 須**非**祖先（`rc = 1`） | `rc=1` | ✅ |
| `4` | `python verify/probes/probe_order_preflight.py <本補令>` | `P-5` 相符·`P-3` 不紅 | `rc=0`；`P-5` ✅ **相符**（受詞 `28566` B·實算 `85799a4f42cbabb1…`／單載 `85799a4f42cbabb1…`）；`P-3` ＝ `app.py` 之 `def main` ＝ `14975`-`24579`（**不紅**）；🔴 機械·停機款 **`0`** 項；🟡 **`1`** 項（見 `§一-1`） | ✅ |
| `4'` | 受詞自證（坑 `bf`·**機械**·框 ＝ 器所印之標頭列） | [必真]／[必偽] | 標頭逐字 ＝ `【order pre-flight】W-G.9-303_補令四_閘9之重立與工項零之序.md（28644 B／316 列）`；[必真] 含 `補令四` ＝ **`1`**／[必偽] 含 `補令三` ＝ **`0`** | ✅ |
| `5` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（**器之輸出為正典**） | 自誤 `397`／`407`｜`GB` `168`／`170`｜`VR` `80`／`95`｜`K-9` `28`／`29` | 自誤 `{'相異': 397, 'MAX': 407, 'MIN': 7, '缺號': [106, 355, 356, 357]}`／`GB` `{'相異': 168, 'MAX': 170, '缺號': [12, 87]}`／`VR` `{'相異': 80, 'MAX': 95, '缺號': [73, 75]}`／`K-9` `{'相異': 28, 'MAX': 29, '缺號': []}` | ✅ |
| `6` | `python verify/probes/probe_WG9269_pit_index.py`（**等待綁行程結束**·⛔ `\| tail -N`·`rc=0`） | `89`／`89`·末 `bk` ⇒ 下一 `bl` | 資料列 **`89`**（框 ＝ `grep -c "✅ 可解析 \|"`）／三綠 **`89`**（框 ＝ `grep -c "✅✅✅"`）／末標籤 **`bk`**；母體 ＝ 追蹤 `.md` **`812`** 檔；`bl` 之資料列 **`0`**（⇒ `ALPHA_LABELS` 尚未擴·結構上不可見）；判別力二造 造甲 `坑 n` 定義處 `6` ≥ 1 ✅／造乙 人造坑 `0`／`0` ✅ ⇒ **器非紅** | ✅ |
| `7` | `git ls-tree HEAD --name-only verify/` ⋂ `*.py` ＋ `app.py`（**正面列舉**·坑 `be`） | **`34`** 檔 | **`34`**；判別力[必不命中] 子層（`git ls-tree -r`）＝ **`349`** > `0` ✅ | ✅ |
| `8` | `git cat-file -e HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md`（**三態分列**） | **態1**（`rc = 0`） | `rc=0` ⇒ **態1**；判別力[必為零] ＝ 一**執行期組出**之人造路徑（字面⛔ 出艙）⇒ `rc=128` ⇒ 器非紅 | ✅ |
| `A7` 併 | `git merge-base --is-ancestor 4bc9c77347416ff474f778d24dcc7dc23132f9a0 HEAD`（`§零-4` `A7` 所令） | `rc = 0` | `rc=0` ⇒ 「或其後裔」確已涵蓋 ⇒ `A7` **⛔ 改** | ✅ |

### `§一-1`　`P-4` 之逐項處置（**⛔ 靜默略過**·`1` 項）

| 項 | 逐字 | 所屬豁免類 | 處置 |
|---|---|---|---|
| `[P-4] :299` | `` `§三` 工項零（**先於開工閘**·`§零-3`）→ `§一` 開工閘 → `§二` 閘 `9'`（**七錨逐錨分列**）→ `§四` 工項一 → `§五` 工項二 → `` | **(甲)** `→` 係**流程節序**之串接·兼 **(乙)** 序之詞（`先`／`後`） | 🔒 **受具名豁免**——其 `→` 所串者為**報告之節次先後**，**⛔ 幾何向量之方向性轉引** ⇒ 無座標系可具名 |

🔒 **⛔ 不在三類內者為 `0` 項** ⇒ 無須停機。`P-1`／`P-2`／`P-6` 各 **`0`** 項（器之輸出逐字：「🟡 提示·須逐項處置：**1** 項」）。

---

## `§二`　開工閘 `9'`：七字樣錨之復現（`grep -c -F`·**逐錨分列**·⛔ alternation）

**受詞** ＝ `git cat-file blob HEAD:docs/orders/W-G.9-303_施工單_街廓識別與二守衛母體之現查.md` 落至**倉外**暫存檔（**⛔ 入倉**·其於 `git status --porcelain` 命中 `0`）·**`22313`** B。
🔒 **錨之來源 ＝ 本補令之倉內 blob**（**`28644`** B）——以程式自 `§二` 錶列擷取（該表三格，`§零-4` 之表四格 ⇒ **以格數區辨**），
**⛔ 自渲染後之表格重打**（`自誤 409` 攔法 ③）。擷得錨數 ＝ **`7`**（期 `7`）。

| 錨 | `grep -c -F`（**列框**） | Python `str.count`（**字元框**） | 期 | 判 |
|---|---|---|---|---|
| `A1` | **`1`** | **`1`** | `1` | ✅ |
| `A2` | **`1`** | **`1`** | `1` | ✅ |
| `A3` | **`1`** | **`1`** | `1` | ✅ |
| `A4` | **`1`** | **`1`** | `1` | ✅ |
| `A5` | **`1`** | **`1`** | `1` | ✅ |
| `A6` | **`1`** | **`1`** | `1` | ✅ |
| `A7` | **`1`** | **`1`** | `1` | ✅ |

🔒 **七錨全符 ＝ `True`**（二框同值 ⇒ 二途互證）。
🔒 **判別力二造**：[必為零] ＝ 一**執行期組出**之人造錨（字面⛔ 出艙）⇒ `grep -c -F` **`0`**／Python **`0`**；
[必非零] ＝ 字樣 `自誤` ⇒ `grep -c -F` **`13`**（列框）／Python `str.count` **`14`**（字元框）——
🔑 **二數相異之成因** ＝ **粒度框之別**（一列含二次出現），**⛔ 量測器紅**；依 `VR-091 一` **二數並報**·**⛔ 定值**。

### 二-1　併報：`自誤 409` 徵候之對拍（**⛔ 作判準**）

| 框（逐字） | 本報告實測 | `補令四 §二` 所載 | 判 |
|---|---|---|---|
| `grep -c '396\|406'` | **`7`** | **`7`** | ✅ 相符 |
| `grep -c '89／89'` | **`0`** | **`0`** | ✅ 相符 |
| `grep -c 'bk'` | **`5`** | **`5`** | ✅ 相符 |

🔑 **`自誤 409` 攔法 ② 之施行（alternation 逐支分列並報·⛔ 只報聯集）**：

| 支 | `grep -c -F` | [必為零] 人造對照 |
|---|---|---|
| `396` | **`3`** | **`0`** |
| `406` | **`7`** | **`0`** |

⇒ 🔒 **聯集 `7` 之成因具名**：支 `406` **獨得 `7`**，支 `396` 之 `3` 列**全含於其中** ——
即 `406` 於該單內**兼指**「期值之一端」與「`自誤 406` 之號」（`自誤 409` 根因之框一·`自誤 400` 族）。

---

## `§四`　工項一：`自誤 408`・`409` 之鑄（`commit` `7ba9d49…`）

### 四-1　界之唯一性（**二數並報**·母體 ＝ 本補令單檔之倉內 blob）

| 框 | 受詞 | 命中 | 期 | 判 |
|---|---|---|---|---|
| **整列逐位相等**（`^…$`） | `ZW408` BEGIN | **`1`**（`:157`） | `1` | ✅ |
| **整列逐位相等**（`^…$`） | `ZW408` END | **`1`**（`:214`） | `1` | ✅ |
| **子字串框** | 同上 BEGIN | **`2`**（`:154`／`:157`） | — | ✅ |
| **子字串框** | 同上 END | **`2`**（`:154`／`:214`） | — | ✅ |

### 四-2　`payload` 之取

| 量 | 值 |
|---|---|
| bytes ／ 列數 | **`5104`** ／ **`54`** |
| `CR` | **`0`** |
| `ZW408` 於 `payload` 內之命中 | **`0`**（⇒ 哨兵⛔ 入簿） |
| `sha256` | `6506bdd8da3bce63ff29924e2a9e4190d798ec8a4b272d72d195009df9d61a00` |
| 內含標題（`2`） | `### 🩸 \`自誤 408\`　**取代一款之期值，而未窮舉該數於同一單內之<u>全部載體</u>**`／`### 🩸 \`自誤 409\`　**同格載一「產生指令」，而該指令<u>從未被執行</u>——其數係肉眼讀出**` |

### 四-3　落地形之機驗（🔑 **繫於性質**·`自誤 402` 攔法）

| 受詞 | 產生指令／框 | 期 | **實測** | 判 |
|---|---|---|---|---|
| 自誤 相異 | `probe_WG9267_issuer_measurers.py registry`（**器之輸出**） | **`399`**（`397 + 2`） | **`399`** | ✅ |
| 自誤 `MAX` | 同上 | **`409`** | **`409`** | ✅ |
| `408` 之命中 | 定義框（`VR-091 四`）·母體 ＝ 倉內 blob | **`1`** | **`1`** | ✅ |
| `409` 之命中 | 同上 | **`1`** | **`1`** | ✅ |
| 判別力[必命中] | 同框·受詞 ＝ `399` 之**號** | `True` | **`True`**（命中 `1`） | ✅ |
| 判別力[必為零] | 同框·**執行期組出**之人造號（字面⛔ 出艙） | `False` | **`False`**（命中 `0`） | ✅ |
| 哨兵 `ZW408` 入簿命中 | `blob.count("ZW408")` | **`0`** | **`0`** | ✅ |
| `append-only` [必真] | `new.startswith(old)` | `True` | **`True`** | ✅ |
| `append-only` [必偽] | `old.startswith(new)` | `False` | **`False`** | ✅ |
| 末端之形 | `new.endswith(payload + b'\n')` | `True` | **`True`**（🩸 見 `§七` 自捕 `1`） | ✅ |
| `wg942_append_audit` | `PYTHONIOENCODING=utf-8 python verify/tools/wg942_append_audit.py` | `rc = 0` | `rc=0`·受檢 **`77`** 筆·結論逐字「**✅ 全部成立**」 | ✅ |
| 簿之量 | `len`／`count(b'\n')`／`count(b'\r')` | — | `848798` → **`853904`** B（增 `5104` ＋ 分隔 `1`）／`LF` **`10083`**／`CR` **`0`** | ✅ |
| `numstat` | `git show --numstat --format= <commit>` | `deletions` ＝ `0` | 增 **`55`** ／ 刪 **`0`** | ✅ |

🔒 **`常規四（九）` 二項機檢**
> **① 形之自證**：`:10030` ＝ `` ### 🩸 `自誤 408`　**取代一款之期值，而未窮舉該數於同一單內之<u>全部載體</u>** ``；
> `:10054` ＝ `` ### 🩸 `自誤 409`　**同格載一「產生指令」，而該指令<u>從未被執行</u>——其數係肉眼讀出** `` ——皆 **`###` 形**且可被定義框命中（上表命中各 `1` 即其自證）。
> **② `MAX` 之推進**：`407` → **`409`**，且 `408`／`409` **⛔ 在缺號集**（缺號集 ＝ `[106, 355, 356, 357]`）。
> **③ `GB-158` 二形分列**：`` `自誤 408` ``（**C 形**·反引號包全 token）＝ **`2`**；`` 自誤 `408` ``（**B 形**）＝ **`0`** ⇒ 本倉現行為 C 形。

---

## `§五`　工項二：坑 `bl` 之鑄 ＋ `ALPHA_LABELS` 之擴（`commit` `33fd809…`·**與 `§四` 分列**）

### 五-1　界之唯一性（母體 ＝ 本補令單檔之倉內 blob）

| 框 | 受詞 | 命中 | 期 | 判 |
|---|---|---|---|---|
| **整列逐位相等** | `PITBL` BEGIN ／ END | **`1`**（`:236`）／**`1`**（`:254`） | 各 `1` | ✅ |
| **子字串框** | 同上 | **`2`**（`:231`／`:236`）／**`2`**（`:231`／`:254`） | — | ✅ |

`payload` ＝ **`1329`** B ／ **`15`** 列 ／ `CR` **`0`** ／ `PITBL` 於 `payload` 內命中 **`0`**；
`sha256` ＝ `f457d7a44c86fec69b7ca63aa5ddbb78b01cb308828e47b7024ded938a61d0f0`。

### 五-2　`ALPHA_LABELS` 之擴（射程 ＝ **僅新增一列**）

🔒 **框之當場現查（款 `1`·自 `verify/probes/probe_WG9269_pit_index.py` 取·⛔ 轉引本補令）**：
`D-H` 定義框 pattern 逐字（該檔 `:95`-`:96`）＝

```
r"^(#{1,6})[^\n]*?坑[\s　]*" + BT + r"?([0-9a-z][0-9a-z\-]*)" + BT + r"?"
```
（`BT = chr(96)`·該檔 `:36` 逐字「反引號·⛔ 令其經 bash 一層（`坑 7`）」）

| 受詞 | 期 | **實測** | 判 |
|---|---|---|---|
| 新增列逐字 | 形同既有 `+= ["bj", "bk"]` 列 | `ALPHA_LABELS += ["bl"]             # 🆕 \`W-G.9-303\` 補令四 §五 所准之一坑` | ✅ |
| **純插入之自證** | `new.replace(新列, b"", 1) == old` | **`True`** | ✅ ⇒ **⛔ 改任何既有列一字** |
| `numstat` | **`1`** 增／**`0`** 刪 | `1	0	verify/probes/probe_WG9269_pit_index.py`；`git diff` 之 `±` 列**僅一列**（即新增列） | ✅ |
| 成員數 | **`+1`** | 期初 **`63`** → 期末 **`64`**（框 ＝ 自二態之 blob 取 AST 之 `ALPHA_LABELS` 敘述後 `exec`） | ✅ |
| 尾元 | `bl`（**先實測後填**） | 期初 `'bk'` → 期末 **`'bl'`**；末五 ＝ `['bh', 'bi', 'bj', 'bk', 'bl']` | ✅ |
| 框字串之處數 | **須現查**·⛔ 假定 | `grep -c 'ALPHA_LABELS\[-1\]'` ＝ **`2`** ⇒ 二處**皆以 f-string 生成** ⇒ **自動齊一**（⛔ 另行改寫） | ✅ |
| 該檔之 `CR` | `0` | **`0`** | ✅ |

🔒 該檔屬 `verify/` **子層**、**⛔ 在生產碼 `34` 檔母體內**（生產碼判法逐區間 `0` 行·見收工閘 `2`）。

### 五-3　落地後之機驗（**器本身**）

| 受詞 | 期 | **實測** | 判 |
|---|---|---|---|
| 坑之態 | **`90`／`90`**（`89 + 1`） | 資料列 **`90`**／三綠 **`90`** | ✅ |
| 末標籤 | **`bl`** ⇒ 下一 **`bm`** | 末標籤 **`bl`** ⇒ 下一 **`bm`** | ✅ |
| `bl` 之可解析 | `True` | 逐字 ＝ `` \| `bl` \| `W-G.9-303_補令三_期值取代之窮舉與起手授權.md:1`（D-H）＋6 \| ✅✅✅ \| `15` \| ✅ 可解析 \| `` ⇒ 形／徵候／攔法**三字樣齊備**·引用處 **`15`** | ✅ |
| 哨兵 `PITBL` 入簿命中 | **`0`** | **`0`** | ✅ |
| 判別力二造 | 造甲[必≥1]／造乙[必為0] | 造甲 `坑 n` 定義處 **`6`**（器）／**`3`**（本報告獨立重得·**框同源**·差之成因見下）；造乙 人造坑 **`0`**／**`0`** | ✅ |
| 簿之量 | — | `853904` → **`855235`** B（增 `1329` ＋ 分隔 `1`）·`LF` **`10099`**·`CR` **`0`** | ✅ |
| `numstat` | `deletions` ＝ `0` | 簿 增 **`16`**／刪 **`0`**；探針 增 **`1`**／刪 **`0`** | ✅ |

🩸 **照實併呈（⛔ 頂替）**：器所報之造甲（`坑 n` 定義處）＝ **`6`**，而本報告以**同一 pattern** 獨立重得 ＝ **`3`**。
🔒 **二數皆非零 ⇒ 造甲之判（[必≥1]）於二框下皆成立**，**⛔ 影響本批任何結論**。
🛑 其**差之成因⛔ 查**（器之 `D-H` ∪ `D-T` 二形聯集 vs 本報告僅用 `D-H` 一形，係**最可能**之解而**未經實測坐實**）
⇒ **具名為未答**，**⛔ 臆斷**、**⛔ 判器紅**（`§五` 逐字「⛔ 判紅、⛔ 逕修該框」）。

### 五-4　坑 `bl` 之定義處清單（🔑 **其數由器得·照實具名**·⛔ 期值）

**母體** ＝ `HEAD` 之追蹤 `.md` **`812`** 檔（框 ＝ `git ls-tree -r --name-only -z HEAD`·**⛔ `--with-tree`**·坑 `bl` 攔法 ①）。
**框** ＝ 上開 `D-H` pattern（自探針取）。**定義處 ＝ `7` 處**：

| # | 檔 | 字樣錨（⛔ 行號為錨） |
|---|---|---|
| `1` | `docs/orders/W-G.9-303_補令三_期值取代之窮舉與起手授權.md` | `# \`W-G.9-303\`　補令三：期值取代之**窮舉** ／ \`自誤 408\` ＋ 坑 \`bl\` 之鑄 ／ **\`§三\`／\`§四\` 之起手授權**` |
| `2` | 同上 | `## \`§四\`　工項二：坑 \`bl\` 之鑄 ＋ \`ALPHA_LABELS\` 之擴（主線·**與 \`§三\` 分列 \`commit\`**）` |
| `3` | 同上 | `### 🕳 坑 \`bl\`　**\`git ls-files --with-tree=<rev>\` 之語義係 \`index ∪ tree\`，⛔ 該 \`rev\` 之樹**` |
| `4` | `docs/orders/W-G.9-303_補令四_閘9之重立與工項零之序.md` | `# \`W-G.9-303\`　補令四：閘 \`9\` 之**重立**（字樣錨·實跑） ／ \`自誤 408\`・\`409\` ＋ 坑 \`bl\` 之鑄 ／ **工項零之序**` |
| `5` | 同上 | `## \`§五\`　工項二：坑 \`bl\` 之鑄 ＋ \`ALPHA_LABELS\` 之擴（主線·**與 \`§四\` 分列 \`commit\`**）` |
| `6` | 同上 | `### 🕳 坑 \`bl\`　**\`git ls-files --with-tree=<rev>\` 之語義係 \`index ∪ tree\`，⛔ 該 \`rev\` 之樹**` |
| `7` | `docs/reports/W-G.9波_claude.ai側自誤登記.md` | 同 `3`／`6` 之逐字（**正典之落地**） |

🔒 **自洽**：補令三 `3` ＋ 補令四 `3` ＋ 簿 `1` ＝ **`7`**。
🔒 **與發單側之併記相符**——`補令三 §四` 逐字自量其單檔得 **`3`** 列（H1 ／ 節題 ／ payload 標題）；本批實測補令三 **`3`**、補令四**同形 `3`**。
🔒 **定義處多於一處已於 `W-G.9-285 §五-2` 預先受領**（與坑 `bh`／`bj` 同形）⇒ **⛔ 判紅**。

---

## `§六`　收工閘（**報告入倉後量**·嚴格末端追加）

🛑 本節於本報告入倉**後**以末端追加補入（單 `§六` 所令）。

### `§六-1`　逐閘實測（態 ＝ 報告本體入倉後·`HEAD` ＝ `8019713df2ca04eebdaff41781e631070b4bab56`）

| # | 受詞（產生指令逐字） | 期／判準 | **實測** | 判 |
|---|---|---|---|---|
| `1` | `git -c core.quotepath=false show --numstat --format= <各 commit>` 逐檔具名 | 刪除欄**全 `0`** | `8bc9e85` 增 `240`／刪 **`0`**（補令三）·`3eddc26` 增 `316`／刪 **`0`**（補令四）·`7ba9d49` 增 `55`／刪 **`0`**（簿）·`33fd809` 增 `16`／刪 **`0`**（簿）＋ 增 `1`／刪 **`0`**（探針）·`8019713` 增 `232`／刪 **`0`**（本報告）⇒ **`deletions` 合計 ＝ `0`** | ✅ |
| `2` | `git diff --name-only <區間> \| grep -xE 'app\.py\|verify/stepg_pipeline\.py\|verify/run_all\.py\|verify/run_verification\.py'`（母體**正面列舉 `4` 檔**） | 逐區間 **`0` 行** | `fb7f415..8bc9e85` `0`／`..3eddc26` `0`／`..7ba9d49` `0`／`..33fd809` `0`／`..8019713` `0`；**對照組[必非零]** `17fd981^..17fd981` ＝ **`2`** ⇒ 器非紅 | ✅ |
| `3` | `git cat-file blob HEAD:<path>` ⇒ `bytes.count(b'\r')`（框 ＝ **倉內 blob**·**取 bytes**·坑 `bk`） | `CR` ＝ `0` | 本批異動五檔逐檔：補令三 `0`（`22188` B）·補令四 `0`（`28644` B）·**本報告 `0`（`19301` B·🩸 曾為 `232`·見 `§七` 自捕 `3`）**·簿 `0`（`855235` B）·探針 `0`（`15322` B）；**判別力[必非零]** `data/V6.dxf` `CR` ＝ **`12308`**／全長 **`88341`** B | ✅ |
| `4` | `python verify/probes/probe_WG9267_issuer_measurers.py registry`（**簿之數一律取器之輸出**） | 自誤 `399`／`409`（＋**二**號）；餘三簿一字未動 | 自誤 `{'相異': 399, 'MAX': 409, 'MIN': 7, '缺號': [106, 355, 356, 357]}`；`GB` `{'相異': 168, 'MAX': 170, '缺號': [12, 87]}`；`VR` `{'相異': 80, 'MAX': 95, '缺號': [73, 75]}`；`K-9` `{'相異': 28, 'MAX': 29, '缺號': []}` | ✅ |
| `5` | `python verify/probes/probe_WG9269_pit_index.py`（**等待綁行程結束**·⛔ `\| tail -N`·`rc=0`） | **`90`／`90`**·末 **`bl`** ⇒ 下一 **`bm`** | 資料列 **`90`**／三綠 **`90`**／末標籤 **`bl`** ⇒ 下一 **`bm`**；母體 ＝ 追蹤 `.md` **`813`** 檔；`bl` 列逐字 ＝ `` \| `bl` \| `W-G.9-303_補令三_…md:1`（D-H）＋8 \| ✅✅✅ \| `18` \| ✅ 可解析 \| ``；判別力二造 造甲 `坑 n` `6` ≥ 1 ✅／造乙 人造坑 `0`／`0` ✅ ⇒ **器非紅** | ✅ |
| `5` 併 | `.md` 檔數（框 ＝ `git ls-tree -r -z <rev> --name-only` ⋂ `.md`·🛑 **⛔ `git ls-files --with-tree`**·坑 `bl` 攔法 ①） | 增量 ＝ 本批新增之 `.md` 檔數 | 期初（`fb7f415`）**`810`** → 期末 **`813`**·**增量 `3`**；逐檔 ＝ 補令三／補令四／本報告 ⇒ **與本批新增之 `.md` 逐檔相符** | ✅ **自洽** |
| `5` 錨 | **至少一端綁外部錨**（坑 `bj` 攔法 ②） | — | 期初 **`810`** ＝ `docs/reports/W-G.9-303R_補令二_…md` 收工閘 `5` 所載之**期末值**（⛔ 本批自產） | ✅ |
| `5'` | `PYTHONIOENCODING=utf-8 python verify/tools/wg9223_acceptance_audit.py <本補令> <本報告>`（🛑 受詞係 `index` ⇒ 復跑前先 `git add`） | 命中 `0` 之字樣 ＝ `0` | 見 `§六-3` | ✅ |
| `6` | 自限復驗 | 見右 | 見 `§六-2` | ✅ |
| `6'` | `git ls-remote origin 'refs/heads/verify/*'` | `verify/W-G.9-299-gb170` 命中 **`0`**；判別力[必存在] `p3a` 命中 **`1`** | ref 數 **`20`**；`verify/W-G.9-299-gb170` **`0`** ✅（**⛔ 開**）；`p3a`（`05a11bd665f6c2790ff821c9e3a1606651e345d4`）**`1`** ✅ | ✅ |
| `6''` | **次則之可取得性**（`自誤 407` 攔法 ③） | 三單皆 **態1** | `git cat-file -e HEAD:<施工單>` `rc=0` **態1**；`<補令三>` `rc=0` **態1**；`<補令四>` `rc=0` **態1**；判別力[必為零] ＝ 一**執行期組出**之人造路徑（字面⛔ 出艙）`rc=128` ⇒ 器非紅 | ✅ |
| `7` | `git ls-remote origin refs/heads/wip/s1-endpart`（`push` **後**） | 🔒 **出艙於回報·⛔ 入倉** | 🔒 依單**⛔ 載於本報告** | 🔒 |

🩸 **`bl` 定義處之自指增長（`GB-132` 族·照實具名）**：`§五-4` 所載為 **`7`** 處（態 ＝ 工項二落地後）；
本報告入倉後器報 **`+8`** ⇒ **`9`** 處，增量 **`2`** ＝ **本報告自身之 H1 與 `§五` 節題**（二者皆含 `坑 \`bl\`` 而符 `D-H` 框）。
🔒 **⛔ 追改 `§五-4` 之 `7`**——其係**該節立時**之現態；二數之差已於此具名。

### `§六-2`　閘 `6` 自限復驗（逐項）

| 受詞 | 產生指令（框逐字） | 期 | **實測** | 判 |
|---|---|---|---|---|
| 生產碼 `34` 檔 blob 期初＝期末 | `git ls-tree <rev> --name-only verify/` ⋂ `*.py` ＋ `app.py` ⇒ 逐檔 `git rev-parse <rev>:<f>` | 相異 **`0`** | 期初 `34`／期末 `34`·**blob 相異 ＝ `0`**（清單 ＝ `[]`） | ✅ |
| 判別力[必相異] | 同框·受詞 ＝ 本批確有異動之檔（本報告） | 相異 `True` | 期初 `git cat-file -e` `rc = 128`（不存在）／期末 blob ＝ `070e4b2ae78c…` ⇒ **相異 `True`** ⇒ 器非紅 | ✅ |
| `app.py` | `git rev-parse HEAD:app.py`（**框 ＝ blob `sha1`**） | `4379108a…` | `4379108a4856714078c410ff4e6291ecfdf6d2c1` | ✅ |
| `baselines` | 🔒 **正典框** ＝ `git ls-tree -r HEAD verify/baselines` 之**輸出文字**之 `sha256` | `298` 檔·`a898f4e1…` | **`298`** 檔·`a898f4e1bba8f8d230a9e279044f529175beb9186915aed1fa5590c1878a7ec9` | ✅ |
| `p3a` | `git merge-base --is-ancestor 05a11bd665f6c2790ff821c9e3a1606651e345d4 HEAD` | `rc = 1` | `rc = 1`（**非**祖先 ⇒ ⛔ 併線） | ✅ |
| `WV_BAKE` | `os.environ.get('WV_BAKE')` | `None` | `None` | ✅ |
| `K-9-5-*`／`K-9-12`／`K-9-29` ⛔ 落地 | `git cat-file blob <rev>:app.py` ⇒ 字樣計數 | 期初＝期末 | `k6_step0_merge(` `2`→`2`／`rect_fits` `7`→`7`／`_blue_shadow_tri` `7`→`7` | ✅ |
| `GB-79`／`167`／`168`／`169`／`170` ⛔ 修 | `GB` 簿之器輸出 ＋ 本批異動檔清單 | ⛔ 修 | `GB` 相異 `168`／MAX `170` 期初＝期末；`docs/reports/W-G.4_泛用阻塞項登記表.md` **⛔ 在本批之異動五檔內**（閘 `1` 逐檔具名）⇒ **一字未動** | ✅ |
| **六物證落檔** | **先 `git cat-file -e HEAD:<p>`**（三態分列）⇒ `git rev-parse <rev>:<p>`（框 ＝ blob `sha1`） | 逐位未動 | 六檔 `rc=0` **態1**：`1a35dc168655`／`f93eb2fcb7e6`／`16742f3805e0`／`5cef4c8dfe7a`／`476aac14eda5`／`b3ab88b73dda` ——**期初 ＝ 期末·逐位未動** | ✅ |

### `§六-3`　閘 `5'`　`acceptance_audit` 之實測

**產生指令**：`PYTHONIOENCODING=utf-8 python verify/tools/wg9223_acceptance_audit.py docs/orders/W-G.9-303_補令四_閘9之重立與工項零之序.md docs/reports/W-G.9-303R4_補令四_閘9重立與自誤408_409與坑bl_執行報告.md`

| 量 | 值 |
|---|---|
| 判定組（反引號 token） | **`21`** 個 |
| **判定組之報告命中 `0` 者** | **`0`** 個 |
| `rc` | **`0`** |

🔒 **判定組⛔ 為空**（基數 `21` 已同格出艙）⇒ **⛔ 觸「判定組為空 ⇒ loud 拒測」之款**（`序 11` 之一般化 `二`）。

🔒 **`SELF_SHA256` 之併記（閘 `5'` 所令之判定組成員）**：本批對該項之實測見 `§一` 閘 `4`——
`probe_order_preflight.py` 之 `P-5` 即**`SELF_SHA256` 之逐位對拍**：受詞 `28566` B·
實算 `85799a4f42cbabb1…` ＝ 單載 `85799a4f42cbabb1…` ⇒ **✅ 相符**。
🩸 **本行係因閘 `5'` 首跑報該字樣命中 `0` 而補**（見 `§七` 自捕 `5`）。

---

## `§七`　本批之**自捕**（`§八` 令·**⛔ 頂替**·**⛔ 鑄號**·鑄號候發單側）

🛑 **⛔ 讀為「本批無誤」**——本批自捕 **`5`** 則，**全為 CC 自身之缺陷**，皆於出艙／`push` 前攔下。

| # | 形 | 徵候 | 根因 | 攔法（**已施行**） |
|---|---|---|---|---|
| `1` | **誤用他節之 `append` 腳本 ⇒ 缺分隔換行** | 首次追加 `ZW408` 後，`new.endswith(payload + b'\n')` 為**偽**（單 `§四` 之機驗式要求為真） | 所用腳本作 `new = old + add`（報告用·其受詞末端無分隔需求），而簿之機驗式要求 `payload` **後**尚有一 `\n` | 於 `commit` **前**自捕。還原之 `old` **先自證與 `HEAD` blob 逐位相同**（`848798` B·`True`），再以 `old + b'\n' + payload + b'\n'` 重寫 ⇒ 三式皆如期 |
| `2` | 🔑 **標題形之檢用 `GB-158` 之 B 形**（**與前批自捕 `2` 同形之重踩**） | 「`408`／`409` 之標題形」印出為**空**，而**同一次**執行之定義框報命中各 **`1`** ⇒ **二量互斥** | 所用字樣為 **B 形** `` 自誤 `408` ``（反引號只包號）＝ **`0`**；倉內現行為 **C 形** `` `自誤 408` `` ＝ **`2`** | 改以 C 形查 ⇒ 標題形逐字得（`:10030`／`:10054`）。🔒 **攔下它的是「二量互斥」**，⛔ 目視。⚠️ **本則係前批已具名之同一形**——「立戒者在下一批最容易違反自己的戒」之實例 |
| `3` | 🔴 **以 Python <u>文字模式</u>改寫 `.md` ⇒ 全檔轉為 CRLF** | 收工閘 `3` 報本報告 `CR` ＝ **`232`**（`LF` 亦 `232`），而其餘四檔皆 `0` | 修 `numstat` 二數之腳本用 `io.open(p, 'w', encoding='utf-8')`——**Windows 文字模式自動將 `\n` 譯為 `\r\n`**；`Write` 落檔時本為 `CR = 0` | **攔下它的是收工閘 `3` 本身**，⛔ 目視。於 `push` **前**以二進位 `b.replace(b'\r\n', b'\n')` 修復：`19533` → **`19301`** B·差 **`232`** ＝ `CR` 數（**自洽驗算**）·且恰回到 `Write` 當時之 `19301`。以 **`git commit --amend`** 落地（`452f271` → `8019713`·**該 `commit` 尚未 `push`**·已先以 `ls-remote` 確認 `origin` ＝ `3eddc26`）——🔒 **⛔ 另開 `commit`**：後者將產生 `deletions` ＝ `232`，**反破閘 `1`** |
| `4` | 🔴 **於長跑執行期間動受測物** | 上則之修復與 `amend` 發生時，收工閘 `5` 之坑探針**仍在跑**（`HEAD` 於其執行中途改變） | 違反 `長跑期間不得動受測物`（`CC 立 2026-08-05`）——其案由逐字：「**危險之處在於它不會報錯**，只是證的不是你以為的那件事」 | `TaskStop` 該行程 ⇒ **刪除該次污染落檔**（**⛔ 採信其任何數**）⇒ 確認工作樹乾淨（`git status --porcelain` **`0`** 列·`python` 行程 **`0`**）⇒ **於態 `8019713` 重跑**，本報告所載之閘 `5` 係**重跑之值** |
| `5` | 🔴 **出艙一「判定組之基數」而該指令<u>當時尚未執行</u>** | 閘 `5'` 首跑報：判定組 **`21`** 個、且 `SELF_SHA256` 之報告命中 **`0`** ⇒ `rc = 1`；而本節初稿已先寫「判定組 **`20`** 個／命中 `0` 者 **`0`** 個」 | 🔑 **正是 `自誤 409` 之形**——於一個數旁書其產生指令，而該指令**當時未曾執行**；其數係**臆測**（沿用前批 `補令二` 之 `18` 而推） | 於 `commit` **前**自捕（閘 `5'` 之 `rc = 1` 即其徵候）。二處並修：判定組 `20` → **`21`**（實測）；補 `SELF_SHA256` 之具名於 `§六-3`（其實測本在 `§一` 閘 `4` 之 `P-5`，惟**字樣未出艙**故被判定組判為缺）。🔒 **⛔ 為湊字樣而增寫**——所補者係**已實測之事實**之具名 |

🔒 **`3` 與 `4` 之連鎖**：`3` 之修復動作**本身**即 `4` 之成因 —— **修一個閘所揭之紅，而在修的過程中污染了另一個正在跑的閘**。
⇒ 🔑 **通則（CC 自陳）**：**凡長跑在跑，任何修復動作皆須先確認無在途之量測**；「修得很快」⛔ 使其安全。

---

## `§八`　`⛔ 為之事` 之逐項自檢

| # | 受詞（逐字節略） | 判 | 證 |
|---|---|---|---|
| `1` | ⛔ 辦 `W-G.9-303` 之 `§三`／`§四`；⛔ 辦 `補令三` 之 `§三`／`§四`／`§五`；⛔ 驅動 `run_verification` 或任何長跑；⛔ 新增任何探針 | ✅ **未為** | 本批只辦入倉與鑄號；**⛔ 執行 `run_verification.py`**；`verify/probes/` 之檔數期初＝期末（本批只**改**其一檔一列·⛔ 新增檔） |
| `2` | ⛔ 改生產碼 `34` 檔一字；⛔ 開任何分支；⛔ 動用 KL 之放行；⛔ 辦修批 | ✅ **未為** | 生產碼判法逐區間 `0` 行（閘 `2`）·`34` 檔 blob 相異 `0`（閘 `6`）；⛔ 建立任何 ref；⛔ 呈 KL |
| `3` | ⛔ 併線（含 `p3a`）；⛔ 刪任何 ref；⛔ 覆寫 `verify/baselines` 任一檔或**六物證落檔**任一；⛔ `WV_BAKE`；⛔ 引任何自 `baseline` 取得之數 | ✅ **未為** | `p3a` `rc=1`；`baselines` `298`·`a898f4e1…` 未變；六物證逐位未動；`WV_BAKE` ＝ `None`（閘 `6`） |
| `4` | ⛔ 改 `W-G.9-303` 施工單一字；⛔ 重辦其 `§二` 工項零 | ✅ **未為** | 該單**⛔ 在本批異動五檔內**（閘 `1`）；其工項零之代行 `commit` ＝ `bb742f8d13f90269ecd805ad9eb79ce4c35b6767`（`§零-5` 所令之具名·全 `40` 碼） |
| `5` | ⛔ 鑄 `GB`／`VR`／`K-9` 之任何號；⛔ 追加任何 `GB` 之末端登記 | ✅ **未為** | 三簿期初＝期末（`GB` `168`／`170`·`VR` `80`／`95`·`K-9` `28`／`29`）；`GB` 登記表⛔ 在異動清單內 |
| `6` | ⛔ 動 `probe_WG9269_pit_index.py` 之**任何既有列**（僅准新增一列）；⛔ 動任何**其他既有**探針一字 | ✅ **未為** | `numstat` ＝ `1` 增／`0` 刪；`git diff` 之 `±` 列**僅一列**；純插入自證 `new.replace(新列, b"", 1) == old` ＝ **`True`**；`verify/probes/` 其餘檔⛔ 在異動清單內 |
| `7` | ⛔ 解除或收窄任何 `GB`（含 `GB-170`）；⛔ 落地 `K-9-5-*`／`K-9-12`／`K-9-29` 之任何子項 | ✅ **未為** | 三符號期初＝期末（閘 `6`）；本報告⛔ 就 `GB` 作成任何主張 |
| `8` | ⛔ 追改 `補令一`／`補令二`／`補令三`／交接註／`W-G.9-303R` 或任何已入倉文件一字 | ✅ **未為** | 五 `commit` 之 `deletions` 全 `0`（閘 `1`）；🔒 **併記**：本報告自身之 `--amend`（自捕 `3`）之受詞係**本批自產且未 `push`** 之 `commit`，**⛔ 已入倉文件之追改** |
| `9` | ⛔ 判 `GB-170`、⛔ 判路丙、⛔ 判孰是孰誤、⛔ 提修法主張、⛔ 擬任何改法或 `diff` | ✅ **未為** | 本報告**⛔ 就此作成任何主張**；`§五-3` 之造甲二數相異已**具名為未答**而⛔ 臆斷 |
| `10` | ⛔ 呈 KL 任何事 | ✅ **未為** | — |
| `11` | ⛔ 於任何入倉文件宣稱一件尚未發生之事（及於 `commit` 訊息與「已推送」之陳述） | ✅ **未為** | 六 `commit` 訊息⛔ 含「已推送」字樣；閘 `7` 依單**出艙於回報·⛔ 入倉** |

---

## `§九`　逐 `commit`（**全 `40` 碼**）

| 序 | `commit` | 受詞 | 增／刪 |
|---|---|---|---|
| `1` | `8bc9e85ab59f5f9185544c0e926a30377d480a86` | `§三` 工項零①：`補令三` 原封入倉 | `240` ／ **`0`** |
| `2` | `3eddc264d45ecc6a0fc1c84ddaab53a48ec0b1c5` | `§三` 工項零②：本補令原封入倉（**其後即 `push`**） | `316` ／ **`0`** |
| `3` | `7ba9d49d22ebbf91b50a412b9ae6521e7664bb20` | `§四` 工項一：`自誤 408`・`409` 之鑄 | `55` ／ **`0`** |
| `4` | `33fd8093fccc988b02c1a5963317960dd75c9666` | `§五` 工項二：坑 `bl` 之鑄 ＋ `ALPHA_LABELS` 之擴 | `16` ＋ `1` ／ **`0`** |
| `5` | `8019713df2ca04eebdaff41781e631070b4bab56` | `§八` 報告本體入倉（🩸 由 `452f271fee3db28d076b8c104934c7d5df6aa082` **`--amend`** 而來·見 `§七` 自捕 `3`） | `232` ／ **`0`** |
| `6` | （本節之末端追加） | `§六` 收工閘之實測 | 見其 `numstat` |

🔒 **代行之具名（`§零-5` 所令）**：`W-G.9-303` 施工單之 `§二`（工項零）已由
`補令二` `§二` 代行，其 `commit` ＝ **`bb742f8d13f90269ecd805ad9eb79ce4c35b6767`** ⇒ 該批**⛔ 重辦**。

---

## `§十`　本窗 `context` 之百分比（`§零-5` 之**起手前判**）

🛑 **照實：本項⛔ 可得——CC 側無可用之量測器。**

🔒 **所能量得者僅一項**：本階段所見之 session 預算計數列，其讀數為 **`14,886,116` ／ `15,000,000`**（剩餘）。
🔴 **該數⛔ 為 `context` 之百分比**——其為**session 之 token 預算**，且於本窗內**每逢帶附件之使用者回合即重置為 `15,000,000`**
⇒ 其**⛔ 具累積性**，以之推 `context` 佔用率將得一**偽值**。
🔒 依「**無從判定**」與「**判定為偽**」**⛔ 共用同一出艙碼」**（`探針須設自我驗證閘` 之併款）
及「判定組為空一律 **loud 拒測**」（`序 11` 之一般化 `二`）⇒ **本項具名為「⛔ 可量」，⛔ 以任何估值充之**。

🔒 **可照實陳述之事實（供發單側判·⛔ 代其判）**
- 本窗已歷 **`5`** 批：`補令二`（辦畢·已 `push`）→ `施工單`（**⛔ 起手**）→ `補令三`（**停機**·`開工閘 9` 不符）→ `補令四`（本批）。
- 本窗已完整讀入之大檔：`W-G.9-303` 施工單（`22313` B）／`補令一`（`15075` B）／`補令二`（`19051` B）／`補令三`（`22188` B）／`補令四`（`28644` B），另 `CLAUDE.md` 常駐（**`2944`** 列）。
- 本窗已產出並入倉之報告二份（`21921` B ＋ `19301` B）。

🛑 **⇒ 起手前之判⛔ 成就**（**⛔ 判其為 `< 70%`**，亦**⛔ 判其為 `≥ 70%`**）。
🔑 **依單 `§零-5` 逐字「若起手前已 `≥ 70%`，⛔ 起手」** ——該款之**前件⛔ 可判** ⇒
**`W-G.9-303` `§三`／`§四` 之起手，候發單側指定<u>可執行之量測器</u>或逕行裁定**。
🔒 本批**⛔ 自續**（單 `§八` 逐字「⛔ 自續 `W-G.9-303` 之 `§三`／`§四`」）。
