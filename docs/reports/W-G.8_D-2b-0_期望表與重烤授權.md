# W-G.8／**D-2b-0**：`K-9-5-4` ② 之新期望表 ＋ D-3 重烤授權入正典（⛔ 零生產碼）

**錨**：`f8b42fc`（D-2）。**驗收**：`git diff -- app.py` ⇒ **0 行**。
🛑 **commit B（D-2b-1 切換）本批未做**——§B-2 之硬閘已觸發，清單見 §四。

---

## §A-1 兩則登記

### (1) **GB-44**：中間態幾何口徑不一致（登記表 43 → **44** 列）

`D-2` 落地後，**街角最小規定範圍**已改 **∥SIDELINE 定寬**，
而**資格閘所比之真G** 仍由 `_corner_first_lot_G` → `_solve_G_one` → `solve_G_binary`
以 **∥ALLOC 幾何**解出（`app.py:11585`）⇒ **閘之左右兩端建立在不同幾何上**。

⚠️ **非新缺陷**——係「`D-②` 只做了①、②未實作」之**中間態**。
🔑 **失效條件（逐字）**：「**`D-2b` 落地 ⇒ 本列關閉。**」

### (2) 正典 `K-9-5-4` 加註（`docs/rulings/K-6_*.md:655`）

> 🔴 **實作狀態（更新於 D-2b-0·2026-08-08）**：**① ＝ 已切換**（D-2·`f8b42fc`）。
> 🔴 **② 之「街角第 1、2 宗之實配境界線」尚未實作**——見 `GB-44`。
> ⛔ **在 `D-2b` 落地前，任何文件不得記載 `D-②` 已完成。**

---

## §A-2 D-3 重烤授權入正典（**逐字**·`docs/rulings/K-6_*.md:692`）

接於「🔒 D 波之範圍界線」之後，標題「🔒 D-3 重烤授權（KL 裁 2026-08-08）」，
五條邊界一字未改（僅限 D-3／逐 `gid` 因果鏈／範圍限於本波過期者／重烤≠移除錨≠放寬閘／不延伸）。

⇒ **D-2 之停機項（GSA 硬錨）自此有了歸屬**：屬 **D-3**，⛔ **本批仍不得重烤**。

---

## §A-3 新期望表（**凍存**）——`verify/out/probe_D2b_expect_凍存.log`

**作法**：patch `ns["_corner_first_lot_G"]` 之 `allocation_dir` → **`rot90_cw(SIDE 方向)`**
（⛔ 不改生產）。⛔ **第 2 宗之遠側界仍 ∥ALLOC**（KL 2026-08-07 逐字）——本檔只動第 1 宗。

🔒 **檔頭逐字聲明（施工單 §A-3 明令）**：
**門檻取自「已切換之生產範圍面積」**（D-2 已落地之 `_build_corner_range_v3`·直接取生產值）；
**真G 為 patch 值**。

### 3-1 🔒 兩道閘（**皆過**）

| 閘 | 結果 |
|---|---|
| **§A-4** patch 出之 `area_geom` vs `G` 之差 ≤ `0.01㎡` | ✅ **未過閘 0／132**（最大 `0.010000` ＝ solver 之 `tol` 邊界·非失敗） |
| **§A-4b** 反靜默退路計數 | ✅ **咬到 132 次／退回 0 次** |

樣本（`allocation_dir` 前 → 後）：
`R4/left (0.719667, −0.694319) → (−0.751733, 0.659468)`；
`R4/right (0.719667, −0.694319) → (−0.684026, 0.729458)`
⇒ **左右兩側取到不同值**（各自 SIDE_LINE 方向）✅

### 3-2 結果

| 項 | 值 |
|---|---|
| 母體列數 | 新 **38**／現況 **38**／聯集 **38**（框內重複鍵 **0**） |
| **真G 之 Δ** | **確實改變**：`−4.56` ～ `+2.79 ㎡`（最劣 `0m R3/right 628-45(2)` `199.73 → 195.17`） |
| **達標翻轉** | **0** |
| **winner 異動** | **0 格** |
| 有 winner／強制抵費地 | **13／3**（**與現況 D-2 態相同**） |

🔑 **結論**：`K-9-5-4` ② 對**真G 有實質影響**（±4.6 ㎡ 量級），
但**不足以翻動任何達標或 winner** ——各格之餘裕遠大於該擾動。
⇒ **D-2b 之土地效益不在 PK 結果上**，而在 §B-3(c)(d)（第 1 宗實配寬度成為**定值**、
`3.5m R1/right` 之實配最小寬對 `T=7.00` 之差）——那兩項**尚未量測**（待 commit B）。

---

## §A-5 對帳器不變量（**本批起制度化**·已內建於探針）

| # | 不變量 | 本檔輸出 |
|---|---|---|
| 1 | 框內重複鍵 ＝ 0，否則 `raise` | ✅ 已斷言 |
| 2 | 母體列數印出並對得上 | ✅ 38／38／38 |
| 3 | 比對欄以**欄名**指名並印出 | ✅ `真G(㎡)`／`門檻(㎡)`／`達標`／`選中★` |
| 4 | 輸出檔名帶批次識別 | ✅ `probe_D2b_expect_凍存.log` |
| 5 | ⛔ 禁寫入他批已入庫之 log 檔名 | ✅ 未寫入任何他批檔 |

---

## §四 🛑 §B-2 硬閘觸發——**commit B 未做**，清單上呈

施工單 §B-2 逐字：「**若切換必須動到 `_build_corner_range_v3` 以外之函式 ⇒ 先列出清單上呈再動。**」

**確實必須**——實配之 `allocation_dir` **完全不經過** `_build_corner_range_v3`
（該函式只管**規定範圍**）；實配走 `_solve_one` → `_solve_G_one` → `solve_G_binary` → `_block_strip`。

| # | 檔 | 落點 | 角色 |
|---|---|---|---|
| 1 | `verify/stepg_pipeline.py` | `:644` | 左 group `_solve_one`（第 1 宗·左） |
| 2 | `verify/stepg_pipeline.py` | `:715` | 右 group `_solve_one`（第 1 宗·右） |
| 3 | `verify/stepg_pipeline.py` | `:728` | 右 group **retry** `_solve_one`（`area_geom < 0.5`） |
| 4 | `app.py` | `:19175` | `main()` 左 group（app 路徑複刻） |
| 5 | `app.py` | `:19254` | `main()` 右 group |
| 6 | `app.py` | `:19268` | `main()` 右 group retry |
| 7 | `app.py` | `:11585` | `_corner_first_lot_G` → `_solve_G_one`（**真G**·資格閘） |

⛔ **已排除並複驗**：`verify/stepg_pipeline.py:858` ＝ `_place_pool_parcels`（池片建構·非第 1 宗）。

### 4-1 三個請 KL 一併裁之點

1. **落點 7（真G）是否同批改？** 不改 ⇒ `GB-44` 之口徑分家**繼續**；
   改 ⇒ `GB-44` 當批關閉，且期望表須連真G 一起 patch（**§A-3 已如此算**）。
2. **落點 1〜6 須依 `is_first_corner` 分流** ——同一呼叫點，第 1 宗傳 `rot90_cw(SIDE)`、
   其餘傳 `allocation_dir_block` ⇒ **呼叫端出現條件分支**。
3. **`app.py` 之三處在 `main()` 內** ⇒ 依 `CLAUDE.md` 鐵律，
   `run_all` **不得單獨作為驗收依據**，須**另附合成案**。

---

## §五 ⛔ 本批**未**做者

1. **commit B（D-2b-1 切換）** —— 🛑 §B-2 硬閘·待 KL 裁清單。
2. **§B-1／§B-3〜§B-5** —— 隨 commit B。
3. **GSA 硬錨重烤** —— 屬 **D-3**（§A-2 已授權，⛔ 本批不得為之）。

---

## §六 🔴 本批之自錯紀錄（**四次·全由閘擋下**）

| # | 錯 | 擋下者 |
|---|---|---|
| 1 | 探針**無**反靜默計數器 ⇒ 首版得「零異動」之漂亮結論 | 自查（想起 GEO-1 之戒）後補上 |
| 2 | patch 取 `kw["_blk"]`，而 `_corner_first_lot_G` 之參數是 `_label` | 補上之計數器：**咬到 0／退回 132** |
| 3 | 改取 `_label` 後**仍 0/132**——`_label` 實為複合字串 `'R4·628(1)·左'`、**非街廓標籤** | **再看一次計數器**（⚠️ 兩次 `rc=0`·⛔ 靠退出碼看不出來） |
| 4 | 改用 `side_mid` 幾何反查後，診斷列 `_old or (0,0)` 於 numpy 陣列真值歧義 | `ValueError`（⚠️ 反而證明幾何反查已生效、走到了該行） |

🔑 **本批最貴之一條**：**修完之後那個閘要再看一次，而且要看它的「數字」、不是看它有沒有報錯。**
第 2 次與第 3 次之 `rc` **皆為 0**，兩次都「跑完了」，而結論兩次都是假的。
⇒ 若非計數器逐次列印**咬到／退回**之數字，第 3 版之假結論會直接成為凍存表進倉，
且其後每一批都會拿它當前提。
（併看失敗考古節 50「探針結論句與其自身 docstring 矛盾」——同族：**外部重建沒有型別可擋**。）
