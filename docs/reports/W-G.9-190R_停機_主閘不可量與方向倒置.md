# 🛑 `W-G.9-190R`　**停機上呈**：`N-1` 主閘 `[T5-FAM2]` **不可量** ＋ 其判準**方向倒置**

**出文窗**：CC　**施工單**：`W-G.9-190`（族②③ 之切換·`W-G.9-5` 之落地）　**基座**：`25857aa`
**性質**：🔒 **停機報告**·⛔ **零生產碼**·⛔ **未動 `app.py` 一字**·⛔ **未鑄任何號**
**取號**：`常規四 三` ⇒ `W-G.9-190R`。檔名依 `常規三` 取停機體例（同 `W-G.9-189R_停機_…`）。

🛑 **停機之時點**：於 `§零` 開工閘**全部辦畢之後**、`§一 N-1` 之**碼面切換之前**。
依 `CLAUDE.md` 工作紀律「**必答題未裁先施工＝停機**」——本報告所呈二問**皆為必答題**：
不裁而施工，其結果將於 `X-2` 觸發**受詞不對之假停機**（見 `§二-3`）。

---

## `§零`　開工閘（`S-0`〜`S-6`）——**全過**

| 閘 | 結果 |
|---|---|
| **`S-0`** | ✅ 取檔內**最末**一個 `SELF_SHA256`（索引 `[11, 190]` ⇒ 取 `190`·**陷阱 6**）；全檔 `13062` B／`191` 列，扣除後 `12984` B ⇒ 實算 `a5db7634127f960b…` **逐位相符** |
| **`S-1`** | ✅ **九檔全符**（⛔ 未照抄單表·逐檔現查）；`app.py` 尾**無 `LF`**、末位元組 `)`、`wc -l` `23411` —— 三者皆與單載相符 |
| **`S-2`** | ✅ `ls-remote` ＝ `25857aa99ced20ff9e6abb1d519bdf7d7ab1ff5a`；`is-ancestor` `rc=0`；`left-right` ＝ `0 0` |
| **`S-3`** | ✅ `C:\w190a`（clone）／`C:\w190x`（自檢器·**置於 clone 外**）；刪除確認見 `§六` |
| **`S-4`** | ✅ 已讀 `§五` 禁改清單；本批**⛔ 未動其中任一處** |
| **`S-5`** | ✅ 三項齊備 ⇒ `verify/out/probe_WG9190_baseline_25857aa.log` |
| **`S-6`** | ✅ **開工復現逐項相符**（見 `§零-2`） |

### `§零-1`　`S-1` 九檔（框 ＝ blob 位元組／`splitlines()`／`sha256` 前 `16`·CRLF 皆 `0`）

`app.py` `1378708`／`23412`／`2be36e4c88b6e129` ✅　`CLAUDE.md` `81200`／`903`／`3df01894ba38ccc5` ✅
`verify/stepg_pipeline.py` `96261`／`1454`／`0260273350021311` ✅　自誤登記表 `402254`／`4347`／`82084b330ff89b4d` ✅
`W-G.4` 阻塞表 `529494`／`3238`／`499ce534e43f332e` ✅　驗證裁定表 `333071`／`4258`／`c5b812d388040de3` ✅
`K-6` 正典 `258295`／`3632`／`6a5cada2901a10e8` ✅　`W-G.9-189R` 停機報告 `14261`／`221`／`4135a6244adaf3d9` ✅
`probe_WG9189_s6_replica.py` `12399`／`280`／`c22b2faa38968505` ✅

### `§零-2`　`S-6` 開工復現（`verify/out/probe_WG9190_S6_open.log`）

| 受詞 | 單所令 | 實測 | 判 |
|---|---|---|---|
| 四區之判 | `True`／`True`／`False`／`False` | 同 | ✅ |
| 實質差 | `20` 處 | `20` 處 | ✅ |
| 判別力造二問 | 皆 `False` | 皆 `False` | ✅ |
| `rc` | `1` | `1` | ✅ |

🔒 **`ast.dump` bytes**：本機得 `4353`／`1293`／`23821`／`20641`／`74569`／`94370`，與倉內 `189R` log **逐項相同**
——因本機直譯器亦為 **`CPython 3.13.11`**（發單側之 `3.12.3` 得另一組，單 `§零` 已具名）。
⇒ 依單「**只對拍判定與差數**，⛔ 不以 dump bytes 判紅」，本項**⛔ 未作為判準**。

### `§零-3`　`S-5` 基準量（`verify/out/probe_WG9190_baseline_25857aa.log`）

**`1.` `[T2-DIAG]`**（情境 `SB = 3.5 m`）：總 `5` 列／逐街廓 `{R1:1, R2:1, R3:1, R4:1, R5:0, R6:1}`。
🔴 `R5` **`0` 列**係期望事實（`②-宗 圍堵閘破[R5]：宗-宗重疊 = 56.8186 > 上界 1.5999`）。

**`3.` `[T3-GATE]`**（情境 `SB = 0.0 m`·【倉】`probe_WG9187_t3gate.log`）：
**錨定框（`` ^\[T3-GATE\] 街廓 ``）＝ `39` 列**／裸框 ＝ `57` 列／差 `18` ＝ 摘錄重列
（`CLAUDE.md:725` 二數並報）⇒ 期初 `39` **✅ 相符**。

**`2.` 🔑 `R2` 三量之 app／shim 二值**（情境 `SB = 3.5 m`）：

| 量 | 池帶2 `s` 寬（**全精度**） | 退化帶數 | `merged` 段數 |
|---|---|---|---|
| **shim 側**（現行·**現跑**） | `33.0810700422`（`[T2-DIAG]` 顯示 `33.0811`） | **`2`** | **`3`** |
| **app 側**（注入復現·**現跑**） | `32.9639559294`（顯示 `32.9640`） | **`3`** | **`4`** |
| shim 側（【倉】`177R §四-4`） | `33.0810700422` | `2` | `3` |
| app 側（【倉】`177R §四-4`） | `32.9639559294` | `3` | `4` |

🔒 **與【倉】之 `|Δ|`：shim `5.542e-13`／app `3.279e-11`**（`177R` 之容差 `1e-4`）⇒ **逐項相符**。
🔒 **框**：全精度值取自 spy 之 `keep[1]` 區間長；`[T2-DIAG]` 之值係 `%.4f` **顯示值**
⇒ ⛔ 不得逐位對拍（`GB-101` 族），只作並列輔證。

🩸 **本檔自身之二缺（CC 於出艙前自捕並修）**：
1. **注入命中之母體錯配**：首版報「命中 `16`，而 `177R` 應為 `2`」——實則 `16` 係**全六街廓合計**，
   `177R` 之 `2` 係**僅 `R2`**。⇒ 二數**母體不同、⛔ 不可直接相比**。改為**逐街廓並報**：
   `{R1:4, R2:2, R3:2, R4:4, R5:2, R6:2}` ⇒ **`R2` 恰 `2`** ✅。
2. **精度框錯**：首版之池帶2 寬印 `10` 位小數而其源係 `[T2-DIAG]` 之 `4dp` **顯示值**
   ⇒ 改自 spy 之 `keep` 區間取**全精度**，並把顯示值並列。

🔒 **實 spy 之佐證（⛔ 非推定·`177R §四-4` 戒 2 所令）**：`R2` 之 `_solve_G_one` 共 **`28`** 次；
`side` 之**相異實收值 ＝ `['無']`**（⛔ 非 `'left'`／`'right'`）；單以 `W_prev == 0.0` 為框
**誤咬 `16`/`28`**，加 `side_mid is not None` 後**恰 `2`** ——與 `177R` 之三戒**逐項相符**。

---

## `§一`　🛑 **問一：`N-1` 主閘 `[T5-FAM2]` 於 harness 內<u>不可量</u>**

### `§一-1`　現查（AST·⛔ 非推定）

`app.py` 之三符號**皆巢狀於 `def main()`**：

| 符號 | 行 | AST 巢狀路徑 |
|---|---|---|
| `_advance_block_with_split` | `:20951` | `def:main › def:_advance_block_with_split` |
| `_solve_one` | `:20623` | `def:main › def:_solve_one` |
| `_build_g_row` | `:20580` | `def:main › def:_build_g_row` |

🔒 **`CLAUDE.md` 常設鐵律逐字**：「`harvest()` 以 AST 跳過 UI、harness 走
`verify/stepg_pipeline.py` 之 `run_step_g`，**`main()` 內之敘述從不被 `run_all` 執行**」。
⇒ 🔴 **改 `app.py:20990-20991` 對 harness 之 `[T2-DIAG]` 零影響。**

### `§一-2`　後果

`N-1` 之主閘 `[T5-FAM2]` 令「切換**前後**之 `R2` 三量並列」。若以**逐街廓 `run_step_g`** 量之
（本批唯一被授權之執行通道），則**切換前後必然逐位相同**——因二者皆走 `stepg_pipeline.py` 之複本，
而該複本**本已是新式**（`:628-629` `_W_prev_left = 0.0`）。
⇒ 🔴 **該閘之判別力為零**：無論切換是否正確，其輸出恆為「三量未變」。

### `§一-3`　⇒ `X-2` 將觸發**受詞不對之假停機**

`X-2` 逐字：「`N-1` 後 `R2` 三量**未同時收斂**（含部分收斂）」⇒ 停機。
在上開結構下，「未收斂」**必然發生**，而其原因**⛔ 非切換失敗**，係**量測通道不存在**。
⇒ 🛑 **CC ⛔ 不施工**——先辦即製造一個受詞不對之停機，且 `app.py` 已被改動。

---

## `§二`　🛑 **問二：`N-1` 判準之<u>方向倒置</u>（三量中二量）**

### `§二-1`　單所令（逐字）

> 三量**同時收斂**至 shim 值（池帶2 寬 `32.9640 → 33.0811`、退化帶數 `2 → 3`、`merged` 段數 `3 → 4`）⇒ ✅ 預測坐實。

### `§二-2`　與【倉】實測之對照

| 量 | app 側（舊式） | shim 側（新式） | 單所令之「app → shim」 | **正確之方向** | 判 |
|---|---|---|---|---|---|
| 池帶2 `s` 寬 | `32.9640` | `33.0811` | `32.9640 → 33.0811` | 同 | ✅ **正** |
| 退化帶數 | **`3`** | **`2`** | `2 → 3` | **`3 → 2`** | 🔴 **倒置** |
| `merged` 段數 | **`4`** | **`3`** | `3 → 4` | **`4 → 3`** | 🔴 **倒置** |

🔒 **成因具名**：單係**逐字轉引 `CLAUDE.md:760`**，而該處之「（池帶2 寬／退化帶數 `2→3`／
`merged` 段數 `3→4`）」記的是 **`177R` 之<u>注入實驗</u>方向**（把 app 舊式**注入 shim** ⇒
shim 由 `2→3`、`3→4`），⛔ **非「app 切換後」之方向**。⇒ **轉引而未轉向。**

⚠️ 池帶2 寬之所以**看起來正確**，是因為 `CLAUDE.md:760` 於該量係寫成「app `32.9640` vs shim `33.0811`」
（**並列式**，⛔ 非箭頭式）⇒ 單據以寫成箭頭時恰好取對；其餘二量本即箭頭式，故照抄即倒置。

### `§二-3`　後果

縱使 `§一` 之通道問題獲解，依單之判準對拍亦將**二量判紅**（`3` 未變為 `3`… 實得 `3→2` 而期 `2→3`）
⇒ 同樣觸發 `X-2` 之假停機。

---

## `§三`　🛑 擬辦之意見（⛔ 不自行改判準·`S-2` 之戒：⛔ 不得改框／母體／容差使其相符）

### `§三-1`　主閘之替代（**建議 (甲)＋(乙) 併用**）

**(甲) 將單之「附閘」升為主閘** —— 單 `N-1` 已載：
> 🔑 **附閘（機械·⛔ 不可省）**：切換後原樣重跑 `probe_WG9189_s6_replica.py`，**實質差須由 `20` 降為 `19`**，且消失者須恰為清單**第 `1` 項**。

🔒 **此閘可量、有判別力、且直證受詞**——它證明的正是「app 於該點已與 stepg 逐位同」，
而族②之全部內容即此。**⇒ 建議以之為主閘。**

**(乙) 注入式對拍（`177R` 同法·本批 `S-5` 已備）** —— 於 harness 內以 spy 復現二側：
> **切換前**：不注入 ⇒ shim 值；注入 app **舊式** ⇒ `(32.9639559294, 3, 4)` ＝ app 值 ✅（本批已跑）
> **切換後**：app 之式**即 `0.0`** ⇒ 「注入 app 新式」≡「不注入」⇒ 其三量 ＝ shim 值
> ⇒ **二造合一**即族②消失之直證。

🛑 **(乙) 之限**：其所量者為 **harness 內之模擬**，⛔ 非 app 路徑之實跑；
其效力止於「若 app 用該式，則得該三量」。逐字具名，⛔ 不以之冒充 app 實跑。

**(丙) KL UI 實跑** —— 唯一能真正量 app 路徑者。本單⛔ 未授權（`189` 單 `X-5` 亦禁）⇒ **⛔ 不辦**。

### `§三-2`　判準之更正（若採 (乙)）

建議將 `N-1` 之判準逐字改為：
> 切換後，**「注入 app 舊式」與「不注入」二造之 `R2` 三量須分別為 `(32.9639559294, 3, 4)` 與
> `(33.0810700422, 2, 3)`**，且**「app 之式」已等同後者**；即三量由 `(…, 3, 4)` **收斂至** `(…, 2, 3)`
> —— 🔒 **退化帶數 `3 → 2`、`merged` 段數 `4 → 3`**（⛔ 非單所載之 `2 → 3`／`3 → 4`）。

### `§三-3`　`CLAUDE.md:760` 之併記（⛔ 本批未改·僅具名）

該處之箭頭係注入方向。建議於 `N-4 5.` 之末端追加中**併記其方向**，
使後續轉引者⛔ 不再倒置（⛔ 不追改原文一字·`常規四（九）五`）。

### `§三-4`　`N-2`／`N-3`／`N-4` 之處置

- `N-2`（族③）：單逐字令「`N-1` **未全項通過前⛔ 不得開始**」⇒ **⛔ 未辦**。
- `N-3`（四分分類器）：其為 commit 3，且單令「須在切換**之後**」⇒ **⛔ 未辦**。
- `N-4`（六項純登記·⛔ 零生產碼）：**⛔ 未辦**——其與 `N-1` 同批交付，本批停機故一併順延。
  🛑 **惟 CC 認 `N-4` 六項<u>不依賴</u> `N-1`**，若發單側願，可令其**單獨先行**（零生產碼·可自辦 push）。

---

## `§四`　⛔ 本批未辦（逐項具名）

⛔ **未動 `app.py` 一字**（`sha16` 期末 ＝ `2be36e4c88b6e129` ＝ 期初）／⛔ 未動 `CLAUDE.md`／
`verify/stepg_pipeline.py`／各登記表一字／⛔ 未辦 `N-1`〜`N-4`／⛔ 未鑄任何號／
⛔ 未跑 `run_all`／⛔ 未請 KL／⛔ 未改任何既有閘或判準。

---

## `§五`　異動清單

| 檔 | `+` | `−` | 類 |
|---|---|---|---|
| `verify/probes/probe_WG9190_baseline.py` | 新檔 | — | `S-5` 基準量（含注入復現） |
| `verify/out/probe_WG9190_baseline_25857aa.log` | 新檔 | — | `S-5` 之出艙 |
| `verify/out/probe_WG9190_S6_open.log` | 新檔 | — | `S-6` 開工復現 |
| `docs/reports/W-G.9-190R_停機_主閘不可量與方向倒置.md` | 新檔 | — | 本報告 |

🔒 **基座檔 `deletions` ＝ `0`**（四者皆新檔·`X-8` 未觸）。
🔒 絕對路徑機檢：本批新增檔命中 **`0`**（探針之 `REPO` 自 `__file__` 推得）。

---

## `§六`　clone 之刪除

`C:\w190a`（clone）／`C:\w190x`（自檢器）⇒ 跑畢整份刪除，並以**對照組**（`/c` 下項數）確認量測器非紅。
（確認之數載於聊天回報。）
