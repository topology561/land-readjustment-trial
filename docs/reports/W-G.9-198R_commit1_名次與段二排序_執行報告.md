# 🧾 `W-G.9-198R` 執行報告（`A-2`·**`commit 1` 備妥但未推**）：街角指數<u>名次</u>之產出與出艙 ＋ 段二全域排序 ＋ 措辭更正

> **執行窗**：CC　**基座**：`origin/wip/s1-endpart` ＝ `6a3f5feb81823f8ff84be7d269fa78056d3e3556`（開工時 `ls-remote` 實查）
> **`A-1` 已入倉並推**：`dd39f34`（零生產碼·單以原名入倉）
> **本報告之 commit**：零生產碼·CC 逕推；🔴 **置於 `commit 1` <u>之前</u>**——理由見 `§零-b`
> **工作副本**：主 checkout；harness 長跑一律於**拋棄式 clone**（`C:/Users/admin/AppData/Local/Temp/w198`·`38` 字元短路徑·`-c core.autocrlf=false -c core.quotepath=false`·detach 於基座）

🔴 **`commit 1` 之狀態 ＝ 備妥、⛔ 推**（單 `§三-3`）。🛑 **放行之粒度**（單 `§七`）：發單側**逐 commit** 請示 KL，⛔ 一次放行三個；**`commit 1` 放行後方進 `commit 2`** ⇒ 本窗於此**停機待放行**，`commit 2`／`3` **⛔ 開工**。
🛑 **土地後果⛔ 確認·待 KL UI 實跑**（`GB-123` ⛔ 解除）。
🔒 本檔一切計數皆於同段具名 ① 母體 ② 框 ③ **產生它的命令逐字**。本報告**⛔ 鑄任何號**。

---

## `§零`　開工閘 `S-0a`〜`S-0e`

| 閘 | 實測 | 判 |
|---|---|---|
| `S-0a`① | `git rev-parse HEAD:app.py` ＝ **`b9a50a2e58ad46ac3d4288bad978c17e87b8fb27`** ＝ 單載 | ✅ |
| `S-0a`② | `git -c core.quotepath=false diff --name-only -z 7fab020..HEAD` ＝ **`26`【檔】**；`app.py ∪ verify/**.py` 命中 **`1`**【列】 ＝ `verify/probes/probe_WG9201_side_assign.py`。判別力對照：`.md` ＝ **`22`**【列】 | 見 `§零-a` |
| `S-0a` 記錄項 | `git ls-remote … refs/heads/wip/s1-endpart` ＝ **`6a3f5feb81823f8ff84be7d269fa78056d3e3556`**（全 `40` 碼·🛑 **⛔ 作判準**） | 🟡 記錄 |
| `S-0b` | `PYTHONIOENCODING=utf-8 python verify/probes/probe_order_preflight.py docs/orders/W-G.9-198R_…md` ⇒ 🔴 **`0`【項】**／🟡 **`5`【項】** | 見 `§零-c` |
| `S-0c` | `P-5`：`declared` ＝ `recomputed` ＝ **`b291070e2bf1ee3aaf2c20200f385f64aef5c62e99016e8550548ba81d3dab54`**（受詞 `26798` B／全檔 `26876` B·`246`【列】·`CR` `0`） | ✅ |
| `S-0d` | 改前基線已凍存 | 見 `§一` |
| `S-0e` | 🆕 `K-9-25` 之碼面查核 ＝ **無** | 見 `§二` |

### `§零-a`　🔴 `S-0a`② 之單內互斥（依 `常規二`：取保守項續辦 ＋ 逐字回報）

**單之判準逐字** ＝ 「② `git diff --name-only 7fab020..HEAD` **逐列⛔ 含** `app.py`、⛔ 含任何 `verify/**.py`」。
**實測** ＝ `verify/**.py` 命中 **`1`**【列】＝ `verify/probes/probe_WG9201_side_assign.py`（`W-G.9-201` 所新增之唯讀探針·commit `ad2b5f9`）⇒ **依單逐字為紅**。
🔴 **同閘之立法理由逐字**（同格）＝「倉內合法多出任意數量之**零生產碼** commit 皆⛔ 得使本閘紅」；而 `CLAUDE.md`「🔧 二」之**生產碼機械界定僅四檔**（`app.py`／`verify/stepg_pipeline.py`／`verify/run_all.py`／`verify/run_verification.py`），**⛔ 含 `verify/probes/**`** ⇒ `ad2b5f9` 係**合法之零生產碼 commit**（其 commit 訊息與 `🔧 二` 之判法皆如此分類）。
⇒ **判準之字面與其立法理由互斥。** 🛑 **保守項** ＝ 依**立法理由**與「🔧 二」之機械界定判之：該命中**⛔ 使本閘紅**。
🔑 **旁證**：`W-G.9-203`（同一發單側·同一形之閘）**明文設有該探針之豁免**並逐字載「本豁免係本單新增——`-201` 已合法新增一支唯讀探針，若沿用舊式判準，本閘**會在合法基座上變紅**（承 `自誤 252` 之一般式）」；本重發本**⛔ 承接該豁免**，係**漏承**。
🛑 **上呈發單側**（⛔ 上呈 KL·`常規二`）：請補述該豁免或改綁性質。

### `§零-c`　`S-0b` 之 🟡 `5` 項（🛑 **以字樣錨具名·⛔ 用行號**·逐筆歸類依裁 `H`）

| 檢 | 字樣錨（`grep -F` 固定字串） | 命中檢 | 歸類與處置 |
|---|---|---|---|
| `P-1`／`P-4` | `` \| # \| 落地後須成立之性質 \| `` | `P-1` ×2、`P-4` ×1 | **判別力參照 ⇒ 具名豁免**（＝ 單 `§一-c` 之第一列）：係結果態之**索引表頭**；各列之母體、框與方向逐列自載於該列本文 |
| `P-1` | `` \| 閘 \| 受詞 \| 判準 \| `` | `P-1` ×1 | **判別力參照 ⇒ 具名豁免**（＝ 單 `§一-c` 之第二列）：係閘之索引表頭 |
| `P-4` | `` - **`K-9-24`**（標題列 `` | `P-4` ×1 | **零參照 ⇒ 具名豁免**（＝ 單 `§一-c` 之第三列）：係正典標題列之引用 |

🔒 **合計 `5`【項】＝ 單 `§一-c` 所載之 `5`**，**⛔ 表外之 🟡**；🔴 機械停機款 **`0`**。
🔑 **判別力自證（⛔ 另造·取既有實跑）**：同器對 `-203` 得 `0`【項】／`-205` 得 `1`【項】（CC 本窗實跑）⇒ 該器**⛔ 恆綠**；其對 `-197` `5`／`-198` 改版 `6`／`-199` `0`／`-200` `2`／`-201` `1` 之既有值見單 `§一-c`。

---

## `§一`　`S-0d`　改前基線之凍存（🛑 ⛔ 於改後回頭重建）

**通道**：拋棄式 clone 內 `PYTHONIOENCODING=utf-8 python verify/run_all.py`（`rc=1` ＝ 本分支之**准紅碼**·`CLAUDE.md` 已載）。
🔴 **主 checkout ⛔ 供長跑之實證**：於主 checkout 試跑一次後，`git status --porcelain` 之**已追蹤異動由 `0` 變 `3`**——`verify/out/probe_ruling_K4_s_origin.log`／`probe_ruling_K8_baseline_pairing.log`／`probe_ruling_K8_sideline_pairing.log`（浮點末位差·`i/lf w/crlf`）。已以 `git checkout --` 逐檔復原、`status` 回空、`eol` 回 `i/lf w/crlf`。**其後全部長跑改於 clone 內為之。**

| 凍存物 | 量 |
|---|---|
| `got_診斷_退縮3.5m_6a3f5fe.csv`（＝「第 1 宗街角地逐候選評點明細」） | `20` 資料列 × **`23`** 欄 |
| `got_診斷_退縮0m_6a3f5fe.csv` | `17` 資料列 × `23` 欄 |
| `got_指配_退縮3.5m_6a3f5fe.csv`／`got_抵費地_退縮3.5m_6a3f5fe.csv` | 併存 |
| **四閘違規全集（不截斷）** `viol_full_6a3f5fe.json` | `88517` B；`v3·診斷0m` **`96`**／`v3·診斷3.5m` **`104`**／`無串聯0m` **`96`**／`無串聯3.5m` **`104`** |

🔒 既有 `23` 欄與單 `R1-4` 之「既有 `23` 欄」**逐值相符**。

---

## `§二`　`S-0e`　🆕 `K-9-25` 之碼面查核 ＝ **無**（⇒ `R2-4` 落為禁令·碼面⛔ 須改一字）

**母體** ＝ `app.py` 與 `verify/stepg_pipeline.py` **二檔**（讀 `HEAD` blob）·框 ＝ 列。
**正面列舉**（街角評選量測路徑之函式·AST 實查其區間後於區間內掃描）：

| 函式 | 區間（基座） | 密合性字樣命中 | 逐處具名 |
|---|---|---|---|
| `select_corner_lots_both_sides_v12` | `12594-12963` | **`2`** | `:12784` 係**註解**（`forced buffer 落點判定`）；`:12847` ＝ `cp = cp.buffer(0)`，**由 `if not cp.is_valid:` 守衛** |
| `_build_corner_range_v3` | `12235-12519` | **`2`** | `:12492` ＝ `blk = blk.buffer(0)`、`:12516` ＝ `rng = rng.buffer(0)`，**皆由 `if not X.is_valid:` 守衛** |
| `_pk_one_side_v12`／`_corner_block_true_G`／`_boundary_len_on_line`／`_chamfer_line_for_side`／`_make_chamfer_tri_wb`／`_corner_first_lot_G` | 各自區間 | **`0`** | — |

**二檔全域**：`snap(` ＝ **`0`／`0`**；`make_valid` ＝ **`0`／`0`**；路徑上**無任何非零 `buffer`**。

🔒 **判讀**：`buffer(0)` 係 **單一幾何之自交修復**（shapely 慣用法），**結構上⛔ 能**閉合**兩個相異幾何之間**的縫隙 ⇒ ⛔ 是 `K-9-25` 所禁之「**密合、補齊、吸收或歸併**」。
🔑 **最強證據為<u>性質面</u>而非計數**：若量測路徑上曾有密合前處理，`K-9-25 五` 所具名之 **`7.9615 ㎡`** 無地號縫隙**不會存在於量測結果中**；而 `W-G.9-201` 探針**實測其存在**（`R4` 左右真交集 `225.76`／`225.19` vs 範圍 `226.01`／`225.42`）。
⇒ 依單 `R2-4`：現況**本無**此類前處理 ⇒ 本項**落為禁令（⛔ 新增）**、**碼面⛔ 須改一字**。

---

## `§三`　`§二`（單）現況正面列舉之**自行復算**（🛑 ⛔ 照抄單表）

**母體** ＝ `app.py` 與 `verify/stepg_pipeline.py` 二檔（`HEAD` blob）·框 ＝ 含該字樣之**列**。

| 字樣 | `app.py` | `stepg` | 單載 | 判 |
|---|---|---|---|---|
| `上鎖`／`未上鎖`／`預掃`／`集中規則`／`K-6-B`／`歸側`／`K-9-24`／`K-9-25` | 逐項 **`0`** | 逐項 **`0`** | `(0, 0)` | ✅ |
| `合併群`（對照組） | **`7`** | **`0`** | `(7, 0)` | ✅ |
| `縫隙`（對照組） | **`3`** | **`0`** | `(3, 0)` | ✅ |
| `k6_step0_block_locked` | **`3`** | **`0`** | `(3, 0)` | ✅ |

🔒 **`k6_step0_block_locked` 之三處逐處具名**：`app.py:11567`（`def`）／`:11631`（註解）／`:11632`（呼叫 `if k6_step0_block_locked(side_lines_by_side, _lbl):`）——`commit 2` 之 `R2-1` 受詞。
🔑 **補充現查（單未列·供 `R1` 之基線）**：`名次`／`priority_rank`／`指數名次` 於二檔改前皆 **`0`**；`排名` 於 `app.py` `0`、`stepg_pipeline.py` **`4`**（既有異義用法·⛔ 本批受詞）。
⇒ 量測器**⛔ 恆紅**（對照組有值）亦**⛔ 恆綠**（受詞為零）。

---

## `§四`　`commit 1` 之落地（🔴 **備妥·⛔ 推**）

### `§四-1`　變更檔與 `numstat`

**命令逐字** ＝ `git -C <M> -c core.quotepath=false diff --numstat`

| 檔 | `+` | `-` | 新 blob（`git -c core.autocrlf=true hash-object --path <f> <f>`） | 舊 blob |
|---|---|---|---|---|
| `app.py` | `110` | **`1`** | **`6f0317e47ef92c7e06d6d904ecf2d129656bd331`** | `b9a50a2e58ad46ac3d4288bad978c17e87b8fb27` |
| `verify/selection_pipeline.py` | `11` | `0` | **`10f90c509ae13c77c83e3e5c76fe3c09ca675b14`** | `4ba5b6f41153939907825f6d2c4d73a213a11419` |
| `verify/run_verification.py` | `3` | `0` | **`d07955c58e67ec06260971a74b5aabe5e846f106`** | `85e34d67eb8ca388b8af05a0060e4a1b4ea645a9` |

🔴 **唯一之刪除列（`-1`）逐字** ＝ `    # 🆕 V13 修正 #3 + #7：第二關優先權指數採用三項法定加權` ——即 **`R1-8` 所令之措辭更正**本身。（命令：`git diff -U0 | grep -E '^-[^-]'` ⇒ 恰 `1` 列。）

### `§四-2`　🔴 **射程之逐字回報**（單之「性質」只名 `app.py`）

單之性質列逐字 ＝ 「🔴 **幾何影響批·含生產碼**（`app.py`）」，惟本 `commit 1` 另動**二檔**：

| 檔 | 動之緣由（逐項） |
|---|---|
| `verify/selection_pipeline.py` | `R1-3` 令**出艙**增欄。harness 側之「第 1 宗街角地逐候選評點明細」（＝ `got_診斷_退縮*.csv`·亦即 `D1-a` 之比對受詞）**由本檔之 `_corner_cand_diag` 產生**（其註解逐字自載「app 13904-13949 **原樣**」）⇒ **⛔ 動本檔則該欄結構上⛔ 可能出現於 `D1-a`／`D1-c` 之受詞內**。所加者為**同構之三處**（累積逐側 PK 結果、增欄、段二呼叫），逐行同 app。 |
| `verify/run_verification.py` | **harness 自身之守衛所明文指示之處置**：`diff_rows` 之 `U-K2` 對新欄出艙「**未宣告之 got-only 欄** `['指數名次']`……請於 `_DECLARED_EXTRAS` 具名申報（欄名＋理由＋加入日），**或補入 baseline**」。後者 ＝ 重烤，係單 `§八` 明文**⛔ 辦**。⇒ 取前者。體例逐字同**倉內既有先例** `真G(㎡)`（`2026-07-27` 宣告·同一理由）。`declared_extras_for` 取 `os.path.basename` ⇒ **二筆涵蓋四閘**。 |

🛑 **此係射程之擴張，逐字上呈發單側**；二檔之改動**皆為 `R1-3` 之必要條件**，⛔ 新增任何規則、⛔ 改任何既有比較邏輯。

### `§四-3`　`§三-3` 所令之**新 AST 區間**（🛑 ⛔ 沿用舊值·`ast` 實查）

| 函式 | 基座（單 `P-3`／`S-0e` 所載） | **`commit 1` 後** |
|---|---|---|
| `def main` | `14242`-`23460` | **`14326`-`23569`**（`9244` 列） |
| `_pk_one_side_v12` | `12966`-`13059` | **`12966`-`13076`**（`111` 列·`+17`） |
| `select_corner_lots_both_sides_v12` | `12594`-`12963` | `12594`-`12963`（`370` 列·**不變**） |
| `k6b_stage2_global_order` 🆕 | （不存在） | **`13079`-`13143`**（`65` 列） |

### `§四-4`　補丁之位元組級自證

工作樹為**純 CRLF**（`app.py` `CR ＝ LF ＝ 23462`）⇒ 補丁以位元組級為之、錨與新碼轉 CRLF 後替換；**逐處自證 `count == 1`**（`app.py` `7` 處／`selection_pipeline.py` `4` 處／`run_verification.py` `1` 處 ＝ **`12` 處**），且**寫入後行尾仍均質**（`CR == LF`）。
`app.py` `1407455`→`1415468` B（`Δ+8013`·`+109` 列）；`selection_pipeline.py` `33663`→`34526` B（`Δ+863`·`+11` 列）；`run_verification.py` `98123`→`98397` B（`Δ+274`·`+3` 列）。

---

## `§五`　`commit 1` 之驗收 `D1-a`〜`D1-h`

| 閘 | 實測 | 判 |
|---|---|---|
| `D1-a` | **土地欄逐格有變 `0` 格**（`3.5m`：`9` 欄 × `20` 列 ＝ `180` 格；`0m`：`9` × `17` ＝ `153` 格。欄 ＝ `真交集`／`整筆幾何`／`範圍面積`／`G估`／`真G`／`門檻`／`達標`／`原位次(投影序)`／`選中`；鍵 ＝ `街廓`＋`端`＋`候選地號`；鍵集僅前／僅後皆 `0`） | ✅ |
| `D1-b` | 四指數欄（`正街角分`／`側街分`／`跨占分`／`總分`）**數值真變 `0` 格**；**純字面差 `0` 格** ⇒ `R1-4` 之「例外預告」**未被觸發** | ✅ |
| `D1-c` | `指數名次` 相異值 ＝ `{1,2,3,4}`（`3.5m`）／`{1,2,3}`（`0m`）⇒ **⛔ 全同值**；空白或 `—` 之列 ＝ **`0`**（`R1-3`）；**逐側**名次連續 `1..n` **且**與 `總分` 之降冪序（平手取 `原位次` 升冪）**逐側一致 ＝ `True`**（`3.5m` `8` 側／`0m` `8` 側·機械驗·⛔ 目視） | ✅ |
| `D1-d` | 段二鍵序① 之接線機械證：對照序 ＝ `['628-41(1)','628-42(1)','628-27(1)','628-40(1)+']`；**擾動 `628-41(1)` 之 `指數名次` `+100` ⇒ 序變為 `['628-42(1)','628-27(1)','628-40(1)+','628-41(1)']`**（**序有變 ＝ `True`**）。🔑 **零對照**：擾動一個**不在表內**之地號 ⇒ 序**不變 ＝ `True`** | ✅ |
| `D1-e` | 段二出艙**存在**且**⛔ 有任何呼叫端消費**——正面列舉（母體 ＝ `git ls-files` 之 `app.py` ∪ `verify/**.py` ＝ `242`【檔】）：`k6b_stage2_global_order` **`3`**【列】＝ `app.py:13079`（**定義**）／`app.py:20134`（**產出**）／`verify/selection_pipeline.py:566`（**產出**）；`f3_k6b_stage2_order` **`3`**【列】＝ `app.py:20135`（**產出**）／`selection_pipeline.py:565`（註解）／`:566`（**產出**）。**⛔ 任一處為「消費」** | ✅ |
| `D1-f` | 四閘違規全集（`probe_WG9168_viol_full`·**不截斷**）二態差集：`v3·診斷0m` `96`⇒`96`／`v3·診斷3.5m` `104`⇒`104`／`無串聯0m` `96`⇒`96`／`無串聯3.5m` `104`⇒`104`；**僅前 `0`／僅後 `0`** ⇒ **合計 `0` 格** | ✅ |
| `D1-g` | `--selftest`：**倉內查無**（見 `§五-1`）。**⛔ 刪改任一既有自檢**：全批被刪之列 ＝ **`1`**（逐列具名於 `§四-1`·係 `R1-8` 所令之註解更正）；自檢計數（框 ＝ `assert `∪`raise RuntimeError`∪`_eq(`）`app.py` `135`→`139`／`selection_pipeline.py` `3`→`3`／`run_verification.py` `10`→`10` ⇒ **皆未減** | ✅ |
| `D1-h` | 見 `§五-2` | ✅（附一項**⛔ 可觀測**之照實回報） |

### `§五-1`　`D1-g` 之受詞倉內查無（照實回報·⛔ 以近似物頂替）

`--selftest` 於**生產碼四檔**之命中（`grep -c -- '--selftest' <f>`）：`app.py` **`0`**／`verify/stepg_pipeline.py` **`0`**／`verify/run_all.py` **`0`**／`verify/run_verification.py` **`0`**。**判別力對照**：`verify/probes/probe_order_preflight.py` ＝ **`6`** ⇒ 量測器⛔ 恆零。
⇒ 🛑 **`app`／harness 側⛔ 存在名為 `--selftest` 之入口**（同 `W-G.9-201R §三-1` 之既有結論）。改以下列**結構證明**滿足其實質：
① **模組級自檢**：`harvest()` 於 clone 內 exec `app.py`（`k6_merge_groups`／`k6_shares_segment` 之 `S-1`〜`S-7` 等）**未 raise**；`ns` 符號 `270`、`k6b_stage2_global_order` 在 `ns` ＝ `True`。
② **`run_all` 之 `34` 閘**（`self_check_diff_engine` 等在內）之**名目與 PASS/FAIL 逐列逐字相同**（見 `§五-2`）。
③ 被刪列 ＝ `1`（非自檢）；自檢計數皆未減。

### `§五-2`　`D1-h`　既有終局⛔ 移位

🔴 **單所指之終局受詞（`k94_assert_baseline_touch`／`R5` `628-53(2)`／差 `38.803m`）於 harness 之 `run_all` stdout <u>二態皆 `0` 命中</u>**（判別力對照：`對帳 FAIL` ＝ `1`／`1` ⇒ 量測器非紅）⇒ 該終局係 **app 路徑**之終局（`W-G.9-197R` 所載係 **KL UI 實跑**），**結構上⛔ 於 harness 可觀測**（`GB-123`）。⇒ 🛑 **該款須待 KL UI 實跑方得確認**，本報告**⛔ 宣稱其已驗**。

**harness 側之 operative 受詞則綠**：

| 受詞 | 實測 |
|---|---|
| `run_all` 之 `34` 閘之**名目與 PASS/FAIL** | **逐列逐字相同 ＝ `True`**（`grep -E '^  (✅ PASS\|🔴 FAIL)'` 二態各 `34` 列·`diff` 空） |
| `run_all` stdout 二態 `diff` | **`64`**【列】，**全屬 `CLAUDE.md` 對帳三分類之<u>類 II</u>**（僅 traceback 行號差）——`verify/run_verification.py` 之 `740`→`743`／`949`→`952`／`1026`→`1029`／`1077`→`1080`／`1143`→`1146`／`1210`→`1213`／`1309`→`1312`／`1408`→`1411`（各出現於「直接 traceback」與「現況 echo」二處 ⇒ `8 × 2 × 2 = 32` 列，另 `32` 列為 `diff` 之標記與分隔）。**⛔ 逐項自動視為同**：`+3` 之位移**逐項可歸因於 `_DECLARED_EXTRAS` 所增之 `3` 列**。 |
| 非行號位移之殘餘列 | **`0`**（命令：`grep -vE 'run_verification\.py", line\|^---$\|^[0-9]+c[0-9]+$' <diff>` ⇒ 空） |

---

## `§六`　`R1-6` 之出艙實況（段二全域排序）

`session_state['f3_k6b_stage2_order']`（app 與 engine **同鍵**）·情境 `SB = 3.50 m`：

| 街廓 | 端 | 暫編地號 | 名次 | 街角試算G(㎡) | 指數數值 | 最終序位 |
|---|---|---|---|---|---|---|
| `R2` | 左 | `628-41(1)` | `1` | `175.95` | `0.89727` | **`1`** |
| `R2` | 左 | `628-42(1)` | `2` | `4.28` | `0.051927` | **`2`** |
| `R2` | 左 | `628-27(1)` | `3` | `26.34` | `0.032177` | **`3`** |
| `R2` | 左 | `628-40(1)+` | `4` | `286.65` | `0.018626` | **`4`** |

🔒 **「僅含待處理街角」之機械自證**（`8` 側逐側）：段二所含之 `(街廓,端)` ＝ `[('R2','左')]`；全部 PK 之 `(街廓,端)` ＝ `8` 個；**逐側「達標數 ＝ 0 ⟺ 入段二」＝ `8/8` 相符**（`R2/左` 達標數 `0` 入表；其餘 `7` 側達標數 `1`〜`2` 皆不入）。

### `§六-1`　🔴 **一項正典未載之受詞（CC 之技術判讀·上呈發單側）**

正典 `K-6 §二 段二` 逐字只給鍵**序**（「① 街角指數**名次** → ② 街角試算 **G** → ③ 街角指數**數值** → ④ 暫編地號**字典序最小**（最終回退）」），**⛔ 給 ②③ 之<u>方向</u>**（僅 ④ 明載「最小」）。
本實作採：① 名次**升冪**（名次定義所內生）／② `G` **降冪**（與 `Phase 8 防護一`「大塊合法街角地⛔ 得被微小畸零地淘汰」同向）／③ 指數**降冪**（與 winner 取 max 同向）／④ 地號**升冪**（正典逐字）。該判讀**已逐字寫入 `k6b_stage2_global_order` 之 docstring**。
🛑 **段三尚未落地（`VR-086`）⇒ 本序今日<u>無消費端、無土地後果</u>**（`R1-7`／`D1-e` 已機械證）；**②③ 之方向須於段三落地<u>前</u>經發單側確認**。

---

## `§七`　`§六`（單）之附帶量測（🛑 ⛔ 阻塞驗收）

| # | 實測 |
|---|---|
| `M-1` | 倉內既有錨逐字（`git show HEAD:verify/out/probe_WG9187_baseline_76378d09.log` 之第 `14` 列）＝ `②-宗 圍堵閘破[R5]：宗-宗重疊 = 56.8186 > 上界 1.5999（(宗數8−1)×0.005×深度45.71）`。🔴 **本批二態之 `run_all` stdout 內 `②-宗 圍堵閘破[R5]` 命中 `0`**——該閘於本 harness 通道係於 `R2` 先行破（`45.9766 > 2.8906`）而未及 `R5` ⇒ **`56.8186` vs KL `7fab020` 實跑之 `56.8187` 之比對，於本通道⛔ 可觀測**；照實回報，⛔ 修、⛔ 鑄號，繫 `GB-67`。 |
| `M-2` | `Phase 8 抵費地碎片合併產生 MultiPolygon` 於二態之 `run_all` stdout 命中**皆 `0`**（`base` `0`／`c1` `0`）⇒ **二態逐字不變**（惟其於本通道⛔ 出現）；⛔ 修。 |
| `M-3` | 🛑 **屬 `commit 2` 之受詞**（`R4` 內無地號面之面積與臨左／臨右截角長之實算），本報告**⛔ 辦**——`commit 2` ⛔ 開工（見 `§八`）。 |

---

## `§八`　`§零-b`／停機點

### `§零-b`　本報告置於 `commit 1` **之前**之理由（🔴 結構性）

單 `§七` 令 `A-2` 為「零生產碼 commit，**CC 可逕推**」，且其內容 ＝ 三生產碼 commit「**備妥但未推**」之量測。
🔴 **git 之推送係線性**：若 `A-2` 之 commit 位於 `commit 1` **之後**，則推 `A-2` **必連帶推出未放行之 `commit 1`** ⇒ 與 `§三-3`「⛔ 自行 `push`」**直接互斥**。
🛑 **保守項** ＝ 令 commit 序為 **`A-1`（已推 `dd39f34`）→ `A-2`（本報告·可推）→ `commit 1`（備妥·⛔ 推）**。本序**⛔ 違反任一條文**：`§七` 只令 `A-1` 置於 `commit 1` 之前、⛔ 定 `A-2` 之位置，且 🔒「`A-1`／`A-2` 與 `L-*` ⛔ 得綁在同一 commit（**同批、不同 commit**）」亦滿足。
🔒 **代價之具名**：本報告因此**⛔ 能載 `commit 1` 之 SHA**（其時尚未存在）——改以 **`app.py` 新 blob ＋ `numstat` ＋ 新 AST 區間**（`§四`）標識之，此即 `§三-3` 所令之回報項；放行時之「同一物件」核驗（`自誤 246`：全 `40` 碼 ∧ `tree` ∧ `numstat` ∧ `app.py` blob）**其中三項已載於本報告**。

### `§八-1`　🛑 停機點

依單 `§七` 之**放行粒度**（「發單側將**逐 commit** 請示 KL，⛔ 一次放行三個。`commit 1` 放行後方進 `commit 2`」）：
- **`commit 1` 備妥、⛔ 推** ⇒ **本窗於此停機待放行**。
- **`commit 2`／`commit 3` ⛔ 開工**（其結果態 `R2-*`／`R3-*` 一字未動；`app.py` 之 `k6_step0_block_locked` 三處**逐字未觸**）。
- 🔴 **土地後果⛔ 確認·待 KL UI 實跑**（`GB-123` ⛔ 解除）。`commit 1` 之**預期**為「⛔ 有土地後果」，`D1-a`（`333` 格）與 `D1-b` 皆 `0` 格，惟該二閘係 **harness 側**；app 路徑之確認繫 KL。

### `§八-2`　三項上呈發單側（⛔ 上呈 KL·`常規二`／`常規三`）

1. **`§零-a`**：`S-0a`② 之字面與其立法理由互斥（本重發本漏承 `-203` 已明文設立之 `-201` 探針豁免）。
2. **`§四-2`**：射程之擴張——`commit 1` 另動 `verify/selection_pipeline.py`（`R1-3` 之必要條件）與 `verify/run_verification.py`（harness 守衛之明文處置），而單之性質列只名 `app.py`。
3. **`§六-1`**：正典 `K-6 §二 段二` **⛔ 載 ②③ 之排序方向**；CC 已依理由擇定並逐字寫入 docstring，須於**段三落地前**確認。

---

## `§九`　⛔ 本報告不辦

⛔ 推 `commit 1`（單 `§三-3` 明令·待發單側請示 KL 放行）。
⛔ 開工 `commit 2`／`commit 3`（單 `§七` 之放行粒度）。
⛔ 動 `k6_step0_block_locked`／`k6_merge_groups`／`k6_shares_segment` 一字（本批逐字未觸）。
⛔ 重烤或重產任何基準（單 `§八`）；⛔ 改 `core.autocrlf`。
⛔ 鑄任何號（`自誤`／`GB`／`VR`／`戒`／`K-9` 皆未動）。
⛔ 辦 `M-3`（屬 `commit 2`）／`GB-67`／`R3` 之 MultiPolygon。
