# `W-G.9-1` 前置偵察：**事前登記**（⛔ 查之前 commit·**零量測**）

**產出**：CC／2026-08-10　**分支**：`wip/s1-endpart`
**基準**：`fd6228a27d238f22f423adfc982a405969c742f8`
**批號 `W-G.9-1` 現查未占用**（`grep -rn "W-G.9-1" . --exclude-dir=worktrees --exclude-dir=.git` ⇒ **0**）；
`GB-59`／`GB-60` 亦各 **0 命中**。

> 🔒 本檔須 commit 於**任何 Q1／Q2 量測之前**；事後以
> `git merge-base --is-ancestor <A> <B>` 貼機械時序證明。
> **預測與結果不符 ⇒ 據實改答並具名，⛔ 不得回頭修改本檔一字。**

---

## §0 「已跑／未跑」聲明（⛔ 誠實界定，否則本登記無效）

**已跑（本登記之前）**：

- 施工單 §0 之編號／批號占用檢查、`HEAD` 與工作樹狀態。
- 施工單 §3-1〜§3-9 之**事實複驗**（皆為倉內 `grep`／`wc`／`ls`，⛔ 非 Q1／Q2 之量測）。
- ⚠️ **併記（跨批·誠實揭露）**：本 session 之 `D-2b-30` 批中，我已讀過
  `_corner_buffer_S` 全文與 `_block_strip` 之 `n_hat` 指派段。
  ⇒ **`P1` 之方向與該次閱讀一致**；惟 `P1`〜`P5` 之**文字係施工單所給**，⛔ 非我事後所擬。

🔴 **未跑（本登記之受詞）**：⛔ **尚未讀取或執行任何**針對
`_cos_dn` 之定義、`_strip_axis` 之函式本體、`_mp_base_W0` 之構造、
以及 Q1-a／Q1-b／Q2 之任何量測。

---

## §0-1 🔴 施工單 §3-1 之數與倉態不符（具名·⛔ 不逕改單之敘述）

施工單 §3-1 載該窮舉指令之輸出為 **33**；**實跑為 `31`**：

```
$ grep -rn "_left_buffer_S\|_right_buffer_S" --include=*.py . | grep -v "^\./\.claude/worktrees/" | wc -l
31
$ …同上… | awk -F: '{print $1}' | sort | uniq -c
     19 ./app.py
      2 ./verify/probes/probe_K9_s62c_attribution.py
     10 ./verify/stepg_pipeline.py
```

**已試之變體皆非 33**：不排除 worktree ⇒ `31`（本 worktree 內無巢狀 worktree）；
不限 `*.py` ⇒ `63`；僅 `app.py` ＋ `verify/*.py` ⇒ `29`。
⇒ **`33` 於基準 SHA 不可複現**，成因不明。

🔒 **對本單之影響：無實質影響**——§3-1 表列之**五族指認式逐一命中且行號與單所載相符**
（①`app:19568`／`stepg:608`、②`app:19571`、③`app:19788`、④`stepg:821`、⑤`app:19487`）。
⇒ 窮舉之**母體改以 `31` 為準**，並於 commit B 逐條分類、附差集自證。

**施工單 §3 其餘各項複驗結果**：§3-2／§3-3（`:612`／`:799`）／§3-4（`app:11077`）／
§3-5（`app:8732`）／§3-6（`stepg` 6 行／`wf_f1` 4 行／`wf_f4` 4 行 ＋ `app.py`）／
§3-7（`verify/out/` **220** 檔·`cos_dn` **0** 檔）／§3-9（`74` 行·命中 **0**）
——**全部與單所載相符**。

---

## §1 預測（⛔ 逐字採施工單 §4 之 `P1`〜`P5`·各附證偽方式）

| # | 預測 | 證偽方式 |
|---|---|---|
| **P1** | `_corner_buffer_S` 回傳之 `buf` 與 `S1_par` **同度量**（皆為沿 `d̂` 參數化之路平行距） | 對**同一條界線**以三法各自獨立量測（甲＝`_strip_axis` 之 s 定義式／乙＝沿 `d̂` 之路平行距／丙＝沿 `â` 之垂距），若 `buf` 恆等於丙而非甲乙 ⇒ **P1 破** |
| **P2** | `_cos_dn` 於本案**非退化**側別皆 `≠ 1.0` | 任一非退化格量得與 `1.0` 之差 `< 1e-9` ⇒ **P2 破** |
| **P3** | `app.py` 與 `verify/stepg_pipeline.py` 之 `_cos_dn` 構造**逐字同構** | 兩處之運算式以 AST 正規化後比對，任一節點不同 ⇒ **P3 破** |
| **P4** | 族②③ ＝ `GB-47` 所載之四處殘存，**⛔ 非新增項** | 以 §3-1 之內容 grep 逐處對照 `GB-47` 條目所述之式，任一處不對應 ⇒ **P4 破** |
| **P5** | `GB-58` 之觸發條件（該側無 WINNER ∧ SIDE ∦ ALLOC）與族②③④ 之啟動條件（`_left_buffer_S > 0`）**同一** | 存在任一 (情境×街廓×側別) 格使二者不同時成立 ⇒ **P5 破** |

---

## §2 停機條件（⛔ 逐字採施工單 §6）

**下列為真域邊界 ⇒ 停機上呈，⛔ 不得自行決定：**

- **S-1**：Q2 各候選之量值於任一格互異**且**正典無條文可決者 ⇒ 停機
  （此即「土地面積由哪一個量決定」，屬域裁）。
- **S-2**：**P1 破**（`buf` 與 `S1_par` 不同度量）⇒ Q2 甲乙之比較基準即失效
  ⇒ **立即停機**，⛔ 不得續量 Q2。

**下列為純技術項 ⇒ 記錄後自行做完至 push，⛔ 不停機：**

- P2／P3／P4／P5 任一破 ⇒ 於報告正面列出，並依破法調整 `GB-59`／`GB-60` 之敘述。
- `GB-53` 之引據補正（施工單 §3-9）。
- 探針、log、報告、登記表、`W-G.9 §七` 增補之一切撰寫。

---

## §3 驗收（⛔ 逐字採施工單 §7·🔴 禁以計數或列數為證據）

| # | 閘 |
|---|---|
| 1 | 每一 (情境 × 街廓 × 側別) 格皆**正面列出**判定，**含不觸發格與退化格**；⛔ 只列命中者即不過 |
| 2 | 每一「無／零命中／不存在」之結論，皆附**產生它之窮舉指令原文**＋**射程聲明**；⛔ 空集合不算通過 |
| 3 | 每一數字皆附**產生它之指令**，且該指令可原樣重跑 |
| 4 | 凡「某符號有 N 處呼叫」之陳述，皆以 **AST `ast.Call`** 為據 |
| 5 | Q1-a 之三法中，**至少兩法為互相獨立之演算法**（⛔ 兩個近似值相符 ≠ 相互驗證）；若三法全異，須附殘差閘 |
| 6 | Q2 之候選清單附「**是否窮盡**」之書面自答；若為列舉而非窮舉，須明寫「**列舉·非窮舉**」四字 |
| 7 | `git diff --stat` 對 `app.py`／`verify/stepg_pipeline.py`／`verify/wf_f*.py` 之**刪除欄與新增欄皆為 0** |

---

## §4 已預先承諾之判準（⛔ 事後不得放寬）

- 判準之鍵一律為 **(情境 × 街廓 × 側別 × s 區間)**；⛔ 不得以宗序號為鍵。
- 閘之門檻 ⛔ 不得自訂——沿用既有探針原式。
- **只量不判**：Q1／Q2 之產出為量值與構造事實，⛔ 不得下「何者為正解」之判。
- 🔒 **考古節 62 之機械修法（⛔ 未答不得開始量測）**：於報告書面回答
  「若現行構造與正典不符，正解是否為『直接改用正典構造』？若是，本量測是否已含該格？」
- 🔒 **考古節 60 之機械修法**：凡宣稱「某符號有 N 處呼叫」須以 AST `ast.Call` 為據，
  ⛔ 禁裸 `grep -c`（我已於 `D-2b-31` commit A 犯過一次·commit A′ 具名更正）。
