# `W-G.9-90`　以 `V6_1` 跑**現行之新判定** ＋ `-段六` 四項覆核 ＋ baseline 過期清單

**基座**：`3c88ece`（`origin/wip/s1-endpart`·`W-G.9-89b` CC 交接文）
🔴 **基座落差之具名**：施工單稱基座 ＝ `b3aaf7b`；本批現查 `origin/wip/s1-endpart` **實為 `3c88ece`**
（`git log --oneline b3aaf7b..3c88ece` ＝ **1 筆**·即 `-89b` CC 交接文·⛔ 非施工單所令而係 KL 交辦）。
⇒ 本批以**實際 tip `3c88ece`** 為基座（⛔ 不改寫歷史·⛔ 不回退），全部閘與 `numstat` 皆對 `3c88ece` 比。

**受詞**：`W-G.9-77` **之後**才出現之判定，於 `V6_1.dxf` 下之值，逐項與 `V6.dxf` 對照
**性質**：🔒 **只量不修**·⛔ 零 `app.py` 變更·⛔ 不換圖／不換快照／不重烤／不動 baseline
**探針**：`verify/probes/probe_WG990_v61delta.py`（新增）　**log**：`verify/out/probe_WG990_v61delta_3c88ece.log`

---

## §0　三句結論

1. 🔒 **施工單七項（＋逐列 `cross`）之<u>判定</u>翻動 ＝ 0 ／ 8**——**現行有效之土地結論
   （`K-9-9 二` 不交叉之違反 ＝ **2 宗**／`R2左第1宗` `3.80822376`／`R5左第1宗` `0.98672881`／
   合計 `4.7950 ㎡`）於 `V6_1` 下 `Δ` ＝ **`0.000000e+00` 逐位**、名單**對稱差 ＝ 0**。
2. 🔴 **惟數值確有 `Δ`，且有<u>一項母體跨越容差門檻</u>**：`|sin(源甲, 源乙)| ≤ 1e-12` 之宗
   由 **8 ⇒ 9**，新入列者 ＝ **`R1|1`**（本批以自補之量 8 指認；`w84` 只印計數、⛔ 不印名單）。
3. ⇒ 🔒 **回答施工單 §八之問**：那一次換圖**不翻動現量到的任何判定** ⇒ 換圖後**只需重烤對帳**；
   ⛔ **惟「重烤對帳」之射程 ＝ 72 檔**（⛔ 非施工單 A-4 所稱之 19——母體不同·詳 §F）。

---

## §A　五組擴搜 ＋ 反向檢五錨 ＋ 三款停機判定

### A-1　⑥ 指令與範圍**逐字出艙**（🔒 Python `re`·⛔ 不經 shell 傳字樣）

**搜尋器**：`repogrep.py`（scratchpad·⛔ 不入倉）——母體 ＝ `git ls-files -z` 之**追蹤檔**
（⇒ ⛔ 未追蹤檔與**巢狀 worktree** 一律不在母體），字樣以 `re.escape()` **直入 Python**、
⛔ 不經 shell（避開 CJK 字元類／轉義層／locale 三陷阱）。**適用動作 ＝ 讀 ＋ 寫**。

```
files = [p.decode("utf-8") for p in
         subprocess.run(["git","-C",REPO,"ls-files","-z"],capture_output=True).stdout.split(b"\x00") if p]
rx = re.compile(re.escape(term));  n = len(rx.findall(io.open(f,encoding="utf-8",newline="").read()))
```

全倉追蹤檔母體 ＝ **1292 檔**。

### A-2　五組之逐字樣命中（🔒 出處分類·節 109）

| 組 | 【出處分類】 | 範圍（母體） | 字樣 → 命中檔／命中總數 |
|---|---|---|---|
| ⓪ | **【正典款號】** | `docs/rulings/`（**1 檔**） | `GB-16` 1/2・`U-K8-1` 1/3・`U-K9-2` 1/2・`K-9-15` 1/4・`K-9-14` 1/6・`K-9-9` 1/**29** |
| ⓪′ | **【土地行話】** | `docs/rulings/`（**1 檔**） | `換圖` 1/**1**・`快照` 1/10・`重烤` 1/20・**`前緣偏移` 0/0**・`截角接合` 1/1・`包含關係` 1/7 |
| ⓪″ | **【對方原詞】** | `docs/rulings/` ＋ `docs/specs/figures/`（**19 檔**） | `V6_1` 1/10・`第 0 宗` 1/7・`第 1 宗` 3/**79**・`不交叉` 1/2 |
| ① | 【我方·可】 | `verify/**.py`＋`app.py`＋`docs/reports/`（**485 檔**） | `弦區間` 18/73・`s*` 90/**397**・`λ_a` 11/83・`λ_b` 12/85・`S_req` 14/159・`餘裕` 74/**238** |
| ② | 【碼面】 | 同上（**485 檔**） | `V6DXF` 104/122・`parse_block_dxf` 4/11・`faces_of` 21/59・`st_frame` 12/60・`case_params_UC9898` 24/36 |

🔒 **判別力自檢**：**寬字樣皆 > 1**（`s*` 397・`餘裕` 238・`第 1 宗` 79・`K-9-9` 29 ⇒ 該搜非恆 0）；
🔴 **`前緣偏移` 於 `docs/rulings/` ＝ 0** ——**具名**：該詞係**報告側**之行話（`-段六` §4-a），
**正典未收**；⛔ 不得因「命中 0」而謂該量不存在。

⚠️ **自身污染之排除（⛔ 逐字出艙）**：`verify/probes/probe_WG990_v61delta.py` 與
`docs/reports/W-G.9-90_V6_1對新判定之影響.md` 自各組母體排除——**其於各組母體內者 ＝ 0 筆**
（搜尋時探針尚未建、報告尚未建 ⇒ **排除 0 筆**·🔒 連「排除了 0 筆」都出艙·同 `VR-048` 記功三）。

### A-3　⑦ 命中清單之**已答／未答** ＋ 🆕 **修法 110 之落實**

**`n ≥ 3` 之命中檔 ＝ 64 檔 ⇒ 逐檔開啟**（全文 `read()`·⛔ 非只看命中數）。

🔒 **分層之判準（⛔ 非省事·係受詞之邏輯結構）**：本單受詞 ＝「該判定**於 `V6_1` 下之值**」
⇒ 一個檔若要「已答」，**必要條件** ＝ 同時含 (i) `V6_1` 與 (ii) 至少一個受詞量字樣。
缺 (i) 者**構造上不可能**答之 ⇒ 判**未答**（⛔ 非「沒讀」——三層皆逐檔開啟）。

| 層 | 定義 | **分母** | 判 |
|---|---|---:|---|
| **A** | 含 `V6_1` **且**含受詞量字樣 | **16** | 逐檔細讀（見下） |
| **B** | 含 `V6_1`、⛔ 不含受詞量字樣 | **2** | **未答**（`verify/wf_f4.py`／`verify/wv_reconcile.py`）|
| **C** | ⛔ 不含 `V6_1` | **46** | **未答**（構造上不可能） |

🔒 **合計自校**：16 ＋ 2 ＋ 46 ＝ **64** ／ `n≥3` 之檔 **64** ⇒ ✅
🔒 **量詞三對齊**：結論句 N ＝ **64** ／ 表列數 ＝ **64** ／ 裸列舉母體 ＝ **64** ⇒ ✅

**層 A 之細讀**：16 檔中，**同一行同時含 `V6_1` 與受詞量者 ＝ 0** 者 **15 檔**
⇒ 其 `V6_1` 之脈絡為**換圖時點／驗收／PROVENANCE**，⛔ 非「以 `V6_1` 跑該判定所得之值」⇒ **未答**。

🔴 **唯一須人工裁者 ＝ `docs/reports/W-G.5_K6-A2-段三c_K9-7接生產.md:229`**：

> 「兩變動端點至 **R5** 為 `58.19m／33.27m`、至 **R4** 僅 `2.50m／2.74m`
>  ⇒ **換 `V6_1` 不會動到 R5 之餘裕**。」

⚠️ **同名不同量之具名**：該處之「餘裕」＝ `0m R5/left` winner **真交集之面積餘裕 `0.07㎡`**
（門檻 `1.0㎡`·真交集 `1.07㎡`），⛔ **非**本單量 6 之**弦區間餘裕（公尺）**。⇒ **二者不得互證**。
🔒 **依修法 112 並列二欄**：

| | 倉內既有 | 本次實得 | 性質 |
|---|---|---|---|
| R5 winner 真交集面積餘裕 | 「不會動到」（**推論**·由端點距離推得） | `probe_ruling_K9_corner_width.csv` 之 **R5 全 42 欄逐格相異 ＝ 0** | 🔒 **覆核**（結論相符·由推論升為實測） |
| `R5|左|1|0` 弦區間餘裕（m） | （倉內無） | `5.10673494938 ⇒ 5.10673494938`·`Δ ＝ 0.000000e+00` | **新增** |

### A-4　⑨ 反向檢五錨之**逐字覆核**（常設 12 ②·節 108）

| 錨 | 逐字（本批現查） | 行號現查 | 效力 | 判 |
|---|---|---|---|:--:|
| `K-6:1651`（`GB-16`） | 「**換圖須與換快照併入波末批同一次重烤**」 | `:1651` **相符** | ⇒ ⛔ 本單不得換圖 | ✅ |
| `K-6:1753`（KL 裁 `U-K8-1` ＝ 乙） | 「（KL 裁 U-K8-1 ＝ 乙：**快照留到 K-8 全案完成後一次換**）」 | `:1753` **相符** | ⇒ ⛔ 本單不得換快照 | ✅ |
| `K-6:237` | 「**換快照＝必須同批重烤 v3 baseline**，**待授權**（U-K6-1／U-K8-1）」 | `:237` **相符** | ⇒ ⛔ 本單不得重烤 | ✅ |
| `run_verification.py:33` | `V6DXF = os.path.join(REPO, "data", "V6.dxf")` | `:33` **相符** | ⇒ 生產路徑仍讀 `V6` | ✅ |
| `W-G.5_K8-段六` §四 | 已答**四項**（前緣偏移／截角接合／包含關係／斜率分支） | §四 ＝ `:107`–`:170` | ⇒ ⛔ 不重做 | ✅ |

（🔒 行號係**本批當場 grep** 之值；依 `CLAUDE.md` 行號衛生，引用時請重 grep。）

### A-5　🛑 三款停機之判定：**全部不觸發**

| 款 | 判準 | 本批現查 | 判 |
|---|---|---|:--:|
| ① | 上開任一錨已被後裁取代（節 92） | `換圖` 於 `docs/rulings/` **全庫命中 ＝ 1 行**（即 `K-6:1651` 本身）⇒ **無後裁**；`U-K8-1＝乙` 另於 `K-6:337` 明載「**照舊**」 | ⛔ **不觸發** |
| ② | `V6DXF` 已非 `V6.dxf`（換圖已生效） | `V6DXF = …"data", "V6.dxf"`（逐字·`:33`） | ⛔ **不觸發** |
| ③ | 已有探針出艙過「以 `V6_1` 跑 `-77` 後之新判定」 | `V6_1` 於 `verify/` 命中 **18 檔**，逐檔逐行現查 ⇒ 平行對照僅 **3 支**（`probe_K9_7d_layer2`／`probe_ruling_K9_corner_width`／`verify_dxf_v6_1`），**皆屬 `-段六` 舊四項**；算新判定之 `WG982`／`WG984`／`WG987`／`WG988` **`V6_1` 命中 ＝ 0** | ⛔ **不觸發** |

### A-6　⑧ 編號現查（3 列 ＋ 判別力對照）

| 受詞 | 命中（**入倉前**） | 期望 | 判 |
|---|---:|---:|:--:|
| `VR-048` ＋ `VR-049` | **0** ／ **0** | 0 | ✅ |
| `自誤 93` | **0** | 0 | ✅ |
| `GB-67` 新加註標題（唯一樣式 `備料完結——落地介面清單已備`）| **0** | 0 | ✅ |
| 🔒 判別力對照 `VR-047` | **28**（6 檔） | ≥1 | ✅ 非恆 0 |
| 🔒 判別力對照 `自誤 92` | **12**（6 檔） | ≥1 | ✅ 非恆 0 |

---

## §B　§Z 三塊之自檢

### B-1　前置三道

| 塊 | bytes | LF | sha256 前 16 | `CR` | 起頭 `\n---\n\n## ` | 收尾單一 `\n` |
|---|---:|---:|---|---:|:--:|:--:|
| `Z-1`（`VR-048` ＋ `VR-049`·🔒 **一塊二則**·⛔ 未分二次） | **10127** ✅ | **127** ✅ | `d70abae7158f2172` ✅ | **0** ✅ | ✅ | ✅ |
| `Z-2`（`GB-67` 加註·🔒 新 sha） | **4717** ✅ | **75** ✅ | `b573fb71853876c7` ✅ | **0** ✅ | ✅ | ✅ |
| `Z-3`（`自誤 93`） | **3244** ✅ | **44** ✅ | `094e8935118b9ab4` ✅ | **0** ✅ | ✅ | ✅ |

🔒 **`Z-2` 之舊 sha `b39e417122821b6b` ⛔ 未入倉**（本批所用者為新 sha·逐位相符）。

### B-2　🩸 **檔長期望之量測框更正（本批之第一則自誤·已自察）**

首次以**工作區 bytes** 驗檔長期望 ⇒ **三檔全 MISMATCH**（`197145`／`365768`／`165530` vs 期望
`194673`／`363445`／`163066`）。**根因 ＝ 我的量測框錯，⛔ 非施工單之數錯**：

- 本倉 `core.autocrlf = true`，而 `*.md` **⛔ 不在 `.gitattributes` 之 `-text` 白名單**
  （其僅含 `*.dxf`／`*.dwg`／`*.cnt`／`*.lin`／`*.jpg`）⇒ **工作區 CRLF、blob LF**。
- 差值 `2472`／`2323`／`2464` **逐檔恰等於該檔之 `CR` 數**（＝ 行數）⇒ 假說**逐檔精確到 0 bytes**。
- `git cat-file -s HEAD:<path>` ＝ `184546`／`358728`／`159822` **恰為施工單所設之現況**。

🔒 **⇒ 檔長期望一律對 <u>blob</u> 驗、⛔ 不對工作區驗。**
🔒 **⇒ 追加一律以 <u>binary 模式</u>**（片段為純 LF）——若以文字模式寫，Windows 會把 `\n` 轉 `\r\n`，
使「末 N bytes 逐位 == 片段」**必然失敗**。

### B-3　入倉後自檢四項

| 自檢 | 受詞 | 實得 | 判 |
|---|---|---|:--:|
| ① 末 N bytes 逐位 == 片段 | 工作區 ＋ **blob 各驗一次** | 三塊**皆逐位相同**（工作區 ＋ blob） | ✅ |
| ② 檔長增量 == N（🔒 對 **blob**） | `184546+10127`／`358728+4717`／`159822+3244` | **`194673`／`363445`／`163066`** ＝ 期望**三檔全中** | ✅ |
| ③ `numstat` 刪除欄全 0（**二法** ＋ 陽性對照） | 法一 `git diff --numstat 3c88ece`／法二 `--cached` | `+75/-0`・`+44/-0`・`+127/-0`・`+8/-0` ⇒ **非 0 者 ＝ 0**（二法皆同）<br>🔒 陽性對照 `git diff --numstat b3aaf7b 1ad98d4` ⇒ 刪除欄非 0 者 ＝ **7**（⇒ 該式**會咬**） | ✅ |
| ④ 四個新編號之定義節標題各 1（唯一樣式） | `^## \`VR-048\``／`^## \`VR-049\``／`^## 自誤 93`／`備料完結——落地介面清單已備` | **各 1**（合計 **4**）<br>🔒 判別力：`^## \`VR-047\`` ＝ 1、`^## \`VR-999\`` ＝ **0** ⇒ 非恆 1 | ✅ |

🔒 **入倉後 blob 之 `CR` ＝ 0**（三檔皆），**對照組** `data/V6.dxf` 之 blob `CR` ＝ **12308**
（`.gitattributes` `-text` ⇒ 應 > 0）⇒ 該檢**會咬**、⛔ 非恆 0。

---

## §C　A-1　七項之 `V6` vs `V6_1` 並列

### C-0　驅動與判別力

三輪**各一 subprocess**（`V6` ／ `V6_1` ／ **`V6` 再一次**）——隔離之由：`harvest()` 係 `exec(app.py)`，
**同行程重入之等冪性未經證** ⇒ ⛔ 不以同行程三呼替代（否則 `Δ ＝ 0` 可能係狀態殘留之**假綠**）。

```
輪 V6    rc=0  md5 62ad89ce081228c25a8e8ea935395b05  攔截 6 格  列 47  違反 2
輪 V6_1  rc=0  md5 8d18f8577ae734ed84364fab37eae208  攔截 6 格  列 47  違反 2
輪 V6#2  rc=0  md5 62ad89ce081228c25a8e8ea935395b05  攔截 6 格  列 47  違反 2
環境：shapely 2.1.2 | GEOS (3, 13, 1) | numpy 2.4.4
```

🔒 **判別力自檢（常設 8·⛔ 不可省）**：`V6` 對 **`V6` 自身**跑同一路徑
⇒ 量 1 `0/59`・量 2 `0/118`・量 3 `0/64`・量 4 對稱差 `0`・逐列 `cross` 翻動 `0`・
量 6 `0/47`・量 7 `0.000000e+00`・量 8 對稱差 `0` ⇒ **全部 `Δ ＝ 0`（逐位）**
⇒ ✅ **該對照器⛔ 非恆報變動**。

### C-1　🔒 **倉內錨之複現**（⛔ 先證能複現 `V6`，否則下游 `Δ` 無意義）

| 錨 | 倉內既有 | 本批實得（`V6`） | 判 |
|---|---|---|:--:|
| 不交叉違反宗數／面積 | 2 宗／`4.7950 ㎡` | **2 宗／`4.7950 ㎡`** | ✅ 複現 |
| `R2左第1宗`／`R5左第1宗` | `3.8082`／`0.9867` | **`3.80822376`／`0.98672881`** | ✅ 複現 |
| 弦區間 `0m R2` | `[0.0000, 44.9154]` | **`[0.000000000000, 44.915400…]`** | ✅ 複現 |
| `0m R3` 餘裕 | `3.502697` | **`-3.50269709390`**（🔒 施工單所載為**絕對值**·號為負·具名） | ✅ 複現 |
| `∠(用,ALLOC)` | `4.3220°`（`R2`／`R5`）・`0.6908°`（`-83` §D-0） | **`R2\|0`／`R5\|0`／`R3\|7` ＝ `4.3220`；`R1\|2` ＝ `0.690831538157`** | ✅ 複現 |
| `②-宗` 破量 | `R2 45.9766`／`R5 56.3293` | **`45.9765888243`／`56.3292619405`** | ✅ 複現 |

### C-2　七項之 `Δ`

| # | 量 | 母體 | **有 `Δ` 之數** | **極大 `\|Δ\|`** | 極大所在 |
|---|---|---:|---:|---|---|
| 1 | `∠(用, ALLOC)`（°·🔒 **逐宗超集**·⛔ 非只六格） | 59 | **3** | **`5.886563e-06`** | `R1\|2` |
| 2 | 弦區間 `[λ_a, λ_b]`（m·逐宗超集） | 118 端 | **14** | **`1.313567e-02`** | `R4\|1.λ_a` |
| 3 | `s*`（m·權威源 ＝ 解算面） | 64 對 | **7** | **`4.478522e-02`** | `R1\|1\|2` |
| 4 | 🔴 **`K-9-9 二` 不交叉**（㎡） | 47 列 | **0** | **`0.000000e+00`** | — |
| 5 | `S_req`（m·🩸 具名例外·4dp） | 200 行 | **0**（含 `S_req` 之相異行） | （4dp 下無差） | — |
| 6 | 餘裕（m·節 103） | 47 | **2** | **`1.934092e-03`** | `R1\|右\|1\|0` |
| 7 | 🔴 **`②-宗` 破量**（㎡） | 2 格 | **0** | **`0.000000e+00`** | — |
| 8 | 🔴 **CC 自補** `\|sin(源甲,源乙)\|` | 59 | **0**（值） | **`0.000000e+00`** | — |

**量 1 之三筆**（`Δ/ulp` 併印）：

```
R1|2   0.690831538157 ⇒ 0.690837424721   Δ = 5.886563e-06   Δ/ulp = 5.302e+10
R4|0   2.713695358308 ⇒ 2.713699657031   Δ = 4.298723e-06   Δ/ulp = 9.680e+09
R4|1   2.867974774646 ⇒ 2.867976278684   Δ = 1.504039e-06   Δ/ulp = 3.387e+09
```

**量 2 之極大二筆**（皆 `R4`·⇒ **與 `GB-15`「圖面落差只有 R4 一處」一致**）：

```
R4|0.λ_a  -0.007074955723 ⇒ -0.000005035762   Δ = 7.069920e-03   Δ/ulp = 8.151e+15
R4|1.λ_a  -0.013141605015 ⇒ -0.000005937376   Δ = 1.313567e-02   Δ/ulp = 7.572e+15
```

🔒 **量 4 之逐宗**（🔒 **現行有效之土地結論**）：

```
V6  ：違反 2 宗／4.7950 ㎡  名單 = ['R2|左|1|0', 'R5|左|1|0']
V6_1：違反 2 宗／4.7950 ㎡  名單 = ['R2|左|1|0', 'R5|左|1|0']
A-0(丁) 對稱差 = 0            面積合計 Δ = 0.000000e+00 ㎡（Δ/ulp = 0.000e+00）
  R2|左|1|0   3.80822376 ⇒ 3.80822376   Δ = 0.000000e+00（0.000e+00 ulp）
  R5|左|1|0   0.98672881 ⇒ 0.98672881   Δ = 0.000000e+00（0.000e+00 ulp）
逐列判定之翻動（母體 47 列·對稱差 0）：`cross` 翻動 = 0／`reason` 變動 = 0
```

🔒 **量 7**：`R2` 對數 `2 ⇒ 2`・面積 `45.9765888243 ⇒ 45.9765888243`・`Δ ＝ 0.000000e+00`；
`R5` 對數 `2 ⇒ 2`・面積 `56.3292619405 ⇒ 56.3292619405`・`Δ ＝ 0.000000e+00`；破閘格集合 `['R2','R5']` **對稱差 0**。

### C-3　🩸 量 5（`S_req`）之**具名例外**

`S_req` 之**全稱式**與其所需之 `groups_of`／`A2` 母體**皆為 `w84.main()` 內之巢狀定義**
（`probe_WG984_gap.py` 之 `def groups_of` 縮排於 `main()` 內）⇒ **⛔ 不可 import**；
重寫之即施工單 §一所禁之**重造**。⇒ 改以**原樣執行 `w84.main()`**（各 DXF 各一次）比其出艙。

🔒 **精度之誠實聲明**：`Δ` 之精度 ＝ 該 log 之列印精度 **`4dp`（`1e-4 m`）**——⛔ **不偽稱 `ulp` 級**。
判別力：`GB-16` 之變動為 `45m` 處 `7.11mm` ＝ `7.11e-3 m` ⇒ **較 `1e-4 m` 粗 71 倍**
⇒ 該精度**足以偵測翻動**；其不足僅在「`Δ` 之尾數」、⛔ 不在「有無 `Δ`」。

**`w84` 全 log 逐行對照**（🔒 已排除 `產生於 commit`／`→ `／含 `\` 之路徑行／`log ＝`／`DXF`）：
可比行 **200 ／ 200**；**相異行 ＝ 20**；**其中含 `S_req` 字樣者 ＝ 0**。

🔒 **三判準之分類**（⛔ 「有無相異行」⛔ 不等於「判定翻動」——必答明訂 ⛔ 非「數值有無變動」）：

| 判準 | 定義 | 實得 |
|---|---|---:|
| (甲) | 去掉**全部數字**後仍相異 | **0** |
| (乙) | **判定 token** 計數相異（`合格`／`不合格`／`單調`／`非單調`／`較鬆`／`較嚴`／`✅`／`🔴`／`是`／`否`／`情形 甲`／`情形 乙`／`破閘`／`未破閘`） | **0** |
| (丙) | 🔴 **整數（計數／母體基數）相異** | **1** |
| (丁) | 純小數 `Δ`（三判準皆不咬） | **19** |

🔒 **合計自校**：`1 ＋ 19 ＝ 20` ／ 相異行 `20` ⇒ ✅
🔒 **判別力**：陽性對照（`8 ⇒ 9`）(丙) 咬到 ＝ **True**；陰性對照（`-10.1319 ⇒ -10.1320`）＝ **False** ⇒ 該式**非恆 0 亦非恆 1**。

⇒ **量 5（`S_req` 本身）之判 ＝ ✅ 未翻動**。

### C-4　🔴 **非受詞、惟本批實得之變動**（⛔ 不得因「不在受詞內」而不報）

(丙) 之唯一一行 ＝ `w84` log `:61`：

```
V6    母體（`|sin| ≤ 1e-12` 之宗）＝ **8**；二源判定**完全相同 ＝ 8**
V6_1  母體（`|sin| ≤ 1e-12` 之宗）＝ **9**；二源判定**完全相同 ＝ 9**
```

🩸 **前二判準皆漏掉此行**——(甲) 之 `strip_num` 把**集合基數**當數值吃掉、(乙) 之 token 集不含數字。
⇒ 🔒 **教訓**：「去掉數字後相同」是**啟發法**，它把**量測實數**與**集合基數**混為一談；
**基數變動係母體變動、⛔ 非數值變動**（`A-0(丁)`：數目相同⛔ 不蘊含母體相同——其逆亦然）。

**`w84` 只印計數、⛔ 不印名單** ⇒ 無從指認**是哪一宗跨過門檻**
⇒ 🔴 **CC 自補量 8**（⛔ 施工單未令）：逐宗算 `|sin(源甲, 源乙)|`
（源甲 ＝ `w40.far_side_dir_and_pt`；源乙 ＝ `w82.uj_of`；式 ＝ `w86._sin`·⛔ 皆原樣 import）：

```
|sin| ≤ 1e-12 之宗：V6   = 8  ['R2|2','R3|10','R3|12','R3|6','R5|10','R5|8','R5|9','R6|8']
                    V6_1 = 9  （上開 8 宗 ＋ 'R1|1'）
🔴 跨門檻之宗 = **R1|1**
```

🔒 **該母體 ＝ `w84` A-1 之<u>判別力對照組</u>**（「已知二源相同」之宗）
⇒ 其基數變動**⛔ 不改 `S_req`**、⛔ 不翻動七項之任一判定，
**惟坐實「換圖確會使宗跨越容差門檻」** ⇒ 🛑 **具名上呈**（詳 §L-2）。

### C-5　🔒 **必答之答**

| # | 項 | 判定之受詞 | **翻動?** | 極大 `\|Δ\|` |
|---|---|---|:--:|---|
| 1 | `∠(用,ALLOC)` | 母體對稱差 ＝ 0 | ✅ 否 | `5.886563e-06` |
| 2 | 弦區間 | `empty` 之真偽 ＋ 母體對稱差 | ✅ 否 | `1.313567e-02` |
| 3 | `s*` | 母體（對）之對稱差 | ✅ 否 | `4.478522e-02` |
| 4 | `K-9-9 二` 不交叉 | 🔴 **違反宗數與名單** | ✅ 否 | `0.000000e+00` |
| 4b | 逐列 `cross` | 逐列合格／不合格 ＋ `reason` | ✅ 否 | — |
| 5 | `S_req` | 含 `S_req` 之相異行 ＋ (甲)(乙) | ✅ 否 | （4dp） |
| 6 | 餘裕 | 節 103 之最接近翻面者（鍵） | ✅ 否 | `1.934092e-03` |
| 7 | `②-宗` 破量 | 🔴 **破閘格集合與對數** | ✅ 否 | `0.000000e+00` |

🔒 **翻動之項數 ＝ 0 ／ 8** ⇒ ✅ **施工單七項無一判定翻動**（⛔ 惟數值仍有 `Δ`）。

---

## §D　A-2　節 103：**最接近翻面者**及其**餘裕**

🔒 **單一門檻**（常設 14 ①）⇒ ⛔ 無未定義帶：判準 ＝ `s* ∈ [λ_a, λ_b]`（`w82.pred_chord`）
⇒ **被判別之量 ＝ `餘裕`**（`s*` 距最近端點之帶號距·`w88.margin_of`·⛔ 非布林）。

| 鍵 | 餘裕（`V6`） | 餘裕（`V6_1`） | `\|餘裕\|` | `\|Δ\|` | 擦邊? |
|---|---|---|---|---|:--:|
| **`R3\|右\|1\|0`** | `-3.50269709390` | `-3.50269709390` | `3.502697e+00` | **`0.000000e+00`** | 否 |
| `R5\|左\|1\|0` | `5.10673494938` | `5.10673494938` | `5.106735e+00` | `0.000000e+00` | 否 |
| `R2\|左\|1\|0` | `10.04067974398` | `10.04067974398` | `1.004068e+01` | `0.000000e+00` | 否 |
| `R1\|右\|1\|0` | `-197.94667132743` | `-197.94473723577` | `1.979467e+02` | `1.934092e-03` | 否 |
| `R1\|左\|1\|0` | `-1835.34164417958` | `-1835.34162053508` | `1.835342e+03` | `2.364450e-05` | 否 |
| `R6\|右\|1\|0` | `-123486.51103664262` | `-123486.51103664262` | `1.234865e+05` | `0.000000e+00` | 否 |

🔒 **最接近翻面者 ＝ `R3|右|1|0`**（＝ 施工單所舉之 `0m R3`）：
`餘裕` **`-3.50269709390 m` ⇒ `-3.50269709390 m`**；`|Δ| ＝ 0.000000e+00`；`|餘裕| / |Δ| ＝ ∞`。

⚠️ **施工單 A-2 之擦邊判準**（`餘裕 < 10 × |Δ|`）⇒ **✅ 非擦邊**。
🔒 **全母體之擦邊計數 ＝ 0 ／ 6**（⛔ 不得只報「未翻動」——本行即該具名）。

🔒 **母體之界定**：`margin` 之有限值僅 **6 鍵 ／ 47 列**（其餘 41 列之 `margin` ＝ `nan`
——`ci` 為 `None`／`s*` 非有限），⇒ **分母以 6 出艙、⛔ 不以 47 充數**。

---

## §E　A-3　`-段六` §四**四項結論**之現況覆核（⛔ 只查不重做）

**前提之覆核**（原樣執行 `verify/tools/verify_dxf_v6_1.py`·rc=0）：

- 二圖 md5 ＝ `62ad89ce…`（`V6`·88341 bytes）／`8d18f857…`（`V6_1`·88453 bytes）
  ⇒ **與 `VR-049` 一所載逐字相符** ⇒ 該四項結論之**輸入未變**。
- 各圖層×型別實體數 **13 組全部一致**；`BLOCK POLYLINE`／`BASELINE`／`ALLOC_LINE`／`CENTERLINE`／
  `CADASTRAL_BOUND`（251）**完全相同**；唯 `BLOCK TEXT` 舊獨有 5／新獨有 5
  ⇒ **逐字符合 `VR-049` 二**（「`BLOCK` 變動之 5 個實體全部為 `TEXT`·街廓之線一條未動」）。

**四項之逐格覆核**（源 ＝ `probe_ruling_K9_corner_width{,_V6_1}.{log,csv}`·**16 格 × 42 欄 ＝ 672 儲存格**·相異 **107**）：

| 項 | `-段六` 之結論 | 本批之逐格覆核 | 判 |
|---|---|---|:--:|
| **4-a** 前緣偏移 | `R4/right` `+0.014826` ⇒ `+0.000006`（已修）；`R1/left` `−0.166141`／`R3/right` `−0.059879`（未變） | 五個字樣**逐一命中**（`0.014826` 17・`0.000006` 12・`-0.166151` 2・`-0.166141` 2・`-0.059879` 兩輪各 1）<br>🔒 陰性對照 `0.999999999` ＝ **0**（⇒ 該檢非恆真） | ✅ **仍有效** |
| **4-b** 截角接合 | `R4/right` **點 ⇒ 邊**；`R1/left` **仍點接合** | `截角接合` 欄 **相異恰 2 格**，且**皆為 `R4/right`**（`0m` 與 `3.5m`），皆 `點接合（Point）⇒ 邊接合（LineString）`；`R1/left` 兩情境**皆仍點接合** | ✅ **仍有效** |
| **4-c** 包含關係 | `R1` 三格 `5.325538㎡` 等未變 | `範圍−宗 溢出(㎡)` **相異僅 1 格**（`3.5m R1/right` `5.067912 ⇒ 5.067860`·`-段六` 已載為「實質未變」）；`R1` 三格 `5.325538` **逐字未變**<br>🔴 `範圍⊆宗` 欄雖有 **5 格**相異，惟**只取括號前之判定詞**後 ⇒ **相異 0 ／ 16**（5 格之差**全係括號內之噪訊註記文字**·如 `噪訊 8.49e-11㎡`）<br>🔒 判別力：`verdict("✅ 成立（噪訊…）") ≠ verdict("🔴 不成立")` ＝ True ⇒ 該式**會咬** | ✅ **仍有效** |
| **4-d** 斜率與 `K-9-7` 分支 | 八街角之分支歸屬**全部不變**（① 4／② 2／③ 2） | `K-9-7分支` 欄 **相異 ＝ 0 ／ 16**（逐格列印·分支字面逐字相同） | ✅ **仍有效** |

🩸 **本項之第二則自誤（已自察）**：首查 `K-9-7分支` 於 `*.log` **命中 0**——**係我查錯檔**，
該欄在 `*.csv`（`COLS` 之第 10 欄），非 log。改查 `.csv` 後兩輪各命中 1、逐格相異 0。
⇒ 🔒 **「命中 0」之第一嫌疑一律是「母體選錯」，⛔ 非「該物不存在」**。

**⇒ A-3 之答：四項結論<u>全部仍有效</u>（4／4）**，且本批以**逐格**證據取代原「逐字樣」證據。

---

## §F　A-4　`verify/baselines/` 過期清單（⛔ 只列不烤）

🔒 **依據**：`W-G.5_K8-段六` §五「所有幾何衍生數字毫米級變動、**全部凍存 baseline 隨之過期**」。
🔒 **⛔ 不得重烤任何一筆**（`K-6:1057`：重烤須另批、須逐 `gid` 因果鏈）。
🔒 本批 `verify/baselines/` 之 `git status --porcelain` ＝ **空** ⇒ **⛔ 未動任何一筆**。

### F-1　🔴 **母體之更正**（⛔ 不指施工單之數為錯——**係母體不同**）

| 施工單所稱 | 本批複現 | 其母體 |
|---|---|---|
| 「**19 檔**」 | **19** ✅ | ＝ `verify/baselines/` 之**頂層 entries**：檔 **13** ＋ 子目錄 **6** |
| 「凍存 CSV **214 列**」 | **214** ✅ | ＝ 頂層 **12 個 CSV** 之**含表頭非空行**合計 |

🛑 **惟該母體漏 285 個子目錄檔**：`git ls-files verify/baselines/**` ＝ **298 檔**
⇒ 以 `19` 為 A-4 之母體會**低估過期射程約 16 倍**。**本表以 298 為母體。**

### F-2　逐桶分類

| 桶 | 檔數 | 幾何衍生 | 非幾何 | 仍對拍? | 換圖後之處置 |
|---|---:|---:|---:|:--:|---|
| （頂層） | 13 | 12 | 1 | ✅ 是 | 🔴 **須重烤（波末批）** |
| `v3` | 17 | 16 | 1 | ✅ 是 | 🔴 **須重烤（波末批）** |
| `wf` | 50 | 44 | 6 | ✅ 是 | 🔴 **須重烤（波末批）** |
| `v2` | 15 | 14 | 1 | ⛔ 否（凍存） | ⚠️ 已凍存·⛔ 不對拍 ⇒ 不須重烤·**惟其數已過時** |
| `PRE-S0b_S0c_幾何漏歸零_凍存` | 67 | 60 | 7 | ⛔ 否（凍存） | 同上 |
| `PRE-比率更新_凍存` | 68 | 61 | 7 | ⛔ 否（凍存） | 同上 |
| `PRE勘誤_期日` | 68 | 60 | 8 | ⛔ 否（凍存） | 同上 |
| **合計** | **298** | **267** | **31** | | |

🔒 **合計自校**：`267 ＋ 31 ＝ 298` ／ 母體 `298` ⇒ ✅
🔴 **須於波末批重烤者（仍對拍 ∧ 幾何衍生）＝ 72 檔**（頂層 12 ＋ `v3` 16 ＋ `wf` 44）。
⚠️ **⛔ 二數不得相加**：`72` 之受詞 ＝ **幾何衍生 ∧ 仍對拍**；`218` 之受詞 ＝ **凍存桶之全部檔**
（含其 `.md`）⇒ `72 ＋ 218 ＝ 290 ≠ 298`。🔒 **正確之切分**：仍對拍桶 `13＋17＋50 ＝ 80` 檔
（其中幾何衍生 **72**、非幾何 **8**）＋ 凍存桶 **218** 檔 ＝ **298** ✅。
⚠️ **已凍存者 ＝ 218 檔**——其數同樣過時，惟依 `run_verification.py:37`（`baselines/v2/＝…凍存不刪、不再對拍`）
與目錄名 ⇒ **⛔ 不再對拍** ⇒ ⛔ 不列入重烤（🔒 **亦不得因此稱其「未過期」**）。
⚠️ **無法判定 ＝ 0**。🔒 **判準之逐字**：`.md`（PROVENANCE／README）＝ **非幾何**；其餘 ＝ **幾何衍生**；⛔ 無第三類。
🔒 **非幾何 31 檔已逐檔具名**（見探針輸出·⛔ 只給計數者讀者無從分辨）。

⛔ **本節不含任何排程建議**（施工單 §二 A-5-8·排程已由 KL 2026-08-20 裁定）。

---

## §G　`P1`〜`P7` 逐項判 ＋ 十一閘

### G-1　預測

| # | 預測（逐字要旨） | 實得 | 判 |
|---|---|---|:--:|
| `P1` | §Z 三塊 bytes／LF／sha256 逐塊相符 ⇒ **3／3**；定義節標題合計 **4**；`docs/rulings/` 變更 **0 檔** | 三塊逐位全中；定義節標題 **4**（各 1）；`docs/rulings/` 變更 **0** | ✅ **成立** |
| `P2` | **強·判別力·可證偽**：`K-9-9 二` 違反於 `V6_1` **仍為 2 宗**且**名單逐宗相同** | **2 宗**·名單 `['R2\|左\|1\|0','R5\|左\|1\|0']`·**對稱差 0**·面積 `Δ ＝ 0.000000e+00` 逐位；🔒 判別力 `V6` vs `V6` 全部 `Δ ＝ 0` | ✅ **成立** |
| `P3` | **強·可證偽**：`∠(用, ALLOC)` 之變動**皆 ≤ `0.02°`** | 極大 `\|Δ\| ＝ 5.886563e-06°`（`R1\|2`）⇒ **較 `0.02°` 小 3398 倍**（`0.02 / 5.886563e-06 ＝ 3397.568…`）；59 宗超集內**無一超過** | ✅ **成立** |
| `P4` | **強·可證偽**：`0m R3` 之餘裕變動 **≤ `0.01 m`** | `R3\|右\|1\|0` `Δ ＝ 0.000000e+00 m` | ✅ **成立** |
| `P5` | 弱·可證偽：`②-宗` 破量之變動 **≤ `0.5 ㎡`**（`R2`／`R5` 各自） | 二格皆 `Δ ＝ 0.000000e+00 ㎡` | ✅ **成立** |
| `P6` | **強**：`-段六` §四之四項結論**全部仍有效** | 4／4 仍有效（§E·逐格證據） | ✅ **成立** |
| `P7` | `app.py` blob 仍 `a9e5671d…`·`diff` 0 行；`data/` 0；`docs/rulings/` 0；`verify/baselines/` 0；`verify/**.py` 變更 **2**；`V6DXF` 逐字未變；`run_all` ＝ **15**（第十六法） | 逐項相符（見 G-2 閘 5／7） | ✅ **成立** |

🔒 **`P2` 未被否證** ⇒ ⛔ 無「立即上呈·不得逕自續判」之觸發。
⚠️ `P2`〜`P6` 係 claude.ai 之預測，其**成立⛔ 不記 CC 之功**，係設計內之產出。

### G-2　十一閘（序：`1 → 4 → 2 → 2′ → 入倉 → 1′`）

| # | 閘 | 期望 | 實得 | 判 |
|---|---|---|---|:--:|
| 1 | 純追加稽核（入倉前·基座 `3c88ece`） | `rc=0` | **`rc=0`**·結論「✅ 全部成立」 | ✅ |
| 4 | `TARGETS` 追加（先現查·只補未在列者） | 只追加 | `72 ⇒ 73`；**移除 ＝ []**；新增 ＝ `['docs/reports/W-G.9-90_V6_1對新判定之影響.md']`；既有 72 項**逐元素全在新表** | ✅ |
| 2 | 變異測試（對象**先證在母體內**·舊文**行中**插字） | `rc=1`·具名 | 受詞 `W-G.9-89_權威源回溯與落地介面盤點.md`（**在 `TARGETS` 中 ＝ True**·變異前 `status` 空）；於 byte **475**（第 5 行後 20 bytes·**行中**）插 1 byte `X` ⇒ **`rc=1`**·具名該檔 `🔴 [檔]` | ✅ |
| 2′ | 變異復原 | `git diff` 0 行·重跑 `rc=0` | `git diff` **0 行**·bytes 復原 `39072`·重跑 **`rc=0`** | ✅ |
| 1′ | 純追加稽核（入倉後） | `rc=0` | **`rc=0`**；三受詞檔皆 `✅ [檔]`·嚴格前綴 True | ✅ |
| 3 | `numstat` 刪除欄全 0（**二法** ＋ 陽性對照） | 全 0 | 法一／法二**皆 0**；陽性對照 `b3aaf7b..1ad98d4` ⇒ **7 筆會咬** | ✅ |
| 5 | `app.py` blob ＋ `verify/**.py` ＋ `data/` ＋ `docs/rulings/` ＋ 🛑 `verify/baselines/` | `a9e5671…`／**2**／**0**／**0**／**0** | `a9e5671d64d254907a0396f898f046d9d85e8283`（HEAD ＝ 工作區 hash·`diff` 空）／**2**／**0**／**0**／**0**；🛑 `V6DXF` 之行**逐字相同**、`run_verification.py` `diff` 空 | ✅ |
| 6 | `py_compile`（二檔） | 全過 | 二檔 `rc=0` | ✅ |
| 6′ | 位元組層複查·附對照組 | 0 ＋ 對照命中 | 三檔 blob `CR ＝ 0`；**對照組** `data/V6.dxf` blob `CR ＝ 12308` ⇒ **會咬** | ✅ |
| 7 | 🔒 `run_all` ⛔ 不跑（逸出條款逐項現查） | 未跑 | 四項逸出條款逐項現查**全部不觸發**（`app.py` 0／`data/` 0／`baselines/` 0／`verify/**.py` 二者皆純加性）；`run_all.py` `diff` **空** | ✅ 未跑 |
| 8 | `git status --porcelain` 全樹（前後各一次） | 無非預期 | 前：**空**；後：**8 項**（3 個 `M` 受詞檔 ＋ 1 個 `M` `wg942_append_audit.py` ＋ 4 個 `??`：新探針、二支 log、`_wg990/`）⇒ **皆為預期** | ✅ |

🔒 **第十六法之逐字定義**（⛔ 與既用十五法皆不同族）：
> `ast.parse(run_all.py)` 後，取全部 `For`／`AsyncFor` 節點中 **`iter` 為 `Tuple`／`List`** 者，
> 計其 **`iter.elts` 之子節點數**，取極大 ⇒ **15**。
> ⛔ **非**第十四法之「`For.iter` 之四元位置切出原始碼片段後正則抓 `.py`」——本法**不看原始碼文字、
> 不用正則、不看字串常數**，係**純 AST 子節點計數**。
> 🔒 判別力：陽性對照（人造 `for a,b in ((1,2),(3,4),(5,6))`）⇒ **3**（咬到）；
> 陰性對照（`for i in range(15)`）⇒ **命中 0**（`iter` 非 `Tuple`/`List`）⇒ 該式**非恆有值**。

---

## §H　施工單項數對帳（🔒 逐項打勾·量詞三對齊自校）

| 節 | 項數 | 逐項 | 辦畢 |
|---|---:|---|---:|
| §零 | **2** | ① `TARGETS` 追加 ✅　② 新增 `probe_WG990_v61delta.py` ✅ | **2** |
| §一 | **9** | ⓪ ✅　⓪′ ✅　⓪″ ✅　① ✅　② ✅　⑥ 指令逐字 ✅　⑦ 已答／未答（分層·每層分母）✅　⑧ 編號現查 3 列＋判別力 ✅　⑨ 反向檢五錨＋三款停機 ✅ | **9** |
| §二 | **6** | A-0 ✅（事前選定甲乙丙丁）　A-1 ✅　A-2 ✅　A-3 ✅　A-4 ✅　A-5 ✅（八款逐款未違） | **6** |
| §三 | **6** | 前置三道（編號現查／目標檔 bytes／片段 `CR`）✅✅✅　＋ `Z-1`／`Z-2`／`Z-3` 入倉 ✅✅✅ | **6** |
| §四 | **7** | `P1`〜`P7` 逐項判 ✅ | **7** |
| §五 | **11** | 閘 1／4／2／2′／1′／3／5／6／6′／7／8 ✅ | **11** |
| §六 | **13** | `§0`／`§A`／`§B`／`§C`／`§D`／`§E`／`§F`／`§G`／`§H`／`§I`／`§J`／`§K`／`§L` | **13** |
| §七 | **15** | 1–7 逐條未違；8–14 常設（同 `-89` §七）；15 節 110＋`自誤 92`＋`自誤 93` 之四款 ✅ | **15** |
| **合計** | **69** | | **69** |

🔒 **量詞三對齊自校**：結論句 N ＝ **69** ／ 表列「項數」欄合計 ＝ `2+9+6+6+7+11+13+15` ＝ **69** ／
「辦畢」欄合計 ＝ **69** ⇒ ✅ **三者相符**。

🔒 **§七 之逐條未違（抽三條具名）**：
第 3 條「⛔ 不換圖／不換快照／不重烤／不改 `V6DXF`」⇒ 閘 5 已證 `V6DXF` 逐字未變、`baselines/` 0 檔；
第 6 條「⛔ 不提排程建議」⇒ §F／§L **零建議**（只列射程與待裁）；
第 7 條「凡出艙『完全分離／0 例外』須同批出艙最接近翻面之反例及其餘裕」⇒ §D 已出艙（`R3|右|1|0`·餘裕 `3.502697`·擦邊計數 `0/6`）。

---

## §I　CC 本批之自誤（🔒 **五則**·皆本批自察並更正）

1. 🩸 **檔長期望之量測框錯**：首次以**工作區 bytes** 驗 §Z 檔長 ⇒ 三檔全 MISMATCH。
   根因 ＝ `core.autocrlf=true` ＋ `*.md` ⛔ 不在 `.gitattributes` 白名單 ⇒ **工作區 CRLF、blob LF**。
   🔒 **通則**：**倉內數之期望值一律對 `blob` 驗、⛔ 不對工作區驗**；差值恰等於 `CR` 數即該病之指紋。
   ⚠️ 此為**第六面**之「量測器靜默給錯數」（前五面：CJK 字元類／`cat -A` 數 CR／轉義層／`||` 假綠／指令沒跑成功）。
2. 🩸 **量 5 之判準過寬**：首版以「**任一相異行 ⇒ 翻動**」判，得「翻動 1／8」——
   而必答明訂 ⛔ **非「數值有無變動」**。⇒ 該版會把 19 行**純小數 `Δ`** 誤判為判定翻動，
   **且把真正的那一行（母體 `8⇒9`）之成因說錯**。改為**三判準**後方得正確歸因。
3. 🩸 **前二判準皆回 0 而真有一行**：(甲)「去掉數字後仍相異」與 (乙)「判定 token 計數」**皆 0**，
   漏掉 `:61` 之 `8 ⇒ 9`——因**集合基數被 `strip_num` 當成數值吃掉**。
   🔒 **「0」最該懷疑**（memory 既有之戒）⇒ 加第三判準（**整數**逐位對拍）方咬到，並補陽性／陰性對照。
4. 🩸 **A-3 之 4-d 查錯檔**：首查 `K-9-7分支` 於 `*.log` 命中 **0**，實則該欄在 `*.csv`。
   🔒 **通則**：「命中 0」之第一嫌疑一律是**母體選錯**，⛔ 非「該物不存在」。
5. 🩸 **第十六法首版算錯**：首版取「`List`/`Tuple` 之 `elts` 中以 `.py` 結尾之字串常數數」⇒ 得 **1**、非 15，
   因 `run_all.py` 之 `for _fx, _what in ((…),(…))` 使 `.py` 常數**多巢一層**。
   ⇒ 重造為**純 AST 子節點計數**（`For.iter.elts` 之長度）⇒ **15**，並補陽性／陰性對照。
   🔒 若當時逕報「`run_all` ＝ 1」，即為**以壞掉的量測器指他人資料有誤**（`wg8-cang-dan-provenance` 之族）。

---

## §J　未涵蓋之事（⛔ 逐項具名·⛔ 不以「未涵蓋」吸收漏辦）

1. 🩸 **量 5 `S_req` 之 `Δ` 僅至 `4dp`**（`1e-4 m`），⛔ **非 `ulp` 級**——具名例外（§C-3）。
   其**充分性**已論證（`GB-16` 之 `7.11mm` 較之粗 71 倍），惟「`Δ` 之尾數」確未取得。
2. **情境母體 ＝ 僅 `0m`**（同 `-88` 之 `SB ＝ 0.0`·⛔ 不擴）——量 1／2／3／4／6／7／8 之
   **`3.5m` 情境未量**。（🔒 量 5 之 `w84` log **涵蓋 `0m` 與 `3.5m` 二情境**，其 20 行相異中
   `:94`／`:95`／`:122`／`:123`／`:155` 即 `3.5m` 之列 ⇒ **`3.5m` 之 `S_req` 亦無判定翻動**。）
3. **A-4 之「幾何衍生」判準為檔級啟發**（`.md` ＝ 非幾何／其餘 ＝ 幾何衍生），
   ⛔ **未逐檔開啟 267 個檔證其確為幾何衍生**——⇒ 該分類為**上界**（偏向多列·符 A-0）。
4. **量 8 之門檻 `1e-12` 取自 `w84.TOL_SAME`**（⛔ 不另立），惟 **`R1|1` 之 `|sin|` 於二圖之實值未出艙**
   （探針只出艙「跨門檻」之事實與集合差）——⇒ 其**距門檻多遠**未量。
5. **`②-宗` 破量之 `3.5m` 情境未量**（`w83` 之 `SB` 亦為 `0.0`·⛔ 非本批限制）。
6. ⛔ **未出艙「應改領現金之宗」**（`W-G.9-40` §C-4 之禁令仍有效）。
7. ⛔ **未提任何落地或重烤之排程建議**（施工單 §二 A-5-8·排程已由 KL 裁定）。

---

## §K　波末佇列現況（🔒 **含 KL 2026-08-20 之排程裁示**）

**KL 逐字（`Z-2` §五·本批入倉）**：

> **先落乙，把最基本的幾何分配位置及方式確定，才做後續甲矩形容納**
> **落地前，先以 shim 量出各自的土地後果**

🔒 **完整次序（`GB-67` 加註所載·⛔ 非本批之建議）**：
`W-G.9-90`（量換圖對**新判定**之影響·零生產碼）⇒ shim 量**乙** ⇒ **落地乙** ⇒
shim 量**甲** ⇒ **落地甲** ⇒ **波末批：換圖 ＋ 換快照 ＋ 重烤（一次）**。

| 項 | 狀態 |
|---|---|
| `W-G.9-90`（本批） | ✅ **已完成**——七項無一判定翻動；换圖後**只需重烤對帳** |
| **乙**（`②-宗` 錨點更正·射程 **26 宗**） | 🛑 待 KL 定範圍 ⇒ 先 shim 量土地後果 |
| **甲**（`K-9-12` 矩形容納·射程 **51 宗**） | 🛑 乙之後 |
| **波末批**（換圖＋換快照＋重烤） | 🔒 三者**鎖成一組**（`K-6:1651`／`:1753`／`:237`）·時點 ＝ `K-8` 全案完成後<br>🔴 **重烤射程 ＝ 72 檔**（⛔ 非 19·§F） |
| `GB-67` | 成因鏈閉合／判準已定／母體已收斂／落地清單已備 ⇒ 待 KL |
| 高水位 | `VR-049`／自誤 **93**／考古節 **110**／正典 `K-9-15` |

---

## §L　交下一窗之三件

1. 🔒 **本批之答可直接用於波末批之規劃**：換圖**不翻動**現量到的任何判定
   （`K-9-9 二` 之 **2 宗／`4.7950 ㎡`** 逐位不變、`②-宗` 破量逐位不變、`-段六` 四項結論全部仍有效）
   ⇒ **換圖後只需重烤對帳、⛔ 不需重新分析**。**惟重烤之射程 ＝ 72 檔**（§F-2）。

2. 🛑 **上呈 KL（⛔ 非建議·係具名之實得）**：
   **`|sin(源甲, 源乙)| ≤ 1e-12` 之宗於換圖後由 8 ⇒ 9，新入列者 ＝ `R1|1`。**
   該母體係 `w84` A-1 之**判別力對照組**，其基數變動⛔ 不改 `S_req`、⛔ 不翻動七項之任一判定；
   **惟其坐實「換圖確會使宗跨越容差門檻」** ⇒ 🔴 **凡以 `1e-12` 級容差分類之閘，
   其分類結果在波末換圖後<u>須重驗</u>**（⛔ 不在「只需重烤對帳」之保證範圍內）。
   ⚠️ 本批**未查**全倉尚有多少個 `1e-12`／`1e-9` 級之分類閘 ⇒ **受詞屬 KL／發單側**。

3. 🔴 **母體之二讀法須裁**（`A-4`）：施工單之「19 檔／214 列」在**頂層 entries** 母體下**完全正確**，
   在 `git ls-files verify/baselines/**`（**298 檔**）母體下則**低估 16 倍**。
   ⇒ 🛑 **波末重烤批之「baseline 過期清單」以何者為母體，須由發單側明訂**——
   本批已備二者之逐檔清單。⛔ **本節不含建議。**

---

## 🔒 附　本報告之機檢（⛔ 憑記憶出艙者一律不寫）

**受詞**：本報告內每一個 **`≥ 3dp`** 之數，須能於**倉內或本批 log** 查到。
**乾草堆**：`probe_WG990_v61delta_3c88ece.log`／`_wg990/w84_V6{,1}.log`／`probe_WG988_nocross_3c88ece.log`／
`probe_ruling_K9_corner_width{,_V6_1}.{log,csv}`／`verify_dxf_v6_1.log`／`baselines/PROVENANCE.md`／
`K-6`／`-段六`／`-段三c`／`驗證裁定登記表`／`泛用阻塞項登記表`／`run_verification.py`／`probe_WG990`／`probe_WG984`。

| 項 | 值 |
|---|---|
| 相異之 `≥3dp` 數 | **67** |
| **查無** | **2** ⇒ 二者皆**具名豁免**（見下） |
| 🔒 判別力（陽性對照） | 人造 `1.234567890123` 於乾草堆 ＝ **False** ⇒ 該檢**會咬** |

**二個具名豁免**：
1. `0.999999999` ——**我方之陰性對照偽錨**（§E 4-a·其**設計即為必查無**，用以證該字樣檢非恆真）。
2. `3397.568` ——**本批當場之除法**（`0.02 / 5.886563e-06`）；🔒 **運算元二者皆已並列於同一格**
   （`0.02` 出自 `P3` 逐字、`5.886563e-06` 出自本批 log）⇒ 讀者可自行覆算。

其餘 **65** 個 `≥3dp` 數**逐一查得**。
🔒 **刪除欄全 0**（閘 3·二法＋陽性對照）；**`app.py`／`data/`／`docs/rulings/`／`verify/baselines/` 皆 0 檔**（閘 5）。

---

🔒 **一句話**：那一次換圖，**不會讓現在量到的任何判定翻動**——`K-9-9 二` 之 2 宗／`4.7950 ㎡` 逐位不動；
**惟它確實會把一個宗推過 `1e-12` 的門檻**（`R1|1`），而**重烤對帳的射程是 72 檔、不是 19 檔**。
