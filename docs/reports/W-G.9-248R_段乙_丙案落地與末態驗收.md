# `W-G.9-248R`　段乙：丙案落地（`c1`＋`c2`）與**末態驗收**

> 🛑 **本報告⛔ 零生產碼**（`app.py` 於本 commit 一字未動）·**鑄 `自誤 311`**。
> 🔴 **生產碼二 commit 已備妥於側分支 `verify/W-G.9-248-c1` ＝ `89a9a7a4fc4c4d5ce1868f4d57c6590b0eb14a86`**
> ——`wip/s1-endpart` **一字未動**（現查 `6010a80`）；候發單側逐位復驗 → 請示 KL → 逐字放行。
> 🔒 **KL 裁（`2026-09-07`）**：工項一採**丙案**；`-248 §二 b`「覆寫欄須出現在介面甲」**作廢**；
> 段乙拆二 commit（`c1` ＝ 工項二＋三／`c2` ＝ 工項一）；**驗收於末態跑一次**。
> 🔑 一切數字現查於倉；三軸逐處具名（`VR-091 一`）。

---

## §零　態錨

| 項 | 值 |
|---|---|
| 基座（本報告之 commit 所在）| `origin/wip/s1-endpart` ＝ **`6010a800683438db75b23c0b8d187088f2d6d891`** |
| 側分支（**候放行**）| `origin/verify/W-G.9-248-c1` ＝ **`89a9a7a4fc4c4d5ce1868f4d57c6590b0eb14a86`**（本地保全 ref `keep/W-G.9-248-c1` 同值）|
| `c1` | `9ff9cad` 工項二＋三 ｜ `app.py` **`85` 增／`3` 刪** |
| `c2` | `89a9a7a` 工項一 ｜ `app.py` **`41` 增／`0` 刪** |
| 累計 | `6010a80..89a9a7a` ＝ `app.py` **`126` 增／`3` 刪**（**單檔**）|
| `app.py` `sha256[:16]` | 改前 `5693242b19d970c1` → **改後 `4860f10828fb8ae9`** |

🔒 **`c1` 之 `3` 刪逐行具名**（皆係**就地取代**·⛔ 移除任何功能）：

| # | 逐字 | 去向 |
|---|---|---|
| `1` | `                "期初淨額": "-",` | 改 `None`（工項三·DF-3）|
| `2` | `                            st.dataframe(_pd.DataFrame(_corner_select_results),` | 包入 helper（工項三·DF-2）|
| `3` | `                                         use_container_width=True, hide_index=True)` | 同上（該呼叫之續行）|

---

## §一　工項一（**丙案**·`c2`）：`K-9-1` 唯讀顯示

🔒 **落點** ＝ 介面甲每街廓 `expander` 之**正面道路區塊下方**
（`fn` 核取 `:19659` → 工項二 `text_input` `:19670` → 本 `caption` `:19713`）。

🔒 **當場現算**——呼叫 module 級純函式 `k91_effective_min_build_area`；
**⛔ 讀 `K91_SS_MBA_EFFECTIVE`**（該鍵於介面乙之後方寫入 ⇒ 讀之必**延遲一輪**·KL 明令避之）。

### 🔴 入參之可得性（裁示令「不可得者 ⇒ 停機具名」）

🔴 **該函式簽名為<u>四參</u>**：`k91_effective_min_build_area(label, category, by_label, by_category)`
——**裁示稱「三入參」**，具名其相異（⛔ 停機款·實質不影響）。

| 入參 | 於介面甲處 | 判 |
|---|---|---|
| `label` | `b['label']` | ✅（`classified_blocks` 元素含 `'label'`·`app.py:17987`＠`f73ce5e`）|
| `category` | `b.get('category', '') or ''` | ✅（同上 `:17988`）|
| `by_label`／`by_category` | 區域變數 `_k91_by_*` 在介面甲**之後**；惟其來源逐字 ＝ `st.session_state.get(K91_SS_MBA_*, {})` ⇒ **同式可於介面甲取得** | ✅ |

⇒ 🟢 **四參皆可得 ⇒ ⛔ 觸發停機。**

### 🔒 入參之取得（**二層**·⛔ 靜默·⛔ 編造）

1. **優先取 widget key**（`f3L_mba_ov_{label}`／`f3L_mba_cat_{cat}`）——該二 widget 在
   `st.form` **外**，其 key 於 rerun 開始前即已更新 ⇒ 得**本輪值·無延遲**。
2. **缺則回落持久 dict**（`K91_SS_MBA_BY_LABEL`／`_BY_CATEGORY`）——介面乙**未渲染之輪**
   Streamlit **丟棄 widget key** 而持久鍵存活（`W-G.9-219R2`／`-226` `G4` 已活體實測）。
3. 二者皆無 ⇒ `0.0` ＝ **該街廓無規定**（與介面乙之語意**同一**·⛔ 編造）。

🛑 **唯讀**：⛔ 回寫任何 `session_state` 鍵、⛔ 設任何 widget key
⇒ **⛔ 觸** `f3L_mba_*` 之「`value=` 之種⛔ 取自 widget key」之戒（該戒之受詞係**種**；
本處係**讀以顯示**、⛔ 回寫 ⇒ 無靜默覆寫之虞）。

---

## §二　工項二（`c1`）：正面道路名稱／識別符

**新鍵** ＝ `SS_FRONT_ROAD_NAME = 'f3_front_road_name_by_bid'`（module 級·`app.py:10285`＠`89a9a7a`）
——**自成一 dict**（鍵 ＝ `bid`），與既有 `SS_ROAD`（`'f3_block_road'`）之 `front_*` 諸鍵
**⛔ 共用、⛔ 覆寫**；寫入於 `if submit_road:` 內另立之 `_frn_persist`。

🔒 `value=` 之種取自**持久 dict** `_front_road_names`、**⛔ 取自 widget key**（體例同 `f3L_mba_*`）。
🔒 本欄在 `st.form` 內 ⇒ **按鈕方生效**；其**⛔ 消費端** ⇒ **無即時性需求**
⇒ 該延遲**⛔ 為缺陷**（段乙停機報告 `§四 b` 已具名）。
🛑 **⛔ 及於側面道路**——`r3` 之受詞為**正面**道路（單 `§三 b`：擴及側面係射程外）。

---

## §三　工項三（`c1`）：混型欄之顯示層修

**新 helper** ＝ `wg9248_stringify_mixed_cols(df, cols)`（module 級·`app.py:10288`·呼叫點 **`2`**）。
🛑 **⛔ 動產生該值之計算一字**——僅於**送入 `st.dataframe` 之前**施於顯示層。
🔒 **受詞欄不存在 ⇒ loud `KeyError`**（`no-silent-fallback`）·⛔ 靜默略過。

### 🔒 受詞之**窮舉**（單 `§四 b` 令同一 DF 之他欄同病須一併處置）

母體 ＝ `app.py` 全檔含 `'—'`／`'-'` 哨兵之 dict 欄 **`25`** 列；逐欄查其**二臂型別**後：

| DF | 渲染點 | **混型欄** | ⛔ 混型者（已排除·逐一具名）|
|---|---|---|---|
| `_corner_rows_init` | `:20326`– | **`4`**：`【左/右】路寬(m)`／`【左/右】截角(㎡)` | `【左/右】街角最小面積(㎡)` ＝ `None` ∪ float ⇒ pandas 轉 `NaN` |
| `_corner_select_results` | `:20837` | **`4`**：`【左/右】達資格候選`／`【左/右】優先權指數` | `【左/右】最小面積(㎡)`／`第1宗指配` 之二臂**皆 f-string／字串** ⇒ 恆字串 |
| `cash_flow_data` | `:16562`– | **`1`**：`期初淨額` | — |

### 🔒 二案之取捨（單令載明）

| 案 | 採用處 | 由 |
|---|---|---|
| **甲** `.astype(str)` | **DF-1／DF-2** | 二表**無** `.style.format` ⇒ 改 `None` 將使 `'—'` 顯示為**空白**、**語意滅失**（單明令⛔ 滅失）⇒ ⛔ 採乙 |
| **乙** `None` ＋ 依欄設格式 | **DF-3** | 其 `.style.format` **已帶 `na_rep="-"`** 且**未含**本欄 ⇒ 改 `None` ＋ 補該欄之 `"{:,.2f}"` ⇒ **語意由既有機制完全保留**、型別一致、且與同表他欄格式一致（**最小改動**）|

🛑 **⛔ 把哨兵改成 `0`**（會與真實之 `0` 混淆·**有判讀後果**·單明令）。

---

## §四　末態驗收（**於 `89a9a7a` 跑一次**·裁示逐字）

### 🟢 `V-0`　`run_all` 全名目 `rc` 前後逐項相同

**方法**：二**拋棄式 clone**（`C:\w248a` ＝ `6010a80`／`C:\w248b` ＝ `89a9a7a`·
`core.autocrlf=false`·**目錄名同長度**）各跑一次 `verify/run_all.py`，**二流分離**
（記憶 ㊲：`2>&1` 之對拍不可信）。

🔒 **併跑之判（具名）**：`CLAUDE.md`「兩支長跑**不可併跑**」之受詞係
「`run_verification` **會寫 CSV**」之互相覆寫；本次係**二獨立 clone**、各自之
`verify/out/` 完全隔離 ⇒ 該戒之受詞不成立。**其正確性由結果自證**——
二端 `verify/out` 產物檔數**皆 `521`**、`got_*` 逐位相同。

| 量 | A（改前 `6010a80`）| B（改後 `89a9a7a`）| 判 |
|---|---|---|---|
| `rc` | **`1`** | **`1`** | ✅ 相同（**准紅碼**·`rc=1` 為預期）|
| `stdout` | `49162` B／`542` 列 | `49162` B／`542` 列 | ✅ |
| `stderr` | `0` B | `0` B | ✅ |
| **正規化後 `sha256[:24]`** | `6a75cf230b6929ee3e6af4da` | **同** | ✅ **逐位相同** |
| **放寬框**之名目判 | `71` 列 | `71` 列 | ✅ **逐列相同** |
| 判之分佈 | `✅ 28`／`🔴 35`／`🟡 8` | **同** | ✅ 逐鍵相同 |
| **判翻轉** | — | — | ✅ **`0` 項** |
| 對帳段（`wv_reconcile`）| `43825` B | `43825` B | ✅ 逐位相同 |

🔑 **比對框之判別力自證**（交接文陷阱 `1`）：**窄框** `^\s*(✅｜🔴)\s+(PASS｜FAIL)\s+` 得 **`34`** 列；
**放寬框** `^  (✅｜🔴｜🟡) ` 得 **`71`** 列 ⇒ **多抓 `37`** ——與交接文所載之 `34`／`71`／`37`
**逐數吻合** ⇒ 二框可分、舊框確有漏（其漏者即「末端夾具」形）。

### 🟢 `V-1`　引擎零影響

| 量 | 實測 | 判 |
|---|---|---|
| `verify/out/got_*.csv` | **`8` 檔**（含 `_partial`）逐檔 `diff` **空** | ✅ 相異 `0` |
| `verify/out` 全部產物檔數 | A `521` ／ B `521` | ✅ |
| **判別力**（器須紅）| 對一 `got_*` 造 **1-byte** 差之人造對照 ⇒ 該檢**偵出** | ✅ 非恆綠 |

### 🟢 `V-2`　`K-9-1` 值不變

| 受詞 | 判 |
|---|---|
| `k91_effective_min_build_area`／`k91_min_alloc_contrib_by_blk` 之**函式源碼**（AST segment）| ✅ **逐位相同** |
| `K91_SS_MBA_BY_CATEGORY`／`_BY_LABEL`／`_EFFECTIVE` 三常數之賦值 | ✅ **逐位相同** |
| 消費端 `_k91_contrib = k91_min_alloc_contrib_by_blk(`／`_k91_eff = {b['label']: …` | ✅ 前 `1`／後 `1` |
| runtime 側 | ✅ 由 `V-1` 涵蓋（`got_*` 逐位相同）|

### 🟢 `V-3`　掛點給證（**AST 實查**·⛔ 以「看起來在」為據）

| # | 受詞 | 行區間 |
|---|---|---|
| ① | `with st.form("f3_road_form", ...)` ＝ AST `With` 節點 | `[19617, 19788]` |
| ② | 其內之 `for b in build_blocks:` | `[19620, 19787]` |
| ③ | 其內之 `with st.expander(...)` | `[19625, 19787]` |

**三新元件之 `lineno` 皆落於 ③ 之區間**：工項二 `text_input` `:19670` ✅／
`k91_effective_min_build_area` 之呼叫 `:19709` ✅／工項一 `st.caption` `:19713` ✅
⇒ **共同之 `with` 區塊 ＝ ③；共同之迴圈變數 ＝ `b`（②）**。

🔑 **判別力（須為 `False`）**：`submit_road` 之 `form_submit_button` `:19788`
在 `expander` 內 ＝ **`False`** ✅ ⇒ 該檢**非恆真**。
🔒 **位序**：`checkbox :19659` < `caption :19713` ⇒ **確在正面道路區塊下方**。

### 🟢 `V-4`　混型欄（**型別層**）

**器** ＝ `verify/probes/probe_wg9248_arrow_mixed.py`（本批入倉·⛔ 生產碼）。
其自 `app.py` **AST 抽取** `wg9248_stringify_mixed_cols`（`1265` B）並 exec
（⛔ import／exec 整個 `app.py`）。`pandas 2.3.3` ／ `pyarrow 23.0.1`。

| DF | ⓪ 靶自檢 | ① 改前 | ② 改後 | ③ 判別力（還原一欄之混型）| ④ 哨兵語意 |
|---|---|---|---|---|---|
| DF-1 | ✅ `4` 欄皆混型 | 🔴 `ArrowInvalid` | ✅ 成功 | ✅ **失敗**（須失敗）| ✅ `'—'` 四欄皆可辨 |
| DF-2 | ✅ `4` 欄皆混型 | 🔴 `ArrowInvalid` | ✅ 成功 | ✅ **失敗** | ✅ |
| DF-3 | — | 🔴 `ArrowInvalid` | ✅ 成功 | `dtype` `object` → **`float64`** | ✅ `na_rep="-"` 之渲染含 `-` |

🛑 **界限（照實·⛔ 頂替）**：本器所證者為**型別層**。單 `V-4` 所令之
「**拋棄式實跑**·`stdout` 之 `ArrowInvalid` 命中 `0`」須操作 UI 至該三頁面
⇒ **屬 `V-K`（KL 目檢）之射程**，本批⛔ 辦。

### 🟢 `V-5`　道路名稱欄**無消費端**

| 母體 | 受詞 | 命中 |
|---|---|---|
| `app.py` **外**之追蹤 `.py`／`.md` **`855`** 檔 | `SS_FRONT_ROAD_NAME`／`f3_front_road_name_by_bid`／`f3_frname_` | **`0` 檔** ✅ |
| `verify/` 八檔（`stepg_pipeline`／`run_verification`／`wf_f0`–`f4`／`selection_pipeline`）| 同上 | **`0`** ✅ |
| `_lot_gate` 之函式體（`:12330`–`:12511`·`181` 列）| `front_road_name` | **`0`** ✅ |
| `app.py` 內 | `SS_FRONT_ROAD_NAME` **`5`** 列 | 定義 `1` ＋ 讀取／寫入／渲染（**皆本批新增**）|

### 🟢 `V-B`／`V-C`

`V-B`：`app.py` diff 逐行——`c1` 之 `3` 刪已於 `§零` **逐行具名**；`c2` `0` 刪。
`V-C`：**十檔 ＝ 變 `1`／不變 `9`**（僅 `app.py` 變·`5693242b19d970c1` → `4860f10828fb8ae9`）；
夾具 `verify/fixture_wf_ns_wiring.py` ＝ `b17826a6f7b98bf5` **不變** ✅

### ⬜ `V-K`（KL 目檢）——**候辦**

併線後 KL 展開任一街廓卡片，須見 **① 最小建築面積（唯讀·現算）** 與 **② 正面道路名稱欄**；
`stdout` **⛔ 再噴 `ArrowInvalid`**。此為本批之**最終驗收**，由發單側轉呈。

---

## §五　CC 自捕**四則**（皆於**入倉前**·責 ＝ CC·⛔ 入 claude.ai 側自誤簿）

🔒 KL `2026-09-07` 逐字：「CC 自捕二則追認，⛔ 入 claude.ai 側自誤簿（簿之受詞不符），
**報告內之具名即其登記**。」本節即其登記載體；段甲之二則見
`docs/reports/W-G.9-248R_段甲_工項零與工項四.md`，本節另補段乙之二則。

| # | 受詞 | 根因 |
|---|---|---|
| `1`（段甲）| **壞錨**：`st.form("f3_road_form")`（**含右括號**）實測命中 **`0`**——真碼為 `with st.form("f3_road_form", clear_on_submit=False):` | **以固定字串為錨者，⛔ 含其右界之標點**（真碼常於該處續接參數）|
| `2`（段甲）| 報告 `S-1` 表之**檔框**誤寫 `1`、實為 `2` | **`檔框 ≠ D1`**（＝ `\|D1 ∪ D2 之檔 ∪ D3 之檔\|`）|
| `3`（**段乙·新**）| 首版 `c2` 驗證器判二函式「**逐位不同**」係 **量測器紅** | 工作區為 **CRLF**（`autocrlf=true`）而 `git cat-file blob` 為 **LF** ⇒ **二端量測框不同**，差恰 ＝ 該函式**行數**（`13` B／`13` 行）。`git diff` 顯示該二函式僅為 **context line** ⇒ 未改。同族 ＝ `GB-139`／「位元組數須聲明量測框」 |
| `4`（**段乙·新**）| `V-4` 探針首跑 DF-2 二紅係 **靶之誤** | 靶中 `【左】達資格候選` 二列**皆為 `int`、無哨兵** ⇒ 該欄**本就不混型** ⇒ 「還原其混型須失敗」必**偽綠**、「哨兵可辨」必**偽紅**。已補一列「左無右有」使四欄皆混型，並加**靶自檢**（每一受詞欄須實際混型）。同族 ＝ 交接文陷阱 `7`（**靶未涵蓋受詞**）|

🩸 **併記（CC 自陳·⛔ 隱去）**：本窗尚有一次**轉義層**踩雷——以 `python -c` 經 shell 傳遞含
`\r\n` 之字面時，該逃脫序列被 shell 展開為**真換行**，毀了驗證器（`SyntaxError`）。
⇒ **`GB-143` 同族**：**含反斜線之字面一律 `Write` 落檔、⛔ 經 shell 傳遞**。已改以 `Edit` 修復。

🔑 **四則之共相**：`1`／`2`／`3`／`4` 皆為「**量測器紅而非受詞紅**」之形；
`3`／`4` 更係**在同一批內**接連發生 ⇒ **`CLAUDE.md`「立戒者在下一批最容易違反自己的戒」
於本批<u>四度</u>應驗**。

---

## §六　鑄 `自誤 311`

**末號當場復算**（三軸 ＝ `blob@6010a80`／登記表**單檔** `622641` B・`7278` LF／**列框**·
框 ＝ `VR-091 四`）：列命中 `308`／範圍框 `8`／相異 `303`／`MIN 7`／**`MAX 310`**／
缺號 **`[106]`**／恆等式 **`True`** ⇒ **裁示載 `MAX 310` ✅ 相符** ⇒ **`N = 311`**。
**對照組**：既有 `310` 命中 `1`（須 `>0`）✅／未鑄 `311` 命中 `0`（須 `0`）✅。
**意指占用**（二形並取·母體 全 `docs/` `580` 檔／可讀 `561`）：
`自誤 311` ＝ `0`／`0`；`` 自誤 `311` `` ＝ `0`／`0`；**對照組** `自誤 310` ＝ 檔 `3`／列 `8` ✅。

**所鑄之則** ＝ `## 自誤 \`311\`　**令一件 Streamlit 語意上做不到的事（\`-248 §二 b\` 之覆寫欄移入）**`
——**責 ＝ 發單側**。其要旨：`§二 b` 令「須移入」，而 `I-2` 令「移入若延遲則停機」，
**延遲係該容器之語意所必然** ⇒ **該二款自始互斥**、該令**自始不可能被滿足**。

---

## §七　收工閘（判準先行載明·⛔ 事後改判準）

| 閘 | 判準 |
|---|---|
| `S-1` | `numstat` 刪除欄逐列為 `0`（本 commit ＝ 報告 ＋ 自誤簿追加 ＋ 新探針）|
| `S-2` | 生產碼判法逐字指令 ⇒ **空輸出**（`rc` 直取·`GB-145` 二 rc 分報）|
| `S-3` | 十檔 ＋ 夾具 `sha256[:16]` 前後**逐位相同**（本 commit 零生產碼 ⇒ 變 `0`／不變 `11`）|
| `S-4` | `MAX` 由 `310` → `311`；`311` 在命中集且⛔ 在缺號集；缺號集恆 `[106]`；恆等式 `True` |
| `S-5` | `wg942_append_audit`（嚴格前綴 ＝ `True`）證自誤簿未被改寫 |
| `S-6` | 新探針⛔ 被 `run_all`／`run_verification` 消費（二檔對其檔名之命中須 `0`）|
| `S-7` | 側分支 `origin/verify/W-G.9-248-c1` ＝ `89a9a7a`；`origin/wip/s1-endpart` 於本 commit 前 ＝ `6010a80`（**未被生產碼污染**）|
