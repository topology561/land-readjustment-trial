# W-G.9 開波：**第 2 宗最小規定範圍** —— 範圍界定

**產出**：CC／2026-08-10（`D-2b-25` §5）　**基準**：`78079fa`
**波名依據**：`docs/reports/` 之波名採 `W-G.<n>` 序列（現有 `.4`／`.5`／`.6`／`.7`／`.8`），
本波為次順位 **`W-G.9`**；`grep -rn "W-G\.9" .`（排除巢狀 worktree）⇒ **0 命中**、不相撞。

> ⛔ **本檔為範圍界定文件**：**只界定範圍，⛔ 零實作**。
> ⛔ 不得於本波實作；⛔ 不得動 `D-2b` 已落地之任何一行。

---

## 一、目標

**第 2 宗最小規定範圍**之構造與判準——依正典
`grep -n "^### 🔒 K-9-5-9" docs/rulings/K-6_街角地分配程序與可分配判準.md` 之
**【裁】5／【裁】6／【知會·未見異議】8**（KL 裁 2026-08-10）。

三者逐字要旨（⛔ 全文以正典為準、本檔不重述）：

- **【裁】5（第 1 種）**：第 2 宗與第 3 宗共用之境界線**平行 ALLOCLINE 方向**；
  第 2 宗最小規定範圍 ＝ 過 `Ps` 之 ∥ALLOCLINE 直線**向街廓內平移畸零地寬**所夾之範圍；
  **最小寬度落在 FRONTLINE 交點**。
- **【裁】6（第 2 種）**：**最小寬度落在 BASELINE 端**；FRONTLINE 與 BASELINE 不平行但皆為直線
  ⇒ 距離沿深度**線性**遞增／遞減 ⇒ 須於**最小深度帶**下解出畸零地寬之位置。
- **【知會 8】**：範圍用**街廓層**最小寬度帶（前緣線沿 BASELINE 法向平移 `round(D_avg,2)` 所夾之帶）；
  **事後寬度驗收仍用該宗自己之帶**。兩把尺不同係**刻意**（打斷寬度／深度互相依賴）。

🛑 **圖證待補**：【裁】5／6 所指之三張示意圖**尚未入倉**（`D-2b-25` §2·訊息未附圖檔）
⇒ **本波動工前應先取得**；在此之前，幾何細節**以正典文字為準**，⛔ 不得以倉內近似圖代替。

---

## 二、已知可重用之機具：`K-8 §三〜§五` 之解析構造

**構造（口語）**：取平行 ALLOCLINE 之直線往街廓內**平行滑動**，
直到帶內二側界間、**平行前緣線**之距離**最小值** ＝ 門檻。

**解析捷徑（逐字·現查 `grep -n "w_lo = min" app.py` ⇒ `:11031`）**：

```
k = sigma / den_a ;  w_lo = min(_W(0,0), _W(D,0)) ;  t_star = (T - w_lo) / k
```

> 🛑 **【`D-2b-29` §2-3 加註·⛔ 上文原句不刪·⛔ 未改寫 §2-1 之判讀】**
>
> **上引之 `app.py:11031` 係「`#` 註解」，⛔ 非活碼。**
> 現查（`grep -n "w_lo = min" app.py` ⇒ **全檔僅 1 命中**，即 `:11031`）：
> 該行位於 `K-6-A2 段三(c)` 之註解區，其自載逐字為
> 「⛔ **舊式已移除**（原為「帶深固定＝`round(D_avg,2)` ＋ 斜率＝`c`」之 …）
> ——即 **`GB-14` ①②** 所登記之二誤」。
>
> ⇒ **上引之式係<u>已被移除</u>之舊式，且其本身即登記在案之二誤**：
> ① 帶深應為**範圍自身之最小深度 `D(t)`**（非街廓平均深度之常數）；
> ② `k<0 ∧ m≠0` 時正解斜率為 **`(c − k·m)`**（非 `c`）。
> `GB-14` 現況為 ✅ **已結案**（`K-6-A2 段三(c)`·2026-08-04·「舊式已移除」）
> ——現行活碼走 `k97_solve_alloc_t`（`grep -n "_k97 = k97_solve_alloc_t" app.py`）。
>
> 🔴 **⇒ 下方 §2-1 之判讀係建立在<u>已移除之舊式</u>上**：
> ⛔ **在坐實前不得引用**；活碼之對應結構見 `D-2b-29` §4 偵察結果
> （`docs/reports/W-G.8_D-2b-29_兩裁入正典與WG9前置偵察.md` §4）。
> 🔒 **§2-1 判讀之存廢屬 KL**——⛔ 本加註只陳事實，**未改寫、未刪除該判讀一字**。

### 2-1 🔴 判讀：**同一構造之兩解、非兩套構造**

`w_lo = min(_W(0,0), _W(D,0))` 取二端點之較小者：

| `min()` 取到 | 對應之情形 |
|---|---|
| **前緣端**（`_W(0,0)`） | **第 1 種**（【裁】5·最小寬度落在 **FRONTLINE 交點**） |
| **BASELINE 端**（`_W(D,0)`） | **第 2 種**（【裁】6·最小寬度落在 **BASELINE 端**） |

⇒ 二者**係同一解析式之兩個分支**，⛔ **非**兩套構造。

> 🔴 **標記：claude.ai 現查所得·<u>待實測坐實</u>**
> ⛔ **不得逕列為事實**。本波動工時須以實測驗證此判讀
> （至少須示：於本案六街廓上，`min()` 之取點與【裁】5／6 之分類**逐格對應**）。

---

## 三、算子現況（**CC 自行 AST 現查**·⛔ 未引任何他處所載之數）

**現查於 `78079fa`**：

| 項 | 方法 | 結果 |
|---|---|---|
| `k97_solve_alloc_t` 之定義 | `ast.FunctionDef` 窮舉 | **1 處**（`app.py:10398`） |
| `app.py` 內之呼叫 | `ast.Call` 窮舉（比對 `func.id`／`func.attr`） | **0** |
| `_WF_NS_NAMES` 是否納入 | 取該 `Assign` 之 `elts` 常數清單 | 清單長度 **25**，**不含** `k97_solve_alloc_t` |
| 全倉其他命中 | `grep -rn "k97_solve_alloc_t" --include=*.py .`（排除巢狀 worktree、`app.py`、`verify/probes/`） | 僅 `verify/fixture_corner_range_k8.py:68`／`:81`／`:148`，**三處皆為否定敘述**（「⛔ 不自 `k97_solve_alloc_t` … 取任何回傳值」）⇒ **非呼叫** |

⇒ **`k97_solve_alloc_t` 定義在倉、生產呼叫數 0、`_WF_NS_NAMES` 未納入。**

🔒 **正典明令其不得刪**——「D 波之範圍界線」第 5 項逐字
（`grep -n "第 2 宗轉為楔形後" docs/rulings/K-6_街角地分配程序與可分配判準.md`）：
「`K-9-6`／`K-9-7`／`K-9-8`／`GB-36`／`GB-40`／`k98_virtual_measure_block`：
對街角第 1 宗失效，但 ⛔ **禁刪除、禁停用、禁標作廢**——**第 2 宗轉為楔形後可能沿用同一套量測機具**。」

---

## 四、套用時須換之兩個參數

| # | 參數 | 現行（街角第 1 宗） | **本波應換為（第 2 宗）** |
|---|---|---|---|
| 1 | **門檻** | 退縮寬 ＋ 畸零地最小寬 | **畸零地最小寬** |
| 2 | **固定之一側界** | 截角前之**側街境界線** | **街角第 1 宗之遠側界** |

⚠️ 參數 2 之「街角第 1 宗之遠側界」即 `K-9-5-4 ②` 所定之 ∥SIDELINE 界線，
其實作已於 `D-2b-23` commit B 落地（`grep -n "K-9-5-4 ② 之唯一決定點" app.py`）。

---

## 五、本波不授權

1. ⛔ **不得於本波實作**——本檔僅界定範圍。
2. ⛔ **不得動 `D-2b` 已落地之任何一行**。
3. ⛔ 不接線 `k97_solve_alloc_t`、不加入 `_WF_NS_NAMES`（動工須另取授權）。
4. ⛔ 不得於圖證入倉前，就【裁】5／6 之幾何細節作任何**逾越正典文字**之推定。

---

## 六、動工前之已知前置

| # | 前置 | 出處 |
|---|---|---|
| 1 | **三張示意圖入倉** | `D-2b-25` §2（訊息未附·待補） |
| 2 | §二-1 之判讀**經實測坐實** | 本檔 §2-1（現標「待實測坐實」） |
| 3 | **`GB-53`**（`_end_band` 之量法與正典不一致）之處置時點 | `docs/reports/W-G.4_泛用阻塞項登記表.md` |
| 4 | 【裁】1 所列之另案（R1 選槽依據與實際落地不符）**是否影響本波** | 正典 `K-9-5-9`【裁】1 逐字「⛔ 二者**不得由『另案』推定**，須另行請裁」 |
