# W-D.4 合併分配規格（分級清單波，claude.ai 撰）

> 性質：**施工規格**。CC 據此進 plan mode → reviewer → harness 綠 → push。
> 波性質：**清單波**。零幾何移動、零財務實算。動的只有：新診斷輸出、harness 閘、文件。
> 停機條件：任何 G／守恆／位次／J／k* 逐格變動＝越界。run_all 既有全部閘全綠＝零移動之證。
> 前置：《W-D.4_域裁鎖定.md》兩條 backlog（ALLOC 靜默、硬編住宅區）**先清再開工**。

---

## 1. 目標

把 22 筆 `⚠️移出/第1調配順位` 旗標（兩情境同名單）**消費**成一份歸戶級四梯分級清單：每一歸戶標明所處梯次、對應 v3 級別、建議路徑與參考估算值。此清單為 W-F 七級調配引擎之**驗收準源**。

## 2. 輸入

| 來源 | 內容 |
|---|---|
| G 值計算結果（v2 baseline 同源管線） | 逐宗 G、S、宗地寬度、畸零地旗標、所屬街廓、原地號、重劃前區段 |
| Tab1 清冊（快照同源） | 所有權人歸戶鍵、持分、**他項權利欄、禁止移轉欄**（本案全空） |
| 參數表（快照） | 逐街廓：分配深度、分區、正面路寬 |
| 畸零地附表查表 | min_width_i = f(分區_i, 正面路寬_i)（**逐塊查，禁寫死**） |
| Tab4（快照） | 逐街廓重劃後評定地價（R1/R4 99,424.43；R2/R6 94,453.21；R3/R5 89,481.99） |

**術語（KL 釘定）**：應分配權利面積＝G；重劃前原有面積＝a。法源：辦法 §31（分配方法）、§52（差額地價）、§53（現金補償兩式＋放棄改領）、§26/27（臨街地特別負擔尺度，引擎已全表實作）。

推導量（**harness 內算並斷言，禁寫死**）：
- 逐街廓最小分配面積 `MinA_i = 分配深度_i × min_width_i`
- 區標準 `MinA_區 = MIN_i(MinA_i)`；1/2 線 `= MinA_區 / 2`
- UC9898 期望值（斷言錨）：MinA_區 = 114.07（R4）、1/2 線 = 57.03

## 3. 演算法（四步）

### 步0・歸戶聚合（KL 撤銷改判 2026-07-08：歸戶主體＝Gxxx 指紋群，非 per-person）
- 以 **Gxxx 歸戶群組** group 全部業主宗地（build_ownership 之 `t8_ownership_groups`；指紋已含
  **持分＋他項權利內容**，claude.ai 進碼證實 → **禁碰禁擴** build_ownership）。梯次判定以
  **群組總額**為準（達標群也要能證明達標）。
- 群組應分配面積 `ΣG_戶 = Σ G_i`（群內各宗於原街廓計算之 G **直加**；指紋已鎖定持分結構故
  **不另做持分×G 拆算**；**不做 a′ 換算**——a′ 僅實際跨街廓改配時生效、屬 v3）。
- 共有宗地：同一 Gxxx 群內持分結構一致（指紋鎖定）；**持分和檢核保留**（分別共有 Σ持分≈1.0
  之 sanity；異常→旗標）。**拆單**（拆共有為個別分配）＝**後置意思決定**（不可自動）：本波僅出
  「拆單候選旗標」＋記載管線性質「**拆單成立→全案重跑**」，不執行拆算。

  > **【計算順序鐵律】共有土地個別分配（手冊 §四(一)；KL 裁定 2026-07-09）**
  >
  > 拆單成立時，計算順序恆為 **① 先按持分拆 `a`（重劃前原有面積）→ ② 再對每份 `a` 各自算 `G`**。
  > **禁「先算共有整宗之 G、再按持分拆 G」**。
  >
  > 理由：`G = [a(1−A·B) − Rw·F·l₁ − S·l₂]×(1−C)` 對 `a` **非線性**（`Rw·F·l₁` 與 `S·l₂`
  > 兩項負擔不隨 `a` 等比例縮放），故 `G(Σaᵢ) ≠ ΣG(aᵢ)`。先拆 G 會把整宗之臨街負擔錯誤攤給
  > 各持分人，且違反「每筆分配地各自承擔其臨街負擔」之法意。
  >
  > 本專案管線**天然滿足**此序：拆單成立 → 產生新的個別宗地（各帶自己的 `a`）→ **全案重跑**
  > Step G。**本註之作用在防未來於清單層（W-D.4／W-F）圖便宜而「拆 G」走捷徑**——
  > 清單層之 `ΣG_戶` 係群內各宗**既有 G 之直加**（各宗 G 本就由各自 `a` 算得），
  > 與「拆 G」性質不同，不得混淆。

### 步0.5・第4梯前置過濾
- 歸戶內任一宗他項權利欄或禁止移轉欄非空 → 該歸戶標 `阻卻=是`（不得合併分配；仍可原位分配）。
- 本案預期全空 → 全數 `阻卻=否`，此為有效驗收結果。
- **宣告紀律**：欄位缺失（非空值、是整欄不存在）→ RuntimeError，不得靜默視為無阻卻。

### 步1・逐宗達標判定
- 宗 i 達標 ⟺ `G_i ≥ MinA_(所屬街廓)` **且** 該宗無畸零旗標（S=0 或寬<最小寬者即使 G 夠大亦不達標——裁示 1 幾何準則優先）。
- 歸戶內全數達標 → **第1梯**：逐宗個別分配，無動作。

### 步1.5・同宗跨占分配線判定（辦法 §31-1-3）
- 同一原地號被 overlay 拆為多街廓暫編（如 628-1(1)/(2)）者：各側 G 對**該側原街廓 MinA** 個別判；
  未達側標 `跨線併大側` 並指向面積較大側之暫編（清單標註，搬移屬 W-F）。
- 此判定先於歸戶聚合之梯次分流（宗級規則優先於歸戶級）。

### 步2・歸戶級分流
對含未達標宗之歸戶，依 `ΣG_戶`：

| 條件 | 梯次 | 路徑標註 | 參考估算（標「參考值，a′ 待 v3」） |
|---|---|---|---|
| ΣG_戶 ≥ MinA_(某可承接街廓)，即集中後可達標 | **第0梯** | 集中合併於應分配面積較大之宗（列標的宗暫編地號）；不得合併於公設地及依法不能建築地 | — |
| MinA_區/2 ≤ ΣG_戶 且集中仍無法達任一街廓標準 | **第2梯（雙出口）** | 出口a：協調共有（預設不成）→ 增配（§31-1-2 後段＋§52-1）。候選目標：最淺深度街廓（R4）＋最低後地價街廓（R3/R5）。出口b：按標準配後**申請放棄改領現金**（§53-2） | 出口a：增配面積≈MinA_目標 − ΣG_戶；差額地價≈增配面積×目標街廓後地價。出口b：放棄補償≈ΣG_戶×分配位置後地價 |
| ΣG_戶 < MinA_區/2 | **第3梯（兩式）** | 申請與他人合併共有（預設不成）→ 現金補償不配地（§31-1-2 前段＋§53-1） | 本文式：補償≈Σa_i×原位置後地價；但書式（勘定後自行分割、繼承/強執除外）：Σa_i×原位置**前**地價（旗標欄，前地價 v2 半中間態下僅標式不算值）；§53-3 他項權利協調旗標 |

（第0梯標的宗選擇：歸戶內 G 最大之達標宗；若歸戶內無達標宗但 ΣG 集中後 ≥ 該宗所在街廓 MinA，仍屬第0梯集中——即 v3 級0' 弱弱聯合。）

### 步3・S=0 抵費地遞補標註（不執行、只標註）
- 對 W-D.3 三分類之每一「碎片」，依域裁 (B) 搜尋規則沿分配序往前找**遞補標的宗**（第一筆 S≠0 且宗地寬度≥min_width_i 者，跳過失格筆），輸出 `碎片→標的宗` 對照（UC9898 斷言錨：R6 85.66 → 628-1(2)）。
- 同角落有失格業主者，加註 `定序=先解業主`（域裁 C）。
- 形狀調整本體屬 W-F，本波僅產標註。

## 4. 輸出（新 CSV，入 v2 baseline）

`W-D.4_四梯分級清單_退縮{0m,3.5m}.csv`，一列一歸戶：

```
歸戶鍵 | 宗數 | 宗清單(暫編) | ΣG_戶 | 含畸零旗標宗數 | 各宗原街廓MinA |
梯次{1|0|2|3} | 對應v3級 | 阻卻 | 路徑標註 | 標的宗/候選目標街廓 |
增配面積(參考) | 差額地價(參考) | 放棄改領現金(參考,§53-2) | 補償額(參考,§53-1本文/但書旗標) | 跨線併大側標註(§31-1-3) | 碎片遞補標註 | 定序註 | 備註
```

附表 `W-D.4_碎片遞補對照_退縮{0m,3.5m}.csv`：`碎片片名 | 標的宗 | 跳過筆 | 定序註`。

參考估算欄一律帶 `(參考,a′待v3)` 後綴語意（欄名或表頭註記擇一，reviewer 驗其存在）。

## 5. 驗收閘（入 run_all）

1. **零幾何移動**：既有全部閘（18＋W-D.3 三閘）對凍結 baseline 逐格不變。
2. **旗標全消費**：22 筆畸零旗標宗，每筆恰出現在某歸戶列之宗清單（無遺漏、無重複）。
3. **推導斷言**：MinA_區==114.07、1/2線==57.03 由參數推導而得（改參數表任一分配深度，值須隨動——reviewer 反向篡改測試）。
4. **遞補標註錨**：R6 85.66 → 628-1(2)、跳過 628(2)。
5. **第4梯 fixture**：harness 合成 1 筆帶查封註記虛構宗（不入 baseline、不碰 Tab1），斷言其歸戶 `阻卻=是` 且被排除於第0梯標的宗候選；移除 fixture 後輸出與 baseline byte-identical。
6. **決定性**：兩次乾淨執行 byte-identical（`lineterminator='\n'`）。
7. 清單 CSV 轉正 v2 baseline＋PROVENANCE 補記（產出 commit、參數身分、「參考估算非實算」聲明）。

## 6. 邊界與禁區

- 不動 app.py 分配引擎；app 側至多加一個唯讀清單展示（可選，非本波必要）。
- 不呼叫 `_allocate_three_tier_v1` 等三個待廢函式。
- 不算 a′、不做增配/補償/共有拆併實算、不跨街廓搬地。
- 歸戶鍵、他項權利欄名以 Tab1 實際欄名為準（CC 偵察後入 plan，禁假設）。

## 7. 與後續波之接線

- 本清單＝W-F 七級引擎驗收準源（每歸戶「應走路徑」為 W-F 之 expected）。
- v3 財務接線後：參考估算欄升級為實算（a′、增配、差額地價、補償），本規格第 3 節步2 之公式即實算規格。

---

## 🔧 KL 之二裁（`2026-09-12`）之登記（`W-G.9-270` 補令一 `§一`·⛔ 上文原句一字不刪·純末端追加）

🛑 **本節⛔ 鑄任何號**——其為**裁之登記**（`戒 36`：非鑄號者⛔ 得採該類號之定義列式形）。
🔒 **落點之依據** ＝ `docs/orders/W-G.9-270_補令一_KL二裁與GB164.md` 之 `§六` 序 `6` 逐字
（「`§一` 二裁入倉（其落點 ＝ **`docs/W-D.4_合併分配規格.md` 之末端追加**·⛔ 追改其原文）」）。
🔒 **本節⛔ 追改本檔第 `1`–`121` 列任一字**（`append-only`·機械驗：改前全檔為改後之**嚴格前綴**）。

---

### 🔧 一　裁一：梯次之判準（**⛔ 論異動歷程**）

🔒 **KL 逐字（`2026-09-12`）**

> 「經 `5` 級調配後仍未有可分配土地，若該同歸戶土地合併後的重劃前面積 `G(a+a'+…)`，就有無達最小可分配街廓面積的 `×1/2`……**但是這個程式目前是為了辦理市地重劃可行性評估試分配程式，所以先不討論土地的異動歷程（分割沿革及是否繼承），一律將未達 1/2 者採現金補償，達 1/2 者一律增配土地**」

🔒 **射程之界（KL 所明示）**：本裁之由為**本程式係「市地重劃可行性評估試分配」**
⇒ 分割沿革／繼承／強制執行之認定**⛔ 在本程式之射程內**。
**日後正式辦理時之適用，⛔ 為本裁所及。**

🔒 **本裁與程式<u>現行行為一致</u>，⇒ 係<u>確認而非變更</u>**——CC 於 `W-G.9-270R5` **當場實跑復現**
（器 ＝ `verify/probes/probe_WG9270R5_p4_tier_band.py`·真相源 ＝ `verify.wd4_tier_list.compute(fixture=False)`·
**記憶體側**·⛔ 讀 `verify/baselines`·落檔 `verify/out/WG9270R5_p4_tier_band.log`·`rc = 0`·
態 ＝ `blob@946a60d036844565cc11f351f2ed3ee80752b8c2`）：

| 歸戶 | `ΣG_戶(㎡)`（`0m`／`3.5m`） | 宗數（`0m`／`3.5m`） | `2×ΣG ≥ MinA_區`？ | 程式判 |
|---|---|---|---|---|
| `G011` | `89.96`／`94.73` | `1`／`2` | **達** | 梯次 `2` ⇒ **增配土地** ✅ |
| `G025` | `0.26`／`0.26` | `1`／`1` | **未達** | 梯次 `3` ⇒ **現金補償** ✅ |
| `G030` | `26.54`／`56.15` | `1`／`2` | **未達** | 梯次 `3` ⇒ **現金補償** ✅ |

🔒 **母體之補全（CC 照實·⛔ 頂替）**：上表係**取樣三戶**；本案**梯 `2` 實為 `3` 戶**
（`G004`／`G011`／`G033`）、**梯 `3` 實為 `2` 戶**（`G025`／`G030`），二情境同。
⇒ 上表**⛔ 全梯 `2`／`3` 之母體**；其餘二戶（`G004` `ΣG 71.60`／`G033` `ΣG 72.77`·皆宗數 `1`·皆**達**）
之判亦與本裁一致。**自洽驗算**：群組總數 `33` ＝ Σ軌別（建地軌 `25` ＋ 公設軌 `8`）＝ Σ梯次 ✅。

🔒 **判定式之形（`verify/wd4_tier_list.py` 逐字·字樣錨 `2×ΣG≥MinA_區`）**：
**`2×ΣG ≥ MinA_區`（整數倍、無除法無二次 round），½ 值僅供顯示**
——其已避開 `round(57.035, 2)` 之 banker's／float 刀口（同檔 `_half_display` 之 docstring 逐字：
「Python `round(57.035,2)=57.03` 之 banker's/float 刀口」）。**發單側核可該設計。**

🔴 **`MinA_區` 之值⛔ 擇一（本節⛔ 定之）**：補令一 `§一-1` 原載「本案實值 **`114.07 ㎡`**（`R4`）·½ **顯示值** `57.04`」，
而 CC 當場實跑得**活體實算 ＝ `115.88`**（½ 顯示 `57.94`）。
🔒 **四值並列及其角色**（`CLAUDE.md` 之四值並列款·⛔ 以任一為唯一真值作推論）：
`114.07` ＝ **期望值·斷言錨**（本檔第 `5` 節驗收閘 `3` 之推導斷言／`verify/wd4_tier_list.py` 之 `MINA_QU_EXPECT`）／
`115.85` ＝ `CLAUDE.md` 之現值記載（½ 顯示 `57.93`）／
**`115.88`** ＝ **活體實算**（½ 顯示 `57.94`）／`115.885` ＝ 正典形＠活體。
🛑 **其擇須經 KL**（½ 門檻為 `57.93` 抑或 `57.94` ⇒ **決定一戶領地抑或領錢**）；
其歸屬判（`p7`）已於 `docs/reports/W-G.9-270R3_補令三_p6p7.md` **停機上呈**，**現在發單側手上**。
🔒 **CC 之當場實測（對本裁之射程有意義·照實）**：本案**⛔ 任一戶之 `2ΣG_戶` 落於 `[114.07, 115.88]` 區間**
（最近者 ＝ `G030`＠`3.5m` 之 `112.30` 與 `G004` 之 `143.20`）
⇒ 🔒 **四值之擇一於<u>本案</u>⛔ 改任一戶之梯次**；⚠️ **⛔ 外推至他案**。

---

### 🔧 二　裁二：現金補償之適用式

🔒 **KL 逐字（`2026-09-12`）**

> 「**現金補一律以本文式**」

⇒ 🔒 **梯 `3`（未達 ½）之現金補償，一律採<u>本文式</u>**：
`補償 ≈ Σa_i × 原位置`**`後`**`地價`（§53-1 本文·即本檔第 `3` 節步 `2` 表列所載之「本文式」）。
🛑 **但書式（× 原位置<u>前</u>地價）自此⛔ 為出艙之補償額**——其欄**⛔ 刪、⛔ 停算**
（保留以備日後正式辦理時之對照），惟凡出艙補償額者，**一律取本文式**，
且須同格具名「**依 `W-G.9-270` 補令一 `§一-2`·KL `2026-09-12` 裁**」。

🔒 **碼面之對應（CC 當場現查·⛔ 猜）**：`verify/wd4_tier_list.py` 之 `tier == "3"` 分支同時算二式
——`_comp_main`（字樣錨 `本文式：後地價`）與 `_comp_proviso`（字樣錨 `但書式：前地價（v3 首次可實算）`）
⇒ 🟢 **碼面之現行行為已合本裁**（**算而不用為出艙額**）；**⛔ 本批動其一字**。
🔴 **併記（照實·⛔ 自裁）**：本檔第 `3` 節步 `2` 表列逐字載但書式為
「旗標欄，前地價 `v2` 半中間態下**僅標式不算值**」，而碼面自 `v3` 起**已實算**
（`G025` `16,422`／`G030` `1,742,230`＠`0m`·`3,657,639`＠`3.5m`）
⇒ **該句已不同步**；🛑 **⛔ 追改該句一字**（`append-only`），其處置候發單側。

🔒 **本裁下之補償額（CC 當場實跑復現·與補令一 `§一-2` 之表<u>逐位相符</u>）**

🛑 **三項齊載（補令一 `§一-2` 明令）**：
`(1)` **估算性質 ＝ `半實算(試分配·非正式)`**（⛔ 稱其為「最終補償數」）；
`(2)` **時態錨** ＝ `blob@946a60d036844565cc11f351f2ed3ee80752b8c2`·落檔 `verify/out/WG9270R5_p4_tier_band.log`；
`(3)` 🛑 **`GB-159`：受影響之宗之配地圖與負擔表 ⛔ 對外**（含本表之數）。

| 歸戶 | 梯次 | `0m` | `3.5m` |
|---|---|---|---|
| `G011` | `2` | 增配 `25.92 ㎡`／§52-1 `1,976,296` 元／§53-2 `6,516,134` 元 | 增配 `21.15 ㎡`／`1,612,602` 元／`6,861,642` 元 |
| `G025` | `3` | **本文式 `30,193` 元** | **本文式 `30,193` 元** |
| `G030` | `3` | **本文式 `3,381,204` 元** | **本文式 `6,902,853` 元** |

🔒 **母體之補全（CC 照實）**：梯 `2` 另有 `G004`（增配 `44.28 ㎡`／§52-1 `3,376,172`／§53-2 `4,913,291`·二情境同）
與 `G033`（增配 `43.11 ㎡`／`3,286,964`／`5,270,999`·二情境同）——**三項齊載同上**。

---

### 🔧 三　本節**⛔ 辦**之事

**⛔ 追改本檔第 `1`–`121` 列任一字**（含第 `3` 節步 `2` 之但書式描述、第 `5` 節之 `114.07`／`57.03` 推導斷言）；
**⛔ 動任何生產碼一字**（本批為零生產碼）；**⛔ 定 `MinA_區` 之值**（其擇須經 KL）；
**⛔ 刪或停算但書式欄**（KL 裁令其保留）；**⛔ 將本節之補償數對外**（`GB-159`）。
