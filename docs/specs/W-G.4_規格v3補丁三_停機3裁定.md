# W-G.4 規格 v3 補丁三：停機③裁定（KL 2026-07-16 裁「行」）＝量子項正名·帳對幾何閘兩級化＋WIP 分支授權

> 交 CC：併入規格/plan/交辦、補記停機③報告與補丁二，commit push 後續行單次重烤。裁定溯源：KL 2026-07-16「行」＝claude.ai 裁定 1–6 全案（含 WIP 授權）。

---

## 一、勘誤（入補丁二補記·claude.ai 自認）

補丁二之「守恆-帳幾何級收口值＝`宗數×0.005`」係 **claude.ai 之維度錯**：以 G 之**面積**量子（0.005㎡）冒充 S 之**長度**量子×深度（0.005m × d(面積)/dS）——與補丁二 §三自身為守恆閘所修之 N0-17-a 錯配同型。停機③ §2 之機制推導與實測（逐宗 max|G−幾何| 六街廓貼 `0.005×深度` 上緣）經 claude.ai 獨立核驗定讞。

## 二、裁定＝(a) 量子項正名·帳對幾何閘**兩級化**（全庫一致改）

凡「帳(G) 對 幾何」之閘，一律兩級：

1. **逐宗主閘（新增·緊閘·志向所在）**：
   `|G_i − 幾何_i| ≤ 0.005×分配深度 ＋ tol(0.01) ＋ 0.005`
   依據＝S 長度捨入半量子×d(面積)/dS ＋ bisect 收斂容差 ＋ G 面積捨入半量子（三者皆已 ruled·無新常數）。任何非捨入成因之病灶於逐宗層立紅，不被街廓 Σ 淹沒。
2. **逐街廓 Σ 閘**：`|Σ(G−幾何)| = |ΣG＋池幾何−街廓| ≤ 宗數×(0.005×深度＋tol＋0.005)`（逐宗上界之和·三角不等式）。全區級＝逐街廓加總。

**落點**：`stepg:807/841`、`app.py:15341`（同構）、`run_verification:889/951/1009`（全區）、預測差量閘②（CC 於 `1794763` 之先行補正核可·由本式涵蓋）。斜交因子 ≤1.004 由 tol 項吸收，不另立項。

**(b) 駁回**（動 G、與補丁二勘誤衝突）；**(c) 駁回**（帳/實分岔於宗側復活且無閘可見、悖 §N3-0 T3）。

## 三、併裁三項

1. **E3 整形守恆閘**（`wf_f4:709`·原 0.1）→ `整形涉及宗數 × (0.005×深度 ＋ 0.005)`（同式·範圍限整形集合；R1 一宗＝0.170）。甲-2 #11 由「待歸因」改「已歸因＝量子項·本式」。
2. **N0-18 推論三補文**：「量子項須依**該捨入所作用之維度**換算：長度捨入 → × d(面積)/d(長度)＝分配深度；面積捨入 → 直取 0.005㎡。」
3. **§N3-0d「5.30↔0.07 對映由重烤實測給出」句刪**，以停機③ §1-b 更正取代：楔形 5.3255 未曾消失、錨免重量測、0.07 係第四源間隙帶挾持 `r1_frag`（`wf_f1:181-183` 取最後一筆）——挾持考古入正典案例（失敗考古編號由 CC 依序給）。

## 四、CC 補正②核可（入規格 §N3-0 T1 修文）

`_S_EPS = 2×1e-4` 立於**區間層**：依據＝T1 自身退化界 ε=1e-4（已裁·非新常數）；面積影響 ≤2e-4×深度 ≤0.009 <①' 0.01 由覆蓋閘把關且 T2-DIAG 印出；T1 回復「應永不觸發」之不變量與回歸訊號意義。§N3-0 T1 補記：「街廓 s 域端點與最外側宗切線重合＝該端本無楔形之正常情形，於區間層濾除、非 T1 事由。」

## 五、域框架（KL 已認·入 N0-18 補記）

逐宗 G（應分配）與實配幾何之差 ≤`0.005×深度`（≈0.17–0.23㎡ ≈16,600 元/宗）＝**對照清冊「增減(㎡)」欄、由差額地價找補之法定常態**——閘寬為 N0-18（公分慣例）之算術投影、非容忍誤差。幾何-幾何閘之 0.01 志向不受影響、原樣保留。

## 六、WIP 分支授權（程序令·KL 已裁）

- **`wip/*` 分支准推紅碼**；**main 維持不綠不推**（原紀律不變）。
- CC 即刻將本機 local commit（`122658d` 等）推上 `wip/W-G.4-S0b-S0c`，供 claude.ai 對真碼獨立複驗、並免機器切換再付重建成本。
- 收官仍以 main 為準；wip 分支不得作為任何「已完成」宣告之依據。

## 七、續行令

補丁三入倉 → 五處閘改寫＋E3＋§N3-0d/T1 修文 → wip 推上 → 三閘＋逐宗主閘全綠 → **單次重烤**（預測差量閘①③④⑤·①照舊絕對）→ run_all 綠 → **merge/push main** → 完工報告入 `docs/reports/`、聊天僅 ping → claude.ai 第二輪獨立複驗（scope diff＋閘值坐實＋逐宗閘抽驗）→ 丙第 2 步（KL pull 實跑錨定）。
