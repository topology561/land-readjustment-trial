# CC 交辦 v3：W-G.4（KL 二輪裁示全收·不拆波·全譜系重烤）

基準 `8ff6394`。《W-G.4_規格_v3.md》先入倉 `docs/specs/`（取代 v1.1/v2）再施工。**v2 之「R6 結構性死局→停機」條款已撤銷**（claude.ai 誤讀 F.4_公設調配 而漏讀 F.3_公設調配，已自我更正並坐實 R6 握有全案最大槓桿）。

## 偵察補查（施工前必坐實，禁假設）
1. **原生 G ＝ F.0_G值 baseline 直接可讀**（claude.ai 前版一階估算已作廢）。3.5m 刀口值：628(1)@R4左 1521.05✅／628-1(1)@R4右 456.93✅／628-36(1)@R1左 575.74✅／**628(5)@R1右 223.44 ❌短3.44**／**628-18(1)@R6右 295.59 ❌短3.54**；0m 八勝者全過。仍須以真 bisect 覆核 F.0 值於 §N1 buffer 幾何化後是否位移（R1/R6 兩端非 forced 故無 buffer，預期不動；R2/R3/R5 之 F.0 值會動）。
1b. **N0-13 回歸基線**：`F.0_合併決策` 現有決策鏈（`全達標·無須併→留置原位`＝G007@R3/G009@R4/G017@R5；級0/級0′ 併至標的宗；併後未達標→轉F.2/F.4）**已合規 N0-13，本波禁撞壞**；施工後逐列比對。
2. **F.3 targeting 全面盤點**：40 筆 PF併入／RD集中之 targeting 決策鏈；坐實 `同原地號·G較大` 規則之實作位置與適用面。
3. **可行性下界精算（方向已更正·幾何基準）**：**需求（吐 G）＝R1 +65.60、R6 +25.78，合計 91.38**；**供給（吸 G）＝R5 76.02（claude.ai 前版誤標為缺口 76.24，CC 照抄未察；R5 池 376.54>range 300.52，殘餘 76.02∈(0,MinA)，殘餘 0 須「吸」非「吐」）＋R2 ≤367.67＋R3 ≤357.98＋R4 ≤30.39**。淨核對 +65.60+25.78−76.02−15.36=0 ✅。通道：R1→R5 走 G003、R6→R5 走 G007/G022、餘 15.36 R6→R3 走 G007。全部隨 S0/§N1 重定。
4. §N1–N3 重烤後之新碎片地景 → §N5 下游錨值重定。

## ⚠️ S0 裁定：KL 裁「**選項 A（漏歸零）**」——規格 §N3-0 為裁定書本文，先讀

S0 歸因報告（`8ceacc2`）恆等式 claude.ai 簽字；聊天摘要「三者皆恆正單向漏源」為誤述（gap_union 帶減號＝負貢獻），**報告本體第 67 行正確**，以報告為準。

**S0a（補洞·擋 S0b）**：恆等式驗於 **trunk A**，漏卻觀測於 **F.4 終態**，兩者型態不同（R6 3.5m：F.4 +0.17 vs trunk A +0.767＝4.5 倍；R2 0m：F.4 +0.69 vs trunk A −0.154＝**正負相反**）。「三機制窮盡·無第四隱藏漏源」**未證於 F.4**。→ **恆等式須於 F.4 終態重驗**；第四源候選＝**F.1 等 G 遞補整形**與 **E3 `_reshape_block`**（trunk A 未跑之幾何操作）。有第四源則逐一歸因、併入裁定後再上呈。「略異」不得作為結論（4.5 倍與變號不是略異）。

**S0a 已收（`925b66e`）·claude.ai 簽字**：雙情境 12/12 殘差 0、net(sgE)≈net(CSV) 全命中（0m R2 +0.692≈+0.690）。0m「效能限」誤判 CC 已自行糾正（print key bug）——教訓採認。

**⚠️ S0a 兩項補正（claude.ai 進倉查出·CC 未察·S0b 必納）**：
1. **窮盡性有效期**：「F.1/E3 排除」之依據＝E3 整形不進 g_tab（wf_f4:711/791）——**但那正是 X2 雙真相病、正是 §N5 要修者**。§N5 令 reshape 回寫 g_tab 後 **E3 幾何即進終態、第四源成立**。→ **S0a 窮盡性僅有效至 §N5 前；恆等式須於 §N5 之後重驗一次**。
2. **病灶四處非兩處**：除 CC 找到的 `stepg:707/716/719` + `app.py:15247/15258`，尚有 **`wf_f4.py:715/716/719/721`（E3 整形段）** 與 **`wf_f1.py:322`**（註解自承「stepg 同式：buffer(0.001) 差集、≥1㎡」）。**T2 四處全改**，「精確鋪滿·無 boolean·無膨脹」原則同時適用整形段。
3. **補償碼連根拆**：`wf_f1.py:242`、`wf_f4.py:1146` 之 `u.buffer(0.0011).buffer(-0.0011)` 係對 1mm 侵蝕之形態學補償；`wf_f1.py:235` 註解逐字自承該侵蝕**已知而繞過**。T2 消滅侵蝕後補償即成**過度校正**，須拆除，不得保留。

**S0b（修漏·三條技術裁定，全文見規格 §N3-0）**：
- **T1**：`g.area >= 1.0` 廢止 → `g.buffer(-1e-4).is_empty` 寬度退化判定；真碎片一律計入池（其後歸池三則／§N5）。**T1 為 T2 之防護網，應永不觸發；觸發即 loud warn 入報告**（代表 T2 未做乾淨）。
- **T2（主修法）**：廢止 `stepg_pipeline.py:707/716/719` 之 `block.difference(allocated_union.buffer(0.001))` → **池片改用與業主宗相同之 `_block_strip` 機制、同一組切線直接切出**（街廓＝p1末筆楔形＋宗帶…＋池帶…＋宗帶…＋p2末筆楔形，精確鋪滿·無 boolean·無膨脹·無間縫）。末筆楔形＝`_block_strip(last_cut → 街廓端)`。
- **T3**：**否決**「池改帳目式」——§N1 forced＝range 為幾何量，池必須有幾何；帳目式使帳/幾何再分岔＝本病之因。
- **新硬閘**：|Σ全片幾何−街廓面積|≤0.01；任兩片交集≤1e-6；池帳−池幾何≤0.01。

**連鎖（KL 已認）**：改生產碼 → 全譜系重烤 → **G.3 三重確立須重基準（W-G 收官條件①重跑）**；舊 baseline PRE 凍存。§N3 可行性總帳與 §N5 下游錨**全數於修漏後重定**（位移量級 0.2~0.8）。

## ⚠️ S0b 追加三項（KL 裁·未納不得送審）＋新正典 N0-16

**新正典 N0-16《驗證來源分層》（規格 §N0；泛化級）** — 先讀，因它改變本波「什麼算證明」：
- **事實（白紙黑字，禁再誇大）**：`verify/app_harvest.py` 逐字「headless 取得 app.py 的**真函式（非複本）**」→ **harness 與 app.py 引擎完全同源**。PROVENANCE 之「非同源」**僅指 driver**（headless vs Streamlit），**不指引擎**。故 UI↔harness 對拍**只驗接線**（session_state／參數餵法／呼叫順序／UI 捨入），**驗不到引擎邏輯**（同碼→同 bug 兩側一起犯）。實證：buffer(0.001) 之漏兩側完全一致（net(sgE)≈net(CSV)）＝同一段碼跑兩次。`wf_f1:322`／`wf_f4:715` 為**抄寫複本**（非 AST 抽取），抄時把 bug 一併抄入＝失敗考古 #20 之根因；**抄寫式重現不構成驗證**。
- **四層權威**：① 法源 golden（手冊／法規·唯一真非同源·活例 `_pk_one_side_v12` 手冊圖8 0.6685/0.3315）→ 法定算式｜② **不變量閘**（絕對·不依賴 baseline·**幾何層唯一絕對真理**）→ 幾何實作｜③ 預測差量閘（舊 baseline＋已證差量）→ 重烤驗收｜④ 接線對拍 → **僅 UI 整合，不得當正確性證明**。
- **本波裁定**：**T2 正確性靠 ②＋③，不靠 ④**（claude.ai 前版誇大已更正）。UI 錨屬 ④，目的＝接線保證＋匯出機制之泛化基建。黃金 fixture 留泛化波（手冊 golden 不了幾何鋪滿）。

**(甲) 全庫全掃盤點（禁靠印象）**：`buffer(0.001)` 於 **app.py 實有 5 處，CC 僅點名 2 處**（另 stepg 1／wf_f1 3／wf_f4 2）——#20 墨未乾即再犯。S0b plan **須先全庫掃 `buffer(0.001)`／`buffer(0.0011)`／`area >= 1.0`／`.difference(`，逐處判「本病灶／無關用途」列表入 plan**。

**新正典 N0-17《閘寬須有原理依據》（規格 §N0；泛化級）**——**禁以「當時殘餘」定閘寬**；殘餘須被閘寬約束，不得反過來定義閘寬。閾值入倉須載明依據，限四類：**幾何精度上限／法定值／數值極限／已證物理量**。一句話測試：「**若這個數改小 10 倍會不會有東西紅？若會，它為什麼是現在這個數？**」答不出＝殘餘定閘。

**失敗考古 #21 交付（CC 撰寫入倉·素材已由 claude.ai 逐處坐實，禁採信、須自驗）**：「**以殘餘定閘寬**」——本案完整證據鏈：
1. W-C 發現白縫（真因＝Tier3 灌 G 不灌幾何·gap 精準對上 R1 1.06×5／R3 5.59×14／R6 7.79×11）→ **儀表化、命名、畫上 UI**（`app.py:15854/15856/15929/15934/15941`）。
2. W-C 修主因 → 白縫 78/86 → **<1.05㎡** → **宣告「白縫已解」**（`docs/W-C_細部plan.md:259` 逐字）。**殘餘 1.05 未歸因，只被容忍。**
3. W-D 將白縫寫進**停機條件**，閘寬 **`<1㎡`**（`docs/W-D.2_開波偵察plan:83`；`docs/W-D.1.3_細部plan:201`「白縫≈0」）。**該閘寬＝W-C 收工殘餘＋餘裕，無原理依據。**
4. **buffer(0.001) 之漏恰好躲在閘寬底下**：buf_leak 0.07~0.58／sliver 0~0.74／net 0.02~0.69，**全 <1㎡ → 永不觸發**。
5. **濾網 `area >= 1.0` 與閘寬 `<1㎡` 為同一常數** → 被丟棄之片天生撐不起觸發門檻＝**自洽盲區**。
6. **白縫從未設閘**（`grep 白縫 verify/run_all.py tests/ CLAUDE.md` → 空；僅 UI 唯讀診斷）。
7. **存活 W-C→W-D→W-F→W-G 四波**，其間**每次 run_all 全綠都在為它背書**。
8. **W-G.4 撞出它的唯一原因是 §N1 要 ±0.01（緊 100 倍）——被更嚴的新需求偶然照出，非被紀律抓出。**
與 #19（多消費端）／#20（多檔案）之別：那兩者是**搜尋範圍**問題；本條**外觀上完全合乎紀律**（有診斷／命名／UI／不變量／停機條件），**但閘是照現況畫的、不是照原理畫的**。推論：**任何「閘全綠」之效力上限＝閘寬之依據強度。**

**(甲-1) 分類更正（claude.ai 覆核 `46feb2f`）**——CC 盤點大體正確，兩處須改：
- **`app.py:15509` 由「§N5 檢視」改列「補償碼拆」**：`.buffer(0.001).buffer(-0.001)`，其上 `15470` 註解自承「防護二：buffer(0.001).buffer(-0.001) **消縫隙**」＝與 `wf_f1:242`／`wf_f4:1146` **同族**。T2 精確鋪滿後無縫可消 → 同成過度校正，**連根拆**。（其所在 Phase 8「抵費地 **<5㎡** 碎片自動合併」依 §N5＋T1 重審；`<5㎡` 亦為殘餘定閘候選。）
- **`app.py:15905-15945` 定性提升**：CC 稱「漏偵測器·正確保留」——保留正確，但**它就是 N0-17 的全部證據**（gap／offset_land重算／白縫㎡ 全都量出來畫在 UI 上而**無閘**）。→ **保留，並依 N0-17 為其設閘**（`白縫 ≤ 0.01`·依據＝幾何精度上限），**使其由診斷升為閘**。

**⚠️ N0-17 補兩條（claude.ai 裁；(甲-2) 之前提）**
- **N0-17-a 依據須與誤差來源同類·禁跨類借用**：浮點運算殘差→數值極限（~1e-9）｜地籍**實測** vs 登記→法定測量公差（㎡ 級）｜報表顯示→顯示精度（2dp）｜法定最小寬/深/面積→法定值。**電腦幾何運算沒有測量誤差，其誤差來源是浮點不是皮尺**；借測量公差容忍運算殘差＝**依據錯配**（殘餘定閘最體面之變體——它舉得出法條）。判別法延伸：「**這個誤差怎麼產生的？我引的依據管的是不是同一種誤差？**」
- **N0-17-b 散文閘**：文件中之閾值敘述**不構成閘**。任何「閘」之陳述**須指出碼中位置（檔:行）**；無碼者標 `⚠️ 散文閘·不存在`，不得作為任何正確性陳述之根據。

**✅ CC 自驗抓 claude.ai 誤·claude.ai 覆核簽字（`bd91bd0`·對稱紀律雙向兌現）**
- claude.ai 斷言「守恆<6 不存在·僅散文」與「全區守恆從未設閘」——**兩句皆錯**。CC grep 正確。
- **兩守恆閘皆真**：① 逐街廓 `abs(_resid)<1.0`（`stepg:813`／`:846`／`app.py:15341`）；② **全區 `cons_resid<6.0`（`run_verification:888/951/1009`，F.2/F.3/F.4，`results.append` bool）**。
- **claude.ai 誤因**：regex 用 `(守恆|conserv)`，變數名為 `cons_resid`（含 `cons` 不含 `conserv`）→ 天生盲目。
- **N0-17-b 舉例更正**：守恆 <6 **移出散文閘、歸入主論**（真閘·但閘寬無依據）。**散文閘正例改為白縫**——claude.ai 窮盡搜重驗（識別子：`白縫`/`uncov`/`_uncov`/`未成抵費地`/`gap=blk`/`_ba - _slot - _soff`/`offset_land重算`）：`run_verification.py` 0／`run_all.py` 0／`tests/` 0；27 處全在 `app.py:15854-15938` UI 唯讀診斷與 docs；而 `W-D.2_plan:83` 將其列**停機條件**、稱**「真不變量」**——**寫進停機條件、稱真不變量、沒有一行碼**＝教科書散文閘。辨析陷阱：`app.py:4016-4116` 之 `uncovered` 係 W-A.2 殘料歸屬（另有閾值 **`UNCOVERED_TOL`@`app.py:4112`·CC 清單原缺·(甲-2) 補**），**非白縫**。
- **(甲-2) 守恆改必立仍完全正確**，僅理由更正為「真閘·閘寬無依據」。

**⚠️ 新增 N0-17-c 否定存在斷言須附窮盡搜尋清單（claude.ai 自摘·#22·對 claude.ai 與 CC 對稱適用）**
**證明「存在」只需一行；證明「不存在」須窮盡搜尋。** 任何「碼中無 X」之斷言**須列出所搜識別子清單**（語意名＋變數名＋縮寫＋別名）與命中數；無清單者**不得作出否定存在斷言**。**縮寫是搜尋的天敵**。與 #20（找到就以為找齊）互為**反向對偶**（#20＝搜到即停；本條＝沒搜到即斷言無）。**#21 補記／#22 由 CC 撰寫入倉，素材禁採信、須自驗。**

**⚠️ 常數 1.0 三重密封（#21 補記素材）**：`area >= 1.0`（濾網·丟棄門檻）／白縫 `<1㎡`（容忍）／守恆閘 `abs(resid) < 1.0`（報警門檻）——**同一個無依據之 1.0 同時扮演丟棄門檻與報警門檻；丟棄者必然低於報警者，by construction**。非巧合疊加，係三重自洽盲區。

**⚠️ 閘寬之金額意義（域原生表述；後均價 72,058.61 元/㎡）**：全區守恆真閘 `cons_resid<6.0`＝**432,352 元**｜守恆現行 1.0㎡/街廓＝**72,059 元/街廓**｜白縫舊閘 1㎡＝72,059 元｜實測最大池漏 0.77㎡＝55,485 元｜**N0-17 新閘 0.01㎡＝721 元**。**閘寬＝「多少錢的土地可以消失而系統說綠燈」。「43 萬元以內不保證」不是可交付之產品規格。**

**(甲-2) 閾值回溯盤點（N0-17 交付·plan 必含）**：全庫盤點現存閾值，逐一補依據或改；無依據者標 `⚠️ 殘餘定閘·待歸因`。已知清單：白縫 `<1㎡`／`area >= 1.0`／`buffer(0.001)`／`buffer(0.0011)`／抵費地碎片 `<5㎡`／守恆 `<6`／E3 整形 `0.1`／bisect `tol 0.01`／T1 `ε=1e-4`。**本波必立三閘（不得暫標）**：① **白縫閘**（`app.py:15905-15945` 由診斷升閘，≤0.01）｜② **鋪滿閘**（`|Σ全片幾何−街廓|≤0.01`）｜③ **守恆閘（claude.ai 改列必立·CC 前版誤列暫標）**：(i) **全區閘 6.0 → 0.01**（`run_verification:888/951/1009`）；(ii) 逐街廓 **1.0 → 0.01**（`stepg:813`／`app.py:15341`）；(iii) **白縫閘新立於碼中**（現僅 UI 診斷＝散文閘）。三者依據皆＝**幾何精度上限**。
**守恆改必立之理由**：T2 驗收不靠守恆閘（靠預測差量閘 ③④），但**永久回歸保護靠它**。若不收緊，**S0b 修好之 0.77 明日任何一波重新引入 0.5 皆全綠——白縫正是如此活過四波。修完不收閘，只是把同一齣戲的計時器重設。**
其餘（`<5㎡`／E3 整形 `0.1`／bisect `tol 0.01`／T1 `ε=1e-4`）得暫標待歸因，**但不得再以其全綠作為正確性陳述之根據**。

**(乙) UI 匯出升級（純 UI·不碰引擎·與 T1/T2 同一 push）**：現行 Phase 7【對照清冊】骨架正確且已坐實其為 **trunk A/F.0 階段**（628(5) G=**223.44** 與 F.0 baseline 一位不差；R6 池 1634.06 vs 終態 273.35），並證 **UI↔harness 於 F.0 吻合＝「基礎引擎零漂移」此刻為真**（T2 後須重證者）。缺七項須補：① 街廓面積欄｜② 小數 2→**≥4 位**（628(5) 應配 223.44／幾何 223.45 差 0.01＝**整個閘寬**）｜③ 情境標記 0m/3.5m｜④ 階段標記 trunk A/B/E｜⑤ **終態 trunk E 匯出**（現僅吐 F.0；§N1/池三則/鋪滿閘全在終態→**F.0 與 E 兩錨都要**）｜⑥ 池片穩定 key（建議 s_rel 起訖；UI `R5-抵費地-2`＝F.4 `R5-抵費地-1`＝同片→**對拍禁以名稱為 key**）｜⑦ 池片幾何另立欄（現塞「原面積」欄、G 欄 NaN）。

**(丙) 五步錨定時序（KL 已裁選項①·順序不得顛倒）**：
1. S0b plan（含甲）→ reviewer → CC 實作 T1/T2 全處＋補償碼連根拆＋乙之匯出升級 → **push**
2. **KL pull → UI 實跑**（0m/3.5m × trunk A＋trunk E）→ 匯出＝新錨（**錨定時刻**，正典「localhost 僅視覺裁判」於此開例外）
3. harness trunk A/E 對拍新錨 → **接線保證**成立（依 N0-16 僅證 ④，非證引擎）
4. **全譜系重烤 → 預測差量閘 ①~⑤＋不變量閘全綠 → T2 正確性之證明在此步，不在第 3 步**
5. 方進 S1

**預測差量閘 ①~⑤**：① 業主宗 **G 一字不變**（絕對閘）｜② 業主宗幾何位移 ≤0.01｜③ 池幾何逐街廓 **+= 已證 net_leak**（3.5m R1+0.19/R2+0.17/R3+0.25/R4+0.02/R5+0.22/R6+0.17；0m R1+0.17/R2**+0.69**/R3+0.05/R4+0.12/R5+0.26/R6+0.17）｜④ 重算後 net_leak==0｜⑤ T1 新留 <1㎡ 片逐片可歸因。

**⚠️ 現在不得請 KL 實跑**：app.py 停於 `d4ff6d2`（2026-07-14 08:23），7/15 八個 commit（178b602→e9c1e5e）**全為文件、零生產碼**，病灶原封未動（`app.py:15247` 註解猶書「buffer(0.001) 消除浮點碎邊」）。須待第 1 步 push。

**PROVENANCE 權威條款改寫**：T2 後「app.py／stepg 推進機一字未改」即為偽 → 全部 wf/* PROVENANCE 權威條款失效。新條款＝「變更經預測差量閘逐格證明恰等於已證漏量，其餘一字不動」＋「不變量閘全綠」＋註記 N0-16。

## 施工序（單波內分段·每段閘綠再前進）
**S0a** F.4 終態恆等式重驗 → **S0b** 修漏（T1/T2）·新硬閘綠 → S1 §N1 buffer 幾何化 → S2 §N2 街角確立改寫（時序前移＋候選限原生宗＋原生 G 不動點門檻）→ S3 §N3 F.3/E1/E2 全段鐵律可行性約束＋比例調配 targeting → S4 全譜系重烤（f3 起；舊 baseline 依 PRE 凍存慣例）→ S5 §N5 下游 → S6 0m 全量重跑 → S7 §N7 十三閘入 run_all → S8 §N9 正典入倉（六個 skill 檔）。

## 已 REPL 坐實之錨（下游錨重烤後重定）
buffer：R2 buffer_S′=8.7157m→實切 309.05；R3 右 389.80→回切 308.93。
街角（原生 G＝F.0 實測刀口值）：628(1)@R4左 1521.05✅／628-1(1)@R4右 456.93✅／628-36(1)@R1左 575.74✅／**628(5)@R1右 223.44❌短3.44**／**628-18(1)@R6右 295.59❌短3.54** → 3.5m forced 擴為 5（R1右226.88、R2左309.05、R3右308.93、R5左300.52、R6右299.13）；0m 無 forced。
**新增 N0-13**（P.179，皆「應」）：達標宗應逐宗個別分配（**禁合併、禁移動**）／未達標部分應朝同歸戶「較」大者集中合併——**「較大者」＝達標宗集合（比較級非最上級）；比例調配合規**。**A/B 案型分立（判別式＝同歸戶有無達標宗；F.0 現行分流已對，禁撞壞）**：
- **A 案型**（同歸戶有達標宗、可分配於原街廓）＝N0-13：未達標宗**一定要**往該等達標宗集中合併（多筆可**比例**合併）；**本就無增配**。禁：自行合併後改走 B 路徑。倉內實例 `G009@R6 → 轉F.2（同歸戶R4有達標宗·級1相鄰街廓）`。
- **B 案型**（同歸戶無達標宗、2×ΣG≥MinA_區）＝N0-14/15：合併後往「**較淺 OR 後地價較低**」街廓（基準＝來源街廓）；**最終結果須為增配金額最少之情形**。機制：後地價低→A↓→G↑（區段 b 之 A：R1/R4 2.0429／R3/R5 1.9407／R2/R6 1.8386；區段 a：1.7364/1.6496/1.5628；b 地 R1→R6 ΔG=+0.03176·a）；較淺→同 G 下 S↑。倉內實例 `G014@R3 → 轉F.4（逃生門·2×G≥MinA_區·7-5≥½增配）`。
- **N0-15 承前既有結論，CC 須以倉內 CLAUDE.md／skill 檔為準覆核，禁重新立法。**
F.3 病灶：R6 淨流入 2528.89；G022 之 628-7(3) PF 1568.71＋628-7(4) RD 404.63＝1973.34 全額→628-7(1)@R6（原生僅 522.97）。
可行性：缺口 167.45 vs 供給 756.04（Σ池恆定 2299.26）。

## 停機上呈（§N8）＋最高紀律
E2 可行性無解／新域裁題 → 停機入倉上呈。**未 push 不得報收官**；完工報告入倉 `docs/reports/`、聊天僅 ping；KL localhost 複驗（3.5m＋0m 至 E 終態、含 hover S）後方為 W-G 收官條件③成立。
