# W-G.4 兩階段落位 plan v3（KL 裁定 C–J 全集落地計畫·**已複驗放行·施工中**）

> canonical 域裁＝`docs/specs/W-G.4_KL域裁_兩階段落位_部分面積拆分_Rw側選.md`（裁定A＋B1–B5）。
> 坐實證據＝`docs/reports/W-G.4_§4_兩階段落位_坐實.md`（R1@3.5m 推進序列 dump）。
> **可復現探針**＝`verify/probes/probe_stage_order.py`（env 開關·預設不影響 `run_all`）：
> ```
> WV_PROBE=1 python verify/probes/probe_stage_order.py --setback 3.5 --block R1 --lot "628-37(1)"
> ```
> 行號基準＝HEAD `951e347`（v1 行號已驗訖，v2 未動碼故不變）。
>
> **v2→v3**：KL 回 D-1／D-2 並增裁定 E/G/H/I；claude.ai 複驗放行並增**裁定J**（S-2 結案）
> ＋**修正一**（½線＝區內標準·非 per-block）＋**修正二**（S-1 結案＝手冊道路五原則）。
> 新增：**§二.5 裁定C**、**§二.6 裁定D 佇列（RD 依五原則）**、**§二.7 裁定E/G**、
> **§八 裁定H/J 補償計價**、**§九 裁定I**；§七 驗收靶 V1–V13；§十 backlog。
>
> **域題全結案**：D-1＝裁定C/D、D-2＝裁定F、D-3＝寬度制、D-4＝各自、S-1＝五原則、S-2＝裁定J。
> **施工紀律**：錨（COMP_EXPECT/F1_REVERIFY/六格）變動屬**波末重烤·施工期勿動**；
> **勿收綠、未 push 不報收官**；施工完成 push 後停機交 claude.ai 複驗實作。

---

## 一、現況機制（root cause 對應碼）

**單一序列混排**：`run_step_g` 把街廓內全部宗（原地主＋wf_f4 遞補合成宗 `add_syn`）一起送
`_spatial_order_parcels_v2` 依**投影位次**排序（單一序列），再以 k* 切左右兩組、兩端往中間推進，池＝殘餘：

| 環節 | 引擎（verify/） | app live 複本（#20 四處同改） |
|---|---|---|
| 投影排序 `_spatial_order_parcels_v2` | stepg_pipeline.py:382 | app.py:5956（def）／15460（call）|
| k* 選槽 `_select_pool_slot` | stepg_pipeline.py:714（orchestration 682–756）| app.py:5889（def）／15815（call）|
| 左右分組＋兩端推進 `_advance_block_with_split` | stepg_pipeline.py:486（def·分組 488–496）/525（左迴圈）/596（右迴圈）| app.py:15575 |
| `S_remain = actual_max_proj − left_cum − right_cum` | 左 :547／右 :618 | app 同構 |
| 右端 `_oblique_s_max` | stepg_pipeline.py:585 | app.py:6921（def）|
| 池片（殘餘）`_pool_strips_for_block` | stepg_pipeline.py:767 | app.py:7046（def）|
| 遞補合成宗生成 `add_syn` | wf_f4.py:178（def）；E1:442/582、E2:915/1001/1053 | —（引擎專屬）|
| E3 整形消費 `推進側別`＋`累積S(m)` 排序 | wf_f4.py:1156 | — |

**破鏈**（坐實報告）：`add_syn` 合成宗 anchor＝池片質心 → 投影落在原地主宗之間 → 單一序列**插隊**
→ 原地主末宗（3.5m 之 628-37(1)）被推至 `S_remain=8.57 <` 需 8.81 → |G−幾何|=7.9。

---

## 二、目標架構（裁定B 兩階段）

### 階段1（B1·原位次定案）
- 輸入＝**重劃前土地**（原地主宗；含 F.0 同歸戶合併之「原位次原地變寬」宗與 F.2 搬 a′ 之受宗——
  皆屬既有宗面積變動、位相不變原則既涵蓋）。
- 流程＝現行：街角第1宗（PK winner）＋其餘依 `_spatial_order_parcels_v2` 原位次、k* 選槽、兩端推進。
- 產出＝原地主宗 G/幾何**定案**＋兩側**確定後 ALLOCLINE**（左組末切線＋右組末切線）。

### 二.5 裁定C 階段1 精確化（原位次定義·D-1 前半）

**現況盤點（碼面坐實·非假設）**——裁定C 之四個構件，倉內已有其三：

| 裁定C 構件 | 現況 | 檔:行 |
|---|---|---|
| 第1宗街角地先定 | ✅ 有（PK winner→`is_first_corner_marker`·`ordered_v2` 最小擾動上位）| stepg:382／app:5956 |
| **末端塊街角地一併先定** | ⚠️ **落差**：現行末端塊 fallback 在 **wf_f4 E3 終態整形**（補丁十 N0-20），非階段1 | wf_f4.py:1161-1186（`_end_gate`／`_end_region_R`）|
| 其餘依投影正街序計 G、往中央分配 | ✅ 有（`ordered_v2` 投影序 ＋ k\* 兩端往中間推進）| stepg:382／:486／:714 |
| **未達 MinA 者移出 → 餘宗重算 G 再分配** | ✅ **已有二次配置**：合併＝標的宗 `面積_m2 += Σ(被併宗 a)`（a′ 累加器）**→ run_step_g 對 Σa 重解**；被併宗自 parcels 移除 | wf_f0.py:21（doctrine）／:98（達標判定 `G≥mina ∧ 無畸零旗標`）／:133-135（`removed` 移除）／:174 |
| 移出去向：他街廓有同歸戶→合併／無同歸戶→調配 | ✅ 有（F.0 標旗轉出 G009→F.2、G014→F.4）| wf_f0.py:22-23 |

**⇒ 唯一落差＝末端塊街角地之時機**。裁定C 要求其與第1宗街角地**同於階段1 決定**（因二者共同界定
階段1 之左右端、進而界定 B4 池範圍）；現行置於 E3（階段2 之後）→ 池範圍在階段2 期間仍會被末端塊
改動，與 B4「左右兩側**確定後** ALLOCLINE」相悖。

**改動**（P2 內）：把末端塊 gate／`_end_region_R` 之**判定與落位**自 E3 前移至階段1 尾
（`_advance_block_with_split` 定案後、池片結算前）；E3 保留**整形**職能但不再改動端部配置。
- 觸發 gate 二條件不變（無 SIDELINE ＋ 該側未臨正街半平面 >ε·side-parametrized·補丁十 §一）。
- winner 選法不變（首個 `G ≥ area(R_end)`·往後找）。
- ⚠️ 前移後 `F1_REVERIFY`（wf_f4.py:57）之 R1 楔形標的複驗錨語意須同步（屬波末重烤範圍）。

### 二.6 裁定D 階段2 佇列＝**類別優先 → 類別內距離優先**

**現行＝價值降序**（`-ginfo[g]["value"]`·wf_f4.py:518／:543／:555，value＝Σ(a×重劃前區段單價)）
——**與裁定D 不符，須改**。

**新佇列鍵**（三層·資料驅動）：
```
sort key = (類別序, 距離, 決定性 tiebreak)
類別序：0＝可建築街廓內重劃前土地之同歸戶合併   （＝裁定E 就地加寬軌·見 §二.7）
        1＝同歸戶公設地（RD 先，PF 後）          （＝現行 pub74 公設軌）
        2＝其餘（無同歸戶之調配）
距離   ：dists[(gid, blk)]（現行量法沿用·wf_f4.py:413-419
         ＝群 anchor 質心 → 該塊最大池片質心之歐氏距）
tiebreak：gid 字典序（決定性·禁擲硬幣）
```
- **同歸戶於 ≥2 街廓皆可分配 → 採距離判斷**（裁定D 括號）＝以 `dists` 取最小者；沿用現行
  `border`（`_level` 三級 相鄰/鄰近/非鄰近·wf_f4.py:422-428）作為可達性排序、**不另創**。
- **RD 先於 PF**：`類` 欄現存於 F.3 轉7-4（值域實測＝`RD`／`PF`·`F.3_轉7-4_退縮*.csv`），資料驅動可取。
- **RD 內部次序＝手冊「(一)道路用地分配原則」五原則**（S-1 結案·圖17 p.192–193·
  原文照錄於 canonical spec）。對映（**順位判準標檔:行**）：

  | 原則 | 語意 | 引擎對映 | 檔:行 |
  |---|---|---|---|
  | 1 | 道路兩側街廓皆有同歸戶地 → 以**道路中心線**為界分向兩側**合併**分配 | **就地加寬**（裁定E·不進池）·兩側各取其側之目標宗 | 中心線＝`cad.centerlines`（CLAUDE.md CAD 圖層）；併入式 wf_f4.py:356-366 |
  | 2 | 分向兩側後**有一側未達該街廓最小分配面積** → 向**另一側集中** | 該側 G<`mina[blk]` → 全額改配另一側（仍就地加寬） | `mina[blk]`（wf_f0:67）·同 :98 達標判定 |
  | 3 | 道路**一側屬公共設施用地** → 向**另一側集中** | 該側非「可建築土地」（`F3_CATEGORY_BURDEN`）→ 全額配另一側 | `ns["F3_CATEGORY_BURDEN"]`（wf_f0:68） |
  | 4 | 兩側**已無空地**或**本無土地** → **調配於其他街廓**（合議制審議）| **進池調配路**＝現行 7-4／E1 佇列 | wf_f4.py:442 起 |
  | 5 | 取得兩側所有權人**同意** → 得集中分配 | **意思決定項**·試分配自動跑不預設同意 | 同 CLAUDE.md「需合議制調配」紀律 |

  - **順位**：原則 1–3（就地/鄰側合併）**優先於** 原則 4（進池）；原則 5 屬意思決定、自動跑**不套用**。
  - ⚠️ **原則 4 之「合議制審議」＝KL 核閱項** → 走原則 4 之戶清單須列入報告（V12 併列）。
  - RD 群內同順位者仍以**距離**排序（`dists`）、tiebreak gid 字典序。
- 每筆可**部分面積**合併/調配（裁定A/D）→ 佇列元素改為 `(gid, 剩餘 a)`，允許同一 gid 多輪分次
  落位（現行 `a_rem` 已是此形·wf_f4.py:482）。

### 二.7 裁定E／G 就地加寬（跨街廓合併·不進池）

- **裁定E ＝ 現行 E0 模式一之推廣**：`a′(源) = a_src × pre_price[z源] / pre_price[z目標]`，
  於目標塊 `面積_m2 += a′` → run_step_g 以 `(a′(源)+a(目標))` 為重劃前面積重算 G
  （wf_f4.py:356-366；模式一/二雙式與附件二對映見 :368-371）。**不進池**（不生合成宗）、
  但目標塊池面積因該宗變寬而**減少** → 傳導至 B4 池範圍（P2 須在階段1 定案後、池片結算前完成）。
- **裁定G 拆分**：若就地加寬致目標塊池落入「**0 < 池 < 最小畸零**」中間態 →
  **只併裝得下之部分**（使池歸零或 ≥ 最小畸零·二擇一），**餘額改走調配軌**（依裁定D 類別序回佇列）。
  與裁定A/D-4 一致：拆出段與殘池**各自**須合法（`get_min_lot_size` 查表·禁字面常數）。
  現行 E0 為**整筆併入**（:360-362 直接 `+= ap`）→ **須加拆分分支**。

### 池範圍（B4）
- ＝左右確定後 ALLOCLINE ＋ FRONTLINE ＋ BASELINE 所圍。
- **操作化＝現成 `_pool_strips_for_block`**（app.py:7046；stepg:767 已消費）：街廓多邊形 ∩ 池 s-區間
  之帶——街廓閉合多邊形（方案B 直讀）自帶 FRONT/BASELINE 邊、帶之兩端切線＝確定後 ALLOCLINE
  （斜交 `_strip_axis` 同軸）。**零新幾何元件**。
- B4 門檻：池面積 ≥ 最小畸零地規範才進階段2；不足 → 該塊不再收池內調配（溢往下一塊，走既有
  E1 spill／E2 佇列 wf_f4.py:485/536/644）。

### 階段2（B2/B3/B5·池內調配）
- 佇列序（B2）：✅ **D-1 已由裁定D 結案 → 見 §二.6**（類別優先→類別內距離優先；
  現行 E1 之**價值降序 :518 須改**）。「同歸戶合併就地加寬」之歸屬亦由**裁定E** 結案：
  **不進池**（屬階段1 面積變動、目標塊池隨之減少）→ 見 §二.7。
- 每筆落位（B5）：
  1. 量池範圍與**左右兩側 18m Rw 範圍**之**跨占寬度**（**寬度制**·斜切軸 s-區間長·§四 D-3 結案式）；
  2. 從跨占大之一側、自池邊界**往內**切帶落位（帶軸＝`_strip_axis` 同軸；面積→G 由既有
     `solve_G_binary` 幾何 bisect，S 預算＝**池 s-區間**、非全街廓）；
  3. 落位後池縮小 → **重算兩側跨占** → 下一筆；兩側皆無跨占 → 任一側往內（決定性 tiebreak：
     取池 s-區間較長之側；再同→左，記入 spec）。
- 部分拆分（裁定A·**D-4 已裁＝各自**）：候選筆所需帶寬 > 池剩餘 s-寬 → 以面積拆分：
  池內裝滿一段、餘額回佇列溢往下一塊（既有 spill 機制）。
  **D-4 結案（claude.ai 2026-07-23·勿再上呈 KL）**：裁定A 字面「所有土地（**含剩餘調配池**）」
  ⇒ **拆出段與留池餘額「各自」**須 ≥ 可建築下限（寬/深/面積·`get_min_lot_size` 查表·禁字面常數）；
  **任一不過 → 該筆不拆、整筆溢往下一塊**，禁靜默 clamp、禁留半合法段或半合法殘池。
- 池終態＝既有池三則（0 或 ≥MinA·run_verification.py:1027-1028）不變。

---

## 三、改動計畫（檔:行·介面）

### P1 合成宗階段旗標（資料驅動·禁名稱判別）
- `wf_f4.py:178 add_syn`：parcel dict 增 `'配地階段': '池內'`；`build_parcels` 原生宗不帶此鍵（預設階段1）。
- 判別一律讀旗標；**禁**以 `74·` 名稱前綴或塊名/側別判別（泛用紀律）。

### P2 引擎 run_step_g 兩階段化（stepg_pipeline.py）
- :382 `_spatial_order_parcels_v2` 呼叫：輸入過濾＝僅階段1宗（`'配地階段' not in tp`）。
- :486 `_advance_block_with_split`：階段1 照舊（k*/兩端推進·僅原地主）。
- 新增 module 級 `_place_pool_parcels(pool_strips, stage2_parcels, side_ctx, ns, …)`：實作 §二·階段2
  （B5 側選＋池內切帶＋拆分）；於 :727（`_adv_final` 定案）之後、:767 池片結算之前呼叫；產出 rows 併入
  `g_rows`（欄位同構·`推進側別`＝落位側·`累積S(m)`＝該側池內累積，E3 排序語意相容 wf_f4.py:1156）。
- 池片結算 :767：改於階段2 落位後計（池＝階段1殘餘 − 階段2已落位）。
- W/Rw thread：✅ **D-2 已由裁定F 結案**。池內落位宗之 G：
  1. **正街臨街地負擔＝必有**（G 公式既有 S 項·無條件掛）；
  2. **側街臨接地負擔＝視該宗落位是否跨占左右側 18m Rw 範圍**——有跨占者依 G 公式以 **Rw 比率**計算
     （沿用現行 `W → rw_from_width` 機制·app.py:5123；W 為該宗至 SIDE_LINE 中點之垂距·累積差額制）；
  3. 進池者之 a′ 依**跨街廓 a′ 公式**（模式二·`a×pre_price[z]/p_avg[blk]`）。
- **與 B5 側選共用同一「池 ∩ 18m 範圍」幾何＝單一真相源**（§四 D-3 寬度制之交集）：
  B5 用其 **s-區間長**決side、裁定F 用其**是否 >0**決side街負擔有無、並以該宗實際 W 查 Rw 比率。
  ⇒ 一次交集、二處消費，禁兩套幾何。
- **telescoping 閘 W_final 定義**（stepg:848-863）：`ΣRw_側 == R(W_final) − R(W₀)`——
  W_final 改取「**該側含階段2 落位宗在內之最遠 W**」（階段1 末值 → 階段2 逐筆續推）；
  無跨占之階段2 宗不計入該側 Rw 鏈（其側街負擔=0·不推進 W_prev）。

### P3 app live 複本同改（#20 四處同改鐵律）
- app.py:15460（ordered_v2 過濾）/15575（_advance 階段1）/15815 後（呼叫同一 `_place_pool_parcels`
  ——**單一真相源**：函式放 app.py module 級、入 `_WF_NS_NAMES` harvest、stepg 經 ns 取用，同
  `_oblique_s_max`/`_corner_buffer_S` 先例；**禁 fork**）。
- ⚠️ app live 迴圈實際只跑原地主宗（合成宗僅存在於 wf_f4 引擎路徑）→ app 端改動＝過濾語意
  no-op ＋ ns 曝出；G.3 雙路同源不破。

### P4 閘與 harness（run_verification.py／stepg_pipeline.py）
- 位次=投影序閘 stepg:398-409：作用域改「階段1宗」（合成宗不再在 ordered_v2·閘語意自然收斂）。
- §N3-0 逐宗主閘 :818-832／守恆-帳幾何級 :915-959：**不動**（兩階段後池內切帶 by-construction
  |G−幾何|≤tol；此即修復驗證靶）。
- `K_STAR_EXPECT`（run_verification.py:502-505）/`GSA_EXPECT`/F.0–F.4 baseline：**行為變更→波末重烤**
  （本 plan 施工波之收官條件；本階段不烤）。
- fixture：新增池內落位單測（左右雙向＋兩側無跨占＋池<畸零＋拆分邊界），仿 `fixture_end_fallback.py`
  真檢（非 tautology）。

### P5 wf_f4 消費端
- `_Eng.rows()`（wf_f4.py:143-145）：合成宗 rows 由階段2 產出（推進側別∈{left,right} 保持）→ 介面不變。
- E1 `_reach`/E2 `_e2_optimal`（:502/:873）：改讀階段2 後之真池剩餘；E2 成本帶 `POS_SLACK_AREA`
  假設（位置相依 G 誤差上界）於池內落位下重驗——band-sufficiency 閘（既有）破則 loud。
- E3 `_reshape_block`（:1156 起）：排序鍵 `累積S(m)` 語意相容（P2 保證）；末端塊 fallback（補丁十）不動。

---

## 四、B5「跨占寬度」＝**寬度制（D-3 已裁結案·claude.ai 2026-07-23）**

> **裁定＝(a) 寬度制**。理由：KL 原話「占 Rw 18m 範圍有 **2m 寬／4m 寬**」為**公尺寬度**、非面積。
> 量測軸＝引擎既有**斜切軸** `_strip_axis`（app.py:6851），與 S 維同軸（純技術決定）。
> **勿再上呈 KL。**

**落地式**：
```
跨占寬度(側) = len_s( 池範圍 ∩ 該側18m負擔範圍多邊形 )      # s＝_strip_axis 斜切軸座標
             = s_max(交集) − s_min(交集)                    # 與 S_remain 同維、可逐筆遞減
```
- 18m 負擔範圍多邊形＝`_build_corner_range_v2(side_mid, verts, centroid, alloc, 18.0, None)`
  （app.py:14332 左／14337 右·W-C §2·v3.1 §5·不扣截角）。
- **純加性曝出**：現行僅存面積（session `f3_burden_range_18m`·app.py:14341/14356）→ 需另存
  **多邊形**於 session（新鍵，如 `f3_burden_range_18m_poly`）並入 `_WF_NS_NAMES` 供引擎 ns 取用；
  **勿改既有面積鍵**（既有消費者 app.py:16409 不受影響）。
- 邊界：該側無 SIDE_LINE ⇒ 無 18m 範圍 ⇒ 跨占＝0（B5「兩側皆無跨占→任一側往內」涵蓋）。
- **決定性 tiebreak**（泛用·禁擲硬幣）：兩側跨占相等或皆 0 → 取池 s-區間較長之側；再同 → 左。

**gap 佐證（保留備查·claude.ai 窮舉搜尋複驗屬實）**——倉內既有、且**皆非**本量法：
- 18m 負擔範圍多邊形雖可由 `_build_corner_range_v2(…,18.0,None)` 生（app.py:14332/14337），
  惟 session `f3_burden_range_18m`（app.py:14341/14356）**只存面積**、多邊形未保留、引擎 ns 未曝。
- 「跨占」既有語意＝街角 PK **跨占面積**（app.py:14497·宗∩街角範圍·面積制）與 W-D.4 跨占分配線
  （wd4_tier_list.py:297）——**皆非**「池×Rw範圍 跨占寬度」。
- 倉內**唯一**池交集碼＝app.py:7164 之池-宗**不得重疊**閘（斷言性質），非量法。
- ⇒ 本量法確為新增；已由 D-3 裁定 (a) 寬度制定案（上式），**非自創**。

---

## 五、影響面、風險、回退

- **行為變更**：合成宗 G/位置全數重定（F.3/F.4 六表、G值表、池片、E3 整形、33 群總決算）→
  F.0–F.2 理論零動（僅原地主；閘驗證）、F.3/F.4 baseline 波末重烤。UI（app）零動（P3 no-op）。
- **風險**：
  1. E2 窮舉最優性假設（位置相依成本帶）與池內落位交互——band-sufficiency 閘看守，破即停機上呈；
  2. 拆分產生之段須合法基地——每步查表驗證，不過即整筆溢（禁半合法段）；
  3. Rw/telescoping 語意（D-2）未裁前 W 鏈續用之暫行——閘若破即停機；
  4. 池 s-區間非單片（如 R1 雙片 0.32/35.28）——階段2 只在 ≥最小畸零之片內落位、小片留池（既有
     池三則語意）；
  5. 邊界：N=0 階段2 佇列／池恰＝MinA／兩側皆無跨占——fixture 全覆蓋。
- **回退**：兩階段為**單一碼路**（舊單序列路徑**整刪**·no-silent-fallback·禁雙路徑並存）；
  回退＝git revert 施工 commit（原子波·一 commit 一機制）。

## 六、域確認題狀態

| # | 題 | 狀態 |
|---|---|---|
| **D-1** | 階段2 佇列序（含就地加寬歸屬）| ✅ **結案＝裁定C／D／E**（KL 2026-07-23·§二.5/§二.6/§二.7）|
| **D-2** | 池內調配宗計否 Rw 側街負擔 | ✅ **結案＝裁定F**（正街必有；側街視跨占·Rw 比率·§三 P2）|
| **D-3** | 跨占量法 | ✅ **結案＝(a) 寬度制**（claude.ai 2026-07-23·§四）|
| **D-4** | 拆分段最小合法性 | ✅ **結案＝各自**（拆出段與留池餘額皆須合法·§二）|

**上呈項（皆已結案·2026-07-23）**：

| # | 題 | 性質 | 影響 |
|---|---|---|---|
| **S-1** | 手冊「道路用地分配原則」章節頁次 | ✅ **結案**（claude.ai 2026-07-23）＝**圖17·p.192–193·五原則**，原文照錄於 canonical spec；對映見 §二.6 |
| **S-2** | 公設用地之「原位置評定重劃後地價」認定 | ✅ **結案＝裁定J**（KL 2026-07-23）＝**公設地後價 `_wavg_post`**（即原提案(甲)）→ **無資料缺口·補償計價葉解除 gate**、可施工 |

**施工放行**：✅ claude.ai 已複驗放行（2026-07-23·裁定J＋修正一二併入後）→
**P1–P5 一路做完**（plan→reviewer→施工→push），施工完成 push 後停機、交 claude.ai 複驗實作
（by-construction·grep 檔:行＋探針重跑）。施工期：勿動錨、勿烤、勿收綠、未 push 不報收官。

---

## 七、修復驗收靶（施工後複驗·明列）

| # | 靶 | 判準 | 量法／出處 |
|---|---|---|---|
| V1 | **主症解除** | 628-37(1)@3.5m `|G−幾何| ≤ _acct_geom_tol_per_lot`（post-S0d 0.015）**且** 其 `S_remain` 足額（≥ 所需帶寬，非踩 0.1 下限） | 探針 `probe_stage_order.py --setback 3.5`；§N3-0 逐宗主閘 stepg:818-832 |
| V2 | **階段1 零動** | F.0／F.1／F.2 六表逐格 **zero diff**（僅原地主宗參與階段1，遞補移出序列不得回頭改動原位次結果）| `run_verification.py` F.0/F.2 diff_rows |
| V3 | **守恆全綠** | §N3-0 逐宗主閘（:818-832）＋守恆-帳幾何級（:915-959）＋①' 覆蓋閘 全綠、無 raise | run_all |
| V4 | **池三則不變** | 各塊池終態 `0 或 ≥MinA`（無中間態）| `run_verification.py:1027-1028` |
| V5 | **位次序不變** | 階段1 宗之 `pre_position ≡ _projection_order`；F.0 位次序/毗鄰閘綠 | stepg:398-409、F.0 `pos_viol`/`adj_viol` |
| V6 | **telescoping** | ΣRw_側 == R(W_final) − R(W₀) 逐側綠（D-2 裁定後定 W_final 語意）| stepg:848-863 |
| V7 | **邊界 fixture 真檢** | 新 fixture 覆蓋：① 兩側皆無跨占 ② 池 < 最小畸零（不進階段2）③ 拆分邊界（拆出段/殘池各自合法·D-4）④ 池 s-區間**非單片**（如 R1 之 0.32/35.28）⑤ 左右雙向對稱。**須真檢**（構造已知答案手算對拍），**非 tautology**（前車：`fixture_end_fallback.py` 舊「4項恆等式」因 end_abate 相消而空轉·reviewer 揭）| 仿 `verify/fixture_end_fallback.py` |
| V8 | **E2 最優性未破** | band-sufficiency 閘綠、`_e2_optimal` 可行數 >0 且 opt ≤ 次優 | `run_verification.py:1029-1035` |
| V9 | **雙路同源** | G.3 app-path == 重烤 baseline（P3 為 no-op ＋ ns 曝出，須實證未破）| wg_g3 |
| V10 | **重烤後全綠** | F.3/F.4 baseline 重烤後 `run_all` ALL GREEN（收官條件·**須 push 進 origin 方得報收官**）| `run_all.py` |

| V11 | **裁定H 逐筆化零差**（重構正確性）| 24 筆 7-4 母體全屬 PF/RD → 逐筆價皆＝`_wavg_post` ⇒ `Σᵢ(aᵢ×pᵢ) ≡ (Σaᵢ)×wavg`：**UC9898 補償金額對現行零變動**（`comp_amounts` 逐戶相等）。**另**手算對照 **3 戶**（取 `comp_groups` 三戶·逐筆 aᵢ×pᵢ 明細入報告） | §八；wf_f4.py:468/478/827 |
| V12 | **裁定I 翻轉戶清單**（供 KL 核閱）| **單因**（修正一：½線口徑不變·仍為 MinA_區）：列出**僅因「續試他可達塊」**由「<½ 現金補償」改「配地」之歸戶，含各戶 ½ 判定塊與 2×G(a′) vs MinA_區。**併列**走手冊道路原則 4（進池調配·合議制審議）之戶清單 | §九；wf_f4.py:451-458 |
| V13 | **裁定C/E/G 結構靶** | 末端塊街角地於**階段1**即定案（E3 不再改端部配置）；就地加寬**不生合成宗**且目標塊池同步減少；裁定G 拆分後池∈{0, ≥最小畸零}（無中間態）| §二.5/§二.7 |

> ⚠️ V1–V9、V11–V13 為**施工波**驗收；V10 之重烤**本階段不做**（勿烤 baseline）。
> **錨變動預期**（勿先動·屬波末重烤）：`COMP_EXPECT`（wf_f4.py:56）因裁定I 預期改變；
> `F1_REVERIFY`（:57）因末端塊前移語意須同步。

---

## 八、裁定H／J 補償計價逐筆化（**資料盤點訖·裁定J 結案·無缺口·可施工**）

### 8.1 現行（＝KL 判定之錯誤實作）
```
wavg = _wavg_post(snap)            # wf_f4.py:300 ＝公設地後價（面積加權均價）
comp = _money(gi["a"] * wavg)      # :468   ＝Σa（全戶彙總）× 單一全區價
```
另 :478（events 文字）、:826-827（anchors `wavg`／`comp_amounts`）同源，須一併改。

### 8.2 資料源盤點（實測·非假設）

| 原位置 | 重劃後地價來源 | 倉內是否存在 |
|---|---|---|
| 可建築街廓 Rᵢ | `財務接線_v3.後街廓_面積單價[Rᵢ].單價_元每m2` | ✅ **有**（R1 76245.977／R2 68621.380／R3 72433.678／R4 76245.977／R5 72433.678／R6 68621.380）|
| 公設用地 RDᵢ／PFᵢ（Gᵢ）| **公設地後價**＝`_wavg_post`＝可供分配建築街廓面積加權平均（**第 11 號公報 P.4·KL 裁定 2026-07-11**·wf_f4.py:78-88）＝72058.610 | ✅ **有**（既有正典；且裁定H 明示「`_wavg_post` 於公設地計價之原用途不動」）|

**本案 24 筆 7-4 母體之原位置實測**：`類` 值域＝**PF 17 筆／RD 7 筆**（雙情境同·
`verify/baselines/wf/f3/F.3_轉7-4_退縮{0m,3.5m}.csv`），**無一坐落 R 塊**。
9 群＝G003/G012/G013/G015/G016/G024/G025/G028/G031。

**⇒ 無硬資料缺口**：R 塊價與公設地價**皆已存在**，裁定H 可完全落地、無須新增地價資料。

### 8.3 改動
- 新增 `_post_price_at(原位置)`（module 級·單一真相源·入 ns）：
  `原位置 ∈ R塊 → 後街廓_面積單價[blk]`；`原位置 ∈ RD/PF/G → _wavg_post(snap)`。
- `comp` 改逐筆：`comp = Σᵢ _money-前累加( aᵢ × _post_price_at(原位置ᵢ) )`，末端一次 `_money` 捨入
  （避免逐筆捨入累積偏差；與現行單次捨入慣例一致）。
- 原位置判別**資料驅動**：優先取該公設筆之 `類`（RD/PF）＋`所屬街廓`（wf_f4.py:388 `tp["所屬街廓"]`）；
  **禁**以地號字串猜測。跨價區者依裁定H「面積加權拆算」——本案 `ginfo` 已強制單一 zone
  （:392-394 跨 zone 即 raise），故本案不觸；泛用碼仍須支援。
- **UC9898 數值預期**：因 24 筆全屬公設用地、單價同為 `_wavg_post`，
  `Σᵢ(aᵢ×wavg) ≡ (Σaᵢ)×wavg` → **補償金額零變動**。⇒ 可作**重構正確性零差靶**（V11）。
  金額之實質變動只在「原位置跨 R 塊」情形出現（本案無、泛用有）。

### 8.4 ✅ S-2 結案＝裁定J（KL 2026-07-23）
**裁定J**：坐落公設用地之重劃前土地，其「原位置評定重劃後地價」＝**公設地後價 `_wavg_post`**
（第 11 號公報 P.4·KL 2026-07-11 既有正典）＝原提案 (甲)。
⇒ **補償計價葉解除 gate·無資料缺口·價源函數直接落地**（如 8.3）。
本案 24 筆全為公設用地 → 金額恰零變動 → **V11 重構零差靶生效**；
逐筆結構與 per-位置價源乃**泛用要件**（原位置跨 R 塊之案即有實差）。跨價區者面積加權拆算。

---

## 九、裁定I ½線判定塊（「可達街廓中最有利者」）

### 9.1 現行（＝僅最近塊）
```
near_any = {gid: sorted(mina, key=lambda b: (dists[(gid,b)], b))[0] for gid in ginfo}   # :451
G0,_,_   = _trial(gid, near_any[gid], ginfo[gid]["a"])                                  # :454
half_r0[gid] = (G0, 2*G0 >= MINA_QU)                                                     # :456
comp_groups  = {g for g,(_,ok) in half_r0.items() if not ok}                             # :457
```

### 9.2 改動（裁定I）
```
對每 gid：依可達序 border[gid]（_level 相鄰→鄰近→非鄰近·再距離·:427-428）逐塊試 _trial；
  任一塊 2×G(a′) ≥ **MinA_區**（＝min(mina.values())·**區內標準·非該塊 mina[blk]**）
    → 判「過½」、**配該塊**（記錄該塊為 ½ 判定塊）、停止續試；
  全部可達塊皆不過 → 落 <½ 現金補償軌。
```
- **可達街廓**＝公設軌無原街廓 → **全可建築塊**（既有 doctrine·wf_f4.py:644-645／:879／:897 手冊(三)）。
- **½線口徑＝重劃「區內」最小分配面積標準（修正一·claude.ai 2026-07-23·KL 已閱未異議）**：
  裁定I **未**含「改 per-block」。KL 原話與手冊 :3589 皆為「重劃**區內**最小分配面積標準」，
  倉內正典閘已認證區內口徑（`run_verification.py:654` MinA_區==114.07 推導正典·:701 reverse-test
  隨動非鎖死）。手冊「該街廓最小分配面積」係**配地可行性**口徑（E1/E2 之 `mina[blk]` 現用·**續用**），
  與 ½ 線**補償**口徑係二事、**勿混**。
  ⇒ ½ 線續用**區內標準**；本波硬編修正＝把 `wf_f4.MINA_QU` 之**字面 114.07** 改為
  **引用 MinA_區 推導正典**（`min(mina.values())`·`mina[lbl]=round(深度×min_width,2)`·
  單一真相源同 `wf_f0._mina_by_block:67`／`wd4_tier_list._mina_by_block:72`），**非**換 per-block。
  ⇒ **V12 簡化為單因**（僅「續試他可達塊」致翻轉）。

  > 🔒 **二口徑永久分明（勿再混）**：
  > **(甲) 配地可行性口徑＝各街廓 `mina[blk]`**（E1/E2 落位是否可行、手冊「該街廓最小分配面積」、
  >   wf_f0:98/:204/:211/:216、§二.6 原則2）——**本波零動**。
  > **(乙) ½線補償口徑＝區內標準 `MinA_區`＝`min(mina.values())`**（手冊 :3589「重劃**區內**
  >   最小分配面積標準」·run_verification:654 正典閘／:701 reverse-test 隨動）——本波僅去字面硬編。
  > 凡新碼引用 MinA 前，先標明係 (甲) 或 (乙)。
- `_trial` 為 probe（撞牆回 None）→ None 視為該塊不可容納、續試下一塊（非判不過½）。
- **與 E1 配地輪銜接**：過½者以其 ½ 判定塊為**首選塊**進入配地輪（`requests`）；
  該塊當輪不可行時，回退依 `border` 續找（現行配地輪邏輯不變）。
- **與 E2 銜接**：仍不可行者照現行 `spill_75` → E2 第2梯類增配佇列（:536／:644）。

### 9.3 錨之預期變動（**勿先動·屬波末重烤**）
- `COMP_EXPECT = {"G013","G024","G028"}`（:56）**預期會變小或清空**（部分群改配地）。
- 連動：`exit_rows` 之「<½ 現金補償」列數、`comp_amounts`（:827）、E5 33 群總決算、
  F.4 六表 baseline。**本階段勿改錨、勿烤**；施工波末重烤時一次更新並附翻轉戶清單（V12）。

---

## 十、波後 backlog（本波不處理·僅登錄）

- **泛用化 watch（claude.ai 必辦④）**：`app.py:17649` 之 §7 引擎 UI 文案自陳
  「**67 項案例錨定斷言**（六格 G(Σa)、SNAP_WAVG、楔形面積錨）」凍結於 UC9898、
  非 UC9898 資料即 `_is_uc9898` 圍欄擋跑——與本專案「**泛用化為本**」紀律有張力。
  現況為 KL 既有裁定（「引擎不得為 UI 妥協」·列泛化波 backlog），本波**不動**；
  惟兩階段落位新碼**須自始 side-agnostic／資料驅動、零新增案例錨**，勿加深此債。
- 探針常設化：`verify/probes/` 往後凡作為裁定依據之實證，探針一律入倉附重跑指令
  （claude.ai 必辦①確立之驗證紀律）。
- **本案常數硬編 → 波後改資料驅動**（claude.ai 2026-07-23 追加；本波**勿動**，錨隨重烤更新除外）：
  - `MINA_QU = 114.07`（wf_f4.py:54）＝MinA_區寫死。**本波已解**：改 `mina_qu = min(mina.values())`
    推導正典（**非** per-block）→ :458/:676 二處退場後 :54 成孤兒、同步刪。
    **本波不動**：`:471`／`:1074` CSV 欄名字串 `"2×G vs 114.07"`（改欄名＝baseline 欄位改名＝
    重烤假紅、污染 V11 零差靶；本案 `min(mina.values())==114.07` 欄名仍為真）＋`:20` docstring
    → **欄名參數化列波後 backlog**。
  - `COMP_EXPECT = {"G013","G024","G028"}`（:56）＝本案具名錨；同 `E0_EXPECT`(:57)／`F1_REVERIFY`(:57)／
    `E2_NAMED`(:58)／`SNAP_WAVG`(:55)——皆屬 UC9898 案例錨，與 app.py:17649 所述「67 項案例錨定斷言」同族。
  - 上述與「泛用化為本」之張力，統一列**泛化波 backlog**；新碼**禁**再增案例錨。
