# PROVENANCE｜宗地分配線邏輯（`.cnt` ／ `.lin`）

**入倉批號**：`W-G.9-40`（2026-08-16）

## §一 來源

| 欄 | 內容 |
|---|---|
| **來源** | **KL** |
| **送達日** | **2026-08-16** |
| **送達途徑** | **對話側 `@` 附件** |
| **入倉檔** | `宗地分配線邏輯.cnt`／`.lin`；`宗地分配線邏輯_2.cnt`／`.lin` |

🛑 **`.jpg` 二檔待補件**：施工單 `D-1` 開六檔，KL 本次以 `@` 送達者為 **`.cnt`／`.lin` 四檔**；
依該條「⛔ 禁編造、禁以倉內近似物代替、**禁自行至本機取用**」，⛔ **未取用**同名 `.jpg`。

## §二 元資料（構造證·量測框 ＝ **byte**）

| 檔 | 來源 | working tree | **blob** | 判 |
|---|---:|---:|---:|---|
| `宗地分配線邏輯.cnt` | 1073 | 1073 | **1073** | ✅ |
| `宗地分配線邏輯.lin` | 588 | 588 | **588** | ✅ |
| `宗地分配線邏輯_2.cnt` | 518 | 518 | **518** | ✅ |
| `宗地分配線邏輯_2.lin` | 252 | 252 | **252** | ✅ |

`.gitattributes` **已有** `*.cnt -text`／`*.lin -text`（`:33-34`·`W-G.9-34` 立）⇒ ⛔ 未重複追加。
🔒 **判別力自檢**：`git hash-object -w --path=` 對同一來源檔套不同副檔名 ⇒
`probe.cnt`／`probe.lin` **1073**；`probe.txt`／`probe.md` **1044**（＝1073−29 CRLF）
⇒ 該式**非恆真**、條款確實生效。

## §三 拓樸（本圖之權威部分·由座標自行導出）

**圖一 `宗地分配線邏輯`（29 點／28 線）**——完整多宗示意：

| 群 | 單位方向 | 段數 | 語意 |
|---|---|---:|---|
| 正街（道路境界線） | `(0.000000, 1.000000)` | 7 | `x = 183.089` |
| **最小深度垂線** | `(1.000000, −0.000000)` | **5** | ⊥正街·每宗一條 |
| 後側境界線（BASELINE） | `≈(0.2059, 0.9786)` | 5 | 同一直線 |
| ALLOCLINE | `≈(0.8707, 0.4918)` | 5 | 各宗遠側界·互相平行 |
| SIDELINE | `≈(0.9859, 0.1673)` | 3 | 含截角段 |
| 截角斜邊 | `(0.577334, 0.816508)` | 1 | — |

**圖二 `宗地分配線邏輯_2`（14 點／12 線）**——**情形乙**之示意（`P2_temp`）：

| 群 | 單位方向 | 段數 |
|---|---|---:|
| 正街 | `≈(0.0152, −0.9999)` | 4 |
| — | `≈(0.1849, −0.9828)` | 2 |
| — | `≈(0.8170, −0.5766)` | 3 |
| — | 其餘 | 3 |

## §四 🔴 權威射程

> 本圖為**拓樸**之權威（何線平行於何線、何處為臨界位置），
> ⛔ **非度量之權威** ⇒ ⛔ **不得作為數值對拍之 fixture**。

**倉內正典落點**：`docs/rulings/K-6_街角地分配程序與可分配判準.md` 之 **`K-9-9`**。
