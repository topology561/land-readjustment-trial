# PROVENANCE｜最小深度示意圖＿座標（`.cnt` ／ `.lin`）

**入倉批號**：`W-G.9-34`（2026-08-15）

---

## §一 來源

| 欄 | 內容 |
|---|---|
| **來源** | **KL** |
| **送達日** | **2026-08-15** |
| **送達途徑** | **對話側**（⛔ 倉內無從覆核其送達過程） |
| **產生系統** | KL 之繪圖系統（座標顯示精度＝**小數點後第 3 位**·見 `K-6` 之 KL 裁 2026-08-15 ④ 原則性依據） |
| **入倉檔名** | `最小深度示意圖＿座標.cnt`／`最小深度示意圖＿座標.lin` |

⚠️ **檔名之全形底線**：本二檔之分隔符為**全形底線 `＿`**（U+FF3F·UTF-8 `EF BC BF`），
⛔ **非**半形 `_`。施工單所載為半形，經現查**與實體不符** ⇒ 依「⛔ 不採信本單·一律自行現查」
以**實體檔名為準**入倉、⛔ 未改名。

---

## §二 檔案元資料（現查實測·量測器已自檢）

| 檔 | 位元組 | CRLF | 孤立 CR | 孤立 LF | 點／線數 |
|---|---:|---:|---:|---:|---|
| `最小深度示意圖＿座標.cnt` | **703** | **19** | 0 | 0 | 19 點 |
| `最小深度示意圖＿座標.lin` | **378** | **18** | 0 | 0 | 18 線 |

🔒 **量測框**：上表之「位元組」係 **byte**（⛔ 非字元、⛔ 非行）。

🔒 **量測器自檢**（`W-G.9-34`·⛔ 先自檢後量測）：以人造已知檔
`a|CRLF|b|CRLF|c|CRLF|d|LF|e|LF|f|CR|g` 自檢，期望值**逐字元手推**
（7 可見字元 ＋ 3×2 ＋ 2×1 ＋ 1×1 ＝ **16** bytes／CRLF 3／孤立 CR 1／孤立 LF 2），
實測全符 ⇒ 量測器可用。
🩸 **首次自檢曾 FAIL**：我把期望位元組誤推為 17（實為 16）——**錯在期望值、⛔ 非量測器**；
三個 EOL 計數當時即全對。已具名，⛔ 未以「改期望值使其通過」了事（期望值係手推後確認）。
判別力自檢：對純 LF 之人造檔量得 CRLF ＝ **0** ⇒ 該計數器**非恆定**。

---

## §三 行尾之凍結（`.gitattributes`）

本二副檔名已於倉根 `.gitattributes` 加上**同型**條款（案由與選定理由沿用 `GB-17`／`*.dxf`）：

```
*.cnt -text
*.lin -text
```

**構造證**（`W-G.9-34` 實測）：

| 檔 | 來源 | working tree | **blob** | 判 |
|---|---:|---:|---:|---|
| `.cnt` | 703 | 703 | **703** | ✅ 三者相同 ⇒ ⛔ 未被行尾正規化 |
| `.lin` | 378 | 378 | **378** | ✅ 三者相同 ⇒ ⛔ 未被行尾正規化 |

🔒 **判別力自檢**（⛔ 證「三者相同」非恆真）：以 `git hash-object -w --path=<X>` 對**同一來源檔**
套用不同副檔名之 attributes：

| `--path` 副檔名 | blob 位元組 | 判 |
|---|---:|---|
| `probe.cnt`／`probe.lin`／`probe.dxf` | **703** | 未正規化（條款生效） |
| `probe.tmpx`／`probe.txt`／`probe.md` | **684** | 🔴 已正規化（恰 ＝ 703 − 19 CRLF） |

⇒ 該式**非恆真**；若無本條款，二檔入庫即少 19／18 位元組、校驗碼失準。
🩸 **首版自檢曾壞**：漏 `-w` 致 blob 未寫入物件庫，`git cat-file -s` 回空值而被我讀成「已正規化」
——空值有多義，**先疑量測器**。已加 `-w` 重跑，⛔ 未沿用壞掉那版之結論。

---

## §四 🔴 權威射程（B-4·⛔ 照抄施工單逐字）

> 本圖為**拓樸**之權威（何線平行於何線、何處為臨界位置），
> ⛔ **非度量之權威**：圖面自身存在大於 1e-3 之不精確
> ⇒ ⛔ **不得作為 1e-3 級數值對拍之 fixture**。

**其不精確之逐筆實測**（`W-G.9-34` 自量·⛔ 未採信施工單之任何數字）：

| # | 受詞 | 應然 | 實然 | **差** |
|---|---|---|---|---:|
| d1 | 點 `id=3`（`Pw`·最小深度垂線之臨正街端） | 落在正街 `x = 183.089` 上 | `x = 183.044` | **0.045000** |
| d2 | `Ps`（`id=7`）與 `Pw`（`id=3`）之距離 | — | — | **0.055803**（Δx `0.045000`／Δy `−0.033000`） |
| d3 | 後側境界線三段（`13→5`／`5→2`／`2→14`）之共線性 | 同一直線 ⇒ 偏移量差 `0` | 偏移量 `−65.497580`／`−65.495748`／`−65.495667` | **0.001913** |
| d4 | SIDELINE 二段（`15→10`／`10→16`）之共線性 | 同一直線 ⇒ 偏移量差 `0` | `13.014358`／`13.009095` | **0.005262** |
| d5 | ALLOCLINE（虛）二段（`5→19`／`19→6`）之共線性 | 同一直線 ⇒ 偏移量差 `0` | `9.230413`／`9.228500` | **0.001912** |

⇒ **最大不精確 ＝ `0.055803`**（d2）> `1e-3` ⇒ **上開射程限制成立**。

---

## §五 拓樸（本圖之權威部分·由座標自行導出·⛔ 未採信施工單）

**元素識別**（依共線分群 ＋ 角色推導）：

| 角色 | 點 id | 座標 |
|---|---|---|
| `P0`（SIDELINE ∩ 正街·未截角） | `17`／`15` | `(183.089, 44.261)` |
| 截角落點（SIDELINE 上） | `10` | `(167.604, 41.634)` |
| `Ps`（街角第 1 宗遠側界 ∩ 正街） | `7` | `(183.089, 72.662)` |
| `Pw`（最小深度垂線 ∩ 正街） | `3` | `(183.044, 72.695)` |
| `B0`（SIDELINE ∩ 後側境界線） | `13`／`16` | `(72.289, 25.461)` |
| `B1`（第 1 宗遠側界 ∩ 後側境界線） | `5`／`8` | `(78.486, 54.913)` |
| `B2`（最小深度垂線 ∩ 後側境界線） | `2`／`4` | `(82.228, 72.695)` |
| ALLOCLINE（虛·過 `B1`）∩ 正街 | `6` | `(183.089, 113.973)` |
| ALLOCLINE（實·過 `B2`）∩ 正街 | `1` | `(183.089, 129.642)` |
| ALLOCLINE（虛）∩ 最小深度垂線 | `19` | `(109.981, 72.695)` |

**方向分組**（單位向量·號已正規化·六位小數）：

| 群 | 單位向量 | 段 | 語意 |
|---|---|---|---|
| 正街（道路境界線） | `(0.000000, 1.000000)` | 5 段 | `x = 183.089` |
| 最小深度垂線 | `(1.000000, −0.000000)` | 2 段 | `y = 72.695` |
| 後側境界線 | `≈(0.206, 0.979)` | 3 段 | 同一直線 |
| SIDELINE ／ 第 1 宗遠側界 | `≈(0.986, 0.167)` | 3 段 | 二條**互相平行**之線 |
| ALLOCLINE（虛·實） | `≈(0.871, 0.492)` | 3 段 | 二條**互相平行**之線 |
| 截角斜邊 | `(0.577334, 0.816508)` | 1 段 | `10 → 9` |
| 街廓端邊 | `(0.999979, 0.006410)` | 1 段 | `12 → 11` |

🔒 **垂直性**（本圖之核心拓樸主張）：最小深度垂線與正街之夾角實測 ＝ **`90.000000000000°`**
（`dot(u_正街, u_垂線) = 0`·至浮點精度）⇒ **垂直性成立**。

🔒 **第 2 宗之投影重疊**（沿正街方向）：臨正街段 `[72.662, 129.642]`／臨後側段 `[54.913, 72.695]`
⇒ 重疊 ＝ **`0.033000`**。該值即 d2 之 Δy ⇒ **係圖面不精確之顯現**，理想圖上二段應恰首尾相接（重疊 0）。
