# CC 交辦：W-G.4 終態幾何消解波

基準：`origin/main` HEAD `d4ff6d2`。規格全文見隨附《終態幾何消解波_規格.md》（請先入倉 `docs/specs/` 再施工）。KL 三題裁示已鎖為 ground truth。

## 節奏（照舊）
偵察（禁假設）→ plan → reviewer → 實作 → run_all 綠 → push（`git rev-parse origin/main` 含 commit 方可報收官）→ 完工報告入倉 `docs/reports/`、聊天僅 ping。

## 偵察重點（先做，禁跳）
1. F.1「等 G 遞補整形」為何 0m 真做、3.5m 未執行——找出分歧點（規格 §1.3 為修補目標）。
2. `solve_G_binary` / `_block_strip` 現行呼叫鏈中 baseline_pt 與 S_front 的取得路徑，評估 §1.5 三處修改之最小侵入面。
3. R2/R1/R6 三處錨點值（75.31 / 5.30 / 85.66 / 309.05 / 367.67 / 273.35 / 776.72）逐一以 REPL 對快照坐實，禁心算。

## 施工項（規格 §2 表列 8 項）
1. 街角補足（§1.2 來源順序）：R2 錨點案。
2. 細碎片消解（§1.3）：R1-抵費地-2、R6-抵費地-2；帳進池、形由鄰宗等 G 滑動吞。
3. 末筆非街角第1宗規定範圍（§1.4）＋末筆 G–S 二分求解（§1.5，含 S_front）。
4. 抵費地 hover 顯示 S（§1.7）。
5. 0m 情境同機制全量重跑（§1.6）。
6. 正典入倉（規格 §5，skill 檔擴充）。

## 硬閘（規格 §3 全文照抄入 run_all 新閘；違反即停）
守恆／位次不變／街角第1宗／末筆第1宗 G≥Z／池三則 ∉(0,MinA)／等 G 逐宗不變／合併 Σa 重解禁直加／no-silent-fallback（末筆求解失敗 loud raise，禁退代數 fallback）／155 閘＋新閘 ALL GREEN。

## KL 覆核結果（全裁，放行施工）
- **X1＝B 案**（規格 §1.5-bis 鎖定）：末筆第 1 宗 a 不變、G 依 §1.5 二分重算（S_front 換 S_guess）、ΔG 由同 Ri 池吸收；一般滑動宗仍等 G 不變。錨點 #3 與硬閘 6 已隨改。
- **X2＝收編**（規格 §1.8）：消解與末筆整形回寫終態 g_tab＋F.4 帳證；`reshape_polys` 降級中間產物；`_reshape_block` helper 收編、「只整形不記帳」廢止。
- **X3＝規格筆誤已正**：正街項＝S_front·l₂＝code `l_front`；code 配對正確不動。
- §1.4-3 檢查順序＝嚴格投影順序、§1.4-4 皆 G<Z→抵費地充當（=Z）：KL 未推翻，即鎖定。

規格 v1.1 為施工唯一依據（先入倉覆蓋 v1.0）。plan → reviewer → 實作 → run_all 綠 → push → 報告入倉。未 push 不得報收官。

## Escalate 條件
不變量破壞、規格缺口、任何新域裁題（含 R2-抵費地-1 調後 367.67 若 < MinA 之續調鏈實際觸發時的順位歧義）。

最高紀律重申：**未 push 不得報收官**。
