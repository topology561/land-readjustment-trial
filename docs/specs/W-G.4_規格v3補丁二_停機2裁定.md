# W-G.4 規格 v3 補丁二：停機②裁定（KL 2026-07-16 晚裁）＝(b) S0c 提前＋N0-19 池片定義

> 交 CC：併入 `docs/specs/W-G.4_規格_v3.md` 與 `W-G.4_S0b_plan.md`，並補記兩份停機報告，commit 後自 §9 中斷點續行（S0c 已插入）。裁定溯源：KL 2026-07-16「行」＝claude.ai 裁定文 1–6 全案核可。

---

## 一、重大勘誤（先於一切·入停機①報告補記）

停機①報告（`ee5f844`）之「(b) 必破預測差量閘①（G 一字不變）、全譜系 G 重定」**係誤判**。碼證（claude.ai 逐碼驗·現倉）：
- `app.py:6792`（收斂）`G_conv = G_target`；`6803`（未收斂）`G_conv = last_G_target` → **G 為財務目標值、非幾何量**。
- 回傳 `'S'` 已捨入（`app.py:6836`）、推進本就吃捨入值（stepg:541/547/632/638）。
- 故 **S0c＝實切改用 `round(S_conv,2)`** 之影響面：**G 欄不動、S 欄不動、下一宗 baseline 不動**；僅 final_cut 幾何與 area_geom 變動，每宗 ≤ 0.005×分配深度。
- **預測差量閘①全程照常有效**（S0c 亦不豁免）；重錨範圍縮小為幾何欄（閘③⑤）。補丁一之「S0c 屬 G 重定、閘①於 S0c 豁免並重立」**依此作廢**。

## 二、裁定＝(b) S0c 提前併入·單次重烤

- S0c 插入 §9 現中斷點（step 4 前）。內容：
  1. `solve_G_binary` 實切（`app.py:6821`）改用 `round(S_conv, 2)`；
  2. **area_geom 改取實切（捨入 S）之幾何面積**（帳實一致；乙-2 之幾何分配 4dp 匯出即匯真值）；
  3. 四同構點自然同源，無另改。
- S0c 後即重驗：①' 覆蓋、②-池、**②-宗圍堵閘收緊 ≤1e-6**（原裁定之收口提前兌現）、守恆-帳幾何級（圍堵界項歸零 → 閘寬收為 `宗數×0.005`）。
- **(a) 間隙側圍堵閘不立**：S0c 後相鄰宗共用同一條捨入切線，縫與疊 by construction 同時歸零——為即將消滅之現象立閘＝死碼。**Σ 診斷保留**印出縫/疊量，作 S0c 收斂之量測證據（應歸 0；不歸 0 即另有病、停機）。
- 續行 F.1/F.4/T1/補償拆/乙匯出 → **單次重烤**（錨只重定一次）。

## 三、新正典 N0-19（池片定義·KL 落章）

- **原子＝s-帶**：以同一組（捨入後）切線分界之帶，§N3-0 T2 明文語意。
- **池片＝相鄰池 s-帶之極大聯集**（maximal run of consecutive pool s-bands）。
- **身分鍵＝s 區間、出生即定**（與乙-6 s_rel key、與「側身分出生即存」正典同構）。
- **連通分量降級為舊 boolean 實作之 artifact**，不再作為片之定義。
- §N4「最大池片」／§N5 碎片消解／N0-6 池三則之「片」皆依此重述（規格內逐處改寫）。

## 四、F.1 錨與預授權停機點

- `WEDGE_AREA_ANCHOR`（wf_f1:31/196）等**單次重烤後重量測、中心值重定**；5.30↔0.07 之對映由重烤實測給出，不做讀碼考古。
- **預授權停機點**：若重烤後 F.1 之語意前提（存在待吞之 R1 楔形片）不成立 → 停機上呈 KL 域裁「R1 整形是否仍需要」。

## 五、其他

- 停機②報告（`4168319`）補記：裁定＝(b)＋N0-19（KL 2026-07-16）；閘式部分照案入倉（已核可）。
- 施工序（修訂後 §9）：S0c 兩行修＋area_geom → 三閘＋圍堵 1e-6 重驗＋Σ診斷歸零 → F.1（錨重量測）→ F.4 → T1/補償拆/乙匯出 → 單次重烤（預測差量閘①③④⑤·①照舊絕對）→ 守恆閘依補丁一之三收緊 → push → 報告入倉。
