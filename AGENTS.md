# AGENTS.md — real-estate-app 專案總則（指標檔）

> **本檔不另訂規則。** 權威規則、鐵律與驗收基準一律以 **`CLAUDE.md`** 為準。
> 本檔僅供依 `AGENTS.md` 慣例讀取專案規則之 agent 導向；**任何與 `CLAUDE.md` 衝突之敘述，
> 以 `CLAUDE.md` 為準**，並回報 KL。

## 📍 請改讀以下檔案（依權威序）

| 序 | 檔案 | 內容 |
|---|---|---|
| 1 | **`docs/rulings/K-6_街角地分配程序與可分配判準.md`** | **裁定正典**（K-6／~~K-7~~／K-8 §一〜§五-2／**K-9 §一〜§五**）。⚠️ **K-7 已於 2026-08-01 整條撤銷·由 K-9 取代**（原文存查於該檔考古段） |
| 2 | **`CLAUDE.md`** | 專案總則、工作紀律、核心鐵律、目前進度 |
| 3 | `.claude/skills/` | 常駐技能規則（驗證流程／失敗考古／停機條件／禁靜默兜底 等） |
| 4 | `docs/配地計算總規格_v3.md` | 完整規格（部分章節已由裁定正典取代，見其檔頭註記） |
| 5 | 104 年市地重劃手冊 | 法源。v3 與手冊衝突時以手冊為準，並回報 KL |

## 為何本檔只是指標

本檔曾為 `CLAUDE.md` 的完整副本。2026-07-31 實測：184 行中 **147 行與 `CLAUDE.md` 逐字相同、
僅 6 行為本檔獨有**，且內容停留在約 2026-07-13、**未同步 N 系列與 K-4〜K-8 全部裁定**，
其中包含一條**已於 K-8 §五-2 退場的驗收基準**（`R5 左側街角規定範圍 ≈ 146.50㎡`）。

該檔當時**未進版控**（`.gitignore` 未排除、只是從未 commit），因此其漂移
**不會出現在任何 diff、review 或報告中**——只是碰巧被一次窮盡 grep 撞到。

⇒ **改為指標檔並納入版控**，使「兩份會各自漂移的權威檔」這個結構性風險消失。

## 最低限度硬規則（即使不往下讀，這幾條也必須遵守）

- **🔒 未 push 不得報收官**：任何「收官／完成」宣稱之前置硬條件＝
  `git rev-parse origin/<當前工作分支>` 已含該 commit。
  現行工作分支＝**`wip/s1-endpart`**（⛔ **不是 `main`**）。**不綠不推、不推不報。**
- **🔒 plan 不是交付物**，不得只交 plan 不交碼。
- **🔒 偵察禁假設**：動工前先量現況；**任何「僅／只有／不存在／必然」之宣稱，
  先跑窮盡 grep 並貼出輸出**。
- **🔒 行號是易腐資產**：引用他處一律用**符號名 ＋ 可自癒 grep 指令**；
  非用行號不可者，同列記下該行號成立之 commit。
- **🔒 禁靜默兜底**：缺值一律 loud raise 或警示，不得以案件值（如 `or 3.5`）編造。
- **🔒 只有真觸及域邊界**（法規解釋、土地分配後果、面積歸屬）**才停機上呈**；
  純技術項一路 plan → reviewer → 實作 → 量測 → push 做完，不要回問。
- **🔒 倉內報告＝唯一本體**：報告本體寫 `docs/reports/W-*.md` 並 commit；聊天僅為 ping。

## 三方分工

- **KL**：域裁定者（市地重劃／花蓮縣畸零地使用規則）、UI 操作者。裁定具約束力。
- **claude.ai**：獨立規格／驗證／裁判層。讀**已 push** 之 commit、由構造驗證、寫施工單。**不寫生產碼。**
- **實作 agent（Claude Code／其他）**：照施工單施工，**push 後才回報**。
